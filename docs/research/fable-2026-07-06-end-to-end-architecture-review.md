# End-to-End Architecture and Backlog Review - Machinery Is Outrunning Judgment

**Date:** 2026-07-06 · **Author:** Fable 5 (dispatched via Claude Code Agent tool) · **Type:** research/review, read-only
**Scope:** full-system pass - live codebase and DB state, shipped v3.0/v3.1 build (through Phase 142B.1 and the Phase 143 plan/replan), the complete forward backlog (idea catalog, priority matrix, ROADMAP Phases 143-154, 33 pending todos), reconciled against the six prior Fable reviews (2026-07-01 through 2026-07-04). Every checkable claim verified against live DB (`psql`), `systemctl`, `config_history`, or file:line - not doc prose.

**Verdict up front:** the epistemology still holds and the build quality of what shipped this week is genuinely good - the Phase 143 replan responded correctly to cross-AI review, the shared IC kernel landed, the concept-registry cluster synced within 24 hours of its review, and EIC-04's FAIL was recorded honestly instead of negotiated. What has broken is the *ratio*: since 2026-07-02 the project has produced two complete phases of measurement machinery, one fully planned phase, six review documents, and **zero new empirical verdicts**. The E1/E2 A/B judgment has never run (`ensemble_weights` still holds only `weight_version='v1'`, 103 rows); the ensemble has never been trained on the two timeframes (1h/1d) that are cost-viable, while 97.8% of `alpha_events` sit in the 5m band already found cost-marginal - behind a cost gate that is still a no-op because the cost-hurdle calibration that three planning docs now describe as "closed" was in fact never executed (`alpha.quant.cost_hurdle.* = 0.0`, zero calibration rows in `config_history`). Meanwhile the ROADMAP still routes the path to the actual endgame (personal live capital, v4.0) through v3.2/AnalogEngine, a milestone the project's own backlog matrix rates LOW/speculative/XL. The single most valuable thing this project can do next is not another build or review: it is running the judgments its machinery was built to produce, in the order the backfill-forced fourth corpus rebuild dictates, and re-gating v4.0 on Phase 147's two gates instead of on v3.2.

---

## 1. Findings

### F1. Verdict starvation: every empirical judgment the last three phases were built to produce is still unexecuted. [HIGH]

The core value is "alpha must be demonstrated empirically." The demonstrations are all idle:

- **E1/E2 A/B judgment (Phase 142B.1's entire point): never run.** Verified 2026-07-06: `ensemble_weights` contains exactly one `weight_version` (`v1`, 103 rows) - no `v1_shrunk`, no `v2_mv` rows anywhere; `alpha.ensemble.weight_method` is still `ic_proportional`. 142B.1 completed 2026-07-04; two days later the machinery it shipped has produced no verdict. (E1's *input* shrinkage did clear its own out-of-fold gate and flip `alpha.ensemble.ic_input=ic_shrunk` on 2026-07-03 - `config_history`: "E1 out-of-fold gate PASSED: 3160 cells, mean_shrunk_error=0.045612 < mean_raw_error=0.046824" - but no trainer run has consumed it, so live weights predate even E1.)
- **1h/1d have never been trained, ever.** Verified with `ensemble_trainer.py`'s own eligibility filter (`symbol='POOLED' AND is_pooled=true AND regime != '_pooled' AND ic_ci_lower>0 AND passes_fdr AND reliable AND feature_status_at_eval='active'`): 1h has **15** qualifying features and 1d has **28**, yet `ensemble_weights` has rows only for 5m (60) and 15m (43). Consequence: `alpha_events` = 344,514 rows, of which 336,958 (97.8%) are 5m and 7,556 are 15m - the two TFs todo 030's Step 0 found net-negative-to-marginal after external costs. The system emits exclusively in the band it believes is cost-marginal and has never once emitted in the bands it believes are cost-viable. The 2026-07-04 EIC-04 deep-dive found this; it remains true today.
- **EIC-04 remains FAIL with a one-cell measurement surface.** `alpha_ensemble_ic`: 572 rows, every one of them `(5m, high_bear)`, scored 2026-07-03/04; 20/572 have `ic_ci_lower > 0`. The whole "ensemble output has IC" question - the primary Gate 1 input for Phase 147 - currently rests on a single (tf, regime) cell.
- **Cost-hurdle calibration: never executed** (F2, below - it is severe enough to be its own finding).

Contrast with what *was* produced in the same window (git log 07-04 to 07-06): Phase 143 research + validation + 3 plans + cross-AI review + replan, this review being the seventh research doc in six days. None of that moves any gate. The critical path is now operator runs, not builds, and the remediation plan (`should-we-back-fill-nested-peacock.md`, in progress - backfill fetch loop confirmed running, `market_data_ohlcv` now at 80 symbols vs `feature_vectors` at 58) already sequences them correctly: backfill → full corpus re-run (which exercises 1h/1d training for the first time as a side effect) → EIC-04/EIC-05 → E1/E2 judgment.

**Fix:** declare a build-and-review moratorium on new phases/reviews until the remediation plan's steps 3-8 have produced verdicts (with the two exceptions in F4 that must land *before* the rebuild's compute stages). The one steady-state guard worth adopting: any phase whose deliverable is measurement machinery gets a companion "first verdict produced" checklist item, so "COMPLETE" can never again mean "built but never run" (this is the third instance of that pattern: 142A/EIC-04, 142B.1/A-B, 141.1/cost calibration).

### F2. The cost-hurdle gate is documented as calibrated and is not - a no-op gate the doc ecosystem now believes is closed. [HIGH]

Verified 2026-07-06:

- `config_state`: `alpha.quant.cost_hurdle.{5m,15m,1h,1d}` are all **0.0**.
- `config_history`: **zero** rows with `changed_by='cost_hurdle_calibration'` (the script's audited write path) - the full `changed_by` distribution contains no cost-hurdle write from any source.
- The script (`scripts/ops/corpus/ops_cost_hurdle_calibration.py`) exists, is tested, and unconditionally writes Step-1 percentile hurdles when run live. It has never been run live.

The 141.1 VERIFICATION report was honest about this (Truth 6: "Live APR values ... remain 0.0 today - this is expected and explicitly scoped ... an operator step run after Phase B completes"). But Phase B completed 2026-07-01 and the operator step never happened, while the *closure language* propagated: STATE.md B2 says "Empirical calibration (cost hurdle, threshold validation, gap contamination check) - done, todo 030 closed 2026-07-02"; PROJECT.md and ROADMAP's 141.1 entries say "cost-hurdle calibration (todo 030 Steps 0-3)" as a completed deliverable; todo 030 sits in `todos/completed/` with no closure note; and the 2026-07-03 roadmap-reconciliation review's F2 asserts outright "the `alpha.quant.cost_hurdle.*` keys are **now calibrated (141.1)**" and builds its Phase 142B net-of-cost-reporting recommendation on that false premise. This is the bottom-up audit's Exec §5 finding (no-op cost gate) not merely unfixed but *re-labeled as fixed* - the exact silent-wrong-answer class the design mindset forbids, occurring in the governance layer instead of the code.

Concretely load-bearing: Phase 142B's SHADOW-REVIEW.md criteria freeze at launch, and the reconciliation's (correct) instruction to decide gross-vs-net reporting "using the calibrated keys" will, as things stand, apply hurdles of 0.0 and report gross P&L as net.

**Fix:** (1) run `ops_cost_hurdle_calibration.py` live as an explicit step in the remediation plan, after the fourth-rebuild corpus lands (running it against the current corpus and again after would also be fine - it is one hour); (2) correct the four documents above now (STATE B2, PROJECT 141.1, ROADMAP 141.1, and an erratum note on reconciliation F2); (3) move todo 030 back to pending or fold its execution into the remediation checklist - a todo is not complete when its deliverable is a script that has never produced its numbers.

### F3. The road to the endgame runs through the backlog's own lowest-rated milestone: v4.0 is hard-gated on v3.2/AnalogEngine. [HIGH]

ROADMAP milestone list, line 31: "**v4.0 Execution Layer** - hard-gated on **v3.2 complete** ... + `alpha_events` schema frozen." Against that:

- PROJECT.md's Core Value (confirmed 2026-07-05): personal live trading capital; "nothing sizes a live trade until the alpha behind it has survived out-of-sample."
- `roadmap-scope-map.md` (re-ranked 2026-07-05 against exactly that endgame) puts **Trade Construction (v4.0) at #2**, explicitly "the literal next step after edge is proven."
- The backlog matrix rates AnalogEngine **LOW / XL effort / High risk / "Speculative"**, and ROADMAP Phases 148-149 are themselves flagged stale (todo 055) pending a rewrite against intel-13.

So the dependency chain to the only milestone that touches the endgame is: 142B → 143 → 146 → 147 (two gates) → **v3.15 (144, 145)** → **v3.2 (148, 149, 150)** → v4.0. Everything from v3.15 onward in that chain exists to *sharpen or diversify* the signal, not to prove it or to trade it. If Phase 147's Gate 1 (OOS ensemble IC) and Gate 2 (OOS counterfactual P&L per pre-committed SHADOW-REVIEW criteria) both pass, the requirement standing between the system and its stated purpose is position sizing, minimum risk controls, and paper/shadow execution - none of which needs a K-NN retrieval substrate. Musk step 1 applied to the milestone graph: the requirement "diversify before deploying" was never interrogated; it is inherited from the Renaissance more-weak-signals doctrine, which is a portfolio-quality argument, not a deployment-precondition argument - especially when v4.0 itself ships shadow-first (paper fills) per this project's own promotion discipline, making early v4.0 the *cheapest possible validator* of everything upstream.

Note what this does **not** change: (a) the v3.1 exit gates stay exactly as hard as they are - no capital moves before 147's two gates pass; (b) Phase 144 (`regime_group`) keeps standalone value independent of AnalogEngine - it fixes live measurement contamination (15 of 58 symbols mislabeled today, and worse post-expansion: the 22 newly-backfilled instruments include UUP/FXE/FXY/FXA/DBA/DBB/DBC/PPLT, i.e. FX and commodity funds that will be silently IC-stratified under equity labels until 144 lands); (c) v3.15's role as Phase 148's prerequisite survives - it just stops being on the v4.0 path.

**Fix:** re-gate v4.0 on "Phase 147 Gates 1+2 pass + SHADOW-REVIEW criteria met + `alpha_events` schema frozen." Move v3.2 (and v3.15's Phase-148-prerequisite role) to opportunistic/parallel post-deployment diversification, keeping Phase 144 sequenced early on measurement-correctness grounds. Execute todo 059 (mine AegisAgent/TradeAgent for risk-overlay and execution-vehicle reuse) as v4.0 pre-planning - it is currently the only backlog item pointed at scope-map priority #2, and nothing gates it.

### F4. The backfill-forced fourth corpus rebuild is the batching window, and two items are mis-batched against it - including a work package the ROADMAP currently assigns to two different phases. [MEDIUM-HIGH]

The project's own doctrine (topdown D5; 2026-07-01 review §6) is to batch label-invalidating changes into one re-run. The fourth rebuild is now scheduled (remediation plan step 3, after the running backfill), and:

1. **todo 026 P2b/P2c/P3 is double-assigned.** ROADMAP v3.15 milestone text: "Batches together in one `ic_engine` re-run per topdown D5: Phase 144 + **todo 026 P2b/P2c/P3** + todo 041 + intel-12's first substitution test." ROADMAP Phase 143 LIFECYCLE-00: "What's left: **P2b ... P2c ... P3**" - and 143-01-PLAN.md implements P2b/P2c now (P3 deferred to manual calibration). Same three items, claimed by two phases in two milestones. Whichever executes first must strip them from the other, or v3.15's batch plan silently shrinks without anyone deciding it.
2. **143-01 should execute before the rebuild's regime stage, not after.** P2c adds the `hmm_churn` column, populated by `regime_writer`. The rebuild runs `regime_writer` over the full 80-symbol, 20-year corpus anyway (the expensive HMM pass). If 143-01 lands first, `hmm_churn` is populated for free in that pass; if it lands after, it requires a second full regime pass over the deepest corpus this project has ever had. The backfill fetch is still running - the window is open now. The same logic applies to P2b's degenerate-model occupation gate: it exists precisely to guard a big refit, and the fourth rebuild *is* the big refit.
3. **Phase 144 should stay out of the rebuild** - deliberately. The rebuild changes the corpus (deeper history + 22 new symbols); adding a regime-label change in the same pass confounds the pre/post comparison Phase 144's own sequencing note demands ("land the ensemble-IC baseline first ... empirical pre/post comparison over blind trust"). 144's own change surface (`market_regimes` + ic_engine stratification) needs only an ic_engine-scale re-run, which is cheap relative to a full rebuild. Current sequencing (144 in v3.15, after) is right; this finding just closes the "why not batch it too" question explicitly so the batching doctrine isn't misapplied.
4. **The EIC-04 gate-weighting decision must be pre-committed before the post-rebuild re-run.** Remediation plan item 9 correctly found the gate unweighted-averages `POOLED` with 49 thin per-symbol cells, and that the one well-powered cell is a genuine null. Deciding how to weight/split the gate *after* seeing the fresh verdict is post-hoc gate renegotiation - exactly what SHADOW-REVIEW-style pre-commitment exists to prevent. Decide it now, log it in `methodology-change-ledger.md`, then re-run.

**Fix:** execute 143-01 (Wave 1 only) before remediation step 3's regime stage; amend v3.15's batch text to "residual 026 items post-143" in the same commit; add the EIC-04 gate-weighting decision + ledger entry to the remediation checklist ahead of the re-run; leave Phase 144 as a separate post-baseline ic_engine re-run.

### F5. Gate-evaluation record-keeping is one design decision away from a fourth adjacent mechanism: Phase 147's `gate_evaluations` table duplicates Phase 143's `integrity_monitor`. [MEDIUM]

Angle-2 answer, verified across the current specs. What exists or is planned:

- `feature_transition_log` (live) - authoritative *transition* record, one row per real status change. Correct, single-purpose.
- `integrity_monitor` (Phase 143, migration 204 planned; `to_regclass` NULL today) - *gate-evaluation facts*: monitor_type, metric vs threshold, pass/fail, one row per run including no-transition runs. intel-14 designed it as the shared table for drift (151), lifecycle (143), ensemble health (152), and todo 036's regime-model monitor. Correct consolidation - the 143-RESEARCH.md alternatives table explicitly rejected conflating it with the transition log, for the right cardinality reason.
- `concept_transition_log` + `concept_gate` last-eval cache (todo 058) - governance grain, different table family by design (registry state is not rebuildable; measurement facts are). Justified separation.
- **Phase 147 SCORE-02/03: "Binary pass/fail written to a `gate_evaluations` audit log with timestamp, gate_id, result, and evidence JSON."** That is the `integrity_monitor` row schema, restated - metric evaluated against a threshold, pass/fail, evidence, per run. A second gate-fact table with a different name, specced before `integrity_monitor` existed (147's text predates the intel-14 consolidation and was never swept).

So the fourth mechanism has not formed yet, but it is written into a planned phase and will form by default the day 147 is planned. One table with `monitor_type='oos_gate'` rows serves SCORE-02/03 exactly, keeps "did any gate fire this run" a single query across lifecycle/drift/OOS gates, and inherits 143's idempotency key design for free.

**Fix:** one-line ROADMAP edit to Phase 147 SCORE-02/03: replace `gate_evaluations` with `integrity_monitor` rows (`monitor_type='oos_gate'`, subject = gate_id, evidence JSONB); note it in intel-14's table section. Do it now while it is a text change, not a migration.

### F6. The Canonical Truth Registry - the enforcement instrument for the sole-writer invariant - was silently abandoned on 2026-06-21, and is now missing every table the v3.x build has added since. [MEDIUM]

`docs/foundation/canonical-truth-registry.md` ("any new table ... must either appear here or explicitly declare that it is a derived projection"): Last Updated 2026-06-21. Verified gaps against the live schema:

- **Missing entirely:** `market_regimes` (writer: `equity_regime_model.py`), `ensemble_weights`/`ensemble_alpha` (writer: `ensemble_trainer.py`, sole-writer status just made load-bearing by Phase 143's LIFECYCLE-01 correction), `alpha_events` (writer: `alpha_publisher`, a CLAUDE.md-level invariant not recorded here), `alpha_ensemble_ic` (writer: `EnsembleICEngine`), `feature_registry`/`feature_transition_log`, `backfill_status`.
- **Actively wrong:** the `feature_ic_scores` row says "`AlphaDecayMonitor` (Phase 139) writes `is_decaying` flag only" - a writer that was never built and never will be (intel-14/Phase 143 rewrote it as an ic_engine hook), for columns 143-02 now *drops*. The roll-events row says "nightly timer" (all timers confirmed disabled). The SLA note still names `signal_ledger_full` as the canonical read surface (renamed `signal_ledger` in Phase 130). The regime row says "HMM Viterbi" (live decode is causal forward-filter; `regime_label_source='forward_filter'` is a recorded key decision).
- **About to be needed:** Phase 143 makes `ic_engine` the first *automated* writer to `feature_registry` (operator/migrations were the only writers before); `integrity_monitor` will have per-`monitor_type` writers (143's hook, then 151/152/036 modules). Neither ownership will exist anywhere if the registry stays frozen.

This matters more than doc hygiene: the whole angle-3 audit below (F9) is only mechanically checkable because I re-derived ownership from code. The registry is the artifact that makes second-writer violations reviewable at design time; it stopped being that three phases ago.

**Fix:** one hygiene pass adding the eight missing rows and correcting the four stale ones; add "update canonical-truth-registry" as an explicit task to 143-03 (integrity_monitor row) and todo 058 (concept_* rows). Cheap insurance: a pre-commit check that a new `CREATE TABLE` migration mentions the registry file in the same commit (sibling of the existing Check 9).

### F7. The bottom-up audit's ghost-daemon findings are still live four days later, and nothing in the 33-item pending backlog tracks them. [MEDIUM]

Verified via `systemctl` 2026-07-06: `indicagent-feature-vector-pipeline.service` **active (running)** - still computing and publishing feature vectors to a topic whose only consumer (`feature_vector_writer`) still has no systemd unit anywhere (bottomup Exec §2, "publishes into the void"); `ctx-writer` and `lineage-writer` still **active (running)** against tables with 0 rows (bottomup Exec §3); one unit still `loaded failed`. Grep over `.planning/todos/pending/`: no todo covers any of this (061 is the adjacent-but-different DDL-in-hot-path item). Under the 2026-07-05 re-rank that promoted Platform/Infrastructure to #3 ("real capital raises the cost of a silent bug"), three daemons burning CPU into the void and a permanently-failed unit are exactly the noise floor that hides a real failure when one matters.

**Fix:** capture one todo: stop `ctx-writer`/`lineage-writer`, decide feature-vector-pipeline's fate (install the writer unit if live v3 vectors are wanted before v4.0, else stop the pipeline daemon too), remove or mask the failed unit, and prune `_DAG_ORDER` so `service_auditor` describes the fleet that exists (bottomup §2.4's aspirational-registry problem).

### F8. Doc-currency drift has reopened across the backlog surface, including one inverted load-bearing "key decision." [MEDIUM]

Per the catalog's and matrix's own maintenance rules, checked against what shipped 07-03 → 07-06:

- **STATE.md is stale again**, two days after its own embedded correction complained about exactly this: frontmatter `last_updated: 2026-07-04`, `status: ready_to_plan`; no record that Phase 143 was planned (07-05, 3 plans), cross-AI reviewed, and replanned (07-06); "Current focus" still sequences the E1/E2 judgment as immediately runnable, superseded by the remediation plan's post-rebuild sequencing.
- **STATE.md Key Decisions carries an inverted invariant:** "Pooled IC (is_pooled=true) - diagnostic only; all ensemble reads filter WHERE is_pooled = false." Live code is the opposite: `ensemble_trainer.py` reads `WHERE symbol = 'POOLED' AND is_pooled = true AND regime != '_pooled'` (lines 317, 430-431, 469, 540) - the semantics changed when cross-sectional POOLED strata became the eligibility source (Phase A / 141.1 regime_scope work) and the "don't re-derive" note was never updated. MEMORY.md's Phase-138 section repeats the stale version ("Phase 139 reads WHERE is_pooled = false"). A future session trusting the note would filter the training population to zero rows.
- **`roadmap-scope-map.md`** (07-05): "Phase 142B.1 - four candidate ensemble-weighting mechanisms (E1-E4) **being A/B judged**" - none has been judged (F1).
- **Backlog matrix** (last real refresh 07-02 evening): 142A row lacks the EIC-04 FAIL verdict; 142B.1 row says "now has a phase plan" (complete since 07-04, judgment pending); 143 row predates its planning. The matrix is the declared "read this first for what's next" doc for Cluster 1, so its staleness is not cosmetic.
- **Idea catalog** (07-03): execution-plan row still "Phase A COMPLETE, Phase B next" (B done 07-01); ETF-expansion row "backfill currently running" now ambiguously points at the *wrong* backfill (the expansion backfill finished; the depth backfill is what runs today - the exact conflation memory `feedback_multiple_concurrent_backfills` warns about).
- **Todo 057** (phase-renumbering sweep across consumer docs) remains pending; the 07-04 review's F9 inventory is still the work list.

**Fix:** one sweep commit: STATE.md refresh (frontmatter, 143 status, is_pooled correction), memory-file is_pooled correction, scope-map "being A/B judged" → "machinery shipped, judgment pending", matrix refresh (three rows + EIC-04 verdict), catalog two rows, then execute todo 057.

### F9. Whole-system DAG/writer audit of the forward backlog: no live violation, four watch items to pin at planning time. [LOW-MEDIUM]

Angle-3 answer. Nothing currently proposed creates an undeclared second writer to a sole-writer table, but four spots will, if planned casually:

1. **`alpha_frames` (Phase 142B) is specced with two writers** - topdown §2.6 lists "writers: AlphaFrameWriter/CounterfactualTracker." Frame-open INSERT vs outcome-close UPDATE is a defensible split only if column ownership is declared (tracker touches outcome columns only, writer never updates). Pin it in the 142B P1 migration comment and the truth registry row, or collapse to one writer with two entry points.
2. **intel-13's analog predictor columns on `feature_vectors`** would add a fourth column-partitioned writer (after feature factory, `regime_writer`, `forward_return_writer`) to the corpus's most load-bearing table. The sibling-table option intel-13/topdown already allow (`analog_scores`, keyed identically, one writer) is the invariant-clean choice; prefer it at Phase 149 planning.
3. **Phase 143 makes `ic_engine` the first automated `feature_registry` writer** via `FeatureRegistryService.record_transition_sync` - correct per intel-14 invariant 1, but the registry row (F6) must name it, and todo 058's item 7 (route `domain='feature'` through `concept_registry` if 058 ships first) would change the answer; the ordering check intel-14 OQ3 specifies must actually run at build time.
4. **`integrity_monitor` is deliberately multi-writer by `monitor_type`** (143 hook now; 151/152/036 modules later). Fine - but that is a *partitioned* ownership model this codebase has no precedent for, and it should be stated as such (one writer per monitor_type, enforced by the idempotency UNIQUE key already in 143-03's migration design) rather than discovered.

Also checked and clean: Phase 150 interaction columns (feature-factory-written), Phase 144 (`market_regimes` single dispatcher), 142B.1's variants (all through `ensemble_trainer`), Kafka `alpha.events` (publisher remains sole writer; still zero consumers, which is dead transport but not a violation).

### F10. Phase 146/147's premise rot (todo 056) is now blocking by implication: the v3.1 finish line is longer on paper than in fact. [LOW-MEDIUM]

Nothing new found beyond todo 056 and reconciliation F3 - re-verified still unapplied: Phase 146's gate cites CORPUS-07, which no completed phase owns (Phase 141 shipped CORPUS-01..06); topdown D7 pre-committed retirement-by-default; v2.x is dead in fact (its retirement half of 147 describes disabling an already-failed unit). Under F3's re-gating, 146/147 become the *entire* remaining distance between a passing v3.1 and v4.0 planning, so the rescope stops being cosmetic: as written, 146 can never fire its own gate, and a planner reading 147's Depends-on ("`i7_conversion_complete = 1`") would build conversion infrastructure D7 already deleted. The blocking operator call (archive vs delete v2.x; decommission before or after the proof gate) has been pending since 07-03.

**Fix:** make the operator call and apply todo 056's sketch. Fifteen minutes of decision, one ROADMAP edit, and the path to the endgame loses a phantom phase.

---

## 2. What's Solid (verified - do not touch)

- **The v3.1 spine and its gates.** 142B blocked on an honestly-recorded EIC-04 FAIL rather than a negotiated pass; SHADOW-REVIEW pre-commitment intact; two-gate promotion model untouched by anything found here. The 2026-07-03 verdict log plus the 2026-07-04 correction entry is exactly how a Renaissance shop should record an inconvenient result.
- **Phase 143's plan set post-replan.** The 07-06 correction is the review process working end-to-end: codex's two confirmed HIGHs (feature-vs-cell aggregation undefined; `pre_shadow_weight` restoring into a column nothing reads) were independently schema-verified, then *resolved by deletion* - `pre_shadow_weight` dropped because `ensemble_trainer.py` recomputes every weight from scratch (verified: sole INSERT ... ON CONFLICT DO UPDATE writer, line ~744), promotion restores weight via status flip + next recompute. Idempotency landed as a real design (UNIQUE-keyed `integrity_monitor` facts, optimistic from_status lock), not a note.
- **The shared IC kernel shipped** - `src/intelligence/statistics/ic_math.py`, imported by both `ic_engine.py:84` and `ensemble_ic_engine.py:82` (todo 048 closed). Topdown D1's acceptable-fallback form (one kernel, two tables) is now real; methodology fixes propagate to both engines by construction.
- **The concept-registry cluster feedback loop.** The 07-04 review's punch list was executed within a day: hub doc updated in place (trigger-fired note, invariant 2 restated as evidence-mass, `alpha_pattern` reserved, per-stratum status note), todo 058 created with the full seeding spec. Best doc-governance behavior in the repo.
- **The remediation plan itself** - especially item 7 (1h/1d unexercised path called out for validation), item 9 (EIC-04's unweighted cell-counting caught *before* the re-run), and the monitoring-script denominator fix. This is the right artifact; it just needs F4's two insertions and F2's calibration step.
- **The backfill operational layer** - retry loop wrapping `infrastructure_run_historical_pipeline --client-id 40 --fetch-only` (running now), gap-aware fetch, APR-ified chunk limits: the IBKR nightly-restart lesson properly absorbed.
- **The endgame clarification itself** (PROJECT.md Core Value, scope-map re-rank, vision docs parked with two named reuse exceptions). F3 is about the ROADMAP not yet absorbing it, not about the decision.

---

## 3. Reconciliation Against Prior Reviews

**vs. 2026-07-04 concept-registry cluster review (baseline):** fully holds, and its punch list was executed - F1's missing todo now exists (058, comprehensive), the hub's F3/F4/F6/F7 edits verified in the live doc, `alpha_pattern` marked reserved. Nothing found here contradicts it. Two extensions: its F9 renumbering inventory is still pending execution (todo 057), and its "build the MVP" item now interacts with Phase 143 ordering per this review's F9.3 (058 item 7's build-time check is real, not hypothetical).

**vs. 2026-07-02 topdown:** D1 landed in its own named fallback form (kernel + two tables) - call it done, not pending. D3 (measurement/decision separation) is executing through 143's plans (dead decay columns dropped, lifecycle state to registry). D5 (one batched re-run) is the item now at risk: F4 shows the batch membership drifting (026 items double-assigned, and the *actual* next re-run - the fourth rebuild - was never named in any batch plan because it was forced by EIC-04's diagnosis, not scheduled by v3.15). D7 (146 collapse) remains unapplied (F10). D9 fired and is tracked (058). D12 held (151/152 stayed behind present-tense work - correctly, per this review too). The Measurement Engine's remaining unification (one table) stays correctly parked in intel-15.
**Superseded by new evidence:** topdown §1.7/D6's sequencing rationale ("conditioning foundations before AnalogEngine") survives, but its premise that v3.2 sits on the main line no longer matches the endgame re-rank; under F3, v3.15→v3.2 becomes a research branch, not the trunk.

**vs. 2026-07-02 bottomup:** its four rigor headliners split cleanly. Fixed and verified: OOS enforcement (§4 → 141.1), weight-epoch DO NOTHING trap (§10 → DO UPDATE verified in code), regime_scope column (§8 → migration applied, 256,566 rows). **Still live and now worse:** §5's no-op cost gate (this review's F2 - worse because docs now claim it fixed) and the 98.3%-in-5m emission concentration (97.8% today, F1); §2/§3's ghost daemons (F7, untouched); §6's synthetic-padding/session-mask issue (todo 035 still pending, unranked against the fourth rebuild - worth a look before it, since the rebuild recomputes everything downstream of the mask). Its "APR keys with zero readers" pattern (§2.3) is partially healing: the four `alpha.decay.*` keys gain readers when 143 executes; the bootstrap-CI key family remains dead.

**vs. 2026-07-01 architecture review:** E1→E2→E3→E4 sequencing held; E1's input-shrinkage even cleared its out-of-fold gate - but the review's core payoff ("judged by Phase 142A's EnsembleICEngine on OOS data") is the unexecuted judgment in F1. Its cross-asset-gap finding (15/58 mislabeled) is now understated: the 80-symbol universe adds FX/commodity funds to the mislabeled population (F3 note). Its Gap A/B fixes were absorbed into the Phase 144 plan (verified via matrix row).

**vs. 2026-07-03 roadmap reconciliation:** its structural pass was executed (v3.15 section real, 143 rewritten, EIC-04 verdict logged, v4.1 bullet fixed) - good compliance. Two corrections to it: F2's "keys are now calibrated (141.1)" is false (this review's F2 - the one factual error found across the six prior reviews, and it propagated); and its F1 fix, as executed, created the 026-P2b/P2c/P3 double-assignment this review's F4.1 flags (the items were added to v3.15's batch text and *kept* in 143's LIFECYCLE-00).

**vs. 2026-07-03 canonical-simulator and intel10-11 reviews:** no contradictions found; the binding rule + pre-commit Check 9 are live; 142B's two time-critical additions (provenance columns, gross-vs-net decision) remain correctly parked at 142B planning - with F2's caveat that the net-of-cost reporting decision must not assume calibrated hurdle keys exist yet.

---

## 4. Punch List (severity-ordered)

1. **[F2] Correct the cost-hurdle record now; run the calibration after the rebuild.** Fix STATE B2 / PROJECT / ROADMAP 141.1 language + reconciliation erratum today; add `ops_cost_hurdle_calibration.py` (live, not dry-run) to the remediation checklist post-rebuild; reopen or re-home todo 030's execution.
2. **[F4] Before the rebuild's compute stages start:** execute Phase 143 Wave 1 (143-01: P2b occupation gate + P2c `hmm_churn`) so the fourth rebuild's regime pass populates it for free; pre-commit the EIC-04 gate-weighting rule to `methodology-change-ledger.md`; strip 026 P2b/P2c/P3 from v3.15's batch text in the same commit.
3. **[F1] Then run the verdicts, in remediation-plan order:** full corpus re-run (verify 1h/1d weights + emissions actually appear, per its item 7) → EIC-04/EIC-05 → E1 (`v1_shrunk`) and E2 (`v2_mv`) through trainer/publisher/EnsembleICEngine → `ops_ensemble_weight_compare.py`. No new phases or review passes until these verdicts exist.
4. **[F3] Re-gate v4.0 on Phase 147's two gates** (drop "v3.2 complete"); move v3.2 to parallel/post-deployment; execute todo 059 (Aegis/TradeAgent reuse read) as v4.0 pre-planning.
5. **[F10] Make the v2.x disposition call** (todo 056 / bottomup Open Q1) and apply the 146/147 rescope - removes a can-never-fire gate from the newly-shortened path.
6. **[F5] One-line ROADMAP edit:** Phase 147 SCORE-02/03 write `integrity_monitor` rows (`monitor_type='oos_gate'`), not a new `gate_evaluations` table; note in intel-14.
7. **[F6] Canonical Truth Registry hygiene pass** (8 missing rows, 4 stale rows); add registry updates as tasks to 143-03 and todo 058; consider the pre-commit migration↔registry check.
8. **[F7] New todo: decommission the ghost daemons** (ctx-writer, lineage-writer, feature-vector-pipeline decision, failed unit, `_DAG_ORDER` prune).
9. **[F8] Doc-currency sweep:** STATE.md (frontmatter, 143 status, **is_pooled inversion**), MEMORY is_pooled line, scope-map "being A/B judged", backlog matrix (142A verdict, 142B.1, 143 rows), idea catalog (execution-plan row, backfill-disambiguation), then todo 057.
10. **[F9] Pin at planning time:** `alpha_frames` column-ownership split (142B P1 migration), `analog_scores` as sibling table not feature_vectors columns (Phase 149), 058-vs-143 registry routing check (intel-14 OQ3), per-monitor_type ownership note for `integrity_monitor`.

## References

- Live DB, queried 2026-07-06: `ensemble_weights` (`v1` only, 103 rows; tf ∈ {5m:60, 15m:43}); `alpha_ensemble_ic` (572 rows, all 5m/high_bear, 20 with `ic_ci_lower>0`); `alpha_events` (344,514; 5m=336,958, 15m=7,556); trainer-eligible features by tf (5m=37/15m=28/1h=15/1d=28, using `ensemble_trainer.py`'s own WHERE clause); `config_state` cost-hurdle keys (all 0.0); `config_history` `changed_by` distribution (zero `cost_hurdle_calibration` rows; `ops-ic-shrinkage` E1 gate-pass entry 2026-07-03); `to_regclass('concept_registry')`/`('integrity_monitor')` both NULL; `feature_registry` 61/61 active; `market_data_ohlcv` 80 symbols vs `feature_vectors` 58
- `systemctl list-units --all` 2026-07-06 (feature-vector-pipeline/ctx-writer/lineage-writer active; one loaded/failed); `ps aux` (backfill retry loop + `--client-id 40 --fetch-only` running)
- Code: `services/ensemble_trainer.py` (:4, :317, :430-431, :469, :540, ~:744), `services/ic_engine.py:84` + `services/ensemble_ic_engine.py:82` (shared kernel imports), `scripts/ops/corpus/ops_cost_hurdle_calibration.py` (unconditional Step-1 writes when live)
- `.planning/ROADMAP.md` (milestone lines 28-32; Phase 142A verdict log; Phase 143 incl. 2026-07-06 correction; Phase 146/147; v3.15 batch text); `.planning/STATE.md`; `.planning/PROJECT.md` (Core Value, 2026-07-05)
- `.planning/milestones/v3.1-phases/143-.../143-{01..03}-PLAN.md`, `143-RESEARCH.md`, `143-REVIEWS.md` (codex findings + independent verification), `141.1-.../141.1-VERIFICATION.md` (Truth 6)
- `/home/bg/.claude/plans/should-we-back-fill-nested-peacock.md` (remediation plan, items 6-9)
- `docs/research/`: `roadmap-scope-map.md` (2026-07-05 re-rank), `2026-07-01-intelligence-lifecycle-backlog-matrix.md`, `idea-catalog.md`, `intel-13`/`intel-14`/`intel-15`, `concept-governance-registries.md` (post-07-04 sync verified)
- `docs/foundation/`: `principles.md` (one-model-one-book), `canonical-truth-registry.md` (staleness evidence), `ship-or-sink-rules.md`, `musk-5-step-process.md`
- Prior reviews reconciled: `.planning/research/2026-07-0{1,2,2,3,3,3,4}-*.md` (all seven artifacts)
- `.planning/todos/`: pending 026/030(completed, contested)/035/036/041/054/055/056/057/058/059/061; completed 048
