---
phase: ad-hoc-htf-pipeline-debug
status: in_progress
last_updated: 2026-03-25T00:36:42Z
---

<current_state>
Post-reboot HTF (5m+) bar staleness debugging. Root cause found + partial fix deployed. 5m events still not reaching DB — second bug in signal_generator path.
</current_state>

<completed_work>

- Root cause: New M6 contracts have 0 rows in intelligence_features for 5m. Fallback seed queries market_data_ohlcv with timeframe='5m' — only 1m stored → 5m BarHistory empty → is_warm=False → 130min wait.
- Fix committed (182d4bc): feature_pipeline_service.py fallback now aggregates 1m→HTF in Python. fallback_bars 0→7524.
- Architecture: feature_pipeline → development.intelligence → signal_generator → development.intelligence.record → feature_writer → intelligence_features DB
</completed_work>

<remaining_work>

BUG 1 (ACTIVE): 5m events not in intelligence_features
- 5m pipeline runs (hmm_fallback_2d at 5m boundaries) but intelligence.record has NO 5m messages (last 500 all 1m)
- signal_generator processes 5m (writes signal_ledger) but does NOT publish BarIntelligenceRecord to intelligence.record
- Publish at signal_generator_service.py:1509 guarded by `if event is not None` at line 1486
- Next: trace _process_single_message (line 1518) for 5m — find where event is None or 5m is filtered

BUG 2 (lower priority): Stochastic "No numeric types to aggregate"
- Fix: stochastic.py lines 47+77: replace pd.NA with np.nan (keeps float64 dtype)
- Also add `import numpy as np` at top of stochastic.py
</remaining_work>

<decisions_made>

- market_data_5m view referenced in CLAUDE.md does NOT exist in DB — only market_data_ohlcv (1m only)
- BarAccumulator is NOT stuck-prone from missing bars — emit is period-boundary triggered
</decisions_made>

<next_action>
Trace signal_generator_service.py _process_single_message (line 1518): how is IntelligenceEvent parsed from payload {"event": "...json..."} and why is event None for 5m bars. Fix. Then fix stochastic.py pd.NA→np.nan.
</next_action>
