---
phase: intelligence-palette-expansion
task: simplify-smc-plugins
status: in_progress
last_updated: 2026-03-02T11:29:47.845Z
---

<current_state>
Mid-way through `/simplify` on the 5 new SMC plugins committed in `3014cc5`.
One file partially edited: `src/intelligence/smart_money/ict_killzones.py` — replaced dead code
(`_KILLZONES` list, `_window_start_minutes`, `_window_end_minutes`) with module-level `_KZ_MAP`
and `_KZ_STARTS` constants. The `compute_full` method still references the old `kz_map` local
dict — needs updating to use `_KZ_MAP` / `_KZ_STARTS`.
</current_state>

<completed_work>

**This session (before reboot recovery):**
- Recovered 2 sessions lost to PC reboot
- Fixed ICTKillzones bug: London/Asia overlap at 8:00 UTC picked wrong zone (removed `break`)
- Fixed VolumeProfile bug: scalar `close` overwritten by array `close_arr` (simplify regression)
- Fixed stale `test_tier_smc_has_8_plugins` → 13
- Fixed ruff: removed unused `prev_close` (amd_cycle), `close` (mitigation_blocks), long line (test_i5)
- Committed all 5 SMC plugins + schema + tests: `3014cc5`
- Committed idea/plan docs: `348535d`

**Phase 1 Correctness Audit:**
- All 15 tasks already complete from prior session (test_correctness_audit.py was already committed)
- 35/35 correctness audit tests pass — all I1–I6 math verified clean

**Simplify pass (in progress — NOT committed):**
- `ict_killzones.py`: removed dead `_KILLZONES` list + `_window_start_minutes` + `_window_end_minutes`;
  added module-level `_KZ_MAP` and `_KZ_STARTS` — but `compute_full` not yet updated to use them

</completed_work>

<remaining_work>

**Simplify — still to do (from 3 agent reviews):**

1. **ict_killzones.py** (partially done):
   - Update `compute_full` to use `_KZ_MAP` instead of local `kz_map` dict
   - Update `starts` line to use `_KZ_STARTS` instead of sorting every call
   - Use `is_num()` from `..utils` instead of `isinstance(v, (int, float))` (if any remain)

2. **amd_cycle.py**:
   - Fix `hour < 0` — unreachable condition (line 80: `if hour >= _ACCUM_START or hour < 0:` → remove `or hour < 0`)
   - Fix `!= True` → `is not True` (line 99)
   - Remove unreachable `elif phase == "accumulation":` block (lines 106-108) — dead code after the if/elif chain

3. **breaker_blocks.py**:
   - Remove unused `_null` dict (lines 52-58) — it's defined but `_from_state()` is always called instead

4. **mitigation_blocks.py**:
   - Cache `last_row = df.iloc[-1]` instead of two separate `.iloc[-1]` calls

5. **premium_discount.py**:
   - Remove unused `_state: dict = field(default_factory=dict)` (line 38) — plugin is stateless
   - Use `clamp(pct)` from `..utils` instead of `max(-1.0, min(1.0, pct))` (line 72)
   - Import: `from ..utils import clamp`

**After simplify:**
- Run `.venv/bin/pytest tests/unit/ -q` and `.venv/bin/ruff check . --fix`
- Commit: `git commit -m "refactor(smc): simplify 5 new SMC plugins — dead code, constants, clamp"`

**Then Phase 7 (next major work):**
- Task 7.1: Add recency weighting to `src/intelligence/confluence/cross_timeframe.py`
- Task 7.2: Integrate I2 event signals into I6 confluence scoring
- Phase 8: Final verification + CLAUDE.md update

</remaining_work>

<decisions_made>

- ICTKillzones: when London (07:00) and Asia (08:00) overlap, primary zone = earliest start (London wins) — fixes test
- VolumeProfile: use `close_arr` (not `close`) for the numpy array to preserve scalar `close` for distance calc
- Phase 1 audit was already done — skipped to Phase 7 next

</decisions_made>

<blockers>
None.
</blockers>

<context>
Working through the Intelligence Palette Expansion plan (`docs/plans/2026-03-01-intelligence-palette-expansion.md`).
Phases 2-6 are all done (I2 tier, I3/I4/I5 new plugins, SMC new plugins).
Phase 1 (audit) is done.
Currently mid-simplify on the 5 SMC plugins from Phase 6.
After simplify → Phase 7 (confluence refactor) → Phase 8 (final verification).
</context>

<next_action>
Read `src/intelligence/smart_money/ict_killzones.py` (already partially edited), update `compute_full`
to use module-level `_KZ_MAP` and `_KZ_STARTS` constants, then fix the 4 remaining files per the
list above. Run tests + ruff + commit. Then proceed to Phase 7.
</next_action>
