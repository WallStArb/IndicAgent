---
phase: ad-hoc-pipeline-debugging
task: 8
total_tasks: 13
status: in_progress
last_updated: 2026-03-23T23:53:15.882Z
---

<current_state>
Signal generator pipeline debugging session. Two bugs remain in signal_generator_service.py preventing clean startup. Logging now works (4112 bytes in log file). Pipeline structurally correct but signal_generator still processing 0 bars.
</current_state>

<completed_work>
- Diagnosed Kafka bootstrap hang (was transient — service now connecting fine)
- Found real issue: min_bars=50 warmup gating all processing (DB only had 29 bars after truncation)
- Fixed feature_writer_service.py: added _periodic_flush_loop background task (buffer was stuck with 4 events never flushing)
- Bootstrap script ran: inserted 4246 rows into intelligence_features from intelligence Kafka topic (DB now has 67 rows for EURUSD 1m)
- Fixed signal_generator_service.py main(): moved service __init__ BEFORE register_all_plugins() — plugin imports were configuring root logger before logging.basicConfig(), silencing all structlog output (0-byte log file bug)
- Logging now works — log file has content, exposing two remaining bugs
</completed_work>

<remaining_work>
**BUG 1 — MUST FIX FIRST** (signal_generator_service.py line 772):
```python
# BROKEN:
rows = await self.db_manager.execute_query(query, (symbol, tf))
# FIX:
rows = await self.db_manager.execute_query(query, symbol, tf)
```
execute_query has *args signature — passing tuple as single arg, server expects 2 args.

**BUG 2** (_load_tod_multipliers_from_db around line 1884):
Query references `regime_type_at_fire` column which doesn't exist in signal_ledger after table truncation. Error: "column regime_type_at_fire does not exist". Check signal_ledger schema and fix query. This is non-blocking (falls back to session priors) but generates noise.

**After fixes:**
- Restart signal_generator: `sudo systemctl restart indicagent-signal-generator`
- Verify: `curl -s http://localhost:9112/metrics | grep generator_bars_processed_total` — should be > 0
- Verify: `docker exec timescaledb psql -U postgres -d indicagent -c "SELECT COUNT(*) FROM signal_ledger;"`
- Commit all changes
</remaining_work>

<decisions_made>
- Bootstrap from Kafka topic directly (one-off /tmp/bootstrap_intelligence_features.py) to break circular dependency
- Added _periodic_flush_loop to feature_writer (time-based flush only worked on message arrival — stuck buffer bug)
- Moved register_all_plugins() after service init to fix structlog silence bug
</decisions_made>

<blockers>
- Bug 1 (execute_query tuple args) causes DB seed to fail → bar history empty → ALL bars fail min_bars=50 → 0 bars processed
- Bug 2 (regime_type_at_fire column) is non-blocking but noisy
</blockers>

<context>
Full pipeline flow (correct design):
feature_pipeline → development.intelligence (Kafka) → signal_generator (I7 processing) → development.intelligence.record (Kafka) → feature_writer → intelligence_features (DB) + signal_ledger (DB)

All services running. Kafka flowing. DB seeded. Just need two small code fixes in signal_generator_service.py then restart.

Uncommitted files: services/feature_writer_service.py, services/signal_generator_service.py
</context>

<next_action>
Fix signal_generator_service.py line 772: change execute_query(query, (symbol, tf)) to execute_query(query, symbol, tf). Then fix _load_tod_multipliers_from_db regime_type_at_fire column error. Then restart and verify.
</next_action>
