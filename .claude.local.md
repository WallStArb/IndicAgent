# Local Claude Notes

## /gsd:review Reviewer Quality

Ollama (gemma4:e4b, 9B params, local CPU on AMD HX AI 370): directional sanity check only. Finds the right problem areas but can't resolve exact mechanisms — cited line numbers, API contracts, return value semantics are beyond its precision. Skip when Codex is available. Reviewer hierarchy for this codebase: **Codex > Gemini > Ollama**.

Note: Ollama is invoked via HTTP API (`http://localhost:11434/v1/chat/completions`), not a CLI binary.
