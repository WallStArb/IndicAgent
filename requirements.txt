# Core Infrastructure
aiohttp>=3.9.0
aiokafka>=0.13.0
asyncpg>=0.31.0
fastapi>=0.129.0
httpx>=0.27.0
msgpack>=1.0.0
psycopg2-binary>=2.9.7
sdnotify>=0.3.2
uvicorn[standard]>=0.40.0
uvloop>=0.22.0; sys_platform != "win32"

# Data Processing
numpy>=2.0.0,<2.5.0  # numba's runtime check hard-fails to import above 2.4 (confirmed live 2026-08-03)
pandas>=2.2.0,<3.0.0  # blocked on 3.0, not a code-risk judgment call: every currently
# installable, non-yanked mlflow version in this project's accepted range (mlflow>=2.10, a real
# live dependency -- src/core/ml/registry.py, ml_trainer.py, ml_scorer_agent.py) itself depends
# on pandas<3 (confirmed via `uv pip install`'s resolver, 2026-08-03). This codebase's own usage
# was separately audited clean against every pandas-3.0 breaking-change category (no
# `dtype == object` checks, no `inplace=True`, no deprecated offset aliases, no pytz, no
# `observed=` groupby calls, no pandas chained assignment, no `Series.add()` against a
# DataFrame) -- revisit once mlflow ships a pandas 3-compatible release.
pandas-market-calendars>=4.3
pyarrow>=14.0.0
polars>=1.0
scipy>=1.15.0
statsmodels>=0.14.4
tsfresh>=0.21.1

# Intelligence / ML
hmmlearn>=0.3.0
langgraph>=1.0.0
lightgbm>=4.0
mlflow>=2.10
scikit-learn>=1.5.0
shap>=0.44
threadpoolctl>=3.1.0

# Observability
opentelemetry-api>=1.20.0
opentelemetry-exporter-otlp-proto-grpc>=1.20.0
opentelemetry-exporter-otlp-proto-http>=1.20.0
opentelemetry-instrumentation>=0.45b0
opentelemetry-instrumentation-fastapi>=0.45b0
opentelemetry-instrumentation-logging>=0.45b0
opentelemetry-sdk>=1.20.0
structlog>=25.5.0

# Settings / Config
cramjam>=2.8.0
ib_insync>=0.9.86
pydantic>=2.12.0
pydantic-ai>=2.0,<3
pydantic-settings>=2.12.0
tzdata>=2025.1

# LLM Platform
litellm>=1.40.0,<2.0.0
instructor>=1.0.0,<2.0.0

# Development & Testing
black>=26.1.0
mypy>=1.19.0
# pandas-ta is test-only (indicator correctness reference oracle). Install separately:
#   uv pip install pandas-ta --no-deps
# It pins numba==0.61.2 which conflicts with numpy>=2.4; --no-deps bypasses this.
numba>=0.65.0
pytest>=8.4.0
pytest-asyncio>=1.1.0
ruff>=0.15.0
tqdm>=4.67.3  # was exact-pinned with no recorded reason; pydantic-ai 2.x's own resolution already needs 4.70+
vulture>=2.11
