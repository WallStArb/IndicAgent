---
phase: 260528-87t-remove-redundant-execstartpre-pg-isready
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/systemd/indicagent-weight-updater.service
  - /etc/systemd/system/indicagent-weight-updater.service
  - docs/guides/running-services.md
  - docs/setup-new-machine.md
  - docs/ideas/architectural-weakness-assessment.md
autonomous: true
must_haves:
  truths:
    - "weight-updater service no longer runs redundant pg_isready ExecStartPre poll"
    - "running-services.md documents both timescaledb-ready and infrastructure.target"
    - "setup-new-machine.md no longer claims DB readiness is assumed"
    - "architectural-weakness-assessment.md item #21 marked RESOLVED"
  artifacts:
    - path: "production/systemd/indicagent-weight-updater.service"
      provides: "Service unit without redundant pg_isready ExecStartPre"
    - path: "/etc/systemd/system/indicagent-weight-updater.service"
      provides: "Installed unit mirroring repo copy"
    - path: "docs/guides/running-services.md"
      provides: "L0 Infrastructure table including timescaledb-ready + infrastructure.target"
    - path: "docs/setup-new-machine.md"
      provides: "Accurate troubleshooting entry referencing infrastructure.target gating"
    - path: "docs/ideas/architectural-weakness-assessment.md"
      provides: "Item #21 + summary table updated to RESOLVED"
  key_links:
    - from: "indicagent-weight-updater.service"
      to: "indicagent-infrastructure.target"
      via: "Requires= directive (existing, retained)"
      pattern: "Requires=indicagent-infrastructure.target"
---

<objective>
Remove redundant `ExecStartPre` pg_isready poll from the weight-updater unit (now superseded by `indicagent-infrastructure.target` Requires) and update three docs to reflect the new infrastructure gating model.

Purpose: Eliminate duplicate readiness logic and align docs with the new target-based startup ordering shipped in quick task 260528-806.
Output: Cleaner systemd unit + accurate docs.
</objective>

<context>
@CLAUDE.md
@production/systemd/indicagent-weight-updater.service
@docs/guides/running-services.md
@docs/setup-new-machine.md
@docs/ideas/architectural-weakness-assessment.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove redundant ExecStartPre from weight-updater unit</name>
  <files>production/systemd/indicagent-weight-updater.service, /etc/systemd/system/indicagent-weight-updater.service</files>
  <action>
    Delete the `ExecStartPre=/bin/sh -c 'until docker exec timescaledb pg_isready -U postgres 2>/dev/null; do sleep 2; done'` line (line 13) from `production/systemd/indicagent-weight-updater.service`.

    Then sync the installed copy at `/etc/systemd/system/indicagent-weight-updater.service` by either copying the repo file or deleting the same line in place. Use sudo (password in `/home/bg/.claude/projects/-home-bg-dev-indicagent/memory/user_sudo_password.md`).

    After the file change, run `sudo systemctl daemon-reload`.

    Do NOT touch the existing `Requires=indicagent-infrastructure.target` / `After=` directives — those provide the actual readiness gate now.
  </action>
  <verify>
    - `grep -n pg_isready production/systemd/indicagent-weight-updater.service` returns no matches
    - `grep -n pg_isready /etc/systemd/system/indicagent-weight-updater.service` returns no matches
    - `systemctl cat indicagent-weight-updater | grep -c ExecStartPre` returns 0 (or only non-pg_isready ExecStartPre lines if any)
    - `systemctl status indicagent-weight-updater` still shows the unit loaded after daemon-reload
  </verify>
  <done>Redundant pg_isready poll removed from both repo and installed unit files; daemon-reload completed cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update running-services.md L0 Infrastructure table</name>
  <files>docs/guides/running-services.md</files>
  <action>
    Locate the L0 Infrastructure table that currently lists only `indicagent-redpanda-ready`. Add two new rows immediately after the redpanda-ready row:

    ```
    | `indicagent-timescaledb-ready` | Readiness sentinel — blocks all app services until TimescaleDB accepts connections |
    | `indicagent-infrastructure.target` | Unified gate — all app services Require this; satisfied when both DB and Kafka are ready |
    ```

    Preserve existing table formatting (column count, alignment). Per memory rule: use single dash, not em dash — replace the em dashes shown above with `-` when writing the file.
  </action>
  <verify>
    - `grep -n "indicagent-timescaledb-ready" docs/guides/running-services.md` returns a match in the L0 table
    - `grep -n "indicagent-infrastructure.target" docs/guides/running-services.md` returns a match in the L0 table
    - Table renders with consistent column count (visual check)
  </verify>
  <done>L0 table documents both new units alongside redpanda-ready.</done>
</task>

<task type="auto">
  <name>Task 3: Update setup-new-machine.md and architectural-weakness-assessment.md</name>
  <files>docs/setup-new-machine.md, docs/ideas/architectural-weakness-assessment.md</files>
  <action>
    **A. docs/setup-new-machine.md (around line 242):**
    Replace the stale troubleshooting entry:
    ```
    **TimescaleDB not ready:** Services wait on `indicagent-redpanda-ready.service` but DB
    readiness is assumed after Docker Compose. If migrations fail, wait 10s and retry.
    ```
    With an accurate description: both TimescaleDB and Redpanda readiness are now gated by `indicagent-infrastructure.target`. All 40 app services `Requires=indicagent-infrastructure.target`, which is satisfied only when both `indicagent-timescaledb-ready.service` and `indicagent-redpanda-ready.service` exit cleanly. Services will not start until both backends accept connections. If startup hangs, check `systemctl status indicagent-timescaledb-ready indicagent-redpanda-ready indicagent-infrastructure.target`. Use plain dashes, not em dashes.

    **B. docs/ideas/architectural-weakness-assessment.md:**
    - Item #21 (around lines 284-292): Mark as RESOLVED. Add a brief resolution note referencing quick task 260528-806 which introduced `indicagent-timescaledb-ready.service` + `indicagent-infrastructure.target`, wiring all 40 services to `Requires=indicagent-infrastructure.target` and superseding the individual `redpanda.service` dependency.
    - Summary table near line 554: change the row for item #21 from its prior status to `RESOLVED`.

    Preserve all other content in both files. No em dashes.
  </action>
  <verify>
    - `grep -n "DB readiness is assumed" docs/setup-new-machine.md` returns no matches
    - `grep -n "indicagent-infrastructure.target" docs/setup-new-machine.md` returns a match in the troubleshooting section
    - `grep -n "RESOLVED" docs/ideas/architectural-weakness-assessment.md` includes the item #21 region and summary table row
    - `grep -c "—" docs/setup-new-machine.md docs/ideas/architectural-weakness-assessment.md docs/guides/running-services.md` shows no NEW em dashes were introduced by these edits (only pre-existing ones if any remain)
  </verify>
  <done>Both docs reflect the infrastructure.target gating model; item #21 marked RESOLVED in body and summary table.</done>
</task>

</tasks>

<verification>
- Unit file diff is a single deletion (the ExecStartPre line); no other directives touched
- `systemctl daemon-reload` exit code 0
- `systemctl cat indicagent-weight-updater` shows Requires=indicagent-infrastructure.target retained
- All three docs updated; no em dashes introduced
- `git diff --stat` shows exactly the 5 files listed in files_modified (minus the /etc/ path which is outside the repo)
</verification>

<success_criteria>
- weight-updater unit no longer contains pg_isready ExecStartPre poll (repo + installed)
- `systemctl daemon-reload` clean
- running-services.md L0 table includes timescaledb-ready and infrastructure.target
- setup-new-machine.md troubleshooting reflects current gating
- architectural-weakness-assessment.md item #21 marked RESOLVED (body + summary)
- Single atomic commit on a feature branch per Done-Coding SOP
</success_criteria>

<output>
After completion, no SUMMARY needed for quick tasks. Commit changes with message like:
`chore(systemd): remove redundant pg_isready ExecStartPre from weight-updater; doc sync for infrastructure.target`
</output>
