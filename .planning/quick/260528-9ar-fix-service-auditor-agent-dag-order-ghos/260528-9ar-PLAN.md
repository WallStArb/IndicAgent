---
phase: 260528-9ar-fix-service-auditor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/service_auditor_agent.py
autonomous: true

must_haves:
  truths:
    - "service_auditor_agent does not reference indicagent-ibkr-restart as a known DAG unit (ghost removed)"
    - "service_auditor_agent recognizes indicagent-timescaledb-ready as an infra sentinel at priority 0"
    - "service_auditor treats indicagent-timescaledb-ready, indicagent-redpanda-ready, indicagent-feature-validation, indicagent-hmm-training as oneshot units (no restart when inactive/dead)"
    - "python -c 'import services.service_auditor_agent' succeeds with no syntax/import errors"
  artifacts:
    - path: "services/service_auditor_agent.py"
      provides: "ServiceAuditorAgent with corrected _DAG_ORDER and _ONESHOT_UNITS"
      contains: "indicagent-timescaledb-ready"
  key_links:
    - from: "_DAG_ORDER"
      to: "_ONESHOT_UNITS"
      via: "oneshot sentinels listed in both with priority 0"
      pattern: "indicagent-timescaledb-ready.*: 0"
---

<objective>
Fix two bugs in `services/service_auditor_agent.py`:

1. Remove ghost entry `indicagent-ibkr-restart` from `_DAG_ORDER` and `_ONESHOT_UNITS` (no corresponding service file in `production/systemd/`).
2. Register 4 missing oneshot services so the auditor stops treating them as failed daemons.

Purpose: Prevent auditor from off-schedule restart attempts on timer-triggered/sentinel services and from referencing a non-existent unit.
Output: Updated `services/service_auditor_agent.py` with corrected registries.
</objective>

<context>
@CLAUDE.md
@services/service_auditor_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update _DAG_ORDER, _ONESHOT_UNITS, _LAG_THRESHOLDS in service_auditor_agent.py</name>
  <files>services/service_auditor_agent.py</files>
  <action>
Edit `services/service_auditor_agent.py` to fix the registries.

**Change A — `_DAG_ORDER` (priority 0 block, around line 53-57):**
- REMOVE this line entirely:
  `"indicagent-ibkr-restart": 0,  # oneshot wrapper: restarts ibkr-provider after TWS nightly restart (timer-triggered, not monitored)`
- ADD a new priority-0 entry mirroring the redpanda-ready pattern:
  `"indicagent-timescaledb-ready": 0,  # infra tier: TimescaleDB readiness sentinel`
  Place it adjacent to `"indicagent-redpanda-ready": 0,` so the infra sentinels cluster together.

**Change B — `_DAG_ORDER` (add timer-triggered oneshot analytics block, around line 89-94):**
Add these two entries alongside the other oneshot timer services (priority 8 cluster) with explanatory comments:
- `"indicagent-feature-validation": 8,  # oneshot: timer-triggered daily, not a daemon`
- `"indicagent-hmm-training": 8,  # oneshot: timer-triggered monthly, not a daemon`

**Change C — `_ONESHOT_UNITS` frozenset (around line 166-179):**
- REMOVE this line:
  `"indicagent-ibkr-restart",  # Type=oneshot timer; triggered by ibkr-restart.timer after TWS nightly restart`
- ADD these four entries to the frozenset (preserve alphabetical-ish grouping; keep redpanda-watchdog at top with other infra):
  - `"indicagent-redpanda-ready",  # Type=oneshot, RemainAfterExit=yes; infra sentinel, inactive between boots is correct`
  - `"indicagent-timescaledb-ready",  # Type=oneshot, RemainAfterExit=yes; infra sentinel, inactive between boots is correct`
  - `"indicagent-feature-validation",  # Type=oneshot, timer-triggered daily`
  - `"indicagent-hmm-training",  # Type=oneshot, timer-triggered monthly`

**Change D — `_LAG_THRESHOLDS` and `_AGENT_ID_TO_UNIT`:**
Do NOT add entries for any of the new oneshot/sentinel units. They are not Kafka consumers (sentinels are systemd-only, timer oneshots run as scripts), matching the existing pattern (redpanda-ready, ml-training etc. have no lag threshold). Confirm `_LAG_THRESHOLDS` has no entry for `indicagent-ibkr-restart` to remove (per inspection it does not).

**Rationale notes (do NOT include in code comments verbatim, only as guidance):**
- `indicagent-ibkr-restart` is in `/etc/systemd/system/` but NOT in `production/systemd/` (the canonical reference). Treat as ghost per task spec.
- Without `_ONESHOT_UNITS` membership, auditor would see `inactive (dead)` between runs and attempt restart — bad for sentinels and timer services.
  </action>
  <verify>
1. `grep -n "indicagent-ibkr-restart" services/service_auditor_agent.py` returns no matches.
2. `grep -n "indicagent-timescaledb-ready" services/service_auditor_agent.py` shows it in both `_DAG_ORDER` (priority 0) and `_ONESHOT_UNITS`.
3. `grep -n "indicagent-feature-validation\|indicagent-hmm-training" services/service_auditor_agent.py` shows each in both `_DAG_ORDER` (priority 8) and `_ONESHOT_UNITS`.
4. `grep -n "indicagent-redpanda-ready" services/service_auditor_agent.py` shows it now also appears inside the `_ONESHOT_UNITS` frozenset.
5. `.venv/bin/python -c "import services.service_auditor_agent as m; assert 'indicagent-ibkr-restart' not in m._DAG_ORDER; assert 'indicagent-timescaledb-ready' in m._DAG_ORDER and m._DAG_ORDER['indicagent-timescaledb-ready'] == 0; assert {'indicagent-timescaledb-ready','indicagent-redpanda-ready','indicagent-feature-validation','indicagent-hmm-training'}.issubset(m._ONESHOT_UNITS); print('OK')"` prints `OK`.
6. `.venv/bin/ruff check services/service_auditor_agent.py` passes.
  </verify>
  <done>
- `indicagent-ibkr-restart` removed from both `_DAG_ORDER` and `_ONESHOT_UNITS`.
- `indicagent-timescaledb-ready` added to `_DAG_ORDER` (priority 0) and `_ONESHOT_UNITS`.
- `indicagent-redpanda-ready` added to `_ONESHOT_UNITS` (already in `_DAG_ORDER`).
- `indicagent-feature-validation` and `indicagent-hmm-training` added to both `_DAG_ORDER` (priority 8) and `_ONESHOT_UNITS`.
- Module imports cleanly; ruff passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run unit tests and commit</name>
  <files>services/service_auditor_agent.py</files>
  <action>
Run the Done-Coding SOP for this change:

1. `.venv/bin/ruff check services/service_auditor_agent.py --fix`
2. `.venv/bin/black services/service_auditor_agent.py`
3. `.venv/bin/pytest tests/unit/ -q -k "service_auditor or auditor"` — must pass (or no tests collected, which is acceptable if no matching tests exist; record the outcome).
4. Quick smoke-test the auditor's helper sort logic still works:
   `.venv/bin/python -c "from services.service_auditor_agent import _DAG_ORDER, _ONESHOT_UNITS; print(sorted(_DAG_ORDER.items(), key=lambda x: x[1])[:5]); print(sorted(_ONESHOT_UNITS))"`
5. Commit on a feature branch:
   ```
   git checkout -b fix/service-auditor-dag-ghost-and-oneshots
   git add services/service_auditor_agent.py
   git commit -m "fix(service-auditor): remove ibkr-restart ghost; register timescaledb-ready and 3 oneshot units"
   ```
6. Fast-forward merge to main per SOP:
   ```
   git checkout main && git merge --ff-only fix/service-auditor-dag-ghost-and-oneshots
   git branch -d fix/service-auditor-dag-ghost-and-oneshots
   git worktree prune
   ```
   (Do NOT push — user will push manually unless they ask.)
  </action>
  <verify>
- `git log -1 --oneline` shows the fix commit on main.
- `git status` is clean.
- ruff and black exit 0.
  </verify>
  <done>
Change committed to main locally, branch deleted, ready for `git push origin main` when user requests.
  </done>
</task>

</tasks>

<verification>
- All grep/python checks in Task 1 verify section pass.
- ruff + black clean.
- Module importable, no `KeyError` or syntax issues.
- Single atomic commit on main.
</verification>

<success_criteria>
- `services/service_auditor_agent.py` no longer references the ghost `indicagent-ibkr-restart` unit.
- Auditor recognizes `indicagent-timescaledb-ready`, `indicagent-redpanda-ready`, `indicagent-feature-validation`, `indicagent-hmm-training` as oneshot units and will not attempt off-schedule restarts when they are `inactive (dead)`.
- One clean commit on main.
</success_criteria>

<output>
After completion, no SUMMARY.md required (quick mode); the commit message documents the change.
</output>
