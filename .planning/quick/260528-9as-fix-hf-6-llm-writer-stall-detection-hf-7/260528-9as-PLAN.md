---
phase: quick-260528-9as
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/bar_aggregator_agent.py
  - services/llm_writer_service.py
  - services/bar_writer_agent.py
  - production/systemd/indicagent-bar-aggregator.service
  - production/systemd/indicagent-alerting-agent.service
  - production/systemd/indicagent-dlq-drain.service
  - production/systemd/indicagent-service-auditor.service
  - /etc/systemd/system/indicagent-bar-aggregator.service
  - /etc/systemd/system/indicagent-alerting-agent.service
  - /etc/systemd/system/indicagent-dlq-drain.service
  - /etc/systemd/system/indicagent-service-auditor.service
  - /etc/logrotate.d/indicagent
autonomous: true

must_haves:
  truths:
    - "bar_writer_agent calls self._record_message_consumed() in its consume loop"
    - "llm_writer_service _stall_watchdog reads the correct base-class attribute and warns only after first message"
    - "bar_aggregator_agent does not override base SETUP_RETRY_ATTEMPTS / SETUP_RETRY_BACKOFF_S"
    - "All non-meta indicagent systemd units cap restart thrash via StartLimitBurst=5 / StartLimitIntervalSec=300"
    - "bar-aggregator OTEL endpoint matches the rest of the fleet (port 4318)"
    - "service-auditor retains StartLimitIntervalSec=0 with an inline comment explaining why"
    - "/etc/logrotate.d/indicagent exists and matches production/indicagent-logrotate.conf"
  artifacts:
    - path: "services/bar_aggregator_agent.py"
      provides: "bar aggregator with default base-class retry policy"
      contains: "class BarAggregatorAgent"
    - path: "/etc/logrotate.d/indicagent"
      provides: "installed logrotate config for indicagent logs"
  key_links:
    - from: "services/bar_writer_agent.py"
      to: "BaseAgent._last_message_ts"
      via: "self._record_message_consumed() in async-for consume loop"
      pattern: "_record_message_consumed"
    - from: "services/llm_writer_service.py::_stall_watchdog"
      to: "BaseAgent._last_message_ts"
      via: "direct attribute read with None startup guard"
      pattern: "self\\._last_message_ts"
    - from: "production/systemd/*.service"
      to: "/etc/systemd/system/*.service"
      via: "sudo cp + sudo systemctl daemon-reload"
      pattern: "StartLimitBurst=5"
---

<objective>
Apply six targeted ops/code fixes flagged in the recent infrastructure audit: confirm HF-6 / HF-7 stall-detection fixes are in place, remove an unjustified retry override in bar_aggregator, normalize systemd restart limits across non-meta units, fix the bar-aggregator OTEL port, and install the existing logrotate config to /etc/logrotate.d.

Purpose: Eliminate undetected stalls, prevent thrashing restart loops, align OTEL exporters on one endpoint, and stop indefinite log growth on the box.
Output: Three atomic commits (code, systemd, ops) and a SUMMARY.md describing the logrotate install (no-code change).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@services/llm_writer_service.py
@services/bar_writer_agent.py
@services/bar_aggregator_agent.py
@production/systemd/indicagent-bar-aggregator.service
@production/systemd/indicagent-alerting-agent.service
@production/systemd/indicagent-dlq-drain.service
@production/systemd/indicagent-service-auditor.service
@production/indicagent-logrotate.conf

Pre-flight findings (must verify before editing — do not blindly re-apply):
- `services/bar_writer_agent.py` line ~256 ALREADY calls `self._record_message_consumed()` inside the `async for ... self._kafka_consumer.messages():` loop (shipped in commit 55196e46, "fix(105-02): BarWriterAgent _record_message_consumed() in custom _run() loop (HF-7)").
- `services/llm_writer_service.py` `_stall_watchdog` (line ~941) ALREADY reads `self._last_message_ts` with a `None` startup guard (shipped in commit 5d6d637c, "fix(105-01): ... stall watchdog ...").

If verification shows these are still correct, Task 1 must NOT re-edit them — it should ONLY remove the bar_aggregator retry override and document the pre-existing fixes in the commit message body. If verification surfaces a regression (attribute name reverted, call site removed), restore the intended behavior using the patterns described in the original audit context.

Sudo password: `/home/bg/.claude/projects/-home-bg-dev-indicagent/memory/user_sudo_password.md` (read once, pipe via `sudo -S`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify HF-6/HF-7 fixes and remove bar_aggregator retry override</name>
  <files>
    services/bar_writer_agent.py
    services/llm_writer_service.py
    services/bar_aggregator_agent.py
  </files>
  <action>
    Step 1 — Verify HF-7 (bar_writer_agent.py):
    - `grep -n "_record_message_consumed" services/bar_writer_agent.py` must return a hit inside the `_run`/consume loop (currently line ~256 between `async for _topic, _key, payload in self._kafka_consumer.messages():` and the buffer append).
    - If present: leave file unchanged. If MISSING (regression): re-add `self._record_message_consumed()` as the first statement after the `if self._stop_event.is_set(): break` guard inside the `async for` loop. Do not call it inside the inner try/except; call it once per message before validation so it counts even when a payload is later rejected — same placement as before (line 256).

    Step 2 — Verify HF-6 (llm_writer_service.py):
    - `grep -n "_last_msg_ts\|_last_message_ts" services/llm_writer_service.py` — there must be NO occurrences of `_last_msg_ts` and at least two of `_last_message_ts` (one for the None guard, one for the `time.monotonic() - self._last_message_ts` subtraction inside `_stall_watchdog`).
    - If `_last_msg_ts` is found anywhere: replace with `_last_message_ts`. Keep the existing None-guard semantics (continue on None, warn only when `idle_secs >= self.max_idle_seconds`).
    - Also verify `_record_message_consumed()` is still called inside `_process_loop` after `if self._stop_event.is_set(): break` (currently line ~970). Restore if missing.

    Step 3 — bar_aggregator_agent.py retry override removal:
    - Open `services/bar_aggregator_agent.py` lines 165-167. Delete the two class attribute lines:
        `SETUP_RETRY_ATTEMPTS: int = 4`
        `SETUP_RETRY_BACKOFF_S: float = 2.0`
    - Also delete the preceding comment `# Preserve the original retry behavior: 4 attempts at 2s base backoff` (line 165) since the override no longer exists.
    - Leave `__init__` and everything else untouched. Base class defaults (3 attempts, 2.0s) now apply.

    Step 4 — Lint + unit test the touched files:
    - `.venv/bin/ruff check services/bar_aggregator_agent.py services/bar_writer_agent.py services/llm_writer_service.py --fix`
    - `.venv/bin/black services/bar_aggregator_agent.py services/bar_writer_agent.py services/llm_writer_service.py`
    - `.venv/bin/pytest tests/unit/ -q -k "bar_aggregator or bar_writer or llm_writer"` — must pass (or report only pre-existing failures unrelated to these edits).

    Step 5 — Commit (single atomic commit covering all three files):
    - Use `node ~/.claude/get-shit-done/bin/gsd-tools.cjs commit` with message body documenting whether HF-6/HF-7 required re-application or were verified-as-already-fixed, plus the bar_aggregator override removal.
    - Commit message:
      `fix(quick-9as): remove bar_aggregator retry override; verify HF-6/HF-7 stall detection`
  </action>
  <verify>
    grep -n "_record_message_consumed" services/bar_writer_agent.py | grep -E ":(2[0-9]{2}|3[0-9]{2})" && \
    ! grep -q "_last_msg_ts" services/llm_writer_service.py && \
    ! grep -q "SETUP_RETRY_ATTEMPTS\|SETUP_RETRY_BACKOFF_S" services/bar_aggregator_agent.py && \
    .venv/bin/pytest tests/unit/ -q -k "bar_aggregator or bar_writer or llm_writer"
  </verify>
  <done>
    - `services/bar_writer_agent.py` calls `self._record_message_consumed()` in its consume loop.
    - `services/llm_writer_service.py` `_stall_watchdog` references only `self._last_message_ts` and never `self._last_msg_ts`.
    - `services/bar_aggregator_agent.py` contains no `SETUP_RETRY_ATTEMPTS` or `SETUP_RETRY_BACKOFF_S` class-level overrides.
    - Targeted unit tests pass.
    - One atomic commit recorded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Systemd restart limits + OTEL port + installed-unit sync</name>
  <files>
    production/systemd/indicagent-bar-aggregator.service
    production/systemd/indicagent-alerting-agent.service
    production/systemd/indicagent-dlq-drain.service
    production/systemd/indicagent-service-auditor.service
  </files>
  <action>
    Step 1 — Edit `production/systemd/indicagent-bar-aggregator.service`:
    - Change `Environment=OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317` to `Environment=OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318`.
    - Existing `StartLimitIntervalSec=300` + `StartLimitBurst=0` (Burst=0 = unlimited). Change `StartLimitBurst=0` to `StartLimitBurst=5` so this matches the standard pattern.

    Step 2 — Edit `production/systemd/indicagent-alerting-agent.service`:
    - In the `[Unit]` section, replace `StartLimitIntervalSec=0` with the two-line standard pattern:
        `StartLimitIntervalSec=300`
        `StartLimitBurst=5`

    Step 3 — Edit `production/systemd/indicagent-dlq-drain.service`:
    - Same change as Task 2 Step 2: replace `StartLimitIntervalSec=0` with `StartLimitIntervalSec=300` and add `StartLimitBurst=5` on the next line.

    Step 4 — Edit `production/systemd/indicagent-service-auditor.service`:
    - KEEP `StartLimitIntervalSec=0` (this is the meta-watchdog and must always restart).
    - Add an inline comment IMMEDIATELY ABOVE the directive:
      ```
      # Meta-watchdog exception: service-auditor must restart unconditionally — it is
      # the supervisor of last resort for every other indicagent unit. Do NOT set
      # StartLimitBurst here.
      StartLimitIntervalSec=0
      ```

    Step 5 — Install updated units to `/etc/systemd/system/` and reload:
    - Read sudo password: `SUDO_PASS=$(cat /home/bg/.claude/projects/-home-bg-dev-indicagent/memory/user_sudo_password.md)`
    - For each of the four units: `echo "$SUDO_PASS" | sudo -S cp production/systemd/<unit>.service /etc/systemd/system/<unit>.service`
    - `echo "$SUDO_PASS" | sudo -S systemctl daemon-reload`
    - Do NOT restart any service; reload is sufficient. The new limits take effect on the next restart.

    Step 6 — Spot-verify installed units match repo copies:
    - `diff production/systemd/indicagent-bar-aggregator.service /etc/systemd/system/indicagent-bar-aggregator.service` (must be empty)
    - Repeat for alerting-agent, dlq-drain, service-auditor.

    Step 7 — Commit:
    - Stage only the four files under `production/systemd/`.
    - Message: `fix(quick-9as): normalize systemd restart limits and bar-aggregator OTEL port`
    - Body: list the four units, note the service-auditor exception, note OTEL 4317->4318 on bar-aggregator, note daemon-reload was applied on the host.
  </action>
  <verify>
    grep -q "OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318" production/systemd/indicagent-bar-aggregator.service && \
    grep -q "StartLimitBurst=5" production/systemd/indicagent-alerting-agent.service && \
    grep -q "StartLimitBurst=5" production/systemd/indicagent-dlq-drain.service && \
    grep -q "Meta-watchdog exception" production/systemd/indicagent-service-auditor.service && \
    diff -q production/systemd/indicagent-alerting-agent.service /etc/systemd/system/indicagent-alerting-agent.service && \
    systemctl status indicagent-service-auditor.service >/dev/null
  </verify>
  <done>
    - bar-aggregator OTEL endpoint is 4318.
    - alerting-agent and dlq-drain use `StartLimitIntervalSec=300` + `StartLimitBurst=5`.
    - service-auditor keeps `StartLimitIntervalSec=0` with an inline comment explaining the exception.
    - `/etc/systemd/system/` copies are byte-identical to `production/systemd/` copies.
    - `systemctl daemon-reload` has been executed.
    - One atomic commit recorded for the four unit files.
  </done>
</task>

<task type="auto">
  <name>Task 3: Install logrotate config + write SUMMARY.md</name>
  <files>
    /etc/logrotate.d/indicagent
    .planning/quick/260528-9as-fix-hf-6-llm-writer-stall-detection-hf-7/260528-9as-SUMMARY.md
  </files>
  <action>
    Step 1 — Install logrotate config (host-side ops change, no repo edit):
    - `SUDO_PASS=$(cat /home/bg/.claude/projects/-home-bg-dev-indicagent/memory/user_sudo_password.md)`
    - `echo "$SUDO_PASS" | sudo -S cp production/indicagent-logrotate.conf /etc/logrotate.d/indicagent`
    - `echo "$SUDO_PASS" | sudo -S chmod 644 /etc/logrotate.d/indicagent`
    - `echo "$SUDO_PASS" | sudo -S chown root:root /etc/logrotate.d/indicagent`

    Step 2 — Validate logrotate config:
    - `sudo logrotate --debug /etc/logrotate.d/indicagent 2>&1 | tee /tmp/logrotate-debug.log`
    - Confirm no `error:` lines in output; expected: a list of `considering log` entries for files under `/home/bg/dev/indicagent/logs/`.
    - Confirm `state file` line shows it processed without complaint.

    Step 3 — Write SUMMARY.md:
    - Path: `.planning/quick/260528-9as-fix-hf-6-llm-writer-stall-detection-hf-7/260528-9as-SUMMARY.md`
    - Sections:
      - `## What shipped` — bullet list of all 6 fixes (T1 retry override, T1 HF-6/HF-7 verification outcome, T2 systemd 4 units, T3 logrotate install)
      - `## Files changed in repo` — list with one-line each
      - `## Host-side changes (not in git)` — `/etc/systemd/system/*.service` updated for 4 units, `systemctl daemon-reload` run, `/etc/logrotate.d/indicagent` installed
      - `## Verification commands run` — paste the actual verify outputs from Tasks 1 and 2 plus the logrotate --debug summary
      - `## Follow-ups` — note that affected services will pick up new StartLimit settings on their next restart; suggest a maintenance-window `systemctl restart` for bar-aggregator (OTEL port change) at operator discretion

    Step 4 — Commit the SUMMARY:
    - `node ~/.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(quick-9as): summary for stall-detection / systemd / logrotate fixes" --files .planning/quick/260528-9as-fix-hf-6-llm-writer-stall-detection-hf-7/260528-9as-SUMMARY.md`

    Step 5 — Done-Coding SOP tail:
    - Confirm working tree clean: `git status --short` returns nothing.
    - Merge to main and clean up per CLAUDE.md SOP only if explicitly requested by the user; otherwise leave commits on current branch.
  </action>
  <verify>
    test -f /etc/logrotate.d/indicagent && \
    diff -q production/indicagent-logrotate.conf /etc/logrotate.d/indicagent && \
    sudo logrotate --debug /etc/logrotate.d/indicagent >/dev/null 2>&1 && \
    test -f .planning/quick/260528-9as-fix-hf-6-llm-writer-stall-detection-hf-7/260528-9as-SUMMARY.md && \
    git log -1 --pretty=%s | grep -q "quick-9as"
  </verify>
  <done>
    - `/etc/logrotate.d/indicagent` exists, is owned by root:root mode 0644, and is byte-identical to `production/indicagent-logrotate.conf`.
    - `sudo logrotate --debug /etc/logrotate.d/indicagent` exits 0 with no `error:` lines.
    - SUMMARY.md exists and documents every code change AND every host-side change (no missing host changes).
    - Final commit lands the SUMMARY; working tree is clean.
  </done>
</task>

</tasks>

<verification>
- `grep -n "_record_message_consumed" services/bar_writer_agent.py` returns a hit in the consume loop.
- `grep -c "_last_msg_ts" services/llm_writer_service.py` returns 0.
- `grep -c "SETUP_RETRY_" services/bar_aggregator_agent.py` returns 0.
- `grep "OTEL_EXPORTER_OTLP_ENDPOINT" production/systemd/indicagent-bar-aggregator.service` shows port 4318.
- All four updated unit files in `/etc/systemd/system/` match `production/systemd/` (diff empty).
- `/etc/logrotate.d/indicagent` exists and `sudo logrotate --debug` runs clean.
- `.planning/quick/260528-9as-.../260528-9as-SUMMARY.md` exists.
- `.venv/bin/pytest tests/unit/ -q -k "bar_aggregator or bar_writer or llm_writer"` passes.
- Three atomic commits on the current branch, each scoped to its task.
</verification>

<success_criteria>
- Three atomic commits land: (1) code fixes, (2) systemd unit fixes, (3) SUMMARY.md.
- Host state matches repo state for all four systemd units and the logrotate config.
- `systemctl daemon-reload` has been executed.
- HF-6 and HF-7 verified in place (or restored if regressed).
- Unit tests still pass for the touched modules.
- No service has been restarted as part of this task — restart timing is the operator's call.
</success_criteria>

<output>
After completion, the SUMMARY.md at
`.planning/quick/260528-9as-fix-hf-6-llm-writer-stall-detection-hf-7/260528-9as-SUMMARY.md`
must capture both repo changes and the host-side ops changes (systemd reload, logrotate install) so the audit trail is complete.
</output>
