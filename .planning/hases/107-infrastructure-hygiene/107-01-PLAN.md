---
phase: 107-infrastructure-hygiene
plan: 01
type: execute
wave: 1
depends_on: ["107-00"]
files_modified:
  - services/signal_replay_auditor_agent.py
  - services/bar_replay_provider_agent.py
  - services/swarm_ledger_writer_agent.py
  - services/service_auditor_agent.py
  - src/core/agent/base.py
autonomous: false
must_haves:
  truths:
    - "Service inventory from Wave 0 proves all 42+ services audited with explicit coverage matrix"
    - "signal_replay_auditor_agent inherits from BaseAgent with clean startup, clean SIGTERM shutdown, no orphan tasks"
    - "bar_replay_provider_agent inherits from BaseAgent with clean startup, clean SIGTERM shutdown, no orphan tasks"
    - "All 3 bypass services use DatabaseManager.create_pool() with JSONB codecs and pool closes on teardown"
    - "BaseAgent._last_msg_ts_attrs uses 'agent_id' label key consistently across all 42+ services"
    - "service_auditor_agent queries use 'agent_id' label for metric aggregation"
    - "Regression smoke test passes after Wave 1 deployment (Redpanda → DB flow intact)"
    - "Wave 1 verification SQL query returns TRUE (all 3 criteria pass)"
    - "Grafana dashboards show stable metrics for 1-2 hours after deployment"
    - "Rollback triggers defined (rollback if connection errors > 5/min, orphan tasks detected, missing metrics)"
  artifacts:
    - path: "services/signal_replay_auditor_agent.py"
      provides: "BaseAgent lifecycle migration with acceptance evidence"
      contains: "class SignalReplayAuditorAgent(BaseAgent)"
      min_lines: 50
    - path: "services/bar_replay_provider_agent.py"
      provides: "BaseAgent lifecycle migration with acceptance evidence"
      contains: "class BarReplayProviderAgent(BaseAgent)"
      min_lines: 50
    - path: "services/swarm_ledger_writer_agent.py"
      provides: "DatabaseManager pool standardization"
      contains: "from src.core.database_manager import create_pool as create_db_pool"
    - path: "src/core/agent/base.py"
      provides: "Consistent agent_id label"
      contains: 'self._last_msg_ts_attrs = {"agent_id": name}'
    - path: ".planning/phases/107-infrastructure-hygiene/107-01-ROLLBACK.md"
      provides: "Rollback triggers and procedures"
      contains: "## Rollback Triggers"
    - path: ".planning/phases/107-infrastructure-hygiene/107-01-ACCEPTANCE.md"
      provides: "Service-by-service acceptance evidence"
      contains: "## Service Acceptance Checklist"
  key_links:
    - from: "services/signal_replay_auditor_agent.py"
      to: "src/core/agent/base.py"
      via: "inheritance"
      pattern: "class SignalReplayAuditorAgent\\(BaseAgent\\)"
    - from: "services/signal_replay_auditor_agent.py"
      to: "src/core/database_manager.py"
      via: "create_pool import"
      pattern: "from src.core.database_manager import create_pool"
    - from: "services/bar_replay_provider_agent.py"
      to: "src/core/agent/base.py"
      via: "inheritance"
      pattern: "class BarReplayProviderAgent\\(BaseAgent\\)"
    - from: "services/swarm_ledger_writer_agent.py"
      to: "src/core/database_manager.py"
      via: "create_pool import"
      pattern: "self._pool = await create_db_pool\\("
    - from: "src/core/agent/base.py"
      to: "service_auditor_agent metric queries"
      via: "label key consistency"
      pattern: '"agent_id"'
---

<objective>
Migrate all identified non-compliant services to BaseAgent lifecycle, standardize all bypass services to DatabaseManager pool wrapper, and fix agent_id label consistency across all 42+ services.

Purpose: Wave 1 establishes service consistency — BaseAgent lifecycle provides SIGTERM handling, stall detection, and OTel instrumentation. DatabaseManager pool wrapper ensures JSONB codecs and pool gauges. Consistent agent_id labels enable fleet-wide dashboards. **Wave 0 inventory proves this covers ALL services, not just examples.**

Output: All 42+ services using standard patterns with 100% BaseAgent adoption and DatabaseManager pool usage, verified by regression smoke test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-infrastructure-hygiene/107-CONTEXT.md
@.planning/phases/107-infrastructure-hygiene/107-RESEARCH.md
@.planning/phases/107-infrastructure-hygiene/107-PATTERNS.md
@.planning/phases/107-infrastructure-hygiene/107-00-SUMMARY.md
@.planning/phases/107-infrastructure-hygiene/107-00-SERVICE-INVENTORY.md
@.planning/phases/107-infrastructure-hygiene/107-00-BASELINE.md
@.planning/phases/phase-106/106-02-SUMMARY.md
@.planning/phases/phase-106/106-05-SUMMARY.md

# Only reference prior plan SUMMARYs if genuinely needed
@services/signal_replay_auditor_agent.py
@services/bar_replay_provider_agent.py
@services/swarm_ledger_writer_agent.py
@src/core/agent/base.py
@src/core/database_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate signal_replay_auditor_agent to BaseAgent</name>
  <files>services/signal_replay_auditor_agent.py</files>
  <read_first>
    - services/signal_replay_auditor_agent.py
    - src/core/agent/base.py
    - src/core/database_manager.py
    - .planning/phases/107-infrastructure-hygiene/107-00-SERVICE-INVENTORY.md
  </read_first>
  <action>
    Convert SignalReplayAuditorAgent from custom lifecycle to BaseAgent inheritance:

    1. Add import: `from src.core.agent.base import BaseAgent`
    2. Change class declaration from `class SignalReplayAuditorAgent:` to `class SignalReplayAuditorAgent(BaseAgent):`
    3. In __init__:
       - Replace `self._log = structlog.get_logger(self.agent_id)` with inherited `self.logger` from BaseAgent
       - Replace `self._settings = Settings()` with inherited `self.settings` from BaseAgent
       - Remove `self._stop = asyncio.Event()` (BaseAgent provides self._stop_event)
       - Add `super().__init__(name="signal_replay_auditor", max_idle_seconds=600)` before other initialization
    4. In _setup():
       - Replace `asyncpg.create_pool()` with `from src.core.database_manager import create_pool as create_db_pool` and `self._pool = await create_db_pool(self.settings.database_url, pool_name="signal_replay_auditor", min_size=1, max_size=3)`
       - Replace `self._settings` with `self.settings`
    5. In _run():
       - Replace `while not self._stop.is_set():` with `while self.running:`
       - Replace `self._log` with `self.logger`
    6. In _teardown():
       - Keep producer and pool cleanup code
       - Remove any manual SIGTERM handling (BaseAgent handles this)
       - Ensure DB pool closes: `await self._pool.close()` must be present

    Preserve all existing business logic around audit cycles, replay requests, and unresolved signal tracking.
  </action>
  <verify>
    grep -n "class SignalReplayAuditorAgent(BaseAgent)" services/signal_replay_auditor_agent.py
    grep -n "super().__init__(name=\"signal_replay_auditor\"" services/signal_replay_auditor_agent.py
    grep -n "from src.core.database_manager import create_pool" services/signal_replay_auditor_agent.py
    grep -n "while self.running:" services/signal_replay_auditor_agent.py
    grep -n "await self._pool.close()" services/signal_replay_auditor_agent.py
    ! grep -n "self._stop = asyncio.Event()" services/signal_replay_auditor_agent.py
  </verify>
  <done>
    signal_replay_auditor_agent inherits from BaseAgent with SIGTERM handling, stall detection, OTel instrumentation, uses DatabaseManager.create_pool() with JSONB codecs, and DB pool closes on teardown.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate bar_replay_provider_agent to BaseAgent</name>
  <files>services/bar_replay_provider_agent.py</files>
  <read_first>
    - services/bar_replay_provider_agent.py
    - src/core/agent/base.py
    - src/core/database_manager.py
    - .planning/phases/107-infrastructure-hygiene/107-00-SERVICE-INVENTORY.md
  </read_first>
  <action>
    Convert BarReplayProviderAgent from custom lifecycle to BaseAgent inheritance:

    1. Add import: `from src.core.agent.base import BaseAgent`
    2. Change class declaration from `class BarReplayProviderAgent:` to `class BarReplayProviderAgent(BaseAgent):`
    3. In __init__:
       - Replace `self._log = structlog.get_logger(self.agent_id)` with inherited `self.logger` from BaseAgent
       - Replace `self._settings = Settings()` with inherited `self.settings` from BaseAgent
       - Remove `self._stop = asyncio.Event()` (BaseAgent provides self._stop_event)
       - Add `super().__init__(name="bar_replay_provider", max_idle_seconds=300)` before other initialization
    4. In _setup():
       - Replace `asyncpg.create_pool()` with `from src.core.database_manager import create_pool as create_db_pool` and `self._pool = await create_db_pool(self.settings.database_url, pool_name="bar_replay_provider", min_size=1, max_size=5)`
       - Replace `self._settings` with `self.settings`
    5. In _run():
       - Replace `while not self._stop.is_set():` with `while self.running:`
       - Replace `self._log` with `self.logger`
    6. In _teardown():
       - Keep producer and pool cleanup code
       - Keep self-termination logic (`sys.exit(0)` on completion)
       - Remove any manual SIGTERM handling (BaseAgent handles this)
       - Ensure DB pool closes: `await self._pool.close()` must be present

    Preserve all existing business logic around bar replay, rate limiting, and completion detection.
  </action>
  <verify>
    grep -n "class BarReplayProviderAgent(BaseAgent)" services/bar_replay_provider_agent.py
    grep -n "super().__init__(name=\"bar_replay_provider\"" services/bar_replay_provider_agent.py
    grep -n "from src.core/database_manager import create_pool" services/bar_replay_provider_agent.py
    grep -n "while self.running:" services/bar_replay_provider_agent.py
    grep -n "await self._pool.close()" services/bar_replay_provider_agent.py
    ! grep -n "self._stop = asyncio.Event()" services/bar_replay_provider_agent.py
  </verify>
  <done>
    bar_replay_provider_agent inherits from BaseAgent with SIGTERM handling, stall detection, OTel instrumentation, uses DatabaseManager.create_pool() with JSONB codecs, and DB pool closes on teardown.
  </done>
</task>

<task type="auto">
  <name>Task 3: Standardize swarm_ledger_writer_agent to DatabaseManager pool</name>
  <files>services/swarm_ledger_writer_agent.py</files>
  <read_first>
    - services/swarm_ledger_writer_agent.py
    - src/core/database_manager.py
    - .planning/phases/107-infrastructure-hygiene/107-00-SERVICE-INVENTORY.md
  </read_first>
  <action>
    Replace direct asyncpg.create_pool() call with DatabaseManager.create_pool():

    1. In imports section, add: `from src.core.database_manager import create_pool as create_db_pool`
    2. In _setup() method, replace the pool creation:
       - BEFORE: `self._pool = await asyncpg.create_pool(self.settings.database_url, pool_name="swarm_ledger_writer", min_size=2, max_size=8)`
       - AFTER: `self._pool = await create_db_pool(self.settings.database_url, pool_name="swarm_ledger_writer", min_size=2, max_size=8)`
    3. Remove `import asyncpg` if no other asyncpg usage remains in the file
    4. In _teardown(), ensure DB pool closes: `await self._pool.close()` must be present

    This ensures JSONB codecs are registered and DB_POOL_SIZE/DB_POOL_IDLE gauges are emitted.
  </action>
  <verify>
    grep -n "from src.core.database_manager import create_pool" services/swarm_ledger_writer_agent.py
    grep -n "self._pool = await create_db_pool(" services/swarm_ledger_writer_agent.py
    grep -n "await self._pool.close()" services/swarm_ledger_writer_agent.py
    ! grep -n "asyncpg.create_pool" services/swarm_ledger_writer_agent.py
  </verify>
  <done>
    swarm_ledger_writer_agent uses DatabaseManager.create_pool() with JSONB codecs, pool gauges, and DB pool closes on teardown.
  </done>
</task>

<task type="auto">
  <name>Task 4: Fix agent_id label consistency in BaseAgent</name>
  <files>src/core/agent/base.py</files>
  <read_first>
    - src/core/agent/base.py
    - services/service_auditor_agent.py
    - .planning/phases/107-infrastructure-hygiene/107-00-SERVICE-INVENTORY.md
  </read_first>
  <action>
    Standardize metric label keys to use "agent_id" instead of "agent":

    1. In BaseAgent.__init__() around line 95-100:
       - Find: `self._last_msg_ts_attrs = {"agent": name}`
       - Replace with: `self._last_msg_ts_attrs = {"agent_id": name}`

    This makes BaseAgent crash metrics consistent with BaseWriterAgent persistence metrics (which already use "agent_id").
  </action>
  <verify>
    grep -n 'self._last_msg_ts_attrs = {"agent_id":' src/core/agent/base.py
    ! grep -n '"agent": name' src/core/agent/base.py
  </verify>
  <done>
    BaseAgent uses "agent_id" label consistently for crash metrics, matching BaseWriterAgent pattern.
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify service_auditor metric queries use agent_id</name>
  <files>services/service_auditor_agent.py</files>
  <read_first>
    - services/service_auditor_agent.py
    - src/core/agent/base.py
    - .planning/phases/107-infrastructure-hygiene/107-00-SERVICE-INVENTORY.md
  </read_first>
  <action>
    Verify service_auditor_agent Prometheus queries use "agent_id" label for metric aggregation:

    1. Check all Prometheus HTTP query calls in service_auditor_agent.py
    2. For each query targeting agent_last_message_timestamp_seconds:
       - Verify it uses `agent_id` label filter: `agent_last_message_timestamp_seconds{agent_id=~".+"}`
       - If any use `agent` label, update to `agent_id`
    3. Check metric name references against _AGENT_ID_TO_UNIT mappings
    4. Ensure query labels match the label keys emitted by BaseAgent and BaseWriterAgent

    The goal is fleet-wide dashboards can aggregate across all services without manual union queries.
  </action>
  <verify>
    grep -n 'agent_id=' services/service_auditor_agent.py | grep -v '_AGENT_ID_TO_UNIT'
    ! grep -n 'agent=' services/service_auditor_agent.py | grep 'prometheus_http_query' || true
  </verify>
  <done>
    service_auditor_agent metric queries use agent_id label for fleet-wide aggregation.
  </done>
</task>

<task type="auto">
  <name>Task 6: Run regression smoke test after Wave 1 changes</name>
  <files>tests/integration/test_pipeline_flow.py</files>
  <read_first>
    - tests/integration/test_pipeline_flow.py
    - .planning/phases/107-infrastructure-hygiene/107-00-BASELINE.md
  </read_first>
  <action>
    Run regression smoke test to prove hot path still works after Wave 1 migrations:

    1. Execute smoke test:
       - Run: `pytest tests/integration/test_pipeline_flow.py -v`
       - Compare results to baseline (107-00-BASELINE.md)
       - Test MUST pass (Redpanda → DB flow intact)

    2. If test fails:
       - DO NOT proceed to deployment
       - Investigate which migration broke the hot path
       - Fix and re-test until smoke test passes

    3. Document regression check:
       - Add results to 107-01-SUMMARY.md
       - Note: "Regression smoke test: PASS" with counts

    This prevents deploying broken migrations to production.
  </action>
  <verify>
    grep -n "test_pipeline_flow_baseline.*PASSED" .planning/phases/107-infrastructure-hygiene/107-01-SUMMARY.md
    ! grep -n "test_pipeline_flow_baseline.*FAILED" .planning/phases/107-infrastructure-hygiene/107-01-SUMMARY.md
  </verify>
  <done>
    Regression smoke test passes after Wave 1 code changes, proving hot path intact.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Wave 1 deployment</name>
  <what-built>Wave 1 complete: All non-compliant services from inventory migrated to BaseAgent (signal_replay_auditor, bar_replay_provider, [others from 107-00-SERVICE-INVENTORY.md]), all bypass services standardized to DatabaseManager pool ([list from inventory]), agent_id label consistency fixed across BaseAgent and service_auditor queries. Regression smoke test passed before deployment.</what-built>
  <how-to-verify>
    **Pre-deployment checks:**

    1. Verify regression smoke test passed:
       - Check 107-01-SUMMARY.md for "test_pipeline_flow_baseline: PASSED"
       - If test failed, DO NOT deploy

    2. Deploy changes: Restart migrated services via systemctl:
       ```bash
       sudo systemctl restart indicagent-signal-replay-auditor indicant-bar-replay-provider indicant-swarm-ledger-writer
       ```

    **Service-by-service acceptance evidence (MUST collect for each migrated service):**

    3. For each migrated service (signal_replay_auditor, bar_replay_provider, swarm_ledger_writer):
       - [ ] Clean startup:
           - Check: `systemctl status indicant-signal-replay-auditor`
           - Expected: `Active: active (running)` since X seconds ago
           - Check: `journalctl -u indicant-signal-replay-auditor -n 30 --no-pager`
           - Expected: No exceptions in first 30s, no traceback

       - [ ] Clean SIGTERM shutdown:
           - Run: `sudo systemctl kill -s SIGTERM indicant-signal-replay-auditor`
           - Check: `journalctl -u indicant-signal-replay-auditor -n 20 --no-pager`
           - Expected: Logs show "Draining queue", "Flushing buffers", "Shutdown complete"
           - Verify: Service exits cleanly (no systemd timeout)

       - [ ] No orphan asyncio tasks:
           - Check: `journalctl -u indicant-signal-replay-auditor --no-pager | grep "task was destroyed but it is pending"`
           - Expected: No warnings about pending tasks

       - [ ] Stall detection fires:
           - Stop Kafka consumer (simulate stall): `docker exec redpanda rkg cluster delete-group signal_replay_auditor`
           - Wait 60 seconds
           - Check Prometheus: `curl http://localhost:9100/metrics | grep agent_last_message_timestamp_seconds`
           - Expected: Metric stops incrementing for signal_replay_auditor
           - Restore consumer: Restart service

       - [ ] DB pool closes on teardown:
           - Check: `journalctl -u indicant-signal-replay-auditor --no-pager | grep "Closing database connection"`
           - Expected: Log message present on shutdown

       - [ ] Metrics visible:
           - Check: `curl http://localhost:9100/metrics | grep signal_replay_auditor`
           - Expected: agent_id label present, metrics emitting

    **Wave-level verification:**

    4. Run Wave 1 verification SQL query from 107-CONTEXT.md:
       ```sql
       SELECT
         (SELECT COUNT(*) FROM services WHERE inherits_from_base_agent = TRUE) = 42 as hyg07_pass,
         (SELECT COUNT(*) FROM database_access_patterns WHERE uses_database_manager_pool = TRUE AND service_category = 'writer') = 3 as hyg08_pass,
         (SELECT COUNT(DISTINCT label_key) = 1 FROM otel_metrics WHERE metric_name LIKE '%agent%' AND label_key IN ('agent', 'agent_id')) as hyg09_pass;
       ```
       Expected: All three return TRUE.

    5. Monitor Grafana dashboards for 1-2 hours:
       - Check "Service Health" dashboard: All services show green (running)
       - Check "Agent Crash Rate" dashboard: agent_id label aggregation works
       - Check "Database Pool Health" dashboard: All services emit pool gauges
       - Check logs: `tail -50 logs/signal_replay_auditor_agent.log`
       - Verify no errors, no AttributeError exceptions, all metrics emitting

    **Rollback triggers (deploy only if ALL pass):**

    Rollback if ANY of:
    - Connection errors > 5 per minute in logs (grep "connection" logs/*_agent.log | wc -l)
    - Orphan asyncio tasks detected (grep "task was destroyed" logs/*_agent.log)
    - Missing metrics (curl http://localhost:9100/metrics | grep agent_id | wc -l drops)
    - Service fails to start (systemctl status shows failed)
    - Service fails to shutdown (systemctl stop times out)
    - Regression smoke test fails (pytest tests/integration/test_pipeline_flow.py)

    Only proceed to Wave 2 if:
    - SQL query returns TRUE for all 3 criteria
    - Grafana dashboards show stable metrics for 1-2 hours (no anomalies)
    - All services pass service-by-service acceptance checks
    - No rollback triggers fired
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Wave 2, or describe issues to rollback</resume-signal>
</task>

</tasks>

<verification>
Wave 1 verification queries:

```sql
-- BaseAgent adoption (100% coverage)
SELECT COUNT(*) = 42 as base_agent_adoption_complete
FROM (
  SELECT 1 FROM services WHERE inherits_from_base_agent = TRUE
  UNION ALL
  SELECT 1 FROM information_schema WHERE class_name LIKE '%Agent' AND inherits_from = 'BaseAgent'
) t;

-- DatabaseManager pool adoption (all bypass services fixed)
SELECT COUNT(*) = 3 as bypass_services_fixed
FROM service_audit_results
WHERE service_name IN ('signal_replay_auditor', 'bar_replay_provider', 'swarm_ledger_writer')
AND uses_database_manager_pool = TRUE;

-- agent_id label consistency (100% coverage)
SELECT COUNT(DISTINCT label_key) = 1 as single_label_key
FROM otel_metrics
WHERE metric_name LIKE '%agent%'
AND label_key IN ('agent', 'agent_id');

-- Rollback trigger checks (should all be 0)
SELECT
  (SELECT COUNT(*) FROM logs WHERE message LIKE '%connection error%' AND timestamp > NOW() - INTERVAL '1 hour') as connection_errors_last_hour,
  (SELECT COUNT(*) FROM logs WHERE message LIKE '%task was destroyed%' AND timestamp > NOW() - INTERVAL '1 hour') as orphan_tasks_last_hour,
  (SELECT COUNT(DISTINCT agent_id) FROM metrics WHERE metric_name LIKE '%agent%' AND timestamp > NOW() - INTERVAL '5 minutes') as active_metrics_count;
```

Success criteria:
- base_agent_adoption_complete = TRUE (42/42 services)
- bypass_services_fixed = TRUE (3/3 services)
- single_label_key = TRUE (all use agent_id)
- connection_errors_last_hour = 0 (no DB connection errors)
- orphan_tasks_last_hour = 0 (no async task leaks)
- active_metrics_count >= 42 (all services emitting metrics)
</verification>

<success_criteria>
1. Service inventory from Wave 0 proves all 42+ services covered
2. All non-compliant services from inventory migrated to BaseAgent
3. All bypass services from inventory use DatabaseManager.create_pool()
4. BaseAgent uses "agent_id" label key consistently across all services
5. service_auditor metric queries aggregate using "agent_id" label
6. Regression smoke test passes after Wave 1 code changes
7. Each migrated service passes acceptance checklist (startup, shutdown, stall detection, DB pool, metrics)
8. Systemctl restart of migrated services succeeds without errors
9. All services emit metrics with consistent agent_id labels
10. Wave 1 verification query returns TRUE (all 3 criteria pass)
11. Grafana dashboards stable for 1-2 hours post-deployment
12. No rollback triggers fired
</success_criteria>

<output>
After completion, create `.planning/phases/107-infrastructure-hygiene/107-01-SUMMARY.md` including:
- List of all migrated services (from 107-00-SERVICE-INVENTORY.md)
- Regression smoke test results
- Service-by-service acceptance evidence (checklist for each service)
- Rollback trigger status (all clear)
</output>
