---
phase: 142B-frame-simulation-counterfactual-tracking
plan: 02
type: execute
wave: 2
depends_on: [142B-01]
files_modified:
  - services/counterfactual_tracker.py
  - src/observability/metrics.py
  - services/service_auditor.py
  - tests/unit/test_counterfactual_tracker_exit_priority.py
  - tests/unit/test_frame_gate.py
  - tests/unit/test_counterfactual_tracker.py
  - .planning/todos/pending/089-ensemble-ic-engine-recurring-cadence.md
autonomous: true
requirements: [FRAME-02, FRAME-03, FRAME-04]
must_haves:
  truths:
    - "Each open frame is closed via exactly one of closed_stop / closed_target / closed_max_hold / closed_ic_decay in strict priority order"
    - "counterfactual_pnl_r / mfe / mae / bars / exit_reason populated for each closed frame; frames are immutable once closed"
    - "Bar-path scan uses a named (server-side) psycopg2 cursor with itersize — never a plain cursor (anti-OOM)"
    - "ProcessPoolExecutor workers return list[dict] only; the single serial write happens in the main process (DAG invariant #3)"
    - "FRAME-04 gate returns pass/fail via scipy bootstrap one-tailed 95% CI per (tf, regime), respecting alpha.scoring.min_strategy_n"
    - "The age of the alpha_ensemble_ic row read for the IC-decay trigger is instrumented (D-10), and the read is never blocked on freshness (D-08)"
  artifacts:
    - path: "services/counterfactual_tracker.py"
      provides: "CounterfactualTracker(BaseBatch) FRAME-02/03 lifecycle + FRAME-04 gate mode"
      exports: ["CounterfactualTracker", "determine_exit", "frame_gate_passes"]
      min_lines: 200
    - path: "src/observability/metrics.py"
      provides: "IC-staleness point gauge for the IC-decay trigger read"
      contains: "counterfactual_tracker_ic_row_age_seconds"
    - path: ".planning/todos/pending/089-ensemble-ic-engine-recurring-cadence.md"
      provides: "D-10 follow-on: recurring ensemble_ic_engine cadence, non-blocking future work"
  key_links:
    - from: "services/counterfactual_tracker.py"
      to: "market_data_ohlcv"
      via: "named server-side psycopg2 cursor with itersize inside ProcessPoolExecutor worker"
      pattern: "conn.cursor\\(name="
    - from: "services/counterfactual_tracker.py"
      to: "alpha.scoring.min_strategy_n"
      via: "frame_gate_passes reads min_n from APR"
      pattern: "min_strategy_n"
    - from: "services/counterfactual_tracker.py"
      to: "alpha_ensemble_ic"
      via: "most-recent-row read for IC-decay trigger, age instrumented"
      pattern: "counterfactual_tracker_ic_row_age_seconds"
---

<objective>
Build `CounterfactualTracker` — the `BaseBatch` oneshot that fills each open frame's geometry from the
T+1 open, scans the subsequent price path to close it via the four-trigger state machine
(FRAME-02/03), scores counterfactual P&L, and evaluates the FRAME-04 exit gate (bootstrap CI per
(tf, regime)). Depends on Plan 01's migration and AlphaFrameWriter output.

Purpose: this is where hypothetical positions become measured counterfactual outcomes — the number
that answers Phase 142B's binary question (can a sensible frame capture the IC as positive P&L). The
correctness-critical requirement is fidelity to the codebase's proven anti-OOM cursor pattern: a
plain psycopg2 cursor at this scale has crashed production twice this week.

Output: `services/counterfactual_tracker.py` (pure-fn exit/gate cores + service + gate mode), an
IC-staleness gauge, service registration, RED→GREEN tests, and the D-10 follow-on todo.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/142B-frame-simulation-counterfactual-tracking-planned/142B-CONTEXT.md
@.planning/phases/142B-frame-simulation-counterfactual-tracking-planned/142B-RESEARCH.md
@.planning/phases/142B-frame-simulation-counterfactual-tracking-planned/142B-PATTERNS.md
@services/alpha_frame_writer.py
@production/migrations/214_alpha_frames_schema.sql

<interfaces>
<!-- alpha_frames (written by Plan 01; this plan updates it) -->
alpha_frames: PK frame_id; status CHECK ('open','closed_stop','closed_target','closed_max_hold','closed_ic_decay');
  geometry cols entry_price/stop_price/target_price/r_multiple/max_hold_bars/stop_atr_mult;
  outcome cols counterfactual_pnl_r/counterfactual_mfe/counterfactual_mae/counterfactual_bars/exit_reason/closed_at/measured_at;
  symbol, tf, bar_ts, regime, direction. UNIQUE (event_id, bar_ts, frame_variant).

<!-- alpha_ensemble_ic (read for IC-decay trigger) — live schema -->
alpha_ensemble_ic: cols event_row_id, symbol, tf, regime, lookahead, ic_ci_lower double, scored_at timestamptz.
  Index (symbol, tf, regime, lookahead, scored_at DESC). Read the most-recent row per (symbol,tf,regime).

<!-- market_data_ohlcv (bar-path scan source) -->
market_data_ohlcv: time col `timestamp`, tf col `timeframe`, cols open/high/low/close; hypertable 250 chunks;
  index idx_ohlcv_symbol_tf_time (symbol, timeframe, timestamp DESC).

<!-- Reuse from Plan 01: -->
services.alpha_frame_writer.compute_frame_geometry(direction, entry_price, atr, stop_atr_mult, target_r_multiple)

<!-- ensemble_ic_engine worker pattern (analog) — named cursor precondition: conn.commit() before conn.cursor(name=...) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure-fn cores — determine_exit (FRAME-02/03), frame_gate_passes (FRAME-04), IC-staleness gauge</name>
  <files>services/counterfactual_tracker.py, src/observability/metrics.py, tests/unit/test_counterfactual_tracker_exit_priority.py, tests/unit/test_frame_gate.py</files>
  <read_first>
    - .planning/phases/142B-frame-simulation-counterfactual-tracking-planned/142B-RESEARCH.md (Code Examples: determine_exit + frame_gate_passes reference shapes; scipy.stats.bootstrap usage)
    - .planning/phases/142B-frame-simulation-counterfactual-tracking-planned/142B-PATTERNS.md (Shared Patterns: bootstrap CI gate, IC-staleness observability gauge)
    - src/observability/metrics.py (point-gauge helper `_meter.create_gauge` + ALPHA_PUBLISHER_* definition style; .set(value, attrs) usage)
    - .planning/ROADMAP.md (FRAME-02 exit-trigger priority text, FRAME-04 gate text)
    - .planning/phases/142B-frame-simulation-counterfactual-tracking-planned/142B-CONTEXT.md (D-08 read regardless of age; D-10 instrument age)
  </read_first>
  <behavior>
    - determine_exit priority (checked per-bar, in order): (1) bar.low <= stop_price → closed_stop; (2) bar.high >= target_price → closed_target; (3) i >= hold_max_bars → closed_max_hold. If a bar hits BOTH stop and target, stop wins (checked first — conservative). After the bar loop with no per-bar exit: (4) ic_ci_lower is not None and ic_ci_lower < 0 → closed_ic_decay; else None (still open).
    - determine_exit returns bars=i (1-indexed from entry) and the exit_price of the triggering rule (stop_price / target_price / bar.close).
    - frame_gate_passes(pnl_r_values, min_n): if len < min_n → (False, nan, nan). Else scipy bootstrap(np.mean, confidence_level=0.95, alternative='greater', method='BCa') → passes iff ci_lower > 0; returns (passes, ci_lower, ci_upper).
    - A degenerate all-equal-positive sample of length >= min_n returns passes=True; an all-negative sample returns passes=False.
    - IC-staleness gauge is a point gauge (create_gauge) named counterfactual_tracker_ic_row_age_seconds, set via .set(age_seconds, {"symbol","tf","regime"}).
  </behavior>
  <action>
    Create `services/counterfactual_tracker.py` with the pure-function cores (service class comes in
    Task 2 — start the file here so the tests can import). Implement:

    `determine_exit(bars_since_entry, stop_price, target_price, hold_max_bars, ic_ci_lower) ->
    ExitResult | None` exactly per <behavior> priority order (stop > target > max_hold > ic_decay).
    Define a small `@dataclass ExitResult(status: str, bars: int, exit_price: float | None)` and a
    `Bar` protocol/namedtuple with low/high/close. The IC-decay branch reads ic_ci_lower regardless of
    the underlying row's age (D-08) — this function takes the value already fetched, it does not gate on
    freshness. This is the pure-function target styled after ensemble_ic_engine.py's
    _select_hold_bars_from_decay.

    `frame_gate_passes(pnl_r_values: np.ndarray, min_n: int) -> tuple[bool, float, float]` using
    `from scipy.stats import bootstrap` with `np.mean`, `confidence_level=0.95, alternative="greater",
    method="BCa"`; return (ci_lower > 0, ci_lower, ci_upper); short-circuit to (False, nan, nan) when
    len(pnl_r_values) < min_n. Do NOT hand-roll a percentile-bootstrap loop (Don't Hand-Roll).

    In `src/observability/metrics.py`: add `COUNTERFACTUAL_TRACKER_IC_ROW_AGE_SECONDS =
    _meter.create_gauge("counterfactual_tracker_ic_row_age_seconds", description=...)` alongside the
    ALPHA_PUBLISHER_* definitions, with a description noting D-08 (no freshness gate) / D-10 (makes
    staleness observable). Point gauge — set via `.set(value, attrs)`, direct OTel SDK, never
    prometheus_client.

    Create `tests/unit/test_counterfactual_tracker_exit_priority.py` covering: stop-only, target-only,
    both-hit-same-bar (stop wins), max-hold reached with no stop/target, ic_decay fires only when no
    per-bar exit and ic_ci_lower < 0, still-open returns None, ic_ci_lower=None never triggers decay.
    Create `tests/unit/test_frame_gate.py` covering: below-min_n short-circuit, all-positive passes,
    all-negative fails, ci_lower/ci_upper ordering.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_counterfactual_tracker_exit_priority.py tests/unit/test_frame_gate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - determine_exit returns closed_stop when a bar's low <= stop_price even if the same bar's high >= target_price (priority proven)
    - determine_exit returns closed_max_hold with bars == hold_max_bars when neither stop nor target hit within the horizon
    - determine_exit returns closed_ic_decay only after the bar loop, only when ic_ci_lower is not None and < 0
    - frame_gate_passes([...], min_n) returns (False, nan, nan) when len < min_n
    - frame_gate_passes on an all-negative array returns passes=False; on a strongly-positive array returns passes=True
    - `grep -c "conn.cursor()" services/counterfactual_tracker.py` returns 0 (no plain cursor introduced even in this scaffold)
    - `grep -c "counterfactual_tracker_ic_row_age_seconds" src/observability/metrics.py` returns >= 1
    - Both test files pass
  </acceptance_criteria>
  <done>The exit-priority state machine, the scipy bootstrap gate, and the IC-staleness gauge are implemented as importable pure/simple units with RED→GREEN unit coverage; no plain cursor exists in the module.</done>
</task>

<task type="auto">
  <name>Task 2: CounterfactualTracker service — geometry fill, ProcessPoolExecutor scan, serial write</name>
  <files>services/counterfactual_tracker.py, services/service_auditor.py, tests/unit/test_counterfactual_tracker.py</files>
  <read_first>
    - services/ensemble_ic_engine.py (ProcessPoolExecutor per-symbol dispatch lines ~634-691; named server-side cursor lines ~710-716; main-process result aggregation ~966-973; manifest-wrapped execute ~877-887)
    - services/alpha_publisher.py (BaseBatch CLI entrypoint, DSN-strip, init_otel_providers, --flag pattern)
    - scripts/infrastructure/backfill/infrastructure_run_historical_pipeline.py (lines ~1044-1052: connection-staleness SELECT 1 + reconnect before each unit of work)
    - services/service_auditor.py (_DAG_ORDER / _ONESHOT_UNITS — mirror the alpha-frame-writer entries from Plan 01)
    - tests/unit/test_ic_engine_compute_split.py (lines ~55-74: grep-based "worker never writes" guard idiom)
    - src/observability/metrics.py (the gauge added in Task 1)
    - services/alpha_frame_writer.py (reuse compute_frame_geometry for the T+1 fill)
  </read_first>
  <action>
    Extend `services/counterfactual_tracker.py` with `class CounterfactualTracker(BaseBatch)`,
    `job_name = "counterfactual-tracker"`, `compute_version = "1.0.0"`, constructor
    `__init__(self, db_dsn: str, backfill: bool = False)`. Manifest-wrapped `execute()` mirroring
    ensemble_ic_engine.py (CorpusManifest("counterfactual_tracker", ...); `except Exception as error:`).

    Wave A — geometry fill (main process): for each open frame, fetch the T+1 open bar from
    market_data_ohlcv (time col `timestamp`, tf col `timeframe`) and populate entry_price, then compute
    stop_price/target_price/r_multiple by calling the imported `compute_frame_geometry(direction,
    entry_price, atr, stop_atr_mult, target_r_multiple)` from alpha_frame_writer.py — ATR read from
    feature_vectors for the frame's (symbol, tf, bar_ts) cell; stop_atr_mult/target_r_multiple from APR.

    Wave B — outcome scan (ProcessPoolExecutor, one task per symbol): mirror ensemble_ic_engine.py's
    `_run_ensemble_ic_worker`. Define `_run_counterfactual_worker(args) -> dict` that opens ONE
    read-only psycopg2 connection for the symbol, loops its (tf, open_frame) set, and for each frame
    scans the subsequent bars over the range (bar_ts .. bar_ts + max_hold_bars) using a NAMED
    server-side cursor: `conn.commit()` (clear txn — required precondition), then
    `with conn.cursor(name=f"cf_scan_{symbol}_{tf}", cursor_factory=psycopg2.extras.RealDictCursor) as
    cur: cur.itersize = <infra.counterfactual_tracker.itersize>; cur.execute(SQL, params); for r in
    cur: ...` — NEVER a plain `conn.cursor()` (this is the single highest-priority correctness
    requirement — plain cursors buffer the entire result client-side and have OOM-crashed production
    twice: ic_engine commit e9b3bcde, ensemble_ic_engine migration 209/212). Feed the streamed bars to
    `determine_exit(...)` and compute counterfactual_pnl_r (in R units relative to entry/stop distance),
    counterfactual_mfe, counterfactual_mae, counterfactual_bars, exit_reason. The IC-decay trigger reads
    the most-recent alpha_ensemble_ic row for the frame's (symbol, tf, regime) via the
    (symbol,tf,regime,scored_at DESC) index regardless of its age (D-08), and sets
    COUNTERFACTUAL_TRACKER_IC_ROW_AGE_SECONDS with age = now - scored_at per (symbol,tf,regime) (D-10)
    — instrument in the main process (workers should return scored_at so the gauge is set outside the
    worker, keeping workers write-free and metric-free). Each worker RETURNS a `list[dict]` of outcome
    rows — it NEVER opens a write connection, never calls executemany/execute_batch/commit-for-write
    (DAG invariant #3). Add a `SELECT 1` staleness check + reconnect before each per-symbol unit of work.

    Main process: after `ProcessPoolExecutor(max_workers=<infra.counterfactual_tracker.workers>).map(
    _run_counterfactual_worker, args, chunksize=1)` completes, aggregate all returned rows and do
    EXACTLY ONE serial async batch UPDATE via the pool (`await wconn.executemany(_UPDATE_SQL, rows)`)
    setting status/counterfactual_*/closed_at/measured_at. Frames are immutable once closed — the
    UPDATE targets `WHERE frame_id = $1 AND status = 'open'` so a re-run never re-closes a closed frame.

    --backfill mode: same anti-join scoping principle as Plan 01 — the pending set is
    `WHERE status = 'open'`; chunk per-symbol and flush incrementally, no long-running write
    transaction (D-05). Standard CLI entrypoint (`init_otel_providers("indicagent-counterfactual-
    tracker")`, DSN strip, `--backfill` flag, `asyncio.run(...)`).

    In `services/service_auditor.py`: add `"indicagent-counterfactual-tracker": 8` to _DAG_ORDER and
    `"indicagent-counterfactual-tracker"` to _ONESHOT_UNITS (mirror alpha-frame-writer). No
    _AGENT_ID_TO_UNIT entry.

    Create `tests/unit/test_counterfactual_tracker.py`: the grep-based worker-no-write guard —
    `inspect.getsource(_run_counterfactual_worker)` must NOT contain 'execute_batch' or 'executemany';
    and must NOT contain a plain `conn.cursor()` (assert `"conn.cursor(name=" ` appears and
    `"conn.cursor()"` does not). A leading `conn.commit()` is permitted (txn-boundary reset before the
    named cursor).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_counterfactual_tracker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "conn.cursor(name=" services/counterfactual_tracker.py` returns >= 1
    - `grep -c "conn.cursor()" services/counterfactual_tracker.py` returns 0 (no plain cursor anywhere)
    - `inspect.getsource(_run_counterfactual_worker)` contains no 'execute_batch' and no 'executemany' (test asserts this)
    - The single main-process UPDATE targets `WHERE ... status = 'open'` (immutability guard; grep present)
    - `grep -c "indicagent-counterfactual-tracker" services/service_auditor.py` returns >= 2
    - Worker function signature returns a list/dict (serializable), opens no write connection (grep: no pool.acquire / no asyncpg inside the worker fn)
    - Exception handler uses `as error`
    - test_counterfactual_tracker.py passes
  </acceptance_criteria>
  <done>CounterfactualTracker fills geometry from the T+1 open, scans price paths with a named server-side cursor inside write-free ProcessPoolExecutor workers, closes each frame via determine_exit into an immutable single serial UPDATE, instruments IC-row age, and is registered as a oneshot; the worker-contract test is green.</done>
</task>

<task type="auto">
  <name>Task 3: FRAME-04 gate evaluation mode + D-10 follow-on todo</name>
  <files>services/counterfactual_tracker.py, tests/unit/test_counterfactual_tracker.py, .planning/todos/pending/089-ensemble-ic-engine-recurring-cadence.md</files>
  <read_first>
    - services/counterfactual_tracker.py (frame_gate_passes from Task 1; execute/CLI structure from Task 2)
    - .planning/ROADMAP.md (FRAME-04 gate text: in-sample only, N >= alpha.scoring.min_strategy_n per (tf, regime))
    - .planning/phases/142B-frame-simulation-counterfactual-tracking-planned/142B-CONTEXT.md (D-01 gross gate; D-09/D-10 cadence out of scope + follow-on todo)
    - .planning/todos/pending/088-hold-max-bars-censoring-not-tracked.md (format/style of an existing pending todo)
    - Key decisions: alpha.validation.oos_start defines the in-sample/OOS boundary (STATE.md)
  </read_first>
  <action>
    Add an `--evaluate-gate` CLI mode to `CounterfactualTracker` (a distinct entrypoint branch; do not
    add a third service — this keeps ROADMAP's 2-service scope). When invoked, it queries closed
    alpha_frames with `frame_variant='primary'` and `bar_ts < alpha.validation.oos_start` (in-sample
    only), groups by (tf, regime), and for each cell calls `frame_gate_passes(pnl_r_array, min_n)` with
    `min_n = ConfigService.get('alpha.scoring.min_strategy_n')`. The gate evaluates on GROSS
    counterfactual_pnl_r (D-01) — do NOT subtract cost. Emit a per-cell verdict (tf, regime, N,
    ci_lower, ci_upper, passes) to the log via structlog and to the CorpusManifest, and a phase-level
    roll-up. This uses the pure `frame_gate_passes` already unit-tested in Task 1 — the mode is the
    query + grouping + logging wiring around it.

    File the D-10 follow-on todo `.planning/todos/pending/089-ensemble-ic-engine-recurring-cadence.md`
    (next number after 088): title it "ensemble_ic_engine recurring cadence"; body states that
    CounterfactualTracker's IC-decay trigger currently reads the most-recent alpha_ensemble_ic row
    regardless of age (D-08) and instruments the age (D-10), but no recurring ensemble_ic_engine
    schedule exists — establishing one is explicitly out of scope for Phase 142B (D-09, would blur the
    142A/142B measurement boundary). Mark it non-blocking future work; reference D-08/D-09/D-10 and the
    counterfactual_tracker_ic_row_age_seconds gauge as the observability hook that will show if
    staleness ever becomes material.

    Extend `tests/unit/test_counterfactual_tracker.py` with a test that the gate-evaluation grouping
    helper (extract the pure grouping/aggregation as a small testable function taking an in-memory list
    of (tf, regime, pnl_r) rows) applies frame_gate_passes per cell and respects the min_n floor — no
    DB. Assert the gate operates on gross pnl_r (the helper takes gross values; there is no cost
    subtraction in its signature/body — grep the helper source for absence of 'cost').
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_counterfactual_tracker.py -x -q && test -f .planning/todos/pending/089-ensemble-ic-engine-recurring-cadence.md</automated>
  </verify>
  <acceptance_criteria>
    - CounterfactualTracker has an `--evaluate-gate` CLI branch that filters `bar_ts < alpha.validation.oos_start` and `frame_variant='primary'` (grep both strings present)
    - The gate reads min_n from `alpha.scoring.min_strategy_n` (grep present) and calls frame_gate_passes per (tf, regime)
    - The gate-evaluation helper contains no 'cost' subtraction (gross-only gate, D-01) — grep the helper source returns 0 for cost-subtraction
    - .planning/todos/pending/089-ensemble-ic-engine-recurring-cadence.md exists and references D-08/D-09/D-10 and counterfactual_tracker_ic_row_age_seconds
    - test_counterfactual_tracker.py passes (including the new grouping test)
  </acceptance_criteria>
  <done>The FRAME-04 gate runs in-sample per (tf, regime) on gross counterfactual_pnl_r via the scipy bootstrap core, respecting min_strategy_n, and the D-10 follow-on cadence todo is filed as non-blocking future work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| market_data_ohlcv / alpha_ensemble_ic (DB) → worker | Internal reads inside ProcessPoolExecutor subprocess; read-only psycopg2 connection |
| worker → main process | Serializable list[dict] only (DAG invariant #3); no worker DB write |
| main process → alpha_frames (DB) | Single serial async batch UPDATE, parameterized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142B-04 | Denial of Service (self-inflicted, demonstrated) | Per-symbol bar-path scan over market_data_ohlcv (up to ~392K bars/cell, 12M+ frames) | mitigate | Named (server-side) psycopg2 cursor with itersize — never a plain cursor. This exact bug OOM-crashed production twice this week (ic_engine e9b3bcde, ensemble_ic_engine 209/212). Highest-priority mitigation; enforced by test_counterfactual_tracker.py grep guard |
| T-142B-05 | Tampering | Range-scan / UPDATE / gate SQL | mitigate | Parameterized queries only ($1/%s); no string-interpolated SQL of symbol/tf/ts values (V5 ASVS) |
| T-142B-06 | Elevation of Privilege / Integrity | Worker writing DB directly | mitigate | Workers return list[dict]; single serial write in main process only (DAG invariant #3); test greps worker source for absence of executemany/execute_batch |
| T-142B-07 | Tampering (config) | Numeric params (itersize, workers, min_strategy_n, stop/target mults) | mitigate | All load via APR/ConfigService (seeded by Plan 01 migration 214); no hardcoded thresholds (CLAUDE.md APR mandate, V5) |
| T-142B-SC | Tampering | package installs | accept | Zero new third-party packages (scipy/psycopg2/asyncpg/numpy all already installed and load-bearing) — no supply-chain surface |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_counterfactual_tracker_exit_priority.py tests/unit/test_frame_gate.py tests/unit/test_counterfactual_tracker.py -x -q` green
- `.venv/bin/pytest tests/unit/ -q` shows no new failures
- No plain `conn.cursor()` anywhere in counterfactual_tracker.py; worker function is write-free
- Migration 214's APR keys (itersize, workers, min_strategy_n) resolve at runtime (no ConfigService default fallback firing silently)
</verification>

<success_criteria>
- Four-trigger state machine closes each frame in strict priority order; frames immutable once closed
- counterfactual_pnl_r/mfe/mae/bars/exit_reason populated via a single serial main-process UPDATE
- Bar scan uses a named server-side cursor; ProcessPoolExecutor workers are write-free and return list[dict]
- FRAME-04 gate passes/fails per (tf, regime) via scipy bootstrap one-tailed 95% CI on GROSS pnl_r, respecting alpha.scoring.min_strategy_n
- IC-decay row age instrumented (D-10), read never freshness-gated (D-08); D-09 cadence deferred via todo 089
- indicagent-counterfactual-tracker registered as a oneshot only
</success_criteria>

<output>
Create `.planning/phases/142B-frame-simulation-counterfactual-tracking-planned/142B-02-SUMMARY.md` when done
</output>
