---
phase: 142B-frame-simulation-counterfactual-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/214_alpha_frames_schema.sql
  - services/alpha_frame_writer.py
  - services/service_auditor.py
  - docs/plans/SHADOW-REVIEW.md
  - tests/unit/test_alpha_frames_schema.py
  - tests/unit/test_alpha_frame_writer_geometry.py
  - tests/unit/test_alpha_frame_writer.py
autonomous: true
requirements: [FRAME-01, FRAME-03]
must_haves:
  truths:
    - "alpha_frames table exists with lifecycle CHECK constraint: closed_ic_decay present, closed_reversal absent"
    - "Every alpha_events row maps to at most one primary alpha_frames row via an idempotent content_key-derived frame_id"
    - "AlphaFrameWriter --backfill processes the full alpha_events backlog in chunks with no long-running write transaction"
    - "Target price uses the ATR path as the sole path (sr_resist_dist is 100% NULL); stop/target/hold all load from APR"
    - "gross_expected_r / cost_r / net_expected_r diagnostic snapshots (D-03) are populated non-NULL on every written frame: gross_expected_r = abs(alpha_score) * alpha.frame.target_r_multiple, net_expected_r = gross_expected_r - cost_r"
    - "SHADOW-REVIEW.md exists, frozen, with gross-pnl gate criteria (D-01) and net_expected_r as a mandatory reporting column (D-02)"
  artifacts:
    - path: "production/migrations/214_alpha_frames_schema.sql"
      provides: "alpha_frames table + alpha.frame.* / alpha.scoring.min_strategy_n / infra.* APR key seeds"
      contains: "CREATE TABLE IF NOT EXISTS alpha_frames"
    - path: "services/alpha_frame_writer.py"
      provides: "AlphaFrameWriter(BaseBatch) FRAME-01 geometry + chunked write"
      exports: ["AlphaFrameWriter"]
      min_lines: 120
    - path: "docs/plans/SHADOW-REVIEW.md"
      provides: "Frozen pre-commitment gate criteria for Phase 147 promotion"
      contains: "counterfactual_pnl_r"
  key_links:
    - from: "services/alpha_frame_writer.py"
      to: "alpha_events / alpha_frames"
      via: "anti-join LEFT JOIN alpha_frames ... WHERE af.frame_id IS NULL, chunked executemany insert"
      pattern: "LEFT JOIN alpha_frames"
    - from: "production/migrations/214_alpha_frames_schema.sql"
      to: "config_schema / config_state"
      via: "APR seed triad for alpha.frame.stop_atr_mult, alpha.frame.target_r_multiple, alpha.scoring.min_strategy_n"
      pattern: "alpha.frame.target_r_multiple"
---

<objective>
Create the `alpha_frames` schema (migration 214) with the D-04-corrected lifecycle state machine,
seed every APR key this phase consumes, and build `AlphaFrameWriter` — a `BaseBatch` oneshot that
writes one hypothetical stop/target/hold frame per `alpha_events` row (FRAME-01). Ship the frozen
`SHADOW-REVIEW.md` pre-commitment document before any counterfactual data is collected.

Purpose: `alpha_frames` is the single canonical home for every counterfactual claim in the system
(platform-canonical-simulator binding rule). FRAME-01 turns proven signal IC into hypothetical
positions whose P&L Plan 02 will score. The pre-committed SHADOW-REVIEW.md fixes the Phase 147
promotion criteria before the data can bias them.

Output: migration 214, `services/alpha_frame_writer.py`, `docs/plans/SHADOW-REVIEW.md`, service
registration, and RED→GREEN unit tests for schema DDL, geometry math, and idempotent writes.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/142B-frame-simulation-counterfactual-tracking-planned/142B-CONTEXT.md
@.planning/phases/142B-frame-simulation-counterfactual-tracking-planned/142B-RESEARCH.md
@.planning/phases/142B-frame-simulation-counterfactual-tracking-planned/142B-PATTERNS.md

<interfaces>
<!-- alpha_events (read source, FK target) — live schema verified 2026-07-09 -->
alpha_events: PRIMARY KEY (event_id text, bar_ts timestamptz); columns symbol text, tf text,
  regime text (nullable), weight_version text, ensemble_version text, alpha_score double NOT NULL,
  alpha_ci_lower double, alpha_ci_upper double, direction text NOT NULL CHECK IN ('long','short'),
  cost_hurdle double NOT NULL. 12,258,206 rows, 78 (symbol,tf) child tables.

<!-- feature_vectors — ATR source for geometry. sr_support_dist/sr_resist_dist are 100% NULL
     (0 of 36,719,598 rows) — ATR path is the ONLY target path, not a fallback branch. -->

<!-- BaseBatch contract (src/core/agent/base_batch.py) -->
class BaseBatch(abc.ABC):
    job_name: str          # kebab-case, must equal systemd unit %n suffix
    compute_version: str
    async def run(self) -> None          # template: _setup_pool → execute(pool) → _emit_completion → teardown
    async def execute(self, pool) -> None  # subclass implements only this
    @staticmethod
    def content_key(*parts: str) -> str    # sha256(join('|'))[:32]; deterministic id
# _emit_completion() auto-emits JOB_COMPLETED_TOTAL.add(1, {"job": job_name, "status": status}) — D-06, no extra code
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 214 — alpha_frames table + APR key seeds + service registration</name>
  <files>production/migrations/214_alpha_frames_schema.sql, services/service_auditor.py, tests/unit/test_alpha_frames_schema.py</files>
  <read_first>
    - docs/plans/2026-06-25-v30-alpha-lifecycle-schema.md (lines 122-193: the governing alpha_frames DDL — columns, indexes, FK, uq constraint; every part governs EXCEPT the status CHECK, see D-04)
    - production/migrations/195_alpha_ensemble_ic.sql (analog: CREATE TABLE + create_hypertable + config_schema/config_state/config_history APR seed triad shape)
    - services/service_auditor.py (_DAG_ORDER dict + _ONESHOT_UNITS frozenset + _AGENT_ID_TO_UNIT — locate the indicagent-ensemble-ic-engine entries to mirror)
    - tests/unit/test_ensemble_ic_config.py (schema-assertion test style)
    - .planning/phases/142B-frame-simulation-counterfactual-tracking-planned/142B-CONTEXT.md (D-04 CHECK constraint, D-03 diagnostic snapshot columns)
  </read_first>
  <action>
    Create `production/migrations/214_alpha_frames_schema.sql`. Build the `CREATE TABLE IF NOT EXISTS
    alpha_frames` from the schema doc's DDL (lines 122-193) with these three deviations:
    (1) The `status` CHECK constraint is EXACTLY `CHECK (status IN ('open', 'closed_stop',
    'closed_target', 'closed_max_hold', 'closed_ic_decay'))` per D-04 — `closed_reversal` is dropped
    entirely, `closed_ic_decay` is added. Do NOT copy the schema doc's literal status block.
    (2) Add two provenance columns `corpus_run_id text` and `weight_epoch text` (Pitfall 6 —
    canonical-simulator provenance clause). (3) Keep the FK to alpha_events(event_id, bar_ts), the
    `uq_alpha_frames_variant UNIQUE (event_id, bar_ts, frame_variant)` constraint, the three indexes,
    and `SELECT create_hypertable('alpha_frames', 'bar_ts', if_not_exists => TRUE)`. Preserve the
    diagnostic snapshot columns gross_expected_r / net_expected_r / cost_r (D-03 — real columns,
    populated at frame creation, not dead schema).

    In the same migration, seed these NEW APR keys using the config_schema + config_state +
    config_history triad shape from migration 195 (each with `ON CONFLICT (config_key) DO NOTHING`
    on schema/state; changed_by='migration_214'):
      - `alpha.frame.stop_atr_mult` = 1.5, type float, description tagged `[initial_estimate]` — NOT an ML target
      - `alpha.frame.target_r_multiple` = 2.0, type float, tagged `[initial_estimate]` — use THIS exact
        name, NOT `target_r_fallback` (Pitfall 3); seed exactly one of the two names
      - `alpha.scoring.min_strategy_n` = 30, type int, tagged `[conventional]` — FRAME-04 (Plan 02)
        reads this; do NOT defer to Phase 144 (Pitfall 4)
      - `infra.alpha_frame_writer.chunk_size` = 50000, type int, tagged `[conventional]`
      - `infra.counterfactual_tracker.itersize` = 5000, type int, tagged `[conventional]` (consumed Plan 02)
      - `infra.counterfactual_tracker.workers` = 12, type int, tagged `[conventional]` (consumed Plan 02)
    Do NOT re-seed the 36 existing `alpha.frame.hold_max_bars.<regime>.<tf>` keys (they already exist
    from migration 195/142A). Do NOT add `alpha.frame.grid_stop_atr_mults` — the 4-variant grid is
    out of scope (frame_variant='primary' only).

    In `services/service_auditor.py`: add `"indicagent-alpha-frame-writer": 8` to `_DAG_ORDER` and
    `"indicagent-alpha-frame-writer"` to `_ONESHOT_UNITS`, mirroring the indicagent-ensemble-ic-engine
    entries. Do NOT add an `_AGENT_ID_TO_UNIT` entry (that dict is for BaseDaemon lag services, not
    oneshots — Pitfall 5). Do NOT add topic_alpha_frames to stream_keys.py (no consumer — Pitfall 5).

    Create `tests/unit/test_alpha_frames_schema.py` asserting against the migration SQL text (read the
    .sql file as a string, mirror test_ensemble_ic_config.py's no-DB style): the status CHECK contains
    'closed_ic_decay' and 'closed_max_hold', does NOT contain 'closed_reversal'; the file contains
    'corpus_run_id' and 'weight_epoch'; the file seeds config_key 'alpha.frame.target_r_multiple' and
    'alpha.scoring.min_strategy_n' and does NOT contain the string 'target_r_fallback'.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_alpha_frames_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "closed_reversal" production/migrations/214_alpha_frames_schema.sql` returns 0
    - `grep -c "closed_ic_decay" production/migrations/214_alpha_frames_schema.sql` returns >= 1
    - `grep -c "target_r_fallback" production/migrations/214_alpha_frames_schema.sql` returns 0
    - Migration seeds config keys: alpha.frame.stop_atr_mult, alpha.frame.target_r_multiple, alpha.scoring.min_strategy_n, infra.alpha_frame_writer.chunk_size, infra.counterfactual_tracker.itersize, infra.counterfactual_tracker.workers (grep each config_key string present)
    - `grep -c "indicagent-alpha-frame-writer" services/service_auditor.py` returns >= 2 (in _DAG_ORDER and _ONESHOT_UNITS)
    - `grep -c "indicagent-alpha-frame-writer" ` against the _AGENT_ID_TO_UNIT block returns 0 (not registered there)
    - test_alpha_frames_schema.py passes
  </acceptance_criteria>
  <done>Migration 214 creates alpha_frames with the corrected state machine and provenance columns, seeds all six new APR keys under the correct names, registers the writer as a oneshot only, and the schema-assertion test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AlphaFrameWriter service — FRAME-01 geometry + expected-R snapshot + chunked idempotent write</name>
  <files>services/alpha_frame_writer.py, tests/unit/test_alpha_frame_writer_geometry.py, tests/unit/test_alpha_frame_writer.py</files>
  <read_first>
    - services/alpha_publisher.py (full — BaseBatch subclass, imports, manifest-wrapped execute(), _CHUNK_SIZE accumulate-and-flush, content_key()-derived id, CLI entrypoint with init_otel_providers/DSN-strip/asyncio.run)
    - services/_batch_utils.py (cfg helper + load_apr_dict_async for APR loading)
    - src/core/agent/base_batch.py (BaseBatch.run/execute/content_key contract)
    - tests/unit/test_ensemble_ic_config.py (pure-fn/dataclass config-binding test style)
    - .planning/phases/142B-frame-simulation-counterfactual-tracking-planned/142B-RESEARCH.md (Pattern 2 chunked write; Pattern 4 anti-join --backfill; Pitfall 1 ATR-only path)
  </read_first>
  <behavior>
    - Geometry (pure fn, ATR-only target path since sr_resist_dist is 100% NULL):
      given direction='long', entry, atr, stop_atr_mult=1.5, target_r_multiple=2.0 →
      stop_price = entry - 1.5*atr; stop_distance = entry - stop_price; target_price = entry + 2.0*stop_distance;
      r_multiple = (target_price - entry)/(entry - stop_price) == 2.0
    - Geometry for direction='short': stop_price = entry + 1.5*atr; target_price = entry - 2.0*stop_distance; r_multiple == 2.0
    - No conditional branch on sr_resist_dist exists (or if present, is dead and not load-bearing)
    - Expected-R diagnostic snapshot (pure fn, D-03): given alpha_score=0.4, target_r_multiple=2.0, cost_r=0.05 →
      gross_expected_r = abs(alpha_score) * target_r_multiple = 0.8; net_expected_r = gross_expected_r - cost_r = 0.75.
      Direction-agnostic: alpha_score=-0.4 also yields gross_expected_r = 0.8 (abs magnitude — the traded direction
      is already chosen by the `direction` column, so the expected payoff magnitude is what the snapshot records).
      All three of gross_expected_r / cost_r / net_expected_r are written non-NULL on EVERY frame.
    - frame_id = BaseBatch.content_key(event_id, str(bar_ts), 'primary') — deterministic; re-running the writer over the same alpha_events row yields the same frame_id (idempotency via ON CONFLICT DO NOTHING)
    - FrameConfig.from_apr binds alpha.frame.stop_atr_mult / alpha.frame.target_r_multiple; applies documented defaults (1.5 / 2.0) when keys absent
  </behavior>
  <action>
    Write `services/alpha_frame_writer.py` defining `class AlphaFrameWriter(BaseBatch)` with
    `job_name = "alpha-frame-writer"`, `compute_version = "1.0.0"`, constructor
    `__init__(self, db_dsn: str, backfill: bool = False)`. Mirror alpha_publisher.py's import block but
    drop all Kafka imports (Pitfall 5). Implement a manifest-wrapped `execute()` exactly like
    alpha_publisher.py (CorpusManifest("alpha_frame_writer", ...), `except Exception as error:` —
    exception var name is `error` per CLAUDE.md).

    Geometry: implement a pure module-level function `compute_frame_geometry(direction, entry_price,
    atr, stop_atr_mult, target_r_multiple) -> tuple[stop_price, target_price, r_multiple]` implementing
    the ATR-only math in <behavior>. This is the ONLY target path — do not read or branch on
    sr_resist_dist/sr_support_dist. Bind stop_atr_mult and target_r_multiple from APR via a small
    `FrameConfig` dataclass with a `from_apr(cfg_dict)` classmethod (defaults 1.5 / 2.0), mirroring
    test_ensemble_ic_config.py's binding pattern. All numeric params come from APR — no hardcoded
    thresholds survive in the code (CLAUDE.md APR mandate); the dataclass defaults are the documented
    fallback only.

    Expected-R diagnostic snapshot (D-03 — the three columns gross_expected_r / cost_r / net_expected_r
    MUST be non-NULL on every frame; NULLs here are a task failure): implement a pure module-level
    helper `compute_expected_r_snapshot(alpha_score, target_r_multiple, cost_r) -> tuple[float, float]`
    returning (gross_expected_r, net_expected_r), defined EXACTLY as:
      - `gross_expected_r = abs(alpha_score) * target_r_multiple` — the ex-ante expected payoff in R at
        entry: the model's directional-confidence magnitude (`alpha_events.alpha_score`, always present)
        scaled by the frame's design R-multiple on a win (`alpha.frame.target_r_multiple`, the APR value
        = 2.0). Use the APR `target_r_multiple`, NOT the `r_multiple` geometry column (that column is
        written NULL at FRAME-01 time and filled by CounterfactualTracker at T+1 — see NOTE below).
        Use `abs(alpha_score)` so the snapshot is direction-agnostic (the traded direction is already
        fixed by the copied `direction` column).
      - `cost_r` = the calibrated `alpha.quant.cost_hurdle.<tf>` APR value for this frame's tf (grep
        config_schema for the exact key suffix before coding — do not assume the suffix format).
      - `net_expected_r = gross_expected_r - cost_r`.
    These three are diagnostic snapshots only (D-03) — they do NOT feed FRAME-04's gate (D-01).

    NOTE on geometry timing: entry_price / stop_price / target_price / r_multiple require the T+1 open,
    which is populated by CounterfactualTracker (Plan 02, per the schema doc). At FRAME-01 write time
    those price columns are written NULL; AlphaFrameWriter writes the APR SNAPSHOT columns
    (stop_atr_mult, max_hold_bars), the signal snapshot from alpha_events, AND the D-03 expected-R
    snapshot triad above (which is computable at creation time because it uses the APR target_r_multiple
    and alpha_score, not the NULL geometry columns). Keep the geometry pure function in this file so
    Plan 02's tracker imports and reuses it for the T+1 fill.

    Write path: read alpha_events via the anti-join query
    `SELECT ae.* FROM alpha_events ae LEFT JOIN alpha_frames af ON af.event_id = ae.event_id AND
    af.bar_ts = ae.bar_ts AND af.frame_variant = 'primary' WHERE af.frame_id IS NULL` (parameterized;
    this same query serves both nightly-incremental and --backfill — Pattern 4). Stream it via asyncpg
    `conn.cursor(SQL, prefetch=10000)`, accumulate output tuples, flush via
    `await wconn.executemany(_INSERT_SQL, chunk)` every `infra.alpha_frame_writer.chunk_size` (50000)
    rows, then a final flush — never one giant in-memory list, never one long transaction (D-05
    constraint). Insert uses `ON CONFLICT (event_id, bar_ts, frame_variant) DO NOTHING` for idempotency.

    Per-frame column population: frame_id = content_key(event_id, str(bar_ts), 'primary');
    frame_variant='primary'; symbol/tf/bar_ts/regime/direction/alpha_score/alpha_ci_lower/alpha_ci_upper
    copied from the alpha_events row; weight_epoch = alpha_events.weight_version (copy-through, A1);
    corpus_run_id = a single ISO-timestamp string pinned once per invocation (mirror
    ensemble_ic_engine.py's run_ts pattern, A2) and stamped on every frame; max_hold_bars snapshot =
    the value of `alpha.frame.hold_max_bars.<regime>.<tf>` for the row's cell; stop_atr_mult snapshot =
    the APR value; status='open'. D-03 diagnostic triad: gross_expected_r / cost_r / net_expected_r
    computed via `compute_expected_r_snapshot(alpha_score, target_r_multiple, cost_r)` above and written
    on every row (non-NULL).

    Add a `--backfill` CLI flag (`action="store_true"`) and the standard CLI entrypoint
    (`init_otel_providers("indicagent-alpha-frame-writer")`, DSN strip, `asyncio.run(...)`) copied from
    alpha_publisher.py's shape. Register no Kafka anything.

    Create `tests/unit/test_alpha_frame_writer_geometry.py` (pure-fn geometry cases from <behavior>,
    long and short, r_multiple == 2.0) and `tests/unit/test_alpha_frame_writer.py` (FrameConfig.from_apr
    binds and defaults; content_key-derived frame_id is stable across two calls with identical inputs —
    idempotency proof; `compute_expected_r_snapshot(0.4, 2.0, 0.05)` returns (0.8, 0.75) and
    `compute_expected_r_snapshot(-0.4, 2.0, 0.05)` also returns (0.8, 0.75) — the D-03 formula and its
    direction-agnostic abs() behavior, with both returns strictly non-None/non-NaN; and a grep-style
    assertion that the module contains no 'sr_resist_dist' load-bearing branch, i.e. the geometry
    function source has no 'sr_resist_dist' reference).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_alpha_frame_writer_geometry.py tests/unit/test_alpha_frame_writer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `compute_frame_geometry('long', 100.0, 2.0, 1.5, 2.0)` returns stop=97.0, target=106.0, r_multiple=2.0
    - `compute_frame_geometry('short', 100.0, 2.0, 1.5, 2.0)` returns stop=103.0, target=94.0, r_multiple=2.0
    - `compute_expected_r_snapshot(0.4, 2.0, 0.05)` returns (gross_expected_r=0.8, net_expected_r=0.75); `compute_expected_r_snapshot(-0.4, 2.0, 0.05)` also returns (0.8, 0.75) — D-03 formula proven, direction-agnostic
    - The _INSERT_SQL column list includes gross_expected_r, cost_r, and net_expected_r, and every row tuple supplies all three as non-NULL values (grep: all three column names present in the insert; test asserts the snapshot helper returns non-None for representative inputs)
    - `content_key(event_id, str(bar_ts), 'primary')` is byte-identical across two invocations with the same inputs
    - `grep -c "sr_resist_dist" services/alpha_frame_writer.py` returns 0 (ATR-only path, no dead branch)
    - `grep -c "topic_alpha_frames\|KafkaProducer" services/alpha_frame_writer.py` returns 0
    - Exception handler uses `as error` (grep `except Exception as error` present; `as exc` absent)
    - The anti-join write SQL uses parameterized placeholders only (no f-string/%-format SQL of user/data values)
    - Both geometry and writer tests pass
  </acceptance_criteria>
  <done>AlphaFrameWriter writes one idempotent primary frame per pending alpha_events row via chunked anti-join flush with the ATR-only geometry as the sole target path, populates the D-03 gross/cost/net expected-R snapshot triad non-NULL on every frame via a unit-tested pure helper, --backfill runs the same query without a long transaction, and RED→GREEN unit tests are green.</done>
</task>

<task type="auto">
  <name>Task 3: SHADOW-REVIEW.md frozen pre-commitment document</name>
  <files>docs/plans/SHADOW-REVIEW.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 142B section: the "Renaissance pre-commitment" criteria block, lines ~988-997)
    - .planning/phases/142B-frame-simulation-counterfactual-tracking-planned/142B-CONTEXT.md (D-01 gross gate, D-02 net_expected_r reporting column)
    - docs/plans/2026-06-25-v30-alpha-lifecycle-schema.md (section-heading discipline, structural precedent only)
  </read_first>
  <action>
    Write `docs/plans/SHADOW-REVIEW.md` as a one-page frozen pre-commitment document specifying the
    exact numeric criteria required for Phase 147 live promotion. Commit the ROADMAP's proposed criteria
    as final values: (1) >= 60 trading days of closed alpha_frames (primary variant); (2)
    mean(counterfactual_pnl_r) > 0 at 95% CI (bootstrap, one-tailed) on OOS data; (3) Sharpe of
    counterfactual_pnl_r > 0.5 annualized; (4) max drawdown of cumulative counterfactual_pnl_r < 25%;
    (5) EnsembleICEngine IC Sharpe stable across the shadow period (no cliff in last 20 days).

    Per D-01: state explicitly that all pass/fail criteria are evaluated on GROSS counterfactual_pnl_r,
    with the reasoned rationale (gating on the unvalidated alpha.quant.cost_hurdle.* keys would conflate
    "does the frame capture IC as P&L" with "is our cost estimate right" — a third question). Per D-02:
    add a mandatory `net_expected_r` reporting column (gross minus the calibrated
    alpha.quant.cost_hurdle.* keys) reported alongside every gross metric — explicitly labeled as
    REPORTING ONLY, NOT A GATE. Include the ROADMAP's no-post-hoc-negotiation clause verbatim ("Post-hoc
    gate negotiation is not permitted. If the gate fails, diagnose — don't renegotiate."). Add a header
    line stating the document is frozen as of the commit date and must exist before any counterfactual
    data is collected.

    Do NOT use "v1"/"simplified"/"placeholder" framing anywhere. Do NOT introduce criteria beyond the
    five above. This is a decision record, not a design doc.
  </action>
  <verify>
    <automated>test -f docs/plans/SHADOW-REVIEW.md && grep -c "net_expected_r" docs/plans/SHADOW-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - File docs/plans/SHADOW-REVIEW.md exists
    - Contains all five criteria strings: "60 trading days", "95% CI", "Sharpe", "drawdown", "IC Sharpe"
    - Contains the word GROSS applied to the gate and states net_expected_r is reporting-only, not a gate
    - Contains the no-post-hoc-negotiation clause
    - `grep -ci "placeholder\|simplified\|v1 version" docs/plans/SHADOW-REVIEW.md` returns 0
  </acceptance_criteria>
  <done>SHADOW-REVIEW.md is frozen in git with the five final promotion criteria, the D-01 gross-gate decision, and the D-02 net_expected_r reporting-only column, before Plan 02's first production run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alpha_events (DB) → AlphaFrameWriter | Internal table read; no external/user input crosses here |
| AlphaFrameWriter → alpha_frames (DB) | Batch write via parameterized SQL; single serial writer connection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142B-01 | Tampering | Anti-join / INSERT SQL in alpha_frame_writer.py | mitigate | Parameterized queries only ($1/executemany params); acceptance criteria greps for no f-string/%-format SQL of data values (V5 ASVS) |
| T-142B-02 | Denial of Service | Full 12.2M-row --backfill pass | mitigate | Chunked accumulate-and-flush at infra.alpha_frame_writer.chunk_size via asyncpg server-side cursor (prefetch); no unbounded fetch, no long-running write transaction (D-05) |
| T-142B-03 | Tampering (config) | Numeric geometry params | mitigate | All numeric params (stop_atr_mult, target_r_multiple, chunk_size) load via APR/ConfigService; migration seeds them; no hardcoded thresholds (CLAUDE.md APR mandate, V5) |
| T-142B-SC | Tampering | package installs | accept | Zero new third-party packages this phase (RESEARCH Package Legitimacy Audit: not applicable) — no supply-chain surface introduced |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_alpha_frames_schema.py tests/unit/test_alpha_frame_writer_geometry.py tests/unit/test_alpha_frame_writer.py -x -q` green
- `.venv/bin/pytest tests/unit/ -q` shows no new failures
- Migration 214 applies cleanly (idempotent CREATE TABLE IF NOT EXISTS + ON CONFLICT DO NOTHING seeds)
- alpha_frame_writer.py contains no Kafka imports and no sr_resist_dist branch
</verification>

<success_criteria>
- alpha_frames table exists with D-04 lifecycle CHECK (closed_ic_decay in, closed_reversal out) plus corpus_run_id/weight_epoch
- Six new APR keys seeded under the correct names (target_r_multiple, not target_r_fallback)
- AlphaFrameWriter writes idempotent primary frames via chunked anti-join, ATR-only geometry, --backfill without long transactions
- D-03 gross_expected_r / cost_r / net_expected_r diagnostic snapshots populated non-NULL on every frame via the unit-tested compute_expected_r_snapshot helper
- SHADOW-REVIEW.md frozen with gross gate (D-01) + net_expected_r reporting column (D-02)
- indicagent-alpha-frame-writer registered as a oneshot only
</success_criteria>

<output>
Create `.planning/phases/142B-frame-simulation-counterfactual-tracking-planned/142B-01-SUMMARY.md` when done
</output>
</output>
