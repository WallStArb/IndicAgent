---
phase: 122-production-hardening
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - production/scripts/feature_replay.py
  - src/api/routes/narrative.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "feature_replay.py _reconstruct_intelligence_event constructs IntelligenceEvent with source='backfill'"
    - "narrative route _SIGNAL_QUERY selects f.i2 not f.market_context"
    - "narrative route _build_context_from_row reads row.get('i2') for the i2= field"
  artifacts:
    - path: "production/scripts/feature_replay.py"
      provides: "Row reconstruction with valid Literal source value"
      contains: "source=\"backfill\""
    - path: "src/api/routes/narrative.py"
      provides: "Signal query selecting correct i2 column"
      contains: "f.i2"
  key_links:
    - from: "feature_replay._reconstruct_intelligence_event"
      to: "IntelligenceEvent"
      via: "source='backfill'"
      pattern: "source=\"backfill\""
    - from: "narrative._SIGNAL_QUERY"
      to: "intelligence_features.i2"
      via: "SELECT f.i2"
      pattern: "f\\.i2"
---

<objective>
Fix two one-line blockers: invalid Literal value in feature_replay.py and wrong column reference in narrative.py.

Purpose: feature_replay.py passes source="feature_replay" which is not a valid Literal["live","backfill"] -- Pydantic raises ValidationError on every row, the script completes with zero signals. narrative.py still selects f.market_context for the i2= field; post migration 124, market_context contains only cross_asset data, so all narratives lack I2 tier context (RSI, Stochastic, ADX events).

Output: feature_replay produces signals; narrative LLM receives full I2 context.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-production-hardening/122-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix source literal in feature_replay.py</name>
  <files>production/scripts/feature_replay.py</files>
  <read_first>production/scripts/feature_replay.py</read_first>
  <action>
    At line 148, change the source argument from the invalid literal to the valid one:

    Before:
    ```python
    source="feature_replay",
    ```

    After:
    ```python
    source="backfill",
    ```

    This is the sole change needed. IntelligenceEvent.source is Literal["live", "backfill"]. "feature_replay" is not a valid value -- Pydantic raises ValidationError on every row reconstruction, silently producing zero signals.

    Do not change any other lines. Do not add a comment (the code is self-explanatory given the type annotation).
  </action>
  <verify>
    ```
    grep -n 'source=' /home/bg/dev/indicagent/production/scripts/feature_replay.py | head -5
    ```
    Must show `source="backfill"` at the _reconstruct_intelligence_event call site. No `source="feature_replay"` remaining.
  </verify>
  <done>feature_replay.py line 148 reads source="backfill". grep finds no occurrence of source="feature_replay" in the file.</done>
  <acceptance_criteria>
    - grep -n 'source="feature_replay"' production/scripts/feature_replay.py returns empty (no match)
    - grep -n 'source="backfill"' production/scripts/feature_replay.py shows line 148
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix narrative route to select f.i2 instead of f.market_context</name>
  <files>src/api/routes/narrative.py</files>
  <read_first>src/api/routes/narrative.py</read_first>
  <action>
    Two changes in src/api/routes/narrative.py:

    Change 1 -- _SIGNAL_QUERY at line 163:
    Before:
    ```python
           f.bar, f.i1, f.market_context, f.i3,
    ```
    After:
    ```python
           f.bar, f.i1, f.i2, f.i3,
    ```

    Change 2 -- _build_context_from_row at line 125:
    Before:
    ```python
        i2=_maybe_validate(I2Events, _parse_jsonb(row.get("market_context"), default=None)),
    ```
    After:
    ```python
        i2=_maybe_validate(I2Events, _parse_jsonb(row.get("i2"), default=None)),
    ```

    After migration 124, the i2 column holds all I2Events data (RSI, Stochastic, ADX, Volume, composite fields). market_context now holds only cross_asset data. The SQL must select f.i2, and the row reader must look up "i2" not "market_context".

    Do not change anything else in this file.
  </action>
  <verify>
    ```
    grep -n "market_context\|f\.i2\|row\.get" /home/bg/dev/indicagent/src/api/routes/narrative.py | grep -E "i2|market_context"
    ```
    Must show f.i2 in the SELECT and row.get("i2") in _build_context_from_row. market_context must not appear in the i2 context lines.
  </verify>
  <done>_SIGNAL_QUERY selects f.i2; _build_context_from_row reads row.get("i2") for i2= field. No reference to market_context for I2 data remains.</done>
  <acceptance_criteria>
    - grep -n 'f\.market_context' src/api/routes/narrative.py returns empty (removed from SELECT)
    - grep -n 'f\.i2' src/api/routes/narrative.py shows the column in _SIGNAL_QUERY
    - grep -n "row.get..\"i2\"" src/api/routes/narrative.py shows the row lookup in _build_context_from_row
    - grep -n "row.get..\"market_context\"" src/api/routes/narrative.py returns empty for the i2= assignment line
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. grep finds no `source="feature_replay"` in feature_replay.py
2. grep finds `source="backfill"` at line 148 in feature_replay.py
3. grep finds `f.i2` in narrative.py _SIGNAL_QUERY SELECT clause
4. grep finds `row.get("i2")` in narrative.py _build_context_from_row i2= assignment
5. grep finds no `f.market_context` in the _SIGNAL_QUERY SELECT clause
</verification>

<success_criteria>
feature_replay.py constructs valid IntelligenceEvent objects on every row. narrative.py LLM context includes full I2 tier data (RSI/Stochastic/ADX/Volume/composite fields) from the dedicated i2 column.
</success_criteria>

<output>
After completion, create `.planning/phases/122-production-hardening/122-09-SUMMARY.md`
</output>
