---
phase: 122-production-hardening
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/125_rename_intelligence_features_columns.sql
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "intelligence_features columns are named i1, i3, i4, i5 in the database"
    - "feature_writer INSERT succeeds without 'column does not exist' error"
    - "API routes referencing i1/i3/i4/i5 columns return data without DB errors"
  artifacts:
    - path: "production/migrations/125_rename_intelligence_features_columns.sql"
      provides: "RENAME COLUMN statements for 4 legacy tier columns"
      contains: "RENAME COLUMN technical_indicators TO i1"
  key_links:
    - from: "production/migrations/125_rename_intelligence_features_columns.sql"
      to: "intelligence_features table"
      via: "psql direct execution"
      pattern: "RENAME COLUMN"
---

<objective>
Apply migration 125 to rename legacy tier columns in the intelligence_features table.

Purpose: The code across feature_writer, historical pipeline, and all API routes was updated in Plans 04-07 to reference canonical column names (i1, i3, i4, i5). Migration 125 was not applied to the database. Every live feature INSERT and every API SELECT referencing these column names fails at runtime with "column does not exist". This plan applies the migration that unlocks all live writes and reads.

Output: intelligence_features table has columns i1, i3, i4, i5 (renamed from technical_indicators, regime_features, confluence_scores, pattern_detections).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-production-hardening/122-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply migration 125 to rename legacy tier columns</name>
  <files>production/migrations/125_rename_intelligence_features_columns.sql</files>
  <read_first>production/migrations/125_rename_intelligence_features_columns.sql</read_first>
  <action>
    Apply migration 125 using psql. The file already exists and contains correct RENAME COLUMN statements:

    ```
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/125_rename_intelligence_features_columns.sql
    ```

    After applying, verify the renamed columns exist:

    ```
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d intelligence_features" | grep -E "i1|i3|i4|i5|technical_indicators|pattern_detections|regime_features|confluence_scores"
    ```

    Expected: i1, i3, i4, i5 present; technical_indicators, pattern_detections, regime_features, confluence_scores absent.

    Note: migration 125 uses IF NOT EXISTS equivalent behavior for RENAME (if columns were already renamed, psql will error -- treat as "already applied" and proceed). If error output contains "column does not exist" this means the migration was already applied; confirm i1/i3/i4/i5 are present.
  </action>
  <verify>
    ```
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d intelligence_features"
    ```
    Output must show columns: i1, i2, i3, i4, i5, smc, bar, market_context, cross_timeframe_context, trading_signals
  </verify>
  <done>intelligence_features table has i1, i3, i4, i5 columns. Old names technical_indicators/pattern_detections/regime_features/confluence_scores are gone.</done>
  <acceptance_criteria>
    - psql confirms columns i1, i3, i4, i5 exist in intelligence_features
    - psql confirms columns technical_indicators, pattern_detections, regime_features, confluence_scores do not exist
    - No "column does not exist" error when running: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT i1, i3, i4, i5 FROM intelligence_features LIMIT 1"
  </acceptance_criteria>
</task>

</tasks>

<verification>
After applying migration 125:
1. `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d intelligence_features"` shows i1, i3, i4, i5 columns
2. `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT i1, i3, i4, i5 FROM intelligence_features LIMIT 1"` returns without error
</verification>

<success_criteria>
intelligence_features table columns renamed from legacy names to canonical tier names. feature_writer INSERTs and API route SELECTs referencing i1/i3/i4/i5 will succeed at runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/122-production-hardening/122-08-SUMMARY.md`
</output>
