---
phase: 171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/regime_writer.py
  - tests/unit/services/test_regime_writer.py
autonomous: true
requirements: [REQ-2]

must_haves:
  truths:
    - "Every walk-forward refit segment emits its own iters_used/n_iter_cap/converged log record, so a full-corpus run yields the cap-headroom distribution todo 226 requires on the path that actually runs at scale"
    - "A single regime_writer.py invocation can run the walk-forward path without mutating the production alpha.hmm.walk_forward.enabled config value"
    - "_run_symbol_worker's branch between _compute_symbol_tf_walk_forward and _compute_symbol_tf is covered by a test that fails if the branch is inverted"
  artifacts:
    - path: "services/regime_writer.py"
      provides: "per-segment convergence instrumentation on the walk-forward path plus a per-invocation walk-forward override"
      contains: "regime_writer.walk_forward_hmm_convergence_iters"
    - path: "tests/unit/services/test_regime_writer.py"
      provides: "dispatch-on-flag coverage and per-segment logging coverage"
      contains: "_run_symbol_worker"
  key_links:
    - from: "services/regime_writer.py::_compute_symbol_tf_walk_forward"
      to: "services/regime_writer.py::_walk_forward_hmm_full"
      via: "symbol/tf threaded down purely for log correlation"
      pattern: "_walk_forward_hmm_full\\("
    - from: "services/regime_writer.py::main"
      to: "services/regime_writer.py::_run_symbol_worker"
      via: "effective walk_forward_enabled resolved from CLI override or APR, passed in the worker arg tuple"
      pattern: "walk_forward_enabled"
---

<objective>
Close the two code-level gaps between `regime_writer.py`'s single-fit path and its walk-forward
path, so the walk-forward path is safe to run at corpus scale and observable while it does.

Purpose: RESEARCH.md's Third Finding proved D-04's `iters_used` cap-headroom data (todo 226)
will NOT materialize during this phase's rollout, because todo 226's instrumentation lives only
inside `_compute_symbol_tf` (line 1262) while the rollout runs entirely through
`_walk_forward_hmm_full`. Separately, the pilot (plan 171-05) must exercise the walk-forward path
on 8 symbols WITHOUT flipping the production `alpha.hmm.walk_forward.enabled` config value —
this project's own parallel-construction-never-mutate-baseline discipline (CONTEXT.md D-03,
Phase 168 D-02). Both are small, well-precedented additions to an existing file.

Output: instrumented + per-invocation-overridable walk-forward dispatch, with unit coverage for
the dispatch branch that RESEARCH.md confirmed is currently untested.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-CONTEXT.md
@.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-RESEARCH.md
@.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
Current signatures the executor must respect (verified 2026-08-07, `services/regime_writer.py`):

- `_walk_forward_hmm_full(obs_matrix, n_components, covariance_type, n_iter, hmm_random_state,
  refit_every_bars, initial_warmup_bars, min_hold_bars, full_cov_min_obs, min_state_occupation)
  -> list[dict]` — defined line 656; per-segment loop at lines 725-804; the corrected convergence
  check `converged = model.monitor_.iter < model.monitor_.n_iter` at line 743.
- `_compute_symbol_tf_walk_forward(conn, symbol, tf, n_components, vol_window, n_iter,
  hmm_random_state, momentum_window, vol_of_vol_window, refit_every_bars, initial_warmup_bars,
  covariance_type="full", min_hold_bars=3, full_cov_min_obs=500, min_state_occupation=0.05,
  churn_window=10, min_obs_factor=_MIN_OBS_FACTOR_DEFAULT) -> tuple | None` — defined line 807;
  calls `_walk_forward_hmm_full` at line 878.
- `_run_symbol_worker(args: tuple) -> dict` — defined line 1465; positional arg-tuple unpack at
  lines 1499-1519 in this exact order: symbol, tfs, dsn, n_components, vol_window,
  momentum_window, vol_of_vol_window, n_iter, hmm_random_state, covariance_type, min_hold_bars,
  heldout_fraction, full_cov_min_obs, min_state_occupation, churn_window, min_obs_factor,
  n_restarts, walk_forward_enabled, walk_forward_params. Branch at line 1533.
- `main()` — defined line 1625; argparse block lines 1627-1656 (existing `--symbols`, `--tf`,
  `--workers`, `--refit`); `walk_forward_enabled` read from APR at line 1715; worker arg tuple
  built at lines 1768-1791.
- Existing three `_walk_forward_hmm_full` call sites in tests use keyword args
  (`tests/unit/services/test_regime_writer.py` lines 1121, 1146, 1180) — any new parameter must
  have a default so these keep passing without edits.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-segment convergence instrumentation on the walk-forward path</name>
  <files>services/regime_writer.py, tests/unit/services/test_regime_writer.py</files>
  <read_first>
    - services/regime_writer.py lines 656-810 (`_walk_forward_hmm_full`, the per-segment loop and
      the already-correct `monitor_.iter < monitor_.n_iter` check at line 743 — do not modify it)
    - services/regime_writer.py lines 1240-1280 (`_compute_symbol_tf`'s existing
      `regime_writer.hmm_convergence_iters` log call — the exact shape to mirror)
    - services/regime_writer.py lines 807-905 (`_compute_symbol_tf_walk_forward`, the only caller,
      which already has `symbol`/`tf` in scope)
    - tests/unit/services/test_regime_writer.py lines 500-560
      (`test_compute_symbol_tf_logs_convergence_iterations` — the `structlog.testing.capture_logs`
      idiom to reuse)
    - tests/unit/services/test_regime_writer.py lines 1090-1200 (existing `_walk_forward_hmm_full`
      tests and their keyword-arg call convention)
    - .planning/todos/pending/226-regime-writer-n-iter-convergence-headroom-check.md
  </read_first>
  <behavior>
    - A `_walk_forward_hmm_full` run over an obs matrix long enough to produce N > 1 refit
      segments emits exactly N `regime_writer.walk_forward_hmm_convergence_iters` log events
      (one per segment), not one per cell.
    - Each event carries `iters_used`, `n_iter_cap`, `converged`, `seg_start`, `seg_end`, and the
      `symbol`/`tf` context threaded from the caller.
    - `converged` in the log reflects the post-retry value (the retry block at lines 744-754 runs
      first), matching the single-fit path's semantics.
    - Existing keyword-arg call sites that omit `symbol`/`tf` still work.
  </behavior>
  <action>
Add two optional keyword parameters `symbol: str | None = None` and `tf: str | None = None` to
`_walk_forward_hmm_full`, positioned after `min_state_occupation`, documented in its docstring as
log-correlation context only (never used in compute). Defaults are required so the three existing
keyword-arg test call sites keep passing unchanged.

Inside the per-segment `while boundary < n:` loop, immediately after the non-convergence retry
block finishes resolving `model`/`converged` (that is, after the current line 754 block, before
`label_map = _build_label_map(model.means_)`), emit `_logger.info` with event name
`regime_writer.walk_forward_hmm_convergence_iters` and fields `symbol`, `tf`,
`iters_used=int(model.monitor_.iter)`, `n_iter_cap=int(model.monitor_.n_iter)`, `converged`,
`seg_start=boundary`, `seg_end=min(boundary + refit_every_bars, n)`, `train_end=boundary`.

The event name MUST differ from the single-fit path's `regime_writer.hmm_convergence_iters` so
todo 226's downstream analysis can tell which code path produced a given record — per PATTERNS.md
this is the whole reason the two shapes must stay distinguishable.

Do NOT touch the `converged = model.monitor_.iter < model.monitor_.n_iter` line or the retry
block — that is todo 229's already-shipped fix (commit `ba8a74ef`) and is correct as written.
Do NOT accumulate or aggregate per-segment records into a per-cell summary: a full-history 5m cell
produces roughly 20 segments, which is a bounded, useful count, not the per-row logging CLAUDE.md
forbids.

Thread `symbol=symbol, tf=tf` through `_compute_symbol_tf_walk_forward`'s call to
`_walk_forward_hmm_full` at line 878 so the context is populated in production.

Add a test `test_walk_forward_hmm_full_logs_convergence_iters_per_segment` in
`tests/unit/services/test_regime_writer.py` using `structlog.testing.capture_logs`, mirroring
`test_compute_symbol_tf_logs_convergence_iterations`. Build a synthetic obs matrix with a small
`refit_every_bars`/`initial_warmup_bars` so it yields at least 3 segments, assert the count of
captured `regime_writer.walk_forward_hmm_convergence_iters` events equals the returned
`len(segment_results)`, and assert each event carries `iters_used`, `n_iter_cap`, `seg_start`,
`seg_end`. Reuse the existing `_make_ranging_closes`/`_make_volumes`/`_make_timestamps` helpers
already in that file — do not hand-roll new fixtures.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/services/test_regime_writer.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "walk_forward_hmm_convergence_iters" services/regime_writer.py` returns a line
      inside `_walk_forward_hmm_full`'s per-segment loop (line number between 725 and 810 in the
      pre-edit numbering).
    - `.venv/bin/pytest tests/unit/services/test_regime_writer.py -q` passes with zero failures,
      including the three pre-existing `_walk_forward_hmm_full` tests at lines 1094, 1133, 1166
      whose keyword-arg call sites were not edited.
    - `.venv/bin/pytest tests/unit/services/test_regime_writer.py -k convergence_iters_per_segment -x`
      passes.
    - `grep -c "monitor_.converged" services/regime_writer.py` returns 0 outside comment lines:
      `grep -v '^\s*#' services/regime_writer.py | grep -c "monitor_\.converged"` is 0.
  </acceptance_criteria>
  <done>A walk-forward run over a multi-segment obs matrix emits one convergence record per
  segment, distinguishable by event name from the single-fit path, with symbol/tf context in
  production, and no existing test call site was broken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-invocation walk-forward override and dispatch-branch coverage</name>
  <files>services/regime_writer.py, tests/unit/services/test_regime_writer.py</files>
  <read_first>
    - services/regime_writer.py lines 1625-1660 (argparse block; note the existing `--workers`
      flag whose `default=None` then falls back to APR `infra.regime_writer.workers` at line 1742
      — that is the exact override precedent to copy)
    - services/regime_writer.py lines 1705-1745 (APR loads including
      `walk_forward_enabled = bool(cfg.get_sync("alpha.hmm.walk_forward.enabled", False))` at
      line 1715)
    - services/regime_writer.py lines 1465-1560 (`_run_symbol_worker` arg-tuple unpack order and
      the `if walk_forward_enabled:` branch at line 1533)
    - services/regime_writer.py lines 1749-1795 (`regime_writer.starting` log and worker arg
      tuple construction)
    - docs/foundation/adaptive-parameter-registry.md (why a per-invocation CLI override of an
      existing APR key is not a new APR parameter)
  </read_first>
  <behavior>
    - `--walk-forward` forces the walk-forward path for that invocation regardless of the APR value.
    - `--no-walk-forward` forces the single-fit path for that invocation regardless of the APR value.
    - Neither flag present: the APR value `alpha.hmm.walk_forward.enabled` decides, exactly as today.
    - The `regime_writer.starting` log records both the effective value and its source
      ("cli" or "apr").
    - `_run_symbol_worker` with `walk_forward_enabled=True` in its arg tuple calls
      `_compute_symbol_tf_walk_forward` and never `_compute_symbol_tf`; with `False` the reverse.
  </behavior>
  <action>
In `main()`'s argparse block, add a mutually exclusive group with `--walk-forward`
(`action="store_true"`, `dest="walk_forward"`) and `--no-walk-forward`
(`action="store_false"`, `dest="walk_forward"`), with `parser.set_defaults(walk_forward=None)` so
"absent" is distinguishable from "explicitly false". Help text must state that this overrides the
APR key `alpha.hmm.walk_forward.enabled` for this invocation only and does NOT write to
`config_state`.

After the existing APR read at line 1715, resolve the effective value: if `args.walk_forward` is
not None use it and set a local `walk_forward_source = "cli"`, else keep the APR value with
`walk_forward_source = "apr"`. Pass the effective value into the worker arg tuple's existing
`walk_forward_enabled` slot (position 18 of 19). Add `walk_forward_source=walk_forward_source` to
the `regime_writer.starting` log call alongside the existing `walk_forward_enabled=` field.

This is deliberately NOT a new APR key: `--workers` already establishes this file's
"CLI overrides an existing APR value for one run" precedent (line 1740-1742), and the APR
mandate's "operator-visible switches → APR" is already satisfied by the underlying
`alpha.hmm.walk_forward.enabled` key that migration 292 shipped.

Add a test `test_run_symbol_worker_dispatches_on_walk_forward_flag` in
`tests/unit/services/test_regime_writer.py`. Monkeypatch
`regime_writer_module._compute_symbol_tf_walk_forward` and
`regime_writer_module._compute_symbol_tf` to sentinel callables that record invocation, and
monkeypatch `regime_writer_module.psycopg.connect` so the worker's own connection attempt does not
hit a real database. Build the 19-element positional arg tuple in the exact order documented at
`_run_symbol_worker` lines 1499-1519 — an out-of-order tuple silently misassigns rather than
raising. Assert with `walk_forward_enabled=True` only the walk-forward sentinel ran, and with
`False` only the single-fit sentinel ran. This is the REQ-2 dispatch-level coverage gap
RESEARCH.md confirmed does not exist today.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/services/test_regime_writer.py -q && .venv/bin/python services/regime_writer.py --help | grep -c -- "--no-walk-forward"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python services/regime_writer.py --help` output contains both `--walk-forward`
      and `--no-walk-forward`, and the help text mentions `alpha.hmm.walk_forward.enabled`.
    - `.venv/bin/pytest tests/unit/services/test_regime_writer.py -k dispatches_on_walk_forward -x`
      passes.
    - Inverting the `if walk_forward_enabled:` condition at `services/regime_writer.py` line 1533
      makes that test FAIL (executor must confirm this by temporary local mutation, then revert).
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT config_value FROM config_state WHERE config_key='alpha.hmm.walk_forward.enabled'"`
      still returns `false` after this task — the override must not write config state.
    - `.venv/bin/ruff check services/regime_writer.py tests/unit/services/test_regime_writer.py`
      is clean.
  </acceptance_criteria>
  <done>A scoped pilot run can select the walk-forward path via CLI without mutating production
  config, and the dispatch branch is covered by a test that fails if the branch is inverted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None in the ASVS sense. Per RESEARCH.md's Security Domain section, this phase is an internal
batch-compute correctness fix: no network-facing surface, no authentication/session/access-control
boundary, no user input, no untrusted data crossing a process boundary. `regime_writer.py` is
invoked manually or by `ops_corpus_pipeline_run.sh` on a single-operator machine against a local
PostgreSQL instance. No ASVS L1 category applies, and a generic ASVS threat-pattern table would be
noise rather than analysis.

## Correctness Threat Register (the real risk class this phase carries)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-01 | Data integrity (silent corruption) | `services/regime_writer.py` CLI override | mitigate | `--walk-forward` overrides in-memory only; explicit acceptance criterion re-asserts `config_state` still reads `false` after the task, so a pilot can never silently leave production flipped |
| T-171-02 | Observability loss | `_walk_forward_hmm_full` | mitigate | Per-segment convergence record with a path-distinct event name, so todo 226's cap-headroom analysis cannot silently attribute walk-forward data to the single-fit path |
| T-171-03 | Regression (silent) | existing `_walk_forward_hmm_full` callers | mitigate | New params are optional with defaults; acceptance criteria require the three pre-existing keyword-arg tests to still pass unedited |
| T-171-SC | Tampering (supply chain) | package installs | accept | This plan installs no packages. RESEARCH.md confirmed zero new dependencies; `hmmlearn`/`scikit-learn` already present. No Package Legitimacy Gate applies. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/services/test_regime_writer.py -q` green (40 pre-existing tests plus
  2 new).
- `.venv/bin/pytest tests/unit/ -q` green (wave merge gate per 171-VALIDATION.md).
- `.venv/bin/ruff check . --fix` and `.venv/bin/black .` clean on the two modified files.
- `alpha.hmm.walk_forward.enabled` still `false` in `config_state`.
</verification>

<success_criteria>
- Walk-forward path emits one convergence record per refit segment, name-distinct from the
  single-fit path's event.
- `regime_writer.py --walk-forward` / `--no-walk-forward` resolve correctly against the APR value
  and log which source won.
- `_run_symbol_worker`'s dispatch branch has a test that fails when the branch is inverted.
- No production config value changed by this plan.
</success_criteria>

<output>
Create `.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-01-SUMMARY.md`
when done.
</output>
