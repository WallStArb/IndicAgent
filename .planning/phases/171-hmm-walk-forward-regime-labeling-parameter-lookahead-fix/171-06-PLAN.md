---
phase: 171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix
plan: 06
type: execute
wave: 3
depends_on: ["171-05"]
files_modified:
  - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-ROLLOUT-PRECONDITION.md
  - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-ROLLOUT-EVIDENCE.md
  - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-06-provenance-report.json
  - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-06-iters-used-distribution.json
autonomous: true
requirements: [REQ-3, REQ-4]

must_haves:
  truths:
    - "The full rollout does not start until a re-checked ps aux proves no competing corpus-scale writer is running and Phase 151 waves 6-7 have been explicitly coordinated with, not assumed idle"
    - "The rollout scope is derived from the corpus as it actually exists at execution time, never from a hardcoded symbol list copied out of a planning document"
    - "alpha.hmm.walk_forward.enabled is flipped through ConfigService so config_history records who changed it and why, never via a raw SQL UPDATE"
    - "Every in-scope (symbol, tf) cell is NULLed and then relabeled, and the checked-in verifier proves single-method provenance across the whole corpus, not a sample"
    - "feature_ic_scores is fully recomputed with --refresh over the same scope, so no IC row survives that was measured against pre-fix regime labels"
    - "todo 226's iters_used cap-headroom distribution is measured at full corpus scale on the path that actually ran, and alpha.hmm.n_iter is NOT changed in this phase"
  artifacts:
    - path: ".planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-ROLLOUT-PRECONDITION.md"
      provides: "the recorded, re-checked evidence that the hard gate was satisfied before the rollout launched"
      contains: "PRECONDITION"
    - path: ".planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-ROLLOUT-EVIDENCE.md"
      provides: "REQ-3/REQ-4 completion evidence plus D-04's cap-headroom distribution report"
      contains: "iters_used"
    - path: ".planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-06-provenance-report.json"
      provides: "per-cell REQ-3 provenance proof across the full corpus scope"
      contains: "verdict"
  key_links:
    - from: "scripts/ops/corpus/ops_regime_null_out_and_verify.py"
      to: "feature_vectors"
      via: "per-(symbol, tf) NULL-out on a single serial connection, full corpus scope"
      pattern: "UPDATE feature_vectors"
    - from: "services/regime_writer.py"
      to: "services/ic_engine.py --refresh"
      via: "regime is the stratification key feature_ic_scores is computed against"
      pattern: "--refresh"
    - from: "logs/regime_writer.log"
      to: ".planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-06-iters-used-distribution.json"
      via: "regime_writer.walk_forward_hmm_convergence_iters events aggregated at full scale"
      pattern: "walk_forward_hmm_convergence_iters"
---

<objective>
Execute the full-corpus rollout: NULL-out and walk-forward relabel every (symbol, tf) cell
(REQ-3), re-run `ic_engine` over the same scope so `feature_ic_scores` reflects the new
stratification key (REQ-4), and collect todo 226's cap-headroom distribution as a side effect
(D-04).

Purpose: this is the phase's blast-radius step — the class CLAUDE.md already documents for an
`HMM_RANDOM_STATE` change. CONTEXT.md's Sequencing section requires it to ride in the same
corpus-recompute pass as Phase 151 waves 6-7 rather than being a standalone third `ic_engine`
pass, and RESEARCH.md Pitfall 4 documents two live multi-day background jobs that must not be
contended with. Those constraints are encoded here as a hard blocking gate, not prose advice.

Output: a relabeled corpus with proven single-method provenance, a fully refreshed
`feature_ic_scores`, and a measured `iters_used` distribution.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-CONTEXT.md
@.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-RESEARCH.md
@.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-PILOT-GATE.md
@CLAUDE.md
@docs/foundation/performance-investigation-sop.md
</context>

<interfaces>
Values and surfaces this plan consumes (verified at plan time 2026-08-07 — every one of these is
a snapshot and must be re-read at execution time):

- `alpha.validation.oos_start = 2025-12-24T05:15:00Z` — the value `ic_engine.py`'s REQUIRED
  `--training-window-end` must be given. Read it live, do not hardcode this string.
- `infra.regime_writer.workers = 12`, `infra.ic_engine.workers = 8`.
- `alpha.hmm.walk_forward.enabled = false`, `alpha.hmm.n_restarts = 1`.
- `feature_vectors` held 80 distinct symbols per tf and 36,854,099 total rows; 83 chunks, 80
  compressed. The ROADMAP's "231 symbols" is the instrument universe, NOT the corpus — derive
  actual scope from the table.
- `services/ic_engine.py` argparse: `--symbols` (nargs="*"), `--tf` (nargs="*", choices are the
  four tfs), `--workers`, `--training-window-end` (REQUIRED), `--cross-sectional-only`,
  `--refresh`, `--dry-run-validity`.
- `services/regime_writer.py` argparse: `--symbols`, `--tf`, `--workers`, `--refit`, plus
  171-01's `--walk-forward` / `--no-walk-forward`.
- `ConfigService.set(key, value, changed_by=..., reason=...)` in
  `src/config/config_service.py` line 165 — async, transactional across `config_history`,
  `config_state`, `config_outbox`. Live usage example:
  `scripts/ops/alpha/ops_ic_shrinkage.py` lines 504-518.
- `regime_writer.py` logs to `logs/regime_writer.log` via `setup_service_logging`, called both
  at module scope (line 91) and inside `_run_symbol_worker` (line 1521) — so the per-segment
  `regime_writer.walk_forward_hmm_convergence_iters` events emitted inside workers land in that
  same file. That file is D-04's data source.
- Corpus consistency gate query (from `scripts/ops/corpus/ops_corpus_pipeline_run.sh` line 105):
  `SELECT COUNT(DISTINCT regime_set) FROM (SELECT symbol, ARRAY_AGG(DISTINCT regime ORDER BY regime)::text AS regime_set FROM feature_vectors WHERE regime IS NOT NULL GROUP BY symbol) sub`
  — must return 1.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Hard blocking precondition gate and scope derivation</name>
  <files>.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-ROLLOUT-PRECONDITION.md</files>
  <read_first>
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-PILOT-GATE.md
      (the `PILOT VERDICT:` line, the decided `alpha.hmm.n_restarts` value, and any quarantined
      cell names this rollout must carry forward)
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-CONTEXT.md
      "Sequencing" paragraph (ride in the same corpus-recompute pass as Phase 151 waves 6-7, not
      a standalone third `ic_engine` pass)
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-RESEARCH.md
      Pitfall 4 and the Environment Availability table (the two live background jobs and the
      explicit instruction that their status is a snapshot which WILL have changed)
    - .planning/phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-07-PLAN.md
      frontmatter and objective, and 151-08-PLAN.md frontmatter (what waves 6-7 actually do:
      a per-partition `feature_vectors` DELETE-then-INSERT recompute plus an interaction IC sweep)
    - .planning/todos/pending/259-single-name-equity-backfill-135-symbols-missing.md
    - CLAUDE.md, "Always parallel tracks" and the CPU/RAM contention caution
  </read_first>
  <blocking_precondition>
This is a HARD GATE. If any check below is not satisfied, STOP, write the findings into
`171-ROLLOUT-PRECONDITION.md`, and report status. Do NOT proceed to Task 2. Do NOT proceed
"carefully" or "with reduced workers". A partial or contended full-corpus mutation of
`feature_vectors` is worse than not starting.

**P1 — Pilot verdict is GO.**
`grep -E "^PILOT VERDICT: (GO|NO-GO)$" .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-PILOT-GATE.md`
must return `PILOT VERDICT: GO`. A NO-GO means this plan does not run at all until the failing
criterion is investigated and the pilot re-rendered.

**P2 — No competing corpus-scale process is running.**
Run, verbatim:
`ps aux | grep -E "ic_engine|infrastructure_run_historical_pipeline" | grep -v grep`
Record the full output in the precondition document, including the empty case.

At plan time (2026-08-07) two processes were confirmed live via `ps aux`: PID 3159680,
`infrastructure_run_historical_pipeline.py --client-id 43` (todo 259's universe-expansion
backfill), and PID 3268544, `ic_engine.py --refresh --tf 15m` (todo 256's ensemble-eligibility
re-check). **These PIDs are a snapshot from planning, not a live fact — re-check, never assume.**
PIDs get recycled; match on the command string, not the number.

Gate: BOTH must have exited. A running `infrastructure_run_historical_pipeline.py` means the
corpus is still growing underneath the scope this plan is about to derive, and a running
`ic_engine.py` means a second concurrent pass would contend for the same DB and CPU and would be
the redundant third pass CONTEXT.md's Sequencing section exists to prevent.

Also check `ps aux | grep -E "regime_writer|backfill_feature_factory" | grep -v grep` and record
it. Any hit here is likewise blocking.

**P3 — Phase 151 waves 6-7 are coordinated, not assumed.**
Run `ls .planning/phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-07-SUMMARY.md .planning/phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-08-SUMMARY.md 2>&1`
and record the result. Three cases, each with a different required action:

- Both SUMMARYs exist -> waves 6-7 already executed. Their `--recompute` pass rewrote
  `feature_vectors` rows. Proceed, and record in the precondition document that this plan's
  `ic_engine --refresh` is the first pass after both changes, therefore covering both.
- Neither exists -> waves 6-7 have not run. Waves 6-7's own `backfill_feature_factory.py
  --recompute` rewrites `feature_vectors` rows per partition, which would invalidate this plan's
  freshly written regime labels for the rewritten columns and force a second `ic_engine` pass.
  **This is blocking.** Report and STOP, recommending that Phase 151 wave 6's recompute runs
  FIRST and this plan's rollout follows it, so one `ic_engine --refresh` serves both — which is
  exactly what CONTEXT.md's Sequencing section asks for.
- Exactly one exists -> wave 7 is in flight or wave 6 is done and 7 pending. Report the specific
  state and STOP; this needs a human sequencing decision, not a planner guess.

**P4 — The corpus is not mid-mutation.**
`PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT pid, state, wait_event_type, wait_event, left(query, 120) FROM pg_stat_activity WHERE datname='indicagent' AND state <> 'idle' ORDER BY query_start"`
Record the output. Any active long-running write against `feature_vectors` is blocking.
  </blocking_precondition>
  <action>
Write `171-ROLLOUT-PRECONDITION.md` in the phase directory recording, verbatim, the output of
every check in the blocking precondition block above, each with a timestamp, plus a single
`PRECONDITION: SATISFIED` or `PRECONDITION: BLOCKED` line naming the specific failing check.

If SATISFIED, additionally derive and record the rollout scope in the same document:

Symbols: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT DISTINCT symbol FROM feature_vectors ORDER BY symbol"`.
Record the count and the full list. Do NOT use the ROADMAP's "231 symbols" figure — that is the
instrument universe, and `feature_vectors` held only 80 distinct symbols per tf at plan time.
The scope is what the table actually contains at execution time.

Cell inventory: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT tf, count(DISTINCT symbol) AS symbols, count(*) AS rows, count(*) FILTER (WHERE regime IS NOT NULL) AS labeled FROM feature_vectors GROUP BY tf ORDER BY tf"`.
Record it. The `labeled` column is the pre-rollout baseline that Task 2's post-conditions are
measured against.

Chunk/compression state: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT count(*) FILTER (WHERE is_compressed) AS compressed, count(*) AS total FROM timescaledb_information.chunks WHERE hypertable_name='feature_vectors'"`.
Record it so the rollout's elapsed time can be interpreted afterward rather than theorized about,
per `docs/foundation/performance-investigation-sop.md`.

Carry-forward parameters from the pilot gate, stated explicitly in this document: the decided
`alpha.hmm.n_restarts` value, and any quarantined (symbol, tf) cells that must be EXCLUDED from
this rollout and reported rather than silently relabeled.

Extrapolated runtime, from the pilot gate's G4 estimate scaled to the derived cell count. State
the number. Launching a multi-day mutation with no estimate is the failure mode this exists to
prevent.

Do not mutate anything in this task. It is read-only reconnaissance plus a written record.
  </action>
  <verify>
    <automated>grep -Eq "^PRECONDITION: (SATISFIED|BLOCKED)$" .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-ROLLOUT-PRECONDITION.md</automated>
  </verify>
  <acceptance_criteria>
    - `171-ROLLOUT-PRECONDITION.md` contains exactly one line matching
      `^PRECONDITION: (SATISFIED|BLOCKED)$`.
    - The document contains the verbatim output of the `ps aux` check for
      `ic_engine|infrastructure_run_historical_pipeline`, with a timestamp, including the empty
      case rendered explicitly rather than omitted.
    - The document names which of the three P3 Phase 151 cases applies and cites the `ls` result
      it was derived from.
    - On SATISFIED: the document lists the derived symbol count and the full symbol list, the
      per-tf row/labeled counts, the chunk compression counts, the carry-forward
      `alpha.hmm.n_restarts` value, any quarantined cells, and an extrapolated runtime estimate.
    - On BLOCKED: the document names the exact failing check and the concrete condition that
      would clear it, and `git status --short` shows no change to any file outside
      `.planning/` — nothing was mutated.
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT config_value FROM config_state WHERE config_key='alpha.hmm.walk_forward.enabled'"`
      still returns `false` at the end of this task.
  </acceptance_criteria>
  <done>The rollout's preconditions are re-checked against live state and recorded, the scope is
  derived from the corpus rather than copied from a document, and a BLOCKED result stops the plan
  instead of degrading it.</done>
</task>

<task type="auto">
  <name>Task 2: Full-corpus NULL-out, production config flip, and walk-forward relabel (REQ-3)</name>
  <files>.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-06-provenance-report.json</files>
  <read_first>
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-ROLLOUT-PRECONDITION.md
      (this task's own gate result, derived scope, and carry-forward parameters)
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-03-SUMMARY.md
      (the NULL-out tool's real CLI surface, manifest semantics, and failure handling)
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-05-SUMMARY.md
      (the pilot's measured per-cell NULL-out and relabel timings — the empirical basis for this
      task's runtime expectations)
    - scripts/ops/alpha/ops_ic_shrinkage.py lines 495-520 (the live `ConfigService.set()` usage
      pattern to mirror for the config flip — `ConfigService(database_url=dsn, pool=apool)`,
      `await initialize()`, then `await set(key, value, changed_by=..., reason=...)`)
    - src/config/config_service.py lines 165-230 (what `set()` guarantees: a transactional write
      across `config_history`, `config_state`, and `config_outbox`)
    - CLAUDE.md, the ProcessPoolExecutor orphan-reaping gotcha and the "never log per-row inside a
      loop over the full corpus" rule
  </read_first>
  <blocking_precondition>
Re-run the P2 process check immediately before launching, even if Task 1 passed — hours may have
elapsed between tasks and a background job can start in that window:

`ps aux | grep -E "ic_engine|infrastructure_run_historical_pipeline|regime_writer|backfill_feature_factory" | grep -v grep`

Must be empty except for this task's own processes. If not empty, STOP and report; do not proceed.

Also re-assert `grep -E "^PRECONDITION: SATISFIED$" .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-ROLLOUT-PRECONDITION.md`
returns a match. A BLOCKED precondition document means this task does not run.
  </blocking_precondition>
  <action>
Step 1 — flip the production flag through ConfigService, first, so that any subsequent
`regime_writer.py` invocation in this corpus (including a resumed one) uses the correct method by
default rather than depending on a CLI flag being remembered.

Create a one-shot operational script at `cache/171_06_flip_walk_forward.py` (deliberately under
`cache/`, not checked in — this is a single operational action, not reusable tooling, and adding
CLI surface for it would be the speculative-build D-00 forbids). It must mirror
`scripts/ops/alpha/ops_ic_shrinkage.py`'s ConfigService block: build the DSN from
`Settings().database_url` with the `postgresql+asyncpg://` prefix replaced by `postgresql://`,
create an `asyncpg` pool, construct `ConfigService(database_url=dsn, pool=apool)`, `await
initialize()`, then `await set("alpha.hmm.walk_forward.enabled", True, changed_by="phase-171-06",
reason=...)` with a reason string citing the pilot gate verdict and the phase number. If the
pilot decided `alpha.hmm.n_restarts` should change from 1, set that key in the same script with
its own reason string citing the D-03 attribution verdict; if the verdict was INCONCLUSIVE, do
not touch that key at all.

Never issue a raw `UPDATE config_state` for this. The whole point of routing through
`ConfigService.set()` is that `config_history` records `changed_by` and `reason`, which is this
project's APR provenance discipline and the only durable record of why a blast-radius-class flag
was flipped.

Verify the flip landed with history:
`PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT timestamp, config_value, changed_by, reason FROM config_history WHERE config_key='alpha.hmm.walk_forward.enabled' ORDER BY timestamp DESC LIMIT 3"`

**Safe Interrupted State — read this before launching Steps 2-4.** The flag flip happens BEFORE
the NULL-out and the tf-staged relabel complete, so an interruption anywhere in Steps 2-4 leaves
the corpus with some cells NULLed-but-not-yet-relabeled, some already carrying walk-forward
labels, and some still untouched. That mixed state is explicitly ACCEPTABLE here, and the reason
is specific to this project's current runtime, not a general tolerance: the live IBKR ingestion
chain is intentionally stopped, no service reads `feature_vectors.regime` on a live path during
the rollout window, and Phase 148's Gate 2 FAIL means no live capital consumes anything
downstream of it. Verify both of those are still true at execution time (`systemctl list-units
--all | grep indicagent`, plus the Task 1 P2 process check) rather than taking this sentence on
faith — if a live consumer HAS been started since plan time, stop and re-sequence instead of
proceeding.

Given that, only two invariants must hold across an interruption:

(a) `config_state`'s `alpha.hmm.walk_forward.enabled` value matches what `config_history`'s latest
row for that key says. A value with no matching history row (or a stale value contradicting the
latest row) is the one genuinely unrecoverable state, because it destroys the audit trail for a
blast-radius-class flag. Re-run the `config_history` query above after any interruption and
confirm agreement.

(b) The NULL-out manifest's per-cell status accurately reflects what actually happened in the
database — never a `verified_null` entry for a cell that was not in fact NULLed. If a kill landed
mid-cell, re-run `--mode verify-post-null` for that cell rather than trusting the manifest entry.

**On an interruption, DO NOT flip `alpha.hmm.walk_forward.enabled` back to `false`.** The correct
and only response is to resume the relabel from the manifest. A flip-back would leave the
already-relabeled cells correctly labeled while stranding the not-yet-relabeled cells permanently:
nothing would ever relabel them under the old flag value once `regime_writer` resumes running on
production defaults, and the corpus would sit mixed-provenance with no process scheduled to
resolve it — the exact failure class RESEARCH.md's Critical Finding documents. Resume forward;
never roll the flag back.

Step 2 — NULL-out the full derived scope. Use a rollout-specific manifest so the pilot's manifest
can never cause a cell to be skipped:

`.venv/bin/python scripts/ops/corpus/ops_regime_null_out_and_verify.py --symbols <full derived list from Task 1> --tf 5m 15m 1h 1d --manifest cache/regime_null_out_manifest_full.json --dry-run`

Review the per-cell plan and total `pre_null_labeled` against Task 1's recorded per-tf `labeled`
counts; they must agree. Then re-run without `--dry-run`, tee'd to `logs/171_06_null_out.log`.
Exclude any cells quarantined by the pilot gate.

The tool runs one UPDATE per (symbol, tf) on a single serial main-process connection by
construction. Do not try to parallelize it: `feature_vectors` is a compressed TimescaleDB
hypertable and concurrent writers on the same hypertable cause index-page deadlocks, which is
CLAUDE.md's explicit ProcessPoolExecutor rule and DAG Invariant 3.

If the run is killed, re-launch it with the same `--manifest` — verified cells are skipped and
only in-progress or absent cells are redone. Do not delete the manifest to "start clean"; that
discards exactly the resumability the tool was built for.

Step 3 — relabel, tf-staged, cheapest first:

`nohup .venv/bin/python services/regime_writer.py --symbols <full derived list> --tf 1d 1h --refit --walk-forward > logs/171_06_relabel_stage_a.log 2>&1 &`

then the `--tf 15m 5m` stage after Stage A completes and its provenance spot-checks pass. Pass
`--walk-forward` explicitly even though the APR flag is now true — belt and braces, so the run
cannot silently depend on the flip having propagated, and so `walk_forward_source=cli` in the
`regime_writer.starting` log proves which value was actually in force.

`--symbols` is mandatory: `_discover_symbols()` skips fully-labeled symbols, and an omitted list
is the exact silent-no-op trap RESEARCH.md's Critical Finding documents.

If a stage must be killed, follow CLAUDE.md's orphan rule exactly: kill the main PID, then
`ps aux | grep regime_writer.py | awk '{print $2}' | xargs -r kill`, and confirm zero remain
before relaunching. Orphaned forkserver workers keep holding DB connections and writing.

Step 4 — prove provenance across the whole scope:

`.venv/bin/python scripts/ops/corpus/ops_regime_null_out_and_verify.py --symbols <full derived list> --tf 5m 15m 1h 1d --manifest cache/regime_null_out_manifest_full.json --mode verify-post-relabel 2>&1 | tee logs/171_06_provenance.log`

Copy `cache/regime_relabel_provenance_report.json` to
`evidence/171-06-provenance-report.json`.

Step 5 — run the corpus consistency gate that `ops_corpus_pipeline_run.sh` runs after its own
regime step:
`PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT COUNT(DISTINCT regime_set) FROM (SELECT symbol, ARRAY_AGG(DISTINCT regime ORDER BY regime)::text AS regime_set FROM feature_vectors WHERE regime IS NOT NULL GROUP BY symbol) sub"`
Expected `1`. A value greater than 1 means symbols carry divergent label sets and must be
investigated before `ic_engine` runs — a thin-history symbol legitimately missing a rare label is
a plausible benign cause, but it must be diagnosed by naming the symbols, not waved through.

Do NOT run `services/ic_engine.py` in this task. That is Task 3, and it must not start until
provenance verification has passed.
  </action>
  <verify>
    <automated>test -s .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-06-provenance-report.json && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT config_value FROM config_state WHERE config_key='alpha.hmm.walk_forward.enabled'" | grep -qx true</automated>
  </verify>
  <acceptance_criteria>
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM config_history WHERE config_key='alpha.hmm.walk_forward.enabled' AND changed_by='phase-171-06'"`
      returns at least 1, and the recorded `reason` cites the pilot gate verdict.
    - `evidence/171-06-provenance-report.json` contains one record per in-scope (symbol, tf) cell
      and zero records with `"verdict": "fail"`.
    - Every cell reported `no_labels` is listed by name in the plan SUMMARY with its
      insufficient-history arithmetic shown (`n_obs < initial_warmup_bars + 5`); an unexplained
      `no_labels` is a failure, not an acceptable outcome.
    - The corpus consistency query returns `1`, or the divergent symbols are named and diagnosed.
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM feature_vectors WHERE regime IS NOT NULL"`
      is non-zero and is compared against Task 1's recorded pre-rollout labeled count, with the
      delta explained by the warmup prefixes now being NULL.
    - `grep -c "walk_forward_source" logs/171_06_relabel_stage_a.log` is at least 1 and the value
      is `cli`.
    - `ps aux | grep regime_writer.py | grep -v grep` is empty at the end of the task — no orphaned
      forkserver workers survive.
  </acceptance_criteria>
  <done>Every in-scope cell carries walk-forward labels with proven single-method provenance, the
  production flag flip is recorded in `config_history` with a reason, and no orphaned worker is
  still writing.</done>
</task>

<task type="auto">
  <name>Task 3: Downstream ic_engine re-run (REQ-4) and D-04 cap-headroom distribution</name>
  <files>.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-ROLLOUT-EVIDENCE.md, .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-06-iters-used-distribution.json</files>
  <read_first>
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-RESEARCH.md
      Pitfall 2 (why `--refresh` is used explicitly rather than trusting fingerprint
      auto-invalidation) and the Runtime State Inventory's "Canonical question answered" paragraph
      (`feature_ic_scores` is the only remaining old-value surface)
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-CONTEXT.md
      D-04 (collect the data, do NOT change `alpha.hmm.n_iter` in this phase)
    - .planning/todos/pending/226-regime-writer-n-iter-convergence-headroom-check.md (the todo's
      own statement of what a real full-corpus measurement must show before any cap change)
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-01-SUMMARY.md
      (the exact field names emitted by the per-segment
      `regime_writer.walk_forward_hmm_convergence_iters` event)
    - services/ic_engine.py lines 4938-4990 (the argparse block — `--training-window-end` is
      REQUIRED and has no default by design)
    - docs/plans/OOS-EVAL-PROTOCOL.md (why `--training-window-end` must be the OOS clamp, not a
      bare MAX(bar_ts))
  </read_first>
  <blocking_precondition>
Re-run the process check immediately before launching `ic_engine`:
`ps aux | grep -E "ic_engine|infrastructure_run_historical_pipeline|regime_writer|backfill_feature_factory" | grep -v grep`
Must be empty. A second concurrent `ic_engine` pass is the specific redundancy CONTEXT.md's
Sequencing section and RESEARCH.md Pitfall 4 exist to prevent. If not empty, STOP and report.

Also re-assert that `evidence/171-06-provenance-report.json` contains zero `"verdict": "fail"`
records. Running `ic_engine` over a corpus whose provenance has not been proven would produce
`feature_ic_scores` rows measured against mixed-method labels — the exact failure this phase is
fixing.
  </blocking_precondition>
  <action>
Step 1 — REQ-4's re-run. Read the training window end from live config rather than hardcoding it:
`PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT config_value FROM config_state WHERE config_key='alpha.validation.oos_start'"`
(the value was `2025-12-24T05:15:00Z` at plan time; use whatever the query returns).

Launch:
`nohup .venv/bin/python services/ic_engine.py --symbols <same derived list as Task 2> --tf 5m 15m 1h 1d --refresh --training-window-end <value from the query above> > logs/171_06_ic_engine.log 2>&1 &`

`--refresh` is mandatory and explicit. Per RESEARCH.md Pitfall 2 and its Assumption A1, whether
`ic_engine`'s Phase 162 whole-cell fingerprint watermark organically detects an in-place
`feature_vectors.regime` mutation was NOT independently verified — `--refresh` bypasses the
fingerprint entirely, so correctness does not depend on that unverified assumption.

Run `--dry-run-validity` first to see the skip/compute partition and confirm the scope is what you
expect before committing to the full pass.

Do NOT pass `--cross-sectional-only`. The per-symbol pass is what recomputes `symbol_hmm`-scope
rows, and plan 171-07's todo-167 verification depends on those rows existing.

Do NOT run `services/forward_return_writer.py` or `services/cross_sectional_regime_model.py` for
this phase. Forward returns do not depend on `regime`, and the cross-sectional regime model writes
`market_regimes`, an independent table that RESEARCH.md's Runtime State Inventory confirms is
untouched by this phase.

Explicit documented scope decision, to be stated in the evidence document rather than silently
omitted: `ensemble_trainer` and `alpha_publisher` are NOT re-run by this phase. RESEARCH.md's
Assumption A2 flagged this as needing confirmation; the reasoned answer recorded here is that
the ROADMAP's five locked requirements name only `ic_engine`/`feature_ic_scores` (REQ-4), and
Phase 148's Gate 2 FAIL means no live capital consumes `alpha_publisher`'s output, so a re-run
would produce no decision-relevant change. Record this reasoning; do not expand scope.

Step 2 — D-04's cap-headroom distribution. `logs/regime_writer.log` now contains one
`regime_writer.walk_forward_hmm_convergence_iters` event per refit segment across the entire
rollout — the full-corpus measurement todo 226's own text requires before any `n_iter` change.

Parse that log for those events (filter on the walk-forward-specific event name, NOT the
single-fit path's `regime_writer.hmm_convergence_iters`, which is why 171-01 gave them distinct
names) and write `evidence/171-06-iters-used-distribution.json` containing: total segment count;
count and fraction with `converged=false`; the `iters_used` distribution as min, p50, p90, p95,
p99, max; the count of segments where `iters_used` equals `n_iter_cap` exactly (the cap-hit set);
and a per-tf breakdown of the same. Also include the per-(symbol, tf) identity of the worst
offenders — the cells contributing the most cap-hit segments — since todo 226's eventual decision
needs to know whether cap pressure is corpus-wide or concentrated.

Do NOT change `alpha.hmm.n_iter`. D-04 is explicit: this phase reads the data, a later todo acts
on it. Changing the cap here on freshly-collected data would be exactly the "don't change n_iter
blind" behavior the todo forbids, and it would invalidate the corpus that was just computed.

Step 3 — write `171-ROLLOUT-EVIDENCE.md` recording: REQ-3's provenance verdict and the counts
behind it; REQ-4's `ic_engine` run parameters, wall-clock, and resulting `feature_ic_scores` row
count per tf (`SELECT tf, count(*) FROM feature_ic_scores GROUP BY tf ORDER BY tf`); the D-04
distribution summary with a plain-language read on whether the 200-iteration cap has headroom;
the corpus consistency gate result; the todo 229 blast-radius confirmation (the now-functional
retry path's behavior at full scale, read off the `converged=false` fraction — this is the
verification commit `ba8a74ef` explicitly deferred to "the next scheduled corpus rebuild"); and
the ensemble/publisher out-of-scope reasoning from Step 1.

Record an expected, non-alarming observation explicitly so a later reader does not mistake it for
a regression: the warmup-prefix bars of every cell are now NULL where they previously carried
labels, so regime-stratified IC cells have fewer rows than before. That is the fix working, not
data loss.
  </action>
  <verify>
    <automated>test -s .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-06-iters-used-distribution.json && grep -q "iters_used" .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-ROLLOUT-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `logs/171_06_ic_engine.log` shows a completed run with a non-zero cell count, and
      `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT tf, count(*), max(computed_at) FROM feature_ic_scores GROUP BY tf ORDER BY tf"`
      shows `max(computed_at)` after this task's start time for every tf.
    - `grep -c -- "--refresh" logs/171_06_ic_engine.log` or the recorded invocation in
      `171-ROLLOUT-EVIDENCE.md` confirms `--refresh` was passed; `--cross-sectional-only` was not.
    - `evidence/171-06-iters-used-distribution.json` parses and contains keys for total segments,
      converged-false count, the `iters_used` percentiles, the exact-cap-hit count, and a per-tf
      breakdown.
    - The distribution's total segment count is greater than the number of (symbol, tf) cells
      relabeled — proof the per-segment instrumentation from 171-01 is emitting once per segment,
      not once per cell.
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT config_value FROM config_state WHERE config_key='feature.hmm.n_iter'"`
      returns the same value it had before this phase — `n_iter` was NOT changed.
    - `171-ROLLOUT-EVIDENCE.md` states the ensemble/publisher out-of-scope reasoning and the
      expected warmup-prefix NULL observation.
    - `ps aux | grep ic_engine | grep -v grep` is empty at the end of the task.
  </acceptance_criteria>
  <done>REQ-4 is satisfied by an explicit `--refresh` pass over the same scope that was relabeled,
  todo 226's full-corpus cap-headroom measurement exists as structured data, and `n_iter` was left
  alone as D-04 requires.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None in the ASVS sense. Per RESEARCH.md's Security Domain section: internal batch-compute
correctness work, no network-facing surface, no authentication/session/access-control boundary,
no untrusted input. No ASVS L1 category applies. The genuine risk class is data integrity and
resource contention over a live multi-hour corpus mutation.

## Correctness Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-25 | Resource contention / corrupted concurrent write | `feature_vectors`, `feature_ic_scores` | mitigate | `<blocking_precondition>` blocks on Tasks 1, 2, and 3 requiring a fresh `ps aux` check for `ic_engine` and `infrastructure_run_historical_pipeline`; STOP-and-report is the only non-satisfied outcome, with no reduced-workers fallback |
| T-171-26 | Redundant third `ic_engine` pass | Phase 151 waves 6-7 coordination | mitigate | P3 enumerates three explicit Phase 151 states with a required action each; the not-yet-run case is blocking, per CONTEXT.md's Sequencing decision |
| T-171-27 | Stale-snapshot scope | rollout symbol list | mitigate | Scope derived from `SELECT DISTINCT symbol FROM feature_vectors` at execution time; the ROADMAP's 231 figure is explicitly identified as the instrument universe, not the corpus |
| T-171-28 | Untraceable production config change | `alpha.hmm.walk_forward.enabled` | mitigate | Flip routed through `ConfigService.set()` with `changed_by='phase-171-06'` and a reason citing the pilot verdict; raw `UPDATE config_state` explicitly forbidden; `config_history` row asserted as an acceptance criterion |
| T-171-29 | Cross-plan manifest contamination | `cache/regime_null_out_manifest*.json` | mitigate | Rollout uses `cache/regime_null_out_manifest_full.json`, distinct from the pilot's manifest, so pilot `verified_null` entries cannot cause a rollout cell to be skipped |
| T-171-30 | Orphaned forkserver workers writing after a kill | `regime_writer.py` ProcessPoolExecutor | mitigate | CLAUDE.md's reap-then-restart procedure written into the action; empty `ps aux \| grep regime_writer.py` asserted as an acceptance criterion |
| T-171-31 | Fingerprint false-skip on a mutated column | `ic_engine.py` Phase 162 whole-cell fingerprint | mitigate | `--refresh` passed explicitly so correctness never depends on RESEARCH.md's unverified Assumption A1; `--dry-run-validity` run first to inspect the partition |
| T-171-32 | Blind cap change on fresh data | `feature.hmm.n_iter` | mitigate | D-04's read-only mandate encoded; acceptance criterion asserts the value is unchanged after the phase |
| T-171-33 | Unbounded runtime on a 96%-compressed hypertable | `feature_vectors` bulk UPDATE | mitigate | Chunk compression state recorded before the run, per-cell `elapsed_s` from the pilot used to extrapolate, resumable manifest so a kill costs one cell rather than the run |
| T-171-SC | Tampering (supply chain) | package installs | accept | This plan installs no packages |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/ -q` green (wave merge gate per 171-VALIDATION.md).
- `171-ROLLOUT-PRECONDITION.md` carries a `PRECONDITION:` line with verbatim check output.
- `evidence/171-06-provenance-report.json` has zero `fail` verdicts across the full scope.
- Corpus consistency query returns 1.
- `config_history` shows the `alpha.hmm.walk_forward.enabled` flip with `changed_by='phase-171-06'`.
- `feature_ic_scores.computed_at` is fresh for every tf.
- `feature.hmm.n_iter` unchanged.
</verification>

<success_criteria>
- REQ-3 is complete at full corpus scale with per-cell provenance proven by the checked-in
  verifier, not by sampling.
- REQ-4 is complete: `feature_ic_scores` fully recomputed with `--refresh` over the same scope,
  leaving no IC row measured against pre-fix regime labels.
- The production flag flip is auditable in `config_history` with a stated reason.
- D-04's cap-headroom distribution exists as structured data and `alpha.hmm.n_iter` was not
  changed.
- The rollout never ran concurrently with a competing corpus-scale job, and a blocked precondition
  would have stopped it rather than degrading it.
</success_criteria>

<output>
Create `.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-06-SUMMARY.md`
when done.
</output>
