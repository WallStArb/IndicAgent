---
phase: 171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/analysis/hmm_walk_forward_seed_stability_pilot.py
  - scripts/analysis/hmm_n_restarts_walk_forward_comparison_pilot.py
autonomous: true
requirements: [REQ-5]

must_haves:
  truths:
    - "_hmm_seed_stability_check can be run against real fetched corpus data for an arbitrary (symbol, tf) list, closing its invoked-nowhere status"
    - "The seed-stability pilot reports min_pairwise_agreement per cell and per training boundary, and the honest corpus-wide signal is the minimum across cells, not the average"
    - "n_restarts=1 and n_restarts>1 are compared entirely in memory, never as two writes to feature_vectors"
    - "The comparison output lets walk-forward-vs-production and n_restarts=1-vs-n_restarts>1 be attributed independently, as two separate verdict blocks"
  artifacts:
    - path: "scripts/analysis/hmm_walk_forward_seed_stability_pilot.py"
      provides: "REQ-5's first real-data exercise of _hmm_seed_stability_check"
      contains: "_hmm_seed_stability_check"
    - path: "scripts/analysis/hmm_n_restarts_walk_forward_comparison_pilot.py"
      provides: "D-03's parallel-arm attribution safeguard, out of band from feature_vectors"
      contains: "paired_bootstrap_ic_difference"
  key_links:
    - from: "scripts/analysis/hmm_walk_forward_seed_stability_pilot.py"
      to: "services/regime_writer.py::_hmm_seed_stability_check"
      via: "direct import, bypassing the CLI and DB-write path"
      pattern: "from services.regime_writer import"
    - from: "scripts/analysis/hmm_n_restarts_walk_forward_comparison_pilot.py"
      to: "scripts/analysis/_nonlinear_interaction_combiner_shared.py"
      via: "bootstrap_ic_stats / paired_bootstrap_ic_difference reuse"
      pattern: "_nonlinear_interaction_combiner_shared"
---

<objective>
Build the two diagnostic scripts the staged pilot (D-01) needs: a real-data seed-stability check
(REQ-5) and an out-of-band multi-seed-restart comparison (D-03).

Purpose: RESEARCH.md confirmed `_hmm_seed_stability_check` has zero call sites outside two
synthetic unit tests — it is exactly the "invoked-nowhere" state REQ-5 forbids. Separately, D-03's
parallel-arm comparison cannot be a live dual-write: `feature_vectors.regime` is a single column
keyed by `(symbol, tf, bar_ts)` with no `construction_name`-style discriminator, so Phase 168
D-02's schema-level pattern does not transfer. Both diagnostics belong in `scripts/analysis/`,
matching every prior pilot in this codebase and RESEARCH.md's Open Question 2 recommendation
(one-off script, not new CLI surface on the production service).

Output: two checked-in analysis scripts. This plan builds them; plan 171-05 runs them against the
pilot symbols and reads their verdicts.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-CONTEXT.md
@.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-RESEARCH.md
@.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-PATTERNS.md
@scripts/analysis/hmm_walk_forward_gate4_ic_pilot_spy_1h.py
</context>

<interfaces>
Signatures the scripts consume (verified 2026-08-07):

- `_hmm_seed_stability_check(obs_matrix, n_components, covariance_type, n_iter, seeds,
  full_cov_min_obs) -> dict` — `services/regime_writer.py` line 978. Returns
  `{"log_likelihoods": {seed: float}, "ll_spread": float,
  "pairwise_label_agreement": {(seed_a, seed_b): float}, "min_pairwise_agreement": float}`.
- `_build_obs_matrix(timestamps, closes, volumes, vol_window, momentum_window,
  vol_of_vol_window) -> tuple[np.ndarray, list]` — line 175.
- `_walk_forward_hmm_labels(obs_matrix, n_components, covariance_type, n_iter, hmm_random_state,
  refit_every_bars, initial_warmup_bars, min_hold_bars, full_cov_min_obs)
  -> tuple[list[str], list[tuple[int,int,int]]]` — line 568. Labels align 1:1 with
  `obs_matrix[initial_warmup_bars:]`.
- `bootstrap_ic_stats(score, actual, block_size, n_boot, seed) -> {"point_ic", "ci_lower",
  "ci_upper", "passes"}` — `scripts/analysis/_nonlinear_interaction_combiner_shared.py` line 627.
- `paired_bootstrap_ic_difference(score_a, score_b, actual, block_size, n_boot, seed)
  -> {"point_diff", "ci_lower", "ci_upper", "a_significantly_better", "b_significantly_better"}`
  — line 647.
- Ordinal label score used by the Gate 4 pilot: trending_down -2, transition_down -1, ranging 0,
  transition_up 1, trending_up 2.
- Forward-return source (Invariant 1, CLAUDE.md): `forward_returns` filtered on
  `return_type = 'executable_open_to_open'` and `complete_fast = true`, column `return_fast`.
- OHLCV source: `market_data_ohlcv_tradeable` (a view; the raw table is a synthetic-fill calendar
  grid), columns `timestamp`, `close`, `volume`, filtered on `symbol` and `timeframe`.
- Live HMM APR values: `feature.hmm.n_components=5`, `feature.hmm.covariance_type='full'`,
  `feature.hmm.n_iter=200`, `alpha.hmm.random_state=42`, `feature.hmm.full_cov_min_obs=500`,
  `feature.hmm.min_hold_bars=3`, `feature.hmm.vol_window=20`,
  `feature.hmm.obs_momentum_window=20`, `feature.hmm.obs_vol_of_vol_window=20`,
  `alpha.hmm.n_restarts=1`.
- Per-tf walk-forward params (`alpha.hmm.walk_forward.{refit_every_bars,initial_warmup_bars}.<tf>`):
  5m (19800, 39600), 15m (6600, 13200), 1h (1650, 3300), 1d (252, 504).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Real-data seed-stability pilot script (REQ-5)</name>
  <files>scripts/analysis/hmm_walk_forward_seed_stability_pilot.py</files>
  <read_first>
    - scripts/analysis/hmm_walk_forward_gate4_ic_pilot_spy_1h.py (full file — the established
      pilot-script shape: sys.path insert, named-cursor OHLCV stream, `_build_obs_matrix`, direct
      primitive call, `"=" * 80` verdict banner)
    - services/regime_writer.py lines 978-1044 (`_hmm_seed_stability_check` and its return
      contract)
    - services/regime_writer.py lines 568-654 (`_walk_forward_hmm_labels` — the boundary schedule
      whose training prefixes this script samples)
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-CONTEXT.md
      D-01 (staged pilot, bar-density spread requirement)
  </read_first>
  <action>
Create `scripts/analysis/hmm_walk_forward_seed_stability_pilot.py`, modeled structurally on
`hmm_walk_forward_gate4_ic_pilot_spy_1h.py` (same `sys.path.insert(0, parents[2])` preamble,
same named-server-side-cursor OHLCV streaming with `fetchmany(10000)`, same
`Settings().database_url` connection, same `"=" * 80` banner style), with these differences:

Multi-cell, not single-symbol. Define `_PILOT_CELLS` as the cross product of the 8 pilot symbols
`SPY, IWM, TLT, GLD, XLE, EEM, FXY, SMH` and the 4 tfs `1d, 1h, 15m, 5m`, expressed in that tf
order so the cheap timeframes report first. These 8 symbols span the bar-density and liquidity
spread D-01 requires rather than being a convenience sample: SPY (deepest history, highest
liquidity), IWM (small-cap beta), TLT (thinnest 1d history in the candidate set at ~2,381 bars,
the density-stress case), GLD (commodity), XLE (sector), EEM (international), FXY (currency,
thinnest 5m at ~276k bars), SMH (shortest overall history at ~3,415 1d bars). Expose
`--symbols` and `--tf` CLI overrides defaulting to these lists so the pilot scope is adjustable
without editing the file.

Per cell: fetch OHLCV from `market_data_ohlcv_tradeable`, build the obs matrix with
`_build_obs_matrix` at the live window values (20/20/20). Then evaluate
`_hmm_seed_stability_check` at the walk-forward TRAINING PREFIXES, not on the full matrix — the
prefixes are what the walk-forward path actually fits. Sample three boundaries per cell by
default (`--boundaries first,middle,last`): the first boundary `initial_warmup_bars`, the middle
boundary `initial_warmup_bars + refit_every_bars * (n_segments // 2)`, and the last boundary
before the end. For each sampled boundary `b`, call `_hmm_seed_stability_check(obs_matrix[:b],
n_components=5, covariance_type="full", n_iter=200, seeds=[42, 43, 44], full_cov_min_obs=500)`.
Seeds are `alpha.hmm.random_state + i`, matching todo 108's deterministic derivation. Expose
`--seeds` and `--boundaries` as CLI overrides.

Read the HMM hyperparameters and the per-tf walk-forward params from `config_state` at startup
(a plain SELECT against `config_state` is sufficient for a read-only analysis script) and print
them in the header, with the module-level constants listed in the Interfaces block above as
fallbacks. Hardcoding them would silently diverge the pilot from what the production refit
actually runs.

Skip a cell with a logged reason when `len(obs_matrix) < initial_warmup_bars + n_components` —
the same insufficient-history condition `_walk_forward_hmm_full` raises on. A skip is reported,
never silently swallowed.

Print per cell: `symbol`, `tf`, `n_obs`, `n_segments`, and for each sampled boundary the
`ll_spread`, every `pairwise_label_agreement` entry, and `min_pairwise_agreement`. Then a
corpus-wide summary block reporting the MINIMUM `min_pairwise_agreement` across all cells and
boundaries, plus the identity of the cell that produced it. The minimum is the honest signal;
do not print an average as the headline number, since an average hides exactly the degenerate
cell this check exists to find.

Emit a `SEED STABILITY` verdict line per cell using the gate this phase adopts: FAIL when
`min_pairwise_agreement <= 0.25`, which is at or below the ~21.7-22.1% five-label chance baseline
that migration 292's own descriptions record — at that level the labels are seed noise, not a
regime measurement. Values above 0.25 are recorded as evidence and do NOT fail: this phase ships
regardless of the Gate 4 ordinal-IC result per the ROADMAP, so the gate's job is catching a
degenerate cell before the 231-symbol blast radius, not re-litigating whether to do the fix.

Write the full per-cell result set to `cache/hmm_seed_stability_pilot_results.json` so plan 171-05
can attach it as evidence and plan 171-06 can quarantine any FAIL cell by name. Also print
per-cell wall-clock elapsed — the 5m cells fit HMMs on prefixes up to ~390k rows and this is the
measurement that sizes the full rollout's runtime, per RESEARCH.md Pitfall 3.

Do NOT write anything to `feature_vectors` from this script. Do NOT call
`regime_writer.py`'s CLI. This is a read-only diagnostic.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/analysis/hmm_walk_forward_seed_stability_pilot.py --symbols TLT --tf 1d --boundaries first</automated>
  </verify>
  <acceptance_criteria>
    - The verify command completes and prints a `SEED STABILITY` verdict line for TLT/1d with a
      numeric `min_pairwise_agreement`.
    - `cache/hmm_seed_stability_pilot_results.json` exists after that run and contains a record
      with `"symbol": "TLT"` and `"tf": "1d"`.
    - `grep -c "_hmm_seed_stability_check" scripts/analysis/hmm_walk_forward_seed_stability_pilot.py`
      is at least 1 (REQ-5's invoked-nowhere status is closed by a real call site).
    - `grep -v '^\s*#' scripts/analysis/hmm_walk_forward_seed_stability_pilot.py | grep -c "UPDATE feature_vectors"`
      is 0.
    - `grep -c "market_data_ohlcv_tradeable" scripts/analysis/hmm_walk_forward_seed_stability_pilot.py`
      is at least 1, and `grep -v '^\s*#' scripts/analysis/hmm_walk_forward_seed_stability_pilot.py | grep -c "FROM market_data_ohlcv "`
      is 0 (the raw synthetic-fill table is never read).
    - `.venv/bin/ruff check scripts/analysis/hmm_walk_forward_seed_stability_pilot.py` is clean.
  </acceptance_criteria>
  <done>`_hmm_seed_stability_check` has a real call site against real corpus data, reports the
  minimum across cells rather than an average, and records per-cell runtime for rollout sizing.</done>
</task>

<task type="auto">
  <name>Task 2: Out-of-band n_restarts parallel-arm comparison (D-03)</name>
  <files>scripts/analysis/hmm_n_restarts_walk_forward_comparison_pilot.py</files>
  <read_first>
    - scripts/analysis/hmm_walk_forward_gate4_ic_pilot_spy_1h.py (full file — the ordinal-score,
      forward-return join, and paired-bootstrap comparison this script extends to a second arm)
    - services/regime_writer.py lines 1181-1260 (`_compute_symbol_tf`'s existing multi-seed
      restart loop — the best-converged-log-likelihood selection logic to replicate per segment,
      in this script only)
    - services/regime_writer.py lines 568-654 (`_walk_forward_hmm_labels` — the per-segment
      structure the n_restarts arm must mirror)
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-RESEARCH.md
      "Second Finding" (why this cannot be a dual-write and why `_walk_forward_hmm_full` takes no
      n_restarts parameter today)
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-CONTEXT.md
      D-03 (attribution safeguard requirement)
  </read_first>
  <action>
Create `scripts/analysis/hmm_n_restarts_walk_forward_comparison_pilot.py`. Structure follows the
Gate 4 pilot; the substance is D-03's attribution safeguard.

Do NOT modify `services/regime_writer.py` in this task. RESEARCH.md's Second Finding established
that `_walk_forward_hmm_full` and `_compute_symbol_tf_walk_forward` accept no `n_restarts`
parameter, and CONTEXT.md D-03 says the production default is set based on what the pilot shows —
not assumed. Threading `n_restarts` through the production path before the pilot has a verdict
would be building speculatively, which D-00 forbids. Implement the multi-restart arm inline in
this script by replicating `_compute_symbol_tf`'s selection loop per segment: for each refit
boundary, fit `n_restarts` candidates at seeds `hmm_random_state + i`, apply the corrected
convergence test `candidate.monitor_.iter < candidate.monitor_.n_iter` (never
`monitor_.converged`, which is unconditionally True in hmmlearn 0.3.3 — todo 229), give each
non-converged candidate the same doubled-`n_iter` single retry the production loop gives, and
select on the tuple `(candidate_converged, candidate_log_likelihood)`.

Produce THREE label sequences per cell, all in memory:
1. `prod_labels` — the existing `feature_vectors.regime` values, fetched from the database. These
   are the retired full-history-fit method's output and exist only until plan 171-05's NULL-out
   runs, which is exactly why plan 171-05 sequences this script BEFORE the NULL-out.
2. `wf_labels_r1` — walk-forward with a single seed, via `_walk_forward_hmm_labels` at
   `hmm_random_state=42`. This is the arm the production rollout will actually run.
3. `wf_labels_rN` — walk-forward with the inline multi-restart loop at `--n-restarts` (default 3).

Print TWO separate verdict blocks, never one conflated number — this is D-03's explicit
requirement:
- Block A, "walk-forward vs production": ordinal-score Spearman IC for `wf_labels_r1` and
  `prod_labels` against `forward_returns.return_fast` filtered on
  `return_type = 'executable_open_to_open'` and `complete_fast = true`, using `bootstrap_ic_stats`
  for each and `paired_bootstrap_ic_difference` for the difference. Also report raw label
  agreement between the two, comparable to the 24.9-56.8% figures migration 292's descriptions
  record.
- Block B, "walk-forward n_restarts=1 vs n_restarts=N": the same three statistics computed between
  `wf_labels_r1` and `wf_labels_rN`, plus their pairwise label agreement.

Use the live `alpha.ic.bootstrap_block_size.<tf>` value for `block_size` where the key exists
(the Gate 4 pilot used 10 at 1h and noted it matches the live APR value), `n_boot=500`, and
`seed=42`, all exposed as CLI flags. Read HMM hyperparameters from `config_state` as in Task 1.

CLI: `--symbols` and `--tf` (default the same 8-symbol / 4-tf pilot scope as Task 1),
`--n-restarts` (default 3), `--block-size`, `--n-boot`, `--seed`. Abort a cell with a printed
reason when fewer than 50 bars overlap across all three sequences and `forward_returns`, matching
the Gate 4 pilot's own minimum.

Emit a `D-03 ATTRIBUTION` summary at the end stating, per cell, whether the multi-restart arm was
better, worse, or indistinguishable, using this gate: the multi-restart arm is preferred only when
`paired_bootstrap_ic_difference(wf_rN, wf_r1, ...)["a_significantly_better"]` is True on a strict
majority of evaluated cells. Anything else is INCONCLUSIVE, and INCONCLUSIVE means
`alpha.hmm.n_restarts` stays at its current value of 1 — the burden of proof is on the change, per
this project's earn-promotion-through-proof principle.

Write the full result set to `cache/hmm_n_restarts_comparison_results.json`.

Do NOT write to `feature_vectors`. Do NOT change any `config_state` value from this script.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/analysis/hmm_n_restarts_walk_forward_comparison_pilot.py --symbols TLT --tf 1d --n-restarts 2 --n-boot 100</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints both a "walk-forward vs production" block and a
      "n_restarts=1 vs n_restarts=2" block as separate sections, and a `D-03 ATTRIBUTION` line.
    - `cache/hmm_n_restarts_comparison_results.json` exists after that run.
    - `grep -v '^\s*#' scripts/analysis/hmm_n_restarts_walk_forward_comparison_pilot.py | grep -c "monitor_\.converged"`
      is 0 (the corrected `monitor_.iter < monitor_.n_iter` test is used instead).
    - `grep -c "executable_open_to_open" scripts/analysis/hmm_n_restarts_walk_forward_comparison_pilot.py`
      is at least 1 (Invariant 1: theoretical close-to-close returns are never used).
    - `grep -v '^\s*#' scripts/analysis/hmm_n_restarts_walk_forward_comparison_pilot.py | grep -c "UPDATE \|INSERT INTO "`
      is 0.
    - `git diff --stat services/regime_writer.py` is empty for this task — the production path was
      not modified.
    - `.venv/bin/ruff check scripts/analysis/hmm_n_restarts_walk_forward_comparison_pilot.py` is
      clean.
  </acceptance_criteria>
  <done>D-03's two comparison arms are measurable in memory with independently attributable
  verdicts, and no production code or corpus row was touched to get them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None in the ASVS sense. Per RESEARCH.md's Security Domain section: internal read-only diagnostic
scripts, no network-facing surface, no authentication/session/access-control boundary, no user
input. No ASVS L1 category applies.

## Correctness Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-13 | Data integrity (accidental write) | `feature_vectors` | mitigate | Both scripts are read-only by construction; grep-based acceptance criteria assert zero `UPDATE`/`INSERT INTO` against `feature_vectors` |
| T-171-14 | Confounded attribution | D-03 verdict | mitigate | Two separate verdict blocks required by acceptance criterion; a single conflated number would make walk-forward and multi-restart effects inseparable |
| T-171-15 | Measurement drift from production | pilot hyperparameters | mitigate | HMM and walk-forward parameters read from live `config_state`, not hardcoded, so the pilot measures what the rollout will actually run |
| T-171-16 | Overstated return signal | IC computation | mitigate | `forward_returns` filtered on `return_type = 'executable_open_to_open'` per CLAUDE.md Invariant 1; grep-enforced |
| T-171-17 | Hidden degenerate cell | seed-stability summary | mitigate | Corpus-wide headline is the MINIMUM `min_pairwise_agreement`, not an average |
| T-171-18 | Speculative production change | `_walk_forward_hmm_full` signature | mitigate | Multi-restart arm implemented inline in the analysis script; `git diff --stat services/regime_writer.py` empty is an acceptance criterion |
| T-171-SC | Tampering (supply chain) | package installs | accept | No packages installed; `hmmlearn`, `scikit-learn`, `numpy`, `psycopg` already present |
</threat_model>

<verification>
- Both scripts run end-to-end on a single cheap cell (TLT/1d) and produce their result JSON files.
- `.venv/bin/pytest tests/unit/ -q` green (wave merge gate) — these scripts add no unit tests of
  their own; they are diagnostic one-offs whose correctness is demonstrated by the live smoke run,
  matching every prior pilot script in `scripts/analysis/`.
- `services/regime_writer.py` unmodified by this plan.
</verification>

<success_criteria>
- REQ-5's "invoked-nowhere" status is closed by a real call site exercised against real corpus data.
- The seed-stability gate is stated as a concrete numeric threshold (0.25, the five-label chance
  baseline) with the rationale that it catches degenerate cells rather than gating the fix.
- D-03's two arms are independently attributable, computed in memory, with the burden of proof on
  changing `alpha.hmm.n_restarts` away from 1.
- No production code path and no corpus row modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-04-SUMMARY.md`
when done.
</output>
