---
phase: 171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix
plan: 07
type: execute
wave: 4
depends_on: ["171-06"]
files_modified:
  - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-TODO-167-VERDICT.md
  - .planning/todos/pending/167-equity-cross-sectional-vs-symbol-hmm-never-falsifier-tested.md
  - .planning/todos/pending/108-hmm-multi-seed-restart-best-likelihood.md
  - .planning/todos/pending/226-regime-writer-n-iter-convergence-headroom-check.md
  - .planning/todos/PRIORITIES.md
autonomous: true
requirements: [REQ-4]

must_haves:
  truths:
    - "Whether this phase's ic_engine pass already covers the 49 equity symbols todo 167 needs is answered by running the gate's own precondition check against fresh data, never assumed from the fact that a full pass ran"
    - "The equity cross-sectional-vs-symbol-HMM falsifier produces a recorded verdict, or the exact remaining blocker is named with the concrete scoped command that would clear it"
    - "Todos 108, 167, and 226 carry the measured results this phase produced, and each is either closed with evidence or left open with a precisely stated remainder — never closed on a guess"
    - "alpha.hmm.n_iter is still unchanged: todo 226 keeps its decision open, this phase only supplied its measurement"
  artifacts:
    - path: ".planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-TODO-167-VERDICT.md"
      provides: "the equity falsifier gate's verbatim output and its interpretation"
      contains: "equity_regime_separation_gate"
    - path: ".planning/todos/PRIORITIES.md"
      provides: "corrected rows for the folded todos, matching their real post-phase state"
      contains: "171"
  key_links:
    - from: "scripts/analysis/equity_regime_separation_gate.py"
      to: "feature_ic_scores"
      via: "STEP 0 precondition check on symbol_hmm-scope row freshness for the 49 equity symbols"
      pattern: "BLOCKED-ON-NEXT-IC-ENGINE-RUN"
    - from: ".planning/todos/pending/226-regime-writer-n-iter-convergence-headroom-check.md"
      to: ".planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-06-iters-used-distribution.json"
      via: "the full-corpus measurement the todo's own text required before any cap change"
      pattern: "171-06-iters-used-distribution"
---

<objective>
Close out the phase's folded-todo obligations against the results it actually produced: run todo
167's already-built equity falsifier gate against the fresh `feature_ic_scores`, and reconcile
todos 108, 167, and 226 with the measurements this phase generated.

Purpose: CONTEXT.md's Folded Todos section is explicit that todo 167 is a **verification task, not
new implementation** — its falsifier gate script already exists and is verified, and the question
is whether this phase's `ic_engine` recompute already covers the 49 equity symbols it needs before
assuming a second scoped run is required. Separately, plans 171-05 and 171-06 produced the
empirical answers todos 108 and 226 were waiting on; leaving those todos describing a question
that has already been measured is the same stale-record failure plan 171-02 corrected for todo 229.

Note on sequencing economics: the gate's precondition depends on `alpha.regime.groups`' `equity`
entry carrying `dual_write_symbol_hmm=true` (migration 262) at the moment `ic_engine` started. That
flag was live and `true` at plan time. If this plan is executed back-to-back with 171-06, it costs
nothing to re-verify that flag BEFORE 171-06's `ic_engine` launches — if it were off, the remedy
would be a scoped ~49-symbol re-run rather than another full pass.

Output: a recorded equity falsifier verdict and three reconciled todo records.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-CONTEXT.md
@.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-ROLLOUT-EVIDENCE.md
@.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-PILOT-GATE.md
@CLAUDE.md
</context>

<interfaces>
Surfaces this plan consumes (verified at plan time 2026-08-07):

- `scripts/analysis/equity_regime_separation_gate.py` — argparse: `--tf` (nargs="*", default
  None = all) and `--force` (skip the STEP 0 precondition check, documented in the script as
  DANGEROUS). `main()` at line 340. On a failed precondition without `--force` it prints
  `BLOCKED-ON-NEXT-IC-ENGINE-RUN` and exits 1. It is read-only: `conn.rollback()` then `close()`
  in a `finally` block, never a commit.
- `_fetch_equity_symbols(conn)` resolves the 49 equity symbols from `instrument_tags`
  (`eq_*`/`intl_*` routing tags), NOT from
  `instruments.contract_details->>'asset_class'` — that column marks ETF-wrapper type and
  returns the wrong 29-symbol set. This distinction was a real bug caught during the script's own
  development; do not "fix" the script back to the asset_class filter.
- `alpha.regime.groups` in `config_state` — verified at plan time: all four groups
  (`equity`, `rates`, `commodity`, `fx`) are `enabled=true` with `dual_write_symbol_hmm=true`.
  Re-verify at execution time.
- Evidence produced by earlier plans in this phase:
  `171-PILOT-GATE.md` (D-03 attribution verdict for todo 108),
  `evidence/171-06-iters-used-distribution.json` (full-corpus cap-headroom data for todo 226),
  `171-ROLLOUT-EVIDENCE.md` (REQ-3/REQ-4 completion record).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run todo 167's equity falsifier gate against the fresh feature_ic_scores</name>
  <files>.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-TODO-167-VERDICT.md</files>
  <read_first>
    - .planning/todos/pending/167-equity-cross-sectional-vs-symbol-hmm-never-falsifier-tested.md
      (full file — especially the "Progress (2026-07-27)" step 3 and the 2026-07-29 plan update
      saying the narrow equity-only pass was abandoned in favor of "one full-scope pass," which
      is what plan 171-06 just ran)
    - scripts/analysis/equity_regime_separation_gate.py lines 300-375 (`run_measurement`'s output
      shape, `main()`'s precondition branch, and the `--force` semantics)
    - scripts/analysis/equity_regime_separation_gate.py `check_precondition` and
      `_fetch_equity_symbols` (grep for their definitions) — what "fresh" actually means to the
      script, so a BLOCKED result can be diagnosed rather than worked around
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-ROLLOUT-EVIDENCE.md
      (the `ic_engine` invocation plan 171-06 actually ran — symbol scope, tfs, `--refresh`, and
      whether `--cross-sectional-only` was correctly omitted)
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-CONTEXT.md
      "Folded Todos" section, the todo 167 bullet (verification task, not new implementation)
  </read_first>
  <action>
Step 1 — establish the coverage fact before running anything, so the result can be interpreted
rather than merely observed.

Confirm the routing config was in force when `ic_engine` ran:
`PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT config_value FROM config_state WHERE config_key='alpha.regime.groups'"`
and confirm the `equity` entry carries `dual_write_symbol_hmm: true`. If it does not, that alone
explains a BLOCKED result and the remedy is a scoped re-run, not a full pass — record it and skip
to Step 3's BLOCKED branch.

Confirm the 49 equity symbols now have `symbol_hmm`-scope rows:
`PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT regime_scope, count(DISTINCT symbol) AS symbols, count(*) AS rows, max(computed_at) AS newest FROM feature_ic_scores GROUP BY regime_scope ORDER BY regime_scope"`
and specifically for a canary symbol the todo names:
`PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT regime_scope, count(*), max(computed_at) FROM feature_ic_scores WHERE symbol='SPY' GROUP BY regime_scope"`
SPY having zero `symbol_hmm` rows was the todo's original 2026-07-21 finding; a non-zero count
with a `computed_at` after plan 171-06's run is the direct evidence that this phase's pass covered
what todo 167 needs.

Step 2 — run the gate:
`.venv/bin/python scripts/analysis/equity_regime_separation_gate.py 2>&1 | tee logs/171_07_equity_regime_gate.log`

Do NOT pass `--force`. The precondition check is the exact question this task exists to answer; a
`--force` run would produce a real-looking verdict from stale data, which is the specific failure
the script's own development already hit once (the asset_class filter incident recorded in todo
167). If the gate is BLOCKED, that is a valid and informative outcome, not a reason to override it.

Step 3 — write `171-TODO-167-VERDICT.md` in the phase directory with the verbatim gate output and
one of two branches:

PASS/verdict branch — the gate ran and produced a separation verdict. Record: the number of equity
symbols with measured `symbol_hmm` rows out of 49; the per-tf cross-sectional spreads and bands;
the gate's own rendered verdict text; and a plain statement of what it means for todo 167's
original question (does cross-sectional regime routing actually separate IC better than per-symbol
HMM for equity symbols, or was the default unearned). This closes todo 167's evidence requirement.

BLOCKED branch — the gate printed `BLOCKED-ON-NEXT-IC-ENGINE-RUN`. Record the precondition
evidence the script printed, the diagnosis from Step 1 (routing flag off, or scope gap, or
`--cross-sectional-only` mistakenly used in 171-06), and the exact scoped command that would clear
it, in the form
`.venv/bin/python services/ic_engine.py --symbols <the 49 equity symbols> --tf 5m 15m 1h 1d --training-window-end <alpha.validation.oos_start value>`
with no `--cross-sectional-only` (the per-symbol pass is what performs the dual-write). State
explicitly whether that scoped run is cheap enough to do now (49 symbols, and Phase 162's
fingerprint mechanism skips already-fresh cross-sectional cells) or whether it should be a new
todo. Do not run it inside this plan without first re-running the `ps aux` contention check for
`ic_engine|infrastructure_run_historical_pipeline`.

End the document with a single line: `TODO-167: VERDICT RECORDED` or `TODO-167: STILL BLOCKED`.

Do not modify `scripts/analysis/equity_regime_separation_gate.py`. CONTEXT.md is explicit that
todo 167 is a verification task; the script already exists and is verified.
  </action>
  <verify>
    <automated>grep -Eq "^TODO-167: (VERDICT RECORDED|STILL BLOCKED)$" .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-TODO-167-VERDICT.md</automated>
  </verify>
  <acceptance_criteria>
    - `171-TODO-167-VERDICT.md` contains exactly one line matching
      `^TODO-167: (VERDICT RECORDED|STILL BLOCKED)$`.
    - The document contains the verbatim `=== STEP 0: Precondition Check ===` block from the gate's
      output.
    - The document records the SPY `regime_scope` row counts and `max(computed_at)` query result,
      and states whether `symbol_hmm` rows now exist where the todo's 2026-07-21 finding said they
      did not.
    - `grep -c -- "--force" logs/171_07_equity_regime_gate.log` is 0 — the precondition was not
      overridden.
    - `git diff --stat scripts/analysis/equity_regime_separation_gate.py` is empty.
    - On STILL BLOCKED: the document contains a concrete, runnable scoped `ic_engine.py` command
      including `--training-window-end` and explicitly NOT including `--cross-sectional-only`.
  </acceptance_criteria>
  <done>Todo 167's question is answered against fresh data, or its remaining blocker is named with
  the exact command that clears it — and neither outcome required editing the already-verified
  gate script.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile folded todos 108, 167, and 226 with this phase's measured results</name>
  <files>.planning/todos/pending/167-equity-cross-sectional-vs-symbol-hmm-never-falsifier-tested.md, .planning/todos/pending/108-hmm-multi-seed-restart-best-likelihood.md, .planning/todos/pending/226-regime-writer-n-iter-convergence-headroom-check.md, .planning/todos/PRIORITIES.md</files>
  <read_first>
    - .planning/todos/pending/108-hmm-multi-seed-restart-best-likelihood.md (full — what question
      it actually asks, so the pilot's verdict is matched to it rather than to a paraphrase)
    - .planning/todos/pending/226-regime-writer-n-iter-convergence-headroom-check.md (full —
      especially its own statement of what a real full-corpus measurement must show, and its
      "don't change n_iter blind" rule)
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-PILOT-GATE.md
      (the G5 D-03 attribution verdict and the resulting `alpha.hmm.n_restarts` value)
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-06-iters-used-distribution.json
      (the numbers todo 226 gets)
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-TODO-167-VERDICT.md
      (Task 1's output)
    - .planning/todos/PRIORITIES.md (the rows for 108, 167, 226, and the already-corrected 229 row
      from plan 171-02 — follow whatever completed-entry convention that plan established rather
      than inventing a second one)
    - .planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-02-SUMMARY.md
      (how todo 229's closure and PRIORITIES.md correction were actually written, so this task is
      consistent with it)
    - CLAUDE.md, the `git add` multi-pathspec gotcha (a renamed path must be staged alone first)
  </read_first>
  <action>
Update each of the three todos with what this phase measured. Close a todo only when its own
stated question has been answered by evidence produced in this phase; otherwise leave it pending
with a precisely narrowed remainder. Never close on a guess — a prematurely closed todo is the
same class of stale record plan 171-02 had to correct for 229.

**Todo 108 (multi-seed HMM restart).** Append a section citing plan 171-05's D-03 parallel-arm
comparison: the pilot scope (8 symbols x 4 tfs), the per-cell attribution results from
`evidence/171-05-n-restarts-comparison.json`, and the resulting verdict from `171-PILOT-GATE.md`'s
G5. If the verdict was decisive in either direction — multi-restart significantly better on a
strict majority of evaluated cells, or clearly not — the todo's question has been answered
empirically: `git mv` it to `.planning/todos/completed/` with a closing note stating the measured
verdict, the resulting `alpha.hmm.n_restarts` value, and that the walk-forward path deliberately
does NOT thread `n_restarts` today (RESEARCH.md's Second Finding), so any future change requires
that threading work as its own task. If the pilot could not evaluate enough cells to reach a
majority, leave the todo pending and rewrite its "what's needed" section to the narrowed
remainder, attaching the data that was collected.

**Todo 167.** If Task 1 recorded `TODO-167: VERDICT RECORDED`, `git mv` the todo to
`completed/` with a closing note citing `171-TODO-167-VERDICT.md`, the gate's verdict, and the
`ic_engine` pass that unblocked it (plan 171-06). Per this project's
close-referenced-concept-docs discipline, also update the two documents todo 167's own References
section names as downstream consumers — `docs/research/stratification-dimension-unification.md`'s
reconciliation section (which points back at this todo as its concrete next step) and
`.planning/todos/pending/135-cross-sectional-regime-grid-shape-never-validated.md`'s dependency
note — so neither is left pointing at a `pending/` path that no longer exists. If Task 1 recorded
`TODO-167: STILL BLOCKED`, leave the todo pending and replace its stale "Progress"/"Plan update"
narrative with the current, accurate state and the exact scoped command from Task 1.

**Todo 226 (n_iter cap headroom).** Do NOT close it and do NOT change `alpha.hmm.n_iter` —
CONTEXT.md D-04 is explicit that this phase reads the data and a later decision acts on it. Append
the full-corpus measurement: total segments, converged-false count and fraction, the `iters_used`
percentiles, the exact-cap-hit count, the per-tf breakdown, and the worst-offender cells, all
citing `evidence/171-06-iters-used-distribution.json` by path. Then rewrite the todo's own
"what's needed" section so it describes only the remaining decision (what, if anything, `n_iter`
should become given this distribution) rather than the measurement obligation, which is now
discharged. Note explicitly that the data came from the walk-forward path's per-segment
`regime_writer.walk_forward_hmm_convergence_iters` event (added in plan 171-01), which is a
distinct event name from the single-fit path's `regime_writer.hmm_convergence_iters` — so the
distribution describes the code path that is now production, not the retired one.

**Todo 229 blast-radius confirmation.** Todo 229 was already closed by plan 171-02. Its closing
note deferred the blast-radius verification to this phase's full-corpus refit. Append that
confirmation to the completed file at
`.planning/todos/completed/229-regime-writer-hmm-retry-logic-structurally-unreachable.md`: the
`converged=false` fraction observed at full scale from the same distribution file, and a plain
statement of whether the now-functional retry path behaved as expected. Do not reopen the todo.

**Quarantined cells (if any).** If plan 171-05's G2 gate recorded a "PASS WITH QUARANTINE" verdict
for any (symbol, tf) cell, that cell was excluded from plan 171-06's full rollout and is still on
the retired full-history-fit method today — per-requirement-3's "every (symbol, tf)" is not fully
met for it. File a new pending todo (next number in sequence per this project's todo-numbering
convention) for each quarantined cell, citing the specific `min_pairwise_agreement` value and
boundary that failed the gate, and add it to `.planning/todos/PRIORITIES.md` with a P2 priority (it
is a known, bounded data-integrity gap, not urgent, but not something this phase's closure should
let fall out of the backlog silently). If no cell was quarantined, state that explicitly in this
task's summary rather than omitting the check.

**PRIORITIES.md.** Update the rows for 108, 167, and 226 to match their real post-phase state,
following the convention plan 171-02 established for todo 229. Every row that changed state must
point at the correct `pending/` or `completed/` path — a row pointing at a moved file is the exact
broken link plan 171-02 had to fix. Then verify no stale path survives:
`grep -n "pending/108-\|pending/167-\|pending/226-" .planning/todos/PRIORITIES.md` must return
nothing for any todo that moved.

Staging discipline, per CLAUDE.md: stage each renamed path alone first
(`git add -- .planning/todos/completed/<file>`, letting git auto-detect the rename) before
batching with the PRIORITIES.md edit. A multi-pathspec `git add` aborts entirely if any single
path no longer matches, silently staging none of the valid ones either.
  </action>
  <verify>
    <automated>test -z "$(grep -o 'pending/108-[a-z0-9-]*\.md' .planning/todos/PRIORITIES.md | while read p; do test -f ".planning/todos/$p" || echo BAD; done)" && test -z "$(grep -o 'pending/167-[a-z0-9-]*\.md' .planning/todos/PRIORITIES.md | while read p; do test -f ".planning/todos/$p" || echo BAD; done)" && test -z "$(grep -o 'pending/226-[a-z0-9-]*\.md' .planning/todos/PRIORITIES.md | while read p; do test -f ".planning/todos/$p" || echo BAD; done)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "171-06-iters-used-distribution" .planning/todos/pending/226-regime-writer-n-iter-convergence-headroom-check.md`
      is at least 1, and that todo is still in `pending/`.
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT config_value FROM config_state WHERE config_key='feature.hmm.n_iter'"`
      returns the pre-phase value — `n_iter` was not changed.
    - Todo 108 is either in `completed/` with a closing note citing the measured D-03 verdict and
      the resulting `alpha.hmm.n_restarts` value, or still in `pending/` with its "what's needed"
      section narrowed to the specific remainder and the collected data attached. Both branches
      must cite `evidence/171-05-n-restarts-comparison.json` by path.
    - Todo 167's state matches Task 1's recorded verdict line: `completed/` on
      `VERDICT RECORDED`, `pending/` with the current state and scoped command on
      `STILL BLOCKED`.
    - `grep -c "converged" .planning/todos/completed/229-regime-writer-hmm-retry-logic-structurally-unreachable.md`
      is at least 1 — the deferred blast-radius confirmation was appended.
    - Every `pending/<id>-` path referenced in PRIORITIES.md for todos 108/167/226 resolves to a
      file that actually exists (the automated verify command above).
    - `git status --short .planning/todos/` shows any moved todo staged as `R`, not as an
      add-plus-delete pair.
    - `git status --short` shows no change outside `.planning/` — this task edits records only.
  </acceptance_criteria>
  <done>The three folded todos describe what this phase actually measured, each is closed with
  evidence or left open with a narrowed remainder, PRIORITIES.md has no broken paths, and
  `n_iter` was left for a later decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None in the ASVS sense. Per RESEARCH.md's Security Domain section: this plan runs one read-only
analysis script (`equity_regime_separation_gate.py`, which explicitly `rollback()`s and never
commits) and edits planning markdown. No network-facing surface, no
authentication/session/access-control boundary, no untrusted input. No ASVS L1 category applies.

## Correctness Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-34 | False-confidence verdict from stale data | `equity_regime_separation_gate.py` STEP 0 | mitigate | `--force` explicitly forbidden and grep-asserted absent from the run log; the precondition check IS the question this task answers, so overriding it would answer nothing |
| T-171-35 | Coverage assumed rather than verified | `feature_ic_scores` `symbol_hmm` rows for 49 equity symbols | mitigate | Direct `regime_scope`/`computed_at` queries run and recorded BEFORE the gate, so a BLOCKED result can be diagnosed rather than merely observed |
| T-171-36 | Premature todo closure | todos 108, 167, 226 | mitigate | Closure is conditional on this phase's own evidence answering the todo's own stated question; both branches (close / narrow-and-keep-open) are specified per todo, and todo 226's closure is forbidden outright by D-04 |
| T-171-37 | Blind parameter change on fresh data | `feature.hmm.n_iter` | mitigate | Acceptance criterion asserts the live value is unchanged; D-04's read-only mandate restated in the action |
| T-171-38 | Broken cross-reference after a todo move | `.planning/todos/PRIORITIES.md`, `docs/research/stratification-dimension-unification.md`, todo 135 | mitigate | An automated verify command resolves every `pending/` path referenced for the three todos; the two named downstream documents are updated in the same task |
| T-171-39 | Partial git staging on a rename | `git add` with multiple pathspecs | mitigate | CLAUDE.md's stage-the-rename-alone-first rule written into the action; `R` status asserted as an acceptance criterion |
| T-171-40 | Unauthorized scope creep into the verified gate script | `scripts/analysis/equity_regime_separation_gate.py` | mitigate | `git diff --stat` on that file asserted empty; CONTEXT.md's "verification task, not new implementation" framing restated |
| T-171-SC | Tampering (supply chain) | package installs | accept | This plan installs no packages |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/ -q` green (wave merge gate per 171-VALIDATION.md).
- `171-TODO-167-VERDICT.md` carries a single `TODO-167:` line and the verbatim STEP 0 block.
- Every `pending/` path referenced in PRIORITIES.md for todos 108/167/226 resolves to a real file.
- `feature.hmm.n_iter` unchanged; todo 226 still in `pending/` carrying the measurement.
- `git status --short` shows changes only under `.planning/` and `docs/research/`.
</verification>

<success_criteria>
- Todo 167's verification question is answered against the fresh `feature_ic_scores` this phase
  produced, without a second scoped `ic_engine` run being assumed necessary or unnecessary.
- Todos 108 and 226 carry this phase's measured results; 108 is resolved by evidence or narrowed,
  226 stays open for its `n_iter` decision with its measurement obligation discharged.
- Todo 229's deferred blast-radius confirmation is appended to its completed record.
- PRIORITIES.md and the named downstream documents have no path pointing at a moved todo.
- No production code, config value, or corpus row was changed by this plan.
</success_criteria>

<output>
Create `.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-07-SUMMARY.md`
when done.
</output>
