# Phase 52.3: Dual-Write Shadow Writer — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-26
**Phase:** 52.3-dual-write-shadow-writer
**Areas discussed:** BaseAgent dependency, FeatureRepository approach, DLQ + error handling, Metrics port + config

---

## BaseAgent Dependency

| Option | Description | Selected |
|--------|-------------|----------|
| Execute 52.2 first, then 52.3 | Correct DAG ordering. BaseAgent provides SIGTERM drain, Golden Signal instrumentation, DLQ. | ✓ |
| Implement as plain asyncio service now | Mirror feature_writer_service.py pattern. Creates refactor debt in later phases. | |
| Stub BaseAgent in 52.3 then replace in 52.2 | Minimal stub now, real impl in 52.2. Risk of conflict. | |

**User's choice:** Execute 52.2 first (recommended)
**Notes:** User applied the Renaissance framing — correct DAG ordering is non-negotiable. No architectural shortcuts.

---

## FeatureRepository Approach

| Option | Description | Selected |
|--------|-------------|----------|
| Add table_name + rename to write_batch() | DRY: one class, configurable target, allow-list validation. Fix camelCase violation. | ✓ |
| Create FeatureShadowWriterRepository | Separate class, explicit separation, but duplicates identical SQL. | |

**User's choice:** Add table_name (recommended) — Claude applied Renaissance reasoning: schema duplication creates maintenance burden when columns evolve.
**Notes:** User deferred to Renaissance framing. Claude reasoned: concern = "write feature vectors," table = configuration. Allow-list enforces valid targets.

---

## DLQ + Error Handling

| Option | Description | Selected |
|--------|-------------|----------|
| Route to DLQ topic + structured log | intelligence.journal.dlq routing. Never drops data. More infra overhead. | |
| Log-and-skip + error counter | Structured log + shadow_parse_errors_total counter. Parity auditor catches gaps. | ✓ |

**User's choice:** Log-and-skip (Claude's Renaissance reasoning applied)
**Notes:** "Never drop data that could contain signal" applies to primary path. Shadow failure = missing row = parity violation detected by Phase 52.5 auditor. DLQ would duplicate what parity audit already surfaces. Zero manual overhead.

---

## Metrics Port + Config

| Option | Description | Selected |
|--------|-------------|----------|
| Port 9119, match feature_writer_service pattern | Config dict with metrics_port default. PYTHONUNBUFFERED=1. Full Golden Signals. | ✓ |
| Port 9119, env-var config only | Simpler but diverges from existing conventions. | |

**User's choice:** Match feature_writer_service pattern (recommended)
**Notes:** Conventions are infrastructure. Breaking them creates hidden maintenance debt.

---

## Claude's Discretion

- Batch size and flush interval: mirror feature_writer_service.py defaults (BATCH_SIZE=50, FLUSH_INTERVAL_SECS=5.0)
- SIGTERM drain: inherit from BaseAgent (Phase 52.2 delivers this)
- `feature_parity_violations` table: include in migration 051 (needed by Phase 52.5)

## Deferred Ideas

None.
