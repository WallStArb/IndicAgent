---
phase: 33-five-new-i7-signal-plugins
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/trading/prev_day_level_test.py
  - src/intelligence/trading/second_leg_continuation.py
  - src/intelligence/trading/vcp.py
  - tests/unit/intelligence/trading/test_prev_day_level_test.py
  - tests/unit/intelligence/trading/test_second_leg_continuation.py
  - tests/unit/intelligence/trading/test_vcp.py
autonomous: true
requirements: [PLUG-03, PLUG-04, PLUG-05]

must_haves:
  truths:
    - "PrevDayLevelTest fires fade variant when price is within 0.5xATR of PDH/PDL/PDC with reversal momentum"
    - "PrevDayLevelTest fires continuation variant when price breaks through level and re-tests"
    - "PrevDayLevelTest returns no_signal when no prior session data available"
    - "SecondLegContinuation fires when price is in 38.2%-61.8% Fib zone with HMM trend regime"
    - "SecondLegContinuation returns no_signal when Leg 1 amplitude < 1.0xATR or ranging regime"
    - "SecondLegContinuation sets targets at 100%, 127.2%, 161.8% of Leg 1 amplitude"
    - "VCP fires after 3+ successive H-L contractions with declining volume and expansion bar"
    - "VCP requires HMM trend regime with prob >= 0.60 (definitional gate)"
    - "VCP state resets at session boundary"
  artifacts:
    - path: "src/intelligence/trading/prev_day_level_test.py"
      provides: "trad_PrevDayLevelTest I7 plugin"
      contains: "class PrevDayLevelTestPlugin"
    - path: "src/intelligence/trading/second_leg_continuation.py"
      provides: "trad_SecondLegContinuation I7 plugin"
      contains: "class SecondLegContinuationPlugin"
    - path: "src/intelligence/trading/vcp.py"
      provides: "trad_VCP I7 plugin"
      contains: "class VCPPlugin"
    - path: "tests/unit/intelligence/trading/test_prev_day_level_test.py"
      provides: "PrevDayLevelTest unit tests"
      contains: "test_fires_fade_variant"
    - path: "tests/unit/intelligence/trading/test_second_leg_continuation.py"
      provides: "SecondLegContinuation unit tests"
      contains: "test_fires_in_fib_zone"
    - path: "tests/unit/intelligence/trading/test_vcp.py"
      provides: "VCP unit tests"
      contains: "test_fires_after_three_contractions"
  key_links:
    - from: "src/intelligence/trading/prev_day_level_test.py"
      to: "src/intelligence/trading/trade_framer.py"
      via: "frame_trade() call"
      pattern: "from .trade_framer import"
    - from: "src/intelligence/trading/second_leg_continuation.py"
      to: "src/intelligence/trading/trade_framer.py"
      via: "frame_trade() call"
      pattern: "from .trade_framer import"
    - from: "src/intelligence/trading/vcp.py"
      to: "src/intelligence/trading/trade_framer.py"
      via: "frame_trade() call"
      pattern: "from .trade_framer import"
---

<objective>
Implement three I7 signal plugins: trad_PrevDayLevelTest (PDH/PDL/PDC fade and continuation), trad_SecondLegContinuation (Fibonacci measured-move continuation), and trad_VCP (Volatility Contraction Pattern breakout). All three call trade_framer.py for stop/target resolution.

Purpose: Cover three more invisible market conditions — previous-day institutional magnet levels, second-leg Fibonacci continuations, and volatility contraction breakouts. Combined with Plan 01's plugins, this completes all five market conditions for Phase 33.

Output: Three plugin files with module-level `plugin` instances, plus three test files with full coverage of gate logic, fire conditions, and edge cases.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-five-new-i7-signal-plugins/33-CONTEXT.md
@.planning/phases/33-five-new-i7-signal-plugins/33-RESEARCH.md

<interfaces>
<!-- Reference implementations and consumed APIs the executor needs -->

From src/intelligence/trading/choch_reversal.py (canonical minimal I7 plugin):
```python
@dataclass
class CHoCHReversalPlugin:
    name: str = "trad_CHoCHReversal"
    outputs: frozenset[str] = frozenset({"signal_type", "direction", "entry_price", "stop_loss", "targets", "confidence", "regime_context", "supporting_factors"})
    min_lookback: int = 20
    supports_incremental: bool = False
    capability_tags: frozenset[str] = frozenset({"trading", "smc", "structure", "regime"})
    inputs: tuple[InputSpec, ...] = (InputSpec(symbol=".*", timeframe=".*", lookback=50),)
    regime_type: str = "any"
    _state: dict = field(default_factory=dict)

    def compute_full(self, frames: dict[str, Any]) -> dict[str, Any]: ...
    def compute_next(self, windows: dict[str, Any]) -> dict[str, Any]: return self.compute_full(windows)
    @staticmethod
    def _no_signal() -> dict[str, Any]: return {"signal_type": "none", "direction": 0, "confidence": 0.0}

plugin = CHoCHReversalPlugin()
```

From src/intelligence/trading/trade_framer.py:
```python
from .trade_framer import frame_trade
# frame_trade(setup_type: str, direction: int, entry: float, features: dict, atr: float) -> TradeFrame
# TradeFrame has: viable (bool), entry (float), stop (float), targets (list[Target]), entry_type (str)
# New signal_type strings fall through to "at_close" in _resolve_entry() — plugin pre-computes entry price
```

From src/intelligence/plugins.py:
```python
from ..plugins import InputSpec
# InputSpec(symbol=".*", timeframe=".*", lookback=50)
```

From tests/unit/intelligence/helpers.py:
```python
def make_ohlcv(close: np.ndarray, volume: np.ndarray | None = None) -> pd.DataFrame:
    # Returns DataFrame with open, high, low, close, volume columns
```

Feature fields consumed (all verified in schemas.py):
- prior_session_high (float), prior_session_low (float), prior_session_close (float) — I3 struct_SessionLevels
- swing_high (float), swing_low (float), swing_high_age_bars (float), swing_low_age_bars (float) — I3 swing_detector
- hmm_regime (float 0.0/1.0/2.0), hmm_regime_prob (float 0.0-1.0) — SMC hmm_regime
- atr_14 (float) — I1
- garch_vol_regime (float 0/1/2) — I4
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement trad_PrevDayLevelTest plugin with tests</name>
  <files>
    src/intelligence/trading/prev_day_level_test.py,
    tests/unit/intelligence/trading/test_prev_day_level_test.py
  </files>
  <read_first>
    src/intelligence/trading/choch_reversal.py,
    src/intelligence/trading/trade_framer.py,
    src/intelligence/trading/trend_following.py,
    tests/unit/intelligence/helpers.py
  </read_first>
  <behavior>
    - test_no_signal_when_no_prior_session_data: features with prior_session_high=None -> no_signal
    - test_fires_fade_variant_near_pdh: close within 0.5xATR of PDH + bearish momentum (close < open) -> direction=-1, setup_variant="fade"
    - test_fires_fade_variant_near_pdl: close within 0.5xATR of PDL + bullish momentum (close > open) -> direction=1, setup_variant="fade"
    - test_fires_fade_variant_near_pdc: close within 0.5xATR of PDC + reversal momentum -> fires with setup_variant="fade"
    - test_fires_continuation_variant: close broke above PDH, pulled back to within 0.5xATR, held above -> direction=1, setup_variant="continuation"
    - test_no_signal_when_price_far_from_levels: close more than 0.5xATR from all three levels -> no_signal
    - test_fade_prefers_mean_reversion_regime: hmm_regime=0.0 (ranging) + fade -> higher confidence than hmm_regime=1.0
    - test_continuation_prefers_trend_regime: hmm_regime=1.0 (trending) + continuation -> higher confidence
    - test_no_signal_when_frame_not_viable: frame_trade returns viable=False -> no_signal
  </behavior>
  <action>
    **Write tests first (RED), then implement (GREEN).**

    Test file `tests/unit/intelligence/trading/test_prev_day_level_test.py`:
    - Import `make_ohlcv` from `tests.unit.intelligence.helpers`
    - Import `PrevDayLevelTestPlugin` from `src.intelligence.trading.prev_day_level_test`
    - For fade near PDH test: set `prior_session_high=5050.0`, `atr_14=10.0`, and close[-1]=5048.0 (within 0.5*10=5.0 of PDH). Set close[-1] < open to indicate bearish bar (reversal momentum at resistance).
    - For continuation test: need stateful tracking. First call: close[-1] > PDH (breakout). Second call: close[-1] within 0.5xATR of PDH from above, close > PDH (re-test held). Use `_state` to track breakout occurred.
    - For no prior session data: set `prior_session_high=None, prior_session_low=None, prior_session_close=None` in features.

    Plugin file `src/intelligence/trading/prev_day_level_test.py`:
    - `@dataclass` class `PrevDayLevelTestPlugin`
    - `name = "trad_PrevDayLevelTest"`
    - `regime_type = "any"` (per RESEARCH.md: has two variants, handles both internally)
    - `capability_tags = frozenset({"trading", "level_test", "session", "regime"})`
    - `_state: dict = field(default_factory=dict)` keyed by `(symbol, timeframe)`

    **compute_full() logic:**
    1. Extract df, features, symbol, tf. Guard len(df) < min_lookback.
    2. Get close, high, low arrays. Get atr with fallback. Guard atr > 0.
    3. Get state: `state = self._state.get((symbol, tf), {})`.
    4. Extract levels: `pdh = features.get("prior_session_high")`, `pdl = features.get("prior_session_low")`, `pdc = features.get("prior_session_close")`. Guard all with `isinstance(val, (int, float)) and val > 0`.
    5. If no valid levels: write state, return _no_signal().
    6. `close_price = float(close[-1])`. `open_price = float(df["open"].iloc[-1])`.
    7. Proximity gate: `proximity = 0.5 * atr`. Check each level:
       - `near_pdh = pdh is not None and abs(close_price - pdh) < proximity`
       - `near_pdl = pdl is not None and abs(close_price - pdl) < proximity`
       - `near_pdc = pdc is not None and abs(close_price - pdc) < proximity`
    8. If no proximity match: write state, return _no_signal().
    9. Pick the nearest level among those within proximity.
    10. **Variant detection:**
        - Check state for prior breakout through this level (continuation tracking).
        - If state has `breakout_level` matching nearest level and close_price is on the breakout side: `setup_variant = "continuation"`, direction follows breakout direction.
        - Otherwise: `setup_variant = "fade"`. Determine direction from momentum:
          - Near PDH: if `close_price < open_price` (bearish bar at resistance): direction = -1 (fade short). Else no clear momentum -> skip or still fire with lower confidence.
          - Near PDL: if `close_price > open_price` (bullish bar at support): direction = 1 (fade long). Else skip.
          - Near PDC: similar momentum logic.
    11. **Breakout tracking for continuation**: if close_price > pdh + proximity: mark `state["breakout_level"] = pdh, state["breakout_direction"] = 1`. Similarly for pdl (close < pdl - proximity -> breakout_direction = -1).
    12. Entry price: for fade = close_price; for continuation = close_price.
    13. Entry type info: fade -> "at_limit"; continuation -> "at_pullback" (informational in output dict).
    14. Confidence: base 0.50.
        - Fade variant + hmm_regime == 0.0 (ranging): +0.12.
        - Fade variant + hmm_regime in (1.0, 2.0): -0.05.
        - Continuation variant + hmm_regime in (1.0, 2.0): +0.12.
        - Continuation variant + hmm_regime == 0.0: -0.05.
        - Clamp [0.10, 0.95].
    15. signal_type: `"prev_day_fade_long"` / `"prev_day_fade_short"` / `"prev_day_cont_long"` / `"prev_day_cont_short"`.
    16. Call `frame_trade(setup_type=signal_type, direction=direction, entry=entry_price, features=features, atr=atr)`. If not viable: no_signal.
    17. Return dict with signal_type, direction, entry_price=frame.entry, stop_loss=frame.stop, targets=[t.price for t in frame.targets], confidence, regime_context, supporting_factors, setup_variant, level_name ("PDH"/"PDL"/"PDC"), level_value.
    18. Write state back.

    Module-level: `plugin = PrevDayLevelTestPlugin()`
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/trading/test_prev_day_level_test.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/intelligence/trading/prev_day_level_test.py` contains `class PrevDayLevelTestPlugin`
    - `prev_day_level_test.py` contains `name: str = "trad_PrevDayLevelTest"`
    - `prev_day_level_test.py` contains `regime_type: str = "any"`
    - `prev_day_level_test.py` contains `setup_variant`
    - `prev_day_level_test.py` contains `from .trade_framer import frame_trade`
    - `prev_day_level_test.py` contains `plugin = PrevDayLevelTestPlugin()`
    - `prev_day_level_test.py` contains `prior_session_high` and `prior_session_low` and `prior_session_close`
    - `test_prev_day_level_test.py` contains at least 7 test functions starting with `test_`
    - `.venv/bin/pytest tests/unit/intelligence/trading/test_prev_day_level_test.py -v -x` exits 0
  </acceptance_criteria>
  <done>PrevDayLevelTest plugin fires fade and continuation variants near PDH/PDL/PDC, applies proximity gate, uses regime-appropriate confidence, calls trade_framer. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement trad_SecondLegContinuation and trad_VCP plugins with tests</name>
  <files>
    src/intelligence/trading/second_leg_continuation.py,
    src/intelligence/trading/vcp.py,
    tests/unit/intelligence/trading/test_second_leg_continuation.py,
    tests/unit/intelligence/trading/test_vcp.py
  </files>
  <read_first>
    src/intelligence/trading/choch_reversal.py,
    src/intelligence/trading/trend_following.py,
    src/intelligence/trading/trade_framer.py,
    src/intelligence/context/session_context.py,
    tests/unit/intelligence/helpers.py
  </read_first>
  <behavior>
    SecondLeg tests:
    - test_fires_in_fib_zone_long: swing_high=5100, swing_low=5000, close in 38.2%-61.8% of that range (5038-5062), hmm_regime=1.0, amplitude >= 1.0xATR -> direction=1, targets at 100%/127.2%/161.8% of leg 1 amplitude
    - test_fires_in_fib_zone_short: swing_high above, swing_low below, close in upper fib zone, hmm_regime=2.0 -> direction=-1
    - test_no_signal_when_amplitude_below_atr: swing_high-swing_low < atr_14 -> no_signal
    - test_no_signal_in_ranging_regime: hmm_regime=0.0 -> no_signal
    - test_no_signal_when_price_outside_fib_zone: close above 61.8% retracement -> no_signal
    - test_targets_match_measured_move: verify target_1 = entry + 100% of leg 1 amplitude, target_2 = entry + 127.2%, target_3 = entry + 161.8%
    - test_no_signal_when_swing_data_missing: swing_high=None -> no_signal
    - test_stale_swing_rejected: swing_high_age_bars > threshold -> no_signal

    VCP tests:
    - test_fires_after_three_contractions: 3+ successive bars with decreasing H-L range and declining volume, then expansion bar -> fires
    - test_no_signal_with_only_two_contractions: only 2 contractions -> no_signal
    - test_no_signal_when_hmm_not_trend: hmm_regime=0.0 -> no_signal
    - test_no_signal_when_hmm_prob_below_060: hmm_regime=1.0, hmm_regime_prob=0.55 -> no_signal
    - test_direction_follows_hmm_trend: hmm_regime=1.0 -> direction=1; hmm_regime=2.0 -> direction=-1
    - test_state_resets_on_new_session: different session date -> contraction list clears
    - test_contraction_count_in_output: valid fire includes contraction_count >= 3 in output
  </behavior>
  <action>
    **Write tests first (RED), then implement (GREEN).**

    **SecondLegContinuation plugin** `src/intelligence/trading/second_leg_continuation.py`:
    - `@dataclass` class `SecondLegContinuationPlugin`
    - `name = "trad_SecondLegContinuation"`
    - `regime_type = "trend"` (per CONTEXT.md: HMM trend regime required)
    - `capability_tags = frozenset({"trading", "fibonacci", "continuation", "regime"})`
    - `min_lookback = 30` (needs enough bars for swing detection context)

    **compute_full() logic:**
    1. Extract df, features. Guard len(df) < min_lookback.
    2. Get close, high, low arrays. Get atr with fallback. Guard atr > 0.
    3. **Regime gate**: `hmm_regime = features.get("hmm_regime", 0.0)`. If hmm_regime not in (1.0, 2.0): return _no_signal(). `hmm_regime_prob = float(features.get("hmm_regime_prob", 0.0))`.
    4. **Swing data**: `swing_high = features.get("swing_high")`, `swing_low = features.get("swing_low")`. Guard isinstance and > 0 for both. Get `swing_high_age = features.get("swing_high_age_bars", 999)`, `swing_low_age = features.get("swing_low_age_bars", 999)`.
    5. **Stale swing guard**: if both ages > 50 bars (Claude's discretion): return _no_signal().
    6. **Leg 1 amplitude**: `amplitude = abs(swing_high - swing_low)`. If `amplitude < 1.0 * atr`: return _no_signal().
    7. **Fib zone**: For bullish (hmm_regime=1.0, expecting up continuation after pullback):
       - Leg 1 was up: `swing_low` is origin, `swing_high` is peak
       - `fib_382 = swing_high - 0.618 * amplitude` (38.2% retracement = 61.8% from bottom)
       - `fib_618 = swing_high - 0.382 * amplitude` (61.8% retracement = 38.2% from bottom)
       - Wait, standard: 38.2% retracement from high = `swing_high - 0.382 * amplitude`, 61.8% retracement = `swing_high - 0.618 * amplitude`
       - Fib zone for long entry: price should be in the 38.2%-61.8% retracement band = between `swing_high - 0.618 * amplitude` and `swing_high - 0.382 * amplitude`
    8. `close_price = float(close[-1])`. If close_price NOT in fib zone: return _no_signal().
    9. Direction: 1 for bullish (hmm_regime=1.0), -1 for bearish (hmm_regime=2.0). For bearish: swing_low is origin, swing_high is peak of downward leg 1, fib zone is retracement up.
    10. Entry price: `fib_50 = (swing_high + swing_low) / 2` (50% retracement = midpoint). Use this as the pre-computed entry for `at_limit`.
    11. **Targets**: For longs (direction=1):
        - T1 = swing_low + amplitude * 2.0 (100% measured move from swing_low gives swing_high, then another 100% = swing_high + amplitude... Actually: T1 = entry + 1.0 * amplitude, T2 = entry + 1.272 * amplitude, T3 = entry + 1.618 * amplitude. Per CONTEXT.md: "100%, 127.2%, 161.8% of Leg 1 amplitude").
        - But actually the spec says measured move targets from entry, so: T1 = entry + amplitude, T2 = entry + 1.272 * amplitude, T3 = entry + 1.618 * amplitude.
    12. Stop: For longs: swing_low (leg 1 origin). For shorts: swing_high (leg 1 origin). Pass to frame_trade as features context.
    13. Call `frame_trade(setup_type="second_leg_long"/"second_leg_short", direction=direction, entry=fib_50, features=features, atr=atr)`. If not viable: no_signal.
    14. Confidence: base 0.55. If hmm_regime_prob > 0.75: +0.10. If close is closer to 50% retracement (ideal): +0.05. Clamp [0.10, 0.95].
    15. Return signal dict with targets computed above (override frame.targets with measured-move targets if frame_trade returns ATR-based fallback targets).

    Module-level: `plugin = SecondLegContinuationPlugin()`

    **VCP plugin** `src/intelligence/trading/vcp.py`:
    - `@dataclass` class `VCPPlugin`
    - `name = "trad_VCP"`
    - `regime_type = "trend"` (per CONTEXT.md: definitional)
    - `capability_tags = frozenset({"trading", "volatility", "contraction", "regime"})`
    - `_state: dict = field(default_factory=dict)` keyed by `(symbol, timeframe)`

    **compute_full() logic:**
    1. Extract df, features, symbol, tf. Guard len(df) < min_lookback.
    2. Get state: `state = self._state.get((symbol, tf), {})`.
    3. **Session reset**: extract timestamp from df (same pattern as ORB plugins). Convert to ET. If `et.date() != state.get("session_date")`: reset contractions list, set new session_date.
       - If df has no `timestamp` column, skip session reset (unit test may not provide timestamps).
    4. **Regime gate**: `hmm_regime = features.get("hmm_regime", 0.0)`. If hmm_regime not in (1.0, 2.0): write state, return _no_signal(). `hmm_regime_prob = float(features.get("hmm_regime_prob", 0.0))`. If hmm_regime_prob < 0.60: write state, return _no_signal().
    5. Get close, high, low, volume arrays. Get atr with fallback. Guard atr > 0.
    6. **Current bar range**: `bar_range = float(high[-1] - low[-1])`. `bar_volume = float(df["volume"].iloc[-1])`.
    7. **Contraction tracking**: `contractions = state.get("contractions", [])` — list of `(range, volume)` tuples.
       - If contractions is empty or `bar_range < contractions[-1][0]` and `bar_volume <= contractions[-1][1]`: append `(bar_range, bar_volume)` — contraction continues.
       - Else if `bar_range >= contractions[-1][0]` and len(contractions) >= 3: this is the expansion bar — potential fire.
       - Else: reset contractions to `[(bar_range, bar_volume)]` (new potential sequence start).
    8. If expansion bar detected (bar_range >= last contraction range AND len(contractions) >= 3):
       - **Volume expansion gate**: `bar_volume > contractions[-1][1] * 1.2` (expansion bar should have more volume than last contraction bar).
       - Direction: follow HMM trend. hmm_regime=1.0 -> direction=1 (long); hmm_regime=2.0 -> direction=-1 (short).
       - Entry: `close_price = float(close[-1])`.
       - For long: check `close_price > float(high[-2])` (closed above prior bar's high — breakout of contraction range).
       - For short: check `close_price < float(low[-2])` (closed below prior bar's low).
       - If not: write state, return _no_signal().
       - `contraction_count = len(contractions)`.
       - signal_type: `"vcp_breakout_long"` / `"vcp_breakout_short"`.
       - Call `frame_trade(setup_type=signal_type, direction=direction, entry=close_price, features=features, atr=atr)`. If not viable: no_signal.
       - Confidence: base 0.50. If contraction_count >= 4: +0.08. If hmm_regime_prob > 0.75: +0.07. Clamp [0.10, 0.95].
       - Clear contractions after fire.
       - Return dict with signal_type, direction, entry_price, stop_loss, targets, confidence, regime_context, supporting_factors including contraction_count.
    9. Write state back.

    Module-level: `plugin = VCPPlugin()`

    **Test files:**

    `tests/unit/intelligence/trading/test_second_leg_continuation.py`:
    - For fib zone test: use close=np.full(60, 5050.0) (midpoint of 5000-5100 range), features with swing_high=5100.0, swing_low=5000.0, atr_14=50.0 (amplitude=100 > 1.0*50), hmm_regime=1.0. 5050 is exactly at 50% retracement.
    - For amplitude filter test: swing_high=5010, swing_low=5000, atr_14=50 -> amplitude 10 < 50 -> no_signal.
    - For target verification test: check that the output dict contains targets with values approximately at entry + amplitude, entry + 1.272*amplitude, entry + 1.618*amplitude.

    `tests/unit/intelligence/trading/test_vcp.py`:
    - For contraction test: call compute_full() 5+ times with progressively decreasing H-L ranges and volumes, then an expansion bar. Use custom DataFrames with controlled high/low/volume for each call.
    - For session reset: include `timestamp` column with different dates between calls.
    - For regime gate: set hmm_regime=0.0 -> verify no_signal.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/trading/test_second_leg_continuation.py tests/unit/intelligence/trading/test_vcp.py tests/unit/intelligence/trading/test_prev_day_level_test.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/intelligence/trading/prev_day_level_test.py` contains `class PrevDayLevelTestPlugin` and `name: str = "trad_PrevDayLevelTest"`
    - `src/intelligence/trading/second_leg_continuation.py` contains `class SecondLegContinuationPlugin` and `name: str = "trad_SecondLegContinuation"`
    - `src/intelligence/trading/vcp.py` contains `class VCPPlugin` and `name: str = "trad_VCP"`
    - `second_leg_continuation.py` contains `regime_type: str = "trend"`
    - `second_leg_continuation.py` contains `0.382` and `0.618` (Fibonacci levels)
    - `second_leg_continuation.py` contains `1.272` and `1.618` (measured move targets)
    - `vcp.py` contains `regime_type: str = "trend"`
    - `vcp.py` contains `hmm_regime_prob` and `0.60` (prob gate)
    - `vcp.py` contains `contractions` (state tracking)
    - `vcp.py` contains `session_date` (session reset)
    - All three files contain `from .trade_framer import frame_trade`
    - All three files contain `plugin =` at module level
    - `test_second_leg_continuation.py` contains at least 6 test functions
    - `test_vcp.py` contains at least 5 test functions
    - `test_prev_day_level_test.py` contains at least 7 test functions
    - `.venv/bin/pytest tests/unit/intelligence/trading/test_prev_day_level_test.py tests/unit/intelligence/trading/test_second_leg_continuation.py tests/unit/intelligence/trading/test_vcp.py -v -x` exits 0
  </acceptance_criteria>
  <done>PrevDayLevelTest, SecondLegContinuation, and VCP plugins implement their respective gate logic, call trade_framer for stop/target, and pass all unit tests. SecondLeg produces 100%/127.2%/161.8% measured move targets. VCP tracks 3+ contractions with session reset. All tests pass.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/intelligence/trading/test_prev_day_level_test.py tests/unit/intelligence/trading/test_second_leg_continuation.py tests/unit/intelligence/trading/test_vcp.py -v -x` — all tests pass
- Each plugin file has `plugin = ClassName()` at module level
- Each plugin uses `from .trade_framer import frame_trade` (no per-plugin stop math)
- Each plugin declares correct `regime_type`
</verification>

<success_criteria>
Three I7 plugin files exist with correct plugin protocol, gate logic, and trade_framer integration. SecondLeg targets match Fibonacci measured moves. VCP tracks contractions statelessly across bars with session reset. All unit tests pass. Plugins are ready for registration in Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/33-five-new-i7-signal-plugins/33-02-SUMMARY.md`
</output>
