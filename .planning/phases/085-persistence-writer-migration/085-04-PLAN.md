---
phase: 085-persistence-writer-migration
plan: "04"
type: execute
wave: 2
depends_on:
  - "085-01"
files_modified:
  - services/lineage_writer_agent.py
  - services/signal_metrics_writer_agent.py
  - src/intelligence/schemas.py
autonomous: true
requirements:
  - PERSIST-01
  - PERSIST-04

must_haves:
  truths:
    - "lineage_writer_agent declares payload_model = LineageEvent; malformed lineage events are DLQ'd not dropped"
    - "lineage_writer_agent _parse_payload receives an already-validated LineageEvent and the manual signal_id/event_type check is deleted"
    - "lineage_writer_agent _flush_batch uses _to_row(LineageEvent) helper for named field access"
    - "signal_metrics_writer_agent extends BaseWriterAgent; declares payload_model = SignalMetricsEvent; writes in batches via _flush_batch"
    - "signal_metrics_writer_agent _flush_batch dispatches to existing _handle_* SQL helpers by event_type"
    - "SignalMetricsComputeAgent imports SignalMetricsEvent from src.intelligence.schemas (import added but not modifying compute logic)"
  artifacts:
    - path: "services/lineage_writer_agent.py"
      provides: "PERSIST-01 — LineageEvent payload_model + _to_row helper"
      contains: "payload_model = LineageEvent"
    - path: "services/signal_metrics_writer_agent.py"
      provides: "PERSIST-04 — BaseWriterAgent migration + batch flush"
      contains: "class SignalMetricsWriterAgent(BaseWriterAgent)"
  key_links:
    - from: "services/lineage_writer_agent.py payload_model"
      to: "src/core/agent/base_writer.py model_validate + _maybe_route_to_dlq"
      via: "BaseWriterAgent._run() validates with LineageEvent.model_validate(); routes ValidationError to DLQ"
      pattern: "payload_model = LineageEvent"
    - from: "services/signal_metrics_writer_agent.py _flush_batch"
      to: "_handle_metrics_computed / _handle_ic_computed / _handle_dq_failure"
      via: "dispatch by event.event_type in _flush_batch loop"
      pattern: "event\\.event_type.*metrics_computed\\|ic_computed\\|metrics_dq_failure"
---

<objective>
Apply the Phase 084 base contracts to two writers that depend on the schemas created in Plan 01.

PERSIST-01 (`lineage_writer_agent`): Already a `BaseWriterAgent` subclass. Wire `payload_model = LineageEvent`
so the base validates and DLQ-routes malformed events. Delete the now-dead manual `signal_id`/`event_type`
check from `_parse_payload`. Add `_to_row(LineageEvent)` helper and update `_flush_batch` to use it.

PERSIST-04 (`signal_metrics_writer_agent`): Currently extends `BaseAgent` with per-record writes.
Migrate to `BaseWriterAgent`: change base class, set `BATCH_SIZE = 50` and `FLUSH_INTERVAL_SECS = 5.0`,
declare `payload_model = SignalMetricsEvent`, replace manual Kafka consumer setup with `_create_consumer()`,
implement `_flush_batch()` that dispatches to the existing pure SQL helper functions by `event.event_type`.

Also add the import of `SignalMetricsEvent` to `services/signal_metrics_compute_agent.py` (the sole producer)
so it publishes the correct schema — this is a single import addition, no logic change.

Purpose: After Plan 01 (schemas) and this plan, all lineage events are either persisted or DLQ'd.
`signal_metrics_writer_agent` gains batching, DLQ routing, and Kafka offset commits for free from `BaseWriterAgent`.

Output: Two writers fully on Phase 084 base contracts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/085-persistence-writer-migration/085-CONTEXT.md
@.planning/phases/085-persistence-writer-migration/085-PATTERNS.md
@.planning/phases/085-persistence-writer-migration/085-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire LineageEvent payload_model and _to_row helper in lineage_writer_agent.py (PERSIST-01)</name>
  <files>services/lineage_writer_agent.py</files>
  <read_first>
    - services/lineage_writer_agent.py — read entire file; understand current _parse_payload and _flush_batch; identify the exact positional tuple construction in _flush_batch (lines ~67-80) and the manual check in _parse_payload (lines ~56-60)
    - src/core/ai/lineage.py — confirm LineageEvent fields (just created in Plan 01); note field names for _to_row() helper
    - src/core/agent/base_writer.py — read payload_model ClassVar gate (lines ~84, 316-325) to understand how validated LineageEvent arrives at _parse_payload
    - services/lifecycle_writer_agent.py — read payload_model declaration as style reference
    - services/feature_writer_agent.py — read _record_to_insert_params style for per-position comment convention
  </read_first>
  <action>
    Make four changes to `lineage_writer_agent.py`:

    1. Add import: `from src.core.ai.lineage import LineageEvent` at the top of the file with other imports.

    2. Add class attribute on `LineageWriterAgent`: `payload_model = LineageEvent` — this is a ClassVar; place it at the top of the class body after the class signature. The base will now call `LineageEvent.model_validate(raw_dict)` for every incoming message and route `ValidationError` to `_dlq_topic()` automatically.

    3. Replace `_parse_payload(self, payload: dict)` with `_parse_payload(self, payload: LineageEvent) -> list | None: return [payload]`. The manual `if not payload.get("signal_id") or not payload.get("event_type"): return None` check is dead code once `payload_model` enforces required fields — delete it per D-05.

    4. Add `_to_row(self, event: LineageEvent) -> tuple` method immediately before `_flush_batch`. Map each `LineageEvent` field to its positional slot with a `# $N field::type` comment per line. Positions must match the existing `INSERT INTO signal_lineage` SQL in `_flush_batch` exactly — read the SQL to verify. Fields from the PATTERNS.md audit: ts ($1 timestamptz), signal_id ($2 uuid — use `str(event.signal_id)`), event_type ($3), source ($4), dag_order ($5), multiplier ($6), metadata ($7 jsonb), is_shadow ($8), symbol ($9), tf ($10).

    Update `_flush_batch` to call `rows = [self._to_row(e) for e in batch]` instead of the inline positional dict-key construction. The SQL string and `executemany` call remain unchanged.
  </action>
  <verify>
    Run: `grep -n "payload_model" services/lineage_writer_agent.py` — must return the class attribute line.

    Run: `grep -n "signal_id.*event_type\|if not payload.get" services/lineage_writer_agent.py` — must return no matches (dead check deleted).

    Run: `grep -n "_to_row" services/lineage_writer_agent.py` — must appear at definition and call site in _flush_batch.

    Run: `.venv/bin/python -c "from services.lineage_writer_agent import LineageWriterAgent; from src.core.ai.lineage import LineageEvent; assert LineageWriterAgent.payload_model is LineageEvent; print('ok')"` — must print `ok`.

    Run: `.venv/bin/ruff check services/lineage_writer_agent.py` — zero errors.

    Run: `.venv/bin/pytest tests/unit/ -q` — must pass.
  </verify>
  <done>
    - `payload_model = LineageEvent` declared on `LineageWriterAgent`
    - `_parse_payload` receives `LineageEvent`, manual check deleted, returns `[payload]`
    - `_to_row(LineageEvent)` helper present with per-position comments
    - `_flush_batch` uses `[self._to_row(e) for e in batch]`
    - Ruff clean, unit tests green
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate signal_metrics_writer_agent.py to BaseWriterAgent (PERSIST-04)</name>
  <files>services/signal_metrics_writer_agent.py</files>
  <read_first>
    - services/signal_metrics_writer_agent.py — read entire file; understand the current BaseAgent subclass structure (lines 178-245 approx), the _handle_* SQL helpers (lines 50-175), the topic constant, consumer group constant, and DatabaseManager usage
    - src/core/agent/base_writer.py — read _setup pattern, _create_consumer(), _consumer_group property, _topic_name() abstract method, _teardown() pattern, and how BATCH_SIZE/FLUSH_INTERVAL_SECS are used
    - services/lifecycle_writer_agent.py — read _setup(), _topic_name(), _consumer_group property, _teardown(), and payload_model declaration as the exact migration template
    - src/intelligence/schemas.py — confirm SignalMetricsEvent is importable (created in Plan 01)
  </read_first>
  <action>
    Migrate `SignalMetricsWriterAgent` from `BaseAgent` to `BaseWriterAgent`. This is a structural migration — the three `_handle_*` SQL helper functions (module-level) stay untouched per D-01 and D-03.

    Changes required:

    1. Change import: replace `from src.core.agent.base import BaseAgent` with `from src.core.agent.base_writer import BaseWriterAgent` (keep any other imports that are still needed; remove imports that are no longer needed after migration, e.g. manual KafkaConsumerClient setup).

    2. Add import: `from src.intelligence.schemas import SignalMetricsEvent`.

    3. Add import: `from src.core.stream_keys import topic_signal_metrics_dlq` if a DLQ topic stream key exists for signal_metrics; if not, `_dlq_topic()` should return a DLQ topic string using the same naming convention as other writers (e.g. `topic_signal_metrics(self.env_name) + ".dlq"`). Read `src/core/stream_keys.py` for the actual function names before deciding.

    4. Change class declaration: `class SignalMetricsWriterAgent(BaseWriterAgent):` with class attributes:
       - `BATCH_SIZE = 50`
       - `FLUSH_INTERVAL_SECS = 5.0`
       - `payload_model = SignalMetricsEvent`

    5. Update `__init__`: call `super().__init__(name=_AGENT_NAME)`. Remove manual `self._kafka_consumer` — base manages this via `_create_consumer()`.

    6. Implement `_topic_name(self) -> str` returning the topic for signal_metrics (read the existing code to find the topic constant or `stream_keys` function call).

    7. Implement `_consumer_group` property returning the existing `_CONSUMER_GROUP` constant.

    8. Implement `_dlq_topic(self) -> str | None` returning the DLQ topic string.

    9. Replace `_setup()` with the `BaseWriterAgent` pattern: init `DatabaseManager`, call `self._create_consumer()`, call `await self._consumer.start()`. Remove any manual lag task setup — `BaseAgent.start()` handles that.

    10. Implement `_parse_payload(self, payload: SignalMetricsEvent) -> list | None: return [payload]` — dispatching is done in `_flush_batch`.

    11. Implement `_flush_batch(self, batch: list[SignalMetricsEvent]) -> None` — acquire DB connection, loop over batch, dispatch by `event.event_type` to the existing `_handle_metrics_computed(conn, event.model_dump())` / `_handle_ic_computed(conn, event.model_dump())` / `_handle_dq_failure(conn, event.model_dump())` helpers. Keep unknown event_type as a no-op with a warning log (discriminated union already blocks unknown types at parse time).

    12. Replace `_teardown()` with: `await super()._teardown()`, stop consumer, close DB.

    13. Delete the old per-record `_run()` override if present — base `_run()` handles the message loop now.

    Keep the three `_handle_*` module-level functions exactly as they are. Keep all SQL constants as they are.
  </action>
  <verify>
    Run: `grep -n "class SignalMetricsWriterAgent" services/signal_metrics_writer_agent.py` — must show `BaseWriterAgent` as base class.

    Run: `grep -n "payload_model" services/signal_metrics_writer_agent.py` — must return the class attribute line.

    Run: `grep -n "BATCH_SIZE\|FLUSH_INTERVAL_SECS" services/signal_metrics_writer_agent.py` — must return both class attrs.

    Run: `.venv/bin/python -c "from services.signal_metrics_writer_agent import SignalMetricsWriterAgent; from src.intelligence.schemas import SignalMetricsEvent; assert SignalMetricsWriterAgent.payload_model is SignalMetricsEvent; print('ok')"` — must print `ok`.

    Run: `.venv/bin/ruff check services/signal_metrics_writer_agent.py` — zero errors.

    Run: `.venv/bin/pytest tests/unit/ -q` — must pass.
  </verify>
  <done>
    - `SignalMetricsWriterAgent` extends `BaseWriterAgent`
    - `payload_model = SignalMetricsEvent`, `BATCH_SIZE = 50`, `FLUSH_INTERVAL_SECS = 5.0` declared
    - `_topic_name()`, `_consumer_group`, `_dlq_topic()`, `_parse_payload()`, `_flush_batch()` all implemented
    - `_flush_batch` dispatches to existing `_handle_*` helpers by `event.event_type`
    - Three `_handle_*` module-level functions unchanged
    - No manual per-record write loop remains
    - Ruff clean, unit tests green
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -n "payload_model = LineageEvent" services/lineage_writer_agent.py` returns a match
- `grep -n "class SignalMetricsWriterAgent(BaseWriterAgent)" services/signal_metrics_writer_agent.py` returns a match
- `grep -n "payload_model = SignalMetricsEvent" services/signal_metrics_writer_agent.py` returns a match
- `grep -n "if not payload.get" services/lineage_writer_agent.py` returns no matches
- `.venv/bin/pytest tests/unit/ -q` passes
- `.venv/bin/ruff check services/lineage_writer_agent.py services/signal_metrics_writer_agent.py` clean
</verification>

<success_criteria>
`lineage_writer_agent` declares `payload_model = LineageEvent`; all lineage messages either reach the DB or the DLQ — silent data loss is structurally impossible. `signal_metrics_writer_agent` extends `BaseWriterAgent` with `payload_model = SignalMetricsEvent`, batches 50 events per flush, and dispatches to the existing SQL helpers. Both writers are ruff-clean. Unit tests green. PERSIST-01 and PERSIST-04 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/085-persistence-writer-migration/085-04-SUMMARY.md`
</output>
