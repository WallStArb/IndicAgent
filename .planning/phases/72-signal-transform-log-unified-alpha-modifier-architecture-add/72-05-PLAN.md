---
phase: 72-signal-transform-log-unified-alpha-modifier-architecture-add
plan: 05
type: execute
wave: 2
depends_on: [02]
files_modified:
  - scripts/validate_skeptic.py
autonomous: true
requirements: [P72-CLI-REFACTOR]
must_haves:
  truths:
    - "validate_skeptic.py imports gate constants and compute functions from graduation.py"
    - "validate_skeptic.py contains zero inline compute logic — only CLI parsing + DB fetch + print"
    - "Sign convention: positive multiplier semantics"
  artifacts:
    - path: "scripts/validate_skeptic.py"
      provides: "Thin CLI wrapper over graduation.py"
      contains: "from src.intelligence.swarm.graduation import"
  key_links:
    - from: "scripts/validate_skeptic.py"
      to: "src.intelligence.swarm.graduation.evaluate_all"
      via: "call site"
      pattern: "evaluate_all\\("
---

<objective>
Refactor scripts/validate_skeptic.py into a thin CLI wrapper that delegates all
computation to src.intelligence.swarm.graduation.

Purpose: Single source of truth. CLI and GraduationComputeAgent compute identically.
Output: ~80-120 line script (down from 530).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-signal-transform-log-unified-alpha-modifier-architecture-add/72-CONTEXT.md
@scripts/validate_skeptic.py
@src/intelligence/swarm/graduation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite validate_skeptic.py as thin CLI wrapper</name>
  <read_first>
    - scripts/validate_skeptic.py (current 530-line implementation; extract only DB fetch logic, drop inline compute)
    - src/intelligence/swarm/graduation.py (Plan 02 output — what the script must import from)
    - .planning/phases/72-signal-transform-log-unified-alpha-modifier-architecture-add/72-CONTEXT.md (validate_skeptic.py refactoring section)
  </read_first>
  <files>scripts/validate_skeptic.py</files>
  <action>
Rewrite scripts/validate_skeptic.py. Keep ONLY: argparse CLI, async DB fetch from alpha_multiplier_shadow JOIN signal_ledger, DataFrame column adaptation, call to evaluate_all, pretty-print result, exit code based on is_graduated.

Delete every inline function: compute_spearman, compute_calibration, compute_expected_shortfall, minimum_detectable_effect, compute_segment_stats, compute_walk_forward, compute_value_add, _sharpe, and the GATE_* constants. They all live in graduation.py now.

Replace gate constant declarations with imports at top:

```
from src.intelligence.swarm.graduation import (
    GATE_SPEARMAN_RHO,
    GATE_CALIBRATION_MAX_ERROR,
    GATE_CVAR_BOTTOM_DECILE,
    evaluate_all,
)
```

In fetch_validation_data, alias `s.predicted_multiplier AS multiplier` in the SELECT so the returned DataFrame has the column name graduation.py expects (multiplier, pnl_r, ts). Drop the `failure_prob` derivation — graduation.py operates on multiplier directly. Keep s.ts in SELECT (compute_walk_forward needs temporal sort by ts column).

In main(), replace the per-dimension print loop with:

```
import json
result = evaluate_all(df, transform_id=args.agent, transform_version="v1", segment_key="__global__")
print(json.dumps(result, indent=2, default=str))
sys.exit(0 if result["is_graduated"] else 1)
```

Keep argparse args: --agent, --days, --symbol-filter. Drop --train-fraction (graduation.py uses EVAL_WALK_FORWARD_FRACTION constant; CLI override out of scope).

Update module docstring's "Validation dimensions" section: change "failure_prob vs pnl_r (negative correlation)" to "multiplier vs pnl_r (positive correlation)".

Final file must be ruff/black clean and runnable end-to-end with --help.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast; m = ast.parse(open('scripts/validate_skeptic.py').read()); assert any('graduation' in (n.module or '') for n in ast.walk(m) if isinstance(n, ast.ImportFrom))" &amp;&amp; .venv/bin/python scripts/validate_skeptic.py --help &amp;&amp; .venv/bin/ruff check scripts/validate_skeptic.py</automated>
  </verify>
  <acceptance_criteria>
    - File scripts/validate_skeptic.py exists and is under 150 lines (`wc -l scripts/validate_skeptic.py` reports < 150)
    - Contains exact string `from src.intelligence.swarm.graduation import`
    - Does NOT contain inline `def compute_spearman` or `def compute_calibration` (grep returns 0 for these — they were moved)
    - Does NOT contain `GATE_SPEARMAN_RHO = -0.15` (old negative constant must be gone)
    - `.venv/bin/python scripts/validate_skeptic.py --help` exits 0 and shows --agent flag
    - `.venv/bin/ruff check scripts/validate_skeptic.py` exits 0
    - `.venv/bin/black --check scripts/validate_skeptic.py` exits 0
  </acceptance_criteria>
  <done>Script is a thin wrapper; no duplicate logic; CLI runnable.</done>
</task>

</tasks>

<verification>
`grep -E "^def (compute_|_sharpe|minimum_detectable)" scripts/validate_skeptic.py` returns nothing (all inline compute deleted).
</verification>

<success_criteria>
graduation.py is the only source of compute logic. CLI script unchanged in user-facing behavior except for new sign convention.
</success_criteria>

<output>
After completion, create `.planning/phases/72-signal-transform-log-unified-alpha-modifier-architecture-add/72-05-SUMMARY.md`
</output>
