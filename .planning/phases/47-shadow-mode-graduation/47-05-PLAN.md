---
phase: 47-shadow-mode-graduation
plan: 05
type: execute
wave: 3
depends_on:
  - 47-01
  - 47-02
  - 47-03
  - 47-04
files_modified:
  - services/tws_daemon.py
  - services/indicator_service.py
  - services/market_analysis_service.py
  - services/signal_generator_service.py
  - services/feature_writer_service.py
  - src/config/settings.py
  - tests/unit/test_roll_detection_algorithm.py
autonomous: false
gap_closure: true
requirements:
  - SHADOW-03

must_haves:
  truths:
    - "validate_roll_detection.py exits 0 (PASS) before ROLL_MONITOR_ENABLED is set — D-21 gate satisfied"
    - "049_roll_premium_pct.sql applied to live DB and roll_premium_pct column confirmed present"
    - "ROLL_MONITOR_ENABLED=true in .env, tws + 4 pipeline services restarted"
    - "5 clean trading days soaked with roll monitor active before scaffolding removal"
    - "roll_monitor_enabled field removed from Settings and all conditional branches removed from 5 services"
    - "get_active_contracts() unconditionally queries DB for front-month contracts (early-return guard removed)"
    - "test_roll_detection_algorithm.py test helpers updated to remove roll_monitor_enabled param; all unit tests pass"
  artifacts:
    - path: "src/config/settings.py"
      provides: "Settings without roll_monitor_enabled field; get_active_contracts always uses DB path"
      contains: "roll_monitor_window_size"
    - path: "services/tws_daemon.py"
      provides: "RollMonitor always active — no self._enabled guard, no is_enabled property usage"
    - path: "services/indicator_service.py"
      provides: "Always subscribes to system_events topic unconditionally"
    - path: "services/market_analysis_service.py"
      provides: "Always subscribes to system_events topic unconditionally"
    - path: "services/signal_generator_service.py"
      provides: "No self._roll_monitor_enabled attribute; always subscribes to system_events"
    - path: "services/feature_writer_service.py"
      provides: "No self._roll_monitor_enabled attribute; always subscribes to system_events"
  key_links:
    - from: "services/tws_daemon.py"
      to: "services/signal_generator_service.py"
      via: "Roll events published unconditionally via system_events topic"
      pattern: "topic_system_events"
    - from: "src/config/settings.py"
      to: "services/*.py"
      via: "roll_monitor_enabled field absent — any service still reading it will fail import"
      pattern: "roll_monitor_enabled"
---

<objective>
Complete the SHADOW-03 roll monitor graduation ceremony: validate the detection algorithm against live data, enable roll monitor in production, soak for 5 trading days, then remove all feature-flag scaffolding from 5 services and Settings.

Purpose: This is the final deferred step of Phase 47. Plans 01-04 completed all other SHADOW requirements. SHADOW-03 was blocked by an empty market_data_5m view at D-21 validation time. Todo 023 captures the remaining steps, which this plan executes.

Output: Roll monitor running unconditionally in production; no roll_monitor_enabled conditional code paths remain in any service; clean DAG with no feature flags.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-shadow-mode-graduation/47-CONTEXT.md
@.planning/phases/47-shadow-mode-graduation/47-02-SUMMARY.md
@.planning/phases/47-shadow-mode-graduation/47-03-SUMMARY.md

<interfaces>
<!-- Exact occurrences to remove — grep-verified against codebase at plan-write time. -->

services/tws_daemon.py:
  line 101: self._enabled = settings.roll_monitor_enabled
  line 131: def is_enabled(self) -> bool:  (property, returns self._enabled)
  line 600: if self._roll_monitor.is_enabled:  (guard before update_volume + check_roll)

services/indicator_service.py:
  line 689-696: # Build topics list comment + if _settings.roll_monitor_enabled: + topics.append(topic_system_events(...))

services/market_analysis_service.py:
  line 768-772: # Build topics list comment + if _settings.roll_monitor_enabled: + topics.append(topic_system_events(...))

services/signal_generator_service.py:
  line 436: self._roll_monitor_enabled = getattr(settings, "roll_monitor_enabled", False)
  line 767-768: if self._roll_monitor_enabled: + topics.append(topic_system_events(...))

services/feature_writer_service.py:
  line 269: self._roll_monitor_enabled: bool = getattr(_s, "roll_monitor_enabled", False)
  line 274: self._roll_monitor_enabled = False  (fallback in except block)
  line 385-386: if self._roll_monitor_enabled: + topics.append(topic_system_events(...))

src/config/settings.py:
  line 95: roll_monitor_enabled: bool = Field(default=False, validation_alias="ROLL_MONITOR_ENABLED")
  line 888: if not s.roll_monitor_enabled: return list(s.contracts)  [early-return gate in get_active_contracts()]

tests/unit/test_roll_detection_algorithm.py:
  line 32: roll_monitor_enabled: bool = True  (param in _make_settings helper)
  line 43: s.roll_monitor_enabled = roll_monitor_enabled
  line 405: daemon.settings = _make_settings(roll_monitor_enabled=True)
  line 415: mock_rm.is_enabled = True
  line 461-462: s_on/_off = _make_settings(roll_monitor_enabled=True/False)
  line 471: s = _make_settings(roll_monitor_enabled=False)
  line 533: daemon.settings = _make_settings(roll_monitor_enabled=enabled)
  line 543: mock_rm.is_enabled = enabled
  line 564/574: docstrings mentioning roll_monitor_enabled=true/false
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Validate roll detection, apply migration, enable ROLL_MONITOR_ENABLED=true, restart services, soak 5 trading days</name>

  <files></files>

  <action>
    This is a human-gated task. Claude does not modify any files. The executor presents the verification steps below and waits for the human to complete them and signal readiness.
  </action>

  <what-built>
    Plans 01-04 completed:
    - Roll detection algorithm rewritten — calendar-driven windows + volume z-score (bug D-16 fixed, D-19 single-arg update_volume)
    - validate_roll_detection.py script written with DETECTION_RATE_GATE=0.90 and FALSE_POSITIVE_RATE_GATE=0.10
    - Migration 049_roll_premium_pct.sql ready to apply
    - Todo 023 captures all remaining enablement steps
    This task executes those steps in the correct ceremony order.
  </what-built>

  <how-to-verify>
    **Step 0 — Confirm market_data_5m has data:**
    ```bash
    docker exec timescaledb psql -U postgres -d indicagent -c \
      "SELECT COUNT(*) FROM market_data_5m WHERE timestamp > NOW() - INTERVAL '7 days';"
    ```
    If count = 0, the view is still empty. Wait for IBKR to be live and 5m bars to accumulate before proceeding.
    If count > 0, continue.

    **Step 1 — Run D-21 pre-enable validation:**
    ```bash
    .venv/bin/python production/scripts/validate_roll_detection.py
    ```
    Required result: exit code 0 (PASS) with:
    - detection_rate >= 0.90 for all tested symbols
    - false_positive_rate < 0.10

    If exit code 2 (SKIP) — market_data_5m still empty; stop here, wait longer.
    If exit code 1 (FAIL) — algorithm does not meet accuracy gates; type "validation-failed: {reason}" and do NOT proceed.

    **Step 2 — Apply DB migration:**
    ```bash
    docker cp production/migrations/049_roll_premium_pct.sql timescaledb:/tmp/049_roll_premium_pct.sql
    docker exec timescaledb psql -U postgres -d indicagent -f /tmp/049_roll_premium_pct.sql
    ```
    Confirm column exists:
    ```bash
    docker exec timescaledb psql -U postgres -d indicagent -c \
      "SELECT column_name FROM information_schema.columns \
       WHERE table_name='intelligence_features' AND column_name='roll_premium_pct';"
    ```
    Expected: 1 row returned.

    **Step 3 — Add ROLL_MONITOR_ENABLED=true to .env:**
    Open the production .env file and add:
    ```
    ROLL_MONITOR_ENABLED=true
    ```

    **Step 4 — Restart affected services:**
    ```bash
    echo '***REDACTED-SUDO-PASSWORD***' | /usr/bin/sudo.ws -S systemctl restart \
      indicagent-tws \
      indicagent-feature-pipeline \
      indicagent-signal-generator \
      indicagent-signal-lifecycle \
      indicagent-feature-writer
    ```

    **Step 5 — Verify roll monitor initialized:**
    ```bash
    journalctl -u indicagent-tws -n 30 --no-pager | grep -i "roll\|error"
    journalctl -u indicagent-signal-generator -n 50 --no-pager | grep -i "system_events\|roll\|error"
    ```
    Expected: tws starts without errors; signal-generator subscribes to system_events topic.

    **Step 6 — Verify regime gate at safety floor:**
    ```bash
    journalctl -u indicagent-signal-generator -n 100 --no-pager | grep "regime_prob\|regime_suppressed"
    ```
    Fewer regime suppressions expected (floor now 0.30 vs prior 0.55). This is expected behavior.

    **Step 7 — Monitor Prometheus for 5 clean trading days:**
    - feature-pipeline: http://localhost:9125/metrics — watch for error rate increase
    - signal-generator: http://localhost:9112/metrics — watch for error rate increase
    If any regression: set ROLL_MONITOR_ENABLED=false in .env, restart, and type "soak-failed: {description}".

    **Step 8 — After 5 clean trading days:**
    Type "approved" to proceed with scaffolding removal.
  </how-to-verify>

  <verify>
    <automated>HUMAN_GATE — human must approve after 5 clean trading days before Task 2 runs</automated>
  </verify>

  <done>5 clean trading days confirmed with ROLL_MONITOR_ENABLED=true and no Prometheus regressions; human has typed "approved"</done>

  <resume-signal>
    Type "approved" after 5 clean trading days with roll monitor active and no regressions.
    Type "validation-failed: {reason}" if D-21 gate failed (algorithm accuracy below gate).
    Type "soak-failed: {description}" if the 5-day soak revealed issues.
    In either failure case, do NOT proceed to Task 2 — scaffolding removal is gate-locked to a successful soak.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Remove roll_monitor_enabled scaffolding from 5 services, Settings, and tests (SHADOW-03 per D-23)</name>

  <read_first>
    services/tws_daemon.py
    services/indicator_service.py
    services/market_analysis_service.py
    services/signal_generator_service.py
    services/feature_writer_service.py
    src/config/settings.py
    tests/unit/test_roll_detection_algorithm.py
  </read_first>

  <files>
    services/tws_daemon.py,
    services/indicator_service.py,
    services/market_analysis_service.py,
    services/signal_generator_service.py,
    services/feature_writer_service.py,
    src/config/settings.py,
    tests/unit/test_roll_detection_algorithm.py
  </files>

  <action>
    Make targeted removals only. Read each file before editing. Do not restructure surrounding code — only delete the conditional scaffolding and dedent the code that was inside it.

    **src/config/settings.py — 2 changes:**

    Change 1: Remove field at line 95:
      roll_monitor_enabled: bool = Field(default=False, validation_alias="ROLL_MONITOR_ENABLED")
    Delete this line entirely.

    Change 2: In get_active_contracts() at line 888, remove the early-return guard:
      if not s.roll_monitor_enabled:
          return list(s.contracts)
    Delete both lines. The function now always proceeds to the DB-backed path (cache check + DB query + fallback).
    Update the comment at line 828 to remove the "(used when roll_monitor_enabled=True)" qualifier — the cache is now always used.

    **services/tws_daemon.py — 3 changes:**

    Change 1: In RollMonitor.__init__ (line 101), remove:
      self._enabled = settings.roll_monitor_enabled

    Change 2: Remove the is_enabled property (lines ~131-133):
      @property
      def is_enabled(self) -> bool:
          return self._enabled
    Delete the full property block.

    Change 3: In _emit_bar (line 600), remove the guard and dedent the 3-line body:
    Before:
      if self._roll_monitor.is_enabled:
          self._roll_monitor.update_volume(symbol, float(state["volume"]))
          if self._roll_monitor.check_roll(symbol, bar_utc):
              await self._roll_monitor._on_roll_confirmed(...)
    After (no conditional, dedented one level):
      self._roll_monitor.update_volume(symbol, float(state["volume"]))
      if self._roll_monitor.check_roll(symbol, bar_utc):
          await self._roll_monitor._on_roll_confirmed(...)

    **services/indicator_service.py — 1 change (lines 689-696):**

    Remove the conditional wrapper and dedent. Before:
      # Build topics list — conditionally add system.events when roll_monitor_enabled
      if _settings.roll_monitor_enabled:
          topics.append(topic_system_events(self.env_name))
    After:
      topics.append(topic_system_events(self.env_name))
    Update the comment to remove "conditionally" language.

    **services/market_analysis_service.py — 1 change (lines 768-772):**

    Same pattern as indicator_service.py. Before:
      if _settings.roll_monitor_enabled:
          topics.append(topic_system_events(self.env_name))
    After:
      topics.append(topic_system_events(self.env_name))

    **services/signal_generator_service.py — 2 changes:**

    Change 1: In __init__ (line 436), remove:
      self._roll_monitor_enabled = getattr(settings, "roll_monitor_enabled", False)

    Change 2: In topic subscription setup (lines 767-768), remove conditional and dedent:
    Before:
      if self._roll_monitor_enabled:
          topics.append(topic_system_events(self.env_name))
    After:
      topics.append(topic_system_events(self.env_name))

    **services/feature_writer_service.py — 3 changes:**

    Change 1: In __init__ (line 269), remove:
      self._roll_monitor_enabled: bool = getattr(_s, "roll_monitor_enabled", False)

    Change 2: In the except block fallback (line 274), remove:
      self._roll_monitor_enabled = False

    Change 3: In topic subscription setup (lines 385-386), remove conditional and dedent:
    Before:
      if self._roll_monitor_enabled:
          topics.append(topic_system_events(self._env_name))
    After:
      topics.append(topic_system_events(self._env_name))

    **tests/unit/test_roll_detection_algorithm.py — cleanup:**

    The _make_settings helper (line 32) has roll_monitor_enabled as a parameter and sets it on the mock
    (line 43). After Settings no longer has this field, setting it silently creates a spurious attribute on
    the mock or causes AttributeError. Fix:

    1. Remove `roll_monitor_enabled: bool = True` parameter from _make_settings signature.
    2. Remove `s.roll_monitor_enabled = roll_monitor_enabled` assignment inside the helper.
    3. Remove all call sites that pass roll_monitor_enabled=True or roll_monitor_enabled=False
       (lines 405, 461, 462, 471, 533).
    4. Remove `mock_rm.is_enabled = True/False` mock setup (lines 415, 543) — the property no longer
       exists on RollMonitor, so this mock attribute is stale.
    5. For tests that tested "enabled vs disabled" behavior (around lines 461-474 and 533-574):
       Roll monitor is now always active. Collapse each "disabled" test variant: if the test was
       asserting that check_roll is NOT called when disabled, replace with a positive assertion that
       check_roll IS called (since the guard is gone, it always runs).
    6. Update docstrings at lines 564 and 574 from "when roll_monitor_enabled=true/false" to
       "roll monitor always active".

    After all edits, run the full test suite:
      .venv/bin/pytest tests/unit/ -x -q

    If any test fails due to roll_monitor_enabled removal, fix the test — do not revert service changes.

    Final verification grep (must return 0 hits for both patterns):
      grep -rn "roll_monitor_enabled\|_roll_monitor_enabled" services/ src/config/settings.py --include="*.py"
      grep -n "is_enabled" services/tws_daemon.py
  </action>

  <verify>
    <automated>
      cd /home/bg/dev/indicagent &&
      ! grep -rn "_roll_monitor_enabled\|roll_monitor_enabled" services/ src/config/settings.py --include="*.py" &&
      ! grep -n "is_enabled" services/tws_daemon.py &&
      grep -q "roll_monitor_window_size" src/config/settings.py &&
      .venv/bin/pytest tests/unit/ -x -q 2>&1 | tail -3 &&
      echo "PASS"
    </automated>
  </verify>

  <acceptance_criteria>
    - grep for `roll_monitor_enabled` across services/ and src/config/settings.py returns 0 hits
    - grep for `_roll_monitor_enabled` across services/ returns 0 hits
    - grep for `is_enabled` in services/tws_daemon.py returns 0 hits (property removed)
    - src/config/settings.py retains roll_monitor_window_size, roll_monitor_cooldown_min, roll_monitor_postroll_bars, roll_monitor_threshold_default (runtime config, not feature flags — do not remove these)
    - get_active_contracts() in settings.py no longer has the early-return guard — it always proceeds to the DB query path
    - services/indicator_service.py unconditionally appends topic_system_events to topics list
    - services/market_analysis_service.py unconditionally appends topic_system_events to topics list
    - services/signal_generator_service.py has no self._roll_monitor_enabled attribute in __init__
    - services/feature_writer_service.py has no self._roll_monitor_enabled attribute in __init__ or except block
    - tests/unit/test_roll_detection_algorithm.py _make_settings helper has no roll_monitor_enabled parameter
    - All unit tests pass: `.venv/bin/pytest tests/unit/ -x -q` exits 0
    - `.venv/bin/ruff check .` exits 0
    - Commit is a separate atomic commit referencing SHADOW-03 (separate from cross-asset cleanup in 47-04)
  </acceptance_criteria>

  <done>
    roll_monitor_enabled feature flag fully removed from Settings, all 5 services, and tests.
    Roll monitor always active — RollMonitor.check_roll() called unconditionally on every bar.
    All services always subscribe to system_events topic.
    get_active_contracts() always queries DB for front-month contracts (no config-file-only early return).
    Clean DAG: services either participate or they do not.
    Todo 023 can be closed.
  </done>
</task>

</tasks>

<verification>
After Task 2 completes:

```bash
# Zero hits for flag and attribute
grep -rn "roll_monitor_enabled\|_roll_monitor_enabled" services/ src/config/settings.py --include="*.py"

# Zero hits for is_enabled in tws_daemon
grep -n "is_enabled" services/tws_daemon.py

# Runtime config fields still present (tuning knobs, not flags)
grep -c "roll_monitor_window_size\|roll_monitor_cooldown_min\|roll_monitor_postroll_bars\|roll_monitor_threshold_default" src/config/settings.py

# All services subscribe unconditionally to system_events
grep -B1 "topic_system_events" services/indicator_service.py
grep -B1 "topic_system_events" services/market_analysis_service.py

# Full test suite green
.venv/bin/pytest tests/unit/ -v 2>&1 | tail -20

# No lint errors
.venv/bin/ruff check .
```
</verification>

<success_criteria>
1. D-21 gate passed (validate_roll_detection.py exits 0) before ROLL_MONITOR_ENABLED was set
2. Migration 049_roll_premium_pct.sql applied — roll_premium_pct column confirmed in intelligence_features
3. 5 clean trading days soaked with ROLL_MONITOR_ENABLED=true and no regressions
4. Zero occurrences of roll_monitor_enabled or _roll_monitor_enabled in services/ or Settings
5. RollMonitor.is_enabled property deleted — check_roll() called unconditionally
6. get_active_contracts() always queries DB (no config-file-only fast path remains)
7. All unit tests pass after scaffolding removal
8. SHADOW-03 requirement fully satisfied — roll monitor participates unconditionally in the DAG
9. Todo 023 closed
</success_criteria>

<output>
After completion:
1. Create `.planning/phases/47-shadow-mode-graduation/47-05-SUMMARY.md`
2. Move `.planning/todos/pending/023-retry-roll-detection-validation-when-market-data-5m-populates.md` to `.planning/todos/done/`
3. Update `.planning/STATE.md` — Phase 47 complete, all 5 plans done
</output>
