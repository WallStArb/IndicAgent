---
phase: 078-i8-alpha-feedback-loop
plan: 03
type: execute
wave: 2
depends_on: ["078-01"]
files_modified:
  - production/migrations/078_alpha_swarm_shadow_enrollment.sql
  - services/alpha_swarm_agent.py
  - tests/unit/service_tests/test_alpha_swarm_agent.py
  - tests/integration/test_swarm_graduation_loop.py
autonomous: true
requirements:
  - P78-GRADUATION

user_setup: []

must_haves:
  truths:
    - "skeptic_v1 row exists in shadow_registry with component_type='swarm_agent' after _setup() completes (idempotent on restart)"
    - "_graduation_loop reads from signal_lineage WHERE event_type='agent_prediction' JOINed with signal_ledger on signal_id — never from signal_transform_log"
    - "Promotion requires N >= 100 resolved signals AND Spearman ρ > 0 AND p < 0.05"
    - "Demotion fires after 3 consecutive 15-min eval cycles with Spearman ρ < 0"
    - "Eval cycle cadence is 15 minutes; loop is idempotent and survives restarts"
  artifacts:
    - path: "production/migrations/078_alpha_swarm_shadow_enrollment.sql"
      provides: "Idempotent enrollment of skeptic_v1 in shadow_registry as component_type='swarm_agent'"
      contains: "ON CONFLICT"
    - path: "services/alpha_swarm_agent.py"
      provides: "_graduation_loop method querying signal_lineage; idempotent enrollment in _setup"
      contains: "_graduation_loop"
    - path: "tests/integration/test_swarm_graduation_loop.py"
      provides: "End-to-end test of enrollment → prediction → outcome → Spearman computation"
      contains: "spearmanr"
  key_links:
    - from: "alpha_swarm_agent._setup"
      to: "shadow_registry"
      via: "INSERT ... ON CONFLICT DO NOTHING for ('swarm_agent', 'skeptic_v1')"
      pattern: "ON CONFLICT"
    - from: "_graduation_loop"
      to: "signal_lineage JOIN signal_ledger"
      via: "SELECT prediction, pnl_r FROM signal_lineage l JOIN signal_ledger s ON l.signal_id = s.signal_id WHERE l.event_type='agent_prediction' AND l.agent_name='skeptic_v1' AND s.outcome IS NOT NULL"
      pattern: "signal_lineage"
    - from: "_graduation_loop"
      to: "scipy.stats.spearmanr"
      via: "Spearman ρ + p-value computed on prediction vs pnl_r"
      pattern: "spearmanr"
---

<objective>
Wave 2 plan that wires the skeptic_v1 swarm agent into the unified `shadow_registry` graduation state machine. Depends on Plan 01 because graduation reads from `signal_lineage` (populated by LineageWriterAgent consuming LineageRecorder output from Plan 01).

Deliverables:

1. Migration `078_alpha_swarm_shadow_enrollment.sql` — idempotent INSERT of `skeptic_v1` into `shadow_registry` with `component_type='swarm_agent'`. Does NOT enroll correlation_v1/volume_v1 (those agents are deleted by Plan 06).
2. `_graduation_loop` method on `AlphaSwarmComputeAgent` running every 15 min: query `signal_lineage` JOIN `signal_ledger`, compute Spearman ρ + p-value, apply promotion/demotion gates, write transitions to `shadow_registry`.
3. Idempotent enrollment in `_setup()` (mirroring `shadow_registry_ensure()` pattern from Phase 75) so missing rows are auto-created on startup.

Why: Skeptic must earn the right through proof — N≥100, ρ>0, p<0.05. Otherwise it stays in shadow forever.

Output: migration + service code + integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/078-i8-alpha-feedback-loop/78-CONTEXT.md
@.planning/phases/078-i8-alpha-feedback-loop/078-01-PLAN.md
@services/alpha_swarm_agent.py
@services/shadow_auditor_agent.py
@src/intelligence/register_plugins.py
@production/migrations/077_shadow_governance.sql

<interfaces>
shadow_registry table contract (from Phase 75 migration 077):
- columns include: component_type (text), component_name (text), state (text: shadow|live|demoted), n_resolved (int), last_eval_at (timestamptz), last_metric (jsonb)
- Read `production/migrations/077_shadow_governance.sql` to confirm exact column names and constraints before writing migration 078.

signal_lineage table contract (Phase 73):
- Read the migration that created `signal_lineage` (search `grep -RIn 'signal_lineage' production/migrations/`) to confirm column names: signal_id, event_type, agent_name, prediction, segment_key, features_snapshot, ts.

shadow_registry_ensure() pattern (`src/intelligence/register_plugins.py:~620`):
- Read this function to mirror its idempotent INSERT ... ON CONFLICT DO NOTHING pattern.

Spearman gate values (D-24, D-25):
- promotion: n_resolved >= 100 AND spearman_rho > 0 AND spearman_p < 0.05
- demotion: 3 consecutive eval cycles with spearman_rho < 0 → state='demoted'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 078 — idempotent skeptic_v1 enrollment</name>
  <files>production/migrations/078_alpha_swarm_shadow_enrollment.sql</files>
  <read_first>
    - production/migrations/077_shadow_governance.sql (exact shadow_registry schema)
    - src/intelligence/register_plugins.py (shadow_registry_ensure pattern)
  </read_first>
  <action>
    Create `production/migrations/078_alpha_swarm_shadow_enrollment.sql`:

    ```sql
    -- Phase 78 D-23/D-26: Enroll skeptic_v1 (swarm_agent) in shadow_registry.
    -- Idempotent: re-running this migration does not change rows.
    -- Does NOT enroll correlation_v1 or volume_v1 (those agents are deleted in Plan 06).

    INSERT INTO shadow_registry (component_type, component_name, state, n_resolved, last_eval_at, last_metric)
    VALUES ('swarm_agent', 'skeptic_v1', 'shadow', 0, NULL, NULL)
    ON CONFLICT (component_type, component_name) DO NOTHING;
    ```

    Adjust column names exactly to match what migration 077 created. If `last_metric` column does not exist, omit it. If the unique constraint is on a different column tuple, match it.

    Per D-23, D-26.
  </action>
  <verify>
    <automated>docker exec timescaledb psql -U postgres -d indicagent -f /dev/stdin < production/migrations/078_alpha_swarm_shadow_enrollment.sql && docker exec timescaledb psql -U postgres -d indicagent -c "SELECT component_type, component_name, state FROM shadow_registry WHERE component_name='skeptic_v1';" | grep -q "swarm_agent.*skeptic_v1.*shadow"</automated>
  </verify>
  <acceptance_criteria>
    - Migration applies cleanly to a fresh DB
    - Re-running the migration produces no changes (idempotent)
    - `SELECT * FROM shadow_registry WHERE component_name='skeptic_v1' AND component_type='swarm_agent'` returns exactly one row with state='shadow'
  </acceptance_criteria>
  <done>skeptic_v1 enrolled in shadow_registry; migration is idempotent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _graduation_loop on AlphaSwarmComputeAgent</name>
  <files>services/alpha_swarm_agent.py, tests/unit/service_tests/test_alpha_swarm_agent.py, tests/integration/test_swarm_graduation_loop.py</files>
  <read_first>
    - services/alpha_swarm_agent.py (post-Plan-01 state — _setup, write path)
    - services/shadow_auditor_agent.py (eval-loop pattern, Spearman computation, demotion counter)
    - src/intelligence/register_plugins.py:~620 (shadow_registry_ensure idempotent pattern)
    - production/migrations/077_shadow_governance.sql (column names)
  </read_first>
  <behavior>
    - `_setup()` calls a `_shadow_registry_ensure_swarm()` helper that runs the same `INSERT ... ON CONFLICT DO NOTHING` as migration 078 — guarantees enrollment even if migration was missed
    - `_graduation_loop()` is an async background task spawned from `_setup()` after `super()._setup()` completes
    - It runs every 15 minutes (configurable via `Settings.SWARM_GRADUATION_INTERVAL_S = 900`)
    - Each cycle:
      a. Query `signal_lineage l JOIN signal_ledger s ON l.signal_id = s.signal_id WHERE l.event_type='agent_prediction' AND l.agent_name='skeptic_v1' AND s.outcome IS NOT NULL` returning `(prediction, pnl_r)` pairs
      b. If `n_resolved < 100`, write current `n_resolved` to shadow_registry but do NOT compute Spearman; reset demotion counter
      c. If `n_resolved >= 100`, compute `scipy.stats.spearmanr(predictions, pnl_rs)` → (rho, p_value)
      d. Promotion: if `state='shadow' AND rho > 0 AND p_value < 0.05` → UPDATE state='live'
      e. Demotion: track in-memory `_demotion_streak`; if `rho < 0` increment, else reset; if streak >= 3 AND state='live' → UPDATE state='demoted'
      f. Always UPDATE `last_eval_at = NOW()` and `last_metric = jsonb_build_object('rho', rho, 'p', p_value, 'n', n_resolved)`
    - Loop is cancellable on shutdown; uses `asyncio.shield` only around the DB transaction
    - Integration test: insert 100 lineage rows + 100 ledger rows with positive correlation; run one cycle; assert state transitions to 'live'
  </behavior>
  <action>
    1. Add a `_shadow_registry_ensure_swarm()` async method to `AlphaSwarmComputeAgent`. It executes the same idempotent INSERT as migration 078, against `self._pool` (the pool from `BaseGroupService._setup()`). Call this from `_setup()` after `super()._setup()`.
    2. Add `_graduation_loop()` async method. Use `asyncio.create_task(self._graduation_loop())` from `_setup()` and store the handle on `self._graduation_task`. Cancel + await it in `_teardown()`.
    3. The loop body:
       - `await asyncio.sleep(self.settings.swarm_graduation_interval_s)` at top of each iteration (so first run happens after one interval — matches ShadowAuditorAgent cadence)
       - Wrap each iteration in `try: ... except Exception as exc: log.error("graduation_cycle_failed", err=str(exc))` — never let one failure kill the loop
    4. Query SQL:
       ```sql
       SELECT l.prediction, s.pnl_r
       FROM signal_lineage l
       JOIN signal_ledger s ON l.signal_id = s.signal_id
       WHERE l.event_type = 'agent_prediction'
         AND l.agent_name = 'skeptic_v1'
         AND s.outcome IS NOT NULL
       ORDER BY l.ts DESC
       LIMIT 5000
       ```
       Use asyncpg `fetch()`. Returns Python list of records; `prediction` is JSONB → asyncpg returns `dict`. Extract the scalar prediction value the agent records (verify exact field by reading what `_record_swarm_result` puts in `prediction` after Plan 01). If prediction is a dict like `{"score": 0.7}`, extract `r["prediction"]["score"]`.
    5. Compute Spearman: `from scipy.stats import spearmanr; rho, p = spearmanr(predictions, pnl_rs)`. Handle `nan` from constant arrays — treat as `rho=0, p=1`.
    6. Maintain `self._demotion_streak: int = 0` initialized in `_setup()`.
    7. State transitions via UPDATE:
       ```sql
       UPDATE shadow_registry
       SET state = $1,
           n_resolved = $2,
           last_eval_at = NOW(),
           last_metric = $3::jsonb
       WHERE component_type = 'swarm_agent' AND component_name = 'skeptic_v1'
       ```
       Pass `last_metric` as Python dict (asyncpg JSONB rule from CLAUDE.md — never `json.dumps`).
    8. Logging: use structlog with explicit field names (no `event=` kwarg per CLAUDE.md). Example: `log.info("graduation_cycle", n=n_resolved, rho=rho, p=p, state=new_state, streak=self._demotion_streak)`.
    9. Unit tests in `tests/unit/service_tests/test_alpha_swarm_agent.py`:
       - test_promotion_gate: synthetic 100 strongly-positive correlated samples → state transitions to 'live'
       - test_under_n_no_eval: 50 samples → no Spearman, state stays 'shadow', last_metric has n=50 only
       - test_demotion_streak: state='live', three consecutive cycles with rho < 0 → state='demoted'; rho < 0 then rho > 0 then rho < 0 → streak resets to 1, no demotion
    10. Integration test `tests/integration/test_swarm_graduation_loop.py`: requires live TimescaleDB. Inserts 100 rows into signal_lineage + signal_ledger with rank-correlated prediction/pnl_r, runs one cycle, asserts state='live'. Mark with `@pytest.mark.integration`.
    Per D-05, D-06, D-07, D-23, D-24, D-25.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/service_tests/test_alpha_swarm_agent.py -x -v -k "promotion or demotion or under_n" && .venv/bin/ruff check services/alpha_swarm_agent.py && grep -q "FROM signal_lineage" services/alpha_swarm_agent.py && grep -q "spearmanr" services/alpha_swarm_agent.py && ! grep -q "signal_transform_log" services/alpha_swarm_agent.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "FROM signal_lineage" services/alpha_swarm_agent.py` succeeds
    - `grep -q "spearmanr" services/alpha_swarm_agent.py` succeeds
    - `! grep -q "signal_transform_log" services/alpha_swarm_agent.py` succeeds (we read from signal_lineage, not the deprecated table)
    - All three unit tests (promotion, demotion, under_n) pass
    - `.venv/bin/ruff check services/alpha_swarm_agent.py` exits 0
  </acceptance_criteria>
  <done>skeptic_v1 enrolled and evaluated every 15 min against signal_lineage; promotion + demotion gates fire correctly under unit-test simulation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal_lineage rows → graduation eval | Untrusted prediction values must be coerced to float; non-numeric drops |
| shadow_registry UPDATE | Race with ShadowAuditorAgent (different component_type) — must scope WHERE clause to component_type='swarm_agent' |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-08 | Tampering | malformed prediction JSONB causes scipy crash | mitigate | filter rows where `prediction` is None or non-float; log dropped count |
| T-78-09 | Repudiation | premature promotion before N=100 | mitigate | hard gate `if n_resolved < 100: return` before computing Spearman |
| T-78-10 | Elevation of privilege | swarm_agent UPDATE accidentally touching i7_plugin rows | mitigate | every UPDATE WHERE clause scopes to `component_type='swarm_agent'` |
| T-78-11 | Denial of service | unbounded query with millions of resolved signals | mitigate | LIMIT 5000 on the lineage JOIN; ORDER BY ts DESC samples most recent |
</threat_model>

<verification>
- Migration 078 applies idempotently
- `SELECT state FROM shadow_registry WHERE component_name='skeptic_v1'` returns 'shadow' on fresh enrollment
- Unit tests for promotion, demotion, and under-N gates all pass
- `! grep -q signal_transform_log services/alpha_swarm_agent.py` succeeds
- `.venv/bin/ruff check services/alpha_swarm_agent.py` exits 0
</verification>

<success_criteria>
- `_graduation_loop` queries the correct table (`signal_lineage`)
- Promotion: N>=100, ρ>0, p<0.05 (statistical rigor — earn the right through proof)
- Demotion: 3 consecutive cycles with ρ<0 — automatic, no human in loop
- Idempotent enrollment guarantees skeptic_v1 row exists after every restart
</success_criteria>

<output>
After completion, create `.planning/phases/078-i8-alpha-feedback-loop/078-03-SUMMARY.md`
</output>
