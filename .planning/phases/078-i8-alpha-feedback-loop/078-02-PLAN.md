---
phase: 078-i8-alpha-feedback-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/ai/safe_wrapper.py
  - src/intelligence/ai/narrative/narrative_group.py
  - src/core/ml/shadow.py
  - src/core/ml/transform_recorder.py
  - tests/unit/test_safe_wrapper.py
  - tests/unit/test_narrative_group.py
autonomous: true
requirements:
  - P78-SAFE-WRAPPER
  - P78-SETUP-OVERRIDE

user_setup: []

must_haves:
  truths:
    - "src/core/ai/safe_wrapper.py is reduced to a stub that raises ImportError if imported"
    - "NarrativeGroupComputeAgent does NOT define _setup() — it inherits BaseGroupService._setup() unmodified"
    - "src/core/ml/shadow.py and src/core/ml/transform_recorder.py carry an ARCHIVED header at the top of the file and emit a DeprecationWarning on import"
    - "No production module under services/ or src/intelligence/ imports ShadowRecorder, TransformRecorder, or SafeAgentWrapper"
  artifacts:
    - path: "src/core/ai/safe_wrapper.py"
      provides: "Stub that raises ImportError"
      contains: "deleted in Phase 78"
    - path: "src/intelligence/ai/narrative/narrative_group.py"
      provides: "NarrativeGroupComputeAgent without _setup() override"
      contains: "class NarrativeGroupComputeAgent"
    - path: "src/core/ml/shadow.py"
      provides: "ARCHIVED ShadowRecorder — historical reference only"
      contains: "ARCHIVED"
    - path: "src/core/ml/transform_recorder.py"
      provides: "ARCHIVED TransformRecorder — historical reference only"
      contains: "ARCHIVED"
  key_links:
    - from: "BaseAIAgent.compute()"
      to: "asyncio.wait_for + neutral fallback"
      via: "already provides timeout protection — SafeAgentWrapper redundant"
      pattern: "asyncio.wait_for"
    - from: "BaseGroupService._setup"
      to: "NarrativeGroupComputeAgent"
      via: "inherited unmodified"
      pattern: "class NarrativeGroupComputeAgent"
---

<objective>
Wave 1 plan that removes dead code paths so the codebase reflects reality after the LineageRecorder migration in Plan 01:

1. Delete `src/core/ai/safe_wrapper.py` content (D-18) — `BaseAIAgent.compute()` already provides `asyncio.wait_for` + neutral fallback.
2. Remove the `_setup()` override on `NarrativeGroupComputeAgent` (D-19) — `BaseGroupService._setup()` already handles empty `bar_topics` correctly.
3. Archive (do NOT delete) `src/core/ml/shadow.py` and `src/core/ml/transform_recorder.py` (D-04) — keep the classes for historical reference but mark them archived and prevent re-introduction.

Why: After Plan 01, nothing in the swarm uses these paths. Leaving live code that no one calls invites future regressions. Renaissance principle: no dual-write debt.

Output: clean deletions/archives with regression tests asserting nothing imports the dead paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/078-i8-alpha-feedback-loop/78-CONTEXT.md
@src/core/ai/safe_wrapper.py
@src/intelligence/ai/narrative/narrative_group.py
@src/core/ai/base_group_service.py
@src/core/ml/shadow.py
@src/core/ml/transform_recorder.py
@src/core/ai/base_ai_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub out SafeAgentWrapper with hard ImportError</name>
  <files>src/core/ai/safe_wrapper.py, tests/unit/test_safe_wrapper.py</files>
  <read_first>
    - src/core/ai/safe_wrapper.py (current content)
    - src/core/ai/base_ai_agent.py (verify `asyncio.wait_for` and neutral fallback exist on BaseAIAgent.compute())
    - run `grep -RIn 'SafeAgentWrapper\|from src.core.ai.safe_wrapper\|src.core.ai.safe_wrapper' src/ services/ tests/` BEFORE editing
  </read_first>
  <action>
    1. List all references: `grep -RIn 'SafeAgentWrapper\|from src.core.ai.safe_wrapper' src/ services/ tests/`.
    2. For any production import (under `src/` or `services/`): remove the import and any usage. `BaseAIAgent.compute()` already provides timeout + fallback.
    3. Replace the entire content of `src/core/ai/safe_wrapper.py` with:
       ```python
       """Stub: SafeAgentWrapper deleted in Phase 78 (D-18).

       BaseAIAgent.compute() already provides asyncio.wait_for + neutral fallback.
       Importing this module raises ImportError to surface any orphaned references loudly.
       """
       raise ImportError(
           "SafeAgentWrapper was deleted in Phase 78 (D-18). "
           "Use BaseAIAgent directly — it provides asyncio.wait_for + neutral fallback."
       )
       ```
    4. Replace `tests/unit/test_safe_wrapper.py` with a single regression test asserting `import src.core.ai.safe_wrapper` raises ImportError with the expected message.
    Per D-18.
  </action>
  <verify>
    <automated>! grep -RIn "from src.core.ai.safe_wrapper\|import SafeAgentWrapper" src/ services/ 2>/dev/null && .venv/bin/python -c "import src.core.ai.safe_wrapper" 2>&1 | grep -q "deleted in Phase 78" && .venv/bin/pytest tests/unit/test_safe_wrapper.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -RIn 'SafeAgentWrapper\|from src.core.ai.safe_wrapper' src/ services/` returns no production imports
    - `python -c "import src.core.ai.safe_wrapper"` raises ImportError with message containing "Phase 78"
    - `tests/unit/test_safe_wrapper.py` passes (single test asserts ImportError)
  </acceptance_criteria>
  <done>SafeAgentWrapper unreachable from production; loud failure on accidental re-import.</done>
</task>

<task type="auto">
  <name>Task 2: Remove NarrativeGroupComputeAgent._setup() override + archive ShadowRecorder/TransformRecorder</name>
  <files>src/intelligence/ai/narrative/narrative_group.py, src/core/ml/shadow.py, src/core/ml/transform_recorder.py, tests/unit/test_narrative_group.py</files>
  <read_first>
    - src/intelligence/ai/narrative/narrative_group.py (current _setup override; what it duplicates)
    - src/core/ai/base_group_service.py (verify _setup handles empty bar_topics correctly)
    - src/core/ml/shadow.py (current ShadowRecorder content)
    - src/core/ml/transform_recorder.py (current TransformRecorder content)
    - run `grep -RIn 'ShadowRecorder\|TransformRecorder' src/ services/` to confirm only test/historical references remain after Plan 01 Task 1
  </read_first>
  <action>
    1. Open `src/intelligence/ai/narrative/narrative_group.py`. Delete the `_setup()` method on `NarrativeGroupComputeAgent`. The class body must contain no `_setup` method — `BaseGroupService._setup()` is inherited unchanged. Delete any helper attributes that existed only to support the override.
    2. Add or update `tests/unit/test_narrative_group.py` to assert `'_setup' not in NarrativeGroupComputeAgent.__dict__` and that the class can be instantiated through the BaseGroupService contract.
    3. At the very top of `src/core/ml/shadow.py` (above any class definition), insert:
       ```python
       """ARCHIVED in Phase 78 (D-04). ShadowRecorder is preserved for historical reference only.

       After Phase 78 the swarm writes via src.core.ai.lineage.LineageRecorder. Do NOT import
       this module from production code. The class body below is retained verbatim so that
       historical migrations and tests can still inspect the old contract.
       """
       import warnings
       warnings.warn(
           "ShadowRecorder is archived (Phase 78 D-04). Use LineageRecorder.",
           DeprecationWarning,
           stacklevel=2,
       )
       ```
       Do not modify the class body.
    4. Apply the equivalent archival header to `src/core/ml/transform_recorder.py` (paraphrased for `TransformRecorder`).
    5. Run `grep -RIn 'from src.core.ml.shadow\|from src.core.ml.transform_recorder' src/ services/` — must return ZERO matches under `src/` and `services/`. If any production import remains, remove it.
    Per D-04, D-19.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_narrative_group.py -x -v && python -c "from src.intelligence.ai.narrative.narrative_group import NarrativeGroupComputeAgent; assert '_setup' not in NarrativeGroupComputeAgent.__dict__, 'NarrativeGroupComputeAgent._setup() override must be removed'" && grep -q "ARCHIVED in Phase 78" src/core/ml/shadow.py && grep -q "ARCHIVED in Phase 78" src/core/ml/transform_recorder.py && ! grep -RIn "from src.core.ml.shadow\|from src.core.ml.transform_recorder" src/ services/ 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "ARCHIVED in Phase 78" src/core/ml/shadow.py` succeeds
    - `grep -q "ARCHIVED in Phase 78" src/core/ml/transform_recorder.py` succeeds
    - `'_setup' not in NarrativeGroupComputeAgent.__dict__` (asserted by test)
    - `python -W error::DeprecationWarning -c "import src.core.ml.shadow"` raises (proves the warning fires)
    - Zero production imports of ShadowRecorder/TransformRecorder under `src/` or `services/`
  </acceptance_criteria>
  <done>Dead code paths removed or archived with loud signals against re-introduction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import-time → runtime | Removing dead code paths must not silently break a transitive import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-05 | Tampering | accidental re-import of ShadowRecorder by future PR | mitigate | DeprecationWarning header makes any new import noisy in test logs |
| T-78-06 | Repudiation | silent revival of SafeAgentWrapper | mitigate | replaced with hard ImportError; CI tests would fail on import |
| T-78-07 | Denial of service | NarrativeGroupComputeAgent without override fails to start | mitigate | unit test asserts inheritance from BaseGroupService and clean construction |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_narrative_group.py tests/unit/test_safe_wrapper.py -x` passes
- `python -c "import src.core.ai.safe_wrapper"` raises ImportError
- `grep -q "ARCHIVED in Phase 78" src/core/ml/shadow.py src/core/ml/transform_recorder.py` succeeds for both files
- `! grep -RIn "from src.core.ml.shadow\|from src.core.ml.transform_recorder" src/ services/` succeeds
- `.venv/bin/ruff check src/core/ai/safe_wrapper.py src/intelligence/ai/narrative/narrative_group.py src/core/ml/shadow.py src/core/ml/transform_recorder.py` exits 0
</verification>

<success_criteria>
- SafeAgentWrapper unreachable from production
- NarrativeGroupComputeAgent inherits `_setup` from BaseGroupService
- ShadowRecorder and TransformRecorder marked ARCHIVED with import-time DeprecationWarning
- No new production imports of any of the four targets
</success_criteria>

<output>
After completion, create `.planning/phases/078-i8-alpha-feedback-loop/078-02-SUMMARY.md`
</output>
