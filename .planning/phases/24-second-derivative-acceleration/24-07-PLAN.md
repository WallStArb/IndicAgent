---
phase: 24-second-derivative-acceleration
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/schemas.py
autonomous: true
gap_closure: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "market_analysis_service consumes from Redis indicator streams and publishes IntelligenceEvent messages"
    - "All IntelligenceEvent validations pass (no Pydantic ValidationError)"
    - "New intelligence_features rows contain hma_20, exhaustion_score, accel_regime, struct_energy, etc."
  artifacts:
    - path: "src/intelligence/schemas.py"
      provides: "I3Structure class with all 75 fields for 8 I3 plugins"
      contains: "swing_amplitude_ratio, swing_amplitude_expanding, swing_velocity_bars, swing_velocity_trend, struct_energy, struct_accel_bias"
  key_links:
    - from: "struct_SwingMomentum plugin (src/intelligence/structure/swing_momentum.py)"
      to: "I3Structure schema (src/intelligence/schemas.py)"
      via: "Pydantic validation in market_analysis_service.py"
      pattern: "IntelligenceEvent.*validation.*I3Structure"
---

<objective>
Fix I3Structure schema to include SwingMomentum plugin outputs, enabling market_analysis_service to consume and publish intelligence features.

Purpose: UAT test 5 failed because market_analysis_service was dropping all IntelligenceEvent messages due to Pydantic ValidationError (extra fields not permitted). The struct_SwingMomentum plugin (added in phase 24) outputs 6 fields not declared in the I3Structure schema, causing every validation to fail.

Output: I3Structure schema updated with 6 new fields, docstring updated to reflect 8 plugins/75 fields, market_analysis_service consuming and publishing successfully.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-second-derivative-acceleration/24-UAT.md
@.planning/debug/resolved/market-analysis-not-consuming.md
@.planning/phases/24-second-derivative-acceleration/24-04-SUMMARY.md

## Gap Context

**UAT Test 5 Failure:** market_analysis_service running but not consuming - 0 consumers in group, 0 new intelligence_features rows in 50 minutes since restart.

**Root Cause (from debug session):**
- I3Structure schema has `model_config = ConfigDict(extra="forbid")` (line 163 in schemas.py)
- struct_SwingMomentum plugin (added in 24-04) outputs 6 fields not declared in schema
- Every IntelligenceEvent validation fails with Pydantic ValidationError
- Line 453 in market_analysis_service.py drops malformed events: `return # Do NOT publish malformed events`

**Missing Fields:**
From struct_SwingMomentum plugin (src/intelligence/structure/swing_momentum.py):
1. `swing_amplitude_ratio: float` — ratio of current swing amplitude to average
2. `swing_amplitude_expanding: int` — 1 if expanding, 0 if contracting
3. `swing_velocity_bars: float` — bars per swing (velocity metric)
4. `swing_velocity_trend: Literal["accelerating", "decelerating", "stable"]` — velocity direction
5. `struct_energy: float` — normalized energy level (0-1)
6. `struct_accel_bias: Literal[-1, 0, 1]` — structural acceleration bias

**Pattern Reference:** All I3Structure fields follow pattern: `<field>: float | None = None` or `<field>: Literal[...] | None = None`. See lines 165-247 in schemas.py for examples.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 6 SwingMomentum fields to I3Structure schema</name>
  <files>src/intelligence/schemas.py</files>
  <action>
    Add the 6 missing fields to I3Structure class after line 247 (after FibonacciZonesPlugin outputs, before the class end):

    ```python
    # SwingMomentumPlugin outputs
    swing_amplitude_ratio: float | None = None
    swing_amplitude_expanding: int | None = None
    swing_velocity_bars: float | None = None
    swing_velocity_trend: Literal["accelerating", "decelerating", "stable"] | None = None
    struct_energy: float | None = None
    struct_accel_bias: Literal[-1, 0, 1] | None = None
    ```

    Update the docstring comment on lines 150-161 to reflect:
    - 8 I3 plugins (was 7) — add struct_SwingMomentum to the list
    - 75 fields total (was 69)
  </action>
  <verify>
    <manual>Restart market_analysis_service (sudo systemctl restart indicagent-market-analysis), wait 1 minute, check logs for "IntelligenceEvent validation failed" errors — should be zero. Verify consumer group has active consumers (redis-cli XINFO GROUPS indicators:ES:1m).</manual>
  </verify>
  <done>I3Structure class has 6 new fields declared, docstring updated to 8 plugins/75 fields</done>
</task>

</tasks>

<verification>
1. **Schema validation**: Manual verification via service restart and log check (no "IntelligenceEvent validation failed" errors)
2. **Service restart verification**: Restart market_analysis_service, check logs for "IntelligenceEvent validation failed" errors — should be zero
3. **Consumer group check**: Verify consumer group has active consumers after restart (`redis-cli XINFO GROUPS indicators:ES:1m`)
4. **Intelligence features flow**: Wait 2-3 minutes, query intelligence_features table for new rows with hma_20, exhaustion_score, accel_regime, struct_energy fields
</verification>

<success_criteria>
- I3Structure schema validates all 8 I3 plugin outputs without errors
- market_analysis_service consumes from indicator streams and publishes IntelligenceEvent messages
- New intelligence_features rows contain all phase 24 fields (hma_20, rsi_curvature, macd_hist_slope, price_accel, hma_slope, hma_accel, exhaustion_score, exhaustion_side, exhaustion_bars, accel_regime, accel_score, accel_agreement, struct_energy, struct_accel_bias, swing_amplitude_ratio, swing_amplitude_expanding, swing_velocity_bars, swing_velocity_trend)
- UAT test 5 passes
</success_criteria>

<output>
After completion, create `.planning/phases/24-second-derivative-acceleration/24-07-SUMMARY.md`
</output>
