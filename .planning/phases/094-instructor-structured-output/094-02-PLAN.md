---
phase: 094-instructor-structured-output
plan: 02
type: execute
wave: 2
depends_on:
  - 094-01
files_modified:
  - src/intelligence/ai/alpha/skeptic_agent.py
  - src/intelligence/ai/alpha/skeptic_prompts.py
autonomous: true
requirements:
  - STRUCT-OUT-01
  - STRUCT-OUT-04
must_haves:
  truths:
    - "SkepticResult Pydantic BaseModel defined in skeptic_agent.py (NOT in skeptic_prompts.py per locked decision)"
    - "SkepticResult has fields: failure_probability (Field ge=0 le=1), confidence (Field ge=0 le=1), risk_factors (list[str] default_factory=list), reasoning (Field max_length=500)"
    - "SkepticResult.risk_factors has @field_validator(mode='before') coercing non-list to single-element list"
    - "_validate_skeptic_fields function deleted from skeptic_prompts.py"
    - "output_schema: ClassVar[dict] removed from SkepticAgent class"
    - "skeptic_agent.py imports SkepticResult locally, ACTIVE_VERSION and build_skeptic_prompt from skeptic_prompts"
    - "skeptic_agent.py no longer imports _validate_skeptic_fields"
    - "skeptic_agent.py no longer calls self._parse_multiplier_response"
    - "skeptic_agent._compute uses self._llm_generate_structured(context, SkepticResult, ...) and reads result.failure_probability / result.confidence / result.risk_factors / result.reasoning"
    - "skeptic_agent.py no longer calls self._report_parse_failure (Instructor handles retry internally)"
    - "Existing skeptic unit tests pass (test_skeptic_agent.py updated for new API if needed)"
  artifacts:
    - path: "src/intelligence/ai/alpha/skeptic_agent.py"
      provides: "SkepticAgent using _llm_generate_structured; SkepticResult model defined in this file; output_schema ClassVar removed; _parse_multiplier_response/validate_skeptic_fields refs removed"
    - path: "src/intelligence/ai/alpha/skeptic_prompts.py"
      provides: "_validate_skeptic_fields removed; only prompt strings + ACTIVE_VERSION + PROMPT_REGISTRY + build_skeptic_prompt remain"
  key_links:
    - from: "src/intelligence/ai/alpha/skeptic_agent.py SkepticAgent._compute"
      to: "BaseAIAgent._llm_generate_structured"
      via: "await self._llm_generate_structured(context, SkepticResult, prompt=prompt, system=_SYSTEM_MESSAGE, max_tokens=500, timeout=self.latency_budget_ms / 1000.0)"
      pattern: "_llm_generate_structured\\(.*SkepticResult"
---

<objective>
Migrate `SkepticAgent` from the manual parse-and-validate boilerplate to `_llm_generate_structured`. Define `SkepticResult` Pydantic model in the agent file (not the prompts file, per locked decision: result models = data contracts = consumer-co-located). Delete `_validate_skeptic_fields` from prompts file. Remove `output_schema: ClassVar[dict]`.

**Locked decisions honored:**
- Result model in agent file, NOT prompts file
- Use `_llm_generate_structured` from Plan 01 (no agent-level parse retry)
- All deferred (search, etc.) and out-of-scope (ml_scorer) NOT touched
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/094-instructor-structured-output/094-RESEARCH.md
@docs/plans/2026-05-20-phase2-instructor.md
@.planning/phases/094-instructor-structured-output/094-01-PLAN.md
</context>

<read_first>
- `src/intelligence/ai/alpha/skeptic_agent.py` — full file; identify _compute method body, output_schema ClassVar, imports list, call to _parse_multiplier_response, _build_multiplier_output call site
- `src/intelligence/ai/alpha/skeptic_prompts.py` — full file; identify _validate_skeptic_fields function (lines ~103-136 per research), __all__ if present, ACTIVE_VERSION location
- `src/core/ai/base_agent.py` — confirm _llm_generate_structured signature (from Plan 01): `(context, result_type, prompt, system, max_tokens, timeout, max_retries=3) -> _T | None`
- `src/core/ai/multiplier_agent.py` — confirm _build_multiplier_output signature; do NOT modify (Plan 05 deletes _parse_multiplier_response from this file)
- `tests/unit/ai_agent_tests/test_skeptic_agent.py` (if exists) — identify mocks/assertions that depend on the old API
</read_first>

<tasks>

<task id="1" name="Add SkepticResult model to skeptic_agent.py and delete _validate_skeptic_fields from prompts">
<action>
1. In `src/intelligence/ai/alpha/skeptic_agent.py`, add the SkepticResult class after existing imports but before the SkepticAgent class. Add these imports if not already present:
   ```python
   from pydantic import BaseModel, Field, field_validator
   ```
   Then add:
   ```python
   class SkepticResult(BaseModel):
       failure_probability: float = Field(ge=0.0, le=1.0)
       confidence: float = Field(ge=0.0, le=1.0)
       risk_factors: list[str] = Field(default_factory=list)
       reasoning: str = Field(default="", max_length=500)

       @field_validator("risk_factors", mode="before")
       @classmethod
       def coerce_to_list(cls, v: object) -> list[str]:
           if v is None:
               return []
           if not isinstance(v, list):
               return [str(v)]
           return [str(x) for x in v]
   ```

2. In `src/intelligence/ai/alpha/skeptic_prompts.py`:
   - Delete the entire `_validate_skeptic_fields` function (typically lines ~103-136)
   - Remove `_validate_skeptic_fields` from `__all__` if present
   - Remove any now-unused imports (e.g. `clamp` from `prompt_utils`) ONLY if no other code in this file uses them — grep first to verify

3. In `src/intelligence/ai/alpha/skeptic_agent.py`:
   - Remove `_validate_skeptic_fields` from the existing import line (e.g. change `from src.intelligence.ai.alpha.skeptic_prompts import (ACTIVE_VERSION, _validate_skeptic_fields, build_skeptic_prompt,)` to `from src.intelligence.ai.alpha.skeptic_prompts import (ACTIVE_VERSION, build_skeptic_prompt,)`)
</action>
<acceptance_criteria>
- `grep -c 'class SkepticResult' src/intelligence/ai/alpha/skeptic_agent.py` returns 1
- `grep -c 'class SkepticResult' src/intelligence/ai/alpha/skeptic_prompts.py` returns 0 (locked: model in agent, not prompts)
- `grep '_validate_skeptic_fields' src/intelligence/ai/alpha/skeptic_prompts.py` returns 0 matches
- `grep '_validate_skeptic_fields' src/intelligence/ai/alpha/skeptic_agent.py` returns 0 matches
- `.venv/bin/python -c "from src.intelligence.ai.alpha.skeptic_agent import SkepticResult; r = SkepticResult(failure_probability=0.5, confidence=0.8); print(r.risk_factors)"` prints `[]`
- `.venv/bin/python -c "from src.intelligence.ai.alpha.skeptic_agent import SkepticResult; r = SkepticResult(failure_probability=0.5, confidence=0.8, risk_factors='single_str'); print(r.risk_factors)"` prints `['single_str']` (coercion works)
</acceptance_criteria>
</task>

<task id="2" name="Migrate SkepticAgent._compute to _llm_generate_structured">
<action>
In `src/intelligence/ai/alpha/skeptic_agent.py`:

1. Remove `output_schema: ClassVar[dict] = {...}` from the SkepticAgent class. If `ClassVar` import is no longer needed, remove it.

2. In `_compute`, replace the block from `response, call_id = await self._llm_generate(...)` through the JSON parse/validate block. New code:
   ```python
   result = await self._llm_generate_structured(
       context,
       SkepticResult,
       prompt=prompt,
       system=_SYSTEM_MESSAGE,
       max_tokens=500,
       timeout=self.latency_budget_ms / 1000.0,
   )
   if result is None:
       return self._neutral(error="Structured parse failed", latency_ms=0.0)

   failure_probability = result.failure_probability
   llm_confidence = result.confidence
   ```

3. Update the `_build_multiplier_output` call to use `result.risk_factors` and `result.reasoning` (whatever fields the existing payload dict uses). Keep all other logic (multiplier computation, prompt_version, etc.) identical.

4. Remove the now-orphaned `await self._report_parse_failure(call_id)` call (Instructor handles retries; no call_id from structured path).

5. Confirm no remaining references to the deleted code:
   - `grep -n '_parse_multiplier_response\|_validate_skeptic_fields\|_report_parse_failure\|output_schema' src/intelligence/ai/alpha/skeptic_agent.py` — must return 0 matches.

6. Run skeptic unit tests: `.venv/bin/pytest tests/unit/ai_agent_tests/ -v -k "skeptic" 2>&1 | tail -20`. If tests reference the old API (e.g. mock `_parse_multiplier_response`), update them to mock `_llm_generate_structured` returning a `SkepticResult` instance. Keep test assertions on agent OUTPUT (multiplier, confidence) unchanged where possible.

7. Run full unit suite: `.venv/bin/pytest tests/unit/ -q --tb=line 2>&1 | tail -5`. No new failures vs baseline.
</action>
<acceptance_criteria>
- `grep 'output_schema' src/intelligence/ai/alpha/skeptic_agent.py` returns 0 matches
- `grep '_parse_multiplier_response' src/intelligence/ai/alpha/skeptic_agent.py` returns 0 matches
- `grep '_report_parse_failure' src/intelligence/ai/alpha/skeptic_agent.py` returns 0 matches
- `grep '_llm_generate_structured' src/intelligence/ai/alpha/skeptic_agent.py` returns at least 1 match
- `grep 'SkepticResult' src/intelligence/ai/alpha/skeptic_agent.py` returns at least 2 matches (class def + usage in _compute)
- `.venv/bin/pytest tests/unit/ai_agent_tests/ -v -k "skeptic" 2>&1 | tail -5` shows all skeptic tests PASSED (0 failures)
- `.venv/bin/pytest tests/unit/ -q --tb=line 2>&1 | tail -3` shows no NEW failures vs pre-task baseline
- `.venv/bin/python -c "from src.intelligence.ai.alpha.skeptic_agent import SkepticAgent; a = SkepticAgent; assert not hasattr(a, 'output_schema'); print('ok')"` prints `ok`
</acceptance_criteria>
</task>

</tasks>

<verification>
Plan 02 complete when:
1. SkepticResult Pydantic model defined in skeptic_agent.py (NOT prompts file)
2. _validate_skeptic_fields deleted from skeptic_prompts.py
3. SkepticAgent uses _llm_generate_structured; output_schema ClassVar removed
4. No references to _parse_multiplier_response / _report_parse_failure remain in skeptic_agent.py
5. Skeptic unit tests pass; full unit suite has no new failures
</verification>

<success_criteria>
- STRUCT-OUT-01: SkepticAgent JSON parsing routed through Instructor via `_llm_generate_structured`
- STRUCT-OUT-04: SkepticResult declared; `_validate_skeptic_fields` deleted; `output_schema: ClassVar[dict]` deleted
</success_criteria>

<output>
After completion, create `.planning/phases/094-instructor-structured-output/094-02-SUMMARY.md`
</output>
