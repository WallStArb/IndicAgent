---
phase: 124
plan: 01
type: execute
wave: 1
requirements: [QUALITY-02]
depends_on: []
files_modified:
  - production/migrations/130_promote_ctf_columns.sql
  - services/feature_writer.py
  - src/core/ml/training_data.py
  - src/core/memory/embedding.py
  - production/scripts/run_historical_pipeline.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - CTF columns (ctf_score, ctf_trend_alignment, ctf_structure_alignment, ctf_regime_agreement) exist as top-level nullable columns on intelligence_features
    - cross_timeframe_context JSONB no longer contains CTF keys (single source of truth)
    - feature_writer ON CONFLICT guard uses WHERE ctf_score IS NULL only (never IS NULL OR = 0.0)
    - CTF readers read from top-level columns, not JSONB
    - run_historical_pipeline.py --warmup flag runs two passes (I1-I6 warmup, then I1-I7 signals)
  artifacts:
    - path: production/migrations/130_promote_ctf_columns.sql
      provides: DDL for 4 CTF columns, backfill from JSONB, key strip
    - path: services/feature_writer.py
      contains: ON CONFLICT DO UPDATE ... WHERE intelligence_features.ctf_score IS NULL
    - path: src/core/ml/training_data.py
      exports: SQL reading f.ctf_score (not JSONB)
    - path: src/core/memory/embedding.py
      contains: Direct ctf_score read from context
    - path: production/scripts/run_historical_pipeline.py
      contains: --warmup argument
  key_links:
    - from: services/feature_writer.py
      to: intelligence_features.ctf_score
      via: ON CONFLICT DO UPDATE guard
      pattern: WHERE intelligence_features.ctf_score IS NULL
    - from: production/scripts/run_historical_pipeline.py
      to: intelligence_features
      via: Two-pass replay with skip_signals=True then False
      pattern: --warmup
---

<objective>
Wave A Foundation: CTF Column Promotion + Cold-Start Guard + Warmup Flag

Purpose: Establish the infrastructure for cold-start correction before behavioral plugin changes. Promote 4 CTF sub-scores from JSONB to top-level columns, wire the IS NULL-only guard in feature_writer, migrate CTF readers, and add the --warmup flag for historical replay.

PRE-EXECUTION CHECK: Confirm lifecycle_replay.py has completed before running the backfill UPDATE in migration 130. The DDL (ALTER TABLE ADD COLUMN IF NOT EXISTS) is safe to run against a live table, but the backfill UPDATE (step 2) races with any active replay inserts. Run: `ps aux | grep lifecycle_replay` — if the process is still running, wait for it to finish or stop it first. The migration uses `WHERE ctf_score IS NULL` so it is safe to re-run after replay completes.

NOTE: `skip_signals` already exists in run_historical_pipeline.py at line 1476. The --warmup flag only needs to wire a two-pass call (skip_signals=True then False) — no new infrastructure required.

Output:
- Migration 130 (4 CTF columns + backfill + JSONB strip)
- feature_writer.py with IS NULL-only ON CONFLICT guard
- CTF reader migration (training_data.py, embedding.py)
- run_historical_pipeline.py --warmup flag (wires existing skip_signals=True/False two-pass)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-signal-universe-integrity-cold-start-hardening/124-CONTEXT.md
@.planning/phases/124-signal-universe-integrity-cold-start-hardening/124-RESEARCH.md
@.planning/phases/124-signal-universe-integrity-cold-start-hardening/124-PATTERNS.md
@.planning/phases/124-signal-universe-integrity-cold-start-hardening/124-VALIDATION.md
@.planning/phases/123-ecl-boundary-restoration/123-CONTEXT.md
@production/migrations/124_add_i2_column.sql
@services/feature_writer.py
@src/intelligence/confluence/cross_timeframe.py
@src/core/ml/training_data.py
@src/core/memory/embedding.py
@production/scripts/run_historical_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 130 (promote 4 CTF columns + backfill + JSONB strip)</name>
  <files>production/migrations/130_promote_ctf_columns.sql</files>
  <read_first>
    production/migrations/124_add_i2_column.sql (ADD COLUMN IF NOT EXISTS pattern, backfill pattern, JSONB strip pattern)
  </read_first>
  <action>
Create production/migrations/130_promote_ctf_columns.sql with three statements:

1. Add 4 nullable columns: ctf_score, ctf_trend_alignment, ctf_structure_alignment, ctf_regime_agreement (all double precision, no default, ADD COLUMN IF NOT EXISTS per Phase 122 pattern)

2. Backfill from JSONB using NULLIF pattern to handle null/missing/empty-string:
   UPDATE intelligence_features
   SET ctf_score = NULLIF(cross_timeframe_context->>'ctf_score', '')::double precision,
       ctf_trend_alignment = NULLIF(cross_timeframe_context->>'ctf_trend_alignment', '')::double precision,
       ctf_structure_alignment = NULLIF(cross_timeframe_context->>'ctf_structure_alignment', '')::double precision,
       ctf_regime_agreement = NULLIF(cross_timeframe_context->>'ctf_regime_agreement', '')::double precision
   WHERE ctf_score IS NULL
     AND cross_timeframe_context ? 'ctf_score';

3. Strip 4 CTF keys from cross_timeframe_context JSONB using - operator:
   UPDATE intelligence_features
   SET cross_timeframe_context = cross_timeframe_context - ARRAY['ctf_score', 'ctf_trend_alignment', 'ctf_structure_alignment', 'ctf_regime_agreement']
   WHERE cross_timeframe_context ? 'ctf_score';

Add comment header with date, description, backfill note, and rollout order comment per migration conventions.
  </action>
  <verify>
ls production/migrations/130_promote_ctf_columns.sql exists and contains ALTER TABLE intelligence_features ADD COLUMN IF NOT EXISTS ctf_score, ctf_trend_alignment, ctf_structure_alignment, ctf_regime_agreement
  </verify>
  <done>
Migration file created with 4 nullable columns, NULLIF backfill handling null/missing/empty-string, and JSONB key strip for single source of truth
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire CTF columns in feature_writer.py (INSERT params + ON CONFLICT guard)</name>
  <files>services/feature_writer.py</files>
  <read_first>
    services/feature_writer.py (lines 63-220 for _INSERT_FEATURE_SQL and _record_to_insert_params)
    src/intelligence/schemas.py (BarIntelligenceRecord I6Confluence structure)
    src/intelligence/confluence/cross_timeframe.py (lines 132-136 for I6 CTF output dict)
  </read_first>
  <action>
Modify services/feature_writer.py:

1. Add 4 CTF columns to _INSERT_FEATURE_SQL column list after feature_schema_version:
   ctf_score, ctf_trend_alignment, ctf_structure_alignment, ctf_regime_agreement

2. Add 4 VALUES placeholders ($34, $35, $36, $37) after $33 in the VALUES clause

3. Change ON CONFLICT from DO NOTHING to DO UPDATE SET with IS NULL-only guard:
   ON CONFLICT (ts, symbol, tf)
   DO UPDATE SET
       ctf_score = EXCLUDED.ctf_score,
       ctf_trend_alignment = EXCLUDED.ctf_trend_alignment,
       ctf_structure_alignment = EXCLUDED.ctf_structure_alignment,
       ctf_regime_agreement = EXCLUDED.ctf_regime_agreement
   WHERE intelligence_features.ctf_score IS NULL

4. In _record_to_insert_params, extract CTF values from event.i6.model_dump():
   i6_dict = event.i6.model_dump(exclude_none=True)
   ctf_score = i6_dict.get('ctf_score')
   ctf_trend_alignment = i6_dict.get('ctf_trend_alignment')
   ctf_structure_alignment = i6_dict.get('ctf_structure_alignment')
   ctf_regime_agreement = i6_dict.get('ctf_regime_agreement')

5. Append ctf_score, ctf_trend_alignment, ctf_structure_alignment, ctf_regime_agreement to the return tuple (becomes $34-$37)
  </action>
  <verify>
grep -n "WHERE intelligence_features.ctf_score IS NULL" services/feature_writer.py returns line with ON CONFLICT guard; grep -n "ctf_score, ctf_trend_alignment, ctf_structure_alignment, ctf_regime_agreement" services/feature_writer.py returns INSERT column list and return tuple entries
  </verify>
  <done>
feature_writer.py INSERT includes 4 CTF columns and ON CONFLICT DO UPDATE with WHERE ctf_score IS NULL guard (Phase 123 semantics)
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate CTF readers from JSONB to top-level columns</name>
  <files>src/core/ml/training_data.py, src/core/memory/embedding.py</files>
  <read_first>
    src/core/ml/training_data.py (lines 38-40 for current JSONB CTF reads)
    src/core/memory/embedding.py (lines 139-143 for current ctf_composite fallback)
  </read_first>
  <action>
For src/core/ml/training_data.py:
1. Change (f.cross_timeframe_context->>'ctf_score')::float to f.ctf_score
2. Change (f.cross_timeframe_context->>'ctf_trend_alignment')::float to f.ctf_trend_alignment
3. Add f.ctf_structure_alignment AS ctf_structure_alignment (new column)
4. Change (f.cross_timeframe_context->>'ctf_regime_agreement')::float to f.ctf_regime_agreement

For src/core/memory/embedding.py:
1. Remove the ctf_composite fallback line (getattr(context, "ctf_composite", None))
2. Direct read: ctf_score = getattr(context, "ctf_score", None)
3. Ensure context object has CTF fields populated from top-level columns via I6Confluence
  </action>
  <verify>
grep -rn "cross_timeframe_context->>'ctf_" src/core/ml/training_data.py src/core/memory/embedding.py returns zero hits; grep -n "f.ctf_score" src/core/ml/training_data.py returns line with column read
  </verify>
  <done>
CTF readers read from top-level columns (training_data.py, embedding.py); zero JSONB CTF key reads remain in src/
  </done>
</task>

<task type="auto">
  <name>Task 4: Add --warmup flag to run_historical_pipeline.py</name>
  <files>production/scripts/run_historical_pipeline.py</files>
  <read_first>
    production/scripts/run_historical_pipeline.py (lines 1471-1623 for replay_symbol signature and skip_docs parameter; main() parser)
  </read_first>
  <action>
Modify production/scripts/run_historical_pipeline.py:

1. In main() argument parser, add --warmup flag:
   parser.add_argument(
       "--warmup",
       action="store_true",
       help="Run I1-I6 warmup pass before signal pass (populates I6 cache for cold-start correction)",
   )

2. Add validation: if args.warmup and not args.replay_only: print error and sys.exit(1)

3. When --warmup is set, orchestrate two-pass execution:
   a) First pass: call run_pipeline() with skip_signals=True (warmup pass - I1-I6 only)
   b) Second pass: call run_pipeline() with skip_signals=False (signal pass - I1-I7 with warm I6 cache)

4. Add print statements to delineate passes: "Running warmup pass (I1-I6 only)..." and "Warmup complete. Running signal pass (I1-I7 with warm I6 cache)..."

The skip_signals parameter already exists in replay_symbol() - this task only adds CLI orchestration.
  </action>
  <verify>
grep -n "add_argument.*--warmup" production/scripts/run_historical_pipeline.py returns argument definition; grep -n "skip_signals=True" production/scripts/run_historical_pipeline.py shows warmup pass call
  </verify>
  <done>
run_historical_pipeline.py accepts --warmup flag; when set, runs two passes (I1-I6 warmup, then I1-I7 signals)
  </done>
</task>

<task type="auto">
  <name>Task 5: Unit tests for migration backfill + ON CONFLICT guard</name>
  <files>tests/unit/test_124_ctf_column_promotion.py</files>
  <read_first>
    tests/unit/intelligence/test_i7_extrinsic_contract.py (Phase 123 test patterns for None-vs-0.0 semantics)
    src/intelligence/schemas.py (BarIntelligenceRecord I6Confluence)
  </read_first>
  <action>
Create tests/unit/test_124_ctf_column_promotion.py with three test cases:

1. test_migration_backfill_nullif_handling: Mock SQL connection, execute migration statements, verify NULLIF converts empty string to NULL, missing key is skipped (not set to NULL)

2. test_on_conflict_is_null_guard: Create BarIntelligenceRecord with ctf_score=0.5, insert row, then re-insert with ctf_score=0.7 - verify row updated to 0.7. Create second row with ctf_score=0.0, re-insert with ctf_score=0.8 - verify row NOT updated (0.0 preserved, Phase 123 semantics)

3. test_warmup_two_pass_execution: Mock run_pipeline call with --warmup flag, verify called twice (first skip_signals=True, second skip_signals=False)
  </action>
  <verify>
pytest tests/unit/test_124_ctf_column_promotion.py -q exits 0; all three tests pass
  </verify>
  <done>
Unit tests verify migration NULLIF handling, ON CONFLICT IS NULL guard preserves genuine 0.0 neutral, and --warmup orchestrates two passes
  </done>
</task>

</tasks>

<verification>
After all tasks complete, run:
1. pytest tests/unit/ -q (all tests green)
2. grep -rn "cross_timeframe_context->>'ctf_" src/ returns zero hits (no JSONB CTF reads)
3. grep -n "WHERE intelligence_features.ctf_score IS NULL" services/feature_writer.py returns ON CONFLICT guard line
4. grep -n "OR.*ctf_score.*=.*0\.0" services/feature_writer.py returns zero hits (no IS NULL OR = 0.0 antipattern)
5. psql -c "SELECT COUNT(*) FROM intelligence_features WHERE ctf_score IS NULL AND cross_timeframe_context ? 'ctf_score'" returns 0 (migration backfill worked, no stale JSONB references)
</verification>

<success_criteria>
1. Migration 130 creates 4 nullable CTF columns and strips JSONB keys
2. feature_writer ON CONFLICT guard uses IS NULL only (Phase 123 semantics)
3. All CTF readers read from top-level columns (grep-zero gate)
4. --warmup flag orchestrates two-pass replay (I1-I6 warmup, then I1-I7 signals)
5. Unit tests pass (pytest green)
</success_criteria>

<output>
After completion, create `.planning/phases/124-signal-universe-integrity-cold-start-hardening/124-01-SUMMARY.md`
</output>

<threat_model>
**ASVS Level 1 Threat Model for Wave A**

High severity threats:
1. **Migration data loss (HIGH):** Backfill with NULLIF but missing key check - if UPDATE omits AND cross_timeframe_context ? 'ctf_score', rows with missing key get NULL instead of preserving NULL state. MITIGATION: Migration includes key existence guard; verification query confirms zero rows with NULL column and key present.
2. **ON CONFLICT overwrites genuine neutral (CRITICAL):** If guard uses IS NULL OR = 0.0, genuine neutral readings (0.0 from I6 computation) are overwritten during --warmup pass, contaminating ML training data with incorrect corrections. MITIGATION: Guard is WHERE ctf_score IS NULL only; unit test proves 0.0 preservation; Phase 123 semantics locked.
3. **JSONB dual source of truth (HIGH):** If strip fails, CTF keys exist in both columns and JSONB - subsequent code may read stale JSONB values. MITIGATION: Migration strips keys in same transaction; grep-zero gate verifies no readers remain; single source of truth enforced.
4. **Cold-start replay incomplete (MEDIUM):** If --warmup only runs I1-I6 but skip_signals path has bugs, I6 cache may remain cold in second pass. MITIGATION: skip_signals parameter already exists and is tested; two-pass orchestration is additive.

Opt-out: None - infrastructure hardening is mandatory for ML training data integrity.
</threat_model>
