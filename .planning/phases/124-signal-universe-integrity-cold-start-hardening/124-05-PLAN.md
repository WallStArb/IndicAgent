---
phase: 124
plan: 05
type: execute
wave: 2
requirements: [QUALITY-01]
depends_on: [124-01]
files_modified:
  - src/intelligence/trading/liquidity_sweep_reclaim.py
  - tests/unit/test_liquidity_sweep_reclaim.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - liquidity_sweep_reclaim trigger fires only on rising edge of sweep_reclaimed (transition 0->1)
    - Close-above swept level with acceptance required (not wick-only reclaim)
    - Persistent sweep_reclaimed flag (stays hot) produces no signal without rising edge
    - deduplicate_event with (sweep_level, sweep_type) prevents re-fire on same level
  artifacts:
    - path: src/intelligence/trading/liquidity_sweep_reclaim.py
      contains: Rising-edge detection via onset_guard, close-above acceptance check, deduplicate_event with structural anchor
    - path: tests/unit/test_liquidity_sweep_reclaim.py
      contains: Synthetic frame tests (flag-stays-hot -> no fire, close-above reclaim -> fire once, wick-only -> no fire)
  key_links:
    - from: src/intelligence/trading/liquidity_sweep_reclaim.py
      to: src/intelligence/trading/state_utils.py
      via: onset_guard for rising edge
      pattern: sweep_reclaimed transition 0->1 detection
---

<objective>
Wave B Plugin 4/5: LiquiditySweepReclaim Structural Rewrite

Purpose: Fix LiquiditySweepReclaim to fire only on the rising edge of sweep_reclaimed (transition from 0 to 1) with close-above acceptance (not wick-only). The persistent flag staying hot is not a trigger; the event is the reclaim transition. Add structural specificity: close must be above swept level with body acceptance.

Output:
- liquidity_sweep_reclaim.py with rising-edge detection (onset_guard on sweep_reclaimed)
- Close-above acceptance check (close > sweep_level, not just high wick)
- deduplicate_event with (sweep_level, sweep_type) for re-arm on re-sweeps
- Synthetic frame unit tests
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-signal-universe-integrity-cold-start-hardening/124-CONTEXT.md
@.planning/phases/124-signal-universe-integrity-cold-start-hardening/124-RESEARCH.md
@.planning/phases/124-signal-universe-integrity-cold-start-hardening/124-PATTERNS.md
@.planning/phases/124-signal-universe-integrity-cold-start-hardening/124-VALIDATION.md
@src/intelligence/trading/liquidity_sweep_reclaim.py (current compute_full lines 54-92)
@src/intelligence/trading/state_utils.py (onset_guard, deduplicate_event)
@src/intelligence/trading/atr_utils.py (get_atr_with_floor_from_frames)
@src/intelligence/trading/confidence_utils.py (compose_confidence)
@src/intelligence/trading/squeeze_expansion.py (reference rising-edge pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement rising-edge detection for sweep_reclaimed</name>
  <files>src/intelligence/trading/liquidity_sweep_reclaim.py</files>
  <read_first>
    src/intelligence/trading/liquidity_sweep_reclaim.py (current compute_full, lines 54-92)
    src/intelligence/trading/state_utils.py (onset_guard reference lines 78-85)
  </read_first>
  <action>
Modify src/intelligence/trading/liquidity_sweep_reclaim.py compute_full():

1. Keep sweep_detected gate (sweep_detected == 1.0 must be true) - return no_signal() if false

2. Replace sweep_reclaimed == 1.0 flag check with rising-edge detection:
   - sweep_reclaimed = features.get("sweep_reclaimed", 0.0)
   - reclaim_rising_edge = onset_guard(self._state, f"{state_key}_reclaim", sweep_reclaimed == 1.0)
   - If reclaim_rising_edge is False: return no_signal()

3. Comment: "# Rising-edge gate: fire only on sweep_reclaimed transition 0->1 (flag staying hot is not trigger)"

4. Keep sweep_type and sweep_level extraction (existing logic)
  </action>
  <verify>
grep -n "onset_guard.*reclaim\|rising.*edge" src/intelligence/trading/liquidity_sweep_reclaim.py returns rising-edge detection; grep -n "sweep_reclaimed.*==.*1\.0" src/intelligence/trading/liquidity_sweep_reclaim.py returns zero hits as direct trigger (should be in onset_guard call)
  </verify>
  <done>
Rising-edge detection: onset_guard on sweep_reclaimed == 1.0 transition (0->1)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add close-above acceptance check (not wick-only)</name>
  <files>src/intelligence/trading/liquidity_sweep_reclaim.py</files>
  <read_first>
    src/intelligence/trading/liquidity_sweep_reclaim.py (compute_full after Task 1)
  </read_first>
  <action>
Modify src/intelligence/trading/liquidity_sweep_reclaim.py compute_full():

1. After rising-edge gate confirmed, add close-above acceptance:
   - sweep_level = features.get("sweep_level", 0.0)
   - close = float(close[-1])
   - For bullish reclaim (sweep_type > 0): close must be > sweep_level (body above level, not just wick)
   - For bearish reclaim (sweep_type < 0): close must be < sweep_level (body below level)

2. If close NOT above/below sweep_level: return no_signal()

3. Comment: "# Structural specificity: close must reclaim level with body acceptance (wick-only is noise)"

4. Keep atr calculation and trade frame logic (existing)
  </action>
  <verify>
grep -n "close.*sweep_level\|body.*acceptance\|wick.*only" src/intelligence/trading/liquidity_sweep_reclaim.py returns close-above check logic
  </verify>
  <done>
Close-above acceptance: close > sweep_level (bullish) or close < sweep_level (bearish); wick-only reclaims rejected
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify deduplicate_event with structural anchor</name>
  <files>src/intelligence/trading/liquidity_sweep_reclaim.py</files>
  <read_first>
    src/intelligence/trading/liquidity_sweep_reclaim.py (current deduplicate_event usage, lines 541-543)
    src/intelligence/trading/state_utils.py (deduplicate_event reference)
  </read_first>
  <action>
Verify src/intelligence/trading/liquidity_sweep_reclaim.py compute_full():

1. Check existing deduplicate_event call (already present):
   event_id = (round(float(sweep_level), 4), int(sweep_type))
   if not deduplicate_event(self._state, state_key, event_id): return no_signal()

2. If NOT present, add it before make_signal_from_frame:
   event_id = (round(float(sweep_level), 4), int(sweep_type))
   if not deduplicate_event(self._state, state_key, event_id): return no_signal()

3. Comment: "# deduplicate_event by (sweep_level, sweep_type): allows re-arm on re-sweeps of same level"

4. Verify min_bars_between_fires is default (allows re-sweep after min bars)
  </action>
  <verify>
grep -n "deduplicate_event.*sweep_level" src/intelligence/trading/liquidity_sweep_reclaim.py returns deduplicate_event call with (sweep_level, sweep_type) tuple
  </verify>
  <done>
deduplicate_event with (sweep_level, sweep_type) event_id prevents re-fire on same level; allows re-arm on re-sweeps
  </done>
</task>

<task type="auto">
  <name>Task 4: Reorder gates (rising edge first, close-above acceptance second)</name>
  <files>src/intelligence/trading/liquidity_sweep_reclaim.py</files>
  <read_first>
    src/intelligence/trading/liquidity_sweep_reclaim.py (current gate ordering lines 78-89)
    src/intelligence/trading/squeeze_expansion.py (lines 72-99 reference)
  </read_first>
  <action>
Reorder src/intelligence/trading/liquidity_sweep_reclaim.py compute_full gates:

1. FIRST: Check sweep_detected == 1.0 - return no_signal() if false
2. SECOND: Check rising-edge of sweep_reclaimed (onset_guard transition 0->1) - return no_signal() if false
3. THIRD: Check sweep_type != 0.0 - return no_signal() if false
4. FOURTH: Check close-above acceptance (close > sweep_level or < sweep_level) - return no_signal() if false
5. FIFTH: Extract OHLCV and compute trade frame (existing logic)
6. SIXTH: Apply deduplicate_event with (sweep_level, sweep_type)
7. SEVENTH: Emit signal

Add comments: "# Sweep existence FIRST", "# Rising-edge SECOND", "# Type check THIRD", "# Close-above acceptance FOURTH"
  </action>
  <verify>
grep -A3 "Rising-edge SECOND" src/intelligence/trading/liquidity_sweep_reclaim.py shows onset_guard on sweep_reclaimed; grep -A3 "Close-above acceptance FOURTH" src/intelligence/trading/liquidity_sweep_reclaim.py shows close vs sweep_level check
  </verify>
  <done>
Gate ordering: sweep_detected -> rising-edge -> sweep_type -> close-above acceptance -> OHLCV -> deduplicate_event -> emit
  </done>
</task>

<task type="auto">
  <name>Task 5: Unit tests for LiquiditySweepReclaim structural rewrite</name>
  <files>tests/unit/test_liquidity_sweep_reclaim.py</files>
  <read_first>
    tests/unit/test_liquidity_sweep_reclaim.py (existing tests if any)
    src/intelligence/trading/liquidity_sweep_reclaim.py (compute_full signature)
  </read_first>
  <action>
Add to tests/unit/test_liquidity_sweep_reclaim.py four test cases:

1. test_flag_stays_hot_no_signal: Create synthetic frames with:
   - sweep_detected = 1.0, sweep_reclaimed = 1.0 (flag stays hot for 10 bars)
   - No rising edge (already reclaimed)
   Verify plugin returns no_signal() for all 10 bars (flag persistence is not trigger)

2. test_rising_edge_fires_once: Create synthetic frames with:
   - Bars 1-5: sweep_detected = 1.0, sweep_reclaimed = 0.0
   - Bar 6: sweep_reclaimed = 1.0 (rising edge), sweep_level = 4200.0, close = 4205.0
   Verify plugin fires on bar 6 (direction=1), not on bars 1-5

3. test_wick_only_no_fire: Create synthetic frames with:
   - sweep_detected = 1.0, sweep_reclaimed = 1.0 (rising edge)
   - sweep_level = 4200.0, high = 4210.0, close = 4195.0 (wick reclaimed but close below)
   Verify plugin returns no_signal() (wick-only is noise)

4. test_deduplicate_prevents_refire: After bar 6 fires, bar 7 has same sweep_level/sweep_type - verify plugin returns no_signal() (deduplicate_event)
  </action>
  <verify>
pytest tests/unit/test_liquidity_sweep_reclaim.py::test_flag_stays_hot_no_signal -q exits 0; pytest tests/unit/test_liquidity_sweep_reclaim.py::test_rising_edge_fires_once -q exits 0; pytest tests/unit/test_liquidity_sweep_reclaim.py::test_wick_only_no_fire -q exits 0; pytest tests/unit/test_liquidity_sweep_reclaim.py::test_deduplicate_prevents_refire -q exits 0
  </verify>
  <done>
Unit tests prove: flag-stays-hot produces no signal, rising edge fires once, wick-only rejected, deduplicate prevents re-fire
  </done>
</task>

</tasks>

<verification>
After all tasks complete, run:
1. pytest tests/unit/test_liquidity_sweep_reclaim.py -q (all tests green)
2. grep -n "sweep_reclaimed.*==.*1\.0" src/intelligence/trading/liquidity_sweep_reclaim.py returns zero hits as direct gate (should be in onset_guard call only)
3. grep -n "onset_guard.*reclaim\|rising.*edge" src/intelligence/trading/liquidity_sweep_reclaim.py returns rising-edge detection
4. grep -n "close.*sweep_level.*acceptance" src/intelligence/trading/liquidity_sweep_reclaim.py shows close-above check
</verification>

<success_criteria>
1. liquidity_sweep_reclaim fires on rising edge of sweep_reclaimed (transition 0->1)
2. Close-above sweep_level required (wick-only rejected)
3. Flag-stays-hot (persistent state) produces no signal without rising edge
4. deduplicate_event with (sweep_level, sweep_type) prevents re-fire
5. Synthetic frame tests pass all scenarios
6. Unit tests pass (pytest green)
</success_criteria>

<output>
After completion, create `.planning/phases/124-signal-universe-integrity-cold-start-hardening/124-05-SUMMARY.md`
</output>

<threat_model>
**ASVS Level 1 Threat Model for LiquiditySweepReclaim Rewrite**

High severity threats:
1. **False rising edge (LOW):** If onset_guard called conditionally (only when sweep_detected true), missed transitions cause false fires. MITIGATION: onset_guard called UNCONDITIONALLY so it sees False when flag drops - enables proper rearm (per state_utils.py docstring).
2. **State corruption (MEDIUM):** If _state not isolated per (symbol, tf), cross-symbol contamination. MITIGATION: state_key uses f"{symbol}_{tf}"; onset_guard manages per-key state.
3. **Wick-only leak (MEDIUM):** If close check uses high/low instead of close, wick-only reclaims leak through. MITIGATION: Explicit close > sweep_level check (not high); unit test verifies wick-only rejection.

Opt-out: None - structural rewrite is mandatory for QUALITY-01.
</threat_model>
