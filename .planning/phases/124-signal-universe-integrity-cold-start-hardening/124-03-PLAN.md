---
phase: 124
plan: 03
type: execute
wave: 2
requirements: [QUALITY-01]
depends_on: [124-01]
files_modified:
  - src/intelligence/trading/ofi_continuation.py
  - tests/unit/test_ofi_continuation.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - ofi_continuation demotes OFI streak to context filter (must have sustained imbalance, not trigger)
    - ofi_continuation trigger fires only on OFI acceleration/thrust bar (EWMA gradient or volume spike)
    - Continuous streak state (count >= min_bars) produces no signal without acceleration
    - Acceleration/thrust event produces fire once per episode
  artifacts:
    - path: src/intelligence/trading/ofi_continuation.py
      contains: OFI acceleration detection (EWMA second derivative or volume spike), streak context filter, Parallel dicts -> dataclass pattern for EWMA buffer
    - path: tests/unit/test_ofi_continuation.py
      contains: Synthetic frame tests (streak-only -> no fire, streak+acceleration -> fire once)
  key_links:
    - from: src/intelligence/trading/ofi_continuation.py
      to: src/intelligence/trading/squeeze_expansion.py
      via: Magnitude threshold on structural event pattern
      pattern: Acceleration threshold (not persistence) as trigger
---

<objective>
Wave B Plugin 2/5: OFIContinuation Structural Rewrite

Purpose: Fix OFIContinuation to fire on OFI acceleration/thrust events (EWMA gradient, volume spike) rather than streak crossing N bars. The streak is context (sustained imbalance must exist), the trigger is fresh acceleration on top of that sustained flow.

Output:
- ofi_continuation.py with EWMA acceleration detection (second derivative)
- Volume spike detection as alternative trigger
- OFI streak demoted to context filter
- Parallel dicts -> dataclass pattern for EWMA buffer (deque maxlen=20)
- Synthetic frame unit tests
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-signal-universe-integrity-cold-start-hardening/124-CONTEXT.md
@.planning/phases/124-signal-universe-integrity-cold-start-hardening/124-RESEARCH.md
@.planning/phases/124-signal-universe-integrity-cold-start-hardening/124-PATTERNS.md
@.planning/phases/124-signal-universe-integrity-cold-start-hardening/124-VALIDATION.md
@src/intelligence/trading/ofi_continuation.py (current compute_full lines 75-132)
@src/intelligence/trading/squeeze_expansion.py (reference structural-onset pattern)
@src/intelligence/trading/state_utils.py (track_consecutive_state, onset_guard)
@src/intelligence/trading/atr_utils.py (get_atr_with_floor_from_frames)
@src/intelligence/trading/confidence_utils.py (compose_confidence)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OFIContinuation state dataclass (EWMA buffer)</name>
  <files>src/intelligence/trading/ofi_continuation.py</files>
  <read_first>
    src/intelligence/trading/ofi_continuation.py (current compute_full implementation)
    CLAUDE.md (Parallel dicts -> dataclass pattern)
  </read_first>
  <action>
Add to src/intelligence/trading/ofi_continuation.py:

1. Import dataclass utilities: from dataclasses import dataclass, field; from typing import Any; from collections import deque

2. Create OFIContinuationState dataclass with fields:
   - ewma_buffer: deque (maxlen=20) of ofi_ewma_20 values for gradient detection
   - last_acceleration_bar: int | None (call count when last acceleration fired, for deduplication)

3. Add _state dict to OFIContinuationPlugin: _state: dict[str, OFIContinuationState] = field(default_factory=dict)

4. Add _get_state() factory method:
   def _get_state(self, symbol: str, tf: str) -> OFIContinuationState:
       key = f"{symbol}_{tf}"
       if key not in self._state:
           self._state[key] = OFIContinuationState(ewma_buffer=deque(maxlen=20))
       return self._state[key]
  </action>
  <verify>
grep -n "OFIContinuationState" src/intelligence/trading/ofi_continuation.py returns dataclass definition; grep -n "ewma_buffer" src/intelligence/trading/ofi_continuation.py shows buffer field
  </verify>
  <done>
OFIContinuationState dataclass with EWMA buffer (maxlen=20) and last_acceleration_bar tracking
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement EWMA acceleration detection (second derivative)</name>
  <files>src/intelligence/trading/ofi_continuation.py</files>
  <read_first>
    src/intelligence/trading/ofi_continuation.py (current compute_full, lines 101-131)
  </read_first>
  <action>
Modify src/intelligence/trading/ofi_continuation.py compute_full():

1. Update EWMA buffer: state = self._get_state(symbol, tf); if ofi_ewma: state.ewma_buffer.append(float(ofi_ewma))

2. Detect acceleration (EWMA gradient/second derivative):
   - If len(state.ewma_buffer) < 5: insufficient history, no acceleration
   - Calculate ewma_change = ewma_buffer[-1] - ewma_buffer[-2]
   - Calculate ewma_change_prev = ewma_buffer[-2] - ewma_buffer[-3]
   - Acceleration = ewma_change - ewma_change_prev (second derivative)
   - Positive acceleration (same direction as trend): (acceleration > 0 AND ofi_ewma > 0) OR (acceleration < 0 AND ofi_ewma < 0)

3. Define acceleration threshold: if abs(acceleration) >= mag_threshold * 0.1 (10% of floor), acceleration confirmed

4. Replace onset_guard(streak crossing) with acceleration detection as primary trigger

5. Keep count >= min_bars as context filter AFTER acceleration trigger
  </action>
  <verify>
grep -n "acceleration" src/intelligence/trading/ofi_continuation.py returns detection logic; grep -n "context filter" src/intelligence/trading/ofi_continuation.py shows streak marked as context
  </verify>
  <done>
EWMA acceleration detection: second derivative (change-of-change) >= 10% of magnitude floor
  </done>
</task>

<task type="auto">
  <name>Task 3: Add volume spike as alternative trigger</name>
  <files>src/intelligence/trading/ofi_continuation.py</files>
  <read_first>
    src/intelligence/trading/ofi_continuation.py (compute_full after Task 2)
  </read_first>
  <action>
Modify src/intelligence/trading/ofi_continuation.py compute_full():

1. Extract volume: current_vol = float(df["volume"].iloc[-1]); vol_sma = features.get("volume_sma_20")

2. Calculate volume ratio: vol_ratio = current_vol / vol_sma if vol_sma else 1.0

3. Define volume spike threshold: if vol_ratio >= 2.0 (2x average volume), volume spike confirmed

4. OR logic for trigger: acceleration OR volume_spike (either qualifies as thrust bar)

5. Use deduplicate_event with event_id (direction, call_count) to prevent re-fire within _DEDUP_MIN_BARS

6. Comment: "# Trigger: acceleration OR volume spike (structural thrust on top of sustained flow)"
  </action>
  <verify>
grep -n "vol_ratio\|volume.*spike" src/intelligence/trading/ofi_continuation.py returns volume spike detection; grep -n "OR.*acceleration" src/intelligence/trading/ofi_continuation.py shows OR trigger logic
  </verify>
  <done>
Volume spike detection: vol_ratio >= 2.0 as alternative thrust trigger; OR with acceleration
  </done>
</task>

<task type="auto">
  <name>Task 4: Reorder gates (structural trigger first, streak context filter after)</name>
  <files>src/intelligence/trading/ofi_continuation.py</files>
  <read_first>
    src/intelligence/trading/ofi_continuation.py (current gate ordering lines 118-131)
    src/intelligence/trading/squeeze_expansion.py (lines 72-99 reference)
  </read_first>
  <action>
Reorder src/intelligence/trading/ofi_continuation.py compute_full gates:

1. FIRST: Check magnitude threshold (abs(ofi_ewma) >= mag_threshold) - return no_signal() if below floor
2. SECOND: Check structural trigger (acceleration OR volume_spike) - return no_signal() if neither
3. THIRD: Check streak context filter (count >= min_bars AND direction sustained) - return no_signal() if insufficient persistence
4. FOURTH: Check HMM regime gate - return no_signal() if blocked
5. FIFTH: Extract OHLCV and compute trade frame (existing logic)
6. SIXTH: Apply deduplicate_event with (direction, call_count) event_id

Add comments marking each gate: "# Magnitude gate FIRST", "# Structural trigger SECOND", "# Context filter THIRD"
  </action>
  <verify>
grep -A3 "Structural trigger SECOND" src/intelligence/trading/ofi_continuation.py shows acceleration/volume_spike check; grep -A3 "Context filter THIRD" src/intelligence/trading/ofi_continuation.py shows count >= min_bars check
  </verify>
  <done>
Gate ordering: magnitude -> structural trigger (acceleration/spike) -> streak context filter -> regime -> OHLCV -> deduplicate_event
  </done>
</task>

<task type="auto">
  <name>Task 5: Unit tests for OFIContinuation structural rewrite</name>
  <files>tests/unit/test_ofi_continuation.py</files>
  <read_first>
    tests/unit/test_ofi_continuation.py (existing tests if any)
    src/intelligence/trading/ofi_continuation.py (compute_full signature)
  </read_first>
  <action>
Add to tests/unit/test_ofi_continuation.py three test cases:

1. test_streak_only_no_signal: Create synthetic frames with OFI streak (count >= 10, same direction EWMA) but NO acceleration (constant EWMA, no volume spike) - verify plugin returns no_signal()

2. test_streak_with_acceleration_fires_once: Create synthetic frames with:
   - EWMA values: [100, 105, 110, 115, 120, 130, 145] (acceleration at bar 7: change increases from 5 to 10 to 15)
   - count >= 10 (streak context satisfied)
   - Verify plugin fires on bar 7 (direction=1), not on bars 1-6

3. test_volume_spike_fires_once: Create synthetic frames with:
   - Volume: bars 1-10 at 1000, bar 11 at 2500 (2.5x spike)
   - EWMA stable (no acceleration)
   - Verify plugin fires on bar 11 (volume spike trigger), deduplicate_event prevents re-fire
  </action>
  <verify>
pytest tests/unit/test_ofi_continuation.py::test_streak_only_no_signal -q exits 0; pytest tests/unit/test_ofi_continuation.py::test_streak_with_acceleration_fires_once -q exits 0; pytest tests/unit/test_ofi_continuation.py::test_volume_spike_fires_once -q exits 0
  </verify>
  <done>
Unit tests prove: streak-only produces no signal, streak+acceleration fires once, volume spike fires once
  </done>
</task>

</tasks>

<verification>
After all tasks complete, run:
1. pytest tests/unit/test_ofi_continuation.py -q (all tests green)
2. grep -n "onset_guard.*condition_active" src/intelligence/trading/ofi_continuation.py returns zero hits (streak onset removed)
3. grep -n "acceleration\|volume_spike" src/intelligence/trading/ofi_continuation.py returns structural trigger logic
4. grep -n "context filter" src/intelligence/trading/ofi_continuation.py shows streak marked as context filter
</verification>

<success_criteria>
1. ofi_continuation fires on EWMA acceleration OR volume spike (structural thrust events)
2. OFI streak is context filter only (not trigger)
3. Synthetic frame tests: streak-only -> no fire, streak+acceleration -> fire once
4. Unit tests pass (pytest green)
</success_criteria>

<output>
After completion, create `.planning/phases/124-signal-universe-integrity-cold-start-hardening/124-03-SUMMARY.md`
</output>

<threat_model>
**ASVS Level 1 Threat Model for OFIContinuation Rewrite**

High severity threats:
1. **Look-ahead bias (CRITICAL):** If acceleration calculation uses future EWMA values, signals contaminated. MITIGATION: ewma_buffer uses deque with maxlen=20, real-time appends only; acceleration checks buffer[-1], [-2], [-3] (historical values only).
2. **State corruption (MEDIUM):** If _state not isolated per (symbol, tf), cross-symbol contamination. MITIGATION: _get_state() uses f"{symbol}_{tf}" key, factory lazy init.
3. **False acceleration (MEDIUM):** If acceleration threshold too low (10% of floor), noise triggers fires. MITIGATION: Threshold verified via unit test; adjustable via config if needed.

Opt-out: None - structural rewrite is mandatory for QUALITY-01.
</threat_model>
