---
plan: 131-01
wave: 1
depends_on: []
phase: 131
files_modified:
  - production/scripts/run_historical_pipeline.py
autonomous: true
requirements:
  - D-06

must_haves:
  truths:
    - "A4 root cause is confirmed empirically (asset_class=None in log output for a rolled-contract symbol) or refuted with documented evidence"
    - "The diagnostic log line is removed after confirmation — no permanent logging noise"
    - "A 10-symbol test replay runs to completion without crashing"
  artifacts:
    - path: "production/scripts/run_historical_pipeline.py"
      provides: "Temporary diagnostic log line in replay_symbol() for asset_class check"
  key_links:
    - from: "replay_symbol()"
      to: "all_features dict"
      via: "log statement before run_i7_and_persist()"
      pattern: "all_features.get.*asset_class"
---

<objective>
Confirm (or refute) that asset_class is None in all_features for rolled-contract symbols in replay_symbol(). D-06 mandates empirical confirmation before the A4 fix is written.

Purpose: The spec and research findings are contradictory — spec says A4 is confirmed; research findings say "STILL UNCONFIRMED — one more trace needed." The A4 fix is non-trivial; writing it against an unconfirmed root cause wastes a wave.

Output: A documented confirmation (log output showing asset_class=None for NQM6 or YMM6) or a refutation (asset_class is populated, meaning the actual root cause is elsewhere). Result is recorded in a comment block at the top of the A4 fix task in plan 131-03.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-signal-generation-integrity/131-CONTEXT.md
</context>

<tasks>

<task id="T-01" type="execute">
  <title>Add diagnostic log + run 10-symbol test replay to confirm A4 root cause</title>
  <read_first>
    - production/scripts/run_historical_pipeline.py lines 1599-1770 — full replay_symbol() body; locate the call to run_i7_and_persist() (around line 1755) and the all_features dict construction (around line 1739); also read the surrounding context to find the best log insertion point before run_i7_and_persist()
    - src/intelligence/pipeline/feature_pipeline_executor.py lines 325-333 — the asset_class injection at line 332 that replay_symbol() does NOT call; this is what should be mirrored by the fix
  </read_first>
  <action>
    1. In replay_symbol(), immediately before the `if not skip_signals:` block (line ~1753), insert a single temporary log statement. Use Python's built-in print() (not structlog) to avoid import changes:
       `print(f"[A4-DIAG] {symbol}/{tf}: asset_class={all_features.get('asset_class')!r}")`

    2. Run a 10-symbol test replay targeting only rolled-contract symbols known to have 0 signals. Use:
       `PGPASSWORD=postgres python -u production/scripts/run_historical_pipeline.py --replay-only --symbols NQM6,YMM6,RTYM6,VXK6,VXM6 --timeframes 1m --since 2026-04-01 --client-id 40 2>&1 | grep "A4-DIAG" | head -30`

       If those symbols have no bars, also try NQU6 or ESM6 as a control — a front-month contract should show asset_class populated if the injection lives elsewhere.

    3. Capture the log output. Expected result if A4 is confirmed: `asset_class=None` for all rolled-contract symbols. Expected result if refuted: `asset_class='futures'` or similar string.

    4. Remove the diagnostic log line after capturing the output. The line must not remain in the committed code.

    5. Document the confirmation in a comment block at the top of replay_symbol() (lines 1599-1610 docstring area):
       ```
       # A4 CONFIRMED (2026-06-17): asset_class=None for NQM6/YMM6/RTYM6 in all_features.
       # Fix: build symbol→asset_class lookup from contract_metadata+instruments and inject
       # before run_i7_and_persist(). See plan 131-03-PLAN.md T-01.
       ```
       Or if refuted:
       ```
       # A4 REFUTED (2026-06-17): asset_class IS populated in all_features for rolled contracts.
       # True root cause TBD — see plan 131-03 notes before implementing any fix.
       ```
  </action>
  <acceptance_criteria>
    - Log output captured showing asset_class value for at least 5 bars of a rolled-contract symbol (NQM6, YMM6, or RTYM6)
    - If asset_class=None confirmed: comment block added to replay_symbol() docstring documenting the confirmation
    - If asset_class is not None: comment block documents the refutation and the diagnostic log is removed without any fix being written
    - No `[A4-DIAG]` print statement remains anywhere in production/scripts/run_historical_pipeline.py after this task (verify with grep)
    - `grep -n "A4-DIAG" production/scripts/run_historical_pipeline.py` returns no output
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `grep -n "A4-DIAG" production/scripts/run_historical_pipeline.py` returns empty (diagnostic removed)
- `grep -n "A4 CONFIRM\|A4 REFUT" production/scripts/run_historical_pipeline.py` returns the documentation comment
- The replay command completed without crashing (exit code 0 or only expected psycopg2 errors)
</verification>

<success_criteria>
A4 root cause status is empirically determined and documented. Plan 131-03 can proceed with either the confirmed fix approach or a revised approach if refuted.
</success_criteria>

<output>
After completion, create `.planning/phases/131-signal-generation-integrity/131-01-SUMMARY.md`
</output>
