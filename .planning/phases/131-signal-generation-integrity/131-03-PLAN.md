---
plan: 131-03
wave: 2
depends_on:
  - 131-01
phase: 131
files_modified:
  - production/scripts/run_historical_pipeline.py
  - src/intelligence/trading/cross_asset_divergence.py
  - src/intelligence/register_plugins.py
autonomous: true
requirements:
  - D-04
  - D-05

must_haves:
  truths:
    - "asset_class is injected into all_features for every symbol in replay_symbol() before run_i7_and_persist() is called"
    - "bar_histories deque maxlen is 800 (not 200) in replay_symbol()"
    - "trad_CrossAssetDivergence has _CORPUS_EXCLUDABLE = True and a live-only docstring comment"
    - "all_features['asset_class'] is non-None for NQM6, YMM6, RTYM6, VXK6, VXM6, ZNM6 after the A4 fix"
  artifacts:
    - path: "production/scripts/run_historical_pipeline.py"
      provides: "A4 fix (asset_class injection) + PrevDayLevelTest fix (maxlen 200→800)"
    - path: "src/intelligence/trading/cross_asset_divergence.py"
      provides: "CrossAssetDivergence live-only annotation (_CORPUS_EXCLUDABLE=True + docstring)"
  key_links:
    - from: "replay_symbol()"
      to: "all_features['asset_class']"
      via: "contract_metadata + instruments lookup at start of replay loop"
      pattern: "all_features\\[.asset_class.\\]"
    - from: "bar_histories"
      to: "deque(maxlen=800)"
      via: "line 1649 of run_historical_pipeline.py"
      pattern: "deque\\(maxlen=800\\)"
---

<objective>
Three targeted fixes that share run_historical_pipeline.py as their primary file. Implementing them together prevents merge conflicts.

A4 fix: Build a symbol→asset_class lookup at the start of replay_symbol() from contract_metadata + instruments tables, then inject `all_features["asset_class"]` before calling run_i7_and_persist(). This mirrors what FeaturePipelineExecutor.execute() does at line 332 for the live pipeline.

trad_PrevDayLevelTest fix: Increase bar_histories deque maxlen from 200 to 800 at run_historical_pipeline.py line 1649. The SessionLevelsPlugin (I3) needs at least 390 bars for the current session window plus another 390 for the prior session window — 200 is insufficient and forces prior_session_high/low/close to None for every bar.

trad_CrossAssetDivergence annotation: Add `_CORPUS_EXCLUDABLE = True` class attribute and a live-only docstring comment to the CrossAssetDivergence plugin file. No behavior change — this is a formal architectural declaration (D-02).

Purpose: After A4 confirmation in plan 131-01, this plan writes the fix. The maxlen fix and CrossAsset annotation are independent of A4 confirmation but share the same file — batching avoids two sequential edits to run_historical_pipeline.py.

Output: Three modified files with no behavior regressions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-signal-generation-integrity/131-CONTEXT.md
@.planning/phases/131-signal-generation-integrity/131-01-SUMMARY.md
</context>

<tasks>

<task id="T-01" type="execute">
  <title>A4 fix — inject asset_class into all_features in replay_symbol()</title>
  <read_first>
    - production/scripts/run_historical_pipeline.py lines 1599-1650 — replay_symbol() signature and initialization section; specifically the psycopg2 db_conn usage pattern and any existing symbol→instrument lookups already in scope
    - production/scripts/run_historical_pipeline.py lines 1599-1612 — function docstring and any existing `instrument` variable setup at top of function (confirm no instrument object already exists here)
    - production/scripts/run_historical_pipeline.py lines 1720-1760 — frames construction and run_i7_and_persist() call; this is where all_features is assembled and where the injection point is
    - src/intelligence/pipeline/feature_pipeline_executor.py lines 325-333 — the live pipeline equivalent at line 332 (`flat_features["asset_class"] = instrument.asset_class.value`) to mirror exactly
    - production/scripts/run_historical_pipeline.py lines 180-215 — the contract_metadata table upsert to understand the schema: columns are `symbol`, `base_symbol`, `asset_class`, `is_front_month` etc.
    - .planning/phases/131-signal-generation-integrity/131-01-SUMMARY.md — read the A4 confirmation or refutation before writing this fix; if A4 was refuted, stop and document in the summary instead of implementing
  </read_first>
  <action>
    PREREQUISITE: Read 131-01-SUMMARY.md first. If A4 was refuted (asset_class IS populated), do not write this fix — instead document the actual root cause in this plan's summary and leave run_historical_pipeline.py unchanged.

    If A4 confirmed (asset_class=None):

    At the top of replay_symbol(), after the `if not bars_by_tf:` early-return block (around line 1640), before the `bar_histories` initialization at line 1649, add a symbol→asset_class lookup using the existing psycopg2 db_conn:

    ```python
    # A4 fix: resolve asset_class for this symbol from contract_metadata (futures rolls)
    # or instruments (equities/ETFs/FX). Mirrors FeaturePipelineExecutor.execute():332.
    _symbol_asset_class: str | None = None
    with db_conn.cursor() as _cur:
        _cur.execute(
            """SELECT COALESCE(cm.asset_class, i.asset_class)
               FROM (SELECT %s::text AS sym) s
               LEFT JOIN contract_metadata cm ON cm.symbol = s.sym
               LEFT JOIN instruments i ON i.symbol = s.sym
               LIMIT 1""",
            (symbol,),
        )
        _row = _cur.fetchone()
        if _row and _row[0] is not None:
            _symbol_asset_class = str(_row[0])
    ```

    Then, in the bar event loop, find where `all_features` is constructed (line ~1739: `all_features = {**i1_features, **intelligence}`). Immediately after that line, add:

    ```python
    if _symbol_asset_class is not None:
        all_features["asset_class"] = _symbol_asset_class
    ```

    This injection must occur before the `if not skip_signals:` block that calls run_i7_and_persist(). In precomputed_features mode (the `if precomputed_features is not None:` branch at line ~1706), apply the same injection after `all_features = precomputed_features.get((tf, ts_utc))`.

    Use psycopg2 cursor pattern consistent with the rest of this file (not asyncpg). The `db_conn` is already a psycopg2 connection.
  </action>
  <acceptance_criteria>
    - `grep -n "_symbol_asset_class" production/scripts/run_historical_pipeline.py` returns at least 3 matches (declaration, query, injection)
    - `grep -n "all_features\[.asset_class.\]" production/scripts/run_historical_pipeline.py` returns the injection line inside replay_symbol()
    - The injection appears before the `if not skip_signals:` block (verify line ordering with grep -n context)
    - `grep -n "COALESCE(cm.asset_class" production/scripts/run_historical_pipeline.py` returns the lookup query
    - `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
</task>

<task id="T-02" type="execute">
  <title>PrevDayLevelTest fix — increase bar_histories deque maxlen from 200 to 800</title>
  <read_first>
    - production/scripts/run_historical_pipeline.py line 1649 — `bar_histories: dict[str, deque] = defaultdict(lambda: deque(maxlen=200))`; verify the current maxlen value before changing
    - docs/plans/2026-06-17-phases-131-133-signal-corpus-integrity.md §"trad_PrevDayLevelTest" — confirms that SessionLevelsPlugin uses `_SESSION_BARS=390` so 200 bars is always insufficient for prior-session lookback
  </read_first>
  <action>
    At production/scripts/run_historical_pipeline.py line 1649, change:
      `bar_histories: dict[str, deque] = defaultdict(lambda: deque(maxlen=200))`
    to:
      `bar_histories: dict[str, deque] = defaultdict(lambda: deque(maxlen=800))`

    Add a comment on the same line or immediately above:
      `# 800 bars: SessionLevelsPlugin needs _SESSION_BARS=390 current + 390 prior window`

    No other changes in this task.
  </action>
  <acceptance_criteria>
    - `grep -n "maxlen=200" production/scripts/run_historical_pipeline.py` returns no output (the 200 is gone)
    - `grep -n "maxlen=800" production/scripts/run_historical_pipeline.py` returns line 1649 (or nearby)
    - `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
</task>

<task id="T-03" type="execute">
  <title>CrossAssetDivergence — annotate as live-only architectural exclusion</title>
  <read_first>
    - src/intelligence/register_plugins.py — search for "CrossAssetDivergence" in TIER_I7 to find the import and the actual plugin file path
    - The CrossAssetDivergence plugin file (path from register_plugins.py search) — read the full file to find the class definition, class attributes, and docstring; the `_CORPUS_EXCLUDABLE` attribute must be added at class level
  </read_first>
  <action>
    In the CrossAssetDivergence plugin file:

    1. Add to the class body (alongside other class-level attributes like `name`, `shadow_only`, etc.):
       `_CORPUS_EXCLUDABLE: bool = True  # live-only: requires cross_asset_service real-time state`

    2. Add or update the class docstring to include:
       ```
       # live-only: requires cross_asset_service real-time state (frames['cross_asset']).
       # Historical replay processes single symbols; cross-instrument bar arrays are not
       # pre-loaded. _CORPUS_EXCLUDABLE = True — zero emissions in replay is architectural,
       # not a bug. See Phase 131 D-02.
       ```
       Place this as a comment block in the docstring or as module-level comment above the class.

    Do NOT change compute_full() or any other behavior.
  </action>
  <acceptance_criteria>
    - `grep -rn "_CORPUS_EXCLUDABLE" src/intelligence/` returns the CrossAssetDivergence plugin file with `_CORPUS_EXCLUDABLE: bool = True`
    - `grep -n "live-only" src/intelligence/` (or the specific file) returns the annotation comment
    - `grep -n "Phase 131 D-02" src/intelligence/` returns the cross-reference in the comment
    - `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `grep -n "_symbol_asset_class\|all_features\[.asset_class.\]" production/scripts/run_historical_pipeline.py` — both present
- `grep -n "maxlen=800" production/scripts/run_historical_pipeline.py` — present at line ~1649
- `grep -rn "_CORPUS_EXCLUDABLE.*True" src/intelligence/` — CrossAssetDivergence plugin
- `.venv/bin/pytest tests/unit/ -q` green
</verification>

<success_criteria>
A4 fix applied (asset_class injected for all symbols including rolled contracts). PrevDayLevelTest now has sufficient bar history for prior-session window. CrossAssetDivergence formally documented as live-only with _CORPUS_EXCLUDABLE marker.
</success_criteria>

<output>
After completion, create `.planning/phases/131-signal-generation-integrity/131-03-SUMMARY.md`
</output>
