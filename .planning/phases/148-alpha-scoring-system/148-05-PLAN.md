---
phase: 148
plan: 05
type: execute
wave: 3
depends_on: [148-02, 148-03, 148-04]
files_modified:
  - docs/plans/archive/2026-07-22-phase148-promotion-decision.md
  - .planning/gate_look_log.jsonl
autonomous: true
requirements: [SCORE-02, SCORE-03, SCORE-04]

must_haves:
  truths:
    - "Gate 1 (signal proof) is run exactly once (D-04) and its verdict recorded in gate_evaluations (gate_id='gate1_signal')"
    - "Gate 2 (execution proof) is run exactly once (D-04), strictly after Gate 1 (D-02), its verdict recorded in gate_evaluations (gate_id='gate2_execution')"
    - "A statistical FAIL verdict from either gate lands as a normal result='fail' row (the gate ran and failed); it is NOT an exception that halts the run"
    - "Each real gate run is preceded by a --dry-run pre-flight and its DB write is atomic + snapshot-logged (mechanism lives inside the scripts from 148-03/148-04); the outer count=0 assertion here is a belt-and-suspenders guard"
    - "A promotion decision record exists disclosing both independent verdicts, all 5 SHADOW-REVIEW criteria with each pass/fail (c5 via the c7_confident_loss proxy), the D-06 known-failing champion numbers, the regime-stratified breakdown, and the SCORE-04 v2.x note"
    - "The OOS window boundary (alpha.validation.oos_start) is never shifted to dodge an unfavorable result (D-05); the record discloses, it does not re-derive theatrically or renegotiate the window"
  artifacts:
    - path: "docs/plans/archive/2026-07-22-phase148-promotion-decision.md"
      provides: "Two-gate promotion verdict + SCORE-04 v2.x note + overall decision"
      contains: "Gate 1"
    - path: ".planning/gate_look_log.jsonl"
      provides: "Auditable run-once trail (with pre-run snapshot) for the gate scripts"
  key_links:
    - from: "docs/plans/archive/2026-07-22-phase148-promotion-decision.md"
      to: "gate_evaluations"
      via: "cites both gate1_signal and gate2_execution rows"
      pattern: "gate_evaluations"
    - from: "docs/plans/archive/2026-07-22-phase148-promotion-decision.md"
      to: "143.1-08-SHADOW-VALIDATION.md sec 6/7"
      via: "verbatim champion numbers (D-06)"
      pattern: "0\\.38512018365944"
---

<objective>
Run the two OOS proof gates exactly once each (Gate 1 before Gate 2 per D-02), and write the
promotion decision record that synthesizes both independent verdicts plus the SCORE-04 v2.x
note. This is the milestone deliverable: the verdict the entire measurement pipeline exists to
produce. These gate runs are IRREVERSIBLE per D-04 - each runs at most once; re-running "to
check if it passes now" is a protocol violation, not a fix.

Purpose: produce the authoritative signal-proof and execution-proof verdicts and the human
decision record. Output: two gate_evaluations rows, an appended look-log, and the decision doc.
</objective>

<review_revisions>
Revised from cross-AI review (148-REVIEWS.md). Changes traceable to specific findings:
- Consensus Concern 2 ("irreversible runs lack a pre-run snapshot and safe-retry story"): the
  atomic check+INSERT and the pre-run snapshot now live INSIDE the gate scripts (148-03/148-04),
  so this plan's pre-run count=0 assertion is a belt-and-suspenders outer guard, not the sole
  safety mechanism. Each real run is preceded by a --dry-run pre-flight (verify the script runs
  clean and the snapshot is sane before consuming the one-shot), and acceptance now checks the
  look-log entry carries the pre-run snapshot.
- Secondary (Antigravity MEDIUM): a statistical FAIL verdict is an EXPECTED outcome that writes a
  result='fail' row -- "the gate ran and failed" is success for this task, distinct from "the gate
  crashed" (a system fault). The known 3-of-5 pooled FAIL (D-06) must land as a row, not a halt.
The tolerance assertion added in Task 2 (the 1e-6 champion-number match), the strict D-02
sequencing, the run-once guards, and the decision-record content are UNCHANGED from the reviewed
version.
</review_revisions>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/148-alpha-scoring-system/148-CONTEXT.md
@.planning/phases/148-alpha-scoring-system/148-VALIDATION.md
@docs/plans/OOS-EVAL-PROTOCOL.md
@docs/plans/SHADOW-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Gate 1 (SCORE-02 signal proof) exactly once</name>
  <files>.planning/gate_look_log.jsonl</files>
  <read_first>
    - scripts/ops/corpus/ops_oos_gate1_signal_eval.py (the script built in plan 148-03, incl. its --dry-run flag and atomic write)
    - docs/plans/OOS-EVAL-PROTOCOL.md (run-once cadence, data-starvation-is-diagnostic failure rule)
    - .planning/gate_look_log.jsonl (confirm no prior gate1_signal run exists before running)
  </read_first>
  <action>
    PRE-FLIGHT (safe, non-consuming): run `ops_oos_gate1_signal_eval.py --dry-run` first over the
    intended universe. Confirm it executes cleanly, prints a coherent would-be verdict, and its
    pre-run snapshot (oos_start, apr_values_used, input_population_row_count, fetch_sql_sha256)
    looks sane. The dry-run writes NOTHING -- verify gate_evaluations and the look-log are still
    untouched afterward. Only proceed to the real run once the dry-run looks correct.

    Before the real run, assert this gate has not already been run for this milestone:
    `SELECT count(*) FROM gate_evaluations WHERE gate_id='gate1_signal'` must be 0. If it is not 0,
    STOP and report - do not run again (D-04). (This is an outer guard; the script itself also
    re-asserts count=0 inside its write transaction so the check+INSERT is atomic and a mid-run
    crash rolls back cleanly.)

    Invoke ops_oos_gate1_signal_eval.py once (real, no --dry-run) against the live DB over the
    active ETF universe (get_active_contracts) across timeframes 5m/15m/1h/1d. This run is
    IRREVERSIBLE per D-04; capture whatever verdict it produces. A verdict of 'fail' or
    'insufficient' is a VALID, EXPECTED outcome that writes a result row -- it is NOT an error to
    retry. Expect a meaningful fraction of OOS cells to return walk_forward_stable=None or
    insufficient-N because the OOS window is much shorter than the in-sample history - per
    OOS-EVAL-PROTOCOL that data-starvation signal is itself diagnostic (data absence vs signal
    absence), NOT a defect to patch by lowering min_reliable_n. Confirm the verdict + evidence
    landed in gate_evaluations (gate_id='gate1_signal') and one entry (with the pre-run snapshot)
    was appended to .planning/gate_look_log.jsonl. Only a genuine system fault (DB unreachable,
    unset oos_start) is an exception; if one occurs, the atomic transaction leaves no partial row
    and the run is safely retryable after the fault is fixed.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tc "SELECT count(*)=1 FROM gate_evaluations WHERE gate_id='gate1_signal';" | grep -q t</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 1 gate_evaluations row with gate_id='gate1_signal' (result is one of 'pass'/'fail'/'insufficient' -- any of these is a successful task outcome; a FAIL row is the gate running and failing, not a crash)
    - That row's evidence jsonb is non-empty, records the oos_start boundary used, and carries the pre-run snapshot
    - .planning/gate_look_log.jsonl has exactly one gate1 entry with a report sha256 AND the pre-run snapshot (apr_values_used, input_population_row_count, fetch_sql_sha256)
    - Zero rows were inserted into alpha_ensemble_ic by this run (`SELECT count(*) FROM alpha_ensemble_ic` unchanged from pre-run)
  </acceptance_criteria>
  <done>Gate 1 dry-run pre-flighted then run once; signal-proof verdict (pass/fail/insufficient) recorded; look-log appended with snapshot.</done>
</task>

<task type="auto">
  <name>Task 2: Run Gate 2 (SCORE-03 execution proof) exactly once, after Gate 1</name>
  <files>.planning/gate_look_log.jsonl</files>
  <read_first>
    - scripts/analysis/score03_gate2_execution_eval.py (the script built in plan 148-04, incl. its --dry-run flag and atomic write)
    - .planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-08-SHADOW-VALIDATION.md sections 6 and 7
    - docs/plans/SHADOW-REVIEW.md (the 5 criteria)
  </read_first>
  <action>
    Enforce D-02 sequencing: only proceed once Task 1 recorded its gate1_signal row.

    PRE-FLIGHT (safe, non-consuming): run `score03_gate2_execution_eval.py --dry-run` against
    weight_epoch='143.1-08-champion' first. Confirm the printed pooled numbers reproduce the known
    143.1-08 champion baseline (c2_ci_lower=-0.1214896346368989, c3_sharpe=0.38512018365944,
    c4_max_dd=9.598299843093644) within tolerance, that the regime_cells companion is present, and
    that the pre-run snapshot looks sane. The dry-run writes NOTHING. If the dry-run numbers do NOT
    reproduce the champion baseline, STOP and root-cause the methodology divergence before the real
    run -- do not consume the one-shot on a script that diverges from 143.1-08.

    Then assert Gate 2 has not already been run: `SELECT count(*) FROM gate_evaluations WHERE gate_id='gate2_execution'` must be 0; if not, STOP (D-04). (Outer guard; the script re-asserts count=0 inside its atomic write transaction.)

    Invoke score03_gate2_execution_eval.py once (real, no --dry-run) against weight_epoch='143.1-08-champion'. This run is IRREVERSIBLE per D-04. Confirm the single gate_evaluations row (gate_id='gate2_execution') contains BOTH the pooled criteria and the regime_cells breakdown in its evidence. The known-in-advance pooled result is a FAIL and this is expected, not a surprise to investigate: c2_ci_lower=-0.1214896346368989 (fails >0), c3_sharpe=0.38512018365944 (fails >0.5), c4_max_dd=9.598299843093644 (fails <0.25 ratio), c1=True (69 OOS days), and c5 (criterion-5 operational proxy = c7_confident_loss) PASSES pooled (no confident loss on the pooled short side). So the pooled verdict is 3-of-5 criteria failing (c2/c3/c4 fail; c1/c5 pass) -- matching D-06's '3 of 5' framing exactly. This result='fail' row is the gate running and failing correctly; it is NOT an error, NOT something to retry, and NOT a reason to shift the window. Confirm the evidence jsonb carries all 5 pooled criteria (c1..c5, c5 as the c7_confident_loss proxy) alongside the regime_cells breakdown. The regime-stratified companion: only 2 of 8 champion cells clear min_clusters=20 coverage (long/mid_bull ci_lower=-0.077 fails; short/mid_bull ci_lower=-0.278 fails); the other 6 are coverage="insufficient". Do NOT shift the OOS window to change this result (D-05: reshifting the window because the visible result is unfavorable is the same banned post-hoc renegotiation as moving a numeric threshold).
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tc "SELECT count(*)=1 FROM gate_evaluations WHERE gate_id='gate2_execution';" | grep -q t</automated>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tc "SELECT abs((evidence->>'c2_ci_lower')::numeric - (-0.1214896346368989)) < 1e-6 AND abs((evidence->>'c3_sharpe')::numeric - 0.38512018365944) < 1e-6 AND abs((evidence->>'c4_max_dd')::numeric - 9.598299843093644) < 1e-6 FROM gate_evaluations WHERE gate_id='gate2_execution';" | grep -q t</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 1 gate_evaluations row with gate_id='gate2_execution' (result='fail' is the expected, valid outcome -- the gate ran and failed, not crashed)
    - No separate row with gate_id='FRAME-04' (`SELECT count(*) FROM gate_evaluations WHERE gate_id='FRAME-04'` == 0)
    - That row's evidence jsonb contains a pooled criteria block covering all 5 SHADOW-REVIEW criteria (c1..c5, with c5 as the c7_confident_loss operational proxy), a regime_cells array, and the pre-run snapshot
    - Task 1's gate1_signal row exists (D-02 ordering held) before this row's run_ts
    - The freshly computed pooled c2_ci_lower/c3_sharpe/c4_max_dd match the known 143.1-08 champion baseline (-0.1214896346368989 / 0.38512018365944 / 9.598299843093644) within 1e-6 -- this is the same champion population re-evaluated, not a re-derivation expected to differ; a mismatch means SCORE-03 diverged from 143.1-08's methodology (wrong min_clusters, a filter difference, etc.) and must be root-caused before trusting this gate's verdict, not silently accepted as "a different but still valid number"
    - .planning/gate_look_log.jsonl has a gate2 entry carrying the pre-run snapshot
  </acceptance_criteria>
  <done>Gate 2 dry-run pre-flighted (numbers reproduced) then run once, after Gate 1; execution-proof result='fail' verdict recorded with pooled + regime-stratified evidence and snapshot.</done>
</task>

<task type="auto">
  <name>Task 3: Write the promotion decision record (SCORE-04 + overall verdict)</name>
  <files>docs/plans/archive/2026-07-22-phase148-promotion-decision.md</files>
  <read_first>
    - docs/plans/SHADOW-REVIEW.md and docs/plans/OOS-EVAL-PROTOCOL.md (governance-rule docs this record answers to)
    - .planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-08-SHADOW-VALIDATION.md sec 6/7 (verbatim champion numbers)
    - The two gate_evaluations rows just written (query them for the actual recorded verdicts)
    - CLAUDE.md doc conventions (Author: provenance line; canonical decision docs live in docs/plans/)
  </read_first>
  <action>
    Create docs/plans/archive/2026-07-22-phase148-promotion-decision.md with these sections:

    (1) Gate 1 - signal proof: state the verdict recorded in gate_evaluations gate1_signal, summarize its IC/CI/walk-forward evidence, and note explicitly how much of the OOS window returned insufficient-N (data starvation as diagnostic, per OOS-EVAL-PROTOCOL).

    (2) Gate 2 - execution proof: cite the champion 143.1-08 pooled numbers VERBATIM (c2_ci_lower=-0.1214896346368989, c3_sharpe=0.38512018365944, c4_max_dd=9.598299843093644, c1=69 OOS days) per D-06, and present the regime-stratified companion (2 of 8 cells clear min_clusters=20; long/mid_bull ci_lower=-0.077 and short/mid_bull ci_lower=-0.278 both fail; 6 cells insufficient coverage) per D-07. Explain what the regime breakdown reveals about WHY the pooled number failed: todo 165 proved on this exact data that a pooled single-window verdict is structurally blind to regime-conditional edge (shorts profited in the COVID crash, were breakeven through the 2022 bear, lost specifically in the one rally window tested). Enumerate all 5 SHADOW-REVIEW criteria with each verdict so the '3 of 5' count is accurate against what was actually evaluated: c1 (>=60 days) PASS (69 days); c2 (mean P&L CI>0) FAIL (ci_lower=-0.1214896346368989); c3 (Sharpe>0.5) FAIL (0.38512018365944); c4 (max_dd<0.25) FAIL (9.598299843093644); c5 (no IC-Sharpe cliff) PASS, evaluated via its documented operational proxy c7_confident_loss (no pooled confident loss) because the literal last_20d/full-period IC-Sharpe cliff is N/A on the champion population (no recurring ensemble_ic_engine cadence to form the trailing-vs-full split). Net: 3 of 5 fail. State up front that these 3-of-5-criteria failures were known going in, not discovered during execution.

    (3) Independence statement: Gate 1 and Gate 2 are reported as two independent verdicts; do not conflate a signal failure with an execution failure or vice versa.

    (4) SCORE-04 v2.x comparison note: documentation only. State that no live v2.x comparison population exists - the v2.x I1-I7 pipeline has been failed since 2026-07-17 with its ExecStart target deleted from disk and zero plugins ever promoted/evaluated live - so this is a prose note, not a numeric comparison.

    (5) Overall promotion decision + rationale, grounded in both gate verdicts. Do NOT propose a fix for a failing gate; diagnosing why Gate 2 failed and whether a frame/execution recalibration helps is explicitly out of scope for this phase (deferred idea in CONTEXT.md).

    Add an `Author:` provenance line per project convention. Do not use em dashes.
  </action>
  <verify>
    <automated>grep -q "0.38512018365944" docs/plans/archive/2026-07-22-phase148-promotion-decision.md && grep -q "9.598299843093644" docs/plans/archive/2026-07-22-phase148-promotion-decision.md && grep -qi "Gate 1" docs/plans/archive/2026-07-22-phase148-promotion-decision.md && grep -qi "Gate 2" docs/plans/archive/2026-07-22-phase148-promotion-decision.md && grep -qi "v2.x" docs/plans/archive/2026-07-22-phase148-promotion-decision.md && grep -qi "cliff" docs/plans/archive/2026-07-22-phase148-promotion-decision.md</automated>
  </verify>
  <acceptance_criteria>
    - Doc cites the verbatim champion numbers -0.1214896346368989, 0.38512018365944, and 9.598299843093644
    - Doc has distinct "Gate 1" and "Gate 2" sections stating each verdict independently
    - Doc enumerates all 5 SHADOW-REVIEW criteria with each pass/fail verdict, including criterion 5 via the c7_confident_loss operational proxy and a note that the literal IC-Sharpe cliff is N/A on this population (so the '3 of 5' count is accurate against what was evaluated)
    - Doc contains the regime-stratified breakdown (2 of 8 cells clear coverage; both evaluated cells fail)
    - Doc contains the SCORE-04 v2.x note stating no live comparison population exists
    - Doc states an overall promotion decision and does not propose a fix for a failing gate
    - Doc has an `Author:` provenance line and contains no em dash characters
  </acceptance_criteria>
  <done>Promotion decision record written: both independent verdicts disclosed, D-06 numbers cited verbatim, regime-stratified companion shown, SCORE-04 v2.x note included, overall decision stated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> irreversible gate run -> DB | Each gate run is a one-shot that consumes part of the frozen OOS holdout; re-running degrades holdout integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-07 | Repudiation | run-once cadence (D-04) | mitigate | --dry-run pre-flight + outer count=0 assertion + the script's own atomic transactional check+INSERT (rollback on crash) + append-only gate_look_log.jsonl with pre-run snapshot records every look and makes any drift diagnosable |
| T-148-08 | Tampering | OOS window boundary (D-05) | mitigate | Window is read from alpha.validation.oos_start, never shifted to change an unfavorable result; the decision record discloses rather than reshifts |
| T-148-09 | Information disclosure | promotion decision record | accept | Internal governance artifact; no external exposure, no secrets |
| T-148-10 | Denial of Service (self) | statistical FAIL mis-handled as a crash | mitigate | A 'fail'/'insufficient' verdict writes a normal result row; only genuine system faults raise, and the atomic transaction leaves no partial state, so the run is safely retryable after a true fault |
</threat_model>

<verification>
- Exactly one gate_evaluations row each for gate1_signal and gate2_execution; no FRAME-04 row.
- Each gate row's result is a valid verdict value (a 'fail' row is expected, not a crash); each carries its pre-run snapshot.
- gate1_signal run_ts precedes gate2_execution run_ts (D-02 ordering).
- .planning/gate_look_log.jsonl has one entry per gate run, each with a pre-run snapshot.
- Promotion decision record exists and passes the grep checks (verbatim numbers, both gates, v2.x note).
- `.venv/bin/pytest tests/unit/ -q` full suite green.
</verification>

<success_criteria>
Both OOS proof gates run exactly once (Gate 1 before Gate 2, each dry-run pre-flighted),
verdicts (pass/fail/insufficient, a FAIL being a valid recorded row not a crash) recorded in
gate_evaluations with pre-run snapshots, and a promotion decision record synthesizes both
independent verdicts plus the SCORE-04 v2.x note and the D-06 known-failing champion numbers.
</success_criteria>

<output>
Create `.planning/phases/148-alpha-scoring-system/148-05-SUMMARY.md` when done.
</output>
