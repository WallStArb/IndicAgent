---
phase: 172-hmm-regime-volatility-only-redesign
plan: 07
type: execute
wave: 5
depends_on: ["172-06"]
files_modified:
  - docs/foundation/glossary.md
  - tests/unit/services/test_ensemble_trainer_regime_source.py
  - .planning/phases/172-hmm-regime-volatility-only-redesign/172-DOWNSTREAM-VERIFICATION.md
  - .planning/phases/172-hmm-regime-volatility-only-redesign/evidence/172-07-ic-refresh-scoped.json
autonomous: true
requirements: [REQ-7]

must_haves:
  truths:
    - "A real scoped ic_engine run produces feature_ic_scores rows keyed on the volatility label set, proving the cutover works end to end rather than only passing unit tests"
    - "ensemble_trainer's stratum discovery is demonstrated to read the cross-sectional POOLED labels and not the per-symbol column this phase changed, with a regression test that goes red if that ever inverts"
    - "docs/foundation/glossary.md's regime entry describes the idiosyncratic regime that actually exists, standing on its own without pointing at a phase planning document"
    - "The phase's downstream re-verification is recorded with real numbers, not asserted"
  artifacts:
    - path: ".planning/phases/172-hmm-regime-volatility-only-redesign/evidence/172-07-ic-refresh-scoped.json"
      provides: "row counts, distinct regimes, and regime_scope breakdown from a real scoped ic_engine --refresh run"
      contains: "distinct_regimes"
    - path: "docs/foundation/glossary.md"
      provides: "the rewritten regime entry naming the calm/elevated/turbulent idiosyncratic vocabulary and feature_vectors.regime_volatility"
      contains: "regime_volatility"
    - path: "tests/unit/services/test_ensemble_trainer_regime_source.py"
      provides: "regression coverage that ensemble_trainer's stratum query keys on the POOLED cross-sectional eligibility filter"
      contains: "_pooled"
  key_links:
    - from: "services/ic_engine.py --refresh"
      to: "feature_ic_scores"
      via: "a scoped real run whose written rows carry volatility labels under regime_scope symbol_hmm"
      pattern: "regime_scope"
    - from: "docs/foundation/glossary.md"
      to: "feature_vectors.regime_volatility"
      via: "the regime entry's Code surface line"
      pattern: "feature_vectors\\.regime_volatility"
---

<objective>
Close the phase's downstream re-verification: run `ic_engine.py --refresh` at a bounded scope and
record what it actually wrote, prove `ensemble_trainer.py`'s stratum source is what this phase
believes it is, and rewrite the `regime` glossary entry so the canonical definition matches the
system that now exists.

Purpose: plan 172-06 changed the code and passed unit tests. Unit tests cannot show that a real
`feature_ic_scores` row now carries `calm` instead of `ranging`, and `172-VALIDATION.md`'s
Per-Task Verification Map for work item 5 explicitly pairs
`.venv/bin/pytest tests/unit/ -k "ic_engine or ensemble_trainer" -q` with a real scoped
`ic_engine.py --refresh`. The `ensemble_trainer` half needs its own treatment because
`172-RESEARCH.md`'s inventory lists it as a downstream regime consumer, while its actual query
filters `symbol = 'POOLED' AND is_pooled = true AND regime != '_pooled'`, which selects
cross-sectional `market_regimes`-sourced strata rather than the per-symbol column this phase
repointed. If that reading is right, the correct deliverable is a regression test pinning it, not a
repoint. If it is wrong, that is a blocking finding, and this plan is where it surfaces.

The glossary rewrite is a phase deliverable, not polish. `172-RESEARCH.md` Pitfall 3 is specific:
the `regime` entry currently states the idiosyncratic regime is a 5-label trend vocabulary stored
in `feature_vectors.regime`, and that sentence becomes actively wrong, not merely stale, the moment
plan 172-06 ships.

Output: one scoped run's evidence, one regression test, one rewritten glossary entry, and one
verification record.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-hmm-regime-volatility-only-redesign/172-RESEARCH.md
@.planning/phases/172-hmm-regime-volatility-only-redesign/172-PATTERNS.md
@.planning/phases/172-hmm-regime-volatility-only-redesign/172-VALIDATION.md
@.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-FINAL-VERDICT.md
@CLAUDE.md
</context>

<interfaces>
Plan-time snapshots. Re-verify each at execution time.

- `services/ic_engine.py` CLI: `--symbols` (nargs="*"), `--tf` (nargs="*", choices `_DEFAULT_TFS`),
  `--workers` (int, default APR `infra.ic_engine.workers`), `--training-window-end` (REQUIRED,
  ISO 8601 timezone-aware, no default by design per Phase 141.1 CR-01 and
  `docs/plans/OOS-EVAL-PROTOCOL.md`), `--cross-sectional-only`, `--refresh` (bypasses the whole-cell
  fingerprint check, intended to be combined with `--symbols`/`--tf` to scope),
  `--dry-run-validity` (prints the fingerprint skip/compute partition and exits before any fetch).
- `services/ensemble_trainer.py`: module docstring line 4 and line 121 carry the eligibility
  filter string `symbol = 'POOLED' AND is_pooled = true AND regime != '_pooled'`. Line 395 and
  lines 485-490 restate it. Lines 738-763 run
  `SELECT DISTINCT tf, regime FROM feature_ic_scores WHERE ... AND regime IS NOT NULL ORDER BY tf, regime`
  and then `regime = stratum["regime"]`. Lines 1069-1131 INSERT into `ensemble_weights` keyed
  `(symbol, tf, regime, weight_version, feature_name)`.
- Existing ensemble test file: `tests/unit/services/test_ensemble_trainer_alignment_gate.py` is the
  only `ensemble_trainer` unit test file today; the new regression test is a new file rather than
  an extension of it.
- `feature_ic_scores` columns relevant here: `regime` (text, not null), `regime_scope` (text, not
  null, CHECK over `cross_sectional` / `symbol_hmm` / `pooled`), `is_pooled`, `symbol`,
  `training_window_end`, `regime_label_source`.
- `docs/foundation/glossary.md`, `### regime` entry at lines 75-96. Section order to preserve:
  a definition paragraph, the two bulleted mechanisms (idiosyncratic and systematic), `**Not:**`,
  `**Banned:**`, `**Status:**`, `**Disambiguation:**`, `**Code surface:**`. The
  `**Banned:**` line holds three banned synonyms, is out of scope for this rewrite, and must be
  treated as frozen text; do not restate its contents in any tracked file. Sibling entries
  `### regime_group` (line 98) and `### conditioning layer` (line 115) are the structural template
  and are also affected: `regime_group`'s body contains a paragraph contrasting itself with
  `feature_vectors.regime` and calling it "the HMM trend label", and `conditioning layer`'s
  Code surface line names `regime_writer.py` and `equity_regime_model.py`.
- CVR codes seeded by migration 307: `calm`, `elevated`, `turbulent` in namespace
  `regime_volatility`. The legacy `regime_hmm` namespace still exists and is still audited during
  the cutover window per plan 172-02.
- Test commands: `.venv/bin/pytest tests/unit/ -k "ic_engine or ensemble_trainer" -q`,
  `.venv/bin/pytest tests/unit/ -q`.
- DB access: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "..."`.
- Orphan-worker rule: `ic_engine.py` uses a `ProcessPoolExecutor`. If a run is killed, follow with
  `ps aux | grep ic_engine.py | awk '{print $2}' | xargs kill` and confirm zero remain.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scoped ic_engine --refresh against the volatility labels, with recorded evidence</name>
  <files>.planning/phases/172-hmm-regime-volatility-only-redesign/evidence/172-07-ic-refresh-scoped.json</files>
  <read_first>
    - `services/ic_engine.py` as plan 172-06 left it: `_assert_prerequisites`, the feature-matrix
      SELECT, and the `--refresh` fingerprint-bypass handling around lines 4977-4992.
    - `.planning/phases/172-hmm-regime-volatility-only-redesign/evidence/172-05-relabel-coverage.json`:
      which cells carry volatility labels, so the scope chosen is one the relabel actually covered.
    - `docs/plans/OOS-EVAL-PROTOCOL.md`: what `--training-window-end` must be set to so this run
      cannot consume the OOS holdout window.
    - `docs/foundation/performance-investigation-sop.md`: measure elapsed time and chunk state
      rather than theorizing about them.
    - `.planning/phases/172-hmm-regime-volatility-only-redesign/172-IC-ENGINE-CUTOVER.md`: plan
      172-06's audit, including the pre-run `feature_ic_scores` row count this task compares against.
  </read_first>
  <action>
Run `ic_engine.py --refresh` at a deliberately small scope. This is a validation run, not a corpus
recompute: a full pass is the multi-day job STATE.md records at roughly 70 hours, and nothing in
this phase needs one.

Choose the scope from cells the relabel actually labeled: three symbols at one timeframe. Use
`SPY`, one non-equity symbol present in the relabel coverage report, and one more equity symbol,
at `1d`. Record why each was chosen. Confirm from `evidence/172-05-relabel-coverage.json` that all
three cells have `verdict` equal to `labeled` before launching; running against a skipped cell
would test nothing.

Set `--training-window-end` explicitly to the value `docs/plans/OOS-EVAL-PROTOCOL.md` prescribes,
which is the OOS holdout clamp, not a bare max bar timestamp. The flag is required precisely so
this is never defaulted.

Before launching, record the pre-run control: `SELECT count(*) FROM feature_ic_scores`, and
`SELECT regime_scope, count(*) FROM feature_ic_scores GROUP BY 1`. Check for competing corpus-scale
compute with
`ps aux | grep -E 'ic_engine|backfill_feature_factory|regime_writer|ensemble_trainer' | grep -v grep`
and do not launch if another is running.

Run `--dry-run-validity` first with the identical scope and flags, record its printed skip/compute
partition, then run for real. Tee stdout to `logs/172-07-ic-refresh-scoped.log`.

After the run, build `evidence/172-07-ic-refresh-scoped.json` from direct queries, with keys:
`generated_at`, `scope` (symbols, tf, training_window_end, workers), `elapsed_s`,
`rows_before`, `rows_after`, `distinct_regimes` (from
`SELECT DISTINCT regime FROM feature_ic_scores WHERE symbol = ANY(<scope symbols>) AND tf = '<tf>' AND training_window_end = '<value>'`),
`regime_scope_breakdown` (the grouped count over the same scope), and `pooled_sentinel_rows` (the
count where `regime = '_pooled'`).

Interpret the result rather than only recording it. Non-pooled `symbol_hmm`-scope rows in this
scope must carry only volatility codes. `cross_sectional`-scope rows, if any appear, still carry
`market_regimes` labels and that is correct, not a contradiction; say so explicitly in the SUMMARY
so a reader does not mistake it for leakage of the retired vocabulary.

If the run produces zero new rows for the scope, that is a finding, not a pass. Investigate whether
the fingerprint bypass took effect and whether the cells had `forward_returns` coverage at the
chosen timeframe, and record the diagnosis.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json,pathlib
d=json.loads(pathlib.Path('.planning/phases/172-hmm-regime-volatility-only-redesign/evidence/172-07-ic-refresh-scoped.json').read_text())
assert d['rows_after']>=d['rows_before'], (d['rows_before'],d['rows_after'])
regs=set(d['distinct_regimes'])
allowed={'calm','elevated','turbulent','_pooled'}
stale=regs & {'trending_up','trending_down','ranging','transition_up','transition_down'}
assert not stale, ('retired trend labels written by a post-cutover run', sorted(stale))
unknown=regs-allowed
assert all(isinstance(r,str) for r in unknown), unknown
assert d['regime_scope_breakdown'], 'no scope breakdown recorded'
assert d['scope']['training_window_end'], 'training_window_end not recorded'
print('refresh evidence ok', sorted(regs))"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/ -k "ic_engine or ensemble_trainer" -q` is green before the run
      is launched, and the SUMMARY records the pass count.
    - The three scoped cells all have `verdict` equal to `labeled` in
      `evidence/172-05-relabel-coverage.json`; the SUMMARY names them and quotes their labeled row
      counts.
    - `--dry-run-validity` output is recorded in the SUMMARY, showing the fingerprint partition
      before the real run.
    - `evidence/172-07-ic-refresh-scoped.json` exists with all eight required keys.
    - No row in the scope carries any of `trending_up`, `trending_down`, `ranging`,
      `transition_up`, `transition_down` under a `training_window_end` equal to this run's value.
      Verify with a direct psql query and paste the result in the SUMMARY.
    - Every non-pooled `symbol_hmm`-scope row in the scope carries a code present in
      `SELECT code FROM controlled_vocabulary WHERE namespace = 'regime_volatility'`.
    - `logs/172-07-ic-refresh-scoped.log` exists and contains the run-complete log line.
    - `ps aux | grep ic_engine.py | grep -v grep` returns nothing after the run.
  </acceptance_criteria>
  <done>A real ic_engine run has written feature_ic_scores rows stratified on the volatility labels, with the before and after counts and the label set recorded as evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin ensemble_trainer's stratum source with a regression test</name>
  <files>tests/unit/services/test_ensemble_trainer_regime_source.py, .planning/phases/172-hmm-regime-volatility-only-redesign/172-DOWNSTREAM-VERIFICATION.md</files>
  <read_first>
    - `services/ensemble_trainer.py` lines 1-130: the module docstring's eligibility-filter
      statement and the filter constant at line 121.
    - `services/ensemble_trainer.py` lines 380-500: the eligibility-filter restatement and the
      `significance_desc` construction.
    - `services/ensemble_trainer.py` lines 730-800: the `SELECT DISTINCT tf, regime` stratum
      discovery and the per-stratum loop reading `stratum["regime"]`.
    - `services/ensemble_trainer.py` lines 1060-1140: the `ensemble_weights` INSERT keyed
      `(symbol, tf, regime, weight_version, feature_name)`.
    - `tests/unit/services/test_ensemble_trainer_alignment_gate.py`: the existing mock and async
      test conventions in this repo for this module.
    - `.planning/phases/172-hmm-regime-volatility-only-redesign/172-IC-ENGINE-CUTOVER.md`: plan
      172-06's `_POOLED_REGIME_SENTINEL` section, whose conclusion this task independently rechecks.
    - `CLAUDE.md` Key Decisions: the load-bearing statement that cross-sectional POOLED strata with
      `is_pooled = true` are the ensemble training eligibility source.
  </read_first>
  <behavior>
    - The stratum-discovery SQL contains all three of `symbol = 'POOLED'`, `is_pooled = true`, and
      `regime != '_pooled'`, and selects `DISTINCT tf, regime` from `feature_ic_scores`.
    - The stratum-discovery SQL does not reference `feature_vectors`, `regime_volatility`, or
      `regime_scope = 'symbol_hmm'`.
    - Given a stub result set of cross-sectional regime labels, the trainer iterates exactly those
      strata and keys its `ensemble_weights` rows on them.
    - Given a stub result set that also contains a volatility label, the trainer treats it as an
      ordinary opaque stratum key and does not special-case any label string, confirming the
      column-name repoint in plan 172-06 could not have changed its logic.
  </behavior>
  <action>
Write `tests/unit/services/test_ensemble_trainer_regime_source.py`. This is a new file; there is no
existing coverage of the stratum-source contract.

Assert the eligibility filter's three clauses are present in the query the module issues, reading
them from the module's own constant or SQL string rather than re-typing them in the test, so the
test fails if the filter is edited rather than passing against a stale copy. Assert the absence of
any `feature_vectors` or `regime_volatility` reference in that query.

Drive the stratum loop with a stubbed connection returning a small set of cross-sectional labels
and assert the strata iterated match. Add a second case whose stub includes `calm` alongside the
cross-sectional labels and assert it is handled identically, with no branch on the label string.
That second case is the actual claim under test: `ensemble_trainer` treats `regime` as an opaque
GROUP BY key, so it inherits whatever vocabulary `ic_engine` wrote and needed no repoint of its own.

Do not edit `services/ensemble_trainer.py` in this task. Plan 172-02 already added the eight new
columns to `_META_COLS`, which is the only change this module needed. If writing these tests
surfaces an actual dependency on the per-symbol label column, stop, do not patch it here, and
record it in the SUMMARY as a blocking finding, because that would mean the phase's scope was
mis-drawn and the plan checker needs to see it.

Start `172-DOWNSTREAM-VERIFICATION.md` with two sections. `## ensemble_trainer stratum source`
records the query text, the three filter clauses, the test file that pins them, and the conclusion
that no repoint was required, with the reasoning stated in its own words rather than by reference.
`## Scoped ic_engine refresh` summarises Task 1's evidence JSON: the scope, the before and after
row counts, the distinct labels, and the scope breakdown, plus the explicit note that
`cross_sectional`-scope rows still carry `market_regimes` labels by design.

State corrected facts plainly. No review narrative, no em dashes.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/services/test_ensemble_trainer_regime_source.py -q && .venv/bin/pytest tests/unit/ -k "ic_engine or ensemble_trainer" -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/services/test_ensemble_trainer_regime_source.py` exists and passes with at least
      four tests.
    - A test asserts the stratum query contains `symbol = 'POOLED'`, `is_pooled = true`, and
      `regime != '_pooled'`, sourced from the module rather than re-typed, and goes red if any
      clause is removed. Verify by temporarily removing one, confirming red, then restoring.
    - A test asserts the stratum query contains neither `feature_vectors` nor `regime_volatility`.
    - A test asserts a `calm` stratum is iterated identically to a cross-sectional stratum.
    - `git status --short` shows no modification to `services/ensemble_trainer.py` from this task.
    - `172-DOWNSTREAM-VERIFICATION.md` contains both required section headings and cites the real
      numbers from `evidence/172-07-ic-refresh-scoped.json`.
    - `.venv/bin/pytest tests/unit/ -q` is green.
    - `.venv/bin/ruff check .` and `.venv/bin/black --check .` are clean.
  </acceptance_criteria>
  <done>ensemble_trainer's independence from the per-symbol regime column is proven by test rather than by argument, and the downstream verification record carries the scoped run's real numbers.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the regime glossary entry and its affected siblings</name>
  <files>docs/foundation/glossary.md</files>
  <read_first>
    - `docs/foundation/glossary.md` lines 70-140 in full: the `regime` entry, the `regime_group`
      entry, and the `conditioning layer` entry, including every `**Not:**`, `**Banned:**`,
      `**Status:**`, `**Disambiguation:**`, and `**Code surface:**` line.
    - `docs/foundation/glossary.md` around `### regime classifier` (line 1309) and any other entry
      a `grep -n "feature_vectors.regime\b" docs/foundation/glossary.md` surfaces.
    - `.planning/phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-FINAL-VERDICT.md`
      sections 3 and 5: the evidence and the design decision the entry has to state on its own
      terms.
    - `production/migrations/307_regime_volatility_schema_apr_cvr.sql`: the exact CVR codes and
      their descriptions, so the glossary and the registry agree word for word on what each label
      means.
    - `CLAUDE.md` Glossary section: the one-definition-per-term rule and the rule that the glossary
      wins over existing code on collision.
  </read_first>
  <action>
Rewrite the `### regime` entry's idiosyncratic bullet. It currently reads that the idiosyncratic
regime is a per-symbol `GaussianHMM` state with 5 labels `trending_down` / `transition_down` /
`ranging` / `transition_up` / `trending_up`, fit from log-return, vol-of-vol and relative-volume
observations, stored in `feature_vectors.regime`. Replace it with the system that exists: a
per-symbol `GaussianHMM` state over a 2-dimensional observation vector of realized volatility and
volatility-of-volatility, K configured by `alpha.hmm_volatility.n_components`, labels `calm` /
`elevated` / `turbulent` ordered by realized volatility ascending, fit walk-forward on an expanding
window so no parameter is estimated from data later than the bar it labels, stored in
`feature_vectors.regime_volatility`.

Include the why, in the entry itself. Per CLAUDE.md's canonical-docs-standalone rule this entry
must state what, why, and what was considered instead, without pointing the reader at
`171-FINAL-VERDICT.md` or any `.planning/` file. The load-bearing facts to state in the entry's own
words: a scrambled-data null-arm control found that only the volatility dimensions carry structure
distinguishable from a model's tendency to split any smooth series in half; the trend and volume
dimensions did not, so they were removed rather than deferred; and the previous 5-label vocabulary
named directions the model was not actually separating on.

Update the systematic bullet only where it is wrong. The cross-sectional `market_regimes` system is
unchanged by this phase and its 9-label description stays as written.

Update the `**Code surface:**` line to name `feature_vectors.regime_volatility` and
`regime_writer.py --regime-column regime_volatility`, and keep `market_regimes` on it. The existing
line names `equity_regime_model.py` as the systematic writer; correct that to
`cross_sectional_regime_model.py`, which replaced it in Phase 144, since the line is being touched
anyway and a known-wrong filename in a canonical doc is worse than an untouched one.

Leave the `**Banned:**` line byte-identical to its pre-edit value. Do not add or remove entries
from it as part of this rewrite, and do not restate its contents anywhere outside the glossary
itself: those synonyms are enforced-banned terms and the pre-commit glossary check flags them in
any tracked file. Capture the line with `git show HEAD:docs/foundation/glossary.md` before editing
and diff it afterwards.

In `**Disambiguation:**`, the existing `volatility_regime` bullet reads "a sub-classification of
regime by realized vol level", which now collides with the idiosyncratic regime itself. Resolve the
collision rather than leaving two overlapping definitions: either fold that bullet into the
idiosyncratic-regime line or state precisely what distinguishes the two. CLAUDE.md's
one-definition-per-term rule makes leaving both a real defect, not a stylistic choice.

Update `### regime_group`'s contrast paragraph, which describes `feature_vectors.regime` as "the
per-symbol idiosyncratic regime (HMM trend label)". Replace the column name and drop the word
trend. Update `### conditioning layer`'s Code surface line the same way it names its writers.

Run `grep -n "feature_vectors\.regime\b" docs/foundation/glossary.md` after editing and confirm
every remaining hit is a deliberate historical reference rather than a current-state claim. Record
the final hit list in the SUMMARY.

Do not delete the legacy vocabulary from the document entirely. One sentence noting that
`feature_vectors.regime` still exists carrying the retired trend vocabulary during the phased
cutover, and is no longer the stratification source, is accurate and prevents a reader from
concluding the column was dropped.

State corrected facts plainly. No review-narrative annotations, no em dashes.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import pathlib,re,subprocess
t=pathlib.Path('docs/foundation/glossary.md').read_text()
i=t.index('### \`regime\`'); j=t.index('### \`regime_group\`')
entry=t[i:j]
for s in ('regime_volatility','calm','elevated','turbulent','realized'):
    assert s in entry, s
for s in ('trending_up','transition_down'):
    assert s not in entry or 'retired' in entry.lower(), ('stale trend vocabulary presented as current: '+s)
banned_now=[l for l in entry.splitlines() if l.startswith('**Banned:**')]
old=subprocess.run(['git','show','HEAD:docs/foundation/glossary.md'],capture_output=True,text=True).stdout
oi=old.index('### \`regime\`'); oj=old.index('### \`regime_group\`')
banned_old=[l for l in old[oi:oj].splitlines() if l.startswith('**Banned:**')]
assert banned_now==banned_old, 'Banned line altered'
assert '171-FINAL-VERDICT' not in entry and '.planning/' not in entry, 'entry points at a planning doc'
assert '—' not in entry, 'em dash found'
print('glossary entry ok', len(entry.splitlines()), 'lines')"</automated>
  </verify>
  <acceptance_criteria>
    - The `### regime` entry's idiosyncratic bullet names `feature_vectors.regime_volatility`, the
      two observation dimensions, the `calm` / `elevated` / `turbulent` vocabulary, and the
      walk-forward expanding-window fit.
    - The entry states, in its own words, the null-arm evidence that removed the trend and volume
      dimensions, and contains no reference to `171-FINAL-VERDICT.md` or any `.planning/` path.
    - The `**Banned:**` line is byte-identical to its pre-edit value, confirmed by diffing
      against `git show HEAD:docs/foundation/glossary.md`.
    - The `**Code surface:**` line names `feature_vectors.regime_volatility`, `market_regimes`, and
      `cross_sectional_regime_model.py`, and no longer names `equity_regime_model.py`.
    - The `volatility_regime` disambiguation bullet no longer defines a concept that overlaps the
      idiosyncratic regime without distinguishing it.
    - `### regime_group`'s contrast paragraph names `feature_vectors.regime_volatility` and does not
      call it a trend label.
    - `grep -c $'—' docs/foundation/glossary.md` returns the same count as before this task, and
      zero within the edited entries.
    - `git log --oneline -1 -- docs/foundation/glossary.md` shows this phase's commit, satisfying
      172-RESEARCH.md Pitfall 3's own warning sign.
  </acceptance_criteria>
  <done>The canonical definition of the idiosyncratic regime describes the system that exists, stands on its own with its rationale, and no sibling entry contradicts it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | A bounded internal batch run, a unit test, and a documentation edit. No external input, no network-facing surface, no auth or session surface, no secrets, no cryptographic material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-07-NA | n/a | scoped `ic_engine.py` run, `ensemble_trainer` tests, glossary | accept | No ASVS threats identified. Same trust boundary as the existing `ic_engine.py` batch job per 172-RESEARCH.md's Security Domain section. |
| T-172-07-D1 | Data integrity (MEDIUM) | `feature_ic_scores` | mitigate | The refresh is scoped to three cells at one timeframe with an explicit `--training-window-end`, and before and after row counts are recorded; the OOS holdout cannot be consumed because the flag is required and set from the protocol document. |
| T-172-07-D2 | Stale canonical documentation | `docs/foundation/glossary.md` | mitigate | Task 3 is a required deliverable with an automated check that the entry names the shipped vocabulary and does not present the retired one as current. |
| T-172-07-SC | Tampering | package installs | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/ -k "ic_engine or ensemble_trainer" -q` green, run both before and
  after the scoped `ic_engine.py --refresh`.
- `.venv/bin/pytest tests/unit/ -q` green.
- `.venv/bin/ruff check .` and `.venv/bin/black --check .` clean.
- `SELECT count(*) FROM feature_ic_scores` recorded before and after the scoped run.
- No row under this run's `training_window_end` carries a retired trend label.
- `git status --short` shows no unexpected file changes beyond `files_modified` and the run log.
</verification>

<success_criteria>
- A real scoped `ic_engine.py --refresh` has written `feature_ic_scores` rows stratified on
  `calm` / `elevated` / `turbulent`, with evidence recorded as JSON.
- `ensemble_trainer`'s cross-sectional POOLED stratum source is pinned by a new regression test,
  with no source change required or made.
- `docs/foundation/glossary.md`'s `regime` entry, and the sibling entries that referenced it,
  describe the idiosyncratic regime that now exists, with rationale stated in the document itself.
- `172-DOWNSTREAM-VERIFICATION.md` records both halves with real numbers.
</success_criteria>

<output>
Create `.planning/phases/172-hmm-regime-volatility-only-redesign/172-07-SUMMARY.md` when done.
</output>
