---
phase: 151-feature-primitives-expansion-theory-motivated-interaction-la
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/schemas.py
  - src/intelligence/feature_factory.py
  - src/intelligence/features/feature_vector_persistence.py
  - services/feature_vector_pipeline.py
  - production/migrations/259_calendar_velocity_atomics.sql
  - tests/unit/intelligence/test_feature_factory_batch.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "FeatureVector carries 10 new tier-0 atomic fields: quarter_cycle_sin/cos, tdom_sin/cos, minute_of_hour_sin/cos, momentum_z_velocity_fast/mid/slow, vwap_dev_sigma_velocity"
    - "feature_registry row count still equals len(dataclasses.fields(FeatureVector)) after the migration"
    - "All 10 new fields are persisted (present in FEATURE_VECTOR_INSERT_SQL, not silently discarded)"
    - "The 2 new window parameters are APR-backed, not Python literals"
    - "Every new feature_registry row has a matching concept_registry (domain='feature') + concept_gate row in the same migration, satisfying Phase 170's ic_engine parity precondition"
  artifacts:
    - path: "production/migrations/259_calendar_velocity_atomics.sql"
      provides: "10 feature_vectors columns + 10 feature_registry rows + 10 paired concept_registry/concept_gate rows (Phase 170 parity) + 2 APR key triplets"
      contains: "quarter_cycle_sin"
    - path: "src/intelligence/schemas.py"
      provides: "10 new FeatureVector dataclass fields + updated Total tally"
      contains: "momentum_z_velocity_fast"
    - path: "src/intelligence/feature_factory.py"
      provides: "compute helpers, FEATURE_VECTOR_DOMAIN entries, compute() wiring, cold-start fallbacks"
      contains: "_minute_of_hour_encoding"
    - path: "src/intelligence/features/feature_vector_persistence.py"
      provides: "_PHASE151_CALENDAR_VELOCITY_FIELD_NAMES slice appended to the INSERT column contract"
      contains: "_PHASE151_CALENDAR_VELOCITY_FIELD_NAMES"
  key_links:
    - from: "src/intelligence/schemas.py"
      to: "src/intelligence/features/feature_vector_persistence.py"
      via: "dataclasses.fields(FeatureVector) slice by field name"
      pattern: "_ALL_FEATURE_VECTOR_FIELD_NAMES.index"
    - from: "production/migrations/259_calendar_velocity_atomics.sql"
      to: "src/intelligence/feature_registry_service.py"
      via: "row-count alignment gate _REGISTRY_ROW_COUNT"
      pattern: "INSERT INTO feature_registry"
---

<objective>
Add the first batch of Phase 151 tier-0 atomic primitives: 6 calendar coordinates (todo 104)
and 4 velocity primitives (todo 123), 10 new `feature_vectors` columns total.

Purpose: These are the cheapest, most self-contained of the phase's 28 atomic candidates —
pure functions of `bar_ts` (calendar) or of an already-computed z-score series (velocity). No
cross-asset bar fetches, no new symbols, no new DAG surface. Landing them first establishes the
column-addition mechanics (schemas → factory → persistence → migration → tests) that plans
151-03 and 151-04 repeat.

Output: 10 new `FeatureVector` fields, 10 `feature_vectors` columns, 10 `feature_registry` rows
(`tier='0_atomic'`, `added_phase='151'`), 2 new APR keys, unit test coverage.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-RESEARCH.md
@.planning/phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-PATTERNS.md
@CLAUDE.md

Reference (read as needed, do not read in full up front):
- `docs/research/signal-temporal-atomic-primitives.md` — exact formulas for `quarter_cycle_sin/cos`,
  `tdom_sin/cos`, `minute_of_hour_sin/cos` (lines 167-169) and the rejected-alternatives table
- `production/migrations/255_vp_structural_primitives.sql` — the migration template
</context>

<interfaces>
Live contracts the executor must build against (verified 2026-07-24, do not re-derive):

`src/intelligence/schemas.py` — `FeatureVector` frozen dataclass, currently 172 fields.
Class docstring line ~1205 states `172 orthogonal feature primitives` and the tally block ends
with `Total: 172 (164 required + 8 optional ...)`. Both must be updated to 182.
Field order is binding (`schemas.py:1215` "Groups and field order are binding").

`src/intelligence/feature_registry_service.py:44`
  `_REGISTRY_ROW_COUNT = len(dataclasses.fields(FeatureVector))`
  Hard `RuntimeError` at `:110` and `:148` if `SELECT count(*) FROM feature_registry` differs.
  Live counts today: `0_atomic`=135, `1_interaction`=8, `2_theory`=29, total 172.

`src/intelligence/features/feature_vector_persistence.py` — canonical INSERT contract.
Existing named slices, each derived by field name (never hand-typed):
  `_RENAISSANCE_PRIMITIVE_FIELD_NAMES` (body_ratio .. vol_skew_product)
  `_CANARY_FIELD_NAMES` (canary_noise_gaussian .. canary_acausal_placebo)
  `_STRUCTURAL_VP_SR_FIELD_NAMES` (nearest_hvn_above_dist_atr .. sr_level_count)
Consumed at `:177-179` (column list), `:200-221` (placeholder ranges), `:231-235` (`_TOTAL_COLUMNS`).
A new field that is not inside one of these slices is computed and then SILENTLY DISCARDED —
this is the documented 2026-07-08 incident (91/152 columns 100% NULL corpus-wide).

`src/intelligence/feature_factory.py` existing helpers to model on:
  `_dow_encoding(bar_ts) -> tuple[float, float]`   (line ~1623)
  `_quarter_position(bar_ts) -> float`             (line ~2842)
  `_vol_velocity_z_series_full(atr_z: np.ndarray, window: int) -> np.ndarray`  (line ~2748)
  `_fixed_window_zscore_series(arr, window) -> np.ndarray`
  `_series_last(arr: np.ndarray, fallback: float) -> float`  (line 3108)
  `FEATURE_VECTOR_DOMAIN: dict[str, str]` (line 62) — values used here: `"calendar"`, `"quant"`
  `_PrecomputedSeries` dataclass (line 2865) + `_precompute_series()` builder
  `_cold_start_vector(cache, tf) -> FeatureVector` (line 4727)

`FeatureFactoryConfig` (feature_factory.py:268) — frozen APR container. Existing sibling key:
  `vol_velocity_window: int  # feature.vol_velocity.window` (live default 14)

Existing `feature_registry` column contract (from live rows):
  `normalization` ∈ {`bounded_signed`, `bounded_unsigned`, `unbounded_ratio`, `unbounded_signed`, `z_scored`}
  `group_name` ∈ {calendar, control, cross_tf, macro, momentum, oscillator, regime, session, structure, volatility, volume}
  Live precedent: `dow_sin` → (`calendar`, `bounded_signed`, linear_ready=`t`);
                  `vol_velocity_z` → (`volatility`, `z_scored`, linear_ready=`t`);
                  `vwap_dev_sigma` → (`volume`, `z_scored`, linear_ready=`t`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add 6 calendar coordinate primitives</name>
  <files>src/intelligence/schemas.py, src/intelligence/feature_factory.py, src/intelligence/features/feature_vector_persistence.py</files>
  <read_first>
    - src/intelligence/schemas.py lines 1204-1300 (FeatureVector docstring + Calendar field block)
    - src/intelligence/feature_factory.py lines 55-150 (FEATURE_VECTOR_DOMAIN)
    - src/intelligence/feature_factory.py lines 1615-1640 (_dow_encoding)
    - src/intelligence/feature_factory.py lines 2835-2865 (_quarter_position, _days_to_month_end_fraction)
    - src/intelligence/feature_factory.py lines 4325-4345 (compute() calendar block)
    - src/intelligence/feature_factory.py lines 4727-4830 (_cold_start_vector)
    - src/intelligence/features/feature_vector_persistence.py lines 75-115 and 170-240
    - docs/research/signal-temporal-atomic-primitives.md lines 160-200
  </read_first>
  <action>
Add exactly 6 new `float` (non-Optional) fields to `FeatureVector`, appended to the existing
Calendar group block in `src/intelligence/schemas.py`, in this order:
`quarter_cycle_sin`, `quarter_cycle_cos`, `tdom_sin`, `tdom_cos`, `minute_of_hour_sin`,
`minute_of_hour_cos`.

Update the class docstring in the same edit: the opening line `172 orthogonal feature
primitives` and the `Total: 172 (164 required + 8 optional ...)` tally. After this task alone the
count is 178; Task 2 takes it to 182 — update to the final number the plan lands (182) only after
Task 2, or update incrementally and let Task 2 correct it. Also extend the `Calendar (11): ...`
group line to `Calendar (17): ...` naming the 6 additions.

In `src/intelligence/feature_factory.py` add three pure helper functions near the existing
`_dow_encoding` / `_quarter_position` block. All take a timezone-aware UTC `bar_ts: datetime` and
return `tuple[float, float]`:

- `_quarter_cycle_encoding(bar_ts)` — first circular harmonic of the EXISTING `_quarter_position()`
  value. Call `_quarter_position(bar_ts)` directly, do not recompute the position. Return
  `(sin(2*pi*qp), cos(2*pi*qp))`.
- `_tdom_encoding(bar_ts)` — trading-day-of-month. `t` = count of Mon-Fri weekdays from the 1st of
  the month through `bar_ts.date()` inclusive; `W` = total Mon-Fri weekday count in that calendar
  month. Return `(sin(2*pi*t/W), cos(2*pi*t/W))`. Weekday counting is closed-form arithmetic;
  deliberately ignores market holidays (per the source doc's rejection of a nonstationary holiday
  table). Guard `W <= 0` → return `(0.0, 0.0)`.
- `_minute_of_hour_encoding(bar_ts)` — `(sin(2*pi*bar_ts.minute/60), cos(2*pi*bar_ts.minute/60))`.
  Constant at 1h/1d by construction; that is expected and correct, not a bug.

Add 6 `FEATURE_VECTOR_DOMAIN` entries, all mapped to `"calendar"`, in the existing
`# Calendar / session` block.

Wire all 6 into the `FeatureVector(...)` constructor call inside `compute()` (the calendar block
around line 4332) and into `compute_batch()`'s per-bar constructor. Calendar coordinates need no
`_PrecomputedSeries` entry and no `_series_full` variant — they are per-bar pure functions of
`bar_ts`, matching the `_in_ny_session` / `_dow_encoding` precedent.

Add all 6 to `_cold_start_vector()`'s `FeatureVector(...)` call. Cold start still has a valid
`bar_ts`, so compute the real value there rather than defaulting to 0.0.

**Do NOT edit `src/intelligence/features/feature_vector_persistence.py` in this task.** All 10 new
fields - this task's 6 calendar coordinates plus Task 2's 4 velocity primitives - are covered by ONE
derived slice, `_PHASE151_CALENDAR_VELOCITY_FIELD_NAMES`, and Task 2 writes that slice once, spanning
`quarter_cycle_sin` .. `vwap_dev_sigma_velocity`. Writing it here would mean either writing it twice
or writing it over a half-complete field run.

What this task must do so Task 2's single slice is valid: declare the 10 fields CONTIGUOUSLY in
`schemas.py` - this task's 6 calendar fields immediately followed by Task 2's 4 velocity fields, with
nothing between them - because the slice is an index range over `_ALL_FEATURE_VECTOR_FIELD_NAMES`,
not a hand-typed list. This task's scope is the `schemas.py` fields plus the factory wiring, nothing
more.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_feature_factory_batch.py tests/unit/test_feature_vector_persistence_completeness.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "quarter_cycle_sin\|quarter_cycle_cos\|tdom_sin\|tdom_cos\|minute_of_hour_sin\|minute_of_hour_cos" src/intelligence/schemas.py` returns 6
    - `python -c "import dataclasses; from src.intelligence.schemas import FeatureVector; print(len(dataclasses.fields(FeatureVector)))"` prints 178
    - All 6 names appear as keys in `FEATURE_VECTOR_DOMAIN` mapped to the literal string `"calendar"`
    - `_quarter_cycle_encoding` body contains a call to `_quarter_position(` (reuses, does not recompute)
    - Calling `FeatureFactory.compute()` on a bar with `bar_ts` at minute 0 yields `minute_of_hour_sin == 0.0` and `minute_of_hour_cos == 1.0`
    - `_cold_start_vector` constructs without `TypeError` (proved by the pytest run above)
  </acceptance_criteria>
  <done>6 calendar fields exist on FeatureVector, compute in both live and batch paths, appear in FEATURE_VECTOR_DOMAIN, and cold-start constructs cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add 4 velocity primitives and close the persistence contract</name>
  <files>src/intelligence/schemas.py, src/intelligence/feature_factory.py, src/intelligence/features/feature_vector_persistence.py</files>
  <read_first>
    - src/intelligence/feature_factory.py lines 2740-2770 (_vol_velocity_z_series_full)
    - src/intelligence/feature_factory.py lines 2865-2960 (_PrecomputedSeries fields)
    - src/intelligence/feature_factory.py lines 2975-3105 (_precompute_series body)
    - src/intelligence/feature_factory.py lines 268-300 and 380-480 (FeatureFactoryConfig docstring + fields)
    - src/intelligence/features/feature_vector_persistence.py lines 75-115, 170-245
  </read_first>
  <action>
Add exactly 4 new `float` fields to `FeatureVector`, declared IMMEDIATELY AFTER the 6 calendar
fields from Task 1 so all 10 form one contiguous run:
`momentum_z_velocity_fast`, `momentum_z_velocity_mid`, `momentum_z_velocity_slow`,
`vwap_dev_sigma_velocity`.

Set the final docstring tally to `Total: 182 (174 required + 8 optional ...)` and update the
opening line to `182 orthogonal feature primitives`.

Add 2 new `FeatureFactoryConfig` int fields with their APR-key comments, mirroring the existing
`vol_velocity_window: int  # feature.vol_velocity.window` line:
  `momentum_velocity_window: int  # feature.momentum_velocity.window`
  `vwap_velocity_window: int      # feature.vwap_velocity.window`
Add matching lines to the `FeatureFactoryConfig` docstring's APR-key list. Do NOT reuse
`vol_velocity_window` for either — momentum-velocity and VWAP-velocity are semantically distinct
families per `docs/foundation/naming-system.md` §7's gradient rule, and todo 123's Fable review
explicitly requires a separate key per family.

Wire both keys into every `FeatureFactoryConfig` construction site:
  `services/feature_vector_pipeline.py` — the `_prewarm_threshold_config` key list around line 516
  (add `("feature.momentum_velocity.window", 14)` and `("feature.vwap_velocity.window", 14)`) and
  the constructor block around line 656 (`momentum_velocity_window=_int("feature.momentum_velocity.window", 14)`,
  same shape for the VWAP key).
  `services/backfill_feature_factory.py` — the config-build block around line 425
  (`momentum_velocity_window=int(cfg.get_sync("feature.momentum_velocity.window", 14))`, same for VWAP).

Add 4 compute functions. All four are the identical construction already proven by
`_vol_velocity_z_series_full`: first difference of an already-computed z-score series, zero-padded
at index 0, then re-z-scored over a fixed window via `_fixed_window_zscore_series`. Reuse
`_vol_velocity_z_series_full` directly by passing the appropriate source array rather than writing
four near-duplicate bodies — its signature `(series: np.ndarray, window: int) -> np.ndarray` is
already generic over the input array; if the parameter is named `atr_z`, rename it to a neutral
name (e.g. `series`) and sweep call sites, keeping behavior byte-identical.

Source arrays and windows:
  `momentum_z_velocity_fast` ← `s.momentum_z_fast`, window `config.momentum_velocity_window`
  `momentum_z_velocity_mid`  ← `s.momentum_z_mid`,  window `config.momentum_velocity_window`
  `momentum_z_velocity_slow` ← `s.momentum_z_slow`, window `config.momentum_velocity_window`
  `vwap_dev_sigma_velocity`  ← `s.vwap_dev_sigma`,  window `config.vwap_velocity_window`

Add 4 `_PrecomputedSeries` ndarray fields with the same names, populate them in
`_precompute_series()`, and read them at the constructor call sites via
`_series_last(s.<field>, 0.0)`. Add 4 `_cold_start_vector()` entries defaulting to `0.0` (a
velocity is undefined with fewer than 2 bars; 0.0 is the correct neutral, matching
`vol_velocity_z`'s own cold-start default).

Add 4 `FEATURE_VECTOR_DOMAIN` entries, all `"quant"` (matching `vol_velocity_z`'s existing
`"quant"` mapping), placed in the momentum / volume blocks respectively.

Now close the persistence contract in `src/intelligence/features/feature_vector_persistence.py`:
1. Add a module-docstring paragraph dated 2026-07-24 recording the extension from 181 to 191
   columns (migration 259, Phase 151 Plan 01), matching the wording style of the existing
   2026-07-08 / 2026-07-11 / 2026-07-23 paragraphs.
2. Add the derived slice after `_STRUCTURAL_VP_SR_FIELD_NAMES`:
   `_PHASE151_CALENDAR_VELOCITY_FIELD_NAMES` spanning index of `quarter_cycle_sin` through index
   of `vwap_dev_sigma_velocity` inclusive — exactly the `_STRUCTURAL_VP_SR_FIELD_NAMES` idiom.
3. Append `{", ".join(_PHASE151_CALENDAR_VELOCITY_FIELD_NAMES)}` to the column list in
   `FEATURE_VECTOR_INSERT_SQL` (after `_STRUCTURAL_VP_SR_FIELD_NAMES`), append the matching
   `$N` placeholder range block, and extend `_TOTAL_COLUMNS` with
   `+ len(_PHASE151_CALENDAR_VELOCITY_FIELD_NAMES)`.
4. Update `feature_vector_to_insert_params`' docstring column map with a
   `$182-$191: 10 calendar/velocity fields (migration 259, Phase 151 Plan 01)` line and change
   `181-element` to `191-element` in both the summary line and the Returns section.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_feature_vector_persistence_completeness.py tests/unit/intelligence/test_feature_factory_batch.py tests/unit/intelligence/test_feature_factory_p7.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import dataclasses; from src.intelligence.schemas import FeatureVector; print(len(dataclasses.fields(FeatureVector)))"` prints 182
    - `python -c "from src.intelligence.features.feature_vector_persistence import FEATURE_VECTOR_INSERT_SQL_PSYCOPG2 as s; print(s.count('%s'))"` prints 191
    - `test_every_feature_vector_field_is_a_sql_column` passes (all 10 new names present as INSERT columns)
    - `test_insert_params_tuple_length_matches_sql` passes
    - `grep -n "feature.momentum_velocity.window\|feature.vwap_velocity.window" services/feature_vector_pipeline.py services/backfill_feature_factory.py` returns at least 4 lines (2 keys x 2 files)
    - `grep -v '^#' src/intelligence/feature_factory.py | grep -c "vol_velocity_window"` is unchanged from before this task (no key reuse for the new families)
  </acceptance_criteria>
  <done>All 10 Plan-01 fields exist, compute, and reach the INSERT tuple; both new APR keys are read via ConfigService at every FeatureFactoryConfig construction site with no hardcoded fallback other than the documented seed default.</done>
</task>

<task type="auto">
  <name>Task 3: Migration 259 and unit tests</name>
  <files>production/migrations/259_calendar_velocity_atomics.sql, tests/unit/intelligence/test_feature_factory_batch.py</files>
  <read_first>
    - production/migrations/255_vp_structural_primitives.sql (full file — header note, DDL, feature_registry INSERT, config triplets)
    - production/migrations/206_partial_ic_interaction_primitives.sql lines 44-91 (second APR-triplet example)
    - tests/unit/intelligence/test_feature_factory_batch.py (existing per-family test shape)
  </read_first>
  <action>
FIRST, re-verify the next-free migration number:
`ls production/migrations/ | sort -t_ -k1 -n | tail -5`. As of 2026-07-24 the latest is
`258_curve_credit_causal_rank_calibration.sql`, so 259 is the provisional target. If 259 is taken
by a concurrent session, use the actual next-free number and update the filename, the
`changed_by='migration_259'` strings, and every reference in this plan's SUMMARY. Migration 255's
own header documents this exact collision happening twice before — copy that header-note pattern.

Write `production/migrations/259_calendar_velocity_atomics.sql`, `BEGIN;`/`COMMIT;`-wrapped,
following migration 255's section structure exactly:

Section 1 — header comment block: migration number, Phase 151 Plan 01, the numbering-collision
note, and a statement that every `feature_vectors` column is `DOUBLE PRECISION` (verified: no
column in this table uses `real`/float32).

Section 2 — DDL. 10 statements of the form
`ALTER TABLE feature_vectors ADD COLUMN IF NOT EXISTS <name> DOUBLE PRECISION;` in dataclass field
order (`quarter_cycle_sin`, `quarter_cycle_cos`, `tdom_sin`, `tdom_cos`, `minute_of_hour_sin`,
`minute_of_hour_cos`, `momentum_z_velocity_fast`, `momentum_z_velocity_mid`,
`momentum_z_velocity_slow`, `vwap_dev_sigma_velocity`), each followed by a
`COMMENT ON COLUMN feature_vectors.<name> IS '...  Phase 151.';` stating the formula.
`ADD COLUMN IF NOT EXISTS` with no DEFAULT is metadata-only against the compressed hypertable —
no decompression step.

Section 3 — `INSERT INTO feature_registry (feature_name, group_name, tier, formula_short,
normalization, linear_ready, requires_htf, status, added_phase) VALUES ... ON CONFLICT
(feature_name) DO NOTHING;` with these exact 10 rows:

| feature_name | group_name | normalization | linear_ready | formula_short |
|---|---|---|---|---|
| quarter_cycle_sin | calendar | bounded_signed | true | sin(2*pi*quarter_position) |
| quarter_cycle_cos | calendar | bounded_signed | true | cos(2*pi*quarter_position) |
| tdom_sin | calendar | bounded_signed | true | sin(2*pi*trading_day_of_month/weekdays_in_month) |
| tdom_cos | calendar | bounded_signed | true | cos(2*pi*trading_day_of_month/weekdays_in_month) |
| minute_of_hour_sin | calendar | bounded_signed | true | sin(2*pi*minute/60) |
| minute_of_hour_cos | calendar | bounded_signed | true | cos(2*pi*minute/60) |
| momentum_z_velocity_fast | momentum | z_scored | true | zscore(diff(momentum_z_fast)) |
| momentum_z_velocity_mid | momentum | z_scored | true | zscore(diff(momentum_z_mid)) |
| momentum_z_velocity_slow | momentum | z_scored | true | zscore(diff(momentum_z_slow)) |
| vwap_dev_sigma_velocity | volume | z_scored | true | zscore(diff(vwap_dev_sigma)) |

All 10 rows: `tier='0_atomic'`, `requires_htf=false`, `status='active'`, `added_phase='151'`.

Section 4 — APR triplets. Two keys, each with the full
`config_schema` / `config_state` / `config_history` triplet (255's exact shape,
`ON CONFLICT (config_key) DO NOTHING` on the first two, plain INSERT on history with
`changed_by='migration_259'`):
  `feature.momentum_velocity.window` — value_type `int`, default `'14'`, min 2, max 200,
    description `[conventional] Bar-lag window for the first-difference-then-z-score construction
    behind momentum_z_velocity_fast/mid/slow. Seeded to match feature.vol_velocity.window's live
    value (14). Phase 151. ML learning target: yes.`
  `feature.vwap_velocity.window` — value_type `int`, default `'14'`, min 2, max 200,
    description `[conventional] Bar-lag window for vwap_dev_sigma_velocity. Separate key from
    feature.vol_velocity.window / feature.momentum_velocity.window per naming-system.md section 7
    (semantically distinct family). Phase 151. ML learning target: yes.`

Section 5 — Phase 170 `concept_registry` parity (added retroactively: this plan was written
2026-07-24, before Phase 170 shipped its dual-write cutover 2026-08-04/05). `ic_engine.py`'s
`_apply_feature_transitions` now runs a PARITY PRECONDITION on every corpus pass that hard-stops
if any `feature_registry` row lacks a `concept_registry` counterpart (Phase 170 Plan 06,
T-170-17) — without this section, the next `ic_engine` run after this migration lands would
treat these 10 rows as a split-brain and refuse to apply any transitions at all, for every
feature, not just these 10. Add immediately after Section 4, same file, same `BEGIN`/`COMMIT`,
deriving every field from the `feature_registry` rows Section 3 just inserted (not re-typed
literals, so the two tables cannot drift) — field mapping matches migration 284's feature-domain
seed exactly:

```sql
-- Phase 170 parity: concept_registry/concept_gate mirror for this migration's 10 rows.
INSERT INTO concept_registry
    (domain, name, description, status, enabled, group_name, is_control, control_expectation,
     added_phase, created_at, metadata)
SELECT
    'feature', fr.feature_name, fr.formula_short, fr.status, (fr.status = 'active'),
    fr.group_name, fr.is_control, fr.control_expectation, fr.added_phase, fr.added_at,
    jsonb_strip_nulls(jsonb_build_object(
        'tier', fr.tier, 'formula_short', fr.formula_short, 'normalization', fr.normalization,
        'linear_ready', fr.linear_ready, 'requires_htf', fr.requires_htf,
        'window_apr_keys', to_jsonb(fr.window_apr_keys), 'source_dims', to_jsonb(fr.source_dims),
        'notes', fr.notes, 'apr_namespace', 'feature.'
    ))
FROM feature_registry fr
WHERE fr.feature_name IN (
    'quarter_cycle_sin', 'quarter_cycle_cos', 'tdom_sin', 'tdom_cos',
    'minute_of_hour_sin', 'minute_of_hour_cos', 'momentum_z_velocity_fast',
    'momentum_z_velocity_mid', 'momentum_z_velocity_slow', 'vwap_dev_sigma_velocity'
)
ON CONFLICT (domain, name) DO NOTHING;

INSERT INTO concept_gate
    (concept_id, gate_metric_name, gate_eval_method, min_gate_metric, min_gate_n,
     fdr_required, fdr_alpha)
SELECT cr.concept_id, 'ic_sharpe_hac', 'bootstrap_ci', fr.min_ic_sharpe, fr.min_ic_n,
       fr.fdr_required, fr.fdr_alpha
FROM feature_registry fr
JOIN concept_registry cr ON cr.domain = 'feature' AND cr.name = fr.feature_name
WHERE fr.feature_name IN (
    'quarter_cycle_sin', 'quarter_cycle_cos', 'tdom_sin', 'tdom_cos',
    'minute_of_hour_sin', 'minute_of_hour_cos', 'momentum_z_velocity_fast',
    'momentum_z_velocity_mid', 'momentum_z_velocity_slow', 'vwap_dev_sigma_velocity'
)
ON CONFLICT (concept_id) DO NOTHING;
```

Add to this task's acceptance criteria:
- `SELECT count(*) FROM concept_registry WHERE domain='feature' AND added_phase='151' AND name IN ('quarter_cycle_sin','quarter_cycle_cos','tdom_sin','tdom_cos','minute_of_hour_sin','minute_of_hour_cos','momentum_z_velocity_fast','momentum_z_velocity_mid','momentum_z_velocity_slow','vwap_dev_sigma_velocity');` returns 10
- `SELECT count(*) FROM concept_gate cg JOIN concept_registry cr USING (concept_id) WHERE cr.domain='feature' AND cr.added_phase='151' AND cr.name IN ('quarter_cycle_sin','quarter_cycle_cos','tdom_sin','tdom_cos','minute_of_hour_sin','minute_of_hour_cos','momentum_z_velocity_fast','momentum_z_velocity_mid','momentum_z_velocity_slow','vwap_dev_sigma_velocity');` returns 10

Apply the migration:
`PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/259_calendar_velocity_atomics.sql`

Then add unit tests to `tests/unit/intelligence/test_feature_factory_batch.py` following that
file's existing per-family test shape:
- `minute_of_hour_sin/cos` at minute 0 → (0.0, 1.0); at minute 15 → (1.0, ~0.0); at minute 30 →
  (~0.0, -1.0).
- `tdom_sin/cos` for a known month (assert `t` counts weekdays only — e.g. for a Monday that is
  the 1st of the month, `t == 1`; for the following Monday, `t == 6`).
- `quarter_cycle_sin/cos` equals `sin/cos(2*pi*_quarter_position(bar_ts))` for a sampled set of
  timestamps (an equivalence assertion, not a re-derivation).
- Velocity family: on a synthetic monotonically-increasing `momentum_z_fast` series the velocity
  is positive; on a flat series it is 0.0; the value at index 0 is 0.0 (zero-padded).
- Batch/live parity: `compute_batch()`'s last-bar output for all 10 new fields equals
  `compute()`'s output on the same bar window (extend the existing parity test in
  `tests/unit/intelligence/test_feature_factory_batch_parity.py` if that file already carries the
  parity harness).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM feature_registry;"` returns 182
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM feature_registry WHERE added_phase='151';"` returns 10
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM information_schema.columns WHERE table_name='feature_vectors' AND column_name IN ('quarter_cycle_sin','quarter_cycle_cos','tdom_sin','tdom_cos','minute_of_hour_sin','minute_of_hour_cos','momentum_z_velocity_fast','momentum_z_velocity_mid','momentum_z_velocity_slow','vwap_dev_sigma_velocity');"` returns 10
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM config_state WHERE config_key IN ('feature.momentum_velocity.window','feature.vwap_velocity.window');"` returns 2
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM config_history WHERE changed_by='migration_259';"` returns 2
    - `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
  <done>Migration 259 applied; registry row count matches the 182-field dataclass; all new APR keys present in config_schema/config_state/config_history; full unit suite green.</done>
</task>

</tasks>

<threat_model>
No applicable ASVS Level 1 category. Per `151-RESEARCH.md`'s Security Domain section: this plan
adds numeric columns and pure compute functions to an already-authenticated internal batch
pipeline. No new external inputs, no authentication surface, no user-facing endpoint, no new
package installs (zero `Package Legitimacy Audit` rows required — this phase installs nothing).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-151-01 | Tampering | migration 259 DDL against the live `feature_vectors` hypertable | mitigate | `ADD COLUMN IF NOT EXISTS` with no DEFAULT (metadata-only, no data rewrite); `BEGIN;`/`COMMIT;` wrapping; `ON CONFLICT DO NOTHING` on every seed INSERT makes re-application idempotent |
</threat_model>

<verification>
1. `.venv/bin/pytest tests/unit/ -q` green.
2. Registry/dataclass alignment holds: importing `src.intelligence.feature_registry_service`
   against the live DB does not raise `RuntimeError` (exercised by
   `tests/unit/intelligence/test_feature_registry_service.py`).
3. All 10 columns reach the INSERT contract (`test_feature_vector_persistence_completeness.py`).
4. Zero hardcoded window literals: `grep -n "momentum_velocity\|vwap_velocity" src/intelligence/feature_factory.py`
   shows the values only as `config.<field>` reads, never as inline integers.
</verification>

<success_criteria>
- `feature_vectors` has 10 new `DOUBLE PRECISION` columns
- `feature_registry` has 10 new `tier='0_atomic'`, `added_phase='151'` rows; total count = 182 = `len(dataclasses.fields(FeatureVector))`
- `concept_registry` (domain='feature') and `concept_gate` each have 10 matching new rows for the same 10 features (Phase 170 parity — Section 5)
- 2 new APR keys registered across `config_schema`/`config_state`/`config_history`
- `FEATURE_VECTOR_INSERT_SQL_PSYCOPG2` has 191 placeholders
- Full unit suite green
</success_criteria>

<output>
Create `.planning/phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-01-SUMMARY.md` when done.
Record the ACTUAL migration number used if it differs from 259.
</output>
