---
phase: 151-feature-primitives-expansion-theory-motivated-interaction-la
plan: 04
type: execute
wave: 3
depends_on: ["151-03"]
files_modified:
  - src/intelligence/schemas.py
  - src/intelligence/feature_factory.py
  - src/intelligence/feature_cache.py
  - src/intelligence/features/cross_asset_series.py
  - src/intelligence/features/feature_vector_persistence.py
  - services/backfill_feature_factory.py
  - services/feature_vector_pipeline.py
  - production/migrations/262_cross_asset_spread_beta_atomics.sql
  - tests/unit/test_feature_factory.py
  - tests/unit/services/test_backfill_feature_factory.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "FeatureVector carries 7 new cross-asset atomic fields: tip_tlt_ret_z, hyg_lqd_ret_z, sb_corr_fast, sb_corr_slow, sb_corr_z, equity_beta_z, rate_beta_z"
    - "All new cross-asset bar reads go through market_data_ohlcv_tradeable, never the raw table"
    - "equity_beta_z is NULL for SPY and rate_beta_z is NULL for TLT — a degenerate self-regression is never emitted as a constant"
    - "Every new feature_registry row has a matching concept_registry (domain='feature') + concept_gate row in the same migration, satisfying Phase 170's ic_engine parity precondition"
    - "All 7 new window/z-score parameters are APR-backed under the sanctioned feature.* namespace"
    - "CrossAssetValues is defined exactly once, in the Ring-1 module src/intelligence/features/cross_asset_series.py, and every construction site uses keyword arguments so field order can never silently matter"
    - "The live-path cross-asset contamination is measured against live data and the functional fix has a named in-phase owner (plan 151-09), not a deferral"
  artifacts:
    - path: "production/migrations/262_cross_asset_spread_beta_atomics.sql"
      provides: "7 feature_vectors columns + 7 feature_registry rows + 7 paired concept_registry/concept_gate rows (Phase 170 parity) + 7 APR key triplets"
      contains: "tip_tlt_ret_z"
    - path: "src/intelligence/features/cross_asset_series.py"
      provides: "the single shared CrossAssetValues contract imported by batch, live, and factory paths"
      contains: "class CrossAssetValues"
    - path: "services/backfill_feature_factory.py"
      provides: "TIP/HYG/LQD bar fetches, extended cross-asset series, per-symbol beta series"
      contains: "_build_symbol_beta_series"
    - path: "src/intelligence/feature_cache.py"
      provides: "extended update_cross_asset plus new rolling-history deques"
      contains: "tip_tlt_ret_z"
  key_links:
    - from: "services/backfill_feature_factory.py"
      to: "market_data_ohlcv_tradeable"
      via: "_fetch_bars_from_db for TIP/HYG/LQD"
      pattern: "_fetch_bars_from_db\\(.*\"(TIP|HYG|LQD)\""
    - from: "services/backfill_feature_factory.py::_build_cross_asset_series"
      to: "src/intelligence/feature_factory.py::compute_batch"
      via: "cross_asset_by_date mapping consumed at the per-bar constructor"
      pattern: "cross_asset_by_date"
---

<objective>
Add the 7 remaining Phase 151 tier-0 atomic candidates that require cross-symbol data: todo 123's
two macro spreads plus stock-bond correlation, and todo 180's two factor betas.

Purpose: `TIP`, `HYG`, `LQD` all entered the universe in the 58→80 ETF expansion (2026-07-01),
which postdates intel-08's "blocked on data availability" deferral. `equity_beta`/`rate_beta` are
the glossary's own canonical primitive examples (`docs/foundation/glossary.md:320`) and have never
existed as live per-bar columns — only as slow-moving static `instrument_tags`.

Output: 7 new `FeatureVector` fields, 7 `feature_vectors` columns, 7 `feature_registry` rows,
7 new APR keys, extended batch and cache cross-asset paths, a single shared `CrossAssetValues`
contract in a new Ring-1 module, unit coverage, a measured record of the live-path cross-asset
contamination (whose functional fix is owned by plan 151-09, in this phase), and one filed todo for
the residual dead-code question.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-RESEARCH.md
@.planning/phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-PATTERNS.md
@.planning/phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-03-SUMMARY.md
@.planning/todos/pending/123-momentum-velocity-and-macro-spread-features.md
@.planning/todos/pending/180-four-new-atomic-primitive-candidates-phase151.md
@CLAUDE.md
</context>

<interfaces>
Live contracts (verified 2026-07-24 by direct source read and DB query — do not re-derive):

`services/backfill_feature_factory.py` — THIS is the corpus/IC-measurement path, and it does NOT
call `FeatureCache.update_cross_asset()`:
  line 122-125: `_SPY = "SPY"`, `_TLT = "TLT"`, `_SHY = "SHY"`
  line 219: `_build_cross_asset_series(spy_bars, tlt_bars, shy_bars, config) -> dict`
            returns `{date: (vix_z, flight_quality, yield_slope_z)}`, built ONCE per run,
            symbol-independent, incremental O(D)
  line 775-785: the three `_fetch_bars_from_db(db_conn, _SPY|_TLT|_SHY, "1d")` calls + the builder call
  line 1028-1035: per-symbol CTF series build (`_CTF_HIGHER_TF` = {5m:1h, 15m:1h, 1h:1d, 1d:1d})
  line 1055-1065: `FeatureFactory.compute_batch(..., cross_asset_by_date=cross_asset_by_date, ...)`
  `_fetch_bars_from_db` already reads `market_data_ohlcv_tradeable` (post todo-124 fix) — extending
  it to new symbols inherits that correctness for free.

`src/intelligence/feature_factory.py:4322-4329` — the batch/live branch, verbatim:
    if cross_asset_by_date is not None:
        _ca = cross_asset_by_date.get(bar_ts.date(), (0.0, 0.0, 0.0))
        vix_z_val, flight_quality_val, yield_slope_z_val = _ca
    else:
        vix_z_val = cache.vix_z
        flight_quality_val = cache.flight_quality
        yield_slope_z_val = cache.yield_slope_z

`src/intelligence/feature_cache.py:304-357` — `update_cross_asset(spy_bars, tlt_bars, shy_bars, config)`,
  with the `yield_slope_z` block (TLT/SHY log-return spread → `_zscore_from_deque`) reproduced in
  full in `151-PATTERNS.md`. Cache state fields at `:52-55`, rolling deques at `:73-74`
  (`_yield_ratio_history: deque = field(default_factory=lambda: deque(maxlen=500), repr=False)`).

**The live-path cross-asset gap - re-investigated during the cross-AI review pass (2026-07-24).
Read this before scoping Task 4: the earlier "out of scope, file a todo" disposition has been
overturned, and Codex (independent reviewer) rated it HIGH rather than deferrable.**

`FeatureCache.update_cross_asset()` has NO production caller. Grep confirms only two test files call
it. The live pipeline (`services/feature_vector_pipeline.py:862`) routes the cross-asset Kafka
payload into `CacheManager.update_cross_asset(tf, payload)` - a DIFFERENT object
(`src/intelligence/pipeline/cache_manager.py:235`) that stores the payload in a per-tf dict and never
touches `FeatureCache`. So `vix_z`, `flight_quality`, and `yield_slope_z` are frozen at their `0.0`
cache defaults on the LIVE path. Same bug class as todo 158's `above_wk_vwap`.

Three further facts were established during the review pass, and they change the disposition:
1. **That Kafka route could never have worked.** Its only producer,
   `services/cross_asset_analyzer.py:471`, publishes `es_nq_spread_z`/`corr_z`/`active_pair` - not
   these three fields - and its unit `indicagent-cross-asset.service` is `inactive (dead)`.
   `CacheSnapshot.cross_asset_data` is consumed only by the archived v2.x
   `src/intelligence/pipeline/feature_pipeline_executor.py:287-294`, never by v3's `FeatureVector`.
2. **`indicagent-feature-vector-pipeline.service` is `active running`** and writes live rows straight
   into `feature_vectors` - the table this entire phase's IC measurement reads
   (`feature_vectors` -> `forward_return_writer` -> `ic_engine` -> `ensemble_trainer` ->
   `alpha_publisher`). The consumer of the frozen fields is the measurement pipeline itself, not a
   hypothetical future live subscriber.
3. **Measured, not inferred.** `SELECT (vix_z = 0.0) AS is_zero, count(*), min(bar_ts), max(bar_ts),
   count(DISTINCT symbol) FROM feature_vectors WHERE tf='5m' AND bar_ts > '2026-06-01' GROUP BY 1;`
   returns 9,183 zero-valued rows spanning 2026-06-23 -> 2026-07-07 across all 80 symbols, against
   139,093 correctly-valued batch rows in the same window. The live tail is contaminated today and
   grows every trading day. (Separately: `vix_z = 0.0` for all pre-2018 rows is a different,
   pre-existing coverage-window artifact of the batch builder's date fallback, documented in plan
   151-07 Task 2's Section C allow-list - do not conflate the two.)

**Disposition: fixed inside this phase, by plan 151-09 (wave 5) - not deferred.**
Reasoning, recorded so it is not re-litigated: (a) Codex is right on substance and the live evidence
above is stronger than the review's own framing - this is active contamination of the measurement
corpus, not a latent risk; (b) the fix has an exact in-repo precedent (todo 159's `_get_cache()`
warm-up for `above_wk_vwap`) so it is not novel architecture; (c) shipping 7 MORE columns into a
known-broken live path makes the asymmetry strictly harder to unwind later. It is NOT folded into
this plan because this plan already carries 4 tasks across 10 files and is at the top of its context
budget - a fifth substantive task would degrade all of them. Task 4 below quantifies the
contamination and files only the residual dead-code question.

Live corpus coverage in `market_data_ohlcv_tradeable` (2026-07-24), relevant to the beta design:
  `1d` = 350,945 rows, 2006-06-26 → 2026-07-06 (all 80 symbols)
  `5m` = 25,387,548 rows — far too large to broadcast a per-tf SPY array into every
  `ProcessPoolExecutor` worker (see `151-RESEARCH.md` Pitfall 6: this script already OOM-killed
  twice at `--workers 12`).

Sanctioned APR namespaces (root `CLAUDE.md`): `threshold.* · weights.* · feature.* · regime.* ·
shadow.* · signal.* · swarm.* · roll.* · ui.* · alpha.* · infra.*`. **`macro.*` is NOT sanctioned.**
Todo 123 proposed `macro.sb_corr.window_fast/slow`; that proposal is superseded here by the
`feature.*` namespace, matching every live sibling (`feature.vix.zscore_window`,
`feature.yield_curve.zscore_window`, `feature.cross_asset.rv_window`).

After plan 151-03, `len(dataclasses.fields(FeatureVector))` == 193, `feature_registry` has 193 rows,
and `FEATURE_VECTOR_INSERT_SQL_PSYCOPG2` has 202 placeholders. This plan takes them to 200 / 200 / 209.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Schema fields, APR config, and the macro-spread/correlation compute</name>
  <files>src/intelligence/schemas.py, src/intelligence/feature_factory.py, src/intelligence/feature_cache.py, src/intelligence/features/cross_asset_series.py</files>
  <read_first>
    - src/intelligence/feature_cache.py lines 36-90 (cache field + deque declaration pattern)
    - src/intelligence/feature_cache.py lines 295-360 (update_cross_asset full method)
    - src/intelligence/feature_factory.py lines 268-300, 380-400 (FeatureFactoryConfig macro fields)
    - src/intelligence/feature_factory.py lines 4315-4335 (cross-asset batch/live branch)
    - src/intelligence/feature_factory.py lines 4727-4830 (_cold_start_vector)
    - src/intelligence/schemas.py lines 1204-1300 (FeatureVector docstring + Cross-asset field block)
    - .planning/todos/pending/123-momentum-velocity-and-macro-spread-features.md
  </read_first>
  <action>
Add 7 fields to `FeatureVector`, declared as one contiguous block immediately after
`abs_ret_autocorr_1` (plan 151-03's last field), in this order:
  `tip_tlt_ret_z: float`
  `hyg_lqd_ret_z: float`
  `sb_corr_fast: float`
  `sb_corr_slow: float`
  `sb_corr_z: float`
  `equity_beta_z: float | None`
  `rate_beta_z: float | None`

The two betas are nullable BY DESIGN. Todo 180's Fable review flagged that regressing SPY against
itself is degenerate (beta ≡ 1, r² ≡ 1); emitting a constant 1.0 would be a silently-wrong feature.
`validate_feature_vector` rejects nan/inf, so NaN is not an option. `None` is, following the
existing nullable VP/SR precedent (`poc_dist_atr: float | None`). Emit `None` for `equity_beta_z`
when the symbol IS the equity factor proxy (`SPY`), and `None` for `rate_beta_z` when the symbol IS
the rate factor proxy (`TLT`).

Update the class docstring: opening line to `200 orthogonal feature primitives`, tally to
`Total: 200 (190 required + 10 optional [3 cross-sectional + 5 canary + 2 factor beta])`, and
extend the `Cross-asset (3)` group line to `Cross-asset (10)` naming the 7 additions.

Add 7 `FeatureFactoryConfig` fields with APR-key comments (all `feature.*`, NOT `macro.*` — see
Interfaces):
  `tip_tlt_zscore_window: int   # feature.tip_tlt.zscore_window`
  `hyg_lqd_zscore_window: int   # feature.hyg_lqd.zscore_window`
  `sb_corr_window_fast: int     # feature.sb_corr.window_fast`
  `sb_corr_window_slow: int     # feature.sb_corr.window_slow`
  `sb_corr_zscore_window: int   # feature.sb_corr.zscore_window`
  `factor_beta_window: int      # feature.factor_beta.window`
  `factor_beta_zscore_window: int # feature.factor_beta.zscore_window`
Key naming note: `factor_beta` rather than a bare `beta` — `docs/foundation/glossary.md`'s `beta`
entry bans the unqualified term. One shared window serves both `equity_beta_z` and `rate_beta_z`
(same statistic, different factor series). Update the config docstring's APR-key list and wire all
7 into both construction sites (`services/feature_vector_pipeline.py` `_prewarm_threshold_config`
key list ~line 516 and constructor ~line 656; `services/backfill_feature_factory.py` config-build
block ~line 425), same pattern as plans 151-01 / 151-03.

In `src/intelligence/feature_cache.py`, extend the live-path cache:
  - 7 new public state fields on the cache dataclass, in the `# Cross-asset cached from ...` block,
    defaulting to `0.0` (betas default `0.0` here; the SPY/TLT null special-case is applied at
    `FeatureVector` construction, not in the cache).
  - 5 new private rolling-history deques mirroring `_yield_ratio_history` exactly
    (`deque(maxlen=500)`, `repr=False`): `_tip_tlt_ratio_history`, `_hyg_lqd_ratio_history`,
    `_sb_corr_history`, `_equity_beta_history`, `_rate_beta_history`.
  - Extend `update_cross_asset`'s signature to
    `(self, spy_bars, tlt_bars, shy_bars, tip_bars, hyg_bars, lqd_bars, config)`. Add a
    `tip_tlt_ret_z` block and a `hyg_lqd_ret_z` block, each a structural copy of the existing
    `yield_slope_z` block: pairwise log returns, take the last-bar difference, append to the
    feature's own deque, then `_zscore_from_deque(<deque>, config.<its own zscore window>)`. Do NOT
    reuse `config.yield_curve_zscore_window` for either — todo 123's review requires a distinct key
    per spread.
  - Add an `sb_corr` block: rolling Pearson correlation between SPY and TLT log returns over
    `config.sb_corr_window_fast` → `sb_corr_fast`, over `config.sb_corr_window_slow` →
    `sb_corr_slow`, and `sb_corr_z` = `_zscore_from_deque(self._sb_corr_history,
    config.sb_corr_zscore_window)` where the deque accumulates the FAST value. A rolling
    correlation coefficient is theory-free (same class as `hurst`/`skewness`); do not attach any
    directional interpretation to its sign in code comments or `formula_short`.
  - Update all existing `update_cross_asset` call sites, including the two test files
    (`tests/unit/test_feature_factory.py` lines ~777-797 and
    `tests/unit/services/test_backfill_feature_factory.py` line ~783) — per CLAUDE.md's
    signature-change test-sweep rule.

Replace the bare 3-tuple with a `typing.NamedTuple` (`CrossAssetValues`) carrying all 8
symbol-independent values (`vix_z`, `flight_quality`, `yield_slope_z`, `tip_tlt_ret_z`,
`hyg_lqd_ret_z`, `sb_corr_fast`, `sb_corr_slow`, `sb_corr_z`) with `0.0` defaults, so a
positional-unpack mistake becomes impossible as this tuple grows again in future phases.
Addresses cross-AI review concern (Codex, MEDIUM: "several waves depend on tuple-shaped payloads and
a growing number of positional outputs - brittle and easy to break with a small field-order
mistake").

Declare it in a NEW Ring-1 module, `src/intelligence/features/cross_asset_series.py` - not in
`feature_factory.py` and not in `services/backfill_feature_factory.py` - so exactly ONE definition is
imported by every producer and consumer (`feature_factory.py`, `services/backfill_feature_factory.py`
in Task 2, and the live pipeline in plan 151-09). Ring rule check: this module holds domain
vocabulary (VIX, yield slope, flight-to-quality), so Ring 1 `src/intelligence/`, not Ring 0
`src/core/`. Do NOT put it in the existing `src/intelligence/cross_asset_features.py`: that module is
archived v2.x ES/NQ futures-spread code carrying its own module-level hardcoded constants, and
touching it would pull unrelated tech debt into this phase.

Every construction site must use KEYWORD arguments, never positional, so field order can never
silently matter. Update the `cross_asset_by_date.get(...)` default to `CrossAssetValues()` and the
live branch in `src/intelligence/feature_factory.py` to read the corresponding cache fields.

Add `FEATURE_VECTOR_DOMAIN` entries: all 7 mapped to `"macro"`, placed in the existing
`# Cross-asset / macro` block alongside `vix_z`/`flight_quality`/`yield_slope_z`.

Add all 7 to `_cold_start_vector()`: the five non-nullable at `0.0`, the two betas at `None`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_feature_factory.py tests/unit/intelligence/test_feature_factory_batch.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import dataclasses; from src.intelligence.schemas import FeatureVector; print(len(dataclasses.fields(FeatureVector)))"` prints 200
    - `python -c "import dataclasses,typing; from src.intelligence.schemas import FeatureVector as F; d={f.name:f.type for f in dataclasses.fields(F)}; print(d['equity_beta_z'], d['rate_beta_z'])"` shows both as `float | None`
    - `grep -c "macro\." production/migrations/262_cross_asset_spread_beta_atomics.sql` is 0 once Task 3 lands (no unsanctioned namespace)
    - `grep -v '^ *#' src/intelligence/feature_cache.py | grep -c "yield_curve_zscore_window"` is 1 (still used only by `yield_slope_z`, not reused for the new spreads)
    - `grep -c "class CrossAssetValues" src/intelligence/features/cross_asset_series.py` returns 1, and `grep -rl "class CrossAssetValues" src/ services/ | wc -l` returns 1 (exactly one definition project-wide)
    - `grep -rn "CrossAssetValues(" src/ services/` shows zero positional constructions - every call site passes keyword arguments
    - All 7 names are keys in `FEATURE_VECTOR_DOMAIN` mapped to the literal `"macro"`
  </acceptance_criteria>
  <done>7 fields declared, 7 APR-backed config fields wired at every construction site, macro spreads and stock-bond correlation compute in the cache path, cross-asset tuple replaced by a named tuple.</done>
</task>

<task type="auto">
  <name>Task 2: Batch-path wiring — TIP/HYG/LQD fetches and per-symbol factor betas</name>
  <files>services/backfill_feature_factory.py, src/intelligence/features/feature_vector_persistence.py</files>
  <read_first>
    - services/backfill_feature_factory.py lines 118-140 (symbol constants + _CTF_HIGHER_TF)
    - services/backfill_feature_factory.py lines 219-315 (_build_cross_asset_series full body)
    - services/backfill_feature_factory.py lines 400-440 (config build)
    - services/backfill_feature_factory.py lines 770-800, 900-960, 1000-1075 (fetch, thread-through, compute_batch call)
    - src/intelligence/features/feature_vector_persistence.py lines 75-125, 170-250, 271-305
  </read_first>
  <action>
Add three symbol constants next to `_SPY`/`_TLT`/`_SHY`: `_TIP = "TIP"`, `_HYG = "HYG"`,
`_LQD = "LQD"`. Add three `_fetch_bars_from_db(db_conn, _TIP|_HYG|_LQD, "1d")` calls alongside the
existing three at line ~775. `_fetch_bars_from_db` already reads `market_data_ohlcv_tradeable`;
confirm this by reading its body and record the confirmation in the SUMMARY — do not assume.

Extend `_build_cross_asset_series(spy_bars, tlt_bars, shy_bars, tip_bars, hyg_bars, lqd_bars, config)`
to return `{date: CrossAssetValues}` (the NamedTuple from Task 1) instead of a 3-tuple. Keep the
existing incremental O(D) structure: add `tip`/`hyg`/`lqd` to the `symbol_bars`/`symbol_dates`/
`cursors` dicts, add `tip_prev_close`/`hyg_prev_close`/`lqd_prev_close` trackers advanced in the
same skip branch as the existing three, and add the three new deques
(`tip_tlt_ratio_history`, `hyg_lqd_ratio_history`, `sb_corr_history`) plus two fixed-length return
buffers for the SPY/TLT correlation windows. Do NOT re-materialize full bar slices per date — the
existing function is deliberately O(D), not O(D·N), and must stay that way.

**Coverage guard:** if any of TIP/HYG/LQD has zero bars for a given date, emit `0.0` for the
affected spread on that date rather than skipping the date entirely — skipping would silently drop
`vix_z`/`yield_slope_z` for dates that predate the newer ETFs' listings. Log the count of dates
with partial cross-asset coverage ONCE at the end of the builder, never per date (CLAUDE.md's
never-log-per-row-over-the-corpus rule).

Add a new per-symbol builder:
  `_build_symbol_beta_series(symbol_1d_bars, spy_1d_bars, tlt_1d_bars, symbol, config) -> dict`
  returning `{date: (equity_beta_z, rate_beta_z)}`, where each beta is the rolling OLS slope of
  the symbol's daily log returns on the factor's daily log returns over `config.factor_beta_window`
  bars (`cov(r_sym, r_factor) / var(r_factor)`, epsilon-guarded on the denominator), then z-scored
  over `config.factor_beta_zscore_window`. Emit `None` for `equity_beta_z` when
  `symbol == _SPY` and `None` for `rate_beta_z` when `symbol == _TLT`.

  **Grain decision (document this in the SUMMARY):** betas are computed at DAILY grain and
  broadcast to all timeframes by date, exactly like the existing `vix_z`/`flight_quality`/
  `yield_slope_z` cadence contract. Computing them at the symbol's own tf would require passing a
  25.4M-row SPY 5m array into every `ProcessPoolExecutor` worker — this script already OOM-killed
  twice at `--workers 12` (`151-RESEARCH.md` Pitfall 6). Daily grain is the same cadence the rest
  of the macro family already uses and costs one extra `_fetch_bars_from_db(conn, symbol, "1d")`
  per symbol (~350K rows total across all 80 symbols).

Thread `beta_by_date` from the per-symbol setup (next to the existing per-symbol CTF build at line
~1028) into `FeatureFactory.compute_batch()` as a new `beta_by_date: dict | None = None` keyword,
mirroring `ctf_by_ts`'s shape exactly. In `compute_batch`, read it in the same
`if cross_asset_by_date is not None:` batch branch, defaulting to `(None, None)` when the date is
absent. Update `compute_batch`'s docstring batch/live contract paragraph.

Then extend `src/intelligence/features/feature_vector_persistence.py`:
1. Module-docstring paragraph dated to this session recording 202 → 209 columns (migration 262,
   Phase 151 Plan 04), matching the existing dated-paragraph style.
2. A sixth derived slice `_PHASE151_CROSS_ASSET_FIELD_NAMES` spanning index of `tip_tlt_ret_z`
   through index of `rate_beta_z` inclusive — the `_STRUCTURAL_VP_SR_FIELD_NAMES` idiom, never a
   hand-typed list.
3. Append to the `FEATURE_VECTOR_INSERT_SQL` column list, append the `$N` placeholder range block,
   extend `_TOTAL_COLUMNS`.
4. Update `feature_vector_to_insert_params`' docstring column map and change `202-element` to
   `209-element`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/services/test_backfill_feature_factory.py tests/unit/test_feature_vector_persistence_completeness.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_fetch_bars_from_db(db_conn, _TIP\|_fetch_bars_from_db(db_conn, _HYG\|_fetch_bars_from_db(db_conn, _LQD' services/backfill_feature_factory.py` returns 3
    - `grep -rn "market_data_ohlcv\b" services/backfill_feature_factory.py | grep -v tradeable | grep -v '^\s*#'` returns no new occurrences versus `git show HEAD:services/backfill_feature_factory.py | grep -c "market_data_ohlcv\b"`
    - `grep -c "def _build_symbol_beta_series" services/backfill_feature_factory.py` returns 1
    - `python -c "from src.intelligence.features.feature_vector_persistence import FEATURE_VECTOR_INSERT_SQL_PSYCOPG2 as s; print(s.count('%s'))"` prints 209
    - `test_every_feature_vector_field_is_a_sql_column` and `test_insert_params_tuple_length_matches_sql` both pass
    - A unit test asserts `_build_symbol_beta_series(..., symbol="SPY", ...)` yields `equity_beta_z is None` at every date, and `symbol="TLT"` yields `rate_beta_z is None` at every date
    - A unit test asserts `_build_cross_asset_series` returns a dict whose values are `CrossAssetValues` instances and whose `vix_z`/`flight_quality`/`yield_slope_z` fields match the pre-change 3-tuple values to 1e-12 on a fixed synthetic input (no regression on the three existing macro features)
  </acceptance_criteria>
  <done>Batch path fetches TIP/HYG/LQD via the tradeable view, emits all 8 symbol-independent cross-asset values plus per-symbol factor betas, and every new field reaches the INSERT tuple.</done>
</task>

<task type="auto">
  <name>Task 3: Migration 262 and unit tests</name>
  <files>production/migrations/262_cross_asset_spread_beta_atomics.sql, tests/unit/test_feature_factory.py, tests/unit/services/test_backfill_feature_factory.py</files>
  <read_first>
    - production/migrations/255_vp_structural_primitives.sql (full file — template, including the nullable-column COMMENT convention)
    - production/migrations/261_recency_statistical_atomics.sql (this phase's prior migration)
  </read_first>
  <action>
Re-verify the next-free migration number: `ls production/migrations/ | sort -t_ -k1 -n | tail -5`.
Provisional target 262 (259/260/261 reserved by plans 151-01/02/03). If taken, use the actual
next-free number and update the filename plus every `changed_by='migration_262'` string.

Write `production/migrations/262_cross_asset_spread_beta_atomics.sql`, `BEGIN;`/`COMMIT;`-wrapped,
same four-section structure as migration 259.

Section 2 DDL — 7 `ALTER TABLE feature_vectors ADD COLUMN IF NOT EXISTS <name> DOUBLE PRECISION;`
in dataclass order, each with a `COMMENT ON COLUMN`. The two beta comments MUST state the null
semantics explicitly: `NULL for the factor proxy itself (SPY for equity_beta_z, TLT for
rate_beta_z) -- a self-regression is degenerate (beta identically 1) and is deliberately not
emitted as a constant. Daily-grain rolling OLS slope, broadcast to all timeframes by date, same
cadence contract as vix_z/yield_slope_z. Phase 151.`

Section 3 `feature_registry` INSERT — 7 rows, all `tier='0_atomic'`, `group_name='macro'`,
`requires_htf=false`, `status='active'`, `added_phase='151'`,
`ON CONFLICT (feature_name) DO NOTHING`:

| feature_name | normalization | linear_ready | formula_short |
|---|---|---|---|
| tip_tlt_ret_z | z_scored | true | zscore(log_ret(TIP) - log_ret(TLT)) |
| hyg_lqd_ret_z | z_scored | true | zscore(log_ret(HYG) - log_ret(LQD)) |
| sb_corr_fast | bounded_signed | true | rolling Pearson(log_ret(SPY), log_ret(TLT)) fast |
| sb_corr_slow | bounded_signed | true | rolling Pearson(log_ret(SPY), log_ret(TLT)) slow |
| sb_corr_z | z_scored | true | zscore(sb_corr_fast) |
| equity_beta_z | z_scored | true | zscore(rolling OLS slope of symbol daily log_ret on SPY daily log_ret) |
| rate_beta_z | z_scored | true | zscore(rolling OLS slope of symbol daily log_ret on TLT daily log_ret) |

`formula_short` values are deliberately instrument-descriptive, not theory-laden — todo 123's Fable
review rejected "real yield spread"/"credit spread" because those names assert a causal referent
the formula does not compute. Match `yield_slope_z`'s convention. Every row is `tier='0_atomic'`
(NOT `2_theory`) because none of these attach a directional interpretation the way `flight_quality`
does with its baked-in "positive = risk-off" claim.

Section 4 APR triplets — 7 keys, all under the sanctioned `feature.*` namespace (this supersedes
todo 123's `macro.*` proposal; record the correction in the SUMMARY), each with the full
`config_schema`/`config_state`/`config_history` triplet and `changed_by='migration_262'`:

| config_key | type | default | min | max | provenance |
|---|---|---|---|---|---|
| feature.tip_tlt.zscore_window | int | 252 | 20 | 2520 | [conventional] |
| feature.hyg_lqd.zscore_window | int | 252 | 20 | 2520 | [conventional] |
| feature.sb_corr.window_fast | int | 30 | 5 | 250 | [conventional] |
| feature.sb_corr.window_slow | int | 60 | 10 | 500 | [conventional] |
| feature.sb_corr.zscore_window | int | 252 | 20 | 2520 | [conventional] |
| feature.factor_beta.window | int | 60 | 10 | 500 | [conventional] |
| feature.factor_beta.zscore_window | int | 252 | 20 | 2520 | [conventional] |

Each description must name the features it drives, carry the provenance tag, state
`Phase 151.`, and state `ML learning target: yes.` The `feature.factor_beta.*` description must
additionally note it is shared by both `equity_beta_z` and `rate_beta_z` and that the
`factor_beta` naming avoids the glossary's ban on an unqualified `beta`.

Section 5 — Phase 170 `concept_registry` parity (added retroactively: this plan was written
2026-07-24, before Phase 170 shipped its dual-write cutover 2026-08-04/05). `ic_engine.py`'s
`_apply_feature_transitions` now runs a PARITY PRECONDITION on every corpus pass that hard-stops
if any `feature_registry` row lacks a `concept_registry` counterpart (Phase 170 Plan 06,
T-170-17). Add immediately after Section 4, same file, same `BEGIN`/`COMMIT`, deriving every
field from the `feature_registry` rows Section 3 just inserted — field mapping matches migration
284's feature-domain seed exactly:

```sql
-- Phase 170 parity: concept_registry/concept_gate mirror for this migration's 7 rows.
INSERT INTO concept_registry
    (domain, name, description, status, enabled, group_name, is_control, control_expectation,
     added_phase, created_at, metadata)
SELECT
    'feature', fr.feature_name, fr.formula_short, fr.status, (fr.status = 'active'),
    fr.group_name, fr.is_control, fr.control_expectation, fr.added_phase, fr.added_at,
    jsonb_strip_nulls(jsonb_build_object(
        'tier', fr.tier, 'formula_short', fr.formula_short, 'normalization', fr.normalization,
        'linear_ready', fr.linear_ready, 'requires_htf', fr.requires_htf,
        'window_apr_keys', to_jsonb(fr.window_apr_keys), 'source_dims', to_jsonb(fr.source_dims),
        'notes', fr.notes, 'apr_namespace', 'feature.'
    ))
FROM feature_registry fr
WHERE fr.feature_name IN (
    'tip_tlt_ret_z', 'hyg_lqd_ret_z', 'sb_corr_fast', 'sb_corr_slow', 'sb_corr_z',
    'equity_beta_z', 'rate_beta_z'
)
ON CONFLICT (domain, name) DO NOTHING;

INSERT INTO concept_gate
    (concept_id, gate_metric_name, gate_eval_method, min_gate_metric, min_gate_n,
     fdr_required, fdr_alpha)
SELECT cr.concept_id, 'ic_sharpe_hac', 'bootstrap_ci', fr.min_ic_sharpe, fr.min_ic_n,
       fr.fdr_required, fr.fdr_alpha
FROM feature_registry fr
JOIN concept_registry cr ON cr.domain = 'feature' AND cr.name = fr.feature_name
WHERE fr.feature_name IN (
    'tip_tlt_ret_z', 'hyg_lqd_ret_z', 'sb_corr_fast', 'sb_corr_slow', 'sb_corr_z',
    'equity_beta_z', 'rate_beta_z'
)
ON CONFLICT (concept_id) DO NOTHING;
```

Add to this task's acceptance criteria:
- `SELECT count(*) FROM concept_registry WHERE domain='feature' AND added_phase='151' AND name IN ('tip_tlt_ret_z','hyg_lqd_ret_z','sb_corr_fast','sb_corr_slow','sb_corr_z','equity_beta_z','rate_beta_z');` returns 7
- `SELECT count(*) FROM concept_gate cg JOIN concept_registry cr USING (concept_id) WHERE cr.domain='feature' AND cr.added_phase='151' AND cr.name IN ('tip_tlt_ret_z','hyg_lqd_ret_z','sb_corr_fast','sb_corr_slow','sb_corr_z','equity_beta_z','rate_beta_z');` returns 7

Apply:
`PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/262_cross_asset_spread_beta_atomics.sql`

Add unit tests:
- `tests/unit/test_feature_factory.py` — extend the existing `update_cross_asset` tests for the new
  6-bar-list signature; assert `tip_tlt_ret_z`/`hyg_lqd_ret_z` are 0.0 with fewer than 2 bars and
  finite thereafter; assert `sb_corr_fast` is within `[-1, 1]` and equals +1.0 for perfectly
  co-moving synthetic SPY/TLT series and -1.0 for perfectly opposed ones.
- `tests/unit/services/test_backfill_feature_factory.py` — the two builder tests named in Task 2's
  acceptance criteria (SPY/TLT null special-cases, and the no-regression equivalence check for the
  three pre-existing macro features).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM feature_registry;"` returns 200
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM feature_registry WHERE added_phase='151';"` returns 28
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM config_schema WHERE config_key LIKE 'feature.tip_tlt.%' OR config_key LIKE 'feature.hyg_lqd.%' OR config_key LIKE 'feature.sb_corr.%' OR config_key LIKE 'feature.factor_beta.%';"` returns 7
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM config_schema WHERE config_key LIKE 'macro.%';"` returns 0
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM feature_registry WHERE added_phase='151' AND tier='0_atomic';"` returns 28
    - `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
  <done>Migration 262 applied; all 28 Phase 151 tier-0 atomic candidates are now live in feature_registry; 200-row registry matches the 200-field dataclass; full unit suite green.</done>
</task>

<task type="auto">
  <name>Task 4: Quantify the live-path contamination and file the residual dead-code todo</name>
  <files>.planning/todos/pending/181-v3-cross-asset-kafka-route-dead-code.md</files>
  <read_first>
    - .planning/todos/pending/180-four-new-atomic-primitive-candidates-phase151.md (frontmatter/format template)
    - .planning/todos/PRIORITIES.md
    - services/feature_vector_pipeline.py lines 840-870
    - src/intelligence/pipeline/cache_manager.py lines 225-250
    - services/cross_asset_analyzer.py lines 440-490 (the payload it actually publishes)
  </read_first>
  <action>
Addresses cross-AI review concern (Codex, HIGH: "`151-04` explicitly leaves
`FeatureCache.update_cross_asset()` broken on the live path... a serious correctness gap"), and the
review's explicit divergence note that this should not silently default to the planner's original
"file a todo, not blocking" call.

**The decision, made and recorded - do not re-litigate it during execution.** The functional fix
lands INSIDE this phase, in plan 151-09 (wave 5), ahead of plan 151-07's corpus recompute. The full
reasoning and the live evidence are in this plan's `<interfaces>` section. This task does the two
things that belong here rather than in 151-09.

**Part A - quantify it, so the decision rests on measurement rather than argument.** Run and record
verbatim in the SUMMARY:
  1. `SELECT (vix_z = 0.0) AS is_zero, count(*), min(bar_ts), max(bar_ts), count(DISTINCT symbol)
      FROM feature_vectors WHERE tf='5m' AND bar_ts > '2026-06-01' GROUP BY 1;`
     Baseline recorded 2026-07-24: 9,183 zero rows over 2026-06-23 -> 2026-07-07 across all 80
     symbols, versus 139,093 non-zero batch rows in the same window.
  2. The same query for `tf='15m'`, `'1h'`, and `'1d'`.
  3. `systemctl is-active indicagent-feature-vector-pipeline indicagent-cross-asset` - expect
     `active` and `inactive`. If `indicagent-cross-asset` has since become `active`, STOP and say so
     in the SUMMARY: a live producer would exist and that changes plan 151-09's design.
State plainly whether the contamination is confined to the live tail or reaches further back, and
confirm plan 151-09 is still in the plan set and still sequenced ahead of plan 151-07.

**Part B - file the residual dead-code todo** (the part 151-09 will NOT fix). Check the current
highest pending todo number first (`ls .planning/todos/pending/`) and continue that sequence - do not
jump to match a phase number.

Content: the v3 cross-asset Kafka route is dead code whose shape actively misleads. Its only
producer, `services/cross_asset_analyzer.py:471`, publishes `es_nq_spread_z`/`corr_z`/`active_pair`
- fields unrelated to `vix_z`/`flight_quality`/`yield_slope_z` - and its unit
`indicagent-cross-asset.service` is `inactive (dead)`. `CacheManager.update_cross_asset`
(`src/intelligence/pipeline/cache_manager.py:235`) stores that payload into `_cross_asset`, which
reaches only the archived v2.x `feature_pipeline_executor.py`. The identically-named
`FeatureCache.update_cross_asset()` is a completely different method on a different object with no
production caller - two same-named methods, one live and irrelevant, one relevant and uncalled, is
precisely how this gap survived unnoticed. Pose the open question rather than answering it here:
should the v3 pipeline stop subscribing to `topic_cross_asset` altogether, or is that subscription
still wanted for the possible v2.x revival recorded in
`project_i1_i7_parallel_reimplementation_intent`?

Classify it P3, area `intelligence`, type `tech-debt` - P3 rather than P2 precisely BECAUSE plan
151-09 fixes the correctness half; what remains is naming/dead-code hygiene, and the open
v2.x-revival question means deleting the subscription is not obviously correct. Cite todo 158 by
number as the same bug class, todo 159 as the adjacent FeatureCache-warming precedent, and plan
151-09 as the owner of the functional fix. Add the matching entry to `.planning/todos/PRIORITIES.md`
in the same commit - PRIORITIES.md drifts silently when a todo is filed without one.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM feature_vectors WHERE tf='5m' AND bar_ts > '2026-06-01' AND vix_z = 0.0;"</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the zero-versus-non-zero split for all 4 timeframes and the `systemctl is-active` output for both units
    - The SUMMARY states explicitly that plan 151-09 owns the functional fix and lands in wave 5, ahead of plan 151-07's corpus recompute
    - A new file exists under `.planning/todos/pending/` whose body cites `services/cross_asset_analyzer.py`, `src/intelligence/pipeline/cache_manager.py:235`, todo 158 by number, and plan 151-09 by number
    - The todo's frontmatter has `status: pending` and `priority: P3`
    - `.planning/todos/PRIORITIES.md` contains a line referencing the new todo number
    - The todo number continues the existing `pending/` sequence (is exactly one greater than the previous maximum)
  </acceptance_criteria>
  <done>The live-path contamination is measured against live data, ownership of the functional fix is unambiguous and in-phase, and the residual dead-code question is tracked rather than forgotten.</done>
</task>

</tasks>

<threat_model>
No applicable ASVS Level 1 category (internal compute + DDL over already-ingested OHLCV, no
external input, no auth surface, no new packages). Per `151-RESEARCH.md`'s Security Domain section.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-151-06 | Tampering | new TIP/HYG/LQD bar reads | mitigate | All reads go through `_fetch_bars_from_db`, which reads `market_data_ohlcv_tradeable` (the `price_sanity_status`-filtering view) per the todo-124 fix; acceptance criteria assert no new raw-table read is introduced |
| T-151-07 | Information Disclosure | degenerate self-regression emitted as a constant feature | mitigate | `equity_beta_z`/`rate_beta_z` typed `float \| None` with an explicit SPY/TLT null special-case, asserted by unit test |
| T-151-08 | Denial of Service | OOM during the eventual corpus recompute | transfer | Handled in plan 151-07 (`--workers 4` plus a resumable per-partition manifest); this plan's daily-grain beta design deliberately avoids broadcasting a 25.4M-row per-tf factor array into workers |
| T-151-09 | Information Disclosure | 10 cross-asset columns emitting a `0.0` placeholder on the live path, indistinguishable from a real measured value in `feature_vectors` | mitigate | Contamination quantified against live data in Task 4 Part A; functional fix owned in-phase by plan 151-09 (wave 5), which lands before plan 151-07's recompute; residual dead-code question tracked as a P3 todo |
</threat_model>

<verification>
1. `.venv/bin/pytest tests/unit/ -q` green.
2. `.venv/bin/ruff check src/intelligence/ services/backfill_feature_factory.py` exits 0.
3. Registry/dataclass alignment: 200 == 200.
4. `grep -rn "macro\." production/migrations/262_*.sql` returns nothing (namespace correction held).
5. The three pre-existing macro features are output-identical (equivalence test in Task 3).
</verification>

<success_criteria>
- 7 new `DOUBLE PRECISION` columns; `equity_beta_z`/`rate_beta_z` nullable with SPY/TLT special-cased
- All 28 Phase 151 tier-0 atomic candidates now registered (`added_phase='151'`, `tier='0_atomic'`)
- `concept_registry` (domain='feature') and `concept_gate` each have 7 matching new rows (Phase 170 parity — Section 5)
- 7 new APR keys, all under `feature.*`; zero keys under the unsanctioned `macro.*`
- `FEATURE_VECTOR_INSERT_SQL_PSYCOPG2` has 209 placeholders
- `CrossAssetValues` defined exactly once, in `src/intelligence/features/cross_asset_series.py`, constructed only with keyword arguments
- The live-path cross-asset contamination is measured and recorded, its functional fix owned in-phase by plan 151-09, and the residual dead-code question filed as a P3 todo
- Full unit suite green
</success_criteria>

<output>
Create `.planning/phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-04-SUMMARY.md` when done.
Record the ACTUAL migration number, the `macro.*` → `feature.*` namespace correction, the
daily-grain beta decision, the Task 4 Part A contamination measurement for all 4 timeframes, and the
new todo number.
</output>
