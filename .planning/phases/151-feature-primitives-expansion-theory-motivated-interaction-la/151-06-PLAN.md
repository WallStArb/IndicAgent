---
phase: 151-feature-primitives-expansion-theory-motivated-interaction-la
plan: 06
type: execute
wave: 5
depends_on: ["151-05"]
files_modified:
  - src/intelligence/schemas.py
  - src/intelligence/feature_factory.py
  - src/intelligence/features/feature_vector_persistence.py
  - production/migrations/264_theory_motivated_interactions.sql
  - tests/unit/intelligence/test_feature_factory_batch.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "10 theory-motivated compound features exist, each a SINGLE operation on exactly 2 numeric tier-0 columns"
    - "Every one carries a one-sentence finance-theory hypothesis in feature_registry.formula_short"
    - "The total tier=1_interaction population is 23 rows — inside ROADMAP's <=50 cap"
    - "No compound multiplies a categorical regime label; every categorical candidate source is resolved to a numeric tier-0 proxy"
    - "No new compound is an affine transform or near-duplicate of any of the 13 pre-existing interaction rows"
    - "Every new feature_registry row has a matching concept_registry (domain='feature') + concept_gate row, and every parent_features pair has a matching concept_parent edge, satisfying Phase 170's ic_engine parity precondition"
  artifacts:
    - path: "production/migrations/264_theory_motivated_interactions.sql"
      provides: "10 feature_vectors columns + 10 tier=1_interaction registry rows with hypotheses + 10 paired concept_registry/concept_gate rows + 20 concept_parent edges (Phase 170 parity)"
      contains: "momentum_vol_regime_product"
    - path: "src/intelligence/feature_factory.py"
      provides: "10 single-operation compound computations at the FeatureVector constructor"
      contains: "momentum_vol_regime_product"
  key_links:
    - from: "production/migrations/264_theory_motivated_interactions.sql"
      to: "feature_registry.parent_features"
      via: "exactly 2 tier-0 parents per row, consumed by partial_spearman_ic"
      pattern: "parent_features"
---

<objective>
Build the Theory-Motivated Interaction Layer proper: 10 curated compound features, each with a
stated finance-theory hypothesis, each a single operation on two numeric tier-0 columns.

Purpose: ROADMAP's design rules for this phase are explicit — a curated layer, NOT a combinatorial
factory. ~30K compound candidates in a BH-FDR pool at FDR=0.05 produces ~1,500 expected false
discoveries and destroys the correction's power guarantees; that design (deferred todo 019) was
rejected on statistical-power grounds before this phase existed. The ≤50 cap is the Musk-step-3
"simplify" already applied at design time, not a scope limitation. Combined with plan 151-05's 5
named candidates and the 8 pre-existing live rows, this brings the tier-1 population to 23 — well
inside the cap, and sized so BH-FDR retains real power.

**Resolution of `151-RESEARCH.md` Open Question 2 (categorical-regime encoding), decided here:**
ROADMAP's candidate-source list names "momentum × low_vol_regime" and "mean-reversion × regime
label," but `market_regimes` and `feature_vectors.regime` are categorical STRINGS, not numeric
`FeatureVector` columns — a product needs two numeric operands. Decision: option (a) from
RESEARCH.md, numeric-proxy substitution. Every "× regime" candidate uses an existing numeric tier-0
regime proxy (`hv_ratio` for volatility expansion/contraction, `adx` for trend strength, `hurst`
and `variance_ratio_fast` for persistence/mean-reversion, `vix_z` for market-wide volatility,
`yield_slope_z` for term-structure regime). Rationale: this keeps every compound a persisted
numeric column that `partial_spearman_ic`'s 2-control contract can screen directly, and avoids
opening a second stratification surface alongside the one `ic_engine` already owns.

Output: 10 new `FeatureVector` fields, 10 `feature_vectors` columns, 10 `feature_registry` rows
(`tier='1_interaction'`), a documented redundancy check, unit coverage. No new APR keys — every
compound is parameterless by the single-operation design rule.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-RESEARCH.md
@.planning/phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-PATTERNS.md
@.planning/phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-05-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
Live contracts (verified 2026-07-24 by DB query — do not re-derive):

The 13 `tier=1_interaction` rows that exist after plan 151-05 (8 pre-existing + 5 named):
    price_vol_corr_fast, price_vol_corr_slow, range_vol_product, ret_vol_product_fast,
    ret_vol_ratio_fast, up_vol_body_diff, vol_body_product, vol_skew_product,
    ret_div_1m_5m, ret_div_5m_1h, ret_div_1h_1d, opex_flag, quad_witching_flag
Existing row shape, e.g. `vol_body_product` → `parent_features={body_ratio,volume_z}`,
`formula_short='body_ratio * volume_z'`, `group_name='volume'`,
`normalization='unbounded_ratio'`, `linear_ready=false`.

**Every parent named in this plan is verified `tier='0_atomic'` AND computed inside
`FeatureFactory` (not enrichment-populated):** `momentum_z_fast`, `momentum_reversal_z`, `adx`,
`hurst`, `hv_ratio`, `variance_ratio_fast`, `efficiency_ratio_fast`, `dist_from_high_fast`,
`amihud_illiq_z`, `volume_z`, `quarter_position`, `vix_z`, `yield_slope_z`.

**Do NOT use `volatility_rank_z`, `momentum_rank_z`, or `volume_rank_z` as a parent.** They are
`tier='0_atomic'` but they are the 3 nullable CROSS-SECTIONAL rank fields, `None` at
`FeatureFactory.compute()` time and populated only by the Phase 139 enrichment pass — a product
built on them would be `None` for every row the factory writes.

`vix_z` and `yield_slope_z` are correct on the BATCH path (`_build_cross_asset_series`) and frozen
at 0.0 on the LIVE path — the pre-existing gap filed as a todo by plan 151-04 Task 4. IC screening
runs off the batch path, so the two macro-conditioned compounds below are measurable; note the
asymmetry in the SUMMARY.

After plan 151-05: `len(dataclasses.fields(FeatureVector))` == 205, `feature_registry` has 205 rows,
`FEATURE_VECTOR_INSERT_SQL_PSYCOPG2` has 214 placeholders. This plan takes them to 215 / 215 / 224.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Redundancy pre-check against the live atomic and interaction set</name>
  <files>.planning/phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-06-SUMMARY.md</files>
  <read_first>
    - src/intelligence/feature_factory.py — grep each proposed parent name to confirm it is bound as a local variable at the `FeatureVector(...)` constructor site in BOTH `compute()` and `compute_batch()`
    - src/intelligence/schemas.py lines 1204-1300 (confirm none of the 13 proposed parents is one of the 3 nullable cross-sectional fields)
  </read_first>
  <action>
Before writing any code, run the redundancy check the ROADMAP's own design discipline requires
(the same check that caught `days_to_month_end` == `1 - month_position` and the migration-211
`new_high_flag`/`new_low_flag` removal).

1. Query all 205 live `feature_registry` rows with their `formula_short`:
   `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT tier, feature_name, parent_features, formula_short FROM feature_registry ORDER BY tier, feature_name;"`
2. For each of the 10 proposed compounds below, confirm in writing that it is NOT an exact
   duplicate, an affine transform, or the same operation on the same parent pair as any existing
   row. Pay specific attention to the near-neighbours: `ret_vol_ratio_fast`
   (`ret_lag_fast / atr_z`) and `ret_vol_product_fast` (`ret_lag_fast * volume_z`) — a
   `momentum_z_fast * atr_z` product would sit uncomfortably close to the first, which is exactly
   why `hv_ratio` (volatility EXPANSION/CONTRACTION) is the chosen volatility-regime proxy here
   rather than `atr_z` (volatility LEVEL).
3. Confirm every one of the 13 named parents is bound as a local at both constructor sites.
4. Record the check's verdict per compound in the SUMMARY. If any compound fails, do NOT silently
   drop it — replace it with a stated alternative from ROADMAP's candidate-source list and record
   both the failure and the substitution.

The 10 compounds, with their binding parent pairs and hypotheses:

| # | feature_name | operation | parents | one-sentence hypothesis |
|---|---|---|---|---|
| 1 | momentum_vol_regime_product | momentum_z_fast * hv_ratio | {momentum_z_fast, hv_ratio} | Momentum carries more strongly when realized volatility is contracting than when it is expanding (low-volatility anomaly, Frazzini & Pedersen 2014). |
| 2 | momentum_trend_product | momentum_z_fast * adx | {momentum_z_fast, adx} | Momentum persists when directional trend strength is high and decays into noise when the tape is choppy. |
| 3 | breakout_volume_product | dist_from_high_fast * volume_z | {dist_from_high_fast, volume_z} | A breakout confirmed by abnormal volume continues more often than an unconfirmed one. |
| 4 | reversion_hurst_product | momentum_reversal_z * hurst | {momentum_reversal_z, hurst} | Short-term reversal is stronger when the series is anti-persistent (Hurst below 0.5) and weaker when it is trending. |
| 5 | quarter_momentum_product | quarter_position * momentum_z_fast | {quarter_position, momentum_z_fast} | Quarter-end window dressing and dealer gamma unwind amplify existing momentum into the quarter close. |
| 6 | variance_ratio_momentum_product | variance_ratio_fast * momentum_z_fast | {variance_ratio_fast, momentum_z_fast} | Momentum is informative when the variance ratio indicates trending microstructure and is noise when it indicates mean reversion. |
| 7 | illiquidity_momentum_product | amihud_illiq_z * momentum_z_fast | {amihud_illiq_z, momentum_z_fast} | Illiquidity amplifies price impact, so identical order flow moves price further and momentum persists longer. |
| 8 | yield_slope_momentum_product | yield_slope_z * momentum_z_fast | {yield_slope_z, momentum_z_fast} | Term-structure regime conditions equity momentum — the carry-times-term-structure candidate source. |
| 9 | vix_reversion_product | vix_z * momentum_reversal_z | {vix_z, momentum_reversal_z} | The short-term reversal premium is larger in high-volatility regimes, where liquidity provision is compensated more. |
| 10 | efficiency_volume_product | efficiency_ratio_fast * volume_z | {efficiency_ratio_fast, volume_z} | Directional efficiency confirmed by volume distinguishes a real trend from directionless drift. |

Every entry is a SINGLE operation (all products here). No multi-step composition — per ROADMAP's
design rule, a multi-step composition is a model, not a feature.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM feature_registry WHERE feature_name IN ('momentum_vol_regime_product','momentum_trend_product','breakout_volume_product','reversion_hurst_product','quarter_momentum_product','variance_ratio_momentum_product','illiquidity_momentum_product','yield_slope_momentum_product','vix_reversion_product','efficiency_volume_product');" | grep -qx 0 && echo "OK: no name collisions"</automated>
  </verify>
  <acceptance_criteria>
    - None of the 10 proposed `feature_name` values already exists in `feature_registry` (query above returns 0)
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM feature_registry WHERE feature_name IN ('momentum_z_fast','momentum_reversal_z','adx','hurst','hv_ratio','variance_ratio_fast','efficiency_ratio_fast','dist_from_high_fast','amihud_illiq_z','volume_z','quarter_position','vix_z','yield_slope_z') AND tier='0_atomic';"` returns 13
    - The SUMMARY contains a per-compound redundancy verdict (10 lines), each naming the closest existing row it was checked against
    - No proposed parent is `volatility_rank_z`, `momentum_rank_z`, or `volume_rank_z`
  </acceptance_criteria>
  <done>All 10 compounds are confirmed novel against the 205 live rows, all 13 parents confirmed tier-0 and factory-computed, verdicts recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the 10 compounds and close the persistence contract</name>
  <files>src/intelligence/schemas.py, src/intelligence/feature_factory.py, src/intelligence/features/feature_vector_persistence.py</files>
  <read_first>
    - src/intelligence/feature_factory.py lines 4300-4400 (compute() constructor local-variable block)
    - src/intelligence/feature_factory.py — grep `ofi_div_val = ofi_z_val - momentum_z_fast_val` for the existing inline-compound precedent
    - src/intelligence/feature_factory.py lines 4727-4830 (_cold_start_vector)
    - src/intelligence/features/feature_vector_persistence.py lines 75-135, 170-260, 271-315
    - src/intelligence/schemas.py lines 1204-1300
  </read_first>
  <action>
Add 10 `float` fields to `FeatureVector`, declared as one contiguous block immediately after
`quad_witching_flag` (plan 151-05's last field), in the table order from Task 1:
`momentum_vol_regime_product`, `momentum_trend_product`, `breakout_volume_product`,
`reversion_hurst_product`, `quarter_momentum_product`, `variance_ratio_momentum_product`,
`illiquidity_momentum_product`, `yield_slope_momentum_product`, `vix_reversion_product`,
`efficiency_volume_product`.

These are non-nullable `float`. Every parent is always computed (defaulting to 0.0 at cold start),
so the product is always defined.

Update the class docstring: opening line to `215 orthogonal feature primitives`, tally to
`Total: 215 (203 required + 12 optional ...)`, and add a group line
`Theory-Motivated Interactions (10, Phase 151 Plan 06, tier=1_interaction): ...` naming them.

Compute each as a single multiplication of two already-bound local variables at the
`FeatureVector(...)` constructor site in BOTH `compute()` and `compute_batch()` — exactly the
existing inline-compound precedent `ofi_div_val = ofi_z_val - momentum_z_fast_val`. Do NOT add
`_PrecomputedSeries` entries, do NOT add `_series_full` helpers, and do NOT introduce any window,
threshold, or scaling constant: a compound with a tunable parameter would violate the
single-operation design rule and would need an APR key this plan deliberately does not add.

Guard against non-finite propagation, and make the guard OBSERVABLE. Addresses cross-AI review
concern (Codex, MEDIUM: "`151-06`'s finite-guard choice for compound products
(`nan_to_num(..., posinf=0.0, neginf=0.0)`) can silently collapse extreme but valid values to zero,
hiding tail behavior rather than merely protecting against overflow").

The idiom already exists - use it, do not invent a clamp: `_guard(v, fallback=0.0)` in
`src/intelligence/feature_factory.py` (grep `def _guard`), which returns
`v if math.isfinite(v) else fallback` and passes `None` through unchanged. Do NOT write a bare
`np.nan_to_num(..., posinf=0.0, neginf=0.0)`.

Two points to settle explicitly rather than leave implied:

- **Explicit range clipping is REJECTED here, deliberately.** A documented clip range would be a
  tunable numeric constant, which this plan's own single-operation design rule forbids and which
  would require an APR key this plan intentionally does not add. The numeric argument for not needing
  one: `math.isfinite` fires only on `nan`/`±inf`, never on a large-but-finite value, so it is
  structurally incapable of collapsing an "extreme but valid" number - and a float64 product of two
  z-scores cannot reach `±inf` short of roughly 1e154 per factor, unreachable for a z-score. The
  guard is a tripwire, not a value-shaping clamp. Record this reasoning in the SUMMARY so the
  rejection is a decision on record rather than an omission.

- **A tripwire that fires silently is still a silent wrong answer.** Add a counted wrapper used ONLY
  by these 10 compounds - e.g. `_guard_counted(v, name)` - that increments a module-level
  `dict[str, int]` when the guard actually substitutes the fallback. Report it ONCE per
  `compute_batch()` call as a single structured log line naming only the compounds whose count is
  non-zero (and emitting nothing at all when every count is zero), then reset the counters. Never log
  per row - CLAUDE.md's never-log-per-row-over-the-corpus rule, and this runs over 36.7M rows. Leave
  `_guard` itself untouched: it is used by the ~190 pre-existing fields and perturbing it is out of
  scope for this plan.

Add all 10 to `_cold_start_vector()` at `0.0` (a product of two cold-start-zero parents is 0.0).

Add 10 `FEATURE_VECTOR_DOMAIN` entries, mapped to the domain of the compound's dominant parent:
`"quant"` for #1-#7 and #10, `"macro"` for #8 and #9.

Then extend `src/intelligence/features/feature_vector_persistence.py`:
1. Module-docstring paragraph dated to this session recording 214 → 224 columns (migration 264,
   Phase 151 Plan 06), matching the existing dated-paragraph style.
2. An eighth derived slice `_PHASE151_THEORY_INTERACTION_FIELD_NAMES` spanning index of
   `momentum_vol_regime_product` through index of `efficiency_volume_product` inclusive — the
   `_STRUCTURAL_VP_SR_FIELD_NAMES` idiom, never a hand-typed list.
3. Append to the `FEATURE_VECTOR_INSERT_SQL` column list, append the `$N` placeholder range block,
   extend `_TOTAL_COLUMNS`.
4. Update `feature_vector_to_insert_params`' docstring column map and change `214-element` to
   `224-element`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_feature_vector_persistence_completeness.py tests/unit/intelligence/test_feature_factory_batch.py tests/unit/intelligence/test_feature_factory_p7.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import dataclasses; from src.intelligence.schemas import FeatureVector; print(len(dataclasses.fields(FeatureVector)))"` prints 215
    - `python -c "from src.intelligence.features.feature_vector_persistence import FEATURE_VECTOR_INSERT_SQL_PSYCOPG2 as s; print(s.count('%s'))"` prints 224
    - `test_every_feature_vector_field_is_a_sql_column` and `test_insert_params_tuple_length_matches_sql` both pass
    - A unit test asserts each of the 10 equals the exact product of its two parents on the same FeatureVector, to 1e-12, on a synthetic bar window
    - `grep -c "_PrecomputedSeries" src/intelligence/feature_factory.py` is unchanged from before this task (no new series machinery added)
    - No new numeric literal other than the finite-guard defaults appears in the 10 compound expressions
    - `grep -c "_guard_counted" src/intelligence/feature_factory.py` is at least 11 (one definition plus 10 call sites) and `grep -c "nan_to_num" src/intelligence/feature_factory.py` returns 0
    - A unit test asserts that a product overflowing to `inf` emits `0.0` AND increments that compound's guard counter to exactly 1 - the substitution is countable, not invisible
    - A unit test asserts the guard-count log line is emitted at most once per `compute_batch()` call and is absent entirely when all counts are zero
  </acceptance_criteria>
  <done>10 compounds compute as single products of already-bound parents in both live and batch paths, with no new series machinery and no new tunable parameter, and all reach the INSERT tuple.</done>
</task>

<task type="auto">
  <name>Task 3: Migration 264 and cap verification</name>
  <files>production/migrations/264_theory_motivated_interactions.sql, tests/unit/intelligence/test_feature_registry_service.py</files>
  <read_first>
    - production/migrations/263_named_interaction_primitives.sql (this phase's prior interaction migration)
    - production/migrations/255_vp_structural_primitives.sql (full template)
  </read_first>
  <action>
Re-verify the next-free migration number: `ls production/migrations/ | sort -t_ -k1 -n | tail -5`.
Provisional target 264 (259-263 reserved by plans 151-01 through 151-05). If taken, use the actual
next-free number and update the filename.

Write `production/migrations/264_theory_motivated_interactions.sql`, `BEGIN;`/`COMMIT;`-wrapped.
No APR-key section — this migration adds zero tunables by design; state that explicitly in the
header comment so a reviewer does not read the absence as an oversight.

Section 2 DDL — 10 `ALTER TABLE feature_vectors ADD COLUMN IF NOT EXISTS <name> DOUBLE PRECISION;`
in dataclass order, each with a `COMMENT ON COLUMN feature_vectors.<name> IS '<operation>. <the
one-sentence hypothesis from Task 1>. Phase 151.';`

Section 3 `feature_registry` INSERT — 10 rows, all `tier='1_interaction'`, `requires_htf=false`,
`normalization='unbounded_ratio'`, `linear_ready=false`, `status='active'`, `added_phase='151'`,
`ON CONFLICT (feature_name) DO NOTHING`. `normalization`/`linear_ready` match the
`vol_body_product` precedent for products of z-scores.

`parent_features` and `formula_short` come verbatim from Task 1's table. **`formula_short` must
carry the one-sentence hypothesis, not just the arithmetic** — ROADMAP's design rule requires the
hypothesis text in `formula_short`, and it is what makes a surviving compound explainable rather
than an unattributable BH-FDR survivor. Format each as
`<operation> -- <hypothesis sentence>`.

`group_name` per row: momentum_vol_regime_product→volatility, momentum_trend_product→momentum,
breakout_volume_product→structure, reversion_hurst_product→momentum,
quarter_momentum_product→calendar, variance_ratio_momentum_product→volatility,
illiquidity_momentum_product→volume, yield_slope_momentum_product→macro,
vix_reversion_product→macro, efficiency_volume_product→structure.

Section 4 — Phase 170 `concept_registry` parity (added retroactively: this plan was written
2026-07-24, before Phase 170 shipped its dual-write cutover 2026-08-04/05). `ic_engine.py`'s
`_apply_feature_transitions` now runs a PARITY PRECONDITION on every corpus pass that hard-stops
if any `feature_registry` row lacks a `concept_registry` counterpart (Phase 170 Plan 06,
T-170-17). Same as plan 151-05, these are `tier='1_interaction'` rows with real `parent_features`,
so `concept_parent` edges are required too. Add immediately after Section 3, same file, same
`BEGIN`/`COMMIT`, deriving every field from the `feature_registry` rows Section 3 just inserted —
field mapping matches migration 284's feature-domain seed exactly:

```sql
-- Phase 170 parity: concept_registry/concept_gate/concept_parent mirror for this migration's 10 rows.
INSERT INTO concept_registry
    (domain, name, description, status, enabled, group_name, is_control, control_expectation,
     added_phase, created_at, metadata)
SELECT
    'feature', fr.feature_name, fr.formula_short, fr.status, (fr.status = 'active'),
    fr.group_name, fr.is_control, fr.control_expectation, fr.added_phase, fr.added_at,
    jsonb_strip_nulls(jsonb_build_object(
        'tier', fr.tier, 'formula_short', fr.formula_short, 'normalization', fr.normalization,
        'linear_ready', fr.linear_ready, 'requires_htf', fr.requires_htf,
        'window_apr_keys', to_jsonb(fr.window_apr_keys), 'source_dims', to_jsonb(fr.source_dims),
        'notes', fr.notes, 'apr_namespace', 'feature.'
    ))
FROM feature_registry fr
WHERE fr.feature_name IN (
    'momentum_vol_regime_product', 'momentum_trend_product', 'breakout_volume_product',
    'reversion_hurst_product', 'quarter_momentum_product', 'variance_ratio_momentum_product',
    'illiquidity_momentum_product', 'yield_slope_momentum_product', 'vix_reversion_product',
    'efficiency_volume_product'
)
ON CONFLICT (domain, name) DO NOTHING;

INSERT INTO concept_gate
    (concept_id, gate_metric_name, gate_eval_method, min_gate_metric, min_gate_n,
     fdr_required, fdr_alpha)
SELECT cr.concept_id, 'ic_sharpe_hac', 'bootstrap_ci', fr.min_ic_sharpe, fr.min_ic_n,
       fr.fdr_required, fr.fdr_alpha
FROM feature_registry fr
JOIN concept_registry cr ON cr.domain = 'feature' AND cr.name = fr.feature_name
WHERE fr.feature_name IN (
    'momentum_vol_regime_product', 'momentum_trend_product', 'breakout_volume_product',
    'reversion_hurst_product', 'quarter_momentum_product', 'variance_ratio_momentum_product',
    'illiquidity_momentum_product', 'yield_slope_momentum_product', 'vix_reversion_product',
    'efficiency_volume_product'
)
ON CONFLICT (concept_id) DO NOTHING;

INSERT INTO concept_parent (child_concept_id, parent_concept_id)
SELECT child.concept_id, parent.concept_id
FROM feature_registry fr
CROSS JOIN LATERAL unnest(fr.parent_features) AS pf(parent_name)
JOIN concept_registry child  ON child.domain = 'feature' AND child.name = fr.feature_name
JOIN concept_registry parent ON parent.domain = 'feature' AND parent.name = pf.parent_name
WHERE fr.feature_name IN (
    'momentum_vol_regime_product', 'momentum_trend_product', 'breakout_volume_product',
    'reversion_hurst_product', 'quarter_momentum_product', 'variance_ratio_momentum_product',
    'illiquidity_momentum_product', 'yield_slope_momentum_product', 'vix_reversion_product',
    'efficiency_volume_product'
)
ON CONFLICT DO NOTHING;
```

Add to this task's acceptance criteria:
- `SELECT count(*) FROM concept_registry WHERE domain='feature' AND added_phase='151' AND name IN ('momentum_vol_regime_product','momentum_trend_product','breakout_volume_product','reversion_hurst_product','quarter_momentum_product','variance_ratio_momentum_product','illiquidity_momentum_product','yield_slope_momentum_product','vix_reversion_product','efficiency_volume_product');` returns 10
- `SELECT count(*) FROM concept_gate cg JOIN concept_registry cr USING (concept_id) WHERE cr.domain='feature' AND cr.added_phase='151' AND cr.name IN ('momentum_vol_regime_product','momentum_trend_product','breakout_volume_product','reversion_hurst_product','quarter_momentum_product','variance_ratio_momentum_product','illiquidity_momentum_product','yield_slope_momentum_product','vix_reversion_product','efficiency_volume_product');` returns 10
- `SELECT count(*) FROM concept_parent cp JOIN concept_registry child ON child.concept_id = cp.child_concept_id WHERE child.domain='feature' AND child.name IN ('momentum_vol_regime_product','momentum_trend_product','breakout_volume_product','reversion_hurst_product','quarter_momentum_product','variance_ratio_momentum_product','illiquidity_momentum_product','yield_slope_momentum_product','vix_reversion_product','efficiency_volume_product');` returns 20

Apply:
`PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/264_theory_motivated_interactions.sql`

Add a permanent cap-guard test to `tests/unit/intelligence/test_feature_registry_service.py`
alongside plan 151-05's `test_every_interaction_row_has_exactly_two_parents`:
  `test_interaction_tier_population_within_cap` — asserts the count of `tier='1_interaction'` rows
  is `<= 50`, with a docstring citing ROADMAP Phase 151's design rule and the BH-FDR power
  rationale (50 tests at FDR=0.05 has well-understood power; 30K does not). Use the same
  mock/fixture style as the neighbouring tests.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM feature_registry;"` returns 215
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM feature_registry WHERE tier='1_interaction';"` returns 23
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM feature_registry WHERE tier='1_interaction' AND array_length(parent_features,1) <> 2;"` returns 0
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM feature_registry WHERE added_phase='151' AND tier='1_interaction' AND formula_short LIKE '%--%';"` returns 10 (every new row carries a hypothesis clause)
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM config_history WHERE changed_by='migration_264';"` returns 0 (zero tunables by design)
    - `test_interaction_tier_population_within_cap` exists and passes
    - `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
  <done>Migration 264 applied; the tier-1 population is 23 of a 50 cap; every row carries 2 parents and a stated hypothesis; the cap is guarded by a permanent test.</done>
</task>

</tasks>

<threat_model>
No applicable ASVS Level 1 category (internal compute + DDL over already-ingested OHLCV, no
external input, no auth surface, no new packages). Per `151-RESEARCH.md`'s Security Domain section.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-151-11 | Tampering | statistical validity of the interaction BH-FDR pool | mitigate | `test_interaction_tier_population_within_cap` makes the ≤50 design rule a machine-enforced invariant rather than a prose commitment a future migration can quietly breach |
| T-151-12 | Repudiation | unexplainable surviving compounds | mitigate | Every row's `formula_short` carries its hypothesis; the acceptance criteria assert all 10 contain the `--` hypothesis delimiter |
</threat_model>

<verification>
1. `.venv/bin/pytest tests/unit/ -q` green.
2. `.venv/bin/ruff check src/intelligence/` exits 0.
3. Registry/dataclass alignment: 215 == 215.
4. `ops_interaction_primitives_pilot.py::_load_interaction_features` returns 23 rows against the
   live DB without raising its 2-parent `ValueError`.
</verification>

<success_criteria>
- 10 new `DOUBLE PRECISION` columns, each a single product of two factory-computed tier-0 parents
- 10 new `tier='1_interaction'` rows, each with exactly 2 parents and a stated hypothesis
- `concept_registry` (domain='feature') and `concept_gate` each have 10 matching new rows, plus 20 `concept_parent` edges (Phase 170 parity — Section 4)
- Total tier-1 population 23, inside the ≤50 cap, guarded by a permanent test
- Zero new APR keys (parameterless by design)
- Redundancy verdict recorded per compound
- Full unit suite green
</success_criteria>

<output>
Create `.planning/phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-06-SUMMARY.md` when done.
Record the ACTUAL migration number, the 10 redundancy verdicts, the finite-guard idiom chosen, and
the Open-Question-2 resolution as implemented.
</output>
