---
phase: 160-concept-registry-mvp
plan: 04
type: execute
wave: 3
depends_on: [160-01, 160-02, 160-03]
files_modified:
  - docs/research/concept-unified-registry.md
  - docs/research/concept-governance-registries.md
  - .planning/todos/pending/118-migrate-feature-domain-into-concept-registry.md
  - .planning/todos/pending/112-concept-registry.md
  - .planning/todos/PRIORITIES.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "The canonical doc concept-unified-registry.md records the invariant-6 exception for domain='ensemble_strategy' and flips its Status line to MVP BUILT"
    - "The hub doc concept-governance-registries.md's three status cells reflect the shipped MVP"
    - "The domain='feature' migration follow-on is filed as a new todo under the next free number (109 is taken; expected 118 - verify at execution time) with the ic_engine lifecycle-hook rewiring in its scope"
    - "The follow-on todo 118 ALSO carries the four review-driven automation-hardening items: H-1 F3 evidence-mass empirical check, L-2 CAS the gate-cache updates, L-3 non-zero exit on REGISTRY: FAILED, L-4 validate operator concept args when automated"
    - "Every reference to the follow-on todo number is consistent (doc-edit steps and the todo filename use the SAME number)"
    - "Todo 112 is closed (moved to completed) and struck from PRIORITIES.md; todo 058 stays a frozen historical record"
    - "The commit that closes todo 112 names 112 (NOT 058 - the source doc's Task 7 commit string is wrong; L-8)"
    - "ruff + black are clean on all changed code files; pytest tests/unit/ -q is green; the Done-Coding SOP (simplify -> review) runs"
    - "The feature branch merges to main --ff-only"
  artifacts:
    - path: ".planning/todos/pending/118-migrate-feature-domain-into-concept-registry.md"
      provides: "the deferred domain='feature' follow-on (D-02), scoped to include ic_engine.py lifecycle-hook rewiring PLUS the H-1/L-2/L-3/L-4 automation-hardening items"
      contains: "Migrate domain='feature'"
    - path: "docs/research/concept-unified-registry.md"
      provides: "invariant-6 ensemble_strategy exception + MVP BUILT status line"
      contains: "ensemble_strategy"
  key_links:
    - from: "docs/research/concept-unified-registry.md status line"
      to: "migrations 232/233 + ConceptRegistryService"
      via: "MVP BUILT status wording naming the shipped artifacts"
      pattern: "MVP BUILT"
    - from: ".planning/todos/completed/112-concept-registry.md"
      to: "PRIORITIES.md"
      via: "112 entry removed from the priority list"
      pattern: "112"
---

<objective>
Finalize the phase: sync the two Concept Registry design docs to the shipped-MVP state, file the
deferred `domain='feature'` follow-on todo under the correct next-free number (carrying the D-02
scope PLUS the four review-driven automation-hardening items), close the live tracking todo (112),
run the Done-Coding SOP, and merge the feature branch to main.

Purpose: Closes the goal loop — the registry is not just built but documented as built (the "why
don't we use this anymore, queryable ten years from now" intent depends on the design docs pointing
at the live tables), and the deferred scope (plus the hardening the review surfaced) is captured as
real future work rather than lost.

Output: updated `concept-unified-registry.md` + `concept-governance-registries.md`, a new follow-on
todo, closed todo 112, and a merged main.

Depends on plans 160-01/02/03 (docs reference the shipped migrations/service by name; the final
lint + full-suite + merge gate everything).
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-concept-registry-mvp-planned/160-CONTEXT.md
@docs/plans/2026-07-13-concept-registry-mvp-implementation-plan.md

<notes>
- The implementation plan doc is authoritative for the EXACT doc-edit text and the follow-on todo
  body: Task 6 Steps 1-4 (the invariant-6 exception paragraph, the canonical Status line, the three
  hub-doc status cells, and the follow-on todo markdown) and Task 7 Steps 1-5 (lint/format, full
  suite, Done-Coding SOP, close the tracking todo, merge). Copy verbatim, with the todo-number
  correction (D-03) and the L-8 commit-message correction below.
- CONTEXT.md D-03 correction (MANDATORY): the source doc's Task 6 files the follow-on as "todo 109"
  and its Step 2/Step 3 doc edits cite "todo 109". That number is TAKEN
  (completed/109-fisher-z-ci-bracket-clamp-belongs-in-ic-math.md). At plan-authoring time the next
  free todo number is 118 (pending/completed/deferred top out at 117). Re-verify at execution time
  with: `ls .planning/todos/pending .planning/todos/completed .planning/todos/deferred | grep -oE '^[0-9]{3}' | sort -n | tail -1`
  then use highest+1. File the follow-on under THAT number and use the SAME number in every "todo
  109" spot the source doc writes into the two design docs (Step 2 canonical Status line, Step 3a/3b/3c
  hub-doc cells). Do NOT write "todo 109" anywhere.
- CONTEXT.md D-02: the follow-on todo's scope MUST include rewiring ic_engine.py's LIFECYCLE hook to
  write through the new service (not assumed a trivial add-on), and be sequenced only after
  ensemble_strategy governance has run at least one real A/B promotion/demotion cycle live. The
  source doc's todo body already carries this scope - preserve it, and ADD the four hardening items
  (H-1/L-2/L-3/L-4) called out in Task 6 Step 4 below.
- CONTEXT.md D-01/D-04: these doc edits describe ensemble_strategy as shipped and feature/regime_model
  as NOT migrated. Do not overstate - feature stays governed by feature_registry.
- L-8 (commit-message correction): Task 7 closes todo 112 (the live tracker), NOT 058 (058 is already
  in completed/ as a frozen historical duplicate - leave it untouched). The source doc's Task 7
  commit-message string reads "close todo 058" - that is wrong; write the close commit message to name
  112 (e.g. `docs(160): close todo 112, concept registry MVP shipped`), never 058.
- No em dashes in new doc text or commit messages. No AI attribution in commits. State corrected
  facts plainly (no review-narrative annotations) per the planning-doc editing style.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 6: Documentation sync + file the domain='feature' follow-on todo</name>
  <files>docs/research/concept-unified-registry.md, docs/research/concept-governance-registries.md, .planning/todos/pending/118-migrate-feature-domain-into-concept-registry.md</files>
  <read_first>
    - docs/plans/2026-07-13-concept-registry-mvp-implementation-plan.md (Task 6 Steps 1-4 - exact anchor text and replacement text for each edit, and the full follow-on todo body)
    - docs/research/concept-unified-registry.md (Invariant 6's closing sentence and the header Status line to locate the edit anchors)
    - docs/research/concept-governance-registries.md (the three status cells: Type 2 row in "Three complementary types", the "Type 2 - Lifecycle Registries" Status line, the "Implementation Status" table row)
  </read_first>
  <action>
    Confirm the next free todo number first (see notes; expected 118). Then apply the source doc's
    Task 6 edits:
    (1) In `concept-unified-registry.md`, append the invariant-6 exception paragraph for
        `domain='ensemble_strategy'` (source doc Task 6 Step 1) directly after Invariant 6's closing
        sentence, verbatim.
    (2) In the same file, replace the leading portion of the header `**Status**:` line with the
        "MVP BUILT 2026-07-13 (todo 058, migrations 232/233)" text from Step 2, but write the follow-on
        todo reference as the verified next-free number (e.g. "todo 118"), NOT "todo 109". Renumber the
        migration references if 232/233 shifted.
    (3) In `concept-governance-registries.md`, make the three replacements from Step 3a/3b/3c (Type 2
        status cell, the Type 2 section Status line, the Implementation Status table row), again writing
        the follow-on as the verified next-free number, not 109.
    (4) Create `.planning/todos/pending/<NNN>-migrate-feature-domain-into-concept-registry.md` (NNN =
        verified next free number, expected 118) with the todo body from Step 4, preserving its D-02
        scope: migrate the ~61 feature_registry rows, port record_transition_sync CAS semantics, REPOINT
        ic_engine.py's post-run lifecycle hook + ensemble_trainer + integrity_monitor, and retire
        feature_registry only after a shadow-mode corpus run verifies identical decisions (D-01/D-02).
        ADD to the todo a "Registry-mechanism hardening (from Phase 160 review, do before automating the
        promotion path)" section carrying these four items verbatim in intent:
        - H-1: Before the first live --challenger-concept promotion cycle, run the F3 evidence-mass floor
          empirical-viability check - measure sum(n_independent) delta across two successive
          alpha_ensemble_ic corpus vintages vs min_new_observations=2000; if the rolling-window delta is
          structurally ~0/negative, redefine the F3 delta (per-stratum growth over the same strata set,
          or corpus new-bar count) or revisit the migration-232 seed before the floor is load-bearing.
        - L-2: CAS the record_win/record_loss gate-cache read-modify-write (currently not
          compare-and-swapped, load outside any transaction) before the feature hot path inherits it
          under ic_engine write pressure.
        - L-3: Make REGISTRY: FAILED paths exit non-zero once the recording path is invoked from
          ops_corpus_pipeline_run.sh automation (silent-failure class this project forbids).
        - L-4: Validate operator-supplied concept args (--champion-concept and any others) against the
          registry when the path is automated (manual-CLI existence check already added in plan 160-03).
  </action>
  <acceptance_criteria>
    - `grep -c "ensemble_strategy" docs/research/concept-unified-registry.md` is > 0 and the file's
      Status line begins with "MVP BUILT" (grep confirms), naming migrations 232/233 and the service.
    - `grep -n "MVP live\|MVP shipped\|✅" docs/research/concept-governance-registries.md` shows all
      three status cells updated to the shipped state.
    - The follow-on todo file exists under the verified next-free number, mentions ic_engine.py
      lifecycle-hook rewiring in its Scope, AND contains the four hardening items (grep the todo for
      `F3 evidence-mass`, `CAS`, `exit non-zero`, `champion` or equivalent tokens).
    - Verify NO occurrence of the string "todo 109" was written:
      `grep -rn "todo 109\|todo-109" docs/research/concept-unified-registry.md docs/research/concept-governance-registries.md .planning/todos/pending/` returns nothing new from this task.
    - The follow-on todo filename number matches the number written into both design docs (consistency).
    - Commit created touching only the two design docs + the new todo file.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Final verification, close todo 112, merge to main</name>
  <files>.planning/todos/pending/112-concept-registry.md, .planning/todos/PRIORITIES.md</files>
  <read_first>
    - docs/plans/2026-07-13-concept-registry-mvp-implementation-plan.md (Task 7 Steps 1-5 - lint/format commands, full-suite expectation, Done-Coding SOP, the close-112 steps, and the --ff-only merge sequence)
    - CLAUDE.md (Done-Coding SOP: simplify -> review -> pytest tests/unit/ green -> feature branch -> --ff-only merge)
    - .planning/todos/PRIORITIES.md (existing convention for how completed todos are removed/struck)
  </read_first>
  <action>
    Run the source doc's Task 7 sequence:
    (1) `ruff check --fix` then `black` on the four changed code files
        (src/intelligence/concept_registry_service.py, scripts/ops/alpha/ops_ensemble_weight_compare.py,
        tests/unit/test_concept_registry_service.py, tests/unit/test_ensemble_weight_compare.py). If
        black reformats, re-run the affected test files and amend.
    (2) `.venv/bin/pytest tests/unit/ -q` must be green (the pre-existing unrelated
        test_no_smooth_or_backward_in_factory failure, if present on main, is not a regression).
    (3) Done-Coding SOP steps 1-2: run the code-simplifier over the changed files, then /review; apply
        fixes as additional commits and re-run the suite.
    (4) Close todo 112 (the live tracker): `git mv .planning/todos/pending/112-concept-registry.md
        .planning/todos/completed/112-concept-registry.md`, remove the 112 line from PRIORITIES.md
        (match the file's existing completed-item convention), and grep for other pending references
        (`grep -rn "todo 112\|todo-112" docs/foundation docs/research docs/intelligence .planning/todos/pending`);
        correct any other doc still calling 112 pending, plainly. Do NOT touch todo 058 (already a
        frozen completed duplicate). Commit with a message NAMING 112, NOT 058 (L-8): e.g.
        `docs(160): close todo 112, concept registry MVP shipped`. Never write "close todo 058".
    (5) Merge per SOP: `git checkout main && git merge --ff-only <feature-branch> && git branch -d
        <feature-branch> && git worktree prune && git push origin main`. If --ff-only fails because
        main advanced (concurrent sessions), rebase the feature branch onto main, re-run step 2, then merge.
    Verify `git status --short` before each add/commit (shared-dir rule).
  </action>
  <acceptance_criteria>
    - `.venv/bin/ruff check src/intelligence/concept_registry_service.py scripts/ops/alpha/ops_ensemble_weight_compare.py tests/unit/test_concept_registry_service.py tests/unit/test_ensemble_weight_compare.py` reports no errors.
    - `.venv/bin/pytest tests/unit/ -q` is green (new tests included, no regression vs main).
    - `.planning/todos/completed/112-concept-registry.md` exists and `.planning/todos/pending/112-concept-registry.md` does not; PRIORITIES.md no longer lists 112.
    - The close commit message names todo 112, not todo 058 (L-8): `git log -1 --format=%s` on the
      close commit contains "112" and does NOT contain "058".
    - Todo 058 remains untouched in `.planning/todos/completed/`.
    - Feature branch merged to main via `--ff-only` (or rebased-then-merged if main advanced) and pushed.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Both design docs describe the shipped MVP with a consistent follow-on todo number; the follow-on
todo is filed with the ic_engine rewiring in scope PLUS the H-1/L-2/L-3/L-4 hardening items; todo 112
closed with a correctly-numbered commit message (L-8); the full unit suite is green; the branch is
merged to main.
</verification>

<success_criteria>
- concept-unified-registry.md and concept-governance-registries.md reflect the shipped MVP; no "todo 109" string written; follow-on number consistent across docs and the todo filename.
- The follow-on todo (D-02) scopes the ic_engine.py lifecycle-hook rewiring, the shadow-mode retirement gate, and the four review-driven hardening items (H-1/L-2/L-3/L-4).
- Todo 112 closed and struck from PRIORITIES.md with a commit naming 112 not 058 (L-8); todo 058 untouched.
- ruff/black clean, pytest tests/unit/ green, Done-Coding SOP run, branch merged --ff-only to main.
</success_criteria>

<output>
Create `.planning/phases/160-concept-registry-mvp-planned/160-04-SUMMARY.md` when done.
</output>
