---
phase: 160-concept-registry-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/232_concept_registry_mvp.sql
  - production/migrations/233_concept_registry_seed_ensemble_strategy.sql
autonomous: true
requirements: []

must_haves:
  truths:
    - "The four tables concept_registry / concept_gate / concept_transition_log / concept_annotation exist in the live DB"
    - "concept_transition_log is a TimescaleDB hypertable partitioned on triggered_at"
    - "The three alpha.concept_registry.ensemble_strategy_* APR keys exist in config_state with values 2, 2000, 1000"
    - "domain='ensemble_strategy' is seeded with exactly 5 concepts, 5 gate rows, 1 genesis transition, 12 annotations"
    - "ic_proportional seeds status=active; e1_shrunk_ic / e2_mean_variance / e3_hierarchical_pooling / e4_decay_half_life seed status=candidate"
    - "Both migrations are idempotent (re-run yields INSERT 0 0 and identical counts)"
    - "No reference-architecture tables (concept_gate_template, concept_gate_stack, concept_eval_run) are created"
  artifacts:
    - path: "production/migrations/232_concept_registry_mvp.sql"
      provides: "four-table schema + 3 APR gate keys (config_schema/config_state/config_history)"
      contains: "CREATE TABLE IF NOT EXISTS concept_registry"
    - path: "production/migrations/233_concept_registry_seed_ensemble_strategy.sql"
      provides: "seed of domain='ensemble_strategy' from verified 2026-07-13 live state"
      contains: "INSERT INTO concept_registry"
  key_links:
    - from: "concept_gate"
      to: "concept_registry"
      via: "concept_id FK, PRIMARY KEY REFERENCES concept_registry(concept_id)"
      pattern: "REFERENCES concept_registry"
    - from: "concept_transition_log"
      to: "ic_proportional genesis row"
      via: "trigger_reason='genesis_seed'"
      pattern: "genesis_seed"
---

<objective>
Create and apply the two raw-SQL migrations that stand up the Concept Registry MVP schema and seed
`domain='ensemble_strategy'` from Phase 142B.1's verified 2026-07-13 live state.

Purpose: This is the persistence foundation of the phase goal — the four-table evidence-gated
lifecycle registry that answers "why don't we use this strategy anymore" queryably. Everything else
in the phase (the service, the wiring) reads these exact table/column names.

Output: `production/migrations/232_concept_registry_mvp.sql` (schema + APR keys) and
`production/migrations/233_concept_registry_seed_ensemble_strategy.sql` (5 concepts / 5 gates /
1 genesis transition / 12 annotations), both applied and verified against the live DB.

Scope (CONTEXT.md D-01/D-04): seed ONLY `domain='ensemble_strategy'`. The `domain` CHECK includes
`'feature'` but ZERO feature rows are seeded here — `feature_registry` remains the live governor for
`domain='feature'` (that migration is the deferred follow-on todo filed in plan 160-04). Do NOT seed
or touch `domain='regime_model'` (zero rows, row-grain question correctly deferred per D-04).
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-concept-registry-mvp-planned/160-CONTEXT.md
@docs/plans/2026-07-13-concept-registry-mvp-implementation-plan.md

<notes>
- The implementation plan doc above is authoritative for the EXACT SQL of both migrations
  (Task 1 Step 3 for migration 232; Task 2 Step 1 for migration 233 — hundreds of lines of
  verified DDL and seed VALUES). Copy that SQL verbatim; do not re-derive it.
- DB access: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent` (plain
  `psql -U postgres` fails; `-h localhost` is mandatory).
- APR mandate: the three gate keys go into config_schema + config_state + config_history with
  provenance tags in their descriptions ([initial_estimate] / [conventional]). This is already
  written into the migration 232 SQL in the source doc.
- No em dashes in any SQL comments or commit messages. No AI attribution in commits.
- Migration numbers 232/233 verified FREE at plan-authoring time (production/migrations/ tips out
  at 231_alpha_events_is_shadow.sql). Task 1 Step 2 re-verifies at execution time; if a 232+ file
  landed since, renumber BOTH migrations to the next free pair and fix every cross-reference.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 232 - four-table schema + APR gate keys</name>
  <files>production/migrations/232_concept_registry_mvp.sql</files>
  <read_first>
    - docs/plans/2026-07-13-concept-registry-mvp-implementation-plan.md (Task 1, especially Step 3's full SQL and the "Global Constraints" scope guards)
    - CLAUDE.md (APR mandate section — provenance tags, config_schema/config_state/config_history pattern)
  </read_first>
  <action>
    First re-confirm 232 is the next free migration number: `ls production/migrations/ | sort -t_ -k1 -n | tail -3`
    must end with `231_alpha_events_is_shadow.sql`. If a 232+ file exists, renumber this and Task 2's
    migration to the next free pair and update every reference.

    Create `production/migrations/232_concept_registry_mvp.sql` with EXACTLY the SQL from the source
    doc's Task 1 Step 3. It creates four tables inside a single BEGIN/COMMIT: `concept_registry`
    (PK `concept_id UUID`, `UNIQUE (domain, name)`, `domain CHECK IN ('feature','ensemble_strategy')`,
    `status CHECK IN ('candidate','shadow_only','active','deprecated')`); `concept_gate` (PK
    `concept_id` REFERENCES concept_registry, with folded eval-state cache columns incl.
    `baseline_metric`, `promotion_consecutive`, `promotion_eval_metrics DOUBLE PRECISION[]`,
    `last_eval_corpus_build_ref`, `min_new_observations`, `min_gate_n`, `min_promotion_consecutive`,
    `fdr_required`, `fdr_alpha`); `concept_transition_log` (PK `(id, triggered_at)`, `corpus_build_ref TEXT`,
    `trigger_reason` CHECK including `'genesis_seed'`, made a TimescaleDB hypertable on `triggered_at`
    via `create_hypertable(..., if_not_exists => TRUE)`); `concept_annotation`. Then INSERT the three
    APR keys `alpha.concept_registry.ensemble_strategy_min_promotion_consecutive` (2),
    `alpha.concept_registry.ensemble_strategy_min_new_observations` (2000),
    `alpha.concept_registry.ensemble_strategy_min_observations` (1000) into config_schema + config_state
    + config_history with the provenance-tagged descriptions from the source doc. All statements
    idempotent (CREATE TABLE IF NOT EXISTS, ON CONFLICT DO NOTHING).

    Do NOT create concept_gate_template, concept_gate_stack, concept_eval_run, or any other
    reference-architecture table (source doc Global Constraints, todo 058 item 1 / F7).

    Apply it: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/232_concept_registry_mvp.sql`
  </action>
  <acceptance_criteria>
    - Applying the migration ends with `COMMIT` and prints no `ERROR` lines (NOTICE lines from IF NOT
      EXISTS are fine).
    - Verification query (source doc Task 1 Step 5): `to_regclass` returns non-null for all four
      concept_* tables AND null for `concept_gate_template` (the `no_template` guard column is `t`).
    - `SELECT count(*) FROM timescaledb_information.hypertables WHERE hypertable_name='concept_transition_log'`
      returns `1`.
    - `SELECT config_key, config_value FROM config_state WHERE config_key LIKE 'alpha.concept_registry.%'
      ORDER BY config_key` returns exactly the 3 keys with values `2`, `2000`, `1000`.
    - Commit created touching only the migration file (verify `git status --short` before staging).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migration 233 - seed domain='ensemble_strategy' from verified live state</name>
  <files>production/migrations/233_concept_registry_seed_ensemble_strategy.sql</files>
  <read_first>
    - docs/plans/2026-07-13-concept-registry-mvp-implementation-plan.md (Task 2 Step 1's full seed SQL, and the "Verified Live State (2026-07-13)" + "Status assignments" sections that justify each status)
    - production/migrations/232_concept_registry_mvp.sql (the CHECK vocabularies and column names this seed must satisfy — must already exist from Task 1)
  </read_first>
  <action>
    Create `production/migrations/233_concept_registry_seed_ensemble_strategy.sql` with EXACTLY the SQL
    from the source doc's Task 2 Step 1 (renumber to match Task 1 if 232/233 shifted). It seeds, inside
    a single BEGIN/COMMIT: 5 `concept_registry` rows with exact names `ic_proportional` (status=active,
    enabled=true, the genesis incumbent), `e1_shrunk_ic` (candidate, enabled=true — deployed
    champion-by-default recorded as an observation annotation, NOT as status per the F2 resolution),
    `e2_mean_variance` (candidate, enabled=true), `e3_hierarchical_pooling` (candidate, enabled=false,
    thesis-only), `e4_decay_half_life` (candidate, enabled=false, thesis-only), each carrying
    `metadata->'recipe'` (definitional ic_input/weight_method pair, null for E3/E4); 5 `concept_gate`
    rows (`gate_metric_name='ensemble_ic_ci_lower'`, `gate_eval_method='oos_holdout'`, `min_gate_n=1000`,
    `fdr_required=true`, NULL min_promotion_consecutive/min_new_observations = inherit the APR defaults
    from migration 232); 1 genesis `concept_transition_log` row for `ic_proportional` only
    (`trigger_reason='genesis_seed'`, corpus_build_ref NULL, guarded by NOT EXISTS); and 12
    `concept_annotation` rows (all `source='human'`, guarded for idempotency by
    `(concept, type, left(content,40))`). Every INSERT is ON CONFLICT DO NOTHING or WHERE NOT EXISTS.

    Apply it: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/233_concept_registry_seed_ensemble_strategy.sql`
    Then apply it a SECOND time to prove idempotency.
  </action>
  <acceptance_criteria>
    - First apply ends with `COMMIT`, no `ERROR` lines, and the INSERT tags report `INSERT 0 5`
      (registry), `INSERT 0 5` (gates), `INSERT 0 1` (transition), `INSERT 0 12` (annotations).
    - Seed verification (source doc Task 2 Step 3): 5 concepts with statuses
      `ic_proportional=active` and `e1_shrunk_ic / e2_mean_variance / e3_hierarchical_pooling /
      e4_decay_half_life = candidate`; `gates=5`; one transition row `ic_proportional | genesis_seed`;
      12 annotation rows all `source=human`.
    - Second apply reports all INSERT tags `INSERT 0 0` and the counts re-query returns identical
      5 / 5 / 1 / 12 (idempotency proven).
    - Commit created touching only the seed migration file (verify `git status --short` before staging).
  </acceptance_criteria>
</task>

</tasks>

<verification>
Both migrations applied cleanly and idempotently; four tables + hypertable + 3 APR keys live;
domain='ensemble_strategy' seeded exactly as specified. Cross-check the final combined state with
the source doc's Task 1 Step 5 and Task 2 Step 3 verification queries.
</verification>

<success_criteria>
- All four concept_* tables exist; concept_transition_log is a hypertable on triggered_at.
- Exactly the 3 alpha.concept_registry.* APR keys present with values 2 / 2000 / 1000.
- 5 concepts / 5 gates / 1 genesis transition / 12 annotations seeded, statuses exactly as specified.
- Re-running migration 233 changes nothing (idempotent).
- No reference-architecture tables created; no feature/regime_model rows seeded.
</success_criteria>

<output>
Create `.planning/phases/160-concept-registry-mvp-planned/160-01-SUMMARY.md` when done.
</output>
