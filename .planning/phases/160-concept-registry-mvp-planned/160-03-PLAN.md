---
phase: 160-concept-registry-mvp
plan: 03
type: execute
wave: 2
depends_on: [160-01, 160-02]
files_modified:
  - scripts/ops/alpha/ops_ensemble_weight_compare.py
  - tests/unit/test_ensemble_weight_compare.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "_registry_outcome reduces per-stratum verdicts to (won, eval_metric, eval_n): won iff any stratum verdict is WIN; eval_metric = mean challenger ic_ci_lower over WIN strata only (WIN-FDR-VETO excluded); eval_n = challenger n_independent summed over ALL compared strata"
    - "ops_ensemble_weight_compare.py gains optional --challenger-concept / --champion-concept / --corpus-build-ref args"
    - "Without --challenger-concept the script's report-only output is byte-identical to today (backward compatible)"
    - "With --challenger-concept the script calls ConceptRegistryService.record_comparison_outcome inside an acquired pool connection and prints a REGISTRY: line with the decision action"
    - "The champion side is never written (a challenger LOSS does not demote, a WIN does not displace; F2)"
    - "All tests in test_ensemble_weight_compare.py and test_concept_registry_service.py pass together"
  artifacts:
    - path: "scripts/ops/alpha/ops_ensemble_weight_compare.py"
      provides: "_registry_outcome pure helper + CLI args + registry-recording block wired to ConceptRegistryService"
      contains: "def _registry_outcome"
    - path: "tests/unit/test_ensemble_weight_compare.py"
      provides: "3 new _registry_outcome tests appended"
      contains: "_registry_outcome"
  key_links:
    - from: "ops_ensemble_weight_compare.py registry block"
      to: "ConceptRegistryService.record_comparison_outcome"
      via: "service call inside async with pool.acquire() as conn"
      pattern: "record_comparison_outcome"
    - from: "ops_ensemble_weight_compare.py"
      to: "alpha.concept_registry.ensemble_strategy_* APR keys"
      via: "config_state SELECT resolves the default_min_* floors"
      pattern: "alpha.concept_registry"
---

<objective>
Wire `ops_ensemble_weight_compare.py`'s win-decision gate to `ConceptRegistryService` so that it
becomes the sole deterministic status-flipper for `domain='ensemble_strategy'` (invariant 1), while
keeping the report-only path byte-identical to today when no concept args are passed.

Purpose: This is the phase goal's "names the win-decision gate as the sole deterministic
status-flipper" deliverable. It joins the schema/seed (plan 160-01) and the service (plan 160-02)
into a runnable governance path.

Output: a pure `_registry_outcome` helper + three optional CLI args + a registry-recording block in
`scripts/ops/alpha/ops_ensemble_weight_compare.py`, plus 3 new unit tests.

Depends on plan 160-02 (imports `ConceptRegistryService` / `ConceptNotFoundError`) and plan 160-01
(the smoke test and the recording path need migrations 232/233 applied so the APR keys and seeded
concept rows exist).
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-concept-registry-mvp-planned/160-CONTEXT.md
@docs/plans/2026-07-13-concept-registry-mvp-implementation-plan.md
@scripts/ops/alpha/ops_ensemble_weight_compare.py

<notes>
- The implementation plan doc is authoritative for the EXACT code: Task 5 Step 1 (the 3 new tests),
  Task 5 Step 3 (the `_registry_outcome` helper, placed directly after `_final_verdict`), Task 5
  Step 5a-5c (the service import, the 3 CLI args, and the registry-recording block that replaces the
  final `return 0`). Copy verbatim; do not re-derive.
- Concept identity arrives via CLI, never derived from `weight_version`: `weight_version` is a
  per-corpus-build epoch tag (migration 224 - "a data-scoping tag only"); the recipe is selected by
  APR at trainer run time and not recorded on ensemble_weights. `--corpus-build-ref` defaults to the
  challenger weight_version (WEIGHT_EPOCH IS the corpus-build identity).
- D-15 citation rule: the recorded metric is `ic_ci_lower`, never `ic_value`. A WIN-FDR-VETO stratum
  is NOT a promotable win and must not inflate the recorded evidence.
- CRITICAL (source doc Task 5 Step 6): do NOT pass `--challenger-concept` against live data in this
  work. The live champion/challenger pair (run_2025122405150000 vs run_2025122405150000_mv) is the
  INVALIDATED pre-todo-094 all-long comparison; recording it would write exactly the outcome Phase
  143.1 ruled non-carrying. The recording path is exercised only by unit tests here.
- Exception variable name is `error`. structlog: no `event=` kwarg. No em dashes. No AI attribution.
</notes>

<interfaces>
<!-- Consumed from plan 160-02 (already shipped in wave 1). Import these EXACT names. -->
From src/intelligence/concept_registry_service.py:
  class ConceptRegistryService  # async record_comparison_outcome(conn, *, domain, name, won,
      #   eval_metric, eval_n, corpus_build_ref, default_min_promotion_consecutive,
      #   default_min_new_observations, default_min_gate_n, notes=None) -> ComparisonDecision
  class ConceptNotFoundError(Exception)

<!-- Produced by this plan (used at execution time only). -->
_registry_outcome(rows: list[dict]) -> tuple[bool, float | None, float]
  # each row dict: {"verdict": str, "ic_ci_lower": float|None, "n_independent": float|None}
CLI args: --challenger-concept, --champion-concept, --corpus-build-ref (all default None)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 5: Wire the win-decision gate to the registry (invariant 1)</name>
  <files>scripts/ops/alpha/ops_ensemble_weight_compare.py, tests/unit/test_ensemble_weight_compare.py</files>
  <read_first>
    - docs/plans/2026-07-13-concept-registry-mvp-implementation-plan.md (Task 5 — full test block at Step 1, the _registry_outcome helper at Step 3, and the wiring diff at Steps 5a/5b/5c, plus the Interfaces + backward-compat note)
    - scripts/ops/alpha/ops_ensemble_weight_compare.py (existing _final_verdict, main(), stratum_data / challenger_by_stratum / strata structures, pool lifecycle)
    - src/intelligence/concept_registry_service.py (record_comparison_outcome signature it must call)
  </read_first>
  <behavior>
    - _registry_outcome([{WIN,0.04,1000},{WIN,0.02,2000},{WIN-FDR-VETO,0.90,500},{LOSS,-0.01,1500}]) == (True, 0.03, 5000.0) (mean over WIN strata only; n summed over ALL strata; veto excluded)
    - _registry_outcome with no WIN strata -> (False, None, sum of non-None n_independent)
    - _registry_outcome([]) -> (False, None, 0.0)
  </behavior>
  <action>
    First append the 3 new tests from the source doc's Task 5 Step 1 to
    `tests/unit/test_ensemble_weight_compare.py` (`_registry_outcome` mean-over-WIN-strata,
    no-win-strata, and empty cases). Run and confirm they fail with
    `ImportError: cannot import name '_registry_outcome'` while existing tests pass.

    Then modify `scripts/ops/alpha/ops_ensemble_weight_compare.py`:
    (a) Add the pure `_registry_outcome(rows)` helper directly after `_final_verdict` using the exact
        code from the source doc Task 5 Step 3 — won = any WIN verdict; eval_metric = mean challenger
        `ic_ci_lower` over WIN strata only (skip None and WIN-FDR-VETO); eval_n = summed
        `n_independent` over all compared strata (skip None).
    (b) Add the `from src.intelligence.concept_registry_service import (ConceptNotFoundError,
        ConceptRegistryService)` import (Step 5a) and the three optional args `--challenger-concept`,
        `--champion-concept`, `--corpus-build-ref` (Step 5b, all default None).
    (c) Replace the FINAL `return 0` in main() (the one after the D-15 reporting print, NOT the
        early-exit return 0s) with the registry-recording block from Step 5c: only when
        `args.challenger_concept` is set, build outcome_rows per stratum, call `_registry_outcome`,
        default `corpus_build_ref` to `args.challenger`, acquire a pool connection, SELECT the three
        `alpha.concept_registry.ensemble_strategy_*` keys from config_state (fail with a printed
        "REGISTRY: FAILED - ... apply migration 232" if fewer than 3 present), call
        `ConceptRegistryService().record_comparison_outcome(...)` with the APR-resolved default_min_*
        floors and the invariant-6-citing `notes`, catch `ConceptNotFoundError as error` and print
        "REGISTRY: FAILED - {error}", then print the `REGISTRY: concept=... -> action=...` line. The
        champion side is deliberately never written (F2).
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_ensemble_weight_compare.py -v`: existing tests still pass plus
      the 3 new _registry_outcome tests pass (mean == 0.03, veto excluded, empty == (False, None, 0.0)).
    - Backward-compat smoke (source doc Task 5 Step 6): running the script with only
      `--champion run_2025122405150000 --challenger run_2025122405150000_mv` (NO --challenger-concept)
      produces the same "## D-12 Ensemble Weight Compare" report as before with NO `REGISTRY:` line,
      exit code 0. Do NOT pass --challenger-concept against live data.
    - `.venv/bin/pytest tests/unit/test_ensemble_weight_compare.py tests/unit/test_concept_registry_service.py -v`
      all pass together.
    - Commit created touching only the ops script + its test file.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI args -> registry write | --challenger-concept / --corpus-build-ref determine which concept is mutated and against which corpus identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-05 | Spoofing | recipe identity via weight_version | mitigate | concept identity is supplied explicitly via --challenger-concept, never inferred from the weight_version epoch tag (migration 224); corpus_build_ref defaults to the challenger weight_version (the WEIGHT_EPOCH corpus-build identity) |
| T-160-06 | Repudiation | recording an invalidated comparison | mitigate | operational guard: --challenger-concept is not passed against the invalidated pre-todo-094 live pair; recording path exercised by unit tests only until the Phase 143.1 re-run |
| T-160-07 | Tampering | inflated evidence from veto strata | mitigate | _registry_outcome excludes WIN-FDR-VETO strata from eval_metric and uses ic_ci_lower (D-15), never ic_value |
| T-160-SC | Tampering | new package installs | accept | no new dependencies added; imports only the in-repo ConceptRegistryService and existing stdlib |
</threat_model>

<verification>
Report-only path unchanged; recording path green under unit tests; the three APR keys resolve the
default floors; champion side never written.
</verification>

<success_criteria>
- _registry_outcome passes all 3 new tests with the exact expected tuples.
- Report-only invocation is byte-identical to pre-change output (no REGISTRY line).
- The recording block calls ConceptRegistryService with APR-resolved floors and prints the decision.
- Combined test run (compare + registry service files) is fully green.
</success_criteria>

<output>
Create `.planning/phases/160-concept-registry-mvp-planned/160-03-SUMMARY.md` when done.
</output>
