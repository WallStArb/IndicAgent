---
phase: 165-swing-fib-trend-structure-primitives
plan: 05
type: execute
wave: 5
depends_on: ["165-04"]
files_modified:
  - src/intelligence/feature_factory.py
  - tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py
autonomous: true
requirements: ["D-01", "D-07", "D-08", "D-09", "D-12", "D-13"]

must_haves:
  truths:
    - "All 16 session-levels columns carry real, varying ATR-distance / percent / flag values in both compute paths — the last 16 of Phase 165's 41 None placeholders are gone"
    - "Not one raw session or weekly price level is persisted: prior_session_high, overnight_low, weekly_pivot, weekly_r1..s2, nearest_session_level and asian_session_high exist as in-function locals only, never as FeatureVector fields or columns (D-16 rule inherited from Phase 163)"
    - "Weekly pivot/R1/R2/S1/S2 are computed from the PRIOR COMPLETED ISO week's high/low/close, never the week in progress — all five are None until a first week has rolled over"
    - "On tf=='1d' the five intraday-only fields (overnight_high_dist_atr, overnight_low_dist_atr, overnight_range_pct, asian_session_high_dist_atr, asian_session_low_dist_atr) are None, following the _NEUTRAL_VP_EXTRA tf=='1d' precedent"
    - "Zero Phase 165 kwargs remain hardcoded None at either _build_feature_vector call site — all 41 columns of the phase now carry computed values"
  artifacts:
    - path: "src/intelligence/feature_factory.py"
      provides: "_SESSION_LEVELS_FALLBACK all-None constant, _SESSION_LEVELS_DAILY_SUPPRESSED, _derive_session_levels(), and both compute paths wired"
      contains: "_derive_session_levels"
    - path: "tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py"
      provides: "session-levels test class plus the phase-closing completeness gate over all 41 columns"
      contains: "test_session_levels_all_41_phase165_fields_wired"
  key_links:
    - from: "src/intelligence/feature_factory.py _derive_session_levels"
      to: "FeatureCache._sl_* / _prior_wk_* state (Plan 04)"
      via: "read raw cache levels + compute-path atr_val, exactly as _derive_session_vp does"
      pattern: "cache\\._sl_prior_session_close"
    - from: "_derive_session_levels"
      to: "weekly pivot arithmetic"
      via: "wp = (prior_wk_high + prior_wk_low + prior_wk_close) / 3.0, then R1/R2/S1/S2"
      pattern: "_prior_wk_close"
    - from: "compute() and compute_batch()"
      to: "_build_feature_vector's 16 session-levels kwargs"
      via: "keyword pass-through replacing the last Plan 01 None placeholders"
      pattern: "gap_filled="
---

<objective>
Derive the final 16 Phase 165 columns from the session/weekly state Plan 04 built, wire them into both
compute paths, and close the phase with a completeness gate proving all 41 columns now carry computed
values.

Purpose: Plan 04 deliberately shipped state with zero `FeatureVector` consumers so the boundary
semantics could be proven in isolation. This plan is the thin, verifiable mapping on top —
`FeatureCache` raw levels plus the compute-path `atr_val` in, ATR-distances and bounded scalars out,
never a raw price persisted. It follows `_derive_session_vp()`'s shape exactly (Phase 163) and
`_derive_amd_cycle()`'s (Phase 164).

Output: one pure derivation helper, both `compute()` and `compute_batch()` wired, the session-levels
regression tests appended to the phase test file, and a gate asserting no Phase 165 kwarg is still a
hardcoded `None`.
</objective>

<source_coverage_audit>
Plan 05's slice of the phase-wide audit recorded in full in 165-01-PLAN.md. Items covered HERE:

GOAL (ROADMAP Phase 165): the 16 `session_levels.py` columns — the second half of the sub-phase ROADMAP
scoped as "built as its own plan within this phase". COVERED by Tasks 1-3.

REQ: no `.planning/REQUIREMENTS.md` exists. 165-CONTEXT.md's D-XX decision IDs are the requirement set.

RESEARCH (165-RESEARCH.md):
- Section 1 `session_levels.py` (only prior_session_*/overnight_*/weekly_* needed the rewrite; the
  Asian sub-feature needs "the standard raw-price -> ATR-distance conversion, same as every other file
  in this phase") -> Task 1 converts all four sub-concepts uniformly. COVERED.
- Section 4 ("session_levels.py is its own plan within this phase, not its own phase") -> satisfied by
  Plans 04+05 both living inside Phase 165. COVERED.

CONTEXT (165-CONTEXT.md):
- D-01 nullable discipline: all-None fallback, per-field None on missing source level. COVERED.
- D-07/D-08/D-09 consumed as state from Plan 04; this plan adds the weekly pivot arithmetic on the
  prior-completed-week snapshot. COVERED.
- D-12 promotion-bar note: `price_position`'s extended near-neighbour screen is recorded in migration
  267's COMMENT (Plan 01); the IC test itself runs via `ic_engine`, explicitly out of phase scope.
  COVERED as documentation.
- D-13 `gap_filled` surfaced as a column from Plan 04's latch. COVERED (Task 1).
- Claude's Discretion (`nearest_session_level_type` categorical companion) — deliberately NOT added,
  per the Deferred Ideas list. Recorded, not silently dropped.

Exclusions (NOT gaps — explicit CONTEXT.md Deferred Ideas): `bars_since_gap_fill`;
`nearest_session_level_type`; running the incremental-IC promotion test itself. Historical
`feature_vectors` backfill is deferred to todo 176's consolidated 163/164/165
`--compute-only --refresh` pass (STATE.md Tier 0 sequencing), not this phase.
</source_coverage_audit>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/165-swing-fib-trend-structure-primitives/165-CONTEXT.md
@.planning/phases/165-swing-fib-trend-structure-primitives/165-RESEARCH.md
@.planning/phases/165-swing-fib-trend-structure-primitives/165-PATTERNS.md
@.planning/phases/165-swing-fib-trend-structure-primitives/165-01-SUMMARY.md
@.planning/phases/165-swing-fib-trend-structure-primitives/165-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Verified live at plan-writing time (2026-07-28, AFTER Phase 164 landed). Re-anchor on the named
     symbols, not the integers, if 164-era code shifts again. -->

Derivation template A — _derive_session_vp(cache, close_, atr_val, poc_price_rolling)
(src/intelligence/feature_factory.py lines 3332-3417, Phase 163). The exact shape to follow:
    atr_valid = atr_val is not None and math.isfinite(atr_val) and atr_val > 0
    ...
    def _above(level: float | None) -> float | None:
        return None if level is None or not atr_valid else (level - close_) / atr_val
    def _below(level: float | None) -> float | None:
        return None if level is None or not atr_valid else (close_ - level) / atr_val
  Reads FeatureCache's RAW levels (set by the mutator before compute()) plus the compute-path
  `atr_val`, returns only ATR-normalized / bounded values. Not one raw price crosses the return
  boundary (D-16). Reuse `_above`/`_below` verbatim in the new helper — same sign convention:
  a level ABOVE close is `(level - close_)`, a level BELOW close is `(close_ - level)`.

Derivation template B — _derive_amd_cycle(cache, bar_ts, config)
(src/intelligence/feature_factory.py lines 4525-4561, Phase 164). Shows the newer convention: a
cache-derive helper placed immediately before `_build_feature_vector`, docstring stating "never
recomputes X itself, matching _derive_session_vp()'s read raw cache state, derive bounded output
shape", plus the explicit "never raises: a cold cache reads back its dataclass defaults" note.

tf=='1d' suppression precedent — _NEUTRAL_VP_EXTRA (src/intelligence/feature_factory.py lines
3268-3290) and its call sites (`compute()` line 5060, `compute_batch()` line 5616):
    if tf == "1d":
        vp_extra: dict[str, float | None] = dict(_NEUTRAL_VP_EXTRA)
    else:
        vp_extra = _derive_session_vp(cache, close_, atr_val, poc_price_rolling)
  Phase 163's own comment (line 3269-3274) states the rationale: "a daily bar has no intraday
  distribution, so session-anchored VP is not meaningful". The same reasoning applies to exactly five
  of this plan's sixteen fields — see Task 1(d).

FeatureCache state available from Plan 04 (all `float | None`, all `repr=False`, all cold-start None
except the flag):
    cache._sl_prior_session_high / _sl_prior_session_low / _sl_prior_session_close
    cache._sl_session_open                       # open of the first bar of the current session day
    cache._sl_overnight_high / _sl_overnight_low # frozen at the last completed session-day rollover
    cache._sl_asia_high / _sl_asia_low
    cache._sl_gap_filled: float                  # 0.0 or 1.0, latched per session day (D-13)
    cache._prior_wk_high / _prior_wk_low / _prior_wk_close   # PRIOR COMPLETED ISO week only
  `cache._sl_session_high` / `_sl_session_low` / `_sl_session_close` exist but are NOT consumed here —
  they feed the `_sl_gap_filled` latch inside the mutator. Do not surface them.
  Phase 164's `cache._overnight_high` / `_overnight_low` are a DIFFERENT window (the AMD 20:00-UTC
  accumulation phase) — never read them here.

compute() call site — src/intelligence/feature_factory.py:
  line 5055: atr_val (already a local float); line 5060: the tf=='1d' VP branch;
  line 5079-5086: the S/R unpack block; Plans 02/03 append their unpacks after it.
  `close_` is a local float (line 5048), `tf` is a parameter.

compute_batch() call site — src/intelligence/feature_factory.py:
  line 5616: the tf=='1d' VP branch inside the per-bar loop; lines 5635-5652: the S/R block.
  Plan 04 added `cache.update_session_levels(bar_ts, open_, high_, low_, close_, config)` in the
  per-bar preamble (before the warm-up gate), so the cache state is already current for bar `i` by the
  time this plan's derivation runs.

_build_feature_vector — src/intelligence/feature_factory.py line 4564, keyword-only (`*,`).
  Plan 01 added all 41 Phase 165 kwargs as `float | None = None`. Plans 02/03 replaced 25 of them with
  real values at both call sites; this plan replaces the last 16.

The 16 column names and their binding formulas/NULL conditions are fixed by migration 267's
`COMMENT ON COLUMN` text (written by Plan 01 Task 1) — read those COMMENTs from the live DB
(`\d+ feature_vectors` or a `pg_description` query) and treat them as the contract, not this plan's
prose, if the two ever disagree.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _derive_session_levels() — 16 fields from Plan 04's cache state, prior-week pivot arithmetic, all-None fallback, tf=='1d' suppression</name>
  <files>src/intelligence/feature_factory.py</files>
  <behavior>
    - Given a warm cache (prior session, frozen overnight, Asian block and a completed prior ISO week all populated) and atr_val > 0 on tf='5m': all 16 fields are finite floats; gap_filled is 0.0 or 1.0; nearest_level_dist_atr >= 0.
    - Given atr_val <= 0 or non-finite: every one of the 16 fields is None (matching migration 267's COMMENT, which binds all 16 to NULL on "ATR <= 0" — deliberately including the three ATR-independent fields opening_gap_pct, overnight_range_pct and gap_filled, so behaviour and the DB documentation cannot drift apart).
    - Given a cold cache (no session has completed): prior_session_*_dist_atr, opening_gap_pct and nearest_level_dist_atr are None while the remaining fields follow their own source availability; nothing is 0.0-by-default.
    - Given `_prior_wk_high`/`_low`/`_close` are None (no ISO week has rolled over yet): all five weekly_*_dist_atr fields are None together.
    - Given a populated prior week: weekly_pivot_dist_atr == (close_ - (ph + pl + pc) / 3.0) / atr_val exactly; weekly_r1_dist_atr uses r1 = 2*wp - pl; weekly_r2_dist_atr uses r2 = wp + (ph - pl); weekly_s1_dist_atr uses s1 = 2*wp - ph; weekly_s2_dist_atr uses s2 = wp - (ph - pl).
    - Sign convention: a level ABOVE close yields a positive distance via (level - close_); a level BELOW close yields a positive distance via (close_ - level). prior_session_high/overnight_high/asian_session_high/weekly_r1/weekly_r2 use the ABOVE form; prior_session_low/prior_session_close/overnight_low/asian_session_low/weekly_pivot/weekly_s1/weekly_s2 use the BELOW form.
    - Given `_sl_overnight_low` is 0.0: overnight_range_pct is None, not a division-by-zero or an infinity.
    - Given `_sl_prior_session_close` is 0.0: opening_gap_pct is None, not 0.0.
    - Given tf == '1d': overnight_high_dist_atr, overnight_low_dist_atr, overnight_range_pct, asian_session_high_dist_atr and asian_session_low_dist_atr are None regardless of cache state; the other 11 are derived normally.
    - nearest_level_dist_atr considers exactly the 7 levels prior_session_high, prior_session_low, weekly_pivot, weekly_r1, weekly_r2, weekly_s1, weekly_s2, skipping any that are None; it is None when all 7 are None.
    - Never raises on any cache state, including an entirely default FeatureCache.
  </behavior>
  <read_first>
    - src/intelligence/feature_factory.py lines 3268-3417 (`_NEUTRAL_VP_EXTRA` + `_derive_session_vp` in full — the `_above`/`_below` helpers, the `atr_valid` guard and the "no raw price crosses the return boundary" discipline)
    - src/intelligence/feature_factory.py lines 4525-4562 (`_derive_amd_cycle` — the newer cache-derive placement and docstring convention)
    - src/intelligence/feature_cache.py — Plan 04's `_sl_*` / `_prior_wk_*` field block and `update_session_levels` docstring (the authoritative description of what each state field means and when it is None)
    - src/intelligence/features/i3_structure/session_levels.py lines 112-179 and 400-432 (the archived weekly-pivot arithmetic and the `levels`/`nearest` selection this plan ports; note lines 118-120's `week_df = prior` fallback — a prior-SESSION substitute silently labelled a weekly level — is a D-01-shaped fake value and must NOT be ported; weekly fields stay None until a real week exists)
    - The live `COMMENT ON COLUMN` text for all 16 columns: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d+ feature_vectors"` (or a pg_description query). These COMMENTs are the binding contract.
    - .planning/phases/165-swing-fib-trend-structure-primitives/165-CONTEXT.md D-01, D-07, D-12, D-13
  </read_first>
  <action>
    Add a new module-level section to src/intelligence/feature_factory.py immediately AFTER
    `_derive_amd_cycle` and BEFORE `def _build_feature_vector`, headed
    `# Session Levels — FeatureCache-derived price geometry (Phase 165 Plan 05)`.

    (a) Define `_SESSION_LEVELS_FALLBACK: dict[str, float | None]` with all sixteen keys mapped to
    `None`: prior_session_high_dist_atr, prior_session_low_dist_atr, prior_session_close_dist_atr,
    overnight_high_dist_atr, overnight_low_dist_atr, overnight_range_pct, opening_gap_pct,
    weekly_pivot_dist_atr, weekly_r1_dist_atr, weekly_r2_dist_atr, weekly_s1_dist_atr,
    weekly_s2_dist_atr, nearest_level_dist_atr, asian_session_high_dist_atr,
    asian_session_low_dist_atr, gap_filled. Comment it with the D-01 rationale and note that it is
    deliberately NOT modelled on `_NEUTRAL_VP_EXTRA`, which preserves two legacy 0.0/0.5 neutral
    defaults for backward compatibility — none of these sixteen columns has a legacy default to
    preserve, so every one is `None`.

    (b) Define `_SESSION_LEVELS_DAILY_SUPPRESSED: tuple[str, ...]` holding exactly the five
    intraday-only field names: overnight_high_dist_atr, overnight_low_dist_atr, overnight_range_pct,
    asian_session_high_dist_atr, asian_session_low_dist_atr. Comment it with the reasoning: a daily bar
    is a whole session, so it has no observable non-RTH sub-block and no Asian sub-block — on tf=='1d'
    `update_session_levels` would classify the single daily bar itself as the overnight block, making
    `overnight_high_dist_atr` a near-duplicate of `prior_session_high_dist_atr` rather than an
    independent measurement. Same class of reasoning as `_NEUTRAL_VP_EXTRA`'s tf=='1d' branch. The
    other eleven fields (prior-session, opening gap, weekly, nearest level, gap_filled) ARE meaningful
    on daily bars and are not suppressed.

    (c) Define `_derive_session_levels(cache, close_, atr_val, tf) -> dict[str, float | None]`.
    Structure it exactly like `_derive_session_vp`: `atr_valid` guard at the top returning
    `dict(_SESSION_LEVELS_FALLBACK)`; local `_above(level)` / `_below(level)` closures with the same
    sign convention; never raises. The docstring must state that it reads `FeatureCache`'s raw session
    and prior-week levels (set by `update_session_levels()` / `update_wk_vwap()`, both called once per
    bar by the caller BEFORE compute()), never recomputes them, and never returns a raw price level
    (D-16). It must also state explicitly why the atr_valid guard suppresses the three ATR-independent
    fields too: migration 267's COMMENT binds all sixteen to NULL on ATR <= 0, and letting behaviour
    diverge from the column documentation is exactly the drift this phase's discipline exists to
    prevent.
    Field derivation:
      prior_session_high_dist_atr  = _above(cache._sl_prior_session_high)
      prior_session_low_dist_atr   = _below(cache._sl_prior_session_low)
      prior_session_close_dist_atr = _below(cache._sl_prior_session_close)
      overnight_high_dist_atr      = _above(cache._sl_overnight_high)
      overnight_low_dist_atr       = _below(cache._sl_overnight_low)
      overnight_range_pct          = (on_high - on_low) / on_low when both are not None and
                                     on_low > 0, else None
      opening_gap_pct              = (cache._sl_session_open - psc) / psc when both are not None and
                                     psc > 0, else None   (psc = cache._sl_prior_session_close)
      asian_session_high_dist_atr  = _above(cache._sl_asia_high)
      asian_session_low_dist_atr   = _below(cache._sl_asia_low)
      gap_filled                   = float(cache._sl_gap_filled)
    Weekly block — read `ph = cache._prior_wk_high`, `pl = cache._prior_wk_low`,
    `pc = cache._prior_wk_close`. When any is None, all five weekly fields are None together (do NOT
    port the archived `week_df = prior` prior-session substitution — a prior SESSION labelled as a
    weekly level is a fake measurement of a different thing, D-01's failure shape). Otherwise:
      wp = (ph + pl + pc) / 3.0 ; wr = ph - pl
      weekly_pivot_dist_atr = _below(wp)
      weekly_r1_dist_atr    = _above(2.0 * wp - pl)
      weekly_r2_dist_atr    = _above(wp + wr)
      weekly_s1_dist_atr    = _below(2.0 * wp - ph)
      weekly_s2_dist_atr    = _below(wp - wr)
    Nearest level — collect the non-None raw levels among
    [cache._sl_prior_session_high, cache._sl_prior_session_low, wp, r1, r2, s1, s2] (the same 7 the
    archived `levels` list used, matching migration 267's COMMENT), and set
    `nearest_level_dist_atr = abs(close_ - min(levels, key=lambda x: abs(x - close_))) / atr_val`, or
    None when the list is empty. Those raw level values stay in-function locals — never returned.

    (d) Apply the tf=='1d' suppression at the END of the derivation, inside the helper (not at the call
    sites), so live and batch cannot diverge on the gating: when `tf == "1d"`, overwrite each name in
    `_SESSION_LEVELS_DAILY_SUPPRESSED` with `None` in the result dict before returning. Doing it inside
    the helper is a deliberate departure from `_derive_session_vp`'s call-site branch — that branch
    exists because VP additionally skips the expensive rolling-POC computation on daily bars, which has
    no analogue here. State that reasoning in the docstring so a reviewer does not read it as an
    inconsistency.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import inspect
from src.intelligence.feature_factory import _derive_session_levels, _SESSION_LEVELS_FALLBACK, _SESSION_LEVELS_DAILY_SUPPRESSED
expected={'prior_session_high_dist_atr','prior_session_low_dist_atr','prior_session_close_dist_atr','overnight_high_dist_atr','overnight_low_dist_atr','overnight_range_pct','opening_gap_pct','weekly_pivot_dist_atr','weekly_r1_dist_atr','weekly_r2_dist_atr','weekly_s1_dist_atr','weekly_s2_dist_atr','nearest_level_dist_atr','asian_session_high_dist_atr','asian_session_low_dist_atr','gap_filled'}
assert set(_SESSION_LEVELS_FALLBACK)==expected, set(_SESSION_LEVELS_FALLBACK)^expected
assert all(v is None for v in _SESSION_LEVELS_FALLBACK.values()), 'D-01: all-None fallback'
assert set(_SESSION_LEVELS_DAILY_SUPPRESSED)=={'overnight_high_dist_atr','overnight_low_dist_atr','overnight_range_pct','asian_session_high_dist_atr','asian_session_low_dist_atr'}
assert list(inspect.signature(_derive_session_levels).parameters)==['cache','close_','atr_val','tf']
print('ok signature + 16 keys + 5 suppressed')"</automated>
    <automated>.venv/bin/python -c "
from src.intelligence.feature_cache import FeatureCache
from src.intelligence.feature_factory import _derive_session_levels, _SESSION_LEVELS_FALLBACK
c=FeatureCache()
# cold cache + invalid ATR -> all None; never raises
for atr in (0.0, -1.0, float('nan'), None):
    out=_derive_session_levels(c, 100.0, atr, '5m')
    assert out==_SESSION_LEVELS_FALLBACK, out
out=_derive_session_levels(c, 100.0, 1.0, '5m')
assert set(out)==set(_SESSION_LEVELS_FALLBACK)
assert out['gap_filled']==0.0
assert out['weekly_pivot_dist_atr'] is None and out['nearest_level_dist_atr'] is None
print('ok cold cache + invalid ATR')"</automated>
    <automated>.venv/bin/python -c "
from src.intelligence.feature_cache import FeatureCache
from src.intelligence.feature_factory import _derive_session_levels
c=FeatureCache(); c._prior_wk_high=110.0; c._prior_wk_low=90.0; c._prior_wk_close=100.0
out=_derive_session_levels(c, 100.0, 2.0, '5m')
wp=(110.0+90.0+100.0)/3.0
assert abs(out['weekly_pivot_dist_atr'] - (100.0-wp)/2.0) < 1e-12, out['weekly_pivot_dist_atr']
assert abs(out['weekly_r1_dist_atr'] - ((2*wp-90.0)-100.0)/2.0) < 1e-12
assert abs(out['weekly_r2_dist_atr'] - ((wp+20.0)-100.0)/2.0) < 1e-12
assert abs(out['weekly_s1_dist_atr'] - (100.0-(2*wp-110.0))/2.0) < 1e-12
assert abs(out['weekly_s2_dist_atr'] - (100.0-(wp-20.0))/2.0) < 1e-12
print('ok weekly pivot arithmetic')"</automated>
    <automated>.venv/bin/python -c "
from src.intelligence.feature_cache import FeatureCache
from src.intelligence.feature_factory import _derive_session_levels, _SESSION_LEVELS_DAILY_SUPPRESSED
c=FeatureCache(); c._sl_overnight_high=105.0; c._sl_overnight_low=95.0; c._sl_asia_high=104.0; c._sl_asia_low=96.0; c._sl_prior_session_close=99.0; c._sl_session_open=101.0
intraday=_derive_session_levels(c, 100.0, 2.0, '5m')
daily=_derive_session_levels(c, 100.0, 2.0, '1d')
for k in _SESSION_LEVELS_DAILY_SUPPRESSED:
    assert intraday[k] is not None, ('intraday must be populated', k)
    assert daily[k] is None, ('tf==1d must suppress', k)
assert daily['opening_gap_pct'] == intraday['opening_gap_pct'] is not None, 'opening_gap_pct is meaningful on 1d'
print('ok tf==1d suppression')"</automated>
    <automated>.venv/bin/python -c "
src=open('src/intelligence/feature_factory.py').read()
i=src.index('# Session Levels — FeatureCache-derived'); j=src.index('def _build_feature_vector')
blk=src[i:j]
assert 'cache._overnight_high' not in blk and 'cache._overnight_low' not in blk, 'must not read Phase 164 AMD state'
assert 'week_df' not in blk and 'prior' in blk
body=src[src.index('def _build_feature_vector'):]
for bad in ('prior_session_high=','overnight_high=','weekly_pivot=','weekly_r1=','nearest_session_level=','asian_session_high='):
    assert bad not in body, ('raw price level leaked into the persisted path', bad)
print('ok no raw level persisted, no AMD state read')"</automated>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM information_schema.columns WHERE table_name='feature_vectors' AND column_name IN ('prior_session_high','prior_session_low','prior_session_close','overnight_high','overnight_low','weekly_pivot','weekly_r1','weekly_r2','weekly_s1','weekly_s2','nearest_session_level','asian_session_high','asian_session_low')" | grep -qx 0</automated>
    <automated>.venv/bin/ruff check src/intelligence/feature_factory.py</automated>
  </verify>
  <acceptance_criteria>
    - `_SESSION_LEVELS_FALLBACK` is all-`None` for exactly the 16 field names; `_SESSION_LEVELS_DAILY_SUPPRESSED` holds exactly the 5 intraday-only names.
    - `_derive_session_levels(cache, close_, atr_val, tf)` never raises, returns exactly the 16 keys, and returns the full fallback for `atr_val` in {0.0, -1.0, nan, None}.
    - Weekly pivot/R1/R2/S1/S2 match the hand-computed formulas to 1e-12 off `_prior_wk_*`, and are None together when any of the three prior-week values is None; the archived `week_df = prior` prior-session substitution is not ported.
    - `overnight_range_pct` and `opening_gap_pct` are `None` (not 0.0, not inf) when their denominator is 0 or None.
    - On `tf == '1d'` the 5 suppressed fields are None while `opening_gap_pct`, the prior-session trio, the weekly five, `nearest_level_dist_atr` and `gap_filled` still derive.
    - The new section never reads `cache._overnight_high` / `cache._overnight_low` (Phase 164's different window).
    - None of the 13 raw session/weekly level names appears as a `_build_feature_vector` kwarg, and zero matching columns exist in `feature_vectors`.
    - `.venv/bin/ruff check src/intelligence/feature_factory.py` clean.
  </acceptance_criteria>
  <done>Sixteen session-levels fields derive from Plan 04's proven state as ATR-distances, percents and one flag, with prior-completed-week pivot anchoring, per-field None on every missing source, tf=='1d' suppression of the five intraday-only fields, and not one raw price crossing the return boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Wire _derive_session_levels() into compute() and compute_batch() — the last 16 placeholders</name>
  <files>src/intelligence/feature_factory.py</files>
  <read_first>
    - src/intelligence/feature_factory.py lines 5055-5090 (compute()'s atr_val, the tf=='1d' VP branch, and the S/R unpack block) plus Plans 02/03's swing/trend/momentum/fib unpack blocks that now follow it
    - src/intelligence/feature_factory.py lines 5610-5660 (compute_batch()'s in-loop VP branch and S/R block) plus Plans 02/03's in-loop blocks
    - src/intelligence/feature_factory.py lines 4564-4610 (`_build_feature_vector`'s keyword-only signature and Phase 163's None-defaulted block) and the two `_build_feature_vector(...)` call sites
    - src/intelligence/feature_factory.py `_cold_start_vector` (line 6256) — confirm the 16 stay explicit `None` there; a cold-start vector has no session state by definition and must NOT be wired
  </read_first>
  <action>
    (a) `compute()`: after Plan 03's fibonacci unpack block, call
    `_session_level_fields = _derive_session_levels(cache, close_, atr_val, tf)` once, unpack all 16
    values into `<name>_val: float | None` locals, and pass each by keyword into
    `_build_feature_vector(...)`, REPLACING the `None` placeholders Plan 01 installed. Carry a comment
    naming Phase 165 Plan 05 and stating that the cache state was updated by the caller's
    `update_session_levels()` call before `compute()` ran.

    (b) `compute_batch()`: add the identical call and unpack in the per-bar loop after Plan 03's
    fibonacci block, and replace the same 16 `None` placeholders at that `_build_feature_vector(...)`
    call site. No slice argument is needed — the helper reads cache state, not arrays. Both paths in
    this same commit; this is the live/batch parity contract Task 3's parity test guards.

    (c) Leave `_cold_start_vector`'s 16 explicit `None` arguments exactly as Plan 01 wrote them and add
    a one-line comment saying so: a cold-start vector is emitted when `len(bars) < 2`, at which point no
    session, overnight, Asian block or ISO week has completed, so `None` is the correct value rather
    than an oversight. Task 3's completeness gate must not flag it.

    (d) After wiring, verify no Phase 165 kwarg is still a hardcoded `None` at either
    `_build_feature_vector(...)` call site. That is the phase's closing invariant and Task 3 turns it
    into a permanent test.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
src=open('src/intelligence/feature_factory.py').read()
assert src.count('_derive_session_levels(cache')==2, ('one call in compute(), one in compute_batch()', src.count('_derive_session_levels(cache'))
print('ok two call sites')"</automated>
    <automated>.venv/bin/python -c "
import re
src=open('src/intelligence/feature_factory.py').read()
p165=['swing_high_dist_atr','swing_low_dist_atr','swing_high_type','swing_low_type','swing_pattern','swing_high_age_bars','swing_low_age_bars','trend_direction','trend_strength','trend_leg_count','structure_integrity','price_position','trend_duration_bars','swing_amplitude_ratio','swing_amplitude_expanding','swing_amplitude_intensity','swing_velocity_bars','swing_velocity_bias','struct_energy','struct_accel_bias','swing_volume_confirmation','nearest_fib_ratio','nearest_fib_dist_atr','fib_cluster_strength','in_fib_discount_zone','prior_session_high_dist_atr','prior_session_low_dist_atr','prior_session_close_dist_atr','overnight_high_dist_atr','overnight_low_dist_atr','overnight_range_pct','opening_gap_pct','weekly_pivot_dist_atr','weekly_r1_dist_atr','weekly_r2_dist_atr','weekly_s1_dist_atr','weekly_s2_dist_atr','nearest_level_dist_atr','asian_session_high_dist_atr','asian_session_low_dist_atr','gap_filled']
assert len(p165)==41
cold=src.index('def _cold_start_vector')
scope=src[:cold]  # exclude _cold_start_vector, where explicit None is correct
bad=[f for f in p165 if re.search(r'^\s+'+f+r'=None,\s*$', scope, re.M)]
assert not bad, ('Phase 165 kwargs still hardcoded None at a compute call site', bad)
print('ok all 41 wired at both compute call sites')"</automated>
    <automated>.venv/bin/python -c "
src=open('src/intelligence/feature_factory.py').read()
cold=src[src.index('def _cold_start_vector'):]
for f in ('gap_filled','weekly_pivot_dist_atr','swing_high_dist_atr'):
    assert f+'=None' in cold, ('_cold_start_vector must keep explicit None', f)
print('ok cold-start vector unchanged')"</automated>
    <automated>.venv/bin/pytest tests/unit/ -q</automated>
    <automated>.venv/bin/ruff check src/intelligence/feature_factory.py</automated>
  </verify>
  <acceptance_criteria>
    - `_derive_session_levels(cache, ...)` is called exactly twice — once in `compute()`, once in `compute_batch()`'s per-bar loop.
    - No Phase 165 field name appears as a bare `<name>=None,` argument anywhere before `_cold_start_vector` — all 41 carry computed values at both `_build_feature_vector` call sites.
    - `_cold_start_vector` still passes explicit `None` for all 41 and carries the one-line comment explaining why.
    - `.venv/bin/pytest tests/unit/ -q` fully green; ruff clean.
  </acceptance_criteria>
  <done>All 41 Phase 165 columns carry computed values in both the live and batch compute paths; the only remaining `None` arguments are the intentional cold-start ones.</done>
</task>

<task type="auto">
  <name>Task 3: Regression tests — session-levels nullability, ATR-unit pin, weekly-pivot pin, tf=='1d' suppression, live/batch parity, and the phase-closing 41-column completeness gate</name>
  <files>tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py</files>
  <read_first>
    - tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py (FULL — Plans 02 and 03 built it; reuse the module-level `N`, `RNG`, `_make_cfg` and fixture helpers rather than redefining them)
    - tests/unit/intelligence/test_session_levels_cache.py (FULL — Plan 04's mutator tests; reuse its `_bars_utc` timestamp helper if it is importable, otherwise mirror its convention rather than inventing a third)
    - src/intelligence/feature_factory.py — Task 1's `# Session Levels` section and Task 2's wiring
    - .planning/phases/165-swing-fib-trend-structure-primitives/165-PATTERNS.md section 10 (test shapes + commit `a748d13d`'s mutation-verification discipline)
  </read_first>
  <action>
    Append to tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py. Add a module-level
    `_SESSION_LEVEL_FIELDS` 16-tuple and a `_PHASE_165_FIELDS` 41-tuple (the union of Plan 02's, Plan
    03's and this plan's field tuples, in dataclass declaration order), and extend the module docstring
    to note the file now covers all four Phase 165 sub-scopes plus the phase completeness gate.

    Every test name must be file-scoped unique and prefixed `test_session_levels_*` or `test_phase165_*`
    — the repo's pre-commit hook rejects duplicate test function names across tests/, and Plan 04
    already used the `test_session_levels_*` prefix in a DIFFERENT file, so pick names that do not
    collide with `tests/unit/intelligence/test_session_levels_cache.py` either.

    The fixtures for these tests must span at least two ET session days and one ISO-week boundary and
    must drive `cache.update_session_levels(...)` (and `cache.advance_bar(...)` for the weekly path)
    per bar in the same order the production call sites do — otherwise every field reads None and the
    tests pass vacuously. Use `compute_batch()` where possible, which already does this wiring.

    Write these tests:
    (a) `test_session_levels_non_constant_batch` — run `compute_batch` over a multi-session, multi-week
    synthetic series; assert each of the 16 fields takes at least 2 distinct non-None values across the
    bars (except `gap_filled`, for which assert both 0.0 and 1.0 occur). The direct todo-153-shaped
    guard, and the guard against a vacuously-None test suite.
    (b) `test_session_levels_cold_nullability` — run `compute_batch` over a series shorter than one
    session; assert prior_session_*_dist_atr, opening_gap_pct, all five weekly_*_dist_atr and
    nearest_level_dist_atr are `None` on the final bar, with a failure message naming D-01.
    (c) `test_session_levels_dist_in_atr_units` — reuse the constant-true-range fixture (Wilder ATR
    converges to exactly 1.0) spanning two sessions; assert `prior_session_high_dist_atr` equals the
    raw price difference between the prior session's high and the final close within 1e-6, pinning the
    ATR conversion rather than asserting finiteness.
    (d) `test_session_levels_weekly_pivot_pinned` — drive a cache across one ISO-week boundary with a
    known prior-week high/low/close, then assert all five weekly `_dist_atr` values equal the
    hand-computed pivot formulas within 1e-9, and that they were `None` on every bar of the first week.
    (e) `test_session_levels_daily_suppression` — run the same bars through `compute_batch` at tf='5m'
    and tf='1d'; assert the five `_SESSION_LEVELS_DAILY_SUPPRESSED` fields are non-None at 5m and None
    at 1d, while `opening_gap_pct` and the weekly five are non-None at both.
    (f) `test_session_levels_gap_filled_column` — the D-13 free-field guard: assert `gap_filled` is
    exactly 0.0 or 1.0 (never None once ATR is valid and a prior session exists), that both values
    occur across a fixture designed to gap and then fill, and that it is 1.0 on the bar that first
    brackets the prior session close.
    (g) `test_session_levels_live_batch_parity` — run `compute()` over the full bar list (driving the
    mutator per bar exactly as the live pipeline does) and `compute_batch()` over the same bars; assert
    all 16 fields match on the final bar to 1e-6, treating `None == None` as a match and `None` vs
    float as a failure.
    (h) `test_phase165_all_41_fields_non_constant_batch` — THE phase-closing behavioural gate. Run
    `compute_batch` over a long multi-session, multi-week fixture and assert every one of the 41
    `_PHASE_165_FIELDS` takes at least one non-None value somewhere in the run, listing by name any
    field that is None on every bar. This is the single test that catches a whole sub-scope silently
    failing to wire — the failure mode that produced todo 153.
    (i) `test_phase165_field_set_matches_registry` — assert the 41 names in `_PHASE_165_FIELDS` are
    exactly the set of `dataclasses.fields(FeatureVector)` names not present in a `FeatureVector`
    constructed before Phase 165... implement instead as the simpler, robust check: assert every name
    in `_PHASE_165_FIELDS` is a `FeatureVector` field, maps to `"structural"` in
    `FEATURE_VECTOR_DOMAIN`, and appears in the `feature_registry` rows with `added_phase='165'` (query
    the DB, skip with `pytest.skip` if no DB is reachable so the test stays CI-clean).

    Apply commit `a748d13d`'s mutation-verification discipline before considering this task done:
    temporarily force `_derive_session_levels` to return its fallback dict and confirm tests (a), (c),
    (d), (e), (f), (g) and (h) FAIL; temporarily change the weekly derivation to use the RUNNING
    `_wk_high`/`_wk_low`/`_wk_close` instead of `_prior_wk_*` and confirm test (d) FAILS; temporarily
    remove the tf=='1d' suppression and confirm test (e) FAILS. Revert all three local edits before
    committing and record the observed failures in the plan SUMMARY.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py -x -q</automated>
    <automated>.venv/bin/python -c "
src=open('tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py').read()
for t in ('test_session_levels_non_constant_batch','test_session_levels_cold_nullability','test_session_levels_dist_in_atr_units','test_session_levels_weekly_pivot_pinned','test_session_levels_daily_suppression','test_session_levels_gap_filled_column','test_session_levels_live_batch_parity','test_phase165_all_41_fields_non_constant_batch','test_phase165_field_set_matches_registry'):
    assert t in src, t
names=[l.split('(')[0] for l in src.split('def ')[1:] if l.startswith('test_')]
assert len(names)==len(set(names)), 'duplicate test function name in file'
other=[l.split('(')[0] for l in open('tests/unit/intelligence/test_session_levels_cache.py').read().split('def ')[1:] if l.startswith('test_')]
assert not (set(names) & set(other)), ('name collision across the two Phase 165 test files', set(names)&set(other))
print('ok 9 tests present, no collisions')"</automated>
    <automated>.venv/bin/python -c "
import dataclasses
from src.intelligence.schemas import FeatureVector
from src.intelligence.feature_factory import FEATURE_VECTOR_DOMAIN
p165=['swing_high_dist_atr','swing_low_dist_atr','swing_high_type','swing_low_type','swing_pattern','swing_high_age_bars','swing_low_age_bars','trend_direction','trend_strength','trend_leg_count','structure_integrity','price_position','trend_duration_bars','swing_amplitude_ratio','swing_amplitude_expanding','swing_amplitude_intensity','swing_velocity_bars','swing_velocity_bias','struct_energy','struct_accel_bias','swing_volume_confirmation','nearest_fib_ratio','nearest_fib_dist_atr','fib_cluster_strength','in_fib_discount_zone','prior_session_high_dist_atr','prior_session_low_dist_atr','prior_session_close_dist_atr','overnight_high_dist_atr','overnight_low_dist_atr','overnight_range_pct','opening_gap_pct','weekly_pivot_dist_atr','weekly_r1_dist_atr','weekly_r2_dist_atr','weekly_s1_dist_atr','weekly_s2_dist_atr','nearest_level_dist_atr','asian_session_high_dist_atr','asian_session_low_dist_atr','gap_filled']
fv={f.name for f in dataclasses.fields(FeatureVector)}
assert len(p165)==41 and set(p165)<=fv
assert all(FEATURE_VECTOR_DOMAIN[f]=='structural' for f in p165)
print('ok 41 fields in dataclass + domain')"</automated>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM feature_registry WHERE added_phase='165'" | grep -qx 41</automated>
    <automated>.venv/bin/pytest tests/unit/ -q</automated>
    <automated>.venv/bin/ruff check tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py</automated>
    <automated>.venv/bin/black --check tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py src/intelligence/feature_factory.py</automated>
  </verify>
  <acceptance_criteria>
    - All 9 named tests exist, carry names unique both within the file and against `tests/unit/intelligence/test_session_levels_cache.py`, and pass.
    - Every session-levels test drives `update_session_levels()` per bar (directly or via `compute_batch`) over a fixture spanning at least two ET session days and one ISO-week boundary, so no assertion passes vacuously on all-None output.
    - `test_session_levels_weekly_pivot_pinned` fails when the derivation is switched to the running-week accumulators (verified by the mutation check).
    - `test_session_levels_daily_suppression` fails when the tf=='1d' suppression is removed.
    - `test_phase165_all_41_fields_non_constant_batch` names every all-None field in its failure message and covers all 41 columns.
    - `test_phase165_field_set_matches_registry` cross-checks the dataclass, `FEATURE_VECTOR_DOMAIN` and `feature_registry`, and skips cleanly without a DB so it stays CI-clean.
    - The mutation check was actually run: the SUMMARY records which tests failed under (i) a forced-fallback `_derive_session_levels`, (ii) running-week pivot anchoring, and (iii) removed tf=='1d' suppression, and all three local edits were reverted.
    - `.venv/bin/pytest tests/unit/ -q` fully green; ruff and black clean.
  </acceptance_criteria>
  <done>The session-levels sub-scope has mutation-verified regression coverage, and a permanent phase-wide gate proves all 41 Phase 165 columns produce real values rather than silently staying NULL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FeatureCache accumulated state -> persisted feature_vectors row | Cache state read at the wrong boundary (or from the wrong accumulator) silently poisons every row until restart. |
| compute() vs compute_batch() -> the same logical bar | A derivation gated differently in the two paths produces train/serve skew invisible to any single-path test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-18 | Tampering (look-ahead leakage) | Anchoring weekly pivots on the week IN PROGRESS, whose `close` component is the current bar | mitigate | Derivation reads `_prior_wk_*` only, snapshotted at an ISO-week rollover; `test_session_levels_weekly_pivot_pinned` fails against running-week anchoring, verified by mutation check. |
| T-165-19 | Tampering (silent wrong data) | Porting the archived `week_df = prior` fallback, which labels a prior SESSION's high/low/close as a WEEKLY level | mitigate | All five weekly fields return None together when any `_prior_wk_*` is None; the source assertion checks the archived `week_df` name never appears in the new section. |
| T-165-20 | Information disclosure (cross-symbol leakage) | Persisting a raw session or weekly price level (non-stationary, non-comparable cross-sectionally) | mitigate | D-16 rule enforced by a source assertion that none of the 13 raw level names is a `_build_feature_vector` kwarg, plus a DB query asserting zero matching `feature_vectors` columns; the derivation returns only ATR-distances, percents and one flag. |
| T-165-21 | Denial of service (silent NULL column) | A whole sub-scope failing to wire and every column staying NULL — the todo-153 failure mode, invisible until `ic_value=0` shows up thousands of cells later | mitigate | `test_phase165_all_41_fields_non_constant_batch` is a permanent behavioural gate over all 41 columns; Task 2's source assertion additionally proves no Phase 165 kwarg remains a hardcoded `None` outside `_cold_start_vector`. |
| T-165-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero new packages. `math`, `numpy` and the `FeatureCache` state are already-live in-repo dependencies. No install task exists in this plan. |
</threat_model>

<verification>
- `_SESSION_LEVELS_FALLBACK` is all-`None` for exactly 16 keys; `_derive_session_levels` never raises and returns the full fallback on invalid ATR.
- Weekly pivot/R1/R2/S1/S2 match the hand-computed formulas off `_prior_wk_*` to 1e-12 and are None together until a first ISO week completes; the archived prior-session substitution is not ported.
- The five intraday-only fields are None on `tf == '1d'` and populated on intraday timeframes, gated inside the helper so live and batch cannot diverge.
- None of the 13 raw session/weekly level names exists as a `_build_feature_vector` kwarg or a `feature_vectors` column.
- `_derive_session_levels(cache, ...)` is called exactly twice; no Phase 165 kwarg is a bare `=None,` outside `_cold_start_vector`.
- `compute()` and `compute_batch()` agree to 1e-6 on all 16 fields.
- All 41 Phase 165 columns produce at least one non-None value across a multi-session, multi-week batch run.
- `.venv/bin/pytest tests/unit/ -q` green; ruff and black clean.
</verification>

<success_criteria>
The final 16 of Phase 165's 41 columns carry real, varying ATR-distance / percent / flag values in both
the live and batch compute paths, derived from Plan 04's timestamp-driven session and prior-completed-
week state with per-field `None` on every missing source and no raw price level persisted anywhere.
With this plan the whole phase is complete: 13 swing/trend (Plan 02) + 12 momentum/fibonacci (Plan 03)
+ 16 session-levels (Plan 05) = 41 columns, every one proven non-constant by a permanent phase-wide
gate, every tunable APR-backed, and both silent-wrong-answer bugs the phase was scoped to fix
eliminated rather than ported.
</success_criteria>

<output>
Create `.planning/phases/165-swing-fib-trend-structure-primitives/165-05-SUMMARY.md` when done.
</output>
</content>
