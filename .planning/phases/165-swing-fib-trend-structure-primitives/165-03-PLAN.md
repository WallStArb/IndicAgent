---
phase: 165-swing-fib-trend-structure-primitives
plan: 03
type: execute
wave: 3
depends_on: ["165-02"]
files_modified:
  - src/intelligence/feature_factory.py
  - tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py
autonomous: true
requirements: ["D-01", "D-03", "D-04", "D-05", "D-06", "D-15"]

must_haves:
  truths:
    - "All 8 swing-momentum fields carry real, varying values across bars; when fewer than feature.swing_momentum.max_extremes confirmed extremes exist, all 8 are None together — never a plausible-looking number"
    - "swing_velocity_bias is the numeric {-1,0,1} encoding of the archived string enum swing_velocity_trend (D-03); the string 'accelerating'/'decelerating'/'stable' appears nowhere in feature_factory.py"
    - "All 4 fibonacci fields are computed from Plan 02's in-memory swing_high_price/swing_low_price intermediates — fibonacci_zones.py's i3.get('swing_high') cross-plugin fallback is deleted outright, not reimplemented (D-05)"
    - "Zero raw fib price levels are persisted: no fib_236/fib_382/fib_500/fib_618/fib_786/nearest_fib_level/fib_swing_high/fib_swing_low field or _build_feature_vector kwarg exists (D-04)"
    - "compute() and compute_batch() produce identical values for all 12 fields on the same bar (live/batch parity)"
  artifacts:
    - path: "src/intelligence/feature_factory.py"
      provides: "_SWING_MOMENTUM_FALLBACK / _FIB_FALLBACK all-None constants, _detect_swing_extremes(), _dedup_swing_extremes(), _compute_swing_momentum(), _compute_fib_zones(), and both compute paths wired"
      contains: "_compute_swing_momentum"
    - path: "tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py"
      provides: "swing-momentum + fibonacci test classes appended to Plan 02's file: nullability, ATR-invariance proof, ATR-unit pin, live/batch parity, APR liveness, D-15 free-field guard"
      contains: "test_swing_momentum_nullability"
  key_links:
    - from: "src/intelligence/feature_factory.py _compute_fib_zones"
      to: "Plan 02's _compute_swing_structure return dict"
      via: "swing_fields['swing_high_price'] / swing_fields['swing_low_price'] in-memory intermediates (D-05)"
      pattern: "swing_fields\\[.swing_high_price.\\]"
    - from: "_compute_swing_momentum"
      to: "_detect_swing_extremes"
      via: "its OWN confirm-window detection, NOT find_peaks/find_troughs (D-06 Finding B)"
      pattern: "config\\.swing_momentum_confirm_n"
    - from: "compute_batch() per-bar loop"
      to: "_compute_swing_momentum"
      via: "highs[_sm_start : i + 1] causal slice, mirroring the Phase 163 S/R call site"
      pattern: "_sm_start"
---

<objective>
Port `i3_structure/swing_momentum.py` (8 fields, including D-15's new `swing_volume_confirmation`)
and `i3_structure/fibonacci_zones.py` (4 fields) into real values on the 12 columns Plan 01 declared,
consuming Plan 02's shared swing intermediates so `fibonacci_zones.py`'s cross-plugin fallback is
deleted outright rather than reimplemented (D-05).

Purpose: these are the last two direct-port files in the phase. `swing_momentum.py` carries six
undeclared magic numbers (RESEARCH Finding C) and two implementation-vs-docstring mismatches that must
be fixed rather than ported. `fibonacci_zones.py` carries a dead cross-plugin read that only existed
because the old wave-based pipeline could not guarantee plugin ordering — v3's `compute()` is a single
deterministic in-process function, so the fallback has no reason to exist at all.

Output: two stateless pure-compute helpers plus a private extreme-detector, wired into both `compute()`
and `compute_batch()`, and the swing-momentum/fibonacci test classes appended to the regression file
Plan 02 created.
</objective>

<source_coverage_audit>
Plan 03's slice of the phase-wide audit recorded in full in 165-01-PLAN.md. Items covered HERE:

GOAL (ROADMAP Phase 165): `swing_momentum.py` (8 cols) + `fibonacci_zones.py` (4 cols) of the
25-column direct-port group. COVERED by Tasks 1-2.

REQ: no `.planning/REQUIREMENTS.md` exists for this project. 165-CONTEXT.md's D-XX decision IDs are the
requirement set, used verbatim in `requirements` frontmatter.

RESEARCH (165-RESEARCH.md):
- Finding B (three undeclared pivot-window conventions; `swing_momentum` keeps its OWN
  `_CONFIRM_N`-style detection, deliberately NOT unified with `find_peaks`) -> Task 1. COVERED.
- Finding C (six undeclared magic numbers in `swing_momentum.py`) -> all six read from
  `FeatureFactoryConfig` in Task 1, seeded by Plan 01. COVERED.
- Section 1 `fibonacci_zones.py` (drop 8 raw/intermediate fields; the fallback needs deleting, not
  resolving) -> Task 2. COVERED.
- Section 1 `swing_momentum.py` "ATR-normalized but not bounded" correction -> Task 1 proves the ATR
  divisor cancels exactly and removes it, so no `divisor = 1.0` fake-fallback branch survives. COVERED.

CONTEXT (165-CONTEXT.md):
- D-01 nullable discipline applied to both new helpers (all-None fallback dicts). COVERED (Tasks 1-3).
- D-03 `swing_velocity_trend` string enum -> `swing_velocity_bias` {-1,0,1}. COVERED (Task 1).
- D-04 fibonacci drops 8 raw/intermediate fields, keeps 4. COVERED (Task 2 + verify greps).
- D-05 delete the cross-plugin fallback entirely. COVERED (Task 2).
- D-06 `feature.swing_momentum.confirm_n` stays a separate key from `feature.swing.pivot_window`.
  COVERED (Task 1).
- D-15 `swing_volume_confirmation`. COVERED (Task 1).

Exclusions (NOT gaps — explicit CONTEXT.md Deferred Ideas): D-14 fibonacci extension levels
(1.272/1.618/2.0). Historical `feature_vectors` backfill (todo 176's consolidated 163/164/165 pass).
</source_coverage_audit>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/165-swing-fib-trend-structure-primitives/165-CONTEXT.md
@.planning/phases/165-swing-fib-trend-structure-primitives/165-RESEARCH.md
@.planning/phases/165-swing-fib-trend-structure-primitives/165-PATTERNS.md
@.planning/phases/165-swing-fib-trend-structure-primitives/165-01-SUMMARY.md
@.planning/phases/165-swing-fib-trend-structure-primitives/165-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Verified live at plan-writing time (2026-07-28, AFTER Phase 164 landed). Line numbers are
     post-164; re-anchor on the named symbols, not the integers, if 164-era code shifts again. -->

Already imported at the top of src/intelligence/feature_factory.py — no new imports needed:
  line 46: from src.intelligence.utils import clamp, find_peaks, find_troughs
  line 47: from src.intelligence.utils.gradient_utils import freshness_decay, linear_ramp

  clamp(value: float, min_val: float = -1.0, max_val: float = 1.0) -> float
      NOTE the defaults are (-1.0, 1.0), NOT (0.0, 1.0) — always pass both bounds positionally.
  linear_ramp(x: float, lo: float, hi: float, out_lo: float = 0.0, out_hi: float = 1.0) -> float
      Piecewise-linear map from [lo, hi] to [out_lo, out_hi], clamped at both boundaries.

Structural template — the Phase 163 S/R block, src/intelligence/feature_factory.py:
  _SR_FALLBACK: dict[str, float]           line 3424   (module-level fallback dict constant)
  _finalize_cluster / _cluster_levels      lines 3435 / 3457  (private helpers beneath the constant)
  _compute_sr_dist_atr(highs, lows, close_, atr_val, volume, tf, config) -> dict[str, float]
                                           line 3484
      atr_valid = atr_val is not None and math.isfinite(atr_val) and atr_val > 0   (line 3508)
      if not atr_valid: return dict(_SR_FALLBACK)
      lookback = config.sr_lookback_by_tf.get(tf, 120); h = highs[-lookback:]; ...
      if n_bars < 2 * config.sr_window + 1: return dict(_SR_FALLBACK)
      ... never raises, returns a flat dict keyed by FeatureVector field name.
  Block ends at line 3566; the Phase 164 SMC section header follows at line 3569.

Plan 02's block (this plan's immediate predecessor) sits between them, headed
  `# Swing / Trend Structure — stateless pivot geometry (Phase 165 Plan 02)`
and exposes, in the dict returned by `_compute_swing_structure(...)`:
  swing_high_price: float | None    # price at the most recent confirmed peak in the window
  swing_low_price:  float | None    # price at the most recent confirmed trough in the window
  swing_high_indices / swing_low_indices: list[int]
  n_bars: int
These are IN-MEMORY INTERMEDIATES ONLY. They are never `_build_feature_vector` kwargs and never
persisted columns (D-02/D-04). This plan's fibonacci helper consumes exactly `swing_high_price` and
`swing_low_price` from that dict — that IS D-05's deletion of the cross-plugin fallback.

compute() call sites to mirror — src/intelligence/feature_factory.py:
  line 5055: atr_val = float(s.atr_raw[-1]) if len(s.atr_raw) > 0 else 0.0   (already a local float —
             do NOT call get_atr() inside any new helper; accept atr_val as a parameter)
  line 5079: _sr_fields = _compute_sr_dist_atr(highs, lows, close_, atr_val, volumes, tf, config)
  lines 5080-5086: one `<name>_val: float | None = _sr_fields["<name>"]` unpack per field
  Plan 02 adds its `_swing_fields` / `_trend_fields` blocks immediately after those unpacks.
  The full-series arrays are `opens/highs/lows/closes/volumes` (lines 5038-5042).

compute_batch() call site to mirror — src/intelligence/feature_factory.py:
  lines 5635-5652: causal pre-slice pattern
      _sr_lookback = config.sr_lookback_by_tf.get(tf, 120)
      _sr_start = max(0, i - _sr_lookback + 1)
      _sr_fields = _compute_sr_dist_atr(highs[_sr_start : i + 1], lows[_sr_start : i + 1], close_,
                                        atr_val, volumes[_sr_start : i + 1], tf, config)
  Phase 164's OB/FVG/sweep blocks at lines 5654-5720 repeat the identical shape. Pre-slicing a window
  ENDING at bar i (never the full series) is what prevents look-ahead.

_build_feature_vector — src/intelligence/feature_factory.py line 4564, keyword-only (`*,`).
  Phase 163's None-defaulted block at lines 4587-4608 is the exact analog. Plan 01 added all 41
  Phase 165 kwargs there as `float | None = None` placeholders; this plan replaces 12 of them with
  real values at BOTH call sites.

APR config fields available from Plan 01 (FeatureFactoryConfig, all defaulted — do not re-add):
  config.swing_momentum_confirm_n: int = 3            # feature.swing_momentum.confirm_n
  config.swing_momentum_max_extremes: int = 6         # feature.swing_momentum.max_extremes (stays EVEN)
  config.swing_momentum_lookback_bars: int = 60       # feature.swing_momentum.lookback_bars
  config.swing_momentum_reference_bars: int = 20      # feature.swing_momentum.reference_bars
  config.swing_momentum_speed_factor_min: float = 0.1 # feature.swing_momentum.speed_factor_min
  config.swing_momentum_speed_factor_max: float = 3.0 # feature.swing_momentum.speed_factor_max
  config.swing_momentum_energy_divisor: float = 3.0   # feature.swing_momentum.energy_divisor
  config.swing_momentum_intensity_ramp_lo: float = 1.0 # feature.swing_momentum.intensity_ramp_lo
  config.swing_momentum_intensity_ramp_hi: float = 2.0 # feature.swing_momentum.intensity_ramp_hi
  config.fib_cluster_atr_divisor: float = 2.0         # feature.fib.cluster_atr_divisor
  config.fib_cluster_fallback_divisor: float = 20.0   # feature.fib.cluster_fallback_divisor
                                                      # (see Task 2 — deliberately dormant)

Test analog — tests/unit/intelligence/test_support_resistance_primitives.py (357 lines, Phase 163
commit a748d13d). Module-level `N = 140`, `RNG = np.random.default_rng(16303)`, a `_SR_FIELDS` tuple,
a `_make_cfg(**overrides)` returning a FeatureFactoryConfig with small windows so features warm up
within N bars, and four test shapes (non-constant, ATR-unit pin on a constant-true-range fixture where
Wilder ATR converges to exactly 1.0, live/batch parity to 1e-6, fallback guard). Plan 02 created
tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py with the same module-level
helpers; this plan APPENDS to that file.

PRE-COMMIT GOTCHA (hit during Phase 164 Plan 04): the repo's pre-commit hook rejects duplicate test
function names across tests/. Every test added here must carry a file-scoped-unique name — prefix all
new tests `test_swing_momentum_*` / `test_fib_*`, never a generic `test_non_constant`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _compute_swing_momentum() — 8 fields off its own confirm-window extreme detector, all-None fallback, six APR-backed constants, two ported-bug fixes</name>
  <files>src/intelligence/feature_factory.py</files>
  <behavior>
    - Given a window containing at least config.swing_momentum_max_extremes (6) confirmed alternating extremes: all 8 fields are finite floats.
    - Given fewer than config.swing_momentum_max_extremes confirmed extremes: every one of the 8 fields is None (not 0.0, not "stable").
    - Given a window shorter than 2 * config.swing_momentum_confirm_n + 1 bars: every one of the 8 fields is None.
    - Given the mean of the last 3 swing amplitudes is zero (flat prices): every one of the 8 fields is None.
    - swing_amplitude_ratio is EXACTLY ATR-invariant: scaling every price in the fixture by a constant factor leaves swing_amplitude_ratio, swing_amplitude_expanding, swing_amplitude_intensity, struct_energy and swing_velocity_bias unchanged to 1e-12.
    - swing_amplitude_expanding == 1.0 only when the LAST three amplitudes are strictly increasing (amplitudes[-3] < amplitudes[-2] < amplitudes[-1]); a fixture whose FIRST three amplitudes increase while its last three do not must yield 0.0.
    - swing_velocity_bias == +1.0 when velocities[-1] < velocities[-2] (swings arriving faster), -1.0 when velocities[-1] > velocities[-2], 0.0 when equal; None when fewer than 2 velocities exist.
    - struct_accel_bias == +1.0 for higher-high + higher-low, -1.0 for lower-high + lower-low, 0.0 mixed; 0.0 when fewer than 2 highs or 2 lows are present among the retained extremes (this 0.0 IS a real "mixed/undetermined" measurement, matching the archived semantics).
    - swing_volume_confirmation == mean(volumes over the bar-index span of the most recent confirmed swing leg) / mean(volumes over the whole bounded window); None when the window mean volume is 0.
    - Changing config.swing_momentum_energy_divisor changes struct_energy; changing config.swing_momentum_confirm_n changes which extremes are detected.
  </behavior>
  <read_first>
    - src/intelligence/features/i3_structure/swing_momentum.py (FULL, 258 lines — `compute_full()` lines 62-134 is the reference algorithm; `_detect_extremes()` 143-179 and the module-level `_dedup_extremes()` 233-255 are the port sources; lines 29-31 are `_CONFIRM_N`/`_MAX_EXTREMES`/`_REFERENCE_BARS`; line 96's `+ 1e-9`, line 99's `amplitudes[0] < amplitudes[1] < amplitudes[2]`, line 103's `linear_ramp(..., 1.0, 2.0)`, line 120's `clamp(..., 0.1, 3.0)`, line 121's `/ 3.0`, and line 184's `divisor = atr if ... else 1.0` are the six APR gaps plus the two bugs)
    - src/intelligence/feature_factory.py lines 3420-3566 (the whole Phase 163 `_SR_FALLBACK` / `_compute_sr_dist_atr` block — the structural template: module-level fallback dict, guard at the top, bounded slice, never raises)
    - src/intelligence/feature_factory.py — Plan 02's `# Swing / Trend Structure` block in full (the `_SWING_FALLBACK` all-None precedent and `_compute_swing_structure`'s return-dict shape)
    - src/intelligence/utils/gradient_utils.py lines 41-90 (`linear_ramp` exact semantics, including the lo == hi edge case)
    - .planning/phases/165-swing-fib-trend-structure-primitives/165-RESEARCH.md Section 1 (`swing_momentum.py` paragraph) and Section 2 Finding B + Finding C
    - .planning/phases/165-swing-fib-trend-structure-primitives/165-CONTEXT.md D-03, D-06, D-15
  </read_first>
  <action>
    Add a new module-level section to src/intelligence/feature_factory.py immediately AFTER Plan 02's
    `# Swing / Trend Structure` block and BEFORE the Phase 164 SMC section header
    (`_compute_order_blocks`'s preceding `# ---` banner). Head the new section
    `# Swing Momentum / Fibonacci Zones — stateless swing geometry (Phase 165 Plan 03)`.
    Task 2 appends the fibonacci helper into this same section.

    (a) Define `_SWING_MOMENTUM_FALLBACK: dict[str, float | None]` with all eight keys mapped to
    `None`: swing_amplitude_ratio, swing_amplitude_expanding, swing_amplitude_intensity,
    swing_velocity_bars, swing_velocity_bias, struct_energy, struct_accel_bias,
    swing_volume_confirmation. Comment it with the same D-01 rationale Plan 02's `_SWING_FALLBACK`
    carries: the archived plugin returns `{}` on insufficient data (already clean, unlike
    trend_structure), and the v3 equivalent of `{}` is an all-`None` dict, never `_SR_FALLBACK`'s zeros.

    (b) Define `_dedup_swing_extremes(extremes: list[tuple[float, int, str]]) -> list[tuple[float, int, str]]`
    ported verbatim from the archived module-level `_dedup_extremes` (lines 233-255): drop consecutive
    same-type extremes, keeping the higher one for `"high"` and the lower one for `"low"`, so the
    retained list alternates. Name it `_dedup_swing_extremes`, not `_dedup_extremes` — the archived
    name is generic enough to collide with a future SMC helper in the same module.

    (c) Define `_detect_swing_extremes(highs, lows, confirm_n, max_extremes) -> list[tuple[float, int, str]]`
    ported from the archived `_detect_extremes` static method. Deliberately does NOT use
    `find_peaks`/`find_troughs` (D-06 / RESEARCH Finding B: this is a different, self-contained
    confirmation algorithm, and unifying it would change its output). Scan `i` over
    `range(confirm_n, len(highs) - confirm_n)`; bar `i` is a peak when
    `highs[i] >= max(highs[i - confirm_n : i + confirm_n + 1])` and a trough when
    `lows[i] <= min(lows[i - confirm_n : i + confirm_n + 1])`; prefer the peak when both hold
    (matching the archived `if is_peak: ... elif is_trough:` ordering). Run the result through
    `_dedup_swing_extremes`, then return the trailing `max_extremes` entries. Indices are positions
    within the ARRAY PASSED IN. Docstring must state that the `>= max` / `<= min` comparison means a
    flat run confirms every bar in it — the reason `_dedup_swing_extremes` exists.

    (d) Define `_compute_swing_momentum(highs, lows, volumes, config) -> dict[str, float | None]`.
    NOTE the signature carries NO `atr_val` and NO `close_` — see (e) for why that is a deliberate
    deletion, and document it in the docstring so a future reader does not "restore" it.
    Structure it like `_compute_sr_dist_atr`: bounded causal slice
    `h = highs[-config.swing_momentum_lookback_bars:]`, `lo = lows[...]`, `v = volumes[...]`;
    return `dict(_SWING_MOMENTUM_FALLBACK)` when `len(h) < 2 * config.swing_momentum_confirm_n + 1`;
    never raises.
    Call `_detect_swing_extremes(h, lo, config.swing_momentum_confirm_n, config.swing_momentum_max_extremes)`;
    return `dict(_SWING_MOMENTUM_FALLBACK)` when `len(extremes) < config.swing_momentum_max_extremes`.

    Derive amplitudes as RAW price differences between consecutive retained extremes:
    `amps[k] = abs(extremes[k][0] - extremes[k + 1][0])`. Derive velocities as
    `vels[k] = max(float(abs(extremes[k + 1][1] - extremes[k][1])), 1.0)` (the 1.0 floor is a
    divide-by-zero guard, an algorithmic invariant, not a tunable — do not APR-back it).
    Return `dict(_SWING_MOMENTUM_FALLBACK)` when either list is empty.

    Field derivation — the two archived implementation-vs-docstring bugs are FIXED here, not ported:
      amp_recent = amps[-3:]  (the archived code averages ALL amplitudes while its own docstring and
                               migration 267's COMMENT both say "mean of last 3" — honor the COMMENT,
                               it is the binding contract the drift gate and the DB document)
      amp_mean   = mean(amp_recent); return dict(_SWING_MOMENTUM_FALLBACK) when amp_mean <= 0
                               (this replaces the archived `+ 1e-9` denominator fudge, which made the
                               result depend on the absolute price scale — see (e))
      swing_amplitude_ratio     = amps[-1] / amp_mean
      swing_amplitude_expanding = 1.0 when len(amps) >= 3 and amps[-3] < amps[-2] < amps[-1] else 0.0
                               (the archived code tests amps[0] < amps[1] < amps[2] — the OLDEST three,
                               contradicting its own docstring and the COMMENT; fix, do not port)
      swing_amplitude_intensity = linear_ramp(swing_amplitude_ratio,
                                              config.swing_momentum_intensity_ramp_lo,
                                              config.swing_momentum_intensity_ramp_hi)
                                  when swing_amplitude_expanding == 1.0 else 0.0
      swing_velocity_bars       = float(vels[-1])
      swing_velocity_bias       = +1.0 when vels[-1] < vels[-2]; -1.0 when vels[-1] > vels[-2];
                                  0.0 when equal; None when len(vels) < 2
                                  (D-03: numeric encoding of the archived string enum
                                  swing_velocity_trend. The archived `else: velocity_trend = "stable"`
                                  on len < 2 is a D-01 fake value — use None. The string literals
                                  "accelerating"/"decelerating"/"stable" must not appear anywhere.)
      speed_factor              = clamp(config.swing_momentum_reference_bars / max(swing_velocity_bars, 1.0),
                                        config.swing_momentum_speed_factor_min,
                                        config.swing_momentum_speed_factor_max)
      struct_energy             = clamp(swing_amplitude_ratio * speed_factor
                                        / config.swing_momentum_energy_divisor, 0.0, 1.0)
      struct_accel_bias         = ported verbatim from the archived `_compute_accel_bias` (lines
                                  200-225): collect the retained highs and lows in bar order; return
                                  0.0 when fewer than 2 of either; +1.0 for higher-high AND higher-low,
                                  -1.0 for lower-high AND lower-low, else 0.0. Emit as a float.
      swing_volume_confirmation = mean(v[i0 : i1 + 1]) / window_mean_volume, where
                                  i0 = min(extremes[-2][1], extremes[-1][1]),
                                  i1 = max(extremes[-2][1], extremes[-1][1]),
                                  window_mean_volume = float(v.mean()); None when window_mean_volume
                                  is not > 0 (D-15).

    (e) Delete the ATR normalization entirely and document why in the function docstring: the archived
    `_compute_amplitudes` divides every amplitude by `atr` (falling back to `1.0` when ATR is
    unavailable — the same silent-un-normalized branch D-01 exists to kill), but every one of the eight
    outputs consumes amplitudes only through the RATIO `amps[-1] / mean(amps[-3:])`, in which a common
    positive divisor cancels exactly. The archived `+ 1e-9` epsilon is the sole reason the ATR divisor
    was not already a no-op, and it made the output silently price-scale-dependent. Removing both the
    divisor and the epsilon (replaced by the explicit `amp_mean <= 0` all-None guard) makes the
    computation exactly ATR-invariant and removes a fake-fallback branch. Task 3's
    `test_swing_momentum_atr_invariance` is the proof obligation for this claim — if it fails, the
    deletion was wrong and must be reverted rather than the test weakened.

    (f) Wire into `compute()` immediately after Plan 02's `_trend_fields` block: call once, unpack the 8
    values into `<name>_val: float | None` locals, and pass each by keyword into
    `_build_feature_vector(...)`, REPLACING the `None` placeholders Plan 01 installed.

    (g) Wire into `compute_batch()`'s per-bar loop immediately after Plan 02's `_trend_fields` block,
    using a causal pre-slice exactly like the S/R call site:
    `_sm_start = max(0, i - config.swing_momentum_lookback_bars + 1)` then pass
    `highs[_sm_start : i + 1]`, `lows[_sm_start : i + 1]`, `volumes[_sm_start : i + 1]`. Replace the
    same 8 `None` placeholders at that call site. Both paths MUST be updated in this same commit.

    No comment or docstring inside the new section may quote the literals `/ 3.0`, `0.1, 3.0`,
    `1.0, 2.0` or `1e-9` — refer to the retired constants by their APR key names instead. Task 1's
    verify greps assert those literals are absent from the section, and a comment mentioning them would
    self-invalidate the gate.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import inspect
from src.intelligence.feature_factory import _compute_swing_momentum, _detect_swing_extremes, _dedup_swing_extremes, _SWING_MOMENTUM_FALLBACK
assert set(_SWING_MOMENTUM_FALLBACK)=={'swing_amplitude_ratio','swing_amplitude_expanding','swing_amplitude_intensity','swing_velocity_bars','swing_velocity_bias','struct_energy','struct_accel_bias','swing_volume_confirmation'}, _SWING_MOMENTUM_FALLBACK
assert all(v is None for v in _SWING_MOMENTUM_FALLBACK.values()), 'D-01: fallback must be all-None'
assert list(inspect.signature(_compute_swing_momentum).parameters)==['highs','lows','volumes','config'], 'no atr_val/close_ parameter -- ATR cancels exactly, see docstring'
assert list(inspect.signature(_detect_swing_extremes).parameters)==['highs','lows','confirm_n','max_extremes']
assert callable(_dedup_swing_extremes)
print('ok signatures + all-None fallback')"</automated>
    <automated>.venv/bin/python -c "
src=open('src/intelligence/feature_factory.py').read()
i=src.index('# Swing Momentum / Fibonacci Zones')
j=src.index('def _compute_order_blocks')
block='\n'.join(l for l in src[i:j].splitlines() if not l.lstrip().startswith('#'))
for bad in ('/ 3.0','0.1, 3.0','1.0, 2.0','1e-9','_CONFIRM_N','_MAX_EXTREMES','_REFERENCE_BARS','accelerating','decelerating'):
    assert bad not in block, ('hardcoded constant or archived string enum survived', bad)
for good in ('config.swing_momentum_confirm_n','config.swing_momentum_max_extremes','config.swing_momentum_lookback_bars','config.swing_momentum_reference_bars','config.swing_momentum_speed_factor_min','config.swing_momentum_speed_factor_max','config.swing_momentum_energy_divisor','config.swing_momentum_intensity_ramp_lo','config.swing_momentum_intensity_ramp_hi'):
    assert good in block, ('APR key not read', good)
print('ok APR-backed, no magic numbers, no string enum')"</automated>
    <automated>.venv/bin/python -c "
src=open('src/intelligence/feature_factory.py').read()
assert 'swing_velocity_trend' not in src, 'D-03: archived string-enum field name must not appear'
body=src[src.index('def _build_feature_vector'):]
for bad in ('swing_high_price=','swing_low_price=','swing_high_indices='):
    assert bad not in body, ('raw intermediate leaked into the persisted path', bad)
print('ok D-03 encoding + no raw intermediates persisted')"</automated>
    <automated>.venv/bin/python -c "
src=open('src/intelligence/feature_factory.py').read()
for site in ('_sm_start',):
    assert site in src, ('compute_batch causal pre-slice missing', site)
print('ok batch causal pre-slice')"</automated>
    <automated>.venv/bin/ruff check src/intelligence/feature_factory.py</automated>
  </verify>
  <acceptance_criteria>
    - `_SWING_MOMENTUM_FALLBACK` exists with exactly the 8 field names, every value `None`.
    - `_compute_swing_momentum(highs, lows, volumes, config)` takes no `atr_val` parameter, never raises on any input (empty arrays, 1 bar, all-flat prices, all-zero volume), and returns exactly the 8 keys.
    - `_detect_swing_extremes` uses its own confirm-window scan and does NOT call `find_peaks`/`find_troughs`; the count of `find_peaks(` call sites in `feature_factory.py` is unchanged from before this task.
    - The new section reads all nine `feature.swing_momentum.*` values from `config` and contains none of the literals `/ 3.0`, `0.1, 3.0`, `1.0, 2.0`, `1e-9` (comments stripped before the check).
    - `swing_velocity_trend` and the strings `"accelerating"`/`"decelerating"` appear nowhere in `feature_factory.py` (D-03).
    - `swing_amplitude_expanding` tests `amps[-3] < amps[-2] < amps[-1]`, and `amp_mean` averages `amps[-3:]` — both matching migration 267's COMMENT rather than the archived implementation.
    - Both `compute()` and `compute_batch()` pass real values for all 8 fields; `compute_batch()` uses a `max(0, i - config.swing_momentum_lookback_bars + 1)` causal pre-slice.
    - `.venv/bin/ruff check src/intelligence/feature_factory.py` clean.
  </acceptance_criteria>
  <done>Eight swing-momentum fields carry real, varying, exactly-ATR-invariant values in both compute paths off an APR-backed self-contained extreme detector; every insufficient-data branch emits None; both archived implementation-vs-docstring bugs are fixed rather than ported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _compute_fib_zones() — 4 fields off Plan 02's swing intermediates, cross-plugin fallback deleted (D-05), 8 raw levels never declared (D-04)</name>
  <files>src/intelligence/feature_factory.py</files>
  <behavior>
    - Given swing_fields carrying finite swing_high_price > swing_low_price and atr_val > 0: all 4 fields are finite; nearest_fib_ratio is one of exactly {0.236, 0.382, 0.500, 0.618, 0.786}; nearest_fib_dist_atr >= 0; fib_cluster_strength is within [0, 1]; in_fib_discount_zone is 0.0 or 1.0.
    - Given swing_high_price is None or swing_low_price is None (Plan 02 found no confirmed pivot): all 4 fields are None.
    - Given swing_high_price <= swing_low_price (swing_range <= 0): all 4 fields are None.
    - Given atr_val <= 0 or non-finite: all 4 fields are None (matching migration 267's COMMENT).
    - Given close_ exactly at the 61.8% level: nearest_fib_ratio == 0.618 and nearest_fib_dist_atr == 0.0.
    - Given close_ strictly between the 50.0% and 78.6% levels: in_fib_discount_zone == 1.0; strictly below the 50.0% level: 0.0.
    - Widening config.fib_cluster_atr_divisor (a smaller threshold) can only lower or hold fib_cluster_strength, never raise it — proving the key is read.
  </behavior>
  <read_first>
    - src/intelligence/features/i3_structure/fibonacci_zones.py (FULL, 108 lines — `compute_full()` lines 40-102 is the reference algorithm; lines 50-59 are the D-05 cross-plugin fallback to DELETE, not resolve; line 9's `_FIB_RATIOS` list is the APR-exempt definitional constant to keep; lines 82-91 are the cluster-strength calc)
    - src/intelligence/feature_factory.py — Task 1's `# Swing Momentum / Fibonacci Zones` section, and Plan 02's `_compute_swing_structure` return-dict keys
    - .planning/phases/165-swing-fib-trend-structure-primitives/165-CONTEXT.md D-04, D-05, D-14
    - .planning/phases/165-swing-fib-trend-structure-primitives/165-RESEARCH.md Section 1 (`fibonacci_zones.py` paragraph — why converting all 5 fib levels to `_dist_atr` columns would reproduce Phase 163's D-17 collinearity finding five times over)
  </read_first>
  <action>
    Append to Task 1's `# Swing Momentum / Fibonacci Zones` section.

    (a) Define `_FIB_RATIOS: tuple[float, ...] = (0.236, 0.382, 0.500, 0.618, 0.786)` at module level
    inside that section, with a comment stating these are APR-EXEMPT definitional mathematical
    constants (Fibonacci retracement ratios), the same exemption class as "the 5 in `momentum_z_5`" per
    CLAUDE.md — so a future migrate-as-you-go pass does not "fix" them into APR keys. Migration 267
    carries the same note in SQL; keep the two consistent.

    (b) Define `_FIB_FALLBACK: dict[str, float | None]` with all four keys mapped to `None`:
    nearest_fib_ratio, nearest_fib_dist_atr, fib_cluster_strength, in_fib_discount_zone.

    (c) Define `_compute_fib_zones(close_, atr_val, swing_fields, config) -> dict[str, float | None]`
    where `swing_fields` is the dict `_compute_swing_structure` returned for the SAME bar. Read
    `swing_fields.get("swing_high_price")` and `swing_fields.get("swing_low_price")`. This IS D-05's
    fix: the archived `i3.get("swing_high")` read plus its rolling-high/low fallback branch existed only
    because the old wave-based pipeline could not guarantee `swing_detector` ran first; v3's `compute()`
    is one deterministic in-process call, so the swing prices are simply already available. Do NOT
    reimplement any fallback — no `df["high"].iloc[-n_lb:].max()` rolling substitute, no
    `min(50, len(df))` window. The docstring must say so explicitly.
    Return `dict(_FIB_FALLBACK)` when either price is None or non-finite, when
    `swing_range = swing_high_price - swing_low_price <= 0`, or when
    `atr_val` is None / non-finite / <= 0. Never raises.
    Field derivation:
      levels[k] = swing_low_price + _FIB_RATIOS[k] * swing_range
      nearest index    = argmin over k of abs(levels[k] - close_)
      nearest_fib_ratio     = float(_FIB_RATIOS[nearest index])
      nearest_fib_dist_atr  = abs(close_ - levels[nearest index]) / atr_val
      in_fib_discount_zone  = 1.0 when levels[2] <= close_ <= levels[4] else 0.0
                              (levels[2] is the 0.500 level, levels[4] is the 0.786 level — index by
                              position in _FIB_RATIOS, never by a magic string key like "fib_500")
      fib_cluster_strength  = cluster_count / max_pairs, where threshold =
                              atr_val / config.fib_cluster_atr_divisor, cluster_count counts unordered
                              level pairs (i < j) with abs(levels[i] - levels[j]) < threshold, and
                              max_pairs = len(levels) * (len(levels) - 1) / 2

    (d) `config.fib_cluster_fallback_divisor` (seeded by Plan 01 from the archived
    `swing_range / 20.0` branch) is DELIBERATELY DORMANT under this contract and must NOT be referenced.
    The archived fallback only fired when ATR was unavailable; migration 267's COMMENT binds all four
    fib fields to NULL in exactly that case, so the branch is unreachable by construction. Record this
    in one comment beside the `config.fib_cluster_atr_divisor` use AND on the
    `fib_cluster_fallback_divisor` field in `FeatureFactoryConfig`, so the key reads as an intentional
    dormant seed rather than a missed wiring. Note it in the plan SUMMARY as a candidate for removal if
    the base 4 fib fields fail their incremental-IC screen (D-14's sequencing).

    (e) D-04 enforcement: the helper returns EXACTLY four keys. No `fib_236`/`fib_382`/`fib_500`/
    `fib_618`/`fib_786`/`nearest_fib_level`/`fib_swing_high`/`fib_swing_low` key, local name, or
    `_build_feature_vector` kwarg may exist. The individual `levels` list stays an in-function local.

    (f) Wire into `compute()` immediately after Task 1's `_swing_momentum_fields` block, passing the
    `_swing_fields` dict Plan 02 already produced for the same bar. Unpack the 4 values into
    `<name>_val: float | None` locals and pass them by keyword into `_build_feature_vector(...)`,
    replacing the Plan 01 `None` placeholders.

    (g) Wire into `compute_batch()`'s per-bar loop the same way, passing that loop iteration's
    `_swing_fields`. No new slice is needed — the fib helper consumes prices, not arrays. Replace the
    same 4 `None` placeholders. Both paths in this same commit.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import inspect
from src.intelligence.feature_factory import _compute_fib_zones, _FIB_FALLBACK, _FIB_RATIOS
assert _FIB_RATIOS==(0.236,0.382,0.500,0.618,0.786), _FIB_RATIOS
assert set(_FIB_FALLBACK)=={'nearest_fib_ratio','nearest_fib_dist_atr','fib_cluster_strength','in_fib_discount_zone'}
assert all(v is None for v in _FIB_FALLBACK.values())
assert list(inspect.signature(_compute_fib_zones).parameters)==['close_','atr_val','swing_fields','config']
print('ok fib signature + ratios + all-None fallback')"</automated>
    <automated>.venv/bin/python -c "
from src.intelligence.feature_factory import _compute_fib_zones, FeatureFactoryConfig
cfg=FeatureFactoryConfig.__dataclass_fields__
import dataclasses
# never-raises smoke: degenerate and cold-start inputs
class _C: pass
from src.intelligence.feature_factory import _FIB_FALLBACK
print('ok import; behavioural coverage lives in Task 3 tests')"</automated>
    <automated>.venv/bin/python -c "
src=open('src/intelligence/feature_factory.py').read()
for bad in ('fib_236','fib_382','fib_500','fib_618','fib_786','nearest_fib_level','fib_swing_high','fib_swing_low'):
    assert bad not in src, ('D-04: raw fib level name must not exist anywhere', bad)
assert 'config.fib_cluster_fallback_divisor' not in src, 'the dormant key must not be referenced (see Task 2d)'
i=src.index('# Swing Momentum / Fibonacci Zones'); j=src.index('def _compute_order_blocks')
block=src[i:j]
assert 'config.fib_cluster_atr_divisor' in block
assert 'swing_fields' in block and 'swing_high_price' in block, 'D-05: fib must consume Plan 02 intermediates'
print('ok D-04 + D-05')"</automated>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM information_schema.columns WHERE table_name='feature_vectors' AND column_name IN ('fib_236','fib_382','fib_500','fib_618','fib_786','nearest_fib_level','fib_swing_high','fib_swing_low')" | grep -qx 0</automated>
    <automated>.venv/bin/ruff check src/intelligence/feature_factory.py</automated>
  </verify>
  <acceptance_criteria>
    - `_FIB_RATIOS` is the 5-tuple `(0.236, 0.382, 0.500, 0.618, 0.786)` with an APR-exemption comment matching migration 267's SQL note.
    - `_FIB_FALLBACK` is all-`None` for the 4 keys; `_compute_fib_zones` never raises and returns exactly those 4 keys.
    - `_compute_fib_zones` reads `swing_fields["swing_high_price"]`/`["swing_low_price"]` and contains no rolling-high/low fallback branch (D-05); the archived `min(50, len(df))` window appears nowhere.
    - None of the 8 dropped names (`fib_236`, `fib_382`, `fib_500`, `fib_618`, `fib_786`, `nearest_fib_level`, `fib_swing_high`, `fib_swing_low`) appears anywhere in `feature_factory.py`, and zero matching columns exist in `feature_vectors` (D-04).
    - `config.fib_cluster_fallback_divisor` is referenced nowhere and its dormancy is documented at both the `FeatureFactoryConfig` field and the `fib_cluster_atr_divisor` use site.
    - Both `compute()` and `compute_batch()` pass real values for all 4 fields, sourced from the same bar's `_swing_fields`.
    - `.venv/bin/ruff check src/intelligence/feature_factory.py` clean.
  </acceptance_criteria>
  <done>Four fibonacci-zone fields carry real values in both compute paths, computed from Plan 02's in-memory swing intermediates with the cross-plugin fallback deleted rather than reimplemented; not one raw fib price level exists in code or schema.</done>
</task>

<task type="auto">
  <name>Task 3: Regression tests — swing-momentum nullability, ATR-invariance proof, fib ATR-unit pin, live/batch parity, APR liveness, D-15 free-field guard</name>
  <files>tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py</files>
  <read_first>
    - tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py (FULL — Plan 02 created it; reuse its module-level `N`, `RNG`, `_make_cfg(**overrides)` and fixture helpers rather than redefining them)
    - tests/unit/intelligence/test_support_resistance_primitives.py (FULL, 357 lines — the constant-true-range fixture where Wilder ATR converges to exactly 1.0, and the four canonical test shapes)
    - src/intelligence/feature_factory.py — Tasks 1-2's `# Swing Momentum / Fibonacci Zones` section, plus `compute()` and `compute_batch()` signatures
    - .planning/phases/165-swing-fib-trend-structure-primitives/165-PATTERNS.md section 10 (test structure + commit `a748d13d`'s mutation-verification discipline)
  </read_first>
  <action>
    Append to tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py. Add module-level
    `_SWING_MOMENTUM_FIELDS` and `_FIB_FIELDS` tuples beside Plan 02's `_SWING_FIELDS`/`_TREND_FIELDS`,
    and extend the module docstring to say the file now also covers Plan 03's swing-momentum and
    fibonacci sub-scopes. Reuse Plan 02's `_make_cfg` — pass `swing_momentum_confirm_n` /
    `swing_momentum_max_extremes` / `swing_momentum_lookback_bars` as overrides where a test needs a
    smaller window rather than adding a second config helper.

    Every test name must be file-scoped unique and prefixed `test_swing_momentum_*` or `test_fib_*` —
    the repo's pre-commit hook rejects duplicate test function names across tests/ (hit during Phase
    164 Plan 04).

    Write these tests:
    (a) `test_swing_momentum_non_constant_batch` — run `compute_batch` over a synthetic series with
    repeated visible swings; assert each of the 8 swing-momentum fields takes at least 2 distinct
    non-None values across bars. The direct todo-153-shaped guard.
    (b) `test_swing_momentum_nullability` — a fixture too short / too smooth to yield
    `config.swing_momentum_max_extremes` confirmed extremes; assert every one of the 8 fields is
    exactly `None`, with a failure message naming D-01 and the archived `{}`-on-insufficient-data
    contract.
    (c) `test_swing_momentum_atr_invariance` — THE proof obligation for Task 1(e). Build one fixture,
    then a second identical fixture with every OHLC price multiplied by a constant factor (choose one
    that changes ATR materially, e.g. 7.0); assert `swing_amplitude_ratio`,
    `swing_amplitude_expanding`, `swing_amplitude_intensity`, `struct_energy` and `swing_velocity_bias`
    match to 1e-12 across the two runs. If this fails, the ATR-divisor deletion was wrong — revert the
    deletion, do not weaken the test.
    (d) `test_swing_momentum_expanding_uses_last_three` — construct amplitudes whose FIRST three are
    strictly increasing while the LAST three are not; assert `swing_amplitude_expanding == 0.0`. This is
    the regression guard for the archived `amplitudes[0] < amplitudes[1] < amplitudes[2]` bug.
    (e) `test_swing_momentum_velocity_bias_encoding` — assert `swing_velocity_bias` is one of
    `{-1.0, 0.0, 1.0}` or `None` across a batch run, and that no `FeatureVector` field anywhere holds a
    string (D-03).
    (f) `test_swing_momentum_volume_confirmation_free_field` — the D-15/D-19-style free-field guard:
    assert `swing_volume_confirmation` is non-None and > 0 on a fixture with varying volume, and that
    it changes when the volume series changes while prices are held fixed (proving it reads volume, not
    price).
    (g) `test_fib_ratio_is_canonical` — assert `nearest_fib_ratio` on a warm bar is a member of
    `{0.236, 0.382, 0.500, 0.618, 0.786}` and never an interpolated value.
    (h) `test_fib_dist_in_atr_units` — reuse the constant-true-range fixture (Wilder ATR converges to
    exactly 1.0) and assert `nearest_fib_dist_atr` equals the raw price distance from close to the
    nearest fib level within 1e-6, pinning the ATR conversion rather than asserting finiteness.
    (i) `test_fib_nullability_on_degenerate_swing` — feed a flat-price series (no confirmed pivots, ATR
    0); assert all 4 fib fields are `None`.
    (j) `test_fib_discount_zone_boundaries` — hand-place `close_` just inside and just outside the
    [50.0%, 78.6%] band via a directly-constructed `swing_fields` dict and assert
    `in_fib_discount_zone` flips 1.0 -> 0.0.
    (k) `test_swing_momentum_fib_live_batch_parity` — run `compute()` over the full bar list and
    `compute_batch()` over the same bars; assert all 12 fields match on the final bar to 1e-6, treating
    `None == None` as a match and `None` vs float as a failure. This is what catches a forgotten
    `compute_batch()` wiring update.
    (l) `test_swing_momentum_fib_apr_keys_are_live` — construct configs differing only in
    `swing_momentum_energy_divisor` and assert `struct_energy` differs; then only in
    `swing_momentum_confirm_n` and assert at least one swing-momentum field differs; then only in
    `fib_cluster_atr_divisor` and assert `fib_cluster_strength` differs or is provably monotone
    non-increasing as the divisor grows. Proves the APR keys are read rather than shadowed.

    Before considering this task done, apply commit `a748d13d`'s mutation-verification discipline:
    temporarily force `_compute_swing_momentum` and `_compute_fib_zones` to return their fallback
    dicts and confirm tests (a), (c), (d), (f), (g), (h), (j), (k) and (l) FAIL; then temporarily
    restore the archived `amplitudes[0] < amplitudes[1] < amplitudes[2]` comparison and confirm test
    (d) FAILS; then temporarily restore the archived `divisor = atr if atr > 0 else 1.0` amplitude
    normalization plus the `+ 1e-9` denominator and confirm test (c) FAILS. Revert all three local
    edits before committing, and record the observed failures in the plan SUMMARY.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py -x -q</automated>
    <automated>.venv/bin/python -c "
src=open('tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py').read()
for t in ('test_swing_momentum_non_constant_batch','test_swing_momentum_nullability','test_swing_momentum_atr_invariance','test_swing_momentum_expanding_uses_last_three','test_swing_momentum_velocity_bias_encoding','test_swing_momentum_volume_confirmation_free_field','test_fib_ratio_is_canonical','test_fib_dist_in_atr_units','test_fib_nullability_on_degenerate_swing','test_fib_discount_zone_boundaries','test_swing_momentum_fib_live_batch_parity','test_swing_momentum_fib_apr_keys_are_live'):
    assert t in src, t
assert src.count('def test_')==len(set(l.split('(')[0] for l in src.split('def ')[1:] if l.startswith('test_'))), 'duplicate test function name in file'
print('ok 12 tests present, names unique')"</automated>
    <automated>.venv/bin/pytest tests/unit/ -q</automated>
    <automated>.venv/bin/ruff check tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py</automated>
    <automated>.venv/bin/black --check tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py src/intelligence/feature_factory.py</automated>
  </verify>
  <acceptance_criteria>
    - All 12 named tests exist, carry file-scoped-unique names, and pass.
    - `test_swing_momentum_atr_invariance` compares two price-scaled fixtures to 1e-12 and is the recorded proof for Task 1(e)'s ATR-divisor deletion.
    - `test_swing_momentum_expanding_uses_last_three` fails against the archived `amplitudes[0..2]` comparison (verified by the mutation check).
    - `test_fib_dist_in_atr_units` pins an exact numeric value on a constant-true-range fixture, not merely `isfinite`.
    - The parity test compares all 12 fields between `compute()` and `compute_batch()` and treats a None/float mismatch as a failure.
    - The mutation check was actually run: the SUMMARY records which tests failed under (i) forced-fallback helpers, (ii) the restored `amplitudes[0..2]` comparison, and (iii) the restored ATR divisor + `1e-9` epsilon, and all three local edits were reverted.
    - `.venv/bin/pytest tests/unit/ -q` fully green; ruff and black clean.
  </acceptance_criteria>
  <done>The swing-momentum and fibonacci sub-scopes carry mutation-verified regression guards for nullability, ATR-invariance, the two fixed port bugs, ATR-unit conversion, live/batch parity and APR liveness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| market_data_ohlcv_tradeable -> FeatureFactory.compute/compute_batch | Already-validated internal OHLCV; no external or untrusted input crosses. |
| compute_batch() bar-index window -> persisted feature_vectors row | A window slice reaching past bar `i` would inject look-ahead into the training corpus. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-10 | Tampering (silent wrong data) | `swing_momentum`'s archived `divisor = atr if atr > 0 else 1.0` silently emitting un-normalized amplitudes when ATR is unavailable | mitigate | The ATR divisor is deleted outright (it cancels exactly in every consuming ratio); `test_swing_momentum_atr_invariance` is the proof obligation, and the `+ 1e-9` denominator fudge that made it non-cancelling is replaced by an explicit `amp_mean <= 0` all-None guard. |
| T-165-11 | Tampering (silent wrong data) | Porting the archived `amplitudes[0] < amplitudes[1] < amplitudes[2]` and full-series mean, which contradict both the plugin's own docstring and migration 267's binding COMMENT | mitigate | Implementation follows the COMMENT (`amps[-3:]`); `test_swing_momentum_expanding_uses_last_three` fails against the archived comparison, verified by mutation check. |
| T-165-12 | Tampering (look-ahead leakage) | `compute_batch()` slicing highs/lows/volumes beyond bar `i` for the momentum window | mitigate | Causal pre-slice `max(0, i - config.swing_momentum_lookback_bars + 1) : i + 1`; `_detect_swing_extremes` only confirms an extreme with `confirm_n` bars on BOTH sides, so the newest `confirm_n` bars are never confirmable and no future bar is read. |
| T-165-13 | Information disclosure (cross-symbol leakage) | Persisting raw fib price levels or the raw swing high/low as columns (non-stationary, non-comparable cross-sectionally) | mitigate | D-04/D-05 enforced by a source assertion that none of the 8 dropped names exists in `feature_factory.py` and a DB query asserting zero matching `feature_vectors` columns; `swing_high_price`/`swing_low_price` are asserted absent from the `_build_feature_vector` body. |
| T-165-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero new packages. `numpy`, `clamp`, `linear_ramp` are already-live in-repo dependencies imported at `feature_factory.py:46-47`. No install task exists in this plan. |
</threat_model>

<verification>
- `_SWING_MOMENTUM_FALLBACK` and `_FIB_FALLBACK` are all-`None`; no numeric or string placeholder survives.
- `_compute_swing_momentum` takes no `atr_val` parameter and is exactly ATR-invariant, proven by a 1e-12 price-scaling test.
- All nine `feature.swing_momentum.*` values plus `feature.fib.cluster_atr_divisor` are read from `FeatureFactoryConfig`; the literals `/ 3.0`, `0.1, 3.0`, `1.0, 2.0`, `1e-9` are absent from the new section with comments stripped.
- `swing_velocity_trend` and the archived string enum values appear nowhere; `swing_velocity_bias` is `{-1.0, 0.0, 1.0}` or `None`.
- None of the 8 dropped fibonacci names exists in code or in `feature_vectors`; `_compute_fib_zones` consumes Plan 02's `swing_high_price`/`swing_low_price` with no fallback branch.
- `compute()` and `compute_batch()` agree to 1e-6 on all 12 fields; `compute_batch()` uses a causal pre-slice.
- `.venv/bin/pytest tests/unit/ -q` green; ruff and black clean.
</verification>

<success_criteria>
Twelve of Phase 165's 41 columns (8 swing-momentum + 4 fibonacci) carry real, varying, bounded or
ATR-normalized values in both the live and batch compute paths. Every tunable is APR-backed, every
insufficient-data branch emits `None`, and both of `swing_momentum.py`'s implementation-vs-docstring
bugs plus its dead ATR divisor are fixed rather than ported. `fibonacci_zones.py`'s cross-plugin
fallback is deleted outright by consuming Plan 02's in-memory swing intermediates, and not one raw
fibonacci price level exists in code or schema.
</success_criteria>

<output>
Create `.planning/phases/165-swing-fib-trend-structure-primitives/165-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
