---
phase: 165-swing-fib-trend-structure-primitives
plan: 02
type: execute
wave: 2
depends_on: ["165-01"]
files_modified:
  - src/intelligence/feature_factory.py
  - tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py
autonomous: true
requirements: ["D-01", "D-02", "D-05", "D-06", "D-12"]

must_haves:
  truths:
    - "swing_high_dist_atr / swing_low_dist_atr carry real, varying ATR-unit distances across bars (not a frozen default)"
    - "When fewer than 2 confirmed swing highs or lows exist in the lookback window, ALL SIX trend_structure fields are None — never trend_direction=0.0 / price_position=0.5 (D-01, the todo-153 failure shape)"
    - "swing_detector and trend_structure share ONE find_peaks/find_troughs pass per bar over the same config.swing_pivot_window window (D-06), and the raw swing high/low prices are exported as in-memory intermediates for Plan 03's fibonacci port (D-05) — never persisted"
    - "compute() and compute_batch() produce identical values for all 13 fields on the same bar (live/batch parity), with compute_batch slicing a causal window ending at the current bar"
    - "No swing_high_idx / swing_low_idx anywhere: raw bar indices stay internal to the helper and are exposed only as *_age_bars (D-02)"
  artifacts:
    - path: "src/intelligence/feature_factory.py"
      provides: "_SWING_FALLBACK / _TREND_STRUCTURE_FALLBACK all-None constants, _compute_swing_structure(), _compute_trend_structure(), and both compute paths wired"
      contains: "_compute_swing_structure"
    - path: "tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py"
      provides: "nullability guard, ATR-unit pin, non-constant guard, live/batch parity for the 13 swing/trend fields"
      contains: "test_trend_structure_nullability"
  key_links:
    - from: "src/intelligence/feature_factory.py _compute_swing_structure"
      to: "src/intelligence/utils.py find_peaks / find_troughs"
      via: "n=config.swing_pivot_window over a config.swing_lookback_bars causal slice"
      pattern: "find_peaks\\(.*config\\.swing_pivot_window"
    - from: "_compute_swing_structure raw swing high/low intermediates"
      to: "Plan 03's fibonacci-zone helper"
      via: "returned in-memory values, never a FeatureVector field (D-05 deletes the archived cross-plugin fallback)"
      pattern: "swing_high_price"
    - from: "compute_batch() per-bar loop"
      to: "_compute_swing_structure / _compute_trend_structure"
      via: "highs[max(0, i - lookback + 1) : i + 1] causal slice, mirroring the Phase 163 S/R call site"
      pattern: "_sw_start"
---

<objective>
Port `i3_structure/swing_detector.py` (7 fields) and `i3_structure/trend_structure.py` (6 fields) into
real values on the 13 columns Plan 01 declared, fixing the D-01 silent-wrong-answer bug in both rather
than carrying it forward, and producing the shared per-bar swing high/low intermediates Plan 03's
fibonacci port consumes (D-05).

Purpose: these two plugins are the only ones in the phase that manufacture a fake-but-numeric "no
signal" value (`trend_direction=0.0`, `price_position=0.5`, `sh_price=0.0`, `high_type=0.0`). That is
structurally the same bug that made `poc_dist_atr`/`va_position`/`sr_support_dist`/`sr_resist_dist` sit
at constant defaults and score `ic_value=0` across 5,510 cells before Phase 163 caught it (todo 153).
A literal port reintroduces a failure this project already paid to discover.

Output: two stateless pure-compute helpers wired into both `compute()` and `compute_batch()`, plus the
phase's first dedicated regression test file — including the nullability guard, which is the single
most important new test in Phase 165.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/165-swing-fib-trend-structure-primitives/165-CONTEXT.md
@.planning/phases/165-swing-fib-trend-structure-primitives/165-RESEARCH.md
@.planning/phases/165-swing-fib-trend-structure-primitives/165-PATTERNS.md
@.planning/phases/165-swing-fib-trend-structure-primitives/165-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Verified live at plan-writing time (2026-07-27). Use directly; no exploration needed. -->

Shared pivot-detection utility — src/intelligence/utils.py lines 14-62, already imported at
feature_factory.py line 46:
  find_peaks(data: np.ndarray, n: int) -> list[int]     # ascending bar indices of local maxima
  find_troughs(data: np.ndarray, n: int) -> list[int]   # ascending bar indices of local minima
  Both return [] when len(data) < 2*n + 1. Both use >=/<= with a required strict inequality, so ties
  do not fire on flat data. Indices are positions within the ARRAY PASSED IN, not the full series.

Closest structural analog — src/intelligence/feature_factory.py lines 3272-3414 (Phase 163's S/R
block, commit fde6a2a4). Same shape: module-level fallback dict, `atr_valid` guard at the top, a
bounded lookback slice, find_peaks/find_troughs, never raises:
  _SR_FALLBACK: dict[str, float] = { "sr_support_dist": 0.0, ... }          # line 3272
  def _compute_sr_dist_atr(highs, lows, close_, atr_val, volume, tf, config) -> dict[str, float]:
      atr_valid = atr_val is not None and math.isfinite(atr_val) and atr_val > 0
      if not atr_valid: return dict(_SR_FALLBACK)
      lookback = config.sr_lookback_by_tf.get(tf, 120); h = highs[-lookback:]; ...
      if n_bars < 2 * config.sr_window + 1: return dict(_SR_FALLBACK)
      peak_indices = find_peaks(h, n=config.sr_window); ...
  CRITICAL DIFFERENCE (D-01): _SR_FALLBACK's zeros are legitimate for S/R (0.0 genuinely means "no
  level found on that side"). They are NOT the model for this plan. `trend_direction=0.0` reads as
  "measured, no trend" and `price_position=0.5` reads as "exactly mid-range" — both are fake
  measurements. This plan's fallback dicts are ALL-None.

Nullable-field precedent — src/intelligence/schemas.py line 1269 (`poc_dist_atr: float | None`).

compute() call site to mirror — feature_factory.py lines 3852-3863:
  _sr_fields = _compute_sr_dist_atr(highs, lows, close_, atr_val, volumes, tf, config)
  sr_support_dist_val: float | None = _sr_fields["sr_support_dist"]
  ... then passed by keyword into _build_feature_vector(...) at ~3932-3970.
  `atr_val` is already a local float (line 3832, `float(s.atr_raw[-1])`). Do NOT call get_atr() inside
  the new helpers — accept atr_val as a parameter, exactly as _compute_sr_dist_atr does.

compute_batch() call site to mirror — feature_factory.py lines 4279-4300:
  _sr_lookback = config.sr_lookback_by_tf.get(tf, 120)
  _sr_start = max(0, i - _sr_lookback + 1)
  _sr_fields = _compute_sr_dist_atr(highs[_sr_start : i + 1], lows[_sr_start : i + 1], close_, atr_val,
                                    volumes[_sr_start : i + 1], tf, config)
  Pre-slicing a causal window ending at bar i (never the full series) is what prevents lookahead.

APR config fields available from Plan 01 (FeatureFactoryConfig, all defaulted):
  config.swing_pivot_window: int = 5                        # feature.swing.pivot_window (D-06, shared)
  config.swing_lookback_bars: int = 120                     # feature.swing.lookback_bars
  config.trend_structure_atr_strength_divisor: float = 5.0  # feature.trend_structure.atr_strength_divisor
  config.trend_structure_range_lookback_bars: int = 20      # feature.trend_structure.range_lookback_bars

Test analog — tests/unit/intelligence/test_support_resistance_primitives.py (357 lines, Phase 163
commit a748d13d): module-level N/RNG constants, a `_make_cfg(**overrides)` helper returning a
FeatureFactoryConfig with small windows so features warm up within N bars, and four test shapes
(non-constant, ATR-unit pin on a constant-true-range fixture where Wilder ATR converges to exactly 1.0,
live/batch parity to 1e-6, and a fallback guard).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _compute_swing_structure() — shared pivot pass, 7 swing fields, all-None fallback, raw intermediates for Plan 03</name>
  <files>src/intelligence/feature_factory.py</files>
  <behavior>
    - Given a bounded window containing at least 2 confirmed swing highs and 2 confirmed swing lows and atr_val > 0: all 7 fields are finite floats; swing_high_dist_atr == (high[last_peak_idx] - close_) / atr_val exactly; swing_high_age_bars == n_bars - 1 - last_peak_idx.
    - Given atr_val <= 0 (or non-finite): every one of the 7 fields is None (not 0.0).
    - Given a window too short for find_peaks (len < 2*swing_pivot_window + 1): every one of the 7 fields is None.
    - Given exactly 1 confirmed swing high: swing_high_dist_atr and swing_high_age_bars are real floats, but swing_high_type is None and swing_pattern is None (cannot classify HH/LH without a predecessor).
    - Given 0 confirmed swing highs: swing_high_dist_atr, swing_high_age_bars, swing_high_type and swing_pattern are all None.
    - The returned dict additionally carries the raw intermediates `swing_high_price` / `swing_low_price` (float | None) which are NOT FeatureVector fields.
  </behavior>
  <read_first>
    - src/intelligence/features/i3_structure/swing_detector.py (FULL, 95 lines — `compute_full()` is the reference algorithm; lines 48-51 and 58/65 are the D-01 fake-default branches to REPLACE with None, not copy; note `swing_high_idx`/`swing_low_idx` at lines 82-83 are dropped entirely per D-02)
    - src/intelligence/feature_factory.py lines 3268-3414 (the _SR_FALLBACK / _cluster_levels / _compute_sr_dist_atr block — the structural template)
    - src/intelligence/utils.py lines 14-62 (find_peaks / find_troughs semantics, including the len < 2n+1 empty-return)
    - .planning/phases/165-swing-fib-trend-structure-primitives/165-CONTEXT.md D-01, D-02, D-05, D-06
  </read_first>
  <action>
    Add a new module-level section to src/intelligence/feature_factory.py immediately after the Phase 163
    S/R block (after line 3414), headed
    `# Swing / Trend Structure — stateless pivot geometry (Phase 165 Plan 02)`.

    (a) Define `_SWING_FALLBACK: dict[str, float | None]` with all seven keys mapped to `None`:
    swing_high_dist_atr, swing_low_dist_atr, swing_high_type, swing_low_type, swing_pattern,
    swing_high_age_bars, swing_low_age_bars. Add a docstring-level comment stating explicitly that this
    is deliberately NOT modelled on `_SR_FALLBACK`'s all-zero dict: 0.0 is a legitimate "no level found"
    value for S/R distances, but a numeric placeholder for a swing classification or position is a fake
    measurement (D-01, todo 153's failure shape).

    (b) Define `_compute_swing_structure(highs, lows, close_, atr_val, config) -> dict[str, float | None]`.
    Structure it exactly like `_compute_sr_dist_atr`: `atr_valid` guard at the top returning
    `dict(_SWING_FALLBACK)`; slice a bounded causal window `h = highs[-config.swing_lookback_bars:]`,
    `lo = lows[-config.swing_lookback_bars:]`; return `dict(_SWING_FALLBACK)` when
    `len(h) < 2 * config.swing_pivot_window + 1`; never raise.
    Call `find_peaks(h, n=config.swing_pivot_window)` and `find_troughs(lo, n=config.swing_pivot_window)`
    ONCE — this single pass is shared with `_compute_trend_structure` (Task 2) and with Plan 03's
    fibonacci helper (D-05/D-06), so the function must also return the raw intermediates
    `swing_high_price` and `swing_low_price` (the prices at the most recent peak/trough, or None), plus
    `swing_high_indices` and `swing_low_indices` (the raw index lists) and `n_bars` (len of the slice)
    so Task 2 does not re-run the detection. Those extra keys are in-memory intermediates only and must
    NEVER be threaded into `_build_feature_vector` (D-02/D-16 — a raw price and a raw bar index are
    equally invalid as persisted columns).
    Field derivation, converting the archived plugin's fake defaults to None per branch:
      swing_high_dist_atr = (h[peaks[-1]] - close_) / atr_val when peaks is non-empty else None
      swing_low_dist_atr  = (close_ - lo[troughs[-1]]) / atr_val when troughs is non-empty else None
      swing_high_age_bars = float(n_bars - 1 - peaks[-1]) when peaks is non-empty else None
                            (archived line 54 returns float(n_bars) here — that is a fake measurement, use None)
      swing_low_age_bars  = float(n_bars - 1 - troughs[-1]) when troughs is non-empty else None
      swing_high_type     = +1.0 if h[peaks[-1]] > h[peaks[-2]] else -1.0, when len(peaks) >= 2, else None
      swing_low_type      = +1.0 if lo[troughs[-1]] > lo[troughs[-2]] else -1.0, when len(troughs) >= 2, else None
      swing_pattern       = None when either type is None; else +1.0 when both are +1.0, -1.0 when both
                            are -1.0, else 0.0 (0.0 here IS a real measurement — "mixed", not "unknown")

    (c) Wire it into `compute()` immediately after the existing `_sr_fields` block (~line 3863), mirroring
    that block's shape: call once, unpack the 7 field values into `<name>_val: float | None` locals, and
    pass each by keyword into `_build_feature_vector(...)`, REPLACING the `None` placeholders Plan 01
    installed. Keep the raw `swing_high_price`/`swing_low_price`/index/n_bars entries in a local named
    `_swing_fields` so Plan 03 can read them from the same call.

    (d) Wire it into `compute_batch()`'s per-bar loop immediately after the existing `_sr_fields` block
    (~line 4300), using a causal pre-slice exactly like the S/R call site:
    `_sw_start = max(0, i - config.swing_lookback_bars + 1)` then pass
    `highs[_sw_start : i + 1]`, `lows[_sw_start : i + 1]`. Replace the same 7 `None` placeholders at that
    call site. Both paths MUST be updated in this same commit — that is the live/batch parity contract
    Task 3's parity test guards.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import inspect
from src.intelligence.feature_factory import _compute_swing_structure, _SWING_FALLBACK
assert set(_SWING_FALLBACK)=={'swing_high_dist_atr','swing_low_dist_atr','swing_high_type','swing_low_type','swing_pattern','swing_high_age_bars','swing_low_age_bars'}, _SWING_FALLBACK
assert all(v is None for v in _SWING_FALLBACK.values()), 'D-01: fallback must be all-None, not zeros'
assert list(inspect.signature(_compute_swing_structure).parameters)==['highs','lows','close_','atr_val','config']
print('ok signature + all-None fallback')"</automated>
    <automated>.venv/bin/python -c "
import numpy as np
from src.intelligence.feature_factory import _compute_swing_structure, FeatureFactoryConfig as C
import dataclasses
cfg=C(**{f.name: f.default for f in dataclasses.fields(C) if f.default is not dataclasses.MISSING})
" 2>/dev/null || echo 'note: build cfg via the test module _make_cfg helper instead'</automated>
    <automated>grep -n 'find_peaks(.*config.swing_pivot_window' src/intelligence/feature_factory.py | grep -v '^#' | wc -l | grep -qx 1</automated>
    <automated>.venv/bin/python -c "
src=open('src/intelligence/feature_factory.py').read()
body=src[src.index('def _build_feature_vector'):]
for bad in ('swing_high_idx','swing_low_idx','swing_high_price=','swing_low_price='):
    assert bad not in body, ('raw intermediate leaked into the persisted path', bad)
print('ok no raw index/price kwarg')"</automated>
    <automated>.venv/bin/ruff check src/intelligence/feature_factory.py</automated>
  </verify>
  <acceptance_criteria>
    - `_SWING_FALLBACK` exists with exactly the 7 field names, every value `None`, and a comment explaining why it is not modelled on `_SR_FALLBACK`.
    - `_compute_swing_structure(highs, lows, close_, atr_val, config)` never raises on any input (empty arrays, 1 bar, all-flat prices, atr_val=0, atr_val=nan) and returns the 7 keys plus the `swing_high_price`/`swing_low_price`/index/`n_bars` intermediates.
    - Exactly one `find_peaks(..., n=config.swing_pivot_window)` call site exists in the file (the shared pass, D-06).
    - Neither `swing_high_idx`/`swing_low_idx` nor `swing_high_price`/`swing_low_price` appear as `_build_feature_vector` kwargs anywhere.
    - Both `compute()` and `compute_batch()` pass real values for all 7 swing fields; `compute_batch()` uses a `max(0, i - config.swing_lookback_bars + 1)` causal pre-slice.
    - `.venv/bin/ruff check src/intelligence/feature_factory.py` clean.
  </acceptance_criteria>
  <done>Seven swing-detection fields carry real ATR-unit distances, categorical types and ages in both compute paths; every insufficient-data branch emits None; raw prices and raw indices stay in-memory only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _compute_trend_structure() — 6 fields, all-None early return (D-01), APR-backed divisor and range lookback</name>
  <files>src/intelligence/feature_factory.py</files>
  <behavior>
    - Given fewer than 2 confirmed swing highs OR fewer than 2 confirmed swing lows: ALL SIX fields are None. Specifically `trend_direction is None` (not 0.0) and `price_position is None` (not 0.5).
    - Given >= 2 of each and atr_val > 0: all six are finite; trend_strength is within [0, 1]; structure_integrity is within [0, 1]; price_position is within [0, 1]; trend_direction is in {-1.0, 0.0, +1.0}.
    - Given a strictly ascending staircase of higher highs and higher lows: trend_direction == +1.0 and trend_leg_count > 0.
    - Given a strictly descending staircase: trend_direction == -1.0.
    - Given atr_val <= 0: all six are None (the archived plugin silently skips the ATR normalization instead — that path is removed).
    - trend_strength changes when `config.trend_structure_atr_strength_divisor` changes, and price/range inputs change when `config.trend_structure_range_lookback_bars` changes (proves neither is hardcoded).
  </behavior>
  <read_first>
    - src/intelligence/features/i3_structure/trend_structure.py (FULL, 181 lines — `compute_full()` lines 35-146 is the reference algorithm; lines 48-56 are the D-01 fake-default early return to REPLACE with all-None; line 95's `high[-20:]`/`low[-20:]` and line 96's `/ 5.0` are the two APR-backed values; `_compute_trend_duration()` lines 151-178 ports as-is)
    - src/intelligence/feature_factory.py — the `_compute_swing_structure` block added in Task 1, plus lines 3332-3414 (_compute_sr_dist_atr's guard/slice/never-raise discipline)
    - .planning/phases/165-swing-fib-trend-structure-primitives/165-RESEARCH.md Section 2 Finding A (why the fake-default early return is the phase's most important fix)
  </read_first>
  <action>
    (a) Define `_TREND_STRUCTURE_FALLBACK: dict[str, float | None]` with all six keys mapped to `None`:
    trend_direction, trend_strength, trend_leg_count, structure_integrity, price_position,
    trend_duration_bars. Comment it with the same D-01 rationale as `_SWING_FALLBACK`, naming the exact
    archived values being rejected (`trend_direction=0.0`, `price_position=0.5`).

    (b) Define `_compute_trend_structure(highs, lows, close_, atr_val, swing_fields, config) -> dict[str, float | None]`
    where `swing_fields` is the dict returned by `_compute_swing_structure` — reuse its
    `swing_high_indices`/`swing_low_indices`/`n_bars` rather than calling `find_peaks`/`find_troughs` a
    second time (D-06: one shared pass, same window, same key). Return `dict(_TREND_STRUCTURE_FALLBACK)`
    when atr_val is not finite/positive, when the indices are absent, or when
    `len(swing_high_indices) < 2 or len(swing_low_indices) < 2` — this replaces the archived lines 48-56
    early return. Never raise.
    Port the rest of `compute_full()` faithfully against the SAME bounded window slice
    (`highs[-config.swing_lookback_bars:]` / `lows[-config.swing_lookback_bars:]`) that produced the
    indices, so index arithmetic stays valid:
      - bullish_legs / bearish_legs from consecutive swing-high and swing-low comparisons (archived 62-72)
      - direction / leg_count from the leg majority (archived 74-86); a tie yields direction 0.0 and
        leg_count 0.0, which ARE real measurements, not placeholders
      - strength = max(bullish, bearish) / total_legs, then
        `price_range = float(np.max(h[-config.trend_structure_range_lookback_bars:]) - np.min(lo[-config.trend_structure_range_lookback_bars:]))`
        and `strength = min(1.0, strength * (price_range / atr_val) / config.trend_structure_atr_strength_divisor)`.
        The archived code applies the ATR normalization only when ATR is available and silently returns
        the un-normalized ratio otherwise — remove that branch: atr_val <= 0 now returns the all-None
        fallback (a half-normalized strength is not comparable across symbols).
      - integrity = 1.0 - overlap_count / max_overlaps over the merged, bar-index-sorted swing list
        (archived 98-115)
      - price_position over the recent swing range (archived 117-127). The archived `else: position = 0.5`
        branch when `swing_range <= 0` is a D-01 fake value — return None for `price_position` in that
        case while leaving the other five fields real.
      - trend_duration_bars via a private `_trend_duration_bars(direction, swing_high_indices, swing_low_indices, h, lo, n_bars)`
        helper ported verbatim from the archived `_compute_trend_duration` static method.
    Keep the archived `round(x, 4)` calls on strength/integrity/position for value-parity with the
    original implementation.

    (c) Wire it into `compute()` and into `compute_batch()`'s per-bar loop immediately after Task 1's
    `_swing_fields` call in each, passing the already-computed `_swing_fields` dict and the same
    causal window/`atr_val`. Replace the 6 remaining `None` placeholders at BOTH `_build_feature_vector`
    call sites in the same commit.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import inspect
from src.intelligence.feature_factory import _compute_trend_structure, _TREND_STRUCTURE_FALLBACK
assert set(_TREND_STRUCTURE_FALLBACK)=={'trend_direction','trend_strength','trend_leg_count','structure_integrity','price_position','trend_duration_bars'}
assert all(v is None for v in _TREND_STRUCTURE_FALLBACK.values()), 'D-01: no numeric placeholders'
assert list(inspect.signature(_compute_trend_structure).parameters)==['highs','lows','close_','atr_val','swing_fields','config']
print('ok signature + all-None fallback')"</automated>
    <automated>.venv/bin/python -c "
src=open('src/intelligence/feature_factory.py').read()
i=src.index('# Swing / Trend Structure')
block=src[i:]
for bad in ('/ 5.0','[-20:]','0.5  #','= 0.5'):
    assert bad not in block, ('hardcoded constant survived APR migration', bad)
assert 'config.trend_structure_atr_strength_divisor' in block
assert 'config.trend_structure_range_lookback_bars' in block
print('ok APR-backed, no magic numbers')"</automated>
    <automated>BASELINE=$(git show HEAD:src/intelligence/feature_factory.py | grep -c 'find_peaks(') && CURRENT=$(grep -c 'find_peaks(' src/intelligence/feature_factory.py) && [ "$CURRENT" -eq "$((BASELINE + 1))" ]</automated>
    <automated>.venv/bin/ruff check src/intelligence/feature_factory.py</automated>
  </verify>
  <acceptance_criteria>
    - `_TREND_STRUCTURE_FALLBACK` is all-`None` for all six keys; the archived numeric early-return (`trend_direction=0.0`, `price_position=0.5`) exists nowhere in `feature_factory.py`.
    - `_compute_trend_structure` consumes Task 1's index lists — exactly ONE new `find_peaks(` call site is added versus this plan's starting commit (Phase 165's shared swing pass), proving no separate second detection pass was added for trend structure. Pre-existing call sites from Phase 163 (S/R) and Phase 164 (SMC order blocks/liquidity/zones) are unrelated and must be left unchanged — count them via a delta against `git show HEAD:...`, not a hardcoded absolute total, since prior phases already changed the baseline and future phases will too.
    - The block contains no literal `5.0` divisor and no literal `[-20:]` slice; both read from `config.trend_structure_atr_strength_divisor` / `config.trend_structure_range_lookback_bars`.
    - `atr_val <= 0` returns the all-None fallback (the archived silent un-normalized path is gone).
    - Both `compute()` and `compute_batch()` pass real values for all 6 trend fields.
    - `.venv/bin/ruff check src/intelligence/feature_factory.py` clean.
  </acceptance_criteria>
  <done>Six trend-structure fields carry real values in both compute paths, computed off Task 1's shared pivot pass, with every insufficient-data branch emitting None instead of a plausible-looking number.</done>
</task>

<task type="auto">
  <name>Task 3: Regression tests — nullability guard, ATR-unit pin, non-constant guard, live/batch parity</name>
  <files>tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py</files>
  <read_first>
    - tests/unit/intelligence/test_support_resistance_primitives.py (FULL, 357 lines — module constants, `_make_cfg(**overrides)`, and the four test shapes this file replicates)
    - src/intelligence/feature_factory.py — the `_compute_swing_structure` / `_compute_trend_structure` block added in Tasks 1-2, plus `compute()` and `compute_batch()` signatures
    - .planning/phases/165-swing-fib-trend-structure-primitives/165-PATTERNS.md section 10 (test structure + the commit `a748d13d` mutation-verification discipline)
  </read_first>
  <action>
    Create tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py. Module docstring:
    "Regression: Phase 165 swing/trend FeatureVector fields are non-constant, ATR-unit, live==batch, and
    NULL (never a plausible number) on insufficient data (D-01, the todo-153 failure shape)."
    Follow the Phase 163 file's structure: module-level `N`, a seeded `RNG = np.random.default_rng(...)`,
    a `_SWING_FIELDS` / `_TREND_FIELDS` tuple, and a `_make_cfg(**overrides)` helper returning a
    `FeatureFactoryConfig` with small windows so everything warms up within N bars. Plan 03 and Plan 04
    append their own test classes to this same file — leave the shared helpers at module level and
    section the tests by sub-scope.

    Write these tests:
    (a) `test_swing_trend_non_constant_batch` — run `compute_batch` over a synthetic random-walk series
    with visible swings; assert each of the 13 fields takes at least 2 distinct non-None values across
    the bars. This is the direct todo-153-shaped guard.
    (b) `test_swing_dist_in_atr_units` — build the hand-constructed constant-true-range fixture the
    Phase 163 file uses (Wilder ATR converges to exactly 1.0); assert `swing_high_dist_atr` equals the
    raw price difference to the known swing high within 1e-6, pinning the ATR conversion rather than
    just asserting finiteness.
    (c) `test_trend_structure_nullability` — THE most important test in this phase. Construct a fixture
    with fewer than 2 confirmed swing highs (e.g. a monotonically rising ramp, which produces no
    interior peaks) and assert every one of the 6 trend fields is exactly `None`; then assert
    explicitly `fv.trend_direction is not 0.0` and `fv.price_position is not 0.5` with a failure message
    naming D-01 and todo 153.
    (d) `test_swing_detector_partial_nullability` — a fixture with exactly one confirmed swing high:
    assert `swing_high_dist_atr` and `swing_high_age_bars` are floats while `swing_high_type` and
    `swing_pattern` are `None`.
    (e) `test_swing_trend_zero_atr_all_none` — feed a flat-price series (ATR 0) and assert all 13 fields
    are `None`.
    (f) `test_swing_trend_live_batch_parity` — run `compute()` over the full bar list and
    `compute_batch()` over the same bars, and assert every one of the 13 fields matches on the final bar
    to 1e-6 (treating `None == None` as a match and `None` vs float as a failure). This is the test that
    catches a forgotten `compute_batch()` wiring update.
    (g) `test_trend_structure_apr_keys_are_live` — construct two configs differing only in
    `trend_structure_atr_strength_divisor` and assert `trend_strength` differs; repeat for
    `swing_pivot_window` and assert at least one swing field differs. Proves the APR keys are actually
    read rather than shadowed by a surviving constant.

    Before considering this task done, apply the commit `a748d13d` mutation-verification discipline:
    temporarily force `_compute_swing_structure` and `_compute_trend_structure` to return their fallback
    dicts, confirm tests (a), (b), (d), (f) and (g) FAIL; then temporarily change
    `_TREND_STRUCTURE_FALLBACK` to the archived numeric defaults (`trend_direction: 0.0`,
    `price_position: 0.5`) and confirm test (c) FAILS. Revert both local edits before committing, and
    record the observed failures in the plan SUMMARY.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py -x -q</automated>
    <automated>.venv/bin/python -c "
src=open('tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py').read()
for t in ('test_swing_trend_non_constant_batch','test_swing_dist_in_atr_units','test_trend_structure_nullability','test_swing_detector_partial_nullability','test_swing_trend_zero_atr_all_none','test_swing_trend_live_batch_parity','test_trend_structure_apr_keys_are_live'):
    assert t in src, t
print('ok 7 tests present')"</automated>
    <automated>.venv/bin/pytest tests/unit/ -q</automated>
    <automated>.venv/bin/ruff check tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py</automated>
  </verify>
  <acceptance_criteria>
    - All 7 named tests exist and pass.
    - The nullability test asserts `is None` on all 6 trend fields and carries a failure message naming D-01 / todo 153.
    - The ATR-unit test pins an exact numeric value on a constant-true-range fixture (not merely `isfinite`).
    - The parity test compares all 13 fields between `compute()` and `compute_batch()` and treats a None/float mismatch as a failure.
    - The mutation check was actually run: the SUMMARY records which tests failed under (i) forced-fallback helpers and (ii) a numeric `_TREND_STRUCTURE_FALLBACK`, and both local edits were reverted.
    - `.venv/bin/pytest tests/unit/ -q` fully green; ruff clean on the new test file.
  </acceptance_criteria>
  <done>The phase's regression test file exists with a proven-effective nullability guard, an ATR-unit pin, a non-constant guard, a live/batch parity gate and an APR-liveness check for the swing/trend sub-scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| market_data_ohlcv_tradeable -> FeatureFactory.compute/compute_batch | Already-validated internal OHLCV; no external or untrusted input crosses. |
| compute_batch() bar-index window -> persisted feature_vectors row | A window slice that reached past bar `i` would inject look-ahead into the training corpus. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-06 | Tampering (silent wrong data) | `trend_structure` / `swing_detector` emitting a fake-but-numeric "no signal" (D-01) | mitigate | All-`None` fallback dicts; per-branch `None`; `test_trend_structure_nullability` plus a mutation check proving the test fails against the archived numeric defaults. |
| T-165-07 | Tampering (look-ahead leakage) | `compute_batch()` slicing highs/lows beyond bar `i`, poisoning IC measurement | mitigate | Causal pre-slice `max(0, i - config.swing_lookback_bars + 1) : i + 1`, mirroring the Phase 163 S/R call site; `find_peaks` confirms a pivot only with `n` bars on both sides, so no future bar is read. |
| T-165-08 | Tampering (train/serve skew) | Wiring `compute()` but forgetting `compute_batch()` (or vice versa) | mitigate | Both call sites updated in the same commit; `test_swing_trend_live_batch_parity` asserts equality to 1e-6 on all 13 fields. |
| T-165-09 | Repudiation (untracked config change) | A surviving hardcoded `5.0` divisor, `[-20:]` slice or `neighbor=5` | mitigate | Source-assertion verify greps the new block for those literals; `test_trend_structure_apr_keys_are_live` proves the config values actually change output. |
| T-165-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero new packages; numpy, `find_peaks`/`find_troughs` are already-live in-repo dependencies. No install task exists. |
</threat_model>

<verification>
- `_SWING_FALLBACK` and `_TREND_STRUCTURE_FALLBACK` are all-`None`; no `trend_direction=0.0` / `price_position=0.5` placeholder survives anywhere.
- Exactly one new `find_peaks(` call site is added versus this plan's starting commit (Phase 165's shared swing pass) — trend structure reuses that same pass rather than adding a third detection pass. Pre-existing Phase 163 (S/R) and Phase 164 (SMC) call sites are unrelated and unchanged.
- No literal `5.0` divisor or `[-20:]` slice in the new block; both read from `FeatureFactoryConfig`.
- `compute()` and `compute_batch()` agree to 1e-6 on all 13 fields; `compute_batch()` uses a causal pre-slice.
- Raw swing prices and raw bar indices exist only as in-memory intermediates — never as `_build_feature_vector` kwargs.
- `.venv/bin/pytest tests/unit/ -q` green; ruff clean.
</verification>

<success_criteria>
Thirteen of Phase 165's 41 columns (7 swing-detection + 6 trend-structure) carry real, varying,
ATR-normalized or bounded values in both the live and batch compute paths, computed from a single
shared APR-backed pivot pass. Both files' silent-wrong-answer bug is fixed rather than ported: every
insufficient-data branch emits `None`, proven by a mutation-verified regression test. The raw swing
high/low intermediates Plan 03's fibonacci port needs are produced and returned in-memory, so D-05's
cross-plugin fallback can be deleted outright rather than reimplemented.
</success_criteria>

<output>
Create `.planning/phases/165-swing-fib-trend-structure-primitives/165-02-SUMMARY.md` when done.
</output>
