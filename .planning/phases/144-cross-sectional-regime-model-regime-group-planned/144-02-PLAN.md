---
phase: 144-cross-sectional-regime-model-regime-group-planned
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/regime_signals/_tf_window.py
  - src/intelligence/regime_signals/breadth_vol.py
  - src/intelligence/regime_signals/curve_credit.py
  - tests/unit/test_regime_signals_breadth_vol.py
  - tests/unit/test_regime_signals_curve_credit.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "breadth_vol.compute uses a causal bisect-based expanding rank — a later value cannot change an earlier rank (no look-ahead)"
    - "_tf_window converts a daily-denominated window to the correct bar count per TF (200 days -> 15600 bars at 5m, 200 at 1d)"
    - "curve_credit produces a signed curve/credit signal with tier bucketing (steep/flat/inverted)"
    - "Both modules expose the compute()/build_tiers()/PROB_KEYS contract and are DB-free pure functions"
  artifacts:
    - path: "src/intelligence/regime_signals/breadth_vol.py"
      provides: "Equity cross-sectional signal (breadth + vol-of-vol), causal rank"
      contains: "bisect"
    - path: "src/intelligence/regime_signals/curve_credit.py"
      provides: "Rates cross-sectional signal (curve slope + credit spread)"
    - path: "src/intelligence/regime_signals/_tf_window.py"
      provides: "Shared daily-window -> TF-bar-count conversion"
      contains: "_BARS_PER_DAY"
    - path: "tests/unit/test_regime_signals_breadth_vol.py"
      provides: "Causal-property + tf_window regression tests"
      contains: "causal"
  key_links:
    - from: "src/intelligence/regime_signals/breadth_vol.py"
      to: "bisect-based expanding rank"
      via: "causal rank replacing pd.rank(pct=True) whole-series"
      pattern: "bisect"
---

<objective>
Build the two ENABLED cross-sectional signal modules — `breadth_vol.py` (equity) and
`curve_credit.py` (rates) — plus the shared `_tf_window.py` helper. Both are pure, DB-free
functions matching the `REGISTRY` contract (`compute(ref_bars, params) -> (sig1, sig2) | None`,
`build_tiers()`, `PROB_KEYS`).

CRITICAL CORRECTION (RESEARCH.md Pitfall 1 / PATTERNS.md): do NOT copy the plan doc's
`breadth_vol.py` verbatim. Its `compute()` uses `pd.rank(pct=True, na_option="keep")` — a non-causal
whole-series rank that reintroduces the exact look-ahead bias Phase 141 P0-T2 fixed. Port the causal
bisect-based expanding rank from `services/equity_regime_model.py` instead.

Purpose: These are the signal math consumed by Plan 04's dispatcher. Wave 1 — no dependency on
migration or dispatcher (pure functions, tested in isolation).
Output: 3 module files + 2 test files.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-cross-sectional-regime-model-regime-group-planned/144-RESEARCH.md
@.planning/phases/144-cross-sectional-regime-model-regime-group-planned/144-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _tf_window helper + breadth_vol signal module (causal-rank port)</name>
  <files>src/intelligence/regime_signals/_tf_window.py, src/intelligence/regime_signals/breadth_vol.py, tests/unit/test_regime_signals_breadth_vol.py</files>
  <read_first>
    - services/equity_regime_model.py (THE extraction source: read lines 84-101 for _tf_window/_BARS_PER_DAY, lines 186-251 for _compute_vix_pct_rank causal bisect rank, lines 298-332 for _compute_breadth_fraction — port these, do NOT invent new versions)
    - tests/unit/services/test_equity_regime_model_causal.py (mirror test_vix_pct_rank_causal_property lines 70-121 and test_tf_window_* lines 45-62)
    - docs/plans/2026-07-01-cross-sectional-regime-model.md (Task 2, lines 308-600 — structural contract for compute()/build_tiers()/PROB_KEYS ONLY; its rank line ~564 pd.rank(pct=True) is the STALE bug — replace it)
  </read_first>
  <behavior>
    - Causal property: appending a large future outlier value must NOT change any earlier non-NaN rank (assert abs diff < 1e-9 across common index)
    - _tf_window(200, '5m') == 15600, _tf_window(252, '1h') == 1764, _tf_window(200, '1d') == 200, _tf_window(1, '15m') == 26 (exact live test values from test_equity_regime_model_causal.py; _BARS_PER_DAY {1d:1, 1h:7, 15m:26, 5m:78})
    - compute() returns (breadth, vol_signal) Series or None; warmup positions before window fill are NaN
    - breadth_vol.compute is DB-free — operates only on the passed ref_bars dict, no psycopg2 import
  </behavior>
  <action>
    Create src/intelligence/regime_signals/_tf_window.py containing `_BARS_PER_DAY` and `_tf_window()`
    ported verbatim from services/equity_regime_model.py:84-101 (single source of truth for the
    day->bar mapping; do NOT redefine it inside breadth_vol.py — RESEARCH.md Don't-Hand-Roll).

    Create src/intelligence/regime_signals/breadth_vol.py with `compute(ref_bars, params)`,
    `build_tiers()`, `PROB_KEYS` per the plan doc Task 2 contract, EXCEPT:
    (1) Replace the plan doc's `vix_z.rank(pct=True, na_option="keep")` with the causal bisect-based
    expanding rank ported from equity_regime_model.py:186-251 (sorted_window list + bisect.insort +
    (bisect_left+bisect_right)/2/len rank; NaN -> NaN, first value -> 1.0). Import `bisect` and `math`.
    (2) breadth_vol.compute() receives ALREADY-BAR-SCALED window ints in `params` — the dispatcher
    (Plan 04) pre-scales via _tf_window before calling. So compute() itself does NOT call _tf_window
    (the module stays TF-agnostic per PATTERNS.md option-b resolution). It still imports _tf_window
    only if a local helper needs it; primary consumer is the dispatcher + tests.
    (3) Port _compute_breadth_fraction logic from equity_regime_model.py:298-332, adapted to operate
    on the pre-fetched `ref_bars: dict[str, pd.DataFrame]` (set_index timestamp, rolling ma_window
    mean, fraction above MA, concat + mean skipna) — no DB fetch inside compute().
    Exception variable name is `error` per CLAUDE.md.

    Create tests/unit/test_regime_signals_breadth_vol.py mirroring test_equity_regime_model_causal.py:
    the causal-property test (append future outlier, assert earlier ranks unchanged) AND the four
    _tf_window value tests. This causal test is the Wave 0 gap flagged in VALIDATION.md — it is NOT in
    the plan doc's original test file and is mandatory here.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_regime_signals_breadth_vol.py -v && grep -q "bisect" src/intelligence/regime_signals/breadth_vol.py && ! grep -q "rank(pct=True" src/intelligence/regime_signals/breadth_vol.py && ! grep -q "import psycopg2" src/intelligence/regime_signals/breadth_vol.py && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_regime_signals_breadth_vol.py -v` all green, includes a causal-property test
    - `grep "bisect" src/intelligence/regime_signals/breadth_vol.py` matches (causal rank ported)
    - `grep "rank(pct=True" src/intelligence/regime_signals/breadth_vol.py` returns NOTHING (stale non-causal rank not present)
    - `grep "import psycopg2" src/intelligence/regime_signals/breadth_vol.py` returns NOTHING (DB-free)
    - `grep "_BARS_PER_DAY" src/intelligence/regime_signals/_tf_window.py` matches
  </acceptance_criteria>
  <done>_tf_window.py + breadth_vol.py exist; breadth_vol uses causal bisect rank (not pd.rank whole-series), is DB-free; test file passes incl. the causal-property regression test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: curve_credit signal module (rates)</name>
  <files>src/intelligence/regime_signals/curve_credit.py, tests/unit/test_regime_signals_curve_credit.py</files>
  <read_first>
    - docs/plans/2026-07-01-cross-sectional-regime-model.md (Task 3, lines 600-858 — curve_credit compute()/build_tiers()/PROB_KEYS + its test file at lines 616-750; RESEARCH.md found this complete with no drift — use as-is)
    - src/intelligence/regime_signals/breadth_vol.py (structural contract to mirror: same compute()/build_tiers()/PROB_KEYS shape, DB-free)
  </read_first>
  <behavior>
    - compute() returns a signed (curve_slope, credit_spread) signal or None
    - build_tiers() buckets into steep/flat/inverted (curve) x wide/tight (credit) tier vocabulary distinct from equity's low/mid/high x bear/neutral/bull
    - Warmup positions before the window fills are NaN
    - Module is DB-free (no psycopg2 import)
  </behavior>
  <action>
    Create src/intelligence/regime_signals/curve_credit.py from the plan doc's Task 3 code (lines
    750-858) — RESEARCH.md confirmed it is new math with no live predecessor and no drift issue, so
    port as-written. Windows (`curve_window`, `credit_window`, default 60) are treated as DAILY bars
    and are `_tf_window`-scaled by the dispatcher (Plan 04) BEFORE compute() is called — matching
    breadth_vol's convention (RESEARCH.md Assumption A1 resolved: consistent scaling across the whole
    regime_signals package, one TF-handling convention). curve_credit.compute() therefore does NOT
    call _tf_window itself; it receives already-bar-scaled window ints in params. Tier vocabulary MUST
    NOT overlap with breadth_vol's (Pitfall 4: feature_ic_scores has no regime_group column, so group
    identity is implicit in label uniqueness) — use steep/flat/inverted x wide/tight. Add a code
    comment documenting this non-overlap invariant. Exception variable name is `error`.

    Create tests/unit/test_regime_signals_curve_credit.py from the plan doc's Task 3 test file
    (complete as written per RESEARCH.md): signal-direction correctness + tier bucketing.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_regime_signals_curve_credit.py -v && ! grep -q "import psycopg2" src/intelligence/regime_signals/curve_credit.py && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_regime_signals_curve_credit.py -v` all green
    - `grep "import psycopg2" src/intelligence/regime_signals/curve_credit.py` returns NOTHING (DB-free)
    - Tier labels use steep/flat/inverted x wide/tight vocabulary (grep confirms non-equity vocabulary)
    - A code comment documents the label-vocabulary non-overlap invariant vs breadth_vol
  </acceptance_criteria>
  <done>curve_credit.py exists with the rates signal + non-overlapping tier vocabulary, DB-free; test file passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure in-memory signal computation — no DB, no network, no external input crosses any boundary in these modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-02-LA | Information Disclosure | breadth_vol causal rank | mitigate | The look-ahead-bias regression (a data-integrity threat: future data leaking into a past regime label that ic_engine stratifies on) is prevented by porting the causal bisect rank and guarded by the mandatory causal-property test. This is the single most important correctness threat in the plan and is unit-enforced. |
| T-144-SC | Tampering | package installs | accept | Zero new packages — pandas/numpy already vendored. Supply-chain N/A. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_regime_signals_breadth_vol.py tests/unit/test_regime_signals_curve_credit.py -v` green
- No psycopg2 import in either signal module (DB-free purity)
- breadth_vol has no `rank(pct=True` occurrence
</verification>

<success_criteria>
- Both enabled signal modules exist, pure/DB-free, matching the compute()/build_tiers()/PROB_KEYS contract
- breadth_vol uses causal bisect rank; look-ahead regression test present and green
- _tf_window helper ported to a shared module (single source of truth)
- Tier vocabularies non-overlapping across the two groups
</success_criteria>

<output>
Create `.planning/phases/144-cross-sectional-regime-model-regime-group-planned/144-02-SUMMARY.md` when done
</output>
