---
phase: 144-cross-sectional-regime-model-regime-group-planned
plan: 06
type: execute
wave: 4
depends_on: ["144-05"]
files_modified:
  - scripts/analysis/phase144_regime_separation_gate.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "D-05/D-06: this plan is Phase 144's own final acceptance-gate task (not a separate follow-up todo) — code-complete alone does not close the phase"
    - "D-07: this task is gated on the in-flight 143.1-07 corpus rebuild completing — code/migration/dispatcher/routing work (Plans 01-05) does not wait on it, only this measurement does"
    - "D-08: the precondition check reads feature_ic_scores/corpus pipeline freshness rather than a hardcoded row count or date"
    - "A fresh SQL query compares TLT per-symbol HMM regime IC separation vs the new rates cross-sectional group separation"
    - "The separation gap is classified against todo 026's bands (gap < 0.01 deficient, 0.01-0.05 ambiguous)"
    - "The measurement runs only after the 143.1-07 corpus rebuild is verified complete (external precondition)"
    - "The falsifier verdict (F1/F2 from the Fable decision doc) is recorded — whether per-symbol HMM stays demoted-to-shadow for rates or the factor-augmented challenger is triggered"
  artifacts:
    - path: "scripts/analysis/phase144_regime_separation_gate.py"
      provides: "D-05 acceptance-gate measurement + band classification + verdict"
      contains: "regime_scope"
  key_links:
    - from: "scripts/analysis/phase144_regime_separation_gate.py"
      to: "feature_ic_scores"
      via: "query on regime_scope + regime + symbol columns"
      pattern: "feature_ic_scores"
---

<objective>
Run the phase's ACCEPTANCE GATE (D-05/D-06): the widened Step 1 protocol from
`docs/research/fable-2026-07-07-phase144-conditioning-decision.md` §6 Input 1 — compare TLT's
per-symbol HMM regime-IC separation against the NEW `rates` cross-sectional group's separation (not
the old contaminated equity comparison), one representative per enabled regime_group, classified
against todo 026's bands (gap < 0.01 deficient, 0.01-0.05 ambiguous). This is the pre-committed
falsifier check (F1/F2) that decides whether per-symbol HMM stays demoted-to-shadow for `rates`
(the default via routing) or the factor-augmented HMM challenger (option c) is triggered.

"A regime label nobody re-measured isn't proven, it's deployed." Code-complete is NOT phase-done
(D-05).

HARD EXTERNAL PRECONDITION (D-07/D-08): this task is gated on the in-flight `143.1-07` corpus
rebuild completing and being verified clean. It CANNOT run against stale data and must respect
single-writer discipline on the derived tables. This gate is external to this phase's own plans —
check `feature_ic_scores` row freshness / the corpus pipeline state before running; do NOT proceed
if the rebuild is still in flight.

The exact SQL for this gate is NOT pre-registered anywhere (RESEARCH.md Open Question 3) — writing
it fresh against `feature_ic_scores.regime_scope` is part of this task's deliverable.

Purpose: The evidence-based promotion/demotion decision for the rates regime model.
Output: `scripts/analysis/phase144_regime_separation_gate.py` + a recorded verdict in the SUMMARY.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-cross-sectional-regime-model-regime-group-planned/144-CONTEXT.md
@.planning/phases/144-cross-sectional-regime-model-regime-group-planned/144-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify corpus freshness + run dispatcher/ic_engine + separation gate measurement</name>
  <files>scripts/analysis/phase144_regime_separation_gate.py</files>
  <read_first>
    - docs/research/fable-2026-07-07-phase144-conditioning-decision.md (§6 Input 1 protocol + falsifiers F1/F2 — the exact comparison and decision rule)
    - .planning/todos/deferred/026-hmm-regime-audit-optimization.md (Step 1/2 per-symbol query SHAPE + the 0.01/0.05 gap bands — the shape is specified, the SQL is not)
    - the corpus pipeline state memory (project_corpus_pipeline_state.md) — the source of truth for whether 143.1-07 has completed; the plan does not hardcode a row count
  </read_first>
  <action>
    STEP 0 — PRECONDITION CHECK (do this first, abort loudly if it fails): verify the 143.1-07 corpus
    rebuild is complete and clean. Check feature_ic_scores freshness (corpus-wide BH-FDR write landed)
    against the corpus pipeline state memory. If the rebuild is still in flight, STOP: do not run the
    dispatcher full-run, the ic_engine re-run, or the measurement — record BLOCKED-ON-143.1-07 in the
    SUMMARY with the freshness evidence and exit. Running now would violate single-writer discipline
    on the derived tables (D-07).

    STEP 1 — regenerate the measurement inputs (only if STEP 0 passes): run
    services/cross_sectional_regime_model.py (full run, all TFs) to populate market_regimes with the
    new regime_group labels including the rates group, then run the batched services/ic_engine.py
    re-run to repopulate feature_ic_scores with both the per-symbol (regime_scope='symbol_hmm') and
    cross-sectional (regime_scope='cross_sectional') strata under the corrected routing.

    STEP 2 — write scripts/analysis/phase144_regime_separation_gate.py FRESH (Open Question 3 — no
    pre-registered SQL exists). Query feature_ic_scores.regime_scope + regime + symbol columns:
    - Per-symbol side: symbol='TLT', regime_scope='symbol_hmm' — the per-symbol HMM regime IC spread.
    - Cross-sectional rates side: the POOLED rates-group rows (symbol='POOLED' AND is_pooled=true,
      regime label set from the rates group, regime_scope='cross_sectional').
    Compute the IC separation gap (spread of regime-conditional IC across the regime labels) for each
    side, one representative per enabled regime_group. Classify the gap against todo 026's bands:
    gap < 0.01 = deficient, 0.01-0.05 = ambiguous, > 0.05 = adequate.

    STEP 3 — record the verdict (F1/F2 from the Fable decision doc): does TLT's per-symbol HMM
    separation justify keeping it, or does it stay demoted-to-shadow for rates (default via routing),
    or is the factor-augmented HMM challenger (option c) triggered? Write the gap numbers, band
    classification, and the resulting decision into the SUMMARY.

    Follow CLAUDE.md: parameterized psycopg2 queries, exception variable name error, timestamps UTC.
    This is an analysis script (not a daemon) — no BaseBatch, no D-06 contract required, but a clean
    argparse + main() is fine.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/analysis/phase144_regime_separation_gate.py').read())" && grep -q "regime_scope" scripts/analysis/phase144_regime_separation_gate.py && grep -q "TLT" scripts/analysis/phase144_regime_separation_gate.py && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - scripts/analysis/phase144_regime_separation_gate.py parses as valid Python and queries feature_ic_scores.regime_scope + regime + symbol
    - The script computes a TLT-per-symbol vs rates-cross-sectional separation gap and classifies it against the 0.01/0.05 bands
    - STEP 0 precondition gate is implemented: the script (or the executor's run log) records either PASS-with-verdict or BLOCKED-ON-143.1-07 with freshness evidence — never runs the writes against a stale/in-flight corpus
    - The SUMMARY records the gap numbers, band classification, and the F1/F2 falsifier verdict (keep / demote-to-shadow / trigger challenger)
  </acceptance_criteria>
  <done>Corpus freshness verified (or task recorded BLOCKED with evidence); if fresh, dispatcher + ic_engine re-run executed, separation gate measured, band-classified, and the falsifier verdict recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-flight 143.1-07 rebuild -> derived tables | Single-writer discipline: running this gate's writes concurrently with the rebuild would corrupt derived tables |
| analysis script -> feature_ic_scores (read) | Read-only query over a locally-trusted DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-06-SW | Tampering (data integrity) | concurrent write vs 143.1-07 | mitigate | STEP 0 precondition check aborts loudly if the rebuild is in flight — enforces single-writer discipline (D-07). The gate never writes against a stale/concurrent corpus. |
| T-144-06-SQL | Tampering | separation-gate query | mitigate | Parameterized psycopg2 read query; TLT/POOLED/regime_scope values bound, not interpolated. Read-only — no write surface in the measurement query itself. |
| T-144-06-BIAS | Repudiation | verdict recording | mitigate | Pre-committed bands (0.01/0.05) and falsifiers (F1/F2) fix the decision rule before the number is seen — the verdict is auditable, not post-hoc rationalized. |
| T-144-SC | Tampering | package installs | accept | Zero new packages. Supply-chain N/A. |
</threat_model>

<verification>
- Script parses and queries feature_ic_scores.regime_scope
- Corpus freshness verified before any write (STEP 0)
- Gap classified against todo 026 bands; F1/F2 verdict recorded in SUMMARY
</verification>

<success_criteria>
- Acceptance gate runs post-143.1-07 (or cleanly blocks with evidence if still in flight)
- TLT-per-symbol vs rates-cross-sectional separation measured with a freshly-written query
- Verdict (keep / demote-to-shadow / trigger factor-augmented challenger) recorded against pre-committed bands/falsifiers
</success_criteria>

<output>
Create `.planning/phases/144-cross-sectional-regime-model-regime-group-planned/144-06-SUMMARY.md` when done
</output>
