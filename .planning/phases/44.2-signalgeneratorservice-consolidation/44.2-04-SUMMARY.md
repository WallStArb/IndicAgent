---
phase: 44.2-signalgeneratorservice-consolidation
plan: 04
subsystem: testing
tags: [pytest, asyncio, pydantic, prometheus, integration-test, signal-pipeline]

# Dependency graph
requires:
  - phase: 44.2-signalgeneratorservice-consolidation
    provides: in-process pipeline + BarIntelligenceRecord schema + _process_event method
provides:
  - E2E integration test validating bar → BarIntelligenceRecord pipeline (9 test functions)
  - Regression gate for _process_event with funnel counts, winner consistency, metrics, audit queue
affects:
  - future refactors of signal_generator_service._process_event
  - PIPE-07 requirement verification

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "__new__ pattern for SignalGeneratorService bypassing __init__ in integration tests"
    - "AsyncMock._kafka_producer.publish capture via reversed call_args_list to extract BarIntelligenceRecord"
    - "Prometheus counter delta test pattern: read _value.get() before/after call to verify increment"

key-files:
  created:
    - tests/integration/test_signal_generator_e2e.py
  modified: []

key-decisions:
  - "Used __new__ + manual attribute injection (not @patch of __init__) to test _process_event in isolation without live Redpanda or DB"
  - "Prometheus counter deltas tested by comparing _value.get() before and after — not by resetting global state"
  - "Audit queue test processes 10 bars and asserts zero drop delta — queue maxsize=1000 is far above 10-bar load"

patterns-established:
  - "integration/test_*_e2e.py: no live infra needed — all I/O mocked, safe in CI alongside unit tests"
  - "_capture_record(): search reversed publish call_args_list for intelligence.record topic to extract BarIntelligenceRecord payload"

requirements-completed: [PIPE-07]

# Metrics
duration: 4min
completed: 2026-03-21
---

# Phase 44.2 Plan 04: E2E Integration Test Summary

**9 pytest-asyncio tests validate the full bar → BarIntelligenceRecord pipeline with mocked I/O — no live infra required**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-21T20:48:59Z
- **Completed:** 2026-03-21T20:53:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Created `tests/integration/test_signal_generator_e2e.py` (529 lines) with 9 test functions exercising the complete `_process_event` pipeline
- All 9 tests pass: all-fields-populated, funnel monotonicity, ranked_signal fields, winner consistency, ledger_written=True/False, Prometheus metrics increment, audit queue zero drops, empty-signal edge case
- Full unit suite (2644 tests) passes with zero regressions
- Lint clean (`ruff check` passes)

## Task Commits

1. **Task 1: E2E integration test for bar → BarIntelligenceRecord pipeline** - `8bb8a3b` (test)

## Files Created/Modified

- `tests/integration/test_signal_generator_e2e.py` - 9 E2E tests for the consolidated in-process pipeline

## Decisions Made

- Used `__new__` pattern (no `__init__`) to construct `SignalGeneratorService` with only the attributes needed by `_process_event`, consistent with existing unit test patterns in `tests/unit/service_tests/`
- `_kafka_producer = AsyncMock()` captures all `publish()` calls; `_capture_record()` searches `call_args_list` in reverse to find the `intelligence.record` topic payload and parses it via `BarIntelligenceRecord.model_validate_json()`
- Prometheus counter delta testing uses `_value.get()` before/after — shared global state, so assertions measure delta not absolute value
- `db_manager = None` by default (no ledger writes); ledger-path tests attach a `MagicMock` db_manager and patch `insert_signals_with_features`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed IntelligenceEvent fixture: hmm_regime in SMCContext, not I4Context**
- **Found during:** Task 1 (RED phase — first test run)
- **Issue:** Plan's example event put `hmm_regime=1` in `I4Context`, but the schema has `extra="forbid"` and `hmm_regime` is a field on `SMCContext`, not `I4Context`
- **Fix:** Moved `hmm_regime/hmm_regime_prob/hmm_regime_duration` to `SMCContext()` in `_make_event()`
- **Files modified:** `tests/integration/test_signal_generator_e2e.py`
- **Committed in:** `8bb8a3b`

**2. [Rule 1 - Bug] Fixed IntelligenceEvent fixture: i2=None invalid, must be I2Events()**
- **Found during:** Task 1 (after hmm_regime fix, second test run)
- **Issue:** `IntelligenceEvent.i2` has type `I2Events` with no `Optional` wrapper — passing `None` fails Pydantic validation
- **Fix:** Changed `i2=None` to `i2=I2Events()` in `_make_event()`
- **Files modified:** `tests/integration/test_signal_generator_e2e.py`
- **Committed in:** `8bb8a3b`

---

**Total deviations:** 2 auto-fixed (both Rule 1 — fixture construction bugs from plan's example code)
**Impact on plan:** Both fixes correct schema usage against the real Pydantic models. No scope changes.

## Issues Encountered

None beyond the two fixture bugs above (both auto-fixed inline).

## Next Phase Readiness

- PIPE-07 regression gate is now in place — any future refactor of `_process_event` must keep all 9 tests passing
- Phase 44.2 complete — all 4 plans (01 pipeline extraction, 02 service consolidation, 03 dead-code cleanup, 04 E2E tests) shipped

---
*Phase: 44.2-signalgeneratorservice-consolidation*
*Completed: 2026-03-21*
