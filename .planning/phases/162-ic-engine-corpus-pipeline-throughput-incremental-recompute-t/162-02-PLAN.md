---
phase: 162-ic-engine-corpus-pipeline-throughput-incremental-recompute-t
plan: 02
type: execute
wave: 2
depends_on: ["162-01"]
files_modified:
  - services/ic_engine.py
  - production/migrations/249_ic_cross_sectional_bootstrap_threads_per_tf.sql
  - tests/unit/test_ic_engine_parallelism.py
autonomous: true
requirements: [SC-5]

must_haves:
  truths:
    - "cross_sectional_bootstrap_threads is a per-tf dict assembled from 4 flat APR keys, not a scalar"
    - "The bootstrap CI call site indexes the dict by tf, matching bootstrap_block_size[tf] one arg over"
    - "Thread count changes wall time only, never output — enforced by the precomputed resample-index matrix from 162-01"
  artifacts:
    - path: "production/migrations/249_ic_cross_sectional_bootstrap_threads_per_tf.sql"
      provides: "4 flat per-tf APR keys for cross-sectional bootstrap threads"
      contains: "alpha.ic.cross_sectional_bootstrap_threads.5m"
    - path: "services/ic_engine.py"
      provides: "per-tf thread dict in ICEngineConfig.from_apr() + indexed call site"
      contains: "cross_sectional_bootstrap_threads"
  key_links:
    - from: "services/ic_engine.py::ICEngineConfig.from_apr"
      to: "config_state 4 flat keys"
      via: "dict-comprehension assembly mirroring bootstrap_block_size"
      pattern: "cross_sectional_bootstrap_threads\\."
    - from: "services/ic_engine.py::_compute_one_cross_sectional_cell"
      to: "config.cross_sectional_bootstrap_threads[tf]"
      via: "subscript at the _circular_block_bootstrap_ic max_workers arg"
      pattern: "cross_sectional_bootstrap_threads\\[tf\\]"
---

<objective>
Convert `cross_sectional_bootstrap_threads` from a single scalar APR key into a per-tf dict
(`{5m, 15m, 1h, 1d}`), so the cross-sectional bootstrap CI stops paying 6-thread dispatch overhead
on timeframes (15m/1h/1d) that finish in minutes serially, while 5m keeps its threading speedup.
This lands AFTER 162-01 so it measures and edits the post-refactor loop the Plan 03 fingerprint will
describe, and avoids merge conflicts on `ICEngineConfig`/`from_apr()` with the structural pass.

Purpose: Close todo 133 — a pure performance knob, zero output change. Thread count must not change
the statistic, which 162-01's precomputed resample-index matrix already makes explicit.

Output: Migration 249 seeding 4 flat per-tf keys; `ICEngineConfig.from_apr()` assembling them into a
dict (mirroring `bootstrap_block_size`); the `_circular_block_bootstrap_ic(..., max_workers=...)` call
site in `_compute_one_cross_sectional_cell` indexing by tf; extended parallelism test.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/162-ic-engine-corpus-pipeline-throughput-incremental-recompute-t/162-RESEARCH.md
@.planning/phases/162-ic-engine-corpus-pipeline-throughput-incremental-recompute-t/162-PATTERNS.md
@.planning/phases/162-ic-engine-corpus-pipeline-throughput-incremental-recompute-t/162-01-SUMMARY.md

<interfaces>
<!-- Extracted from live main @ 2f5334f2. -->

Current scalar field (services/ic_engine.py:482, bound at from_apr line 612-614):
  cross_sectional_bootstrap_threads: int = 1
  cross_sectional_bootstrap_threads=int(cfg.get_sync("infra.ic_engine.cross_sectional_bootstrap_threads", 1))

Per-tf dict precedent to mirror EXACTLY (services/ic_engine.py:472-474 field, 598-606 from_apr):
  bootstrap_block_size: dict[str, int] = dataclasses.field(default_factory=lambda: {"5m":78,"15m":26,"1h":10,"1d":10})
  bootstrap_block_size={tf: int(cfg.get_sync(f"alpha.ic.bootstrap_block_size.{tf}", default))
                        for tf, default in (("5m",78),("15m",26),("1h",10),("1d",10))}

Call site to update (in the extracted _compute_one_cross_sectional_cell after 162-01), the
_circular_block_bootstrap_ic invocation currently passing max_workers=config.cross_sectional_bootstrap_threads,
adjacent to config.bootstrap_block_size[tf] one argument over.

Migration APR-key seed precedent: production/migrations/157_alpha_ic_apr_keys.sql:44-71 (4 flat
bootstrap_block_size.{tf} keys) + 151-154 (config_state 4 parallel rows).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seed 4 per-tf bootstrap-thread APR keys and assemble them into a dict</name>
  <files>production/migrations/249_ic_cross_sectional_bootstrap_threads_per_tf.sql, services/ic_engine.py, tests/unit/test_ic_engine_parallelism.py</files>
  <behavior>
    - ICEngineConfig.cross_sectional_bootstrap_threads is a dict[str,int] keyed by tf, not an int
    - from_apr() assembles it from alpha.ic.cross_sectional_bootstrap_threads.{5m,15m,1h,1d}
    - Absent keys fall back to defaults (5m threaded, 15m/1h/1d serial=1) so behavior is safe pre-migration
  </behavior>
  <action>
    Author `production/migrations/249_ic_cross_sectional_bootstrap_threads_per_tf.sql` following
    migration 157's per-tf flat-key pattern (config_schema + config_state ON CONFLICT DO NOTHING +
    config_history WHERE NOT EXISTS triple): seed `alpha.ic.cross_sectional_bootstrap_threads.5m` (e.g.
    6, `[initial_estimate]` — 5m cross-sectional cells are the largest and benefit from threading),
    `.15m` / `.1h` / `.1d` (each 1, `[conventional]` — these finish in minutes serially; threading only
    adds dispatch overhead). Note in the descriptions this is the cross-sectional pass ONLY; the
    per-symbol path already runs inside an n_workers pool and must never thread on top. Re-check
    `ls production/migrations/ | sort -t_ -k1 -n | tail -3` before writing; increment past any 248+ that
    landed. Change the dataclass field to `cross_sectional_bootstrap_threads: dict[str, int]` with a
    `dataclasses.field(default_factory=...)` default of `{"5m":6,"15m":1,"1h":1,"1d":1}` (or the chosen
    seeds). In `from_apr()`, replace the scalar bind with the dict-comprehension form mirroring
    `bootstrap_block_size` (loop over `(("5m",6),("15m",1),("1h",1),("1d",1))`, reading
    `alpha.ic.cross_sectional_bootstrap_threads.{tf}`). Retire the old scalar key read
    `infra.ic_engine.cross_sectional_bootstrap_threads` (migrate-as-you-go — do not leave a dead scalar
    read). Extend `test_ic_engine_parallelism.py` with a case asserting `from_apr()` (fed a stub cfg
    returning per-tf values) produces a `{5m,15m,1h,1d}` dict, not a scalar. Also fix that file's stale
    module docstring line ("replaced by Fisher z-transform CI") since you are editing it — the circular
    block bootstrap is what is live.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_ic_engine_parallelism.py tests/unit/test_ic_engine_compute_split.py -x -q</automated>
  </verify>
  <done>cross_sectional_bootstrap_threads is a per-tf dict; migration 249 seeds 4 flat keys; old scalar key read removed; parallelism test asserts dict shape; stale docstring fixed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Index the thread dict by tf at the bootstrap CI call site (output unchanged)</name>
  <files>services/ic_engine.py, tests/unit/test_ic_engine_parallelism.py</files>
  <behavior>
    - The _circular_block_bootstrap_ic call in _compute_one_cross_sectional_cell passes max_workers=config.cross_sectional_bootstrap_threads[tf]
    - Bootstrap output (CI bounds) is identical regardless of thread count, because resample block-start indices are precomputed once per scale (162-01 invariant)
  </behavior>
  <action>
    In the extracted `_compute_one_cross_sectional_cell`, change the `_circular_block_bootstrap_ic(...)`
    invocation's `max_workers=config.cross_sectional_bootstrap_threads` to
    `max_workers=config.cross_sectional_bootstrap_threads[tf]`, matching `config.bootstrap_block_size[tf]`'s
    subscript one argument over. Confirm no per-symbol-path call site indexes this dict (per-symbol bootstrap
    must stay serial=1 inside the worker pool). Add a parallelism test asserting the call site reads the dict
    by `tf` (inspect.getsource: `cross_sectional_bootstrap_threads[tf]` present in the cross-sectional cell
    fn, absent from the per-symbol cell fn). Thread-count-does-not-change-output is guaranteed structurally by
    162-01's precomputed resample-index matrix — do not re-derive it here, just cite it.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_ic_engine_parallelism.py tests/unit/test_ic_engine_idempotency.py -x -q</automated>
  </verify>
  <done>Cross-sectional call site indexes the thread dict by tf; per-symbol path untouched (serial); test confirms subscript placement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cross-sectional bootstrap thread pool | intra-process ThreadPoolExecutor; compute-only; no data crosses a trust boundary |

Internal batch service — no HTTP/auth/session/injection surface. ASVS web categories **N/A** (no
attacker-reachable boundary). Relevant class is data-integrity.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-02-01 | Tampering | thread count silently changing bootstrap CI output | mitigate | Resample block-start index matrix precomputed once per scale (162-01) makes output thread-count-invariant by construction; idempotency test guards it |
| T-162-02-02 | Elevation of Privilege | per-tf thread key accidentally applied to the per-symbol pool (core oversubscription) | mitigate | Only the cross-sectional cell fn subscripts the dict; test asserts per-symbol fn does not |
| T-162-02-SC | Tampering | migration 249 number collision | mitigate | Re-check next-free migration number at write time |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/ -q` green.
- Benchmark (15m/1h/1d at max_workers=1 vs 6 against the post-162-01 loop) is a resource-contention
  gated ops measurement (Risk 5) — run only with `ps aux | grep ic_engine` clear; it informs the seed
  values but is NOT a unit test.
</verification>

<success_criteria>
- SC-5: 15m/1h/1d cross-sectional cells run within ~10% of measured serial wall time; 5m keeps its
  threading speedup; output identical across thread counts.
</success_criteria>

<output>
Create `.planning/phases/162-ic-engine-corpus-pipeline-throughput-incremental-recompute-t/162-02-SUMMARY.md` when done.
</output>
