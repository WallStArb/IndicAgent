---
phase: 093-mathematical-correctness-audit
plan: 05
type: execute
wave: 3
depends_on: ["093-02", "093-03", "093-04"]
files_modified:
  - tests/unit/intelligence/correctness/test_edge_cases.py
  - tests/unit/intelligence/correctness/test_numerical_stability.py
  - pytest.ini
  - .planning/phases/093-mathematical-correctness-audit/093-CI-GATE.md
autonomous: true
requirements:
  - MATH-04
  - MATH-05

must_haves:
  truths:
    - "Edge cases for each Tier 1 plugin (gap up/down, zero volume, single bar, empty input) produce defined behavior with no crashes"
    - "Numerical stability tests run each plugin on 10,000 bars of synthetic data and assert all outputs are finite"
    - "The existing `pytest tests/unit/` command includes tests/unit/intelligence/correctness/; no new gate framework introduced"
    - "Full correctness suite (`pytest tests/unit/intelligence/correctness/`) passes with zero failures"
    - "No regression: pre-existing `pytest tests/unit/intelligence/` tests still pass"
  artifacts:
    - path: "tests/unit/intelligence/correctness/test_edge_cases.py"
      provides: "Edge case coverage: gap, zero volume, single bar, empty input"
      contains: "synthetic_ohlcv_gap"
    - path: "tests/unit/intelligence/correctness/test_numerical_stability.py"
      provides: "Long-run numerical stability (10K bars, all finite)"
      contains: "10_000"
    - path: ".planning/phases/093-mathematical-correctness-audit/093-CI-GATE.md"
      provides: "Documented CI gate: pytest testpaths already includes correctness/"
  key_links:
    - from: "pytest.ini"
      to: "tests/unit/intelligence/correctness/"
      via: "testpaths=tests (already covers the new directory)"
      pattern: "testpaths.*=.*tests"
---

<objective>
Close the remaining gaps in Phase 093:

1. Edge case coverage — every Tier 1 plugin must handle gap up/down, zero-volume bars, single-bar input, and empty input gracefully (no crash, defined output).
2. Numerical stability — drive each plugin on 10,000 bars of synthetic data; assert every output is finite, no overflow, no underflow.
3. CI gate confirmation — verify the existing pytest configuration (testpaths=tests) already picks up tests/unit/intelligence/correctness/; document this and confirm no new gate framework is required.

This plan depends on 093-02, 093-03, and 093-04 because the full correctness suite must already pass before the CI gate is locked in.

Purpose: Renaissance principle — automation over manual sign-off. The correctness tests must run on every push, no human "looks right" gate.

Output: 2 new test files, pytest.ini reviewed (no change expected), 1 CI gate confirmation doc.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/093-mathematical-correctness-audit/093-CONTEXT.md
@pytest.ini
@tests/unit/intelligence/correctness/conftest.py
@src/intelligence/features/i1_indicators/atr.py
@src/intelligence/features/i1_indicators/bollinger.py
@src/intelligence/context/kalman_trend.py
@src/intelligence/context/garch_volatility.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edge case coverage for Tier 1 plugins</name>
  <files>
    tests/unit/intelligence/correctness/test_edge_cases.py
  </files>
  <read_first>
    tests/unit/intelligence/correctness/conftest.py
    src/intelligence/features/i1_indicators/atr.py
    src/intelligence/features/i1_indicators/bollinger.py
    src/intelligence/features/i1_indicators/mfi.py
    src/intelligence/context/kalman_trend.py
    src/intelligence/context/garch_volatility.py
  </read_first>
  <action>
    Create `test_edge_cases.py` with class `TestEdgeCases` containing parametrized tests covering edge fixtures from conftest:

    - `test_plugins_handle_gap(synthetic_ohlcv_gap)` — parametrize over each Tier 1 plugin (ATR, Bollinger, MACD, RSI, Stochastic, MFI, CCI, Williams R, ADX, VWAP, Kalman, GARCH). For each: drive compute_full on the 200-bar gap fixture. Assert no exception raised, all numeric outputs are finite (math.isfinite), and the bar immediately after the gap produces a defined output (key exists, value is not None and is finite). Use `@pytest.mark.parametrize` with a list of (plugin_class, output_keys) tuples.

    - `test_plugins_handle_zero_volume(synthetic_ohlcv_zero_volume)` — volume-dependent plugins (MFI, VWAP, VolumeZscore) must NOT crash and must produce a finite value or a documented sentinel (0.0 or NaN). Non-volume plugins are unaffected — exclude them from this test. Read each plugin to determine its zero-volume behavior, then assert that exact behavior.

    - `test_plugins_handle_single_bar(synthetic_ohlcv_single_bar)` — every plugin must return an empty dict `{}` or a dict where output keys are absent (because lookback is insufficient). Assert no exception raised. The contract: insufficient data -> empty/missing outputs, NEVER crash.

    - `test_plugins_handle_empty_frames()` — pass `{"main": pd.DataFrame(columns=["open","high","low","close","volume"])}` to each plugin's compute_full. Assert it returns `{}` and does not crash.

    Build a module-level constant `TIER1_PLUGINS: list[tuple[type, list[str]]]` mapping each plugin class to its expected output keys. This makes the parametrize concise and gives a single source of truth for edge-case coverage.

    Read each plugin file before parametrizing to determine the actual output key names (e.g., `atr_14`, `bb_20_2_upper`, etc.).
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/correctness/test_edge_cases.py -v` all tests pass.
    `grep -c "pytest.mark.parametrize" tests/unit/intelligence/correctness/test_edge_cases.py` returns at least 1.
  </verify>
  <acceptance_criteria>
    - File `tests/unit/intelligence/correctness/test_edge_cases.py` exists
    - Defines class `TestEdgeCases` with at least 4 test methods named: `test_plugins_handle_gap`, `test_plugins_handle_zero_volume`, `test_plugins_handle_single_bar`, `test_plugins_handle_empty_frames`
    - Defines module-level constant `TIER1_PLUGINS` as a list with at least 10 entries
    - File uses `@pytest.mark.parametrize` at least once
    - `.venv/bin/pytest tests/unit/intelligence/correctness/test_edge_cases.py -v` exits 0
  </acceptance_criteria>
  <done>
    Every Tier 1 plugin's behavior under gap, zero volume, single bar, and empty input is asserted by an automated test. No plugin can regress to crashing on these inputs without the test failing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Numerical stability tests + CI gate confirmation</name>
  <files>
    tests/unit/intelligence/correctness/test_numerical_stability.py
    .planning/phases/093-mathematical-correctness-audit/093-CI-GATE.md
    pytest.ini
  </files>
  <read_first>
    pytest.ini
    tests/unit/intelligence/correctness/conftest.py
    src/intelligence/context/garch_volatility.py
    src/intelligence/context/kalman_trend.py
  </read_first>
  <action>
    Create `test_numerical_stability.py` with class `TestNumericalStability`:

    - Module-level fixture or helper: `long_run_ohlcv` — 10_000-bar deterministic synthetic OHLCV (trending with noise, seed 99). Either add to conftest.py OR define locally in this file with @pytest.fixture(scope="module") to avoid memory blowing up across the suite.

    - `test_atr_stable_over_10k_bars(long_run_ohlcv)` — drive ATRPlugin compute_full once on full 10K bars; assert every value of atr_14 in the output series (collected by sliding-window compute_full or by incremental compute_next over the series) is finite and positive.

    - `test_kalman_stable_over_10k_bars(long_run_ohlcv)` — drive KalmanTrendPlugin compute_full at bar 100, then compute_next bar-by-bar to bar 10_000; assert every output is finite, P_est stays positive, no NaN/Inf in state at any step.

    - `test_garch_stable_over_10k_bars(long_run_ohlcv)` — drive GARCHVolatilityPlugin compute_full then compute_next to bar 10_000; assert garch_sigma stays in (1e-12, 1e6) and is finite at every step.

    - `test_bollinger_stable_over_10k_bars(long_run_ohlcv)` — drive BollingerPlugin to bar 10_000; assert upper >= mid >= lower at every bar, all values finite.

    Mark this test class with `@pytest.mark.slow` so it can be skipped in fast CI runs if needed via `-m "not slow"` (the marker is already declared in pytest.ini line 18).

    Now confirm the CI gate: read pytest.ini and verify line `testpaths = tests` already causes tests/unit/intelligence/correctness/ to be picked up by the standard `pytest tests/unit/` invocation. Run `.venv/bin/pytest tests/unit/intelligence/correctness/ --collect-only -q` and `.venv/bin/pytest tests/unit/ --collect-only -q | grep "correctness/"` to confirm the suite is discovered under the existing testpaths configuration.

    Create `.planning/phases/093-mathematical-correctness-audit/093-CI-GATE.md` documenting:
    - The pytest.ini config: `testpaths = tests` already discovers the new correctness/ directory.
    - The CI command unchanged: `.venv/bin/pytest tests/unit/ -v` runs the full correctness suite by default.
    - To skip slow numerical stability tests in fast runs: `.venv/bin/pytest tests/unit/ -m "not slow"`.
    - To run ONLY correctness tests: `.venv/bin/pytest tests/unit/intelligence/correctness/ -v`.
    - Confirm: NO new pytest.ini change required. The CI gate is already in effect.

    Do NOT modify pytest.ini unless the discover-only check above shows the directory is missed; in that case, append `tests/unit/intelligence/correctness` to testpaths.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/correctness/test_numerical_stability.py -v` all tests pass.
    `.venv/bin/pytest tests/unit/intelligence/correctness/ -v` full correctness suite passes.
    `.venv/bin/pytest tests/unit/ --collect-only -q 2>&1 | grep -c "tests/unit/intelligence/correctness/"` returns at least 1.
    `.venv/bin/pytest tests/unit/intelligence/ -v` existing tests still pass (no regression).
    `cat .planning/phases/093-mathematical-correctness-audit/093-CI-GATE.md` shows the documented gate.
  </verify>
  <acceptance_criteria>
    - File `tests/unit/intelligence/correctness/test_numerical_stability.py` exists
    - Defines class `TestNumericalStability` decorated or marked with `@pytest.mark.slow`
    - Contains the integer literal `10_000` or `10000` at least once
    - Defines at least 4 test methods covering ATR, Kalman, GARCH, Bollinger
    - File `.planning/phases/093-mathematical-correctness-audit/093-CI-GATE.md` exists and contains the strings "testpaths", "pytest tests/unit/", and "correctness/"
    - `.venv/bin/pytest tests/unit/ --collect-only -q` output contains at least one line matching `tests/unit/intelligence/correctness/`
    - `.venv/bin/pytest tests/unit/intelligence/correctness/ -v` exits 0
    - `.venv/bin/pytest tests/unit/intelligence/ --ignore=tests/unit/intelligence/correctness -v` exits 0 (no regression in pre-existing tests)
  </acceptance_criteria>
  <done>
    Every Tier 1 plugin is proven numerically stable over 10K bars. The CI gate is confirmed in place via the existing pytest testpaths configuration. The standard `pytest tests/unit/` command runs the correctness suite, blocking merges on failure.
  </done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/intelligence/correctness/ -v` exits 0 (full phase suite passes)
- `.venv/bin/pytest tests/unit/intelligence/ -v` exits 0 (no regression in pre-existing tests)
- `.venv/bin/pytest tests/unit/ --collect-only -q | grep -c "tests/unit/intelligence/correctness/"` returns at least 10 (one per test file)
- `.planning/phases/093-mathematical-correctness-audit/093-CI-GATE.md` exists and documents the gate
</verification>

<success_criteria>
Phase 093 is complete: every Tier 1 indicator has reference validation, Kalman/GARCH have invariant tests, edge cases are covered, numerical stability is enforced over 10K bars, and the CI gate is in effect via the existing pytest configuration. The intelligence pipeline's mathematical correctness is proven, not asserted.
</success_criteria>

<output>
After completion, create `.planning/phases/093-mathematical-correctness-audit/093-05-SUMMARY.md`
</output>
