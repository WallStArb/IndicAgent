---
phase: 093-mathematical-correctness-audit
plan: 03
type: execute
wave: 2
depends_on: ["093-01"]
files_modified:
  - tests/unit/intelligence/correctness/test_kalman_invariants.py
  - tests/unit/intelligence/correctness/test_garch_invariants.py
autonomous: true
requirements:
  - MATH-03

must_haves:
  truths:
    - "Kalman filter invariants are enforced by tests: P_est > 0 always, K in (0, 1) always, no NaN/Inf in state across 500 bars"
    - "GARCH invariants are enforced by tests: sigma2 > 0 always, no overflow/underflow, convergence to long-run variance on flat data"
    - "Incremental state updates equal full recomputation on the same input window for both Kalman and GARCH"
    - "Tests pass under `pytest tests/unit/intelligence/correctness/test_kalman_invariants.py tests/unit/intelligence/correctness/test_garch_invariants.py`"
  artifacts:
    - path: "tests/unit/intelligence/correctness/test_kalman_invariants.py"
      provides: "Kalman invariant + incremental equivalence tests"
      contains: "KalmanTrend"
    - path: "tests/unit/intelligence/correctness/test_garch_invariants.py"
      provides: "GARCH invariant + long-run variance convergence tests"
      contains: "GARCH"
  key_links:
    - from: "test_kalman_invariants.py"
      to: "src/intelligence/context/kalman_trend.py"
      via: "from src.intelligence.context.kalman_trend import"
      pattern: "from src.intelligence.context.kalman_trend"
    - from: "test_garch_invariants.py"
      to: "src/intelligence/context/garch_volatility.py"
      via: "from src.intelligence.context.garch_volatility import"
      pattern: "from src.intelligence.context.garch_volatility"
---

<objective>
Author invariant tests for the two stateful Tier 1 computations: Kalman trend filter and GARCH(1,1) volatility. These tests must enforce in code (not docs) the mathematical contracts that make these computations stable: positivity of variance/covariance estimates, bounded Kalman gain, no NaN/Inf propagation, and convergence to long-run variance on flat data.

Additionally, each plugin's compute_next (incremental) must produce identical results to compute_full when given the same input window. This invariant catches state-corruption bugs like the ones fixed pre-phase.

Purpose: Renaissance principle — invariants are invariants, enforced in code. The pre-phase state-init crashes proved that stateful code is the highest-risk surface. Encode every algebraic precondition as a test.

Output: Two invariant test files. All tests pass.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/093-mathematical-correctness-audit/093-CONTEXT.md
@tests/unit/intelligence/correctness/conftest.py
@src/intelligence/context/kalman_trend.py
@src/intelligence/context/garch_volatility.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kalman trend invariant tests</name>
  <files>
    tests/unit/intelligence/correctness/test_kalman_invariants.py
  </files>
  <read_first>
    src/intelligence/context/kalman_trend.py
    tests/unit/intelligence/correctness/conftest.py
  </read_first>
  <action>
    Create `test_kalman_invariants.py` with class `TestKalmanInvariants` containing these test methods:

    - `test_kalman_covariance_strictly_positive(synthetic_ohlcv_trending)` — drive KalmanTrendPlugin compute_full then iterate compute_next bar-by-bar over 500 bars. After each call, inspect returned `_state` and assert `state["P_est"] > 0.0` (or whatever the covariance key is named in kalman_trend.py — read the file to find the actual key). Use python's `assert math.isfinite(P_est)` to catch NaN/Inf.

    - `test_kalman_gain_bounded(synthetic_ohlcv_trending)` — after each compute_next call, assert Kalman gain K satisfies `0.0 < K < 1.0`. If the plugin doesn't expose K in state, compute it from R and P in the test (read kalman_trend.py to determine the formula).

    - `test_kalman_no_nan_inf_in_outputs(synthetic_ohlcv_trending, synthetic_ohlcv_ranging, synthetic_ohlcv_gap)` — for each fixture, drive the plugin and assert every output value (level estimate, slope, etc.) is finite (`math.isfinite`). Parametrize via @pytest.mark.parametrize on the three fixture names using `request.getfixturevalue`.

    - `test_kalman_incremental_equals_full(synthetic_ohlcv_trending)` — compute_full on bars[0:100], then call compute_next bar-by-bar for bars[100:200] threading state. Separately call compute_full on bars[0:200]. Assert the final output values match within atol=1e-6 (use math.isclose per output key).

    - `test_kalman_slope_directionally_tracks_trend(synthetic_ohlcv_trending, synthetic_ohlcv_ranging)` — on trending data, the kalman slope on the last bar must be positive. On ranging data, the absolute slope must be smaller than on trending data (sanity check that slope responds to trend).

    Read kalman_trend.py first to determine: (a) the exact plugin class name, (b) the output key names for level/slope, (c) the state dict key names. Use the actual identifiers from source, not assumed names.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/correctness/test_kalman_invariants.py -v` all tests pass.
  </verify>
  <acceptance_criteria>
    - File `tests/unit/intelligence/correctness/test_kalman_invariants.py` exists
    - Defines class `TestKalmanInvariants` with at least 5 test methods named exactly: `test_kalman_covariance_strictly_positive`, `test_kalman_gain_bounded`, `test_kalman_no_nan_inf_in_outputs`, `test_kalman_incremental_equals_full`, `test_kalman_slope_directionally_tracks_trend`
    - File imports the Kalman plugin class from `src.intelligence.context.kalman_trend`
    - File uses `math.isfinite` or `numpy.isfinite` at least once
    - `.venv/bin/pytest tests/unit/intelligence/correctness/test_kalman_invariants.py -v` exits 0
  </acceptance_criteria>
  <done>
    Kalman filter's mathematical invariants are enforced by automated tests covering 500 bars across three fixture scenarios. Incremental state evolution is proven equivalent to full recomputation.
  </done>
</task>

<task type="auto">
  <name>Task 2: GARCH(1,1) invariant tests</name>
  <files>
    tests/unit/intelligence/correctness/test_garch_invariants.py
  </files>
  <read_first>
    src/intelligence/context/garch_volatility.py
    tests/unit/intelligence/correctness/conftest.py
  </read_first>
  <action>
    Create `test_garch_invariants.py` with class `TestGARCHInvariants` containing these test methods:

    - `test_garch_sigma2_strictly_positive(synthetic_ohlcv_trending)` — drive GARCHVolatilityPlugin compute_full then compute_next across 500 bars; after each call assert `state["prev_sigma2"] > 0.0` and finite.

    - `test_garch_no_overflow_on_long_run(synthetic_ohlcv_trending, synthetic_ohlcv_ranging)` — parametrize over both fixtures. Drive 500 bars and assert all garch_sigma outputs are finite and below a sane upper bound (e.g., 1e6) — protects against unbounded growth.

    - `test_garch_no_underflow_on_flat_data(synthetic_ohlcv_flat)` — flat-price data has zero realized returns. Drive 500 bars; assert sigma2 stays >= a small positive floor (e.g., 1e-12) and never becomes exactly zero or negative.

    - `test_garch_converges_to_long_run_variance(synthetic_ohlcv_flat)` — on perfectly flat data (zero returns), GARCH sigma2 must converge toward the long-run variance `omega / (1 - alpha - beta)`. Read garch_volatility.py to find omega, alpha, beta parameters. After 500 bars, assert `abs(sigma2_final - long_run_var) / long_run_var < 0.05` (within 5%).

    - `test_garch_shock_uses_prior_sigma2(synthetic_ohlcv_trending)` — verify the shock = epsilon^2 / sigma2_prior invariant by reading two consecutive bars and re-deriving shock from outputs/state; assert match to atol=1e-9.

    - `test_garch_incremental_equals_full(synthetic_ohlcv_trending)` — same pattern as Kalman task: compute_full on [0:100], then compute_next iteratively to bar 200, vs compute_full on [0:200]; assert garch_sigma final value matches to atol=1e-6.

    Read garch_volatility.py first to determine: (a) the exact plugin class name, (b) state key names (likely `prev_sigma2`), (c) output key names (likely `garch_sigma`, `garch_shock`), (d) the alpha/beta/omega parameter names and values.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/correctness/test_garch_invariants.py -v` all tests pass.
  </verify>
  <acceptance_criteria>
    - File `tests/unit/intelligence/correctness/test_garch_invariants.py` exists
    - Defines class `TestGARCHInvariants` with at least 6 test methods named exactly: `test_garch_sigma2_strictly_positive`, `test_garch_no_overflow_on_long_run`, `test_garch_no_underflow_on_flat_data`, `test_garch_converges_to_long_run_variance`, `test_garch_shock_uses_prior_sigma2`, `test_garch_incremental_equals_full`
    - File imports the GARCH plugin class from `src.intelligence.context.garch_volatility`
    - The convergence test references the formula `omega / (1 - alpha - beta)` (these tokens must appear in the file)
    - `.venv/bin/pytest tests/unit/intelligence/correctness/test_garch_invariants.py -v` exits 0
  </acceptance_criteria>
  <done>
    GARCH(1,1) invariants are enforced by tests: strict positivity, no overflow, no underflow, long-run variance convergence on flat data, unbiased shock formula, and incremental-equals-full equivalence.
  </done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/intelligence/correctness/test_kalman_invariants.py tests/unit/intelligence/correctness/test_garch_invariants.py -v` exits 0
- Both test files reference `math.isfinite` or `numpy.isfinite`
- Both files have an incremental_equals_full test
</verification>

<success_criteria>
Kalman and GARCH stateful computations have enforced mathematical invariants in test code. Positivity, finiteness, bounded gain, long-run variance convergence, and incremental/full equivalence are all proven. State-corruption regressions will be caught at test time.
</success_criteria>

<output>
After completion, create `.planning/phases/093-mathematical-correctness-audit/093-03-SUMMARY.md`
</output>
