---
phase: 093-mathematical-correctness-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - tests/unit/intelligence/correctness/__init__.py
  - tests/unit/intelligence/correctness/conftest.py
  - tests/unit/intelligence/correctness/README.md
autonomous: true
requirements:
  - MATH-05

must_haves:
  truths:
    - "pandas-ta is installed in the project venv as a dev dependency"
    - "tests/unit/intelligence/correctness/ exists as a Python package"
    - "A shared conftest.py provides synthetic OHLCV fixtures and a reference-validation helper"
    - "pytest collects tests under tests/unit/intelligence/correctness/ without errors"
  artifacts:
    - path: "tests/unit/intelligence/correctness/__init__.py"
      provides: "Python package marker"
    - path: "tests/unit/intelligence/correctness/conftest.py"
      provides: "Synthetic OHLCV fixtures + reference comparison helper (assert_close_to_reference)"
      contains: "def assert_close_to_reference"
    - path: "requirements.txt"
      provides: "pandas-ta as test-only dependency"
      contains: "pandas-ta"
  key_links:
    - from: "tests/unit/intelligence/correctness/conftest.py"
      to: "pandas_ta"
      via: "import pandas_ta as pta"
      pattern: "import pandas_ta"
    - from: "pytest collection"
      to: "tests/unit/intelligence/correctness/"
      via: "pytest.ini testpaths=tests"
      pattern: "testpaths.*tests"
---

<objective>
Establish the test infrastructure for mathematical correctness validation: install pandas-ta as a test dependency, create the tests/unit/intelligence/correctness/ package, and provide shared fixtures (synthetic OHLCV + historical snapshot loader) plus a reference-comparison helper that enforces the project tolerance (max absolute error < 1e-6, directional agreement > 99.9%).

Purpose: Renaissance principle — every Tier 1 computation must be provable against a reference implementation. This plan builds the scaffolding all downstream correctness tests will use. Without it, plans 02 and 03 cannot run.

Output: A correctness/ test package collectable by pytest, a conftest.py that exposes OHLCV fixtures and the assert_close_to_reference helper, and pandas-ta available in the venv.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/093-mathematical-correctness-audit/093-CONTEXT.md
@.planning/phases/093-mathematical-correctness-audit/093-00-RESEARCH.md
@pytest.ini
@requirements.txt
@tests/unit/intelligence/helpers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pandas-ta and scaffold correctness package</name>
  <files>
    requirements.txt
    tests/unit/intelligence/correctness/__init__.py
    tests/unit/intelligence/correctness/README.md
  </files>
  <read_first>
    requirements.txt
    pytest.ini
    tests/unit/intelligence/__init__.py
  </read_first>
  <action>
    Add pandas-ta as a test-only dependency to requirements.txt under the "Development and Testing" section. Pin version pandas-ta>=0.3.14b0 (last stable). Install into the project venv via `.venv/bin/pip install pandas-ta>=0.3.14b0`.

    Create empty `tests/unit/intelligence/correctness/__init__.py` to make it a Python package (pytest already discovers via testpaths=tests in pytest.ini, no pytest.ini change needed).

    Create `tests/unit/intelligence/correctness/README.md` describing: (1) purpose — mathematical correctness validation vs reference, (2) tolerance contract — max abs error < 1e-6, directional agreement > 99.9%, (3) file layout — one test_<indicator>.py per plugin, (4) how to add a new correctness test, (5) reference library = pandas-ta.
  </action>
  <verify>
    `.venv/bin/python -c "import pandas_ta; print(pandas_ta.__version__)"` prints a version string.
    `ls tests/unit/intelligence/correctness/__init__.py tests/unit/intelligence/correctness/README.md` shows both files.
    `grep -q "pandas-ta" requirements.txt` exits 0.
  </verify>
  <acceptance_criteria>
    - `requirements.txt` contains a line matching `pandas-ta>=0.3.14b0`
    - `tests/unit/intelligence/correctness/__init__.py` exists (may be empty)
    - `tests/unit/intelligence/correctness/README.md` exists and references the strings "1e-6", "99.9", and "pandas-ta"
    - `.venv/bin/python -c "import pandas_ta"` exits 0
    - `.venv/bin/pytest tests/unit/intelligence/correctness/ --collect-only` exits 0 (no tests yet, but no collection errors)
  </acceptance_criteria>
  <done>
    pandas-ta is importable in the venv, the correctness package exists and is collectible by pytest, and the README documents the contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author shared conftest.py with OHLCV fixtures and reference helper</name>
  <files>
    tests/unit/intelligence/correctness/conftest.py
  </files>
  <read_first>
    tests/unit/intelligence/helpers.py
    tests/unit/intelligence/correctness/__init__.py
    src/intelligence/features/i1_indicators/atr.py
  </read_first>
  <action>
    Create `tests/unit/intelligence/correctness/conftest.py` exposing these pytest fixtures and helpers:

    Fixtures (all session-scoped where possible):
    - `synthetic_ohlcv_trending` — 500-bar deterministic upward-trend OHLCV DataFrame with columns [open, high, low, close, volume]; seed numpy with 42; returns pandas.DataFrame indexed by UTC DatetimeIndex at 1-minute frequency.
    - `synthetic_ohlcv_ranging` — 500-bar mean-reverting OHLCV around a constant price (sine wave + noise), same schema, seed 43.
    - `synthetic_ohlcv_gap` — 200-bar OHLCV with a 5% gap up at bar index 100 (open[100] != close[99]).
    - `synthetic_ohlcv_zero_volume` — 100-bar OHLCV where volume column is all zeros.
    - `synthetic_ohlcv_single_bar` — 1-row OHLCV DataFrame.
    - `synthetic_ohlcv_flat` — 500-bar OHLCV where high=low=close=open=100.0, constant volume (for GARCH long-run variance convergence).

    Helper function (NOT a fixture, importable):
    - `assert_close_to_reference(ours: pd.Series | np.ndarray, reference: pd.Series | np.ndarray, *, atol: float = 1e-6, directional_min: float = 0.999, name: str = "") -> None` — aligns the two arrays by dropping leading NaN warm-up bars from both, asserts max absolute error <= atol, computes sign agreement on diffs (ours.diff().apply(sign) vs reference.diff().apply(sign)) and asserts >= directional_min. Raise AssertionError with a clear diagnostic string including max_err, agreement, and name on failure.

    Helper function:
    - `frames_from_ohlcv(df: pd.DataFrame) -> dict[str, pd.DataFrame]` — returns `{"main": df}` for the plugin protocol (compute_full takes frames dict).

    All synthetic fixtures must produce valid OHLC (high >= max(open, close), low <= min(open, close)).

    Do NOT add a fixture that hits the live database; correctness tests run with synthetic and fixture data only.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/correctness/ --collect-only` exits 0 with no errors.
    `.venv/bin/python -c "from tests.unit.intelligence.correctness.conftest import assert_close_to_reference, frames_from_ohlcv; print('ok')"` prints `ok`.
  </verify>
  <acceptance_criteria>
    - File `tests/unit/intelligence/correctness/conftest.py` exists
    - Defines six pytest fixtures named exactly: `synthetic_ohlcv_trending`, `synthetic_ohlcv_ranging`, `synthetic_ohlcv_gap`, `synthetic_ohlcv_zero_volume`, `synthetic_ohlcv_single_bar`, `synthetic_ohlcv_flat`
    - Defines function `assert_close_to_reference` with signature accepting (ours, reference, atol, directional_min, name) and raising AssertionError on tolerance breach
    - Defines function `frames_from_ohlcv` returning a dict keyed by `"main"`
    - All OHLCV fixtures return pandas.DataFrame with columns `["open", "high", "low", "close", "volume"]`
    - `.venv/bin/pytest tests/unit/intelligence/correctness/ --collect-only` exits 0
  </acceptance_criteria>
  <done>
    Shared conftest provides deterministic OHLCV fixtures covering normal, edge, and convergence scenarios plus a tolerance-enforcing reference helper. All downstream correctness tests can import from it.
  </done>
</task>

</tasks>

<verification>
- `.venv/bin/python -c "import pandas_ta"` succeeds
- `.venv/bin/pytest tests/unit/intelligence/correctness/ --collect-only` exits 0
- `grep -c "def " tests/unit/intelligence/correctness/conftest.py` returns at least 8 (6 fixtures + 2 helpers)
</verification>

<success_criteria>
Test infrastructure for mathematical correctness exists: pandas-ta installed, correctness/ package created, conftest.py exposes synthetic OHLCV fixtures and assert_close_to_reference helper. Plans 093-02 and 093-03 can now author correctness tests by importing from this conftest.
</success_criteria>

<output>
After completion, create `.planning/phases/093-mathematical-correctness-audit/093-01-SUMMARY.md`
</output>
