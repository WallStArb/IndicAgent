---
phase: 104-storage-architecture-redesign
plan: 03
type: execute
wave: 2
depends_on: [104-01, 104-02]
files_modified:
  - db/migrations/092_rename_intelligence_feature_tiers.sql
  - db/migrations/093_slim_signal_ledger.sql
  - services/feature_writer_agent.py
  - services/signal_writer_agent.py
  - src/persistence/repository/feature_repository.py
  - src/persistence/repository/feature_snapshot_repository.py
  - src/persistence/repository/signal_ledger_repository.py
  - src/persistence/logic/stream_merger.py
  - src/persistence/logic/warmup_provider.py
  - src/core/bar_history_seeder.py
  - src/core/ai/context.py
  - src/core/ai/base_group_service.py
  - src/core/ml/extractor.py
  - src/core/ml/features.py
  - src/core/ml/training_data.py
  - src/intelligence/ml/feature_builder.py
  - src/intelligence/pipeline/signal_processor.py
  - src/intelligence/pipeline/feature_pipeline_executor.py
  - src/intelligence/services/feature_validation_compute_agent.py
  - src/intelligence/services/hmm_training_compute_agent.py
  - src/intelligence/trading/zone_engine.py
  - src/intelligence/confluence/cross_timeframe.py
  - src/validation/cross_tier_validation.py
  - src/validation/validation_engine.py
  - src/core/schemas/parity.py
  - src/api/routes/signals.py
  - src/api/routes/features.py
  - src/api/routes/sse.py
  - src/api/routes/narrative.py
  - services/signal_tracker_compute_agent.py
  - services/lifecycle_writer_agent.py
  - services/signal_auditor_agent.py
  - services/signal_metrics_compute_agent.py
  - src/intelligence/metrics/compute.py
autonomous: true

must_haves:
  truths:
    - "intelligence_features has columns technical_indicators, market_context, pattern_detections, regime_features, confluence_scores, cross_timeframe_context, trading_signals, llm_narrative (smc unchanged)"
    - "intelligence_features no longer has columns i1, i2, i3, i4, i5, i6, i7, i8"
    - "signal_ledger has approximately 38 columns (down from 97); all fire-time duplicate columns dropped"
    - "signal_writer_agent inserts batches successfully with the slim LedgerEntry schema"
    - "feature_writer_agent inserts feature vectors using the renamed columns"
    - "Dashboard API /signals route returns fire-time fields by LATERAL-joining intelligence_features.trading_signals"
    - "ML training pipeline (feature_builder._TRAINING_SQL) uses renamed columns and continues producing training matrix without error"
    - "All services in the read path (signal_tracker, lifecycle, signal_auditor, signal_metrics) restart cleanly with the new schema"
  artifacts:
    - path: "db/migrations/092_rename_intelligence_feature_tiers.sql"
      provides: "8 ALTER TABLE RENAME COLUMN statements for i1..i8"
      contains: "RENAME COLUMN i1 TO technical_indicators"
    - path: "db/migrations/093_slim_signal_ledger.sql"
      provides: "ALTER TABLE DROP COLUMN statements for ~47 fire-time duplicate columns"
      contains: "ALTER TABLE signal_ledger DROP COLUMN"
    - path: "src/persistence/repository/signal_ledger_repository.py"
      provides: "Slim LedgerEntry dataclass with ~38 fields; updated _INSERT_SQL"
      contains: "class LedgerEntry"
    - path: "src/api/routes/signals.py"
      provides: "LATERAL jsonb_array_elements JOIN to trading_signals for fire-time fields"
      contains: "jsonb_array_elements"
  key_links:
    - from: "feature_writer_agent INSERT"
      to: "intelligence_features renamed columns"
      via: "_INSERT_FEATURE_SQL"
      pattern: "technical_indicators.*market_context.*pattern_detections.*regime_features.*confluence_scores.*cross_timeframe_context.*trading_signals"
    - from: "signal_writer_agent LedgerEntry"
      to: "signal_ledger slim columns"
      via: "_INSERT_SQL parameter list"
      pattern: "INSERT INTO signal_ledger"
    - from: "src/api/routes/signals.py get_active_signals"
      to: "intelligence_features.trading_signals JSONB"
      via: "LEFT JOIN LATERAL jsonb_array_elements"
      pattern: "LATERAL jsonb_array_elements"
    - from: "src/intelligence/ml/feature_builder.py _TRAINING_SQL"
      to: "renamed columns"
      via: "f.regime_features->>'hmm_regime', f.technical_indicators->>'atr_pct', f.trading_signals->0"
      pattern: "f\\.regime_features.*f\\.technical_indicators.*f\\.trading_signals"
---

<objective>
Execute the binding maintenance window: in a single coordinated outage, rename intelligence_features
tier columns (i1..i8 -> concept names) and drop ~47 fire-time duplicate columns from signal_ledger.
Update every read/write callsite atomically so services start cleanly with the new schema.

Why one window: per RESEARCH.md line 23 and PATTERNS.md "Critical Sequencing Notes" — services that
INSERT to intelligence_features must be stopped before the DDL and restarted after code deploy.
Doing rename and slim together amortizes one maintenance window. Python attribute names i1..i8 on
BarIntelligenceRecord are NOT changed (in-memory only) — only SQL column names change.

Purpose: Single source of truth — fire-time signal data lives only in intelligence_features.trading_signals
JSONB; signal_ledger owns only lifecycle/outcome. Eliminates ~6 GB of duplicate writes over time.
Output: Renamed canonical feature table; slim signal_ledger; dashboard + ML pipeline updated to JOIN
for fire-time data; all writers/readers restarted on new schema.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/104-storage-architecture-redesign/104-CONTEXT.md
@.planning/phases/104-storage-architecture-redesign/104-RESEARCH.md
@.planning/phases/104-storage-architecture-redesign/104-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update write-path code (feature_writer + signal_writer + repositories) without restarting</name>
  <files>
    services/feature_writer_agent.py
    services/signal_writer_agent.py
    src/persistence/repository/feature_repository.py
    src/persistence/repository/feature_snapshot_repository.py
    src/persistence/repository/signal_ledger_repository.py
    src/persistence/logic/stream_merger.py
  </files>
  <read_first>
    - services/feature_writer_agent.py (current _INSERT_FEATURE_SQL lines ~63-93 and i7 UPSERT line ~101)
    - services/signal_writer_agent.py (_payload_to_ledger_entries lines ~156-280; _flush_batch lines ~125-136)
    - src/persistence/repository/signal_ledger_repository.py (LedgerEntry dataclass lines ~55-232; _INSERT_SQL lines ~240-296; _SELECT_ACTIVE_SQL; _SELECT_ACTIVE_BY_SYMBOL_SQL; fetch_active_signals)
    - src/persistence/repository/feature_repository.py (_INSERT_SQL_TEMPLATE referencing i1..i7)
    - src/persistence/repository/feature_snapshot_repository.py (get_recent_features SELECT)
    - src/persistence/logic/stream_merger.py (EXPECTED_TIERS frozenset)
    - .planning/phases/104-storage-architecture-redesign/104-PATTERNS.md (signal_ledger_repository.py section lists exact KEEP vs DROP columns; feature_writer_agent.py section lines 528-557 has exact SQL transformation)
    - .planning/phases/104-storage-architecture-redesign/104-RESEARCH.md (signal_ledger slim columns lines 55-85)
  </read_first>
  <action>
    Code changes only (no DDL, no service restarts yet — Task 2 does both atomically).
    Edit services/feature_writer_agent.py _INSERT_FEATURE_SQL: replace column names i1->technical_indicators, i2->market_context, i3->pattern_detections, i4->regime_features, i5->confluence_scores, i6->cross_timeframe_context, i7->trading_signals, i8->llm_narrative. smc stays as smc. The parameter positions $8..$15 do NOT change — only the SQL identifiers. Also update the i7 UPSERT clause "SET i7 = COALESCE(i7, '{}'::jsonb) || $4::jsonb" to "SET trading_signals = COALESCE(trading_signals, '{}'::jsonb) || $4::jsonb".
    Edit src/persistence/repository/feature_repository.py _INSERT_SQL_TEMPLATE: same column renames as above.
    Edit src/persistence/repository/feature_snapshot_repository.py get_recent_features() SELECT: rename i1..i6 column references to concept names.
    Edit src/persistence/logic/stream_merger.py EXPECTED_TIERS frozenset: this set contains the SQL column names — rename "i1".."i8" string members to concept names (verify whether this frozenset gates SQL or Python attribute names; if SQL, rename; if Python attrs, leave).
    Edit src/persistence/repository/signal_ledger_repository.py:
      LedgerEntry dataclass — delete the 47 dropped fields listed in PATTERNS.md lines 98-111 (entry_price, stop_loss, targets, confidence, confluence_score, regime_context, supporting_factors, num_signals_bar, num_agreeing, num_conflicting, resolution_method, composite_rank, market_context, cis_score, bucket_scores, weights_version, determined_at, ask_at_signal, bid_at_signal, market_price_at_signal, entry_zone_low, entry_zone_high, zone_valid_at_signal, cis_attribution, stop_basis, stop_structure_type, stop_structure_age_bars, structural_stop_distance_atr, hmm_regime_at_fire, garch_sigma_at_fire, chandelier_vol_source, trailing_stop_tightening_rate, raw_cis_score, filtered_cis_score, calibrated_confidence, regime_type_at_fire, pre_quality_confidence, pre_calibration_confidence, entry_type, co_fire_count, co_fire_partners, features_snapshot, adjusted_confidence, swarm_multiplier, swarm_agent_count). KEEP only the ~38 fields listed in PATTERNS.md lines 82-95.
      _INSERT_SQL — drop the column names and JSONB casts for dropped fields; renumber $N placeholders sequentially; preserve ON CONFLICT ... DO NOTHING.
      _to_row() — drop the matching positional values.
      _SELECT_ACTIVE_SQL and _SELECT_ACTIVE_BY_SYMBOL_SQL — drop SELECT references to dropped columns.
      UPDATE methods used by lifecycle_writer remain unchanged (only INSERT shrinks; UPDATE writes activated_at, exit_at, pnl_r etc which are kept).
    Edit services/signal_writer_agent.py _payload_to_ledger_entries(): delete all keyword arguments to LedgerEntry(...) corresponding to dropped fields (entry_price=, stop_loss=, targets=, confidence=, etc.). _parse_payload() and _flush_batch() unchanged.
    Do not run anything yet. Code is staged but DDL not applied; running services still expect old schema.
    Run unit tests with --no-cov to verify syntax + import correctness only: .venv/bin/pytest tests/unit/persistence tests/unit/service_tests -q --no-cov 2>&1 | tail -30 — failures expected if tests reference dropped columns; record failures for Task 3.
  </action>
  <verify>
    grep -c "technical_indicators\|market_context\|pattern_detections\|regime_features\|confluence_scores\|cross_timeframe_context\|trading_signals\|llm_narrative" services/feature_writer_agent.py (expect >= 8)
    grep -c '"i1"\|"i2"\|"i3"\|"i4"\|"i5"\|"i6"\|"i7"\|"i8"' services/feature_writer_agent.py (expect 0)
    grep -c "entry_price\|stop_loss\|confidence\|cis_score" src/persistence/repository/signal_ledger_repository.py (expect 0 in LedgerEntry/INSERT/SELECT contexts; may remain in comments)
    python -c "from src.persistence.repository.signal_ledger_repository import LedgerEntry; import dataclasses; print(len(dataclasses.fields(LedgerEntry)))" (expect ~38)
    .venv/bin/ruff check services/feature_writer_agent.py services/signal_writer_agent.py src/persistence/repository/ (no errors)
  </verify>
  <acceptance_criteria>
    - services/feature_writer_agent.py _INSERT_FEATURE_SQL contains all 8 concept column names; contains zero "i1".."i8" identifiers (in SQL string context)
    - services/feature_writer_agent.py i7 UPSERT clause now reads "trading_signals = COALESCE(trading_signals, '{}'::jsonb)"
    - src/persistence/repository/signal_ledger_repository.py LedgerEntry dataclass has approximately 38 fields (verify via dataclasses.fields() == ~38)
    - src/persistence/repository/signal_ledger_repository.py LedgerEntry has zero fields named entry_price, stop_loss, targets, confidence, cis_score, bucket_scores, features_snapshot, calibrated_confidence, swarm_multiplier
    - services/signal_writer_agent.py _payload_to_ledger_entries no longer passes entry_price=, stop_loss=, confidence=, cis_score=, features_snapshot=, swarm_multiplier= to LedgerEntry(...)
    - .venv/bin/ruff check on all 6 modified files returns no errors
  </acceptance_criteria>
  <done>Write-path code matches the target slim + renamed schema; ready for atomic DDL + restart in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Atomic maintenance window — stop writers, apply DDL, deploy code, restart services</name>
  <files>
    db/migrations/092_rename_intelligence_feature_tiers.sql
    db/migrations/093_slim_signal_ledger.sql
  </files>
  <read_first>
    - .planning/phases/104-storage-architecture-redesign/104-RESEARCH.md (Standard Procedures section 1 lines 156-180 for verified RENAME SQL; Pitfalls 1-3 lines 452-470)
    - .planning/phases/104-storage-architecture-redesign/104-PATTERNS.md (Critical Sequencing Notes lines 686-694)
    - CLAUDE.md (DAG order — L4 bar-writer L5 intelligence-pipeline L6 feature-writer signal-writer signal-tracker-compute lifecycle-writer L8 ml-training L9 signal-auditor)
  </read_first>
  <action>
    Create db/migrations/092_rename_intelligence_feature_tiers.sql with header "-- Phase 104 Plan 03 Task 2: rename i1..i8 to concept names on intelligence_features. Catalog-only op on TimescaleDB 2.25.1 — no decompression. Run with: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f db/migrations/092_rename_intelligence_feature_tiers.sql".
    Contents (in BEGIN/COMMIT transaction):
      ALTER TABLE intelligence_features RENAME COLUMN i1 TO technical_indicators;
      ALTER TABLE intelligence_features RENAME COLUMN i2 TO market_context;
      ALTER TABLE intelligence_features RENAME COLUMN i3 TO pattern_detections;
      ALTER TABLE intelligence_features RENAME COLUMN i4 TO regime_features;
      ALTER TABLE intelligence_features RENAME COLUMN i5 TO confluence_scores;
      ALTER TABLE intelligence_features RENAME COLUMN i6 TO cross_timeframe_context;
      ALTER TABLE intelligence_features RENAME COLUMN i7 TO trading_signals;
      ALTER TABLE intelligence_features RENAME COLUMN i8 TO llm_narrative;
    (smc NOT renamed; ts/symbol/tf NOT renamed per Pitfall 1.)

    Create db/migrations/093_slim_signal_ledger.sql with header "-- Phase 104 Plan 03 Task 2: drop fire-time duplicate columns from signal_ledger (already in intelligence_features.trading_signals JSONB). WARNING: may trigger recompression on 77 chunks (10-30 min I/O). Run with: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f db/migrations/093_slim_signal_ledger.sql".
    Contents (single transaction; ALTER TABLE statements for the 47 columns from PATTERNS.md lines 98-111, each as: ALTER TABLE signal_ledger DROP COLUMN IF EXISTS entry_price; ALTER TABLE signal_ledger DROP COLUMN IF EXISTS stop_loss; ... etc — every column in the DROP list).

    Maintenance window execution sequence (run as a single shell script to minimize downtime):
      1. Stop services that INSERT to either table (DAG order, reverse - L9 first to drain reads, then L6 writers):
         sudo systemctl stop indicagent-signal-auditor indicagent-signal-metrics-compute indicagent-lifecycle-writer indicagent-signal-tracker-compute indicagent-signal-writer indicagent-feature-writer indicagent-intelligence-pipeline
      2. Apply rename DDL: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f db/migrations/092_rename_intelligence_feature_tiers.sql
      3. Apply slim DDL (long): time PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f db/migrations/093_slim_signal_ledger.sql — log duration; expect 10-30 minutes for 77 chunks.
      4. Verify columns: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d intelligence_features" (should show concept names; NO i1..i8); PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d signal_ledger" (should show ~38 columns; verify entry_price/stop_loss/confidence/cis_score absent).
      5. Restart services in DAG forward order: sudo systemctl start indicagent-intelligence-pipeline indicagent-feature-writer indicagent-signal-writer indicagent-signal-tracker-compute indicagent-lifecycle-writer indicagent-signal-metrics-compute indicagent-signal-auditor
      6. Tail key logs for 60 seconds to confirm no INSERT errors: tail -f logs/feature_writer_agent.log logs/signal_writer_agent.log
      7. Verify writes are landing: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT symbol, tf, MAX(ts) FROM intelligence_features GROUP BY symbol, tf ORDER BY MAX(ts) DESC LIMIT 5" (timestamps should be within last 2 minutes during market hours)
      8. Verify signal_ledger inserts: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT count(*) FROM signal_ledger WHERE timestamp > NOW() - INTERVAL '5 minutes'" (should be > 0 during market hours)
  </action>
  <verify>
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT column_name FROM information_schema.columns WHERE table_name='intelligence_features' AND column_name IN ('i1','i2','i3','i4','i5','i6','i7','i8')" (expect 0 rows)
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT column_name FROM information_schema.columns WHERE table_name='intelligence_features' AND column_name IN ('technical_indicators','market_context','pattern_detections','regime_features','confluence_scores','cross_timeframe_context','trading_signals','llm_narrative')" (expect 8 rows)
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT count(*) FROM information_schema.columns WHERE table_name='signal_ledger'" (expect ~38-50; not 97)
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT column_name FROM information_schema.columns WHERE table_name='signal_ledger' AND column_name IN ('entry_price','stop_loss','confidence','cis_score','features_snapshot')" (expect 0 rows)
    systemctl is-active indicagent-feature-writer indicagent-signal-writer indicagent-intelligence-pipeline (each active)
    grep -E "ERROR|column.*does not exist" logs/feature_writer_agent.log | tail -5 (expect 0 since restart)
  </verify>
  <acceptance_criteria>
    - information_schema.columns has zero rows for table intelligence_features with column_name in (i1..i8)
    - information_schema.columns has exactly 8 rows for table intelligence_features with the 8 new concept column names
    - signal_ledger column count is approximately 38 (verified via SELECT count(*) FROM information_schema.columns WHERE table_name='signal_ledger')
    - signal_ledger has zero columns named entry_price, stop_loss, confidence, cis_score, features_snapshot, calibrated_confidence
    - systemctl is-active reports "active" for indicagent-feature-writer, indicagent-signal-writer, indicagent-intelligence-pipeline, indicagent-signal-tracker-compute, indicagent-lifecycle-writer, indicagent-signal-auditor
    - logs/feature_writer_agent.log has no "column does not exist" errors after restart timestamp
    - logs/signal_writer_agent.log has no "column does not exist" errors after restart timestamp
    - During market hours: SELECT MAX(ts) FROM intelligence_features returns timestamp within last 2 minutes
  </acceptance_criteria>
  <done>DB schema is in target state; all writers restarted cleanly and inserting fresh rows; ~6 GB of duplicate-column writes per week eliminated.</done>
</task>

<task type="auto">
  <name>Task 3: Update read-path code (services, dashboard API, ML feature builder, validation)</name>
  <files>
    src/core/bar_history_seeder.py
    src/core/ai/context.py
    src/core/ai/base_group_service.py
    src/core/ml/extractor.py
    src/core/ml/features.py
    src/core/ml/training_data.py
    src/intelligence/ml/feature_builder.py
    src/intelligence/pipeline/signal_processor.py
    src/intelligence/pipeline/feature_pipeline_executor.py
    src/intelligence/services/feature_validation_compute_agent.py
    src/intelligence/services/hmm_training_compute_agent.py
    src/intelligence/trading/zone_engine.py
    src/intelligence/confluence/cross_timeframe.py
    src/validation/cross_tier_validation.py
    src/validation/validation_engine.py
    src/core/schemas/parity.py
    src/persistence/logic/warmup_provider.py
    src/api/routes/signals.py
    src/api/routes/features.py
    src/api/routes/sse.py
    src/api/routes/narrative.py
    services/signal_tracker_compute_agent.py
    services/lifecycle_writer_agent.py
    services/signal_auditor_agent.py
    services/signal_metrics_compute_agent.py
    src/intelligence/metrics/compute.py
  </files>
  <read_first>
    - .planning/phases/104-storage-architecture-redesign/104-PATTERNS.md (Service Update Scope table lines 354-385 names every file with column refs; signal_auditor_agent section lines 283-318; src/api/routes/signals.py section lines 429-477)
    - .planning/phases/104-storage-architecture-redesign/104-RESEARCH.md (Pitfalls 5 and 7 lines 478-494)
    - src/intelligence/ml/feature_builder.py (_TRAINING_SQL lines ~84-113 with f.i4, f.i1, f.i7 refs)
    - src/api/routes/signals.py (get_active_signals lines ~141-240 with fire-time field SELECTs; _build_signal_row lines ~67-130 with row["i1"], row["i3"], etc)
    - services/signal_auditor_agent.py (lines 275-326 — pipeline_lag_ms and cis_score queries flagged in PATTERNS.md lines 283-318)
    - src/intelligence/metrics/compute.py (signal_metrics_compute_agent's helpers — audit for confidence/cis_score/bucket_scores references)
    - services/signal_tracker_compute_agent.py (bootstrap SQL — flagged in PATTERNS.md lines 234-255)
  </read_first>
  <action>
    Two categories of edits in this task:

    (A) SQL column rename in queries (i1..i8 -> concept names): edit every file listed in PATTERNS.md Service Update Scope table for the i1-i8 rename. Mechanical find/replace inside SQL string contexts only:
      i1 -> technical_indicators
      i2 -> market_context
      i3 -> pattern_detections
      i4 -> regime_features
      i5 -> confluence_scores
      i6 -> cross_timeframe_context
      i7 -> trading_signals
      i8 -> llm_narrative
    smc unchanged. DO NOT rename Python attribute access on BarIntelligenceRecord — the in-memory event bus keeps i1..i8 as Python attrs per RESEARCH.md line 387 ("Python attrs i1-i8 remain, only SQL column names change"). Concretely: row["i1"] in API code accessing asyncpg Record by SQL column key must become row["technical_indicators"]. But event.i1 in pipeline code accessing dataclass attribute stays event.i1.
    Specific updates per RESEARCH.md and PATTERNS.md:
      src/intelligence/ml/feature_builder.py _TRAINING_SQL: f.i4->>'hmm_regime' -> f.regime_features->>'hmm_regime'; f.i4->>'trend_regime' -> f.regime_features->>'trend_regime'; f.i1->>'atr_pct' -> f.technical_indicators->>'atr_pct'; f.i1->>'volume_z_score' -> f.technical_indicators->>'volume_z_score'; f.i7->0->>'tod_multiplier' -> f.trading_signals->0->>'tod_multiplier'. Also the WHERE clause "sl.features_snapshot IS NOT NULL" must be replaced — features_snapshot was dropped from signal_ledger. Replace with a check via the JOIN: AND f.trading_signals IS NOT NULL.
      src/api/routes/signals.py _build_signal_row include_features block: change row access keys "i1","i3","i4","i5","i6" to concept names. JSON response keys facing dashboard may stay as "i1","i3","i4","i5","i6" for backward compat OR be renamed — coordinate by keeping response keys as-is to avoid breaking dashboard.
      src/api/routes/signals.py get_active_signals query: previously selected entry_price, stop_loss, confidence, cis_score, targets, regime_context, stop_basis, market_price_at_signal, ask_at_signal, bid_at_signal, entry_zone_low, entry_zone_high, zone_valid_at_signal from signal_ledger — all dropped. Rewrite using LATERAL JOIN per PATTERNS.md lines 442-460:
        SELECT sl.signal_id, sl.symbol, sl.timeframe, sl.status, sl.was_selected, sl.feature_ts, sl.timestamp, sl.signal_computed_at,
               tf_sig.value->>'entry_price' AS entry_price, tf_sig.value->>'stop_loss' AS stop_loss, tf_sig.value->>'confidence' AS confidence,
               tf_sig.value->>'cis_score' AS cis_score, tf_sig.value->'targets' AS targets, tf_sig.value->>'regime_context' AS regime_context
        FROM signal_ledger sl
        LEFT JOIN intelligence_features f ON f.ts = sl.feature_ts AND f.symbol = sl.symbol AND f.tf = sl.feature_tf
        LEFT JOIN LATERAL jsonb_array_elements(f.trading_signals) AS tf_sig(value) ON tf_sig.value->>'signal_id' = sl.signal_id::text
        WHERE sl.status IN ('pending', 'active') AND sl.timestamp >= NOW() - INTERVAL '7 days'
        ORDER BY sl.symbol, sl.timeframe, sl.signal_computed_at DESC LIMIT 500

    (B) Signal_ledger dropped-column references (audit for cis_score, pipeline_lag_ms, confidence, bucket_scores, features_snapshot, entry_price, stop_loss, etc):
      services/signal_auditor_agent.py:
        Query 1 (coverage, line ~225): uses only symbol/timeframe/timestamp — unchanged.
        Query 2 (pipeline_lag_ms, lines 276-288): pipeline_lag_ms column was a DB-only computed/defaulted column NOT in slim schema. Replace by reading intelligence_features.pipeline_latency_ms instead — change SELECT/FROM to query intelligence_features with same percentile_cont aggregation, filter ts >= NOW() - INTERVAL '1 hour', symbol/tf join.
        Query 3 (cis_score distribution, lines 324-326): cis_score is dropped. Rewrite via JOIN: SELECT percentile aggregates on (tf_sig.value->>'cis_score')::float FROM signal_ledger sl JOIN intelligence_features f ON ... JOIN LATERAL jsonb_array_elements(f.trading_signals) tf_sig ON tf_sig.value->>'signal_id'=sl.signal_id::text WHERE sl.symbol=$1 AND sl.timeframe=$2 AND sl.timestamp>=NOW()-INTERVAL '1 hour'.
      services/signal_tracker_compute_agent.py: audit bootstrap SQL — if it SELECTs dropped columns (entry_price, stop_loss, confidence, targets) from signal_ledger, either remove those columns from SELECT (if not used by SignalState in-memory dict) or JOIN to intelligence_features.trading_signals for them. SignalState in-memory fields (mae, mfe, market_mae, market_mfe, chandelier_state, staleness_consecutive, activated_at, active_bars_elapsed, bars_since_activation) are all in slim schema, so likely bootstrap SELECT can simply drop the fire-time columns from its SELECT list.
      services/lifecycle_writer_agent.py: _TIMESTAMP_FIELDS (activated_at, exit_at, shadow_tracking_start_ts, market_entry_at, market_entry_exit_at) are all in slim schema — no change. Verify no UPDATE statement writes to dropped columns (calibrated_confidence, swarm_multiplier, etc) — grep for these in the file.
      src/intelligence/metrics/compute.py (called by signal_metrics_compute_agent): audit compute_signal_metrics() and compute_ic_metrics() — if they reference dropped columns (confidence, cis_score, bucket_scores, entry_price, stop_loss) in SELECT lists, either remove or replace with LATERAL JOIN to trading_signals.

    After all edits:
      .venv/bin/ruff check . --fix
      .venv/bin/black .
      .venv/bin/pytest tests/unit/ -q --no-cov 2>&1 | tail -30 — failures may include tests that reference old column names; fix or delete obsolete tests.
      Restart the affected services to pick up code changes:
        sudo systemctl restart indicagent-signal-tracker-compute indicagent-lifecycle-writer indicagent-signal-auditor indicagent-signal-metrics-compute
        sudo systemctl restart indicagent-api (if running)
        sudo systemctl restart indicagent-ml-training.timer (timer service - reload only)
      Tail each affected service log for 60s; confirm no "column does not exist" errors.
      Test dashboard signals endpoint: curl -s http://localhost:8000/api/signals/active | head -50 — should return JSON with entry_price/stop_loss/confidence populated from JSONB.
  </action>
  <verify>
    grep -rn '"i1"\|"i2"\|"i3"\|"i4"\|"i5"\|"i6"\|"i7"\|"i8"' src/api/routes/ src/intelligence/ml/feature_builder.py services/signal_auditor_agent.py 2>&1 | grep -v "test_\|.pyc" | head -20 (review remaining; should only be Python attribute access or backward-compat response keys, not SQL identifiers)
    grep -c "entry_price\|stop_loss\|confidence" services/signal_auditor_agent.py src/intelligence/metrics/compute.py services/signal_tracker_compute_agent.py | grep -v ":0" (should reference these only via JSONB extraction tf_sig.value->>'entry_price' patterns)
    grep -q "LATERAL jsonb_array_elements" src/api/routes/signals.py
    grep -q "regime_features\|technical_indicators\|trading_signals" src/intelligence/ml/feature_builder.py
    .venv/bin/ruff check . 2>&1 | tail -5 (no errors)
    systemctl is-active indicagent-signal-auditor indicagent-signal-tracker-compute indicagent-lifecycle-writer indicagent-signal-metrics-compute (all active)
    curl -s http://localhost:8000/api/signals/active 2>&1 | head -5 (returns JSON, not error)
  </verify>
  <acceptance_criteria>
    - src/intelligence/ml/feature_builder.py _TRAINING_SQL contains "f.regime_features", "f.technical_indicators", "f.trading_signals" and contains zero "f.i1", "f.i4", "f.i7" SQL refs
    - src/api/routes/signals.py contains "LATERAL jsonb_array_elements" referencing trading_signals
    - src/api/routes/signals.py get_active_signals SELECT no longer references sl.entry_price, sl.stop_loss, sl.confidence, sl.cis_score, sl.targets directly from signal_ledger (only via JSONB extraction)
    - services/signal_auditor_agent.py no longer references sl.pipeline_lag_ms; uses intelligence_features.pipeline_latency_ms or equivalent
    - services/signal_auditor_agent.py no longer references sl.cis_score directly; uses JSONB extraction tf_sig.value->>'cis_score'
    - .venv/bin/ruff check . returns no errors
    - .venv/bin/pytest tests/unit/ passes (after deleting/updating tests referencing dropped columns)
    - systemctl is-active reports active for indicagent-signal-tracker-compute, indicagent-lifecycle-writer, indicagent-signal-auditor, indicagent-signal-metrics-compute
    - curl http://localhost:8000/api/signals/active returns valid JSON with populated entry_price/stop_loss/confidence fields
    - logs/signal_auditor_agent.log has no "column does not exist" errors in the 60 seconds after restart
  </acceptance_criteria>
  <done>All read paths updated; dashboard API serves fire-time fields via JSONB JOIN; ML training pipeline uses renamed columns; all services running cleanly on new schema.</done>
</task>

</tasks>

<verification>
- intelligence_features has all 8 renamed columns + smc; zero i1..i8 columns
- signal_ledger column count down from 97 to ~38
- All writer services (feature_writer, signal_writer) inserting fresh rows post-restart
- All reader services (signal_tracker, lifecycle, signal_auditor, signal_metrics_compute) running without errors
- Dashboard /api/signals/active returns fire-time fields populated from JSONB
- ML feature_builder produces training matrix from new schema
- Unit test suite passes
</verification>

<success_criteria>
- ~6 GB/week reduction in signal_ledger growth (drop ~47 duplicate columns)
- 0 column-rename related errors in service logs 60s after restart
- Dashboard remains functional throughout (verified via curl /api/signals/active)
- ML training pipeline continues producing training data (verified by feature_builder query execution)
</success_criteria>

<output>
After completion, create `.planning/phases/104-storage-architecture-redesign/104-03-SUMMARY.md`
</output>
