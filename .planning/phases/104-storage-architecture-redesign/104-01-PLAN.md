---
phase: 104-storage-architecture-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/plans/storage-audit.md
  - db/migrations/090_retention_policies.sql
autonomous: true

must_haves:
  truths:
    - "Every hypertable except dlq/metrics has a retention policy"
    - "Every Kafka topic has a byte retention cap"
    - "Storage audit doc exists and describes the 3-store target architecture"
  artifacts:
    - path: "docs/plans/storage-audit.md"
      provides: "Authoritative inventory of all hypertables, retention policies, Kafka topic caps, growth analysis"
      min_lines: 80
    - path: "db/migrations/090_retention_policies.sql"
      provides: "add_retention_policy calls for 9 hypertables"
      contains: "add_retention_policy"
  key_links:
    - from: "TimescaleDB scheduler"
      to: "9 hypertables"
      via: "add_retention_policy job rows"
      pattern: "SELECT count(*) FROM timescaledb_information.jobs WHERE proc_name='policy_retention'"
    - from: "Redpanda topic config"
      to: "6 previously-unbounded topics"
      via: "rpk topic alter-config retention.bytes"
      pattern: "retention.bytes=524288000"
---

<objective>
Apply pure-config storage hygiene that does not require any service restarts or schema changes.
Produce the canonical storage audit doc, install TimescaleDB retention policies on the 9 hypertables
currently missing them, and apply 500 MB byte caps to the 6 unbounded Kafka topics.

Purpose: Bound storage growth immediately while the more invasive plans (drop shadow, rename, slim) proceed.
Output: Audit doc, retention SQL migration, Kafka config commands applied to live cluster.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/104-storage-architecture-redesign/104-CONTEXT.md
@.planning/phases/104-storage-architecture-redesign/104-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Storage audit document</name>
  <files>docs/plans/storage-audit.md</files>
  <read_first>
    - .planning/phases/104-storage-architecture-redesign/104-CONTEXT.md (full problem statement, target architecture)
    - .planning/phases/104-storage-architecture-redesign/104-RESEARCH.md (live DB inventory table, retention findings)
  </read_first>
  <action>
    Create docs/plans/storage-audit.md as authoritative reference for phase 104.
    Required sections (use H2 headers): "Current Inventory" (table of all 15 hypertables from RESEARCH.md with size, chunks, compression policy, current retention status), "Root Causes" (the 6 root causes from CONTEXT.md: feature_snapshots_shadow duplication, signal_ledger 97-column bloat, signal volume explosion, naming convention violation, 6 unbounded Kafka topics, signal_lineage spike explanation), "Target Architecture" (3-store diagram: intelligence_features canonical + signal_ledger slim ~25 cols + ml_signal_training new), "Migration Sequence" (the 8-step dependency order from CONTEXT.md), "Estimated Impact" (the disk-growth table from CONTEXT.md showing reduction from 6 GB/week to 1.5 GB/week).
    Include the exact column rename mapping (i1->technical_indicators through i8->llm_narrative; smc unchanged).
    Reference db/migrations/090_retention_policies.sql as the live artifact.
    Live numbers (May 22 2026) from RESEARCH.md: intelligence_features 19 GB / 77 chunks, signal_ledger 12 GB / 77 chunks, feature_snapshots_shadow 13 GB / 14 chunks.
  </action>
  <verify>
    test -f docs/plans/storage-audit.md
    wc -l docs/plans/storage-audit.md (expect > 80)
    grep -q "technical_indicators" docs/plans/storage-audit.md
    grep -q "ml_signal_training" docs/plans/storage-audit.md
    grep -q "feature_snapshots_shadow" docs/plans/storage-audit.md
  </verify>
  <acceptance_criteria>
    - docs/plans/storage-audit.md exists with > 80 lines
    - Contains H2 sections: Current Inventory, Root Causes, Target Architecture, Migration Sequence, Estimated Impact
    - Contains the full column rename table (i1..i8 -> concept names)
    - Mentions each of 9 hypertables that will receive retention policies
    - Mentions all 6 Kafka topics receiving 500 MB byte caps by name
  </acceptance_criteria>
  <done>Audit doc committed; serves as canonical reference for all subsequent phase 104 plans.</done>
</task>

<task type="auto">
  <name>Task 2: Retention policy migration on 9 hypertables</name>
  <files>db/migrations/090_retention_policies.sql</files>
  <read_first>
    - .planning/phases/104-storage-architecture-redesign/104-RESEARCH.md (section "TimescaleDB retention policies (exact SQL)" lines 182-209 has verified SQL)
    - db/migrations/089_hardening.sql (style/header conventions of recent migration)
  </read_first>
  <action>
    Create db/migrations/090_retention_policies.sql containing add_retention_policy() calls for 9 hypertables that currently have NO retention policy.
    Header comment: "-- Phase 104 Plan 01: retention policies for hypertables missing them. Run with: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f db/migrations/090_retention_policies.sql"
    Policies to install (each uses if_not_exists => true):
      - intelligence_features: INTERVAL '2 years'
      - signal_ledger: INTERVAL '1 year'
      - llm_calls: INTERVAL '90 days'
      - signal_lineage: INTERVAL '90 days'
      - signal_transform_log: INTERVAL '90 days'
      - market_data_ohlcv: INTERVAL '2 years'
      - macro_features: INTERVAL '1 year'
      - ctx_events: INTERVAL '30 days'
      - alpha_multiplier_shadow: INTERVAL '30 days'
    Do NOT include feature_snapshots_shadow (Plan 02 drops it).
    Do NOT include ml_signal_training (Plan 04 creates it with its own policy).
    Apply the migration after writing: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f db/migrations/090_retention_policies.sql
  </action>
  <verify>
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT hypertable_name, config->>'drop_after' FROM timescaledb_information.jobs WHERE proc_name='policy_retention' ORDER BY hypertable_name"
    Expected: 9 new rows (intelligence_features, signal_ledger, llm_calls, signal_lineage, signal_transform_log, market_data_ohlcv, macro_features, ctx_events, alpha_multiplier_shadow) plus the 4 pre-existing rows.
  </verify>
  <acceptance_criteria>
    - db/migrations/090_retention_policies.sql exists
    - File contains exactly 9 add_retention_policy() calls
    - Every call uses if_not_exists => true
    - After applying: SELECT count(*) FROM timescaledb_information.jobs WHERE proc_name='policy_retention' AND hypertable_name='intelligence_features' returns 1
    - After applying: SELECT count(*) FROM timescaledb_information.jobs WHERE proc_name='policy_retention' AND hypertable_name='signal_ledger' returns 1
    - Re-running the migration produces no errors (idempotent via if_not_exists)
  </acceptance_criteria>
  <done>9 retention policies installed and verified in timescaledb_information.jobs.</done>
</task>

<task type="auto">
  <name>Task 3: Kafka byte retention caps on 6 unbounded topics</name>
  <files>docs/plans/storage-audit.md</files>
  <read_first>
    - .planning/phases/104-storage-architecture-redesign/104-RESEARCH.md (section "Kafka byte retention caps (exact commands)" lines 222-234)
  </read_first>
  <action>
    Apply 500 MB (524288000 byte) retention cap to each of these 6 topics currently with retention.bytes=-1:
      intelligence.signal.audit, swarm.alpha, narratives, intelligence.signal_lineage, llm.calls, llm.outcomes
    Command per topic: docker exec redpanda rpk topic alter-config <topic> --set retention.bytes=524288000
    After applying, append a "## Kafka Byte Caps Applied" section to docs/plans/storage-audit.md listing each topic and the timestamp of application. Verify each topic via: docker exec redpanda rpk topic describe <topic> | grep retention.bytes
    retention.ms=86400000 (24h) is preserved unchanged as the primary driver; bytes cap is a safety ceiling per RESEARCH.md.
  </action>
  <verify>
    for t in intelligence.signal.audit swarm.alpha narratives intelligence.signal_lineage llm.calls llm.outcomes; do docker exec redpanda rpk topic describe "$t" | grep retention.bytes; done
    Expected: each row shows retention.bytes 524288000 (not -1)
  </verify>
  <acceptance_criteria>
    - Each of the 6 topics returns retention.bytes=524288000 from rpk topic describe
    - No topic returns retention.bytes=-1 among these 6
    - docs/plans/storage-audit.md has a "## Kafka Byte Caps Applied" section listing all 6 topics with application timestamp
    - retention.ms=86400000 unchanged on each (verified via rpk topic describe)
  </acceptance_criteria>
  <done>All 6 unbounded Kafka topics now have a 500 MB safety floor; storage audit doc records the action.</done>
</task>

</tasks>

<verification>
- All 9 retention policies present in timescaledb_information.jobs
- All 6 Kafka topics have retention.bytes=524288000
- docs/plans/storage-audit.md complete with all required sections
- No service restarts required (pure config change)
</verification>

<success_criteria>
- Disk growth on 9 hypertables now bounded by retention policies
- Kafka growth on 6 topics bounded by 500 MB byte caps
- Audit doc serves as canonical reference for downstream phase 104 plans
</success_criteria>

<output>
After completion, create `.planning/phases/104-storage-architecture-redesign/104-01-SUMMARY.md`
</output>
