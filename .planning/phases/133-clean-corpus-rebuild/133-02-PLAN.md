---
phase: 133-clean-corpus-rebuild
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - production/scripts/reset_pipeline_data.py
  - production/scripts/run_historical_pipeline.py
  - production/scripts/lifecycle_replay.py
  - production/scripts/feature_replay.py
  - src/intelligence/trading/zone_engine.py
  - src/intelligence/trading/confidence.py
autonomous: true

must_haves:
  truths:
    - "reset_pipeline_data.py uses a single TRUNCATE ... CASCADE statement covering all D-07 tables rather than a per-table loop"
    - "run_historical_pipeline.py has no --warmup flag, no two-pass warmup loop, and no warmup references"
    - "lifecycle_replay.py uses async with conn.transaction(): context manager for all per-(symbol,tf) DB operations — no manual await conn.execute('COMMIT') in finally blocks"
    - "feature_replay.py calls incremental_compute() with per-symbol state accumulation, not compute_full()"
    - "_cfg() in zone_engine.py is renamed to _read_config() and all call sites updated"
    - "confidence_utils.py is renamed to confidence.py and all import sites across the entire repo updated"
    - "phase_127 snapshot scripts are deleted and test sweep confirms no broken imports"
    - "migrate_signal_ledger.py is archived to production/scripts/archive/"
  artifacts:
    - path: "production/scripts/reset_pipeline_data.py"
      provides: "Fixed TRUNCATE CASCADE wipe script"
      contains: "TRUNCATE TABLE"
    - path: "production/scripts/lifecycle_replay.py"
      provides: "Asyncpg transaction hygiene fix"
      contains: "async with conn.transaction():"
    - path: "production/scripts/feature_replay.py"
      provides: "Stateful incremental_compute() fix"
      contains: "incremental_compute"
  key_links:
    - from: "reset_pipeline_data.py _execute_wipe()"
      to: "signal_events, trade_frames, trade_executions, intelligence_features"
      via: "single TRUNCATE ... CASCADE statement"
      pattern: "TRUNCATE TABLE.*CASCADE"
    - from: "lifecycle_replay.py per-symbol loop"
      to: "DB transaction"
      via: "async with conn.transaction():"
      pattern: "async with conn.transaction"
---

<objective>
Fix all D-05 script cleanup items (B2/B3/B4/B5/D) before the TRUNCATE + rebuild. These are prerequisites for a clean rebuild, not post-rebuild cleanup. The TRUNCATE in plan 133-04 depends on all B-fixes being applied first.

Purpose: A clean rebuild requires a clean reset script (B4), a working replay script (B2), a correct validation tool (B3), no dead code confusing the replay (B5), and no stale file references (Layer D).
Output: Five scripts fixed, dead code deleted, confidence.py rename complete, phase_127 scripts deleted, migrate_signal_ledger.py archived.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/133-clean-corpus-rebuild/133-CONTEXT.md
@production/scripts/reset_pipeline_data.py
@production/scripts/run_historical_pipeline.py
@production/scripts/lifecycle_replay.py
@production/scripts/feature_replay.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: B4 — fix reset_pipeline_data.py TRUNCATE CASCADE</name>
  <files>
    production/scripts/reset_pipeline_data.py
  </files>
  <read_first>
    - production/scripts/reset_pipeline_data.py (full file — understand current _TRUNCATE_TABLES list and _execute_wipe function)
  </read_first>
  <action>
    The current _execute_wipe() iterates _TRUNCATE_TABLES and issues one TRUNCATE per table. This breaks when FK constraints exist (trade_executions references trade_frames).

    Replace _execute_wipe() with a single TRUNCATE ... CASCADE statement covering all D-07 tables:

    Tables to TRUNCATE in one statement (from D-07 scope in CONTEXT.md):
      signal_events, trade_frames, trade_executions, intelligence_features,
      signal_metrics_dq_failures, llm_calls, llm_model_scores, ml_signal_training,
      shadow_transition_log, swarm_agent_weights, alpha_multiplier_shadow,
      setup_performance, signal_ai_enrichment, signal_lineage, signal_metrics,
      signal_metrics_ic, signal_transform_log

    Also keep the shadow_registry eval stats UPDATE (not TRUNCATE — enrollment rows preserved).

    New _execute_wipe():
    ```python
    def _execute_wipe(conn) -> None:
        print(f"[{_ts()}] Wiping signal-derived tables...")
        _CASCADE_TRUNCATE = """
            TRUNCATE TABLE
                signal_events,
                trade_frames,
                trade_executions,
                intelligence_features,
                signal_metrics_dq_failures,
                llm_calls,
                llm_model_scores,
                ml_signal_training,
                shadow_transition_log,
                swarm_agent_weights,
                alpha_multiplier_shadow,
                setup_performance,
                signal_ai_enrichment,
                signal_lineage,
                signal_metrics,
                signal_metrics_ic,
                signal_transform_log
            CASCADE
        """
        with conn.cursor() as cur:
            cur.execute(_CASCADE_TRUNCATE)
            print("  TRUNCATED all signal-derived tables (CASCADE)")
            cur.execute(_SHADOW_RESET_SQL)
            print("  RESET shadow_registry eval stats")
        conn.commit()
        print(f"[{_ts()}] Wipe complete.\n")
    ```

    Also update _print_plan() to list all D-07 tables (not just _TRUNCATE_TABLES).

    Dry-run verification (do NOT commit — just verify cascade scope):
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "
    BEGIN;
    TRUNCATE TABLE signal_events, trade_frames, trade_executions, intelligence_features,
      signal_metrics_dq_failures, llm_calls, llm_model_scores, ml_signal_training,
      shadow_transition_log, swarm_agent_weights, alpha_multiplier_shadow,
      setup_performance, signal_ai_enrichment, signal_lineage, signal_metrics,
      signal_metrics_ic, signal_transform_log CASCADE;
    ROLLBACK;
    "
    If ROLLBACK succeeds without error, the cascade scope is confirmed correct.
    Record any additional tables that CASCADE reveals (from FK dependencies not in the list above) — add them to the script's table list.
  </action>
  <verify>
    grep -n "CASCADE" production/scripts/reset_pipeline_data.py
    Expected: single TRUNCATE TABLE ... CASCADE statement in _execute_wipe(), not per-table individual TRUNCATE calls.

    python production/scripts/reset_pipeline_data.py
    Expected: dry-run output prints all D-07 tables with row counts, exits without error.
  </verify>
  <done>
    reset_pipeline_data.py contains a single TRUNCATE TABLE ... CASCADE statement covering all 17 D-07 tables. Dry-run (without --confirm) runs without error and prints the plan.
  </done>
</task>

<task type="auto">
  <name>Task 2: B5 — remove dead --warmup flag from run_historical_pipeline.py</name>
  <files>
    production/scripts/run_historical_pipeline.py
  </files>
  <read_first>
    - production/scripts/run_historical_pipeline.py (search for "warmup" to find all occurrences)
  </read_first>
  <action>
    Remove all warmup references from run_historical_pipeline.py:
    1. grep -n "warmup" production/scripts/run_historical_pipeline.py to find all occurrences.
    2. Remove the --warmup argparse argument declaration.
    3. Remove the two-pass loop in the workers==1 branch that handles warmup.
    4. Remove any warmup-related NOTE comment in the parallel mode branch.
    5. Remove any args.warmup references in the worker dispatch logic.

    Also sweep across the codebase:
    grep -rn "warmup" tests/ docs/ production/ src/ --include="*.py" --include="*.md" --include="*.sh"
    Remove or update all references that reference the --warmup flag as a feature.
    (References explaining WHY warmup is a no-op may be converted to comments noting it was removed.)
  </action>
  <verify>
    grep -n "warmup" production/scripts/run_historical_pipeline.py
    Expected: zero results.

    python production/scripts/run_historical_pipeline.py --help 2>&1 | grep warmup
    Expected: no output (flag no longer exists).
  </verify>
  <done>
    No --warmup argument, no two-pass loop, no warmup references remain in run_historical_pipeline.py. grep returns zero results.
  </done>
</task>

<task type="auto">
  <name>Task 3: B2 — asyncpg transaction hygiene in lifecycle_replay.py; B3 — stateful incremental_compute in feature_replay.py; B7 — verify SQL fan-out fix; Layer D — code hygiene</name>
  <files>
    production/scripts/lifecycle_replay.py
    production/scripts/feature_replay.py
    src/intelligence/trading/zone_engine.py
    src/intelligence/trading/confidence.py
  </files>
  <read_first>
    - production/scripts/lifecycle_replay.py (search for "COMMIT", "transaction", "finally", and the per-symbol DB write loop)
    - production/scripts/lifecycle_replay.py (_verify_replay function — find the COUNT(*) query that overcounts due to JOIN fan-out)
    - production/scripts/feature_replay.py (search for "compute_full", "incremental_compute", and the per-symbol processing loop)
    - src/intelligence/trading/zone_engine.py (search for "_cfg(" to find all call sites)
    - find src/intelligence/trading/ -name "confidence_utils.py" to confirm the file path
  </read_first>
  <action>
    **B2 — lifecycle_replay.py asyncpg transaction hygiene:**

    Search lifecycle_replay.py for all occurrences of:
    - await conn.execute('COMMIT')
    - await conn.execute('ROLLBACK')
    - Manual transaction management in finally blocks

    Replace each per-(symbol, tf) DB operation block with:
    ```python
    async with conn.transaction():
        # ... all DB writes for this symbol/tf combination ...
    ```
    The context manager commits on clean exit and rolls back (including connection-level cleanup) on any exception. Remove any manual COMMIT/ROLLBACK execute calls.

    **B7 — _verify_replay fan-out fix:**

    Find _verify_replay() in lifecycle_replay.py. The query does a LEFT JOIN trade_frames + trade_executions on signal_events, which inflates count when a frame has multiple execution rows.

    Fix: add COUNT(DISTINCT se.signal_id) as a separate aggregate in the verify output, or restructure the main count to aggregate at signal_events level before joining. The zero-checks (stale_unresolved, target_no_pnl, orphan_signal_events) are trustworthy — only the absolute totals need fixing.

    **B3 — feature_replay.py stateful incremental_compute:**

    Search feature_replay.py for calls to compute_full(). Switch to incremental_compute() with per-symbol state accumulation:
    1. Declare a state dict: plugin_states: dict[tuple[str,str], dict] = {} keyed by (symbol, tf).
    2. Before each bar computation, pass the current state for (symbol, tf) into incremental_compute().
    3. After each bar, update plugin_states[(symbol, tf)] with the returned state.

    This is the architecturally correct path. GARCH/Kalman/HMM/BOCPD require state accumulation across bars. compute_full() is stateless and produces wrong outputs for stateful plugins.

    **Layer D — Code hygiene:**

    zone_engine.py:
    1. grep -n "_cfg(" src/intelligence/trading/zone_engine.py to find the function definition and all call sites.
    2. Rename the function to _read_config() in the definition.
    3. Update all call sites within zone_engine.py from _cfg( to _read_config(.

    confidence_utils.py -> confidence.py:
    1. git mv src/intelligence/trading/confidence_utils.py src/intelligence/trading/confidence.py
    2. grep -r "confidence_utils" . --include="*.py" --include="*.md" (search entire repo including docs/ and CLAUDE.md files)
    3. Update all import sites in .py files: from src.intelligence.trading.confidence_utils import -> from src.intelligence.trading.confidence import
    4. Update live CLAUDE.md files (e.g., src/intelligence/CLAUDE.md) that reference confidence_utils.py.
    5. IMPORTANT: docs/plans/ files are read-only historical records — do NOT modify them even if grep matches. Only update live source code and active CLAUDE.md files.

    phase_127 snapshot scripts:
    1. find production/scripts/ -name "phase_127*" to locate them.
    2. Delete each file: git rm production/scripts/phase_127_before_snapshot.py production/scripts/phase_127_monitor_replay.py (or whatever names exist).
    3. grep -r "phase_127" tests/ --include="*.py" to find any test imports that would break at collection time. Remove or update them.

    migrate_signal_ledger.py:
    1. find production/scripts/ -name "migrate_signal_ledger.py" to confirm it exists.
    2. mkdir -p production/scripts/archive/
    3. git mv production/scripts/migrate_signal_ledger.py production/scripts/archive/migrate_signal_ledger.py
    4. grep -r "migrate_signal_ledger" . --include="*.py" --include="*.md" to check for references. Update any import/reference that would break.
  </action>
  <verify>
    # B2 transaction hygiene
    grep -n "await conn.execute.*COMMIT" production/scripts/lifecycle_replay.py
    Expected: zero results.
    grep -n "async with conn.transaction" production/scripts/lifecycle_replay.py
    Expected: at least one match per per-symbol write block.

    # B3 stateful compute
    grep -n "incremental_compute" production/scripts/feature_replay.py
    Expected: at least one match in the per-symbol processing loop.
    grep -n "compute_full" production/scripts/feature_replay.py
    Expected: zero results (removed).

    # Layer D — zone_engine
    grep -n "_cfg(" src/intelligence/trading/zone_engine.py
    Expected: zero results.
    grep -n "_read_config(" src/intelligence/trading/zone_engine.py
    Expected: at least one result (function definition + call sites).

    # Layer D — confidence rename
    grep -r "confidence_utils" . --include="*.py" --include="*.md"
    Expected: zero results across entire repo.

    # Layer D — phase_127 scripts
    ls production/scripts/phase_127* 2>/dev/null
    Expected: no files found.

    # Layer D — migrate_signal_ledger archived
    ls production/scripts/archive/migrate_signal_ledger.py
    Expected: file exists.
    ls production/scripts/migrate_signal_ledger.py 2>/dev/null
    Expected: file not found.

    # Unit tests
    .venv/bin/pytest tests/unit/ -q
    Expected: all green (no broken imports from renames/deletes).
  </verify>
  <done>
    B2: lifecycle_replay.py uses async with conn.transaction(): throughout, no manual COMMIT/ROLLBACK execute calls. B3: feature_replay.py calls incremental_compute() with per-(symbol,tf) state dict. B7: _verify_replay outputs COUNT(DISTINCT signal_id). Layer D: _cfg->_read_config, confidence.py in place, phase_127 scripts deleted, migrate_signal_ledger.py in archive/. Unit tests green.
  </done>
</task>

</tasks>

<verification>
- python production/scripts/reset_pipeline_data.py (dry-run, no --confirm) prints plan without error
- grep "CASCADE" production/scripts/reset_pipeline_data.py returns single TRUNCATE statement
- grep "warmup" production/scripts/run_historical_pipeline.py returns zero results
- grep "async with conn.transaction" production/scripts/lifecycle_replay.py returns match
- grep "incremental_compute" production/scripts/feature_replay.py returns match
- grep -r "confidence_utils" . --include="*.py" returns zero results
- .venv/bin/pytest tests/unit/ -q passes green
</verification>

<success_criteria>
All D-05 items complete: B4 CASCADE truncate, B5 warmup removed, B2 tx hygiene, B3 stateful compute, B7 verify fix, Layer D renames/deletes. Unit tests green. These fixes are committed before plan 133-04 runs the actual TRUNCATE.
</success_criteria>

<output>
After completion, create `.planning/phases/133-clean-corpus-rebuild/133-02-SUMMARY.md`
</output>
