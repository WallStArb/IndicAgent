---
phase: 133-clean-corpus-rebuild
plan: "07"
type: execute
wave: 5
depends_on:
  - "133-05"
  - "133-06"
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Unit tests pass green: .venv/bin/pytest tests/unit/ -q exits 0"
    - "No Phase 131/132 testing service drop-ins remain in /etc/systemd/system/"
    - "All Phase 133 code changes are committed and pushed to origin main"
    - "133-SUMMARY.md exists and is committed"
    - "STATE.md is updated to Phase 133 COMPLETE"
  artifacts:
    - path: ".planning/phases/133-clean-corpus-rebuild/133-SUMMARY.md"
      provides: "Phase 133 completion summary with all acceptance gate results"
      contains: "Phase 133 COMPLETE"
  key_links:
    - from: "133-ACCEPTANCE-REPORT.md"
      to: "133-SUMMARY.md"
      via: "summary cites acceptance report as evidence"
      pattern: "ALL GATES PASSED"
---

<objective>
Final cleanup, unit test verification, and phase closure. Remove any testing artifacts from Phase 131/132. Commit all changes. Write the phase summary. Update STATE.md.

Purpose: Clean close — no dangling service overrides, no uncommitted changes, no half-finished state.
Output: Committed and pushed codebase with 133-SUMMARY.md. Phase declared complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/133-clean-corpus-rebuild/133-CONTEXT.md
@.planning/phases/133-clean-corpus-rebuild/133-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service cleanup, unit tests, commit, and phase closure</name>
  <files>
    .planning/phases/133-clean-corpus-rebuild/133-SUMMARY.md
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/phases/133-clean-corpus-rebuild/133-06-SUMMARY.md (confirm ALL GATES PASSED before closing)
    - .planning/phases/133-clean-corpus-rebuild/133-ACCEPTANCE-REPORT.md
  </read_first>
  <action>
    **GATE: 133-06-SUMMARY.md must confirm ALL GATES PASSED.**

    **Step 1: Remove Phase 131/132 testing service drop-ins.**
    Check for drop-ins from testing (Restart=no overrides or similar):
    find /etc/systemd/system/ -name "*.conf" -newer /etc/systemd/system/indicagent-intelligence-pipeline.service 2>/dev/null
    ls /etc/systemd/system/indicagent-*.service.d/ 2>/dev/null

    If any drop-ins exist that were created for Phase 131/132 testing:
    sudo rm -f /etc/systemd/system/indicagent-*.service.d/<testing-override>.conf
    sudo systemctl daemon-reload

    If no testing drop-ins exist, note that in the summary.

    **Step 2: Restore services if they were stopped.**
    If services were stopped for the rebuild and have not been restarted:
    sudo systemctl start indicagent-feature-writer
    sudo systemctl start indicagent-signal-tracker
    # Note: indicagent-intelligence-pipeline is a live pipeline — only restart if appropriate
    # for current workflow. The corpus rebuild does not require it running.

    Verify services are in expected state:
    systemctl is-active indicagent-feature-writer
    systemctl is-active indicagent-signal-tracker

    **Step 3: Run full unit test suite.**
    .venv/bin/pytest tests/unit/ -q
    Must exit 0. If any tests fail:
    - Identify the failure
    - Fix it (it is likely a broken import from the Layer D renames in 133-02)
    - Re-run until green
    Do NOT proceed to commit if tests are red.

    **Step 4: Stage all Phase 133 changes.**
    git status
    git add production/migrations/149_trade_frames_hypertable.sql
    git add production/scripts/reset_pipeline_data.py
    git add production/scripts/run_historical_pipeline.py
    git add production/scripts/lifecycle_replay.py
    git add production/scripts/feature_replay.py
    git add src/intelligence/trading/zone_engine.py
    git add src/intelligence/trading/confidence.py
    git add .planning/phases/133-clean-corpus-rebuild/
    # If confidence_utils.py was git-moved, the delete is already tracked:
    git add -u src/intelligence/trading/confidence_utils.py 2>/dev/null || true
    # Phase_127 scripts deleted:
    git add -u production/scripts/phase_127_before_snapshot.py 2>/dev/null || true
    git add -u production/scripts/phase_127_monitor_replay.py 2>/dev/null || true
    # migrate_signal_ledger.py moved to archive:
    git add production/scripts/archive/migrate_signal_ledger.py 2>/dev/null || true
    git add -u production/scripts/migrate_signal_ledger.py 2>/dev/null || true
    # MEMORY.md update:
    git add ~/.claude/projects/-home-bg-dev-indicagent/memory/MEMORY.md 2>/dev/null || true
    # ROADMAP.md update:
    git add .planning/ROADMAP.md

    **Step 5: Write 133-SUMMARY.md.**
    Create .planning/phases/133-clean-corpus-rebuild/133-SUMMARY.md with:
    - Phase: 133-clean-corpus-rebuild
    - Status: COMPLETE
    - Date: <today>
    - All 7 plans executed and their outcomes
    - Acceptance gates: reference 133-ACCEPTANCE-REPORT.md, state "ALL GATES PASSED"
    - Key changes:
      * Migration 149: trade_frames is now a TimescaleDB hypertable (compress_segmentby='symbol,tf')
      * B4: reset_pipeline_data.py uses single TRUNCATE ... CASCADE
      * B5: --warmup flag removed from run_historical_pipeline.py
      * B2: lifecycle_replay.py uses async with conn.transaction() throughout
      * B3: feature_replay.py calls incremental_compute() with state accumulation
      * B7: _verify_replay outputs COUNT(DISTINCT signal_id)
      * Layer D: _cfg->_read_config, confidence.py, phase_127 scripts deleted, migrate_signal_ledger.py archived
    - Corpus state: ~1,036,513 signal_events, 35 plugins firing, all acceptance gates green
    - Next phase: ML training (Phase 134+) is unblocked

    **Step 6: Commit.**
    git commit -m "feat(133): clean corpus rebuild complete — hypertable migration, script fixes, 35-plugin corpus"

    **Step 7: Merge to main and push.**
    git checkout main && git merge --ff-only <feature-branch> 2>/dev/null || true
    # (if already on main, just push)
    git push origin main

    **Step 8: Update STATE.md.**
    Open .planning/STATE.md and update:
    - Current Position section: Phase 133 COMPLETE
    - Last activity: Phase 133 complete — clean corpus rebuild done, ML training unblocked
    - Progress: increment completed_phases and completed_plans counts

    Commit STATE.md update:
    git add .planning/STATE.md
    git commit -m "docs(state): Phase 133 complete"
    git push origin main
  </action>
  <verify>
    .venv/bin/pytest tests/unit/ -q
    Expected: exit code 0, all tests pass.

    git log --oneline -3
    Expected: Phase 133 commits visible.

    ls .planning/phases/133-clean-corpus-rebuild/133-SUMMARY.md
    Expected: file exists.

    grep "Phase 133 COMPLETE\|COMPLETE" .planning/phases/133-clean-corpus-rebuild/133-SUMMARY.md
    Expected: match found.
  </verify>
  <done>
    Unit tests green. All Phase 133 changes committed and pushed. 133-SUMMARY.md exists with COMPLETE status and cites ALL GATES PASSED. STATE.md updated to Phase 133 complete. No testing drop-ins remain.
  </done>
</task>

</tasks>

<verification>
- .venv/bin/pytest tests/unit/ -q exits 0
- git log --oneline shows Phase 133 commit
- grep "ALL GATES PASSED" .planning/phases/133-clean-corpus-rebuild/133-SUMMARY.md returns match
- grep "Phase 133 COMPLETE\|COMPLETE" .planning/STATE.md returns match
</verification>

<success_criteria>
All unit tests green. All Phase 133 code changes committed and pushed. 133-SUMMARY.md records COMPLETE status with all acceptance gate evidence. STATE.md updated. No testing artifacts remain. ML training is unblocked.
</success_criteria>

<output>
Phase 133 is complete. ML training (Phase 134+) is unblocked.
</output>
