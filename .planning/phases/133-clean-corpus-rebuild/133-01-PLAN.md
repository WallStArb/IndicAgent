---
phase: 133-clean-corpus-rebuild
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/133-clean-corpus-rebuild/133-C2-VERIFICATION.md
autonomous: true
# NOTE 2026-06-20: C2 is already verified correct. This plan is a documentation-only task.
# intelligence_features already has functional column names; TIER_DB_COLUMNS mapping confirmed.
# Execution is a quick DB query + MEMORY.md close. No schema changes needed.

must_haves:
  truths:
    - "TIER_DB_COLUMNS mapping in schemas.py maps i1->technical_indicators, i2->composite_events, i3->regime_features, i4->confluence_scores, i5->pattern_detections, i6->cross_timeframe_context end-to-end through feature_writer to live DB columns"
    - "intelligence_features table has columns: technical_indicators, composite_events, regime_features, confluence_scores, pattern_detections, cross_timeframe_context (not i1..i6)"
    - "133-C2-VERIFICATION.md exists and records the column name verification result"
    - "MEMORY.md open decision item about intelligence_features column naming is closed"
  artifacts:
    - path: ".planning/phases/133-clean-corpus-rebuild/133-C2-VERIFICATION.md"
      provides: "C2 column naming end-to-end verification report and MEMORY.md closure"
      contains: "VERIFIED"
    - path: "src/intelligence/schemas.py"
      provides: "TIER_DB_COLUMNS canonical mapping"
      contains: "technical_indicators"
  key_links:
    - from: "src/intelligence/schemas.py TIER_DB_COLUMNS"
      to: "intelligence_features table columns"
      via: "feature_writer at write time translation"
      pattern: "TIER_DB_COLUMNS.*technical_indicators"
---

<objective>
Verify the intelligence_features column naming is already correct end-to-end (schemas.py -> feature_writer -> DB), then close the open MEMORY.md decision item. No migration is needed — this is a verification + documentation task.

Purpose: Close the open MEMORY.md decision before the rebuild so the corpus is produced against a verified schema baseline.
Output: 133-C2-VERIFICATION.md confirming functional column names end-to-end; MEMORY.md item closed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/133-clean-corpus-rebuild/133-CONTEXT.md
@src/intelligence/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify column naming end-to-end and close MEMORY.md open item</name>
  <files>
    .planning/phases/133-clean-corpus-rebuild/133-C2-VERIFICATION.md
  </files>
  <read_first>
    - src/intelligence/schemas.py (TIER_DB_COLUMNS at lines 35-44)
    - src/persistence/feature_writer_agent.py or services/feature_writer_agent.py (find the write path that translates tier codes to DB column names)
  </read_first>
  <action>
    1. Read src/intelligence/schemas.py lines 35-44 to confirm TIER_DB_COLUMNS maps: i1->technical_indicators, i2->composite_events, i3->regime_features, i4->confluence_scores, i5->pattern_detections, i6->cross_timeframe_context, i7->trading_signals, smc->smc.

    2. Find the feature_writer service (grep -r "TIER_DB_COLUMNS" src/ services/ to locate the write callsite). Confirm it reads TIER_DB_COLUMNS to translate IntelligenceEvent tier-code fields to DB column names at write time.

    3. Verify the live DB columns match:
       PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d intelligence_features" | grep -E "technical_indicators|composite_events|regime_features|confluence_scores|pattern_detections|cross_timeframe_context"
       Expected: all 6 functional names present as JSONB columns. None of i1, i2, i3, i4, i5, i6 should appear.

    4. Confirm validate_intelligence_features_columns() function in schemas.py (lines 47-64) would pass against the live DB: it checks every TIER_DB_COLUMNS value exists as a real column.

    5. Write .planning/phases/133-clean-corpus-rebuild/133-C2-VERIFICATION.md with:
       - Status: VERIFIED
       - schemas.py TIER_DB_COLUMNS mapping (paste exact dict)
       - feature_writer callsite location and how it uses TIER_DB_COLUMNS
       - DB column confirmation (output of \d query or psql column list)
       - Decision closure: "The intelligence_features columns are already functional names per Phase 104 migration. No rename migration is needed. MEMORY.md open item closed: 'intelligence_features column naming — resolved, functional names confirmed end-to-end.'"

    6. Open ~/.claude/projects/-home-bg-dev-indicagent/memory/MEMORY.md and close the open decision:
       Find: "**intelligence_features column naming**: Phase 122 applied..."
       Replace with: "**intelligence_features column naming**: RESOLVED (Phase 133) — DB columns are already functional names (technical_indicators, composite_events, regime_features, confluence_scores, pattern_detections, cross_timeframe_context) per Phase 104 migration. TIER_DB_COLUMNS in schemas.py is the canonical mapping; feature_writer translates tier codes at write time. No rename migration needed."
  </action>
  <verify>
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT column_name FROM information_schema.columns WHERE table_name = 'intelligence_features' AND column_name IN ('technical_indicators','composite_events','regime_features','confluence_scores','pattern_detections','cross_timeframe_context') ORDER BY column_name;"
    Expected: 6 rows returned, exactly those 6 functional names.

    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT column_name FROM information_schema.columns WHERE table_name = 'intelligence_features' AND column_name IN ('i1','i2','i3','i4','i5','i6');"
    Expected: 0 rows (no tier-code column names exist in DB).
  </verify>
  <done>
    133-C2-VERIFICATION.md exists, contains "VERIFIED", lists all 6 functional column names confirmed in live DB. MEMORY.md open decision item replaced with RESOLVED entry. No tier-code column names exist in intelligence_features.
  </done>
</task>

</tasks>

<verification>
- grep "VERIFIED" .planning/phases/133-clean-corpus-rebuild/133-C2-VERIFICATION.md -- must return a match
- PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM information_schema.columns WHERE table_name='intelligence_features' AND column_name IN ('technical_indicators','composite_events','regime_features','confluence_scores','pattern_detections','cross_timeframe_context');" -- must return 6
</verification>

<success_criteria>
All 6 functional column names confirmed in live DB. Zero tier-code names (i1-i6) in intelligence_features. MEMORY.md open decision closed with RESOLVED status. 133-C2-VERIFICATION.md committed.
</success_criteria>

<output>
After completion, create `.planning/phases/133-clean-corpus-rebuild/133-01-SUMMARY.md`
</output>
