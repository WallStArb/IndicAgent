---
phase: 133-clean-corpus-rebuild
plan: "04"
type: execute
wave: 3
depends_on:
  - "133-01"
  - "133-02"
  - "133-03"
files_modified: []
autonomous: true

must_haves:
  truths:
    - "All D-07 signal-derived tables are empty after TRUNCATE (verified by row count queries)"
    - "signal_events row count is within 2% of 1,036,513 after backfill replay completes"
    - "intelligence_features row count is positive and non-trivially large (>4M rows)"
    - "run_historical_pipeline.py --replay-only --include-rolled --client-id 40 --workers 8 exits with code 0"
    - "EXPLAIN ANALYZE on the B6 integrity assertion SQL was run before the rebuild to pre-diagnose timeout risk"
  artifacts:
    - path: ".planning/phases/133-clean-corpus-rebuild/133-TRUNCATE-VERIFY.md"
      provides: "Pre-TRUNCATE dry-run output and post-TRUNCATE row counts confirming all tables empty"
      contains: "TRUNCATE complete"
  key_links:
    - from: "reset_pipeline_data.py --confirm --wipe-only"
      to: "all D-07 tables"
      via: "TRUNCATE TABLE ... CASCADE"
      pattern: "Wipe complete"
    - from: "run_historical_pipeline.py"
      to: "signal_events"
      via: "replay I1-I7 pipeline per symbol/TF"
      pattern: "signal_events.*1036513"
---

<objective>
Execute the full corpus rebuild: TRUNCATE (D-03 step 1) followed by backfill replay (D-03 step 2). This is the point of no return — all prior plans must be complete and verified before this runs. The TRUNCATE is irreversible; the backfill takes multiple hours.

Purpose: Produce a clean signal corpus from scratch with all Phase 131 and 132 fixes applied.
Output: ~1,036,513 signal_events rows from 35 eligible plugins across all active-contract instruments.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/133-clean-corpus-rebuild/133-CONTEXT.md
@.planning/phases/133-clean-corpus-rebuild/133-01-SUMMARY.md
@.planning/phases/133-clean-corpus-rebuild/133-02-SUMMARY.md
@.planning/phases/133-clean-corpus-rebuild/133-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-rebuild verification — confirm prerequisites and pre-diagnose B6 timeout risk</name>
  <files>
    .planning/phases/133-clean-corpus-rebuild/133-TRUNCATE-VERIFY.md
  </files>
  <read_first>
    - production/scripts/run_historical_pipeline.py (search for _assert_backfill_integrity to find the integrity assertion SQL)
    - .planning/phases/133-clean-corpus-rebuild/133-01-SUMMARY.md
    - .planning/phases/133-clean-corpus-rebuild/133-02-SUMMARY.md
    - .planning/phases/133-clean-corpus-rebuild/133-03-SUMMARY.md
  </read_first>
  <action>
    **Step 1: Confirm all prerequisite plans succeeded.**
    Read 133-01-SUMMARY.md, 133-02-SUMMARY.md, 133-03-SUMMARY.md. If any plan does not show a successful completion, STOP and resolve before continuing.

    **Step 2: Confirm key signals of readiness.**
    - grep "CASCADE" production/scripts/reset_pipeline_data.py -- must show single TRUNCATE ... CASCADE (B4 applied)
    - grep "warmup" production/scripts/run_historical_pipeline.py -- must return zero results (B5 applied)
    - PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT * FROM timescaledb_information.hypertables WHERE hypertable_name = 'trade_frames';" -- must return 1 row (migration 149 applied)

    **Step 3: Pre-diagnose B6 integrity assertion timeout risk.**
    Find _assert_backfill_integrity in production/scripts/run_historical_pipeline.py. Locate the SQL it runs. Run EXPLAIN ANALYZE on it against the live DB (which still has data at this point, before TRUNCATE):

    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "EXPLAIN ANALYZE <paste the assertion SQL here>"

    If the query plan shows a sequential scan on intelligence_features or signal_events with estimated cost > 10M, it will likely timeout on a fresh full corpus. Note the plan and if necessary, document that the assertion should be chunked by symbol or time window in run_historical_pipeline.py.

    **Step 4: Dry-run TRUNCATE to confirm CASCADE scope.**
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent << 'SQL'
    BEGIN;
    TRUNCATE TABLE
        signal_events,
        trade_frames,
        trade_executions,
        intelligence_features,
        signal_metrics_dq_failures,
        llm_calls,
        llm_model_scores,
        ml_signal_training,
        shadow_transition_log,
        swarm_agent_weights,
        alpha_multiplier_shadow,
        setup_performance,
        signal_ai_enrichment,
        signal_lineage,
        signal_metrics,
        signal_metrics_ic,
        signal_transform_log
    CASCADE;
    -- If CASCADE reveals additional tables, list them here.
    ROLLBACK;
    SQL

    Record all tables that CASCADE affects. Verify the list includes all D-07 tables and no "preserve" tables (market_data_ohlcv, instruments, contract_metadata, config_state).

    **Step 5: Write 133-TRUNCATE-VERIFY.md** with:
    - Prerequisite plan check results (all three summaries confirmed)
    - B6 EXPLAIN ANALYZE plan summary (will it timeout? what is the estimated cost?)
    - Dry-run CASCADE scope (list of tables affected)
    - Readiness verdict: PROCEED or BLOCKED (with reason)
  </action>
  <verify>
    cat .planning/phases/133-clean-corpus-rebuild/133-TRUNCATE-VERIFY.md
    Expected: file exists, contains "PROCEED" in readiness verdict.
  </verify>
  <done>
    133-TRUNCATE-VERIFY.md exists with readiness verdict = PROCEED. All prerequisite checks passed. B6 timeout risk assessed. CASCADE scope confirmed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute TRUNCATE then full backfill replay</name>
  <!-- NOTE 2026-06-20: reset_pipeline_data.py now covers ~50 tables (much more than the 17
       listed in CONTEXT D-07). Invoke the script — do not duplicate the table list inline.
       The CASCADE handles FK dependencies automatically. -->
  <files>
    .planning/phases/133-clean-corpus-rebuild/133-TRUNCATE-VERIFY.md
  </files>
  <read_first>
    - .planning/phases/133-clean-corpus-rebuild/133-TRUNCATE-VERIFY.md (must say PROCEED)
  </read_first>
  <action>
    **GATE: 133-TRUNCATE-VERIFY.md must say PROCEED before continuing.**

    **Step 1: Stop lifecycle-writing services.**
    sudo systemctl stop indicagent-intelligence-pipeline indicagent-feature-writer indicagent-signal-tracker 2>/dev/null || true
    Verify stopped: systemctl is-active indicagent-intelligence-pipeline -- must return "inactive"

    **Step 2: Execute TRUNCATE using fixed reset_pipeline_data.py.**
    python production/scripts/reset_pipeline_data.py --confirm --wipe-only

    Expected output: "TRUNCATED all signal-derived tables (CASCADE)" and "RESET shadow_registry eval stats" and "Wipe complete."

    If the script exits non-zero, STOP. Do not proceed to backfill.

    **Step 3: Verify all D-07 tables are empty.**
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "
    SELECT tablename, (SELECT COUNT(*) FROM information_schema.tables t WHERE t.table_name = tablename LIMIT 1) as exists,
    CASE tablename
      WHEN 'signal_events' THEN (SELECT COUNT(*) FROM signal_events)
      WHEN 'trade_frames' THEN (SELECT COUNT(*) FROM trade_frames)
      WHEN 'trade_executions' THEN (SELECT COUNT(*) FROM trade_executions)
      WHEN 'intelligence_features' THEN (SELECT COUNT(*) FROM intelligence_features)
      WHEN 'llm_calls' THEN (SELECT COUNT(*) FROM llm_calls)
    END as row_count
    FROM (VALUES ('signal_events'),('trade_frames'),('trade_executions'),('intelligence_features'),('llm_calls')) t(tablename);"

    All counts must be 0. If any is non-zero, STOP — the TRUNCATE did not complete correctly.

    **Step 4: Start backfill replay.**
    Run in background with output logged:
    .venv/bin/python -u production/scripts/run_historical_pipeline.py \
        --replay-only \
        --include-rolled \
        --client-id 40 \
        --workers 8 \
        > /tmp/133-backfill.log 2>&1

    Monitor progress:
    tail -f /tmp/133-backfill.log

    This is a multi-hour operation. Expected to produce ~1,036,513 signal_events rows.

    **Step 5: Verify backfill row count when complete.**
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM signal_events;"
    Expected: between 1,015,783 and 1,057,243 (1,036,513 ± 2%)

    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM intelligence_features;"
    Expected: > 4,000,000 (substantially non-zero).

    **Step 6: Append backfill results to 133-TRUNCATE-VERIFY.md.**
    Add section "Backfill Results":
    - signal_events count: <actual count>
    - intelligence_features count: <actual count>
    - trade_frames count: <actual count>
    - Exit code: 0 (confirm)
    - Status: BACKFILL COMPLETE
  </action>
  <verify>
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM signal_events;"
    Expected: between 1,015,783 and 1,057,243.

    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(DISTINCT setup_plugin) FROM signal_events;"
    Expected: between 30 and 36 (at least 30 distinct plugins; exact count verified in plan 133-06).

    grep "Exit code: 0\|BACKFILL COMPLETE" .planning/phases/133-clean-corpus-rebuild/133-TRUNCATE-VERIFY.md
    Expected: match found.
  </verify>
  <done>
    All D-07 tables were empty after TRUNCATE. run_historical_pipeline.py --replay-only --include-rolled --client-id 40 --workers 8 exited 0. signal_events count is within 2% of 1,036,513. 133-TRUNCATE-VERIFY.md records BACKFILL COMPLETE.
  </done>
</task>

</tasks>

<verification>
- PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM signal_events;" returns between 1,015,783 and 1,057,243
- PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM intelligence_features;" returns > 4,000,000
- grep "BACKFILL COMPLETE" .planning/phases/133-clean-corpus-rebuild/133-TRUNCATE-VERIFY.md returns a match
</verification>

<success_criteria>
TRUNCATE complete (all D-07 tables at zero rows). Backfill replay completes exit-0 with signal_events count within 2% of 1,036,513 baseline. 133-TRUNCATE-VERIFY.md records the pre-check, dry-run scope, and backfill results.
</success_criteria>

<output>
After completion, create `.planning/phases/133-clean-corpus-rebuild/133-04-SUMMARY.md`
</output>
