---
phase: 133-clean-corpus-rebuild
plan: "05"
type: execute
wave: 4
depends_on:
  - "133-04"
files_modified: []
autonomous: true

must_haves:
  truths:
    - "lifecycle_replay.py --workers 8 --commit-every 500 completes with exit code 0"
    - "_verify_replay gate passes: stale_unresolved=0, target_no_pnl=0, orphan_signal_events=0"
    - "trade_executions contains rows (lifecycle_replay wrote exit outcomes)"
    - "stopped_at_entry rows exist in trade_executions (lifecycle_replay.py writes this exit_reason)"
  artifacts:
    - path: ".planning/phases/133-clean-corpus-rebuild/133-LIFECYCLE-VERIFY.md"
      provides: "lifecycle_replay completion record and _verify_replay gate output"
      contains: "stale_unresolved=0"
  key_links:
    - from: "lifecycle_replay.py"
      to: "trade_executions"
      via: "INSERT with exit_reason per evaluated signal frame"
      pattern: "lifecycle_replay.*complete"
    - from: "_verify_replay"
      to: "signal_events"
      via: "COUNT(DISTINCT signal_id) orphan check"
      pattern: "orphan_signal_events=0"
---

<objective>
Run lifecycle_replay to compute outcomes (pnl_r, exits, MAE/MFE) for all signals produced by the backfill. Then run the _verify_replay integrity gate to confirm zero stale/orphan signals. This is D-03 steps 3 and 4.

Purpose: Without lifecycle replay, trade_executions is empty and the corpus has no outcome labels. The _verify_replay gate is the hard go/no-go criterion before acceptance criteria are checked.
Output: trade_executions populated with outcomes. _verify_replay returns 0/0/0.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/133-clean-corpus-rebuild/133-CONTEXT.md
@.planning/phases/133-clean-corpus-rebuild/133-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run lifecycle_replay and verify integrity gate</name>
  <files>
    .planning/phases/133-clean-corpus-rebuild/133-LIFECYCLE-VERIFY.md
  </files>
  <read_first>
    - .planning/phases/133-clean-corpus-rebuild/133-04-SUMMARY.md (confirm backfill completed with exit 0 and signal_events count within range)
    - production/scripts/lifecycle_replay.py (find --commit-every flag usage and _verify_replay function)
  </read_first>
  <action>
    **GATE: 133-04-SUMMARY.md must show BACKFILL COMPLETE with signal_events between 1,015,783 and 1,057,243.**

    **Step 1: Confirm backfill produced expected rows.**
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM signal_events;"
    Must be between 1,015,783 and 1,057,243. If not, STOP and document.

    **Step 2: Confirm trade_executions is still empty (lifecycle has not run yet).**
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM trade_executions;"
    Expected: 0 (backfill does not populate trade_executions).

    **Step 3: Run lifecycle_replay.**
    .venv/bin/python -u production/scripts/lifecycle_replay.py \
        --workers 8 \
        --commit-every 500 \
        > /tmp/133-lifecycle.log 2>&1

    Monitor: tail -f /tmp/133-lifecycle.log
    This is a multi-hour operation.

    **Step 4: Verify exit code 0.**
    echo "Exit code: $?"
    If non-zero, STOP. Do not proceed to acceptance criteria. Document the failure.

    **Step 5: Run _verify_replay gate.**
    The _verify_replay function is internal to lifecycle_replay.py. It can be triggered by running lifecycle_replay again (it runs automatically at end) or by calling it directly if there is a standalone flag.

    Check lifecycle_replay.py for a --verify-only flag. If it exists:
    .venv/bin/python -u production/scripts/lifecycle_replay.py --verify-only

    If not, re-read the end of the lifecycle replay log to find the _verify_replay output.

    The output must show:
    - stale_unresolved=0
    - target_no_pnl=0
    - orphan_signal_events=0

    If any is non-zero, STOP and document. The corpus is not clean.

    **Step 6: Sanity-check trade_executions.**
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT exit_reason, COUNT(*) FROM trade_executions GROUP BY 1 ORDER BY 2 DESC;"
    Expected:
    - Multiple exit_reason categories present (ttl_expired_ahead, ttl_expired_behind, target_1, target_full, stopped_in, stopped_at_entry, etc.)
    - stopped_at_entry present (this is what plan 133-06 checks the rate of)
    - Total trade_executions count is roughly proportional to signal_events count (each signal can have multiple frames)

    Note: DO NOT interpret stopped_at_entry rate here — that is plan 133-06's job. Just confirm the column is populated.

    **Step 7: Write 133-LIFECYCLE-VERIFY.md** with:
    - Pre-lifecycle trade_executions count: 0
    - lifecycle_replay exit code: 0
    - _verify_replay output: stale_unresolved=<N>, target_no_pnl=<N>, orphan_signal_events=<N>
    - Post-lifecycle trade_executions count: <actual>
    - exit_reason distribution (paste SQL output)
    - Verdict: LIFECYCLE COMPLETE (if _verify_replay is 0/0/0) or BLOCKED (with reason)
  </action>
  <verify>
    grep "stale_unresolved=0" .planning/phases/133-clean-corpus-rebuild/133-LIFECYCLE-VERIFY.md
    Expected: match found.

    grep "target_no_pnl=0" .planning/phases/133-clean-corpus-rebuild/133-LIFECYCLE-VERIFY.md
    Expected: match found.

    grep "orphan_signal_events=0" .planning/phases/133-clean-corpus-rebuild/133-LIFECYCLE-VERIFY.md
    Expected: match found.

    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM trade_executions;"
    Expected: > 0 (lifecycle replay wrote exit outcomes).
  </verify>
  <done>
    lifecycle_replay.py exited 0. _verify_replay output shows stale_unresolved=0, target_no_pnl=0, orphan_signal_events=0. trade_executions count > 0. stopped_at_entry exit_reason is present in trade_executions. 133-LIFECYCLE-VERIFY.md records LIFECYCLE COMPLETE.
  </done>
</task>

</tasks>

<verification>
- grep "LIFECYCLE COMPLETE" .planning/phases/133-clean-corpus-rebuild/133-LIFECYCLE-VERIFY.md returns match
- grep "stale_unresolved=0\|target_no_pnl=0\|orphan_signal_events=0" .planning/phases/133-clean-corpus-rebuild/133-LIFECYCLE-VERIFY.md returns 3 matches
- PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM trade_executions;" returns > 0
</verification>

<success_criteria>
lifecycle_replay.py completes exit-0. _verify_replay gate passes with all three counters at zero. trade_executions populated with outcomes including stopped_at_entry rows. 133-LIFECYCLE-VERIFY.md records all outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/133-clean-corpus-rebuild/133-05-SUMMARY.md`
</output>
