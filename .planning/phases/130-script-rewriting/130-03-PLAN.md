---
phase: 130-script-rewriting
plan: 03
type: execute
wave: 3
depends_on: [130-01, 130-02]
requirements: [REWRITE-01]
files_modified:
  - services/signal_writer.py
  - services/lifecycle_writer.py
autonomous: true

must_haves:
  truths:
    - "signal_writer groups incoming signals by signal_id and writes one signal_events row + N trade_frames rows per group in one transaction"
    - "lifecycle_writer status transitions UPDATE signal_events.status; activation metadata UPDATEs trade_frames.frame_details"
    - "Both writers load batch_size, flush_interval, max_buffer from APR at _setup() instead of class constants"
  artifacts:
    - path: "services/signal_writer.py"
      provides: "G0 grouping write path to 3-table schema"
      contains: "defaultdict"
    - path: "services/lifecycle_writer.py"
      provides: "status + frame_details lifecycle writes"
      contains: "update_signal_status"
  key_links:
    - from: "services/signal_writer.py _parse_payload"
      to: "SignalEventsRepository.insert_signal_with_frames"
      via: "group by signal_id, build event + frame rows"
      pattern: "insert_signal_with_frames"
    - from: "services/lifecycle_writer.py"
      to: "SignalEventsRepository.update_signal_status / update_frame_details"
      via: "lifecycle transition flush"
      pattern: "update_frame_details"
---

<objective>
Rewrite the two live write-path writers onto the 3-table schema. signal_writer implements G0 grouping (group by signal_id, one signal_events row + N trade_frames rows per group, single transaction). lifecycle_writer routes status transitions to signal_events.status and activation metadata to trade_frames.frame_details. Both migrate their hardcoded batch/flush constants to APR.

Purpose: This is the live production write path. CONTEXT D-04 dictates signal_writer is rewritten first so there is no gap in live writes. Both writers consume the SignalEventsRepository from plan 02.
Output: signal_writer.py with G0 grouping + APR loading; lifecycle_writer.py with status/frame_details writes + APR loading.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-script-rewriting/130-CONTEXT.md
@.planning/phases/130-script-rewriting/130-RESEARCH.md
@src/persistence/repository/signal_events_repository.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite signal_writer with G0 grouping and APR loading</name>
  <read_first>
    - services/signal_writer.py (current 265-line write path; read the full file — __init__ class constants, _setup, _parse_payload, the LedgerEntry construction, and the repository call site)
    - src/persistence/repository/signal_events_repository.py (the insert_signal_with_frames signature and the SignalEventRow/TradeFrameRow dataclasses produced in plan 02)
    - .planning/phases/130-script-rewriting/130-RESEARCH.md (Pattern 1 G0 grouping, Pattern 2 signal_events INSERT fields, Pattern 3 trade_frames INSERT fields, Pattern 7 APR loading, Pitfall 6 direction encoding, Pitfall 7 schema version int)
    - .planning/phases/130-script-rewriting/130-CONTEXT.md (D-02 G0 contract, D-08 new columns, D-12 APR keys feature.signal_writer.*)
  </read_first>
  <action>
    Rewrite services/signal_writer.py _parse_payload to implement G0 grouping: groups = defaultdict(list) keyed by signal["signal_id"]; for each group, detection = signals[0] builds one signal_events row, and each signal in the group builds one trade_frames row (one per entry_type). Call repository.insert_signal_with_frames per group so signal_events + trade_frames commit atomically. Convert direction int (1/-1) to text "long"/"short" before building the signal_events row. Pass signal_schema_version as the int constant (no str()). Leave concurrent_signal_count and concurrent_plugins NULL (separate-process constraint, RESEARCH Pitfall 5). Remove the class-level BATCH_SIZE=100, FLUSH_INTERVAL_SECS=5.0, MAX_BUFFER_SIZE=10000 constants; load them in _setup() via config_service.get with keys feature.signal_writer.batch_size (default 100), feature.signal_writer.flush_interval_secs (default 5.0), feature.signal_writer.max_buffer_size (default 10000), assigning to self._batch_size / self._flush_interval / self._max_buffer_size. Update the import to SignalEventsRepository. Fix the module docstring line 2: "persists all I7 signals to signal_ledger hypertable" -> "persists I7 signals to signal_events/trade_frames (3-table schema)". Keep validate_signal gate and DLQ behavior intact. Use except X as error: naming; never pass event= to structlog.
  </action>
  <verify>.venv/bin/python -c "import services.signal_writer as m; assert not hasattr(m.SignalWriter,'BATCH_SIZE'); import inspect; src=inspect.getsource(m); assert 'defaultdict' in src and 'insert_signal_with_frames' in src and 'signal_events_repository' in src; print('ok')"</verify>
  <acceptance_criteria>
    - signal_writer.py imports from src.persistence.repository.signal_events_repository
    - signal_writer.py contains "defaultdict" (G0 grouping) and "insert_signal_with_frames"
    - SignalWriter class no longer defines BATCH_SIZE / FLUSH_INTERVAL_SECS / MAX_BUFFER_SIZE class attributes
    - signal_writer.py loads feature.signal_writer.batch_size from config_service (grep "feature.signal_writer.batch_size" matches)
    - Module docstring line 2 no longer contains "signal_ledger hypertable"
    - grep -c "signal_outcomes\|INSERT INTO signal_ledger " services/signal_writer.py returns 0
    - Verify command prints "ok"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewrite lifecycle_writer status + frame_details writes with APR loading</name>
  <read_first>
    - services/lifecycle_writer.py (current 234-line file; read the full file — class constants, _setup, the transition grouping by type, and the execute_batch / repository call sites that target signal_outcomes)
    - src/persistence/repository/signal_events_repository.py (update_signal_status, update_frame_details, record_execution signatures from plan 02)
    - .planning/phases/130-script-rewriting/130-RESEARCH.md (Pattern 5 lifecycle status UPDATE + frame_details UPDATE, "lifecycle_writer Activation Transition Mapping" code example, Pitfall 2 activation fields)
    - .planning/phases/130-script-rewriting/130-CONTEXT.md (signal_outcomes column mapping table; D-08 regime_at_activation; D-12 APR keys feature.lifecycle_writer.*)
  </read_first>
  <action>
    Rewrite services/lifecycle_writer.py so each lifecycle transition type maps to the 3-table schema: status transitions call repository.update_signal_status(signal_id, status) (signal_events.status). Activation transitions call update_signal_status(..., "active") then update_frame_details(signal_id, {activated_at: format_iso_ts(...), activation_price, zone_entry_pct, bars_to_activation, regime_at_activation}). Exit/resolution transitions call record_execution with actual_pnl_r, actual_fill_price (market_entry_price), exit_reason (outcome), exited_at — per the signal_outcomes column mapping table in CONTEXT.md. MAE/MFE shadow fields go to trade_frames.frame_details (already archived in Phase 129; for live writes append to frame_details JSONB). Remove class-level batch/flush/buffer constants; load feature.lifecycle_writer.batch_size (default 100), feature.lifecycle_writer.flush_interval_secs (default 5.0), feature.lifecycle_writer.max_buffer_size (default 10000) in _setup(). Update import to SignalEventsRepository. Fix module docstring line 2: "persists signal lifecycle transitions to signal_ledger" -> "persists signal lifecycle transitions to signal_events". Use format_iso_ts for timestamps; except X as error:; no event= kwarg to structlog.
  </action>
  <verify>.venv/bin/python -c "import services.lifecycle_writer as m, inspect; src=inspect.getsource(m); assert 'update_signal_status' in src and 'update_frame_details' in src and 'signal_events_repository' in src; assert 'INSERT INTO signal_outcomes' not in src and 'UPDATE signal_outcomes' not in src; print('ok')"</verify>
  <acceptance_criteria>
    - lifecycle_writer.py imports from src.persistence.repository.signal_events_repository
    - lifecycle_writer.py contains "update_signal_status" and "update_frame_details"
    - grep -c "signal_outcomes" services/lifecycle_writer.py returns 0
    - lifecycle_writer.py loads feature.lifecycle_writer.batch_size from config_service
    - Module docstring line 2 references signal_events, not signal_ledger
    - Activation transition writes regime_at_activation into frame_details (grep "regime_at_activation" matches)
    - Verify command prints "ok"
  </acceptance_criteria>
</task>

</tasks>

<verification>
- signal_writer G0 grouping produces one signal_events row + N trade_frames rows per signal_id, atomically
- lifecycle_writer routes status to signal_events, activation/exit metadata to trade_frames.frame_details / trade_executions
- Both writers load operational constants from APR
- No signal_outcomes or direct signal_ledger writes remain in either file
</verification>

<success_criteria>
- Live write path fully on 3-table schema with no production gap
- direction text-encoded, schema_version int, JSONB passed as dict
- APR-backed batch/flush/buffer for both writers
</success_criteria>

<output>
After completion, create `.planning/phases/130-script-rewriting/130-03-SUMMARY.md`
</output>
