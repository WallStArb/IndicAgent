---
phase: 130-script-rewriting
plan: 05
type: execute
wave: 3
depends_on: [130-01, 130-02]
requirements: [REWRITE-01]
files_modified:
  - src/api/routes/signals.py
  - src/api/routes/narrative.py
autonomous: true

must_haves:
  truths:
    - "Signals API endpoints query the signal_ledger_full view (renamed to signal_ledger after I8) and return only columns that exist in the new schema"
    - "API responses no longer reference dropped columns (signal_type, feature_tf, bucket_scores, staleness_score, staleness_trigger_reason, feature_schema_version, pipeline_lag_ms)"
    - "stop_basis is extracted from trade_frames.frame_details JSONB where still needed"
    - "narrative SignalContext uses tf instead of the dropped feature_tf column"
    - "All API window/threshold/result-count constants load from ui.signals.* APR keys"
  artifacts:
    - path: "src/api/routes/signals.py"
      provides: "3-table-backed signal endpoints with APR-driven windows"
      contains: "ui.signals."
    - path: "src/api/routes/narrative.py"
      provides: "narrative context query on signal_ledger_full / tf column"
      contains: "tf"
  key_links:
    - from: "src/api/routes/signals.py"
      to: "signal_ledger_full view (signal_ledger post-I8)"
      via: "SELECT from join view, dropped columns removed"
      pattern: "signal_ledger"
    - from: "src/api/routes/narrative.py construct_signal_context"
      to: "tf column"
      via: "row['tf'] replacing row['feature_tf']"
      pattern: "tf"
---

<objective>
Rewrite the read-path API routes onto the new schema. signals.py is the largest API file (1041 lines, most endpoints) and references several dropped columns. narrative.py uses the dropped feature_tf column. Both must query the signal_ledger_full view (which becomes signal_ledger after plan 07's I8 rename), drop columns that no longer exist, extract stop_basis from frame_details, and migrate UX/window constants to the ui.signals.* APR keys seeded in plan 01.

Purpose: CONTEXT D-10 mandates API endpoints use the signal_ledger_full view to minimize rewrite risk. RESEARCH discovery #5 and the column-gaps table enumerate the 8 dropped columns the API must stop selecting. D-12 + D-13 require ui.signals.* keys (now writable after plan 01).
Output: signals.py and narrative.py querying the view, free of dropped-column references, with ui.signals.* APR loading.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-script-rewriting/130-CONTEXT.md
@.planning/phases/130-script-rewriting/130-RESEARCH.md
@production/migrations/137_3table_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite signals.py queries and migrate UX constants to ui.signals.* APR</name>
  <read_first>
    - src/api/routes/signals.py (read the full file — every SELECT against signal_ledger / signal_ledger_full / signal_outcomes, the response object construction, and the hardcoded constants at the line numbers in D-12: 33, 72, 203, 334, 448, 454, 487, 527)
    - production/migrations/137_3table_schema.sql (the signal_ledger_full view column list — the authoritative surface of what the API may select)
    - .planning/phases/130-script-rewriting/130-RESEARCH.md (Column Gaps table: signal_type, feature_tf, pipeline_lag_ms, feature_schema_version, staleness_score, staleness_trigger_reason, bucket_scores dropped; stop_basis -> frame_details->>'stop_basis')
    - .planning/phases/130-script-rewriting/130-CONTEXT.md (D-10 view strategy, D-12 ui.signals.* keys + seed values, D-14 docstring update line 4)
  </read_first>
  <action>
    In src/api/routes/signals.py: point all signal queries at the signal_ledger_full view (it is renamed to signal_ledger in plan 07's I8 — write queries against signal_ledger_full now so they work pre-rename; plan 07 handles the post-rename reference sweep, but note any signal_ledger_full string here will need the rename). Remove these dropped columns from all SELECT clauses and response dicts: signal_type, feature_tf, bucket_scores, staleness_score, staleness_trigger_reason, feature_schema_version, pipeline_lag_ms. Where stop_basis is needed, extract it from trade_frames.frame_details via the view or an explicit join: frame_details->>'stop_basis'. Where feature_tf was returned, substitute tf. For any ML-specific endpoint needing trade_frames columns not in the view, write an explicit 3-table JOIN (D-10). Migrate every hardcoded constant to ui.signals.* APR keys loaded via config_service.get: recent_window_days (90, line 33), min_confidence (0.40, lines 72/334/454), min_cis_score (0.35, line 334), today_window_hours (24), yesterday_window_hours (48, line 448), short_window_days (7), medium_window_days (30), latency_threshold_minutes (5, line 487), max_results (500, line 203), top_n_results (10, line 527). Load these at request time or module setup via the ConfigService accessor the API layer already uses. Fix the module docstring line 4 per D-14: "Provides access to signal_ledger with optional JOIN..." -> "Queries signal_events/trade_frames via signal_ledger (join view)". Use format_iso_ts for timestamp serialization.
  </action>
  <verify>.venv/bin/python -c "import src.api.routes.signals; print('import ok')"</verify>
  <acceptance_criteria>
    - signals.py contains no reference to any of: signal_type, feature_tf, bucket_scores, staleness_score, staleness_trigger_reason, feature_schema_version, pipeline_lag_ms (grep each returns 0 matches in SELECT/response context)
    - signals.py loads ui.signals.recent_window_days and ui.signals.min_confidence from config_service (grep "ui.signals." matches)
    - signals.py module docstring no longer claims "access to signal_ledger with optional JOIN"
    - signals.py imports without error (verify prints "import ok")
    - No literal 0.40 / 90 / 500 magic numbers remain at the D-12 line locations (replaced by APR lookups)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update narrative.py to use tf instead of feature_tf</name>
  <read_first>
    - src/api/routes/narrative.py (read construct_signal_context at ~88-121, the SELECT at ~156-174 using sl.feature_tf and the f.tf JOIN condition, and the row access at ~247)
    - production/migrations/137_3table_schema.sql (confirm the view exposes tf, not feature_tf)
    - .planning/phases/130-script-rewriting/130-CONTEXT.md (D-09 narrative.py audit; D-14)
  </read_first>
  <action>
    In src/api/routes/narrative.py: the SELECT at ~156 selects sl.feature_tf and joins ON sl.feature_tf = f.tf; feature_tf is a dropped column. Replace sl.feature_tf with sl.tf (the view/signal_events provides tf), and update the JOIN condition to sl.tf = f.tf. Update construct_signal_context (timeframe=row["feature_tf"] at ~121) and the row access at ~247 to use row["tf"]. Keep the FROM signal_ledger_full reference (renamed to signal_ledger in plan 07; note for the rename sweep). Update the docstring at ~88-91 that references "signal_ledger + intelligence_features row" to reflect the join view. No other behavioral change.
  </action>
  <verify>.venv/bin/python -c "import src.api.routes.narrative as m, inspect; src=inspect.getsource(m); assert 'feature_tf' not in src; print('ok')"</verify>
  <acceptance_criteria>
    - narrative.py contains no "feature_tf" reference (grep returns 0)
    - narrative.py uses row["tf"] / sl.tf
    - narrative.py imports without error
    - Verify command prints "ok"
  </acceptance_criteria>
</task>

</tasks>

<verification>
- signals.py free of all 7 dropped columns; UX constants from ui.signals.* APR; stop_basis from frame_details
- narrative.py uses tf, not feature_tf
- Both modules import cleanly
</verification>

<success_criteria>
- API read path returns only columns present in the new schema
- All API window/threshold/result-count values are APR-backed and editable via /config/parameters
- No dropped-column references that would error after the DROP migration
</success_criteria>

<output>
After completion, create `.planning/phases/130-script-rewriting/130-05-SUMMARY.md`
</output>
