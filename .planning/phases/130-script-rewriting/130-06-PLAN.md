---
phase: 130-script-rewriting
plan: 06
type: execute
wave: 3
depends_on: [130-01, 130-02]
requirements: [REWRITE-01]
files_modified:
  - production/scripts/run_historical_pipeline.py
  - production/scripts/lifecycle_replay.py
  - production/scripts/feature_replay.py
autonomous: true

must_haves:
  truths:
    - "Historical backfill inserts signal_events + trade_frames (G0 grouping) via psycopg2, with deterministic uuid5 frame_ids"
    - "lifecycle_replay UPDATEs signal_events.status instead of writing signal_outcomes"
    - "feature_replay writes the 3-table schema (it was missing from the original D-09 file list)"
  artifacts:
    - path: "production/scripts/run_historical_pipeline.py"
      provides: "psycopg2 G0 insert to signal_events + trade_frames"
      contains: "INSERT INTO trade_frames"
    - path: "production/scripts/feature_replay.py"
      provides: "3-table replay writes"
      contains: "signal_events"
  key_links:
    - from: "production/scripts/run_historical_pipeline.py _batch_insert_signals"
      to: "signal_events + trade_frames"
      via: "execute_values with uuid5 frame_id"
      pattern: "INSERT INTO signal_events"
    - from: "production/scripts/lifecycle_replay.py"
      to: "signal_events.status"
      via: "UPDATE signal_events SET status"
      pattern: "UPDATE signal_events"
---

<objective>
Rewrite the three historical backfill/replay scripts onto the 3-table schema. run_historical_pipeline.py inserts signal_events + trade_frames with G0 grouping (psycopg2/execute_values). lifecycle_replay.py UPDATEs signal_events.status instead of writing signal_outcomes. feature_replay.py — discovered missing from CONTEXT D-09 (RESEARCH discovery #1) but it writes signal_ledger and signal_outcomes — is brought into scope and rewritten with the same G0 pattern.

Purpose: After plan 07 makes signal_ledger read-only / dropped, any script still inserting to it fails. These scripts feed the clean replay (Phase 127) which runs after Phase 130, so they must land data in the final 3-table schema. RESEARCH Pitfall 3 flags feature_replay explicitly.
Output: All three scripts writing signal_events + trade_frames (+ trade_executions for resolution), with deterministic frame_ids and direction text encoding.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-script-rewriting/130-CONTEXT.md
@.planning/phases/130-script-rewriting/130-RESEARCH.md
@production/scripts/migrate_signal_ledger.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite run_historical_pipeline.py signal inserts (G0, psycopg2)</name>
  <read_first>
    - production/scripts/run_historical_pipeline.py (read _batch_insert_signals at ~891 and any signal_outcomes seed insert; the file is 2422+ lines — focus on the signal/outcome insert paths and the import of SignalLedgerRepository)
    - production/scripts/migrate_signal_ledger.py (established psycopg2 patterns: execute_values, uuid5 frame_id, direction int-to-text, column mapping for signal_events and trade_frames)
    - .planning/phases/130-script-rewriting/130-RESEARCH.md ("run_historical_pipeline.py psycopg2 Pattern" code example — _INSERT_SIGNAL_EVENTS_SYNC_SQL and _INSERT_TRADE_FRAMES_SYNC_SQL with VALUES %s and ON CONFLICT)
    - production/migrations/137_3table_schema.sql (authoritative column names/types)
  </read_first>
  <action>
    Rewrite the signal insert path in run_historical_pipeline.py to the 3-table schema using psycopg2 execute_values (the script uses psycopg2, not asyncpg). Group signals by signal_id (G0); for each group insert one signal_events row (VALUES %s, ON CONFLICT (signal_id, ts) DO NOTHING) and N trade_frames rows (VALUES %s, ON CONFLICT (frame_id) DO NOTHING). frame_id = uuid5(NAMESPACE_DNS, f"{signal_id}:{entry_type}"); for single-frame historical backfill use entry_type "at_close". Convert direction 1/-1 to "long"/"short" text. Set counterfactual_pnl_r NULL, was_selected per the signal dict, is_backfill TRUE, status 'pending'. Populate factor_scores / context_features JSONB from the signal dict (ECL fields, Phase 123). Pass signal_schema_version as the int constant. Move stop_basis and other stop metadata into frame_details JSONB. Update the import to SignalEventsRepository. Remove any signal_outcomes seed insert (status starts in signal_events; lifecycle_replay updates it).
  </action>
  <verify>.venv/bin/python -c "import ast; ast.parse(open('production/scripts/run_historical_pipeline.py').read()); print('parse ok')"</verify>
  <acceptance_criteria>
    - run_historical_pipeline.py contains "INSERT INTO signal_events" and "INSERT INTO trade_frames"
    - run_historical_pipeline.py contains "uuid5" for frame_id
    - grep -c "INSERT INTO signal_outcomes\|INSERT INTO signal_ledger " production/scripts/run_historical_pipeline.py returns 0
    - run_historical_pipeline.py parses (verify prints "parse ok")
    - Direction conversion to "long"/"short" present (grep "long" and "short" in the insert path)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewrite lifecycle_replay.py and feature_replay.py</name>
  <read_first>
    - production/scripts/lifecycle_replay.py (read the signal_outcomes UPDATE paths; the file is 1309+ lines — focus on where it writes status/outcome to signal_outcomes)
    - production/scripts/feature_replay.py (read the full ~130-line file: INSERT INTO signal_ledger at line 82, ON CONFLICT at 115, INSERT INTO signal_outcomes at 125)
    - .planning/phases/130-script-rewriting/130-RESEARCH.md (Pitfall 3 feature_replay; the run_historical psycopg2 pattern reused here; signal_outcomes column mapping)
    - production/migrations/137_3table_schema.sql (column authority)
  </read_first>
  <action>
    lifecycle_replay.py: replace signal_outcomes writes. Status transitions become UPDATE signal_events SET status=%s WHERE signal_id=%s::uuid (idempotent). Resolution/exit data (pnl_r, outcome, market_entry_price) becomes INSERT INTO trade_executions (actual_pnl_r, actual_fill_price, exit_reason, exited_at) ON CONFLICT DO NOTHING. Activation metadata (activated_at, activation_price, zone_entry_pct, bars_to_activation) becomes UPDATE trade_frames SET frame_details = frame_details || %s::jsonb WHERE signal_id=%s::uuid. MAE/MFE go to trade_frames.frame_details.
    feature_replay.py: this file (missing from CONTEXT D-09) writes INSERT INTO signal_ledger (line 82) and INSERT INTO signal_outcomes (line 125). Rewrite both using the same G0 pattern as Task 1: one signal_events row + N trade_frames rows per signal_id group, deterministic uuid5 frame_id, direction text, ON CONFLICT (signal_id, ts) / (frame_id) DO NOTHING. Remove the signal_outcomes insert; status starts as 'pending' in signal_events. Update import to SignalEventsRepository.
    Both files: except X as error:; format_iso_ts for timestamps; no event= to structlog.
  </action>
  <verify>.venv/bin/python -c "import ast; ast.parse(open('production/scripts/lifecycle_replay.py').read()); ast.parse(open('production/scripts/feature_replay.py').read()); print('parse ok')"</verify>
  <acceptance_criteria>
    - lifecycle_replay.py contains "UPDATE signal_events SET status" and contains no "signal_outcomes"
    - feature_replay.py contains "signal_events" and "trade_frames"
    - grep -c "signal_outcomes\|INSERT INTO signal_ledger " production/scripts/feature_replay.py returns 0
    - feature_replay.py contains "uuid5" for frame_id
    - Both files parse (verify prints "parse ok")
  </acceptance_criteria>
</task>

</tasks>

<verification>
- run_historical_pipeline + feature_replay insert signal_events + trade_frames with G0 grouping and uuid5 frame_ids
- lifecycle_replay UPDATEs signal_events.status and INSERTs trade_executions
- No script writes signal_ledger or signal_outcomes
</verification>

<success_criteria>
- All replay/backfill scripts produce data in the final 3-table schema (ready for Phase 127 clean replay)
- Deterministic frame_ids enable idempotent re-replay
- feature_replay (RESEARCH-discovered gap) brought into scope and rewritten
</success_criteria>

<output>
After completion, create `.planning/phases/130-script-rewriting/130-06-SUMMARY.md`
</output>
