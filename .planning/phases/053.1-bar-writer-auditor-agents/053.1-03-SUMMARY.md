---
phase: 053.1-bar-writer-auditor-agents
plan: 03
subsystem: infra
tags: [systemd, ohlcv, bar-writer, bar-auditor, gap-fill, feature-compute-agent]

requires:
  - phase: 053.1-01
    provides: BarWriterAgent service (services/bar_writer_agent.py)
  - phase: 053.1-02
    provides: BarAuditorAgent service (services/bar_auditor_agent.py)

provides:
  - FCA without _ohlcv_buffer (OHLCV persistence fully delegated to BarWriterAgent)
  - indicagent-bar-writer systemd unit installed and enabled
  - indicagent-bar-auditor systemd unit installed and enabled
  - gap_fill_service archived (services/_archived_gap_fill_service.py)
  - CLAUDE.md reflecting new service topology with Bar Writer + Bar Auditor rows

affects: [feature-compute, bar-writer-agent, bar-auditor-agent, service-topology, docs]

tech-stack:
  added: []
  patterns:
    - "FCA is DB-write-free in hot path — OHLCV delegated to BarWriterAgent (D-08)"
    - "Legacy service archived with _ prefix rather than deleted — preserves reference code"

key-files:
  created:
    - services/_archived_gap_fill_service.py (renamed from gap_fill_service.py)
  modified:
    - services/feature_compute_agent.py (removed _ohlcv_buffer and all OHLCV write logic)
    - CLAUDE.md (Active Services table + market_data_ohlcv description + Cold tier)

key-decisions:
  - "Pre-existing ruff E501 at line 638 (was 705) deferred — out of scope per deviation scope boundary rule"
  - "gap_fill_service renamed to _archived_gap_fill_service.py (not deleted) — preserves reference implementation"
  - "systemd units depend on indicagent-data-provider and indicagent-bar-writer respectively via After/Wants"

patterns-established:
  - "FCA is DB-read-only: _seed_bar_history warmup reads, no writes"

requirements-completed: []

duration: 12min
completed: 2026-03-28
---

# Phase 053.1 Plan 03: Cleanup, Systemd, Retire gap_fill Summary

**FCA hot path made DB-write-free (D-08), two new systemd units installed and enabled, gap_fill_service archived, CLAUDE.md updated with Bar Writer/Auditor service topology**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-03-28T17:45:00Z
- **Completed:** 2026-03-28T17:57:00Z
- **Tasks:** 2
- **Files modified:** 3 (feature_compute_agent.py, CLAUDE.md, gap_fill_service.py→_archived)

## Accomplishments

- Removed all OHLCV write logic from FeatureComputeAgent: _OHLCV_BATCH_SIZE, _OHLCV_FLUSH_INTERVAL, _INSERT_OHLCV_SQL constants; _ohlcv_buffer, _ohlcv_last_flush, ohlcv_writes_total from __init__; _queue_ohlcv_write and _flush_ohlcv methods; flush trigger block in main loop; drain call in stop()
- BarWriterAgent (indicagent-bar-writer.service) and BarAuditorAgent (indicagent-bar-auditor.service) installed and enabled in systemd
- gap_fill_service.py archived to _archived_gap_fill_service.py (D-15 compliant)
- CLAUDE.md Active Services table updated with Bar Writer (:9121) and Bar Auditor (:9123) rows, market_data_ohlcv description updated to "backfill + live via BarWriterAgent", Data Provider row updated to include "gap-fill consumer", Cold tier updated to include BarWriterAgent

## Task Commits

1. **Task 1: Remove _ohlcv_buffer from FeatureComputeAgent (D-08)** - `5597de9` (feat)
2. **Task 2: Install systemd units, retire gap_fill_service, update CLAUDE.md** - `e91c748` (chore)

## Files Created/Modified

- `services/feature_compute_agent.py` — removed 82 lines of OHLCV write code; self._db and _seed_bar_history retained for warmup reads
- `services/_archived_gap_fill_service.py` — renamed from gap_fill_service.py (preserved, not deleted)
- `CLAUDE.md` — Active Services table (2 new rows), market_data_ohlcv, Cold tier, Data Provider purpose updated

## Decisions Made

- Pre-existing ruff E501 at line 638 (was 705 before removals shifted numbering) deferred — pre-existed before our changes, out of scope per deviation scope boundary rule
- gap_fill_service archived with _ prefix (not deleted) — preserves reference implementation as per D-15 intent
- systemd unit for bar-auditor uses `After=indicagent-bar-writer.service` — enforces startup ordering

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. The ruff E501 was confirmed pre-existing by running `git stash` and re-checking.

## User Setup Required

None - no external service configuration required. Both systemd units are installed and enabled but NOT started (the agents' service files from plans 01 and 02 must be merged to main branch before starting).

## Next Phase Readiness

- FCA is now DB-write-free in its hot path — OHLCV persistence fully owned by BarWriterAgent
- Both systemd units installed and enabled — ready to start once plan 01/02 service files are merged
- Phase 053.1 all 3 plans complete

---
*Phase: 053.1-bar-writer-auditor-agents*
*Completed: 2026-03-28*
