---
phase: 145-stratificationdimension-formalization-planned
plan: 02
type: execute
wave: 2
depends_on: ["145-01"]
files_modified:
  - src/intelligence/stratification/gates.py
  - src/intelligence/stratification/fdr.py
  - tests/unit/test_stratification_gates.py
autonomous: true
requirements: [D-03, D-04, D-07]
must_haves:
  truths:
    - "Effective sample size for a regime cell is computed from state-visit runs (transitions+1), not raw bar count, and degrades safely on empty/single-element/all-NaN input"
    - "A candidate dimension that passes the raw substitution-test criterion is NOT promotion-eligible until it also clears BH-FDR across that regime_group's cumulative candidate-test history"
    - "BH-FDR is applied once per regime_group family, never globally across all groups and never per-candidate in isolation"
    - "gate1_orthogonality refuses to run with an unset max_correlation rather than silently defaulting to a guessed constant"
    - "gate2_substitution_test rejects a cell whose effective N is below the floor even when the IC Sharpe improvement exceeds the pass threshold"
    - "No file under src/intelligence/stratification/ imports from services/"
  artifacts:
    - path: "src/intelligence/stratification/gates.py"
      provides: "Gate 0 structural pre-filter, gate 1 orthogonality, gate 2 substitution test, effective-N-from-transitions, min-hold smoother"
      exports: ["effective_n_from_transitions", "min_hold_smooth", "gate0_structural_redundancy", "gate1_orthogonality", "gate2_substitution_test", "GateResult"]
    - path: "src/intelligence/stratification/fdr.py"
      provides: "D-03 BH-FDR correction across a regime_group's cumulative candidate-test history"
      exports: ["CandidateTest", "CandidateTestResult", "apply_candidate_pool_fdr"]
    - path: "tests/unit/test_stratification_gates.py"
      provides: "Guard-order, effective-N, orthogonality, substitution-test and FDR-family coverage"
      min_lines: 200
  key_links:
    - from: "src/intelligence/stratification/fdr.py"
      to: "src/intelligence/statistics/ic_math.py::apply_bh_fdr"
      via: "direct import, single call per regime_group family"
      pattern: "from src.intelligence.statistics.ic_math import apply_bh_fdr"
    - from: "src/intelligence/stratification/gates.py"
      to: "_MAX_CORRELATION_DEFAULT / _EFFECTIVE_N_FLOOR_DEFAULT"
      via: "module constants carrying an explicit D-07 APR-deferral comment"
      pattern: "D-07"
---

<objective>
Build the three-stage gate cascade's statistical primitives as pure, DB-free Ring 1 functions:
the structural redundancy pre-filter (gate 0), the orthogonality study (gate 1), the
effective-N-corrected substitution test (gate 2, D-04), and the BH-FDR correction across a
`regime_group`'s cumulative candidate-test history (D-03).

Purpose: D-03 and D-04 are the two statistical safeguards this phase adds beyond the original
design docs. `regime_model`'s cascade currently has no family-wise error control (unlike the
`confluence` domain's already-specced gate 2) and states its N floor in raw bars, which is wrong
for autocorrelated, `min_hold_bars`-smoothed regime labels. This plan closes both.

Output: `gates.py`, `fdr.py`, and a unit suite proving guard ordering, family scoping, and the
refuse-rather-than-guess behavior of the unset correlation threshold.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-stratificationdimension-formalization-planned/145-CONTEXT.md
@.planning/phases/145-stratificationdimension-formalization-planned/145-RESEARCH.md
@.planning/phases/145-stratificationdimension-formalization-planned/145-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- Reusable primitives already in the codebase. Import these; do not reimplement. -->

`src/intelligence/statistics/ic_math.py` (Ring 1 — IMPORT directly):

    def apply_bh_fdr(p_values: list[float], alpha: float) -> tuple[np.ndarray, np.ndarray]
        # returns (reject, p_corrected) as parallel arrays, same order as p_values.
        # returns two EMPTY arrays for empty input.
        # Its own docstring: this shape was hand-rolled at three call sites before extraction.
        # Do not create a fourth hand-rolled copy.

    def partial_spearman_ic(
        x: np.ndarray, y: np.ndarray, controls: np.ndarray, condition_max: float
    ) -> tuple[float, float, int]          # (partial_ic, p_value, n); (nan, nan, n) on guard trip

    def compute_ic_vectorized(X: np.ndarray, y: np.ndarray) -> np.ndarray
    def _p_values_from_ic(ic_vector: np.ndarray, n: int, df: int | None = None) -> np.ndarray
    def _hac_sharpe_nd(...)   # Newey-West inflation factor — same "autocorrelation shrinks
                              # effective N" family as D-04. Study as prior art; the primary
                              # D-04 estimator is transition counting, not HAC.

`src/intelligence/stratification/contract.py` (from plan 145-01): `Grain`, `CausalityBasis`,
`registry_name`, `parse_registry_name`.

**Ring 2 pattern references — READ ONLY, NEVER IMPORT** (`services/` is Ring 2, this module is
Ring 1; the import direction is forbidden):

    services/regime_writer.py::_smooth_states(raw_states: np.ndarray, min_hold: int) -> np.ndarray
        # min_hold_bars smoother. Requires min_hold consecutive bars of the same NEW state before
        # confirming a transition. Causal, no look-ahead. Mirror this algorithm in min_hold_smooth.

    services/regime_writer.py::_check_occupation_gate(...) -> tuple[bool, dict[str, Any]]
        # Guard-ORDER discipline to mirror: every empty/short/degenerate check runs BEFORE any
        # division by len(...), and every skip path returns the same (flag, dict-with-"reason")
        # shape so the caller handles all uniformly.

Live APR values (psql-verified 2026-08-06, referenced as defaults only):
    feature.hmm.min_hold_bars = 3
    alpha.ic.min_obs_per_regime = 3000
    alpha.ic.partial_control_condition_max = 1000.0
    alpha.regime.groups -> names: equity, rates, commodity_energy, commodity_metals,
                                  commodity_agri, fx

**D-07 APR exception (binding):** `alpha.regime_stratification.fdr_alpha`,
`.max_correlation`, `.effective_n_floor` and `.min_hold_bars` are NOT to be migrated into
`config_schema`/`config_state` in this phase. CONTEXT.md D-07 defers that migration until Phase
170 lands. Hold them as module-level `_SCREAMING_SNAKE_CASE` constants following
`scripts/ops/alpha/ops_canary_integrity_assert.py`'s `_FDR_ALPHA_DEFAULT` convention, each with a
comment that cites D-07 and Phase 170 explicitly as the reason it is a temporary, user-ratified
exception to CLAUDE.md's migrate-as-you-go APR mandate. A bare number with no such comment is a
plan violation.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write the gate-cascade and FDR test suite</name>
  <files>tests/unit/test_stratification_gates.py</files>
  <read_first>
    - tests/unit/test_regime_writer_occupation_gate.py (the guard-order test organization to mirror: empty → short → non-converged → degenerate, each its own test, plus one "skip marker shape is uniform" cross-cutting test)
    - src/intelligence/statistics/ic_math.py (read `apply_bh_fdr` around line 545 and `partial_spearman_ic` around line 703 — the exact return contracts under test)
    - services/regime_writer.py (read `_smooth_states` and `_check_occupation_gate`, roughly lines 395-465 — algorithm and guard order being mirrored, NOT imported)
    - .planning/phases/145-stratificationdimension-formalization-planned/145-PATTERNS.md (section `tests/unit/test_stratification_gates.py`)
  </read_first>
  <behavior>
    effective_n (`-k effective_n`):
    - Empty array returns `(0, {"reason": "empty_series", ...})` with no ZeroDivisionError and no IndexError.
    - Single-element array returns `(1, {"reason": "single_visit", ...})`.
    - `np.array([0,0,0,1,1,2,2,2,2])` returns effective_n 3 (2 transitions + 1), with
      diagnostics reporting `n_bars=9` and `n_transitions=2`.
    - A 5000-element constant array returns effective_n 1 while `n_bars=5000` — the headline
      demonstration that D-04's correction is not cosmetic.
    - Works on a string-label array (`np.array(["lo","lo","hi"], dtype=object)`), not just ints.
    - An array containing `None` entries counts a `None` run as its own visit and does not crash.

    min_hold_smooth:
    - `min_hold <= 1` returns a copy equal to the input (and is not the same object).
    - A single-bar excursion in `[0,0,0,1,0,0,0]` with `min_hold=3` is absorbed (no transition
      survives), so `effective_n_from_transitions(min_hold_smooth(x, 3))` returns 1.
    - A sustained change (`[0]*10 + [1]*10`) with `min_hold=3` still produces exactly one
      transition.
    - Causal property: smoothing the first k elements of a series equals the first k elements of
      smoothing the full series (prefix invariance), for k in a few sampled positions.

    gate0 (`-k gate0`):
    - A candidate whose `observation_basis` is a subset of an incumbent's returns
      `passed=False` with `reason="structurally_redundant"` and names the offending incumbent.
    - A candidate with a disjoint `observation_basis` returns `passed=True`.
    - A candidate listed as exempt (`volatility_pct`, `dispersion`) returns `passed=True` with
      `reason="exempt"` even when its basis overlaps.

    gate1 (`-k orthogonality`):
    - Calling with `max_correlation=None` for a non-exempt candidate raises `ValueError` naming
      `alpha.regime_stratification.max_correlation` — refuses to guess a threshold.
    - Two perfectly correlated score arrays fail the gate; two independent random arrays pass.
    - An exempt candidate returns `passed=True` but the diagnostics dict still reports the measured
      per-incumbent correlations (the study runs even when rejection is waived — this is what
      supplies the empirical basis for the threshold).
    - Returns a per-incumbent correlation mapping keyed by incumbent dimension name.
    - NaN-containing score arrays are pairwise-dropped, not propagated as NaN correlation.

    gate2 (`-k substitution`):
    - A joint cell with IC Sharpe improvement of 25% and effective N above the floor returns
      `passed=True`.
    - The same cell with effective N one below the floor returns `passed=False` with
      `reason="effective_n_below_floor"` — proving the D-04 correction can veto a headline pass.
    - Improvement of exactly 10% does not pass (criterion is strictly greater than 10%).
    - `effective_n_floor=None` raises `ValueError` naming
      `alpha.regime_stratification.effective_n_floor` rather than defaulting.
    - Every gate function returns the same `(bool, dict)`-shaped result with a `reason` key on
      every path (one cross-cutting uniformity test, mirroring
      `test_skip_marker_shape_is_uniform_across_all_skip_reasons`).

    FDR (`-k fdr`):
    - Two `CandidateTest` records in `regime_group="equity"` and three in `regime_group="rates"`
      produce exactly two `apply_bh_fdr` families; a monkeypatched/spied `apply_bh_fdr` is called
      exactly twice, with p-value list lengths 2 and 3.
    - A test with `raw_pass=True` whose BH-corrected p exceeds alpha is returned with
      `promotion_eligible=False`.
    - A test with `raw_pass=False` is `promotion_eligible=False` regardless of FDR outcome.
    - Family-wise effect: a marginal test that is eligible when it is the only record in its
      group becomes ineligible once 20 additional failing tests are appended to the SAME
      regime_group's cumulative history — and stays eligible if those 20 are appended to a
      DIFFERENT regime_group. This is the D-03 assertion; it must be an explicit test.
    - An empty test list returns an empty result list without raising.
    - Result order matches input order.
  </behavior>
  <action>
Create `tests/unit/test_stratification_gates.py`. Mirror
`tests/unit/test_regime_writer_occupation_gate.py`'s header exactly:
`from __future__ import annotations`, project-root `sys.path` insert, module docstring stating
"No DB. Pure numpy." and citing Phase 145 D-03/D-04 by ID as what the suite proves.

Import from `src.intelligence.stratification.gates` and `src.intelligence.stratification.fdr` —
neither exists yet, so the suite is RED until Task 2 and Task 3 land.

Organize as classes: `TestEffectiveNFromTransitions`, `TestMinHoldSmooth`,
`TestGate0StructuralRedundancy`, `TestGate1Orthogonality`, `TestGate2SubstitutionTest`,
`TestCandidatePoolFdr`, plus one module-level `test_all_gate_results_share_reason_key`.

Use `pytest.raises(ValueError, match=...)` with the APR key name as the match pattern for the
refuse-to-guess tests, so the assertion binds to the specific key, not just any ValueError.

For the "apply_bh_fdr called exactly twice" assertion, use `monkeypatch.setattr` against the
`apply_bh_fdr` symbol as imported into `src.intelligence.stratification.fdr`, wrapping the real
function so behavior is unchanged and only the call count/arg lengths are recorded.

CI-clean: no DB, no network, no filesystem writes. Seed every RNG explicitly
(`np.random.default_rng(<int>)`) — no unseeded randomness in an assertion path.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_stratification_gates.py -q 2>&1 | tail -5; test -f tests/unit/test_stratification_gates.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_stratification_gates.py` exists.
    - `.venv/bin/pytest tests/unit/test_stratification_gates.py -q` fails with a collection/import
      error naming `src.intelligence.stratification.gates` (RED proven).
    - `grep -c "def test_" tests/unit/test_stratification_gates.py` returns at least 25.
    - `grep -c "alpha.regime_stratification.max_correlation" tests/unit/test_stratification_gates.py` returns at least 1.
    - `grep -c "alpha.regime_stratification.effective_n_floor" tests/unit/test_stratification_gates.py` returns at least 1.
    - A test exists whose name contains both `family` and `regime_group` (the D-03 family-wise
      assertion).
    - `grep -c "default_rng" tests/unit/test_stratification_gates.py` returns at least 1 and
      `grep -c "np.random.rand\|np.random.randn" tests/unit/test_stratification_gates.py` returns 0.
  </acceptance_criteria>
  <done>RED suite exists, fails for the right reason, and encodes every D-03/D-04 behavior as an explicit assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement gates.py (effective-N, smoother, gates 0/1/2)</name>
  <files>src/intelligence/stratification/gates.py</files>
  <read_first>
    - tests/unit/test_stratification_gates.py (the RED suite — it is the spec)
    - services/regime_writer.py (`_smooth_states` and `_check_occupation_gate`, ~lines 395-465 — mirror the algorithm and guard order; do NOT import)
    - src/intelligence/statistics/ic_math.py (read `partial_spearman_ic` ~line 703 and `_hac_sharpe_nd` ~line 967)
    - scripts/ops/alpha/ops_canary_integrity_assert.py (read lines 60-110 for the module-constant + custom-exception convention being mirrored)
    - .planning/phases/145-stratificationdimension-formalization-planned/145-CONTEXT.md (D-04 and D-07 wording to cite in docstrings/comments)
  </read_first>
  <behavior>
    Turns every `TestEffectiveNFromTransitions`, `TestMinHoldSmooth`, `TestGate0*`, `TestGate1*`,
    `TestGate2*` and the shared-`reason`-key assertion green without editing the test file.
  </behavior>
  <action>
Create `src/intelligence/stratification/gates.py` with:

Module constants, each carrying a provenance comment that cites D-07 and Phase 170 as the reason
it is not yet APR-backed (see `<interfaces>` — a bare number without this comment fails review):
- `_FDR_ALPHA_DEFAULT = 0.05` (target APR key `alpha.regime_stratification.fdr_alpha`)
- `_MAX_CORRELATION_DEFAULT: float | None = None` (target key
  `alpha.regime_stratification.max_correlation`; deliberately `None` — the design doc asserts no
  default until the first orthogonality study runs, and plan 145-06 sets it from the pilot)
- `_EFFECTIVE_N_FLOOR_DEFAULT: int | None = None` (target key
  `alpha.regime_stratification.effective_n_floor`; deliberately `None` — plan 145-06 sets it from
  the pilot's derived value. D-04 replaces the design doc's "N > 20,000 bars" with a
  transition-derived floor, so a bar-count default would be exactly the wrong answer.)
- `_MIN_HOLD_BARS_DEFAULT = 3` (matches the live `feature.hmm.min_hold_bars = 3`; comment must
  state that using the SAME value as the incumbent HMM is required for the substitution test to
  compare equally-autocorrelated sequences — Pitfall 5)
- `_IC_SHARPE_IMPROVEMENT_THRESHOLD = 0.10` (the design doc's "+>10% in at least one joint cell")
- `_ORTHOGONALITY_EXEMPT: frozenset[str] = frozenset({"volatility_pct", "dispersion"})` (the
  design doc's two named exemptions: already measurably distinct in kind, not just presumed)

`GateResult` — a frozen dataclass or a documented `tuple[bool, dict[str, Any]]` alias. Choose the
tuple-plus-dict shape to match `_check_occupation_gate`'s existing convention; export a
`GateResult` type alias so callers can annotate. Every return path on every gate must populate a
`"reason"` key.

Functions:

`effective_n_from_transitions(labels) -> tuple[int, dict[str, Any]]` — D-04's estimator. Accepts a
plain `np.ndarray` or sequence of labels (int, str, or object dtype with `None`), NOT a
`regime_writer` handle: this decoupling is what keeps the function Ring-1-clean. Guard order,
strictly before any indexing or division: length-0 → `(0, {"reason": "empty_series", "n_bars": 0})`;
length-1 → `(1, {"reason": "single_visit", "n_bars": 1, "n_transitions": 0})`; otherwise count
run boundaries by elementwise comparison of `labels[1:]` against `labels[:-1]` (use an
equality comparison that works for object dtype and treats `None != None` as False — do not use
`np.diff`, which fails on string/object arrays) and return
`(n_transitions + 1, {"reason": "ok", "n_bars": ..., "n_transitions": ...})`. Docstring must state
the D-04 rationale verbatim in substance: a run of 500 consecutive `trending_up` bars is one
independent observation of that state, not 500; HMM and percentile-rank labels are autocorrelated
by construction and additionally smoothed by `min_hold_bars`, so raw bar count overstates
independent evidence. Docstring must also state explicitly whether the caller is expected to pass
pre-smoothed or raw labels (answer: pre-smoothed, and the caller must use the same `min_hold`
across every dimension being compared — Pitfall 5), and cite `_hac_sharpe_nd` as the codebase's
other, complementary effective-N mechanism.

`min_hold_smooth(labels, min_hold) -> np.ndarray` — mirrors `services/regime_writer.py::_smooth_states`'s
algorithm exactly (requires `min_hold` consecutive bars of the same new value before confirming a
transition; causal, no look-ahead; returns a copy when `min_hold <= 1`). The docstring must state
that this is a deliberate Ring-boundary duplication of `_smooth_states`, name that function and
its file, cite the Ring rule as the reason it is not imported, and record the extraction bar (a
second per-symbol dimension needing this smoothing is when it gets extracted to a shared home).

`gate0_structural_redundancy(candidate_name, candidate_observation_basis, incumbent_observation_bases, exempt=_ORTHOGONALITY_EXEMPT) -> tuple[bool, dict]`
— free, no query. `incumbent_observation_bases` is `dict[str, frozenset[str]]` keyed by incumbent
dimension name. Reject when the candidate's basis is a subset of any incumbent's, reporting
`reason="structurally_redundant"` and the offending incumbent name. Docstring cites the design
doc's own precedent: Hurst/mean-reversion and autocorrelation-sign were rejected this way against
the incumbent HMM because they are direct proxies for `momentum`/`vol_of_vol`, two of its five
observation dimensions.

`gate1_orthogonality(candidate_name, candidate_score, incumbent_scores, max_correlation=_MAX_CORRELATION_DEFAULT, exempt=_ORTHOGONALITY_EXEMPT) -> tuple[bool, dict]`
— Pearson correlation of the candidate's continuous `score()` output against each incumbent's, on
pairwise-complete observations. `incumbent_scores` is `dict[str, np.ndarray]`. When
`max_correlation is None` and the candidate is not exempt, raise `ValueError` whose message names
`alpha.regime_stratification.max_correlation` and states that the design doc asserts no default
until the first empirical study runs. Exempt candidates return `passed=True, reason="exempt"` but
STILL compute and return every measured correlation in the diagnostics — the study is the
deliverable that supplies the threshold's empirical basis (D-06), so it must never be short-circuited.

`gate2_substitution_test(joint_cells, effective_n_floor=_EFFECTIVE_N_FLOOR_DEFAULT, improvement_threshold=_IC_SHARPE_IMPROVEMENT_THRESHOLD) -> tuple[bool, dict]`
— `joint_cells` is a list of dicts, each with `cell` (an identifying tuple/str),
`ic_sharpe_without`, `ic_sharpe_with`, `effective_n`. Pass criterion, per the design doc as
corrected by D-04: at least one cell where `(ic_sharpe_with - ic_sharpe_without) /
abs(ic_sharpe_without)` is strictly greater than `improvement_threshold` AND
`effective_n > effective_n_floor`. Raise `ValueError` naming
`alpha.regime_stratification.effective_n_floor` when the floor is `None`. Diagnostics must report
every cell's improvement ratio and effective N plus a `qualifying_cells` list, and the failure
`reason` must distinguish `no_cell_met_improvement` from `effective_n_below_floor` (the latter
when at least one cell met the improvement bar but no cell met both).

Ring rule: imports limited to stdlib, numpy, pandas, and `src/`. Zero imports from `services/`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_stratification_gates.py -q -k "effective_n or min_hold or gate0 or orthogonality or substitution or reason_key" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All `-k "effective_n or min_hold or gate0 or orthogonality or substitution or reason_key"`
      tests pass with 0 failures.
    - `grep -v '^\s*#' src/intelligence/stratification/gates.py | grep -c "from services\.\|import services\."` returns 0.
    - `grep -c "D-07" src/intelligence/stratification/gates.py` returns at least 4 (one per
      deferred APR constant).
    - `grep -c "Phase 170" src/intelligence/stratification/gates.py` returns at least 1.
    - `grep -c "_smooth_states" src/intelligence/stratification/gates.py` returns at least 1 and it
      appears only inside a comment or docstring, never as an import or call.
    - `grep -c "np.diff" src/intelligence/stratification/gates.py` returns 0 (object/string label
      arrays must not go through `np.diff`).
    - `.venv/bin/ruff check src/intelligence/stratification/gates.py` and
      `.venv/bin/black --check src/intelligence/stratification/gates.py` both exit 0.
  </acceptance_criteria>
  <done>gates.py implements D-04's estimator plus gates 0/1/2 with uniform result shape, refuse-to-guess thresholds, and documented D-07 exceptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — implement fdr.py (D-03 per-regime_group candidate-pool correction)</name>
  <files>src/intelligence/stratification/fdr.py</files>
  <read_first>
    - tests/unit/test_stratification_gates.py (the `TestCandidatePoolFdr` class — it is the spec)
    - src/intelligence/statistics/ic_math.py (read `apply_bh_fdr` ~lines 545-565 including its docstring on why the scatter-back step stays local to each caller)
    - src/intelligence/stratification/gates.py (the constants and result conventions this module aligns with)
    - .planning/phases/145-stratificationdimension-formalization-planned/145-CONTEXT.md (D-03 rationale)
  </read_first>
  <behavior>
    Turns every `TestCandidatePoolFdr` assertion green without editing the test file.
  </behavior>
  <action>
Create `src/intelligence/stratification/fdr.py` as a thin, deliberately minimal wrapper.

`@dataclass(frozen=True) class CandidateTest` with fields: `dimension: str`, `regime_group: str`,
`tf: str`, `gate_metric: float` (the IC Sharpe improvement ratio), `gate_n: int` (the
effective N from `effective_n_from_transitions`, not the raw bar count), `p_value: float`,
`raw_pass: bool` (did it meet the substitution-test criterion in isolation),
`tested_at: datetime`, `notes: str | None = None`.

`@dataclass(frozen=True) class CandidateTestResult` with fields: `test: CandidateTest`,
`p_corrected: float`, `fdr_reject: bool`, `promotion_eligible: bool`.

`apply_candidate_pool_fdr(tests: Sequence[CandidateTest], alpha: float = _FDR_ALPHA_DEFAULT) -> list[CandidateTestResult]`
— group `tests` by `regime_group`; for each group, make exactly ONE
`ic_math.apply_bh_fdr(p_values, alpha)` call over that group's ENTIRE cumulative history (every
test ever run for that group, pass or fail — this is why every test must be logged, per the
project's never-drop-data principle); scatter `reject`/`p_corrected` back onto the group's
records; set `promotion_eligible = test.raw_pass and fdr_reject`. Return results in the SAME order
as the input sequence, not grouped order. Empty input returns an empty list without calling
`apply_bh_fdr`.

Import `_FDR_ALPHA_DEFAULT` from `src.intelligence.stratification.gates` rather than redeclaring
it, so the D-07 exception comment lives in exactly one place.

Module docstring must state the D-03 rationale in substance: the candidate list carries 15+ named
dimensions, testing that many over the system's lifetime with no family-wise error control
reproduces the failure pattern this project has already lived through three times (CTF momentum
join leak todo 243, HMM parameter lookahead todo 248, `nonlinear_interaction_combiner`'s inflated
headline number), and the `confluence` domain already specs BH-FDR across its discovery batch
while `regime_model`'s cascade had no analogous step. It must also state explicitly why the family
is the `regime_group` and not the global pool: promotion scope is per `regime_group` (a dimension
can be live for `equity` and shadow for `rates`), so the decision family is the group's own test
history.

Ring rule: imports limited to stdlib, numpy, and `src/`. Zero imports from `services/`. Do not
call `statsmodels.multipletests` directly — that would be the fourth hand-rolled copy
`apply_bh_fdr`'s own docstring exists to prevent.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_stratification_gates.py -x -q && .venv/bin/pytest tests/unit/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_stratification_gates.py -x -q` passes with 0 failures.
    - `.venv/bin/pytest tests/unit/ -q` passes (no regression).
    - `grep -c "multipletests" src/intelligence/stratification/fdr.py` returns 0.
    - `grep -c "from src.intelligence.statistics.ic_math import" src/intelligence/stratification/fdr.py` returns 1.
    - `grep -v '^\s*#' src/intelligence/stratification/fdr.py | grep -c "from services\.\|import services\."` returns 0.
    - `grep -c "regime_group" src/intelligence/stratification/fdr.py` returns at least 3.
    - `.venv/bin/ruff check src/intelligence/stratification/` and
      `.venv/bin/black --check src/intelligence/stratification/` both exit 0.
  </acceptance_criteria>
  <done>fdr.py applies BH-FDR once per regime_group family over cumulative history, reusing the existing wrapper, with promotion eligibility gated on both raw pass and FDR rejection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw gate metric → promotion decision | An uncorrected per-candidate significance result crossing into a promotion claim |
| raw bar count → statistical evidence claim | Autocorrelated, smoothed regime labels presented as independent observations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-05 | Repudiation | `apply_candidate_pool_fdr` promotion decisions | mitigate | Family is the `regime_group`'s cumulative test history including every failed test; `promotion_eligible` requires both `raw_pass` and `fdr_reject`; an explicit test proves eligibility is revoked when the same group's history grows |
| T-145-06 | Information Disclosure (overstated evidence count) | `gate2_substitution_test` N gating | mitigate | `effective_n_from_transitions` replaces raw bar count; an explicit test proves a 5000-bar constant-label sequence yields effective N of 1 |
| T-145-07 | Tampering | Guessed correlation/N thresholds silently entering the gate | mitigate | `_MAX_CORRELATION_DEFAULT` and `_EFFECTIVE_N_FLOOR_DEFAULT` are `None`; both gates raise `ValueError` naming the target APR key rather than defaulting |
| T-145-08 | Tampering | Mismatched smoothing across compared dimensions | mitigate | `min_hold_smooth` defaults to `_MIN_HOLD_BARS_DEFAULT = 3` matching the live `feature.hmm.min_hold_bars`; the effective-N docstring states the pre-smoothed-input contract explicitly (Pitfall 5) |
| T-145-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; `statsmodels`/`scipy` are pre-existing verified dependencies |
</threat_model>

<verification>
1. `.venv/bin/pytest tests/unit/test_stratification_gates.py -v` — all green.
2. `.venv/bin/pytest tests/unit/ -q` — full suite green.
3. `.venv/bin/ruff check . --fix && .venv/bin/black .` — clean.
4. `grep -v '^\s*#' src/intelligence/stratification/*.py | grep -c "from services\."` returns 0.
5. `git diff --name-only` shows zero files under `production/migrations/`.
</verification>

<success_criteria>
- `effective_n_from_transitions` is the phase's D-04 deliverable and demonstrably shrinks a
  5000-bar constant sequence to effective N of 1.
- `apply_candidate_pool_fdr` calls `apply_bh_fdr` exactly once per `regime_group`, over that
  group's cumulative history, and can revoke a marginal candidate's eligibility as the family grows.
- Both unset thresholds refuse to run rather than guessing.
- All four deferred APR keys are module constants carrying an explicit D-07 / Phase 170 comment.
- Zero Ring 2 imports; zero migrations; zero direct `multipletests` calls.
</success_criteria>

<output>
Create `.planning/phases/145-stratificationdimension-formalization-planned/145-02-SUMMARY.md` when done
</output>
