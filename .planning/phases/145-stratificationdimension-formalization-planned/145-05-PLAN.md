---
phase: 145-stratificationdimension-formalization-planned
plan: 05
type: execute
wave: 4
depends_on: ["145-02", "145-03", "145-04"]
files_modified:
  - scripts/analysis/volatility_pct_stratification_gate_pilot.py
  - tests/unit/test_volatility_pct_pilot_artifact.py
  - docs/analysis/145-volatility-pct-pilot-gate-cascade.json
  - docs/analysis/145-volatility-pct-pilot-gate-cascade.md
autonomous: true
requirements: [D-03, D-04, D-05, D-06, D-07]
must_haves:
  truths:
    - "volatility_pct is run through the full corrected gate stack end to end: registration gate (D-05), gate 0, gate 1 orthogonality study, effective-N-corrected gate 2 substitution test (D-04), then BH-FDR across the equity regime_group's candidate history (D-03)"
    - "The pilot's measurement scope (symbols, tf, regime_group, feature set) is pre-registered and written to the artifact BEFORE any IC number is computed"
    - "Every gate result, pass or fail, is recorded in a durable standalone artifact — no test result is dropped"
    - "The artifact is shaped like a concept_transition_log row and is explicitly marked pending Phase 170 backfill"
    - "Zero rows are written to concept_registry, concept_transition_log, or any database table by this pilot"
    - "The pilot reads market_data_ohlcv_tradeable or feature_vectors, never the raw market_data_ohlcv table"
  artifacts:
    - path: "scripts/analysis/volatility_pct_stratification_gate_pilot.py"
      provides: "D-06's end-to-end gate-cascade pilot runner"
      exports: ["build_preregistration", "build_transition_log_rows", "run_cascade", "main"]
    - path: "docs/analysis/145-volatility-pct-pilot-gate-cascade.json"
      provides: "concept_transition_log-shaped gate results, pending Phase 170 backfill"
      contains: "pending_phase_170_backfill"
    - path: "docs/analysis/145-volatility-pct-pilot-gate-cascade.md"
      provides: "Written analysis of the pilot's gate-by-gate numbers and verdict"
    - path: "tests/unit/test_volatility_pct_pilot_artifact.py"
      provides: "Artifact-shape and pre-registration coverage for the pilot runner's pure helpers"
  key_links:
    - from: "scripts/analysis/volatility_pct_stratification_gate_pilot.py"
      to: "src/intelligence/stratification/gates.py + fdr.py + acausal_placebo_gate.py"
      via: "direct imports — the script orchestrates, it does not reimplement any gate"
      pattern: "from src.intelligence.stratification"
    - from: "docs/analysis/145-volatility-pct-pilot-gate-cascade.json"
      to: "concept_transition_log"
      via: "row-shaped payload awaiting Phase 170 backfill, never inserted"
      pattern: "regime_model"
---

<objective>
Run `volatility_pct` through the full corrected gate stack against real corpus data (D-06) and
record every result in a durable standalone artifact shaped like the eventual
`concept_transition_log` row.

Purpose: this is the proof the machinery works end to end. It also produces the two empirical
values the gate cascade currently refuses to guess — the orthogonality threshold and the
effective-N floor — which plan 145-06 then bakes into `gates.py`.

**Hard constraint (psql-verified 2026-08-06):** `concept_registry.domain` is CHECK-constrained to
`('feature','ensemble_strategy')` and `concept_gate_stack` does not exist as a table. There is no
`concept_registry` row for a `regime_model` dimension to reference, and
`concept_transition_log.concept_id` is a NOT NULL FK to it. Any task that attempts a real registry
write will fail at execution time. D-07 defers that schema work to Phase 170. This pilot writes a
file, not a row.

Output: the pilot runner script, a unit test for its pure helpers, and the two artifacts under
`docs/analysis/`.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-stratificationdimension-formalization-planned/145-CONTEXT.md
@.planning/phases/145-stratificationdimension-formalization-planned/145-RESEARCH.md
@.planning/phases/145-stratificationdimension-formalization-planned/145-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- Pre-registered measurement scope. These values are the pilot's contract with itself:
     declared before evaluation, never chosen after seeing per-stratum results. -->

    regime_group : "equity"
    tf           : "1h"
    symbols      : QQQ, IWM, XLF, XLK, XLE
    features     : momentum_z_fast, momentum_z_mid, momentum_z_slow
    return column: forward_returns.return_fast, gated on complete_fast = true
    return_type  : 'executable_open_to_open'   (Invariant 1 — non-negotiable)
    candidate input column : feature_vectors.garman_klass_vol_z

Live-data facts verified by psql 2026-08-06 (do not re-derive, do verify they still hold):
  - Each of the 5 symbols has ~35,560 `feature_vectors` rows at tf='1h' with zero NULLs in
    `garman_klass_vol_z`, `regime`, and all three `momentum_z_*` columns.
  - Each has ~34,540 `forward_returns` rows at tf='1h' with `return_type='executable_open_to_open'`.
  - `market_regimes` has 173,529 rows for `regime_group='equity'`, tf='1h', spanning 2006-09-24
    to 2026-07-28. Columns: `ts, regime_prob_vector, regime_group, tf, regime_label`.
  - Incumbent continuous scores available on `feature_vectors`: `hmm_regime_prob`, `hmm_entropy`,
    `hmm_churn`.

Live `concept_transition_log` columns (the artifact's target row shape):
    id, concept_id, domain, name, from_status, to_status, trigger_reason, gate_metric,
    gate_n, ci_lower, decay_ratio, regime_scope, corpus_build_ref, notes, triggered_at

Modules to orchestrate (all Ring 1, all already tested by plans 145-02/03/04):
    src.intelligence.stratification.contract      : validate_registration, registry_name
    src.intelligence.stratification.gates          : gate0_structural_redundancy,
                                                     gate1_orthogonality, gate2_substitution_test,
                                                     effective_n_from_transitions, min_hold_smooth
    src.intelligence.stratification.fdr            : CandidateTest, apply_candidate_pool_fdr
    src.intelligence.stratification.acausal_placebo_gate : make_placebo_gate
    src.intelligence.stratification.volatility_pct : VolatilityPct
    src.intelligence.statistics.ic_math            : compute_ic_vectorized, _compute_ic_rolling_metrics

Script conventions to mirror — `scripts/analysis/equity_regime_separation_gate.py`:
  - shebang + module docstring stating the todo/decision, the reused machinery, the
    HARD PRECONDITION, and a `Usage:` block
  - `from __future__ import annotations`, project-root `sys.path` insert
  - `import psycopg` + `from psycopg.rows import dict_row`, `_dsn()` / `_connect()` helpers
  - `argparse.ArgumentParser(description=__doc__)`
  - module-level `_SCREAMING_SNAKE_CASE` constants for every pre-registered choice, each with a
    comment marking it as this script's own pre-registered decision

DB access: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent` for manual checks.
This pilot is READ-ONLY. It issues zero INSERT/UPDATE/DELETE/DDL. A full-universe OHLCV backfill
is in flight; a 5-symbol read of ~180k rows is negligible against it, but do not widen scope.

**Raw-table boundary:** `market_data_ohlcv` must not be queried. All inputs come from
`feature_vectors`, `forward_returns`, and `market_regimes`. If any OHLCV read becomes necessary it
must target `market_data_ohlcv_tradeable`, and an allow-list entry in
`tests/unit/test_market_data_ohlcv_boundary.py` would be required — avoid this entirely.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write the pre-registration and artifact-shape test suite</name>
  <files>tests/unit/test_volatility_pct_pilot_artifact.py</files>
  <read_first>
    - scripts/analysis/equity_regime_separation_gate.py (read the module docstring, `_dsn`/`_connect`, and `render_verdict` — the script conventions and pre-registration discipline being mirrored)
    - src/intelligence/stratification/fdr.py (the `CandidateTest`/`CandidateTestResult` shapes the artifact serialises)
    - .planning/phases/145-stratificationdimension-formalization-planned/145-RESEARCH.md (Pitfall 1 and Pitfall 2 — why this is a file and not a DB row)
  </read_first>
  <behavior>
    - `build_preregistration()` returns a dict containing `regime_group="equity"`, `tf="1h"`,
      `symbols` exactly `["QQQ","IWM","XLF","XLK","XLE"]`, `features` exactly
      `["momentum_z_fast","momentum_z_mid","momentum_z_slow"]`,
      `return_type="executable_open_to_open"`, `candidate_input_column="garman_klass_vol_z"`, and a
      `declared_at` ISO-8601 UTC timestamp.
    - `build_preregistration()` is deterministic in every field except `declared_at`.
    - `build_transition_log_rows(preregistration, cascade_result)` returns a list of dicts, each
      carrying every live `concept_transition_log` column name: `concept_id`, `domain`, `name`,
      `from_status`, `to_status`, `trigger_reason`, `gate_metric`, `gate_n`, `ci_lower`,
      `decay_ratio`, `regime_scope`, `corpus_build_ref`, `notes`, `triggered_at`.
    - Every returned row has `domain == "regime_model"`, `concept_id is None`, and
      `pending_phase_170_backfill is True`.
    - `name` equals `registry_name("volatility_pct", "equity")` i.e. `"volatility_pct__equity"`
      (D-01's Option B encoding, not a bare dimension name).
    - `gate_n` carries the effective N from `effective_n_from_transitions`, not a raw bar count —
      assert with a fixture where the two differ, that the emitted value is the effective one.
    - A FAILING cascade still produces rows (`to_status="candidate"`,
      `trigger_reason` naming the failing gate). Nothing is dropped on failure — assert an explicit
      "failed gate still logged" test.
    - `build_transition_log_rows` raises `ValueError` if `preregistration` is missing or if its
      `declared_at` is later than the cascade's `evaluated_at` (pre-registration must precede
      evaluation, not be back-filled).
    - The serialised JSON round-trips through `json.dumps`/`json.loads` with no non-serialisable
      values (numpy scalars must be cast to Python floats/ints).
  </behavior>
  <action>
Create `tests/unit/test_volatility_pct_pilot_artifact.py`. CI-clean: no DB, no network, no
filesystem writes (build fixture cascade-result dicts inline).

Header: `from __future__ import annotations`, project-root `sys.path` insert, module docstring
citing Phase 145 D-06 and stating that the pilot cannot write `concept_transition_log` because
`concept_registry.domain` does not admit `regime_model` yet, so this suite guards the standalone
artifact's shape instead.

Import the pure helpers from the script module:
`from scripts.analysis.volatility_pct_stratification_gate_pilot import (build_preregistration,
build_transition_log_rows)`. The script does not exist yet — RED until Task 2. Ensure
`scripts/` and `scripts/analysis/` are importable; if `__init__.py` files are absent there, load
the module via `importlib.util.spec_from_file_location` rather than adding package init files to
`scripts/` (the existing `scripts/analysis/` modules are standalone scripts, not a package — do
not restructure them).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_volatility_pct_pilot_artifact.py -q 2>&1 | tail -5; test -f tests/unit/test_volatility_pct_pilot_artifact.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_volatility_pct_pilot_artifact.py` exists.
    - `.venv/bin/pytest tests/unit/test_volatility_pct_pilot_artifact.py -q` fails with an
      import/collection error naming `volatility_pct_stratification_gate_pilot` (RED proven).
    - `grep -c "def test_" tests/unit/test_volatility_pct_pilot_artifact.py` returns at least 10.
    - `grep -c "pending_phase_170_backfill" tests/unit/test_volatility_pct_pilot_artifact.py` returns at least 1.
    - `grep -c "volatility_pct__equity" tests/unit/test_volatility_pct_pilot_artifact.py` returns at least 1.
    - A test whose name contains `fail` and `logged` exists (never-drop-data assertion).
    - `grep -c "__init__.py" tests/unit/test_volatility_pct_pilot_artifact.py` returns 0 and no
      `scripts/__init__.py` or `scripts/analysis/__init__.py` was created.
  </acceptance_criteria>
  <done>RED suite exists, fails for the right reason, and pins the pre-registration and artifact-row contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — write the gate-cascade pilot runner script</name>
  <files>scripts/analysis/volatility_pct_stratification_gate_pilot.py</files>
  <read_first>
    - tests/unit/test_volatility_pct_pilot_artifact.py (the RED suite — it is the spec for the pure helpers)
    - scripts/analysis/equity_regime_separation_gate.py (read in full — the script structure, `_dsn`/`_connect`, argparse wiring, and precondition-check discipline to mirror)
    - src/intelligence/stratification/gates.py (the exact gate signatures and their `(bool, dict)` return shape)
    - src/intelligence/stratification/fdr.py (`CandidateTest` field list)
    - src/intelligence/stratification/acausal_placebo_gate.py (`make_placebo_gate` signature)
    - src/intelligence/stratification/volatility_pct.py (`VolatilityPct.score`/`compute` and the input-column parameter)
    - src/intelligence/statistics/ic_math.py (read `_compute_ic_rolling_metrics` ~line 1009 including its `SharpeWindowConfig` Protocol, and `compute_ic_vectorized` ~line 441)
  </read_first>
  <behavior>
    Turns every assertion in `tests/unit/test_volatility_pct_pilot_artifact.py` green without
    editing that test file. The DB-touching paths are exercised in Task 3, not here.
  </behavior>
  <action>
Create `scripts/analysis/volatility_pct_stratification_gate_pilot.py`, mirroring
`scripts/analysis/equity_regime_separation_gate.py`'s structure.

Module docstring: state that this is Phase 145 D-06's pilot, name the full corrected gate stack in
order, state the HARD PRECONDITION (the script must never write to `concept_registry` or
`concept_transition_log` — `concept_registry.domain` is CHECK-constrained to
`('feature','ensemble_strategy')` and `concept_gate_stack` does not exist; both psql-verified
2026-08-06; D-07 defers that schema work to Phase 170), state that every result pass or fail is
written to the standalone artifact, and include a `Usage:` block.

Module-level pre-registration constants, each commented as this script's own pre-registered
choice made before any evaluation: `_REGIME_GROUP = "equity"`, `_TF = "1h"`,
`_SYMBOLS = ("QQQ","IWM","XLF","XLK","XLE")`,
`_FEATURES = ("momentum_z_fast","momentum_z_mid","momentum_z_slow")`,
`_RETURN_TYPE = "executable_open_to_open"`, `_RETURN_COLUMN = "return_fast"`,
`_COMPLETE_COLUMN = "complete_fast"`, `_CANDIDATE_INPUT_COLUMN = "garman_klass_vol_z"`,
`_INCUMBENT_SCORE_COLUMNS = ("hmm_regime_prob","hmm_entropy","hmm_churn")`,
`_ARTIFACT_JSON`/`_ARTIFACT_MD` pointing at the two `docs/analysis/145-volatility-pct-pilot-gate-cascade.*`
paths.

Structure:

`_dsn()` / `_connect()` — copy the convention from the analog script (psycopg, `dict_row`).

`check_precondition(conn) -> tuple[bool, str]` — assert the pilot can run and must not write:
verify `concept_registry`'s `domain` CHECK still excludes `regime_model` (query `pg_constraint`)
and that `to_regclass('concept_gate_stack')` is NULL, and FAIL LOUDLY with an instruction to
re-plan if either has changed (Phase 170 may have landed since planning; a silently-stale
assumption is worse than a halt). Also verify each pre-registered symbol has non-zero
`forward_returns` rows at `_TF` with `_RETURN_TYPE`.

`fetch_panel(conn) -> dict[str, pd.DataFrame]` — one query per concern, joined in pandas:
`feature_vectors` (symbol, bar_ts, `_CANDIDATE_INPUT_COLUMN`, `regime`,
`_INCUMBENT_SCORE_COLUMNS`, `_FEATURES`) filtered to `_SYMBOLS`/`_TF`; `forward_returns` (symbol,
bar_ts, `_RETURN_COLUMN`, `_COMPLETE_COLUMN`) filtered to `_SYMBOLS`/`_TF`/
`return_type = _RETURN_TYPE`; `market_regimes` (ts, regime_label, regime_prob_vector) filtered to
`regime_group = _REGIME_GROUP`/`_TF`. Never query `market_data_ohlcv`. Inner-join on
(symbol, bar_ts) and keep only rows where `_COMPLETE_COLUMN` is true.

`build_preregistration() -> dict` — emit the pre-registration block from the constants plus a
`declared_at` UTC ISO timestamp via `format_iso_ts` from `src/core/service_utils.py` (never an
inline `.isoformat().replace(...)`).

`run_cascade(panel, preregistration) -> dict` — the orchestrator, in this exact order:
  1. **Registration gate (D-05):** build `make_placebo_gate(frame, forward_returns, seed=145)` from
     the real panel and call `contract.validate_registration(VolatilityPct(), gate)`. A raised
     `AcausalPlaceboRegistrationViolation` aborts the cascade and is recorded as a FAILED result,
     not swallowed.
  2. **Gate 0:** `gate0_structural_redundancy` with `volatility_pct`'s `observation_basis` against
     the incumbents' bases (`hmm_price_vol` -> the HMM's five observation dimensions;
     `cross_sectional_equity` -> its VIX-proxy/breadth basis). Record the result.
  3. **Gate 1 orthogonality study:** `gate1_orthogonality` comparing `VolatilityPct().score(...)`
     against each incumbent continuous score. `volatility_pct` is exempt from REJECTION per the
     design doc, but the study still runs and its measured per-incumbent correlations are the
     deliverable that supplies the empirical basis for
     `alpha.regime_stratification.max_correlation`. Record every correlation.
  4. **Effective-N derivation (D-04):** for each joint cell of
     `(feature_vectors.regime, volatility_pct label)`, compute both the raw bar count and
     `effective_n_from_transitions` on the cell's smoothed label sequence. Record the full
     distribution — this is what plan 145-06 turns into the floor.
  5. **Gate 2 substitution test:** per feature in `_FEATURES`, compute IC Sharpe stratified by the
     incumbent axis alone versus the joint (incumbent x volatility_pct) axis, using
     `_compute_ic_rolling_metrics` from `ic_math` rather than a new Sharpe implementation, and feed
     the per-cell improvement ratios and effective Ns into `gate2_substitution_test`. Pass the
     effective-N floor in explicitly from a `--effective-n-floor` CLI argument, since the module
     default is deliberately `None` on first run.
  6. **BH-FDR (D-03):** build one `CandidateTest` per (feature, tf) evaluation, all tagged
     `regime_group="equity"`, and call `apply_candidate_pool_fdr`. Record `promotion_eligible` per
     test. Note in the report that this is the equity group's FIRST candidate-test cohort, so the
     family is small; future candidates append to the same family.

`build_transition_log_rows(preregistration, cascade_result) -> list[dict]` — serialise to the
`concept_transition_log` column shape listed in `<interfaces>`, with `concept_id=None`,
`domain="regime_model"`, `name=registry_name("volatility_pct", _REGIME_GROUP)`,
`regime_scope=_REGIME_GROUP`, `gate_n` set from the EFFECTIVE N, `corpus_build_ref` set from the
max `feature_vectors.bar_ts` observed in the panel, and an added
`pending_phase_170_backfill: True` marker plus a `notes` string naming this plan and stating the
row must be backfilled into `concept_transition_log` once Phase 170 widens the `domain` CHECK.
Cast every numpy scalar to a plain Python float/int before returning. Raise `ValueError` when
`declared_at` is missing or later than `evaluated_at`.

`write_artifacts(preregistration, cascade_result, rows) -> None` — write the JSON artifact and a
markdown analysis doc mirroring `docs/analysis/hmm-parameter-lookahead-pilot-spy-1h.md`'s
convention (context, method, per-gate numbers, verdict, what it does and does not prove).

`main()` — argparse with `--effective-n-floor` (int, required for a gate-2 verdict),
`--max-correlation` (float, optional — informational for the study),
`--preregister-only` (emit the pre-registration block and exit 0 without touching the DB),
`--dry-run` (run the precondition check and the panel fetch, print row counts, write nothing).
Exit non-zero on precondition failure or on an `AcausalPlaceboRegistrationViolation`, mirroring
`ops_canary_integrity_assert.py`'s `print(..., file=sys.stderr); return 1` pattern.

Absolute prohibitions for this script: no `INSERT`, `UPDATE`, `DELETE`, `CREATE`, or `ALTER`
statement anywhere in the file; no query against `market_data_ohlcv`; no new migration file.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_volatility_pct_pilot_artifact.py -x -q && .venv/bin/python scripts/analysis/volatility_pct_stratification_gate_pilot.py --help >/dev/null && .venv/bin/python scripts/analysis/volatility_pct_stratification_gate_pilot.py --preregister-only >/dev/null && .venv/bin/pytest tests/unit/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_volatility_pct_pilot_artifact.py -x -q` passes with 0 failures.
    - `.venv/bin/python scripts/analysis/volatility_pct_stratification_gate_pilot.py --help` exits 0.
    - `.venv/bin/python scripts/analysis/volatility_pct_stratification_gate_pilot.py --preregister-only`
      exits 0 and prints JSON containing `"regime_group": "equity"` and all five pre-registered symbols.
    - `grep -viE "^\s*#" scripts/analysis/volatility_pct_stratification_gate_pilot.py | grep -ciE "\b(insert|update|delete|create table|alter table)\b"` returns 0.
    - `grep -c "market_data_ohlcv" scripts/analysis/volatility_pct_stratification_gate_pilot.py` returns 0.
    - `grep -c "executable_open_to_open" scripts/analysis/volatility_pct_stratification_gate_pilot.py` returns at least 1.
    - `grep -c "from src.intelligence.stratification" scripts/analysis/volatility_pct_stratification_gate_pilot.py` returns at least 4
      (the script orchestrates the shipped gates, it does not reimplement them).
    - `grep -c "_compute_ic_rolling_metrics\|compute_ic_vectorized" scripts/analysis/volatility_pct_stratification_gate_pilot.py` returns at least 1.
    - `.venv/bin/ruff check scripts/analysis/volatility_pct_stratification_gate_pilot.py` and
      `.venv/bin/black --check scripts/analysis/volatility_pct_stratification_gate_pilot.py` both exit 0.
    - `.venv/bin/pytest tests/unit/ -q` passes (no regression, including
      `tests/unit/test_market_data_ohlcv_boundary.py`).
  </acceptance_criteria>
  <done>The pilot runner exists, orchestrates the shipped gates without reimplementing them, is provably read-only, and its pure helpers are green.</done>
</task>

<task type="auto">
  <name>Task 3: Run the pilot against the live corpus and write the standalone artifacts</name>
  <files>docs/analysis/145-volatility-pct-pilot-gate-cascade.json, docs/analysis/145-volatility-pct-pilot-gate-cascade.md</files>
  <read_first>
    - scripts/analysis/volatility_pct_stratification_gate_pilot.py (the script being run, from Task 2)
    - docs/analysis/hmm-parameter-lookahead-pilot-spy-1h.md (the written-analysis convention this markdown artifact mirrors: context, method, numbers, verdict, and an explicit statement of what the result does NOT prove)
    - .planning/phases/145-stratificationdimension-formalization-planned/145-CONTEXT.md (D-04 and D-06 — what the derived floor and the pilot verdict are for)
  </read_first>
  <action>
Step 1 — re-verify the blocking schema facts still hold before running anything (Phase 170 is
running concurrently in another session and may have landed since planning):

    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "select pg_get_constraintdef(oid) from pg_constraint where conrelid='concept_registry'::regclass and contype='c';"
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "select to_regclass('concept_gate_stack');"

If the `domain` CHECK now admits `regime_model`, or `concept_gate_stack` now exists, STOP and
record the change in the SUMMARY rather than opportunistically writing registry rows — D-07's
sequencing decision is the user's, and Phase 170's migration is another session's work.

Step 2 — dry run to confirm the panel fetch and row counts:
`.venv/bin/python scripts/analysis/volatility_pct_stratification_gate_pilot.py --dry-run`.
Expect roughly 34,500 joined rows per symbol at tf=1h. Investigate, do not paper over, any symbol
returning materially fewer.

Step 3 — derive the effective-N floor (D-04). Run the cascade with a deliberately permissive
`--effective-n-floor 0` first purely to emit the effective-N distribution across all
`(regime, volatility_pct)` joint cells, then choose the floor as a stated, defensible rule rather
than a round number. Use this rule and record it verbatim in the markdown artifact: the floor is
the effective N at which a cell's IC standard error is small enough for a 10% IC Sharpe difference
to be distinguishable from noise, expressed as a concrete integer derived from the observed cell
distribution. State the arithmetic, not just the answer. Cross-check it against
`alpha.ic.min_obs_per_regime = 3000` (which is a RAW-bar gate) and state explicitly how many raw
bars the derived effective-N floor corresponds to in this corpus, so the two are comparable.

Step 4 — run the real cascade with the derived floor and let it write both artifacts.

Step 5 — verify the artifacts. The JSON must contain the pre-registration block with
`declared_at` earlier than `evaluated_at`, every gate's result (pass AND fail alike), the full
effective-N distribution, every measured orthogonality correlation, the FDR outcome, and rows
carrying `domain="regime_model"`, `name="volatility_pct__equity"`, `concept_id: null`, and
`pending_phase_170_backfill: true`.

Step 6 — write/complete the markdown artifact: context (why this pilot exists, D-06), method
(pre-registered scope verbatim), gate-by-gate numbers, the derived effective-N floor with its
arithmetic, the derived `max_correlation` candidate value with its empirical basis, the verdict,
and an explicit "what this does NOT prove" section (single candidate, single regime_group, single
tf, first cohort so the FDR family is small; the incumbent HMM itself has never been run through
the D-05 prefix-invariance check and would be expected to fail it given todo 248's confirmed
full-history parameter fit — flag that as a follow-up finding, not a Phase 145 deliverable).

Step 7 — confirm nothing was written to the database:

    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "select count(*) from concept_transition_log where domain='regime_model';"
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "select count(*) from concept_registry where domain='regime_model';"

Both must return 0.

Do not tune the pre-registered scope after seeing results. If a gate fails, the failure is the
result — record it and move on. Scope reduction, retroactive symbol selection, or picking the
best-looking stratum after the fact is exactly the repudiation risk this pilot's pre-registration
block exists to prevent.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json;d=json.load(open('docs/analysis/145-volatility-pct-pilot-gate-cascade.json'));rows=d['transition_log_rows'];assert rows,'no rows';assert all(r['domain']=='regime_model' for r in rows);assert all(r['concept_id'] is None for r in rows);assert all(r['pending_phase_170_backfill'] for r in rows);assert all(r['name']=='volatility_pct__equity' for r in rows);assert d['preregistration']['declared_at'] < d['evaluated_at'];print('artifact ok',len(rows))" && test -s docs/analysis/145-volatility-pct-pilot-gate-cascade.md && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "select count(*) from concept_transition_log where domain='regime_model';" | grep -qx " *0" && echo "no db writes confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/analysis/145-volatility-pct-pilot-gate-cascade.json` exists, parses as JSON, and
      contains top-level keys `preregistration`, `evaluated_at`, `gates`, `effective_n_distribution`,
      `orthogonality_correlations`, `fdr`, and `transition_log_rows`.
    - Every element of `transition_log_rows` has `domain == "regime_model"`, `concept_id is None`,
      `pending_phase_170_backfill is True`, and `name == "volatility_pct__equity"`.
    - `preregistration.declared_at` is strictly earlier than the top-level `evaluated_at`.
    - `docs/analysis/145-volatility-pct-pilot-gate-cascade.md` exists, is non-empty, and contains a
      section stating the derived effective-N floor with its arithmetic and its raw-bar equivalent.
    - `SELECT count(*) FROM concept_transition_log WHERE domain='regime_model'` returns 0.
    - `SELECT count(*) FROM concept_registry WHERE domain='regime_model'` returns 0.
    - `git status --short production/migrations/` shows no changes.
    - The markdown artifact contains a "what this does not prove" section naming the single-candidate,
      single-group, single-tf limitations.
    - `.venv/bin/pytest tests/unit/ -q` still passes.
  </acceptance_criteria>
  <done>The pilot ran end to end against real corpus data, every gate result including failures is durably recorded in a concept_transition_log-shaped artifact marked pending Phase 170 backfill, and zero database rows were written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pilot script → production database | A read-only analysis script with credentials that could write |
| gate result → promotion claim | An un-preregistered, post-hoc-selected stratum presented as evidence |
| design-doc target schema → live schema | Planning against `concept_registry`'s eventual shape instead of its current CHECK constraint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-18 | Tampering | Pilot writing to `concept_registry`/`concept_transition_log` | mitigate | `check_precondition` asserts the live `domain` CHECK still excludes `regime_model` and halts if it changed; a source-level grep gate proves the script contains zero INSERT/UPDATE/DELETE/DDL; a post-run psql count asserts 0 `regime_model` rows |
| T-145-19 | Repudiation | Post-hoc stratum or symbol selection | mitigate | `build_preregistration()` emits the scope from module constants and is written to the artifact with `declared_at`, and `build_transition_log_rows` raises if `declared_at` is not earlier than `evaluated_at` |
| T-145-20 | Information Disclosure (dropped evidence) | Failed gate results discarded | mitigate | An explicit unit test asserts a failing cascade still produces transition-log rows; the project principle is never to drop data that could contain signal |
| T-145-21 | Tampering | Reading synthetic/carry-forward bars as if tradeable | mitigate | The pilot never queries `market_data_ohlcv`; it reads `feature_vectors`/`forward_returns`/`market_regimes` only, and the `forward_returns` query is pinned to `return_type='executable_open_to_open'` per Invariant 1 |
| T-145-22 | Denial of Service | Heavy corpus read contending with the in-flight universe backfill | accept | Scope is 5 symbols at one timeframe, roughly 180k rows, read-only; the pre-registered scope is fixed and must not be widened |
| T-145-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; `psycopg`/`numpy`/`pandas`/`statsmodels` are pre-existing verified dependencies |
</threat_model>

<verification>
1. `.venv/bin/pytest tests/unit/test_volatility_pct_pilot_artifact.py -v` — green.
2. `.venv/bin/pytest tests/unit/ -q` — full suite green, including the OHLCV boundary test.
3. `.venv/bin/ruff check . --fix && .venv/bin/black .` — clean.
4. `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "select count(*) from concept_transition_log where domain='regime_model';"` returns 0.
5. `git status --short` shows no changes under `production/migrations/` or `services/`.
</verification>

<success_criteria>
- `volatility_pct` completed the full corrected gate stack against real corpus data.
- The effective-N floor and the orthogonality correlation distribution are derived from real
  measurements with stated arithmetic, not guessed.
- Every gate result, pass or fail, is durably recorded in a `concept_transition_log`-shaped JSON
  artifact plus a written analysis doc, both explicitly marked pending Phase 170 backfill.
- Zero database rows written; zero migrations; the live `concept_registry` CHECK is untouched.
</success_criteria>

<output>
Create `.planning/phases/145-stratificationdimension-formalization-planned/145-05-SUMMARY.md` when done
</output>
