---
phase: 145-stratificationdimension-formalization-planned
plan: 06
type: execute
wave: 5
depends_on: ["145-05"]
files_modified:
  - src/intelligence/stratification/gates.py
  - tests/unit/test_stratification_gates.py
  - docs/research/stratification-dimension-unification.md
  - docs/foundation/apr-calibration-backlog.md
autonomous: true
requirements: [D-03, D-04, D-06, D-07, GOAL-candidate-scoping]
must_haves:
  truths:
    - "alpha.regime_stratification.max_correlation has a real, empirically-derived value traceable to the pilot's measured correlation distribution, not a guessed round number"
    - "alpha.regime_stratification.effective_n_floor has a real, transition-derived value with stated arithmetic, replacing the design doc's raw-bar 'N > 20,000' criterion"
    - "Both derived constants are still module constants carrying an explicit D-07 / Phase 170 APR-deferral comment, and the deferred migration is registered in the APR calibration backlog so it is not silently lost"
    - "The canonical design doc records the pilot's verdict and states which candidate dimensions are worth planning next, with the reason for each"
    - "No concept_registry or concept_transition_log row is written"
  artifacts:
    - path: "src/intelligence/stratification/gates.py"
      provides: "Derived, no-longer-None orthogonality and effective-N thresholds"
      contains: "_EFFECTIVE_N_FLOOR_DEFAULT"
    - path: "docs/research/stratification-dimension-unification.md"
      provides: "Pilot verdict, corrected gate criteria, and the next-candidate scoping this phase's goal asks for"
    - path: "docs/foundation/apr-calibration-backlog.md"
      provides: "Registered record of the four deferred alpha.regime_stratification.* APR keys awaiting Phase 170"
  key_links:
    - from: "src/intelligence/stratification/gates.py"
      to: "docs/analysis/145-volatility-pct-pilot-gate-cascade.md"
      via: "constant provenance comment citing the deriving pilot artifact"
      pattern: "145-volatility-pct-pilot-gate-cascade"
---

<objective>
Close the loop: take the two empirical values the pilot derived, bake them into the gate cascade
as no-longer-`None` constants with full provenance, register the deferred APR migration so it is
not lost, and record the phase's verdict plus the next-candidate scoping in the canonical design
doc.

Purpose: D-06's stated rationale is that running one candidate all the way through "forces
`alpha.regime_stratification.max_correlation` (currently no default asserted) to get a real
empirically-derived value rather than remaining open-ended config," and D-04 makes deriving the
effective-N floor "a real deliverable of this phase, not a caveat to note and defer indefinitely."
This plan is where both land. It also satisfies the ROADMAP Phase 145 goal's third clause: scope
which candidate dimensions are worth planning next.

Output: updated `gates.py` constants and tests, an updated canonical design doc, and an APR
calibration-backlog entry for the four deferred keys.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-stratificationdimension-formalization-planned/145-CONTEXT.md
@.planning/phases/145-stratificationdimension-formalization-planned/145-RESEARCH.md
@CLAUDE.md
</context>

<interfaces>
Inputs produced by plan 145-05 (read them, do not recompute):
    docs/analysis/145-volatility-pct-pilot-gate-cascade.json
        keys: preregistration, evaluated_at, gates, effective_n_distribution,
              orthogonality_correlations, fdr, transition_log_rows
    docs/analysis/145-volatility-pct-pilot-gate-cascade.md
        the written derivation, including the effective-N floor arithmetic and its raw-bar equivalent

Constants to update in `src/intelligence/stratification/gates.py` (currently `None` by design):
    _MAX_CORRELATION_DEFAULT: float | None    -> target APR key alpha.regime_stratification.max_correlation
    _EFFECTIVE_N_FLOOR_DEFAULT: int | None    -> target APR key alpha.regime_stratification.effective_n_floor

Constants already set, unchanged by this plan, but which must also appear in the APR backlog entry:
    _FDR_ALPHA_DEFAULT = 0.05                 -> alpha.regime_stratification.fdr_alpha
    _MIN_HOLD_BARS_DEFAULT = 3                -> alpha.regime_stratification.min_hold_bars
                                                 (mirrors the live feature.hmm.min_hold_bars = 3)

`docs/foundation/apr-calibration-backlog.md` — the project's existing inventory of `config_schema`
rows by provenance tag. Its stated purpose is to be checked before assuming a threshold has a real
empirical basis. This plan adds a section for keys that do not exist in `config_schema` YET, with
the reason (D-07 sequencing behind Phase 170) and the provenance each will carry when migrated.

Provenance tags used by this project's APR descriptions: `[initial_estimate]`, `[conventional]`,
`[rca_analysis]`, `[user_preference]`. The pilot-derived values are `[rca_analysis]`-class
(measured against real corpus data), not `[conventional]`.

**D-07 remains binding.** This plan does NOT create a migration and does NOT insert into
`config_schema`/`config_state`. The values stay as module constants. The only change is that they
are now derived rather than `None`, and the deferred migration is registered so Phase 170's
follow-up has a written handle.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bake the pilot-derived thresholds into gates.py with full provenance</name>
  <files>src/intelligence/stratification/gates.py, tests/unit/test_stratification_gates.py</files>
  <read_first>
    - docs/analysis/145-volatility-pct-pilot-gate-cascade.json (the derived values and their supporting distributions)
    - docs/analysis/145-volatility-pct-pilot-gate-cascade.md (the stated derivation arithmetic to cite verbatim in the constant comments)
    - src/intelligence/stratification/gates.py (current constant block and the `ValueError` paths that fire when a threshold is None)
    - tests/unit/test_stratification_gates.py (the existing refuse-to-guess tests that must be adapted, not deleted)
    - docs/foundation/adaptive-parameter-registry.md (the provenance-tag vocabulary and the migration pattern this deferral is an exception to)
  </read_first>
  <behavior>
    - `_MAX_CORRELATION_DEFAULT` is a float, no longer `None`, equal to the value derived in the
      pilot artifact.
    - `_EFFECTIVE_N_FLOOR_DEFAULT` is an int, no longer `None`, equal to the value derived in the
      pilot artifact.
    - `gate1_orthogonality` called with default arguments for a non-exempt candidate no longer
      raises; it evaluates against the derived threshold.
    - `gate2_substitution_test` called with default arguments no longer raises; it evaluates
      against the derived floor.
    - The refuse-to-guess behavior is PRESERVED for explicit `None`: passing
      `max_correlation=None` or `effective_n_floor=None` explicitly still raises `ValueError`
      naming the target APR key. The existing tests are updated to pass `None` explicitly rather
      than deleted — losing that guard would be a regression.
    - A new test asserts each derived constant's value matches the value recorded in
      `docs/analysis/145-volatility-pct-pilot-gate-cascade.json`, so the code and the artifact
      cannot silently drift apart.
  </behavior>
  <action>
Read the derived values out of `docs/analysis/145-volatility-pct-pilot-gate-cascade.json` and
`.md` (plan 145-05's output). Do not invent, round, or "tidy" them; use exactly what the pilot
derived.

In `src/intelligence/stratification/gates.py`, replace the two `None` sentinels with the derived
values. Each constant's comment must now carry:
  - the target APR key name,
  - the derived value's basis in one sentence (for `max_correlation`: the measured correlation of
    `volatility_pct`'s score against each incumbent continuous score, and the headroom rule used
    to turn that distribution into a threshold; for `effective_n_floor`: the transition-derived
    arithmetic and its raw-bar equivalent in this corpus),
  - a citation of `docs/analysis/145-volatility-pct-pilot-gate-cascade.md` as the deriving artifact,
  - the provenance tag `[rca_analysis]` (measured, not conventional),
  - the existing D-07 / Phase 170 deferral note, unchanged — these are still not APR-backed.

For `_EFFECTIVE_N_FLOOR_DEFAULT` specifically, the comment must also state that this REPLACES the
design doc's `N > 20,000 bars` criterion and why (D-04: HMM and percentile-rank regime states are
autocorrelated by construction and further smoothed by `min_hold_bars`, so consecutive same-state
bars are near-duplicate observations, not independent draws).

In `tests/unit/test_stratification_gates.py`:
  - Update the two refuse-to-guess tests to pass `max_correlation=None` / `effective_n_floor=None`
    EXPLICITLY, keeping their `pytest.raises(ValueError, match=<apr key name>)` assertions intact.
  - Add `TestDerivedThresholds` with: a test that `_MAX_CORRELATION_DEFAULT` is a float in the
    open interval (0.0, 1.0); a test that `_EFFECTIVE_N_FLOOR_DEFAULT` is a positive int; and a
    drift test that loads `docs/analysis/145-volatility-pct-pilot-gate-cascade.json` and asserts
    both constants equal the recorded derived values. Guard the drift test with
    `pytest.mark.skipif` on the artifact file's absence so the suite stays runnable in a clean
    checkout, but do NOT make the skip unconditional.
  - Add a test that `gate1_orthogonality` and `gate2_substitution_test` now succeed with default
    arguments on a fixture that previously would have raised.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_stratification_gates.py -x -q && .venv/bin/pytest tests/unit/ -q 2>&1 | tail -3 && .venv/bin/python -c "from src.intelligence.stratification.gates import _EFFECTIVE_N_FLOOR_DEFAULT as f, _MAX_CORRELATION_DEFAULT as c; assert isinstance(f,int) and f>0, f; assert isinstance(c,float) and 0.0<c<1.0, c; print('derived',f,c)"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_stratification_gates.py -x -q` passes with 0 failures.
    - `.venv/bin/pytest tests/unit/ -q` passes (no regression).
    - `_MAX_CORRELATION_DEFAULT` is a `float` strictly between 0.0 and 1.0; `_EFFECTIVE_N_FLOOR_DEFAULT`
      is a positive `int` (asserted by the automated verify command above).
    - `grep -c "145-volatility-pct-pilot-gate-cascade" src/intelligence/stratification/gates.py` returns at least 2.
    - `grep -c "rca_analysis" src/intelligence/stratification/gates.py` returns at least 2.
    - `grep -c "D-07" src/intelligence/stratification/gates.py` returns at least 4 (deferral notes preserved).
    - `grep -c "20,000\|20000" src/intelligence/stratification/gates.py` returns at least 1 and appears
      only inside the comment explaining what the effective-N floor replaces.
    - The two `pytest.raises(ValueError, ...)` refuse-to-guess tests still exist in
      `tests/unit/test_stratification_gates.py` (`grep -c "pytest.raises(ValueError" ` returns at least 2).
    - `git status --short production/migrations/` shows no changes.
    - `.venv/bin/ruff check src/intelligence/stratification/` and
      `.venv/bin/black --check src/intelligence/stratification/` both exit 0.
  </acceptance_criteria>
  <done>Both thresholds carry real derived values with full provenance, the refuse-to-guess guard survives for explicit None, and a drift test binds the code to the deriving artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Record the verdict, the corrected gate criteria, and the next-candidate scoping</name>
  <files>docs/research/stratification-dimension-unification.md, docs/foundation/apr-calibration-backlog.md</files>
  <read_first>
    - docs/research/stratification-dimension-unification.md (read § Governance lines ~391-510 and § Candidate Dimensions lines ~511-590 — the gate criteria being corrected and the candidate table being scoped)
    - docs/analysis/145-volatility-pct-pilot-gate-cascade.md (the verdict and numbers being recorded)
    - docs/analysis/145-volatility-pct-pilot-gate-cascade.json (the per-gate results including any failures)
    - docs/foundation/apr-calibration-backlog.md (the existing inventory structure this entry must match)
    - .planning/phases/145-stratificationdimension-formalization-planned/145-CONTEXT.md (D-03, D-04, D-06 — the safeguards now shipped, and the deferred-candidate list that must stay backlog)
  </read_first>
  <action>
Edit `docs/research/stratification-dimension-unification.md`:

1. In § Governance gate 1, replace "no default asserted until the first study runs — needs
   empirical judgment, not a guessed constant" with the derived `max_correlation` value, its
   empirical basis (the pilot's measured per-incumbent correlations), and a citation of
   `docs/analysis/145-volatility-pct-pilot-gate-cascade.md`.
2. In § Governance gate 2, replace the pass criterion's `N > 20,000 bars` clause with the
   effective-N criterion: N is the count of independent state-visits from
   `effective_n_from_transitions`, not raw bars, with the derived floor value and its raw-bar
   equivalent in this corpus. State the D-04 rationale in one sentence.
3. Insert a new numbered stage between gate 0 and gate 1 documenting the D-05 acausal-placebo
   registration gate as SHIPPED, naming
   `src/intelligence/stratification/acausal_placebo_gate.py`, its two sub-checks
   (prefix invariance and placebo IC), and the fact that it raises rather than warns. State the
   follow-up finding surfaced by the pilot: the incumbent `hmm_price_vol` provider has never been
   run through this gate and, given todo 248's confirmed full-history parameter fit, would be
   expected to fail the prefix-invariance sub-check — flag as a follow-up, not a Phase 145
   deliverable.
4. Add a stage after gate 2 documenting the D-03 BH-FDR correction as SHIPPED, naming
   `src/intelligence/stratification/fdr.py`, stating the family is the `regime_group`'s cumulative
   candidate-test history (every test, pass or fail), and that promotion to `active` requires both
   the raw criterion and FDR rejection.
5. Add a `## Phase 145 pilot result` section recording: the pre-registered scope verbatim, the
   verdict for `volatility_pct` at `(equity, 1h)`, whether it is promotion-eligible after FDR, and
   an explicit statement that the result covers one candidate, one regime_group, and one timeframe.
   If the pilot FAILED any gate, record the failure plainly — a failed gate is the result, not
   something to soften.
6. In § Candidate Dimensions, add a "Next to plan" note answering the ROADMAP Phase 145 goal's
   third clause. Rank the remaining candidates by what the pilot actually taught, and give a
   one-line reason each. `dispersion` (the other orthogonality-exempt candidate, cross-sectional,
   invisible to the per-symbol HMM by construction) and `term_structure` (per regime_group,
   computable from active symbols) are the natural next two. State explicitly, per D-06, that none
   of them are built or tested in this phase and each still enters through the same corrected
   cascade. Do NOT promote any deferred candidate into scope here.
7. Record the FDR-budget consequence: every future candidate test appends to its `regime_group`'s
   family, so opening the candidate pool has a measurable statistical cost, which is the reason
   data-driven candidate generation stays deferred (cross-reference the "candidate generation is
   theory-first" subsection added by plan 145-01).

Edit `docs/foundation/apr-calibration-backlog.md`: add a section for the four
`alpha.regime_stratification.*` keys that do not yet exist in `config_schema` —
`fdr_alpha`, `max_correlation`, `effective_n_floor`, `min_hold_bars` — recording for each: the
current module-constant value, where it lives
(`src/intelligence/stratification/gates.py`), its provenance tag (`[rca_analysis]` for the two
pilot-derived values, `[conventional]` for `fdr_alpha`, and for `min_hold_bars` a note that it
mirrors the live `feature.hmm.min_hold_bars = 3` deliberately for smoothing parity), and the
reason it is not yet APR-backed (D-07: the migration sequences after Phase 170's
`feature_registry` to `concept_registry` work lands). Also list
`alpha.regime_stratification.placebo_alpha`/`.placebo_shuffles` from
`acausal_placebo_gate.py` and `alpha.volatility_regime.low_pct`/`.high_pct` plus
`alpha.regime_stratification.volatility_pct.input_column` from `volatility_pct.py` in the same
section. This entry is what stops the deferral from becoming silent drift.

Writing rules: state corrected facts plainly, no review-narrative annotations, no em dashes in
newly written prose.
  </action>
  <verify>
    <automated>grep -c "effective_n_from_transitions" docs/research/stratification-dimension-unification.md && grep -c "Phase 145 pilot result" docs/research/stratification-dimension-unification.md && grep -c "acausal_placebo_gate" docs/research/stratification-dimension-unification.md && grep -c "alpha.regime_stratification.effective_n_floor" docs/foundation/apr-calibration-backlog.md && grep -c "alpha.regime_stratification.max_correlation" docs/foundation/apr-calibration-backlog.md && ! grep -q "no default asserted until the first study runs" docs/research/stratification-dimension-unification.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/research/stratification-dimension-unification.md` contains a `## Phase 145 pilot result`
      section naming the pre-registered scope and the verdict.
    - The literal string `no default asserted until the first study runs` no longer appears in
      `docs/research/stratification-dimension-unification.md`.
    - `grep -c "N > 20,000 bars" docs/research/stratification-dimension-unification.md` returns 0
      as a live pass criterion; any surviving mention is inside text explaining what it was replaced by.
    - `grep -c "effective_n_from_transitions" docs/research/stratification-dimension-unification.md` returns at least 1.
    - `grep -c "acausal_placebo_gate" docs/research/stratification-dimension-unification.md` returns at least 1.
    - `grep -c "src/intelligence/stratification/fdr.py" docs/research/stratification-dimension-unification.md` returns at least 1.
    - The § Candidate Dimensions section contains a "Next to plan" note naming `dispersion` and
      `term_structure` with a one-line reason each, and stating none are built in this phase.
    - `docs/foundation/apr-calibration-backlog.md` lists all four `alpha.regime_stratification.*`
      keys plus `alpha.volatility_regime.low_pct`/`.high_pct`, each with a value, a source file, a
      provenance tag, and the D-07 / Phase 170 deferral reason.
    - `SELECT count(*) FROM concept_registry WHERE domain='regime_model'` still returns 0 and
      `git status --short production/migrations/` shows no changes.
    - No em dashes in newly written prose.
  </acceptance_criteria>
  <done>The canonical design doc records the shipped safeguards, the corrected gate criteria, the pilot verdict, and the next-candidate scoping; the deferred APR migration is registered in the calibration backlog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pilot artifact → shipped gate constant | A derived value transcribed into code can be rounded, mistyped, or later drift from its source |
| deferred APR migration → institutional memory | An intentional exception with no written handle becomes indistinguishable from an oversight |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-23 | Tampering | Transcription of derived thresholds into `gates.py` | mitigate | A drift test loads `docs/analysis/145-volatility-pct-pilot-gate-cascade.json` and asserts both constants equal the recorded derived values |
| T-145-24 | Repudiation | Loss of the refuse-to-guess guard once defaults are set | mitigate | The two `pytest.raises(ValueError, match=<apr key>)` tests are updated to pass `None` explicitly rather than deleted; an acceptance criterion counts their continued presence |
| T-145-25 | Repudiation | D-07's APR deferral silently becoming permanent drift | mitigate | All seven deferred `alpha.regime_stratification.*` / `alpha.volatility_regime.*` keys are registered in `docs/foundation/apr-calibration-backlog.md` with value, source file, provenance tag, and deferral reason |
| T-145-26 | Elevation of Privilege | Scope creep promoting a deferred candidate during the doc pass | mitigate | The "Next to plan" note is explicitly labelled as not-built-this-phase; no code for `dispersion`/`term_structure` is added and `files_modified` contains no new provider module |
| T-145-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
1. `.venv/bin/pytest tests/unit/ -v` — full suite green.
2. `.venv/bin/ruff check . --fix && .venv/bin/black .` — clean.
3. `.venv/bin/python -c "from src.intelligence.stratification.gates import _EFFECTIVE_N_FLOOR_DEFAULT, _MAX_CORRELATION_DEFAULT; print(_EFFECTIVE_N_FLOOR_DEFAULT, _MAX_CORRELATION_DEFAULT)"` — prints two non-None values.
4. `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "select count(*) from concept_registry where domain='regime_model';"` returns 0.
5. `git status --short` shows no changes under `production/migrations/` or `services/`.
</verification>

<success_criteria>
- D-06's stated purpose is met: `alpha.regime_stratification.max_correlation` is no longer
  open-ended config, it has an empirically-derived value.
- D-04's stated purpose is met: the effective-N floor is a derived deliverable of this phase with
  stated arithmetic, not a deferred caveat.
- The refuse-to-guess guard survives for explicit `None`.
- The design doc records the shipped D-03/D-04/D-05 safeguards, the pilot verdict including any
  failure, and the next-candidate scoping the ROADMAP goal asks for.
- The seven deferred APR keys are registered in the calibration backlog; no migration was created.
</success_criteria>

<output>
Create `.planning/phases/145-stratificationdimension-formalization-planned/145-06-SUMMARY.md` when done
</output>
