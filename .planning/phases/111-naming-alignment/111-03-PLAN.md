---
phase: 111-naming-alignment
plan: 03
type: execute
wave: 3
depends_on:
  - 111-02
files_modified:
  - services/bar_aggregator.py
  - services/bar_auditor.py
  - services/bar_writer.py
  - services/macro_analyzer.py
  - services/narrative_swarm.py
  - services/feature_writer.py
  - services/signal_writer.py
  - services/signal_tracker.py
  - services/context_writer.py
  - services/lineage_writer.py
  - services/swarm_ledger_writer.py
  - services/graduation_writer.py
  - services/signal_auditor.py
  - services/alert_monitor.py
  - services/data_quality_auditor.py
  - services/ml_discovery_analyzer.py
  - services/ml_orchestrator.py
  - src/core/ai/base_agent.py
  - src/core/agent/base.py
autonomous: true
requirements:
  - ALIGN-04

must_haves:
  truths:
    - "All structlog event strings in services match {derived_agent_id}.{action} pattern"
    - "No structlog event string embeds an old _agent suffix in its prefix segment"
    - "src/core/agent/base.py event strings use daemon. prefix instead of agent. prefix"
    - "src/core/ai/base_agent.py event strings use ai_worker. prefix instead of ai_agent. prefix"
  artifacts:
    - path: "services/bar_aggregator.py"
      provides: "structlog events with bar_aggregator. prefix"
      contains: "bar_aggregator."
    - path: "src/core/agent/base.py"
      provides: "structlog events with daemon. prefix"
      contains: "daemon.starting"
    - path: "src/core/ai/base_agent.py"
      provides: "structlog events with ai_worker. prefix"
      contains: "ai_worker."
  key_links:
    - from: "services/bar_aggregator.py structlog events"
      to: "bar_aggregator derived agent_id"
      via: "prefix match: bar_aggregator."
      pattern: "\"bar_aggregator\\."
    - from: "src/core/agent/base.py structlog events"
      to: "daemon. prefix convention"
      via: "prefix match: daemon."
      pattern: "\"daemon\\."
---

<objective>
Replace all structlog event string prefixes across 20 files so they match the derived agent_id for each service. This is a mechanical sed-style replacement pass — no behavior change, only string literals updated.

Purpose: Operators searching Loki/logs by event prefix get correct results. Events like "bar_aggregator_agent.htf_bar_published" become "bar_aggregator.htf_bar_published", matching the Prometheus agent_id label set in Wave 1.
Output: ~100 event string prefixes updated across 20 files; all match {derived_agent_id}.{action} pattern.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/111-naming-alignment/111-02-SUMMARY.md
@docs/plans/2026-05-30-phase-111-naming-alignment.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace structlog event prefixes in services/</name>
  <files>
    services/bar_aggregator.py
    services/bar_auditor.py
    services/bar_writer.py
    services/macro_analyzer.py
    services/narrative_swarm.py
    services/feature_writer.py
    services/signal_writer.py
    services/signal_tracker.py
    services/context_writer.py
    services/lineage_writer.py
    services/swarm_ledger_writer.py
    services/graduation_writer.py
    services/signal_auditor.py
    services/alert_monitor.py
    services/data_quality_auditor.py
    services/ml_discovery_analyzer.py
    services/ml_orchestrator.py
  </files>
  <read_first>
    - Each file being modified: read the file first to count event strings and understand old prefix before editing. Confirm the old prefix actually appears before making changes.
    - Check whether graduation_compute.py and signal_metrics_compute.py (from D-05 note) also have stale prefixes: grep -n "logger\.\(info\|warning\|error\|debug\)" services/graduation_analyzer.py services/signal_metrics_analyzer.py
  </read_first>
  <action>
For each file, do a targeted replacement of the old prefix string to the new prefix string in structlog event arguments. The event string is the first positional argument to self.logger.info/warning/error/debug/exception calls.

**Replacement table (from CONTEXT.md D-05):**

| File | Old prefix | New prefix |
|---|---|---|
| services/bar_aggregator.py | `bar_aggregator_agent.` | `bar_aggregator.` |
| services/bar_auditor.py | `bar_auditor_agent.` | `bar_auditor.` |
| services/bar_writer.py | `bar_writer_agent.` | `bar_writer.` |
| services/macro_analyzer.py | `macro_compute_agent.` | `macro_analyzer.` |
| services/narrative_swarm.py | `narrative_group_compute_agent.` | `narrative_swarm.` |
| services/feature_writer.py | `feature_writer_agent.` | `feature_writer.` |
| services/signal_writer.py | `signal_writer_agent.` | `signal_writer.` |
| services/signal_tracker.py | various stale prefixes | `signal_tracker.` |
| services/context_writer.py | `ctx_writer_agent.` | `context_writer.` |
| services/lineage_writer.py | `lineage_writer_agent.` | `lineage_writer.` |
| services/swarm_ledger_writer.py | `swarm_ledger_writer_agent.` | `swarm_ledger_writer.` |
| services/graduation_writer.py | `graduation_writer_agent.` | `graduation_writer.` |
| services/signal_auditor.py | `signal_auditor_agent.` | `signal_auditor.` |
| services/alert_monitor.py | `alerting_agent.` | `alert_monitor.` |
| services/data_quality_auditor.py | `ml_data_quality_agent.` | `data_quality_auditor.` |
| services/ml_discovery_analyzer.py | `ml_discovery_agent.` | `ml_discovery_analyzer.` |
| services/ml_orchestrator.py | `ml_orchestrator_agent.` | `ml_orchestrator.` |

**For services/signal_tracker.py**, grep the file first to find the actual stale prefixes:
```bash
grep -n '"signal_tracker_compute_agent\.\|"SignalTrackerComputeAgent\.' services/signal_tracker.py | head -20
```
Replace all found stale prefixes with `signal_tracker.`.

**For graduation_compute.py and signal_metrics_compute.py** — check if they have stale prefixes:
```bash
grep -n '"graduation_compute_agent\.\|"GraduationComputeAgent\.' services/graduation_analyzer.py | head -10
grep -n '"signal_metrics_compute_agent\.\|"signal_metrics_compute\.' services/signal_metrics_analyzer.py | head -10
```
If stale prefixes exist, fix them to `graduation_analyzer.` and `signal_metrics_analyzer.` respectively.

**Method:** Read each file, identify event strings, make targeted string replacements. Do not use sed on whole files — read the file, find the exact lines, use the Edit tool to make precise replacements. This avoids accidentally replacing similar strings in comments or non-event contexts.

**Scope:** Only event strings — the first positional string argument to self.logger.info/warning/error/debug/exception. Do not modify other string literals or comments.
  </action>
  <verify>
    grep -rn '"bar_aggregator_agent\.\|"bar_auditor_agent\.\|"bar_writer_agent\.\|"feature_writer_agent\.\|"signal_writer_agent\.\|"signal_auditor_agent\.\|"graduation_writer_agent\.\|"lineage_writer_agent\.\|"alerting_agent\.\|"ctx_writer_agent\.\|"ml_data_quality_agent\.\|"ml_discovery_agent\.\|"ml_orchestrator_agent\.\|"swarm_ledger_writer_agent\.\|"narrative_group_compute_agent\.\|"macro_compute_agent\.\|"signal_tracker_compute_agent\.' services/
  </verify>
  <done>Zero stale event string prefixes remain in services/; all event strings use the derived agent_id prefix matching the class name</done>
  <acceptance_criteria>
    - `grep -rn '"bar_aggregator_agent\.\|"bar_auditor_agent\.\|"bar_writer_agent\.\|"feature_writer_agent\.\|"signal_writer_agent\.\|"signal_auditor_agent\.\|"graduation_writer_agent\.\|"alerting_agent\.\|"ctx_writer_agent\.\|"ml_data_quality_agent\.\|"ml_discovery_agent\.\|"ml_orchestrator_agent\."' services/` returns zero results
    - `grep -n '"bar_aggregator\.' services/bar_aggregator.py` returns one or more results (confirms replacement happened)
    - `grep -n '"signal_tracker\.' services/signal_tracker.py` returns one or more results
    - `grep -n '"alert_monitor\.' services/alert_monitor.py` returns one or more results
    - `.venv/bin/pytest tests/unit/ -q` exits 0
    - `.venv/bin/ruff check services/` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace event prefixes in src/core/agent/base.py and src/core/ai/base_agent.py</name>
  <files>
    src/core/agent/base.py
    src/core/ai/base_agent.py
  </files>
  <read_first>
    - src/core/agent/base.py (grep for all self.logger event strings — they use "agent." prefix now, need to become "daemon.")
    - src/core/ai/base_agent.py (grep for all self.logger event strings — they use "ai_agent." prefix now, need to become "ai_worker.")
  </read_first>
  <action>
**src/core/agent/base.py:**
Find all structlog event strings with prefix `"agent."` and replace with `"daemon."`.
Current known events (verify by reading the file):
- `"agent.starting"` → `"daemon.starting"`
- `"agent.stopped"` → `"daemon.stopped"`
- `"agent.setup_failed"` → `"daemon.setup_failed"`
- `"agent.run_failed"` → `"daemon.run_failed"`
- `"agent.setup_retry"` → `"daemon.setup_retry"`
- `"agent.config_pre_load_unexpected_error"` → `"daemon.config_pre_load_unexpected_error"`
- `"agent.config_consumer_unexpected_error"` → `"daemon.config_consumer_unexpected_error"`
- `"agent.stall_detected"` → `"daemon.stall_detected"`
- `"agent.dlq_discard"` → `"daemon.dlq_discard"`
- `"agent.dlq_routed"` → `"daemon.dlq_routed"`
- `"agent.dlq_no_producer"` → `"daemon.dlq_no_producer"`
- `"agent.dlq_route_failed"` → `"daemon.dlq_route_failed"`

Read the file first, find all `"agent.` event strings, replace each one.

**src/core/ai/base_agent.py:**
Find all structlog event strings with prefix `"ai_agent."` and replace with `"ai_worker."`.
Grep the file: `grep -n '"ai_agent\.' src/core/ai/base_agent.py`
Replace each occurrence.

Do not modify any `agent=self.name` keyword argument or other uses of the word "agent" in non-event-string contexts.
  </action>
  <verify>
    grep -n '"agent\.' src/core/agent/base.py
    grep -n '"ai_agent\.' src/core/ai/base_agent.py
  </verify>
  <done>src/core/agent/base.py uses "daemon." event prefix throughout; src/core/ai/base_agent.py uses "ai_worker." event prefix throughout; no "agent." or "ai_agent." event string prefixes remain in these files</done>
  <acceptance_criteria>
    - `grep -c '"agent\.' src/core/agent/base.py` returns 0
    - `grep -c '"ai_agent\.' src/core/ai/base_agent.py` returns 0
    - `grep -n '"daemon\.starting"' src/core/agent/base.py` returns one result
    - `grep -n '"ai_worker\.' src/core/ai/base_agent.py` returns one or more results
    - `.venv/bin/pytest tests/unit/core/ -q` exits 0
    - `.venv/bin/ruff check src/core/agent/base.py src/core/ai/base_agent.py` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. grep -rn '"bar_aggregator_agent\.\|"bar_writer_agent\.\|"feature_writer_agent\.\|"alerting_agent\.\|"signal_auditor_agent\.\|"ctx_writer_agent\.\|"ml_data_quality_agent\.\|"ml_discovery_agent\.\|"graduation_writer_agent\.\|"lineage_writer_agent\.\|"swarm_ledger_writer_agent\.\|"narrative_group_compute_agent\.\|"macro_compute_agent\.' services/ — zero results
2. grep -n '"agent\.' src/core/agent/base.py — zero results
3. grep -n '"ai_agent\.' src/core/ai/base_agent.py — zero results
4. .venv/bin/pytest tests/unit/ -q — passes
5. .venv/bin/ruff check . — clean
</verification>

<success_criteria>
All ~100 structlog event string prefixes updated to match derived agent_id values; daemon. prefix in BaseDaemon; ai_worker. prefix in BaseAIWorker; pytest green; ruff clean.
</success_criteria>

<output>
After completion, create `.planning/phases/111-naming-alignment/111-03-SUMMARY.md`
</output>
