---
phase: 111-naming-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/agent/base.py
  - services/bar_aggregator.py
  - services/bar_auditor.py
  - services/bar_writer.py
  - services/bar_replay_provider.py
  - services/signal_tracker.py
  - services/signal_auditor.py
  - services/signal_replay_auditor.py
  - services/signal_metrics_analyzer.py
  - services/signal_metrics_writer.py
  - services/lineage_writer.py
  - services/graduation_writer.py
  - services/swarm_ledger_writer.py
  - services/context_writer.py
  - services/graduation_analyzer.py
  - services/ml_discovery_analyzer.py
  - services/alert_monitor.py
  - services/feature_writer.py
  - services/provider_merger.py
  - services/intelligence_pipeline.py
  - services/macro_analyzer.py
  - services/narrative_swarm.py
  - services/service_auditor.py
  - production/grafana/dashboards/pipeline-health.json
  - production/grafana/dashboards/operations.json
  - production/grafana/dashboards/plugin-latency.json
  - production/grafana/dashboards/signals-i8.json
  - production/alertmanager-rules.yml
autonomous: true
requirements:
  - ALIGN-01
  - ALIGN-03

must_haves:
  truths:
    - "BaseDaemon auto-derives agent_id from class name; no service passes a stale name= string"
    - "Prometheus agent_id labels reflect current class names (bar_aggregator, not bar_aggregator_agent)"
    - "Log files named logs/<snake_case_class>.log match the class of the running service"
    - "Grafana panels and alertmanager rules reference current agent_id label values"
    - "_AGENT_ID_TO_UNIT keys match the auto-derived agent_id values produced by BaseDaemon"
  artifacts:
    - path: "src/core/agent/base.py"
      provides: "_to_snake_case() utility + optional name= in BaseDaemon.__init__"
      contains: "def _to_snake_case"
    - path: "services/service_auditor.py"
      provides: "_AGENT_ID_TO_UNIT with 11 updated keys"
      contains: "bar_aggregator"
    - path: "production/alertmanager-rules.yml"
      provides: "alertmanager rules with current agent_id values"
  key_links:
    - from: "BaseDaemon.__init__"
      to: "_to_snake_case(self.__class__.__name__)"
      via: "when name is None"
      pattern: "if name is None"
    - from: "services/*"
      to: "BaseDaemon.__init__"
      via: "super().__init__() with no name= arg"
      pattern: "super().__init__\\(\\)"
    - from: "_AGENT_ID_TO_UNIT"
      to: "Prometheus agent_id labels"
      via: "service_auditor lag threshold lookup"
      pattern: "bar_aggregator.*indicagent-bar-aggregator"
---

<objective>
Fix the root cause of stale Prometheus labels, log paths, and monitoring mismatches: make BaseDaemon auto-derive agent_id from the class name so no service can pass a stale string. Remove all 18 name= overrides, 15 setup_service_logging() path overrides, and 2 _AGENT_NAME constants. Fix the hardcoded "feature_writer_agent" metric label. Update 11 _AGENT_ID_TO_UNIT keys and all Grafana/alertmanager label references.

Purpose: Structural enforcement — every future BaseDaemon subclass gets the correct Prometheus label and log path at zero cost. Drift is impossible rather than discouraged.
Output: BaseDaemon with _to_snake_case + optional name=, all services relying on auto-derive, monitoring infrastructure aligned.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-05-30-phase-111-naming-alignment.md
@src/core/agent/base.py
@services/service_auditor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _to_snake_case to BaseDaemon and make name optional</name>
  <files>src/core/agent/base.py</files>
  <read_first>
    - src/core/agent/base.py (full file — understand __init__ signature, log_name derivation at line 124, _last_msg_ts_attrs, _consumer_lag_attrs, _agent_label)
  </read_first>
  <action>
Add module-level `import re` if not present (it is already imported inline in __init__). Add a module-level function before the BaseDaemon class:

```python
def _to_snake_case(name: str) -> str:
    """Convert PascalCase/CamelCase to snake_case.
    BarAggregator -> bar_aggregator, MLDiscoveryAnalyzer -> ml_discovery_analyzer
    """
    s1 = re.sub(r'([A-Z]+)([A-Z][a-z])', r'\1_\2', name)
    s2 = re.sub(r'([a-z0-9])([A-Z])', r'\1_\2', s1)
    return s2.lower()
```

Update `BaseDaemon.__init__` signature from `def __init__(self, name: str, ...)` to `def __init__(self, name: str | None = None, ...)`. At the top of __init__, before the log_name derivation, add:

```python
if name is None:
    name = _to_snake_case(self.__class__.__name__)
```

Remove the inline `import re` at line 124 (it is now at module level). The existing `log_name = re.sub(r"(?<!^)(?=[A-Z])", "_", name).lower()` logic becomes dead code once name is already snake_case from _to_snake_case — leave it as-is since name may still be passed explicitly (it is applied to the already-snake name which is a no-op). The log path derivation continues to work correctly.

Add `import re` at the top of the module-level imports section (after `from __future__ import annotations`, before `import abc`).
  </action>
  <verify>
    python3 -c "from src.core.agent.base import _to_snake_case; assert _to_snake_case('BarAggregator') == 'bar_aggregator'; assert _to_snake_case('MLDiscoveryAnalyzer') == 'ml_discovery_analyzer'; assert _to_snake_case('SignalTracker') == 'signal_tracker'; print('OK')"
  </verify>
  <done>_to_snake_case exists as module-level function; BaseDaemon.__init__ accepts name: str | None = None; calling BarAggregator() without name= argument produces agent_id = "bar_aggregator"</done>
  <acceptance_criteria>
    - `grep -n "def _to_snake_case" src/core/agent/base.py` returns exactly one result
    - `grep -n "name: str | None = None" src/core/agent/base.py` returns a result
    - `grep -n "if name is None:" src/core/agent/base.py` returns a result
    - `python3 -c "from src.core.agent.base import _to_snake_case; assert _to_snake_case('MLDiscoveryAnalyzer') == 'ml_discovery_analyzer'"` exits 0
    - `pytest tests/unit/core/test_base_agent.py -q` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove all name= overrides and setup_service_logging path overrides from services</name>
  <files>
    services/bar_aggregator.py
    services/bar_auditor.py
    services/bar_writer.py
    services/bar_replay_provider.py
    services/signal_tracker.py
    services/signal_auditor.py
    services/signal_replay_auditor.py
    services/signal_metrics_analyzer.py
    services/signal_metrics_writer.py
    services/lineage_writer.py
    services/graduation_writer.py
    services/swarm_ledger_writer.py
    services/context_writer.py
    services/graduation_analyzer.py
    services/ml_discovery_analyzer.py
    services/alert_monitor.py
    services/feature_writer.py
    services/provider_merger.py
    services/intelligence_pipeline.py
    services/macro_analyzer.py
    services/narrative_swarm.py
  </files>
  <read_first>
    - services/bar_aggregator.py (understand super().__init__ call and setup_service_logging usage)
    - services/feature_writer.py (special case — has setup_service_logging with env-var override, verify pattern before removing)
    - services/intelligence_pipeline.py (has _log_file from env var — keep that pattern if it overrides from env, remove only if it is hardcoded stale string)
    - services/macro_analyzer.py (similar env-var pattern)
    - services/signal_metrics_analyzer.py (uses _AGENT_NAME constant)
    - services/signal_metrics_writer.py (uses _AGENT_NAME constant)
    - services/swarm_ledger_writer.py (has explicit setup_service_logging call)
  </read_first>
  <action>
For each service file, make exactly these changes:

**name= override removal (18 services):**
- Remove the `name="..."` argument from every `super().__init__(name="...", ...)` call
- Keep all other arguments (max_idle_seconds=, settings=, **kwargs) unchanged
- Exception: `services/dlq_drain_agent.py` — keep `name=` for now (Wave 2 renames the file; handle there)
- Exception: `services/swarm_ledger_writer.py` — `name="swarm_ledger_writer"` is already correct after auto-derive (class name is `SwarmLedgerWriter` → `swarm_ledger_writer`); remove the override

**_AGENT_NAME constant removal (2 services):**
- `services/signal_metrics_analyzer.py`: delete the line `_AGENT_NAME = "signal_metrics_compute"`. Remove `name=_AGENT_NAME` from super().__init__. For the 3 call sites using `{"agent": _AGENT_NAME}`, replace with `{"agent": self._agent_label}` — this attribute is set by BaseDaemon.__init__ and equals `self.name.lower().replace(" ", "_")`.
- `services/signal_metrics_writer.py`: delete `_AGENT_NAME = "signal_metrics_writer"`. Remove `name=_AGENT_NAME` from super().__init__. No other _AGENT_NAME usages in this file.

**setup_service_logging path override removal (15 services):**
Remove these explicit `setup_service_logging("logs/...")` calls (the stale-path ones). Keep `setup_service_logging` imports only if still used:
- `services/graduation_writer.py:108` — delete `setup_service_logging("logs/graduation_writer_agent.log")` and the import
- `services/context_writer.py:406` — delete `setup_service_logging("logs/ctx_writer_agent.log")` and the import
- `services/ml_discovery_analyzer.py:67` — delete `setup_service_logging("logs/ml_discovery_agent.log")` and the import
- `services/data_quality_auditor.py:50` — delete `setup_service_logging("logs/ml_data_quality_agent.log")` and the import
- `services/ml_orchestrator.py:60` — delete `setup_service_logging("logs/ml_orchestrator_agent.log")` and the import
- `services/swarm_ledger_writer.py:84` — delete `setup_service_logging("logs/swarm_ledger_writer_agent.log")` and the import
- `services/lineage_writer.py:102` — delete `setup_service_logging("logs/lineage_writer_agent.log")` and the import
- `services/narrative_swarm.py:171` — delete `setup_service_logging("logs/narrative_group_compute_agent.log")` and the import
- `services/shadow_auditor_agent.py:338` — delete `setup_service_logging("logs/shadow_auditor_agent.log")` — Wave 2 renames this file; ok to touch now for the path cleanup

For `services/feature_writer.py` and `services/intelligence_pipeline.py` and `services/macro_analyzer.py`: These use env-var driven log paths. Read the actual call site. If the log path is computed from an env var (not hardcoded stale string), leave the override in place — BaseDaemon's auto-derive is the fallback and the env-var override is legitimate.

**Hardcoded metric label fix in feature_writer.py:**
Grep for `"feature_writer_agent"` string in `services/feature_writer.py`. Replace all occurrences of the literal string `"feature_writer_agent"` used as an OTel attribute value with `self._agent_label` (already available from BaseDaemon after removing the name= override, it will be `"feature_writer"`).
  </action>
  <verify>
    grep -rn 'name="bar_aggregator_agent"\|name="bar_writer_agent"\|name="bar_auditor_agent"\|name="alerting_agent"\|name="lineage_writer_agent"\|name="provider_merger_agent"\|name="SignalTrackerComputeAgent"\|name="GraduationComputeAgent"\|name="MLDiscoveryComputeAgent"' services/ && echo "FOUND_STALE" || echo "CLEAN"
  </verify>
  <done>All 18 stale name= strings removed from services; 2 _AGENT_NAME constants deleted; 15 stale setup_service_logging path overrides deleted; "feature_writer_agent" hardcoded label string eliminated from feature_writer.py</done>
  <acceptance_criteria>
    - `grep -rn 'name="bar_aggregator_agent"\|name="bar_writer_agent"\|name="alerting_agent"\|name="lineage_writer_agent"\|name="provider_merger_agent"\|name="SignalTrackerComputeAgent"\|name="GraduationComputeAgent"\|name="MLDiscoveryComputeAgent"' services/` returns zero results
    - `grep -rn '_AGENT_NAME' services/signal_metrics_analyzer.py services/signal_metrics_writer.py` returns zero results
    - `grep -rn '"feature_writer_agent"' services/feature_writer.py` returns zero results (metric label uses self._agent_label)
    - `grep -rn 'setup_service_logging.*graduation_writer_agent\|setup_service_logging.*ctx_writer_agent\|setup_service_logging.*swarm_ledger_writer_agent\|setup_service_logging.*narrative_group_compute' services/` returns zero results
    - `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update _AGENT_ID_TO_UNIT keys and Grafana/alertmanager label values</name>
  <files>
    services/service_auditor.py
    production/grafana/dashboards/pipeline-health.json
    production/grafana/dashboards/operations.json
    production/grafana/dashboards/plugin-latency.json
    production/grafana/dashboards/signals-i8.json
    production/alertmanager-rules.yml
  </files>
  <read_first>
    - services/service_auditor.py lines 128-160 (_AGENT_ID_TO_UNIT dict)
    - production/grafana/dashboards/pipeline-health.json (search for "agent_id" and old label values)
    - production/alertmanager-rules.yml (search for old label values)
  </read_first>
  <action>
**service_auditor.py — update 11 _AGENT_ID_TO_UNIT keys:**
The keys must match the auto-derived agent_id values (what _to_snake_case(ClassName) produces). Replace:
- `"bar_writer_agent"` → `"bar_writer"` (class: BarWriter)
- `"bar_aggregator_agent"` → `"bar_aggregator"` (class: BarAggregator)
- `"intelligence_pipeline_agent"` → `"intelligence_pipeline"` (class: IntelligencePipeline)
- `"feature_writer_agent"` → `"feature_writer"` (class: FeatureWriter)
- `"SignalTrackerComputeAgent"` → `"signal_tracker"` (class: SignalTracker)
- `"signal_writer_agent"` → `"signal_writer"` (class: SignalWriter)
- `"llm_writer_agent"` → `"llm_writer"` (class: LLMWriter)
- `"CrossAssetComputeAgent"` → `"cross_asset_analyzer"` (class: CrossAssetAnalyzer)
- `"bar_auditor_agent"` → `"bar_auditor"` (class: BarAuditor)
- `"provider_merger_agent"` → `"provider_merger"` (class: ProviderMerger)
- `"lifecycle_writer_agent"` → `"lifecycle_writer"` (class: LifecycleWriter)
- `"lineage_writer_agent"` → `"lineage_writer"` (class: LineageWriter)
- `"AlphaSwarmComputeAgent"` → `"alpha_swarm"` (class: AlphaSwarm)
- `"NarrativeGroupComputeAgent"` → `"narrative_swarm"` (class: NarrativeSwarm)
- `"signal_auditor_agent"` → `"signal_auditor"` (class: SignalAuditor)
- `"GraduationComputeAgent"` → `"graduation_analyzer"` (class: GraduationAnalyzer)
- `"graduation_writer_agent"` → `"graduation_writer"` (class: GraduationWriter)
- `"ctx_writer_agent"` → `"context_writer"` (class: ContextWriter)
- `"dlq_drain_agent"` → `"dlq_drain"` (Wave 2 will rename the class; update the key now to match)

Also update the comment at line 126-127 that describes what keys must match — change from "name= argument" to "auto-derived agent_id from BaseDaemon._to_snake_case(ClassName)".

Also update `"indicagent-alerting-agent"` in _DAG_ORDER to `"indicagent-alert-monitor"` if the systemd unit name changed in Phase 110 (grep the systemd dir to confirm the current unit name: `ls production/systemd/ | grep alert`).

**Grafana dashboards — update stale label values:**
- `pipeline-health.json` line 122: change `agent_id=\"feature_writer_agent\"` to `agent_id=\"feature_writer\"`
- `pipeline-health.json` line 243: change `agent=\"bar_aggregator_agent\"` to `agent=\"bar_aggregator\"`
- `operations.json` line 502: change `signal_writer_agent_buffer_overflow_total` to `signal_writer_buffer_overflow_total` if metric name changed, OR keep metric name but fix agent label filter — read the actual line before editing
- Search all 4 JSON files for any other occurrences of `_agent` in label filter expressions and update to match new derived values

**alertmanager-rules.yml:**
Search for hardcoded agent_id filter values referencing old names and update to match new derived names.
  </action>
  <verify>
    grep -n "bar_writer_agent\|bar_aggregator_agent\|feature_writer_agent\|signal_writer_agent\|llm_writer_agent\|bar_auditor_agent\|provider_merger_agent\|lifecycle_writer_agent\|lineage_writer_agent\|graduation_writer_agent\|ctx_writer_agent\|dlq_drain_agent\|SignalTrackerComputeAgent\|CrossAssetComputeAgent\|AlphaSwarmComputeAgent\|NarrativeGroupComputeAgent\|GraduationComputeAgent" services/service_auditor.py production/grafana/dashboards/*.json production/alertmanager-rules.yml
  </verify>
  <done>All _AGENT_ID_TO_UNIT keys updated to derived values; Grafana dashboards and alertmanager rules reference current agent_id label values; no stale _agent suffix in label filter expressions</done>
  <acceptance_criteria>
    - `grep "bar_aggregator_agent\|feature_writer_agent\|bar_writer_agent\|SignalTrackerComputeAgent\|NarrativeGroupComputeAgent\|AlphaSwarmComputeAgent\|GraduationComputeAgent" services/service_auditor.py` returns zero results
    - `grep '"bar_aggregator".*indicagent-bar-aggregator' services/service_auditor.py` returns exactly one result
    - `grep '"feature_writer".*indicagent-feature-writer' services/service_auditor.py` returns exactly one result
    - `grep "feature_writer_agent" production/grafana/dashboards/pipeline-health.json` returns zero results
    - `.venv/bin/ruff check services/service_auditor.py` exits 0
    - `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. pytest tests/unit/ -q — must pass with 0 failures
2. .venv/bin/ruff check . — clean
3. grep -rn 'name="bar_aggregator_agent"\|name="bar_writer_agent"\|name="feature_writer_agent"\|name="alerting_agent"\|name="lineage_writer_agent"\|name="provider_merger_agent"\|name="SignalTrackerComputeAgent"\|name="GraduationComputeAgent"' services/ — zero results
4. python3 -c "from src.core.agent.base import _to_snake_case; assert _to_snake_case('BarAggregator') == 'bar_aggregator'; assert _to_snake_case('MLDiscoveryAnalyzer') == 'ml_discovery_analyzer'" — exits 0
5. grep '"feature_writer_agent"' production/grafana/dashboards/pipeline-health.json services/feature_writer.py — zero results
</verification>

<success_criteria>
BaseDaemon auto-derives agent_id; zero stale name= strings remain in services; _AGENT_ID_TO_UNIT keys match current derived names; Grafana and alertmanager reference current label values; pytest green; ruff clean.
</success_criteria>

<output>
After completion, create `.planning/phases/111-naming-alignment/111-01-SUMMARY.md`
</output>
