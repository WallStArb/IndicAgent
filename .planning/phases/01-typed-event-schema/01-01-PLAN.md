---
phase: 01-typed-event-schema
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/intelligence/schemas.py
  - services/market_analysis_service.py
  - tests/unit/service_tests/test_market_analysis_service.py
autonomous: true
requirements:
  - BUS-01
  - BUS-02

must_haves:
  truths:
    - "IntelligenceEvent is a valid Pydantic v2 BaseModel with schema_version='1.0', platform='futures', source Literal, and all 7 sub-models (OHLCVBar, I1Indicators, I3Structure, I4Context, I5Patterns, SMCContext, I6Confluence)"
    - "Constructing IntelligenceEvent with an unknown field raises ValidationError (extra='forbid' enforced on all sub-models except I1Indicators)"
    - "market_analysis_service._publish_intelligence() emits a single Redis XADD payload with key 'event' containing model_dump_json() output — not flat str(v) fields"
    - "Constructing IntelligenceEvent with realistic plugin output dicts succeeds without ValidationError"
    - "All existing tests pass after publisher update"
  artifacts:
    - path: "src/intelligence/schemas.py"
      provides: "IntelligenceEvent + all sub-models"
      exports: ["IntelligenceEvent", "OHLCVBar", "I1Indicators", "I3Structure", "I4Context", "I5Patterns", "SMCContext", "I6Confluence"]
    - path: "services/market_analysis_service.py"
      provides: "Updated publisher using IntelligenceEvent"
      contains: "model_dump_json"
    - path: "tests/unit/service_tests/test_market_analysis_service.py"
      provides: "Tests for schema + publisher"
      contains: "IntelligenceEvent"
  key_links:
    - from: "services/market_analysis_service.py"
      to: "src/intelligence/schemas.py"
      via: "import IntelligenceEvent and sub-models"
      pattern: "from src\\.intelligence\\.schemas import IntelligenceEvent"
    - from: "market_analysis_service._publish_intelligence()"
      to: "redis XADD"
      via: "event.model_dump_json()"
      pattern: "model_dump_json"
    - from: "_run_analysis_pipeline()"
      to: "tiered dict return"
      via: "dict with i3/i4/i5/smc/i6 keys"
      pattern: "return \\{[\"']i3[\"']"
---

<objective>
Define the IntelligenceEvent Pydantic v2 schema and update market_analysis_service.py to publish validated typed events.

Purpose: Establish the canonical typed contract for the intelligence bus. Every downstream migration in plans 01-02 and 01-03 depends on this schema existing and the publisher emitting it.

Output: src/intelligence/schemas.py with all sub-models; market_analysis_service.py publishing IntelligenceEvent JSON to intelligence: stream; tests proving the schema validates correctly and the publisher produces the new format.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-typed-event-schema/01-CONTEXT.md
@.planning/phases/01-typed-event-schema/01-RESEARCH.md
</context>

<tasks>

<task type="tdd">
  <name>IntelligenceEvent schema + publisher update</name>
  <files>src/intelligence/schemas.py, services/market_analysis_service.py, tests/unit/service_tests/test_market_analysis_service.py</files>
  <behavior>
    Schema behavior (RED tests first):
    - IntelligenceEvent(schema_version="1.0", ts=datetime.now(), symbol="ES", tf="1m", platform="futures", source="live", bar=OHLCVBar(...), i1=I1Indicators(), i3=I3Structure(), i4=I4Context(), i5=I5Patterns(), smc=SMCContext(), i6=I6Confluence()) constructs without error
    - I3Structure(unknown_field=1.0) raises pydantic.ValidationError (extra="forbid")
    - I4Context(unknown_field=1.0) raises pydantic.ValidationError (extra="forbid")
    - I1Indicators(any_extra_field=1.0) does NOT raise (extra="allow")
    - event.model_dump_json() produces valid JSON string containing "schema_version":"1.0"
    - IntelligenceEvent.model_validate_json(event.model_dump_json()) round-trips cleanly
    - I4Context with realistic GARCH/Kalman values (garch_sigma=0.015, kalman_trend=1.2, garch_vol_regime=1) constructs and validates
    - Publisher: mock redis xadd captures call where fields dict has key b"event" (or "event") with JSON value; no individual field keys like b"garch_sigma"

    Cases:
    - valid full event → constructs, serializes, deserializes
    - I3Structure with unknown_field="bad" → ValidationError
    - I4Context garch_vol_regime=1 (int) → validates (not float)
    - I3Structure swing_pattern="HH→HL" (str) → validates (not float)
    - SMCContext bos_detected=True (bool) → validates
    - publisher emits {"event": "<json>"} not flat {"garch_sigma": "0.015", ...}
  </behavior>
  <action>
    STEP 1 — Read plugin output frozensets to complete field lists (before writing any code):

    Read these files to extract exact output field names — required before writing SMCContext and I5Patterns:
    - src/intelligence/smart_money/bocpd_changepoint.py (outputs frozenset)
    - src/intelligence/smart_money/hmm_regime.py (outputs frozenset)
    - src/intelligence/smart_money/liquidity_pools.py (outputs frozenset)
    - src/intelligence/smart_money/supply_demand_zones.py (outputs frozenset)
    - src/intelligence/patterns/ (all pattern plugins: double_top_bottom.py, head_shoulders.py, trend_confluence.py, triangle_wedge.py — read outputs frozenset for each)
    - src/intelligence/structure/ (SwingDetector, SupportResistance, TrendStructure — confirm field names match RESEARCH.md list)
    - src/intelligence/context/ (VolatilityRegime, TrendRegime, MomentumContext, GARCHVolatility, KalmanTrend — confirm field names)

    STEP 2 — RED: Write failing tests first:
    In tests/unit/service_tests/test_market_analysis_service.py, add a TestIntelligenceEventSchema class:
    - test_intelligence_event_constructs_valid_full_event
    - test_i3_structure_rejects_unknown_field
    - test_i4_context_rejects_unknown_field
    - test_i1_indicators_allows_extra_fields
    - test_i4_context_garch_fields_accept_correct_types (garch_vol_regime is int | None)
    - test_i3_structure_swing_pattern_is_str (not float)
    - test_smc_context_bos_detected_is_bool
    - test_intelligence_event_model_dump_json_roundtrip
    Add a TestPublisherFormat class:
    - test_publish_intelligence_emits_single_event_field (mock redis.xadd, verify call args have "event" key, not flat field keys)
    - test_publish_intelligence_drops_event_on_validation_error (inject bad plugin output, verify xadd NOT called)
    Run: pytest tests/unit/service_tests/test_market_analysis_service.py -k "TestIntelligenceEvent or TestPublisher" — ALL MUST FAIL.

    STEP 3 — GREEN: Create src/intelligence/schemas.py:
    Use the complete field names from STEP 1 + RESEARCH.md code examples. Key decisions:
    - OHLCVBar: fields o, h, l, c, v (int) — defined in src/intelligence/schemas.py (NOT reusing providers.base.OHLCVBar which is a different module-path class with a different source field shape)
    - I1Indicators: extra="allow", declare ~15 most-used fields for IDE support (rsi_14, atr_14, macd_12_26_9, bb_20_2_upper, bb_20_2_lower, bb_20_2_middle, volume_ratio, roc_14, stoch_k, stoch_d, adx_14, cci_14, williams_r, mfi_14, obv)
    - I3Structure: extra="forbid", all fields from SwingDetector + SupportResistance + TrendStructure plugins
    - I4Context: extra="forbid", all fields from VolatilityRegime + TrendRegime + MomentumContext + GARCHVolatility + KalmanTrend plugins; garch_vol_regime: int | None (not float)
    - I5Patterns: extra="forbid", all fields from RSIDivergence + BollingerSqueeze + Confluence + VolumeDivergence + the 4 remaining pattern plugins read in STEP 1
    - SMCContext: extra="forbid", all fields from BOSCHoCH + FairValueGap + OrderBlocks + LiquiditySweeps + 4 plugins read in STEP 1; rename SMC trend_direction to smc_trend_direction to avoid collision with I3's trend_direction
    - I6Confluence: extra="forbid", fields from RESEARCH.md examples
    - IntelligenceEvent: extra="forbid", schema_version: Literal["1.0"] = "1.0", platform: str = "futures", source: Literal["live", "backfill"] = "live"

    STEP 4 — Update services/market_analysis_service.py:
    Read the full file first to understand current _run_analysis_pipeline() and _publish_intelligence() structure.
    Changes:
    a) _run_analysis_pipeline(): change return from flat merged dict to tiered dict: {"i3": i3_results, "i4": i4_results, "i5": i5_results, "smc": smc_results, "i6": i6_results}. Keep a "flat" key if downstream callers (e.g., intelligence_cache) depend on the merged form.
    b) _publish_intelligence(): replace the flat str(v) XADD loop with IntelligenceEvent construction + model_dump_json(). Wrap in try/except ValidationError: log error + return (do NOT publish malformed events). Use RESEARCH.md Pattern 2 exactly.
    c) _calculate_intelligence() or _persist_intelligence(): update any callers of _run_analysis_pipeline() that break because return type changed.
    d) Add import: from src.intelligence.schemas import IntelligenceEvent, OHLCVBar, I1Indicators, I3Structure, I4Context, I5Patterns, SMCContext, I6Confluence

    STEP 5 — GREEN: Run failing tests — all should now pass.
    Run: pytest tests/unit/service_tests/test_market_analysis_service.py -k "TestIntelligenceEvent or TestPublisher"

    STEP 6 — REFACTOR: Run full test suite to confirm no regressions.
    Run: pytest tests/ --tb=short
    If existing tests break due to _run_analysis_pipeline() return type change, fix the callers in market_analysis_service.py. Do NOT change test expectations — fix the implementation.

    STEP 7 — Commit: git commit with message "feat(01-01): define IntelligenceEvent schema and update publisher"
  </action>
  <verify>
    ```bash
    cd /home/bg/dev/indicagent
    source .venv/bin/activate
    pytest tests/ --tb=short -q
    python -c "from src.intelligence.schemas import IntelligenceEvent, I3Structure, I4Context; print('import OK')"
    python -c "from pydantic import ValidationError; from src.intelligence.schemas import I3Structure; I3Structure(bad_field=1.0)" 2>&1 | grep ValidationError
    grep -n "model_dump_json" /home/bg/dev/indicagent/services/market_analysis_service.py
    ```
  </verify>
  <done>
    src/intelligence/schemas.py exists with IntelligenceEvent and all 7 sub-models (OHLCVBar named per locked decision). I3Structure/I4Context/I5Patterns/SMCContext/I6Confluence have extra="forbid"; I1Indicators has extra="allow". market_analysis_service._publish_intelligence() uses event.model_dump_json() and emits {"event": "..."} to Redis. pytest tests/ -q passes with 551+ tests (zero regressions). New TestIntelligenceEventSchema tests all pass (RED→GREEN confirmed).
  </done>
</task>

</tasks>

<verification>
Run after task complete:

```bash
cd /home/bg/dev/indicagent
source .venv/bin/activate
pytest tests/ --tb=short -q
```

All 551+ tests must pass. Also verify:
```bash
python -c "from src.intelligence.schemas import IntelligenceEvent, I3Structure, I4Context; print('import OK')"
python -c "from pydantic import ValidationError; from src.intelligence.schemas import I3Structure; I3Structure(bad_field=1.0)" 2>&1 | grep ValidationError
grep -n "model_dump_json" /home/bg/dev/indicagent/services/market_analysis_service.py
```
</verification>

<success_criteria>
- src/intelligence/schemas.py exists with IntelligenceEvent and all 7 sub-models; OHLCVBar is the class name (per locked CONTEXT.md decision)
- I3Structure/I4Context/I5Patterns/SMCContext/I6Confluence have extra="forbid"; I1Indicators has extra="allow"
- SMCContext uses smc_trend_direction (not trend_direction) to avoid I3 collision
- market_analysis_service._publish_intelligence() uses event.model_dump_json() and emits {"event": "..."} to Redis
- market_analysis_service._run_analysis_pipeline() returns tiered dict with i3/i4/i5/smc/i6 keys
- pytest tests/ -q passes with 551+ tests (zero regressions)
- New TestIntelligenceEventSchema tests all pass (RED→GREEN confirmed)
</success_criteria>

<output>
After completion, create `.planning/phases/01-typed-event-schema/01-01-SUMMARY.md` with:
- What was built (schema structure, field counts per sub-model)
- Key decisions made (field names, type annotations, any deviations from RESEARCH.md)
- Any new findings about plugin output types that differ from the RESEARCH.md spec
- Test count added
</output>
