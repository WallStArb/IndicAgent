---
phase: 090-signal-ledger-thread-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/persistence/repository/signal_ledger_repository.py
  - tests/unit/test_signal_ledger_repository.py
  - tests/unit/test_pipeline_attribution.py
autonomous: true
requirements: [LEDGER-01, LEDGER-02]

must_haves:
  truths:
    - "LedgerEntry exposes a private _to_row() method returning a 67-element tuple"
    - "All in-tree references to to_insert_params() have been removed (grep returns zero hits)"
    - "Every position in _to_row() is annotated with a `# $N field_name` inline comment"
    - "A self-maintaining tuple-count guard test asserts len(_to_row()) == count of $N tokens in _INSERT_SQL"
    - "Lifecycle UPDATE call sites (>=5 positional params) carry # $N field_name inline comments per arg"
  artifacts:
    - path: "src/persistence/repository/signal_ledger_repository.py"
      provides: "LedgerEntry._to_row() helper with full named-field annotations and updated call sites"
      contains: "def _to_row(self)"
    - path: "tests/unit/test_signal_ledger_repository.py"
      provides: "Dynamic tuple-count guard test using re.findall on _INSERT_SQL"
      contains: "def test_to_row_returns_correct_count"
    - path: "tests/unit/test_pipeline_attribution.py"
      provides: "Updated call site using _to_row() with dynamic param-count assertion"
      contains: "entry._to_row()"
  key_links:
    - from: "src/persistence/repository/signal_ledger_repository.py insert_signals (line 555)"
      to: "LedgerEntry._to_row()"
      via: "list comprehension passing tuple to executemany"
      pattern: "entry\\._to_row\\(\\)"
    - from: "src/persistence/repository/signal_ledger_repository.py insert_signals_with_features (line 568)"
      to: "LedgerEntry._to_row()"
      via: "*-unpack into conn.execute"
      pattern: "\\*entry\\._to_row\\(\\)"
    - from: "tests/unit/test_signal_ledger_repository.py test_to_row_returns_correct_count"
      to: "_INSERT_SQL"
      via: "re.findall(r'\\$\\d+', _INSERT_SQL)"
      pattern: "re\\.findall.*_INSERT_SQL"
---

<objective>
Rename `LedgerEntry.to_insert_params()` to `_to_row()` (module-private), annotate every one of the 67 returned fields with a `# $N field_name` inline comment matching the fleet reference style, update all three call sites in the same commit, add field comments to the five lifecycle UPDATE call sites (>=5 positional params), and add a self-maintaining tuple-count guard test.

Purpose: Close the fleet gap left by PERSIST-05 in Phase 085. `signal_ledger_repository.py` is the highest-volume writer and the only one still using an unannotated positional tuple. Future v2.7 lanes will keep adding columns; the named-field helper plus the dynamic guard test makes column drift detectable at test time.

Output: Refactored `signal_ledger_repository.py`, new guard test, updated existing test call site. Zero behavior change, zero schema change.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/090-signal-ledger-thread-safety/090-CONTEXT.md
@.planning/phases/090-signal-ledger-thread-safety/090-RESEARCH.md
@.planning/phases/090-signal-ledger-thread-safety/090-PATTERNS.md
@services/feature_writer_agent.py
@src/persistence/repository/signal_ledger_repository.py
@tests/unit/service_tests/test_feature_writer_agent.py
@tests/unit/test_signal_ledger_repository.py
@tests/unit/test_pipeline_attribution.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename to_insert_params to _to_row with full per-position field comments and update repository call sites</name>
  <files>src/persistence/repository/signal_ledger_repository.py</files>
  <read_first>
    - src/persistence/repository/signal_ledger_repository.py (current to_insert_params at lines 154-233; _INSERT_SQL at line 240; call sites at lines 555, 568; lifecycle SQL constants at lines 409, 482, 492, 508, 524; lifecycle call sites at lines 581-668)
    - services/feature_writer_agent.py (lines 158-203 — fleet reference for the `# $N field_name` comment style on a tuple return; copy comment style verbatim)
    - .planning/phases/090-signal-ledger-thread-safety/090-PATTERNS.md (Plan 01 sections)
  </read_first>
  <action>
    In src/persistence/repository/signal_ledger_repository.py:

    1. Rename the method `LedgerEntry.to_insert_params(self) -> tuple` to `_to_row(self) -> tuple`. Keep the implementation byte-equivalent in evaluation order; only the method name and the per-position annotation change.

    2. Replace the docstring with one matching the fleet reference style. Required text: "Return a 67-element tuple of INSERT parameters for _INSERT_SQL." plus a second paragraph noting "JSONB columns (targets, supporting_factors, market_context, bucket_scores, trailing_stop_price, features_snapshot) are passed as Python dicts/lists; asyncpg serializes them to jsonb natively via codec." Use 67 — not 65. The CONTEXT.md count of 65 is stale; code shows $1..$67 in _INSERT_SQL.

       VERIFY FIELD COUNT DYNAMICALLY BEFORE EDITING: run `.venv/bin/python -c "import re; from src.persistence.repository.signal_ledger_repository import _INSERT_SQL; print(len(re.findall(r'\$\d+', _INSERT_SQL)))"` first to obtain the ground-truth count from `_INSERT_SQL`. If that count is NOT 67 (CONTEXT.md says 65; this plan says 67; the SQL is the only authority), update the docstring number, the tuple, and all per-position `# $N` comments to match the actual count. Never hardcode a count that disagrees with `len(re.findall(r"\$\d+", _INSERT_SQL))`.

    3. Annotate every one of the 67 returned tuple elements with an inline comment `# $N field_name` on the same line as the field expression, matching the exact style at services/feature_writer_agent.py lines 171-203. No blank lines between elements. Group-context comments (e.g. `# Phase 32: stop basis fields`) are allowed on their own line preceding a group, only when they clarify a logical cluster. The $N numbering MUST agree with the $N tokens in _INSERT_SQL at line 240. For JSONB/array typed positions, mirror feature_writer_agent's style (e.g. `# $47::jsonb market_context`, `# $64::text[] co_fire_partners`) where the existing partial annotations already used that notation.

    4. Update the two repository call sites:
       - Line 555 in insert_signals(): change `params = [entry.to_insert_params() for entry in entries]` to `params = [entry._to_row() for entry in entries]`.
       - Line 568 in insert_signals_with_features(): change `await conn.execute(_INSERT_SQL, *entry.to_insert_params())` to `await conn.execute(_INSERT_SQL, *entry._to_row())`.

    5. Add `# $N field_name` inline comments on every positional argument passed to `execute_command()` for the five lifecycle UPDATE call sites (D-03 scope). Use the exact comments listed in 090-PATTERNS.md "Lifecycle SQL call sites" section:
       - update_signal_status() at lines 581-600 — 17 args matching _UPDATE_STATUS_SQL ($1..$17)
       - record_activation() at lines 611-618 — 5 args matching _RECORD_ACTIVATION_SQL ($1..$5)
       - record_zone_resolution() at lines 622-636 — 12 args matching _RECORD_ZONE_RESOLUTION_SQL ($1..$12)
       - record_market_resolution() at lines 640-652 — 10 args matching _RECORD_MARKET_RESOLUTION_SQL ($1..$10)
       - record_zone_resolution_with_activation() at lines 656-668 — 16 args matching _RECORD_ZONE_WITH_ACTIVATION_SQL ($1..$16)

    6. Do NOT change any SQL constants, schema, validation, or runtime behavior. Pure rename + comment addition.

    7. Run `grep -rn "to_insert_params" src/ services/ tests/` after editing; the only remaining hits permitted are in tests/ which Task 2 will update. There must be ZERO hits in src/ or services/ after this task.
  </action>
  <verify>
    .venv/bin/ruff check src/persistence/repository/signal_ledger_repository.py
    .venv/bin/black --check src/persistence/repository/signal_ledger_repository.py
    grep -c "to_insert_params" src/persistence/repository/signal_ledger_repository.py
    grep -c "def _to_row" src/persistence/repository/signal_ledger_repository.py
    .venv/bin/python -c "import re; from src.persistence.repository.signal_ledger_repository import _INSERT_SQL; print(len(re.findall(r'\$\d+', _INSERT_SQL)))"
  </verify>
  <acceptance_criteria>
    - `grep -c "def _to_row(self) -> tuple:" src/persistence/repository/signal_ledger_repository.py` returns 1
    - `grep -c "def to_insert_params" src/persistence/repository/signal_ledger_repository.py` returns 0
    - `grep -c "to_insert_params" src/persistence/repository/signal_ledger_repository.py` returns 0
    - `grep -c "entry._to_row()" src/persistence/repository/signal_ledger_repository.py` returns at least 2 (insert_signals + insert_signals_with_features call sites)
    - `python -c "import re; from src.persistence.repository.signal_ledger_repository import _INSERT_SQL; print(len(re.findall(r'\$\\d+', _INSERT_SQL)))"` outputs `67`
    - Source assertion: the docstring of `_to_row` contains the literal substring `"67-element tuple"`
    - Source assertion: every `self.<field>,` line inside the `_to_row` return tuple has a trailing `# $N` inline comment (verified by `grep -P "^\s+self\.\w+,\s*#\s*\$\d+" src/persistence/repository/signal_ledger_repository.py | wc -l` returning a count >= 60)
    - Lifecycle comment count assertion: `grep -cP "kwargs\.get\(\"\w+\"\),\s+#\s*\$\d+" src/persistence/repository/signal_ledger_repository.py` returns at least 55 (sum of 16+5+12+10+16 expected kwargs-style comments minus signal_id direct args)
    - `.venv/bin/ruff check src/persistence/repository/signal_ledger_repository.py` exits 0
    - `.venv/bin/black --check src/persistence/repository/signal_ledger_repository.py` exits 0
    - `grep -rn "to_insert_params" src/ services/` returns zero results
  </acceptance_criteria>
  <done>LedgerEntry._to_row() exists, returns the 67-element tuple with every position annotated `# $N field_name`, all in-src call sites updated, no remaining `to_insert_params` symbols in src/ or services/.</done>
</task>

<task type="auto">
  <name>Task 2: Add dynamic tuple-count guard test and update test_pipeline_attribution.py call site</name>
  <files>
    tests/unit/test_signal_ledger_repository.py
    tests/unit/test_pipeline_attribution.py
  </files>
  <read_first>
    - tests/unit/test_signal_ledger_repository.py (current imports and structure; check existing LedgerEntry fixture or build minimal one)
    - tests/unit/test_pipeline_attribution.py (lines 94-97 — current `entry.to_insert_params()` call and hardcoded `assert len(params) == 67`)
    - tests/unit/service_tests/test_feature_writer_agent.py (lines 133-141 — fleet reference test `test_record_to_insert_params_returns_31_tuple`)
    - src/persistence/repository/signal_ledger_repository.py (LedgerEntry dataclass field list; _INSERT_SQL constant; _to_row method as produced by Task 1)
    - .planning/phases/090-signal-ledger-thread-safety/090-PATTERNS.md (Plan 01 test sections)
  </read_first>
  <action>
    Part A — Add test_to_row_returns_correct_count to tests/unit/test_signal_ledger_repository.py:

    1. Add `import re` at the top of the file with the other stdlib imports if not already present.
    2. Extend the existing repository import line to also import `LedgerEntry` and `_INSERT_SQL` from src.persistence.repository.signal_ledger_repository.
    3. Add a new top-level test function `def test_to_row_returns_correct_count():` with the docstring `"_to_row() tuple length matches $N param count in _INSERT_SQL — self-maintaining."`.
    4. Inside, instantiate a minimal LedgerEntry using its dataclass fields with placeholder-but-type-correct values. Required fields to populate (from LedgerEntry definition in signal_ledger_repository.py): signal_id (str UUID literal "00000000-0000-0000-0000-000000000001"), timestamp (datetime.now(UTC)), symbol "ES", timeframe "1m", setup_plugin "test_plugin", signal_type "long", direction "long", entry_price 4500.0, stop_loss 4490.0, targets [], confidence 0.75, confluence_score 0.7, regime_context {}, supporting_factors [], was_selected True, num_signals_bar 1, num_agreeing 1, num_conflicting 0, resolution_method "in_process", composite_rank 1. For any additional required fields without defaults in the dataclass, use the type-appropriate zero/None/empty value. Use `datetime` and `UTC` from the standard library.
    5. Compute `sql_param_count = len(re.findall(r"\$\d+", _INSERT_SQL))`.
    6. Assert: `isinstance(entry._to_row(), tuple)` AND `len(entry._to_row()) == sql_param_count`. Use two separate assert statements so the failure message identifies which invariant failed. Do NOT use a hardcoded count like `== 67`.

    Part B — Update tests/unit/test_pipeline_attribution.py:

    1. Add `import re` near the existing imports if not already present.
    2. Add `from src.persistence.repository.signal_ledger_repository import _INSERT_SQL` to the existing repository import block (or as a new import line).
    3. At line ~94, change `params = entry.to_insert_params()` to `params = entry._to_row()`.
    4. Change `assert len(params) == 67, f"Expected 67 params, got {len(params)}"` to:
       `sql_param_count = len(re.findall(r"\$\d+", _INSERT_SQL))`
       `assert len(params) == sql_param_count, f"Expected {sql_param_count} params, got {len(params)}"`.
    5. Leave the positional index assertions on `params[58]` and `params[59]` unchanged — they verify field-to-position mapping, independent of count.

    Run both test files; both must pass.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/test_signal_ledger_repository.py -v
    .venv/bin/pytest tests/unit/test_pipeline_attribution.py -v
    .venv/bin/ruff check tests/unit/test_signal_ledger_repository.py tests/unit/test_pipeline_attribution.py
    grep -rn "to_insert_params" tests/
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_to_row_returns_correct_count" tests/unit/test_signal_ledger_repository.py` returns 1
    - `grep -c "re.findall" tests/unit/test_signal_ledger_repository.py` returns at least 1
    - `grep -c "len(params) == 67" tests/unit/test_pipeline_attribution.py` returns 0
    - `grep -c "entry._to_row()" tests/unit/test_pipeline_attribution.py` returns at least 1
    - `grep -c "entry.to_insert_params" tests/unit/test_pipeline_attribution.py` returns 0
    - `grep -c "_INSERT_SQL" tests/unit/test_pipeline_attribution.py` returns at least 1
    - `.venv/bin/pytest tests/unit/test_signal_ledger_repository.py::test_to_row_returns_correct_count -v` exits 0
    - `.venv/bin/pytest tests/unit/test_signal_ledger_repository.py -v` exits 0 (all tests pass)
    - `.venv/bin/pytest tests/unit/test_pipeline_attribution.py -v` exits 0
    - `.venv/bin/ruff check tests/unit/test_signal_ledger_repository.py tests/unit/test_pipeline_attribution.py` exits 0
    - `grep -rn "to_insert_params" tests/` returns zero results (post-Task-1+Task-2 the symbol is fully eliminated)
  </acceptance_criteria>
  <done>Self-maintaining guard test exists in test_signal_ledger_repository.py and passes; test_pipeline_attribution.py uses `_to_row()` with dynamic count; no references to `to_insert_params` remain anywhere under tests/.</done>
</task>

</tasks>

<verification>
Final phase-level checks:

1. `grep -rn "to_insert_params" src/ services/ tests/` — must return zero results.
2. `.venv/bin/pytest tests/unit/test_signal_ledger_repository.py tests/unit/test_pipeline_attribution.py -v` — all green.
3. `.venv/bin/python -c "import re; from src.persistence.repository.signal_ledger_repository import _INSERT_SQL, LedgerEntry; import inspect; src = inspect.getsource(LedgerEntry._to_row); print(len(re.findall(r'#\s*\\$\\d+', src)))"` — output should equal `len(re.findall(r'\\$\\d+', _INSERT_SQL))` (i.e. 67), proving every $N has a Python-side comment.
4. `.venv/bin/ruff check src/persistence/repository/signal_ledger_repository.py tests/unit/test_signal_ledger_repository.py tests/unit/test_pipeline_attribution.py` — no errors.
5. `.venv/bin/black --check src/persistence/repository/signal_ledger_repository.py tests/unit/test_signal_ledger_repository.py tests/unit/test_pipeline_attribution.py` — no diff.
6. `grep -rn "to_insert_params" src/ services/` — must return zero results (static-analysis enforcement: no leftover legacy symbol references in production code per cross-AI review suggestion).
</verification>

<success_criteria>
- LEDGER-01 satisfied: `_to_row()` exists with full per-position field annotations matching fleet style.
- LEDGER-02 satisfied: all five lifecycle UPDATE call sites (>=5 positional params) carry `# $N field_name` inline comments.
- Dynamic tuple-count guard test passes and uses `re.findall(r"\$\d+", _INSERT_SQL)` — no hardcoded 67.
- `tests/unit/test_pipeline_attribution.py` updated to use `_to_row()` and the dynamic count pattern.
- Zero remaining references to `to_insert_params` anywhere in `src/`, `services/`, or `tests/`.
- All targeted unit tests pass; ruff and black clean on every modified file.
</success_criteria>

<output>
After completion, create `.planning/phases/090-signal-ledger-thread-safety/090-01-SUMMARY.md` describing:
- Final tuple count produced by `_to_row()` (must be 67) and SQL `$N` count (must match).
- The exact wording of the new docstring.
- Confirmation that `grep -rn "to_insert_params"` returns zero hits.
- Test names added and their pass status.
- Lifecycle call sites updated (5 sites enumerated).
</output>
