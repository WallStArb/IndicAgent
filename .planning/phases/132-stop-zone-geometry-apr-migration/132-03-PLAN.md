---
phase: 132-stop-zone-geometry-apr-migration
plan: 03
type: execute
wave: 2
depends_on: ["132-02"]
files_modified:
  - production/migrations/145_phase132_adaptive_buffer_apr.sql
  - src/intelligence/trading/trade_framer.py
  - services/intelligence_pipeline.py
autonomous: true
gap_closure: false

must_haves:
  truths:
    - "All 12 adaptive buffer piecewise coefficients are read via _cfg() with the current value as the fallback"
    - "Migration 145 seeds the 12 adaptive buffer APR keys with a coupling warning in the descriptions"
    - "_adaptive_buffer() output is byte-identical at seed values to the pre-migration piecewise function (proven by unit test at the three anchor points)"
  artifacts:
    - path: "production/migrations/145_phase132_adaptive_buffer_apr.sql"
      provides: "config_schema + config_state + config_history for 12 adaptive buffer coefficient keys"
      contains: "feature.trade_framer.adaptive_buffer_low_vol_base"
    - path: "src/intelligence/trading/trade_framer.py"
      provides: "_adaptive_buffer() body reading coefficients via _cfg()"
  key_links:
    - from: "_adaptive_buffer() body"
      to: "config_state"
      via: "_cfg(key, fallback) for each piecewise coefficient"
      pattern: "_cfg\\(\"feature.trade_framer.adaptive_buffer"
    - from: "services/intelligence_pipeline.py _THRESHOLD_KEYS"
      to: "ConfigService prewarm cache"
      via: "12 adaptive buffer key registrations"
      pattern: "adaptive_buffer"
---

<objective>
Migrate the 12 distinct configurable coefficients inside `_adaptive_buffer()` (trade_framer.py lines 136-159) to APR (migration 145) and wire the function body to read them via `_cfg()`. RESEARCH §3.2 decomposes the piecewise GARCH vol-response curve into 12 keys: vol_ratio clamp min/max, low-vol base + slope num/den, high-vol slope num/den, Hurst trend/MR thresholds, Hurst tighten rate, GARCH shock threshold, GARCH shock multiplier. Also wire the internal `ADAPTIVE_BUFFER_HARD_CAP` read (key seeded by Plan 02 migration 144).

Depends on Plan 02: the module-level constants are already migrated and run_historical_pipeline.py is already wired. This plan only touches the adaptive buffer function body and adds 12 keys.

Purpose: APR mandate completeness — the adaptive buffer coefficients are the highest-value ML learning target (the GARCH vol-response curve). Renaissance silent-failure guard (RESEARCH §7.3): a single mistyped coefficient silently corrupts stop geometry for all high-vol bars, so the function MUST have a regression test at the three anchor points.
Output: migration 145, APR-backed _adaptive_buffer(), prewarm registrations, anchor-point regression test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-stop-zone-geometry-apr-migration/132-CONTEXT.md
@.planning/phases/132-stop-zone-geometry-apr-migration/132-RESEARCH.md
@docs/foundation/adaptive-parameter-registry.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 145 (12 adaptive buffer keys) + register in _THRESHOLD_KEYS</name>
  <read_first>
    - production/migrations/142_phase130_apr_seeds.sql (canonical 3-block migration format)
    - .planning/phases/132-stop-zone-geometry-apr-migration/132-RESEARCH.md §3.2 (the 12-key decomposition table with exact key names and values) and §9.2 (anchor-point expected outputs)
    - src/intelligence/trading/trade_framer.py lines 136-159 (the _adaptive_buffer function body — confirm every literal against the table)
    - services/intelligence_pipeline.py lines 376-405 (_THRESHOLD_KEYS format)
  </read_first>
  <files>production/migrations/145_phase132_adaptive_buffer_apr.sql, services/intelligence_pipeline.py</files>
  <action>
    Create `production/migrations/145_phase132_adaptive_buffer_apr.sql` following the migration 142 3-block format. Seed these 12 keys at the exact literals in _adaptive_buffer() (RESEARCH §3.2):

    | APR key | type | seed | min | max |
    |---------|------|------|-----|-----|
    | feature.trade_framer.adaptive_buffer_vol_ratio_min | float | 0.70 | 0.1 | 1.0 |
    | feature.trade_framer.adaptive_buffer_vol_ratio_max | float | 1.50 | 1.0 | 3.0 |
    | feature.trade_framer.adaptive_buffer_low_vol_base | float | 0.80 | 0.3 | 1.0 |
    | feature.trade_framer.adaptive_buffer_low_vol_slope_num | float | 0.20 | 0.0 | 1.0 |
    | feature.trade_framer.adaptive_buffer_low_vol_slope_den | float | 0.30 | 0.05 | 1.0 |
    | feature.trade_framer.adaptive_buffer_high_vol_slope_num | float | 0.35 | 0.0 | 1.0 |
    | feature.trade_framer.adaptive_buffer_high_vol_slope_den | float | 0.50 | 0.05 | 1.0 |
    | feature.trade_framer.adaptive_buffer_hurst_trend_threshold | float | 0.55 | 0.5 | 0.9 |
    | feature.trade_framer.adaptive_buffer_hurst_mr_threshold | float | 0.45 | 0.1 | 0.5 |
    | feature.trade_framer.adaptive_buffer_hurst_tighten_rate | float | 0.16 | 0.0 | 1.0 |
    | feature.trade_framer.adaptive_buffer_garch_shock_threshold | float | 3.0 | 1.0 | 10.0 |
    | feature.trade_framer.adaptive_buffer_garch_shock_mult | float | 1.35 | 1.0 | 3.0 |

    Note: `feature.trade_framer.adaptive_buffer_hard_cap` is seeded separately in Plan 02 migration 144, NOT here. This migration seeds exactly these 12 coefficient keys.

    CRITICAL (CONTEXT D-03 + RESEARCH §3.2 coupling note): every config_schema description MUST include `[initial_estimate]`, the phrase "ML learning target", AND a coupling warning: "WARNING: these 12 coefficients define a single piecewise GARCH vol-response curve. Tuning one in isolation can break the function shape — tune as a group or preserve the piecewise structure." Add a denominator guard note for the slope_den keys (min > 0 prevents division by zero). config_history `changed_by`='migration_145', `reason`='Phase 132 A5 APR seed — adaptive buffer piecewise coefficient'.

    Add all 12 keys to `_THRESHOLD_KEYS` in services/intelligence_pipeline.py with a comment `# --- migration 145: Phase 132 adaptive buffer coefficients (coupled piecewise — tune as a group) ---`. Each default equals the seed.

    Apply: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/145_phase132_adaptive_buffer_apr.sql`.
  </action>
  <verify>
    `SELECT COUNT(*) FROM config_state WHERE config_key LIKE 'feature.trade_framer.adaptive_buffer\_%' ESCAPE '\' AND config_key != 'feature.trade_framer.adaptive_buffer_hard_cap';` returns exactly 12 (the 12 new migration-145 coefficient keys). Note: adaptive_buffer_hard_cap (seeded by migration 144) DOES match the LIKE pattern because '%' matches 'hard_cap', so it is excluded explicitly; the unfiltered count of the pattern is 13.
  </verify>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM config_state WHERE config_key LIKE 'feature.trade_framer.adaptive_buffer\_%' ESCAPE '\' AND config_key != 'feature.trade_framer.adaptive_buffer_hard_cap'` returns exactly 12
    - `SELECT COUNT(*) FROM config_state WHERE config_key LIKE 'feature.trade_framer.adaptive_buffer\_%' ESCAPE '\'` returns 13 (12 new coefficient keys + adaptive_buffer_hard_cap from migration 144)
    - `SELECT config_value FROM config_state WHERE config_key='feature.trade_framer.adaptive_buffer_low_vol_base'` returns '0.80' (or '0.8')
    - Every config_schema description for the 12 keys contains the literal substring "tune as a group"
    - `grep -c "adaptive_buffer_" services/intelligence_pipeline.py` returns at least 12
    - migration 145 applied with no psql error
  </acceptance_criteria>
  <done>12 adaptive buffer coefficient keys exist in config_state at exact seed values with coupling warnings in descriptions and are registered in _THRESHOLD_KEYS; migration 145 applied cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire _adaptive_buffer() body + hard cap to read coefficients via _cfg()</name>
  <read_first>
    - src/intelligence/trading/trade_framer.py lines 73-74 (_cfg) and 136-159 (the _adaptive_buffer body to rewire) plus line 133 region (adaptive_buffer_hard_cap key seeded by Plan 02)
    - .planning/phases/132-stop-zone-geometry-apr-migration/132-RESEARCH.md §3.2 (function decomposition) and §7.3 (silent failure: coefficient typo)
  </read_first>
  <files>src/intelligence/trading/trade_framer.py</files>
  <action>
    Rewrite the body of `_adaptive_buffer()` so every literal coefficient is read via `_cfg()` with the current value as fallback. Read each coefficient ONCE into a local variable at the top of the function (avoid repeated _cfg() calls in branches), then use the locals in the piecewise math. Exact mapping:

    - vol_ratio clamp: `max(_cfg("feature.trade_framer.adaptive_buffer_vol_ratio_min", 0.70), min(_cfg("feature.trade_framer.adaptive_buffer_vol_ratio_max", 1.50), vol_ratio))`
    - low-vol branch: `_cfg(low_vol_base, 0.80) + (vol_ratio - vol_ratio_min) * (_cfg(low_vol_slope_num, 0.20) / _cfg(low_vol_slope_den, 0.30))`
    - high-vol branch: `1.00 + (vol_ratio - 1.00) * (_cfg(high_vol_slope_num, 0.35) / _cfg(high_vol_slope_den, 0.50))`
    - Hurst: trend threshold 0.55, MR threshold 0.45, tighten rate 0.16 via their keys
    - GARCH shock: threshold 3.0, multiplier 1.35 via their keys
    - hard cap: `min(result, base_mult * _cfg("feature.trade_framer.adaptive_buffer_hard_cap", 1.40))` — this replaces the bare `ADAPTIVE_BUFFER_HARD_CAP` reference at line 159. Remove the `ADAPTIVE_BUFFER_HARD_CAP = 1.40` module constant definition (line 133) if no other site uses it; otherwise replace remaining sites too.

    Preserve the EXACT piecewise structure and operator precedence — the only change is literal → `_cfg(key, literal)`. Do not reorder branches, do not change the clamp logic, do not change the `1.00` constants in the high-vol branch (those are mathematical anchors of the curve, not tunable — leave as `1.00`; only the slope and base are tunable per the table).

    Run `.venv/bin/ruff check src/intelligence/trading/trade_framer.py --fix` and `.venv/bin/black src/intelligence/trading/trade_framer.py`.
  </action>
  <verify>
    `grep -c "ADAPTIVE_BUFFER_HARD_CAP" src/intelligence/trading/trade_framer.py` returns 0 (or only a comment); `.venv/bin/python -c "import src.intelligence.trading.trade_framer"` exits 0; the 6 _cfg adaptive_buffer reads are present in the function body.
  </verify>
  <acceptance_criteria>
    - `grep -nE "^ADAPTIVE_BUFFER_HARD_CAP *=" src/intelligence/trading/trade_framer.py` returns zero results
    - `grep -c "_cfg(\"feature.trade_framer.adaptive_buffer" src/intelligence/trading/trade_framer.py` returns at least 12
    - The `1.00` anchor constants in the high-vol branch remain as literals (not migrated)
    - `.venv/bin/ruff check src/intelligence/trading/trade_framer.py` exits 0
    - `.venv/bin/python -c "import src.intelligence.trading.trade_framer"` exits 0
  </acceptance_criteria>
  <done>_adaptive_buffer() reads all 12 coefficients + hard cap via _cfg(); no bare ADAPTIVE_BUFFER_HARD_CAP constant remains; piecewise structure and 1.00 anchors preserved; module imports clean.</done>
</task>

<task type="auto">
  <name>Task 3: Anchor-point regression test for _adaptive_buffer()</name>
  <read_first>
    - tests/unit/intelligence/ (existing trade_framer test style; the regression test from Plan 02 Task 4 if present)
    - .planning/phases/132-stop-zone-geometry-apr-migration/132-RESEARCH.md §9.2 (the three anchor points and expected outputs)
    - src/intelligence/trading/trade_framer.py _adaptive_buffer signature + set_config_service
  </read_first>
  <files>tests/unit/intelligence/ (adaptive_buffer regression test)</files>
  <action>
    Add unit tests proving _adaptive_buffer() at seed values produces the documented anchor and interior outputs (RESEARCH §9.2), with base_mult=1.0:

    Anchor points (endpoints and boundary):
    - `vol_ratio=0.70` → low-vol branch at clamp min → result 0.80
    - `vol_ratio=1.00` → branch boundary → result 1.00
    - `vol_ratio=1.50` → high-vol branch: 1.00 + 0.50*(0.35/0.50) = 1.35, then min(1.35, hard_cap 1.40) = 1.35

    Interior points (catch slope inversions and branch miswiring that endpoint tests miss):
    - Low-vol interior: `vol_ratio=0.85` → 0.80 + (0.85 - 0.70) * (0.20/0.30) = 0.90 (within EPSILON_TOLERANCE)
    - High-vol interior: `vol_ratio=1.25` → 1.00 + (1.25 - 1.00) * (0.35/0.50) = 1.175 (within EPSILON_TOLERANCE)

    Also test the GARCH shock floor: `garch_shock > 3.0` forces `result >= base_mult * 1.35`. And the Hurst tightening: regime_type="trend" with hurst=0.65 applies `result *= 1.0 - (0.65-0.55)*0.16`.

    Use a STRICT mock ConfigService: `get_sync(key, default)` returns the seed value for known keys and RAISES `ValueError(f"Unknown APR key in test: {key!r}")` for any unknown key. A permissive mock silently returns `default` for typos, defeating the regression intent.

    Run each assertion BOTH with `set_config_service(None)` (hardcoded fallback path) AND with the strict mock returning seed values, asserting both produce identical output within EPSILON_TOLERANCE. Reset `set_config_service(None)` in teardown.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/ -k adaptive_buffer -q` passes; full unit suite green.
  </verify>
  <acceptance_criteria>
    - A test asserts vol_ratio=0.70 → 0.80, vol_ratio=0.85 → 0.90, vol_ratio=1.00 → 1.00 (low-vol anchor + interior + boundary)
    - A test asserts vol_ratio=1.25 → 1.175, vol_ratio=1.50 → 1.35 (high-vol interior + anchor)
    - A test asserts the garch_shock>3.0 floor and the Hurst trend tightening behavior
    - Each assertion is verified under both None-config and strict-mock-seed-config paths
    - The strict mock raises ValueError for any key not in the seed dict (not silently returning default)
    - `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
  <done>Anchor-point test confirms _adaptive_buffer output is byte-identical at seed values (0.70->0.80, 1.00->1.00, 1.50->1.35) under both None-config and seed-config paths; full unit suite green.</done>
</task>

</tasks>

<verification>
- 12 adaptive buffer coefficient keys exist in config_state at exact seed values
- _adaptive_buffer() reads all coefficients via _cfg(); no bare ADAPTIVE_BUFFER_HARD_CAP constant remains
- anchor-point regression test proves identical output at seed values
- full unit suite green
</verification>

<success_criteria>
- `SELECT COUNT(*) FROM config_state WHERE config_key LIKE 'feature.trade_framer.adaptive_buffer\_%' ESCAPE '\' AND config_key != 'feature.trade_framer.adaptive_buffer_hard_cap'` returns 12
- _adaptive_buffer output is byte-identical at seed values to pre-migration (anchor-point test)
- coupling warning present in all 12 key descriptions
</success_criteria>

<output>
After completion, create `.planning/phases/132-stop-zone-geometry-apr-migration/132-03-SUMMARY.md`
</output>
</content>
</invoke>
