---
phase: 132-stop-zone-geometry-apr-migration
plan: 05
type: execute
wave: 4
depends_on: ["132-01", "132-02", "132-03", "132-04"]
files_modified:
  - .planning/phases/132-stop-zone-geometry-apr-migration/132-VERIFICATION.md
autonomous: true
gap_closure: false

must_haves:
  truths:
    - "A 1-month sample replay + lifecycle_replay shows stopped_at_entry < 5% of all stop exits in trade_executions"
    - "All 35 trade_framer APR keys are present in config_state and visible via the /config/parameters dashboard surface"
    - "No migratable bare numeric literal remains at module level in trade_framer.py (only the intentionally-retained constants)"
    - "The full unit suite is green and the phase is committed and pushed"
  artifacts:
    - path: ".planning/phases/132-stop-zone-geometry-apr-migration/132-VERIFICATION.md"
      provides: "Final gate results: stopped_at_entry rate, APR key count, bare-literal audit, suite status"
      min_lines: 40
  key_links:
    - from: "1-month run_historical_pipeline.py + lifecycle_replay.py"
      to: "trade_executions.exit_reason"
      via: "lifecycle replay writes stopped_at_entry"
      pattern: "exit_reason"
    - from: "config_state feature.trade_framer.* keys"
      to: "/config/parameters dashboard"
      via: "ConfigService-backed parameters route"
      pattern: "feature.trade_framer"
---

<objective>
Final phase verification: run a 1-month sample replay + lifecycle_replay, confirm `stopped_at_entry < 5%` of stop exits, confirm all 35 trade_framer APR keys are present and dashboard-visible, confirm no migratable bare literals remain in trade_framer.py, run the full unit suite, then commit and push per the Done-Coding SOP.

35 keys total = 19 (migration 144, Plan 02) + 12 (migration 145, Plan 03) + 4 (migration 146, Plan 04). Note one of the 19, `threshold.trade_framer.min_rr_t1`, is under the `threshold.` namespace, so the `feature.trade_framer.%` count is 34 and the combined count is 35.

Purpose: the phase verification gate (CONTEXT domain section). Renaissance earn-promotion-through-proof — the phase does not close until the empirical gate (< 5% stopped_at_entry on fresh data) and the architecture gate (zero bare literals, all keys in APR) are both proven.
Output: `132-VERIFICATION.md` with all gate results; committed and pushed phase.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-stop-zone-geometry-apr-migration/132-CONTEXT.md
@.planning/phases/132-stop-zone-geometry-apr-migration/132-RESEARCH.md
@.planning/phases/132-stop-zone-geometry-apr-migration/132-A2-MEASUREMENT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 1-month sample replay + lifecycle_replay + stopped_at_entry gate</name>
  <read_first>
    - production/scripts/run_historical_pipeline.py (--replay-only, --clean, --days, --symbols, --client-id)
    - production/scripts/lifecycle_replay.py (--reset, --reset-after, --confirm, --symbols, --timeframes, --workers)
    - .planning/phases/132-stop-zone-geometry-apr-migration/132-A2-MEASUREMENT.md (Plan 01 baseline + the symbol sample used) and 132-RESEARCH.md §9.3 (the gate query)
  </read_first>
  <files>(no source files — runs replay + gate queries; produces 132-VERIFICATION.md in Task 3)</files>
  <action>
    Use the same multi-asset-class symbol sample as Plan 01 (one each from equity_etf / futures_large_tick / commodity_small_tick / fx). Resolve front-month futures — expect 2 rows; if either is missing, STOP:
    `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT base_symbol, symbol FROM contract_metadata WHERE base_symbol IN ('ES','NG') AND is_front_month=true ORDER BY base_symbol;"`

    Before running the replay, confirm each symbol has bars in the 30-day window; 0 rows means zero signals and a silent gap:
    `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT symbol, COUNT(*) FROM market_data_ohlcv WHERE symbol IN ('QQQ','<ES_front>','<NG_front>','GBPUSD') AND timestamp >= '2026-05-18T00:00:00Z' GROUP BY symbol ORDER BY symbol;"`
    If any symbol returns 0 rows, flag it before proceeding.

    This run uses the APR-wired replay path from Plan 02 Task 3, so it exercises the seeded APR values.

    Step 1 — 1-month fresh replay:
    `.venv/bin/python production/scripts/run_historical_pipeline.py --replay-only --clean --days 30 --symbols QQQ,<ES_front>,<NG_front>,GBPUSD --client-id 40`
    Confirm the "config service wired" line from Plan 02 prints (proves APR is active in replay).

    Step 2 — lifecycle_replay over the 30-day window:
    `.venv/bin/python -u production/scripts/lifecycle_replay.py --reset --reset-after <30d-ago-ISO> --confirm --symbols QQQ,<ES_front>,<NG_front>,GBPUSD --workers 8`
    (30d-ago given today 2026-06-17 = 2026-05-18T00:00:00Z.)

    Step 3 — the gate query. The phase goal is stopped_at_entry < 5% OF STOP EXITS, not of all exits. Measure stopped_at_entry as a fraction of stop-type exits only (stop_loss + stopped_at_entry); using all exits as the denominator would understate the rate and let a high stopped_at_entry rate slip through (with stop_loss ~37% of all exits, a 12.5% stopped_at_entry rate among stop exits would read as <5% of all exits and incorrectly pass). Also capture the full distribution for the verification doc:

    Combined distribution + gate query (single scan; `stopped_pct_of_stop_exits` is identical in every row — read it from the `stop_loss` or `stopped_at_entry` row):
    `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT exit_reason, COUNT(*) AS cnt, ROUND(COUNT(*)*100.0/SUM(COUNT(*)) OVER (),2) AS pct_of_all, ROUND(COUNT(*) FILTER (WHERE exit_reason='stopped_at_entry') * 100.0 / NULLIF(COUNT(*) FILTER (WHERE exit_reason IN ('stop_loss','stopped_at_entry')), 0), 2) AS stopped_pct_of_stop_exits FROM trade_executions te JOIN trade_frames tf ON te.frame_id=tf.frame_id JOIN signal_events se ON tf.signal_id=se.signal_id WHERE se.symbol IN ('QQQ','<ES_front>','<NG_front>','GBPUSD') AND se.ts >= '2026-05-18T00:00:00Z' GROUP BY 1 ORDER BY 2 DESC;"`

    PASS criterion: `stopped_pct_of_stop_exits` is NULL (no stop-type exits) OR < 5.00. If the gate FAILS (>= 5%), record the offending zone_source distribution and STOP — do not commit; flag for a gap-closure follow-up plan (the per-class floor or stop distance floor may need adjustment).
  </action>
  <verify>
    Both scripts exit 0; the full-distribution query returns a distribution; the gate query returns stopped_pct_of_stop_exits < 5.00 (or NULL).
  </verify>
  <acceptance_criteria>
    - run_historical_pipeline.py 30-day replay exits 0 and prints the "config service wired" confirmation
    - lifecycle_replay.py exits 0
    - The gate query (stopped_at_entry as a fraction of stop-type exits, denominator = COUNT(*) FILTER (WHERE exit_reason IN ('stop_loss','stopped_at_entry'))) returns stopped_pct_of_stop_exits < 5.00 OR NULL
    - The full exit_reason distribution (pct_of_all) is also captured for 132-VERIFICATION.md
  </acceptance_criteria>
  <done>1-month sample replay + lifecycle_replay complete; stopped_at_entry < 5.00% of stop-type exits (or no stop exits) with the full exit_reason distribution captured.</done>
</task>

<task type="auto">
  <name>Task 2: APR key count + dashboard visibility + bare-literal audit</name>
  <read_first>
    - src/intelligence/trading/trade_framer.py (full file — for the bare-literal audit)
    - .planning/phases/132-stop-zone-geometry-apr-migration/132-RESEARCH.md §3.1 (the intentionally-NOT-migrated constants: EPSILON_TOLERANCE, ATR_EMERGENCY_FALLBACK_PCT, ATR_TARGET_MAX_MULTIPLIER, ATR_TARGET_MAX_MULTIPLIER_BY_TF, MIN_ZONE_WIDTH_ATR fallback) and §9.4 (key count check)
    - src/api or dashboard route serving /config/parameters (grep for the parameters endpoint to confirm it reads config_state — to document dashboard visibility)
  </read_first>
  <files>(read-only audit — no source files modified)</files>
  <action>
    APR key count:
    `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -t -c "SELECT COUNT(*) FROM config_state WHERE config_key LIKE 'feature.trade_framer.%' OR config_key='threshold.trade_framer.min_rr_t1';"` — expect 35 (34 feature.* + 1 threshold.*).

    Dashboard visibility: confirm the /config/parameters route surfaces these keys. Since the dashboard reads config_state via ConfigService, presence in config_state with valid config_schema rows is sufficient. Confirm each of the 35 keys has a config_schema row: `SELECT COUNT(*) FROM config_schema WHERE config_key LIKE 'feature.trade_framer.%' OR config_key='threshold.trade_framer.min_rr_t1';` — expect 35. Document the route path that renders them (grep the API for the parameters endpoint).

    Bare-literal audit: confirm no migratable numeric constant definitions remain at module level. The ONLY numeric module-level constants that may remain are the intentionally-retained set (RESEARCH §3.1): EPSILON_TOLERANCE, ATR_EMERGENCY_FALLBACK_PCT, ATR_TARGET_MAX_MULTIPLIER, ATR_TARGET_MAX_MULTIPLIER_BY_TF, MIN_ZONE_WIDTH_ATR (kept as APR fallback). Run:
    `grep -nE "^[A-Z_]+ *= *[0-9]" src/intelligence/trading/trade_framer.py`
    and confirm every line in the output is one of the intentionally-retained constants. Any migrated constant (ATR_STOP_*, MIN_STOP_ATR_MULTIPLIER, MIN_RR_T1, ADAPTIVE_BUFFER_HARD_CAP, ATR_ZONE_*, etc.) appearing as an active assignment is a FAIL.
  </action>
  <verify>
    config_state count = 35; config_schema count = 35; the bare-literal grep output contains only intentionally-retained constants.
  </verify>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM config_state WHERE config_key LIKE 'feature.trade_framer.%' OR config_key='threshold.trade_framer.min_rr_t1'` returns 35
    - `SELECT COUNT(*) FROM config_schema WHERE config_key LIKE 'feature.trade_framer.%' OR config_key='threshold.trade_framer.min_rr_t1'` returns 35
    - `grep -nE "^[A-Z_]+ *= *[0-9]" src/intelligence/trading/trade_framer.py` output contains ONLY EPSILON_TOLERANCE, ATR_EMERGENCY_FALLBACK_PCT, ATR_TARGET_MAX_MULTIPLIER, ATR_TARGET_MAX_MULTIPLIER_BY_TF, MIN_ZONE_WIDTH_ATR (no migrated constant assignments)
    - The /config/parameters route path that surfaces these keys is documented in 132-VERIFICATION.md
  </acceptance_criteria>
  <done>config_state and config_schema each return 35 trade_framer keys; bare-literal grep contains only intentionally-retained constants; dashboard route documented.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite + write 132-VERIFICATION.md + commit + push</name>
  <read_first>
    - CLAUDE.md Done-Coding SOP (simplify -> review -> test -> commit on branch -> merge ff-only main -> delete branch -> prune -> push) and "No AI attribution in commits" + "No human checkpoints" memory rules
    - .planning/phases/132-stop-zone-geometry-apr-migration/132-A2-MEASUREMENT.md (Plan 01 results to fold into the verification doc)
  </read_first>
  <files>.planning/phases/132-stop-zone-geometry-apr-migration/132-VERIFICATION.md</files>
  <action>
    Run the full unit suite: `.venv/bin/pytest tests/unit/ -q`. Must be green. Run `.venv/bin/ruff check . --fix` and `.venv/bin/black .` on the changed files if not already clean.

    Write `.planning/phases/132-stop-zone-geometry-apr-migration/132-VERIFICATION.md` with sections: (1) stopped_at_entry gate — the full 1-month exit_reason distribution + the stopped_pct_of_stop_exits gate metric + pass/fail; (2) APR key inventory — the 35 keys grouped by migration (144/145/146) with seed values, plus the config_state and config_schema counts; (3) bare-literal audit — the grep output and confirmation all remaining constants are intentional; (4) regression test results — the seed-value==hardcoded proofs from Plans 02/03/04; (5) DAG invariant confirmation — trade_framer.py does not touch the DB (grep for asyncpg/psql imports returns none); (6) A2 disposition reference (from 132-A2-MEASUREMENT.md); (7) unit suite status.

    Commit per Done-Coding SOP on a feature branch, merge ff-only to main, delete branch, prune worktrees, push. NO Co-Authored-By / AI attribution in the commit message (memory rule). Commit message: `feat(132): migrate trade_framer constants to APR + per-class stop floors + verify stop geometry`. Include the migration files, trade_framer.py, intelligence_pipeline.py, run_historical_pipeline.py, the new tests, and the .planning verification docs.

    Run everything autonomously — no human checkpoints (memory rule).
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/ -q` exits 0; 132-VERIFICATION.md exists with all 7 sections; `git log --oneline -1` shows the phase commit on main; `git status` clean.
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/ -q` exits 0
    - 132-VERIFICATION.md exists and contains all 7 sections (stopped_at_entry gate, APR inventory, bare-literal audit, regression results, DAG invariant, A2 disposition, suite status)
    - `grep -nE "import asyncpg|PGPASSWORD|psycopg" src/intelligence/trading/trade_framer.py` returns zero results (DAG invariant: no direct DB access)
    - The commit message contains no "Co-Authored-By" or AI attribution
    - `git status --porcelain` is empty after push (clean tree, merged to main)
  </acceptance_criteria>
  <done>Full unit suite green; 132-VERIFICATION.md written with all 7 sections; DAG invariant confirmed (no DB access in trade_framer); phase committed (no AI attribution), merged ff-only to main, and pushed with a clean tree.</done>
</task>

</tasks>

<verification>
- stopped_at_entry < 5% of stop-type exits on 1-month sample replay
- 35 APR keys in config_state + config_schema, dashboard-visible
- no migratable bare literals in trade_framer.py
- DAG invariant confirmed (no DB access in trade_framer)
- full unit suite green; phase committed and pushed
</verification>

<success_criteria>
- stopped_at_entry exit_reason < 5% of stop-type exits (or no stop exits) on fresh 1-month replay + lifecycle_replay
- `SELECT COUNT(*) FROM config_state WHERE config_key LIKE 'feature.trade_framer.%' OR config_key='threshold.trade_framer.min_rr_t1'` == 35
- only intentionally-retained numeric constants remain at module level in trade_framer.py
- 132-VERIFICATION.md documents all gates; phase merged to main and pushed
</success_criteria>

<output>
After completion, create `.planning/phases/132-stop-zone-geometry-apr-migration/132-05-SUMMARY.md`
</output>
</content>
</invoke>
