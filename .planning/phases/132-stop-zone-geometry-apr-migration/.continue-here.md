---
context: phase
phase: 132-stop-zone-geometry-apr-migration
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-06-18T02:26:06.658Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure in this session.
> Acknowledge each one explicitly before proceeding.

- [x] CONSTRAINT: stopped_at_entry-not-in-db — RESOLVED. Plan 05 redesigned to use `trade_executions.actual_mfe <= 0.05 OR actual_bars <= 2 OR actual_bars IS NULL` as the stopped_at_entry classifier, with denominator `exit_reason = 'stop_loss'`. Replicates `_classify_stop_outcome()` exactly (lifecycle_tracker.py:556-564). No log parsing.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| stopped_at_entry DB assumption | `trade_executions.exit_reason` never contains `'stopped_at_entry'` — tracker always writes `'stop_loss'`. `stopped_at_entry` is a `SignalOutcome` enum from `lifecycle_tracker._classify_stop_outcome()`, classified in-memory, never persisted. Any query on `exit_reason='stopped_at_entry'` returns 0, trivially passing any % gate (silent wrong answer). | FIXED in Plan 05 | Gate query uses `actual_mfe <= 0.05 OR actual_bars <= 2 OR actual_bars IS NULL` (replicates classifier logic exactly) with `WHERE exit_reason = 'stop_loss'` denominator. Confirmed: `trade_executions` has both `actual_mfe` and `actual_bars` columns written by lifecycle_replay. |
| migration number collision | Main branch added migration 145 (db_optimization_indexes) while Plan 02 was running in a worktree. Both used 145_ prefix. Plan 02's was renamed 146_phase132_trade_framer_apr.sql after merge. Plans 03/04 already updated to 147/148. | advisory | Always resolve front-month futures and check `ls production/migrations/` from the MAIN branch worktree before writing a new migration filename. |

<current_state>
Wave 1 complete. Plans 01 and 02 executed, merged, and committed. Main branch is clean at commit `1d74fe22`. Tests: 4792 passed, 37 skipped.

Paused before Wave 2 (Plan 03: adaptive buffer 12-key APR migration) due to context exhaustion in orchestrator.

Migration status:
- 144_signal_events_integrity_index.sql — pre-existing
- 145_db_optimization_indexes.sql — pre-existing (main branch)
- 146_phase132_trade_framer_apr.sql — Phase 132 Plan 02 (19 constants, APPLIED to DB)
- 147_phase132_adaptive_buffer_apr.sql — Plan 03 (PENDING)
- 148_phase132_stop_floor_per_class.sql — Plan 04 (PENDING)

ADAPTIVE_BUFFER_HARD_CAP is still in trade_framer.py as a module-level constant (Plan 02 retained it as a Plan 03 placeholder per SUMMARY). Plan 03 removes it.
</current_state>

<completed_work>

**Plan 132-01: A2 empirical measurement** (commits c363d6aa, cb48baea)
- 2-week sample replay + lifecycle_replay run on QQQ, ES front-month, NG front-month, GBPUSD
- A2 disposition: GAP — 44.7% stopped_at_entry rate measured from lifecycle_replay logs (NOT from DB)
- Root cause confirmed: semantic classification in lifecycle_tracker._classify_stop_outcome() (current_mfe<=0.05R), not geometry bug
- zone_engine confirmed to have no bypass (grep proves no TradeFrame construction)
- Two bugs fixed: FK deletion order in run_historical_pipeline.py, NoneType.isoformat() crash in lifecycle_replay.py
- 132-A2-MEASUREMENT.md written (240 lines, 5 required sections)

**Plan 132-02: 19-constant APR migration** (commits 66570075, 34660610, 03fbf19c, 9542d26c, 0d8d8ffd)
- Migration 146_phase132_trade_framer_apr.sql applied — 19 APR keys at exact seed values
- All 19 module-level ATR multipliers replaced with _cfg() calls (31 call sites total)
- ADAPTIVE_BUFFER_HARD_CAP retained as Plan 03 placeholder
- run_historical_pipeline.py wired: set_config_service() called in both single-worker and parallel-worker paths via _warm_config_service()
- 27-test regression suite with strict mock ConfigService — zero behavioral change at seed values
- circular import in plugin_utils.py fixed (ATR_STOP_FALLBACK_MULTIPLIER → literal 2.0)

**Post-wave cleanup (this session)**
- Migration 145_phase132_trade_framer_apr.sql renamed to 146_ (commit ecacac9e)
- Plans 03/04/05 migration numbers updated: 145→147, 146→148 (commit 1d74fe22)
- Both worktrees merged, cleaned, pruned
- ROADMAP updated: plans 01 and 02 marked complete
</completed_work>

<remaining_work>

**Wave 2 — Plan 132-03: Adaptive buffer 12-key APR migration (migration 147)**
- Write migration 147_phase132_adaptive_buffer_apr.sql (12 coefficient keys)
- Register 12 keys in _THRESHOLD_KEYS (intelligence_pipeline.py)
- Rewrite _adaptive_buffer() body to read via _cfg()
- Remove ADAPTIVE_BUFFER_HARD_CAP module constant (Plan 02 kept it as placeholder)
- Add startup continuity assertion: `low_vol_base + (1.0 - vol_ratio_min) * (slope_num/slope_den) ≈ 1.0` (IMPORTANT: Renaissance council finding — the 12 keys define a piecewise function with a mathematical continuity invariant at vol_ratio=1.0)
- Anchor-point regression test with strict mock (0.70→0.80, 1.00→1.00, 1.50→1.35)

**Wave 3 — Plan 132-04: Per-class stop floor APR keys (migration 148)**
- 4 per-class floor keys (fx=1.0, commodity_small_tick=1.5, equity_etf=1.0, futures_large_tick=1.0)
- _min_stop_multiplier_floor(asset_class) router (mirrors _min_zone_width_atr pattern)

**Wave 4 — Plan 132-05: Final verification**
- CRITICAL: redesign stopped_at_entry gate — do NOT use trade_executions.exit_reason='stopped_at_entry'
  Use lifecycle_replay log output counts OR query trade_frames for current_mfe<=0.05R / stop exits
- 35 APR keys count gate (34 feature.trade_framer.* + 1 threshold.trade_framer.min_rr_t1)
- Bare-literal audit
- Full unit suite + push
</remaining_work>

<decisions_made>

- Migration numbering: Plan 02 used 146 (not 145 — collision with db_optimization_indexes); Plans 03/04 use 147/148
- stopped_at_entry root cause: semantic classification (current_mfe<=0.05R), not zone geometry — Phase 126 gates are working
- Adaptive buffer continuity invariant should be enforced via startup assertion, not architectural redesign (12 independent scalars is acceptable for Phase 132, assertion catches breakage)
- ATR_TARGET_MAX_MULTIPLIER_BY_TF dict (6 per-TF target ceilings) was NOT migrated — gap noted; defer to Phase 133 or gap closure
- ROADMAP plans 01/02 marked complete; ROADMAP.md has uncommitted changes
</decisions_made>

<blockers>

- ATR_TARGET_MAX_MULTIPLIER_BY_TF not migrated — semantic inconsistency, non-blocking for Phase 132 but should be captured as a todo
</blockers>

## Required Reading (in order)
1. `.planning/phases/132-stop-zone-geometry-apr-migration/132-A2-MEASUREMENT.md` — A2 findings, stopped_at_entry semantics, measurement methodology
2. `.planning/phases/132-stop-zone-geometry-apr-migration/132-02-SUMMARY.md` — what Plan 02 did; note ADAPTIVE_BUFFER_HARD_CAP placeholder
3. `.planning/phases/132-stop-zone-geometry-apr-migration/132-RESEARCH.md §3.2` — adaptive buffer 12-key decomposition table
4. `src/intelligence/trading/trade_framer.py` lines 65-74 (_cfg pattern), 136-159 (_adaptive_buffer current body)

## Infrastructure State
- Main branch: clean at `1d74fe22` (except .planning/ROADMAP.md uncommitted)
- DB: migration 146 applied (19 trade_framer APR keys live in config_state)
- Unit tests: 4792 passed, 37 skipped — green
- Worktrees: all cleaned and pruned

<context>
Session executed Wave 1 (Plans 01+02) in parallel worktrees. Renaissance council review raised two findings mid-session: (1) adaptive buffer piecewise continuity invariant needs a startup assertion, (2) ATR_TARGET_MAX_MULTIPLIER_BY_TF dict is a gap. Plan 01 surfaced the critical finding that stopped_at_entry is not in trade_executions — this invalidates Plan 05's gate query and requires redesign. Context exhausted before Wave 2 could start. Everything is committed and clean. Resume by re-running execute-phase 132 — it will skip plans with SUMMARY.md and start from Plan 03.
</context>

<next_action>
Run `/gsd-execute-phase 132` — it will discover plans 01/02 have SUMMARYs and start Wave 2 (Plan 03: adaptive buffer migration). Before Plan 05 executes, the gate query must be redesigned per the blocking constraint above (the verifier agent reading this file will enforce the check).
</next_action>
