---
phase: 132-stop-zone-geometry-apr-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/144_phase132_trade_framer_apr.sql
  - src/intelligence/trading/trade_framer.py
  - services/intelligence_pipeline.py
  - production/scripts/run_historical_pipeline.py
autonomous: true
gap_closure: false

must_haves:
  truths:
    - "All 19 migratable module-level constants in trade_framer.py are read via _cfg() with the current value as the fallback"
    - "Migration 144 seeds the 19 module-level APR keys with config_schema + config_state + config_history rows"
    - "run_historical_pipeline.py wires trade_framer.set_config_service() so replay exercises APR values, not silent hardcoded fallbacks"
    - "At seed values, signal count and stop geometry are unchanged versus pre-migration (the seed equals the prior hardcoded value)"
  artifacts:
    - path: "production/migrations/144_phase132_trade_framer_apr.sql"
      provides: "config_schema + config_state + config_history for 19 module-level APR keys"
      contains: "feature.trade_framer.min_stop_atr"
    - path: "src/intelligence/trading/trade_framer.py"
      provides: "_cfg()-backed constant reads replacing module-level literals"
    - path: "services/intelligence_pipeline.py"
      provides: "_THRESHOLD_KEYS entries for the 19 new keys"
    - path: "production/scripts/run_historical_pipeline.py"
      provides: "ConfigService construction + trade_framer.set_config_service() wiring for the replay path"
  key_links:
    - from: "trade_framer.py constant sites"
      to: "config_state"
      via: "_cfg(key, fallback) reading the prewarmed ConfigService cache"
      pattern: "_cfg\\(\"feature.trade_framer"
    - from: "run_historical_pipeline.py"
      to: "trade_framer.set_config_service"
      via: "ConfigService injection into the replay framing path"
      pattern: "set_config_service"
    - from: "services/intelligence_pipeline.py _THRESHOLD_KEYS"
      to: "ConfigService prewarm cache"
      via: "_prewarm_threshold_config loads each key at startup"
      pattern: "feature.trade_framer"
---

<objective>
Migrate the 19 migratable module-level numeric constants in `trade_framer.py` to APR (migration 144), wire every constant site through `_cfg(key, fallback)`, register the keys in `_THRESHOLD_KEYS`, and fix the silent-degradation gap in `run_historical_pipeline.py` (which currently never calls `trade_framer.set_config_service()`, so replay runs entirely on hardcoded fallbacks).

The 1-tick gate (`stop <= entry + 1 tick`) and the genuinely non-tunable constants (EPSILON_TOLERANCE, ATR_EMERGENCY_FALLBACK_PCT, ATR_TARGET_MAX_MULTIPLIER, the per-TF max-target dict, STRUCTURE_SNAP_PROXIMITY_ATR is migrated; see table) are out of scope per CONTEXT D-03 and RESEARCH §3.1.

Purpose: APR mandate (CLAUDE.md) — every hardcoded numeric threshold/weight/multiplier in src/ is an architecture violation. Renaissance correctness guarantee: every `_cfg(key, fallback)` fallback equals the current hardcoded value, so at seed values behavior is bit-identical and ML training data integrity is preserved.
Output: migration 144 (module-level portion), APR-backed trade_framer.py, prewarm registrations, and replay config wiring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-stop-zone-geometry-apr-migration/132-CONTEXT.md
@.planning/phases/132-stop-zone-geometry-apr-migration/132-RESEARCH.md
@docs/foundation/adaptive-parameter-registry.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 144 (module-level keys) + register in _THRESHOLD_KEYS</name>
  <read_first>
    - production/migrations/142_phase130_apr_seeds.sql (canonical migration format: config_schema with default_value/min_value/max_value/description, config_state with version, config_history with changed_by/reason)
    - .planning/phases/132-stop-zone-geometry-apr-migration/132-RESEARCH.md §3.1 (the 19-key table with exact APR key names, values, and ML-target flags) and §6.4 (migration SQL format)
    - services/intelligence_pipeline.py lines 376-405 (_THRESHOLD_KEYS tuple format)
    - src/config/config_service.py lines 39-80 (OPS_PREFIXES — confirm "feature." and "threshold." already present; no change needed)
  </read_first>
  <files>production/migrations/144_phase132_trade_framer_apr.sql, services/intelligence_pipeline.py</files>
  <action>
    Create `production/migrations/144_phase132_trade_framer_apr.sql`. This migration covers ONLY the 19 module-level constants (the adaptive buffer keys go in Plan 03 migration 145; the per-class floors go in Plan 04 migration 146). Follow the exact 3-block format of migration 142: config_schema INSERT, config_state INSERT, config_history INSERT, all with `ON CONFLICT (config_key) DO NOTHING` on the first two.

    Seed these 19 keys at exactly their current hardcoded values (RESEARCH §3.1). config_state value is the string form of the current constant:

    | APR key | type | seed | min | max | ML target |
    |---------|------|------|-----|-----|-----------|
    | feature.trade_framer.stop_demand_buffer_atr | float | 0.25 | 0.05 | 2.0 | Yes |
    | feature.trade_framer.stop_sweep_buffer_atr | float | 0.30 | 0.05 | 2.0 | Yes |
    | feature.trade_framer.stop_ob_buffer_atr | float | 0.20 | 0.05 | 2.0 | Yes |
    | feature.trade_framer.stop_swing_buffer_atr | float | 0.25 | 0.05 | 2.0 | Yes |
    | feature.trade_framer.stop_sr_buffer_atr | float | 0.50 | 0.05 | 2.0 | Yes |
    | feature.trade_framer.stop_fallback_atr | float | 2.0 | 0.5 | 5.0 | Yes |
    | feature.trade_framer.zone_sweep_atr | float | 0.76 | 0.1 | 2.0 | Yes |
    | feature.trade_framer.zone_low_atr | float | 1.0 | 0.1 | 3.0 | Yes |
    | feature.trade_framer.zone_high_atr | float | 0.5 | 0.1 | 3.0 | Yes |
    | feature.trade_framer.target_min_atr | float | 0.5 | 0.1 | 3.0 | Yes |
    | feature.trade_framer.zone_plugin_fallback_atr | float | 0.2 | 0.05 | 2.0 | No (structural fallback) |
    | feature.trade_framer.vp_proximity_atr | float | 0.5 | 0.05 | 2.0 | Yes |
    | feature.trade_framer.fallback_t1_atr | float | 2.0 | 0.5 | 6.0 | Yes |
    | feature.trade_framer.fallback_t2_atr | float | 3.5 | 1.0 | 8.0 | Yes |
    | feature.trade_framer.fallback_t3_atr | float | 5.5 | 1.0 | 12.0 | Yes |
    | feature.trade_framer.min_stop_atr | float | 1.0 | 0.25 | 3.0 | Yes — primary ML stop control, bounded below by feature.zone_engine.min_stop_distance_atr |
    | threshold.trade_framer.min_rr_t1 | float | 1.5 | 0.5 | 5.0 | Yes |
    | feature.trade_framer.adaptive_buffer_hard_cap | float | 1.40 | 1.0 | 3.0 | Operator preference |
    | feature.trade_framer.structure_snap_proximity_atr | float | 1.5 | 0.25 | 5.0 | No (structural classification) |

    Note: confirm the live `ATR_ZONE_SWEEP_MULTIPLIER` value by reading trade_framer.py line 101-103 — RESEARCH §3.1 lists 0.76 while the todo table lists 0.5. Use the actual code value as the seed (the seed MUST equal the current hardcoded value). Whichever it is, the config_state seed string must match the literal in the code.

    Every config_schema description must include the `[initial_estimate]` provenance tag and, for ML targets, the phrase "ML learning target". config_history `changed_by` = 'migration_144', `reason` = 'Phase 132 A5 APR seed — trade_framer module-level constant'.

    Then add all 19 keys to `_THRESHOLD_KEYS` in services/intelligence_pipeline.py (tuple-of-tuples format `("feature.trade_framer.min_stop_atr", 1.0)`), grouped with a comment `# --- migration 144: Phase 132 trade_framer module-level constants ---`. Each default in the tuple MUST equal the migration seed.

    Apply the migration: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/144_phase132_trade_framer_apr.sql`.
  </action>
  <verify>
    `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -t -c "SELECT COUNT(*) FROM config_state WHERE config_key LIKE 'feature.trade_framer.%' OR config_key = 'threshold.trade_framer.min_rr_t1';"` returns 19; the migration file applies with no SQL errors.
  </verify>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM config_state WHERE config_key LIKE 'feature.trade_framer.%' OR config_key='threshold.trade_framer.min_rr_t1'` returns exactly 19
    - `SELECT config_value FROM config_state WHERE config_key='feature.trade_framer.min_stop_atr'` returns '1.0'
    - `grep -c "feature.trade_framer\.\|threshold.trade_framer.min_rr_t1" services/intelligence_pipeline.py` returns at least 19 (the _THRESHOLD_KEYS registrations)
    - Every config_schema description for the 19 keys contains the string "[initial_estimate]"
    - migration 144 file applied with no psql error (exit code 0)
  </acceptance_criteria>
  <done>19 module-level keys exist in config_state at exact seed values and are registered in _THRESHOLD_KEYS; migration 144 applied cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Replace module-level constant references in trade_framer.py with _cfg() calls</name>
  <read_first>
    - src/intelligence/trading/trade_framer.py (constant definitions lines 86-165; _cfg() at line 73-74; _resolve_stop_long at line 537+; _resolve_stop_short ~line 600+; all usage sites — grep each constant name)
    - .planning/phases/132-stop-zone-geometry-apr-migration/132-RESEARCH.md §7.5 (silent failure: partial reference replacement) and §3.1 (NOT-migrated list: EPSILON_TOLERANCE, ATR_EMERGENCY_FALLBACK_PCT, ATR_TARGET_MAX_MULTIPLIER, ATR_TARGET_MAX_MULTIPLIER_BY_TF dict)
  </read_first>
  <files>src/intelligence/trading/trade_framer.py</files>
  <action>
    For each of the 19 migrated constants, replace EVERY usage site with the corresponding `_cfg("feature.trade_framer.<...>", <current_value>)` call (or `threshold.trade_framer.min_rr_t1` for min_rr_t1). The fallback argument MUST be the current hardcoded literal value — this is the regression-safety guarantee.

    For constants used inside `_adaptive_buffer()` calls as the `base_mult` argument (MIN_STOP_ATR_MULTIPLIER, ATR_STOP_DEMAND_MULTIPLIER, ATR_STOP_OB_MULTIPLIER, etc. at lines 544, 550, 559, 613, 628, and elsewhere), pass the `_cfg(...)` result as base_mult. Do NOT touch the internal piecewise coefficients of `_adaptive_buffer` — those are Plan 03.

    Do NOT migrate `ADAPTIVE_BUFFER_HARD_CAP`'s usage inside `_adaptive_buffer()` in this plan if it is read internally by the buffer function — Plan 03 owns the adaptive buffer body. However the module-level KEY for adaptive_buffer_hard_cap IS seeded in migration 144 (Task 1) so it exists for Plan 03 to read; just do not rewire the internal `min(result, base_mult * ADAPTIVE_BUFFER_HARD_CAP)` line here (Plan 03 does that). Coordinate: Task 1 seeds the key; Plan 03 wires the internal read.

    Remove each migrated module-level constant definition (lines 86-165 region) ONLY after confirming zero remaining references via grep. Leave a one-line comment noting the APR key that replaced it, e.g. `# ATR_STOP_DEMAND_MULTIPLIER -> feature.trade_framer.stop_demand_buffer_atr (APR, migration 144)`.

    Do NOT remove or alter: EPSILON_TOLERANCE, ATR_EMERGENCY_FALLBACK_PCT, ATR_TARGET_MAX_MULTIPLIER, ATR_TARGET_MAX_MULTIPLIER_BY_TF, the 1-tick gate logic, MIN_ZONE_WIDTH_ATR (already APR-backed as a fallback at line 86, leave as fallback constant).

    Run `.venv/bin/ruff check src/intelligence/trading/trade_framer.py --fix` and `.venv/bin/black src/intelligence/trading/trade_framer.py` after edits.
  </action>
  <verify>
    grep for each removed constant name returns only comment lines or zero results; ruff and black pass clean; `.venv/bin/python -c "import src.intelligence.trading.trade_framer"` imports without error.
  </verify>
  <acceptance_criteria>
    - `grep -nE "^(ATR_STOP_DEMAND_MULTIPLIER|ATR_STOP_SWEEP_MULTIPLIER|ATR_STOP_OB_MULTIPLIER|ATR_STOP_SWING_MULTIPLIER|ATR_STOP_SR_MULTIPLIER|ATR_STOP_FALLBACK_MULTIPLIER|ATR_ZONE_SWEEP_MULTIPLIER|ATR_ZONE_LOW_MULTIPLIER|ATR_ZONE_HIGH_MULTIPLIER|ATR_TARGET_MIN_MULTIPLIER|ATR_ZONE_PLUGIN_FALLBACK_MULTIPLIER|VP_PROXIMITY_THRESHOLD_ATR|ATR_FALLBACK_T1_MULTIPLIER|ATR_FALLBACK_T2_MULTIPLIER|ATR_FALLBACK_T3_MULTIPLIER|MIN_STOP_ATR_MULTIPLIER|MIN_RR_T1|ADAPTIVE_BUFFER_HARD_CAP|STRUCTURE_SNAP_PROXIMITY_ATR) *= *[0-9]" src/intelligence/trading/trade_framer.py` returns zero results (all 19 migrated definitions removed). Note: ADAPTIVE_BUFFER_HARD_CAP's internal *usage* inside _adaptive_buffer() is rewired by Plan 03, but its module-level *assignment* must be gone here since the key is seeded in migration 144 — if the bare assignment must remain temporarily for Plan 03's internal read, that single line is the only permitted exception and must be explicitly noted; otherwise expect zero results.
    - `grep -c "_cfg(\"feature.trade_framer\|_cfg(\"threshold.trade_framer" src/intelligence/trading/trade_framer.py` returns at least 19
    - Every `_cfg("feature.trade_framer...")` call in the stop/zone/target sections passes a numeric fallback equal to the migration 144 seed for that key
    - `.venv/bin/ruff check src/intelligence/trading/trade_framer.py` exits 0
    - `.venv/bin/python -c "import src.intelligence.trading.trade_framer"` exits 0
    - EPSILON_TOLERANCE, ATR_EMERGENCY_FALLBACK_PCT, and the 1-tick gate logic are unchanged (grep confirms they still exist)
  </acceptance_criteria>
  <done>All 19 migratable module-level constants read via _cfg() with matching fallbacks; no migratable bare-literal definitions remain; retained constants and 1-tick gate unchanged; module imports clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire ConfigService into run_historical_pipeline.py replay path</name>
  <read_first>
    - production/scripts/run_historical_pipeline.py (imports around line 98-99; replay_symbol entry around line 1640-1700; main/argparse around line 2102-2230; the aggregate() call at line 1261)
    - services/intelligence_pipeline.py lines 254, 376-405, 497-535 (_prewarm_threshold_config: how ConfigService is constructed and which set_config_service() calls are made — replicate the trade_framer + zone_engine subset)
    - src/config/config_service.py (ConfigService constructor signature + get_sync + how it loads from config_state)
    - .planning/phases/132-stop-zone-geometry-apr-migration/132-RESEARCH.md §6.3 and §7.1 (the wiring gap and silent-degradation risk)
  </read_first>
  <files>production/scripts/run_historical_pipeline.py</files>
  <action>
    The replay path currently never injects a ConfigService, so trade_framer (and zone_engine, aggregator, etc.) run on `_config_service is None` → `_cfg()` returns hardcoded defaults. This is the silent-degradation gap from RESEARCH §7.1. Because the migration seeds == hardcoded defaults, behavior is currently identical — but APR values would never be exercised in replay, defeating the purpose of the migration and any future ML-tuned values.

    Construct a ConfigService in the replay startup (in `main()` or wherever the pipeline modules are first imported for replay), load it from the DB (same construction pattern as intelligence_pipeline.py's _prewarm_threshold_config), pre-warm the relevant keys, and call `trade_framer.set_config_service(cfg)` plus the other framing-path modules that already have set_config_service (zone_engine, aggregator, confidence_utils — match the subset that replay_symbol actually exercises). Use the SAME import names as intelligence_pipeline.py:510-522.

    The ConfigService must be constructed once and injected before the first `replay_symbol()` / `frame_trade()` call. If ConfigService construction requires an async event loop or DB pool that the replay script does not have, prefer the synchronous DB load path (config_state is a simple table read); confirm ConfigService supports a sync warm load by reading its constructor. If it strictly requires async init, wrap the warm-up in the existing replay async context.

    Add a single structlog/print line at injection confirming `trade_framer config service wired (N keys)`. Do NOT pass `event=` as a structlog kwarg (CLAUDE.md gotcha) — use `signal=` or a print.

    Confirm DAG invariant: ConfigService reads config_state at warm-up only, then serves from in-memory cache via get_sync(). This is infrastructure config loading, not domain DB access from the framing hot path — DAG invariant 3 is satisfied (RESEARCH §7.6).
  </action>
  <verify>
    `grep -n "set_config_service\|ConfigService" production/scripts/run_historical_pipeline.py` shows the new wiring; a dry sample replay (`--replay-only --days 2 --symbols QQQ --client-id 40`) prints the "config service wired" confirmation and exits 0.
  </verify>
  <acceptance_criteria>
    - `grep -c "trade_framer.set_config_service" production/scripts/run_historical_pipeline.py` returns at least 1
    - `grep -c "ConfigService" production/scripts/run_historical_pipeline.py` returns at least 1
    - A `--replay-only --days 2 --symbols QQQ --client-id 40` run prints a confirmation line containing "config service" and exits 0
    - No `event=` kwarg is passed to structlog in the added code (`grep "event=" <added lines>` is empty)
    - ConfigService is constructed once before the first replay_symbol call (not per-symbol)
  </acceptance_criteria>
  <done>Replay path constructs a ConfigService and calls trade_framer.set_config_service() once before the first replay_symbol call; a 2-day sample replay prints the wiring confirmation and exits 0.</done>
</task>

<task type="auto">
  <name>Task 4: Regression test — APR-backed reads at seed values produce identical output</name>
  <read_first>
    - tests/unit/intelligence/ (existing trade_framer or trading-layer unit tests for the established test pattern + fixture style)
    - src/intelligence/trading/trade_framer.py (frame_trade signature, _resolve_stop_long/_resolve_stop_short, set_config_service, _cfg)
    - .planning/phases/132-stop-zone-geometry-apr-migration/132-RESEARCH.md §9.2 (regression test design)
  </read_first>
  <files>tests/unit/intelligence/test_trade_framer_apr_regression.py</files>
  <action>
    Add a unit test (new file `tests/unit/intelligence/test_trade_framer_apr_regression.py` or extend an existing trade_framer test) proving that with the ConfigService returning the seed values, frame_trade / _resolve_stop_long produce byte-identical stop/zone/target output to the hardcoded baseline.

    Approach: build a fixed `features` dict and call _resolve_stop_long(entry, atr, features, regime_type) (1) with `set_config_service(None)` (uses hardcoded fallbacks) and (2) with a mock ConfigService whose `get_sync(key, default)` returns the exact seed value for each `feature.trade_framer.*` key. Assert the two stop tuples are equal within EPSILON_TOLERANCE. Do the same for the zone and target paths and for at least one full frame_trade() call.

    Reset `set_config_service(None)` in test teardown to avoid leaking module-level state across tests (the _config_service global is module-scoped — CLAUDE.md module-level utility pattern).
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/test_trade_framer_apr_regression.py -q` passes.
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/intelligence/test_trade_framer_apr_regression.py -q` exits 0 with all tests passing
    - The test asserts equality between hardcoded-fallback output and seed-value-ConfigService output for stop, zone, and target
    - The test resets the module-level _config_service to None in teardown
    - `.venv/bin/pytest tests/unit/ -q` exits 0 (no regression in the broader unit suite)
  </acceptance_criteria>
  <done>Regression test proves seed-value ConfigService output equals hardcoded-fallback output for stop, zone, and target; full unit suite green.</done>
</task>

</tasks>

<verification>
- 19 module-level keys exist in config_state at exact seed values
- trade_framer.py reads all 19 via _cfg() with matching fallbacks; no migratable bare literals remain
- run_historical_pipeline.py wires trade_framer.set_config_service()
- regression test proves seed-value output == hardcoded output
- full unit suite green
</verification>

<success_criteria>
- `SELECT COUNT(*) FROM config_state WHERE config_key LIKE 'feature.trade_framer.%' OR config_key='threshold.trade_framer.min_rr_t1'` returns 19
- No migratable module-level numeric constant definitions remain in trade_framer.py
- Replay path exercises APR values (not silent hardcoded fallbacks)
- Regression test confirms zero behavioral change at seed values
</success_criteria>

<output>
After completion, create `.planning/phases/132-stop-zone-geometry-apr-migration/132-02-SUMMARY.md`
</output>
</content>
</invoke>
