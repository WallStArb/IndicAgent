---
phase: 49.2-hmm-operational-fixes-observability-fallback-logging-warm-up-noise
plan: 01
subsystem: intelligence
tags: [hmm, structlog, observability, warm-up, pydantic, tdd]

requires:
  - phase: 49.1-regime-gate-fix
    provides: signal_ledger writes decoupled; regime gating foundation for HMM prob suppression
provides:
  - structlog fallback warning when HMM degrades to 2D emission mode
  - hmm_n_dims (2 or 5) in every HMM output dict and intelligence_features.smc JSONB
  - hmm_warmed_up (bool) in every HMM output dict and intelligence_features.smc JSONB
  - warm-up prob suppression via existing regime_gate prob_min floor (zero changes to gate)
  - SMCContext schema fields hmm_n_dims and hmm_warmed_up
affects:
  - feature-writer-service
  - intelligence-features-schema
  - phase-54-ml-scoring

tech-stack:
  added: []
  patterns:
    - "bars_processed counter in HMM _state tracks convergence; warmed_up = bars_processed >= min_lookback"
    - "structlog.warning with structured kwargs instead of log.warning(f-string)"
    - "_build_output() zero-guards all prob fields when not warmed_up — single location, no scattered if-checks"

key-files:
  created: []
  modified:
    - src/intelligence/smart_money/hmm_regime.py
    - src/intelligence/schemas.py
    - tests/unit/intelligence/test_smart_money_plugins.py

key-decisions:
  - "Warm-up suppression zeroes all 4 hmm_prob_* fields (not just hmm_regime_prob) for audit trail consistency"
  - "bars_processed initialized to 0 in _reset_state() and incremented in _forward_step() so compute_full() with N bars sets bars_processed=N-1 (returns)"
  - "structlog logger at module level (logger = structlog.get_logger(__name__)) follows project pattern"
  - "n_dims stored in _state after _reset_state() call so it persists through the forward loop and into _build_output()"

patterns-established:
  - "Plugin convergence tracking: _state['bars_processed'] counter + warmed_up = bars_processed >= min_lookback"
  - "Structured warning events: logger.warning('event_name', field=value) — not f-strings"

requirements-completed: [HMM-01, HMM-02, HMM-03, HMM-04]

duration: 7min
completed: 2026-03-23
---

# Phase 49.2 Plan 01: HMM Observability and Warm-Up Suppression Summary

**structlog 2D fallback warning + hmm_n_dims/hmm_warmed_up observability fields + warm-up prob zeroing in HMMRegimePlugin, with 11 new TDD tests covering all 4 behaviors**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-03-23T19:33:00Z
- **Completed:** 2026-03-23T19:37:14Z
- **Tasks:** 2 (RED + GREEN)
- **Files modified:** 3

## Accomplishments

- HMM-01: `_resolve_dims()` now emits `hmm_fallback_2d` (with `missing_fields` kwarg) or `hmm_fallback_2d_no_features` via structlog — silent in full 5D mode
- HMM-02: `hmm_n_dims` (int: 2 or 5) added to outputs frozenset, `_state`, and `_build_output()` — persists to intelligence_features.smc JSONB automatically
- HMM-03: `hmm_warmed_up` (bool) added to outputs frozenset and `_build_output()`, derived from `bars_processed >= min_lookback`
- HMM-04: All 4 `hmm_prob_*` fields zeroed in `_build_output()` when `warmed_up is False` — warm-up noise suppressed through existing `regime_gate` `prob_min=0.30` floor with zero changes to regime_gate.py
- SMCContext updated with `hmm_n_dims: int | None` and `hmm_warmed_up: bool | None` — no migration needed
- 19 total HMM tests pass (6 existing + 11 new + 2 schema)

## Task Commits

Each task was committed atomically:

1. **Task 1: RED — Write failing tests for all 4 HMM behaviors** - `afeaec3` (test)
2. **Task 2: GREEN — Implement HMM fixes in schemas.py and hmm_regime.py** - `f11b4f3` (feat)

_Note: TDD tasks committed as test → feat per TDD execution protocol_

## Files Created/Modified

- `src/intelligence/smart_money/hmm_regime.py` — structlog import + logger, updated outputs frozenset, _reset_state bars_processed/n_dims init, _forward_step increment, _resolve_dims warnings, compute_full/compute_next n_dims storage, _build_output warm-up suppression
- `src/intelligence/schemas.py` — `hmm_n_dims: int | None = None` and `hmm_warmed_up: bool | None = None` added to SMCContext after hmm_regime_duration
- `tests/unit/intelligence/test_smart_money_plugins.py` — `import structlog` added; 11 new TestHMMRegime methods + TestSMCContextHMMFields class (2 tests)

## Decisions Made

- Warm-up suppression zeroes all 4 `hmm_prob_*` fields (not just `hmm_regime_prob`) for full audit trail consistency — when ML scoring reads the JSONB, cold-start bars are uniformly marked, not partially suppressed
- `bars_processed` initialized to 0 in `_reset_state()` and incremented at the end of `_forward_step()` — after `compute_full()` with N returns, `bars_processed = N` (correct: each return is one observation)
- Module-level `logger = structlog.get_logger(__name__)` follows project structlog pattern (not per-method instantiation)
- `n_dims` stored in `_state` immediately after `_reset_state()` call so it is available in `_build_output()` without passing through method arguments

## Deviations from Plan

None — plan executed exactly as written. The warm-up test strategy from the plan (directly manipulating `_state["bars_processed"]` to test `_build_output()`) worked correctly.

## Issues Encountered

None. Pre-existing ruff E501 error in schemas.py line 259 (comment too long) and 6 pre-existing test failures confirmed to be unrelated to this plan.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- HMM observability complete: `hmm_n_dims` and `hmm_warmed_up` flow automatically into intelligence_features.smc JSONB on next service restart
- `hmm_regime_prob=0.0` during warm-up will trigger existing `prob_min=0.30` suppression in `regime_gate.py` without any code change — regime gating is now noise-free on service restart
- Phase 49 (DB performance) can proceed: signal_ledger composite index, query optimization

---
*Phase: 49.2-hmm-operational-fixes-observability-fallback-logging-warm-up-noise*
*Completed: 2026-03-23*
