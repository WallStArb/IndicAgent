---
phase: 49.2
slug: hmm-operational-fixes-observability-fallback-logging-warm-up-noise
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-23
---

# Phase 49.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x |
| **Config file** | `pytest.ini` / `pyproject.toml` |
| **Quick run command** | `.venv/bin/pytest tests/unit/intelligence/test_smart_money_plugins.py -v -k "HMM"` |
| **Full suite command** | `.venv/bin/pytest tests/unit/ -v` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `.venv/bin/pytest tests/unit/intelligence/test_smart_money_plugins.py -v -k "HMM"`
- **After every plan wave:** Run `.venv/bin/pytest tests/unit/ -v`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 49.2-01-01 | 01 | 1 | HMM-01 | unit | `.venv/bin/pytest tests/unit/intelligence/test_smart_money_plugins.py -v -k "HMM and (n_dims or fallback or warn)"` | ❌ W0 | ⬜ pending |
| 49.2-01-02 | 01 | 1 | HMM-02 | unit | `.venv/bin/pytest tests/unit/intelligence/test_smart_money_plugins.py -v -k "HMM and (warmed_up or warmup or warm)"` | ❌ W0 | ⬜ pending |
| 49.2-01-03 | 01 | 1 | HMM-03 | unit | `.venv/bin/pytest tests/unit/intelligence/test_smart_money_plugins.py -v -k "HMM and schema"` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/unit/intelligence/test_hmm_operational_fixes.py` — new test file with stubs for:
  - `test_resolve_dims_5d_with_all_features` — 5D when all four indicator features present
  - `test_resolve_dims_2d_missing_features` — 2D fallback when any feature missing
  - `test_fallback_2d_emits_warning` — structlog warning fired with missing field names
  - `test_output_includes_hmm_n_dims` — plugin output contains `hmm_n_dims` key (2 or 5)
  - `test_output_includes_hmm_warmed_up` — plugin output contains `hmm_warmed_up` key
  - `test_warmup_suppresses_regime_prob` — hmm_regime_prob == 0.0 for first min_lookback bars
  - `test_warmed_up_true_after_min_lookback` — hmm_warmed_up=True once bars_processed >= min_lookback
  - `test_schema_accepts_new_fields` — SMCContext validates with hmm_n_dims + hmm_warmed_up

*If none: "Existing infrastructure covers all phase requirements."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Warm-up suppression in live pipeline | HMM-02 | Requires service restart + live bar injection | Restart feature-pipeline, watch logs for `hmm_warmed_up=False` in first 20 bars, confirm no signals fire through regime gate during warm-up |
| 2D fallback warning in service logs | HMM-01 | Requires live features missing RSI/ADX/ATR/MACD | Check `logs/feature_pipeline_service.log` for `hmm_fallback_2d` structured log events |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
