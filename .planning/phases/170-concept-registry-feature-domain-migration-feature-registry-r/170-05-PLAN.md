---
phase: 170-concept-registry-feature-domain-migration-feature-registry-r
plan: 05
type: execute
wave: 4
depends_on: ["170-04"]
blocked_on_data: "alpha_ensemble_ic must have real rows (0 as of 2026-08-04, pending the in-flight corpus rebuild). Task 1 asserts this and ABORTS the plan if unmet — do not execute waves 4-7 before then."
files_modified:
  - docs/analysis/phase170-registry-live-data-rehearsal.md
  - scripts/ops/alpha/ops_concept_registry_rehearsal.sh
autonomous: true
requirements: [H-1, M-B]
user_setup: []

must_haves:
  truths:
    - "The plan refuses to proceed, loudly, while alpha_ensemble_ic is empty — the data gate is mechanical, not a note in a doc"
    - "ConceptRegistryService.record_comparison_outcome's live main() recording path has executed end-to-end against real data at least once, against a scratch database, before any real promotion depends on it"
    - "The registry write, the concept_transition_log row, and the concept_annotation observation row are each individually confirmed to have landed"
    - "The F3 evidence-mass floor (min_new_observations=2000) is checked for empirical viability against two successive real corpus vintages, with a stated verdict"
  artifacts:
    - path: "docs/analysis/phase170-registry-live-data-rehearsal.md"
      provides: "The H-1 F3-floor viability measurement and the M-B rehearsal transcript with a PASS/FAIL verdict per artefact"
      contains: "F3 evidence-mass floor"
    - path: "scripts/ops/alpha/ops_concept_registry_rehearsal.sh"
      provides: "Re-runnable scratch-DB rehearsal harness for the --challenger-concept recording path"
      contains: "indicagent_rehearsal"
  key_links:
    - from: "ops_ensemble_weight_compare.py --challenger-concept"
      to: "concept_registry / concept_transition_log / concept_annotation"
      via: "ConceptRegistryService.record_comparison_outcome executed against real alpha_ensemble_ic rows"
      pattern: "record_comparison_outcome"
---

<objective>
Execute, for the first time ever, the `ConceptRegistryService.record_comparison_outcome` live
recording path against real data — on a scratch database — and measure whether the F3
evidence-mass floor is empirically achievable before it becomes load-bearing.

Purpose: todo 118's H-1 and the Fable M-B finding. Every existing test of this path covers pure
helpers or mocks; the live `main()` recording path has NEVER run, because `alpha_ensemble_ic` has
been empty since the mechanism shipped. Two real defects (a migration-drift bug and a runtime
crash on the durable-annotation insert, both fixed in commit `6f1b4257`) survived unit tests AND a
full plan review undetected. This phase makes `ic_engine` a second automated writer against the
same registry — running that machinery for real once, cheaply, before trusting it is the entire
point.

Output: `docs/analysis/phase170-registry-live-data-rehearsal.md` with a per-artefact verdict, and
a re-runnable rehearsal harness.

## DATA GATE — read before executing anything

`alpha_ensemble_ic` had **0 rows** as of 2026-08-04. The corpus pipeline is halted at
`ops_canary_integrity_assert.py` (STATE.md Tier -1, todo 230), so steps 6-8 —
`ic_shrinkage` / `ensemble_trainer` / `alpha_publisher` — have not run, and
`alpha_ensemble_ic` is `ensemble_trainer`'s downstream output. Task 1 asserts row presence and
ABORTS on zero. Do not "work around" the gate with synthetic rows: the whole value of this plan
is that the path meets real data for the first time. If the gate fails, stop the phase here and
report — Plans 05-08 stay unexecuted until the corpus rebuild lands.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/todos/pending/118-migrate-feature-domain-into-concept-registry.md
@.planning/STATE.md

<interfaces>
`alpha_ensemble_ic` — `ensemble_trainer` / `EnsembleICEngine` output; the A/B comparison's only
data source. Read by `_COMPARE_SQL` in `scripts/ops/alpha/ops_ensemble_weight_compare.py`
(line ~93), filtered to POOLED strata at `alpha.ensemble_ic.gate_lookahead`.

`scripts/ops/alpha/ops_ensemble_weight_compare.py` — the rehearsal target. Invocation:
  .venv/bin/python scripts/ops/alpha/ops_ensemble_weight_compare.py \
      --champion <weight_version> --challenger <weight_version> \
      --challenger-concept <concept_registry.name> [--champion-concept <name>] \
      [--corpus-build-ref <ref>]
Omitting `--challenger-concept` runs report-only with no registry write — that is the safe dry
run to do first.

The five seeded `domain='ensemble_strategy'` concepts (live): 4 `candidate`, 1 `active`. Get the
actual names with:
  SELECT name, status FROM concept_registry WHERE domain='ensemble_strategy' ORDER BY name;
The rehearsal must target a `candidate` row (only `candidate` can ever reach `promote` — see
`decide_comparison_action`), and must NOT be run against live `indicagent`.

Three artefacts the recording path is supposed to produce, each verified separately:
  1. `concept_registry.status` / `concept_gate` eval-cache update (via `record_comparison_outcome`)
  2. one `concept_transition_log` row (only on a `promote` action)
  3. one `concept_annotation` row with `annotation_type='observation'`, `source='empirical'`
     (written for actions `record_win`, `record_loss`, `promote`) — this is the insert that
     crashed at runtime and was fixed in `6f1b4257`

F3 evidence-mass floor: `alpha.concept_registry.ensemble_strategy_min_new_observations` = 2000
(live in `config_state`). `decide_comparison_action` blocks re-evaluation when
`(eval_n - last_eval_n) < min_new_observations`, where `eval_n` is the challenger's summed
`n_independent` over compared strata. H-1's question: across two successive real corpus vintages,
is that delta structurally >= 2000, or is it ~0/negative (because each vintage recomputes the same
strata rather than accumulating)? If the latter, the floor is unreachable and either the F3 delta
definition or the migration-232/225 seed value must be revisited BEFORE it gates a real promotion.

CLAUDE.md: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "..."` — plain
`psql -U postgres` fails.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert the data gate, then measure F3 evidence-mass floor viability (H-1)</name>
  <files>docs/analysis/phase170-registry-live-data-rehearsal.md</files>
  <read_first>
    - .planning/todos/pending/118-migrate-feature-domain-into-concept-registry.md (H-1 in full,
      including its "cannot check yet" note and the three redefinition options it offers)
    - src/intelligence/concept_registry_service.py, `decide_comparison_action` (the exact
      `blocked_evidence_floor` predicate being tested for viability, and what `eval_n` is summed
      over)
    - scripts/ops/alpha/ops_ensemble_weight_compare.py lines 56-135 (`_COMPARE_SQL`,
      `_registry_outcome` — how `eval_n` is actually computed from `n_independent`)
    - .planning/STATE.md, Tier -1 (why `alpha_ensemble_ic` is empty and what has to complete first)
  </read_first>
  <action>
    GATE FIRST — before any other work in this plan:
      PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc \
        "SELECT count(*) FROM alpha_ensemble_ic"
    If the result is 0, STOP. Do not proceed to Task 2, do not create the doc, do not mark the
    plan complete. Report: "Phase 170 Plan 05 BLOCKED: alpha_ensemble_ic has 0 rows; the H-1/M-B
    rehearsal cannot run until the corpus rebuild reaches ensemble_trainer (STATE.md Tier -1,
    todo 230). Plans 05-08 remain unexecuted." Exit the plan as blocked, not as failed and not as
    done.

    If rows exist, measure the F3 floor's empirical viability:

    (a) Enumerate the distinct corpus vintages present:
        SELECT weight_version, count(*) AS n_rows, sum(n_independent) AS total_n_independent,
               count(DISTINCT (tf, regime)) AS n_strata, min(computed_at), max(computed_at)
        FROM alpha_ensemble_ic GROUP BY weight_version ORDER BY min(computed_at);
        (Confirm the real column names against `\d alpha_ensemble_ic` first — do not assume
        `computed_at` exists; use whatever timestamp column the table actually has.)

    (b) For each ADJACENT pair of vintages, compute the F3 delta exactly as
        `decide_comparison_action` would see it: `eval_n` for a vintage is the sum of
        `n_independent` over the strata that vintage's comparison would cover (the POOLED strata
        at `alpha.ensemble_ic.gate_lookahead`), and the delta is
        `eval_n(vintage_k) - eval_n(vintage_{k-1})`. Report the delta for each pair against the
        2000 floor.

    (c) Also compute the two alternative delta definitions H-1 names, so the verdict has
        something concrete to recommend rather than only a rejection:
          - per-stratum growth over the SAME strata set (sum of per-stratum `n_independent`
            increases, ignoring strata that appeared or vanished)
          - corpus new-bar count (the growth in underlying bars backing the window)

    (d) Write `docs/analysis/phase170-registry-live-data-rehearsal.md` with an `Author:` /
        `Informed by:` provenance line per this project's doc convention, a "F3 evidence-mass
        floor" section carrying the three measured delta series, and an explicit VERDICT:
        VIABLE (delta structurally >= 2000, floor stands as seeded), or NOT VIABLE (name which
        alternative definition to adopt, or what the floor should be reseeded to, with the
        measured number backing it). Do NOT change the APR value in this plan — measure and
        recommend; a reseed is its own migration and its own decision.
        If only ONE vintage exists, say so plainly: the check is inconclusive, the floor is
        untested, and it stays untested until a second vintage lands. An inconclusive verdict is
        an honest outcome; a fabricated one is not.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) > 0 FROM alpha_ensemble_ic" | grep -qx t &amp;&amp; test -f docs/analysis/phase170-registry-live-data-rehearsal.md &amp;&amp; grep -q "VERDICT" docs/analysis/phase170-registry-live-data-rehearsal.md</automated>
  </verify>
  <acceptance_criteria>
    - If `alpha_ensemble_ic` is empty: the plan is reported BLOCKED with the exact message above,
      no files are created, and no further task runs. This is a correct, expected outcome — not a
      failure.
    - Otherwise: `docs/analysis/phase170-registry-live-data-rehearsal.md` exists with a section
      titled `F3 evidence-mass floor` containing measured per-vintage `sum(n_independent)` values,
      the adjacent-pair deltas, and all three delta definitions from (c).
    - The doc states an explicit `VERDICT:` of VIABLE, NOT VIABLE, or INCONCLUSIVE with the
      measured numbers backing it.
    - `SELECT config_value FROM config_state WHERE config_key='alpha.concept_registry.ensemble_strategy_min_new_observations'`
      is unchanged (still `2000`) — this plan measures, it does not reseed.
  </acceptance_criteria>
  <done>Either the phase is correctly blocked on the data gate, or the F3 floor has a measured,
  documented viability verdict grounded in two real corpus vintages.</done>
</task>

<task type="auto">
  <name>Task 2: Run the M-B live-data rehearsal of the --challenger-concept recording path on a scratch DB</name>
  <files>scripts/ops/alpha/ops_concept_registry_rehearsal.sh, docs/analysis/phase170-registry-live-data-rehearsal.md</files>
  <read_first>
    - scripts/ops/alpha/ops_ensemble_weight_compare.py (in full, 572 lines — every branch the
      rehearsal exercises: the APR-key preflight, the champion/challenger validation, the
      `record_comparison_outcome` call, the `concept_annotation` insert at lines 532-552, and the
      post-Plan-02 `fdr_passed` / `blocked_fdr_unverified` paths)
    - src/intelligence/concept_registry_service.py (the transactional apply path being exercised)
    - .planning/todos/pending/118-migrate-feature-domain-into-concept-registry.md (the M-B
      paragraph in full — it names exactly which three artefacts must be confirmed)
    - scripts/ops/corpus/ops_corpus_pipeline_run.sh (how ops scripts are invoked under automation,
      and the `set -euo pipefail` / logging conventions a new `.sh` here should match)
    - docs/operations/operations-database.md (DB access conventions; the `PGPASSWORD=postgres`
      requirement)
  </read_first>
  <action>
    Build a scratch database and rehearse against it. NEVER run `--challenger-concept` against
    live `indicagent` in this plan — a real registry write here would consume the concept's
    `corpus_build_ref` (invariant 2's re-evaluation guard blocks a repeat at the same ref), poisoning
    the first genuine promotion decision.

    Write `scripts/ops/alpha/ops_concept_registry_rehearsal.sh` (`set -euo pipefail`, structured
    echo progress, non-zero exit on any failed assertion) that:

    1. Drops and recreates `indicagent_rehearsal`:
         PGPASSWORD=postgres dropdb -U postgres -h localhost --if-exists indicagent_rehearsal
         PGPASSWORD=postgres createdb -U postgres -h localhost indicagent_rehearsal
    2. Loads the live schema: `pg_dump -s` from `indicagent` piped into the new DB. If the
       TimescaleDB extension/hypertable DDL fails to restore cleanly, fall back to
       `CREATE EXTENSION IF NOT EXISTS timescaledb;` first and re-run — record whatever was needed
       in the doc so the harness is reproducible.
    3. Copies the data the path reads and writes:
       `alpha_ensemble_ic`, `concept_registry`, `concept_gate`, `concept_transition_log`,
       `concept_annotation`, `config_state`, `config_schema`, `config_history`
       (`pg_dump -a -t <table>` per table into the scratch DB; keep the list in one array variable
       so it is greppable).
    4. Captures BEFORE counts of the three artefacts for the target concept.
    5. Runs a DRY RUN first — the same comparison WITHOUT `--challenger-concept` — and asserts it
       exits 0 and prints a stratum table. If the report-only path already fails on real data,
       stop here and record that: it means the defect is upstream of the registry path.
    6. Runs the REAL recording path against the scratch DB with
       `DATABASE_URL` (or whatever env var `Settings()` reads — confirm in
       `src/config/settings.py`) pointed at `indicagent_rehearsal`, targeting a `candidate`
       `domain='ensemble_strategy'` concept, with an explicit `--corpus-build-ref` distinct from
       the target's `last_eval_corpus_build_ref` (otherwise invariant 2 blocks it and the
       rehearsal proves nothing).
    7. Asserts each of the three artefacts individually, exiting non-zero on any miss:
         a. `concept_gate` eval cache moved: `last_eval_at`, `last_eval_n`,
            `last_eval_corpus_build_ref` changed for the target concept
         b. `concept_transition_log` gained a row IF and ONLY IF the printed action was `promote`
            (a `record_win` legitimately writes no transition row — assert the conditional, not an
            unconditional presence)
         c. `concept_annotation` gained exactly one `annotation_type='observation'`,
            `source='empirical'` row for the target concept — the insert that crashed in
            `6f1b4257`
    8. Prints the script's own `REGISTRY:` summary line and the observed process exit code.

    Run it. Then append a `## M-B live-data rehearsal` section to
    `docs/analysis/phase170-registry-live-data-rehearsal.md` recording: the exact command line,
    the target concept and its before/after status, the decision `action` returned, a PASS/FAIL
    line per artefact (a, b, c), the process exit code, and — importantly — ANY defect surfaced.
    If the rehearsal surfaces a bug, fix it in the same plan (that is the entire reason M-B
    exists) and re-run until clean, documenting the fix and its commit.

    Finally, drop the scratch DB or leave it and say which — do not leave an undocumented database
    behind on the server.
  </action>
  <verify>
    <automated>bash scripts/ops/alpha/ops_concept_registry_rehearsal.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/ops/alpha/ops_concept_registry_rehearsal.sh` exists, is committed, uses
      `set -euo pipefail`, and exits 0 on a clean rehearsal.
    - The script's target database is `indicagent_rehearsal` in every statement — `grep -c "\-d indicagent\b" scripts/ops/alpha/ops_concept_registry_rehearsal.sh`
      returns 0 except for the explicitly-labelled `pg_dump` source lines.
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM concept_annotation"`
      is UNCHANGED from before the plan — the rehearsal wrote nothing to live.
    - `docs/analysis/phase170-registry-live-data-rehearsal.md` contains a `## M-B live-data
      rehearsal` section with an explicit PASS/FAIL for each of the three artefacts and the
      observed exit code.
    - The `concept_annotation` assertion (c) is present and passed — this is the specific insert
      that crashed at runtime before, and a rehearsal that does not check it does not close M-B.
    - Any defect found is fixed, committed, and the rehearsal re-run clean; the doc names the
      commit.
  </acceptance_criteria>
  <done>The registry recording path has met real data end-to-end at least once, on a scratch
  database, with each of its three durable artefacts individually confirmed — so the first real
  promotion decision is not also the first execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rehearsal harness -> live indicagent | a rehearsal that accidentally targets production would burn the concept's corpus_build_ref and poison the first real promotion |
| scratch DB credentials -> server | `indicagent_rehearsal` is created with the same superuser credentials as production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-170-14 | Tampering | rehearsal writing to production | mitigate | every write statement targets `indicagent_rehearsal`; acceptance criteria assert live `concept_annotation` count is unchanged after the plan |
| T-170-15 | Denial of Service | premature execution on empty data | mitigate | Task 1's hard row-count gate aborts the plan rather than fabricating synthetic rows that would prove nothing |
| T-170-16 | Repudiation | undocumented rehearsal outcome | mitigate | per-artefact PASS/FAIL plus exit code recorded in a committed analysis doc, not just in a terminal scrollback |
| T-170-SC | Tampering | package installs | accept | no npm/pip/cargo installs in this plan |
</threat_model>

<verification>
1. `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM alpha_ensemble_ic"` > 0 (else the plan is correctly BLOCKED).
2. `bash scripts/ops/alpha/ops_concept_registry_rehearsal.sh` exits 0.
3. Live `indicagent` `concept_registry` / `concept_gate` / `concept_transition_log` /
   `concept_annotation` row counts are identical before and after this plan.
4. `docs/analysis/phase170-registry-live-data-rehearsal.md` carries both the H-1 verdict and the
   M-B per-artefact results.
</verification>

<success_criteria>
The data gate is enforced mechanically; the F3 floor has a measured viability verdict; and the
never-before-executed live recording path has run end-to-end against real data with every durable
artefact confirmed — before `ic_engine` is pointed at the same mechanism.
</success_criteria>

<output>
Create `.planning/phases/170-concept-registry-feature-domain-migration-feature-registry-r/170-05-SUMMARY.md` when done.
</output>
