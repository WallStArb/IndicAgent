---
phase: 170-concept-registry-feature-domain-migration-feature-registry-r
plan: 07
type: execute
wave: 6
depends_on: ["170-06"]
blocked_on_data: "Inherits Plan 05/06's gate — the writers must already be cut over and dual-writing before readers are repointed, so the two registries are provably in sync."
files_modified:
  - scripts/ops/alpha/ops_canary_integrity_assert.py
  - scripts/ops/alpha/ops_ensemble_ablation.py
  - scripts/ops/alpha/ops_ic_shrinkage.py
  - scripts/ops/alpha/ops_broadcast_feature_audit.py
  - scripts/ops/alpha/ops_lookahead_horizon_response.py
  - scripts/ops/alpha/ops_interaction_primitives_pilot.py
  - scripts/ops/alpha/ops_concept_registry_override.py
  - src/config/vocabulary_drift.py
  - tests/unit/test_ic_shrinkage_step.py
  - tests/integration/test_feature_vectors_schema.py
  - tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py
autonomous: true
requirements: [S-3, L-10]
user_setup: []

must_haves:
  truths:
    - "Every read-only consumer of feature_registry reads concept_registry (domain='feature') instead, with a demonstrated identical result set"
    - "The two control-canary scripts filter on concept_registry.is_control as a typed column, not a JSONB predicate"
    - "The operator lifecycle actuator writes through ConceptRegistryService's CAS path against the concept tables"
    - "Feature family (group_name) resolves from a dedicated column and tier from metadata JSONB, per todo 118's routing decision"
    - "No test asserts against feature_registry any more, so the Plan 08 DROP breaks nothing at collection time"
  artifacts:
    - path: "scripts/ops/alpha/ops_concept_registry_override.py"
      provides: "Operator lifecycle actuator on the concept tables, replacing ops_feature_registry_override.py"
      contains: "operator_override"
    - path: "src/config/vocabulary_drift.py"
      provides: "tier vocabulary namespace sourced from concept_registry metadata"
      contains: "concept_registry"
  key_links:
    - from: "ops_canary_integrity_assert.py"
      to: "concept_registry (domain='feature', is_control=true)"
      via: "JOIN on feature_ic_scores.feature_name = concept_registry.name"
      pattern: "is_control"
    - from: "ops_ic_shrinkage.py"
      to: "concept_registry.group_name"
      via: "peer-group lookup for leave-one-out shrinkage"
      pattern: "group_name"
---

<objective>
Repoint every remaining read-only consumer of `feature_registry` — eight scripts/modules and
three test modules — onto `concept_registry`, and replace the operator lifecycle actuator with a
concept-table equivalent.

Purpose: todo 118 scope item 3, reader half. Plan 06 moved the writers; these are the remaining
callers that would break the moment Plan 08 drops the table. They are repointed AFTER the writers
so that both registries are provably in sync (Plan 06's dual write plus parity assertion) while
the swap happens — a reader repointed before the writers would silently read a snapshot going
stale.

Output: eight source files repointed, one script renamed, three test modules updated.

## DATA GATE
Inherits Plan 05/06's gate. Task 1 re-asserts that
`.venv/bin/python scripts/ops/alpha/ops_concept_feature_migration_verify.py` prints
`VERDICT: PASS` before any edit. If it does not, the registries have diverged and repointing
readers would change behaviour — report BLOCKED and stop.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/todos/pending/118-migrate-feature-domain-into-concept-registry.md
@.planning/phases/170-concept-registry-feature-domain-migration-feature-registry-r/170-06-SUMMARY.md

<interfaces>
Complete inventory of remaining `feature_registry` readers, located this session by
`grep -rln "feature_registry" --include=*.py --include=*.sh src/ services/ scripts/ tests/`:

REAL QUERIES (must be repointed):
  scripts/ops/alpha/ops_canary_integrity_assert.py:81
      `_CANARY_ROWS_SQL`: selects `r.control_expectation` FROM feature_ic_scores s
       JOIN feature_registry r ON r.feature_name = s.feature_name
       WHERE r.is_control = true AND s.training_window_end = $1
  scripts/ops/alpha/ops_ensemble_ablation.py:328,336
      `_WEIGHTS_SQL`: JOIN feature_registry freg ON freg.feature_name = ew.feature_name,
           selecting `freg.group_name`
      `_FAMILIES_SQL = "SELECT DISTINCT group_name FROM feature_registry ORDER BY group_name"`
      also `_CONTROL_FAMILY = "control"` (line 100) — a group_name VALUE, unchanged by this work
  scripts/ops/alpha/ops_ic_shrinkage.py:100
      `_FEATURE_GROUP_SQL = "SELECT feature_name, group_name FROM feature_registry"`
      (peer-group source for leave-one-out shrinkage — a live corpus-pipeline step)
  scripts/ops/alpha/ops_broadcast_feature_audit.py:79
      `_FEATURE_REGISTRY_SQL = "SELECT feature_name, group_name, status FROM feature_registry"`
  scripts/ops/alpha/ops_lookahead_horizon_response.py:171
      `_FEATURE_REGISTRY_SQL = "SELECT feature_name, status FROM feature_registry"`
  scripts/ops/alpha/ops_interaction_primitives_pilot.py:135
      SELECT feature_name, parent_features FROM feature_registry
      WHERE tier = '1_interaction' AND status = 'active' ORDER BY feature_name
      plus an arity check requiring exactly 2 parents per row (raises ValueError otherwise)
  scripts/ops/alpha/ops_feature_registry_override.py (123 lines, whole file)
      operator actuator: reads SELECT status FROM feature_registry WHERE feature_name = %s,
      then calls FeatureRegistryService.record_transition_sync(...) with
      reason='operator_override'; this is the ONLY legitimate path to status='deprecated'
  src/config/vocabulary_drift.py:174
      `_UNWINDOWED_NAMESPACE_QUERIES["tier"] = "SELECT DISTINCT tier FROM feature_registry"`

TESTS (must be repointed):
  tests/unit/test_ic_shrinkage_step.py:102 — docstring/behaviour referencing group_name absence
  tests/integration/test_feature_vectors_schema.py:272-292 —
      `test_feature_registry_row_count_matches_gate` imports
      `feature_registry_service._REGISTRY_ROW_COUNT` and counts `feature_registry` rows
  tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py:1357 —
      SELECT feature_name FROM feature_registry WHERE added_phase = '165' (expects 41 rows)

COMMENT-ONLY references (update the prose on the named line, no code change, no scope expansion):
  src/intelligence/feature_factory.py:302,1766-1767
  src/intelligence/schemas.py:1562-1563
  src/intelligence/statistics/ic_math.py:717
  src/observability/metrics.py:1270 (metric description string)
  scripts/ops/corpus/ops_corpus_pipeline_run.sh:157 (echo text)
  scripts/analysis/ops_primitive_discovery_report.py:8 (module docstring)

Column mapping for every repoint:
  feature_registry.feature_name        -> concept_registry.name   (+ WHERE domain = 'feature')
  feature_registry.status              -> concept_registry.status
  feature_registry.group_name          -> concept_registry.group_name (dedicated column, migration 283)
  feature_registry.is_control          -> concept_registry.is_control (dedicated column)
  feature_registry.control_expectation -> concept_registry.control_expectation
  feature_registry.tier                -> concept_registry.metadata->>'tier'   (JSONB, per todo 118)
  feature_registry.parent_features     -> concept_parent join (below)
  feature_registry.added_phase         -> concept_registry.added_phase

Lineage query replacement (for ops_interaction_primitives_pilot.py):
    SELECT c.name AS feature_name,
           array_agg(p.name ORDER BY p.name) AS parent_features
    FROM concept_registry c
    JOIN concept_parent cp ON cp.child_concept_id = c.concept_id
    JOIN concept_registry p ON p.concept_id = cp.parent_concept_id
    WHERE c.domain = 'feature' AND c.metadata->>'tier' = '1_interaction'
      AND c.status = 'active'
    GROUP BY c.name
    ORDER BY c.name
  NOTE the ordering difference: `feature_registry.parent_features` is a TEXT[] in insertion
  order; `array_agg(... ORDER BY p.name)` is alphabetical. The pilot's downstream
  `partial_spearman_ic` treats the two parents as a symmetric control set, so order should not be
  load-bearing — CONFIRM that at the call site before relying on it.

Every consumer is read-only except `ops_feature_registry_override.py`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the four identity/family readers (canary, ablation, shrinkage, broadcast audit)</name>
  <files>scripts/ops/alpha/ops_canary_integrity_assert.py, scripts/ops/alpha/ops_ensemble_ablation.py, scripts/ops/alpha/ops_ic_shrinkage.py, scripts/ops/alpha/ops_broadcast_feature_audit.py</files>
  <read_first>
    - scripts/ops/alpha/ops_canary_integrity_assert.py (lines 1-100: the module docstring's
      explanation of what a control canary is, and lines 70-95 for `_CANARY_ROWS_SQL`)
    - scripts/ops/alpha/ops_ensemble_ablation.py (lines 1-110 for `_BASELINE_ARM` /
      `_CONTROL_FAMILY`, and lines 318-340 for `_WEIGHTS_SQL` / `_FAMILIES_SQL` including the
      comment asserting the registry-alignment gate guarantees exactly one row per feature_name —
      that guarantee now comes from the concept-registry alignment gate)
    - scripts/ops/alpha/ops_ic_shrinkage.py (lines 90-160: `_FEATURE_GROUP_SQL` and the
      "rows whose feature has no group_name mapping are skipped" behaviour that must not change)
    - scripts/ops/alpha/ops_broadcast_feature_audit.py (lines 70-90 and 190-210: the query and the
      report text that names the registry)
    - production/migrations/283_concept_registry_feature_domain_schema.sql (the dedicated
      group_name / is_control / control_expectation columns and why they are columns, not JSONB)
  </read_first>
  <action>
    GATE FIRST: run `.venv/bin/python scripts/ops/alpha/ops_concept_feature_migration_verify.py`.
    It must print `VERDICT: PASS`. If not, report BLOCKED and stop.

    For each of the four files: capture a BEFORE result set for the query being changed, make the
    edit, capture the AFTER result set, and assert they are identical. Do this with psql, not by
    inspection — these queries feed live measurement decisions, so a changed result set is a
    behaviour regression, not a cosmetic diff.

    (a) `ops_canary_integrity_assert.py` `_CANARY_ROWS_SQL` — replace the join with
        `JOIN concept_registry r ON r.name = s.feature_name AND r.domain = 'feature'` and keep
        `WHERE r.is_control = true AND s.training_window_end = $1`. `r.control_expectation` in the
        select list is unchanged (same column name). Update the module docstring's
        "(feature_registry.is_control=true rows, migration 223)" to
        "(concept_registry domain='feature', is_control=true rows; migrations 283/284)" and the
        "status='candidate' in feature_registry" reference likewise.

    (b) `ops_ensemble_ablation.py`:
        `_WEIGHTS_SQL` -> `JOIN concept_registry freg ON freg.name = ew.feature_name AND freg.domain = 'feature'`,
        still selecting `freg.group_name`.
        `_FAMILIES_SQL` -> `SELECT DISTINCT group_name FROM concept_registry WHERE domain = 'feature' AND group_name IS NOT NULL ORDER BY group_name`.
        The `IS NOT NULL` guard is NEW and necessary: `concept_registry.group_name` is nullable
        because `domain='ensemble_strategy'` rows have none; without it a NULL family would enter
        the ablation sweep. Update the `_CONTROL_FAMILY` / `_BASELINE_ARM` comments' table names;
        their VALUES do not change.

    (c) `ops_ic_shrinkage.py` `_FEATURE_GROUP_SQL` ->
        `SELECT name AS feature_name, group_name FROM concept_registry WHERE domain = 'feature'`.
        Aliasing to `feature_name` keeps every downstream dict key and the skip-when-unmapped
        behaviour byte-identical — do NOT rename the Python-side key.

    (d) `ops_broadcast_feature_audit.py` `_FEATURE_REGISTRY_SQL` ->
        `SELECT name AS feature_name, group_name, status FROM concept_registry WHERE domain = 'feature'`.
        Rename the constant to `_CONCEPT_REGISTRY_SQL` and update the two prose references
        (module docstring ~lines 19-28, report text ~line 200); the line-28 note about making the
        classification durable now points at `concept_registry.group_name`.

    Record all four BEFORE/AFTER comparisons in the plan SUMMARY, including the exact SQL used.
  </action>
  <verify>
    <automated>grep -L "FROM feature_registry\|JOIN feature_registry" scripts/ops/alpha/ops_canary_integrity_assert.py scripts/ops/alpha/ops_ensemble_ablation.py scripts/ops/alpha/ops_ic_shrinkage.py scripts/ops/alpha/ops_broadcast_feature_audit.py | wc -l | grep -qx 4</automated>
  </verify>
  <acceptance_criteria>
    - None of the four files contains `FROM feature_registry` or `JOIN feature_registry`.
    - Every repointed query carries `domain = 'feature'`.
    - `_FAMILIES_SQL` includes the `group_name IS NOT NULL` guard.
    - `ops_ic_shrinkage.py`'s query aliases `name AS feature_name`, so no downstream Python key
      changed.
    - BEFORE/AFTER result-set equality is demonstrated for all four queries, with the comparison
      SQL reproduced in the SUMMARY — a bare "verified" claim is not acceptable.
    - `.venv/bin/python scripts/ops/alpha/ops_canary_integrity_assert.py --help` and
      `.venv/bin/python scripts/ops/alpha/ops_ic_shrinkage.py --help` exit 0.
    - `.venv/bin/ruff check . --fix` and `.venv/bin/black .` clean.
  </acceptance_criteria>
  <done>The four scripts reading feature identity, control-canary flags and family labels read them
  from the concept tables, with result-set equality demonstrated per query.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the status/tier/lineage readers and replace the operator actuator</name>
  <files>scripts/ops/alpha/ops_lookahead_horizon_response.py, scripts/ops/alpha/ops_interaction_primitives_pilot.py, src/config/vocabulary_drift.py, scripts/ops/alpha/ops_concept_registry_override.py</files>
  <read_first>
    - scripts/ops/alpha/ops_lookahead_horizon_response.py (lines 55-70: the docstring naming the
      active-status filter; lines 165-180: `_FEATURE_REGISTRY_SQL`; line ~333: the --help text)
    - scripts/ops/alpha/ops_interaction_primitives_pilot.py (lines 120-160:
      `_load_interaction_features`, the exactly-2-parents arity validation and its rationale; plus
      the `partial_spearman_ic` call site that consumes `parents`, to confirm parent ORDER is not
      load-bearing before switching to an alphabetically-ordered `array_agg`)
    - src/config/vocabulary_drift.py (lines 140-200: `_UNWINDOWED_NAMESPACE_QUERIES` and the
      comment explaining why tier/asset_class are unwindowed — that rationale changes because
      `concept_registry` is a mixed-domain table)
    - scripts/ops/alpha/ops_feature_registry_override.py (all 123 lines — the file being replaced)
    - src/intelligence/concept_registry_service.py (Plan 04's `record_transition_sync` signature,
      the operator-only `deprecated` rule, and `_AUTOMATED_SYNC_REASONS`)
    - production/migrations/233_controlled_vocabulary_seed_namespaces.sql (how the `tier`
      namespace's registered vocabulary was seeded — the observed values must still match it)
  </read_first>
  <action>
    (a) `ops_lookahead_horizon_response.py` `_FEATURE_REGISTRY_SQL` ->
        `SELECT name AS feature_name, status FROM concept_registry WHERE domain = 'feature'`.
        Rename the constant to `_CONCEPT_REGISTRY_SQL`; update the docstring line naming
        `feature_registry.status = 'active'` and the `--help` text at ~line 333.

    (b) `ops_interaction_primitives_pilot.py` `_load_interaction_features` — replace the
        `parent_features` text-array read with the `concept_parent` join from the interfaces
        block. KEEP the exactly-2-parents `ValueError` check verbatim (it prevents a silent
        tuple-unpack corruption deep in the per-cell loop); update its message to name
        `concept_registry`/`concept_parent` and to note the arity now comes from FK-enforced edges
        rather than an unvalidated text array. Before switching, confirm at the
        `partial_spearman_ic` call site that parent ORDER is not load-bearing and record that
        finding in a code comment. If it IS load-bearing, preserve insertion order by joining
        through the original array `WITH ORDINALITY` instead — never silently change semantics.
        Also update the module docstring's `(feature_registry.tier='1_interaction')`.

    (c) `src/config/vocabulary_drift.py` — `_UNWINDOWED_NAMESPACE_QUERIES["tier"]` ->
        `SELECT DISTINCT metadata->>'tier' FROM concept_registry WHERE domain = 'feature' AND metadata ? 'tier'`.
        The `metadata ? 'tier'` guard prevents a NULL entering the observed vocabulary set from
        `domain='ensemble_strategy'` rows (no tier), which would otherwise make the drift audit
        report a spurious unregistered code every run. Update the surrounding comment: the source
        is no longer a ~150-row feature-only dimension table but a mixed-domain registry — state
        the row-count reasoning against `concept_registry`'s actual size and keep the conclusion
        (still not a DoS surface, still unwindowed).

    (d) Replace the operator actuator:
        `git mv scripts/ops/alpha/ops_feature_registry_override.py scripts/ops/alpha/ops_concept_registry_override.py`
        then rewrite it against the concept tables:
          - argparse: keep the existing flags, add `--domain` defaulting to `feature` (the
            actuator now sits on a cross-domain registry; do not hard-code one domain into it)
          - existence/status read: `SELECT status FROM concept_registry WHERE domain = %s AND name = %s`
          - write via `ConceptRegistryService.record_transition_sync(conn, domain=..., name=...,
            from_status=..., to_status=..., reason='operator_override', notes=<operator-supplied>)`
          - preserve all four existing behaviours, renaming their structlog event names from
            `ops_feature_registry_override.*` to `ops_concept_registry_override.*`:
            not-found, noop-already-at-target, optimistic-lock-miss, applied
          - keep the module docstring's warning that a manual SQL UPDATE bypasses the transition
            log, with table names updated
          - exit non-zero on not-found and on optimistic-lock-miss (Plan 02's L-3 discipline for
            governance actuators); exit 0 on noop-already-at-target
        Verify end-to-end WITHOUT mutating real state: run it against `indicagent_test`, or inside
        a psql transaction that is ROLLBACKed. Record the transcript in the SUMMARY.
  </action>
  <verify>
    <automated>test ! -f scripts/ops/alpha/ops_feature_registry_override.py &amp;&amp; grep -L "feature_registry" scripts/ops/alpha/ops_lookahead_horizon_response.py scripts/ops/alpha/ops_interaction_primitives_pilot.py src/config/vocabulary_drift.py scripts/ops/alpha/ops_concept_registry_override.py | wc -l | grep -qx 4 &amp;&amp; .venv/bin/pytest tests/unit/test_vocabulary_drift_audit.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/ops/alpha/ops_feature_registry_override.py` no longer exists;
      `scripts/ops/alpha/ops_concept_registry_override.py` does, and `git log --follow` shows the
      rename (use `git mv`, not delete-and-create).
    - `grep -c "feature_registry" <file>` == 0 for all four files in this task.
    - The interaction pilot's exactly-2-parents `ValueError` guard is still present and still
      raises before any DB fetch work.
    - The `tier` vocabulary query includes the `metadata ? 'tier'` guard, and
      `SELECT DISTINCT metadata->>'tier' FROM concept_registry WHERE domain='feature' AND metadata ? 'tier'`
      returns exactly the same three values as `SELECT DISTINCT tier FROM feature_registry`
      (`0_atomic`, `1_interaction`, `2_theory`) — record both result sets in the SUMMARY.
    - `.venv/bin/pytest tests/unit/test_vocabulary_drift_audit.py -q` passes.
    - The actuator transcript (dry/rolled-back run) is recorded in the SUMMARY, showing a
      successful transition and a non-zero exit on an unknown name.
  </acceptance_criteria>
  <done>Status, tier and lineage readers resolve from the concept tables, and the only operator
  write path into feature lifecycle governance is a concept-registry actuator with CAS semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint the three test modules and sweep comment-only references</name>
  <files>tests/unit/test_ic_shrinkage_step.py, tests/integration/test_feature_vectors_schema.py, tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py</files>
  <read_first>
    - tests/integration/test_feature_vectors_schema.py lines 260-300
      (`test_feature_registry_row_count_matches_gate` — what it asserts and why; note it imports
      `_REGISTRY_ROW_COUNT` from `feature_registry_service`, a module deleted in Plan 08)
    - tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py lines 1330-1370 (the
      Phase 165 field-set cross-check and its `added_phase = '165'` query expecting 41 rows)
    - tests/unit/test_ic_shrinkage_step.py lines 90-115 (the no-group-mapping skip behaviour)
    - src/intelligence/schemas.py lines 1555-1570 and src/intelligence/feature_factory.py
      lines 295-310 and 1760-1775 (the comment-only references to update)
    - src/observability/metrics.py line ~1270 (the `ALPHA_DECAY_ENSEMBLE_REBUILD_TOTAL`
      description string)
  </read_first>
  <action>
    Tests:

    (a) `tests/integration/test_feature_vectors_schema.py` — rename
        `test_feature_registry_row_count_matches_gate` to
        `test_concept_registry_feature_row_count_matches_gate`; replace the
        `feature_registry_service._REGISTRY_ROW_COUNT` import with a locally-derived
        `len(dataclasses.fields(FeatureVector))` (the same derivation, without importing a module
        that Plan 08 deletes — and derived, never hard-coded, for the same anti-drift reason the
        original documents); count
        `SELECT COUNT(*) FROM concept_registry WHERE domain = 'feature'`. Keep the assertion
        message's remediation hint, updated to name migration 284.

    (b) `tests/unit/intelligence/test_swing_fib_trend_structure_primitives.py` — repoint the
        `added_phase = '165'` query to
        `SELECT name FROM concept_registry WHERE domain = 'feature' AND added_phase = '165'`.
        The expected count (41) and every assertion message stay.

    (c) `tests/unit/test_ic_shrinkage_step.py` — update the docstring/comment naming
        `feature_registry.group_name` to `concept_registry.group_name`; the behaviour under test
        (a feature with no group mapping cannot form a peer group and is skipped) is unchanged.

    Comment sweep — prose only, one line each, no code change and no scope expansion beyond the
    named lines: `src/intelligence/feature_factory.py` (302, 1766-1767),
    `src/intelligence/schemas.py` (1562-1563), `src/intelligence/statistics/ic_math.py` (717),
    `src/observability/metrics.py` (1270), `scripts/ops/corpus/ops_corpus_pipeline_run.sh` (157),
    `scripts/analysis/ops_primitive_discovery_report.py` (8). Each should say
    `concept_registry (domain='feature')` where it currently says `feature_registry`.

    For `src/observability/metrics.py:1270` specifically: it is the description of
    `ALPHA_DECAY_ENSEMBLE_REBUILD_TOTAL`, "Count of real feature_registry transitions
    (active->shadow_only demotion, ...)". Change the table name in the description only — do NOT
    rename the metric itself. A metric rename breaks existing Grafana panels and Prometheus
    history for zero governance benefit, and the metric name carries no registry table name.

    After the sweep, the only remaining `feature_registry` references anywhere outside
    `production/migrations/`, `docs/`, `.planning/` and
    `tests/integration/fixtures/schema_baseline_2026-07-18.sql` must be:
      - `src/intelligence/feature_registry_service.py` (deleted in Plan 08)
      - `tests/unit/intelligence/test_feature_registry_service.py` (deleted in Plan 08)
      - `services/ic_engine.py`'s retained dual-WRITE import and calls (removed in Plan 08)
    Enumerate the actual remaining set in the SUMMARY and confirm it matches that list exactly.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "feature_registry" --include=*.py --include=*.sh src/ services/ scripts/ tests/`
      returns EXACTLY: `src/intelligence/feature_registry_service.py`,
      `tests/unit/intelligence/test_feature_registry_service.py`, `services/ic_engine.py`.
      Any fourth path is an unfinished repoint.
    - `test_concept_registry_feature_row_count_matches_gate` exists and passes; no test imports
      `src.intelligence.feature_registry_service` except
      `tests/unit/intelligence/test_feature_registry_service.py`.
    - The Phase 165 cross-check still asserts 41 rows, now against `concept_registry`.
    - `ALPHA_DECAY_ENSEMBLE_REBUILD_TOTAL`'s metric NAME is unchanged (`git diff` shows only the
      description string moved).
    - `.venv/bin/pytest tests/unit/ -q` fully green;
      `.venv/bin/pytest tests/integration/test_feature_vectors_schema.py -m integration -q` passes.
  </acceptance_criteria>
  <done>No test and no comment outside the three files Plan 08 deletes still refers to
  `feature_registry`, so the DROP cannot break test collection or leave stale prose behind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI -> concept_registry.status | `ops_concept_registry_override.py` is the only sanctioned path to `status='deprecated'` |
| repointed measurement queries -> live verdicts | `ops_canary_integrity_assert.py` and `ops_ic_shrinkage.py` gate real corpus-pipeline decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-170-20 | Tampering | silently changed result sets | mitigate | every repointed query is BEFORE/AFTER diffed against its predecessor with the comparison SQL recorded, not eyeballed |
| T-170-21 | Elevation of Privilege | operator actuator | mitigate | the rewritten actuator writes only through `ConceptRegistryService.record_transition_sync`, inheriting the CAS optimistic lock and the operator-only `deprecated` rule; a raw SQL UPDATE path is documented as forbidden |
| T-170-22 | Information Disclosure (noise) | vocabulary drift audit | mitigate | `metadata ? 'tier'` guard prevents cross-domain NULLs entering the observed vocabulary and generating false drift alerts every run |
| T-170-SC | Tampering | package installs | accept | no npm/pip/cargo installs in this plan |
</threat_model>

<verification>
1. `grep -rl "feature_registry" --include=*.py --include=*.sh src/ services/ scripts/ tests/`
   returns exactly the three expected paths.
2. `.venv/bin/pytest tests/unit/ -q` fully green.
3. `.venv/bin/python scripts/ops/alpha/ops_concept_feature_migration_verify.py` -> `VERDICT: PASS`.
4. `.venv/bin/ruff check . --fix` and `.venv/bin/black .` clean.
</verification>

<success_criteria>
Every consumer of the retiring registry reads the concept tables, with demonstrated result-set
equality; the operator actuator writes through the CAS path; and nothing outside the three files
Plan 08 deletes still references `feature_registry`.
</success_criteria>

<output>
Create `.planning/phases/170-concept-registry-feature-domain-migration-feature-registry-r/170-07-SUMMARY.md` when done.
</output>
