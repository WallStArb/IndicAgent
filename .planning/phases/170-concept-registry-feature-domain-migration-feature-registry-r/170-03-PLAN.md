---
phase: 170-concept-registry-feature-domain-migration-feature-registry-r
plan: 03
type: execute
wave: 2
depends_on: ["170-01"]
files_modified:
  - production/migrations/284_concept_registry_feature_domain_seed.sql
  - scripts/ops/alpha/ops_concept_feature_migration_verify.py
autonomous: true
requirements: [S-1, L-7]
user_setup: []

must_haves:
  truths:
    - "Every feature_registry row has exactly one corresponding concept_registry row with domain='feature', identical name and identical status"
    - "Every feature_registry gate value (min_ic_sharpe, min_ic_n, fdr_required, fdr_alpha) and every live recovery counter is preserved on the corresponding concept_gate row"
    - "Every multi-parent lineage edge is preserved as an FK-enforced concept_parent row, with a hard failure if any parent name failed to resolve"
    - "Every feature_transition_log row is replayed into concept_transition_log with its original timestamp and a mapped trigger_reason — nothing is left pointing back at a table that is about to be dropped"
    - "A re-runnable verification script proves the parity above and exits non-zero on any single mismatch"
  artifacts:
    - path: "production/migrations/284_concept_registry_feature_domain_seed.sql"
      provides: "The domain='feature' fold-in: registry rows, gate rows, lineage edges, genesis transitions, and full feature_transition_log replay"
      contains: "genesis_seed"
    - path: "scripts/ops/alpha/ops_concept_feature_migration_verify.py"
      provides: "Re-runnable feature_registry <-> concept_registry parity assertion, non-zero exit on mismatch"
      contains: "def main"
  key_links:
    - from: "feature_registry"
      to: "concept_registry (domain='feature')"
      via: "migration 284 INSERT ... SELECT"
      pattern: "FROM feature_registry"
    - from: "feature_transition_log"
      to: "concept_transition_log"
      via: "replay with trigger_reason mapping"
      pattern: "WHEN 'ic_promotion' THEN 'promotion'"
    - from: "feature_registry.parent_features"
      to: "concept_parent"
      via: "unnest + name resolution to concept_id"
      pattern: "unnest"
---

<objective>
Fold `domain='feature'` into the Concept Registry: 249 registry rows, 249 gate rows, 16 lineage
edges, 249 genesis transitions, and a full replay of `feature_transition_log` — plus a
re-runnable parity verifier.

Purpose: todo 118 scope item 1. This is the one-way-door step. `feature_registry` and
`feature_transition_log` are DROPped in Plan 08, so anything not carried across here is
destroyed. Todo 118 explicitly rules out the lighter "genesis-seed with a pointer back"
alternative for exactly this reason: a pointer to a table that is about to be dropped is a
dangling reference, not a preservation path.

Output: migration `284_concept_registry_feature_domain_seed.sql` applied to live `indicagent`,
and `scripts/ops/alpha/ops_concept_feature_migration_verify.py` proving parity.

Scope guard: this plan writes DATA only. It changes zero application code — `feature_registry`
remains the sole authority for the live lifecycle until Plan 06 cuts the writers over. The
concept rows seeded here are, until then, a faithful snapshot, not a second writer.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/todos/pending/118-migrate-feature-domain-into-concept-registry.md
@.planning/phases/170-concept-registry-feature-domain-migration-feature-registry-r/170-01-SUMMARY.md

<interfaces>
Live data profile, measured this session against `indicagent` — assert these, do not assume they
are still true at execution time:

  SELECT count(*) FROM feature_registry;                          -> 249
  status/tier breakdown: active/0_atomic 130, active/1_interaction 8,
                         active/2_theory 106, candidate/0_atomic 5
  rows with parent_features non-NULL                              -> 8  (each with exactly 2 parents = 16 edges)
  rows with is_control = true                                     -> 5
  DISTINCT control_expectation WHERE is_control                   -> 'negative_control', 'positive_control'
  rows with consecutive_shadow_passes > 0                         -> 0
  rows with observations_since_demotion > 0                       -> 0
  SELECT count(*) FROM feature_transition_log;                    -> 2  (both trigger_reason='operator_override')
  SELECT count(*) FROM concept_registry;                          -> 5  (all domain='ensemble_strategy')

  The 8 multi-parent rows and their parents:
    vol_body_product     <- {body_ratio, volume_z}
    ret_vol_product_fast <- {ret_lag_fast, volume_z}
    price_vol_corr_fast  <- {ret_lag_1, volume_z}
    price_vol_corr_slow  <- {ret_lag_1, volume_z}
    range_vol_product    <- {range_vs_atr, volume_z}
    up_vol_body_diff     <- {up_vol_ratio_fast, body_ratio}
    ret_vol_ratio_fast   <- {ret_lag_fast, atr_z}
    vol_skew_product     <- {ret_skew_z, volume_z}

`feature_registry` columns (all 27), for the field-by-field mapping:
  feature_name, group_name, tier, formula_short, normalization, linear_ready, source_dims,
  requires_htf, window_apr_keys, parent_features, status, min_ic_sharpe, min_ic_n,
  fdr_required, fdr_alpha, last_ic_value, last_ic_sharpe, last_ic_n, last_eval_at,
  added_phase, notes, added_at, consecutive_shadow_passes, observations_since_demotion,
  is_control, control_expectation

`feature_transition_log` columns:
  id, feature_name, from_status, to_status, triggered_at, trigger_reason, ic_value, ic_sharpe, ic_n
  trigger_reason CHECK: ('ic_promotion','ic_demotion','parent_cascade','operator_override')

`concept_transition_log` trigger_reason CHECK (no 'ic_*' values — mapping is mandatory):
  ('promotion','demotion_performance','demotion_decay','demotion_redundancy','operator_override',
   'parent_cascade','candidate_timeout','implementation_change','genesis_seed')

Columns added by Plan 01's migration 283 (available here):
  concept_registry.is_control, .control_expectation, .group_name
  concept_gate.consecutive_shadow_passes, .observations_since_demotion
  concept_parent(child_concept_id, parent_concept_id, created_at)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and apply migration 284 (seed domain='feature' + replay the transition log)</name>
  <files>production/migrations/284_concept_registry_feature_domain_seed.sql</files>
  <read_first>
    - .planning/todos/pending/118-migrate-feature-domain-into-concept-registry.md — scope item 1,
      scope item 4's "must actually replay, not genesis-seed-with-a-pointer-back" mandate, the
      "Everything else folds into metadata JSONB" list, and L-7's shared-fingerprint contract
    - production/migrations/225_concept_registry_mvp.sql (in full — in particular the
      `domain='ensemble_strategy'` seed block: its `genesis_seed` transition rows are the
      convention this seed must mirror, including which `from_status` value it uses)
    - production/migrations/283_concept_registry_feature_domain_schema.sql (Plan 01's output —
      the exact column names and the cascade trigger this seed must not accidentally fire)
    - production/migrations/169_feature_registry.sql and
      production/migrations/209_feature_registry_lifecycle_columns.sql (original semantics of
      every column being mapped, especially the lifecycle counters)
  </read_first>
  <action>
    Confirm the number is free (`ls production/migrations/ | sort -V | tail -3`); expect 283 last
    from Plan 01, so use 284.

    Write `production/migrations/284_concept_registry_feature_domain_seed.sql`. Header comment
    block must state: Phase 170, todo 118 scope item 1; that this migration is DATA-only and
    changes no schema; the explicit field-by-field mapping table below; and L-7's contract note
    verbatim — that `concept_transition_log` provenance keys off the same fingerprint tuple
    (`code_content_key` / `apr_snapshot_key` / `upstream_watermark`) that `ic_engine` uses, so
    todo 252's eventual fix must reuse it rather than invent a second versioning scheme.
    (Recording the contract is in scope; implementing todo 252 is NOT.)

    Field mapping to implement, in one BEGIN/COMMIT, in this order:

    (1) `concept_registry` rows:
        domain              <- 'feature'
        name                <- fr.feature_name
        description         <- fr.formula_short   (NOT NULL on concept_registry; formula_short is
                                                   NOT NULL on feature_registry, so this is safe)
        status              <- fr.status
        enabled             <- (fr.status = 'active')
        group_name          <- fr.group_name
        is_control          <- fr.is_control
        control_expectation <- fr.control_expectation
        added_phase         <- fr.added_phase
        created_at          <- fr.added_at
        parent_concept_id   <- NULL  (superseded by concept_parent; see step 3)
        redundancy_group    <- NULL  (feature_registry has no equivalent; the canonical doc's
                                      "only one holds active" displacement rule must NOT be
                                      switched on for this domain by accident)
        sensitivity         <- NULL
        metadata            <- jsonb_strip_nulls(jsonb_build_object(
                                   'tier', fr.tier,
                                   'formula_short', fr.formula_short,
                                   'normalization', fr.normalization,
                                   'linear_ready', fr.linear_ready,
                                   'requires_htf', fr.requires_htf,
                                   'window_apr_keys', to_jsonb(fr.window_apr_keys),
                                   'source_dims', to_jsonb(fr.source_dims),
                                   'notes', fr.notes,
                                   'apr_namespace', 'feature.',
                                   'migrated_from', 'feature_registry',
                                   'migrated_by', 'migration_284'))
        `ON CONFLICT (domain, name) DO NOTHING` for re-runnability.
        Comment the `enabled` decision explicitly: `feature_registry` had no `enabled` column —
        `status='active'` WAS the enabling condition. Seeding `enabled = (status='active')` keeps
        the two consistent, and Plan 04's sync transition path maintains that invariant on every
        write. Seeding `false` uniformly would render 138 active features as disabled to any
        human or query reading the table, which is worse than the redundancy.
        Do NOT create a dedicated `tier` column — todo 118 explicitly routes `tier` to metadata.

    (2) `concept_gate` rows (one per seeded concept):
        gate_metric_name            <- 'ic_sharpe_hac'   (the metric feature_registry.min_ic_sharpe gates)
        gate_eval_method            <- 'bootstrap_ci'    (must be one of the CHECK's three values)
        min_gate_metric             <- fr.min_ic_sharpe
        min_gate_n                  <- fr.min_ic_n
        fdr_required                <- fr.fdr_required
        fdr_alpha                   <- fr.fdr_alpha
        last_eval_metric            <- fr.last_ic_sharpe
        last_eval_n                 <- fr.last_ic_n
        last_eval_at                <- fr.last_eval_at
        consecutive_shadow_passes   <- fr.consecutive_shadow_passes
        observations_since_demotion <- fr.observations_since_demotion
        min_promotion_consecutive / min_new_observations / demotion_threshold / decay_floor /
        regime_scope / baseline_metric / decay_ratio    <- leave NULL (inherit APR defaults)
        `fr.last_ic_value` is DELIBERATELY DROPPED — todo 118 states this is a decided consequence
        of the measurement/governance/reporting split (full IC richness stays in
        `feature_ic_scores`), not an oversight. Say so in a comment so nobody "restores" it later.
        Join `concept_registry cr ON cr.domain='feature' AND cr.name = fr.feature_name`.
        `ON CONFLICT (concept_id) DO NOTHING`.

    (3) `concept_parent` edges from `feature_registry.parent_features`:
        INSERT INTO concept_parent (child_concept_id, parent_concept_id)
        SELECT child.concept_id, parent.concept_id
        FROM feature_registry fr
        CROSS JOIN LATERAL unnest(fr.parent_features) AS pf(parent_name)
        JOIN concept_registry child  ON child.domain='feature'  AND child.name = fr.feature_name
        JOIN concept_registry parent ON parent.domain='feature' AND parent.name = pf.parent_name
        WHERE fr.parent_features IS NOT NULL
        ON CONFLICT DO NOTHING;
        Then a hard integrity assertion in the same transaction — this is the whole point of L-8's
        FK-enforced join table, so an unresolved parent name must abort the migration, not warn:
        DO $$
        DECLARE expected INT; actual INT;
        BEGIN
            SELECT COALESCE(sum(array_length(parent_features,1)),0) INTO expected FROM feature_registry;
            SELECT count(*) INTO actual FROM concept_parent;
            IF expected <> actual THEN
                RAISE EXCEPTION 'concept_parent edge count mismatch: expected %, got % -- an unresolved parent name would silently orphan lineage', expected, actual;
            END IF;
        END $$;
        (`expected` is 16 today. Compute it, do not hard-code it.)

    (4) `concept_transition_log` genesis rows, one per seeded concept, stamped at `fr.added_at`
        (NOT now()) so the log reads chronologically as genesis-then-later-transitions:
        trigger_reason = 'genesis_seed', to_status = fr.status,
        from_status = <mirror exactly what migration 225's ensemble_strategy genesis block used>,
        notes = 'Migrated from feature_registry (Phase 170, todo 118). Establishes the incumbent
                 status at migration time without fabricating a promotion event.'

    (5) `feature_transition_log` REPLAY — every row, with its original `triggered_at`:
        trigger_reason CASE mapping (exhaustive over feature_transition_log's CHECK):
            'ic_promotion'      -> 'promotion'
            'ic_demotion'       -> 'demotion_performance'
            'parent_cascade'    -> 'parent_cascade'
            'operator_override' -> 'operator_override'
        metric mapping: gate_metric <- ftl.ic_sharpe, gate_n <- ftl.ic_n, ci_lower <- ftl.ic_value
        notes = 'Replayed from feature_transition_log id=' || ftl.id || ' (Phase 170, todo 118).'
        Followed by a second hard assertion in the same transaction:
        DO $$ ... IF (SELECT count(*) FROM feature_transition_log)
                 <> (SELECT count(*) FROM concept_transition_log
                     WHERE domain='feature' AND notes LIKE 'Replayed from feature_transition_log%')
               THEN RAISE EXCEPTION 'transition-log replay incomplete: % source rows vs % replayed --
                    a feature present in the log but absent from the registry would silently lose
                    its history, and feature_transition_log is about to be dropped', ...;
           END IF; END $$;
        Make the CASE expression have NO `ELSE NULL` fallthrough — an unmapped reason must violate
        the NOT NULL / CHECK and abort, not insert a NULL.

    Guard against accidental trigger fire: none of these statements UPDATE
    `concept_registry.status`, so `trg_cascade_concept_parent_deprecation` cannot fire during the
    seed. Confirm this by checking `concept_transition_log` for `trigger_reason='parent_cascade'`
    rows created by this migration (expected: 0).

    Apply:
      PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent \
        -f production/migrations/284_concept_registry_feature_domain_seed.sql
    Any ERROR means the migration is wrong — fix the file and re-apply. Never hand-patch live.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT (SELECT count(*) FROM concept_registry WHERE domain='feature') = (SELECT count(*) FROM feature_registry) AND (SELECT count(*) FROM concept_gate g JOIN concept_registry r USING (concept_id) WHERE r.domain='feature') = (SELECT count(*) FROM feature_registry) AND (SELECT count(*) FROM concept_parent) = (SELECT COALESCE(sum(array_length(parent_features,1)),0) FROM feature_registry) AND (SELECT count(*) FROM concept_transition_log WHERE domain='feature' AND trigger_reason='genesis_seed') = (SELECT count(*) FROM feature_registry)" | grep -qx t</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT count(*) FROM concept_registry WHERE domain='feature'` equals
      `SELECT count(*) FROM feature_registry` (249 at time of writing — assert equality, not the
      literal).
    - `SELECT count(*) FROM concept_registry WHERE domain='ensemble_strategy'` is still 5.
    - Zero status mismatches:
      `SELECT count(*) FROM feature_registry fr JOIN concept_registry cr ON cr.domain='feature' AND cr.name=fr.feature_name WHERE cr.status IS DISTINCT FROM fr.status` == 0.
    - Zero enabled-invariant violations:
      `SELECT count(*) FROM concept_registry WHERE domain='feature' AND enabled <> (status='active')` == 0.
    - `SELECT count(*) FROM concept_parent` == 16 and equals
      `SELECT sum(array_length(parent_features,1)) FROM feature_registry`.
    - `SELECT count(*) FROM concept_registry WHERE domain='feature' AND is_control` == 5, and the
      5 names match `SELECT feature_name FROM feature_registry WHERE is_control` exactly.
    - `SELECT count(*) FROM concept_transition_log WHERE domain='feature' AND trigger_reason='genesis_seed'` == 249.
    - `SELECT count(*) FROM concept_transition_log WHERE notes LIKE 'Replayed from feature_transition_log%'`
      equals `SELECT count(*) FROM feature_transition_log` (2 today).
    - `SELECT count(*) FROM concept_transition_log WHERE domain='feature' AND trigger_reason='parent_cascade'` == 0
      (no accidental cascade fired during the seed).
    - `SELECT count(*) FROM concept_registry WHERE domain='feature' AND metadata->>'tier' IS NULL` == 0.
    - The migration file is committed and re-running it is a clean no-op (re-apply it once and
      confirm every count above is unchanged).
  </acceptance_criteria>
  <done>All 249 features, their gates, their 16 lineage edges, their genesis rows, and the full
  `feature_transition_log` history exist under `domain='feature'` in the concept tables, with
  hard in-migration assertions that would have aborted on any silent loss.</done>
</task>

<task type="auto">
  <name>Task 2: Write the re-runnable feature_registry <-> concept_registry parity verifier</name>
  <files>scripts/ops/alpha/ops_concept_feature_migration_verify.py</files>
  <read_first>
    - production/migrations/284_concept_registry_feature_domain_seed.sql (the file just written —
      the mapping this script must independently re-check)
    - scripts/ops/alpha/ops_canary_integrity_assert.py (script shape for this family: shebang,
      `project_root` sys.path insert, argparse, structlog setup, printed markdown-ish summary
      block, and — critically — its assert-and-exit-non-zero convention)
    - src/core/database_manager.py (`_setup_codecs` — a bare `asyncpg.connect()` has no JSONB
      codec registered; this script reads `concept_registry.metadata`, so it must either use the
      pooled path or call `_setup_codecs(conn)` explicitly, per CLAUDE.md's asyncpg gotcha)
    - src/config/settings.py (`Settings().database_url` needs
      `.replace("postgresql+asyncpg://", "postgresql://")` before asyncpg)
  </read_first>
  <action>
    Write `scripts/ops/alpha/ops_concept_feature_migration_verify.py`: a read-only script that
    asserts full parity between `feature_registry` and `concept_registry`/`concept_gate`/
    `concept_parent` and exits 1 on ANY mismatch, 0 only on total agreement. It exists so the
    parity claim is a command anyone can re-run, not a one-time eyeball at migration time — Plan
    06 and Plan 08 both re-run it as a gate.

    Checks, each reporting its own PASS/FAIL line and each contributing to the exit code:
      1. row-count parity: count(feature_registry) == count(concept_registry WHERE domain='feature')
      2. name-set parity: the two name sets are exactly equal (report the symmetric difference on
         failure, not just a count)
      3. per-feature status parity (zero rows where status differs)
      4. enabled invariant: `enabled = (status='active')` for every feature row
      5. gate parity: min_gate_metric/min_gate_n/fdr_required/fdr_alpha equal the corresponding
         min_ic_sharpe/min_ic_n/fdr_required/fdr_alpha, and consecutive_shadow_passes /
         observations_since_demotion match — use `IS DISTINCT FROM` so NULLs compare correctly
      6. lineage parity: for every feature with parent_features, the set of
         `concept_parent` parent names equals the set in the text array (rebuild both sides and
         compare as sets, not counts — a count check would pass on a swapped pair)
      7. control parity: is_control and control_expectation match row-for-row
      8. group_name parity: matches row-for-row
      9. metadata completeness: every feature row has non-NULL `metadata->>'tier'`,
         `metadata->>'normalization'`, and `metadata->>'formula_short'`
     10. transition-log replay completeness: count(feature_transition_log) ==
         count(concept_transition_log WHERE notes LIKE 'Replayed from feature_transition_log%'),
         and for each source row a replayed row exists with the same (name, from_status,
         to_status, triggered_at)

    Add `--json` to emit a machine-readable result dict alongside the human summary (Plan 06 and
    Plan 08 consume it programmatically). Print a final `VERDICT: PASS` / `VERDICT: FAIL (n
    checks failed)` line and `sys.exit(0 if ok else 1)`.

    The script must gracefully report — not crash — when `feature_registry` no longer exists
    (`to_regclass('public.feature_registry') IS NULL`): print
    `SKIPPED: feature_registry has been dropped (Phase 170 Plan 08 complete); parity is no longer
    checkable` and exit 0. Without this, the script becomes a permanent broken command the moment
    Plan 08 lands.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/ops/alpha/ops_concept_feature_migration_verify.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python scripts/ops/alpha/ops_concept_feature_migration_verify.py` prints
      `VERDICT: PASS` and exits 0 against the live database after migration 284.
    - All ten numbered checks appear in the output with an explicit PASS/FAIL each.
    - Negative control: temporarily flipping one concept row's status in a transaction that is
      then ROLLBACKed (e.g. via `psql -c "BEGIN; UPDATE concept_registry SET status='deprecated'
      WHERE domain='feature' AND name='volume_z'; \! .venv/bin/python scripts/ops/alpha/ops_concept_feature_migration_verify.py; ROLLBACK;"`)
      makes the script exit 1 — a verifier that cannot fail is not a verifier. Record the observed
      exit code in the plan SUMMARY.
    - `--json` emits parseable JSON.
    - `.venv/bin/ruff check . --fix` and `.venv/bin/black .` clean.
  </acceptance_criteria>
  <done>Parity between the retiring registry and its replacement is a re-runnable, self-failing
  command, proven to fail on an injected mismatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration 284 -> live governance data | a one-way-door data copy immediately preceding a DROP TABLE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-170-07 | Information Disclosure (loss) | lineage + transition history copy | mitigate | two in-migration `RAISE EXCEPTION` assertions abort the whole transaction on any edge-count or replay-count shortfall, so a partial copy can never commit |
| T-170-08 | Tampering | trigger_reason CASE mapping | mitigate | no `ELSE` branch — an unmapped source reason violates NOT NULL/CHECK and aborts, rather than inserting a NULL that reads as "no reason recorded" |
| T-170-09 | Repudiation | genesis vs replayed rows | mitigate | genesis rows stamped at `added_at` and replayed rows at their original `triggered_at`, so the audit trail's chronology survives the migration instead of collapsing to migration time |
| T-170-SC | Tampering | package installs | accept | no npm/pip/cargo installs in this plan |
</threat_model>

<verification>
1. `.venv/bin/python scripts/ops/alpha/ops_concept_feature_migration_verify.py` -> `VERDICT: PASS`, exit 0.
2. Re-applying migration 284 is a clean no-op (every count unchanged).
3. `.venv/bin/pytest tests/unit/ -q` green (nothing in the application layer reads these rows
   yet, so any failure is a real regression).
4. `SELECT count(*) FROM concept_registry WHERE domain='ensemble_strategy'` == 5 — the pre-existing
   domain is untouched.
</verification>

<success_criteria>
`domain='feature'` is fully materialised in the concept tables with provable, re-checkable parity
against `feature_registry`, including the multi-parent lineage and the complete transition
history — the preconditions for dropping the old tables without losing anything.
</success_criteria>

<output>
Create `.planning/phases/170-concept-registry-feature-domain-migration-feature-registry-r/170-03-SUMMARY.md` when done.
</output>
