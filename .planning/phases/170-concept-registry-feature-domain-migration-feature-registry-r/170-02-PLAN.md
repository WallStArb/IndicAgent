---
phase: 170-concept-registry-feature-domain-migration-feature-registry-r
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/concept_registry_service.py
  - scripts/ops/alpha/ops_ensemble_weight_compare.py
  - tests/unit/test_concept_registry_service.py
  - tests/unit/test_ensemble_weight_compare.py
autonomous: true
requirements: [L-2, L-3, L-4, L-6]
user_setup: []

must_haves:
  truths:
    - "A promotion cannot be written for a concept whose gate requires FDR unless the caller proves multiplicity correction was applied — enforced by the service, not by caller discipline"
    - "Every REGISTRY: FAILED path exits non-zero, so an automated corpus-pipeline invocation cannot mistake a registry failure for success"
    - "An operator-supplied --challenger-concept that does not exist is rejected before any registry write, with a non-zero exit"
    - "The gate-cache read-modify-write is provably inside the FOR UPDATE transaction, with a regression test that fails if that lock is ever removed"
  artifacts:
    - path: "src/intelligence/concept_registry_service.py"
      provides: "fail-closed FDR enforcement inside record_comparison_outcome"
      contains: "blocked_fdr_unverified"
    - path: "scripts/ops/alpha/ops_ensemble_weight_compare.py"
      provides: "non-zero exits on every REGISTRY failure + challenger-concept existence validation"
      contains: "unknown challenger concept"
  key_links:
    - from: "ops_ensemble_weight_compare.py"
      to: "ConceptRegistryService.record_comparison_outcome"
      via: "explicit fdr_passed argument"
      pattern: "fdr_passed="
    - from: "concept_gate.fdr_required"
      to: "record_comparison_outcome decision core"
      via: "_LOAD_CONCEPT_SQL now selects g.fdr_required"
      pattern: "fdr_required"
---

<objective>
Close the three Phase 160 registry-mechanism hardening items todo 118 says must land "before
automating the promotion path" — because this phase IS that automation — and verify the fourth
(L-2) is genuinely already satisfied rather than assumed.

Purpose: after this phase, `ic_engine` becomes a second automated writer against the concept
tables. Today `ConceptRegistryService`'s own docstring states FDR enforcement "lives entirely
upstream in ops_ensemble_weight_compare.py" — a documented assumption, not an enforced
invariant. A second caller that forgets it silently promotes without multiplicity correction
(todo 118 L-6).

Output: fail-closed FDR enforcement inside the service; non-zero exits on every REGISTRY failure
path (L-3); challenger-concept validation (L-4); an L-2 lock-regression test.

Scope guard: this plan touches ONLY `domain='ensemble_strategy'` behaviour and the generic
service. It seeds no rows, adds no schema, and does not touch `ic_engine`/`ensemble_trainer`. It
is deliberately parallel to Plan 01 (zero file overlap).
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/todos/pending/118-migrate-feature-domain-into-concept-registry.md

<interfaces>
`src/intelligence/concept_registry_service.py` (416 lines) — current shape, verified this session:

  `decide_comparison_action(state: GateState, *, won, eval_metric, eval_n, corpus_build_ref) -> ComparisonDecision`
      Pure core, unit-tested without a DB. Guard order today:
        deprecated -> 'noop_deprecated'
        corpus_build_ref == last_eval_corpus_build_ref -> 'blocked_same_corpus'
        eval_n < min_gate_n -> 'blocked_min_n'
        (eval_n - last_eval_n) < min_new_observations -> 'blocked_evidence_floor'
        not won -> 'record_loss'
        consecutive >= min_promotion_consecutive and status=='candidate' -> 'promote'
        ... -> 'record_win_not_promotable' / 'record_win'

  `@dataclass(frozen=True) GateState(status, promotion_consecutive, promotion_eval_metrics,
        last_eval_corpus_build_ref, last_eval_n, min_promotion_consecutive,
        min_new_observations, min_gate_n)`   <- has NO fdr_required field today

  `@dataclass(frozen=True) ComparisonDecision(action, new_promotion_consecutive,
        new_promotion_eval_metrics, baseline_metric)`

  `_LOAD_CONCEPT_SQL` selects `r.concept_id, r.status, g.promotion_consecutive,
        g.promotion_eval_metrics, g.last_eval_corpus_build_ref, g.last_eval_n,
        g.min_promotion_consecutive, g.min_new_observations, g.min_gate_n`
        FROM concept_registry r JOIN concept_gate g USING (concept_id)
        WHERE r.domain=$1 AND r.name=$2  **FOR UPDATE**

  `async ConceptRegistryService.record_comparison_outcome(conn, *, domain, name, won,
        eval_metric, eval_n, corpus_build_ref, default_min_promotion_consecutive,
        default_min_new_observations, default_min_gate_n, notes=None) -> ComparisonDecision`
      Everything runs inside `async with conn.transaction():` with the FOR UPDATE row lock held
      from load through write.

  Class docstring, L-7 paragraph (the text L-6 makes obsolete):
      "this service never reads concept_gate.fdr_required. BH-FDR enforcement lives entirely
       upstream in ops_ensemble_weight_compare.py - a future second caller of
       record_comparison_outcome must not assume this service enforces FDR itself."

`scripts/ops/alpha/ops_ensemble_weight_compare.py` (572 lines) — the sole caller today.
  Registry block starts at `if args.challenger_concept:` (line ~439).
  `_registry_outcome(outcome_rows) -> (won, eval_metric, eval_n, win_strata_count, win_only_n)`
  `_final_verdict(win, bh_reject) -> "WIN" | "WIN-FDR-VETO" | ...` — a stratum only reaches
  "WIN" when `bh_reject` is true, i.e. BH-FDR correction was applied AND survived;
  "WIN-FDR-VETO" means it passed the pairwise D-10 test but did not survive correction.
  `apply_bh_fdr(p_raw_list, compare_fdr_alpha)` from `src/intelligence/statistics/ic_math.py`
  runs unconditionally in "Pass 2" (line ~388), before the registry block.
  Three `REGISTRY: FAILED` sites today, ALL of which `return 0`:
    1. unknown `--champion-concept` (line ~468)
    2. `alpha.concept_registry.*` gate keys missing (line ~487)
    3. `ConceptNotFoundError` from `record_comparison_outcome` (line ~529)
  There is NO `--challenger-concept` pre-validation today — that is L-4's gap.
  `main()` returns `int` and the module bottom is `sys.exit(asyncio.run(main()))`, so a returned
  1 really does become a non-zero process exit.

`scripts/ops/corpus/ops_corpus_pipeline_run.sh` — the automation harness whose FATAL-halt
semantics make a silently-zero exit a correctness bug, not a style nit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move FDR enforcement inside record_comparison_outcome, fail closed (L-6, L-2)</name>
  <files>src/intelligence/concept_registry_service.py, tests/unit/test_concept_registry_service.py</files>
  <read_first>
    - src/intelligence/concept_registry_service.py (in full — 416 lines: the guard ordering in
      `decide_comparison_action`, the `GateState`/`ComparisonDecision` dataclasses, the L-7
      docstring paragraph being invalidated, and `_LOAD_CONCEPT_SQL`'s FOR UPDATE contract)
    - tests/unit/test_concept_registry_service.py (in full — the pure-core test style the new
      coverage must match)
    - .planning/todos/pending/118-migrate-feature-domain-into-concept-registry.md (L-2 and L-6)
    - docs/research/concept-unified-registry.md, "Promotion/Demotion Design for Autonomous
      Self-Improvement" invariants 1, 7 and 9 (the access-control framing this belongs to)
  </read_first>
  <action>
    Add `fdr_required: bool` to the `GateState` dataclass and select `g.fdr_required` in
    `_LOAD_CONCEPT_SQL` (append to the existing select list; `concept_gate.fdr_required` is
    `BOOLEAN NOT NULL DEFAULT false`, so no COALESCE is needed).

    Add a keyword-only `fdr_passed: bool | None = None` parameter to BOTH
    `decide_comparison_action` and `ConceptRegistryService.record_comparison_outcome`, threaded
    through. Docstring semantics, stated verbatim: `fdr_passed` means "this outcome's win
    determination survived an actually-executed multiplicity correction across the strata
    compared this round". `None` means the caller ran none — which is NOT the same as False and
    must be treated as unproven, i.e. blocked.

    New guard in `decide_comparison_action`, inserted AFTER the `blocked_evidence_floor` check
    and BEFORE the `if not won:` branch, returning a new action `'blocked_fdr_unverified'`:
        if won and state.fdr_required and fdr_passed is not True:
            return ComparisonDecision('blocked_fdr_unverified', state.promotion_consecutive,
                                      state.promotion_eval_metrics, None)
    Comment the placement rationale: the guard applies to WINS only. A loss is never a promotion
    and must still be recorded — swallowing losses would keep a decaying concept's consecutive
    counter alive, the opposite of fail-closed.

    Add `'blocked_fdr_unverified'` to the blocked/noop tuple in `record_comparison_outcome` that
    short-circuits before any write, and extend that path's existing
    `_logger.info("concept_registry.comparison_blocked", ...)` call with
    `fdr_required=state.fdr_required` and `fdr_passed=fdr_passed` so the block is diagnosable
    from the log alone.

    Update the `ComparisonDecision` docstring's action-vocabulary block to document
    `'blocked_fdr_unverified'`.

    DELETE the class docstring's L-7 paragraph ("this service never reads
    concept_gate.fdr_required. BH-FDR enforcement lives entirely upstream...") and replace it
    with the inverted statement: FDR enforcement is fail-closed inside this method, a caller that
    cannot prove correction was applied cannot promote, `concept_gate.fdr_required` IS read
    here — cite todo 118 L-6 and Phase 170.

    Tests to add in `tests/unit/test_concept_registry_service.py` (pure-core, no DB, matching the
    file's existing `decide_comparison_action` style):
      - `test_win_blocked_when_fdr_required_and_fdr_passed_none` -> 'blocked_fdr_unverified'
      - `test_win_blocked_when_fdr_required_and_fdr_passed_false` -> 'blocked_fdr_unverified'
      - `test_win_allowed_when_fdr_required_and_fdr_passed_true` -> action in ('record_win','promote')
      - `test_win_allowed_when_fdr_not_required_and_fdr_passed_none` -> unchanged behaviour
        (backwards compatibility for a gate with `fdr_required=false`)
      - `test_loss_recorded_even_when_fdr_required_and_unproven` -> 'record_loss'
      - `test_fdr_guard_runs_after_evidence_floor` — a state that is BOTH below the evidence
        floor and FDR-unproven returns 'blocked_evidence_floor', pinning guard order.

    L-2 is VERIFICATION, not implementation — record this conclusion in the plan SUMMARY. The
    read-modify-write L-2 flagged is already CAS-protected by `_LOAD_CONCEPT_SQL`'s `FOR UPDATE`
    held across the single `conn.transaction()` (Phase 160 review finding 1). Do NOT add a
    second redundant CAS. Add one regression test, `test_load_concept_sql_holds_row_lock`,
    asserting `"FOR UPDATE" in _LOAD_CONCEPT_SQL` and that
    `inspect.getsource(ConceptRegistryService.record_comparison_outcome)` contains
    `conn.transaction()`. Add a line to the existing L-2 comment above `_GATE_CACHE_UPDATE_SQL`
    recording that L-2 is closed by verification under Phase 170.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_concept_registry_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `_LOAD_CONCEPT_SQL` selects `g.fdr_required` and still contains `FOR UPDATE`.
    - `grep -v '^#' src/intelligence/concept_registry_service.py | grep -c "blocked_fdr_unverified"`
      is >= 3 (guard return, blocked-tuple membership, docstring vocabulary).
    - `grep -c "lives entirely upstream in" src/intelligence/concept_registry_service.py` is 0.
    - All six new FDR tests plus `test_load_concept_sql_holds_row_lock` exist and pass.
    - `test_fdr_guard_runs_after_evidence_floor` asserts `'blocked_evidence_floor'` — a test that
      only checks the FDR block in isolation does not pin the ordering and is not acceptable.
    - `.venv/bin/pytest tests/unit/ -q` fully green; `.venv/bin/ruff check . --fix` and
      `.venv/bin/black .` clean.
  </acceptance_criteria>
  <done>`concept_gate.fdr_required` is enforced fail-closed inside the service; a caller that
  cannot prove multiplicity correction was applied cannot write a win or a promotion; L-2 is
  closed by an explicit lock-regression test rather than by assumption.</done>
</task>

<task type="auto">
  <name>Task 2: Non-zero exits and challenger-concept validation in ops_ensemble_weight_compare (L-3, L-4)</name>
  <files>scripts/ops/alpha/ops_ensemble_weight_compare.py, tests/unit/test_ensemble_weight_compare.py</files>
  <read_first>
    - scripts/ops/alpha/ops_ensemble_weight_compare.py (lines 220-300 for the argparse block and
      the pre-existing `return 0` failure idiom; lines 330-400 for the Pass-2 `apply_bh_fdr`
      block that establishes whether correction actually ran; lines 439-568 for the whole
      registry block being modified)
    - tests/unit/test_ensemble_weight_compare.py (in full — existing coverage of
      `_registry_outcome` / `_final_verdict`, the helpers this task must NOT change)
    - scripts/ops/corpus/ops_corpus_pipeline_run.sh (how a step's exit status is consumed and
      what FATAL-halts the pipeline — the reason L-3 matters)
    - src/intelligence/concept_registry_service.py (post-Task-1: the new `fdr_passed` parameter
      this caller must supply)
  </read_first>
  <action>
    (a) L-4 — validate `--challenger-concept` before any registry work. Immediately after the
    existing `--champion-concept` existence check inside `async with pool.acquire() as conn:`,
    add the symmetric check for `args.challenger_concept` against
    `concept_registry WHERE domain='ensemble_strategy' AND name = $1`; on zero rows print
    `REGISTRY: FAILED - unknown challenger concept '<name>'` and `return 1`. Leave the
    `ConceptNotFoundError` catch in place as defence-in-depth.

    (b) L-3 — change EVERY `REGISTRY: FAILED` site from `return 0` to `return 1`: unknown
    champion concept, the new unknown challenger concept, missing `alpha.concept_registry.*` APR
    keys, and the `ConceptNotFoundError` handler. Do NOT change the earlier report-only failure
    paths (missing `alpha.ensemble_ic.gate_lookahead`, missing `alpha.ensemble.compare_fdr_alpha`,
    the `alpha_ensemble_ic` query failure) — those predate the registry path and changing them is
    a separate behavioural change to report-only mode; add a one-line comment at the first of
    them recording that they were deliberately left alone and why.

    (c) Pass `fdr_passed` to `record_comparison_outcome`. Derive it from the run's own state, not
    a constant: capture `fdr_correction_applied = bool(p_raw_list)` at the Pass-2
    `apply_bh_fdr` call site and pass `fdr_passed=fdr_correction_applied`. Comment that `won` is
    already gated on `bh_reject` via `_final_verdict`'s WIN rule, so `fdr_passed` asserts the
    correction machinery ran at all — the service's guard then makes "the caller forgot to run
    it" structurally impossible to promote through.

    (d) Add a `REGISTRY: BLOCKED` print branch for
    `decision.action == 'blocked_fdr_unverified'` that returns 1, so an automated run hitting the
    new guard halts loudly instead of printing a win-shaped summary line.

    (e) Update the `--challenger-concept` argparse help text: an unknown name is now a hard
    failure (exit 1), not a report-only warning.

    Test in `tests/unit/test_ensemble_weight_compare.py` — keep it at the pure-helper/source
    level this file already uses; do NOT build a live-DB harness here (that is Plan 05's
    rehearsal). Add `test_registry_failure_paths_return_nonzero`: read the module source, find
    every line containing `REGISTRY: FAILED`, and assert the next `return` statement within the
    following 6 lines is `return 1`. Crude, but it is exactly the regression L-3 names and costs
    nothing.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_ensemble_weight_compare.py -q &amp;&amp; .venv/bin/python -c "src=open('scripts/ops/alpha/ops_ensemble_weight_compare.py').read(); assert src.count('REGISTRY: FAILED') >= 4; assert 'fdr_passed=' in src; assert 'blocked_fdr_unverified' in src; assert 'unknown challenger concept' in src; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "REGISTRY: FAILED" scripts/ops/alpha/ops_ensemble_weight_compare.py` is >= 4
      (three existing sites plus the new challenger-concept site).
    - No `REGISTRY: FAILED` print is followed by `return 0` within the next 6 lines.
    - The call to `record_comparison_outcome` passes `fdr_passed=`.
    - A `blocked_fdr_unverified` branch exists and returns 1.
    - `--help` output for `--challenger-concept` mentions the non-zero exit.
    - `test_registry_failure_paths_return_nonzero` exists and passes.
    - `.venv/bin/pytest tests/unit/ -q` fully green; `.venv/bin/ruff check . --fix` and
      `.venv/bin/black .` clean.
  </acceptance_criteria>
  <done>Every registry failure path in the actuator exits non-zero, an unknown challenger concept
  is rejected before any write, and the FDR-proof argument is wired from the caller's real
  correction state into the service's new fail-closed guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI args -> concept_registry writes | `--challenger-concept` / `--champion-concept` are untrusted free text naming registry rows |
| ops_corpus_pipeline_run.sh -> this script's exit code | automation treats exit 0 as success; a silently-zero failure corrupts the pipeline's own verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-170-04 | Elevation of Privilege | `record_comparison_outcome` promotion path | mitigate | fail-closed `blocked_fdr_unverified` guard: a caller that cannot prove multiplicity correction ran cannot promote, regardless of what it claims about `won` |
| T-170-05 | Repudiation | `REGISTRY: FAILED` exit status | mitigate | every registry failure path returns 1, so `ops_corpus_pipeline_run.sh` cannot record a failed governance write as a successful pipeline step |
| T-170-06 | Tampering | operator-supplied concept names | mitigate | parameterised `$1` queries only (no string interpolation) plus existence validation before any write |
| T-170-SC | Tampering | package installs | accept | no npm/pip/cargo installs in this plan; no legitimacy gate applies |
</threat_model>

<verification>
1. `.venv/bin/pytest tests/unit/ -q` fully green.
2. `.venv/bin/ruff check . --fix` and `.venv/bin/black .` clean.
3. `grep -c "lives entirely upstream in" src/intelligence/concept_registry_service.py` == 0.
4. Running the actuator with a deliberately bogus concept name exits non-zero:
   `.venv/bin/python scripts/ops/alpha/ops_ensemble_weight_compare.py --champion v1 --challenger v1 --challenger-concept __definitely_not_a_concept__; echo "exit=$?"` prints `exit=1`.
</verification>

<success_criteria>
FDR enforcement is a service-level invariant instead of a caller convention, every registry
failure is loud and non-zero, unknown concept names are rejected before any write, and L-2's
lock guarantee has a test that fails if the lock is ever removed.
</success_criteria>

<output>
Create `.planning/phases/170-concept-registry-feature-domain-migration-feature-registry-r/170-02-SUMMARY.md` when done.
</output>
