---
phase: 167-cross-sectional-trade-construction-t3
plan: 06
type: execute
wave: 5
depends_on: ["167-05"]
files_modified:
  - docs/research/trade-construction-layer.md
  - docs/research/data-edge-source-thesis.md
  - docs/operations/operations-infrastructure.md
  - docs/reference/cheatsheet.md
autonomous: true
requirements:
  - "TCL-VG-1: Validation Gate 1 - produce the REAL verdict, not a code-correctness proof"
  - "TCL-VG-2: Validation Gate 2 - produce the REAL verdict"
  - "TCL-VG-3: Validation Gate 3 - record that the T2-vs-T3 comparison already satisfies it"
  - "167-VALIDATION Manual-Only: live --evaluate-gate run over the real accumulating construction_spreads population"
  - "RESEARCH Assumption A4 / Open Question 3: document the manual-invocation decision operationally"

must_haves:
  truths:
    - "construction_spreads is populated over the full 2006-2026 15m equity corpus from a real backfill run"
    - "A live --evaluate-gate run produced a real Validation Gate 1 verdict from real persisted rows"
    - "A live --evaluate-attribution run produced a real Validation Gate 2 verdict"
    - "Both verdicts are recorded in the governing research docs with their actual numbers, whatever they say"
    - "An operator can find how and when to run this service without reading the source"
  artifacts:
    - path: "docs/research/trade-construction-layer.md"
      provides: "Validation Gates section updated with the live Gate 1/2/3 verdicts"
      contains: "Gate 1"
    - path: "docs/research/data-edge-source-thesis.md"
      provides: "T3 section updated from 'passed a falsification script' to 'productionized, gates run'"
      contains: "T3"
    - path: "docs/operations/operations-infrastructure.md"
      provides: "runbook entry: manual invocation, cadence, no systemd timer, and why"
      contains: "cross_sectional_spread_tracker"
    - path: "docs/reference/cheatsheet.md"
      provides: "the four CLI invocations"
      contains: "cross_sectional_spread_tracker"
  key_links:
    - from: "docs/research/trade-construction-layer.md"
      to: "services/cross_sectional_spread_tracker.py"
      via: "Validation Gates section citing the service and its evaluation modes"
      pattern: "cross_sectional_spread_tracker"
    - from: "docs/research/data-edge-source-thesis.md"
      to: "docs/research/trade-construction-layer.md"
      via: "T3 section pointing at the recorded gate verdicts"
      pattern: "trade-construction-layer"
---

<objective>
Run the construction for real and record what it says.

Purpose: every prior plan in this phase proves the CODE is correct. None of them answers the
question the phase exists to answer. 167-VALIDATION.md states this explicitly in its Manual-Only
Verifications table: "a unit test can prove the gate-evaluation code is correct, but cannot
substitute for the real measurement the phase's goal is about." This plan backfills the full
corpus, runs both gates live, and transcribes the actual verdicts - pass or fail - into the
governing research documents.

This is a Renaissance-discipline plan: the deliverable is a measured answer, not a favorable one.
Phase 148 is this project's precedent - it ran its irreversible gates, got Gate 2 FAIL, and
recorded DO NOT PROMOTE. If Gate 1 or Gate 2 fails here, that verdict gets written down with the
same fidelity a pass would.

Output: a populated `construction_spreads` table, two live gate verdicts, updated research docs,
and an operational runbook entry.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-cross-sectional-trade-construction-t3/167-CONTEXT.md
@.planning/phases/167-cross-sectional-trade-construction-t3/167-RESEARCH.md
@.planning/phases/167-cross-sectional-trade-construction-t3/167-VALIDATION.md
@.planning/phases/167-cross-sectional-trade-construction-t3/167-03-SUMMARY.md
@.planning/phases/167-cross-sectional-trade-construction-t3/167-04-SUMMARY.md
@.planning/phases/167-cross-sectional-trade-construction-t3/167-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill the full corpus and verify incremental behavior on live data</name>
  <files>logs/cross_sectional_spread_tracker.log</files>
  <read_first>
    - services/cross_sectional_spread_tracker.py - the completed service. Confirm the CLI flags,
      the log event names, and the `construction_spreads` column order before running anything.
    - .planning/phases/167-cross-sectional-trade-construction-t3/167-03-SUMMARY.md - the resolved
      APR key names and the actual INSERT column order.
    - CLAUDE.md - "Infrastructure" (this machine IS the server, never SSH; DB queries need
      `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent`), and "Core Patterns"
      (the ProcessPoolExecutor orphan-worker warning - not applicable here since this service has
      no worker pool, but confirm no stray processes remain after the run regardless).
    - .planning/STATE.md - check whether todo 183's corpus recompute is still running. If
      `ic_engine` is actively writing, note it: this service only READS `feature_vectors` and
      `forward_returns` and writes only `construction_spreads`, so there is no write contention,
      but a concurrently-changing corpus means the backfill's row count is a snapshot. Record the
      concurrent state in the summary rather than ignoring it.
  </read_first>
  <action>
    Before running, verify the input corpus is in the state RESEARCH.md measured. Query the live
    database for the count of 15m equity `feature_vectors` rows with non-NULL `ctf_momentum`
    joined to complete `executable_open_to_open` forward returns, and the distinct symbol count
    and `bar_ts` range. RESEARCH.md's Environment Availability table recorded 8,792,411 rows,
    2006-07-07 through 2026-07-07, 80 symbols. If the observed numbers differ materially, record
    the difference in the summary - do not silently proceed on a different corpus than the one
    T3's result was measured on.

    Run the full backfill:
    `.venv/bin/python services/cross_sectional_spread_tracker.py --backfill`.
    Expect a long single-pass run. Capture the summary log line's `n_bars_processed`,
    `n_bars_skipped_degenerate`, `n_rows_written`, and the mean and median `one_way_turnover`.

    Verify the persisted result against T3's measured construction. Query
    `construction_spreads` for: total row count; distinct `construction_name` and `tf` values
    (must be exactly `ctf_momentum_decile_ls` and `15m`); the count of rows with NULL
    `one_way_turnover` (must be exactly 1 - the genuinely-first corpus bar, per Plan 01 design
    decision 2 and Pitfall 4); the mean and median `one_way_turnover` across all non-NULL rows;
    and the mean `gross_spread_fast` and `gross_spread_slow` in basis points. Compare the turnover
    statistics against T3's measured values (mean one-way leg turnover ~19.5%/bar, median ~6.25%,
    per D-03) and the mean spreads against T3's published full-history numbers (fast 5.9bp/bar,
    slow 11.1bp/bar). These are NOT gate criteria - they are equivalence checks proving the
    production service reproduces what the falsification script measured. A material divergence
    means the productionization introduced a bug and must be investigated before the gates are
    trusted, not explained away.

    Then verify incremental behavior on live data: run
    `.venv/bin/python services/cross_sectional_spread_tracker.py` (no flags) immediately after
    the backfill. Confirm from the log that it resolved a non-NULL watermark, that it seeded
    prior legs from a persisted row (the log line Plan 03 requires), and that it processed
    approximately zero new bars. Confirm `construction_spreads`' row count is unchanged and no
    `created_at` value moved - the idempotency guarantee (Plan 03 design decision 3) holding on
    real data, not just in the integration test.

    Confirm no stray processes remain: `ps aux | grep cross_sectional_spread_tracker` should show
    nothing after the runs complete.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT (SELECT count(*) FROM construction_spreads) > 100000 AND (SELECT count(DISTINCT construction_name) FROM construction_spreads) = 1 AND (SELECT count(DISTINCT tf) FROM construction_spreads) = 1 AND (SELECT count(*) FROM construction_spreads WHERE one_way_turnover IS NULL) = 1;" | grep -qx t && echo BACKFILL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT count(*) FROM construction_spreads` returns a value greater than 100,000 (a 20-year
      15m corpus with a valid decile split at most bars).
    - `SELECT DISTINCT construction_name, tf FROM construction_spreads` returns exactly one row:
      `ctf_momentum_decile_ls, 15m`.
    - `SELECT count(*) FROM construction_spreads WHERE one_way_turnover IS NULL` returns exactly
      `1`, and that row's `bar_ts` equals `SELECT MIN(bar_ts) FROM construction_spreads`
      (Pitfall 4 - more than one NULL is a bug, not a data property).
    - `SELECT count(*) FROM construction_spreads WHERE net_spread_fast_by_cost_bps IS NULL AND
      one_way_turnover IS NOT NULL` returns `0` (every turnover-bearing row has a live cost sweep).
    - `SELECT avg(one_way_turnover), percentile_cont(0.5) WITHIN GROUP (ORDER BY one_way_turnover)
      FROM construction_spreads` produces a mean and median recorded in the summary and compared
      explicitly against T3's ~0.195 / ~0.0625.
    - `SELECT avg(gross_spread_fast)*10000, avg(gross_spread_slow)*10000 FROM construction_spreads`
      produces basis-point means recorded in the summary and compared explicitly against T3's
      5.9bp / 11.1bp.
    - The second (no-flag) run leaves `count(*)` and `max(created_at)` unchanged.
    - `logs/cross_sectional_spread_tracker.log` contains the prior-leg seeding log line showing
      the seed came from a persisted row on the second run.
    - `ps aux | grep -c '[c]ross_sectional_spread_tracker'` returns 0 after both runs.
  </acceptance_criteria>
  <done>The full 2006-2026 15m equity corpus is persisted, its turnover and gross-spread statistics are recorded and compared against T3's measured values, and incremental re-run behavior is confirmed on live data.</done>
</task>

<task type="auto">
  <name>Task 2: Run both Validation Gates live and capture the real verdicts</name>
  <files>logs/cross_sectional_spread_tracker.log</files>
  <read_first>
    - .planning/phases/167-cross-sectional-trade-construction-t3/167-04-SUMMARY.md - the exact
      `gate_verdict`, `null_check`, and `gate1_summary` log event names and their field sets.
    - .planning/phases/167-cross-sectional-trade-construction-t3/167-05-SUMMARY.md - the exact
      `attribution_verdict` and `gate2_summary` log event names, their field sets, and the
      operational meaning of each of the three `verdict_text` outcomes.
    - services/cross_sectional_spread_tracker.py - `_run_evaluate_gate` and
      `_run_evaluate_attribution` (what each reads and what each emits).
    - docs/plans/2026-07-22-phase148-promotion-decision.md - this project's precedent for
      recording an irreversible gate verdict honestly. Phase 148 ran its gates, got Gate 2 FAIL,
      and wrote DO NOT PROMOTE. Match that fidelity.
  </read_first>
  <action>
    Confirm the OOS boundary before running: query
    `SELECT config_value FROM config_state WHERE config_key = 'alpha.validation.oos_start'` and
    record it. Query the count of `construction_spreads` rows at or after that boundary and the
    count of distinct `bar_ts::date` values among them - this is the day-cluster population Gate 1
    and Gate 2 rest on. If the day-cluster count is below `alpha.scoring.min_strategy_n`, both
    gates will return a degenerate `(False, nan, nan)` and the verdict would be meaningless;
    record that condition explicitly rather than reporting it as a FAIL.

    Run `.venv/bin/python services/cross_sectional_spread_tracker.py --evaluate-gate` and capture
    from the log: all eight `gate_verdict` cells (2 scales x 4 cost tiers) with their
    `n_bars`, `n_clusters`, `ci_lower`, `ci_upper`, `passes`, and `coverage`; both `null_check`
    lines with `null_p`, `null_mean`, `null_std`, and `n_shuffles`; and the single `gate1_summary`
    line with `gate1_passes`, the binding cost tier, and `verdict_text`. Also capture the
    `in_sample_diagnostic` cells - these are the comparison against T3's published full-history
    numbers.

    Run `.venv/bin/python services/cross_sectional_spread_tracker.py --evaluate-attribution` and
    capture both `attribution_verdict` lines (per scale: `beta`, `intercept`, `static_r2`,
    `static_dominates`, `n_bars`, `n_clusters`, `residual_ci_lower`, `residual_ci_upper`,
    `residual_passes`, `gate2_passes`, and the `max_static_r2` in force) and the `gate2_summary`
    line with `gate2_passes_overall` and `verdict_text`.

    Confirm neither run wrote anything: re-query `SELECT count(*), max(created_at) FROM
    construction_spreads` and assert both are unchanged from Task 1's post-backfill values. Both
    evaluation modes are read-only by contract (Plans 04/05 threat T-167-13); this is the live
    proof.

    Assemble the captured numbers into a structured verdict block for Task 3 to transcribe. Do
    not editorialize, do not round away precision, and do not omit a failing cell. If Gate 1 or
    Gate 2 fails, the phase's honest outcome is a recorded FAIL - the same shape as Phase 148's
    result, which is this project's most valuable recorded finding precisely because it was not
    the hoped-for one.
  </action>
  <verify>
    <automated>grep -q 'gate1_summary' logs/cross_sectional_spread_tracker.log && grep -q 'gate2_summary' logs/cross_sectional_spread_tracker.log && grep -q 'null_check' logs/cross_sectional_spread_tracker.log && grep -q 'in_sample_diagnostic' logs/cross_sectional_spread_tracker.log && echo GATES_RUN_OK</automated>
  </verify>
  <acceptance_criteria>
    - `logs/cross_sectional_spread_tracker.log` contains exactly one `gate1_summary` event and one
      `gate2_summary` event from this session's runs.
    - The log contains 8 `gate_verdict` events carrying `segment="oos"` - one per
      (scale, cost_bps) cell - each with a numeric `n_bars` and `n_clusters`.
    - The log contains 2 `null_check` events, one per scale, each with a numeric `null_p`.
    - The log contains `gate_verdict` events carrying `segment="in_sample_diagnostic"`.
    - The log contains 2 `attribution_verdict` events, one per scale, each with numeric `beta`,
      `static_r2`, and `residual_ci_lower`.
    - `SELECT count(*), max(created_at) FROM construction_spreads` is byte-identical to Task 1's
      post-backfill values (both evaluation modes wrote nothing).
    - The summary records the OOS `bar_ts` boundary, the OOS row count, and the OOS day-cluster
      count as explicit numbers.
    - The summary records `gate1_passes` and `gate2_passes_overall` as literal booleans, with the
      full eight-cell grid and both `null_p` values, whether they pass or fail.
  </acceptance_criteria>
  <done>Both Validation Gates have been run against the real persisted OOS population, every verdict field is captured, and neither run mutated the table.</done>
</task>

<task type="auto">
  <name>Task 3: Record the verdicts in the governing research docs and add the operational runbook entry</name>
  <files>docs/research/trade-construction-layer.md, docs/research/data-edge-source-thesis.md, docs/operations/operations-infrastructure.md, docs/reference/cheatsheet.md</files>
  <read_first>
    - docs/research/trade-construction-layer.md - read the "Validation Gates" section (lines
      205-216) and the "Sequencing" section (lines 217-228) in full. This doc IS this phase's
      requirements list; the gates section is where the verdicts belong.
    - docs/research/data-edge-source-thesis.md - read the T3 section (line 135 onward) in full,
      plus the T2 section header (line 88) to see how a verdict with a standing caveat is
      recorded in this file's own style.
    - docs/operations/operations-infrastructure.md - find where manual/on-demand oneshots are
      documented and match the existing entry shape; note CLAUDE.md's statement that all
      indicagent systemd timers are confirmed disabled as of 2026-07-02, which is part of the
      no-timer rationale.
    - docs/reference/cheatsheet.md - find the service-command section and match its formatting.
    - .planning/phases/167-cross-sectional-trade-construction-t3/167-03-SUMMARY.md - Plan 03's
      design decision 1 (the four-reason no-systemd-timer rationale) to reproduce in the runbook.
    - /home/bg/.claude/CLAUDE.md - writing constraints that apply to every doc edited here: never
      use em dashes; never add AI attribution.
    - docs/reference/documentation-standards.md - the project's own doc conventions, including
      the provenance convention for a doc written or materially informed by a model.
  </read_first>
  <action>
    Update `docs/research/trade-construction-layer.md`'s "Validation Gates" section. Under each of
    the three numbered gates, add the recorded outcome:

    Gate 1 - state the live verdict (`gate1_passes`), the binding cost tier, and the full
    eight-cell grid of `ci_lower` / `passes` per (scale, cost tier), plus both scales' `null_p`,
    `n_bars`, and `n_clusters`, and the OOS window boundary the verdict rests on. State that this
    is measured on the OOS segment (`bar_ts >= alpha.validation.oos_start`), and record the
    in-sample diagnostic numbers separately with an explicit note that they are the comparable
    figures to T3's published full-history result and are NOT the gate.

    Gate 2 - state the live verdict (`gate2_passes_overall`), both scales' `static_r2`, `beta`,
    `residual_ci_lower`, and `static_dominates`, and the `attribution_max_static_r2` ceiling in
    force. Add a short paragraph recording HOW Gate 2 was operationalized, since the doc's own
    text is prose and this phase resolved it: static bucket membership is each symbol's
    time-averaged net leg membership over the window, collapsed into a single benchmark return
    series, with the spread regressed on it and the residual gated through the same day-clustered
    bootstrap as Gate 1. Note that the naive 80-dummy-regressor reading was rejected as overfit.

    Gate 3 - record that this gate was already satisfied by the T2-vs-T3 comparison in
    `data-edge-source-thesis.md` and requires no new work in this phase, per RESEARCH.md's
    phase-requirements mapping. State it plainly; do not re-run anything.

    Also update the "Sequencing" section's stale status: the doc currently says "Recommended next
    step: scope this as a phase." That step is done. Replace it with the current state - the v1
    construction is built as `services/cross_sectional_spread_tracker.py`, is shadow-measurement
    only with no live capital, and what the recorded verdicts imply for the Phase 156-159 gate.
    If both gates passed, state that the Phase 156-159 execution/sizing chain's stated
    precondition is now met and the next decision is the user's. If either failed, state plainly
    that it is not met and name what would have to change.

    Update `docs/research/data-edge-source-thesis.md`'s T3 section. It currently records a
    falsification-script result. Add the productionization outcome: the construction is now a real
    service, the two Validation Gates have been run against a live OOS population, and the
    verdicts are recorded in `trade-construction-layer.md` (link, do not duplicate the full
    tables - one doc owns the numbers). Update the T3 section header's status marker to reflect
    the gate outcome. Preserve the existing T3 result text as the original falsification finding
    rather than overwriting it, matching how the T2 section preserves its own verdict alongside a
    later caveat.

    Add an entry to `docs/operations/operations-infrastructure.md` documenting the service
    operationally: the four CLI invocations; that `--backfill` is a one-time full-corpus pass and
    the no-flag invocation is the incremental mode; that both evaluation modes are read-only and
    safe to run any time; the recommended manual cadence for keeping the OOS shadow track record
    current; and the explicit statement that this service is deliberately NOT on a systemd timer
    and NOT in `service_auditor._DAG_ORDER`, reproducing Plan 03 design decision 1's four reasons
    in condensed form. Include the recovery note that `infrastructure_truncate_derived_tables.sh`
    truncates `construction_spreads` on a corpus rebuild and that `--backfill` is the documented
    way to repopulate it.

    Add the four CLI invocations to `docs/reference/cheatsheet.md` in that file's existing format.

    Across all four files: no em dashes (use a single dash, comma, or semicolon), no AI
    attribution, and state corrected facts plainly without review-narrative annotations.
  </action>
  <verify>
    <automated>grep -q 'cross_sectional_spread_tracker' docs/operations/operations-infrastructure.md && grep -q 'cross_sectional_spread_tracker' docs/reference/cheatsheet.md && grep -q 'evaluate-attribution' docs/research/trade-construction-layer.md && grep -q 'cross_sectional_spread_tracker' docs/research/data-edge-source-thesis.md && ! grep -qP '\x{2014}' docs/operations/operations-infrastructure.md docs/reference/cheatsheet.md docs/research/trade-construction-layer.md docs/research/data-edge-source-thesis.md && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/research/trade-construction-layer.md`'s Validation Gates section contains a recorded
      outcome under all three numbered gates, and the Gate 1 entry contains the literal OOS
      boundary timestamp and both `null_p` values.
    - `docs/research/trade-construction-layer.md` contains the phrase describing Gate 2's
      operationalization (time-averaged net leg membership collapsed into a single benchmark
      series) and states that the 80-dummy-regressor reading was rejected.
    - `docs/research/trade-construction-layer.md`'s Sequencing section no longer says
      "Recommended next step: scope this as a phase"
      (`grep -c 'scope this as a phase' docs/research/trade-construction-layer.md` returns 0).
    - `docs/research/data-edge-source-thesis.md`'s T3 section links to
      `trade-construction-layer.md` for the gate verdicts and does not duplicate the full verdict
      tables (`grep -c 'gate1_passes\|eight-cell' docs/research/data-edge-source-thesis.md`
      returns 0).
    - `docs/operations/operations-infrastructure.md` contains all four CLI invocations and the
      explicit statement that no systemd timer exists for this service, with the reasons.
    - `docs/reference/cheatsheet.md` contains `services/cross_sectional_spread_tracker.py`.
    - No em dash character appears in any line added to the four files:
      `git diff -U0 -- docs/ | grep '^+' | grep -cP '\x{2014}'` returns 0 (grep -P matches the
      em dash by codepoint, so this check cannot be broken by a search-and-replace pass).
    - `git diff -- docs/ | grep -ci 'co-authored-by\|generated with claude'` returns 0.
    - `.venv/bin/pytest tests/unit/ -q` still exits 0 and
      `.venv/bin/pytest tests/integration/ -k cross_sectional_spread -m integration -x -q` still
      exits 0.
  </acceptance_criteria>
  <done>The live Gate 1 and Gate 2 verdicts are recorded in the two governing research docs with their actual numbers, the design doc's stale sequencing text is corrected, and an operator can run and understand the service from the runbook and cheatsheet alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live corpus (`feature_vectors`, `forward_returns`) → persisted measurement | A concurrently-changing corpus (todo 183's recompute) means the backfill captures a snapshot; an unrecorded concurrent state makes the verdict unreproducible |
| Live gate output → recorded research verdict | The transcription step is where an inconvenient number could be softened, omitted, or rounded away; the docs become the durable record downstream phases act on |

No external-facing surface, no auth, no user input, no secrets. Tasks 2 and 3 write no database
rows at all. ASVS V2/V3/V4/V6 all N/A per RESEARCH.md's Security Domain analysis.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-17 | Repudiation | Verdict transcription fidelity | mitigate | Task 2's acceptance criteria require capturing `gate1_passes` and `gate2_passes_overall` as literal booleans plus the full eight-cell grid, whether passing or failing; Task 3's criteria require the recorded doc to contain the OOS boundary and both `null_p` values verbatim, so a softened summary fails its own grep |
| T-167-18 | Tampering | Backfill run against a different corpus than T3 measured | mitigate | Task 1 verifies row count, symbol count, and `bar_ts` range against RESEARCH.md's recorded 8,792,411 / 80 / 2006-07-07 to 2026-07-07 before running, and requires a material divergence to be recorded rather than silently accepted; the turnover and gross-spread equivalence checks against T3's ~0.195 / ~0.0625 / 5.9bp / 11.1bp are a second independent divergence detector |
| T-167-19 | Denial of Service | Concurrent corpus recompute contention | accept | This service reads `feature_vectors`/`forward_returns` and writes only `construction_spreads`, so there is no write contention with `ic_engine`'s recompute; Task 1 requires the concurrent state to be recorded so the snapshot is reproducible, and CLAUDE.md's ProcessPoolExecutor orphan-worker hazard does not apply since this service has no worker pool |
| T-167-13 | Elevation of Privilege | Evaluation modes mutating state | mitigate | Task 2 re-queries `count(*)` and `max(created_at)` after both gate runs and requires them byte-identical to the post-backfill values, proving the read-only contract on live data rather than only in unit tests |
| T-167-05 | Repudiation | Gate reproducibility | mitigate | The OOS boundary, day-cluster count, bootstrap seed key, and `attribution_max_static_r2` in force are all recorded alongside the verdict, so any future re-run can be checked for equivalence |
| T-167-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages and writes no code; it runs an already-reviewed service and edits Markdown |
</threat_model>

<verification>
1. `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM construction_spreads"` - greater than 100,000
2. `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM construction_spreads WHERE one_way_turnover IS NULL"` - exactly 1
3. `grep -c 'gate1_summary\|gate2_summary' logs/cross_sectional_spread_tracker.log` - at least 2
4. `git diff -U0 -- docs/ | grep '^+' | grep -cP '\x{2014}'` - 0 (no em dashes)
5. `.venv/bin/pytest tests/unit/ -q` - green
6. `.venv/bin/pytest tests/integration/ -k cross_sectional_spread -m integration -x -q` - green
7. `ps aux | grep -c '[c]ross_sectional_spread_tracker'` - 0
</verification>

<success_criteria>
- `construction_spreads` is populated from a real full-corpus backfill, with exactly one
  NULL-turnover row and turnover/spread statistics recorded and compared against T3's measured
  values.
- Both Validation Gates have been run live against the real OOS population and neither mutated
  the table.
- The actual Gate 1 and Gate 2 verdicts, pass or fail, are recorded in
  `trade-construction-layer.md` with their full numeric detail, and `data-edge-source-thesis.md`'s
  T3 section links to them.
- `trade-construction-layer.md`'s stale "scope this as a phase" sequencing text is corrected.
- An operator can run all four modes from the runbook and cheatsheet without reading source.
- All seven verification commands pass.
</success_criteria>

<output>
Create `.planning/phases/167-cross-sectional-trade-construction-t3/167-06-SUMMARY.md` when done.
Record the literal `gate1_passes` and `gate2_passes_overall` values, the full eight-cell Gate 1
grid, both `null_p` values, both scales' `static_r2` and `residual_ci_lower`, the OOS boundary and
day-cluster count, and the observed-vs-T3 equivalence comparison. If either gate failed, state the
failure plainly and name what the verdict implies for the Phase 156-159 gate - Phase 148's honest
DO NOT PROMOTE is the precedent, not an exception.
</output>
