---
phase: 167-cross-sectional-trade-construction-t3
plan: 04
type: execute
wave: 3
depends_on: ["167-03"]
files_modified:
  - services/cross_sectional_spread_tracker.py
  - tests/unit/test_cross_sectional_spread_tracker.py
autonomous: true
requirements:
  - "TCL-VG-1: Validation Gate 1 - net-of-cost spread Sharpe > 0 at 95% bootstrap CI, beats the shuffled-ranking null"
  - "TCL-MD-6: Minimal Design step 6 - portfolio-level measurement vs. benchmarks"
  - "D-05: cost-hurdle computed live, verdict reported per cost tier"
  - "RESEARCH Pattern 3: --evaluate-gate as a separate read-only CLI mode"
  - "RESEARCH Todo 186 assessment: reuse frame_gate_passes day-clustered bootstrap, do NOT build a new cross-sectional block bootstrap"

must_haves:
  truths:
    - "A --evaluate-gate run produces a Validation Gate 1 verdict over the OOS window without writing any row"
    - "The verdict is reported per (lookahead scale, cost tier) cell, all eight of them, not as a single number"
    - "The shuffled-ranking null is recomputed live every evaluation, not read from a cached script result"
    - "The binding Gate 1 verdict requires the MOST CONSERVATIVE cost tier to pass at both lookahead scales AND the null P-value to be below 0.05"
    - "The in-sample segment is reported as a clearly labeled diagnostic and is never the gate"
  artifacts:
    - path: "services/cross_sectional_spread_tracker.py"
      provides: "evaluate_spread_gate pure function, shuffled_ranking_null_p, _run_evaluate_gate, --evaluate-gate CLI mode"
      exports: ["evaluate_spread_gate", "shuffled_ranking_null_p"]
      contains: "--evaluate-gate"
    - path: "tests/unit/test_cross_sectional_spread_tracker.py"
      provides: "test_evaluate_gate and test_shuffled_ranking_null unit coverage"
      contains: "def test_evaluate_gate"
  key_links:
    - from: "services/cross_sectional_spread_tracker.py"
      to: "services/counterfactual_tracker.py"
      via: "verbatim reuse of evaluate_frame_gate / frame_gate_passes"
      pattern: "from services.counterfactual_tracker import"
    - from: "services/cross_sectional_spread_tracker.py"
      to: "construction_spreads"
      via: "read-only OOS-window query, no persistence"
      pattern: "net_spread_fast_by_cost_bps"
    - from: "services/cross_sectional_spread_tracker.py"
      to: "config_state alpha.validation.oos_start"
      via: "OOS boundary read with a fail-loud guard"
      pattern: "alpha.validation.oos_start"
---

<objective>
Add Validation Gate 1 as a separate read-only `--evaluate-gate` CLI mode: a day-clustered
bootstrap CI over the persisted net-of-cost spread across the OOS window, at every cost tier and
both lookahead scales, plus a live shuffled-ranking null.

Purpose: this is the bar `docs/research/trade-construction-layer.md` sets for the construction -
"net-of-cost spread Sharpe > 0 at 95% bootstrap CI, and beats the shuffled-ranking null." Every
statistical primitive needed already exists and is proven; RESEARCH.md's Todo 186 Scope
Assessment establishes that a spread return is a single scalar time series, not a pooled panel,
so `frame_gate_passes`' day-clustered bootstrap is exactly the right machinery and no new
cross-sectional block bootstrap is required.

Output: two pure functions, a read-only evaluation entrypoint, a `--evaluate-gate` CLI flag, and
unit tests.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-cross-sectional-trade-construction-t3/167-CONTEXT.md
@.planning/phases/167-cross-sectional-trade-construction-t3/167-RESEARCH.md
@.planning/phases/167-cross-sectional-trade-construction-t3/167-VALIDATION.md
@.planning/phases/167-cross-sectional-trade-construction-t3/167-03-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contracts this plan DEFINES. Plan 05 adds a sibling mode to the same module. -->

- `evaluate_spread_gate(rows: Iterable[Mapping[str, Any]], min_n: int, bootstrap_max_n: int,
  bootstrap_batch: int, bootstrap_random_state: int) -> list[dict[str, Any]]`
  Pure grouping/verdict core. Each input row carries `scale` (`"fast"` or `"slow"`), `cost_bps`
  (int), `cluster_id` (a `date`), and `pnl_r` (the net spread for that scale and tier). Delegates
  to `counterfactual_tracker.evaluate_frame_gate` with
  `group_key=lambda row: (row["scale"], row["cost_bps"])`. Returns one verdict dict per cell;
  `evaluate_frame_gate` populates the group key's two elements into fields named `tf` and
  `regime`, so this function must rename them to `scale` and `cost_bps` before returning
  (`alpha_scorer.py` handles the identical rename, see its module docstring's SHAPE CONSTRAINT
  note).

- `shuffled_ranking_null_p(panel_rows: Sequence[Mapping[str, Any]], return_col: str,
  decile_fraction: float, observed_mean_spread: float, n_shuffles: int,
  seed: int) -> tuple[float, float, float]`
  Returns `(null_p, null_mean, null_std)`. Permutes `ctf_momentum` WITHIN each `bar_ts`,
  re-forms the decile split via `decile_legs`, and computes each draw's mean gross spread.
  `null_p` is the fraction of draws whose mean spread is `>= observed_mean_spread`.

CLI surface after this plan:
- `python services/cross_sectional_spread_tracker.py` - incremental compute-and-persist
- `python services/cross_sectional_spread_tracker.py --backfill` - full-corpus first pass
- `python services/cross_sectional_spread_tracker.py --evaluate-gate` - read-only Gate 1
</interfaces>

<design_decisions>
1. **Gate 1 is evaluated on the OOS window (`bar_ts >= alpha.validation.oos_start`), not the full
   history.** The design doc's Gate 1 text says "Shadow spread portfolio on the OOS window."
   Note the direction: `counterfactual_tracker.py`'s FRAME-04 gate uses `bar_ts < oos_start`
   because it is deliberately an IN-SAMPLE exit gate; this gate is the opposite and must use
   `>=`. Copying the `<` comparison from that file would silently evaluate the wrong segment and
   produce a verdict that looks valid. The live value of `alpha.validation.oos_start` is
   `2025-12-24T05:15:00Z`, giving roughly 130 OOS trading days for the 15m corpus - enough
   day-clusters for a bootstrap CI.

2. **The in-sample segment is reported as a labeled diagnostic, never as the gate.** T3's
   published numbers (fast 5.9bp/bar `ci_lower`=5.6bp, slow 11.1bp/bar `ci_lower`=9.7bp) were
   measured over the FULL 2006-2026 history. Reporting the in-sample segment alongside the OOS
   verdict makes that comparison possible without letting in-sample evidence contaminate the
   verdict. Label it explicitly as `segment="in_sample_diagnostic"` in the log payload, and never
   include it in the pass/fail computation.

3. **The verdict is reported per (scale, cost_bps) cell - all eight - and the binding Gate 1
   verdict requires the MOST CONSERVATIVE tier to pass at BOTH scales.** T3's own cost treatment
   was a sweep, not a single number (D-05), and reporting only the cheapest tier would be the
   optimistic reading. Taking the maximum configured `cost_bps` (10 by default) as binding is the
   conservative reading and matches the discipline that produced Phase 148's honest DO-NOT-PROMOTE
   verdict.

4. **The shuffled-ranking null is recomputed live at every evaluation.** It cannot be derived from
   persisted `construction_spreads` rows - a permutation of the ranking produces different legs,
   so the raw panel must be re-queried for the OOS window. That window is small (roughly 3,400
   bars x 80 symbols), so 40 permutations are cheap. Hardcoding "the null was cleared at
   P=0.0000" from this session's script output would be exactly the anti-pattern RESEARCH.md
   names for the cost hurdle, applied to the null instead.

5. **No new bootstrap implementation.** `frame_gate_passes` is reused verbatim via
   `evaluate_frame_gate`. RESEARCH.md's Todo 186 Scope Assessment is explicit: a spread return is
   ONE scalar per bar, not a pooled panel, so the day-clustered BCa/CLT bootstrap is the correct
   object and todo 186's cross-sectional block bootstrap is NOT required for this phase. Do not
   add a task for it.
</design_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Implement the Gate 1 evaluation core, the live shuffled null, and the --evaluate-gate mode</name>
  <files>services/cross_sectional_spread_tracker.py</files>
  <read_first>
    - services/cross_sectional_spread_tracker.py - current state after Plan 03 (the pure
      functions, the service class, the existing CLI block this task extends).
    - services/counterfactual_tracker.py - read lines 172-236 (`frame_gate_passes`: the exact
      signature, the `min_n` and `<2 clusters` early returns that yield `(False, nan, nan)`, and
      the BCa-vs-CLT method switch at `bootstrap_max_n`), lines 906-980 (`evaluate_frame_gate`:
      the `group_key` contract and the CRITICAL constraint that it unpacks the key as a 2-TUPLE
      via `for (dim_a, dim_b), bucket in groups.items()` - a 3-tuple raises ValueError), lines
      982-1050 (`_GATE_QUERY_SQL` and `_run_evaluate_gate`: the read-only branch shape, the
      `alpha.validation.oos_start` fail-loud guard, `CorpusManifest` usage, and the explicit
      comment "No D-06 job_completed_total emission -- this performs no persistence"), and lines
      1056-1091 (the argparse block this task adds a flag to).
    - services/alpha_scorer.py - lines 25-40, the SHAPE CONSTRAINT note explaining that
      `evaluate_frame_gate` returns its group key's two elements in fields literally named `tf`
      and `regime` regardless of what was grouped, and that callers must rename them. This is the
      exact trap `evaluate_spread_gate` must handle.
    - scripts/analysis/t3_cross_sectional_long_short_ctf_momentum_check.py - lines 102-127
      (`_generate_shuffled_feature_draws` and `_null_spread_from_shuffled_feature`: the
      within-`bar_ts` permutation and the `P(null >= observed)` computation) and lines 192-253
      (`_run_one_scale`: the verdict wording that distinguishes "clears CI but not the null" from
      a genuine pass - reproduce that three-way distinction).
    - .planning/phases/167-cross-sectional-trade-construction-t3/167-03-SUMMARY.md - the final
      `construction_spreads` column names and the resolved APR key names.
  </read_first>
  <action>
    Extend `services/cross_sectional_spread_tracker.py`. Do not modify Plan 02's construction
    primitives or Plan 03's write path - this is an additive, read-only branch.

    Implement `evaluate_spread_gate` with the signature in `<interfaces>`. It calls
    `counterfactual_tracker.evaluate_frame_gate` with
    `group_key=lambda row: (row["scale"], row["cost_bps"])` and then renames each returned
    verdict's `tf` field to `scale` and `regime` field to `cost_bps`. Add a docstring citing
    `alpha_scorer.py`'s SHAPE CONSTRAINT note as the reason the rename exists and warning that a
    3-tuple group key would raise `ValueError: too many values to unpack`. Rename `n_frames` to
    `n_bars` in the output, since a spread row is a bar, not a frame. Do not reimplement any
    bootstrap math.

    Implement `shuffled_ranking_null_p` with the signature in `<interfaces>`. Seed
    `numpy.random.default_rng(seed)` once. For each of `n_shuffles` draws: permute the
    `ctf_momentum` values WITHIN each `bar_ts` group (never across bars - a cross-bar permutation
    would destroy the cross-sectional structure being tested, not just the ranking information),
    re-form legs per bar with `decile_legs` at the configured `decile_fraction`, compute each
    bar's spread with `spread_from_legs`, and take that draw's mean spread. Return
    `(null_p, null_mean, null_std)` where `null_p = mean(draw_means >= observed_mean_spread)`.
    Follow the T3 script's efficiency lesson from its `_generate_shuffled_feature_draws`
    docstring: the permutation does not depend on which return scale is being tested, so generate
    the draws ONCE and reuse them for both scales rather than re-permuting per scale.

    Add module-level SQL constants: `_GATE_ROWS_SQL` selecting
    `bar_ts, bar_ts::date AS cluster_id, gross_spread_fast, gross_spread_slow,
    net_spread_fast_by_cost_bps, net_spread_slow_by_cost_bps` from `construction_spreads`
    `WHERE construction_name = $1 AND tf = $2 AND bar_ts >= $3 AND one_way_turnover IS NOT NULL`
    (the turnover filter excludes the single genuinely-first corpus bar, whose net spread is
    undefined), and `_GATE_PANEL_SQL` re-selecting the raw panel for the same OOS window using
    Plan 03's exact filter set (`_PANEL_SQL_TEMPLATE` with an `AND fv.bar_ts >= $2` clause) for
    the null computation. Every value bound as a placeholder; no dynamic interpolation.

    Implement `async def _run_evaluate_gate(db_dsn: str) -> None` mirroring
    `counterfactual_tracker._run_evaluate_gate`'s shape exactly: open a plain
    `asyncpg.connect(db_dsn)` (not a pool - this is a read-only reporting branch), load
    `alpha.scoring.min_strategy_n`, `alpha.scoring.bootstrap_max_n`,
    `alpha.scoring.bootstrap_batch`, and `alpha.scoring.bootstrap_random_state` (falling back to
    `_DEFAULT_BOOTSTRAP_RANDOM_STATE`) plus `alpha.construction.decile_fraction`,
    `alpha.construction.cost_hurdle_bps_round_trip`, and `alpha.construction.null_shuffles`; call
    `validate_construction_config` on them. Read `alpha.validation.oos_start` from `config_state`
    and raise a `RuntimeError` naming this service if it is NULL - reproduce
    `counterfactual_tracker`'s guard rationale verbatim in the message: a missing OOS boundary
    would silently exclude every row from the gate. Then:

    Fetch the OOS `construction_spreads` rows. Flatten them into the `evaluate_spread_gate` input
    shape: for each row and each `scale` in (`fast`, `slow`) and each configured `cost_bps`, emit
    one record with `scale`, `cost_bps`, `cluster_id`, and `pnl_r` read from the matching
    `net_spread_*_by_cost_bps` jsonb keyed by `str(cost_bps)`. Call `evaluate_spread_gate` and log
    one `cross_sectional_spread_tracker.gate_verdict` line per cell with
    `segment="oos"` in the payload.

    Fetch the OOS raw panel and compute the observed mean GROSS spread per scale, then call
    `shuffled_ranking_null_p` once per scale reusing one set of permutation draws. Log a
    `cross_sectional_spread_tracker.null_check` line per scale with `null_p`, `null_mean`,
    `null_std`, and `n_shuffles`.

    Compute the binding Gate 1 verdict per design decision 3: `gate1_passes` is true only when,
    for BOTH scales, the cell at `max(cost_bps)` has `passes is True`, AND both scales' `null_p`
    is strictly below `0.05`. Log a single `cross_sectional_spread_tracker.gate1_summary` line
    with `gate1_passes`, the binding cost tier, both scales' `ci_lower`/`ci_upper`/`n_bars`/
    `n_clusters`, both `null_p` values, and a `verdict_text` field carrying the three-way
    distinction from the T3 script's `_run_one_scale`: a genuine pass, a CI-clears-but-null-not-
    distinguishable result (a dollar-neutral construction artifact, NOT a pass), or a
    CI-does-not-clear-zero failure.

    Repeat the `construction_spreads` fetch and `evaluate_spread_gate` call once more with
    `bar_ts < oos_start`, and log those verdicts with `segment="in_sample_diagnostic"` (design
    decision 2). Never feed them into `gate1_passes`. Add an inline comment stating that these
    exist solely to make the T3 script's published full-history numbers comparable.

    Write a `CorpusManifest("cross_sectional_spread_tracker_gate", ...)` with `set_inputs` and a
    `mark_success()`/`write()`, mirroring `counterfactual_tracker._run_evaluate_gate`. Add an
    explicit comment, copying that function's own wording, that this branch emits NO D-06
    `job_completed_total` metric because it performs no persistence.

    Add `--evaluate-gate` to the existing argparse block with help text stating it evaluates
    Validation Gate 1 over `bar_ts >= alpha.validation.oos_start` per (lookahead scale, cost
    tier) via the day-clustered bootstrap plus a live shuffled-ranking null, and that it is
    read-only and writes no `construction_spreads` rows. Route it to
    `asyncio.run(_run_evaluate_gate(db_dsn))` before the default compute path, matching
    `counterfactual_tracker.py`'s dispatch order.

    Run `.venv/bin/ruff check services/cross_sectional_spread_tracker.py --fix` and
    `.venv/bin/black services/cross_sectional_spread_tracker.py`.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from services.cross_sectional_spread_tracker import evaluate_spread_gate, shuffled_ranking_null_p, _run_evaluate_gate; print('GATE1_OK')" && .venv/bin/python services/cross_sectional_spread_tracker.py --help | grep -q -- '--evaluate-gate' && .venv/bin/ruff check services/cross_sectional_spread_tracker.py && .venv/bin/pytest tests/unit/test_cross_sectional_spread_tracker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `evaluate_spread_gate`, `shuffled_ranking_null_p`, and `_run_evaluate_gate` all import
      cleanly from `services.cross_sectional_spread_tracker`.
    - `.venv/bin/python services/cross_sectional_spread_tracker.py --help` output contains
      `--evaluate-gate`.
    - `grep -c "from services.counterfactual_tracker import" services/cross_sectional_spread_tracker.py`
      is at least 1 and the import list includes `evaluate_frame_gate` - the bootstrap is reused,
      not reimplemented.
    - `grep -c "scipy.stats import bootstrap\|def frame_gate_passes\|def _bca\|BCa" services/cross_sectional_spread_tracker.py`
      returns 0 (no second bootstrap implementation anywhere in this module).
    - `grep -c "bar_ts >= \$3\|bar_ts >= " services/cross_sectional_spread_tracker.py` is at
      least 1, and inspecting `grep -n "bar_ts <" services/cross_sectional_spread_tracker.py`
      shows every `<` comparison is on the in-sample diagnostic query only (design decision 1).
    - `grep -c "alpha.validation.oos_start" services/cross_sectional_spread_tracker.py` is at
      least 1, and the file contains a `RuntimeError` raise for the NULL case.
    - `grep -c "in_sample_diagnostic" services/cross_sectional_spread_tracker.py` is at least 1.
    - `grep -c "JOB_COMPLETED_TOTAL" services/cross_sectional_spread_tracker.py` returns 0 in the
      gate branch - verify by inspection that `_run_evaluate_gate` does not call
      `BaseBatch.run()` and emits no completion metric.
    - `grep -v '^#' services/cross_sectional_spread_tracker.py | grep -c "0.0000\|survives.*True"`
      returns 0 (no hardcoded null-clearance or cost-survival conclusion in executable code).
    - `.venv/bin/ruff check services/cross_sectional_spread_tracker.py` exits 0.
    - Plans 02 and 03's tests still pass:
      `.venv/bin/pytest tests/unit/test_cross_sectional_spread_tracker.py -x -q` and
      `.venv/bin/pytest tests/integration/ -k cross_sectional_spread -m integration -x -q` both
      exit 0.
  </acceptance_criteria>
  <done>A read-only `--evaluate-gate` mode computes an eight-cell OOS verdict grid, a live shuffled-ranking null per scale, a binding Gate 1 pass/fail at the most conservative cost tier, and a clearly labeled in-sample diagnostic - reusing `frame_gate_passes` verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for the Gate 1 grouping core and the shuffled null</name>
  <files>tests/unit/test_cross_sectional_spread_tracker.py</files>
  <read_first>
    - tests/unit/test_cross_sectional_spread_tracker.py - current state after Plan 02 (append,
      do not rewrite; the five existing tests must keep passing).
    - tests/unit/test_counterfactual_tracker.py - locate its `evaluate_frame_gate` test and copy
      its synthetic-row construction shape (this is the analog 167-VALIDATION.md row 167-04-01
      explicitly points at: "adapt `test_counterfactual_tracker.py`'s gate-evaluation shape").
    - services/cross_sectional_spread_tracker.py - the two functions written in Task 1 (exact
      signatures and return-field names).
    - services/counterfactual_tracker.py - lines 172-212, specifically the early-return
      conditions: `len(pnl_r_values) < min_n` and `< 2` day-clusters both yield
      `(False, nan, nan)`. The tests must construct enough rows and clusters to get past both.
    - .planning/phases/167-cross-sectional-trade-construction-t3/167-VALIDATION.md - row
      167-04-01 binds the test name `test_evaluate_gate` exactly.
  </read_first>
  <action>
    Append to `tests/unit/test_cross_sectional_spread_tracker.py`. No live DB.

    `test_evaluate_gate` - build synthetic rows spanning 2 scales (`fast`, `slow`) x 4 cost tiers
    (`1`, `3`, `5`, `10`) x enough distinct `cluster_id` dates and `pnl_r` values to clear both
    `min_n` and the 2-cluster floor (use `min_n=30` and at least 40 distinct dates). Give the
    `fast`/`10` cell an unambiguously positive `pnl_r` distribution and the `slow`/`10` cell an
    unambiguously negative one. Assert `evaluate_spread_gate` returns exactly 8 verdict dicts;
    assert each dict has keys `scale`, `cost_bps`, `n_bars`, `n_clusters`, `ci_lower`,
    `ci_upper`, `passes`, `coverage` and does NOT have keys `tf` or `regime` (the rename from
    `alpha_scorer.py`'s SHAPE CONSTRAINT is the specific thing under test here); assert the
    `fast`/`10` cell has `passes is True` and `ci_lower > 0`, and the `slow`/`10` cell has
    `passes is False`. Assert that a group of rows below `min_n` yields `passes is False` with a
    NaN `ci_lower`, proving the sufficiency floor is respected rather than bypassed.

    `test_evaluate_gate_group_key_is_a_two_tuple` - a source-level contract test in the style of
    `test_counterfactual_tracker.py`'s source assertions. Use `inspect.getsource` on
    `evaluate_spread_gate` and assert the `group_key` lambda it passes to `evaluate_frame_gate`
    returns exactly two elements. Include an assertion message naming the
    `ValueError: too many values to unpack` failure mode a 3-tuple would cause.

    `test_shuffled_ranking_null` - build a synthetic panel over 30 `bar_ts` values and 20 symbols
    where `ctf_momentum` is PERFECTLY correlated with `return_fast` (a maximally informative
    ranking). Compute the observed mean spread with `decile_legs` + `spread_from_legs`, then call
    `shuffled_ranking_null_p` with a fixed seed and at least 20 shuffles; assert `null_p` is
    strictly below `0.05` - a real ranking must beat its own permutation null. Then build a
    second panel where `ctf_momentum` is generated independently of the returns and assert
    `null_p` is NOT below `0.05` - a meaningless ranking must NOT clear the null. This second
    case is the one that matters: it proves the null check can actually fail, which is what makes
    the passing case evidence rather than a formality. Assert reproducibility: two calls with the
    same seed return identical `null_p`.

    `test_shuffled_null_permutes_within_bar_only` - assert that after permutation the SET of
    `ctf_momentum` values present at each `bar_ts` is unchanged (only the symbol-to-value
    assignment moves). A cross-bar permutation would destroy the cross-sectional structure being
    tested, not just the ranking information. Implement by exposing or reusing the draw-generation
    step and comparing per-bar sorted value arrays before and after.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_cross_sectional_spread_tracker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_cross_sectional_spread_tracker.py::test_evaluate_gate -x`
      passes (167-VALIDATION.md row 167-04-01).
    - `.venv/bin/pytest tests/unit/test_cross_sectional_spread_tracker.py::test_shuffled_ranking_null -x`
      passes.
    - `.venv/bin/pytest tests/unit/test_cross_sectional_spread_tracker.py::test_evaluate_gate_group_key_is_a_two_tuple -x`
      passes.
    - `.venv/bin/pytest tests/unit/test_cross_sectional_spread_tracker.py::test_shuffled_null_permutes_within_bar_only -x`
      passes.
    - `.venv/bin/pytest tests/unit/test_cross_sectional_spread_tracker.py -x -q` exits 0 with at
      least 9 tests (Plan 02's 5 plus this plan's 4).
    - `grep -c 'asyncpg\|psycopg2\|create_pool' tests/unit/test_cross_sectional_spread_tracker.py`
      returns 0.
    - `.venv/bin/pytest tests/unit/ -q` exits 0.
  </acceptance_criteria>
  <done>Four new unit tests prove the eight-cell verdict grid, the field rename, the two-tuple group-key contract, a null check that can actually fail, and within-bar-only permutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config_state` (`alpha.validation.oos_start`, `alpha.scoring.*`, `alpha.construction.*`) → gate verdict | Operator-mutable values determine which segment is evaluated and what counts as passing; a wrong or missing value silently changes a research verdict |
| Persisted `construction_spreads` rows → gate verdict | The gate rests entirely on rows written by Plan 03; an unnoticed measurement gap would produce a confidently wrong verdict |

No external-facing surface, no auth, no user input, no secrets. This mode performs no writes at
all. ASVS V2/V3/V4/V6 all N/A per RESEARCH.md's Security Domain analysis.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-01 | Tampering | APR load in `_run_evaluate_gate` (ASVS V5) | mitigate | `validate_construction_config` is called on the loaded values before evaluation; a missing `alpha.validation.oos_start` raises `RuntimeError` rather than silently evaluating an empty row set (reproducing `counterfactual_tracker`'s own guard) |
| T-167-12 | Tampering | Wrong OOS comparison direction | mitigate | Design decision 1 documents that this gate uses `bar_ts >= oos_start` while `counterfactual_tracker`'s FRAME-04 uses `<`; the acceptance criteria assert every `<` comparison in the module belongs to the in-sample diagnostic query only |
| T-167-02 | Tampering | Cost-hurdle and null-clearance conclusions hardcoded | mitigate | The verdict grid is computed live per cost tier from persisted `net_spread_*_by_cost_bps`, and the null is recomputed from the raw panel every run; an acceptance criterion greps executable code for `0.0000` / `survives.*True` literals |
| T-167-05 | Repudiation | Gate verdict reproducibility | mitigate | `alpha.scoring.bootstrap_random_state` seeds the BCa resampling and `alpha.construction.null_shuffles`' RNG is seeded from `_DEFAULT_BOOTSTRAP_RANDOM_STATE`; `test_shuffled_ranking_null` asserts identical output for identical seeds |
| T-167-13 | Elevation of Privilege | Read-only mode acquiring write capability | mitigate | `_run_evaluate_gate` opens a plain `asyncpg.connect`, never calls `BaseBatch.run()`, contains no INSERT/UPDATE/DELETE, and emits no D-06 metric - mirroring `counterfactual_tracker._run_evaluate_gate`'s explicit no-persistence contract |
| T-167-04 | Tampering | Gate SQL construction | mitigate | `_GATE_ROWS_SQL` and `_GATE_PANEL_SQL` are fixed module-level strings; `construction_name`, `tf`, and the OOS boundary are all bound as asyncpg placeholders |
| T-167-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; `numpy` and `scipy` (via the reused `frame_gate_passes`) are already pinned project dependencies |
</threat_model>

<verification>
1. `.venv/bin/pytest tests/unit/test_cross_sectional_spread_tracker.py -x -q` - at least 9 passed
2. `.venv/bin/pytest tests/unit/ -q` - green
3. `.venv/bin/pytest tests/integration/ -k cross_sectional_spread -m integration -x -q` - still green
4. `.venv/bin/python services/cross_sectional_spread_tracker.py --help` shows `--backfill` and `--evaluate-gate`
5. `.venv/bin/ruff check services/cross_sectional_spread_tracker.py` - exits 0
</verification>

<success_criteria>
- `--evaluate-gate` runs read-only and produces an eight-cell (2 scales x 4 cost tiers) OOS
  verdict grid plus a per-scale live shuffled-ranking null.
- The binding Gate 1 verdict requires the most conservative cost tier to pass at both scales AND
  both null P-values below 0.05.
- The in-sample segment is logged as `in_sample_diagnostic` and never enters the verdict.
- No second bootstrap implementation exists in the module; `frame_gate_passes` is reused verbatim.
- Nine or more unit tests pass, including a null check proven capable of failing.
</success_criteria>

<output>
Create `.planning/phases/167-cross-sectional-trade-construction-t3/167-04-SUMMARY.md` when done.
Record the exact log event names emitted (`gate_verdict`, `null_check`, `gate1_summary`) - Plan 06
harvests them for the live verdict.
</output>
