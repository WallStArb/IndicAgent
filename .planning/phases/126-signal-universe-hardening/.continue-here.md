---
context: phase
phase: 126-signal-universe-hardening
task: 7
total_tasks: 7
status: complete
last_updated: 2026-06-15T15:58:48Z
---

# Phase 126 — COMPLETE

All 7 plans executed. 4754 tests pass. Pushed to remote. Nothing to resume here.

**Next phase:** 128 — 3-Table Schema Design and ADR

Phase 127 (Clean Replay) was moved AFTER 128-130 so replay lands in the final 3-table schema and produces `counterfactual_pnl_r`-populated training data immediately. Sequence: 128 → 129 → 130 → 127.

Start with: `/gsd-discuss-phase 128` then `/gsd-plan-phase 128`

## What Was Done

| Plan | Description | Commit |
|------|-------------|--------|
| 126-00 | Pre-flight diagnostics | 345a47a0 |
| 126-01 | Zone width gate: frame_trade rejects zones < 1.5×ATR | 28e1dfea |
| 126-02 | Plugin completeness: _I7_I6_EXEMPT deleted, 8 plugins wired to I6 | 28e1dfea |
| 126-05 | Signal quality audit: IC league table | 28e1dfea |
| 126-06 | Pipeline annotation layer: _annotate_signal(), SIGNAL_SCHEMA_VERSION v4 | 28e1dfea |
| test fixes | 53 failures → 0 (4754 pass, 37 skip) | 26611166 |
| 126-07 | REQUIREMENTS.md gap closure: SIGNAL-QUALITY-01/02 corrected | c517fa26 |

## Key Decisions

- **MIN_ZONE_WIDTH_ATR**: raised 0.25 → 1.5 in `zone_engine.py`. Zone engine min must match `frame_trade` gate threshold. Producing sub-gate zones is a silent production bug — fixing only test fixtures would mask it.
- **ATR_ZONE_SWEEP_MULTIPLIER**: raised 0.5 → 0.76 in `trade_framer.py`. 0.5×ATR gives 1.0×ATR sweep bands (below 1.5×ATR gate). 0.76 gives 1.52×ATR (just above gate).
- **_nullable_float()**: returns `None` for absent ECL keys, not 0.0. `None` = cold-start; `0.0` = genuine neutral. ML training integrity requires this distinction.
- **ECL annotation at pipeline layer**: `signal_processor._annotate_signal()` stamps all 5 ECL fields. `capture_signal_features()` marked DEPRECATED in `confidence_utils.py` (deletion Phase 128).
- **stopped_at_entry measurement**: deferred to Phase 127 REPLAY-01. Gate enforced prospectively; historical data lacks outcome rows.

## Phase 127 Context (for resuming agent)

**REPLAY-01**: Full historical replay on corrected pipeline (Phases 123-126) with `--warmup`. Measures:
- `context_features` coverage > 99% for non-cold-start signals
- All 5 over-firing plugins confirmed < 3% fire rate in replay output

**REPLAY-02**: Phase 121-02 deferred validation report. Critical methodology constraint:
- Do NOT run Welch's t-test across before/after pnl_r — populations are not exchangeable
- Correct measures: signal volume delta, CTF as feature analysis, firing rate distribution, cold-start null distribution

<current_state>
Phase 126 fully complete. All work committed and pushed to remote main (commits 28e1dfea..e7b0b745). Unit tests: 4754 pass, 37 skip, 0 fail. REQUIREMENTS.md accurate. Ready for Phase 127.
</current_state>

<next_action>
Start Phase 128 (3-Table Schema Design and ADR) — NOT 127. Phase 127 replay was intentionally moved after 128-130 to replay into the final schema. Run `/gsd-discuss-phase 128` then `/gsd-plan-phase 128`.
</next_action>
