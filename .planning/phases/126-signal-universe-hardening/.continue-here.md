---
context: phase
phase: 126-signal-universe-hardening
task: 7
total_tasks: 7
status: in_progress
last_updated: 2026-06-15T14:42:50.707Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: intelligence_features column names — DB uses FUNCTIONAL names (technical_indicators, regime_features, confluence_scores, pattern_detections), NOT tier codes (i1, i3, i4, i5). Migration 126_revert_tier_column_names.sql reverted the rename. Do NOT change API SQL back to tier codes.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Wrong direction on column rename | Assumed DB uses i1/i3/i4/i5 (stale test mock was wrong); started updating API SQL in wrong direction | blocking | Always read migration files before touching API/test SQL; migration 126_revert_tier_column_names.sql is the authority |

<current_state>
Phase 126 (signal-universe-hardening) code execution is complete (5 plans summarized: 00, 01, 02, 05, 06). A post-facto REQUIREMENTS.md update plan (126-07) exists but has no SUMMARY yet.

A background agent (launched mid-session) is fixing ~60 remaining unit test failures in tests/unit/. The agent's task: fix zone width gate test failures, replay auditor temporal guard, ctf_score annotation changes, and various individual test updates. Do NOT touch any test files until the background agent completes.

There are uncommitted changes from this session (see below).
</current_state>

<completed_work>

This session:
- Systematic root cause analysis of 90 test failures (13 distinct root causes identified)
- Fixed: `plugin_utils.emit_signal()` — removed stale `features_snapshot=features_snapshot` kwarg (make_signal_from_frame no longer accepts it after 126-06 pipeline annotation)
- Fixed: deleted dead `tests/unit/scripts/test_pipeline_reset.py` (imports from deleted pipeline_reset.py script)
- Fixed: `test_signal_replay_auditor.py` — temporal guard: changed `_evaluate_zone_track(signal_dict, bars, T0)` to use `T0 - timedelta(minutes=1)` (temporal guard blocks bar_time <= signal_timestamp per D-01)
- Fixed: `test_lifecycle_tracker.py::test_activation_blocked_when_bar_time_equals_signal_timestamp` — updated test to expect `t is None` (not activation)
- Fixed: `test_signal_ledger.py` — all param count assertions 34→36 (raw_confidence + calibrated_confidence added to _to_row())
- Fixed: `test_settings_equity.py` — ibkr_max_subscriptions 80→100
- Fixed: `test_features_route.py` — mock keys updated to functional column names (technical_indicators, regime_features, confluence_scores, pattern_detections)
- Reverted incorrect changes to src/api/routes/features.py and signals.py (those were correct; test mock was wrong)

Phase 126 code work (from prior session, already committed):
- 126-00: Pre-flight diagnostics
- 126-01: Zone width gate (frame_trade() rejects zones < 1.5×ATR)
- 126-02: Plugin completeness (8 exempt plugins wired to I6, _I7_I6_EXEMPT deleted)
- 126-05: Signal quality audit (IC league table + detection verifiability)
- 126-06: Pipeline annotation layer (_annotate_signal(), SIGNAL_SCHEMA_VERSION v4)
</completed_work>

<remaining_work>

1. **Wait for background agent to finish** — it is fixing ~60 zone-width and other test failures. Do NOT touch test files until it reports completion. Then verify with `.venv/bin/pytest tests/unit/ -q --tb=no`.

2. **126-07: REQUIREMENTS.md update** — PLAN exists (126-07-PLAN.md), no SUMMARY yet. Must_haves:
   - SIGNAL-QUALITY-01: update gate location (frame_trade, not _resolve_zone_bounds), APR key names (feature.zone_engine.min_zone_width_atr.{equity,fx,futures}), threshold values (1.5/1.0/1.5), mark satisfied
   - SIGNAL-QUALITY-02: update to reflect _I7_I6_EXEMPT deleted, pipeline annotation via _annotate_signal(), SIGNAL_SCHEMA_VERSION v4, mark satisfied
   - stopped_at_entry rate deferred to Phase 127 (no replay data yet)
   
3. **Commit this session's changes** (after agent done and tests green)

4. **STATE.md update** — still says "Phase 126 Plan 1 of 5 executing"

5. **Phase 127** next — clean replay validation (ROADMAP has it deprioritized until 128-130 complete per recent re-ordering; check ROADMAP for current sequence)
</remaining_work>

<decisions_made>
- DB uses functional column names (technical_indicators etc.) per migration 126_revert — do not use tier codes in SQL
- Verification found 3 gaps: REQUIREMENTS.md has stale text for SIGNAL-QUALITY-01 and 02; stopped_at_entry measurement deferred to Phase 127
- 126-07 plan was added to update REQUIREMENTS.md to match actual implementation
- Background agent approach for zone-width test fixes (35+ test files, mechanical updates)
</decisions_made>

<blockers>
- Background agent in flight — cannot commit until it completes and tests are green
- 126-07 REQUIREMENTS.md update not yet executed
</blockers>

## Required Reading (in order)
1. `.planning/phases/126-signal-universe-hardening/126-VERIFICATION.md` — 3 gaps found, understand before executing 126-07
2. `.planning/phases/126-signal-universe-hardening/126-07-PLAN.md` — REQUIREMENTS.md update plan
3. `production/migrations/126_revert_tier_column_names.sql` — confirms DB column names are functional

## Infrastructure State
- Background agent: fixing ~60 unit test failures (zone width, ctf_score, stop_basis, etc.)
- Tests: 90 failures at session start; partially fixed this session; agent handling remainder

<context>
Phase 126 code is done. This session was spent on test triage — 13 distinct root causes across 90 failures, all mechanical (stale test expectations after Phase 126 changes). Biggest category: zone width gate (1.5×ATR) now rejects 0.5×ATR single-point SR zones in tests. Handed off to background agent. Before starting 126-07, wait for agent and verify green tests.
</context>

<next_action>
1. Check if background agent completed (it will notify automatically)
2. Run `.venv/bin/pytest tests/unit/ -q --tb=no` — must be green
3. Execute 126-07: update REQUIREMENTS.md per the 3 gaps in VERIFICATION.md
4. Commit: "fix(tests): update unit tests for Phase 126 zone gate, temporal guard, ECL annotation"
5. Write 126-07-SUMMARY.md
</next_action>
