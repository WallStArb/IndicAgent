---
phase: 126-signal-universe-hardening
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - docs/plans/2026-06-14-phase-126-usdjpy-diagnostic.md
autonomous: true
requirements:
  - SIGNAL-QUALITY-01
must_haves:
  truths:
    - "USDJPY anomaly has a documented verdict (data quality / zone geometry / structural) before any Wave 1+ code runs"
    - "Bar completeness, zone/ATR ratio, and time-of-day distributions for USDJPY are quantified against EURUSD/USDCHF"
  artifacts:
    - path: "docs/plans/2026-06-14-phase-126-usdjpy-diagnostic.md"
      provides: "USDJPY diagnostic findings with verdict section"
      contains: "## Verdict"
  key_links:
    - from: "docs/plans/2026-06-14-phase-126-usdjpy-diagnostic.md"
      to: "Wave 1 zone width gate / Wave 2 plugin completeness"
      via: "verdict gates whether USDJPY data is fit for replay; informs threshold/scope decisions"
      pattern: "Verdict"
---

<objective>
Diagnose why USDJPY performs like an equity ETF (14.39% win rate, -0.380 avg pnl_r) while EURUSD (63.60%) and USDCHF (64.42%) perform as expected for forex pairs. This must be resolved BEFORE any code changes in Waves 1-4, because running a multi-day clean replay (Phase 127) on potentially corrupted instrument data would poison the ML training corpus.

Purpose: Establish whether USDJPY's anomaly is (a) a data quality defect (fix data pipeline now), (b) a zone geometry artifact (confirmed addressed by Wave 1 zone width gate), or (c) a structural property (time-of-day / carry dynamics — document as ML training segment note, add regime tag). This is the Wave 0 data-integrity prerequisite (D-17).

Output: docs/plans/2026-06-14-phase-126-usdjpy-diagnostic.md with three SQL result sets and a verdict.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-signal-universe-hardening/126-CONTEXT.md
@docs/plans/2026-06-14-phase-126-signal-universe-hardening.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the three USDJPY diagnostic SQL queries</name>
  <files>docs/plans/2026-06-14-phase-126-usdjpy-diagnostic.md (raw query output captured here in Task 2)</files>
  <read_first>
    - docs/plans/2026-06-14-phase-126-signal-universe-hardening.md (Wave 0 / P126-00 section, lines 153-201 — the three SQL queries are written there verbatim; do NOT invent new queries)
    - CLAUDE.md (DB query rule: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "..."` — plain `psql -U postgres` fails)
  </read_first>
  <action>
    Run the three diagnostic SQL queries exactly as written in the design doc Wave 0 section against the live `indicagent` database, using the documented psql invocation `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "..."`:

    1. Bar completeness diagnostic (total_bars, valid_bars, avg_range, earliest, latest, trading_days) for symbols USDJPY, EURUSD, USDCHF over the last 60 days from `market_data_ohlcv`.
    2. Zone/ATR ratio comparison (avg_zone_atr_ratio, n, avg_pnl_r) for USDJPY vs EURUSD vs USDCHF from `signal_ledger` joined to `intelligence_features` ATR.
    3. USDJPY by time-of-day (hour_utc, n, avg_pnl_r) to separate Asian session from London/NY.

    The intelligence_features ATR is in the `i1` JSONB column (`(i1->>'atr')::float`). The signal_ledger time column for the join is `timestamp` and the features time column is `ts` (per CLAUDE.md table notes). If a query in the design doc references a column that does not exist (e.g. `fired_at`), adapt the JOIN to the actual columns (`signal_ledger.timestamp` = `intelligence_features.ts`, both filtered on matching `symbol` and `timeframe`) and note the adaptation in the output doc. Do NOT change the analytical intent of the query.

    Capture the full raw result table from each query (do not summarize away rows).
  </action>
  <verify>
    Each of the three queries returns rows (or an explicit empty-set with a documented reason). Raw output captured for all three.
  </verify>
  <done>All three Wave 0 design-doc queries executed against the live DB; raw result tables captured for the diagnostic doc.</done>
  <acceptance_criteria>
    - All three queries from the design doc Wave 0 section were executed against the live DB and returned result sets (or a documented empty set)
    - Query 1 returns one row per symbol for USDJPY, EURUSD, USDCHF (or documents which symbols have zero bars in window)
    - Query 3 returns USDJPY pnl_r broken down by hour_utc
    - Any column-name adaptation from the design doc query (e.g. `fired_at` -> `timestamp`/`ts`) is recorded in the diagnostic doc
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write the USDJPY diagnostic findings doc with verdict</name>
  <files>docs/plans/2026-06-14-phase-126-usdjpy-diagnostic.md</files>
  <read_first>
    - docs/plans/2026-06-14-phase-126-signal-universe-hardening.md (Wave 0 verdict criteria at line 200: "If data quality issue found -> fix data pipeline in this wave. If zone/ATR ratio issue -> confirmed addressed by Wave 1. If structural (time-of-day, carry dynamics) -> document as ML training segment note; add usdjpy regime tag to affected signals.")
    - .planning/phases/126-signal-universe-hardening/126-CONTEXT.md (D-17: Wave 0 must complete before code changes)
  </read_first>
  <action>
    Create docs/plans/2026-06-14-phase-126-usdjpy-diagnostic.md with these sections:

    1. `## Diagnostic Queries` — the exact SQL run (with any adaptations noted) and the raw result tables from Task 1.
    2. `## Analysis` — interpret the three result sets:
       - Bar completeness: is USDJPY missing bars, has invalid bars (high <= low), or abnormal avg_range vs EURUSD/USDCHF?
       - Zone/ATR ratio: is USDJPY's avg_zone_atr_ratio materially smaller than EURUSD/USDCHF (which would mean zone geometry, addressed by Wave 1)?
       - Time-of-day: is the negative pnl_r concentrated in specific hours (Asian session), suggesting a structural/carry property?
    3. `## Verdict` — pick exactly ONE primary cause and state it explicitly:
       - DATA QUALITY: if bar gaps / invalid bars found. Action: specify the data pipeline fix needed in this wave (and create a `.planning/todos/pending/` item if the fix is non-trivial).
       - ZONE GEOMETRY: if USDJPY zones are sub-ATR like equities. Action: state "addressed by Wave 1 zone width gate (P126-01)".
       - STRUCTURAL: if time-of-day or carry dynamics drive it. Action: state it is an ML training segment note; add a `usdjpy` regime tag recommendation for affected signals.
    4. `## Replay Fitness` — one-line conclusion: is USDJPY data fit for the Phase 127 clean replay as training data, and under what conditions (e.g. with a regime segment tag, after a data fix, etc.).

    The verdict must be backed by the numbers in the result tables, not asserted.
  </action>
  <verify>
    File exists with a `## Verdict` section naming exactly one primary cause and a `## Replay Fitness` conclusion.
  </verify>
  <done>Diagnostic doc exists with Diagnostic Queries, Analysis, Verdict (one primary cause), and Replay Fitness sections, all backed by the result tables.</done>
  <acceptance_criteria>
    - `docs/plans/2026-06-14-phase-126-usdjpy-diagnostic.md` exists
    - File contains section headers `## Diagnostic Queries`, `## Analysis`, `## Verdict`, `## Replay Fitness`
    - The `## Verdict` section names exactly one of: DATA QUALITY / ZONE GEOMETRY / STRUCTURAL, with a stated action
    - If verdict is DATA QUALITY and the fix is non-trivial, a corresponding item exists under `.planning/todos/pending/`
    - The verdict cites specific values from the Task 1 result tables (not generic prose)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- docs/plans/2026-06-14-phase-126-usdjpy-diagnostic.md exists with `## Verdict`
- All three design-doc Wave 0 queries were run and their raw output is in the doc
- Verdict names a single primary cause backed by the data
</verification>

<success_criteria>
Maps to design doc Success Criterion 0: "USDJPY diagnostic complete — docs/plans/2026-06-14-phase-126-usdjpy-diagnostic.md exists with verdict: data quality, zone geometry, or structural."
</success_criteria>

<output>
After completion, create `.planning/phases/126-signal-universe-hardening/126-00-SUMMARY.md`
</output>
