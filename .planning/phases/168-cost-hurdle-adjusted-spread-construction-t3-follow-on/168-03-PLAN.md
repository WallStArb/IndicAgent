---
phase: 168-cost-hurdle-adjusted-spread-construction-t3-follow-on
plan: 03
type: execute
wave: 2
depends_on: ["168-01", "168-02"]
files_modified:
  - services/cross_sectional_spread_tracker.py
  - tests/integration/test_cross_sectional_spread_tracker.py
autonomous: true
requirements: [D-01, D-02, D-03]
user_setup: []

must_haves:
  truths:
    - "The same service class produces either construction, selected by a CLI flag, with no duplicated file"
    - "The cost-gated construction writes rows under construction_name ctf_momentum_decile_ls_cost_gated"
    - "The baseline construction's output is bit-identical to before this plan"
    - "Backfilling the cost-gated variant when baseline rows already exist produces the same result as backfilling into an empty table"
  artifacts:
    - path: "services/cross_sectional_spread_tracker.py"
      provides: "construction_variant-parameterized CrossSectionalSpreadTracker writing either partition"
      contains: "ctf_momentum_decile_ls_cost_gated"
    - path: "tests/integration/test_cross_sectional_spread_tracker.py"
      provides: "Cross-construction state-isolation regression test"
      contains: "test_backfill_second_construction_name_isolated"
  key_links:
    - from: "CrossSectionalSpreadTracker._execute_inner"
      to: "hysteresis_legs"
      via: "process_bar leg selection when construction_variant is cost_gated"
      pattern: "hysteresis_legs\\("
    - from: "every construction_spreads query in _execute_inner"
      to: "self.construction_name"
      via: "bound $1 placeholder, never the module constant"
      pattern: "self\\.construction_name"
---

<objective>
Parameterize `CrossSectionalSpreadTracker` so one class produces both constructions, wire
`hysteresis_legs()` into the cost-gated path, and prove the two `construction_name` partitions
cannot leak state into one another.

Purpose: D-02 locks "a second `construction_spreads.construction_name` value computed by the same
service class, parameterized by the new rebalance rule, not a duplicated file. The existing
validated baseline must keep running unmutated so the before/after comparison stays measurable
indefinitely." The single highest-risk mechanical surface here is RESEARCH Pitfall 2 — a
watermark or prior-leg query that still binds the module constant instead of the instance
attribute would seed the new partition's turnover from the old partition's rows and silently
fabricate the very number D-04's gate is measured on.

Output: a `--construction-variant` CLI flag, `self.construction_name` threaded through every
`construction_spreads` query, and an integration test that fails if isolation ever regresses.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/168-cost-hurdle-adjusted-spread-construction-t3-follow-on/168-CONTEXT.md
@.planning/phases/168-cost-hurdle-adjusted-spread-construction-t3-follow-on/168-RESEARCH.md
@.planning/phases/168-cost-hurdle-adjusted-spread-construction-t3-follow-on/168-PATTERNS.md
@.planning/phases/168-cost-hurdle-adjusted-spread-construction-t3-follow-on/168-02-SUMMARY.md
@CLAUDE.md

<interfaces>
Available after Plan 02 (in `services/cross_sectional_spread_tracker.py`):

```python
def hysteresis_legs(
    prior_long: frozenset[str], prior_short: frozenset[str],
    ranked_symbols: Sequence[str], feature_values: Sequence[float],
    decile_fraction: float, margin: float,
) -> tuple[list[str], list[str]] | None: ...

def resolve_hysteresis_margin(cost_bps: Sequence[int], bps_per_feature_unit: float) -> float: ...
def validate_hysteresis_config(bps_per_feature_unit: float) -> None: ...
```

Available after Plan 01 (in `config_state`):
  `alpha.construction.bps_per_feature_unit` (float, > 0)
  `alpha.construction.gross_degradation_tolerance_frac` (float, 0.10)

Existing structure being modified (verified by direct read):

```python
_CONSTRUCTION_NAME = "ctf_momentum_decile_ls"                    # line 111

class CrossSectionalSpreadTracker(BaseBatch):
    job_name = "cross-sectional-spread-tracker"
    compute_version = "1.0.0"
    def __init__(self, db_dsn: str, backfill: bool = False) -> None:   # line 825
        super().__init__(db_dsn)
        self.backfill = backfill
```

`_execute_inner` (line 850) references `_CONSTRUCTION_NAME` at exactly three places:
  - the watermark query, line ~879 (`WHERE construction_name = $1 AND tf = $2`)
  - the prior-leg seed query, line ~928 (same filter)
  - the `write_buffer.append(...)` tuple's first element, line ~982
`_INSERT_SQL` (line 802) takes `construction_name` as `$1`.
The APR load at line 853 is
  `cfg_dict = await _load_apr(conn, extra_like_patterns=["infra.cross_sectional_spread_tracker.%"])`
and `validate_construction_config(...)` is called at line 866, BEFORE any panel work.

CLI entrypoint (line ~1601): `mode_group = parser.add_mutually_exclusive_group()` holding
`--backfill`, `--evaluate-gate`, `--evaluate-attribution`; the final else branch constructs
`CrossSectionalSpreadTracker(db_dsn, backfill=args.backfill).run()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize the tracker by construction_variant and wire in hysteresis</name>
  <files>services/cross_sectional_spread_tracker.py</files>
  <read_first>
    - services/cross_sectional_spread_tracker.py lines 100-115 (module constants), lines 802-830
      (`_INSERT_SQL`, class definition, `__init__`), lines 850-1062 (`_execute_inner` in full —
      the APR load, watermark resolution, the CRITICAL prior-leg-seeding comment block, the
      `process_bar` closure, and the manifest/summary reporting), and lines 1595-1660 (the CLI
      entrypoint's argparse block and dispatch)
    - .planning/phases/168-cost-hurdle-adjusted-spread-construction-t3-follow-on/168-RESEARCH.md
      Pitfall 2 (the CR-01 bug class this change can resurrect)
    - .planning/phases/168-cost-hurdle-adjusted-spread-construction-t3-follow-on/168-PATTERNS.md
      section "CrossSectionalSpreadTracker — construction_variant param"
  </read_first>
  <action>
Add a second module constant beside line 111:
`_CONSTRUCTION_NAME_COST_GATED = "ctf_momentum_decile_ls_cost_gated"`, with a comment stating it
is D-02's parallel construction identity and that the baseline constant must remain untouched so
the before/after comparison stays measurable indefinitely.

Extend the constructor to
`def __init__(self, db_dsn: str, backfill: bool = False, construction_variant: str = "baseline") -> None:`
- Accept exactly two values, `"baseline"` and `"cost_gated"`. Any other value raises `ValueError`
  naming the observed value and the two legal values — fail loud at construction time, before any
  DB connection is opened.
- Set `self.construction_variant = construction_variant` and
  `self.construction_name = _CONSTRUCTION_NAME if construction_variant == "baseline" else _CONSTRUCTION_NAME_COST_GATED`.

Re-point all three `_CONSTRUCTION_NAME` references inside `_execute_inner` to
`self.construction_name`: the watermark query's `$1` binding (line ~879), the prior-leg seed
query's `$1` binding (line ~928), and the `write_buffer.append` tuple's first element (line ~982).
Verify by grep that no `_CONSTRUCTION_NAME` reference remains anywhere inside the class body.
Both queries already bind `construction_name` as `$1`; do not convert either to an f-string
(T-168-02). Do NOT change `_INSERT_SQL`.

In `_execute_inner`'s APR block (after `validate_construction_config(...)` at line ~866), when
`self.construction_variant == "cost_gated"`:
- Read `bps_per_feature_unit = _cfg(cfg_dict, "alpha.construction.bps_per_feature_unit", None)`.
  If it is `None`, raise `RuntimeError` naming the key and instructing the operator to apply
  migration 275 — a missing calibration constant must not silently fall back to a default margin.
- Compute `margin = resolve_hysteresis_margin(cost_bps, bps_per_feature_unit)`, which internally
  calls `validate_hysteresis_config`. Do this BEFORE any panel work begins, alongside the
  existing validation (T-168-01, ASVS V5).
- When `self.construction_variant == "baseline"`, do not read the key at all and set
  `margin = None`; the baseline path must not acquire a new failure mode.

In the `process_bar` closure (line ~958), replace the unconditional
`legs = decile_legs(symbols, feature_values, decile_fraction)` with a variant-dispatched call:
`decile_legs(...)` for `"baseline"`, `hysteresis_legs(prior_long, prior_short, symbols,
feature_values, decile_fraction, margin)` for `"cost_gated"`. Everything downstream of `legs` —
`spread_from_legs`, `one_way_turnover`, `net_spread_by_cost_bps`, the write buffer, the
`prior_long, prior_short = cur_long, cur_short` carry-forward — is UNCHANGED and must not be
edited. Resolve the dispatch once outside the closure (bind a local leg-selection callable
before the closure is defined) rather than branching per bar; this is a hot loop over millions
of panel rows.

Do NOT bump `compute_version`. It stays `"1.0.0"`: the baseline's computed output is bit-identical
after this change, and construction identity is carried by `construction_name`, not
`compute_version`. Bumping it would make new baseline rows spuriously differ from Phase 167's.

Add the CLI flag. `--construction-variant` is NOT part of `mode_group` (it composes with
`--backfill`): add it as a plain argument with `choices=["baseline", "cost_gated"]`,
`default="baseline"`, and help text stating that `cost_gated` writes the
`ctf_momentum_decile_ls_cost_gated` partition using D-01's leg-level cost-derived hysteresis
margin, that it never mutates the baseline partition, and that it requires migration 275's
`alpha.construction.bps_per_feature_unit` to be seeded. Pass it through in the final dispatch:
`CrossSectionalSpreadTracker(db_dsn, backfill=args.backfill, construction_variant=args.construction_variant).run()`.

Extend the run-completion logging and manifest inputs (lines ~1033-1062) with
`construction_name=self.construction_name` and, for the cost-gated variant, `margin` and
`bps_per_feature_unit` — the realized margin must be visible in the manifest, not only in a log
line that rotates away. Also update the module docstring's `Usage:` block with the new invocation
form and add a CORRECTNESS INVARIANTS bullet for D-02 (parallel construction, never in-place
mutation) written in the same voice as the existing bullets.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_cross_sectional_spread_tracker.py -q && .venv/bin/ruff check services/cross_sectional_spread_tracker.py && .venv/bin/python -c "import sys; sys.path.insert(0,'.'); from services.cross_sectional_spread_tracker import CrossSectionalSpreadTracker as T; a=T('postgresql://x/y'); b=T('postgresql://x/y', construction_variant='cost_gated'); assert a.construction_name=='ctf_momentum_decile_ls'; assert b.construction_name=='ctf_momentum_decile_ls_cost_gated'; import pytest; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `CrossSectionalSpreadTracker(dsn).construction_name` equals `ctf_momentum_decile_ls` and
      `CrossSectionalSpreadTracker(dsn, construction_variant="cost_gated").construction_name`
      equals `ctf_momentum_decile_ls_cost_gated`.
    - `CrossSectionalSpreadTracker(dsn, construction_variant="bogus")` raises `ValueError`.
    - No `_CONSTRUCTION_NAME` reference survives inside the class body: the output of
      `sed -n '815,1063p' services/cross_sectional_spread_tracker.py | grep -c '_CONSTRUCTION_NAME'`
      is 0.
    - No construction name is interpolated into SQL text:
      `grep -v '^#' services/cross_sectional_spread_tracker.py | grep -cE 'construction_name = .{0,3}(f"|%s|\{)'`
      returns 0, and both the watermark and prior-leg-seed queries still contain
      `construction_name = $1`.
    - `compute_version` is still exactly `"1.0.0"`:
      `grep -c 'compute_version = "1.0.0"' services/cross_sectional_spread_tracker.py` returns 1.
    - `python services/cross_sectional_spread_tracker.py --help` lists
      `--construction-variant` with choices `baseline` and `cost_gated`, and `--backfill` remains
      combinable with it (they are not in the same mutually-exclusive group).
    - Every pre-existing unit test passes unchanged.
  </acceptance_criteria>
  <done>One service class writes either construction partition, selected by `--construction-variant`; the cost-gated path uses `hysteresis_legs` with an APR-derived margin validated before any panel work; the baseline path is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Prove cross-construction state isolation with an integration test</name>
  <files>tests/integration/test_cross_sectional_spread_tracker.py</files>
  <read_first>
    - tests/integration/test_cross_sectional_spread_tracker.py in full (module docstring's
      ISOLATION contract, `pytestmark`, `_TEST_DB_URL`, `_reset_panel_tables`, `_seed_bars`,
      `_decode_jsonb`, and especially
      `test_cross_sectional_spread_backfill_on_nonempty_table_does_not_fabricate_turnover`
      at line ~428 — the CR-01 regression this new test generalizes to two partitions)
    - services/cross_sectional_spread_tracker.py lines 897-940 (the CRITICAL prior-leg-seeding
      comment block explaining why backfill mode must seed empty frozensets)
    - .planning/phases/168-cost-hurdle-adjusted-spread-construction-t3-follow-on/168-RESEARCH.md
      Pitfall 2
  </read_first>
  <action>
Add `test_backfill_second_construction_name_isolated` to
`tests/integration/test_cross_sectional_spread_tracker.py`, following the file's existing
fixture and isolation discipline exactly (its `pytestmark` of
`[pytest.mark.integration, pytest.mark.requires_db]`, its 12-symbol synthetic universe,
`tf='15m'`, and `_reset_panel_tables` at test start).

The test must seed `alpha.construction.bps_per_feature_unit` in the test database's
`config_state` (a small, finite, positive value chosen so the derived margin is non-trivial
against the synthetic panel's `ctf_momentum` spread of -1000.0 / +1000.0 / `float(sym_idx)` —
pick a value that makes the margin small enough that displacement is still reachable, and assert
the chosen margin in a comment) before constructing the cost-gated tracker.

Assertion structure — the control-versus-contaminated comparison:
1. Seed the synthetic panel via `_seed_bars`.
2. CONTROL run: with `construction_spreads` empty, run
   `CrossSectionalSpreadTracker(dsn, backfill=True, construction_variant="cost_gated")`. Capture
   the full ordered row set for `construction_name = 'ctf_momentum_decile_ls_cost_gated'`:
   `bar_ts`, `long_leg_symbols`, `short_leg_symbols`, `one_way_turnover`, `gross_spread_fast`.
3. Reset `construction_spreads` completely.
4. CONTAMINATED run: first run the BASELINE variant with `backfill=True` (populating
   `ctf_momentum_decile_ls` rows over the identical bars), then run the cost-gated variant with
   `backfill=True`. Capture the same ordered row set for the cost-gated partition.
5. Assert the two captured sequences are equal element-for-element, including that the FIRST
   bar's `one_way_turnover` is `None` in both — that None is the specific value CR-01's bug
   replaced with a fabricated float, and it is the assertion that actually catches a
   construction-name scoping regression.
6. Additionally assert the baseline partition's rows are byte-identical before and after the
   cost-gated run (the cost-gated pass must not mutate, insert into, or delete from the
   `ctf_momentum_decile_ls` partition), and that
   `SELECT count(DISTINCT construction_name) FROM construction_spreads` equals 2 after the
   contaminated run.

Include a named-provenance comment above the assertions citing RESEARCH Pitfall 2 and the CR-01
finding, matching this file's existing comment style.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/integration/test_cross_sectional_spread_tracker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/integration/test_cross_sectional_spread_tracker.py::test_backfill_second_construction_name_isolated -x`
      passes.
    - The test asserts the CONTROL and CONTAMINATED cost-gated row sequences are equal, and
      asserts `one_way_turnover IS None` for the first bar in both.
    - The test asserts the baseline partition's rows are unchanged by the cost-gated run and that
      exactly 2 distinct `construction_name` values exist afterward.
    - Every pre-existing integration test in the file still passes unchanged — in particular
      `test_cross_sectional_spread_backfill_on_nonempty_table_does_not_fabricate_turnover`,
      `test_cross_sectional_spread_incremental_watermark_scoping`, and
      `test_cross_sectional_spread_rerun_is_idempotent`.
    - Deliberately reverting Task 1's watermark-query binding back to `_CONSTRUCTION_NAME` makes
      this new test FAIL (mutation check — run it once, confirm the failure, then restore).
  </acceptance_criteria>
  <done>The isolation test passes and provably fails under a reverted construction-name binding, so a future Pitfall-2 regression cannot land silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI to write path | `--construction-variant` selects which table partition a write pass mutates |
| construction_spreads partition to partition | Two logical constructions share one physical table keyed by `construction_name` |
| config_state to compute | The APR-loaded margin controls which positions are held |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-168-03 | Tampering / Information Disclosure (wrong verdict) | watermark query, prior-leg seed query, insert tuple | mitigate | All three bind `self.construction_name` as `$1`; `test_backfill_second_construction_name_isolated` asserts a control-vs-contaminated equality and is mutation-verified to fail if the binding regresses |
| T-168-02 | Tampering | SQL text construction | mitigate | Construction names remain asyncpg `$1` placeholders; a grep acceptance criterion forbids f-string or `%s` interpolation of any construction name into SQL |
| T-168-01 | Tampering / DoS | `alpha.construction.bps_per_feature_unit` load | mitigate | `resolve_hysteresis_margin` invokes `validate_hysteresis_config` before any panel work; a missing key raises `RuntimeError` naming the key rather than falling back to a default margin |
| T-168-09 | Tampering | operator passes an unknown variant string | mitigate | Constructor raises `ValueError` naming the legal values before opening a DB connection; argparse `choices` blocks it at the CLI layer as defense in depth |
| T-168-10 | Repudiation | which margin a persisted row was computed under | mitigate | `margin` and `bps_per_feature_unit` recorded in the run's `CorpusManifest` inputs, not only in a rotating log line |
| T-168-SC | Tampering | package-manager installs | accept | Zero new packages in this phase |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/ -q` — full unit suite green
- `.venv/bin/pytest tests/integration/test_cross_sectional_spread_tracker.py -q` — all
  integration tests green including the new isolation test
- `.venv/bin/ruff check services/cross_sectional_spread_tracker.py tests/integration/test_cross_sectional_spread_tracker.py`
- `.venv/bin/black --check services/cross_sectional_spread_tracker.py tests/integration/test_cross_sectional_spread_tracker.py`
- `python services/cross_sectional_spread_tracker.py --help` shows `--construction-variant`
- `git diff services/cross_sectional_spread_tracker.py` shows no change to `decile_legs`,
  `spread_from_legs`, `one_way_turnover`, `net_spread_by_cost_bps`, `_INSERT_SQL`, or
  `compute_version`
</verification>

<success_criteria>
- A single `CrossSectionalSpreadTracker` class produces both constructions via
  `--construction-variant`, with no duplicated file (D-02).
- The cost-gated path selects legs via `hysteresis_legs` with an APR-derived, pre-validated
  margin (D-01, D-03).
- Every `construction_spreads` query in the write path binds `self.construction_name`.
- The isolation integration test passes and is mutation-verified.
- The baseline construction is provably unmutated: all pre-existing tests pass unchanged and
  `compute_version` is untouched.
</success_criteria>

<output>
Create `.planning/phases/168-cost-hurdle-adjusted-spread-construction-t3-follow-on/168-03-SUMMARY.md` when done.
Record: the resolved margin value the cost-gated variant computes against live APR, the mutation
check's observed failure mode, and any place where the two constructions' bar sets could diverge.
</output>
