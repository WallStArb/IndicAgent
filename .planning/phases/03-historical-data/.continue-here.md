---
phase: 03-historical-data
plan: 03-02
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-02-24T10:50:41.248Z
---

<current_state>
Phase 3, Plan 03-02 (run backfill + validate). Stage 1 (IBKR fetch) and Stage 2
(intelligence replay) have both completed, but intelligence_features only has 98 rows
(should be ~630K). The 98 rows were written by a debug test session, NOT by Stage 2 itself.
Stage 2 wrote 354,922 signals to signal_ledger but 0 rows to intelligence_features.

Root cause is NOT confirmed but isolated: `_build_intelligence_event` works correctly in
isolation (200/200 test bars succeed), but silently returns None during the actual replay
loop in replay_symbol(). Need to add explicit exception logging to find the actual error.
</current_state>

<completed_work>

- Plan 03-01 COMPLETE (committed): Extended historical_backfill.py with intelligence_features
  write path. 4 new functions: `_INSERT_FEATURE_SYNC_SQL`, `_build_intelligence_event`,
  `_event_to_sync_params`, `_insert_features_sync`. Updated `_build_ledger_entries` and
  `replay_symbol`. 23/23 tests pass. 2 atomic commits.

- ibkr.py chunking fix COMMITTED: Added `_MAX_CHUNK_DAYS` per-timeframe dict and refactored
  `fetch_historical_bars` to loop through date range in ≤6-day chunks with 10s asyncio.sleep
  pacing between chunks. Fixes Error 162 (365-day request exceeded IBKR 7-day limit for 1m bars).

- Stage 1 COMPLETE: 753,415 bars stored across 17 symbols (35 days, 1m bars).
  6 symbols skipped (qualify failed): BZJ6, NGJ6, SR1H6, 6EH6, 6JH6, BTCH6.
  market_data_ohlcv now has 35 days of 1m history.

- Stage 2 COMPLETE (but buggy): 354,922 signals in signal_ledger. intelligence_features
  only has 98 rows (written by debug session, not Stage 2). Bug identified but not fixed.

- signal_ledger: 452,594 rows total (97,672 pre-Phase-3 + 354,922 from Stage 2).
  All pre-existing rows have feature_ts=NULL. Stage 2 rows also have feature_ts=NULL
  because features were never written.

- JOIN integrity: orphaned_signals = 0 (vacuously true — no signal has feature_ts set
  because features were never written).
</completed_work>

<remaining_work>

- Task 3 (current): Debug why Stage 2 writes 0 rows to intelligence_features despite
  _build_intelligence_event working in isolation. Then:
  1. Add exception logging inside replay_symbol bar loop (around _insert_features_sync call)
  2. Truncate intelligence_features (0 real rows exist anyway)
  3. Truncate signal_ledger (optional — remove pre-Phase-3 rows for clean baseline)
  4. Re-run Stage 2: python -u production/scripts/historical_backfill.py --replay-only --timeframes 1m
  5. Validate: intelligence_features >> 0 rows, orphaned_signals = 0, JSONB structure valid

- After Task 3: Create 03-02-SUMMARY.md, update STATE.md + ROADMAP.md

- After 03-02: Phase 3 verification (gsd-verifier) + roadmap completion
</remaining_work>

<decisions_made>

- Used --days 35 for backfill (5 × 7-day chunks) — enough for ML warmup, avoids long waits
- ibkr.py uses 6-day chunks (not 7) as conservative safety margin under IBKR hard limit
- Stage 2 runs --timeframes 1m only (Stage 2 replay only uses 1m bars — higher TFs skipped
  internally because bars_by_tf only has "1m" key)
- signal_ledger NOT truncated before Stage 2 — pre-Phase-3 rows still present with feature_ts=NULL
- intelligence_features table has source='backfill' default='live' so source must be explicitly set

- Redis NOT involved in backfill — backfill writes directly to TimescaleDB, bypassing all
  Redis streams. Live services pick up going forward from the populated DB baseline.
</decisions_made>

<blockers>

- intelligence_features write bug: Stage 2 writes 0 rows to intelligence_features.
  `_build_intelligence_event` wraps entire body in try/except and returns None on ANY exception.
  In isolation (200 test bars) it returns valid events. In actual replay context it returns None.
  Hypothesis: some bar in the actual replay has data that triggers a silent exception inside
  _build_intelligence_event that doesn't occur with the first 200 bars.

  To debug: temporarily replace the try/except with explicit logging:
  ```python
  except Exception as e:
      import traceback
      print(f"_build_intelligence_event failed bar {symbol}/{tf}/{ts}: {e}")
      traceback.print_exc()
      return None
  ```
  Then rerun Stage 2 (or just the first symbol) and check the log.
</blockers>

<context>
The core write path (Plan 03-01) is correct and tested. The Stage 2 execution bug is in
_build_intelligence_event silently swallowing exceptions. The fix is: (1) add logging to
surface the real exception, (2) fix the root cause, (3) re-run Stage 2.

Stage 1 data is solid — 753K bars in market_data_ohlcv. Only Stage 2 needs to be re-run
after fixing the silent exception in _build_intelligence_event.

The ibkr.py chunking fix (committed) is clean and tested — future Stage 1 runs will work.
</context>

<next_action>
Start with: Add exception logging to _build_intelligence_event in historical_backfill.py
to surface the silent failure. Run a quick test of just ESH6:

```bash
source .venv/bin/activate
python -u production/scripts/historical_backfill.py --replay-only --timeframes 1m --symbols ESH6 2>&1 | grep -E "failed|error|Error|exception|Exception" | head -20
```

Then fix the root cause and re-run full Stage 2.
</next_action>
