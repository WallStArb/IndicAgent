---
phase: 163-vp-sr-structural-primitives
plan: 03
type: execute
wave: 3
depends_on: ["163-02"]
files_modified:
  - src/intelligence/feature_factory.py
  - services/backfill_feature_factory.py
  - tests/unit/intelligence/test_support_resistance_primitives.py
autonomous: true
requirements: ["TODO-153", "D-02", "D-04", "D-05", "D-06", "D-14"]

must_haves:
  truths:
    - "sr_support_dist and sr_resist_dist vary across bars (not stuck at 0.0) in both live compute() and batch compute_batch()"
    - "S/R distance is expressed in ATR units (raw percentage from the archived plugin is converted by dividing by atr_val)"
    - "S/R is computed statelessly inline (no FeatureCache mutator) over a bounded per-timeframe lookback, reusing the live find_peaks/find_troughs utilities"
    - "All stale 'requires I3' docstrings are removed (D-05); ctx_SRConsensus is NOT built (D-14)"
  artifacts:
    - path: "src/intelligence/feature_factory.py"
      provides: "inline pivot-clustering S/R with ATR-unit sr_support_dist / sr_resist_dist"
      contains: "find_peaks"
    - path: "tests/unit/intelligence/test_support_resistance_primitives.py"
      provides: "regression test: S/R non-constant + live==batch parity"
      contains: "def test_"
  key_links:
    - from: "src/intelligence/feature_factory.py compute()/compute_batch()"
      to: "find_peaks / find_troughs (src/intelligence/utils.py)"
      via: "inline pivot detection over bounded lookback window"
      pattern: "find_(peaks|troughs)"
    - from: "sr_support_dist / sr_resist_dist"
      to: "atr_val"
      via: "ATR-unit conversion (close - support)/atr, (resist - close)/atr"
      pattern: "atr_val"
---

<objective>
Replace the stubbed `sr_support_dist`/`sr_resist_dist` cache reads with real, stateless,
inline support/resistance computation, closing the S/R half of todo 153. Port
`i3_structure/support_resistance.py`'s pivot-clustering approach (D-02/D-04) directly into
compute()/compute_batch() — same stateless-windowed pattern as CMF/CCI — reusing the already-live
`find_peaks`/`find_troughs` utilities, and convert its percentage distance output to ATR units.
Explicitly NOT building `ctx_SRConsensus`'s multi-plugin confluence (D-14 — deferred until Phase
164's SMC atomics exist). Finish D-05 cleanup by removing the remaining stale "requires I3"
docstrings.

Purpose: The last two frozen-default structural columns start producing real, ATR-normalized,
non-constant values in both live and batch, fully closing todo 153.
Output: Inline S/R computation at both compute sites, a regression test, and clean docstrings.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-vp-sr-structural-primitives/163-CONTEXT.md
@.planning/phases/163-vp-sr-structural-primitives/163-02-SUMMARY.md

<interfaces>
<!-- Port source (D-02/D-04): src/intelligence/features/i3_structure/support_resistance.py -->
find_peaks(data: np.ndarray, n: int) -> list[int]      # src/intelligence/utils.py L15 (live)
find_troughs(data: np.ndarray, n: int) -> list[int]    # src/intelligence/utils.py L41 (live)

Reference clustering (support_resistance.py):
  peaks = find_peaks(high, n=window); troughs = find_troughs(low, n=window)
  cluster pivots within (atr * cluster_atr_mult); nearest resistance ABOVE close, nearest support BELOW close.
  Archived output is PERCENT distance; convert to ATR units (D-02):
    sr_resist_dist = (nearest_resistance - close) / atr_val
    sr_support_dist = (close - nearest_support) / atr_val

APR config (seeded in migration 243, Plan 01; exposed on FeatureFactoryConfig, Plan 01 Task 2):
  config.sr_window (10), config.sr_cluster_atr_mult (0.5), config.sr_lookback_by_tf (dict per tf)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inline stateless S/R computation in compute()/compute_batch() + D-05 docstring cleanup</name>
  <files>src/intelligence/feature_factory.py, services/backfill_feature_factory.py</files>
  <behavior>
    - sr_resist_dist > 0 when a pivot-cluster resistance sits above close; sr_support_dist > 0 when a support sits below close; both change as close moves through the window.
    - Distances are in ATR units (a 1-ATR move to the nearest level yields ~1.0), not percent.
    - When no qualifying pivot exists on a side, or atr_val <= 0, that side falls back to 0.0 without raising.
    - Live compute() and compute_batch() produce identical S/R values for the same ordered bars.
  </behavior>
  <read_first>
    - src/intelligence/features/i3_structure/support_resistance.py (FULL — clustering + nearest-level logic; note it reports PERCENT distance)
    - src/intelligence/utils.py lines 15-63 (find_peaks / find_troughs — n-neighbor semantics)
    - src/intelligence/feature_factory.py lines 3441-3455 (compute() atr_val + current S/R cache reads)
    - src/intelligence/feature_factory.py lines 3768-3841 (compute_batch window + current S/R cache reads left by Plan 02)
    - src/intelligence/feature_factory.py CMF/CCI inline computation (grep "_cmf(" / "range_position") as the stateless-window template
    - services/backfill_feature_factory.py lines 990-1015 (stale "requires I3" docstring)
  </read_first>
  <action>
    (1) Add a stateless helper `_compute_sr_dist_atr(highs, lows, close_, atr_val, tf, config)` in
      feature_factory.py returning (sr_support_dist, sr_resist_dist):
        - lookback = config.sr_lookback_by_tf.get(tf, 120); slice the last `lookback` bars.
        - peaks = find_peaks(highs_window, n=config.sr_window); troughs = find_troughs(lows_window, ...).
        - Cluster pivot prices within (atr_val * config.sr_cluster_atr_mult) — port
          support_resistance.py's _cluster_levels logic (simple sorted single-pass clustering; strength
          scoring is optional and not persisted, so a minimal cluster-to-mean-price is sufficient).
        - nearest_resistance = min cluster level > close; nearest_support = max cluster level < close.
        - Convert to ATR units: sr_resist_dist = (nearest_resistance - close_) / atr_val,
          sr_support_dist = (close_ - nearest_support) / atr_val. Guard atr_val <= 0 and absent
          pivots -> 0.0. Do NOT return percent distances.
    (2) compute() (live): replace the `cache.sr_support_dist` / `cache.sr_resist_dist` reads
      (currently ~3452-3455 for non-1d) with `_compute_sr_dist_atr(highs, lows, close_, atr_val, tf,
      config)`. Keep tf=='1d' behavior consistent with VP (S/R over daily bars is valid — use the
      1d lookback from config; do not force neutral unless the daily window is too short).
    (3) compute_batch(): replace the `cache.sr_support_dist` / `cache.sr_resist_dist` reads Plan 02
      left in place. The per-tf S/R lookback (60-120) exceeds compute_batch's MIN_WINDOW (~40), so
      build the S/R window from the full `bars` list: `bars[max(0, i - lookback + 1):i+1]` -> extract
      high/low arrays. Reuse atr_val already computed at index i.
    (4) D-05 cleanup: remove the stale "requires I3 intraday injection" docstring in
      services/backfill_feature_factory.py (~L990-1015). Confirm no "requires I3" / "I3 intraday"
      strings remain anywhere in feature_factory.py or backfill_feature_factory.py.
    Do NOT add a FeatureCache mutator for S/R — it is stateless per window (D-04). Do NOT import or
    reference ctx_SRConsensus / zone_engine (D-14).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_support_resistance_primitives.py -x -q</automated>
    <automated>grep -rn "requires I3\|I3 intraday" src/intelligence/feature_factory.py services/backfill_feature_factory.py | grep -v '^#' | wc -l | grep -qx 0</automated>
    <automated>grep -c "SRConsensus\|zone_engine\|collect_sr_candidates" src/intelligence/feature_factory.py | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `_compute_sr_dist_atr` exists and is called in both compute() and compute_batch(); no `cache.sr_support_dist` / `cache.sr_resist_dist` reads remain in the non-cold-start paths.
    - S/R distances are ATR-normalized (test asserts a constructed 1-ATR-away level yields ~1.0 within tolerance), not percent.
    - No "requires I3" text remains in feature_factory.py or backfill_feature_factory.py.
    - No reference to ctx_SRConsensus / zone_engine / collect_sr_candidates.
    - `.venv/bin/ruff check src/intelligence/feature_factory.py services/backfill_feature_factory.py` clean.
  </acceptance_criteria>
  <done>sr_support_dist / sr_resist_dist are computed inline in ATR units in both paths; all stale I3 docstrings removed; ctx_SRConsensus untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Regression unit test — S/R non-constant, ATR-unit, live/batch parity</name>
  <files>tests/unit/intelligence/test_support_resistance_primitives.py</files>
  <read_first>
    - src/intelligence/feature_factory.py (compute / compute_batch signatures; _compute_sr_dist_atr)
    - tests/unit/intelligence/test_volume_profile_primitives.py (Plan 02 — reuse its bar-sequence fixture/harness pattern)
    - src/intelligence/features/i3_structure/support_resistance.py (expected clustering behavior for the assertion design)
  </read_first>
  <action>
    Create tests/unit/intelligence/test_support_resistance_primitives.py. Build a bar sequence with
    clear repeated local highs and lows (so pivots and clusters form), ~120 bars, plus a controlled
    micro-case where a resistance level sits exactly ~1 ATR above the final close.
    Tests:
    (a) test_sr_non_constant_batch: run compute_batch; assert sr_support_dist and sr_resist_dist each
      take >1 distinct finite value across bars and are not all 0.0 (the todo-153 regression guard).
    (b) test_sr_in_atr_units: on the controlled micro-case, assert sr_resist_dist is close to 1.0
      (level one ATR above close), NOT a percent value like 0.5-2.0-range-of-percent — i.e. assert
      it equals (level-close)/atr within 1e-6.
    (c) test_sr_live_batch_parity: compare the final bar's sr_* from the live compute() pattern vs the
      matching compute_batch row; assert equal within 1e-6.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_support_resistance_primitives.py -x -q</automated>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_volume_profile_primitives.py tests/unit/intelligence/test_support_resistance_primitives.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All three S/R tests pass and are CI-clean.
    - test_sr_in_atr_units pins the ATR-unit conversion (fails if reverted to percent distance).
    - test_sr_non_constant_batch fails if sr_* is reverted to a constant default.
    - Both VP (Plan 02) and S/R test modules pass together.
  </acceptance_criteria>
  <done>Committed regression tests prove S/R is non-constant, ATR-normalized, and live==batch, fully closing todo 153's measurement gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| market_data_ohlcv_tradeable -> FeatureFactory | Internal validated OHLCV; no external input crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-05 | Tampering (silent wrong data) | S/R persists constant 0.0 or percent-scaled (non-ATR) values | mitigate | test_sr_non_constant_batch + test_sr_in_atr_units are committed regression gates. |
| T-163-06 | Elevation of scope (unapproved complexity) | Accidentally pulling in ctx_SRConsensus / zone_engine dependency chain | mitigate | D-14 enforced; verify grep asserts zero references; stateless inline only. |
| T-163-SC | Tampering (supply chain) | package installs | accept | No new packages; find_peaks/find_troughs already live. |
</threat_model>

<verification>
- pytest test_support_resistance_primitives passes; combined VP+SR suites green.
- sr_support_dist / sr_resist_dist non-constant and ATR-normalized in both paths.
- No "requires I3" text and no ctx_SRConsensus/zone_engine references remain.
</verification>

<success_criteria>
Support/resistance structural primitives compute real, ATR-normalized, non-constant values inline
in both live and batch paths using the simple self-contained pivot-clustering approach (not
ctx_SRConsensus), with regression tests and clean docstrings — fully closing todo 153.
</success_criteria>

<output>
Create `.planning/phases/163-vp-sr-structural-primitives/163-03-SUMMARY.md` when done.
</output>
