---
phase: 44.3-atomic-persistence-ohlcv-unification
plan: "04"
subsystem: api-dashboard-sse
tags: [sse, kafka, dashboard, typescript, signal-scorecard, topic-retirement]
dependency_graph:
  requires: [44.3-01, 44.3-02, 44.3-03]
  provides: [signal-scorecard-from-intelligence-record, intelligence-i7-retired]
  affects: [src/api/routes/sse.py, src/api/main.py, services/signal_generator_service.py, dashboard/src/lib/types.ts]
tech_stack:
  added: []
  patterns:
    - KafkaSSEBroadcaster._extract_signal_scorecard_payload() — inline transform for intelligence.record → signal_scorecard payload shape
    - RankedSignal TypeScript interface updated to match Python BarIntelligenceRecord.ranked_signals schema
key_files:
  created:
    - tests/unit/api/test_sse_intelligence_record.py
  modified:
    - src/api/routes/sse.py
    - src/api/main.py
    - src/core/stream_keys.py
    - services/signal_generator_service.py
    - dashboard/src/lib/types.ts
    - dashboard/src/components/signal-scorecard.tsx
    - tests/unit/service_tests/test_signal_generator_service.py
decisions:
  - "Inline payload transform in KafkaSSEBroadcaster.run() — extracts ranked_signals from BarIntelligenceRecord envelope into signal_scorecard shape {ts, symbol, tf, data}"
  - "RankedSignal TypeScript type updated to match Python schema (plugin/calibrated_confidence/adjusted_rank vs old setup_type/confidence/composite_rank)"
  - "topic_intelligence_i7 marked deprecated in stream_keys.py — function kept for any legacy callers until cleanup"
  - "Producer call to intelligence.i7 removed from signal_generator_service — no producers remain"
metrics:
  duration_seconds: 638
  completed_date: "2026-03-22"
  tasks_completed: 2
  tasks_total: 3
  files_modified: 7
---

# Phase 44.3 Plan 04: SSE Rewire + intelligence.i7 Retirement Summary

One-liner: SSE broadcaster rewired from intelligence.i7 to intelligence.record with inline payload transform; RankedSignal types updated; intelligence.i7 producer retired.

## What Was Built

### Task 1: Rewire SSE broadcaster to intelligence.record (COMPLETE)

- `src/api/routes/sse.py`: Replaced `topic_intelligence_i7` subscription with `topic_intelligence_record` in `_build_topic_list()`
- Added `_extract_signal_scorecard_payload()` static method to `KafkaSSEBroadcaster` — transforms `BarIntelligenceRecord` JSON (double-serialized by the producer) into `{ts, symbol, tf, data}` shape expected by the dashboard
- Added `intelligence.record` → `signal_scorecard` mapping in `_event_name_for_topic()` (before existing `intelligence.i7` check)
- Added `intelligence.record` to `known_prefixes` for env prefix stripping
- `src/api/main.py`: Replaced `topic_intelligence_i7` with `topic_intelligence_record` in `KafkaConsumerClient` subscription list
- `src/core/stream_keys.py`: Added `# DEPRECATED: retired after Phase 44.3` comment to `topic_intelligence_i7()`
- 10 new unit tests all GREEN

### Task 2: Update dashboard RankedSignal type (COMPLETE)

- `dashboard/src/lib/types.ts`: Updated `RankedSignal` interface to match Python `BarIntelligenceRecord.ranked_signals` schema:
  - Old fields: `setup_type`, `confidence`, `composite_rank`, `suppression_reason`, `entry`, `stop`, `target`
  - New fields: `signal_id`, `plugin`, `raw_confidence`, `calibrated_confidence`, `quality_score`, `tod_multiplier`, `adjusted_rank`
  - `is_winner: boolean` was already present
- `dashboard/src/components/signal-scorecard.tsx`:
  - Updated field references: `plugin` (was `setup_type`), `calibrated_confidence` (was `confidence`), `adjusted_rank` (was `composite_rank`)
  - Removed broken `stripPrefix` (was calling undefined function) and `suppressionLabel` (field no longer present)
  - Sorted by `adjusted_rank` descending (higher rank first)

### Task 3 automation: intelligence.i7 producer retired (COMPLETE — awaiting human verify)

- `services/signal_generator_service.py`: Removed backward-compat publish block for `intelligence.i7`; removed `topic_intelligence_i7` import
- Updated test `test_backward_compat_i7_published` → `test_backward_compat_i7_retired` to verify: (1) no publish to `intelligence.i7`, (2) `intelligence.record` IS published
- API restarted: `indicagent-api` service restarted and active
- `docker exec redpanda rpk group list | grep i7` → no consumer groups found

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed broken RankedSignal field names in signal-scorecard.tsx**
- **Found during:** Task 2
- **Issue:** `signal-scorecard.tsx` was referencing `setup_type`, `composite_rank`, `suppression_reason`, and calling `stripPrefix` (undefined). These field names haven't matched the actual Kafka payload since Phase 44.2 — the component was silently showing undefined values.
- **Fix:** Updated all field references to match Python `RankedSignal` schema. Removed `suppressionLabel`/`suppression_reason` (not in new schema). Removed `stripPrefix` (called non-existent function, replaced with `stripPluginPrefix`).
- **Files modified:** `dashboard/src/components/signal-scorecard.tsx`
- **Commit:** 1dc9326

**2. [Rule 1 - Bug] Updated test_backward_compat_i7_published to reflect retirement**
- **Found during:** Task 3
- **Issue:** Existing test was verifying the backward-compat publish that we intentionally removed.
- **Fix:** Updated test to verify retirement: `intelligence.i7` NOT published, `intelligence.record` IS published.
- **Files modified:** `tests/unit/service_tests/test_signal_generator_service.py`
- **Commit:** fd732f7

## Pending: Human Verification (Task 3, Step 3)

Human must verify:
1. Open dashboard → signal scorecard panel shows ranked signals for a symbol
2. Signals render with plugin name, direction, confidence%, eligibility checkmark
3. Winner highlighted correctly (● vs ○)

After verification, Topic retirement:
```bash
docker exec redpanda rpk topic delete development.intelligence.i7
```

## Commits

| Hash | Description |
|------|-------------|
| b1cee22 | feat(44.3-04): rewire SSE broadcaster from intelligence.i7 to intelligence.record |
| 1dc9326 | feat(44.3-04): update dashboard RankedSignal type to match BarIntelligenceRecord schema |
| fd732f7 | chore(44.3-04): retire intelligence.i7 producer and update test |

## Test Results

All 2662 unit tests pass.

## Known Stubs

None — all wiring is complete. Signal scorecard data flows from `BarIntelligenceRecord.ranked_signals` through the SSE broadcaster to the dashboard.

## Self-Check: PASSED

All created/modified files exist. All 3 commits found. 2662 unit tests pass.
