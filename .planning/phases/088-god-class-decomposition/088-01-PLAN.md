---
phase: 088-god-class-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/pipeline/output_queue.py
  - src/intelligence/pipeline/__init__.py
  - services/intelligence_pipeline_agent.py
  - tests/unit/pipeline_tests/__init__.py
  - tests/unit/pipeline_tests/test_output_queue.py
  - tests/unit/pipeline_helpers.py
autonomous: true
must_haves:
  truths:
    - "OutputQueue class lives in src/intelligence/pipeline/output_queue.py and owns the asyncio.Queue, drain loop, enqueue, enqueue_blocking, and join."
    - "Orchestrator no longer owns _output_queue / _output_buffer_drops / _output_buffer_depth / _output_publish_failures attributes — those move to OutputQueue."
    - "Phase 086 contract preserved: enqueue_blocking() awaits put() rather than dropping; drain loop still publishes via producer.publish(topic, msg=value, key=key)."
    - "All existing tests/unit/ tests remain green (no behavior change)."
    - "src/intelligence/pipeline/__init__.py still exports the 6 existing pipeline stage functions plus the new OutputQueue."
  artifacts:
    - path: "src/intelligence/pipeline/output_queue.py"
      provides: "OutputQueue class with __init__(producer, maxsize), enqueue, enqueue_blocking, join, drain_loop, owned OTel metrics"
      contains: "class OutputQueue"
    - path: "src/intelligence/pipeline/__init__.py"
      provides: "Extended re-exports including OutputQueue plus existing stage functions"
      contains: "OutputQueue"
    - path: "tests/unit/pipeline_tests/__init__.py"
      provides: "Marks pipeline_tests as a Python package"
    - path: "tests/unit/pipeline_tests/test_output_queue.py"
      provides: "Isolated unit tests for OutputQueue (drop-on-full, blocking, drain)"
      contains: "def test_"
  key_links:
    - from: "services/intelligence_pipeline_agent.py"
      to: "src/intelligence/pipeline/output_queue.py"
      via: "import OutputQueue from src.intelligence.pipeline"
      pattern: "from src.intelligence.pipeline import .*OutputQueue"
    - from: "services/intelligence_pipeline_agent.py:_setup"
      to: "OutputQueue.__init__"
      via: "self._out_queue = OutputQueue(producer=self._kafka_producer, maxsize=_OUTPUT_QUEUE_MAXSIZE)"
      pattern: "self\\._out_queue\\s*=\\s*OutputQueue"
    - from: "OutputQueue.drain_loop"
      to: "KafkaProducerClient.publish"
      via: "await self._producer.publish(topic, msg=value, key=key)"
      pattern: "self\\._producer\\.publish\\(.*msg="
---

<objective>
Extract OutputQueue as the first DAG node from the IntelligencePipelineComputeAgent god class. This is the most isolated extraction (validates the new module structure with minimal cross-cutting concerns) and preserves the Phase 086 blocking-instead-of-drop contract.

Purpose: Establish the src/intelligence/pipeline/ class-extraction pattern, prove the test directory layout, and remove ~30 lines + 4 attributes from the orchestrator.
Output: Working OutputQueue class wired into the orchestrator, dedicated unit tests, and pipeline_helpers.py updated so __new__-based regression tests still construct a valid agent.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/088-god-class-decomposition/088-CONTEXT.md
@.planning/phases/088-god-class-decomposition/088-RESEARCH.md
@.planning/phases/088-god-class-decomposition/088-PATTERNS.md
@services/intelligence_pipeline_agent.py
@src/intelligence/pipeline/__init__.py
@src/core/kafka_utils.py
@src/observability/metrics.py
@tests/unit/pipeline_helpers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OutputQueue module and pipeline_tests package skeleton</name>
  <files>
    src/intelligence/pipeline/output_queue.py
    src/intelligence/pipeline/__init__.py
    tests/unit/pipeline_tests/__init__.py
  </files>
  <read_first>
    services/intelligence_pipeline_agent.py
    src/intelligence/pipeline/__init__.py
    src/core/kafka_utils.py
    src/observability/metrics.py
    .planning/phases/088-god-class-decomposition/088-PATTERNS.md
  </read_first>
  <action>
    Create new file src/intelligence/pipeline/output_queue.py defining class OutputQueue with constructor signature OutputQueue(producer: KafkaProducerClient, maxsize: int). Move logic from services/intelligence_pipeline_agent.py lines 1030-1057 (_enqueue, _enqueue_blocking, _drain_output) verbatim into public methods enqueue(topic, key, value), async enqueue_blocking(topic, key, value), async drain_loop(running_fn), and async join(). The OTel metrics intelligence_pipeline_output_buffer_drops_total (counter), intelligence_pipeline_output_buffer_depth (gauge), intelligence_pipeline_output_publish_failures_total (counter) are constructed inside __init__ via counter()/gauge() from src.observability.metrics — preserve the exact existing metric names. The drain_loop must continue to call self._producer.publish(topic, msg=value, key=key) — keyword is msg, not value. enqueue_blocking awaits self._queue.put() (Phase 086 contract: block rather than drop). Module-level constant _OUTPUT_QUEUE_MAXSIZE stays in services/intelligence_pipeline_agent.py and is passed in by orchestrator.

    Update src/intelligence/pipeline/__init__.py: keep all 6 existing stage-function imports (apply_calibration, apply_quality_gate, apply_regime_gate, apply_tod_adjustment, rank_signals, select_winner) intact; APPEND a new import line "from src.intelligence.pipeline.output_queue import OutputQueue" and add "OutputQueue" to __all__. Do not remove or reorder any existing entry.

    Create tests/unit/pipeline_tests/__init__.py as an empty file so pytest treats it as a package.
  </action>
  <verify>
    .venv/bin/python -c "from src.intelligence.pipeline import OutputQueue, apply_quality_gate, rank_signals; print(OutputQueue.__name__)"
    .venv/bin/ruff check src/intelligence/pipeline/output_queue.py src/intelligence/pipeline/__init__.py
  </verify>
  <acceptance_criteria>
    src/intelligence/pipeline/output_queue.py contains "class OutputQueue"
    src/intelligence/pipeline/output_queue.py contains "def __init__(self, producer: KafkaProducerClient, maxsize: int)"
    src/intelligence/pipeline/output_queue.py contains "async def enqueue_blocking"
    src/intelligence/pipeline/output_queue.py contains "async def drain_loop"
    src/intelligence/pipeline/output_queue.py contains "async def join"
    src/intelligence/pipeline/output_queue.py contains "msg=value"
    src/intelligence/pipeline/output_queue.py contains "intelligence_pipeline_output_buffer_drops_total"
    src/intelligence/pipeline/output_queue.py contains "intelligence_pipeline_output_buffer_depth"
    src/intelligence/pipeline/output_queue.py contains "intelligence_pipeline_output_publish_failures_total"
    src/intelligence/pipeline/__init__.py contains all original 6 stage function names AND "OutputQueue"
    tests/unit/pipeline_tests/__init__.py exists
    .venv/bin/python -c "from src.intelligence.pipeline import OutputQueue" exits 0
    .venv/bin/ruff check src/intelligence/pipeline/ exits 0
  </acceptance_criteria>
  <done>OutputQueue exists as a standalone module exporting via the package __init__; existing stage exports unaffected; pipeline_tests package created.</done>
</task>

<task type="auto">
  <name>Task 2: Wire OutputQueue into orchestrator and remove migrated attributes</name>
  <files>
    services/intelligence_pipeline_agent.py
    tests/unit/pipeline_helpers.py
  </files>
  <read_first>
    services/intelligence_pipeline_agent.py
    tests/unit/pipeline_helpers.py
    src/intelligence/pipeline/output_queue.py
  </read_first>
  <action>
    In services/intelligence_pipeline_agent.py:
    1. Add import: "from src.intelligence.pipeline import OutputQueue" alongside existing pipeline imports.
    2. Remove __init__ attribute assignments at lines 478 (self._output_queue), 493 (self._output_buffer_depth gauge), 497 (self._output_buffer_drops counter), 501 (self._output_publish_failures counter).
    3. In _setup(): construct self._out_queue = OutputQueue(producer=self._kafka_producer, maxsize=_OUTPUT_QUEUE_MAXSIZE). Replace the existing asyncio.create_task(self._drain_output()) line with asyncio.create_task(self._out_queue.drain_loop(lambda: self._running)). Add the resulting task to self._background_tasks.
    4. Delete the now-orphan methods _enqueue (line 1030), _enqueue_blocking (line 1037), _drain_output (line 1044).
    5. Replace every call site of self._enqueue(...) with self._out_queue.enqueue(...) and every self._enqueue_blocking(...) with await self._out_queue.enqueue_blocking(...). Search the file for both names — there are call sites in _run_i7_inner and _publish_signals_or_dlq among others.
    6. In _teardown(): replace "await asyncio.wait_for(self._output_queue.join(), timeout=10.0)" with "await asyncio.wait_for(self._out_queue.join(), timeout=10.0)".

    In tests/unit/pipeline_helpers.py:
    1. Inside make_agent(): remove the lines that inject agent._output_queue, agent._output_buffer_drops, agent._output_buffer_depth, agent._output_publish_failures.
    2. Add: "from src.intelligence.pipeline.output_queue import OutputQueue" at top, then "agent._out_queue = OutputQueue(producer=MagicMock(), maxsize=500)" (or appropriate MagicMock spec — the existing helper already uses MagicMock for the Kafka producer; reuse that mock instance).
    3. Any test in tests/unit/ that referenced agent._output_queue or the migrated metrics must be updated to reference agent._out_queue (or the new metric handles on the OutputQueue instance). Run pytest after editing and fix any AttributeError fallout.
  </action>
  <verify>
    .venv/bin/ruff check services/intelligence_pipeline_agent.py tests/unit/pipeline_helpers.py
    .venv/bin/pytest tests/unit/ -q
    grep -n "self\._output_queue\|self\._enqueue\b\|self\._enqueue_blocking\|self\._output_buffer_drops\|self\._output_buffer_depth\|self\._output_publish_failures\|self\._drain_output" services/intelligence_pipeline_agent.py
  </verify>
  <acceptance_criteria>
    services/intelligence_pipeline_agent.py contains "from src.intelligence.pipeline import" with OutputQueue in the import list
    services/intelligence_pipeline_agent.py contains "self._out_queue = OutputQueue("
    services/intelligence_pipeline_agent.py does NOT contain "self._output_queue ="
    services/intelligence_pipeline_agent.py does NOT contain "def _enqueue(" or "def _enqueue_blocking(" or "async def _drain_output("
    services/intelligence_pipeline_agent.py contains "self._out_queue.drain_loop"
    services/intelligence_pipeline_agent.py contains "self._out_queue.join()"
    tests/unit/pipeline_helpers.py contains "OutputQueue(" and does NOT contain "agent._output_queue ="
    grep above prints zero matches (no orphan references remain)
    .venv/bin/pytest tests/unit/ -q exits 0
    .venv/bin/ruff check services/intelligence_pipeline_agent.py exits 0
  </acceptance_criteria>
  <done>Orchestrator delegates all output queue work to self._out_queue; no orphan _output_* attributes or _enqueue* methods remain; all unit tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Add dedicated OutputQueue unit tests</name>
  <files>
    tests/unit/pipeline_tests/test_output_queue.py
  </files>
  <read_first>
    src/intelligence/pipeline/output_queue.py
    tests/unit/service_tests/test_bar_writer_agent.py
    .planning/phases/088-god-class-decomposition/088-PATTERNS.md
  </read_first>
  <action>
    Create tests/unit/pipeline_tests/test_output_queue.py with isolated unit tests that construct OutputQueue directly using an AsyncMock producer (no full agent). Required test cases:
    - test_enqueue_non_blocking_drops_on_full: fill queue to maxsize via .enqueue(), then a further .enqueue() returns without raising and the drop is observable (queue size still equals maxsize).
    - test_enqueue_blocking_awaits_on_full: with maxsize=1 and a full queue, await enqueue_blocking() in a task; only completes after a get() drains the queue. Use asyncio.wait_for with a small timeout to confirm it blocks then completes.
    - test_drain_loop_publishes_via_producer_msg_kwarg: enqueue one item, run drain_loop briefly via a running_fn that returns True then False; assert producer.publish was awaited with kwargs containing msg=<value> and key=<key>.
    - test_drain_loop_increments_failure_counter_on_publish_exception: producer.publish raises; assert task_done is still called and the loop continues. Use AsyncMock side_effect.
    - test_join_returns_when_drained: enqueue, drain, then await join() under wait_for(timeout=1.0) and verify it completes.

    All tests use @pytest.mark.asyncio. Producer is created as AsyncMock with publish=AsyncMock(). Do not import IntelligencePipelineComputeAgent.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/pipeline_tests/test_output_queue.py -v
    .venv/bin/ruff check tests/unit/pipeline_tests/test_output_queue.py
  </verify>
  <acceptance_criteria>
    tests/unit/pipeline_tests/test_output_queue.py contains "def test_enqueue_non_blocking_drops_on_full"
    tests/unit/pipeline_tests/test_output_queue.py contains "def test_enqueue_blocking_awaits_on_full"
    tests/unit/pipeline_tests/test_output_queue.py contains "def test_drain_loop_publishes_via_producer_msg_kwarg"
    tests/unit/pipeline_tests/test_output_queue.py contains "def test_drain_loop_increments_failure_counter_on_publish_exception"
    tests/unit/pipeline_tests/test_output_queue.py contains "def test_join_returns_when_drained"
    tests/unit/pipeline_tests/test_output_queue.py does NOT import IntelligencePipelineComputeAgent
    tests/unit/pipeline_tests/test_output_queue.py contains "msg="
    .venv/bin/pytest tests/unit/pipeline_tests/test_output_queue.py -v exits 0 with at least 5 tests passing
    .venv/bin/pytest tests/unit/ -q exits 0
  </acceptance_criteria>
  <done>OutputQueue has 5+ isolated unit tests that all pass; full unit suite remains green.</done>
</task>

</tasks>

<verification>
- .venv/bin/pytest tests/unit/ -q exits 0
- .venv/bin/ruff check src/intelligence/pipeline/ services/intelligence_pipeline_agent.py tests/unit/pipeline_tests/ exits 0
- services/intelligence_pipeline_agent.py no longer defines _enqueue, _enqueue_blocking, _drain_output methods
- All ~4 _output_* attributes removed from the orchestrator __init__
</verification>

<success_criteria>
- OutputQueue is a self-contained module under src/intelligence/pipeline/
- Orchestrator wires it in _setup() and delegates all queue operations to it
- Phase 086 blocking contract preserved
- All existing unit tests still green; new isolated unit tests added
- ARCH-05 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/088-god-class-decomposition/088-01-SUMMARY.md` documenting: lines removed from orchestrator, new OutputQueue surface, any unexpected call sites discovered.
</output>
