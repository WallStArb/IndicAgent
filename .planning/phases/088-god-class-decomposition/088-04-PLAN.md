---
phase: 088-god-class-decomposition
plan: 04
type: execute
wave: 4
depends_on: ["088-01", "088-02", "088-03"]
files_modified:
  - src/intelligence/pipeline/executor.py
  - src/intelligence/pipeline/__init__.py
  - services/intelligence_pipeline_agent.py
  - tests/unit/pipeline_tests/test_executor.py
  - tests/unit/pipeline_helpers.py
autonomous: true
must_haves:
  truths:
    - "PluginExecutor owns ThreadPoolExecutor, plugin cache, instrument map, circuit breakers, plugin call counts, _ANALYSIS_WAVES, reload_hmm_parameters."
    - "PluginExecutor holds NO reference to PluginStateManager — it receives state: dict and lock: threading.Lock as per-call parameters per D-10."
    - "PluginExecutor holds NO reference to CacheManager — shadow_cache is passed as a per-call parameter to run_tiers per D-07 and Pitfall 2."
    - "_collect_plugin_results returns (outputs, state_updates) tuple; orchestrator calls state_mgr.update_batch(state_updates) per Pitfall 3."
    - "PluginExecutor.shutdown() called by orchestrator.stop(); the orchestrator's ThreadPoolExecutor attribute is renamed self._thread_pool (held by executor); the PluginExecutor instance is stored as self._executor per D-06."
    - "All existing tests/unit/ tests green; orchestrator no longer holds _plugin_cache / _instrument_map / _plugin_circuit_breakers / _plugin_call_counts / _plugin_skipped_total attributes; the bare self._executor attribute now refers to the PluginExecutor instance (D-06)."
  artifacts:
    - path: "src/intelligence/pipeline/executor.py"
      provides: "PluginExecutor with __init__(thread_pool, plugin_cache, instrument_map, circuit_breakers), run_tiers, run_i1, run_tier, run_i7_plugins, reload_hmm_parameters, shutdown, _get_plugin_cb, _is_shadow, _collect_plugin_results, _ANALYSIS_WAVES classvar"
      contains: "class PluginExecutor"
    - path: "src/intelligence/pipeline/__init__.py"
      provides: "Extended exports including PluginExecutor"
      contains: "PluginExecutor"
    - path: "tests/unit/pipeline_tests/test_executor.py"
      provides: "Isolated unit tests for PluginExecutor (run_tiers returns state_updates, _is_shadow reads passed cache, circuit breaker opens, shutdown)"
      contains: "def test_"
  key_links:
    - from: "services/intelligence_pipeline_agent.py:_process_bar_inner"
      to: "PluginExecutor.run_tiers"
      via: "tiered, state_updates = await self._executor.run_tiers(state, lock, bar, symbol, tf, frames, shadow_cache=self._cache_mgr.shadow_cache)"
      pattern: "self\\._executor\\.run_tiers"
    - from: "services/intelligence_pipeline_agent.py:_process_bar_inner"
      to: "PluginStateManager.update_batch"
      via: "self._state_mgr.update_batch(state_updates)"
      pattern: "update_batch\\(state_updates\\)"
    - from: "services/intelligence_pipeline_agent.py:stop"
      to: "PluginExecutor.shutdown"
      via: "self._executor.shutdown()"
      pattern: "self\\._executor\\.shutdown"
---

<objective>
Extract PluginExecutor as the fourth DAG node. This is the largest extraction (~400 lines): owns the thread pool, the I1/analysis waves/I7-plugin execution, circuit breakers, and the plugin cache. It must be DB-ignorant and state-pure — receiving state/lock/shadow_cache as per-call parameters (D-10, D-07) so the orchestrator remains the only writer to PluginStateManager.

Purpose: Encode the typed-DAG-node contract in the most behavior-rich class; prove that state/cache are passed in (not held) so the Phase 089 plugin-state-as-parameter refactor has a clean interface to plug into; eliminate ~250 lines from the orchestrator.
Output: PluginExecutor class wired into the orchestrator, dedicated unit tests, pipeline_helpers.py updated; orchestrator _process_bar_inner becomes near-DAG-shaped for plugin work.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/088-god-class-decomposition/088-CONTEXT.md
@.planning/phases/088-god-class-decomposition/088-RESEARCH.md
@.planning/phases/088-god-class-decomposition/088-PATTERNS.md
@.planning/phases/088-god-class-decomposition/088-02-SUMMARY.md
@.planning/phases/088-god-class-decomposition/088-03-SUMMARY.md
@services/intelligence_pipeline_agent.py
@src/intelligence/pipeline/__init__.py
@src/intelligence/pipeline/state_manager.py
@src/intelligence/register_plugins.py
@src/observability/circuit_breaker.py
@src/observability/metrics.py
@tests/unit/pipeline_helpers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PluginExecutor module with state-as-parameter interface</name>
  <files>
    src/intelligence/pipeline/executor.py
    src/intelligence/pipeline/__init__.py
  </files>
  <read_first>
    services/intelligence_pipeline_agent.py
    src/intelligence/pipeline/__init__.py
    src/intelligence/register_plugins.py
    src/observability/circuit_breaker.py
    src/observability/metrics.py
    .planning/phases/088-god-class-decomposition/088-PATTERNS.md
  </read_first>
  <action>
    Create src/intelligence/pipeline/executor.py defining class PluginExecutor with constructor PluginExecutor(thread_pool: ThreadPoolExecutor, plugin_cache: dict, instrument_map: dict, circuit_breakers: dict).

    __init__ creates:
      - self._thread_pool, self._plugin_cache, self._instrument_map, self._plugin_circuit_breakers, self._logger
      - self._plugin_call_counts: dict = {}
      - OTel metrics owned here (D-16) using exact existing names: self._plugin_skipped_total = counter("intelligence_pipeline_plugin_skipped_total", "Plugin executions skipped due to asset class mismatch"); plus any other executor-specific metric currently created in the god class. Module-level shared metrics PLUGIN_DURATION_MS, PLUGIN_ERRORS_TOTAL, CIRCUIT_BREAKER_STATE, FEATURES_COMPUTED_TOTAL imported directly from src.observability.metrics (do not recreate).

    Class-level constant _ANALYSIS_WAVES (port verbatim from god class line 1186):
      tuple containing 4 waves of ((tier_key, tier_plugins),) — using TIER_I3, TIER_I5, TIER_I6, I2_WAVE_A, I2_WAVE_B, I4_WAVE_A, I4_WAVE_B, SMC_WAVE_A, SMC_WAVE_B from src.intelligence.register_plugins.

    Methods (port from god class with the state/shadow-cache parameter change):
      - shutdown() -> None: self._thread_pool.shutdown(wait=True) (god class line 551)
      - _get_plugin_cb(plugin_name) -> CircuitBreaker: lazy-init from god class line 559. Use Phase 086 manual-tracking pattern (allow_request / record_success / record_failure outside .call()).
      - _is_shadow(plugin_name: str, shadow_cache: dict) -> bool: port from god class line 573 but READ from the passed shadow_cache parameter rather than self._shadow_cache (Pitfall 2).
      - _collect_plugin_results(tasks: list, results: list, lock: threading.Lock, log_prefix: str = "plugin") -> tuple[list[dict], dict]: port from god class line 1069 but MODIFY to return (outputs, state_updates) tuple per D-10/Pitfall 3. state_updates is dict mapping task.state_key -> output.pop("_state"). Do NOT write to any external state — the orchestrator calls state_mgr.update_batch on the returned dict. The lock is the per-(symbol,tf) lock passed in for serialized state extraction.
      - async run_i1(state: dict, lock: threading.Lock, frames, symbol, tf, shadow_cache: dict) -> tuple[dict, dict]: port god class _run_i1 (line 1123) but accept state and lock as parameters. Plugins still use the existing plugin._state = state[plugin_name] mutation pattern (Phase 089 deferred) — read state from the parameter dict instead of self._plugin_states. Returns (tier_outputs, state_updates).
      - async run_tier(tier_key, tier_plugins, state: dict, lock: threading.Lock, symbol, tf, frames, loop, shadow_cache: dict) -> tuple[dict, dict]: port god class _run_tier (line 1193) with same parameter changes; returns (tier_outputs, state_updates).
      - async run_tiers(state: dict, lock: threading.Lock, bar, symbol, tf, frames: dict, shadow_cache: dict) -> tuple[dict, dict]: NEW public entry point per D-08. Calls run_i1 then iterates _ANALYSIS_WAVES calling run_tier for each, accumulating both tiered_results and state_updates. Returns the consolidated (tiered, state_updates).
      - async run_i7_plugins(state, lock, bar, symbol, tf, plugin_input, shadow_cache) -> tuple[list[dict], dict]: extract the I7-plugin-execution portion of god class _run_i7_inner (the part that runs I7 plugins via TIER_I7 and returns raw signals + state_updates). The signal-pipeline-stages portion stays in plan 05 (SignalProcessor). Returns (raw_signals_list, state_updates).
      - reload_hmm_parameters() -> list[str]: port from god class line 750 (iterates TIER_SMC plugins in self._plugin_cache and calls plugin.reload_parameters()). Returns list of reloaded plugin names.

    Imports: asyncio, threading, time, concurrent.futures.ThreadPoolExecutor, typing.Any, typing.ClassVar, structlog, src.core.service_utils.should_skip_plugin, all the tier constants from src.intelligence.register_plugins, src.observability.circuit_breaker.{CircuitBreaker, CircuitState}, src.observability.metrics.{CIRCUIT_BREAKER_STATE, PLUGIN_DURATION_MS, PLUGIN_ERRORS_TOTAL, counter, gauge}, the PluginTask dataclass from services.intelligence_pipeline_agent (or move PluginTask alongside if it has no other consumers — check first; if only the executor uses it, move it into executor.py module scope).

    Update src/intelligence/pipeline/__init__.py: add "from src.intelligence.pipeline.executor import PluginExecutor" and append "PluginExecutor" to __all__.
  </action>
  <verify>
    .venv/bin/python -c "from src.intelligence.pipeline import PluginExecutor; from concurrent.futures import ThreadPoolExecutor; ex = PluginExecutor(thread_pool=ThreadPoolExecutor(max_workers=1), plugin_cache={}, instrument_map={}, circuit_breakers={}); print(ex._ANALYSIS_WAVES is not None)"
    .venv/bin/ruff check src/intelligence/pipeline/executor.py src/intelligence/pipeline/__init__.py
  </verify>
  <acceptance_criteria>
    src/intelligence/pipeline/executor.py contains "class PluginExecutor"
    src/intelligence/pipeline/executor.py contains "def __init__(self, thread_pool: ThreadPoolExecutor, plugin_cache: dict, instrument_map: dict, circuit_breakers: dict)"
    src/intelligence/pipeline/executor.py contains "async def run_tiers"
    src/intelligence/pipeline/executor.py contains "async def run_i1"
    src/intelligence/pipeline/executor.py contains "async def run_tier"
    src/intelligence/pipeline/executor.py contains "async def run_i7_plugins"
    src/intelligence/pipeline/executor.py contains "def reload_hmm_parameters"
    src/intelligence/pipeline/executor.py contains "def shutdown"
    src/intelligence/pipeline/executor.py contains "_ANALYSIS_WAVES"
    src/intelligence/pipeline/executor.py contains "shadow_cache: dict" (parameter, not attribute)
    src/intelligence/pipeline/executor.py contains "state: dict" (parameter)
    src/intelligence/pipeline/executor.py contains "state_updates" (return value)
    src/intelligence/pipeline/executor.py does NOT contain "self._shadow_cache" (Pitfall 2 — no lateral cache reference)
    src/intelligence/pipeline/executor.py does NOT contain "self._plugin_states" (D-10 — no lateral state reference)
    src/intelligence/pipeline/executor.py does NOT import "from src.intelligence.pipeline.state_manager" and does NOT import "from src.intelligence.pipeline.cache_manager"
    src/intelligence/pipeline/__init__.py contains "PluginExecutor"
    .venv/bin/python -c "from src.intelligence.pipeline import PluginExecutor" exits 0
    .venv/bin/ruff check src/intelligence/pipeline/ exits 0
  </acceptance_criteria>
  <done>PluginExecutor module exists with state and shadow_cache as per-call parameters; no lateral imports from sibling modules; all 5 public async methods present.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PluginExecutor into orchestrator and remove migrated methods</name>
  <files>
    services/intelligence_pipeline_agent.py
    tests/unit/pipeline_helpers.py
  </files>
  <read_first>
    services/intelligence_pipeline_agent.py
    tests/unit/pipeline_helpers.py
    src/intelligence/pipeline/executor.py
  </read_first>
  <action>
    In services/intelligence_pipeline_agent.py:
    1. Add import "from src.intelligence.pipeline import PluginExecutor".
    2. Rename the orchestrator's ThreadPoolExecutor attribute from self._executor to self._thread_pool (search for all uses — it is constructed in __init__ and used in _executor.submit and _executor.shutdown). After this rename the bare self._executor name is FREE for use by the PluginExecutor instance per D-06.
    3. Remove __init__ assignments for self._plugin_circuit_breakers (line 481), self._plugin_call_counts (line 535), self._plugin_skipped_total counter (line 536). Keep self._plugin_cache (lines 422-426) and self._instrument_map (line 428) until end of step 4 below.
    4. In _setup(): after CacheManager construction (plan 03), construct: self._executor = PluginExecutor(thread_pool=self._thread_pool, plugin_cache=self._plugin_cache, instrument_map=self._instrument_map, circuit_breakers={}). After construction, you may delete the orchestrator's self._plugin_cache / self._instrument_map attributes (they are now owned by the executor; if the orchestrator still needs read-only access for SIGUSR1 reload, access via self._executor._plugin_cache or expose an executor method). Per D-06 the attribute is named self._executor.
    5. Delete the orchestrator methods _get_plugin_cb (line 559), _is_shadow (line 573), _update_plugin_state (line 1063), _collect_plugin_results (line 1069), _run_i1 (line 1123), _run_tier (line 1193), _run_analysis_pipeline (line 1250), and the I7-plugin-execution portion of _run_i7_inner (keep only the signal-pipeline-stages portion — that comes out in plan 05). The class-level _ANALYSIS_WAVES (line 1186) moves to executor.py and is removed from the orchestrator.
    6. Rewrite _process_bar_inner DAG section: replace the existing plugin-execution code with:
       state = self._state_mgr.get_state(key)
       lock = self._state_mgr.get_lock(key)
       tiered, state_updates = await self._executor.run_tiers(state, lock, bar, symbol, tf, frames, shadow_cache=self._cache_mgr.shadow_cache)
       self._state_mgr.update_batch(state_updates)
       Then call the existing _run_i7 (still in orchestrator until plan 05) — but factor it: extract the I7-plugin portion via await self._executor.run_i7_plugins(state, lock, bar, symbol, tf, plugin_input, shadow_cache=self._cache_mgr.shadow_cache) and pass raw_signals into the still-orchestrator-resident _run_i7 signal-stage code.
    7. SIGUSR1 reload: rewrite the handler so reloaded_names = self._executor.reload_hmm_parameters() replaces the old in-orchestrator iteration over TIER_SMC.
    8. stop(): the old self._executor.shutdown() now refers to the PluginExecutor instance (correct, per D-06). Confirm self._thread_pool is shut down by self._executor.shutdown() (which calls self._thread_pool.shutdown(wait=True) internally) — the orchestrator should NOT also call self._thread_pool.shutdown directly (would be a double-shutdown).

    In tests/unit/pipeline_helpers.py make_agent():
    1. Remove agent._plugin_circuit_breakers, agent._plugin_call_counts, agent._plugin_skipped_total injections.
    2. Rename agent._executor injection to agent._thread_pool (a ThreadPoolExecutor MagicMock or real thread pool with max_workers=1).
    3. Add "from src.intelligence.pipeline.executor import PluginExecutor" then "agent._executor = PluginExecutor(thread_pool=agent._thread_pool, plugin_cache=agent._plugin_cache if hasattr(agent,'_plugin_cache') else {}, instrument_map=agent._instrument_map if hasattr(agent,'_instrument_map') else {}, circuit_breakers={})". Per D-06 the attribute on the agent fixture is named agent._executor.
    4. Any test reading agent._plugin_circuit_breakers, _plugin_call_counts, _plugin_skipped_total, calling agent._run_i1 / _run_tier / _collect_plugin_results / _is_shadow directly — update to use agent._executor.<method> or the executor's internal attributes. The existing test_pipeline_attribution.py / test_pipeline_determinism.py / test_pipeline_exception_isolation.py / test_pipeline_parallelization.py exercise these paths; trace through each and update assertions/setup accordingly.
  </action>
  <verify>
    .venv/bin/ruff check services/intelligence_pipeline_agent.py tests/unit/pipeline_helpers.py
    .venv/bin/pytest tests/unit/ -q
    grep -n "def _run_i1\b\|def _run_tier\b\|def _collect_plugin_results\|def _is_shadow\|def _get_plugin_cb\|def _update_plugin_state\|def _run_analysis_pipeline\|self\._plugin_circuit_breakers\|self\._plugin_call_counts\|self\._plugin_skipped_total" services/intelligence_pipeline_agent.py
  </verify>
  <acceptance_criteria>
    services/intelligence_pipeline_agent.py contains "self._executor = PluginExecutor("
    services/intelligence_pipeline_agent.py contains "await self._executor.run_tiers"
    services/intelligence_pipeline_agent.py contains "self._state_mgr.update_batch(state_updates)"
    services/intelligence_pipeline_agent.py contains "self._executor.shutdown()"
    services/intelligence_pipeline_agent.py contains "self._executor.reload_hmm_parameters"
    services/intelligence_pipeline_agent.py contains "self._thread_pool" (the underlying ThreadPoolExecutor)
    services/intelligence_pipeline_agent.py does NOT contain "self._executor_obj" (D-06 rejects this name)
    services/intelligence_pipeline_agent.py does NOT contain "def _run_i1"
    services/intelligence_pipeline_agent.py does NOT contain "def _run_tier"
    services/intelligence_pipeline_agent.py does NOT contain "def _collect_plugin_results"
    services/intelligence_pipeline_agent.py does NOT contain "def _is_shadow"
    services/intelligence_pipeline_agent.py does NOT contain "def _get_plugin_cb"
    services/intelligence_pipeline_agent.py does NOT contain "def _update_plugin_state"
    services/intelligence_pipeline_agent.py does NOT contain "_ANALYSIS_WAVES"
    grep above prints zero matches (no leftover bare references to the removed methods/attrs)
    tests/unit/pipeline_helpers.py contains "agent._executor = PluginExecutor(" and does NOT contain "agent._plugin_circuit_breakers ="
    tests/unit/pipeline_helpers.py does NOT contain "agent._executor_obj"
    .venv/bin/pytest tests/unit/ -q exits 0
    .venv/bin/ruff check services/intelligence_pipeline_agent.py exits 0
  </acceptance_criteria>
  <done>Orchestrator delegates all plugin execution to PluginExecutor (stored as self._executor per D-06) with state/lock/shadow_cache passed per call; ThreadPoolExecutor renamed to self._thread_pool; no lateral coupling between executor and state_mgr/cache_mgr; all unit tests green including the 5 pipeline regression test files.</done>
</task>

<task type="auto">
  <name>Task 3: Add dedicated PluginExecutor unit tests</name>
  <files>
    tests/unit/pipeline_tests/test_executor.py
  </files>
  <read_first>
    src/intelligence/pipeline/executor.py
    src/observability/circuit_breaker.py
    .planning/phases/088-god-class-decomposition/088-PATTERNS.md
  </read_first>
  <action>
    Create tests/unit/pipeline_tests/test_executor.py with isolated unit tests. Required cases:
    - test_is_shadow_reads_from_passed_cache: instantiate PluginExecutor; call ex._is_shadow("plugin_a", shadow_cache={"plugin_a": True}) returns True; same call with shadow_cache={} returns False. Asserts D-07 compliance: shadow state is a parameter, not an attribute.
    - test_collect_plugin_results_returns_state_updates: build a list of fake PluginTasks with results containing dicts with "_state" keys; call _collect_plugin_results; assert returned tuple is (outputs, state_updates) where state_updates is a dict mapping task.state_key to the popped _state. Verify no side-effect writes to any external dict.
    - test_get_plugin_cb_lazy_init: first call to _get_plugin_cb("foo") returns a CircuitBreaker; second call returns the same instance (is-equality); circuit_breakers dict now contains "foo".
    - test_circuit_breaker_opens_after_threshold_failures: get cb for "foo"; call cb.record_failure() 3 times; assert cb.state == CircuitState.OPEN.
    - test_shutdown_calls_thread_pool_shutdown: ex.shutdown() should call self._thread_pool.shutdown(wait=True); use a MagicMock thread_pool to assert the call.
    - test_run_tiers_returns_state_updates_dict: stub plugin cache with a tiny TIER_I1 plugin that returns {"x": 1, "_state": {"seen": True}}; call await ex.run_tiers(state={}, lock=threading.Lock(), bar=fake_bar, symbol="ES", tf="1m", frames={}, shadow_cache={}); assert returned tuple's second element contains the state update keyed by the plugin's state_key.
    - test_no_lateral_imports: open src/intelligence/pipeline/executor.py source via inspect; assert "from src.intelligence.pipeline.state_manager" not in source and "from src.intelligence.pipeline.cache_manager" not in source (architectural assertion).

    Use real ThreadPoolExecutor(max_workers=1) for tests that exercise run_*. Mark async tests with @pytest.mark.asyncio. Do not import IntelligencePipelineComputeAgent.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/pipeline_tests/test_executor.py -v
    .venv/bin/pytest tests/unit/ -q
    .venv/bin/ruff check tests/unit/pipeline_tests/test_executor.py
  </verify>
  <acceptance_criteria>
    tests/unit/pipeline_tests/test_executor.py contains "def test_is_shadow_reads_from_passed_cache"
    tests/unit/pipeline_tests/test_executor.py contains "def test_collect_plugin_results_returns_state_updates"
    tests/unit/pipeline_tests/test_executor.py contains "def test_get_plugin_cb_lazy_init"
    tests/unit/pipeline_tests/test_executor.py contains "def test_circuit_breaker_opens_after_threshold_failures"
    tests/unit/pipeline_tests/test_executor.py contains "def test_shutdown_calls_thread_pool_shutdown"
    tests/unit/pipeline_tests/test_executor.py contains "def test_no_lateral_imports"
    tests/unit/pipeline_tests/test_executor.py does NOT import IntelligencePipelineComputeAgent
    .venv/bin/pytest tests/unit/pipeline_tests/test_executor.py -v exits 0 with at least 6 tests passing
    .venv/bin/pytest tests/unit/ -q exits 0
  </acceptance_criteria>
  <done>PluginExecutor has 6+ isolated unit tests including an architectural assertion that prevents future lateral coupling regressions; full suite green.</done>
</task>

</tasks>

<verification>
- .venv/bin/pytest tests/unit/ -q exits 0
- .venv/bin/ruff check src/intelligence/pipeline/ services/intelligence_pipeline_agent.py tests/unit/pipeline_tests/ exits 0
- src/intelligence/pipeline/executor.py does NOT import from state_manager or cache_manager (architectural)
- Orchestrator no longer defines _run_i1 / _run_tier / _collect_plugin_results / _is_shadow / _get_plugin_cb / _update_plugin_state / _ANALYSIS_WAVES
- All 5 existing pipeline regression tests (test_pipeline_attribution / determinism / exception_isolation / parallelization / recorder_wiring) pass
</verification>

<success_criteria>
- PluginExecutor owns all plugin-execution concerns
- ARCH-01 requirement satisfied
- Pitfall 2 (shadow_cache parameter, not attribute) verified by test
- Pitfall 3 (state_updates returned, not written) verified by test
- D-10 interface ready for Phase 089 (plugin state as parameter)
- D-06 attribute naming honored: self._executor = PluginExecutor instance; self._thread_pool = ThreadPoolExecutor
</success_criteria>

<output>
After completion, create `.planning/phases/088-god-class-decomposition/088-04-SUMMARY.md` documenting: ~250 line reduction in orchestrator, PluginExecutor surface, the run_tiers DAG entry point, and confirmation that executor has zero imports from sibling extracted modules.
</output>
