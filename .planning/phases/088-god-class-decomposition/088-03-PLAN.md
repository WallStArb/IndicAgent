---
phase: 088-god-class-decomposition
plan: 03
type: execute
wave: 3
depends_on: ["088-01", "088-02"]
files_modified:
  - src/intelligence/pipeline/cache_manager.py
  - src/intelligence/pipeline/__init__.py
  - services/intelligence_pipeline_agent.py
  - tests/unit/pipeline_tests/test_cache_manager.py
  - tests/unit/pipeline_helpers.py
autonomous: true
must_haves:
  truths:
    - "CacheManager owns exactly 6 cache dicts (perf_weights, cis_weights, calibration_curves, shadow_cache, drift_penalties, tod_priors) plus _cis_kalman_params (config-file loaded in __init__). _pattern_reliability is deleted without rehoming — it is a dead attribute (written at lines 450 and 632 of the god class but never read anywhere)."
    - "CacheManager.start_refresh_loops() creates and returns 6 background asyncio.Tasks at unchanged intervals (3600, 14400, 1800, 1800, 14400, 300)."
    - "Atomic dict replacement preserved for 5 of 6 caches; tod_priors preserves merge semantics ({**self._tod_priors, **priors})."
    - "_load_cis_weights does NOT call cis_scorer.update_weights — that mediation moves to the orchestrator (D-07 / Pitfall 4)."
    - "CacheManager.update_hmm_regime(hmm_val) is called by the orchestrator per bar; current_hmm_regime_label() drives _load_perf_weights regime conditioning."
    - "CacheManager exposes a uniform public seed API (seed_tod_priors, seed_perf_weights, seed_cis_weights, seed_calibration_curves, seed_shadow_cache, seed_drift_penalties) so tests and checkpoint restore do not reach into private dicts."
    - "All existing tests/unit/ tests green; orchestrator no longer holds 8+ cache attributes."
  artifacts:
    - path: "src/intelligence/pipeline/cache_manager.py"
      provides: "CacheManager(db, settings) with 7 properties, start_refresh_loops, update_hmm_regime, current_hmm_regime_label, cis_weights_version, 6 seed_* methods, _load_* private methods"
      contains: "class CacheManager"
    - path: "src/intelligence/pipeline/__init__.py"
      provides: "Extended exports including CacheManager"
      contains: "CacheManager"
    - path: "tests/unit/pipeline_tests/test_cache_manager.py"
      provides: "Isolated unit tests for CacheManager (refresh loop, tod_priors merge, atomic replacement, hmm regime label, seed API)"
      contains: "def test_"
  key_links:
    - from: "services/intelligence_pipeline_agent.py:_setup"
      to: "CacheManager.start_refresh_loops"
      via: "for task in self._cache_mgr.start_refresh_loops(): self._background_tasks.add(task)"
      pattern: "self\\._cache_mgr\\.start_refresh_loops"
    - from: "services/intelligence_pipeline_agent.py"
      to: "CacheManager.update_hmm_regime"
      via: "self._cache_mgr.update_hmm_regime(hmm_val) called inside _run_i7_inner"
      pattern: "update_hmm_regime"
    - from: "CacheManager._load_tod_multipliers"
      to: "self._tod_priors merge"
      via: "self._tod_priors = {**self._tod_priors, **priors}"
      pattern: "\\{\\*\\*self\\._tod_priors"
---

<objective>
Extract CacheManager as the third DAG node. This is the largest plan in Wave 1 (~300 lines): owns 6 refresh loops and all DB cache reads. CacheManager has zero lateral references — it pushes data via properties and its own background loops. The orchestrator only stores the task handles and calls update_hmm_regime per bar.

Purpose: Centralize all cache state into one self-managing class; eliminate ~200 lines from orchestrator; preserve all 8 critical entanglements (CIS scorer mediation, tod_priors merge, hmm regime conditioning, kalman config-file load, etc.). Expose a uniform seed_* API so tests and checkpoint restore never need to mutate private dicts.
Output: CacheManager class wired into orchestrator, dedicated unit tests, pipeline_helpers.py updated; all 6 _load_* and _refresh_* methods removed from god class.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/088-god-class-decomposition/088-CONTEXT.md
@.planning/phases/088-god-class-decomposition/088-RESEARCH.md
@.planning/phases/088-god-class-decomposition/088-PATTERNS.md
@services/intelligence_pipeline_agent.py
@src/intelligence/pipeline/__init__.py
@src/core/database_manager.py
@src/monitoring/ks_drift_monitor.py
@tests/unit/pipeline_helpers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CacheManager module with 6 refresh loops, properties, and seed API</name>
  <files>
    src/intelligence/pipeline/cache_manager.py
    src/intelligence/pipeline/__init__.py
  </files>
  <read_first>
    services/intelligence_pipeline_agent.py
    src/intelligence/pipeline/__init__.py
    src/core/database_manager.py
    src/monitoring/ks_drift_monitor.py
    .planning/phases/088-god-class-decomposition/088-PATTERNS.md
  </read_first>
  <action>
    Create src/intelligence/pipeline/cache_manager.py defining class CacheManager with constructor signature CacheManager(db: DatabaseManager, settings: Settings).

    __init__ creates:
      - self._db, self._settings, self._logger = structlog.get_logger(__name__)
      - 6 cache dicts initialized to {}: _perf_weights, _cis_weights, _calibration_curves, _shadow_cache, _drift_penalties, _tod_priors
      - self._cis_kalman_params: dict = _load_cis_kalman_params()  — move the helper function from god class line 171 into this module (one-time JSON read; no refresh loop per D-13 list)
      - self._last_hmm_regime: int | None = None
      - self._cis_weights_version: int = 0

    Public read surface as @property: perf_weights, cis_weights, cis_kalman_params, calibration_curves, drift_penalties, shadow_cache, tod_priors. Each returns the underlying dict (atomic replacement makes property read GIL-safe).

    Public methods:
      - cis_weights_version (property) -> int
      - update_hmm_regime(hmm_val: int | None) -> None  (sets self._last_hmm_regime)
      - current_hmm_regime_label() -> str  — port god class _current_hmm_regime_label at line 1757 verbatim: returns "mean_reversion" if hmm==0, "trend" if hmm in (1,2), else "all"
      - async _run_refresh_loop(load_fn, interval_sec: int) -> None  — port from god class line 1748 (while True: sleep then load; log+continue on exception)
      - start_refresh_loops() -> list[asyncio.Task]  — returns 6 tasks at intervals (perf_weights=3600, drift_penalties=14400, cis_weights=1800, calibration_curves=1800, tod_multipliers=14400, shadow_cache=300)

    **Uniform public seed API (NEW — for tests and checkpoint restore):**
      - seed_tod_priors(priors: dict) -> None: self._tod_priors = {**self._tod_priors, **priors}  (MERGE semantics — Pitfall 7)
      - seed_perf_weights(weights: dict) -> None: self._perf_weights = dict(weights)  (atomic replace)
      - seed_cis_weights(weights: dict, version: int = 0) -> None: self._cis_weights = dict(weights); if version: self._cis_weights_version = int(version)  (atomic replace; never calls cis_scorer per Pitfall 4)
      - seed_calibration_curves(curves: dict) -> None: self._calibration_curves = dict(curves)
      - seed_shadow_cache(cache: dict) -> None: self._shadow_cache = dict(cache)
      - seed_drift_penalties(penalties: dict) -> None: self._drift_penalties = dict(penalties)
      All seed methods are the SINGLE allowed way to populate caches from outside the class (tests, checkpoint restore, repair tooling). Direct mutation of private _* attributes is forbidden — pipeline_helpers.py and tests MUST use these.

    Private async loaders — port verbatim from god class, with these adjustments:
      - _load_perf_weights (line 1771): uses self.current_hmm_regime_label() instead of self._current_hmm_regime_label(); atomic replacement self._perf_weights = weights at end.
      - _load_shadow_cache (line 1839): atomic replacement.
      - _refresh_drift_penalties (line 1850): atomic replacement of self._drift_penalties. Imports DRIFT_PENALTIES from src.monitoring.ks_drift_monitor as before.
      - _load_cis_weights (line 1854): atomic replacement of self._cis_weights AND self._cis_weights_version = int(row["version"]). CRITICAL: do NOT call any cis_scorer.update_weights here (Pitfall 4 — orchestrator mediates).
      - _load_calibration_curves (line 1871): atomic replacement.
      - _load_tod_multipliers (line 1900): MERGE semantics: self._tod_priors = {**self._tod_priors, **priors} — do NOT atomic-replace (Pitfall 7).

    Imports: asyncio, json, pathlib.Path, typing.Any, structlog, src.core.database_manager.DatabaseManager, src.config.settings.Settings, src.monitoring.ks_drift_monitor.DRIFT_PENALTIES, and whatever helpers _load_perf_weights currently uses (numpy if needed).

    Update src/intelligence/pipeline/__init__.py: add "from src.intelligence.pipeline.cache_manager import CacheManager" and append "CacheManager" to __all__. Preserve all prior exports.
  </action>
  <verify>
    .venv/bin/python -c "from src.intelligence.pipeline import CacheManager; from unittest.mock import MagicMock; cm = CacheManager(db=None, settings=MagicMock()); cm.seed_perf_weights({'a':1.0}); print(cm.perf_weights); print(cm.current_hmm_regime_label())"
    .venv/bin/ruff check src/intelligence/pipeline/cache_manager.py src/intelligence/pipeline/__init__.py
  </verify>
  <acceptance_criteria>
    src/intelligence/pipeline/cache_manager.py contains "class CacheManager"
    src/intelligence/pipeline/cache_manager.py contains "def __init__(self, db: DatabaseManager, settings: Settings)"
    src/intelligence/pipeline/cache_manager.py contains "def start_refresh_loops"
    src/intelligence/pipeline/cache_manager.py contains "@property" appearing at least 7 times
    src/intelligence/pipeline/cache_manager.py contains "def current_hmm_regime_label"
    src/intelligence/pipeline/cache_manager.py contains "def update_hmm_regime"
    src/intelligence/pipeline/cache_manager.py contains "def seed_tod_priors"
    src/intelligence/pipeline/cache_manager.py contains "def seed_perf_weights"
    src/intelligence/pipeline/cache_manager.py contains "def seed_cis_weights"
    src/intelligence/pipeline/cache_manager.py contains "def seed_calibration_curves"
    src/intelligence/pipeline/cache_manager.py contains "def seed_shadow_cache"
    src/intelligence/pipeline/cache_manager.py contains "def seed_drift_penalties"
    src/intelligence/pipeline/cache_manager.py contains "{**self._tod_priors, **priors}"
    src/intelligence/pipeline/cache_manager.py does NOT contain "cis_scorer.update_weights" or "_cis_scorer.update_weights"
    src/intelligence/pipeline/cache_manager.py does NOT contain "_pattern_reliability" (dead attribute — never rehomed here)
    src/intelligence/pipeline/cache_manager.py contains "3600" and "14400" and "1800" and "300" (refresh intervals)
    src/intelligence/pipeline/__init__.py contains "CacheManager"
    .venv/bin/python -c "from src.intelligence.pipeline import CacheManager" exits 0
    .venv/bin/ruff check src/intelligence/pipeline/ exits 0
  </acceptance_criteria>
  <done>CacheManager module exists with all 6 loaders, 7 properties, refresh loop runner, hmm helpers, and 6 public seed_* methods; importable; no lateral references to other extracted classes; _pattern_reliability NOT included (deleted as dead code).</done>
</task>

<task type="auto">
  <name>Task 2: Wire CacheManager into orchestrator and mediate cross-class concerns</name>
  <files>
    services/intelligence_pipeline_agent.py
    tests/unit/pipeline_helpers.py
  </files>
  <read_first>
    services/intelligence_pipeline_agent.py
    tests/unit/pipeline_helpers.py
    src/intelligence/pipeline/cache_manager.py
  </read_first>
  <action>
    In services/intelligence_pipeline_agent.py:
    1. Add import "from src.intelligence.pipeline import CacheManager".
    2. Remove __init__ attribute assignments for: _perf_weights (line 467), _cis_weights_cache (line 464), _cis_kalman_params (line 465), _calibration_curves (line 466), _drift_penalties (line 468), _shadow_cache (line 419), _tod_priors (line 444), _last_hmm_regime (line 470). Also remove _pattern_reliability (line 450) — NOTE: _pattern_reliability is a dead attribute (written at lines 450 and 632 of the god class but never read anywhere). Delete it without rehoming. Do NOT add it to CacheManager — adding it would perpetuate dead code. Also remove the module-level _load_cis_kalman_params helper (it moved to cache_manager.py) and the _current_hmm_regime_label method (line 1757) and _run_refresh_loop method (line 1748) and all 6 _load_*/_refresh_* loader methods (lines 1771, 1839, 1850, 1854, 1871, 1900).
    3. In _setup() (after PluginStateManager is constructed per plan 02): construct self._cache_mgr = CacheManager(db=self._db, settings=self.settings). Then: for task in self._cache_mgr.start_refresh_loops(): self._background_tasks.add(task); task.add_done_callback(self._background_tasks.discard).
    4. CIS scorer mediation: in the same per-bar path where the orchestrator currently reads cis_weights (search for self._cis_weights_cache uses), insert a sync check — if self._cache_mgr.cis_weights_version != self._last_synced_cis_version: self._cis_scorer.update_weights(self._cache_mgr.cis_weights, self._cache_mgr.cis_weights_version); self._last_synced_cis_version = self._cache_mgr.cis_weights_version. Add self._last_synced_cis_version = 0 to __init__. (After plan 05 lands, this mediation moves into SignalProcessor.sync_cis_weights called from the orchestrator.)
    5. HMM regime update: in the bar processing path where the god class currently does self._last_hmm_regime = hmm_val (search _run_i7_inner), replace with self._cache_mgr.update_hmm_regime(hmm_val).
    6. Restore checkpointed tod_priors: in _setup() after read_checkpoint, replace any direct self._tod_priors assignment with self._cache_mgr.seed_tod_priors(extra.get("tod_priors", {})). Remove the orchestrator's self._tod_priors attribute entirely.
    7. Update _assemble_checkpoint_extra() (from plan 02) to read self._cache_mgr.tod_priors instead of self._tod_priors. Also update the interim _assemble_checkpoint_extra acceptance test added in plan 02 — it should now assert tod_priors flows from self._cache_mgr (not orchestrator attr). kalman_state and setup_last_fire remain on the orchestrator until plan 05.
    8. Replace all read-side references to the migrated dicts with the cache_mgr property: self._perf_weights -> self._cache_mgr.perf_weights, self._calibration_curves -> self._cache_mgr.calibration_curves, self._drift_penalties -> self._cache_mgr.drift_penalties, self._shadow_cache -> self._cache_mgr.shadow_cache, self._cis_weights_cache -> self._cache_mgr.cis_weights, self._cis_kalman_params -> self._cache_mgr.cis_kalman_params, self._tod_priors -> self._cache_mgr.tod_priors. Audit every call site in _run_i7_inner, _is_shadow, _run_i1, _run_tier, _publish_signals_or_dlq.
    9. _is_shadow(plugin_name) currently reads self._shadow_cache — update to read self._cache_mgr.shadow_cache (this method stays in the orchestrator for now; plan 04 moves it into PluginExecutor with shadow_cache passed as a per-call param).

    In tests/unit/pipeline_helpers.py make_agent():
    1. Remove all agent._perf_weights / _cis_weights_cache / _cis_kalman_params / _calibration_curves / _drift_penalties / _shadow_cache / _tod_priors / _last_hmm_regime injections. Also remove agent._pattern_reliability — it is dead code and is being deleted, not migrated.
    2. Add "from src.intelligence.pipeline.cache_manager import CacheManager" then "from unittest.mock import AsyncMock, MagicMock" then "db = AsyncMock(); db.execute_query = AsyncMock(return_value=[]); agent._cache_mgr = CacheManager(db=db, settings=agent.settings if hasattr(agent,'settings') else MagicMock())".
    3. Add "agent._last_synced_cis_version = 0".
    4. Fix any test that reads agent._perf_weights or sibling caches directly — replace with agent._cache_mgr.<property> for reads. **For test seeding, use the public seed API** — agent._cache_mgr.seed_perf_weights({...}), agent._cache_mgr.seed_cis_weights({...}, version=1), agent._cache_mgr.seed_calibration_curves({...}), agent._cache_mgr.seed_shadow_cache({...}), agent._cache_mgr.seed_drift_penalties({...}), agent._cache_mgr.seed_tod_priors({...}). **Do NOT mutate private dicts** like agent._cache_mgr._perf_weights.update(...) — that bypasses the public contract and is brittle.
  </action>
  <verify>
    .venv/bin/ruff check services/intelligence_pipeline_agent.py tests/unit/pipeline_helpers.py
    .venv/bin/pytest tests/unit/ -q
    grep -n "self\._perf_weights\|self\._cis_weights_cache\|self\._calibration_curves\|self\._drift_penalties\|self\._shadow_cache\|self\._tod_priors\|self\._last_hmm_regime\|self\._cis_kalman_params\b\|def _load_perf_weights\|def _load_cis_weights\|def _load_calibration_curves\|def _load_shadow_cache\|def _load_tod_multipliers\|def _refresh_drift_penalties\|def _run_refresh_loop\|def _current_hmm_regime_label\|self\._pattern_reliability" services/intelligence_pipeline_agent.py
    grep -n "_cache_mgr\._perf_weights\|_cache_mgr\._cis_weights\|_cache_mgr\._calibration_curves\|_cache_mgr\._shadow_cache\|_cache_mgr\._drift_penalties\|_cache_mgr\._tod_priors" tests/unit/
  </verify>
  <acceptance_criteria>
    services/intelligence_pipeline_agent.py contains "from src.intelligence.pipeline import" with CacheManager in list
    services/intelligence_pipeline_agent.py contains "self._cache_mgr = CacheManager("
    services/intelligence_pipeline_agent.py contains "self._cache_mgr.start_refresh_loops"
    services/intelligence_pipeline_agent.py contains "self._cache_mgr.update_hmm_regime"
    services/intelligence_pipeline_agent.py contains "self._cache_mgr.seed_tod_priors"
    services/intelligence_pipeline_agent.py contains "self._last_synced_cis_version"
    services/intelligence_pipeline_agent.py does NOT contain "self._perf_weights ="
    services/intelligence_pipeline_agent.py does NOT contain "self._tod_priors ="
    services/intelligence_pipeline_agent.py does NOT contain "self._pattern_reliability" (dead attribute deleted without rehoming)
    services/intelligence_pipeline_agent.py does NOT contain "def _load_perf_weights"
    services/intelligence_pipeline_agent.py does NOT contain "def _load_cis_weights"
    services/intelligence_pipeline_agent.py does NOT contain "def _load_tod_multipliers"
    services/intelligence_pipeline_agent.py does NOT contain "def _run_refresh_loop"
    services/intelligence_pipeline_agent.py does NOT contain "def _current_hmm_regime_label"
    grep -n "self._pattern_reliability" services/intelligence_pipeline_agent.py returns no results
    first grep prints zero matches (only _cache_mgr references should appear for cache state)
    second grep prints zero matches (tests use public seed_* API, not private _* attrs)
    tests/unit/pipeline_helpers.py contains "CacheManager(" and does NOT contain "agent._perf_weights ="
    tests/unit/pipeline_helpers.py does NOT contain "agent._pattern_reliability"
    The plan-02 interim _assemble_checkpoint_extra test is updated to assert tod_priors flows from self._cache_mgr.tod_priors (orchestrator no longer owns the attr)
    .venv/bin/pytest tests/unit/ -q exits 0
    .venv/bin/ruff check services/intelligence_pipeline_agent.py exits 0
  </acceptance_criteria>
  <done>Orchestrator delegates all cache reads to CacheManager properties; _pattern_reliability deleted (dead code — never read); tests seed via public seed_* API only; refresh loops self-managed; CIS scorer mediation in orchestrator (will move to SignalProcessor in plan 05); tod_priors merge preserved; all unit tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Add dedicated CacheManager unit tests</name>
  <files>
    tests/unit/pipeline_tests/test_cache_manager.py
  </files>
  <read_first>
    src/intelligence/pipeline/cache_manager.py
    .planning/phases/088-god-class-decomposition/088-PATTERNS.md
  </read_first>
  <action>
    Create tests/unit/pipeline_tests/test_cache_manager.py with isolated unit tests. Required cases:
    - test_initial_caches_are_empty_dicts: properties perf_weights, cis_weights, calibration_curves, shadow_cache, drift_penalties, tod_priors all equal {} on fresh instance.
    - test_update_hmm_regime_drives_label: update_hmm_regime(0) -> current_hmm_regime_label() == "mean_reversion"; update_hmm_regime(1) -> "trend"; update_hmm_regime(2) -> "trend"; update_hmm_regime(None) -> "all"; update_hmm_regime(5) -> "all".
    - test_seed_tod_priors_merge_preserves_existing: seed_tod_priors({"a": 1.0}); seed_tod_priors({"b": 2.0}); assert tod_priors == {"a": 1.0, "b": 2.0}.
    - test_seed_perf_weights_atomic_replace: seed_perf_weights({"a": 1.0}); seed_perf_weights({"b": 2.0}); assert perf_weights == {"b": 2.0} (atomic replace, not merge).
    - test_seed_cis_weights_sets_version: seed_cis_weights({"a": 1.0}, version=3); assert cis_weights == {"a": 1.0} and cis_weights_version == 3.
    - test_seed_api_does_not_call_scorer: assert "cis_scorer" not in inspect.getsource(CacheManager) (Pitfall 4 architectural assertion).
    - test_load_tod_multipliers_merge_not_replace: monkeypatch self._db.execute_query to return a row producing priors {"b": 9.0}; preload via seed_tod_priors({"a": 1.0}); await _load_tod_multipliers; assert tod_priors contains both "a":1.0 and "b":9.0 (Pitfall 7 regression test).
    - test_load_perf_weights_atomic_replacement: seed via seed_perf_weights({"stale": 1.0}); await _load_perf_weights (db returns rows); assert self.perf_weights is a new dict containing only fresh data.
    - test_load_cis_weights_does_not_call_scorer: monkeypatch _db to return cis_weights row; await _load_cis_weights; assert self.cis_weights and self.cis_weights_version are set; no exception about missing scorer.
    - test_start_refresh_loops_returns_six_tasks: cancel each task immediately; assert len(tasks) == 6 and all isinstance(t, asyncio.Task).

    Use AsyncMock for db; mark async tests with @pytest.mark.asyncio. Do not import IntelligencePipelineComputeAgent.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/pipeline_tests/test_cache_manager.py -v
    .venv/bin/pytest tests/unit/ -q
    .venv/bin/ruff check tests/unit/pipeline_tests/test_cache_manager.py
  </verify>
  <acceptance_criteria>
    tests/unit/pipeline_tests/test_cache_manager.py contains "def test_update_hmm_regime_drives_label"
    tests/unit/pipeline_tests/test_cache_manager.py contains "def test_seed_tod_priors_merge_preserves_existing"
    tests/unit/pipeline_tests/test_cache_manager.py contains "def test_seed_perf_weights_atomic_replace"
    tests/unit/pipeline_tests/test_cache_manager.py contains "def test_seed_cis_weights_sets_version"
    tests/unit/pipeline_tests/test_cache_manager.py contains "def test_load_tod_multipliers_merge_not_replace"
    tests/unit/pipeline_tests/test_cache_manager.py contains "def test_load_perf_weights_atomic_replacement"
    tests/unit/pipeline_tests/test_cache_manager.py contains "def test_load_cis_weights_does_not_call_scorer"
    tests/unit/pipeline_tests/test_cache_manager.py contains "def test_start_refresh_loops_returns_six_tasks"
    tests/unit/pipeline_tests/test_cache_manager.py does NOT import IntelligencePipelineComputeAgent
    .venv/bin/pytest tests/unit/pipeline_tests/test_cache_manager.py -v exits 0 with at least 9 tests passing
    .venv/bin/pytest tests/unit/ -q exits 0
  </acceptance_criteria>
  <done>CacheManager has 9+ isolated unit tests covering each entanglement (merge, atomic replacement, no scorer call, hmm label, 6 tasks, full seed API); full suite green.</done>
</task>

</tasks>

<verification>
- .venv/bin/pytest tests/unit/ -q exits 0
- .venv/bin/ruff check src/intelligence/pipeline/ services/intelligence_pipeline_agent.py tests/unit/pipeline_tests/ exits 0
- Orchestrator has zero _perf_weights/_cis_weights_cache/_calibration_curves/_drift_penalties/_shadow_cache/_tod_priors/_pattern_reliability attributes
- All 6 _load_* methods and _run_refresh_loop removed from orchestrator
- _load_cis_weights does NOT call cis_scorer.update_weights anywhere in CacheManager
- Tests use public seed_* API exclusively (no private _* mutations in tests/unit/)
- grep -n "self._pattern_reliability" services/intelligence_pipeline_agent.py returns no results
</verification>

<success_criteria>
- CacheManager owns exactly 6 caches and their refresh schedule; _pattern_reliability deleted (dead code, not migrated)
- ARCH-04 requirement satisfied
- tod_priors merge semantics preserved (Pitfall 7)
- CIS scorer mediation lives in orchestrator (until plan 05 moves it into SignalProcessor)
- Uniform seed_* public API is the single contract for outside writers (tests, checkpoint restore)
</success_criteria>

<output>
After completion, create `.planning/phases/088-god-class-decomposition/088-03-SUMMARY.md` documenting: ~200 line reduction in orchestrator, CacheManager surface (7 properties + start_refresh_loops + update_hmm_regime + 6 seed_* methods + current_hmm_regime_label + cis_weights_version), and the temporary CIS-sync code in the orchestrator that plan 05 will absorb.
</output>
