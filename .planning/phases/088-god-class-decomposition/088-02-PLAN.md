---
phase: 088-god-class-decomposition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/pipeline/state_manager.py
  - src/intelligence/pipeline/__init__.py
  - services/intelligence_pipeline_agent.py
  - tests/unit/pipeline_tests/test_state_manager.py
  - tests/unit/pipeline_helpers.py
autonomous: true
must_haves:
  truths:
    - "PluginStateManager owns _plugin_states dict, per-key threading.Lock dict, and the checkpoint file (read + write)."
    - "PluginStateManager exposes write_checkpoint(extra_state: dict) accepting cross-owned snapshot fields assembled by the orchestrator."
    - "PluginStateManager owns its own background checkpoint loop via start_checkpoint_loop(interval_sec, get_extra_fn); orchestrator no longer calls _write_local_checkpoint directly on every bar."
    - "Phase 086 contract preserved: write_checkpoint raises on failure (no swallow)."
    - "All existing tests/unit/ tests green; orchestrator no longer holds _plugin_states / _plugin_states_locks attributes."
    - "_CHECKPOINT_FIELDS spans 3 owners but PluginStateManager is the only file writer; cross-owned fields are passed in via extra_state arg."
  artifacts:
    - path: "src/intelligence/pipeline/state_manager.py"
      provides: "PluginStateManager class with get_state, get_lock, update, update_batch, get_all_states, write_checkpoint(extra_state), read_checkpoint, start_checkpoint_loop"
      contains: "class PluginStateManager"
    - path: "src/intelligence/pipeline/__init__.py"
      provides: "Extended exports including PluginStateManager"
      contains: "PluginStateManager"
    - path: "tests/unit/pipeline_tests/test_state_manager.py"
      provides: "Isolated unit tests for PluginStateManager (lazy-init lock, checkpoint roundtrip, raise-on-failure, background loop)"
      contains: "def test_"
  key_links:
    - from: "services/intelligence_pipeline_agent.py:_setup"
      to: "PluginStateManager.start_checkpoint_loop"
      via: "ckpt_task = self._state_mgr.start_checkpoint_loop(300, self._assemble_checkpoint_extra)"
      pattern: "start_checkpoint_loop\\("
    - from: "services/intelligence_pipeline_agent.py:_teardown"
      to: "PluginStateManager.write_checkpoint"
      via: "self._state_mgr.write_checkpoint(self._assemble_checkpoint_extra())"
      pattern: "self\\._state_mgr\\.write_checkpoint"
    - from: "PluginStateManager.write_checkpoint"
      to: "src.core.state_serializer._tag_value"
      via: "payload[k] = _tag_value(v)"
      pattern: "_tag_value\\("
---

<objective>
Extract PluginStateManager as the second DAG node. This class owns plugin state dicts, per-key threading locks, and the checkpoint file. It also introduces a self-managed background checkpoint loop (Renaissance automation principle: caller starts it once, class owns the timing).

Purpose: Single writer for plugin_states; eliminate ~70 lines from orchestrator; lay the cross-owned-checkpoint pattern (extra_state passed in) that plans 03 and 05 will plug into.
Output: PluginStateManager class wired into the orchestrator, dedicated unit tests, pipeline_helpers.py updated, _write_local_checkpoint / _read_local_checkpoint methods removed from the god class.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/088-god-class-decomposition/088-CONTEXT.md
@.planning/phases/088-god-class-decomposition/088-RESEARCH.md
@.planning/phases/088-god-class-decomposition/088-PATTERNS.md
@services/intelligence_pipeline_agent.py
@src/intelligence/pipeline/__init__.py
@src/core/state_serializer.py
@tests/unit/pipeline_helpers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PluginStateManager module with checkpoint and background loop</name>
  <files>
    src/intelligence/pipeline/state_manager.py
    src/intelligence/pipeline/__init__.py
  </files>
  <read_first>
    services/intelligence_pipeline_agent.py
    src/intelligence/pipeline/__init__.py
    src/core/state_serializer.py
    .planning/phases/088-god-class-decomposition/088-PATTERNS.md
  </read_first>
  <action>
    Create src/intelligence/pipeline/state_manager.py defining class PluginStateManager with constructor signature PluginStateManager(checkpoint_path: Path).

    Move these constants from services/intelligence_pipeline_agent.py (lines ~307-310) into state_manager.py module scope: _CHECKPOINT_PATH (default Path("cache/pipeline_checkpoint.json")), _CHECKPOINT_FIELDS = ("plugin_states", "kalman_state", "tod_priors", "last_bar_offset", "setup_last_fire"), _AGENT_VERSION = "v1". Keep the same exact names — orchestrator will still import _CHECKPOINT_PATH from this module to pass into the constructor.

    Public methods:
      - get_state(key: tuple) -> dict: returns self._plugin_states.get(key, {})
      - get_lock(key: tuple) -> threading.Lock: lazy-init (port logic from god class _get_state_lock at line 567)
      - update(key: tuple, state: dict) -> None: self._plugin_states[key] = state
      - update_batch(state_updates: dict) -> None: self._plugin_states.update(state_updates) — interface required for Plan 04
      - get_all_states() -> dict: dict(self._plugin_states)
      - write_checkpoint(extra_state: dict) -> None: ports god class _write_local_checkpoint (lines 1565-1573). Serializes payload {"version": _AGENT_VERSION, "ts": isoformat, "plugin_states": _tag_value(self._plugin_states), **{k: _tag_value(v) for k,v in extra_state.items()}}. Writes atomically via .tmp + rename. RAISES on failure (Phase 086 contract D-15) — do NOT swallow exceptions.
      - async read_checkpoint() -> dict | None: ports god class _read_local_checkpoint (lines 1575-1598). Restores self._plugin_states (using _restore_tuple_key helper inlined or imported from src.core.state_serializer if exposed; otherwise replicate the same key-restoration as god class). Returns dict of remaining _CHECKPOINT_FIELDS (excluding "plugin_states") for the orchestrator to distribute — keys: "kalman_state", "tod_priors", "last_bar_offset", "setup_last_fire". Returns None on file-miss or version-mismatch.
      - start_checkpoint_loop(interval_sec: int, get_extra_fn: Callable[[], dict]) -> asyncio.Task: creates and returns an asyncio.Task running an internal _loop coroutine. The loop does while True: await asyncio.sleep(interval_sec); self.write_checkpoint(get_extra_fn()). On exception inside the loop, log and re-raise (D-15). Orchestrator stores the returned task in _background_tasks.

    Imports: asyncio, json, threading, datetime (UTC, datetime), pathlib.Path, typing.Any, typing.Callable, structlog, from src.core.state_serializer import _ensure_default_models_registered, _tag_value, _untag_value (plus _restore_tuple_key if defined there; otherwise port the helper locally).

    Update src/intelligence/pipeline/__init__.py: add "from src.intelligence.pipeline.state_manager import PluginStateManager" and append "PluginStateManager" to __all__. Preserve all prior exports including OutputQueue if plan 01 already merged.
  </action>
  <verify>
    .venv/bin/python -c "from src.intelligence.pipeline import PluginStateManager; pm = PluginStateManager(__import__('pathlib').Path('/tmp/test.json')); print(pm.get_state(('x','1m')))"
    .venv/bin/ruff check src/intelligence/pipeline/state_manager.py src/intelligence/pipeline/__init__.py
  </verify>
  <acceptance_criteria>
    src/intelligence/pipeline/state_manager.py contains "class PluginStateManager"
    src/intelligence/pipeline/state_manager.py contains "def __init__(self, checkpoint_path: Path)"
    src/intelligence/pipeline/state_manager.py contains "def get_lock(self, key"
    src/intelligence/pipeline/state_manager.py contains "def update_batch(self, state_updates"
    src/intelligence/pipeline/state_manager.py contains "def write_checkpoint(self, extra_state"
    src/intelligence/pipeline/state_manager.py contains "async def read_checkpoint"
    src/intelligence/pipeline/state_manager.py contains "def start_checkpoint_loop"
    src/intelligence/pipeline/state_manager.py contains "_CHECKPOINT_FIELDS"
    src/intelligence/pipeline/state_manager.py does NOT contain "except Exception" wrapping write_checkpoint body in a swallow-and-return pattern (Phase 086: must raise)
    src/intelligence/pipeline/__init__.py contains "PluginStateManager"
    .venv/bin/python -c "from src.intelligence.pipeline import PluginStateManager" exits 0
    .venv/bin/ruff check src/intelligence/pipeline/ exits 0
  </acceptance_criteria>
  <done>PluginStateManager module exists with full surface area; exports updated; importable.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PluginStateManager into orchestrator and remove migrated methods</name>
  <files>
    services/intelligence_pipeline_agent.py
    tests/unit/pipeline_helpers.py
  </files>
  <read_first>
    services/intelligence_pipeline_agent.py
    tests/unit/pipeline_helpers.py
    src/intelligence/pipeline/state_manager.py
  </read_first>
  <action>
    In services/intelligence_pipeline_agent.py:
    1. Add import: "from src.intelligence.pipeline import PluginStateManager" and "from src.intelligence.pipeline.state_manager import _CHECKPOINT_PATH, _CHECKPOINT_FIELDS, _AGENT_VERSION" (or remove orchestrator references to those constants entirely if no longer needed).
    2. Remove __init__ attribute assignments at line 431 (self._plugin_states = {}) and line 432 (self._plugin_states_locks = {}).
    3. Delete the orchestrator's _get_state_lock method (line 567), _write_local_checkpoint (line 1565), and _read_local_checkpoint (line 1575).
    4. In _setup(): construct self._state_mgr = PluginStateManager(checkpoint_path=_CHECKPOINT_PATH) before any other class instantiation that depends on it. Then call: extra = await self._state_mgr.read_checkpoint(). If extra is not None, store its fields locally on the orchestrator for now (this plan): self._last_bar_offset = extra.get("last_bar_offset", {}); the remaining keys ("kalman_state", "tod_priors", "setup_last_fire") are owned by classes not yet extracted in this wave — TEMPORARILY restore them onto the existing orchestrator attributes self._kalman_state, self._tod_priors, self._setup_last_fire so behavior is identical until plans 03/05 land. After construction, call: ckpt_task = self._state_mgr.start_checkpoint_loop(300, self._assemble_checkpoint_extra); self._background_tasks.add(ckpt_task); task.add_done_callback(self._background_tasks.discard).
    5. Add a new orchestrator helper method _assemble_checkpoint_extra(self) -> dict that returns {"kalman_state": self._kalman_state, "tod_priors": self._tod_priors, "last_bar_offset": self._last_bar_offset, "setup_last_fire": self._setup_last_fire}. This helper will be reduced after plans 03/05 land — for now it reads orchestrator-owned attrs.
    6. Anywhere the orchestrator currently does self._plugin_states[key] / [key] = / .get(key) / .update(...), replace with self._state_mgr.get_state(key) / .update(key, value) / .update_batch(d). For per-key thread Lock access, replace self._get_state_lock(key) with self._state_mgr.get_lock(key). Audit all call sites in _process_bar_inner, _run_i1, _run_tier, _collect_plugin_results.
    7. In _teardown(): replace any explicit self._write_local_checkpoint() call with self._state_mgr.write_checkpoint(self._assemble_checkpoint_extra()).

    In tests/unit/pipeline_helpers.py make_agent():
    1. Remove "agent._plugin_states = {}" and "agent._plugin_states_locks = {}".
    2. Add: "from src.intelligence.pipeline.state_manager import PluginStateManager" then "import tempfile, pathlib; agent._state_mgr = PluginStateManager(checkpoint_path=pathlib.Path(tempfile.mkdtemp()) / 'ckpt.json')".
    3. If any test in tests/unit/ does agent._plugin_states[k] = v directly, update it to agent._state_mgr.update(k, v). Run pytest and fix all AttributeError fallout.
  </action>
  <verify>
    .venv/bin/ruff check services/intelligence_pipeline_agent.py tests/unit/pipeline_helpers.py
    .venv/bin/pytest tests/unit/ -q
    grep -n "self\._plugin_states\b\|self\._plugin_states_locks\|self\._get_state_lock\|_write_local_checkpoint\|_read_local_checkpoint" services/intelligence_pipeline_agent.py
  </verify>
  <acceptance_criteria>
    services/intelligence_pipeline_agent.py contains "PluginStateManager" import
    services/intelligence_pipeline_agent.py contains "self._state_mgr = PluginStateManager("
    services/intelligence_pipeline_agent.py contains "self._state_mgr.start_checkpoint_loop("
    services/intelligence_pipeline_agent.py contains "def _assemble_checkpoint_extra"
    services/intelligence_pipeline_agent.py does NOT contain "self._plugin_states ="
    services/intelligence_pipeline_agent.py does NOT contain "self._plugin_states_locks"
    services/intelligence_pipeline_agent.py does NOT contain "def _get_state_lock"
    services/intelligence_pipeline_agent.py does NOT contain "def _write_local_checkpoint"
    services/intelligence_pipeline_agent.py does NOT contain "def _read_local_checkpoint"
    grep above prints zero matches besides _state_mgr references
    tests/unit/pipeline_helpers.py contains "PluginStateManager(" and does NOT contain "agent._plugin_states ="
    .venv/bin/pytest tests/unit/ -q exits 0
    .venv/bin/ruff check services/intelligence_pipeline_agent.py exits 0
  </acceptance_criteria>
  <done>Orchestrator delegates all plugin state and checkpoint operations to PluginStateManager; background checkpoint loop owned by the class; all unit tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Add dedicated PluginStateManager unit tests</name>
  <files>
    tests/unit/pipeline_tests/test_state_manager.py
  </files>
  <read_first>
    src/intelligence/pipeline/state_manager.py
    src/core/state_serializer.py
    .planning/phases/088-god-class-decomposition/088-PATTERNS.md
  </read_first>
  <action>
    Create tests/unit/pipeline_tests/test_state_manager.py with isolated unit tests. Required cases:
    - test_get_lock_returns_same_lock_per_key: get_lock(("ES","1m")) returns is-same object on repeat call; different key returns different lock.
    - test_update_and_get_state_roundtrip: update(key, {"x": 1}); assert get_state(key) == {"x": 1}; assert get_state(("missing",)) == {}.
    - test_update_batch_merges: update_batch({k1: s1, k2: s2}); assert both readable via get_state.
    - test_write_then_read_checkpoint_roundtrip: write_checkpoint({"kalman_state": {"a": 1.0}, "tod_priors": {}, "last_bar_offset": {"p:0": 42}, "setup_last_fire": {}}); construct a fresh PluginStateManager pointing at same path; await read_checkpoint() returns dict with the same kalman_state and last_bar_offset values.
    - test_write_checkpoint_raises_on_io_error: monkeypatch path so .tmp.write_text raises OSError; assert write_checkpoint(...) raises (Phase 086 contract D-15).
    - test_read_checkpoint_returns_none_on_missing_file: path that does not exist; await read_checkpoint() returns None.
    - test_start_checkpoint_loop_writes_periodically: start_checkpoint_loop(0.05, lambda: {"kalman_state": {}, "tod_priors": {}, "last_bar_offset": {}, "setup_last_fire": {}}); sleep 0.2s; cancel task; assert file exists with valid JSON containing the keys.

    Use tempfile.mkdtemp() for the checkpoint path. Mark async tests with @pytest.mark.asyncio. Do not import IntelligencePipelineComputeAgent.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/pipeline_tests/test_state_manager.py -v
    .venv/bin/pytest tests/unit/ -q
    .venv/bin/ruff check tests/unit/pipeline_tests/test_state_manager.py
  </verify>
  <acceptance_criteria>
    tests/unit/pipeline_tests/test_state_manager.py contains "def test_get_lock_returns_same_lock_per_key"
    tests/unit/pipeline_tests/test_state_manager.py contains "def test_write_then_read_checkpoint_roundtrip"
    tests/unit/pipeline_tests/test_state_manager.py contains "def test_write_checkpoint_raises_on_io_error"
    tests/unit/pipeline_tests/test_state_manager.py contains "def test_start_checkpoint_loop_writes_periodically"
    tests/unit/pipeline_tests/test_state_manager.py does NOT import IntelligencePipelineComputeAgent
    .venv/bin/pytest tests/unit/pipeline_tests/test_state_manager.py -v exits 0 with at least 6 tests passing
    .venv/bin/pytest tests/unit/ -q exits 0
  </acceptance_criteria>
  <done>PluginStateManager has 6+ isolated unit tests covering happy path, raise-on-failure, and the new background loop; full suite green.</done>
</task>

</tasks>

<verification>
- .venv/bin/pytest tests/unit/ -q exits 0
- .venv/bin/ruff check src/intelligence/pipeline/ services/intelligence_pipeline_agent.py tests/unit/pipeline_tests/ exits 0
- Orchestrator no longer defines _get_state_lock / _write_local_checkpoint / _read_local_checkpoint
- write_checkpoint raises on failure (no silent swallow)
</verification>

<success_criteria>
- PluginStateManager is the single writer of cache/pipeline_checkpoint.json
- Background checkpoint loop is owned by the class (orchestrator calls start_checkpoint_loop once)
- ARCH-02 requirement satisfied
- Cross-owned checkpoint fields (kalman_state, tod_priors, setup_last_fire) are passed via extra_state — interface ready for plans 03/05
</success_criteria>

<output>
After completion, create `.planning/phases/088-god-class-decomposition/088-02-SUMMARY.md` documenting: methods removed from orchestrator, PluginStateManager surface, and current state of _assemble_checkpoint_extra (notes the temporary orchestrator-owned fields that plans 03/05 will move).
</output>
