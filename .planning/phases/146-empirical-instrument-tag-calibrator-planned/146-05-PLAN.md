---
phase: 146-empirical-instrument-tag-calibrator
plan: 05
type: execute
wave: 2
depends_on: [146-02]
files_modified:
  - docs/research/tag-calibrator-phase2-regime-conditioning.md
autonomous: true
requirements: [TAG-02]
must_haves:
  truths:
    - "A design document exists specifying the Phase 2 regime-conditioning extension (no code)"
    - "It specifies the PK extension (symbol, tag) -> (symbol, tag, regime) and the IC-stratification gate that triggers it"
    - "It documents F6.3's per-stratum sample-size gate as the sample-sufficiency guard"
    - "It states the ship condition: Phase 2 ships only when IC stratification by tag shows regime-dependent divergence"
  artifacts:
    - path: "docs/research/tag-calibrator-phase2-regime-conditioning.md"
      provides: "Phase 2 regime-conditioning design (TAG-02), design-only"
      contains: "regime"
  key_links: []
---

<objective>
Wave 3 design-only deliverable (TAG-02) — the Phase 2 regime-conditioning DESIGN document. No code.
Per the ROADMAP and CONTEXT.md deferred scope, Phase 2 (regime conditioning) does not ship in this
phase; it ships later, gated on IC stratification showing regime-dependent divergence. This plan
captures that design so the Phase 1 schema decisions (Plan 02) are made with the Phase 2 extension
path documented.

Purpose: Record how the PK extends from (symbol, tag) to (symbol, tag, regime), the trigger gate,
and the per-stratum sample-size guard — so future work has a settled design to execute against.
Output: one design doc under docs/research/.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-empirical-instrument-tag-calibrator-planned/146-CONTEXT.md
@.planning/phases/146-empirical-instrument-tag-calibrator-planned/146-RESEARCH.md
@docs/research/stratification-instrument-tag-calibrator.md

Key facts for the design:
- TAG-02 (ROADMAP): PK (symbol, tag) -> (symbol, tag, regime). Different regimes produce different
  factor exposures (e.g. flight-to-quality makes TLT beta unreliable for equity instruments).
  Ships when IC stratification by tag shows regime-dependent divergence — NOT in Phase 146.
- Dual regime system (STATE.md): per-symbol HMM labels (feature_vectors.regime) and cross-sectional
  market_regimes keyed by regime_group (Phase 144). The design must state which regime axis conditions
  the tag (or both) and how a tag's regime label resolves as a (dimension, label) pair.
- F6.3 (design doc): per-stratum sample-size gate — conditioning on regime shrinks each stratum's N,
  so a min-sample-per-regime guard is required before a regime-conditioned tag is trusted.
- RESEARCH.md: TAG-02 is explicitly OUT OF SCOPE for Wave 1-2 code; this is design-only.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Phase 2 regime-conditioning design document</name>
  <files>docs/research/tag-calibrator-phase2-regime-conditioning.md</files>
  <read_first>
    - docs/research/stratification-instrument-tag-calibrator.md (Phase 2 references, F6.3 per-stratum gate, revised schema — the design this extends)
    - .planning/phases/146-empirical-instrument-tag-calibrator-planned/146-RESEARCH.md (TAG-02 row, dual regime system)
    - .planning/STATE.md (dual regime system section — feature_vectors.regime vs market_regimes/regime_group)
    - docs/research/close-referenced-concept-docs (convention) — follow the project's canonical-doc standalone-with-rationale style
  </read_first>
  <action>
    Write a design-only doc (no code, no migration SQL — pseudocode/schema-sketch only) covering:
    (1) Problem — why a single (symbol, tag) loading is insufficient when factor exposure is
    regime-dependent (flight-to-quality TLT example). (2) Schema extension — PK (symbol, tag) ->
    (symbol, tag, regime); how the new regime column relates to the Phase 1 measurement-contract
    columns from migration 238; how valid_from/valid_to interact per-regime. (3) Regime axis choice —
    state whether conditioning uses the per-symbol HMM label (feature_vectors.regime), the
    cross-sectional market_regimes label keyed by regime_group, or both, and resolve the label as a
    (dimension, label) pair (never a bare label), consistent with the dual regime system. (4) Trigger
    gate — Phase 2 ships only when IC stratification by tag shows regime-dependent divergence; specify
    what "divergence" means operationally (e.g., loadings differ across regimes beyond CI overlap).
    (5) Per-stratum sample-size guard — document F6.3: regime conditioning shrinks per-stratum N, so a
    min-sample-per-regime gate is required before trusting a regime-conditioned loading; relate it to
    the Phase 1 min_sample_n APR key. (6) Non-goals / deferral — explicitly state this does not ship
    in Phase 146 and lists what must be true first. Add a provenance/Author line per the project's
    doc-provenance convention. Keep it standalone-with-rationale (what/why/alternatives), no "see plan
    doc" for load-bearing content.
  </action>
  <verify>
    <automated>test -f docs/research/tag-calibrator-phase2-regime-conditioning.md && grep -qi "regime" docs/research/tag-calibrator-phase2-regime-conditioning.md && grep -qi "sample" docs/research/tag-calibrator-phase2-regime-conditioning.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The doc exists and covers all six sections (problem, schema extension, regime-axis choice, trigger gate, per-stratum sample-size guard, non-goals/deferral).
    - It states the PK extension (symbol, tag) -> (symbol, tag, regime) explicitly.
    - It documents the F6.3 per-stratum sample-size gate and ties it to Phase 1's min_sample_n.
    - It states the ship condition (IC stratification showing regime-dependent divergence) and that Phase 2 does NOT ship in Phase 146.
    - Contains no executable code or migration SQL (design-only); carries an Author/provenance line.
  </acceptance_criteria>
  <done>Phase 2 regime-conditioning design is documented and settled for future execution; TAG-02 covered as design-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author -> design doc | A markdown design document; no code, no DB, no runtime surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-11 | Tampering (design-integrity) | a design that under-specifies the per-stratum sample gate could later license regime-conditioned tags on thin data | mitigate | Acceptance criteria require the F6.3 min-sample-per-regime gate to be documented and tied to min_sample_n before any Phase 2 execution. |
| T-146-SC | Tampering | package installs | accept | Zero packages, zero code — documentation-only plan. |
</threat_model>

<verification>
- Design doc exists with all six required sections and the explicit deferral statement.
- No code/SQL in the doc (design-only).
</verification>

<success_criteria>
The Phase 2 regime-conditioning extension is documented (PK extension, regime-axis choice, trigger
gate, per-stratum sample guard, deferral), giving TAG-02 a settled design without shipping code now.
</success_criteria>

<output>
Create `.planning/phases/146-empirical-instrument-tag-calibrator-planned/146-05-SUMMARY.md` when done.
</output>
