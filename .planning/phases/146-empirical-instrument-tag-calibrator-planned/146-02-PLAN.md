---
phase: 146-empirical-instrument-tag-calibrator
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/238_tag_calibrator_measurement_contract.sql
autonomous: true
requirements: [TAG-01, TAG-03]
must_haves:
  truths:
    - "instrument_tags gains valid_from/valid_to and the empirical measurement columns without breaking existing rows (D-10)"
    - "tag_vocabulary gains factor_series/measurement_type/lookback_days/loading_threshold/half_life_days"
    - "Each measurable Phase-1 primitive tag carries the correct factor_series under its REAL live name per D-02 (rate_sensitive→TLT, dollar_strength→UUP, china_demand→FXI, credit_risk→HYG-IEF, inflation→TIP-IEF, yield_curve→IEF-SHY, oil_price→XLE-SPY, volatility→SPY_REALIZED_VOL); a NEW equity_beta row (category=sensitivity) carries factor_series=SPY"
    - "Every non-measurable tag_vocabulary row is measurement_type='definitional' (no beta_regression row has NULL factor_series) — the schema is self-describing (Option A, D-12); fed_policy/geopolitical are definitional with an owner (D-06); volatility row kept (D-08)"
    - "No gold-sensitivity human tag is seeded; GLD remains a factor-series input only (D-04)"
    - "The 7 alpha.tag_calibrator.* APR keys exist in config_schema/config_state/config_history"
  artifacts:
    - path: "production/migrations/238_tag_calibrator_measurement_contract.sql"
      provides: "revised schema DDL + measurement-contract seeding + APR keys"
      contains: "ADD COLUMN valid_to"
  key_links:
    - from: "tag_vocabulary.factor_series"
      to: "TagCalibrator measurement loop (Plan 04)"
      via: "generic (symbol, factor_series, measurement_type) contract"
      pattern: "factor_series"
---

<objective>
Wave 0/Wave 1 boundary migration (D-01, D-10). Add the measurement-contract schema columns the
calibration engine reads/writes, seed the factor-series contract for the initial primitive set, and
seed the 7 APR tunables. This migration MUST land before the TagCalibrator service (Plan 04) — the
ROADMAP had the service before the migration, which D-01 corrects.

Purpose: The engine is generic — it drives off `(symbol, factor_series, measurement_type)` from
`tag_vocabulary` (D-12). Those columns and the expiry columns (valid_from/valid_to) do not exist yet
(verified live), so the service cannot run until this migration ships.
Output: migration 238.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-empirical-instrument-tag-calibrator-planned/146-CONTEXT.md
@.planning/phases/146-empirical-instrument-tag-calibrator-planned/146-RESEARCH.md
@.planning/phases/146-empirical-instrument-tag-calibrator-planned/146-PATTERNS.md
@docs/research/stratification-instrument-tag-calibrator.md

Revised schema (design doc §"Revised schema", authoritative):
- tag_vocabulary ADD: factor_series text (NULL for definitional); measurement_type text DEFAULT
  'beta_regression' CHECK IN ('beta_regression','correlation','cross_correlation',
  'mutual_information','definitional'); lookback_days int DEFAULT 252; loading_threshold float;
  half_life_days int DEFAULT 180. (p_value_threshold and min_r2 are NOT added — deleted per F1/F2/F3.)
- instrument_tags ADD: loading float; p_value float; bh_adjusted_p float; passes_fdr boolean;
  consecutive_fails int DEFAULT 0; sample_n int; estimated_at timestamptz; valid_from timestamptz
  NOT NULL DEFAULT now(); valid_to timestamptz. weight (existing, CHECK [0,1]) := |loading|, column
  unchanged. instrument_annotations already has valid_from/valid_to — do NOT touch it.
- Pitfall 4 (RESEARCH.md): the tag_vocabulary.category CHECK is ALREADY the 6-value set (migration
  228). Do NOT re-migrate the category CHECK.

LIVE TAG NAMES (verified live 2026-07-16 — CRITICAL): the live `tag_vocabulary` uses concept names,
NOT the `_beta`-suffixed shorthand used loosely in CONTEXT.md/RESEARCH.md. The real rows are
`rate_sensitive` (sensitivity), `dollar_strength` (macro_driver), `china_demand` (macro_driver),
`oil_price` (macro_driver), `volatility` (sensitivity), `credit_risk`/`inflation`/`yield_curve`
(sensitivity). There is NO `equity_beta` row and NO row that means "beta to SPY" — `high_beta`
(exposure) is a DIFFERENT concept (a high-vol equity-style factor exposure), NOT this instrument's
market beta, so it must NOT be reused. A NEW `equity_beta` row is INSERTed. No `_beta`-suffixed row
(`rate_beta`/`dollar_beta`/`china_beta`/`oil_beta`/`vol_beta`/`gold_beta`) exists — do not UPDATE by
those names.

APR keys (design doc §"APR keys", 7 total), namespace alpha.tag_calibrator.* (F9 rename), all
loaded by the default alpha.% APR pattern:
- alpha.tag_calibrator.fdr_alpha = 0.05 [conventional]
- alpha.tag_calibrator.expiry_consecutive_fails = 3 [initial_estimate]
- alpha.tag_calibrator.discovery_oos_days = 63 [initial_estimate]
- alpha.tag_calibrator.min_sample_n = 60 [initial_estimate]
- alpha.tag_calibrator.hac_max_lag = 5 [conventional]
- alpha.tag_calibrator.half_life_min_days = 30 [initial_estimate]
- alpha.tag_calibrator.half_life_max_days = 365 [initial_estimate]
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 238 — revised schema DDL (both tables)</name>
  <files>production/migrations/238_tag_calibrator_measurement_contract.sql</files>
  <read_first>
    - production/migrations/227_instrument_tag_vocabulary.sql (current tag_vocabulary/instrument_tags DDL shape)
    - docs/research/stratification-instrument-tag-calibrator.md (§"Revised schema", lines ~597-621)
    - .planning/phases/146-empirical-instrument-tag-calibrator-planned/146-RESEARCH.md (Live Schema State + Pitfall 4)
  </read_first>
  <action>
    Create migration 238, BEGIN/COMMIT wrapped. Apply the two ALTER TABLE blocks exactly as the
    design doc's Revised schema specifies (reproduced in this plan's context). tag_vocabulary gains
    factor_series, measurement_type (with the 5-value CHECK), lookback_days DEFAULT 252,
    loading_threshold, half_life_days DEFAULT 180. instrument_tags gains loading, p_value,
    bh_adjusted_p, passes_fdr, consecutive_fails DEFAULT 0, sample_n, estimated_at, valid_from
    timestamptz NOT NULL DEFAULT now(), valid_to timestamptz. Do NOT add p_value_threshold or min_r2
    (deleted per F1/F2/F3). Do NOT alter the tag_vocabulary.category CHECK (already 6-value, migration
    228 — Pitfall 4). Do NOT touch instrument_annotations (already has valid_from/valid_to). Use
    ADD COLUMN IF NOT EXISTS where the DB supports it, or plain ADD COLUMN — existing rows must
    survive (valid_from defaults to now(), all new nullable columns default NULL, consecutive_fails 0).
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/238_tag_calibrator_measurement_contract.sql && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d instrument_tags" | grep -E "valid_from|valid_to|loading|passes_fdr"</automated>
  </verify>
  <acceptance_criteria>
    - Migration applies without error; pre-existing 410 instrument_tags rows are preserved (no data loss).
    - `\d instrument_tags` shows valid_from, valid_to, loading, p_value, bh_adjusted_p, passes_fdr, consecutive_fails, sample_n, estimated_at.
    - `\d tag_vocabulary` shows factor_series, measurement_type, lookback_days, loading_threshold, half_life_days.
    - tag_vocabulary.category CHECK constraint is unchanged (still the 6-value set).
    - No columns added to instrument_annotations.
  </acceptance_criteria>
  <done>Both tables carry the revised measurement-contract schema; existing rows intact.</done>
</task>

<task type="auto">
  <name>Task 2: Seed the factor-series measurement contract for the Phase-1 primitive set</name>
  <files>production/migrations/238_tag_calibrator_measurement_contract.sql</files>
  <read_first>
    - .planning/phases/146-empirical-instrument-tag-calibrator-planned/146-CONTEXT.md (D-02, D-04, D-05, D-06, D-08, D-12 in full)
    - .planning/phases/146-empirical-instrument-tag-calibrator-planned/146-RESEARCH.md (Pitfall 1 tradeable-view bar counts)
  </read_first>
  <action>
    Append INSERT/UPDATE statements to migration 238 that establish the measurement contract. USE THE
    REAL LIVE TAG NAMES (verified live 2026-07-16, see context) — the `_beta`-suffixed shorthand in
    CONTEXT.md/RESEARCH.md is NOT what the live schema calls these rows.

    Single-symbol beta_regression (per D-02), UPDATE the existing rows by their real names:
      UPDATE tag_vocabulary SET factor_series='TLT' WHERE tag='rate_sensitive';
      UPDATE tag_vocabulary SET factor_series='UUP' WHERE tag='dollar_strength';
      UPDATE tag_vocabulary SET factor_series='FXI' WHERE tag='china_demand';

    Long-short beta_regression (encode the pair as 'LONG-SHORT' so the Plan 04 service parses on the
    hyphen and calls the shared long_short constructor), UPDATE existing rows by real names:
      UPDATE tag_vocabulary SET factor_series='HYG-IEF' WHERE tag='credit_risk';
      UPDATE tag_vocabulary SET factor_series='TIP-IEF' WHERE tag='inflation';
      UPDATE tag_vocabulary SET factor_series='IEF-SHY' WHERE tag='yield_curve';
      UPDATE tag_vocabulary SET factor_series='XLE-SPY' WHERE tag='oil_price';   -- (was "oil_beta")

    Vol proxy sentinel (D-02/D-08 — 'volatility' is vol_beta's natural home, kept despite zero holders):
      UPDATE tag_vocabulary SET factor_series='SPY_REALIZED_VOL' WHERE tag='volatility';
    ('SPY_REALIZED_VOL' is a sentinel, not a tradeable symbol — the Plan 04 service maps it to
    breadth_vol's SPY-realized-vol proxy, D-02.)

    NEW equity-beta concept — there is NO existing live tag for "this instrument's beta to SPY"
    (high_beta is a DIFFERENT exposure concept, verified). INSERT a new row, then set its factor_series:
      INSERT INTO tag_vocabulary (tag, category, description)
        VALUES ('equity_beta', 'sensitivity',
                'Sensitivity (OLS beta) of the instrument''s daily returns to the broad equity
                 market (SPY); general equity-market-beta, empirically measured.');
      UPDATE tag_vocabulary SET factor_series='SPY' WHERE tag='equity_beta';
    category MUST be 'sensitivity' (matches rate_sensitive/volatility/credit_risk/inflation/
    yield_curve), NOT 'exposure' (that category means something different — see the live high_beta row).

    D-05 free-rider single-symbol proxies (measurable at near-zero marginal cost under the F8
    full-matrix loop), UPDATE existing rows:
      UPDATE tag_vocabulary SET factor_series='SMH' WHERE tag='semi_cycle';
      UPDATE tag_vocabulary SET factor_series='FXY' WHERE tag='yen_carry';
      UPDATE tag_vocabulary SET factor_series='EEM' WHERE tag='em_flows';
    (At execution time confirm each clears the 252-day lookback via market_data_ohlcv_tradeable and
    follow the design doc if a proxy lacks history — these three tickers are Claude's Discretion
    within D-05, not locked.)

    For ALL of the above measurable tags (the 12: rate_sensitive, dollar_strength, china_demand,
    credit_risk, inflation, yield_curve, oil_price, volatility, equity_beta, semi_cycle, yen_carry,
    em_flows) also set measurement_type='beta_regression', lookback_days=252, loading_threshold=0.2
    ([initial_estimate], correlation units, the F2 magnitude hypothesis; per-tag tunable),
    half_life_days=180. (Simplest: fold these into each UPDATE, and include equity_beta's
    loading_threshold in its UPDATE.)

    OPTION A self-describing sweep (D-12, resolves the Plan 04 null-factor_series hazard): after the
    measurable rows are set, flip EVERY remaining row to definitional in one statement:
      UPDATE tag_vocabulary SET measurement_type='definitional' WHERE factor_series IS NULL;
    This makes the whole schema self-describing — `measurement_type='beta_regression' AND
    factor_series IS NOT NULL` ⇔ a measurable Phase-1 tag; `measurement_type='definitional'` ⇔
    everything else (benchmark, spread_leg, sector_rotation, factor_rotation, sentiment,
    stress_indicator, growth, value, momentum, breadth, leading_indicator, regime_classifier, all
    exposure/cycle_position/factor_regime rows, fed_policy, geopolitical). The design doc names
    benchmark/spread_leg/sector_rotation/factor_rotation/sentiment/stress_indicator as canonical
    definitional examples — this sweep is consistent with that. (Note: credit_cycle/housing_cycle are
    deleted by Plan 01; if either still exists when 238 runs, the sweep harmlessly marks it
    definitional — UPDATEs by the measurable names above simply affect 0 rows for a deleted tag.)

    D-06: fed_policy and geopolitical are caught by the definitional sweep (factor_series NULL);
    additionally record an owner annotation for each (per TAG-03). D-04: seed NO gold tag — do NOT
    INSERT any gold_beta/gold-sensitivity row and seed zero human gold assignments; GLD stays a
    factor-series INPUT only. D-12: add NO new stratification tags beyond equity_beta (a real Phase-1
    primitive, not new scope) — no tech_beta / GICS-sector rows.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tc "SELECT tag, factor_series, measurement_type FROM tag_vocabulary WHERE tag IN ('equity_beta','rate_sensitive','dollar_strength','china_demand','credit_risk','inflation','yield_curve','oil_price','volatility','semi_cycle','yen_carry','em_flows','fed_policy','geopolitical') ORDER BY tag"</automated>
  </verify>
  <acceptance_criteria>
    - rate_sensitive/dollar_strength/china_demand carry factor_series TLT/UUP/FXI, measurement_type='beta_regression'.
    - credit_risk/inflation/yield_curve/oil_price carry factor_series 'HYG-IEF'/'TIP-IEF'/'IEF-SHY'/'XLE-SPY', measurement_type='beta_regression'.
    - volatility carries factor_series 'SPY_REALIZED_VOL', measurement_type='beta_regression'; the row still exists (kept, D-08).
    - A NEW equity_beta row exists with category='sensitivity' and factor_series='SPY', measurement_type='beta_regression'.
    - semi_cycle/yen_carry/em_flows carry factor_series 'SMH'/'FXY'/'EEM', measurement_type='beta_regression'.
    - fed_policy and geopolitical carry measurement_type='definitional' and factor_series IS NULL, each with an owner annotation.
    - Self-describing invariant holds: `SELECT count(*) FROM tag_vocabulary WHERE measurement_type='beta_regression' AND factor_series IS NULL` returns 0.
    - Exactly 12 measurable rows: `SELECT count(*) FROM tag_vocabulary WHERE measurement_type='beta_regression' AND factor_series IS NOT NULL` returns 12.
    - No gold tag exists: `SELECT count(*) FROM tag_vocabulary WHERE tag ILIKE '%gold%'` returns 0; no human gold-sensitivity instrument_tags row exists.
    - No tech_beta (or other new-stratification) row was added beyond equity_beta.
  </acceptance_criteria>
  <done>Every measurable Phase-1 primitive has its factor-series contract under its real live name; the new equity_beta row exists; all other rows are definitional (self-describing schema); no scope creep (D-12).</done>
</task>

<task type="auto">
  <name>Task 3: Seed the 7 alpha.tag_calibrator.* APR keys</name>
  <files>production/migrations/238_tag_calibrator_measurement_contract.sql</files>
  <read_first>
    - production/migrations/235_ibkr_apr_migration.sql (3-table APR seed pattern, full file)
    - docs/research/stratification-instrument-tag-calibrator.md (§"APR keys introduced by this review")
  </read_first>
  <action>
    Append the APR 3-table seed (config_schema, config_state, config_history — each ON CONFLICT DO
    NOTHING) for the 7 keys, following migration 235's exact pattern, changed_by='migration_238':
    alpha.tag_calibrator.fdr_alpha=0.05 (float, [conventional]);
    alpha.tag_calibrator.expiry_consecutive_fails=3 (int, min 1 max 10, [initial_estimate]);
    alpha.tag_calibrator.discovery_oos_days=63 (int, [initial_estimate]);
    alpha.tag_calibrator.min_sample_n=60 (int, [initial_estimate]);
    alpha.tag_calibrator.hac_max_lag=5 (int, [conventional]);
    alpha.tag_calibrator.half_life_min_days=30 (int, [initial_estimate]);
    alpha.tag_calibrator.half_life_max_days=365 (int, [initial_estimate]).
    Each config_schema.description must carry the provenance tag shown and state whether it is an ML
    learning target (these are not — statistical governance thresholds). Set sensible min/max bounds.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tc "SELECT config_key, config_value FROM config_state WHERE config_key LIKE 'alpha.tag_calibrator.%' ORDER BY config_key"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 alpha.tag_calibrator.* keys are present in config_state with the specified default values.
    - Each key has a matching config_schema row (value_type, bounds, provenance-tagged description) and a config_history row (changed_by='migration_238').
    - `SELECT count(*) FROM config_state WHERE config_key LIKE 'alpha.tag_calibrator.%'` returns 7.
  </acceptance_criteria>
  <done>All 7 tunables live in APR; the Plan 04 service loads them via the default alpha.% pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → live DB | One-time DDL + seed over a locally-trusted psql connection; no external/user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-03 | Tampering (data-integrity) | schema ALTER on 410 live rows | mitigate | New columns are nullable or defaulted so existing rows survive; verification asserts row preservation and column presence before Wave 2 depends on it. |
| T-146-04 | Tampering | factor-series seeding | mitigate | Seeds only D-02 proxies + D-05 discretionary single-symbol proxies validated against tradeable-view history, keyed on REAL live tag names (verified); the self-describing sweep marks every other row definitional so no measurable row is left with NULL factor_series; D-12 no-new-stratification asserted in acceptance criteria. |
| T-146-SC | Tampering | package installs | accept | Zero new packages (SQL-only plan). No supply-chain surface. |
</threat_model>

<verification>
- Migration applies clean; `\d instrument_tags` and `\d tag_vocabulary` show all new columns.
- All 7 APR keys present. Factor-series contract seeded per D-02/D-05 under real tag names; every
  other row definitional (self-describing invariant holds); definitional tags per D-06.
- `.venv/bin/pytest tests/unit/ -q` stays green.
</verification>

<success_criteria>
The measurement-contract schema and factor-series contract exist so the generic engine (Plan 04)
can drive off (symbol, factor_series, measurement_type). Expiry columns (valid_from/valid_to) present.
Every tag_vocabulary row is unambiguously either measurable (beta_regression + non-NULL factor_series)
or definitional — no ambiguous beta_regression-with-NULL-factor_series rows remain.
</success_criteria>

<output>
Create `.planning/phases/146-empirical-instrument-tag-calibrator-planned/146-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
