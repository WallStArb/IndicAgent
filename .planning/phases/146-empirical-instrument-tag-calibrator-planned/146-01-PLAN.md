---
phase: 146-empirical-instrument-tag-calibrator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/237_tag_vocabulary_taxonomy_cleanup.sql
  - tests/unit/test_spread_leg_pair_validity.py
  - docs/foundation/glossary.md
autonomous: true
requirements: [TAG-03]
must_haves:
  truths:
    - "credit_cycle no longer exists as a tag in tag_vocabulary or instrument_tags (D-03)"
    - "housing_cycle no longer exists as a tag in tag_vocabulary or instrument_tags (D-07)"
    - "Every remaining spread_leg row has evidence->>'pair' resolving to a valid instruments.symbol (D-09)"
    - "spread_leg pair references are symmetric (if A names B, B names A)"
    - "glossary.md documents credit_cycle as a banned alias and carries the T7 category-overlap note"
  artifacts:
    - path: "production/migrations/237_tag_vocabulary_taxonomy_cleanup.sql"
      provides: "credit merge, housing_cycle delete, spread_leg evidence backfill"
      contains: "DELETE FROM tag_vocabulary WHERE tag = 'credit_cycle'"
    - path: "tests/unit/test_spread_leg_pair_validity.py"
      provides: "spread_leg pair-validity + symmetry data-contract test"
      contains: "def test_"
    - path: "docs/foundation/glossary.md"
      provides: "credit_cycle banned-alias entry + T7 category-overlap note"
  key_links:
    - from: "tests/unit/test_spread_leg_pair_validity.py"
      to: "instrument_tags / instruments"
      via: "DB query on evidence->>'pair' resolving to instruments.symbol"
      pattern: "spread_leg"
---

<objective>
Wave 0 taxonomy cleanup (D-01). Retire two broken/redundant tags and repair the `spread_leg`
evidence contract so the taxonomy is sound before the calibration engine is built. Covers D-03
(credit_cycle→credit_risk merge), D-07 (housing_cycle deletion), D-09 (spread_leg evidence
backfill + boundary-style test), and the T7 glossary category-overlap doc fix.

Purpose: The calibration engine (Wave 2) derives empirical tags against a clean vocabulary. A
self-regression tautology (`housing_cycle`), a genuine duplicate (`credit_cycle`), and 17
NULL-evidence `spread_leg` rows are pre-existing data defects that must be cleared first.
Output: migration 237, a new data-contract unit test, and glossary edits.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-empirical-instrument-tag-calibrator-planned/146-CONTEXT.md
@.planning/phases/146-empirical-instrument-tag-calibrator-planned/146-PATTERNS.md
@.planning/phases/146-empirical-instrument-tag-calibrator-planned/146-RESEARCH.md

Live-verified facts (RESEARCH.md, 2026-07-17):
- HYG carries credit_cycle=0.9 / credit_risk=1.0; LQD carries credit_cycle=0.8 / credit_risk=0.8.
  Max-weight-on-collision means credit_risk already dominates on both holders — so the merge is a
  straight delete of credit_cycle rows, no credit_risk weight bump needed (PATTERNS.md).
- housing_cycle has exactly one holder (XHB, weight 1.0); its own factor series IS XHB, so its
  "measurement" is a self-regression tautology (D-07) — delete outright.
- spread_leg: 28 instrument_tags rows, 17 with NULL evidence. Mechanically recoverable pairs:
  LQD←CWB, TLT←EDV, SPY←IPO/EZU, SCHD←VYM; missing reciprocal rows to add: UUP, USMV, FXI.
- Data-boundary rule: reads use market_data_ohlcv_tradeable; but this plan touches instrument_tags/
  instruments/tag_vocabulary only, no OHLCV reads.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 237 — credit merge, housing_cycle delete, spread_leg evidence backfill</name>
  <files>production/migrations/237_tag_vocabulary_taxonomy_cleanup.sql</files>
  <read_first>
    - production/migrations/235_ibkr_apr_migration.sql (house migration header/BEGIN-COMMIT convention)
    - production/migrations/227_instrument_tag_vocabulary.sql (spread_leg seed rows + evidence JSONB shape)
    - .planning/phases/146-empirical-instrument-tag-calibrator-planned/146-CONTEXT.md (D-03, D-07, D-09 in full)
  </read_first>
  <action>
    Create migration 237, BEGIN/COMMIT wrapped, doing three data operations in order.
    (a) credit_cycle→credit_risk merge (D-03): `DELETE FROM instrument_tags WHERE tag = 'credit_cycle'`
    then `DELETE FROM tag_vocabulary WHERE tag = 'credit_cycle'`. No credit_risk weight bump is
    needed — verified live that credit_risk already carries >= credit_cycle's weight on both holders
    (HYG, LQD). If (defensively) any credit_cycle holder lacks a credit_risk row, INSERT a credit_risk
    row at the max of the two weights before the delete; guard this with a WHERE NOT EXISTS.
    (b) housing_cycle deletion (D-07): `DELETE FROM instrument_tags WHERE tag = 'housing_cycle'`
    then `DELETE FROM tag_vocabulary WHERE tag = 'housing_cycle'`.
    (c) spread_leg evidence backfill (D-09): UPDATE the mechanically-recoverable rows to
    `evidence = '{"pair": "<SYM>", "reason": "..."}'::jsonb` for the pairs LQD←CWB, TLT←EDV,
    SPY←IPO, SPY←EZU, SCHD←VYM (set evidence->>'pair' to the counterparty symbol). INSERT the three
    missing reciprocal spread_leg rows for UUP, USMV, FXI (source='human', weight matching their
    counterparty, evidence with the pair). For the 13 non-mechanically-recoverable NULL-evidence rows
    that cannot be resolved from mirror mentions in other rows' evidence, DELETE them rather than
    fabricate evidence (per RESEARCH.md A4 fallback and D-09's "unrecoverable rows get deleted, not
    guessed"). Emit a comment in the migration listing exactly which symbols were backfilled, which
    reciprocals were added, and which were deleted, so the record is auditable.
    Use parameterized-safe literal SQL only (no dynamic string building). All symbol references must
    already exist in instruments.symbol.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/237_tag_vocabulary_taxonomy_cleanup.sql && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tc "SELECT count(*) FROM instrument_tags WHERE tag IN ('credit_cycle','housing_cycle')"</automated>
  </verify>
  <acceptance_criteria>
    - Migration applies without error against the live indicagent DB.
    - `SELECT count(*) FROM instrument_tags WHERE tag IN ('credit_cycle','housing_cycle')` returns 0.
    - `SELECT count(*) FROM tag_vocabulary WHERE tag IN ('credit_cycle','housing_cycle')` returns 0.
    - `SELECT count(*) FROM instrument_tags WHERE tag='spread_leg' AND evidence IS NULL` returns 0.
    - Every remaining spread_leg row's evidence->>'pair' names a symbol that exists in instruments.symbol.
  </acceptance_criteria>
  <done>credit_cycle and housing_cycle fully removed; every spread_leg row has non-NULL, symbol-valid pair evidence.</done>
</task>

<task type="auto">
  <name>Task 2: spread_leg pair-validity + symmetry data-contract test</name>
  <files>tests/unit/test_spread_leg_pair_validity.py</files>
  <read_first>
    - tests/unit/test_market_data_ohlcv_boundary.py (house boundary/data-contract test pattern, two-test shape)
    - .planning/phases/146-empirical-instrument-tag-calibrator-planned/146-PATTERNS.md (spread_leg test section)
  </read_first>
  <action>
    Create a new unit test modeled on test_market_data_ohlcv_boundary.py's two-assertion shape, but
    sourcing from a live DB read (or a fixture snapshot if the runner is DB-free) rather than a
    filesystem grep — D-09 frames this as a data-contract check. Two tests:
    (1) test_every_spread_leg_pair_resolves_to_a_valid_symbol — query
    `SELECT symbol, evidence->>'pair' AS pair FROM instrument_tags WHERE tag='spread_leg'`; assert
    every non-null pair value exists in instruments.symbol and no row has NULL/missing pair.
    (2) test_spread_leg_pairs_are_symmetric — assert if row A's spread_leg evidence names B as pair,
    then B has a spread_leg row whose evidence names A back (reciprocal). Use the project psql/asyncpg
    connection convention (PGPASSWORD=postgres, host localhost, db indicagent) or asyncpg via Settings;
    keep it CI-runnable. Name assertions with clear failure messages listing the offending symbol(s).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_spread_leg_pair_validity.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Both tests exist and pass against the post-migration-237 database.
    - Deliberately removing one reciprocal row (mental check / not committed) would fail the symmetry test — i.e., the symmetry assertion is real, not a tautology.
    - Test file imports nothing from raw market_data_ohlcv and adds no new allow-list entry to the boundary test.
  </acceptance_criteria>
  <done>Data-contract test green; enforces pair validity and symmetry for all spread_leg rows.</done>
</task>

<task type="auto">
  <name>Task 3: glossary.md — credit_cycle banned alias + T7 category-overlap note</name>
  <files>docs/foundation/glossary.md</files>
  <read_first>
    - docs/foundation/glossary.md (lines ~330-408, the six category definitions and any existing banned-alias convention)
    - .planning/phases/146-empirical-instrument-tag-calibrator-planned/146-CONTEXT.md (D-03 banned-alias instruction, canonical_refs T7)
  </read_first>
  <action>
    Two edits. (1) Record credit_cycle as a banned alias of credit_risk (D-03) using whatever
    banned-alias / deprecated-term convention already exists in glossary.md; if none exists, add a
    short "Banned aliases" note under the tag/taxonomy section stating credit_cycle was merged into
    credit_risk in Phase 146 (migration 237) and must not be reintroduced. (2) Apply the T7 doc fix
    to the six category definitions (exposure, sensitivity, factor_regime, cycle_position, signal_role,
    macro_driver): add the narrative-intent-only note that `category` is a display/organizational
    label the calibrator never reads for measurement logic (the measurement contract lives in
    factor_series/measurement_type), and the one-factor-series-one-tag collision rule (two tags must
    not share the same factor_series). Do not alter the category CHECK constraint or the definitions'
    substance — this is a clarifying note only.
  </action>
  <verify>
    <automated>grep -qi "credit_cycle" docs/foundation/glossary.md && grep -qi "factor_series" docs/foundation/glossary.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - glossary.md contains a credit_cycle banned-alias / merged-into-credit_risk note referencing Phase 146.
    - glossary.md's category section carries the T7 note that category is display-only (not read by the calibrator) and states the one-factor-series-one-tag collision rule.
    - No change to the six category names or the CHECK-constraint value set.
  </acceptance_criteria>
  <done>Glossary documents the credit_cycle retirement and the category-is-display-only clarification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → live DB | One-time DDL/DML over a locally-trusted psql connection; no external/user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-01 | Tampering (data-integrity) | migration 237 spread_leg backfill | mitigate | Only mechanically-recoverable pairs are backfilled from existing mirror evidence; unrecoverable rows are DELETED, never guessed (D-09). Test 2 enforces validity + symmetry so a fabricated/asymmetric pair fails CI. |
| T-146-02 | Tampering | credit/housing deletes | mitigate | Deletes are literal `WHERE tag = '...'` on a locally-trusted connection; verification asserts row counts hit 0 with no collateral deletion. |
| T-146-SC | Tampering | package installs | accept | Zero new packages installed this plan (no npm/pip/cargo). No supply-chain surface. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_spread_leg_pair_validity.py -v` green.
- `.venv/bin/pytest tests/unit/ -q` stays green (no regression).
- SQL spot-check: 0 rows for credit_cycle/housing_cycle in both tables; 0 NULL-evidence spread_leg rows.
</verification>

<success_criteria>
credit_cycle and housing_cycle retired; every spread_leg row has valid, symmetric pair evidence
enforced by a CI test; glossary documents both changes. Vocabulary is clean for Wave 2's engine.
</success_criteria>

<output>
Create `.planning/phases/146-empirical-instrument-tag-calibrator-planned/146-01-SUMMARY.md` when done.
</output>
