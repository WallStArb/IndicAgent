---
phase: 49.1
slug: regime-gate-fix-write-all-signals-to-signal-ledger
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-23
---

# Phase 49.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x |
| **Config file** | `pyproject.toml` |
| **Quick run command** | `.venv/bin/pytest tests/unit/service_tests/test_signal_generator_service.py -v` |
| **Full suite command** | `.venv/bin/pytest tests/unit/ -v` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `.venv/bin/pytest tests/unit/service_tests/test_signal_generator_service.py -v`
- **After every plan wave:** Run `.venv/bin/pytest tests/unit/ -v`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 49.1-01-01 | 01 | 1 | D-01 | unit | `.venv/bin/pytest tests/unit/service_tests/test_signal_generator_service.py -v -k "entries"` | ✅ | ⬜ pending |
| 49.1-01-02 | 01 | 1 | D-02 | unit | `.venv/bin/pytest tests/unit/service_tests/test_signal_generator_service.py -v -k "regime_type"` | ✅ | ⬜ pending |
| 49.1-01-03 | 01 | 1 | D-01 | unit | `.venv/bin/pytest tests/unit/service_tests/test_signal_generator_service.py -v -k "suppressed"` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. The test file `tests/unit/service_tests/test_signal_generator_service.py` already exists with 5 passing tests and `_make_svc_for_pipeline` helper. No new test stubs or conftest required — 6 new test functions will be added to the existing file in the implementation tasks.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `signal_ledger` rows written for regime-suppressed bars in production | D-01 | Requires live IBKR + running services | After deploy: `docker exec timescaledb psql -U postgres -d indicagent -c "SELECT status, COUNT(*) FROM signal_ledger WHERE ts > NOW() - INTERVAL '1 hour' GROUP BY status;"` — should show `regime_suppressed` rows appearing |
| `regime_type_at_fire` populated correctly in DB | D-02 | Requires live data | `SELECT regime_type_at_fire, COUNT(*) FROM signal_ledger WHERE ts > NOW() - INTERVAL '1 hour' GROUP BY regime_type_at_fire;` — should show `ranging`/`trending`, no NULLs from new bars |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
