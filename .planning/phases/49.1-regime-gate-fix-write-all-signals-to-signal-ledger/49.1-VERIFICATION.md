---
phase: 49.1-regime-gate-fix-write-all-signals-to-signal-ledger
verified: 2026-03-23T19:15:00Z
status: human_needed
score: 6/6 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: 5/6
  gaps_closed:
    - "Ruff lint passes on services/signal_generator_service.py — E501 fixed in commit 2c5bede"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Deploy and observe suppressed-only bar writes in production"
    expected: "On bars where all signals are regime-suppressed, signal_ledger rows appear with status=regime_suppressed and non-NULL regime_type_at_fire"
    why_human: "Cannot verify DB writes without a running service against a live Redpanda stream; unit tests confirm code path but not end-to-end production behavior"
---

# Phase 49.1: Regime Gate Fix — Write All Signals to Signal Ledger Verification Report

**Phase Goal:** Fix regime gate bug where signal_ledger writes are coupled to winner selection — all signals in all_ranked must write to signal_ledger unconditionally, with regime_type_at_fire and hmm_regime_at_fire populated.
**Verified:** 2026-03-23T19:15:00Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure (ruff E501 fix in commit 2c5bede)

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Every bar with non-empty all_ranked writes ALL signals to signal_ledger, regardless of winner | VERIFIED | `if all_ranked:` block at line 1310 is NOT nested under `if winner:` — entries loop runs unconditionally; DB INSERT at line 1393 runs on `if entries and self.db_manager:` |
| 2 | Bars where all signals are regime-suppressed (winner=None) still produce signal_ledger rows with status=REGIME_SUPPRESSED | VERIFIED | `_status = SignalStatus.PENDING if ranked_sig.get("regime_eligible", True) else SignalStatus.REGIME_SUPPRESSED` at line 1341-1345; `test_all_suppressed_signals_still_write_to_ledger` PASSES |
| 3 | Bars where winner is gate-suppressed (_check_gate=True) still produce signal_ledger rows with was_selected=False for all | VERIFIED | Stage 6a (lines 1291-1305) sets `winner = None` on gate fire leaving `_winner_signal_id = ""`; guard at line 1338 (`if _winner_signal_id != "" else False`) makes all entries `was_selected=False`; `test_gate_suppressed_winner_still_writes_to_ledger` PASSES |
| 4 | regime_type_at_fire is 'ranging' for hmm_regime=0, 'trending' for hmm_regime=1 or 2, None when regime_data missing | VERIFIED | `_HMM_REGIME_LABEL = {0: "ranging", 1: "trending", 2: "trending"}` at line 144; applied at line 1319; `test_regime_type_at_fire_populated_from_hmm_regime` and `test_regime_type_at_fire_none_when_regime_data_missing` both PASS |
| 5 | hmm_regime_at_fire is the raw integer from regime_data for every written entry | VERIFIED | `hmm_regime_at_fire=_hmm_regime_int` at line 1379 on every LedgerEntry; `test_hmm_regime_at_fire_populated` PASSES with hmm=0 and hmm=2 assertions |
| 6 | ledger_written=True in BarIntelligenceRecord whenever DB INSERT succeeds, even on suppressed-only bars | VERIFIED | `ledger_written = True` at line 1402 set after successful `insert_signals_with_features`; flows into `BarIntelligenceRecord(ledger_written=ledger_written)` at line 1496; `test_ledger_written_true_on_suppressed_only_bar` PASSES |

**Score:** 6/6 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `services/signal_generator_service.py` | Decoupled persist stage (Stage 6a) unconditional on all_ranked, winner execution (Stage 6b) gated on winner; contains `_HMM_REGIME_LABEL` | VERIFIED | 1,600+ lines; `_HMM_REGIME_LABEL` at line 144; `if all_ranked:` at 1310 (not under `if winner:`); `if winner:` Kafka publish at 1416; DB INSERT at 1393 unconditional on winner |
| `tests/unit/service_tests/test_signal_generator_service.py` | 6 new test functions covering regime gate fix scenarios; min_lines 1400 | VERIFIED | 2,025 lines; all 6 test functions confirmed at lines 1538, 1600, 1656, 1706, 1764, 1863 |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `services/signal_generator_service.py` | `src/intelligence/trading/signal_ledger.py` | `insert_signals_with_features(pool, entries, {})` | WIRED | `await insert_signals_with_features(self.db_manager.pool, entries, {})` at line 1395-1397; called inside `if entries and self.db_manager:` which is NOT gated on winner |
| `services/signal_generator_service.py` | `_HMM_REGIME_LABEL` | module-level constant dict lookup | WIRED | `_HMM_REGIME_LABEL.get(_hmm_regime_int)` at line 1319 populates `_regime_type_label` which flows to every LedgerEntry's `regime_type_at_fire` |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `services/signal_generator_service.py` entries loop | `_regime_type_label` | `_HMM_REGIME_LABEL.get(int(regime_data["hmm_regime"]))` | Yes — dict lookup from live regime_data passed to `_process_event()`; None when regime_data is None (graceful) | FLOWING |
| `services/signal_generator_service.py` entries loop | `_hmm_regime_int` | `int(regime_data.get("hmm_regime"))` with TypeError/ValueError guard | Yes — raw integer from HMM output; None on missing data | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 6 new tests pass | `.venv/bin/pytest tests/unit/service_tests/test_signal_generator_service.py -q` | 57 passed in 0.84s (51 existing + 6 new) | PASS |
| No regressions in service test module | Same command above | All 51 pre-existing tests still pass | PASS |
| Ruff lint on modified service file | `.venv/bin/ruff check services/signal_generator_service.py` | All checks passed! (exit 0) | PASS |
| Structural decoupling — entries loop not under `if winner:` | `grep -n "if all_ranked\|if winner" services/signal_generator_service.py` | `if all_ranked:` at 1310, preceding `if winner:` Kafka at 1416 — correct ordering | PASS |

Note: The API test collection errors (`tests/unit/api/test_health_endpoints.py`, `tests/unit/test_signals_api_*.py`) are pre-existing — caused by a stale `get_redis_manager` import in `src/api/routes/health.py` from commit `0d8706f` (v1.6, predates this phase entirely). These errors were present before phase 49.1 and are not regressions.

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| SHADOW-01 | 49.1-01-PLAN.md | `hmm_regime` gating thresholds moved from hardcoded constants to Settings fields; safety-floor maximizes labeled training data | SATISFIED (enabling condition) | Phase 49.1 is the downstream enabler of SHADOW-01's data collection intent: by writing regime-suppressed signals unconditionally, every bar now produces labeled rows that enable the "N>=200 regime-suppressed outcomes" threshold bucket analysis referenced in SHADOW-01. The Settings fields themselves (`REGIME_PROB_MIN`, `REGIME_DUR_MIN`) were shipped in Phase 47 per REQUIREMENTS.md. SHADOW-01 marked `[x]` in requirements. |
| DATA-11 | 49.1-01-PLAN.md | `signal_performance_segmented` table stores per-(plugin, timeframe, regime_type, symbol) rolling 30d win rates; only rows with sample_size >= 30 written | ENABLED (prerequisite fix) | DATA-11 requires non-NULL `regime_type_at_fire` for per-regime segmentation. Phase 49.1 fixes the regime field population so future `signal_performance_segmented` rows will have valid `regime_type` values. DATA-11 itself (the table, the writer) was shipped in a prior phase and marked `[x]`. This phase removes a data quality blocker that was silently corrupting the training sample. |

---

### Anti-Patterns Found

None. No stub implementations found. No TODO/FIXME/placeholder comments in phase-modified code paths. No hardcoded empty returns in the restructured Stage 6. Ruff lint passes cleanly (exit 0) after commit 2c5bede wrapped the SessionType ternary on line 795.

---

### Human Verification Required

#### 1. Production suppressed-only bar write validation

**Test:** After deploying to production, run: `SELECT symbol, tf, status, regime_type_at_fire, hmm_regime_at_fire FROM signal_ledger WHERE status = 'regime_suppressed' AND ts > NOW() - INTERVAL '2 hours' ORDER BY ts DESC LIMIT 20;`
**Expected:** Rows appear with `regime_type_at_fire` = 'ranging' or 'trending' (not NULL), `hmm_regime_at_fire` as an integer (0, 1, or 2)
**Why human:** Unit tests confirm the code path; production validation requires a live service processing real bars against a live Redpanda stream

---

### Gaps Summary

No gaps remain. All 6 must-have truths are verified, all 6 new tests pass, structural decoupling is correct, and ruff lint exits 0 after the E501 fix in commit 2c5bede. The only outstanding item is the production deployment spot-check which requires human verification (a running service and live Redpanda stream).

---

_Verified: 2026-03-23T19:15:00Z_
_Verifier: Claude (gsd-verifier)_
