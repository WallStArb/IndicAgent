---
phase: 05-live-pipeline
plan: 02
task: final verification (human UAT checkpoint)
total_tasks: 0 remaining
status: nearly_done
last_updated: 2026-02-24T20:15:00.000Z
---

<current_state>
Plan 05-02 bugs all fixed and committed (055ac4f). Dashboard should now show
live indicator and intelligence values. Pending human UAT to confirm dashboard
displays values then create 05-02-SUMMARY.md.
</current_state>

<completed_work>

**All 3 dashboard/pipeline bugs fixed (commit 055ac4f):**

Bug 1: indicator_service + signal_generator_service — timestamped consumer
groups (indicator_service_{ts}) caused fresh start at "0" on every restart,
replaying all 33+ hours of historical bars. Fixed: stable group names +
150-bar rewind on startup via XGROUP SETID.

Bug 2: use-market-stream.ts indicator_data handler — expected single-indicator
format (indicator_name + value) but stream sends flat dict (rsi_14, macd_12_26_9,
etc.). Fixed: map flat payload to IndicatorData fields.

Bug 3: use-market-stream.ts intelligence_data handler — used payload.symbol
(undefined) instead of parsing event JSON for symbol. Fixed: extract from
event.symbol.

**Pipeline status (20:14 EST):**
- Indicator service: live (19:57 bar, market at 19:58) ✅
- Intelligence stream: live (3s old) ✅  
- SSE emitting: tick_data, market_data, indicator_data, signal_data, intelligence_data ✅
- Dashboard: hot-reloaded with indicator handler fix ✅
- TypeScript: clean ✅
- Unit tests: 594 passing (3 pre-existing failures unchanged) ✅

</completed_work>

<remaining_work>

**Plan 05-02 remaining:**
- Human UAT: open dashboard, verify indicator values show (not "--")
- Verify intelligence panels show structure/context/pattern data
- Create 05-02-SUMMARY.md and commit

**Plan 05-03 — Stability audit (NOT STARTED)**
- Metrics endpoints health check (all 5 Prometheus ports)
- Restart counts verification (systemctl show NRestarts)
- Consumer groups audit (no runaway lag)
- Exception scan in logs (Stochastic warnings are known-benign)
- Investigate qualify_instrument failures for SR1H6, 6EH6, 6JH6, BTCH6, NGJ6
- AI narratives (I8) empty — check ai_narrative_service config

</remaining_work>

<blockers>

1. **indicagent-timeframes.service not installed** (requires sudo):
   `sudo cp services/indicagent-timeframes.service /etc/systemd/system/`
   `sudo systemctl daemon-reload && sudo systemctl enable --now indicagent-timeframes`

2. **AI narratives (I8) empty** — likely same consumer group issue. Check
   ai_narrative_service.py for timestamped group name + "0" position bug.

3. **Pre-existing unit test failures** (5 tests) in test_settings.py,
   test_ibkr_provider.py, test_historical_backfill.py — separate bug-fix session.

4. **Stochastic "No numeric types to aggregate"** warnings on warm-up are
   known-benign (plugin can't compute without enough bars) — not a bug, just noise.

</blockers>
