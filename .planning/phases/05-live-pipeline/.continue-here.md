---
phase: 05-live-pipeline
plan: 02
task: 1 (checkpoint verification)
total_tasks: 2 (plan 02) + 2 (plan 03)
status: in_progress
last_updated: 2026-02-24T20:32:16.521Z
---

<current_state>
Executing Phase 05 live pipeline verification. Plan 05-01 is COMPLETE. Plan 05-02 (smoke test) is
partially done — services are running and ticks are FRESH, but the indicator service is not yet
publishing I1 indicators to the downstream pipeline. The root cause has been diagnosed and partially
mitigated. RTH ends at 21:00 UTC (16:00 ET) — about 28 minutes from pause time.
</current_state>

<completed_work>

**Plan 05-01 — COMPLETE (commits d67d49c, 2925e63, 3207b3e, a903d3b)**
- Task 1: Fixed TWS daemon false-connected state bug — `provider.is_connected()` guard added before reconnect block at line 396
- Task 2: Fixed feature_writer_service to use `get_active_contracts()` instead of hardcoded 6-symbol list
- Task 2: Created `services/indicagent-timeframes.service` systemd unit file
- Bonus fix: Fixed `subscribed_symbols` AttributeError in `health_check()` (line 327) — replaced with `self.contracts` (commit a903d3b)
- 607 tests passing (was 584)
- 05-01-SUMMARY.md created

**Plan 05-02 — Partial**
- Restarted all 8 services via SIGTERM (systemd auto-restarted, NRestarts=1 for all)
- TWS daemon connected to IBKR: `tick_rate=34.58/sec`, `symbols=23` confirmed
- `development:ticks:ESH6:live`: FRESH (age=0s, ~100 ticks/20s)
- Market bars flowing: `development:market:ESH6:1m` receiving authoritative + tick_derived bars every minute

**NOT DONE for 05-02:**
- `indicagent-timeframes.service` NOT yet installed (requires sudo with password — couldn't auth)
- `development:indicators:ESH6:1m`: STALE — still at 74 msgs from 14:59 (see blocker)
- `development:intelligence:ESH6:1m`: EMPTY (depends on indicators)
- `development:signals:ESH6:1m:aggregated`: EMPTY
- `intelligence_features` live rows: could not check (psql needs TCP connection: `psql "postgresql://postgres:postgres@localhost:5432/indicagent"`)
- Human checkpoint approval: NOT YET

**Plan 05-03 — NOT STARTED**

</completed_work>

<remaining_work>

**Immediate: Fix indicator service publishing (BLOCKER)**
The indicator service (PID ~938XXX) is processing tick_derived bars (plugin warnings in logs at 20:30:12)
but NOT publishing new indicator messages. Need to diagnose why `xadd` isn't firing after `_run_i1_plugins`.

**Plan 05-02 remaining:**
- Install timeframes service: `sudo cp services/indicagent-timeframes.service /etc/systemd/system/ && sudo systemctl daemon-reload && sudo systemctl enable --now indicagent-timeframes`
- Verify indicators stream gains messages
- Wait for market_analysis to accumulate 120 indicator bars (~2 hours from cold start, OR it may have history)
- Verify intelligence stream non-empty
- Verify signals stream non-empty
- Verify DB live rows (use TCP: `psql "postgresql://postgres:postgres@localhost:5432/indicagent"`)
- Human approval
- Create 05-02-SUMMARY.md

**Plan 05-03 remaining:**
- All tasks (stability audit: metrics endpoints, restart counts, consumer groups, exception scan)
- Investigate qualify_instrument failures for SR1H6, 6EH6, 6JH6, BTCH6, NGJ6
- Create 05-03-SUMMARY.md

**Then:** Phase verification via gsd-verifier, update roadmap

</remaining_work>

<decisions_made>

- Used SIGTERM to services (as User=bg, no sudo needed) rather than `sudo systemctl restart` — systemd auto-restarted all with NRestarts=1
- Fast-forwarded indicator consumer group from position ~1771964345247 to current stream head via `XGROUP SETID` on all 17 1m streams — this skips the 2000-bar authoritative backlog (bar_history already full from 193K messages processed)
- Did NOT modify indicator_service.py code (consumer group start-at-zero design issue is a known limitation, not a blocker if the XGROUP SETID workaround is applied after each restart)
- Determined psql needs TCP connection string: `psql "postgresql://postgres:postgres@localhost:5432/indicagent"` (not socket)

</decisions_made>

<blockers>

**BLOCKER 1 — Indicator service not publishing (CRITICAL)**
- `logs/indicator_service.log` shows plugin warnings at 20:30:12 (VWAP tz-aware, Stochastic) for tick_derived bar
- BUT `development:indicators:ESH6:1m` count still 74 after tick_derived bar processed
- The `_process_single_bar` code path for tick_derived should call `xadd` after `_run_i1_plugins`
- Suspect: either `features` is empty after all plugins fail, OR `build_i1_message` / `xadd` has an error that's silently caught
- To debug: set log level to DEBUG temporarily, or add print statement, OR check if `bars_processed_total` Prometheus counter incremented at http://localhost:9109/metrics
- Prometheus at :9109 metrics may show `indicator_bars_processed_total` — check if it incremented at 20:30

**BLOCKER 2 — sudo password required for timeframes service install**
- `sudo cp services/indicagent-timeframes.service /etc/systemd/system/` requires password
- File is created and ready at `services/indicagent-timeframes.service`
- User must run this manually

**BLOCKER 3 — market_analysis 120-bar warmup**
- Even once indicators flow, market_analysis needs 120 bars to warm up before publishing intelligence
- RTH ends 21:00 UTC; if indicators start flowing now, intelligence starts at ~22:00 UTC (after hours)
- May need to verify during next day's RTH (next session)

</blockers>

<context>
Phase 05 is a live pipeline verification phase — code changes are minimal (bug fixes in 05-01), the
main work is observing the live system during RTH. The big insight from this session:

1. TWS daemon had false-connected bug (now fixed) causing ticks to stall after overnight
2. feature_writer was using stale 6-symbol hardcoded list (now fixed to use get_active_contracts)
3. Indicator service design: starts consumer group at "0" (historical) on every restart → 30+ min
   backlog delay before real-time processing. Workaround: XGROUP SETID to current stream head.
   But this session revealed a deeper issue: tick_derived bars are processed but indicators not published.
4. The REAL indicator publishing bug: most likely `features` dict is empty when all I1 plugins fail
   (VWAP, Stochastic, CMF all fail on the current bar data). If `build_i1_message` with empty features
   still publishes, the stream count should increase. If it doesn't publish on empty features, that's
   the bug. Check the `build_i1_message` function in the codebase.

The pipeline architecture is: TWS → market:SYM:TF → indicator_service → indicators:SYM:TF →
market_analysis_service → intelligence:SYM:TF → signal_generator → signals:SYM:TF:aggregated

All upstream components (TWS, market bars) are WORKING. The indicator layer is the bottleneck.
</context>

<next_action>
Start with: Check `curl http://localhost:9109/metrics | grep indicator_bars_processed_total` — if
the counter is > 0 and matches the expected count, publishing IS happening but to a different stream key.
If counter is 0, the `_process_single_bar` code path to `xadd` is not being reached.

Then: Search for `build_i1_message` in the codebase to understand what it returns and whether it
can return an empty/invalid message that fails the xadd.

Also: The market_analysis_service may have its own indicator bar history from the OLD service run
(before our restart). Check `logs/market_analysis_service.log` to see if it has cached 120+ bars.
If it does, intelligence events might start flowing even before new indicators are published.
</next_action>
