---
phase: 05-live-pipeline
plan: 02
task: 2 (smoke test — dashboard intelligence display)
total_tasks: 2 (plan 02) + 2 (plan 03)
status: in_progress
last_updated: 2026-02-24T21:19:05.410Z
---

<current_state>
Plan 05-02 smoke test: the I1→I8 pipeline is flowing in Redis (verified), but the dashboard is
showing NO indicators or intelligence — only tick/price data. Three bugs were fixed and committed
(35dcd2d). The remaining issue is the dashboard display: SSE sends indicator_data events but the
dashboard shows "--" for all indicator values. Root cause not yet confirmed but two suspects:

1. Indicator service rewound to 150-bars-ago → re-publishing OLD bars (04:46 AM timestamps).
   Dashboard may be ignoring stale-timestamped indicator events.
2. Intelligence_data events may not be reaching the SSE snapshot correctly (the `event` field
   in intelligence stream messages is a JSON string inside a JSON object — may fail parsing
   in the SSE payload builder).
</current_state>

<completed_work>

**Plan 05-01 — COMPLETE (commits d67d49c, 2925e63, 3207b3e, a903d3b)**
- TWS false-connected fix, feature_writer active contracts, timeframes systemd unit, health_check fix
- 607 tests passing, 05-01-SUMMARY.md created

**Plan 05-02 — Three critical bugs fixed (commit 35dcd2d)**
- Bug 1: `indicator_service.py` — flip tick_derived→authoritative bars (real IBKR bars drive pipeline)
- Bug 2: `signal_generator_service.py` — fix timeframes ["5m","15m"]→["1m"], symbols hardcoded 3→get_active_contracts()
- Bug 3: `feature_writer_service.py` — env_prefix hardcoded ""→reads from Settings() (was writing to wrong stream keys)
- 594 unit tests passing (3 pre-existing failures in ibkr_provider + historical_backfill)

**Pipeline verification (Redis streams — all confirmed flowing):**
- TWS: 35.42 ticks/sec, 23 symbols connected
- I1 indicators: development:indicators:ESH6:1m gaining ~1/min ✅
- I3-I6 intelligence: development:intelligence:ESH6:1m 200+ events ✅
- I7 signals: ESH6:1m=4+, CLJ6=3+, GCJ6=4+ ✅
- signal_ledger: 248K+ rows, 2s stale ✅
- intelligence_features: batch writes resumed (+683 rows post-fix, catching up backlog) ✅
- Dashboard: live tick/price data confirmed showing ✅

**05-02-SUMMARY.md created and committed (a435cae)**

</completed_work>

<remaining_work>

**Plan 05-02 remaining — Dashboard intelligence display (BLOCKER)**

Issue: SSE sends indicator_data events (confirmed via curl) but dashboard shows "--" for all
indicator values. Intelligence_data events not appearing at all in SSE output.

Suspects to investigate:
1. **Stale indicator timestamps**: indicator_service rewound to 150-bars-ago → publishing bars
   from 04:46 AM. Dashboard likely ignores 11+ hour old data. Fix: after catching up to live,
   indicator service will naturally start publishing current bars. Wait ~10 min or restart with
   smaller rewind (e.g. 5 bars).

2. **Intelligence SSE payload**: The intelligence stream message has `{"event": "<JSON string>"}`.
   In SSE route sse.py line 134: `json.dumps({"payload": fields})` — if fields contains bytes
   from Redis (decode_responses=False), this may fail. The except at line 136-138 catches it
   and returns `{"payload": {}}`. Dashboard gets empty payload → shows "--".
   File to check: `src/api/routes/sse.py` around line 130-140. Look for how fields bytes are
   decoded for the JSON payload.

3. **indicagent-timeframes.service**: Still not installed (requires sudo with password).
   ```bash
   sudo cp services/indicagent-timeframes.service /etc/systemd/system/
   sudo systemctl daemon-reload && sudo systemctl enable --now indicagent-timeframes
   ```

**Plan 05-02 remaining tasks:**
- Fix dashboard intelligence display (debug SSE payload for intelligence_data)
- Verify indicator_data shows live values (not stale timestamps)
- Verify intelligence_data appears in dashboard panels
- Verify DB live rows (intelligence_features catching up — check max(ts) approaching now)
- Human approval checkpoint
- Create 05-02-SUMMARY.md final version (current one is draft)

**Plan 05-03 — Stability audit (NOT STARTED)**
- Metrics endpoints health check (all 5 Prometheus ports)
- Restart counts verification (systemctl show NRestarts)
- Consumer groups audit (no runaway lag)
- Exception scan in logs
- Investigate qualify_instrument failures for SR1H6, 6EH6, 6JH6, BTCH6, NGJ6
- AI narratives (I8) empty — ai_narrative_service running but narratives:ESH6:1m=0
- Create 05-03-SUMMARY.md

</remaining_work>

<decisions_made>

- `tick_derived` bars are display-only; `authoritative` (real IBKR) bars drive the I1→I8 pipeline
- Signal generator timeframe changed to 1m (matches what market_analysis actually publishes)
- Signal generator symbols changed to get_active_contracts() (was hardcoded 3-symbol list)
- feature_writer env_prefix fix: reads from Settings() same as all other services
- Consumer group warm-up workaround: XGROUP SETID to N-bars-ago on restart (150 for indicators, 60 for signals) — stable group names are a backlog item
- Consumer group rewind approach: rewind to N-bars-ago (not full head) to allow bar_history warm-up

</decisions_made>

<blockers>

1. **Dashboard showing "--" for indicators/intelligence** (ACTIVE BLOCKER):
   - SSE emits indicator_data but dashboard ignores (stale timestamps from 04:46 AM)
   - SSE may not emit intelligence_data (bytes serialization issue in sse.py payload builder)
   - First check: after indicator service catches up to live bars (~10 min), do indicators appear?
   - Second check: src/api/routes/sse.py line 130-140 — bytes decode for intelligence payload

2. **indicagent-timeframes.service not installed** (requires sudo):
   `sudo cp services/indicagent-timeframes.service /etc/systemd/system/ && sudo systemctl daemon-reload && sudo systemctl enable --now indicagent-timeframes`

3. **AI narratives (I8) empty** — ai_narrative_service running (since 15:11) but narratives:ESH6:1m=0
   Likely same consumer group issue (consuming wrong timeframe/stream). Check ai_narrative_service config.

4. **Pre-existing unit test failures** (5 tests) in test_settings.py, test_ibkr_provider.py, test_historical_backfill.py — separate bug-fix session needed.

</blockers>

<context>
Phase 05 is a live pipeline VERIFICATION phase — the pipeline code is mostly done, we're just
confirming it works end-to-end. The big session wins today:

1. Fixed 3 critical configuration bugs that were silently preventing the pipeline from flowing
2. The pipeline IS flowing in Redis (confirmed via direct stream inspection)
3. The dashboard issue is likely a stale-data or SSE payload bytes-decoding problem

The "consumer group rewind" workaround (XGROUP SETID to N-bars-ago) needs to be done manually
on each service restart until we implement stable group names. This is the root cause of the
stale indicator timestamps showing in the dashboard.

IMPORTANT: The indicator service is currently catching up — it's re-publishing old bars (04:46 AM).
This will self-resolve in ~10-20 minutes as it catches up to live bars. If it doesn't, check
`development:indicators:ESH6:1m` — xrevrange should show last message with today's live timestamp.

The intelligence_data SSE issue is the more interesting bug. The intelligence stream messages
look like: `{"event": "<JSON-encoded IntelligenceEvent>"}`. The SSE route does
`json.dumps({"payload": fields})` where `fields` is a bytes dict from Redis xread. The bytes
keys/values need to be decoded before JSON serialization. Check sse.py lines 130-145 for the
decode logic.
</context>

<next_action>
Start with: Check if indicator service has caught up to live bars yet:
  `.venv/bin/python -c "import redis,time; r=redis.Redis.from_url('redis://localhost:6379/0'); msgs=r.xrevrange('development:indicators:ESH6:1m',count=1); mid=msgs[0][0].decode(); ts=int(mid.split('-')[0])/1000; print(f'last indicator age: {time.time()-ts:.0f}s')"`

If age < 120s: indicators are live → check if dashboard now shows values
If age > 300s: still catching up → investigate sse.py bytes decode for intelligence_data

Then: Check src/api/routes/sse.py lines 130-145 for how bytes fields are handled in JSON payload.
If fields dict has bytes keys/values, json.dumps fails silently → intelligence_data payload is {}.
Fix would be: decode fields bytes before json.dumps.
</next_action>
