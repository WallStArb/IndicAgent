---
phase: 174-universe-expansion-single-name-breadth-scaling-targeted-etf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/336_ic_engine_disk_backed_cell_apr_keys.sql
  - services/_batch_utils.py
  - tests/unit/test_batch_utils.py
autonomous: true
requirements: [D-04b]

must_haves:
  truths:
    - "Float32ChunkAccumulator can accumulate a multi-GB row stream without holding every chunk in anonymous RAM"
    - "Disk-backed mode produces a row-identical array to in-RAM mode for the same input"
    - "Existing in-RAM callers (_compute_symbol_tf, _compute_cross_sectional_tf) are unaffected by the new mode"
    - "Scratch files are deleted after use, never accumulated across a multi-day corpus run"
    - "The scratch directory and activation threshold are APR-backed, not hardcoded"
  artifacts:
    - path: "production/migrations/336_ic_engine_disk_backed_cell_apr_keys.sql"
      provides: "infra.ic_engine.memmap_scratch_dir + infra.ic_engine.disk_backed_min_rows APR keys"
      contains: "infra.ic_engine.memmap_scratch_dir"
    - path: "services/_batch_utils.py"
      provides: "Float32ChunkAccumulator disk_backed mode + close()"
      contains: "disk_backed"
    - path: "tests/unit/test_batch_utils.py"
      provides: "disk-backed equivalence, cleanup, and guard coverage"
      contains: "disk_backed"
  key_links:
    - from: "services/_batch_utils.py"
      to: "numpy.memmap"
      via: "disk-backed chunk writes at a running row offset"
      pattern: "np\\.memmap"
    - from: "production/migrations/336_ic_engine_disk_backed_cell_apr_keys.sql"
      to: "config_state"
      via: "paired config_schema/config_state INSERT"
      pattern: "INSERT INTO config_state"
---

<objective>
Give `Float32ChunkAccumulator` a disk-backed (np.memmap) accumulation mode so peak anonymous
RSS during cross-sectional cell materialization is bounded by one chunk's size rather than
the whole cell's size, and register the two APR keys that mode needs. This is the first half
of D-04's structural fix — the ic_engine wiring that consumes it is Plan 05.

Purpose: `Float32ChunkAccumulator.finalize()`'s `np.vstack(self._chunks)` currently holds both
the full chunk list AND the vstacked result in RAM simultaneously (2x the cell size). At the
182-symbol scale that OOM-killed ic_engine on 2026-09-07 (todo 371), that is the dominant
allocation. Fixing it here, in the shared class, keeps both callers on one implementation
rather than forking a parallel accumulator (the exact mistake migration 249 already corrected
once for `_check_cell_size`).

Output: migration 336 (applied live + committed), a `disk_backed=True` constructor mode with
explicit `close()` cleanup, and unit coverage proving row-identity with the in-RAM path.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/174-universe-expansion-single-name-breadth-scaling-targeted-etf-/174-RESEARCH.md
@.planning/phases/174-universe-expansion-single-name-breadth-scaling-targeted-etf-/174-PATTERNS.md
@.planning/todos/pending/371-ic-engine-cross-sectional-cell-size-guard-post-materialization-ooms-at-universe-scale.md
</context>

<interfaces>
Current class contract that must remain source-compatible for in-RAM callers
(`services/_batch_utils.py` lines 1029-1074):

- `Float32ChunkAccumulator(flush_at: int | None = None)`
- `append_row(row: Any) -> None`
- `append_chunk(rows: Any) -> None`
- `finalize() -> np.ndarray | None`

Valid APR namespace prefixes (`ConfigService.OPS_PREFIXES`, `src/config/config_service.py`
line 39): `regime.`, `swarm.`, `alert.`, `ai.`, `feature.`, `threshold.`, `roll.`,
`cross_asset.`, `macro.`, `ui.`, `weights.`, `alpha.`, `infra.`. There is no `universe.`
prefix — a key outside this allowlist raises `ConfigValidationError` on read.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Register the two disk-backed-accumulator APR keys (migration 336)</name>
  <files>production/migrations/336_ic_engine_disk_backed_cell_apr_keys.sql</files>
  <read_first>
    - production/migrations/307_regime_volatility_schema_apr_cvr.sql (canonical paired config_schema/config_state INSERT pattern + header-comment and idempotency conventions)
    - docs/foundation/adaptive-parameter-registry.md (provenance-tag requirement, ML-learning-target statement)
    - src/config/config_service.py lines 39-84 (OPS_PREFIXES allowlist)
  </read_first>
  <action>
Create `production/migrations/336_ic_engine_disk_backed_cell_apr_keys.sql` following migration
307's structure exactly: a rationale header comment, then a paired `INSERT INTO config_schema
(config_key, value_type, default_value, min_value, max_value, description) ... ON CONFLICT
(config_key) DO NOTHING;` and `INSERT INTO config_state (config_key, config_value, version)
... ON CONFLICT (config_key) DO NOTHING;`, then a closing idempotency statement.

Register exactly two keys:

1. `infra.ic_engine.memmap_scratch_dir` — value_type `str`, default_value
   `/var/tmp/ic_engine_scratch`, min/max NULL. Description opens with `[initial_estimate]`,
   names Phase 174 / D-04 / todo 371, states that it is the directory holding disk-backed
   `Float32ChunkAccumulator` memmap scratch files for cross-sectional cell compute, that the
   directory must live on a filesystem with headroom for one full cell (`/var/tmp`, not
   `/tmp`, because `/tmp` may be tmpfs-backed and would defeat the entire fix by putting the
   "disk" array back in RAM), and that it is not an ML learning target.
2. `infra.ic_engine.disk_backed_min_rows` — value_type `int`, default_value `2000000`,
   min_value `0`, max_value NULL. Description opens with `[initial_estimate]`, names Phase 174
   / D-04, states that cross-sectional cells whose pre-flight estimated row count meets or
   exceeds this threshold use the disk-backed accumulator and smaller cells stay in-RAM
   (memmap has real per-cell setup/IO cost that is not worth paying for small cells), that
   `0` means always-disk-backed, and that it is not an ML learning target.

Header comment must state explicitly what is deliberately NOT being done, matching migration
307's convention: `alpha.ic.max_cell_rows` is NOT recalibrated by this migration — migrations
259 and 332 already did ceiling-bumps as symptom workarounds and todo 371 records that as the
anti-pattern this phase exists to stop repeating.

Apply the migration live against the running database in the same step as writing it
(`PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/336_ic_engine_disk_backed_cell_apr_keys.sql`)
and stage the file for commit immediately — per CLAUDE.md, a migration applied live via
`psql -f` has no forcing function to get committed.

Create the scratch directory on the server (`mkdir -p /var/tmp/ic_engine_scratch`) and confirm
via `df -h /var/tmp` that it is on the 568GB root filesystem, not a tmpfs mount.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT config_key, config_value FROM config_state WHERE config_key IN ('infra.ic_engine.memmap_scratch_dir','infra.ic_engine.disk_backed_min_rows') ORDER BY 1" | grep -c . | grep -x 2</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT count(*) FROM config_schema WHERE config_key LIKE 'infra.ic_engine.%_dir' OR config_key = 'infra.ic_engine.disk_backed_min_rows'` returns 2
    - `config_state` holds `infra.ic_engine.memmap_scratch_dir = /var/tmp/ic_engine_scratch` and `infra.ic_engine.disk_backed_min_rows = 2000000`
    - Both `config_schema.description` values start with `[initial_estimate]` and contain the string `Not an ML learning target`
    - Re-running the migration file a second time produces zero errors and zero row changes (idempotent)
    - `findmnt -T /var/tmp -o FSTYPE -n` does not return `tmpfs`
    - `git status --porcelain production/migrations/336_ic_engine_disk_backed_cell_apr_keys.sql` shows the file staged or committed, not untracked
  </acceptance_criteria>
  <done>Both APR keys readable via ConfigService, scratch dir exists on non-tmpfs storage, migration committed.</done>
</task>

<task type="auto">
  <name>Task 2: Add disk_backed mode to Float32ChunkAccumulator</name>
  <files>services/_batch_utils.py</files>
  <read_first>
    - services/_batch_utils.py lines 1029-1074 (the class as it stands; docstring convention naming both callers and the todo)
    - services/ic_engine.py lines 3186-3205 (the append_row streaming-cursor caller that must stay unaffected)
    - services/ic_engine.py lines 4450-4520 (the append_chunk caller Plan 05 will switch to disk-backed)
    - .planning/phases/174-universe-expansion-single-name-breadth-scaling-targeted-etf-/174-PATTERNS.md lines 24-93 (the validated constructor-extension shape)
  </read_first>
  <action>
Extend `Float32ChunkAccumulator` in `services/_batch_utils.py` with an additive disk-backed
mode. Do NOT fork a parallel class — RESEARCH.md's Don't-Hand-Roll table names that as the
specific mistake to avoid.

Constructor becomes:
`__init__(self, flush_at: int | None = None, *, disk_backed: bool = False, estimated_rows: int | None = None, n_cols: int | None = None, scratch_dir: str | None = None) -> None`

All new params are keyword-only, so every existing positional call site is unchanged.

Behavior:
- `disk_backed=False` (default): byte-for-byte the current behavior. No new branches on the
  hot path beyond a single boolean check.
- `disk_backed=True`: require both `estimated_rows` and `n_cols`; raise `ValueError` naming
  both missing-parameter names if either is falsy. Allocate a `tempfile.NamedTemporaryFile(dir=scratch_dir, suffix=".memmap", delete=False)`
  and a `np.memmap(path, dtype=np.float32, mode="w+", shape=(estimated_rows, n_cols))`.
  Track `self._write_offset`.
- `append_chunk(rows)`: in disk-backed mode, convert to `np.array(rows, dtype=np.float32)`,
  write it into `self._memmap[self._write_offset : self._write_offset + n]`, advance the
  offset. Empty batch is still a no-op. Raise `ValueError` (crash-loud, per CLAUDE.md "silent
  wrong answers are worse than loud crashes") if the write would exceed `estimated_rows` —
  a pre-flight estimate that undercounts must fail loud, never silently truncate the cell.
- `append_row(row)`: in disk-backed mode, buffer into `self._buf` exactly as today and route
  `_flush_buf()` through the same memmap write path, so both append modes work disk-backed.
- `finalize()`: in disk-backed mode, call `_flush_buf()` first, then return
  `self._memmap[: self._write_offset]` — a view, no `np.vstack`, no copy. Return `None` (and
  clean up the scratch file immediately) when `self._write_offset == 0`, matching the in-RAM
  mode's empty contract.
- New public `close(self) -> None`: flush the memmap, close its underlying mmap, and unlink
  the scratch file. Idempotent (safe to call twice, safe to call in in-RAM mode where it is a
  no-op). This is what Plan 05's caller will invoke in a `finally` block. Also add
  `__enter__`/`__exit__` delegating to `close()` so the caller can use `with`.

Use `except X as error:` for any new exception handling — never `exc`. Extend the class
docstring in the existing convention: name todo 371 and Phase 174 D-04, name why the mode is
additive rather than a fork, and state that `estimated_rows` is expected to be a conservative
upper bound (the caller's pre-flight estimate) with `finalize()` trimming to actual rows
written.

Do not import `ConfigService` here — `_batch_utils.py` stays config-agnostic; the caller
(Plan 05) reads the APR keys and passes `scratch_dir` in.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_batch_utils.py -q && .venv/bin/ruff check services/_batch_utils.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "disk_backed" services/_batch_utils.py` shows the keyword-only constructor param, the append branch, the finalize branch, and the docstring mention
    - `grep -n "def close" services/_batch_utils.py` returns a match inside the `Float32ChunkAccumulator` class body
    - `grep -c "except .* as exc" services/_batch_utils.py` returns 0
    - `python -c "import inspect, services._batch_utils as m; s=inspect.signature(m.Float32ChunkAccumulator.__init__); assert list(s.parameters)[1]=='flush_at'; assert all(s.parameters[p].kind.name=='KEYWORD_ONLY' for p in ('disk_backed','estimated_rows','n_cols','scratch_dir'))"` exits 0
    - `np.vstack` no longer appears on the disk-backed code path (`grep -n "vstack" services/_batch_utils.py` matches only inside the in-RAM `finalize` branch)
    - `.venv/bin/ruff check services/_batch_utils.py` reports no findings
  </acceptance_criteria>
  <done>Disk-backed mode exists, in-RAM callers are source- and behavior-unchanged, cleanup is explicit via close()/context manager.</done>
</task>

<task type="auto">
  <name>Task 3: Unit coverage for disk-backed equivalence, cleanup, and crash-loud guards</name>
  <files>tests/unit/test_batch_utils.py</files>
  <read_first>
    - tests/unit/test_batch_utils.py lines 260-330 (existing TestFloat32ChunkAccumulator cases and their assertion style)
    - services/_batch_utils.py (the implementation written in Task 2)
    - .planning/phases/174-universe-expansion-single-name-breadth-scaling-targeted-etf-/174-VALIDATION.md (requirement D-04b row)
  </read_first>
  <action>
Extend `tests/unit/test_batch_utils.py`'s existing `TestFloat32ChunkAccumulator` class with
disk-backed cases. Every test uses pytest's `tmp_path` fixture as `scratch_dir` — no test may
write into the real `/var/tmp/ic_engine_scratch`.

Required cases (each a separate test method, all with `disk_backed` in the method name so
`-k disk_backed` selects them):

1. Row-identity equivalence: feed an identical sequence of 3 ragged `append_chunk` batches
   (e.g. 7, 3, 11 rows of 5 float columns, including negative values and a 0.0) into an in-RAM
   accumulator and a `disk_backed=True` accumulator sized `estimated_rows=64, n_cols=5`.
   Assert `np.array_equal` on the two `finalize()` results and equal `.shape` and `.dtype`.
2. `append_row` path equivalence in disk-backed mode with `flush_at=2`, same assertion shape.
3. Over-estimate trimming: `estimated_rows=1000` but only 21 rows appended — `finalize()`
   returns shape `(21, 5)`, not `(1000, 5)`.
4. Under-estimate is crash-loud: `estimated_rows=5`, append a 9-row chunk — `pytest.raises(ValueError)`.
5. Missing-parameter guard: `Float32ChunkAccumulator(disk_backed=True)` with no
   `estimated_rows`/`n_cols` raises `ValueError`.
6. Empty accumulation: `finalize()` returns `None` and no `.memmap` file remains under
   `tmp_path` afterwards.
7. Cleanup: after `close()`, `list(tmp_path.glob("*.memmap"))` is empty. Assert the same after
   using the accumulator as a context manager.
8. Scratch file is actually on disk while live: assert `len(list(tmp_path.glob("*.memmap"))) == 1`
   after the first `append_chunk` and before `close()`.

Do not add a test that depends on a live database or on the APR keys from Task 1 — this file
is part of the CI-clean unit suite.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_batch_utils.py -k disk_backed -v && .venv/bin/pytest tests/unit/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_batch_utils.py -k disk_backed -q` collects at least 8 tests and all pass
    - `.venv/bin/pytest tests/unit/ -q` exits 0 (standing CI gate stays green)
    - `grep -c "tmp_path" tests/unit/test_batch_utils.py` is at least 8
    - `grep -c "var/tmp" tests/unit/test_batch_utils.py` returns 0
    - The equivalence test asserts `np.array_equal(in_ram_result, disk_result)`, not an approximate comparison
  </acceptance_criteria>
  <done>Disk-backed mode is proven row-identical to in-RAM mode, cleanup is proven, and both under- and missing-parameter cases are proven crash-loud.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ic_engine process → local scratch filesystem | Multi-GB float32 corpus feature data is written outside process memory for the first time |
| migration file → live database | DDL/DML applied by hand via `psql -f` against production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-04 | Denial of Service | `Float32ChunkAccumulator` disk-backed scratch files | mitigate | Explicit `close()` + context-manager support; Task 3 case 7 proves unlink. Plan 05 wires `close()` into a `finally` block so a multi-day corpus run cannot accumulate scratch files until the disk fills (the 2026-08-13 768GB disk-full incident is the precedent this guards against) |
| T-174-05 | Information Disclosure | memmap scratch file permissions/location | mitigate | `tempfile.NamedTemporaryFile` creates files mode 0600 by owner-only; scratch dir is `/var/tmp/ic_engine_scratch` (APR-configured), never a world-writable shared path, and never `/tmp` (tmpfs would also defeat the memory fix itself) |
| T-174-06 | Tampering (measurement integrity) | under-estimated `estimated_rows` | mitigate | Over-capacity write raises `ValueError` rather than truncating the cell — a silently short cell would change what is measured, the exact failure class D-04 forbids. Task 3 case 4 proves it |
| T-174-SC | Tampering | package-manager installs | accept | No packages are installed by this plan. `np.memmap` and `tempfile` are stdlib/already-installed (numpy 2.4.6 verified). RESEARCH.md's Package Legitimacy Audit is N/A for this phase |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/ -q` green.
- `.venv/bin/ruff check services/_batch_utils.py tests/unit/test_batch_utils.py` clean.
- Both APR keys readable: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT config_key FROM config_state WHERE config_key LIKE 'infra.ic_engine.%'"` includes the two new keys.
- No behavior change for existing callers: `git diff services/_batch_utils.py` shows no edits to the `disk_backed=False` control flow beyond the added branch conditions.
</verification>

<success_criteria>
- `Float32ChunkAccumulator(disk_backed=True, ...)` accumulates and finalizes without any `np.vstack`, returning a memmap-backed view.
- Disk-backed output is `np.array_equal` to in-RAM output for the same input.
- Scratch files are unlinked by `close()` and by context-manager exit.
- Migration 336 applied live, idempotent, and committed to git.
</success_criteria>

<output>
Create `.planning/phases/174-universe-expansion-single-name-breadth-scaling-targeted-etf-/174-01-SUMMARY.md` when done
</output>
