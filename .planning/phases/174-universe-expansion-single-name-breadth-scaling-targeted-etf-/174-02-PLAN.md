---
phase: 174-universe-expansion-single-name-breadth-scaling-targeted-etf
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/337_instruments_governance_split.sql
  - scripts/analysis/instrument_compute_eligibility_audit.py
autonomous: true
requirements: [D-07a]

must_haves:
  truths:
    - "instruments carries three independent eligibility dimensions, not one conflated boolean"
    - "Every existing active instrument keeps exactly the behavior it had before the migration"
    - "No instrument is live-tradeable by default — a future live-streaming restart cannot silently exceed IBKR's 80-subscription cap"
    - "The compute_eligible default for existing rows is backed by a measured audit, not an assumption"
  artifacts:
    - path: "production/migrations/337_instruments_governance_split.sql"
      provides: "compute_eligible + live_tradeable columns with explicit backfill"
      contains: "ADD COLUMN IF NOT EXISTS compute_eligible"
    - path: "scripts/analysis/instrument_compute_eligibility_audit.py"
      provides: "per-symbol history-depth audit answering whether is_active=true really means compute-ready"
      exports: ["main"]
  key_links:
    - from: "production/migrations/337_instruments_governance_split.sql"
      to: "instruments"
      via: "additive ALTER TABLE + explicit UPDATE backfill"
      pattern: "UPDATE instruments SET compute_eligible"
---

<objective>
Replace the single conflated `instruments.is_active` boolean with a 3-way
backfill-eligible / compute-eligible / live-tradeable split, additively, so none of the 37
existing `get_active_contracts()` call sites change behavior (D-07). Plan 06 exposes the split
through `get_active_contracts(dimension=)`; this plan lands the schema and proves the default
mapping is correct rather than assumed.

Purpose: `is_active=true` today means "backfill it AND compute on it AND (implicitly) it would
be eligible for any future live-trading code path that reads the same flag." Marking a
Russell-3000-scale sample `is_active=true` under that model silently makes hundreds-to-thousands
of names live-eligible. IBKR's 80-simultaneous-subscription cap binds live streaming only — it
is not a backfill constraint — but a future `indicagent-ibkr-provider` restart against an
unsplit universe would nondeterministically fail to honor it. This gap already survived the
111→231 expansion untouched (todo 274); it does not survive a second, much larger one.

Output: migration 337 (applied live + committed), plus a reusable audit script whose measured
output justifies the `compute_eligible=true` default for all existing rows.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/174-universe-expansion-single-name-breadth-scaling-targeted-etf-/174-RESEARCH.md
@.planning/phases/174-universe-expansion-single-name-breadth-scaling-targeted-etf-/174-PATTERNS.md
@.planning/todos/pending/274-live-tradeable-vs-corpus-universe-flag.md
@docs/foundation/instrument-tag-registry.md
</context>

<interfaces>
Live `instruments` table columns relevant here: `symbol` (PK), `base`, `contract_details`
(jsonb — asset class lives at `contract_details->>'asset_class'`, values `equity` / `futures`
/ `fx`), `is_active` (boolean). `instruments` is NOT a hypertable and is NOT compressed
(verified in RESEARCH.md), so CLAUDE.md's compressed-hypertable decompress/recompress/VACUUM
migration rule does not apply to this migration.

Today's only eligibility query, `src/config/settings.py` line 525-529:
`SELECT symbol, base, contract_details FROM instruments WHERE is_active = true AND contract_details->>'asset_class' != 'futures'`

Bar data lives in `market_data_ohlcv` (time column `timestamp`, timeframe column `timeframe`).
For compute-readiness measurement use the `market_data_ohlcv_tradeable` view, not the raw
table — the raw table contains synthetic-fill and flat-carry-forward placeholder bars
(~82% of intraday rows) and raw-table access outside the allow-list fails CI.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Measure whether is_active=true really means compute-ready for all existing rows</name>
  <files>scripts/analysis/instrument_compute_eligibility_audit.py</files>
  <read_first>
    - scripts/analysis/ic_sharpe_stride_bias_check.py (argparse/structlog/DB-connection shape for a one-off analysis script in this repo)
    - src/config/settings.py lines 476-530 (the exact query whose semantics this audit is validating)
    - tests/unit/test_market_data_ohlcv_boundary.py (the allow-list this script must not violate — use the `_tradeable` view)
  </read_first>
  <action>
Create `scripts/analysis/instrument_compute_eligibility_audit.py` — a read-only one-off that
answers RESEARCH.md Open Question 3: does every `is_active=true` row genuinely have enough
history to be compute-ready, or are some mid-onboarding?

For every row in `instruments` where `is_active = true`, report per symbol: row count and
earliest/latest `timestamp` in `market_data_ohlcv_tradeable` per timeframe in ('5m','15m','1h','1d'),
and whether a `backfill_status` row exists per timeframe. Emit a summary table to stdout plus
a single structlog line with aggregate counts (`n_active`, `n_with_all_four_tfs`,
`n_missing_any_tf`, `n_zero_rows`). Never log per symbol inside the loop — accumulate and log
once, matching `ic_engine.py`'s `n_skipped` pattern.

Use parameterized queries (`%(...)s` placeholders) throughout; never f-string-interpolate a
symbol into SQL. Use `except X as error:`. Exit non-zero if the query fails; exit 0 regardless
of what the audit finds — this is a measurement tool, not a gate.

Run it and capture the output. The finding decides the migration's default in Task 2:
- If every `is_active=true` symbol has non-zero tradeable rows in at least one timeframe,
  `compute_eligible=true` for all existing rows is the correct, behavior-preserving default.
- If any symbol has zero rows in every timeframe, list those symbols explicitly in the
  migration header comment and still set `compute_eligible=true` for them (preserving today's
  exact behavior — narrowing them would be a silent behavior change), with the header noting
  them as candidates for a follow-up demotion decision outside this phase.
  </action>
  <verify>
    <automated>.venv/bin/python scripts/analysis/instrument_compute_eligibility_audit.py && .venv/bin/ruff check scripts/analysis/instrument_compute_eligibility_audit.py</automated>
  </verify>
  <acceptance_criteria>
    - Script exits 0 and prints a per-symbol table plus one aggregate summary line containing the keys `n_active`, `n_with_all_four_tfs`, `n_missing_any_tf`, `n_zero_rows`
    - `grep -c "market_data_ohlcv_tradeable" scripts/analysis/instrument_compute_eligibility_audit.py` is at least 1 and `grep -c "FROM market_data_ohlcv " scripts/analysis/instrument_compute_eligibility_audit.py` returns 0
    - `grep -cE 'f"""?\s*SELECT|f"SELECT' scripts/analysis/instrument_compute_eligibility_audit.py` returns 0 (no f-string SQL)
    - `grep -c "except .* as exc" scripts/analysis/instrument_compute_eligibility_audit.py` returns 0
    - `.venv/bin/ruff check` on the file reports no findings
    - The aggregate summary output is recorded verbatim in the plan SUMMARY so the migration's default choice is traceable to a measurement
  </acceptance_criteria>
  <done>The compute_eligible default for existing rows is backed by measured per-symbol history depth, not an assumption.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 337 — additive compute_eligible / live_tradeable columns</name>
  <files>production/migrations/337_instruments_governance_split.sql</files>
  <read_first>
    - production/migrations/307_regime_volatility_schema_apr_cvr.sql (multi-block additive-migration shape, idempotency convention, "deliberately NOT done" header style)
    - production/migrations/296_single_name_equity_sector_orthogonality_expansion.sql (instruments-table migration conventions)
    - src/config/settings.py lines 476-530 (the 37-call-site query this migration must not break)
    - The Task 1 audit output (decides whether any symbol needs calling out in the header)
  </read_first>
  <action>
Create `production/migrations/337_instruments_governance_split.sql`.

DDL:
`ALTER TABLE instruments ADD COLUMN IF NOT EXISTS compute_eligible boolean NOT NULL DEFAULT true, ADD COLUMN IF NOT EXISTS live_tradeable boolean NOT NULL DEFAULT false;`

Then an explicit, reviewable backfill statement even though it is redundant with the column
defaults (per CLAUDE.md "silent wrong answers are worse than loud crashes" — the semantic
mapping decision must be a visible SQL statement, not an implicit default):
`UPDATE instruments SET compute_eligible = true, live_tradeable = false WHERE is_active = true;`

Add a `COMMENT ON COLUMN` for each of the three dimensions so the semantics live in the schema
itself, not only in a planning doc:
- `instruments.is_active` — "Dimension 1 of 3 (Phase 174 D-07): BACKFILL-ELIGIBLE. Retained
  with unchanged semantics; 37 call sites read it. Do not repurpose or rename."
- `instruments.compute_eligible` — "Dimension 2 of 3 (Phase 174 D-07): COMPUTE-ELIGIBLE —
  feature_factory / ic_engine / regime models consume this symbol. Newly onboarded symbols are
  set false until backfill completes, then promoted."
- `instruments.live_tradeable` — "Dimension 3 of 3 (Phase 174 D-07): LIVE-TRADEABLE — eligible
  for IBKR live streaming (reqMktData / reqHistoricalData keepUpToDate=True), which is subject
  to the 80-simultaneous-subscription cap. Defaults false for every row; no row is live-eligible
  until a subscription whitelist is explicitly chosen."

Header comment must state, in migration 307's style:
- Why `is_active` is deliberately NOT renamed or repurposed (37 call sites; it already means
  backfill-eligible in practice).
- The default mapping chosen for the existing rows and the Task 1 audit result that justifies it.
- That `live_tradeable=false` for all 231 existing rows is honest rather than a regression:
  nothing reads `live_tradeable` yet, live IBKR streaming is confirmed dormant, and no
  ≤80-symbol whitelist has ever been chosen.
- That this migration deliberately does NOT touch `contract_metadata` (futures-only roll
  bookkeeping, no futures in Phase 174's scope).
- A closing idempotency statement: DDL uses `ADD COLUMN IF NOT EXISTS`; the `UPDATE` is
  naturally idempotent; safe to re-run.

Apply live in the same step (`PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/337_instruments_governance_split.sql`)
and stage for commit immediately.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM information_schema.columns WHERE table_name='instruments' AND column_name IN ('compute_eligible','live_tradeable')" | grep -x 2</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT count(*) FROM instruments WHERE is_active = true AND compute_eligible = true` equals `SELECT count(*) FROM instruments WHERE is_active = true`
    - `SELECT count(*) FROM instruments WHERE live_tradeable = true` returns 0
    - `SELECT count(*) FROM information_schema.columns WHERE table_name='instruments' AND column_name IN ('compute_eligible','live_tradeable') AND is_nullable='NO'` returns 2
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM pg_description d JOIN pg_attribute a ON a.attrelid=d.objoid AND a.attnum=d.objsubid WHERE a.attrelid='instruments'::regclass AND a.attname IN ('is_active','compute_eligible','live_tradeable')"` returns 3
    - Re-running the migration file produces zero errors and leaves all counts above unchanged
    - `git status --porcelain production/migrations/337_instruments_governance_split.sql` shows the file staged or committed
    - `.venv/bin/pytest tests/unit/ -q` still exits 0 (no existing consumer broke)
  </acceptance_criteria>
  <done>Three-dimension eligibility schema is live, existing behavior is bit-for-bit preserved, and nothing is live-tradeable by default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `instruments` eligibility flag → IBKR live-streaming subscription path | A flag read by a future live-trading consumer determines how many simultaneous IBKR subscriptions are opened |
| migration file → live production database | DDL applied by hand against a table read by 37 call sites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-01 | Elevation of Privilege | `instruments.is_active` conflating corpus membership with live-trading eligibility | mitigate | Add `live_tradeable boolean NOT NULL DEFAULT false`; explicit `UPDATE ... live_tradeable = false` for all existing rows; `COMMENT ON COLUMN` records that the 80-subscription cap binds this dimension. A future live restart can no longer inherit a Russell-3000-scale universe by accident |
| T-174-07 | Tampering | Silent behavior change for the 37 existing `get_active_contracts()` call sites | mitigate | Columns are additive with behavior-preserving defaults, `is_active` is untouched, and Task 1's measured audit (not an assumption) justifies `compute_eligible=true` for existing rows. Plan 06 adds the regression test proving `dimension="compute"` returns the identical symbol set |
| T-174-08 | Denial of Service | `ALTER TABLE` rewrite locking a table read by every consumer | accept | `instruments` is a small (~253-row), non-hypertable, uncompressed table; `ADD COLUMN ... NOT NULL DEFAULT` is metadata-only on PostgreSQL 11+. No consumer downtime expected; ic_engine is not mid-run when this lands |
| T-174-SC | Tampering | package-manager installs | accept | No packages installed by this plan |
</threat_model>

<verification>
- `SELECT symbol FROM instruments WHERE is_active = true AND contract_details->>'asset_class' != 'futures'` returns the same row count before and after the migration.
- `.venv/bin/pytest tests/unit/ -q` green.
- Audit script output recorded in the SUMMARY.
</verification>

<success_criteria>
- `instruments` has `compute_eligible` and `live_tradeable`, both NOT NULL, both commented.
- Zero rows are `live_tradeable = true`.
- `compute_eligible = true` for every row where `is_active = true`, justified by a measured audit.
- Migration 337 applied live, idempotent, committed.
</success_criteria>

<output>
Create `.planning/phases/174-universe-expansion-single-name-breadth-scaling-targeted-etf-/174-02-SUMMARY.md` when done
</output>
