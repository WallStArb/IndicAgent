---
phase: 176-earnings-season-calendar-primitive-todo-353
plan: 05
type: execute
wave: 3
depends_on: ["176-02"]
files_modified:
  - services/ensemble_trainer.py
  - tests/unit/test_ensemble_trainer.py
  - .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-SCOPE-CONSUMER-AUDIT.md
autonomous: true
requirements: [ES-06]
must_haves:
  truths:
    - "ensemble_trainer's eligibility query cannot pick up earnings-season IC cells as new training strata"
    - "Every feature_ic_scores consumer outside ic_engine has a recorded verdict: scope-agnostic, or filtered"
    - "Every consumer whose output can reach a promotion, gating, weighting, emission, lifecycle or deployment decision is PATCHED, not merely documented"
    - "The pre-existing eligibility predicate is otherwise unchanged for the three original scopes"
  artifacts:
    - path: "services/ensemble_trainer.py"
      provides: "regime_scope exclusion in _eligibility_where"
      contains: "earnings_season"
    - path: ".planning/phases/176-earnings-season-calendar-primitive-todo-353/176-SCOPE-CONSUMER-AUDIT.md"
      provides: "Per-file verdict for every feature_ic_scores reader in services/, scripts/, api/, src/, each with an explicit decision-driving determination"
  key_links:
    - from: "services/ensemble_trainer.py"
      to: "feature_ic_scores.regime_scope"
      via: "eligibility WHERE clause exclusion"
      pattern: "regime_scope"
---

<objective>
Stop the new `regime_scope='earnings_season'` rows from silently becoming ensemble training inputs,
and produce a written audit of every other `feature_ic_scores` consumer.

Purpose: Pitfall 2, made concrete during planning. Cross-sectional earnings-season cells will be
written with `symbol='POOLED'`, `is_pooled=true` and a non-`_pooled` regime label — which is
exactly the shape `ensemble_trainer._eligibility_where()` selects on today
(`symbol = 'POOLED' AND is_pooled = true AND regime != '_pooled' AND ...`). Left alone, plan
176-06's output would silently add new `(tf, regime)` strata to ensemble weighting and alpha
emission. This project's standing rule is shadow mode first and prove edge before wiring
production consumers; this phase ends at a measurement gate, not a promotion, so the new scope must
be measurement-only.

The regime-routing bug class is not theoretical here: commit `b8af2b749` (2026-09-17) fixed a
corpus-wide `ic_engine.py` regime-*group* routing bug that blocked the engine entirely, and commit
`d1ce8d6bb` fixed a sibling tag-routing contamination the same week.

Output: a one-clause eligibility exclusion with tests, plus `176-SCOPE-CONSUMER-AUDIT.md`.

This plan runs in parallel with 176-04: it touches `services/ensemble_trainer.py`, not
`services/ic_engine.py` (whose own in-file scope consumers are 176-04's Task 2).
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/176-earnings-season-calendar-primitive-todo-353/176-RESEARCH.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Live-verified from services/ensemble_trainer.py this planning session. -->

    def _eligibility_where(sign_symmetric: bool) -> tuple[str, str]:
        ...
        base_where = (
            "symbol = 'POOLED' AND is_pooled = true AND regime != '_pooled'"
            f" AND {significance_clause}"
            " AND reliable = true AND ic_sharpe_hac IS NOT NULL"
            " AND passes_walkforward = true"
        )
        return base_where, base_where + " AND passes_fdr = true"

Three call sites consume it (`_assert_prerequisites` at ~line 490, the stratum-discovery query at
~line 753, and `EnsembleTrainer._process_stratum` at ~line 881), and
`tests/unit/test_ensemble_trainer.py` already asserts that all three route through this single
builder — so a change here is sufficient and no call site needs editing.

`tests/unit/test_ensemble_trainer.py` (~line 195) pins a byte-identical equivalence property:

    _OLD_BASE_WHERE = (
        "symbol = 'POOLED' AND is_pooled = true AND regime != '_pooled'"
        " AND ic_ci_lower > 0"
        " AND reliable = true AND ic_sharpe_hac IS NOT NULL"
        " AND passes_walkforward = true"
    )
    def test_flag_off_is_byte_identical_to_pre_component_e_predicate(...)

That test's expected constant must be updated in the same commit as the builder, and its docstring
must record that the Phase 176 exclusion is the reason the string changed — otherwise a future
reader will read the equivalence property as broken rather than deliberately re-baselined.

Row shape written by the cross-sectional earnings cells (plan 176-06): `symbol='POOLED'`,
`is_pooled=True`, `regime` = a season-qualified label, `regime_scope='earnings_season'`.
Row shape written by the per-symbol earnings cells (plan 176-04): `is_pooled=False`,
`regime='in_season'`/`'off_season'`, `regime_scope='earnings_season'` — already excluded by the
existing `symbol = 'POOLED'` clause, but the new clause covers both uniformly.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exclude earnings_season scope from ensemble eligibility</name>
  <files>services/ensemble_trainer.py, tests/unit/test_ensemble_trainer.py</files>
  <read_first>
    - services/ensemble_trainer.py lines 95-135 (`_eligibility_where` and the Component-E comment block explaining the significance-clause contract)
    - services/ensemble_trainer.py lines 480-520 (`_assert_prerequisites` startup gate and its failure message listing the eligibility description)
    - services/ensemble_trainer.py lines 700-760 and 870-895 (the other two call sites)
    - tests/unit/test_ensemble_trainer.py lines 140-230 (the single-builder routing assertions and the byte-identical equivalence test with `_OLD_BASE_WHERE`)
    - .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-RESEARCH.md (Pitfall 2)
  </read_first>
  <behavior>
    - `_eligibility_where(sign_symmetric=False)[0]` contains `regime_scope <> 'earnings_season'`
    - `_eligibility_where(sign_symmetric=True)[0]` contains the same exclusion
    - The exclusion appears in the base clause, so both returned strings (base and full) carry it
    - Every other token of the pre-existing predicate is unchanged and in the same order
    - The full clause still ends with ` AND passes_fdr = true`
    - All three call sites still route through the builder (the existing `inspect.getsource` assertions stay green)
  </behavior>
  <action>
    Add ` AND regime_scope <> 'earnings_season'` to `base_where` in
    `services/ensemble_trainer.py::_eligibility_where`, positioned immediately after the
    `regime != '_pooled'` clause so the scope/identity conditions stay adjacent. Use `<>` (not
    `!=`) to match the SQL style already used elsewhere in this file's generated predicates, or
    match whichever operator the surrounding clause uses — pick one and be consistent within the
    string.

    `<>` is safe here rather than `IS DISTINCT FROM`, and this was verified live during planning
    rather than assumed: `feature_ic_scores.regime_scope` is `NOT NULL` in
    `information_schema.columns`, and a live count returned 0 NULL-scope rows out of 9,861,639
    across the three existing scope values (`cross_sectional`, `pooled`, `symbol_hmm`). There is
    therefore no three-valued-logic path by which `<>` could silently drop a pre-existing row from
    eligibility. Re-assert the NOT NULL constraint in a unit test (read it from
    `information_schema`, or assert the count is 0) so that a future migration relaxing the column
    breaks this test loudly instead of silently narrowing ensemble eligibility.

    Add a comment above `base_where` recording: Phase 176 introduced a fourth `regime_scope` value
    for calendar-conditioned IC measurement; those cells share the `symbol='POOLED' AND
    is_pooled=true` shape that this predicate selects on, and admitting them would silently add new
    `(tf, regime)` training strata to ensemble weighting. They are measurement-only until a
    promotion decision is taken in a later phase — this exclusion is the enforcement point.

    Update `tests/unit/test_ensemble_trainer.py`'s `_OLD_BASE_WHERE` constant to the new expected
    string, rename nothing, and extend the equivalence test's docstring to state that the constant
    was re-baselined by Phase 176 and what the Component-E equivalence property still guarantees
    (only the significance clause varies with `sign_symmetric`). Add three new tests: one asserting
    the exclusion is present for both `sign_symmetric` values, one asserting the full clause still
    ends with `passes_fdr = true`, and one asserting `feature_ic_scores.regime_scope` is still
    NOT NULL (the precondition that makes `<>` safe).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_ensemble_trainer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_ensemble_trainer.py -x -q` exits 0
    - `.venv/bin/pytest tests/unit/services/test_ensemble_trainer_regime_source.py tests/unit/services/test_ensemble_trainer_alignment_gate.py -x -q` exits 0
    - `.venv/bin/python3 -c "from services.ensemble_trainer import _eligibility_where as e; b,f=e(False); assert \"earnings_season\" in b and \"earnings_season\" in f; assert f.endswith('passes_fdr = true'); b2,_=e(True); assert 'earnings_season' in b2; print('OK')"` prints OK
    - `grep -v '^\s*#' services/ensemble_trainer.py | grep -c "earnings_season"` is exactly 1
    - A test asserts `feature_ic_scores.regime_scope` is still NOT NULL, so a future migration relaxing it fails loudly
    - `.venv/bin/ruff check services/ensemble_trainer.py` and `.venv/bin/black --check services/ensemble_trainer.py tests/unit/test_ensemble_trainer.py` exit 0
  </acceptance_criteria>
  <done>Ensemble eligibility provably cannot admit earnings-season cells, with the equivalence property deliberately re-baselined and documented, and NULL-scope rows provably still eligible.</done>
</task>

<task type="auto">
  <name>Task 2: Audit every remaining feature_ic_scores consumer</name>
  <files>.planning/phases/176-earnings-season-calendar-primitive-todo-353/176-SCOPE-CONSUMER-AUDIT.md, plus any consumer file this task patches</files>
  <read_first>
    - services/alpha_publisher.py (confirm whether it reads feature_ic_scores at all, or only ensemble outputs)
    - scripts/analysis/equity_regime_separation_gate.py lines 110-160 and 300-335 (an explicit scope allow-lister)
    - scripts/analysis/phase144_regime_separation_gate.py lines 105-160 and 315-345 (same pattern)
    - scripts/ops/corpus/ops_ic_fingerprint_equivalence.py lines 70-150 (treats regime_scope as a compared column)
    - scripts/ops/alpha/ops_ensemble_ic_diagnosis.py lines 70-160 (conditional regime_scope handling)
    - scripts/analysis/statistical_factor_residual_stage3_ic_falsification.py lines 150-185 (groups by regime_scope)
    - src/intelligence/concept_registry_service.py (the lifecycle writer — if any audited consumer feeds it, that consumer is decision-driving by definition)
    - .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-RESEARCH.md (Pitfall 2's prescribed grep sweep)
  </read_first>
  <action>
    Run `grep -rn "feature_ic_scores" services/ scripts/ src/ api/ --include=*.py` and
    `grep -rn "regime_scope" services/ scripts/ src/ api/ --include=*.py`, union the file list, and
    exclude `services/ic_engine.py` (covered by plan 176-04 Task 2) and
    `services/ensemble_trainer.py` (covered by Task 1 above). Save the raw grep output verbatim into
    the audit file under a `## Raw grep output` section, so a later reviewer can confirm the file
    list was complete rather than trusting the summary table.

    For each remaining file, answer the decision-driving question FIRST, before assigning a verdict:
    can this file's output — directly, or by being read/imported/invoked by something else — reach a
    promotion, gating, weighting, emission, lifecycle-transition, or deployment decision? Treat as
    decision-driving: anything writing `ensemble_weights`, `alpha_events`, `concept_registry`,
    `concept_gate`, `concept_transition_log`, or `config_state`; anything imported by a service under
    `services/`; anything invoked by `scripts/ops/corpus/ops_corpus_pipeline_run.sh` or any other
    chained pipeline runner; and any API endpoint under `api/`. Record the answer per file as an
    explicit `DECISION-DRIVING: yes|no` field with the one-line reason.

    Then assign exactly one of three verdicts, constrained by that answer:
      - ALLOW-LISTED — already filters to an explicit scope value, so the new rows are invisible.
        Valid for any file, decision-driving or not.
      - NEEDS-FILTER — would silently mix earnings-season rows into a result that assumes the
        three-value vocabulary. State the exact line and the one-clause fix.
      - SCOPE-AGNOSTIC — reads all rows regardless of scope and a fourth value changes nothing
        semantically. PERMITTED ONLY where `DECISION-DRIVING: no`. "Offline" and "one-shot" are
        NOT by themselves sufficient grounds: an offline script whose printed output is quoted into
        a promotion decision is decision-driving. If a file is offline but its result could be read
        as evidence for or against promoting anything, it is decision-driving and cannot be
        SCOPE-AGNOSTIC.

    Patch, in this task, every NEEDS-FILTER file marked `DECISION-DRIVING: yes` — do not defer it to
    a note. Add the one-clause `regime_scope` filter, and a unit test if the file already has a test
    module. For NEEDS-FILTER files marked `DECISION-DRIVING: no`, recording the exact line and the
    one-clause fix without applying it is acceptable; state explicitly in the row that it was left
    unpatched and why it cannot influence a decision.

    If any file's decision-driving status is genuinely ambiguous, resolve it as `yes` and patch it.
    The asymmetry is deliberate: an unnecessary filter costs one clause, a missed one silently
    contaminates a promotion decision with an unvalidated measurement axis.

    Write `176-SCOPE-CONSUMER-AUDIT.md` as a table: file, the line(s) inspected, `DECISION-DRIVING`,
    the verdict, whether it was patched, and the reasoning in one sentence. Add a closing section stating the
    phase-level contract in one line — rows with `regime_scope='earnings_season'` are
    measurement-only for Phase 176 and feed no weighting, emission, lifecycle or promotion decision —
    and naming the two enforcement points (ensemble_trainer eligibility, ic_engine lifecycle guard).
  </action>
  <verify>
    <automated>test -f .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-SCOPE-CONSUMER-AUDIT.md &amp;&amp; grep -qE "SCOPE-AGNOSTIC|ALLOW-LISTED|NEEDS-FILTER" .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-SCOPE-CONSUMER-AUDIT.md &amp;&amp; grep -q "DECISION-DRIVING" .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-SCOPE-CONSUMER-AUDIT.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The audit file exists and contains one table row per file returned by the two greps, minus the two excluded files
    - Every row carries exactly one of the three verdict tokens AND an explicit `DECISION-DRIVING: yes|no` with a reason
    - Zero rows are both `DECISION-DRIVING: yes` and `SCOPE-AGNOSTIC` (that combination is prohibited by construction)
    - Zero rows are both `DECISION-DRIVING: yes` and `NEEDS-FILTER` while left unpatched
    - Any NEEDS-FILTER row names a specific line number and a specific one-clause fix
    - The audit file contains a `## Raw grep output` section with the verbatim output of both greps
    - The file contains the one-line measurement-only contract statement naming both enforcement points
    - `.venv/bin/pytest tests/unit/ -q` exits 0 (including any consumer patched in this task)
  </acceptance_criteria>
  <done>Every feature_ic_scores consumer has a recorded decision-driving determination and a reasoned verdict, every decision-driving consumer that needed a filter has one, and the measurement-only contract is written down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `feature_ic_scores` table -> ensemble weighting / alpha emission | an unvalidated measurement axis reaching a production decision path |
| `feature_ic_scores` table -> any audited consumer -> a promotion decision | an offline-looking script whose output is quoted as promotion evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-05-01 | Elevation of Privilege | Earnings-season cells entering `ensemble_trainer`'s eligibility set and gaining weight in `ensemble_weights`/`alpha_events` without ever passing a promotion gate | mitigate | `AND regime_scope <> 'earnings_season'` (or `IS DISTINCT FROM`, per the NULL-semantics check) in the single shared `_eligibility_where` builder, pinned by a test that asserts the clause for both `sign_symmetric` values; all three call sites already route through that builder (existing tests enforce it) |
| T-176-05-02 | Repudiation | The re-baselined byte-identical equivalence test read later as a broken invariant | mitigate | Test docstring records the Phase 176 re-baseline and restates what the Component-E property still guarantees |
| T-176-05-03 | Elevation of Privilege | A consumer dismissed as "offline, therefore scope-agnostic" whose output in fact feeds a promotion, gating, weighting, emission or lifecycle decision, letting the unvalidated scope influence that decision unnoticed | mitigate | Every audited file carries an explicit `DECISION-DRIVING: yes\|no` determination made BEFORE the verdict, against a named list of decision surfaces (`ensemble_weights`, `alpha_events`, `concept_registry`/`concept_gate`/`concept_transition_log`, `config_state`, imported-by-a-service, chained into a pipeline runner, `api/` endpoint); SCOPE-AGNOSTIC is structurally prohibited for `DECISION-DRIVING: yes`; ambiguous cases resolve to `yes` and get patched; two acceptance criteria assert the prohibited combinations are absent |
| T-176-05-04 | Tampering | `regime_scope <> 'earnings_season'` silently dropping pre-existing NULL-scope rows from ensemble eligibility (three-valued-logic regression on the original three scopes) | mitigate | Structurally impossible today and verified live during planning: the column is `NOT NULL` and 0 of 9,861,639 rows have a NULL scope, so `<>` cannot evaluate to NULL. A unit test re-asserts the NOT NULL constraint so a future migration relaxing it fails loudly rather than silently narrowing eligibility |
| T-176-05-05 | Information Disclosure | An analysis script reporting an extra scope group and being misread as a new result | accept | Recorded as SCOPE-AGNOSTIC in the audit with reasoning, and only permitted where the decision-driving determination is `no` — so the misreading cannot propagate into a decision |
| T-176-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_ensemble_trainer.py tests/unit/services/test_ensemble_trainer_regime_source.py tests/unit/services/test_ensemble_trainer_alignment_gate.py -q` green
- `.venv/bin/pytest tests/unit/ -q` green
- The audit file covers every grep hit, with raw grep output included
- No file is simultaneously decision-driving and scope-agnostic
</verification>

<success_criteria>
- Earnings-season IC rows cannot reach ensemble weighting or alpha emission
- Every other `feature_ic_scores` consumer carries a decision-driving determination and a recorded verdict
- Every decision-driving consumer that needed a scope filter has one applied, not just noted
- The measurement-only contract for `regime_scope='earnings_season'` is written down with its two
  enforcement points named
</success_criteria>

<output>
Create `.planning/phases/176-earnings-season-calendar-primitive-todo-353/176-05-SUMMARY.md` when done
</output>
</content>
