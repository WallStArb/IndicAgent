---
phase: 176-earnings-season-calendar-primitive-todo-353
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/350_earnings_season_calendar_primitive.sql
  - tests/unit/test_earnings_season_migration_contract.py
autonomous: true
requirements: [ES-03, ES-04, ES-06]
must_haves:
  truths:
    - "feature_vectors has earnings_season_flag and days_since_quarter_end columns before any code writes them"
    - "The 14/42-day window boundaries are readable through ConfigService, not hardcoded anywhere"
    - "concept_registry carries both new features at tier 1_interaction with broadcast=true and non-empty parent_features"
    - "feature_ic_scores.regime_scope accepts 'earnings_season' and still rejects an unregistered 5th value"
  artifacts:
    - path: "production/migrations/350_earnings_season_calendar_primitive.sql"
      provides: "Schema columns, APR seeds, concept_registry/concept_gate genesis rows, regime_scope CHECK widening"
      contains: "feature_ic_scores_regime_scope_chk"
    - path: "tests/unit/test_earnings_season_migration_contract.py"
      provides: "Source-level assertions that migration 350 contains every required clause"
  key_links:
    - from: "production/migrations/350_earnings_season_calendar_primitive.sql"
      to: "concept_registry"
      via: "genesis-seed INSERT with metadata->>'broadcast' = 'true'"
      pattern: "'broadcast', true"
    - from: "production/migrations/350_earnings_season_calendar_primitive.sql"
      to: "feature_ic_scores"
      via: "CHECK constraint widening to include earnings_season"
      pattern: "earnings_season"
---

<objective>
Land migration 350: the two new `feature_vectors` columns, the APR keys behind the window
boundaries and the ic_engine run-level switch, the `concept_registry`/`concept_gate` genesis seed
for both new features, and the `feature_ic_scores.regime_scope` CHECK widening that Pitfall 1
shows will otherwise crash the first earnings-season cell INSERT.

Purpose: schema must exist before code references it (an INSERT naming a missing column fails
loud, so ordering matters). Landing the migration first also keeps this phase compliant with
CLAUDE.md's rule that a migration applied live via `psql -f` gets committed in the same breath.

Output: an applied, committed, idempotent migration plus a source-contract unit test.

CRITICAL correction to 176-RESEARCH.md's drafted `concept_registry` INSERT: it omits the
`broadcast` metadata key. Verified live this planning session — every existing calendar feature
(`dow_sin`, `month_position`, `quarter_position`, `opex_flag`, `quad_witching_flag`) carries
`metadata->>'broadcast' = 'true'`, and `ic_engine.py` reads exactly that key (joined to
`concept_gate`) to build `broadcast_features`. Both new fields are symbol-invariant pure
functions of `bar_ts`, so omitting the flag would pool one observation across ~231 symbols and
massively overstate their cross-sectional IC significance — the exact Phase 173 bug class this
project already paid to fix. `'broadcast', true` is mandatory in both metadata objects.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/176-earnings-season-calendar-primitive-todo-353/176-RESEARCH.md
@.planning/phases/176-earnings-season-calendar-primitive-todo-353/176-PATTERNS.md
@docs/foundation/adaptive-parameter-registry.md
@docs/foundation/unified-concept-registry.md
</context>

<interfaces>
<!-- Exact identifiers this migration must create. Do not rename any of them. -->

New feature_vectors columns (type `real`, nullable, no DEFAULT):
  earnings_season_flag
  days_since_quarter_end

New APR keys:
  feature.earnings_season.start_days      int, seed 14,  namespace `feature.*`
  feature.earnings_season.end_days        int, seed 42,  namespace `feature.*`
  alpha.ic.earnings_season_conditioned    bool, seed true, namespace `alpha.*` (ic_engine run-level switch)

concept_registry rows (domain='feature', group_name='calendar', added_phase='176', status='active'):
  earnings_season_flag    metadata.tier='1_interaction', normalization='bounded_unsigned',
                          linear_ready=false, requires_htf=false, broadcast=true,
                          apr_namespace='feature.earnings_season.',
                          parent_features=['quarter_position','quarter_cycle_sin']
  days_since_quarter_end  metadata.tier='1_interaction', normalization='unbounded_unsigned',
                          linear_ready=true, requires_htf=false, broadcast=true,
                          apr_namespace='feature.',
                          parent_features=['quarter_position','quarter_cycle_cos']

concept_gate rows for both: gate_metric_name='ic_sharpe_hac', gate_eval_method='bootstrap_ci',
  min_gate_n=100, fdr_required=true, fdr_alpha=0.05

Widened constraint:
  ALTER TABLE feature_ic_scores DROP CONSTRAINT IF EXISTS feature_ic_scores_regime_scope_chk;
  ALTER TABLE feature_ic_scores ADD CONSTRAINT feature_ic_scores_regime_scope_chk
      CHECK (regime_scope IN ('cross_sectional', 'symbol_hmm', 'pooled', 'earnings_season'));

Live-verified facts this migration depends on:
- `ic_engine.py` resolves broadcast features with:
  SELECT cr.name FROM concept_registry cr JOIN concept_gate cg USING (concept_id)
  WHERE cr.domain = 'feature' AND cr.metadata->>'broadcast' = 'true'
  -> a concept_registry row WITHOUT a matching concept_gate row is invisible to it.
- 349 is the highest existing migration number; 350 is free.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 350</name>
  <files>production/migrations/350_earnings_season_calendar_primitive.sql</files>
  <read_first>
    - production/migrations/316_velocity_primitives_extension.sql (the full current-generation template: ADD COLUMN + COMMENT + config_schema/config_state/config_history triple + concept_registry + concept_gate)
    - production/migrations/290_named_interaction_primitives.sql (the tier-1 event-flag registration shape with non-empty parent_features)
    - production/migrations/187_feature_ic_scores_regime_scope.sql (the exact DROP CONSTRAINT IF EXISTS / ADD CONSTRAINT idempotent shape to mirror)
    - docs/foundation/adaptive-parameter-registry.md (provenance tag conventions for the description field)
    - docs/foundation/unified-concept-registry.md (migration-time genesis-seeding exemption)
    - .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-PATTERNS.md (migration section — the drafted INSERT, which must be amended with the broadcast key per this plan's objective)
  </read_first>
  <action>
    Create `production/migrations/350_earnings_season_calendar_primitive.sql`, fully idempotent
    (safe to re-run), following migration 316's file structure.

    Section 1 — columns. `ALTER TABLE feature_vectors ADD COLUMN IF NOT EXISTS
    earnings_season_flag real;` and the same for `days_since_quarter_end real;`. Plain ADD COLUMN
    against a compressed hypertable needs no decompress/recompress and therefore no trailing
    `VACUUM` (that rule applies only to `ALTER COLUMN TYPE` migrations — see migration 316's own
    header comment for the precedent). Add `COMMENT ON COLUMN` for both: the flag's comment must
    name the two APR keys that define its window; the companion's comment must state it is the raw
    calendar-day count since the most recent quarter end (Mar 31/Jun 30/Sep 30/Dec 31) and that it
    correlates 0.935 (Spearman) with `quarter_position`, which is why `quarter_position` is one of
    its `parent_features`.

    Section 2 — APR seeds. For each of `feature.earnings_season.start_days` (seed 14, min 0, max
    91), `feature.earnings_season.end_days` (seed 42, min 1, max 91) and
    `alpha.ic.earnings_season_conditioned` (bool, seed true), write the standard triple:
    `INSERT INTO config_schema (...) ... ON CONFLICT (config_key) DO NOTHING`,
    `INSERT INTO config_state (config_key, config_value, version) VALUES (..., 1) ON CONFLICT
    (config_key) DO NOTHING`, and `INSERT INTO config_history (timestamp, config_key, version,
    config_value, changed_by, reason) VALUES (NOW(), ..., 1, ..., 'migration_350', ...) ON
    CONFLICT DO NOTHING`. Every `config_schema.description` must carry a provenance tag: the two
    window boundaries are `[initial_estimate]` (they come from D-04's corrected proxy test, not a
    convention) and must state that changing them invalidates every previously computed
    `earnings_season_flag` value and requires a feature recompute; the ic_engine switch is
    `[user_preference]` and must state it gates an additional IC stratification pass whose cost
    scales with cross-sectional cell count.

    Section 3 — concept_registry genesis seed. Insert both rows exactly as specified in this
    plan's interfaces block, using `jsonb_build_object(...)` with `jsonb_build_array(...)` for
    `parent_features`, `ON CONFLICT (domain, name) DO NOTHING`. The `'broadcast', true` key is
    mandatory in BOTH metadata objects — see this plan's objective for why omitting it silently
    overstates cross-sectional significance.

    Section 4 — concept_gate seed. `INSERT INTO concept_gate (...) SELECT cr.concept_id,
    'ic_sharpe_hac', 'bootstrap_ci', 100, true, 0.05 FROM concept_registry cr WHERE cr.domain =
    'feature' AND cr.name IN ('earnings_season_flag','days_since_quarter_end') ON CONFLICT
    (concept_id) DO NOTHING`. Without these rows `ic_engine.py`'s broadcast resolver (an inner
    JOIN on concept_gate) will not see the new features at all.

    Section 5 — regime_scope CHECK widening, verbatim in the shape given in the interfaces block.

    Section 6 — a `DO $$ ... END $$;` assertion block in migration 187's style that raises if any
    of the following is false after the migration body runs: both columns exist in
    `information_schema.columns` for `feature_vectors`; all three APR keys exist in
    `config_state`; both `concept_registry` rows exist with `metadata->>'broadcast' = 'true'` and
    `metadata->>'tier' = '1_interaction'`; both have a matching `concept_gate` row; and the
    widened CHECK constraint exists on `feature_ic_scores`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_compressed_hypertable_migration_vacuum_check.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `production/migrations/350_earnings_season_calendar_primitive.sql` exists
    - `grep -c "ADD COLUMN IF NOT EXISTS" production/migrations/350_earnings_season_calendar_primitive.sql` is >= 2
    - `grep -c "'broadcast', true" production/migrations/350_earnings_season_calendar_primitive.sql` is exactly 2
    - `grep -c "feature.earnings_season.start_days" production/migrations/350_earnings_season_calendar_primitive.sql` is >= 3 (config_schema, config_state, config_history)
    - `grep -c "alpha.ic.earnings_season_conditioned" production/migrations/350_earnings_season_calendar_primitive.sql` is >= 3
    - `grep -c "concept_gate" production/migrations/350_earnings_season_calendar_primitive.sql` is >= 1
    - `grep -c "feature_ic_scores_regime_scope_chk" production/migrations/350_earnings_season_calendar_primitive.sql` is >= 2 (drop + add)
    - `grep -c "'earnings_season'" production/migrations/350_earnings_season_calendar_primitive.sql` is >= 1
    - `.venv/bin/pytest tests/unit/test_compressed_hypertable_migration_vacuum_check.py -x -q` exits 0 (the CI gate that would fail if this migration were misclassified as a column-type change)
  </acceptance_criteria>
  <done>Migration file written, idempotent, with a self-asserting verification block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration source-contract test + apply live + commit</name>
  <files>tests/unit/test_earnings_season_migration_contract.py, production/migrations/350_earnings_season_calendar_primitive.sql</files>
  <read_first>
    - production/migrations/350_earnings_season_calendar_primitive.sql (written in Task 1)
    - tests/unit/_source_grep_helpers.py (the existing shared helper this repo uses for source-level contract assertions)
    - tests/unit/test_compressed_hypertable_migration_vacuum_check.py (the existing migration-contract test shape to mirror: reads the .sql file as text, asserts on clauses)
  </read_first>
  <behavior>
    - The migration text contains both `ADD COLUMN IF NOT EXISTS earnings_season_flag` and `... days_since_quarter_end`
    - Both concept_registry metadata objects contain `'broadcast', true` and `'tier', '1_interaction'`
    - Both concept_registry rows declare a non-empty `parent_features` array containing `quarter_position`
    - The widened CHECK list contains all four scopes: cross_sectional, symbol_hmm, pooled, earnings_season
    - All three APR keys appear in config_schema, config_state and config_history
    - No `ALTER COLUMN ... TYPE` appears (so the compressed-hypertable VACUUM rule does not apply)
  </behavior>
  <action>
    Create `tests/unit/test_earnings_season_migration_contract.py` reading
    `production/migrations/350_earnings_season_calendar_primitive.sql` as text and asserting every
    case in the behavior block. Strip SQL comment lines (`--` prefixed) before counting tokens so
    a header sentence mentioning a clause cannot satisfy its own assertion.

    Then apply the migration live: `PGPASSWORD=postgres psql -U postgres -h localhost -d
    indicagent -f production/migrations/350_earnings_season_calendar_primitive.sql`. Before
    applying, check `pg_stat_activity` for an in-flight `decompress_chunk`/`compress_chunk`
    against `feature_vectors` and wait it out if present (Pitfall 5) — `ADD COLUMN` takes an
    ACCESS EXCLUSIVE lock and will queue behind, and ahead of, other work on that hypertable.

    After applying, run the four smoke checks (Wave 0 gap 4) as separate psql statements and
    record their output in the plan summary: (1) both columns present in
    `information_schema.columns`; (2) `SELECT config_key, config_value FROM config_state WHERE
    config_key LIKE 'feature.earnings_season.%' OR config_key = 'alpha.ic.earnings_season_conditioned'`
    returns 3 rows; (3) `SELECT name, metadata->>'tier', metadata->>'broadcast' FROM
    concept_registry cr JOIN concept_gate cg USING (concept_id) WHERE cr.name IN
    ('earnings_season_flag','days_since_quarter_end')` returns 2 rows with tier `1_interaction`
    and broadcast `true`; (4) the CHECK constraint accepts `'earnings_season'` and rejects a bogus
    5th value — prove the rejection inside an explicitly rolled-back transaction
    (`BEGIN; INSERT ... regime_scope='bogus_scope' ...; ROLLBACK;`) and confirm the INSERT raises a
    check-constraint violation.

    Commit the migration file and the test in the same commit as the live apply (CLAUDE.md: a
    migration applied via `psql -f` has no forcing function to get committed — do it in the same
    breath, not later).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_earnings_season_migration_contract.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_earnings_season_migration_contract.py -x -q` exits 0
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM information_schema.columns WHERE table_name='feature_vectors' AND column_name IN ('earnings_season_flag','days_since_quarter_end')"` prints `2`
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM config_state WHERE config_key IN ('feature.earnings_season.start_days','feature.earnings_season.end_days','alpha.ic.earnings_season_conditioned')"` prints `3`
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM concept_registry cr JOIN concept_gate cg USING (concept_id) WHERE cr.domain='feature' AND cr.name IN ('earnings_season_flag','days_since_quarter_end') AND cr.metadata->>'broadcast'='true' AND cr.metadata->>'tier'='1_interaction'"` prints `2`
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='feature_ic_scores_regime_scope_chk'"` output contains `earnings_season`
    - A rolled-back INSERT with `regime_scope='bogus_scope'` raises a check-constraint violation (recorded in the summary)
    - Re-running the migration file a second time exits 0 with no error (idempotence)
    - `git log --oneline -1 -- production/migrations/350_earnings_season_calendar_primitive.sql` shows the migration committed
  </acceptance_criteria>
  <done>Migration applied live, all four smoke checks pass, file and test committed together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration DDL -> shared production hypertable | ACCESS EXCLUSIVE lock on `feature_vectors`, which live/batch writers use |
| migration DML -> governance tables | `concept_registry`/`config_state` rows that change runtime behavior of ic_engine and feature_factory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-02-01 | Denial of Service | `ALTER TABLE feature_vectors ADD COLUMN` taking ACCESS EXCLUSIVE while a compress/decompress job runs | mitigate | `pg_stat_activity` pre-check before `psql -f`; wait out in-flight chunk operations rather than queueing the DDL behind them (Pitfall 5) |
| T-176-02-02 | Tampering | A concept_registry row missing `broadcast=true` silently inflating cross-sectional significance for a symbol-invariant feature | mitigate | Mandatory `'broadcast', true` in both metadata objects; asserted by the migration's own DO block, by the source-contract test, and by a live psql acceptance check |
| T-176-02-03 | Repudiation | An APR value changed with no recorded provenance | mitigate | `config_history` row per key with `changed_by='migration_350'` and a reason string carrying the provenance tag |
| T-176-02-04 | Elevation of Privilege | Widening a CHECK constraint admits a scope value that downstream consumers silently mis-route | transfer | Handled explicitly by plans 176-04 and 176-05 (consumer isolation); this plan only widens the constraint and records the dependency |
| T-176-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- Migration applies cleanly twice in a row (idempotent)
- All four Wave 0 smoke checks pass, including the negative case for the CHECK constraint
- `.venv/bin/pytest tests/unit/ -q` green
</verification>

<success_criteria>
- `feature_vectors.earnings_season_flag` and `feature_vectors.days_since_quarter_end` exist
- All three APR keys readable via `config_state`
- Both features registered in `concept_registry` at tier `1_interaction` with `broadcast=true` and
  a matching `concept_gate` row
- `feature_ic_scores.regime_scope` accepts `earnings_season` and rejects an unregistered value
</success_criteria>

<output>
Create `.planning/phases/176-earnings-season-calendar-primitive-todo-353/176-02-SUMMARY.md` when done
</output>
