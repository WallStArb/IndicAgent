---
phase: 176-earnings-season-calendar-primitive-todo-353
plan: 08
type: execute
wave: 5
depends_on: ["176-04", "176-05", "176-06", "176-07"]
files_modified:
  - .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-GATE-VERDICT.md
  - .planning/todos/PRIORITIES.md
  - .planning/STATE.md
autonomous: true
requirements: [ES-09]
must_haves:
  truths:
    - "Both new features have a real feature_ic_scores FDR/walk-forward verdict measured against the corpus, not a unit-test proxy"
    - "The earnings-season regime-conditioning workstream has a measured answer to whether conditioning sharpens any feature's IC"
    - "The verdict document states the scope it measured and does not overstate it"
    - "A CONDITIONING_VERDICT that is not SHARPENS rolls alpha.ic.earnings_season_conditioned back to false, so a negative result stops taxing every future ic_engine corpus run"
    - "todo 353 is closed and STATE.md no longer carries the superseded D-04 numbers"
  artifacts:
    - path: ".planning/phases/176-earnings-season-calendar-primitive-todo-353/176-GATE-VERDICT.md"
      provides: "The phase's completion criterion: measured gate verdicts with tokens, scope statement, conditioning result, and live isolation proof"
      contains: "GATE_VERDICT_EARNINGS_SEASON_FLAG="
    - path: ".planning/phases/176-earnings-season-calendar-primitive-todo-353/176-GATE-VERDICT.md"
      provides: "The recorded APR rollback decision and its config_history reason"
      contains: "CONDITIONING_ROLLBACK="
    - path: ".planning/todos/PRIORITIES.md"
      provides: "Updated links after todo 353 moves to completed/ (link integrity is CI-enforced)"
  key_links:
    - from: "services/ic_engine.py"
      to: "feature_ic_scores"
      via: "corpus run writing rows for both new features and for regime_scope='earnings_season'"
      pattern: "earnings_season"
    - from: ".planning/phases/176-earnings-season-calendar-primitive-todo-353/176-RECOMPUTE-LOG.md"
      to: ".planning/phases/176-earnings-season-calendar-primitive-todo-353/176-GATE-VERDICT.md"
      via: "COVERED_SCOPE manifest defines what the verdict is entitled to claim"
      pattern: "COVERED_SCOPE"
---

<objective>
Run `ic_engine` against the recomputed corpus and record the phase's actual completion criterion:
a real `feature_ic_scores` FDR/walk-forward gate verdict for `earnings_season_flag` and
`days_since_quarter_end`, plus the measured answer to the regime-conditioning question that
motivated the second workstream. Then close todo 353 and correct the project-state record.

Purpose: CONTEXT.md's Phase Boundary is explicit — "this phase ends at a real `feature_ic_scores`
FDR/walk-forward gate pass, same as any other new primitive." Plans 176-01 through 176-07 build and
populate; none of them measure. D-02 is equally explicit that `days_since_quarter_end` is NOT
exempted by `is_earnings_season`'s evidence and must clear its own gate. D-04's closing note says
the proxy Welch t-stat overstates effective N because daily returns inside a 6-week earnings block
are autocorrelated, and that "the real test is `ic_engine.py`'s FDR/walk-forward gate, not this
proxy." This plan is that test.

Output: `176-GATE-VERDICT.md` with verdict tokens, todo 353 closed, STATE.md corrected.

If plans 176-04/176-05/176-06 were HARD STOPPED by their D-01a evidence gate (176-01's sweep found
no broad, FDR-surviving feature), this plan still runs — the primitive workstream (176-02/176-03/
176-07) shipped unconditionally and its `feature_ic_scores` gate is the phase's stated completion
criterion. In that case: skip Query 2 and Query 4's earnings-season assertions, emit
`CONDITIONING_VERDICT=NOT_BUILT` and `CONDITIONING_ROLLBACK=DISABLED`, still flip
`alpha.ic.earnings_season_conditioned` to `false` through `ConfigService` (the key was seeded `true`
by migration 350 and there is no conditioning code reading it, so leaving it `true` is a latent
misconfiguration), and record the sweep's `SWEEP_VERDICT` / `SWEEP_SURVIVORS` in the verdict
document as the reason the conditioning workstream was not built. The pre-flight coverage check
(step 3) and the `regime_scope='earnings_season'` row-count assertions in Task 1's acceptance
criteria do not apply in that case — state so explicitly in the document rather than leaving them
unexplained.

A FAIL verdict is a legitimate, complete outcome. Renaissance rigor (D-03) means the phase ships a
measured answer, not a positive one. Do not tune the window, re-run with different boundaries, or
search scopes until something passes — that is the overfitting this project's principles explicitly
prohibit. Record what the corpus says.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/176-earnings-season-calendar-primitive-todo-353/176-CONTEXT.md
@.planning/phases/176-earnings-season-calendar-primitive-todo-353/176-RECOMPUTE-LOG.md
@.planning/phases/176-earnings-season-calendar-primitive-todo-353/176-EVIDENCE-A1.md
@docs/foundation/performance-investigation-sop.md
</context>

<interfaces>
<!-- Live-verified this planning session. Use these directly. -->

Run command (`--training-window-end` is REQUIRED — there is deliberately no default, because a bare
`MAX(bar_ts)` would silently consume the OOS holdout; Phase 141.1 CR-01):

    .venv/bin/python services/ic_engine.py \
        --symbols <space-separated list from COVERED_SCOPE> \
        --tf 5m 15m 1h 1d \
        --training-window-end "$TRAINING_WINDOW_END"

TRAINING_WINDOW_END must be derived with the OOS clamp, verbatim from
`scripts/ops/corpus/ops_corpus_pipeline_run.sh` (reversing this clamp back to a bare MAX(bar_ts) is
a protocol violation — see docs/plans/OOS-EVAL-PROTOCOL.md):

    SELECT LEAST(
        (SELECT MAX(bar_ts) FROM feature_vectors),
        (SELECT NULLIF(config_value,'')::timestamptz FROM config_state
          WHERE config_key='alpha.validation.oos_start'))

No `--refresh` flag is needed, and adding one would be wrong (it would also invalidate cells this
phase did not change). Every cell's fingerprint is ALREADY invalidated by this phase's own edits —
reasoned against `_fingerprint_computational_key` (services/ic_engine.py ~line 1406), which compares
`code_content_key` + `apr_snapshot_key` + `upstream_watermark` (minus status_hash, keeping
`broadcast_hash`):
  - `broadcast_hash` moves: migration 350 added two `concept_registry` rows with
    `metadata->>'broadcast' = 'true'`
  - `apr_snapshot_key` moves: `alpha.ic.earnings_season_conditioned` is a new IC-affecting APR key
  - `code_content_key` moves: plans 176-04 and 176-06 edit `services/ic_engine.py`
Confirm this empirically with `--dry-run-validity` BEFORE committing to the full run — that flag
prints the skip/compute partition and exits before any fetch, compute or write.

Registry alignment gate (`main()`): `concept_registry(domain='feature')` membership must equal
`FeatureVector`'s field list exactly, or the run FAILs at startup. Migration 350 seeded both new
names, so this should pass — assert it in pre-flight rather than discovering it hours in.

Runtime priors from `logs/corpus_pipeline/step_timings.jsonl` (full-corpus ic_engine step 5):
40,842s (11.3h, done) · 72,908s (20.3h, failed) · 237,730s (66h, done) · 276,180s (76.7h, failed).
Budget days, not hours, and expect a possible mid-run failure. Resume is cheap and correct: per-cell
fingerprints persist in `ic_cell_fingerprints`, so re-running the identical command after a failure
skips cells that already completed. Do not add `--refresh` on a resume.

`feature_ic_scores` gate columns (live-verified column list): `reliable`, `ic_value`, `ic_ci_lower`,
`passes_ci_gate`, `bh_adjusted_p`, `passes_fdr`, `wf_fold_count`, `wf_pass_count`, `wf_ic_sharpe`,
`passes_walkforward`, `ic_sharpe_hac`, `n_independent`, `regime_scope`, `feature_status_at_eval`,
`partial_ic`, `partial_ic_p_value`, `passes_partial_fdr`, `cumulative_e_value`.

Scope vocabulary after this phase: `cross_sectional`, `symbol_hmm`, `pooled`, `earnings_season`.
Cross-sectional season sub-cell labels are parent-qualified `{regime_label}__in_season` /
`{regime_label}__off_season` (plan 176-06); per-symbol season labels are bare `in_season` /
`off_season` (plan 176-04).

Existing reporting shapes to borrow queries from rather than rewriting:
`scripts/debug/analysis/debug_analyze_feature_ic.py` (per-feature IC distribution with FDR and
walk-forward counts against the latest `training_window_end`).

Lifecycle: `ic_engine.py`'s post-run hook calls `ConceptRegistryService.record_transition_sync()`
(services/ic_engine.py ~lines 5731/5774) and writes `concept_transition_log`. Both new concepts were
genesis-seeded `status='active'` by migration 350, so this run is their first real evidence event.
Report what the hook did; never hand-edit `concept_registry.status` — `ConceptRegistryService` is
the only code path allowed to flip an existing concept's status (UCR Invariant 1).

todo 353 closure (CONTEXT.md, Folded Todos): the todo file moves `pending/` -> `completed/`, and
`.planning/todos/PRIORITIES.md` carries TWO references to it (a 2026-08-26 drift-catch note around
line 236 and a priority table row around line 258) that both need their links updated — link
integrity is CI-enforced by `tests/unit/test_todo_priorities_link_integrity.py`. CLAUDE.md `git add`
gotcha: stage the renamed path alone first (`git add -- <new_path>`; git auto-detects the rename)
before batching it with other paths, or the entire `git add` aborts and stages nothing.

APR rollback path (Fable cross-AI review, 2026-09-23 — the missing negative-verdict action).
`alpha.ic.earnings_season_conditioned` was seeded `true` by migration 350 so the conditioning passes
would run and be measurable. It is the run-level switch plans 176-04/176-06 read via
`config.earnings_season_conditioned`. Every `ic_engine` corpus run (historically 11-76h) pays the
extra per-symbol and cross-sectional passes while it is `true`. Flip it with `ConfigService`, never
with a bare UPDATE — the write must land in `config_history` with a `changed_by`/`reason`:

    from src.config.config_service import ConfigService   # confirm the import path by grep before use
    await cfg.set("alpha.ic.earnings_season_conditioned", "false",
                  changed_by="phase-176-08", reason="<verdict token> — see 176-GATE-VERDICT.md")

Grep `config_state`/`config_history` writers already in the repo for the exact `ConfigService` setter
name and signature (it differs between the async and `_sync` paths) rather than assuming the shape
above; the non-negotiable part is that the write goes through `ConfigService` and records a reason.

STATE.md correction needed (Roadmap Evolution, the "Phase 176 added 2026-09-22" bullet): it still
cites the superseded proxy numbers "4.3x higher in-season, p=1.2e-17; 81% of symbols". D-04
supersedes these with 1.90x / Welch p=5.05e-05 / 67% (155/233) on the corrected 14-42-day window,
and binds any write-up to the corrected figures.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight gate, dry-run validity check, then the corpus IC run</name>
  <files>.planning/phases/176-earnings-season-calendar-primitive-todo-353/176-GATE-VERDICT.md</files>
  <read_first>
    - .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-RECOMPUTE-LOG.md (the COVERED_SCOPE manifest — this run's `--symbols` argument comes from there, nowhere else)
    - .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-04-SUMMARY.md, 176-05-SUMMARY.md, 176-06-SUMMARY.md (what actually landed, including 176-06's recorded BH-FDR family decision and 176-04's audited regime_scope consumer list)
    - services/ic_engine.py main() — the registry alignment/drift gate and the `--dry-run-validity` branch (grep for `dry_run_validity` and for the alignment gate's error text)
    - scripts/ops/corpus/ops_corpus_pipeline_run.sh lines 252-300 (the TRAINING_WINDOW_END derivation and its OOS-holdout rationale)
    - docs/plans/OOS-EVAL-PROTOCOL.md (why the clamp exists and what reversing it costs)
    - docs/foundation/performance-investigation-sop.md (mandatory before running anything expensive against feature_vectors)
  </read_first>
  <action>
    Pre-flight, in order, aborting on any failure rather than working around it.

    (1) Competing-process and lock checks, identical to plan 176-07 Task 1: `ps aux` for
    backfill_feature_factory / ic_engine / regime_writer / forward_return_writer, and
    `pg_stat_activity` for decompress_chunk / compress_chunk / autovacuum against `feature_vectors`
    or `_timescaledb_internal._hyper_%`. Record what was observed either way.

    (2) Registry alignment: assert `SELECT count(*) FROM concept_registry WHERE domain='feature'`
    equals `len(dataclasses.fields(FeatureVector))`, and that both new names are present with a
    matching `concept_gate` row. A mismatch fails the run at startup — catch it here, not hours in.

    (3) Coverage precondition: assert zero NULL `earnings_season_flag` rows within the COVERED_SCOPE
    manifest. Without this, plan 176-04's `flag_column_all_null` skip path fires silently and the run
    produces no earnings-season cells at all while otherwise looking successful.

    (4) Derive TRAINING_WINDOW_END with the verbatim LEAST() clamp from the interfaces block. Record
    the raw `MAX(bar_ts)`, the `alpha.validation.oos_start` value, and the effective clamped value.
    If `alpha.validation.oos_start` is unset, record that NO holdout is in effect and carry that
    statement into the verdict document's limitations paragraph — do not silently proceed as if a
    holdout existed.

    (5) `--dry-run-validity`: run ic_engine with that flag and the derived window end, and record the
    printed skip/compute partition, as an explicit `compute / (compute + skip)` fraction.

    The pass threshold is pinned here, before the run, and is not to be adjusted after seeing the
    number: **proceed only if compute >= 80% of candidate cells.** Below 80%, STOP and find the
    cause before spending days of compute.

    The threshold is numeric on purpose rather than "a large majority", matching the discipline this
    plan already applies to its own CONDITIONING_VERDICT rule and to 176-01's A1 ratio bands — an
    ambiguous acceptance bar is one a tired operator talks themselves past at hour 40. The reasoning
    behind 80%: all three fingerprint components moved (`broadcast_hash` via migration 350's two new
    `concept_registry` rows, `apr_snapshot_key` via the new `alpha.ic.earnings_season_conditioned`
    key, `code_content_key` via plans 176-04 and 176-06 editing `services/ic_engine.py`), so the
    reasoned expectation is ~100% compute. Anything materially below that contradicts the reasoning
    and means the run would silently serve pre-phase results as this phase's measurement. The 20%
    slack absorbs legitimate edge cases (cells excluded for insufficient N, symbols outside the
    COVERED_SCOPE manifest) without absorbing a real fingerprint failure.

    Record the fraction in `176-GATE-VERDICT.md` whether it passes or not. If it is between 80% and
    ~99%, proceed but list the skipped cells and state why they were skipped — a passing fraction
    with unexplained skips is still a scope caveat the verdict document must carry.

    Then run ic_engine in the background with the COVERED_SCOPE symbol list, all four tfs, and the
    derived `--training-window-end`, logging under `logs/corpus_pipeline/`. Poll progress rather than
    blocking; record start time, periodic progress observations, and end time. On a mid-run failure,
    capture the error, re-run the identical command (fingerprints make the resume skip completed
    cells), and record both attempts. Never add `--refresh` on a resume.

    Open `176-GATE-VERDICT.md` with a "Run provenance" section: the exact command, the derived window
    end and its two inputs, the dry-run partition counts, start/end timestamps, elapsed wall clock,
    and the git SHA of `services/ic_engine.py` at run time.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM feature_ic_scores WHERE regime_scope='earnings_season'" | grep -qvx 0 &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `176-GATE-VERDICT.md` exists with a Run provenance section carrying the exact command, both TRAINING_WINDOW_END inputs, the effective clamped value, and the ic_engine git SHA
    - The dry-run-validity partition counts are recorded as an explicit `compute / (compute + skip)` fraction, and that fraction is `>= 0.80`
    - If the fraction is below 1.0, the skipped cells are listed with the reason each was skipped
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM feature_ic_scores WHERE feature_name IN ('earnings_season_flag','days_since_quarter_end')"` prints a non-zero value
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM feature_ic_scores WHERE regime_scope='earnings_season'"` prints a non-zero value
    - Any mid-run failure and its resume are both recorded with timestamps and the observed error
  </acceptance_criteria>
  <done>ic_engine has run to completion over the covered scope with a provably invalidated cell set, and rows exist for both new features and for the new regime scope.</done>
</task>

<task type="auto">
  <name>Task 2: Extract and write the gate verdict</name>
  <files>.planning/phases/176-earnings-season-calendar-primitive-todo-353/176-GATE-VERDICT.md</files>
  <read_first>
    - scripts/debug/analysis/debug_analyze_feature_ic.py (existing per-feature IC / FDR / walk-forward query shapes to borrow)
    - .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-EVIDENCE-A1.md (plan 176-01's measured A1 verdict — the conditioning result must be read against it, not against the todo's original numbers)
    - .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-CONTEXT.md (D-02: days_since_quarter_end is not exempted by the flag's evidence; D-04: cite 1.90x / p=5.05e-05 / 67%, never 4.3x / p=1.2e-17 / 81%)
    - .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-RECOMPUTE-LOG.md (COVERED_SCOPE — the scope statement must match it exactly)
    - services/ic_engine.py lines 5700-5800 (the ConceptRegistryService lifecycle hook, so reported transitions are attributed correctly)
    - services/ensemble_trainer.py `_eligibility_where` (as landed by plan 176-05 — Query 4 runs this predicate live)
  </read_first>
  <action>
    Write the four decision rules into `176-GATE-VERDICT.md` BEFORE running the queries, then run
    them. Do not adjust a rule after seeing its result.

    Query 1 — the primitive gate. For `feature_name IN ('earnings_season_flag',
    'days_since_quarter_end')` at this run's `training_window_end`, report per (tf, regime_scope,
    is_pooled): cell count, `n_independent`, `ic_value`, `ic_ci_lower`, `ic_sharpe_hac`,
    `bh_adjusted_p`, `passes_ci_gate`, `passes_fdr`, `wf_pass_count`/`wf_fold_count`,
    `passes_walkforward`, `reliable`. Emit `GATE_VERDICT_EARNINGS_SEASON_FLAG=` and
    `GATE_VERDICT_DAYS_SINCE_QUARTER_END=`, each exactly one of `PASS`, `FAIL`, `INSUFFICIENT_N`.
    Rule: PASS iff at least one cell has `reliable = true AND passes_fdr = true AND
    passes_walkforward = true`; INSUFFICIENT_N iff no cell reached `reliable = true` at all; FAIL
    otherwise.

    Query 2 — the conditioning result. Compare, per feature and tf, `ic_value` and `ic_sharpe_hac`
    across three groups: `regime_scope='earnings_season'` in-season cells, off-season cells, and the
    corresponding unconditioned parent cell. Report the features with the largest in-season vs
    off-season separation, and specifically report `up_vol_body_diff` — the feature whose claimed IC
    doubling (RESEARCH.md A1) motivated this workstream. Emit `CONDITIONING_VERDICT=`, one of
    `SHARPENS`, `NEUTRAL`, `DEGRADES`, `INSUFFICIENT_N`, `NOT_BUILT` (the last only when plans
    176-04/05/06 were gated off by D-01a — see the objective). Rule, pinned before running (same
    ratio convention as 176-01's A1 re-verification, for consistency): SHARPENS iff at least one
    feature that passes FDR unconditioned shows an in-season `|ic_sharpe_hac|` at least 1.7x both
    its off-season and its unconditioned value in at least two tfs; DEGRADES iff the conditioned
    cells are uniformly weaker (in-season `|ic_sharpe_hac|` <= 1.1x parent) than their parents;
    INSUFFICIENT_N iff too few season cells reached `reliable = true`; NEUTRAL otherwise (ratio
    lands between 1.1x and 1.7x, or mixed across tfs). State explicitly whether this agrees with
    `176-EVIDENCE-A1.md`'s proxy verdict — agreement or disagreement between the cheap proxy and the
    real gate is itself a finding worth recording, and it is the direct empirical answer to D-03's
    "verify the number before wiring it in" instruction.

    Query 3 — lifecycle. Report every `concept_transition_log` row written for the two new concepts
    during this run, with from/to status and the triggering metric. Never hand-edit
    `concept_registry.status` (UCR Invariant 1) — only report what the hook did. If the hook wrote
    nothing, say so and state why (e.g. gate n below `concept_gate.min_gate_n = 100`).

    Query 4 — isolation proof, live rather than unit-test-only. Confirm the measurement-only contract
    held: (a) `regime_scope='earnings_season'` rows exist; (b) running
    `ensemble_trainer._eligibility_where(False)[1]` as the WHERE clause of a COUNT against
    `feature_ic_scores` returns zero earnings-season rows; (c) this run's
    `ic_engine.regime_label_unmapped` warning count in the run log is not inflated by `in_season` /
    `off_season` / `__in_season` / `__off_season` labels. This closes the loop on plans 176-04 Task 2
    and 176-05 Task 1 with production evidence.

    Step 5 — act on the verdict, do not just record it. `alpha.ic.earnings_season_conditioned` is
    currently `true`, which means every future `ic_engine` corpus run keeps paying for the
    conditioning passes. Apply this rule, decided here before the numbers are known:

      - `CONDITIONING_VERDICT=SHARPENS` -> leave the key `true`. Record
        `CONDITIONING_ROLLBACK=RETAINED` in the verdict document with the reason.
      - `CONDITIONING_VERDICT` is `NEUTRAL`, `DEGRADES` or `INSUFFICIENT_N` -> set
        `alpha.ic.earnings_season_conditioned` to `false` through `ConfigService` (see the
        interfaces block — never a bare SQL UPDATE, the write must record `changed_by` and a
        `reason`), with a reason string that cites the verdict token and links
        `176-GATE-VERDICT.md`. Record `CONDITIONING_ROLLBACK=DISABLED` in the verdict document.

    `INSUFFICIENT_N` rolls back for the same reason `NEUTRAL` does: an unmeasurable axis is not a
    justification for paying its cost on every subsequent run. Re-enabling it later is a one-row
    APR change with no code deploy — that reversibility is exactly why the switch exists, and it is
    what makes rolling back the cheap default rather than a loss.

    Then verify the flip actually landed rather than assuming the setter succeeded: query
    `SELECT config_value FROM config_state WHERE config_key = 'alpha.ic.earnings_season_conditioned'`
    and the most recent matching `config_history` row, and paste both into the verdict document
    beside the `CONDITIONING_ROLLBACK=` token. The recorded value must agree with the rule above: it
    is `false` if and only if the verdict is not `SHARPENS`.

    Finish the document with a "Scope of this verdict" section quoting the COVERED_SCOPE manifest's
    symbol count, tf list, row count and `MAX(bar_ts)`, followed by an explicit limitations
    paragraph covering: whether an OOS holdout was in effect; that `days_since_quarter_end`
    correlates 0.935 (Spearman) with the pre-existing `quarter_position`, so its marginal
    contribution is better read from `partial_ic` / `partial_ic_p_value` / `passes_partial_fdr` than
    from raw `ic_value`; and D-04's autocorrelation caveat on the original proxy evidence.
  </action>
  <verify>
    <automated>grep -qE "GATE_VERDICT_EARNINGS_SEASON_FLAG=(PASS|FAIL|INSUFFICIENT_N)" .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-GATE-VERDICT.md &amp;&amp; grep -qE "GATE_VERDICT_DAYS_SINCE_QUARTER_END=(PASS|FAIL|INSUFFICIENT_N)" .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-GATE-VERDICT.md &amp;&amp; grep -qE "CONDITIONING_VERDICT=(SHARPENS|NEUTRAL|DEGRADES|INSUFFICIENT_N|NOT_BUILT)" .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-GATE-VERDICT.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All three verdict tokens are present, each with exactly one allowed value
    - The four decision rules appear in the document ABOVE their result tables
    - The per-(tf, regime_scope) result table is present for both new features, with n, IC, CI, HAC Sharpe, FDR and walk-forward columns
    - `up_vol_body_diff`'s in-season vs off-season vs unconditioned comparison is reported by name, and compared against `176-EVIDENCE-A1.md`'s verdict
    - The `concept_transition_log` result is reported, or its absence explained
    - The isolation proof reports zero earnings-season rows selectable by the ensemble eligibility predicate
    - The Scope section matches `176-RECOMPUTE-LOG.md`'s COVERED_SCOPE figures exactly
    - The document cites D-04's corrected 1.90x / p=5.05e-05 / 67% figures and nowhere cites 4.3x / p=1.2e-17 / 81%
    - `grep -qE "CONDITIONING_ROLLBACK=(RETAINED|DISABLED)" .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-GATE-VERDICT.md` succeeds with exactly one token present
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT config_value FROM config_state WHERE config_key='alpha.ic.earnings_season_conditioned'"` prints `false` if and only if `CONDITIONING_VERDICT` is not `SHARPENS`, and the printed value is quoted verbatim in the verdict document
    - When the rollback fired, a `config_history` row exists for that key whose `reason` contains the verdict token and references `176-GATE-VERDICT.md`, and that row is quoted in the document
    - When the rollback did NOT fire, the document states `CONDITIONING_ROLLBACK=RETAINED` and shows the live `config_state` value is still `true`
  </acceptance_criteria>
  <done>The phase's completion criterion is a written, measured verdict with fixed-in-advance decision rules, an honest scope statement, live proof that the new scope stayed measurement-only, and an APR state that matches the verdict rather than silently taxing every future run.</done>
</task>

<task type="auto">
  <name>Task 3: Close todo 353 and correct the project-state record</name>
  <files>.planning/todos/PRIORITIES.md, .planning/STATE.md</files>
  <read_first>
    - .planning/todos/pending/353-earnings-season-calendar-primitive-candidate.md (the todo being closed)
    - .planning/todos/PRIORITIES.md lines 230-260 (both references to todo 353: the 2026-08-26 drift-catch note and the priority table row)
    - tests/unit/test_todo_priorities_link_integrity.py (the CI gate that fails on a stale link)
    - .planning/STATE.md lines 236-243 (the Phase 176 Roadmap Evolution bullet carrying the superseded proxy numbers)
    - .planning/phases/176-earnings-season-calendar-primitive-todo-353/176-GATE-VERDICT.md (the verdict this closure record must reference)
    - .planning/todos/completed/ (an existing completed todo, for the closure-annotation convention this repo uses)
  </read_first>
  <action>
    Move the todo: `git mv .planning/todos/pending/353-earnings-season-calendar-primitive-candidate.md
    .planning/todos/completed/`. Append a dated closure note to the moved file recording the phase
    directory, the three verdict tokens, and one line on what shipped (two calendar primitives plus
    the ic_engine earnings-season conditioning axis). Correct the todo's own body where it still
    states the superseded 4.3x / p=1.2e-17 / 81% proxy numbers — annotate them as superseded by D-04
    rather than deleting them, so the correction itself stays auditable.

    Update both `PRIORITIES.md` references to point at `completed/353-...md`. Do not delete the
    priority table row if this repo's convention is to retain closed rows; follow whatever the
    neighbouring recently-closed entries do.

    Update STATE.md's Phase 176 Roadmap Evolution bullet: replace the superseded proxy numbers with
    D-04's corrected 1.90x / Welch p=5.05e-05 / 67% (155/233) on the 14-42-day window, change the
    status from "Not planned yet" to executed with its verdict tokens, and link the phase directory
    and `176-GATE-VERDICT.md`. Keep the bullet compressed to current status only, matching that
    section's stated convention — do not add a planning narrative.

    Staging order matters (CLAUDE.md): `git add -- .planning/todos/completed/353-...md` alone first
    so git registers the rename, then stage the remaining paths.

    Run the CI gate before committing: `.venv/bin/pytest tests/unit/test_todo_priorities_link_integrity.py -x -q`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_todo_priorities_link_integrity.py -x -q &amp;&amp; test ! -f .planning/todos/pending/353-earnings-season-calendar-primitive-candidate.md &amp;&amp; test -f .planning/todos/completed/353-earnings-season-calendar-primitive-candidate.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_todo_priorities_link_integrity.py -x -q` exits 0
    - The todo file exists under `completed/` and not under `pending/`
    - `grep -c "pending/353-" .planning/todos/PRIORITIES.md` is `0`
    - The moved todo carries a dated closure note naming the phase directory and all three verdict tokens
    - `grep -c "4.3x\|1.2e-17" .planning/STATE.md` is `0`
    - STATE.md's Phase 176 bullet cites 1.90x, p=5.05e-05 and 67%, and links `176-GATE-VERDICT.md`
    - `git log --oneline -1` shows the closure committed
  </acceptance_criteria>
  <done>todo 353 is closed with its verdict recorded, and the project-state record cites the corrected evidence rather than the superseded proxy numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `feature_vectors` -> `feature_ic_scores` | a multi-day measurement run whose output becomes the phase's evidence of record |
| new `regime_scope` rows -> ensemble weighting / alpha emission | an unpromoted measurement axis adjacent to a production decision path |
| verdict document -> future phases | a scope claim later work will build on without re-deriving |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-08-01 | Tampering | A fingerprint-valid cell set causing ic_engine to silently serve pre-phase results as if they were this phase's measurement | mitigate | Mandatory `--dry-run-validity` pre-check with a numeric STOP condition pinned before the run — proceed only if compute >= 80% of candidate cells, recorded as an explicit fraction; the three moved fingerprint components are reasoned in the interfaces block and re-verified empirically; skips below 100% must be individually explained in the verdict document |
| T-176-08-02 | Tampering | Verdict criteria adjusted after seeing the numbers, converting a FAIL into a PASS | mitigate | All four decision rules are written into the document before the queries run, asserted by an acceptance criterion on rule-above-results ordering; the objective states a FAIL is a complete outcome |
| T-176-08-03 | Information Disclosure | The verdict overstating its own scope (claiming corpus-wide when a staged recompute covered less) | mitigate | Scope section must quote `176-RECOMPUTE-LOG.md`'s COVERED_SCOPE figures exactly; limitations paragraph mandatory, including OOS-holdout status |
| T-176-08-04 | Elevation of Privilege | Earnings-season cells reaching ensemble weighting despite plan 176-05's guard | mitigate | Query 4 runs the live `_eligibility_where` predicate against the real table and asserts zero earnings-season rows selected — production evidence, not a unit test |
| T-176-08-05 | Repudiation | A concept status flipped by hand rather than by `ConceptRegistryService`, breaking UCR Invariant 1 and the transition audit trail | mitigate | Task 2 reports `concept_transition_log` only; hand-editing `concept_registry.status` is explicitly prohibited in both the interfaces block and the action |
| T-176-08-06 | Denial of Service | The multi-day run colliding with another corpus job and deadlocking | mitigate | Pre-flight competing-process and `pg_stat_activity` checks identical to plan 176-07 Task 1; abort rather than proceed |
| T-176-08-07 | Denial of Service | A `NEUTRAL`/`DEGRADES`/`INSUFFICIENT_N` conditioning verdict recorded but not acted on, leaving `alpha.ic.earnings_season_conditioned` true and charging every future 11-76h `ic_engine` corpus run for a measurement axis that was already answered in the negative | mitigate | Task 2 Step 5 makes the rollback an explicit action with a rule pinned before the numbers are known; the `CONDITIONING_ROLLBACK=` token is required by an automated grep; the live `config_state` value and its `config_history` reason row must both be quoted in the verdict document and must agree with the verdict |
| T-176-08-08 | Repudiation | The APR key flipped by a bare SQL UPDATE, leaving no `config_history` trail of why conditioning was disabled | mitigate | The interfaces block and Task 2 Step 5 both require the write to go through `ConfigService` with `changed_by` and a `reason` citing the verdict token; an acceptance criterion asserts the `config_history` row exists and names `176-GATE-VERDICT.md` |
| T-176-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- The `--dry-run-validity` compute fraction is recorded and is >= 0.80
- `feature_ic_scores` carries rows for both new features and for `regime_scope='earnings_season'`
- `176-GATE-VERDICT.md` carries all three verdict tokens with decision rules stated above the results
- The ensemble eligibility predicate selects zero earnings-season rows against the live table
- `.venv/bin/pytest tests/unit/ -q` green
- `.venv/bin/pytest tests/unit/test_todo_priorities_link_integrity.py -x -q` green
- `alpha.ic.earnings_season_conditioned`'s live `config_state` value agrees with the recorded
  `CONDITIONING_ROLLBACK=` token, with a matching `config_history` reason row when it was flipped
</verification>

<success_criteria>
- Both new primitives have a real, recorded `feature_ic_scores` FDR/walk-forward verdict — the
  phase's stated completion criterion, measured rather than asserted
- The regime-conditioning workstream has a measured `CONDITIONING_VERDICT`, compared explicitly
  against plan 176-01's proxy-measured A1 verdict
- The verdict document states its scope and limitations honestly and cites D-04's corrected numbers
- The conditioning APR switch is left in a state that matches the measured verdict — retained only
  on `SHARPENS`, rolled back to `false` with a recorded `config_history` reason otherwise
- todo 353 is closed, PRIORITIES.md link integrity passes CI, and STATE.md no longer carries the
  superseded proxy figures
</success_criteria>

<output>
Create `.planning/phases/176-earnings-season-calendar-primitive-todo-353/176-08-SUMMARY.md` when done
</output>
