---
phase: 109-config-foundation-self-healing-engine
plan: 05
type: execute
wave: 5
depends_on: [109-04]
files_modified:
  - services/self_healing_agent.py
  - production/systemd/indicagent-self-healing-agent.service
  - src/config/settings.py
  - services/service_auditor_agent.py
autonomous: true
must_haves:
  truths:
    - "SelfHealingAgent HTTP service accepts webhooks"
    - "Runtime params migrated from settings.py to config DB"
    - "Lag thresholds loaded from config instead of _LAG_THRESHOLDS"
    - "AI agent shadow_mode loaded from config instead of hardcoded"
    - "Config changes hot-reload without service restart"
  artifacts:
    - path: "services/self_healing_agent.py"
      provides: "SelfHealingAgent HTTP service"
      exports: ["app", "/webhook/alertmanager"]
    - path: "src/config/settings.py"
      provides: "Settings with runtime params removed"
      contains: "get_config_value method"
    - path: "services/service_auditor_agent.py"
      provides: "ServiceAuditorAgent without _LAG_THRESHOLDS"
      contains: "_load_lag_thresholds method"
  key_links:
    - from: "services/self_healing_agent.py"
      to: "src/self_healing/engine.py"
      via: "import"
      pattern: "from src.self_healing.engine import SelfHealingEngine"
    - from: "services/service_auditor_agent.py"
      to: "src/config/config_service.py"
      via: "get_config_value"
      pattern: "get_config\('alert.lag"
user_setup:
  - service: prometheus-alertmanager
    why: "Alertmanager webhook integration for self-healing triggers"
    env_vars:
      - name: CONFIG_SELF_HEALING_ENABLED
        source: ".env file or systemd environment"
    dashboard_config:
      - task: "Configure Alertmanager webhook URL"
        location: "Alertmanager config file (usually /etc/alertmanager/alertmanager.yml)"
---

<objective>
Create SelfHealingAgent systemd service, migrate runtime params from settings.py to config system, and clean up technical debt (remove hardcoded values).

Purpose: Complete config foundation with self-healing agent deployment, migrate existing runtime params to DB-backed config, and remove hardcoded values per Renaissance principle.
Output: SelfHealingAgent service, migration of 15+ runtime params, cleanup of _LAG_THRESHOLDS and shadow_only flags
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-config-foundation-self-healing-engine/109-CONTEXT.md
@.planning/phases/109-config-foundation-self-healing-engine/109-RESEARCH.md
@.planning/phases/109-config-foundation-self-healing-engine/109-PATTERNS.md
@.planning/phases/109-config-foundation-self-healing-engine/109-04-SUMMARY.md

@src/self_healing/engine.py (SelfHealingEngine)
@src/config/config_service.py (ConfigService)
@src/config/settings.py (current runtime params)
@services/service_auditor_agent.py (_LAG_THRESHOLDS)
@services/alerting_agent.py (BaseAgent pattern)
@production/systemd/indicagent-alerting-agent.service
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SelfHealingAgent systemd service</name>
  <files>services/self_healing_agent.py, production/systemd/indicagent-self-healing-agent.service</files>
  <read_first>
    - src/self_healing/engine.py (SelfHealingEngine)
    - src/core/agent/base.py (BaseAgent)
    - services/alerting_agent.py (FastAPI webhook pattern)
    - production/systemd/indicagent-alerting-agent.service
  </read_first>
  <action>
    Create services/self_healing_agent.py with:

    1. #!/usr/bin/env python3
    """SelfHealingAgent — receives Alertmanager webhooks, executes remediation strategies.

    Control-theory-based self-healing:
    - Sensor: measure pre-state from Prometheus
    - Setpoint: compare to threshold
    - Actuator: execute remediation action
    - Feedback: measure post-state, record outcome

    Conservative Phase 1 strategies: disk cleanup, consumer restart, pool flush.
    """

    2. from fastapi import FastAPI, HTTPException
    from fastapi.responses import JSONResponse

    3. app = FastAPI(title="IndicAgent Self-Healing Engine", version="1.0.0")

    4. self_healing_engine: SelfHealingEngine | None = None

    5. @app.on_startup("startup")
    async def startup():
       from src.config.settings import get_settings
       from src.core.database_manager import create_pool
       settings = get_settings()
       pool = await create_pool(settings.database_url, pool_name="self_healing")
       global self_healing_engine
       self_healing_engine = SelfHealingEngine(pool)
       await self_healing_engine.initialize()

    6. @app.post("/webhook/alertmanager")
    async def webhook_alertmanager(payload: dict):
       if not self_healing_engine:
           raise HTTPException(status_code=503, detail="Engine not initialized")
       WEBHOOK_RECEIVED_TOTAL.add(1, {"severity": payload.get("severity", "unknown")})
       try:
           result = await self_healing_engine.handle_webhook(payload)
           return JSONResponse(content={"remediation_id": result.remediation_id, "status": result.status}, status_code=200)
       except Exception as exc:
           WEBHOOK_VALIDATION_FAILED_TOTAL.add(1, {"reason": type(exc).__name__})
           raise HTTPException(status_code=400, detail=str(exc))

    7. if __name__ == "__main__":
       import uvicorn
       uvicorn.run(app, host="0.0.0.0", port=9002)

    8. Add shebang #!/usr/bin/env python3

    Create production/systemd/indicagent-self-healing-agent.service:
    [Unit]
    Description=IndicAgent Self-Healing Engine — Alertmanager webhook receiver with automated remediation
    After=network-online.target indicagent-infrastructure.target
    Requires=indicagent-infrastructure.target

    [Service]
    Type=simple
    User=bg
    WorkingDirectory=/home/bg/dev/indicagent
    Environment=PYTHONPATH=/home/bg/dev/indicagent
    Environment=PYTHONUNBUFFERED=1
    Environment=METRICS_PORT=9007
    ExecStart=/home/bg/dev/indicagent/.venv/bin/python services/self_healing_agent.py
    Restart=always
    WatchdogSec=60
    NotifyAccess=main
    RestartSec=10

    [Install]
    WantedBy=multi-user.target

    Follow FastAPI webhook pattern. Use port 9002. Emit WEBHOOK_RECEIVED_TOTAL and WEBHOOK_VALIDATION_FAILED_TOTAL metrics.
  </action>
  <verify>
    Run: .venv/bin/python services/self_healing_agent.py (verify server starts on port 9002)
    Run: curl -X POST http://localhost:9002/webhook/alertmanager -H "Content-Type: application/json" -d '{"alert_id": "test-123", "state_variable": "disk_usage", "current_value": 85.0, "threshold": 80.0}' (verify returns 200)
  </verify>
  <done>
    SelfHealingAgent HTTP service on port 9002. POST /webhook/alertmanager receives alerts, delegates to SelfHealingEngine. Returns remediation_id and status. Systemd unit created with WatchdogSec=60.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate runtime params from settings.py to config system</name>
  <files>src/config/settings.py, production/migrations/109_config_foundation.sql</files>
  <read_first>
    - src/config/settings.py (existing runtime params)
    - production/migrations/109_config_foundation.sql (config_schema seed data)
  </read_first>
  <action>
    Modify src/config/settings.py:

    1. Remove ~15 runtime params that are now in config system:
       - REGIME_PROB_MIN → config key "regime.prob_min"
       - REGIME_DUR_MIN → config key "regime.dur_min"
       - SWARM_MIN_CONFIDENCE → config key "swarm.min_confidence"
       - SWARM_MIN_TF_MINUTES → config key "swarm.min_tf_minutes"
       - SWARM_WEIGHT_MIN_SAMPLES → config key "swarm.weight_min_samples"
       - SWARM_WEIGHT_FLOOR → config key "swarm.weight_floor"
       - SWARM_MAX_CONCURRENT_CALLS → config key "swarm.max_concurrent_calls"
       - ROLL_MONITOR_WINDOW_SIZE → config key "roll.monitor_window_size"
       - ROLL_MONITOR_THRESHOLD_DEFAULT → config key "roll.threshold_default"
       - ROLL_MONITOR_POSTROLL_BARS → config key "roll.postroll_bars"
       - ROLL_MONITOR_COOLDOWN_MIN → config key "roll.cooldown_min"
       - ROLL_CONFIRMATION_BARS → config key "roll.confirmation_bars"
       - ROLL_TIME_OF_DAY_GATED → config key "roll.time_of_day_gated"
       - CROSS_ASSET_WINDOW_BARS → config key "cross_asset.window_bars"
       - MACRO_WINDOW_BARS → config key "macro.window_bars"

    2. For each removed param, add classmethod @classmethod def get_config_value(cls, key: str, default: Any = None) -> Any: return from config cache via BaseAgent.get_config if available, else default

    3. Update call sites to use Settings.get_config_value("regime.prob_min", 0.30) instead of Settings.regime_prob_min

    4. Add comment explaining migration to config system

    Update production/migrations/109_config_foundation.sql seed data:

    5. Add INSERT rows for each migrated param with:
       - config_key: dotted notation (e.g., "regime.prob_min")
       - value_type: 'int', 'float', 'bool', or 'string'
       - default_value: current value from settings.py
       - category: 'OPS'
       - description: same as existing field description

    6. Add config_state rows with current values

    Follow exact dotted notation from CONTEXT.md migration table. Preserve existing values as defaults. Category is 'OPS' for all (hot-reloadable).
  </action>
  <verify>
    Run: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT config_key, default_value FROM config_schema WHERE config_key LIKE 'regime.%' OR config_key LIKE 'swarm.%' OR config_key LIKE 'roll.%'" (verify migrated keys exist)
    Run: .venv/bin/mypy src/config/settings.py (no errors for removed fields)
  </verify>
  <done>
    15 runtime params removed from settings.py. Settings.get_config_value() method added for backward compatibility. All 15 params seeded in config_schema with current values. config_state has current values. Dotted notation used (regime.prob_min not REGIME_PROB_MIN).
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove _LAG_THRESHOLDS from service_auditor_agent.py</name>
  <files>services/service_auditor_agent.py, production/migrations/109_config_foundation.sql</files>
  <read_first>
    - services/service_auditor_agent.py (_LAG_THRESHOLDS dict)
    - production/migrations/109_config_foundation.sql (config_schema)
  </read_first>
  <action>
    Modify services/service_auditor_agent.py:

    1. Remove _LAG_THRESHOLDS dict (~25 entries)

    2. Add async def _load_lag_thresholds(self) -> None:
       a. Query config_state WHERE config_key LIKE 'alert.lag.%'
       b. Parse into dict: {service_name: threshold_value}
       c. Store as self._lag_thresholds

    3. Call await self._load_lag_thresholds() in _setup after super()._setup()

    4. Update lag check to use self._lag_thresholds.get(unit_name, 0) instead of _LAG_THRESHOLDS.get(unit_name, 0)

    5. Add config reload support: in _reload_config_loop, if key starts with "alert.lag.", call await self._load_lag_thresholds()

    Update production/migrations/109_config_foundation.sql:

    6. Add config_schema rows for each lag threshold:
       - config_key: "alert.lag.feature-writer" (unit name with kebab-case)
       - value_type: 'int'
       - default_value: current threshold from _LAG_THRESHOLDS
       - category: 'OPS'
       - description: "Consumer lag threshold for {unit}"

    7. Add config_state rows with current threshold values

    Follow CONTEXT.md migration table: alert.lag.* keys. Use kebab-case for unit names (indicagent-feature-writer → feature-writer in config key). Load thresholds on startup and reload on config change.
  </action>
  <verify>
    Run: grep "_LAG_THRESHOLDS" services/service_auditor_agent.py (should return 0 results)
    Run: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM config_schema WHERE config_key LIKE 'alert.lag.%'" (should return 25+)
    Run: .venv/bin/python -c "from services.service_auditor_agent import ServiceAuditorAgent; print('Agent imports without _LAG_THRESHOLDS')"
  </verify>
  <done>
    _LAG_THRESHOLDS dict removed from service_auditor_agent.py. _load_lag_thresholds() loads from config DB. Config reload updates thresholds on alert.lag.* changes. 25+ lag thresholds seeded in config_schema.
  </done>
</task>

<task type="auto">
  <name>Task 4: Remove hardcoded shadow_only flags from AI agents</name>
  <files>src/intelligence/ai/skeptic_agent.py, src/intelligence/ai/correlation_agent.py, src/intelligence/ai/volume_agent.py, production/migrations/109_config_foundation.sql</files>
  <read_first>
    - src/intelligence/ai/skeptic_agent.py (shadow_only pattern)
    - production/migrations/109_config_foundation.sql (config_schema)
  </read_first>
  <action>
    For each AI agent (skeptic, correlation, volume, etc.) with hardcoded shadow_only=True:

    1. Remove shadow_only=True from class definition

    2. Add shadow_only: bool = False class attribute (default False)

    3. In __init__ or agent setup, check config for override:
       self.shadow_only = self.get_config(f"ai.agent.{self.agent_id}.shadow_mode", False)

    4. Update agent registration to use config value

    Update production/migrations/109_config_foundation.sql:

    5. Add config_schema rows for each agent:
       - config_key: "ai.agent.skeptic.shadow_mode" (agent_id from agent.agent_id)
       - value_type: 'bool'
       - default_value: "true" (current state: all agents are shadow-only)
       - category: 'OPS'
       - description: "Shadow mode for {agent_id} agent"

    6. Add config_state rows with "true" values

    Follow CONTEXT.md migration table: ai.agent.shadow_mode keys. Use agent.agent_id for key suffix. Default to "true" (current state). Config hot-reload enables promotion without deployment.
  </action>
  <verify>
    Run: grep "shadow_only = True" src/intelligence/ai/*.py (should return 0 results)
    Run: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT config_key, default_value FROM config_schema WHERE config_key LIKE 'ai.agent.%'" (verify agent shadow_mode keys exist)
    Run: .venv/bin/mypy src/intelligence/ai/*.py (no new errors)
  </verify>
  <done>
    Hardcoded shadow_only=True removed from AI agents. shadow_only class attribute defaults to False, reads from config via get_config(). 8+ agent shadow_mode keys seeded in config_schema with default "true". Config hot-reload enables promotion.
  </done>
</task>

</tasks>

<verification>
Verify SelfHealingAgent systemd unit can start. Test webhook endpoint with sample alert. Verify all migrated config keys exist in DB. Verify _LAG_THRESHOLDS removed. Verify shadow_only flags removed.
</verification>

<success_criteria>
1. SelfHealingAgent HTTP service on port 9002
2. POST /webhook/alertmanager endpoint accepts alerts
3. 15 runtime params migrated from settings.py to config system
4. Settings.get_config_value() method for backward compatibility
5. _LAG_THRESHOLDS dict removed from service_auditor_agent.py
6. Lag thresholds loaded from config DB
7. Hardcoded shadow_only flags removed from AI agents
8. Agent shadow_mode loaded from config DB
9. All config changes hot-reloadable without restart
</success_criteria>

<output>
After completion, create `.planning/phases/109-config-foundation-self-healing-engine/109-05-SUMMARY.md` with:
- SelfHealingAgent deployment status
- Runtime params migration summary (15 params)
- Lag thresholds migration summary (25+ entries)
- Shadow flags cleanup summary (8+ agents)
- Total technical debt removed
- Next steps for user (Alertmanager webhook configuration)
</output>
