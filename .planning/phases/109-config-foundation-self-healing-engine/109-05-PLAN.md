---
phase: 109-config-foundation-self-healing-engine
plan: 05
type: execute
wave: 5
depends_on: [109-04]
files_modified:
  - services/self_healing_agent.py
  - production/systemd/indicagent-self-healing-agent.service
  - src/config/settings.py
  - src/config/runtime_defaults.py
  - src/self_healing/pool_manager.py
  - services/service_auditor_agent.py
  - services/alpha_swarm_agent.py
  - src/intelligence/ai/alpha/correlation_agent.py
  - src/intelligence/ai/alpha/counterfactual_agent.py
  - src/intelligence/ai/alpha/regime_coherence_agent.py
  - src/intelligence/ai/alpha/ml_scorer_agent.py
  - production/migrations/109_config_foundation.sql
autonomous: true
must_haves:
  truths:
    - "SelfHealingAgent HTTP service accepts webhooks on port 9002"
    - "OTel metrics exposed on port 9007 (distinct from HTTP API)"
    - "Webhook requires shared secret authentication"
    - "Runtime defaults extracted to runtime_defaults.py (typed _DEFAULT_* constants)"
    - "Settings.get_config_value() returns typed _DEFAULT_* fallback (NOT None) when config unavailable"
    - "SWARM_*/REGIME_*/ROLL_*/CROSS_ASSET_*/MACRO_* Settings fields remain in place for Phase 110 (zero call-site disruption this phase)"
    - "Lag thresholds loaded from config instead of _LAG_THRESHOLDS"
    - "AI agent shadow_only defaults to True (fail-closed)"
    - "AI agent shadow_only reads from config if available"
    - "Config changes hot-reload without service restart"
    - "AlphaSwarmComputeAgent routes ai.agent.*.shadow_mode config updates to individual agents via _on_config_message_received override"
    - "Config DB takes precedence over shadow_registry for shadow_only when a config entry exists (D-07 precedence)"
  artifacts:
    - path: "services/self_healing_agent.py"
      provides: "SelfHealingAgent HTTP service on port 9002"
      exports: ["app", "/webhook/alertmanager"]
    - path: "src/config/runtime_defaults.py"
      provides: "Typed code-level default constants for migrated OPS params (fallback)"
      contains: "_DEFAULT_REGIME_PROB_MIN, _DEFAULT_SWARM_MIN_CONFIDENCE, ..."
    - path: "src/config/settings.py"
      provides: "Settings with get_config_value() shim that returns typed RUNTIME_DEFAULTS fallback. Existing fields retained for back-compat; removal deferred to Phase 110."
      contains: "get_config_value method"
    - path: "services/service_auditor_agent.py"
      provides: "ServiceAuditorAgent without _LAG_THRESHOLDS"
      contains: "_load_lag_thresholds method"
    - path: "services/alpha_swarm_agent.py"
      provides: "AlphaSwarmComputeAgent with _on_config_message_received override + D-07 precedence in _refresh_shadow_state_from_registry"
      contains: "_on_config_message_received, _refresh_shadow_state_from_registry"
  key_links:
    - from: "services/self_healing_agent.py"
      to: "src/self_healing/engine.py"
      via: "import"
      pattern: "from src.self_healing.engine import SelfHealingEngine"
    - from: "services/self_healing_agent.py"
      to: "src/self_healing/pool_manager.py"
      via: "import"
      pattern: "from src.self_healing.pool_manager import ManagedPool"
    - from: "src/config/settings.py"
      to: "src/config/runtime_defaults.py"
      via: "import inside get_config_value"
      pattern: "from src.config.runtime_defaults import RUNTIME_DEFAULTS"
    - from: "services/service_auditor_agent.py"
      to: "src/core/agent/base.py"
      via: "get_config"
      pattern: "self.get_config\\('alert.lag"
    - from: "services/alpha_swarm_agent.py"
      to: "src/intelligence/ai/alpha/correlation_agent.py"
      via: "_on_config_message_received delegation"
      pattern: "agent._apply_shadow_mode_config"
user_setup:
  - service: prometheus-alertmanager
    why: "Alertmanager webhook integration for self-healing triggers"
    env_vars:
      - name: CONFIG_SELF_HEALING_ENABLED
        source: ".env file or systemd environment"
      - name: CONFIG_WEBHOOK_SHARED_SECRET
        source: "Generate secure secret and set in .env or systemd environment"
      - name: PROMETHEUS_URL
        source: "Defaults to http://localhost:9090; override via env if Prometheus runs elsewhere"
    dashboard_config:
      - task: "Configure Alertmanager webhook URL"
        location: "Alertmanager config file (usually /etc/alertmanager/alertmanager.yml)"
        value: "http://localhost:9002/webhook/alertmanager"
      - task: "Set shared secret in Alertmanager config"
        location: "Alertmanager webhook configuration"
        value: "Same value as CONFIG_WEBHOOK_SHARED_SECRET"

---

<objective>
Create SelfHealingAgent systemd service on port 9002 (OTel metrics on 9007), introduce runtime defaults fallback module + Settings.get_config_value() shim WITHOUT removing the existing SWARM_*/REGIME_*/ROLL_*/CROSS_ASSET_*/MACRO_* Settings fields, and clean up technical debt (remove hardcoded _LAG_THRESHOLDS and hardcoded shadow_only flags) while maintaining fail-closed defaults. Wire AlphaSwarmComputeAgent to route ai.agent.*.shadow_mode config updates to individual AI agents.

Review findings addressed:
- Consensus MEDIUM-HIGH: settings.py defaults must remain typed. Solution: src/config/runtime_defaults.py exports _DEFAULT_* constants; Settings.get_config_value() falls back to those constants, never None.
- Checker BLOCKER (109-05 requirement_coverage): services/alpha_swarm_agent.py still references self.settings.SWARM_* at 5 call sites. To eliminate cross-plan migration risk and keep this phase shippable, we DEFER the SWARM_*/REGIME_*/ROLL_*/CROSS_ASSET_*/MACRO_* field REMOVAL and the alpha_swarm_agent.py SWARM_* call-site migration to Phase 110. This phase only adds the runtime_defaults.py module and the Settings.get_config_value() shim (additive, zero call-site disruption to SWARM_* references).
- Revision BLOCKER (constructor mismatch): Plan 04 defines `ManagedPool.__init__(self, database_url: str, ...)` — it owns pool creation internally. Plan 05 Task 1 startup MUST call `ManagedPool(settings.database_url, pool_name="self_healing")` directly, NOT pre-create a raw pool and pass it in (TypeError).
- Revision BLOCKER (shadow_only hot-reload path): Individual AI agents (correlation, counterfactual, regime_coherence, ml_scorer) do NOT have their own `_setup()` or Kafka consumer — they live inside AlphaSwarmComputeAgent.self._agents and are constructed in AlphaSwarmComputeAgent._setup(). The Kafka delivery path for ai.agent.*.shadow_mode updates MUST go through AlphaSwarmComputeAgent._on_config_message_received override. Adding that override is Task 4's responsibility.
- D-07 precedence (locked decision): Config DB takes precedence over shadow_registry for shadow_only when a config entry exists. _refresh_shadow_state_from_registry must check `get_config('ai.agent.{agent_id}.shadow_mode')` first and only fall back to shadow_registry if no config entry exists.
- Codex MEDIUM (port mismatch): explicit: HTTP API 9002, OTel metrics 9007.
- Codex MEDIUM (migration rollout order): documented in summary; migration applied first, services restarted last.
- Checker WARNING (Task 4 files_modified): replaced inaccurate file list (volume_agent.py does not exist; skeptic_agent.py has shadow_only=False) with the verified list of agents whose `shadow_only = True`: correlation_agent.py, counterfactual_agent.py, regime_coherence_agent.py, ml_scorer_agent.py.

Output: SelfHealingAgent service, additive runtime_defaults.py module, Settings.get_config_value() shim, _LAG_THRESHOLDS cleanup, AI agent shadow_only cleanup (correlation/counterfactual/regime_coherence/ml_scorer) with AlphaSwarmComputeAgent as the Kafka-delivery vehicle.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-config-foundation-self-healing-engine/109-CONTEXT.md
@.planning/phases/109-config-foundation-self-healing-engine/109-RESEARCH.md
@.planning/phases/109-config-foundation-self-healing-engine/109-PATTERNS.md
@.planning/phases/109-config-foundation-self-healing-engine/109-REVIEWS.md
@.planning/phases/109-config-foundation-self-healing-engine/109-04-SUMMARY.md

@src/self_healing/engine.py
@src/self_healing/pool_manager.py
@src/config/config_service.py
@src/config/settings.py
@services/service_auditor_agent.py
@services/alerting_agent.py
@services/alpha_swarm_agent.py
@production/systemd/indicagent-alerting-agent.service
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SelfHealingAgent HTTP service on port 9002</name>
  <files>services/self_healing_agent.py, production/systemd/indicagent-self-healing-agent.service</files>
  <read_first>
    - src/self_healing/engine.py (SelfHealingEngine handle_webhook contract)
    - src/self_healing/pool_manager.py (Plan 04: ManagedPool.__init__(database_url, pool_name, ...) — creates pool internally)
    - services/alerting_agent.py (FastAPI webhook pattern)
    - production/systemd/indicagent-alerting-agent.service
  </read_first>
  <action>
    Create services/self_healing_agent.py:

    #!/usr/bin/env python3
    """SelfHealingAgent — receives Alertmanager webhooks, executes remediation strategies.

    Port convention:
      9002 = HTTP API (uvicorn) for /webhook/alertmanager
      9007 = OTel metrics scrape endpoint (set via METRICS_PORT in systemd unit)

    Authentication is defense-in-depth: both this HTTP layer AND the engine layer
    (src/self_healing/engine.py) verify CONFIG_WEBHOOK_SHARED_SECRET. Engine-layer
    auth exists because the engine may be invoked from non-HTTP paths in future.

    Pool management: ManagedPool (Plan 04) owns pool creation internally. Pass
    settings.database_url directly to ManagedPool(...) — do NOT pre-create a raw
    asyncpg.Pool. ManagedPool.__init__ signature is
    `ManagedPool(database_url: str, pool_name: str = ..., drain_timeout_seconds: float = ..., **pool_kwargs)`.
    """

    from fastapi import FastAPI, HTTPException, Request
    from fastapi.responses import JSONResponse
    import os

    app = FastAPI(title="IndicAgent Self-Healing Engine", version="1.0.0")
    self_healing_engine: "SelfHealingEngine | None" = None
    _WEBHOOK_SECRET = os.getenv("CONFIG_WEBHOOK_SHARED_SECRET")

    @app.on_event("startup")
    async def startup():
        from src.config.settings import get_settings
        from src.self_healing.engine import SelfHealingEngine
        from src.self_healing.pool_manager import ManagedPool
        settings = get_settings()
        # ManagedPool owns pool creation (Plan 04 contract). Do NOT pre-create a raw pool.
        managed_pool = ManagedPool(settings.database_url, pool_name="self_healing")
        await managed_pool.initialize()
        global self_healing_engine
        self_healing_engine = SelfHealingEngine(managed_pool)
        await self_healing_engine.initialize()

    @app.on_event("shutdown")
    async def shutdown():
        if self_healing_engine:
            await self_healing_engine.close()

    @app.post("/webhook/alertmanager")
    async def webhook_alertmanager(request: Request):
        from src.observability.metrics import WEBHOOK_RECEIVED_TOTAL, WEBHOOK_AUTH_FAILED_TOTAL, WEBHOOK_VALIDATION_FAILED_TOTAL
        if not self_healing_engine:
            raise HTTPException(status_code=503, detail="Engine not initialized")

        payload = await request.json()

        # HTTP-layer auth (engine ALSO verifies; defense-in-depth)
        if _WEBHOOK_SECRET:
            secret = payload.get("webhook_secret") or payload.get("shared_secret") or request.headers.get("X-Webhook-Secret")
            if secret != _WEBHOOK_SECRET:
                WEBHOOK_AUTH_FAILED_TOTAL.add(1, {"layer": "http", "reason": "invalid_secret"})
                raise HTTPException(status_code=401, detail="Invalid webhook shared secret")

        WEBHOOK_RECEIVED_TOTAL.add(1, {"severity": payload.get("severity", "unknown")})
        try:
            result = await self_healing_engine.handle_webhook(payload)
            return JSONResponse(content={"remediation_id": result.remediation_id, "status": result.status, "error": result.error}, status_code=200)
        except HTTPException:
            raise
        except Exception as exc:
            WEBHOOK_VALIDATION_FAILED_TOTAL.add(1, {"reason": type(exc).__name__})
            raise HTTPException(status_code=400, detail=str(exc))

    if __name__ == "__main__":
        import uvicorn
        # Port 9002 = HTTP API. OTel metrics on 9007 via METRICS_PORT env (systemd unit).
        uvicorn.run(app, host="0.0.0.0", port=9002)

    Create production/systemd/indicagent-self-healing-agent.service:
    [Unit]
    Description=IndicAgent Self-Healing Engine (HTTP webhook port 9002, OTel metrics port 9007)
    After=network-online.target indicagent-infrastructure.target
    Requires=indicagent-infrastructure.target

    [Service]
    Type=simple
    User=bg
    WorkingDirectory=/home/bg/dev/indicagent
    Environment=PYTHONPATH=/home/bg/dev/indicagent
    Environment=PYTHONUNBUFFERED=1
    Environment=METRICS_PORT=9007
    # Environment=CONFIG_WEBHOOK_SHARED_SECRET=... (set via override or EnvironmentFile)
    # Environment=PROMETHEUS_URL=http://localhost:9090
    # EnvironmentFile=-/etc/indicagent/self-healing.env
    ExecStart=/home/bg/dev/indicagent/.venv/bin/python services/self_healing_agent.py
    Restart=always
    WatchdogSec=60
    NotifyAccess=main
    RestartSec=10

    [Install]
    WantedBy=multi-user.target
  </action>
  <verify>
    Run: .venv/bin/python services/self_healing_agent.py (verify starts on 9002)
    With CONFIG_WEBHOOK_SHARED_SECRET set:
    1. curl POST without secret -> 401
    2. curl POST with wrong secret -> 401
    3. curl POST with correct secret + disk_usage alert (strategy disabled) -> 200 status with "no_strategy"
    Run: systemd-analyze verify production/systemd/indicagent-self-healing-agent.service
  </verify>
  <acceptance_criteria>
    - CLI: server listens on TCP/9002 after startup
    - CLI: POST /webhook/alertmanager without secret returns 401 when CONFIG_WEBHOOK_SHARED_SECRET set
    - CLI: POST with correct secret + minimal valid payload returns 200 with JSON body containing keys: remediation_id, status, error
    - Source assertion: `grep -q "port=9002" services/self_healing_agent.py` returns 0
    - Source assertion: file contains comment explaining 9002=HTTP, 9007=OTel
    - CLI: `systemd-analyze verify production/systemd/indicagent-self-healing-agent.service` exits 0
    - Source assertion: systemd Description mentions both 9002 and 9007
    - Source assertion: `grep -qE "ManagedPool\(settings\.database_url" services/self_healing_agent.py` returns 0 (ManagedPool constructed with database_url string, NOT a pre-created pool, per Plan 04 contract)
    - Source assertion: `grep -q "create_pool" services/self_healing_agent.py` returns NON-ZERO (the agent must NOT call create_pool directly — ManagedPool handles pool creation)
    - Source assertion: `grep -q "from src.self_healing.pool_manager import ManagedPool" services/self_healing_agent.py` returns 0
  </acceptance_criteria>
  <done>
    Self-healing HTTP service on 9002 with HTTP-layer auth (defense-in-depth). Systemd unit with METRICS_PORT=9007 verifies. ManagedPool constructed with database_url string per Plan 04 contract — no raw pool pre-creation. Documentation explicit about port roles and ManagedPool ownership of pool creation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add runtime_defaults.py + Settings.get_config_value() shim (additive; field removal DEFERRED to Phase 110)</name>
  <files>src/config/runtime_defaults.py, src/config/settings.py, production/migrations/109_config_foundation.sql</files>
  <read_first>
    - src/config/settings.py (current runtime params to identify exact current values; DO NOT remove any fields in this phase)
    - services/alpha_swarm_agent.py (confirm 5 self.settings.SWARM_* call sites still resolve - they MUST continue to work after this task)
    - production/migrations/109_config_foundation.sql (existing seed structure)
  </read_first>
  <action>
    SCOPE NOTE: This task is intentionally NARROWER than the original plan. The checker flagged a BLOCKER: services/alpha_swarm_agent.py (lines 173, 281, 282, 446, 453) still calls self.settings.SWARM_*. To keep this phase shippable and avoid migrating SWARM_* call sites out-of-band, we DEFER the Settings field removal AND the alpha_swarm_agent SWARM_* call-site migration to Phase 110. This task is ADDITIVE only:
      - ADD src/config/runtime_defaults.py with _DEFAULT_* constants and RUNTIME_DEFAULTS dict.
      - ADD Settings.get_config_value() classmethod (a shim that returns typed RUNTIME_DEFAULTS fallback).
      - DO NOT remove SWARM_*/REGIME_*/ROLL_*/CROSS_ASSET_*/MACRO_* fields from Settings.
      - DO NOT touch alpha_swarm_agent.py SWARM_* references (Task 4 DOES modify alpha_swarm_agent.py but only to add _on_config_message_received and update _refresh_shadow_state_from_registry — it must NOT touch SWARM_* references).
      - Seed config_schema + config_state rows (the DB seeds remain valid; they prepare for Phase 110 call-site migration).

    Step PRE-A (MANDATORY before writing any constant): For EACH of the 15 dotted keys
    listed in Step C, run grep against src/config/settings.py to extract the actual current
    default value of the corresponding Settings field (e.g., for `regime.prob_min` -> find
    `REGIME_PROB_MIN: float = ...` in settings.py and copy the literal). The values in
    runtime_defaults.py MUST equal the live settings.py defaults EXACTLY (type + value).
    The 6 keys flagged as MOST LIKELY to diverge from prior drafts (executor MUST double-check
    each against settings.py and adjust runtime_defaults.py if they differ):
      - swarm.min_confidence  (Settings: SWARM_MIN_CONFIDENCE)
      - swarm.weight_min_samples  (Settings: SWARM_WEIGHT_MIN_SAMPLES)
      - swarm.max_concurrent_calls  (Settings: SWARM_MAX_CONCURRENT_CALLS)
      - roll.monitor_window_size  (Settings: ROLL_MONITOR_WINDOW_SIZE)
      - cross_asset.window_bars  (Settings: CROSS_ASSET_WINDOW_BARS)
      - macro.window_bars  (Settings: MACRO_WINDOW_BARS)
    If any of these (or any of the other 9 keys) differs in settings.py from the value shown
    in Step A's constant skeleton below, the executor MUST update both Step A (the constant)
    AND Step C (the migration seed) so all three sources of truth (settings.py / runtime_defaults.py
    / migration) agree.

    Step A: Create NEW file src/config/runtime_defaults.py:

    """
    Runtime defaults for OPS config keys migrated to the config DB in Phase 109.

    These constants are the CODE-LEVEL FALLBACK used by Settings.get_config_value()
    when the config DB / Kafka are unavailable, addressing the consensus review
    finding that returning None for numeric thresholds is unsafe.

    Phase 110 will:
      (a) migrate every call site (e.g., services/alpha_swarm_agent.py's
          self.settings.SWARM_* references) to BaseAgent.get_config() with explicit
          defaults at each call site, then
      (b) remove the corresponding fields from Settings, then
      (c) remove this module.

    Keys mirror the dotted notation used in config_schema; values mirror the
    pre-migration values in settings.py exactly.
    """

    # regime.*
    _DEFAULT_REGIME_PROB_MIN: float = 0.30
    _DEFAULT_REGIME_DUR_MIN: int = 1

    # swarm.*
    _DEFAULT_SWARM_MIN_CONFIDENCE: float = 0.60
    _DEFAULT_SWARM_MIN_TF_MINUTES: int = 5
    _DEFAULT_SWARM_WEIGHT_MIN_SAMPLES: int = 30
    _DEFAULT_SWARM_WEIGHT_FLOOR: float = 0.05
    _DEFAULT_SWARM_MAX_CONCURRENT_CALLS: int = 8

    # roll.*
    _DEFAULT_ROLL_MONITOR_WINDOW_SIZE: int = 100
    _DEFAULT_ROLL_MONITOR_THRESHOLD_DEFAULT: float = 1.2
    _DEFAULT_ROLL_MONITOR_POSTROLL_BARS: int = 10
    _DEFAULT_ROLL_MONITOR_COOLDOWN_MIN: int = 30
    _DEFAULT_ROLL_CONFIRMATION_BARS: int = 3
    _DEFAULT_ROLL_TIME_OF_DAY_GATED: bool = True

    # cross_asset.*
    _DEFAULT_CROSS_ASSET_WINDOW_BARS: int = 20

    # macro.*
    _DEFAULT_MACRO_WINDOW_BARS: int = 10

    # Lookup table used by Settings.get_config_value as the typed fallback.
    # Keys are the dotted notation OPS keys; values are the typed Python defaults.
    RUNTIME_DEFAULTS: dict[str, object] = {
        "regime.prob_min": _DEFAULT_REGIME_PROB_MIN,
        "regime.dur_min": _DEFAULT_REGIME_DUR_MIN,
        "swarm.min_confidence": _DEFAULT_SWARM_MIN_CONFIDENCE,
        "swarm.min_tf_minutes": _DEFAULT_SWARM_MIN_TF_MINUTES,
        "swarm.weight_min_samples": _DEFAULT_SWARM_WEIGHT_MIN_SAMPLES,
        "swarm.weight_floor": _DEFAULT_SWARM_WEIGHT_FLOOR,
        "swarm.max_concurrent_calls": _DEFAULT_SWARM_MAX_CONCURRENT_CALLS,
        "roll.monitor_window_size": _DEFAULT_ROLL_MONITOR_WINDOW_SIZE,
        "roll.threshold_default": _DEFAULT_ROLL_MONITOR_THRESHOLD_DEFAULT,
        "roll.postroll_bars": _DEFAULT_ROLL_MONITOR_POSTROLL_BARS,
        "roll.cooldown_min": _DEFAULT_ROLL_MONITOR_COOLDOWN_MIN,
        "roll.confirmation_bars": _DEFAULT_ROLL_CONFIRMATION_BARS,
        "roll.time_of_day_gated": _DEFAULT_ROLL_TIME_OF_DAY_GATED,
        "cross_asset.window_bars": _DEFAULT_CROSS_ASSET_WINDOW_BARS,
        "macro.window_bars": _DEFAULT_MACRO_WINDOW_BARS,
    }

    Step B: Modify src/config/settings.py (ADDITIVE - do NOT remove any existing fields):
    1. ADD a module-level sentinel near the top: _UNSET = object()

    2. ADD classmethod (DO NOT remove SWARM_*/REGIME_*/ROLL_*/CROSS_ASSET_*/MACRO_* fields):

       @classmethod
       def get_config_value(cls, key: str, default: Any = _UNSET) -> Any:
           """
           Backward-compatible accessor for OPS config values migrated to the config DB.

           Resolution order:
             1. Typed default from src.config.runtime_defaults.RUNTIME_DEFAULTS (Phase 109 fallback).
             2. Caller-provided default if explicitly passed.
             3. None (only if caller explicitly passed default=None or omitted with no RUNTIME_DEFAULTS entry).

           This addresses the cross-AI review consensus finding that returning None for
           numeric thresholds (e.g., REGIME_PROB_MIN=0.30) is unsafe. The RUNTIME_DEFAULTS
           module preserves typed pre-migration values until Phase 110 finishes call-site
           migration AND removes the corresponding Settings fields.

           NOTE: Existing self.settings.SWARM_*, self.settings.REGIME_*, etc. attributes
           continue to work as before; this shim is opt-in for callers that want the
           dotted-key API. The shim is the migration entry point.
           """
           import warnings
           warnings.warn(
               "Settings.get_config_value() is a temporary shim. Migrate to BaseAgent.get_config() at call site.",
               DeprecationWarning,
               stacklevel=2,
           )
           from src.config.runtime_defaults import RUNTIME_DEFAULTS
           if key in RUNTIME_DEFAULTS:
               return RUNTIME_DEFAULTS[key]
           if default is _UNSET:
               return None
           return default

    3. EXPLICITLY KEEP all existing INFRA fields (DATABASE_URL, KAFKA_BROKERS, etc.), all STRUCT fields (plugin tiers references, etc.), AND all SWARM_*/REGIME_*/ROLL_*/CROSS_ASSET_*/MACRO_* fields. The actual removal happens in Phase 110 after call sites migrate.

    Step C: Append to production/migrations/109_config_foundation.sql:
    -- Seed OPS config_schema for migrated settings.py params.
    -- Values mirror src/config/runtime_defaults.py exactly.
    -- NOTE (Phase 109): These rows prepare for Phase 110 call-site migration of
    -- alpha_swarm_agent.py and related modules. The corresponding Settings fields
    -- (SWARM_*, REGIME_*, etc.) remain in src/config/settings.py during Phase 109.
    INSERT INTO config_schema (config_key, value_type, default_value, description) VALUES
      ('regime.prob_min', 'float', '0.30', 'Minimum regime probability for signal eligibility'),
      ('regime.dur_min', 'int', '1', 'Minimum regime duration (bars)'),
      ('swarm.min_confidence', 'float', '0.60', 'Minimum swarm agent confidence'),
      ('swarm.min_tf_minutes', 'int', '5', 'Minimum timeframe (minutes) for swarm processing'),
      ('swarm.weight_min_samples', 'int', '30', 'Minimum samples before weight learning'),
      ('swarm.weight_floor', 'float', '0.05', 'Minimum weight value (prevents zero weights)'),
      ('swarm.max_concurrent_calls', 'int', '8', 'Max concurrent LLM calls'),
      ('roll.monitor_window_size', 'int', '100', 'Roll monitor sliding window size'),
      ('roll.threshold_default', 'float', '1.2', 'Roll detection threshold default'),
      ('roll.postroll_bars', 'int', '10', 'Bars to monitor after roll'),
      ('roll.cooldown_min', 'int', '30', 'Cooldown minutes after roll'),
      ('roll.confirmation_bars', 'int', '3', 'Confirmation bars for roll detection'),
      ('roll.time_of_day_gated', 'bool', 'true', 'Gate roll detection by time-of-day'),
      ('cross_asset.window_bars', 'int', '20', 'Cross-asset analysis window size'),
      ('macro.window_bars', 'int', '10', 'Macro context window size')
    ON CONFLICT (config_key) DO NOTHING;

    INSERT INTO config_state (config_key, config_value, version) VALUES
      ('regime.prob_min', '0.30', 1),
      ('regime.dur_min', '1', 1),
      ('swarm.min_confidence', '0.60', 1),
      ('swarm.min_tf_minutes', '5', 1),
      ('swarm.weight_min_samples', '30', 1),
      ('swarm.weight_floor', '0.05', 1),
      ('swarm.max_concurrent_calls', '8', 1),
      ('roll.monitor_window_size', '100', 1),
      ('roll.threshold_default', '1.2', 1),
      ('roll.postroll_bars', '10', 1),
      ('roll.cooldown_min', '30', 1),
      ('roll.confirmation_bars', '3', 1),
      ('roll.time_of_day_gated', 'true', 1),
      ('cross_asset.window_bars', '20', 1),
      ('macro.window_bars', '10', 1)
    ON CONFLICT (config_key) DO NOTHING;

    NOTE: 15 INSERT rows in each table. NO category column. Caller-site migration is DEFERRED to Phase 110.
  </action>
  <verify>
    Run: .venv/bin/python -c "from src.config.runtime_defaults import RUNTIME_DEFAULTS; assert len(RUNTIME_DEFAULTS) == 15; assert RUNTIME_DEFAULTS['regime.prob_min'] == 0.30; assert isinstance(RUNTIME_DEFAULTS['swarm.max_concurrent_calls'], int); print('OK')"
    Run: .venv/bin/python -c "from src.config.settings import Settings; v = Settings.get_config_value('regime.prob_min'); assert v == 0.30 and isinstance(v, float); v2 = Settings.get_config_value('unknown.key', default='fallback'); assert v2 == 'fallback'; v3 = Settings.get_config_value('unknown.key'); assert v3 is None; print('OK')"
    Run: .venv/bin/python -c "from src.config.settings import get_settings; s = get_settings(); assert s.SWARM_MIN_CONFIDENCE is not None; print('back-compat OK')"
    Run: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM config_schema WHERE config_key IN ('regime.prob_min','regime.dur_min','swarm.min_confidence','swarm.min_tf_minutes','swarm.weight_min_samples','swarm.weight_floor','swarm.max_concurrent_calls','roll.monitor_window_size','roll.threshold_default','roll.postroll_bars','roll.cooldown_min','roll.confirmation_bars','roll.time_of_day_gated','cross_asset.window_bars','macro.window_bars')" returns 15
    Run: .venv/bin/mypy src/config/settings.py src/config/runtime_defaults.py
    Run: grep -nE "self\.settings\.SWARM_" services/alpha_swarm_agent.py (must still return ~5 matches; we did not migrate SWARM_* references)
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/config/runtime_defaults.py` exists and exports RUNTIME_DEFAULTS dict with exactly 15 keys
    - Cross-file equality (the 6 historically divergent keys MUST match settings.py exactly):
        * RUNTIME_DEFAULTS["swarm.min_confidence"]      == Settings field SWARM_MIN_CONFIDENCE default
        * RUNTIME_DEFAULTS["swarm.weight_min_samples"]  == Settings field SWARM_WEIGHT_MIN_SAMPLES default
        * RUNTIME_DEFAULTS["swarm.max_concurrent_calls"]== Settings field SWARM_MAX_CONCURRENT_CALLS default
        * RUNTIME_DEFAULTS["roll.monitor_window_size"]  == Settings field ROLL_MONITOR_WINDOW_SIZE default
        * RUNTIME_DEFAULTS["cross_asset.window_bars"]   == Settings field CROSS_ASSET_WINDOW_BARS default
        * RUNTIME_DEFAULTS["macro.window_bars"]         == Settings field MACRO_WINDOW_BARS default
      Verification command (must print ALL OK):
        .venv/bin/python -c "from src.config.settings import get_settings; from src.config.runtime_defaults import RUNTIME_DEFAULTS; s=get_settings(); pairs=[('swarm.min_confidence','SWARM_MIN_CONFIDENCE'),('swarm.weight_min_samples','SWARM_WEIGHT_MIN_SAMPLES'),('swarm.max_concurrent_calls','SWARM_MAX_CONCURRENT_CALLS'),('roll.monitor_window_size','ROLL_MONITOR_WINDOW_SIZE'),('cross_asset.window_bars','CROSS_ASSET_WINDOW_BARS'),('macro.window_bars','MACRO_WINDOW_BARS')]; [print(k,'OK' if RUNTIME_DEFAULTS[k]==getattr(s,a) else f'MISMATCH {RUNTIME_DEFAULTS[k]} vs {getattr(s,a)}') for k,a in pairs]" 
    - Source assertion: every value in RUNTIME_DEFAULTS has the type matching the migration table (float/int/bool)
    - Source assertion: `grep -E "^\\s*(REGIME_PROB_MIN|SWARM_MIN_CONFIDENCE|ROLL_MONITOR_WINDOW_SIZE|CROSS_ASSET_WINDOW_BARS|MACRO_WINDOW_BARS)" src/config/settings.py` returns AT LEAST ONE line per field (fields RETAINED - DEFERRED removal)
    - Source assertion: `grep -q "def get_config_value" src/config/settings.py` returns 0
    - Source assertion: `grep -c "self.settings.SWARM_" services/alpha_swarm_agent.py` returns the SAME count as before this task (Task 2 does NOT modify SWARM_* references; Task 4 modifies alpha_swarm_agent but only for shadow_mode plumbing)
    - Behavior: Settings.get_config_value('regime.prob_min') returns 0.30 (NOT None) - this is the consensus fix
    - Behavior: Settings.get_config_value('unknown.key', default='x') returns 'x'
    - Behavior: Settings.get_config_value('unknown.key') returns None (only when no default + not in RUNTIME_DEFAULTS)
    - Behavior: get_settings().SWARM_MIN_CONFIDENCE still resolves to its pre-existing value (back-compat preserved)
    - DB assertion: 15 new rows in config_schema and 15 in config_state with dotted keys
    - CLI: rerunning the migration produces no duplicate-key errors (ON CONFLICT DO NOTHING)
    - mypy clean on both files
  </acceptance_criteria>
  <done>
    runtime_defaults.py provides 15 typed _DEFAULT_* constants. Settings.get_config_value() returns typed defaults from RUNTIME_DEFAULTS (NOT None) - resolves consensus MEDIUM-HIGH finding. Existing SWARM_*/REGIME_*/ROLL_*/CROSS_ASSET_*/MACRO_* fields RETAINED (removal deferred to Phase 110). alpha_swarm_agent.py SWARM_* references unchanged. Migration seeds 15 config_schema + 15 config_state rows idempotently, preparing for Phase 110 call-site migration.
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace _LAG_THRESHOLDS in service_auditor_agent with config-backed loader + _on_config_message_received hook</name>
  <files>services/service_auditor_agent.py, production/migrations/109_config_foundation.sql</files>
  <read_first>
    - services/service_auditor_agent.py (current _LAG_THRESHOLDS dict; identify exact keys/values)
    - production/migrations/109_config_foundation.sql (config_schema seeds)
    - src/config/config_consumer.py (_on_config_message_received hook added in Plan 03)
  </read_first>
  <action>
    Modify services/service_auditor_agent.py:

    1. Capture the current _LAG_THRESHOLDS dict contents BEFORE removing (read_first). Each entry maps `indicagent-<kebab-name>` -> int threshold.

    2. Remove the _LAG_THRESHOLDS module/class dict.

    3. In ServiceAuditorAgent.__init__ (or class body), set:
       self._config_prefixes = ("alert.lag.",)  # restricts mixin reload to relevant keys (avoids storm)
       self._lag_thresholds: dict[str, int] = {}

    4. Add async def _load_lag_thresholds(self) -> None:
       """Load lag thresholds from config_state (alert.lag.* keys). NON-FATAL."""
       try:
           # Use BaseAgent's config cache populated by _pre_setup_config_load (Plan 03)
           # Key format: "alert.lag.<unit-name-kebab>" (e.g., "alert.lag.feature-writer")
           result: dict[str, int] = {}
           for key, value in self._config_cache.items():
               if not key.startswith("alert.lag."):
                   continue
               unit_suffix = key[len("alert.lag."):]
               full_unit = f"indicagent-{unit_suffix}"
               try:
                   result[full_unit] = int(value)
               except (TypeError, ValueError):
                   continue
           self._lag_thresholds = result
       except Exception as exc:
           structlog.get_logger().warning("lag_thresholds.load_failed", error=str(exc))

    5. Call await self._load_lag_thresholds() at end of _setup() (after super()._setup() and after Plan 03's _pre_setup_config_load has populated _config_cache).

    6. Replace all lookups `_LAG_THRESHOLDS.get(unit, 0)` with `self._lag_thresholds.get(unit, 0)`.

    7. Override the per-message hook _on_config_message_received added by Plan 03's ConfigConsumerMixin. This is the SINGLE chosen pattern (no multi-path ambiguity):

       async def _on_config_message_received(self, key: str, value: object) -> None:
           # Plan 03 mixin already updated self._config_cache before invoking this hook.
           # ServiceAuditorAgent only needs to refresh its derived view of alert.lag.* keys.
           if key.startswith("alert.lag."):
               await self._load_lag_thresholds()

    Update production/migrations/109_config_foundation.sql - append:
    -- alert.lag.* keys seeded from the original _LAG_THRESHOLDS dict.
    -- Executor MUST read services/service_auditor_agent.py to identify the exact unit names and values
    -- BEFORE removing the dict, and emit one INSERT row per dict entry below.
    -- Example shape (replace with full dict contents):
    INSERT INTO config_schema (config_key, value_type, default_value, description) VALUES
      ('alert.lag.feature-writer', 'int', '<value-from-dict>', 'Consumer lag threshold for indicagent-feature-writer'),
      -- ... one row per current _LAG_THRESHOLDS entry ...
    ON CONFLICT (config_key) DO NOTHING;

    INSERT INTO config_state (config_key, config_value, version) VALUES
      ('alert.lag.feature-writer', '<value-from-dict>', 1),
      -- ... one row per entry ...
    ON CONFLICT (config_key) DO NOTHING;

    Executor MUST count the entries in the ORIGINAL _LAG_THRESHOLDS dict and emit exactly that many rows in each INSERT.
  </action>
  <verify>
    Run: grep -c "_LAG_THRESHOLDS" services/service_auditor_agent.py (should return 0)
    Run: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM config_schema WHERE config_key LIKE 'alert.lag.%'" - must match the count of entries removed from the original dict
    Run: .venv/bin/python -c "from services.service_auditor_agent import ServiceAuditorAgent; print('OK')"
    Integration test: start service_auditor with config DB populated, verify _lag_thresholds dict matches DB content. Update one row via Config API, observe Kafka delivery invokes _on_config_message_received, which calls _load_lag_thresholds, and self._lag_thresholds reflects new value.
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "_LAG_THRESHOLDS" services/service_auditor_agent.py` returns 0
    - Source assertion: `grep -q "_config_prefixes" services/service_auditor_agent.py` returns 0 (uses Plan 03 prefix-filter mechanism to limit reload work)
    - Source assertion: `_load_lag_thresholds` defined and called from _setup()
    - Source assertion: `grep -q "async def _on_config_message_received" services/service_auditor_agent.py` returns 0 (single chosen pattern)
    - Behavior: count of `alert.lag.*` rows in config_schema EQUALS the count of entries in the pre-migration _LAG_THRESHOLDS dict
    - Behavior: after hot-reload via API, `self._lag_thresholds[<unit>]` reflects the new value WITHOUT process restart
    - DB assertion: re-running migration is idempotent (ON CONFLICT DO NOTHING)
  </acceptance_criteria>
  <done>
    _LAG_THRESHOLDS dict removed. Thresholds loaded from config DB via Plan 03 mixin (snapshot at startup, hot-reload on alert.lag.* Kafka updates routed through _on_config_message_received override). _config_prefixes=("alert.lag.",) restricts reload work per consensus suggestion. Number of seeded rows matches pre-migration count.
  </done>
</task>

<task type="auto">
  <name>Task 4: Remove hardcoded shadow_only flags from AI agents + wire AlphaSwarmComputeAgent Kafka delivery path</name>
  <files>src/intelligence/ai/alpha/correlation_agent.py, src/intelligence/ai/alpha/counterfactual_agent.py, src/intelligence/ai/alpha/regime_coherence_agent.py, src/intelligence/ai/alpha/ml_scorer_agent.py, services/alpha_swarm_agent.py, production/migrations/109_config_foundation.sql</files>
  <read_first>
    - src/intelligence/ai/alpha/correlation_agent.py (current shadow_only=True pattern)
    - src/intelligence/ai/alpha/counterfactual_agent.py (current shadow_only=True pattern)
    - src/intelligence/ai/alpha/regime_coherence_agent.py (current shadow_only=True pattern)
    - src/intelligence/ai/alpha/ml_scorer_agent.py (current shadow_only=True pattern)
    - services/alpha_swarm_agent.py (lines around _setup, self._agents, _refresh_shadow_state_from_registry — this is the GroupService that hosts the individual AI agents)
    - src/intelligence/ai/AUTHORING.md (BaseAIAgent contract)
    - src/core/agent/base.py (BaseAgent.get_config from Plan 03)
    - src/config/config_consumer.py (Plan 03 ConfigConsumerMixin._on_config_message_received hook)
    - production/migrations/109_config_foundation.sql (seed pattern)
  </read_first>
  <action>
    SCOPE NOTE: Individual AI agents (CorrelationComputeAgent, CounterfactualComputeAgent, RegimeCoherenceComputeAgent, MLScorerMultiplierAgent) do NOT have their own `_setup()` method and do NOT consume Kafka directly. They are constructed inside `AlphaSwarmComputeAgent._setup()` and live in `self._agents: list[BaseMultiplierAgent]`. Therefore the Kafka delivery path for `ai.agent.*.shadow_mode` config updates MUST go through AlphaSwarmComputeAgent. This task has THREE parts:
      (1) Add `_apply_shadow_mode_config()` to each of the four agent files (no Kafka consumption — pure local method).
      (2) Override `_on_config_message_received` in AlphaSwarmComputeAgent to iterate `self._agents` and invoke `_apply_shadow_mode_config()` when the key starts with `ai.agent.`.
      (3) Update `_refresh_shadow_state_from_registry` in AlphaSwarmComputeAgent to honor D-07 precedence (config DB wins over shadow_registry when a config entry exists).

    The previous version of this task listed `volume_agent.py` and `skeptic_agent.py` in files_modified. The checker verified that:
      - `src/intelligence/ai/alpha/volume_agent.py` does NOT exist
      - `src/intelligence/ai/alpha/skeptic_agent.py` has `shadow_only = False` (live agent — must NOT be flipped to shadow)
    The accurate list of BaseAIAgent subclasses with `shadow_only = True` is:
      - correlation_agent.py
      - counterfactual_agent.py
      - regime_coherence_agent.py
      - ml_scorer_agent.py
    Before editing, executor MUST run `grep -ln "^\s*shadow_only\s*=\s*True" src/intelligence/ai/alpha/*.py` to confirm the list is current.

    === PART A: Per-agent _apply_shadow_mode_config method ===

    For EACH of the four agents (correlation, counterfactual, regime_coherence, ml_scorer):

    1. Replace literal `shadow_only = True` (class attribute) with an annotated class attribute defaulting to True PLUS a config-override method.

       Pattern (e.g., correlation_agent.py):
           class CorrelationComputeAgent(BaseAIAgent):  # use existing real class name
               # Phase 109: shadow_only defaults to True (FAIL-CLOSED).
               # Promotion to live requires an explicit ai.agent.<agent_id>.shadow_mode=false
               # config entry. If config load fails or the key is missing, agent STAYS shadow.
               shadow_only: bool = True

               def _apply_shadow_mode_config(self) -> None:
                   """Read ai.agent.<self.agent_id>.shadow_mode from config; fail-closed on miss.

                   Called by:
                     - AlphaSwarmComputeAgent._setup() after agents are constructed (initial load)
                     - AlphaSwarmComputeAgent._on_config_message_received() on Kafka update
                       (hot-reload — see Part B)
                   """
                   override = self.get_config(f"ai.agent.{self.agent_id}.shadow_mode", None)
                   if override is None:
                       return  # keep class default True (fail-closed)
                   # override may arrive as bool (from RUNTIME_DEFAULTS) or str (from Kafka); normalize:
                   if isinstance(override, bool):
                       self.shadow_only = override
                   elif isinstance(override, str):
                       self.shadow_only = override.strip().lower() in ("true", "1", "yes")
                   else:
                       # Unknown type - keep fail-closed
                       return

    2. Do NOT add a Kafka consumer to these agents — they are not standalone services. Kafka delivery is wired in Part B via AlphaSwarmComputeAgent.

    3. Explicitly DO NOT modify src/intelligence/ai/alpha/skeptic_agent.py (shadow_only=False — already live; flipping it to shadow_only=True would regress production behavior).

    === PART B: AlphaSwarmComputeAgent Kafka delivery + D-07 precedence ===

    Modify services/alpha_swarm_agent.py:

    1. Override `_on_config_message_received` (the per-message hook added by Plan 03's ConfigConsumerMixin). This is the Kafka delivery path from a config update to agent.shadow_only flipping:

       async def _on_config_message_received(self, key: str, value: object) -> None:
           """Route ai.agent.*.shadow_mode config updates to the individual AI agents.

           Plan 03 mixin already updated self._config_cache before invoking this hook,
           so agent.get_config('ai.agent.<id>.shadow_mode') sees the new value.
           """
           if key.startswith("ai.agent."):
               for agent in self._agents:
                   # Each AI agent has its own _apply_shadow_mode_config that reads its
                   # own dotted key. Iterating all agents is cheap and avoids parsing
                   # the agent_id out of the dotted key.
                   if hasattr(agent, "_apply_shadow_mode_config"):
                       agent._apply_shadow_mode_config()

    2. After agents are constructed in `_setup()` (existing code around line 162-170), invoke `_apply_shadow_mode_config()` on each agent so the initial state reflects the config DB BEFORE any Kafka traffic arrives. Add immediately after the agent list is fully populated (after the ml_scorer append at ~line 170, before the shadow_registry_ensure call at ~line 177):

       # Phase 109: apply config-DB shadow_mode overrides BEFORE shadow_registry sync,
       # because D-07 precedence makes config DB the winner when both sources exist.
       for agent in self._agents:
           if hasattr(agent, "_apply_shadow_mode_config"):
               agent._apply_shadow_mode_config()

    3. Update `_refresh_shadow_state_from_registry` to honor D-07 precedence (config DB wins over shadow_registry when a config entry exists for the agent's shadow_mode). The current method (around line 190-205) sets `agent.shadow_only = bool(registry_state[agent.agent_id])`. Change it to:

       async def _refresh_shadow_state_from_registry(self) -> None:
           """Refresh agent.shadow_only.

           D-07 precedence (Phase 109): config DB takes precedence over shadow_registry
           when a config entry exists for ai.agent.<agent_id>.shadow_mode. Only fall
           back to shadow_registry if no config entry exists for this agent.
           """
           # ... existing registry fetch logic ...
           for agent in self._agents:
               # D-07: check config DB FIRST
               config_override = self.get_config(f"ai.agent.{agent.agent_id}.shadow_mode", None)
               if config_override is not None:
                   # Config DB wins — delegate to the agent's normalizer.
                   if hasattr(agent, "_apply_shadow_mode_config"):
                       agent._apply_shadow_mode_config()
                   continue
               # No config entry — fall back to shadow_registry
               if agent.agent_id in registry_state:
                   agent.shadow_only = bool(registry_state[agent.agent_id])

    4. Set `_config_prefixes` on AlphaSwarmComputeAgent (or extend an existing tuple) to include `("ai.agent.",)` so Plan 03's prefix filter does not drop these messages and does not flood reload on unrelated keys. Add other prefixes the service already needs if any.

    === PART C: Migration seeds ===

    Update production/migrations/109_config_foundation.sql — append one config_schema + config_state row PER AI agent:

       INSERT INTO config_schema (config_key, value_type, default_value, description) VALUES
         ('ai.agent.correlation.shadow_mode', 'bool', 'true', 'Shadow mode for correlation agent (true=shadow, false=live)'),
         ('ai.agent.counterfactual.shadow_mode', 'bool', 'true', 'Shadow mode for counterfactual agent'),
         ('ai.agent.regime_coherence.shadow_mode', 'bool', 'true', 'Shadow mode for regime_coherence agent'),
         ('ai.agent.ml_scorer.shadow_mode', 'bool', 'true', 'Shadow mode for ml_scorer agent')
       ON CONFLICT (config_key) DO NOTHING;

       INSERT INTO config_state (config_key, config_value, version) VALUES
         ('ai.agent.correlation.shadow_mode', 'true', 1),
         ('ai.agent.counterfactual.shadow_mode', 'true', 1),
         ('ai.agent.regime_coherence.shadow_mode', 'true', 1),
         ('ai.agent.ml_scorer.shadow_mode', 'true', 1)
       ON CONFLICT (config_key) DO NOTHING;

    NOTE: default 'true' preserves current fail-closed state. Use each agent's actual agent_id string (verify via the class attribute) when generating dotted keys.
  </action>
  <verify>
    Run: grep -lEn "^\s*shadow_only\s*=\s*True" src/intelligence/ai/ (should match exactly the four files listed; if any extra file appears, add it to the migration; if skeptic_agent.py appears, abort - that file must stay shadow_only=False)
    Run: grep -n "shadow_only" src/intelligence/ai/alpha/skeptic_agent.py (should still show shadow_only = False - we did NOT touch this file)
    Run: grep -rn "shadow_only: bool = True" src/intelligence/ai/alpha/correlation_agent.py src/intelligence/ai/alpha/counterfactual_agent.py src/intelligence/ai/alpha/regime_coherence_agent.py src/intelligence/ai/alpha/ml_scorer_agent.py (should return exactly four matches - one per file)
    Run: grep -n "_on_config_message_received\|_apply_shadow_mode_config" services/alpha_swarm_agent.py (should show the override and the initial-load invocation)
    Run: grep -n "get_config(f\"ai.agent" services/alpha_swarm_agent.py (should appear in _refresh_shadow_state_from_registry for D-07 precedence)
    Run: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM config_schema WHERE config_key LIKE 'ai.agent.%.shadow_mode'" - count must equal 4
    Run: .venv/bin/mypy src/intelligence/ai/alpha/correlation_agent.py src/intelligence/ai/alpha/counterfactual_agent.py src/intelligence/ai/alpha/regime_coherence_agent.py src/intelligence/ai/alpha/ml_scorer_agent.py services/alpha_swarm_agent.py
    Behavior test 1 (fail-closed): Start AlphaSwarmComputeAgent with empty config DB. After _setup(), each of the four agents.shadow_only is True.
    Behavior test 2 (initial override): Seed config_state with ai.agent.correlation.shadow_mode='false'. Start AlphaSwarmComputeAgent. After _setup(), CorrelationComputeAgent.shadow_only is False; others remain True.
    Behavior test 3 (hot-reload): With AlphaSwarmComputeAgent running, publish a Kafka config update setting ai.agent.correlation.shadow_mode='false'. Within one poll cycle, CorrelationComputeAgent.shadow_only flips to False without restart.
    Behavior test 4 (D-07 precedence): Seed shadow_registry with correlation.shadow_only=False AND config DB with ai.agent.correlation.shadow_mode='true'. After _refresh_shadow_state_from_registry, CorrelationComputeAgent.shadow_only is True (config wins).
  </verify>
  <acceptance_criteria>
    - Source assertion: each of the four migrated agent files contains `shadow_only: bool = True` (annotated class attribute)
    - Source assertion: each migrated agent file contains `def _apply_shadow_mode_config`
    - Source assertion: src/intelligence/ai/alpha/skeptic_agent.py is UNCHANGED (still `shadow_only = False`)
    - Source assertion: src/intelligence/ai/alpha/volume_agent.py is NOT created (file does not exist; no new file written)
    - Source assertion: services/alpha_swarm_agent.py contains `async def _on_config_message_received` that iterates `self._agents` and calls `_apply_shadow_mode_config()` when `key.startswith("ai.agent.")`
    - Source assertion: services/alpha_swarm_agent.py `_setup()` calls `_apply_shadow_mode_config()` on each agent AFTER agents are constructed and BEFORE `_shadow_registry_ensure_agents` is invoked (verify via line-number ordering with grep -n)
    - Source assertion: services/alpha_swarm_agent.py `_refresh_shadow_state_from_registry` checks `get_config(f"ai.agent.{agent.agent_id}.shadow_mode", None)` FIRST and only falls back to shadow_registry when None (D-07 precedence)
    - Source assertion: services/alpha_swarm_agent.py declares `_config_prefixes` containing `"ai.agent."`
    - Source assertion: `grep -c "self.settings.SWARM_" services/alpha_swarm_agent.py` returns the SAME count as before this task (Task 4 does NOT touch SWARM_* references — those are Phase 110)
    - DB assertion: count of ai.agent.*.shadow_mode rows in config_schema equals 4
    - Behavior: with empty config cache, shadow_only stays True after setup (FAIL-CLOSED)
    - Behavior: with config_cache override to False, shadow_only becomes False after setup
    - Behavior: after publishing `ai.agent.correlation.shadow_mode=false` config update, `CorrelationComputeAgent.shadow_only` flips to False within one poll cycle WITHOUT restart
    - Behavior (D-07): config DB entry takes precedence over shadow_registry entry in _refresh_shadow_state_from_registry
    - mypy clean on all five modified files
  </acceptance_criteria>
  <done>
    Four BaseAIAgent subclasses (correlation, counterfactual, regime_coherence, ml_scorer) use fail-closed class-level shadow_only=True + config-override hook `_apply_shadow_mode_config`. skeptic_agent.py is left untouched. AlphaSwarmComputeAgent owns the Kafka delivery path: `_on_config_message_received` override iterates `self._agents` and applies the config; `_setup` invokes `_apply_shadow_mode_config` after agent construction so the initial state honors the config DB; `_refresh_shadow_state_from_registry` checks config DB first (D-07 precedence) and only falls back to shadow_registry when no config entry exists. `_config_prefixes` includes `"ai.agent."`. Config schema seeded with default 'true' for every migrated agent. SWARM_* references in alpha_swarm_agent.py are untouched (Phase 110).
  </done>
</task>

</tasks>

<verification>
End-to-end: bring up config-service (Plan 02) and self-healing-agent (Plan 05 Task 1). Verify port mapping: 9001 config HTTP, 9005 config metrics, 9002 self-healing HTTP, 9007 self-healing metrics. Confirm self-healing-agent starts (no TypeError on ManagedPool construction) — the agent passes settings.database_url to ManagedPool, which owns pool creation. Run integration: set ai.agent.correlation.shadow_mode=false via Config API; observe AlphaSwarmComputeAgent._on_config_message_received fires, invokes CorrelationComputeAgent._apply_shadow_mode_config, and CorrelationComputeAgent.shadow_only flips to False within Kafka latency window WITHOUT restart. Verify D-07 precedence by seeding both shadow_registry (False) and config DB (True) for the same agent — _refresh_shadow_state_from_registry must honor the config DB. Stop config DB; restart service_auditor; verify it starts using runtime_defaults fallbacks AND prior alert.lag.* values are unavailable (so lag thresholds are empty, no false alarms — that is the expected NON-FATAL behavior). Bring DB back up; observe lag thresholds reload on next Kafka message via service_auditor._on_config_message_received. Verify alpha_swarm_agent.py STILL works because Settings.SWARM_* fields were retained AND SWARM_* references were not touched.
</verification>

<success_criteria>
1. SelfHealingAgent HTTP service on port 9002 (OTel metrics on 9007)
2. HTTP-layer auth (defense-in-depth alongside engine-layer auth in Plan 04)
3. SelfHealingAgent constructs ManagedPool(settings.database_url, pool_name="self_healing") — NO pre-created raw pool (Plan 04 contract honored)
4. runtime_defaults.py provides 15 typed _DEFAULT_* constants
5. Settings.get_config_value returns RUNTIME_DEFAULTS value (NOT None) for migrated keys
6. Existing SWARM_*/REGIME_*/ROLL_*/CROSS_ASSET_*/MACRO_* Settings fields RETAINED (removal deferred to Phase 110)
7. services/alpha_swarm_agent.py SWARM_* references UNCHANGED — still work via existing Settings attributes
8. Migration seeds 15 new config_schema + 15 config_state rows idempotently
9. _LAG_THRESHOLDS removed from service_auditor_agent.py; loaded via Plan 03 mixin
10. service_auditor uses _config_prefixes=("alert.lag.",) and overrides _on_config_message_received (single chosen pattern)
11. Number of alert.lag.* seeded rows EQUALS the pre-migration dict size
12. Four AI agents (correlation, counterfactual, regime_coherence, ml_scorer) use shadow_only: bool = True class attribute (FAIL-CLOSED default)
13. skeptic_agent.py UNCHANGED (still live - shadow_only=False)
14. No new volume_agent.py created
15. AI agents have `_apply_shadow_mode_config` to read ai.agent.<id>.shadow_mode override
16. AlphaSwarmComputeAgent overrides `_on_config_message_received` to route ai.agent.* updates to self._agents (Kafka delivery path)
17. AlphaSwarmComputeAgent._setup invokes _apply_shadow_mode_config on each agent after construction (initial state)
18. AlphaSwarmComputeAgent._refresh_shadow_state_from_registry honors D-07 (config DB wins over shadow_registry)
19. AlphaSwarmComputeAgent._config_prefixes includes "ai.agent."
20. After publishing ai.agent.correlation.shadow_mode=false, CorrelationComputeAgent.shadow_only flips within one poll cycle without restart
21. Number of ai.agent.*.shadow_mode seeded rows EQUALS 4
22. All migrations idempotent (ON CONFLICT DO NOTHING)
23. mypy clean on settings.py, runtime_defaults.py, four migrated AI agent files, services/alpha_swarm_agent.py
</success_criteria>

<output>
.planning/phases/109-config-foundation-self-healing-engine/109-05-SUMMARY.md with:
- Port mapping confirmation (9001/9005, 9002/9007)
- Auth test results (HTTP layer 401/200)
- ManagedPool construction proof (settings.database_url passed in; no raw pool pre-created; Plan 04 contract honored)
- runtime_defaults.py contents + Settings.get_config_value behavior proof (typed fallback, NOT None)
- Confirmation that SWARM_*/REGIME_*/ROLL_*/CROSS_ASSET_*/MACRO_* fields remain in Settings (deferred to Phase 110)
- Confirmation that alpha_swarm_agent.py SWARM_* references were not modified
- _LAG_THRESHOLDS migration summary (count equality check)
- service_auditor _on_config_message_received hook verification (single pattern)
- AI agent shadow_only migration summary (four files: correlation, counterfactual, regime_coherence, ml_scorer; skeptic untouched; no volume_agent created)
- AlphaSwarmComputeAgent Kafka delivery proof: publishing ai.agent.correlation.shadow_mode=false flips CorrelationComputeAgent.shadow_only without restart
- D-07 precedence proof: config DB entry overrides shadow_registry in _refresh_shadow_state_from_registry
- Fail-closed verification + hot-reload test for each migrated agent
- Rollout order documentation (migration -> services restart) per Codex MEDIUM
- Phase 110 prerequisites list (SWARM_* call-site migration of alpha_swarm_agent.py + Settings field removal)
- Next steps for user (Alertmanager webhook config, CONFIG_WEBHOOK_SHARED_SECRET, PROMETHEUS_URL)
</output>
</content>
</invoke>