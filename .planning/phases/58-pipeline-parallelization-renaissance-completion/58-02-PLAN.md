---
phase: 58-pipeline-parallelization-renaissance-completion
plan: 02
type: execute
wave: 2
depends_on: ["58-01"]
files_modified:
  - tests/unit/test_pipeline_determinism.py
  - tests/unit/test_pipeline_exception_isolation.py
autonomous: true
requirements: [PIPE-04, PIPE-05, PIPE-01, PIPE-02]

must_haves:
  truths:
    - "100 bars processed sequentially produce identical output to 100 bars processed in parallel (i1 keys/values, i7 signal list, winner fields)"
    - "A single plugin exception in I1 never crashes the pipeline — remaining plugins complete and downstream tiers receive partial output"
    - "All I1 plugins raising exceptions results in empty dict passed to I2 — pipeline continues without crash"
    - "A single plugin exception in I7 never crashes the pipeline — remaining I7 plugins complete and signal ranking proceeds"
    - "PLUGIN_ERRORS_TOTAL counter increments on each plugin exception"
    - "PLUGIN_DURATION_MS histogram records per-plugin latency for successful plugin executions"
  artifacts:
    - path: "tests/unit/test_pipeline_determinism.py"
      provides: "Determinism proof — sequential vs parallel equivalence for I1 and I7"
      contains: "test_i1_sequential_equals_parallel"
    - path: "tests/unit/test_pipeline_exception_isolation.py"
      provides: "Exception isolation proof — plugin failures gracefully handled"
      contains: "test_single_i1_plugin_raises_does_not_crash"
  key_links:
    - from: "tests/unit/test_pipeline_determinism.py"
      to: "services/intelligence_pipeline_agent.py"
      via: "IntelligencePipelineComputeAgent.__new__ + _run_i1 / _run_i7"
      pattern: "from services.intelligence_pipeline_agent import"
    - from: "tests/unit/test_pipeline_exception_isolation.py"
      to: "src/observability/metrics.py"
      via: "PLUGIN_ERRORS_TOTAL.labels().inc() verified in assertions"
      pattern: "PLUGIN_ERRORS_TOTAL"
---

<objective>
Create two correctness test files that prove the parallel pipeline implementation is deterministic and exception-isolated.

Purpose: Close PIPE-04 (determinism proof) and PIPE-05 (exception isolation proof). Also validates PIPE-01/PIPE-02 recording via assertion.
Output: Two new test files with comprehensive test cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-pipeline-parallelization-renaissance-completion/58-CONTEXT.md
@.planning/phases/58-pipeline-parallelization-renaissance-completion/58-RESEARCH.md
@.planning/phases/58-pipeline-parallelization-renaissance-completion/58-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — executor must verify these exist before proceeding -->

From services/intelligence_pipeline_agent.py (after Plan 01):
```python
def _timed_plugin_call(plugin, frames):
    """Wrapper that returns (result, duration_ms) tuple."""
    t0 = time.perf_counter()
    result = plugin.compute_full(frames)
    duration_ms = (time.perf_counter() - t0) * 1000
    return result, duration_ms

def _collect_plugin_results(self, tasks, results, log_prefix="plugin", tier="I1") -> list[dict]:
    # Unpacks (result, duration_ms) tuples
    # Records PLUGIN_DURATION_MS.labels(plugin_name=..., tier=...).observe(duration_ms)
    # Records PLUGIN_ERRORS_TOTAL.labels(plugin_name=..., tier=...).inc() on exception
```

From src/observability/metrics.py (after Plan 01):
```python
PLUGIN_DURATION_MS = Histogram("intelligence_pipeline_plugin_duration_ms", ..., ["plugin_name", "tier"])
PLUGIN_ERRORS_TOTAL = Counter("intelligence_pipeline_plugin_errors_total", ..., ["plugin_name", "tier"])
```

From tests/unit/test_pipeline_parallelization.py (existing _make_agent pattern):
```python
def _make_agent():
    agent = IntelligencePipelineComputeAgent.__new__(IntelligencePipelineComputeAgent)
    agent.name = "intelligence_pipeline_agent"
    agent._stop_event = asyncio.Event()
    agent.logger = MagicMock()
    # ... (see full list in test file)
    cpu_count = os.cpu_count() or 24
    agent._executor = ThreadPoolExecutor(max_workers=cpu_count * 2, thread_name_prefix="test_intel_")
    return agent
```

From src/intelligence/register_plugins.py:
```python
TIER_I1 = [...]  # 27 plugin names
TIER_I7 = [...]  # 36 plugin names (also exported as I7_PLUGINS from intelligence_pipeline_agent.py)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create determinism test — sequential vs parallel output equivalence</name>
  <files>tests/unit/test_pipeline_determinism.py</files>
  <read_first>
    - tests/unit/test_pipeline_parallelization.py (canonical _make_agent pattern, _mock_plugin helper, import pattern)
    - services/intelligence_pipeline_agent.py (lines 1035-1076 _run_i1, lines 1130-1200 _run_i7, lines 1000-1029 _collect_plugin_results — confirm Plan 01 changes are present: _timed_plugin_call, tier parameter)
    - src/intelligence/register_plugins.py (TIER_I1 list — needed for plugin name iteration)
  </read_first>
  <behavior>
    - Test 1 (test_i1_100_bars_deterministic): Run _run_i1 100 times with deterministic mock plugins. All 100 results must be identical dicts (same keys, same values). Each mock plugin returns a fixed dict keyed by plugin name (e.g., {"plugin_name_val": float(i)}).
    - Test 2 (test_i1_parallel_matches_sequential): Run _run_i1 once with parallel executor, then run same plugins sequentially (direct compute_full calls). Outputs must match within tolerance abs(a - b) < 1e-10 for any float values.
    - Test 3 (test_i7_100_bars_deterministic): Run _run_i7 100 times with deterministic mock plugins (some returning signals, some not). All 100 signal lists must be identical.
    - Test 4 (test_thread_pool_size_configurable): Create agent with mocked Settings.intelligence_thread_pool_workers=64, verify agent._executor._max_workers == 64.
  </behavior>
  <action>
Create `tests/unit/test_pipeline_determinism.py`:

1. Copy `_make_agent()` helper from `test_pipeline_parallelization.py` exactly — then add any new attributes from Plan 01 if needed. The `_make_agent` must set `agent._settings.intelligence_thread_pool_workers = 0` and `agent._settings.pipeline_metrics_port = 9125`.

2. Create `_deterministic_plugin(output_dict)` helper — returns a class with `compute_full(self, frames)` that returns a copy of `output_dict` every time. No randomness, no side effects.

3. Create `_deterministic_signal_plugin(plugin_name, direction)` helper for I7 — returns class whose `compute_full` returns `{"signal": {"direction": direction, "confidence": 0.75, "setup_plugin": plugin_name, ...}}`.

4. Write 4 test methods in `TestPipelineDeterminism` class:

**test_i1_100_bars_deterministic**: Load 5 mock I1 plugins (use first 5 names from TIER_I1), each returning `{f"{name}_rsi": float(idx)}`. Call `await agent._run_i1(frames, "ES", "1m")` 100 times. Assert `all(r == results[0] for r in results[1:])`.

**test_i1_parallel_matches_sequential**: Same 5 mock plugins. Run `_run_i1` once for parallel result. Then compute sequential result by calling each plugin's `compute_full` directly and merging dicts. Assert parallel result keys == sequential result keys. For each key, assert values match within `abs(a - b) < 1e-10` for floats.

**test_i7_100_bars_deterministic**: Load 3 mock I7 plugins (use first 3 names from I7_PLUGINS/TIER_I7), 2 returning signals and 1 returning empty dict. Run `_run_i7` 100 times. Assert signal lists are identical across all runs (compare `setup_plugin`, `direction`, `confidence` fields).

**test_thread_pool_size_configurable**: Patch `Settings` to return `intelligence_thread_pool_workers=64`. Instantiate agent (or verify via `_make_agent` with explicit executor). Assert `agent._executor._max_workers == 64`.

Use `@pytest.mark.asyncio` on all async tests. Use `MagicMock()` for `frames` dict.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_pipeline_determinism.py -v -x --timeout=120</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_pipeline_determinism.py exists
    - File contains `class TestPipelineDeterminism`
    - File contains `test_i1_100_bars_deterministic` that runs 100 iterations and asserts all results equal
    - File contains `test_i1_parallel_matches_sequential` that compares parallel vs sequential output
    - File contains `test_i7_100_bars_deterministic` that runs 100 iterations of I7 with signal-producing mock plugins
    - File contains `test_thread_pool_size_configurable` that verifies env var controls pool size
    - All test methods have `@pytest.mark.asyncio` decorator
    - `_make_agent()` helper is present with all required attributes (including any new ones from Plan 01)
    - All 4 tests pass
  </acceptance_criteria>
  <done>Determinism test passes: 100 bars sequential vs parallel outputs identical for both I1 and I7; thread pool size is configurable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create exception isolation test — plugin failure graceful degradation</name>
  <files>tests/unit/test_pipeline_exception_isolation.py</files>
  <read_first>
    - tests/unit/test_pipeline_parallelization.py (canonical _make_agent pattern)
    - tests/unit/test_pipeline_determinism.py (from Task 1 — reuse _make_agent if identical, or copy)
    - services/intelligence_pipeline_agent.py (lines 1000-1029 _collect_plugin_results with tier and error recording)
    - src/observability/metrics.py (PLUGIN_ERRORS_TOTAL, PLUGIN_DURATION_MS — verify they exist)
  </read_first>
  <behavior>
    - Test 1 (test_single_i1_plugin_raises_does_not_crash): One I1 plugin raises RuntimeError. Remaining 4 plugins succeed. _run_i1 returns dict with 4 plugins' outputs. Pipeline does not crash.
    - Test 2 (test_all_i1_plugins_raise_returns_empty): All I1 plugins raise. _run_i1 returns empty dict. Pipeline does not crash.
    - Test 3 (test_single_i7_plugin_raises_does_not_crash): One I7 plugin raises. Remaining I7 plugins succeed. Signal ranking proceeds with available signals.
    - Test 4 (test_error_counter_increments_on_exception): Plugin raises. PLUGIN_ERRORS_TOTAL.labels(plugin_name=..., tier=...).inc() is called. Verify via patching the metric.
    - Test 5 (test_plugin_duration_recorded_on_success): Plugin succeeds. PLUGIN_DURATION_MS.labels(plugin_name=..., tier=...).observe() is called with a positive float. Verify via patching.
    - Test 6 (test_partial_output_propagates_to_downstream): Mix of failing and succeeding I1 plugins. Verify that the output dict contains exactly the keys from succeeding plugins and none from the failing one.
  </behavior>
  <action>
Create `tests/unit/test_pipeline_exception_isolation.py`:

1. Copy `_make_agent()` helper (same as determinism test). Copy `_deterministic_plugin()` helper.

2. Create `_failing_plugin(error_cls=RuntimeError, msg="injected failure")` helper — returns class whose `compute_full` raises `error_cls(msg)`.

3. Write 6 test methods in `TestExceptionIsolation` class:

**test_single_i1_plugin_raises_does_not_crash**: Assign first TIER_I1 plugin → `_failing_plugin()`. Assign next 4 → `_deterministic_plugin({"val": 1.0})`. Call `await agent._run_i1(frames, "ES", "1m")`. Assert `isinstance(result, dict)`. Assert result contains keys from the 4 succeeding plugins.

**test_all_i1_plugins_raise_returns_empty**: All TIER_I1 plugins → `_failing_plugin()`. Call `_run_i1`. Assert result is empty dict `{}`.

**test_single_i7_plugin_raises_does_not_crash**: First I7_PLUGINS → `_failing_plugin()`. Next 2 → signal-producing plugins. Call `_run_i7`. Assert it returns without raising. Assert result dict has `"ranked"` key.

**test_error_counter_increments_on_exception**: Use `unittest.mock.patch` on `src.observability.metrics.PLUGIN_ERRORS_TOTAL` (or patch at the import location in intelligence_pipeline_agent module). One failing I1 plugin. Call `_run_i1`. Assert `PLUGIN_ERRORS_TOTAL.labels.return_value.inc.assert_called()` (or equivalent based on how patching resolves). The key is verifying the labeled `.labels(plugin_name=TIER_I1[0], tier="I1").inc()` was called.

**test_plugin_duration_recorded_on_success**: Use `unittest.mock.patch` on `PLUGIN_DURATION_MS`. One succeeding I1 plugin. Call `_run_i1`. Assert `PLUGIN_DURATION_MS.labels.return_value.observe.assert_called()`. Assert the observed value is a positive float (duration_ms > 0).

**test_partial_output_propagates_to_downstream**: Use 5 I1 plugins. Plugin index 0 and 2 fail. Plugins 1, 3, 4 succeed with unique keys (`{"p1_key": 1}`, `{"p3_key": 3}`, `{"p4_key": 4}`). Assert result contains `p1_key`, `p3_key`, `p4_key`. Assert result does NOT contain any key from failing plugins.

Use `@pytest.mark.asyncio` on all async tests.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_pipeline_exception_isolation.py -v -x --timeout=120</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_pipeline_exception_isolation.py exists
    - File contains `class TestExceptionIsolation`
    - File contains `test_single_i1_plugin_raises_does_not_crash`
    - File contains `test_all_i1_plugins_raise_returns_empty`
    - File contains `test_single_i7_plugin_raises_does_not_crash`
    - File contains `test_error_counter_increments_on_exception` that verifies PLUGIN_ERRORS_TOTAL.labels(...).inc() called
    - File contains `test_plugin_duration_recorded_on_success` that verifies PLUGIN_DURATION_MS.labels(...).observe() called with positive value
    - File contains `test_partial_output_propagates_to_downstream`
    - All test methods have `@pytest.mark.asyncio` decorator
    - All 6 tests pass
  </acceptance_criteria>
  <done>Exception isolation tests pass: plugin failure never crashes pipeline; error counter fires; duration recorded; partial output propagates correctly</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/test_pipeline_determinism.py tests/unit/test_pipeline_exception_isolation.py -v` — all 10 tests pass
- `.venv/bin/pytest tests/unit/test_pipeline_parallelization.py -v -x` — existing tests still pass (regression check)
</verification>

<success_criteria>
- Determinism test: 100 bars sequential == parallel for both I1 and I7
- Exception isolation: single/all plugin failures handled gracefully in both I1 and I7
- Error counter PLUGIN_ERRORS_TOTAL fires on plugin exception
- Duration histogram PLUGIN_DURATION_MS records per-plugin latency
- Thread pool size configurability verified
- All 10 new tests pass, existing parallelization tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/58-pipeline-parallelization-renaissance-completion/58-02-SUMMARY.md`
</output>
