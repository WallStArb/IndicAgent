---
phase: 82-ml-intelligence-quality-qualitative-foundation
plan: 05
type: execute
wave: 4
depends_on: [82-02]
files_modified:
  - production/migrations/086_validation_results.sql
  - src/intelligence/services/feature_validation_compute_agent.py
  - services/feature_validation_agent.py
  - production/systemd/indicagent-feature-validation.service
  - production/systemd/indicagent-feature-validation.timer
  - src/api/main.py
  - src/api/routes/validation.py
  - src/observability/metrics.py
  - tests/unit/test_feature_validation_compute_agent.py
autonomous: true
requirements:
  - P82-FEATURE-VALIDATION
user_setup: []

must_haves:
  truths:
    - "validation_results hypertable exists with the documented columns"
    - "shadow_registry has a promotion_evidence JSONB column"
    - "FeatureValidationComputeAgent writes VALIDATED/TWEAK/KILL rows daily"
    - "GET /api/validation/results returns latest per-plugin decisions"
    - "feature_validation_decisions_total{decision} Prometheus counter increments per write"
    - "Migration 086 is idempotent (IF NOT EXISTS) so replay does not fail"
  artifacts:
    - path: "production/migrations/086_validation_results.sql"
      provides: "Schema for validation_results hypertable + shadow_registry.promotion_evidence column"
      contains: "validation_results"
    - path: "src/intelligence/services/feature_validation_compute_agent.py"
      provides: "Daily IC/p-value computation agent"
      exports: ["FeatureValidationComputeAgent"]
    - path: "services/feature_validation_agent.py"
      provides: "Oneshot entrypoint"
    - path: "src/api/routes/validation.py"
      provides: "GET /api/validation/results endpoint"
    - path: "production/systemd/indicagent-feature-validation.service"
      provides: "Type=oneshot systemd unit"
    - path: "production/systemd/indicagent-feature-validation.timer"
      provides: "Daily 02:00 ET timer"
  key_links:
    - from: "src/intelligence/services/feature_validation_compute_agent.py"
      to: "tools/validate_i6_backtest.py"
      via: "imports validate_backtest_results"
      pattern: "from tools.validate_i6_backtest"
    - from: "src/intelligence/services/feature_validation_compute_agent.py"
      to: "validation_results table"
      via: "asyncpg INSERT"
      pattern: "INSERT INTO validation_results"
    - from: "src/intelligence/services/feature_validation_compute_agent.py"
      to: "shadow_registry.promotion_evidence"
      via: "asyncpg UPDATE with JSONB"
      pattern: "promotion_evidence"
---

<objective>
Build the FeatureValidationService: daily IC/p-value computation wrapping `tools/validate_i6_backtest.py`, writing VALIDATED/TWEAK/KILL decisions to a new `validation_results` hypertable and seeding `shadow_registry.promotion_evidence` as the Phase 75 contract. Expose results via a read-only API endpoint.

Purpose: Computation/governance separation — this phase produces *evidence*; Phase 75's ShadowAuditorAgent will *act* on that evidence.
Output: Migration 086 + compute agent + entrypoint + systemd timer + API route + tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-CONTEXT.md
@.planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-RESEARCH.md
@.planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-PATTERNS.md
@tools/validate_i6_backtest.py
@services/ml_training_agent.py
@src/intelligence/services/ml_training_compute_agent.py
@production/migrations/077_shadow_governance.sql
@production/migrations/084_ai_enrichment_tables.sql
@src/api/main.py
</context>

<threat_model>
- Migration 086 MUST use `CREATE TABLE IF NOT EXISTS`, `ADD COLUMN IF NOT EXISTS`, and `CREATE INDEX IF NOT EXISTS` so replay is safe.
- API endpoint is read-only. Do NOT expose any POST/PUT for `validation_results` — decisions are produced only by the compute agent.
- ASVS L1: validate query params on the API route (clamp `limit` and validate `plugin_name` against a non-empty regex if accepted).
- No external network calls; DB-only.
- `validation_results` writes use asyncpg parameter binding (no string interpolation of user data).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Migration 086 — validation_results hypertable + shadow_registry.promotion_evidence column</name>
  <files>production/migrations/086_validation_results.sql</files>
  <read_first>
    - production/migrations/077_shadow_governance.sql
    - production/migrations/084_ai_enrichment_tables.sql
    - .planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-CONTEXT.md (D-05 schema section)
    - .planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-RESEARCH.md (P82-FEATURE-VALIDATION section)
  </read_first>
  <action>
    Create `production/migrations/086_validation_results.sql` with statements (in order):
    1. `CREATE TABLE IF NOT EXISTS validation_results (...)` with columns:
       - `plugin_name TEXT NOT NULL`
       - `timeframe TEXT NOT NULL`
       - `regime_type TEXT` (nullable for global slices)
       - `ic DOUBLE PRECISION NOT NULL`
       - `p_value DOUBLE PRECISION NOT NULL`
       - `n INTEGER NOT NULL`
       - `decision TEXT NOT NULL CHECK (decision IN ('VALIDATED','TWEAK','KILL'))`
       - `bonferroni_corrected BOOLEAN NOT NULL DEFAULT TRUE`
       - `computed_at TIMESTAMPTZ NOT NULL DEFAULT now()`
    2. `SELECT create_hypertable('validation_results', 'computed_at', if_not_exists => TRUE);`
    3. `CREATE INDEX IF NOT EXISTS validation_results_plugin_tf_idx ON validation_results (plugin_name, timeframe, computed_at DESC);`
    4. `ALTER TABLE shadow_registry ADD COLUMN IF NOT EXISTS promotion_evidence JSONB;`
    5. Comment block at top of file: migration 086, purpose, dependencies (none), idempotency note.

    Before writing, examine 077 to confirm exact `shadow_registry` table name (it must match exactly). If the table is named differently or has a schema prefix, mirror that.

    Do NOT execute the migration as part of this plan. Operational deployment runs migrations.
  </action>
  <verify>
    `test -f production/migrations/086_validation_results.sql && echo OK` prints OK.
    `grep -c "IF NOT EXISTS" production/migrations/086_validation_results.sql` returns at least 3 (table, index, column).
    `grep "create_hypertable" production/migrations/086_validation_results.sql` returns at least one line.
  </verify>
  <acceptance_criteria>
    - File `production/migrations/086_validation_results.sql` exists
    - Contains `CREATE TABLE IF NOT EXISTS validation_results`
    - Contains `create_hypertable('validation_results', 'computed_at'` (positional or keyword)
    - Contains `ALTER TABLE shadow_registry ADD COLUMN IF NOT EXISTS promotion_evidence JSONB`
    - Contains `CHECK (decision IN ('VALIDATED','TWEAK','KILL'))` (or equivalent)
    - All DDL statements use `IF NOT EXISTS` (idempotent)
    - File contains a comment block referencing migration number 086 at the top
  </acceptance_criteria>
  <done>
    Migration script is idempotent, creates the hypertable, and adds the evidence column.
  </done>
</task>

<task type="auto">
  <name>Task 2: FeatureValidationComputeAgent + oneshot entrypoint + systemd timer + metrics counter</name>
  <files>src/intelligence/services/feature_validation_compute_agent.py, services/feature_validation_agent.py, production/systemd/indicagent-feature-validation.service, production/systemd/indicagent-feature-validation.timer, src/observability/metrics.py</files>
  <read_first>
    - tools/validate_i6_backtest.py
    - services/ml_training_agent.py
    - src/intelligence/services/ml_training_compute_agent.py
    - production/systemd/indicagent-ml-training.service
    - production/systemd/indicagent-ml-training.timer
    - src/observability/metrics.py (after Plan 04 Task 1)
  </read_first>
  <action>
    Create `src/intelligence/services/feature_validation_compute_agent.py`:
    - Class `FeatureValidationComputeAgent(db_manager, settings)`.
    - Method `async def run(self) -> list[dict]`:
      * Resolve plugins to validate. Initial implementation: query `SELECT DISTINCT component_name, component_type FROM shadow_registry` and validate every row whose `component_type` is in the set of plugin-bearing types observed.
      * For each (plugin, tf, regime_type) slice, fetch the joined dataset of `intelligence_features` + `signal_ledger` outcomes filtered to that slice (exclude `is_backfill = TRUE` and rows without resolved `outcome`).
      * If `n < 30` skip the slice.
      * Import and call `validate_backtest_results(df, field_name=<plugin_field>, min_ic=0.05, alpha=0.01, min_n=30)` from `tools.validate_i6_backtest`.
      * Apply Bonferroni correction inside the call (the function already supports it).
      * Insert one row into `validation_results` per slice via asyncpg parameter binding.
      * Update `shadow_registry.promotion_evidence` with a JSONB document containing the latest decision, IC, p, n, computed_at — passed as a dict (not `json.dumps`, per CLAUDE.md asyncpg rule).
      * Increment `FEATURE_VALIDATION_DECISIONS_TOTAL.labels(decision=<VALIDATED|TWEAK|KILL>).inc()` per row written.
      * Return the list of decision dicts for logging.
    - Method `async def start(self) -> None`: orchestrates `run()`, logs row count per decision via structlog (`setup_service_logging("logs/feature_validation_compute_agent.log")`).
    - Wrap individual slice failures in try/except — log + skip, do not crash the entire run.

    Create `services/feature_validation_agent.py`:
    - Mirror `services/ml_training_agent.py`: load Settings, build DB manager, instantiate the compute agent, `asyncio.run(agent.start())`.
    - Logging via `setup_service_logging("logs/feature_validation_agent.log")`.

    Create `production/systemd/indicagent-feature-validation.service`:
    - `Type=oneshot`, `Restart=no`, `User=indicagent`, `ExecStart=/opt/indicagent/.venv/bin/python -m services.feature_validation_agent`, `Environment=PYTHONPATH=/opt/indicagent`, `[Install] WantedBy=multi-user.target`. Mirror the layout of `indicagent-ml-training.service`.

    Create `production/systemd/indicagent-feature-validation.timer`:
    - `OnCalendar=*-*-* 02:00:00 America/New_York` (daily 02:00 ET — confirm timezone token is supported; if not, fall back to `OnCalendar=*-*-* 07:00:00 UTC` with a comment noting the ET equivalent).
    - `Persistent=true`. `Unit=indicagent-feature-validation.service`. `[Install] WantedBy=timers.target`.

    Edit `src/observability/metrics.py`:
    - Register `Counter` named `feature_validation_decisions_total` with label `decision`, exposed as module-level constant `FEATURE_VALIDATION_DECISIONS_TOTAL`. Use the same registration helper used for `REGIME_SOFT_GATE_SIGNALS_TOTAL` (Plan 04).
  </action>
  <verify>
    `.venv/bin/python -c "from src.intelligence.services.feature_validation_compute_agent import FeatureValidationComputeAgent; print('OK')"` exits 0.
    `.venv/bin/python -c "from src.observability.metrics import FEATURE_VALIDATION_DECISIONS_TOTAL; FEATURE_VALIDATION_DECISIONS_TOTAL.labels(decision='VALIDATED').inc(); print('OK')"` exits 0.
    `grep -q "Type=oneshot" production/systemd/indicagent-feature-validation.service && echo OK` prints OK.
    `grep -q "02:00" production/systemd/indicagent-feature-validation.timer && echo OK` prints OK.
  </verify>
  <acceptance_criteria>
    - `src/intelligence/services/feature_validation_compute_agent.py` defines class `FeatureValidationComputeAgent` with async methods `run` and `start`
    - File contains `from tools.validate_i6_backtest import validate_backtest_results` (or equivalent import)
    - File contains literal substring `INSERT INTO validation_results` (case insensitive ok)
    - File contains literal substring `promotion_evidence` (UPDATE on shadow_registry)
    - File uses asyncpg parameter binding (no `f"{user_input}"` style query construction)
    - `services/feature_validation_agent.py` calls `asyncio.run(...)` on the agent
    - `production/systemd/indicagent-feature-validation.service` contains `Type=oneshot` and `ExecStart` referencing `services.feature_validation_agent`
    - `production/systemd/indicagent-feature-validation.timer` contains an `OnCalendar=` line with `02:00`
    - `src/observability/metrics.py` defines `FEATURE_VALIDATION_DECISIONS_TOTAL` counter with `decision` label
    - `.venv/bin/ruff check src/intelligence/services/feature_validation_compute_agent.py services/feature_validation_agent.py src/observability/metrics.py` exits 0
  </acceptance_criteria>
  <done>
    Compute agent and oneshot wiring ready to run daily and write evidence.
  </done>
</task>

<task type="auto">
  <name>Task 3: GET /api/validation/results endpoint and unit tests</name>
  <files>src/api/routes/validation.py, src/api/main.py, tests/unit/test_feature_validation_compute_agent.py</files>
  <read_first>
    - src/api/main.py
    - src/api/routes (list directory and pick an existing route as the pattern donor; research suggests features.py as role-match)
    - src/intelligence/services/feature_validation_compute_agent.py (after Task 2)
  </read_first>
  <action>
    Create `src/api/routes/validation.py`:
    - FastAPI router with prefix `/api/validation`.
    - `GET /results`: query params `plugin_name: str | None = None`, `limit: int = Query(default=50, ge=1, le=500)`. Returns JSON list of latest `validation_results` rows ordered by `computed_at DESC`. If `plugin_name` provided, filter by it via parameter binding.
    - Internal SQL: `SELECT plugin_name, timeframe, regime_type, ic, p_value, n, decision, bonferroni_corrected, computed_at FROM validation_results WHERE ($1::text IS NULL OR plugin_name = $1) ORDER BY computed_at DESC LIMIT $2;`
    - Read-only; no POST/PUT/DELETE.
    - Use the DB manager injection pattern from the donor route file.

    Edit `src/api/main.py`:
    - Import the new router and call `app.include_router(...)` at the same place sibling routers are registered.

    Create `tests/unit/test_feature_validation_compute_agent.py`:
    - Use mocked DB manager and mocked `validate_backtest_results` import (patch at the agent module path).
    - Test `test_skips_slices_below_n_threshold`: feed a slice with n=10; assert no INSERT call on validation_results for that slice.
    - Test `test_validated_decision_writes_row_and_evidence`: feed a slice with IC=0.10, p=0.001, n=100 → mock returns decision=VALIDATED. Assert one INSERT call to validation_results with `decision='VALIDATED'` and one UPDATE call on shadow_registry with `promotion_evidence` jsonb containing the decision.
    - Test `test_tweak_and_kill_paths`: feed slices producing TWEAK and KILL; assert appropriate INSERTs and metric label increments.
    - Test `test_decision_check_constraint_values`: assert the agent only ever writes `decision` values in `{"VALIDATED","TWEAK","KILL"}` (capture all INSERT params, validate the set).
    - Test `test_metrics_counter_increments_per_decision`: patch `FEATURE_VALIDATION_DECISIONS_TOTAL.labels` and assert `.inc()` called per slice processed.
    - Test `test_individual_slice_failure_does_not_crash_run`: configure the mocked `validate_backtest_results` to raise on slice 2 of 3; assert the agent processes slices 1 and 3 and logs slice 2's failure.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/test_feature_validation_compute_agent.py -v` exits 0.
    `.venv/bin/python -c "from src.api.main import app; routes = [r.path for r in app.routes]; assert '/api/validation/results' in routes, routes; print('OK')"` exits 0.
    `.venv/bin/ruff check src/api/routes/validation.py src/api/main.py tests/unit/test_feature_validation_compute_agent.py` exits 0.
  </verify>
  <acceptance_criteria>
    - `src/api/routes/validation.py` exists and defines a FastAPI `APIRouter` with a `GET /results` endpoint
    - The endpoint declares query params `plugin_name: str | None` and `limit: int` with `ge=1, le=500` constraints
    - The endpoint executes a parameterized SQL query (no f-string interpolation of user input — verifiable by absence of `f"...plugin_name..."` patterns)
    - `src/api/main.py` includes the new router (`include_router` call referencing the validation module)
    - `/api/validation/results` appears in `app.routes`
    - `tests/unit/test_feature_validation_compute_agent.py` contains all six named tests
    - `.venv/bin/pytest tests/unit/test_feature_validation_compute_agent.py -v` exits 0
    - `.venv/bin/ruff check src/api/routes/validation.py src/api/main.py tests/unit/test_feature_validation_compute_agent.py` exits 0
  </acceptance_criteria>
  <done>
    Read-only API surface available, decision write paths covered by tests including failure isolation.
  </done>
</task>

</tasks>

<verification>
- Migration 086 file contains all required statements with `IF NOT EXISTS` idempotency.
- `.venv/bin/pytest tests/unit/test_feature_validation_compute_agent.py -v` exits 0.
- `.venv/bin/ruff check production/migrations/086_validation_results.sql` — N/A (SQL file). Instead run `grep -E "IF NOT EXISTS" production/migrations/086_validation_results.sql | wc -l` >= 3.
- All Python modules import cleanly via `.venv/bin/python -c "import ..."` smoke tests.
</verification>

<success_criteria>
- Migration 086 ready to deploy and idempotent.
- FeatureValidationComputeAgent computes daily decisions, writes evidence rows, and updates shadow_registry.
- Read-only API surface exposes latest results.
- Metrics counter labels decisions for observability.
- Failure isolation: one bad slice does not crash the entire run.
</success_criteria>

<output>
After completion, create `.planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-05-SUMMARY.md` summarizing migration 086, the compute-agent architecture, decision thresholds (delegated to validate_i6_backtest.py), and the API contract.
</output>
