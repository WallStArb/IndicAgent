---
phase: 82-ml-intelligence-quality-qualitative-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-01-SUMMARY.md
autonomous: true
requirements:
  - P82-DATA02
user_setup: []

must_haves:
  truths:
    - "DerivativeOscillatorPlugin shadow status is decided by data, not default"
    - "ACOscillatorPlugin shadow status is decided by data, not default"
    - "Gate decision is recorded with N, Pearson r, p-value in plan summary"
  artifacts:
    - path: ".planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-01-SUMMARY.md"
      provides: "Documented gate outcome per plugin"
      contains: "DerivativeOscillator"
  key_links:
    - from: "signal_ledger.outcome"
      to: "shadow_registry.is_shadow"
      via: "validate_alpha.py --promote"
      pattern: "validate_alpha\\.py.*--plugin"
---

<objective>
Execute the DATA-02 gate check for DerivativeOscillator and ACOscillator plugins. Decide promote (IS_SHADOW=False) or demote (IS_SHADOW=True) based on Pearson r > 0, p < 0.05, N >= 30 evidence from signal_ledger outcomes.

Purpose: Convert accumulated outcome data into a binary shadow-status decision before adding new infrastructure on top of it.
Output: Updated `shadow_registry` rows + 82-01-SUMMARY.md documenting gate evidence.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-CONTEXT.md
@.planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-RESEARCH.md
@production/scripts/validate_alpha.py
</context>

<threat_model>
- Read-only DB queries except for `shadow_registry.is_shadow` flip via `validate_alpha.py --promote`.
- No new external network calls. No new auth surfaces.
- Migration idempotency not applicable — no migrations in this plan.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run DATA-02 gate check and apply shadow_registry decision for DerivOsc + ACOsc</name>
  <files>.planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-01-SUMMARY.md</files>
  <read_first>
    - production/scripts/validate_alpha.py
    - .planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-CONTEXT.md (D-01 section)
    - .planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-RESEARCH.md (P82-DATA02 section)
  </read_first>
  <action>
    Step 1: Resolve real plugin names. Run psql command against indicagent DB:
    `SELECT DISTINCT component_name FROM shadow_registry WHERE component_name ILIKE '%DerivativeOscillator%' OR component_name ILIKE '%ACOscillator%' OR component_name ILIKE '%trad_%';`
    Also run `SELECT DISTINCT plugin_name FROM signal_ledger WHERE plugin_name ILIKE '%Deriv%' OR plugin_name ILIKE '%ACOsc%';`
    Use the actual resolved names (e.g. `DerivativeOscillatorPlugin` vs `trad_DerivativeOscillator`) in subsequent steps; do not assume.

    Step 2: Gate SQL. Run:
    `SELECT plugin_name, count(*) FROM signal_ledger WHERE plugin_name IN (<resolved names>) AND outcome IS NOT NULL GROUP BY plugin_name;`
    Record counts. Gate requires N >= 30 per plugin.

    Step 3: If N >= 30, execute `production/scripts/validate_alpha.py --plugin <resolved_DerivOsc_name> --promote` and `production/scripts/validate_alpha.py --plugin <resolved_ACOsc_name> --promote` from the project venv (`.venv/bin/python production/scripts/validate_alpha.py ...`). Capture stdout (Pearson r, p-value, N, decision) for each plugin.

    Step 4: If N < 30 for either plugin, do NOT run --promote for that one. Instead run `validate_alpha.py --plugin <name>` (read-only) to record current evidence, and explicitly note in the summary that the plugin remains IS_SHADOW=True pending more data.

    Step 5: Verify post-state with `SELECT component_name, is_shadow, last_eval_n, last_eval_at FROM shadow_registry WHERE component_name IN (<resolved names>);`

    Step 6: Write 82-01-SUMMARY.md containing: resolved plugin names, gate SQL counts, full validate_alpha.py output blocks per plugin, post-run `shadow_registry` row state, and an explicit "DECISION" line per plugin in the form `DerivativeOscillatorPlugin: PROMOTED | DEMOTED | INSUFFICIENT_DATA (n=<N>, r=<r>, p=<p>)`.

    Do not modify any source files. This is an operational plan — no code edits.
  </action>
  <verify>
    `psql -U postgres -d indicagent -c "SELECT component_name, is_shadow FROM shadow_registry WHERE component_name ILIKE '%DerivativeOscillator%' OR component_name ILIKE '%ACOscillator%';"` returns rows matching the documented DECISION lines in 82-01-SUMMARY.md.
  </verify>
  <acceptance_criteria>
    - 82-01-SUMMARY.md exists in `.planning/phases/82-ml-intelligence-quality-qualitative-foundation/`
    - 82-01-SUMMARY.md contains a line matching pattern `DerivativeOscillator.*: (PROMOTED|DEMOTED|INSUFFICIENT_DATA)`
    - 82-01-SUMMARY.md contains a line matching pattern `ACOscillator.*: (PROMOTED|DEMOTED|INSUFFICIENT_DATA)`
    - 82-01-SUMMARY.md includes the raw `SELECT plugin_name, count(*)` gate SQL result counts
    - 82-01-SUMMARY.md includes the full stdout from each `validate_alpha.py` invocation
    - `shadow_registry.is_shadow` value for each plugin matches the decision recorded in summary (verified via psql)
    - No source files under `src/` or `services/` were modified by this plan
  </acceptance_criteria>
  <done>
    Gate decision applied, shadow_registry updated to reflect data evidence, full audit trail in 82-01-SUMMARY.md.
  </done>
</task>

</tasks>

<verification>
- `git status` shows only `.planning/phases/82-*/82-01-SUMMARY.md` as new file.
- Resolved plugin names are quoted exactly in the summary so downstream plans reference correct identifiers.
</verification>

<success_criteria>
- DATA-02 gate decision is data-driven, documented, and reflected in `shadow_registry`.
- Downstream plans (02-06) can proceed knowing baseline shadow status of DerivOsc/ACOsc.
</success_criteria>

<output>
After completion, ensure `.planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-01-SUMMARY.md` exists with the DECISION lines, raw SQL output, and validate_alpha.py output.
</output>
