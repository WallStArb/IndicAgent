---
context: phase
phase: 82-ml-intelligence-quality-qualitative-foundation
task: 0
total_tasks: 0
status: simplify_pending
last_updated: 2026-05-13T23:54:14.840Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints — Phase 82 is complete. This is a post-completion simplify pass._

## Critical Anti-Patterns

_None._

<current_state>
Phase 82 is COMPLETE (verified 27/27, committed). The /simplify command ran three review agents and produced a clear fix list. No fixes have been applied yet — session hit context limit before edits could be made.

The phase itself is done. The remaining work is a code quality cleanup on the new Phase 82 files.
</current_state>

<completed_work>

- Phase 82 fully executed (6/6 plans, Wave 1-4)
- All worktrees merged, tests passed (3264 passed, 2 pre-existing failures unrelated to Phase 82)
- Code review: 82-REVIEW.md committed (9 findings, advisory)
- Regression gate: passed
- Verification: 27/27 must-haves — PASSED
- Phase marked complete in ROADMAP/STATE/REQUIREMENTS
- /simplify launched 3 review agents — all completed with findings
</completed_work>

<remaining_work>

**Simplify fixes — apply in order (all in Phase 82 new files):**

1. `src/intelligence/services/feature_validation_compute_agent.py` lines 173-194
   DELETE the entire dead first query block. The `regime_filter` f-string query and params list built there are unconditionally overwritten by the `if regime_type is not None / else` block at lines 197-231.

2. `services/feature_validation_agent.py` line 28
   DELETE `setup_service_logging("logs/feature_validation_agent.log")` and the unused import of `setup_service_logging`. The compute agent's `start()` already calls it with the correct log file.

3. `src/api/routes/validation.py` lines 100-103
   REPLACE the `hasattr(isoformat)` guard pattern with `format_iso_ts(row["computed_at"])`.
   Add import: `from ...core.service_utils import format_iso_ts`

4. `services/ctx_writer_agent.py` lines 163 and 189
   Change `_parse_payload` to return `dict | None` (return `payload` not `[payload]`).
   Change `_run()` to use `msg = self._parse_payload(payload)` directly (drop `rows = ...` and `msg = rows[0]`).

5. `src/intelligence/services/feature_validation_compute_agent.py` lines 282-287
   Extract module-level constants: `_DECISION_VALIDATED = "VALIDATED"`, `_DECISION_TWEAK = "TWEAK"`, `_DECISION_KILL = "KILL"`. Replace raw string literals throughout the file.

6. `services/ctx_writer_agent.py` lines 217-218 and 244
   Remove the redundant `# Pass inner_payload as dict (asyncpg JSONB rule — never json.dumps)` and `# ctx_data as dict (asyncpg JSONB rule — never json.dumps)` inline comments. The JSONB rule is documented once at the module level — repeating it per-call is noise.

7. `src/intelligence/services/feature_validation_compute_agent.py` `run()` method
   Add `asyncio.gather` across plugins. Extract inner tf×regime loop to a helper `_validate_all_slices(plugin_name)`, then replace the `for plugin_name in plugins` loop with `await asyncio.gather(*[self._validate_all_slices(p) for p in plugins], return_exceptions=True)`. Flatten and return results.

**After fixes:** Run `.venv/bin/pytest tests/unit/test_feature_validation_compute_agent.py tests/unit/test_ctx_writer_agent.py -v` to confirm nothing broke, then commit.
</remaining_work>

<decisions_made>

- orjson is NOT in requirements.txt — skip the json.dumps→orjson swap (efficiency agent suggested it but it's not available)
- WR-04 (db_manager.pool.close()) does NOT apply to feature_validation_compute_agent — it uses raw asyncpg.Pool directly, not DatabaseManager. No fix needed.
- asyncio.gather fix (#7) is a real perf win (~480 sequential awaits → concurrent) but is the most complex fix. Do last.
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)
1. This file — understand that Phase 82 is complete; this is purely a simplify pass

## Infrastructure State
- Git branch: `feat/phase80-swarm-observability-ux`
- Phase 82: COMPLETE, all commits on branch
- DB: migrations 085 + 086 exist in files but NOT yet applied to the database

<context>
Phase 82 done. /simplify found 7 actionable fixes across 3 files. No fixes applied — session hit context limit. Fresh session should apply fixes, run tests, commit, then optionally plan Phase 83.
</context>

<next_action>
Apply the 7 simplify fixes listed above, run targeted tests, commit as "refactor(82): simplify — dead code, double logging, format_iso_ts, decision constants".
Then: `/gsd-discuss-phase 83` or `/gsd-plan-phase 83`
</next_action>
