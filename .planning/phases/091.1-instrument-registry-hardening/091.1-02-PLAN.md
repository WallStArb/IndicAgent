---
phase: 091.1-instrument-registry-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/settings.py
autonomous: true
must_haves:
  truths:
    - "get_active_contracts() opens exactly one psycopg2 connection per cache miss"
    - "All three queries (futures templates, front-month contracts, non-futures) run within the same connection block"
    - "Function behavior and return value are identical to before the change"
  artifacts:
    - path: "src/config/settings.py"
      provides: "get_active_contracts with single psycopg2 connection"
      contains: "psycopg2.connect"
  key_links:
    - from: "get_active_contracts"
      to: "psycopg2.connect"
      via: "single with block containing all three cursor operations"
      pattern: "with psycopg2\\.connect"
---

<objective>
Apply Fix 4: consolidate the three separate psycopg2.connect() calls in get_active_contracts() into one `with psycopg2.connect(s.database_url) as conn:` block with three cursor operations inside.

Purpose: Each of the 3 sequential connections creates a new TCP socket to PostgreSQL, runs authentication, and tears down. One connection per cache miss is the correct pattern — one-third the overhead, same semantics.

Output: settings.py with a single psycopg2 connection block containing all three queries.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/091.1-instrument-registry-hardening/091.1-CONTEXT.md
@src/config/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate three psycopg2 connections into one</name>
  <files>src/config/settings.py</files>
  <read_first>
    - src/config/settings.py lines 370-470 (the full get_active_contracts function body, all three psycopg2.connect blocks)
    - Note the structure: block 1 fetches tmpl_rows, block 2 fetches rows (front-month), block 3 fetches nf_rows
    - Note the processing between block 1 and block 2 (building config_by_base, config_by_symbol dicts)
  </read_first>
  <action>
    Rewrite the psycopg2 section of `get_active_contracts()` to use one connection for all three queries.

    The current structure is:
    ```
    with psycopg2.connect(...) as conn:   # block 1 — tmpl_rows
        ...
    # processing: build config_by_base, config_by_symbol
    with psycopg2.connect(...) as conn:   # block 2 — rows (front-month)
        ...
    # processing: build db_instruments
    with psycopg2.connect(...) as conn:   # block 3 — nf_rows
        ...
    ```

    Replace with a single `with psycopg2.connect(s.database_url) as conn:` block that:
    1. Opens one cursor, executes the futures templates query, fetches tmpl_rows, closes cursor
    2. Still builds config_by_base / config_by_symbol from tmpl_rows (same code, just indented inside the with block)
    3. Opens another cursor, executes the front-month query, fetches rows, closes cursor
    4. Still builds db_instruments (same code, indented inside the with block)
    5. Opens another cursor, executes the non-futures query, fetches nf_rows, closes cursor
    6. Closes the with block — connection released

    Then the non_futures list building (which currently runs after block 3) moves to after the with block or remains inside it — your discretion, but it must use nf_rows which must be in scope.

    Use `with conn.cursor() as cur:` for each of the three cursor sub-blocks (matches existing pattern). Do not change any SQL strings. Do not change any processing logic.
  </action>
  <verify>
    grep -c "psycopg2.connect" src/config/settings.py
    # Must return 1
    .venv/bin/python -c "from src.config.settings import get_active_contracts; print('import ok')"
    .venv/bin/pytest tests/unit/ -q -k "settings" 2>/dev/null | tail -5
  </verify>
  <done>
    - `psycopg2.connect` appears exactly once in settings.py
    - All three queries (futures templates, front-month contracts, non-futures) are executed within that single connection block
    - `from src.config.settings import get_active_contracts` imports without error
    - Unit tests pass (settings-related tests stay green)
  </done>
</task>

</tasks>

<verification>
grep -c "psycopg2.connect" src/config/settings.py  # must be 1
.venv/bin/python -c "from src.config.settings import get_active_contracts, invalidate_active_contracts_cache; print('ok')"
.venv/bin/pytest tests/unit/ -q 2>/dev/null | tail -5
</verification>

<success_criteria>
- Exactly one psycopg2.connect() call in get_active_contracts()
- All three queries run inside that one connection
- Module imports cleanly
- Unit test suite stays green
</success_criteria>

<output>
After completion, create `.planning/phases/091.1-instrument-registry-hardening/091.1-02-SUMMARY.md`
</output>
