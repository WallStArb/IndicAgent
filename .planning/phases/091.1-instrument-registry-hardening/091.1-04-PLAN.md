---
phase: 091.1-instrument-registry-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - "091.1-01"
  - "091.1-03"
files_modified:
  - services/intelligence_pipeline_agent.py
autonomous: true
must_haves:
  truths:
    - "CacheManager is constructed with on_instruments_changed=invalidate_active_contracts_cache"
    - "Pipeline raises RuntimeError at startup if get_active_contracts returns empty list"
    - "ensure_instruments_trigger is called before starting the instruments listener"
  artifacts:
    - path: "services/intelligence_pipeline_agent.py"
      provides: "Wired callback, fail-fast guard, trigger auto-installation"
      contains: "on_instruments_changed=invalidate_active_contracts_cache"
  key_links:
    - from: "intelligence_pipeline_agent.__init__"
      to: "RuntimeError"
      via: "guard after get_active_contracts() call"
      pattern: "if not self\\._contracts"
    - from: "intelligence_pipeline_agent._setup"
      to: "self._db_manager.ensure_instruments_trigger()"
      via: "await call before listener start"
      pattern: "ensure_instruments_trigger"
    - from: "CacheManager construction"
      to: "invalidate_active_contracts_cache"
      via: "on_instruments_changed keyword argument"
      pattern: "on_instruments_changed=invalidate_active_contracts_cache"
---

<objective>
Wire the three intelligence_pipeline_agent.py changes that depend on Wave 1: Fix 1 (callback wiring to CacheManager), Fix 3 (startup fail-fast guard), Fix 7 (call ensure_instruments_trigger before listener).

Purpose: Plans 01 and 03 added the infrastructure (callback param on CacheManager, ensure_instruments_trigger on DatabaseManager). This plan uses both from the agent that orchestrates them. After this plan, the full dependency DAG is correct and the pipeline never starts silently broken.

Output: intelligence_pipeline_agent.py with callback wired, fail-fast guard, and trigger auto-installed on startup.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/091.1-instrument-registry-hardening/091.1-CONTEXT.md
@services/intelligence_pipeline_agent.py
@.planning/phases/091.1-instrument-registry-hardening/091.1-01-SUMMARY.md
@.planning/phases/091.1-instrument-registry-hardening/091.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Startup fail-fast guard (Fix 3)</name>
  <files>services/intelligence_pipeline_agent.py</files>
  <read_first>
    - services/intelligence_pipeline_agent.py lines 100-120 (__init__ body, the get_active_contracts call and lines after it)
    - The exact line is: `self._contracts = get_active_contracts(self.settings)` around line 114
  </read_first>
  <action>
    After the line `self._contracts = get_active_contracts(self.settings)` (which sets `self._contracts`), insert immediately:

    ```python
    if not self._contracts:
        raise RuntimeError(
            "intelligence_pipeline_agent: no active instruments at startup. "
            "DB unreachable or instruments table empty. Check DB connectivity "
            "and ensure production/scripts/migrate_instruments.py has been run."
        )
    ```

    This must be the very next statement after the `get_active_contracts` call — before `self._symbols = [c.symbol for c in self._contracts]`.
  </action>
  <verify>
    grep -n "if not self._contracts" services/intelligence_pipeline_agent.py
    # Must appear immediately after the get_active_contracts line
    grep -A3 "get_active_contracts(self.settings)" services/intelligence_pipeline_agent.py | head -6
  </verify>
  <done>
    - `if not self._contracts: raise RuntimeError(...)` appears in __init__ after get_active_contracts call
    - Error message includes "no active instruments at startup"
    - `self._symbols = [c.symbol for c in self._contracts]` still follows after the guard
  </done>
</task>

<task type="auto">
  <name>Task 2: Callback wiring (Fix 1) + ensure_trigger call (Fix 7)</name>
  <files>services/intelligence_pipeline_agent.py</files>
  <read_first>
    - services/intelligence_pipeline_agent.py lines 245-265 (_setup body around CacheManager construction and listener start)
    - The exact line to modify: `self._cache_mgr = CacheManager(db=self._db, settings=self.settings, symbols=_symbol_filter)`
    - The listener start: `listener_task = self._cache_mgr.start_instruments_listener()`
    - Check the imports at the top — is `invalidate_active_contracts_cache` already imported?
  </read_first>
  <action>
    1. At the top of the file, `invalidate_active_contracts_cache` must be imported. The existing import line is:
       `from src.config.settings import get_active_contracts, get_settings`
       Change it to:
       `from src.config.settings import get_active_contracts, get_settings, invalidate_active_contracts_cache`

    2. Locate the CacheManager construction line (around line 252):
       `self._cache_mgr = CacheManager(db=self._db, settings=self.settings, symbols=_symbol_filter)`
       Add the callback as a keyword argument:
       `self._cache_mgr = CacheManager(db=self._db, settings=self.settings, symbols=_symbol_filter, on_instruments_changed=invalidate_active_contracts_cache)`

    3. Locate the listener start section. Find this block:
       ```
       listener_task = self._cache_mgr.start_instruments_listener()
       self._background_tasks.add(listener_task)
       listener_task.add_done_callback(self._background_tasks.discard)
       self._logger.info("intelligence_pipeline.instruments_listener_started")
       ```
       Insert `await self._db_manager.ensure_instruments_trigger()` on the line BEFORE `listener_task = self._cache_mgr.start_instruments_listener()`.
  </action>
  <verify>
    grep -n "invalidate_active_contracts_cache\|on_instruments_changed\|ensure_instruments_trigger" services/intelligence_pipeline_agent.py
    # Must show: import at top, kwarg in CacheManager(...), and await call before listener
    .venv/bin/python -c "import _path_bootstrap; from services.intelligence_pipeline_agent import IntelligencePipelineComputeAgent; print('import ok')" 2>&1 | head -5
  </verify>
  <done>
    - `invalidate_active_contracts_cache` imported from src.config.settings at module level
    - `on_instruments_changed=invalidate_active_contracts_cache` in the CacheManager(...) construction call
    - `await self._db_manager.ensure_instruments_trigger()` appears before `listener_task = self._cache_mgr.start_instruments_listener()`
    - Module imports without error
  </done>
</task>

</tasks>

<verification>
grep -c "if not self._contracts" services/intelligence_pipeline_agent.py  # must be 1
grep -c "on_instruments_changed=invalidate_active_contracts_cache" services/intelligence_pipeline_agent.py  # must be 1
grep -c "ensure_instruments_trigger" services/intelligence_pipeline_agent.py  # must be 1
.venv/bin/pytest tests/unit/ -q 2>/dev/null | tail -5
</verification>

<success_criteria>
- Startup guard raises RuntimeError when no contracts loaded
- CacheManager constructed with invalidate_active_contracts_cache as callback
- ensure_instruments_trigger called before listener starts
- Unit test suite stays green
</success_criteria>

<output>
After completion, create `.planning/phases/091.1-instrument-registry-hardening/091.1-04-SUMMARY.md`
</output>
