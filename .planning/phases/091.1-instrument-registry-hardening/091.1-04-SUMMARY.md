---
phase: 091.1-instrument-registry-hardening
plan: "04"
status: complete
wave: 2
commits:
  - "feat(091.1-04): startup fail-fast guard (Fix 3) + callback wiring (Fix 1) + trigger call (Fix 7)"
---

# Phase 091.1 Plan 04: intelligence_pipeline_agent.py Wiring Summary

**Three architectural fixes applied to intelligence_pipeline_agent.py — completing the dependency DAG established by plans 01 and 03.**

## Performance

- **Duration:** ~10 min
- **Completed:** 2026-05-20
- **Tasks:** 2 (combined into 1 commit)
- **Files modified:** 1

## Accomplishments

- **Fix 3 (startup fail-fast guard):** Added `if not self._contracts: raise RuntimeError(...)` immediately after `get_active_contracts(self.settings)` in `__init__`. Pipeline now refuses to start silently when DB is unreachable or instruments table is empty.
- **Fix 1 (callback wiring):** Added `invalidate_active_contracts_cache` to the import from `src.config.settings`. Passed as `on_instruments_changed=invalidate_active_contracts_cache` to `CacheManager(...)` construction — connects the pg_notify listener reload to the settings cache invalidation.
- **Fix 7 (trigger auto-install):** Added `await self._db_manager.ensure_instruments_trigger()` on the line before `listener_task = self._cache_mgr.start_instruments_listener()`. Trigger is now idempotently installed on every pipeline startup before the listener begins.

## Verification

```
grep -c "if not self._contracts" services/intelligence_pipeline_agent.py  # → 1
grep -c "on_instruments_changed=invalidate_active_contracts_cache" services/intelligence_pipeline_agent.py  # → 1
grep -c "ensure_instruments_trigger" services/intelligence_pipeline_agent.py  # → 1
```

All three checks → 1. Module imports without error. Unit test suite: 8 pre-existing failures (all confirmed pre-existing before this plan's changes), 3398 passing.

## Files Modified

- `services/intelligence_pipeline_agent.py` — fail-fast guard in `__init__`, callback kwarg in CacheManager construction, trigger call before listener start

## Decisions Made

- Combined Task 1 and Task 2 into one atomic commit since they touch the same file with no risk of conflict
- All 8 test failures confirmed pre-existing via git stash comparison — zero regressions introduced

## Deviations from Plan

None — plan executed exactly as written.

## Next Phase Readiness

- Plan 05 (wave 3): unit test updates for `test_cache_manager.py` + integration tests for instrument registry — no blockers

---
*Phase: 091.1-instrument-registry-hardening*
*Completed: 2026-05-20*
