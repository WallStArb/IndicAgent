---
phase: 100-plugin-shared-infrastructure
plan: 06
type: execute
wave: 4
depends_on: [100-01, 100-03]
files_modified:
  - src/intelligence/features/i1_indicators/cvd.py
  - src/intelligence/features/i1_indicators/ofi.py
  - src/intelligence/features/i1_indicators/ma_composites.py
  - tests/unit/intelligence/test_plugin_mixins.py
autonomous: true
requirements:
  - PLUGIN-INFRA-05

must_haves:
  truths:
    - "CVD plugin sets supports_incremental = False (delegation pattern, not genuine incremental)"
    - "OFI plugin sets supports_incremental = False (delegation pattern, not genuine incremental)"
    - "MAComposite plugin sets supports_incremental = False (delegation pattern, not genuine incremental)"
    - "All three plugins correctly declare they do not support incremental computation"
    - "Executor validation no longer expects _state from these plugins"
    - "Conformance test verifies supports_incremental flag correctness"
  artifacts:
    - path: "src/intelligence/features/i1_indicators/cvd.py"
      provides: "CVD plugin with corrected supports_incremental flag"
      contains: "supports_incremental = False"
    - path: "src/intelligence/features/i1_indicators/ofi.py"
      provides: "OFI plugin with corrected supports_incremental flag"
      contains: "supports_incremental = False"
    - path: "src/intelligence/features/i1_indicators/ma_composites.py"
      provides: "MAComposite plugin with corrected supports_incremental flag"
      contains: "supports_incremental = False"
    - path: "tests/unit/intelligence/test_plugin_mixins.py"
      provides: "Conformance test for supports_incremental flag correctness"
      contains: "test_supports_incremental_flag_correctness"
  key_links:
    - from: "src/intelligence/pipeline/executor.py"
      to: "CVD, OFI, MAComposite"
      via: "Executor skips incremental validation when supports_incremental=False"
      pattern: "supports_incremental.*False"
    - from: "Design doc finding"
      to: "Flag correction"
      via: "3 plugins claim incremental but delegate to compute_full"
      pattern: "supports_incremental = True.*compute_next.*compute_full"
---

<objective>
Correct supports_incremental flags on 3 delegation plugins (CVD, OFI, MAComposite) that claim incremental support but only delegate compute_next to compute_full. Set supports_incremental=False to match actual behavior. Add conformance test verifying flag correctness.

Purpose: The design doc identified 3 plugins with supports_incremental=True but compute_next just calls compute_full (no genuine incremental optimization). This is misleading — executor expects _state returns and invokes incremental path, but plugins gain no performance benefit. Setting the flag correctly removes them from incremental validation and clarifies actual capability.

Output: 3 plugins have supports_incremental=False. Conformance test validates that supports_incremental=True plugins have genuine incremental logic (non-delegation compute_next).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/100-plugin-shared-infrastructure/100-RESEARCH.md
@.planning/phases/100-plugin-shared-infrastructure/100-03-SUMMARY.md
@docs/plans/2026-05-21-plugin-infrastructure-design.md
@src/intelligence/features/i1_indicators/cvd.py
@src/intelligence/features/i1_indicators/ofi.py
@src/intelligence/features/i1_indicators/ma_composites.py
@src/intelligence/pipeline/executor.py
@tests/unit/intelligence/test_plugin_mixins.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set supports_incremental=False on CVD plugin</name>
  <files>
    src/intelligence/features/i1_indicators/cvd.py
  </files>
  <read_first>
    src/intelligence/features/i1_indicators/cvd.py
    src/intelligence/pipeline/executor.py
  </read_first>
  <action>
    Fix CVDPlugin in src/intelligence/features/i1_indicators/cvd.py:

    1. Find the line: supports_incremental: bool = True (or similar)
    2. Change to: supports_incremental: bool = False
    3. Add docstring comment explaining why: "# Delegates to compute_full — no genuine incremental logic (per-plugin state architecture, not state parameter)"

    The design doc found CVD uses self._state per-symbol sub-dicts instead of the state parameter — an architectural bug. Setting supports_incremental=False removes it from incremental validation. Full fix requires architectural redesign (deferred to future work).

    Keep all CVD computation logic unchanged. Only correct the flag.
  </action>
  <verify>
    `grep "supports_incremental" src/intelligence/features/i1_indicators/cvd.py` returns exactly 1 line with "False".
    `grep -c "self._state" src/intelligence/features/i1_indicators/cvd.py` returns at least 1 (architectural bug remains, but flag is correct).
    `.venv/bin/python -c "from src.intelligence.features.i1_indicators.cvd import CVDPlugin; assert CVDPlugin.supports_incremental is False"` succeeds.
  </verify>
  <acceptance_criteria>
    - CVDPlugin.supports_incremental is False
    - Docstring or comment explains delegation pattern
    - CVD computation logic unchanged
  </acceptance_criteria>
  <done>
    CVD plugin correctly declares supports_incremental=False. Removed from incremental validation path.
  </done>
</task>

<task type="auto">
  <name>Task 2: Set supports_incremental=False on OFI plugin</name>
  <files>
    src/intelligence/features/i1_indicators/ofi.py
  </files>
  <read_first>
    src/intelligence/features/i1_indicators/ofi.py
    src/intelligence/features/i1_indicators/cvd.py
  </read_first>
  <action>
    Fix OFIPlugin in src/intelligence/features/i1_indicators/ofi.py:

    1. Find the line: supports_incremental: bool = True
    2. Change to: supports_incremental: bool = False
    3. Add docstring comment: "# Delegates to compute_full — no genuine incremental logic (per-symbol state architecture, not state parameter)"

    OFI has the same architectural issue as CVD (per-symbol self._state sub-dicts). Correct the flag; defer architectural fix.

    Keep all OFI computation logic unchanged.
  </action>
  <verify>
    `grep "supports_incremental" src/intelligence/features/i1_indicators/ofi.py` returns exactly 1 line with "False".
    `grep -c "self._state" src/intelligence/features/i1_indicators/ofi.py` returns at least 1.
    `.venv/bin/python -c "from src.intelligence.features.i1_indicators.ofi import OFIPlugin; assert OFIPlugin.supports_incremental is False"` succeeds.
  </verify>
  <acceptance_criteria>
    - OFIPlugin.supports_incremental is False
    - Docstring or comment explains delegation pattern
    - OFI computation logic unchanged
  </acceptance_criteria>
  <done>
    OFI plugin correctly declares supports_incremental=False.
  </done>
</task>

<task type="auto">
  <name>Task 3: Set supports_incremental=False on MAComposite plugin</name>
  <files>
    src/intelligence/features/i1_indicators/ma_composites.py
  </files>
  <read_first>
    src/intelligence/features/i1_indicators/ma_composites.py
    src/intelligence/features/i1_indicators/cvd.py
  </read_first>
  <action>
    Fix MACompositePlugin in src/intelligence/features/i1_indicators/ma_composites.py (note: path may be src/intelligence/composites/ma_composites.py — verify actual location):

    1. Find the file location (may be in composites/ subdirectory)
    2. Find the line: supports_incremental: bool = True
    3. Change to: supports_incremental: bool = False
    4. Add docstring comment: "# Delegates to compute_full — simple golden cross death cross comparison, no incremental state"

    MAComposite computes golden/death cross signals from MA comparison. No incremental state needed — delegating compute_next to compute_full is correct behavior.

    Keep all MA computation logic unchanged.
  </action>
  <verify>
    `grep -r "class.*MAComposite" src/intelligence/ --include="*.py" | head -1` finds the file.
    `grep "supports_incremental" <found_file>` returns exactly 1 line with "False".
    `.venv/bin/python -c "from <correct_module> import MACompositePlugin; assert MACompositePlugin.supports_incremental is False"` succeeds.
  </verify>
  <acceptance_criteria>
    - MACompositePlugin.supports_incremental is False
    - Docstring or comment explains delegation pattern
    - MA computation logic unchanged
  </acceptance_criteria>
  <done>
    MAComposite plugin correctly declares supports_incremental=False.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add conformance test for supports_incremental flag correctness</name>
  <files>
    tests/unit/intelligence/test_plugin_mixins.py
  </files>
  <read_first>
    tests/unit/intelligence/test_plugin_mixins.py
    src/intelligence/register_plugins.py
    src/intelligence/pipeline/executor.py
  </read_first>
  <action>
    Add conformance test to tests/unit/intelligence/test_plugin_mixins.py:

    class TestSupportsIncrementalFlagCorrectness:
      - test_supports_incremental_plugins_have_genuine_logic():
        Iterate all plugins with supports_incremental=True
        Verify compute_next is NOT a simple delegation to compute_full
        Check: compute_next method has more than 5 lines of actual logic (not just "return compute_full")
        Or: compute_next uses the state parameter (greps for "state[" or "state.")
        Fail if any True-flagged plugin is a trivial delegation

      - test_delegation_plugins_have_false_flag():
        Verify CVD, OFI, MAComposite have supports_incremental=False
        This is a regression test for the 3 plugins fixed in this plan

      Use src.intelligence.register_plugins imports to get all plugin instances.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/test_plugin_mixins.py::TestSupportsIncrementalFlagCorrectness -v` exits 0 with all tests passing.
    `grep "class TestSupportsIncrementalFlagCorrectness" tests/unit/intelligence/test_plugin_mixins.py` returns exactly 1.
    `grep "def test_supports_incremental_plugins_have_genuine_logic\|def test_delegation_plugins_have_false_flag" tests/unit/intelligence/test_plugin_mixins.py | wc -l` returns 2.
  </verify>
  <acceptance_criteria>
    - TestSupportsIncrementalFlagCorrectness class exists
    - Test verifies all supports_incremental=True plugins have genuine compute_next logic
    - Test verifies CVD, OFI, MAComposite have supports_incremental=False
    - All tests pass
  </acceptance_criteria>
  <done>
    Conformance test ensures supports_incremental flags match actual plugin behavior. Prevents future delegation plugins from incorrectly claiming incremental support.
  </done>
</task>

</tasks>

<verification>
- `grep "supports_incremental" src/intelligence/features/i1_indicators/cvd.py src/intelligence/features/i1_indicators/ofi.py | grep -c "False"` returns 2
- `grep "supports_incremental" src/intelligence/composites/ma_composites.py src/intelligence/features/i1_indicators/ma_composites.py 2>/dev/null | grep -c "False"` returns 1
- `.venv/bin/pytest tests/unit/intelligence/test_plugin_mixins.py::TestSupportsIncrementalFlagCorrectness -v` exits 0
- `.venv/bin/python -c "from src.intelligence.features.i1_indicators.cvd import CVDPlugin; from src.intelligence.features.i1_indicators.ofi import OFIPlugin; assert CVDPlugin.supports_incremental is False and OFIPlugin.supports_incremental is False"` succeeds
</verification>

<success_criteria>
All 3 delegation plugins (CVD, OFI, MAComposite) have supports_incremental=False. Conformance test validates that True-flagged plugins have genuine incremental logic. Flags now match actual behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/100-plugin-shared-infrastructure/100-06-SUMMARY.md`
</output>
