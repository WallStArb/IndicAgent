---
phase: 100-plugin-shared-infrastructure
plan: 03
type: execute
wave: 2
depends_on: [100-01]
files_modified:
  - src/intelligence/features/i1_indicators/rsi.py
  - src/intelligence/features/i1_indicators/cmf.py
  - src/intelligence/features/i3_structure/market_profile.py
  - src/intelligence/features/i3_structure/session_levels.py
  - src/intelligence/features/smc_context/bocpd_changepoint.py
autonomous: true
requirements:
  - PLUGIN-INFRA-01
  - PLUGIN-INFRA-03

must_haves:
  truths:
    - "RSI plugin no longer reads self._state; uses state parameter from _compute_next_core"
    - "RSI plugin returns _state in both compute_full and compute_next (via mixin or manual)"
    - "CMF plugin no longer reads self._state; uses state parameter from _compute_next_core"
    - "CMF plugin returns _state in both compute_full and compute_next"
    - "MarketProfile compute_next returns _state (fixes HIGH bug)"
    - "SessionLevels compute_next returns _state (fixes HIGH bug)"
    - "BOCPD compute_full returns _state (fixes HIGH bug)"
    - "All 5 HIGH bugs from design doc are fixed"
  artifacts:
    - path: "src/intelligence/features/i1_indicators/rsi.py"
      provides: "RSI plugin with self._state bug fixed"
      contains: "def compute_next", "state parameter", "_state in return"
    - path: "src/intelligence/features/i1_indicators/cmf.py"
      provides: "CMF plugin with self._state bug fixed"
      contains: "def compute_next", "state parameter", "_state in return"
    - path: "src/intelligence/features/i3_structure/market_profile.py"
      provides: "MarketProfile plugin with missing _state bug fixed"
      contains: "def compute_next", 'result["_state"]'
    - path: "src/intelligence/features/i3_structure/session_levels.py"
      provides: "SessionLevels plugin with missing _state bug fixed"
      contains: "def compute_next", 'result["_state"]'
    - path: "src/intelligence/features/smc_context/bocpd_changepoint.py"
      provides: "BOCPD plugin with missing _state bug fixed"
      contains: "def compute_full", 'result["_state"]'
  key_links:
    - from: "src/intelligence/pipeline/executor.py"
      to: "src/intelligence/features/i1_indicators/rsi.py"
      via: "_timed_plugin_call validation"
      pattern: '"_state" not in result'
    - from: "Phase 093 correctness tests"
      to: "All 5 fixed plugins"
      via: "test_plugin_incremental.py passes"
      pattern: "TestRSIIncremental|TestCMF|TestMarketProfile"
---

<objective>
Fix the 5 HIGH-severity bugs identified in the design doc: RSI and CMF read self._state instead of state parameter and never return _state; MarketProfile, SessionLevels, and BOCPD forget to return _state. These fixes are prerequisite before any incremental mixin migration.

Purpose: The design doc's bug analysis found 5 HIGH-severity bugs where incremental computation is silently broken or crashes. RSI/CMF read self._state (PERF-03 violation) and never return _state, so incremental mode never activates. MarketProfile/SessionLevels/BOCPD don't return _state, causing executor validation to crash. These must be fixed before any conformance tests or mixin migration.

Output: All 5 bugs fixed with minimal changes — add _state returns, replace self._state with state parameter. Full mixin migration deferred to plan 04.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/100-plugin-shared-infrastructure/100-RESEARCH.md
@docs/plans/2026-05-21-plugin-infrastructure-design.md
@src/intelligence/features/i1_indicators/rsi.py
@src/intelligence/features/i1_indicators/cmf.py
@src/intelligence/features/i3_structure/market_profile.py
@src/intelligence/features/i3_structure/session_levels.py
@src/intelligence/features/smc_context/bocpd_changepoint.py
@src/intelligence/pipeline/executor.py
@tests/unit/intelligence/test_plugin_incremental.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix RSI plugin — replace self._state with state parameter, add _state return</name>
  <files>
    src/intelligence/features/i1_indicators/rsi.py
  </files>
  <read_first>
    src/intelligence/features/i1_indicators/rsi.py
    src/intelligence/features/i1_indicators/atr.py
    src/intelligence/pipeline/executor.py
  </read_first>
  <action>
    Fix RSIPlugin in src/intelligence/features/i1_indicators/rsi.py:

    1. In compute_next method (around line 66):
       - Change `if not self._state:` to `if not state:`
       - Change all `self._state[key]` reads to `state[key]`
       - Change all `s["..."]` assignments to update the state dict passed in
       - Add `out["_state"] = state` before return
       - Remove self._state usage entirely (this is a PERF-03 violation — state must come from parameter)

    2. In _seed_state method (around line 42):
       - Change `self._state[f"rsi_{p}"]` assignment to build a local state_dict
       - Return state_dict instead of modifying self._state
       - Update compute_full to use `out["_state"] = self._seed_state(frames)`

    3. Use wilders_update from mixins.py for the Wilder's smoothing lines (lines 58-59, 82-83):
       - Add import: from src.intelligence.plugins.mixins import wilders_update
       - Replace `(up_val * (p - 1) + gain) / p` with `wilders_update(up_val, gain, p)`
       - Replace `(down_val * (p - 1) + loss) / p` with `wilders_update(down_val, loss, p)`
       - Do the same in _seed_state

    4. Keep the mathematical computation identical — only change state handling.

    This is a bug fix, not a full mixin migration. The plugin still implements compute_full/compute_next directly (not _compute_full_core/_compute_next_core). Mixin migration happens in plan 04.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/test_plugin_incremental.py::TestRSIIncremental -v` exits 0 with test passing.
    `grep -c "self._state" src/intelligence/features/i1_indicators/rsi.py` returns 0 (only _state field declaration remains, no usage).
    `grep "if not state:" src/intelligence/features/i1_indicators/rsi.py` returns at least 1 (compute_next uses parameter).
    `grep 'out\["_state"\] = state' src/intelligence/features/i1_indicators/rsi.py` returns at least 1 (returns state).
    `grep "wilders_update" src/intelligence/features/i1_indicators/rsi.py` returns at least 2 (uses shared utility).
  </verify>
  <acceptance_criteria>
    - RSIPlugin compute_next reads from state parameter, not self._state
    - RSIPlugin compute_next returns _state in output dict
    - RSIPlugin uses wilders_update from mixins.py
    - TestRSIIncremental passes (proves correctness maintained)
    - No self._state reads remain in compute_next or _seed_state
  </acceptance_criteria>
  <done>
    RSI plugin HIGH bug fixed: no longer reads self._state, uses state parameter correctly, returns _state, and uses shared wilders_update utility. Incremental mode now activates correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix CMF plugin — replace self._state with state parameter, add _state return</name>
  <files>
    src/intelligence/features/i1_indicators/cmf.py
  </files>
  <read_first>
    src/intelligence/features/i1_indicators/cmf.py
    src/intelligence/features/i1_indicators/rsi.py
    src/intelligence/pipeline/executor.py
  </read_first>
  <action>
    Fix CMFPlugin in src/intelligence/features/i1_indicators/cmf.py:

    1. In compute_next method:
       - Find `if not self._state:` or similar pattern and change to `if not state:`
       - Replace all `self._state[key]` reads with `state[key]`
       - Update state dict in-place (state["..."] = ...)
       - Add `out["_state"] = state` before return
       - Remove any self._state usage

    2. In compute_full method:
       - Ensure _state is seeded: either via _seed_state method or inline state dict
       - Return `out["_state"] = {state initialization}`

    3. Apply the same pattern as RSI fix — state parameter, not self._state.

    Keep all MFV (money flow volume) computation identical. Only fix state handling.
  </action>
  <verify>
    `grep -c "self._state" src/intelligence/features/i1_indicators/cmf.py` returns 0 (no usage in compute_next).
    `grep "if not state:" src/intelligence/features/i1_indicators/cmf.py` returns at least 1.
    `grep 'out\["_state"\] = state' src/intelligence/features/i1_indicators/cmf.py` returns at least 1.
    `.venv/bin/python -c "from src.intelligence.features.i1_indicators.cmf import CMFPlugin; p = CMFPlugin(); import pandas as pd; df = pd.DataFrame({'open': [1]*20, 'high': [2]*20, 'low': [0.5]*20, 'close': [1]*20, 'volume': [1000]*20}); result = p.compute_full({'main': df}); assert '_state' in result"` succeeds.
  </verify>
  <acceptance_criteria>
    - CMFPlugin compute_next uses state parameter, not self._state
    - CMFPlugin compute_next returns _state in output dict
    - CMFPlugin compute_full seeds _state correctly
    - No self._state reads remain in compute_next
  </acceptance_criteria>
  <done>
    CMF plugin HIGH bug fixed: no longer reads self._state, uses state parameter correctly, returns _state. Incremental mode now activates correctly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix MarketProfile plugin — add missing _state return in compute_next</name>
  <files>
    src/intelligence/features/i3_structure/market_profile.py
  </files>
  <read_first>
    src/intelligence/features/i3_structure/market_profile.py
    src/intelligence/pipeline/executor.py
  </read_first>
  <action>
    Fix MarketProfilePlugin in src/intelligence/features/i3_structure/market_profile.py:

    1. In compute_next method:
       - Find the return statement
       - Before return, add: `out["_state"] = state` (or equivalent state dict)
       - Ensure state is updated with incremental changes (volume_buckets, bar_count, etc.)

    2. Verify compute_full seeds _state correctly:
       - Check that compute_full returns `out["_state"] = {state initialization}`
       - If missing, add it

    The bug is that compute_next updates state internally but doesn't return it, so executor validation fails with "_state not in result".
  </action>
  <verify>
    `grep -A 5 "def compute_next" src/intelligence/features/i3_structure/market_profile.py | grep "_state"` returns at least 1 (state is returned).
    `grep 'out\["_state"\]' src/intelligence/features/i3_structure/market_profile.py | wc -l` returns at least 2 (both compute_full and compute_next).
    `.venv/bin/python -c "from src.intelligence.features.i3_structure.market_profile import MarketProfilePlugin; p = MarketProfilePlugin(); import pandas as pd; df = pd.DataFrame({'open': [1]*20, 'high': [2]*20, 'low': [0.5]*20, 'close': [1]*20, 'volume': [1000]*20}); result = p.compute_next({'main': df.iloc[-5:]}, state={}); assert '_state' in result"` succeeds or returns empty dict gracefully.
  </verify>
  <acceptance_criteria>
    - MarketProfilePlugin compute_next returns _state in output dict
    - MarketProfilePlugin compute_full returns _state in output dict
    - Executor validation error no longer occurs
  </acceptance_criteria>
  <done>
    MarketProfile plugin HIGH bug fixed: compute_next now returns _state. Executor validation no longer fails. Incremental state persists correctly.
  </done>
</task>

<task type="auto">
  <name>Task 4: Fix SessionLevels plugin — add missing _state return in compute_next</name>
  <files>
    src/intelligence/features/i3_structure/session_levels.py
  </files>
  <read_first>
    src/intelligence/features/i3_structure/session_levels.py
    src/intelligence/pipeline/executor.py
  </read_first>
  <action>
    Fix SessionLevelsPlugin in src/intelligence/features/i3_structure/session_levels.py:

    1. In compute_next method:
       - Find the return statement
       - Before return, add: `out["_state"] = state` (or equivalent state dict)
       - Ensure state is updated with session boundary tracking (bar_count, sess_n, session_start_idx, pivot keys)

    2. Verify compute_full seeds _state correctly:
       - Check that compute_full returns `out["_state"] = {state initialization}`
       - If missing, add it

    The bug is identical to MarketProfile — state is tracked but not returned.
  </action>
  <verify>
    `grep -A 5 "def compute_next" src/intelligence/features/i3_structure/session_levels.py | grep "_state"` returns at least 1.
    `grep 'out\["_state"\]' src/intelligence/features/i3_structure/session_levels.py | wc -l` returns at least 2.
    `.venv/bin/python -c "from src.intelligence.features.i3_structure.session_levels import SessionLevelsPlugin; p = SessionLevelsPlugin(); import pandas as pd; df = pd.DataFrame({'open': [1]*20, 'high': [2]*20, 'low': [0.5]*20, 'close': [1]*20, 'volume': [1000]*20}); result = p.compute_next({'main': df.iloc[-5:]}, state={}); assert '_state' in result or result == {}"` succeeds.
  </verify>
  <acceptance_criteria>
    - SessionLevelsPlugin compute_next returns _state in output dict
    - SessionLevelsPlugin compute_full returns _state in output dict
    - Executor validation error no longer occurs
  </acceptance_criteria>
  <done>
    SessionLevels plugin HIGH bug fixed: compute_next now returns _state. Executor validation no longer fails. Session state (bar_count, pivots) persists correctly.
  </done>
</task>

<task type="auto">
  <name>Task 5: Fix BOCPD plugin — add missing _state return in compute_full</name>
  <files>
    src/intelligence/features/smc_context/bocpd_changepoint.py
  </files>
  <read_first>
    src/intelligence/features/smc_context/bocpd_changepoint.py
    src/intelligence/pipeline/executor.py
  </read_first>
  <action>
    Fix BOCPDPlugin in src/intelligence/features/smc_context/bocpd_changepoint.py:

    1. In compute_full method:
       - Find the return statement
       - Before return, add: `out["_state"] = {state initialization}`
       - State should include: run_length_probs, mu, kappa, alpha, beta arrays, cp_prob, prev_close

    2. Verify compute_next returns _state (may already be correct):
       - Check that compute_next has `out["_state"] = state`

    The bug is that compute_full doesn't return _state, so executor crashes on cold start with "_state not in result".
  </action>
  <verify>
    `grep 'out\["_state"\]' src/intelligence/features/smc_context/bocpd_changepoint.py | wc -l` returns at least 2 (both methods).
    `grep -A 20 "def compute_full" src/intelligence/features/smc_context/bocpd_changepoint.py | grep "_state"` returns at least 1.
    `.venv/bin/python -c "from src.intelligence.features.smc_context.bocpd_changepoint import BOCPDPlugin; p = BOCPDPlugin(); import pandas as pd; df = pd.DataFrame({'open': [1]*20, 'high': [2]*20, 'low': [0.5]*20, 'close': [1]*20, 'volume': [1000]*20}); result = p.compute_full({'main': df}); assert '_state' in result"` succeeds.
  </verify>
  <acceptance_criteria>
    - BOCPDPlugin compute_full returns _state in output dict
    - BOCPDPlugin compute_next returns _state in output dict
    - Executor validation error no longer occurs on cold start
  </acceptance_criteria>
  <done>
    BOCPD plugin HIGH bug fixed: compute_full now returns _state. Executor validation no longer crashes on cold start. Bayesian state persists correctly.
  </done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/intelligence/test_plugin_incremental.py -v` exits 0 with all incremental plugin tests passing
- `grep -c "self._state" src/intelligence/features/i1_indicators/rsi.py src/intelligence/features/i1_indicators/cmf.py` returns 0 (no self._state usage in compute_next)
- `grep 'out\["_state"\]' src/intelligence/features/i3_structure/market_profile.py src/intelligence/features/i3_structure/session_levels.py src/intelligence/features/smc_context/bocpd_changepoint.py | wc -l` returns at least 6 (all fixed)
- `.venv/bin/python -c "from src.intelligence.pipeline.executor import _timed_plugin_call; from src.intelligence.features.i1_indicators.rsi import RSIPlugin; from src.intelligence.features.i1_indicators.cmf import CMFPlugin; from src.intelligence.features.smc_context.bocpd_changepoint import BOCPDPlugin; import pandas as pd; df = pd.DataFrame({'open': [1]*20, 'high': [2]*20, 'low': [0.5]*20, 'close': [1]*20, 'volume': [1000]*20}); result, _ = _timed_plugin_call(RSIPlugin(), {'main': df}, {}); assert '_state' in result"` succeeds for all 5 fixed plugins
</verification>

<success_criteria>
All 5 HIGH-severity bugs fixed: RSI and CMF use state parameter and return _state; MarketProfile, SessionLevels, and BOCPD return _state. Executor validation no longer fails or crashes. All incremental plugin tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/100-plugin-shared-infrastructure/100-03-SUMMARY.md`
</output>
