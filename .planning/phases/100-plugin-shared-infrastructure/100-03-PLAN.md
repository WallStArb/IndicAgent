---
phase: 100-plugin-shared-infrastructure
plan: 03
type: execute
wave: 2
depends_on: [100-01]
files_modified:
  - src/intelligence/features/i1_indicators/rsi.py
  - src/intelligence/features/i1_indicators/cmf.py
  - src/intelligence/features/i3_structure/market_profile.py
  - src/intelligence/features/i3_structure/session_levels.py
  - src/intelligence/features/smc_context/bocpd_changepoint.py
autonomous: true
requirements:
  - PLUGIN-INFRA-01
  - PLUGIN-INFRA-03

must_haves:
  truths:
    - "RSI plugin no longer reads self._state; uses state parameter from compute_next"
    - "RSI plugin returns _state in both compute_full and compute_next"
    - "CMF plugin no longer reads self._state; uses state parameter from compute_next"
    - "CMF plugin returns _state in both compute_full and compute_next"
    - "MarketProfile compute_next returns _state (fixes HIGH bug)"
    - "SessionLevels compute_next returns _state (fixes HIGH bug)"
    - "BOCPD compute_full returns _state (fixes HIGH bug)"
    - "All 5 HIGH bugs from design doc are fixed"
  artifacts:
    - path: "src/intelligence/features/i1_indicators/rsi.py"
      provides: "RSI plugin with self._state bug fixed"
      contains: "def compute_next", "state parameter", "_state in return"
    - path: "src/intelligence/features/i1_indicators/cmf.py"
      provides: "CMF plugin with self._state bug fixed"
      contains: "def compute_next", "state parameter", "_state in return"
    - path: "src/intelligence/features/i3_structure/market_profile.py"
      provides: "MarketProfile plugin with missing _state bug fixed"
      contains: "def compute_next", 'result["_state"]'
    - path: "src/intelligence/features/i3_structure/session_levels.py"
      provides: "SessionLevels plugin with missing _state bug fixed"
      contains: "def compute_next", 'result["_state"]'
    - path: "src/intelligence/features/smc_context/bocpd_changepoint.py"
      provides: "BOCPD plugin with missing _state bug fixed"
      contains: "def compute_full", 'result["_state"]'
  key_links:
    - from: "src/intelligence/pipeline/executor.py"
      to: "src/intelligence/features/i1_indicators/rsi.py"
      via: "_timed_plugin_call validation"
      pattern: '"_state" not in result'
    - from: "Phase 093 correctness tests"
      to: "All 5 fixed plugins"
      via: "test_plugin_incremental.py passes"
      pattern: "TestRSIIncremental|TestCMF|TestMarketProfile"
---

<objective>
Fix the 5 HIGH-severity bugs identified in the design doc: RSI and CMF read self._state instead of state parameter and never return _state; MarketProfile, SessionLevels, and BOCPD forget to return _state. These fixes are prerequisite before any incremental mixin migration.

Purpose: The design doc's bug analysis found 5 HIGH-severity bugs where incremental computation is silently broken or crashes. RSI/CMF read self._state (PERF-03 violation) and never return _state, so incremental mode never activates. MarketProfile/SessionLevels/BOCPD do not return _state, causing executor validation to crash. These must be fixed before any conformance tests or mixin migration.

Output: All 5 bugs fixed with minimal changes -- add _state returns, replace self._state with state parameter. Full mixin migration deferred to plan 04.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/100-plugin-shared-infrastructure/100-RESEARCH.md
@docs/plans/2026-05-21-plugin-infrastructure-design.md
@src/intelligence/features/i1_indicators/rsi.py
@src/intelligence/features/i1_indicators/cmf.py
@src/intelligence/features/i3_structure/market_profile.py
@src/intelligence/features/i3_structure/session_levels.py
@src/intelligence/features/smc_context/bocpd_changepoint.py
@src/intelligence/pipeline/executor.py
@tests/unit/intelligence/test_plugin_incremental.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix RSI plugin -- replace self._state with state parameter, add _state return, use wilders_update</name>
  <files>
    src/intelligence/features/i1_indicators/rsi.py
  </files>
  <read_first>
    src/intelligence/features/i1_indicators/rsi.py
    src/intelligence/features/i1_indicators/atr.py
    src/intelligence/plugins/mixins.py
    src/intelligence/pipeline/executor.py
  </read_first>
  <action>
    Fix RSIPlugin in src/intelligence/features/i1_indicators/rsi.py. This is a bug fix, not a full mixin migration.

    Bug 1 (line 67): `if not self._state:` reads self._state instead of state parameter.
    Bug 2 (line 93): compute_next never returns _state, so state is not persisted.
    Bug 3 (line 42-64): _seed_state writes to self._state instead of returning a state dict.

    Fixes:
    1. In compute_full (line 28-39):
       - Line 38: change `self._seed_state(frames)` to build a local state dict
       - Line 39: change `out["_state"] = self._state` to use the returned state dict
       - Remove self._state usage

    2. Refactor _seed_state (line 42-64):
       - Change return type from None to dict
       - Build a local state_dict instead of writing to self._state
       - Return the state dict
       - Add import: from src.intelligence.plugins.mixins import wilders_update
       - Replace lines 58-59: `(up_val * (p - 1) + max(delta, 0)) / p` with `wilders_update(up_val, max(delta, 0), p)`
       - Replace line equivalent for down_val: `(down_val * (p - 1) + max(-delta, 0)) / p` with `wilders_update(down_val, max(-delta, 0), p)`

    3. In compute_next (line 66-93):
       - Line 67: change `if not self._state:` to `if not state:`
       - Line 76: change `if key not in self._state:` to `if key not in state:`
       - Line 78: change `s = self._state[key]` to `s = state[key]`
       - Lines 82-83: replace inline Wilder's smoothing with wilders_update calls:
         `s["avg_gain"] = wilders_update(s["avg_gain"], gain, p)`
         `s["avg_loss"] = wilders_update(s["avg_loss"], loss, p)`
       - Add before return: `out["_state"] = state`
       - Remove all self._state reads

    Keep the mathematical computation identical -- only change state handling.
    Note: `_state: dict = field(default_factory=dict)` field declaration may remain since
    the plugin is not yet migrated to IncrementalMixin. It will be removed during mixin migration.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/test_plugin_incremental.py::TestRSIIncremental -v` exits 0 with test passing.
    `grep -c "self._state" src/intelligence/features/i1_indicators/rsi.py` returns at most 1 (only the field declaration line, no usage in methods).
    `grep "if not state:" src/intelligence/features/i1_indicators/rsi.py` returns at least 1 (compute_next uses parameter).
    `grep 'out\["_state"\] = state' src/intelligence/features/i1_indicators/rsi.py` returns at least 1 (returns state).
    `grep "wilders_update" src/intelligence/features/i1_indicators/rsi.py` returns at least 4 (uses shared utility in both _seed_state and compute_next).
  </verify>
  <acceptance_criteria>
    - RSIPlugin compute_next reads from state parameter, not self._state
    - RSIPlugin compute_next returns _state in output dict (out["_state"] = state)
    - RSIPlugin compute_full seeds _state via _seed_state return value, not self._state
    - RSIPlugin uses wilders_update from mixins.py in both _seed_state and compute_next
    - TestRSIIncremental passes (proves correctness maintained)
    - No self._state reads remain in compute_next, compute_full, or _seed_state methods (field declaration may remain)
  </acceptance_criteria>
  <done>
    RSI plugin HIGH bug fixed: no longer reads self._state, uses state parameter correctly, returns _state, and uses shared wilders_update utility. Incremental mode now activates correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix CMF plugin -- replace self._state with state parameter, add _state return</name>
  <files>
    src/intelligence/features/i1_indicators/cmf.py
  </files>
  <read_first>
    src/intelligence/features/i1_indicators/cmf.py
    src/intelligence/features/i1_indicators/rsi.py
    src/intelligence/pipeline/executor.py
  </read_first>
  <action>
    Fix CMFPlugin in src/intelligence/features/i1_indicators/cmf.py.

    Bug 1 (line 60): `if not self._state:` reads self._state instead of state parameter.
    Bug 2 (line 80): compute_next never returns _state.
    Bug 3 (line 53-56): compute_full writes to self._state instead of a local variable.

    Fixes:
    1. In compute_full (line 32-57):
       - Line 53-56: change `self._state = {...}` to `state = {...}` (local variable)
       - Line 57: return includes `"_state": state`

    2. In compute_next (line 59-80):
       - Line 60: change `if not self._state:` to `if not state:`
       - Line 71: change `s = self._state` to `s = state`
       - Line 80: add `out["_state"] = state` before return
       - Remove all self._state reads

    Keep all MFV (money flow volume) computation identical. Only fix state handling.
  </action>
  <verify>
    `grep -c "self._state" src/intelligence/features/i1_indicators/cmf.py` returns at most 1 (only field declaration).
    `grep "if not state:" src/intelligence/features/i1_indicators/cmf.py` returns at least 1.
    `grep 'out\["_state"\] = state' src/intelligence/features/i1_indicators/cmf.py` returns at least 1.
    `.venv/bin/python -c "from src.intelligence.features.i1_indicators.cmf import CMFPlugin; p = CMFPlugin(); import pandas as pd; df = pd.DataFrame({'open': [1]*20, 'high': [2]*20, 'low': [0.5]*20, 'close': [1]*20, 'volume': [1000]*20}); result = p.compute_full({'main': df}); assert '_state' in result"` succeeds.
  </verify>
  <acceptance_criteria>
    - CMFPlugin compute_next uses state parameter, not self._state
    - CMFPlugin compute_next returns _state in output dict
    - CMFPlugin compute_full seeds _state as local variable, not self._state
    - No self._state reads remain in compute_next or compute_full methods
  </acceptance_criteria>
  <done>
    CMF plugin HIGH bug fixed: no longer reads self._state, uses state parameter correctly, returns _state. Incremental mode now activates correctly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix MarketProfile, SessionLevels, and BOCPD -- add missing _state returns</name>
  <files>
    src/intelligence/features/i3_structure/market_profile.py
    src/intelligence/features/i3_structure/session_levels.py
    src/intelligence/features/smc_context/bocpd_changepoint.py
  </files>
  <read_first>
    src/intelligence/features/i3_structure/market_profile.py
    src/intelligence/features/i3_structure/session_levels.py
    src/intelligence/features/smc_context/bocpd_changepoint.py
    src/intelligence/pipeline/executor.py
  </read_first>
  <action>
    Fix the remaining 3 plugins with missing _state returns.

    MarketProfile (src/intelligence/features/i3_structure/market_profile.py):
    - In compute_next: find the return statement, add `out["_state"] = state` before it.
      State contains volume_buckets dict and bar_count -- already mutated in place, just not returned.
    - Verify compute_full returns _state: check for `out["_state"]` in compute_full return.

    SessionLevels (src/intelligence/features/i3_structure/session_levels.py):
    - In compute_next: find the return statement, add `out["_state"] = state` before it.
      State contains bar_count, session tracking -- already mutated in place, just not returned.
    - Verify compute_full returns _state: check for `out["_state"]` in compute_full return.

    BOCPD (src/intelligence/features/smc_context/bocpd_changepoint.py):
    - In compute_full: find the return statement, add `out["_state"] = {state initialization}`.
      State should include run_length_probs, mu, kappa, alpha, beta, cp_prob, prev_close.
    - Verify compute_next returns _state (may already be correct).

    For all three: read the current code first to understand the exact state shape.
    The bug is the same pattern: state is tracked internally but not returned in the output dict,
    causing executor validation to crash with "_state not in result".
  </action>
  <verify>
    `grep -A 5 "def compute_next" src/intelligence/features/i3_structure/market_profile.py | grep "_state"` returns at least 1 (state is returned).
    `grep -A 5 "def compute_next" src/intelligence/features/i3_structure/session_levels.py | grep "_state"` returns at least 1.
    `grep -A 20 "def compute_full" src/intelligence/features/smc_context/bocpd_changepoint.py | grep "_state"` returns at least 1.
    `grep 'out\["_state"\]' src/intelligence/features/i3_structure/market_profile.py src/intelligence/features/i3_structure/session_levels.py src/intelligence/features/smc_context/bocpd_changepoint.py | wc -l` returns at least 4 (each plugin returns _state in both methods).
  </verify>
  <acceptance_criteria>
    - MarketProfilePlugin compute_next returns _state in output dict
    - MarketProfilePlugin compute_full returns _state in output dict
    - SessionLevelsPlugin compute_next returns _state in output dict
    - SessionLevelsPlugin compute_full returns _state in output dict
    - BOCPDPlugin compute_full returns _state in output dict
    - BOCPDPlugin compute_next returns _state in output dict
    - Executor validation error no longer occurs for any of the 3 plugins
  </acceptance_criteria>
  <done>
    MarketProfile, SessionLevels, and BOCPD HIGH bugs fixed: all compute_next and compute_full methods return _state. Executor validation no longer fails or crashes. State persists correctly across incremental calls.
  </done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/intelligence/test_plugin_incremental.py -v` exits 0 with all incremental plugin tests passing
- `grep -c "self._state" src/intelligence/features/i1_indicators/rsi.py src/intelligence/features/i1_indicators/cmf.py` returns at most 2 (only field declarations, no usage in methods)
- `grep 'out\["_state"\]' src/intelligence/features/i3_structure/market_profile.py src/intelligence/features/i3_structure/session_levels.py src/intelligence/features/smc_context/bocpd_changepoint.py | wc -l` returns at least 6 (all methods fixed)
- `grep "wilders_update" src/intelligence/features/i1_indicators/rsi.py | wc -l` returns at least 4
</verification>

<success_criteria>
All 5 HIGH-severity bugs fixed: RSI and CMF use state parameter and return _state; MarketProfile, SessionLevels, and BOCPD return _state. Executor validation no longer fails or crashes. All incremental plugin tests pass. RSI and CMF use wilders_update shared utility.
</success_criteria>

<output>
After completion, create `.planning/phases/100-plugin-shared-infrastructure/100-03-SUMMARY.md`
</output>
