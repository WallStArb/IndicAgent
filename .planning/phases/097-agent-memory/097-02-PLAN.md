---
phase: 097-agent-memory
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/memory/__init__.py
  - src/core/memory/types.py
  - src/core/memory/backends/__init__.py
  - src/config/settings.py
  - config/memory.yaml
  - src/observability/metrics.py
autonomous: true
requirements: [MEM-01, MEM-03]

must_haves:
  truths:
    - "Episode, CalibrationStats, RegimeHistory are importable frozen dataclasses"
    - "Four backend Protocols are defined (EpisodicBackend, CalibrationBackend, RegimeBackend, Mem0Backend)"
    - "Settings exposes AGENT_MEMORY_ENABLED defaulting to False"
    - "config/memory.yaml defines epoch_decay, recall_limit, timeout_ms, queue_maxsize"
    - "Five memory OTel instruments exist in src/observability/metrics.py"
  artifacts:
    - path: "src/core/memory/types.py"
      provides: "Episode, CalibrationStats, RegimeHistory frozen dataclasses"
      contains: "epoch_weight"
    - path: "src/core/memory/backends/__init__.py"
      provides: "4 typed backend Protocols"
      contains: "Protocol"
    - path: "config/memory.yaml"
      provides: "epoch_decay, recall_limit, timeout_ms, queue_maxsize"
  key_links:
    - from: "src/config/settings.py"
      to: "AGENT_MEMORY_ENABLED"
      via: "Field default=False"
      pattern: "agent_memory_enabled"
---

<objective>
Define the contract layer for the memory subsystem: frozen return-type dataclasses, four backend Protocols, the Settings feature flag, the YAML config, and the five OTel instruments. Ring 0 only — no domain vocabulary, no Ring 1 imports. Wave 2 implements against these contracts.

Purpose: Lock down the shapes (D-19, D-20, D-21) so backend implementations and the client can be written in parallel against a stable interface.
Output: src/core/memory/ package skeleton + Settings flag + config/memory.yaml + 5 metrics.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/097-agent-memory/097-CONTEXT.md
@docs/plans/2026-06-02-agent-memory-design.md
@src/observability/metrics.py
@src/config/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create memory package with frozen return-type dataclasses and backend Protocols</name>
  <read_first>
    - docs/plans/2026-06-02-agent-memory-design.md (Section 6.1, 6.2 — field lists for each dataclass)
    - src/core/ai/worker_context.py (Ring 0 dataclass + from __future__ import annotations convention)
    - CLAUDE.md (Ring rule: src/core/ = Ring 0, no domain vocab — but memory IS infrastructure so plain nouns OK)
  </read_first>
  <action>
    Create the directory src/core/memory/ with __init__.py re-exporting the public types (Episode, CalibrationStats, RegimeHistory, and the four Protocols).

    Create src/core/memory/types.py with three frozen dataclasses (from __future__ import annotations at top; @dataclass(frozen=True)):
    - Episode: fields exactly per design Section 6.2 — id, ts, kind, signal_id, symbol, timeframe, agent_id, hmm_regime, vol_regime, entry_type, regime_epoch, outcome, pnl_r, mae, mfe, bars_in_trade, memory_assisted, payload, similarity, epoch_weight. Use precise types: id/signal_id as str (UUIDs stringified per CLAUDE asyncpg rule), ts as datetime, regime_epoch as int, payload as dict, similarity/epoch_weight as float, nullable fields as X | None.
    - CalibrationStats: fields per design Section 6.2 — agent_id, symbol, hmm_regime, entry_type, regime_epoch, sample_n, mean_prediction, actual_rate, calibration_error, calibration_error_variance, correction_factor (float | None), correction_factor_stable (bool), bootstrapped (bool — True when from memory_calibration_promoted, False when partial below N≥30 gate), skill_score, information_coefficient, ic_p_value, brier_score, base_rate, ci_lower, ci_upper, p_value_corrected, p_signal (float | None), feedback_loop_quarantine (bool), quarantine_review_at (datetime | None), promoted_at (datetime | None — None for partial/cold-start rows), window_start (datetime | None), window_end (datetime | None). Document in a docstring: correction_factor is None when correction_factor_stable is False; bootstrapped=False means sample_n is populated but N≥30 gate was not met — agents should reduce confidence weight accordingly.
    - RegimeHistory: fields per design Section 6.2 — symbol, timeframe, current_regime, ts_start, elapsed_bars (int), duration_median_bars, duration_p25_bars, duration_p75_bars, transition_probs (dict | None), transition_n, win_rate (float | None), avg_pnl_r. Document that elapsed_bars is computed at query time, transition_probs/win_rate are None below the N>=30 gate.

    Create src/core/memory/backends/__init__.py defining four runtime-checkable Protocols (typing.Protocol; from __future__ import annotations). Each method is async and declares the graceful-degradation return contract in its docstring (returns [] or None on timeout/error, never raises — D-19):
    - EpisodicBackend: async recall(embedding, agent_id, symbol, hmm_regime, entry_type, regime_epoch, limit) -> list[Episode]
    - CalibrationBackend: async get_calibration(agent_id, symbol, hmm_regime, entry_type, regime_epoch) -> CalibrationStats | None; also async get_partial_sample_n(agent_id, symbol, hmm_regime, entry_type, regime_epoch) -> int (count of labeled episodes below the N≥30 gate — used by MemoryClient cold-start path, D-F3)
    - RegimeBackend: async get_regime_history(symbol, timeframe) -> RegimeHistory | None
    - Mem0Backend: async search(query, tier, symbol, timeframe, limit) -> list[dict]

    No DB or Ollama imports in this plan — Protocols and dataclasses only.
  </action>
  <verify>
    .venv/bin/python -c "from src.core.memory import Episode, CalibrationStats, RegimeHistory, EpisodicBackend, CalibrationBackend, RegimeBackend, Mem0Backend; print('ok')" prints ok. .venv/bin/python -c "from src.core.memory.types import Episode; import dataclasses; assert Episode.__dataclass_params__.frozen" succeeds.
  </verify>
  <acceptance_criteria>
    - src/core/memory/{__init__.py,types.py,backends/__init__.py} exist
    - All three dataclasses are frozen and import cleanly
    - All four Protocols import cleanly and are typing.Protocol subclasses
    - Episode has similarity and epoch_weight fields; CalibrationStats has skill_score, feedback_loop_quarantine, AND bootstrapped; RegimeHistory has elapsed_bars
    - CalibrationBackend Protocol has both get_calibration() and get_partial_sample_n() methods
    - .venv/bin/ruff check src/core/memory/ passes
  </acceptance_criteria>
  <done>Contract layer (return types + backend protocols) is importable and frozen.</done>
</task>

<task type="auto">
  <name>Task 2: Add AGENT_MEMORY_ENABLED flag, config/memory.yaml, and five OTel metrics</name>
  <read_first>
    - src/config/settings.py (lines 130-160 — Field convention with validation_alias, around ollama_* fields)
    - src/observability/metrics.py (Config metrics block ~line 445 for placement; create_histogram/create_counter/create_gauge patterns)
    - docs/plans/2026-06-02-agent-memory-design.md (Section 6.5 OTel metrics table; Section 6.4 epoch_decay default 0.3)
    - CLAUDE.md (OTel call patterns: histogram .record, counter .add, point gauge .set)
  </read_first>
  <action>
    Add to the Settings class in src/config/settings.py a boolean Field agent_memory_enabled with default=False, validation_alias="AGENT_MEMORY_ENABLED", and a description noting it is gated on the D-12 recall@10 validation and N>=200 labeled episodes (MEM-03 shadow gate). Place it near the ollama_* fields. Do not touch any other Settings field.

    Create config/memory.yaml with these keys and values: epoch_decay: 0.3 (prior epoch weight; epoch-2+ = 0.09), recall_limit: 10, timeout_ms: 40 (10ms margin under the 50ms agent budget per D-13/D-19), queue_maxsize: 500 (bounded write queue per D-13), over_fetch_multiplier: 3 (D-23 limit x 3 over-fetch), hnsw_ef_search: 100 (D-11). Add a header comment explaining each value and which decision it traces to.

    Add a new section to src/observability/metrics.py (header comment "Agent memory metrics (Phase 097)") with ALL instruments used by Plans 03-05, exactly named:
    - MEMORY_RECALL_LATENCY_MS = _meter.create_histogram("memory_recall_latency_ms", ..., unit="ms")
    - MEMORY_RECALL_RESULTS_TOTAL = _meter.create_counter("memory_recall_results_total", ...)  # labels: tier, result={hit,miss,timeout}
    - MEMORY_CALIBRATION_APPLIED = _meter.create_counter("memory_calibration_applied", ...)  # labels: agent_id, stable={true,false}
    - MEMORY_WRITE_QUEUE_DEPTH = _meter.create_gauge("memory_write_queue_depth", ...)  # point gauge, .set()
    - MEMORY_WRITE_DROPPED_TOTAL = _meter.create_counter("memory_write_dropped_total", ...)
    - MEMORY_EMBED_LATENCY_MS = _meter.create_histogram("memory_embed_latency_ms", ..., unit="ms")  # labels: batch={true,false}
    - MEMORY_EMBED_STALL_SECONDS = _meter.create_gauge("memory_embed_stall_seconds", ...)  # point gauge — seconds since last successful drain; alert > 30s (F1)
    - MEMORY_EPISODES_LABELED = _meter.create_gauge("memory_episodes_labeled", ...)  # point gauge — post-run labeled count from BackfillJob
    - MEMORY_COHORTS_PROMOTED_TOTAL = _meter.create_counter("memory_cohorts_promoted_total", ...)  # labels: agent_id
    - MEMORY_COHORTS_QUARANTINED_TOTAL = _meter.create_counter("memory_cohorts_quarantined_total", ...)  # labels: agent_id
    - MEMORY_PROMOTION_SKIPPED_N_ELIGIBLE = _meter.create_counter("memory_promotion_skipped_n_eligible", ...)  # labels: agent_id (F6)
    Use create_gauge (point gauge) for MEMORY_WRITE_QUEUE_DEPTH, MEMORY_EMBED_STALL_SECONDS, MEMORY_EPISODES_LABELED since they are absolute values set per cycle. Use create_counter for event counters.
  </action>
  <verify>
    .venv/bin/python -c "from src.config.settings import Settings; assert Settings().agent_memory_enabled is False; print('flag ok')". .venv/bin/python -c "from src.observability.metrics import MEMORY_RECALL_LATENCY_MS, MEMORY_RECALL_RESULTS_TOTAL, MEMORY_CALIBRATION_APPLIED, MEMORY_WRITE_QUEUE_DEPTH, MEMORY_WRITE_DROPPED_TOTAL, MEMORY_EMBED_LATENCY_MS, MEMORY_EMBED_STALL_SECONDS, MEMORY_EPISODES_LABELED, MEMORY_COHORTS_PROMOTED_TOTAL, MEMORY_COHORTS_QUARANTINED_TOTAL, MEMORY_PROMOTION_SKIPPED_N_ELIGIBLE; print('metrics ok')". .venv/bin/python -c "import yaml; d=yaml.safe_load(open('config/memory.yaml')); assert d['timeout_ms']==40 and d['queue_maxsize']==500 and d['epoch_decay']==0.3; print('yaml ok')".
  </verify>
  <acceptance_criteria>
    - Settings().agent_memory_enabled is False by default and overridable via AGENT_MEMORY_ENABLED env
    - config/memory.yaml loads and contains epoch_decay=0.3, recall_limit=10, timeout_ms=40, queue_maxsize=500
    - All eleven memory metric instruments import from src.observability.metrics
    - MEMORY_WRITE_QUEUE_DEPTH is a point gauge (create_gauge), others match design types
    - .venv/bin/ruff check src/config/settings.py src/observability/metrics.py passes
  </acceptance_criteria>
  <done>Feature flag (default off), YAML config, and OTel instruments are in place for Wave 2 to consume.</done>
</task>

</tasks>

<verification>
- All memory contract types import without touching the DB
- AGENT_MEMORY_ENABLED defaults False (MEM-03 shadow gate honored)
- Six OTel instruments importable
</verification>

<success_criteria>
The interface surface (return types, backend protocols, settings flag, config, metrics) is complete and stable so Wave 2 backends and client can be implemented against it without further contract changes.
</success_criteria>

<output>
After completion, create `.planning/phases/097-agent-memory/097-02-SUMMARY.md`
</output>
