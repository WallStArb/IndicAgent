---
phase: 097-agent-memory
plan: 07
type: execute
wave: 5
depends_on: ["097-04", "097-06"]
gap_closure: true
autonomous: true
files_modified:
  - src/core/ai/base_agent.py
  - services/alpha_swarm.py
  - tests/unit/core/test_base_agent_memory_wiring.py
requirements: [MEM-01, MEM-04]

must_haves:
  truths:
    - "BaseAIWorker.__init__ accepts memory_client: MemoryClient | None and stores it"
    - "WorkerContext constructed in base_agent.py:427 passes memory_client=self._memory_client"
    - "alpha_swarm injects its build_memory_client result into each agent at construction time"
    - "With AGENT_MEMORY_ENABLED=True, an agent's context.memory_client is the live MemoryClient (not None)"
    - "With AGENT_MEMORY_ENABLED=False, context.memory_client is None"
  artifacts:
    - path: "src/core/ai/base_agent.py"
      provides: "memory_client attribute on BaseAIWorker + injection into WorkerContext"
      contains: "memory_client"
    - path: "services/alpha_swarm.py"
      provides: "memory_client passed into each constructed agent"
      contains: "memory_client"
  key_links:
    - from: "services/alpha_swarm._memory_client"
      to: "BaseAIWorker._memory_client"
      via: "memory_client= kwarg at agent construction OR setter after construction"
      pattern: "memory_client"
    - from: "BaseAIWorker._memory_client"
      to: "WorkerContext.memory_client"
      via: "memory_client= kwarg in WorkerContext(...) at base_agent.py:427"
      pattern: "WorkerContext\\("
---

<objective>
Close Gap 1 (MEM-01 structural wiring). The WorkerContext built inside `BaseAIWorker.compute()` at `src/core/ai/base_agent.py:427` is constructed without `memory_client=`, so every agent receives `context.memory_client = None` regardless of `AGENT_MEMORY_ENABLED`. `services/alpha_swarm.py` builds `self._memory_client` via `build_memory_client` but never injects it into the agents. This plan threads the MemoryClient from the swarm through `BaseAIWorker` into the WorkerContext so agents actually receive it.

Purpose: Make MEM-01 end-to-end functional — agents can read `context.memory_client.recall(...)`. This is prerequisite to measuring the MEM-04 p95 latency gate (Plan 097-08).
Output: `BaseAIWorker` carries a `memory_client` attribute; `alpha_swarm` injects its client into every agent; WorkerContext construction forwards it.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/097-agent-memory/097-CONTEXT.md
@.planning/phases/097-agent-memory/097-VERIFICATION.md
@src/core/ai/base_agent.py
@src/core/ai/worker_context.py
@services/alpha_swarm.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread memory_client through BaseAIWorker into WorkerContext</name>
  <read_first>
    - src/core/ai/base_agent.py (lines 96-110 __init__; lines 414-430 compute() WorkerContext construction)
    - src/core/ai/worker_context.py (memory_client: MemoryClient | None field, TYPE_CHECKING import pattern)
    - src/core/memory/client.py (MemoryClient type to import under TYPE_CHECKING)
  </read_first>
  <action>
    Edit src/core/ai/base_agent.py.

    1. TYPE_CHECKING import block (lines 28-35): add `from src.core.memory.client import MemoryClient` so Ring 0 stays import-light at runtime (mirror the existing LLMProviderChain TYPE_CHECKING pattern). Do NOT add a runtime import.

    2. `BaseAIWorker.__init__` (line 96): add a keyword-only parameter `memory_client: MemoryClient | None = None` (place it after `name`). Store it on the instance: `self._memory_client: MemoryClient | None = memory_client`. Keep `**kwargs` pass-through to `super().__init__` unchanged. Add a `set_memory_client(self, memory_client: MemoryClient | None) -> None` method that sets `self._memory_client` — this allows the swarm to inject the client after construction without changing every agent constructor signature.

    3. WorkerContext construction (line 427): change
       `worker_ctx = WorkerContext(signal_context=context, llm_chain=self._llm)`
       to
       `worker_ctx = WorkerContext(signal_context=context, llm_chain=self._llm, memory_client=self._memory_client)`

    Default None must be preserved everywhere so agents constructed without a client (and AGENT_MEMORY_ENABLED=False) continue to receive `context.memory_client = None`. Do not change any other behavior in compute().
  </action>
  <verify>
    .venv/bin/python -c "from src.core.ai.base_agent import BaseAIWorker; print('ok')" succeeds.
    grep -n "memory_client" src/core/ai/base_agent.py shows: TYPE_CHECKING import, __init__ param + self._memory_client assignment, set_memory_client method, and the WorkerContext(...) call.
    grep -n "WorkerContext(" src/core/ai/base_agent.py shows the call now includes memory_client=self._memory_client.
    .venv/bin/ruff check src/core/ai/base_agent.py passes.
  </verify>
  <acceptance_criteria>
    - BaseAIWorker.__init__ accepts memory_client and stores self._memory_client (default None)
    - set_memory_client(...) method exists for post-construction injection
    - WorkerContext at base_agent.py:427 is constructed with memory_client=self._memory_client
    - MemoryClient imported only under TYPE_CHECKING (no runtime Ring 1/0 import added)
    - .venv/bin/ruff check passes
  </acceptance_criteria>
  <done>BaseAIWorker carries a memory_client and forwards it into every WorkerContext it builds.</done>
</task>

<task type="auto">
  <name>Task 2: Inject alpha_swarm's MemoryClient into every agent + prove wiring with a test</name>
  <read_first>
    - services/alpha_swarm.py (lines 44, 101, 141-142 — build_memory_client call and self._memory_client storage; the _agents construction block in _setup())
    - src/core/ai/base_agent.py (set_memory_client method added in Task 1)
    - tests/unit/core/test_memory_client.py (existing memory test style/fixtures)
  </read_first>
  <action>
    Edit services/alpha_swarm.py. After `self._memory_client = build_memory_client(self.settings, self._pool)` (line 142) AND after the agents are constructed in `_setup()`, iterate every agent in the swarm's agent collection (e.g. `self._agents`) and call `agent.set_memory_client(self._memory_client)`. Place this injection AFTER agent construction so the client (or None when the flag is off) reaches each agent. If agents are constructed before line 142, move the injection loop to run after both the agents and the client exist. Do not change agent construction signatures.

    Create tests/unit/core/test_base_agent_memory_wiring.py with two unit tests proving the structural wiring (no live DB / Ollama needed):

    1. test_memory_client_injected_reaches_worker_context: construct a minimal concrete BaseAIWorker subclass (agent_id set), call set_memory_client(sentinel) where sentinel is an object(); monkeypatch/stub the agent so compute() builds a WorkerContext; assert the WorkerContext receives memory_client is the sentinel. If invoking full compute() is heavy, assert directly that after set_memory_client(sentinel), self._memory_client is sentinel AND that a WorkerContext built as in base_agent.py:427 (WorkerContext(signal_context=ctx, llm_chain=None, memory_client=agent._memory_client)) carries the sentinel — this proves the kwarg path.

    2. test_memory_client_none_by_default: a freshly constructed agent has _memory_client is None, and a WorkerContext built from it has memory_client is None (proves AGENT_MEMORY_ENABLED=False path leaves agents with None).

    Keep tests CI-clean: no asyncpg pool, no Ollama, no network. Use a plain object() as the MemoryClient sentinel (type checking is via TYPE_CHECKING only, so a stand-in is acceptable at runtime).
  </action>
  <verify>
    grep -n "set_memory_client" services/alpha_swarm.py shows the injection loop over agents.
    .venv/bin/pytest tests/unit/core/test_base_agent_memory_wiring.py -q passes (2 tests green).
    .venv/bin/ruff check services/alpha_swarm.py tests/unit/core/test_base_agent_memory_wiring.py passes.
  </verify>
  <acceptance_criteria>
    - alpha_swarm calls agent.set_memory_client(self._memory_client) for every constructed agent after both exist
    - test_base_agent_memory_wiring.py proves: injected client reaches WorkerContext.memory_client; default is None
    - Tests are CI-clean (no DB, no Ollama, no network)
    - .venv/bin/pytest tests/unit/core/test_base_agent_memory_wiring.py -q green
  </acceptance_criteria>
  <done>alpha_swarm's MemoryClient is injected into every agent; a test proves context.memory_client is the live client when enabled and None when not.</done>
</task>

</tasks>

<verification>
- BaseAIWorker forwards memory_client into WorkerContext at base_agent.py:427
- alpha_swarm injects self._memory_client into every agent post-construction
- Unit test proves the end-to-end structural wiring (injected -> WorkerContext) and the None default
- MEM-01 key link "self._memory_client -> WorkerContext.memory_client" is now WIRED
</verification>

<success_criteria>
With AGENT_MEMORY_ENABLED=True, an agent's `context.memory_client` is the live MemoryClient and `recall(...)` is reachable (MEM-01). With the flag False, it is None. The wiring gap blocking the MEM-04 measurement (Plan 097-08) is removed.
</success_criteria>

<output>
After completion, create `.planning/phases/097-agent-memory/097-07-SUMMARY.md`
</output>
