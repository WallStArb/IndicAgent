---
phase: 42-candlestick-pattern-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/patterns/candlestick_patterns.py
  - src/intelligence/schemas.py
  - tests/unit/test_candlestick_patterns.py
autonomous: true
requirements:
  - CANDLE-01  # Note: Clarification - adds 10 NEW patterns (29 total from 19 existing, not "18 new")
               # 8 patterns already in codebase: Harami Cross, Dark Cloud Cover, Piercing Line,
               # Three White Soldiers, Three Black Crows, Morning Star, Evening Star,
               # Three Inside Up, Three Inside Down. CANDLE-01 requirement text in REQUIREMENTS.md
               # should be updated to reflect "10 new patterns" for accuracy.

must_haves:
  truths:
    - "All 10 new candlestick patterns fire when their structural conditions are met"
    - "Pattern outputs are float (0.0 or 1.0) matching existing pattern convention"
    - "Schema validation passes with new fields added to I5Patterns"
    - "Unit tests verify valid pattern detection and reject malformed fixtures"
  artifacts:
    - path: src/intelligence/patterns/candlestick_patterns.py
      provides: "10 new pattern detection functions"
      min_lines: 400
    - path: src/intelligence/schemas.py
      provides: "I5Patterns schema with 10 new candlestick fields"
      contains: "harami_bull, harami_bear, abandoned_baby_bull, abandoned_baby_bear, tweezer_top, tweezer_bottom, belt_hold_bull, belt_hold_bear, kicker_bull, kicker_bear"
    - path: tests/unit/test_candlestick_patterns.py
      provides: "Unit test coverage for new patterns"
      min_lines: 200
  key_links:
    - from: candlestick_patterns.py
      to: schemas.py I5Patterns
      via: "outputs frozenset must match schema fields"
      pattern: "outputs.*frozenset.*harami_bull"
    - from: schemas.py
      to: candlestick_patterns.py
      via: "extra='forbid' validation requires all outputs declared"
      pattern: "model_config.*ConfigDict.*extra.*forbid"
---

<objective>
Implement 10 new I5 candlestick pattern detectors with schema extension and unit tests.

Purpose: Expand pattern library from 19 to 29 patterns, adding Harami directional variants, Abandoned Baby, Tweezer, Belt Hold, and Kicker patterns.
Output: Working pattern detection functions, updated schema, test coverage
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-candlestick-pattern-expansion/42-CONTEXT.md

# Key Interfaces

From src/intelligence/patterns/candlestick_patterns.py (existing patterns):
```python
# Existing 19 outputs
outputs: frozenset[str] = frozenset({
    "engulfing_bull", "engulfing_bear",
    "pin_bar_bull", "pin_bar_bear",
    "hammer_detected", "shooting_star_detected",
    "inside_bar", "outside_bar", "doji_detected",
    "three_white_soldiers", "three_black_crows",
    "morning_star", "evening_star",
    "three_inside_up", "three_inside_down",
    "harami_cross", "dark_cloud_cover", "piercing_line"
})

# Pattern detection signature (3-bar lookback)
def compute_full(self, frames: dict[str, Any]) -> dict[str, Any]:
    c = df.iloc[-1]  # current bar
    p = df.iloc[-2]  # prior bar
    pp = df.iloc[-3] # two-bars-ago
    # metrics: c_body, c_range, c_bullish, c_bearish, etc.
    # return: {pattern_name: 0.0 or 1.0}
```

From src/intelligence/schemas.py (I5Patterns candlestick section):
```python
# Lines 489-508: Existing 19 candlestick fields
engulfing_bull: float | None = None
engulfing_bear: float | None = None
# ... (17 more fields)
piercing_line: float | None = None
```

From 42-CONTEXT.md pattern definitions (locked decisions):
- Harami Bull/Bear: pp large body (>50% range), p small body inside pp, direction matches p candle
- Abandoned Baby: pp large body, p doji with gap, c large body reversing
- Tweezer Top/Bottom: identical highs/lows within 0.1×ATR
- Belt Hold: long candle with no wick on one side (body>70% range, opposite wick<10%)
- Kicker: gap from prior candle, no wick on gapped side
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 10 new pattern detection functions to candlestick_patterns.py</name>
  <files>src/intelligence/patterns/candlestick_patterns.py</files>

  <read_first>
    - src/intelligence/patterns/candlestick_patterns.py (full file to understand existing structure)
    - .planning/phases/42-candlestick-pattern-expansion/42-CONTEXT.md (pattern definitions section)
  </read_first>

  <behavior>
    Implement 10 new candlestick pattern detection functions following existing patterns:
    1. harami_bull: pp large body (>50% range), p small bullish body inside pp
    2. harami_bear: pp large body, p small bearish body inside pp
    3. abandoned_baby_bull: pp bearish large, p doji with gap up, c bullish large reversing
    4. abandoned_baby_bear: pp bullish large, p doji with gap down, c bearish large reversing
    5. tweezer_top: two consecutive bars with near-identical highs (abs(h1-h2) <= 0.1*ATR)
    6. tweezer_bottom: two consecutive bars with near-identical lows
    7. belt_hold_bull: long white candle, no upper wick (body>70% range, upper_wick<10%)
    8. belt_hold_bear: long black candle, no lower wick
    9. kicker_bull: gap up from prior black, no upper wick on gapped side
    10. kicker_bear: gap down from prior white, no lower wick

    Each pattern returns 1.0 if conditions met, 0.0 otherwise.
    Use ATR from features dict for Tweezer pattern tolerance.
    Gap detection: p body does not overlap pp body (p_o > pp_h for gap up, p_o < pp_l for gap down).
  </behavior>

  <action>
    In src/intelligence/patterns/candlestick_patterns.py:

    1. Extend outputs frozenset (after line 36, add 10 new pattern names):
       Add to frozenset: "harami_bull", "harami_bear", "abandoned_baby_bull", "abandoned_baby_bear",
       "tweezer_top", "tweezer_bottom", "belt_hold_bull", "belt_hold_bear", "kicker_bull", "kicker_bear"

    2. After line 298 (piercing_line), add 10 new pattern detection blocks:

    # Harami Bull (directional variant - p is bullish harami inside pp)
    harami_bull = 0.0
    pp_body_high = max(pp_o, pp_c)
    pp_body_low = min(pp_o, pp_c)
    if (
        pp_range > 0 and pp_body > 0.5 * pp_range
        and p_range > 0 and p_body < 0.3 * p_range
        and p_h <= pp_body_high and p_l >= pp_body_low
        and p_bullish
    ):
        harami_bull = 1.0

    # Harami Bear (directional variant - p is bearish harami inside pp)
    harami_bear = 0.0
    if (
        pp_range > 0 and pp_body > 0.5 * pp_range
        and p_range > 0 and p_body < 0.3 * p_range
        and p_h <= pp_body_high and p_l >= pp_body_low
        and p_bearish
    ):
        harami_bear = 1.0

    # Abandoned Baby Bull (gap up doji + reversal)
    abandoned_baby_bull = 0.0
    gap_up = p_l > pp_h  # gap up: p low above pp high
    if (
        pp_bearish and pp_range > 0 and pp_body > 0.6 * pp_range
        and gap_up
        and p_range > 0 and p_body / p_range < 0.10  # doji
        and c_bullish and c_range > 0 and c_body > 0.6 * c_range
    ):
        abandoned_baby_bull = 1.0

    # Abandoned Baby Bear (gap down doji + reversal)
    abandoned_baby_bear = 0.0
    gap_down = p_h < pp_l  # gap down: p high below pp low
    if (
        pp_bullish and pp_range > 0 and pp_body > 0.6 * pp_range
        and gap_down
        and p_range > 0 and p_body / p_range < 0.10
        and c_bearish and c_range > 0 and c_body > 0.6 * c_range
    ):
        abandoned_baby_bear = 1.0

    # Tweezer Top (near-identical highs)
    tweezer_top = 0.0
    atr_val = float(features.get("atr_14") or atr)
    if atr_val > 0 and abs(p_h - c_h) <= 0.1 * atr_val:
        tweezer_top = 1.0

    # Tweezer Bottom (near-identical lows)
    tweezer_bottom = 0.0
    if atr_val > 0 and abs(p_l - c_l) <= 0.1 * atr_val:
        tweezer_bottom = 1.0

    # Belt Hold Bull (long white, no upper wick)
    belt_hold_bull = 0.0
    if (
        c_bullish and c_range > 0
        and c_body > 0.70 * c_range
        and c_upper_wick < 0.10 * c_range
    ):
        belt_hold_bull = 1.0

    # Belt Hold Bear (long black, no lower wick)
    belt_hold_bear = 0.0
    if (
        c_bearish and c_range > 0
        and c_body > 0.70 * c_range
        and c_lower_wick < 0.10 * c_range
    ):
        belt_hold_bear = 1.0

    # Kicker Bull (gap up from black, no upper wick)
    kicker_bull = 0.0
    gap_up_clean = p_bearish and c_o > pp_h  # gap up from bearish pp
    if (
        gap_up_clean
        and c_bullish and c_range > 0 and c_body > 0.6 * c_range
        and c_upper_wick < 0.15 * c_range
    ):
        kicker_bull = 1.0

    # Kicker Bear (gap down from white, no lower wick)
    kicker_bear = 0.0
    gap_down_clean = p_bullish and c_o < pp_l  # gap down from bullish pp
    if (
        gap_down_clean
        and c_bearish and c_range > 0 and c_body > 0.6 * c_range
        and c_lower_wick < 0.15 * c_range
    ):
        kicker_bear = 1.0

    3. Add to return dict (after line 320, add 10 new entries):
    "harami_bull": harami_bull,
    "harami_bear": harami_bear,
    "abandoned_baby_bull": abandoned_baby_bull,
    "abandoned_baby_bear": abandoned_baby_bear,
    "tweezer_top": tweezer_top,
    "tweezer_bottom": tweezer_bottom,
    "belt_hold_bull": belt_hold_bull,
    "belt_hold_bear": belt_hold_bear,
    "kicker_bull": kicker_bull,
    "kicker_bear": kicker_bear

    DO NOT change the plugin name, min_lookback, or other class attributes.
  </action>

  <verify>
    <automated>
      .venv/bin/pytest tests/unit/test_candlestick_patterns.py -xvs 2>&1 | head -50
    </automated>
  </verify>

  <done>
    - candlestick_patterns.py has 29 pattern outputs (19 existing + 10 new)
    - All 10 new patterns return float 0.0 or 1.0
    - grep -c "abandoned_baby_bull\|harami_bull\|tweezer_top" src/intelligence/patterns/candlestick_patterns.py returns non-zero
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend I5Patterns schema with 10 new candlestick fields</name>
  <files>src/intelligence/schemas.py</files>

  <read_first>
    - src/intelligence/schemas.py (lines 389-530 for I5Patterns class)
  </read_first>

  <action>
    In src/intelligence/schemas.py, locate the I5Patterns class candlestick section (lines 489-508).

    After line 508 (piercing_line), add 10 new optional float fields:

    # 10 new candlestick patterns (Phase 42)
    harami_bull: float | None = None
    harami_bear: float | None = None
    abandoned_baby_bull: float | None = None
    abandoned_baby_bear: float | None = None
    tweezer_top: float | None = None
    tweezer_bottom: float | None = None
    belt_hold_bull: float | None = None
    belt_hold_bear: float | None = None
    kicker_bull: float | None = None
    kicker_bear: float | None = None

    Update the docstring comment on line 405 from:
    # CandlestickPatternsPlugin outputs (19 total fields)
    To:
    # CandlestickPatternsPlugin outputs (29 total fields)

    Update the docstring comment on line 409 from:
    Total: 75 fields
    To:
    Total: 85 fields
  </action>

  <verify>
    <automated>
      grep -c "harami_bull\|abandoned_baby\|tweezer_top\|belt_hold\|kicker" src/intelligence/schemas.py | xargs test $(cat) -eq 10
    </automated>
  </verify>

  <done>
    - I5Patterns class declares all 10 new pattern fields as float | None
    - Docstring reflects 29 total candlestick fields (19 + 10)
    - Total field count is 85 (was 75)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create unit tests for 10 new candlestick patterns</name>
  <files>tests/unit/test_candlestick_patterns.py</files>

  <read_first>
    - src/intelligence/patterns/candlestick_patterns.py (pattern detection logic)
  </read_first>

  <behavior>
    Unit tests verify:
    1. Harami Bull fires when pp large body, p small bullish inside pp
    2. Harami Bear fires when pp large body, p small bearish inside pp
    3. Abandoned Baby Bull fires with gap up doji + bullish reversal
    4. Abandoned Baby Bear fires with gap down doji + bearish reversal
    5. Tweezer Top fires when highs within 0.1*ATR
    6. Tweezer Bottom fires when lows within 0.1*ATR
    7. Belt Hold Bull fires with long white, no upper wick
    8. Belt Hold Bear fires with long black, no lower wick
    9. Kicker Bull fires with gap up from black candle
    10. Kicker Bear fires with gap down from white candle
    11. Malformed fixtures return 0.0 for all patterns
  </behavior>

  <action>
    Create tests/unit/test_candlestick_patterns.py with test fixtures:

    import pytest
    import pandas as pd
    from src.intelligence.patterns.candlestick_patterns import plugin

    def _make_ohlc_frame(opens, highs, lows, closes):
        """Helper to create minimal OHLCV frame."""
        df = pd.DataFrame({
            "open": opens,
            "high": highs,
            "low": lows,
            "close": closes,
            "volume": [1000] * len(opens)
        })
        return df

    def test_harami_bull_valid():
        """Harami Bull: large bearish pp, small bullish p inside pp."""
        df = _make_ohlc_frame(
            opens=[100, 95, 97],
            highs=[105, 100, 99],
            lows=[95, 90, 96],
            closes=[96, 97, 98.5]  # pp bearish (96<100), p bullish (97>95), p inside pp
        )
        frames = {"main": df, "features": {"atr_14": 5.0}}
        result = plugin.compute_full(frames)
        assert result.get("harami_bull") == 1.0

    def test_harami_bull_not_fired_p_bearish():
        """Harami Bull requires p bullish."""
        df = _make_ohlc_frame(
            opens=[100, 95, 97],
            highs=[105, 100, 99],
            lows=[95, 90, 96],
            closes=[96, 94, 98.5]  # p bearish (94<95)
        )
        frames = {"main": df, "features": {"atr_14": 5.0}}
        result = plugin.compute_full(frames)
        assert result.get("harami_bull") == 0.0

    def test_harami_bear_valid():
        """Harami Bear: large bullish pp, small bearish p inside pp."""
        df = _make_ohlc_frame(
            opens=[100, 105, 103],
            highs=[110, 107, 104],
            lows=[95, 100, 102],
            closes=[108, 103, 102.5]  # pp bullish, p bearish inside
        )
        frames = {"main": df, "features": {"atr_14": 5.0}}
        result = plugin.compute_full(frames)
        assert result.get("harami_bear") == 1.0

    def test_abandoned_baby_bull_valid():
        """Abandoned Baby Bull: gap up doji + bullish reversal."""
        df = _make_ohlc_frame(
            opens=[100, 108, 108],  # gap up from pp
            highs=[105, 109, 112],
            lows=[95, 107.5, 108],  # p low > pp high = gap
            closes=[96, 108.1, 111]  # pp bearish, p doji, c bullish
        )
        frames = {"main": df, "features": {"atr_14": 5.0}}
        result = plugin.compute_full(frames)
        assert result.get("abandoned_baby_bull") == 1.0

    def test_abandoned_baby_bear_valid():
        """Abandoned Baby Bear: gap down doji + bearish reversal."""
        df = _make_ohlc_frame(
            opens=[100, 92, 92],   # gap down
            highs=[105, 92.5, 93],
            lows=[95, 91, 88],     # p high < pp low = gap
            closes=[104, 92.1, 89] # pp bullish, p doji, c bearish
        )
        frames = {"main": df, "features": {"atr_14": 5.0}}
        result = plugin.compute_full(frames)
        assert result.get("abandoned_baby_bear") == 1.0

    def test_tweezer_top_valid():
        """Tweezer Top: near-identical highs within 0.1*ATR."""
        df = _make_ohlc_frame(
            opens=[100, 105, 102],
            highs=[110, 110.03, 110],  # highs within 0.03
            lows=[95, 100, 100],
            closes=[108, 103, 104]
        )
        frames = {"main": df, "features": {"atr_14": 1.0}}  # 0.1*ATR = 0.1, 0.03 < 0.1
        result = plugin.compute_full(frames)
        assert result.get("tweezer_top") == 1.0

    def test_tweezer_bottom_valid():
        """Tweezer Bottom: near-identical lows within 0.1*ATR."""
        df = _make_ohlc_frame(
            opens=[100, 95, 98],
            highs=[105, 100, 102],
            lows=[90, 90.04, 90],  # lows within 0.04
            closes=[92, 97, 99]
        )
        frames = {"main": df, "features": {"atr_14": 1.0}}
        result = plugin.compute_full(frames)
        assert result.get("tweezer_bottom") == 1.0

    def test_belt_hold_bull_valid():
        """Belt Hold Bull: long white, no upper wick."""
        df = _make_ohlc_frame(
            opens=[100, 102, 104],
            highs=[105, 104.5, 110],  # c upper wick tiny
            lows=[95, 100, 104],
            closes=[104, 103, 109]   # c bullish, body large relative to range
        )
        frames = {"main": df, "features": {}}
        result = plugin.compute_full(frames)
        assert result.get("belt_hold_bull") == 1.0

    def test_belt_hold_bear_valid():
        """Belt Hold Bear: long black, no lower wick."""
        df = _make_ohlc_frame(
            opens=[100, 98, 96],
            highs=[105, 99, 96],
            lows=[95, 95.5, 90],  # c lower wick tiny
            closes=[96, 97, 91]    # c bearish
        )
        frames = {"main": df, "features": {}}
        result = plugin.compute_full(frames)
        assert result.get("belt_hold_bear") == 1.0

    def test_kicker_bull_valid():
        """Kicker Bull: gap up from bearish candle."""
        df = _make_ohlc_frame(
            opens=[100, 94, 108],   # pp bearish close 94, c opens at 108 > pp high 105
            highs=[105, 98, 110],
            lows=[95, 92, 107],
            closes=[94, 95, 109]
        )
        frames = {"main": df, "features": {}}
        result = plugin.compute_full(frames)
        assert result.get("kicker_bull") == 1.0

    def test_kicker_bear_valid():
        """Kicker Bear: gap down from bullish candle."""
        df = _make_ohlc_frame(
            opens=[100, 106, 92],   # pp bullish close 106, c opens at 92 < pp low 95
            highs=[105, 108, 93],
            lows=[95, 100, 88],
            closes=[106, 105, 89]
        )
        frames = {"main": df, "features": {}}
        result = plugin.compute_full(frames)
        assert result.get("kicker_bear") == 1.0

    def test_all_patterns_zero_with_insufficient_bars():
        """All patterns return 0.0 with fewer than 3 bars."""
        df = _make_ohlc_frame([100], [105], [95], [104])
        frames = {"main": df, "features": {"atr_14": 5.0}}
        result = plugin.compute_full(frames)
        new_patterns = ["harami_bull", "harami_bear", "abandoned_baby_bull", "abandoned_baby_bear",
                       "tweezer_top", "tweezer_bottom", "belt_hold_bull", "belt_hold_bear",
                       "kicker_bull", "kicker_bear"]
        for pattern in new_patterns:
            assert result.get(pattern, 0.0) == 0.0

    Run: .venv/bin/pytest tests/unit/test_candlestick_patterns.py -xvs
  </action>

  <verify>
    <automated>.venv/bin/pytest tests/unit/test_candlestick_patterns.py -x --tb=short</automated>
  </verify>

  <done>
    - test_candlestick_patterns.py exists with 14+ test functions
    - All tests pass (exit code 0)
    - Coverage includes all 10 new patterns with valid and invalid fixtures
  </done>
</task>

</tasks>

<verification>
1. Schema coverage check: `validate_schema_coverage()` in register_plugins.py passes without errors
2. Plugin outputs count: `candlestick_patterns.py` outputs frozenset has 29 elements
3. All new patterns fire in a historical replay: `.venv/bin/python production/scripts/historical_backfill.py --symbols ES --days 7 --replay-only` then `SELECT pattern_name, COUNT(*) FROM signal_ledger WHERE setup_plugin = 'trad_CandlestickPatternSetup' AND computed_at > NOW() - INTERVAL '7 days' GROUP BY pattern_name` shows new patterns
</verification>

<success_criteria>
- All 10 new candlestick patterns detect their respective formations correctly
- I5Patterns schema declares all 10 new fields with extra='forbid' validation passing
- Unit tests cover valid pattern detection and reject malformed fixtures
- Patterns flow through existing I5 pipeline without breaking schema validation
</success_criteria>

<output>
After completion, create `.planning/phases/42-candlestick-pattern-expansion/42-01-SUMMARY.md` with execution results and any deviations from plan.
</output>
