---
phase: 42-candlestick-pattern-expansion
plan: 04
type: execute
wave: 2
depends_on:
  - 42-01
  - 42-02
  - 42-03
files_modified:
  - src/intelligence/weight_updater.py
  - production/scripts/historical_backfill.py
autonomous: true
requirements:
  - CANDLE-01
  - CANDLE-02

must_haves:
  truths:
    - "weight_updater extends to calibrate pattern_reliability table from signal_ledger outcomes"
    - "Pattern calibration runs per (pattern_name, timeframe) with sample_size >= 30 gate"
    - "Statistical significance computed via proportions z-test (p < 0.05)"
    - "7-day historical backtest validates pattern viability (>= 6 of 7 patterns fire)"
  artifacts:
    - path: src/intelligence/weight_updater.py
      provides: "Extended run_weight_update with pattern calibration"
      contains: "_calibrate_pattern_reliability, run_pattern_calibration"
    - path: pattern_reliability table
      provides: "Updated weights from live outcomes"
      contains: "Non-NULL win_rate, p_value for patterns with sample_size >= 30"
  key_links:
    - from: signal_ledger table
      to: pattern_reliability table
      via: "_calibrate_pattern_reliability UPDATE query"
      pattern: "UPDATE pattern_reliability.*SET base_confidence.*win_rate.*sample_size"
    - from: weight_updater.py
      to: signal_ledger table
      via: "SELECT query for pattern outcomes"
      pattern: "SELECT.*pattern_name.*COUNT.*AVG.*FROM signal_ledger.*WHERE setup_plugin"
---

<objective>
Extend weight_updater for pattern calibration and validate patterns with historical backtest.

Purpose: Close the Renaissance feedback loop — pattern weights self-calibrate from live outcomes, and 7-day backtest confirms all patterns fire in real market conditions.
Output: Extended weight_updater, backtest validation results
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-candlestick-pattern-expansion/42-CONTEXT.md

# Existing weight_updater Structure (from weight_updater.py)

```python
async def run_weight_update(db_manager: Any) -> WeightUpdateResult | None:
    # Query DB for resolved signals
    # Train global model
    # Train per-cluster models
    # Run calibration update (CAL-02)
    return global_result
```

# Signal Ledger Pattern Metadata

CandlestickPatternSetup stores pattern_name in signal_type:
- Format: "candlestick_{pattern_name}_{suffix}"
- Example: "candlestick_harami_bull_long", "candlestick_abandoned_baby_bear_short"
- Parse: split("_") and extract pattern_name (index 1)

# Statistical Test (from promote_shadow.py pattern)

```python
from statsmodels.stats.proportion import proportions_ztest
counts = np.array([wins, losses])
nobs = np.array([n_resolved, n_resolved])
stat, pval = proportions_ztest(counts, nobs)
```

# Validation Strategy (from 42-CONTEXT.md)

Success criteria: >= 6 of 7 new patterns fire at least once in 7-day ES 1m window.
Query: `SELECT pattern_name, COUNT(*) FROM signal_ledger WHERE setup_plugin = 'trad_CandlestickPatternSetup' AND computed_at > NOW() - INTERVAL '7 days' GROUP BY pattern_name`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pattern calibration function to weight_updater.py</name>
  <files>src/intelligence/weight_updater.py</files>

  <read_first>
    - src/intelligence/weight_updater.py (full file)
    - .planning/phases/42-candlestick-pattern-expansion/42-CONTEXT.md (calibration section)
  </read_first>

  <action>
    In src/intelligence/weight_updater.py:

    1. Add import at top of file (after line 32):
       from statsmodels.stats.proportion import proportions_ztest

    2. Add pattern calibration function after _clip_and_renormalize (line 103):

    async def _calibrate_pattern_reliability(
        db_manager: Any,
    ) -> dict[str, Any] | None:
        """Calibrate pattern_reliability table from signal_ledger outcomes.

        Queries resolved CandlestickPatternSetup signals, computes win_rate and
        p_value per (pattern_name, timeframe), updates pattern_reliability when
        sample_size >= 30 and p < 0.05. Sets is_bootstrap=false on promotion.

        Returns:
            Dict with calibration stats or None if no patterns eligible.
        """
        # Query resolved candlestick signals with pattern name extracted
        rows = await db_manager.execute_query("""
            SELECT
                SPLIT_PART(signal_type, '_', 2) AS pattern_name,
                timeframe,
                COUNT(*) AS sample_size,
                AVG(CASE WHEN outcome IN ('target_1', 'target_1_2', 'target_full') THEN 1.0 ELSE 0.0 END) AS win_rate,
                STDDEV(CASE WHEN outcome IN ('target_1', 'target_1_2', 'target_full') THEN 1.0 ELSE 0.0 END) AS win_rate_std
            FROM signal_ledger
            WHERE setup_plugin = 'trad_CandlestickPatternSetup'
              AND outcome IS NOT NULL
              AND is_shadow = FALSE
            GROUP BY pattern_name, timeframe
            HAVING COUNT(*) >= 30
        """)

        if not rows:
            logger.info("Pattern calibration: no patterns with sample_size >= 30")
            return None

        calibration_stats = {}
        for row in rows:
            pattern_name = row["pattern_name"]
            timeframe = row["timeframe"]
            n = row["sample_size"]
            win_rate = float(row["win_rate"])

            # Proportions z-test for statistical significance
            wins = int(round(win_rate * n))
            losses = n - wins
            if wins == 0 or losses == 0:
                p_value = 1.0  # No variance, not significant
            else:
                try:
                    stat, p_value = proportions_ztest(
                        count=np.array([wins, losses]),
                        nobs=np.array([n, n])
                    )
                    p_value = float(p_value)
                except Exception:
                    p_value = 1.0

            # Only update if statistically significant (p < 0.05)
            if p_value < 0.05:
                # Compute IC score (optional placeholder for Phase 46)
                ic_score = None  # TODO: Phase 46 ML analysis

                await db_manager.execute_command("""
                    UPDATE pattern_reliability
                    SET
                        base_confidence = $1,
                        sample_size = $2,
                        win_rate = $3,
                        p_value = $4,
                        ic_score = $5,
                        is_bootstrap = false,
                        last_updated = NOW()
                    WHERE pattern_name = $6 AND timeframe = $7
                """, win_rate, n, win_rate, p_value, ic_score, pattern_name, timeframe)

                calibration_stats[pattern_name] = {
                    "timeframe": timeframe,
                    "sample_size": n,
                    "win_rate": win_rate,
                    "p_value": p_value,
                    "promoted": True
                }
                logger.info(
                    f"Pattern calibration: {pattern_name} ({timeframe}) promoted to "
                    f"data-driven weight: win_rate={win_rate:.3f}, n={n}, p={p_value:.3f}"
                )
            else:
                calibration_stats[pattern_name] = {
                    "timeframe": timeframe,
                    "sample_size": n,
                    "win_rate": win_rate,
                    "p_value": p_value,
                    "promoted": False
                }
                logger.info(
                    f"Pattern calibration: {pattern_name} ({timeframe}) not significant "
                    f"(p={p_value:.3f}, n={n}), keeping bootstrap weight"
                )

        return calibration_stats if calibration_stats else None

    3. Update run_weight_update to call pattern calibration (after line 302, before return):

    # Phase 42: Run pattern reliability calibration
    try:
        pattern_stats = await _calibrate_pattern_reliability(db)
        if pattern_stats:
            promoted_count = sum(1 for s in pattern_stats.values() if s.get("promoted", False))
            logger.info(
                f"Pattern calibration complete: {len(pattern_stats)} patterns analyzed, "
                f"{promoted_count} promoted to data-driven weights"
            )
    except Exception as exc:
        logger.error("Pattern calibration failed", error=str(exc))

    Keep the existing return global_result statement (line 304).
  </action>

  <verify>
    <automated>
      grep -n "_calibrate_pattern_reliability\|pattern_calibration" src/intelligence/weight_updater.py | head -5
    </automated>
  </verify>

  <done>
    - _calibrate_pattern_reliability async function added
    - Queries signal_ledger for pattern outcomes grouped by (pattern_name, timeframe)
    - Computes win_rate and p_value via proportions_ztest
    - Updates pattern_reliability when p < 0.05 and sample_size >= 30
    - Sets is_bootstrap=false on promotion
    - Called from run_weight_update with error handling
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run 7-day historical backtest to validate pattern viability</name>
  <files>
    production/scripts/historical_backfill.py
  </files>

  <read_first>
    - production/scripts/historical_backfill.py (usage pattern)
    - .planning/phases/42-candlestick-pattern-expansion/42-CONTEXT.md (validation strategy section)
  </read_first>

  <action>
    Run a 7-day historical backtest on ES 1m timeframe to validate that all 10 new patterns can fire in real market conditions:

    1. Ensure services are running:
       sudo systemctl status indicagent-market-analysis | grep active
       sudo systemctl status indicagent-signal-generator | grep active

    2. Run backfill for ES symbol, 7 days, replay-only (uses existing seed data):
       .venv/bin/python production/scripts/historical_backfill.py --symbols ES --days 7 --replay-only

    3. Monitor logs for pattern detections:
       journalctl -u indicagent-signal-generator -f --since "1 minute ago" | grep -i "candlestick"

    4. After completion, query signal_ledger for pattern counts:
       docker exec timescaledb psql -U postgres -d indicagent -c "
       SELECT
           SPLIT_PART(signal_type, '_', 2) AS pattern_name,
           COUNT(*) AS fire_count
       FROM signal_ledger
       WHERE setup_plugin = 'trad_CandlestickPatternSetup'
         AND computed_at > NOW() - INTERVAL '7 days'
       GROUP BY pattern_name
       ORDER BY pattern_name;
       "

    Expected result: >= 6 of 10 new patterns show fire_count >= 1
    New patterns to check: harami, abandoned_baby, tweezer, belt_hold, kicker

    5. If fewer than 6 patterns fire:
       - Check logs for pattern detection failures
       - Verify patterns are being read from I5 outputs
       - Confirm trend regime gate not suppressing all signals
       - Consider if 7-day window had unusual market conditions

    Success criteria: At least 6 of 10 new patterns fire in ES 1m over 7 days
  </action>

  <verify>
    <automated>
      docker exec timescaledb psql -U postgres -d indicagent -t -c "
      SELECT COUNT(DISTINCT SPLIT_PART(signal_type, '_', 2))
      FROM signal_ledger
      WHERE setup_plugin = 'trad_CandlestickPatternSetup'
        AND computed_at > NOW() - INTERVAL '7 days'
        AND SPLIT_PART(signal_type, '_', 2) IN ('harami', 'abandoned_baby', 'tweezer', 'belt_hold', 'kicker');
      " 2>&1 | awk '{if($1>=6) print "PASS: "$1" patterns fired"; else print "FAIL: only "$1" patterns fired"}'
    </automated>
  </verify>

  <done>
    - 7-day historical backtest completed without errors
    - Query returns >= 6 distinct new patterns with fire_count >= 1
    - Logs show pattern detections flowing through pipeline
    - If < 6 patterns fire: investigation documented and next steps determined
  </done>

  <what-built>
    Phase 42 implementation complete:
    - 10 new I5 candlestick patterns (harami_bull/bear, abandoned_baby_bull/bear, tweezer_top/bottom, belt_hold_bull/bear, kicker_bull/bear)
    - pattern_reliability table with bootstrap priors
    - CandlestickPatternSetup extended with DB-driven weights
    - weight_updater extended for pattern calibration

    Task 2 validates that all patterns fire in real market conditions.
  </what-built>

  <how-to-verify>
    1. Check query output for pattern counts:
       docker exec timescaledb psql -U postgres -d indicagent -c "
       SELECT
           SPLIT_PART(signal_type, '_', 2) AS pattern_name,
           COUNT(*) AS fire_count
       FROM signal_ledger
       WHERE setup_plugin = 'trad_CandlestickPatternSetup'
         AND computed_at > NOW() - INTERVAL '7 days'
       GROUP BY pattern_name
       ORDER BY pattern_name;
       "

    2. Verify >= 6 new patterns show fire_count >= 1

    3. Check dashboard drill panel for candlestick signals (if live)

    4. Review logs for any pattern detection errors:
       journalctl -u indicant-market-analysis -n 100 | grep -i candlestick
       journalctl -u indicant-signal-generator -n 100 | grep -i candlestick
  </how-to-verify>

  <resume-signal>
    Type "approved" if >= 6 patterns fired, or describe investigation results if fewer patterns fired.
  </resume-signal>

</task>

</tasks>

<verification>
1. Pattern calibration function exists: grep shows _calibrate_pattern_reliability in weight_updater.py
2. Proportions z-test imported: statsmodels.stats.proportion import in weight_updater.py
3. Integration with run_weight_update: pattern calibration called before return
4. Backtest query shows >= 6 patterns fired: COUNT(DISTINCT pattern_name) >= 6 for new patterns
</verification>

<success_criteria>
- weight_updater calibrates pattern_reliability table from signal_ledger outcomes
- Patterns with sample_size >= 30 and p < 0.05 promoted to data-driven weights
- 7-day backtest confirms >= 6 of 10 new patterns fire in ES 1m
- Full feedback loop: patterns fire → outcomes recorded → weights calibrated → future signals use updated weights
</success_criteria>

<output>
After completion, create `.planning/phases/42-candlestick-pattern-expansion/42-04-SUMMARY.md` with calibration results and backtest validation.
Also create phase summary `.planning/phases/42-candlestick-pattern-expansion/42-candlestick-pattern-expansion-SUMMARY.md`.
</output>
