---
phase: 42-candlestick-pattern-expansion
plan: 03
type: execute
wave: 2
depends_on:
  - 42-01
  - 42-02
files_modified:
  - src/intelligence/trading/candlestick_pattern_setup.py
autonomous: true
requirements:
  - CANDLE-02

must_haves:
  truths:
    - "CandlestickPatternSetup reads pattern_weights from frames['pattern_weights'] (injected by service)"
    - "Fallback weights hardcoded in plugin ensure correct behavior when service cache not yet warm"
    - "10 new patterns added to candidate collection with correct base_confidence and direction"
    - "Bootstrap priors used until calibration updates (is_bootstrap=true rows)"
    - "compute_full remains synchronous — no async code in plugin"
  artifacts:
    - path: src/intelligence/trading/candlestick_pattern_setup.py
      provides: "I7 plugin reading pattern weights from frames dict"
      contains: "pattern_weights = frames.get(\"pattern_weights\") or fallback_weights"
  key_links:
    - from: signal_generator_service.py (42-04)
      to: candlestick_pattern_setup.py
      via: "frames['pattern_weights'] dict injected before I7 loop"
      pattern: "frames\\[.pattern_weights.\\]"
    - from: pattern_reliability table
      to: signal_generator_service.py (42-04)
      via: "service loads weights with 15-min cache, passes via frames"
      pattern: "_pattern_reliability_cache"
    - from: candlestick_pattern_setup.py
      to: fallback_weights dict
      via: "used when frames['pattern_weights'] absent or empty"
      pattern: "fallback_weights"
---

<objective>
Extend CandlestickPatternSetup I7 plugin to use service-injected pattern weights and add 10 new patterns.

Purpose: Replace hardcoded base_confidence values with DB-driven weights passed via frames["pattern_weights"],
keeping compute_full synchronous (plugin protocol requirement). The service owns the DB cache (Plan 42-04);
the plugin reads from frames. Bootstrap priors hardcoded as fallback.
Output: Plugin reads injected weights, 10 new patterns integrated with correct priorities and directions
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-candlestick-pattern-expansion/42-CONTEXT.md

# Existing CandlestickPatternSetup (lines 76-128 from candlestick_pattern_setup.py)

Current hardcoded pattern collection:
```python
# CNDL-01: Read I5 pattern flags (no re-detection of raw price)
engulfing_bull = float(features.get("engulfing_bull", 0.0))
# ... (existing 19 patterns)

# Collect directional candidates with priority
candidates = []
if hammer > 0.0:
    candidates.append((0, 1, "hammer", 0.65, True))
# ... (existing patterns with hardcoded base_confidence)
```

# DB Query Pattern (from weight_updater.py)

```python
# Async query pattern (lines 213-217)
existing = await db_manager.execute_query(
    "SELECT MAX(version) as max_v FROM cis_weights WHERE asset_cluster = $1 AND timeframe = $2",
    asset_cluster,
    timeframe,
)

# Cache pattern from signal_generator_service.py (CIS calibration cache)
_cis_scorer: CISScorer | None = None
_cis_calibration_cache_ts: datetime | None = None
_cis_calibration_cache_ttl_sec = 1800  # 30 minutes
```

# Bootstrap Priors (from 42-CONTEXT.md)

Tier 1 (0.70): abandoned_baby_bull/bear, kicker_bull/bear
Tier 2 (0.55-0.60): harami_bull/bear, tweezer_top/bottom, belt_hold_bull/bear
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read injected pattern_weights from frames in CandlestickPatternSetup</name>
  <files>src/intelligence/trading/candlestick_pattern_setup.py</files>

  <read_first>
    - src/intelligence/trading/candlestick_pattern_setup.py (full file)
  </read_first>

  <action>
    In src/intelligence/trading/candlestick_pattern_setup.py compute_full method:

    NOTE: compute_full MUST remain synchronous — the plugin protocol does not support async.
    The service (signal_generator_service) owns the DB cache and injects a preloaded weights
    dict via frames["pattern_weights"] (implemented in Plan 42-04).

    After the existing pattern flag reads (after piercing_line read), add:

    # Get pattern weights injected by service (15-min DB cache), or use fallback
    # fallback_weights match bootstrap priors from 42-02; used when service cache not yet warm
    fallback_weights = {
        "hammer": 0.65,
        "shooting_star": 0.65,
        "engulfing": 0.55,
        "three_white_soldiers": 0.75,
        "three_black_crows": 0.75,
        "pin_bar": 0.45,
        "morning_star": 0.80,
        "evening_star": 0.80,
        "three_inside_up": 0.65,
        "three_inside_down": 0.65,
        "dark_cloud_cover": 0.70,
        "piercing_line": 0.70,
        "harami_cross": 0.60,
        # Phase 42 bootstrap priors
        "abandoned_baby": 0.70,
        "kicker": 0.70,
        "harami": 0.60,
        "tweezer": 0.60,
        "belt_hold": 0.55,
    }
    pattern_weights: dict[str, float] = frames.get("pattern_weights") or fallback_weights

    DO NOT add any async code. DO NOT add DB queries. DO NOT add new class attributes.
    The service injects the weights; the plugin just reads from frames.
  </action>

  <verify>
    <automated>
      grep -n "pattern_weights\|fallback_weights" src/intelligence/trading/candlestick_pattern_setup.py | head -10
    </automated>
  </verify>

  <done>
    - fallback_weights dict defined inside compute_full with all 19 pattern names
    - pattern_weights = frames.get("pattern_weights") or fallback_weights
    - compute_full remains synchronous (no async/await added)
    - No new class attributes or methods added to the plugin
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 10 new patterns to candidate collection with DB-weight lookup</name>
  <files>src/intelligence/trading/candlestick_pattern_setup.py</files>

  <read_first>
    - src/intelligence/trading/candlestick_pattern_setup.py (compute_full method, lines 58-221)
    - .planning/phases/42-candlestick-pattern-expansion/42-CONTEXT.md (pattern priorities section)
  </read_first>

  <action>
    In src/intelligence/trading/candlestick_pattern_setup.py compute_full method:

    1. Add new pattern reads after line 92 (after piercing_line):

    # Phase 42: 10 new candlestick patterns
    harami_bull = float(features.get("harami_bull", 0.0))
    harami_bear = float(features.get("harami_bear", 0.0))
    abandoned_baby_bull = float(features.get("abandoned_baby_bull", 0.0))
    abandoned_baby_bear = float(features.get("abandoned_baby_bear", 0.0))
    tweezer_top = float(features.get("tweezer_top", 0.0))
    tweezer_bottom = float(features.get("tweezer_bottom", 0.0))
    belt_hold_bull = float(features.get("belt_hold_bull", 0.0))
    belt_hold_bear = float(features.get("belt_hold_bear", 0.0))
    kicker_bull = float(features.get("kicker_bull", 0.0))
    kicker_bear = float(features.get("kicker_bear", 0.0))

    2. Replace the existing candidates list (lines 96-128) with pattern_flags + priority_ranks approach.
       Use `pattern_weights` from Task 1 (already in scope):

    # Collect pattern flags first (all 29 patterns)
    pattern_flags = {
        ("hammer", 1): hammer,
        ("shooting_star", -1): shooting_star,
        ("engulfing", 1): engulfing_bull,
        ("engulfing", -1): engulfing_bear,
        ("three_white_soldiers", 1): three_white_soldiers,
        ("three_black_crows", -1): three_black_crows,
        ("pin_bar", 1): pin_bar_bull,
        ("pin_bar", -1): pin_bar_bear,
        ("morning_star", 1): morning_star,
        ("evening_star", -1): evening_star,
        ("three_inside_up", 1): three_inside_up,
        ("three_inside_down", -1): three_inside_down,
        ("dark_cloud_cover", -1): dark_cloud_cover,
        ("piercing_line", 1): piercing_line,
        ("harami_cross", 0): harami_cross,  # direction assigned dynamically
        # Phase 42: 10 new patterns
        ("harami", 1): harami_bull,
        ("harami", -1): harami_bear,
        ("abandoned_baby", 1): abandoned_baby_bull,
        ("abandoned_baby", -1): abandoned_baby_bear,
        ("tweezer", -1): tweezer_top,    # bearish reversal (forms at highs)
        ("tweezer", 1): tweezer_bottom,  # bullish reversal (forms at lows)
        ("belt_hold", 1): belt_hold_bull,
        ("belt_hold", -1): belt_hold_bear,
        ("kicker", 1): kicker_bull,
        ("kicker", -1): kicker_bear,
    }

    # Priority rank mapping (lower = higher priority)
    priority_ranks = {
        "hammer": 0,
        "shooting_star": 0,
        "engulfing": 1,
        "three_white_soldiers": 1,
        "three_black_crows": 1,
        "pin_bar": 2,
        "morning_star": 2,
        "evening_star": 2,
        "three_inside_up": 3,
        "three_inside_down": 3,
        "dark_cloud_cover": 3,
        "piercing_line": 3,
        "harami_cross": 4,
        # Phase 42 priorities
        "abandoned_baby": 1,  # Tier 1 reliability
        "kicker": 1,          # Tier 1 reliability
        "harami": 2,          # Tier 2 reliability
        "tweezer": 3,         # Tier 2 reliability
        "belt_hold": 3,       # Tier 2 reliability
    }

    # Build candidates list using pattern_weights (from Task 1: injected or fallback)
    candidates = []
    for (pattern_name, direction), flag_value in pattern_flags.items():
        if flag_value <= 0.0:
            continue
        priority = priority_ranks.get(pattern_name, 3)
        base_conf = pattern_weights.get(pattern_name, fallback_weights.get(pattern_name, 0.55))
        sr_auto = pattern_name in ("hammer", "shooting_star")
        candidates.append((priority, direction, pattern_name, base_conf, sr_auto))

    3. The rest of compute_full (trend regime gate, volume/S/R confirmation) remains unchanged.
  </action>

  <verify>
    <automated>
      grep -c "harami_bull\|abandoned_baby_bull\|kicker_bull\|tweezer_top\|belt_hold_bull" src/intelligence/trading/candlestick_pattern_setup.py | xargs -I{} test {} -ge 5
    </automated>
  </verify>

  <done>
    - All 10 new patterns read from features dict
    - Candidate collection uses pattern_flags + priority_ranks loop
    - Tweezer directions correct: tweezer_top→(-1,bearish), tweezer_bottom→(1,bullish)
    - pattern_weights variable from Task 1 used for base_conf lookup (no DB/async code here)
    - Pattern names match pattern_reliability table keys
  </done>
</task>

</tasks>

<verification>
1. All 10 new patterns in candidate collection: grep for harami_bull, abandoned_baby, tweezer_top, belt_hold_bull, kicker_bull
2. Priority ranks correct: abandoned_baby and kicker at rank 1, harami at rank 2, tweezer/belt_hold at rank 3
3. Tweezer directions: tweezer_top is direction=-1 (bearish), tweezer_bottom is direction=1 (bullish)
4. No async code: grep "async def\|await" src/intelligence/trading/candlestick_pattern_setup.py returns nothing new
5. plugin loads: python -c "from src.intelligence.trading.candlestick_pattern_setup import plugin; print('OK')"
6. pattern_weights reads from frames: grep 'frames.get("pattern_weights")' confirms injection point
</verification>

<success_criteria>
- compute_full remains synchronous (no async added to plugin)
- CandlestickPatternSetup reads pattern_weights from frames["pattern_weights"] (injected by service in 42-04)
- Fallback weights ensure correct behavior when service cache not yet warm
- 10 new patterns integrated with correct priorities and tweezer directions
- No new class attributes, no DB queries, no cache logic in the plugin
</success_criteria>

<output>
After completion, create `.planning/phases/42-candlestick-pattern-expansion/42-03-SUMMARY.md` with plugin extension results.
</output>
