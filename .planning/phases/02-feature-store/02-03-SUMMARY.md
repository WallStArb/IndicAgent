---
phase: 02-feature-store
plan: 03
subsystem: database
tags: [signal_ledger, ledger_entry, feature_ts, feature_tf, backfill, psycopg2, asyncpg]

# Dependency graph
requires:
  - phase: 02-feature-store/02-01
    provides: signal_ledger feature_ts/feature_tf columns (migration 010)

provides:
  - LedgerEntry dataclass with feature_ts/feature_tf fields (default=None, backward compatible)
  - to_insert_params() returning 24-element tuple with $23/$24 for feature_ts/feature_tf
  - _INSERT_SQL updated with feature_ts/feature_tf in column list and VALUES
  - signal_generator_service.py build_ledger_entries() populates feature_ts=timestamp (event.ts) and feature_tf=timeframe
  - historical_backfill.py _build_ledger_entries() passes feature_ts=None, feature_tf=None
  - historical_backfill.py _INSERT_SYNC_SQL updated with 24 %s placeholders (no param-count mismatch)

affects: [03-feature-writer-service, ml-scoring-model, historical-backfill-runs]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Nullable feature_ts/feature_tf on LedgerEntry: live path populates from IntelligenceEvent.ts/tf; backfill writes NULL — NULL indicates pre-Phase-2 or backfill-generated signal"
    - "24-param INSERT tuple: last two elements are always feature_ts/feature_tf, nullable, appended to end of existing 22 params"

key-files:
  created: []
  modified:
    - src/intelligence/trading/signal_ledger.py
    - services/signal_generator_service.py
    - production/scripts/historical_backfill.py
    - tests/unit/intelligence/test_signal_ledger.py

key-decisions:
  - "feature_ts defaults to None on LedgerEntry — all existing callsites are backward compatible without modification"
  - "Backfill passes NULL for feature_ts/feature_tf (no IntelligenceEvent available during replay) — NULL correctly represents pre-Phase-2 signals"
  - "market_analysis_service.py not touched — it publishes IntelligenceEvent but never constructs LedgerEntry directly"

patterns-established:
  - "Feature linkage pattern: live signals carry feature_ts=event.ts, feature_tf=event.tf for JOIN to intelligence_features; backfill signals carry NULL"

requirements-completed: [FST-03]

# Metrics
duration: 2min
completed: 2026-02-23
---

# Phase 2 Plan 03: Feature Column Wiring Summary

**LedgerEntry extended to 24-param INSERT with feature_ts/feature_tf wired from IntelligenceEvent.ts in live path and NULL in backfill path**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-23T19:07:36Z
- **Completed:** 2026-02-23T19:10:02Z
- **Tasks:** 2
- **Files modified:** 4 (signal_ledger.py, signal_generator_service.py, historical_backfill.py, test_signal_ledger.py)

## Accomplishments
- LedgerEntry dataclass gains `feature_ts: datetime | None = None` and `feature_tf: str | None = None` fields — fully backward compatible with default=None
- `to_insert_params()` now returns a 24-element tuple; `_INSERT_SQL` updated with `feature_ts, feature_tf` columns and `$23, $24` VALUES placeholders
- `build_ledger_entries()` in signal_generator_service.py passes `feature_ts=timestamp` (the IntelligenceEvent bar timestamp) and `feature_tf=timeframe`
- `historical_backfill.py` patched: `_build_ledger_entries()` passes `feature_ts=None, feature_tf=None`; `_INSERT_SYNC_SQL` gains two new `%s` placeholders; `_insert_signals_sync()` appends `e.feature_ts, e.feature_tf` to params tuple — no psycopg2 param-count mismatch
- All 19 relevant tests pass (signal_ledger + historical_backfill unit tests)

## Task Commits

Each task was committed atomically:

1. **Task 1: Update LedgerEntry and _INSERT_SQL** - `05ce68e` (feat)
2. **Task 2: Wire feature_ts/feature_tf in signal_generator and patch backfill** - `ef8420e` (feat)

**Plan metadata:** _(docs commit pending)_

## Files Created/Modified
- `src/intelligence/trading/signal_ledger.py` - Added feature_ts/feature_tf fields; updated to_insert_params() to 24-tuple; updated _INSERT_SQL
- `services/signal_generator_service.py` - Added feature_ts=timestamp, feature_tf=timeframe to LedgerEntry constructor in build_ledger_entries()
- `production/scripts/historical_backfill.py` - Added feature_ts=None/feature_tf=None to LedgerEntry; updated _INSERT_SYNC_SQL; updated _insert_signals_sync() params tuple
- `tests/unit/intelligence/test_signal_ledger.py` - Updated test_to_insert_params to assert 24 params and verify feature_ts/tf default to None

## Decisions Made
- `feature_ts` defaults to `None` on `LedgerEntry` — makes the change backward compatible for all callsites (no ripple updates needed)
- Backfill correctly writes NULL for `feature_ts`/`feature_tf` — signals generated by the backfill replay are semantically different from live signals: they have no IntelligenceEvent at generation time
- `market_analysis_service.py` was intentionally not modified — it publishes IntelligenceEvent to the Redis stream but never constructs LedgerEntry objects

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated test_to_insert_params assertion from 22 to 24 params**
- **Found during:** Task 1 (Update LedgerEntry and _INSERT_SQL)
- **Issue:** Existing test `test_to_insert_params` asserted `len(params) == 22`, which became a false failure after the intentional schema change to 24 params
- **Fix:** Updated assertion to `len(params) == 24` and added checks that params[22] and params[23] are None (feature_ts/feature_tf defaults)
- **Files modified:** tests/unit/intelligence/test_signal_ledger.py
- **Verification:** All 8 signal_ledger tests pass; assertion confirms both the count and the new field defaults
- **Committed in:** `05ce68e` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - test expectation update for intentional behavior change)
**Impact on plan:** Essential correctness fix — the test was encoding the old param count; updated to reflect the new 24-element contract.

## Issues Encountered
None — plan executed cleanly. The pre-existing failures in `test_feature_writer_service.py` (from plan 02-02 TDD red phase) and `test_settings.py`/`test_ibkr_provider.py` are unrelated to this plan's scope and were present before execution.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- signal_ledger is now ready to receive feature_ts/feature_tf on live writes
- historical_backfill.py will not fail with param-count mismatches when Stage 2 runs against the updated DB schema
- Plan 02-02 (feature_writer_service.py) can now complete its implementation (TDD green phase) knowing the signal_ledger schema is stable

---
*Phase: 02-feature-store*
*Completed: 2026-02-23*
