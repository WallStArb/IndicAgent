---
phase: 119-remaining-16-setup-refactoring
plan: "03"
type: execute
wave: 2
depends_on: ["119-01", "119-02"]
requirements: [REFACTOR-08]
files_modified:
  - src/intelligence/plugins/base.py
  - tests/unit/intelligence/test_i6_confluence_enforcement.py
  - tests/unit/intelligence/test_i7_extrinsic_contract.py
autonomous: true
must_haves:
  truths:
    - "`validate_tier()` raises `ArchitectureViolation` for any I7 plugin where `requires_i6_confluence` is falsy (missing OR False), in addition to the existing missing-attribute check."
    - "`registry.validate_tier(TIER_I7, \"I7\")` does NOT raise after Plans 01 and 02 (all 17 plugins are now True)."
    - "A parametrized test asserts `requires_i6_confluence is True` for every plugin in TIER_I7."
    - "A parametrized test asserts `shadow_only is True` for every plugin in TIER_I7."
    - "`test_i7_extrinsic_contract.py` no longer perturbs `ctf_score` for the 17 Phase-119 plugins (ctf_score is now a gate; perturbing it blocks the signal and would yield a false failure)."
    - "`.venv/bin/pytest tests/unit/intelligence/ -q` passes with the full suite green."
---

<objective>
Enforce the Phase 119 invariant in validate_tier() (every I7 plugin must have requires_i6_confluence=True) and update the regression test suite to assert shadow_only=True + requires_i6_confluence=True across all TIER_I7 plugins and to stop perturbing ctf_score (now a gate) for the 17 refactored plugins.
</objective>

<must_haves>
  <truths>
    - `validate_tier()` raises `ArchitectureViolation` for any I7 plugin where `requires_i6_confluence` is falsy (missing OR False), in addition to the existing missing-attribute check.
    - `registry.validate_tier(TIER_I7, "I7")` does NOT raise after Plans 01 and 02 (all 17 plugins are now True).
    - A parametrized test asserts `requires_i6_confluence is True` for every plugin in TIER_I7.
    - A parametrized test asserts `shadow_only is True` for every plugin in TIER_I7.
    - `test_i7_extrinsic_contract.py` no longer perturbs `ctf_score` for the 17 Phase-119 plugins (ctf_score is now a gate; perturbing it blocks the signal and would yield a false failure).
    - `.venv/bin/pytest tests/unit/intelligence/ -q` passes with the full suite green.
  </truths>
</must_haves>

<tasks>

  <task type="auto">
    <name>Add requires_i6_confluence=True enforcement to validate_tier()</name>
    <read_first>
      - src/intelligence/plugins/base.py — validate_tier() I7 block, existing requires_i6_confluence hasattr check at lines 148-153
    </read_first>
    <action>
      In the I7 block of `validate_tier()` in `src/intelligence/plugins/base.py`, immediately AFTER the existing `if not hasattr(plugin, "requires_i6_confluence"): raise ArchitectureViolation(...)` block (ends ~line 153), add a second check that does NOT delete the first:
      `if not getattr(plugin, "requires_i6_confluence", None):` raising `ArchitectureViolation` with message `f"I7 plugin '{name}' must have requires_i6_confluence=True. Phase 119 requires all I7 setups consume I6 cross-timeframe data."`. This catches both a missing attribute and an explicit `False` value. No other lines in validate_tier change.
    </action>
    <acceptance_criteria>
      - `grep -n "getattr(plugin, \"requires_i6_confluence\", None)" src/intelligence/plugins/base.py` returns a match inside the I7 block.
      - `grep -n "must have requires_i6_confluence=True" src/intelligence/plugins/base.py` returns a match.
      - The original `if not hasattr(plugin, "requires_i6_confluence")` block is still present (not deleted).
    </acceptance_criteria>
    <files>src/intelligence/plugins/base.py</files>
    <verify>grep -n 'must have requires_i6_confluence=True' src/intelligence/plugins/base.py</verify>
    <done>validate_tier() raises ArchitectureViolation when requires_i6_confluence is falsy, in addition to the existing missing-attribute check.</done>
  </task>

  <task type="auto">
    <name>Update test_i6_confluence_enforcement.py for the True-only invariant + shadow_only sweep</name>
    <read_first>
      - tests/unit/intelligence/test_i6_confluence_enforcement.py — current tests; test_false_values_have_todo_rationale at lines 44-67 still accepts False values
      - src/intelligence/register_plugins.py — TIER_I7 membership
    </read_first>
    <action>
      Replace `test_false_values_have_todo_rationale` (lines 44-67) with a new parametrized test `test_requires_i6_confluence_true(self, plugin_name)` (decorated `@pytest.mark.parametrize("plugin_name", TIER_I7)`) that asserts `getattr(registry.patterns.get(plugin_name), "requires_i6_confluence", False) is True` with a Phase-119 failure message. Keep `test_requires_i6_confluence_declared` and `test_validate_tier_raises_no_architecture_violation` as-is (they remain valid).

      Add a new parametrized test `test_shadow_only_declared(self, plugin_name)` (decorated `@pytest.mark.parametrize("plugin_name", TIER_I7)`) asserting `getattr(registry.patterns.get(plugin_name), "shadow_only", False) is True` with a message that Phase 119 requires shadow mode until Phase 120 promotes. Use the same `setup_method` registration pattern already in the class.
    </action>
    <acceptance_criteria>
      - `grep -n "test_false_values_have_todo_rationale" tests/unit/intelligence/test_i6_confluence_enforcement.py` returns nothing.
      - `grep -n "def test_requires_i6_confluence_true\|def test_shadow_only_declared" tests/unit/intelligence/test_i6_confluence_enforcement.py` returns both.
      - `.venv/bin/pytest tests/unit/intelligence/test_i6_confluence_enforcement.py -q` passes.
    </acceptance_criteria>
    <files>tests/unit/intelligence/test_i6_confluence_enforcement.py</files>
    <verify>.venv/bin/pytest tests/unit/intelligence/test_i6_confluence_enforcement.py -q</verify>
    <done>test_false_values_have_todo_rationale removed; test_requires_i6_confluence_true and test_shadow_only_declared parametrized over TIER_I7 pass.</done>
  </task>

  <task type="auto">
    <name>Stop perturbing ctf_score for Phase-119 plugins in test_i7_extrinsic_contract.py</name>
    <read_first>
      - tests/unit/intelligence/test_i7_extrinsic_contract.py — _EXTRINSIC_KEYS dict at lines 45-54 includes ctf_score: 0.9; module docstring describes the perturbation invariant
      - .planning/phases/119-remaining-16-setup-refactoring/119-01-SUMMARY.md — any ctf_score-perturbation failures handed off from Plan 01
      - .planning/phases/119-remaining-16-setup-refactoring/119-02-SUMMARY.md — any ctf_score-perturbation failures handed off from Plan 02
    </read_first>
    <action>
      `ctf_score` is now a mandatory gate for the 17 Phase-119 plugins, so perturbing it (setting 0.9 then comparing confidence) is invalid — it either flips the gate result or changes whether the signal fires. Introduce a module-level frozenset `_PHASE_119_PLUGINS` containing the 17 refactored plugin names (the 8 from Plan 01 + the 9 from Plan 02). In the extrinsic-perturbation test, build the per-plugin perturbation dict as `_EXTRINSIC_KEYS` with `ctf_score` EXCLUDED when the plugin under test is in `_PHASE_119_PLUGINS` (e.g. `{k: v for k, v in _EXTRINSIC_KEYS.items() if not (k == "ctf_score" and plugin_name in _PHASE_119_PLUGINS)}`). Leave `ctf_structure_alignment`, `ctf_trend_alignment`, `ctf_regime_agreement`, and the exhaustion keys in the perturbation set — those remain capture-only, not gates. Do NOT weaken the [0.0, 0.95] confidence-range assertion. Update the module docstring to note that ctf_score is a gate (not perturbable) for Phase-119 plugins.

      If either Plan 01 or Plan 02 SUMMARY handed off additional contract failures, address them here (e.g. add firing-scenario factories for any of the 17 plugins not yet covered, ensuring their fixtures supply hmm probabilities >= 0.30 and ctf_score >= 0.30 so the dual gate passes and the plugin fires).
    </action>
    <acceptance_criteria>
      - `grep -n "_PHASE_119_PLUGINS" tests/unit/intelligence/test_i7_extrinsic_contract.py` returns the frozenset definition and its use in the perturbation builder.
      - The frozenset contains all 17 names (8 Wave-1 + 9 Wave-2).
      - `.venv/bin/pytest tests/unit/intelligence/test_i7_extrinsic_contract.py -q` passes.
    </acceptance_criteria>
    <files>tests/unit/intelligence/test_i7_extrinsic_contract.py</files>
    <verify>.venv/bin/pytest tests/unit/intelligence/test_i7_extrinsic_contract.py -q</verify>
    <done>_PHASE_119_PLUGINS frozenset (17 names) excludes ctf_score from perturbation for those plugins; contract test passes.</done>
  </task>

  <task type="auto">
    <name>Full intelligence suite regression run</name>
    <read_first>
      - tests/unit/intelligence/ — directory of affected tests
    </read_first>
    <action>
      Run `.venv/bin/ruff check src/intelligence/plugins/base.py tests/unit/intelligence/ --fix`, then `.venv/bin/black src/intelligence/plugins/base.py tests/unit/intelligence/`, then `.venv/bin/pytest tests/unit/intelligence/ -q`. The full suite must be green: validate_tier enforcement does not raise for the now-compliant 17 plugins, and the contract test no longer perturbs ctf_score for them. Fix any remaining failure at its source (test fixture or plugin code) — do NOT skip or xfail to hide a real gate regression.
    </action>
    <acceptance_criteria>
      - `.venv/bin/ruff check src/intelligence/plugins/base.py tests/unit/intelligence/` exits 0.
      - `.venv/bin/pytest tests/unit/intelligence/ -q` passes with no skips added for Phase-119 plugins.
    </acceptance_criteria>
    <files>(no source edits — regression task)</files>
    <verify>.venv/bin/ruff check src/intelligence/plugins/base.py tests/unit/intelligence/ && .venv/bin/pytest tests/unit/intelligence/ -q</verify>
    <done>Full intelligence unit suite green with no Phase-119 skips/xfails added.</done>
  </task>

</tasks>

<verification>
  - `grep -n "must have requires_i6_confluence=True" src/intelligence/plugins/base.py` returns a match.
  - `grep -n "def test_requires_i6_confluence_true\|def test_shadow_only_declared" tests/unit/intelligence/test_i6_confluence_enforcement.py` returns both.
  - `grep -n "_PHASE_119_PLUGINS" tests/unit/intelligence/test_i7_extrinsic_contract.py` returns matches.
  - `.venv/bin/pytest tests/unit/intelligence/ -q` passes.
</verification>

<output>
After completion, create `.planning/phases/119-remaining-16-setup-refactoring/119-03-SUMMARY.md` documenting the validate_tier change, the test updates, and confirmation that the full intelligence suite is green.
</output>
