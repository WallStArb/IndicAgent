---
phase: 119-remaining-16-setup-refactoring
plan: "03"
type: execute
wave: 2
depends_on: ["119-01", "119-02"]
requirements: [REFACTOR-08]
files_modified:
  - src/intelligence/plugins/base.py
  - src/intelligence/register_plugins.py
  - tests/unit/intelligence/test_i6_confluence_enforcement.py
  - tests/unit/intelligence/test_i7_extrinsic_contract.py
autonomous: true
must_haves:
  truths:
    - "`validate_tier()` raises `ArchitectureViolation` for any NON-EXEMPT I7 plugin where `requires_i6_confluence` is falsy (missing OR False), in addition to the existing missing-attribute check."
    - "A module-level `_I7_I6_EXEMPT` frozenset in register_plugins.py lists exactly the 8 TIER_I7 plugins that are NOT in Phase 118/119 scope and still have `requires_i6_confluence=False`; `validate_tier()` skips the True-enforcement for those names only."
    - "`registry.validate_tier(TIER_I7, \"I7\")` does NOT raise at import/startup after Plans 01 and 02 (the 22 in-scope plugins are True; the 8 exempt ones are skipped)."
    - "A parametrized test asserts `requires_i6_confluence is True` for every TIER_I7 plugin NOT in `_I7_I6_EXEMPT`."
    - "A test asserts `validate_tier()` RAISES `ArchitectureViolation` when a non-exempt I7 plugin is forced to `requires_i6_confluence=False` (proves False is rejected, not just missing)."
    - "A parametrized test asserts `shadow_only is True` for every plugin in `_PHASE_119_PLUGINS` (the 17 refactored plugins)."
    - "A module-level `_PHASE_119_PLUGINS` frozenset (length asserted == 17) excludes `ctf_score` from perturbation in `test_i7_extrinsic_contract.py` for exactly those 17 plugins; `ctf_structure_alignment` and `ctf_trend_alignment` remain perturbed."
    - "The `[0.0, 0.95]` confidence-range assertion is unchanged."
    - "`.venv/bin/pytest tests/unit/intelligence/ -q` passes with the full suite green and no Phase-119 skips/xfails added."
---

<objective>
Enforce the Phase 119 invariant in `validate_tier()` (every NON-EXEMPT I7 plugin must have `requires_i6_confluence=True`) WITHOUT crashing pipeline startup on the 8 TIER_I7 plugins that remain out-of-scope, and update the regression suite to assert the True invariant, prove False is rejected, assert shadow_only over the 17 refactored plugins, and stop perturbing ctf_score (now a gate) for those 17.

CRITICAL SCOPE RECONCILIATION (Codex HIGH blocker, audited 2026-06-10):
`registry.validate_tier(TIER_I7, "I7")` runs over the FULL TIER_I7 list (37 plugins) at pipeline startup (test_i6_confluence_enforcement.py:38-42 and the live pipeline). After Plans 01+02, 22 I7 plugins are `requires_i6_confluence=True` (5 from Phase 118 + 17 from Phase 119). But 8 TIER_I7 plugins are OUT OF SCOPE and still `False`:
  regime_transition, prev_day_level_test, anchored_vwap_reversion, poc_rejection, hvn_rejection, cross_asset_divergence  (6 unrefactored I7 setups)
  mean_reversion, squeeze_expansion  (2 "GOOD setups" per CONTEXT that nonetheless still carry requires_i6_confluence=False)
A blanket `if not getattr(plugin, "requires_i6_confluence", None): raise` would crash startup on all 8. Resolution (the "documented temporary exemption mechanism" Codex recommended; honors D-02's intent for the in-scope set): add a `_I7_I6_EXEMPT` frozenset of these 8 names that `validate_tier()` skips, with a TODO pointing at the follow-up phase that will refactor them. This makes enforcement HARD for the 22 compliant plugins while keeping the system bootable.
</objective>

<must_haves>
  <truths>
    - `validate_tier()` raises `ArchitectureViolation` for any NON-EXEMPT I7 plugin where `requires_i6_confluence` is falsy (missing OR False), in addition to the existing missing-attribute check.
    - A module-level `_I7_I6_EXEMPT` frozenset in register_plugins.py lists exactly the 8 out-of-scope TIER_I7 plugin names; validate_tier() skips True-enforcement for those only.
    - `registry.validate_tier(TIER_I7, "I7")` does NOT raise at import/startup after Plans 01 and 02.
    - A parametrized test asserts `requires_i6_confluence is True` for every TIER_I7 plugin NOT in `_I7_I6_EXEMPT`.
    - A test asserts `validate_tier()` RAISES `ArchitectureViolation` when a non-exempt I7 plugin is forced to `requires_i6_confluence=False`.
    - A parametrized test asserts `shadow_only is True` for every plugin in `_PHASE_119_PLUGINS` (17 plugins).
    - A module-level `_PHASE_119_PLUGINS` frozenset (length asserted == 17) excludes `ctf_score` from perturbation for exactly those 17 plugins; `ctf_structure_alignment`/`ctf_trend_alignment` remain perturbed.
    - The `[0.0, 0.95]` confidence-range assertion is unchanged.
    - `.venv/bin/pytest tests/unit/intelligence/ -q` passes with the full suite green and no Phase-119 skips/xfails added.
  </truths>
</must_haves>

<tasks>

  <task type="auto">
    <name>Add _I7_I6_EXEMPT set + requires_i6_confluence=True enforcement (exempt-aware) to validate_tier()</name>
    <read_first>
      - src/intelligence/plugins/base.py — validate_tier() I7 block; existing requires_i6_confluence hasattr check at lines 148-153
      - src/intelligence/register_plugins.py — TIER_I7 list (lines 610-647); plugin .name attributes for the 8 exempt plugins
    </read_first>
    <action>
      In `src/intelligence/register_plugins.py`, immediately after the `TIER_I7` list definition (after line 647), add a module-level frozenset documenting the temporary exemption:
      `_I7_I6_EXEMPT: frozenset[str]` containing exactly the eight `.name` values for: regime_transition_plugin, prev_day_level_test_plugin, anchored_vwap_reversion_plugin, poc_rejection_plugin, hvn_rejection_plugin, cross_asset_divergence_plugin, mean_revert_plugin, squeeze_exp_plugin. Add a one-line comment (single dash, no em dash): these I7 plugins are not yet I6-integrated; refactor them in a follow-up phase, then delete this set. Build the set from the `_plugin.name` attributes (same pattern as TIER_I7), not hardcoded strings, so a rename cannot silently desync.

      In the I7 block of `validate_tier()` in `src/intelligence/plugins/base.py`, immediately AFTER the existing `if not hasattr(plugin, "requires_i6_confluence"): raise ArchitectureViolation(...)` block (ends ~line 153) and WITHOUT deleting it, add a second check. Import `_I7_I6_EXEMPT` from `..register_plugins` at the top of the check (or pass-through — use a local import inside validate_tier to avoid a circular import, mirroring how the module already avoids cycles). The new check:
      `if name not in _I7_I6_EXEMPT and not getattr(plugin, "requires_i6_confluence", None):` raise `ArchitectureViolation` with message `f"I7 plugin '{name}' must have requires_i6_confluence=True. Phase 119 requires all I7 setups consume I6 cross-timeframe data."`. This catches a missing attribute AND an explicit `False` value for every non-exempt plugin, while the 8 exempt plugins pass. No other lines in validate_tier change.

      Confirm there is no circular-import failure: if importing `_I7_I6_EXEMPT` at module top of base.py creates a cycle (register_plugins imports base), use a function-local `from ..register_plugins import _I7_I6_EXEMPT` inside validate_tier().
    </action>
    <acceptance_criteria>
      - `grep -n "_I7_I6_EXEMPT" src/intelligence/register_plugins.py` shows the frozenset built from `_plugin.name` attributes for the 8 names.
      - `python -c "from src.intelligence.register_plugins import _I7_I6_EXEMPT; print(sorted(_I7_I6_EXEMPT))"` prints exactly 8 names: the trad_ names for RegimeTransition, PrevDayLevelTest, AnchoredVWAPReversion, POCRejection, HVNRejection, CrossAssetDivergence, MeanReversion, SqueezeExpansion.
      - `grep -n "getattr(plugin, \"requires_i6_confluence\", None)" src/intelligence/plugins/base.py` returns a match inside the I7 block, guarded by `name not in _I7_I6_EXEMPT`.
      - `grep -n "must have requires_i6_confluence=True" src/intelligence/plugins/base.py` returns a match.
      - The original `if not hasattr(plugin, "requires_i6_confluence")` block is still present.
      - `python -c "from src.intelligence.plugins import registry; from src.intelligence.register_plugins import TIER_I7, register_all_plugins; register_all_plugins(); registry.validate_tier(TIER_I7, 'I7')"` exits 0 (no raise).
    </acceptance_criteria>
    <files>src/intelligence/register_plugins.py, src/intelligence/plugins/base.py</files>
    <verify>python -c "from src.intelligence.plugins import registry; from src.intelligence.register_plugins import TIER_I7, register_all_plugins; register_all_plugins(); registry.validate_tier(TIER_I7, 'I7'); print('ok')"</verify>
    <done>validate_tier() raises ArchitectureViolation on falsy requires_i6_confluence for non-exempt I7 plugins; the 8 exempt plugins are skipped via _I7_I6_EXEMPT; startup validation passes after Plans 01-02.</done>
  </task>

  <task type="auto">
    <name>Update test_i6_confluence_enforcement.py: True-invariant (exempt-aware), raises-on-False, shadow_only sweep</name>
    <read_first>
      - tests/unit/intelligence/test_i6_confluence_enforcement.py — current tests; test_false_values_have_todo_rationale at lines 44-67
      - src/intelligence/register_plugins.py — TIER_I7, _I7_I6_EXEMPT, _PHASE_119_PLUGINS (if defined here) membership
      - src/intelligence/plugins/base.py — ArchitectureViolation import path
    </read_first>
    <action>
      Replace `test_false_values_have_todo_rationale` (lines 44-67) with a parametrized test `test_requires_i6_confluence_true(self, plugin_name)` decorated `@pytest.mark.parametrize("plugin_name", [n for n in TIER_I7 if n not in _I7_I6_EXEMPT])` that asserts `getattr(registry.patterns.get(plugin_name), "requires_i6_confluence", False) is True` with a Phase-119 failure message. Import `_I7_I6_EXEMPT` from register_plugins. Keep `test_requires_i6_confluence_declared` and `test_validate_tier_raises_no_architecture_violation` as-is.

      Add `test_validate_tier_rejects_false(self)`: pick one NON-EXEMPT I7 plugin (e.g. ofi_continuation_plugin.name), monkeypatch/override its registered instance's `requires_i6_confluence` to `False`, assert `registry.validate_tier(TIER_I7, "I7")` raises `ArchitectureViolation`, then restore. Use the existing setup_method registration. This proves False (not just missing) is rejected.

      Add `test_exempt_plugins_are_known(self)`: assert `_I7_I6_EXEMPT` has length 8 and every member is in TIER_I7 and is registered. This pins the exemption set so it cannot silently grow.

      Add a parametrized `test_shadow_only_declared(self, plugin_name)` decorated `@pytest.mark.parametrize("plugin_name", sorted(_PHASE_119_PLUGINS))` asserting `getattr(registry.patterns.get(plugin_name), "shadow_only", False) is True`. Import `_PHASE_119_PLUGINS` from register_plugins (define it there in Task 1's file if not already present, OR define it in this test module's import block — prefer register_plugins as the single source). Scope the shadow_only sweep to the 17 refactored plugins, NOT all TIER_I7 (the 5 Phase-118 GOOD plugins and exempt plugins may legitimately be non-shadow).
    </action>
    <acceptance_criteria>
      - `grep -n "test_false_values_have_todo_rationale" tests/unit/intelligence/test_i6_confluence_enforcement.py` returns nothing.
      - `grep -n "def test_requires_i6_confluence_true\|def test_validate_tier_rejects_false\|def test_exempt_plugins_are_known\|def test_shadow_only_declared" tests/unit/intelligence/test_i6_confluence_enforcement.py` returns all four.
      - `.venv/bin/pytest tests/unit/intelligence/test_i6_confluence_enforcement.py -q` passes.
    </acceptance_criteria>
    <files>tests/unit/intelligence/test_i6_confluence_enforcement.py</files>
    <verify>.venv/bin/pytest tests/unit/intelligence/test_i6_confluence_enforcement.py -q</verify>
    <done>True-invariant test is exempt-aware; raises-on-False and exempt-set-pinning tests added; shadow_only sweep over the 17 refactored plugins passes.</done>
  </task>

  <task type="auto">
    <name>Stop perturbing ctf_score for the 17 Phase-119 plugins in test_i7_extrinsic_contract.py</name>
    <read_first>
      - tests/unit/intelligence/test_i7_extrinsic_contract.py — _EXTRINSIC_KEYS dict at lines 45-54 includes ctf_score: 0.9; module docstring describes the perturbation invariant
      - src/intelligence/register_plugins.py — _PHASE_119_PLUGINS source of truth (the 17 names)
      - .planning/phases/119-remaining-16-setup-refactoring/119-01-SUMMARY.md — any ctf_score-perturbation failures handed off from Plan 01
      - .planning/phases/119-remaining-16-setup-refactoring/119-02-SUMMARY.md — any ctf_score-perturbation failures handed off from Plan 02
    </read_first>
    <action>
      Define `_PHASE_119_PLUGINS` as the single source of truth (prefer adding it to register_plugins.py built from the 8 Wave-1 + 9 Wave-2 `_plugin.name` attributes, then import it here; if Task 2 already added it there, import it). Assert at module load that `len(_PHASE_119_PLUGINS) == 17`.

      `ctf_score` is now a mandatory gate for the 17 Phase-119 plugins, so perturbing it (setting 0.9 then comparing confidence) flips the gate or changes whether the signal fires. In the extrinsic-perturbation test, build the per-plugin perturbation dict by EXCLUDING only `ctf_score` when the plugin under test is in `_PHASE_119_PLUGINS`: `{k: v for k, v in _EXTRINSIC_KEYS.items() if not (k == "ctf_score" and plugin_name in _PHASE_119_PLUGINS)}`. Keep `ctf_structure_alignment`, `ctf_trend_alignment`, `ctf_regime_agreement`, and the exhaustion keys IN the perturbation set for all plugins (surgical exclusion — only ctf_score, per Codex). Do NOT weaken the `[0.0, 0.95]` confidence-range assertion. Update the module docstring to note that ctf_score is a gate (not perturbable) for Phase-119 plugins.

      If Plan 01 or Plan 02 SUMMARY handed off additional contract failures, address them here: add or fix firing-scenario factories for any of the 17 plugins not yet covered, ensuring their fixtures supply hmm probabilities (hmm_trending_weight / up / down / ranging as relevant) >= 0.30 AND `ctf_score` >= 0.30 so the dual gate passes and the plugin fires. Do not add skips/xfails for Phase-119 plugins.
    </action>
    <acceptance_criteria>
      - `grep -n "_PHASE_119_PLUGINS" tests/unit/intelligence/test_i7_extrinsic_contract.py` returns the import/use and the length assertion.
      - `python -c "from src.intelligence.register_plugins import _PHASE_119_PLUGINS; assert len(_PHASE_119_PLUGINS)==17; print('17 ok')"` prints `17 ok`.
      - `grep -n "ctf_structure_alignment\|ctf_trend_alignment" tests/unit/intelligence/test_i7_extrinsic_contract.py` shows they remain in the perturbation keys (NOT excluded).
      - `grep -n "0.0\|0.95" tests/unit/intelligence/test_i7_extrinsic_contract.py` confirms the range assertion is intact.
      - `.venv/bin/pytest tests/unit/intelligence/test_i7_extrinsic_contract.py -q` passes.
    </acceptance_criteria>
    <files>tests/unit/intelligence/test_i7_extrinsic_contract.py</files>
    <verify>.venv/bin/pytest tests/unit/intelligence/test_i7_extrinsic_contract.py -q</verify>
    <done>_PHASE_119_PLUGINS (17 names, length-asserted) excludes only ctf_score from perturbation for those plugins; ctf_structure/ctf_trend remain perturbed; range assertion intact; contract test passes.</done>
  </task>

  <task type="auto">
    <name>Full intelligence suite regression run</name>
    <read_first>
      - tests/unit/intelligence/ — directory of affected tests
    </read_first>
    <action>
      Run `.venv/bin/ruff check src/intelligence/plugins/base.py src/intelligence/register_plugins.py tests/unit/intelligence/ --fix`, then `.venv/bin/black src/intelligence/plugins/base.py src/intelligence/register_plugins.py tests/unit/intelligence/`, then `.venv/bin/pytest tests/unit/intelligence/ -q`. The full suite must be green: validate_tier enforcement does not raise for the 22 compliant plugins, skips the 8 exempt, and the contract test no longer perturbs ctf_score for the 17. Fix any remaining failure at its source (test fixture or plugin code) — do NOT skip or xfail to hide a real gate regression.
    </action>
    <acceptance_criteria>
      - `.venv/bin/ruff check src/intelligence/plugins/base.py src/intelligence/register_plugins.py tests/unit/intelligence/` exits 0.
      - `.venv/bin/pytest tests/unit/intelligence/ -q` passes with no skips added for Phase-119 plugins.
    </acceptance_criteria>
    <files>(no source edits — regression task)</files>
    <verify>.venv/bin/ruff check src/intelligence/plugins/base.py src/intelligence/register_plugins.py tests/unit/intelligence/ && .venv/bin/pytest tests/unit/intelligence/ -q</verify>
    <done>Full intelligence unit suite green with no Phase-119 skips/xfails added.</done>
  </task>

</tasks>

<verification>
  - `grep -n "_I7_I6_EXEMPT" src/intelligence/register_plugins.py` and `grep -n "must have requires_i6_confluence=True" src/intelligence/plugins/base.py` both return matches.
  - `python -c "from src.intelligence.plugins import registry; from src.intelligence.register_plugins import TIER_I7, register_all_plugins; register_all_plugins(); registry.validate_tier(TIER_I7, 'I7'); print('ok')"` prints ok.
  - `grep -n "def test_requires_i6_confluence_true\|def test_validate_tier_rejects_false\|def test_shadow_only_declared" tests/unit/intelligence/test_i6_confluence_enforcement.py` returns all three.
  - `grep -n "_PHASE_119_PLUGINS" tests/unit/intelligence/test_i7_extrinsic_contract.py` returns matches.
  - `.venv/bin/pytest tests/unit/intelligence/ -q` passes.
</verification>

<output>
After completion, create `.planning/phases/119-remaining-16-setup-refactoring/119-03-SUMMARY.md` documenting the validate_tier change, the `_I7_I6_EXEMPT` set (8 names + rationale), the `_PHASE_119_PLUGINS` set (17 names), the new tests (True-invariant, raises-on-False, exempt-pinning, shadow_only sweep), the surgical ctf_score perturbation exclusion, and confirmation that the full intelligence suite is green and the pipeline imports/validates without raising.
</output>
