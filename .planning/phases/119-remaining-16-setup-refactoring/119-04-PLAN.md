---
phase: 119-remaining-16-setup-refactoring
plan: "04"
type: execute
wave: 2
depends_on: ["119-01", "119-02"]
requirements: [REFACTOR-08]
files_modified:
  - docs/architecture/i7-setup-confidence-patterns.md
  - CLAUDE.md
  - src/intelligence/CLAUDE.md
autonomous: true
must_haves:
  truths:
    - "A new doc `docs/architecture/i7-setup-confidence-patterns.md` exists with verified `current` status and recipe-card format, enumerating all 6 GOOD patterns with the canonical code structure and the D-01/D-03 gate thresholds."
    - "The doc names `ofi_continuation.py` as the canonical reference implementation and lists the 22 compliant I7 setups (5 from Phase 118 + 17 from Phase 119)."
    - "The doc states the exact gate thresholds: `_MIN_REGIME_WEIGHT = 0.30`, `_MIN_CTF_SCORE = 0.25`, z-score gate `>= 2.0`, and the `hmm_trending_weight` rule for `regime_type=\"any\"` plugins."
    - "CLAUDE.md Plugin System section links to the new doc and states that all I7 setups must follow the 6 GOOD patterns + that validate_tier() enforces requires_i6_confluence=True."
    - "src/intelligence/CLAUDE.md \"Creating a New I7 Plugin\" section references the new doc."
    - "No fabricated claims: every code reference (line ranges, function names, constants) in the doc is verified against the source files."
---

<objective>
Document the 6 GOOD patterns as a canonical architecture reference and link it from CLAUDE.md and src/intelligence/CLAUDE.md, so future I7 setups are authored compliant-by-default rather than rediscovering the pattern.
</objective>

<must_haves>
  <truths>
    - A new doc `docs/architecture/i7-setup-confidence-patterns.md` exists with verified `current` status and recipe-card format, enumerating all 6 GOOD patterns with the canonical code structure and the D-01/D-03 gate thresholds.
    - The doc names `ofi_continuation.py` as the canonical reference implementation and lists the 22 compliant I7 setups (5 from Phase 118 + 17 from Phase 119).
    - The doc states the exact gate thresholds: `_MIN_REGIME_WEIGHT = 0.30`, `_MIN_CTF_SCORE = 0.25`, z-score gate `>= 2.0`, and the `hmm_trending_weight` rule for `regime_type="any"` plugins.
    - CLAUDE.md Plugin System section links to the new doc and states that all I7 setups must follow the 6 GOOD patterns + that validate_tier() enforces requires_i6_confluence=True.
    - src/intelligence/CLAUDE.md "Creating a New I7 Plugin" section references the new doc.
    - No fabricated claims: every code reference (line ranges, function names, constants) in the doc is verified against the source files.
  </truths>
</must_haves>

<tasks>

  <task type="auto">
    <name>Author docs/architecture/i7-setup-confidence-patterns.md</name>
    <read_first>
      - docs/foundation/documentation-system.md — recipe-card format + verified-status convention
      - src/intelligence/trading/ofi_continuation.py — canonical reference; cite its actual ClassVar lines + 4-factor composite
      - .planning/phases/119-remaining-16-setup-refactoring/119-CONTEXT.md — D-01, D-02, D-03, D-04 threshold decisions
      - .planning/phases/119-remaining-16-setup-refactoring/119-01-SUMMARY.md — per-plugin 4-factor weights chosen in Wave 1
      - .planning/phases/119-remaining-16-setup-refactoring/119-02-SUMMARY.md — per-plugin weights chosen in Wave 2
    </read_first>
    <action>
      Create `docs/architecture/i7-setup-confidence-patterns.md` following the recipe-card format from `docs/foundation/documentation-system.md`, with a status line marking it `current` and dated 2026-06-10. Sections:
        1. Purpose — why I7 confidence integrity matters (SoC: confidence = intrinsic signal strength; regime + I6 = eligibility gates).
        2. The 6 GOOD Patterns — enumerate each with its concrete code shape: (1) multi-factor intrinsic confidence (4 named clamp01 factors, weights summing to 1.0, wrapped in compose_confidence); (2) requires_i6_confluence=True; (3) strict dual gate before OHLCV extraction; (4) continuous hmm_regime_weight (not binary); (5) early-gate optimization (cheap checks before extract_ohlcv); (6) shadow_only=True until Phase 120 promotion.
        3. Gate thresholds — `_MIN_REGIME_WEIGHT = 0.30`, `_MIN_CTF_SCORE = 0.25`, z-score features gate at `>= 2.0`; and the rule that `regime_type="any"` plugins MUST use `hmm_trending_weight(features)` because `hmm_regime_weight(features, "any")` returns the 0.5 neutral fallback.
        4. Canonical reference — point to `ofi_continuation.py` (cite its real `shadow_only`/`requires_i6_confluence` ClassVar lines and the 4-factor composite lines after verifying them).
        5. Compliant setups — table of the 22 compliant I7 setups: the 5 from Phase 118 (OFIContinuation, PatternCompletion, GapAnalysisSetup, CVDDivergence, DivergenceStack) plus the 17 from Phase 119, noting MomentumBreakout keeps a 3-factor formula per D-04.
        6. Enforcement — validate_tier() raises ArchitectureViolation if requires_i6_confluence is not True; shadow_only is verified by code review + the test sweep (truth lives in the shadow_registry DB table, per D-02).
      Do not invent line numbers — open the source and cite verified ranges, or omit line numbers if uncertain.
    </action>
    <acceptance_criteria>
      - `test -f docs/architecture/i7-setup-confidence-patterns.md` succeeds.
      - `grep -n "current" docs/architecture/i7-setup-confidence-patterns.md` shows a status line.
      - `grep -n "_MIN_REGIME_WEIGHT\|_MIN_CTF_SCORE\|hmm_trending_weight\|0.30\|2.0" docs/architecture/i7-setup-confidence-patterns.md` shows the thresholds and the any-regime rule.
      - `grep -n "ofi_continuation" docs/architecture/i7-setup-confidence-patterns.md` shows the canonical reference.
      - The compliant-setups table lists all 17 Phase-119 plugin names plus the 5 Phase-118 plugins.
    </acceptance_criteria>
    <files>docs/architecture/i7-setup-confidence-patterns.md</files>
    <verify>test -f docs/architecture/i7-setup-confidence-patterns.md && grep -q 'current' docs/architecture/i7-setup-confidence-patterns.md</verify>
    <done>New doc exists with current status, recipe-card format, the 6 GOOD patterns, gate thresholds, canonical reference, and the 22-setup compliance table.</done>
  </task>

  <task type="auto">
    <name>Link the new doc from CLAUDE.md and src/intelligence/CLAUDE.md</name>
    <read_first>
      - CLAUDE.md — Plugin System section (lines ~117-123); Shadow governance bullet; I6→I7 confluence bullet
      - src/intelligence/CLAUDE.md — "Creating a New I7 Plugin" subsection
    </read_first>
    <action>
      In CLAUDE.md Plugin System section, add one bullet: I7 setup confidence integrity — all I7 setups follow the 6 GOOD patterns (4-factor intrinsic confidence, dual regime+I6 gate before OHLCV, shadow_only=True), enforced by `validate_tier()` which raises ArchitectureViolation unless `requires_i6_confluence=True`. Link `docs/architecture/i7-setup-confidence-patterns.md`. Keep the existing "I6→I7 confluence" and "Shadow governance" bullets — add, do not replace. Use a single dash, no em dash.

      In `src/intelligence/CLAUDE.md`, under "Creating a New I7 Plugin", add a reference line pointing to `docs/architecture/i7-setup-confidence-patterns.md` as the required pattern guide (4-factor confidence + dual gate + shadow_only), so new plugins are compliant by default.
    </action>
    <acceptance_criteria>
      - `grep -n "i7-setup-confidence-patterns.md" CLAUDE.md src/intelligence/CLAUDE.md` returns a match in both files.
      - `grep -n "6 GOOD\|four-factor\|4-factor\|dual.*gate" CLAUDE.md` returns a match in the Plugin System section.
      - `grep -c "—" CLAUDE.md` does not increase versus the pre-edit count (no em dash introduced) — verify the added lines use single dashes only.
    </acceptance_criteria>
    <files>CLAUDE.md, src/intelligence/CLAUDE.md</files>
    <verify>grep -rn 'i7-setup-confidence-patterns.md' CLAUDE.md src/intelligence/CLAUDE.md</verify>
    <done>Both CLAUDE.md and src/intelligence/CLAUDE.md link the new doc; CLAUDE.md Plugin System section states the 6 GOOD patterns + validate_tier enforcement; no em dash introduced.</done>
  </task>

</tasks>

<verification>
  - `test -f docs/architecture/i7-setup-confidence-patterns.md && grep -q "current" docs/architecture/i7-setup-confidence-patterns.md` succeeds.
  - `grep -rn "i7-setup-confidence-patterns.md" CLAUDE.md src/intelligence/CLAUDE.md` shows links in both.
  - `grep -n "_MIN_CTF_SCORE\|_MIN_REGIME_WEIGHT\|hmm_trending_weight" docs/architecture/i7-setup-confidence-patterns.md` confirms the gate thresholds are documented.
</verification>

<output>
After completion, create `.planning/phases/119-remaining-16-setup-refactoring/119-04-SUMMARY.md` documenting the new doc path, the CLAUDE.md links added, and confirmation that all cited line/function references were verified against source.
</output>
