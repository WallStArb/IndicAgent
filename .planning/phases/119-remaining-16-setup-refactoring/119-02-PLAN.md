---
phase: 119-remaining-16-setup-refactoring
plan: "02"
type: execute
wave: 2
depends_on: ["119-01"]
requirements: [REFACTOR-06, REFACTOR-07]
files_modified:
  - src/intelligence/trading/lvn_breakout.py
  - src/intelligence/trading/orb15.py
  - src/intelligence/trading/orb30.py
  - src/intelligence/trading/second_leg_continuation.py
  - src/intelligence/trading/vcp.py
  - src/intelligence/trading/vwap_reclaim.py
  - src/intelligence/trading/dual_divergence.py
  - src/intelligence/trading/vwap_deviation.py
  - src/intelligence/trading/momentum_breakout.py
autonomous: true
must_haves:
  truths:
    - "All 9 Wave-2 plugin classes declare `shadow_only: bool = True` as an explicit ClassVar."
    - "All 9 Wave-2 plugin classes declare `requires_i6_confluence: bool = True` (no `= False`, no `# TODO`)."
    - "Every binary/integer regime equality check (`hmm_regime in (1, 2)`, `hmm_regime not in (1.0, 2.0)`, `hmm_regime_prob < 0.60`) is replaced by a continuous `hmm_regime_weight()`/`hmm_trending_weight()` gate against `_MIN_REGIME_WEIGHT = 0.30`."
    - "Every Wave-2 plugin runs the regime gate AND I6 ctf_score gate (`abs(ctf_score) < _MIN_CTF_SCORE`, `_MIN_CTF_SCORE = 0.25`) BEFORE any `extract_ohlcv()`/`df[\"close\"]`/ATR access."
    - "VWAPDeviation and MomentumBreakout have their `extract_ohlcv()` call moved to AFTER the dual gate (no OHLCV extraction at the top of compute_full)."
    - "LVNBreakout and VWAPReclaim retain their existing 4-factor composite confidence; VWAPDeviation retains its existing 3-factor composite confidence; MomentumBreakout retains its existing 3-factor confidence. No confidence rewrite for these four — only gates/ClassVars change."
    - "ORB15, ORB30, SecondLegContinuation, VCP, DualDivergence have new 4-factor `clamp01`-bounded composite confidence whose weights sum to 1.0, with NO HMM probability used as a confidence factor (HMM is gate-only)."
    - "DualDivergence (`regime_type=\"mean_reversion\"`) gates regime via `hmm_regime_weight(features, \"ranging\")`."
    - "`.venv/bin/pytest tests/unit/intelligence/ -q` passes."
---

<objective>
Apply the 6 GOOD patterns to the 9 Wave-2 I7 setups (LVNBreakout, ORB15, ORB30, SecondLegContinuation, VCP, VWAPReclaim, DualDivergence, VWAPDeviation, MomentumBreakout): replace binary regime checks with continuous hmm_regime_weight gates, add the I6 ctf_score gate before OHLCV extraction, and bring single-factor confidence formulas up to 4-factor composites.

Review-resolved facts (audited 2026-06-10, baked into this plan — these supersede any conflicting RESEARCH/CONTEXT text):
- VWAPDeviation already has a CORRECT 3-factor composite (`0.40*dev_score + 0.35*regime_compat + 0.25*vol_contraction`, vwap_deviation.py:128). It is KEPT as-is (3 factors). The earlier "4-factor / already compliant" wording was imprecise — there is no contradiction: keep its existing 3-factor formula, change only gates + ClassVars. (Resolves Codex HIGH "VWAPDeviation factor count".) VWAPDeviation also calls `apply_exhaustion_boost` (line 139) — that line is PRESERVED unchanged.
- DualDivergence `regime_type = "mean_reversion"` (dual_divergence.py:63). Regime gate is `hmm_regime_weight(features, "ranging")`. (Resolves Codex MEDIUM "DualDivergence regime unresolved".)
- VCP exposes only `hmm_regime` / `hmm_regime_prob` as HMM features but also rich NON-HMM intrinsic features: `contraction_count`, expansion `bar_range`, `bar_volume` vs `last_vol`, and breakout margin `(close_price - prior_high)/atr`. The 4th confidence factor MUST come from these non-HMM features, never from `hmm_regime_prob`. (Resolves Codex/consensus MEDIUM "VCP HMM-as-confidence-factor".)
- LVNBreakout `trend_clarity` already uses `hmm_probability` as an intrinsic confidence factor (lvn_breakout.py:173). LVNBreakout is in the "keep existing 4-factor formula" set, so this stays — but it is the ONLY plugin in this plan permitted an HMM-derived confidence factor, because that factor predates Phase 119 and the plan does NOT rewrite its confidence. New 4-factor formulas (ORB/SecondLeg/VCP/DualDivergence) MUST NOT introduce HMM into confidence.
</objective>

<must_haves>
  <truths>
    - All 9 Wave-2 plugin classes declare `shadow_only: bool = True` as an explicit ClassVar.
    - All 9 Wave-2 plugin classes declare `requires_i6_confluence: bool = True` (no `= False`, no `# TODO`).
    - Every binary/integer regime equality check (`hmm_regime in (1, 2)`, `hmm_regime not in (1.0, 2.0)`, `hmm_regime_prob < 0.60`) is replaced by a continuous `hmm_regime_weight()`/`hmm_trending_weight()` gate against `_MIN_REGIME_WEIGHT = 0.30`.
    - Every Wave-2 plugin runs the regime gate AND I6 ctf_score gate (`abs(ctf_score) < _MIN_CTF_SCORE`, `_MIN_CTF_SCORE = 0.25`) BEFORE any `extract_ohlcv()`/`df["close"]`/ATR access.
    - VWAPDeviation and MomentumBreakout have their `extract_ohlcv()` call moved to AFTER the dual gate (no OHLCV extraction at the top of compute_full).
    - LVNBreakout and VWAPReclaim retain their existing 4-factor composite confidence; VWAPDeviation retains its existing 3-factor composite; MomentumBreakout retains its existing 3-factor formula. No confidence rewrite for these four.
    - ORB15, ORB30, SecondLegContinuation, VCP, DualDivergence have new 4-factor `clamp01`-bounded composite confidence whose weights sum to 1.0, with NO HMM probability used as a confidence factor.
    - DualDivergence (`regime_type="mean_reversion"`) gates regime via `hmm_regime_weight(features, "ranging")`.
    - `.venv/bin/pytest tests/unit/intelligence/ -q` passes.
  </truths>
</must_haves>

<tasks>

  <task type="auto">
    <name>Batch F/G/J — gates-only plugins (no confidence rewrite): LVNBreakout, VWAPReclaim, VWAPDeviation, MomentumBreakout</name>
    <read_first>
      - src/intelligence/trading/lvn_breakout.py — binary `hmm in (1,2)` gate (~lines 84-91); 4-factor confidence (~lines 186-188); trend_clarity uses hmm_probability (~line 173)
      - src/intelligence/trading/vwap_reclaim.py — binary trend_align scoring; close = df["close"].to_numpy() at ~line 97; 4-factor confidence (~lines 189-195); regime_type="any"
      - src/intelligence/trading/vwap_deviation.py — extract_ohlcv FIRST at ~line 60; EXISTING 3-factor composite at line 128; apply_exhaustion_boost at line 139; regime_type="mean_reversion"
      - src/intelligence/trading/momentum_breakout.py — extract_ohlcv FIRST at ~line 58; 3-factor confidence to KEEP per D-04; regime_type="trend"
      - src/intelligence/trading/ofi_continuation.py — reference ClassVar block + features-then-gate-then-OHLCV ordering
    </read_first>
    <action>
      For EACH of the four files: ensure imports `hmm_regime_weight, hmm_trending_weight` and `clamp01`. Add module-level `_MIN_REGIME_WEIGHT: float = 0.30` and `_MIN_CTF_SCORE: float = 0.25`. Add `shadow_only: bool = True` ClassVar and change `requires_i6_confluence: bool = False  # TODO...` to `requires_i6_confluence: bool = True`.

      LVNBreakout (`regime_type="trend"`): DELETE the binary regime block (`hmm = features.get("hmm_regime"); if hmm is None: return no_signal(); hmm = int(hmm); if hmm not in (1, 2): return no_signal()`). Replace with the continuous dual gate before any `df`/volume numeric access: Gate 1 `if hmm_regime_weight(features, "up") < _MIN_REGIME_WEIGHT and hmm_regime_weight(features, "down") < _MIN_REGIME_WEIGHT: return no_signal()`; Gate 2 ctf_score. Keep the 4-factor confidence (`0.30*vol_score + 0.25*trend_clarity + 0.25*lvn_inverse + 0.20*close_strength`) UNCHANGED — `trend_clarity` using `hmm_probability` is a pre-existing intrinsic confidence factor and stays (this plugin is in the no-rewrite set).

      VWAPReclaim (`regime_type="any"`): insert the dual gate (Gate 1 `if hmm_trending_weight(features) < _MIN_REGIME_WEIGHT: return no_signal()`, Gate 2 ctf_score) AFTER the session_vwap null check and BEFORE `close = df["close"].to_numpy()` at ~line 97. Keep the 4-factor confidence including its `trend_align` factor UNCHANGED.

      VWAPDeviation (`regime_type="mean_reversion"`): REORDER — currently `result = extract_ohlcv(frames, self.min_lookback)` is the first statement. Change the top to `df = frames.get("main"); if df is None or len(df) < self.min_lookback: return no_signal()`, then assemble the `features` dict, then the dual gate (Gate 1 `if hmm_regime_weight(features, "ranging") < _MIN_REGIME_WEIGHT: return no_signal()`; Gate 2 ctf_score), THEN call `extract_ohlcv(frames, self.min_lookback)`. Preserve the existing `garch_vol_regime`/`vwap_std` domain gates, the existing 3-factor confidence at line 128 (DO NOT add a 4th factor), and the existing `apply_exhaustion_boost` call at line 139.

      MomentumBreakout (`regime_type="trend"`): same REORDER pattern as VWAPDeviation — move `extract_ohlcv` (line ~58) to after the dual gate. Gate 1 uses the up/down dual `hmm_regime_weight` form; Gate 2 ctf_score. Confidence formula is UNCHANGED (D-04): keep `0.40*roc_score + 0.35*vol_score + 0.25*break_margin`.
    </action>
    <acceptance_criteria>
      - `grep -n "hmm not in\|hmm in (1\|int(hmm)" src/intelligence/trading/lvn_breakout.py` returns nothing (binary check removed).
      - `grep -n "shadow_only: bool = True\|requires_i6_confluence: bool = True" src/intelligence/trading/lvn_breakout.py src/intelligence/trading/vwap_reclaim.py src/intelligence/trading/vwap_deviation.py src/intelligence/trading/momentum_breakout.py` shows both ClassVars in all four.
      - In vwap_deviation.py and momentum_breakout.py the line number of `_MIN_CTF_SCORE` gate is LESS than the line number of `extract_ohlcv`.
      - `grep -n "0.40 \* dev_score\|dev_score" src/intelligence/trading/vwap_deviation.py` confirms the 3-factor formula is still present and `grep -n "apply_exhaustion_boost" src/intelligence/trading/vwap_deviation.py` still returns a match (preserved).
      - `grep -n "roc_score" src/intelligence/trading/momentum_breakout.py` confirms the 3-factor formula is still present (unchanged).
      - `grep -n "features.get(\"ctf_score\")\|abs(ctf_score)" {all four files}` shows the I6 gate in each.
    </acceptance_criteria>
    <files>src/intelligence/trading/lvn_breakout.py, src/intelligence/trading/vwap_reclaim.py, src/intelligence/trading/vwap_deviation.py, src/intelligence/trading/momentum_breakout.py</files>
    <verify>.venv/bin/pytest tests/unit/intelligence/ -q</verify>
    <done>All four declare shadow_only=True and requires_i6_confluence=True; LVNBreakout binary regime check removed; VWAPDeviation and MomentumBreakout extract OHLCV after the dual gate; VWAPDeviation keeps 3-factor + exhaustion boost; all four existing confidence formulas unchanged.</done>
  </task>

  <task type="auto">
    <name>Batch B/H — confidence rewrite + gate replacement (NO HMM in confidence): ORB15, ORB30, SecondLegContinuation, VCP</name>
    <read_first>
      - src/intelligence/trading/orb15.py — single-factor `0.50 + gap_boost`; df.close before regime check; regime_type="trend"
      - src/intelligence/trading/orb30.py — mirrors ORB15
      - src/intelligence/trading/second_leg_continuation.py — binary `hmm not in (1.0, 2.0)` gate; partially intrinsic confidence; regime_type="trend"
      - src/intelligence/trading/vcp.py — binary `hmm not in (1.0,2.0)` (line 111) + `hmm_regime_prob < _MIN_HMM_PROB` (line 116); confidence at lines 205-209 uses hmm_regime_prob; non-HMM features available: contraction_count, bar_range, bar_volume/last_vol, (close_price-prior_high)/atr; regime_type="trend"
      - src/intelligence/trading/ofi_continuation.py — reference 4-factor composite + ordering
    </read_first>
    <action>
      For EACH of the four files (all `regime_type="trend"`): ensure imports `hmm_regime_weight, hmm_trending_weight` and `clamp01`; add `_MIN_REGIME_WEIGHT: float = 0.30` and `_MIN_CTF_SCORE: float = 0.25`; add `shadow_only: bool = True`; change `requires_i6_confluence: bool = False  # TODO...` to `= True`.

      ORB15 and ORB30: REORDER so features-dict + dual gate run before `df["close"]` access. Gate 1 (trend): `if hmm_regime_weight(features, "up") < _MIN_REGIME_WEIGHT and hmm_regime_weight(features, "down") < _MIN_REGIME_WEIGHT: return no_signal()`. Gate 2 ctf_score. Replace `0.50 + gap_boost` single-factor with a 4-factor `clamp01` composite (all NON-HMM): `breakout_margin_score` (`(close - orb_high)/atr` or `(orb_low - close)/atr` by direction, clamped), `range_quality_score` (tighter opening range = cleaner; `clamp01(1.0 - range_width/atr)` form), `volume_score` (rel_volume expansion), `gap_alignment_score` (existing gap signal, bounded with clamp01 — do not double-count CTF or regime). Weights `0.35, 0.25, 0.25, 0.15` summing to 1.0, wrapped in `compose_confidence()`. ORB30 mirrors ORB15 exactly with its own range constants.

      SecondLegContinuation: DELETE the binary `hmm not in (1.0, 2.0)` gate. Replace with continuous dual gate (up/down `hmm_regime_weight` form + ctf_score). Build a 4-factor confidence from NON-HMM features: `leg_quality_score`, `momentum_persistence_score`, `volume_alignment_score`, `structure_quality_score` (weights summing to 1.0). The existing intrinsic `hmm_probability` confidence bonus MUST be removed from the confidence formula — HMM is now gate-only. No binary string/int regime comparison may remain.

      VCP: DELETE the binary `hmm not in (1.0,2.0)` (line 111) AND the `hmm_regime_prob < _MIN_HMM_PROB` gate (line 116). Replace with the continuous dual gate (up/down `hmm_regime_weight` form + ctf_score), placed before `df["close"].to_numpy()` (line 121). Build a 4-factor confidence from NON-HMM intrinsic features only: `contraction_quality_score` (`clamp01((contraction_count - _MIN_CONTRACTIONS) / 4.0)`), `volume_expansion_score` (`clamp01((bar_volume / max(last_vol, 1e-9) - 1.0) / 1.0)`), `breakout_margin_score` (`clamp01(abs(close_price - prior_high)/atr)` long / `(prior_low - close_price)/atr` short), `range_compression_score` (`clamp01(1.0 - bar_range/atr)`). Weights e.g. `0.30, 0.25, 0.25, 0.20` summing to 1.0, wrapped in `compose_confidence()`. The `0.50 + 0.08 + (hmm_regime_prob>0.75 ? 0.07)` formula is fully removed; `hmm_regime_prob` MUST NOT appear in the new confidence. Preserve the existing `apply_exhaustion_guard` call (line 218) and direction-from-HMM logic for SIGNAL DIRECTION only (direction is allowed to read hmm_regime; confidence is not).
    </action>
    <acceptance_criteria>
      - `grep -n "not in (1.0\|not in (1,\|in (1, 2)\|hmm_regime_prob <" src/intelligence/trading/second_leg_continuation.py src/intelligence/trading/vcp.py` returns nothing.
      - `grep -n "shadow_only: bool = True\|requires_i6_confluence: bool = True" src/intelligence/trading/orb15.py src/intelligence/trading/orb30.py src/intelligence/trading/second_leg_continuation.py src/intelligence/trading/vcp.py` shows both in all four.
      - In orb15.py and orb30.py the dual-gate (`_MIN_CTF_SCORE`) line precedes the first `df["close"]`/`.to_numpy(`/`extract_ohlcv` line; same check for vcp.py.
      - In the NEW confidence formula of vcp.py and second_leg_continuation.py, `grep -n "hmm_regime_prob\|hmm_probability" {file}` shows no occurrence inside the confidence composition (HMM may appear only in the gate and in direction selection).
      - Each of the four files shows exactly 4 named `_score` factor variables and a composite whose weights sum to 1.0.
      - `grep -n "0.50 + " src/intelligence/trading/orb15.py src/intelligence/trading/orb30.py` returns nothing (single-factor formula gone).
    </acceptance_criteria>
    <files>src/intelligence/trading/orb15.py, src/intelligence/trading/orb30.py, src/intelligence/trading/second_leg_continuation.py, src/intelligence/trading/vcp.py</files>
    <verify>.venv/bin/pytest tests/unit/intelligence/ -q</verify>
    <done>All four declare shadow_only=True and requires_i6_confluence=True; binary regime gates replaced with continuous hmm gates; each has a new 4-factor composite summing to 1.0 with NO HMM probability in the confidence.</done>
  </task>

  <task type="auto">
    <name>Batch I — DualDivergence (additive magnitudes -> 4-factor, ranging gate) + Wave-2 mechanical audit + verification</name>
    <read_first>
      - src/intelligence/trading/dual_divergence.py — additive `0.60 + abs(ofi_div)*0.05 + abs(cvd_div)*0.05`; regime_type="mean_reversion" (line 63)
      - src/intelligence/trading/ofi_divergence.py — sibling pattern from Plan 01 (analog for divergence confidence factors)
      - tests/unit/intelligence/test_i7_extrinsic_contract.py — DO NOT modify (Plan 03 owns ctf_score exclusion)
    </read_first>
    <action>
      DualDivergence (`regime_type="mean_reversion"`): ensure imports `hmm_regime_weight` and `clamp01`; add `_MIN_REGIME_WEIGHT: float = 0.30`, `_MIN_CTF_SCORE: float = 0.25`; add `shadow_only: bool = True`; change `requires_i6_confluence: bool = False  # TODO...` to `= True`. Gate 1: `if hmm_regime_weight(features, "ranging") < _MIN_REGIME_WEIGHT: return no_signal()` (mean_reversion uses the ranging probability — resolved, do not use hmm_trending_weight). Gate 2: ctf_score. Both gates run after features-dict assembly and before any `df` numeric access (this plugin is features-first today; verify and place the gate before the first numeric `df` access).

      Replace the additive `0.60 + abs(ofi_div)*0.05 + abs(cvd_div)*0.05` with a 4-factor `clamp01` composite (NO HMM): `ofi_divergence_score` (`clamp01(math.tanh(abs(ofi_div) / 3.0))`), `cvd_divergence_score` (`clamp01(math.tanh(abs(cvd_div) / 3.0))`), `confirmation_bars_score` (persistence/count factor), `volume_score` (`features.get("rel_volume")`). Weights `0.35, 0.30, 0.20, 0.15` summing to 1.0, wrapped in `compose_confidence()`.

      MECHANICAL AUDIT (Codex/Ollama HIGH): for all 9 Wave-2 plugins, confirm via `grep -n` that the `_MIN_CTF_SCORE` gate line precedes the FIRST occurrence of `extract_ohlcv`, `get_atr_with_floor_from_frames`, `df["close"]`, `df["volume"]`, `.to_numpy(`, or `frame_trade(`. Fix any plugin where an expensive/numeric access precedes the gate. Record the gate-line vs first-OHLCV-line per plugin in the SUMMARY.

      Then run `.venv/bin/ruff check src/intelligence/trading/ --fix` and `.venv/bin/black src/intelligence/trading/`, then `.venv/bin/pytest tests/unit/intelligence/ -q`. If `test_i7_extrinsic_contract.py` fails only because ctf_score perturbation now blocks a Wave-2 plugin at the gate, record the plugin names in the SUMMARY for Plan 03; fix any other failure in plugin code.
    </action>
    <acceptance_criteria>
      - `grep -n "shadow_only: bool = True\|requires_i6_confluence: bool = True" src/intelligence/trading/dual_divergence.py` shows both.
      - `grep -n "0.60 + abs" src/intelligence/trading/dual_divergence.py` returns nothing; 4 named `_score` variables and a composite summing to 1.0 are present; no `hmm` token appears in the confidence composition.
      - `grep -n "hmm_regime_weight(features, \"ranging\")" src/intelligence/trading/dual_divergence.py` shows the ranging regime gate; `grep -n "features.get(\"ctf_score\")\|abs(ctf_score)" ...` shows the I6 gate.
      - For all 9 Wave-2 plugins: `grep -n` confirms the `_MIN_CTF_SCORE` gate line precedes the first OHLCV/ATR/`.to_numpy(`/`frame_trade(` line (recorded in SUMMARY).
      - `.venv/bin/ruff check src/intelligence/trading/` exits 0.
      - `.venv/bin/pytest tests/unit/intelligence/ -q` passes (or only documented ctf_score-perturbation failures handed to Plan 03).
    </acceptance_criteria>
    <files>src/intelligence/trading/dual_divergence.py</files>
    <verify>.venv/bin/ruff check src/intelligence/trading/ && .venv/bin/pytest tests/unit/intelligence/ -q</verify>
    <done>DualDivergence declares shadow_only=True and requires_i6_confluence=True with a 4-factor non-HMM composite and a ranging regime gate; Wave-2 gate-ordering audit passes for all 9 plugins; ruff clean; intelligence suite green or only documented ctf_score failures handed to Plan 03.</done>
  </task>

</tasks>

<verification>
  - `grep -rln "requires_i6_confluence: bool = False" src/intelligence/trading/lvn_breakout.py src/intelligence/trading/orb15.py src/intelligence/trading/orb30.py src/intelligence/trading/second_leg_continuation.py src/intelligence/trading/vcp.py src/intelligence/trading/vwap_reclaim.py src/intelligence/trading/dual_divergence.py src/intelligence/trading/vwap_deviation.py src/intelligence/trading/momentum_breakout.py` returns nothing.
  - `grep -L "shadow_only: bool = True" {those nine files}` returns no files.
  - `grep -n "in (1, 2)\|not in (1.0\|hmm_regime_prob <" src/intelligence/trading/lvn_breakout.py src/intelligence/trading/second_leg_continuation.py src/intelligence/trading/vcp.py` returns nothing.
  - `.venv/bin/pytest tests/unit/intelligence/ -q` passes (or only ctf_score-perturbation failures handed to Plan 03).
</verification>

<output>
After completion, create `.planning/phases/119-remaining-16-setup-refactoring/119-02-SUMMARY.md` documenting per-plugin gate replacements, the 4-factor weights chosen, which plugins kept their existing formulas (LVNBreakout/VWAPReclaim 4-factor, VWAPDeviation/MomentumBreakout 3-factor), confirmation that no new confidence formula uses HMM, the Wave-2 gate-ordering audit table, and any ctf_score-perturbation contract failures handed to Plan 03.
</output>
