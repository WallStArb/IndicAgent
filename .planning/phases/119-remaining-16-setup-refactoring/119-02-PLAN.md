---
phase: 119-remaining-16-setup-refactoring
plan: "02"
type: execute
wave: 2
depends_on: ["119-01"]
requirements: [REFACTOR-06, REFACTOR-07]
files_modified:
  - src/intelligence/trading/lvn_breakout.py
  - src/intelligence/trading/orb15.py
  - src/intelligence/trading/orb30.py
  - src/intelligence/trading/second_leg_continuation.py
  - src/intelligence/trading/vcp.py
  - src/intelligence/trading/vwap_reclaim.py
  - src/intelligence/trading/dual_divergence.py
  - src/intelligence/trading/vwap_deviation.py
  - src/intelligence/trading/momentum_breakout.py
autonomous: true
must_haves:
  truths:
    - "All 9 Wave-2 plugin classes declare `shadow_only: bool = True` as an explicit ClassVar."
    - "All 9 Wave-2 plugin classes declare `requires_i6_confluence: bool = True` (no `= False`, no `# TODO`)."
    - "Every binary/integer regime equality check (`hmm_regime in (1, 2)`, `hmm_regime not in (1.0, 2.0)`, `hmm_regime_prob < 0.60`) is replaced by a continuous `hmm_regime_weight()`/`hmm_trending_weight()` gate against `_MIN_REGIME_WEIGHT = 0.30`."
    - "Every Wave-2 plugin runs the regime gate AND I6 ctf_score gate (`abs(ctf_score) < _MIN_CTF_SCORE`, `_MIN_CTF_SCORE = 0.25`) BEFORE any `extract_ohlcv()`/`df[\"close\"]`/ATR access."
    - "VWAPDeviation and MomentumBreakout have their `extract_ohlcv()` call moved to AFTER the dual gate (no OHLCV extraction at the top of compute_full)."
    - "LVNBreakout, VWAPReclaim, VWAPDeviation retain their existing 4-factor composite confidence (no rewrite); only gates/ClassVars change."
    - "MomentumBreakout retains its 3-factor confidence formula (`0.40*roc_score + 0.35*vol_score + 0.25*break_margin`) unchanged per D-04."
    - "ORB15, ORB30, SecondLegContinuation, VCP, DualDivergence have new 4-factor `clamp01`-bounded composite confidence whose weights sum to 1.0."
    - "`.venv/bin/pytest tests/unit/intelligence/ -q` passes."
---

<objective>
Apply the 6 GOOD patterns to the 9 Wave-2 I7 setups (LVNBreakout, ORB15, ORB30, SecondLegContinuation, VCP, VWAPReclaim, DualDivergence, VWAPDeviation, MomentumBreakout), replacing binary regime checks with continuous hmm_regime_weight gates, adding the I6 ctf_score gate before OHLCV extraction, and bringing single-factor confidence formulas up to 4-factor composites (MomentumBreakout keeps its 3-factor formula per D-04).
</objective>

<must_haves>
  <truths>
    - All 9 Wave-2 plugin classes declare `shadow_only: bool = True` as an explicit ClassVar.
    - All 9 Wave-2 plugin classes declare `requires_i6_confluence: bool = True` (no `= False`, no `# TODO`).
    - Every binary/integer regime equality check (`hmm_regime in (1, 2)`, `hmm_regime not in (1.0, 2.0)`, `hmm_regime_prob < 0.60`) is replaced by a continuous `hmm_regime_weight()`/`hmm_trending_weight()` gate against `_MIN_REGIME_WEIGHT = 0.30`.
    - Every Wave-2 plugin runs the regime gate AND I6 ctf_score gate (`abs(ctf_score) < _MIN_CTF_SCORE`, `_MIN_CTF_SCORE = 0.25`) BEFORE any `extract_ohlcv()`/`df["close"]`/ATR access.
    - VWAPDeviation and MomentumBreakout have their `extract_ohlcv()` call moved to AFTER the dual gate (no OHLCV extraction at the top of compute_full).
    - LVNBreakout, VWAPReclaim, VWAPDeviation retain their existing 4-factor composite confidence (no rewrite); only gates/ClassVars change.
    - MomentumBreakout retains its 3-factor confidence formula (`0.40*roc_score + 0.35*vol_score + 0.25*break_margin`) unchanged per D-04.
    - ORB15, ORB30, SecondLegContinuation, VCP, DualDivergence have new 4-factor `clamp01`-bounded composite confidence whose weights sum to 1.0.
    - `.venv/bin/pytest tests/unit/intelligence/ -q` passes.
  </truths>
</must_haves>

<tasks>

  <task type="auto">
    <name>Batch F/G/J — gates-only plugins (no confidence rewrite): LVNBreakout, VWAPReclaim, VWAPDeviation, MomentumBreakout</name>
    <read_first>
      - src/intelligence/trading/lvn_breakout.py — binary `hmm in (1,2)` gate (~lines 84-91); 4-factor confidence (~lines 186-188); trend_clarity uses hmm_probability (~lines 173-174)
      - src/intelligence/trading/vwap_reclaim.py — binary trend_align scoring; close = df["close"].to_numpy() at ~line 97; 4-factor confidence (~lines 189-195); regime_type="any"
      - src/intelligence/trading/vwap_deviation.py — extract_ohlcv FIRST at ~line 60; 3-factor compliant confidence; regime_type="mean_reversion"
      - src/intelligence/trading/momentum_breakout.py — extract_ohlcv FIRST at ~line 58; 3-factor confidence to KEEP per D-04; regime_type="trend"
      - src/intelligence/trading/ofi_continuation.py — reference ClassVar block + features-then-gate-then-OHLCV ordering
    </read_first>
    <action>
      For EACH of the four files: ensure imports `hmm_regime_weight, hmm_trending_weight` and `clamp01`. Add module-level `_MIN_REGIME_WEIGHT: float = 0.30` and `_MIN_CTF_SCORE: float = 0.25`. Add `shadow_only: bool = True` ClassVar and change `requires_i6_confluence: bool = False  # TODO...` to `requires_i6_confluence: bool = True`.

      LVNBreakout (`regime_type="trend"`): DELETE the binary regime block (`hmm = features.get("hmm_regime"); if hmm is None: return no_signal(); hmm = int(hmm); if hmm not in (1, 2): return no_signal()`). Replace with the continuous dual gate: Gate 1 `if hmm_regime_weight(features, "up") < _MIN_REGIME_WEIGHT and hmm_regime_weight(features, "down") < _MIN_REGIME_WEIGHT: return no_signal()`; Gate 2 ctf_score. Keep the 4-factor confidence (`0.30*vol_score + 0.25*trend_clarity + 0.25*lvn_inverse + 0.20*close_strength`) unchanged — `trend_clarity` using `hmm_probability` is an intrinsic confidence factor and stays.

      VWAPReclaim (`regime_type="any"`): insert the dual gate (Gate 1 `hmm_trending_weight(features)`, Gate 2 ctf_score) AFTER the session_vwap null check and BEFORE `close = df["close"].to_numpy()` at ~line 97. Keep the 4-factor confidence including its `trend_align` factor unchanged.

      VWAPDeviation (`regime_type="mean_reversion"`): REORDER — currently `result = extract_ohlcv(frames, self.min_lookback)` is the first statement. Change the top to `df = frames.get("main"); if df is None or len(df) < self.min_lookback: return no_signal()`, then assemble the `features` dict, then the dual gate (Gate 1 `hmm_regime_weight(features, "ranging") < _MIN_REGIME_WEIGHT`; Gate 2 ctf_score), THEN call `extract_ohlcv(frames, self.min_lookback)`. Preserve the existing `garch_vol_regime`/`vwap_std` domain gates and the existing 3-factor confidence.

      MomentumBreakout (`regime_type="trend"`): same REORDER pattern as VWAPDeviation — move `extract_ohlcv` (line ~58) to after the dual gate. Gate 1 uses the up/down dual `hmm_regime_weight` form; Gate 2 ctf_score. Confidence formula is UNCHANGED (D-04): keep `0.40*roc_score + 0.35*vol_score + 0.25*break_margin`.
    </action>
    <acceptance_criteria>
      - `grep -n "hmm not in\|hmm in (1\|int(hmm)" src/intelligence/trading/lvn_breakout.py` returns nothing (binary check removed).
      - `grep -n "shadow_only: bool = True\|requires_i6_confluence: bool = True" src/intelligence/trading/lvn_breakout.py src/intelligence/trading/vwap_reclaim.py src/intelligence/trading/vwap_deviation.py src/intelligence/trading/momentum_breakout.py` shows both ClassVars in all four.
      - In vwap_deviation.py and momentum_breakout.py the line number of `_MIN_CTF_SCORE` gate is LESS than the line number of `extract_ohlcv`.
      - `grep -n "0.40 \* roc_score\|roc_score" src/intelligence/trading/momentum_breakout.py` confirms the 3-factor formula is still present (unchanged).
      - `grep -n "abs(ctf_score)\|abs(float(features.get(\"ctf_score\")" {all four files}` shows the I6 gate in each.
    </acceptance_criteria>
    <files>src/intelligence/trading/lvn_breakout.py, src/intelligence/trading/vwap_reclaim.py, src/intelligence/trading/vwap_deviation.py, src/intelligence/trading/momentum_breakout.py</files>
    <verify>.venv/bin/pytest tests/unit/intelligence/ -q</verify>
    <done>All four declare shadow_only=True and requires_i6_confluence=True; binary regime checks removed; VWAPDeviation and MomentumBreakout extract OHLCV after the dual gate; existing confidence formulas unchanged.</done>
  </task>

  <task type="auto">
    <name>Batch B/H — confidence rewrite + gate replacement: ORB15, ORB30, SecondLegContinuation, VCP</name>
    <read_first>
      - src/intelligence/trading/orb15.py — single-factor `0.50 + gap_boost`; df.close before regime check; regime_type="trend"
      - src/intelligence/trading/orb30.py — mirrors ORB15
      - src/intelligence/trading/second_leg_continuation.py — binary `hmm not in (1.0, 2.0)` gate; partially intrinsic confidence; regime_type="trend"
      - src/intelligence/trading/vcp.py — binary `hmm not in (1.0,2.0)` + `hmm_regime_prob < 0.60`; single-factor confidence; regime_type="trend"
      - src/intelligence/trading/ofi_continuation.py — reference 4-factor composite + ordering
    </read_first>
    <action>
      For EACH of the four files (all `regime_type="trend"`): ensure imports `hmm_regime_weight, hmm_trending_weight` and `clamp01`; add `_MIN_REGIME_WEIGHT: float = 0.30` and `_MIN_CTF_SCORE: float = 0.25`; add `shadow_only: bool = True`; change `requires_i6_confluence: bool = False  # TODO...` to `= True`.

      ORB15 and ORB30: REORDER so features-dict + dual gate run before `df["close"]` access. Gate 1 (trend): `if hmm_regime_weight(features, "up") < _MIN_REGIME_WEIGHT and hmm_regime_weight(features, "down") < _MIN_REGIME_WEIGHT: return no_signal()`. Gate 2 ctf_score. Replace `0.50 + gap_boost` single-factor with a 4-factor `clamp01` composite: `breakout_margin_score` (`(close - orb_high)/atr` or `(orb_low - close)/atr` by direction), `range_quality_score` (tighter opening range = cleaner; inverse of range_width/atr), `volume_score` (rel_volume expansion), `gap_alignment_score` (existing gap signal). Weights e.g. `0.35, 0.25, 0.25, 0.15` summing to 1.0, wrapped in `compose_confidence()`. ORB30 mirrors ORB15 exactly with its own range constants.

      SecondLegContinuation: DELETE the binary `hmm not in (1.0, 2.0)` gate. Replace with continuous dual gate (up/down `hmm_regime_weight` form + ctf_score). Build a 4-factor confidence: `leg_quality_score`, `momentum_persistence_score`, `volume_alignment_score`, `structure_quality_score` (weights summing to 1.0). Any existing intrinsic hmm_probability bonus may fold into a confidence factor but MUST NOT be a binary string/int comparison.

      VCP: DELETE the binary `hmm not in (1.0,2.0)` AND the `hmm_regime_prob < 0.60` gate. Replace with the continuous dual gate. Build a 4-factor confidence: `contraction_quality_score` (contraction count), `volume_profile_score` (volume dry-up), `breakout_margin_score`, `momentum_context_score` (hmm_probability as an intrinsic factor, continuous, not a gate). Weights sum to 1.0.
    </action>
    <acceptance_criteria>
      - `grep -n "not in (1.0\|not in (1,\|hmm_regime_prob < 0.60\|in (1, 2)" src/intelligence/trading/second_leg_continuation.py src/intelligence/trading/vcp.py` returns nothing.
      - `grep -n "shadow_only: bool = True\|requires_i6_confluence: bool = True" src/intelligence/trading/orb15.py src/intelligence/trading/orb30.py src/intelligence/trading/second_leg_continuation.py src/intelligence/trading/vcp.py` shows both in all four.
      - In orb15.py and orb30.py the dual-gate (`_MIN_CTF_SCORE`) line precedes the first `df["close"]`/`extract_ohlcv` line.
      - Each of the four files shows exactly 4 named `_score` factor variables and a composite whose weights sum to 1.0.
      - `grep -n "0.50 + " src/intelligence/trading/orb15.py src/intelligence/trading/orb30.py` returns nothing (single-factor formula gone).
    </acceptance_criteria>
    <files>src/intelligence/trading/orb15.py, src/intelligence/trading/orb30.py, src/intelligence/trading/second_leg_continuation.py, src/intelligence/trading/vcp.py</files>
    <verify>.venv/bin/pytest tests/unit/intelligence/ -q</verify>
    <done>All four declare shadow_only=True and requires_i6_confluence=True; binary regime gates replaced with continuous hmm gates; each has a new 4-factor composite summing to 1.0.</done>
  </task>

  <task type="auto">
    <name>Batch I — DualDivergence (additive magnitudes → 4-factor) and Wave-2 verification</name>
    <read_first>
      - src/intelligence/trading/dual_divergence.py — additive `0.60 + abs(ofi_div)*0.05 + abs(cvd_div)*0.05`; regime_type (verify mean_reversion vs any)
      - src/intelligence/trading/ofi_divergence.py — sibling pattern from Plan 01 (analog for divergence confidence factors)
    </read_first>
    <action>
      DualDivergence: ensure imports `hmm_regime_weight, hmm_trending_weight` and `clamp01`; add `_MIN_REGIME_WEIGHT: float = 0.30`, `_MIN_CTF_SCORE: float = 0.25`; add `shadow_only: bool = True`; change `requires_i6_confluence: bool = False  # TODO...` to `= True`. Read the class `regime_type`: if `"mean_reversion"` use `hmm_regime_weight(features, "ranging")` for Gate 1; if `"any"` use `hmm_trending_weight(features)`. Gate 2 is ctf_score. Both gates run after features-dict assembly and before any OHLCV access (this plugin is features-first today, so no reorder of extract_ohlcv is expected — but verify and place the gate before any `df` numeric access).

      Replace the additive `0.60 + abs(ofi_div)*0.05 + abs(cvd_div)*0.05` with a 4-factor `clamp01` composite: `ofi_divergence_score` (tanh of ofi_div magnitude), `cvd_divergence_score` (tanh of cvd_div magnitude), `confirmation_bars_score` (persistence/count factor), `volume_score` (rel_volume). Weights e.g. `0.35, 0.30, 0.20, 0.15` summing to 1.0, wrapped in `compose_confidence()`.

      Then run `.venv/bin/ruff check src/intelligence/trading/ --fix` and `.venv/bin/black src/intelligence/trading/`, then `.venv/bin/pytest tests/unit/intelligence/ -q`. If `test_i7_extrinsic_contract.py` fails only because ctf_score perturbation now blocks a Wave-2 plugin at the gate, record the plugin names in the SUMMARY for Plan 03; fix any other failure in plugin code.
    </action>
    <acceptance_criteria>
      - `grep -n "shadow_only: bool = True\|requires_i6_confluence: bool = True" src/intelligence/trading/dual_divergence.py` shows both.
      - `grep -n "0.60 + abs" src/intelligence/trading/dual_divergence.py` returns nothing; 4 named `_score` variables and a composite summing to 1.0 are present.
      - `grep -n "hmm_regime_weight\|hmm_trending_weight" src/intelligence/trading/dual_divergence.py` shows the regime gate; `grep -n "abs(ctf_score)\|features.get(\"ctf_score\")" ...` shows the I6 gate.
      - `.venv/bin/ruff check src/intelligence/trading/` exits 0.
      - `.venv/bin/pytest tests/unit/intelligence/ -q` passes (or only documented ctf_score-perturbation failures handed to Plan 03).
    </acceptance_criteria>
    <files>src/intelligence/trading/dual_divergence.py</files>
    <verify>.venv/bin/ruff check src/intelligence/trading/ && .venv/bin/pytest tests/unit/intelligence/ -q</verify>
    <done>DualDivergence declares shadow_only=True and requires_i6_confluence=True with a 4-factor composite and dual gate; ruff clean; intelligence suite green or only documented ctf_score failures handed to Plan 03.</done>
  </task>

</tasks>

<verification>
  - `grep -rln "requires_i6_confluence: bool = False" src/intelligence/trading/` returns nothing (combined with Plan 01, all 17 are True).
  - `grep -L "shadow_only: bool = True" src/intelligence/trading/lvn_breakout.py src/intelligence/trading/orb15.py src/intelligence/trading/orb30.py src/intelligence/trading/second_leg_continuation.py src/intelligence/trading/vcp.py src/intelligence/trading/vwap_reclaim.py src/intelligence/trading/dual_divergence.py src/intelligence/trading/vwap_deviation.py src/intelligence/trading/momentum_breakout.py` returns no files.
  - `grep -n "in (1, 2)\|not in (1.0" src/intelligence/trading/lvn_breakout.py src/intelligence/trading/second_leg_continuation.py src/intelligence/trading/vcp.py` returns nothing.
  - `.venv/bin/pytest tests/unit/intelligence/ -q` passes (or only ctf_score-perturbation failures handed to Plan 03).
</verification>

<output>
After completion, create `.planning/phases/119-remaining-16-setup-refactoring/119-02-SUMMARY.md` documenting per-plugin gate replacements, the 4-factor weights chosen, which plugins kept their existing formulas, and any ctf_score-perturbation contract failures handed to Plan 03.
</output>
