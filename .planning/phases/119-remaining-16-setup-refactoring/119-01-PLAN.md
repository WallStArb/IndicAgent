---
phase: 119-remaining-16-setup-refactoring
plan: "01"
type: execute
wave: 1
depends_on: []
requirements: [REFACTOR-06, REFACTOR-07]
files_modified:
  - src/intelligence/trading/ofi_divergence.py
  - src/intelligence/trading/failed_breakout.py
  - src/intelligence/trading/microstructure_utils.py
  - src/intelligence/trading/ofi_spike.py
  - src/intelligence/trading/cvd_spike.py
  - src/intelligence/trading/candlestick_pattern_setup.py
  - src/intelligence/trading/session_extremes_setup.py
  - src/intelligence/trading/liquidity_hunt.py
  - src/intelligence/trading/delta_exhaustion.py
autonomous: true
must_haves:
  truths:
    - "All 8 Wave-1 plugin classes declare `shadow_only: bool = True` as an explicit (non-inherited) ClassVar."
    - "All 8 Wave-1 plugin classes declare `requires_i6_confluence: bool = True` (no `= False`, no `# TODO` comment remaining)."
    - "Each Wave-1 plugin (or its shared helper `detect_spike_signal`) runs a regime gate AND an I6 ctf_score gate BEFORE any `extract_ohlcv()`, `get_atr_with_floor_from_frames()`, or `df[\"close\"]`/`df[\"volume\"]` access."
    - "The regime gate uses `hmm_regime_weight()` (for `\"trend\"`/`\"mean_reversion\"`) or `hmm_trending_weight()` (for `\"any\"`) against `_MIN_REGIME_WEIGHT = 0.30`; it is NOT a binary string/integer equality check."
    - "The I6 gate is `abs(float(features.get(\"ctf_score\") or 0.0)) < _MIN_CTF_SCORE` with `_MIN_CTF_SCORE = 0.25` returning `no_signal()`."
    - "`detect_spike_signal()` no longer folds `hmm_regime_weight` or `ctf_score` additively into the confidence value; both are gates only."
    - "Every Wave-1 confidence formula (except where delegated through the shared helper) is a weighted composite of exactly 4 named `clamp01`-bounded factor variables whose weights sum to 1.0, wrapped by `compose_confidence()`."
    - "DeltaExhaustion still uses the `exempt_exhaustion` capture profile and does NOT call `apply_exhaustion_boost`/`apply_exhaustion_guard`."
    - "No target plugin (or shared helper) accesses `extract_ohlcv()`, `df[\"close\"]`, `df[\"volume\"]`, ATR helpers, or `frame_trade()` before BOTH dual gates have passed."
    - "`.venv/bin/pytest tests/unit/intelligence/ -q` passes."
---

<objective>
Apply the 6 GOOD patterns (shadow_only=True, requires_i6_confluence=True, dual gate before OHLCV, 4-factor intrinsic confidence, early-gate optimization, named _MIN_* constants) to the 8 Wave-1 I7 setups: OFIDivergence, OFISpike, CVDSpike, CandlestickPatternSetup, FailedBreakout, LiquidityHunt, DeltaExhaustion, SessionExtremesSetup.

Review-resolved facts (audited 2026-06-10, baked into this plan):
- `detect_spike_signal()` has EXACTLY two consumers — `ofi_spike.py:52` and `cvd_spike.py:55`. No other `src/` file calls it. Refactoring the helper is safe and self-contained. (Resolves Gemini/Codex "consumer audit" HIGH.)
- LiquidityHunt already has `requires_i6_confluence=True` but is MISSING `shadow_only` and runs OHLCV-first — it still needs full treatment minus the i6 flag flip.
</objective>

<must_haves>
  <truths>
    - All 8 Wave-1 plugin classes declare `shadow_only: bool = True` as an explicit (non-inherited) ClassVar.
    - All 8 Wave-1 plugin classes declare `requires_i6_confluence: bool = True` (no `= False`, no `# TODO` comment remaining).
    - Each Wave-1 plugin (or its shared helper `detect_spike_signal`) runs a regime gate AND an I6 ctf_score gate BEFORE any `extract_ohlcv()`, `get_atr_with_floor_from_frames()`, or `df["close"]`/`df["volume"]` access.
    - The regime gate uses `hmm_regime_weight()` (for `"trend"`/`"mean_reversion"`) or `hmm_trending_weight()` (for `"any"`) against `_MIN_REGIME_WEIGHT = 0.30`; it is NOT a binary string/integer equality check.
    - The I6 gate is `abs(float(features.get("ctf_score") or 0.0)) < _MIN_CTF_SCORE` with `_MIN_CTF_SCORE = 0.25` returning `no_signal()`.
    - `detect_spike_signal()` no longer folds `hmm_regime_weight` or `ctf_score` additively into the confidence value; both are gates only.
    - Every Wave-1 confidence formula (except where delegated through the shared helper) is a weighted composite of exactly 4 named `clamp01`-bounded factor variables whose weights sum to 1.0, wrapped by `compose_confidence()`.
    - DeltaExhaustion still uses the `exempt_exhaustion` capture profile and does NOT call `apply_exhaustion_boost`/`apply_exhaustion_guard`.
    - No target plugin (or shared helper) accesses `extract_ohlcv()`, `df["close"]`, `df["volume"]`, ATR helpers, or `frame_trade()` before BOTH dual gates have passed.
    - `.venv/bin/pytest tests/unit/intelligence/ -q` passes.
  </truths>
</must_haves>

<tasks>

  <task type="auto">
    <name>Refactor detect_spike_signal helper (drives OFISpike + CVDSpike) and flip both plugin ClassVars</name>
    <read_first>
      - src/intelligence/trading/microstructure_utils.py — the shared helper both spike plugins delegate to; current additive hmm/ctf confidence at lines 79-91
      - src/intelligence/trading/ofi_spike.py — plugin class (delegates at line 52); needs ClassVar changes only
      - src/intelligence/trading/cvd_spike.py — plugin class (delegates at line 55); needs ClassVar changes only
      - src/intelligence/trading/ofi_continuation.py — reference for compose_confidence + 4-factor structure
      - src/intelligence/utils/gradient_utils.py — confirm hmm_trending_weight signature
      - tests/unit/intelligence/ — grep for any spike test that asserts CTF/HMM changes confidence additively (these become invalid once HMM/CTF are gates, not confidence terms)
    </read_first>
    <action>
      CONSUMER AUDIT (do first, must hold): run `grep -rn "detect_spike_signal" src/` and confirm the only call sites are `ofi_spike.py:52` and `cvd_spike.py:55`. If any third consumer appears, STOP and record it in the SUMMARY before editing the helper. (Audit at plan time showed exactly these two — proceed unless that has changed.)

      In `microstructure_utils.py`: add `hmm_trending_weight` to the existing import `from ..utils.gradient_utils import hmm_regime_weight` (keep both). Add `clamp01` to `from .confidence_utils import capture_signal_features, compose_confidence`. Add module-level constants `_MIN_REGIME_WEIGHT: float = 0.30`, `_MIN_CTF_SCORE: float = 0.25`, and (for reuse) confirm the existing `_SPIKE_THRESHOLD: float = 2.0` stays.

      Insert the dual gate immediately AFTER the `if abs(spike_z) <= _SPIKE_THRESHOLD: return no_signal()` check (line ~68) and BEFORE `atr = get_atr_with_floor_from_frames(frames)` (line ~70). Gate 1 (spike signals are `regime_type="any"`): `if hmm_trending_weight(features) < _MIN_REGIME_WEIGHT: return no_signal()` — use `hmm_trending_weight`, NOT `hmm_regime_weight(features, "any")` which always returns the 0.5 neutral fallback. Gate 2: read `ctf_score = float(features.get("ctf_score") or 0.0)` then `if abs(ctf_score) < _MIN_CTF_SCORE: return no_signal()`. Both gates must precede `get_atr_with_floor_from_frames` and any `df`/volume numeric access.

      Delete the additive confidence block at lines 79-91 (the `raw = 0.50 + abs(spike_z) * 0.05`, the `if abs(ctf_score) > 0.3: raw += ...`, and the `regime_w = hmm_regime_weight(...); raw += 0.10 * (regime_w - 0.5)` lines). Replace with a 4-factor intrinsic composite using these named factor variables, each wrapped in `clamp01`. IMPORTANT (Codex MEDIUM): `volume_score` MUST derive from the already-assembled `features` dict (e.g. `features.get("rel_volume")`), NOT from a fresh `df["volume"]` read placed before the gate.
        - `z_score_score = clamp01((abs(spike_z) - _SPIKE_THRESHOLD) / 3.0)` — z excess above the 2-sigma gate
        - `volume_score` from `features.get("rel_volume")`: `clamp01((float(rel_vol) - 1.0) / 1.5)` if not None else `0.3`
        - `ctf_factor = clamp01((abs(ctf_score) - _MIN_CTF_SCORE) / (1.0 - _MIN_CTF_SCORE))`
        - `persistence_score`: if `features.get("price_return_z")` is present use `clamp01(abs(spike_z) / max(1.0, abs(float(price_return_z))) - 1.0)`, else `0.3`
      Compose: `raw = 0.45 * z_score_score + 0.25 * volume_score + 0.20 * ctf_factor + 0.10 * persistence_score` (weights sum to 1.0), then `confidence = compose_confidence(raw)`. Keep the existing `ctf_score` reference in the `supporting` factors list. The `regime_w` variable is removed entirely.

      In BOTH `ofi_spike.py` and `cvd_spike.py`: add `shadow_only: bool = True` as an explicit ClassVar alongside the other class-level attributes, and change `requires_i6_confluence: bool = False  # TODO(...)` to `requires_i6_confluence: bool = True` (remove the TODO comment). No other changes to these two plugin files — they delegate to the helper.
    </action>
    <acceptance_criteria>
      - `grep -rn "detect_spike_signal" src/` lists ONLY ofi_spike.py, cvd_spike.py (call sites), microstructure_utils.py (definition), and the CLAUDE.md doc reference — no third code consumer.
      - `grep -n "hmm_trending_weight\|_MIN_REGIME_WEIGHT\|_MIN_CTF_SCORE" src/intelligence/trading/microstructure_utils.py` shows the gate and constants present.
      - `grep -n "regime_w" src/intelligence/trading/microstructure_utils.py` returns nothing (additive regime term removed).
      - `grep -nc "raw += " src/intelligence/trading/microstructure_utils.py` returns 0.
      - `grep -n "0.45\|0.25\|0.20\|0.10" src/intelligence/trading/microstructure_utils.py` shows the 4 composite weights.
      - The line number of the `_MIN_CTF_SCORE` gate is LESS than the line number of `get_atr_with_floor_from_frames` in microstructure_utils.py (verify with `grep -n`).
      - `grep -n "shadow_only: bool = True" src/intelligence/trading/ofi_spike.py src/intelligence/trading/cvd_spike.py` shows it in both; `grep -n "requires_i6_confluence: bool = True" ...` shows it in both; `grep -n "TODO" ...` shows no requires_i6 TODO.
    </acceptance_criteria>
    <files>src/intelligence/trading/microstructure_utils.py, src/intelligence/trading/ofi_spike.py, src/intelligence/trading/cvd_spike.py</files>
    <verify>.venv/bin/pytest tests/unit/intelligence/ -q</verify>
    <done>detect_spike_signal has dual gate before ATR access and a 4-factor composite (volume sourced from features, not df); ofi_spike.py and cvd_spike.py declare shadow_only=True and requires_i6_confluence=True; consumer audit confirms exactly two call sites.</done>
  </task>

  <task type="auto">
    <name>Refactor OFIDivergence and FailedBreakout (Batch A: features-first, new 4-factor confidence)</name>
    <read_first>
      - src/intelligence/trading/ofi_divergence.py — current additive confidence at lines ~125-152; regime_type="any"
      - src/intelligence/trading/failed_breakout.py — current single-factor binary confidence; regime_type="trend"
      - src/intelligence/trading/ofi_continuation.py — reference 4-factor composite (lines 138-164) and ClassVar block (lines 54-73)
    </read_first>
    <action>
      For EACH of the two files: add imports `hmm_regime_weight, hmm_trending_weight` from `..utils.gradient_utils`; ensure `clamp01` is imported from `.confidence_utils`. Add module-level constants `_MIN_REGIME_WEIGHT: float = 0.30` and `_MIN_CTF_SCORE: float = 0.25`. In the dataclass add `shadow_only: bool = True` ClassVar and change `requires_i6_confluence: bool = False  # TODO...` to `requires_i6_confluence: bool = True`.

      Both plugins already assemble the `features` dict before any OHLCV access and neither extracts OHLCV at the top, so insert the dual gate immediately after the `if df is None or len(df) < self.min_lookback: return no_signal()` check and after the features dict is assembled, BEFORE any numeric `df`/array access.

      OFIDivergence (`regime_type="any"`): Gate 1 uses `hmm_trending_weight(features)`. FailedBreakout direction is fully bidirectional, so for OFIDivergence accept either direction via `hmm_trending_weight` (max of up/down). Gate 2 uses `ctf_score`. Replace the additive confidence block (`confidence = 0.42; confidence += 0.25 * math.tanh(...)`, EWMA bonuses, rel_volume bonus) with a 4-factor composite of named `clamp01` variables:
        - `magnitude_score = clamp01(math.tanh(peak_abs / 3.0))`
        - `alignment_score` from ewma5/ewma20 sign agreement with `direction`: `clamp01((1.0 if ewma5_aligned else 0.3) * 0.6 + (1.0 if ewma20_aligned else 0.3) * 0.4)`
        - `persistence_score = clamp01((count - _MIN_PERSISTENCE) / 5.0)` (reuse the existing persistence/count variable; if no such constant exists, add `_MIN_PERSISTENCE` as a named module constant set to the current minimum used)
        - `volume_score` from `features.get("rel_volume")`: `clamp01((float(rel_vol) - 1.0) / 1.5)` if not None else `0.3`
      Compose: `raw_conf = 0.40 * magnitude_score + 0.25 * alignment_score + 0.20 * persistence_score + 0.15 * volume_score`, then `compose_confidence(raw_conf)`.

      FailedBreakout (`regime_type="trend"`): Gate 1 is the direction-specific trend form — `if hmm_regime_weight(features, "up") < _MIN_REGIME_WEIGHT and hmm_regime_weight(features, "down") < _MIN_REGIME_WEIGHT: return no_signal()`. This intentionally passes if EITHER up or down has >= 0.30 probability (it must NOT collapse to `max(up, down)` in a way that loses the AND-of-two-low-probabilities semantics — both must be below threshold to block). Gate 2 uses `ctf_score`. Replace the `0.55 + 0.15 if ranging` single-factor formula with a 4-factor composite of named `clamp01` variables sourced from the plugin's existing failed-breakout state: `break_magnitude_score`, `rejection_strength_score`, `volume_score`, `structure_quality_score` (use bars-since-BOS / structure features already read by the plugin). Compose with weights `0.35, 0.30, 0.20, 0.15` summing to 1.0, wrapped in `compose_confidence()`. Preserve the existing direction logic and signal construction.
    </action>
    <acceptance_criteria>
      - `grep -n "shadow_only: bool = True" src/intelligence/trading/ofi_divergence.py src/intelligence/trading/failed_breakout.py` shows both.
      - `grep -n "requires_i6_confluence: bool = True" src/intelligence/trading/ofi_divergence.py src/intelligence/trading/failed_breakout.py` shows both; `grep -n "TODO" ...` shows no requires_i6 TODO.
      - `grep -n "hmm_trending_weight" src/intelligence/trading/ofi_divergence.py` shows the any-regime gate; `grep -n "hmm_regime_weight(features, \"up\")\|hmm_regime_weight(features, \"down\")" src/intelligence/trading/failed_breakout.py` shows the direction-specific trend gate.
      - `grep -n "abs(ctf_score) < _MIN_CTF_SCORE\|features.get(\"ctf_score\")" src/intelligence/trading/ofi_divergence.py src/intelligence/trading/failed_breakout.py` shows the I6 gate in both.
      - Each file shows 4 named `_score` factor variables and a `raw_conf =` line whose 4 weights sum to 1.0.
    </acceptance_criteria>
    <files>src/intelligence/trading/ofi_divergence.py, src/intelligence/trading/failed_breakout.py</files>
    <verify>.venv/bin/pytest tests/unit/intelligence/ -q</verify>
    <done>Both plugins declare shadow_only=True and requires_i6_confluence=True, run the dual gate, and use a 4-factor composite summing to 1.0; FailedBreakout's trend gate blocks only when both up AND down are below 0.30.</done>
  </task>

  <task type="auto">
    <name>Refactor the 4 OHLCV-reorder / special-case Wave-1 plugins: CandlestickPatternSetup, SessionExtremesSetup, LiquidityHunt, DeltaExhaustion</name>
    <read_first>
      - src/intelligence/trading/candlestick_pattern_setup.py — extract_ohlcv at ~line 118 (OHLCV-first); regime_type="any"
      - src/intelligence/trading/session_extremes_setup.py — df.close accessed before any cheap gate; regime_type="mean_reversion"
      - src/intelligence/trading/liquidity_hunt.py — extract_ohlcv at ~line 53 (OHLCV-first); already has requires_i6_confluence=True, MISSING shadow_only
      - src/intelligence/trading/delta_exhaustion.py — df access before regime check; regime_type="mean_reversion"; uses exempt_exhaustion profile; MUST NOT use apply_exhaustion_boost/guard
      - src/intelligence/trading/ofi_continuation.py — reference 4-factor composite + features-dict-then-gate-then-OHLCV ordering
    </read_first>
    <action>
      For EACH of the four files: ensure imports `hmm_regime_weight, hmm_trending_weight` (from `..utils.gradient_utils`) and `clamp01` (from `.confidence_utils`) are present. Add module-level constants `_MIN_REGIME_WEIGHT: float = 0.30` and `_MIN_CTF_SCORE: float = 0.25`. Add `shadow_only: bool = True` ClassVar to every one (LiquidityHunt included — it is missing shadow_only even though it already has requires_i6_confluence=True). Change `requires_i6_confluence: bool = False  # TODO...` to `requires_i6_confluence: bool = True` in CandlestickPatternSetup, SessionExtremesSetup, DeltaExhaustion (LiquidityHunt is already True — leave that line).

      REORDER each plugin so the order is: (1) `df = frames.get("main")` and null/length check, (2) assemble the `features` dict from all frame tiers including `**(frames.get("i6") or {})`, (3) dual gate, (4) `extract_ohlcv()` / `df["close"]` / ATR access. Move the existing OHLCV/`extract_ohlcv` call DOWN to after the dual gate in CandlestickPatternSetup (line ~118), SessionExtremesSetup (df.close access), and LiquidityHunt (line ~53). Domain-specific cheap gates that already exist (session-timing windows, pattern presence checks, proximity checks) stay; place them after the dual gate but they may remain before OHLCV if they do not touch the DataFrame.

      Regime gate by `regime_type`:
        - CandlestickPatternSetup (`"any"`): `if hmm_trending_weight(features) < _MIN_REGIME_WEIGHT: return no_signal()`. Remove the existing `trend_regime` binary string check from the gating path (it may remain as an intrinsic confidence factor input).
        - SessionExtremesSetup (`"mean_reversion"`): `if hmm_regime_weight(features, "ranging") < _MIN_REGIME_WEIGHT: return no_signal()`.
        - LiquidityHunt: read the class `regime_type`; if `"trend"` use the up/down dual `hmm_regime_weight` form, if `"mean_reversion"` use the ranging form, if `"any"` use `hmm_trending_weight`.
        - DeltaExhaustion (`"mean_reversion"`): `if hmm_regime_weight(features, "ranging") < _MIN_REGIME_WEIGHT: return no_signal()`.
      Gate 2 (all four): `if abs(float(features.get("ctf_score") or 0.0)) < _MIN_CTF_SCORE: return no_signal()`.

      Replace single/two/count-based confidence in CandlestickPatternSetup, SessionExtremesSetup, LiquidityHunt, DeltaExhaustion with a 4-factor `clamp01`-bounded composite (weights summing to 1.0) wrapped in `compose_confidence()`:
        - CandlestickPatternSetup: pattern_confidence + body_ratio + volume_confirmation + zone_proximity (weights e.g. 0.35, 0.25, 0.25, 0.15).
        - SessionExtremesSetup: level_proximity + rejection_strength + session_timing_score + volume_context (weights e.g. 0.35, 0.30, 0.20, 0.15).
        - LiquidityHunt: hunt/sweep significance + rejection/reclaim strength + volume_context + structure_quality (weights e.g. 0.35, 0.30, 0.20, 0.15).
        - DeltaExhaustion: cvd_z_score (from `cvd_spike_z`) + price_fail_score (price_follow_ratio inverse) + hmm_mean_reversion_score (regime alignment factor) + ctf_score_factor (weights e.g. 0.35, 0.30, 0.20, 0.15). NOTE (Gemini MEDIUM): the `hmm_mean_reversion_score` confidence factor here is the regime ALIGNMENT magnitude used as intrinsic signal quality, NOT the exhaustion boost/guard the plugin is exempt from — verify the composite does not reintroduce any `apply_exhaustion_*` behavior.
      DeltaExhaustion MUST keep `capture_signal_features(..., "exempt_exhaustion", ...)` and MUST NOT call `apply_exhaustion_boost` or `apply_exhaustion_guard`.
    </action>
    <acceptance_criteria>
      - `grep -n "shadow_only: bool = True" src/intelligence/trading/candlestick_pattern_setup.py src/intelligence/trading/session_extremes_setup.py src/intelligence/trading/liquidity_hunt.py src/intelligence/trading/delta_exhaustion.py` shows all four.
      - `grep -n "requires_i6_confluence: bool = True" {those four files}` shows all four True; no requires_i6 TODO remains in any.
      - In each of the four files, the line number of the dual gate (`_MIN_CTF_SCORE`) is LESS than the line number of `extract_ohlcv`/`df["close"]`/`get_atr_with_floor_from_frames` — verify with `grep -n`.
      - `grep -n "apply_exhaustion_boost\|apply_exhaustion_guard" src/intelligence/trading/delta_exhaustion.py` returns nothing; `grep -n "exempt_exhaustion" src/intelligence/trading/delta_exhaustion.py` returns a match.
      - Each of the four files shows 4 named `_score`/factor variables and a composite line whose weights sum to 1.0.
    </acceptance_criteria>
    <files>src/intelligence/trading/candlestick_pattern_setup.py, src/intelligence/trading/session_extremes_setup.py, src/intelligence/trading/liquidity_hunt.py, src/intelligence/trading/delta_exhaustion.py</files>
    <verify>.venv/bin/pytest tests/unit/intelligence/ -q</verify>
    <done>All four plugins reorder dual gate before OHLCV, declare shadow_only=True and requires_i6_confluence=True; DeltaExhaustion keeps exempt_exhaustion and adds no exhaustion boost/guard.</done>
  </task>

  <task type="auto">
    <name>Mechanical gate-ordering audit + spike-test rewrite + Wave-1 verification</name>
    <read_first>
      - tests/unit/intelligence/test_i7_extrinsic_contract.py — existing contract test over I7 plugins (do NOT modify it here — Plan 03 owns the ctf_score perturbation update)
      - tests/unit/intelligence/ — any spike-specific test asserting CTF/HMM additively changes confidence (now invalid)
    </read_first>
    <action>
      MECHANICAL AUDIT (Codex/Ollama HIGH — gate-ordering safety): for each of the 8 Wave-1 plugins and `microstructure_utils.py`, confirm via `grep -n` that the line number of the `_MIN_CTF_SCORE` I6 gate precedes the FIRST occurrence of any of `extract_ohlcv`, `get_atr_with_floor_from_frames`, `df["close"]`, `df["volume"]`, `.to_numpy(`, or `frame_trade(`. If any expensive/numeric access precedes the gate, fix the ordering in that plugin before proceeding. Record the per-plugin gate-line vs first-OHLCV-line in the SUMMARY.

      SPIKE TEST REWRITE (Codex HIGH): grep the intelligence test tree for any test asserting that perturbing `ctf_score`/`hmm_regime`/`hmm_trending_weight` changes OFISpike/CVDSpike confidence additively. Such assertions are now FALSE (HMM/CTF are gates, not confidence terms). Update those specific spike tests so: (a) HMM/CTF are treated as gate-only (below-threshold inputs yield `no_signal()`, not a lowered confidence), and (b) above-threshold CTF perturbation does NOT change the composite confidence value. Do NOT touch `test_i7_extrinsic_contract.py` (Plan 03 owns its ctf_score exclusion).

      Then run `.venv/bin/ruff check src/intelligence/trading/ --fix` and `.venv/bin/black src/intelligence/trading/`, then `.venv/bin/pytest tests/unit/intelligence/ -q`. If `test_i7_extrinsic_contract.py` fails specifically because perturbing `ctf_score` now blocks a Wave-1 plugin at the new gate, do NOT loosen the production gate — record the failing plugin names in the SUMMARY as a handoff note for Plan 03. All other failures must be fixed in plugin or spike-test code.
    </action>
    <acceptance_criteria>
      - For all 8 Wave-1 plugins and microstructure_utils.py: `grep -n` confirms the `_MIN_CTF_SCORE` gate line precedes the first OHLCV/ATR/`.to_numpy(`/`frame_trade(` line (recorded in SUMMARY).
      - No remaining intelligence test asserts that CTF or HMM additively raises OFISpike/CVDSpike confidence.
      - `.venv/bin/ruff check src/intelligence/trading/` exits 0.
      - `.venv/bin/pytest tests/unit/intelligence/ -q` passes, OR the only failures are documented ctf_score-perturbation contract failures in `test_i7_extrinsic_contract.py` explicitly handed off to Plan 03.
    </acceptance_criteria>
    <files>tests/unit/intelligence/ (spike-test updates only — no source edits in this task)</files>
    <verify>.venv/bin/ruff check src/intelligence/trading/ && .venv/bin/pytest tests/unit/intelligence/ -q</verify>
    <done>Gate-ordering audit passes for all 8 plugins + helper; spike tests no longer assert additive HMM/CTF confidence; ruff clean; intelligence suite green or only documented ctf_score-perturbation failures handed to Plan 03.</done>
  </task>

</tasks>

<verification>
  - `grep -L "shadow_only: bool = True" src/intelligence/trading/ofi_divergence.py src/intelligence/trading/failed_breakout.py src/intelligence/trading/ofi_spike.py src/intelligence/trading/cvd_spike.py src/intelligence/trading/candlestick_pattern_setup.py src/intelligence/trading/session_extremes_setup.py src/intelligence/trading/liquidity_hunt.py src/intelligence/trading/delta_exhaustion.py` returns no files (all have it).
  - `grep -rn "requires_i6_confluence: bool = False" src/intelligence/trading/{ofi_divergence,failed_breakout,ofi_spike,cvd_spike,candlestick_pattern_setup,session_extremes_setup,liquidity_hunt,delta_exhaustion}.py` returns nothing.
  - `grep -n "raw += " src/intelligence/trading/microstructure_utils.py` returns nothing.
  - `grep -rn "detect_spike_signal" src/` shows only the two delegating call sites plus the definition and the CLAUDE.md doc line.
  - `.venv/bin/pytest tests/unit/intelligence/ -q` passes (or only ctf_score-perturbation failures handed to Plan 03).
</verification>

<output>
After completion, create `.planning/phases/119-remaining-16-setup-refactoring/119-01-SUMMARY.md` documenting per-plugin changes, the 4-factor weights chosen per plugin, the gate-line vs first-OHLCV-line audit table, the spike-test rewrites, the consumer-audit confirmation for detect_spike_signal, and any ctf_score-perturbation contract failures handed off to Plan 03.
</output>
