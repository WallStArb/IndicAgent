---
phase: 142.5
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_feature_factory.py
  - tests/integration/test_feature_vectors_schema.py
  - tests/unit/services/test_ic_engine.py
  - scripts/analysis/ops_primitive_discovery_report.py
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Test infrastructure exists for all 11 primitive categories"
    - "All test methods run and fail (RED state) before implementation"
    - "Schema validation test checks for 152 feature_vectors columns"
    - "Discovery report script skeleton is executable"
  artifacts:
    - path: "tests/unit/test_feature_factory.py"
      provides: "Test methods for all 91 new primitives + existing 61"
      min_lines: 100
    - path: "tests/integration/test_feature_vectors_schema.py"
      provides: "Schema validation for 152 columns"
      min_lines: 30
    - path: "tests/unit/services/test_ic_engine.py"
      provides: "IC evaluation test for Renaissance primitives"
      min_lines: 40
    - path: "scripts/analysis/ops_primitive_discovery_report.py"
      provides: "Discovery report generation script"
      min_lines: 80
  key_links:
    - from: "tests/unit/test_feature_factory.py"
      to: "src/intelligence/feature_factory.py"
      via: "Import statements and test assertions"
      pattern: "from src.intelligence.feature_factory import"
    - from: "tests/integration/test_feature_vectors_schema.py"
      to: "production/migrations/206_add_renaissance_primitives.sql"
      via: "Column existence queries"
      pattern: "information_schema.columns"
---

<objective>
Wave 0: Test infrastructure for 91 Renaissance primitives across 11 categories. Creates RED tests
before implementation begins (TDD protocol). All automated tests run via pytest; manual verification
reserved for long-running backfill.
Purpose: Establish verification surface before code is written. Tests fail initially (RED), pass as
primitives are implemented (GREEN).
Output: 4 test files with failing tests, executable discovery report skeleton.
</objective>

<execution_context>
@.planning/phases/142.5-renaissance-primitives/142.5-RESEARCH.md
@.planning/phases/142.5-renaissance-primitives/142.5-PATTERNS.md
@.planning/phases/142.5-renaissance-primitives/142.5-PLAN-OUTLINE.md
@tests/unit/test_feature_factory.py (existing patterns)
@tests/unit/services/test_ic_engine.py (IC test patterns)
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Test Infrastructure Requirements (from PLAN-OUTLINE.md, 91 new primitives)

| Category | Primitives | Test Method |
|----------|------------|-------------|
| Bar Anatomy | 8 | `test_bar_anatomy_primitives` |
| Lagged Returns | 6 | `test_lagged_returns` |
| Open-to-Close Split | 4 | `test_open_to_close_split` |
| Temporal Coords (new pairs + month) | 10 | `test_temporal_coordinates` |
| Volume Structure | 12 | `test_volume_structure` |
| Return Distribution | 7 | `test_return_distribution` |
| Realized Variance | 14 | `test_realized_variance` |
| Alt Volatility | 3 | `test_alt_volatility` |
| Vol Dynamics | 5 | `test_volatility_dynamics` |
| Breakout Distance | 14 | `test_breakout_distance` |
| Price-Volume Interactions | 8 | `test_price_volume_interactions` |
| **Total** | **91** | 11 methods |

Note: `test_open_to_close_split`, `test_price_volume_interactions`, and the month_sin/cos additions
inside `test_temporal_coordinates` MAY be authored inside their owning plans (01, 05.5, 02) rather
than here — Wave 0 must at minimum create the RED surface for the 9 categories that have no owning-plan
test authoring step. Either way, all 11 methods exist and are RED before their implementation lands.

## Existing test pattern (test_feature_factory.py)

`_make_config(**overrides)` returns a minimal FeatureFactoryConfig with all numeric params explicit;
`_make_bars()` generates synthetic OHLCV. Primitive tests call helper functions directly and assert
expected output ranges per docs/research/signal-renaissance-primitives-ohlcv.md.

## Final schema target

61 baseline + 91 Renaissance = 152 feature_vectors columns == 152 feature_registry rows ==
_REGISTRY_ROW_COUNT. The integration test asserts 152.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend test_feature_factory.py with primitive category test methods (RED)</name>
  <files>tests/unit/test_feature_factory.py</files>
  <read_first>
    tests/unit/test_feature_factory.py (lines 1-100 for import patterns and fixtures)
    docs/research/signal-renaissance-primitives-ohlcv.md (primitive formulas and expected ranges)
  </read_first>
  <action>
    Add RED test methods (one per category) to tests/unit/test_feature_factory.py covering all 91
    Renaissance primitives: test_bar_anatomy_primitives (8), test_lagged_returns (6),
    test_open_to_close_split (4), test_temporal_coordinates (10 including month_sin/cos),
    test_volume_structure (12), test_return_distribution (7), test_realized_variance (14),
    test_alt_volatility (3), test_volatility_dynamics (5), test_breakout_distance (14),
    test_price_volume_interactions (8). Each uses _make_config() and _make_bars(), calls the (not yet
    existing) primitive helpers directly, asserts expected ranges and edge cases (degenerate bars,
    insufficient history, epsilon guards). DO NOT implement the primitives — tests must fail RED with
    ImportError/AttributeError. (Owning plans 01/02/05.5 may also author their own tests; this task
    guarantees the full RED surface exists.)

    Also add one `test_compute_batch_parity` method (RED) that asserts `compute()` (per-bar live
    path) and `compute_batch()` (backfill path) return identical values for every new field on the
    same synthetic bar series — this is the general regression test for the M1 vectorized-precompute
    work in Plans 02-05.5 (Round 2 review M4/M1: without this, a `_*_series_full` precompute helper
    that silently diverges from its per-bar counterpart — e.g. an off-by-one window boundary — would
    never be caught).
  </action>
  <acceptance_criteria>
    - 11 category test methods present: `grep -c "def test_bar_anatomy_primitives\|def test_lagged_returns\|def test_open_to_close_split\|def test_temporal_coordinates\|def test_volume_structure\|def test_return_distribution\|def test_realized_variance\|def test_alt_volatility\|def test_volatility_dynamics\|def test_breakout_distance\|def test_price_volume_interactions" tests/unit/test_feature_factory.py` reports 11
    - `grep -c "def test_compute_batch_parity" tests/unit/test_feature_factory.py` reports 1
    - `pytest tests/unit/test_feature_factory.py::test_bar_anatomy_primitives -x` fails RED (ImportError/AttributeError)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/unit/test_feature_factory.py::test_bar_anatomy_primitives -x 2>&1 | head -20</automated>
  </verify>
  <done>
    - 11 category test methods + 1 compute()/compute_batch() parity test added; all RED; coverage spans 91 Renaissance primitives + edge cases
  </done>
</task>

<task type="auto">
  <name>Task 2: Create schema validation integration test (RED, 152 columns)</name>
  <files>tests/integration/test_feature_vectors_schema.py</files>
  <read_first>
    tests/unit/test_feature_factory.py (test patterns)
    src/intelligence/schemas.py (FeatureVector field list)
    .planning/phases/142.5-renaissance-primitives/142.5-PLAN-OUTLINE.md (final total 152)
  </read_first>
  <action>
    Create tests/integration/test_feature_vectors_schema.py with `test_renaissance_columns_exist`: an
    @pytest.mark.integration async test that queries information_schema.columns for feature_vectors,
    builds an expected_columns set (61 baseline + all 91 Renaissance column names enumerated), asserts
    no missing columns, and asserts total column count == 152. Use asyncpg parameterized queries. Test
    fails RED until migration 206 is applied.
  </action>
  <acceptance_criteria>
    - Test asserts `== 152` (grep confirms) and enumerates the 91 new column names
    - `pytest tests/integration/test_feature_vectors_schema.py::test_renaissance_columns_exist -x` fails RED before migration 206
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/integration/test_feature_vectors_schema.py::test_renaissance_columns_exist -x 2>&1 | head -20</automated>
  </verify>
  <done>
    - Integration test created; enumerates 152 expected columns; RED until migration 206
  </done>
</task>

<task type="auto">
  <name>Task 3: Create IC evaluation test for Renaissance primitives (RED)</name>
  <files>tests/unit/services/test_ic_engine.py</files>
  <read_first>
    tests/unit/services/test_ic_engine.py (existing IC test patterns)
    services/ic_engine.py (IC measurement logic)
  </read_first>
  <action>
    Add `test_renaissance_primitive_evaluation` enumerating all 91 Renaissance primitive feature names,
    verifying the query structure for feature_ic_scores rows per primitive. Skeleton only — does not run
    the IC engine.
  </action>
  <acceptance_criteria>
    - Test method present; enumerates 91 Renaissance feature names
    - `pytest tests/unit/services/test_ic_engine.py::test_renaissance_primitive_evaluation -x` runs (structure verified)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/unit/services/test_ic_engine.py::test_renaissance_primitive_evaluation -x 2>&1 | head -20</automated>
  </verify>
  <done>
    - test_renaissance_primitive_evaluation added; enumerates 91 primitives; query structure ready
  </done>
</task>

<task type="auto">
  <name>Task 4: Create discovery report script skeleton</name>
  <files>scripts/analysis/ops_primitive_discovery_report.py</files>
  <read_first>
    services/ic_engine.py (IC measurement patterns)
    docs/research/signal-renaissance-primitives-ohlcv.md (primitive catalog)
  </read_first>
  <action>
    Create scripts/analysis/ops_primitive_discovery_report.py with shebang, imports (asyncpg, argparse),
    a generate_primitive_discovery_report() function documenting the query structure (read
    feature_ic_scores, rank by IC Sharpe, stratify by regime, pass/fail gate IC Sharpe > 0 AND p < 0.05
    across all 152 features), and a main guard. Do NOT implement full query logic.
  </action>
  <acceptance_criteria>
    - `python scripts/analysis/ops_primitive_discovery_report.py --help` executes (argparse main guard present)
  </acceptance_criteria>
  <verify>
    <automated>python scripts/analysis/ops_primitive_discovery_report.py --help 2>&1 | head -10</automated>
  </verify>
  <done>
    - Script created with shebang, imports, main guard, documented query structure
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test->code | Unit tests import from src/intelligence/ |
| test->db | Integration tests query feature_vectors schema |
| script->db | Discovery report queries feature_ic_scores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142.5-SC | Tampering | Test code execution | accept | Test infrastructure runs in isolated environment; no production impact |
| T-142.5-01 | Injection | SQL queries in integration tests | mitigate | asyncpg parameterized queries; no string concatenation |
| T-142.5-02 | Denial | Test database connections | mitigate | pytest fixtures with proper connection pooling/teardown |
</threat_model>

<verification>
## Overall Phase Checks

- [ ] pytest tests/unit/test_feature_factory.py -v shows 11 failing category tests (RED)
- [ ] pytest tests/integration/test_feature_vectors_schema.py::test_renaissance_columns_exist -x fails (migration not applied)
- [ ] pytest tests/unit/services/test_ic_engine.py::test_renaissance_primitive_evaluation -x has structure
- [ ] python scripts/analysis/ops_primitive_discovery_report.py --help executes
- [ ] Total test coverage spans 152 features (61 baseline + 91 Renaissance)
</verification>

<success_criteria>
## Measurable Completion

1. **Test Infrastructure Complete:** 11 category test methods + 1 compute/compute_batch parity test in test_feature_factory.py, 1 integration test, 1 IC test, 1 discovery script
2. **All Tests RED:** Every new primitive test fails with expected error
3. **Primitive Coverage Verified:** 91 Renaissance primitives enumerated across test methods
4. **Schema Validation Ready:** Integration test checks for 152 feature_vectors columns
5. **Discovery Script Executable:** ops_primitive_discovery_report.py has shebang, imports, main guard

**Output:** `.planning/phases/142.5-renaissance-primitives/142.5-00-SUMMARY.md`
</success_criteria>

<output>
Create `.planning/phases/142.5-renaissance-primitives/142.5-00-SUMMARY.md` when done
</output>
