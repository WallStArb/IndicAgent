---
phase: 142.5
reviewers: [antigravity, codex, claude, agy-claude-opus, fable]
reviewed_at: 2026-07-07T08:53:00Z
plans_reviewed: [142.5-00-PLAN.md, 142.5-01-PLAN.md, 142.5-02-PLAN.md, 142.5-03-PLAN.md, 142.5-04-PLAN.md, 142.5-05-PLAN.md, 142.5-05.5-PLAN.md, 142.5-06-PLAN.md]
---

# Cross-AI Plan Review — Phase 142.5 (Renaissance Primitives)

**Two review rounds.** Round 1 (2026-07-06, below as "Round 1") found 5 HIGH runtime-blocking
defects in the pre-replan plans; the plans were rewritten (commits `c891d617`, `3edff246`) to fix
all 5. Round 2 (2026-07-07) re-reviews the replanned version to verify those fixes and catch
anything new — see immediately below.

---

## Round 2 — Post-Replan Re-Review (2026-07-07)

**Reviewers invited:** agy (Claude Opus 4.6 backend), Fable 5 subagent, codex
**Reviews completed:** 1/2 independent (agy via Claude Opus 4.6). Codex still rate-limited (resets
2026-07-17). Fable hit its weekly usage limit mid-run (resets 2026-07-07 23:00 ET) before producing
output — not counted as a completed review.

| Reviewer | Status |
|---|---|
| agy (Claude Opus 4.6) | ✅ Complete — independent serving path (not Claude Code CLI) |
| Fable 5 (subagent) | ❌ Failed — weekly usage limit, resets 2026-07-07 23:00 ET |
| codex | ❌ Failed — usage limit reached, resets 2026-07-17 6:25 PM |

Note on `agy`: default backend model (Gemini 3.5 Flash) was quota-exhausted (resets ~2026-07-13).
A fallback attempt with `--model "GPT-OSS 120B (Medium)"` produced a garbled, single-plan,
hallucinated-reference response and was discarded as unusable. `--model "Claude Opus 4.6 (Thinking)"`
produced the substantive review below. See [[feedback_review_tooling]] for the corrected agy usage
notes (agy is a multi-backend wrapper, not one model — quota is per-backend).

### agy (Claude Opus 4.6) Review

**Verdict:** ✅ APPROVE WITH FINDINGS — "high-quality plans with strong count reconciliation and
cold-start discipline." All 5 Round-1 HIGH findings assessed as resolved (see Fix Verification
below); 3 new HIGH findings (verification-gap class, not runtime-blocking), 5 MEDIUM, 6 LOW.

**Fix Verification (Round 1's 5 HIGH findings):**

| # | Round 1 finding | Verdict | Evidence |
|---|---|---|---|
| 1 | Cold-start crash (Plans 01-04 missing `_cold_start_vector` fallbacks) | ✅ RESOLVED | "Cold-start contract is called out in every plan (the most common crash vector in dataclass-based pipelines)" |
| 2 | `feature_registry` never seeded | ✅ RESOLVED | Plan 06 Task 3 seeds 91 rows |
| 3 | `_REGISTRY_ROW_COUNT` desync | ✅ RESOLVED | Count reconciliation audit: 61→79→101→122→130→144→152 verified consistent across all plans |
| 4 | Column-count arithmetic mismatch | ✅ RESOLVED | Independently re-summed: 18+22+21+8+14+8 = 91 new, 61+91 = 152 total — ties out |
| 5 | 8 Price-Volume Interactions unimplemented | ✅ RESOLVED | Plan 05.5 exists and implements all 8 end-to-end |

**New findings (verified against actual plan files below, not just trusted at face value):**

**HIGH — verification-gap class (acceptance criteria don't check everything the action text adds):**

- **H1 — Plan 04 Task 3 acceptance criteria checks only 5 of 8 new config fields**, omitting the
  3 `*_z_window` fields entirely. **Also confirmed:** Plan 04 names these fields `parkinson_vol_z_window`
  etc., but the codebase's established convention (verified in `feature_factory.py` — every existing
  field uses this pattern: `momentum_zscore_window`, `volume_zscore_window`, `amihud_zscore_window`,
  etc.) is `*_zscore_window`, and Plan 06's own APR key list already uses `feature.parkinson_vol.zscore_window`.
  Plan 04 is the only place in the entire phase using the `_z_window` spelling — a real naming-convention
  break, not just an unverified field.
- **H2 — Plan 03 Task 3 acceptance criteria checks only 6 of 18 config fields it adds** — verified
  directly: the `assert all(k in s for k in [...])` one-liner at `142.5-03-PLAN.md:208` lists
  `ret_kurtosis_fast, updown_ratio_fast, realized_var_fast, variance_ratio_fast, hv_fast, hv_ratio_window`
  — 6 names — against 18 fields actually added in the Task 3 action text. 12 fields could ship silently
  missing and the plan's own automated check would still pass.
- **H3 — `_make_config()` test fixture cascading-update gap.** Plans 01-05.5 collectively add 44 new
  required config fields to `FeatureFactoryConfig`. No plan explicitly tasks updating `_make_config()`
  (the shared test fixture defined in `142.5-PATTERNS.md`) with defaults for each wave's new fields.
  If `FeatureFactoryConfig` stays a frozen dataclass with non-optional fields (established pattern),
  every plan after 01 needs `_make_config()` extended or all prior-wave tests break at construction time.

**MEDIUM:**

- **M1 — No `_*_series_full` batch precompute helpers specified for rolling-window primitives in
  Plans 02, 03, 04, 05.** Verified: none of the four plans' "wire into compute()/compute_batch()"
  tasks reference this pattern; all describe wiring "inside the bar loop." The codebase's own
  established pattern for existing rolling features (`_momentum_z_series_full` at
  `feature_factory.py:603` et al.) precomputes full vectorized series once in `compute_batch()`
  rather than calling the per-bar helper in a loop. Skipping this for ~40 new rolling-window
  primitives risks O(n×window) or worse backfill time across 58 ETFs × 4 TFs × years of history —
  directly relevant to this project's "survives 10x volume?" design gate.
- **M2 — Migration 206 is written across two plans (05 then 06) with no "do not apply early" guard.**
  If Wave 2 (Plans 03/04) and Wave 3 (Plan 05) are parallelized by an executor, the file could look
  "done" after Plan 05 lands its 14 breakout columns, before Plan 06 appends the other 77.
- M3 — `session_time_pos` formula ambiguity: Plan 01 implements a continuous timestamp-fraction
  formula; the source spec describes a discrete bar-index formula. Not resolved as an explicit
  decision anywhere.
- M4 — Threat models across all plans are formulaic (generic STRIDE) and miss the actual domain
  risks: silent 0.0-on-cold-start being indistinguishable from a real 0.0 signal in the IC engine,
  no `compute()`/`compute_batch()` parity test specified anywhere, and time-of-day features risking
  spurious IC from return seasonality.
- M5 — Plan 02's acceptance criteria for volume structure nominally checks helper-function count (10)
  rather than output-field count (12) — **downgraded on verification**: Plan 02 Task 2 actually
  delegates to `pytest tests/unit/test_feature_factory.py::test_volume_structure`, which Plan 00
  authors with explicit per-field assertions on all 12 outputs. Not a real gap; the reviewer didn't
  trace that the pytest indirection already covers this. No action needed.

**LOW:** L1 (Wave 0 RED state via `ImportError` is a pytest collection error, not a test failure —
cosmetic), L2 (`_ret_lag_*` could consolidate into one parameterized helper — style only), L3
(migration 206 number should be re-verified as still-available immediately before execution — routine),
L4 (intermediate `_REGISTRY_ROW_COUNT` values across plans mean partial-deployment would crash startup
— acceptable since GSD executes phases as one unit, not plan-by-plan to production), L6 (**false positive
on verification** — claimed `parent_features` column doesn't exist in `feature_registry`; checked
`production/migrations/172_feature_registry.sql:39` — column exists, `TEXT[]`, no action needed).

**DAG/count validation:** agy independently re-walked the full dependency graph (acyclic, correct
wave ordering, Plan 06 as universal sink) and the full count reconciliation table (91 new fields,
54 APR keys, 91 migration columns, 91 registry rows) — all tie out correctly.

---

## Antigravity Review (independent)

**Verdict:** REQUEST CHANGES
**Severity distribution:** 5 HIGH / 3 MEDIUM / 2 LOW

### Executive Summary

The plans correctly use TDD, keep `FeatureFactory` stateless/DAG-compliant, and parameterize all lookback windows via APR. But in their current state, execution would produce a build that crashes at daemon startup and fails integration tests — five HIGH-severity runtime-blocking bugs and structural mismatches.

### HIGH severity

1. **Cold-start crash — missing `_cold_start_vector` updates (Plans 01-04).** `FeatureVector` is a frozen dataclass with non-optional fields; the constructor requires every field. Plans 01, 02, 03, 04 add fields to the dataclass but never add matching neutral/fallback values to `_cold_start_vector()` in `feature_factory.py` (only Plan 05 does this, for its own 14 fields). Any cold-start bar (history count < 2) processed while Waves 1-4's fields exist raises a `TypeError` and crashes the pipeline daemon.
2. **Missing `feature_registry` DB seeding (Plan 06).** `FeatureRegistryService` hard-gates on `len(rows) == _REGISTRY_ROW_COUNT` at startup. Plan 06's migration adds columns to `feature_vectors` but never inserts the corresponding 83 metadata rows into `feature_registry`. The table stays at 61 rows against a code expectation of 144-146 → `RuntimeError` at startup.
3. **`_REGISTRY_ROW_COUNT` out of sync (Plans 04/05/06).** The constant is correctly bumped through Plans 01→75, 02→97, 03→118, 04→126, but neither Plan 05 nor Plan 06 update it further — it stays stuck at 126 even after the DB is fully seeded. Even if finding #2 is fixed, the row-count gate still fails.
4. **Column count mismatch — 85 actual vs 144 asserted.** Summing Plan 06's migration task line items across categories yields 85 new columns (`61 + 85 = 146` total), not the 83 stated in the outline (`61 + 83 = 144`). The integration test `test_renaissance_columns_exist` in `tests/integration/test_feature_vectors_schema.py` asserts 144 and will fail. Root cause: Volume Structure is labeled "10" but lists 12 items; Return Distribution labeled "8" but lists 7; Realized Variance labeled "13" but lists 14.
5. **8 Price-Volume Interaction primitives have zero implementation.** Plan 06 adds their APR params, DB columns, and an integration-test assertion for their presence — but no plan (01-05) computes them in `FeatureFactory`. The columns exist but will always be `NULL`.

### MEDIUM severity

- **M1 — Open-to-Close Split (4) and Cross-Timeframe Divergences (3) silently dropped.** Present in the source spec (`signal-renaissance-primitives-ohlcv.md`), absent from every plan and the migration, with no explicit deferral note in the outline.
- **M2 — Baseline assumption gap: `session_time_pos`, `month_sin/cos`.** Research assumes these already exist in baseline `FeatureFactory`; codebase only has `dow_sin/cos` and `month_position`. Not implemented anywhere in these plans, so they simply remain missing.
- **M3 — `142.5-VALIDATION.md` references a 14-task/5-wave structure that no longer matches the consolidated 7-plan/4-wave structure**, making the verification map useless for tracking execution.

### LOW severity

- **L1 — Migration numbered `177`; current highest in `production/migrations/` is `201`.** Confirmed by directly checking migration files — actual next number is `202`.
- **L2 — Plan 05 frontmatter `files_modified` omits `src/intelligence/schemas.py`**, which Task 2 Step 4 does modify.

### Architectural alignment (all compliant)

| Area | Rating |
|---|---|
| DAG invariance (stateless, no DB/network side effects) | ✅ |
| APR parameterization of all windows | ✅ |
| TDD / test-first (Plan 00 red-state surface) | ✅ |
| Vectorized stateless helpers (no O(N²) backfill) | ✅ |

### Plan reconciliation matrix (antigravity's count reconstruction)

| Category | Spec count | Plans implement | Migration columns | Status |
|---|---|---|---|---|
| Bar Anatomy | 8 | 8 | 8 | Coherent |
| Lagged Returns | 6 | 6 | 6 | Coherent |
| Temporal Coordinates | 12 (6 new pairs) | 8 new | 8 | Coherent (existing pairs correctly left out of migration) |
| Volume Structure | 10 | 12 | 12 (mislabeled 10) | Label wrong, math consistent |
| Return Distribution | 8 | 7 | 7 (mislabeled 8) | Label wrong, math consistent |
| Realized Variance | 13 | 14 | 14 (mislabeled 13) | Label wrong, math consistent |
| Alternative Volatility | 3 | 3 | 3 | Coherent |
| Volatility Dynamics | 5 | 5 | 5 | Coherent |
| Breakout Distance | 14 | 14 | 14 | Coherent |
| Price-Volume Interactions | 8 | **0** | 8 | Critical gap — DB column exists, no compute |
| Open-to-Close Split | 4 | 0 | 0 | Deferred, undocumented |
| Cross-TF Divergences | 3 | 0 | 0 | Deferred, undocumented |
| **Total** | **95** | **77** | **85** | 3-way mismatch |

### Antigravity's recommended fixes, in order

1. Add `142.5-05.5-PLAN.md` implementing the 8 Price-Volume Interaction primitives end-to-end (helper functions, `FeatureFactory.compute()`/`compute_batch()`, `FeatureVector` fields, `FEATURE_VECTOR_DOMAIN` registration).
2. Add `_cold_start_vector` fallback-value tasks to Plans 01, 02, 03, 04.
3. Add `feature_registry` seeding INSERTs to Plan 06's migration task (pattern: `production/migrations/172_feature_registry.sql`).
4. Sync `_REGISTRY_ROW_COUNT`: Plan 05 → 140, Plan 06 → final count (146, once Price-Volume Interactions are counted).
5. Renumber the migration file from `177` to `202` everywhere it's referenced.
6. Re-map `142.5-VALIDATION.md`'s task IDs/waves to the actual 7-plan structure.

---

## Prior Internal Claude Review (non-independent, retained for coverage)

Captured during the same planning session, before this cross-AI pass — kept because it surfaces findings antigravity's independent pass doesn't fully duplicate (the triple primitive-count mismatch framing, APR parameter count).

**Verdict:** REQUEST CHANGES · 4 HIGH / 3 MEDIUM / 2 LOW

- **H1 — Primitive count mismatch across documents:** source doc states 100, plan outline header states 83, sum of plan allocations is 77-79 depending on how counted. 17-23 primitives unaccounted for.
- **H2 — Temporal coordinates double-count:** plans describe "6 new temporal" but 6 sin/cos pairs = 12 features; only some are genuinely new.
- **H3 — APR parameter count mismatch:** outline says "~40," Plan 06 says "48," source doc implies 45-48.
- **H4 — Plan 02 volume count mismatch:** plan states "16 volume," research doc's Volume Structure section shows 12 — likely Interaction primitives bleeding into the volume count.
- Medium/low findings substantially overlap antigravity's M1/M3/L1 above (test coverage gap for missing primitives, migration-before-implementation ordering, migration number 177→202).

---

## Consensus Summary

### Agreed strengths (both reviewers)

- DAG invariants respected — `FeatureFactory` stays compute-only, stateless, and vectorized.
- APR mandate correctly applied to every lookback window.
- TDD protocol followed — Plan 00 establishes a red-state test surface before implementation.

### Agreed concerns — highest priority, both reviewers independently flagged these

1. **The plans do not implement everything the migration/APR/tests expect.** Both reviews landed on the same root defect from different angles: antigravity traced it to the 8 unimplemented Price-Volume Interaction primitives specifically; the internal pass traced it to a broader 3-way primitive-count mismatch (100 spec / 83 outline / ~77-79 implemented). These are the same gap described at different granularity — resolve by reconciling the primitive inventory once, not twice.
2. **Migration number `177` is wrong.** Both reviews independently checked the filesystem and confirmed the actual next number is `202`.
3. **Schema/column-count arithmetic doesn't tie out.** Antigravity found the precise mechanism (mislabeled category totals summing to 85, not 83, columns); the internal pass caught the same symptom as a 100/83/79 discrepancy. Fix the category labels and the total falls out correctly.
4. **Undocumented scope omissions** (Open-to-Close Split, Cross-Timeframe Divergences, Price-Volume Interactions) need an explicit IN/OUT decision recorded in the outline — not silently dropped.

### New in this pass (antigravity only — runtime-blocking, not previously caught)

- **`_cold_start_vector` will crash on any cold-start bar** once Plans 01-04's fields exist, because those plans never add fallback values for their new fields (only Plan 05 does). This is a distinct, more severe finding than the registry-count issue previously identified — it's a guaranteed `TypeError` on the very first cold-start bar processed, independent of database state.
- **`feature_registry` table itself is never seeded** with the 83 new rows (previously the concern was framed only as "`_REGISTRY_ROW_COUNT` constant not updated" — antigravity clarifies both the DB-seeding INSERT statements and the code constant are missing, and either alone still crashes startup).

### Divergent framing (not a real disagreement, worth noting)

- The two reviews count "how many primitives are missing/unimplemented" differently (77 vs 79 vs the internal pass's 79/83/100 triple). This is because the internal pass and antigravity partially disagree on whether "existing" temporal pairs and Interaction primitives should count against the phase's ~83 target. Resolve this by having the plan outline state, category-by-category, the canonical count — then both framings collapse to the same number.

---

## Round 1 Recommended Actions (priority order) — all applied in the replan

**Before implementation starts:**

1. Reconcile the primitive inventory once, in `142.5-PLAN-OUTLINE.md`: for every category, state spec count vs. plan-implemented count vs. migration-column count, and make them equal. Explicitly mark Open-to-Close Split (4), Cross-Timeframe Divergences (3), and Price-Volume Interactions (8) as either IN (add a plan) or OUT (documented deferral).
2. If Price-Volume Interactions are IN: add `142.5-05.5-PLAN.md` to implement them (compute, dataclass fields, domain registration) — this is the only category with DB/APR scaffolding but zero code.
3. Add `_cold_start_vector` fallback-value tasks to Plans 01, 02, 03, 04 — this is the highest-severity fix, since it crashes on the first cold-start bar regardless of any other fix.
4. Add `feature_registry` seeding INSERT statements to Plan 06, and sync `_REGISTRY_ROW_COUNT` across Plans 05 (→140) and 06 (→final count).
5. Renumber the migration from `177` to `202` in all references.
6. Fix the category-count labels in Plan 06 (Volume Structure, Return Distribution, Realized Variance) so the column-count arithmetic ties to whatever total step 1 settles on.

**During implementation:**

7. Re-map `142.5-VALIDATION.md` to the actual 7-plan/4-wave structure.
8. Add `schemas.py` to Plan 05's `files_modified` frontmatter.

## Round 1 Conclusion (superseded — see Round 2 above for current state)

Architecturally sound (DAG, APR, TDD all compliant), but not executable as written — five independently-confirmed runtime-blocking defects (cold-start crash, registry seeding, registry count sync, column arithmetic, unimplemented Interactions) must be fixed before Wave 0 starts. Re-run `/gsd-plan-phase 142.5 --reviews` to fold this feedback back into the plans, or apply the fixes above directly and re-verify.

---

## Round 2 Actions Applied (2026-07-07)

All findings verified against actual plan/source file content before fixing (not applied on trust):

1. **H1 fixed** — Plan 04: renamed `parkinson_vol_z_window`/`garman_klass_vol_z_window`/`yang_zhang_vol_z_window` → `*_zscore_window` (matches codebase convention and Plan 06's APR key names); acceptance criteria expanded from 5 to all 8 fields.
2. **H2 fixed** — Plan 03: acceptance criteria expanded from 6 to all 18 config fields.
3. **H3 fixed** — Plans 01-05.5: added an explicit `_make_config()` fixture-update step to each plan's config-extension task, so the shared test fixture in `142.5-PATTERNS.md` gets defaults for every new field as it's introduced.
4. **M1 fixed** — Plans 02, 03, 04, 05: wire-in tasks now require `_*_series_full` vectorized batch precompute helpers (matching the existing `_momentum_z_series_full` pattern) for rolling-window primitives, instead of per-bar calls inside the `compute_batch()` loop.
5. **M2 fixed** — Plan 05: added an explicit note that migration 206 is incomplete until Plan 06 lands; do not apply to a live database after Plan 05 alone.
6. **M3 resolved as a decision** — Plan 01: documented that `session_time_pos` intentionally uses the continuous timestamp-fraction formula (TF-independent) rather than the spec's discrete bar-index formula, with rationale.
7. **M4 partially addressed** — added a `compute()`/`compute_batch()` parity assertion to Plan 00's Wave 0 test infrastructure guidance (full parity test authoring left to executor discretion per plan, consistent with existing test-writing latitude).
8. **M5, L1-L4, L6** — no action; verified as non-issues (L6 especially: `parent_features` column already exists in migration 172) or acceptable given existing project conventions (L4: GSD executes phases as one unit).

**Conclusion:** All 5 Round-1 HIGH findings confirmed resolved by independent re-review. The 3 new Round-2 HIGH findings (all in the "acceptance criteria under-checks its own action text" class, not runtime-blocking) plus M1/M2/M3 are now fixed directly in the plan files. Phase 142.5 is ready to execute pending a full third-party review pass once Codex or agy's default backend quota resets (optional — the fixes applied are narrow, mechanical, and independently verified against source, not speculative).
