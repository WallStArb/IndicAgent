---
phase: 142.5-renaissance-primitives
plan: "04"
subsystem: feature-engineering
tags: [feature-factory, feature-vector, apr, numpy, tdd, volatility, parkinson, garman-klass, yang-zhang]

# Dependency graph
requires:
  - phase: 142.5-00 (Renaissance Primitives Test Infrastructure, Wave 0)
    provides: test_alt_volatility / test_volatility_dynamics RED tests
      (already existed, GREEN now); RENAISSANCE_PRIMITIVE_FIELDS canonical
      list (already included this plan's 8 field names at count 91);
      test_compute_batch_parity cross-plan regression guard
  - phase: 142.5-01 (Bar Anatomy, Lagged Returns, Open-to-Close Split)
    provides: FeatureFactoryConfig/FeatureVector extension pattern; collateral
      call-site fix pattern (production config-builders + test fixtures);
      _*_series_full precompute pattern
  - phase: 142.5-02 (Temporal Coordinates + Volume Structure)
    provides: _rolling_std_series / _fixed_window_zscore_series shared helpers
  - phase: 142.5-03 (Return Distribution + Realized Variance, merged before
      this plan despite lower plan number)
    provides: FeatureVector at 136 fields going into this plan (not the
      phase outline's assumed 101/109); hv_z_series_full / vol_of_vol_series_full
      as the closest existing analog for the "smooth then z-score" pattern
provides:
  - 3 alternative OHLC-based volatility estimators: parkinson_vol_z,
    garman_klass_vol_z, yang_zhang_vol_z
  - 5 volatility dynamics primitives: parkinson/garman_klass/
    yang_zhang_vol_velocity, vol_velocity_z, intraday_noise_ratio
  - FeatureVector extended to 144 fields (61 baseline + 18 Plan01 + 22 Plan02
    + 14 Plan05 + 21 Plan03 + 8 Plan04)
  - FeatureFactoryConfig extended with 8 feature.parkinson_vol.*/
    garman_klass_vol.*/yang_zhang_vol.*/vol_velocity.*/intraday_noise.* APR
    fields
  - 6 new pure-value helper functions (_parkinson_vol, _garman_klass_vol,
    _yang_zhang_vol, _parkinson_vol_z, _garman_klass_vol_z, _yang_zhang_vol_z)
    + 2 more (_vol_velocity_z, _intraday_noise_ratio) + 5 new
    _*_series_full vectorized batch precompute functions + shared
    _rolling_mean_series helper
  - FEATURE_VECTOR_DOMAIN, _cold_start_vector, and _REGISTRY_ROW_COUNT synced
    to 144
  - Production config-prewarm wiring (feature_vector_pipeline.py,
    backfill_feature_factory.py) for the 8 new APR keys
affects: [142.5-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Two-window alt-vol estimator (parkinson_vol_z, garman_klass_vol_z):
       per-bar variance-proxy term smoothed via a rolling mean over `window`,
       then that smoothed series z-scored over a separate `zscore_window` --
       distinct from Plan 02/03's single-window double-duty pattern
       (vol_std_z, hv_z, vol_of_vol) because the task explicitly required
       2 separate APR fields per estimator, not 1"
    - "New shared _rolling_mean_series helper (O(n) cumsum, mirrors the
       pre-existing _rolling_std_series) -- first primitive requiring a
       plain rolling mean rather than a rolling std/zscore"
    - "Velocity-as-diff-of-precomputed-series: parkinson/garman_klass/
       yang_zhang_vol_velocity are NOT separate _series_full functions --
       they are the O(1) difference of consecutive elements of the
       already-precomputed *_vol_z arrays (s.parkinson_vol_z[i] -
       s.parkinson_vol_z[i-1]), computed inline in both compute() and
       compute_batch(), guaranteeing exact parity by construction with zero
       extra precompute cost"
    - "Yang-Zhang O(n x window) nested-loop series_full (var(overnight) +
       k*var(open-to-close) recomputed per bar over a trailing window) --
       same cost class as Plan 03's high_low_corr/vol_asymmetry_z/
       ret_kurtosis_z nested loops, since YZ's variance-based formula has no
       O(n) prefix-sum shortcut the way single-bar Parkinson/GK terms do"
    - "Two-tier fallback convention distinguished explicitly: _cold_start_vector
       (len(bars) < 2) uses 1.0 for intraday_noise_ratio (matches every other
       ratio-style field's neutral fallback in this codebase), while the
       primitive's own insufficient-window guard (fewer than session_bars + 1
       closes available, a normal mid-warmup scenario in compute_batch's
       early bars) returns 0.0 per the plan's literal action text -- these
       are two different call contexts with two different documented
       fallbacks, not an inconsistency"

key-files:
  created: []
  modified:
    - src/intelligence/feature_factory.py
    - src/intelligence/schemas.py
    - src/intelligence/feature_registry_service.py
    - services/feature_vector_pipeline.py
    - services/backfill_feature_factory.py
    - tests/unit/test_feature_factory.py
    - tests/unit/intelligence/test_feature_factory_p7.py
    - tests/unit/intelligence/test_feature_factory_batch_parity.py
    - tests/unit/services/test_backfill_feature_factory.py
    - tests/unit/services/test_feature_vector_writer.py
    - tests/unit/services/test_feature_vector_writer_column_mapping.py
    - tests/unit/pipeline/pipeline_helpers.py

key-decisions:
  - "Worktree branch was already caught up through Plan 03 (base commit
     1ca74b3e, 'chore: merge executor worktree' for 142.5-03) at spawn time
     -- no fast-forward or cherry-pick needed before starting Task 1, unlike
     prior plans in this phase that had to catch up mid-execution."
  - "This plan's own frontmatter (depends_on: [01, 02], _REGISTRY_ROW_COUNT
     must_haves = 130 = 61+18+22+14+8+... — see the plan file's literal
     comment '61 baseline + 18 Plan 01 + 22 Plan 02 + 21 Plan 03 + 8 Plan
     04') assumed a base that doesn't match the actual dependency-DAG-valid
     merge order (Plans 01/02/05/03 all landed before this plan started,
     giving an actual base of 136, not the outline's assumed 101/109).
     Used the actual current count throughout for internal consistency
     (FeatureVector fields == FEATURE_VECTOR_DOMAIN entries ==
     _REGISTRY_ROW_COUNT == 144 = 136 + 8), matching the exact precedent
     Plans 03/05 documented for the identical situation."
  - "parkinson_vol_z/garman_klass_vol_z use a two-window design (window +
     zscore_window) rather than Plan 02/03's single-window double-duty
     pattern, because Task 3's action text explicitly lists 6 distinct APR
     fields for the 3 estimators (2 each), not 3. window smooths the raw
     per-bar variance-proxy term via a rolling mean (standard practice for
     Parkinson/Garman-Klass, which are otherwise noisy single-bar
     estimates); zscore_window normalizes the smoothed series against its
     own trailing history."
  - "Yang-Zhang's window parameter is used directly as the variance-sample
     window (no separate smoothing step) since YZ is inherently a
     multi-bar variance estimator (needs multiple overnight-gap and
     open-to-close observations to compute a variance), unlike Parkinson/GK
     which are already single-bar variance proxies before smoothing."
  - "The 3 *_vol_velocity primitives are computed as an inline O(1)
     difference of the precomputed *_vol_z series (s.field[i] -
     s.field[i-1]) in both compute() and compute_batch(), rather than as
     separate _*_velocity_series_full functions -- Task 4's action text
     only names 5 series_full functions (the 3 z-scores + vol_velocity_z +
     intraday_noise_ratio), not 8, confirming this was the intended design:
     velocity is a derived view of an already-materialized series, not an
     independent precompute pass."
  - "k = 0.34 (Yang-Zhang weighting constant) and 4*ln(2) / (2*ln(2)-1)
     (Parkinson/Garman-Klass normalizing constants) are hardcoded as
     definitional formula constants, not APR keys -- consistent with the
     project's APR-exempt category for 'mathematical constants' /
     'statistical concept definitions,' and with this codebase's existing
     precedent of not APR-ing Garman-Klass's own '2*ln(2)-1' term."
  - "intraday_noise_ratio's own insufficient-window guard returns 0.0 (per
     the plan's literal action text: '0.0 when len < session_bars + 1'),
     while _cold_start_vector's separate len(bars)<2 guard uses 1.0 (matching
     every other ratio-style field's cold-start convention in this
     codebase, e.g. hv_ratio, vol_trend_ratio, updown_ratio_fast/slow). Both
     fallbacks are finite and the distinction is explicit in the plan text
     and in code comments, so this was implemented literally rather than
     treated as a Rule-1 bug to unify."

requirements-completed: [REQ-142.5-07, REQ-142.5-08]

# Metrics
duration: 35min
completed: 2026-07-07
---

# Phase 142.5 Plan 04: Alternative Volatility Estimators + Volatility Dynamics Summary

**8 new FeatureFactory primitives (Parkinson/Garman-Klass/Yang-Zhang OHLC
volatility estimators plus 5 velocity/noise-ratio dynamics primitives) wired
into compute()/compute_batch(), extending FeatureVector to 144 fields with
full cold-start safety, APR-backed windows, and verified compute()/
compute_batch() parity across a 199-bar sweep.**

## Performance

- **Duration:** 35 min
- **Started:** 2026-07-07T15:05:00Z (approx.)
- **Completed:** 2026-07-07T15:14:00Z
- **Tasks:** 5/5 completed
- **Files modified:** 12 (5 core intelligence/service files, 6 test fixture
  files, 1 planning summary)

## Accomplishments

- Implemented 3 alternative OHLC-based volatility estimator functions
  (`_parkinson_vol`, `_garman_klass_vol` as single-bar variance-proxy terms;
  `_yang_zhang_vol` as a windowed var(overnight) + k*var(open-to-close)
  estimator) plus their `_z` z-score wrappers, all delegating to vectorized
  `_*_series_full` implementations (single source of truth shared by
  compute() and compute_batch())
- Implemented 2 more volatility-dynamics helper functions:
  `_vol_velocity_z` (z-score of the rolling velocity/first-difference of
  atr_z) and `_intraday_noise_ratio` (sum(|log_ret|)/|net_ret| over a
  trailing session window)
- Added 5 `_*_series_full` vectorized batch precompute functions:
  `_parkinson_vol_z_series_full`/`_garman_klass_vol_z_series_full` (O(n) via
  boolean masking + a new shared `_rolling_mean_series` cumsum helper),
  `_yang_zhang_vol_z_series_full` (O(n x window) nested loop, same cost
  class as Plan 03's high_low_corr/vol_asymmetry_z), `_vol_velocity_z_series_full`
  (O(n), diff of atr_z then fixed-window z-score), `_intraday_noise_ratio_series_full`
  (O(n) via cumsum over a fixed session_bars window)
- The 3 `*_vol_velocity` primitives are computed inline as the O(1)
  difference of consecutive elements of the precomputed `*_vol_z` arrays in
  both compute() and compute_batch() -- no separate series_full functions
  needed, guaranteeing parity by construction
- Extended `FeatureFactoryConfig` with 8 APR fields
  (`parkinson_vol_window`/`parkinson_vol_zscore_window`,
  `garman_klass_vol_window`/`garman_klass_vol_zscore_window`,
  `yang_zhang_vol_window`/`yang_zhang_vol_zscore_window`,
  `vol_velocity_window`, `intraday_noise_window`)
- Extended `FeatureVector` to 144 fields (136 -> 144), inserted after the
  Plan 03 realized-variance block (dataclass field-ordering requirement,
  same pattern as Plans 01/02/03/05)
- Wired all 8 primitives into both `FeatureFactory.compute()` and
  `compute_batch()` -- verified byte-exact parity at bar indices
  10/30/60/90/120/149/198 via a manual spot-check script (after correcting
  an off-by-one in the verification script itself -- `compute_batch()`'s
  result list starts at bar index 1, not 0), plus a full 199-bar sweep with
  `python -W error` confirming zero non-finite values and zero
  RuntimeWarnings
- Updated `FEATURE_VECTOR_DOMAIN` (144 entries), `_cold_start_vector()` (8
  neutral fallbacks, verified no TypeError on 0/1/2/3-bar history), and
  `feature_registry_service._REGISTRY_ROW_COUNT` (136 -> 144)
- `test_alt_volatility` and `test_volatility_dynamics` (both already existed
  from Wave 0's RED test infrastructure, including the 8 field names already
  present in `RENAISSANCE_PRIMITIVE_FIELDS` at count 91) are now GREEN

## Task Commits

Each task was committed atomically (with additional collateral-fix commits
per the established multi-plan pattern):

1. **Task 5 (schema part, landed first — wiring requires the fields to
   exist): Extend FeatureVector dataclass with 8 new fields (144 total)** -
   `232b8f93` (test)
2. **Tasks 1 + 2 + 3 + 4: Implement 3 alt-vol + 5 vol-dynamics helper
   functions, extend FeatureFactoryConfig with 8 APR fields, wire into
   compute()/compute_batch(), update FEATURE_VECTOR_DOMAIN +
   _cold_start_vector + `_make_config()` test fixture** - `6fd2da18` (feat)
3. **Task 5 (registry part): bump `_REGISTRY_ROW_COUNT` to 144** -
   `8f6446bc` (feat)

**Collateral fix commits (not separate plan tasks, see Deviations):**
- `328f9d82` (fix): wire 8 new APR keys into the 2 production
  config-builders
- `d880a728` (fix): update 6 collateral test fixtures broken by the schema
  extension

## Files Created/Modified

- `src/intelligence/feature_factory.py` - 6 new pure-value helper functions
  (`_parkinson_vol`, `_garman_klass_vol`, `_yang_zhang_vol`,
  `_parkinson_vol_z`, `_garman_klass_vol_z`, `_yang_zhang_vol_z`) + 2 more
  (`_vol_velocity_z`, `_intraday_noise_ratio`), 5 `_*_series_full` batch
  precompute functions, 1 new shared `_rolling_mean_series` helper,
  `_PrecomputedSeries` +5 array fields, `_precompute_series` wiring,
  `_build_feature_vector()` +8 params, `compute()`/`compute_batch()` wiring
  (velocity computed inline as O(1) diff), `FEATURE_VECTOR_DOMAIN` +8
  entries, `_cold_start_vector()` +8 fallbacks, `FeatureFactoryConfig` +8
  APR fields
- `src/intelligence/schemas.py` - `FeatureVector` +8 fields (144 total),
  docstring updated
- `src/intelligence/feature_registry_service.py` - `_REGISTRY_ROW_COUNT` 136
  -> 144, with a note documenting the actual vs. planned merge order
- `services/feature_vector_pipeline.py` - `_THRESHOLD_KEYS` +
  `_prewarm_threshold_config()` source the 8 new APR keys (production
  config-builder, Rule 2 fix)
- `services/backfill_feature_factory.py` - `_build_feature_factory_config()`
  sources the 8 new APR keys (production config-builder, Rule 2 fix)
- `tests/unit/test_feature_factory.py` - `_make_config()` fixture +8
  fields; `test_all_fields_are_finite_floats` count 136 -> 144 (collateral
  fix)
- `tests/unit/intelligence/test_feature_factory_p7.py` - local `_make_cfg()`
  fixture +8 fields; `test_feature_vector_domain_complete` count 136 -> 144
  (collateral fix)
- `tests/unit/intelligence/test_feature_factory_batch_parity.py` -
  `_make_cfg()` fixture +8 fields; both `_build_feature_vector(...)` direct
  calls (`test_build_feature_vector_guards_nan`,
  `test_build_feature_vector_none_passthrough`) +8 kwargs (collateral fix)
- `tests/unit/services/test_backfill_feature_factory.py` - config fixture
  +8 fields; `_make_zero_vector()` +8 fields (collateral fix)
- `tests/unit/services/test_feature_vector_writer.py` -
  `_make_valid_feature_vector()` +8 fields (collateral fix)
- `tests/unit/services/test_feature_vector_writer_column_mapping.py` -
  sentinel `FeatureVector(...)` construction +8 distinguishable values
  (59.01-59.08, collateral fix)
- `tests/unit/pipeline/pipeline_helpers.py` - `agent._feature_factory_config`
  fixture +8 fields (collateral fix)

## Decisions Made

See `key-decisions` in frontmatter for the full list. Summary of the most
significant:
- Parkinson/Garman-Klass use a two-window design (smoothing window +
  z-score window) rather than the single-window double-duty pattern used
  for `vol_std_z`/`hv_z`/`vol_of_vol` in Plans 02/03, because Task 3's
  action text explicitly required 2 separate APR fields per estimator.
- The 3 velocity primitives are computed inline as a difference of
  consecutive precomputed z-score elements, not as independent
  `_*_series_full` functions -- matches Task 4's literal list of 5 (not 8)
  series_full functions to build.
- Yang-Zhang's `window` parameter is the variance-sample window directly
  (no smoothing step), since YZ is inherently a multi-bar variance
  estimator, unlike the single-bar Parkinson/GK terms.
- `k = 0.34` and the Parkinson/GK normalizing constants (`4*ln(2)`,
  `2*ln(2)-1`) are hardcoded as definitional formula constants (APR-exempt
  mathematical constants), not APR keys.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Plan's field-count target (130) assumed a merge
order that did not hold**
- **Found during:** Reading prior summaries before Task 1, and confirmed via
  a direct field-count check before writing any code.
- **Issue:** The plan's frontmatter `must_haves` and the phase's
  `142.5-PLAN-OUTLINE.md` cumulative table assume this plan's own dependency
  set (`depends_on: [01, 02]`, i.e. 101 baseline fields from Plans 01/02
  only) yields a target close to 109-130 total `FeatureVector` fields. This
  worktree's actual fork point (spawned at commit `1ca74b3e`, already past
  the `142.5-03` merge commit) had Plans 01, 02, 05, AND 03 all merged
  (136 fields) -- Plans 05/03 landed before this plan despite their
  numbering, because their own frontmatter dependency sets (`[00, 01]` and
  `[00, 01, 02, 05]` respectively) made that dispatch order
  dependency-DAG-valid.
- **Fix:** Used the actual current count throughout: 136 (baseline at fork
  time) + 8 (this plan) = 144. Set `FeatureVector` to 144 fields,
  `FEATURE_VECTOR_DOMAIN` to 144 entries, and `_REGISTRY_ROW_COUNT` to 144,
  keeping all three internally consistent. Updated the two hardcoded test
  assertions (`test_all_fields_are_finite_floats`,
  `test_feature_vector_domain_complete`) to check 144, not a stale count.
  Documented the reasoning inline as a code comment next to
  `_REGISTRY_ROW_COUNT`.
- **Files modified:** `src/intelligence/schemas.py`,
  `src/intelligence/feature_registry_service.py`,
  `tests/unit/test_feature_factory.py`,
  `tests/unit/intelligence/test_feature_factory_p7.py`
- **Verification:** `python -c "... assert len(dataclasses.fields(FeatureVector)) == 144"`
  passes; `test_all_fields_are_finite_floats` and
  `test_feature_vector_domain_complete` both pass at 144.
- **Impact on Plan 06:** None -- Plan 06 depends on ALL of 01/02/03/04/05/05.5
  and will reconcile the final total to 152 once every plan has landed,
  regardless of the order in which they actually merged. This is the exact
  same situation Plans 03/05 documented for their own base-count
  discrepancies.

**2. [Rule 1 - Bug] Collateral test-fixture breakage from extending
FeatureFactoryConfig/FeatureVector**
- **Found during:** Post-wiring full-suite verification sweep.
- **Issue:** Same mechanical consequence Plans 01/02/03/05 documented for
  their own extensions: 6 test files construct `FeatureFactoryConfig(...)`
  or `FeatureVector(...)` directly with explicit kwargs (not via the shared
  `_make_config()`/`_make_cfg()` fixtures), and 2 tests hardcode a
  `136`-field/entry count assertion. All of these broke with
  `TypeError`/`AssertionError` once the dataclasses gained the 16 new
  required fields (8 FeatureVector + 8 config).
- **Fix:** Added the new fields with neutral/sentinel values to each
  construction site; updated the two hardcoded counts from 136 to 144.
- **Files modified:** `tests/unit/test_feature_factory.py`,
  `tests/unit/services/test_feature_vector_writer.py`,
  `tests/unit/services/test_feature_vector_writer_column_mapping.py`,
  `tests/unit/services/test_backfill_feature_factory.py`,
  `tests/unit/intelligence/test_feature_factory_p7.py`,
  `tests/unit/intelligence/test_feature_factory_batch_parity.py`,
  `tests/unit/pipeline/pipeline_helpers.py`
- **Verification:** `pytest tests/unit/test_feature_factory.py
  tests/unit/intelligence/ tests/unit/pipeline/
  tests/unit/services/test_feature_vector_writer.py
  tests/unit/services/test_feature_vector_writer_column_mapping.py
  tests/unit/services/test_backfill_feature_factory.py` — 3647 passed, only
  2 expected future-plan RED tests (Plan 05.5's price-volume-interaction
  fields) plus 1 pre-existing unrelated failure remain (see Issues
  Encountered).
- **Committed in:** `d880a728`

**3. [Rule 2 - Missing Critical] Production config-builders would crash the
live pipeline on startup**
- **Found during:** After Task 3/4's config extension, verified via a
  repo-wide grep for other `FeatureFactoryConfig(` construction sites.
- **Issue:** `services/feature_vector_pipeline.py::_prewarm_threshold_config()`
  and `services/backfill_feature_factory.py::_build_feature_factory_config()`
  both construct `FeatureFactoryConfig(...)` with the pre-extension field
  set. Since `FeatureFactoryConfig` is a non-optional-argument frozen
  dataclass, both would raise `TypeError: missing 8 required positional
  arguments` at daemon startup / backfill invocation the moment this plan's
  schema change landed.
- **Fix:** Added the 8 new `feature.*` APR keys to `_THRESHOLD_KEYS` and the
  constructor call in both files, with the same defaults used in the test
  fixtures.
- **Files modified:** `services/feature_vector_pipeline.py`,
  `services/backfill_feature_factory.py`
- **Verification:** `pytest tests/unit/pipeline/ tests/unit/services/
  test_backfill_feature_factory.py
  test_feature_vector_pipeline_publisher_normalization.py
  test_pipeline_backpressure.py test_service_auditor.py` — all pass.
- **Committed in:** `328f9d82`

---

**Total deviations:** 3 auto-fixed (1 Rule 3 blocking, 1 Rule 1 bug fix, 1
Rule 2 missing-critical-functionality). All were prerequisites for the
plan's own schema change to land safely, or corrections to a stale numeric
target; no architectural changes, no scope creep.

## Issues Encountered

- `tests/unit/intelligence/test_binary_pattern_scanner.py::test_zero_binary_violations`
  fails on a pre-existing violation in `src/intelligence/ensemble/weights.py:192`,
  unrelated to any file touched by this plan (same failure already noted by
  every prior 142.5 plan's summary). Out of scope; not fixed.
- `test_price_volume_interactions` and `test_compute_batch_parity` (which
  iterates `RENAISSANCE_PRIMITIVE_FIELDS`, including Plan 05.5's
  price-volume-interaction fields) fail with `AttributeError` on
  `vol_body_product` and friends -- these belong to Plan 05.5, not this
  plan, and remain RED as expected until Plan 05.5 lands. This plan's own 8
  fields (parkinson/garman_klass/yang_zhang_vol_z, the 3 velocities,
  vol_velocity_z, intraday_noise_ratio) are verified GREEN and at parity
  independently.
- Did not apply or modify
  `production/migrations/206_add_renaissance_primitives.sql` -- this plan's
  task list has no migration step (same scope as Plans 01/02/03); the 8 new
  fields remain write-only within the in-memory `FeatureVector`.
- `pytest tests/unit/` full run not attempted (every prior 142.5 plan noted
  it times out on a long-running/network-dependent test unrelated to this
  plan). Verified instead via the plan's own explicit verification commands
  plus a targeted sweep of `tests/unit/test_feature_factory.py` +
  `tests/unit/intelligence/` + `tests/unit/pipeline/` +
  `tests/unit/services/test_feature_vector_writer*.py` +
  `tests/unit/services/test_backfill_feature_factory.py` +
  `tests/unit/services/test_feature_vector_pipeline_publisher_normalization.py`
  + `tests/unit/services/test_pipeline_backpressure.py` +
  `tests/unit/services/test_service_auditor.py` (3647+ tests total: all
  pass except the 3 documented above).
- The plan's Task 5 acceptance criterion
  `pytest tests/unit/test_feature_factory.py -k "cold_start" -x` matches
  zero tests (no test function has "cold_start" in its name), same issue
  every prior 142.5 plan documented. Verified the actual intent
  (`_cold_start_vector` constructs without `TypeError`) directly via Python
  one-liners calling `FeatureFactory.compute([], ...)` and
  `FeatureFactory.compute(bars_len_1/2/3, ...)`, all succeed with finite
  values for all 8 new fields.
- Independently ran a manual `compute()`/`compute_batch()` parity
  spot-check at bar indices 10/30/60/90/120/149/198 plus a full 199-bar
  `-W error` sweep confirming zero non-finite values and zero
  RuntimeWarnings for all 8 fields. First attempt at this script had an
  off-by-one bug (comparing `compute()` on `bars[:idx+1]` against
  `batch_results[idx]`, when `compute_batch()`'s result list actually
  starts at bar index 1 so `batch_results[k]` corresponds to bar index
  `k+1`) -- this was a bug in the ad-hoc verification script, not in the
  implementation; corrected and re-verified parity holds exactly.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `test_alt_volatility` and `test_volatility_dynamics` are GREEN; the
  remaining RED tests in `test_feature_factory.py`
  (`test_price_volume_interactions`, `test_compute_batch_parity`) remain RED
  as expected -- they belong to Plan 05.5.
- `FeatureVector` is now at 144 fields. Plan 05.5 (price-volume
  interactions, 8 fields) adds its own field group on top of whatever base
  is present at its own fork/merge time; Plan 06 (depends on ALL of
  01-05.5) reconciles the final total to 152 regardless of merge order.
- `test_compute_batch_parity` (Wave 0's cross-plan regression guard) now
  covers this plan's 8 fields automatically (verified via a manual
  multi-index spot-check that `compute()` and `compute_batch()` produce
  identical values, plus a full 199-bar sweep confirming no NaN/inf/
  RuntimeWarning) -- it stays RED overall (AttributeError on Plan 05.5's
  fields) until every plan lands.
- The 8 new fields are NOT yet persisted to `feature_vectors` (no DB column
  applied -- migration 206 exists from Plan 05 but this plan does not touch
  it, per its own task scope). Plan 06 owns applying the completed
  migration.
- No blockers for Plan 05.5 or Plan 06.

---
*Phase: 142.5-renaissance-primitives*
*Completed: 2026-07-07*

## Self-Check: PASSED

All claimed files exist (`src/intelligence/feature_factory.py`,
`src/intelligence/schemas.py`, `src/intelligence/feature_registry_service.py`,
`services/feature_vector_pipeline.py`, `services/backfill_feature_factory.py`,
this SUMMARY.md) and all claimed commit hashes (`232b8f93`, `6fd2da18`,
`8f6446bc`, `328f9d82`, `d880a728`) resolve in `git log --oneline --all`. No
missing items.
