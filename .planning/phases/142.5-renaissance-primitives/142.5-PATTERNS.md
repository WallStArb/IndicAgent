# Phase 142.5: Renaissance Primitives - Pattern Map

**Mapped:** 2026-07-06
**Files analyzed:** 7 new/modified files
**Analogs found:** 7 / 7

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `src/intelligence/feature_factory.py` | utility | transform | `src/intelligence/feature_factory.py` (existing primitives) | exact |
| `src/intelligence/schemas.py` | model | transform | `src/intelligence/schemas.py` (FeatureVector dataclass) | exact |
| `src/intelligence/feature_registry_service.py` | service | request-response | `src/intelligence/feature_registry_service.py` | exact |
| `production/migrations/206_add_renaissance_primitives.sql` | migration | batch | `production/migrations/159_foundation_hardening.sql` | exact |
| `tests/unit/test_feature_factory.py` | test | transform | `tests/unit/test_feature_factory.py` (existing tests) | exact |
| `scripts/analysis/ops_primitive_discovery_report.py` | utility | batch | `services/ic_engine.py` (IC measurement) | role-match |
| `tests/integration/test_feature_vectors_schema.py` | test | integration | `tests/unit/test_feature_factory.py` | role-match |

## Pattern Assignments

### `src/intelligence/feature_factory.py` (utility, transform)

**Analog:** `src/intelligence/feature_factory.py` (lines 240-299 for primitive functions)

**Stateless primitive helper pattern** (lines 240-299):
```python
def _bar_close_pos(high: float, low: float, close: float, eps: float = 1e-10) -> float:
    """Intra-bar conviction: close position within high-low range.

    Formula: (close - low) / (high - low + eps)
    Returns 0.5 when high == low (epsilon guard; degenerate doji bar).
    """
    hl = high - low
    if hl < eps:
        return 0.5
    return (close - low) / hl
```

**Rolling window primitive pattern** (lines 290-299):
```python
def _vol_ratio(closes: np.ndarray, short_bars: int, long_bars: int) -> float:
    """Realized volatility ratio: std(short) / std(long).

    Returns 1.0 on cold start or degenerate std.
    """
    if len(closes) < long_bars + 1:
        return 1.0
    long_returns = np.diff(np.log(np.maximum(closes[-(long_bars + 1) :], 1e-10)))
    short_returns = long_returns[-short_bars:]
    vol_short = float(np.std(short_returns))
    vol_long = float(np.std(long_returns))
    return vol_short / vol_long if vol_long > 1e-10 else 1.0
```

**APR-backed config pattern** (lines 138-228):
```python
@dataclass(frozen=True)
class FeatureFactoryConfig:
    """Frozen APR-backed parameter container for FeatureFactory.compute().
    
    Built ONCE by IntelligencePipeline._prewarm_threshold_config() or the
    backfill init. Passed as an explicit argument to compute() on every bar.
    APR keys (feature.* namespace) map directly to these fields.
    """
    momentum_window_fast: int  # feature.momentum.window_fast
    momentum_window_mid: int  # feature.momentum.window_mid
    momentum_zscore_window: int  # feature.momentum.zscore_window
    volume_zscore_window: int  # feature.volume.zscore_window
```

**Batch precomputation pattern** (lines 500-600):
```python
def _precompute_series(
    opens: np.ndarray,
    highs: np.ndarray,
    lows: np.ndarray,
    closes: np.ndarray,
    volumes: np.ndarray,
    config: FeatureFactoryConfig,
) -> _SeriesCache:
    """Precompute all rolling series in O(n) for batch backfill efficiency.
    
    Called once per bar series in compute_batch(), avoiding per-bar O(n²) cost.
    All primitive computations use these precomputed series instead of
    recalculating rolling windows on every bar.
    """
```

**Feature domain registry pattern** (lines 58-131):
```python
FEATURE_VECTOR_DOMAIN: dict[str, str] = {
    # Momentum
    "momentum_z_fast": "quant",
    "momentum_z_mid": "quant",
    "range_position": "quant",
    "bar_close_pos": "quant",
    # Volume and order flow
    "informed_flow": "quant",
    "volume_z": "quant",
    "ofi_z": "quant",
    # ... all 61 current features
}
```

### `src/intelligence/schemas.py` (model, transform)

**Analog:** `src/intelligence/schemas.py` (lines 1204-1299 for FeatureVector dataclass)

**FeatureVector dataclass pattern** (lines 1204-1299):
```python
@dataclasses.dataclass(frozen=True)
class FeatureVector:
    """61 orthogonal feature primitives computed per bar by FeatureFactory.

    Frozen dataclass (not Pydantic) per D-08: pure-function output, no IO,
    immutable after construction. Non-optional fields typed float, no defaults
    — every field must be supplied by the caller. Optional fields are
    cross-sectional ranks populated by Phase 139 enrichment pass.
    """

    # Momentum (7 total: 5 original + 2 new scale-named)
    momentum_z_fast: float  # fast-scale return z-score (APR: feature.momentum.window_fast)
    momentum_z_mid: float  # mid-scale return z-score (APR: feature.momentum.window_mid)
    range_position: float
    bar_close_pos: float
    gap_z: float
    momentum_z_slow: float  # slow-scale return z-score (APR: feature.momentum.window_slow)
    momentum_reversal_z: float
    # Volume and order flow (8)
    informed_flow: float
    volume_z: float
    ofi_z: float
    # ... continues for all 61 fields
```

### `src/intelligence/feature_registry_service.py` (service, request-response)

**Analog:** `src/intelligence/feature_registry_service.py` (lines 36-37 for row count gate)

**Row count gate pattern** (lines 36-37):
```python
_REGISTRY_ROW_COUNT = 61
```

**Row count validation pattern** (lines 84-86):
```python
if len(rows) != _REGISTRY_ROW_COUNT:
    raise RuntimeError(
        f"feature_registry row count mismatch: expected {_REGISTRY_ROW_COUNT}, "
        f"got {len(rows)}. Run migrations to seed feature_registry."
    )
```

### `production/migrations/206_add_renaissance_primitives.sql` (migration, batch)

**Analog:** `production/migrations/159_foundation_hardening.sql` (lines 1-79 for feature column additions)

**APR parameter seeding pattern** (lines 43-50):
```sql
-- ── New APR key: momentum slow window ─────────────────────────────────────────
-- Insert if not already present (idempotent with ON CONFLICT DO NOTHING).

INSERT INTO config_schema (config_key, value_type, description)
VALUES (
    'feature.momentum.window_slow',
    'int',
    'Slow-scale momentum z-score lookback window (bars). [initial_estimate]. ML learning target.'
)
ON CONFLICT (config_key) DO NOTHING;

INSERT INTO config_state (config_key, config_value, version)
VALUES ('feature.momentum.window_slow', '60', 1)
ON CONFLICT (config_key, version) DO NOTHING;
```

**Column addition pattern** (lines 57-79):
```sql
-- ── Calendar primitives expansion ────────────────────────────────────────────────

ALTER TABLE feature_vectors ADD COLUMN IF NOT EXISTS days_to_month_end FLOAT;
ALTER TABLE feature_vectors ADD COLUMN IF NOT EXISTS quarter_position FLOAT;
ALTER TABLE feature_vectors ADD COLUMN IF NOT EXISTS feature_factory_version VARCHAR(32) NOT NULL DEFAULT '1.0.0';

-- Comment: All ADD COLUMN statements use IF NOT EXISTS for idempotency.
```

### `tests/unit/test_feature_factory.py` (test, transform)

**Analog:** `tests/unit/test_feature_factory.py` (lines 30-80 for config fixture pattern)

**Config fixture pattern** (lines 30-80):
```python
def _make_config(**overrides: int) -> FeatureFactoryConfig:
    """Return a minimal FeatureFactoryConfig for testing. All numeric params explicit."""
    defaults = dict(
        momentum_window_fast=5,
        momentum_window_mid=20,
        momentum_window_slow=60,
        momentum_zscore_window=30,
        volume_zscore_window=20,
        ofi_zscore_window=20,
        cvd_slope_bars=5,
        cmf_period=20,
        vol_short_bars=5,
        vol_long_bars=20,
        # ... continues for all APR parameters
        # Plan 02 added 12 more: dollar_vol_window, vol_range_ratio_window,
        # vol_trend_fast/slow, up_vol_ratio_fast/slow, vol_percentile_window,
        # vol_persistence_window, vol_std_window, mfi_fast/slow, obv_window.
    )
    defaults.update(overrides)
    return FeatureFactoryConfig(**defaults)
```

**Primitive test pattern** (lines 100-150 for existing primitive tests):
```python
def test_bar_close_pos_primitive():
    """Test _bar_close_pos primitive: close position within high-low range."""
    # Test normal case
    result = _bar_close_pos(100.0, 90.0, 95.0)
    assert result == pytest.approx(0.5)  # Close at middle

    # Test degenerate bar (high == low)
    result = _bar_close_pos(100.0, 100.0, 100.0)
    assert result == 0.5  # Epsilon guard returns neutral

    # Test close at high
    result = _bar_close_pos(100.0, 90.0, 100.0)
    assert result == pytest.approx(1.0)
```

### `scripts/analysis/ops_primitive_discovery_report.py` (utility, batch)

**Analog:** `services/ic_engine.py` (IC measurement pattern)

**IC-driven discovery pattern** from `ic_engine.py`:
- IC engine evaluates all features identically — no feature-specific logic
- Measures IC Sharpe per feature × symbol × TF × regime × lookahead
- Outputs feature_ic_scores table with rankings
- Discovery report script consumes feature_ic_scores to rank primitives by IC Sharpe

**Pattern for discovery report script:**
```python
# Script structure (not direct excerpt, but follows IC engine pattern):
def generate_primitive_discovery_report():
    """Generate IC Sharpe ranking report for all 144 Renaissance primitives.
    
    Reads feature_ic_scores table, ranks by IC Sharpe, produces:
    - Per-feature IC Sharpe statistics (mean, std, min, max)
    - Regime subgroup analysis (9 cross-sectional labels)
    - Pass/fail gate (IC Sharpe > 0 AND p < 0.05)
    - Top-N and bottom-N primitive lists
    """
```

### `tests/integration/test_feature_vectors_schema.py` (test, integration)

**Analog:** `tests/unit/test_feature_factory.py` (schema validation pattern)

**Schema validation pattern** from existing tests:
```python
def test_renaissance_columns_exist():
    """Verify that all 83 new Renaissance primitive columns exist in feature_vectors."""
    # Query information_schema.columns for feature_vectors table
    # Verify all expected Renaissance primitive column names exist
    # Fail fast if any column is missing (silent wrong answers are worse than loud crashes)
```

## Shared Patterns

### APR Parameter Seeding
**Source:** `production/migrations/159_foundation_hardening.sql` (lines 43-50)
**Apply to:** All 83 window-based Renaissance primitives

```sql
-- Pattern: Insert into config_schema (metadata) and config_state (seed value)
INSERT INTO config_schema (config_key, value_type, description)
VALUES ('feature.<primitive>.<scale>', 'int', '[initial_estimate] <primitive> <scale> window. ML learning target.')
ON CONFLICT (config_key) DO NOTHING;

INSERT INTO config_state (config_key, config_value, version)
VALUES ('feature.<primitive>.<scale>', '<default_value>', 1)
ON CONFLICT (config_key, version) DO NOTHING;
```

### Cold Start Handling
**Source:** `src/intelligence/feature_factory.py` (lines 290-299)
**Apply to:** All rolling window primitives

```python
# Pattern: Guard insufficient history with neutral fallback
if len(closes) < required_window:
    return neutral_value  # 0.0, 1.0, or 0.5 depending on primitive
```

### Z-Score Computation
**Source:** `src/intelligence/feature_factory.py` (rolling z-score helpers)
**Apply to:** All primitives requiring normalization (unbounded ratios, raw returns)

```python
# Pattern: Stable z-score with epsilon guard for zero std
def _rolling_zscore(values: np.ndarray, window: int) -> float:
    if len(values) < window:
        return 0.0
    window_values = values[-window:]
    mean = float(np.mean(window_values))
    std = float(np.std(window_values))
    if std < 1e-10:
        return 0.0
    return (float(values[-1]) - mean) / std
```

### Frozen Dataclass Contract
**Source:** `src/intelligence/schemas.py` (line 1204)
**Apply to:** FeatureVector extension (+83 fields)

```python
# Pattern: All features as frozen dataclass fields (non-optional, typed float)
@dataclasses.dataclass(frozen=True)
class FeatureVector:
    primitive_name: float  # Descriptive comment with APR key reference
```

### Batch Parity Enforcement
**Source:** `src/intelligence/feature_factory.py` (compute vs compute_batch methods)
**Apply to:** All new primitives added to FeatureFactory

```python
# Pattern: compute() and compute_batch() must return identical values
# Test by calling both on same bars and asserting equality
def test_primitive_batch_parity():
    bars = generate_test_bars(100)
    cache = FeatureCache()
    config = _make_config()
    
    # Single-bar compute
    single_result = FeatureFactory.compute(bars, "TEST", "5m", cache, config)
    
    # Batch compute
    batch_result = FeatureFactory.compute_batch(bars, "TEST", "5m", cache, config)[-1]
    
    # Assert parity for new primitive
    assert single_result.new_primitive == pytest.approx(batch_result.new_primitive)
```

## No Analog Found

**None** — all files have exact matches in the existing codebase. Renaissance primitives implementation follows established Feature Factory patterns exactly.

## Metadata

**Analog search scope:** `src/intelligence/`, `tests/unit/`, `tests/integration/`, `production/migrations/`, `scripts/analysis/`, `services/`
**Files scanned:** 15 files across 6 directories
**Pattern extraction date:** 2026-07-06
**Pattern confidence:** HIGH — all analogs are exact matches from the same codebase patterns

**Key insight:** Phase 142.5 is pure expansion within existing architecture. No new patterns required — all 83 Renaissance primitives follow the exact same helper function + APR config + batch precomputation + dataclass field + schema column pattern as the existing 61 features.