---
phase: 173-broadcast-feature-significance-correction-fix-pooled-cross-s
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/ic_engine.py
  - tests/unit/test_ic_engine_compute_split.py
  - .planning/todos/pending/354-context-features-intraday-autocorrelation-per-symbol-pooled-cell.md
  - .planning/todos/pending/355-context-features-writer-orphaned-after-phase-173.md
autonomous: true
requirements: [D-01]

must_haves:
  truths:
    - "No code path in ic_engine.py runs 231 redundant per-symbol significance tests of the same daily macro time series"
    - "The CONTEXT_FEATURES frozenset no longer exists as a hand-maintained broadcast list anywhere in the codebase"
    - "_compute_symbol_tf's remaining (non-daily-cadence) responsibilities are unchanged and still verified by its existing tests"
    - "The two consequences of the deletion that this phase does not itself fix are captured as filed todos, not left implicit"
    - "No measurement path is deleted until a live query has PROVEN, in this run, that it produced zero rows — the proof is an executable gate that hard-stops the task, not a planning-time note"
  artifacts:
    - path: "services/ic_engine.py"
      provides: "ic_engine with the bespoke CONTEXT_FEATURES daily-cadence path removed"
    - path: ".planning/todos/pending/354-context-features-intraday-autocorrelation-per-symbol-pooled-cell.md"
      provides: "Follow-up capturing the per-symbol intraday-autocorrelation exposure this deletion exposes"
    - path: ".planning/todos/pending/355-context-features-writer-orphaned-after-phase-173.md"
      provides: "Follow-up capturing that context_features table now has no ic_engine consumer"
  key_links:
    - from: "services/ic_engine.py"
      to: "context_features table"
      via: "removed — no remaining query"
      pattern: "context_features"
---

<objective>
Delete `_compute_symbol_tf`'s bespoke `CONTEXT_FEATURES` daily-cadence significance path (D-01) —
the second, different bug todo 270 identified: 231 redundant per-symbol significance tests of the
literal same macro time series, each entering BH-FDR independently.

Purpose: D-01 explicitly forbids leaving this running alongside the new broadcast mechanism. One
unified broadcast significance path, no lingering second bug in production.

Output: `services/ic_engine.py` with ~370 lines of dead-end measurement machinery removed, the
`CONTEXT_FEATURES` frozenset gone, and the two downstream consequences filed as todos.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/173-broadcast-feature-significance-correction-fix-pooled-cross-s/173-CONTEXT.md
@.planning/phases/173-broadcast-feature-significance-correction-fix-pooled-cross-s/173-RESEARCH.md
@CLAUDE.md
</context>

<planner_findings>
Verified live during planning (2026-08-24):

1. `grep -rn CONTEXT_FEATURES` across the repo returns exactly three code sites in
   `services/ic_engine.py` — line ~228 (the frozenset definition), line ~2842 (the deleted loop's
   `for cf_idx, cf_name in enumerate(sorted(CONTEXT_FEATURES))`), and line ~3431 (a KNOWN GAP
   paragraph inside `_compute_cross_sectional_tf`'s docstring). Zero test files reference the
   symbol, so CLAUDE.md's "File/class renames require test sweep" rule is a no-op for this name —
   but the SURROUNDING functions' structural tests are not (see finding 4).

2. `config.min_obs_daily` (ICEngineConfig field at line ~507, loaded at line ~738 from APR key
   `alpha.ic.min_obs_daily_features`, listed in `_COMPUTATIONAL_CONFIG_FIELDS` at line ~868) is
   used ONLY inside the block being deleted (lines ~2812, ~2858, ~2892). It becomes dead after this
   task. CI enforces dead-code detection via vulture, so it must be removed, not left.

3. **The deletion was zero-impact on live data AT PLANNING TIME, and that is not sufficient.**
   A live query on 2026-08-24 returned `SELECT regime_label_source, count(*) FROM feature_ic_scores
   GROUP BY 1` = `forward_filter` 6,154,892 and `market_regimes` 143,040 — zero rows with
   `regime_label_source='context_features'`. A corpus run is in flight (STATE.md step 5/8), so this
   count CAN become nonzero between planning and execution. Task 1 therefore opens with an
   executable pre-deletion gate that HARD-STOPS on a nonzero count. This is not a warning and not a
   note: per CLAUDE.md, never drop data that could contain signal, and silent wrong answers are
   worse than loud crashes.

4. `tests/unit/test_ic_engine_compute_split.py` contains source-introspection tests that pin the
   SHAPE of `_compute_symbol_tf` and its siblings, not just their outputs — notably
   `test_compute_symbol_tf_return_keys`, `test_compute_symbol_tf_has_no_db_write_code`,
   `test_compute_cross_sectional_tf_closes_connection_before_clustering`, and
   `test_both_cell_functions_call_subsample_and_rank`. The deleted block opens its own
   `short_lived_conn(dsn)` inside `_compute_symbol_tf`; removing it changes that function's
   connection-scoping shape. These tests MUST be run, not assumed.

5. `_POOLED_REGIME_SENTINEL` (line 174) is used by other paths and must NOT be deleted. Only the
   daily block's own locals go.

6. The `context_features` TABLE is populated (2,995 daily rows each for `flight_quality`,
   `yield_slope_z`, `vix_z`, written by
   `scripts/infrastructure/backfill/infrastructure_context_features_writer.py`). After this
   deletion, `ic_engine.py` has no query against it. Do NOT drop the table or the writer in this
   phase — file todo 355 instead.
</planner_findings>

<tasks>

<task type="auto">
  <name>Task 1: Gate on live row count, then delete the CONTEXT_FEATURES daily-cadence significance path</name>
  <files>services/ic_engine.py</files>
  <read_first>
    - services/ic_engine.py lines 160-260 — the constants region containing `_POOLED_REGIME_SENTINEL`,
      `_CROSS_SECTIONAL_SYMBOL`, `_FEATURE_NAMES`, the CONTEXT_FEATURES comment paragraph and
      frozenset, and `_resolve_regime_scope`
    - services/ic_engine.py lines 480-520 and 720-760 — `ICEngineConfig`'s `min_obs_daily` field
      declaration and its `cfg.get_sync("alpha.ic.min_obs_daily_features", 1000)` load
    - services/ic_engine.py lines 840-980 — `_COMPUTATIONAL_CONFIG_FIELDS`, `_OPERATIONAL_CONFIG_FIELDS`,
      and `_fingerprint_computational_key`'s docstring explaining why moving that key matters
    - services/ic_engine.py lines 2760-3170 — the block boundary: the per-regime-cell loop that must
      SURVIVE (ends ~2799), the daily-cadence block that must GO (~2801 through ~3163), and the
      BH-FDR cluster-representative selection that must SURVIVE (starts ~3030 inside
      `_compute_symbol_tf`, immediately after the deleted block's closing `with` scope)
    - services/ic_engine.py lines 3409-3460 — `_compute_cross_sectional_tf`'s docstring KNOWN GAP
      paragraph referencing CONTEXT_FEATURES
    - tests/unit/test_ic_engine_compute_split.py — the structural assertions this edit must not break
  </read_first>
  <action>
    **Step 0 — PRE-DELETION GATE. Run this FIRST, before editing a single line, and obey it:**

    ```
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc \
      "SELECT count(*) FROM feature_ic_scores WHERE regime_label_source='context_features'"
    ```

    - If the result is **exactly 0**: proceed to the deletion below, and record the observed count
      and the timestamp of the check in the SUMMARY.
    - If the result is **anything other than 0**: **HARD STOP. Do not delete anything. Do not
      proceed to any later step of this task or this plan.** Report, as a task failure:
      (i) the exact count; (ii) the output of
      `SELECT training_window_end, tf, count(*) FROM feature_ic_scores
       WHERE regime_label_source='context_features' GROUP BY 1,2 ORDER BY 1 DESC`;
      (iii) whether an `ic_engine.py` process is currently running (`ps aux | grep ic_engine`).
      Those rows are real measurements at a vintage nothing in this phase rewrites — deleting their
      producer would orphan them silently. The correct response is to escalate for a decision on
      whether to archive or invalidate them first, NOT to proceed with a warning logged.

    This gate is the executable form of planner_findings 3. The planning-time observation of zero
    rows does not authorize the deletion; only this run's own observation does.

    **Steps a-d — the deletion itself, gated on Step 0 returning 0.**

    Remove, from `services/ic_engine.py`, in this order:

    (a) The daily-cadence block inside `_compute_symbol_tf`. It starts at the comment banner
    beginning `# Context features: daily-cadence features in context_features table.` and ends where
    the `for cf_idx, cf_name in enumerate(sorted(CONTEXT_FEATURES))` loop's final
    `all_results.append({...})` closes and its enclosing `with short_lived_conn(dsn) as conn:` scope
    ends. Everything inside that `with` block goes, including the `cf_sql_tmpl` SQL against the
    `context_features` table, the `min_obs_daily`/`n_scales` locals defined immediately above it, and
    the `cf_cluster_id = 10000 + cf_idx` assignment.

    Be surgical about the boundary: the per-regime `_compute_one_regime_cell` loop ABOVE the block
    and the `cluster_groups` BH-FDR representative-selection loop BELOW it both stay. The
    `skip_reasons["insufficient_daily_n"]` counters disappear with the block; verify no other code
    reads that key.

    (b) The `CONTEXT_FEATURES` frozenset (line ~228) AND the ~11-line comment paragraph directly
    above it (the one describing daily-cadence IC observation and ending with the `KNOWN GAP (todo 270)`
    note). The whole paragraph is about the deleted mechanism and is now false.

    (c) `min_obs_daily` from `ICEngineConfig` (the field declaration), from the config load in the
    constructor/factory (the `cfg.get_sync("alpha.ic.min_obs_daily_features", 1000)` line), and from
    `_COMPUTATIONAL_CONFIG_FIELDS`. Removing it from `_COMPUTATIONAL_CONFIG_FIELDS` deliberately
    moves `apr_snapshot_key`, invalidating every cell fingerprint and forcing a full corpus
    recompute. That is CORRECT and expected for this phase — the IC scores change corpus-wide
    regardless. Note it explicitly in the SUMMARY so nobody reads the mass invalidation as a bug.
    Leave the APR key row itself in `config_schema`/`config_state` alone — do not write a migration
    deleting it; an unread key is harmless and Plan 173-04's follow-up may revisit it.

    (d) Rewrite (do not delete) the `KNOWN GAP (todo 270)` paragraph in
    `_compute_cross_sectional_tf`'s docstring at line ~3431. It currently says the fix is pending
    and points at `CONTEXT_FEATURES below`. Replace it with a statement of the mechanism this phase
    installs: broadcast (symbol-invariant) features are excluded from this cell's matrix and measured
    in a separate broadcast cell against an equal-weighted market-aggregate return, with the
    classification read from `concept_registry.metadata->>'broadcast'`. Reference Phase 173 and
    todo 270. Plans 03 and 04 will refine this text as they land; write it in the future-tense-free
    form describing the target design, not "will be."

    Do NOT touch `_POOLED_REGIME_SENTINEL`, `_subsample_and_rank`, `_compute_one_regime_cell`,
    `_compute_one_cross_sectional_cell`, or any fingerprint machinery in this task.
  </action>
  <acceptance_criteria>
    - **The pre-deletion gate ran and returned 0 in THIS execution, and the SUMMARY records the count and the time it was observed.** If it returned anything else, this task is FAILED, not partially complete — no file was edited.
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM feature_ic_scores WHERE regime_label_source='context_features'"` returns exactly 0
    - `grep -c "CONTEXT_FEATURES" services/ic_engine.py` returns 0
    - `grep -c "context_features" services/ic_engine.py` returns 0
    - `grep -c "min_obs_daily" services/ic_engine.py` returns 0
    - `grep -c "insufficient_daily_n" services/ic_engine.py` returns 0
    - `grep -c "10000 + cf_idx" services/ic_engine.py` returns 0
    - `grep -c "_POOLED_REGIME_SENTINEL" services/ic_engine.py` returns a value greater than 0 (the sentinel survives)
    - `.venv/bin/python -c "import services.ic_engine"` exits 0
    - `.venv/bin/pytest tests/unit/test_ic_engine_compute_split.py -q` passes with zero failures — specifically `test_compute_symbol_tf_return_keys`, `test_compute_symbol_tf_has_no_db_write_code`, `test_compute_cross_sectional_tf_closes_connection_before_clustering`, and `test_both_cell_functions_call_subsample_and_rank` all pass
    - `.venv/bin/pytest tests/unit/ -q -k ic_engine` passes with zero failures
    - `.venv/bin/vulture services/ic_engine.py --min-confidence 80` reports no new dead code attributable to this change
    - `services/ic_engine.py` shrinks by at least 350 lines relative to its pre-task state (`git diff --stat`)
  </acceptance_criteria>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM feature_ic_scores WHERE regime_label_source='context_features'" | grep -qx 0 && .venv/bin/pytest tests/unit/ -q -k ic_engine && test "$(grep -c CONTEXT_FEATURES services/ic_engine.py)" = "0" && test "$(grep -c context_features services/ic_engine.py)" = "0"</automated>
  </verify>
  <done>A live query proved zero context_features-sourced rows exist in this run; the bespoke daily-cadence CONTEXT_FEATURES significance path, its frozenset, and its now-dead min_obs_daily config field are gone; every existing ic_engine unit test still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add a regression test and file the two follow-up todos this deletion exposes</name>
  <files>tests/unit/test_ic_engine_compute_split.py, .planning/todos/pending/354-context-features-intraday-autocorrelation-per-symbol-pooled-cell.md, .planning/todos/pending/355-context-features-writer-orphaned-after-phase-173.md</files>
  <read_first>
    - tests/unit/test_ic_engine_compute_split.py — read in full (696 lines); `test_compute_symbol_tf_has_no_db_write_code`
      (line ~73) is the closest existing source-introspection precedent to mirror
    - services/ic_engine.py — the post-Task-1 state of `_compute_symbol_tf`
    - .planning/todos/completed/270-broadcast-feature-significance-overstates-effective-n.md — the parent
      todo whose scope these two follow-ups are carved out of; match its file format
    - .planning/todos/PRIORITIES.md — the single source of truth for pending-todo ranking; both new
      todos need an entry here or they drift out of sync silently
  </read_first>
  <action>
    (a) Add one test to `tests/unit/test_ic_engine_compute_split.py` named
    `test_compute_symbol_tf_has_no_context_features_daily_path`. Mirror
    `test_compute_symbol_tf_has_no_db_write_code`'s `inspect.getsource()` style: assert the source of
    `_compute_symbol_tf` contains none of the strings `CONTEXT_FEATURES`, `context_features`, or
    `min_obs_daily`. This is the CI enforcement for D-01 — without it, a future edit can quietly
    reintroduce a hand-maintained broadcast frozenset, which is exactly the failure mode todo 270's
    own comment called out as "the cautionary example why."

    (b) File `.planning/todos/pending/354-context-features-intraday-autocorrelation-per-symbol-pooled-cell.md`.
    Content: deleting the daily-cadence path means `vix_z`/`yield_slope_z`/`flight_quality` are now
    measured in `_compute_symbol_tf`'s ordinary intraday path only, where a single daily value is
    duplicated across every intraday bar of that day (~78x at 5m). The deleted block's own comment
    documented this as the reason the daily path existed. Phase 173 does not fix it: this phase's
    scope is cross-sectional pseudo-replication (independence across SYMBOLS), and this is temporal
    pseudo-replication (independence across BARS within one symbol) — a different bug in a different
    cell. Record that the broadcast cell built in Plan 173-04 does NOT cover the per-symbol path.
    Record the count Task 1's pre-deletion gate actually observed (expected 0) and the timestamp of
    that observation, so a later reader can see the deletion regressed no live measurement without
    re-deriving it. Propose the candidate fix (a temporal-decimation stride for broadcast features
    in the per-symbol pooled cell, analogous to `subsample_min_stride`) without implementing it.
    Assign a priority in `PRIORITIES.md`.

    (c) File `.planning/todos/pending/355-context-features-writer-orphaned-after-phase-173.md`.
    Content: `scripts/infrastructure/backfill/infrastructure_context_features_writer.py` writes 2,995
    daily rows each for `flight_quality`/`yield_slope_z`/`vix_z` into the `context_features` table
    whose docstring names the IC engine as its consumer. After Phase 173, `ic_engine.py` has no query
    against that table. Record the decision needed (retire the writer and drop the table, or repoint
    it at a real consumer) and that this phase deliberately did NOT decide it — the equivalent
    columns exist in `feature_vectors` and are still measured, so nothing is lost, but an unowned
    daily writer is exactly the kind of drift CLAUDE.md's DAG-invariant discipline exists to catch.
    Assign a priority in `PRIORITIES.md`.

    Both todos follow the existing pending-todo file format (read todo 270's file for the shape) and
    continue the existing numeric sequence — the highest number in `pending/` plus `completed/` at
    planning time was 353, so 354 and 355 are expected to be free. RE-VERIFY with
    `ls .planning/todos/pending/ .planning/todos/completed/ | grep -oE '^[0-9]+' | sort -n | tail -1`
    and shift both numbers if another session has filed since.
  </action>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_ic_engine_compute_split.py::test_compute_symbol_tf_has_no_context_features_daily_path -q` passes
    - That test fails if `CONTEXT_FEATURES` is reintroduced into `_compute_symbol_tf` — verify by temporarily adding the string in a comment inside the function, running the test to confirm RED, then reverting
    - `.planning/todos/pending/354-context-features-intraday-autocorrelation-per-symbol-pooled-cell.md` exists, states explicitly that Phase 173's broadcast cell does NOT cover the per-symbol path, and records the pre-deletion gate's observed count and timestamp
    - `.planning/todos/pending/355-context-features-writer-orphaned-after-phase-173.md` exists and names `scripts/infrastructure/backfill/infrastructure_context_features_writer.py`
    - `grep -c "354" .planning/todos/PRIORITIES.md` returns at least 1 and `grep -c "355" .planning/todos/PRIORITIES.md` returns at least 1
    - `.venv/bin/pytest tests/unit/ -q` passes with zero failures
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_ic_engine_compute_split.py -q && test -f .planning/todos/pending/354-context-features-intraday-autocorrelation-per-symbol-pooled-cell.md && test -f .planning/todos/pending/355-context-features-writer-orphaned-after-phase-173.md</automated>
  </verify>
  <done>D-01's deletion is CI-enforced against reintroduction, and both consequences of the deletion are captured as ranked todos rather than left as silent drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | This plan is pure deletion within an existing internal batch process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-04 | Tampering | `apr_snapshot_key` moves when `min_obs_daily` leaves `_COMPUTATIONAL_CONFIG_FIELDS`, invalidating every cell fingerprint | accept | Intended and required: this phase changes IC scores corpus-wide, so a full recompute is correct, not collateral damage. Documented in the SUMMARY so the mass invalidation is never mistaken for a fingerprint bug. |
| T-173-05 | Information Disclosure | Deleting a measurement path could silently drop existing results | mitigate | Task 1 Step 0 is an executable pre-deletion gate: a live `SELECT count(*) FROM feature_ic_scores WHERE regime_label_source='context_features'` must return 0 in the executing run, or the task HARD-STOPS with no file edited and escalates the affected `training_window_end`/`tf` breakdown. The same query is an acceptance criterion and is the first clause of the automated verify, so a nonzero count fails the task rather than logging a warning. |
| T-173-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed by this plan. Package Legitimacy Gate is N/A. |
</threat_model>

<verification>
- The pre-deletion gate query returned 0 in the executing run and is recorded in the SUMMARY
- `.venv/bin/pytest tests/unit/ -q` green
- `.venv/bin/ruff check services/ic_engine.py tests/unit/test_ic_engine_compute_split.py` clean
- `.venv/bin/black --check services/ic_engine.py tests/unit/test_ic_engine_compute_split.py` clean
- `grep -c "CONTEXT_FEATURES\|context_features\|min_obs_daily" services/ic_engine.py` returns 0
- `.venv/bin/python -c "import services.ic_engine"` exits 0
</verification>

<success_criteria>
- The deletion was authorized by a live zero-row proof taken during execution, not by a planning-time note
- The 231-redundant-tests bug is gone from production code, not merely superseded
- `CONTEXT_FEATURES` cannot be reintroduced without a CI failure
- No existing ic_engine test regressed
- Both downstream consequences are filed todos with PRIORITIES.md entries
</success_criteria>

<output>
Create `.planning/phases/173-broadcast-feature-significance-correction-fix-pooled-cross-s/173-02-SUMMARY.md` when done
</output>
