---
phase: 183-research-layer-runner-ledger-combiner-book-test
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/intelligence/research/spec.py
  - src/intelligence/research/provenance.py
  - tests/unit/research/test_spec.py
  - tests/unit/research/test_runner_git.py
autonomous: true
requirements: [D-01, D-02, D-18]

must_haves:
  truths:
    - "A family or book spec parses from YAML into a strict schema that rejects YAML type traps, unknown keys and dotted paths outside src.intelligence.research.families."
    - "The spec hash is sha256 over canonical JSON of the parsed spec, so whitespace and comments do not change it, and a book's hash changes when any family spec it names changes"
    - "Real-mode provenance refuses an untracked, staged-only or modified spec, and any dirty or untracked file under the checked paths, and reads the spec from the HEAD blob"
  artifacts:
    - path: "src/intelligence/research/spec.py"
      provides: "pydantic spec schema, canonical hash, HEAD-blob and file loaders, member resolver"
      exports: ["FamilySpec", "BookSpec", "LoadedSpec", "parse_spec_text", "canonical_json", "load_spec_from_head", "load_spec_from_file", "resolve_member", "MEMBER_PREFIX"]
    - path: "src/intelligence/research/provenance.py"
      provides: "git refusal checks (D-02)"
      exports: ["ProvenanceRefusal", "repo_root", "head_commit", "require_committed", "dirty_paths", "loaded_first_party_files", "require_clean"]
  key_links:
    - from: "spec.load_spec_from_head"
      to: "git show HEAD:<path>"
      via: "parse the committed blob, never the working file"
      pattern: "show.*HEAD:"
    - from: "spec.resolve_member"
      to: "importlib.import_module"
      via: "prefix allow-list checked before import"
      pattern: "MEMBER_PREFIX"
---

<objective>
Build the spec-as-pre-registration layer (architecture step 4, D-01) and the git provenance
checks the runner refuses on (D-02): a strict pydantic schema for family and book specs, the
canonical hash, loaders that read the committed HEAD blob, the dotted-path resolver with a
prefix allow-list, and git plumbing checks verified against throwaway repositories.

Purpose: the runner (plans 07 and 09) hashes and refuses on these; every constant a run depends
on lives in the spec, the one reviewed place. The schema below is the contract the family spec
(plan 07) and the book spec (plan 09) are written against.

Output: `spec.py`, `provenance.py`, two test files.

Worktree discipline (D-18): work only in `/home/bg/dev/indicagent-183` (branch
`phase-183-research-layer`); every Bash command starts with `cd /home/bg/dev/indicagent-183 &&`;
never read or write `/home/bg/dev/indicagent`. Use `.venv/bin/...`. No AI attribution or
Co-Authored-By in commits. No em dashes.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-CONTEXT.md
@.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-RESEARCH.md
@.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-PATTERNS.md
@docs/plans/2026-09-25-family1-intraday-periodicity-prereg.md
@scripts/analysis/sleeve_walk_forward/run.py

<interfaces>
Contract this plan creates. Plans 05, 06, 07, 08, 09 are written against these names; do not
rename. Every model uses `model_config = ConfigDict(extra="forbid", strict=True, frozen=True)`.
Dates are quoted ISO strings validated with a `^\d{4}-\d{2}-\d{2}$` pattern (PyYAML turns an
unquoted date into datetime.date, which strict str rejects: that rejection is intended).

spec.py
- `MEMBER_PREFIX = "src.intelligence.research.families."`
- `class PanelSpec`: universe: Literal["compute_eligible", "compute_eligible_1d", "live_tradeable"]; tf: str; bars_per_session: int (>= 1); start: str; end_exclusive: str
- `class MemberSpec`: name: str (pattern `^[a-z][a-z0-9_]*$`); signal: str (must start with MEMBER_PREFIX); params: dict[str, int | float]; slot_history_sessions: int (>= 1); declared_memory_rows: int (>= 1)
- `class ConstructionSpec`: name: Literal["rank_vol_neutral"]; direction: int (validator: in {-1, 1}); coverage_floor: int (>= 2); vol_window_sessions: int (>= 2); vol_min_finite_fraction: float (0 < x <= 1)
- `class ScoringSpec`: session_scoring: bool; trading_start: str; sub_periods: list[tuple[str, str]] (non-empty, each start <= end, ordered); min_shift: int (sessions); bootstrap_mean_block: int (sessions); bootstrap_reps: int; seed: int; warmup_sessions: int; calibration_refit_sessions: int; coverage_fraction: float; ridge_epsilon_fraction: float; mv_condition_max: float; ic_shrinkage_k: float. Method `evaluation_config() -> ResearchEvaluationConfig` returning a frozen dataclass (defined in spec.py) that satisfies evaluate.EvaluationConfig (fields trading_start, sub_periods as a tuple of tuples, min_shift, bootstrap_mean_block, bootstrap_reps, seed, warmup_sessions, calibration_refit_sessions, coverage_fraction, ridge_epsilon_fraction).
- `class GuardSpec`: seed: int; n_random: int; max_rows: int; s1_probe_sessions: int
- `class CostSpec`: bps_low: float; bps_high: float (bps_low <= bps_high); diagnostics only, never a gate
- `class FamilySpec`: kind: Literal["family"]; family: str (pattern as member name); prior_source: str (repo-relative path); horizon: int (>= 1); factor_spec: Literal["vintage_1"]; panel: PanelSpec; members: list[MemberSpec] (min 1, unique names); construction: ConstructionSpec; scoring: ScoringSpec; guards: GuardSpec; costs: CostSpec
- `class CombinerSpec`: window_sessions: int; refit_sessions: int; penalty: float (> 0); min_obs: int
- `class PowerSpec`: planted_rank_ic: float; participation_ratio: float; n_common_factors: int; plant_lags_sessions: int; replicates: int; seed: int; calibration_panels: int; calibration_seed: int; calibration_tolerance: float; stop_check_every: int
- `class BookSpec`: kind: Literal["book"]; book: str; prior_source: str; families: list[str] (repo-relative paths under research/specs/, min 1, unique); combiner: CombinerSpec; construction: ConstructionSpec; scoring: ScoringSpec; guards: GuardSpec; costs: CostSpec; power: PowerSpec
- `@dataclass(frozen=True) class LoadedSpec`: path: str; model: FamilySpec | BookSpec; canonical: str; spec_hash: str; blob_sha: str | None; families: tuple["LoadedSpec", ...] = ()
- `def parse_spec_text(text: str) -> FamilySpec | BookSpec` (yaml.safe_load, dispatch on "kind", model_validate)
- `def canonical_json(obj: dict) -> str` = json.dumps(obj, sort_keys=True, separators=(",", ":"), ensure_ascii=False, allow_nan=False)
- `def load_spec_from_head(root: Path, rel: str) -> LoadedSpec` (text from `git -C root show HEAD:rel`, blob from `git -C root rev-parse HEAD:rel`; a book loads each family from HEAD the same way)
- `def load_spec_from_file(path: Path, root: Path | None = None) -> LoadedSpec` (synthetic mode; blob_sha None; families read from the working tree)
- Hash rule: family canonical = canonical_json(model.model_dump(mode="json")); book canonical = canonical_json({"book": book.model_dump(mode="json"), "families": [json.loads(f.canonical) for f in families in listed order]}); spec_hash = sha256(canonical.encode()).hexdigest()
- `def resolve_member(dotted: str) -> Callable[..., np.ndarray]` (refuses any path not starting with MEMBER_PREFIX before importing; module part imported, attribute fetched, must be callable)
- Book validation at load: every family shares the book's panel (equal PanelSpec), horizon and factor_spec; otherwise ValueError

provenance.py (all git calls `git -C <root> ...`, list args, check as noted, capture_output, text)
- `class ProvenanceRefusal(Exception)`
- `def repo_root(start: Path) -> Path` (git rev-parse --show-toplevel)
- `def head_commit(root: Path) -> str` (git rev-parse HEAD, 40 hex)
- `def require_committed(root: Path, rel: str) -> str`: `git cat-file -e HEAD:<rel>` must exit 0 (else "not committed": ls-files is the wrong check, it passes for a staged-only file) and `git diff --quiet HEAD -- <rel>` must exit 0 (else "differs from HEAD"); returns the blob sha
- `def dirty_paths(root: Path, paths: Sequence[str]) -> list[str]`: `git status --porcelain=v1 --untracked-files=all -- <paths>` lines, empty when clean
- `def loaded_first_party_files(root: Path) -> list[str]`: repo-relative paths of `sys.modules` entries whose `__file__` resolves under root and under src/, services/ or scripts/research/
- `def require_clean(root: Path, extra: Sequence[str] = ()) -> None`: refuses when dirty_paths(root, ["src/intelligence", "research/specs", *loaded_first_party_files(root), *extra]) is non-empty, naming the files
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: spec.py strict schema, canonical hash, loaders and member resolver (D-01)</name>
  <files>src/intelligence/research/spec.py, tests/unit/research/test_spec.py</files>
  <read_first>
    - .planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-RESEARCH.md "Pitfall 1" (measured PyYAML traps), "Code examples 1", "Pattern 5" (spec content from the HEAD blob)
    - src/intelligence/research/evaluate.py lines 54-80 (EvaluationConfig fields the ResearchEvaluationConfig must carry)
    - scripts/analysis/sleeve_walk_forward/config.py (the frozen-constants precedent and values such as bootstrap_mean_block 21, bootstrap_reps 2000)
  </read_first>
  <behavior>
    - The same family spec written with different indentation, key order, blank lines and comments gives the same spec_hash
    - Changing one member param (window_sessions 5 -> 6) changes the hash
    - An unquoted `1e-4` for a float field fails validation (PyYAML parses it as a string); `1.0e-4` passes
    - An unquoted date (start: 2006-07-01) fails validation; the quoted string passes
    - `no` for a bool field and any unknown key at any level raise ValidationError
    - A member signal `os.system` or `src.intelligence.research.evaluate.evaluate` is rejected by validation and by resolve_member before any import (monkeypatched importlib.import_module is never called)
    - direction 0 or 2 is rejected; duplicate member names are rejected; sub_periods out of order are rejected
    - A book spec's hash changes when a family spec it names changes, with the book file untouched (load_spec_from_file on tmp files)
    - A book whose families disagree on panel or horizon raises ValueError at load
    - ScoringSpec.evaluation_config() satisfies every name in evaluate.ROW_SCALED_FIELDS and UNSCALED_FIELDS and dataclasses.replace works on it (rows_config needs it)
    - load_spec_from_head in a tmp git repo returns the committed content even when the working file has uncommitted edits, and records blob_sha equal to `git rev-parse HEAD:<rel>`
    - canonical_json refuses NaN (allow_nan False)
  </behavior>
  <action>
    RED: tests/unit/research/test_spec.py from the behavior list, with small YAML fixtures as
    strings in the test module (a minimal valid family spec with two members pointing at a
    dotted path under MEMBER_PREFIX; members need not exist for parse tests, resolve_member
    tests monkeypatch importlib). Commit `test(183-03): add failing tests for spec schema and hash`.

    GREEN: implement spec.py exactly per the interfaces block (D-01: one file per family,
    members listed inside, dotted paths into families/, constants in the spec). Use
    `yaml.safe_load` only, never `yaml.load` (research V5). pydantic models frozen, strict,
    extra forbidden; tuples in model_dump(mode="json") become lists, which is fine for hashing
    since canonical_json is applied to the dump. ResearchEvaluationConfig is a frozen dataclass
    in spec.py with exactly the EvaluationConfig and PortfolioConfig field names. Module
    docstring: the spec is the pre-registration (architecture 3.4), the hash rule, why the HEAD
    blob and not the working file is parsed, why dotted paths are prefix-restricted, and that
    evaluate's inert calibrated-arm knobs (warmup_sessions, calibration_refit_sessions,
    coverage_fraction, ridge_epsilon_fraction, mv_condition_max, ic_shrinkage_k) are still
    pinned because evaluate() always builds a covariance plan. No `from services` import (Ring 1).

    Commit `feat(183-03): spec schema, canonical hash and HEAD-blob loader`.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent-183 && .venv/bin/pytest tests/unit/research/test_spec.py -q && .venv/bin/ruff check src/intelligence/research/spec.py tests/unit/research/test_spec.py && ! grep -n "yaml.load(\|from services" src/intelligence/research/spec.py</automated>
  </verify>
  <acceptance_criteria>
    - test_spec.py passes with every behavior bullet as a test
    - `grep -n "safe_load" src/intelligence/research/spec.py` present; `grep -c "yaml.load(" src/intelligence/research/spec.py` is 0
    - `.venv/bin/python -c "from src.intelligence.research.spec import FamilySpec, BookSpec, LoadedSpec, parse_spec_text, canonical_json, load_spec_from_head, load_spec_from_file, resolve_member, MEMBER_PREFIX"` exits 0
  </acceptance_criteria>
  <done>Specs parse strictly, hash canonically, load from HEAD, and resolve only family members.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: provenance.py git refusal checks against throwaway repos (D-02); rebase and push</name>
  <files>src/intelligence/research/provenance.py, tests/unit/research/test_runner_git.py</files>
  <read_first>
    - .planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-RESEARCH.md "Pattern 5" (command table, verified behavior), "Pitfall 2", "Pitfall 11"
    - scripts/analysis/sleeve_walk_forward/run.py lines 60-95 (subprocess style to copy; its whole-tree status check is the wrong scope)
    - services/ic_engine.py lines 5376-5415 (loaded-module enumeration algorithm to imitate, never import)
  </read_first>
  <behavior>
    Each test builds a repo in tmp_path (`git init`, set user.name/user.email locally, commit research/specs/f.yaml and src/intelligence/x.py):
    - require_committed passes on the clean committed spec and returns `git rev-parse HEAD:research/specs/f.yaml`
    - a new spec that is only staged (git add, no commit) raises ProvenanceRefusal "not committed"
    - an untracked spec raises "not committed"
    - a committed spec with an unstaged edit, and with a staged edit, raises "differs from HEAD"
    - require_clean passes on a clean tree; raises naming the file for a modified src/intelligence/x.py (staged or unstaged), for an untracked src/intelligence/families/new.py, and for an untracked research/specs/other.yaml
    - a dirty file outside the checked paths (e.g. docs/notes.md) does not refuse
    - loaded_first_party_files, given a root containing a module imported in the test (write src/mod_probe.py under tmp root, add tmp root to sys.path, import it), returns "src/mod_probe.py"; modules outside root are excluded
    - head_commit returns 40 hex characters equal to `git rev-parse HEAD`
  </behavior>
  <action>
    RED: tests/unit/research/test_runner_git.py per the behavior list; commit
    `test(183-03): add failing tests for git provenance refusals`.

    GREEN: provenance.py per the interfaces block (D-02: refuse before touching the snapshot when
    the spec is untracked or differs from HEAD, or any tracked file under src/intelligence/ or
    the spec is dirty). Beyond D-02's named paths, research pattern 5 recommends also checking
    every first-party module actually loaded (src/core, src/config and the CLI's
    services/_batch_utils.py are loaded by a run); include untracked files because an untracked
    families/*.py could be imported by dotted path yet be absent from the recorded commit.
    Every git command uses `-C <root>` (pitfall 11: the runner resolves the root with
    `git rev-parse --show-toplevel`, never the cwd). Exception variables named `error`. No
    services import.

    Commit `feat(183-03): git provenance refusal checks`.

    Finish (D-18): `git fetch origin && git rebase origin/main`, `.venv/bin/pytest tests/unit/research -q`,
    `git push origin HEAD:main`, retrying fetch/rebase/push if rejected.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent-183 && .venv/bin/pytest tests/unit/research/test_runner_git.py tests/unit/research/test_spec.py -q && .venv/bin/ruff check src/intelligence/research/provenance.py tests/unit/research/test_runner_git.py && ! grep -n "ls-files\|from services" src/intelligence/research/provenance.py</automated>
  </verify>
  <acceptance_criteria>
    - test_runner_git.py passes, covering untracked, staged-only, modified-unstaged, modified-staged, dirty code, untracked code, clean
    - `grep -n "cat-file" src/intelligence/research/provenance.py` present; `grep -c "ls-files" src/intelligence/research/provenance.py` is 0
    - `git merge-base --is-ancestor HEAD origin/main` exits 0 after the push
  </acceptance_criteria>
  <done>Real-mode provenance refusals are implemented and proven on throwaway repositories; pushed.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| committed YAML spec -> runner | the spec names code to import and constants to run with |
| git working tree -> recorded code commit | a dirty tree would make the recorded commit a lie |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-10 | Elevation | spec dotted path naming arbitrary code | mitigate | MEMBER_PREFIX checked in the schema and again in resolve_member before importlib |
| T-183-11 | Elevation | YAML object construction | mitigate | yaml.safe_load only; grep acceptance forbids yaml.load |
| T-183-12 | Tampering | silent type coercion changing a pinned constant | mitigate | pydantic strict mode, extra forbidden, quoted dates; tests for each measured PyYAML trap |
| T-183-13 | Repudiation | a run recorded against a commit that is not the code that ran | mitigate | require_committed on the spec, require_clean over src/intelligence, research/specs and every loaded first-party module, untracked included |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/research -q`
- `.venv/bin/ruff check src/intelligence/research`
</verification>

<success_criteria>
The spec schema and hash rule are pinned and tested, provenance refusals are proven in throwaway
repos, and the plan is on origin/main.
</success_criteria>

<output>
Create `.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-03-SUMMARY.md` when done
</output>
