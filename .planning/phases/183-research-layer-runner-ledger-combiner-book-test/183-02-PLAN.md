---
phase: 183-research-layer-runner-ledger-combiner-book-test
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/366_research_run_ledger.sql
  - src/intelligence/research/ledger.py
  - tests/unit/research/test_ledger_sole_writer.py
  - tests/unit/research/test_ledger_jsonable.py
  - tests/integration/test_research_ledger.py
autonomous: true
requirements: [D-02, D-05, D-06, D-07, D-08, D-18]

must_haves:
  truths:
    - "The live indicagent DB has an append-only research_run table: rows insert as started, move exactly once to a terminal status, and can never be deleted, truncated or otherwise edited"
    - "The database itself refuses a second real-data run for the same spec hash and concept (partial unique index), not only Python"
    - "The vintage-1 budget (vintage id, M = 30, alpha 0.05) and the runner worker count are APR keys seeded by the migration"
    - "ledger.py is the only code that writes research_run rows or construction-domain concept_registry rows, and a unit test fails on any other writer"
    - "A book test cannot start once the charged count for the vintage has reached M; refused and guard_failed book tests and all evidence runs are uncharged, started/completed/failed book tests are charged"
  artifacts:
    - path: "production/migrations/366_research_run_ledger.sql"
      provides: "research_run table, terminal-once and no-delete triggers, partial unique index, APR seeds"
      contains: "research_run_real_spec_once"
    - path: "src/intelligence/research/ledger.py"
      provides: "S6 sole writer: identity rows, concept_parent edges, research_run start/finish, budget count, jsonable"
      exports: ["PostgresLedger", "Identity", "RunRequest", "LedgerRefusal", "jsonable", "TERMINAL_STATUSES", "UNCHARGED_STATUSES", "LEDGER_LOCK_KEY"]
    - path: "tests/unit/research/test_ledger_sole_writer.py"
      provides: "repo-grep sole-writer guard (D-05)"
    - path: "tests/integration/test_research_ledger.py"
      provides: "DB-level proof of D-02/D-07/D-08"
  key_links:
    - from: "src/intelligence/research/ledger.py"
      to: "research_run / concept_registry / concept_parent"
      via: "one transaction under pg_advisory_xact_lock(LEDGER_LOCK_KEY)"
      pattern: "pg_advisory_xact_lock"
    - from: "src/intelligence/research/ledger.py"
      to: "src/core/database_manager.connect_with_codecs"
      via: "jsonb codec on every ledger connection"
      pattern: "connect_with_codecs"
---

<objective>
Build the S6 ledger (architecture step 5): migration 366 (the `research_run` run-record table,
its append-only triggers, the spec-once partial unique index, and the vintage-1 APR keys),
applied live and committed in the same step, and `src/intelligence/research/ledger.py`, the
sole writer of run records and construction-domain identity rows (D-05 to D-08).

Purpose: the runner (plan 07) must write a `started` row before touching the snapshot and a
terminal row after, and the book test (plan 09) must be budget-charged. Every invariant the
framework states about the ledger is enforced by the database, not only by Python.

Output: migration 366 (live, committed), ledger.py, a sole-writer grep test, a jsonable unit
test, an integration test against `indicagent_test`.

Worktree discipline (D-18): work only in `/home/bg/dev/indicagent-183` (branch
`phase-183-research-layer`). Every Bash command starts with `cd /home/bg/dev/indicagent-183 &&`;
never read or write `/home/bg/dev/indicagent` (phase 182 executes there). Use `.venv/bin/...`
from this worktree. Commit messages carry no AI attribution and no Co-Authored-By line. No em
dashes in SQL comments, code or docs.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-CONTEXT.md
@.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-RESEARCH.md
@.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-PATTERNS.md
@production/migrations/357_concept_evaluation_ledger.sql
@production/migrations/283_concept_registry_feature_domain_schema.sql
@production/migrations/340_ic_engine_streaming_correlation_apr_key.sql
@src/core/database_manager.py
@tests/integration/test_concept_parent_lineage.py
@tests/unit/_source_grep_helpers.py
@tests/unit/test_market_data_ohlcv_boundary.py

<interfaces>
Live schema facts (read from the DB 2026-09-25):
- `concept_registry(concept_id uuid PK default gen_random_uuid(), domain text CHECK in ('feature','ensemble_strategy','construction'), name text, description text NOT NULL, status text default 'candidate' CHECK in ('candidate','shadow_only','active','deprecated'), enabled bool default false, parent_concept_id uuid, redundancy_group text, metadata jsonb, added_phase text, sensitivity text, created_at, is_control bool default false, control_expectation text, group_name text)`, `UNIQUE (domain, name)`.
- `concept_parent(child_concept_id uuid, parent_concept_id uuid, created_at)`, `PRIMARY KEY (child_concept_id, parent_concept_id)`, CHECK child <> parent, cycle-guard trigger from migration 283.
- APR value_type values in use: 'int', 'float', 'str', 'bool', 'json'. `alpha.validation.oos_start` = `2025-12-24T05:15:00Z`. `infra.ic_engine.workers` = 8.
- `connect_with_codecs(database_url: str) -> asyncpg.Connection` in src/core/database_manager.py.

Contract this plan creates in `src/intelligence/research/ledger.py` (plans 07 and 09 are written against exactly these names):
- `LEDGER_LOCK_KEY: int = 183366` (a fixed advisory-lock key, a schema identifier, APR-exempt)
- `TERMINAL_STATUSES = ("completed", "guard_failed", "refused", "failed")`
- `UNCHARGED_STATUSES = ("refused", "guard_failed")`
- `class LedgerRefusal(Exception)`: a run the ledger will not start (prior real run for the spec hash; budget exhausted)
- `@dataclass(frozen=True) class Identity: name: str; kind: Literal["family", "member", "book_version"]; description: str; metadata: dict; parents: tuple[str, ...] = ()` (parents are identity names; edges child -> parent)
- `@dataclass(frozen=True) class RunRequest: kind: Literal["evidence", "book_test"]; spec_path: str; spec_hash: str; spec_blob: str; code_commit: str; vintage: str; budget_m: int | None = None; screen_alpha: float | None = None`
- `def jsonable(value: Any) -> Any`: recursive; numpy scalars to Python, numpy arrays via tolist, NaN and +-inf to None, tuples to lists, dict keys to str; raises TypeError on any other non-JSON type
- `class PostgresLedger: __init__(self, dsn: str)`; each method opens its own connection with connect_with_codecs and closes it (a 6-hour run never holds an idle connection):
  - `async def has_real_run(self, spec_hash: str) -> bool`
  - `async def charged_book_tests(self, vintage: str) -> int`
  - `async def start_runs(self, request: RunRequest, identities: Sequence[Identity], run_concepts: Sequence[str]) -> dict[str, str]` returns {concept name: run_id} for each name in run_concepts, all rows sharing one new run_group uuid
  - `async def finish_run(self, run_id: str, *, status: str, snapshot_hash: str | None, evidence: dict) -> None`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 366, research_run ledger and vintage-1 APR keys; apply live and commit immediately</name>
  <files>production/migrations/366_research_run_ledger.sql</files>
  <read_first>
    - production/migrations/357_concept_evaluation_ledger.sql (header style, table/index/constraint naming, COMMENT ON)
    - production/migrations/283_concept_registry_feature_domain_schema.sql lines 1-160 (advisory-lock writer contract wording; trigger function and DROP/CREATE TRIGGER shape)
    - production/migrations/340_ic_engine_streaming_correlation_apr_key.sql (two-INSERT APR seed idiom)
    - .planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-RESEARCH.md "Code examples 4", "Pitfall 7", "Pattern 6"
  </read_first>
  <action>
    Confirm the number first (D-06, D-18): `git fetch origin && git ls-tree origin/main production/migrations/ | awk '{print $4}' | sort | tail -5` and `ls production/migrations | sort | tail -3`. Phase 182 holds 364 and 365. If 366 is already taken on origin/main, use the next free number, use it consistently in the filename and header, and record the number used in the SUMMARY (plans 07/09/10 refer to "migration 366" meaning this migration).

    Write the migration inside BEGIN/COMMIT. Header comment (357 style): what research_run is
    (S6 run records of the research layer, phase 183, one row per evidence measurement or book
    test); why concept_evaluation is not reused (D-06: its primary key, lifecycle
    evaluated_status and guard_status enumerations belong to the feature lifecycle and do not
    fit a started/terminal run state; a deliberate deviation from evidence framework draft 3's
    sketch); identity lives in concept_registry(domain='construction') and lineage in
    concept_parent, written only by src/intelligence/research/ledger.py; the writer contract
    (283 wording): any writer of research_run, construction identity rows or their
    concept_parent edges takes pg_advisory_xact_lock(183366) first, then counts and inserts in
    the same transaction; row volume order of magnitude (tens per vintage); "Not a hypertable;
    no VACUUM step". Do not use IF NOT EXISTS on the table: a second apply must fail loudly.

    Table research_run (per D-06, D-07, research code example 4):
    run_id uuid PRIMARY KEY DEFAULT gen_random_uuid(); run_group uuid NOT NULL; concept_id uuid
    NOT NULL REFERENCES concept_registry (concept_id); kind text NOT NULL CHECK in ('evidence',
    'book_test'); mode text NOT NULL CHECK (mode = 'real') (D-04 writes no synthetic rows);
    spec_path text NOT NULL; spec_hash text NOT NULL CHECK (spec_hash ~ '^[0-9a-f]{64}$');
    spec_blob text NOT NULL; code_commit text NOT NULL CHECK (code_commit ~ '^[0-9a-f]{40}$');
    snapshot_hash text; vintage text NOT NULL; budget_m integer; screen_alpha double precision;
    status text NOT NULL DEFAULT 'started' CHECK in ('started','completed','guard_failed',
    'refused','failed'); evidence jsonb; started_at timestamptz NOT NULL DEFAULT now();
    finished_at timestamptz. Named CHECK constraints `research_run_<column>_check`, plus
    `research_run_book_budget_check`: kind = 'evidence' OR (budget_m IS NOT NULL AND
    screen_alpha IS NOT NULL). Indexes: `CREATE UNIQUE INDEX research_run_real_spec_once ON
    research_run (spec_hash, concept_id) WHERE mode = 'real'` (backs D-02 in the database) and
    `research_run_budget_idx ON research_run (vintage, kind, status)`.

    Triggers (283's function-then-DROP-then-CREATE shape, RAISE EXCEPTION ... USING ERRCODE =
    'check_violation', messages starting "research_run is append-only"):
    - fn_research_run_guard BEFORE INSERT OR UPDATE FOR EACH ROW. On INSERT: NEW.status must be
      'started' and NEW.evidence, NEW.finished_at, NEW.snapshot_hash must be NULL. On UPDATE:
      OLD.status must be 'started' (a terminal row never changes again); NEW.status must be one
      of the four terminal statuses; NEW.finished_at must be NOT NULL; every column other than
      status, snapshot_hash, evidence, finished_at must be IS NOT DISTINCT FROM its OLD value;
      snapshot_hash may go from NULL to a value, or stay equal, never change from one value to
      another (pitfall 7).
    - fn_research_run_no_delete: BEFORE DELETE FOR EACH ROW and a separate BEFORE TRUNCATE FOR
      EACH STATEMENT trigger, both raising unconditionally.
    COMMENT ON TABLE and on each function.

    APR seeds (D-08), 340's two-INSERT idiom with ON CONFLICT (config_key) DO NOTHING:
    - alpha.research.vintage_id, 'str', 'vintage_1': "[user_preference] Research vintage the
      screen budget is charged against. Vintage 1 = data before alpha.validation.oos_start
      (2025-12-24). A change needs a methodology-change-ledger entry
      (docs/plans/methodology-change-ledger.md). Not an ML learning target."
    - alpha.research.budget_m, 'int', '30', min 1: "[user_preference] M, the number of book
      versions that may be screen-tested on the vintage (evidence framework E15); bar is
      screen_alpha / M. A change needs a methodology-change-ledger entry ... Not an ML learning
      target."
    - alpha.research.screen_alpha, 'float', '0.05', min 0 max 1: same provenance wording,
      one-sided family alpha before division by M.
    - infra.research_runner.workers, 'int', '8', min 1: "[initial_estimate] Worker processes for
      the research runner's shift null and power replicates. Throughput only; results do not
      depend on it. Matches infra.ic_engine.workers. Not an ML learning target."

    Apply live immediately:
    `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -v ON_ERROR_STOP=1 -f production/migrations/366_research_run_ledger.sql`
    then in the same step `git add production/migrations/366_research_run_ledger.sql && git commit -m "feat(183-02): research_run ledger migration 366 (applied live)"` (CLAUDE.md: a live-applied migration is committed in the same breath).
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent-183 && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -At -c "SELECT count(*) FROM pg_indexes WHERE indexname='research_run_real_spec_once' AND indexdef LIKE '%WHERE (mode = %real%'" | grep -qx 1 && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -At -c "SELECT count(*) FROM config_state WHERE config_key IN ('alpha.research.vintage_id','alpha.research.budget_m','alpha.research.screen_alpha','infra.research_runner.workers')" | grep -qx 4 && .venv/bin/pytest tests/unit/test_migration_number_uniqueness.py -q && git log -1 --name-only | grep -q "_research_run_ledger.sql"</automated>
  </verify>
  <acceptance_criteria>
    - `\d research_run` in the live DB shows the columns above, the partial unique index research_run_real_spec_once with predicate `mode = 'real'`, and the budget index
    - `SELECT tgname FROM pg_trigger WHERE tgrelid='research_run'::regclass AND NOT tgisinternal` lists the insert/update guard, the delete guard and the truncate guard
    - `SELECT config_value FROM config_state WHERE config_key='alpha.research.budget_m'` returns 30; screen_alpha 0.05; vintage_id vintage_1; infra.research_runner.workers 8
    - `grep -c "concept_evaluation" production/migrations/366_research_run_ledger.sql` is at least 1 (the D-06 deviation is recorded)
    - The migration file is in HEAD's commit
  </acceptance_criteria>
  <done>research_run is live with DB-enforced append-only and spec-once rules, APR keys seeded, migration committed in the same step.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ledger.py sole writer, jsonable, and the sole-writer grep test (D-05, D-06, D-08)</name>
  <files>src/intelligence/research/ledger.py, tests/unit/research/test_ledger_jsonable.py, tests/unit/research/test_ledger_sole_writer.py</files>
  <read_first>
    - src/core/database_manager.py lines 19-41 (connect_with_codecs)
    - tests/unit/_source_grep_helpers.py (find_pattern_references, assert_no_unlisted_references, assert_allow_list_has_no_stale_entries)
    - tests/unit/test_market_data_ohlcv_boundary.py (allow-list test shape, reason strings)
    - production/migrations/329_concept_registry_construction_domain.sql and 328, 330, 333, 334, 335 (legacy construction rows to allow-list)
    - .planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-RESEARCH.md "Pattern 6", "Pitfall 8", "Pitfall 9"
  </read_first>
  <behavior>
    - jsonable({"a": np.float32(1.5), "b": np.array([1.0, np.nan]), "c": float("inf"), "d": (np.int64(2),), 3: None}) == {"a": 1.5, "b": [1.0, None], "c": None, "d": [2], "3": None}; json.dumps(result, allow_nan=False) succeeds
    - jsonable of nested dicts/lists recurses; jsonable(object()) raises TypeError; jsonable(np.bool_(True)) is True
    - Sole-writer scan: every file under services/, src/, scripts/, production/ (py and sql) that writes research_run (INSERT INTO / UPDATE / DELETE FROM research_run) or inserts a concept_registry row whose statement text names 'construction' is on the allow-list; the allow-list has no stale entries; src/intelligence/research/ledger.py and the 366 migration are allowed; the legacy verdict migrations are allowed with PERMANENT reasons
  </behavior>
  <action>
    RED then GREEN for jsonable; the grep test is written against the finished module.

    ledger.py (D-05, D-06, D-07, D-08), module docstring: S6 sole writer of research_run and of
    concept_registry rows with domain='construction' and their concept_parent edges; restates
    the advisory-lock contract; states it never updates concept_registry.status (UCR Invariant
    1: only ConceptRegistryService flips status); every connection comes from
    connect_with_codecs. Implement the interfaces-block contract exactly.

    start_runs, one transaction on one connection: `SELECT pg_advisory_xact_lock($1)` with
    LEDGER_LOCK_KEY as the first statement; if a research_run row with mode='real' exists for
    request.spec_hash raise LedgerRefusal ("spec already run", D-02); if request.kind ==
    'book_test': require budget_m and screen_alpha, count charged rows (`kind = 'book_test' AND
    vintage = $1 AND status NOT IN ('refused','guard_failed')`, so started, completed and failed
    count, D-08) and raise LedgerRefusal when count >= budget_m; insert every identity with
    `INSERT INTO concept_registry (domain, name, description, status, enabled, metadata,
    added_phase) VALUES ('construction', $1, $2, 'candidate', false, $3, '183') ON CONFLICT
    (domain, name) DO NOTHING`, metadata carrying {"kind": identity.kind, ...identity.metadata};
    read back concept_id by (domain, name); insert every parent edge with ON CONFLICT DO NOTHING;
    insert one research_run row per run_concepts name with a shared run_group
    (uuid.uuid4()), mode 'real', status defaulted, and return {name: str(run_id)}. All SQL uses
    $n parameters, never string formatting of spec values.

    finish_run: status must be in TERMINAL_STATUSES (ValueError otherwise); serialize
    `json.dumps(jsonable(evidence), allow_nan=False)` before touching the DB; if that raises
    TypeError or ValueError, write status 'failed' with evidence {"error": "evidence not
    serializable: <message>"} and then re-raise, so a row never stays started because the final
    write crashed (pitfall 8). One UPDATE setting status, snapshot_hash, evidence (the dict, via
    the jsonb codec), finished_at = now().

    has_real_run and charged_book_tests are single SELECTs. No numeric literal besides the lock
    key; M and alpha always come from the request (the runner reads them from APR, plan 07).
    Exception variables are named `error`.

    test_ledger_sole_writer.py: modeled on test_market_data_ohlcv_boundary.py with the shared
    helpers. Two patterns, compiled case-insensitive with DOTALL:
    `(INSERT\s+INTO|UPDATE|DELETE\s+FROM)\s+research_run\b` and
    `INSERT\s+INTO\s+concept_registry\b[^;]{0,3000}?'construction'`. Scan ("services", "src",
    "scripts", "production") with globs ("*.py", "*.sql"). Run the scan once first and
    allow-list exactly what it finds besides ledger.py and the 366 migration, each with a reason
    ("PERMANENT: legacy construction-domain verdict row inserted by migration before ledger.py
    became the sole writer (phase 183, D-05)" for the 328-335 migrations; for any analysis
    script hit, read the file and give the specific reason, or if it is a runtime writer, stop
    and report it rather than allow-listing silently). Two tests: no unlisted references, no
    stale entries. Tests directories are not scanned (the integration test issues raw UPDATE and
    DELETE statements to prove the triggers refuse them).

    Commit `feat(183-02): research ledger sole writer and sole-writer guard`.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent-183 && .venv/bin/pytest tests/unit/research/test_ledger_jsonable.py tests/unit/research/test_ledger_sole_writer.py -q && .venv/bin/ruff check src/intelligence/research/ledger.py tests/unit/research && .venv/bin/python -c "from src.intelligence.research.ledger import PostgresLedger, Identity, RunRequest, LedgerRefusal, jsonable, TERMINAL_STATUSES, UNCHARGED_STATUSES, LEDGER_LOCK_KEY" && ! grep -rn "from services" src/intelligence/research/ledger.py</automated>
  </verify>
  <acceptance_criteria>
    - Both unit test files pass
    - `grep -n "pg_advisory_xact_lock" src/intelligence/research/ledger.py` finds the lock as the first statement in start_runs' transaction
    - `grep -c "UPDATE concept_registry" src/intelligence/research/ledger.py` is 0 (Invariant 1)
    - `grep -n "connect_with_codecs" src/intelligence/research/ledger.py` present
    - Temporarily adding a file `src/intelligence/research/_probe.py` containing `INSERT INTO research_run` makes test_ledger_sole_writer fail (checked, then the probe file deleted before commit)
  </acceptance_criteria>
  <done>ledger.py implements the pinned contract; the grep guard enforces it as the sole writer.</done>
</task>

<task type="auto">
  <name>Task 3: Integration test proving D-02, D-07 and D-08 in the database; rebase and push</name>
  <files>tests/integration/test_research_ledger.py</files>
  <read_first>
    - tests/integration/test_concept_parent_lineage.py (fixture, _TrackedConnection, unique names, cleanup, trigger-assertion idiom, surviving-state assertion)
    - tests/integration/conftest.py (migrated_test_database fixture, cutoff 234, how migrations are replayed into indicagent_test)
    - src/intelligence/research/ledger.py (from Task 2)
  </read_first>
  <action>
    Write tests/integration/test_research_ledger.py, `pytestmark = [pytest.mark.integration,
    pytest.mark.requires_db]`, against `postgresql://postgres:postgres@localhost:5432/indicagent_test`
    (PostgresLedger takes that DSN). Unique names `f"_p183_{uuid4().hex[:8]}"`; unique fake spec
    hashes (`uuid4().hex * 2`, 64 hex) and commits (40 hex). Cleanup cannot DELETE research_run
    rows (the trigger forbids it, by design): in the fixture teardown, disable the two
    research_run triggers inside a transaction (`ALTER TABLE research_run DISABLE TRIGGER
    USER`), delete the test's rows, re-enable (`ENABLE TRIGGER USER`), then delete its
    concept_parent edges and concept_registry rows; this runs only against indicagent_test.
    Test names contain the -k selectors the validation map uses:
    - test_spec_once_python: start_runs twice for the same spec_hash raises LedgerRefusal the second time
    - test_spec_once_index: a raw second INSERT of a real row with the same (spec_hash, concept_id) raises asyncpg.UniqueViolationError
    - test_trigger_started_to_terminal_once: finish_run completed succeeds; a second finish_run raises asyncpg.PostgresError matching "append-only"; the row's status is still completed afterwards
    - test_trigger_rejects_other_column_edits: a raw UPDATE of spec_hash or code_commit on a started row raises; a raw UPDATE moving snapshot_hash from one value to another raises; NULL to value together with a terminal status succeeds
    - test_trigger_insert_must_be_started: a raw INSERT with status 'completed' raises
    - test_trigger_delete_and_truncate_rejected: DELETE and TRUNCATE on research_run raise
    - test_budget_charge_rules: with budget_m 3 and a unique vintage string: book tests ending refused and guard_failed plus evidence runs do not count; a started, a completed and a failed book test count; the next start_runs for a book test raises LedgerRefusal; charged_book_tests returns 3
    - test_budget_apr_keys_seeded: the four APR keys exist in indicagent_test's config_state with the seeded values
    - test_identity_rows_and_edges: start_runs with a family identity and two member identities (parents = family) creates three construction rows with status 'candidate', enabled false, added_phase '183', and two concept_parent edges; a second start_runs (different spec hash) with the same identities creates no duplicates
    - test_evidence_roundtrip: finish_run with a dict containing numpy values and NaN stores JSON with null in place of NaN, read back as a dict (connect_with_codecs)
    - test_finish_run_unserializable_writes_failed: evidence containing object() leaves the row status 'failed' with an "error" key and raises TypeError

    Run: `.venv/bin/pytest tests/integration/test_research_ledger.py -m integration -q`. Also run
    `.venv/bin/pytest tests/integration/test_migration_schema_sync.py -m integration -q` and
    record its result in the SUMMARY; if it fails only because of phase 182's migration 365
    state (not 366), note that and do not edit phase 182 files.

    Commit `test(183-02): integration tests for research_run append-only, spec-once and budget`.
    Then (D-18): `git fetch origin && git rebase origin/main`, `.venv/bin/pytest tests/unit/research -q`,
    `git push origin HEAD:main`; on rejection fetch, rebase, push again.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent-183 && .venv/bin/pytest tests/integration/test_research_ledger.py -m integration -q && .venv/bin/pytest tests/unit/research -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/integration/test_research_ledger.py -m integration -q` passes with at least 11 tests
    - `-k spec_once`, `-k trigger`, `-k budget` each select at least one test
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -At -c "SELECT count(*) FROM research_run"` returns 0 on the live DB (tests never touched it)
    - `git merge-base --is-ancestor HEAD origin/main` exits 0 after the push
  </acceptance_criteria>
  <done>The database-level ledger rules are proven by tests, and the plan is on origin/main.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| runner -> ledger -> live DB | the only write path from research code into Postgres |
| migration file -> live DB | DDL applied by psql |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-04 | Repudiation | research_run rows edited or deleted after a result is seen | mitigate | BEFORE INSERT/UPDATE guard, BEFORE DELETE and TRUNCATE triggers; integration tests prove each |
| T-183-05 | Tampering | a spec re-run until it passes | mitigate | Python LedgerRefusal plus partial unique index research_run_real_spec_once |
| T-183-06 | Elevation | concurrent runners both passing the budget count | mitigate | pg_advisory_xact_lock(183366) then count then insert in one transaction |
| T-183-07 | Tampering | SQL built from spec strings | mitigate | asyncpg $n parameters only; no f-string SQL in ledger.py |
| T-183-08 | Tampering | a second module writing construction rows | mitigate | repo-grep sole-writer test with a two-way allow-list |
| T-183-09 | Denial of service | row stuck in started because the terminal write crashed on NaN | mitigate | jsonable plus allow_nan=False pre-serialization; failure writes 'failed' with the error |
</threat_model>

<verification>
- Live schema checks in Task 1's verify
- `.venv/bin/pytest tests/unit/research -q`
- `.venv/bin/pytest tests/integration/test_research_ledger.py -m integration -q`
</verification>

<success_criteria>
Migration 366 is live and committed; ledger.py is the tested sole writer; the database enforces
append-only, spec-once and (with ledger.py) the M = 30 budget; pushed to origin/main.
</success_criteria>

<output>
Create `.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-02-SUMMARY.md` when done
</output>
