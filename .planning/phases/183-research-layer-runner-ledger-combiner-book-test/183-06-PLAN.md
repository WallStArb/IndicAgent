---
phase: 183-research-layer-runner-ledger-combiner-book-test
plan: 06
type: execute
wave: 2
depends_on: ["183-01", "183-04"]
files_modified:
  - src/intelligence/research/book.py
  - tests/unit/research/test_book.py
autonomous: true
requirements: [D-10, D-11, D-16, D-17, D-18, D-22]

must_haves:
  truths:
    - "The S8 book test shifts the stacked member panel [t, i, k] jointly by whole sessions, refits the S7 ridge on every shifted stack, constructs with R1 and scores per session, all through evaluate()"
    - "The book null is calibrated: on unplanted synthetic panels the rejection rate at nominal 0.05 falls inside the binomial band"
    - "One book shift at full size (127,400 rows x 233 names x 4 members) costs about a second, so the real null fits in minutes on the worker pool"
  artifacts:
    - path: "src/intelligence/research/book.py"
      provides: "BOOK_ARM, flatten_stack, book_returns, book_weights, book_memory_rows, book_sharpe, evaluate_book"
      exports: ["BOOK_ARM", "flatten_stack", "book_returns", "book_weights", "book_memory_rows", "book_sharpe", "evaluate_book"]
    - path: "tests/unit/research/test_book.py"
      provides: "S8 unit tests and the slow null-calibration test"
  key_links:
    - from: "book.book_returns"
      to: "combiner.walk_forward_ridge then portfolio.rank_vol_neutral_returns"
      via: "the combiner runs inside the construction, so every shift refits"
      pattern: "walk_forward_ridge\\("
    - from: "book.evaluate_book"
      to: "evaluate.evaluate(session_scoring=True)"
      via: "one statistics path (D-11)"
      pattern: "session_scoring=True"
---

<objective>
Build S8, the book test (D-11), as a construction handed to the unchanged `evaluate()`: the
stacked member panel is flattened to `[n, m*K]` so `shift_panel` rolls all members jointly by
whole sessions, and the construction unflattens it, refits the S7 ridge (plan 04) on the shifted
stack against the true residual target, and constructs with R1 (plan 01). Scoring is R2.

Purpose: book version 1 is family 1's screen test; the power estimator (plan 08) scores its
replicates through `book_sharpe`, the same path, so observed, null and power agree by
construction.

Output: `book.py`, `test_book.py`.

Worktree discipline (D-18): work only in `/home/bg/dev/indicagent-183` (branch
`phase-183-research-layer`); every Bash command starts with `cd /home/bg/dev/indicagent-183 &&`;
never read or write `/home/bg/dev/indicagent`. Use `.venv/bin/...`. Bit identity reads frozen
artifacts through `--logs /home/bg/dev/indicagent/logs`. No AI attribution or Co-Authored-By.
No em dashes.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-CONTEXT.md
@.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-RESEARCH.md
@.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-01-SUMMARY.md
@.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-04-SUMMARY.md
@src/intelligence/research/evaluate.py
@src/intelligence/research/combiner.py
@src/intelligence/research/portfolio.py
@src/intelligence/statistics/panel_null.py

<interfaces>
From plan 01 (evaluate.py, portfolio.py):
- `evaluate(..., session_scoring: bool = False)`; `aggregate_sessions(r, trade, bars_per_session)`; `scored_sharpe(r, trade, *, periods_per_year, bars_per_session, session_scoring)`; `session_shifts(n, bars_per_session, min_shift, memory)`; `trade_mask(dates, cfg)`; `SESSIONS_PER_YEAR`
- `rank_vol_neutral_returns(alpha, fwd_ret, plan, *, vol, direction, coverage_floor) -> {RANK_VOL_NEUTRAL_ARM: r}`; `rank_vol_neutral_weights(alpha, *, vol, direction, coverage_floor) -> (weights, has_position)`

From plan 04 (combiner.py):
- `RidgeSpec(window_rows, refit_rows, penalty, embargo, min_obs)`; `walk_forward_ridge(stack [n, m, K], target [n, m], spec) -> [n, m]`

From panel_null.py: `shift_panel(panel_2d, k)` (rejects 3-D input by design; the flattened stack is 2-D).

Contract this plan creates (plans 08 and 09 import exactly these names):
- `BOOK_ARM = "book"`
- `flatten_stack(stack: np.ndarray) -> np.ndarray` [n, m*K], C-order reshape (a view when contiguous)
- `book_returns(alpha_flat, fwd_ret, plan, *, n_members: int, ridge: RidgeSpec, vol: np.ndarray, direction: float, coverage_floor: int) -> dict[str, np.ndarray]` returns {BOOK_ARM: r}: unflatten to [n, m, n_members], combined = walk_forward_ridge(stack, fwd_ret, ridge), then R1 on combined; ignores plan (may be None); module-level so functools.partial pickles
- `book_weights(alpha_flat, fwd_ret, *, n_members, ridge, vol, direction, coverage_floor) -> tuple[np.ndarray, np.ndarray]` the observed book's R1 weights and has_position (turnover diagnostics)
- `book_memory_rows(member_memory_rows: Sequence[int], horizon: int) -> int` = max(member_memory_rows) + fwd_span(horizon)
- `book_sharpe(alpha_flat, fwd_ret, construction, trade, *, bars_per_session: int, shift: int = 0) -> float` = scored_sharpe(construction(shift_panel(alpha_flat, shift), fwd_ret, None)[BOOK_ARM], trade, periods_per_year=SESSIONS_PER_YEAR * bars_per_session, bars_per_session=bars_per_session, session_scoring=True)
- `evaluate_book(stack, fwd_resid, closes, dates, valid, cfg, *, ridge, vol, direction, coverage_floor, memory, bars_per_session, workers, pool_factory, mv_condition_max, ic_shrinkage_k, shifts=None) -> EvaluationResult`: builds the partial and calls evaluate(flatten_stack(stack), fwd_resid, closes, dates, cfg, construction=..., memory=memory, embargo=ridge.embargo, bars_per_session=bars_per_session, valid=valid, session_scoring=True, workers=workers, pool_factory=pool_factory, mv_condition_max=..., ic_shrinkage_k=..., shifts=shifts)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: S8 book construction and evaluate_book (D-10, D-11, D-22)</name>
  <files>src/intelligence/research/book.py, tests/unit/research/test_book.py</files>
  <read_first>
    - .planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-RESEARCH.md "Pattern 4" (why the flatten works, why the ridge adds no read-ahead to the shift memory)
    - src/intelligence/research/evaluate.py (evaluate() reads alpha only for its row count and hands the unshifted fwd_ret to every shifted construction)
    - src/intelligence/statistics/panel_null.py lines 44-54 (shift_panel)
    - src/intelligence/research/combiner.py and the R1 functions in portfolio.py (plans 01, 04)
  </read_first>
  <behavior>
    - flatten roll equals joint roll: shift_panel(flatten_stack(stack), k).reshape(n, m, K) equals np.roll(stack, k, axis=0) exactly, for float32 and float64 stacks
    - book_returns equals walk_forward_ridge followed by rank_vol_neutral_returns called by hand
    - Refit per shift: with workers=1 and walk_forward_ridge wrapped by a counting monkeypatch in the book module, evaluate_book calls it exactly 1 + len(shifts) times; and each sharpe_null[k] equals book_sharpe(..., shift=shifts[k]) computed independently (the null is the refit, not a post-fit shift of the combined alpha)
    - The post-fit alternative differs: on a panel with a planted member-target relation, shifting the fitted combined alpha instead of refitting gives a different null distribution (at least one shifted Sharpe differs), which is why D-11 refits
    - Shifts are whole sessions (every shift % bars_per_session == 0) and match session_shifts(n, bps, cfg.min_shift, memory)
    - book_memory_rows([7124, 7228, 7618, 8138], 2) == 8141
    - A small panel whose shift count is below 600 is refused by require_testable(n_shifts=len(shifts), alpha_level=0.05, budget_m=30, power=1.0) with the shift message (the runner applies this refusal; the test pins the count the book produces)
    - Complete cases: a (row, name) with one member NaN has no book position (combined NaN), D-22
    - evaluate_book with workers=2 and a thread-pool pool_factory equals workers=1
    - Planted power sanity: a panel where the target equals 0.5 * member1 plus noise gives a book p far below 0.05; an unplanted panel does not
  </behavior>
  <action>
    Write tests/unit/research/test_book.py per the behavior list first (RED, commit
    `test(183-06): add failing tests for S8 book test`), then book.py per the interfaces block
    (GREEN). Build the synthetic test panels in the test module: bars_per_session 4 (2 slots),
    about 400 sessions, 40 names, 3 members, members NaN off placement rows as family 1's are,
    dates as 15-minute UTC stamps (the test_evaluate_intraday fixture style), a small
    EvaluationConfig-satisfying frozen dataclass with min_shift small enough to leave a few
    hundred shifts, and a RidgeSpec with window_rows 80 * 4, refit_rows 10 * 4, penalty 1.0,
    embargo 3, min_obs 200.

    book.py docstring (D-11): S8 shifts the stacked member panel jointly by whole sessions and
    reruns S7 on the shifted stack (the owner's decision: the null refits the combiner), so the
    fit's capacity is inside the null; it goes through evaluate(), one statistics path; the
    flattened [n, m*K] form exists because shift_panel is a shared 2-D primitive and a row roll
    of the flattened array is the joint roll of the stack; memory for the shift set is the
    largest member memory plus the forward span (the ridge adds none, research pattern 4);
    refusals (600 shifts, 50% power) and budget charging are the runner's (plan 09). Compute
    only, no DB, no services import (D-17); the pool comes in through pool_factory.

    Commit `feat(183-06): S8 book construction with per-shift combiner refit`.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent-183 && .venv/bin/pytest tests/unit/research/test_book.py -q -m "not slow" && .venv/bin/ruff check src/intelligence/research/book.py tests/unit/research/test_book.py</automated>
  </verify>
  <acceptance_criteria>
    - Non-slow tests in test_book.py pass, covering every behavior bullet
    - `grep -c "from services" src/intelligence/research/book.py` is 0
    - `.venv/bin/python -c "from src.intelligence.research.book import BOOK_ARM, flatten_stack, book_returns, book_weights, book_memory_rows, book_sharpe, evaluate_book"` exits 0
  </acceptance_criteria>
  <done>S8 exists as an evaluate() construction whose null refits the combiner per shift.</done>
</task>

<task type="auto">
  <name>Task 2: Null calibration (slow) and full-size shift timing; bit identity; rebase and push</name>
  <files>tests/unit/research/test_book.py</files>
  <read_first>
    - tests/unit/research/test_book.py (Task 1 fixtures)
    - .planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-RESEARCH.md measurements table and "Assumptions log" A1 (about 1 s per shift is an assumption this task measures)
  </read_first>
  <action>
    Add `test_book_null_is_calibrated` marked `@pytest.mark.slow`: 100 unplanted synthetic panels
    (independent Gaussian members and target, members NaN off placement rows, seeds 0..99),
    each small enough (for example 30 names, bars_per_session 4, sessions sized to give about 100
    admissible shifts) that the whole test finishes under 60 s serially; compute each panel's
    one-arm permutation p through evaluate_book; assert the count with p < 0.05 lies inside the
    two-sided 99% binomial interval for n = 100 at the exact nominal rejection probability
    implied by the shift count ((floor(0.05 * (1 + K) - 1e-12)) / (1 + K) style, computed
    exactly with Fraction in the test). This is the V2-style size check of D-11 on the book path.

    Measure (not a unit test) one full-size book shift and record it in the SUMMARY: a
    `.venv/bin/python -c` snippet that builds a float32 stack of 127,400 x 233 x 4 with odd rows
    NaN, a residual target and vol of the same shape, RidgeSpec(window_rows=252*26,
    refit_rows=21*26, penalty=1.0, embargo=3, min_obs=10000), and times book_sharpe for one
    shift (median of 3). Target: under 2 s. If it is over 3 s, profile (ridge vs R1 vs session
    aggregation) and optimize the slow piece in book.py, combiner.py or the R1 function without
    changing results (tests must stay green); record before and after. The power runtime in
    plan 08 scales linearly with this number (research A1: at 3 s per shift power is 15-20 h).

    Run the bit-identity check:
    `.venv/bin/python scripts/analysis/sleeve_walk_forward/repro_frozen.py "$(mktemp -d)" --logs /home/bg/dev/indicagent/logs`
    (three "bit-identical" lines).

    Commit `test(183-06): book null calibration and full-size shift timing`.
    Finish (D-18): `git fetch origin && git rebase origin/main`, `.venv/bin/pytest tests/unit/research -q`,
    `git push origin HEAD:main`, retrying fetch/rebase/push if rejected.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent-183 && .venv/bin/pytest tests/unit/research/test_book.py -q --durations=3 && .venv/bin/python scripts/analysis/sleeve_walk_forward/repro_frozen.py "$(mktemp -d)" --logs /home/bg/dev/indicagent/logs 2>&1 | grep -c "bit-identical" | grep -qx 3</automated>
  </verify>
  <acceptance_criteria>
    - `test_book_null_is_calibrated` passes and its duration is under 60 s
    - The SUMMARY records the measured full-size seconds per book shift, and it is under 3 s
    - repro_frozen.py prints three "bit-identical" lines
    - `git merge-base --is-ancestor HEAD origin/main` exits 0 after the push
  </acceptance_criteria>
  <done>The book null has correct size on unplanted panels, per-shift cost is measured at full size, and the plan is on origin/main.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| none external | in-process numpy compute; workers return arrays |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-20 | Repudiation | a null that shifts the fitted combination and understates the fit's capacity | mitigate | combiner inside the construction; counting test (1 + K refits) and per-shift equality to an independent refit |
| T-183-21 | Tampering | a mis-sized null (anti-conservative p) | mitigate | slow calibration test on 100 unplanted panels inside the exact binomial band |
| T-183-22 | Denial of service | per-shift cost making the null or power check infeasible | mitigate | full-size timing measured and optimized in Task 2 |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/research -q`
- repro_frozen.py bit identity
</verification>

<success_criteria>
S8 runs through evaluate() with a per-shift combiner refit, the null is calibrated, per-shift
cost is measured under 3 s at full size, and the plan is on origin/main.
</success_criteria>

<output>
Create `.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-06-SUMMARY.md` when done
</output>
