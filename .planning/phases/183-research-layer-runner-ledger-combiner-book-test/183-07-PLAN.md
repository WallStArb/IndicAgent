---
phase: 183-research-layer-runner-ledger-combiner-book-test
plan: 07
type: execute
wave: 2
depends_on: ["183-01", "183-02", "183-03", "183-05"]
files_modified:
  - src/intelligence/research/evidence.py
  - src/intelligence/research/runner.py
  - src/intelligence/research/store.py
  - src/intelligence/research/snapshot.py
  - scripts/research/run_spec.py
  - research/specs/family1_intraday_periodicity.yaml
  - tests/unit/research/test_runner_evidence.py
  - tests/unit/research/test_runner_order.py
  - tests/unit/research/test_family1_spec.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-09, D-15, D-16, D-17, D-18, D-21, D-24, D-25]

must_haves:
  truths:
    - "A real-data family run refuses before any ledger row or snapshot when the spec is not committed or differs from HEAD, the checked code is dirty, or the spec hash already has a real run"
    - "A real-data family run writes its started rows before S0, then runs S0 (verified content hash), S1, S2, S3 guards, the shift-floor refusal, S5 per member, and a terminal update; a guard failure ends every row guard_failed, a refusal ends them refused, an exception ends them failed, and a hard kill leaves them started"
    - "Synthetic mode runs the same pipeline on a supplied panel with no git check, no ledger row and no budget charge"
    - "Each evidence record carries the D-09 fields (estimate, bootstrap SE and interval, permutation p, per-period estimates, shape diagnostics, turnover and cost band as diagnostics, n_shifts, power null, coverage, resolution time, hashes) as plain JSON with no NaN and no PASS/FAIL token"
    - "Family 1's committed spec reproduces every number the prereg pins"
  artifacts:
    - path: "src/intelligence/research/runner.py"
      provides: "refusal gate, ordered pipeline, S1/S2/S3/S5 stages, run_family"
      exports: ["RunContext", "BudgetConfig", "LedgerPort", "RunRefused", "run_family", "compute_residuals", "compute_members", "real_guards", "family_identities"]
    - path: "src/intelligence/research/evidence.py"
      provides: "evidence record assembly (D-09)"
      exports: ["evidence_record", "resolution_years", "turnover_per_session", "coverage_summary"]
    - path: "scripts/research/run_spec.py"
      provides: "CLI: DSN, APR, pool injection, real and synthetic modes"
    - path: "research/specs/family1_intraday_periodicity.yaml"
      provides: "family 1 spec, members P1-P4"
      contains: "same_slot_mean"
  key_links:
    - from: "runner.run_family (real)"
      to: "ledger.start_runs before snapshot build"
      via: "ordered awaits; test with fakes records the call order"
      pattern: "start_runs"
    - from: "scripts/research/run_spec.py"
      to: "services._batch_utils.make_worker_pool"
      via: "pool_factory injected into RunContext (Ring 1 code never imports services)"
      pattern: "make_worker_pool"
    - from: "runner S5"
      to: "evaluate(session_scoring=True, construction=partial(rank_vol_neutral_returns, ...))"
      via: "R1 + R2 per member"
      pattern: "rank_vol_neutral_returns"
---

<objective>
Build the spec-as-pre-registration runner (architecture step 4) for family evidence runs: the
refusal gate (D-02), the ordered real-data pipeline with ledger rows around it (D-03),
synthetic mode (D-04), the real-data S3 split (D-25), evidence records (D-09, power null per
D-21), the CLI, and family 1's committed spec (D-01, D-15).

Purpose: this is the shortest path to family 1's first evidence records; book mode (plan 09)
extends the same runner.

Output: `evidence.py`, `runner.py`, small changes to `store.py` and `snapshot.py`, the CLI, the
family 1 spec, three test files.

Worktree discipline (D-18): work only in `/home/bg/dev/indicagent-183` (branch
`phase-183-research-layer`); every Bash command starts with `cd /home/bg/dev/indicagent-183 &&`;
never read or write `/home/bg/dev/indicagent`. Use `.venv/bin/...`. Bit identity reads frozen
artifacts through `--logs /home/bg/dev/indicagent/logs`. No AI attribution or Co-Authored-By.
No em dashes. No real-data run happens in this plan (D-15: the first real run is plan 10, only
through the runner).
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-CONTEXT.md
@.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-RESEARCH.md
@.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-01-SUMMARY.md
@.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-02-SUMMARY.md
@.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-03-SUMMARY.md
@.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-05-SUMMARY.md
@docs/plans/2026-09-25-family1-intraday-periodicity-prereg.md
@docs/plans/2026-09-24-evidence-framework.md
@src/intelligence/research/snapshot.py
@src/intelligence/research/store.py
@scripts/analysis/sleeve_walk_forward/run.py

<interfaces>
Upstream contracts (plans 01, 02, 03, 05; read their SUMMARYs for any renames):
- evaluate.py: `evaluate(..., session_scoring=False)`, `EvaluationResult` (sharpe_obs, sharpe_null, shifts, adjusted_p, excess, sub_period_excess, excess_ci, excess_se, diagnostics, observed_daily, null_median_daily), `session_shifts`, `trade_mask`, `aggregate_sessions`
- portfolio.py: `trailing_vol(returns, *, window_rows, min_finite)`, `rank_vol_neutral_returns(alpha, fwd_ret, plan, *, vol, direction, coverage_floor)`, `rank_vol_neutral_weights(alpha, *, vol, direction, coverage_floor) -> (weights, has_position)`, `RANK_VOL_NEUTRAL_ARM`
- ledger.py: `PostgresLedger(dsn)` with `has_real_run`, `charged_book_tests`, `start_runs(request, identities, run_concepts) -> {name: run_id}`, `finish_run(run_id, *, status, snapshot_hash, evidence)`; `Identity`, `RunRequest`, `LedgerRefusal`, `jsonable`
- spec.py: `FamilySpec`, `BookSpec`, `LoadedSpec`, `load_spec_from_head`, `load_spec_from_file`, `resolve_member`, `ScoringSpec.evaluation_config()`
- provenance.py: `ProvenanceRefusal`, `repo_root`, `head_commit`, `require_committed`, `require_clean`
- guards.py: `integrity`, `probe_rows`, `causality_probe`, `memory_check`, `causality_probe_array`, `memory_check_array`, `require_testable(..., power: float | None)`, `GuardFailure`, `IntegrityReport`
- families/common.py: `declared_memory_rows`, `centred_rank`; member contract `member(resid_bar_returns, *, bars_per_session, coverage_floor, **params)`
- factors.py: `residual_returns`, `VINTAGE_1`; panel.py: `bar_returns`, `forward_returns(opens, horizon, session, closes)`, `fwd_span`, `load`, `Panel`
- snapshot.py (today): `build_panel(dsn, out_dir, *, symbols, tf, start, end_exclusive) -> Path`, `read_only_pool(dsn)`; store.py: `verify(path) -> None` (today)

Contract this plan creates (plan 09 extends runner.py against these names):

store.py / snapshot.py (backward compatible)
- `store.verify(path) -> str` now returns the full sha256 hex digest after the prefix check (callers ignoring the return are unaffected)
- `snapshot.UNIVERSE_DIMENSIONS = ("compute_eligible", "compute_eligible_1d", "live_tradeable")`
- `async def snapshot.universe_symbols(dsn: str, dimension: str) -> list[str]` (read-only pool; dimension validated against UNIVERSE_DIMENSIONS before it is placed in SQL; `SELECT symbol FROM instruments WHERE <dimension> = true ORDER BY symbol`)
- `build_panel(..., manifest_extra: dict | None = None)` merged into the manifest (used to record the universe dimension)

evidence.py
- `resolution_years(estimate: float | None) -> float | None` = ((z_0.95 + z_0.80) / e)^2 with z from scipy.stats.norm.ppf (1.645 + 0.842, evidence framework section 2), None when e is None, not finite or <= 0
- `turnover_per_session(weights: np.ndarray, has_position: np.ndarray, trade: np.ndarray, bars_per_session: int) -> float`: mean over scored sessions of the traded notional, where each row's book is opened and closed within its horizon, so a row contributes 2 * sum_i |w[t, i]|; stated in the docstring as the definition
- `coverage_summary(report: IntegrityReport, alpha: np.ndarray, trade: np.ndarray) -> dict` over scored rows where any alpha is finite: mean, p05, min coverage, rows with a position, mean finite names
- `evidence_record(res: EvaluationResult, *, kind: str, subject: str, n_shifts: int, power: dict | None, coverage: dict, turnover: float, costs: CostSpec, hashes: dict, guards: dict, sub_periods, screen: dict | None = None) -> dict`: keys schema ("research_evidence_v1"), kind, subject, estimate (res.excess[0]), bootstrap_se (res.excess_se[0]), bootstrap_ci (res.excess_ci[0]), permutation_p (res.adjusted_p[0]), sharpe_observed, null_median_sharpe, per_period (list of {start, end, mean_session_excess}), shape (res.diagnostics), n_shifts, power, coverage, turnover_per_session, cost_band ({bps_low, bps_high, annual_drag_low, annual_drag_high} = turnover * bps * 1e-4 * 252, labelled diagnostic_only true), resolution_years_80pct_power, hashes, guards, and screen only when given; passed through ledger.jsonable

runner.py
- `@dataclass(frozen=True) class BudgetConfig: vintage_id: str; budget_m: int; screen_alpha: float`
- `class RunRefused(Exception)`: pre-row refusal (D-02 or a ledger refusal); no row exists
- `class LedgerPort(Protocol)`: the four PostgresLedger methods
- `@dataclass class RunContext: root: Path; budget: BudgetConfig; workers: int = 1; pool_factory: PoolFactory | None = None; ledger: LedgerPort | None = None; dsn: str | None = None; snapshot_dir: Path | None = None; snapshot: Path | None = None; build_snapshot: Callable[..., Awaitable[Path]] | None = None` (default build_snapshot = snapshot.universe_symbols then snapshot.build_panel)
- `@dataclass(frozen=True) class Residuals: bar: np.ndarray; fwd: np.ndarray` and `compute_residuals(panel, *, horizon: int, factor_spec) -> Residuals` (S1, twice)
- `compute_members(spec: FamilySpec, resid_bar, *, bars_per_session) -> dict[str, np.ndarray]` (S2, via resolve_member)
- `real_guards(panel, spec: FamilySpec, members: dict, residuals: Residuals, *, factor_spec) -> dict` (S3 split per D-25; raises GuardFailure; returns a guards summary for the evidence)
- `family_identities(loaded: LoadedSpec) -> list[Identity]` (family row "family.<family>", member rows "member.<family>.<name>" with parent the family)
- `async def run_family(loaded: LoadedSpec, ctx: RunContext, *, mode: Literal["real", "synthetic"], panel: Panel | None = None) -> dict[str, dict]` returns {member name: {"status": ..., "run_id": ... or None, "evidence": {...}}}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Evidence record assembly (D-09, D-21)</name>
  <files>src/intelligence/research/evidence.py, tests/unit/research/test_runner_evidence.py</files>
  <read_first>
    - .planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-RESEARCH.md "Pattern 7" (field map), "Pitfall 8"
    - docs/plans/2026-09-24-evidence-framework.md sections 2 and 5
    - src/intelligence/research/evaluate.py (EvaluationResult as changed by plan 01)
    - src/intelligence/research/ledger.py (jsonable)
  </read_first>
  <behavior>
    - evidence_record on a hand-built EvaluationResult (one arm, NaN in one sub-period and in a diagnostic) returns a dict for which json.dumps(..., allow_nan=False) succeeds, every value is a plain Python type, and NaN became None
    - It contains every key in the interfaces list; power is None for an evidence run (D-21); screen is absent unless given; no key or string value anywhere equals "PASS", "FAIL", "verdict" or "pass"/"fail" tokens (walk the dict)
    - estimate, bootstrap_se, bootstrap_ci, permutation_p map to res.excess[0], res.excess_se[0], res.excess_ci[0], res.adjusted_p[0] exactly
    - resolution_years(0.5) == pytest.approx(((1.6449 + 0.8416) / 0.5) ** 2, rel 1e-3) (about 24.7 years; the framework table rounds to 25); resolution_years(0.0), (-0.2), (nan), None give None
    - turnover_per_session on weights with 2 rows per session of known |w| sums equals the hand value (2 * sum over rows per session, averaged over scored sessions)
    - cost_band drags equal turnover * bps * 1e-4 * 252 for both bps figures and carry diagnostic_only true
    - With screen={"alpha": 0.05, "budget_m": 30, "bar": 0.05 / 30, "p": 0.001, "p_below_bar": True} the record carries it unchanged
  </behavior>
  <action>
    RED then GREEN, per the interfaces block and D-09: the record is the evidence framework's
    section 5 record; turnover and the cost band are diagnostics only, never a gate (standing
    directive, prereg section 4); no PASS/FAIL token for evidence runs; a book test adds the
    screen outcome `p < alpha / M` (plan 09 passes it). The 0.95/0.80 quantiles are
    mathematical constants (norm.ppf), the 252 is SESSIONS_PER_YEAR imported from evaluate,
    and the 1e-4 is the bps unit; bps figures come from the spec. The rank-IC readout is not
    built (deferred in CONTEXT.md). Commits `test(183-07): ...` then
    `feat(183-07): evidence record assembly`.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent-183 && .venv/bin/pytest tests/unit/research/test_runner_evidence.py -q && .venv/bin/ruff check src/intelligence/research/evidence.py tests/unit/research/test_runner_evidence.py</automated>
  </verify>
  <acceptance_criteria>
    - test_runner_evidence.py passes with every behavior bullet
    - `grep -ci "\"pass\"\|\"fail\"\|verdict" src/intelligence/research/evidence.py` is 0
  </acceptance_criteria>
  <done>Evidence records are complete, JSON-clean and token-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Runner: refusal gate, ordered real pipeline, synthetic mode, real-data S3 split (D-02, D-03, D-04, D-25)</name>
  <files>src/intelligence/research/runner.py, src/intelligence/research/store.py, src/intelligence/research/snapshot.py, tests/unit/research/test_runner_order.py</files>
  <read_first>
    - .planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-RESEARCH.md "System architecture diagram", "Pattern 3", "Pattern 5", "Pitfall 5", "Pitfall 6", "Pitfall 10", "Pitfall 12", "Pitfall 13"
    - src/intelligence/research/snapshot.py (build_panel, read_only_pool, manifest) and store.py (verify)
    - src/intelligence/research/guards.py (after plan 05) and families/common.py
    - docs/plans/2026-09-25-family1-intraday-periodicity-prereg.md sections 2-5
  </read_first>
  <behavior>
    Tests use a FakeLedger (records calls, returns run ids, can be told to refuse or to fail on finish), a fake build_snapshot that saves a pre-built synthetic Panel with panel.save and records its call, and a tmp git repo (research/specs/f.yaml committed, src/intelligence/ committed) as ctx.root. Synthetic panel: bars_per_session 4 (2 slots), 400 sessions, 30 names, market+group+idiosyncratic returns converted to open/close/volume, a few missing bars. Test spec: a family spec with two members (windows 1 and 5) pointing at same_slot_mean, horizon 2, declared_memory_rows = declared_memory_rows(w, 4), coverage_floor 20, scoring dates inside the panel, small min_shift, bootstrap_reps 200, guards max_rows 6, s1_probe_sessions 330. BudgetConfig(vintage_id "test", budget_m 1, screen_alpha 0.05) so the floor is 20 shifts.
    - Real mode, clean: call order is ledger.has_real_run, ledger.start_runs, build_snapshot, then finish_run once per member with status completed; start_runs precedes build_snapshot
    - Real mode refusals raise RunRefused with no start_runs and no build_snapshot call: spec untracked; spec modified after commit; untracked file under src/intelligence; has_real_run True; start_runs raising LedgerRefusal (converted to RunRefused)
    - Spec consistency refusal before any row: a member whose declared_memory_rows differs from declared_memory_rows(slot_history_sessions, bps, VINTAGE_1) raises RunRefused
    - Guard failure: a member function (test-only module registered under the families prefix via monkeypatched resolve_member) that reads one row ahead ends every row guard_failed with the member and message in the evidence
    - Shift refusal: BudgetConfig(budget_m=30) on this panel (fewer than 600 shifts) ends every row refused, evidence containing the shift count and "null cannot resolve the bar"
    - Exception: evaluate monkeypatched to raise RuntimeError ends rows failed (evidence has the error text) and the RuntimeError propagates
    - Hard crash: FakeLedger.finish_run raising ConnectionError leaves rows without a terminal status (FakeLedger shows only started) and the error propagates (D-03: the started row counts)
    - Synthetic mode: ledger None; provenance functions monkeypatched to raise if called are never called; build_snapshot never called; returns completed evidence for each member with run_id None
    - The snapshot hash passed to finish_run equals store.verify(path) of the saved panel (64 hex)
    - store.verify returns the full digest and still raises on a modified snapshot; universe_symbols rejects a dimension not in UNIVERSE_DIMENSIONS without touching the DB
  </behavior>
  <action>
    RED: tests/unit/research/test_runner_order.py per the behavior list; commit
    `test(183-07): add failing tests for runner order and refusals`.

    GREEN. store.py: verify returns the digest. snapshot.py: UNIVERSE_DIMENSIONS,
    universe_symbols (read_only_pool; the dimension is a validated column identifier, never
    caller text in SQL), and the optional manifest_extra argument on build_panel; defaults keep
    every existing call identical.

    runner.py per the interfaces block. Real mode, in this order (D-02, D-03):
    1. Gate, no row yet: resolve root with provenance.repo_root; require_committed(root,
       loaded.path) and check its blob equals loaded.blob_sha (the spec was loaded with
       load_spec_from_head); check each member's declared_memory_rows against
       declared_memory_rows(slot_history_sessions, bars_per_session, VINTAGE_1) and that
       bars_per_session is even; resolve every member (imports the family modules); then
       require_clean(root) (covers src/intelligence, research/specs and every loaded first-party
       module, untracked included); ledger.has_real_run(spec_hash). Any failure raises RunRefused.
    2. ledger.start_runs(RunRequest(kind="evidence", spec_path, spec_hash, spec_blob,
       code_commit=head_commit(root), vintage=budget.vintage_id), family_identities(loaded),
       member concept names); LedgerRefusal becomes RunRefused.
    3. Everything after is inside try: S0 (ctx.snapshot reused after store.verify, or built with
       build_snapshot using the spec's universe, tf, start, end_exclusive and
       manifest_extra={"universe": ...}); load the panel with panel.load (verifies the hash);
       check manifest tf, start, end_exclusive, bars_per_session and universe against the spec,
       failing with ValueError on a mismatch. S1: compute_residuals (bar residuals with horizon
       None, forward residuals from forward_returns(open, horizon, panel.session,
       closes=panel.close) with horizon, both with VINTAGE_1, as the prereg's target). S2:
       compute_members. S3 real_guards (D-25): integrity(panel, alpha) per member; rows =
       probe_rows(panel, ...) from the spec's guards; causality_probe_array and
       memory_check_array per member on the fixed residuals.bar with declared =
       slot_history_sessions * bars_per_session (rows filtered to those with that many rows
       after them); then S1 probed once on a sub-panel of the first s1_probe_sessions sessions
       (dataclasses.replace with sliced arrays): causality_probe for the bar-residual function
       (reach 0), causality_probe for the forward-residual target (reach fwd_span(horizon)),
       and memory_check for the bar-residual function with declared (VINTAGE_1.window_sessions +
       VINTAGE_1.refit_sessions) * bars_per_session over probe rows that leave that many rows
       after them. Refusal checks: per member, shifts = session_shifts(n, bps, scoring.min_shift,
       declared_memory_rows + fwd_span(horizon)); require_testable(n_shifts=len(shifts),
       alpha_level=budget.screen_alpha, budget_m=budget.budget_m, power=None) (D-21). S5 per
       member: vol = trailing_vol(residuals.bar, window_rows=vol_window_sessions * bps,
       min_finite=ceil(vol_min_finite_fraction * window_rows)) computed once (D-24: R1's own
       volatility of residual bar returns, research pattern 1); construction =
       functools.partial(rank_vol_neutral_returns, vol=vol, direction=float(direction),
       coverage_floor=coverage_floor); evaluate(alpha, residuals.fwd, panel.close,
       panel.timestamps, scoring.evaluation_config(), mv_condition_max, ic_shrinkage_k,
       shifts=shifts, workers, construction, memory, bars_per_session, valid=panel.valid,
       embargo=fwd_span(horizon), pool_factory, session_scoring=scoring.session_scoring); then
       evidence_record with power None, coverage from integrity, turnover from
       rank_vol_neutral_weights, hashes {spec_hash, spec_blob, snapshot_hash, code_commit}, and
       finish_run(completed).
    4. Terminal handling: GuardFailure in S3 finishes every row guard_failed; GuardFailure from
       require_testable finishes every row refused; any other Exception finishes every
       unfinished row failed with the error text, then re-raises; if finish_run itself fails the
       original error propagates and the rows stay started (D-03).
    Synthetic mode (D-04): skip steps 1, 2 and every ledger call; the panel argument replaces
    S0 (snapshot_hash recorded as None); everything else identical, so tests exercise the real
    pipeline. Members are evaluated one after another, each evaluate() using the injected pool.
    Arrays: cast member alphas to float32 only if memory requires it; residual arrays stay
    float64. No `from services` import anywhere in src/intelligence/research (Ring 1, pitfall
    10). Exception variables named `error`. Structlog is not needed; the runner returns data and
    the CLI prints.

    Run repro_frozen.py (store.py and snapshot.py changed):
    `.venv/bin/python scripts/analysis/sleeve_walk_forward/repro_frozen.py "$(mktemp -d)" --logs /home/bg/dev/indicagent/logs`.

    Commit `feat(183-07): research runner with refusal gate, ordered ledger rows and S3 split`.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent-183 && .venv/bin/pytest tests/unit/research/test_runner_order.py -q && .venv/bin/ruff check src/intelligence/research && ! grep -rn "^from services\|^import services" src/intelligence/research && .venv/bin/python scripts/analysis/sleeve_walk_forward/repro_frozen.py "$(mktemp -d)" --logs /home/bg/dev/indicagent/logs 2>&1 | grep -c "bit-identical" | grep -qx 3</automated>
  </verify>
  <acceptance_criteria>
    - test_runner_order.py passes every behavior bullet
    - `grep -rn "from services" src/intelligence/research` returns nothing
    - repro_frozen.py prints three "bit-identical" lines
    - In runner.py, `start_runs` appears before the snapshot build in run_family's real path (the order test proves it)
  </acceptance_criteria>
  <done>The runner refuses, orders, records and handles failures exactly as D-02/D-03/D-04 state, with the real-data S3 split of D-25.</done>
</task>

<task type="auto">
  <name>Task 3: Family 1 spec, CLI, prereg-conformance test, synthetic CLI smoke; rebase and push</name>
  <files>research/specs/family1_intraday_periodicity.yaml, scripts/research/run_spec.py, tests/unit/research/test_family1_spec.py</files>
  <read_first>
    - docs/plans/2026-09-25-family1-intraday-periodicity-prereg.md (every pinned number: sections 2-5)
    - scripts/analysis/sleeve_walk_forward/run.py (DSN from Settings, make_worker_pool import, argparse style, sys.path insert)
    - src/config/config_service.py lines 36-120 (ConfigService(database_url), initialize, get)
    - src/intelligence/research/spec.py (schema from plan 03)
  </read_first>
  <action>
    Write research/specs/family1_intraday_periodicity.yaml (D-01, D-15), header comments citing
    the prereg and stating that editing any value after a real run is a new spec hash, a new
    recorded run and, per the prereg, a methodology change. Values (every date quoted, every
    float with a decimal point):
    kind family; family intraday_periodicity; prior_source
    docs/plans/2026-09-25-family1-intraday-periodicity-prereg.md; horizon 2; factor_spec
    vintage_1; panel {universe compute_eligible, tf "15m", bars_per_session 26, start
    "2006-01-01", end_exclusive "2025-12-24"} with a comment that "2025-12-24" is read as UTC
    midnight, before oos_start 2025-12-24T05:15Z (research pitfall 13); members same_slot_lag1
    (window 1), same_slot_mean5 (5), same_slot_mean20 (20), same_slot_mean40 (40), each with
    signal src.intelligence.research.families.intraday_periodicity.same_slot_mean, params
    {window_sessions: w}, slot_history_sessions w, declared_memory_rows 7124 / 7228 / 7618 /
    8138; construction {name rank_vol_neutral, direction 1, coverage_floor 20,
    vol_window_sessions 20, vol_min_finite_fraction 0.5}; scoring {session_scoring true,
    trading_start "2010-01-04", sub_periods [["2010-01-04", "2014-12-31"], ["2015-01-01",
    "2019-12-31"], ["2020-01-01", "2025-12-23"]], min_shift 63, bootstrap_mean_block 21,
    bootstrap_reps 2000, seed 183, warmup_sessions 252, calibration_refit_sessions 21,
    coverage_fraction 0.95, ridge_epsilon_fraction 0.10, mv_condition_max 1000.0,
    ic_shrinkage_k 100.0} with a comment that the last six only feed evaluate()'s unused
    covariance plan; guards {seed 183, n_random 4, max_rows 12, s1_probe_sessions 330}; costs
    {bps_low 1.0, bps_high 5.0} marked diagnostics only. Discretion choices the prereg does not
    pin, recorded in the YAML comments and the SUMMARY with their reasons: vol window 20
    sessions of residual bar returns (D-24 requires a pinned window; about one month is long
    enough for a stable per-name scale and short enough to track regime changes), cost band
    1 to 5 bps per side (large-cap and ETF half-spread range; diagnostic only), min_shift 63
    sessions (phase 179's value), guard probe sizes (bounded probe cost on the full panel).

    Write tests/unit/research/test_family1_spec.py: load_spec_from_file on the YAML parses; each
    member resolves; declared_memory_rows equals declared_memory_rows(w, 26); and every
    prereg-pinned number is asserted with a comment citing the prereg section (universe
    compute_eligible, 15m, 26 bars, 13 slots implied, windows 1/5/20/40, direction +1, floor 20,
    horizon 2, session scoring, scored span 2010-01-04 through 2025-12-23, the three
    sub-periods, end_exclusive before oos_start, factor_spec vintage_1).

    Write scripts/research/run_spec.py (CLI, the only layer that imports services, D-17):
    `sys.path.insert(0, ".")` as run.py does; args --spec (repo-relative), --mode {real,
    synthetic} (default real), --snapshot-dir (default logs/research/snapshots), --snapshot
    (reuse a built snapshot directory), --panel (synthetic mode: a saved panel directory),
    --workers (default from APR infra.research_runner.workers; throughput only, not part of any
    hash), --dsn (default Settings().database_url normalized to postgresql:// as run.py does).
    Real mode: repo_root(Path.cwd()); load the spec with load_spec_from_head; read
    alpha.research.vintage_id, alpha.research.budget_m, alpha.research.screen_alpha,
    infra.research_runner.workers and infra.blas_threads_per_worker through ConfigService
    (failing loudly if any budget key is missing); PostgresLedger(dsn); pool_factory =
    functools.partial(make_worker_pool, blas_threads_per_worker=...). Synthetic mode:
    load_spec_from_file, panel.load(--panel), no ledger, no DB unless workers come from APR
    (use --workers or 1). Dispatch on the spec kind: family now; a book spec exits with a clear
    "book mode lands in plan 09" error until plan 09 replaces it. Print one line per member,
    `RESULT {"member": ..., "status": ..., "run_id": ..., "estimate": ..., "permutation_p": ...}`,
    plus the full evidence JSON under logs as `<snapshot-dir>/../runs/<spec_hash[:16]>_<member>.json`.
    Exit 0 when every row reached a terminal status (completed, guard_failed and refused are
    recorded outcomes), 2 on RunRefused (no row), 1 on an exception.

    Smoke-test the CLI in synthetic mode (not a unit test): build the Task 2 style synthetic
    panel at 400 sessions x 4 bars x 30 names, panel.save it to a temp dir, write a temp spec
    with bars_per_session 4 and matching declared memory, and run
    `.venv/bin/python scripts/research/run_spec.py --mode synthetic --spec <tmp spec> --panel <tmp panel> --workers 2`;
    expect exit 0 and two RESULT lines with status completed. Record the command and output in
    the SUMMARY. Also, after committing the YAML, append a comment line to it without committing,
    run `.venv/bin/python scripts/research/run_spec.py --spec research/specs/family1_intraday_periodicity.yaml --mode real`,
    confirm exit 2 with a "differs from HEAD" message and that research_run still has 0 rows,
    then `git checkout -- research/specs/family1_intraday_periodicity.yaml`. (The gate runs
    before any ledger call and before S0, so this touches no real returns.)

    Commit `feat(183-07): family 1 spec and research runner CLI`.
    Finish (D-18): `git fetch origin && git rebase origin/main`, `.venv/bin/pytest tests/unit/research -q`,
    `git push origin HEAD:main`, retrying fetch/rebase/push if rejected.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent-183 && .venv/bin/pytest tests/unit/research -q && .venv/bin/ruff check scripts/research tests/unit/research && git ls-files --error-unmatch research/specs/family1_intraday_periodicity.yaml && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -At -c "SELECT count(*) FROM research_run" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/research -q` passes, test_family1_spec.py included
    - The synthetic CLI smoke run exits 0 with two completed RESULT lines (recorded in the SUMMARY)
    - The dirty-spec real-mode invocation exits 2 with "differs from HEAD"
    - The live `research_run` table still has 0 rows (no real run in this plan, D-15)
    - `grep -n "make_worker_pool" scripts/research/run_spec.py` present; `grep -rn "make_worker_pool" src/intelligence/research` returns nothing
    - `git merge-base --is-ancestor HEAD origin/main` exits 0 after the push
  </acceptance_criteria>
  <done>Family 1's spec is committed and prereg-conformant, the CLI runs the pipeline in synthetic mode and refuses a dirty spec in real mode, and the plan is on origin/main.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| committed spec + git tree -> runner | provenance and the spec decide what runs |
| runner -> live DB (read) | S0 reads market_data_ohlcv_tradeable and instruments through a read-only pool |
| runner -> ledger (write) | the only write path, via PostgresLedger |
| CLI -> services/_batch_utils | the pool is injected; Ring 1 code never imports services |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-23 | Repudiation | a real-data number produced without a ledger row | mitigate | start_runs before S0; crash-order tests; synthetic mode never reads the DB |
| T-183-24 | Tampering | a run on uncommitted or edited spec/code | mitigate | require_committed + blob equality + require_clean over loaded modules, before any row |
| T-183-25 | Tampering | SQL injection through the spec's universe field | mitigate | Literal in the schema and UNIVERSE_DIMENSIONS allow-list before the identifier reaches SQL |
| T-183-26 | Information disclosure | S0 writes to production tables | mitigate | read_only_pool with default_transaction_read_only on (existing) |
| T-183-27 | Tampering | snapshot modified between build and use | mitigate | store.verify on load, full digest recorded in the terminal row |
| T-183-28 | Denial of service | S3 recomputing S1 per probe on the full panel | mitigate | D-25 split: member probes on fixed residuals, S1 probed once on a 330-session sub-panel |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/research -q`
- repro_frozen.py bit identity
- synthetic CLI smoke run exit 0
- live research_run row count 0
</verification>

<success_criteria>
Family evidence runs can be executed only through the runner, with provenance refusals, ledger
rows around every computation, the D-25 guard split and complete evidence records; family 1's
spec is committed; nothing touched real returns; pushed to origin/main.
</success_criteria>

<output>
Create `.planning/phases/183-research-layer-runner-ledger-combiner-book-test/183-07-SUMMARY.md` when done
</output>
