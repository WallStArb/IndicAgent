---
phase: 65-gradient-audit-of-existing-plugins-i1-i7-broader-sweep
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/intelligence/features/smc_context/bos_choch.py
  - src/intelligence/features/smc_context/liquidity_sweeps.py
  - src/intelligence/features/smc_context/ict_killzones.py
  - src/intelligence/features/smc_context/supply_demand_zones.py
  - src/intelligence/features/smc_context/amd_cycle.py
  - src/intelligence/features/i3_structure/market_profile.py
  - src/intelligence/features/i5_patterns/mtf_volatility.py
  - src/intelligence/features/i5_patterns/candlestick_patterns.py
  - src/intelligence/schemas.py
  - src/intelligence/register_plugins.py
  - tests/unit/intelligence/test_i5_new_plugins.py
autonomous: true
requirements: [GRAD-I3-STRUCT, GRAD-SMC, GRAD-I5-PATTERNS]

must_haves:
  truths:
    - "BOS/CHoCH have continuous strength companion fields (distance / ATR)"
    - "Liquidity sweeps have continuous sweep_strength and reclaim_velocity"
    - "ICT killzones output progress fraction instead of binary"
    - "Supply/demand zone freshness uses exponential decay instead of 2-step"
    - "MTF volatility uses upstream continuous values directly"
    - "CandlestickPatterns have inside_bar_depth and outside_bar_expansion companions"
    - "All additive fields registered in schemas.py -- validate_schema_coverage() passes"
  artifacts:
    - path: "src/intelligence/features/smc_context/bos_choch.py"
      provides: "bos_strength + choch_strength continuous companion fields"
    - path: "src/intelligence/features/smc_context/supply_demand_zones.py"
      provides: "Exponential freshness decay"
    - path: "src/intelligence/schemas.py"
      provides: "New additive fields for I3/SMC/I5"
  key_links:
    - from: "src/intelligence/schemas.py"
      to: "src/intelligence/register_plugins.py"
      via: "validate_schema_coverage() tier_checks"
      pattern: "I3Structure|SMCContext|I5Patterns"
---

<objective>
Add continuous gradient companion fields to I3 structure, SMC context, and I5 pattern plugins. These plugins detect discrete events (BOS, sweep, squeeze) -- the detection stays binary but a continuous strength/quality field is added alongside.

Purpose: Preserve the binary detection flag for I7 direction gating while adding gradient signal for ML training and confidence weighting. This is ADDITIVE -- no existing fields are removed or renamed.

Output: ~15 new additive fields across 8 plugins, with schema registration and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/65-gradient-audit-of-existing-plugins-i1-i7-broader-sweep/65-CONTEXT.md
@.planning/phases/65-gradient-audit-of-existing-plugins-i1-i7-broader-sweep/65-RESEARCH.md
@.planning/phases/65-gradient-audit-of-existing-plugins-i1-i7-broader-sweep/65-01-SUMMARY.md
@src/intelligence/utils/gradient_utils.py
@src/intelligence/schemas.py
@src/intelligence/register_plugins.py

<interfaces>
<!-- gradient_utils.py exports -->
```python
from src.intelligence.utils.gradient_utils import (
    linear_ramp, threshold_decay, session_progress, freshness_decay, streak_score
)
```

<!-- Schema classes with extra='forbid' that MUST be updated -->
From schemas.py:
- I3Structure (extra='forbid'): MarketProfile fields need va_position_pct, va_distance_atr
- SMCContext (extra='forbid'): BOSCHoCH needs bos_strength/choch_strength, LiquiditySweeps needs sweep_strength/reclaim_velocity, ICTKillzones needs kz_progress fields, SupplyDemandZones freshness already exists (just change computation), AMDCycle needs manip_strength
- I5Patterns (extra='forbid'): CandlestickPatterns needs inside_bar_depth/outside_bar_expansion, MTFVolatility fields already exist (just change computation to use continuous upstream)

<!-- CRITICAL: validate_schema_coverage() crashes on startup if plugin.outputs has fields not in schema -->
Every new field added to a plugin's outputs frozenset MUST have a corresponding field in the schema class.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gradient companion fields to SMC + I3 MarketProfile plugins + schema registration</name>
  <files>
    src/intelligence/features/smc_context/bos_choch.py,
    src/intelligence/features/smc_context/liquidity_sweeps.py,
    src/intelligence/features/smc_context/ict_killzones.py,
    src/intelligence/features/smc_context/supply_demand_zones.py,
    src/intelligence/features/smc_context/amd_cycle.py,
    src/intelligence/features/i3_structure/market_profile.py,
    src/intelligence/schemas.py,
    src/intelligence/register_plugins.py
  </files>
  <action>
    **SCHEMA FIRST** -- Before modifying any plugin, add ALL new fields to schemas.py:

    In `SMCContext` class, add:
    ```python
    # BOSCHoCH gradient companions
    bos_strength: float | None = None  # break distance / ATR, continuous [0, inf)
    choch_strength: float | None = None  # break magnitude / ATR, continuous [0, inf)
    # LiquiditySweeps gradient companions
    sweep_strength: float | None = None  # depth_pct normalized, [0, 1]
    reclaim_velocity: float | None = None  # 1/bars_to_reclaim normalized, [0, 1]
    # ICTKillzones gradient companions
    kz_asia_progress: float | None = None  # progress fraction [0, 1]
    kz_london_progress: float | None = None
    kz_ny_am_progress: float | None = None
    kz_ny_pm_progress: float | None = None
    # AMDCycle gradient companion
    manip_strength: float | None = None  # spike z-score or impulse magnitude, [0, inf)
    ```

    In `I3Structure` class, add:
    ```python
    # MarketProfile gradient companions
    va_position_pct: float | None = None  # position within VA as percentage [0, 1]
    va_distance_atr: float | None = None  # distance from VA boundary in ATR units
    ```

    Update docstring field counts in both classes.

    **PLUGIN MODIFICATIONS:**

    **bos_choch.py** -- Keep `bos_detected` (0.0/1.0) for direction gating. Add `bos_strength = abs(close - break_level) / atr` when `bos_detected == 1.0`, else 0.0. Same for `choch_strength`. Add both field names to the plugin's `outputs` frozenset. Import `linear_ramp` if needed for clamping.

    **liquidity_sweeps.py** -- Keep `sweep_detected` (0.0/1.0). Add `sweep_strength`: when sweep detected, compute `depth_pct` (how far price penetrated the liquidity level) normalized by ATR. `sweep_strength = linear_ramp(depth_pct, 0, 0.02)` (0-2% depth maps to 0-1). Add `reclaim_velocity = linear_ramp(1.0 / max(1, bars_to_reclaim), 0, 0.5)` -- fast reclaims score higher. Add both to `outputs` frozenset.

    **ict_killzones.py** -- Keep `in_asia_killzone` etc. (0.0/1.0) binary flags. Add companion progress fields: `kz_asia_progress = session_progress(current_time_frac, asia_kz_start, asia_kz_end)` when inside killzone, 0.0 when outside. Same for london, ny_am, ny_pm. Add all 4 to `outputs` frozenset.

    **supply_demand_zones.py** -- Replace the 2-step `freshness` (1.0 -> 0.5) with `freshness_decay(touch_count, k=0.5)`. This changes the EXISTING field value (not additive), so no schema field addition needed -- just different computation. The `freshness` field already exists in SMCContext schema.

    **amd_cycle.py** -- Keep `manip_detected` (0.0/1.0). Add `manip_strength`: when manipulation detected, use the z-score or impulse magnitude that triggered detection. `manip_strength = z_score if manip_detected else 0.0`. Add to `outputs` frozenset.

    **market_profile.py** -- Keep `price_in_va`, `price_above_va`, `price_below_va` as-is (they are consumed as position indicators). Add:
    - `va_position_pct`: When inside VA, `(close - va_low) / (va_high - va_low)`. When outside, 0.0 if below, 1.0 if above.
    - `va_distance_atr`: When outside VA, `abs(close - nearest_va_boundary) / atr`. When inside, 0.0.
    Add both to `outputs` frozenset.

    **register_plugins.py** -- validate_schema_coverage() tier_checks already includes these plugins. No changes needed to the check lists -- just ensure the schema class fields match the plugin outputs. Run the validation mentally: each new field in plugin.outputs must exist in the corresponding schema class.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_i2_registration.py -v -x -k "schema_coverage or register" && .venv/bin/python -c "from src.intelligence.register_plugins import register_all_plugins; register_all_plugins(); print('Schema coverage OK')"</automated>
  </verify>
  <done>All new companion fields are in schemas.py, all plugins have updated outputs frozensets, validate_schema_coverage() passes, supply_demand freshness uses exponential decay</done>
</task>

<task type="auto">
  <name>Task 2: Convert I5 MTFVolatility + CandlestickPatterns to gradients + update tests</name>
  <files>
    src/intelligence/features/i5_patterns/mtf_volatility.py,
    src/intelligence/features/i5_patterns/candlestick_patterns.py,
    src/intelligence/schemas.py,
    tests/unit/intelligence/test_i5_new_plugins.py
  </files>
  <action>
    **SCHEMA FIRST** -- Add to `I5Patterns` class in schemas.py:
    ```python
    # CandlestickPatterns gradient companions
    inside_bar_depth: float | None = None  # how contained: min margin / bar_range, [0, 1]
    outside_bar_expansion: float | None = None  # expansion ratio vs prev range, [0, inf)
    ```
    Update I5Patterns docstring field count.

    **mtf_volatility.py** -- Replace binary `mtf_exp_15m` and `mtf_exp_1h`:
    - Currently: `1.0 if float(exp_15m) > 0 else 0.0`
    - Replace with: use the upstream continuous `exp_15m` value directly. `mtf_exp_15m = max(0.0, float(exp_15m))` -- already a continuous expansion magnitude. Same for `mtf_exp_1h`.
    - For `squeeze_within`: Currently `1.0 if (is_squeezing and higher_expanding) else 0.0`. Replace with: when both conditions met, output `linear_ramp(squeeze_depth * expansion_magnitude, 0, 1.0)` combining the two factors. When not met, 0.0. This preserves the compound condition but adds magnitude.
    - These are existing field name modifications (not new fields) so no schema changes needed beyond what's already there (I5Patterns has `mtf_exp_15m`, `mtf_exp_1h`, `squeeze_within` if they exist -- verify in schema).

    **candlestick_patterns.py** -- Keep `inside_bar` and `outside_bar` (0.0/1.0) detection flags. Add companions:
    - `inside_bar_depth`: When `inside_bar == 1.0`, compute `min(prev_high - curr_high, curr_low - prev_low) / (prev_high - prev_low)`. The more deeply contained, the higher the score. Clamped to [0, 1].
    - `outside_bar_expansion`: When `outside_bar == 1.0`, compute `((curr_high - prev_high) + (prev_low - curr_low)) / (prev_high - prev_low)`. Measures total expansion relative to previous range. Clamped to [0, 3.0] via min().
    - Add both to plugin `outputs` frozenset.

    **Tests** -- Update `test_i5_new_plugins.py`:
    - MTFVolatility: Change assertions from `== 1.0` to range checks where applicable. Assert `0.0 < mtf_exp_15m < 1.0` for moderate expansion inputs.
    - CandlestickPatterns: Add tests for `inside_bar_depth` and `outside_bar_expansion` companion fields. Assert they are > 0 when the detection flag is 1.0. Assert they are 0.0 when detection flag is 0.0.
    - Add gradient continuity test: for a bar that is moderately inside the previous bar, `0.0 < inside_bar_depth < 1.0`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_i5_new_plugins.py -v -x && .venv/bin/python -c "from src.intelligence.register_plugins import register_all_plugins; register_all_plugins(); print('Schema coverage OK')"</automated>
  </verify>
  <done>MTFVolatility outputs continuous expansion values, CandlestickPatterns have inside_bar_depth and outside_bar_expansion companions, all I5 tests pass, schema coverage validates</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- additive field changes to existing compute-only plugins.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-04 | N/A | SMC/I3/I5 gradient companions | accept | Pure compute additions. No I/O changes, no auth, no network. Additive fields only -- no existing fields removed. |
</threat_model>

<verification>
- validate_schema_coverage() passes (all new plugin output fields registered in schemas.py)
- All SMC/I3/I5 tests pass
- New companion fields output continuous values for mid-range inputs
</verification>

<success_criteria>
- bos_strength/choch_strength output break distance / ATR when detection is active
- sweep_strength outputs normalized depth when sweep detected
- kz_*_progress outputs time progression through killzone
- supply_demand freshness decays exponentially with touch count
- mtf_exp_15m/1h output continuous upstream values
- inside_bar_depth/outside_bar_expansion have gradient values
- All schema coverage validates, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/65-gradient-audit-of-existing-plugins-i1-i7-broader-sweep/65-03-SUMMARY.md`
</output>
