---
phase: 134-signal-classification-type-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/149_phase134_outcome_column.sql
  - production/scripts/lifecycle_replay.py
  - src/intelligence/trading/signal_outcome.py
  - src/intelligence/trading/lifecycle_tracker.py
autonomous: true
gap_closure: false

must_haves:
  truths:
    - "trade_executions.outcome column exists, is typed TEXT NOT NULL DEFAULT NULL, has a CHECK constraint enumerating all 8 SignalOutcome values, and is indexed"
    - "lifecycle_replay writes outcome for every zone exit and market exit row (no NULL outcomes after a fresh replay)"
    - "Historical rows are backfilled: outcome derived from actual_mfe, actual_bars, exit_reason using exact _classify_stop_outcome() logic"
    - "The full unit suite is green after the change"
  artifacts:
    - path: "production/migrations/149_phase134_outcome_column.sql"
      provides: "Migration adding trade_executions.outcome column with CHECK constraint"
      min_lines: 15
  key_links:
    - from: "lifecycle_tracker._classify_stop_outcome(current_mfe, bars_in_trade_count)"
      to: "trade_executions.outcome"
      via: "lifecycle_replay zone_exit and market_exit write paths"
      pattern: "outcome"
---

<objective>
Persist `SignalOutcome` to `trade_executions.outcome`. Today lifecycle_replay computes the 8-class
taxonomy (`stopped_at_entry`, `stopped_in_trade`, `target_1`, etc.) via `_classify_stop_outcome()`
but never writes it to the DB — it exists only in memory and logs. This forces every downstream
query to re-derive the classification, which is where the Plan 05 stopped_at_entry gate bug came
from. The fix: add `outcome TEXT` to `trade_executions`, wire lifecycle_replay to write it at
event time, and backfill historical rows from existing `actual_mfe`/`actual_bars` data.

After this plan, `stopped_at_entry` rate = `COUNT(*) FILTER (WHERE outcome='stopped_at_entry') /
COUNT(*)` with no derivation. The classification logic lives in exactly one place
(_classify_stop_outcome), written once, never re-derived.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/intelligence/trading/signal_outcome.py
@src/intelligence/trading/lifecycle_tracker.py
@production/scripts/lifecycle_replay.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 149 — add outcome column to trade_executions</name>
  <read_first>
    - production/migrations/ (ls to confirm next number; 148 is the last Phase 132 migration)
    - src/intelligence/trading/signal_outcome.py (all 8 SignalOutcome values — CHECK constraint must enumerate them exactly)
    - CLAUDE.md §TimescaleDB Tables (trade_executions schema, primary key, existing columns)
  </read_first>
  <files>production/migrations/149_phase134_outcome_column.sql</files>
  <action>
    Write migration `149_phase134_outcome_column.sql`:

    1. Add column: `ALTER TABLE trade_executions ADD COLUMN IF NOT EXISTS outcome TEXT;`
       Column is nullable initially to allow backfill before enforcement.

    2. CHECK constraint enumerating all 8 SignalOutcome values:
       `ALTER TABLE trade_executions ADD CONSTRAINT chk_te_outcome CHECK (outcome IS NULL OR outcome IN ('never_activated','stopped_at_entry','stopped_in_trade','target_1','target_1_2','target_full','ttl_expired_ahead','ttl_expired_behind'));`

    3. Index for query efficiency:
       `CREATE INDEX IF NOT EXISTS idx_trade_executions_outcome ON trade_executions(outcome) WHERE outcome IS NOT NULL;`

    Apply the migration:
    `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/149_phase134_outcome_column.sql`

    Verify column exists:
    `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d trade_executions" | grep outcome`
  </action>
  <verify>
    `\d trade_executions` shows `outcome text` column and `chk_te_outcome` constraint.
  </verify>
  <acceptance_criteria>
    - `production/migrations/149_phase134_outcome_column.sql` exists
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d trade_executions"` shows `outcome` column and `chk_te_outcome` CHECK constraint
    - `SELECT COUNT(*) FROM trade_executions WHERE outcome IS NOT NULL` returns 0 (column exists, not yet populated — backfill is Task 3)
  </acceptance_criteria>
  <done>Migration 149 applied; trade_executions.outcome column exists with CHECK constraint and index.</done>
</task>

<task type="auto">
  <name>Task 2: Wire lifecycle_replay to write outcome at every exit event</name>
  <read_first>
    - production/scripts/lifecycle_replay.py (lines 760-800: zone_exit pending_writes block; lines 820-880: market_exit / ttl_expired writes; _flush_writes function to see INSERT structure)
    - src/intelligence/trading/lifecycle_tracker.py:556-564 (_classify_stop_outcome — exact logic to replicate in the write path)
    - src/intelligence/trading/signal_outcome.py (SignalOutcome enum values)
  </read_first>
  <files>production/scripts/lifecycle_replay.py</files>
  <action>
    In `lifecycle_replay.py`, wire the `outcome` field into every pending_writes dict that results
    in a `trade_executions` INSERT:

    **Zone exit path** (around line 770-789, the `"zone_exit"` pending_writes.append call):
    The dict already contains `"outcome": z_outcome` — verify this is passed through to
    `_flush_writes`. If it is not included in the INSERT, add it. `z_outcome` is already computed
    at line 767 via `_classify_stop_outcome(z_mfe, z_bit)`.

    **Market exit path** (target hits, ttl_expired, condition_expired):
    For each `"market_exit"` and `"ttl_expired"` pending_writes.append call, compute the outcome
    from the exit type:
    - `exit_reason='stop_loss'`: `outcome = _classify_stop_outcome(current_mfe, bars_in_trade_count).value`
    - `exit_reason` in `('target_1','target_1_2','target_full')`: `outcome = exit_reason` (these are SignalOutcome values directly)
    - `exit_reason` in `('ttl_expired_ahead','ttl_expired_behind')`: `outcome = exit_reason`
    - `exit_reason='ttl_expired'` (bare, without _ahead/_behind suffix): map to `ttl_expired_ahead` if `current_mfe > 0` else `ttl_expired_behind` (matches lifecycle_tracker.py:636)
    - `exit_reason='condition_expired'`: this is not a SignalOutcome — map to `ttl_expired_behind` (trade never progressed) or consider adding `condition_expired` to SignalOutcome. Defer adding to SignalOutcome (separate decision); for now map to `ttl_expired_behind`.
    - `exit_reason='chandelier_stop'`: maps to `stopped_in_trade` (chandelier only fires after significant favorable move).

    Confirm `_flush_writes` INSERT statement includes the `outcome` column. If the INSERT is
    dynamic (built from the dict keys), adding `"outcome"` to the write dict is sufficient.
    If it is a hardcoded INSERT, add `outcome` to the column list and `$N` parameter.

    Run lifecycle_replay on a small sample to confirm outcome is written:
    `.venv/bin/python -u production/scripts/lifecycle_replay.py --reset --reset-after 2026-06-11T00:00:00Z --confirm --symbols QQQ --workers 4`
    Then: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT exit_reason, outcome, COUNT(*) FROM trade_executions te JOIN trade_frames tf ON te.frame_id=tf.frame_id JOIN signal_events se ON tf.signal_id=se.signal_id WHERE se.symbol='QQQ' AND se.ts >= '2026-06-11T00:00:00Z' GROUP BY 1,2 ORDER BY 3 DESC;"`
    Confirm: every row has a non-NULL outcome and the exit_reason→outcome mapping is correct.
  </action>
  <verify>
    lifecycle_replay exits 0; QQQ sample shows non-NULL outcome on all trade_executions rows;
    stop_loss rows show outcome in ('stopped_at_entry','stopped_in_trade'); target rows show matching outcome.
  </verify>
  <acceptance_criteria>
    - lifecycle_replay.py writes `outcome` for every zone exit and market exit write dict
    - Sample replay of QQQ over 7 days: `SELECT COUNT(*) FROM trade_executions WHERE outcome IS NULL AND exited_at >= '2026-06-11T00:00:00Z'` returns 0
    - `SELECT DISTINCT exit_reason, outcome FROM trade_executions WHERE exited_at >= '2026-06-11T00:00:00Z' ORDER BY exit_reason` shows correct exit_reason→outcome mapping (stop_loss→stopped_at_entry or stopped_in_trade; target_1→target_1; ttl_expired_ahead→ttl_expired_ahead)
    - No CHECK constraint violations during replay (constraint enforces write-time correctness)
  </acceptance_criteria>
  <done>lifecycle_replay writes outcome for all exit types; sample replay produces zero NULL outcomes; exit_reason→outcome mapping verified.</done>
</task>

<task type="auto">
  <name>Task 3: Backfill outcome for all historical trade_executions rows</name>
  <read_first>
    - production/migrations/149_phase134_outcome_column.sql (the migration just applied)
    - src/intelligence/trading/lifecycle_tracker.py:556-564 (_classify_stop_outcome thresholds: current_mfe<=0.05 OR bars<=2)
    - CLAUDE.md §TimescaleDB Tables (trade_executions columns: actual_mfe, actual_bars, exit_reason)
  </read_first>
  <files>production/migrations/149_phase134_outcome_column.sql</files>
  <action>
    Append a backfill UPDATE to `149_phase134_outcome_column.sql` (or run it as a direct psql
    command — document whichever approach is used):

    The backfill must replicate `_classify_stop_outcome()` exactly:
    ```sql
    UPDATE trade_executions SET outcome =
      CASE
        WHEN exit_reason = 'stop_loss' THEN
          CASE WHEN actual_mfe <= 0.05 OR actual_bars IS NULL OR actual_bars <= 2
               THEN 'stopped_at_entry'
               ELSE 'stopped_in_trade'
          END
        WHEN exit_reason = 'chandelier_stop' THEN 'stopped_in_trade'
        WHEN exit_reason = 'condition_expired' THEN 'ttl_expired_behind'
        WHEN exit_reason = 'ttl_expired' THEN
          CASE WHEN actual_mfe > 0 THEN 'ttl_expired_ahead' ELSE 'ttl_expired_behind' END
        WHEN exit_reason IN ('target_1','target_1_2','target_full','ttl_expired_ahead','ttl_expired_behind')
          THEN exit_reason
        ELSE NULL
      END
    WHERE outcome IS NULL;
    ```

    Run and verify:
    `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM trade_executions WHERE outcome IS NULL;"`
    Must return 0.

    Check distribution:
    `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT outcome, COUNT(*) FROM trade_executions GROUP BY 1 ORDER BY 2 DESC;"`
    Confirm no NULL rows; all values are in the SignalOutcome set.
  </action>
  <verify>
    `SELECT COUNT(*) FROM trade_executions WHERE outcome IS NULL` returns 0.
    Distribution query shows all 8 classes (or a subset thereof) with no unexpected values.
  </verify>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM trade_executions WHERE outcome IS NULL` returns 0
    - `SELECT DISTINCT outcome FROM trade_executions ORDER BY outcome` contains only values from ('never_activated','stopped_at_entry','stopped_in_trade','target_1','target_1_2','target_full','ttl_expired_ahead','ttl_expired_behind') — no unexpected strings
    - Backfill logic is documented in the migration file or a comment in lifecycle_replay.py
  </acceptance_criteria>
  <done>All historical trade_executions rows have a non-NULL outcome; distribution is clean; no CHECK constraint violations.</done>
</task>

<task type="auto">
  <name>Task 4: Unit tests + Done-Coding SOP</name>
  <read_first>
    - CLAUDE.md Done-Coding SOP (simplify -> review -> test -> commit on branch -> merge ff-only -> delete branch -> prune -> push)
    - CLAUDE.md "No AI attribution in commits" and "No human checkpoints" memory rules
    - tests/unit/intelligence/trading/ (existing lifecycle_tracker and lifecycle_replay tests for pattern)
  </read_first>
  <files>tests/unit/intelligence/trading/test_outcome_persistence.py</files>
  <action>
    Write `tests/unit/intelligence/trading/test_outcome_persistence.py` with 4 tests:

    1. `test_classify_stop_outcome_mfe_threshold` — `_classify_stop_outcome(0.05, 10)` returns
       `STOPPED_AT_ENTRY`; `_classify_stop_outcome(0.06, 10)` returns `STOPPED_IN_TRADE`.

    2. `test_classify_stop_outcome_bars_threshold` — `_classify_stop_outcome(0.10, 2)` returns
       `STOPPED_AT_ENTRY`; `_classify_stop_outcome(0.10, 3)` returns `STOPPED_IN_TRADE`.

    3. `test_classify_stop_outcome_none_bars` — `_classify_stop_outcome(0.10, None)` returns
       `STOPPED_AT_ENTRY` (None bars = never activated = entry stop).

    4. `test_backfill_sql_coverage` — parametrize over the CASE mapping table and assert each
       `exit_reason` produces the expected `outcome` string using the same CASE logic as the
       backfill SQL (pure Python, no DB required).

    Run: `.venv/bin/pytest tests/unit/intelligence/trading/test_outcome_persistence.py -v`
    Run full suite: `.venv/bin/pytest tests/unit/ -q`

    Follow Done-Coding SOP: simplify -> review -> test -> commit on feature branch ->
    merge ff-only to main -> delete branch -> prune worktrees -> push.
    Commit message: `feat(134-01): persist SignalOutcome to trade_executions.outcome`
    No Co-Authored-By or AI attribution.
  </action>
  <verify>
    All 4 new tests pass; full unit suite green; committed and pushed.
  </verify>
  <acceptance_criteria>
    - `tests/unit/intelligence/trading/test_outcome_persistence.py` exists with 4 tests
    - `.venv/bin/pytest tests/unit/intelligence/trading/test_outcome_persistence.py -v` exits 0
    - `.venv/bin/pytest tests/unit/ -q` exits 0
    - `git log --oneline -1` shows the phase commit on main with no AI attribution
    - `git status --porcelain` is clean after push
  </acceptance_criteria>
  <done>4 unit tests green; full suite green; committed (no AI attribution), merged ff-only, pushed.</done>
</task>

</tasks>

<verification>
- trade_executions.outcome column exists with CHECK constraint enumerating all 8 SignalOutcome values
- lifecycle_replay writes non-NULL outcome for every exit type
- All historical rows backfilled (0 NULL outcomes in trade_executions)
- exit_reason→outcome mapping matches _classify_stop_outcome() exactly
- Unit suite green; committed and pushed
</verification>

<success_criteria>
- `SELECT COUNT(*) FROM trade_executions WHERE outcome IS NULL` returns 0
- `SELECT DISTINCT outcome FROM trade_executions` contains only SignalOutcome values
- lifecycle_replay produces 0 NULL outcomes on a fresh 7-day QQQ replay
- `.venv/bin/pytest tests/unit/ -q` exits 0; phase committed and pushed
</success_criteria>

<output>
After completion, create `.planning/phases/134-signal-classification-type-safety/134-01-SUMMARY.md`
</output>
