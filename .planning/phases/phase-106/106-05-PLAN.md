---
phase: phase-106-foundation-hardening
plan: 05
type: execute
wave: 2
depends_on: [phase-106-01]
files_modified:
  - src/core/plugin_circuit_breaker.py
  - services/intelligence_pipeline_agent.py
autonomous: true

must_haves:
  truths:
    - "PluginCircuitBreaker is populated from the plugin registry at pipeline startup (circuit_breakers is no longer empty dict)"
    - "Each plugin in TIER_I1 through TIER_I7 has a dedicated PluginCircuitBreaker instance"
    - "All breakers default to enabled=False (shadow mode - transparent passthrough, no behavior change)"
    - "PLUGIN_CB_ENABLED=true env var enables real circuit breaking"
    - "Circuit breaker state exported as OTel gauge per plugin (0=closed, 1=open, 2=half_open)"
    - "A breaker-open event is logged with structlog WARNING"
  artifacts:
    - path: "services/intelligence_pipeline_agent.py"
      provides: "circuit_breakers dict populated at _setup() from plugin registry"
      contains: "PluginCircuitBreaker"
    - path: "src/core/plugin_circuit_breaker.py"
      provides: "enabled flag; OTel state gauge; WARNING log on open"
      contains: "intelligence_pipeline_plugin_cb_state"
  key_links:
    - from: "services/intelligence_pipeline_agent.py"
      to: "src/core/plugin_circuit_breaker.py"
      via: "circuit_breakers dict passed to PluginExecutor at construction"
      pattern: "circuit_breakers.*PluginCircuitBreaker"
    - from: "src/core/plugin_circuit_breaker.py"
      to: "src/observability/metrics.py"
      via: "create_gauge instrument recording state per plugin"
      pattern: "intelligence_pipeline_plugin_cb_state"
---

<objective>
Wire the 584-line PluginCircuitBreaker (currently unused on the hot path) into the
intelligence pipeline plugin execution loop. Shadow-mode-first: all breakers are
transparent at startup (enabled=False). A single env var enables real circuit breaking
for live use.

Purpose: Finding CL-2 — a slow BOCPD/HMM plugin running at 4500ms/bar is invisible
to the existing simple CircuitBreaker. At double symbol count (v2.8 target), one
rogue plugin degrades the entire pipeline. This plan is Wave 2 because it uses OTel
metric infrastructure added in Plan 01.
Output: circuit_breakers dict populated at startup; shadow-mode flag; OTel state gauge.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@docs/architecture/audit-2026-05-23-compute-latency.md
@docs/ideas/service-resilience-patterns.md
@src/core/plugin_circuit_breaker.py
@services/intelligence_pipeline_agent.py
@src/intelligence/register_plugins.py
@.planning/phases/phase-106/106-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add enabled flag + OTel state gauge to PluginCircuitBreaker; populate dict at startup</name>
  <files>
    src/core/plugin_circuit_breaker.py
    services/intelligence_pipeline_agent.py
  </files>
  <action>
    Addresses finding CL-2.

    Read src/core/plugin_circuit_breaker.py in full first. Understand:
    - constructor signature and state model (what states exist, how transitions occur)
    - how allow_request(), record_success(), record_failure() work
    - what metrics are already emitted (LANGGRAPH_WORKFLOW_* per audit note)

    Read services/intelligence_pipeline_agent.py around line 284 where circuit_breakers={}
    is passed to PluginExecutor.

    Read src/intelligence/register_plugins.py to understand TIER_I1 through TIER_I7 lists
    and how plugin names are represented (class objects, strings, or tuples).

    Read the PluginExecutor source to understand how circuit_breakers dict is consumed
    (_get_plugin_cb() or similar). Verify it already has a code path for PluginCircuitBreaker
    vs simple CircuitBreaker.

    1. src/core/plugin_circuit_breaker.py — add enabled flag and shadow mode:
       Add enabled: bool = False parameter to __init__.
       When enabled=False:
         - allow_request() returns True unconditionally (transparent passthrough)
         - record_failure() is a no-op
         - record_success() is a no-op
       Add class-level env var check for global enable:
         import os
         _GLOBALLY_ENABLED = os.environ.get("PLUGIN_CB_ENABLED", "false").lower() == "true"
       In __init__: self._enabled = enabled or _GLOBALLY_ENABLED

       Add module-level OTel gauge for state export. Use the _meter from metrics.py:
         from src.observability.metrics import _meter
         _CB_STATE = _meter.create_gauge(
             "intelligence_pipeline_plugin_cb_state",
             description="Circuit breaker state per plugin: 0=closed 1=open 2=half_open",
         )
       Or create a local meter with get_meter("indicagent") if importing _meter directly
       causes circular import issues — check first.
       In each state transition method (to open, to closed, to half-open), emit:
         _CB_STATE.set(state_int, {"plugin": self.name})
       where state_int is 0=closed, 1=open, 2=half_open. Adapt to actual state model.
       Add structlog WARNING on transition to open:
         import structlog
         logger = structlog.get_logger()
         logger.warning("plugin_circuit_breaker.opened", plugin=self.name, failure_count=...)

    2. services/intelligence_pipeline_agent.py — populate circuit_breakers at _setup():
       In _setup() where circuit_breakers={} is passed to PluginExecutor, replace with:
         from src.intelligence.register_plugins import TIER_I1, TIER_I2, TIER_I3, TIER_I4, TIER_I5, TIER_I6, TIER_I7
         from src.core.plugin_circuit_breaker import PluginCircuitBreaker
         import os
         cb_enabled = os.environ.get("PLUGIN_CB_ENABLED", "false").lower() == "true"
         all_plugin_tiers = [TIER_I1, TIER_I2, TIER_I3, TIER_I4, TIER_I5, TIER_I6, TIER_I7]
         # Extract plugin names — adapt to actual TIER list structure
         # If TIER lists contain class objects: use cls.__name__
         # If they contain strings: use directly
         # If they contain (name, class) tuples: use the name element
         circuit_breakers = {}
         for tier in all_plugin_tiers:
             for plugin in tier:
                 name = plugin.__name__ if hasattr(plugin, "__name__") else str(plugin)
                 circuit_breakers[name] = PluginCircuitBreaker(name=name, enabled=cb_enabled)

       Pass circuit_breakers=circuit_breakers where PluginExecutor is constructed.

    3. Verify PluginExecutor uses the dict:
       Read the executor to confirm _get_plugin_cb() or the breaker-lookup code already
       handles PluginCircuitBreaker entries. If the lookup only supports the simple
       CircuitBreaker, update it to check self._circuit_breakers dict first:
         cb = self._circuit_breakers.get(plugin_name)
         if cb is None:
             cb = CircuitBreaker(...)  # existing simple breaker as fallback
       The fallback preserves existing behavior for any plugin name not in the dict.
  </action>
  <verify>
    cd /home/bg/dev/indicagent
    .venv/bin/python -c "import src.core.plugin_circuit_breaker; print('OK')"
    .venv/bin/python -c "import services.intelligence_pipeline_agent; print('OK')"
    grep -n "PluginCircuitBreaker\|circuit_breakers" services/intelligence_pipeline_agent.py | grep -v "^#" | head -10
    grep -n "intelligence_pipeline_plugin_cb_state\|_CB_STATE" src/core/plugin_circuit_breaker.py | head -5
    grep -n "self._enabled\|enabled=False\|PLUGIN_CB_ENABLED" src/core/plugin_circuit_breaker.py | head -5
    grep -n "plugin_circuit_breaker.opened\|WARNING.*opened" src/core/plugin_circuit_breaker.py | head -3
    .venv/bin/pytest tests/unit/ -q --tb=short -x 2>&1 | tail -10
  </verify>
  <done>
    - circuit_breakers dict populated from plugin registry at pipeline startup
    - All breakers have enabled=False by default (shadow mode — transparent passthrough)
    - PLUGIN_CB_ENABLED=true env var enables real circuit breaking
    - OTel gauge intelligence_pipeline_plugin_cb_state exported per plugin
    - structlog WARNING emitted when breaker transitions to open state
    - Existing simple CircuitBreaker is still used as fallback for any unmapped plugin
    - All unit tests pass
  </done>
</task>

</tasks>

<verification>
cd /home/bg/dev/indicagent
grep -n "PluginCircuitBreaker" services/intelligence_pipeline_agent.py | grep -v "^#"
grep -n "_enabled\|enabled=False" src/core/plugin_circuit_breaker.py | head -5
.venv/bin/pytest tests/unit/ -q 2>&1 | tail -5
</verification>

<success_criteria>
- PluginCircuitBreaker active on every TIER_I1-I7 plugin (not an empty dict placeholder)
- Default behavior UNCHANGED: enabled=False means fully transparent passthrough
- Observable: intelligence_pipeline_plugin_cb_state{plugin=X} = 0 (closed) for all plugins at startup
- Operator enables via PLUGIN_CB_ENABLED=true without code change
- No regression in plugin execution behavior
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/phase-106/106-05-SUMMARY.md`
</output>
