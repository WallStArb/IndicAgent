---
phase: 106-foundation-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/service_auditor_agent.py
  - production/systemd/indicagent-intelligence-pipeline.service
  - production/systemd/indicagent-alerting-agent.service
  - production/systemd/indicagent-dlq-drain.service
autonomous: true

must_haves:
  truths:
    - "service_auditor knows about all 9 previously-missing services"
    - "Timer-triggered oneshot services are never restarted by the auditor's restart loops"
    - "Lag thresholds exist for every Kafka-consumer service in _DAG_ORDER"
    - "_AGENT_ID_TO_UNIT keys match the actual agent name= arguments"
    - "systemd unit dependencies reference services that actually exist"
  artifacts:
    - path: "services/service_auditor_agent.py"
      provides: "Complete DAG registry with oneshot guard"
      contains: "_ONESHOT_UNITS"
  key_links:
    - from: "services/service_auditor_agent.py restart loops"
      to: "_ONESHOT_UNITS"
      via: "if unit in _ONESHOT_UNITS: continue guard"
      pattern: "_ONESHOT_UNITS"
---

<objective>
Close the DAG-correctness blind spots in `service_auditor_agent.py`: 9 services are missing from `_DAG_ORDER`, two Kafka consumers lack `_LAG_THRESHOLDS`, one `_AGENT_ID_TO_UNIT` key is wrong, and timer-triggered oneshots can be wrongly restarted. Also fix three systemd unit files whose `After=`/dependency clauses point at non-existent units.

Purpose: The service auditor is the L10 meta-monitor that restarts everything. Blind spots mean newly added services are never monitored, and missing oneshot guards mean the auditor fights systemd timers.
Output: Complete, accurate DAG registry plus a `_ONESHOT_UNITS` guard.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-106/106-RESEARCH.md
@.planning/phases/phase-106/106-PATTERNS.md
@CLAUDE.md
</context>

<threat_model>
- **DAG mismatch causing auditor blind spots:** A service absent from `_DAG_ORDER` gets the default priority 99 and is never properly ordered; if also absent from `_AGENT_ID_TO_UNIT`, its consumer-lag metric cannot be mapped to a unit, so stall detection silently skips it. Mitigation: add all 9 services with correct priorities; verify keys against actual `name=` args.
- **Auditor fighting systemd timers (restart thrash):** Adding oneshot timer services (weight-updater, shadow-auditor, ml-*) to `_DAG_ORDER` without a guard makes the graduated restart loop restart them on the auditor's schedule instead of letting systemd timers fire them — they then look "failed" between runs. Mitigation: add `_ONESHOT_UNITS` frozenset and guard BOTH restart paths (stall loop ~line 381 and graduated restart ~line 470/522).
- **Phantom entries:** Do NOT add `indicagent-hmm-training` or `indicagent-feature-validation` — RESEARCH.md confirmed these are not in the live system.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add 9 missing services to _DAG_ORDER, _LAG_THRESHOLDS, _AGENT_ID_TO_UNIT and fix priorities</name>
  <files>services/service_auditor_agent.py</files>
  <read_first>
    - services/service_auditor_agent.py (lines 53-160 — the three dicts)
    - .planning/phases/phase-106/106-RESEARCH.md (DAG Correctness section, priority table)
  </read_first>
  <action>
    1. Add these 9 entries to `_DAG_ORDER` (lines 55-96) with priorities per RESEARCH.md:
       - `"indicagent-redpanda-ready": 0`
       - `"indicagent-redpanda-watchdog": 0`
       - `"indicagent-weight-updater": 8`
       - `"indicagent-shadow-auditor": 8`
       - `"indicagent-ml-orchestrator": 8`
       - `"indicagent-ml-data-quality": 8`
       - `"indicagent-ml-discovery": 8`
       - `"indicagent-api": 10`
       - `"indicagent-dashboard": 10`
       Do NOT add `indicagent-hmm-training` or `indicagent-feature-validation` (phantom — not in live system).
    2. Fix `indicagent-intelligence-pipeline` priority: change to 6 (cross-asset stays 5, since intelligence-pipeline consumes cross-asset topic output per audit D-8). Confirm the current value by reading before editing.
    3. Leave `indicagent-roll-compute` at priority 8 (it is a timer oneshot; audit D-10's priority-3 suggestion assumes a daemon model that does not apply — add a one-line comment explaining this).
    4. Add to `_LAG_THRESHOLDS` (lines 99-121): `"indicagent-graduation-compute": 500` and `"indicagent-roll-compute": 500` (both are Kafka consumers currently missing thresholds). Use 500 to match the existing default pattern in that dict.
    5. Fix `_AGENT_ID_TO_UNIT` (line ~130): the key `"feature_writer"` must become `"feature_writer_agent"` to match the agent's `name=` arg. Verify by checking the `name=` in feature_writer_agent.py before editing. Map it to `"indicagent-feature-writer"`.
    6. Add `_AGENT_ID_TO_UNIT` entries for any of the 9 new services that are Kafka consumers with a consumer-lag metric (redpanda-ready/watchdog and api/dashboard are NOT consumers — skip them; ml-* and shadow-auditor and weight-updater are oneshots that publish/consume — add mappings only if they emit `PERSISTENCE_CONSUMER_LAG` with an agent_id label; if uncertain, omit rather than guess a wrong key).
  </action>
  <verify>
    `grep -c "indicagent-redpanda-ready\|indicagent-redpanda-watchdog\|indicagent-weight-updater\|indicagent-shadow-auditor\|indicagent-ml-orchestrator\|indicagent-ml-data-quality\|indicagent-ml-discovery\|indicagent-api\|indicagent-dashboard" services/service_auditor_agent.py` returns >= 9.
    `grep -n "feature_writer_agent" services/service_auditor_agent.py` returns a match.
    `grep -n "\"feature_writer\":" services/service_auditor_agent.py ; echo "exit=$?"` returns no match (old wrong key gone).
    `grep -n "indicagent-graduation-compute\|indicagent-roll-compute" services/service_auditor_agent.py` shows both in _LAG_THRESHOLDS region.
    `.venv/bin/python -c "import services.service_auditor_agent"` exits 0.
  </verify>
  <acceptance_criteria>
    - All 9 service names appear in `_DAG_ORDER`.
    - `indicagent-intelligence-pipeline` priority is 6.
    - `"feature_writer_agent"` key present; `"feature_writer"` key absent.
    - `indicagent-graduation-compute` and `indicagent-roll-compute` present in `_LAG_THRESHOLDS`.
    - Module imports cleanly.
  </acceptance_criteria>
  <done>All three registries are complete and accurate; module imports.</done>
</task>

<task type="auto">
  <name>Task 2: Add _ONESHOT_UNITS frozenset and guard the restart loops</name>
  <files>services/service_auditor_agent.py</files>
  <read_first>
    - services/service_auditor_agent.py (lines 53-96 dicts; lines 371-385 stall restart loop; lines 460-525 graduated restart in _evaluate_service_dynamic)
    - .planning/phases/phase-106/106-PATTERNS.md (_ONESHOT_UNITS guard pattern)
  </read_first>
  <action>
    1. Add a module-level `_ONESHOT_UNITS: frozenset[str]` near the other registries (after `_AGENT_ID_TO_UNIT`) containing the timer-triggered oneshots:
       `frozenset({"indicagent-weight-updater", "indicagent-shadow-auditor", "indicagent-ml-orchestrator", "indicagent-ml-data-quality", "indicagent-ml-discovery", "indicagent-ml-training", "indicagent-ml-signal-training-materialize", "indicagent-roll-compute"})`
    2. Guard the stall-detection restart at line ~381 (inside `_prometheus_check_loop`): before `await self._restart_service_by_unit(unit)`, add `if unit in _ONESHOT_UNITS: continue`.
    3. Guard the graduated restart in `_evaluate_service_dynamic` (lines ~470 and ~522 both call `_restart_service_by_unit`): at the top of the dynamic-evaluation restart decision, add an early `if unit in _ONESHOT_UNITS: return` so a oneshot that is `inactive (dead)` between runs is never treated as a failure. Place the guard so it short-circuits BEFORE the "dead/failed" branch that triggers restart.
    4. Add a brief comment citing CLAUDE.md: "ML batch services inactive (dead) between runs is correct — do not restart."
  </action>
  <verify>
    `grep -n "_ONESHOT_UNITS" services/service_auditor_agent.py` shows the definition plus at least 2 guard usages.
    `.venv/bin/python -c "import services.service_auditor_agent"` exits 0.
    `.venv/bin/pytest tests/unit/services/ -k "auditor" -q` passes (or reports no tests if none exist — must not error).
  </verify>
  <acceptance_criteria>
    - `grep -c "_ONESHOT_UNITS" services/service_auditor_agent.py` >= 3 (1 definition + 2+ guards).
    - Both restart paths (stall loop and graduated eval) reference `_ONESHOT_UNITS`.
    - Module imports cleanly; auditor tests pass.
  </acceptance_criteria>
  <done>Oneshot services cannot be restarted by either auditor restart path.</done>
</task>

<task type="auto">
  <name>Task 3: Fix systemd unit file dependencies pointing at non-existent units</name>
  <files>production/systemd/indicagent-intelligence-pipeline.service, production/systemd/indicagent-alerting-agent.service, production/systemd/indicagent-dlq-drain.service</files>
  <read_first>
    - production/systemd/indicagent-intelligence-pipeline.service
    - production/systemd/indicagent-alerting-agent.service
    - production/systemd/indicagent-dlq-drain.service
    - .planning/phases/phase-106/106-RESEARCH.md (Systemd unit files section)
  </read_first>
  <action>
    1. In `indicagent-intelligence-pipeline.service`: the `After=` clause references the non-existent `indicagent-bar-aggregator-compute.service`. The correct unit is `indicagent-bar-aggregator.service`. Read the file, confirm the wrong token, and replace it. If the file does not exist under `production/systemd/`, search `grep -rln "bar-aggregator-compute" production/systemd/ /etc/systemd/system/` and fix the reference file in `production/systemd/` only (do not edit `/etc/systemd/system` — that is the installed copy).
    2. In `indicagent-alerting-agent.service` and `indicagent-dlq-drain.service`: replace any dependency on the non-existent `redpanda.service` with `indicagent-redpanda-ready.service` in both `After=` and `Requires=`/`Wants=` clauses.
    3. If any of these three reference files do not exist in `production/systemd/`, note it in the SUMMARY but do not fabricate a file. Edit only files that exist.
  </action>
  <verify>
    `grep -rn "bar-aggregator-compute\|[^-]redpanda.service" production/systemd/ ; echo "exit=$?"` returns no matches (the bad references are gone). Note `indicagent-redpanda-ready.service` is the valid replacement and may still appear.
    `grep -rn "indicagent-redpanda-ready.service" production/systemd/indicagent-alerting-agent.service production/systemd/indicagent-dlq-drain.service` returns matches (if those files exist).
  </verify>
  <acceptance_criteria>
    - No `production/systemd/` file references `indicagent-bar-aggregator-compute.service`.
    - No `production/systemd/` file references bare `redpanda.service` as a dependency.
    - alerting and dlq-drain units reference `indicagent-redpanda-ready.service`.
  </acceptance_criteria>
  <done>All systemd `After=`/dependency clauses reference units that exist.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -c "import services.service_auditor_agent"` exits 0.
- `grep -rn "bar-aggregator-compute" production/systemd/` returns nothing.
- `.venv/bin/pytest tests/unit/services/ -q` stays green.
</verification>

<success_criteria>
- All 9 missing services registered with correct priorities.
- `_ONESHOT_UNITS` guards both restart paths.
- Lag thresholds and agent-id mappings corrected.
- systemd dependencies reference real units.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-106/106-02-SUMMARY.md`
</output>
