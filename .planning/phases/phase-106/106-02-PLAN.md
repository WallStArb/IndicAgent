---
phase: phase-106-foundation-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/service_auditor_agent.py
  - production/systemd/indicagent-intelligence-pipeline.service
  - production/systemd/indicagent-alerting-agent.service
  - production/systemd/indicagent-dlq-drain.service
  - production/systemd/indicagent-bar-aggregator.service
autonomous: true

must_haves:
  truths:
    - "intelligence-pipeline systemd unit correctly declares After=indicagent-bar-aggregator.service (not bar-aggregator-compute)"
    - "alerting-agent and dlq-drain declare Requires=indicagent-redpanda-ready.service"
    - "All 11 previously-missing services appear in _DAG_ORDER with correct priority levels"
    - "_AGENT_ID_TO_UNIT maps feature_writer_agent (not feature_writer)"
    - "cross-asset at priority 5, intelligence-pipeline at priority 6 in _DAG_ORDER"
    - "roll-compute at priority 3 in _DAG_ORDER (not 8)"
    - "indicagent-bar-aggregator.service reference file exists in production/systemd/"
  artifacts:
    - path: "services/service_auditor_agent.py"
      provides: "_DAG_ORDER with all 11 new services; fixed priorities; fixed _AGENT_ID_TO_UNIT"
      contains: "indicagent-weight-updater"
    - path: "production/systemd/indicagent-intelligence-pipeline.service"
      provides: "After= referencing bar-aggregator (not bar-aggregator-compute)"
      contains: "indicagent-bar-aggregator.service"
    - path: "production/systemd/indicagent-alerting-agent.service"
      provides: "redpanda-ready dependency"
      contains: "indicagent-redpanda-ready.service"
    - path: "production/systemd/indicagent-dlq-drain.service"
      provides: "redpanda-ready dependency"
      contains: "indicagent-redpanda-ready.service"
    - path: "production/systemd/indicagent-bar-aggregator.service"
      provides: "Versioned reference file for bar-aggregator"
  key_links:
    - from: "production/systemd/indicagent-intelligence-pipeline.service"
      to: "indicagent-bar-aggregator.service"
      via: "After= and Wants= declarations"
      pattern: "After=.*indicagent-bar-aggregator\\.service"
    - from: "services/service_auditor_agent.py"
      to: "_DAG_ORDER dict entry"
      via: "indicagent-weight-updater at priority 8"
      pattern: "indicagent-weight-updater"
---

<objective>
Fix all systemd unit file wiring gaps and service_auditor_agent._DAG_ORDER correctness
issues identified in the DAG audit. After this plan, cold-boot service ordering is
deterministic, the 11 phantom services are monitored, and the feature-writer lag metric
is correctly associated to its agent ID.

Purpose: These wiring bugs cause silent failures on cold boot and leave 11 deployed
services completely invisible to the service auditor. Fixed before v2.8 because new AI
Platform services will be added to this registry.
Output: Correct systemd unit files applied to /etc/systemd/system/, updated _DAG_ORDER
in service_auditor_agent.py, bar-aggregator.service reference file created.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@docs/architecture/audit-2026-05-23-dag.md
@services/service_auditor_agent.py
@production/systemd/indicagent-intelligence-pipeline.service
@production/systemd/indicagent-alerting-agent.service
@production/systemd/indicagent-dlq-drain.service
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix systemd unit files — intelligence-pipeline, alerting-agent, dlq-drain, bar-aggregator</name>
  <files>
    production/systemd/indicagent-intelligence-pipeline.service
    production/systemd/indicagent-alerting-agent.service
    production/systemd/indicagent-dlq-drain.service
    production/systemd/indicagent-bar-aggregator.service
  </files>
  <action>
    Addresses findings D-2, D-3, D-4, D-8, D-9.

    Read the installed unit files before editing to capture current full content:
    sudo cat /etc/systemd/system/indicagent-intelligence-pipeline.service
    sudo cat /etc/systemd/system/indicagent-alerting-agent.service
    sudo cat /etc/systemd/system/indicagent-dlq-drain.service
    sudo cat /etc/systemd/system/indicagent-bar-aggregator.service

    1. indicagent-intelligence-pipeline.service (D-3, D-8, D-9):
       Change After= line from:
         After=network-online.target indicagent-bar-aggregator-compute.service indicagent-provider-merger.service
       To:
         After=network-online.target indicagent-bar-aggregator.service indicagent-provider-merger.service indicagent-cross-asset.service indicagent-macro-compute.service
       Change Wants= line from:
         Wants=indicagent-bar-aggregator-compute.service
       To:
         Wants=indicagent-bar-aggregator.service indicagent-cross-asset.service indicagent-macro-compute.service

    2. indicagent-alerting-agent.service (D-4):
       Change After=network-online.target redpanda.service
       To:   After=network-online.target indicagent-redpanda-ready.service
       Add line: Requires=indicagent-redpanda-ready.service

    3. indicagent-dlq-drain.service (D-4):
       Same as alerting-agent: replace redpanda.service with indicagent-redpanda-ready.service,
       add Requires=indicagent-redpanda-ready.service.

    4. indicagent-bar-aggregator.service (D-2):
       Copy exact content of /etc/systemd/system/indicagent-bar-aggregator.service into
       production/systemd/indicagent-bar-aggregator.service. This creates the missing reference file.

    Apply to live system (daemon-reload only — do NOT restart running services):
    sudo cp production/systemd/indicagent-intelligence-pipeline.service /etc/systemd/system/
    sudo cp production/systemd/indicagent-alerting-agent.service /etc/systemd/system/
    sudo cp production/systemd/indicagent-dlq-drain.service /etc/systemd/system/
    sudo systemctl daemon-reload
  </action>
  <verify>
    sudo systemctl cat indicagent-intelligence-pipeline | grep -E "After=|Wants="
    sudo systemctl cat indicagent-alerting-agent | grep -E "After=|Requires="
    sudo systemctl cat indicagent-dlq-drain | grep -E "After=|Requires="
    test -f /home/bg/dev/indicagent/production/systemd/indicagent-bar-aggregator.service && echo "PASS: bar-aggregator.service created" || echo "FAIL"
    grep "bar-aggregator-compute" production/systemd/indicagent-intelligence-pipeline.service && echo "FAIL: ghost unit still present" || echo "PASS: ghost unit removed"
    grep "redpanda.service" production/systemd/indicagent-alerting-agent.service && echo "FAIL: raw redpanda.service still present" || echo "PASS"
  </verify>
  <done>
    - intelligence-pipeline After= references indicagent-bar-aggregator.service (not -compute variant)
    - intelligence-pipeline After= also declares cross-asset and macro-compute ordering
    - alerting-agent and dlq-drain have Requires=indicagent-redpanda-ready.service
    - production/systemd/indicagent-bar-aggregator.service exists
    - systemctl daemon-reload applied successfully
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix _DAG_ORDER priorities, add 11 missing services, fix _AGENT_ID_TO_UNIT</name>
  <files>
    services/service_auditor_agent.py
  </files>
  <action>
    Addresses findings D-1, D-7, D-8, D-10, D-17.

    Read services/service_auditor_agent.py in full first to understand _DAG_ORDER,
    _AGENT_ID_TO_UNIT, _LAG_THRESHOLDS, and any restart-loop logic.

    1. Priority reorder (D-8, D-10):
       - Ensure indicagent-cross-asset is at priority 5
       - Move indicagent-intelligence-pipeline from priority 5 to priority 6
       - Move indicagent-roll-compute from priority 8 to priority 3

    2. Add 11 missing services to _DAG_ORDER (D-1):
       Priority 0 (infrastructure sentinels — non-restartable, sentinel entries):
         "indicagent-redpanda-ready": 0,
         "indicagent-redpanda-watchdog": 0,
       Priority 8 (analytics batch — timer-triggered oneshots):
         "indicagent-weight-updater": 8,       # Type=oneshot; inactive(dead) between runs is normal
         "indicagent-shadow-auditor": 8,        # Type=oneshot; inactive(dead) between runs is normal
         "indicagent-ml-orchestrator": 8,       # Type=oneshot; inactive(dead) between runs is normal
         "indicagent-ml-data-quality": 8,       # Type=oneshot; inactive(dead) between runs is normal
         "indicagent-ml-discovery": 8,          # Type=oneshot; inactive(dead) between runs is normal
         "indicagent-hmm-training": 8,          # Type=oneshot; inactive(dead) between runs is normal
         "indicagent-feature-validation": 8,    # Type=oneshot; inactive(dead) between runs is normal
       Priority 10 (always-on top-level):
         "indicagent-api": 10,
         "indicagent-dashboard": 10,

    3. Fix _AGENT_ID_TO_UNIT key mismatch (D-7):
       Change "feature_writer": "indicagent-feature-writer"
       To:    "feature_writer_agent": "indicagent-feature-writer"

    4. Add missing lag thresholds (D-17):
       Add to _LAG_THRESHOLDS:
         "indicagent-graduation-compute": 500,
         "indicagent-roll-compute": 500,

    5. Oneshot services must not be auto-restarted by the graduated restart loop.
       Check whether a _ONESHOT_UNITS set (or similar) already exists. If it does, add
       the 7 oneshot services to it. If it does not exist, add:
         _ONESHOT_UNITS: frozenset[str] = frozenset({
             "indicagent-weight-updater",
             "indicagent-shadow-auditor",
             "indicagent-ml-orchestrator",
             "indicagent-ml-data-quality",
             "indicagent-ml-discovery",
             "indicagent-hmm-training",
             "indicagent-feature-validation",
         })
       And guard the restart call in the graduated restart loop:
         if unit_name in _ONESHOT_UNITS:
             continue  # timer-triggered; systemd timer handles restart
  </action>
  <verify>
    cd /home/bg/dev/indicagent
    .venv/bin/python -c "import services.service_auditor_agent; print('OK')"
    grep -n "indicagent-weight-updater" services/service_auditor_agent.py
    grep -n "indicagent-redpanda-ready" services/service_auditor_agent.py
    grep -n "feature_writer_agent" services/service_auditor_agent.py
    grep -n "indicagent-roll-compute.*[0-9]" services/service_auditor_agent.py
    .venv/bin/pytest tests/unit/ -q --tb=short -x 2>&1 | tail -10
  </verify>
  <done>
    - _DAG_ORDER contains all 11 previously missing services at correct priorities
    - indicagent-intelligence-pipeline is at priority 6
    - indicagent-roll-compute is at priority 3
    - _AGENT_ID_TO_UNIT maps "feature_writer_agent" (not "feature_writer")
    - _LAG_THRESHOLDS includes graduation-compute and roll-compute at 500
    - Oneshot services excluded from graduated restart loop
    - All unit tests pass
  </done>
</task>

</tasks>

<verification>
sudo systemctl cat indicagent-intelligence-pipeline | grep "bar-aggregator-compute" && echo "FAIL" || echo "PASS: ghost unit gone"
sudo systemctl cat indicagent-intelligence-pipeline | grep "After=" | grep "bar-aggregator.service"
grep -c "indicagent-weight-updater" services/service_auditor_agent.py
grep "feature_writer_agent" services/service_auditor_agent.py
.venv/bin/pytest tests/unit/ -q 2>&1 | tail -5
</verification>

<success_criteria>
- Cold boot dependency ordering correct: bar-aggregator starts before intelligence-pipeline
- alerting-agent and dlq-drain wait for Redpanda readiness gate on cold boot
- Service auditor monitors all 11 previously-phantom services
- feature_writer consumer lag visible in Prometheus (agent ID matches)
- roll-compute restarts at priority 3 (before intelligence pipeline) after outage
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/phase-106/106-02-SUMMARY.md`
</output>
