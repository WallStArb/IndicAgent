---
phase: 106-foundation-hardening
plan: 06
type: execute
wave: 3
depends_on: [106-01, 106-02, 106-03, 106-04, 106-05]
files_modified:
  - tests/unit/services/test_service_auditor_agent.py
  - tests/unit/intelligence/test_state_manager.py
  - tests/unit/intelligence/test_plugin_circuit_breaker_wiring.py
  - tests/unit/services/test_pipeline_backpressure.py
autonomous: true

must_haves:
  truths:
    - "A test proves oneshot services are never restarted by the auditor"
    - "A test proves _states_by_key stays consistent with _plugin_states across update/update_batch/restore"
    - "A test proves get_all_states_for returns the same result as the old O(N) scan"
    - "A test proves circuit_breakers dict is populated and breakers are shadow-mode by default"
    - "A test proves intel/journal enqueue uses the blocking path"
    - "Full unit suite is green"
  artifacts:
    - path: "tests/unit/intelligence/test_state_manager.py"
      provides: "Index/primary parity regression tests"
      contains: "_states_by_key"
  key_links:
    - from: "tests"
      to: "Phase 106 changes 106-01..106-05"
      via: "regression assertions"
      pattern: "def test_"
---

<objective>
Add focused regression tests proving the Phase 106 structural changes work and cannot silently regress: oneshot restart guard, state-manager index parity, circuit-breaker wiring + shadow default, and backpressure enqueue. Then confirm the full unit suite is green.

Purpose: These are structural invariants (auditor never restarts oneshots, index never drifts, breakers stay transparent in shadow mode). Without tests they regress invisibly.
Output: New test files + green full suite.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-106/106-RESEARCH.md
@.planning/phases/phase-106/106-PATTERNS.md
@CLAUDE.md
</context>

<threat_model>
- **Tests that pass vacuously:** A test that imports but never asserts the new behavior gives false confidence. Mitigation: each test must assert an observable difference (oneshot skipped, index equals scan, dict non-empty, blocking awaited).
- **Mock pitfalls:** `get_connection()` returns an async context manager — per CLAUDE.md mock with `MagicMock(side_effect=async_cm_func)`, not `AsyncMock`. Restart calls should be mocked to assert call/no-call.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Auditor oneshot-guard + DAG completeness tests</name>
  <files>tests/unit/services/test_service_auditor_agent.py</files>
  <read_first>
    - services/service_auditor_agent.py (_DAG_ORDER, _ONESHOT_UNITS, restart paths)
    - tests/unit/services/ (existing auditor test if present, for fixture style)
  </read_first>
  <action>
    Create/extend `tests/unit/services/test_service_auditor_agent.py`:
    1. `test_all_live_services_in_dag_order`: assert each of the 9 added units is a key in `_DAG_ORDER` with the expected priority (0 for redpanda-ready/watchdog, 8 for ml-*/shadow-auditor/weight-updater, 10 for api/dashboard).
    2. `test_intelligence_pipeline_priority_is_6`: assert `_DAG_ORDER["indicagent-intelligence-pipeline"] == 6`.
    3. `test_oneshot_units_not_restarted`: mock `_restart_service_by_unit` (AsyncMock) and drive the graduated-eval path with a oneshot unit in a failed/dead state; assert the restart mock was NOT awaited. Also verify the stall-loop guard by exercising that path with a oneshot unit.
    4. `test_lag_thresholds_cover_consumers`: assert `indicagent-graduation-compute` and `indicagent-roll-compute` are keys in `_LAG_THRESHOLDS`.
    5. `test_agent_id_to_unit_feature_writer_key`: assert `_AGENT_ID_TO_UNIT["feature_writer_agent"] == "indicagent-feature-writer"` and that the old `"feature_writer"` key is absent.
  </action>
  <verify>`.venv/bin/pytest tests/unit/services/test_service_auditor_agent.py -q` passes.</verify>
  <acceptance_criteria>
    - 5 tests present and passing.
    - `test_oneshot_units_not_restarted` asserts the restart mock is not awaited for a oneshot unit.
  </acceptance_criteria>
  <done>Auditor DAG completeness and oneshot guard are regression-locked.</done>
</task>

<task type="auto">
  <name>Task 2: State-manager index parity + backpressure tests</name>
  <files>tests/unit/intelligence/test_state_manager.py, tests/unit/services/test_pipeline_backpressure.py</files>
  <read_first>
    - src/intelligence/pipeline/state_manager.py (update, update_batch, get_all_states_for, checkpoint restore)
    - src/intelligence/pipeline/output_queue.py (enqueue, enqueue_blocking)
    - services/intelligence_pipeline_agent.py (enqueue call sites)
  </read_first>
  <action>
    Create `tests/unit/intelligence/test_state_manager.py`:
    1. `test_index_matches_scan_after_update`: populate via `update`/`update_batch` for multiple (symbol, tf, plugin) combos; assert `get_all_states_for(symbol, tf)` equals the brute-force scan `{p: s for (p, sym, t), s in mgr._plugin_states.items() if sym==symbol and t==tf}`.
    2. `test_index_rebuilt_on_restore`: simulate checkpoint restore (populate `_plugin_states` then call the restore/rebuild path); assert `_states_by_key` is rebuilt and equals the scan, and assert the checkpoint SAVE payload does NOT contain `_states_by_key` (inspect the serialized dict).
    3. `test_get_all_states_empty_key`: `get_all_states_for("NONE", "1m")` returns `{}`.

    Create `tests/unit/services/test_pipeline_backpressure.py`:
    4. `test_intel_and_journal_use_blocking_enqueue`: this is a structural assertion — grep-style is brittle, so instead instantiate (or mock) the agent's out_queue with a spy where `enqueue` raises AssertionError if called and `enqueue_blocking` is an AsyncMock; drive the code path that publishes intel and journal; assert `enqueue_blocking` was awaited and `enqueue` (non-blocking) was never called. If full path instantiation is too heavy, assert via source inspection: read the agent source and assert it contains no `self._out_queue.enqueue(` substring (only `enqueue_blocking`).
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/test_state_manager.py tests/unit/services/test_pipeline_backpressure.py -q` passes.
  </verify>
  <acceptance_criteria>
    - Index-parity, restore-rebuild, and empty-key tests pass.
    - Backpressure test proves blocking enqueue is used for intel + journal and non-blocking is not.
  </acceptance_criteria>
  <done>State index consistency and lossless enqueue are regression-locked.</done>
</task>

<task type="auto">
  <name>Task 3: Circuit-breaker wiring + shadow-mode test, then full suite</name>
  <files>tests/unit/intelligence/test_plugin_circuit_breaker_wiring.py</files>
  <read_first>
    - services/intelligence_pipeline_agent.py (circuit_breakers population)
    - src/intelligence/pipeline/executor.py (_get_plugin_cb)
    - src/observability/circuit_breaker.py / src/core/plugin_circuit_breaker.py (whichever got the enabled flag)
  </read_first>
  <action>
    Create `tests/unit/intelligence/test_plugin_circuit_breaker_wiring.py`:
    1. `test_circuit_breakers_populated`: build the agent's circuit_breakers dict (call the helper added in 106-05) with a small fake plugin registry; assert the dict is non-empty and keyed by plugin name.
    2. `test_breakers_default_shadow_mode`: assert a breaker constructed without `PLUGIN_CB_ENABLED` has `allow_request()` returning True even after recording failures past the threshold (transparent passthrough proves shadow mode).
    3. `test_enabled_breaker_opens`: monkeypatch the env/flag to enabled, record failures past threshold, assert `allow_request()` returns False (proves the gate still works when enabled).
    4. Run the FULL unit suite `.venv/bin/pytest tests/unit/ -q` and confirm green. Run `.venv/bin/ruff check . ` and `.venv/bin/black --check .` (format if needed).
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/test_plugin_circuit_breaker_wiring.py -q` passes.
    `.venv/bin/pytest tests/unit/ -q` passes (full suite green).
    `.venv/bin/ruff check .` clean.
  </verify>
  <acceptance_criteria>
    - 3 circuit-breaker tests pass.
    - Full `tests/unit/` suite green.
    - ruff clean.
  </acceptance_criteria>
  <done>Circuit-breaker wiring + shadow behavior locked; entire Phase 106 verified green.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/ -q` passes end to end.
- `.venv/bin/ruff check .` reports no errors.
</verification>

<success_criteria>
- New regression tests cover every Phase 106 structural change.
- Full unit suite green; lint clean.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-106/106-06-SUMMARY.md`
</output>
