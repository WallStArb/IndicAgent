---
status: testing
phase: 106-foundation-hardening
source: [106-01-SUMMARY.md, 106-02-SUMMARY.md, 106-03-SUMMARY.md, 106-04-SUMMARY.md, 106-05-SUMMARY.md, 106-06-SUMMARY.md]
started: 2026-05-25T01:10:00Z
updated: 2026-05-25T01:10:00Z
---

## Current Test

number: 1
name: Dead Files Deleted
expected: |
  src/core/ml/shadow.py and src/core/llm/guardrails.py no longer exist.
  Running `ls src/core/ml/shadow.py src/core/llm/guardrails.py` should return file-not-found errors.
awaiting: user response

## Tests

### 1. Dead Files Deleted
expected: src/core/ml/shadow.py and src/core/llm/guardrails.py no longer exist. Running `ls src/core/ml/shadow.py src/core/llm/guardrails.py` returns file-not-found errors.
result: [pending]

### 2. Dead Settings Fields Removed
expected: The 7 removed fields (LLM_RATE_LIMIT_RPM, LLM_RATE_LIMIT_TPM, SHADOW_CORRELATION_THRESHOLD, SHADOW_MIN_SAMPLES, LANGFUSE_HOST, MLFLOW_TRACKING_URI, SWARM_QUEUE_TIMEOUT_MS) are absent from src/config/settings.py. `grep -c "LLM_RATE_LIMIT_RPM\|LANGFUSE_HOST\|MLFLOW_TRACKING_URI" src/config/settings.py` returns 0.
result: [pending]

### 3. TEMPLATE_agent Uses Audited LLM Wrapper
expected: src/intelligence/ai/TEMPLATE_agent.py calls self._llm_generate() not self._llm.generate(). `grep "self._llm.generate" src/intelligence/ai/TEMPLATE_agent.py` returns no output.
result: [pending]

### 4. Oneshot Services Cannot Be Restarted by Auditor
expected: _ONESHOT_UNITS frozenset exists in service_auditor_agent.py and contains indicagent-roll-compute. `grep -c "_ONESHOT_UNITS" services/service_auditor_agent.py` returns at least 4 (definition + 3 guard call sites).
result: [pending]

### 5. Intel + Journal Output Uses Blocking Enqueue
expected: No bare self._out_queue.enqueue() calls exist in intelligence_pipeline_agent.py — only enqueue_blocking. `grep "self._out_queue.enqueue(" services/intelligence_pipeline_agent.py` returns no output.
result: [pending]

### 6. Plugin Circuit Breakers in Shadow Mode by Default
expected: All plugin circuit breakers are built with enabled=False so live routing is unchanged. `grep "enabled=False" services/intelligence_pipeline_agent.py` returns at least 1 match, and the _build_plugin_circuit_breakers method exists.
result: [pending]

### 7. Per-Tier Latency Histogram Defined
expected: intelligence_pipeline_tier_latency_ms histogram exists in feature_pipeline_executor.py. `grep "tier_latency_ms" src/intelligence/pipeline/feature_pipeline_executor.py` returns at least 1 match.
result: [pending]

### 8. Regression Test Suite Passes
expected: The 18 new regression tests pass. Running `.venv/bin/pytest tests/unit/intelligence/test_plugin_state_manager.py tests/unit/intelligence/test_plugin_circuit_breaker_wiring.py tests/unit/services/test_pipeline_backpressure.py tests/unit/services/test_service_auditor_agent.py -q` shows all tests passing (some pre-existing failures in other files are expected, but these 4 files should be clean).
result: [pending]

## Summary

total: 8
passed: 0
issues: 0
pending: 8
skipped: 0
blocked: 0

## Gaps

[none yet]
