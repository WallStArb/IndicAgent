---
phase: phase-106-foundation-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/intelligence_pipeline_agent.py
  - src/intelligence/pipeline/feature_pipeline_executor.py
  - src/observability/metrics.py
  - services/shadow_auditor_agent.py
  - services/bar_writer_agent.py
  - src/core/agent/base.py
autonomous: true

must_haves:
  truths:
    - "Pipeline latency histograms export p50/p95/p99 in Grafana (not flat zero)"
    - "Per-stage histograms exist for I1, I2-I3 wave, I4 wave, I5+SMC wave, I6"
    - "Hot path bar-to-signal is wrapped in a single OTel span (pipeline.process_bar_inner)"
    - "Shadow governance metrics show correct absolute values (not ever-accumulating totals)"
    - "All metric emission sites use agent_id label key (no agent/agent_id split cardinality)"
    - "BarWriterAgent._run() calls _record_message_consumed() so stall watchdog sees it"
  artifacts:
    - path: "services/intelligence_pipeline_agent.py"
      provides: "histogram instruments for pipeline/i1/i7 latency; observed_span wrapper on _process_bar_inner"
      contains: "create_histogram"
    - path: "src/intelligence/pipeline/feature_pipeline_executor.py"
      provides: "per-stage tier latency histograms recorded at each wave boundary"
      contains: "tier_latency"
    - path: "src/observability/metrics.py"
      provides: "shadow metrics changed to create_gauge"
      contains: "create_gauge"
    - path: "src/core/agent/base.py"
      provides: "agent_id label key for AGENT_LAST_MESSAGE_TIMESTAMP_SECONDS and AGENT_CRASH_TOTAL"
      contains: "agent_id"
  key_links:
    - from: "services/intelligence_pipeline_agent.py"
      to: "src/observability/spans.py"
      via: "observed_span('pipeline.process_bar_inner')"
      pattern: "observed_span.*process_bar"
    - from: "src/intelligence/pipeline/feature_pipeline_executor.py"
      to: "src/observability/metrics.py"
      via: "histogram.record() at each tier boundary"
      pattern: "tier_latency.*record"
    - from: "services/shadow_auditor_agent.py"
      to: "src/observability/metrics.py"
      via: ".set() on create_gauge instruments"
      pattern: "\\.set\\("
---

<objective>
Replace all mis-typed OTel instruments on the hot path and add span coverage to
intelligence_pipeline_agent._process_bar_inner(). This unblocks p50/p95/p99 latency
visibility in Grafana, fixes shadow governance dashboards that currently show permanently
inflated values, and closes the label cardinality split that prevents cross-agent aggregation.

Purpose: Observability is the prerequisite for every v2.8 AI agent improvement. Without
correct latency histograms and spans, regressions introduced by new agents are invisible.
Output: Correct histogram instruments, per-stage tier latency, one span on the hot path,
correct shadow gauges, unified agent_id label key everywhere.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md
@docs/architecture/audit-2026-05-23-telemetry.md
@docs/architecture/audit-2026-05-23-compute-latency.md
@src/observability/metrics.py
@src/observability/spans.py
@services/intelligence_pipeline_agent.py
@src/intelligence/pipeline/feature_pipeline_executor.py
@services/shadow_auditor_agent.py
@services/bar_writer_agent.py
@src/core/agent/base.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix metric instrument types — latency histograms, shadow gauges, agent_id label</name>
  <files>
    services/intelligence_pipeline_agent.py
    src/observability/metrics.py
    services/shadow_auditor_agent.py
    services/bar_writer_agent.py
    src/core/agent/base.py
  </files>
  <action>
    Addresses findings T-2, T-3, T-5, T-6, T-8 — five findings in one pass.

    1. `services/intelligence_pipeline_agent.py:176-193` — replace four gauge() calls with
       correct instrument types:
       - `_i1_latency_ms` → `_meter.create_histogram("intelligence_pipeline_i1_latency_ms", unit="ms", description="I1 tier latency per bar")`
       - `_i7_latency_ms` → `_meter.create_histogram("intelligence_pipeline_i7_latency_ms", unit="ms", description="I7 tier latency per bar")`
       - `_pipeline_latency` → `_meter.create_histogram("intelligence_pipeline_pipeline_latency_ms", unit="ms", description="End-to-end bar processing latency")`
       - `_bars_processed` → `_meter.create_counter("intelligence_pipeline_bars_processed_total", description="Bars processed")`
       Change all .add() call sites on these four instruments to .record() (histograms) or .add(1, ...) (counter).
       Finding T-3: wire the dead instruments. Add `self._i1_latency_ms.record(i1_ms, {"symbol": bar.symbol, "tf": bar.tf})` in _process_bar_inner() or FeaturePipelineExecutor after I1 execution. Find where pipeline_latency_ms is computed (near line 541 per audit) and add `self._pipeline_latency.record(pipeline_latency_ms, {"symbol": bar.symbol, "tf": bar.tf})`.

    2. `src/observability/metrics.py:224-243` — shadow metrics (T-5):
       Change SHADOW_WIN_RATE, SHADOW_N_RESOLVED, SHADOW_EV_R, SHADOW_DAYS_TO_GATE,
       SHADOW_EV_CI_LOWER from create_up_down_counter to create_gauge. Do not change metric names.

    3. `services/shadow_auditor_agent.py:170-194` — change every .add(absolute_value, ...) on
       the five shadow metrics to .set(absolute_value, ...) (point-in-time gauge API).

    4. `src/core/agent/base.py` — label standardization (T-8):
       Line where _last_msg_ts_attrs is set: change {"agent": name} to {"agent_id": name}.
       Line where _crash_attrs is set: change {"agent": ...} to {"agent_id": ...}.
       Do not rename metric instruments.

    5. `services/bar_writer_agent.py` — two fixes (T-6, T-8):
       Find _batch_latency_attrs or equivalent using {"agent": self.name} and change key to "agent_id".
       In BarWriterAgent._run() custom loop (around line 241), inside the async for loop body,
       add self._record_message_consumed() after the message routing check.
  </action>
  <verify>
    cd /home/bg/dev/indicagent
    .venv/bin/python -c "import services.intelligence_pipeline_agent; print('OK')"
    .venv/bin/python -c "import services.shadow_auditor_agent; print('OK')"
    .venv/bin/python -c "import src.core.agent.base; print('OK')"
    grep -n "create_histogram" services/intelligence_pipeline_agent.py
    grep -n "create_gauge" src/observability/metrics.py | grep -i shadow
    grep -n '"agent_id"' src/core/agent/base.py | head -5
    grep -n "_record_message_consumed" services/bar_writer_agent.py
    .venv/bin/pytest tests/unit/ -q --tb=short -x 2>&1 | tail -20
  </verify>
  <done>
    - intelligence_pipeline_i1_latency_ms, _i7_latency_ms, _pipeline_latency are create_histogram
    - Both previously-dead instruments have .record() call sites in the pipeline
    - Shadow metrics are create_gauge and use .set() not .add()
    - base.py uses agent_id label key in _last_msg_ts_attrs and _crash_attrs
    - bar_writer_agent uses agent_id label key and calls _record_message_consumed() in _run() loop
    - All unit tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Per-stage tier latency histograms and process_bar_inner OTel span</name>
  <files>
    src/intelligence/pipeline/feature_pipeline_executor.py
    services/intelligence_pipeline_agent.py
  </files>
  <action>
    Addresses findings T-4, T-16, CL-1.

    1. `src/intelligence/pipeline/feature_pipeline_executor.py` — add tier-latency histogram (T-4, CL-1):
       Read the file to understand the actual structure of run() and how tiers are executed
       (single loop or separate method calls). Then add a module-level histogram:

       from opentelemetry import metrics as _otel_metrics
       _fpe_meter = _otel_metrics.get_meter("indicagent")
       _TIER_LATENCY = _fpe_meter.create_histogram(
           "intelligence_pipeline_tier_latency_ms",
           unit="ms",
           description="Per-tier latency in the feature pipeline executor",
       )

       Wrap each tier boundary with perf_counter and record to _TIER_LATENCY with
       {"tier": tier_name} attribute. Tier names: "i1", "i2_i3", "i4", "i5_smc", "i6".
       If the executor iterates over tier names in a loop (e.g. run_tier(name)), record
       inside the loop keyed on the tier name. If it calls separate methods, add timing
       around each call site.

    2. `services/intelligence_pipeline_agent.py` — add observed_span on _process_bar_inner (T-16):
       Confirm observed_span is already imported (or add: from src.observability.spans import observed_span).
       At the top of _process_bar_inner(), wrap the entire function body:

       async with observed_span(
           "pipeline.process_bar_inner",
           {"symbol": bar.symbol, "tf": bar.tf},
       ):
           <existing body>

       One span only — do not nest additional spans inside for tier execution.
       The tier histograms from step 1 provide per-stage visibility within this span.
  </action>
  <verify>
    cd /home/bg/dev/indicagent
    .venv/bin/python -c "import src.intelligence.pipeline.feature_pipeline_executor; print('OK')"
    .venv/bin/python -c "import services.intelligence_pipeline_agent; print('OK')"
    grep -n "tier_latency\|_TIER_LATENCY" src/intelligence/pipeline/feature_pipeline_executor.py
    grep -n "observed_span" services/intelligence_pipeline_agent.py | grep "process_bar"
    .venv/bin/pytest tests/unit/ -q --tb=short -x 2>&1 | tail -10
  </verify>
  <done>
    - intelligence_pipeline_tier_latency_ms histogram exists in feature_pipeline_executor.py
    - Tier latency recorded for i1, i2_i3, i4, i5_smc, i6
    - _process_bar_inner() body is wrapped in observed_span("pipeline.process_bar_inner")
    - All unit tests pass
  </done>
</task>

</tasks>

<verification>
cd /home/bg/dev/indicagent
.venv/bin/pytest tests/unit/ -q 2>&1 | tail -5
grep -rn "create_histogram" services/intelligence_pipeline_agent.py
grep -rn '"agent_id"' src/core/agent/base.py | grep -v "test"
grep -rn "observed_span" services/intelligence_pipeline_agent.py
grep -rn "\.set(" services/shadow_auditor_agent.py | grep -i shadow
grep -rn "_record_message_consumed" services/bar_writer_agent.py
</verification>

<success_criteria>
- p50/p95/p99 latency queries against intelligence_pipeline_pipeline_latency_ms return real data
- Per-stage visibility exists for I1 through I6 via intelligence_pipeline_tier_latency_ms{tier=...}
- Hot path traceable: every bar's processing enclosed in pipeline.process_bar_inner span
- Shadow governance dashboards show correct absolute values
- Cross-agent Grafana aggregations work: all agents emit agent_id label key
- BarWriterAgent stall detection active: _record_message_consumed() called in _run() loop
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/phase-106/106-01-SUMMARY.md`
</output>
