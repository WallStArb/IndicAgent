---
phase: 112-intelligence-pipeline-signal-integrity
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [PIPE-INT-01]
files_modified:
  - production/migrations/110_add_feature_schema_version_to_intelligence_features.sql
  - production/migrations/111_add_feature_schema_version_to_signal_ledger.sql
  - production/migrations/112_update_signal_ledger_full_view.sql
  - production/migrations/113_reset_setup_performance_to_neutral.sql
  - src/intelligence/schemas.py
  - src/intelligence/pipeline/state_manager.py
  - services/feature_writer.py
  - src/persistence/repository/signal_ledger_repository.py
  # services/signal_writer.py — added to scope pending Task 4 preflight writer inventory
  # (audit step determines if this file needs updating; add to files_modified in SUMMARY if it does)
autonomous: true
must_haves:
  truths:
    - "After Phase 0 deploy + pipeline restart, every NEW intelligence_features row has feature_schema_version = 2"
    - "Pre-fix intelligence_features and signal_ledger rows remain NULL (no backfill) and are excluded by feature_schema_version >= 2 filters"
    - "On next pipeline restart the prior checkpoint is discarded (version mismatch) with a WARNING log, forcing clean state rebuild"
    - "All setup_performance rows are reset so no contaminated setup is eligible for ranking (sample_size = 0)"
    - "feature_writer and signal_ledger_repository INSERT paths write feature_schema_version into the new column — new rows are NOT NULL"
    - "All INSERT paths into signal_ledger and intelligence_features are inventoried; signal_writer.py is explicitly confirmed or ruled out"
    - "signal_ledger_full VIEW dependencies are audited before DROP VIEW CASCADE; all dependent objects are restored in migration 112"
    - "No signal logic behavior changes in this plan — boundary only"
  artifacts:
    - path: "production/migrations/110_add_feature_schema_version_to_intelligence_features.sql"
      provides: "nullable feature_schema_version INTEGER on intelligence_features"
      contains: "ADD COLUMN IF NOT EXISTS feature_schema_version"
    - path: "production/migrations/111_add_feature_schema_version_to_signal_ledger.sql"
      provides: "nullable feature_schema_version INTEGER on signal_ledger"
      contains: "ADD COLUMN IF NOT EXISTS feature_schema_version"
    - path: "production/migrations/112_update_signal_ledger_full_view.sql"
      provides: "signal_ledger_full VIEW recreated with feature_schema_version exposed + all dependencies audited"
      contains: "CREATE VIEW signal_ledger_full"
    - path: "production/migrations/113_reset_setup_performance_to_neutral.sql"
      provides: "contaminated setup_performance rows neutralized"
      contains: "UPDATE setup_performance"
    - path: "src/intelligence/schemas.py"
      provides: "FEATURE_SCHEMA_VERSION constant + IntelligenceEvent field"
      contains: "FEATURE_SCHEMA_VERSION"
    - path: "src/intelligence/pipeline/state_manager.py"
      provides: "CHECKPOINT_VERSION constant + discard-on-mismatch load logic"
      contains: "CHECKPOINT_VERSION"
    - path: "services/feature_writer.py"
      provides: "feature_schema_version wired into _INSERT_FEATURE_SQL + _record_to_insert_params"
      contains: "feature_schema_version"
    - path: "src/persistence/repository/signal_ledger_repository.py"
      provides: "feature_schema_version wired into _INSERT_SQL column list + value params"
      contains: "feature_schema_version"
  key_links:
    - from: "src/intelligence/schemas.py IntelligenceEvent"
      to: "intelligence_features.feature_schema_version column"
      via: "feature_writer persists event.feature_schema_version into the new column"
      pattern: "feature_schema_version"
    - from: "services/feature_writer.py _INSERT_FEATURE_SQL"
      to: "intelligence_features.feature_schema_version column"
      via: "_record_to_insert_params includes event.feature_schema_version at the matching position"
      pattern: "feature_schema_version"
    - from: "src/persistence/repository/signal_ledger_repository.py _INSERT_SQL"
      to: "signal_ledger.feature_schema_version column"
      via: "signal payload carries feature_schema_version from the originating IntelligenceEvent"
      pattern: "feature_schema_version"
    - from: "src/intelligence/pipeline/state_manager.py load path"
      to: "on-disk checkpoint file"
      via: "version field comparison against CHECKPOINT_VERSION on load"
      pattern: "CHECKPOINT_VERSION"
---

<objective>
Establish the forensic contamination boundary BEFORE any logic change. Add `feature_schema_version = 2` markers to DB and the typed event, recreate the `signal_ledger_full` VIEW to expose the new column (after auditing dependencies), reset contaminated `setup_performance`, wire the new column through ALL writer INSERT paths so freshly-produced rows are stamped, and force checkpoint discard on next restart so the pipeline rebuilds clean state.

Purpose: Every row written after this deploy is provably clean (`feature_schema_version = 2`); every pre-fix row stays NULL and is filtered out by downstream training queries. Adding the field to the typed event is NOT sufficient — the hardcoded writer INSERTs must carry it or new rows stay NULL and the boundary is ineffective. This is the atomic boundary — no calibration, ranking, or lifecycle fixes belong here.
Output: 4 migrations in `production/migrations/`, `FEATURE_SCHEMA_VERSION` constant + `IntelligenceEvent` field, writer INSERT wiring for ALL identified paths, `CHECKPOINT_VERSION` discard logic, writer inventory doc in SUMMARY.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-intelligence-pipeline-signal-integrity/112-CONTEXT.md
@.planning/phases/112-intelligence-pipeline-signal-integrity/112-RESEARCH.md
@docs/plans/2026-06-02-intelligence-pipeline-signal-integrity.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit signal_ledger_full VIEW dependencies + add feature_schema_version columns + recreate VIEW</name>
  <read_first>
    - production/migrations/097_signal_ledger_expires_at.sql (the AUTHORITATIVE current signal_ledger_full DDL — copy its SELECT enumeration verbatim, do NOT use SELECT *)
    - production/migrations/095_signal_ledger_split.sql (origin of the view; confirms column list)
    - docs/plans/2026-06-02-intelligence-pipeline-signal-integrity.md (sections 0-A, Migration Checklist, lines 55-62 and 334-344)
  </read_first>
  <action>
    STEP 0 — DEPENDENCY AUDIT (do this first, before writing migration 112):
    Run the following query against the live DB to enumerate all objects that depend on signal_ledger_full:

    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "
    SELECT DISTINCT dependent_ns.nspname AS dep_schema,
                    dependent_view.relname AS dep_name,
                    dependent_view.relkind AS dep_kind
    FROM pg_depend
    JOIN pg_rewrite ON pg_depend.objid = pg_rewrite.oid
    JOIN pg_class AS dependent_view ON pg_rewrite.ev_class = dependent_view.oid
    JOIN pg_class AS referenced_class ON pg_depend.refobjid = referenced_class.oid
    JOIN pg_namespace AS dependent_ns ON dependent_view.relnamespace = dependent_ns.oid
    WHERE referenced_class.relname = 'signal_ledger_full'
    AND dependent_view.relname != 'signal_ledger_full';"

    Record the output. For each dependent object found: capture its full CREATE DDL using `\d+ <name>` or `pg_get_viewdef`. Migration 112 MUST explicitly recreate every dependent view/object AFTER recreating signal_ledger_full so they are not silently dropped. If no dependents are found, document "no dependents found" and proceed.

    MIGRATION 110:
    Create `110_add_feature_schema_version_to_intelligence_features.sql`: wrap in BEGIN/COMMIT. `ALTER TABLE intelligence_features ADD COLUMN IF NOT EXISTS feature_schema_version INTEGER;` (nullable, NO default — pre-fix rows must stay NULL per D-01). Do NOT touch the existing `schema_version text '1.0'` column; this is a separate INTEGER column named `feature_schema_version`.

    MIGRATION 111:
    Create `111_add_feature_schema_version_to_signal_ledger.sql`: wrap in BEGIN/COMMIT. `ALTER TABLE signal_ledger ADD COLUMN IF NOT EXISTS feature_schema_version INTEGER;` (nullable, NO default).

    MIGRATION 112:
    Create `112_update_signal_ledger_full_view.sql`: wrap in BEGIN/COMMIT. `DROP VIEW IF EXISTS signal_ledger_full CASCADE;` then `CREATE VIEW signal_ledger_full AS` copying the EXACT explicit column enumeration from migration 097, add `sl.feature_schema_version` adjacent to `sl.signal_schema_version`. Keep the `LEFT JOIN signal_outcomes so ON sl.signal_id = so.signal_id`. After the CREATE VIEW, add CREATE statements to restore every dependent object found in the audit step above. If the audit found no dependents, add a SQL comment: `-- Dependency audit run YYYY-MM-DD: no dependent views or grants found.`

    Number the files 110, 111, 112 (highest existing in production/migrations/ is 109). These three must apply in order.
  </action>
  <verify>
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/110_add_feature_schema_version_to_intelligence_features.sql && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/111_add_feature_schema_version_to_signal_ledger.sql && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/112_update_signal_ledger_full_view.sql
  </verify>
  <acceptance_criteria>
    - The dependency audit query was run and its output is documented in the SUMMARY (either a list of dependents recreated, or "no dependents found")
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d intelligence_features"` shows a column `feature_schema_version | integer` that is nullable (no "not null")
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d signal_ledger"` shows a column `feature_schema_version | integer` that is nullable
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT feature_schema_version FROM signal_ledger_full LIMIT 1"` exits 0 (column exposed on view)
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM intelligence_features WHERE feature_schema_version IS NOT NULL"` returns 0 (no backfill of existing rows)
    - The view SELECT enumerates columns explicitly (grep confirms `SELECT *` is NOT used in 112_update_signal_ledger_full_view.sql)
    - Migration 112 contains a comment or CREATE statements addressing every dependent object found in the audit
    - If dependents were found, they are accessible after migration 112 runs (query them to confirm)
  </acceptance_criteria>
  <done>Both tables carry a nullable feature_schema_version INTEGER, signal_ledger_full exposes it, all existing rows remain NULL, and all dependent objects on the view are explicitly accounted for and restored.</done>
</task>

<task type="auto">
  <name>Task 2: Reset contaminated setup_performance to neutral</name>
  <read_first>
    - src/intelligence/setup_performance_updater.py (lines 123-158 — confirms perf_multiplier is COMPUTED via Sharpe rank from rows where sample_size >= 30, NOT stored; the eligibility query is `WHERE sample_size >= $1` with MIN_SAMPLE_SIZE=30)
    - production/migrations/021_setup_performance_table.sql (original schema — confirms NO perf_multiplier column, NO signal_schema_version column)
    - docs/plans/2026-06-02-intelligence-pipeline-signal-integrity.md (section 0-D, lines 73-83)
  </read_first>
  <action>
    Create `production/migrations/113_reset_setup_performance_to_neutral.sql` wrapped in BEGIN/COMMIT.

    The live `setup_performance` table has columns: setup_plugin, win_rate, avg_pnl_r, sample_size, sharpe_ratio, timeframe, regime, updated_at, direction, symbol. It has NEITHER a `perf_multiplier` column NOR a `signal_schema_version` column (verified against live DB). The spec's reset SQL referencing `perf_multiplier` and `signal_schema_version < $new_version` cannot run verbatim.

    Reinterpret D-01/0-D: ALL existing rows are contaminated, and `perf_multiplier` is derived at runtime from setups with `sample_size >= 30`. Resetting `sample_size = 0` removes every contaminated setup from the eligibility query, so `run_setup_performance_update` returns no eligible setups and the computed multiplier defaults to neutral 1.0. The reset statement: `UPDATE setup_performance SET sample_size = 0, updated_at = now();` (no WHERE clause — all rows are pre-fix contaminated). Also set `win_rate = 0.0, avg_pnl_r = 0.0, sharpe_ratio = 0.0` so a partial-recovery setup cannot rank on stale stats.

    Add a SQL comment block at the top of the file documenting WHY the spec's `perf_multiplier`/`signal_schema_version` WHERE clause is replaced (column does not exist; resetting sample_size to 0 is the equivalent neutralization because perf_multiplier is computed from eligible rows only).
  </action>
  <verify>
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/113_reset_setup_performance_to_neutral.sql && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM setup_performance WHERE sample_size > 0"
  </verify>
  <acceptance_criteria>
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM setup_performance WHERE sample_size != 0"` returns 0
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT count(*) FROM setup_performance WHERE sample_size >= 30"` returns 0 (no eligible contaminated setups remain)
    - The migration file contains a comment explaining the deviation from the spec's perf_multiplier/signal_schema_version WHERE clause
    - The migration does NOT reference a perf_multiplier column or a signal_schema_version column (grep returns empty for those identifiers as column targets in an UPDATE)
  </acceptance_criteria>
  <done>No setup_performance row is eligible for ranking; the computed perf_multiplier defaults to neutral 1.0 until clean outcomes re-accumulate.</done>
</task>

<task type="auto">
  <name>Task 3: Add FEATURE_SCHEMA_VERSION to schemas.py and CHECKPOINT_VERSION discard logic to state_manager.py</name>
  <read_first>
    - src/intelligence/schemas.py (IntelligenceEvent model definition — find existing field declaration style and any version/schema fields)
    - src/intelligence/pipeline/state_manager.py (current `_AGENT_VERSION = "v1"`, `_CHECKPOINT_PATH`, and the checkpoint load method that deserializes the on-disk file)
    - docs/plans/2026-06-02-intelligence-pipeline-signal-integrity.md (sections 0-A and 0-B, lines 55-67)
    - CLAUDE.md rule: timestamps UTC; structlog never uses event= kwarg (use data=/signal=)
  </read_first>
  <action>
    In `src/intelligence/schemas.py`: add a module-level constant `FEATURE_SCHEMA_VERSION = 2` (integer). Add a field to the `IntelligenceEvent` Pydantic model: `feature_schema_version: int = FEATURE_SCHEMA_VERSION`. This is what feature_writer persists into the new DB column, marking every freshly-produced row as clean.

    In `src/intelligence/pipeline/state_manager.py`: add a module-level constant `CHECKPOINT_VERSION = 2`. In the checkpoint LOAD path: after deserializing the on-disk checkpoint, read its stored `version` field (write `CHECKPOINT_VERSION` into the checkpoint payload on SAVE so loads can compare). If the loaded version != CHECKPOINT_VERSION (including a missing/None version on a legacy checkpoint), DISCARD the checkpoint (treat as cold start — return empty/None state) and emit a structlog WARNING using a non-`event` kwarg, e.g. `logger.warning("checkpoint version mismatch, discarding", loaded_version=<v>, expected_version=CHECKPOINT_VERSION)`. Do NOT raise — discard and rebuild. Ensure the SAVE path writes `version=CHECKPOINT_VERSION` into the persisted dict so future loads round-trip correctly.
  </action>
  <verify>
    .venv/bin/python -c "from src.intelligence.schemas import FEATURE_SCHEMA_VERSION, IntelligenceEvent; assert FEATURE_SCHEMA_VERSION == 2; assert 'feature_schema_version' in IntelligenceEvent.model_fields; print('OK')" && .venv/bin/python -c "from src.intelligence.pipeline.state_manager import CHECKPOINT_VERSION; assert CHECKPOINT_VERSION == 2; print('OK')"
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "from src.intelligence.schemas import FEATURE_SCHEMA_VERSION; print(FEATURE_SCHEMA_VERSION)"` prints `2`
    - `.venv/bin/python -c "from src.intelligence.schemas import IntelligenceEvent; print(IntelligenceEvent().feature_schema_version if 'feature_schema_version' in IntelligenceEvent.model_fields else 'MISSING')"` does not print MISSING and the default equals 2
    - `.venv/bin/python -c "from src.intelligence.pipeline.state_manager import CHECKPOINT_VERSION; print(CHECKPOINT_VERSION)"` prints `2`
    - The state_manager load path discards a checkpoint whose stored version != CHECKPOINT_VERSION and logs a WARNING (grep confirms `logger.warning` with a version-mismatch message and NO `event=` kwarg)
    - The state_manager save path writes `version=CHECKPOINT_VERSION` (grep confirms the version is persisted into the saved payload)
  </acceptance_criteria>
  <done>IntelligenceEvent stamps feature_schema_version=2 on every event; the next pipeline restart discards the old checkpoint and cold-starts clean.</done>
</task>

<task type="auto">
  <name>Task 4: Preflight writer inventory + wire feature_schema_version into ALL writer INSERT paths</name>
  <read_first>
    - services/feature_writer.py — `_INSERT_FEATURE_SQL` at line 63 (the explicit column list + `$N` placeholders) and `_record_to_insert_params` at line 164 (builds the tuple; docstring says "31-element tuple" — that count must increment by one); the call site at line 322 and execute_batch at line 335
    - src/persistence/repository/signal_ledger_repository.py — `_INSERT_SQL` at line 130 (column list + value placeholders); `_to_row()` (used at the execute_batch / conn.execute call sites at lines 405 and 426); trace where the per-signal entry object is built and whether it already carries feature_schema_version from the originating IntelligenceEvent
    - services/signal_writer.py — FULL FILE READ. Determine whether this file converts a Kafka signal payload to a LedgerEntry and calls any INSERT. If it does, it is a missed write path and MUST be updated to carry feature_schema_version.
    - src/intelligence/schemas.py — IntelligenceEvent.feature_schema_version field (added in Task 3); BarIntelligenceRecord shape that feature_writer consumes (confirm it exposes feature_schema_version, or trace how the event field reaches the record)
    - docs/plans/2026-06-02-intelligence-pipeline-signal-integrity.md (section 0-A — the contamination boundary depends on these writes being non-NULL)
  </read_first>
  <action>
    STEP 0 — PREFLIGHT WRITER INVENTORY (do this before any code edits):
    Run: `grep -rn "INSERT INTO signal_ledger\|INSERT INTO intelligence_features" services/ src/`
    Record every file and line that appears. Explicitly classify each one:
    - services/feature_writer.py → being updated in this task (HIGH priority)
    - src/persistence/repository/signal_ledger_repository.py → being updated in this task (HIGH priority)
    - services/signal_writer.py → AUDIT: determine if it writes signal_ledger (see below)
    - Any other file found → classify as "updating in this task", "explicitly out of scope (document reason)", or "follow-up required"
    Record this inventory table in the SUMMARY as "Writer INSERT Inventory — Plan 01".

    SIGNAL_WRITER.PY AUDIT: read the full file. If signal_writer.py constructs a LedgerEntry or executes an INSERT INTO signal_ledger (directly or via a repository call), then:
    - It MUST be updated to carry feature_schema_version in the Kafka payload it consumes or defaults to FEATURE_SCHEMA_VERSION constant at the write site
    - Add services/signal_writer.py to this task's scope and to files_modified in the SUMMARY
    If signal_writer.py does NOT write to signal_ledger (e.g. it only publishes to Kafka and the repository handles persistence), document "signal_writer.py: read-only / no INSERT path — confirmed not a missed write path" in the SUMMARY.

    FEATURE WRITER (services/feature_writer.py): add `feature_schema_version` as a new column in `_INSERT_FEATURE_SQL` (append to the explicit column list and add the matching `$N` placeholder — renumber if the list is positional). Update `_record_to_insert_params` to include the feature_schema_version value at the matching tuple position (read from the record/event; if the consumed record exposes it use `record.feature_schema_version`, otherwise default to the schemas.py `FEATURE_SCHEMA_VERSION` constant and document the source in the SUMMARY). Update the "31-element tuple" docstring count accordingly.

    SIGNAL LEDGER REPOSITORY (src/persistence/repository/signal_ledger_repository.py): add `feature_schema_version` to the `_INSERT_SQL` column list and a corresponding value placeholder. The value must come from the IntelligenceEvent that generated the signal — TRACE the field: read how the signal entry object is constructed (the `_to_row()` source and where the entry is built from the signal payload) and ensure feature_schema_version flows from the IntelligenceEvent -> signal payload -> ledger entry -> `_to_row()`. If the signal payload does not yet carry feature_schema_version, add it at the point the signal is built from the event (note: the signal-build path may live in signal_processor.py / the producer; if the field is genuinely unavailable at the entry-construction site, default to the `FEATURE_SCHEMA_VERSION` constant and document the trace + the gap in the SUMMARY for a follow-up).

    SIGNAL_WRITER.PY (if the audit confirms it writes signal_ledger): update the Kafka payload consumption or LedgerEntry construction to carry feature_schema_version, sourcing from the payload field if available or defaulting to FEATURE_SCHEMA_VERSION constant.

    Keep ON CONFLICT / batch semantics intact for all writers — only add the one column.
  </action>
  <verify>
    grep -rn "INSERT INTO signal_ledger\|INSERT INTO intelligence_features" services/ src/ && grep -n 'feature_schema_version' services/feature_writer.py && grep -n 'feature_schema_version' src/persistence/repository/signal_ledger_repository.py && .venv/bin/python -c "import ast; ast.parse(open('services/feature_writer.py').read()); ast.parse(open('src/persistence/repository/signal_ledger_repository.py').read()); print('OK')"
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains a "Writer INSERT Inventory" table listing every file that contains INSERT INTO signal_ledger or INSERT INTO intelligence_features with a disposition for each (updating / out-of-scope + reason / follow-up)
    - services/signal_writer.py is explicitly classified in the inventory (either "updated — carries feature_schema_version" or "confirmed no INSERT path")
    - `grep -n '_INSERT_FEATURE_SQL' services/feature_writer.py` plus reading the SQL block shows `feature_schema_version` in the column list with a matching placeholder
    - `_record_to_insert_params` includes the feature_schema_version value at the correct tuple position (read confirms; tuple-length docstring updated)
    - `grep -n '_INSERT_SQL' src/persistence/repository/signal_ledger_repository.py` plus reading the SQL block shows `feature_schema_version` in the column list with a matching value placeholder
    - The SUMMARY documents the value source for each writer (event field vs. constant fallback) and any trace gap found in the signal-build path
    - After deploy + pipeline restart, `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM intelligence_features WHERE feature_schema_version = 2"` increases with each processed bar (NOT NULL)
    - `.venv/bin/pytest tests/unit/ -q -k "feature_writer or signal_ledger or repository"` passes (or remains green)
  </acceptance_criteria>
  <done>All INSERT paths to signal_ledger and intelligence_features are inventoried; ALL identified paths persist feature_schema_version=2; new rows are stamped non-NULL, making the contamination boundary effective.</done>
</task>

</tasks>

<verification>
- Migrations apply cleanly in order 110 -> 111 -> 112 -> 113 against the live DB
- Task 1 dependency audit: signal_ledger_full dependents are documented and restored in migration 112
- `SELECT COUNT(*) FROM intelligence_features WHERE feature_schema_version IS NULL` is high (all pre-fix rows) and will DECREASE monotonically only after pipeline restart begins writing version-2 rows
- Writer inventory complete: all INSERT paths carry feature_schema_version — new rows are non-NULL (Task 4 is what makes the boundary real)
- No SIGNAL_SCHEMA_VERSION change in this plan (that is deferred to Plan 02's final task per D-03)
- No calibration / ranking / lifecycle logic touched
- Break-glass rollback: if feature_schema_version causes issues post-deploy, the writer logic can be reverted by setting FEATURE_SCHEMA_VERSION = None (or removing the field default), which causes new rows to write NULL (same as pre-fix) — the DB column stays (nullable) and the pipeline restart is the rollback trigger. The migration files themselves are NOT rolled back.
</verification>

<success_criteria>
- 4 migration files exist in production/migrations/ (110-113) and apply without error
- feature_schema_version INTEGER (nullable) on both intelligence_features and signal_ledger; exposed on signal_ledger_full
- signal_ledger_full VIEW dependencies audited; all dependent objects restored in migration 112
- All writer INSERT paths identified in preflight inventory; all confirmed-in-scope paths persist feature_schema_version so new rows are non-NULL
- setup_performance reset so no row has sample_size >= 30
- FEATURE_SCHEMA_VERSION=2 constant + IntelligenceEvent field; CHECKPOINT_VERSION=2 with discard-on-mismatch + WARNING
- Writer INSERT Inventory table in SUMMARY
- PIPE-INT-01 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/112-intelligence-pipeline-signal-integrity/112-01-SUMMARY.md`
</output>
