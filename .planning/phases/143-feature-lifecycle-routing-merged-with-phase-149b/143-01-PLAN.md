---
phase: 143-feature-lifecycle-routing-merged-with-phase-149b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/200_hmm_lifecycle_apr_keys.sql
  - production/migrations/201_feature_vectors_hmm_churn.sql
  - services/regime_writer.py
  - tests/unit/test_regime_writer_occupation_gate.py
  - tests/unit/test_regime_writer_churn.py
autonomous: true
requirements: [LIFECYCLE-00]
must_haves:
  truths:
    - "regime_writer skips writing and logs a warning when any HMM state occupation fraction < feature.hmm.min_state_occupation (degenerate model)"
    - "regime_writer skips deterministically (empty write set, warning) on empty/single-bar/non-converged inputs before the occupation gate runs — no divide-by-zero, no partial write"
    - "feature_vectors.hmm_churn is populated with the rolling label-change rate over feature.hmm.churn_window bars"
    - "The regime-shift guard downstream (Plan 03 / LIFECYCLE-04) reads trustworthy regime labels because degenerate models no longer silently enter feature_vectors"
  artifacts:
    - path: "production/migrations/200_hmm_lifecycle_apr_keys.sql"
      provides: "feature.hmm.min_state_occupation and feature.hmm.churn_window APR keys (config_schema + config_state)"
      contains: "feature.hmm.min_state_occupation"
    - path: "production/migrations/201_feature_vectors_hmm_churn.sql"
      provides: "feature_vectors.hmm_churn column"
      contains: "hmm_churn"
    - path: "services/regime_writer.py"
      provides: "occupation-fraction gate (P2b) + hmm_churn compute (P2c)"
      contains: "min_state_occupation"
  key_links:
    - from: "services/regime_writer.py"
      to: "feature_vectors.hmm_churn"
      via: "_write_regime_results bulk update"
      pattern: "hmm_churn"
    - from: "services/regime_writer.py"
      to: "config_state feature.hmm.min_state_occupation"
      via: "cfg.get_sync"
      pattern: "min_state_occupation"
---

<objective>
Close the remaining open scope of LIFECYCLE-00 (HMM regime label validation): P2b degenerate-model
occupation-fraction gate, P2c hmm_churn rolling-instability feature, and the APR keys both need. This
ships FIRST in the phase because the regime-shift guard (LIFECYCLE-04, Plan 03) is only as trustworthy
as the regime labels it reads — a degenerate HMM (one state absorbing >90% of observations) or a
high-churn boundary-oscillating symbol must not be silently misread as a real regime and then misread
again as mass feature decay.

Purpose: Guarantee regime labels entering feature_vectors are non-degenerate and carry an instability
signal, so LIFECYCLE-04's ">=60% of cells failing = regime shift" classification is grounded in real
regime structure, not HMM artifacts.

Output: 2 migrations, occupation gate + churn feature in regime_writer.py, 2 unit test files.

Scope guard: P0/P1a/P2a are already shipped per ROADMAP; P1b is explicitly out of scope. Do NOT
recalibrate P3 thresholds here — P3 empirical calibration is a manual-only verification (see
143-VALIDATION.md "Manual-Only Verifications"): the APR defaults ship at the plan-doc values and remain
in force until the later calibration pass lands; they are recalibrated against a live corpus run later,
not in this plan.

Review note (Codex 143-REVIEWS.md, LOW/MEDIUM on this plan): edge-case skip behavior for
empty/single-bar/non-converged HMM fits is now explicit in Task 2; a smallest-valid-sequence regression
test is added in Task 3. The update_rows tuple-shape change is self-contained — its only consumer is
regime_writer's own _write_regime_results / _bulk_update_by_key (grep-confirmed at execution); no
external module reads that tuple, so the contract break is bounded to this file and covered by the churn
test. These are low-cost hardening additions and do NOT widen P2b/P2c scope.

Note for executor: a grep during planning found no n_restarts multi-restart loop in regime_writer.py
(only a single convergence retry). ROADMAP scopes LIFECYCLE-00 remaining work to P2b/P2c/P3 and marks
P2a "shipped" — do NOT expand this plan to add a restart loop. If P2a's absence is confirmed at
execution, capture it as a new todo in .planning/todos/pending/, do not widen this plan's scope.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-feature-lifecycle-routing-merged-with-phase-149b/143-RESEARCH.md
@.planning/phases/143-feature-lifecycle-routing-merged-with-phase-149b/143-VALIDATION.md
@docs/plans/2026-06-28-hmm-regime-audit-optimization.md
@services/regime_writer.py
@production/migrations/161_alpha_ic_apr_keys.sql

<interfaces>
<!-- Existing regime_writer.py contracts the executor extends (planning grep) -->
- `_compute_symbol_tf(...) -> (update_rows: list[tuple], converged: bool, heldout_ll: float)` — fits GaussianHMM, runs alpha-pass, builds `update_rows` where each tuple is
  `(label, p_up, p_ranging, p_down, prob_val, entropy_val, duration, symbol, tf, ts)` (regime_writer.py ~lines 505-537). Occupation gate belongs after `model.fit(...)` / smoothed_states are known; churn value is a new element appended per-row.
- `_write_regime_results(conn, symbol, tf, update_rows, converged, heldout_ll, tracer) -> int` — serial main-process write via `_bulk_update_by_key` to feature_vectors (regime_writer.py ~line 541). New hmm_churn column joins the bulk-update key set here. This function is the ONLY consumer of the update_rows tuple shape — confirm via grep at execution.
- Config load in `main()` uses `cfg.get_sync("feature.hmm.<key>", default)` (regime_writer.py ~lines 824-834). New keys read the same way; pass values down into `_compute_symbol_tf`'s existing keyword args (mirror how `min_hold_bars`, `heldout_fraction` are threaded through).
- APR seed pattern: config_schema row = `(config_key, value_type, default_value, min_value, max_value, description)`; config_state row = `(config_key, config_value, version)` (migration 161 sections 1-2). Description MUST carry a provenance tag: use `[initial_estimate]` for both new keys.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: APR keys + hmm_churn column migrations</name>
  <files>production/migrations/200_hmm_lifecycle_apr_keys.sql, production/migrations/201_feature_vectors_hmm_churn.sql</files>
  <read_first>
    - production/migrations/161_alpha_ic_apr_keys.sql (config_schema + config_state INSERT structure, ON CONFLICT DO NOTHING, min/max bounds)
    - docs/plans/2026-06-28-hmm-regime-audit-optimization.md (P2b default 0.05, P2c churn_window default 10)
    - ls production/migrations/ | sort | tail -3 (re-confirm 199 is the highest applied number before creating 200/201 — another phase may have landed a migration since research)
  </read_first>
  <action>
    Create migration 200_hmm_lifecycle_apr_keys.sql inserting two APR keys into BOTH config_schema
    and config_state, mirroring migration 161's two-section structure with ON CONFLICT (config_key)
    DO NOTHING:
      - feature.hmm.min_state_occupation, value_type 'float', default '0.05', min 0.005, max 0.30,
        description tagged [initial_estimate]: minimum alpha-pass occupation fraction any HMM state
        must hold; below this the model is degenerate and its labels are skipped (P2b). Not an ML
        learning target.
      - feature.hmm.churn_window, value_type 'int', default '10', min 3, max 100, description tagged
        [initial_estimate]: rolling window (bars) over which hmm_churn is computed as the fraction of
        prior-window bars where the regime label changed (P2c). Not an ML learning target.
    Create migration 201_feature_vectors_hmm_churn.sql adding column hmm_churn (double precision,
    nullable — legacy rows stay NULL until re-labeled) to feature_vectors via
    ALTER TABLE feature_vectors ADD COLUMN IF NOT EXISTS hmm_churn double precision, with a
    COMMENT ON COLUMN describing it as the rolling churn_window-bar regime-label-change rate. Do NOT
    backfill; the next regime_writer run populates it.
    If 199 is not the highest number at execution time, renumber both files to the next two free
    sequential numbers and update all references in this plan's later tasks accordingly.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/200_hmm_lifecycle_apr_keys.sql && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/201_feature_vectors_hmm_churn.sql && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM config_state WHERE config_key IN ('feature.hmm.min_state_occupation','feature.hmm.churn_window')" | grep -qx 2</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT config_value FROM config_state WHERE config_key='feature.hmm.min_state_occupation'` returns `0.05`
    - `SELECT config_value FROM config_state WHERE config_key='feature.hmm.churn_window'` returns `10`
    - `\d feature_vectors` shows an `hmm_churn` column of type double precision, nullable
    - Both config_schema rows carry a `[initial_estimate]` provenance tag and min/max bounds
    - Migrations are idempotent (re-running is a no-op via IF NOT EXISTS / ON CONFLICT DO NOTHING)
  </acceptance_criteria>
  <done>Both migrations applied cleanly; two APR keys live at documented defaults; feature_vectors.hmm_churn exists as nullable double precision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Degenerate-model occupation-fraction gate (P2b)</name>
  <files>services/regime_writer.py, tests/unit/test_regime_writer_occupation_gate.py</files>
  <read_first>
    - services/regime_writer.py lines 352-540 (_compute_symbol_tf: fit, alpha-pass, smoothed_states, update_rows build) and lines 757-845 (main: cfg.get_sync load block)
    - .planning/phases/143-feature-lifecycle-routing-merged-with-phase-149b/143-RESEARCH.md (Pattern 1 APR-binding, structlog dotted-event convention)
    - docs/plans/2026-06-28-hmm-regime-audit-optimization.md P2b section
  </read_first>
  <behavior>
    - Test: a model whose smoothed_states assign one state >= (1 - min_state_occupation) of bars (e.g. 96% to one state, min_state_occupation=0.05 → the other states are below floor) → _compute_symbol_tf returns an empty update_rows list (skip write) and the degenerate condition is signalled (converged flag / a returned marker the caller can log).
    - Test: a healthy model where every state's occupation fraction >= min_state_occupation → update_rows is non-empty (write proceeds).
    - Test: occupation fractions are computed from smoothed_states (label assignments actually written), not from the stationary distribution.
    - Test (short/degenerate-input edge cases, Codex MEDIUM): empty series, single-bar series, and a non-converged fit each return a deterministic empty update_rows list (no DB write, warning emitted) — no divide-by-zero, no IndexError. The caller receives the same empty-write marker as the degenerate-occupation path so its handling is uniform.
  </behavior>
  <action>
    In _compute_symbol_tf, after smoothed_states is finalized, compute per-state occupation fraction =
    count(smoothed_states == k) / len(smoothed_states) for each of the n_components states. Guard the
    edge cases FIRST: if the series is empty, single-bar (len < n_components or len < 2), or the fit did
    not converge, return an empty update_rows list with the degenerate marker and a structlog warning
    BEFORE dividing by len(smoothed_states). If ANY state's fraction < min_state_occupation, log a
    structlog warning "regime_writer.degenerate_model_skipped" with symbol, tf, min_occupation observed,
    and the offending state, then return an empty update_rows list so _write_regime_results writes nothing
    for that (symbol, tf) cell. Thread min_state_occupation into _compute_symbol_tf as a new keyword
    argument loaded in main() via cfg.get_sync("feature.hmm.min_state_occupation", 0.05), matching how
    min_hold_bars/heldout_fraction are already threaded. Do NOT read config_state inside the worker;
    bind the value in main() and pass it down (ProcessPoolExecutor workers are compute-only). Keep the
    `except Exception as error:` convention if you add any guarded block.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_regime_writer_occupation_gate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_regime_writer_occupation_gate.py -x` passes with >=4 assertions covering degenerate-skip, healthy-write, smoothed_states-source, and short/non-converged edge-case skip
    - `grep -n "min_state_occupation" services/regime_writer.py` shows it read via cfg.get_sync in main() and consumed as a keyword arg in _compute_symbol_tf, with NO hard-coded 0.05 literal in the gate logic
    - A warning event "regime_writer.degenerate_model_skipped" is emitted on the skip path
    - No `config_state` read occurs inside any ProcessPoolExecutor worker function
    - Empty/single-bar/non-converged inputs return the deterministic empty-write marker without raising
  </acceptance_criteria>
  <done>Degenerate HMM fits and short/non-converged inputs are detected and skipped with a loud warning, never written; healthy fits write unchanged; threshold is APR-backed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: hmm_churn rolling-instability feature (P2c)</name>
  <files>services/regime_writer.py, tests/unit/test_regime_writer_churn.py</files>
  <read_first>
    - services/regime_writer.py lines 505-620 (update_rows build loop, _write_regime_results, _bulk_update_by_key key set)
    - production/migrations/201_feature_vectors_hmm_churn.sql (column name/type this task writes to)
  </read_first>
  <behavior>
    - Test: given a smoothed label sequence with a known number of changes in the prior churn_window bars, hmm_churn at each bar == (label changes in prior churn_window window) / churn_window, matching a hand-computed expected array.
    - Test: the first (churn_window - 1) bars use the available prefix (partial window), not NaN garbage — define and assert the exact edge behavior (e.g. divide by bars-available).
    - Test: a perfectly stable regime run yields hmm_churn == 0.0 for all bars; a strictly alternating sequence yields the maximum churn value.
    - Test (smallest valid sequence, Codex suggestion): the smallest sequence that clears the occupation gate (length == n_components, one bar per state) produces a well-defined hmm_churn array (no NaN, no IndexError) — the partial-window prefix rule is exercised at minimum length.
  </behavior>
  <action>
    In _compute_symbol_tf, compute hmm_churn per bar as the rolling fraction of the prior
    feature.hmm.churn_window bars whose smoothed label differs from the immediately preceding bar's
    label (label-change rate). Append the churn value as a new trailing element on each update_rows
    tuple BEFORE the symbol/tf/ts positional trio, or extend the tuple consistently — then update
    _write_regime_results / _bulk_update_by_key so hmm_churn is included in the UPDATE column set
    targeting feature_vectors.hmm_churn. Thread churn_window into _compute_symbol_tf as a keyword arg
    loaded in main() via cfg.get_sync("feature.hmm.churn_window", 10) (bound in main, passed down —
    not read in the worker). Keep the existing tuple ordering contract internally consistent across
    build, write, and any tests that assert on update_rows shape (the only consumer is
    _write_regime_results — no external module reads it).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_regime_writer_churn.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_regime_writer_churn.py -x` passes with the hand-computed churn array, edge-window behavior, stable/alternating boundary cases, and the smallest-valid-sequence case asserted
    - `grep -n "churn_window\|hmm_churn" services/regime_writer.py` shows churn_window read via cfg.get_sync in main() and hmm_churn present in the feature_vectors UPDATE column set
    - No hard-coded window literal (e.g. bare 10) in the churn computation
    - update_rows tuple shape is consistent between the build loop and _write_regime_results
  </acceptance_criteria>
  <done>hmm_churn is computed as a rolling label-change rate and persisted to feature_vectors.hmm_churn; window is APR-backed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config_state → regime_writer | APR values loaded at run start; out-of-range values could weaken the gate |
| HMM fit output → feature_vectors | degenerate/garbage labels crossing into the corpus is the exact failure this plan blocks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Tampering | feature.hmm.min_state_occupation / churn_window APR values | mitigate | config_schema min/max bounds (0.005–0.30, 3–100) reject nonsensical values at write |
| T-143-02 | Information Disclosure (silent bad data) | degenerate HMM labels entering feature_vectors | mitigate | occupation gate + edge-case guard skip write + emit "regime_writer.degenerate_model_skipped" warning (loud crash over silent wrong answer) |
| T-143-SC | Tampering | supply chain (pip installs) | accept | zero new packages installed this plan; nothing to slopcheck |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_regime_writer_occupation_gate.py tests/unit/test_regime_writer_churn.py -q` green
- `.venv/bin/pytest tests/unit/ -q` green (no regressions from tuple-shape change)
- Both migrations applied; two APR keys live; feature_vectors.hmm_churn present
</verification>

<success_criteria>
- Degenerate HMM models (occupation fraction < min_state_occupation) and short/non-converged fits are skipped with a warning, never written
- feature_vectors.hmm_churn populated on the next regime_writer run
- All new thresholds APR-backed; no hard-coded numeric literals introduced
- P3 calibration correctly deferred to manual verification, not attempted here
</success_criteria>

<output>
Create `.planning/phases/143-feature-lifecycle-routing-merged-with-phase-149b/143-01-SUMMARY.md` when done
</output>
