---
phase: 136-post-reboot-system-repair
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - services/feature_writer.py
  - production/scripts/run_historical_pipeline.py
autonomous: true

must_haves:
  truths:
    - "feature_writer crashes at startup with a RuntimeError naming the missing column and migration when a Phase-130 CTF column is absent"
    - "feature_writer starts normally when all four CTF columns exist"
    - "Live feature writes no longer duplicate CTF keys inside cross_timeframe_context JSONB"
    - "Replay writes (run_historical_pipeline) no longer duplicate CTF keys inside cross_timeframe_context JSONB"
  artifacts:
    - path: "services/feature_writer.py"
      provides: "_verify_schema() pre-flight + CTF-key exclusion when building cross_timeframe_context"
      contains: "_verify_schema"
    - path: "production/scripts/run_historical_pipeline.py"
      provides: "CTF-key exclusion when building cross_timeframe_context"
      contains: "ctf_score"
  key_links:
    - from: "services/feature_writer.py _setup()"
      to: "_verify_schema() before Kafka start"
      via: "await self._verify_schema() between _connect_database() and _setup_kafka_clients()"
      pattern: "_verify_schema"
    - from: "services/feature_writer.py cross_timeframe_context build (~line 217)"
      to: "intelligence_features.cross_timeframe_context JSONB"
      via: "event.i6.model_dump(exclude={ctf_score, ctf_trend_alignment, ctf_structure_alignment, ctf_regime_agreement})"
      pattern: "exclude=\\{"
---

<objective>
W2: Harden feature_writer with (2a) a startup pre-flight schema check scoped to the four Phase-130 CTF columns, and (2b) a JSONB write-path fix that stops both the live writer and the replay script from duplicating CTF keys inside `cross_timeframe_context`. The four CTF values already live in dedicated top-level columns; duplicating them in the JSONB blob is what Migration 130 Statement 3 exists to clean. 2b must be deployed before W1 replay so replayed rows are written clean.

Purpose: 2a converts a silent multi-hour data loss (migration not applied → buffer overflow → data dropped) into a loud startup crash. 2b makes Migration 130 Statement 3 durable (otherwise live writes immediately reintroduce the keys).
Output: _verify_schema() pre-flight in feature_writer; CTF-key exclusion at both JSONB build sites.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-06-18-post-reboot-repair-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 2a — Add _verify_schema() pre-flight called in _setup before Kafka start</name>
  <read_first>
    - services/feature_writer.py (read lines 380-395 for `_setup()` order: `_connect_database()` then `_setup_kafka_clients()`; read 440-490 to confirm `self.db_manager` is set in `_connect_database()` and `await self._kafka_consumer.start()` happens inside `_setup_kafka_clients()`)
    - src/core/database_manager.py (read lines 105-125 — confirm `async def fetch(self, query, *args)` signature)
    - docs/plans/2026-06-18-post-reboot-repair-design.md (section "W2 2a" — _REQUIRED_COLUMNS set, table_schema='public' filter is mandatory, RuntimeError message format)
  </read_first>
  <files>services/feature_writer.py</files>
  <action>
    Add a module-level (or class-level) frozenset `_REQUIRED_COLUMNS = frozenset({"ctf_score", "ctf_trend_alignment", "ctf_structure_alignment", "ctf_regime_agreement"})`.
    Add `async def _verify_schema(self) -> None:` that runs `SELECT column_name FROM information_schema.columns WHERE table_name = 'intelligence_features' AND table_schema = 'public'` via `await self.db_manager.fetch(...)`, builds `existing = {row["column_name"] for row in rows}`, computes `missing = _REQUIRED_COLUMNS - existing`, and if `missing` raises `RuntimeError(f"intelligence_features schema mismatch — missing columns: {sorted(missing)}. Run migration 130 (production/migrations/130_promote_ctf_columns.sql).")`. The `table_schema = 'public'` filter is mandatory (without it, columns from same-named tables in other schemas cause false negatives). Use a single dash, not an em dash, in the message.
    Call `await self._verify_schema()` inside `_setup()` AFTER `await self._connect_database()` and BEFORE `await self._setup_kafka_clients()` (i.e., before any Kafka subscription/start). Guard the call to ensure `self.db_manager` is non-None at that point (it is set by `_connect_database()`).
  </action>
  <verify>grep -n "_verify_schema\|_REQUIRED_COLUMNS\|table_schema = 'public'" services/feature_writer.py && .venv/bin/python -c "import ast; ast.parse(open('services/feature_writer.py').read()); print('parses')"</verify>
  <acceptance_criteria>
    - feature_writer.py defines `_REQUIRED_COLUMNS` as a frozenset of exactly the four CTF column names
    - `async def _verify_schema` queries `information_schema.columns` with `table_schema = 'public'` in the WHERE clause
    - `_verify_schema` raises `RuntimeError` whose message names `sorted(missing)` and `migration 130`
    - `_setup()` calls `await self._verify_schema()` after `_connect_database()` and before `_setup_kafka_clients()`
    - Behavior (dev verification): temporarily dropping one CTF column causes feature_writer startup to raise RuntimeError naming that column; restoring it lets startup proceed
  </acceptance_criteria>
  <done>feature_writer fails loudly at startup when a Phase-130 CTF column is missing, and starts normally when all four are present.</done>
</task>

<task type="auto">
  <name>Task 2: 2b — Exclude CTF keys from cross_timeframe_context in both write paths</name>
  <read_first>
    - services/feature_writer.py (read lines 185-220 — the param tuple where `event.i6.model_dump(exclude_none=True)` populates `$14 cross_timeframe_context` at ~line 217, and note ctf_* are already extracted to top-level columns at lines 191-195)
    - production/scripts/run_historical_pipeline.py (read lines 740-755 — the `event.i6.model_dump(exclude_none=True)` populating cross_timeframe_context at ~line 749)
    - docs/plans/2026-06-18-post-reboot-repair-design.md (section "W2 2b" — exact exclude set)
  </read_first>
  <files>services/feature_writer.py, production/scripts/run_historical_pipeline.py</files>
  <action>
    In services/feature_writer.py at the cross_timeframe_context build (~line 217), change `event.i6.model_dump(exclude_none=True)` to `event.i6.model_dump(exclude_none=True, exclude={"ctf_score", "ctf_trend_alignment", "ctf_structure_alignment", "ctf_regime_agreement"})`. This affects ONLY the `$14 cross_timeframe_context` argument — the four CTF values are still written to their dedicated top-level columns ($ for ctf_score etc.), so no information is lost; they are simply no longer duplicated inside the JSONB blob.
    In production/scripts/run_historical_pipeline.py at ~line 749, apply the identical change: `event.i6.model_dump(exclude_none=True)` → `event.i6.model_dump(exclude_none=True, exclude={"ctf_score", "ctf_trend_alignment", "ctf_structure_alignment", "ctf_regime_agreement"})`, keeping the surrounding `_sanitize_for_json(...)` / `json.dumps(...)` wrapping intact.
    Do not alter any other `model_dump` call (i1/i3/i4/i5/smc/i2) in either file.
  </action>
  <verify>grep -n "exclude={\"ctf_score\"\|exclude={'ctf_score'" services/feature_writer.py production/scripts/run_historical_pipeline.py && .venv/bin/python -c "import ast; ast.parse(open('services/feature_writer.py').read()); ast.parse(open('production/scripts/run_historical_pipeline.py').read()); print('parse ok')"</verify>
  <acceptance_criteria>
    - The cross_timeframe_context build in feature_writer.py (~line 217) calls model_dump with an `exclude=` set containing all four CTF keys
    - The cross_timeframe_context build in run_historical_pipeline.py (~line 749) calls model_dump with the same `exclude=` set
    - No other model_dump call (i1/i3/i4/i5/smc/i2) in either file gained an exclude= for CTF keys
    - Both files parse via ast
    - Behavior (post-deploy): spot-check of 10 recent intelligence_features rows shows `cross_timeframe_context` does NOT contain a `ctf_score` key
  </acceptance_criteria>
  <done>Both live and replay write paths emit cross_timeframe_context JSONB without the four CTF keys; top-level CTF columns remain populated.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -c "import ast; ast.parse(open('services/feature_writer.py').read()); ast.parse(open('production/scripts/run_historical_pipeline.py').read())"` exits 0
- `.venv/bin/ruff check services/feature_writer.py production/scripts/run_historical_pipeline.py` is clean
- `.venv/bin/pytest tests/unit/ -q -k "feature_writer or historical"` exits 0 (or "no tests ran" — record which)
- Note: 2b MUST be deployed and running before Plan 05 (W1 replay) executes; Plan 06 (Migration 130 Statement 3) depends on 2b being live.
</verification>

<success_criteria>
- _verify_schema pre-flight runs before Kafka start; loud RuntimeError on missing CTF column
- Both JSONB write paths exclude the four CTF keys from cross_timeframe_context
- Top-level CTF columns unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/136-post-reboot-system-repair/136-04-SUMMARY.md`
</output>
