---
phase: 136-post-reboot-system-repair
plan: 05
type: execute
wave: 2
depends_on: ["04"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Every signal_events row in the gap window has a matching intelligence_features row (zero orphans)"
    - "Replayed intelligence_features rows carry non-null ctf_score where I6 was expected to fire"
    - "No duplicate intelligence_features rows exist for the gap window"
  artifacts: []
  key_links:
    - from: "production/scripts/run_historical_pipeline.py --replay-only --overwrite-features"
      to: "intelligence_features (gap window 2026-06-18 11:15 → 19:10 UTC)"
      via: "I1-I7 recompute from market_data_ohlcv, ON CONFLICT (ts,symbol,tf) DO UPDATE"
      pattern: "DO UPDATE"
---

<objective>
W1 (operational): Recover the 1,343 orphaned signal_events rows by replaying I1-I7 over the outage gap window (2026-06-18 11:15:00 UTC through 19:10:00 UTC) and writing recomputed features directly to intelligence_features. The bar data is intact in market_data_ohlcv; replay is the only recovery path. This is a deterministic operational step run AFTER Plan 04 (W2b JSONB fix) is deployed and running, so recovered rows are written with the clean schema.

Purpose: Restore permanent ML training context for 1,343 signals that currently point at missing intelligence_features rows.
Output: Zero orphans in the gap-window JOIN query; non-null ctf_score on replayed rows; no duplicate feature rows.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-06-18-post-reboot-repair-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm W2b is deployed, capture baseline orphan count, confirm DO UPDATE path</name>
  <read_first>
    - docs/plans/2026-06-18-post-reboot-repair-design.md (section "W1" — prerequisite, ON CONFLICT behavior, command, fallback notes)
    - production/scripts/run_historical_pipeline.py (read lines 630-650 — `_feature_insert_sql(overwrite)` uses DO UPDATE when overwrite True; read argparse around lines 2150-2175 to confirm `--replay-only`, `--client-id` default 40, `--timeframes`, and the `--overwrite-features` / `args.overwrite_features` flag; note there is NO `--start-ts`/`--end-ts` flag → use `--days 1` fallback)
  </read_first>
  <files></files>
  <action>
    Confirm Plan 04 (W2b) is live: the running feature_writer / run_historical_pipeline build excludes CTF keys from cross_timeframe_context. If W2b is not yet deployed, STOP — recovered rows would themselves need Statement 3 cleanup.
    Capture the baseline orphan count by running the design's JOIN query (signal_events LEFT JOIN intelligence_features on feature_ts/symbol/tf, gap window `2026-06-18 11:15:00+00` to `19:10:00+00`, feature_ts NOT NULL, inf.ts IS NULL) via `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "..."`. Record the count (expected ~1,343).
    Confirm replay will use DO UPDATE not DO NOTHING for intelligence_features: the `--overwrite-features` flag drives `overwrite_features=True` which selects the `DO UPDATE SET ... = EXCLUDED.*` branch in `_feature_insert_sql`. The replay command MUST include `--overwrite-features` so partial pre-failure rows are replaced rather than skipped.
    Confirm the gap window falls within the `--days 1` lookback (today is 2026-06-18) since the script has no `--start-ts`/`--end-ts`.
  </action>
  <verify>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -t -c "SELECT COUNT(*) FROM signal_events se LEFT JOIN intelligence_features inf ON se.feature_ts = inf.ts AND se.symbol = inf.symbol AND se.tf = inf.tf WHERE se.ts BETWEEN '2026-06-18 11:15:00+00' AND '2026-06-18 19:10:00+00' AND se.feature_ts IS NOT NULL AND inf.ts IS NULL;"</verify>
  <acceptance_criteria>
    - Baseline orphan count recorded (a positive integer, expected near 1,343)
    - W2b confirmed deployed (cross_timeframe_context in recent rows lacks ctf_score key) — if not, plan halts here
    - `run_historical_pipeline.py` confirmed to expose `--overwrite-features` and `--replay-only`; default `--client-id` is 40
    - Confirmed no `--start-ts`/`--end-ts` flag exists → `--days 1` is the window mechanism
  </acceptance_criteria>
  <done>Prerequisites verified; baseline orphan count captured; DO UPDATE path confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Run the gap-window replay (replay-only, overwrite-features) without restarting the live pipeline</name>
  <read_first>
    - docs/plans/2026-06-18-post-reboot-repair-design.md (section "W1" — Command, Notes: do NOT restart intelligence_pipeline; feature_writer may remain running; client-id 40; deterministic chronological replay)
    - production/scripts/run_historical_pipeline.py (read lines 2150-2180 + 2660-2690 to confirm flag plumbing for --replay-only, --timeframes, --overwrite-features)
  </read_first>
  <files></files>
  <action>
    Run the replay from the scripts directory using the project venv. Use `--replay-only` (skips IBKR fetch — bars are already in market_data_ohlcv), `--client-id 40` (provider uses 35; 56 default exceeds _MAX_CLIENT_ID=50), `--timeframes 1m,5m,15m,1h`, `--overwrite-features` (forces ON CONFLICT DO UPDATE), and `--days 1` (the no-date-flag fallback covering today's gap window). Concretely: `cd production/scripts && ../../.venv/bin/python run_historical_pipeline.py --replay-only --client-id 40 --timeframes 1m,5m,15m,1h --overwrite-features --days 1`.
    Do NOT restart `indicagent-intelligence-pipeline` during replay (would conflict Kafka offsets). The live feature_writer may remain running — replay writes directly to the DB (not via Kafka) for this historical timerange, so there is no dual-write conflict (live pipeline only writes current bars). Capture stdout/stderr to a log for the summary. Replay is deterministic given the same bar history (chronological, no look-ahead).
    After completion, spot-check that rows at `2026-06-18 11:15:00 UTC` are present (confirms `--days 1` covered the start of the gap window).
  </action>
  <verify>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -t -c "SELECT COUNT(*) FROM intelligence_features WHERE ts BETWEEN '2026-06-18 11:15:00+00' AND '2026-06-18 11:20:00+00';"</verify>
  <acceptance_criteria>
    - Replay command completes with a non-error exit code; stdout/stderr captured
    - intelligence_features has rows at the start of the gap window (2026-06-18 11:15-11:20 UTC count > 0)
    - intelligence_pipeline service was NOT restarted during the run
  </acceptance_criteria>
  <done>I1-I7 features recomputed and written for the gap window via DO UPDATE; gap-window start rows present.</done>
</task>

<task type="auto">
  <name>Task 3: Verify recovery — zero orphans, non-null ctf_score, no duplicates</name>
  <read_first>
    - docs/plans/2026-06-18-post-reboot-repair-design.md (section "W1" Verification + "Verification Checklist" W1 — orphan query must return 0, ctf_score spot-check, duplicate query must return 0 rows)
  </read_first>
  <files></files>
  <action>
    Re-run the orphan JOIN query from Task 1 — it MUST now return 0. If it returns >0, the replay did not cover all orphaned rows (check timeframe coverage and whether `--days 1` reached the earliest orphan ts); re-run replay before proceeding.
    Spot-check 5 replayed rows in the gap window where I6 was expected to fire and confirm `ctf_score` is non-null (top-level column).
    Run the duplicate-detection query: `SELECT ts, symbol, tf, COUNT(*) FROM intelligence_features WHERE ts BETWEEN '2026-06-18 11:15:00+00' AND '2026-06-18 19:10:00+00' GROUP BY ts, symbol, tf HAVING COUNT(*) > 1` — it must return 0 rows (DO UPDATE on the (ts,symbol,tf) PK guarantees no duplicates).
    Record all three results in the summary as recovery evidence.
  </action>
  <verify>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -t -c "SELECT COUNT(*) FROM signal_events se LEFT JOIN intelligence_features inf ON se.feature_ts = inf.ts AND se.symbol = inf.symbol AND se.tf = inf.tf WHERE se.ts BETWEEN '2026-06-18 11:15:00+00' AND '2026-06-18 19:10:00+00' AND se.feature_ts IS NOT NULL AND inf.ts IS NULL;"</verify>
  <acceptance_criteria>
    - Orphan JOIN query returns 0
    - At least 5 gap-window rows confirmed with non-null `ctf_score`
    - Duplicate-detection query returns 0 rows
  </acceptance_criteria>
  <done>Zero orphans, non-null ctf_score on replayed rows, no duplicate feature rows — recovery confirmed.</done>
</task>

</tasks>

<verification>
- Orphan JOIN query returns 0
- Duplicate query returns 0 rows
- 5+ replayed rows have non-null ctf_score
- intelligence_pipeline was not restarted during replay
</verification>

<success_criteria>
- 1,343 orphaned signal_events now have matching intelligence_features context
- Recovery verified by the three queries above
- Plan 06 (Migration 130 Statement 3) may now run
</success_criteria>

<output>
After completion, create `.planning/phases/136-post-reboot-system-repair/136-05-SUMMARY.md`
</output>
