---
phase: 136-post-reboot-system-repair
plan: 06
type: execute
wave: 3
depends_on: ["04", "05"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "No intelligence_features row contains a ctf_score key inside cross_timeframe_context JSONB"
    - "The cleanup is idempotent and safe to re-run"
    - "The CTF values remain present in their dedicated top-level columns (not lost by the cleanup)"
  artifacts: []
  key_links:
    - from: "Migration 130 Statement 3 UPDATE"
      to: "intelligence_features.cross_timeframe_context"
      via: "JSONB key-subtraction of the four CTF keys WHERE cross_timeframe_context ? 'ctf_score'"
      pattern: "ctf_score"
---

<objective>
Migration 130 Statement 3 (operational cleanup): Strip the four CTF keys from `cross_timeframe_context` in all existing intelligence_features rows. Prerequisite: Plan 04 (W2b) deployed so live/replay writers no longer reintroduce the keys, and Plan 05 (W1 replay) complete so the gap-window rows are already written clean. Without W2b live, this cleanup would be undone by the next feature write.

Purpose: Enforce single-source-of-truth for the CTF values (top-level columns only), removing the duplicated JSONB copies.
Output: Zero rows with ctf_score inside cross_timeframe_context; top-level CTF columns intact.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-06-18-post-reboot-repair-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm W2b live and W1 replay complete; capture pre-cleanup count</name>
  <read_first>
    - docs/plans/2026-06-18-post-reboot-repair-design.md (section "Migration 130 Statement 3" — prerequisite ordering, transaction, max_tuples_decompressed setting, idempotent WHERE predicate, spot checks)
    - production/migrations/130_promote_ctf_columns.sql (read to confirm Statements 1-2 already applied — the four CTF top-level columns exist — and locate where Statement 3 belongs)
  </read_first>
  <files></files>
  <action>
    Confirm Plan 05 (W1 replay) verification passed (orphan JOIN query returned 0) and Plan 04 (W2b) is the deployed/running write path. If either is not satisfied, STOP — running cleanup now risks immediate re-pollution (W2b) or leaves orphans unrecovered (W1).
    Confirm the four top-level CTF columns (ctf_score, ctf_trend_alignment, ctf_structure_alignment, ctf_regime_agreement) exist on intelligence_features via information_schema (Migration 130 Statements 1-2). The cleanup removes only the JSONB duplicates, never the top-level columns.
    Capture the pre-cleanup count of rows still carrying the key: `SELECT COUNT(*) FROM intelligence_features WHERE cross_timeframe_context ? 'ctf_score'`. Record it.
  </action>
  <verify>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -t -c "SELECT COUNT(*) FROM intelligence_features WHERE cross_timeframe_context ? 'ctf_score';"</verify>
  <acceptance_criteria>
    - W1 (Plan 05) orphan query confirmed 0 and W2b (Plan 04) confirmed deployed — recorded in summary
    - Four top-level CTF columns confirmed present on intelligence_features
    - Pre-cleanup `cross_timeframe_context ? 'ctf_score'` count captured (a non-negative integer)
  </acceptance_criteria>
  <done>Prerequisites verified; pre-cleanup count captured.</done>
</task>

<task type="auto">
  <name>Task 2: Run Statement 3 inside a transaction with decompression DML setting</name>
  <read_first>
    - docs/plans/2026-06-18-post-reboot-repair-design.md (section "Migration 130 Statement 3" — exact SQL, BEGIN/COMMIT, timescaledb.max_tuples_decompressed_per_dml_transaction = 0, idempotent predicate)
  </read_first>
  <files></files>
  <action>
    Execute the cleanup as a single transaction via `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent`. Before the UPDATE, set `SET timescaledb.max_tuples_decompressed_per_dml_transaction = 0;` so compressed chunks can be modified without the per-DML decompression cap aborting the statement.
    The UPDATE: `UPDATE intelligence_features SET cross_timeframe_context = cross_timeframe_context - ARRAY['ctf_score','ctf_trend_alignment','ctf_structure_alignment','ctf_regime_agreement'] WHERE cross_timeframe_context ? 'ctf_score';` wrapped in BEGIN; ... COMMIT;. The `WHERE cross_timeframe_context ? 'ctf_score'` predicate makes the statement idempotent and safe to re-run if interrupted.
    Run the whole thing as one psql invocation so the SET applies within the transaction. Capture the reported UPDATE row count.
  </action>
  <verify>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "BEGIN; SET LOCAL timescaledb.max_tuples_decompressed_per_dml_transaction = 0; UPDATE intelligence_features SET cross_timeframe_context = cross_timeframe_context - ARRAY['ctf_score','ctf_trend_alignment','ctf_structure_alignment','ctf_regime_agreement'] WHERE cross_timeframe_context ? 'ctf_score'; COMMIT;"</verify>
  <acceptance_criteria>
    - The transaction commits without error (psql reports COMMIT)
    - The UPDATE row count is reported and recorded (should match or be <= the pre-cleanup count from Task 1)
    - timescaledb.max_tuples_decompressed_per_dml_transaction was set so compressed chunks did not abort the DML
  </acceptance_criteria>
  <done>CTF keys stripped from cross_timeframe_context across all existing rows in one committed transaction.</done>
</task>

<task type="auto">
  <name>Task 3: Post-cleanup spot checks — zero keyed rows, top-level columns intact</name>
  <read_first>
    - docs/plans/2026-06-18-post-reboot-repair-design.md (section "Migration 130 Statement 3" Post-Statement-3 spot check + "Verification Checklist" W2b)
  </read_first>
  <files></files>
  <action>
    Run the post-cleanup checks: (1) `SELECT COUNT(*) FROM intelligence_features WHERE cross_timeframe_context ? 'ctf_score'` MUST return 0. (2) Sample 10 recent rows `SELECT ts, symbol, tf, cross_timeframe_context FROM intelligence_features ORDER BY ts DESC LIMIT 10` and confirm none contain a ctf_score key in the JSONB. (3) Confirm the top-level CTF columns are still populated on recent rows (the cleanup must not have touched them) via `SELECT ts, symbol, tf, ctf_score FROM intelligence_features WHERE ctf_score IS NOT NULL ORDER BY ts DESC LIMIT 5`.
    Record all three results as cleanup evidence in the summary. If check (1) returns >0, re-run Task 2 (idempotent) and re-check; if recent rows still gain the key, W2b is not actually deployed — escalate.
  </action>
  <verify>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -t -c "SELECT COUNT(*) FROM intelligence_features WHERE cross_timeframe_context ? 'ctf_score';"</verify>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM intelligence_features WHERE cross_timeframe_context ? 'ctf_score'` returns 0
    - 10 most-recent rows show no ctf_score key inside cross_timeframe_context
    - Recent rows still have non-null top-level ctf_score (column not affected by cleanup)
  </acceptance_criteria>
  <done>No row carries ctf_score in cross_timeframe_context; top-level CTF columns remain populated.</done>
</task>

</tasks>

<verification>
- `SELECT COUNT(*) FROM intelligence_features WHERE cross_timeframe_context ? 'ctf_score'` returns 0
- 10 recent rows: no ctf_score key in cross_timeframe_context JSONB
- Top-level ctf_score column still populated on recent rows
- Re-running Statement 3 changes 0 rows (idempotent)
</verification>

<success_criteria>
- CTF keys fully removed from cross_timeframe_context across all rows
- Single-source-of-truth enforced (top-level columns only)
- Cleanup durable (W2b prevents reintroduction)
</success_criteria>

<output>
After completion, create `.planning/phases/136-post-reboot-system-repair/136-06-SUMMARY.md`
</output>
