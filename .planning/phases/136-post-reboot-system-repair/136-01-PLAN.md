---
phase: 136-post-reboot-system-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/trading/plugin_utils.py
  - src/intelligence/trading/signal_schema.py
  - src/intelligence/pipeline/executor.py
  - services/signal_writer.py
  - tests/unit/intelligence/test_plugin_utils.py
  - tests/unit/intelligence/test_signal_schema.py
  - tests/unit/intelligence/test_emit_signal_validation.py
autonomous: true

must_haves:
  truths:
    - "Stop-correction error messages report a dimensionless ATR ratio, not a raw price distance"
    - "A zero-ATR bar in the stop-correction path raises ValueError, not ZeroDivisionError"
    - "validate_signal returns a value whose truthiness is unchanged for all existing call sites"
    - "executor.schema_violation log events include a reason field naming the failure class"
  artifacts:
    - path: "src/intelligence/trading/plugin_utils.py"
      provides: "Corrected ATR-ratio error strings + zero-ATR epsilon guard"
      contains: "_ATR_EPSILON"
    - path: "src/intelligence/trading/signal_schema.py"
      provides: "ValidationResult NamedTuple + validate_signal returning it"
      contains: "class ValidationResult"
  key_links:
    - from: "src/intelligence/pipeline/executor.py"
      to: "validate_signal ValidationResult.reason"
      via: "result = validate_signal(sig); log reason=result.reason"
      pattern: "reason=result.reason"
    - from: "src/intelligence/trading/signal_schema.py ValidationResult.__bool__"
      to: "all existing `if validate_signal(sig):` call sites"
      via: "__bool__ delegates to .valid"
      pattern: "def __bool__"
---

<objective>
Fix W6 (plugin_utils ATR unit-label bug + epsilon guard) and W5 (validate_signal failure-reason instrumentation via ValidationResult NamedTuple). Both are self-contained code fixes with no external dependencies and no shared files with other Wave 1 plans.

Purpose: W6 stops misleading telemetry (price distance labeled as "ATR ratio") and prevents a ZeroDivisionError from masking the intended ValueError. W5 makes the 19 schema_violation events across 6 plugins diagnosable by surfacing the failure reason, without breaking any boolean call site.
Output: Corrected error strings in plugin_utils.py, ValidationResult type in signal_schema.py, reason logging in executor.py, and updated unit tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-06-18-post-reboot-repair-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: W6 — Fix plugin_utils ATR ratio label and add zero-ATR epsilon guard</name>
  <read_first>
    - src/intelligence/trading/plugin_utils.py (read lines 130-200 to see both ValueError sites and confirm `atr` is in scope and non-None by this branch)
    - docs/plans/2026-06-18-post-reboot-repair-design.md (section "W6")
  </read_first>
  <files>src/intelligence/trading/plugin_utils.py</files>
  <action>
    Add a module-level constant `_ATR_EPSILON = 1e-8` with comment noting it guards against zero-ATR bars (market close / zero-range candles).
    There are two ValueError format strings (around lines 153-154 and 190-191) that currently interpolate `{original_inside_distance:.2f}` immediately before the literal text ` ATR inside zone`. In both, replace `original_inside_distance` in the interpolation with `original_inside_distance / max(atr, _ATR_EPSILON)` so the reported number is a dimensionless ATR ratio, not a raw price distance. Keep the surrounding message text (`stop correction too extreme`, `stop {stop_loss:.2f} is`, `inside zone [{zone_low}, {zone_high}])`) unchanged.
    Do not change the threshold check `if original_inside_distance > atr * 3.0:` — only the display value inside the error string changes. `atr` is guaranteed non-None on this path (an earlier `if atr is None: raise` branch runs first); the epsilon only protects against `atr == 0.0`.
  </action>
  <verify>.venv/bin/python -c "import src.intelligence.trading.plugin_utils" && grep -n "_ATR_EPSILON\|max(atr, _ATR_EPSILON)" src/intelligence/trading/plugin_utils.py</verify>
  <acceptance_criteria>
    - plugin_utils.py contains the literal `_ATR_EPSILON = 1e-8`
    - Both ATR-ratio error strings interpolate `original_inside_distance / max(atr, _ATR_EPSILON)` (grep returns 2 matches for `max(atr, _ATR_EPSILON)`)
    - The threshold comparison `original_inside_distance > atr * 3.0` is still present and unchanged
    - `.venv/bin/python -c "import src.intelligence.trading.plugin_utils"` exits 0
  </acceptance_criteria>
  <done>Both stop-correction ValueError messages report a dimensionless ATR ratio; a zero-ATR bar produces a large finite ratio in the message rather than a ZeroDivisionError.</done>
</task>

<task type="auto">
  <name>Task 2: W5 — Add ValidationResult NamedTuple and convert validate_signal to return it</name>
  <read_first>
    - src/intelligence/trading/signal_schema.py (read the full file; note REQUIRED_SIGNAL_FIELDS at line ~36 and validate_signal at line ~113, plus every existing return path inside validate_signal)
    - docs/plans/2026-06-18-post-reboot-repair-design.md (section "W5" — failure reason string literals are authoritative)
  </read_first>
  <files>src/intelligence/trading/signal_schema.py</files>
  <action>
    Define `class ValidationResult(NamedTuple)` (import NamedTuple from typing) with fields `valid: bool` and `reason: str`, and a `__bool__(self) -> bool` method that returns `self.valid`. Place it above `validate_signal`.
    Change `validate_signal` signature from `-> bool` to `-> ValidationResult`. Replace every `return True` / `return False` inside the function with `ValidationResult(...)` using these exact reason string literals: not-a-dict → `ValidationResult(False, "not_dict")`; REQUIRED_SIGNAL_FIELDS not a subset of keys → `ValidationResult(False, "missing_fields")`; `signal["type"] != "signal.v1"` → `ValidationResult(False, "type_mismatch")`; confidence outside [0.0, 1.0] → `ValidationResult(False, "confidence_oor")`; direction not in (1, -1, 1.0, -1.0) → `ValidationResult(False, "direction_invalid")`; empty or non-list targets → `ValidationResult(False, "targets_empty")`; stop on wrong side of entry → `ValidationResult(False, "stop_geometry")`; a target on wrong side of entry → `ValidationResult(False, "target_geometry")`; success path → `ValidationResult(True, "")`.
    Use the empty string `""` for the valid reason, not `"ok"`. Preserve the existing validation ordering/logic; only the return values change. If the current function does not already perform all of these checks, map only the checks that exist to the matching literal and use the success return for the final path — do not invent new validation logic beyond what the design lists.
  </action>
  <verify>.venv/bin/python -c "from src.intelligence.trading.signal_schema import validate_signal, ValidationResult; r=validate_signal({}); assert not r and r.reason; print('ok', r)"</verify>
  <acceptance_criteria>
    - signal_schema.py contains the literal `class ValidationResult(NamedTuple):`
    - ValidationResult defines `def __bool__(self) -> bool:` returning `self.valid`
    - `validate_signal({})` returns a falsy ValidationResult with a non-empty `.reason`
    - `bool(validate_signal(<a valid signal dict>))` is True and its `.reason == ""`
    - The literal `"not_dict"` appears in signal_schema.py
  </acceptance_criteria>
  <done>validate_signal returns ValidationResult whose truthiness matches the prior bool semantics, and whose .reason names the specific failure class.</done>
</task>

<task type="auto">
  <name>Task 3: W5 — Log reason at executor + signal_writer call sites and update unit tests</name>
  <read_first>
    - src/intelligence/pipeline/executor.py (read lines 880-915 — the validate_signal call at line ~898 and the schema_violation log)
    - services/signal_writer.py (read lines 100-145 — validate_signal at line ~112 and DLQ path at ~139)
    - tests/unit/intelligence/test_signal_schema.py (look for any `valid, reason = validate_signal(...)` tuple-unpacking)
    - tests/unit/intelligence/test_emit_signal_validation.py (line ~68 — same)
    - tests/unit/intelligence/test_plugin_utils.py (assertions on the ATR error message text)
  </read_first>
  <files>src/intelligence/pipeline/executor.py, services/signal_writer.py, tests/unit/intelligence/test_signal_schema.py, tests/unit/intelligence/test_emit_signal_validation.py, tests/unit/intelligence/test_plugin_utils.py</files>
  <action>
    In executor.py at the line ~898 call site, change `if not validate_signal(sig):` to `result = validate_signal(sig)` followed by `if not result:`, and add `reason=result.reason` as a kwarg to the existing `self._logger.error("executor.schema_violation", ...)` call (keep the existing `plugin=` and `missing_fields=` kwargs). Do not pass `event=`.
    In signal_writer.py, the call at line ~112 uses `if validate_signal(sig):` for the happy path — this remains correct under __bool__ and requires no change for correctness; optionally capture `result = validate_signal(sig)` and include `reason=result.reason` on the DLQ-path log at line ~139 if a log call exists there. If adding the reason requires no structural change, do it; otherwise leave the boolean usage as-is.
    Update any test that unpacks the return as a tuple: replace `valid, reason = validate_signal(...)` with `result = validate_signal(...); valid, reason = result` (ValidationResult is still iterable as a 2-tuple, so direct unpacking also works — prefer whichever keeps the test green). Update test_plugin_utils.py assertions that check the stop-correction error message so they match the new ATR-ratio wording (the number is now a ratio; assert on the stable literal substring ` ATR inside zone` rather than a specific numeric value).
  </action>
  <verify>.venv/bin/pytest tests/unit/intelligence/test_signal_schema.py tests/unit/intelligence/test_emit_signal_validation.py tests/unit/intelligence/test_plugin_utils.py -q</verify>
  <acceptance_criteria>
    - executor.py line ~898 region contains `reason=result.reason` inside the schema_violation error log
    - executor.py no longer calls `validate_signal(sig)` directly inside the `if not` condition (it binds to `result` first)
    - `.venv/bin/pytest tests/unit/intelligence/test_signal_schema.py tests/unit/intelligence/test_emit_signal_validation.py tests/unit/intelligence/test_plugin_utils.py -q` exits 0
    - No test references a removed numeric-distance value in the ATR error string
  </acceptance_criteria>
  <done>Schema-violation log events carry the failure reason and all touched unit tests are green.</done>
</task>

</tasks>

<verification>
- `.venv/bin/ruff check src/intelligence/trading/plugin_utils.py src/intelligence/trading/signal_schema.py src/intelligence/pipeline/executor.py services/signal_writer.py` is clean
- `.venv/bin/pytest tests/unit/intelligence/test_signal_schema.py tests/unit/intelligence/test_emit_signal_validation.py tests/unit/intelligence/test_plugin_utils.py -q` exits 0
- `grep -rn "validate_signal" src/ services/` shows no call site that treats validate_signal's return as a multi-element truthy tuple in a boolean condition
</verification>

<success_criteria>
- ValidationResult exists with __bool__ delegating to .valid; all 8 failure literals + "" success literal present
- plugin_utils reports dimensionless ATR ratio with epsilon guard
- executor.schema_violation logs reason=
- All affected unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/136-post-reboot-system-repair/136-01-SUMMARY.md`
</output>
