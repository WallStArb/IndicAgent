---
phase: 136-post-reboot-system-repair
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/register_plugins.py
autonomous: true

must_haves:
  truths:
    - "FVGFill no longer participates in the I7 trade-signal tier"
    - "SMC-tier FVG detection (fvg_type/fvg_top/fvg_bottom features) remains active"
    - "TIER_I7 membership tests and tier-size assertions pass with FVGFill removed"
  artifacts:
    - path: "src/intelligence/register_plugins.py"
      provides: "TIER_I7 list without fvg_fill_plugin.name; restoration comment"
      contains: "FVGFill removed"
  key_links:
    - from: "src/intelligence/register_plugins.py TIER_I7"
      to: "shadow_registry auto-enroll loop (for plugin_name in TIER_I7)"
      via: "removal of fvg_fill_plugin.name from TIER_I7 means no new shadow enrollment"
      pattern: "for plugin_name in TIER_I7"
---

<objective>
W4: Disable the FVGFill I7 trade-signal plugin by removing `fvg_fill_plugin.name` from `TIER_I7` in register_plugins.py, leaving the SMC-tier FVG detection plugin untouched. FVGFill generates 4,187 circuit-breaker events per boot (entry-timing defect), masking real errors in the intelligence_pipeline log.

Purpose: Eliminate structural log noise without losing FVG zone information (FVG features are computed by upstream SMC plugins, not the I7 generator).
Output: TIER_I7 list with FVGFill removed and a restoration comment, plus all affected tests swept and green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-06-18-post-reboot-repair-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove fvg_fill_plugin.name from TIER_I7 with restoration comment</name>
  <read_first>
    - src/intelligence/register_plugins.py (read lines 110-120 for the import, 405-415 for registry.register_pattern, and 631-710 for the TIER_I7 list definition and the shadow auto-enroll loop)
    - docs/plans/2026-06-18-post-reboot-repair-design.md (section "W4" — note what must NOT change)
  </read_first>
  <files>src/intelligence/register_plugins.py</files>
  <action>
    In the `TIER_I7: list[str]` definition (starts ~line 631), remove the entry `fvg_fill_plugin.name` (~line 642) and replace it with a comment on its own line: `# FVGFill removed: entry-timing defect (see plugin docstring). Restore after at_limit redesign.`
    Do NOT remove the import at line ~117 (`from .trading.fvg_fill import plugin as fvg_fill_plugin`) and do NOT remove the `registry.register_pattern(fvg_fill_plugin)` call at line ~410 — the plugin class stays importable and registered as a pattern object; only its TIER_I7 membership (which drives I7 signal generation and shadow auto-enroll) is removed. Leaving the import in place avoids an unused-import lint error only if the import is still referenced; if removing it from TIER_I7 makes the import unused per ruff, instead keep the registry.register_pattern reference (which uses it) so the import remains live. Do not touch the SMC-tier FVG detection plugin (the one computing fvg_type/fvg_top/fvg_bottom).
  </action>
  <verify>grep -n "fvg_fill\|FVGFill removed" src/intelligence/register_plugins.py && .venv/bin/python -c "from src.intelligence.register_plugins import TIER_I7; from src.intelligence.trading.fvg_fill import plugin as p; assert p.name not in TIER_I7, 'still in TIER_I7'; print('absent from TIER_I7')"</verify>
  <acceptance_criteria>
    - `fvg_fill_plugin.name` is not present in the TIER_I7 list
    - register_plugins.py contains the literal comment `FVGFill removed`
    - `.venv/bin/python -c "from src.intelligence.register_plugins import TIER_I7; from src.intelligence.trading.fvg_fill import plugin as p; assert p.name not in TIER_I7"` exits 0
    - `.venv/bin/ruff check src/intelligence/register_plugins.py` is clean (no unused-import error)
  </acceptance_criteria>
  <done>FVGFill is absent from TIER_I7; the SMC FVG detection plugin and the FVGFill import/registration object remain intact.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep and update all tests referencing fvg_fill / FVGFill / TIER_I7 membership</name>
  <read_first>
    - tests/unit/intelligence/test_i7_registration.py (TIER_I7 size/membership assertions)
    - tests/unit/intelligence/test_plugin_registry.py (registration counts)
    - All files returned by `grep -rln "fvg_fill\|FVGFill\|TIER_I7" tests/`
    - docs/plans/2026-06-18-post-reboot-repair-design.md (section "W4" — "Test sweep required")
  </read_first>
  <files>tests/unit/intelligence/test_i7_registration.py, tests/unit/intelligence/test_plugin_registry.py</files>
  <action>
    Run `grep -rln "fvg_fill\|FVGFill\|TIER_I7" tests/` to enumerate every affected test file (do not assume only the two listed here). For each: update tier-size assertions (any hardcoded `len(TIER_I7) == N` must use N-1, or assert membership relationally), update membership assertions that expect `fvg_fill_plugin.name in TIER_I7` to expect absence, and update any registration-count assertion that counted FVGFill as an I7 generator. A test asserting FVGFill is still importable/registered as a pattern object is correct and should NOT be changed. Keep all other assertions intact. If a test hardcodes the I7 plugin count via an APR-derived or computed value rather than a literal, prefer recomputing rather than editing a literal.
  </action>
  <verify>grep -rln "fvg_fill\|FVGFill\|TIER_I7" tests/ | xargs .venv/bin/pytest -q</verify>
  <acceptance_criteria>
    - `grep -rln "fvg_fill\|FVGFill\|TIER_I7" tests/` enumerated and every returned file's relevant assertions updated
    - `grep -rln "fvg_fill\|FVGFill\|TIER_I7" tests/ | xargs .venv/bin/pytest -q` exits 0
    - No test asserts `fvg_fill_plugin.name in TIER_I7`
  </acceptance_criteria>
  <done>All tests referencing FVGFill / TIER_I7 are updated and green with FVGFill removed from the I7 tier.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -c "from src.intelligence.register_plugins import TIER_I7; from src.intelligence.trading.fvg_fill import plugin as p; assert p.name not in TIER_I7"` exits 0
- Full sweep `grep -rln "fvg_fill\|FVGFill\|TIER_I7" tests/ | xargs .venv/bin/pytest -q` exits 0
- Note for deploy (operational, not part of this plan): `indicagent-intelligence-pipeline` must be restarted after deploy to drop FVGFill firing.
</verification>

<success_criteria>
- FVGFill absent from TIER_I7, restoration comment present
- SMC FVG detection untouched
- All affected tests green
</success_criteria>

<output>
After completion, create `.planning/phases/136-post-reboot-system-repair/136-02-SUMMARY.md`
</output>
