---
phase: 118-confidence-integrity-top5-setup-refactoring
plan: "00"
type: execute
wave: 0
depends_on: []
requirements: [REFACTOR-01, REFACTOR-02, REFACTOR-03, REFACTOR-04, REFACTOR-05]
files_modified:
  - src/intelligence/trading/ofi_continuation.py
  - src/intelligence/trading/gap_analysis_setup.py
  - src/intelligence/trading/cvd_divergence.py
  - src/intelligence/trading/failed_breakout.py
  - src/intelligence/trading/ofi_divergence.py
  - src/intelligence/trading/orb15.py
  - src/intelligence/trading/orb30.py
  - src/intelligence/trading/prev_day_level_test.py
  - src/intelligence/trading/choch_reversal.py
  - src/intelligence/trading/supply_demand_setup.py
  - src/intelligence/trading/liquidity_sweep_reclaim.py
  - src/intelligence/trading/liquidity_hunt.py
  - src/intelligence/trading/momentum_breakout.py
  - src/intelligence/trading/squeeze_expansion.py
  - src/intelligence/trading/trend_following.py
  - tests/unit/intelligence/test_i7_extrinsic_contract.py
autonomous: true

must_haves:
  truths:
    - "No hmm_regime_weight, apply_exhaustion_boost, or apply_exhaustion_guard call sites remain in any I7 trading plugin confidence path"
    - "No ctf_score, ctf_structure_alignment, or ctf_trend_alignment arithmetic appears in any I7 confidence formula"
    - "No zone context (in_supply_zone, in_demand_zone, supply_strength, demand_strength) modifies confidence in any I7 plugin"
    - "No SMC event fields (fvg_type, ob_type, choch_detected, bos_detected, price_in_premium) modify confidence in liquidity_hunt or any I7 plugin"
    - "Every modified plugin's final confidence still flows through compose_confidence(raw_conf)"
    - "Extrinsic fields (ctf_score, exhaustion, zone, SMC) still appear in capture_signal_features() / feature-capture path so they reach ML training unchanged"
    - "A contract test proves that perturbing only extrinsic feature keys leaves confidence unchanged for each modified plugin"
    - "All unit tests pass after the strip"
    - "momentum_breakout uses 3-factor composite (roc+vol+break_margin), squeeze_expansion uses 3-factor composite, trend_following uses 2- or 3-factor intrinsic composite"
  artifacts:
    - path: "src/intelligence/trading/ofi_continuation.py"
      provides: "OFI continuation plugin with extrinsic modifiers stripped"
    - path: "src/intelligence/trading/momentum_breakout.py"
      provides: "Momentum breakout with redistributed intrinsic composite"
    - path: "src/intelligence/trading/trend_following.py"
      provides: "Trend following with Kalman-only composite"
    - path: "src/intelligence/trading/liquidity_hunt.py"
      provides: "Liquidity hunt stripped of zone/SMC/exhaustion/HMM/CTF confidence modifiers"
    - path: "tests/unit/intelligence/test_i7_extrinsic_contract.py"
      provides: "Contract test: extrinsic-only perturbation leaves confidence unchanged; confidence in [0.0,0.95]"
  key_links:
    - from: "src/intelligence/trading/*.py"
      to: "src/intelligence/utils/gradient_utils.py"
      via: "imports"
      pattern: "hmm_regime_weight"
      expected_after: "zero call sites in confidence path"
    - from: "src/intelligence/trading/*.py"
      to: "src/intelligence/trading/exhaustion_utils.py"
      via: "imports"
      pattern: "apply_exhaustion_boost|apply_exhaustion_guard"
      expected_after: "zero call sites in confidence path"
    - from: "src/intelligence/trading/*.py"
      to: "src/intelligence/utils/confidence_utils.py"
      via: "compose_confidence()"
      pattern: "compose_confidence"
      expected_after: "every confidence output routed through compose_confidence"
---

<objective>
Strip all signal-extrinsic confidence modifiers from every affected I7 trading plugin.

Purpose: Confidence must reflect only the intrinsic quality of the detected pattern. Extrinsic data (HMM regime, CTF/I6 scores, exhaustion state, zone context, SMC events) already travels in `capture_signal_features()` and the feature vector — removing it from confidence routes it correctly for ML training without discarding it. This wave is a prerequisite for Waves 1-5; the three files shared between this wave and the setup refactors (ofi_continuation, gap_analysis_setup, cvd_divergence) must have their extrinsic strips committed before the intrinsic formula upgrades begin.

This wave touches 15 plugin files in one serial plan — the blast radius is large (Codex HIGH). The mitigation is a dedicated contract test (Task 6) that proves, per plugin, that extrinsic-only perturbations do not move confidence, plus a broadened semantic grep that catches locally-renamed extrinsic helpers, CTF, zone, and SMC contamination (Gemini + Codex HIGH).

Output: 15 modified plugin files, one new contract test file, all unit tests green, zero extrinsic modifier calls remaining in any confidence path, and every confidence output routed through `compose_confidence()`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-06-08-confidence-intrinsic-cleanup.md
@docs/ideas/signal-07-signal-context-enricher.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip HMM + CTF + exhaustion_guard from ofi_continuation, gap_analysis_setup, cvd_divergence</name>
  <read_first>
    src/intelligence/trading/ofi_continuation.py
    src/intelligence/trading/gap_analysis_setup.py
    src/intelligence/trading/cvd_divergence.py
    src/intelligence/utils/confidence_utils.py
    docs/plans/2026-06-08-confidence-intrinsic-cleanup.md
  </read_first>
  <files>
    src/intelligence/trading/ofi_continuation.py
    src/intelligence/trading/gap_analysis_setup.py
    src/intelligence/trading/cvd_divergence.py
  </files>
  <action>
Apply the removals specified in `docs/plans/2026-06-08-confidence-intrinsic-cleanup.md` Task 1, PLUS the additional exhaustion_guard removal from ofi_continuation found in research.

**ofi_continuation.py** — remove ALL of the following:
1. `apply_exhaustion_guard` call (line ~123): remove the call and any result capture
2. ctf_score additive block (lines ~126-129):
   - `ctf_score = float(features.get("ctf_score", 0.0))`
   - `if abs(ctf_score) > 0.3:` block that does `raw_conf += 0.15 * min(1.0, abs(ctf_score) / 0.7)` and `supporting.append(f"ctf_score={ctf_score:.3f}")`
3. hmm_regime_weight additive (lines ~132-133):
   - `regime_w = hmm_regime_weight(features, "up" if direction == 1 else "down")`
   - `raw_conf += 0.10 * (regime_w - 0.5)`
4. Remove unused imports: `from ..utils.gradient_utils import hmm_regime_weight` and `from .exhaustion_utils import apply_exhaustion_guard`
5. Confirm the final confidence still passes through `compose_confidence(raw_conf)` before being attached to the signal. If the strip leaves a bare `raw_conf` that bypasses `compose_confidence`, wire it back in.

**gap_analysis_setup.py** — remove:
1. hmm_regime_weight additive block: `regime_w = hmm_regime_weight(...)` and `base += 0.10 * (regime_w - 0.5)`
2. apply_exhaustion_boost call (line ~150): `confidence, supporting = apply_exhaustion_boost(features, direction, confidence, supporting)`
3. Remove unused imports: `from ..utils.gradient_utils import hmm_regime_weight` and `from .exhaustion_utils import apply_exhaustion_boost`
4. Confirm `compose_confidence(...)` still wraps the final confidence value.

**cvd_divergence.py** — remove:
1. hmm_regime_weight additive (lines ~143-144): `regime_w = hmm_regime_weight(...)` and `raw_conf += 0.10 * (regime_w - 0.5)`
2. ctf_score additive block (lines ~138-141): `ctf_score = float(features.get("ctf_score", 0.0))` and the `if abs(ctf_score) > 0.3:` block doing `raw_conf += 0.15 * min(1.0, abs(ctf_score) / 0.7)`
3. ctf_score supporting.append reference (~line 167) — remove the append only, not any surrounding logic
4. Remove unused import: `from ..utils.gradient_utils import hmm_regime_weight`
5. Confirm `compose_confidence(...)` still wraps the final confidence value.

After each file: run `.venv/bin/ruff check src/intelligence/trading/<file>.py --fix` to catch unused variable warnings.

Commit: `refactor(118): strip extrinsic confidence modifiers from ofi_continuation, gap_analysis, cvd_divergence`
  </action>
  <verify>
    .venv/bin/pytest tests/unit/ -q
    grep -n "hmm_regime_weight\|apply_exhaustion_boost\|apply_exhaustion_guard\|ctf_score" src/intelligence/trading/ofi_continuation.py src/intelligence/trading/gap_analysis_setup.py src/intelligence/trading/cvd_divergence.py
    grep -n "compose_confidence" src/intelligence/trading/ofi_continuation.py src/intelligence/trading/gap_analysis_setup.py src/intelligence/trading/cvd_divergence.py
  </verify>
  <acceptance_criteria>
    - grep for hmm_regime_weight / apply_exhaustion_boost / apply_exhaustion_guard returns zero confidence-path hits in all three files (reads inside capture_signal_features / supporting / regime_context strings are acceptable and must be manually confirmed as non-arithmetic)
    - No `raw_conf += ... ctf_score ...` or `base += ... regime_w ...` arithmetic remains in any of the three files
    - Each of the three files contains at least one `compose_confidence(` call wrapping the final confidence value
    - `.venv/bin/pytest tests/unit/ -q` exits 0
    - `.venv/bin/ruff check src/intelligence/trading/ofi_continuation.py src/intelligence/trading/gap_analysis_setup.py src/intelligence/trading/cvd_divergence.py` reports no unused-import or undefined-name errors
  </acceptance_criteria>
  <done>Unit tests green. grep shows no hmm_regime_weight, exhaustion_boost/guard, or ctf_score in confidence-modifying positions in the three files (reads for logging/supporting only are acceptable). compose_confidence still wraps every final confidence.</done>
</task>

<task type="auto">
  <name>Task 2: Strip HMM additive boosts from failed_breakout, ofi_divergence, orb15, orb30, prev_day_level_test</name>
  <read_first>
    src/intelligence/trading/failed_breakout.py
    src/intelligence/trading/ofi_divergence.py
    src/intelligence/trading/orb15.py
    src/intelligence/trading/orb30.py
    src/intelligence/trading/prev_day_level_test.py
    docs/plans/2026-06-08-confidence-intrinsic-cleanup.md
  </read_first>
  <files>
    src/intelligence/trading/failed_breakout.py
    src/intelligence/trading/ofi_divergence.py
    src/intelligence/trading/orb15.py
    src/intelligence/trading/orb30.py
    src/intelligence/trading/prev_day_level_test.py
  </files>
  <action>
Apply removals from `docs/plans/2026-06-08-confidence-intrinsic-cleanup.md` Task 2 exactly as written.

**failed_breakout.py** — remove the `ranging_w` / `trending_w` computation and the `confidence += 0.15 * ranging_w` / `confidence -= 0.10 * trending_w` modifiers. Remove unused import `hmm_regime_weight`.

**ofi_divergence.py** — remove the entire `hmm_regime` confidence-modification block (~lines 151-158) that does `confidence += 0.06 * ranging_w` and `confidence -= 0.06 * trending_w`. KEEP any later `hmm_regime` reads used only for `supporting.append` or `regime_context` strings — those are metadata, not confidence modifiers. If `hmm_regime` is re-read later for logging, re-establish the local assignment there. Remove unused import `hmm_regime_weight`.

**orb15.py and orb30.py** (apply same change to both) — remove `trending_w = max(hmm_regime_weight(...))` and `confidence += 0.10 * trending_w`. Keep the `gap_boost` line — it is intrinsic. Remove unused import from each file.

**prev_day_level_test.py** — remove the `ranging_w` / `trending_w` computation and the conditional boost/penalty block (the `if setup_variant == "fade":` block that adds/subtracts `ranging_w`/`trending_w`). confidence starts at 0.50 after removal — correct baseline. Remove unused import.

For each file, confirm the final confidence still routes through `compose_confidence(...)`; if a file did not previously use it, leave the existing clamping in place (do NOT introduce compose_confidence where the file never used it — Task 6 contract test validates the [0,0.95] bound regardless).

After all 5 files: run `.venv/bin/ruff check src/intelligence/trading/ --fix`

Commit: `refactor(118): strip hmm_regime_weight from failed_breakout, ofi_divergence, orb15/30, prev_day_level_test`
  </action>
  <verify>
    .venv/bin/pytest tests/unit/ -q
    grep -n "hmm_regime_weight" src/intelligence/trading/failed_breakout.py src/intelligence/trading/ofi_divergence.py src/intelligence/trading/orb15.py src/intelligence/trading/orb30.py src/intelligence/trading/prev_day_level_test.py
  </verify>
  <acceptance_criteria>
    - grep for `hmm_regime_weight` returns zero hits across all five files
    - No `confidence += ... ranging_w` / `confidence -= ... trending_w` arithmetic remains in any of the five files
    - `.venv/bin/pytest tests/unit/ -q` exits 0
    - `.venv/bin/ruff check src/intelligence/trading/` reports no unused-import or undefined-name errors for the five files
  </acceptance_criteria>
  <done>Unit tests green. No hmm_regime_weight call sites remain in any of the 5 files.</done>
</task>

<task type="auto">
  <name>Task 3: Strip HMM + CTF from choch_reversal; strip HMM + CTF + exhaustion from supply_demand_setup and liquidity_sweep_reclaim</name>
  <read_first>
    src/intelligence/trading/choch_reversal.py
    src/intelligence/trading/supply_demand_setup.py
    src/intelligence/trading/liquidity_sweep_reclaim.py
    docs/plans/2026-06-08-confidence-intrinsic-cleanup.md
  </read_first>
  <files>
    src/intelligence/trading/choch_reversal.py
    src/intelligence/trading/supply_demand_setup.py
    src/intelligence/trading/liquidity_sweep_reclaim.py
  </files>
  <action>
Apply removals from `docs/plans/2026-06-08-confidence-intrinsic-cleanup.md` Tasks 3 and 4 exactly.

**choch_reversal.py** — remove the HMM block (`up_w`/`down_w` reads and the `raw_conf += 0.2 * up_w` / `raw_conf += 0.2 * down_w` modifiers, plus the `supporting.append("hmm_regime_bullish/bearish")` lines inside that block). Remove the three CTF boost blocks (ctf_structure_alignment, ctf_trend_alignment, ctf_score) per plan Task 3 exact code. Remove unused import `hmm_regime_weight`. Check if `import math` is still needed after removal — keep if used elsewhere.

**supply_demand_setup.py** — remove:
1. `ranging_w = hmm_regime_weight(...)` + `confidence += 0.05 * ranging_w`
2. CTF boost block (`ctf = float(features.get("ctf_score", 0.0))...`) per plan Task 4
3. exhaustion boost call: `confidence, supporting = apply_exhaustion_boost(...)`
Remove unused imports `hmm_regime_weight` and `apply_exhaustion_boost` if no longer called.

**liquidity_sweep_reclaim.py** — remove:
1. `ranging_w = hmm_regime_weight(...)` + `confidence += 0.10 * ranging_w`
2. CTF boost block (`ctf_score = features.get("ctf_score", 0.0)...`) per plan Task 4
3. exhaustion boost call: `confidence, supporting = apply_exhaustion_boost(...)`
Remove unused import `hmm_regime_weight` only (keep `linear_ramp` if still used). Remove `apply_exhaustion_boost` import if no longer called.

Confirm each file's final confidence still routes through its existing clamp / `compose_confidence(...)`.

Run `.venv/bin/ruff check src/intelligence/trading/ --fix` after all three files.

Commit: `refactor(118): strip extrinsic modifiers from choch_reversal, supply_demand_setup, liquidity_sweep_reclaim`
  </action>
  <verify>
    .venv/bin/pytest tests/unit/ -q
    grep -n "hmm_regime_weight\|apply_exhaustion_boost\|ctf_structure_alignment\|ctf_trend_alignment\|ctf_score" src/intelligence/trading/choch_reversal.py src/intelligence/trading/supply_demand_setup.py src/intelligence/trading/liquidity_sweep_reclaim.py
  </verify>
  <acceptance_criteria>
    - grep returns zero confidence-path hits for hmm_regime_weight, apply_exhaustion_boost, ctf_structure_alignment, ctf_trend_alignment, ctf_score across the three files (capture/supporting reads acceptable, manually confirmed non-arithmetic)
    - No `raw_conf +=` / `confidence +=` arithmetic referencing up_w, down_w, ranging_w, ctf, or ctf_score remains
    - `.venv/bin/pytest tests/unit/ -q` exits 0
    - ruff reports no unused-import / undefined-name errors for the three files
  </acceptance_criteria>
  <done>Unit tests green. No extrinsic modifier call sites in the three files.</done>
</task>

<task type="auto">
  <name>Task 4: Full strip on liquidity_hunt (zone + SMC + exhaustion + HMM + CTF)</name>
  <read_first>
    src/intelligence/trading/liquidity_hunt.py
    docs/plans/2026-06-08-confidence-intrinsic-cleanup.md
  </read_first>
  <files>
    src/intelligence/trading/liquidity_hunt.py
  </files>
  <action>
Read the full liquidity_hunt.py before editing — it has the most removals. Apply plan Task 5 exactly.

Remove in order:
1. HMM weight: `trending_w = max(hmm_regime_weight(...))` + `confidence += 0.10 * trending_w`
2. CTF boost block: `ctf = float(features.get("ctf_score", 0.0))...`
3. Zone alignment boost AND opposing penalty (both the boost block and the penalty block for in_supply/in_demand, supply_zone_aligned/demand_zone_aligned/penalty_demand_zone_opposing/penalty_supply_zone_opposing)
4. SMC event boosts — ALL of: price_in_premium block, fvg_type block, ob_type block, choch/bos block
5. exhaustion boost call: `confidence, supporting = apply_exhaustion_boost(...)`

Remove unused imports: `from ..utils.gradient_utils import hmm_regime_weight` and `from .exhaustion_utils import apply_exhaustion_boost`.

Confirm the final confidence still routes through its existing clamp / `compose_confidence(...)`.

Run `.venv/bin/ruff check src/intelligence/trading/liquidity_hunt.py --fix`

Commit: `refactor(118): strip zone/SMC/exhaustion/HMM/CTF from liquidity_hunt confidence`
  </action>
  <verify>
    .venv/bin/pytest tests/unit/ -q
    grep -n "hmm_regime_weight\|apply_exhaustion_boost\|ctf_score\|in_supply_zone\|in_demand_zone\|fvg_type\|ob_type\|choch_detected\|bos_detected\|price_in_premium" src/intelligence/trading/liquidity_hunt.py
  </verify>
  <acceptance_criteria>
    - grep returns zero confidence-path hits for all of: hmm_regime_weight, apply_exhaustion_boost, ctf_score, in_supply_zone, in_demand_zone, fvg_type, ob_type, choch_detected, bos_detected, price_in_premium (any surviving references must be inside capture_signal_features / supporting / regime_context only, manually confirmed)
    - No `confidence +=` / `confidence -=` arithmetic referencing any zone, SMC, CTF, HMM, or exhaustion value remains
    - `.venv/bin/pytest tests/unit/ -q` exits 0
    - ruff reports no unused-import / undefined-name errors for liquidity_hunt.py
  </acceptance_criteria>
  <done>Unit tests green. No extrinsic modifier references remain in confidence-modifying positions in liquidity_hunt.py.</done>
</task>

<task type="auto">
  <name>Task 5: Composite restructures — momentum_breakout, squeeze_expansion, trend_following</name>
  <read_first>
    src/intelligence/trading/momentum_breakout.py
    src/intelligence/trading/squeeze_expansion.py
    src/intelligence/trading/trend_following.py
    docs/plans/2026-06-08-confidence-intrinsic-cleanup.md
  </read_first>
  <files>
    src/intelligence/trading/momentum_breakout.py
    src/intelligence/trading/squeeze_expansion.py
    src/intelligence/trading/trend_following.py
  </files>
  <action>
Apply plan Tasks 6, 7, 8.

**momentum_breakout.py** — remove regime_score component and replace composite formula. Remove `regime_aligns` variable if only used to gate regime_score. Remove zone friction penalty block (in_supply_zone/in_demand_zone/supply_strength/demand_strength confidence subtraction). Remove exhaustion guard call. Replace composite with `raw_conf = 0.40 * roc_score + 0.35 * vol_score + 0.25 * break_margin`. Before the weighted sum, clamp each of `roc_score`, `vol_score`, `break_margin` to [0,1] with `min(1.0, max(0.0, <factor>))` if they are not already guaranteed bounded (Codex factor-level clamp). Remove unused imports `hmm_regime_weight` and `apply_exhaustion_guard`. Check if `trend_regime` variable is still referenced in supporting factors — keep the read if so, remove if only used for regime_aligns.

**squeeze_expansion.py** — remove regime_score component and the if/else block computing it. Remove exhaustion guard call. Replace composite with `raw_conf = 0.35 * squeeze_bars_score + 0.35 * vol_expansion_score + 0.30 * momentum_score`. Clamp each factor to [0,1] before the weighted sum if not already bounded. Remove unused imports `hmm_regime_weight` and `apply_exhaustion_guard`. Check if `trend_regime` is still used in supporting — keep read if so.

**trend_following.py** — read the full file FIRST to check whether `swing_pattern` variable is available and non-trivially populated. Then:
- Remove `trend_regime` component (35%) and `ctf_score` component (20%) from the composite
- Remove zone friction penalty (same pattern as momentum_breakout)
- Remove exhaustion guard call
- If `swing_pattern` is available and meaningful: use `0.45 * trend_conf + 0.35 * trend_strength + 0.20 * swing_pattern`
- If `swing_pattern` is absent or always zero: use `0.55 * trend_conf + 0.45 * trend_strength`
- Clamp each factor to [0,1] before the weighted sum
- Remove unused imports. Check if `ctf_score` variable is read for supporting factors elsewhere — keep the read if so, remove only from composite arithmetic.

Confirm all three files route their final confidence through `compose_confidence(...)`.

Run `.venv/bin/ruff check src/intelligence/trading/ --fix && .venv/bin/black src/intelligence/trading/`

Commit: `refactor(118): composite restructure for momentum_breakout, squeeze_expansion, trend_following`
  </action>
  <verify>
    .venv/bin/pytest tests/unit/ -q
    .venv/bin/ruff check src/intelligence/trading/ --fix
    grep -n "roc_score\|vol_score\|break_margin\|squeeze_bars_score\|vol_expansion_score\|momentum_score\|trend_conf\|trend_strength" src/intelligence/trading/momentum_breakout.py src/intelligence/trading/squeeze_expansion.py src/intelligence/trading/trend_following.py
  </verify>
  <acceptance_criteria>
    - momentum_breakout composite is exactly `0.40 * roc_score + 0.35 * vol_score + 0.25 * break_margin` (weights sum to 1.0)
    - squeeze_expansion composite is exactly `0.35 * squeeze_bars_score + 0.35 * vol_expansion_score + 0.30 * momentum_score` (weights sum to 1.0)
    - trend_following composite weights sum to 1.0 and contain no trend_regime or ctf_score term
    - Each factor in all three composites is clamped to [0,1] before weighting (visible `min(1.0, max(0.0, ...))` or a documented already-bounded source)
    - No regime_score / regime_aligns arithmetic remains in the confidence path
    - `.venv/bin/pytest tests/unit/ -q` exits 0; ruff and black clean
  </acceptance_criteria>
  <done>Unit tests green. Composites use stated weights summing to 1.0, factor-clamped. ruff/black clean.</done>
</task>

<task type="auto">
  <name>Task 6: Contract test — extrinsic-only perturbation leaves confidence unchanged across all stripped plugins</name>
  <read_first>
    src/intelligence/trading/ofi_continuation.py
    src/intelligence/trading/liquidity_hunt.py
    src/intelligence/utils/confidence_utils.py
    tests/unit/intelligence/test_gap_analysis_setup.py
    src/intelligence/register_plugins.py
  </read_first>
  <files>
    tests/unit/intelligence/test_i7_extrinsic_contract.py
  </files>
  <action>
Create a single contract test file that covers the Wave 0 blast radius (Codex HIGH: 15 plugins changed in one plan without per-plugin regression safety) and proves the capture-vs-confidence separation (Codex MEDIUM: extrinsic factors must still be captured, just not in confidence).

Read `test_gap_analysis_setup.py` for the helper pattern used to build a controlled features dict and invoke `compute_full`. Read `register_plugins.py` (TIER_I7 list) to obtain the canonical set of I7 plugin classes so the parametrized test can iterate the ones modified in this wave.

Implement these tests:

1. **test_extrinsic_perturbation_does_not_change_confidence** (parametrized over each plugin class modified in Wave 0 that can fire on a synthetic features dict):
   - Build a baseline features dict with the intrinsic keys each plugin needs to FIRE (reuse per-plugin helpers from existing tests where they exist; for plugins without helpers, build the minimal intrinsic dict).
   - Capture confidence A.
   - Build a perturbed copy that ADDS/CHANGES only extrinsic keys: `ctf_score=0.9`, `ctf_structure_alignment=1.0`, `ctf_trend_alignment=1.0`, `hmm_regime=1`, `in_supply_zone=True`, `in_demand_zone=True`, `supply_strength=1.0`, `demand_strength=1.0`, `fvg_type="bull"`, `ob_type="bull"`, `choch_detected=True`, `bos_detected=True`, `price_in_premium=True`, and exhaustion fields the plugin previously read (e.g. `delta_exhaustion`, `exhaustion_score`).
   - Capture confidence B from the same firing scenario.
   - Assert `abs(A - B) < 1e-9` (extrinsic keys may not move confidence at all).
   - For any plugin that legitimately cannot produce a deterministic firing scenario in a unit test, xfail/skip it explicitly with a reason rather than silently dropping it.

2. **test_confidence_within_bounds** (parametrized over the same plugins): for each plugin's firing scenario, assert returned confidence is within `[0.0, 0.95]` (compose_confidence ceiling).

3. **test_extrinsic_still_captured_in_features_snapshot**: pick OFIContinuation (it calls `capture_signal_features(features, direction, "microstructure", confidence)`). Build a firing scenario with `ctf_score=0.8` present in features. Assert the returned signal's captured features payload (features_snapshot / capture dict) still contains the extrinsic key(s) — proving removal from confidence did NOT remove them from the ML feature path.

Follow import patterns from existing `tests/unit/intelligence/` files. Test through the public `compute_full` interface; do not mock plugin internals.

Run `.venv/bin/pytest tests/unit/intelligence/test_i7_extrinsic_contract.py -v`

Commit: `test(118): contract test — extrinsic perturbation leaves I7 confidence unchanged; extrinsic still captured`
  </action>
  <verify>
    .venv/bin/pytest tests/unit/intelligence/test_i7_extrinsic_contract.py -v
    .venv/bin/pytest tests/unit/ -q
  </verify>
  <acceptance_criteria>
    - test_extrinsic_perturbation_does_not_change_confidence passes for every Wave-0-modified plugin that can fire (skipped/xfailed plugins carry an explicit reason)
    - test_confidence_within_bounds passes — every plugin firing returns confidence in [0.0, 0.95]
    - test_extrinsic_still_captured_in_features_snapshot passes — ctf_score (or other extrinsic key) appears in the captured features payload despite being absent from the confidence formula
    - `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
  <done>Contract test passes: extrinsic perturbation does not move confidence, confidence bounded [0,0.95], extrinsic still captured for ML. Full suite green.</done>
</task>

</tasks>

<verification>
Run after all 6 tasks complete:

```bash
# Primary gate — known extrinsic helpers, must return zero confidence-path results
grep -rn "hmm_regime_weight\|apply_exhaustion_boost\|apply_exhaustion_guard" src/intelligence/trading/*.py | grep -v "def \|\"\"\"" | grep -v "gradient_utils\|exhaustion_utils"

# Broadened semantic gate (Gemini + Codex HIGH) — CTF/zone/SMC contamination.
# Each hit MUST be manually classified as confidence-path (FAIL) vs capture/supporting/regime_context (OK).
grep -rn "ctf_score\|ctf_structure_alignment\|ctf_trend_alignment\|in_supply_zone\|in_demand_zone\|supply_strength\|demand_strength\|fvg_type\|ob_type\|choch_detected\|bos_detected\|price_in_premium" src/intelligence/trading/*.py | grep -v "capture_signal_features\|supporting\|regime_context\|features.get\|# "

# Invariant gate (Codex HIGH) — every modified plugin routes final confidence through compose_confidence
grep -L "compose_confidence" src/intelligence/trading/ofi_continuation.py src/intelligence/trading/gap_analysis_setup.py src/intelligence/trading/cvd_divergence.py src/intelligence/trading/momentum_breakout.py src/intelligence/trading/squeeze_expansion.py src/intelligence/trading/trend_following.py src/intelligence/trading/choch_reversal.py src/intelligence/trading/supply_demand_setup.py src/intelligence/trading/liquidity_sweep_reclaim.py src/intelligence/trading/liquidity_hunt.py
# (Files in the list above that previously used compose_confidence must still list it; grep -L prints files MISSING it — investigate any output)

# Contract + unit tests
.venv/bin/pytest tests/unit/intelligence/test_i7_extrinsic_contract.py -v
.venv/bin/pytest tests/unit/ -q
```
</verification>

<success_criteria>
- Primary grep (known extrinsic helpers) returns zero confidence-path results
- Broadened semantic grep hits are all manually classified as capture/supporting/regime_context (no confidence-path arithmetic)
- Every plugin that previously used compose_confidence still routes final confidence through it
- Contract test proves extrinsic-only perturbation leaves confidence unchanged for every fireable Wave 0 plugin, and confidence stays in [0.0, 0.95]
- Extrinsic fields still captured in the feature-capture path (proven by test_extrinsic_still_captured_in_features_snapshot)
- All unit tests pass; ruff and black clean
- The three files shared with Waves 1-5 (ofi_continuation, gap_analysis_setup, cvd_divergence) are committed and clean — ready for intrinsic formula upgrades
</success_criteria>

<output>
After completion, create `.planning/phases/118-confidence-integrity-top5-setup-refactoring/118-00-SUMMARY.md`
</output>
</content>
</invoke>
