---
phase: 44-i7-dag-refactor
plan: 03
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - src/intelligence/confluence/confluence_weights.py
  - src/intelligence/confluence/confluence_alignment.py
  - src/intelligence/confluence/confluence_smc.py
  - src/intelligence/confluence/cross_timeframe.py
autonomous: true
requirements: [DAG-04]

must_haves:
  truths:
    - "CrossTimeframeConfluencePlugin class interface unchanged — market_analysis_service sees no difference"
    - "cross_timeframe.py is a thin orchestrator importing from 3 focused modules"
    - "All existing I6 cross-timeframe tests pass without modification"
    - "confluence_weights.py contains only pure numeric helpers (no market domain logic)"
    - "confluence_smc.py contains only SMC-specific scoring (BOS, FVG, OB)"
  artifacts:
    - path: "src/intelligence/confluence/confluence_weights.py"
      provides: "Pure numeric helpers — TF authority, recency, proximity decay"
      exports: ["_TF_MINUTES", "_sign", "_proximity_decay", "get_recency_weight", "extract_trend_sign"]
    - path: "src/intelligence/confluence/confluence_alignment.py"
      provides: "Trend/structure/regime/pattern scoring"
      exports: ["score_trend_alignment", "score_structure_alignment", "score_regime_agreement", "score_pattern_confirmation", "score_i2_events"]
    - path: "src/intelligence/confluence/confluence_smc.py"
      provides: "SMC-specific scoring — BOS, FVG, OB alignment"
      exports: ["score_smc_bos_alignment", "score_fvg_alignment", "score_ob_alignment"]
    - path: "src/intelligence/confluence/cross_timeframe.py"
      provides: "Thin orchestrator class — unchanged interface"
      contains: "class CrossTimeframeConfluencePlugin"
  key_links:
    - from: "src/intelligence/confluence/cross_timeframe.py"
      to: "confluence_weights.py, confluence_alignment.py, confluence_smc.py"
      via: "direct imports"
      pattern: "from .confluence_weights import"
    - from: "src/intelligence/confluence/confluence_smc.py"
      to: "confluence_weights.py"
      via: "imports _proximity_decay, extract_trend_sign"
      pattern: "from .confluence_weights import"
---

<objective>
Decompose `cross_timeframe.py` (464 lines) into 3 focused modules by computation stage, keeping the `CrossTimeframeConfluencePlugin` class interface intact.

Purpose: Per D-20/D-21/D-22, split the monolith into independently testable modules by computation stage: pure math (weights), market scoring (alignment), and SMC-specific logic (smc). The class stays as a thin orchestrator. Zero downstream change — `market_analysis_service.py` imports the class, not the internals.

Output: 3 new module files + reduced `cross_timeframe.py` orchestrator.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-i7-dag-refactor/44-CONTEXT.md
@.planning/phases/44-i7-dag-refactor/44-RESEARCH.md

@src/intelligence/confluence/cross_timeframe.py
@tests/unit/intelligence/test_cross_timeframe.py

<interfaces>
From the research decomposition map (verified against actual code):

confluence_weights.py — lines ~1-58 + scattered helpers:
  _TF_MINUTES: dict[str, int]    # {"1m": 1, "5m": 5, "15m": 15, "1h": 60, "4h": 240, "1d": 1440}
  _sign(x: float) -> int         # 1 if x > 0, -1 if x < 0, 0 if zero
  _proximity_decay(price, level_top, level_bottom, atr) -> float  # 1.0 within 1 ATR, linear to 0 at 3 ATR
  get_recency_weight(frames, tf) -> float
  extract_trend_sign(data) -> int

confluence_alignment.py — scoring functions:
  _I2_BULLISH_EVENTS, _I2_BEARISH_EVENTS  # constant sets
  score_trend_alignment(cur_trend, other_intel, weights) -> float
  score_structure_alignment(features, other_intel, weights) -> float
  score_regime_agreement(features, other_intel, weights) -> float
  score_pattern_confirmation(features, other_intel) -> float
  score_i2_events(features) -> float

confluence_smc.py — SMC scoring:
  score_smc_bos_alignment(features, other_intel, weights, extract_trend_sign_fn) -> float
  score_fvg_alignment(features, other_intel, current_tf, cur_trend, proximity_decay_fn) -> tuple[float, dict]
  score_ob_alignment(features, other_intel, current_tf, cur_trend, proximity_decay_fn) -> tuple[float, dict]

cross_timeframe.py (reduced) — orchestrator importing from all three.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract confluence_weights.py and confluence_alignment.py</name>
  <files>
    src/intelligence/confluence/confluence_weights.py,
    src/intelligence/confluence/confluence_alignment.py
  </files>
  <read_first>
    src/intelligence/confluence/cross_timeframe.py
  </read_first>
  <action>
    Read `cross_timeframe.py` fully. Identify and extract two groups of functions.

    **confluence_weights.py** — Pure numeric helpers with NO market domain knowledge:
    - `_TF_MINUTES` dict constant (maps timeframe strings to minute values)
    - `_sign(x)` function — returns 1, -1, or 0
    - `_proximity_decay(price, level_top, level_bottom, atr)` — returns float in [0, 1], 1.0 within 1 ATR, linear decay to 0 at 3 ATR
    - `get_recency_weight(frames, tf)` — computes recency weighting for TF data
    - `extract_trend_sign(data)` — extracts trend direction from intel data

    Copy these functions verbatim from `cross_timeframe.py`. Preserve exact logic, variable names, and docstrings. Add module docstring: "Pure numeric helpers for cross-timeframe confluence scoring. No market domain knowledge — independently testable with numeric inputs alone."

    Add necessary imports at the top (typing, numpy if used, etc.).

    **confluence_alignment.py** — Market-aware scoring functions:
    - `_I2_BULLISH_EVENTS` and `_I2_BEARISH_EVENTS` constant sets
    - `score_trend_alignment(cur_trend, other_intel, weights)` → float
    - `score_structure_alignment(features, other_intel, weights)` → float
    - `score_regime_agreement(features, other_intel, weights)` → float
    - `score_pattern_confirmation(features, other_intel)` → float
    - `score_i2_events(features)` → float

    Copy these functions verbatim. Some may reference `_sign()` or `extract_trend_sign()` — import them from `confluence_weights`:
    ```python
    from .confluence_weights import _sign, extract_trend_sign
    ```

    Add module docstring: "Trend, structure, regime, and pattern scoring functions for cross-timeframe confluence."

    **Important:** If any extracted function was a method on the `CrossTimeframeConfluencePlugin` class (using `self`), convert it to a module-level function by removing `self` and passing any instance state as explicit parameters. Verify by checking if the function uses any `self.*` attributes — if so, those must become function parameters.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && .venv/bin/python -c "from src.intelligence.confluence.confluence_weights import _TF_MINUTES, _sign, _proximity_decay, get_recency_weight, extract_trend_sign; print('weights OK')" && .venv/bin/python -c "from src.intelligence.confluence.confluence_alignment import score_trend_alignment, score_structure_alignment, score_regime_agreement, score_pattern_confirmation, score_i2_events; print('alignment OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/intelligence/confluence/confluence_weights.py exists and contains `_TF_MINUTES`, `def _sign(`, `def _proximity_decay(`, `def get_recency_weight(`, `def extract_trend_sign(`
    - src/intelligence/confluence/confluence_weights.py does NOT contain any scoring function (no `score_`)
    - src/intelligence/confluence/confluence_alignment.py exists and contains `def score_trend_alignment(`, `def score_structure_alignment(`, `def score_regime_agreement(`, `def score_pattern_confirmation(`, `def score_i2_events(`
    - Both files import successfully without errors
    - confluence_alignment.py imports from confluence_weights (not from cross_timeframe)
  </acceptance_criteria>
  <done>Two new modules created with extracted functions. All imports resolve. No logic changes — verbatim extraction.</done>
</task>

<task type="auto">
  <name>Task 2: Extract confluence_smc.py and reduce cross_timeframe.py to orchestrator</name>
  <files>
    src/intelligence/confluence/confluence_smc.py,
    src/intelligence/confluence/cross_timeframe.py
  </files>
  <read_first>
    src/intelligence/confluence/cross_timeframe.py,
    src/intelligence/confluence/confluence_weights.py,
    src/intelligence/confluence/confluence_alignment.py,
    tests/unit/intelligence/test_cross_timeframe.py
  </read_first>
  <action>
    **Step 1 — Extract confluence_smc.py:**
    Extract SMC-specific scoring functions from `cross_timeframe.py`:
    - `score_smc_bos_alignment(features, other_intel, weights, ...)` → float
    - `score_fvg_alignment(features, other_intel, current_tf, cur_trend, ...)` → tuple[float, dict]
    - `score_ob_alignment(features, other_intel, current_tf, cur_trend, ...)` → tuple[float, dict]

    These functions use `_proximity_decay` and `extract_trend_sign` from `confluence_weights.py`. Import them:
    ```python
    from .confluence_weights import _proximity_decay, extract_trend_sign, _TF_MINUTES
    ```

    Per Pitfall 3 from RESEARCH.md: `confluence_smc.py` imports directly from `confluence_weights.py`. This is the only inter-module dependency. The import graph is: `cross_timeframe.py` → all three modules; `confluence_smc.py` → `confluence_weights.py`. No circular imports.

    If any SMC scoring function was a method using `self`, convert to module-level function with explicit parameters.

    Add module docstring: "SMC-specific scoring — BOS, FVG, and Order Block alignment for cross-timeframe confluence."

    **Step 2 — Reduce cross_timeframe.py to thin orchestrator:**
    Remove all extracted functions from `cross_timeframe.py`. Replace with imports:
    ```python
    from .confluence_weights import _TF_MINUTES, get_recency_weight, extract_trend_sign, _sign, _proximity_decay
    from .confluence_alignment import score_trend_alignment, score_structure_alignment, score_regime_agreement, score_pattern_confirmation, score_i2_events
    from .confluence_smc import score_smc_bos_alignment, score_fvg_alignment, score_ob_alignment
    ```

    The `CrossTimeframeConfluencePlugin` class stays in `cross_timeframe.py`. Its `compute_full()` method now calls the imported scoring functions instead of local methods/functions. The class interface (inputs, outputs, name, tier) is unchanged.

    **Step 3 — Run existing I6 tests:**
    Per D-22, all existing tests in `test_cross_timeframe.py` must pass unchanged. The tests import `CrossTimeframeConfluencePlugin` from `cross_timeframe.py` — since the class is still there with the same interface, tests should pass without modification.

    If any test imports an internal function directly from `cross_timeframe` (e.g., `from src.intelligence.confluence.cross_timeframe import _sign`), update that import to the new module path.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_cross_timeframe.py -v</automated>
  </verify>
  <acceptance_criteria>
    - src/intelligence/confluence/confluence_smc.py exists and contains `def score_smc_bos_alignment(`, `def score_fvg_alignment(`, `def score_ob_alignment(`
    - src/intelligence/confluence/cross_timeframe.py contains `from .confluence_weights import` and `from .confluence_alignment import` and `from .confluence_smc import`
    - src/intelligence/confluence/cross_timeframe.py contains `class CrossTimeframeConfluencePlugin`
    - src/intelligence/confluence/cross_timeframe.py does NOT contain any standalone scoring function definitions (no `def score_trend_alignment`, `def _sign`, `def _proximity_decay` etc. as standalone functions — only the class and its methods remain)
    - wc -l src/intelligence/confluence/cross_timeframe.py shows significantly fewer lines than 464 (target: under 150)
    - All tests in test_cross_timeframe.py pass with exit code 0
    - .venv/bin/pytest tests/unit/intelligence/ -q --tb=no passes
  </acceptance_criteria>
  <done>cross_timeframe.py decomposed into 3 modules + thin orchestrator. Class interface unchanged. All existing I6 tests pass. No downstream service changes needed.</done>
</task>

</tasks>

<verification>
All I6 tests pass unchanged:
```bash
.venv/bin/pytest tests/unit/intelligence/test_cross_timeframe.py -v
```

Full intelligence suite still green:
```bash
.venv/bin/pytest tests/unit/intelligence/ -q --tb=no
```

Line count confirms decomposition:
```bash
wc -l src/intelligence/confluence/*.py
```
</verification>

<success_criteria>
- 3 new modules exist: confluence_weights.py, confluence_alignment.py, confluence_smc.py
- cross_timeframe.py reduced from ~464 to ~150 lines (class + imports)
- CrossTimeframeConfluencePlugin class interface unchanged
- All existing I6 tests pass without modification
- No downstream service changes
</success_criteria>

<output>
After completion, create `.planning/phases/44-i7-dag-refactor/44-03-SUMMARY.md`
</output>
