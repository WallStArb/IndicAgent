# 143.1-07 Corpus Re-run Manifest

**Status: BLOCKED — full-pipeline re-run NOT triggered.**

This plan's Task 1 requires the ONE full-pipeline corpus re-run from Step 1
(`backfill_feature_factory.py --compute-only`) that bundles Components A+B+C+D+F
together. Per the plan's own `<action>` text and hard precondition:

> Verify the Plan 01 Task 3 staged-validation gate passed (methodology-change-ledger
> shows the SUSPECT rate within the pre-committed bound) AND Plan 01 Task 4's bootstrap
> runtime budget is recorded - if either is missing, STOP and do not run.

Both halves of the precondition were checked against the live methodology-change-ledger
and the pending todo backlog before any pipeline command was issued.

## Precondition check

### 1. Component A staged-validation gate (Plan 01 Task 3) — NOT PASSED

`docs/plans/methodology-change-ledger.md` entry **E6** (2026-07-11) records:

- **Pre-committed pass threshold** (recorded before the bootstrap diagnostic was run):
  `<= 2` SUSPECT cells out of 66-72 evaluated, AND no single `(tf, is_pooled)` stratum
  with more than 1 SUSPECT cell.
- **Measured result:** `ops_ic_null_calibration.py --ci-method bootstrap` on the same
  66-cell stratified sample (29 evaluated) reported **6/29 (20.7%) SUSPECT** at the
  default `--n-permutations 200` — down from 11/29 (37.9%) under the old Fisher-z CI,
  a real improvement, but still over the pre-committed bound. `tf=5m,is_pooled=false`
  alone carries 3 SUSPECT cells against a `<=1` per-stratum bound.
- **Re-run at `--n-permutations 1000`** (5x more null-permutation precision) did not
  clear the gate either — 8/29 SUSPECT, with `tf=5m` worsening to 4. This rules out
  small-sample noise in the null benchmark as the explanation.
- **Block-size robustness sweep** (78/156/390/780, ~1 to ~10 trading days) produced an
  IDENTICAL 4/7 SUSPECT count for the 5m stratum at every block size tested, with
  `se_ratio` flat-to-worsening as block size grew. This rules out "block too short to
  capture autocorrelation" as the explanation.
- **Explicit verdict recorded in E6:** `**VERDICT: GATE NOT CLEARED.**` The residual is
  concentrated in autocorrelation/momentum-family features (`ret_autocorr_1`/VNQ,
  `ctf_momentum`/XHB+IBB, `month_sin`/EFA) at `tf=5m` specifically. Full investigation
  filed as `.planning/todos/pending/099-bootstrap-ci-staged-validation-gate-not-cleared-5m-residual.md`.
- **E6's own pre-committed consequence:** "Per this entry's own pre-committed rule:
  Plan 07's corpus-wide re-run remains BLOCKED pending either (a) resolution of the 5m
  residual, or (b) an explicit, separately-recorded project-owner decision to accept the
  residual risk and proceed anyway."

Todo 099 (still `status: pending`, filed 2026-07-11, not moved to `deferred/` or
`completed/`) restates the same "Decision needed" section verbatim and has not been
closed or annotated with a project-owner acceptance decision. `git log` against
`docs/plans/methodology-change-ledger.md` shows the most recent entry touching this
gate is E9 (Component E, 2026-07-11) — no entry recording a residual-risk acceptance
decision exists anywhere in the ledger as of this session.

**Verdict: gate (a) not passed, (b) no project-owner override recorded. Precondition
half 1 FAILS.**

### 2. Component A runtime budget (Plan 01 Task 4) — RECORDED (this half passes)

Also in E6, a separate sub-entry (2026-07-11, "Runtime budget"): bootstrap CI cost was
benchmarked at the live/unchanged APR params (`bootstrap_resamples=2000`,
`bootstrap_seed=42`, `bootstrap_block_size.{5m,15m,1h,1d}=78/26/10/10`). Central
estimate ≈16.5min parallel wall-clock at `infra.ic_engine.workers=12`;
**pre-committed acceptable runtime budget: 60 minutes wall-clock** for the bootstrap-CI
portion of a corpus-wide re-run (≈3.6x the central estimate, sized for the heavy right
tail of large POOLED cells). This half of the precondition is satisfied and recorded.

### Combined verdict

The plan's hard precondition requires **both** halves to be satisfied before triggering
Task 1's pipeline run ("if either is missing, STOP and do not run"). Half 2 (runtime
budget) is recorded; half 1 (staged-validation gate) explicitly did NOT clear and no
project-owner override exists. **Per the plan's own instruction, this executor STOPPED
and did not run `scripts/ops/corpus/ops_corpus_pipeline_run.sh` or any of its steps.**

## What was NOT done as a consequence

- No `bash scripts/ops/corpus/ops_corpus_pipeline_run.sh --from-step 1` invocation was
  made. No `backfill_status` seeding was needed since no run was attempted.
- No process check was strictly required for the halt decision, but was performed
  anyway as due diligence (see below) — confirmed clean, so this is not a "something
  else is already running" situation; it is a genuine precondition failure.
- `feature_vectors` row count at time of this check: 36,719,598 (pre-existing corpus
  state, unchanged by this plan — the 6th rebuild from 2026-07-09, per STATE.md /
  Corpus pipeline state memory). This plan did not add, modify, or truncate any rows.
- `alpha.ensemble.sign_symmetric` APR flag confirmed still `false` live in
  `config_state` (unchanged — the champion-flag-OFF concern from this plan's objective
  is moot since no ensemble training ran at all this session).
- No new `feature_vectors` canary columns, no B/C decomposition columns, no
  sign-symmetric walk-forward measurement, no F A/B corpus-wide verdict were produced
  this session, because none of the code that would produce them ran against fresh
  data. Components A/B/C/D/E/F's CODE all shipped in Plans 01-06 (confirmed via prior
  SUMMARY.md self-checks) and remain correct and committed — only the corpus re-run
  that would EXERCISE that code corpus-wide is blocked.
- Task 2 of this plan (validate D/F/B/C/sign-symmetric measurement on the live re-run
  result) has no re-run result to validate against and was not attempted.

## Due-diligence checks performed before halting (read-only, no side effects)

```
ps aux | grep -iE "corpus_pipeline|backfill_feature_factory|regime_writer|forward_return_writer|ic_engine|ensemble_trainer|alpha_publisher" | grep -v grep
  -> (no output — no concurrent corpus pipeline process running)

PGPASSWORD=postgres psql ... -c "SELECT count(*) FROM feature_vectors;"
  -> 36719598 (unchanged pre-existing corpus)

PGPASSWORD=postgres psql ... -c "SELECT config_key, config_value FROM config_state WHERE config_key='alpha.ensemble.sign_symmetric';"
  -> alpha.ensemble.sign_symmetric | false (champion eligibility unaffected, unaffected because no training ran)

git log --oneline -- docs/plans/methodology-change-ledger.md .planning/todos/pending/099-*.md
  -> most recent commits are 143.1-01 through 143.1-04's own ledger entries (E6/E7/E8/E9);
     no subsequent commit records a residual-risk acceptance decision
```

## Recommended next step

This is a decision only the project owner can make, per the plan's own text, E6's
pre-committed rule, and todo 099's "Decision needed" section (all three independently
state the same thing). Two paths forward, matching todo 099's framing exactly:

1. **Investigate and resolve the 5m residual** — todo 099's "Not yet investigated"
   section lists three concrete next steps (regime-conditional/wild-bootstrap variant
   for the flagged autocorrelation/momentum features; validating whether
   `_circular_shift_null`'s single-global-shift null is even the right ground truth for
   high-persistence features; or scoping how rare/consequential these 5 features are in
   the full ~150-feature corpus). This is genuinely new statistical work, not a
   parameter tweak — the block-size sweep already ruled out the cheap fix.
2. **Explicit, recorded risk-acceptance decision** — if the project owner judges the
   residual (3-4 SUSPECT cells out of 29, concentrated in named autocorrelation/momentum
   features at 5m, a real but bounded and now well-characterized miscalibration) an
   acceptable cost against the alternative of continuing to run the still-worse Fisher-z
   CI, that decision needs its own methodology-change-ledger entry per this ledger's own
   convention ("any change to gates... made after observing results... gets an entry
   here") — accepting a failed gate's result *is* such a change, applied to Plan 07's own
   gate rather than to the underlying IC machinery.

Once either path resolves the block, Plan 07 (or a re-spawned continuation of it) can
re-attempt Task 1's `--from-step 1` full-pipeline re-run and this manifest should be
updated (not replaced) with the actual run's step-by-step record.

## Champion flag-OFF mechanism (recorded for when the run does proceed)

Confirmed via live psql query: `alpha.ensemble.sign_symmetric` APR default is `false`.
Per this plan's interface notes, the APR default being `false` is itself sufficient to
keep the champion (Steps 7-8) on old eligibility without needing an explicit
`--no-sign-symmetric` CLI override — but the plan's acceptance criteria ask for the
mechanism to be recorded either way. **Mechanism: APR default (`false`), confirmed live,
not an explicit CLI flag.** This remains true and unaffected by this session's halt;
record it now so Plan 07's eventual re-attempt does not need to re-derive it.

---
*Plan: 143.1-07 · Status: BLOCKED, no pipeline run attempted · Recorded: 2026-07-11*
