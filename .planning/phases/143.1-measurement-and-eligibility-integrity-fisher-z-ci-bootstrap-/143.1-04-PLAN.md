---
phase: 143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap
plan: 04
type: execute
wave: 3
depends_on: ["143.1-03"]
files_modified:
  - services/ic_engine.py
  - services/ensemble_trainer.py
  - src/intelligence/ensemble/feature_selector.py
  - src/intelligence/ensemble/weights.py
  - src/intelligence/ensemble/alpha_score.py
  - docs/plans/methodology-change-ledger.md
autonomous: true
requirements: [E]
must_haves:
  truths:
    - "Walk-forward fold pass criterion is sign-consistent (fold_ic * ic_sign > 0) in ALL THREE ic_engine blocks, including _compute_cross_sectional_tf (the POOLED block that actually gates ensemble eligibility)"
    - "Ensemble eligibility (_ELIGIBILITY_BASE_WHERE) is sign-aware: (ic_sign=1 AND ic_ci_lower>0) OR (ic_sign=-1 AND ic_ci_upper<0)"
    - "compute_quality_weight preserves magnitude on the negative-IC side via ic_sign framing (NOT naive abs)"
    - "mean_variance_weights is fed sign-adjusted ic_signs*ic_shrunk and derive_weights no longer silently re-zeros contrarian features"
  artifacts:
    - path: "services/ic_engine.py"
      provides: "Sign-consistent walk-forward criterion in all 3 fold-pass blocks"
      contains: "ic_sign"
    - path: "services/ensemble_trainer.py"
      provides: "Sign-aware _ELIGIBILITY_BASE_WHERE + ic_signs threaded into resolve_stratum_weights"
      contains: "ic_sign = -1"
    - path: "src/intelligence/ensemble/feature_selector.py"
      provides: "Sign-aware compute_quality_weight"
      contains: "ic_sign"
  key_links:
    - from: "services/ensemble_trainer.py"
      to: "feature_ic_scores"
      via: "sign-aware eligibility predicate accepting ic_sign=-1 rows"
      pattern: "ic_sign = -1 AND ic_ci_upper"
    - from: "services/ensemble_trainer.py resolve_stratum_weights"
      to: "src/intelligence/ensemble/weights.py mean_variance_weights"
      via: "sign-adjusted input ic_signs * ic_shrunk"
      pattern: "ic_signs \\* ic_shrunk"
---

<objective>
Component E (todo 094, P0). Make ensemble eligibility, walk-forward validation, quality weighting, and the E2 mean-variance path sign-symmetric so contrarian (negative-IC) features can reach ensemble weighting. Root cause (Fable-reviewed, live-DB-verified): two sign-asymmetric gates exclude 100% of negative-IC features (1,527 eligible rows, zero at ic_sign=-1; alpha_events 99.99% long-only).

CRITICAL wiring precision (RESEARCH Pitfall 1): the walk-forward sign fix MUST land in `_compute_cross_sectional_tf` (~1443-1447), the only block whose output carries symbol='POOLED' - the only value `_ELIGIBILITY_BASE_WHERE` accepts. CONTEXT.md's original `ic_engine.py:812-814` citation is the per-symbol block, whose output NEVER reaches the ensemble; fixing only it would compile, run, and have ZERO effect on the long/short imbalance. Fix all three blocks for consistency (resolved item 2), but the cross-sectional one is load-bearing.

Purpose: this changes champion scoring behavior for what becomes the owner's live trading capital - shadow-mode validation (Plan 08) is mandatory before promotion. This plan lands the code + code-level tests only; the corpus re-run (Plan 07) and shadow validation (Plan 08) prove it on live data.

Output: sign-consistent walk-forward (3 blocks), sign-aware eligibility, sign-aware quality weight, E2 sign-path fix, stale-docstring/param fixes, ledger entry.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-CONTEXT.md
@.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-RESEARCH.md

<interfaces>
services/ic_engine.py walk-forward blocks (all share `wf_pass_count_nd = (fold_ic_arr > 0).sum(axis=0)`):
- ~811-814 `_compute_symbol_tf` (per-symbol; output symbol=<SYM>, NEVER 'POOLED' - diagnostic only)
- ~1040-1041 daily context-features loop
- ~1443-1447 `_compute_cross_sectional_tf` (output symbol='POOLED' - THIS gates ensemble eligibility)
Fix shape (all three): `fold_sign_agree = (fold_ic_arr * ic_sign_nd[None, :]) > 0; wf_pass_count_nd = fold_sign_agree.sum(axis=0)`. Full-sample `ic_sign` is already derived in each function (ic_engine.py:873 stores `ic_sign = 1 if ic_val>0 else -1`).

services/ensemble_trainer.py:
- `_ELIGIBILITY_BASE_WHERE` (91-96): currently `... AND ic_ci_lower > 0 AND reliable = true AND ic_sharpe_hac IS NOT NULL AND passes_walkforward = true`. `_ELIGIBILITY_WHERE` adds `AND passes_fdr = true`. `ic_sign` is a populated smallint column - no schema change for the predicate.
- `resolve_stratum_weights(...)` (197): calls `derive_weights(aged_quality_weights, ...)` and, for mean_variance, `mean_variance_weights(cov_matrix, ic_shrunk, mv_condition_max)` (252) then `derive_weights(mv_raw, ...)` (260). `ic_signs` is NOT currently a parameter (resolved item 10 - thread it in).
- inline `signed_weights = weights * ic_signs` (~875-876) is the sign-application point; `compute_alpha_score()` in alpha_score.py has ZERO callers (dead code) - sign is applied inline, not via it.

src/intelligence/ensemble/feature_selector.py:
- `compute_quality_weight(ic_ci_lower, ic_sharpe, sharpe_floor)` (31). New form: `qw = (ic_sign * nearest_ci_bound) * max(sharpe_floor, ic_sign * ic_sharpe)`. Do NOT use naive abs(ic_sharpe) (explicitly rejected - misapplies to the ~57-row decay-signature population).

src/intelligence/ensemble/weights.py:
- `derive_weights(ic_sharpes, ...)` (28): line 51 `w = np.where(... & (ic_sharpes > 0), ic_sharpes, 0.0)` - the `>0` filter that re-zeros contrarians. Docstring "features with non-positive IC Sharpe are excluded" is stale; `ic_sharpes` param name is misleading (receives derived quality weights). Fix both in the same commit.
- `mean_variance_weights(cov_matrix, ic_shrunk, condition_max)` (167): must receive sign-adjusted input and keep positive-magnitude convention.
- `cluster_deflate_weights` clusters on abs(corr) - already correct, no change.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sign-consistent walk-forward criterion in all 3 ic_engine blocks (cross-sectional is load-bearing)</name>
  <files>services/ic_engine.py, tests/unit/test_ensemble_ic_wf_stability.py</files>
  <read_first>
    - services/ic_engine.py ~811-814, ~1040-1041, ~1443-1447 (the three fold_ic > 0 blocks) and where ic_sign/ic_full is derived in each function
    - services/ic_engine.py line 143 (_CROSS_SECTIONAL_SYMBOL), 1184-1490 (_compute_cross_sectional_tf docstring confirming symbol='POOLED' output)
    - tests/unit/test_ensemble_ic_wf_stability.py (verify whether it exercises the cross-sectional block or only per-symbol - Wave 0 gap per VALIDATION.md)
    - 143.1-RESEARCH.md Pitfall 1 (CRITICAL, wrong-function failure mode) and the Gate 2 Code Examples
  </read_first>
  <behavior>
    - Test: a feature with negative full-sample ic_sign whose folds are consistently negative PASSES walk-forward (fold_ic * ic_sign > 0), under the cross-sectional path specifically.
    - Test: a feature whose folds flip sign relative to its full-sample estimate FAILS.
    - Test (coverage gap closure): test_ensemble_ic_wf_stability.py exercises `_compute_cross_sectional_tf`'s block, not only the per-symbol path.
  </behavior>
  <action>
    Change the fold-pass criterion in ALL THREE blocks from `(fold_ic_arr > 0)` to `(fold_ic_arr * ic_sign_nd[None, :]) > 0` (broadcast the already-derived per-feature full-sample sign across folds), per the Gate 2 fix shape. The cross-sectional block (~1443-1447) is load-bearing for ensemble eligibility (Pitfall 1); the per-symbol (~811-814) and context-features (~1040-1041) blocks get the identical fix for consistency and correct per-symbol diagnostic `passes_walkforward` (resolved item 2). Extend/verify tests/unit/test_ensemble_ic_wf_stability.py covers the cross-sectional path specifically. Warning sign to watch: if after the corpus re-run alpha_events short-side count is still ~0, the fix landed in the wrong function.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_ensemble_ic_wf_stability.py -v; grep -n "fold_ic_arr \* ic_sign\|ic_sign_nd\[None" services/ic_engine.py</automated>
  </verify>
  <acceptance_criteria>
    - All three `(fold_ic_arr > 0)` walk-forward blocks in ic_engine.py now multiply by the per-feature full-sample sign (grep shows the sign-broadcast in 3 places).
    - `.venv/bin/pytest tests/unit/test_ensemble_ic_wf_stability.py -v` passes, including a case that exercises the cross-sectional (POOLED) block with a negative-sign feature.
    - No bare `(fold_ic_arr > 0)` remains as the pass criterion in any of the three blocks.
  </acceptance_criteria>
  <done>Walk-forward pass criterion is sign-consistent in all three blocks, the cross-sectional POOLED block is fixed and test-covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sign-aware eligibility predicate + sign-aware compute_quality_weight</name>
  <files>services/ensemble_trainer.py, src/intelligence/ensemble/feature_selector.py, tests/unit/test_ensemble_trainer.py, tests/unit/test_ensemble_ic_math.py</files>
  <read_first>
    - services/ensemble_trainer.py 91-97 (_ELIGIBILITY_BASE_WHERE / _ELIGIBILITY_WHERE)
    - src/intelligence/ensemble/feature_selector.py 31-38 (compute_quality_weight)
    - tests/unit/test_ensemble_trainer.py (add Gate 1 sign-aware case - Wave 0 gap), tests/unit/test_ensemble_ic_math.py / test_ensemble_math.py (quality-weight cases)
    - CONTEXT.md Component E quality-weight decision + the explicit rejection of naive abs(ic_sharpe) and the ~57-row inconsistent-population note
  </read_first>
  <behavior>
    - Test: a synthetic feature_ic_scores row with ic_sign=-1 and ic_ci_upper<0 PASSES the sign-aware eligibility predicate; ic_sign=-1 with ic_ci_upper>=0 fails.
    - Test: ic_sign=1 with ic_ci_lower>0 still passes (positive side unchanged).
    - Test: compute_quality_weight for a negative-IC feature yields a positive-magnitude quality weight equal to `(ic_sign*nearest_ci_bound) * max(sharpe_floor, ic_sign*ic_sharpe)` - and a decay-signature row (positive full-sample IC, negative window sharpe) is NOT up-weighted (naive abs would up-weight it).
  </behavior>
  <action>
    In services/ensemble_trainer.py replace `ic_ci_lower > 0` inside `_ELIGIBILITY_BASE_WHERE` with `((ic_sign = 1 AND ic_ci_lower > 0) OR (ic_sign = -1 AND ic_ci_upper < 0))`. In src/intelligence/ensemble/feature_selector.py rewrite `compute_quality_weight` to take `ic_sign` and compute `qw = (ic_sign * nearest_ci_bound) * max(sharpe_floor, ic_sign * ic_sharpe)` where nearest_ci_bound is ic_ci_lower for ic_sign=1 and ic_ci_upper for ic_sign=-1 - do NOT use naive abs(ic_sharpe) (explicitly rejected). Add the diagnostic eligibility condition `ic_sign * ic_sharpe_hac > 0` if the ~57-row inconsistent population's positive IC is concentrated in an early decayed subperiod (check as part of this task; if confirmed, add the condition to exclude them without abs). Add the sign-aware test cases (Wave 0 gaps).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_ensemble_trainer.py tests/unit/test_ensemble_ic_math.py tests/unit/test_ensemble_math.py -v; grep -n "ic_sign = -1 AND ic_ci_upper" services/ensemble_trainer.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ic_sign = -1 AND ic_ci_upper < 0" services/ensemble_trainer.py` matches inside _ELIGIBILITY_BASE_WHERE.
    - `compute_quality_weight` signature includes ic_sign and does NOT call abs(ic_sharpe) (grep confirms no naive abs on ic_sharpe).
    - New unit tests prove ic_sign=-1 rows can pass eligibility and negative-IC quality weights are positive-magnitude; decay-signature rows are not up-weighted.
    - `.venv/bin/pytest tests/unit/test_ensemble_trainer.py tests/unit/test_ensemble_ic_math.py tests/unit/test_ensemble_math.py -v` passes.
  </acceptance_criteria>
  <done>Eligibility is sign-aware, quality weight preserves negative-side magnitude without naive abs, decay-signature population handled, tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: E2 sign-path fix (thread ic_signs into mean_variance) + stale docstring/param cleanup</name>
  <files>services/ensemble_trainer.py, src/intelligence/ensemble/weights.py, src/intelligence/ensemble/alpha_score.py, tests/unit/test_ensemble_mean_variance.py, tests/unit/test_ensemble_trainer_weight_method.py</files>
  <read_first>
    - services/ensemble_trainer.py resolve_stratum_weights (197-266), especially the mean_variance branch (~251-260) and the inline signed_weights = weights * ic_signs (~875-876)
    - src/intelligence/ensemble/weights.py derive_weights (28-83, the >0 filter at 51) and mean_variance_weights (167)
    - src/intelligence/ensemble/alpha_score.py (73, ic_signs multiply - confirm it is unchanged and is dead code today)
    - tests/unit/test_ensemble_mean_variance.py, test_ensemble_trainer_weight_method.py
    - CONTEXT.md Component E "E2 sign-path fix" + resolved item 10 (thread ic_signs into resolve_stratum_weights signature)
  </read_first>
  <behavior>
    - Test: mean_variance_weights fed `ic_signs * ic_shrunk` (with some ic_signs=-1) keeps contrarian features non-zero after the subsequent derive_weights call (they are NOT re-zeroed by the >0 filter).
    - Test: output weights are positive-magnitude (sign applied once at score time via the existing inline signed_weights multiply), matching E1's convention.
    - Test: the E1 path is byte-for-byte unchanged for all-positive input (no regression).
  </behavior>
  <action>
    Thread `ic_signs` into `resolve_stratum_weights`'s signature (resolved item 10 - a real, small signature change) and change the mean_variance call to `mean_variance_weights(cov_matrix, ic_signs * ic_shrunk, mv_condition_max)`, keeping mean_variance_weights output in positive-magnitude convention so the subsequent `derive_weights(mv_raw, ...)` `>0` filter (weights.py:51) does not silently re-zero contrarians - sign is applied once at score time via the existing inline `signed_weights = weights * ic_signs`. Fix weights.py `derive_weights`'s stale docstring ("features with non-positive IC Sharpe are excluded") and rename/clarify the misleading `ic_sharpes` param (it receives derived quality weights) in the same commit. Leave `alpha_score.py:73`'s ic_signs multiply unchanged (dead code, correct as-is). Add/extend the mean-variance sign-path tests (Wave 0).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_ensemble_mean_variance.py tests/unit/test_ensemble_trainer_weight_method.py -v; grep -n "ic_signs \* ic_shrunk" services/ensemble_trainer.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ic_signs \* ic_shrunk" services/ensemble_trainer.py` matches the mean_variance call.
    - `resolve_stratum_weights` signature includes ic_signs.
    - New tests prove contrarian features survive the E2 path (not re-zeroed) and the E1 path is unchanged for all-positive input.
    - weights.py derive_weights docstring no longer says "non-positive IC Sharpe are excluded"; the misleading param is renamed/clarified.
  </acceptance_criteria>
  <done>E2 mean-variance path is sign-correct end-to-end, contrarians survive derive_weights, docstring/param cleaned, tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature_ic_scores -> ensemble_trainer eligibility SQL | Sign-aware predicate; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143.1-04-V2 | Spoofing/Auth | none | accept | N/A - internal batch/ensemble code, no network surface |
| T-143.1-04-V3 | Session | none | accept | N/A |
| T-143.1-04-V4 | Access Control | none | accept | N/A |
| T-143.1-04-V5 | Input Validation | none | accept | N/A - no new APR keys or external input in this plan |
| T-143.1-04-V6 | Cryptography | none | accept | N/A |
| T-143.1-04-INT | Data/statistical integrity | sign-symmetric gates | mitigate | Pitfall-1 wrong-function risk mitigated by fixing the cross-sectional block + a cross-sectional-specific test; mandatory shadow validation (Plan 08) before any promotion |
| T-143.1-04-SC | Tampering (supply chain) | none | accept | N/A - no npm/pip/cargo installs; no new packages |
</threat_model>

<verification>
- Ledger: append a methodology-change-ledger entry recording the sign-symmetry redesign (Gates 1/2, quality weight, E2) BEFORE any promotion status changes (project pre-commitment convention).
- `.venv/bin/pytest tests/unit/test_ensemble_ic_wf_stability.py tests/unit/test_ensemble_trainer.py tests/unit/test_ensemble_ic_math.py tests/unit/test_ensemble_math.py tests/unit/test_ensemble_mean_variance.py tests/unit/test_ensemble_trainer_weight_method.py -v` green.
- Full suite `.venv/bin/pytest tests/unit/ -v` no new failures.
</verification>

<success_criteria>
- Walk-forward sign-consistent in all 3 blocks (cross-sectional load-bearing block fixed + tested).
- Eligibility sign-aware; quality weight sign-aware without naive abs; E2 sign-path fixed; docstring/param cleaned.
- Emission-threshold re-measurement flagged for Plan 07 (thresholds were calibrated on a long-only-skewed distribution).
</success_criteria>

<output>
Create `.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-04-SUMMARY.md` when done.
</output>
