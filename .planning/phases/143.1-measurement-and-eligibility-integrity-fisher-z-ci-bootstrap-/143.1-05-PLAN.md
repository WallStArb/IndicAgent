---
phase: 143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap
plan: 05
type: execute
wave: 4
depends_on: ["143.1-04"]
files_modified:
  - src/intelligence/statistics/ic_math.py
  - services/ic_engine.py
  - tests/unit/test_ic_decomposition.py
  - production/migrations/2XX_ic_decomposition_columns.sql
autonomous: true
requirements: [B]
must_haves:
  truths:
    - "feature_ic_scores gains sign_hit_rate (directional accuracy) and a magnitude-conditional IC diagnostic column, computed in the existing corpus loop"
    - "The new columns are diagnostic only - no eligibility/gate predicate changes"
    - "The decomposition kernels reuse the existing IC math (no re-derived Spearman)"
  artifacts:
    - path: "src/intelligence/statistics/ic_math.py"
      provides: "sign_hit_rate + magnitude-conditional IC kernels"
      contains: "def "
    - path: "production/migrations/2XX_ic_decomposition_columns.sql"
      provides: "feature_ic_scores.sign_hit_rate + magnitude-conditional IC columns"
      contains: "sign_hit_rate"
  key_links:
    - from: "services/ic_engine.py"
      to: "src/intelligence/statistics/ic_math.py"
      via: "call sign_hit_rate / magnitude-conditional IC kernels in the cross-sectional loop"
      pattern: "sign_hit_rate"
---

<objective>
Component B (todo 090, medium priority). Add two diagnostic columns to `feature_ic_scores` - `sign_hit_rate` (fraction of bars where prediction sign matches realized return sign) and IC conditional on large `|prediction|` (magnitude alignment) - computed as cheap kernel additions inside the existing corpus IC computation. No gate change. Sharpens Phase 143's decay monitors by decomposing IC into directional-accuracy vs magnitude-alignment components.

Purpose: batched into the same corpus re-run per the project's batching doctrine (running a separate re-run for a diagnostic-only column burns a rerun cycle). Depends on Plan 04 because it shares `ic_engine.py`/`ic_math.py` (file ownership serialization), and it wants the corrected sign-aware corpus.

Output: two new columns, kernels in ic_math.py, write-path wiring in ic_engine.py, migration, unit coverage.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-CONTEXT.md
@.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-RESEARCH.md

<interfaces>
From src/intelligence/statistics/ic_math.py:
- `_vectorized_ic(ranks_X, ranks_Y)` (79) - reuse for any IC recomputation; do NOT re-derive Spearman via scipy.stats.spearmanr (RESEARCH Don't Hand-Roll).
- `apply_bh_fdr(...)` (211) - reuse if any new column needs FDR; do not add a 4th multipletests implementation.

From services/ic_engine.py:
- `_compute_cross_sectional_tf` (1184) writes feature_ic_scores rows; the X_raw / return arrays for computing hit-rate and magnitude-conditional IC are already in scope in the corpus loop.
- feature_ic_scores currently has 37 columns, no sign_hit_rate / magnitude-conditional column (verified).
- Never log per-cell inside the corpus loop (accumulate + report once).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sign_hit_rate + magnitude-conditional IC kernels in ic_math.py</name>
  <files>src/intelligence/statistics/ic_math.py, tests/unit/test_ic_decomposition.py</files>
  <read_first>
    - src/intelligence/statistics/ic_math.py (_vectorized_ic, _p_values_from_ic, existing kernel style)
    - tests/unit/test_ensemble_ic_math.py (house style for ic_math kernel tests)
    - CONTEXT.md Component B definition (sign_hit_rate = directional accuracy / sign-agreement fraction; magnitude-conditional IC = IC conditional on large |prediction|)
  </read_first>
  <behavior>
    - Test: sign_hit_rate on a synthetic array where prediction and realized signs agree in 75% of bars returns ~0.75.
    - Test: magnitude-conditional IC computed on the top-|prediction| subset differs from full-sample IC on a fixture where the tail carries stronger alignment.
    - Test: kernels handle NaN/degenerate input without raising (return None/NaN sentinel per existing convention).
  </behavior>
  <action>
    Add two kernels to src/intelligence/statistics/ic_math.py: `sign_hit_rate` (fraction of bars where sign(prediction) == sign(realized), NaN-safe) and a magnitude-conditional IC (IC recomputed over the subset with |prediction| above a percentile threshold, reusing `_vectorized_ic` on the subset - never re-derive Spearman). Vectorized, consistent with the existing kernel style. Write tests/unit/test_ic_decomposition.py FIRST.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_ic_decomposition.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Both kernels exist in ic_math.py and reuse `_vectorized_ic` (no scipy.stats.spearmanr re-derivation).
    - `.venv/bin/pytest tests/unit/test_ic_decomposition.py -v` passes including the NaN/degenerate case.
  </acceptance_criteria>
  <done>sign_hit_rate and magnitude-conditional IC kernels exist, reuse existing IC math, and are unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the decomposition columns into ic_engine's write path + migration</name>
  <files>services/ic_engine.py, production/migrations/2XX_ic_decomposition_columns.sql</files>
  <read_first>
    - services/ic_engine.py _compute_cross_sectional_tf write-path / feature_ic_scores INSERT (the row dict construction ~1471-1490)
    - production/migrations/217_drop_dead_feature_ic_scores_columns.sql (recent feature_ic_scores column-change shape to imitate)
    - CLAUDE.md "never log per-row inside a corpus loop"
  </read_first>
  <action>
    Compute `sign_hit_rate` and the magnitude-conditional IC per cell in `_compute_cross_sectional_tf` using the Task 1 kernels on arrays already in scope, and add them to the feature_ic_scores row dict / INSERT. Add migration `2XX_ic_decomposition_columns.sql` (VERIFY next free number by listing production/migrations/ at execution time - planning tip is 220; Plans 01/02 in wave 1 claim 221/222, Plan 04 in wave 3 also claims a number in this range before this plan's wave 4 runs - do not assume 223, list the directory and take the actual next-free number; todo 095 collision risk) with `ALTER TABLE feature_ic_scores ADD COLUMN sign_hit_rate ...` and the magnitude-conditional IC column. Diagnostic only - do NOT reference these columns in any eligibility/gate predicate. Accumulate/report once, no per-cell logging.
  </action>
  <verify>
    <automated>grep -c "sign_hit_rate" production/migrations/2*_ic_decomposition_columns.sql services/ic_engine.py; .venv/bin/pytest tests/unit/test_ensemble_ic_pooled_dispatch.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Migration adds sign_hit_rate + magnitude-conditional IC columns to feature_ic_scores (verify via `production/migrations/2*_ic_decomposition_columns.sql`, filename number determined at execution time).
    - ic_engine.py computes and writes both columns in the cross-sectional loop.
    - No eligibility/gate predicate references the new columns (grep ensemble_trainer.py shows no new column in _ELIGIBILITY_*).
    - `.venv/bin/pytest tests/unit/test_ensemble_ic_pooled_dispatch.py -v` passes (write-path wiring intact).
  </acceptance_criteria>
  <done>Both diagnostic columns are computed and persisted in the corpus loop, migration adds them, and no gate consumes them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature_vectors/forward_returns -> ic_engine read | Existing read path; diagnostic columns only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143.1-05-V2 | Spoofing/Auth | none | accept | N/A - internal batch measurement code, no network surface |
| T-143.1-05-V3 | Session | none | accept | N/A |
| T-143.1-05-V4 | Access Control | none | accept | N/A |
| T-143.1-05-V5 | Input Validation | none | accept | N/A - no new APR keys or external input |
| T-143.1-05-V6 | Cryptography | none | accept | N/A |
| T-143.1-05-INT | Data/statistical integrity | decomposition kernels | mitigate | reuse existing _vectorized_ic (no numeric drift from a re-derivation); diagnostic-only, cannot affect a gate |
| T-143.1-05-SC | Tampering (supply chain) | none | accept | N/A - no npm/pip/cargo installs; no new packages |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_ic_decomposition.py tests/unit/test_ensemble_ic_pooled_dispatch.py -v` green.
- Full suite no new failures.
</verification>

<success_criteria>
- feature_ic_scores has sign_hit_rate + magnitude-conditional IC columns computed in the corpus loop, reusing existing IC math, with no gate impact.
</success_criteria>

<output>
Create `.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-05-SUMMARY.md` when done.
</output>
