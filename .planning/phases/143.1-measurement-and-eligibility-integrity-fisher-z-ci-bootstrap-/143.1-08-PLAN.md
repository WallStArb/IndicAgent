---
phase: 143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap
plan: 08
type: execute
wave: 7
depends_on: ["143.1-07"]
files_modified:
  - docs/plans/methodology-change-ledger.md
  - docs/plans/SHADOW-REVIEW.md
  - .planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-08-SHADOW-VALIDATION.md
autonomous: true
requirements: [E]
must_haves:
  truths:
    - "Component E's sign-symmetric change is validated in shadow mode via a challenger weight_version scored in parallel with the champion (no promotion without evidence)"
    - "The FRAME-04 decision rule is PRE-COMMITTED before the full-corpus result is seen (goalposts cannot move after the fact)"
    - "SHADOW-REVIEW.md's 5 frozen criteria are evaluated filtered by weight_epoch (champion vs challenger)"
    - "The E1-vs-E2 A/B judgment is re-run on the now sign-symmetric universe (the prior 20/20 all-long-vs-all-long result does not carry forward)"
    - "alpha_events emission volume is re-measured post-fix (thresholds were calibrated on a long-only-skewed distribution)"
  artifacts:
    - path: ".planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-08-SHADOW-VALIDATION.md"
      provides: "Shadow-mode plan: challenger tag, pre-committed FRAME-04 rule, champion-vs-challenger criteria eval, E1/E2 re-run result, emission-volume delta"
      min_lines: 30
  key_links:
    - from: "ensemble_trainer.py --weight-version <challenger>"
      to: "alpha_publisher.py --weight-version <challenger>"
      via: "weight_version-scoped challenger scoring stream"
      pattern: "--weight-version"
    - from: "alpha_frames.weight_epoch"
      to: "SHADOW-REVIEW.md 5 frozen criteria"
      via: "champion vs challenger evaluation filtered by weight_epoch"
      pattern: "weight_epoch"
---

<objective>
Component E, shadow validation (mandatory, not optional). The sign-symmetric change (Plan 04) alters champion scoring behavior, eligibility membership, and score distribution simultaneously - for what becomes the owner's live trading capital. Validate it in shadow mode BEFORE any promotion: train a challenger under a new `weight_version`, score in parallel with the champion, backfill frames, and evaluate SHADOW-REVIEW.md's 5 frozen criteria filtered by `weight_epoch`. Promote only on evidence.

Pre-commitment (carried from the Fable review as a material gap to close): the FRAME-04 decision rule for the full-corpus result MUST be written down BEFORE the result is seen - deciding after seeing the number is how goalposts quietly move.

Re-run the E1-vs-E2 A/B judgment: the prior 20/20 E1-win result was produced on an all-long-vs-all-long input where E2's sign-symmetric distinguishing capability was structurally unexercisable. It does not carry forward and must not be cited as settled once the universe becomes genuinely sign-symmetric.

Uses the EXISTING `weight_version` scoping mechanism end-to-end (RESEARCH Pattern) - no new schema, no new script. Runs on top of Plan 07's completed corpus re-run.

Output: a completed shadow-validation cycle with a pre-committed decision rule, champion-vs-challenger criteria evaluation, the E1/E2 re-run result, and the re-measured emission volume.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-CONTEXT.md
@.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-RESEARCH.md
@docs/plans/SHADOW-REVIEW.md

<interfaces>
weight_version scoping (verified end-to-end, no new schema):
- Champion: `ensemble_trainer.py --weight-version <champion-tag>` -> `alpha_publisher.py --weight-version <champion-tag>`.
- Challenger (sign-fixed E1, and E2): `ensemble_trainer.py --weight-version <challenger-tag>` -> `alpha_publisher.py --weight-version <challenger-tag>` (any string; supersedes APR default `alpha.ensemble.weight_version`, default 'v1').
- `AlphaFrameWriter --backfill` and `CounterfactualTracker --backfill --evaluate-gate` need NO flag - anti-join on (event_id, bar_ts, frame_variant='primary'); event_id embeds weight_version so champion/challenger get distinct alpha_frames rows, distinguishable via `alpha_frames.weight_epoch`.
- Evaluate SHADOW-REVIEW.md's 5 frozen criteria `WHERE weight_epoch = '<challenger-tag>'` vs `WHERE weight_epoch = '<champion-tag>'`.
- ic_engine.py has NO weight_version axis - Plan 07's corpus run is the single canonical input both champion and challenger train from.

E1/E2 A/B judgment: `ops_ensemble_weight_compare.py` (D-10 win gate: challenger.ic_ci_lower > champion.ic_ci_upper AND challenger.walk_forward_stable, per-stratum). E1 = `v1_shrunk` (ops_ic_shrinkage.py), E2 = `v2_mv` (mean-variance). Prior A/B: E2 rejected 20/20 - does NOT carry forward.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-commit the FRAME-04 decision rule, train challenger weight_version, score + backfill frames in parallel with champion</name>
  <files>.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-08-SHADOW-VALIDATION.md, docs/plans/methodology-change-ledger.md</files>
  <read_first>
    - docs/plans/SHADOW-REVIEW.md (the 5 frozen criteria + FRAME-04 evaluation)
    - services/ensemble_trainer.py (--weight-version override, _effective_weight_version), services/alpha_publisher.py (--weight-version, event_id composition)
    - services/alpha_frame_writer.py + counterfactual_tracker (backfill anti-join, weight_epoch)
    - CONTEXT.md Component E shadow-mode + the FRAME-04 pre-commitment note (decide the rule before seeing the number)
  </read_first>
  <action>
    FIRST, write the pre-committed FRAME-04 decision rule into 143.1-08-SHADOW-VALIDATION.md and the methodology-change-ledger - the exact promote/hold criteria and thresholds - BEFORE running anything that produces the full-corpus result (goalpost integrity). Then, on top of Plan 07's corpus re-run: train the champion `ensemble_trainer.py --weight-version <champion-tag>` -> `alpha_publisher.py --weight-version <champion-tag>` and the sign-fixed challenger `ensemble_trainer.py --weight-version <challenger-tag>` -> `alpha_publisher.py --weight-version <challenger-tag>`. Run `AlphaFrameWriter --backfill` then `CounterfactualTracker --backfill --evaluate-gate` (no flag - both auto-detect new events via the anti-join; champion and challenger get distinct alpha_frames rows via distinct event_ids / weight_epoch). Record the challenger tag, champion tag, and run commands.
  </action>
  <verify>
    <automated>grep -c "FRAME-04\|weight_epoch\|weight-version" .planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-08-SHADOW-VALIDATION.md</automated>
    <human-check>The FRAME-04 decision rule is recorded BEFORE the result; champion and challenger weight_versions are both trained/scored; alpha_frames carries distinct champion vs challenger rows (distinct weight_epoch).</human-check>
  </verify>
  <acceptance_criteria>
    - The pre-committed FRAME-04 decision rule is written down before any result-producing run (timestamped ahead in the ledger + SHADOW-VALIDATION.md).
    - Champion and challenger weight_version runs both completed (ensemble_trainer -> alpha_publisher for each tag).
    - alpha_frames has distinct rows for champion vs challenger weight_epoch (psql count both non-zero).
  </acceptance_criteria>
  <done>FRAME-04 rule pre-committed; champion + challenger scored in parallel via weight_version; frames backfilled with distinguishable weight_epoch.</done>
</task>

<task type="auto">
  <name>Task 2: Evaluate the 5 frozen criteria (champion vs challenger), re-run E1-vs-E2 A/B, re-measure emission volume</name>
  <files>.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-08-SHADOW-VALIDATION.md, docs/plans/methodology-change-ledger.md, docs/plans/SHADOW-REVIEW.md</files>
  <read_first>
    - docs/plans/SHADOW-REVIEW.md (5 frozen promotion criteria)
    - scripts/ops/alpha/ops_ensemble_weight_compare.py (D-10 win-decision gate) - the E1/E2 A/B judge
    - CONTEXT.md Component E "re-run E1-vs-E2 A/B" + emission-threshold re-measurement note
    - the emission thresholds alpha.quant.threshold.{tf} (calibrated on long-only-skewed distribution)
  </read_first>
  <action>
    Evaluate SHADOW-REVIEW.md's 5 frozen criteria filtered `WHERE weight_epoch = '<challenger-tag>'` vs `'<champion-tag>'`, and apply the PRE-COMMITTED FRAME-04 rule to reach a promote/hold decision - do not adjust the rule after seeing the number. Re-run the E1-vs-E2 A/B judgment via ops_ensemble_weight_compare.py on the now sign-symmetric universe and record the result (explicitly noting the prior 20/20 all-long result does not carry forward). Re-measure alpha_events emission volume post-fix vs the pre-fix 99.99% long-only / 11.81M-long baseline and record the long/short split and total-volume delta (thresholds were calibrated on the old skewed distribution - do not assume they still mean the same thing). Record everything in 143.1-08-SHADOW-VALIDATION.md and append the promote/hold decision + E1/E2 result to the methodology-change-ledger. Promotion is evidence-gated; if the pre-committed rule is not met, HOLD (do not promote the challenger to champion).
  </action>
  <verify>
    <automated>grep -c "criteria\|E1\|E2\|long\|short\|emission\|promote\|hold" .planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-08-SHADOW-VALIDATION.md</automated>
    <human-check>5 frozen criteria evaluated champion-vs-challenger; pre-committed FRAME-04 rule applied for the promote/hold call; E1/E2 A/B re-run recorded on the sign-symmetric universe; emission long/short split re-measured vs the 99.99%-long baseline.</human-check>
  </verify>
  <acceptance_criteria>
    - The 5 frozen criteria are evaluated for both weight_epochs and the pre-committed FRAME-04 rule is applied to a promote/hold decision (rule unchanged from Task 1).
    - The E1-vs-E2 A/B judgment is re-run on the sign-symmetric universe; result recorded with the note that the prior 20/20 does not carry forward.
    - Post-fix alpha_events long/short split + total-volume delta recorded vs the pre-fix 99.99%-long baseline.
    - The promote/hold decision and E1/E2 result are appended to the methodology-change-ledger.
  </acceptance_criteria>
  <done>Shadow validation complete: criteria evaluated, pre-committed rule applied, E1/E2 re-judged on a sign-symmetric universe, emission volume re-measured, decision recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| weight_version-scoped tables -> shadow evaluation | Champion/challenger coexist by design; internal, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143.1-08-V2 | Spoofing/Auth | none | accept | N/A - internal batch scoring, no network surface |
| T-143.1-08-V3 | Session | none | accept | N/A |
| T-143.1-08-V4 | Access Control | none | accept | N/A |
| T-143.1-08-V5 | Input Validation | none | accept | N/A - no new APR keys or external input |
| T-143.1-08-V6 | Cryptography | none | accept | N/A |
| T-143.1-08-INT | Data/statistical integrity | shadow promotion | mitigate | mandatory shadow-mode + pre-committed FRAME-04 rule prevents an unproven scoring change reaching live trading capital; promotion is evidence-gated (HOLD on failure) |
| T-143.1-08-SC | Tampering (supply chain) | none | accept | N/A - no npm/pip/cargo installs; no new packages |
</threat_model>

<verification>
- FRAME-04 decision rule recorded before any result-producing run.
- Champion + challenger weight_versions scored; frames backfilled; 5 criteria evaluated by weight_epoch.
- E1/E2 A/B re-run on sign-symmetric universe; emission volume re-measured.
- Promote/hold decision + all results recorded in SHADOW-VALIDATION.md and the methodology-change-ledger.
</verification>

<success_criteria>
- Component E's sign-symmetric change is shadow-validated with a pre-committed decision rule; promotion is evidence-gated.
- E1-vs-E2 A/B re-judged on the sign-symmetric universe (prior 20/20 explicitly retired); emission long/short split re-measured.
</success_criteria>

<output>
Create `.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-08-SUMMARY.md` when done.
</output>
