---
phase: 143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/statistics/ic_math.py
  - services/ic_engine.py
  - tests/unit/test_bootstrap_ic.py
  - scripts/ops/alpha/ops_ic_null_calibration.py
  - production/migrations/221_reactivate_bootstrap_ci_apr_keys.sql
  - docs/plans/methodology-change-ledger.md
autonomous: true
requirements: [A]
must_haves:
  truths:
    - "ic_engine.py computes production IC CIs via a corrected circular block bootstrap, not _fisher_z_ci, at all 3 of its call sites"
    - "The corrected bootstrap re-ranks the resampled subset per iteration on RAW (X, Y) observations (does not resample pre-ranked values)"
    - "The staged-validation diagnostic shows a near-zero SUSPECT rate on the 66-72 cell sample before any corpus-wide re-run is triggered"
    - "The 6 alpha.ic.bootstrap_* APR keys have a real reader and no longer carry the [deprecated] prefix"
  artifacts:
    - path: "src/intelligence/statistics/ic_math.py"
      provides: "_circular_block_bootstrap_ic (raw-input, per-iteration re-rank, memory-safe per-iteration allocation)"
      contains: "def _circular_block_bootstrap_ic"
    - path: "tests/unit/test_bootstrap_ic.py"
      provides: "RED-first unit coverage for the corrected bootstrap"
      min_lines: 40
    - path: "production/migrations/221_reactivate_bootstrap_ci_apr_keys.sql"
      provides: "Strip [deprecated] prefix from the 6 alpha.ic.bootstrap_* config_schema rows"
      contains: "REPLACE(description, '[deprecated] ', '')"
  key_links:
    - from: "services/ic_engine.py"
      to: "src/intelligence/statistics/ic_math.py"
      via: "_circular_block_bootstrap_ic call at the 3 former _fisher_z_ci sites"
      pattern: "_circular_block_bootstrap_ic"
    - from: "services/ic_engine.py"
      to: "config_state (APR)"
      via: "ConfigService.get_sync for bootstrap_seed / bootstrap_resamples / bootstrap_block_size.{tf}"
      pattern: "alpha\\.ic\\.bootstrap_"
---

<objective>
Component A (todo 091, P0). Replace the empirically-miscalibrated analytic Fisher-z CI (`_fisher_z_ci`, 38% SUSPECT rate across 4/8 strata per the 2026-07-09 diagnostic) with a correctly-implemented circular block bootstrap as the production IC confidence interval, at all three `ic_engine.py` call sites, and prove it is calibrated on the existing 66-72 cell diagnostic sample BEFORE any corpus-wide re-run is allowed to start.

Purpose: `_fisher_z_ci` gates BH-FDR pass/fail, walk-forward validation, and the EIC-04 hard gate. A miscalibrated CI silently corrupts every downstream eligibility decision. First in the locked A -> F -> E sequence because Components F and E both read/write the same `ic_ci_lower`/`ic_ci_upper` values this fix produces.

Output: corrected bootstrap in Ring 1 `ic_math.py`, three re-wired `ic_engine.py` call sites, reactivated APR keys, a new unit test, a passing staged-validation gate. This plan does NOT trigger the corpus re-run (Plan 07 does).
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-CONTEXT.md
@.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-RESEARCH.md

<interfaces>
From src/intelligence/statistics/ic_math.py (current):
- `_fisher_z_ci(ic_vector, n) -> tuple[ndarray, ndarray]` (line 50) - the analytic CI to be superseded. Cannot be swapped same-signature; a bootstrap needs raw paired (X, Y), not point-estimate + N (RESEARCH Pitfall 2).
- `_vectorized_ic(ranks_X, ranks_Y) -> np.ndarray` (line 79) - Pearson-on-ranks kernel; reuse, do not re-derive.
- `_circular_shift_null(...)` (line 99) - a NULL generator; reuse only its circular-wrap mechanic, NOT the function (RESEARCH Pitfall 4).

Removed reference implementation (restore-and-fix): `git show c6f5056b^:services/ic_engine.py` lines ~344-382 - correct memory-safe per-iteration allocation AND the pre-ranking bug to fix.

ic_engine.py call sites (verified): 772 (`_compute_symbol_tf`, raw `X_sub_nd[valid_mask]`/`Y_scale` in scope), 1009 (context-features loop, `x_daily`/`y_daily`), 1418 (`_compute_cross_sectional_tf`, `X_sub_nd[valid_mask]`/`Y_scale`).

Existing APR defaults (verify still sensible): `alpha.ic.bootstrap_resamples`=2000, `alpha.ic.bootstrap_block_size.{5m,15m,1h,1d}`=78/26/10/10, `alpha.ic.bootstrap_seed` present. All 6 currently carry a `[deprecated]` prefix (migration 177).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED then GREEN - corrected-bootstrap unit test, then restore-and-fix _circular_block_bootstrap_ic in ic_math.py</name>
  <files>tests/unit/test_bootstrap_ic.py, src/intelligence/statistics/ic_math.py</files>
  <read_first>
    - src/intelligence/statistics/ic_math.py (_fisher_z_ci, _vectorized_ic, _circular_shift_null in full)
    - tests/unit/test_fisher_z_ci.py (house style for CI tests)
    - Output of `git show c6f5056b^:services/ic_engine.py` (removed _circular_block_bootstrap_ic - correct index construction + per-iteration allocation, WITH the pre-ranking bug)
    - 143.1-RESEARCH.md Pitfalls 3, 4, 5 and Code Examples section
  </read_first>
  <behavior>
    - Test: resample+re-rank a subset yields DIFFERENT IC than resampling pre-ranked values, on a synthetic AR(1) fixture where a resampled block changes local rank order (guards against the pre-ranking bug).
    - Test: CI width shrinks as N grows.
    - Test: determinism - same seed produces identical bounds across two calls.
    - Test: circular-wrap block indices near the boundary stay in-range (max index less than n) and do not raise.
  </behavior>
  <action>
    Write tests/unit/test_bootstrap_ic.py FIRST with the four behaviors above; run it, confirm it fails (function absent). Then add `_circular_block_bootstrap_ic(X_raw, Y_raw, block_size, n_boot, rng)` to src/intelligence/statistics/ic_math.py, restoring the removed version's circular-block index construction (`starts = rng.integers(0, n, size=n_blocks); idx = (starts[:, None] + offsets).ravel()[:n] % n`) and its per-iteration allocation loop (`for b in range(n_boot): boot_ics[b] = ...`, NOT a single `(n_boot, n_blocks, block_size)` broadcast which OOMs at ~7.5GB/worker per Pitfall 5). Fix the pre-ranking bug: accept RAW unranked paired arrays, re-rank the resampled subset every iteration via `scipy.stats.rankdata(X_raw[idx], axis=0)` / `rankdata(Y_raw[idx])` before calling existing `_vectorized_ic`, then take the 2.5/97.5 percentile CI. Do NOT call `_circular_shift_null` in a loop (Pitfall 4). Leave `_fisher_z_ci` unchanged for the diagnostic/other-file callers this plan does not touch.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_bootstrap_ic.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _circular_block_bootstrap_ic" src/intelligence/statistics/ic_math.py` returns 1.
    - `grep -n "rankdata" src/intelligence/statistics/ic_math.py` shows re-ranking inside the bootstrap loop.
    - `.venv/bin/pytest tests/unit/test_bootstrap_ic.py -v` passes all four tests.
    - The pre-rank-vs-re-rank divergence test asserts the two paths differ on the AR(1) fixture.
    - No `(n_boot, n_blocks, block_size)` single-broadcast allocation exists (per-iteration loop only).
  </acceptance_criteria>
  <done>Corrected bootstrap exists in ic_math.py, re-ranks raw resampled subsets per iteration, allocates per-iteration, dedicated unit test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Re-wire ic_engine.py's 3 CI call sites to the bootstrap; reactivate APR keys; worker-safe RNG</name>
  <files>services/ic_engine.py, production/migrations/221_reactivate_bootstrap_ci_apr_keys.sql</files>
  <read_first>
    - services/ic_engine.py lines 475-820, 990-1020, 1184-1490 (confirm raw paired arrays in scope at 772, 1009, 1418)
    - services/ic_engine.py APR/config-prewarm section (existing ConfigService.get_sync usage for alpha.ic.* keys)
    - CLAUDE.md "ProcessPoolExecutor workers are compute-only" + APR mandate
    - Removed `_derive_worker_rng_seed(symbol, bootstrap_seed)` pattern (git show c6f5056b^:services/ic_engine.py)
  </read_first>
  <action>
    Replace `_fisher_z_ci(ic_vector*, n_valid)` at services/ic_engine.py lines 772, 1009, 1418 with `_circular_block_bootstrap_ic(...)`, passing the RAW pre-rank arrays already in scope (`X_sub_nd[valid_mask]`/`Y_scale` at 772 and 1418; `x_daily`/`y_daily` at 1009). Genuine signature change at every site (Pitfall 2); update all three in one commit, no site left on the old two-arg form. Read `alpha.ic.bootstrap_resamples`, `alpha.ic.bootstrap_block_size.{tf}`, `alpha.ic.bootstrap_seed` via existing ConfigService.get_sync (never hardcode). Derive per-worker RNG seed deterministically from `bootstrap_seed` + the worker's symbol/tf key so workers stay compute-only (no worker DB write). Add migration 221 (VERIFY next free number first by listing production/migrations/ at execution time - tip is 220 as of planning; todo 095 tracks a directory-split collision risk. This plan runs in parallel with 143.1-02 in the same wave, which is instructed to claim the number immediately after this one - if the verified next-free number is not 221, claim the LOWER of the next two available numbers and leave the following one for 143.1-02) running `UPDATE config_schema SET description = REPLACE(description, '[deprecated] ', '') WHERE config_key IN ('alpha.ic.bootstrap_seed','alpha.ic.bootstrap_resamples','alpha.ic.bootstrap_block_size.5m','alpha.ic.bootstrap_block_size.15m','alpha.ic.bootstrap_block_size.1h','alpha.ic.bootstrap_block_size.1d')`.
  </action>
  <verify>
    <automated>test $(grep -c "_circular_block_bootstrap_ic" services/ic_engine.py) -ge 3 && echo OK; grep -n "_fisher_z_ci(" services/ic_engine.py | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_circular_block_bootstrap_ic" services/ic_engine.py` returns 3 or more.
    - `grep -n "_fisher_z_ci(" services/ic_engine.py | grep -v '^#'` returns zero remaining CALL sites.
    - `grep -c "REPLACE(description" production/migrations/221_*.sql` returns 1.
    - No numeric bootstrap resample/block-size/seed literal hardcoded in ic_engine.py.
    - `.venv/bin/pytest tests/unit/test_ensemble_ic_bh_fdr.py tests/unit/test_ensemble_ic_gate.py -v` passes.
  </acceptance_criteria>
  <done>All 3 ic_engine.py CI call sites use the bootstrap with APR-driven params and worker-safe RNG; migration 221 reactivates the 6 APR keys.</done>
</task>

<task type="auto">
  <name>Task 3: Staged-validation gate - prove the bootstrap CI is calibrated on the 66-72 cell diagnostic; record ledger decision</name>
  <files>scripts/ops/alpha/ops_ic_null_calibration.py, docs/plans/methodology-change-ledger.md</files>
  <read_first>
    - scripts/ops/alpha/ops_ic_null_calibration.py (full - _parse_args, --se-ratio-suspect-threshold, SUSPECT verdict printout ~304-331)
    - docs/plans/2026-07-09-ic-null-calibration-design.md
    - docs/plans/methodology-change-ledger.md (append-a-decision convention)
  </read_first>
  <action>
    Add a mode/flag (or minimal branch) to the diagnostic so the same 66-72 cell stratified sample is evaluated with the corrected `_circular_block_bootstrap_ic` instead of `_fisher_z_ci`, then run it and confirm the SUSPECT rate drops from 38% toward zero. This is a HARD sequencing gate: Plan 07's corpus-wide re-run must not start until this passes. Append a methodology-change-ledger entry recording: the Fisher-z-to-bootstrap decision, before (38% SUSPECT) and after (measured) rates, APR defaults used, and the explicit scope boundary that `services/ensemble_ic_engine.py:777` (EIC-04 CI) and `scripts/ops/corpus/ops_oos_holdout_eval.py:213` intentionally stay on Fisher-z this phase (deferred per RESEARCH resolved item 3, consistent with EIC-04's PASS-verdict reconsideration deferred to the project owner). Never log per-cell inside the loop - accumulate and report once (already the script's style).
  </action>
  <verify>
    <automated>.venv/bin/python scripts/ops/alpha/ops_ic_null_calibration.py --help; grep -c "_circular_block_bootstrap_ic" scripts/ops/alpha/ops_ic_null_calibration.py</automated>
    <human-check>Diagnostic re-run with corrected bootstrap shows near-zero SUSPECT rate on the 66-72 cell sample (down from 38%); this gate must pass before Plan 07's corpus re-run is triggered.</human-check>
  </verify>
  <acceptance_criteria>
    - The diagnostic can compute CIs via `_circular_block_bootstrap_ic` (grep match in the script).
    - A fresh run prints a SUSPECT count near zero on the same stratified sample that previously reported 38%.
    - `docs/plans/methodology-change-ledger.md` has a new entry naming Component A, the before/after SUSPECT rates, and the ensemble_ic_engine/oos-holdout deferral scope boundary.
  </acceptance_criteria>
  <done>Bootstrap CI is empirically calibrated on the diagnostic sample, the staged gate passes, and the decision + scope boundary are recorded in the methodology-change-ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config_state (APR) -> ic_engine reads | Reactivated bootstrap APR keys read at runtime; values already bounded by config_schema min/max |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143.1-01-V2 | Spoofing/Auth | none | accept | N/A - internal batch measurement code, no network endpoint, no auth surface |
| T-143.1-01-V3 | Session | none | accept | N/A |
| T-143.1-01-V4 | Access Control | none | accept | N/A |
| T-143.1-01-V5 | Input Validation | reactivated APR keys | mitigate | bootstrap_seed/resamples/block_size read through existing ConfigService/config_schema bounds validation; no new validation code needed |
| T-143.1-01-V6 | Cryptography | none | accept | N/A |
| T-143.1-01-INT | Data/statistical integrity | bootstrap CI | mitigate | corrected re-ranking + staged-validation gate (Task 3) proves calibration before any downstream gate consumes the CI |
| T-143.1-01-SC | Tampering (supply chain) | none | accept | N/A - no npm/pip/cargo installs in this plan; no new packages |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_bootstrap_ic.py tests/unit/test_ensemble_ic_bh_fdr.py tests/unit/test_ensemble_ic_gate.py -v` green.
- Full suite `.venv/bin/pytest tests/unit/ -v` shows no new failures beyond the known pre-existing `test_no_smooth_or_backward_in_factory`.
- Staged-validation diagnostic SUSPECT rate near-zero (hard gate for Plan 07).
</verification>

<success_criteria>
- All 3 ic_engine.py CI call sites use the corrected circular block bootstrap; zero remaining `_fisher_z_ci(` call sites in ic_engine.py.
- The 6 alpha.ic.bootstrap_* APR keys have a live reader and no `[deprecated]` prefix.
- Staged-validation gate passes; methodology-change-ledger records the decision and scope boundary.
</success_criteria>

<output>
Create `.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-01-SUMMARY.md` when done.
</output>
