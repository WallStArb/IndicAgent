---
phase: 143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap
plan: 07
subsystem: alpha-ensemble
tags: [corpus-pipeline, ic-engine, blocked, staged-validation-gate]

# Dependency graph
requires:
  - phase: 143.1-01
    provides: circular block bootstrap CI code (Component A) + the staged-validation gate result this plan is blocked by
  - phase: 143.1-02
    provides: canary integrity assertion (Component D), not yet exercised against fresh data
  - phase: 143.1-03
    provides: vol-normalized POOLED target A/B script (Component F), not yet exercised against fresh data
  - phase: 143.1-04
    provides: sign-symmetric eligibility redesign (Component E), not yet exercised against fresh data
  - phase: 143.1-05
    provides: IC decomposition columns (Component B), not yet exercised against fresh data
  - phase: 143.1-06
    provides: e-values pilot (Component C), not yet exercised against fresh data
provides:
  - "143.1-07-CORPUS-RERUN.md: documented BLOCKED status with the precondition-check evidence trail"
  - "Explicit confirmation (via live psql + git log) that no project-owner override of the E6 staged-validation gate exists as of this session"
affects: [143.1-08-shadow-mode-validation, phase-144-cross-sectional-regime-model]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created:
    - .planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-07-CORPUS-RERUN.md
  modified: []

key-decisions:
  - "Did NOT trigger the full-pipeline corpus re-run. The plan's own Task 1 action text contains a hard precondition (\"if either is missing, STOP and do not run\") gating the run on the Component A staged-validation gate having passed. methodology-change-ledger.md's E6 entry explicitly records VERDICT: GATE NOT CLEARED (6-8/29 SUSPECT cells vs a pre-committed <=2 bound; tf=5m,is_pooled=false carries 3-4 SUSPECT vs a <=1 per-stratum bound), and no subsequent ledger entry or todo-099 annotation records a project-owner decision to accept this residual risk. Per the project's collaboration-style convention (run autonomously unless genuinely blocked on a decision only the user can make), fabricating an acceptance decision on the user's behalf for a multi-hour, resource-intensive rebuild of the corpus that feeds live-trading-capital decisions is out of scope for an executor agent - this is exactly the kind of decision the plan itself, the ledger's own pre-commitment rule, and todo 099 all independently flag as requiring explicit project-owner sign-off."
  - "Performed the precondition check as live verification, not by reading the ledger alone: confirmed no corpus pipeline process was already running (ps aux clean), confirmed feature_vectors row count unchanged (36,719,598, matching the pre-existing 6th-rebuild corpus), confirmed alpha.ensemble.sign_symmetric APR flag still false live in config_state, and confirmed via git log that no commit since 143.1-04's E9 entry touches the E6 gate or todo 099."
  - "Did not attempt Task 2 (validating D/F/B/C/sign-symmetric measurement on the re-run's output) since there is no re-run output to validate - Task 2 is entirely dependent on Task 1's pipeline run having actually executed."

requirements-completed: []
requirements-blocked: [A, B, C, D, F]

# Metrics
duration: ~25min
completed: 2026-07-11
---

# Phase 143.1 Plan 07: Full-Pipeline Corpus Re-run Summary

**BLOCKED — the plan's own hard precondition (Component A staged-validation gate must have passed) failed a live check against methodology-change-ledger.md's E6 entry, which explicitly records the gate as NOT CLEARED with no project-owner override recorded; no pipeline command was issued, and this session documents the block rather than fabricating a risk-acceptance decision on the user's behalf.**

## Performance

- **Duration:** ~25 min (precondition verification + documentation; no multi-hour pipeline run occurred)
- **Started:** 2026-07-11
- **Completed:** 2026-07-11 (as a blocked/halted plan, not a fully executed one)
- **Tasks:** 0/2 completed as originally scoped; 1 documentation task (the halt record) performed in their place

## What Happened

This plan's Task 1 requires triggering a multi-hour, full-pipeline corpus re-run
(`scripts/ops/corpus/ops_corpus_pipeline_run.sh --from-step 1`) that bundles Components
A+B+C+D+F together. The plan's own `<action>` text for Task 1 contains an explicit hard
stop: "Verify the Plan 01 Task 3 staged-validation gate passed ... AND Plan 01 Task 4's
bootstrap runtime budget is recorded - if either is missing, STOP and do not run."

Before issuing any pipeline command, this session verified both halves of that
precondition against the live `docs/plans/methodology-change-ledger.md`:

1. **Runtime budget (Plan 01 Task 4): RECORDED.** E6's runtime-budget sub-entry
   documents a 60-minute pre-committed wall-clock budget at 12 workers, measured
   against live APR params. This half is satisfied.
2. **Staged-validation gate (Plan 01 Task 3): NOT PASSED.** E6's own text states,
   verbatim: "**VERDICT: GATE NOT CLEARED.**" The bootstrap CI's SUSPECT rate (6-8 of
   29 evaluated cells, depending on `--n-permutations`) exceeds the pre-committed bound
   (`<= 2` total, `<= 1` per stratum) — concentrated in `tf=5m,is_pooled=false`
   autocorrelation/momentum features. Two follow-up robustness checks (higher
   permutation count, block-size sweep) both failed to clear the gate and ruled out the
   two cheapest explanations (small-sample noise, insufficient block length). E6 itself
   states the consequence: "Plan 07's corpus-wide re-run remains BLOCKED pending either
   (a) resolution of the 5m residual, or (b) an explicit, separately-recorded
   project-owner decision to accept the residual risk and proceed anyway."

Todo `.planning/todos/pending/099-bootstrap-ci-staged-validation-gate-not-cleared-5m-residual.md`
restates the same open decision and remains `status: pending` — not resolved, not
deferred with an acceptance note. A `git log` check against both files confirmed no
commit since 143.1-04's E9 entry (the most recent ledger entry) records a
project-owner override.

**Per the plan's own instruction, this executor halted before Task 1's pipeline
invocation.** `143.1-07-CORPUS-RERUN.md` records the full precondition-check evidence
trail (exact E6 quotes, the due-diligence commands run and their output, and the two
concrete paths forward per todo 099's own framing) so a future continuation of this
plan does not need to re-derive the block from scratch.

Task 2 (validating Components D/F/B/C and the sign-symmetric measurement proof on the
re-run's live output) was not attempted — it has no re-run result to validate against,
since Task 1 never ran.

## Task Commits

1. **Halt documentation** — `6f7b7d90` (docs): `143.1-07-CORPUS-RERUN.md` created,
   recording the precondition-check evidence and the two paths forward.

_No separate plan-metadata commit beyond this SUMMARY.md — orchestrator owns
STATE.md/ROADMAP.md updates after merge, per this plan's parallel-execution
instructions._

## Files Created/Modified

- `.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-07-CORPUS-RERUN.md` — created. Documents the BLOCKED status, both halves of the precondition check with exact ledger quotes, due-diligence commands run (process check, row counts, APR flag value, git log), and the two paths forward (investigate the 5m residual per todo 099, or an explicit recorded risk-acceptance decision).
- `docs/plans/methodology-change-ledger.md` — NOT modified this session. E6/E7/E8/E9 already fully document the gate's pre-committed threshold and measured non-clearance; no new result exists to record, so no new ledger entry was added (adding one would either duplicate E6 or require fabricating a decision that belongs to the project owner).

## Decisions Made

See `key-decisions` in frontmatter. In short: the plan's own hard precondition failed a
live check, and per this project's collaboration convention (run autonomously unless
genuinely blocked on a decision only the user can make), this is exactly that kind of
decision — a multi-hour rebuild of the corpus feeding live-trading-capital decisions,
gated by the plan itself, the ledger's own pre-commitment rule, and a still-open todo,
all independently pointing at the same "project owner must decide" boundary.

## Deviations from Plan

**No auto-fixable deviations (Rules 1-3) applied — nothing to fix.** This is a Rule 4
situation (architectural/methodology decision requiring explicit project-owner input)
that the plan's own text already anticipated and explicitly instructed the executor to
stop on, rather than a deviation this executor is introducing or resolving unilaterally.

### Not a checkpoint in the GSD sense

This plan contains no `type="checkpoint:*"` task nodes (`grep -c "checkpoint:" ...PLAN.md` = 0),
so this is not a checkpoint pause in the workflow-protocol sense. It is a plan-level
`<action>`-text-embedded hard precondition that failed. The distinction matters for how
the orchestrator should treat this: there is no `human-verify`/`decision`/`human-action`
checkpoint state to resume from later with a simple "approved" signal — the actual
unblock requires either new statistical investigation work (todo 099) or a genuine
project-owner risk-acceptance call recorded in the ledger, neither of which this
executor can manufacture.

## Issues Encountered

None beyond the precondition failure itself, which is the entire subject of this
summary.

## User Setup Required

**A project-owner decision is required before Plan 07 can proceed.** Two paths, per
todo 099:

1. Investigate and resolve the `tf=5m,is_pooled=false` bootstrap-CI residual (todo 099's
   "Not yet investigated" section: regime-conditional/wild-bootstrap variant for the
   flagged autocorrelation/momentum features, or validating whether the diagnostic's
   null benchmark is even the right ground truth for high-persistence features, or
   scoping the bounded risk of these 5 named features specifically).
2. Or an explicit, ledger-recorded decision to accept the residual risk and proceed with
   Plan 07's full-pipeline re-run anyway (per this ledger's own convention, accepting a
   failed pre-committed gate's result is itself a methodology change requiring its own
   entry).

Once either path resolves, Plan 07 (or a re-spawned continuation) can re-attempt Task
1's `--from-step 1` full-pipeline re-run.

## Next Phase Readiness

- **Plan 08 (shadow-mode validation) is blocked transitively** — it depends on Plan 07's
  corpus-wide re-run output (the corrected `feature_ic_scores` and the flag-ON
  challenger training run), neither of which exists yet.
- **Phase 144 (Cross-Sectional Regime Model)** was already noted (STATE.md) as needing
  its evidence re-measured on this corrected pipeline — that re-measurement remains
  unavailable until Plan 07 actually runs.
- Components A-F's CODE (Plans 01-06) all shipped correctly and remain committed and
  unit-tested — only the corpus-wide EXERCISE of that code is blocked. No code
  regression, no lost work; this is purely a sequencing/data-integrity gate.

### Self-Check: PASSED

- `.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-07-CORPUS-RERUN.md` — FOUND.
- Commit `6f7b7d90` — FOUND in `git log`.
- `docs/plans/methodology-change-ledger.md` E6 "VERDICT: GATE NOT CLEARED" text — FOUND, verified by direct read.
- `.planning/todos/pending/099-bootstrap-ci-staged-validation-gate-not-cleared-5m-residual.md` still `status: pending` — FOUND, verified by direct read.
- Live psql checks (feature_vectors count, alpha.ensemble.sign_symmetric flag, no running corpus process) — all executed and results recorded verbatim in 143.1-07-CORPUS-RERUN.md.

---
*Phase: 143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap*
*Completed: 2026-07-11 (BLOCKED status)*
