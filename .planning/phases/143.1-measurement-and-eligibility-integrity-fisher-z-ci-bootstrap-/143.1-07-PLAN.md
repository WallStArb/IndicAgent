---
phase: 143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap
plan: 07
type: execute
wave: 6
depends_on: ["143.1-01", "143.1-02", "143.1-03", "143.1-04", "143.1-05", "143.1-06"]
files_modified:
  - docs/plans/methodology-change-ledger.md
  - .planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-07-CORPUS-RERUN.md
autonomous: true
requirements: [A, B, C, D, F]
must_haves:
  truths:
    - "A SINGLE full-pipeline corpus re-run from Step 1 (backfill_feature_factory --compute-only) reflects Components A + B + C + D + F together (D's new canary FeatureVector columns force Step 1, not an ic_engine-only re-run)"
    - "Component A's staged-validation gate passed BEFORE this re-run was triggered"
    - "Component D canary integrity assertion ran during the re-run and did not halt on a false-positive (negative controls null, acausal placebo clears)"
    - "Component F A/B result (raw vs vol-normalized POOLED rankings) is recorded; keep-or-retire decision made"
    - "Component B decomposition columns and Component C 5m e-values are populated corpus-wide"
    - "feature_ic_scores now contains eligible rows at ic_sign=-1 (sign-symmetric measurement landed - proves Plan 04's cross-sectional fix worked, not the wrong function)"
  artifacts:
    - path: ".planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-07-CORPUS-RERUN.md"
      provides: "Re-run manifest: row counts, canary assertion result, F A/B result, negative-IC eligibility count, before/after emission-threshold note"
      min_lines: 30
  key_links:
    - from: "scripts/ops/corpus/ops_corpus_pipeline_run.sh --from-step 1"
      to: "feature_vectors / feature_ic_scores"
      via: "full pipeline recompute landing all canary columns + sign-aware CI + decomposition + e-values"
      pattern: "from-step 1"
---

<objective>
Execute the ONE full-pipeline corpus re-run this entire phase bundles into, and validate the measurement-layer components (A, B, C, D, F) on the live result. Because Component D adds new canary `FeatureVector` columns, this re-run MUST start from Step 1 (`backfill_feature_factory.py --compute-only`), a multi-hour full recompute across all symbols/timeframes/history - NOT a cheap `ic_engine.py`-only invocation (RESEARCH Pitfall 6). Landing D before this single re-run makes it genuinely one re-run covering A+B+C+D+F, consistent with the phase's batching rationale.

HARD PRECONDITION: Component A's staged-validation gate (Plan 01 Task 3) must have passed - the bootstrap CI shows a near-zero SUSPECT rate - before this re-run is triggered. Do not start otherwise.

Purpose: this is not "future work" - it is the deliverable that produces the corrected, re-measured corpus Phase 144 builds on. Component E's shadow-mode validation (Plan 08) runs on top of this re-run's output.

Output: a completed full-pipeline re-run, a recorded canary assertion result, Component F's A/B result + keep/retire decision, populated B/C columns, and confirmation that feature_ic_scores now carries eligible ic_sign=-1 rows (the proof Plan 04's fix landed in the load-bearing cross-sectional function).
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-CONTEXT.md
@.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-RESEARCH.md

<interfaces>
scripts/ops/corpus/ops_corpus_pipeline_run.sh:
- `--from-step N` and `--symbols SYM1,SYM2`. Steps: 1 backfill_feature_factory (--compute-only), 2 regime_writer, 3 forward_return_writer, 4 equity_regime_model, 5 ic_engine, 6 ic_shrinkage, 7 ensemble_trainer, 8 alpha_publisher. Corpus Consistency Gate runs after step 2 (regime-label uniformity).
- backfill_feature_factory silently skips all symbols if backfill_status is empty - seed from market_data_ohlcv after any truncation (STATE.md Corpus Pipeline Gotcha).
- New canary columns require Step 1 so feature_vectors gets them populated corpus-wide.
scripts/ops/alpha/ops_canary_integrity_assert.py (Plan 02) - runs after Step 5.
scripts/ops/alpha/ops_vol_normalized_target_ab.py (Plan 03) - the F A/B comparison, run post-re-run.
Live DB: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "..."`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm Component A staged gate passed, seed backfill_status, trigger the single full-pipeline re-run from Step 1</name>
  <files>.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-07-CORPUS-RERUN.md</files>
  <read_first>
    - scripts/ops/corpus/ops_corpus_pipeline_run.sh (full - step order, Corpus Consistency Gate, --from-step, resume-on-failure)
    - docs/plans/methodology-change-ledger.md (confirm the Component A staged-validation entry from Plan 01 exists and passed)
    - .planning/STATE.md Corpus Pipeline Gotcha (backfill_status seeding) + Corpus pipeline state memory notes
    - CONTEXT.md/RESEARCH.md Pitfall 6 (why Step 1 is mandatory)
  </read_first>
  <action>
    Verify the Plan 01 Task 3 staged-validation gate passed (methodology-change-ledger shows near-zero SUSPECT rate) - if not, STOP and do not run. Seed backfill_status from market_data_ohlcv if empty (STATE.md gotcha SQL). Run `bash scripts/ops/corpus/ops_corpus_pipeline_run.sh --from-step 1` as a background/long-running operation (multi-hour). This single run lands A (bootstrap CI in step 5), D (canary columns in step 1 + assertion after step 5), B (decomposition columns in step 5), C (5m e-values in step 5), and the sign-aware CI. The Corpus Consistency Gate (regime-label uniformity) must pass after step 2. Create 143.1-07-CORPUS-RERUN.md recording the run start, the --from-step 1 rationale (D's new columns), and per-step completion.
  </action>
  <verify>
    <automated>bash -n scripts/ops/corpus/ops_corpus_pipeline_run.sh && echo "orchestrator syntax OK"; grep -c "from-step 1\|Step 1" .planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-07-CORPUS-RERUN.md</automated>
    <human-check>Full-pipeline re-run completes all 8 steps from Step 1; feature_vectors shows the new canary columns populated across the full symbol/tf/history range; the Corpus Consistency Gate passed after step 2.</human-check>
  </verify>
  <acceptance_criteria>
    - Component A staged gate confirmed passed in the ledger before the run started.
    - The re-run used `--from-step 1` (recorded rationale: D's new FeatureVector columns).
    - 143.1-07-CORPUS-RERUN.md records run start, step order, and completion; canary columns populated in feature_vectors (psql count > 0).
  </acceptance_criteria>
  <done>The single full-pipeline corpus re-run from Step 1 completed, landing A+B+C+D+F in one pass, with canary columns populated corpus-wide.</done>
</task>

<task type="auto">
  <name>Task 2: Validate D (canary assertion), F (raw-vs-vol A/B), B/C column population, and sign-symmetric measurement landed</name>
  <files>.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-07-CORPUS-RERUN.md, docs/plans/methodology-change-ledger.md</files>
  <read_first>
    - scripts/ops/alpha/ops_canary_integrity_assert.py (Plan 02), scripts/ops/alpha/ops_vol_normalized_target_ab.py (Plan 03)
    - RESEARCH.md Pitfall 1 warning sign (if alpha_events short-side count still ~0, the walk-forward fix landed in the wrong function)
    - docs/plans/methodology-change-ledger.md (F A/B recording location from Plan 03)
    - CONTEXT.md Component E emission-threshold re-measurement note
  </read_first>
  <action>
    After the re-run: (1) confirm the canary integrity assertion (ops_canary_integrity_assert.py) passed during the run - negative-control canaries null, acausal placebo cleared the gate; record the result. (2) Run ops_vol_normalized_target_ab.py and record the raw-vs-vol-normalized POOLED ranking comparison; make the keep-or-retire decision per the locked contract (retire if materially identical) and record it in the methodology-change-ledger. (3) Confirm B's sign_hit_rate + magnitude-conditional IC columns and C's 5m e-values are populated (psql counts). (4) CRITICAL sign-symmetry proof: query `SELECT count(*) FROM feature_ic_scores WHERE ic_sign = -1 AND ((ic_sign=1 AND ic_ci_lower>0) OR (ic_sign=-1 AND ic_ci_upper<0)) AND passes_walkforward = true` (adapt to the sign-aware predicate) - a non-zero count proves Plan 04's walk-forward fix landed in the load-bearing cross-sectional function, not the wrong per-symbol block. Record all in 143.1-07-CORPUS-RERUN.md. Do NOT re-litigate EIC-04's prior PASS verdict (explicitly deferred to the project owner) - record the fact that A's CI changed, not a verdict reversal.
  </action>
  <verify>
    <automated>grep -c "canary\|vol_normalized\|ic_sign\|e_value\|sign_hit_rate" .planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-07-CORPUS-RERUN.md</automated>
    <human-check>Canary assertion passed (negative controls null, placebo clears); F A/B recorded with keep/retire decision; B/C columns populated; feature_ic_scores has a non-zero count of sign-symmetric-eligible ic_sign=-1 rows (Pitfall 1 proof).</human-check>
  </verify>
  <acceptance_criteria>
    - Canary assertion result recorded: negative controls did NOT clear the gate; acausal placebo DID.
    - Component F A/B ranking comparison recorded with an explicit keep-or-retire decision in the methodology-change-ledger.
    - B's two columns and C's 5m e-values are populated (psql counts > 0 recorded).
    - A non-zero count of sign-symmetric-eligible ic_sign=-1 rows exists in feature_ic_scores (proves the cross-sectional walk-forward fix, not the wrong function).
    - Emission-threshold re-measurement flagged for Plan 08 (thresholds were calibrated on a long-only-skewed distribution).
  </acceptance_criteria>
  <done>All measurement-layer components validated on the live re-run; sign-symmetric measurement confirmed present; F decision recorded; canary integrity confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus orchestrator -> live DB tables | Full recompute writes feature_vectors/feature_ic_scores; internal, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143.1-07-V2 | Spoofing/Auth | none | accept | N/A - local corpus batch operation, no network surface |
| T-143.1-07-V3 | Session | none | accept | N/A |
| T-143.1-07-V4 | Access Control | none | accept | N/A |
| T-143.1-07-V5 | Input Validation | none | accept | N/A - no new APR keys or external input in this plan |
| T-143.1-07-V6 | Cryptography | none | accept | N/A |
| T-143.1-07-INT | Data/statistical integrity | full re-run | mitigate | staged-validation precondition + canary assertion during the run + explicit sign-symmetry proof query prevent a silently-wrong corpus reaching Phase 144 |
| T-143.1-07-SC | Tampering (supply chain) | none | accept | N/A - no npm/pip/cargo installs; no new packages |
</threat_model>

<verification>
- Full-pipeline re-run completed all 8 steps from Step 1; Corpus Consistency Gate passed.
- Canary assertion passed; F A/B recorded; B/C columns populated; ic_sign=-1 eligible rows present.
- 143.1-07-CORPUS-RERUN.md and methodology-change-ledger updated.
</verification>

<success_criteria>
- One full-pipeline re-run from Step 1 reflects A+B+C+D+F together; the corpus is trustworthy for Phase 144 and for Plan 08's shadow validation.
- Sign-symmetric measurement confirmed present (Pitfall 1 proof); F keep/retire decided; canary integrity confirmed.
</success_criteria>

<output>
Create `.planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-07-SUMMARY.md` when done.
</output>
