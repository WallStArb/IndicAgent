---
phase: 105-architecture-hotfix-sprint
plan: 05
type: execute
wave: 3
depends_on: [105-01, 105-02, 105-03, 105-04]
files_modified:
  - tests/unit/pipeline/test_signal_processor.py
  - tests/unit/services/test_shadow_auditor_agent.py
  - tests/unit/services/test_ctx_writer_agent.py
  - tests/unit/services/test_swarm_ledger_writer_agent.py
  - tests/unit/services/test_bar_writer_agent.py
autonomous: true

must_haves:
  truths:
    - "A regression test proves a shadow plugin can never be the select_winner result"
    - "A regression test proves shadow_auditor counts only is_shadow=FALSE rows in promotion gate"
    - "A regression test proves CtxWriterAgent flush uses .add() and teardown calls super()._teardown()"
    - "A regression test proves SwarmLedgerWriterAgent constructs with enable_auto_commit=False"
    - "A regression test proves BarWriterAgent._run() calls _record_message_consumed()"
    - "Full unit suite is green after all phase-105 fixes"
  artifacts:
    - path: "tests/unit/pipeline/test_signal_processor.py"
      provides: "shadow winner-suppression regression test"
      contains: "is_shadow"
    - path: "tests/unit/services/test_shadow_auditor_agent.py"
      provides: "is_shadow=FALSE promotion-gate filter test"
      contains: "is_shadow"
    - path: "tests/unit/services/test_swarm_ledger_writer_agent.py"
      provides: "enable_auto_commit=False construction test"
      contains: "enable_auto_commit"
  key_links:
    - from: "test_signal_processor shadow test"
      to: "signal_processor.eligible_ranked filter"
      via: "asserts winner setup_plugin is never a shadow-cache plugin"
      pattern: "eligible_ranked|is_shadow"
---

<objective>
Add regression tests that lock in the phase-105 fixes so they cannot silently regress, and confirm
the full unit suite is green. The critical test is shadow winner-suppression: prove a shadow plugin
can never become the live winner.

Purpose: Phase-105 fixes are mechanical and easy to revert accidentally. Tests make the contracts
executable.

Output: New/updated unit tests for the shadow suppression path, shadow auditor filter, ctx_writer
flush + teardown, swarm_ledger auto-commit, and bar_writer liveness. Full `pytest tests/unit/` green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/phase-105/105-RESEARCH.md
@.planning/phases/phase-105/105-PATTERNS.md
</context>

<threat_model>
Risk vectors for this test plan:
- **False-green tests:** A test that mocks too much can pass even if the fix is reverted. Each test
  must assert against the ACTUAL fixed behavior (winner identity, .set vs .add call, constructor
  kwarg), not against a re-mock of the fix.
- **get_connection mock pitfall (CLAUDE.md):** `get_connection()` returns an async context manager,
  not a coroutine. Mock with `MagicMock(side_effect=async_cm_func)`, NOT `AsyncMock`, or `async with`
  breaks and the test fails for the wrong reason.
- **OTel call assertions:** Assert `.set`/`.add` via a Mock on the instrument; do not import the real
  OTel SDK gauge in a unit test (it would no-op silently and give a false green).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Shadow suppression + shadow auditor regression tests</name>
  <files>
    tests/unit/pipeline/test_signal_processor.py
    tests/unit/services/test_shadow_auditor_agent.py
  </files>
  <read_first>
    tests/unit/pipeline/test_signal_processor.py
    tests/unit/services/test_shadow_auditor_agent.py
    src/intelligence/pipeline/signal_processor.py
    services/shadow_auditor_agent.py
    .planning/phases/phase-105/105-PATTERNS.md
  </read_first>
  <action>
    1. In test_signal_processor.py add a test that constructs a `CacheSnapshot` whose `shadow_cache`
       marks one plugin `True` (shadow) and another `False` (live). Feed a `ranked` list where the
       shadow plugin has the HIGHEST score (would win without the filter). Run `process()` and assert
       the returned winner's `setup_plugin` is the LIVE plugin, never the shadow plugin. Also assert
       the shadow signal still appears in the persisted ranked list with `status == "regime_suppressed"`
       and `is_shadow is True`. Follow the existing fixture/mock style already in the file.

    2. In test_shadow_auditor_agent.py add a test for `_check_promotion()` that mocks the DB fetch and
       asserts the SQL passed to `conn.fetch` contains `is_shadow = FALSE`. Mock `get_connection()`
       with `MagicMock(side_effect=async_cm_func)` per CLAUDE.md (NOT AsyncMock). Add a second test
       asserting `_run_audit()` skips a registry row with `component_type == "swarm_agent"` (the swarm
       row produces no promotion/demotion evaluation call).
  </action>
  <verify>
    .venv/bin/pytest tests/unit/pipeline/test_signal_processor.py tests/unit/services/test_shadow_auditor_agent.py -q 2>&1 | tail -15
  </verify>
  <acceptance_criteria>
    - test_signal_processor.py has a test asserting the winner's setup_plugin is the live (non-shadow) plugin even when the shadow plugin has the top score
    - That test asserts the shadow signal in ranked has status "regime_suppressed" and is_shadow True
    - test_shadow_auditor_agent.py has a test asserting the promotion query SQL contains "is_shadow = FALSE"
    - test_shadow_auditor_agent.py has a test asserting swarm_agent registry rows are skipped in _run_audit
    - `.venv/bin/pytest tests/unit/pipeline/test_signal_processor.py tests/unit/services/test_shadow_auditor_agent.py -q` exits 0
  </acceptance_criteria>
  <done>
    Shadow plugin cannot win; auditor promotion filters is_shadow=FALSE and skips swarm agents — all proven by tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Writer-service regression tests (ctx flush/teardown, swarm auto-commit, bar liveness)</name>
  <files>
    tests/unit/services/test_ctx_writer_agent.py
    tests/unit/services/test_swarm_ledger_writer_agent.py
    tests/unit/services/test_bar_writer_agent.py
  </files>
  <read_first>
    tests/unit/services/test_ctx_writer_agent.py
    tests/unit/services/test_swarm_ledger_writer_agent.py
    tests/unit/services/test_bar_writer_agent.py
    services/ctx_writer_agent.py
    services/swarm_ledger_writer_agent.py
    services/bar_writer_agent.py
    .planning/phases/phase-105/105-PATTERNS.md
  </read_first>
  <action>
    1. test_ctx_writer_agent.py: add a test that replaces `_events_written` / `_snapshots_written`
       with Mock instruments, runs a flush with a non-empty batch, and asserts `.add` was called with
       the batch length and `.inc` was NEVER called (`assert not mock.inc.called`). Add a second test
       that patches the agent's buffers and asserts `_teardown()` invokes `super()._teardown()` (spy
       on the base method or assert buffers are drained after teardown).

    2. test_swarm_ledger_writer_agent.py: add a test that patches `KafkaConsumerClient` with a Mock,
       triggers consumer construction (via `_setup()` or the construction helper), and asserts the
       constructor was called with `enable_auto_commit=False` (inspect the captured kwargs).

    3. test_bar_writer_agent.py: add a test that drives one message through `_run()` with a mocked
       consumer yielding a single bar payload, and asserts `self._record_message_consumed` was called
       (spy/Mock on the method). Mock `get_connection()` per CLAUDE.md if the path touches the DB.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/services/test_ctx_writer_agent.py tests/unit/services/test_swarm_ledger_writer_agent.py tests/unit/services/test_bar_writer_agent.py -q 2>&1 | tail -15
  </verify>
  <acceptance_criteria>
    - test_ctx_writer_agent.py asserts `.add` called and `.inc` never called on the write counters
    - test_ctx_writer_agent.py asserts `_teardown()` invokes `super()._teardown()` (or drains buffers)
    - test_swarm_ledger_writer_agent.py asserts KafkaConsumerClient constructed with `enable_auto_commit=False`
    - test_bar_writer_agent.py asserts `_record_message_consumed` is called when a bar is consumed
    - `.venv/bin/pytest tests/unit/services/test_ctx_writer_agent.py tests/unit/services/test_swarm_ledger_writer_agent.py tests/unit/services/test_bar_writer_agent.py -q` exits 0
  </acceptance_criteria>
  <done>
    Writer-service fixes are locked in by regression tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full unit suite green + lint/format sweep</name>
  <files>
    tests/unit/services/test_bar_writer_agent.py
  </files>
  <read_first>
    .planning/phases/phase-105/105-RESEARCH.md
  </read_first>
  <action>
    Run the full unit suite and the lint/format gate over all phase-105 touched files. Fix any
    failures introduced by the new tests or the code fixes (e.g. fixture drift, import errors). Do not
    weaken or skip tests to make them pass — fix the root cause. If a failure traces to a pre-existing
    unrelated test, note it in the SUMMARY but do not modify unrelated tests.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/ -q 2>&1 | tail -15
    .venv/bin/ruff check services/ src/intelligence/pipeline/ src/observability/ tests/unit/
    .venv/bin/black --check services/ src/intelligence/pipeline/ src/observability/ tests/unit/
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/ -q` exits 0 (full suite green)
    - `.venv/bin/ruff check services/ src/intelligence/pipeline/ src/observability/ tests/unit/` exits 0
    - `.venv/bin/black --check ...` exits 0 (or files reformatted and committed)
  </acceptance_criteria>
  <done>
    Full unit suite green; ruff and black clean across all phase-105 files.
  </done>
</task>

</tasks>

<verification>
.venv/bin/pytest tests/unit/ -q 2>&1 | tail -20
.venv/bin/ruff check services/ src/intelligence/pipeline/ src/observability/ tests/unit/
.venv/bin/black --check services/ src/intelligence/pipeline/ src/observability/ tests/unit/
</verification>

<success_criteria>
1. Shadow winner-suppression regression test proves a shadow plugin never wins
2. Shadow auditor test proves is_shadow=FALSE promotion filter and swarm_agent skip
3. ctx_writer test proves .add() used (never .inc()) and super()._teardown() called
4. swarm_ledger test proves enable_auto_commit=False
5. bar_writer test proves _record_message_consumed() called
6. Full `pytest tests/unit/` green; ruff + black clean
</success_criteria>

<output>
After completion, create `.planning/phases/phase-105/105-05-SUMMARY.md` following the template
at `~/.claude/get-shit-done/templates/summary.md`.
</output>
