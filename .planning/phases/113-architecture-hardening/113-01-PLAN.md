---
phase: "113-architecture-hardening"
plan: "113-01"
type: "refactor"
wave: 1
depends_on: []
files_modified:
  - src/api/routes/sse.py
  - src/observability/metrics.py
  - tests/unit/api/test_sse_intelligence.py
  - tests/unit/api/test_sse_broadcaster.py
  - src/intelligence/pipeline/signal_processor.py
  - production/migrations/115_signal_id_unique.sql
  - src/intelligence/ml/confidence_calibrator.py
  - services/intelligence_pipeline.py
  - src/core/agent/base_writer.py
  - src/persistence/repository/feature_repository.py
  - tests/unit/intelligence/test_dag_invariants.py
  - src/core/stream_keys.py
  - production/migrations/116_setup_performance_gate.sql
autonomous: true
must_haves:
  truths:
    - "signal_id is content-addressed (SHA-256) — epoch ns timestamps, sort_keys=True canonical JSON, normalized tf — stable across replay"
    - "calibrated_confidence is populated in-process before I7; fallback is raw_confidence when no cached curve — never null, never blocking"
    - "BaseWriter._parse_payload returns (valid, invalid) tuple — subclass inventory table built before any modification — no None/[] sentinel ambiguity"
    - "intelligence_features writer uses DO UPDATE on conflict — replay is idempotent"
    - "DAG Invariant 2 is enforced by a pytest fixture in CI (covers src/intelligence/ + services/intelligence_pipeline.py)"
    - "migration 115 runs outside a transaction (CONCURRENTLY); duplicate pre-check runs first"
    - "hot-reload uses atomic reference swap per daemon; last-known-good preserved on bad update; reload success/failure metrics emitted"
    - "backpressure drops log symbol+tf for operator visibility; gap-awareness documented — not a steady-state drop policy"
    - "All unit tests green after each task"
  artifacts:
    - ".planning/phases/113-architecture-hardening/113-01-PLAN.md"
    - "production/migrations/115_signal_id_unique.sql"
    - "production/migrations/116_setup_performance_gate.sql"
    - "tests/unit/api/test_sse_broadcaster.py"
    - "tests/unit/intelligence/test_dag_invariants.py"
---

# Phase 113 Plan 01: Architecture Hardening

> **Source:** `docs/plans/2026-06-03-sse-broadcaster-hardening.md` (imported 2026-06-03)
> **Origin:** Renaissance Technologies first-principles architecture review — addresses all CRITICAL, HIGH, MEDIUM, and structural findings.

**Goal:** Fix twelve correctness and integrity problems surfaced by the cross-AI architecture review: dead Redis SSE code, drop telemetry, bounded snapshot cache, topic-indexed fan-out, content-addressed signal IDs, in-process confidence calibration, BaseWriter sentinel contract, idempotent feature upserts, DAG invariant CI enforcement, live contract hot-reload, setup_performance sample gate, and pipeline backpressure.

**Note on CRITICAL-03 (shadow governance):** t-test promotion gate and rolling Sharpe demotion are routed to Phase 101 CONTEXT.md — must be implemented in `PromotionGate`/`DemotionGate` in `src/intelligence/ai/fitness/gates.py`, not in `shadow_auditor.py` directly.

**Note on MEDIUM-03 (DSPy data gate):** Already fully specified in Phase 098 CONTEXT.md. No action needed here.

---

For the full task breakdown with step-by-step instructions and code, see:
`docs/plans/2026-06-03-sse-broadcaster-hardening.md`

Tasks are numbered 1–12 in the source document. Execute in order; each task ends with a commit.

## Task Summary

| # | Finding | Files | Commit message |
|---|---------|-------|---------------|
| 1 | Dead Redis SSE code | `src/api/routes/sse.py`, delete `test_sse_intelligence.py` | `chore: delete dead Redis SSE helpers and their test` |
| 2 | SSE drop telemetry | `src/observability/metrics.py` | `feat(metrics): add sse_messages_dropped_total counter` |
| 3 | Topic-indexed SSE fan-out + bounded cache | `src/api/routes/sse.py` | `refactor(sse): topic-indexed fan-out, bounded snapshot cache, drop counter` |
| 4 | SSE unit tests | `tests/unit/api/test_sse_broadcaster.py` | `test(sse): unit tests for topic-indexed broadcaster fan-out` |
| 5 | CRITICAL-01: Content-addressed signal_id | `src/intelligence/pipeline/signal_processor.py`, migration 115 | `feat(signals): content-addressed signal_id via SHA-256` |
| 6 | CRITICAL-02: In-process calibration | `services/intelligence_pipeline.py`, `confidence_calibrator.py` | `feat(pipeline): in-process confidence calibration before I7` |
| 7 | HIGH-01: BaseWriter (valid, invalid) tuple | `src/core/agent/base_writer.py` + all subclasses | `refactor(writer): _parse_payload returns (valid, invalid) tuple` |
| 8 | HIGH-03: features DO UPDATE | `src/persistence/repository/feature_repository.py` | `fix(features): upsert on conflict DO UPDATE for idempotent replay` |
| 9 | MEDIUM-01: DAG invariant CI | `tests/unit/intelligence/test_dag_invariants.py` | `test(dag): CI enforcement of DAG Invariant 2` |
| 10 | MEDIUM-02: Live contract updates | `src/core/stream_keys.py`, affected daemons | `feat(contracts): live hot-reload via contracts.updated topic` |
| 11 | Structural: setup_performance gate | migration 116 + gate check code | `fix(shadow): raise setup_performance gate to 100 samples minimum` |
| 12 | Structural: pipeline backpressure | `services/intelligence_pipeline.py`, `src/observability/metrics.py` | `feat(pipeline): backpressure circuit breaker` |
