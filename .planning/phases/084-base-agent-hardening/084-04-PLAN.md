---
phase: 084-base-agent-hardening
plan: 04
type: execute
wave: 3
depends_on: ["084-01", "084-02", "084-03"]
files_modified:
  - production/grafana/dashboards/plugin-latency.json
autonomous: true
requirements: [OBS-01]

must_haves:
  truths:
    - "A Grafana dashboard panel exists that ranks plugins by p95 latency descending using the existing PLUGIN_DURATION_MS histogram"
    - "Panel queries the {plugin_name, tier} labels already emitted by intelligence_pipeline_agent.py line 1077 — no pipeline code changes"
    - "Panel covers both p50 and p95 percentiles for the same plugin set"
  artifacts:
    - path: "production/grafana/dashboards/plugin-latency.json"
      provides: "Grafana dashboard JSON with p50/p95 plugin latency ranking"
      contains: "plugin_duration_ms"
  key_links:
    - from: "production/grafana/dashboards/plugin-latency.json"
      to: "PLUGIN_DURATION_MS Prometheus/OTel histogram"
      via: "PromQL histogram_quantile() query against plugin_duration_ms_bucket"
      pattern: "histogram_quantile"
---

<objective>
Satisfy OBS-01 by adding a Grafana dashboard panel that ranks plugins by p95 latency using the existing PLUGIN_DURATION_MS histogram. No pipeline code change required — the metric already records `{plugin_name, tier}` at services/intelligence_pipeline_agent.py line 1077.

Purpose: Make "which plugin is the bottleneck" a one-click answer. The OBS-01 data drives later Phase 089 PERF-04 optimization decisions.

Output: A new Grafana dashboard JSON file at production/grafana/dashboards/plugin-latency.json with at least two panels (p50 ranking, p95 ranking) sorted by p95 descending.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/084-base-agent-hardening/084-CONTEXT.md
@.planning/phases/084-base-agent-hardening/084-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Grafana dashboard JSON for plugin latency ranking</name>
  <files>production/grafana/dashboards/plugin-latency.json</files>
  <read_first>
    - production/grafana/dashboards/ (list existing dashboards via `ls production/grafana/dashboards/` to mirror existing schema_version, datasource UID, tag, and panel layout conventions — pick one existing dashboard JSON file and read it to confirm the structure)
    - services/intelligence_pipeline_agent.py (around line 1077 — confirm metric name and labels: `plugin_duration_ms` histogram, labels `plugin_name` and `tier`)
    - src/observability/metrics.py (around line 39 — confirm PLUGIN_DURATION_MS instrument definition)
  </read_first>
  <action>
    Create a new Grafana dashboard JSON at production/grafana/dashboards/plugin-latency.json that uses the existing PLUGIN_DURATION_MS histogram metric (exported as `plugin_duration_ms_bucket` / `plugin_duration_ms_sum` / `plugin_duration_ms_count` in Prometheus naming).

    Required structure (mirror an existing dashboard in production/grafana/dashboards/ for schema_version, datasource UID format, and folder/tag conventions — do not invent values that diverge from the project's other dashboards):

    - Dashboard title: "Plugin Latency (Phase 84 OBS-01)"
    - Tags: include at least "intelligence", "performance", "phase-84"
    - Default time range: last 1 hour; refresh: 30s

    Panel 1: "Plugin p95 latency (top N)" — bar chart or table sorted by value desc.
        PromQL: `topk(20, histogram_quantile(0.95, sum by (plugin_name, le) (rate(plugin_duration_ms_bucket[5m]))))`
        Legend: `{{plugin_name}}`
        Unit: milliseconds (ms)

    Panel 2: "Plugin p50 latency" — same shape as Panel 1 with quantile 0.50.
        PromQL: `topk(20, histogram_quantile(0.50, sum by (plugin_name, le) (rate(plugin_duration_ms_bucket[5m]))))`
        Legend: `{{plugin_name}}`
        Unit: milliseconds (ms)

    Panel 3 (optional but recommended): "p95 latency time series" — line graph with `{plugin_name}` legend, same quantile expression without the topk so the user can see trends over time. Limit display via legend `desc` sort if Grafana version supports it.

    JSON must be valid (parseable by `python -m json.tool`). Use the same datasource UID placeholder pattern other dashboards in this directory use (likely `${DS_PROMETHEUS}` or a specific UID — copy from an existing dashboard).

    Do NOT change PLUGIN_DURATION_MS recording code in services/intelligence_pipeline_agent.py or src/observability/metrics.py. The pipeline instrumentation already exists.

    Do NOT introduce per-symbol granularity in the query — that is explicitly deferred to Phase 089 per CONTEXT.md "Deferred Ideas".
  </action>
  <verify>
    test -f production/grafana/dashboards/plugin-latency.json
    .venv/bin/python -m json.tool production/grafana/dashboards/plugin-latency.json > /dev/null
    grep -c "histogram_quantile" production/grafana/dashboards/plugin-latency.json
    grep -c "plugin_duration_ms_bucket" production/grafana/dashboards/plugin-latency.json
    grep -c "plugin_name" production/grafana/dashboards/plugin-latency.json
  </verify>
  <acceptance_criteria>
    - File production/grafana/dashboards/plugin-latency.json exists
    - File is valid JSON (python -m json.tool exits 0)
    - File contains `histogram_quantile(0.95` (p95 expression)
    - File contains `histogram_quantile(0.5` or `histogram_quantile(0.50` (p50 expression)
    - File contains `plugin_duration_ms_bucket` at least twice (once per panel)
    - File contains `plugin_name` legend reference
    - File contains `topk(` to enforce descending ranking
    - File contains the literal string `Plugin Latency` (title) and a tag entry referencing `phase-84` or `phase-084`
    - File does NOT include a `symbol` label in any query (per CONTEXT.md deferred scope)
  </acceptance_criteria>
  <done>OBS-01 satisfied via Grafana dashboard; p95 ranking visible without any pipeline code change.</done>
</task>

</tasks>

<verification>
- `test -f production/grafana/dashboards/plugin-latency.json && python -m json.tool production/grafana/dashboards/plugin-latency.json > /dev/null` exits 0
- Dashboard JSON references `plugin_duration_ms_bucket` and `histogram_quantile`
- No pipeline source files (services/intelligence_pipeline_agent.py, src/observability/metrics.py) were modified by this plan
</verification>

<success_criteria>
- OBS-01 requirement satisfied: developers can see slowest plugins at p50/p95 without adding instrumentation
- Dashboard JSON valid and committed
- Per-symbol granularity deferred to Phase 089 (confirmed absent from queries)
</success_criteria>

<output>
After completion, create `.planning/phases/084-base-agent-hardening/084-04-SUMMARY.md`
</output>
