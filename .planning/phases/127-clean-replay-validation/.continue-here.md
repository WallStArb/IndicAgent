---
context: phase
phase: 127-clean-replay-validation
task: 0
total_tasks: 4
status: in_progress
last_updated: 2026-06-16T20:48:41Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each was discovered through actual failure this session.

- [ ] CONSTRAINT: **Read-path migration is NOT done.** Phase 130 migrated signal WRITE paths only. ~9 live readers query dropped `signal_outcomes` + absent view columns. Two are live hazards: `shadow_validator` crashes on its timer (JOINs dropped table); `shadow_auditor` spuriously demotes every cycle (ev_r=0.0 on empty corpus). Mitigation: execute Plan 00 Task 1 FIRST, before any replay or ml-training.
- [ ] CONSTRAINT: **No outcome data in replay corpus.** `counterfactual_pnl_r` is NULL (0/1.44M) until v2.11 CounterfactualTracker; `trade_executions` is empty (replay never executes trades). Do NOT measure calibration/edge this phase. Do NOT substitute `actual_pnl_r` (also absent). Map all `outcome`/`pnl_r` readers to `counterfactual_pnl_r`; they no-op cleanly until v2.11.
- [ ] CONSTRAINT: **Fire rate is descriptive, not a gate.** Do NOT encode `<3%` as pass/fail. It's `100*signals/intelligence_features bar-instances`, reported per setup, compared only to the Phase 124 onset_guard leak baseline (15-30%/bar).
- [ ] CONSTRAINT: **`trade_executions` joins on `frame_id`, not `signal_id`.** The column does not exist. Chain: signal_events.signal_id -> trade_frames.signal_id -> trade_frames.frame_id -> trade_executions.frame_id.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|--------------------------|
| Proxy-as-target | Substituting an arbitrary fire-rate % for the unmeasurable question (edge) | blocking | Fire rate is reported, never gated; edge explicitly deferred to v2.11 |
| Calibration theater | Emitting empty bootstrap/calibration columns as if measured | blocking | Calibration removed from Plan 02 entirely; named as v2.11-deferred |
| False rescue | Using `actual_pnl_r` as fallback for `counterfactual_pnl_r` | blocking | trade_executions is empty in replay; both are absent until live trade / v2.11 |

<current_state>
Plan 00 (read-path 3-table migration) is fully specified and committed, but NOT executed. We are on branch `phase-127/00-read-path-3table-migration`. The code migration (9 files) is the immediate next work. This session ran out of context after writing the plan.
</current_state>

<completed_work>

This session (review + planning, all committed in c267d3b4):
- Audited Phase 127 plans: found no-outcome-data gap, wrong join keys, stale volume numbers, fire-rate metric error
- Revised 127-PHASE.md, 127-CONTEXT.md, 127-RESEARCH.md (10 issues), 127-01/02/03-PLAN.md
- Created 127-00-PLAN.md (wave-0 read-path debt migration) with exact per-site SQL rewrites + 4 locked design decisions
- Created execution checklist: `.planning/todos/pending/2026-06-16-phase127-00-readpath-execution.md`
- Verified all claims against live DB + code (signal_outcomes dropped, view columns, frame_id join, counterfactual NULL)
</completed_work>

<remaining_work>

Plan 00 execution (NO code written yet):
- Task 1 (CRITICAL — do first): shadow_validator.py:162-178, shadow_auditor.py:86-97
- Task 2: confidence_calibrator.py, weight_updater.py, feature_builder.py, training_data.py
- Task 3: alpha_swarm.py, graduation_analyzer.py, feature_validation_analyzer.py, ml_signal_training_materializer.py
- Task 4: record_execution regime_at_exit (signal_events_repository.py) + retire obsolete scripts (rebuild_signal_ledger.py, reset_pipeline_data.py, signal_quality_audit.py, batch_agent_memory.py, signal_ledger_snapshot.py)

Then the rest of Phase 127:
- Plan 01: before-snapshot + clean replay (hours, backgrounded, can run parallel with Plan 00)
- Plan 02: outcome-free validation report
- Plan 03: ml-training pre-flight (DEFERRED-to-v2.11 record; depends on Plan 00)
</remaining_work>

<decisions_made>

- EDGE + calibration deferred to v2.11 (no outcome target in replay corpus)
- Fire rate descriptive, not gated
- Outcome 8-class -> 2-class = (counterfactual_pnl_r > 0)
- feature_validation IC feature = raw_confidence
- shadow_auditor n=0 -> no demotion
- trade_executions joins on frame_id
See HANDOFF.json for full rationale.
</decisions_made>

<blockers>
- Two live unfixed hazards (shadow_validator crash, shadow_auditor spurious demotion) — execute Plan 00 Task 1 first.
</blockers>

## Required Reading (in order)
1. `.planning/phases/127-clean-replay-validation/127-00-PLAN.md` — the plan to execute (exact SQL rewrites per site, 4 locked decisions, false positives listed)
2. `.planning/todos/pending/2026-06-16-phase127-00-readpath-execution.md` — prioritized execution checklist
3. `.planning/phases/127-clean-replay-validation/127-CONTEXT.md` — methodology constraints
4. `.planning/phases/127-clean-replay-validation/127-RESEARCH.md` — 10 issues incl. no-outcome-data, fire-rate definition, join chain

## Infrastructure State
- Branch: `phase-127/00-read-path-3table-migration` (created this session)
- DB: indicagent @ localhost, `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent`. signal_events ~1.44M, trade_frames ~1.44M, trade_executions 0, counterfactual_pnl_r 0 non-null.
- No background processes running.

<context>
This was a review session that uncovered real debt: Phase 130's "complete" label was true for writes but the read-path migration never happened, leaving enabled services with SQL time-bombs. The Jim Simons move is to fix that debt as a wave-0 precondition (Plan 00) before the clean replay — a corpus isn't ML-ready if its readers crash. All execution work is precisely specified; a fresh session just applies it.
</context>

<next_action>
Start with: execute Plan 00 Task 1 — fix `services/shadow_validator.py:162-178` and `services/shadow_auditor.py:86-97` (the two live hazards), then run `.venv/bin/pytest tests/unit/ -q` and commit atomically. Full rewrites are in 127-00-PLAN.md.
</next_action>
