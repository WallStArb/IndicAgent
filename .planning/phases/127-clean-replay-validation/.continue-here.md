---
context: phase
phase: 127-clean-replay-validation
task: lifecycle-replay-fix
status: blocked
last_updated: 2026-06-17T07:47:53Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **Services are DOWN by design.** `indicagent-intelligence-pipeline` + `indicagent-feature-writer` have `Restart=no` drop-in overrides at `/etc/systemd/system/<svc>.service.d/no-restart.conf`. `indicagent-service-auditor` + `indicagent-self-healing-agent` are STOPPED. Restore only after lifecycle_replay succeeds.
- [ ] CONSTRAINT: **Do NOT re-run the backfill.** It completed successfully (1,036,513 signals / 1,036,513 frames / 4.9M features / 0 orphans). Only `lifecycle_replay.py` needs fixing + re-running.
- [ ] CONSTRAINT: **`--warmup` is a no-op** (memory `project_warmup_noop_finding.md`). Never use it; never assume single-worker is needed for correctness.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Per-table TRUNCATE in reset_pipeline_data.py | One-at-a-time TRUNCATE without CASCADE fails on FK parents; rolls back whole txn | blocking | Use single `TRUNCATE TABLE ... CASCADE` for multi-table wipes |
| Trusting the warmup double-pass | Local plugin_states/intelligence_cache die between passes; warmup forces workers=1 + 2x compute for zero benefit | advisory | Use `--workers 8` without `--warmup` |

<current_state>
Phase 127 clean-slate rebuild. Full wipe done 2026-06-16. Backfill (`--workers 8`, no warmup) COMPLETED overnight (~02:29 UTC): 1,036,513 signals across 114 symbols, 1,036,513 frames (1:1, 0 orphans), 4,917,458 features. `lifecycle_replay.py` then FAILED with `insert or update on table "trade_executions" violates foreign key constraint "fk_trade_executions_frame"` on every symbol/tf. Restart gate correctly aborted → services stayed down, `logs/REBUILD_STATUS=FAILED`. Features/signals/frames are valid and intact; only lifecycle outcomes + service restart remain.
</current_state>

<completed_work>
- Full clean wipe: all 14 signal-derived tables + intelligence_features via single `TRUNCATE ... CASCADE` (reset_pipeline_data.py FK bug bypassed manually)
- Neutralized all auto-restart vectors (auditors stopped, Restart=no drop-ins)
- Discovered + proved `--warmup` is a no-op; relaunched at `--workers 8`
- Backfill COMPLETE: 1.036M signals / 1.036M frames / 4.9M features / 114 symbols / 0 orphans
- Captured 9 findings in `.planning/todos/pending/2026-06-16-replay-issues-and-findings.md`
</completed_work>

<remaining_work>
1. **[BLOCKER] Fix lifecycle_replay FK violation.** Hypothesis (unconfirmed): entry_type divergence. `lifecycle_replay.py:1014,1041` hardcode `_make_frame_id(sid, "at_close")`, but backfill may write frames with other entry_types (`at_pullback`, `at_limit`, `at_reclaim`, `zone_proximal`) → frame_id mismatch → FK violation. CONFIRM FIRST: `SELECT entry_type, count(*) FROM trade_frames GROUP BY 1;`. Fix = read the frame's actual entry_type instead of hardcoding "at_close".
2. **Re-run lifecycle_replay only** (`--workers 8`). No re-backfill.
3. **Restart services:** remove `no-restart.conf` drop-ins, `daemon-reload`, `reset-failed`, start writers + service-auditor + self-healing-agent.
4. **Validate** (checklist in findings todo): CTF non-null, stale feature keys now populate (`volume_sma_20` etc.), status transitions off pending, trade_executions populated.
5. Post-rebuild investigations (findings todo #1, #8, #9): plugin errors on rolled-month contracts; emission-gate stop≈entry coverage gaps.
6. Code cleanups: remove `--warmup` flag; delete obsolete scripts; fix reset_pipeline_data.py FK bug.
</remaining_work>

<decisions_made>
- Full clean slate (wipe all signal-derived + stale features) over scoped/partial — features were stale (0/4.2M rows had `volume_sma_20`/`hmm_probability`/`zone_friction_score`/`rel_volume`)
- `--workers 8` no warmup over `--workers 1 --warmup` — warmup proven no-op; correctness preserved, ~8x faster
- counterfactual_pnl_r stays NULL through rebuild — populated by live alpha_swarm CounterfactualEvaluator (Phase 130), not historical replay
</decisions_made>

<blockers>
- lifecycle_replay FK violation on trade_executions.frame_id — must fix before outcomes compute and services restart
</blockers>

## Required Reading (in order)
1. `.planning/todos/pending/2026-06-16-replay-issues-and-findings.md` — all 9 findings + validation checklist
2. `.planning/todos/pending/2026-06-16-remove-dead-warmup-flag.md` — warmup dead-code removal
3. `~/.claude/projects/-home-bg-dev-indicagent/memory/project_warmup_noop_finding.md` — warmup proof
4. `production/scripts/lifecycle_replay.py:1014,1041` — the hardcoded "at_close" frame_id lookup (the bug)

## Infrastructure State
- **Services DOWN (intentional):** intelligence-pipeline, feature-writer (masked, Restart=no drop-ins); service-auditor, self-healing-agent (stopped). signal-tracker inactive.
- **DB:** clean rebuild data present (signals/frames/features valid); trade_executions EMPTY (lifecycle failed); signal_events.status all still 'pending'
- **REBUILD_STATUS:** `logs/REBUILD_STATUS` = FAILED
- **No background processes running** (chain2/restart2 exited after abort)
- **Sudo:** `echo '!123Angelina' | /usr/bin/sudo.ws -S <cmd>` (sudo.ws required)

<context>
The rebuild's hardest part (full backfill of 116 symbols) succeeded. One bug away from done. The FK violation is systematic (every symbol) → deterministic derivation mismatch, not data loss. entry_type in the frame_id lookup is the prime suspect; fix is likely a one-liner. The safety gate (don't restart on failed lifecycle) worked exactly as designed — production protected.
</context>

<next_action>
Start with: `SELECT entry_type, count(*) FROM trade_frames GROUP BY 1;` to confirm whether non-`at_close` entry_types exist. Then read `lifecycle_replay.py` around lines 1010-1045 to see if frame_id uses hardcoded "at_close" or the frame's actual entry_type. Fix the lookup, re-run `lifecycle_replay.py --workers 8`, then restore services per remaining_work step 3.
</next_action>
