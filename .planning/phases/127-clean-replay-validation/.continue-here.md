---
context: phase
phase: 127-clean-replay-validation
task: reconcile-after-rebuild
status: paused
last_updated: 2026-06-17T08:36:53Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These were discovered through actual failure this session. Acknowledge each before proceeding.

- [ ] CONSTRAINT: **GSD wave 0 collided with a parallel clean-slate rebuild.** My 127-01 executor launched `run_historical_pipeline --clean --replay-only --warmup --workers 1` (PID 1398422). That process is DEAD and the approach is discredited: `--warmup` is a proven no-op (memory `project_warmup_noop_finding.md`). A parallel manual rebuild used `--workers 8` (no warmup) and produced the real corpus. DO NOT re-run my replay. The rebuild is the source of truth.
- [ ] CONSTRAINT: **`--warmup` is a no-op.** Never use it; never assume single-worker is required for correctness. Use `--workers 8`.
- [ ] CONSTRAINT: **Services are DOWN by design.** `intelligence-pipeline` + `feature-writer` have `Restart=no` drop-ins at `/etc/systemd/system/<svc>.service.d/no-restart.conf`; `service-auditor` + `self-healing-agent` are STOPPED. Restore ONLY after rebuild validation passes.
- [ ] CONSTRAINT: **Do NOT re-run the backfill.** It completed (`--workers 8`, no warmup): 1,036,513 signals / 1,036,513 frames / 4.9M features / 0 orphans. Only `lifecycle_replay.py` was re-running (the FK bug was fixed and it is succeeding).
- [ ] CONSTRAINT: **Plans 01/02/03 are entangled with the rebuild** and must be reconciled before continuing `execute-phase` — do not blindly execute them.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Running concurrent replays | My GSD executor launched `--clean --warmup --workers 1` while a parallel rebuild ran `--workers 8`; my process was superseded and used a discredited flag | blocking | Treat the rebuild as the single source of truth; never launch a second replay while one is running; never use `--warmup` |
| Trusting `--warmup` | Forces `--workers 1` + 2x compute for zero benefit (local cache dies between passes) | advisory | Use `--workers 8` without `--warmup` (see memory `project_warmup_noop_finding.md`) |

<current_state>
Phase 127 execute-phase wave 0 paused mid-reconciliation. Plan 127-00 (read-path 3-table migration) is COMPLETE, merged, and fully green (4753 unit tests). Plan 127-01's replay was superseded by a parallel clean-slate rebuild that is healthy and mid-flight: `lifecycle_replay.py --workers 8 --commit-every 500` (PID 1736187) was actively populating trade_executions (587,538 and climbing toward ~1,036,513 at pause time). REBUILD_STATUS=FAILED is stale. Branch: `phase-127/00-read-path-3table-migration`.
</current_state>

<completed_work>

- **127-00 read-path 3-table migration — DONE (durable).** 9 readers migrated off dropped `signal_outcomes` / absent view columns to `counterfactual_pnl_r`. Two live hazards fixed: `shadow_validator` (no longer JOINs dropped table) and `shadow_auditor` (n=0 no-demotion guard). `record_execution` writes `regime_at_exit`. Obsolete `rebuild_signal_ledger.py` deleted. Merged (b492f741).
- **127-00 post-merge gate fixed.** Gate caught 7 test-fixture failures (production migrated, fixtures still asserted old schema). Fixed 3 test files (confidence_calibrator `is_win`, weight_updater V2.11 no-op, graduation `counterfactual_pnl_r` query). 4753 passed. Committed.
- **127-01 baseline snapshot script + JSON — done but superseded** (captured pre-rebuild; rebuild regenerated the corpus).
- **127-01 my replay — SUPERSEDED / DEAD.** Do not re-run.
</completed_work>

<remaining_work>

1. **[HIGH] Let rebuild finish + validate.** Confirm PID 1736187 done and trade_executions ≈ 1,036,513. Run rebuild validation checklist in `.planning/todos/pending/2026-06-16-replay-issues-and-findings.md` (CTF non-null, stale feature keys populate, status off pending, trade_executions populated). This supersedes Plan 02's validation report.
2. **[HIGH] Reconcile Plans 01/02/03 vs the rebuild.** They assumed MY replay produces the corpus; the rebuild did instead. Recommend folding validation into the rebuild's checklist rather than executing the plans as-written. Plan 03 (calibration v2.11 blocker log) is doc-only and still valid.
3. **[HIGH] Restore services** after validation passes: remove `no-restart.conf` drop-ins, `daemon-reload`, `reset-failed`, start writers + service-auditor + self-healing-agent.
4. **Cleanup orphan worktree** `agent-a88695d6c7efc3f22` + its branch (my dead replay's worktree; code already merged, safe to remove).
5. Post-rebuild investigations (findings todo #1, #8, #9): plugin errors on rolled-month contracts; emission-gate stop≈entry coverage gaps.
6. Code cleanups: remove `--warmup` flag; fix `reset_pipeline_data.py` FK (per-table TRUNCATE) bug.
</remaining_work>

<decisions_made>

- 127-00 read-path migration is complete and durable; no further work needed there.
- 127-01 my replay is superseded — the clean rebuild (`--workers 8`, no warmup) is the corpus source of truth.
- counterfactual_pnl_r stays NULL through the rebuild — populated by live `alpha_swarm` CounterfactualEvaluator (Phase 130), not historical replay.
- `--warmup` is a no-op; never use it.
</decisions_made>

<blockers>
- Reconciliation: GSD Plans 01/02/03 collide with the rebuild. Must decide how to proceed before continuing execute-phase.
- Services DOWN by design — restore only after validation.
</blockers>

## Required Reading (in order)
1. `.planning/todos/pending/2026-06-16-replay-issues-and-findings.md` — 9 findings + rebuild validation checklist
2. `~/.claude/projects/-home-bg-dev-indicagent/memory/project_warmup_noop_finding.md` — warmup no-op proof
3. `.planning/phases/127-clean-replay-validation/127-00-SUMMARY.md` — what the completed migration did
4. `.planning/HANDOFF.json` — machine-readable state for /gsd:resume-work

## Infrastructure State
- **DB:** clean rebuild corpus present — signal_events=1,036,513, trade_frames=1,036,513 (1:1, 0 orphans), trade_executions climbing (~587K at pause, target ~1,036,513). All entry_type=at_close.
- **lifecycle_replay PID 1736187:** was RUNNING (`--workers 8 --commit-every 500`) — check if finished on resume.
- **Services DOWN (intentional):** intelligence-pipeline, feature-writer (Restart=no drop-ins); service-auditor, self-healing-agent (stopped).
- **REBUILD_STATUS:** `logs/REBUILD_STATUS` = FAILED (STALE — rebuild is actually succeeding).
- **Orphan worktree:** `.claude/worktrees/agent-a88695d6c7efc3f22` (my dead replay; code merged, safe to remove).
- **Sudo:** `echo '***REDACTED-SUDO-PASSWORD***' | /usr/bin/sudo.ws -S <cmd>`
- **Branch:** `phase-127/00-read-path-3table-migration`

<context>
The GSD execute-phase wave and a parallel manual rebuild ran concurrently and collided. Only 127-00 (read-path migration) produced durable, non-conflicting value — and it passed its post-merge gate cleanly after a test-fixture fix. The replay/validation plans are entangled with the rebuild. The rebuild itself is healthy and mid-flight (lifecycle_replay actively populating trade_executions). The right move on resume is to let the rebuild finish, validate it, restore services, and reconcile/fold the GSD validation plans rather than re-running my superseded replay.
</context>

<next_action>
Start with: check if PID 1736187 finished and `SELECT count(*) FROM trade_executions` ≈ 1,036,513. If yes, run the rebuild validation checklist (`.planning/todos/pending/2026-06-16-replay-issues-and-findings.md`). Then reconcile Plans 01/02/03 against the rebuild (do NOT re-run my replay). The 127-00 read-path migration is DONE.
</next_action>
