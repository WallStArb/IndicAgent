---
phase: 127-clean-replay-validation
plan: 03
type: execute
wave: 3
depends_on: [127-01, 127-02]
requirements: [REPLAY-02]
files_modified:
  - docs/plans/phase-127-calibration-retrain-log.md
autonomous: true

must_haves:
  truths:
    - "Calibration curve retraining has been triggered on the clean replay corpus"
    - "The retraining consumed signal_events + trade_executions (actual_pnl_r) from the 3-table schema"
    - "The retrain outcome is recorded (success/failure, rows consumed, curves produced)"
  artifacts:
    - path: "docs/plans/phase-127-calibration-retrain-log.md"
      provides: "Log of the calibration retrain trigger, input corpus stats, and output curves"
      contains: "retrain"
  key_links:
    - from: "ml-training service"
      to: "signal_events + trade_executions"
      via: "reads clean replay corpus as calibration training input"
      pattern: "actual_pnl_r"
---

<objective>
Trigger calibration curve retraining on the clean Phase 127 replay corpus via the ml-training service, and record the outcome.

Purpose: The clean replay corpus (Plan 01) is the first ML-training-quality data on the corrected 3-table schema. Retraining calibration curves on it operationalizes SC-05 and produces the calibrated confidence surfaces the live pipeline will consume. This closes the v2.10 milestone: architecture changes are not just validated empirically (Plan 02) but also feed back into the calibration model.

Output: A retrain trigger + outcome log at docs/plans/phase-127-calibration-retrain-log.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-clean-replay-validation/127-CONTEXT.md
@.planning/phases/127-clean-replay-validation/127-01-SUMMARY.md
@.planning/phases/127-clean-replay-validation/127-02-SUMMARY.md
</context>

<critical_corrections>
1. **DO NOT write new training code.** ml-training is an existing service (`services/ml_training_agent.py`, oneshot). This plan only TRIGGERS it and RECORDS the outcome. The training pipeline already knows how to read the signal tables.

2. **3-TABLE SCHEMA.** The training corpus is signal_events + trade_frames + trade_executions. calibration pairs use actual_pnl_r (from trade_executions) since counterfactual_pnl_r is NULL in Phase 127 (CounterfactualTracker is v2.11). Confirm the ml-training service reads actual_pnl_r as the calibration target before triggering — if it still references counterfactual_pnl_r or signal_outcomes, that is a Phase 130 migration gap that must be reported, NOT silently worked around.

3. **ml-training is a oneshot service** (D-06). It emits `job_completed_total{job, status}` at exit. Capture that metric or the systemd journal to confirm completion status.
</critical_corrections>

<tasks>

<task type="auto">
  <name>Task 1: Trigger ml-training calibration retrain on clean corpus and record outcome</name>
  <files>docs/plans/phase-127-calibration-retrain-log.md</files>
  <read_first>
    - services/ml_training_agent.py (confirm the training input query — does it read actual_pnl_r from trade_executions or counterfactual_pnl_r from trade_frames? does it reference signal_outcomes? this is the load-bearing pre-flight check)
    - .planning/phases/127-clean-replay-validation/127-01-SUMMARY.md (clean corpus row counts — the training input)
    - CLAUDE.md (ml-training oneshot service; systemctl invocation; INDICAGENT_ENV consistency)
    - docs/operations/operations-infrastructure.md (oneshot service trigger pattern if needed)
  </read_first>
  <action>
    **Pre-flight schema check (FAIL FAST):** Before triggering, grep `services/ml_training_agent.py` (and any module it imports for the calibration training query) for forbidden references:
    - `grep -rnE "signal_outcomes|signal_ledger_full" services/ml_training_agent.py` — if non-empty, this is a Phase 130 migration gap. STOP, report it in the log, and do NOT trigger (training on the wrong schema produces silent wrong calibration). Surface as a blocker.
    - `grep -rn "counterfactual_pnl_r" services/ml_training_agent.py` — if the training target is counterfactual_pnl_r and it is NULL in Phase 127, the retrain would train on all-NULL targets. If so, confirm the service falls back to actual_pnl_r; if it does not, report this as the v2.11 dependency (do NOT trigger a corrupt retrain).

    **Trigger the retrain** (ml-training is a oneshot service). Use the systemd oneshot invocation consistent with CLAUDE.md:
    - `sudo systemctl start indicagent-ml-training.service` (use the exact unit name from `systemctl list-units --all | grep ml-training`; if the name differs, use the actual one).
    - INDICAGENT_ENV must match the running services (per CLAUDE.md gotcha). Confirm with `systemctl show indicagent-ml-training.service -p Environment` if uncertain.

    **Poll for completion:** ml-training is oneshot — it runs to completion and exits. Poll `systemctl status indicagent-ml-training.service` until it shows `inactive (dead)` (the correct terminal state for oneshot ML batch services per CLAUDE.md) or `failed`. Capture the journal tail: `sudo journalctl -u indicagent-ml-training.service -n 200 --no-pager`.

    **Record outcome in `docs/plans/phase-127-calibration-retrain-log.md`:**
    - Pre-flight check results (forbidden-reference grep outputs; training target field)
    - Trigger command + timestamp
    - Completion status (success / failed) + journal tail
    - Input corpus stats (from 127-01-SUMMARY): signal_events count, trade_executions count, actual_pnl_r non-null count
    - Output: calibration curves produced (query setup_performance or the calibration output table the service writes to — confirm via the service's write path; record row count / coverage)
    - If the pre-flight check fails OR the service fails, the log records the blocker and this task is NOT marked done — surface the gap for triage.

    Also verify the retrain populated `setup_performance` (the table CLAUDE.md says drives `perf_multiplier`, repopulated by ml-training): `SELECT COUNT(*), MAX(updated_at) FROM setup_performance` — should show a recent timestamp post-retrain.
  </action>
  <verify>
    - `test -f docs/plans/phase-127-calibration-retrain-log.md` succeeds
    - The log records the pre-flight schema check outputs (forbidden-reference grep results)
    - The log records the trigger command and completion status
    - If the retrain succeeded: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT MAX(updated_at) FROM setup_performance"` shows a timestamp after the retrain trigger
    - If the pre-flight found forbidden references OR counterfactual_pnl_r-as-target with no actual_pnl_r fallback, the log records a blocker and the task is explicitly NOT done (surfaced for triage) — no silent corrupt retrain
  </verify>
  <acceptance_criteria>
    - Pre-flight schema check executed; results recorded
    - If pre-flight clean: ml-training triggered, completed (success or failed status captured), setup_performance updated with a post-trigger timestamp
    - If pre-flight dirty: blocker recorded, no retrain triggered, gap surfaced (no silent wrong calibration)
    - Log captures journal tail, input corpus stats, and output coverage
  </acceptance_criteria>
  <done>Calibration curve retraining has been triggered on the clean 3-table replay corpus with a pre-flight schema check, and the outcome (success or surfaced blocker) is recorded — closing the v2.10 milestone's calibration loop.</done>
</task>

</tasks>

<verification>
- Calibration retrain triggered on the clean corpus (or a pre-flight blocker surfaced)
- No silent corrupt retrain (pre-flight catches schema/target gaps)
- Outcome recorded in docs/plans/phase-127-calibration-retrain-log.md
</verification>

<success_criteria>
- SC-05 satisfied: calibration curves retrained on clean corpus (or blocker surfaced with clear next step)
- Training consumes the 3-table schema with actual_pnl_r as target (counterfactual NULL documented for v2.11)
- Outcome measurable: setup_performance updated timestamp or recorded failure
</success_criteria>

<output>
After completion, create `.planning/phases/127-clean-replay-validation/127-03-SUMMARY.md` recording: the pre-flight schema check result, the trigger command, the completion status, the input corpus stats (signal_events, trade_executions, actual_pnl_r non-null counts), and the output (setup_performance updated timestamp / calibration curve coverage). If a blocker was surfaced, record it as the v2.11 dependency.
</output>
