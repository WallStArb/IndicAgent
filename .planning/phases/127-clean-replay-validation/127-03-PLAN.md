---
phase: 127-clean-replay-validation
plan: 03
type: execute
wave: 3
depends_on: [127-01, 127-02]
requirements: [REPLAY-02]
files_modified:
  - docs/plans/phase-127-calibration-retrain-log.md
autonomous: true

must_haves:
  truths:
    - "Plan 03 is reframed: it SURFACES the v2.11 calibration dependency; it does NOT retrain this phase"
    - "A pre-flight schema/target check runs against services/ml_training_agent.py before any trigger decision"
    - "If the calibration target is counterfactual_pnl_r (NULL this phase) or actual_pnl_r (trade_executions empty), the retrain is NOT triggered and the blocker is recorded"
    - "The outcome (blocker surfaced OR, only if a valid populated target exists, a completed retrain) is recorded"
  artifacts:
    - path: "docs/plans/phase-127-calibration-retrain-log.md"
      provides: "Record of the pre-flight check, the v2.11 calibration blocker, and the dependency chain"
      contains: "DEFERRED"
  key_links:
    - from: "ml-training service"
      to: "counterfactual_pnl_r / actual_pnl_r"
      via: "pre-flight confirms whether a populated PnL target exists for calibration (it does not this phase)"
    pattern: "counterfactual_pnl_r"
---

<objective>
Surface, do not paper over, the calibration-retrain dependency on v2.11. Run a pre-flight check against the ml-training service's calibration target; confirm there is no populated PnL outcome in the Phase 127 corpus; record the blocker. Do NOT trigger a retrain on NULL/empty targets.

Purpose: The clean replay corpus (Plan 01) is structurally clean but carries no PnL outcome - `counterfactual_pnl_r` is NULL by design (CounterfactualTracker is v2.11) and `trade_executions.actual_pnl_r` is absent (replay does not execute trades). Calibrating confidence against an outcome that does not exist would produce a silent-wrong calibration. The Renaissance-correct action is to verify the gap programmatically, record it as the v2.11 dependency, and leave the corpus ML-ready so that the moment v2.11 populates `counterfactual_pnl_r`, calibration can run with no second replay. SC-05 ("calibration curves retrained on clean corpus") is therefore DEFERRED to v2.11 by design - this plan delivers the deferral record, not a fake retrain.

Output: docs/plans/phase-127-calibration-retrain-log.md documenting the pre-flight result and the v2.11 dependency.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-clean-replay-validation/127-CONTEXT.md
@.planning/phases/127-clean-replay-validation/127-RESEARCH.md
@.planning/phases/127-clean-replay-validation/127-01-SUMMARY.md
@.planning/phases/127-clean-replay-validation/127-02-SUMMARY.md
</context>

<critical_corrections>
1. **DO NOT trigger a retrain on an absent target.** ml-training is an existing oneshot service (`services/ml_training_agent.py`). This plan only TRIGGERS it if a populated PnL outcome target exists; otherwise it records the blocker. The training pipeline itself is not modified.

2. **The expected Phase 127 outcome is "blocker surfaced", not "retrain succeeded".** `counterfactual_pnl_r` is NULL (verified in Plan 01/02: 0 non-null in 1.44M rows) and `trade_executions` is empty (0 rows; no INSERT path in the replay). The pre-flight MUST find no valid target. If the pre-flight somehow finds a populated target, investigate before triggering - do not assume.

3. **ml-training is a oneshot service (D-06).** It emits `job_completed_total{job, status}` at exit. If a retrain IS triggered (only with a valid target), capture that metric or the systemd journal to confirm completion. In the expected case, no trigger occurs and there is no job to capture.

4. **3-TABLE SCHEMA.** calibration target lives on `trade_frames.counterfactual_pnl_r` (v2.11) or `trade_executions.actual_pnl_r` (populated only by live execution). If ml-training still references `signal_outcomes` or `signal_ledger_full`, that is a Phase 130 migration gap - report it, do not work around it.
</critical_corrections>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight the ml-training calibration target; surface the v2.11 dependency (do NOT retrain on absent outcome)</name>
  <files>docs/plans/phase-127-calibration-retrain-log.md</files>
  <read_first>
    - services/ml_training_agent.py (confirm the calibration training query: does it read counterfactual_pnl_r from trade_frames, actual_pnl_r from trade_executions, or (legacy) signal_outcomes? This is the load-bearing pre-flight check.)
    - any module ml_training_agent.py imports for the calibration query
    - .planning/phases/127-clean-replay-validation/127-01-SUMMARY.md (clean corpus row counts + the counterfactual_pnl_r NULL confirmation)
    - CLAUDE.md (ml-training oneshot service; systemctl invocation; INDICAGENT_ENV consistency)
  </read_first>
  <action>
    **Pre-flight checks (all must run; record every output in the log):**

    (a) Schema-migration check on the training query:
    - `grep -rnE "signal_outcomes|signal_ledger_full" services/ml_training_agent.py` (and imported modules). If non-empty -> Phase 130 migration gap. Record it; do NOT trigger.

    (b) Calibration target identification:
    - `grep -rnE "counterfactual_pnl_r|actual_pnl_r" services/ml_training_agent.py`. Record which (if either) the service uses as its calibration target.

    (c) Target-population check against the clean replay corpus (the decisive check):
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT COUNT(*), COUNT(counterfactual_pnl_r) FROM trade_frames"` -> expect (N, 0): counterfactual_pnl_r is NULL this phase.
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT COUNT(*) FROM trade_executions"` -> expect 0: no executions from replay.
    - If the service's calibration target is `counterfactual_pnl_r`: target is all-NULL -> retrain would learn nothing. Record blocker, do NOT trigger.
    - If the service's calibration target is `actual_pnl_r`: target table is empty -> same outcome. Record blocker, do NOT trigger.

    **Trigger decision (default = DO NOT TRIGGER in Phase 127):**
    - Only if (a) is clean AND (b) identifies a target AND (c) shows that target is genuinely populated (non-trivial non-null count) does a trigger become permissible. In that case, before triggering, STOP and reconsider - the expectation is no populated target; a populated one means an assumption is wrong and must be understood first.
    - If the expected blocker is found, do NOT call `systemctl start indicagent-ml-training.service`. The v2.11 dependency is the deliverable.

    **Record `docs/plans/phase-127-calibration-retrain-log.md`:**
    - Pre-flight (a)/(b)/(c) outputs verbatim.
    - The trigger decision and rationale (expected: not triggered; target absent).
    - The dependency chain: Phase 127 clean corpus -> [CounterfactualTracker v2.11 populates counterfactual_pnl_r] -> ml-training calibration retrain -> setup_performance / calibration surfaces. State that the corpus is ML-ready now and no second replay is needed once v2.11 lands.
    - Input corpus stats (from 127-01-SUMMARY): signal_events count, trade_frames count, trade_executions count (0), counterfactual_pnl_r non-null count (0).
    - SC-05 status: DEFERRED to v2.11 (by design, not by failure).
    - If a migration gap was found in (a), surface it explicitly for triage.

    **setup_performance timestamp check (informational):** `SELECT COUNT(*), MAX(updated_at) FROM setup_performance`. Note that this table is NOT refreshed in Phase 127 (no retrain); its timestamp reflects the last pre-127 run. Record this so nobody mistakes a stale timestamp for a fresh calibration.
  </action>
  <verify>
    - `test -f docs/plans/phase-127-calibration-retrain-log.md` succeeds
    - The log records all three pre-flight check outputs (a/b/c) verbatim, including the psql counts
    - The log records the trigger decision (expected: NOT triggered) with rationale
    - The log states SC-05 status as DEFERRED to v2.11
    - The log records setup_performance's current MAX(updated_at) with a note that it is stale (not refreshed this phase)
    - If pre-flight (a) found a migration gap, it is surfaced (not silently worked around)
    - No `systemctl start indicagent-ml-training.service` was invoked in the expected (target-absent) case
  </verify>
  <acceptance_criteria>
    - Pre-flight checks executed; all outputs recorded
    - Expected outcome: calibration target is absent (counterfactual_pnl_r NULL, trade_executions empty); retrain NOT triggered; v2.11 dependency recorded as the deliverable; SC-05 marked DEFERRED
    - Corpus confirmed ML-ready (no second replay needed post-v2.11)
    - If (exceptionally) a populated target was found, the trigger was paused for investigation rather than assumed correct
    - No silent corrupt retrain under any branch
  </acceptance_criteria>
  <done>The calibration-retrain dependency on v2.11 has been surfaced programmatically: pre-flight confirms no populated PnL outcome target exists in the Phase 127 replay corpus, the retrain is correctly NOT triggered, SC-05 is marked DEFERRED to v2.11, and the clean corpus is confirmed ML-ready for the moment CounterfactualTracker populates counterfactual_pnl_r - closing the v2.10 milestone without overclaiming calibration it cannot support.</done>
</task>

</tasks>

<verification>
- Pre-flight ran against the actual corpus; results recorded
- No silent corrupt retrain (target-absent case handled by NOT triggering)
- v2.11 dependency and SC-05-DEFERRED recorded in docs/plans/phase-127-calibration-retrain-log.md
</verification>

<success_criteria>
- SC-05 correctly DEFERRED to v2.11 (the honest outcome), not faked
- The dependency chain is documented: clean corpus -> CounterfactualTracker v2.11 -> calibration retrain
- Corpus confirmed ML-ready (no second replay needed)
</success_criteria>

<output>
After completion, create `.planning/phases/127-clean-replay-validation/127-03-SUMMARY.md` recording: the three pre-flight outputs, the trigger decision (not triggered; target absent), SC-05 status = DEFERRED to v2.11, input corpus stats (signal_events, trade_frames, trade_executions=0, counterfactual_pnl_r non-null=0), setup_performance's stale timestamp, and confirmation the corpus is ML-ready for v2.11.
</output>
