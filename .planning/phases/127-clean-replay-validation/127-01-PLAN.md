---
phase: 127-clean-replay-validation
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [REPLAY-02]
files_modified:
  - production/scripts/phase_127_before_snapshot.py
  - docs/plans/phase-127-before-snapshot.json
autonomous: true

must_haves:
  truths:
    - "A before-snapshot JSON exists on disk capturing pre-replay signal volume per setup on the 3-table schema"
    - "The before-snapshot keys are queryable against signal_events + trade_frames (3-table), NOT signal_ledger+signal_outcomes"
    - "A clean replay has been triggered on the full historical corpus with --clean and --warmup (fresh slate, two-pass I1-I6 warmup)"
    - "Post-replay row counts are recorded (signal_events count, trade_frames count, trade_executions count)"
  artifacts:
    - path: "production/scripts/phase_127_before_snapshot.py"
      provides: "Pre-replay baseline capture script targeting 3-table schema"
      contains: "signal_events"
    - path: "docs/plans/phase-127-before-snapshot.json"
      provides: "Machine-readable baseline: per-setup total_signals, selection_rate, context_features coverage, cold-start count"
      contains: "captured_at"
  key_links:
    - from: "phase_127_before_snapshot.py"
      to: "signal_events"
      via: "asyncpg SELECT GROUP BY setup_plugin, is_shadow"
      pattern: "FROM signal_events"
---

<objective>
Capture the pre-replay baseline on the 3-table schema, then trigger a clean historical replay with --warmup on the full corpus.

Purpose: The old phase-121-before-snapshot.json is incompatible with the 3-table schema (it referenced signal_ledger + signal_outcomes, both dropped in Phase 130). Renaissance-grade rigor demands a fresh, apples-to-apples baseline measured on the SAME schema the replay will write to. This baseline is the "before" anchor for the Plan 02 validation report (signal volume delta).

Output: production/scripts/phase_127_before_snapshot.py and docs/plans/phase-127-before-snapshot.json (pre-replay), plus a replay execution log confirming completion.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-clean-replay-validation/127-CONTEXT.md
@.planning/phases/127-clean-replay-validation/127-RESEARCH.md
@.planning/phases/130-script-rewriting/130-06-SUMMARY.md
</context>

<critical_corrections>
1. **3-TABLE SCHEMA ONLY.** All queries target signal_events / trade_frames / trade_executions / signal_ledger (JOIN view). DO NOT reference signal_outcomes, signal_ledger_full, signal_type, feature_tf, bucket_scores, or staleness_score. Those are dropped.

2. **Old phase-121-before-snapshot.json is INVALID.** Do not read or reuse it. Regenerate from scratch on the 3-table schema. Note: the old phase_121_before_snapshot.py and phase_121_report.py scripts are NOT in production/scripts/ (already cleaned up). Build phase_127_before_snapshot.py fresh.

3. **counterfactual_pnl_r is NULL in Phase 127.** CounterfactualTracker is v2.11. The before-snapshot must NOT depend on counterfactual_pnl_r being populated. Use actual_pnl_r (from trade_executions) where a pnl metric is needed, and record NULL-coverage as a known field.

4. **cold-start signals are NOT penalized.** ROADMAP SC-02 requires > 99% context_features coverage for NON-cold-start signals. The before-snapshot must record: (a) cold_start_count (context_features IS NULL or '{}'::jsonb), (b) non_cold_start_total, (c) coverage_pct computed on the non-cold-start subset only.

5. **--warmup IS A REAL CLI FLAG (verified).** `run_historical_pipeline.py` exposes `--warmup` (action="store_true", line 1991) which triggers a two-pass replay: Pass 1 I1-I6 only (skip_signals=True) builds the per-symbol I6 cache; Pass 2 I1-I7 emits signals with warm CTF scores (no cold-start NULLs). Two hard constraints enforced by the script: (a) `--warmup` REQUIRES `--replay-only` (line 2003-2005, exits 1 otherwise); (b) `--warmup` is ONLY honored with `--workers 1` — parallel mode silently prints "NOTE: --warmup is only supported with --workers 1 (parallel mode skips warmup pass)" and skips it (line 2447). Therefore the replay command MUST use `--workers 1`.
</critical_corrections>

<tasks>

<task type="auto">
  <name>Task 1: Create phase_127_before_snapshot.py — capture pre-replay baseline on 3-table schema</name>
  <files>production/scripts/phase_127_before_snapshot.py, docs/plans/phase-127-before-snapshot.json</files>
  <read_first>
    - production/scripts/run_historical_pipeline.py (lines 1892-1985 — argparse conventions, asyncpg/DatabaseManager usage, OTel oneshot pattern; look for init_otel_providers / JOB_COMPLETED_TOTAL / flush_and_shutdown_metrics usage in this file as the canonical oneshot exit template)
    - production/scripts/lifecycle_replay.py (asyncpg + DatabaseManager connection pool pattern used post-130 migration)
    - production/migrations/ (find the migration that created signal_events/trade_frames/trade_executions — confirm exact column names: signal_events.{signal_id, ts, setup_plugin, is_shadow, raw_confidence, context_features, ctf_score, ctf_confirmed, zone_friction_score, status, direction}; trade_frames.{frame_id, signal_id, entry_type, entry_price, stop_price, target_price, counterfactual_pnl_r, was_selected, frame_details}; trade_executions.{execution_id, signal_id, actual_pnl_r, actual_fill_price, exit_reason})
    - services/shadow_validator.py (lines ~53-84 — _SHADOW_VALIDATION_SETUPS frozenset for cluster derivation)
  </read_first>
  <action>
    Create `production/scripts/phase_127_before_snapshot.py` as a oneshot asyncpg script (NOT a daemon). Use DatabaseManager from src/core/database_manager.py for the connection pool. Module docstring: "Pre-replay baseline capture for Phase 127 clean replay. Targets 3-table schema (signal_events/trade_frames/trade_executions). Captures the 'before' anchor for the validation report's signal volume delta."

    Implement `_capture_baseline(conn) -> dict` returning a JSON-serializable dict with this exact structure:
    ```json
    {
      "captured_at": "<ISO UTC>",
      "schema": "3-table (signal_events/trade_frames/trade_executions)",
      "totals": {
        "signal_events": <int>,
        "trade_frames": <int>,
        "trade_executions": <int>
      },
      "cold_start": {
        "cold_start_count": <int>,
        "non_cold_start_total": <int>,
        "context_features_coverage_pct": <float 0-100>
      },
      "setups": [
        {
          "setup_plugin": "<str>",
          "is_shadow": <bool>,
          "total_signals": <int>,
          "selected": <int>,
          "selection_rate_pct": <float>,
          "context_features_coverage_pct": <float, computed on non-cold-start subset>,
          "cold_start_count": <int>,
          "stopped_at_entry_count": <int>
        }
      ]
    }
    ```

    Concrete queries (all against 3-table schema):
    - Totals: `SELECT COUNT(*) FROM signal_events`, same for trade_frames, trade_executions.
    - Per-setup signal counts: `SELECT setup_plugin, is_shadow, COUNT(*) AS total_signals FROM signal_events GROUP BY setup_plugin, is_shadow`.
    - Per-setup selection (selected = trade_frames with was_selected=true joined to signal_events): `SELECT se.setup_plugin, se.is_shadow, COUNT(*) FILTER (WHERE tf.was_selected) AS selected FROM signal_events se LEFT JOIN trade_frames tf ON tf.signal_id = se.signal_id GROUP BY se.setup_plugin, se.is_shadow`. selection_rate_pct = selected/total_signals*100. (NOTE: selection_rate is NOT the firing rate - see Plan 02 for the emission fire-rate metric. Capture it here for completeness only; it is not a success gate.)
    - Per-setup cold-start: `COUNT(*) FILTER (WHERE se.context_features IS NULL OR se.context_features = '{}'::jsonb)`. non_cold_start_total = total_signals - cold_start_count. coverage_pct = (non_cold_start_with_features / non_cold_start_total)*100 where "with_features" = context_features IS NOT NULL AND context_features != '{}'::jsonb.
    - Per-setup stopped_at_entry: the join chain is `signal_events -> trade_frames -> trade_executions` (trade_executions has NO signal_id; it joins on frame_id): `SELECT se.setup_plugin, COUNT(*) FILTER (WHERE te.exit_reason='stopped_at_entry') AS stopped_at_entry_count FROM signal_events se LEFT JOIN trade_frames tf ON tf.signal_id = se.signal_id LEFT JOIN trade_executions te ON te.frame_id = tf.frame_id GROUP BY se.setup_plugin`. EXPECTED VALUE: 0 in this phase - replay does not populate trade_executions, so this is structurally zero and carries no information (see 127-RESEARCH.md Issue 8). Capture it as a known-zero field for schema completeness; do NOT interpret 0 as "leak fixed".

    Use a single asyncpg connection from the pool; run queries sequentially. Use `format_iso_ts(datetime.now(UTC))` from src/core/service_utils.py for captured_at (NEVER `datetime.now()` — DAG invariant 6).

    Oneshot exit: emit `JOB_COMPLETED_TOTAL.add(1, {"job": "phase-127-before-snapshot", "status": "success"})` (or "failure" on exception); wrap OTel init in try/except OTelInitError guard; call flush_and_shutdown_metrics() in finally. Match the exact pattern used in run_historical_pipeline.py's main().

    Write the JSON to `docs/plans/phase-127-before-snapshot.json` with `json.dumps(baseline, indent=2, default=str)`.

    Then RUN `.venv/bin/python production/scripts/phase_127_before_snapshot.py` and confirm the JSON is written and totals are non-zero (assuming pre-replay data exists; if signal_events is already empty from a prior clean, that is a valid zero baseline — record zeros honestly).
  </action>
  <verify>
    - `.venv/bin/python -c "import ast; ast.parse(open('production/scripts/phase_127_before_snapshot.py').read())"` exits 0
    - `.venv/bin/ruff check production/scripts/phase_127_before_snapshot.py` exits 0
    - `grep -n "FROM signal_events" production/scripts/phase_127_before_snapshot.py` returns matches
    - `grep -nE "signal_outcomes|signal_ledger_full|signal_type|bucket_scores|staleness_score" production/scripts/phase_127_before_snapshot.py` returns ZERO matches (forbidden columns absent)
    - `grep -n "counterfactual_pnl_r" production/scripts/phase_127_before_snapshot.py` returns ZERO matches or only a comment noting NULL — the baseline must NOT depend on it
    - `grep -n "JOB_COMPLETED_TOTAL" production/scripts/phase_127_before_snapshot.py` returns the oneshot emission
    - After run: `test -f docs/plans/phase-127-before-snapshot.json` succeeds
    - `.venv/bin/python -c "import json; d=json.load(open('docs/plans/phase-127-before-snapshot.json')); assert 'setups' in d and 'totals' in d and 'cold_start' in d, d.keys()"` exits 0
  </verify>
  <acceptance_criteria>
    - phase_127_before_snapshot.py exists, parses, passes ruff
    - All queries target signal_events/trade_frames/trade_executions (no dropped tables/columns)
    - JSON written with totals, cold_start, and per-setup sections
    - context_features_coverage_pct is computed on the non-cold-start subset only (cold-start excluded, not penalized)
    - Baseline does not depend on counterfactual_pnl_r (NULL in Phase 127)
    - captured_at is a UTC ISO timestamp via format_iso_ts
  </acceptance_criteria>
  <done>A pre-replay baseline JSON exists on the 3-table schema, capturing per-setup signal volume, selection rates, cold-start counts, and context_features coverage — the "before" anchor for the Plan 02 validation report.</done>
</task>

<task type="auto">
  <name>Task 2: Execute clean replay with --warmup on the full historical corpus (backgrounded, single-worker two-pass)</name>
  <files>docs/plans/phase-127-replay-log.md</files>
  <read_first>
    - production/scripts/run_historical_pipeline.py (lines 30-60 usage example; lines 1990-2005 the `--warmup` flag definition and the `--warmup requires --replay-only` enforcement; lines 2396-2448 the do_warmup branch — confirms warmup only runs when `args.workers == 1`, else prints the skip note at line 2447; line 2409 the warmup log marker "Running warmup pass (I1-I6 only)")
    - .planning/phases/127-clean-replay-validation/127-RESEARCH.md (Issue 5 — determinism: uuid5 frame_id, G0 grouping)
    - production/scripts/lifecycle_replay.py (verify_replay / _assert_backfill_integrity — the post-replay integrity checks already built in)
  </read_first>
  <action>
    Execute the clean replay. The scripts are ALREADY migrated to 3-table in Phase 130-06 — DO NOT modify them. Only run them and capture output.

    Replay command (full historical corpus, fresh slate, two-pass warmup):
    ```
    .venv/bin/python production/scripts/run_historical_pipeline.py \
      --replay-only \
      --warmup \
      --clean \
      --include-rolled \
      --timeframes 1m,5m,15m,1h,4h,1d \
      --workers 1
    ```
    Rationale for each flag:
    - `--replay-only`: market_data_ohlcv is already backfilled; skip IBKR fetch. ALSO REQUIRED BY `--warmup` (run_historical_pipeline.py line 2003-2005 enforces this — exits 1 otherwise).
    - `--warmup`: REAL CLI flag (action="store_true", line 1991). Triggers a two-pass replay: Pass 1 I1-I6 only (skip_signals=True) builds the per-symbol I6 cache so no cold-start NULL CTF values remain; Pass 2 I1-I7 emits signals against the warm I6 cache. This is the cold-start correction the phase goal calls out as "with --warmup".
    - `--clean`: delete existing signal_events/trade_frames/trade_executions (Phase 130-06 updated the clean path to DELETE trade_executions → trade_frames → signal_events in FK order) before replay. Guarantees a fresh, deterministic corpus.
    - `--include-rolled`: replay the full roll-chain history (all expired front-month contracts), not just current is_front_month. Renaissance demands complete historical validation.
    - `--timeframes`: all 6 TFs.
    - `--workers 1`: MANDATORY for `--warmup`. The script only honors `--warmup` in the single-worker branch (line 2398/2403); with any other worker count it prints "NOTE: --warmup is only supported with --workers 1 (parallel mode skips warmup pass)" (line 2447) and silently skips the warmup pass, defeating the purpose. Do NOT raise `--workers`.

    BACKGROUNDING (load-bearing): Full historical corpus × all symbols × 6 TFs × single-worker two-pass replay is HOURS of compute. Launch the replay in the background so it survives across turns, then poll:
    - Launch: `nohup .venv/bin/python production/scripts/run_historical_pipeline.py --replay-only --warmup --clean --include-rolled --timeframes 1m,5m,15m,1h,4h,1d --workers 1 > docs/plans/phase-127-replay-log.md.raw 2>&1 &` (or use the Bash tool's `run_in_background: true`). Capture the PID.
    - Poll across turns: check the process is alive (`kill -0 <PID>`), and tail the raw log for progress markers (`"<symbol> [warmup]:"` per symbol in pass 1, then `"<symbol>:"` per symbol in pass 2, then `"Warmup complete. Running signal pass..."`).
    - Treat a process that dies before completion as a hard failure — capture the traceback and STOP. The integrity gates below will catch a partial corpus (orphan rows / short counts) and must NOT be papered over.

    Capture stdout+stderr to `docs/plans/phase-127-replay-log.md` as a fenced code block at the top, followed by a short summary section recording:
    - Start time / end time (UTC)
    - Final row counts: `SELECT COUNT(*) FROM signal_events`, trade_frames, trade_executions
    - Whether the process exited 0 or errored (if errored, capture the traceback and STOP — do not proceed to Plan 02 with a partial corpus)

    After replay completes, run the built-in integrity check from lifecycle_replay.py if it exposes a standalone verify mode (check `grep -n "verify\|_assert" production/scripts/lifecycle_replay.py`); otherwise run these smoke queries directly:
    - `SELECT COUNT(*) FROM signal_events se WHERE NOT EXISTS (SELECT 1 FROM trade_frames tf WHERE tf.signal_id = se.signal_id)` — orphan signal_events (should be 0; every detection row must have >=1 frame per G0)
    - `SELECT COUNT(*) FROM signal_events WHERE ts > now()` — future-dated signals (should be 0; replay is historical)

    Record these smoke-query results in the replay log summary.
  </action>
  <verify>
    - `test -f docs/plans/phase-127-replay-log.md` succeeds
    - `grep -n "Running warmup pass (I1-I6 only)" docs/plans/phase-127-replay-log.md` returns a match — confirms the warmup pass ACTUALLY RAN (not silently skipped by parallel mode)
    - `grep -n "Warmup complete. Running signal pass" docs/plans/phase-127-replay-log.md` returns a match — confirms pass 2 (signal emission) executed after warmup
    - `grep -c "only supported with --workers 1 (parallel mode skips warmup pass)" docs/plans/phase-127-replay-log.md` returns 0 — if this NOTE appears, warmup was skipped and the task FAILS (halt and re-run with --workers 1)
    - The replay process exited 0 (grep the log for the final exit status; if it shows an exception/traceback, this task FAILS — halt and surface the error)
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT COUNT(*) FROM signal_events"` returns a non-zero count (corpus populated)
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT COUNT(*) FROM signal_events se WHERE NOT EXISTS (SELECT 1 FROM trade_frames tf WHERE tf.signal_id = se.signal_id)"` returns 0 (G0 integrity — no orphan signal_events)
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT COUNT(*) FROM trade_frames tf WHERE NOT EXISTS (SELECT 1 FROM signal_events se WHERE se.signal_id = tf.signal_id)"` returns 0 (no orphan trade_frames)
  </verify>
  <acceptance_criteria>
    - Clean replay completed with exit code 0 on the full historical corpus (--include-rolled, all 6 TFs)
    - Replay log contains the "Running warmup pass (I1-I6 only)" and "Warmup complete. Running signal pass" markers — the two-pass warmup actually executed (single-worker enforced)
    - Replay log does NOT contain the "parallel mode skips warmup pass" NOTE (warmup was not silently dropped)
    - signal_events, trade_frames, trade_executions all have non-zero row counts
    - Zero orphan signal_events (every detection row has >=1 frame — G0 grouping holds)
    - Zero orphan trade_frames (no frames without a parent signal_event)
    - Replay log records start/end time and final row counts
    - If the process errors, the traceback is captured and the plan halts (no partial-corpus reporting downstream)
  </acceptance_criteria>
  <done>A clean, deterministic historical replay has executed on the 3-table schema with the two-pass --warmup (I1-I6 cache build, then I1-I7 signal emission) actually honored under single-worker mode, leaving zero orphaned records and populating signal_events/trade_frames/trade_executions as the clean ML training corpus.</done>
</task>

</tasks>

<verification>
- phase_127_before_snapshot.py passes ast.parse and ruff, targets only 3-table schema
- docs/plans/phase-127-before-snapshot.json exists with totals + cold_start + per-setup sections
- Clean replay ran to completion (exit 0) on full historical corpus with --warmup honored (single-worker)
- G0 integrity holds: zero orphan signal_events, zero orphan trade_frames
</verification>

<success_criteria>
- Pre-replay baseline JSON is on the 3-table schema (the old phase-121 baseline is superseded)
- Clean replay produced a non-empty, integrity-clean corpus in signal_events/trade_frames/trade_executions
- The two-pass --warmup actually ran (log markers present, no parallel-mode skip NOTE)
- The corpus is ready for the Plan 02 validation report and Plan 03 calibration retraining
</success_criteria>

<output>
After completion, create `.planning/phases/127-clean-replay-validation/127-01-SUMMARY.md` recording: the before-snapshot totals (signal_events/trade_frames/trade_executions counts, cold-start count, context_features coverage %), the replay start/end times and final post-replay row counts, confirmation that the --warmup two-pass markers appeared in the log, the G0 integrity smoke-query results (orphan counts = 0), and confirmation that the corpus is clean and ready for Plan 02.
</output>
