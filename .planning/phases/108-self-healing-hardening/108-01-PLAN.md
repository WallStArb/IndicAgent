---
phase: 108-self-healing-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/observability/metrics.py
  - src/core/agent/base.py
  - requirements.txt
autonomous: true
requirements:
  - HEAL-01

must_haves:
  truths:
    - "BaseAgent emits watchdog_notify_total counter on every successful sd_notify ping"
    - "BaseAgent emits watchdog_notify_suppressed_total counter when ping is suppressed"
    - "DLQ_QUARANTINE_TOTAL, CONSUMER_STALL_DETECTED_TOTAL, JOB_COMPLETED_TOTAL, API_HEALTH instruments are importable from src.observability.metrics"
    - "opentelemetry-instrumentation-fastapi is declared in requirements.txt"
  artifacts:
    - path: "src/core/agent/base.py"
      provides: "WATCHDOG_NOTIFY_TOTAL and WATCHDOG_NOTIFY_SUPPRESSED_TOTAL counters wired into _watchdog_notify()"
      contains: "WATCHDOG_NOTIFY_TOTAL"
    - path: "src/observability/metrics.py"
      provides: "DLQ_QUARANTINE_TOTAL, CONSUMER_STALL_DETECTED_TOTAL, JOB_COMPLETED_TOTAL, API_HEALTH"
      contains: "DLQ_QUARANTINE_TOTAL"
    - path: "requirements.txt"
      provides: "opentelemetry-instrumentation-fastapi dependency"
      contains: "opentelemetry-instrumentation-fastapi"
  key_links:
    - from: "src/core/agent/base.py::_watchdog_notify"
      to: "WATCHDOG_NOTIFY_TOTAL.add / WATCHDOG_NOTIFY_SUPPRESSED_TOTAL.add"
      via: "increment inside if/else branch of should_notify"
      pattern: "WATCHDOG_NOTIFY_(TOTAL|SUPPRESSED_TOTAL)\\.add"
    - from: "downstream plans (03, 04, 05)"
      to: "src/observability/metrics.py"
      via: "from src.observability.metrics import <NEW_COUNTER>"
      pattern: "from src\\.observability\\.metrics import"
---

<objective>
Establish the OTel foundation layer for Phase 108: define every new OTel instrument
in `src/observability/metrics.py`, add the two watchdog counters to BaseAgent so all
39 daemons inherit them, and declare the new FastAPI instrumentation dependency.

Purpose: Every downstream plan (03 DLQ quarantine, 04 ServiceAuditor + Pipeline,
05 FastAPI) imports counters/gauges from these two files. Centralizing them in
Wave 1 unblocks Wave 2 parallelism.

Output: New OTel instruments registered, BaseAgent watchdog visibility live for
every subclass, requirements.txt updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/108-self-healing-hardening/108-CONTEXT.md
@.planning/phases/108-self-healing-hardening/108-RESEARCH.md
@.planning/phases/108-self-healing-hardening/108-PATTERNS.md
@src/observability/metrics.py
@src/core/agent/base.py
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new OTel instruments to src/observability/metrics.py</name>
  <files>src/observability/metrics.py</files>
  <read_first>
    - src/observability/metrics.py (current instrument blocks; module-level `_meter` at line 24; existing SERVICE_AUDITOR_SERVICE_RESTARTS_TOTAL pattern at lines 302-309; existing DLQ_MESSAGES_TOTAL block around line 324)
    - .planning/phases/108-self-healing-hardening/108-PATTERNS.md (section: src/observability/metrics.py — new instruments — for exact instrument names, descriptions, and section grouping)
    - .planning/phases/108-self-healing-hardening/108-CONTEXT.md (D-04, D-14, D-21, D-24, D-06 — instrument naming and label-key contract)
  </read_first>
  <action>
    Append four new OTel instruments at module level in src/observability/metrics.py, each in its own commented section after the existing DLQ metrics block:

    1. `DLQ_QUARANTINE_TOTAL` — `_meter.create_counter` named `"dlq_quarantine_total"`, description: "DLQ messages quarantined after DLQ_MAX_RETRIES identical errors in 24h". Section header comment: "DLQ quarantine metrics (Phase 108)".
    2. `CONSUMER_STALL_DETECTED_TOTAL` — `_meter.create_counter` named `"consumer_stall_detected_total"`, description: "Consumer stall events detected by ServiceAuditor before restart". Section header comment: "Consumer stall detection (Phase 108)".
    3. `JOB_COMPLETED_TOTAL` — `_meter.create_counter` named `"job_completed_total"`, description: "Oneshot job completions by name and status". Section header comment: "Oneshot job completion counters (Phase 108)".
    4. `API_HEALTH` — `_meter.create_gauge` named `"api_health"`, description: "API DB connectivity: 1=reachable, 0=unreachable". Section header comment: "API health gauge (Phase 108)".

    Use `_meter.create_counter` / `_meter.create_gauge` directly (matches existing module-level pattern). Do NOT use `_base_meter` (that is reserved for src/core/agent/base.py per Pitfall 1 in RESEARCH.md). Do NOT add per-instrument labels in this file (labels are supplied at call sites). Instrument names must match exactly — they are referenced by downstream plans.
  </action>
  <verify>
    `python -c "from src.observability.metrics import DLQ_QUARANTINE_TOTAL, CONSUMER_STALL_DETECTED_TOTAL, JOB_COMPLETED_TOTAL, API_HEALTH; print('ok')"` prints `ok` with no ImportError.
    `grep -E '"(dlq_quarantine_total|consumer_stall_detected_total|job_completed_total|api_health)"' src/observability/metrics.py` returns 4 lines.
  </verify>
  <done>All four instruments are defined exactly once at module level and importable.</done>
  <acceptance_criteria>
    - src/observability/metrics.py contains `DLQ_QUARANTINE_TOTAL = _meter.create_counter(`
    - src/observability/metrics.py contains `CONSUMER_STALL_DETECTED_TOTAL = _meter.create_counter(`
    - src/observability/metrics.py contains `JOB_COMPLETED_TOTAL = _meter.create_counter(`
    - src/observability/metrics.py contains `API_HEALTH = _meter.create_gauge(`
    - Each instrument string name appears exactly once: `dlq_quarantine_total`, `consumer_stall_detected_total`, `job_completed_total`, `api_health`
    - Import test: `python -c "from src.observability.metrics import DLQ_QUARANTINE_TOTAL, CONSUMER_STALL_DETECTED_TOTAL, JOB_COMPLETED_TOTAL, API_HEALTH"` exits 0
    - No duplicate instrument warnings appear when running `python -c "import src.observability.metrics"`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add WATCHDOG_NOTIFY_TOTAL and WATCHDOG_NOTIFY_SUPPRESSED_TOTAL to BaseAgent._watchdog_notify()</name>
  <files>src/core/agent/base.py</files>
  <read_first>
    - src/core/agent/base.py (current `_watchdog_notify()` at ~lines 349-372; existing `AGENT_CRASH_TOTAL` module-level instrument around lines 52-55; `self._last_msg_ts_attrs` attribute definition at ~line 120)
    - .planning/phases/108-self-healing-hardening/108-PATTERNS.md (section: `src/core/agent/base.py` — exact placement, `_base_meter` usage, and label-attr selection)
    - .planning/phases/108-self-healing-hardening/108-RESEARCH.md (Pitfall 1: OTel duplicate instrument registration — _base_meter vs _meter split)
  </read_first>
  <action>
    Modify src/core/agent/base.py in two locations:

    1. Module-level (place immediately after the existing `AGENT_CRASH_TOTAL = _base_meter.create_counter(...)` block, using the same `_base_meter`):
       - Add `WATCHDOG_NOTIFY_TOTAL` counter named `"watchdog_notify_total"`, description: "Successful sd_notify WATCHDOG=1 pings per agent".
       - Add `WATCHDOG_NOTIFY_SUPPRESSED_TOTAL` counter named `"watchdog_notify_suppressed_total"`, description: "Suppressed watchdog pings (agent alive but idle/stalled) per agent".
       - Use `_base_meter.create_counter(...)` — do NOT import from `src/observability/metrics.py`. The split is intentional per Pitfall 1.

    2. Inside `_watchdog_notify()`, in the existing `while self.running:` loop:
       - Inside the `if should_notify:` branch (immediately after `notifier.notify("WATCHDOG=1")`): call `WATCHDOG_NOTIFY_TOTAL.add(1, self._last_msg_ts_attrs)`.
       - Add an `else:` branch (paired with `if should_notify:`) that calls `WATCHDOG_NOTIFY_SUPPRESSED_TOTAL.add(1, self._last_msg_ts_attrs)`.
       - Label attribute MUST be `self._last_msg_ts_attrs` (already `{"agent_id": name}` per CLAUDE.md rule "label key is agent_id").
       - Do NOT change any other behavior of `_watchdog_notify()` — early-return, interval calculation, `should_notify` computation, and `await asyncio.sleep(interval_s)` remain identical.

    When `max_idle_seconds == 0`, `should_notify` stays True forever and the suppressed counter never increments — this is correct (no behavior change).
  </action>
  <verify>
    `grep -n "WATCHDOG_NOTIFY_TOTAL\|WATCHDOG_NOTIFY_SUPPRESSED_TOTAL" src/core/agent/base.py` returns at least 4 lines (2 definitions, 2 call sites).
    `.venv/bin/pytest tests/unit/ -q -k "base_agent or watchdog"` exits 0 (or runs cleanly with no new failures vs main).
    `python -c "from src.core.agent.base import WATCHDOG_NOTIFY_TOTAL, WATCHDOG_NOTIFY_SUPPRESSED_TOTAL; print('ok')"` prints `ok`.
  </verify>
  <done>BaseAgent._watchdog_notify() increments WATCHDOG_NOTIFY_TOTAL on every ping and WATCHDOG_NOTIFY_SUPPRESSED_TOTAL on every suppressed ping; all subclasses inherit automatically.</done>
  <acceptance_criteria>
    - src/core/agent/base.py contains `WATCHDOG_NOTIFY_TOTAL = _base_meter.create_counter(`
    - src/core/agent/base.py contains `WATCHDOG_NOTIFY_SUPPRESSED_TOTAL = _base_meter.create_counter(`
    - src/core/agent/base.py contains both strings `"watchdog_notify_total"` and `"watchdog_notify_suppressed_total"`
    - src/core/agent/base.py `_watchdog_notify` body contains `WATCHDOG_NOTIFY_TOTAL.add(1, self._last_msg_ts_attrs)`
    - src/core/agent/base.py `_watchdog_notify` body contains `WATCHDOG_NOTIFY_SUPPRESSED_TOTAL.add(1, self._last_msg_ts_attrs)`
    - The `if should_notify:` block still calls `notifier.notify("WATCHDOG=1")` exactly once per iteration
    - `.venv/bin/pytest tests/unit/ -q` exits 0 (no new unit test regressions)
    - `.venv/bin/ruff check src/core/agent/base.py` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add opentelemetry-instrumentation-fastapi to requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (existing opentelemetry-* lines for version-pin convention)
    - .planning/phases/108-self-healing-hardening/108-RESEARCH.md (Standard Stack: New Dependencies — version `>=0.45b0`)
  </read_first>
  <action>
    Append a single line to requirements.txt declaring `opentelemetry-instrumentation-fastapi>=0.45b0`. Place it adjacent to the existing `opentelemetry-instrumentation*` block to keep grouping consistent. Do not change any other dependency line. Do not regenerate the file. Do not run `pip install` yet (Plan 05 installs and wires the instrumentor).
  </action>
  <verify>
    `grep -E '^opentelemetry-instrumentation-fastapi' requirements.txt` returns exactly one line containing `>=0.45b0`.
  </verify>
  <done>Dependency is declared so Plan 05 (and CI) can `pip install -r requirements.txt` and import `FastAPIInstrumentor`.</done>
  <acceptance_criteria>
    - requirements.txt contains exactly one line matching `^opentelemetry-instrumentation-fastapi>=0\.45b0$`
    - No other lines in requirements.txt are modified (verify via `git diff requirements.txt` shows only one added line)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/ -q` is green
- `.venv/bin/ruff check src/observability/metrics.py src/core/agent/base.py` is clean
- `python -c "from src.observability.metrics import DLQ_QUARANTINE_TOTAL, CONSUMER_STALL_DETECTED_TOTAL, JOB_COMPLETED_TOTAL, API_HEALTH; from src.core.agent.base import WATCHDOG_NOTIFY_TOTAL, WATCHDOG_NOTIFY_SUPPRESSED_TOTAL"` exits 0
- No duplicate-instrument OTel SDK warnings at import time
</verification>

<success_criteria>
1. Four new instruments importable from `src/observability/metrics.py`
2. Two new watchdog counters defined in `src/core/agent/base.py` and wired into `_watchdog_notify()`
3. requirements.txt declares `opentelemetry-instrumentation-fastapi>=0.45b0`
4. Unit tests pass; lint clean; no OTel SDK warnings
</success_criteria>

<output>
After completion, create `.planning/phases/108-self-healing-hardening/108-01-SUMMARY.md`.
</output>
