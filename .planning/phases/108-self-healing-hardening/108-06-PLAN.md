---
phase: 108-self-healing-hardening
plan: 06
type: execute
wave: 2
depends_on:
  - 108-01
files_modified:
  - services/ml_training_agent.py
  - services/shadow_auditor_agent.py
  - production/scripts/roll_batch.py
autonomous: true
requirements:
  - HEAL-03

must_haves:
  truths:
    - "ml_training_agent emits job_completed_total{job='ml-training', status='success'} on clean exit and status='failure' on exception"
    - "shadow_auditor_agent emits job_completed_total{job='shadow-auditor', status='success|failure'} at script termination"
    - "roll_batch.py emits job_completed_total{job='roll-batch', status='success|failure'} at script termination"
    - "Job label values match the systemd unit suffix exactly (no underscores, no prefix)"
  artifacts:
    - path: "services/ml_training_agent.py"
      provides: "JOB_COMPLETED_TOTAL.add() calls in both success and except paths"
      contains: "JOB_COMPLETED_TOTAL"
    - path: "services/shadow_auditor_agent.py"
      provides: "JOB_COMPLETED_TOTAL.add() calls in both success and except paths"
      contains: "JOB_COMPLETED_TOTAL"
    - path: "production/scripts/roll_batch.py"
      provides: "JOB_COMPLETED_TOTAL.add() calls in both success and except paths"
      contains: "JOB_COMPLETED_TOTAL"
  key_links:
    - from: "oneshot script exits"
      to: "src.observability.metrics.JOB_COMPLETED_TOTAL"
      via: ".add(1, {'job': '<name>', 'status': 'success|failure'})"
      pattern: "JOB_COMPLETED_TOTAL\\.add"
---

<objective>
Add the three-line oneshot completion counter contract (D-06) to ml_training, shadow_auditor,
and roll_batch. Grafana can then alert if any oneshot has not emitted a success in >25h
even when systemd reports `inactive (dead)` correctly (which is normal between runs).

Purpose: HEAL-03 closes one more visibility gap — timer-triggered scripts are invisible
to RED dashboards unless they emit a single counter at exit.

Output: Three oneshot scripts emit JOB_COMPLETED_TOTAL on every run with status label.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/108-self-healing-hardening/108-CONTEXT.md
@.planning/phases/108-self-healing-hardening/108-RESEARCH.md
@.planning/phases/108-self-healing-hardening/108-PATTERNS.md
@services/ml_training_agent.py
@services/shadow_auditor_agent.py
@production/scripts/roll_batch.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JOB_COMPLETED_TOTAL to ml_training_agent.py and shadow_auditor_agent.py</name>
  <files>services/ml_training_agent.py, services/shadow_auditor_agent.py</files>
  <read_first>
    - services/ml_training_agent.py (locate the top-level entrypoint — typically `if __name__ == "__main__":` or an async main() — and the outermost try/except that wraps the whole run; identify where the script exits successfully vs raises)
    - services/shadow_auditor_agent.py (same — identify the script's top-level try/except and clean-exit point before resource teardown)
    - .planning/phases/108-self-healing-hardening/108-PATTERNS.md (section: Oneshot scripts — job_completed_total counter — exact label values and placement)
    - src/observability/metrics.py (verify JOB_COMPLETED_TOTAL exported from Plan 01)
    - production/systemd/indicagent-ml-training.service (confirm job label `ml-training` matches the unit suffix; the value must be exactly `ml-training`, not `ml_training`)
    - production/systemd/indicagent-shadow-auditor.service (confirm job label `shadow-auditor`)
  </read_first>
  <action>
    For services/ml_training_agent.py:

    1. Add import near other src.observability imports: `from src.observability.metrics import JOB_COMPLETED_TOTAL`.

    2. Wrap the existing top-level script body (the code that runs under `if __name__ == "__main__":` or inside an async `main()` orchestrator) in an outermost try/except that adds two counter calls:
       - On successful completion, before final resource teardown / process exit: `JOB_COMPLETED_TOTAL.add(1, {"job": "ml-training", "status": "success"})`.
       - In an `except Exception` block (after logging the failure with the existing structlog logger using `error=str(e)` kwarg, never `event=`): `JOB_COMPLETED_TOTAL.add(1, {"job": "ml-training", "status": "failure"})` and then re-raise via `raise` so systemd sees a non-zero exit code.

    3. If a top-level try/except already exists, add the two counter calls inside the existing branches without changing exception propagation.

    Repeat the identical pattern for services/shadow_auditor_agent.py with job label `shadow-auditor`. Both files share the same import, same call pattern, and same job-label-must-match-systemd-unit-suffix contract.

    Do NOT change any business logic. Do NOT add a counter call in the middle of the run (only at script exit). Do NOT use underscores in the job label (`ml-training`, not `ml_training`). The label must match the systemd unit's `%n` suffix.
  </action>
  <verify>
    `grep -n 'JOB_COMPLETED_TOTAL' services/ml_training_agent.py` returns at least 3 lines (import + 2 add calls).
    `grep -n 'JOB_COMPLETED_TOTAL' services/shadow_auditor_agent.py` returns at least 3 lines.
    `grep -E '"job":\\s*"ml-training"' services/ml_training_agent.py` returns at least 2 lines.
    `grep -E '"job":\\s*"shadow-auditor"' services/shadow_auditor_agent.py` returns at least 2 lines.
    `.venv/bin/ruff check services/ml_training_agent.py services/shadow_auditor_agent.py` exits 0.
    Trigger one oneshot run manually as a smoke test (acceptable during off-market hours): `sudo systemctl start indicagent-shadow-auditor` then within 60s `journalctl -u indicagent-shadow-auditor --since '2 minutes ago' | tail -50` shows the script completed (success or failure). `curl -s http://localhost:8000/metrics | grep 'job_completed_total'` returns at least one line with `job="shadow-auditor"` after the run (acceptable if metrics endpoint is on a different service; counter is recorded in-process and exported via OTel collector).
  </verify>
  <done>Both oneshots emit JOB_COMPLETED_TOTAL with correct job label on every run.</done>
  <acceptance_criteria>
    - services/ml_training_agent.py contains literal `from src.observability.metrics import JOB_COMPLETED_TOTAL`
    - services/ml_training_agent.py contains literal `JOB_COMPLETED_TOTAL.add(1, {"job": "ml-training", "status": "success"})`
    - services/ml_training_agent.py contains literal `JOB_COMPLETED_TOTAL.add(1, {"job": "ml-training", "status": "failure"})`
    - services/shadow_auditor_agent.py contains literal `from src.observability.metrics import JOB_COMPLETED_TOTAL`
    - services/shadow_auditor_agent.py contains literal `JOB_COMPLETED_TOTAL.add(1, {"job": "shadow-auditor", "status": "success"})`
    - services/shadow_auditor_agent.py contains literal `JOB_COMPLETED_TOTAL.add(1, {"job": "shadow-auditor", "status": "failure"})`
    - Neither file contains the underscore variant `"ml_training"` or `"shadow_auditor"` as a job-label value
    - `.venv/bin/ruff check services/ml_training_agent.py services/shadow_auditor_agent.py` exits 0
    - `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add JOB_COMPLETED_TOTAL to production/scripts/roll_batch.py</name>
  <files>production/scripts/roll_batch.py</files>
  <read_first>
    - production/scripts/roll_batch.py (locate the existing OTel counter block at lines 51-58 referenced in PATTERNS.md — _RUNS and _ERRORS counters; identify the existing top-level try/except wrapping the main batch run; identify where the asyncpg pool is closed)
    - .planning/phases/108-self-healing-hardening/108-PATTERNS.md (section: Oneshot scripts — job_completed_total counter — confirm shared counter from src.observability.metrics is used, not a new local counter)
    - production/systemd/indicagent-roll-batch.service (confirm job label `roll-batch` matches the unit suffix exactly)
  </read_first>
  <action>
    Edits to production/scripts/roll_batch.py:

    1. Add import: `from src.observability.metrics import JOB_COMPLETED_TOTAL` near the existing observability imports. Keep the existing local `_RUNS` and `_ERRORS` counters in place — they are pre-existing per-script metrics and remain valid. The new `JOB_COMPLETED_TOTAL` is a fleet-wide counter, added without replacing the local ones.

    2. In the existing top-level try/except (the outer block that wraps the main batch logic and decides script exit):
       - On clean completion, before `await pool.close()` (or before the script's main resource teardown), call `JOB_COMPLETED_TOTAL.add(1, {"job": "roll-batch", "status": "success"})`.
       - In the corresponding `except Exception` block (alongside the existing `_ERRORS.add(...)` call or where the error is logged), call `JOB_COMPLETED_TOTAL.add(1, {"job": "roll-batch", "status": "failure"})`. Preserve existing re-raise/exit behavior.

    Job label must be exactly `roll-batch` (matches systemd unit %n). Do NOT remove existing `_RUNS` / `_ERRORS` counters. Do NOT change the asyncpg pool lifecycle.
  </action>
  <verify>
    `grep -n 'JOB_COMPLETED_TOTAL' production/scripts/roll_batch.py` returns at least 3 lines (import + 2 add calls).
    `grep -E '"job":\\s*"roll-batch"' production/scripts/roll_batch.py` returns exactly 2 lines.
    `.venv/bin/ruff check production/scripts/roll_batch.py` exits 0.
    Dry-run import: `.venv/bin/python -c "import production.scripts.roll_batch"` exits 0 (no syntax errors, no import errors).
  </verify>
  <done>roll_batch.py emits JOB_COMPLETED_TOTAL with job=roll-batch on every run, alongside existing _RUNS/_ERRORS counters.</done>
  <acceptance_criteria>
    - production/scripts/roll_batch.py contains literal `from src.observability.metrics import JOB_COMPLETED_TOTAL`
    - production/scripts/roll_batch.py contains literal `JOB_COMPLETED_TOTAL.add(1, {"job": "roll-batch", "status": "success"})`
    - production/scripts/roll_batch.py contains literal `JOB_COMPLETED_TOTAL.add(1, {"job": "roll-batch", "status": "failure"})`
    - production/scripts/roll_batch.py still contains both `_RUNS` and `_ERRORS` (pre-existing counters preserved)
    - production/scripts/roll_batch.py does NOT contain the underscore variant `"roll_batch"` as a job-label value
    - `.venv/bin/ruff check production/scripts/roll_batch.py` exits 0
    - `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- All three files import JOB_COMPLETED_TOTAL and emit it with exactly two label-value combinations per file
- Job labels (`ml-training`, `shadow-auditor`, `roll-batch`) match the corresponding systemd unit's `%n` suffix
- Existing per-script counters (_RUNS, _ERRORS in roll_batch.py) are preserved
</verification>

<success_criteria>
1. ml_training, shadow_auditor, roll_batch all emit JOB_COMPLETED_TOTAL on success and failure paths
2. Job label values match systemd unit suffix exactly (kebab-case, not snake_case)
3. No existing functionality is removed or altered
4. All three files lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/108-self-healing-hardening/108-06-SUMMARY.md`.
</output>
