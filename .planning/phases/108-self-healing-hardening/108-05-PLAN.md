---
phase: 108-self-healing-hardening
plan: 05
type: execute
wave: 2
depends_on:
  - 108-01
files_modified:
  - src/api/main.py
  - src/api/routes/health.py
autonomous: true
requirements:
  - HEAL-03

must_haves:
  truths:
    - "FastAPIInstrumentor().instrument_app(app) is called exactly once in src/api/main.py"
    - "opentelemetry-instrumentation-fastapi is installed in the venv"
    - "api_health gauge is updated to 1 when DB SELECT 1 succeeds and 0 when it fails"
    - "A background coroutine in the FastAPI lifespan refreshes api_health every 30s independent of HTTP traffic"
  artifacts:
    - path: "src/api/main.py"
      provides: "FastAPIInstrumentor.instrument_app(app) + background api_health refresh task in lifespan"
      contains: "FastAPIInstrumentor"
    - path: "src/api/routes/health.py"
      provides: "API_HEALTH.set(1|0) calls in /health/database endpoint"
      contains: "API_HEALTH.set"
  key_links:
    - from: "src/api/main.py::lifespan"
      to: "src.observability.metrics.API_HEALTH"
      via: "asyncio.create_task(_refresh_api_health()) refreshes gauge every 30s"
      pattern: "API_HEALTH\\.set"
    - from: "FastAPI app routes"
      to: "OTel collector"
      via: "FastAPIInstrumentor auto-instruments every request with rate/error/latency"
      pattern: "FastAPIInstrumentor"
---

<objective>
Add OTel HTTP instrumentation to indicagent-api and a dedicated api_health DB-connectivity
gauge. After this plan: every API route auto-emits rate/error/latency metrics with zero
custom middleware, and Prometheus always scrapes a fresh api_health value (1 or 0)
even when no client traffic hits /health/database.

Purpose: HEAL-03 (API health visibility half). Closes the only daemon currently invisible
to Grafana's RED dashboards.

Output: indicagent-api running with FastAPIInstrumentor enabled, /metrics endpoint
exposing http_server_duration_milliseconds and api_health, and a 30s background
DB-check task in the FastAPI lifespan.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/108-self-healing-hardening/108-CONTEXT.md
@.planning/phases/108-self-healing-hardening/108-RESEARCH.md
@.planning/phases/108-self-healing-hardening/108-PATTERNS.md
@src/api/main.py
@src/api/routes/health.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install opentelemetry-instrumentation-fastapi and wire FastAPIInstrumentor + lifespan refresh in src/api/main.py</name>
  <files>src/api/main.py</files>
  <read_first>
    - src/api/main.py (existing lifespan() function; existing app = FastAPI(...) definition; existing imports; existing asyncio.create_task patterns including _broadcaster_task)
    - .planning/phases/108-self-healing-hardening/108-PATTERNS.md (section: src/api/main.py — exact location of FastAPIInstrumentor call and lifespan background task)
    - .planning/phases/108-self-healing-hardening/108-RESEARCH.md (Pattern 3 + Pitfall 6: gauge staleness without background refresh)
    - requirements.txt (Plan 01 added opentelemetry-instrumentation-fastapi>=0.45b0)
  </read_first>
  <action>
    Pre-step: ensure the dependency is installed in the active venv: `source .venv/bin/activate && uv pip install opentelemetry-instrumentation-fastapi>=0.45b0` (idempotent).

    Three edits to src/api/main.py:

    1. Add imports in the existing import block:
       - `from opentelemetry.instrumentation.fastapi import FastAPIInstrumentor`
       - `from src.observability.metrics import API_HEALTH`

    2. After the `app = FastAPI(...)` definition (locate by grep `app = FastAPI(`), add a single call: `FastAPIInstrumentor().instrument_app(app)`. Do not pass excluded_urls or tracer_provider arguments — the default auto-instrumentation is the contract per D-13.

    3. Inside the existing `lifespan()` async context manager, after the existing DB / dependency initialization (`dependencies.db_manager.initialize()` or equivalent) and alongside existing background task creation (e.g. `_broadcaster_task = asyncio.create_task(...)`), add a new background coroutine:
       - Define an inner async function `async def _refresh_api_health() -> None:` that loops forever:
         - `try`: acquire a DB connection via `dependencies.db_manager.connection_manager.get_connection()`, execute `await conn.fetchval("SELECT 1")`, close the connection, then `API_HEALTH.set(1, {"service": "indicagent-api"})`.
         - `except Exception`: `API_HEALTH.set(0, {"service": "indicagent-api"})`. Log via structlog at warning level with the existing logger; use the structlog kwarg `error=str(e)` (never `event=`).
         - `await asyncio.sleep(30)` at the end of every iteration (both success and failure paths).
       - Schedule with `_api_health_task = asyncio.create_task(_refresh_api_health())`.
       - In the `finally:` block (or shutdown teardown) of `lifespan`, cancel the task: `_api_health_task.cancel()` followed by a guarded `await _api_health_task` inside `try/except asyncio.CancelledError: pass` (match the existing teardown pattern used for `_broadcaster_task`).

    Do NOT add custom HTTP middleware to duplicate FastAPIInstrumentor (Don't-Hand-Roll rule). Do NOT remove `_broadcaster_task` or any existing lifespan logic. Use `dependencies.db_manager.connection_manager.get_connection()` — never construct a new asyncpg pool inside the refresh task.
  </action>
  <verify>
    `grep -n 'FastAPIInstrumentor' src/api/main.py` returns at least 2 lines (import + call).
    `grep -n '_refresh_api_health\\|_api_health_task' src/api/main.py` returns at least 3 lines.
    `python -c "from opentelemetry.instrumentation.fastapi import FastAPIInstrumentor; print('ok')"` prints `ok`.
    `.venv/bin/ruff check src/api/main.py` exits 0.
    Restart service: `sudo systemctl restart indicagent-api` then `systemctl is-active indicagent-api` returns `active`.
    `curl -s http://localhost:8000/metrics | grep -E '^http_server_duration_milliseconds|^api_health'` returns at least 2 metric families after 35 seconds (one refresh cycle plus scrape window).
  </verify>
  <done>indicagent-api emits HTTP rate/error/latency via FastAPIInstrumentor and refreshes api_health gauge every 30s in the background.</done>
  <acceptance_criteria>
    - src/api/main.py contains literal `from opentelemetry.instrumentation.fastapi import FastAPIInstrumentor`
    - src/api/main.py contains literal `FastAPIInstrumentor().instrument_app(app)`
    - src/api/main.py contains literal `from src.observability.metrics import API_HEALTH`
    - src/api/main.py contains literal `async def _refresh_api_health` inside lifespan
    - src/api/main.py contains literal `API_HEALTH.set(1, {"service": "indicagent-api"})`
    - src/api/main.py contains literal `API_HEALTH.set(0, {"service": "indicagent-api"})`
    - src/api/main.py contains literal `await asyncio.sleep(30)`
    - src/api/main.py contains literal `_api_health_task.cancel()` inside the lifespan teardown block
    - `pip show opentelemetry-instrumentation-fastapi` returns a version string >= 0.45b0
    - `.venv/bin/ruff check src/api/main.py` exits 0
    - `systemctl is-active indicagent-api` returns `active`
    - `curl -s http://localhost:8000/metrics | grep -c '^api_health'` returns at least 1
    - `curl -s http://localhost:8000/metrics | grep -c '^http_server_duration_milliseconds\\|^http_server_request_duration_seconds'` returns at least 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update /health/database endpoint to set API_HEALTH gauge on both branches</name>
  <files>src/api/routes/health.py</files>
  <read_first>
    - src/api/routes/health.py (existing /database endpoint at lines 31-47; existing imports)
    - .planning/phases/108-self-healing-hardening/108-PATTERNS.md (section: src/api/routes/health.py — gauge write placement on success and failure branches)
    - src/observability/metrics.py (verify API_HEALTH was created via _meter.create_gauge in Plan 01)
  </read_first>
  <action>
    Two edits to src/api/routes/health.py:

    1. Add import: `from src.observability.metrics import API_HEALTH`.

    2. In the existing `/database` endpoint:
       - On the success path (immediately after `await conn.close()` and before `return {...}`), call `API_HEALTH.set(1, {"service": "indicagent-api"})`.
       - In the `except Exception as e:` block (immediately after the existing `logger.error(...)` call and before `raise HTTPException(...)`), call `API_HEALTH.set(0, {"service": "indicagent-api"})`.

    These per-request writes complement the 30s background refresh from Task 1 — both keep Prometheus scrapes fresh under different traffic patterns. Do not remove or reorder existing error handling. Do not change the HTTP status codes returned.
  </action>
  <verify>
    `grep -n 'API_HEALTH.set' src/api/routes/health.py` returns at least 2 lines (one set(1, ...), one set(0, ...)).
    `.venv/bin/ruff check src/api/routes/health.py` exits 0.
    `curl -s -o /dev/null -w '%{http_code}\\n' http://localhost:8000/health/database` returns `200` when DB is up.
    Within 5s after a successful `/health/database` call: `curl -s http://localhost:8000/metrics | grep '^api_health' | grep -E '} 1(\\.|$| )'` returns at least one line.
  </verify>
  <done>/health/database endpoint sets the gauge to 1 on success and 0 on failure, supplementing the background refresh.</done>
  <acceptance_criteria>
    - src/api/routes/health.py contains literal `from src.observability.metrics import API_HEALTH`
    - src/api/routes/health.py contains literal `API_HEALTH.set(1, {"service": "indicagent-api"})`
    - src/api/routes/health.py contains literal `API_HEALTH.set(0, {"service": "indicagent-api"})`
    - The endpoint still returns HTTP 200 on success and HTTP 503 on failure (unchanged)
    - `.venv/bin/ruff check src/api/routes/health.py` exits 0
    - `.venv/bin/pytest tests/unit/ -q` exits 0
    - `curl -s -o /dev/null -w '%{http_code}' http://localhost:8000/health/database` returns `200`
    - `curl -s http://localhost:8000/metrics | grep -c '^api_health'` returns at least 1
  </acceptance_criteria>
</task>

</tasks>

<verification>
- indicagent-api restarted; `systemctl is-active indicagent-api` returns `active`
- /metrics endpoint exposes both `api_health` and `http_server_duration_milliseconds` (or the equivalent opentelemetry FastAPI metric name in installed version)
- No new errors in `journalctl -u indicagent-api --since '5 minutes ago' -p err`
</verification>

<success_criteria>
1. opentelemetry-instrumentation-fastapi installed and importable
2. FastAPIInstrumentor().instrument_app(app) called exactly once
3. /health/database endpoint sets api_health gauge on both branches
4. lifespan background task refreshes api_health every 30s
5. /metrics endpoint exposes HTTP rate/error/latency and api_health
6. indicagent-api running cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/108-self-healing-hardening/108-05-SUMMARY.md`.
</output>
