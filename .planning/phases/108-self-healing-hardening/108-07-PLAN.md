---
phase: 108-self-healing-hardening
plan: 07
type: execute
wave: 3
depends_on:
  - 108-01
  - 108-02
  - 108-03
  - 108-04
  - 108-05
  - 108-06
files_modified:
  - CLAUDE.md
  - .planning/phases/108-self-healing-hardening/108-HYGIENE-07-AUDIT.md
  - .planning/phases/108-self-healing-hardening/108-HEAL-02-DEFERRAL.md
autonomous: true
requirements:
  - HEAL-01
  - HEAL-02
  - HEAL-03
  - HEAL-04

must_haves:
  truths:
    - "CLAUDE.md documents the OTel health contract (D-04 + D-26) as a mandatory rule for new BaseAgent subclasses"
    - "CLAUDE.md documents the four Grafana alert SLOs (D-27)"
    - "HYGIENE-07 status is verified via grep audit and the audit document captures the finding (no daemon outside BaseAgent)"
    - "HEAL-02 (DB backup) deferral is documented with restore scenario gap explanation"
  artifacts:
    - path: "CLAUDE.md"
      provides: "OTel Health Contract SOP block + Grafana SLO alerting section"
      contains: "OTel Health Contract"
    - path: ".planning/phases/108-self-healing-hardening/108-HYGIENE-07-AUDIT.md"
      provides: "grep audit output for BaseAgent inheritance + verification finding"
      contains: "HYGIENE-07"
    - path: ".planning/phases/108-self-healing-hardening/108-HEAL-02-DEFERRAL.md"
      provides: "Explicit deferral rationale and re-evaluation trigger for DB backup work"
      contains: "HEAL-02"
  key_links:
    - from: "CLAUDE.md OTel Health Contract section"
      to: "src/core/agent/base.py mandatory counters"
      via: "Documentation references the five signals (D-04) that every BaseAgent subclass inherits"
      pattern: "watchdog_notify_total"
    - from: ".planning/phases/108-self-healing-hardening/108-HYGIENE-07-AUDIT.md"
      provides: "Verification record closing HYGIENE-07 without code migration"
      via: "grep audit confirms targets already inherit BaseAgent transitively"
      pattern: "BaseAgent"
---

<objective>
Wrap up Phase 108 with three documentation deliverables:
1. Update CLAUDE.md with the OTel health contract SOP (D-04 + D-26) so future agent
   authors cannot omit the five mandatory signals, plus the Grafana SLO alert list (D-27).
2. Run the HYGIENE-07 BaseAgent inheritance audit (D-12) and record the verification
   result. RESEARCH.md indicates both named targets already inherit BaseAgent
   transitively, so this is verification, not migration.
3. Record HEAL-02 (DB backup) deferral with explicit re-evaluation trigger so the
   requirement is not forgotten.

Purpose: Documentation locks the contract in place. Without the SOP update, the next
agent author can ship an OTel-blind service and the audit fails again. Without the
HYGIENE-07 audit record, the requirement appears unaddressed.

Output: CLAUDE.md updated, two audit/deferral artifacts produced, all four HEAL
requirement IDs accounted for across the phase plans.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/108-self-healing-hardening/108-CONTEXT.md
@.planning/phases/108-self-healing-hardening/108-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run HYGIENE-07 BaseAgent inheritance audit and write 108-HYGIENE-07-AUDIT.md</name>
  <files>.planning/phases/108-self-healing-hardening/108-HYGIENE-07-AUDIT.md</files>
  <read_first>
    - .planning/phases/108-self-healing-hardening/108-RESEARCH.md (HYGIENE-07 status section: both named targets already inherit BaseAgent transitively via BaseWriterAgent / BaseGroupService / BaseProviderAgent)
    - .planning/phases/108-self-healing-hardening/108-CONTEXT.md (D-11, D-12 — audit method and migration criteria)
    - services/signal_replay_auditor_agent.py (verify current base class is BaseAgent or a subclass thereof)
    - services/bar_replay_provider_agent.py (verify current base class)
  </read_first>
  <action>
    Three audit commands plus a written record. Execute the audit first, then write the document.

    1. Run `grep -rL "BaseAgent\\|BaseWriterAgent\\|BaseGroupService\\|BaseProviderAgent" services/*.py` and capture the output. Files in the output do NOT inherit BaseAgent through any known route. Expected per RESEARCH.md: empty list or only utility scripts that are intentionally non-agent (verify each remaining file is NOT registered as a `Type=simple` systemd unit).

    2. For each `services/*.py` file with a corresponding `production/systemd/indicagent-*.service` unit of `Type=simple`, run `grep -E "^class\\s+\\w+Agent\\(\\w+(Agent|Service)\\)" <file>` and confirm the base class is one of: BaseAgent, BaseWriterAgent, BaseGroupService, BaseProviderAgent. Record any exceptions.

    3. Write `.planning/phases/108-self-healing-hardening/108-HYGIENE-07-AUDIT.md` containing:
       - Audit date and command output (verbatim).
       - List of all `services/*.py` files mapped to `Type=simple` systemd unit and their direct base class.
       - Explicit verification statement: HYGIENE-07 closed by transitive inheritance (no migration required) OR list of files needing migration in a follow-up plan.
       - Reference to RESEARCH.md Open Question 1 finding.

    Do NOT migrate code in this task. If the audit surfaces a true gap, create a quick task or a follow-up plan rather than expanding Plan 07 scope. Document the finding either way.
  </action>
  <verify>
    File `.planning/phases/108-self-healing-hardening/108-HYGIENE-07-AUDIT.md` exists and is non-empty.
    `grep -c 'BaseAgent\\|BaseWriterAgent\\|BaseGroupService\\|BaseProviderAgent' .planning/phases/108-self-healing-hardening/108-HYGIENE-07-AUDIT.md` returns at least 4 (each known base class named).
    `grep -E 'signal_replay_auditor|bar_replay_provider' .planning/phases/108-self-healing-hardening/108-HYGIENE-07-AUDIT.md` returns at least 2 lines (both explicit targets named).
    `grep -E 'HYGIENE-07' .planning/phases/108-self-healing-hardening/108-HYGIENE-07-AUDIT.md` returns at least one line.
    `grep -E 'Date|Audit date|2026-05' .planning/phases/108-self-healing-hardening/108-HYGIENE-07-AUDIT.md` returns at least one line.
  </verify>
  <done>Audit recorded; HYGIENE-07 status documented either as closed-by-verification or with explicit follow-up scope.</done>
  <acceptance_criteria>
    - File .planning/phases/108-self-healing-hardening/108-HYGIENE-07-AUDIT.md exists and is non-empty
    - File contains the verbatim grep command output from step 1
    - File names both `signal_replay_auditor_agent.py` and `bar_replay_provider_agent.py` with their actual base class
    - File contains an explicit conclusion sentence: either "HYGIENE-07 closed: no migration required" or "HYGIENE-07 follow-up needed: <list>"
    - File references "RESEARCH.md" (linking back to the source-of-truth finding)
    - File contains the audit date string `2026-05-28` or later
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md with OTel Health Contract SOP and Grafana SLO alerts</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (existing "Key Rules" and "Services" sections — locate the right insertion point next to existing OTel and BaseAgent rules)
    - .planning/phases/108-self-healing-hardening/108-CONTEXT.md (D-04, D-06, D-26, D-27 — exact contract and SLO list)
    - src/core/agent/base.py (so the contract text references real instrument names actually emitted)
  </read_first>
  <action>
    Add a new section header to CLAUDE.md titled `## OTel Health Contract (Phase 108 SOP)`. Place it after the `## Key Rules` block but before `## Infrastructure`. The section must include:

    1. Statement of the contract: "Every new daemon that inherits BaseAgent MUST emit these five OTel signals; non-compliance is a code review rejection (D-26)." Then a bullet list of the five mandatory signals with their type and label key:
       - `agent_last_message_timestamp_seconds` (gauge, label `agent_id`) — liveness
       - `agent_crash_total` (counter, label `agent_id`) — uncaught exceptions
       - `agent_dlq_total` (counter, label `agent_id`) — DLQ routing events
       - `watchdog_notify_total` (counter, label `agent_id`) — successful sd_notify pings (D-04 Phase 108)
       - `watchdog_notify_suppressed_total` (counter, label `agent_id`) — suppressed pings (D-04 Phase 108)
       Note that all five are inherited automatically from BaseAgent — no per-service code is needed.

    2. Oneshot contract (D-06): "Oneshot timer-triggered scripts MUST emit `job_completed_total{job, status}` at script exit. Label `job` MUST match the systemd unit `%n` suffix exactly (kebab-case)."

    3. Grafana SLO alerts (D-27) as a bullet list:
       - `agent_last_message_timestamp_seconds` stale > 120s -> page
       - `watchdog_notify_suppressed_total` rate > 0 -> warning
       - `dlq_quarantine_total` increment > 0 -> warning
       - `api_health` = 0 -> page
       - `rate(bars_processed_total[5m])` drops > 50% from baseline -> warning
       - `consumer_stall_detected_total` rate > 0 -> warning
       - Any oneshot `job_completed_total{status="failure"}` increment -> warning; `time_since_last_success{job=X} > 25h` -> page

    4. Brief reference: "Phase 108 closed HEAL-01, HEAL-03, HEAL-04. HEAL-02 (DB backup) is deferred — see .planning/phases/108-self-healing-hardening/108-HEAL-02-DEFERRAL.md."

    Constraints: Use single dashes only (no em-dashes per MEMORY.md feedback_no_emdash). Do NOT relocate existing CLAUDE.md content. Do NOT bump the Version line at the top of CLAUDE.md by more than one minor (e.g., 5.43.0 -> 5.44.0); update it to reflect the SOP addition. Keep the new section under ~50 lines (CLAUDE.md is per-turn context).
  </action>
  <verify>
    `grep -n 'OTel Health Contract' CLAUDE.md` returns at least one line.
    `grep -c 'watchdog_notify_total\\|watchdog_notify_suppressed_total\\|job_completed_total\\|api_health\\|dlq_quarantine_total\\|consumer_stall_detected_total' CLAUDE.md` returns at least 6.
    `grep -n 'Phase 108' CLAUDE.md` returns at least one line in the new SOP section.
    `grep -E '^Version: 5\\.4[3-9]' CLAUDE.md` returns at least one line (version bumped).
    `grep -c '—' CLAUDE.md` (em dash literal U+2014) does not increase from before the edit (still zero em dashes in newly added content). Acceptable if any pre-existing em dashes remain elsewhere; the newly added section must contain none.
  </verify>
  <done>CLAUDE.md has the OTel Health Contract SOP block, Grafana SLO list, and a pointer to the HEAL-02 deferral doc.</done>
  <acceptance_criteria>
    - CLAUDE.md contains literal `OTel Health Contract`
    - CLAUDE.md contains the five mandatory signal names: `agent_last_message_timestamp_seconds`, `agent_crash_total`, `agent_dlq_total`, `watchdog_notify_total`, `watchdog_notify_suppressed_total`
    - CLAUDE.md contains the SLO threshold strings `stale > 120s`, `api_health` (in alert context), `dlq_quarantine_total`, `consumer_stall_detected_total`, and `job_completed_total`
    - CLAUDE.md contains a pointer to `.planning/phases/108-self-healing-hardening/108-HEAL-02-DEFERRAL.md`
    - CLAUDE.md Version line is bumped to 5.44.0 or higher
    - The newly added OTel Health Contract section (the block under that heading) contains no U+2014 em dash characters
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Document HEAL-02 deferral with re-evaluation trigger</name>
  <files>.planning/phases/108-self-healing-hardening/108-HEAL-02-DEFERRAL.md</files>
  <read_first>
    - .planning/phases/108-self-healing-hardening/108-CONTEXT.md (D-28 — deferral rationale and the "no clear restore scenario identified" justification)
    - .planning/REQUIREMENTS.md (HEAL-02 wording: nightly pg_dump to /var/backups/indicagent/, .sql.gz < 25h old, 7-day retention)
  </read_first>
  <action>
    Create `.planning/phases/108-self-healing-hardening/108-HEAL-02-DEFERRAL.md` containing:

    1. Title: `HEAL-02 Deferral Record — DB Backup`.
    2. Status: Deferred from Phase 108.
    3. Original requirement quoted from REQUIREMENTS.md: indicagent-db-backup.service + .timer, nightly pg_dump to /var/backups/indicagent/, .sql.gz < 25h old, 7-day retention.
    4. Deferral rationale referencing D-28: no clear restore scenario identified; nightly backups without a tested restore workflow are operational theater.
    5. Re-evaluation trigger (the condition under which HEAL-02 must be revisited): any of (a) a disk-loss incident in test or prod, (b) regulatory or auditor requirement for point-in-time recovery, (c) introduction of a system component whose state cannot be reconstructed from Kafka + raw OHLCV (e.g., long-lived ML model artifacts, manual configuration tables), (d) explicit user decision to add it as a quick task.
    6. Implementation hint (when revisited): smaller scope than the original requirement — start with `pg_dump --schema-only` plus selected user tables (instruments, contract_metadata, signal_ledger), tested restore on a staging DB, before extending to a full hypertable dump.
    7. Forward pointer: this record will be linked from CLAUDE.md OTel Health Contract section.

    Keep the file concise (~30-40 lines). Use single-dash style (no em dashes). Do not add a systemd unit file or pg_dump script — those belong in the future quick task or phase that picks this up.
  </action>
  <verify>
    File `.planning/phases/108-self-healing-hardening/108-HEAL-02-DEFERRAL.md` exists and is non-empty.
    `grep -E 'Deferral|Deferred' .planning/phases/108-self-healing-hardening/108-HEAL-02-DEFERRAL.md` returns at least one line.
    `grep -E 'pg_dump|/var/backups/indicagent/' .planning/phases/108-self-healing-hardening/108-HEAL-02-DEFERRAL.md` returns at least one line.
    `grep -E 'Re-evaluation|re-evaluation|trigger' .planning/phases/108-self-healing-hardening/108-HEAL-02-DEFERRAL.md` returns at least one line.
    `grep -c '—' .planning/phases/108-self-healing-hardening/108-HEAL-02-DEFERRAL.md` returns 0 (no em dashes per MEMORY.md feedback_no_emdash).
  </verify>
  <done>HEAL-02 deferral recorded with rationale, re-evaluation triggers, and implementation hints.</done>
  <acceptance_criteria>
    - File .planning/phases/108-self-healing-hardening/108-HEAL-02-DEFERRAL.md exists
    - File contains literal `HEAL-02`
    - File contains literal `pg_dump`
    - File contains literal `/var/backups/indicagent/`
    - File contains the phrase `no clear restore scenario` or semantically equivalent reference to D-28
    - File enumerates at least 4 re-evaluation triggers
    - File contains zero U+2014 em-dash characters
  </acceptance_criteria>
</task>

</tasks>

<verification>
- CLAUDE.md updated; version bumped; new SOP block present
- HYGIENE-07 audit document records the verification with verbatim grep output
- HEAL-02 deferral document records rationale and re-evaluation triggers
- All four HEAL requirement IDs (HEAL-01, HEAL-02, HEAL-03, HEAL-04) appear in at least one plan's requirements frontmatter across the seven phase plans
</verification>

<success_criteria>
1. CLAUDE.md OTel Health Contract section exists and lists five mandatory signals
2. CLAUDE.md Grafana SLO alert list documents at least six alerts
3. HYGIENE-07 audit document records grep output and verification conclusion
4. HEAL-02 deferral document captures rationale and re-evaluation triggers
5. No em dashes introduced into new content
</success_criteria>

<output>
After completion, create `.planning/phases/108-self-healing-hardening/108-07-SUMMARY.md`.
</output>
