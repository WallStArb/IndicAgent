---
phase: 39.1-intelligence-layer-enforcement
plan: 01
subsystem: intelligence
tags: [protocol, validation, regime_type, i7, plugins, enforcement]

# Dependency graph
requires: []
provides:
  - PatternPlugin Protocol with mandatory regime_type field
  - validate_tier() I7 regime_type runtime validation
  - Startup crash guard for missing/invalid regime_type on all I7 plugins

affects:
  - signal_generator_service (calls validate_tier at startup)
  - All future I7 plugin development (regime_type now in Protocol)
  - 39.1-02 (SignalStatus enum plan — builds on same enforcement pattern)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Protocol field enforcement: ClassVar fields in Protocol make missing attrs a type error"
    - "Runtime startup gate: validate_tier() crashes service before any bar is processed"

key-files:
  created: []
  modified:
    - src/intelligence/plugins.py

key-decisions:
  - "validate_tier() lives in plugins.py (PluginRegistry class), not register_plugins.py — plan described wrong file but code structure makes plugins.py correct"
  - "I7-only validation: validate_tier checks regime_type only for tier=='I7', not for I3/I4/I5 which don't use regime gating"
  - "All 36 existing I7 plugins already had valid regime_type — zero remediation needed"

patterns-established:
  - "PatternPlugin Protocol: regime_type: ClassVar[str] is now a required field alongside name, outputs, min_lookback, etc."
  - "Startup validation pattern: validate_tier(TIER_I7, 'I7') called at service startup hard-crashes with clear error on any missing/invalid value"

requirements-completed: [CODE-Q-01]

# Metrics
duration: 2min
completed: 2026-03-19
---

# Phase 39.1 Plan 01: PatternPlugin Protocol regime_type Enforcement Summary

**PatternPlugin Protocol extended with mandatory regime_type field and validate_tier() now hard-crashes service startup if any I7 plugin is missing or has an invalid regime_type value**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-03-19T17:06:42Z
- **Completed:** 2026-03-19T17:07:44Z
- **Tasks:** 3
- **Files modified:** 1

## Accomplishments

- Added `regime_type: ClassVar[str]` to `PatternPlugin` Protocol — makes the field mandatory at the type-system level
- Extended `validate_tier()` with I7-specific regime validation block — crashes at startup if `regime_type` is missing or not in `{"trend", "mean_reversion", "any"}`
- Verified all 36 current I7 plugins already declare valid `regime_type` — zero remediation required

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend PatternPlugin Protocol with regime_type field** - `efcd111` (feat)
2. **Task 2: Add regime_type validation to validate_tier()** - `02aa3fd` (feat)
3. **Task 3: Verify all 36 I7 plugins pass validation** - (no code change needed; verification passed)

## Files Created/Modified

- `src/intelligence/plugins.py` - Added `regime_type: ClassVar[str]` to `PatternPlugin` Protocol; extended `validate_tier()` with I7 regime_type validation block

## Decisions Made

- `validate_tier()` lives in `plugins.py` (as a method of `PluginRegistry`), not in `register_plugins.py` as the plan described. The plan's file reference was incorrect — the actual code structure makes `plugins.py` the right location.
- I7-only validation scope: only the I7 tier uses regime gating (`hmm_regime` suppression), so adding validation to other tiers would be noise.
- All 36 existing I7 plugins were already compliant — the old CLAUDE.md and intelligence CLAUDE.md documented `regime_type` as mandatory but without Protocol or runtime enforcement.

## Deviations from Plan

None - plan executed exactly as written. The only deviation was a minor plan inaccuracy (wrong file named for `validate_tier` location) — handled automatically by following the actual code structure.

## Issues Encountered

None — all 36 I7 plugins already declared `regime_type` correctly. The `validate_tier` method was found in `plugins.py` rather than `register_plugins.py` (plan had incorrect file reference), but the change location was unambiguous from reading the actual code.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 02 (SignalStatus enum) is ready to execute — same enforcement pattern but for signal status strings
- Any new I7 plugin that forgets `regime_type` will now cause an immediate `ValueError` at service startup with a precise error message identifying the offending plugin

---
*Phase: 39.1-intelligence-layer-enforcement*
*Completed: 2026-03-19*
