---
phase: 067-observability-alerting-automation
plan: 10
type: execute
wave: 3
depends_on: ["067-09"]
files_modified:
  - services/cross_asset_service.py
  - tests/unit/service_tests/test_cross_asset_bootstrap.py
autonomous: true
gap_closure: true
requirements:
  - OBS-DASHBOARDS

must_haves:
  truths:
    - "No backward compatibility shim for CrossAssetService in cross_asset_service.py"
    - "Tests import CrossAssetComputeAgent directly, not the old name"
  artifacts:
    - path: "services/cross_asset_service.py"
      provides: "Clean module with no alias"
      contains: "CrossAssetComputeAgent"
    - path: "tests/unit/service_tests/test_cross_asset_bootstrap.py"
      provides: "Tests importing new name directly"
  key_links: []
---

<objective>
Remove the backward compatibility shim `CrossAssetService = CrossAssetComputeAgent` and update all test imports to use the new name directly.

Purpose: The alias was a temporary migration artifact from Plan 067-05. All consumers should use the canonical name `CrossAssetComputeAgent` now. The alias creates confusion about which name is canonical.

Output: Clean module and test files with no alias.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/067-observability-alerting-automation/067-VERIFICATION.md
@.planning/phases/067-observability-alerting-automation/067-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove CrossAssetService alias and update all imports</name>
  <files>services/cross_asset_service.py, tests/unit/service_tests/test_cross_asset_bootstrap.py</files>
  <read_first>
    - services/cross_asset_service.py (lines 455-466, the shim section at the bottom)
    - tests/unit/service_tests/test_cross_asset_bootstrap.py (full file, check all import statements and class references)
  </read_first>
  <action>
    **Step 1: Remove the alias from cross_asset_service.py**

    At the bottom of `services/cross_asset_service.py`, remove lines 461-466:
    ```python
    # ---------------------------------------------------------------------------
    # Backward compatibility shim
    # ---------------------------------------------------------------------------

    # Preserve old class name for test compatibility
    CrossAssetService = CrossAssetComputeAgent
    ```

    **Step 2: Update test imports**

    In `tests/unit/service_tests/test_cross_asset_bootstrap.py`, search for any import or reference to `CrossAssetService`. Replace with `CrossAssetComputeAgent`.

    **Step 3: Search entire codebase for remaining references**

    Run: `grep -rn "CrossAssetService" --include="*.py" .` to find any remaining references to the old name. Update them all to `CrossAssetComputeAgent`. Exclude archived files (those in `_archived_*` names).

    Common locations to check:
    - `tests/unit/service_tests/test_cross_asset_bootstrap.py`
    - Any other test files that import the old name
    - Any service files that reference the old name
  </action>
  <verify>
    <automated>grep -rn "CrossAssetService" --include="*.py" services/ tests/ src/ 2>/dev/null | grep -v "_archived" | grep -v "__pycache__"; echo "Expected: 0 matches (all references updated)"</automated>
  </verify>
  <acceptance_criteria>
    - Zero references to `CrossAssetService` in non-archived Python files
    - The alias line removed from cross_asset_service.py
    - All test files import `CrossAssetComputeAgent` directly
    - Tests pass: `.venv/bin/pytest tests/unit/service_tests/test_cross_asset_bootstrap.py -v`
  </acceptance_criteria>
  <done>No backward compatibility shim remains. All code references CrossAssetComputeAgent directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal rename | No external trust boundary — purely internal code hygiene |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-067-10-01 | Repudiation | cross_asset_service.py | accept | Purely cosmetic rename, no security impact |
</threat_model>

<verification>
- grep for `CrossAssetService` returns zero matches in non-archived files
- Test suite passes with new import name
</verification>

<success_criteria>
- No `CrossAssetService` alias in cross_asset_service.py
- All test imports use `CrossAssetComputeAgent`
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/067-observability-alerting-automation/067-10-SUMMARY.md`
</output>
