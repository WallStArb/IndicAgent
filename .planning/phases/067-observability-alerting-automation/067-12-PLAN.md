---
phase: 067-observability-alerting-automation
plan: 12
type: execute
wave: 2
depends_on: []
files_modified:
  - services/ai_narrative_agent.py
  - services/bar_aggregator_agent.py
  - services/bar_auditor_agent.py
  - services/contract_metadata_writer_agent.py
  - services/cross_asset_service.py
  - services/intelligence_pipeline_agent.py
  - services/lifecycle_writer_agent.py
  - services/ml_data_quality_agent.py
  - services/ml_discovery_agent.py
  - services/ml_orchestrator_agent.py
  - services/parity_auditor_agent.py
  - services/roll_compute_agent.py
  - services/service_auditor_agent.py
  - services/signal_auditor_agent.py
  - services/signal_metrics_compute_agent.py
  - services/signal_metrics_writer_agent.py
  - services/signal_writer_agent.py
  - services/swarm_orchestrator_agent.py
autonomous: true
gap_closure: true
requirements:
  - OBS-CONSUMER-LAG

must_haves:
  truths:
    - "All consumer agents emit PERSISTENCE_CONSUMER_LAG metric"
  artifacts:
    - path: "services/lifecycle_writer_agent.py"
      provides: "Consumer lag reporting via _report_consumer_lag()"
      contains: "_report_consumer_lag"
  key_links: []
---

<objective>
Add consumer lag reporting to all agents that inherit from BaseAgent but don't override _report_consumer_lag().

Purpose: 19 agents currently have no lag reporting. BaseAgent has a no-op _report_consumer_lag() that the stall watchdog calls periodically. Agents that already report (feature_writer, signal_writer, lifecycle_writer, llm_writer, feature_snapshot_writer) use a buffer-length proxy. The standard pattern: override _report_consumer_lag() to set PERSISTENCE_CONSUMER_LAG.labels(agent_id=self.name).set(buffer_size_or_lag).

Output: All agents emit PERSISTENCE_CONSUMER_LAG.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/067-observability-alerting-automation/067-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _report_consumer_lag() to all agents missing it</name>
  <files>services/ai_narrative_agent.py, services/bar_aggregator_agent.py, services/bar_auditor_agent.py, services/contract_metadata_writer_agent.py, services/cross_asset_service.py, services/intelligence_pipeline_agent.py, services/lifecycle_writer_agent.py, services/ml_data_quality_agent.py, services/ml_discovery_agent.py, services/ml_orchestrator_agent.py, services/parity_auditor_agent.py, services/roll_compute_agent.py, services/service_auditor_agent.py, services/signal_auditor_agent.py, services/signal_metrics_compute_agent.py, services/signal_metrics_writer_agent.py, services/signal_writer_agent.py, services/swarm_orchestrator_agent.py</files>
  <read_first>
    - services/feature_writer_agent.py (REFERENCE — has _report_consumer_lag with buffer length)
    - services/feature_snapshot_writer_agent.py (REFERENCE — has _report_consumer_lag with buffer length)
    - services/lifecycle_writer_agent.py (already imports PERSISTENCE_CONSUMER_LAG but may not override _report_consumer_lag)
    - services/signal_writer_agent.py (already imports PERSISTENCE_CONSUMER_LAG but may not override _report_consumer_lag)
    - src/core/agent/base.py (lines 240-260, the _report_consumer_lag no-op and _stall_watchdog that calls it)
  </read_first>
  <action>
    For each agent file listed above, add consumer lag reporting:

    **Pattern A — Buffer-based agents** (writers that accumulate messages before flushing):
    ```python
    async def _report_consumer_lag(self) -> None:
        PERSISTENCE_CONSUMER_LAG.labels(agent_id=self.name).set(len(self._buffer))
    ```

    **Pattern B — Stream processors** (compute agents that process one message at a time):
    ```python
    async def _report_consumer_lag(self) -> None:
        PERSISTENCE_CONSUMER_LAG.labels(agent_id=self.name).set(0)
    ```

    For each file:
    1. Import `PERSISTENCE_CONSUMER_LAG` from `src/observability/metrics.py` (if not already imported)
    2. Add the `_report_consumer_lag()` override method
    3. Use Pattern A if the agent has a buffer/list accumulator, Pattern B otherwise

    **Agents to classify:**
    - Buffer-based (Pattern A): bar_aggregator (accumulates bars), bar_auditor (audit results), cross_asset (window bars), ml_discovery (discoveries), signal_metrics_compute (metrics buffer), signal_metrics_writer (write buffer), swarm_orchestrator (task queue)
    - Stream processors (Pattern B): ai_narrative, contract_metadata_writer, intelligence_pipeline, lifecycle_writer, ml_data_quality, ml_orchestrator, parity_auditor, roll_compute, service_auditor, signal_auditor

    For lifecycle_writer and signal_writer: check if they already override _report_consumer_lag. If they import PERSISTENCE_CONSUMER_LAG but don't override, add the override.
  </action>
  <verify>
    <automated>for f in services/ai_narrative_agent.py services/bar_aggregator_agent.py services/bar_auditor_agent.py services/cross_asset_service.py services/intelligence_pipeline_agent.py services/parity_auditor_agent.py services/service_auditor_agent.py services/signal_auditor_agent.py services/swarm_orchestrator_agent.py; do echo -n "$f: "; grep -c "_report_consumer_lag" "$f" 2>/dev/null; done; echo "Expected: all >= 1"</automated>
  </verify>
  <acceptance_criteria>
    - All 18 listed agents import PERSISTENCE_CONSUMER_LAG
    - All 18 listed agents override _report_consumer_lag()
    - Reference implementations (feature_writer, feature_snapshot_writer) unchanged
  </acceptance_criteria>
  <done>All agents emit PERSISTENCE_CONSUMER_LAG metric via _report_consumer_lag().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal metric emission | Adding Prometheus gauge updates in existing async methods |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-067-12-01 | Denial of Service | _report_consumer_lag | accept | Metric emission is lightweight, no external calls |
</threat_model>

<verification>
- grep for `_report_consumer_lag` returns matches in all 18 target files
- grep for `PERSISTENCE_CONSUMER_LAG` import in all 18 target files
</verification>

<success_criteria>
- All agents that inherit from BaseAgent emit PERSISTENCE_CONSUMER_LAG
- No existing lag reporting broken in agents that already had it
</success_criteria>

<output>
After completion, create `.planning/phases/067-observability-alerting-automation/067-12-SUMMARY.md`
</output>
