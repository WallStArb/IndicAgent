---
phase: 067-observability-alerting-automation
plan: 13
type: execute
wave: 3
depends_on: ["067-12"]
files_modified:
  - production/grafana/dashboards/pipeline-health.json
autonomous: true
gap_closure: true
requirements:
  - OBS-DASHBOARDS

must_haves:
  truths:
    - "Pipeline health dashboard has consumer lag panels"
  artifacts:
    - path: "production/grafana/dashboards/pipeline-health.json"
      provides: "Consumer lag visualization panels"
      contains: "persistence_consumer_lag_records"
  key_links: []
---

<objective>
Add consumer lag panels to the pipeline-health Grafana dashboard.

Purpose: After Plan 067-12 adds lag reporting to all agents, the dashboard needs panels to visualize it. The pipeline-health.json dashboard currently has Latency (p95) and Throughput rows but no Consumer Lag section.

Output: pipeline-health.json with new Consumer Lag row containing overview and per-agent panels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/067-observability-alerting-automation/067-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add consumer lag panels to pipeline-health dashboard</name>
  <files>production/grafana/dashboards/pipeline-health.json</files>
  <read_first>
    - production/grafana/dashboards/pipeline-health.json (full file, understand panel structure and ID numbering)
  </read_first>
  <action>
    In `production/grafana/dashboards/pipeline-health.json`, add a new row section "Consumer Lag" with 2 panels after the existing Throughput section:

    **Panel 1: "Consumer Lag by Agent" (stat panel)**
    - ID: next available ID after the highest existing panel ID
    - Grid position: new row below the Throughput section
    - Type: stat
    - Target: `persistence_consumer_lag_records`
    - Legend: `{{agent_id}}`
    - Color mode: value (green → yellow → red thresholds: 0, 100, 1000)

    **Panel 2: "Consumer Lag Trend" (time series)**
    - ID: next available ID after Panel 1
    - Grid position: next to Panel 1
    - Type: timeseries
    - Target: `persistence_consumer_lag_records`
    - Legend: `{{agent_id}}`
    - Y-axis: "Records"
    - Show multiple agents as separate series

    Follow the exact JSON structure used by existing panels in the dashboard for consistency (same datasource ref, gridPos layout, etc.).
  </action>
  <verify>
    <automated>grep -c "persistence_consumer_lag_records" production/grafana/dashboards/pipeline-health.json; echo "Expected: 2+ (queries in both panels)"</automated>
  </verify>
  <acceptance_criteria>
    - pipeline-health.json is valid JSON
    - Contains "Consumer Lag" row with at least 2 panels
    - Panels reference persistence_consumer_lag_records metric
    - Panel IDs are unique (no conflicts with existing panels)
  </acceptance_criteria>
  <done>Pipeline health dashboard has consumer lag visualization panels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grafana -> Prometheus | Dashboard queries Prometheus for metric data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-067-13-01 | Information Disclosure | dashboard panels | accept | Only shows operational metrics, no sensitive data |
</threat_model>

<verification>
- pipeline-health.json is valid JSON
- Contains persistence_consumer_lag_records PromQL query
- Panel count increased by at least 2 (row + panels)
</verification>

<success_criteria>
- Consumer Lag row with stat and timeseries panels added
- Valid JSON structure matching existing dashboard patterns
</success_criteria>

<output>
After completion, create `.planning/phases/067-observability-alerting-automation/067-13-SUMMARY.md`
</output>
