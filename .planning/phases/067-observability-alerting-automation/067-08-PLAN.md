---
phase: 067-observability-alerting-automation
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - production/grafana/provisioning/alerting/alert-rules.yml
  - production/grafana/dashboards/operations.json
autonomous: true
gap_closure: true
requirements:
  - OBS-GRAFANA-ALERTS

must_haves:
  truths:
    - "Grafana alert for signal buffer drops fires when messages overflow"
    - "Operations dashboard signal writer panel uses correct metric name"
  artifacts:
    - path: "production/grafana/provisioning/alerting/alert-rules.yml"
      provides: "Alert rules with correct metric names"
      contains: "signal_writer_agent_buffer_overflow_total"
  key_links:
    - from: "production/grafana/provisioning/alerting/alert-rules.yml"
      to: "Prometheus signal_writer_agent_buffer_overflow_total metric"
      via: "PromQL increase() expression"
      pattern: "signal_writer_agent_buffer_overflow_total"
---

<objective>
Fix Grafana alert rules and dashboard panels that reference nonexistent Prometheus metric names.

Purpose: The `signals_dropped` alert and the Signal Writer Buffer Drops dashboard panel query `signal_writer_buffer_dropped_total`, which does not exist. The actual metric emitted by BaseWriterAgent is `signal_writer_agent_buffer_overflow_total`. Without this fix, the CRITICAL alert for signal data loss will never fire.

Output: Corrected alert-rules.yml and operations.json with the real metric name.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/067-observability-alerting-automation/067-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix nonexistent metric name in Grafana alert rule and dashboard</name>
  <files>production/grafana/provisioning/alerting/alert-rules.yml, production/grafana/dashboards/operations.json</files>
  <read_first>
    - production/grafana/provisioning/alerting/alert-rules.yml (lines 86-120, the signals_dropped alert)
    - production/grafana/dashboards/operations.json (around line 502, the Signal Writer Buffer Drops panel)
    - src/core/agent/base_writer.py (lines 87-88, to confirm the metric naming pattern: `{agent_snake}_buffer_overflow_total`)
  </read_first>
  <action>
    In `production/grafana/provisioning/alerting/alert-rules.yml`, replace ALL occurrences of `signal_writer_buffer_dropped_total` with `signal_writer_agent_buffer_overflow_total`. There are exactly 2 occurrences:
    - Line 93: `expr: increase(signal_writer_buffer_dropped_total[5m])` -> `expr: increase(signal_writer_agent_buffer_overflow_total[5m])`
    - Line 117: description text -> replace `signal_writer_buffer_dropped_total` with `signal_writer_agent_buffer_overflow_total`

    In `production/grafana/dashboards/operations.json`, replace the occurrence of `signal_writer_buffer_dropped_total` with `signal_writer_agent_buffer_overflow_total`. This is around line 502 in the "Signal Writer Buffer Drops" panel.

    Do NOT change any other alert rules or panels. Only the metric name substitution.
  </action>
  <verify>
    <automated>grep -c "signal_writer_buffer_dropped_total" production/grafana/provisioning/alerting/alert-rules.yml production/grafana/dashboards/operations.json; echo "Expected: 0 occurrences of old metric"; grep -c "signal_writer_agent_buffer_overflow_total" production/grafana/provisioning/alerting/alert-rules.yml production/grafana/dashboards/operations.json; echo "Expected: 2+ occurrences of correct metric"</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `signal_writer_buffer_dropped_total` in alert-rules.yml
    - Zero occurrences of `signal_writer_buffer_dropped_total` in operations.json
    - At least 2 occurrences of `signal_writer_agent_buffer_overflow_total` in alert-rules.yml
    - At least 1 occurrence of `signal_writer_agent_buffer_overflow_total` in operations.json
  </acceptance_criteria>
  <done>Alert rule signals_dropped and dashboard panel both reference the correct `signal_writer_agent_buffer_overflow_total` metric.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grafana -> Prometheus | Alert rules query Prometheus; wrong metric name = silent failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-067-08-01 | Denial of Service | alert-rules.yml | accept | Metric name typo causes alert to never fire — already the current state, this fix resolves it |
</threat_model>

<verification>
grep returns zero matches for `signal_writer_buffer_dropped_total` across both files.
grep returns matches for `signal_writer_agent_buffer_overflow_total` in both files.
</verification>

<success_criteria>
- All references to `signal_writer_buffer_dropped_total` replaced with `signal_writer_agent_buffer_overflow_total`
- No other alert rules or panels modified
</success_criteria>

<output>
After completion, create `.planning/phases/067-observability-alerting-automation/067-08-SUMMARY.md`
</output>
