---
phase: 067-observability-alerting-automation
plan: 09
type: execute
wave: 2
depends_on: ["067-08"]
files_modified:
  - services/bar_auditor_agent.py
  - production/scripts/provision_dlq_topics.sh
  - src/core/stream_keys.py
  - services/parity_auditor_agent.py
autonomous: true
gap_closure: true
requirements:
  - OBS-BAR-AUDITOR
  - OBS-DASHBOARDS

must_haves:
  truths:
    - "BarAuditorAgent does not publish gap requests for already-resolved gaps"
    - "All DLQ topics defined in stream_keys.py are provisioned in the shell script"
    - "No duplicate DLQ topic function definitions in stream_keys.py"
    - "ParityAuditorAgent exposes metrics on a single port, not two"
  artifacts:
    - path: "services/bar_auditor_agent.py"
      provides: "Fixed gap request logic — no requests for resolved gaps"
    - path: "production/scripts/provision_dlq_topics.sh"
      provides: "Complete DLQ topic provisioning for all defined topics"
      min_lines: 20
    - path: "src/core/stream_keys.py"
      provides: "No duplicate topic_swarm_writer_dlq definition"
    - path: "services/parity_auditor_agent.py"
      provides: "Single metrics port via BaseAgent.__init__ only"
  key_links:
    - from: "services/bar_auditor_agent.py"
      to: "market_data_gaps table"
      via: "completeness >= 1.0 branch"
      pattern: "resolve.*gap.*NOT.*append.*gap_request"
---

<objective>
Fix four code quality issues: BarAuditorAgent duplicate gap requests, DLQ provisioning completeness, stream_keys.py duplicate definition, and ParityAuditorAgent dual metrics port.

Purpose: These are correctness and reliability bugs that cause wasted gap-fill requests, incomplete DLQ coverage, confusing duplicate definitions, and a port conflict risk.

Output: Four files with targeted fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/067-observability-alerting-automation/067-VERIFICATION.md
@.planning/phases/067-observability-alerting-automation/067-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix BarAuditorAgent duplicate gap request for resolved gaps</name>
  <files>services/bar_auditor_agent.py</files>
  <read_first>
    - services/bar_auditor_agent.py (lines 320-373, the gap detection branch that handles both resolved and unresolved gaps)
  </read_first>
  <action>
    In `services/bar_auditor_agent.py`, the `elif completeness >= 1.0:` branch (around line 342) currently does THREE things:
    1. Calls `_resolve_market_data_gap()` (correct)
    2. Appends a new `BarGapRequest` to the `gaps` list (WRONG — this requests a gap fill for an already-resolved gap)
    3. Adds to `_requested_today` (unnecessary since no request needed)

    Fix: In the `elif completeness >= 1.0:` branch, AFTER calling `_resolve_market_data_gap()`, REMOVE the dedup logic (lines 348-354), the gap_detected warning log (lines 356-364), the `gaps.append()` call (lines 365-371), and the `_requested_today.add()` call (line 373). Keep ONLY the `_resolve_market_data_gap()` call.

    The resolved branch should become simply:
    ```python
    elif completeness >= 1.0:
        await self._resolve_market_data_gap(
            conn, instrument.symbol, "1m", date_start_utc
        )
    ```

    The code AFTER the if/elif block (HTF completeness section starting around line 375) should remain unchanged.
  </action>
  <verify>
    <automated>grep -A5 "elif completeness >= 1.0:" services/bar_auditor_agent.py | grep -c "BarGapRequest"; echo "Expected: 0 (no gap request in resolved branch)"</automated>
  </verify>
  <acceptance_criteria>
    - The `elif completeness >= 1.0:` branch contains ONLY the `_resolve_market_data_gap()` call
    - No `BarGapRequest` construction or `gaps.append()` in the completeness >= 1.0 branch
    - No `_requested_today.add()` in the completeness >= 1.0 branch
    - The HTF completeness section below remains unchanged
  </acceptance_criteria>
  <done>BarAuditorAgent no longer publishes gap-fill requests for already-resolved gaps.</done>
</task>

<task type="auto">
  <name>Task 2: Fix DLQ provisioning script + remove duplicate stream_keys.py definition</name>
  <files>production/scripts/provision_dlq_topics.sh, src/core/stream_keys.py</files>
  <read_first>
    - production/scripts/provision_dlq_topics.sh (full file, currently 11 topics)
    - src/core/stream_keys.py (lines 317-325 for the FIRST topic_swarm_writer_dlq, and lines 392-395 for the DUPLICATE)
  </read_first>
  <action>
    **Part A: Remove duplicate in stream_keys.py**

    The function `topic_swarm_writer_dlq` is defined TWICE in stream_keys.py:
    - First definition at line 322-324 (in the Swarm topics section, lines 288-325)
    - Second definition at line 392-395 (in the DLQ topics section, lines 367-425)

    Both return the same value: `f"{env_prefix(env_name)}swarm.writer.dlq"`. The FIRST definition (lines 322-324) should be REMOVED since the DLQ section already has it. Remove lines 322-324 (the function definition and its docstring) from the Swarm topics section. Keep the one in the DLQ section (lines 392-395).

    **Part B: Add missing DLQ topics to provisioning script**

    The provisioning script currently creates 11 topics. Add the following DLQ topics that exist as functions in stream_keys.py but are missing from the script. Add them in logical groups:

    After the "Writer agent DLQs" section, add a comment `# Pre-existing DLQ topics (defined before Plan 067-07)` with:
    ```bash
    docker exec redpanda rpk topic create ${ENV_NAME}.market.events.roll.dlq --config retention.ms=${RETENTION_MS}
    docker exec redpanda rpk topic create ${ENV_NAME}.intelligence.service_auditor.journal.dlq --config retention.ms=${RETENTION_MS}
    docker exec redpanda rpk topic create ${ENV_NAME}.intelligence.signal.dlq --config retention.ms=${RETENTION_MS}
    docker exec redpanda rpk topic create ${ENV_NAME}.swarm.orchestrator.dlq --config retention.ms=${RETENTION_MS}
    docker exec redpanda rpk topic create ${ENV_NAME}.ml.orchestrator.dlq --config retention.ms=${RETENTION_MS}
    docker exec redpanda rpk topic create ${ENV_NAME}.gap_fill.dlq --config retention.ms=${RETENTION_MS}
    ```

    After adding, the total should be 17 topics (11 original + 6 new). Note: the `gap_fill.dlq` topic was added by Plan 067-01 but not included in the Plan 067-07 provisioning script.
  </action>
  <verify>
    <automated>grep -c "def topic_swarm_writer_dlq" src/core/stream_keys.py; echo "Expected: 1 (no duplicate)"; grep -c "rpk topic create" production/scripts/provision_dlq_topics.sh; echo "Expected: 17 total topics"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 1 definition of `topic_swarm_writer_dlq` in stream_keys.py
    - The remaining definition is in the DLQ topics section (around line 392)
    - 17 `rpk topic create` commands in provision_dlq_topics.sh
    - All 6 new topics match the output of their corresponding `topic_*` function
  </acceptance_criteria>
  <done>DLQ provisioning script creates all defined DLQ topics. No duplicate function definitions in stream_keys.py.</done>
</task>

<task type="auto">
  <name>Task 3: Fix ParityAuditorAgent dual metrics port</name>
  <files>services/parity_auditor_agent.py</files>
  <read_first>
    - services/parity_auditor_agent.py (lines 357-367, the main() function)
    - services/parity_auditor_agent.py (lines 190-193, the __init__ which passes metrics_port=METRICS_PORT to super())
    - src/core/agent/base.py (lines 137-139, the start() method which calls start_metrics_server if metrics_port is set)
  </read_first>
  <action>
    In `services/parity_auditor_agent.py`, the `main()` function (around line 360-363) calls `start_metrics_server(port=METRICS_PORT)` BEFORE `agent.start()`. But `agent.start()` also calls `start_metrics_server(port=self._metrics_port)` because `metrics_port=METRICS_PORT` is passed to `super().__init__()`. This starts the metrics server twice on port 9133.

    Fix: Remove the standalone `start_metrics_server(port=METRICS_PORT)` call from `main()`. The `BaseAgent.start()` method already handles this.

    Change `main()` from:
    ```python
    async def main() -> None:
        start_metrics_server(port=METRICS_PORT)
        agent = ParityAuditorAgent()
        await agent.start()
    ```
    To:
    ```python
    async def main() -> None:
        agent = ParityAuditorAgent()
        await agent.start()
    ```

    Also remove the unused import of `start_metrics_server` if it exists and is no longer used elsewhere in the file. Check if `start_metrics_server` is imported at the top of the file and whether it's used anywhere else besides the removed call. If it's only used in `main()`, remove the import.
  </action>
  <verify>
    <automated>grep -n "start_metrics_server" services/parity_auditor_agent.py; echo "Expected: only the import line (if still used elsewhere) or zero matches"</automated>
  </verify>
  <acceptance_criteria>
    - No `start_metrics_server()` call in `main()` function
    - `super().__init__()` still receives `metrics_port=METRICS_PORT`
    - ParityAuditorAgent metrics will be served on port 9133 via BaseAgent.start()
  </acceptance_criteria>
  <done>ParityAuditorAgent exposes metrics on a single port (9133) via BaseAgent.start() only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BarAuditorAgent -> IBKR | Gap fill requests trigger IBKR API calls; duplicate requests waste pacing budget |
| Provisioning script -> Redpanda | Missing topics cause DLQ messages to fail silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-067-09-01 | Denial of Service | bar_auditor_agent.py | mitigate | Remove duplicate gap requests to prevent IBKR pacing violations |
| T-067-09-02 | Information Disclosure | provision_dlq_topics.sh | mitigate | Missing DLQ topics mean errors go untracked — add all defined topics |
</threat_model>

<verification>
- BarAuditorAgent resolved branch has no BarGapRequest construction
- stream_keys.py has exactly 1 `topic_swarm_writer_dlq` definition
- Provisioning script has 17 topic create commands
- ParityAuditorAgent main() has no standalone start_metrics_server call
</verification>

<success_criteria>
- BarAuditorAgent no longer publishes gap-fill requests for resolved gaps
- All DLQ topics from stream_keys.py are provisioned in the shell script
- No duplicate function definitions in stream_keys.py
- ParityAuditorAgent metrics on single port
</success_criteria>

<output>
After completion, create `.planning/phases/067-observability-alerting-automation/067-09-SUMMARY.md`
</output>
