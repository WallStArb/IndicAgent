# Plan 3: Grafana Alerting + Roll Automation + Bar Auditor DLQ

**Phase:** 67 — Observability, Alerting & Automation
**Plan:** 3 of 4
**Scope:** Grafana contact-point and alert-rule provisioning YAML; roll event consumer + auto-restart in service_auditor_agent; bar_auditor_agent market_data_gaps write path and gap-fill DLQ routing.
**Depends on:** Plan 1 (webhook dispatcher, BAR_AUDITOR_GAP_FILL_DLQ_DEPTH counter, topic_gap_fill_dlq stream key, 062 migration must exist)
**Wave:** 2

---

## Goal

Three gaps closed in one plan because they share a dependency chain: Plan 1's webhook dispatcher enables the roll automation alert, the DLQ counter enables the Grafana HIGH rule, and the migration enables the market_data_gaps write path. After this plan, a futures roll fires an automatic `ibkr-provider` restart, every detected gap is persisted for ML exclusion, and exhausted gap-fill retries land in a DLQ rather than silently disappearing.

---

## Threat Model

- **`subprocess.run` injection risk.** `_restart_ibkr_provider` must use a fixed list — `["sudo", "systemctl", "restart", "indicagent-ibkr-provider"]` — never string interpolation. The service name is a constant, not user input.
- **Sudoers scope is minimal.** The one-line sudoers entry allows ONLY `systemctl restart indicagent-ibkr-provider`, not a wildcard. Document this requirement; do NOT automate the sudoers edit.
- **Roll dedup prevents double restart.** `_handled_rolls: set[tuple[str, str]]` keyed by `(symbol, new_expiry)` is in-memory. On process restart the set is cleared — acceptable, because after a process restart the consumer replays from latest and the roll event is likely stale. The dedup is a within-process guard against Kafka at-least-once redelivery.
- **market_data_gaps UPSERT is idempotent.** `ON CONFLICT (symbol, tf, gap_start_ts) DO UPDATE` — safe to call on every 5-minute audit cycle without creating duplicate rows.
- **DLQ payload contains no credentials.** `{symbol, tf, start_ts, end_ts, retry_count, error}` — no PII, no secrets.
- **Contact-points.yml is gitignored.** Real credentials live only in `contact-points.yml` (gitignored). `contact-points.example.yml` is committed with placeholder values. Never commit actual tokens.

---

## TDD Task List

### Task 1: Grafana Contact Points — Example File (committed) + Production File (gitignored)
**Files:** `production/grafana/provisioning/alerting/contact-points.example.yml`, `production/grafana/provisioning/alerting/contact-points.yml`
**Test first:** Smoke test that example file parses as valid YAML with required keys.

- [ ] Write `tests/unit/test_grafana_provisioning.py::test_contact_points_example_valid_yaml`:
  ```python
  import yaml, pathlib
  def test_contact_points_example_valid_yaml():
      path = pathlib.Path("production/grafana/provisioning/alerting/contact-points.example.yml")
      data = yaml.safe_load(path.read_text())
      assert "contactPoints" in data or "apiVersion" in data
      contact_names = [cp["name"] for cp in data.get("contactPoints", [])]
      assert "telegram-critical" in contact_names
      assert "discord-ops" in contact_names
  ```

- [ ] Create directory: `production/grafana/provisioning/alerting/`

- [ ] Create `production/grafana/provisioning/alerting/contact-points.example.yml`:

  ```yaml
  # contact-points.example.yml — COMMITTED (placeholder values only)
  # Copy to contact-points.yml and fill in real credentials.
  # contact-points.yml is gitignored — never commit it.
  apiVersion: 1

  contactPoints:
    - orgId: 1
      name: telegram-critical
      receivers:
        - uid: telegram-critical-uid
          type: telegram
          settings:
            bottoken: YOUR_BOT_TOKEN
            chatid: "YOUR_CHAT_ID"
            message: |
              {{ template "default.message" . }}
          disableResolveMessage: false

    - orgId: 1
      name: discord-ops
      receivers:
        - uid: discord-ops-uid
          type: webhook
          settings:
            url: YOUR_DISCORD_WEBHOOK_URL
            httpMethod: POST
          disableResolveMessage: false
  ```

- [ ] Create `production/grafana/provisioning/alerting/contact-points.yml` (for local use — gitignored, contains example values):

  ```yaml
  # contact-points.yml — GITIGNORED
  # This file is created locally. Replace placeholder values with real credentials.
  # See contact-points.example.yml for structure reference.
  apiVersion: 1

  contactPoints:
    - orgId: 1
      name: telegram-critical
      receivers:
        - uid: telegram-critical-uid
          type: telegram
          settings:
            bottoken: REPLACE_WITH_REAL_BOT_TOKEN
            chatid: "REPLACE_WITH_REAL_CHAT_ID"
            message: |
              {{ template "default.message" . }}
          disableResolveMessage: false

    - orgId: 1
      name: discord-ops
      receivers:
        - uid: discord-ops-uid
          type: webhook
          settings:
            url: REPLACE_WITH_REAL_DISCORD_WEBHOOK_URL
            httpMethod: POST
          disableResolveMessage: false
  ```

- [ ] Verify smoke test passes

### Task 2: Grafana Alert Rules YAML
**File:** `production/grafana/provisioning/alerting/alert-rules.yml`
**Test first:** Smoke test for YAML parse + required rule names.

- [ ] Write `tests/unit/test_grafana_provisioning.py::test_alert_rules_valid_yaml_and_complete`:
  ```python
  import yaml, pathlib
  def test_alert_rules_valid_yaml_and_complete():
      data = yaml.safe_load(
          pathlib.Path("production/grafana/provisioning/alerting/alert-rules.yml").read_text()
      )
      groups = data.get("groups", [])
      assert len(groups) >= 1
      all_rule_names = [r["title"] for g in groups for r in g.get("rules", [])]
      required = [
          "provider_dead",
          "service_crash_looping",
          "signals_dropped",
          "signal_dlq_growing",
          "data_completeness_critical",
          "consumer_lag_writer",
          "bar_flow_stale",
          "output_buffer_pressure",
          "gap_fill_dlq_growing",
          "data_completeness_soft",
      ]
      for name in required:
          assert name in all_rule_names, f"Missing rule: {name}"
  ```

- [ ] Create `production/grafana/provisioning/alerting/alert-rules.yml`:

  ```yaml
  # alert-rules.yml — COMMITTED
  # Grafana unified alerting rules for IndicAgent pipeline.
  # Contact points: telegram-critical (CRITICAL), discord-ops (HIGH/MEDIUM)
  # All PromQL expressions reference current active service names only.
  apiVersion: 1

  groups:
    - orgId: 1
      name: IndicAgent CRITICAL Alerts
      folder: IndicAgent
      interval: 1m
      rules:

        - uid: provider_dead_uid
          title: provider_dead
          condition: C
          data:
            - refId: A
              datasourceUid: prometheus
              model:
                expr: rate(provider_bars_produced_total[5m])
                intervalMs: 60000
                maxDataPoints: 43200
                refId: A
            - refId: C
              datasourceUid: "__expr__"
              model:
                conditions:
                  - evaluator:
                      params: [0]
                      type: lt
                    operator:
                      type: and
                    query:
                      params: [A]
                    reducer:
                      type: last
                type: classic_conditions
                refId: C
          noDataState: NoData
          execErrState: Error
          for: 5m
          annotations:
            summary: "IBKR provider producing zero bars for 5+ minutes"
            description: "rate(provider_bars_produced_total[5m]) == 0. Check indicagent-ibkr-provider."
          labels:
            severity: critical
            contact_point: telegram-critical

        - uid: service_crash_looping_uid
          title: service_crash_looping
          condition: C
          data:
            - refId: A
              datasourceUid: prometheus
              model:
                expr: rate(service_auditor_service_restarts_total[10m])
                intervalMs: 60000
                maxDataPoints: 43200
                refId: A
            - refId: C
              datasourceUid: "__expr__"
              model:
                conditions:
                  - evaluator:
                      params: [0.005]
                      type: gt
                    operator:
                      type: and
                    query:
                      params: [A]
                    reducer:
                      type: last
                type: classic_conditions
                refId: C
          noDataState: NoData
          execErrState: Error
          for: 0s
          annotations:
            summary: "Service crash-looping: > 3 restarts in 10 minutes"
            description: "rate(service_auditor_service_restarts_total[10m]) > 3/600s. Service: {{ $labels.service_name }}"
          labels:
            severity: critical
            contact_point: telegram-critical

        - uid: signals_dropped_uid
          title: signals_dropped
          condition: C
          data:
            - refId: A
              datasourceUid: prometheus
              model:
                expr: increase(signal_writer_buffer_dropped_total[5m])
                intervalMs: 60000
                maxDataPoints: 43200
                refId: A
            - refId: C
              datasourceUid: "__expr__"
              model:
                conditions:
                  - evaluator:
                      params: [0]
                      type: gt
                    operator:
                      type: and
                    query:
                      params: [A]
                    reducer:
                      type: last
                type: classic_conditions
                refId: C
          noDataState: NoData
          execErrState: Error
          for: 0s
          annotations:
            summary: "Signals being dropped by signal writer buffer"
            description: "increase(signal_writer_buffer_dropped_total[5m]) > 0. Training data loss in progress."
          labels:
            severity: critical
            contact_point: telegram-critical

        - uid: signal_dlq_growing_uid
          title: signal_dlq_growing
          condition: C
          data:
            - refId: A
              datasourceUid: prometheus
              model:
                expr: increase(intelligence_pipeline_signal_dlq_total[5m])
                intervalMs: 60000
                maxDataPoints: 43200
                refId: A
            - refId: C
              datasourceUid: "__expr__"
              model:
                conditions:
                  - evaluator:
                      params: [0]
                      type: gt
                    operator:
                      type: and
                    query:
                      params: [A]
                    reducer:
                      type: last
                type: classic_conditions
                refId: C
          noDataState: NoData
          execErrState: Error
          for: 0s
          annotations:
            summary: "Signal DLQ growing — null-CIS signals detected"
            description: "increase(intelligence_pipeline_signal_dlq_total[5m]) > 0. CIS regression in I7 pipeline."
          labels:
            severity: critical
            contact_point: telegram-critical

        - uid: data_completeness_critical_uid
          title: data_completeness_critical
          condition: C
          data:
            - refId: A
              datasourceUid: prometheus
              model:
                expr: bar_auditor_canonical_completeness_pct
                intervalMs: 60000
                maxDataPoints: 43200
                refId: A
            - refId: C
              datasourceUid: "__expr__"
              model:
                conditions:
                  - evaluator:
                      params: [0.9]
                      type: lt
                    operator:
                      type: and
                    query:
                      params: [A]
                    reducer:
                      type: last
                type: classic_conditions
                refId: C
          noDataState: NoData
          execErrState: Error
          for: 5m
          annotations:
            summary: "Data completeness below 90% — ML training impact"
            description: "bar_auditor_canonical_completeness_pct < 90% for {{ $labels.symbol }}/{{ $labels.tf }}."
          labels:
            severity: critical
            contact_point: telegram-critical

    - orgId: 1
      name: IndicAgent HIGH Alerts
      folder: IndicAgent
      interval: 1m
      rules:

        - uid: consumer_lag_writer_uid
          title: consumer_lag_writer
          condition: C
          data:
            - refId: A
              datasourceUid: prometheus
              model:
                expr: persistence_consumer_lag_records
                intervalMs: 60000
                maxDataPoints: 43200
                refId: A
            - refId: C
              datasourceUid: "__expr__"
              model:
                conditions:
                  - evaluator:
                      params: [1000]
                      type: gt
                    operator:
                      type: and
                    query:
                      params: [A]
                    reducer:
                      type: last
                type: classic_conditions
                refId: C
          noDataState: NoData
          execErrState: Error
          for: 2m
          annotations:
            summary: "Writer consumer lag > 1000 messages"
            description: "persistence_consumer_lag_records > 1000 for agent {{ $labels.agent_id }}. DB write backlog."
          labels:
            severity: high
            contact_point: discord-ops

        - uid: bar_flow_stale_uid
          title: bar_flow_stale
          condition: C
          data:
            - refId: A
              datasourceUid: prometheus
              model:
                expr: bar_agg_time_since_last_bar_seconds
                intervalMs: 60000
                maxDataPoints: 43200
                refId: A
            - refId: C
              datasourceUid: "__expr__"
              model:
                conditions:
                  - evaluator:
                      params: [300]
                      type: gt
                    operator:
                      type: and
                    query:
                      params: [A]
                    reducer:
                      type: last
                type: classic_conditions
                refId: C
          noDataState: NoData
          execErrState: Error
          for: 1m
          annotations:
            summary: "Bar flow stale — no bars in 5+ minutes"
            description: "bar_agg_time_since_last_bar_seconds > 300. Aggregator may be stuck."
          labels:
            severity: high
            contact_point: discord-ops

        - uid: output_buffer_pressure_uid
          title: output_buffer_pressure
          condition: C
          data:
            - refId: A
              datasourceUid: prometheus
              model:
                expr: intelligence_pipeline_output_buffer_depth
                intervalMs: 60000
                maxDataPoints: 43200
                refId: A
            - refId: C
              datasourceUid: "__expr__"
              model:
                conditions:
                  - evaluator:
                      params: [400]
                      type: gt
                    operator:
                      type: and
                    query:
                      params: [A]
                    reducer:
                      type: last
                type: classic_conditions
                refId: C
          noDataState: NoData
          execErrState: Error
          for: 1m
          annotations:
            summary: "Intelligence pipeline output buffer > 400 depth"
            description: "intelligence_pipeline_output_buffer_depth > 400. Writer is falling behind."
          labels:
            severity: high
            contact_point: discord-ops

        - uid: gap_fill_dlq_growing_uid
          title: gap_fill_dlq_growing
          condition: C
          data:
            - refId: A
              datasourceUid: prometheus
              model:
                expr: increase(bar_auditor_gap_fill_dlq_depth[5m])
                intervalMs: 60000
                maxDataPoints: 43200
                refId: A
            - refId: C
              datasourceUid: "__expr__"
              model:
                conditions:
                  - evaluator:
                      params: [0]
                      type: gt
                    operator:
                      type: and
                    query:
                      params: [A]
                    reducer:
                      type: last
                type: classic_conditions
                refId: C
          noDataState: NoData
          execErrState: Error
          for: 0s
          annotations:
            summary: "Gap fill DLQ growing — gap requests failing after retry"
            description: "increase(bar_auditor_gap_fill_dlq_depth[5m]) > 0. IBKR gap fill retries exhausted."
          labels:
            severity: high
            contact_point: discord-ops

    - orgId: 1
      name: IndicAgent MEDIUM Alerts
      folder: IndicAgent
      interval: 5m
      rules:

        - uid: data_completeness_soft_uid
          title: data_completeness_soft
          condition: C
          data:
            - refId: A
              datasourceUid: prometheus
              model:
                expr: bar_auditor_canonical_completeness_pct
                intervalMs: 300000
                maxDataPoints: 43200
                refId: A
            - refId: C
              datasourceUid: "__expr__"
              model:
                conditions:
                  - evaluator:
                      params: [0.95]
                      type: lt
                    operator:
                      type: and
                    query:
                      params: [A]
                    reducer:
                      type: last
                type: classic_conditions
                refId: C
          noDataState: NoData
          execErrState: Error
          for: 10m
          annotations:
            summary: "Data completeness below 95% soft threshold"
            description: "bar_auditor_canonical_completeness_pct < 95% for {{ $labels.symbol }}/{{ $labels.tf }}."
          labels:
            severity: medium
            contact_point: discord-ops
  ```

- [ ] Verify alert rules smoke test passes

### Task 3: Roll Event Consumer in ServiceAuditorAgent
**File:** `services/service_auditor_agent.py`
**Test first:** `tests/unit/service_tests/test_service_auditor_roll_consumer.py` (new file)

- [ ] Write failing test: `test_handle_roll_event_fires_on_roll_complete_only`
  - Mock `_restart_ibkr_provider`, `_dispatch_webhook`
  - Call `await agent._handle_roll_event({"event_type": "roll_complete", "symbol": "ES", "new_expiry": "ESM6"})`
  - Assert `_restart_ibkr_provider` called once
  - Call again with `event_type="roll_imminent"` — assert `_restart_ibkr_provider` NOT called
  - Call again with `event_type="roll_detected"` — assert `_restart_ibkr_provider` NOT called

- [ ] Write failing test: `test_handle_roll_event_dedup_prevents_double_restart`
  - Mock `_restart_ibkr_provider`
  - Call `_handle_roll_event` twice with same `(symbol, new_expiry)`
  - Assert `_restart_ibkr_provider` called exactly once (second call deduped)

- [ ] Write failing test: `test_restart_ibkr_provider_increments_counter`
  - Mock `subprocess.run` to return `CompletedProcess(returncode=0)`
  - Call `await agent._restart_ibkr_provider()`
  - Assert `SERVICE_AUDITOR_SERVICE_RESTARTS_TOTAL.labels(service_name="indicagent-ibkr-provider")._value.get() == 1`

- [ ] Write failing test: `test_restart_ibkr_provider_subprocess_failure_is_logged`
  - Mock `subprocess.run` to raise `subprocess.CalledProcessError(1, "systemctl")`
  - Call `await agent._restart_ibkr_provider()`
  - Assert no exception propagates; log error is captured

- [ ] Implement in `services/service_auditor_agent.py`:

  Add to `__init__` after `_service_states`:
  ```python
  self._handled_rolls: set[tuple[str, str]] = set()
  ```

  Add to `_run()` — create roll task alongside existing tasks:
  ```python
  async def _run(self) -> None:
      prom_task = asyncio.create_task(self._prometheus_check_loop())
      sysd_task = asyncio.create_task(self._systemd_check_loop())
      hb_task = asyncio.create_task(self._heartbeat_loop())
      roll_task = asyncio.create_task(self._roll_consumer_loop())
      await self._stop_event.wait()
      for t in (prom_task, sysd_task, hb_task, roll_task):
          t.cancel()
      for t in (prom_task, sysd_task, hb_task, roll_task):
          try:
              await t
          except asyncio.CancelledError:
              pass
  ```

  Add roll Kafka consumer to `_setup()`:
  ```python
  from src.core.kafka_utils import KafkaConsumerClient
  from src.core.stream_keys import topic_roll_events
  # ... (in _setup, after kafka_producer started)
  self._roll_consumer = KafkaConsumerClient(
      topic_roll_events(self._env_name),
      bootstrap_servers=self._settings.kafka_bootstrap_servers,
      group_id="service_auditor_roll_consumer",
      auto_offset_reset="latest",
  )
  await self._roll_consumer.start()
  ```

  Add to `_teardown()`:
  ```python
  if self._roll_consumer:
      await self._roll_consumer.stop()
  ```

  Add `_roll_consumer_loop`, `_handle_roll_event`, `_restart_ibkr_provider` methods:
  ```python
  async def _roll_consumer_loop(self) -> None:
      """Consume topic_roll_events and trigger ibkr-provider restart on roll_complete."""
      if self._roll_consumer is None:
          return
      async for _topic, _key, payload in self._roll_consumer.messages():
          if self._stop_event.is_set():
              break
          try:
              await self._handle_roll_event(payload)
          except Exception as exc:
              self.logger.error("roll_consumer.error", error=str(exc))

  async def _handle_roll_event(self, payload: dict) -> None:
      """Process a RollEvent. Only fires on roll_complete; deduped by (symbol, new_expiry)."""
      event_type = payload.get("event_type")
      if event_type != "roll_complete":
          return

      symbol = payload.get("symbol", "")
      new_expiry = payload.get("new_expiry", "")
      old_expiry = payload.get("old_expiry", "")
      dedup_key = (symbol, new_expiry)

      if dedup_key in self._handled_rolls:
          self.logger.debug("roll_consumer.dedup_skip", symbol=symbol, new_expiry=new_expiry)
          return

      self._handled_rolls.add(dedup_key)
      self.logger.info(
          "roll_automation.triggered",
          symbol=symbol,
          old_expiry=old_expiry,
          new_expiry=new_expiry,
      )

      await self._dispatch_webhook(
          "HIGH",
          f"Futures roll: {symbol} {old_expiry} → {new_expiry}",
          "Restarting indicagent-ibkr-provider to subscribe to new front-month contract.",
      )
      await self._restart_ibkr_provider()

  async def _restart_ibkr_provider(self) -> None:
      """Restart indicagent-ibkr-provider via sudo systemctl.

      Requires sudoers entry (one-time manual setup):
          bg ALL=(ALL) NOPASSWD: /usr/bin/systemctl restart indicagent-ibkr-provider

      Uses subprocess.run (blocking) — acceptable since this is an infrequent operation
      (at most once per futures roll cycle, ~4x/year per contract).
      """
      import subprocess
      cmd = ["sudo", "systemctl", "restart", "indicagent-ibkr-provider"]
      try:
          subprocess.run(cmd, check=True, capture_output=True)
          SERVICE_AUDITOR_SERVICE_RESTARTS_TOTAL.labels(
              service_name="indicagent-ibkr-provider"
          ).inc()
          self.logger.info("roll_automation.restart_complete", service="indicagent-ibkr-provider")
      except subprocess.CalledProcessError as exc:
          self.logger.error(
              "roll_automation.restart_failed",
              service="indicagent-ibkr-provider",
              returncode=exc.returncode,
              stderr=exc.stderr.decode() if exc.stderr else "",
          )
  ```

  Add import at top of file:
  ```python
  from src.observability.metrics import SERVICE_AUDITOR_SERVICE_RESTARTS_TOTAL
  ```

- [ ] Add `_roll_consumer: KafkaConsumerClient | None = None` to `__init__` instance variables

- [ ] Verify all four roll consumer tests pass

### Task 4: BarAuditorAgent — market_data_gaps Write Path + DLQ Routing
**File:** `services/bar_auditor_agent.py`
**Test first:** `tests/unit/service_tests/test_bar_auditor_gaps.py` (new file)

- [ ] Write failing test: `test_upsert_gap_on_incomplete_audit`
  - Mock DB pool with `asyncpg.Pool`
  - Call `await agent._upsert_market_data_gap(conn, symbol="ES", tf="1m", gap_start_ts=dt, bars_expected=390, bars_missing=45)`
  - Assert `conn.execute` called with SQL containing `INSERT INTO market_data_gaps` and `ON CONFLICT`

- [ ] Write failing test: `test_resolve_gap_on_complete_audit`
  - Mock `conn.fetchval` to return a row id (open gap exists)
  - Call `await agent._resolve_market_data_gap(conn, symbol="ES", tf="1m", gap_start_ts=dt)`
  - Assert `conn.execute` called with SQL containing `UPDATE market_data_gaps` and `resolved_at`

- [ ] Write failing test: `test_resolve_gap_noop_when_no_open_gap`
  - Mock `conn.fetchval` to return `None` (no open gap)
  - Assert `conn.execute` NOT called

- [ ] Write failing test: `test_gap_requests_consumer_uses_earliest_offset`
  - Inspect `BarAuditorAgent.__init__` or `_setup` to verify no explicit `auto_offset_reset` is set on the gap_requests consumer (it doesn't exist yet — we're adding it)
  - After implementation: assert `gap_requests_consumer` is created with `auto_offset_reset="earliest"`

- [ ] Write failing test: `test_dlq_published_on_retry_exhaustion`
  - Mock `_kafka_producer.publish` to capture calls
  - Mock the retry mechanism to exhaust after 3 attempts
  - Call `await agent._publish_gap_fill_dlq(symbol="ES", tf="1m", start_ts=dt, end_ts=dt2, retry_count=3, error="timeout")`
  - Assert `publish` called with topic containing `gap_fill.dlq`
  - Assert `BAR_AUDITOR_GAP_FILL_DLQ_DEPTH` incremented

- [ ] Implement in `services/bar_auditor_agent.py`:

  Add imports at top:
  ```python
  import asyncpg
  from src.core.stream_keys import topic_gap_fill_dlq
  from src.observability.metrics import BAR_AUDITOR_GAP_FILL_DLQ_DEPTH
  ```

  Add to `__init__` after `_requested_today_date`:
  ```python
  # Gap request dedup: (symbol, tf, date_str) — cleared every 24h
  self._seen_gap_requests: set[tuple[str, str, str]] = set()
  self._seen_gap_requests_date: str = ""

  # Module-level counter (already registered in metrics.py via Plan 1)
  self._gap_fill_dlq_depth = BAR_AUDITOR_GAP_FILL_DLQ_DEPTH
  ```

  Add private methods for market_data_gaps write path:
  ```python
  async def _upsert_market_data_gap(
      self,
      conn: asyncpg.Connection,
      symbol: str,
      tf: str,
      gap_start_ts: datetime,
      bars_expected: int,
      bars_missing: int,
  ) -> None:
      """UPSERT a gap row. Updates bars_missing if row already exists."""
      await conn.execute(
          """
          INSERT INTO market_data_gaps
              (symbol, tf, gap_start_ts, bars_expected, bars_missing, detected_at)
          VALUES ($1, $2, $3, $4, $5, $6)
          ON CONFLICT (symbol, tf, gap_start_ts)
          DO UPDATE SET
              bars_missing = EXCLUDED.bars_missing,
              detected_at  = EXCLUDED.detected_at
          """,
          symbol,
          tf,
          gap_start_ts,
          bars_expected,
          bars_missing,
          datetime.now(UTC),
      )

  async def _resolve_market_data_gap(
      self,
      conn: asyncpg.Connection,
      symbol: str,
      tf: str,
      gap_start_ts: datetime,
  ) -> None:
      """Mark an open gap as resolved (completeness reached 100%)."""
      existing_id = await conn.fetchval(
          """
          SELECT id FROM market_data_gaps
          WHERE symbol = $1 AND tf = $2 AND gap_start_ts = $3
            AND resolved_at IS NULL
          """,
          symbol,
          tf,
          gap_start_ts,
      )
      if existing_id is None:
          return
      now = datetime.now(UTC)
      await conn.execute(
          """
          UPDATE market_data_gaps
          SET resolved_at = $1, gap_end_ts = $1
          WHERE id = $2
          """,
          now,
          existing_id,
      )

  async def _publish_gap_fill_dlq(
      self,
      symbol: str,
      tf: str,
      start_ts: datetime,
      end_ts: datetime,
      retry_count: int,
      error: str,
  ) -> None:
      """Publish unresolvable gap request to DLQ after retry exhaustion."""
      payload = {
          "symbol": symbol,
          "tf": tf,
          "start_ts": start_ts.isoformat(),
          "end_ts": end_ts.isoformat(),
          "retry_count": retry_count,
          "error": error,
      }
      await self._kafka_producer.publish(
          topic_gap_fill_dlq(self._env_name),
          payload,
          key=symbol,
      )
      self._gap_fill_dlq_depth.inc()
      self.logger.warning(
          "bar_auditor_agent.gap_fill_dlq",
          symbol=symbol,
          tf=tf,
          retry_count=retry_count,
          error=error,
      )
  ```

  Wire `_upsert_market_data_gap` / `_resolve_market_data_gap` into `_detect_gaps`: after computing `completeness` for each (instrument, date), if `completeness < threshold`, also call `_upsert_market_data_gap` with the gap parameters. If `completeness == 1.0`, call `_resolve_market_data_gap`. Pass `conn` (already in scope inside the async-with block).

  ```python
  # Inside _detect_gaps, after setting canonical_completeness gauge:
  if completeness < threshold:
      await self._upsert_market_data_gap(
          conn,
          instrument.symbol,
          "1m",
          date_start_utc,
          expected,
          expected - actual,
      )
  elif completeness >= 1.0:
      await self._resolve_market_data_gap(conn, instrument.symbol, "1m", date_start_utc)
  ```

- [ ] Verify all five bar auditor gap tests pass

### Manual Setup Note (NOT automated)
The sudoers entry for roll automation MUST be added manually by the operator before the feature is functional. Add to `/etc/sudoers.d/indicagent-roll`:
```
bg ALL=(ALL) NOPASSWD: /usr/bin/systemctl restart indicagent-ibkr-provider
```
Verify with: `sudo -l | grep indicagent-ibkr-provider`

---

## Verification

```bash
# All plan 3 tests pass
.venv/bin/pytest \
  tests/unit/test_grafana_provisioning.py \
  tests/unit/service_tests/test_service_auditor_roll_consumer.py \
  tests/unit/service_tests/test_bar_auditor_gaps.py \
  -v

# Full unit suite still clean
.venv/bin/pytest tests/unit/ -v --tb=short

# Zero ruff violations
.venv/bin/ruff check . --fix

# Formatted
.venv/bin/black .

# Confirm alerting YAML parses
python3 -c "
import yaml, pathlib
d = yaml.safe_load(pathlib.Path('production/grafana/provisioning/alerting/alert-rules.yml').read_text())
rules = [r['title'] for g in d['groups'] for r in g['rules']]
print('Alert rules:', rules)
assert len(rules) == 10
print('OK — 10 rules defined')
"

# Confirm topic_gap_fill_dlq is wired in bar_auditor_agent
python3 -c "
import ast, pathlib
src = pathlib.Path('services/bar_auditor_agent.py').read_text()
assert 'topic_gap_fill_dlq' in src
assert '_upsert_market_data_gap' in src
assert '_resolve_market_data_gap' in src
print('bar_auditor_agent: OK')
"

# Confirm roll consumer wired in service_auditor_agent
python3 -c "
src = open('services/service_auditor_agent.py').read()
assert '_handle_roll_event' in src
assert '_restart_ibkr_provider' in src
assert 'service_auditor_roll_consumer' in src
print('service_auditor_agent: OK')
"
```

---

## Files Changed

- `production/grafana/provisioning/alerting/contact-points.example.yml` — committed example with placeholder values
- `production/grafana/provisioning/alerting/contact-points.yml` — gitignored local file with example structure
- `production/grafana/provisioning/alerting/alert-rules.yml` — 10 alert rules across CRITICAL/HIGH/MEDIUM groups
- `services/service_auditor_agent.py` — roll consumer loop, `_handle_roll_event`, `_restart_ibkr_provider`, `_handled_rolls` dedup set, `SERVICE_AUDITOR_SERVICE_RESTARTS_TOTAL` import
- `services/bar_auditor_agent.py` — `_upsert_market_data_gap`, `_resolve_market_data_gap`, `_publish_gap_fill_dlq`, `_seen_gap_requests` dedup, gap write path wired into `_detect_gaps`
- `tests/unit/test_grafana_provisioning.py` — YAML smoke tests for contact points + alert rules
- `tests/unit/service_tests/test_service_auditor_roll_consumer.py` — four roll consumer tests
- `tests/unit/service_tests/test_bar_auditor_gaps.py` — five gap write path tests
