---
phase: 110-renaissance-rename
plan: 02
type: execute
wave: 2
depends_on: ["110-01"]
requirements_addressed: [RENAME-02]
files_modified:
  - src/core/ai/multiplier_agent.py
  - src/core/ai/evaluator.py
  - src/core/ai/context.py
  - src/intelligence/ai/alpha/skeptic_agent.py
  - src/intelligence/ai/alpha/correlation_agent.py
  - src/intelligence/ai/alpha/counterfactual_agent.py
  - src/intelligence/ai/alpha/regime_coherence_agent.py
  - src/intelligence/ai/alpha/ml_scorer_agent.py
  - src/intelligence/ai/narrative/narrative_agent.py
autonomous: true

must_haves:
  truths:
    - "git grep finds zero references to the retired Ring 1 names (BaseMultiplierAgent, SkepticComputeAgent, CorrelationComputeAgent, CounterfactualComputeAgent, RegimeCoherenceComputeAgent, MLScorerMultiplierAgent, NarrativeComputeAgent, AIContext, AIContextCache) across src/, services/, tests/"
    - "The abstract Evaluator class lives at src/core/ai/evaluator.py (moved from multiplier_agent.py) and all consumers import it from the new location"
    - "All 5 alpha evaluators and the narrative synthesizer inherit from the correct renamed bases"
    - "pytest tests/unit/ passes with zero failures; ruff check . passes"
  artifacts:
    - path: "src/core/ai/evaluator.py"
      provides: "Evaluator abstract base (renamed from BaseMultiplierAgent, file moved)"
      contains: "class Evaluator(BaseAIWorker"
    - path: "src/intelligence/ai/alpha/skeptic_agent.py"
      provides: "SkepticEvaluator"
      contains: "class SkepticEvaluator(Evaluator)"
    - path: "src/intelligence/ai/alpha/correlation_agent.py"
      provides: "CorrelationAnalyzer"
      contains: "class CorrelationAnalyzer(Evaluator)"
    - path: "src/intelligence/ai/narrative/narrative_agent.py"
      provides: "NarrativeSynthesizer"
      contains: "class NarrativeSynthesizer(BaseAIWorker)"
    - path: "src/core/ai/context.py"
      provides: "SignalContext + SignalContextCache (renamed from AIContext/AIContextCache)"
      contains: "class SignalContext(BaseModel)"
  key_links:
    - from: "src/intelligence/ai/alpha/*.py evaluators"
      to: "src/core/ai/evaluator.py"
      via: "from src.core.ai.evaluator import Evaluator"
      pattern: "from src.core.ai.evaluator import Evaluator"
    - from: "all AIContext consumers (27 files)"
      to: "src/core/ai/context.py"
      via: "import SignalContext"
      pattern: "import SignalContext"
---

<objective>
Rename the Ring 1 AI evaluation layer to mathematical-object names per Section 9 of the naming spec, and move the abstract evaluator base into Ring 0. This is Wave 2 of the atomic rename. It depends on Wave 1 (Plan 01) because these classes inherit from the renamed `BaseAIWorker`/`BaseSwarmCoordinator`.

Class renames (identifier + all references across src/, services/, tests/):
- `BaseMultiplierAgent` -> `Evaluator` (abstract) AND file move `src/core/ai/multiplier_agent.py` -> `src/core/ai/evaluator.py`
- `SkepticComputeAgent` -> `SkepticEvaluator` (src/intelligence/ai/alpha/skeptic_agent.py)
- `CorrelationComputeAgent` -> `CorrelationAnalyzer` (src/intelligence/ai/alpha/correlation_agent.py)
- `CounterfactualComputeAgent` -> `CounterfactualEvaluator` (src/intelligence/ai/alpha/counterfactual_agent.py)
- `RegimeCoherenceComputeAgent` -> `RegimeCoherenceAnalyzer` (src/intelligence/ai/alpha/regime_coherence_agent.py)
- `MLScorerMultiplierAgent` -> `MLEvaluator` (src/intelligence/ai/alpha/ml_scorer_agent.py)
- `NarrativeComputeAgent` -> `NarrativeSynthesizer` (src/intelligence/ai/narrative/narrative_agent.py)
- `AIContext` -> `SignalContext` (src/core/ai/context.py; 27 importers)
- `AIContextCache` -> `SignalContextCache` (src/core/ai/context.py; 6 importers)

NOTE ON CONTEXT.md DISCREPANCIES (verified 2026-05-30):
- `BaseMultiplierAgent` is ALREADY in Ring 0 at `src/core/ai/multiplier_agent.py` (not Ring 1 `src/intelligence/ai/`). The CONTEXT.md "cross-ring file move from src/intelligence/ai/" framing is incorrect. The actual change is a same-ring file RENAME: `multiplier_agent.py` -> `evaluator.py` within `src/core/ai/`. The class already inherits from `BaseAIWorker` (renamed in Wave 1).
- `AIContext`/`AIContextCache` are ALREADY in Ring 0 at `src/core/ai/context.py` (the CONTEXT.md calls them "Ring 1 context carriers"). The rename of the identifiers is the same regardless of ring label; the file stays `context.py`.

CONSTRAINT: `agent_id`/`name=` exception still applies. Do NOT change `name="..."` string literals, `agent=` structlog values, `agent_id` labels, `agent_id` attrs, or the `agent_id`-keyed `_AGENT_ID_TO_UNIT`. Rename Python identifiers and imports only. File-name changes other than the `multiplier_agent.py -> evaluator.py` move are deferred to Wave 4.

NARRATIVE DISAMBIGUATION: `NarrativeComputeAgent` (the individual LLM worker, this plan -> `NarrativeSynthesizer`) is DISTINCT from `NarrativeGroupComputeAgent` (the Ring 2 daemon, renamed to `NarrativeSwarm` in Wave 3 / Plan 04). Do NOT touch `NarrativeGroupComputeAgent` in this plan — only the word-boundary-exact `NarrativeComputeAgent`.

Purpose: Establish correct evaluator vocabulary so Phase 095 writes new evaluators against the right names.
Output: 9 renamed Ring 1 classes + 1 file move; full codebase imports resolve; unit tests green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-renaissance-rename/110-CONTEXT.md
@docs/foundation/naming-system.md
@docs/plans/2026-05-30-renaissance-naming-system-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move BaseMultiplierAgent -> Evaluator (file move + rename)</name>
  <files>src/core/ai/evaluator.py (was multiplier_agent.py), src/core/ai/__init__.py</files>
  <read_first>
    - src/core/ai/multiplier_agent.py (line 19: `class BaseMultiplierAgent(BaseAIWorker, ABC)` after Wave 1)
    - src/core/ai/__init__.py (currently only a docstring — check whether it re-exports BaseMultiplierAgent)
    - One consumer for the import pattern, e.g. src/intelligence/ai/alpha/skeptic_agent.py (its `from ... import BaseMultiplierAgent` line)
  </read_first>
  <action>
    On branch `rename/phase-110` (already created in Plan 01; `git checkout rename/phase-110` if needed).

    1. Move the file preserving git history: `git mv src/core/ai/multiplier_agent.py src/core/ai/evaluator.py`
    2. In `src/core/ai/evaluator.py`, rename the class: `class BaseMultiplierAgent(BaseAIWorker, ABC):` -> `class Evaluator(BaseAIWorker, ABC):`. Update the module docstring and any internal self-references (`BaseMultiplierAgent.` -> `Evaluator.`).
    3. Update EVERY importer across `src/`, `services/`, `tests/`. There are ~10 code importers (the 5 alpha agents, alpha_swarm_agent, ml_scorer, plus tests). The import path changes from `src.core.ai.multiplier_agent` to `src.core.ai.evaluator` AND the symbol changes from `BaseMultiplierAgent` to `Evaluator`.

    Mechanical approach:
    ```
    # rename the symbol everywhere (code + tests)
    git grep -lw "BaseMultiplierAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' | xargs sed -i 's/\bBaseMultiplierAgent\b/Evaluator/g'
    # fix the module path in import statements
    git grep -ln "src.core.ai.multiplier_agent\|core\.ai\.multiplier_agent\|ai.multiplier_agent" -- '*.py' | xargs sed -i 's/core\.ai\.multiplier_agent/core.ai.evaluator/g'
    ```
    Then manually scan for any relative imports (`from ...core.ai.multiplier_agent import` or `from ..multiplier_agent import`) and fix the path to `evaluator`.

    If `src/core/ai/__init__.py` re-exports the symbol, update it to `from src.core.ai.evaluator import Evaluator`.
  </action>
  <verify>
    `git grep -nw "BaseMultiplierAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns zero.
    `git grep -n "multiplier_agent" -- '*.py'` returns zero (no stale module path).
    `test -f src/core/ai/evaluator.py && test ! -f src/core/ai/multiplier_agent.py` succeeds.
    `.venv/bin/python -c "from src.core.ai.evaluator import Evaluator"` exits 0.
  </verify>
  <acceptance_criteria>
    - `git grep -nw "BaseMultiplierAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns 0 results
    - `git grep -n "multiplier_agent" -- '*.py'` returns 0 results
    - `src/core/ai/evaluator.py` exists and contains `class Evaluator(BaseAIWorker, ABC):`
    - `src/core/ai/multiplier_agent.py` does NOT exist
    - `.venv/bin/python -c "from src.core.ai.evaluator import Evaluator"` exits 0
  </acceptance_criteria>
  <done>Evaluator abstract base lives at src/core/ai/evaluator.py; all importers updated; no stale module path.</done>
</task>

<task type="auto">
  <name>Task 2: Rename the 5 alpha evaluators + NarrativeSynthesizer + SignalContext/Cache</name>
  <files>src/intelligence/ai/alpha/skeptic_agent.py, src/intelligence/ai/alpha/correlation_agent.py, src/intelligence/ai/alpha/counterfactual_agent.py, src/intelligence/ai/alpha/regime_coherence_agent.py, src/intelligence/ai/alpha/ml_scorer_agent.py, src/intelligence/ai/narrative/narrative_agent.py, src/core/ai/context.py</files>
  <read_first>
    - src/intelligence/ai/alpha/skeptic_agent.py:35, correlation_agent.py:55, counterfactual_agent.py:56, regime_coherence_agent.py:55, ml_scorer_agent.py:74 (class defs — now inherit from `Evaluator` after Task 1)
    - src/intelligence/ai/narrative/narrative_agent.py:32 (`class NarrativeComputeAgent(BaseAIWorker)`)
    - services/narrative_group_compute_agent.py (imports NarrativeComputeAgent on line 30; references `NarrativeComputeAgent._NARRATIVE_TFS` — these references rename, but the NarrativeGroupComputeAgent class itself does NOT)
    - src/core/ai/context.py:101 (`class AIContext(BaseModel)`) and :206 (`class AIContextCache`)
    - services/alpha_swarm_agent.py (constructs the alpha agents; check `self._agents` list and any `agent_id` strings)
  </read_first>
  <action>
    Rename each evaluator class identifier across `src/`, `services/`, `tests/` (word-boundary safe):

    - `SkepticComputeAgent` -> `SkepticEvaluator`
    - `CorrelationComputeAgent` -> `CorrelationAnalyzer`
    - `CounterfactualComputeAgent` -> `CounterfactualEvaluator`
    - `RegimeCoherenceComputeAgent` -> `RegimeCoherenceAnalyzer`
    - `MLScorerMultiplierAgent` -> `MLEvaluator`
    - `NarrativeComputeAgent` -> `NarrativeSynthesizer` (word-boundary exact — must NOT match `NarrativeGroupComputeAgent`)
    - `AIContext` -> `SignalContext`
    - `AIContextCache` -> `SignalContextCache`

    Mechanical approach — run AIContextCache BEFORE AIContext so the longer token is consumed first, and use word boundaries throughout:
    ```
    git grep -lw "AIContextCache" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' | xargs sed -i 's/\bAIContextCache\b/SignalContextCache/g'
    git grep -lw "AIContext" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' | xargs sed -i 's/\bAIContext\b/SignalContext/g'
    git grep -lw "SkepticComputeAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' | xargs sed -i 's/\bSkepticComputeAgent\b/SkepticEvaluator/g'
    git grep -lw "CorrelationComputeAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' | xargs sed -i 's/\bCorrelationComputeAgent\b/CorrelationAnalyzer/g'
    git grep -lw "CounterfactualComputeAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' | xargs sed -i 's/\bCounterfactualComputeAgent\b/CounterfactualEvaluator/g'
    git grep -lw "RegimeCoherenceComputeAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' | xargs sed -i 's/\bRegimeCoherenceComputeAgent\b/RegimeCoherenceAnalyzer/g'
    git grep -lw "MLScorerMultiplierAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' | xargs sed -i 's/\bMLScorerMultiplierAgent\b/MLEvaluator/g'
    git grep -lw "NarrativeComputeAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' | xargs sed -i 's/\bNarrativeComputeAgent\b/NarrativeSynthesizer/g'
    ```

    Because `\b` does not treat `NarrativeGroupComputeAgent` as containing the standalone token `NarrativeComputeAgent` (the `Group` prefix means `NarrativeComputeAgent` is not a whole-word match inside it), the `NarrativeGroupComputeAgent` daemon class is left untouched. Verify this explicitly (see acceptance criteria).

    Do NOT rename file names (Wave 4). Do NOT change `name="..."` literals, `agent_id` labels, or `agent_id`-keyed dicts. The `agent_id`/`group`/`prompt_version` class attribute VALUES (which may be strings like "skeptic_v1") are runtime identity, not class identifiers — leave them unchanged unless they literally equal an old CLASS name being renamed (they do not).
  </action>
  <verify>
    `git grep -nw "SkepticComputeAgent\|CorrelationComputeAgent\|CounterfactualComputeAgent\|RegimeCoherenceComputeAgent\|MLScorerMultiplierAgent\|NarrativeComputeAgent\|AIContext\|AIContextCache" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns zero.
    `git grep -nw "NarrativeGroupComputeAgent" -- 'src/**/*.py' 'services/**/*.py'` STILL returns results (proving the group daemon was untouched).
  </verify>
  <acceptance_criteria>
    - `git grep -nw "SkepticComputeAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns 0 results
    - `git grep -nw "AIContext" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns 0 results
    - `git grep -nw "AIContextCache" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns 0 results
    - `git grep -nw "NarrativeComputeAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns 0 results
    - `git grep -nw "NarrativeGroupComputeAgent" -- 'src/**/*.py' 'services/**/*.py'` returns NON-zero results (group daemon preserved for Wave 3)
    - `src/intelligence/ai/alpha/skeptic_agent.py` contains `class SkepticEvaluator(Evaluator):`
    - `src/intelligence/ai/alpha/correlation_agent.py` contains `class CorrelationAnalyzer(Evaluator):`
    - `src/intelligence/ai/narrative/narrative_agent.py` contains `class NarrativeSynthesizer(BaseAIWorker):`
    - `src/core/ai/context.py` contains `class SignalContext(BaseModel):` and `class SignalContextCache`
  </acceptance_criteria>
  <done>All 9 Ring 1 identifiers renamed; NarrativeGroupComputeAgent preserved; imports resolve.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 2 CI gate — lint, type-check, unit tests, commit</name>
  <files>(no source files — runs tests/lint and commits)</files>
  <read_first>
    - CLAUDE.md (Core Commands section)
  </read_first>
  <action>
    Run the full Wave 2 CI gate (must be green before Wave 3 / Plan 03 starts):
    1. `.venv/bin/ruff check . --fix` then `.venv/bin/ruff check .`
    2. `.venv/bin/black .`
    3. `.venv/bin/mypy src/ --ignore-missing-imports` (fix rename-introduced errors)
    4. `.venv/bin/pytest tests/unit/ -q`

    Fix any test referencing an old Ring 1 identifier the grep missed (string-literal patch targets, monkeypatch paths). Remember `name=`/`agent_id` string literals are NOT class names.

    Commit: `git add -A && git commit -m "refactor(110): rename Ring 1 AI evaluation layer (Wave 2)" -m "BaseMultiplierAgent->Evaluator (moved to src/core/ai/evaluator.py), Skeptic/Correlation/Counterfactual/RegimeCoherence/MLScorer/Narrative renames, AIContext->SignalContext" -m "Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"`
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/ -q` exits 0. `.venv/bin/ruff check .` exits 0. `git log --oneline -1` shows Wave 2 commit.
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/ -q` exits 0
    - `.venv/bin/ruff check .` exits 0
    - `git grep -nw "BaseMultiplierAgent\|SkepticComputeAgent\|CorrelationComputeAgent\|CounterfactualComputeAgent\|RegimeCoherenceComputeAgent\|MLScorerMultiplierAgent\|NarrativeComputeAgent\|AIContext\|AIContextCache" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns 0 results
    - `git log --oneline -1` contains "rename Ring 1 AI evaluation"
  </acceptance_criteria>
  <done>Wave 2 CI gate green; committed; safe to proceed to Wave 3.</done>
</task>

</tasks>

<verification>
- 9 Ring 1 identifiers renamed; Evaluator moved to evaluator.py; NarrativeGroupComputeAgent preserved.
- agent_id/name= exceptions honored.
- pytest tests/unit/ green; ruff clean; committed on rename/phase-110.
</verification>

<success_criteria>
- `git grep -nw "BaseMultiplierAgent\|SkepticComputeAgent\|CorrelationComputeAgent\|CounterfactualComputeAgent\|RegimeCoherenceComputeAgent\|MLScorerMultiplierAgent\|NarrativeComputeAgent\|AIContext\|AIContextCache" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns zero results
- Evaluator importable from src.core.ai.evaluator; all 6 evaluators/synthesizer defined at new names
- pytest + ruff pass
</success_criteria>

<output>
After completion, create `.planning/phases/110-renaissance-rename/110-02-SUMMARY.md`
</output>
