---
phase: 110-renaissance-rename
plan: 01
type: execute
wave: 1
depends_on: []
requirements_addressed: [RENAME-01]
files_modified:
  - src/core/agent/base.py
  - src/core/agent/base_writer.py
  - src/core/agent/__init__.py
  - src/providers/base_provider_agent.py
  - src/providers/base.py
  - src/core/ai/base_agent.py
  - src/core/ai/base_group_service.py
autonomous: true

must_haves:
  truths:
    - "git grep finds zero references to the 5 retired Ring 0 base class names (BaseAgent, BaseWriterAgent, BaseProviderAgent, BaseAIAgent, BaseGroupService) anywhere in src/, services/, tests/"
    - "All subclasses across src/, services/, tests/ inherit from the renamed Ring 0 bases and import them at the new names"
    - "pytest tests/unit/ passes with zero failures after the rename"
    - "ruff check . passes"
  artifacts:
    - path: "src/core/agent/base.py"
      provides: "BaseDaemon base class (renamed from BaseAgent)"
      contains: "class BaseDaemon"
    - path: "src/core/agent/base_writer.py"
      provides: "BaseWriter base class (renamed from BaseWriterAgent)"
      contains: "class BaseWriter(BaseDaemon"
    - path: "src/providers/base_provider_agent.py"
      provides: "BaseProvider base class (renamed from BaseProviderAgent)"
      contains: "class BaseProvider(BaseDaemon"
    - path: "src/core/ai/base_agent.py"
      provides: "BaseAIWorker base class (renamed from BaseAIAgent)"
      contains: "class BaseAIWorker(BaseDaemon"
    - path: "src/core/ai/base_group_service.py"
      provides: "BaseSwarmCoordinator base class (renamed from BaseGroupService)"
      contains: "class BaseSwarmCoordinator(BaseDaemon"
  key_links:
    - from: "src/core/agent/__init__.py"
      to: "src/core/agent/base.py"
      via: "from src.core.agent.base import BaseDaemon"
      pattern: "import BaseDaemon"
    - from: "all subclasses (BaseWriter, BaseProvider, BaseAIWorker, BaseSwarmCoordinator)"
      to: "src/core/agent/base.py"
      via: "inherit from BaseDaemon"
      pattern: "class .*\\(BaseDaemon"
---

<objective>
Rename the 5 Ring 0 infrastructure base classes from their `*Agent`/`*Service` names to the role-noun names defined in Section 9 of the naming spec. This is Wave 1 of the Phase 110 atomic rename — the highest-leverage change, because every daemon in the codebase inherits from these classes.

Renames (class name + all references across src/, services/, tests/):
- `BaseAgent` -> `BaseDaemon` (`src/core/agent/base.py`)
- `BaseWriterAgent` -> `BaseWriter` (`src/core/agent/base_writer.py`)
- `BaseProviderAgent` -> `BaseProvider` (`src/providers/base_provider_agent.py`)
- `BaseAIAgent` -> `BaseAIWorker` (`src/core/ai/base_agent.py`)
- `BaseGroupService` -> `BaseSwarmCoordinator` (`src/core/ai/base_group_service.py`)

NOTE ON CONTEXT.md DISCREPANCIES (verified against the live codebase 2026-05-30):
- `BaseProviderAgent` lives at `src/providers/base_provider_agent.py`, NOT `src/core/agent/base_provider.py`.
- `BaseAIAgent` lives at `src/core/ai/base_agent.py`, NOT `src/core/ai/base_ai_agent.py`.
- `AgentContext` (-> `WorkerContext`) and `AgentProtocol` (-> `AIWorkerProtocol`) do NOT exist in the codebase yet — they are Phase 095 deliverables. They are explicitly OUT OF SCOPE for this plan because there is nothing to rename. Phase 095 will create them at the correct names directly. Do not create stub files for them.

CRITICAL CONSTRAINT — `agent_id` operational exception:
The `name=` string argument passed to `BaseDaemon.__init__(name=...)` by each subclass becomes the `agent_id` metric label and structlog field. These string values MUST NOT be changed in this plan. You are renaming Python class identifiers and their imports ONLY — never the `name="..."` string literals, the `agent=`/`agent_id=` OTel label values, or `_AGENT_ID_TO_UNIT` dict keys.

This is a file-internal-only rename: file names stay the same in Wave 1 (file renames are Wave 4). Only the `class` keyword identifiers, type annotations, isinstance checks, and import statements change.

Purpose: Establish the correct Ring 0 vocabulary before any Ring 1/2 code inherits from it, so the rest of the rename builds on a correct foundation.
Output: 5 renamed base classes; full codebase compiles, imports resolve, unit tests green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-renaissance-rename/110-CONTEXT.md
@docs/foundation/naming-system.md
@docs/plans/2026-05-30-renaissance-naming-system-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rename/phase-110 branch and rename BaseAgent -> BaseDaemon</name>
  <files>src/core/agent/base.py, src/core/agent/__init__.py</files>
  <read_first>
    - src/core/agent/base.py (the class definition — line 89: `class BaseAgent(abc.ABC, ConfigConsumerMixin)`; note line 130 `getattr(BaseAgent, "_log_configured_path", ...)` and line 132 `BaseAgent._log_configured_path = ...` self-references)
    - src/core/agent/__init__.py (re-exports BaseAgent on lines 3 and 7)
    - docs/plans/2026-05-30-renaissance-naming-system-design.md (Section 9, Ring 0 table)
  </read_first>
  <action>
    First create the atomic rename branch (all 4 waves of Phase 110 share it):
    `git checkout -b rename/phase-110` (if it already exists, `git checkout rename/phase-110`).

    Rename `BaseAgent` -> `BaseDaemon` everywhere it appears as a Python identifier across `src/`, `services/`, and `tests/`. This is the most-referenced base (27 files). Use a word-boundary-safe replacement so you do not touch the `agent_id` label, the `agent=` structlog bind value, `_AGENT_ID_TO_UNIT`, or `_log_configured_path`.

    Recommended mechanical approach:
    `git grep -lw "BaseAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' | xargs sed -i 's/\bBaseAgent\b/BaseDaemon/g'`

    This correctly renames:
    - The class definition `class BaseDaemon(abc.ABC, ConfigConsumerMixin):` in src/core/agent/base.py
    - The internal self-references `getattr(BaseDaemon, "_log_configured_path", ...)` and `BaseDaemon._log_configured_path = ...`
    - All `from src.core.agent.base import BaseAgent` -> `... import BaseDaemon`
    - All `from src.core.agent import BaseAgent` and `class X(BaseAgent)` subclass declarations
    - The `__all__` entry and docstring mentions in src/core/agent/__init__.py

    Do NOT change: the file name (base.py stays), `name="..."` literals, `agent_id`, `agent=name`, or comments that intentionally describe the `agent_id` exception.

    After replacement, verify the OTel label code in base.py around line 139-160 still reads `{"agent_id": name}` and `{"agent": self._agent_label}` — these are string label keys and must be untouched.
  </action>
  <verify>
    `cd /home/bg/dev/indicagent && git grep -nw "BaseAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns zero results.
    `git grep -n "class BaseDaemon" src/core/agent/base.py` returns one result.
    `git grep -nw "agent_id" src/core/agent/base.py` still returns the label-key lines (proving labels untouched).
    `.venv/bin/python -c "from src.core.agent.base import BaseDaemon"` exits 0.
  </verify>
  <acceptance_criteria>
    - `git grep -nw "BaseAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns 0 results
    - `src/core/agent/base.py` contains `class BaseDaemon(abc.ABC, ConfigConsumerMixin):`
    - `src/core/agent/__init__.py` contains `from src.core.agent.base import BaseDaemon` and `BaseDaemon` in `__all__`
    - `git grep -n '"agent_id": name' src/core/agent/base.py` still returns the OTel label line (label key preserved)
    - `git branch --show-current` outputs `rename/phase-110`
  </acceptance_criteria>
  <done>BaseAgent is fully renamed to BaseDaemon; zero old references; agent_id labels preserved; imports resolve.</done>
</task>

<task type="auto">
  <name>Task 2: Rename BaseWriterAgent, BaseProviderAgent, BaseAIAgent, BaseGroupService</name>
  <files>src/core/agent/base_writer.py, src/providers/base_provider_agent.py, src/core/ai/base_agent.py, src/core/ai/base_group_service.py, src/core/agent/__init__.py</files>
  <read_first>
    - src/core/agent/base_writer.py (line 65: `class BaseWriterAgent(BaseAgent, abc.ABC)` — now reads `(BaseDaemon, abc.ABC)` after Task 1)
    - src/providers/base_provider_agent.py (line 44: `class BaseProviderAgent(BaseAgent)` — now `(BaseDaemon)`)
    - src/core/ai/base_agent.py (line 54: `class BaseAIAgent(BaseAgent, ABC)` — now `(BaseDaemon, ABC)`)
    - src/core/ai/base_group_service.py (line 29: `class BaseGroupService(BaseAgent, ABC)` — now `(BaseDaemon, ABC)`)
    - src/core/agent/__init__.py (re-exports BaseWriterAgent on lines 4 and 7)
  </read_first>
  <action>
    Rename the remaining 4 Ring 0 bases as Python identifiers across `src/`, `services/`, and `tests/`. Apply each substitution with word boundaries:

    - `BaseWriterAgent` -> `BaseWriter` (defined src/core/agent/base_writer.py:65; 11 files reference it; update `__init__.py` re-export and `__all__`)
    - `BaseProviderAgent` -> `BaseProvider` (defined src/providers/base_provider_agent.py:44; 5 files; note `src/providers/base.py` and `src/providers/ibkr_adapter.py` docstrings/comments mention it)
    - `BaseAIAgent` -> `BaseAIWorker` (defined src/core/ai/base_agent.py:54; 9 files reference it)
    - `BaseGroupService` -> `BaseSwarmCoordinator` (defined src/core/ai/base_group_service.py:29; 5 files reference it)

    Mechanical approach (run each separately):
    ```
    git grep -lw "BaseWriterAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' | xargs sed -i 's/\bBaseWriterAgent\b/BaseWriter/g'
    git grep -lw "BaseProviderAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' | xargs sed -i 's/\bBaseProviderAgent\b/BaseProvider/g'
    git grep -lw "BaseAIAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' | xargs sed -i 's/\bBaseAIAgent\b/BaseAIWorker/g'
    git grep -lw "BaseGroupService" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' | xargs sed -i 's/\bBaseGroupService\b/BaseSwarmCoordinator/g'
    ```

    Then update `src/core/agent/__init__.py`: the module docstring on line 1 mentions "BaseAgent, BaseWriterAgent" — change to "BaseDaemon, BaseWriter". Confirm `__all__` reads `["BaseDaemon", "BaseWriter", "ProcessManifest"]`.

    Do NOT rename file names (Wave 4) and do NOT touch `name=` string literals or `agent_id` labels.

    NOTE: `BaseMultiplierAgent` (defined src/core/ai/multiplier_agent.py) inherits from `BaseAIAgent` — after this task its definition will read `class BaseMultiplierAgent(BaseAIWorker, ABC)`. That is correct; `BaseMultiplierAgent` itself is renamed in Wave 2 (Plan 02), not here.
  </action>
  <verify>
    `git grep -nw "BaseWriterAgent\|BaseProviderAgent\|BaseAIAgent\|BaseGroupService" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns zero results.
    `.venv/bin/python -c "from src.core.agent.base_writer import BaseWriter; from src.providers.base_provider_agent import BaseProvider; from src.core.ai.base_agent import BaseAIWorker; from src.core.ai.base_group_service import BaseSwarmCoordinator"` exits 0.
  </verify>
  <acceptance_criteria>
    - `git grep -nw "BaseWriterAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns 0 results
    - `git grep -nw "BaseProviderAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns 0 results
    - `git grep -nw "BaseAIAgent" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns 0 results
    - `git grep -nw "BaseGroupService" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns 0 results
    - `src/core/agent/base_writer.py` contains `class BaseWriter(BaseDaemon, abc.ABC):`
    - `src/providers/base_provider_agent.py` contains `class BaseProvider(BaseDaemon):`
    - `src/core/ai/base_agent.py` contains `class BaseAIWorker(BaseDaemon, ABC):`
    - `src/core/ai/base_group_service.py` contains `class BaseSwarmCoordinator(BaseDaemon, ABC):`
    - `src/core/agent/__init__.py` `__all__` equals `["BaseDaemon", "BaseWriter", "ProcessManifest"]`
  </acceptance_criteria>
  <done>All 5 Ring 0 base classes renamed; subclasses inherit from new names; imports resolve.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 1 CI gate — lint, type-check, unit tests</name>
  <files>(no source files — runs tests/lint and commits)</files>
  <read_first>
    - CLAUDE.md (Core Commands section — exact test/lint/format invocations)
  </read_first>
  <action>
    Run the full Wave 1 CI gate. This MUST be green before Wave 2 (Plan 02) is allowed to start — it prevents compound breakage across waves.

    1. `cd /home/bg/dev/indicagent && .venv/bin/ruff check . --fix` then `.venv/bin/ruff check .`
    2. `.venv/bin/black .`
    3. `.venv/bin/mypy src/ --ignore-missing-imports` (advisory — record any failures that pre-existed vs introduced by rename; rename-introduced errors must be fixed)
    4. `.venv/bin/pytest tests/unit/ -q`
    5. Extended-scope grep sweep — confirm the 5 retired base names are absent from root-level scripts and oneshot scripts too (not just src/services/tests):
       `git grep -nw "BaseAgent\|BaseWriterAgent\|BaseProviderAgent\|BaseAIAgent\|BaseGroupService" -- '*.py' 'production/scripts/*.py'` returns 0 results (covers historical_backfill.py, lifecycle_replay.py at repo root and any oneshot scripts). Fix any hit before committing.

    If any unit test fails because it references an old base name that the grep missed (e.g. a string literal in an assertion, a monkeypatch target, or a dynamically built import path), fix that reference. Note that `name="SignalTrackerComputeAgent"` style string literals are NOT old base-class names and must stay — only fix references to the 5 renamed base CLASS identifiers.

    Commit the wave: `git add -A && git commit -m "refactor(110): rename Ring 0 base classes to role nouns (Wave 1)" -m "BaseAgent->BaseDaemon, BaseWriterAgent->BaseWriter, BaseProviderAgent->BaseProvider, BaseAIAgent->BaseAIWorker, BaseGroupService->BaseSwarmCoordinator" -m "Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"`
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/ -q` exits 0.
    `.venv/bin/ruff check .` exits 0.
    `git grep -nw "BaseAgent\|BaseWriterAgent\|BaseProviderAgent\|BaseAIAgent\|BaseGroupService" -- '*.py' 'production/scripts/*.py'` returns zero results.
    `git log --oneline -1` shows the Wave 1 commit.
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/ -q` exits 0 (zero failures)
    - `.venv/bin/ruff check .` exits 0
    - `git grep -nw "BaseAgent\|BaseWriterAgent\|BaseProviderAgent\|BaseAIAgent\|BaseGroupService" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns 0 results
    - `git grep -nw "BaseAgent\|BaseWriterAgent\|BaseProviderAgent\|BaseAIAgent\|BaseGroupService" -- '*.py' 'production/scripts/*.py'` returns 0 results (root-level scripts + oneshot scripts covered)
    - `git log --oneline -1` contains "rename Ring 0 base classes"
  </acceptance_criteria>
  <done>Wave 1 CI gate green; rename committed on rename/phase-110; safe to proceed to Wave 2.</done>
</task>

</tasks>

<verification>
- All 5 Ring 0 base classes renamed; zero old identifiers remain in code/tests.
- `agent_id` labels, `name=` literals, `_AGENT_ID_TO_UNIT` keys untouched.
- pytest tests/unit/ green; ruff clean.
- Work committed on branch rename/phase-110.
</verification>

<success_criteria>
- `git grep -nw "BaseAgent\|BaseWriterAgent\|BaseProviderAgent\|BaseAIAgent\|BaseGroupService" -- 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py'` returns zero results
- The 5 renamed classes are defined and importable at their new names
- `.venv/bin/pytest tests/unit/ -q` and `.venv/bin/ruff check .` both pass
</success_criteria>

<output>
After completion, create `.planning/phases/110-renaissance-rename/110-01-SUMMARY.md`
</output>
