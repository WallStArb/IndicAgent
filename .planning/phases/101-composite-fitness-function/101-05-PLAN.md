---
phase: 101-composite-fitness-function
plan: 05
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - src/intelligence/ai/fitness/gates.py
  - tests/unit/intelligence/test_fitness_gates.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "PromotionGate.can_promote returns (True, None) only when ALL D-14 criteria pass"
    - "PromotionGate blocks with a named reason string when any single criterion fails"
    - "DemotionGate.should_demote returns (True, trigger_name) when ANY D-15 trigger fires"
    - "Both gates are pure stateless classes: no DB access, no Settings import — thresholds passed in"
    - "history is documented as: PRIOR rows only (NOT including the current fitness_row), ordered most-recent-first; gates that need 3 rows use AT LEAST 3 and read the latest 3 (never fail when more history exists)"
  artifacts:
    - path: "src/intelligence/ai/fitness/gates.py"
      provides: "PromotionGate + DemotionGate pure classes (D-08, D-14, D-15)"
      contains: "class PromotionGate"
    - path: "tests/unit/intelligence/test_fitness_gates.py"
      provides: "criterion-by-criterion gate tests (each pass/fail path)"
      contains: "def test_"
  key_links:
    - from: "shadow_auditor.py (plan 06)"
      to: "PromotionGate.can_promote / DemotionGate.should_demote"
      via: "fitness-row-based gate calls replacing inline _should_promote/_should_demote"
      pattern: "PromotionGate|DemotionGate"
---

<objective>
Build `PromotionGate` and `DemotionGate` as pure stateless classes in `src/intelligence/ai/fitness/gates.py` (D-08). They accept a latest fitness row dict plus a short history of prior rows and return `(bool, str | None)` — decision plus a reason naming the failing criterion / firing trigger. No DB access, no Settings import; all thresholds are constructor arguments. These replace the inline `_should_promote` / `_should_demote` functions that plan 06 removes from `shadow_auditor.py`.

Purpose: Isolating gate logic into pure classes makes the governance decision fully unit-testable without a database (the explicit motivation in D-08). Each of the 5 PromotionGate criteria (D-14) and 4 DemotionGate triggers (D-15) gets its own pass/fail test.
Output: gates.py and its test file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-composite-fitness-function/101-CONTEXT.md
@.planning/phases/101-composite-fitness-function/101-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: TDD PromotionGate (D-14, ALL criteria must pass)</name>
  <files>src/intelligence/ai/fitness/gates.py, tests/unit/intelligence/test_fitness_gates.py</files>
  <read_first>
    - services/shadow_auditor.py (current `_should_promote` / `_tail_risk_blocks_promotion` pure functions, lines 50-78 — the str|None reason pattern to generalize)
    - .planning/phases/101-composite-fitness-function/101-CONTEXT.md (D-14 the 5 promotion criteria + D-06 thresholds)
    - .planning/phases/101-composite-fitness-function/101-PATTERNS.md (gate class signature, lines 180-214; gate test pattern, lines 453-481)
  </read_first>
  <action>
    RED: In `tests/unit/intelligence/test_fitness_gates.py` write failing tests for `PromotionGate(min_n: int, novelty_threshold: float, stability_threshold: float)`. `can_promote(fitness_row: dict, history: list[dict]) -> tuple[bool, str | None]`. A `fitness_row` dict has keys: `composite_score`, `novelty_score`, `regime_score`, `n_resolved`, and per-regime composites under `dimensions_jsonb["per_regime_composite"]` (dict regime->float). `history` is the agent's PRIOR fitness rows (most recent first), each with `composite_score`, and explicitly does NOT include the current `fitness_row`; callers may pass more than 3 — the gate uses AT LEAST 3 and reads the latest 3 (review fix: never fail because an agent has accumulated more than 3 rows). Tests — one per criterion, plus the all-pass case:
    - all criteria satisfied -> (True, None)
    - composite_score is None -> (False, "composite_null")
    - any per-regime composite <= 0.05 -> (False, "regime_coverage")
    - n_resolved < min_n -> (False, "insufficient_n")
    - stddev over the latest 3 history composites >= stability_threshold (0.02) -> (False, "unstable")
    - fewer than 3 history rows -> (False, "insufficient_stability_history")
    - MORE than 3 history rows but the latest 3 are stable -> still passes (does NOT fail for having extra history)
    - novelty_score <= novelty_threshold (0.15) -> (False, "low_novelty")
    GREEN: Implement `PromotionGate` in `src/intelligence/ai/fitness/gates.py`. Constructor stores the three thresholds. `can_promote` checks D-14 criteria in order and returns the first failure as `(False, "<reason>")`, else `(True, None)`. Criteria: (1) composite not None, (2) every value in `dimensions_jsonb["per_regime_composite"]` > 0.05, (3) `n_resolved >= min_n`, (4) `len(history) >= 3` AND `statistics.stdev(c for c in the latest 3 history composites) < stability_threshold` — slice `history[:3]` since history is most-recent-first, so extra history rows never cause a failure (review fix: "at least 3, use latest 3", not "exactly 3"), (5) `novelty_score > novelty_threshold`. Pure class — no DB, no Settings import, `import statistics` only.
    REFACTOR: ensure each criterion returns a distinct, stable reason string used by tests.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/test_fitness_gates.py -k promotion -q` exits 0.
  </verify>
  <acceptance_criteria>
    - src/intelligence/ai/fitness/gates.py contains `class PromotionGate`
    - gates.py contains NO `import asyncpg`, NO `from src.config`
    - all-pass fitness row returns `(True, None)`
    - each of the 5 criteria has a test asserting `(False, "<specific_reason>")` and all pass
    - an agent with 5 stable history rows still returns `(True, None)` (extra history does not block — "at least 3, use latest 3")
    - reason strings include: composite_null, regime_coverage, insufficient_n, unstable, low_novelty
    - test file `-k promotion` selects >= 6 tests and all pass
  </acceptance_criteria>
  <done>PromotionGate.can_promote enforces all 5 D-14 criteria as a pure class, returning the first failing criterion's name as reason.</done>
</task>

<task type="tdd">
  <name>Task 2: TDD DemotionGate (D-15, ANY trigger fires)</name>
  <files>src/intelligence/ai/fitness/gates.py, tests/unit/intelligence/test_fitness_gates.py</files>
  <read_first>
    - src/intelligence/ai/fitness/gates.py (PromotionGate from Task 1 — share module conventions)
    - .planning/phases/101-composite-fitness-function/101-CONTEXT.md (D-15 the 4 demotion triggers + promotion_baseline*0.80, novelty<0.15 x2 cycles, regime_score=0 x2 cycles, parse rate<0.80)
  </read_first>
  <action>
    RED: Add failing tests to the same test file for `DemotionGate(decay_fraction: float, novelty_floor: float, parse_rate_floor: float)`. `should_demote(fitness_row: dict, history: list[dict], promotion_baseline: float | None, parse_success_rate: float | None) -> tuple[bool, str | None]`. Tests — one per trigger plus the no-trigger case:
    - no trigger fires -> (False, None)
    - composite_score < promotion_baseline * decay_fraction (0.80) -> (True, "fitness_decay")
    - novelty_score < novelty_floor (0.15) in the current row AND in history[0] (the most-recent prior row) -> (True, "correlation_rise")
    - novelty below floor in current row only but history is empty -> does NOT fire (need 2 consecutive cycles)
    - regime_score == 0.0 in current row AND in history[0] -> (True, "regime_shift_failure")
    - parse_success_rate < parse_rate_floor (0.80) -> (True, "parse_failure")
    - promotion_baseline is None -> trigger 1 is a no-op (does not fire) — covers first-deploy case (RESEARCH pitfall 7)
    GREEN: Implement `DemotionGate` in `gates.py`. `should_demote` evaluates the 4 triggers; ANY firing returns `(True, "<trigger_name>")`, none returns `(False, None)`. Trigger 1: only if `promotion_baseline is not None` and `composite_score is not None` and `composite_score < promotion_baseline * decay_fraction`. Trigger 2 ("2 consecutive cycles" = current row AND the single most-recent PRIOR history row, i.e. `history[0]`): fires only when `fitness_row["novelty_score"] < novelty_floor` AND `history` is non-empty AND `history[0]["novelty_score"] < novelty_floor`; if `history` is empty the trigger cannot fire (need 2 cycles). Trigger 3 (same 2-consecutive semantics): fires only when `fitness_row["regime_score"] == 0.0` AND `history` non-empty AND `history[0]["regime_score"] == 0.0`. Trigger 4: `parse_success_rate is not None and parse_success_rate < parse_rate_floor`. Pure class — no DB, no Settings import.
    REFACTOR: ensure trigger evaluation order is deterministic and each trigger name is a stable string.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/test_fitness_gates.py -k demotion -q` exits 0.
    `.venv/bin/pytest tests/unit/intelligence/test_fitness_gates.py -q` exits 0.
  </verify>
  <acceptance_criteria>
    - gates.py contains `class DemotionGate`
    - gates.py contains NO `import asyncpg`, NO `from src.config`
    - no-trigger row returns `(False, None)`
    - each of the 4 triggers has a test asserting `(True, "<trigger_name>")` and all pass
    - trigger names include: fitness_decay, correlation_rise, regime_shift_failure, parse_failure
    - promotion_baseline=None does not fire fitness_decay
    - test file `-k demotion` selects >= 5 tests and all pass
  </acceptance_criteria>
  <done>DemotionGate.should_demote fires on any of the 4 D-15 triggers as a pure class, with promotion_baseline=None handled as a no-op for trigger 1.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/intelligence/test_fitness_gates.py -q` exits 0.
- `.venv/bin/ruff check src/intelligence/ai/fitness/gates.py` exits 0.
- gates.py imports neither asyncpg nor Settings (pure classes).
</verification>

<success_criteria>
PromotionGate (ALL of D-14) and DemotionGate (ANY of D-15) are pure, DB-free, fully unit-tested classes ready for plan 06 to wire into shadow_auditor.py, replacing the inline gate functions.
</success_criteria>

<output>
After completion, create `.planning/phases/101-composite-fitness-function/101-05-SUMMARY.md`.

In the SUMMARY, document the gate-threshold tuning range (cross-AI review concern: the combination of per-agent stability `stddev < 0.02` and population diversity `stddev >= 0.2` is tight and may freeze the governance loop at small populations or during regime shifts). Record the current defaults, note that at < 10 live agents these are expected to be hard to satisfy, and flag `stability_threshold` (0.02) and `novelty_floor` (0.15) as the first knobs to relax if promotions stall — so the post-integration tuning phase has a documented starting point. These are pure constructor arguments, so retuning requires no code change to the gate classes.
</output>
