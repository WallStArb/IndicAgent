---
phase: 101-composite-fitness-function
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - services/fitness_auditor.py
  - services/shadow_auditor.py
  - services/service_auditor.py
  - production/systemd/indicagent-fitness-auditor.service
  - production/systemd/indicagent-fitness-auditor.timer
  - tests/unit/services/test_fitness_auditor.py
autonomous: true
requirements: [FIT-06]

must_haves:
  truths:
    - "fitness_auditor reads signal_ledger_full + llm_calls, computes all 5 dimensions + composite per agent, runs the novelty population pass, and writes one agent_fitness row per agent"
    - "fitness_auditor computes the FIT-06 variance gate: stddev(composite) across latest row per live agent, with insufficient_population below 5 agents"
    - "fitness_auditor emits job_completed_total{job=fitness-auditor} on exit"
    - "shadow_auditor skips agents whose latest agent_fitness row is older than FITNESS_STALENESS_THRESHOLD_HOURS"
    - "shadow_auditor uses PromotionGate/DemotionGate against agent_fitness; the swarm_agent early-continue is replaced by the fitness path"
    - "fitness-auditor is registered in the service DAG (_DAG_ORDER + _AGENT_ID_TO_UNIT) with a 60-minute timer"
  artifacts:
    - path: "services/fitness_auditor.py"
      provides: "oneshot: 5-dimension fitness computation + novelty pass + FIT-06 variance gate + agent_fitness writes"
      contains: "FITNESS_POPULATION_STDDEV"
    - path: "services/shadow_auditor.py"
      provides: "staleness check + PromotionGate/DemotionGate wiring against agent_fitness"
      contains: "PromotionGate"
    - path: "production/systemd/indicagent-fitness-auditor.timer"
      provides: "60-minute timer unit"
      contains: "OnCalendar"
    - path: "tests/unit/services/test_fitness_auditor.py"
      provides: "variance gate + dimension assembly + write-row tests"
      contains: "def test_"
  key_links:
    - from: "services/fitness_auditor.py"
      to: "agent_fitness"
      via: "INSERT after computing all dimensions"
      pattern: "INSERT INTO agent_fitness"
    - from: "services/fitness_auditor.py"
      to: "src.intelligence.ai.fitness (accuracy/calibration/regime/efficiency/novelty/composite)"
      via: "calculator imports"
      pattern: "from src.intelligence.ai.fitness"
    - from: "services/shadow_auditor.py"
      to: "agent_fitness"
      via: "DISTINCT ON latest fitness row per agent + staleness check"
      pattern: "agent_fitness"
    - from: "services/shadow_auditor.py"
      to: "src.intelligence.ai.fitness.gates"
      via: "PromotionGate/DemotionGate replace inline _should_promote/_should_demote"
      pattern: "from src.intelligence.ai.fitness.gates import"
---

<objective>
Wire the fitness system together. Build `services/fitness_auditor.py` (a oneshot cloned from `shadow_auditor.py`) that reads `signal_ledger_full + llm_calls`, computes all 5 dimensions + composite per agent, runs the population-level novelty pass, computes the FIT-06 discriminative-power variance gate, and writes one `agent_fitness` row per agent. Then refactor `shadow_auditor.py` to read the latest `agent_fitness` row per agent, apply a staleness check, and route promotion/demotion through `PromotionGate`/`DemotionGate` instead of the inline 1D logic. Register the new oneshot in the service DAG and create its systemd units.

Purpose: This is the integration capstone (Wave 3). It realizes the clean DAG (analytics: fitness_auditor → lifecycle: shadow_auditor, no reverse coupling, D-07) and delivers FIT-06 — the discriminative-power gate that blocks Phases 102-103 until the fitness function provably separates agents.
Output: fitness_auditor.py, refactored shadow_auditor.py, service_auditor.py registry edits, systemd unit + timer, fitness_auditor unit tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-composite-fitness-function/101-CONTEXT.md
@.planning/phases/101-composite-fitness-function/101-RESEARCH.md
@.planning/phases/101-composite-fitness-function/101-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build fitness_auditor.py oneshot — dimensions + novelty pass + FIT-06 variance gate + agent_fitness writes</name>
  <files>services/fitness_auditor.py, tests/unit/services/test_fitness_auditor.py</files>
  <read_first>
    - services/shadow_auditor.py (full oneshot structure: imports, _run_audit loop, _check_promotion DB queries, _amain/main with JOB_COMPLETED_TOTAL, lines 1-358 — this is the exact clone template)
    - .planning/phases/101-composite-fitness-function/101-RESEARCH.md (query for resolved signals lines 271-285; llm_calls query lines 289-304; agent_fitness write lines 308-323; FIT-06 variance gate SQL lines 327-340; pitfalls 1-7 lines 216-264)
    - .planning/phases/101-composite-fitness-function/101-PATTERNS.md (fitness_auditor analog mapping, lines 36-130)
    - src/intelligence/ai/fitness/composite.py and novelty.py and accuracy.py (calculator signatures created in plans 02-04)
    - src/config/settings.py (FITNESS_* fields added in plan 01)
    - tests/unit/services/test_shadow_auditor.py (mock pool / AsyncMock conn test pattern)
  </read_first>
  <action>
    Create `services/fitness_auditor.py` cloned from `shadow_auditor.py`. Structure:
    - Imports: `from src.config.settings import Settings`, `from src.core.database_manager import create_pool as create_db_pool`, the 6 calculator imports `from src.intelligence.ai.fitness.accuracy import accuracy_score` (and calibration/regime/efficiency/novelty/composite), `from src.observability.metrics import JOB_COMPLETED_TOTAL, FITNESS_COMPOSITE_SCORE, FITNESS_POPULATION_STDDEV, FITNESS_VARIANCE_GATE_STATUS, flush_and_shutdown_metrics`. `structlog` logger.
    - `_WIN_OUTCOMES = {"target_1", "target_1_2", "target_full"}` (match shadow_auditor).
    - `async def _fetch_agent_signals(conn, name, ctype)`: for `i7_plugin` query `signal_ledger_full WHERE setup_plugin=$1 AND outcome IS NOT NULL AND outcome NOT IN ('never_activated','ttl_expired_behind')` returning `outcome, pnl_r, cis_score, hmm_regime_at_fire, signal_id`. For `swarm_agent`/`ai_agent` query `llm_calls WHERE agent_id=$1 AND outcome IS NOT NULL` returning `outcome, pnl_r, confidence, regime, tokens_est, parse_success, signal_id`. Return rows. (Pitfalls 1, 2, 5 — different key + confidence + token source per component type.)
    - `async def _compute_one_agent(rows, ctype, settings) -> dict`: build pnl_r list, outcomes, confidence list (cis_score for i7, confidence for swarm), per-regime dict (group by hmm_regime_at_fire integer for i7, regime text for swarm), tokens_est list (empty for i7 -> efficiency 1.0). Call `accuracy_score(pnl_r, settings.FITNESS_ACCURACY_MIN_N)`, `calibration_score(confidence, outcomes, settings.FITNESS_CALIBRATION_MIN_N)`, `regime_score(per_regime, settings.FITNESS_REGIME_MIN_N_PER_REGIME, settings.FITNESS_REGIME_MIN_REGIMES)`, `efficiency_score(tokens_est, settings.FITNESS_EFFICIENCY_MIN_N, settings.FITNESS_EFFICIENCY_TOKEN_CEILING)` (for i7 pass an empty list to get 1.0). Return a partial dict of the 4 per-agent dimensions + n_resolved + the signal_id->pnl_r map for the novelty pass + per_regime_composite sub-dict for PromotionGate.
    - `async def _run_fitness_audit(pool, settings)`: fetch `shadow_registry` rows (NO swarm_agent early-continue — evaluate all). FIRST PASS: for each agent call `_fetch_agent_signals` + `_compute_one_agent`, collect into `per_agent[name]` and `signals_by_agent[name] = {signal_id: pnl_r}`. SECOND PASS (novelty): for each agent `novelty = novelty_score(name, signals_by_agent, settings.FITNESS_NOVELTY_MIN_OVERLAP)`. Then `composite = composite_score(accuracy, novelty, calibration, regime, efficiency)`. Build `dimensions_jsonb` dict with per-regime composites, raw sub-metrics (pass as a plain dict — never json.dumps). INSERT one row into `agent_fitness` per agent with `datetime.now(UTC)` for evaluated_at, all 5 dimension scores + composite + n_resolved + promotion_ready (False here — promotion decided by shadow_auditor) + dimensions_jsonb dict. Emit `FITNESS_COMPOSITE_SCORE.set(composite, {"agent_id": name})` when composite is not None.
    - `def _variance_gate(composites: list[float], min_population: int, threshold: float) -> tuple[str, float | None]`: a pure helper. If `len(composites) < min_population` return `("insufficient_population", None)`. Else `sd = statistics.pstdev(composites)`; return `("passed", sd)` if `sd >= threshold` else `("blocked", sd)`. After writing all rows, query the latest composite per live agent (the DISTINCT ON query from RESEARCH lines 327-340 OR reuse in-memory composites), call `_variance_gate(...)`, set `FITNESS_POPULATION_STDDEV.set(sd, {})` and `FITNESS_VARIANCE_GATE_STATUS.set(1|0|-1, {})` (1=passed, 0=blocked, -1=insufficient_population), and log the gate decision at info level (`fitness_variance_gate`, status=..., stddev=..., population=...).
    - `_amain` / `main` exactly like shadow_auditor but `job="fitness-auditor"` in BOTH JOB_COMPLETED_TOTAL.add calls.
    - All timestamps `datetime.now(UTC)`. asyncpg JSONB as dict. No prometheus_client.
    Write `tests/unit/services/test_fitness_auditor.py` (analog of test_shadow_auditor.py): test `_variance_gate` directly — insufficient_population below min, blocked when sd < threshold, passed when sd >= threshold; test `_compute_one_agent` returns None composite when a dimension is below its min_n; test the i7 path yields efficiency 1.0 (empty tokens). Use AsyncMock pool/conn for any DB-touching test.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/services/test_fitness_auditor.py -q` exits 0.
    `.venv/bin/python -c "from services.fitness_auditor import _variance_gate; assert _variance_gate([0.1,0.5,0.9,0.2,0.8], 5, 0.2)[0]=='passed'; assert _variance_gate([0.5]*5, 5, 0.2)[0]=='blocked'; assert _variance_gate([0.5,0.6], 5, 0.2)[0]=='insufficient_population'"`.
    `grep -n 'job": "fitness-auditor"' services/fitness_auditor.py` returns 2 matches.
  </verify>
  <acceptance_criteria>
    - services/fitness_auditor.py contains `INSERT INTO agent_fitness`, `from src.intelligence.ai.fitness`, `FITNESS_POPULATION_STDDEV`, `FITNESS_VARIANCE_GATE_STATUS`
    - fitness_auditor.py contains exactly 2 occurrences of `"job": "fitness-auditor"` (success + failure)
    - fitness_auditor.py has NO `swarm_agent` early-continue (it evaluates all component types)
    - fitness_auditor.py uses `datetime.now(UTC)` and passes a dict (not json.dumps) for dimensions_jsonb
    - `_variance_gate([0.1,0.5,0.9,0.2,0.8],5,0.2)[0]=='passed'`, `_variance_gate([0.5]*5,5,0.2)[0]=='blocked'`, `_variance_gate([0.5,0.6],5,0.2)[0]=='insufficient_population'`
    - `.venv/bin/pytest tests/unit/services/test_fitness_auditor.py -q` exits 0
    - `.venv/bin/ruff check services/fitness_auditor.py` exits 0
  </acceptance_criteria>
  <done>fitness_auditor computes all 5 dimensions + composite + novelty per agent, writes agent_fitness, and emits the FIT-06 variance gate decision; job label matches the systemd unit suffix.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor shadow_auditor.py — staleness check + PromotionGate/DemotionGate against agent_fitness</name>
  <files>services/shadow_auditor.py</files>
  <read_first>
    - services/shadow_auditor.py (full file 1-358: the swarm_agent early-continue at lines 99-103, _should_promote/_should_demote at lines 54-59, _check_promotion lines 113-252, _check_demotion lines 255-335, _run_audit lines 85-110)
    - src/intelligence/ai/fitness/gates.py (PromotionGate/DemotionGate signatures created in plan 05)
    - .planning/phases/101-composite-fitness-function/101-PATTERNS.md (staleness check replacement snippet, lines 395-419)
    - src/observability/metrics.py (FITNESS_STALE_SKIP_TOTAL, FITNESS_PROMOTION_READY, FITNESS_DEMOTION_TRIGGERED added in plan 01)
  </read_first>
  <action>
    Refactor `services/shadow_auditor.py`:
    1. Add imports: `from src.intelligence.ai.fitness.gates import PromotionGate, DemotionGate` and `from src.observability.metrics import FITNESS_STALE_SKIP_TOTAL, FITNESS_PROMOTION_READY, FITNESS_DEMOTION_TRIGGERED` (alongside existing metric imports). Add `settings: Settings` access — `_run_audit` must take or construct `Settings()` to read `FITNESS_STALENESS_THRESHOLD_HOURS`, `FITNESS_ACCURACY_MIN_N`, `FITNESS_NOVELTY_MIN_OVERLAP`.
    2. Add `async def _fetch_latest_fitness(pool, agent_id) -> dict | None`: `SELECT ... FROM agent_fitness WHERE agent_id=$1 ORDER BY evaluated_at DESC LIMIT 1`. Add `async def _fetch_fitness_history(pool, agent_id, limit=3) -> list[dict]`: last N rows ordered DESC. Add `async def _fetch_promotion_baseline(pool, agent_id) -> float | None`: `SELECT promotion_baseline FROM shadow_registry WHERE component_name=$1`.
    3. In `_run_audit`: REMOVE the `if ctype == "swarm_agent": continue` block (lines 99-103). For EVERY agent: fetch latest fitness row; if None, log `shadow_audit_no_fitness_row` and continue; compute `staleness_hours = (datetime.now(UTC) - row["evaluated_at"]).total_seconds()/3600`; if `staleness_hours > settings.FITNESS_STALENESS_THRESHOLD_HOURS`, `FITNESS_STALE_SKIP_TOTAL.add(1, {"agent_id": name})`, log warning, continue.
    4. Replace `_check_promotion`'s inline `bootstrap_ci_lower` + `_should_promote` path with `PromotionGate(min_n=settings.FITNESS_ACCURACY_MIN_N, novelty_threshold=0.15, stability_threshold=0.02)`. Call `passed, reason = gate.can_promote(fitness_row, history)`. On pass: set `shadow_registry.is_shadow=FALSE, promotion_baseline=fitness_row["composite_score"], promoted_at=now`, write a `shadow_transition_log` row with `trigger_reason='fitness_gate_cleared'`, `FITNESS_PROMOTION_READY.set(1, {"agent_id": name})`. On block: `FITNESS_PROMOTION_READY.set(0, {"agent_id": name})` and log reason.
    5. Replace `_check_demotion`'s inline EV[R] path with `DemotionGate(decay_fraction=0.80, novelty_floor=0.15, parse_rate_floor=0.80)`. Fetch `promotion_baseline` and compute `parse_success_rate` from a rolling-50 `llm_calls` query for the agent. Call `fired, trigger = gate.should_demote(fitness_row, history, promotion_baseline, parse_success_rate)`. On fire: set `is_shadow=TRUE` (soft death), write `shadow_transition_log` with `trigger_reason=trigger`, `FITNESS_DEMOTION_TRIGGERED.add(1, {"agent_id": name, "trigger": trigger})`.
    6. Delete the now-dead `_should_promote` and `_should_demote` functions (no parallel gate paths — RESEARCH anti-pattern). Keep `_tail_risk_blocks_promotion` only if still referenced; otherwise remove. Update the module docstring (lines 1-9) to describe the fitness-based gates.
    Preserve the oneshot entrypoint and `job="shadow-auditor"` label unchanged. asyncpg dicts for JSONB, `datetime.now(UTC)` throughout.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/services/test_shadow_auditor.py -q` runs (update any tests broken by the gate-function removal so the suite exits 0).
    `grep -n "swarm_agent.*continue\|def _should_promote\|def _should_demote" services/shadow_auditor.py` returns nothing.
    `grep -n "PromotionGate\|DemotionGate\|_fetch_latest_fitness\|FITNESS_STALENESS_THRESHOLD_HOURS\|promotion_baseline" services/shadow_auditor.py` returns matches.
  </verify>
  <acceptance_criteria>
    - shadow_auditor.py contains `from src.intelligence.ai.fitness.gates import PromotionGate, DemotionGate`
    - shadow_auditor.py contains NO `def _should_promote` and NO `def _should_demote`
    - shadow_auditor.py contains NO `if ctype == "swarm_agent"` early-continue
    - shadow_auditor.py contains `_fetch_latest_fitness`, a staleness comparison against `FITNESS_STALENESS_THRESHOLD_HOURS`, and sets `promotion_baseline` on promotion
    - shadow_auditor.py still emits `"job": "shadow-auditor"` (unchanged)
    - `.venv/bin/pytest tests/unit/services/test_shadow_auditor.py -q` exits 0
    - `.venv/bin/ruff check services/shadow_auditor.py` exits 0
  </acceptance_criteria>
  <done>shadow_auditor reads agent_fitness, skips stale agents, and makes promotion/demotion decisions exclusively through the pure gate classes — no inline 1D logic remains.</done>
</task>

<task type="auto">
  <name>Task 3: Systemd units + service DAG registration for fitness-auditor</name>
  <files>production/systemd/indicagent-fitness-auditor.service, production/systemd/indicagent-fitness-auditor.timer, services/service_auditor.py</files>
  <read_first>
    - production/systemd/indicagent-shadow-auditor.service (exact .service template)
    - production/systemd/indicagent-shadow-auditor.timer (exact .timer template — change cadence to 60 minutes)
    - services/service_auditor.py (lines 57-95 _DAG_ORDER, line 95 indicagent-shadow-auditor entry, lines 128+ _AGENT_ID_TO_UNIT, line 171 the shadow-auditor list entry, _LAG_THRESHOLDS if present)
  </read_first>
  <action>
    1. Create `production/systemd/indicagent-fitness-auditor.service` cloned from the shadow-auditor service: `Description=IndicAgent Fitness Auditor`, `Type=oneshot`, `WorkingDirectory=/home/bg/dev/indicagent`, `ExecStart=/home/bg/dev/indicagent/.venv/bin/python services/fitness_auditor.py`, `Environment=PYTHONUNBUFFERED=1`, `After=`/`Requires=indicagent-infrastructure.target`, `[Install] WantedBy=multi-user.target`.
    2. Create `production/systemd/indicagent-fitness-auditor.timer`: `Description=Fitness Auditor Timer — every 60 minutes`, `[Timer] OnCalendar=*:0/60`, `Persistent=true`, `Unit=indicagent-fitness-auditor.service`, `[Install] WantedBy=timers.target` (D-10: 60-minute cadence).
    3. In `services/service_auditor.py`: add `"indicagent-fitness-auditor"` to `_DAG_ORDER` adjacent to the shadow-auditor entry (line 95) with a sensible ordering integer; add it to the `_AGENT_ID_TO_UNIT` mapping (key `fitness-auditor` or `fitness_auditor` matching the agent_id/job convention used for shadow-auditor) and to any oneshot unit list (e.g. line 171 region). If a `_LAG_THRESHOLDS` dict has a shadow-auditor entry, add a fitness-auditor entry too. Match the exact pattern the shadow-auditor uses — do not invent a new convention.
    Do not enable the units on the running system in this task (planning artifact); just create the files. Note in the SUMMARY that `sudo systemctl daemon-reload && sudo systemctl enable --now indicagent-fitness-auditor.timer` is the deploy step.
  </action>
  <verify>
    `test -f production/systemd/indicagent-fitness-auditor.service && test -f production/systemd/indicagent-fitness-auditor.timer && echo units-ok`.
    `grep -n "OnCalendar=\*:0/60" production/systemd/indicagent-fitness-auditor.timer`.
    `grep -n "indicagent-fitness-auditor\|fitness-auditor\|fitness_auditor" services/service_auditor.py` returns matches in _DAG_ORDER and _AGENT_ID_TO_UNIT regions.
    `.venv/bin/python -c "import services.service_auditor"` exits 0.
  </verify>
  <acceptance_criteria>
    - production/systemd/indicagent-fitness-auditor.service exists with `ExecStart=...services/fitness_auditor.py` and `Type=oneshot`
    - production/systemd/indicagent-fitness-auditor.timer exists and contains `OnCalendar=*:0/60`
    - services/service_auditor.py contains `indicagent-fitness-auditor` in the _DAG_ORDER dict
    - services/service_auditor.py maps the fitness-auditor job/agent_id in _AGENT_ID_TO_UNIT
    - `import services.service_auditor` succeeds (exit 0)
    - `.venv/bin/ruff check services/service_auditor.py` exits 0
  </acceptance_criteria>
  <done>fitness-auditor has systemd service + 60-minute timer units and is registered in the canonical service DAG registry consistent with shadow-auditor.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/services/test_fitness_auditor.py tests/unit/services/test_shadow_auditor.py -q` exits 0.
- `.venv/bin/ruff check services/fitness_auditor.py services/shadow_auditor.py services/service_auditor.py` exits 0.
- fitness_auditor writes agent_fitness with all 5 dimensions + composite; FIT-06 variance gate emits passed/blocked/insufficient_population.
- shadow_auditor routes all promotion/demotion through PromotionGate/DemotionGate with staleness protection; no inline 1D gate remains.
- fitness-auditor registered in _DAG_ORDER + _AGENT_ID_TO_UNIT; systemd units present with 60-minute cadence.
</verification>

<success_criteria>
FIT-06 is delivered: the fitness_auditor computes the cross-agent composite variance gate and emits its status. The full DAG (fitness_auditor → agent_fitness → shadow_auditor) is wired with pure gate classes and staleness protection, and the new oneshot is registered + has systemd units.
</success_criteria>

<output>
After completion, create `.planning/phases/101-composite-fitness-function/101-06-SUMMARY.md`
</output>
