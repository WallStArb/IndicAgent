---
phase: 101-composite-fitness-function
plan: 04
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - src/intelligence/ai/fitness/novelty.py
  - src/intelligence/ai/fitness/composite.py
  - tests/unit/intelligence/test_fitness_novelty_composite.py
autonomous: true
requirements: [FIT-02]

must_haves:
  truths:
    - "novelty_score = 1 - max(|Pearson r|) across all live-agent pnl_r pairs over overlapping resolved signals"
    - "novelty_score = 1.0 when population size is 1 (no peers to correlate against)"
    - "Pairs with fewer than FITNESS_NOVELTY_MIN_OVERLAP overlapping signals contribute r=0 (benefit of doubt)"
    - "composite_score returns None if ANY of the 5 dimensions is None"
    - "composite_score collapses to 0.0 if ANY dimension is exactly 0.0 (geometric mean)"
    - "Both calculators are pure: no DB access, no Settings import, no side effects"
  artifacts:
    - path: "src/intelligence/ai/fitness/novelty.py"
      provides: "novelty_score() population-level pairwise calculator (FIT-02)"
      contains: "def novelty_score"
    - path: "src/intelligence/ai/fitness/composite.py"
      provides: "composite_score() geometric-mean combiner with NULL guard (D-04, D-05)"
      contains: "def composite_score"
    - path: "tests/unit/intelligence/test_fitness_novelty_composite.py"
      provides: "pairwise novelty + geometric-mean composite tests"
      contains: "def test_"
  key_links:
    - from: "novelty.py"
      to: "numpy.corrcoef"
      via: "pairwise Pearson r on aligned pnl_r vectors"
      pattern: "corrcoef|pearson"
---

<objective>
Build the novelty dimension (FIT-02) and the geometric-mean composite combiner (D-04, D-05). Novelty is the only population-level dimension: for a target agent it computes `1 - max(|Pearson r|)` against every other live agent over their overlapping resolved `signal_id` sets, measuring PnL decorrelation (true alpha redundancy, not analytical agreement). Composite is the geometric mean of all 5 dimensions with the hard rule that any NULL dimension makes composite NULL and any 0.0 dimension collapses composite to 0.0.

Purpose: Novelty must be a function over the full population (RESEARCH pitfall 4 — cannot be computed per-agent in isolation). The composite combiner is the structural enforcement of "weaknesses cannot be hidden" — geometric mean, no weighted-linear alternative, no compensation logic.
Output: novelty.py, composite.py, and a shared test file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-composite-fitness-function/101-CONTEXT.md
@.planning/phases/101-composite-fitness-function/101-RESEARCH.md
@.planning/phases/101-composite-fitness-function/101-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: TDD novelty_score (FIT-02)</name>
  <files>src/intelligence/ai/fitness/novelty.py, tests/unit/intelligence/test_fitness_novelty_composite.py</files>
  <read_first>
    - .planning/phases/101-composite-fitness-function/101-RESEARCH.md (Pearson r helper, lines 363-377; D-11/D-12 novelty rules; pitfall 4 population second-pass, lines 237-242)
    - .planning/phases/101-composite-fitness-function/101-CONTEXT.md (D-11, D-12, D-13: 1-max(|r|), min 20 overlap, population=1 -> 1.0)
  </read_first>
  <action>
    RED: In `tests/unit/intelligence/test_fitness_novelty_composite.py` write failing tests for `novelty_score(target_id: str, signals_by_agent: dict[str, dict[str, float]], min_overlap: int) -> float` where `signals_by_agent` maps `agent_id -> {signal_id: pnl_r}`:
    - population of 1 (only target present) -> returns 1.0
    - two agents with perfectly correlated overlapping pnl_r (r=1.0) over >= min_overlap signals -> returns ~0.0
    - two agents with perfectly anti-correlated pnl_r (r=-1.0) -> returns ~0.0 (uses |r|)
    - two agents with uncorrelated pnl_r -> returns close to 1.0
    - a peer with < min_overlap overlapping signals contributes r=0 (does not reduce novelty)
    - novelty is in [0,1]
    GREEN: Implement `novelty_score` in `src/intelligence/ai/fitness/novelty.py`. Import `numpy as np`. For each peer `!= target_id`: align on the intersection of signal_id keys; if `len(overlap) < min_overlap` treat r as 0.0; if either aligned vector has zero variance treat r as 0.0; else `r = float(np.corrcoef(a, b)[0,1])`. `max_abs_r = max(|r| over all peers, default 0.0)`. Return `1.0 - max_abs_r`. If no peers, return 1.0. No Settings import, no DB. Also expose a thin internal helper `_pearson_r(vec_a, vec_b, min_overlap)` returning 0.0 on insufficient overlap / zero variance (mirrors RESEARCH helper) so it is unit-testable.
    REFACTOR: ensure clamping keeps the result in [0,1].
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/test_fitness_novelty_composite.py -k novelty -q` exits 0.
    `.venv/bin/python -c "from src.intelligence.ai.fitness.novelty import novelty_score; assert novelty_score('a', {'a': {str(i):0.1 for i in range(25)}}, 20)==1.0"`.
  </verify>
  <acceptance_criteria>
    - src/intelligence/ai/fitness/novelty.py contains `def novelty_score` and `corrcoef`
    - novelty.py contains NO `import asyncpg`, NO `from src.config`
    - single-agent population returns 1.0
    - perfectly correlated pair (>= 20 overlap) returns a value near 0.0 (< 0.05)
    - a peer below min_overlap does not reduce novelty below 1.0
    - test file `-k novelty` selects >= 5 tests and all pass
  </acceptance_criteria>
  <done>novelty_score is the population-level 1-max(|r|) decorrelation metric with the overlap gate and population=1 convention.</done>
</task>

<task type="tdd">
  <name>Task 2: TDD composite_score geometric mean (D-04, D-05)</name>
  <files>src/intelligence/ai/fitness/composite.py, tests/unit/intelligence/test_fitness_novelty_composite.py</files>
  <read_first>
    - src/intelligence/ai/fitness/novelty.py (created in Task 1 — replicate pure-module shape)
    - .planning/phases/101-composite-fitness-function/101-RESEARCH.md (geometric mean with NULL guard, lines 170-187)
    - .planning/phases/101-composite-fitness-function/101-CONTEXT.md (D-04 geometric mean non-negotiable; D-05 NULL until all 5 computable)
  </read_first>
  <action>
    RED: Add failing tests to the same test file for `composite_score(accuracy, novelty, calibration, regime, efficiency)` where each arg is `float | None`:
    - returns None if any argument is None (test each of the 5 positions individually)
    - returns 0.0 if any argument is exactly 0.0 (geometric mean collapse — test each position)
    - five equal values v return v (e.g. all 0.5 -> 0.5)
    - known case: (0.5, 0.5, 0.5, 0.5, 0.5) -> 0.5; (1.0, 1.0, 1.0, 1.0, 0.32) -> 0.32^(1/5)
    - result is in [0,1] for all in-range inputs
    GREEN: Implement `composite_score` in `src/intelligence/ai/fitness/composite.py`. Signature `composite_score(accuracy: float | None, novelty: float | None, calibration: float | None, regime: float | None, efficiency: float | None) -> float | None`. Collect into a list; if `any(s is None ...)` return None (D-05). Else return `math.pow(math.prod(scores), 1.0 / 5)` (D-04). Do NOT special-case the zero — `math.prod` with a 0.0 yields 0.0 and `pow(0, 0.2)==0.0`, which is the intended collapse. No weighted-linear alternative, no compensation. No Settings import, no DB. Use `import math` only.
    REFACTOR: confirm there is exactly one return path for None and one for the geometric mean — no branches that special-case individual dimensions.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/test_fitness_novelty_composite.py -k composite -q` exits 0.
    `.venv/bin/python -c "from src.intelligence.ai.fitness.composite import composite_score; assert composite_score(0.5,0.5,0.5,0.5,0.5)==0.5; assert composite_score(0.9,None,0.8,0.6,0.9) is None; assert composite_score(0.9,0.0,0.8,0.6,0.9)==0.0"`.
  </verify>
  <acceptance_criteria>
    - src/intelligence/ai/fitness/composite.py contains `def composite_score`
    - composite.py uses `math.prod` and `math.pow` (geometric mean), NOT a weighted sum
    - composite.py contains NO `import asyncpg`, NO `from src.config`
    - `composite_score(0.5,0.5,0.5,0.5,0.5)` returns 0.5
    - `composite_score(0.9,None,0.8,0.6,0.9)` returns None
    - `composite_score(0.9,0.0,0.8,0.6,0.9)` returns 0.0
    - test file `-k composite` selects >= 8 tests (None+zero in each of 5 positions) and all pass
  </acceptance_criteria>
  <done>composite_score is the geometric mean of 5 dimensions, NULL if any None, 0.0 if any zero — no compensation logic.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/intelligence/test_fitness_novelty_composite.py -q` exits 0.
- `.venv/bin/ruff check src/intelligence/ai/fitness/novelty.py src/intelligence/ai/fitness/composite.py` exits 0.
- Neither module imports asyncpg or Settings.
</verification>

<success_criteria>
FIT-02 (novelty) is the population-level decorrelation metric and the composite combiner enforces geometric-mean collapse (NULL on any None, 0.0 on any zero), fully unit-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/101-composite-fitness-function/101-04-SUMMARY.md`
</output>
