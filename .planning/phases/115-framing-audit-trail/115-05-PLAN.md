---
phase: 115
plan: 05
type: tdd
wave: 4
depends_on:
  - 115-01
  - 115-03
files_modified:
  - src/observability/metrics.py
  - src/intelligence/trading/trade_framer.py
  - tests/unit/intelligence/test_trade_framer.py
autonomous: true
must_haves:
  truths:
    - "STOP_BUFFER_MULT_DISTRIBUTION histogram exists in src/observability/metrics.py"
    - "frame_trade() records the multiplier via STOP_BUFFER_MULT_DISTRIBUTION.record(adaptive_buffer_mult, {regime_type, stop_type}) on every call"
    - "frame_trade() emits a structlog DEBUG event 'adaptive_buffer_applied' when buffer_mult != 1.0 AND regime_type is trend or mean_reversion"
    - "No debug log when buffer is neutral (multiplier == 1.0)"
    - "TestFrameTradeObservability tests pass"
    - "Full unit suite passes"
  artifacts:
    - path: "src/observability/metrics.py"
      provides: "STOP_BUFFER_MULT_DISTRIBUTION histogram"
      contains: "STOP_BUFFER_MULT_DISTRIBUTION"
    - path: "src/intelligence/trading/trade_framer.py"
      provides: "OTel histogram record + structlog debug block after adaptive_buffer_mult computation"
      contains: "STOP_BUFFER_MULT_DISTRIBUTION.record"
    - path: "tests/unit/intelligence/test_trade_framer.py"
      provides: "TestFrameTradeObservability class with 2 caplog tests"
      contains: "TestFrameTradeObservability"
  key_links:
    - from: "frame_trade() after adaptive_buffer_mult = _adaptive_buffer(...)"
      to: "STOP_BUFFER_MULT_DISTRIBUTION.record()"
      via: "direct call with {regime_type: regime_type or 'any', stop_type: stop_type}"
      pattern: "STOP_BUFFER_MULT_DISTRIBUTION\\.record"
    - from: "frame_trade() if adaptive_buffer_mult != 1.0 block"
      to: "_logger.debug('adaptive_buffer_applied', ...)"
      via: "structlog conditional debug"
      pattern: "adaptive_buffer_applied"
---

<objective>
Add an OTel histogram to metrics.py for adaptive buffer multiplier distribution, and add structured observability (histogram record + conditional structlog debug) inside frame_trade() to make vol regime drift operationally detectable.

Purpose: Without instrumentation, there is no way to know if Hurst tightening is active, how often it fires, or whether it's drifting. The histogram enables Grafana alerting on regime-based buffer shifts.
Output: STOP_BUFFER_MULT_DISTRIBUTION histogram + conditional debug log + 2 passing tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@docs/superpowers/plans/2026-06-05-framing-audit-trail.md
@src/observability/metrics.py
@src/intelligence/trading/trade_framer.py
@tests/unit/intelligence/test_trade_framer.py
</context>

<tasks>

<task type="tdd">
  <name>Task 5: Add OTel histogram and structlog debug to frame_trade()</name>
  <files>
    src/observability/metrics.py
    src/intelligence/trading/trade_framer.py
    tests/unit/intelligence/test_trade_framer.py
  </files>
  <read_first>
    - src/observability/metrics.py (read the full file — find the pattern used to create existing histograms, e.g. SIGNAL_MFE_DISTRIBUTION, to match the exact style. Note: use _meter.create_histogram(), unit="1", no prometheus_client imports)
    - src/intelligence/trading/trade_framer.py (check imports at top: does structlog already exist? Does any import from src.observability.metrics already exist? Find where adaptive_buffer_mult is computed post-plan-01 — the observability block goes immediately after that line, before _classify_stop_basis)
    - tests/unit/intelligence/test_trade_framer.py (read TestFrameTradeAuditFields class added in plan 01 — understand the frame_trade import and test style)
    - docs/superpowers/plans/2026-06-05-framing-audit-trail.md (Task 5 Steps 5.1-5.4 for exact test code, histogram definition, import additions, and observability block)
  </read_first>
  <action>
    RED phase — write failing tests first per source plan Task 5 Step 5.1:
    Add class TestFrameTradeObservability to tests/unit/intelligence/test_trade_framer.py.
    IMPORTANT: Use the test code below, NOT the code in source plan Step 5.1 — source plan Step 5.1 uses caplog which does not capture structlog processor output without a stdlib bridge. Use structlog.testing.capture_logs() instead:
    Pattern:
      from structlog.testing import capture_logs
      def test_structlog_debug_emitted_when_hurst_fires(self):
          with capture_logs() as cap_logs:
              f = {"garch_vol_ratio": 1.0, "hurst_exponent": 0.75, "timeframe": "5m"}
              frame_trade("trend_long", 1, 5000.0, f, atr=10.0, regime_type="trend")
          events = [e["event"] for e in cap_logs]
          assert "adaptive_buffer_applied" in events

      def test_no_debug_when_buffer_neutral(self):
          with capture_logs() as cap_logs:
              f = {"garch_vol_ratio": 1.0, "timeframe": "5m"}
              frame_trade("trend_long", 1, 5000.0, f, atr=10.0)
          events = [e["event"] for e in cap_logs]
          assert "adaptive_buffer_applied" not in events
    Run to confirm FAIL (event not yet emitted).

    GREEN phase — add histogram to metrics.py per source plan Task 5 Step 5.2:
    After the SIGNAL_MFE_DISTRIBUTION block (or the last existing histogram), add:
    STOP_BUFFER_MULT_DISTRIBUTION = _meter.create_histogram(
        "stop_buffer_mult_distribution",
        description="Adaptive buffer multiplier at frame time by regime_type and stop_type — alerts on vol regime drift",
        unit="1",
    )
    Use the same _meter reference pattern as existing histograms in that file.

    Add observability to trade_framer.py per source plan Task 5 Steps 5.3:
    1. Check if structlog is already imported. If not, add: import structlog as _structlog and _logger = _structlog.get_logger(__name__)
    2. Add import: from src.observability.metrics import STOP_BUFFER_MULT_DISTRIBUTION
    3. After the adaptive_buffer_mult = _adaptive_buffer(...) line (added in plan 01) AND after stop_type is resolved (check the frame_trade() body to confirm stop_type is available at that point — it may be resolved earlier in the function), add:
       STOP_BUFFER_MULT_DISTRIBUTION.record(
           adaptive_buffer_mult,
           {"regime_type": regime_type or "any", "stop_type": stop_type},
       )
       if adaptive_buffer_mult != 1.0 and regime_type in ("trend", "mean_reversion"):
           _logger.debug(
               "adaptive_buffer_applied",
               regime_type=regime_type,
               vol_ratio=features.get("garch_vol_ratio"),
               hurst=features.get("hurst_exponent"),
               buffer_mult=round(adaptive_buffer_mult, 4),
               stop_type=stop_type,
           )

    Note on placement: if stop_type is resolved AFTER _classify_stop_basis, the histogram call must go after _classify_stop_basis so stop_type is defined. Check the variable name — in the frame_trade() body it may be named "stop_type" throughout or only resolved from _classify_stop_basis output. Adjust placement accordingly.

    Run: .venv/bin/pytest tests/unit/intelligence/test_trade_framer.py -q --tb=short
    Run: .venv/bin/pytest tests/unit/ -q --tb=short 2>&1 | tail -5
  </action>
  <verify>
    .venv/bin/pytest tests/unit/intelligence/test_trade_framer.py::TestFrameTradeObservability -v 2>&1 | tail -8
    grep -n "STOP_BUFFER_MULT_DISTRIBUTION" src/observability/metrics.py src/intelligence/trading/trade_framer.py
  </verify>
  <done>Both TestFrameTradeObservability tests PASS using capture_logs(). STOP_BUFFER_MULT_DISTRIBUTION exists in metrics.py and is imported + recorded in trade_framer.py. Full unit suite green.</done>
</task>

</tasks>

<verification>
```bash
# Histogram in metrics
grep -n "STOP_BUFFER_MULT_DISTRIBUTION" src/observability/metrics.py

# Histogram used in framer
grep -n "STOP_BUFFER_MULT_DISTRIBUTION\|adaptive_buffer_applied" src/intelligence/trading/trade_framer.py

# Observability tests
.venv/bin/pytest tests/unit/intelligence/test_trade_framer.py::TestFrameTradeObservability -v

# Full suite
.venv/bin/pytest tests/unit/ -q --tb=short 2>&1 | tail -5

# End-to-end verification greps from source plan Step 5.5
grep -rn "frame_trade(" src/intelligence/trading/ --include="*.py" \
  | grep -v "def frame_trade\|regime_type=\|microstructure_utils\|test_\|trade_framer.py"
# → 0 results (all call sites wired)
```
</verification>

<success_criteria>
- STOP_BUFFER_MULT_DISTRIBUTION histogram defined in src/observability/metrics.py using _meter.create_histogram() (never prometheus_client)
- frame_trade() records histogram on every call with regime_type and stop_type labels
- frame_trade() emits structlog DEBUG event "adaptive_buffer_applied" only when buffer != 1.0 and regime_type is trend or mean_reversion
- TestFrameTradeObservability: both tests PASS (debug fires for Hurst, silent for neutral)
- Full unit suite exits 0
</success_criteria>

<output>
After completion, create docs/superpowers/plans/framing-audit-trail/115-05-SUMMARY.md with: histogram labels, structlog condition, test results, full suite status.

Then run the Done-Coding SOP:
1. code-simplifier agent (invoke automatically)
2. /review
3. .venv/bin/pytest tests/unit/ -q  -- must be green
4. git checkout main && git merge --ff-only &lt;branch&gt;
5. git branch -d &lt;branch&gt;
6. git worktree prune
7. git push origin main
</output>
