---
phase: 115
plan: 04
type: tdd
wave: 4
depends_on:
  - 115-03
files_modified:
  - production/migrations/119_framing_audit_trail.sql
  - src/persistence/repository/signal_ledger_repository.py
  - services/signal_writer.py
  - tests/unit/services/test_signal_writer.py
autonomous: true
must_haves:
  truths:
    - "signal_ledger table has 5 new nullable columns: stop_basis, stop_type, structural_stop_distance_atr, adaptive_buffer_mult, plugin_regime_type"
    - "signal_ledger_full view is recreated and exposes all 5 new columns"
    - "LedgerEntry dataclass has all 5 new fields (all nullable/optional)"
    - "_INSERT_SQL includes all 5 new columns at positions $29-$33"
    - "_to_row() tuple has 33 elements matching the 33-parameter INSERT"
    - "signal_writer.py populates all 5 fields from signal dict via sig.get() with None defaults"
    - "All TestFramingAuditFieldsInLedgerEntry tests pass"
    - "signal_ledger_full SELECT of new columns does not error"
  artifacts:
    - path: "production/migrations/119_framing_audit_trail.sql"
      provides: "ALTER TABLE + CREATE OR REPLACE VIEW for signal_ledger_full"
      contains: "adaptive_buffer_mult"
    - path: "src/persistence/repository/signal_ledger_repository.py"
      provides: "LedgerEntry with 5 new fields, _INSERT_SQL at $33, _to_row() with 33 elements"
      contains: "stop_basis"
    - path: "services/signal_writer.py"
      provides: "5 new field extractions from signal dict"
      contains: "adaptive_buffer_mult=sig.get"
    - path: "tests/unit/services/test_signal_writer.py"
      provides: "TestFramingAuditFieldsInLedgerEntry class with 6 tests"
      contains: "TestFramingAuditFieldsInLedgerEntry"
  key_links:
    - from: "services/signal_writer.py _payload_to_ledger_entries()"
      to: "LedgerEntry constructor"
      via: "stop_basis=sig.get('stop_basis'), adaptive_buffer_mult=sig.get('adaptive_buffer_mult'), etc."
      pattern: "adaptive_buffer_mult=sig\\.get"
    - from: "LedgerEntry._to_row()"
      to: "asyncpg executemany"
      via: "33-element tuple matching _INSERT_SQL $1-$33"
      pattern: "self\\.plugin_regime_type.*#.*\\$33"
---

<objective>
Add the 5 framing audit fields to signal_ledger (migration 119), update LedgerEntry and _INSERT_SQL to persist them, and wire signal_writer.py to extract them from the signal dict.

Purpose: The capture chain (TradeFrame → signal dict → signal_ledger) is only complete when the fields reach the database. This plan closes the persistence gap. signal_ledger_full view is recreated so downstream analytics sees the new columns immediately.
Output: Migration SQL + updated repository + writer + 6 passing persistence tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@docs/superpowers/plans/2026-06-05-framing-audit-trail.md
@src/persistence/repository/signal_ledger_repository.py
@services/signal_writer.py
@tests/unit/services/test_signal_writer.py
</context>

<tasks>

<task type="tdd">
  <name>Task 4a: Write failing tests, create migration 119, update LedgerEntry and _INSERT_SQL</name>
  <files>
    tests/unit/services/test_signal_writer.py
    production/migrations/119_framing_audit_trail.sql
    src/persistence/repository/signal_ledger_repository.py
  </files>
  <read_first>
    - src/persistence/repository/signal_ledger_repository.py (full file — read LedgerEntry dataclass fields, _INSERT_SQL with current $1-$28 layout, _to_row() tuple to understand the existing parameter count before extending to $33)
    - tests/unit/services/test_signal_writer.py (read all existing tests and imports to understand how _payload_to_ledger_entries is imported and what fixtures exist)
    - docs/superpowers/plans/2026-06-05-framing-audit-trail.md (Task 4 Steps 4.1-4.6 for exact test fixtures, SQL migration, LedgerEntry additions, and _INSERT_SQL / _to_row() target state)
    - Run first: ls production/migrations/ | sort | tail -3 — confirm 118 is latest, so 119 is correct
  </read_first>
  <action>
    RED phase — write tests first:
    Add class TestFramingAuditFieldsInLedgerEntry to tests/unit/services/test_signal_writer.py per source plan Task 4 Step 4.2. The fixture has a minimal payload dict with all 5 audit fields. The 6 tests check each field is extracted and missing fields default to None. Run to confirm AttributeError on LedgerEntry.stop_basis.

    Create migration 119 per source plan Task 4 Step 4.3:
    File: production/migrations/119_framing_audit_trail.sql
    Content: ALTER TABLE signal_ledger ADD COLUMN IF NOT EXISTS for all 5 columns (text/double precision types) + COMMENT ON COLUMN for each + CREATE OR REPLACE VIEW signal_ledger_full AS with FULL explicit column list including all 5 new columns and the LEFT JOIN to signal_outcomes. The complete SQL is in source plan Step 4.3.
    CRITICAL: The view definition must include ALL existing columns plus the 5 new ones — copy the full SELECT list from source plan exactly to avoid dropping columns from the view.

    GREEN phase — update LedgerEntry:
    In signal_ledger_repository.py, add 5 new optional fields to LedgerEntry after feature_schema_version per source plan Task 4 Step 4.4:
    - stop_basis: str | None = None
    - stop_type: str | None = None
    - structural_stop_distance_atr: float | None = None
    - adaptive_buffer_mult: float | None = None
    - plugin_regime_type: str | None = None
    (The status field with default SignalStatus.PENDING remains last — it is NOT stored in signal_ledger)

    Update _INSERT_SQL per source plan Task 4 Step 4.5: add 5 column names to the INSERT list and 5 placeholders ($29-$33). Current INSERT has $28 parameters; new INSERT has $33. Verify the column name list and placeholder count match exactly before committing.

    Update _to_row() per source plan Task 4 Step 4.6: extend the tuple with 5 new entries at positions $29-$33. Verify tuple length is 33.

    Run: .venv/bin/pytest tests/unit/services/test_signal_writer.py::TestFramingAuditFieldsInLedgerEntry -q
  </action>
  <verify>.venv/bin/pytest tests/unit/services/test_signal_writer.py -q --tb=short 2>&1 | tail -10</verify>
  <done>All 6 TestFramingAuditFieldsInLedgerEntry tests PASS. _to_row() returns a 33-element tuple — verify by counting elements or confirming the last placeholder is $33. Migration file exists at production/migrations/119_framing_audit_trail.sql.</done>
</task>

<task type="auto">
  <name>Task 4b: Wire signal_writer.py, apply migration, verify DB view</name>
  <files>
    services/signal_writer.py
  </files>
  <read_first>
    - services/signal_writer.py (find _payload_to_ledger_entries() — read the LedgerEntry(...) constructor call to locate feature_schema_version=FEATURE_SCHEMA_VERSION line where new fields are appended after)
    - docs/superpowers/plans/2026-06-05-framing-audit-trail.md (Task 4 Steps 4.7-4.9 for signal_writer changes and DB verification commands)
  </read_first>
  <action>
    Update signal_writer.py per source plan Task 4 Step 4.7:
    In _payload_to_ledger_entries(), in the LedgerEntry(...) constructor call, after the feature_schema_version=FEATURE_SCHEMA_VERSION line, add:
    - stop_basis=sig.get("stop_basis"),
    - stop_type=sig.get("stop_type"),
    - structural_stop_distance_atr=sig.get("structural_stop_distance_atr"),
    - adaptive_buffer_mult=sig.get("adaptive_buffer_mult"),
    - plugin_regime_type=sig.get("plugin_regime_type"),

    Apply migration to local DB:
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/119_framing_audit_trail.sql
    Expected output: ALTER TABLE ... CREATE OR REPLACE VIEW

    Verify view exposes new columns (per source plan Step 4.9):
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c \
      "SELECT column_name FROM information_schema.columns WHERE table_name='signal_ledger_full' AND column_name IN ('stop_basis','stop_type','adaptive_buffer_mult','plugin_regime_type');"
    Must return 4 rows.

    Also run the SELECT 0-row check:
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c \
      "SELECT stop_basis, stop_type, adaptive_buffer_mult, plugin_regime_type FROM signal_ledger_full LIMIT 0;"
    Must not error.

    Run full unit suite: .venv/bin/pytest tests/unit/ -q --tb=short 2>&1 | tail -5
  </action>
  <verify>
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c \
      "SELECT column_name FROM information_schema.columns WHERE table_name='signal_ledger_full' AND column_name IN ('stop_basis','stop_type','adaptive_buffer_mult','plugin_regime_type');" 2>&1 | grep -c "stop_"
    # → 4
    .venv/bin/pytest tests/unit/ -q --tb=short 2>&1 | tail -5
  </verify>
  <done>Migration applied. signal_ledger_full exposes all 5 new columns. signal_writer.py has all 5 sig.get() extractions. Full unit suite green.</done>
</task>

</tasks>

<verification>
```bash
# LedgerEntry has 5 new fields
grep -n "stop_basis\|stop_type\|structural_stop\|adaptive_buffer_mult\|plugin_regime_type" \
  src/persistence/repository/signal_ledger_repository.py

# Writer extracts 5 fields
grep -n "sig.get.*stop_basis\|sig.get.*adaptive_buffer_mult\|sig.get.*plugin_regime_type" \
  services/signal_writer.py

# View has new columns
PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c \
  "SELECT stop_basis, stop_type, adaptive_buffer_mult, plugin_regime_type FROM signal_ledger_full LIMIT 0;"

# Unit tests green
.venv/bin/pytest tests/unit/ -q --tb=short 2>&1 | tail -5
```
</verification>

<success_criteria>
- migration 119 applied; ALTER TABLE and CREATE OR REPLACE VIEW complete without error
- LedgerEntry has 5 new optional fields inserted before the status field
- _INSERT_SQL has 33 parameters ($1-$33); _to_row() returns a 33-element tuple
- signal_writer.py extracts all 5 fields from signal dict with sig.get() (None default)
- SELECT of 5 new columns from signal_ledger_full does not error
- .venv/bin/pytest tests/unit/ exits 0
</success_criteria>

<output>
After completion, create docs/superpowers/plans/framing-audit-trail/115-04-SUMMARY.md with: migration output, view column count, INSERT param count, test results.
</output>
