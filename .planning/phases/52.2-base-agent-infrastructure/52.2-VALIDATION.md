---
phase: 52.2
slug: base-agent-infrastructure
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-26
---

# Phase 52.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.0.2 + pytest-asyncio |
| **Config file** | `pytest.ini` (root) |
| **Quick run command** | `.venv/bin/pytest tests/unit/test_base_agent.py tests/unit/test_agent_registry.py tests/unit/test_indicator_compute_agent_class.py -v` |
| **Full suite command** | `.venv/bin/pytest tests/unit/ -q` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run the new test file for that task
- **After Wave 1:** `.venv/bin/pytest tests/unit/ -q`
- **Phase gate:** Full suite green before `/gsd:verify-work`

---

## Wave 0 Gaps (tests to write before implementation)

- [ ] `tests/unit/test_base_agent.py` — covers SC-1, SC-2 (BaseAgent exists, lifecycle methods)
- [ ] `tests/unit/test_agent_registry.py` — covers SC-6 (AgentRegistry singleton + enumerate)
- [ ] `tests/unit/test_indicator_compute_agent_class.py` — covers SC-3, SC-4, SC-5 (inherits BaseAgent, class renamed, no hot-path DB)
- [ ] Fix broken imports in 4 existing test files (covers SC-7: no import regressions)

---

## Phase Requirements → Test Map

| SC-ID | Behavior | Test Type | Automated Command | File |
|-------|----------|-----------|-------------------|------|
| SC-1 | `src/core/agent/__init__.py` and `base.py` exist | unit | `pytest tests/unit/test_base_agent.py -v` | Wave 0 |
| SC-2 | BaseAgent provides start/stop/SIGTERM/_report_consumer_lag | unit | `pytest tests/unit/test_base_agent.py -v` | Wave 0 |
| SC-3 | `isinstance(IndicatorComputeAgent(), BaseAgent)` is True | unit | `pytest tests/unit/test_indicator_compute_agent_class.py -v` | Wave 0 |
| SC-4 | IndicatorComputeAgent class name used everywhere (no IndicatorService) | unit (AST) | `pytest tests/unit/test_indicator_compute_agent_class.py -v` | Wave 0 |
| SC-5 | No hot-path DB writes in IndicatorComputeAgent | unit (grep) | `pytest tests/unit/test_indicator_compute_agent_class.py -v` | Wave 0 |
| SC-6 | AgentRegistry registers and enumerates live agents; singleton | unit | `pytest tests/unit/test_agent_registry.py -v` | Wave 0 |
| SC-7 | All unit tests pass; no import regressions in 4 broken test files | unit | `.venv/bin/pytest tests/unit/ -q` | Existing (fixed) |
