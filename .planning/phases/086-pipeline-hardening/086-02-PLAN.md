---
phase: 086-pipeline-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/signal_writer_agent.py
autonomous: true
requirements: [PIPE-02]
must_haves:
  truths:
    - "Every signal entering SignalWriterAgent is validated against validate_signal() before persistence"
    - "Invalid signals are routed to topic_signal_writer_dlq via the inherited _send_to_dlq path"
    - "Valid signals continue through _payload_to_ledger_entries unchanged"
  artifacts:
    - path: "services/signal_writer_agent.py"
      provides: "Signal validation gate + pending DLQ buffer drained in _flush_batch"
      contains: "validate_signal"
  key_links:
    - from: "SignalWriterAgent._parse_payload"
      to: "validate_signal from src.intelligence.trading.signal_schema"
      via: "per-signal filter, invalid -> _invalid_signals buffer"
      pattern: "validate_signal\\(sig\\)"
    - from: "SignalWriterAgent._flush_batch"
      to: "self._send_to_dlq(sig, ValueError(...))"
      via: "drain _invalid_signals before insert_signals"
      pattern: "await self\\._send_to_dlq"
---

<objective>
Wire `validate_signal()` at the I7 output boundary inside SignalWriterAgent so that malformed signal dictionaries are DLQ'd before reaching `signal_ledger` (PIPE-02).

Purpose: `signal_ledger` is the last line of defense before persisted state; today the writer accepts whatever the pipeline publishes. A schema-broken or partial signal silently lands in the ledger.
Output: `services/signal_writer_agent.py` updated with validation gate and async DLQ drain.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/086-pipeline-hardening/086-RESEARCH.md
@.planning/phases/086-pipeline-hardening/086-PATTERNS.md
@src/intelligence/trading/signal_schema.py
@src/core/agent/base.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add validate_signal gate + pending DLQ buffer to SignalWriterAgent</name>
  <files>services/signal_writer_agent.py</files>
  <read_first>
    - services/signal_writer_agent.py (current `_parse_payload` at lines 92-94, `_dlq_topic` override at lines 85-87, `_flush_batch` and `__init__`, `_payload_to_ledger_entries` module-level helper)
    - src/intelligence/trading/signal_schema.py (validate_signal definition, REQUIRED_SIGNAL_FIELDS, SIGNAL_SCHEMA_VERSION constant)
    - src/core/agent/base.py (`_send_to_dlq` signature and DLQPayload assembly, lines 355-400)
  </read_first>
  <action>
    Modify `services/signal_writer_agent.py`:

    1. Add import: `from src.intelligence.trading.signal_schema import validate_signal`.
    2. In `SignalWriterAgent.__init__`, after the existing field initializations, add: `self._invalid_signals: list[dict] = []`.
    3. Rewrite `_parse_payload(self, payload: dict) -> list | None`:
       - Read `symbol = payload.get("symbol", "")`, `tf = payload.get("tf", "")`, `signals: list[dict] = payload.get("signals", [])`.
       - Iterate signals; partition into `valid: list[dict]` and `invalid: list[dict]` based on `validate_signal(sig)`.
       - Extend `self._invalid_signals` with the invalid list (drained async in `_flush_batch`).
       - Call `rows = _payload_to_ledger_entries({**payload, "signals": valid})` and return `rows if rows else None`.
       - If invalid signals were partitioned, log `self.logger.warning("signal_writer.invalid_signals_partitioned", count=len(invalid), symbol=symbol, tf=tf)`.
    4. Modify `_flush_batch(self, batch: list) -> None`: BEFORE the existing `assert self._repo is not None` / `insert_signals` block, drain the buffer:
       ```
       for sig in self._invalid_signals:
           await self._send_to_dlq(sig, ValueError("validate_signal failed"))
       self._invalid_signals.clear()
       ```
       Leave the existing insert + metric logic unchanged.
    5. Do NOT modify `_dlq_topic()` — it already returns `topic_signal_writer_dlq(self.settings.env_name)`.
    6. Do NOT modify the module-level `_payload_to_ledger_entries` helper.

    Structlog kwargs only; never `event=`. UUIDs must be `str()`'d before any JSON/Kafka path (already handled by `_send_to_dlq`/DLQPayload).
  </action>
  <verify>
    grep -n "from src.intelligence.trading.signal_schema import validate_signal" services/signal_writer_agent.py
    grep -n "self._invalid_signals" services/signal_writer_agent.py
    grep -n "validate_signal(sig)" services/signal_writer_agent.py
    grep -n "await self._send_to_dlq" services/signal_writer_agent.py
    .venv/bin/ruff check services/signal_writer_agent.py
    .venv/bin/pytest tests/unit/ -k "signal_writer or signal_schema" -q
  </verify>
  <acceptance_criteria>
    - Source assertion: `services/signal_writer_agent.py` imports `validate_signal` from `src.intelligence.trading.signal_schema`
    - Source assertion: `__init__` initializes `self._invalid_signals: list[dict] = []`
    - Source assertion: `_parse_payload` partitions signals via `validate_signal(sig)` into valid and invalid lists
    - Source assertion: `_parse_payload` extends `self._invalid_signals` with invalid items and passes only valid signals to `_payload_to_ledger_entries`
    - Source assertion: `_flush_batch` contains a loop `for sig in self._invalid_signals: await self._send_to_dlq(sig, ...)` followed by `self._invalid_signals.clear()` BEFORE the existing insert call
    - Source assertion: `_dlq_topic` is unchanged (still returns `topic_signal_writer_dlq(self.settings.env_name)`)
    - Behavior assertion: a payload containing one valid signal and one signal with missing `targets` field results in 1 row passed to `insert_signals` and 1 DLQ publish call (verifiable via unit test or by tracing through partition logic)
    - CLI: `.venv/bin/ruff check services/signal_writer_agent.py` exits 0
  </acceptance_criteria>
  <done>
    Validation gate wired; invalid signals buffered then DLQ'd in `_flush_batch`; valid signals continue to ledger; ruff clean.
  </done>
</task>

</tasks>

<verification>
- `validate_signal()` is called once per signal in `_parse_payload`
- `_send_to_dlq` is only called from async context (inside `_flush_batch`)
- No change to `BaseWriterAgent` base class signatures
- DLQ payload type is the existing `DLQPayload` envelope (constructed inside `_send_to_dlq`)
</verification>

<success_criteria>
- PIPE-02: validation gate wired at writer boundary; invalid signals DLQ'd; no silent acceptance
- `.venv/bin/ruff check services/signal_writer_agent.py` exits 0
- `.venv/bin/pytest tests/unit/ -q` exits 0
</success_criteria>

<output>
Create `.planning/phases/086-pipeline-hardening/086-02-SUMMARY.md` documenting: (a) chosen async-bridge pattern (pending buffer in __init__ vs alternative of making `_parse_payload` async), (b) any backfill-signal compatibility concerns observed (per Pitfall 4 in 086-RESEARCH.md), and (c) recommended follow-up unit test capturing the partition behavior.
</output>
