---
phase: 086-pipeline-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/agent/base.py
  - services/service_auditor_agent.py
autonomous: true
requirements: [OBS-03]
must_haves:
  truths:
    - "Every BaseAgent subclass exposes a `last_processed_at: datetime | None` property reflecting the wall-clock UTC time of the most recently consumed message"
    - "service_auditor detects agents whose last-message timestamp is older than 360 seconds while systemd reports them active, and restarts them"
    - "The new stall detector does not collide with the in-process _stall_watchdog (which fires at 300s)"
  artifacts:
    - path: "src/core/agent/base.py"
      provides: "last_processed_at property + wall-clock setter in _record_message_consumed"
      contains: "last_processed_at"
    - path: "services/service_auditor_agent.py"
      provides: "Prometheus-driven stall detection wired into _prometheus_check_loop"
      contains: "_fetch_stalled_agents"
  key_links:
    - from: "BaseAgent._record_message_consumed"
      to: "self._last_processed_at = datetime.now(UTC)"
      via: "wall-clock assignment alongside monotonic _last_message_ts"
      pattern: "self\\._last_processed_at = datetime\\.now\\(UTC\\)"
    - from: "service_auditor _prometheus_check_loop"
      to: "_fetch_stalled_agents -> _restart_unit"
      via: "query agent_last_message_timestamp_seconds, compare to time.time()"
      pattern: "agent_last_message_timestamp_seconds"
---

<objective>
Expose `last_processed_at` on `BaseAgent` and use the existing `agent_last_message_timestamp_seconds` metric in `service_auditor_agent` to detect stalled agents and restart them (OBS-03).

Purpose: The in-process `_stall_watchdog` exits the process at 300s, but external observation must also detect stalls authoritatively for the system health endpoint and for cases where the in-process watchdog has been disabled or has itself stalled.
Output: Public property on BaseAgent + Prometheus-driven stall detection in service_auditor.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/086-pipeline-hardening/086-RESEARCH.md
@.planning/phases/086-pipeline-hardening/086-PATTERNS.md
@src/observability/metrics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add last_processed_at property to BaseAgent</name>
  <files>src/core/agent/base.py</files>
  <read_first>
    - src/core/agent/base.py (current imports at top of file, __init__ around line 116 where `_last_message_ts` is initialized, `_record_message_consumed` at lines 295-304, `_stall_watchdog` for cross-reference)
  </read_first>
  <action>
    Modify `src/core/agent/base.py`:

    1. Ensure `from datetime import UTC, datetime` is present at the top of the file (add to existing import block if missing).
    2. In `BaseAgent.__init__`, after the existing `self._last_message_ts: float | None = None` (around line 116), add: `self._last_processed_at: datetime | None = None`.
    3. Add a public property method on `BaseAgent` (place it adjacent to `_record_message_consumed`):
       ```
       @property
       def last_processed_at(self) -> datetime | None:
           """Wall-clock UTC datetime of last successfully processed message."""
           return self._last_processed_at
       ```
    4. Modify `_record_message_consumed(self) -> None` to set the wall-clock alongside the monotonic timestamp. Order:
       - `self._last_message_ts = time.monotonic()`
       - `self._last_processed_at = datetime.now(UTC)`
       - `AGENT_LAST_MESSAGE_TIMESTAMP_SECONDS.set(time.time(), self._last_msg_ts_attrs)`

    Do NOT touch `_stall_watchdog`. Do NOT change the type or semantics of `_last_message_ts` (monotonic float used by watchdog).
    Structlog kwargs only; never `event=`.
  </action>
  <verify>
    grep -n "from datetime import UTC, datetime" src/core/agent/base.py
    grep -n "self._last_processed_at" src/core/agent/base.py
    grep -n "def last_processed_at" src/core/agent/base.py
    .venv/bin/ruff check src/core/agent/base.py
    .venv/bin/pytest tests/unit/ -k "base_agent or agent_base" -q
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/core/agent/base.py` contains `from datetime import UTC, datetime`
    - Source assertion: `__init__` contains `self._last_processed_at: datetime | None = None`
    - Source assertion: `_record_message_consumed` contains `self._last_processed_at = datetime.now(UTC)` ordered between the monotonic assignment and the `AGENT_LAST_MESSAGE_TIMESTAMP_SECONDS.set(...)` call
    - Source assertion: a `@property` named `last_processed_at` exists with return type `datetime | None`
    - Source assertion: `_last_message_ts` is still a monotonic float (untouched)
    - CLI: `.venv/bin/ruff check src/core/agent/base.py` exits 0
    - CLI: `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
  <done>
    `last_processed_at` property exposed; wall-clock setter wired without disturbing monotonic stall watchdog; ruff and unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Prometheus-driven stall detection to service_auditor</name>
  <files>services/service_auditor_agent.py</files>
  <read_first>
    - services/service_auditor_agent.py (existing `_query_prometheus` helper around lines 598-608, `_fetch_prometheus_lag` at lines 556-563, `_prometheus_check_loop` around line 296, restart helper — locate `_restart_unit` or `_trigger_restart`, and the `_AGENT_ID_TO_UNIT` mapping at top of file)
  </read_first>
  <action>
    Modify `services/service_auditor_agent.py`:

    1. Add module-level constant `_STALL_THRESHOLD_SECONDS: int = 360` (300s in-process watchdog + 60s grace) near the existing `_LAG_THRESHOLDS` definition.
    2. Add async method `_fetch_stalled_agents(self) -> list[str]` modeled directly on `_fetch_prometheus_lag` (lines 556-563):
       - Call `results = await self._query_prometheus("agent_last_message_timestamp_seconds")`.
       - Compute `now = time.time()`.
       - For each result, extract `ts = float(r["value"][1])` and `agent_id = r["metric"].get("agent_id") or r["metric"].get("agent") or ""` (try both label keys defensively — verify which is emitted by `_last_msg_ts_attrs`).
       - Map `agent_id` to its systemd unit via `_AGENT_ID_TO_UNIT.get(agent_id)`. Skip if unit is None.
       - If `now - ts > _STALL_THRESHOLD_SECONDS`, append the unit name to `stalled`.
       - Return the deduplicated list.
    3. In `_prometheus_check_loop` (around line 296), after the existing lag check, call `stalled = await self._fetch_stalled_agents()`. For each stalled unit, call the existing restart helper (use the same helper invoked when lag thresholds are exceeded — confirm whether it is `_restart_unit(unit)` or `_trigger_restart(unit)` by reading the file). Log `self.logger.warning("service_auditor.stall_detected", unit=unit, threshold_seconds=_STALL_THRESHOLD_SECONDS)` before requesting restart.
    4. Guard against false positives during cold start: if the unit was started less than `_STALL_THRESHOLD_SECONDS` ago, skip restart. Use whatever uptime check already exists in the file (`systemctl show <unit> --property=ActiveEnterTimestamp` pattern, if present). If no such check exists, instead skip restart when the metric series has fewer than 1 sample or when the agent restart counter has incremented within the last threshold window — pick the simpler available mechanism and document the choice in the summary.
    5. Verify label key: read `BaseAgent._last_msg_ts_attrs` initialization to determine whether the OTel attribute key is `agent_id` or `agent`. Use the same key consistently in step 2.

    Structlog kwargs only; never `event=`. Use `time` module (already imported).
  </action>
  <verify>
    grep -n "_STALL_THRESHOLD_SECONDS" services/service_auditor_agent.py
    grep -n "_fetch_stalled_agents" services/service_auditor_agent.py
    grep -n "service_auditor.stall_detected" services/service_auditor_agent.py
    grep -n "agent_last_message_timestamp_seconds" services/service_auditor_agent.py
    .venv/bin/ruff check services/service_auditor_agent.py
    .venv/bin/pytest tests/unit/ -k "service_auditor" -q
  </verify>
  <acceptance_criteria>
    - Source assertion: `services/service_auditor_agent.py` defines module-level `_STALL_THRESHOLD_SECONDS: int = 360`
    - Source assertion: an `async def _fetch_stalled_agents(self) -> list[str]:` method exists and calls `self._query_prometheus("agent_last_message_timestamp_seconds")`
    - Source assertion: `_prometheus_check_loop` invokes `_fetch_stalled_agents()` and, for each returned unit, calls the existing restart helper
    - Source assertion: a `service_auditor.stall_detected` warning log is emitted with `unit` and `threshold_seconds` kwargs
    - Source assertion: the label key used to extract the agent identifier matches the key actually emitted by `BaseAgent._last_msg_ts_attrs` (verify by reading base.py)
    - CLI: `.venv/bin/ruff check services/service_auditor_agent.py` exits 0
    - CLI: `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
  <done>
    Prometheus-driven stall detection wired into _prometheus_check_loop; threshold 360s; restart helper called per stalled unit; ruff and tests clean.
  </done>
</task>

</tasks>

<verification>
- `BaseAgent` exposes a public `last_processed_at` property returning `datetime | None`
- Wall-clock setter coexists with monotonic `_last_message_ts` (does not replace it)
- `service_auditor_agent` queries `agent_last_message_timestamp_seconds` with the correct label key
- Stall threshold is 360s (300s watchdog + 60s grace) — does not race with `_stall_watchdog`
- Restart helper invocation uses the existing helper, not a new path
</verification>

<success_criteria>
- OBS-03: `BaseAgent.last_processed_at` property exists; service_auditor queries Prometheus and restarts stalled units
- `.venv/bin/ruff check src/core/agent/base.py services/service_auditor_agent.py` exits 0
- `.venv/bin/pytest tests/unit/ -q` exits 0
</success_criteria>

<output>
Create `.planning/phases/086-pipeline-hardening/086-03-SUMMARY.md` documenting: (a) confirmed OTel label key for `agent_last_message_timestamp_seconds` (`agent_id` vs `agent`), (b) chosen cold-start false-positive guard, and (c) recommended follow-up integration test that mocks Prometheus and asserts restart is requested when ts is stale.
</output>
