---
phase: 107.5-signal-lifecycle-architecture-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 107.5-01
files_modified:
  - services/signal_writer_agent.py
  - production/migrations/096_signal_ledger_zone_fields.sql
autonomous: true
requirements:
  - LIFECYCLE-FIX-02

must_haves:
  truths:
    - "New signals written after this deploy have non-NULL entry_zone_low and entry_zone_high in signal_ledger"
    - "signal_ledger schema contains entry_zone_low and entry_zone_high columns (they already exist; migration is a no-op DDL safety check)"
  artifacts:
    - path: "services/signal_writer_agent.py"
      provides: "Zone field fix — reads zone_low/zone_high from signal dict"
      contains: "sig.get(\"zone_low\")"
    - path: "production/migrations/096_signal_ledger_zone_fields.sql"
      provides: "Schema safety check — confirms columns exist (idempotent ADD COLUMN IF NOT EXISTS)"
  key_links:
    - from: "services/signal_writer_agent.py::_build_entries"
      to: "LedgerEntry.entry_zone_low / entry_zone_high"
      via: "sig.get(\"zone_low\") and sig.get(\"zone_high\")"
      pattern: "sig\\.get\\(.zone_low.\\)"
---

<objective>
Fix the field name mismatch in `SignalWriterAgent._build_entries` and add a migration DDL safety check.

Purpose: `signal_writer_agent.py` lines 207-208 read `sig.get("entry_zone_low")` and `sig.get("entry_zone_high")`, but the signal dict produced by `make_signal_from_frame()` in `signal_schema.py` writes these values under `"zone_low"` and `"zone_high"` (lines 223-224). This mismatch means all 958k signals have NULL zone fields, causing the replay auditor to rely on a LATERAL JOIN to reconstruct them.

Output: Two files — the writer fix (two lines changed) and a migration DDL confirming the columns exist.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@docs/plans/2026-05-27-signal-lifecycle-architecture.md
@.planning/phases/107.5-signal-lifecycle-architecture-fix/107.5-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix zone field names in signal_writer_agent.py</name>
  <read_first>
    - services/signal_writer_agent.py (lines 180-216 — _build_entries method)
    - src/intelligence/trading/signal_schema.py (lines 210-231 — make_signal_from_frame zone field assignments)
    - src/persistence/repository/signal_ledger_repository.py (lines 55-155 — LedgerEntry dataclass and INSERT SQL)
  </read_first>
  <files>services/signal_writer_agent.py</files>
  <action>
    In `_build_entries` (lines 207-208), change:

      entry_zone_low=sig.get("entry_zone_low"),
      entry_zone_high=sig.get("entry_zone_high"),

    to:

      entry_zone_low=sig.get("zone_low"),
      entry_zone_high=sig.get("zone_high"),

    No other changes to this method. The LedgerEntry dataclass fields are already named `entry_zone_low` and `entry_zone_high` — only the dict key being read changes.

    Verify the source: `signal_schema.py` line 223 sets `sig["zone_low"]` and line 224 sets `sig["zone_high"]` — confirm these are the correct keys before committing.
  </action>
  <verify>
    grep -n "zone_low\|zone_high" services/signal_writer_agent.py
    Expected output shows:
      entry_zone_low=sig.get("zone_low"),
      entry_zone_high=sig.get("zone_high"),

    grep -n "zone_low\|zone_high" src/intelligence/trading/signal_schema.py | head -5
    Expected: sig["zone_low"] and sig["zone_high"] assignments at lines 223-224.

    .venv/bin/pytest tests/unit/ -q -k "signal_writer" 2>/dev/null | tail -5
  </verify>
  <done>
    - `sig.get("zone_low")` and `sig.get("zone_high")` appear in _build_entries
    - No `sig.get("entry_zone_low")` or `sig.get("entry_zone_high")` remains in that method
    - Unit tests pass or no signal_writer unit tests exist
  </done>
</task>

<task type="auto">
  <name>Task 2: Write migration 096 — DDL safety check for zone columns</name>
  <read_first>
    - production/migrations/095_signal_ledger_split.sql (lines 14-53 — signal_ledger CREATE TABLE; confirm entry_zone_low/entry_zone_high are present)
    - production/migrations/089_hardening.sql (for migration file style reference)
  </read_first>
  <files>production/migrations/096_signal_ledger_zone_fields.sql</files>
  <action>
    Create `production/migrations/096_signal_ledger_zone_fields.sql` with the following DDL safety block:

    -- Migration 096: confirm entry_zone_low / entry_zone_high columns exist on signal_ledger
    -- These columns were added in migration 095. This migration is a no-op safety check.
    -- Run before deploying the signal_writer_agent zone field fix (Plan 107.5-02).
    --
    -- DDL only — no DML. Backfill of historical NULLs is in 107.5-03-PLAN.md (separate DML script).

    BEGIN;

    ALTER TABLE signal_ledger
        ADD COLUMN IF NOT EXISTS entry_zone_low  numeric,
        ADD COLUMN IF NOT EXISTS entry_zone_high numeric;

    COMMIT;

    The migration must be idempotent (ADD COLUMN IF NOT EXISTS). Since 095 already added these columns, the migration will be a no-op on a current schema, but protects against any environment where 095 was skipped.
  </action>
  <verify>
    cat production/migrations/096_signal_ledger_zone_fields.sql
    Expected: ADD COLUMN IF NOT EXISTS for entry_zone_low and entry_zone_high.

    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d signal_ledger" | grep "entry_zone"
    Expected: both entry_zone_low and entry_zone_high appear as numeric columns.
  </verify>
  <done>
    - production/migrations/096_signal_ledger_zone_fields.sql exists
    - Contains idempotent ADD COLUMN IF NOT EXISTS for both zone columns
    - psql confirms both columns exist in signal_ledger
  </done>
</task>

</tasks>

<verification>
After deploying signal_writer_agent:
  sudo systemctl restart indicagent-signal-writer
  sleep 5 && sudo systemctl is-active indicagent-signal-writer

After one full bar cycle (wait ~2 minutes for a new signal), verify new signals have zone fields:
  PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "
    SELECT signal_id, entry_zone_low, entry_zone_high, timestamp
    FROM signal_ledger
    WHERE timestamp > NOW() - INTERVAL '5 minutes'
    ORDER BY timestamp DESC LIMIT 5;"

Expected: entry_zone_low and entry_zone_high are non-NULL for signals emitted after the deploy.
</verification>

<success_criteria>
- `sig.get("zone_low")` and `sig.get("zone_high")` are the dict accessors in _build_entries
- Migration 096 applied successfully (idempotent — no error if columns already exist)
- New signals after deploy have non-NULL entry_zone_low and entry_zone_high in signal_ledger
</success_criteria>

<output>
After completion, create `.planning/phases/107.5-signal-lifecycle-architecture-fix/107.5-02-SUMMARY.md`
</output>
