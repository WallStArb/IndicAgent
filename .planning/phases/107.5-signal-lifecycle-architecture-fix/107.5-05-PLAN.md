---
phase: 107.5-signal-lifecycle-architecture-fix
plan: 05
type: execute
wave: 5
depends_on:
  - 107.5-04
files_modified:
  - services/signal_writer_agent.py
  - src/intelligence/trading/lifecycle_tracker.py
  - services/signal_tracker_compute_agent.py
  - src/persistence/repository/signal_ledger_repository.py
autonomous: true
requirements:
  - LIFECYCLE-FIX-03

must_haves:
  truths:
    - "signal_writer_agent writes expires_at to signal_ledger at signal creation using tf_to_seconds()"
    - "lifecycle_tracker.evaluate_signal checks bar_ts >= signal expires_at (wall-clock) instead of bars_elapsed >= ttl_bars (bar-count)"
    - "signal_tracker_compute_agent uses expires_at from the signal record for TTL evaluation; raises an error metric for NULL expires_at post-backfill"
    - "Both evaluators switch in a single deployment — no window where they use different TTL models"
    - "LedgerEntry dataclass and INSERT SQL include the expires_at column"
  artifacts:
    - path: "services/signal_writer_agent.py"
      provides: "expires_at written at signal creation"
      contains: "expires_at"
    - path: "src/intelligence/trading/lifecycle_tracker.py"
      provides: "Wall-clock TTL check using expires_at"
      contains: "expires_at"
    - path: "services/signal_tracker_compute_agent.py"
      provides: "TTL evaluation via expires_at; error metric for NULL"
      contains: "expires_at"
    - path: "src/persistence/repository/signal_ledger_repository.py"
      provides: "LedgerEntry.expires_at field and INSERT SQL column"
      contains: "expires_at"
  key_links:
    - from: "services/signal_writer_agent.py::_build_entries"
      to: "signal_ledger.expires_at"
      via: "LedgerEntry(expires_at=bar_ts + timedelta(seconds=ttl_bars * tf_to_seconds(tf)))"
      pattern: "expires_at.*tf_to_seconds"
    - from: "src/intelligence/trading/lifecycle_tracker.py::evaluate_signal"
      to: "signal['expires_at']"
      via: "bar_time >= signal['expires_at'] as TTL check"
      pattern: "bar_time.*expires_at"
---

<objective>
Atomic deployment: write `expires_at` at signal creation in `signal_writer_agent.py`, update both evaluators (`lifecycle_tracker.py` and `signal_tracker_compute_agent.py`) to use wall-clock TTL, and wire `LedgerEntry` to carry the field.

Purpose: After Plan 07.5-04, `expires_at` is populated for all existing signals. This plan switches both TTL evaluators to use it in a single atomic deployment — no window where the two evaluators run different TTL models. NULL `expires_at` after deployment is an error metric, not a silent fallback.

Output: Four files modified together, deployed together as one systemd restart sequence.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@docs/plans/2026-05-27-signal-lifecycle-architecture.md
@.planning/phases/107.5-signal-lifecycle-architecture-fix/107.5-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire expires_at into LedgerEntry and signal_writer_agent</name>
  <read_first>
    - src/persistence/repository/signal_ledger_repository.py (lines 55-155 — LedgerEntry dataclass, _to_row tuple, _INSERT_SQL)
    - services/signal_writer_agent.py (lines 15-40 — imports; lines 155-216 — _build_entries)
    - src/core/service_utils.py (the tf_to_seconds function added in Plan 04)
    - production/migrations/097_signal_ledger_expires_at.sql (confirms column position)
  </read_first>
  <files>
    src/persistence/repository/signal_ledger_repository.py
    services/signal_writer_agent.py
  </files>
  <action>
    In `signal_ledger_repository.py`:

    1. Add `expires_at: datetime | None = None` field to `LedgerEntry` dataclass after `pipeline_lag_ms`.
       Import `datetime` is already present at the top of the file.

    2. Add `self.expires_at,  # $28` to `_to_row()` return tuple (append at end).

    3. Add `expires_at` to `_INSERT_SQL`:
       - In the column list: add `expires_at` after `pipeline_lag_ms`
       - In the VALUES list: add `$28` after `$27`
       The ON CONFLICT clause is unchanged.

    In `signal_writer_agent.py`:

    1. Add import: `from src.core.service_utils import parse_iso_ts, tf_to_seconds`
       (parse_iso_ts is already imported; just add tf_to_seconds to the existing import line)
       Also add: `from datetime import timedelta` (if not already imported)

    2. In `_build_entries`, compute expires_at before the LedgerEntry(...) call:
         ttl = sig.get("ttl_bars")
         expires_at_val = None
         if ttl is not None and tf:
             expires_at_val = bar_ts + timedelta(seconds=ttl * tf_to_seconds(tf))

    3. Add `expires_at=expires_at_val,` to the LedgerEntry constructor call.

    Do NOT change the INSERT SQL placeholder numbering for existing fields — only append $28.
  </action>
  <verify>
    grep -n "expires_at" src/persistence/repository/signal_ledger_repository.py
    Expected: field in dataclass, value in _to_row, column + placeholder in _INSERT_SQL.

    grep -n "expires_at\|tf_to_seconds\|timedelta" services/signal_writer_agent.py
    Expected: import of tf_to_seconds, timedelta usage, expires_at_val computation, expires_at= in LedgerEntry.

    .venv/bin/pytest tests/unit/ -q 2>/dev/null | tail -5
  </verify>
  <done>
    - LedgerEntry has expires_at: datetime | None = None
    - _to_row() has self.expires_at as $28
    - _INSERT_SQL has expires_at column and $28 placeholder
    - signal_writer_agent computes expires_at_val = bar_ts + timedelta(seconds=ttl * tf_to_seconds(tf))
    - Unit tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Switch TTL evaluation to wall-clock in lifecycle_tracker and signal_tracker</name>
  <read_first>
    - src/intelligence/trading/lifecycle_tracker.py (lines 177-350 — evaluate_signal full body; focus on TTL check)
    - services/signal_tracker_compute_agent.py (lines 388-420 — _ingest_signal; lines 454-500 — _publish_ttl_expired_transition_sync; look for bars_elapsed >= ttl_bars TTL checks)
    - src/observability/metrics.py (look for existing NULL-guard metric patterns)
  </read_first>
  <files>
    src/intelligence/trading/lifecycle_tracker.py
    services/signal_tracker_compute_agent.py
  </files>
  <action>
    In `lifecycle_tracker.py::evaluate_signal`:

    Locate the TTL check — currently `bars >= ttl` or `bars_elapsed >= ttl_bars` pattern in the function body.
    Replace the bar-count TTL check with a wall-clock check using expires_at:

      expires_at = signal.get("expires_at")
      if expires_at is not None:
          if bar_time is not None and bar_time >= expires_at:
              # Wall-clock TTL expired
              <existing ttl-expired transition logic>
      else:
          # NULL expires_at is a data integrity error post-backfill
          # Import and call the NULL-expires_at error counter (see metrics.py)
          # Fall back to bar-count check to avoid dropping the signal silently,
          # BUT record the error metric so the upstream bug surfaces.
          # D-17: surface as error metric, not silent fallback.
          _NULL_EXPIRES_AT_COUNTER.add(1, {"symbol": signal.get("symbol","unknown"), "timeframe": signal.get("timeframe","unknown")})
          # Use original bar-count fallback only to avoid silent drops
          if bars >= ttl:
              <existing ttl-expired transition logic>

    Add `_NULL_EXPIRES_AT_COUNTER` as a module-level OTel counter using `_counter("signal_tracker_null_expires_at_total", ...)`. Import `counter as _counter` from `src.observability.metrics` — already imported in this file.

    In `signal_tracker_compute_agent.py`:

    Update `_ingest_signal` fast-path (modified in Plan 01 to use bars_elapsed >= ttl_bars) to also check expires_at if present on the canonical dict:

      expires_at = canonical.get("expires_at")
      if expires_at is not None:
          if now_utc >= expires_at:
              self._publish_ttl_expired_transition_sync(canonical, bars_elapsed)
              ...
              return
      elif bars_elapsed >= canonical["ttl_bars"]:
          # NULL expires_at: use bar-count fallback, increment error counter
          _NULL_EXPIRES_AT_COUNTER.add(1, ...)
          self._publish_ttl_expired_transition_sync(canonical, bars_elapsed)
          ...
          return

    Import `tf_to_seconds` from `src.core.service_utils` (replace the inline TF_SECONDS.get(tf, 60) usage in _publish_ttl_expired_transition_sync with `tf_to_seconds(tf)` for consistency).

    The NULL fallback path MUST increment an error counter (D-17: fail loudly). Use the same `signal_tracker_null_expires_at_total` counter name for easy alerting.
  </action>
  <verify>
    grep -n "expires_at\|null_expires_at" src/intelligence/trading/lifecycle_tracker.py
    Expected: expires_at check in evaluate_signal body; _NULL_EXPIRES_AT_COUNTER definition and call.

    grep -n "expires_at\|tf_to_seconds" services/signal_tracker_compute_agent.py
    Expected: expires_at check in _ingest_signal; tf_to_seconds import and usage.

    .venv/bin/pytest tests/unit/ -q 2>/dev/null | tail -5
  </verify>
  <done>
    - evaluate_signal checks bar_time >= signal["expires_at"] for TTL
    - NULL expires_at increments signal_tracker_null_expires_at_total counter (not silent)
    - _ingest_signal checks now_utc >= expires_at (preferred) or bars_elapsed >= ttl_bars (fallback with counter)
    - tf_to_seconds used in place of TF_SECONDS.get(tf, 60) in _publish_ttl_expired_transition_sync
    - Unit tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Atomic deploy — restart signal_writer and signal_tracker_compute together</name>
  <read_first>
    - services/signal_writer_agent.py (confirm expires_at changes are in place)
    - services/signal_tracker_compute_agent.py (confirm expires_at changes are in place)
  </read_first>
  <files></files>
  <action>
    Deploy both services atomically to eliminate any window where they use different TTL models:

      sudo systemctl stop indicagent-signal-writer indicagent-signal-tracker-compute
      sudo systemctl start indicagent-signal-writer indicagent-signal-tracker-compute
      sleep 5
      sudo systemctl is-active indicagent-signal-writer indicagent-signal-tracker-compute

    Both must show "active" before proceeding.

    Verify the NULL counter is zero (no data integrity issues):
      curl -s http://localhost:9124/metrics 2>/dev/null | grep null_expires_at || echo "metric_not_found_yet"

    Check signal_writer log for expires_at writes:
      grep "signal_written\|expires_at" logs/signal_writer_agent.log | tail -10

    Check signal_tracker log for fast-path events:
      grep "backfill_ttl_fast_path\|null_expires_at" logs/signal_tracker_compute_agent.log | tail -10
  </action>
  <verify>
    sudo systemctl is-active indicagent-signal-writer indicagent-signal-tracker-compute
    Expected: both return "active"

    # Verify new signals have expires_at
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "
      SELECT signal_id, timestamp, expires_at, feature_tf, ttl_bars
      FROM signal_ledger
      WHERE timestamp > NOW() - INTERVAL '5 minutes'
      ORDER BY timestamp DESC LIMIT 3;"
    Expected: expires_at is non-NULL for newly written signals.

    # Verify null counter is zero (no data integrity issues after backfill)
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "
      SELECT COUNT(*) FROM signal_ledger WHERE expires_at IS NULL AND ttl_bars IS NOT NULL AND feature_tf IS NOT NULL;"
    Expected: 0
  </verify>
  <done>
    - Both services active after restart
    - New signals have non-NULL expires_at
    - signal_tracker_null_expires_at_total counter at zero (no fallback path firing)
    - No unexpected NULLs in signal_ledger
  </done>
</task>

</tasks>

<verification>
Post-deployment health check:
  sudo systemctl is-active indicagent-signal-writer indicagent-signal-tracker-compute
  grep -c "null_expires_at" logs/signal_tracker_compute_agent.log || echo "0 null errors"

Both evaluators now use wall-clock expires_at. A non-zero null_expires_at count is a pager-worthy data integrity alert.
</verification>

<success_criteria>
- LedgerEntry.expires_at field wired; INSERT SQL includes $28
- signal_writer_agent computes expires_at = bar_ts + timedelta(seconds=ttl * tf_to_seconds(tf))
- lifecycle_tracker uses bar_time >= expires_at for TTL; NULL increments error counter
- signal_tracker_compute uses expires_at in _ingest_signal fast-path; NULL increments same counter
- Both services restarted atomically and are active
- signal_tracker_null_expires_at_total counter = 0 in steady state
</success_criteria>

<output>
After completion, create `.planning/phases/107.5-signal-lifecycle-architecture-fix/107.5-05-SUMMARY.md`
</output>
