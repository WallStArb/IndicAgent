---
phase: 121-lifecycle-replay-validation
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [REPLAY-01]
files_modified:
  - production/scripts/lifecycle_replay.py
  - production/scripts/historical_backfill.py
  - production/scripts/phase_121_before_snapshot.py
  - docs/plans/phase-121-before-snapshot.json
autonomous: true

must_haves:
  truths:
    - "lifecycle_replay.py processes ALL pending signals with no hardcoded date window"
    - "historical_backfill.py --clean can be scoped to the 22 shadow setups without destroying GOOD control setups"
    - "A before-snapshot JSON baseline is captured atomically before any DELETE runs"
    - "The 22 shadow setups' old noise signals are deleted and regenerated with corrected plugin code"
    - "All pending signal outcomes are computed by lifecycle replay"
    - "Integrity gate hard-fails on stopped_at_entry in shadow signals or orphan ledger rows"
  artifacts:
    - path: "production/scripts/lifecycle_replay.py"
      provides: "Schema-aligned, date-window-free lifecycle replay with D-06 integrity gate"
      contains: "shadow_stopped_at_entry"
    - path: "production/scripts/historical_backfill.py"
      provides: "--setups flag for plugin-scoped --clean"
      contains: "--setups"
    - path: "production/scripts/phase_121_before_snapshot.py"
      provides: "Atomic per-setup before-snapshot capture"
      contains: "phase-121-before-snapshot.json"
    - path: "docs/plans/phase-121-before-snapshot.json"
      provides: "Authoritative before baseline for Wave 2"
      contains: "captured_at"
  key_links:
    - from: "historical_backfill.py --clean path"
      to: "_SHADOW_VALIDATION_SETUPS (22 setups)"
      via: "--setups argparse flag gating DELETE to setup_plugin = ANY(%s)"
      pattern: "setup_plugin = ANY"
    - from: "lifecycle_replay.py _verify_replay"
      to: "shadow signal integrity (stopped_at_entry == 0, no orphan ledger rows)"
      via: "removed is_shadow=false filter + D-06 hard-fail checks"
      pattern: "shadow_stopped_at_entry"
---

<objective>
Redesign the lifecycle replay infrastructure to match the current schema, add a plugin-scoped clean mode, capture an atomic before-snapshot, then execute the D-01 clean+regenerate+replay sequence for the 22 shadow setups and compute all pending outcomes.

Purpose: The signal ledger must reflect corrected reality. The 22 refactored setups (shadow mode since Phases 118-119) fired ~5.17M noise signals under broken plugin code. Those signals are deleted and regenerated with corrected code, then all pending outcomes are computed. This delivers a clean ledger and the before-baseline that Wave 2's comparison report depends on.

Output: Modified lifecycle_replay.py and historical_backfill.py, new phase_121_before_snapshot.py, captured before-snapshot JSON, regenerated shadow signals, computed outcomes, passing integrity gate.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-lifecycle-replay-validation/121-CONTEXT.md
@.planning/phases/121-lifecycle-replay-validation/121-RESEARCH.md
@.planning/phases/121-lifecycle-replay-validation/121-PATTERNS.md
</context>

<critical_corrections>
The planner verified these against live code — they OVERRIDE any conflicting text in CONTEXT.md:

1. **CONTEXT D-03 has a factual error.** It lists `trad_VWAPDeviation` as a GOOD control setup. It is NOT — `trad_VWAPDeviation` IS in `_SHADOW_VALIDATION_SETUPS` (services/shadow_validator.py line 72) and WILL be deleted+regenerated. The authoritative delete scope is the `_SHADOW_VALIDATION_SETUPS` frozenset (len == 22, asserted in code). NEVER hardcode a setup list — import the frozenset.

2. **Backfill integrity plan Tasks 1-5 are ALREADY DONE.** `_load_calibration_curves`, `_load_perf_weights`, and `_assert_backfill_integrity` already exist and are wired in historical_backfill.py. Do NOT re-implement them. The only backfill work in this phase is (a) adding the `--setups` flag and (b) running the operation.

3. **intelligence_features must NOT be deleted in the plugin-scoped clean.** That table has no `setup_plugin` column and its per-bar feature rows are shared across all 30 setups. Deleting it by symbol would destroy the 8 GOOD control setups' feature data. The plugin-scoped clean deletes ONLY from `signal_outcomes` and `signal_ledger`.

4. **Live counts as of 2026-06-10:** ~1.54M non-shadow pending signals; ~5.17M non-shadow rows under the 22 shadow setup names (the noise to delete+regenerate); ~16.6K live-accumulated shadow signals. The before-snapshot captures the true numbers; do not bake any count into code.
</critical_corrections>

<tasks>

<task type="auto">
  <name>Task 1: Add --setups plugin-scoped clean filter to historical_backfill.py</name>
  <files>production/scripts/historical_backfill.py</files>
  <read_first>
    - production/scripts/historical_backfill.py (lines 1731-1790 for argparse block; lines 2066-2116 for the --clean delete path — read the full --clean block to see the symbol_values construction and the three DELETE statements)
    - services/shadow_validator.py (lines 53-84 — the _SHADOW_VALIDATION_SETUPS frozenset and its len==22 assert)
    - .planning/phases/121-lifecycle-replay-validation/121-PATTERNS.md (historical_backfill.py section — psycopg2 %s placeholder pattern, plugin-scoped delete pattern)
  </read_first>
  <action>
    Add a `--setups` argparse argument (type=str, default=None) near the other parser.add_argument calls (around line 1780). Help text: comma-separated setup_plugin names to scope the --clean deletion; when provided only signal_ledger and signal_outcomes rows for these setups are deleted and intelligence_features is NOT deleted; default None means full-symbol clean.

    In the `if args.clean:` block (line 2067), branch on whether a setup filter is active. Resolve the setup list as follows: if `args.setups` is provided, parse it as a comma-separated list; otherwise default to the full 22-setup scope by importing `from services.shadow_validator import _SHADOW_VALIDATION_SETUPS` and using `list(_SHADOW_VALIDATION_SETUPS)`. Do NOT hardcode setup names in this file.

    When a setup filter is active (which is the default for this phase's run):
    - SKIP the `DELETE FROM intelligence_features WHERE symbol = ANY(%s)` statement entirely (line 2078). intelligence_features has no setup_plugin column and its rows are shared across all setups.
    - Scope the signal_outcomes delete to `WHERE signal_id IN (SELECT signal_id FROM signal_ledger WHERE symbol = ANY(%s) AND setup_plugin = ANY(%s))` (psycopg2 %s placeholders, params tuple `(symbol_values, setup_values)`).
    - Scope the signal_ledger delete to `WHERE symbol = ANY(%s) AND setup_plugin = ANY(%s)` with the same params.

    Preserve the existing full-symbol behavior path only if `args.setups` is explicitly the sentinel "ALL" (or document that the unfiltered full-clean is no longer the default). Log the resolved setup count and the symbols affected before executing the deletes. Use psycopg2 cur.execute (%s), NOT asyncpg ($1). This script stays psycopg2 — do not introduce asyncpg here.
  </action>
  <verify>
    - `.venv/bin/python -c "import ast; ast.parse(open('production/scripts/historical_backfill.py').read())"` exits 0
    - `grep -n "\-\-setups" production/scripts/historical_backfill.py` returns the argparse line
    - `grep -n "setup_plugin = ANY" production/scripts/historical_backfill.py` returns at least 2 matches (outcomes + ledger deletes)
    - `grep -n "_SHADOW_VALIDATION_SETUPS" production/scripts/historical_backfill.py` returns the import
    - `.venv/bin/ruff check production/scripts/historical_backfill.py` exits 0
  </verify>
  <acceptance_criteria>
    - historical_backfill.py argparse contains a `--setups` argument with default=None
    - The --clean path imports `_SHADOW_VALIDATION_SETUPS` from services.shadow_validator and uses it as the default scope (no hardcoded setup names)
    - When a setup filter is active, the intelligence_features DELETE is skipped (not executed)
    - The signal_outcomes and signal_ledger deletes both contain `setup_plugin = ANY(%s)` (psycopg2 placeholder)
    - `ast.parse` and `ruff check` both succeed on the file
  </acceptance_criteria>
  <done>historical_backfill.py --clean is plugin-scopeable; default scope is the 22 shadow setups; intelligence_features is never deleted under a setup filter; GOOD control setups are untouchable by the scoped clean.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign lifecycle_replay.py — remove date windows, add 14 schema columns, extend integrity gate</name>
  <files>production/scripts/lifecycle_replay.py</files>
  <read_first>
    - production/scripts/lifecycle_replay.py (read the whole file — specifically lines 105-131 _seed_orphan_outcomes, lines 362-520 _process_symbol_tf and its SELECT at 396, lines 1046-1110 _verify_replay, lines 1112-1210 main_async incl. argparse defaults at 1143/1149 and cutoff at 1199)
    - production/migrations/119_framing_audit_trail.sql (the 5 new signal_ledger columns: stop_basis, stop_type_col, structural_stop_distance_atr, adaptive_buffer_mult, plugin_regime_type)
    - production/migrations/112_update_signal_ledger_full_view.sql (authoritative signal_ledger_full column list)
    - services/shadow_validator.py (lines 53-84 — _SHADOW_VALIDATION_SETUPS for the integrity-gate setup scope)
    - .planning/phases/121-lifecycle-replay-validation/121-PATTERNS.md (lifecycle_replay.py section — _seed_orphan_outcomes replacement, SELECT columns to add, _verify_replay D-06 extensions, hardcoded date removals; note ZONE_CHUNK/MARKET_CHUNK/ACTIVATION_CHUNK and _REPLAY_LOCK_ID MUST NOT change)
  </read_first>
  <action>
    Four coordinated edits. Preserve ALL existing structure not named here — advisory lock pattern (_REPLAY_LOCK_ID = 20260602 unchanged), _flush_writes chunk sizes (ZONE_CHUNK=1500, MARKET_CHUNK=2000, ACTIVATION_CHUNK=4000 unchanged), the `SET timescaledb.max_tuples_decompressed_per_dml_transaction = 0` session setting, the try/finally advisory-lock release, and the flush_and_shutdown_metrics + JOB_COMPLETED_TOTAL oneshot exit.

    (a) Remove hardcoded date windows:
    - In `_seed_orphan_outcomes` (line 105): drop the `cutoff` parameter and remove both `AND sl.timestamp >= '2026-05-21'` and `AND sl.timestamp < $1` predicates. New signature `_seed_orphan_outcomes(conn, symbols, timeframes)`; query filters only on symbol/timeframe membership with `ON CONFLICT (signal_id) DO NOTHING`. Renumber the remaining $-placeholders ($2→$1, $3→$2).
    - At the call site (line 1200) remove the `cutoff = datetime(2026, 6, 2, ...)` assignment (line 1199) and call `_seed_orphan_outcomes(preflight_conn, symbols, timeframes)`.
    - Change the `--reset-before` default (line 1143) and `--reset-after` default (line 1149) from the hardcoded ISO strings to `default=None`. Update the help text on `--reset-after` to drop the "(default: 2026-05-21)" claim. These flags become optional overrides only; when None, no date bound is applied.

    (b) Add the 14 missing columns to the SELECT in `_process_symbol_tf` (line 396). From signal_ledger add: `sl.stop_basis, sl.stop_type_col, sl.structural_stop_distance_atr, sl.adaptive_buffer_mult, sl.plugin_regime_type`. From signal_outcomes add: `so.trailing_stop_price, so.staleness_score, so.staleness_trigger_reason, so.chandelier_vol_source, so.shadow_tracking_start_ts, so.shadow_mae, so.shadow_mfe, so.shadow_outcome, so.effective_ts`. `trailing_stop_price` is jsonb — asyncpg returns it as dict; do NOT json.loads it. These columns are added for schema completeness (prevents future SELECT breakage); do NOT thread them into evaluate_signal() — the evaluator recomputes staleness/chandelier state fresh from bar data.

    (c) Extend `_verify_replay` (line 1046) for D-06. Remove the blanket `AND sl.is_shadow = false` filter (line 1074) so shadow signals are included in integrity checks. Add a `setups` parameter carrying `list(_SHADOW_VALIDATION_SETUPS)`. Add two aggregate checks to the verify query: `shadow_stopped_at_entry` = COUNT of rows where `so.outcome = 'stopped_at_entry' AND sl.is_shadow = true AND sl.setup_plugin = ANY($3)`; and `orphan_ledger_rows` = COUNT of rows where the signal_ledger row has no matching signal_outcomes row. After fetching, append to the issues list and raise RuntimeError if `shadow_stopped_at_entry > 0` (Phase 117 fix must eliminate all stopped_at_entry for shadow setups) or `orphan_ledger_rows > 0` (Phase 104 invariant). Keep the existing stale-unresolved check. Pass the setups arg from the main_async call site.

    (d) Bump the module version docstring/comment from v1.2 to v1.3 and note the Phase 121 changes (no date window default, 14 new columns, shadow-inclusive integrity gate).
  </action>
  <verify>
    - `.venv/bin/python -c "import ast; ast.parse(open('production/scripts/lifecycle_replay.py').read())"` exits 0
    - `grep -c "2026-05-21\|2026, 6, 2\|2026-06-02T00\|2026-05-21T00" production/scripts/lifecycle_replay.py` returns 0
    - `grep -n "is_shadow = false" production/scripts/lifecycle_replay.py` returns nothing
    - `grep -n "shadow_stopped_at_entry\|orphan_ledger_rows" production/scripts/lifecycle_replay.py` returns matches
    - `grep -n "stop_basis\|trailing_stop_price\|shadow_outcome\|effective_ts" production/scripts/lifecycle_replay.py` returns the new SELECT columns
    - `grep -c "_REPLAY_LOCK_ID = 20260602\|ZONE_CHUNK = 1500\|MARKET_CHUNK = 2000\|ACTIVATION_CHUNK = 4000" production/scripts/lifecycle_replay.py` confirms preserved constants
    - `.venv/bin/python production/scripts/lifecycle_replay.py --help` exits 0 and shows `--reset-before`/`--reset-after` with no hardcoded date in their help
    - `.venv/bin/ruff check production/scripts/lifecycle_replay.py` exits 0
  </verify>
  <acceptance_criteria>
    - Zero matches for any of the four hardcoded date strings (2026-05-21, datetime(2026, 6, 2), 2026-06-02T00, 2026-05-21T00) in the file
    - `_seed_orphan_outcomes` signature is `(conn, symbols, timeframes)` with no cutoff/date predicates
    - SELECT in `_process_symbol_tf` contains all 5 signal_ledger and all 9 signal_outcomes new columns
    - `_verify_replay` has no `is_shadow = false` filter and raises RuntimeError when shadow_stopped_at_entry>0 or orphan_ledger_rows>0
    - `_REPLAY_LOCK_ID = 20260602`, `ZONE_CHUNK = 1500`, `MARKET_CHUNK = 2000`, `ACTIVATION_CHUNK = 4000` all still present unchanged
    - `lifecycle_replay.py --help` exits 0; ast.parse and ruff both succeed
  </acceptance_criteria>
  <done>lifecycle_replay.py (v1.3) processes all pending signals with no date window, SELECTs the full current schema, and hard-fails on shadow stopped_at_entry or orphan ledger rows while preserving all lock/chunk/oneshot invariants.</done>
</task>

<task type="auto">
  <name>Task 3: Create phase_121_before_snapshot.py and execute the full D-01 clean+replay sequence</name>
  <files>production/scripts/phase_121_before_snapshot.py, docs/plans/phase-121-before-snapshot.json</files>
  <read_first>
    - production/scripts/compute_ic.py (asyncpg oneshot analog — imports, DatabaseManager connection, stdout+file output, oneshot exit)
    - production/scripts/lifecycle_replay.py (post-Task-2 state — DatabaseManager usage, JOB_COMPLETED_TOTAL/flush_and_shutdown_metrics/init_otel_providers oneshot pattern, advisory lock _REPLAY_LOCK_ID)
    - services/shadow_validator.py (lines 53-84 — _SHADOW_VALIDATION_SETUPS for the --setups argument value)
    - .planning/phases/121-lifecycle-replay-validation/121-CONTEXT.md (D-01 strict execution sequence; D-04 exact before-snapshot SQL; specifics: snapshot inside the advisory-lock transaction, abort if snapshot file already exists)
    - .planning/phases/121-lifecycle-replay-validation/121-PATTERNS.md (phase_121_before_snapshot.py section — exact imports, _fetch_snapshot SQL, _write_snapshot with exists-guard, oneshot exit with job="phase-121-before-snapshot")
  </read_first>
  <action>
    First create `production/scripts/phase_121_before_snapshot.py` (asyncpg via DatabaseManager, NOT psycopg2). Module docstring: captures per-setup metrics before any deletes, writes the authoritative before baseline. Implement:
    - `_fetch_snapshot(conn)` running the exact D-04 SQL (signal_ledger LEFT JOIN signal_outcomes, GROUP BY setup_plugin, is_shadow, selecting total_signals, selected, selection_rate, avg_pnl_r, and `CORR(sl.cis_score, (so.pnl_r > 0)::int) FILTER (WHERE so.pnl_r IS NOT NULL)` as calibration_corr, ORDER BY total_signals DESC). Use the SQL verbatim from CONTEXT D-04.
    - Acquire `pg_try_advisory_lock(_REPLAY_LOCK_ID)` (import or redefine the 20260602 constant) and run the snapshot fetch inside that locked session for atomicity with the later delete; release the lock in a finally block.
    - `_write_snapshot(rows)` writing to `docs/plans/phase-121-before-snapshot.json` with keys `captured_at` (UTC ISO) and `setups` (list of dicts). Guard: if the file already exists, print an ABORT message and `raise SystemExit(1)` — never overwrite an existing baseline.
    - Oneshot exit: `JOB_COMPLETED_TOTAL.add(1, {"job": "phase-121-before-snapshot", "status": "success"})` on success and `"failure"` on exception; `init_otel_providers("phase-121-before-snapshot")` in main with OTelInitError guard; `flush_and_shutdown_metrics()` in finally.

    Then EXECUTE the D-01 sequence in strict order (run each, capture output, confirm success before the next):
    1. Run `.venv/bin/python production/scripts/phase_121_before_snapshot.py` — confirm `docs/plans/phase-121-before-snapshot.json` is written with all setups.
    2. Run `.venv/bin/python production/scripts/historical_backfill.py --replay-only --clean --setups "<the 22 _SHADOW_VALIDATION_SETUPS names, comma-separated>" --workers 8` (resolve the names from the frozenset; do not invent). This deletes the 22 setups' old signal_ledger/signal_outcomes rows and regenerates them from historical bars with corrected plugin code. intelligence_features is NOT touched.
    3. Run `.venv/bin/python production/scripts/lifecycle_replay.py --workers 8` (no --reset, no date flags — process ALL pending signals). Per RESEARCH open-question 2, do NOT pass --reset; remaining non-shadow resolved signals already have correct outcomes.
    4. The replay's `_verify_replay` integrity gate runs at the end — confirm it passes (no RuntimeError). If it raises, capture the violation, STOP, and record it in the SUMMARY for diagnosis — do not proceed.

    Record the before-snapshot row count, post-clean regenerated signal count, pending-outcomes-resolved count, and integrity-gate result in the plan SUMMARY. These numbers feed Wave 2.
  </action>
  <verify>
    - `.venv/bin/python -c "import ast; ast.parse(open('production/scripts/phase_121_before_snapshot.py').read())"` exits 0
    - `.venv/bin/ruff check production/scripts/phase_121_before_snapshot.py` exits 0
    - `test -f docs/plans/phase-121-before-snapshot.json && python -c "import json; d=json.load(open('docs/plans/phase-121-before-snapshot.json')); assert 'captured_at' in d and len(d['setups'])>0; print(len(d['setups']), 'setups')"` exits 0
    - After the backfill clean+regenerate: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM signal_ledger sl WHERE sl.setup_plugin = ANY(ARRAY(SELECT unnest FROM unnest(string_to_array('trad_OFIContinuation,trad_PatternCompletion', ',')))) AND sl.is_shadow = true;"` returns a non-error result (regenerated shadow signals exist)
    - After replay: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM signal_ledger sl JOIN signal_outcomes so ON sl.signal_id=so.signal_id WHERE so.outcome='stopped_at_entry' AND sl.is_shadow=true;"` returns 0
    - lifecycle_replay.py exits 0 (integrity gate passed, no RuntimeError)
  </verify>
  <acceptance_criteria>
    - phase_121_before_snapshot.py exists, parses, passes ruff, and refuses to overwrite an existing snapshot file
    - docs/plans/phase-121-before-snapshot.json exists with captured_at and a non-empty setups array
    - historical_backfill.py was run with --replay-only --clean --setups scoped to the 22 frozenset setups; intelligence_features rows were NOT deleted (GOOD control setups intact)
    - lifecycle_replay.py ran across all pending signals and exited 0 with the integrity gate passing
    - psql confirms 0 stopped_at_entry outcomes for shadow signals
    - The SUMMARY records before-count, regenerated-count, resolved-count, and integrity-gate PASS
  </acceptance_criteria>
  <done>Before-baseline captured atomically; 22 shadow setups deleted and regenerated with corrected code; all pending outcomes computed; integrity gate passed with zero stopped_at_entry and zero orphan ledger rows.</done>
</task>

</tasks>

<verification>
- historical_backfill.py and lifecycle_replay.py both pass ast.parse and ruff check
- No hardcoded Phase-121 dates remain in lifecycle_replay.py
- before-snapshot JSON exists and is non-empty
- 0 stopped_at_entry outcomes for shadow signals (psql)
- 0 signal_ledger rows without a signal_outcomes row (Phase 104 invariant, enforced by integrity gate)
- lifecycle_replay.py integrity gate (_verify_replay) passed without raising
</verification>

<success_criteria>
- The 22 `_SHADOW_VALIDATION_SETUPS` (and only those) had their old noise signals deleted and regenerated
- The 8 GOOD control setups and intelligence_features are untouched
- All ~1.54M+ pending signal outcomes are computed
- docs/plans/phase-121-before-snapshot.json is the authoritative before baseline for Wave 2
- Integrity gate hard-fails are all clear (stopped_at_entry=0, orphan_ledger_rows=0, no stale unresolved)
</success_criteria>

<output>
After completion, create `.planning/phases/121-lifecycle-replay-validation/121-01-SUMMARY.md` recording: code changes made, the exact --setups list used, before-snapshot setup count, regenerated signal count, pending-outcomes-resolved count, and the integrity-gate result (with measured numbers for Wave 2 to consume).
</output>
