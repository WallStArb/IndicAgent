---
phase: 121-lifecycle-replay-validation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/trading/confidence_utils.py
  - src/intelligence/context/macro_context.py
  - src/intelligence/CLAUDE.md
autonomous: true

must_haves:
  truths:
    - "Every I7 signal fired carries all 5 MacroContextPlugin fields (ftq_score, ftq_regime, yield_curve_slope, yield_curve_regime, corr_z) in its shadow features_snapshot dict"
    - "Macro fields are captured as None when unavailable — never substituted with 0.0"
    - "capture_signal_features() returns identical confidence values before and after the change (pure data capture, zero behavior change)"
    - "The symbol scope of frames['cross_asset'] (and therefore which symbols actually populate macro fields) is documented as a known, intentional constraint — not silently assumed to be all symbols"
  artifacts:
    - path: "src/intelligence/trading/confidence_utils.py"
      provides: "capture_signal_features() with 5 new macro shadow keys (22 keys total)"
      contains: "ftq_score"
    - path: "src/intelligence/context/macro_context.py"
      provides: "Corrected docstring reflecting the EQ_INDEX-gated frames['cross_asset'] source"
      contains: "feature_pipeline_executor"
  key_links:
    - from: "src/intelligence/context/macro_context.py (MacroContextPlugin.compute_full)"
      to: "src/intelligence/trading/confidence_utils.py (capture_signal_features)"
      via: "I4Context.model_dump() -> build_flat_features -> frames['features'] -> features.get('ftq_score')"
      pattern: "features\\.get\\(\"ftq_score\"\\)"
    - from: "services/intelligence_pipeline.py macro_topic cache (update_macro)"
      to: "frames['cross_asset'] in feature_pipeline_executor.py"
      via: "pre-existing wiring (121-03) — verified read-only in Task 1, not created here"
      pattern: "cross_asset\\.update\\(cache_snapshot\\.macro_data"
---

<objective>
Refine signal-capture compute BEFORE the Phase 126 clean lifecycle replay so every replayed signal is labeled with full macro regime context.

Item 1 (highest priority): add the 5 MacroContextPlugin fields (ftq_score, ftq_regime, yield_curve_slope, yield_curve_regime, corr_z) to `capture_signal_features()` so they land in each I7 signal's shadow `features_snapshot`. Pure data labeling — zero confidence modification, zero gating.

Item 2: verify the upstream macro data path actually delivers all 5 fields into `frames["cross_asset"]` at the point `MacroContextPlugin` reads it. Investigation has already surfaced a scope constraint (see Task 1) — Task 1 confirms it and corrects the plugin docstring so the constraint is explicit rather than misleading.

Purpose: An I7 signal fired without macro regime context is a wasted training sample. Once these fields are captured, every future signal AND every signal regenerated during the Phase 126 replay carries macro regime at fire time, enabling regime-segmented calibration (deferred Macro P2).

Output: capture_signal_features() emitting a 22-key shadow dict; an accurate MacroContextPlugin docstring; verified end-to-end macro data flow.

DAG invariants honored: no pipeline/plugin code touches the DB; MacroContextPlugin remains DB-ignorant; no new topic strings (uses existing `topic_macro_signals` / `topic_cross_asset` already wired). capture_signal_features() change is data capture only and does not alter any confidence value.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-lifecycle-replay-validation/121-CONTEXT.md

@src/intelligence/trading/confidence_utils.py
@src/intelligence/context/macro_context.py
@src/intelligence/schemas.py
@src/intelligence/pipeline/feature_pipeline_executor.py
@src/intelligence/pipeline/feature_flattening.py
@services/intelligence_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify macro data flow into frames["cross_asset"] and correct the MacroContextPlugin docstring</name>
  <files>src/intelligence/context/macro_context.py</files>
  <read_first>
    - src/intelligence/context/macro_context.py (the plugin being corrected; reads frames["cross_asset"])
    - src/intelligence/pipeline/feature_pipeline_executor.py lines 204-211 (the merge + EQ_INDEX gate)
    - src/intelligence/cross_asset_features.py lines 33-42 (resolve_eq_index_base — _EQ_INDEX_BASES = {ES, NQ, RTY, YM})
    - src/intelligence/pipeline/cache_manager.py lines 235-276 (update_cross_asset, update_macro, snapshot)
    - services/intelligence_pipeline.py lines 441-471 (cross_asset_topic vs macro_topic caching paths)
    - services/macro_analyzer.py lines 199-218 (_publish_macro_signal — payload shape, "timeframe": bar["tf"])
    - services/cross_asset_analyzer.py lines 438-467 (corr_z computed and placed in cross_asset payload)
    - src/intelligence/pipeline/feature_flattening.py lines 59-98 (build_flat_features — None-filtered I4 flatten)
  </read_first>
  <action>
    This is a read-only investigation that produces exactly one source edit: a corrected docstring on MacroContextPlugin. Do NOT change pipeline wiring, the executor, or the cache manager in this task.

    Trace and confirm the following chain, recording each confirmation in the SUMMARY:

    1. corr_z source: cross_asset_analyzer.py writes result["corr_z"] into the cross_asset payload published to topic_cross_asset. intelligence_pipeline.py routes that payload via _cache_mgr.update_cross_asset(tf, payload), so corr_z lands in cache_snapshot.cross_asset_data[tf].

    2. ftq/yield_curve source: macro_analyzer.py publishes {timeframe, yield_curve_slope, yield_curve_regime} and {timeframe, ftq_score, ftq_regime} to topic_macro_signals. intelligence_pipeline.py extracts only the keys yield_curve_slope, yield_curve_regime, ftq_score, ftq_regime (note: corr_z is NOT in the macro_topic extraction list) and routes via _cache_mgr.update_macro(tf, macro_fields), landing in cache_snapshot.macro_data[tf].

    3. The merge: feature_pipeline_executor.py builds frames["cross_asset"] = {**cross_asset_data.get(tf, {"ready": False})} then .update(macro_data.get(tf, {})). This merges BOTH sources into one dict, so MacroContextPlugin sees corr_z (from cross_asset_data) AND ftq/yield_curve (from macro_data) together. CONFIRM this merge produces all 5 fields in one dict for a tf where both producers have published.

    4. THE SCOPE CONSTRAINT (the load-bearing finding): the merge at feature_pipeline_executor.py:205 is gated by `if resolve_eq_index_base(symbol) is not None:`. resolve_eq_index_base returns non-None ONLY for full contract symbols whose base is in {ES, NQ, RTY, YM} (e.g. "ESM6"), and returns None for bare bases and all non-EQ_INDEX symbols. Therefore frames["cross_asset"] is populated ONLY for EQ_INDEX contract symbols. For every other symbol, frames.get("cross_asset") is absent/empty, MacroContextPlugin.compute_full returns {} (its `if not xa.get("ready"): return {}` guard fires), and all 5 macro fields are None for that symbol.

    5. The macro_analyzer / cross_asset_analyzer producer scope: confirm whether these analyzers publish for symbols outside EQ_INDEX. If they only ever publish EQ_INDEX-derived macro signals, then the EQ_INDEX gate is harmless (no data is being dropped). If they publish instrument-agnostic macro signals (ftq/yield-curve are macro-wide, not per-instrument), then the EQ_INDEX gate is SUPPRESSING macro context for non-EQ_INDEX symbols and that is a wiring defect to record (do NOT fix here — record it as a finding and, if it is a defect, write a new todo in .planning/todos/pending/ describing the ungated-macro-frame fix).

    6. Reconcile against the MacroContextPlugin docstring, which currently claims: "Applies to ALL symbols — no EQ_INDEX guard needed (macro context is instrument-agnostic)." This claim is contradicted by the executor gate at line 205. Correct the docstring (module docstring lines 1-10 AND the class docstring lines 28-34) to state the actual behavior: the plugin itself has no symbol guard, but its only data source (frames["cross_asset"]) is populated by FeaturePipelineService ONLY for EQ_INDEX contract symbols (resolve_eq_index_base is not None), so in practice macro fields are populated only for ES/NQ/RTY/YM contracts. Reference feature_pipeline_executor.py:205 by name in the docstring so the next reader can find the gate. Do not invent behavior — describe exactly what the code does.

    Do not modify compute_full logic, outputs, or any other file. The only edit is the docstring text.
  </action>
  <verify>
    - `.venv/bin/ruff check src/intelligence/context/macro_context.py` passes.
    - `grep -n "feature_pipeline_executor" src/intelligence/context/macro_context.py` returns the docstring reference.
    - `grep -n "ALL symbols" src/intelligence/context/macro_context.py` returns NOTHING (the false claim is removed).
    - `grep -n "resolve_eq_index_base\|EQ_INDEX" src/intelligence/context/macro_context.py` returns the corrected scope note.
    - SUMMARY contains an explicit verdict on step 5: either "macro producers are EQ_INDEX-scoped, gate is harmless" OR "macro producers are instrument-agnostic, EQ_INDEX gate suppresses data — todo NNN written".
  </verify>
  <done>
    The end-to-end macro data path (producer -> topic -> cache -> merge -> frames["cross_asset"] -> MacroContextPlugin) is traced and documented in the SUMMARY, the EQ_INDEX scope constraint is confirmed, the plugin docstring states the true symbol scope and references the executor gate line, and any genuine suppression defect is captured as a pending todo (not fixed in this plan).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the 5 macro fields to capture_signal_features() shadow dict</name>
  <files>src/intelligence/trading/confidence_utils.py, src/intelligence/CLAUDE.md</files>
  <read_first>
    - src/intelligence/trading/confidence_utils.py (the file being modified; module docstring lines 11-16, capture_signal_features lines 122-203, existing 4-field block lines 153-158)
    - src/intelligence/context/macro_context.py (the 5 field names and types: ftq_score float|None, ftq_regime str|None, yield_curve_slope float|None, yield_curve_regime str|None, corr_z float|None)
    - src/intelligence/schemas.py lines 482-501 (I4Context field types for the 5 macro fields)
    - src/intelligence/pipeline/feature_flattening.py lines 86-91 (confirms I4 fields flatten into features with None values FILTERED OUT — so features.get(key) returns None when the field is absent)
    - .planning/phases/121-lifecycle-replay-validation/121-CONTEXT.md D-06 (None means data unavailable; never substitute 0.0)
  </read_first>
  <action>
    Depends on Task 1's verdict only for the SUMMARY note about which symbols will actually carry non-None values — the code change below is identical regardless.

    In src/intelligence/trading/confidence_utils.py, extend the existing I4 macro context block inside capture_signal_features() (the block currently at lines 153-158 that captures vix_level, vix_z, eq_spread_z, eq_pairs_confirming).

    Add these 5 keys to the shadow dict using EXACTLY the same accessor pattern as the existing 4 macro fields — `features.get("<field>")` with NO default argument (which yields None when absent). Do NOT pass 0.0 as a default for any of them. Per D-06, None means "data unavailable" and a substituted 0.0 would be indistinguishable from a real z-score of 0.0, corrupting training data. Match the existing inline comments style (`# float | None` / `# str | None`):
    - "ftq_score": features.get("ftq_score")  -> float | None
    - "ftq_regime": features.get("ftq_regime")  -> str | None
    - "yield_curve_slope": features.get("yield_curve_slope")  -> float | None
    - "yield_curve_regime": features.get("yield_curve_regime")  -> str | None
    - "corr_z": features.get("corr_z")  -> float | None

    Do NOT coerce ftq_regime/yield_curve_regime to float and do NOT wrap the floats in float() — keep them as raw `.get()` values exactly like vix_level/vix_z above them (those are stored unwrapped). MacroContextPlugin already coerces ftq_score/yield_curve_slope/corr_z to float|None and the regimes to str|None upstream, so values arriving in `features` are already the correct type. This preserves the "pure capture, zero transformation" contract.

    Place the new block immediately after the existing eq_pairs_confirming line, under a comment that updates the provenance, e.g. `# I4 macro regime context (Phase 121 Wave 2 / D-10 MacroContextPlugin): flight-to-quality, yield curve, stock-bond corr.` Keep the existing `# Per D-06: None means data unavailable` rationale comment so it covers both the old and new macro fields.

    Update the key-count documentation in three places so it stays truthful:
    1. Module docstring (lines 11-16): change "Shadow dict has 17 keys" to "22 keys", and update the macro count from "4 I4 macro context (vix_level, vix_z, eq_spread_z, eq_pairs_confirming)" to "9 I4 macro context (vix_level, vix_z, eq_spread_z, eq_pairs_confirming, ftq_score, ftq_regime, yield_curve_slope, yield_curve_regime, corr_z)".
    2. Function docstring Returns line (lines 141-143): change "Shadow dict with 17 keys: 4 I4 macro context ..." to "Shadow dict with 22 keys: 9 I4 macro context ...".
    3. src/intelligence/CLAUDE.md confidence_utils.py table row: it currently reads "writes `_shadow` dict (15 keys: 2 metadata, 6 I6 CTF, 4 I4 macro, 3 exhaustion)". Update the macro count to 9 and the total to reflect the 5 added fields. Match the actual structure: 2 metadata + I6 CTF + 9 I4 macro + 3 exhaustion. (The pre-existing CLAUDE.md "15 keys" figure is stale relative to the docstring's "17"; set CLAUDE.md to the same total the docstring now states — 22 — and the macro count to 9, so all three docs agree.)

    CRITICAL CONSTRAINT: this task adds keys to a returned dict only. It must NOT touch existing_confidence, compose_confidence(), any clamp, or any confidence-bearing field. The function's confidence-related output must be byte-identical to before.
  </action>
  <verify>
    - `.venv/bin/ruff check src/intelligence/trading/confidence_utils.py` passes; `.venv/bin/black --check src/intelligence/trading/confidence_utils.py` passes.
    - `grep -c "features.get(\"ftq_score\")\|features.get(\"ftq_regime\")\|features.get(\"yield_curve_slope\")\|features.get(\"yield_curve_regime\")\|features.get(\"corr_z\")" src/intelligence/trading/confidence_utils.py` returns 5.
    - `grep -n "0.0" src/intelligence/trading/confidence_utils.py` shows NO 0.0 default on any of the 5 new lines (confirm by inspection that ftq_score/corr_z/yield_curve_slope use bare `.get()`).
    - `grep -n "22 keys" src/intelligence/trading/confidence_utils.py` returns the module docstring AND function docstring lines.
    - `grep -n "9 I4 macro\|9 macro" src/intelligence/CLAUDE.md` returns the updated table row.
    - Behavior-equivalence check: write a throwaway snippet (or add to an existing unit test) calling capture_signal_features with a features dict containing the 4 old macro fields and none of the 5 new ones; assert the returned dict has the 5 new keys all equal to None AND that existing_confidence-derived output is unchanged. Run `.venv/bin/pytest tests/unit/ -q -k confidence` (or the relevant capture test) green.
    - `.venv/bin/pytest tests/unit/ -q` is green (no regressions).
  </verify>
  <done>
    capture_signal_features() returns a 22-key shadow dict that includes ftq_score, ftq_regime, yield_curve_slope, yield_curve_regime, and corr_z captured with None-on-absent semantics (no 0.0 substitution); the key-count documentation in confidence_utils.py (module + function docstrings) and src/intelligence/CLAUDE.md all agree at 22 keys / 9 macro; and the confidence value produced by the function is provably unchanged.
  </done>
</task>

</tasks>

<verification>
- `.venv/bin/ruff check src/ && .venv/bin/black --check src/intelligence/trading/confidence_utils.py src/intelligence/context/macro_context.py` clean.
- `.venv/bin/pytest tests/unit/ -q` green.
- A signal fired through an I7 plugin on an EQ_INDEX contract (e.g. ESM6) with macro producers running carries non-None values for at least one of the 5 new fields in its features_snapshot; a signal on a non-EQ_INDEX symbol carries them as None (consistent with the documented scope constraint from Task 1).
- No confidence value emitted by any I7 plugin changed as a result of this plan (capture_signal_features is data-capture only).
</verification>

<success_criteria>
- Every I7 signal's shadow features_snapshot now contains all 5 MacroContextPlugin fields, labeled None when the upstream macro data is unavailable for that symbol/bar.
- The macro data path is verified end-to-end and the EQ_INDEX scope constraint is documented in both the MacroContextPlugin docstring and the SUMMARY; any genuine data-suppression defect is captured as a pending todo for a future phase.
- capture_signal_features() makes zero behavior change to confidence (pure data capture).
- The Phase 126 clean replay will regenerate signals carrying full macro regime context, enabling future regime-segmented calibration.
</success_criteria>

<deferred>
The following were explicitly deferred during phase design — they are NOT tasks in this plan and must not be implemented here.

- **Item 3 — I7 macro regime gates (shadow-only):** Gating I7 signals on macro regime (yield_curve_regime / ftq_regime) requires calibration data (n >= 100 per regime bucket) before any gate is wired. No outcome-labeled, regime-segmented data exists yet. Belongs in a dedicated phase AFTER the Phase 126 replay generates outcome data. This is the same constraint as Macro P2 in 121-CONTEXT.md's Deferred Ideas.

- **Item 4 — I6 confluence macro weighting:** Weighting ctf_score by macro regime needs the same empirical, regime-segmented outcome data as Item 3. Deferred to the same future phase.

- **Item 5 — SR strength calibration (todo 019):** DEFERRED. todo 019 is calibration-data-dependent by its own gate: it requires `n >= 500` signals with an SR-plugin-sourced confluence score in signal_ledger before any regression can be fit, and explicitly mandates a 30-day shadow-mode side-by-side run before replacing the v0 `default_strength` placeholders in zone_engine.py. None of this is a pre-replay compute refinement — it consumes the very outcome data the Phase 126 replay produces. It must run AFTER the replay, not before. Left in .planning/todos/pending/019-sr-strength-calibration.md unchanged.

- **Item 6 — the 8 `_I7_I6_EXEMPT` setups:** prev_day_level_test, anchored_vwap_reversion, mean_reversion, hvn_rejection, poc_rejection, cross_asset_divergence, regime_transition, squeeze_expansion. These retain `requires_i6_confluence = False` deliberately. They are the GOOD control group: their logic is sound and they serve as the pre-refactor baseline for the Phase 126 before/after comparison report (121-02 spec, now in Phase 126). Refactoring them now would destroy the control group and invalidate the comparison. DO NOT touch them in this plan or before the Phase 126 report runs.
</deferred>

<output>
After completion, create `.planning/phases/121-lifecycle-replay-validation/121-04-SUMMARY.md`.

The SUMMARY must record:
- Task 1's macro-data-flow trace (each of the 6 confirmation steps) and the explicit verdict on producer scope (step 5).
- Whether a new pending todo was written for an ungated-macro-frame defect (and its number), or a statement that the EQ_INDEX gate is harmless.
- Confirmation that the 22-key count is consistent across confidence_utils.py module docstring, function docstring, and src/intelligence/CLAUDE.md.
- Confirmation that no confidence value changed.
</output>
