---
phase: 121-lifecycle-replay-validation
plan: 02
type: execute
wave: 2
depends_on: [121-01]
requirements: [REPLAY-02]
files_modified:
  - production/scripts/phase_121_report.py
  - docs/plans/phase-121-validation-report.md
  - docs/plans/2026-06-07-signal-quality-crisis-root-cause-analysis.md
autonomous: true

must_haves:
  truths:
    - "A before/after comparison report exists covering all 30 setups with 5 metrics each"
    - "The report diffs post-replay metrics against the before-snapshot baseline"
    - "Per-setup verdicts (PASS/FAIL/PARTIAL) are computed against roadmap targets"
    - "Cluster-level SNR rollup (GOOD/MODERATE/NEEDS_REFACTOR) shows the structural shift"
    - "stopped_at_entry is 0 for all regenerated shadow signals in the report"
    - "RCA doc Part VI is updated with MEASURED values"
    - "Bootstrap 95% CI is reported for calibration_corr on setups with pnl_r_n >= 30"
    - "Welch's t-test p-value is reported for pnl_r shift before vs. after on setups with pnl_r_n >= 30 in both snapshots"
    - "Regime-stratified SNR section covers the NEEDS_REFACTOR cluster grouped by plugin_regime_type"
  artifacts:
    - path: "production/scripts/phase_121_report.py"
      provides: "Before/after comparison report generator with verdict logic, bootstrap CI, and Welch's t-test"
      contains: "calib_ci_lo"
    - path: "docs/plans/phase-121-validation-report.md"
      provides: "The validation report consumed by humans and the RCA update"
      contains: "verdict"
    - path: "docs/plans/2026-06-07-signal-quality-crisis-root-cause-analysis.md"
      provides: "RCA Part VI with measured before/after numbers"
      contains: "MEASURED"
  key_links:
    - from: "phase_121_report.py"
      to: "docs/plans/phase-121-before-snapshot.json"
      via: "reads JSON as the before baseline (includes pnl_r_std, pnl_r_n, pnl_r_p50 from Change 2), diffs against post-replay query"
      pattern: "phase-121-before-snapshot"
    - from: "phase_121_report.py"
      to: "_verify_replay integrity gate"
      via: "aborts report generation if integrity checks fail"
      pattern: "stopped_at_entry"
---

<objective>
Generate a reproducible before/after comparison report for the Phase 121 replay, then update the RCA doc Part VI success metrics with the measured numbers.

Purpose: Prove the signal-quality refactoring worked. The report quantifies the structural shift — signal count reduction, SNR improvement, calibration correlation, and zero stopped_at_entry — across all 30 setups, with per-setup verdicts against the roadmap targets. Bootstrap CIs and Welch's t-test separate signal from noise. This is the empirical evidence that earns the refactoring its place in the corrected ledger.

Output: production/scripts/phase_121_report.py, docs/plans/phase-121-validation-report.md, and an updated RCA Part VI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-lifecycle-replay-validation/121-CONTEXT.md
@.planning/phases/121-lifecycle-replay-validation/121-RESEARCH.md
@.planning/phases/121-lifecycle-replay-validation/121-PATTERNS.md
@.planning/phases/121-lifecycle-replay-validation/121-01-SUMMARY.md
</context>

<critical_corrections>
1. **Calibration metric differs by signal type (D-05).** For shadow setups use `CORR(cis_score, (pnl_r > 0)::int)`. For non-shadow setups use `CORR(confidence, was_selected)`. The report must pick the right correlation per setup based on is_shadow.

2. **SNR == selection_rate presented as a percentage.** Do not invent a separate SNR computation — D-05 defines SNR as selection_rate.

3. **setup_performance and swarm_agent_weights are empty/stale by design** after the replay (they repopulate on nightly ml-training 11pm and weekly ml-orchestrator Monday). The report must NOTE this, not treat it as a failure.

4. **Use the frozenset for cluster membership.** The 22 `_SHADOW_VALIDATION_SETUPS` are NEEDS_REFACTOR (now refactored, in shadow). The 8 non-shadow setups are the GOOD/MODERATE control group. Do not hardcode cluster lists by memory — derive shadow membership from `is_shadow` in the query and the frozenset.
</critical_corrections>

<tasks>

<task type="auto">
  <name>Task 1: Create phase_121_report.py — before/after diff, verdicts, cluster rollup, bootstrap CI, Welch's t-test, regime-stratified SNR</name>
  <files>production/scripts/phase_121_report.py, docs/plans/phase-121-validation-report.md</files>
  <read_first>
    - docs/plans/phase-121-before-snapshot.json (the before baseline produced by 121-01 — its exact key structure: captured_at, setups[].{setup_plugin,is_shadow,total_signals,selected,selection_rate,avg_pnl_r,pnl_r_std,pnl_r_n,pnl_r_p05..pnl_r_p95,win_count,calibration_corr,outcome_expired,outcome_stopped_at_entry,outcome_filled})
    - production/scripts/phase_121_before_snapshot.py (reuse its asyncpg DatabaseManager connection pattern, query shape, and oneshot exit — keep them consistent)
    - production/scripts/compute_ic.py (asyncpg analog — markdown table rendering, file write, stdout output)
    - services/shadow_validator.py (lines 53-84 — _SHADOW_VALIDATION_SETUPS for stopped_at_entry scope and cluster derivation)
    - .planning/phases/121-lifecycle-replay-validation/121-CONTEXT.md (D-05 mandatory metrics, report table column spec, cluster rollup, success-criteria verdicts; specifics: verdict per roadmap target, integrity gate as hard prerequisite)
    - .planning/phases/121-lifecycle-replay-validation/121-PATTERNS.md (phase_121_report.py section — imports, _fetch_post_replay_metrics, _fetch_stopped_at_entry, _render_markdown_table, _write_report, oneshot exit with job="phase-121-report")
    - .planning/ROADMAP.md (lines 1645-1653 — Phase 121 success criteria / roadmap targets for verdict logic: 7.85M→4.0M total, OFI 1.59M→~250K + 15-25% SNR, PatternCompletion 795K→~95K, calibration>0.3, zero stopped_at_entry)
    - production/migrations/119_framing_audit_trail.sql (plugin_regime_type column definition — used in regime-stratified SNR section)
  </read_first>
  <action>
    Create `production/scripts/phase_121_report.py` (asyncpg via DatabaseManager, oneshot). Module docstring: reads the before-snapshot baseline, runs identical post-replay metrics, diffs, writes the markdown report. Implement:

    - Hard prerequisite check at start: if `docs/plans/phase-121-before-snapshot.json` does not exist, abort with a clear error and SystemExit(1). Per D-06/specifics, also run the integrity checks before producing the report — both conditions must be satisfied or the script aborts rather than emitting a misleading report:
      (a) `stopped_at_entry` for shadow signals must be 0 (query: COUNT from signal_ledger JOIN signal_outcomes WHERE outcome='stopped_at_entry' AND is_shadow=true)
      (b) `orphan_ledger_rows` must be 0 (query: COUNT from signal_ledger LEFT JOIN signal_outcomes WHERE signal_outcomes.signal_id IS NULL)
      If either count is non-zero, print a clear error message naming which check failed and the exact count, then raise SystemExit(1). This prevents a corrupt or partially-replayed ledger from producing a report that looks valid.

    - `_fetch_post_replay_metrics(conn)`: same query shape as the before-snapshot (signal_ledger LEFT JOIN signal_outcomes, GROUP BY setup_plugin, is_shadow). For shadow setups compute `CORR(cis_score, (pnl_r > 0)::int)`; for non-shadow compute `CORR(confidence, was_selected)`. Either run two queries split on is_shadow or compute both correlations and select by is_shadow when building rows. Also fetch `pnl_r_std` and `pnl_r_n` (STDDEV and COUNT of non-null pnl_r) in this query — they are required for Welch's t-test.

    - `_fetch_stopped_at_entry(conn)`: per-setup COUNT of `outcome='stopped_at_entry' AND is_shadow=true` grouped by setup_plugin.

    - Build per-setup report rows with columns: `setup_plugin | cluster | signals_before | signals_after | delta_pct | snr_before | snr_after | calibration_corr | calib_ci_lo | calib_ci_hi | pnl_t_stat | pnl_p_value | stopped_at_entry | verdict`. cluster = NEEDS_REFACTOR if setup is in the shadow frozenset else GOOD/MODERATE (derive from is_shadow). SNR = selection_rate as a percentage. delta_pct = (after-before)/before*100. signals_before/snr_before come from the loaded JSON baseline (match on setup_plugin + is_shadow); use 0/None when a setup is absent from the baseline.

    - Verdict logic (PASS/FAIL/PARTIAL) against roadmap targets from ROADMAP lines 1645-1653: e.g. trad_OFIContinuation target 1.59M→~250K and 15-25% SNR; trad_PatternCompletion 795K→~95K; calibration_corr>0.3; stopped_at_entry==0. PASS if all applicable targets met, PARTIAL if some, FAIL if signal reduction or stopped_at_entry target missed. Encode the per-setup numeric targets as a small mapping; setups without an explicit target are judged on calibration>0.3 and stopped_at_entry==0.

    - Cluster-level rollup section: aggregate SNR before/after for NEEDS_REFACTOR, MODERATE, GOOD clusters to show the structural shift.

    - `_render_markdown_table(rows)` and a header/summary section; `_write_report(content)` writing `docs/plans/phase-121-validation-report.md`. Include a NOTE section documenting that setup_performance and swarm_agent_weights repopulate on next scheduled ml runs (nightly 11pm / weekly Mon).

    **Subsection A: Bootstrap CI on calibration_corr (post-replay)**

    Implement `_bootstrap_calib_ci(rows, n_boot=1000)` using numpy only (no scipy dependency). For each setup with `pnl_r_n >= 30` (minimum sample gate — below 30 the CI is meaningless):
    - Fetch raw `(cis_score, pnl_r)` pairs from the DB for that setup. For shadow setups (`is_shadow=true`) use `(cis_score, pnl_r)` pairs from signal_ledger JOIN signal_outcomes where pnl_r IS NOT NULL. For non-shadow setups use `(confidence, (was_selected::int))` pairs.
    - Bootstrap: sample with replacement n_boot times, compute Pearson correlation each time using `np.corrcoef(sample[:,0], sample[:,1])[0,1]`.
    - Report `[np.percentile(boot_corrs, 2.5), np.percentile(boot_corrs, 97.5)]` as the 95% CI.
    - Populate `calib_ci_lo` and `calib_ci_hi` columns in the per-setup report row.

    If `pnl_r_n < 30`, set `calib_ci_lo` and `calib_ci_hi` to `None` and add a "n<30" note in the report for that setup.

    Import numpy as np; do NOT import scipy for the bootstrap step.

    **Subsection B: Welch's t-test on pnl_r shift (before vs. after)**

    Implement `_welch_t_test(before, after)` where `before` is a dict with keys `avg_pnl_r`, `pnl_r_std`, `pnl_r_n` (read from the before-snapshot JSON) and `after` is the same dict from the post-replay query.

    Formula (Welch's t):
    ```python
    se_sq_sum = (after['pnl_r_std']**2 / after['pnl_r_n']) + (before['pnl_r_std']**2 / before['pnl_r_n'])
    t = (after['avg_pnl_r'] - before['avg_pnl_r']) / (se_sq_sum ** 0.5)
    ```

    For the p-value, wrap a scipy import in try/except ImportError: if scipy is available, use `scipy.stats.t.sf(abs(t), df=min(before['pnl_r_n'], after['pnl_r_n']) - 1) * 2`; if scipy is not importable, set p_value to None and note "p-value: scipy not installed" in the report row. Do NOT make scipy a hard dependency.

    Skip setups where `pnl_r_n < 30` in either before or after — set `pnl_t_stat` and `pnl_p_value` to None and flag as "n<30".

    Add `pnl_t_stat` and `pnl_p_value` columns to the per-setup report row.

    **Subsection C: Regime-stratified SNR section**

    After the cluster rollup, add a regime-stratified section for the NEEDS_REFACTOR cluster only. Query: for each distinct `plugin_regime_type` value among the 22 shadow setups in the post-replay data, compute `COUNT(*) AS signal_count, AVG(CASE WHEN so.was_selected = true THEN 1.0 ELSE 0.0 END) AS snr` grouped by `sl.plugin_regime_type`. Render as a second markdown table in the report under `## Regime-Stratified SNR (NEEDS_REFACTOR Cluster)`. This shows whether SNR improvement is uniform across regimes or concentrated in one.

    If `plugin_regime_type` is NULL for all rows returned (field not yet populated), skip this section with a note: "Regime-stratified SNR: plugin_regime_type not populated — will populate on next live run."

    - Oneshot exit: `JOB_COMPLETED_TOTAL.add(1, {"job": "phase-121-report", "status": "success"})` (and "failure" on exception); `init_otel_providers("phase-121-report")` with OTelInitError guard; `flush_and_shutdown_metrics()` in finally.

    Then RUN `.venv/bin/python production/scripts/phase_121_report.py` and confirm the report is written and covers all 30 setups.
  </action>
  <verify>
    - `.venv/bin/python -c "import ast; ast.parse(open('production/scripts/phase_121_report.py').read())"` exits 0
    - `.venv/bin/ruff check production/scripts/phase_121_report.py` exits 0
    - `grep -n "phase-121-before-snapshot" production/scripts/phase_121_report.py` returns the baseline read
    - `grep -n "stopped_at_entry\|verdict\|cluster" production/scripts/phase_121_report.py` returns matches
    - `grep -n "orphan_ledger_rows" production/scripts/phase_121_report.py` returns the prerequisite integrity check
    - `grep -n "_bootstrap_calib_ci\|_welch_t_test\|plugin_regime_type" production/scripts/phase_121_report.py` returns matches
    - After run: `test -f docs/plans/phase-121-validation-report.md` succeeds
    - `grep -c "verdict\|NEEDS_REFACTOR\|GOOD" docs/plans/phase-121-validation-report.md` returns matches
    - `grep -n "calib_ci_lo\|pnl_t_stat\|Regime-Stratified" docs/plans/phase-121-validation-report.md` returns matches
    - The rendered report contains a row count consistent with 30 setups (grep table rows)
  </verify>
  <acceptance_criteria>
    - phase_121_report.py exists, parses, passes ruff
    - The script aborts if the before-snapshot JSON is missing
    - The script aborts if `stopped_at_entry > 0` for any shadow signal (integrity check a)
    - The script aborts if `orphan_ledger_rows > 0` (integrity check b — Phase 104 invariant)
    - Shadow setups use CORR(cis_score, pnl_r>0); non-shadow use CORR(confidence, was_selected)
    - docs/plans/phase-121-validation-report.md exists with the per-setup table (including calib_ci_lo/calib_ci_hi and pnl_t_stat/pnl_p_value columns), a cluster SNR rollup, and a verdict column
    - The report contains a NOTE that setup_performance/swarm_agent_weights repopulate on scheduled ml runs
    - stopped_at_entry column shows 0 for all regenerated shadow setups
    - `_bootstrap_calib_ci` uses numpy only (no scipy import required for bootstrap); scipy is optional for p-value only
    - The report includes `calib_ci_lo`/`calib_ci_hi` per setup (or "n<30" flag where pnl_r_n < 30)
    - The report includes `pnl_t_stat`/`pnl_p_value` per setup (or "n<30" flag where insufficient sample in either snapshot)
    - The report includes a `## Regime-Stratified SNR (NEEDS_REFACTOR Cluster)` section (or a note explaining why it is absent)
  </acceptance_criteria>
  <done>A reproducible validation report covering all 30 setups exists with before/after diffs, per-setup verdicts, cluster SNR rollup, bootstrap 95% CI on calibration_corr, Welch's t-test on pnl_r shift, regime-stratified SNR for the NEEDS_REFACTOR cluster, a zeroed stopped_at_entry column, and a hard prerequisite gate that refuses to emit a report if orphan_ledger_rows>0 or stopped_at_entry>0.</done>
</task>

<task type="auto">
  <name>Task 2: Update RCA doc Part VI with measured before/after values</name>
  <files>docs/plans/2026-06-07-signal-quality-crisis-root-cause-analysis.md</files>
  <read_first>
    - docs/plans/phase-121-validation-report.md (the just-generated report — source of all measured numbers)
    - docs/plans/2026-06-07-signal-quality-crisis-root-cause-analysis.md (Part VI success metrics — read the existing Part VI table structure and the targets it lists so the update aligns column-for-column)
    - .planning/phases/121-lifecycle-replay-validation/121-CONTEXT.md (D-05 RCA update spec; specifics: `| Metric | Target | Before (measured) | After (measured) | Status |` table format, append `MEASURED YYYY-MM-DD`)
  </read_first>
  <action>
    Update Part VI "Success Metrics" of the RCA doc using the measured values from docs/plans/phase-121-validation-report.md. Use a table with columns `| Metric | Target | Before (measured) | After (measured) | Status |`. Populate Before (measured) from the before-snapshot baseline and After (measured) from the post-replay report — NOT from the stale 7.85M/0.19% documentation figures (those were estimates; the measured baseline is authoritative). For each metric row append `MEASURED 2026-06-10` (use today's date) in the Status column alongside PASS/FAIL/PARTIAL.

    Cover at minimum the roadmap-target metrics: total signal count, overall selection rate, NEEDS_REFACTOR cluster SNR, trad_OFIContinuation count + SNR, trad_PatternCompletion count + SNR, calibration correlation, and stopped_at_entry count. Do not delete the original target text — annotate it with measured reality. Preserve all other RCA sections unchanged.
  </action>
  <verify>
    - `grep -c "MEASURED" docs/plans/2026-06-07-signal-quality-crisis-root-cause-analysis.md` returns >= 1
    - `grep -n "Before (measured)\|After (measured)" docs/plans/2026-06-07-signal-quality-crisis-root-cause-analysis.md` returns the table header
    - `grep -n "stopped_at_entry\|OFIContinuation\|PatternCompletion" docs/plans/2026-06-07-signal-quality-crisis-root-cause-analysis.md` returns the metric rows
  </verify>
  <acceptance_criteria>
    - RCA Part VI contains a `| Metric | Target | Before (measured) | After (measured) | Status |` table
    - Each measured row carries a `MEASURED 2026-06-10` annotation plus a PASS/FAIL/PARTIAL status
    - Measured numbers match the values in docs/plans/phase-121-validation-report.md
    - No pre-existing RCA section outside Part VI was modified
  </acceptance_criteria>
  <done>RCA Part VI reflects measured before/after reality with MEASURED annotations, closing the loop from crisis diagnosis to empirical validation.</done>
</task>

</tasks>

<verification>
- phase_121_report.py passes ast.parse and ruff
- docs/plans/phase-121-validation-report.md exists, covers 30 setups, has verdict + cluster rollup + bootstrap CI + Welch's t-test + regime-stratified SNR
- stopped_at_entry column is 0 for all regenerated shadow setups
- RCA Part VI contains the measured before/after table with MEASURED annotations
</verification>

<success_criteria>
- All 30 setups appear in the validation report with metrics and a verdict each
- The report reads the before-snapshot baseline (including pnl_r_std, pnl_r_n from Change 2) and diffs against live post-replay data
- Cluster SNR rollup demonstrates the structural shift (NEEDS_REFACTOR improvement)
- Bootstrap 95% CI provides confidence bounds on calibration_corr for setups with sufficient sample
- Welch's t-test distinguishes signal from noise in the pnl_r shift between before and after
- Regime-stratified SNR shows whether improvement is uniform or regime-concentrated
- RCA Part VI success metrics are updated with measured numbers
</success_criteria>

<output>
After completion, create `.planning/phases/121-lifecycle-replay-validation/121-02-SUMMARY.md` recording: the measured total signal count before/after, NEEDS_REFACTOR cluster SNR before/after, OFI and PatternCompletion verdicts, calibration correlation summary (with bootstrap CI where available), and confirmation that stopped_at_entry is 0 across regenerated shadow setups.
</output>
