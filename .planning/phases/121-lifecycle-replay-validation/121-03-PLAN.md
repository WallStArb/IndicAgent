---
phase: 121-lifecycle-replay-validation
plan: 03
type: execute
wave: 2
depends_on: [121-01]
requirements: [REPLAY-01]
files_modified:
  - src/intelligence/trading/atr_utils.py
  - src/intelligence/confluence/confluence_smc.py
  - src/intelligence/confluence/cross_tf_sr_confluence.py
  - src/intelligence/trading/zone_engine.py
  - src/intelligence/context/macro_context.py
  - src/intelligence/schemas.py
  - src/intelligence/register_plugins.py
autonomous: true

must_haves:
  truths:
    - "get_atr_valid(features) exists in atr_utils.py and raises ValueError when ATR is None"
    - "confluence_smc.py score_fvg_alignment uses if-not-atr early return — no or 0.0 fallback"
    - "cross_tf_sr_confluence.py compute_full uses if-not-atr early return — no or 1.0 fallback"
    - "MacroContextPlugin exists in src/intelligence/context/macro_context.py and is registered in TIER_I4"
    - "I4Context has all 5 macro fields active: ftq_score, ftq_regime, yield_curve_slope, yield_curve_regime, corr_z"
    - "No DB migration required — macro fields flow through I4Context JSONB"
    - "Unit tests pass with no regressions"
  artifacts:
    - path: "src/intelligence/trading/atr_utils.py"
      provides: "Strict ATR accessor for I7 use — raises ValueError on None"
      contains: "get_atr_valid"
    - path: "src/intelligence/confluence/confluence_smc.py"
      provides: "ATR early-return guard in score_fvg_alignment"
      contains: "if not atr: return"
    - path: "src/intelligence/confluence/cross_tf_sr_confluence.py"
      provides: "ATR early-return guard in compute_full"
      contains: "if not atr: return"
    - path: "src/intelligence/context/macro_context.py"
      provides: "MacroContextPlugin — I4 macro context (ftq, yield curve, corr_z)"
      exports: ["plugin"]
    - path: "src/intelligence/schemas.py"
      provides: "I4Context with 5 active macro fields"
      contains: "ftq_score"
    - path: "src/intelligence/register_plugins.py"
      provides: "MacroContextPlugin registered in TIER_I4"
      contains: "macro_ctx_plugin"
  key_links:
    - from: "src/intelligence/context/macro_context.py"
      to: "frames[\"cross_asset\"]"
      via: "compute_full reads xa = frames.get(\"cross_asset\") or {}"
      pattern: "cross_asset"
    - from: "src/intelligence/register_plugins.py TIER_I4"
      to: "src/intelligence/context/macro_context.py"
      via: "macro_ctx_plugin.name in TIER_I4 list"
      pattern: "macro_ctx_plugin"
    - from: "src/intelligence/schemas.py I4Context"
      to: "MacroContextPlugin outputs"
      via: "ftq_score, ftq_regime, corr_z fields declared in I4Context"
      pattern: "ftq_score.*float.*None"
---

<objective>
Harden the I6 confluence layer against silent ATR data corruption and wire the five macro cross-asset fields into I4Context so replayed and live bars carry correct macro context.

Purpose: ATR fallbacks (or 0.0 / or 1.0) propagate nonsensical values into ratio calculations on quiet or off-hours bars. The macro fields (ftq_score, ftq_regime, yield_curve_slope, yield_curve_regime, corr_z) are already extracted from the macro_signals topic and merged into frames["cross_asset"] — the only missing piece is the I4 plugin that reads them and the I4Context fields that hold them. Both fixes must land before the Phase 126 clean replay so replayed data reflects correct pipeline behavior.

Output: Modified atr_utils.py (new get_atr_valid), modified confluence_smc.py and cross_tf_sr_confluence.py (early-return guards), commented zone_engine.py (no code change), new macro_context.py (MacroContextPlugin), modified schemas.py (5 active macro fields in I4Context), modified register_plugins.py (TIER_I4 updated).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-lifecycle-replay-validation/121-CONTEXT.md
@.planning/phases/121-lifecycle-replay-validation/121-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ATR hardening — early-return guards in I6 confluence, get_atr_valid in atr_utils.py, clarifying comment in zone_engine.py</name>
  <files>
    src/intelligence/trading/atr_utils.py
    src/intelligence/confluence/confluence_smc.py
    src/intelligence/confluence/cross_tf_sr_confluence.py
    src/intelligence/trading/zone_engine.py
  </files>
  <read_first>
    - src/intelligence/trading/atr_utils.py (full file — understand existing get_atr, get_atr_with_period, get_atr_with_floor, get_atr_with_floor_from_frames signatures before adding get_atr_valid)
    - src/intelligence/confluence/confluence_smc.py (lines 41-84 — score_fvg_alignment; locate the `atr = get_atr(features) or 0.0` line and the `if cur_trend == 0 or atr <= 0: return 0.0, {}` guard that follows it)
    - src/intelligence/confluence/cross_tf_sr_confluence.py (lines 108-115 — compute_full inner loop; locate the `atr = get_atr(intel) or 1.0` line and how atr is used in dist_to_resistance / dist_to_support calculations)
    - src/intelligence/trading/zone_engine.py (lines 227-235 — confirm the existing `if atr is None: return []` check is already correct; note the comment to add at line 232)
  </read_first>
  <action>
    Four coordinated edits. Read each file in full before touching it.

    (A) src/intelligence/trading/atr_utils.py — add get_atr_valid after get_atr_with_floor_from_frames. Function signature: `def get_atr_valid(features: dict[str, Any]) -> float`. Docstring: "Strict ATR accessor for I7 plugin use. Returns positive float ATR value. Raises ValueError when ATR is None, zero, or negative. Use ONLY in I7 compute_full() where no_signal() is the correct failure contract. For I6, use the early-return pattern (if not atr: return {}) instead — I6 must gracefully degrade, not raise." Implementation: `atr = get_atr(features)` then `if not atr: raise ValueError(f"ATR unavailable or non-positive: {features.get('atr_14')!r}")` then `return atr`. No changes to existing functions.

    (B) src/intelligence/confluence/confluence_smc.py — fix score_fvg_alignment. The current code at line 54 reads `atr = get_atr(features) or 0.0` followed at line 55 by `if cur_trend == 0 or atr <= 0: return 0.0, {}`. Replace these two lines with three lines: `atr = get_atr(features)` then `if not atr: return {}, {}` then `if cur_trend == 0: return 0.0, {}`. The early return on missing ATR now returns the correct type `({}, {})` — a tuple of (float, dict) where the caller pattern matches `(score, contributions)`. Check the call sites of score_fvg_alignment in the same file to confirm the return type contract. The `or 0.0` pattern is the bug: it silently lets atr=0.0 propagate into the proximity decay divisor. Note: preserve the existing `cur_price = features.get("close") or 0.0` line above the ATR line — that line is correct (zero close is a valid early-exit, not ATR data corruption).

    (C) src/intelligence/confluence/cross_tf_sr_confluence.py — fix compute_full inner loop. The current code at line 110 reads `atr = get_atr(intel) or 1.0`. Replace with `atr = get_atr(intel)` then `if not atr: continue`. The `continue` skips this timeframe — semantically correct: if ATR is missing for a timeframe's intel dict, that timeframe contributes nothing to the S/R confluence score. The division `(float(resistance) - close) / float(atr)` at lines 113-114 now has a guaranteed positive float. This is a one-line split into two lines; do not change the subsequent distance calculations or the sr_scores dict update.

    (D) src/intelligence/trading/zone_engine.py — add clarifying comment only, no code change. At line 232 (the `atr = get_atr_with_floor(features, symbol)` call), add an inline comment: `# symbol-variant API: frames not in scope here; symbol read from features dict`. The existing `if atr is None: return []` check at line 233 is already correct per D-09 — this is documentation only. Mark done.
  </action>
  <verify>
    - `.venv/bin/python -c "import ast; ast.parse(open('src/intelligence/trading/atr_utils.py').read()); print('OK')"` prints OK
    - `.venv/bin/python -c "import ast; ast.parse(open('src/intelligence/confluence/confluence_smc.py').read()); print('OK')"` prints OK
    - `.venv/bin/python -c "import ast; ast.parse(open('src/intelligence/confluence/cross_tf_sr_confluence.py').read()); print('OK')"` prints OK
    - `.venv/bin/python -c "import ast; ast.parse(open('src/intelligence/trading/zone_engine.py').read()); print('OK')"` prints OK
    - `grep -n "get_atr_valid" src/intelligence/trading/atr_utils.py` returns the function definition
    - `grep -n "or 0.0" src/intelligence/confluence/confluence_smc.py` returns zero matches for the ATR line (the close line `or 0.0` may still remain — confirm it is on the `cur_price` line, not the ATR line)
    - `grep -n "or 1.0" src/intelligence/confluence/cross_tf_sr_confluence.py` returns zero matches
    - `grep -n "if not atr" src/intelligence/confluence/confluence_smc.py` returns at least one match
    - `grep -n "if not atr" src/intelligence/confluence/cross_tf_sr_confluence.py` returns at least one match
    - `grep -n "frames not in scope" src/intelligence/trading/zone_engine.py` returns the comment at line 232
    - `.venv/bin/ruff check src/intelligence/trading/atr_utils.py src/intelligence/confluence/confluence_smc.py src/intelligence/confluence/cross_tf_sr_confluence.py src/intelligence/trading/zone_engine.py` exits 0
  </verify>
  <acceptance_criteria>
    - get_atr_valid(features) exists in atr_utils.py, raises ValueError when ATR is None/zero, returns float when ATR is valid
    - score_fvg_alignment in confluence_smc.py returns ({}, {}) when ATR is missing — no or 0.0 ATR fallback in that function
    - compute_full in cross_tf_sr_confluence.py uses continue when atr is falsy — no or 1.0 ATR fallback
    - zone_engine.py has the clarifying comment at line 232, no code change
    - All four files parse and pass ruff
  </acceptance_criteria>
  <done>ATR data corruption eliminated from I6 confluence layer. Missing ATR causes graceful degradation (zero contribution to score) rather than silent propagation of 0.0 or 1.0. get_atr_valid available for I7 plugins that need loud failure semantics.</done>
</task>

<task type="auto">
  <name>Task 2: MacroContextPlugin — create macro_context.py, activate I4Context macro fields, register in TIER_I4</name>
  <files>
    src/intelligence/context/macro_context.py
    src/intelligence/schemas.py
    src/intelligence/register_plugins.py
  </files>
  <read_first>
    - src/intelligence/context/cross_asset_context.py (full file — canonical pattern for frames["cross_asset"] reads, dataclass plugin structure, outputs frozenset, compute_full / compute_next signatures, `plugin = ClassName()` module-level singleton)
    - src/intelligence/schemas.py (lines 342-360 — I4Context class definition and docstring; lines 1055-1071 — the commented ftq_score/ftq_regime and yield_curve_slope/yield_curve_regime fields; verify exact line numbers before editing)
    - src/intelligence/register_plugins.py (lines 1-80 — existing import block structure; lines 498-512 — TIER_I4 list; locate where context plugins are imported, e.g. cross_asset_ctx_plugin import, to add macro_ctx_plugin import in the same style)
    - services/intelligence_pipeline.py (lines 460-470 — confirms ftq_score/ftq_regime/yield_curve_slope/yield_curve_regime/corr_z are already extracted from macro_signals topic into the cache that populates frames["cross_asset"]; read to understand the exact field names injected)
    - src/intelligence/pipeline/feature_pipeline_executor.py (lines 207-210 — confirms macro_data already merged into frames["cross_asset"]; no changes needed here, read for field name verification)
  </read_first>
  <action>
    Three coordinated edits. Read all five files above before writing any code.

    (A) Create src/intelligence/context/macro_context.py. Follow the CrossAssetContextPlugin pattern exactly: dataclass, module docstring, `from __future__ import annotations`, typing imports, InputSpec import from `..plugins`. Do NOT import from cross_asset_context.py.

    Module docstring: "Macro cross-asset context plugin — I4 tier. Reads ftq_score, ftq_regime, yield_curve_slope, yield_curve_regime, and corr_z from frames['cross_asset'] (injected by FeaturePipelineService via macro_signals topic). Applies to ALL symbols — no EQ_INDEX guard needed (macro context is instrument-agnostic). Returns {} when cross_asset data not ready (same graceful-degradation contract as CrossAssetContextPlugin). Phase 121 Wave 2 (D-10): one plugin per data source."

    Class MacroContextPlugin (dataclass):
    - name: str = "ctx_MacroContext"
    - outputs: frozenset[str] = frozenset({"ftq_score", "ftq_regime", "yield_curve_slope", "yield_curve_regime", "corr_z"})
    - min_lookback: int = 1
    - supports_incremental: bool = False
    - capability_tags: frozenset[str] = frozenset({"context", "macro"})
    - inputs: tuple[InputSpec, ...] = ()

    compute_full(self, frames: dict[str, Any]) -> dict[str, Any]:
      xa = frames.get("cross_asset") or {}
      if not xa.get("ready"):
          return {}
      Extract each of the 5 fields with safe casting:
      - ftq_score: xa.get("ftq_score") — cast to float if not None, else None; catch (TypeError, ValueError) and set to None
      - ftq_regime: xa.get("ftq_regime") — string or None (no cast needed beyond str() or None)
      - yield_curve_slope: xa.get("yield_curve_slope") — float or None
      - yield_curve_regime: xa.get("yield_curve_regime") — string or None
      - corr_z: xa.get("corr_z") — float or None
      Return a dict with all 5 keys. Return {} only when xa["ready"] is falsy. When xa is ready but individual fields are absent, return the dict with None values for missing keys — the I4Context dataclass fields default to None anyway, but returning them explicitly makes the output contract explicit.

    compute_next(self, windows: dict[str, Any], *, state: dict | None = None) -> dict[str, Any]:
      return self.compute_full(windows)

    Module-level: `plugin = MacroContextPlugin()`

    (B) src/intelligence/schemas.py — three changes to I4Context:

    1. Uncomment ftq_score and ftq_regime. Current state (lines ~1065-1066):
       `# ftq_score: float | None = None`
       `# ftq_regime: str | None = None`
       Remove the leading `# ` from both lines. Preserve the comment above them: `# Flight-to-quality factor (Plan 64-03B)`.

    2. Add corr_z field. After the ftq_regime line, add:
       `# Stock-bond correlation z-score (MacroContextPlugin)`
       `corr_z: float | None = None`

    3. Update the docstring. Find the I4Context class docstring (lines 343-358). Change two things:
       - Add `- MacroContext (5 fields)` to the plugin list, after `CrossAssetContext (2 fields)`.
       - Update the `Total: 93 fields` line to `Total: 98 fields` (93 + 5 new/activated = 98).
       Note: yield_curve_slope and yield_curve_regime were already declared (2 fields); ftq_score, ftq_regime, corr_z are the 3 newly added. But the prior Total: 93 counted yield_curve as 2 orphaned fields already. So: 93 existing + 3 new (ftq_score, ftq_regime, corr_z) = 96 total — NOT 98. Verify by counting: read the docstring plugin list and sum the per-plugin field counts. If the current count already includes yield_curve_slope and yield_curve_regime, the delta is +3. Update Total to the correct computed value, not a fixed number. Write the final number after counting.

    (C) src/intelligence/register_plugins.py — two changes:

    1. Add import. Find the block where context plugins are imported (look for `cross_asset_ctx_plugin` import to locate the right section). Add immediately after it (or in alphabetical order with the other context imports):
       `from src.intelligence.context.macro_context import plugin as macro_ctx_plugin`

    2. Add to TIER_I4. In the TIER_I4 list (lines 498-512), add `macro_ctx_plugin.name,  # "ctx_MacroContext" — Phase 121 Wave 2` as the last item before the closing `]`. The list is not sorted — append at end.
  </action>
  <verify>
    - `.venv/bin/python -c "import ast; ast.parse(open('src/intelligence/context/macro_context.py').read()); print('OK')"` prints OK
    - `.venv/bin/python -c "from src.intelligence.context.macro_context import plugin; print(plugin.name, plugin.outputs)"` prints `ctx_MacroContext frozenset({...5 fields...})`
    - `grep -n "ftq_score\|ftq_regime\|corr_z" src/intelligence/schemas.py` returns uncommented field declarations in I4Context (no leading `#`)
    - `grep -n "MacroContext\|macro_ctx_plugin" src/intelligence/register_plugins.py` returns the import line and the TIER_I4 entry
    - `grep -c "ctx_MacroContext" src/intelligence/register_plugins.py` returns at least 1 (the name in TIER_I4)
    - `grep -n "Total:" src/intelligence/schemas.py | grep -A2 "I4Context"` shows the updated field count
    - `.venv/bin/python -c "from src.intelligence.schemas import I4Context; i = I4Context(ts=__import__('datetime').datetime.now(__import__('datetime').timezone.utc), symbol='ES', timeframe='1m', ftq_score=0.5, ftq_regime='risk_off', corr_z=-1.2); print(i.ftq_score, i.ftq_regime, i.corr_z)"` prints `0.5 risk_off -1.2`
    - `.venv/bin/ruff check src/intelligence/context/macro_context.py src/intelligence/schemas.py src/intelligence/register_plugins.py` exits 0
  </verify>
  <acceptance_criteria>
    - macro_context.py exists, parses, imports cleanly; MacroContextPlugin reads from frames["cross_asset"]; returns {} when not ready; no EQ_INDEX guard
    - I4Context in schemas.py has ftq_score and ftq_regime uncommented; corr_z is a new declared field; yield_curve_slope and yield_curve_regime remain; docstring field count updated to correct total
    - register_plugins.py imports macro_ctx_plugin and includes its name in TIER_I4
    - I4Context can be instantiated with ftq_score, ftq_regime, corr_z without extra="forbid" rejection
    - All three files parse and pass ruff
  </acceptance_criteria>
  <done>MacroContextPlugin wired into the I4 tier. All 5 macro fields (ftq_score, ftq_regime, yield_curve_slope, yield_curve_regime, corr_z) flow from frames["cross_asset"] through I4Context JSONB to intelligence_features.i4 on every bar. No DB migration needed.</done>
</task>

<task type="auto">
  <name>Task 3: Run unit tests and ruff across all changed files</name>
  <files></files>
  <read_first>
    - None — this is a validation-only task.
  </read_first>
  <action>
    Run the full unit test suite and ruff on all files touched in Tasks 1 and 2. Fix any failures before marking done.

    Step 1: ruff check on all changed files:
    `.venv/bin/ruff check src/intelligence/trading/atr_utils.py src/intelligence/confluence/confluence_smc.py src/intelligence/confluence/cross_tf_sr_confluence.py src/intelligence/trading/zone_engine.py src/intelligence/context/macro_context.py src/intelligence/schemas.py src/intelligence/register_plugins.py`

    Step 2: black check on the same files:
    `.venv/bin/black --check src/intelligence/trading/atr_utils.py src/intelligence/confluence/confluence_smc.py src/intelligence/confluence/cross_tf_sr_confluence.py src/intelligence/trading/zone_engine.py src/intelligence/context/macro_context.py src/intelligence/schemas.py src/intelligence/register_plugins.py`
    If black reports formatting differences, apply them: `.venv/bin/black <file>` for each flagged file.

    Step 3: run unit tests:
    `.venv/bin/pytest tests/unit/ -q`

    If tests fail: read the failure output, identify the root cause, fix it in the appropriate source file, and re-run tests. Do not mark this task done until `pytest tests/unit/ -q` exits 0.

    Common failure modes to watch for:
    - I4Context extra="forbid" rejection: if any test constructs I4Context without the new macro fields and the test passes extra fields, that is a test bug — fix the test. If a required field is rejected, the issue is in schemas.py — re-read the ConfigDict.
    - score_fvg_alignment return type change: Task 1 changed the early-return from `0.0, {}` to `{}, {}` when ATR is missing. If any test or caller expects `(float, dict)` from that branch, the test or call site must be updated to handle `({}, {})` — but verify the caller first; the empty-dict branch may be unreachable in test coverage.
    - MacroContextPlugin registration: validate_tier() in register_plugins.py hard-crashes if a plugin name is in TIER_I4 but not registered. Ensure `macro_ctx_plugin.name` matches the `name` attribute in MacroContextPlugin exactly ("ctx_MacroContext").
  </action>
  <verify>
    - `.venv/bin/ruff check src/intelligence/trading/atr_utils.py src/intelligence/confluence/confluence_smc.py src/intelligence/confluence/cross_tf_sr_confluence.py src/intelligence/trading/zone_engine.py src/intelligence/context/macro_context.py src/intelligence/schemas.py src/intelligence/register_plugins.py` exits 0
    - `.venv/bin/pytest tests/unit/ -q` exits 0 with no FAILED or ERROR lines
  </verify>
  <acceptance_criteria>
    - ruff exits 0 on all 7 changed files
    - pytest tests/unit/ exits 0 (all tests pass or skip, zero failures)
  </acceptance_criteria>
  <done>All changed files are lint-clean and the full unit test suite passes with no regressions from Task 1 or Task 2 changes.</done>
</task>

</tasks>

<verification>
- `grep -n "get_atr_valid" src/intelligence/trading/atr_utils.py` returns function definition
- `grep -n "or 0.0" src/intelligence/confluence/confluence_smc.py` has zero matches on the ATR line (score_fvg_alignment)
- `grep -n "or 1.0" src/intelligence/confluence/cross_tf_sr_confluence.py` has zero matches
- `grep -n "if not atr" src/intelligence/confluence/confluence_smc.py` and `cross_tf_sr_confluence.py` each return at least one match
- `grep -n "ftq_score\|ftq_regime\|corr_z" src/intelligence/schemas.py` shows uncommented fields in I4Context
- `grep -n "macro_ctx_plugin" src/intelligence/register_plugins.py` returns import and TIER_I4 entry
- `.venv/bin/python -c "from src.intelligence.context.macro_context import plugin; assert plugin.name == 'ctx_MacroContext'; print('OK')"` prints OK
- `.venv/bin/pytest tests/unit/ -q` exits 0
</verification>

<success_criteria>
- ATR data corruption eliminated from I6: score_fvg_alignment and cross_tf_sr_confluence compute_full both return early with zero contribution when ATR is unavailable
- get_atr_valid available in atr_utils.py for I7 plugins requiring loud failure semantics
- MacroContextPlugin registered in TIER_I4; all 5 macro fields flow from frames["cross_asset"] through I4Context JSONB to intelligence_features.i4 on every bar
- No DB migration required
- Unit test suite green with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/121-lifecycle-replay-validation/121-03-SUMMARY.md` recording: files changed, exact edits made (before/after for each ATR fix, field names added to I4Context, TIER_I4 update), final pytest result, and any test fixes required.
</output>
