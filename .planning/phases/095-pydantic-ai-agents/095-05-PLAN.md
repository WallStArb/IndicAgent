---
phase: 095-pydantic-ai-agents
slug: pydantic-ai-agents
plan: 05
type: execute
wave: 4
depends_on: [095-04]
files_modified:
  - src/intelligence/ai/alpha/skeptic_agent.py
  - services/alpha_swarm.py
  - src/api/routes/ai_stats.py
  - tools/validate_skeptic.py
  - tests/unit/services/test_skeptic_agent.py
  - tests/unit/services/test_alpha_swarm.py
  - tests/integration/test_swarm_graduation_loop.py
autonomous: true
user_setup: []
requirements:
  - AGENT-EXEC-04

must_haves:
  truths:
    - "SkepticEvaluator sets result_type = SkepticResult as a ClassVar and its _compute() calls await self._run_typed(...) instead of _llm_generate_structured()"
    - "SkepticEvaluator.agent_id is 'skeptic' (the 'skeptic_v1' AGENT-ID suffix is removed)"
    - "prompt_version stays ACTIVE_VERSION ('skeptic_v2') and the PROMPT_REGISTRY key 'skeptic_v1' (a prompt-template version, NOT the agent id) is left unchanged in skeptic_prompts.py"
    - "Every operational reference to the agent_id 'skeptic_v1' is renamed to 'skeptic': skeptic_agent.py, alpha_swarm._SWARM_AGENT_TO_TRANSFORM, ai_stats._AGENT_DISPLAY, validate_skeptic.py help text, and test agent_id assertions/mocks"
    - "There is no SkepticPydanticEvaluator / SkepticComputeAgentV2 parallel class and no ENABLE_PYDANTIC_SKEPTIC gate"
    - "The transfer function is unchanged: multiplier = (1.0 - failure_probability) * llm_confidence; output built via _build_multiplier_output"
    - "AlphaSwarm._setup() still constructs SkepticEvaluator(llm_chain=...) unchanged"
    - "On LLM/validation failure the swarm still receives a neutral AgentOutput (no crash) via the compute() wrapper - covered by a regression test that forces a pydantic validation failure path, not only a mocked _run_typed exception"
  artifacts:
    - path: "src/intelligence/ai/alpha/skeptic_agent.py"
      provides: "SkepticEvaluator migrated to _run_typed with agent_id='skeptic'"
      contains: "self._run_typed"
    - path: "services/alpha_swarm.py"
      provides: "transform mapping updated to 'skeptic'"
      contains: "\"skeptic\""
    - path: "src/api/routes/ai_stats.py"
      provides: "_AGENT_DISPLAY key updated from 'skeptic_v1' to 'skeptic'"
      contains: "\"skeptic\""
    - path: "tests/unit/services/test_alpha_swarm.py"
      provides: "swarm tests reference agent_id 'skeptic' and the updated _SWARM_AGENT_TO_TRANSFORM expected dict"
      contains: "skeptic"
    - path: "tests/unit/services/test_skeptic_agent.py"
      provides: "Updated tests for the migrated evaluator (agent_id, result_type, transfer fn, neutral on failure)"
      contains: "skeptic"
  key_links:
    - from: "src/intelligence/ai/alpha/skeptic_agent.py:_compute"
      to: "BaseAIWorker._run_typed"
      via: "await self._run_typed(context, prompt=..., system=...)"
      pattern: "_run_typed"
    - from: "src/intelligence/ai/alpha/skeptic_agent.py"
      to: "src.intelligence.ai.alpha.skeptic_prompts.SkepticResult"
      via: "result_type: ClassVar = SkepticResult"
      pattern: "result_type"
    - from: "services/alpha_swarm.py:_SWARM_AGENT_TO_TRANSFORM"
      to: "SkepticEvaluator.agent_id"
      via: "key 'skeptic'"
      pattern: "skeptic"
---

<objective>
Migrate `SkepticEvaluator` to the typed path as a straight replacement (D-14, D-15, D-16): set `result_type = SkepticResult`, replace the `_llm_generate_structured()` call in `_compute()` with `await self._run_typed(...)`, rename the AGENT-ID from `"skeptic_v1"` to `"skeptic"`, and propagate that agent_id rename across every operational reference (REVIEWS HIGH item 4 - the blast radius is wider than the evaluator file). No parallel class, no feature gate.

Critical distinction (verified by grep): `"skeptic_v1"` appears in two unrelated roles. (1) As the AGENT-ID - renamed to `"skeptic"` everywhere it identifies the agent. (2) As a PROMPT-TEMPLATE VERSION key in `PROMPT_REGISTRY` in `skeptic_prompts.py` and as `prompt_version` history - this is NOT the agent id and is LEFT UNCHANGED. `ACTIVE_VERSION` stays `"skeptic_v2"`; `prompt_version` stays `ACTIVE_VERSION`.

Purpose: Prove the Plan 03/04 infrastructure end to end on the reference alpha agent. pydantic-ai's grammar-constrained structured output is strictly more correct than instructor's post-hoc parse, so commit to it. The transfer function, prompt building, and AlphaSwarm wiring are unchanged - only the LLM transport and the agent_id change.

Output: migrated `SkepticEvaluator`; agent_id rename propagated to `_SWARM_AGENT_TO_TRANSFORM`, `_AGENT_DISPLAY` (ai_stats), and `validate_skeptic.py` help text; updated `test_skeptic_agent.py`, `test_alpha_swarm.py`, and `test_swarm_graduation_loop.py` reflecting the new agent_id and the migrated transfer path. The orphaned `skeptic_v1` shadow_registry row is left to decay (D-15 - no DB migration).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/095-pydantic-ai-agents/095-CONTEXT.md
@.planning/phases/095-pydantic-ai-agents/095-RESEARCH.md
@CLAUDE.md
@src/intelligence/ai/alpha/skeptic_agent.py
@src/intelligence/ai/alpha/skeptic_prompts.py
@src/core/ai/base_agent.py
@services/alpha_swarm.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover full rename scope, then migrate SkepticEvaluator to _run_typed()</name>
  <read_first>
    - Run `grep -rn "skeptic_v1" --include="*.py" .` FIRST and triage every hit into AGENT-ID role (rename to "skeptic") vs PROMPT-VERSION role (leave). Known AGENT-ID hits: skeptic_agent.py:50, alpha_swarm.py:75, ai_stats.py:17, validate_skeptic.py:11/93 (help text), test_alpha_swarm.py (multiple mock agent_ids + expected dict line ~496), test_skeptic_agent.py (agent_id assertions). Known PROMPT-VERSION hits to LEAVE: skeptic_prompts.py:20 (PROMPT_REGISTRY key), skeptic_agent.py:135 (docstring "Used when ACTIVE_VERSION == 'skeptic_v1'"), test_skeptic_prompts_v2.py:151/177 (prompt registry tests), test_skeptic_agent.py:23 (monkeypatch ACTIVE_VERSION to a prompt key).
    - src/intelligence/ai/alpha/skeptic_agent.py (full file - current _compute at 82-129 uses _llm_generate_structured; agent_id="skeptic_v1" at line 50; _SYSTEM_MESSAGE at 25-32)
    - src/intelligence/ai/alpha/skeptic_prompts.py (SkepticResult at 105-128; ACTIVE_VERSION="skeptic_v2"; build_skeptic_prompt at 131-183)
    - src/core/ai/base_agent.py (_run_typed signature and behavior from Plan 04)
    - src/core/ai/evaluator.py (_build_multiplier_output signature - unchanged, still the output builder)
  </read_first>
  <files>src/intelligence/ai/alpha/skeptic_agent.py</files>
  <action>
    Edit `SkepticEvaluator`:
    1. Add `result_type: ClassVar[type[BaseModel]] = SkepticResult` to the class-attribute block (alongside `output_schema`). `SkepticResult` is already imported (line 18). Confirm `ClassVar` is in the typing import (it imports `Any, ClassVar` at line 9); add `BaseModel` to the import only if needed for the annotation.
    2. Change `agent_id = "skeptic_v1"` to `agent_id = "skeptic"` (D-15 - AGENT-ID version suffix removed). Leave `prompt_version = ACTIVE_VERSION` unchanged (still resolves to "skeptic_v2"). Leave `group`, `tiers_needed`, `latency_budget_ms`, `shadow_only`, and `_apply_shadow_mode_config()` unchanged (D-16 - shadow_only is the containment mechanism; no new gate).
    3. Rewrite `_compute()`: keep the existing prompt-building branch (v2 vs v1) producing `prompt`. Replace the `_llm_generate_structured(...)` block (and the `if result is None` neutral block) with:
       `result = await self._run_typed(context, prompt=prompt, system=_SYSTEM_MESSAGE, max_tokens=500)`.
       Do NOT add a None guard - _run_typed returns a validated SkepticResult or raises, and the BaseAIWorker.compute() wrapper converts any raise into a neutral AgentOutput (RESEARCH.md Pattern 4). Keep `# type: ignore[assignment]` on the assignment if the type checker complains about BaseModel vs SkepticResult.
       Then keep the transfer function exactly: `failure_probability = result.failure_probability`; `llm_confidence = result.confidence`; `multiplier = (1.0 - failure_probability) * llm_confidence`; return `self._build_multiplier_output(context=context, multiplier=multiplier, confidence=llm_confidence, payload={"failure_probability": failure_probability, "risk_factors": result.risk_factors, "reasoning": result.reasoning}, prompt_version=ACTIVE_VERSION)`.
    4. Remove the now-dead `logger.warning("skeptic_agent.structured_output_failed", ...)` and its comment block (it belonged to the old None path). Leave `_context_to_dict` and the v1 prompt-adapter path intact, including the docstring at line 135 that references the PROMPT VERSION "skeptic_v1" (that is a prompt-template id, not the agent id - do NOT touch it).
    5. Confirm there is NO SkepticPydanticEvaluator / SkepticComputeAgentV2 class and NO ENABLE_PYDANTIC_SKEPTIC reference anywhere in the file (D-14, D-16).
  </action>
  <verify>`.venv/bin/python -c "from src.intelligence.ai.alpha.skeptic_agent import SkepticEvaluator; from src.intelligence.ai.alpha.skeptic_prompts import SkepticResult, ACTIVE_VERSION; assert SkepticEvaluator.agent_id=='skeptic'; assert SkepticEvaluator.result_type is SkepticResult; assert SkepticEvaluator.prompt_version==ACTIVE_VERSION=='skeptic_v2'; import inspect; src=inspect.getsource(SkepticEvaluator); assert '_run_typed' in src; assert '_llm_generate_structured' not in src; assert 'ENABLE_PYDANTIC_SKEPTIC' not in src; print('ok')"` prints ok</verify>
  <acceptance_criteria>
    - `SkepticEvaluator.agent_id == "skeptic"` and `SkepticEvaluator.result_type is SkepticResult`
    - `SkepticEvaluator.prompt_version == "skeptic_v2"` (prompt version unchanged)
    - `_compute` calls `await self._run_typed(...)` and does NOT call `_llm_generate_structured`
    - The transfer function `(1.0 - failure_probability) * llm_confidence` and `_build_multiplier_output(...)` payload keys are preserved
    - The line-135 docstring referencing the prompt version "skeptic_v1" is preserved (prompt-template id, not agent id)
    - No `SkepticPydanticEvaluator`, `SkepticComputeAgentV2`, or `ENABLE_PYDANTIC_SKEPTIC` anywhere in the file
  </acceptance_criteria>
  <done>SkepticEvaluator is a straight migration: result_type=SkepticResult, _compute uses _run_typed, agent_id="skeptic", prompt_version still "skeptic_v2", transfer function and output builder unchanged, no parallel class, no gate</done>
</task>

<task type="auto">
  <name>Task 2: Propagate the agent_id rename to all operational references</name>
  <read_first>
    - services/alpha_swarm.py (_SWARM_AGENT_TO_TRANSFORM at lines 74-80; _setup() agent construction at 165-170 - confirm SkepticEvaluator(llm_chain=...) needs no change)
    - src/api/routes/ai_stats.py (_AGENT_DISPLAY at lines 16-23 - key "skeptic_v1": "Skeptic")
    - tools/validate_skeptic.py (lines 11 and 93 - help text / example referencing "skeptic_v1")
  </read_first>
  <files>services/alpha_swarm.py</files>
  <action>
    1. services/alpha_swarm.py: In `_SWARM_AGENT_TO_TRANSFORM`, change the key `"skeptic_v1"` to `"skeptic"` (value stays `("swarm_skeptic", 6)`). This mapping is keyed by `agent_id` for LineageRecorder attribution; the rename in Task 1 must be reflected here or skeptic lineage attribution breaks. Do NOT change the `SkepticEvaluator(llm_chain=self._llm_chain)` construction in `_setup()` - the constructor interface is unchanged (D-15).
    2. src/api/routes/ai_stats.py: In `_AGENT_DISPLAY`, change the key `"skeptic_v1"` to `"skeptic"` (value stays `"Skeptic"`). This dict maps agent_id -> display name in the AI stats route; leaving the old key means migrated skeptic rows show as an unknown agent. Leave the other `*_v1` keys (correlation_v1 etc.) untouched - only skeptic migrated in this phase.
    3. tools/validate_skeptic.py: Update the `--agent` example/help text on lines 11 and 93 from `skeptic_v1` to `skeptic` so the operational tooling documents the current agent_id. The `--agent` value is a runtime CLI arg (not a hardcoded constant), so this is a doc-text change with no behavioral break.
  </action>
  <verify>`.venv/bin/python -c "from services.alpha_swarm import _SWARM_AGENT_TO_TRANSFORM as m; assert 'skeptic' in m and 'skeptic_v1' not in m; from src.api.routes.ai_stats import _AGENT_DISPLAY as d; assert 'skeptic' in d and 'skeptic_v1' not in d; import pathlib; t=pathlib.Path('tools/validate_skeptic.py').read_text(); assert 'skeptic_v1' not in t; print('ok')"` prints ok</verify>
  <acceptance_criteria>
    - `_SWARM_AGENT_TO_TRANSFORM` contains key `"skeptic"` mapping to `("swarm_skeptic", 6)` and no `"skeptic_v1"` key
    - `ai_stats._AGENT_DISPLAY` contains key `"skeptic"` -> `"Skeptic"` and no `"skeptic_v1"` key; other `*_v1` keys unchanged
    - `tools/validate_skeptic.py` contains no `"skeptic_v1"` string
    - `SkepticEvaluator(llm_chain=self._llm_chain)` construction in `_setup()` is unchanged
  </acceptance_criteria>
  <done>The agent_id rename is propagated to the swarm transform mapping, the AI-stats display map, and the validate-skeptic tool help text; swarm construction unchanged</done>
</task>

<task type="auto">
  <name>Task 3: Update unit + integration tests for the migration and rename</name>
  <read_first>
    - tests/unit/services/test_skeptic_agent.py (current tests - what they assert about agent_id, parsing, transfer function; line 23 monkeypatches ACTIVE_VERSION to the PROMPT key "skeptic_v1" - that is a prompt version, leave it)
    - tests/unit/services/test_alpha_swarm.py (every `agent_id="skeptic_v1"` mock at lines 67,153,160,190,196,225,231,291,298,387,396,411,418,453,651,670,679,703,749,758 and the expected `_SWARM_AGENT_TO_TRANSFORM` dict at line 496 - all are AGENT-ID role, rename to "skeptic")
    - tests/integration/test_swarm_graduation_loop.py (lines 20,24,27,41,46,52,92,114,117,134 - the integration test enrolls/promotes the skeptic agent_id in shadow_registry)
    - src/intelligence/ai/alpha/skeptic_agent.py (the migrated class from Task 1)
  </read_first>
  <files>tests/unit/services/test_skeptic_agent.py</files>
  <action>
    Update three test files. PRESERVE every `skeptic_v1` that is a PROMPT-VERSION reference (PROMPT_REGISTRY key, ACTIVE_VERSION monkeypatch); RENAME every `skeptic_v1` that is an AGENT-ID to `"skeptic"`.

    A) tests/unit/services/test_skeptic_agent.py: (1) Any test asserting `agent_id == "skeptic_v1"` updates to `"skeptic"`. (2) Do NOT touch line 23's `monkeypatch.setattr(_sp, "ACTIVE_VERSION", "skeptic_v1")` - that selects the v1 PROMPT TEMPLATE, not the agent id. (3) Remove or rewrite tests that exercised the old `_llm_generate_structured` None-path / parse-failure path (that path no longer exists). (4) Add a test that monkeypatches `SkepticEvaluator._run_typed` (async) to return a fixed `SkepticResult(failure_probability=0.4, confidence=0.8, risk_factors=["x"], reasoning="r")`, then `await evaluator._compute(fake_context)` and asserts the returned AgentOutput has `output_type=="multiplier"`, `payload["multiplier"] == pytest.approx((1.0-0.4)*0.8)`, `payload["failure_probability"]==0.4`, `payload["risk_factors"]==["x"]`. (5) Add a neutral-on-failure regression test (REVIEWS suggestion): rather than only mocking `_run_typed` to raise, force the real validation-failure path - monkeypatch the worker's `_llm` chain `generate` to return invalid JSON (e.g. `'{"failure_probability": "not a number"}'`) so pydantic-ai validation fails after retries, then call `await evaluator.compute(fake_context)` (the wrapper, not _compute) and assert a neutral AgentOutput is returned (`output_type=="neutral"`, `error` set). If wiring the full pydantic-ai stack in a unit test is impractical, fall back to monkeypatching `_run_typed` to raise a `pydantic.ValidationError` (constructed via `_Result.model_validate({})` in a try/except) - the point is proving compute() neutralizes a validation failure, not only a generic Exception.

    B) tests/unit/services/test_alpha_swarm.py: Replace every AGENT-ID `agent_id="skeptic_v1"` (and bare `"skeptic_v1"` in called_ids assertions and the expected `_SWARM_AGENT_TO_TRANSFORM` dict at line ~496) with `"skeptic"`. The expected mapping dict must read `"skeptic": ("swarm_skeptic", 6)`.

    C) tests/integration/test_swarm_graduation_loop.py: Replace the AGENT-ID `'skeptic_v1'` enrollment/promotion references with `'skeptic'` so the graduation loop test operates on the current agent_id (the orphaned `skeptic_v1` shadow row is intentionally left to decay per D-15, so the test must drive the NEW id). Update the test function name `test_graduation_loop_promotes_skeptic_v1_end_to_end` to `test_graduation_loop_promotes_skeptic_end_to_end` and all `component_name='skeptic_v1'` SQL literals to `'skeptic'`.

    Build the fake_context with the SignalContext fields the prompt builder and _build_audit_context read (symbol, timeframe, ts, signal_id, i1/i4/i6/i7/smc/bar - reuse any existing fixture in this test file or test_skeptic_prompts_v2.py). Keep tests that validate SkepticResult field coercion.
  </action>
  <verify>`.venv/bin/pytest tests/unit/services/test_skeptic_agent.py tests/unit/services/test_alpha_swarm.py -q` passes; then `grep -rn "skeptic_v1" tests/unit/services/test_alpha_swarm.py tests/integration/test_swarm_graduation_loop.py` returns no AGENT-ID matches (a prompt-version mention, if any, may remain only in test_skeptic_prompts_v2.py which is out of scope)</verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/services/test_skeptic_agent.py tests/unit/services/test_alpha_swarm.py -q` exits 0
    - test_skeptic_agent.py asserts `agent_id == "skeptic"` and a transfer-function test via _compute with a mocked _run_typed
    - test_skeptic_agent.py has a neutral-on-failure test driving a pydantic validation failure (not only a generic mocked exception)
    - test_alpha_swarm.py contains no AGENT-ID `"skeptic_v1"`; the expected `_SWARM_AGENT_TO_TRANSFORM` dict uses `"skeptic"`
    - test_swarm_graduation_loop.py operates on `'skeptic'` (function renamed, all `component_name='skeptic'` SQL); no `'skeptic_v1'` agent-id literal remains
    - test_skeptic_agent.py line ~23 still monkeypatches ACTIVE_VERSION to the prompt-version `"skeptic_v1"` (preserved)
    - No test references `_llm_generate_structured` or a parse-failure None path
  </acceptance_criteria>
  <done>All three test files reflect the migrated evaluator and the agent_id rename: new agent_id, _run_typed-based transfer-function test, validation-failure neutral test, updated swarm mocks/mapping, updated graduation integration test; prompt-version references preserved; suites green</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/services/test_skeptic_agent.py tests/unit/services/test_alpha_swarm.py -q` passes
- `.venv/bin/pytest tests/unit/ -q` shows no regressions across the suite
- `.venv/bin/python -c "from services.alpha_swarm import _SWARM_AGENT_TO_TRANSFORM; assert 'skeptic' in _SWARM_AGENT_TO_TRANSFORM and 'skeptic_v1' not in _SWARM_AGENT_TO_TRANSFORM; print('ok')"` prints ok
- `grep -rn "skeptic_v1" --include="*.py" src/ services/ tools/` returns only PROMPT-VERSION matches (skeptic_prompts.py PROMPT_REGISTRY key + skeptic_agent.py line-135 docstring); no AGENT-ID matches remain
- `.venv/bin/ruff check src/intelligence/ai/alpha/skeptic_agent.py services/alpha_swarm.py src/api/routes/ai_stats.py tools/validate_skeptic.py tests/unit/services/test_skeptic_agent.py tests/unit/services/test_alpha_swarm.py` clean
</verification>

<success_criteria>
SkepticEvaluator runs through _run_typed() with result_type=SkepticResult and agent_id="skeptic", prompt_version still "skeptic_v2", transfer function and AlphaSwarm wiring preserved, no parallel class or feature gate, failures degrade to neutral output (proven against a validation failure), the agent_id rename propagated to the swarm mapping + ai_stats display + validate tool + all tests + the graduation integration test, prompt-version references preserved, tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/095-pydantic-ai-agents/095-05-SUMMARY.md`
</output>
