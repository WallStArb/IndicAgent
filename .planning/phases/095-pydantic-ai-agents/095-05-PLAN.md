---
phase: 095-pydantic-ai-agents
slug: pydantic-ai-agents
plan: 05
type: execute
wave: 4
depends_on: [095-04]
files_modified:
  - src/intelligence/ai/alpha/skeptic_agent.py
  - services/alpha_swarm.py
  - tests/unit/services/test_skeptic_agent.py
autonomous: true
user_setup: []
requirements:
  - AGENT-EXEC-04

must_haves:
  truths:
    - "SkepticEvaluator sets result_type = SkepticResult as a ClassVar and its _compute() calls await self._run_typed(...) instead of _llm_generate_structured()"
    - "SkepticEvaluator.agent_id is 'skeptic' (the 'skeptic_v1' version suffix is removed)"
    - "There is no SkepticPydanticEvaluator / SkepticComputeAgentV2 parallel class and no ENABLE_PYDANTIC_SKEPTIC gate"
    - "The transfer function is unchanged: multiplier = (1.0 - failure_probability) * llm_confidence; output built via _build_multiplier_output"
    - "alpha_swarm._SWARM_AGENT_TO_TRANSFORM maps 'skeptic' (not 'skeptic_v1') and AlphaSwarm._setup() still constructs SkepticEvaluator(llm_chain=...) unchanged"
    - "On LLM/validation failure the swarm still receives a neutral AgentOutput (no crash) via the compute() wrapper"
  artifacts:
    - path: "src/intelligence/ai/alpha/skeptic_agent.py"
      provides: "SkepticEvaluator migrated to _run_typed with agent_id='skeptic'"
      contains: "self._run_typed"
    - path: "services/alpha_swarm.py"
      provides: "transform mapping updated to 'skeptic'"
      contains: "\"skeptic\""
    - path: "tests/unit/services/test_skeptic_agent.py"
      provides: "Updated tests for the migrated evaluator (agent_id, result_type, transfer fn, neutral on failure)"
      contains: "skeptic"
  key_links:
    - from: "src/intelligence/ai/alpha/skeptic_agent.py:_compute"
      to: "BaseAIWorker._run_typed"
      via: "await self._run_typed(context, prompt=..., system=...)"
      pattern: "_run_typed"
    - from: "src/intelligence/ai/alpha/skeptic_agent.py"
      to: "src.intelligence.ai.alpha.skeptic_prompts.SkepticResult"
      via: "result_type: ClassVar = SkepticResult"
      pattern: "result_type"
    - from: "services/alpha_swarm.py:_SWARM_AGENT_TO_TRANSFORM"
      to: "SkepticEvaluator.agent_id"
      via: "key 'skeptic'"
      pattern: "skeptic"
---

<objective>
Migrate `SkepticEvaluator` to the typed path as a straight replacement (D-14, D-15, D-16): set `result_type = SkepticResult`, replace the `_llm_generate_structured()` call in `_compute()` with `await self._run_typed(...)`, rename `agent_id` from `"skeptic_v1"` to `"skeptic"`, and update the `_SWARM_AGENT_TO_TRANSFORM` mapping in `alpha_swarm.py`. No parallel class, no feature gate.

Purpose: Prove the Plan 03/04 infrastructure end to end on the reference alpha agent. pydantic-ai's grammar-constrained structured output is strictly more correct than instructor's post-hoc parse, so commit to it. The transfer function, prompt building, and AlphaSwarm wiring are unchanged — only the LLM transport and the agent_id change.

Output: migrated `SkepticEvaluator`; updated transform mapping; updated `test_skeptic_agent.py` reflecting the new agent_id, result_type, preserved transfer function, and neutral-on-failure behavior. The orphaned `skeptic_v1` shadow_registry row is left to decay (D-15 — no migration).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/095-pydantic-ai-agents/095-CONTEXT.md
@.planning/phases/095-pydantic-ai-agents/095-RESEARCH.md
@CLAUDE.md
@src/intelligence/ai/alpha/skeptic_agent.py
@src/intelligence/ai/alpha/skeptic_prompts.py
@src/core/ai/base_agent.py
@services/alpha_swarm.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate SkepticEvaluator to _run_typed()</name>
  <read_first>
    - src/intelligence/ai/alpha/skeptic_agent.py (full file — current _compute at 82-129 uses _llm_generate_structured; agent_id="skeptic_v1" at line 50; _SYSTEM_MESSAGE at 25-32)
    - src/intelligence/ai/alpha/skeptic_prompts.py (SkepticResult at 105-128; ACTIVE_VERSION="skeptic_v2"; build_skeptic_prompt at 131-183)
    - src/core/ai/base_agent.py (_run_typed signature and behavior from Plan 04)
    - src/core/ai/evaluator.py (_build_multiplier_output signature — unchanged, still the output builder)
  </read_first>
  <files>src/intelligence/ai/alpha/skeptic_agent.py</files>
  <action>
    Edit `SkepticEvaluator`:
    1. Add `from pydantic import BaseModel` import and import is unnecessary if only annotating; add `result_type: ClassVar[type[BaseModel]] = SkepticResult` to the class-attribute block (alongside `output_schema`). `SkepticResult` is already imported (line 18). Add `ClassVar` to the typing import if not present (it imports `Any, ClassVar` already at line 9 — confirm).
    2. Change `agent_id = "skeptic_v1"` to `agent_id = "skeptic"` (D-15 — version suffix removed). Leave `prompt_version = ACTIVE_VERSION` unchanged. Leave `group`, `tiers_needed`, `latency_budget_ms`, `shadow_only`, and `_apply_shadow_mode_config()` unchanged (D-16 — shadow_only is the containment mechanism; no new gate).
    3. Rewrite `_compute()`: keep the existing prompt-building branch (v2 vs v1) producing `prompt`. Replace the `_llm_generate_structured(...)` block (and the `if result is None` neutral block) with:
       `result = await self._run_typed(context, prompt=prompt, system=_SYSTEM_MESSAGE, max_tokens=500)`.
       Do NOT add a None guard — _run_typed returns a validated SkepticResult or raises, and the BaseAIWorker.compute() wrapper converts any raise into a neutral AgentOutput (RESEARCH.md Pattern 4). Keep `# type: ignore[assignment]` on the assignment if the type checker complains about BaseModel vs SkepticResult.
       Then keep the transfer function exactly: `failure_probability = result.failure_probability`; `llm_confidence = result.confidence`; `multiplier = (1.0 - failure_probability) * llm_confidence`; return `self._build_multiplier_output(context=context, multiplier=multiplier, confidence=llm_confidence, payload={"failure_probability": failure_probability, "risk_factors": result.risk_factors, "reasoning": result.reasoning}, prompt_version=ACTIVE_VERSION)`.
    4. Remove the now-dead `logger.warning("skeptic_agent.structured_output_failed", ...)` and its comment block (it belonged to the old None path). Leave `_context_to_dict` and the v1 prompt-adapter path intact (still used for prompt building when ACTIVE_VERSION=="skeptic_v1").
    5. Confirm there is NO SkepticPydanticEvaluator / SkepticComputeAgentV2 class and NO ENABLE_PYDANTIC_SKEPTIC reference anywhere in the file (D-14, D-16).
  </action>
  <verify>`.venv/bin/python -c "from src.intelligence.ai.alpha.skeptic_agent import SkepticEvaluator; from src.intelligence.ai.alpha.skeptic_prompts import SkepticResult; assert SkepticEvaluator.agent_id=='skeptic'; assert SkepticEvaluator.result_type is SkepticResult; import inspect; src=inspect.getsource(SkepticEvaluator); assert '_run_typed' in src; assert '_llm_generate_structured' not in src; assert 'ENABLE_PYDANTIC_SKEPTIC' not in src; print('ok')"` prints ok</verify>
  <acceptance_criteria>
    - `SkepticEvaluator.agent_id == "skeptic"` and `SkepticEvaluator.result_type is SkepticResult`
    - `_compute` calls `await self._run_typed(...)` and does NOT call `_llm_generate_structured`
    - The transfer function `(1.0 - failure_probability) * llm_confidence` and `_build_multiplier_output(...)` payload keys are preserved
    - No `SkepticPydanticEvaluator`, `SkepticComputeAgentV2`, or `ENABLE_PYDANTIC_SKEPTIC` anywhere in the file
  </acceptance_criteria>
  <done>SkepticEvaluator is a straight migration: result_type=SkepticResult, _compute uses _run_typed, agent_id="skeptic", transfer function and output builder unchanged, no parallel class, no gate</done>
</task>

<task type="auto">
  <name>Task 2: Update alpha_swarm transform mapping for the new agent_id</name>
  <read_first>
    - services/alpha_swarm.py (_SWARM_AGENT_TO_TRANSFORM at lines 74-80; _setup() agent construction at 165-170 — confirm SkepticEvaluator(llm_chain=...) needs no change)
  </read_first>
  <files>services/alpha_swarm.py</files>
  <action>
    In `_SWARM_AGENT_TO_TRANSFORM`, change the key `"skeptic_v1"` to `"skeptic"` (value stays `("swarm_skeptic", 6)`). This mapping is keyed by `agent_id` for LineageRecorder attribution; the rename in Task 1 must be reflected here or skeptic lineage attribution breaks. Do NOT change the `SkepticEvaluator(llm_chain=self._llm_chain)` construction in `_setup()` — the constructor interface is unchanged (D-15). Verify no other `"skeptic_v1"` string literal remains in this file (search and update if any, e.g. comments referencing the old id may stay but functional references must update).
  </action>
  <verify>`.venv/bin/python -c "import ast,io; src=open('services/alpha_swarm.py').read(); assert '\"skeptic\":' in src or \"'skeptic':\" in src; assert 'skeptic_v1' not in src.replace('# ',''); print('ok')"` prints ok (allows historical mentions only in stripped comments if any; functional key is 'skeptic')</verify>
  <acceptance_criteria>
    - `_SWARM_AGENT_TO_TRANSFORM` contains key `"skeptic"` mapping to `("swarm_skeptic", 6)`
    - No functional `"skeptic_v1"` reference remains in services/alpha_swarm.py
    - `SkepticEvaluator(llm_chain=self._llm_chain)` construction in `_setup()` is unchanged
  </acceptance_criteria>
  <done>alpha_swarm's transform mapping uses the new "skeptic" agent_id; swarm construction unchanged</done>
</task>

<task type="auto">
  <name>Task 3: Update SkepticEvaluator unit tests</name>
  <read_first>
    - tests/unit/services/test_skeptic_agent.py (current tests — what they assert about agent_id, parsing, transfer function)
    - tests/unit/services/test_skeptic_prompts_v2.py (sibling prompt tests — leave these alone unless they reference agent_id)
    - src/intelligence/ai/alpha/skeptic_agent.py (the migrated class from Task 1)
  </read_first>
  <files>tests/unit/services/test_skeptic_agent.py</files>
  <action>
    Update `tests/unit/services/test_skeptic_agent.py` to reflect the migration: (1) Any test asserting `agent_id == "skeptic_v1"` updates to `"skeptic"`. (2) Remove or rewrite tests that exercised the old `_llm_generate_structured` None-path / parse-failure path (that path no longer exists). (3) Add/keep a test that monkeypatches `SkepticEvaluator._run_typed` (async) to return a fixed `SkepticResult(failure_probability=0.4, confidence=0.8, risk_factors=["x"], reasoning="r")`, then `await evaluator._compute(fake_context)` and asserts the returned AgentOutput has `output_type=="multiplier"`, `payload["multiplier"] == pytest.approx((1.0-0.4)*0.8)`, and `payload["failure_probability"]==0.4`, `payload["risk_factors"]==["x"]`. (4) Add a test that monkeypatches `_run_typed` to raise, calls `await evaluator.compute(fake_context)` (the wrapper, not _compute) and asserts a neutral AgentOutput is returned (`output_type=="neutral"`, `error` is set) — proving graceful failure. Build the fake_context with the SignalContext fields the prompt builder and _build_audit_context read (symbol, timeframe, ts, signal_id, i1/i4/i6/i7/smc/bar — reuse any existing fixture in this test file or test_skeptic_prompts_v2.py). Keep tests that validate SkepticResult field coercion (those still apply).
  </action>
  <verify>`.venv/bin/pytest tests/unit/services/test_skeptic_agent.py -q` passes</verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/services/test_skeptic_agent.py -q` exits 0
    - Tests assert `agent_id == "skeptic"` (no "skeptic_v1")
    - A test proves the transfer function output via _compute with a mocked _run_typed
    - A test proves compute() returns a neutral AgentOutput when _run_typed raises
    - No test references `_llm_generate_structured` or a parse-failure None path
  </acceptance_criteria>
  <done>test_skeptic_agent.py reflects the migrated evaluator: new agent_id, _run_typed-based transfer-function test, neutral-on-failure test, no dead parse-failure tests; suite green</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/services/test_skeptic_agent.py -q` passes
- `.venv/bin/pytest tests/unit/ -q` shows no regressions across the suite
- `.venv/bin/python -c "from services.alpha_swarm import _SWARM_AGENT_TO_TRANSFORM; assert 'skeptic' in _SWARM_AGENT_TO_TRANSFORM and 'skeptic_v1' not in _SWARM_AGENT_TO_TRANSFORM; print('ok')"` prints ok
- `.venv/bin/ruff check src/intelligence/ai/alpha/skeptic_agent.py services/alpha_swarm.py tests/unit/services/test_skeptic_agent.py` clean
</verification>

<success_criteria>
SkepticEvaluator runs through _run_typed() with result_type=SkepticResult and agent_id="skeptic", transfer function and AlphaSwarm wiring preserved, no parallel class or feature gate, failures degrade to neutral output, transform mapping updated, tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/095-pydantic-ai-agents/095-05-SUMMARY.md`
</output>
