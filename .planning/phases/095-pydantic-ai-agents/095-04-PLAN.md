---
phase: 095-pydantic-ai-agents
slug: pydantic-ai-agents
plan: 04
type: execute
wave: 3
depends_on: [095-03]
files_modified:
  - src/core/ai/base_agent.py
  - tests/unit/core/test_core_ai_base_agent.py
autonomous: true
user_setup: []
requirements:
  - AGENT-EXEC-03
  - AGENT-EXEC-05

must_haves:
  truths:
    - "BaseAIWorker declares result_type: ClassVar[type[BaseModel] | None] = None and _default_max_tokens: ClassVar[int] = 2048"
    - "BaseAIWorker._run_typed() constructs a WorkerContext, builds a per-call LLMAdapter, runs a pydantic_ai.Agent(output_type=result_type), and returns the validated result_type instance"
    - "_run_typed() uses pydantic-ai 1.0 API: output_type= kwarg on Agent and result.output to read the value"
    - "_run_typed() derives its timeout from self._timeout_s (never recomputed) and max_tokens from the arg or _default_max_tokens"
    - "Calling _run_typed() when result_type is None raises RuntimeError immediately"
    - "Agents that never set result_type and never call _run_typed() are byte-for-byte unchanged; existing base_agent tests still pass"
  artifacts:
    - path: "src/core/ai/base_agent.py"
      provides: "result_type ClassVar + _default_max_tokens ClassVar + _run_typed() method on BaseAIWorker"
      contains: "_run_typed"
    - path: "tests/unit/core/test_core_ai_base_agent.py"
      provides: "Tests for _run_typed happy path, RuntimeError on None result_type, timeout/max_tokens derivation, audit context build"
      contains: "_run_typed"
  key_links:
    - from: "src/core/ai/base_agent.py:_run_typed"
      to: "src.core.ai.llm_adapter.make_llm_adapter"
      via: "per-call adapter construction"
      pattern: "make_llm_adapter"
    - from: "src/core/ai/base_agent.py:_run_typed"
      to: "pydantic_ai.Agent"
      via: "Agent(adapter, output_type=self.result_type)"
      pattern: "output_type="
    - from: "src/core/ai/base_agent.py:_run_typed"
      to: "self._build_audit_context"
      via: "audit_context passed into make_llm_adapter"
      pattern: "_build_audit_context"
---

<objective>
Add the universal typed-execution path to `BaseAIWorker`: a `result_type: ClassVar` opt-in, a `_default_max_tokens: ClassVar`, and an `async def _run_typed()` that constructs a `WorkerContext`, builds a per-call `LLMAdapter`, runs a `pydantic_ai.Agent(output_type=self.result_type)`, and returns the validated `result_type` instance.

Purpose: D-10/D-11/D-12/D-13. Universal opt-in for all AI agents (alpha, narrative, risk) without per-subclass base changes. Timeout is always derived from `self._timeout_s` so the latency budget is impossible to forget. Misconfiguration (calling `_run_typed` with `result_type is None`) fails loud with RuntimeError. Agents that never opt in are unchanged (AGENT-EXEC-05).

Output: the three additions on `BaseAIWorker`; unit tests for happy path (mocked adapter/agent), RuntimeError on None, timeout and max_tokens derivation, and audit-context construction. `_llm_generate` and `_llm_generate_structured` are NOT modified.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/095-pydantic-ai-agents/095-CONTEXT.md
@.planning/phases/095-pydantic-ai-agents/095-RESEARCH.md
@CLAUDE.md
@src/core/ai/base_agent.py
@src/core/ai/worker_context.py
@src/core/ai/llm_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add result_type / _default_max_tokens ClassVars and _run_typed() to BaseAIWorker</name>
  <read_first>
    - src/core/ai/base_agent.py (full file — note: ClassVar attrs declared at lines 78-83; __init__ sets self._timeout_s at line 90 and self._llm at line 92; _build_audit_context at lines 192-213; _llm_generate_structured at 268-320 is the pattern to mirror for audit/span but must NOT be modified)
    - .planning/phases/095-pydantic-ai-agents/095-RESEARCH.md (Pattern 3: _run_typed — lines 226-286; Pitfall 2: output_type vs result_type; Pitfall 5: result.output)
    - .venv/bin/python -c "from pydantic_ai import Agent; import inspect; print('run' in dir(Agent))"  (confirm Agent.run and output_type kwarg in the installed version)
  </read_first>
  <files>src/core/ai/base_agent.py</files>
  <action>
    Add to the class-attribute block of `BaseAIWorker` (near lines 78-83): `result_type: ClassVar[type[BaseModel] | None] = None` and `_default_max_tokens: ClassVar[int] = 2048`. Add `ClassVar` to the typing import (line 9 currently imports from typing; `BaseModel` is already imported at line 14). Add docstrings noting result_type is the universal opt-in and _default_max_tokens is the conservative default per CONTEXT.md "compute cost discipline".

    Add a new method `async def _run_typed(self, context, prompt, system, max_tokens=None) -> BaseModel` with annotations `context: "SignalContext"`, `prompt: str`, `system: str`, `max_tokens: int | None = None`. Behavior (D-10 through D-13, RESEARCH Pattern 3):
    1. If `self.result_type is None`: raise `RuntimeError(f"{self.__class__.__name__}.result_type is None — set result_type: ClassVar = YourModel to use _run_typed()")` (D-13, loud failure, no fallback).
    2. Lazy local imports inside the method (avoid module-load cost / circulars): `from pydantic_ai import Agent`, `from src.core.ai.llm_adapter import make_llm_adapter`, `from src.core.ai.worker_context import WorkerContext`, `from uuid import uuid4`.
    3. `_max_tokens = max_tokens if max_tokens is not None else self._default_max_tokens`.
    4. `call_id = str(uuid4())`; `audit_context = self._build_audit_context(context, prompt, call_id)` (reuses existing audit builder — instrumentation preserved).
    5. Build `worker_ctx = WorkerContext(signal_context=context, llm_chain=self._llm)`.
    6. Build `adapter = make_llm_adapter(worker_context=worker_ctx, system=system, max_tokens=_max_tokens, timeout=self._timeout_s, audit_context=audit_context)` (D-11: timeout from self._timeout_s, never recomputed).
    7. Build `agent = Agent(adapter, output_type=self.result_type, retries=1)` per-call (Pitfall 3 — adapter has closured fresh audit_context). Use `output_type=` NOT `result_type=` (Pitfall 2).
    8. `result = await agent.run(prompt)`; `return result.output` (Pitfall 5 — `.output` not `.data`). Return type is the validated result_type instance (D-12 — caller converts to AgentOutput; _run_typed does not wrap).

    Wrap the body (after the RuntimeError guard) in a tracer span `"agent.run_typed"` with attributes ATTR_AGENT_ID / ATTR_SYMBOL / ATTR_TF mirroring `_llm_generate_structured`, setting StatusCode.ERROR + record_exception on raise so failures are observable. Do NOT modify `_llm_generate` or `_llm_generate_structured` (AGENT-EXEC-05 — base class behavior for unmigrated agents stays identical).
  </action>
  <verify>`.venv/bin/python -c "import inspect; from src.core.ai.base_agent import BaseAIWorker; assert 'result_type' in BaseAIWorker.__annotations__ or hasattr(BaseAIWorker,'result_type'); assert BaseAIWorker.result_type is None; assert BaseAIWorker._default_max_tokens==2048; assert inspect.iscoroutinefunction(BaseAIWorker._run_typed); sig=inspect.signature(BaseAIWorker._run_typed); assert set(sig.parameters)>={'context','prompt','system','max_tokens'}; print('ok')"` prints ok</verify>
  <acceptance_criteria>
    - `BaseAIWorker.result_type` defaults to None; `BaseAIWorker._default_max_tokens` is 2048
    - `_run_typed` is an async method with params context, prompt, system, max_tokens (default None)
    - Source uses `Agent(... output_type=self.result_type ...)` and `result.output` (grep confirms both `output_type=` and `.output`)
    - Source raises `RuntimeError` when `self.result_type is None`
    - Source builds `make_llm_adapter(... timeout=self._timeout_s ...)` and `self._build_audit_context(...)`
    - `git diff` shows NO changes to `_llm_generate` (lines ~215-266) or `_llm_generate_structured` (lines ~268-320)
  </acceptance_criteria>
  <done>BaseAIWorker has result_type/_default_max_tokens ClassVars and an _run_typed() that builds a per-call adapter+Agent using pydantic-ai 1.0 API, derives timeout from _timeout_s, preserves audit context, and raises RuntimeError on misconfiguration; legacy LLM methods untouched</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for _run_typed()</name>
  <read_first>
    - src/core/ai/base_agent.py (the additions from Task 1)
    - tests/unit/core/test_core_ai_base_agent.py (existing tests — extend this file; understand how a concrete test subclass of BaseAIWorker is built and how self._llm / self._timeout_s are set)
  </read_first>
  <files>tests/unit/core/test_core_ai_base_agent.py</files>
  <action>
    Extend `tests/unit/core/test_core_ai_base_agent.py` with tests for `_run_typed`. Define a tiny `class _Result(BaseModel)` with one field (e.g. `value: float`). Define a concrete test worker subclass of BaseAIWorker that sets `result_type = _Result`, a dummy agent_id/group, and implements the abstract `_compute` as a no-op. Construct it with a fake `self._llm` (object) and ensure `self._timeout_s` is set.

    Tests (async): (1) Happy path: monkeypatch `src.core.ai.base_agent.make_llm_adapter` (the name imported inside _run_typed — patch where it is looked up, i.e. patch `src.core.ai.llm_adapter.make_llm_adapter` AND ensure the lazy import resolves to the patched object; simplest is to monkeypatch the module attribute on `src.core.ai.llm_adapter`) to return a sentinel adapter, and monkeypatch `pydantic_ai.Agent` so that constructing it records the `output_type` kwarg and its `.run()` returns an object with `.output == _Result(value=0.5)`. Call `await worker._run_typed(ctx, prompt="p", system="s")` and assert the returned object is the `_Result(value=0.5)` and that `Agent` was constructed with `output_type=_Result` and `retries=1`. (2) RuntimeError: a subclass leaving `result_type=None` raises RuntimeError when `_run_typed` is awaited (`pytest.raises(RuntimeError)`). (3) Timeout derivation: assert `make_llm_adapter` was called with `timeout == worker._timeout_s`. (4) max_tokens default: call without max_tokens and assert `make_llm_adapter` received `max_tokens == 2048`; call with `max_tokens=500` and assert it received 500. (5) Audit: assert `make_llm_adapter` received an `audit_context` dict whose `agent_id` matches the worker's agent_id (proves _build_audit_context wiring).

    Use a minimal fake `ctx` exposing `.symbol`, `.timeframe`, `.signal_id` (None), `.group`/`.smc` as needed by `_build_audit_context` (read that method to mirror exactly — it reads context.symbol, context.signal_id, context.timeframe, context.smc). Keep mocks local; do not require real pydantic-ai network calls.
  </action>
  <verify>`.venv/bin/pytest tests/unit/core/test_core_ai_base_agent.py -q` passes (both pre-existing and new tests)</verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/core/test_core_ai_base_agent.py -q` exits 0 with the pre-existing tests still passing (AGENT-EXEC-05 regression guard)
    - A test proves the happy path returns the validated result_type instance and Agent got `output_type=` + `retries=1`
    - A test proves RuntimeError on `result_type is None`
    - A test proves timeout == self._timeout_s was passed to make_llm_adapter
    - A test proves max_tokens defaults to 2048 and respects an explicit override
    - A test proves audit_context carries the correct agent_id
  </acceptance_criteria>
  <done>_run_typed is covered by passing async unit tests for happy path, RuntimeError, timeout/max_tokens derivation, and audit context; existing base_agent tests still green</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/core/test_core_ai_base_agent.py -q` passes
- `.venv/bin/pytest tests/unit/ -q` shows no regressions from base class changes
- `git diff src/core/ai/base_agent.py` shows additions only (ClassVars + _run_typed); no edits to _llm_generate / _llm_generate_structured
- `.venv/bin/ruff check src/core/ai/base_agent.py tests/unit/core/test_core_ai_base_agent.py` clean
</verification>

<success_criteria>
BaseAIWorker exposes result_type/_default_max_tokens ClassVars and a fully-tested _run_typed() using pydantic-ai 1.0 API (output_type=, result.output), per-call adapter construction, timeout from _timeout_s, preserved audit, and RuntimeError on misconfiguration. Unmigrated agents and existing tests are unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/095-pydantic-ai-agents/095-04-SUMMARY.md`
</output>
