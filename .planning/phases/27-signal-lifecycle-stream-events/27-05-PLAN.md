---
phase: 27-signal-lifecycle-stream-events
plan: 05
type: execute
wave: 3
depends_on: []
files_modified: [dashboard/src/lib/types.ts]
autonomous: true
requirements: [SLES-03]
user_setup: []

must_haves:
  truths:
    - "SignalData type includes resolved, outcome, exit_price, and signal_id fields"
    - "resolved is optional boolean (undefined for live signals)"
    - "outcome is optional string with 8-class values or undefined"
    - "exit_price is optional number or undefined"
  artifacts:
    - path: "dashboard/src/lib/types.ts"
      provides: "Extended SignalData interface"
      min_lines: 270
  key_links:
    - from: "SignalData interface"
      to: "resolved signal display logic"
      via: "signal-panel.tsx checks resolved field"
      pattern: "resolved === true"
---

<objective>
Extend `SignalData` TypeScript interface in `dashboard/src/lib/types.ts` to include resolved state fields from terminal lifecycle events.

Purpose: Provide type safety for resolved signal display in dashboard components. The resolved state (true/false), outcome badge text, and exit price must be properly typed so downstream components can render dimmed resolved signals with outcome badges.

Output: SignalData interface extended with resolved, outcome, exit_price fields maintaining backward compatibility.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-03-06-signal-lifecycle-stream-events-design.md
@dashboard/src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SignalData interface with resolved fields</name>
  <files>dashboard/src/lib/types.ts</files>
  <action>
    Locate `SignalData` interface definition (lines ~231-270).

    Note: The design doc (lines 265-269) shows these fields are ALREADY in the current SignalData interface — verify they exist:

    Check for:
    ```typescript
    // Resolved state — set when lifecycle service publishes direction=0 terminal event
    resolved?: boolean;            // true = signal closed, outcome known
    outcome?: string;              // 8-class: "never_activated" | "stopped_at_entry" | "stopped_in_trade" | "target_1" | "target_1_2" | "target_full" | "ttl_expired_behind" | "ttl_expired_ahead"
    exit_price?: number;           // price at which signal was closed
    pnl_r?: number;                // profit/loss in R-multiples (e.g., 2.5R = 2.5x profit)
    ```

    If these fields ARE already present (as of current types.ts), this task is NO-OP — just verify they're correct and no changes needed.

    If they are NOT present, add them to the interface before the closing brace, maintaining optional fields with `?`:

    Add after line 263 (after `signal_id?: string;`):
    ```typescript
    // Resolved state — set when lifecycle service publishes direction=0 terminal event
    resolved?: boolean;            // true = signal closed, outcome known
    outcome?: string;              // 8-class: "never_activated" | "stopped_at_entry" | "stopped_in_trade" | "target_1" | "target_1_2" | "target_full" | "ttl_expired_behind" | "ttl_expired_ahead"
    exit_price?: number;           // price at which signal was closed
    ```

    Also verify `pnl_r?: number;` exists (line ~269) — it should already be there from Phase 16 work.
  </action>
  <verify>
    <automated>grep -E "resolved\?|outcome\?|exit_price\?" dashboard/src/lib/types.ts | grep "SignalData" -A 40</automated>
  </verify>
  <done>SignalData interface includes resolved, outcome, exit_price optional fields with proper JSDoc comments</done>
</task>

</tasks>

<verification>
- SignalData interface has resolved?: boolean field
- SignalData interface has outcome?: string field with 8-class values in comment
- SignalData interface has exit_price?: number field
- Fields are optional (marked with ?) for backward compatibility
- Existing SignalData properties remain unchanged
</verification>

<success_criteria>
- TypeScript compilation succeeds (no type errors in dashboard)
- SignalData interface properly typed for resolved state
- No breaking changes to existing SignalData consumers
</success_criteria>

<output>
After completion, create `.planning/phases/27-signal-lifecycle-stream-events/27-05-SUMMARY.md`
</output>
