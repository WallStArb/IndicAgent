---
phase: 161-controlled-vocabulary-system
plan: 03
type: execute
wave: 2
depends_on: [161-01, 161-02]
files_modified:
  - production/migrations/239_vocabulary_drift_window_apr_key.sql
  - src/config/vocabulary_drift.py
  - tests/unit/test_vocabulary_drift_audit.py
  - scripts/ops/corpus/ops_corpus_pipeline_run.sh
autonomous: true
requirements: []

must_haves:
  truths:
    - "The drift-audit recent-window length is sourced from APR (infra.vocabulary_drift.window_days), never a hardcoded interval literal"
    - "The drift audit runs a bounded, recent-window SELECT DISTINCT per column-backed namespace (never a full-hypertable scan)"
    - "regime_cross_sectional_equity and regime_cross_sectional_rates queries each scope by regime_group ('equity' / 'rates')"
    - "The regime_hmm query filters WHERE regime <> '' (empty-string placeholder is not a code)"
    - "An unregistered live code (data-superset) writes a monitor_type='vocabulary_drift' row to integrity_monitor and emits a loud OTel counter + logger.error"
    - "A SELECT DISTINCT regime_group guard flags any regime_group the registry does not know"
    - "An empty observed set is treated as source-idle (skip, log info), never as mass deprecation"
    - "The oneshot is chained into ops_corpus_pipeline_run.sh after the final step, non-blocking"
  artifacts:
    - path: "production/migrations/239_vocabulary_drift_window_apr_key.sql"
      provides: "APR key infra.vocabulary_drift.window_days (default 30, provenance [initial_estimate])"
      contains: "infra.vocabulary_drift.window_days"
    - path: "src/config/vocabulary_drift.py"
      provides: "Importable drift-audit module + thin oneshot CLI entrypoint"
      contains: "monitor_type"
    - path: "tests/unit/test_vocabulary_drift_audit.py"
      provides: "Pure-Python tests for '' filtering, regime_group scoping, and observed-vs-registered comparison"
      contains: "def test_"
  key_links:
    - from: "src/config/vocabulary_drift.py"
      to: "integrity_monitor"
      via: "INSERT monitor_type='vocabulary_drift'"
      pattern: "INSERT INTO integrity_monitor"
    - from: "src/config/vocabulary_drift.py"
      to: "infra.vocabulary_drift.window_days"
      via: "ConfigService.get_sync at oneshot startup, bound as a query parameter"
      pattern: "infra.vocabulary_drift.window_days"
    - from: "scripts/ops/corpus/ops_corpus_pipeline_run.sh"
      to: "src/config/vocabulary_drift.py"
      via: "oneshot CLI invocation after alpha_publisher"
      pattern: "vocabulary_drift"
---

<objective>
Build the column-backed drift audit as an importable module (`src/config/vocabulary_drift.py`)
plus a thin D-06 oneshot CLI, persisting results to `integrity_monitor`
(`monitor_type='vocabulary_drift'`), and chain the oneshot into
`scripts/ops/corpus/ops_corpus_pipeline_run.sh`. Not wired into any systemd daemon/timer (D-09;
every auditor-family unit on this host is verifiably dead — see the Fable review, which supersedes
RESEARCH.md's Open Question 2 / Alternatives bar_auditor row / Assumptions A1-A2).

Purpose: Make silent taxonomy drift loud — a live column emitting a code the registry never heard
of fires an alert, honoring "never drop data that could contain signal."
Output: APR migration + drift module + oneshot + pure-Python tests + one non-blocking pipeline hook.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/161-controlled-vocabulary-system-planned/161-CONTEXT.md
@.planning/phases/161-controlled-vocabulary-system-planned/161-PATTERNS.md
@docs/research/fable-2026-07-16-controlled-vocabulary-open-questions-review.md
@src/config/vocabulary_service.py

<interfaces>
<!-- integrity_monitor columns (live, verified): monitor_type TEXT NOT NULL, subject TEXT, -->
<!--   metric_name TEXT NOT NULL, metric_value double precision, threshold_value double precision, -->
<!--   passed BOOLEAN NOT NULL, training_window_end TIMESTAMPTZ, evaluated_at TIMESTAMPTZ DEFAULT now(). -->
<!--   Idempotency UNIQUE: (monitor_type, training_window_end, metric_name, COALESCE(subject,''), evaluated_at). -->
<!-- For vocabulary drift: monitor_type='vocabulary_drift', subject=<namespace>, -->
<!--   metric_name='unregistered_code_count', metric_value=<count>, threshold_value=0, -->
<!--   passed=(count == 0), training_window_end=NULL. -->
<!-- Recent-window length: APR key infra.vocabulary_drift.window_days (default 30), read once via -->
<!--   ConfigService.get_sync("infra.vocabulary_drift.window_days", 30) and bound as a query parameter -->
<!--   ($1 as an interval), NOT a hardcoded interval literal. -->
<!-- Column-backed namespace source columns (all bounded by the APR window param): -->
<!--   regime_hmm  -> feature_vectors.regime (time col bar_ts), filter regime <> '' -->
<!--   regime_cross_sectional_equity -> market_regimes.regime_label (time col ts), regime_group='equity' -->
<!--   regime_cross_sectional_rates  -> market_regimes.regime_label (time col ts), regime_group='rates' -->
<!--   timeframe -> market_data_ohlcv.timeframe ; asset_class -> instruments.contract_details->>'asset_class' ; -->
<!--   tier -> feature_registry.tier -->
<!-- Registered codes come from VocabularyService.codes(namespace) (Plan 02). -->
<!-- Use asyncpg ($1 placeholders), NOT psycopg %s. Oneshot analog: -->
<!--   scripts/ops/alpha/ops_canary_integrity_assert.py (async def main() -> int; sys.exit(asyncio.run(main()))). -->
<!-- APR-load precedent: services/equity_regime_model.py:423 -->
<!--   (ma_window_days = int(cfg.get_sync("alpha.regime.ma_window", 200))). -->
<!-- Migration precedent: production/migrations/219_ic_staleness_apr_key.sql (config_schema + config_state -->
<!--   + config_history triple-INSERT, ON CONFLICT DO NOTHING, [initial_estimate] provenance). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: APR key migration for the drift-audit window</name>
  <files>production/migrations/239_vocabulary_drift_window_apr_key.sql</files>
  <read_first>
    - /home/bg/dev/indicagent/production/migrations/219_ic_staleness_apr_key.sql (the exact config_schema + config_state + config_history triple-INSERT shape, BEGIN/COMMIT, ON CONFLICT DO NOTHING, [initial_estimate] provenance to copy)
  </read_first>
  <action>
    First confirm 239 is still a free migration number (`ls production/migrations/ | grep '^239'` must be empty); if taken, use the next free integer and note it in the SUMMARY.
    Create `production/migrations/239_vocabulary_drift_window_apr_key.sql` seeding the APR key `infra.vocabulary_drift.window_days` (CLAUDE.md: infrastructure performance constants live under `infra.*`). Mirror migration 219 exactly: an `INSERT INTO config_schema` (config_key `infra.vocabulary_drift.window_days`, value_type `int`, default_value `'30'`, min_value 1, max_value 365, description prefixed `[initial_estimate]` and noting "recent-window length (days) for the column-backed vocabulary drift audit; not an ML learning target"), an `INSERT INTO config_state` (config_value `'30'`, version 1), and an `INSERT INTO config_history` (changed_by `'migration_239'`, reason describing the initial estimate), all `ON CONFLICT DO NOTHING`, wrapped in `BEGIN;`/`COMMIT;`.
  </action>
  <verify>
    <automated>psql -U postgres -h localhost -d indicagent -f production/migrations/239_vocabulary_drift_window_apr_key.sql && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT config_value FROM config_state WHERE config_key='infra.vocabulary_drift.window_days'" | grep -qx 30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "infra.vocabulary_drift.window_days" production/migrations/239_vocabulary_drift_window_apr_key.sql` succeeds.
    - The migration inserts into all three of `config_schema`, `config_state`, `config_history` (matching the 219 pattern) and carries `[initial_estimate]` provenance.
    - After applying, `SELECT config_value FROM config_state WHERE config_key='infra.vocabulary_drift.window_days'` returns `30`.
  </acceptance_criteria>
  <done>APR key `infra.vocabulary_drift.window_days` (default 30, `[initial_estimate]`) seeded via a 219-shaped migration; applies cleanly and is readable via ConfigService.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drift-audit module + oneshot CLI</name>
  <files>src/config/vocabulary_drift.py, tests/unit/test_vocabulary_drift_audit.py</files>
  <read_first>
    - /home/bg/dev/indicagent/services/ic_engine.py (lines ~2490-2651: the integrity_monitor INSERT + bounded-check pattern to replicate — note it uses psycopg; convert to asyncpg)
    - /home/bg/dev/indicagent/services/equity_regime_model.py (line ~423: the ConfigService.get_sync APR-load precedent — `ma_window_days = int(cfg.get_sync("alpha.regime.ma_window", 200))`)
    - /home/bg/dev/indicagent/scripts/ops/alpha/ops_canary_integrity_assert.py (lines ~305-353: the `async def main() -> int` + `sys.exit(asyncio.run(main()))` oneshot shape — the sibling already living in ops_corpus_pipeline_run.sh)
    - /home/bg/dev/indicagent/src/config/vocabulary_service.py (import VocabularyService to source registered codes)
    - /home/bg/dev/indicagent/docs/research/fable-2026-07-16-controlled-vocabulary-open-questions-review.md (V1/V2/V3/V5: regime_group scoping, the regime_group guard, module+oneshot decision, empty-set-skip rule)
    - /home/bg/dev/indicagent/.planning/phases/161-controlled-vocabulary-system-planned/161-PATTERNS.md (vocabulary_drift.py pattern assignment, queries, oneshot shape)
  </read_first>
  <behavior>
    - Test: the observed-vs-registered comparison core returns the set of observed codes NOT in the registered set (the data-superset), given two plain code lists — no DB.
    - Test: the regime_hmm observed-code extractor drops the empty string '' from an input list containing it (Finding 3).
    - Test: the regime_group guard returns the set of observed regime_group values not in the registered qualifier set {'equity','rates'} (V2).
    - Test: an empty observed set yields an empty data-superset and is classified source-idle (skip), not deprecation (V5).
  </behavior>
  <action>
    Create `src/config/vocabulary_drift.py` as an importable module. Factor the pure comparison logic into standalone functions (testable with literal lists, no DB): e.g. `unregistered_codes(observed: Iterable[str], registered: Iterable[str]) -> set[str]` (returns observed − registered), and a guard `unregistered_groups(observed_groups, registered_groups) -> set[str]`. Keep all DB I/O in a thin async runner.
    Source the recent-window length from APR — never a hardcoded interval literal (CLAUDE.md APR mandate: tunable periods must be APR-backed). At oneshot startup read `window_days = int(cfg.get_sync("infra.vocabulary_drift.window_days", 30))` (ConfigService, seeded by Task 1's migration 239; precedent equity_regime_model.py:423) and thread it into `run_drift_audit` as an argument. Each bounded query binds the window as an asyncpg parameter, e.g. `WHERE bar_ts > now() - ($1 || ' days')::interval` with `$1 = str(window_days)` (or pass a `timedelta`/interval bound param) — do NOT interpolate or hardcode `interval '30 days'`.
    The async runner, e.g. `async def run_drift_audit(pool, vocab: VocabularyService, window_days: int) -> int` (returns count of namespaces with drift), iterates the column-backed namespaces and for each runs a BOUNDED recent-window `SELECT DISTINCT` (mandatory — never a full-table distinct scan; addresses the DoS threat T-161-02), with the window supplied by the APR parameter above:
    - regime_hmm: `SELECT DISTINCT regime FROM feature_vectors WHERE bar_ts > now() - ($1 || ' days')::interval AND regime <> ''`.
    - regime_cross_sectional_equity: `SELECT DISTINCT regime_label FROM market_regimes WHERE ts > now() - ($1 || ' days')::interval AND regime_group = 'equity'`.
    - regime_cross_sectional_rates: same but `regime_group = 'rates'`.
    - timeframe: `SELECT DISTINCT timeframe FROM market_data_ohlcv WHERE timestamp > now() - ($1 || ' days')::interval`.
    - asset_class: `SELECT DISTINCT contract_details->>'asset_class' FROM instruments WHERE is_active = true`.
    - tier: `SELECT DISTINCT tier FROM feature_registry`.
    Use asyncpg `$1` parameterization for the window value and any other interpolated value (never string-format SQL). For each namespace, compute `unregistered_codes(observed, vocab.codes(namespace))`. If observed set is empty → log info "source idle, skip" and do NOT write a deprecation row (V5). Otherwise write one `integrity_monitor` row: `monitor_type='vocabulary_drift'`, `subject=<namespace>`, `metric_name='unregistered_code_count'`, `metric_value=len(unregistered)`, `threshold_value=0`, `passed=(len(unregistered)==0)`, `training_window_end=NULL`, using the ON CONFLICT idempotency clause matching the live unique index `(monitor_type, training_window_end, metric_name, COALESCE(subject,''), evaluated_at)`. When `unregistered` is non-empty, emit a loud alert: an OTel counter `.add(1, {...})` (from src/observability/metrics.py) AND `logger.error(...)` naming the namespace and the unregistered codes.
    Add the V2 regime_group guard: `SELECT DISTINCT regime_group FROM market_regimes WHERE ts > now() - ($1 || ' days')::interval`, compare via `unregistered_groups(...)` against the registered qualifier set derived from the `regime_cross_sectional_*` namespace suffixes ({'equity','rates'}); any unknown group fires the same loud-alert + integrity_monitor row shape (`subject='regime_group'`).
    Add a thin oneshot CLI: `async def main() -> int` that builds Settings + an asyncpg pool + a ConfigService (initialize, read the window key) + a VocabularyService (initialize), runs `run_drift_audit(pool, vocab, window_days)`, closes resources, and returns 0 (audit is observability-only, never a hard gate — return non-zero only on an actual runtime error, not on detected drift); `if __name__ == "__main__": sys.exit(asyncio.run(main()))`. Optionally emit `JOB_COMPLETED_TOTAL` with `job="vocabulary-drift-audit"` for OTel consistency (no systemd unit consumes it — informational only per D-09).
    Write `tests/unit/test_vocabulary_drift_audit.py` covering the four behaviors above against literal fixtures (no DB, no pool).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_vocabulary_drift_audit.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_vocabulary_drift_audit.py` passes all four behaviors.
    - The window is APR-sourced: `grep -q 'get_sync("infra.vocabulary_drift.window_days"' src/config/vocabulary_drift.py` succeeds.
    - No hardcoded window literal: `grep -c "interval '30 days'" src/config/vocabulary_drift.py` returns 0 (grep -L confirms the literal is absent); the window reaches every bounded query as a `$1`/parameter binding, not an inline literal.
    - Every source-column query on a hypertable is bounded by the APR window parameter: `grep -c "now() - (\$1" src/config/vocabulary_drift.py` is ≥ 4 (regime_hmm, both cross_sectional, timeframe, regime_group guard). Verify by inspection that feature_vectors/market_regimes/market_data_ohlcv queries all carry the parameterized time-window predicate.
    - `grep -qE "regime_group = '(equity|rates)'" src/config/vocabulary_drift.py` succeeds for BOTH qualifiers (per-namespace scoping present).
    - `grep -q "regime <> ''" src/config/vocabulary_drift.py` succeeds (empty-string filter present).
    - `grep -q "monitor_type" src/config/vocabulary_drift.py` and the INSERT targets `integrity_monitor` with `metric_name='unregistered_code_count'`.
    - `grep -q "regime_group" src/config/vocabulary_drift.py` shows the DISTINCT regime_group guard is present.
    - No psycopg `%s` SQL parameterization: `grep -c "cur.execute\|%s" src/config/vocabulary_drift.py` returns 0 (asyncpg `$1` only).
  </acceptance_criteria>
  <done>Importable drift module with an APR-sourced (never hardcoded) recent-window, bounded per-namespace queries, regime_group scoping + guard, '' filter, empty-set skip, integrity_monitor persistence, loud alert on data-superset, and a non-gating oneshot CLI; unit tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Chain the drift oneshot into the corpus pipeline script</name>
  <files>scripts/ops/corpus/ops_corpus_pipeline_run.sh</files>
  <read_first>
    - /home/bg/dev/indicagent/scripts/ops/corpus/ops_corpus_pipeline_run.sh (existing conventions: PYTHON=".venv/bin/python", LOG_DIR="logs/corpus_pipeline", run_step vs. bare-command usage, the final `run_step 8 "alpha_publisher"` block and the Summary section that follows)
    - /home/bg/dev/indicagent/.planning/phases/161-controlled-vocabulary-system-planned/161-PATTERNS.md (ops script append pattern — non-blocking, not run_step, not a hard gate)
    - /home/bg/dev/indicagent/src/config/vocabulary_drift.py (the module the oneshot invokes — read after Task 2)
  </read_first>
  <action>
    After the `run_step 8 "alpha_publisher" ...` block and BEFORE the `# Summary` banner, append the drift-audit invocation as a plain, NON-BLOCKING command (not wrapped in `run_step`, which halts the pipeline on non-zero exit, and not modeled on `check_canary_integrity`, which is a hard gate). Per D-09 the audit is observability-only and must never block `alpha_publisher`'s completion. Use the form: run `"$PYTHON" -m src.config.vocabulary_drift` (or the module's script path if it exposes one), tee its output to `"$LOG_DIR/vocabulary_drift_$(date +%Y%m%d_%H%M%S).log"`, and suffix `|| true` so a non-zero exit is swallowed. Add a one-line comment above it explaining it writes a loud integrity_monitor row and never gates the pipeline.
  </action>
  <verify>
    <automated>bash -n scripts/ops/corpus/ops_corpus_pipeline_run.sh && grep -q "vocabulary_drift" scripts/ops/corpus/ops_corpus_pipeline_run.sh && grep -q "vocabulary_drift.*|| true" scripts/ops/corpus/ops_corpus_pipeline_run.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/ops/corpus/ops_corpus_pipeline_run.sh` reports no syntax error.
    - The drift invocation appears AFTER the `run_step 8 "alpha_publisher"` line and before the `# Summary` banner (verify ordering by line number).
    - The invocation is non-blocking: the line ends with `|| true` and is NOT wrapped in `run_step`.
    - The invocation tees to a timestamped log under `$LOG_DIR`.
    - Phase-end verification: run the oneshot once manually against the live DB (`.venv/bin/python -m src.config.vocabulary_drift`); it exits 0 and a `SELECT count(*) FROM integrity_monitor WHERE monitor_type='vocabulary_drift'` returns ≥ 1.
  </acceptance_criteria>
  <done>Drift oneshot chained non-blockingly into the corpus pipeline after the final step; manual phase-end run writes a vocabulary_drift row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live source columns → drift audit | Reads DB column contents; the audit compares them against the registry (no untrusted external input) |
| drift audit → integrity_monitor | Writes fact rows via parameterized asyncpg INSERT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-02 | Denial of Service | unbounded SELECT DISTINCT full-hypertable scan | mitigate | Every source-column query carries a `WHERE <time_col> > now() - ($1 || ' days')::interval` bound whose window is the APR key `infra.vocabulary_drift.window_days` (regime_hmm/feature_vectors, market_regimes, market_data_ohlcv); no full-table distinct scan |
| T-161-05 | Tampering | asyncpg query construction | mitigate | All interpolated values (including the window) use `$1` parameterization; regime_group qualifiers are code-literals, never derived from external input |
| T-161-06 | Repudiation | silent drift going unlogged | mitigate | Data-superset writes an integrity_monitor row + OTel counter + logger.error; regime_group guard catches unregistered qualifiers so no new taxonomy drifts past both scoped queries |
| T-161-SC | Tampering | package installs | accept | No new packages (asyncpg/structlog/OTel metrics already vendored) |
</threat_model>

<verification>
- `psql -f production/migrations/239_vocabulary_drift_window_apr_key.sql` applies; `infra.vocabulary_drift.window_days` = 30 in config_state.
- `.venv/bin/pytest tests/unit/test_vocabulary_drift_audit.py -q` green.
- `bash -n scripts/ops/corpus/ops_corpus_pipeline_run.sh` clean; `vocabulary_drift` invocation present, non-blocking, after step 8.
- Phase-end: one manual oneshot run writes ≥ 1 `monitor_type='vocabulary_drift'` row to integrity_monitor.
</verification>

<success_criteria>
- Drift-audit recent-window is APR-sourced (`infra.vocabulary_drift.window_days`), bound as a query parameter — no hardcoded interval literal anywhere in the module.
- Drift audit runs bounded per-namespace queries with correct regime_group scoping, '' filter, regime_group guard, and empty-set skip.
- Data-superset drift is loud (integrity_monitor row + OTel counter + logger.error).
- Oneshot chained non-blockingly into the corpus pipeline; no new systemd unit created.
</success_criteria>

<output>
Create `.planning/phases/161-controlled-vocabulary-system-planned/161-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
