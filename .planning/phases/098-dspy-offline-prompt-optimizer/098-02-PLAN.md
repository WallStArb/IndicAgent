---
phase: 098-dspy-offline-prompt-optimizer
plan: 02
type: execute
wave: 2
depends_on: ["098-01"]
files_modified:
  - src/intelligence/optimization/__init__.py
  - src/intelligence/optimization/dspy_optimizer.py
  - src/intelligence/optimization/agent_signatures.py
autonomous: true
requirements: [OPT-01, OPT-02]

must_haves:
  truths:
    - "DSPyOptimizer reads labeled (prompt, parse_success, outcome) rows from llm_calls where outcome IS NOT NULL"
    - "Each agent with >= 500 labeled rows is compiled independently; ineligible agents are skipped and logged with their exact row count"
    - "When zero agents are eligible, the optimizer emits job_completed_total{status=skipped_data_gate} and returns cleanly"
    - "Each compiled DSPy program is serialized and written to prompt_versions as a candidate JSONB row"
    - "An ineligible or failing agent never aborts the loop for other agents"
  artifacts:
    - path: "src/intelligence/optimization/dspy_optimizer.py"
      provides: "DSPyOptimizer batch class: data gate, BootstrapFewShot compile, JSONB write"
      contains: "class DSPyOptimizer"
      min_lines: 120
    - path: "src/intelligence/optimization/agent_signatures.py"
      provides: "AGENT_SIGNATURES registry mapping agent_id -> (PROMPT_REGISTRY module, dspy.Signature)"
      contains: "AGENT_SIGNATURES"
    - path: "src/intelligence/optimization/__init__.py"
      provides: "package marker"
  key_links:
    - from: "src/intelligence/optimization/dspy_optimizer.py"
      to: "llm_calls"
      via: "asyncpg SELECT WHERE outcome IS NOT NULL"
      pattern: "outcome IS NOT NULL"
    - from: "src/intelligence/optimization/dspy_optimizer.py"
      to: "prompt_versions"
      via: "asyncpg INSERT ... ON CONFLICT (version_tag) DO NOTHING"
      pattern: "INSERT INTO prompt_versions"
    - from: "src/intelligence/optimization/dspy_optimizer.py"
      to: "dspy.BootstrapFewShot"
      via: "optimizer.compile(student, trainset=...)"
      pattern: "BootstrapFewShot"
---

<objective>
Build the `DSPyOptimizer` core class — the compile-and-store path. This is the heart of OPT-01 (read labeled tuples, compile offline) and the write half of OPT-02 (store compiled variant in prompt_versions). The class mirrors the `MLTrainer` batch-class pattern exactly: a `BaseDaemon` subclass with `_setup`/`_run`/`_teardown`, a per-agent data gate, and asyncpg DB access.

Purpose: Produce optimized few-shot prompt variants per agent and persist them as `candidate` rows, with strict separation of concerns — this class never touches the live inference path (DAG Invariant 3).
Output: `src/intelligence/optimization/dspy_optimizer.py` (DSPyOptimizer class), `agent_signatures.py` (per-agent DSPy Signature registry), `__init__.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/098-dspy-offline-prompt-optimizer/098-CONTEXT.md
@.planning/phases/098-dspy-offline-prompt-optimizer/098-RESEARCH.md
@.planning/phases/098-dspy-offline-prompt-optimizer/098-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent_signatures registry</name>
  <files>src/intelligence/optimization/agent_signatures.py</files>
  <read_first>
    - src/intelligence/ai/alpha/correlation_prompts.py (ACTIVE_VERSION="correlation_v1", PROMPT_REGISTRY structure, output fields coherence_score/confidence/reasoning)
    - src/intelligence/ai/alpha/skeptic_prompts.py (ACTIVE_VERSION="skeptic_v2")
    - src/intelligence/ai/alpha/counterfactual_prompts.py (ACTIVE_VERSION="counterfactual_v1")
    - src/intelligence/ai/alpha/regime_coherence_prompts.py (ACTIVE_VERSION="regime_coherence_v1")
    - src/intelligence/ai/narrative/narrative_prompts.py (ACTIVE_VERSION="narrative_v1")
  </read_first>
  <action>
    Create `src/intelligence/optimization/agent_signatures.py`. Define one `dspy.Signature` subclass per LLM agent. Per RESEARCH open-question 1, use a single `prompt_context: str = dspy.InputField(...)` input field for each (the optimizer selects among examples; rich field decomposition adds no value for BootstrapFewShot). Each Signature's OutputFields must match that agent's parsed output keys (e.g. correlation: `coherence_score: float`, `confidence: float`, `reasoning: str`). Use the agent's `PROMPT_REGISTRY[ACTIVE_VERSION]` first sentence as the Signature docstring (instructions).
    Define module-level `AGENT_SIGNATURES: dict[str, type[dspy.Signature]]` mapping each `agent_id` (the ACTIVE_VERSION string used as agent_id, e.g. `"correlation_v1"`, `"skeptic_v2"`, `"counterfactual_v1"`, `"regime_coherence_v1"`, `"narrative_v1"`) to its Signature class. This registry is the explicit, no-filesystem-scan list of optimizable agents (mirrors the AGENT_MODULES philosophy).
    Do NOT modify any `*_prompts.py` file (D-04: DB is the only output).
  </action>
  <verify>
    `.venv/bin/python -c "from src.intelligence.optimization.agent_signatures import AGENT_SIGNATURES; print(sorted(AGENT_SIGNATURES))"`
  </verify>
  <acceptance_criteria>
    - `src/intelligence/optimization/agent_signatures.py` defines `AGENT_SIGNATURES` as a dict
    - Importing AGENT_SIGNATURES exits 0 and contains keys `correlation_v1`, `skeptic_v2`, `counterfactual_v1`, `regime_coherence_v1`, `narrative_v1`
    - Each value is a subclass of `dspy.Signature` with at least one `dspy.InputField` named `prompt_context`
    - No `*_prompts.py` file under src/intelligence/ai/ is modified (git diff shows none)
  </acceptance_criteria>
  <done>AGENT_SIGNATURES maps every LLM agent_id to a dspy.Signature with a prompt_context input and that agent's output fields.</done>
</task>

<task type="auto">
  <name>Task 2: Implement DSPyOptimizer class — gate, compile, store</name>
  <files>src/intelligence/optimization/__init__.py, src/intelligence/optimization/dspy_optimizer.py</files>
  <read_first>
    - src/intelligence/services/ml_trainer.py (BaseDaemon subclass pattern: __init__, _setup, _run top-level exception swallow, _teardown, asyncpg pool via create_db_pool, _write_report style)
    - src/intelligence/optimization/agent_signatures.py (AGENT_SIGNATURES registry — created in Task 1)
    - src/observability/metrics.py (JOB_COMPLETED_TOTAL is already registered — import, do not re-register)
    - production/migrations/115_prompt_versions.sql (prompt_versions column names/types — Plan 01)
    - CLAUDE.md (asyncpg JSONB rule: pass dict not json.dumps; UTC-only timestamps; structlog never event= kwarg)
  </read_first>
  <action>
    Create `src/intelligence/optimization/__init__.py` (empty package marker) and `src/intelligence/optimization/dspy_optimizer.py`.
    Define `class DSPyOptimizer(BaseDaemon)`:
    - `__init__(self, settings: Settings)`: call `setup_service_logging("logs/dspy_optimizer.log")`, `super().__init__("DSPyOptimizer")`, store `self.settings`, init `self._pool: asyncpg.Pool | None = None`.
    - `async def _setup(self)`: `self._pool = await create_db_pool(self.settings.database_url)`.
    - `async def _teardown(self)`: close pool if set.
    - `async def _run(self)`: wrap `await self._run_optimization()` in try/except that logs `dspy_optimizer.error_top_level` via `logger.exception` and returns (oneshot exits 0).
    - Module constant `_DATA_GATE_MIN = 500`.
    - `async def _check_data_gate(self, agent_id) -> int`: `SELECT COUNT(*) FROM llm_calls WHERE agent_id = $1 AND outcome IS NOT NULL`; return int.
    - `async def _fetch_trainset_rows(self, agent_id) -> list`: SELECT call_id, prompt, response, parse_success, outcome, regime, called_at FROM llm_calls WHERE agent_id=$1 AND outcome IS NOT NULL AND called_at >= NOW() - INTERVAL '90 days' ORDER BY called_at DESC LIMIT 2000.
    - `def _configure_dspy_lm(self)`: build `dspy.LM(model=f"ollama_chat/{self.settings.ollama_model}", api_base="http://localhost:11434", api_key="", timeout=120, max_retries=2)` and `dspy.configure(lm=lm)`. (Discretion: same OLLAMA_MODEL as production per RESEARCH.)
    - `def _compile_agent(self, agent_id, signature_cls, rows) -> dict`: build `student = dspy.Predict(signature_cls)`; build trainset of `dspy.Example(prompt_context=row["prompt"]).with_inputs("prompt_context")` for rows; define `parse_success_metric(example, pred, trace=None) -> bool` that evaluates the PREDICTED output fields (try float() coercion of numeric OutputFields into [0,1], return False on AttributeError/ValueError/TypeError) — NOT comparing against example.parse_success (RESEARCH pitfall 2); run `BootstrapFewShot(metric=..., max_bootstrapped_demos=4, max_labeled_demos=16, max_rounds=1).compile(student, trainset=trainset)`; serialize via `compiled.save(tmp_json_path)` then `json.loads(Path(tmp).read_text())`, unlink temp; return the compiled dict.
    - `async def _store_candidate(self, agent_id, compiled_dict) -> str`: build `version_tag = f"{agent_id}_dspy_{datetime.now(UTC):%Y%m%d}_{datetime.now(UTC):%H}"` (hour suffix guards same-day re-run UNIQUE collision per RESEARCH pitfall 7); `INSERT INTO prompt_versions (agent_id, version_tag, compiled_prompt, status) VALUES ($1,$2,$3,'candidate') ON CONFLICT (version_tag) DO NOTHING` passing `compiled_dict` as a dict (asyncpg JSONB — no json.dumps); return version_tag.
    - `async def _run_optimization(self)`: iterate `AGENT_SIGNATURES.items()`; for each, call `_check_data_gate`, log `dspy_optimizer.gate_check` with agent_id+row_count+required; collect per_agent_counts. Eligible = count >= _DATA_GATE_MIN. If no eligible agents: log `dspy_optimizer.gate_skip_all` with agent_counts=per_agent_counts and emit `JOB_COMPLETED_TOTAL.add(1, {"job":"dspy-optimizer","status":"skipped_data_gate"})`, then return. Otherwise, configure DSPy LM once, then for each eligible agent: wrap compile+store in try/except (log `dspy_optimizer.agent_error` with agent_id, continue — one agent never blocks another per D-03); on success log `dspy_optimizer.compile_complete` (agent_id, duration_s, n_demos) and `dspy_optimizer.variant_written` (agent_id, version_tag).
    Use `logger = structlog.get_logger(__name__)`. Never pass `event=` kwarg.
  </action>
  <verify>
    `.venv/bin/python -c "from src.intelligence.optimization.dspy_optimizer import DSPyOptimizer"`
    `.venv/bin/ruff check src/intelligence/optimization/`
  </verify>
  <acceptance_criteria>
    - `src/intelligence/optimization/dspy_optimizer.py` contains `class DSPyOptimizer` and `class DSPyOptimizer(BaseDaemon)` inheritance
    - File contains module constant `_DATA_GATE_MIN = 500`
    - The gate query contains `outcome IS NOT NULL`
    - The store method contains `INSERT INTO prompt_versions` and `ON CONFLICT (version_tag) DO NOTHING`
    - `compiled_dict` is passed to the asyncpg placeholder WITHOUT a `json.dumps()` call (grep shows no json.dumps wrapping the JSONB param)
    - The compile method references `BootstrapFewShot` and `max_bootstrapped_demos=4`
    - The metric function does NOT compare against `example.parse_success`
    - The skip-all branch emits `JOB_COMPLETED_TOTAL.add(1, {"job": "dspy-optimizer", "status": "skipped_data_gate"})`
    - `version_tag` construction uses `datetime.now(UTC)` (no datetime.now() or datetime.utcnow())
    - `from src.intelligence.optimization.dspy_optimizer import DSPyOptimizer` exits 0
    - `ruff check src/intelligence/optimization/` exits 0
  </acceptance_criteria>
  <done>DSPyOptimizer gates per agent, skips ineligible ones with logged counts, compiles eligible agents independently with BootstrapFewShot, and writes each compiled program as a candidate JSONB row; zero-eligible exits cleanly with skipped_data_gate.</done>
</task>

</tasks>

<verification>
- DSPyOptimizer imports cleanly and ruff is green
- Gate query filters on `outcome IS NOT NULL`; gate threshold is 500
- Compiled program serialized via DSPy save then stored as dict (asyncpg JSONB)
- Metric evaluates predicted output, not the stored label
- Per-agent try/except isolation present (D-03)
</verification>

<success_criteria>
- DSPyOptimizer reads labeled rows, compiles per eligible agent via BootstrapFewShot, writes candidate rows to prompt_versions
- Ineligible agents skipped + logged; zero-eligible emits skipped_data_gate
- No live inference path touched; no prompt file modified
</success_criteria>

<output>
After completion, create `.planning/phases/098-dspy-offline-prompt-optimizer/098-02-SUMMARY.md`
</output>
