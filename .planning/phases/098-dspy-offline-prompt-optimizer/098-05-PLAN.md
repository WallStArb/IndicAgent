---
phase: 098-dspy-offline-prompt-optimizer
plan: 05
type: execute
wave: 4
depends_on: ["098-02", "phase_096"]
deferred_until: phase_096
files_modified:
  - src/intelligence/ai/base_group_service.py
  - src/intelligence/optimization/prompt_loader.py
  - tests/unit/intelligence/optimization/test_prompt_loader.py
autonomous: true
requirements: [OPT-02]

must_haves:
  truths:
    - "At BaseSwarmCoordinator startup, the active compiled_prompt for each agent is loaded from prompt_versions exactly once"
    - "The loaded compiled prompt is injected into each agent via the Phase 096 AgentDependencies injection point"
    - "No prompt_versions read occurs in the live per-call inference path (DAG Invariant 3)"
    - "An agent with no active prompt_version row falls back to its file-based ACTIVE_VERSION default without error"
    - "A new active version takes effect only on the next service restart"
  artifacts:
    - path: "src/intelligence/optimization/prompt_loader.py"
      provides: "load_active_compiled_prompts(pool) -> dict[agent_id, compiled_dict]"
      contains: "status = 'active'"
    - path: "src/intelligence/ai/base_group_service.py"
      provides: "startup injection call wired into _setup()"
  key_links:
    - from: "src/intelligence/ai/base_group_service.py"
      to: "prompt_versions"
      via: "load_active_compiled_prompts during _setup, once per startup"
      pattern: "load_active_compiled_prompts"
    - from: "load_active_compiled_prompts"
      to: "AgentDependencies"
      via: "compiled_prompt injected into agent dependencies"
      pattern: "compiled_prompt"
---

<objective>
Wire the inference-time consumption of compiled prompts (D-06): at `BaseSwarmCoordinator._setup()`, load the `active` compiled_prompt per agent from `prompt_versions` once and inject it via the Phase 096 `AgentDependencies` mechanism. This completes the read half of OPT-02 — the A/B routing field flip in `prompt_versions.status` actually changes inference behavior on next restart.

DEFERRED: This plan depends on Phase 096 (AgentRegistry / AgentDependencies), which is NOT yet built. The executor MUST NOT attempt this plan until Phase 096 has shipped. `AgentDependencies` does not exist in the codebase as of 2026-06-02 — importing it now will fail.

Purpose: Make promotion observable in production without violating DAG Invariant 3 (no DB in live path; load at startup only, same model as contract rolls and calibration curves).
Output: `prompt_loader.py` startup loader, injection wired into `_setup()`, unit tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/098-dspy-offline-prompt-optimizer/098-CONTEXT.md
@.planning/phases/098-dspy-offline-prompt-optimizer/098-RESEARCH.md
@.planning/phases/096-agent-registry/096-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GATE — confirm Phase 096 AgentDependencies exists</name>
  <files>src/intelligence/ai/base_group_service.py</files>
  <read_first>
    - .planning/phases/096-agent-registry/096-CONTEXT.md (AgentRegistry, AgentDependencies, BaseGroupCoordinator wiring decisions D-01..D-06)
    - src/intelligence/ai/base_group_service.py (current _setup; confirm whether AgentDependencies/AgentRegistry are now wired)
  </read_first>
  <action>
    Verify Phase 096 has shipped before proceeding. Run `grep -rn "class AgentDependencies" src/` and `grep -rn "class AgentRegistry" src/`. If neither exists, STOP and report that Phase 098 Plan 05 is blocked on Phase 096 — do not stub or invent the injection point. If they exist, read their definitions to learn the exact field name and injection call for compiled prompts, and confirm whether `AgentDependencies` already has a `compiled_prompt` field or needs one added (per CONTEXT.md code_context: "extend it with `compiled_prompt: dict | None = None`").
  </action>
  <verify>
    `grep -rn "class AgentDependencies" src/` returns a definition (otherwise plan is correctly blocked)
  </verify>
  <acceptance_criteria>
    - If `class AgentDependencies` does NOT exist in src/, the task halts with a clear "blocked on Phase 096" report and no files are modified
    - If it exists, the executor has identified the exact injection field/call and whether `compiled_prompt` needs adding
  </acceptance_criteria>
  <done>Phase 096 dependency confirmed present (or plan correctly halted as blocked).</done>
</task>

<task type="auto">
  <name>Task 2: Build prompt_loader and wire startup injection</name>
  <files>src/intelligence/optimization/prompt_loader.py, src/intelligence/ai/base_group_service.py</files>
  <read_first>
    - src/intelligence/ai/base_group_service.py (_setup: self._pool exists at line ~123; inject after pool creation, before/at super()._setup())
    - src/intelligence/optimization/agent_signatures.py (AGENT_SIGNATURES — the set of agent_ids whose active prompt to load)
    - production/migrations/115_prompt_versions.sql (status='active' query shape)
    - The Phase 096 AgentDependencies definition identified in Task 1
    - CLAUDE.md (asyncpg JSONB returns dict directly; UTC; structlog rules)
  </read_first>
  <action>
    Create `src/intelligence/optimization/prompt_loader.py` with `async def load_active_compiled_prompts(pool) -> dict[str, dict]`: for each agent_id in AGENT_SIGNATURES, `SELECT compiled_prompt, version_tag FROM prompt_versions WHERE agent_id = $1 AND status = 'active' ORDER BY promoted_at DESC LIMIT 1`. asyncpg returns `compiled_prompt` as a dict (JSONB) — no json.loads. Return `{agent_id: compiled_dict}` only for agents with an active row; agents with no active row are absent from the dict (caller falls back to file ACTIVE_VERSION). Log `prompt_loader.loaded` (agent_id, version_tag, n_demos) per loaded agent and `prompt_loader.fallback` (agent_id) per missing agent.
    In `BaseSwarmCoordinator._setup()`, after `self._pool` is created (~line 127) and before agents are dispatched, call `active_prompts = await load_active_compiled_prompts(self._pool)` and inject each agent's compiled prompt into its Phase 096 `AgentDependencies` (use the exact field/call confirmed in Task 1; add `compiled_prompt: dict | None = None` to AgentDependencies if not already present). This is the ONLY prompt_versions read in the service lifetime — no per-call read (DAG Invariant 3).
    Do not add a Kafka topic or hot-swap mechanism (deferred per CONTEXT.md).
  </action>
  <verify>
    `.venv/bin/python -c "from src.intelligence.optimization.prompt_loader import load_active_compiled_prompts"`
    `.venv/bin/python -c "import src.intelligence.ai.base_group_service"`
    `.venv/bin/ruff check src/intelligence/optimization/ src/intelligence/ai/base_group_service.py`
  </verify>
  <acceptance_criteria>
    - `src/intelligence/optimization/prompt_loader.py` defines `async def load_active_compiled_prompts` and its query contains `status = 'active'`
    - The loader returns a dict keyed by agent_id; agents without an active row are omitted (fallback)
    - `base_group_service.py` contains exactly one call to `load_active_compiled_prompts` inside `_setup()` (grep count == 1)
    - No `load_active_compiled_prompts` or `prompt_versions` SELECT appears in any per-call/_compute path (grep of agent _compute methods shows none)
    - `compiled_prompt` is injected via AgentDependencies (the field is present on AgentDependencies)
    - imports + `ruff check` exit 0
  </acceptance_criteria>
  <done>Active compiled prompts load once at startup and inject via AgentDependencies; missing agents fall back to file defaults; no live-path DB read.</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for prompt_loader fallback + once-only load</name>
  <files>tests/unit/intelligence/optimization/test_prompt_loader.py</files>
  <read_first>
    - src/intelligence/optimization/prompt_loader.py (Task 2)
    - tests/unit/intelligence/optimization/ (existing test style from Plan 04)
  </read_first>
  <action>
    Create `tests/unit/intelligence/optimization/test_prompt_loader.py`. Using a fake/mock asyncpg pool (no live DB), test: (a) an agent with an active row returns its compiled_dict keyed by agent_id; (b) an agent with no active row is absent from the returned dict (fallback path); (c) the loader issues exactly one query per agent (assert mock call count == len(AGENT_SIGNATURES)). Keep tests CI-clean (no DB, no Ollama).
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/optimization/test_prompt_loader.py -q`
  </verify>
  <acceptance_criteria>
    - `test_prompt_loader.py` exists with tests for active-present, active-absent (fallback), and once-per-agent query count
    - `pytest tests/unit/intelligence/optimization/test_prompt_loader.py -q` exits 0
    - Tests use a mock pool (no create_db_pool, no live psql)
  </acceptance_criteria>
  <done>prompt_loader fallback and once-only load behavior are unit-tested CI-clean.</done>
</task>

</tasks>

<verification>
- Phase 096 gate confirmed before any modification
- load_active_compiled_prompts reads status='active' once at startup, returns agent_id->compiled_dict, omits missing agents
- Injection wired into _setup() exactly once; no per-call DB read
- Fallback + once-only behavior unit-tested; pytest green
</verification>

<success_criteria>
- Promoted active prompts take effect on next restart via AgentDependencies injection
- DAG Invariant 3 preserved (single startup read, no live-path DB)
- Agents without an active variant fall back cleanly to file ACTIVE_VERSION
</success_criteria>

<output>
After completion, create `.planning/phases/098-dspy-offline-prompt-optimizer/098-05-SUMMARY.md`
</output>
