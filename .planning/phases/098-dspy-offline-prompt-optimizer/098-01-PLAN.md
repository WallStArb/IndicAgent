---
phase: 098-dspy-offline-prompt-optimizer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/115_prompt_versions.sql
  - requirements.txt
autonomous: true
requirements: [OPT-02]

must_haves:
  truths:
    - "prompt_versions table exists in the indicagent database"
    - "status column rejects any value outside candidate/active/retired"
    - "version_tag is unique across all rows"
    - "dspy>=3.2.1 is declared in requirements.txt"
  artifacts:
    - path: "production/migrations/115_prompt_versions.sql"
      provides: "prompt_versions table DDL with CHECK constraint and agent_status index"
      contains: "CREATE TABLE IF NOT EXISTS prompt_versions"
    - path: "requirements.txt"
      provides: "dspy package pin"
      contains: "dspy>=3.2.1"
  key_links:
    - from: "prompt_versions.status"
      to: "CHECK constraint"
      via: "CHECK (status IN ('candidate', 'active', 'retired'))"
      pattern: "CHECK \\(status IN"
---

<objective>
Create the `prompt_versions` table migration and pin the `dspy` package. This is the storage substrate for OPT-02: compiled DSPy variants are written here as JSONB, and the `status` field drives A/B routing and promotion via a DB-only field flip (no application code change).

Purpose: Establish the durable, queryable store for compiled prompt variants before the optimizer that writes to it (Plan 02) and the promotion runner that flips its status (Plan 04) are built.
Output: `production/migrations/115_prompt_versions.sql` applied to the live DB; `dspy>=3.2.1` in `requirements.txt`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/098-dspy-offline-prompt-optimizer/098-CONTEXT.md
@.planning/phases/098-dspy-offline-prompt-optimizer/098-RESEARCH.md
@.planning/phases/098-dspy-offline-prompt-optimizer/098-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 115_prompt_versions.sql migration</name>
  <files>production/migrations/115_prompt_versions.sql</files>
  <read_first>
    - production/migrations/084_ai_enrichment_tables.sql (table-creation header + IF NOT EXISTS style)
    - production/migrations/099_dlq_quarantine.sql (status-enum CHECK constraint pattern)
    - production/migrations/114_calibration_curves_add_timeframe.sql (confirms 114 is current latest; 115 is next)
  </read_first>
  <action>
    Create `production/migrations/115_prompt_versions.sql`. Include a header comment block stating migration number 115, Phase 098, and an "idempotent: safe to re-apply" declaration.
    Define `CREATE TABLE IF NOT EXISTS prompt_versions` with exactly these columns per D-05:
    - `version_id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
    - `agent_id TEXT NOT NULL`
    - `version_tag TEXT UNIQUE NOT NULL`
    - `compiled_prompt JSONB NOT NULL`
    - `status TEXT NOT NULL DEFAULT 'candidate' CHECK (status IN ('candidate', 'active', 'retired'))`
    - `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`
    - `promoted_at TIMESTAMPTZ` (nullable, no default)
    Add `CREATE INDEX IF NOT EXISTS idx_prompt_versions_agent_status ON prompt_versions (agent_id, status);` so the startup active-version lookup (Plan 05) and per-agent promotion query (Plan 04) are indexed.
    Use TIMESTAMPTZ (never bare TIMESTAMP) per CLAUDE.md UTC rule.
  </action>
  <verify>
    Apply the migration: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/115_prompt_versions.sql`
    Confirm table: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d prompt_versions"`
  </verify>
  <acceptance_criteria>
    - File `production/migrations/115_prompt_versions.sql` exists and contains `CREATE TABLE IF NOT EXISTS prompt_versions`
    - File contains `CHECK (status IN ('candidate', 'active', 'retired'))`
    - File contains `version_tag TEXT UNIQUE NOT NULL`
    - File contains `idx_prompt_versions_agent_status`
    - After applying, `psql -c "\d prompt_versions"` lists columns version_id, agent_id, version_tag, compiled_prompt, status, created_at, promoted_at
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "INSERT INTO prompt_versions (agent_id, version_tag, compiled_prompt, status) VALUES ('t','t1','{}','bogus')"` fails with a CHECK constraint violation
    - Re-running the migration file a second time produces no error (idempotent)
  </acceptance_criteria>
  <done>prompt_versions table exists in DB with CHECK-constrained status, unique version_tag, and agent_status index; re-applying the migration is a no-op.</done>
</task>

<task type="auto">
  <name>Task 2: Pin dspy in requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (current pin style — see existing >= and == entries)
  </read_first>
  <action>
    Add `dspy>=3.2.1` to `requirements.txt`. Use the package name `dspy` — NOT `dspy-ai` (the old name, last version 2.6.0, per RESEARCH pitfall 1). Place it alphabetically among the runtime dependencies. Then install it into the venv: `uv pip install dspy`.
  </action>
  <verify>
    `grep -i "dspy" requirements.txt` shows `dspy>=3.2.1`
    `.venv/bin/python -c "import dspy; print(dspy.__version__)"` prints a 3.2.x or later version
  </verify>
  <acceptance_criteria>
    - `requirements.txt` contains the exact string `dspy>=3.2.1`
    - `requirements.txt` does NOT contain `dspy-ai`
    - `.venv/bin/python -c "import dspy; print(dspy.__version__)"` exits 0 and prints a version >= 3.2.1
  </acceptance_criteria>
  <done>dspy>=3.2.1 is pinned and importable in the venv.</done>
</task>

</tasks>

<verification>
- `\d prompt_versions` shows all 7 columns with correct types and the CHECK constraint
- Inserting an out-of-domain status value is rejected
- `import dspy` succeeds with version >= 3.2.1
</verification>

<success_criteria>
- prompt_versions table is live, CHECK-constrained, indexed on (agent_id, status)
- dspy>=3.2.1 pinned and importable
- Migration is idempotent
</success_criteria>

<output>
After completion, create `.planning/phases/098-dspy-offline-prompt-optimizer/098-01-SUMMARY.md`
</output>
