---
phase: 098-dspy-offline-prompt-optimizer
plan: 03
type: execute
wave: 3
depends_on: ["098-02"]
files_modified:
  - services/dspy_optimizer.py
  - production/systemd/indicagent-dspy-optimizer.service
  - production/systemd/indicagent-dspy-optimizer.timer
  - services/service_auditor.py
autonomous: true
requirements: [OPT-03]

must_haves:
  truths:
    - "Running services/dspy_optimizer.py executes one optimization pass and exits (no daemon loop)"
    - "job_completed_total{job=dspy-optimizer, status} is emitted at process exit for success and failure"
    - "indicagent-dspy-optimizer.timer fires weekly on Monday 07:00 UTC"
    - "The optimizer never opens a Kafka consumer or writes to the live inference path"
  artifacts:
    - path: "services/dspy_optimizer.py"
      provides: "oneshot main() entrypoint wrapping DSPyOptimizer"
      contains: "DSPyOptimizer"
    - path: "production/systemd/indicagent-dspy-optimizer.service"
      provides: "Type=oneshot systemd unit"
      contains: "Type=oneshot"
    - path: "production/systemd/indicagent-dspy-optimizer.timer"
      provides: "weekly Monday timer"
      contains: "OnCalendar=Mon"
  key_links:
    - from: "services/dspy_optimizer.py"
      to: "JOB_COMPLETED_TOTAL"
      via: "add(1, {job: dspy-optimizer, status})"
      pattern: "job.*dspy-optimizer"
    - from: "indicagent-dspy-optimizer.service"
      to: "services/dspy_optimizer.py"
      via: "ExecStart python services/dspy_optimizer.py"
      pattern: "dspy_optimizer.py"
---

<objective>
Build the oneshot entrypoint and systemd timer that drive OPT-03: the optimizer runs as a timer-triggered batch job (not a daemon) and does not touch the live inference path. The entrypoint mirrors `ml_training_agent.py` verbatim; the systemd units mirror the ml-training unit pair with a weekly Monday schedule.

Purpose: Make the DSPyOptimizer (Plan 02) runnable and schedulable, with the mandatory D-06 oneshot OTel contract.
Output: `services/dspy_optimizer.py`, `indicagent-dspy-optimizer.service`, `indicagent-dspy-optimizer.timer`, and a `_DAG_ORDER` registration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/098-dspy-offline-prompt-optimizer/098-CONTEXT.md
@.planning/phases/098-dspy-offline-prompt-optimizer/098-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create oneshot entrypoint services/dspy_optimizer.py</name>
  <files>services/dspy_optimizer.py</files>
  <read_first>
    - services/ml_training_agent.py (canonical oneshot main(): _path_bootstrap first, try/except/finally, JOB_COMPLETED_TOTAL, flush_and_shutdown_metrics)
    - src/intelligence/optimization/dspy_optimizer.py (DSPyOptimizer class to wrap — Plan 02)
    - CLAUDE.md "Oneshot contract (D-06)" (job label must match systemd unit %n suffix exactly)
  </read_first>
  <action>
    Create `services/dspy_optimizer.py` by copying the `ml_training_agent.py` structure exactly, substituting:
    - module docstring referencing Phase 098 and the weekly Mon 07:00 UTC / 02:00 ET timer
    - `import _path_bootstrap  # noqa: F401` as the FIRST import before any `from src.*` (RESEARCH pitfall 6 — do not reorder)
    - `from src.intelligence.optimization.dspy_optimizer import DSPyOptimizer`
    - `agent = DSPyOptimizer(settings)`
    - success counter: `JOB_COMPLETED_TOTAL.add(1, {"job": "dspy-optimizer", "status": "success"})`
    - failure counter (in except, then re-raise): `JOB_COMPLETED_TOTAL.add(1, {"job": "dspy-optimizer", "status": "failure"})`
    - `flush_and_shutdown_metrics()` in `finally`
    Do NOT emit `skipped_data_gate` here — that is emitted from inside DSPyOptimizer._run() (Plan 02). main() emits `success` on a clean gate-skip exit.
    The `job` label value MUST be exactly `"dspy-optimizer"` (matches systemd unit suffix).
  </action>
  <verify>
    `.venv/bin/python -c "import ast; ast.parse(open('services/dspy_optimizer.py').read())"`
    `.venv/bin/ruff check services/dspy_optimizer.py`
  </verify>
  <acceptance_criteria>
    - `services/dspy_optimizer.py` first import line is `import _path_bootstrap` (before any `from src.` import)
    - File contains `from src.intelligence.optimization.dspy_optimizer import DSPyOptimizer`
    - File contains `{"job": "dspy-optimizer", "status": "success"}` and `{"job": "dspy-optimizer", "status": "failure"}`
    - File contains `flush_and_shutdown_metrics()` inside a `finally` block
    - File does NOT contain the literal `skipped_data_gate` (that lives in the class, not main)
    - `ruff check services/dspy_optimizer.py` exits 0
  </acceptance_criteria>
  <done>services/dspy_optimizer.py is a oneshot entrypoint wrapping DSPyOptimizer with the D-06 success/failure counters and metrics flush.</done>
</task>

<task type="auto">
  <name>Task 2: Create systemd service + timer and register in _DAG_ORDER</name>
  <files>production/systemd/indicagent-dspy-optimizer.service, production/systemd/indicagent-dspy-optimizer.timer, services/service_auditor.py</files>
  <read_first>
    - production/systemd/indicagent-ml-training.service (Type=oneshot template: User, WorkingDirectory, Environment, TimeoutStartSec)
    - production/systemd/indicagent-ml-training.timer (OnCalendar/Persistent/Unit structure)
    - services/service_auditor.py (_DAG_ORDER, _LAG_THRESHOLDS, _AGENT_ID_TO_UNIT registration points)
  </read_first>
  <action>
    Create `production/systemd/indicagent-dspy-optimizer.service` copying the ml-training service file, changing only:
    - `Description=IndicAgent DSPy Offline Prompt Optimizer -- weekly BootstrapFewShot compilation`
    - `ExecStart=/home/bg/dev/indicagent/.venv/bin/python services/dspy_optimizer.py`
    Keep `Type=oneshot`, `User=bg`, `WorkingDirectory=/home/bg/dev/indicagent`, `Environment=PYTHONPATH=...`, `Environment=PYTHONUNBUFFERED=1`, `TimeoutStartSec=7200`, `[Install] WantedBy=multi-user.target`.
    Create `production/systemd/indicagent-dspy-optimizer.timer` copying the ml-training timer, changing:
    - `Description=DSPy Optimizer Timer -- weekly Monday 07:00 UTC (02:00 ET)`
    - `OnCalendar=Mon *-*-* 07:00:00 UTC`
    - `Unit=indicagent-dspy-optimizer.service`
    - keep `Persistent=true` and `[Install] WantedBy=timers.target`.
    Register the service in `services/service_auditor.py`: add `indicagent-dspy-optimizer` to `_DAG_ORDER` (alongside other oneshot batch entries like ml-training), and add any required entries to `_AGENT_ID_TO_UNIT` if the auditor maps job names to units. Follow the exact registration style of `indicagent-ml-training`. Do NOT add a lag threshold for a oneshot (timer-triggered jobs are not consumer-lag monitored) — match how ml-training is handled.
  </action>
  <verify>
    `systemd-analyze verify production/systemd/indicagent-dspy-optimizer.service` (expect no errors)
    `grep -n "indicagent-dspy-optimizer" services/service_auditor.py`
    `.venv/bin/python -c "import services.service_auditor"`
  </verify>
  <acceptance_criteria>
    - `production/systemd/indicagent-dspy-optimizer.service` contains `Type=oneshot` and `ExecStart=/home/bg/dev/indicagent/.venv/bin/python services/dspy_optimizer.py`
    - `production/systemd/indicagent-dspy-optimizer.timer` contains `OnCalendar=Mon *-*-* 07:00:00 UTC` and `Unit=indicagent-dspy-optimizer.service`
    - `grep "indicagent-dspy-optimizer" services/service_auditor.py` returns at least one match in `_DAG_ORDER`
    - `.venv/bin/python -c "import services.service_auditor"` exits 0
    - `systemd-analyze verify production/systemd/indicagent-dspy-optimizer.service` reports no syntax errors
  </acceptance_criteria>
  <done>The systemd service+timer pair exists (Type=oneshot, weekly Monday 07:00 UTC) and the service is registered in service_auditor _DAG_ORDER.</done>
</task>

</tasks>

<verification>
- Entrypoint parses, ruff-clean, _path_bootstrap first, D-06 counters present
- Service is Type=oneshot pointing at services/dspy_optimizer.py
- Timer fires Monday 07:00 UTC, Persistent=true
- Service registered in _DAG_ORDER; service_auditor imports cleanly
</verification>

<success_criteria>
- Optimizer is runnable as a oneshot and schedulable weekly
- D-06 job_completed_total contract satisfied (success/failure from main, skipped_data_gate from class)
- No daemon loop, no Kafka consumer, no live-path coupling (OPT-03)
</success_criteria>

<output>
After completion, create `.planning/phases/098-dspy-offline-prompt-optimizer/098-03-SUMMARY.md`
</output>
