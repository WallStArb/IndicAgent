---
phase: 182-security-classification-hierarchy-todo-384
plan: 07
type: execute
wave: 3
depends_on: [182-03, 182-04, 182-05, 182-06]
files_modified:
  - tests/integration/test_classification_schema.py
  - tests/integration/test_instrument_classification_coverage.py
  - docs/foundation/security-classification-hierarchy.md
  - docs/foundation/glossary.md
  - docs/research/stratification-security-classification-hierarchy.md
  - CLAUDE.md
  - .planning/todos/PRIORITIES.md
  - .planning/todos/completed/384-security-classification-hierarchy-build-trigger-already-fired.md
autonomous: true
requirements: [D-01, D-05, D-06, D-07, D-08, D-09, D-10, D-11]

must_haves:
  truths:
    - "Every active instrument (and every instruments row) has exactly one current indicagent_v1 assignment in the live DB, valid from the build date"
    - "Single names sit at level 4; SPY at EQ.BROAD, XLK at EQ.IT, SMH at EQ.IT.SEMI.EQUIP"
    - "The nightly coverage audit reports zero uncovered active instruments against the live DB"
    - "get_active_contracts() returns real sector names from the classification"
    - "Integration tests for schema constraints, the node-immutability guard, and coverage pass against the live DB, and the phase record states they are not GitHub-CI-enforced"
    - "The classification system is documented where every sibling registry is documented, and todo 384 is closed"
  artifacts:
    - path: "tests/integration/test_classification_schema.py"
      provides: "D-01 live checks: constraints, append-only current-row index, node-immutability guard"
    - path: "tests/integration/test_instrument_classification_coverage.py"
      provides: "D-05/D-06/D-07/D-09 live checks and a ClassificationService round trip"
    - path: "docs/foundation/security-classification-hierarchy.md"
      provides: "canonical doc for the built Layer 1 system"
  key_links:
    - from: "production/migrations/365_indicagent_v1_classification_seed.sql"
      to: "live indicagent DB"
      via: "psql -v ON_ERROR_STOP=1 -f, after --check equality"
      pattern: "classification_seed --check"
    - from: "tests/integration/test_classification_schema.py"
      to: "src/config/classification_seed.render_node_guard_sql"
      via: "inconsistent node rendered and executed in a rolled-back transaction"
      pattern: "render_node_guard_sql"
---

<objective>
Apply the human-approved seed (migration 365) to the live DB, prove the phase goal end to end
(coverage, depth, source_ref, build-date floor, service round trip, D-10 builders, nightly audit),
and document and close.

Integration-suite note: `tests/integration/conftest.py`'s autouse session fixture rebuilds
`indicagent_test` and currently fails on migration 322 (todo 413, open, pre-existing, not this
phase's scope). The two new integration tests query the live `indicagent` DB, like
`test_instrument_registry.py`, and are run with `--noconftest` to bypass that broken rebuild. They are
marked `integration` and are not GitHub-CI-enforced (CI runs `tests/unit/` only, with no DB); the
enforcement that does run automatically is the seed guard, the onboarding gate, and the nightly audit
(D-09). The SUMMARY and the foundation doc say this plainly.

Output: seed applied, two integration test files, foundation doc, glossary/CLAUDE.md/design-doc
updates, todo 384 closed.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/182-security-classification-hierarchy-todo-384/182-CONTEXT.md
@.planning/phases/182-security-classification-hierarchy-todo-384/182-VALIDATION.md
@.planning/phases/182-security-classification-hierarchy-todo-384/182-03-SUMMARY.md
@tests/integration/test_instrument_registry.py

<interfaces>
From Plan 01: `ClassificationService`, `DEFAULT_SCHEME`, `ALLOWED_SOURCE_REFS`, `unclassified_code`.
From Plan 03: `src.config.classification_seed.render_node_guard_sql(scheme: str, nodes: Sequence[NodeSeed]) -> str`,
`NodeSeed(code, parent_code, level, name)`; `src.config.classification_seed_data.SCHEME/NODES/ASSIGNMENTS`;
CLI `python -m src.config.classification_seed --check PATH`.
From Plan 06: `python -m src.config.classification_coverage`.
Integration test precedent (tests/integration/test_instrument_registry.py): `pytestmark = pytest.mark.integration`,
`asyncpg.connect(settings.database_url)` in try/finally (normalize a `postgresql+asyncpg://` DSN the way
src/config/vocabulary_drift.py does).
Build-date floor for D-07: 2026-09-25 (phase build start; valid_from is the apply date, which is on or after it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the approved seed migration live and verify the goal end to end</name>
  <files>production/migrations/365_indicagent_v1_classification_seed.sql</files>
  <read_first>
    - .planning/phases/182-security-classification-hierarchy-todo-384/182-03-SUMMARY.md (confirms human approval and the migration number used)
    - production/migrations/365_indicagent_v1_classification_seed.sql (header and guard blocks)
  </read_first>
  <action>
    Preconditions, each must hold or stop and report: Plan 03's SUMMARY records the user's approval;
    `python -m src.config.classification_seed --check <migration>` exits 0; the live instruments
    symbol set still equals the seed's (rerun Plan 03 Task 2's set comparison; if an instrument was
    onboarded since, it went through the Plan 04 gate and already has a row, which the seed's
    skip-if-current logic tolerates only when codes agree).
    Apply per D-11: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -v ON_ERROR_STOP=1 -f production/migrations/365_indicagent_v1_classification_seed.sql`.
    If any guard raises, the transaction rolls back; fix the data module (never the SQL by hand),
    re-render, get the change re-approved if it alters an assignment, and reapply. The migration file
    is already committed from Plan 03; if review edits changed it, commit the re-rendered file in the
    same step as the apply.
    Then verify live: run `.venv/bin/python -m src.config.classification_coverage` (must report 0
    uncovered); run the get_active_contracts smoke from Plan 05 (sector names are real, no
    `indicagent_v1:unclassified` among active instruments unless an instrument is legitimately assigned
    above level 2, which none are); record counts in the SUMMARY.
  </action>
  <verify>
    <automated>test "$(PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -At -c "SELECT count(*) FROM instruments i WHERE i.is_active AND NOT EXISTS (SELECT 1 FROM instrument_classification ic WHERE ic.symbol=i.symbol AND ic.scheme='indicagent_v1' AND ic.valid_to IS NULL)")" = "0" && test "$(PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -At -c "SELECT authority FROM classification_scheme WHERE scheme='indicagent_v1'")" = "IndicAgent" && .venv/bin/python -m src.config.classification_coverage | grep -q "Uncovered active instruments: 0$"</automated>
  </verify>
  <acceptance_criteria>
    - Zero active instruments without a current indicagent_v1 row; count of current rows equals count of instruments rows
    - `SELECT min(valid_from) FROM instrument_classification` is >= 2026-09-25 and equals the apply date (D-07)
    - classification_scheme has exactly the one indicagent_v1 row, authority IndicAgent (D-03)
    - Coverage audit reports 0 uncovered; get_active_contracts sectors are node names
  </acceptance_criteria>
  <done>The approved classification is live for every instrument and every enforcement point reads green.</done>
</task>

<task type="auto">
  <name>Task 2: Live-DB integration tests for schema, guard, and coverage (D-01, D-05, D-06, D-07, D-09)</name>
  <files>tests/integration/test_classification_schema.py, tests/integration/test_instrument_classification_coverage.py</files>
  <read_first>
    - tests/integration/test_instrument_registry.py (marker, connect/finally shape)
    - tests/integration/conftest.py lines 1-40 (why --noconftest is used, todo 413)
    - src/config/classification_seed.py (render_node_guard_sql)
  </read_first>
  <action>
    Both files: module docstring stating they query the live indicagent DB, are marked integration,
    are not GitHub-CI-enforced, and are run with `--noconftest` until todo 413 fixes the scratch-DB
    rebuild. `pytestmark = pytest.mark.integration`.

    test_classification_schema.py (D-01):
    - PK of instrument_classification is exactly (symbol, scheme, valid_from) (pg_index/pg_attribute query)
    - uq_instrument_classification_current is unique, on (symbol, scheme), predicate `valid_to IS NULL`
    - FK to instruments has confdeltype 'r'; classification_node has the self-FK and PK (scheme, code)
    - append-only: inside a transaction that is always rolled back, inserting a second current row for
      SPY raises asyncpg.UniqueViolationError
    - immutability guard: inside an always-rolled-back transaction, executing
      `render_node_guard_sql("indicagent_v1", [NodeSeed("EQ.IT.SEMI", "EQ.HC", 3, "Semis")])` raises
      asyncpg.RaiseError (existing code, different parent); the same call with the true parent does not raise
    - every node's path equals its ancestor chain rebuilt from parent_code (recursive CTE comparison returns zero mismatches)
    - no scheme row names GICS in name/authority/source_ref (D-03)

    test_instrument_classification_coverage.py:
    - coverage (D-09): zero active instruments without a current row (the same query as the seed guard and the nightly audit)
    - source_ref (D-06): every current row's source_ref is in ALLOWED_SOURCE_REFS; single_name_equity-tagged symbols all use ibkr_contract_details+review
    - valid_from (D-07): min(valid_from) >= date(2026, 9, 25)
    - depth (D-05): every single_name_equity symbol's current node is level 4; SPY -> EQ.BROAD, XLK -> EQ.IT, SMH -> EQ.IT.SEMI.EQUIP
    - service round trip (D-08): ClassificationService initialized on the live DB: name_at_level("SMH", 2) == "Information Technology"; node_at_level("SPY", 4) == "indicagent_v1:unclassified"; node_at_level("SPY", 1, as_of=date(2026, 9, 24)) is unclassified (no pre-build history)
    - D-10: get_active_contracts(dimension="backfill") returns no active instrument whose sector equals its contract_details JSON sector unless that string is also the node name (spot-check SMH: sector "Information Technology", not "technology")
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/integration/test_classification_schema.py tests/integration/test_instrument_classification_coverage.py -m integration --noconftest -q</automated>
  </verify>
  <acceptance_criteria>
    - Both files pass against the live DB with --noconftest
    - Rolled-back tests leave no trace: `SELECT count(*) FROM instrument_classification WHERE valid_to IS NULL` is unchanged before and after the run
    - `.venv/bin/pytest tests/unit/ -q` stays green (the integration files are not collected into unit runs)
  </acceptance_criteria>
  <done>Every VALIDATION.md integration row (D-01, D-05, D-06, D-07, D-09) has a passing live test.</done>
</task>

<task type="auto">
  <name>Task 3: Document the system and close todo 384</name>
  <files>docs/foundation/security-classification-hierarchy.md, docs/foundation/glossary.md, docs/research/stratification-security-classification-hierarchy.md, CLAUDE.md, .planning/todos/PRIORITIES.md, .planning/todos/completed/384-security-classification-hierarchy-build-trigger-already-fired.md</files>
  <read_first>
    - docs/foundation/controlled-vocabulary-registry.md (sibling canonical doc: structure and tone)
    - docs/foundation/glossary.md lines 425-445 (`classification scheme` entry, currently "Status: design")
    - CLAUDE.md sections "Controlled Vocabulary Registry (CVR)" and "Unified Concept Registry (UCR)"
    - .planning/todos/PRIORITIES.md (row for 384) and .planning/PLANNING-SYSTEM.md (how todos close)
  </read_first>
  <action>
    New `docs/foundation/security-classification-hierarchy.md` (standalone with rationale, Author line
    per the doc provenance convention, no em dashes, sentence-case headings): what Layer 1 is and why it
    is a third registry separate from CVR and ITR; the three tables and their invariants (append-only,
    one current row, RESTRICT, node immutability, deepest-node membership, as-of join rule, no history
    before 2026-09-25); `indicagent_v1` (four levels, codes embed parents, equity levels named after the
    public GICS structure and not GICS, authority IndicAgent, source_ref values and when each applies,
    geography/style deliberately not levels); the read layer (ClassificationService, unclassified
    stratum, current_level_name_sql for synchronous builders); the three D-09 enforcement points and the
    fact that the integration tests are local-only; how to reclassify (a migration that closes valid_to
    and inserts a new row) and how to add a node (render_node_guard_sql in the migration); Layer 2 not
    built, trigger unchanged. Link the research design doc.
    Glossary `classification scheme`: Status -> built (Phase 182), code surface -> the three tables,
    `ClassificationService`, `indicagent_v1`; add a one-line note that `indicagent_v1` is IndicAgent's own
    scheme, not GICS. Design doc: add a dated line under the header noting Layer 1 was built in Phase
    182 (link the foundation doc), leave the rest as the historical design record.
    CLAUDE.md: add a short "Security Classification Hierarchy (SCH)" section after CVR, matching the
    sibling sections' density (tables, scheme, service, enforcement points, contract_details.sector is
    historical only, link to the foundation doc). Bump nothing else.
    Todo 384: move it to `.planning/todos/completed/` (stage the renamed path alone first, per the
    CLAUDE.md git-add gotcha), append a closure note (Phase 182, migrations 364/365, date), and update
    PRIORITIES.md to drop or re-point its row according to the file's own conventions for completed
    todos. Also update the phase-180 line in PRIORITIES.md that lists 384 if the convention requires it.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_todo_priorities_link_integrity.py -x -q && test -f .planning/todos/completed/384-security-classification-hierarchy-build-trigger-already-fired.md && ! test -f .planning/todos/pending/384-security-classification-hierarchy-build-trigger-already-fired.md && grep -q "security-classification-hierarchy.md" CLAUDE.md && ! grep -nP "\x{2014}" docs/foundation/security-classification-hierarchy.md</automated>
  </verify>
  <acceptance_criteria>
    - Todo link-integrity test passes; 384 lives in completed/
    - The foundation doc contains no em dash and states the integration tests are not GitHub-CI-enforced
    - Glossary `classification scheme` status reads built, not design
    - `.venv/bin/pytest tests/unit/ -q` is green (phase gate)
  </acceptance_criteria>
  <done>The built system is documented alongside its sibling registries and todo 384 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| reviewed migration -> live DB | seed DML applied by the operator |
| integration tests -> live DB | tests write only inside always-rolled-back transactions |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-23 | Tampering | applying unreviewed or drifted seed SQL | mitigate | precondition: recorded approval plus --check byte equality before apply; guards roll back on any disagreement |
| T-182-24 | Tampering | integration tests mutating live reference data | mitigate | every write test runs in an explicit transaction that is rolled back in finally; acceptance compares current-row counts before and after |
| T-182-25 | Repudiation | false sense of CI enforcement | mitigate | docs and SUMMARY state the integration tests are local-only; automatic enforcement is seed guard, onboarding gate, nightly audit |
</threat_model>

<verification>
- Live coverage query returns 0; coverage audit reports 0 uncovered
- `.venv/bin/pytest tests/integration/test_classification_schema.py tests/integration/test_instrument_classification_coverage.py -m integration --noconftest -q`
- `.venv/bin/pytest tests/unit/ -q` green
</verification>

<success_criteria>
All 273 active instruments (and every instruments row) carry a dated indicagent_v1 classification
read through one source of truth, enforced at seed, onboarding, and nightly audit, with the flat
sector label retired as a source of truth, documented, and todo 384 closed.
</success_criteria>

<output>
Create `.planning/phases/182-security-classification-hierarchy-todo-384/182-07-SUMMARY.md` when done
</output>
