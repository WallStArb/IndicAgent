---
phase: 182-security-classification-hierarchy-todo-384
plan: 06
type: execute
wave: 2
depends_on: [182-01]
files_modified:
  - src/config/classification_coverage.py
  - tests/unit/test_classification_coverage.py
  - scripts/ops/corpus/ops_corpus_pipeline_run.sh
autonomous: true
requirements: [D-09, D-08]

must_haves:
  truths:
    - "Every nightly corpus pipeline run reports any active instrument without a current indicagent_v1 assignment, loudly (integrity_monitor row, OTel counter, logger.error with the symbols)"
    - "The audit also reports the size of the unclassified stratum at every level, so D-08's explicit stratum is countable"
    - "The audit never gates the pipeline; only a runtime error exits non-zero"
  artifacts:
    - path: "src/config/classification_coverage.py"
      provides: "ClassificationCoverageAuditor (BaseBatch oneshot) + pure coverage functions"
      exports: ["uncovered_symbols", "unclassified_counts_by_level", "ClassificationCoverageAuditor"]
    - path: "scripts/ops/corpus/ops_corpus_pipeline_run.sh"
      provides: "non-blocking chained audit step beside the vocabulary drift audit"
      contains: "src.config.classification_coverage"
  key_links:
    - from: "ClassificationCoverageAuditor.execute"
      to: "ClassificationService"
      via: "constructed on the BaseBatch pool, initialize(), node_at_level(symbol, 1)"
      pattern: "ClassificationService\\("
    - from: "ClassificationCoverageAuditor"
      to: "integrity_monitor"
      via: "emit_integrity_fact_async(conn, 'classification_coverage', ...)"
      pattern: "classification_coverage"
---

<objective>
D-09 point 3: a coverage drift audit in the nightly chain beside `VocabularyDriftAuditor`, reporting
any active instrument without a current `indicagent_v1` assignment. It catches what the seed guard
(one moment in time) and the onboarding gate (one write path) cannot: a raw `UPDATE instruments SET
is_active = true`, or a row added outside `onboard_instrument()`.

The auditor is also `ClassificationService`'s first production consumer (D-08): it measures coverage
through the as-of read layer (level 1 unclassified means no current assignment) rather than a
second hand-written SQL definition of "covered".

Output: `src/config/classification_coverage.py`, unit tests, one chained step in
`ops_corpus_pipeline_run.sh`.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/182-security-classification-hierarchy-todo-384/182-CONTEXT.md
@src/config/vocabulary_drift.py

<interfaces>
From src/config/classification_service.py (Plan 01):
- `ClassificationService(database_url, pool=None)`; `initialize()`; `close()` closes only a pool it created
- `node_at_level(symbol, level, *, scheme=DEFAULT_SCHEME, as_of=None) -> str`; `max_level(scheme=DEFAULT_SCHEME) -> int`
- `unclassified_code(scheme=DEFAULT_SCHEME) -> str`; `DEFAULT_SCHEME`

From src/config/vocabulary_drift.py (existing template, same ring, same chain):
- `class VocabularyDriftAuditor(BaseBatch)`: `job_name = _JOB_LABEL`, `compute_version = "1.0.0"`, `async def execute(self, pool)`
- `emit_integrity_fact_async(conn, monitor_type, subject, metric_name, metric_value, threshold_value, passed, training_window_end)` from src.core.integrity_monitor
- `counter(name, description)` from src.observability.metrics; used as `.add(1, attrs)`
- `__main__`: init_otel_providers("indicagent-vocabulary-drift-audit"), Settings(), DSN normalization, `asyncio.run(Auditor(db_dsn=...).run())`

Chain wiring (scripts/ops/corpus/ops_corpus_pipeline_run.sh ~line 430): a backgrounded, `|| true`
subshell running `"$PYTHON" -m src.config.vocabulary_drift` with tee into `$LOG_DIR`, then an echo of the PID.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClassificationCoverageAuditor with pure coverage functions</name>
  <files>src/config/classification_coverage.py, tests/unit/test_classification_coverage.py</files>
  <read_first>
    - src/config/vocabulary_drift.py (full file: module docstring, pure/IO split, BaseBatch subclass, __main__)
    - src/config/classification_service.py (Plan 01 output)
    - tests/unit/test_vocabulary_drift_audit.py lines 1-40 (no-DB style)
  </read_first>
  <behavior>
    Using a real ClassificationService whose `_nodes`/`_assignments` caches are populated directly (no DB):
    - uncovered_symbols(["NVDA", "SPY", "NEWCO"], service, as_of) == ["NEWCO"] (sorted, level-1 unclassified means uncovered)
    - a symbol whose only row closed before as_of is uncovered
    - unclassified_counts_by_level(["NVDA", "SPY", "TLT"], service, as_of) == {1: 0, 2: 0, 3: 2, 4: 2} for NVDA at a level-4 node, SPY at EQ.BROAD (level 2), TLT at FI.RATES (level 2); levels run 1..service.max_level()
    - empty input returns [] and a dict of zeros
    - ClassificationCoverageAuditor.job_name == "classification-coverage-audit"
  </behavior>
  <action>
    Write `src/config/classification_coverage.py` mirroring vocabulary_drift.py's layout (per D-09):
    module docstring (Phase 182, D-09 point 3, observability-only like the vocabulary drift audit,
    chained in ops_corpus_pipeline_run.sh, not a systemd timer; the seed guard and onboarding gate are
    the other two enforcement points; GitHub CI cannot see the live DB, which is why this exists).
    Pure functions `uncovered_symbols(active_symbols, service, as_of) -> list[str]` and
    `unclassified_counts_by_level(active_symbols, service, as_of) -> dict[int, int]`, no IO.
    `_JOB_LABEL = "classification-coverage-audit"`; counter
    `classification_coverage_uncovered_total` ("Runs where the classification coverage audit found
    active instruments without a current assignment"), recorded with `.add(1, {"scheme": DEFAULT_SCHEME})`.

    `ClassificationCoverageAuditor(BaseBatch)`, `compute_version = "1.0.0"`, `execute(pool)`:
    fetch `SELECT symbol FROM instruments WHERE is_active = true ORDER BY symbol`; build
    `ClassificationService(self._db_dsn, pool=pool)`, `initialize()`, and in finally `close()` (which
    must not close the injected pool). as_of = `datetime.now(UTC).date()`. Compute both results; emit
    one integrity fact `("classification_coverage", DEFAULT_SCHEME, "uncovered_active_count",
    float(n_uncovered), 0.0, n_uncovered == 0, None)` and one per level
    `("classification_coverage", DEFAULT_SCHEME, f"unclassified_at_level_{level}", float(count), None, True, None)`
    (the stratum size is informational, never a failure). On any uncovered symbol: counter add and
    `logger.error("classification_coverage.uncovered_active_instruments", symbols=[...], count=n)`.
    Print a short report like VocabularyDriftAuditor's, including the exact line `Uncovered active instruments: <n>` (Plan 07 greps it) and one line per level, ending with "observability-only, never a hard
    gate". A detected gap never raises; a runtime error propagates (BaseBatch D-06 contract).
    `__main__` mirrors vocabulary_drift.py with service name "indicagent-classification-coverage-audit".
    One summary log per run, no per-symbol logging. No numeric thresholds exist here (the pass
    condition is zero uncovered by definition), so no APR key is needed. Tests first.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_classification_coverage.py -x -q && .venv/bin/ruff check src/config/classification_coverage.py tests/unit/test_classification_coverage.py && ! grep -n "asyncpg.connect\|get_settings" tests/unit/test_classification_coverage.py</automated>
  </verify>
  <acceptance_criteria>
    - Behavior tests pass with no DB
    - Live dry run against the current DB (seed not yet applied): `.venv/bin/python -m src.config.classification_coverage` exits 0 and reports every active instrument as uncovered (273 at plan time) (proves the audit is loud on a real gap); after Plan 07 it must report 0
    - `SELECT count(*) FROM integrity_monitor WHERE monitor_type='classification_coverage'` increases after the run
  </acceptance_criteria>
  <done>The coverage audit exists, measures through ClassificationService, and reports gaps loudly without gating.</done>
</task>

<task type="auto">
  <name>Task 2: Chain the audit into the nightly corpus pipeline beside the vocabulary drift audit</name>
  <files>scripts/ops/corpus/ops_corpus_pipeline_run.sh</files>
  <read_first>
    - scripts/ops/corpus/ops_corpus_pipeline_run.sh lines 400-445
  </read_first>
  <action>
    Directly after the vocabulary drift audit block, add a comment block (Phase 182, D-09 point 3:
    observability-only, backgrounded, `|| true`, never gates alpha_publisher's committed completion,
    PID echoed for traceability) and the same subshell shape running
    `"$PYTHON" -m src.config.classification_coverage` teeing into
    `$LOG_DIR/classification_coverage_$(date +%Y%m%d_%H%M%S).log`, then echo its PID. Do not wrap it
    in run_step (run_step halts the pipeline on non-zero).
  </action>
  <verify>
    <automated>bash -n scripts/ops/corpus/ops_corpus_pipeline_run.sh && grep -n "src.config.classification_coverage" scripts/ops/corpus/ops_corpus_pipeline_run.sh && ! grep -n 'run_step.*classification_coverage' scripts/ops/corpus/ops_corpus_pipeline_run.sh</automated>
  </verify>
  <acceptance_criteria>
    - The script parses (`bash -n`)
    - The new step is backgrounded with `|| true` and appears after the vocabulary drift step
  </acceptance_criteria>
  <done>The nightly chain runs the coverage audit on every corpus pipeline run.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| auditor -> DB | read-only queries plus integrity_monitor inserts |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-20 | Repudiation | silent coverage decay | mitigate | integrity_monitor fact every run, OTel counter and logger.error on any uncovered active instrument |
| T-182-21 | Denial of service | audit failure blocking the pipeline | mitigate | backgrounded `|| true` step outside run_step |
| T-182-22 | Tampering | auditor closing the shared BaseBatch pool | mitigate | ClassificationService.close() closes only a pool it created (Plan 01); unit-tested there |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_classification_coverage.py -x -q`
- `bash -n scripts/ops/corpus/ops_corpus_pipeline_run.sh`
- Live run reports every active instrument uncovered before the seed is applied
</verification>

<success_criteria>
A nightly, non-gating, loud coverage audit exists and demonstrably detects the pre-seed gap.
</success_criteria>

<output>
Create `.planning/phases/182-security-classification-hierarchy-todo-384/182-06-SUMMARY.md` when done
</output>
