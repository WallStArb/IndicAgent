---
phase: 182-security-classification-hierarchy-todo-384
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/providers/ibkr.py
  - tests/unit/providers/test_ibkr_contract_classification.py
  - scripts/infrastructure/classification_ibkr_sourcing.py
  - config/classification/ibkr_classification_candidates.csv
autonomous: true
requirements: [D-06]

must_haves:
  truths:
    - "IBKR industry/category/subcategory for every single_name_equity instrument is fetched fresh this phase, through src/providers/ibkr.py only"
    - "The fetched candidates are a committed, reviewable file with a per-row fetched_at timestamp and status"
    - "A down or logged-out gateway makes the sourcing script exit non-zero with an actionable message and write no file; rerunning after recovery produces the full file"
  artifacts:
    - path: "src/providers/ibkr.py"
      provides: "IBKRProvider.fetch_contract_classification + ContractClassification dataclass"
      contains: "def fetch_contract_classification"
    - path: "scripts/infrastructure/classification_ibkr_sourcing.py"
      provides: "one-off sourcing script writing the candidates CSV"
    - path: "config/classification/ibkr_classification_candidates.csv"
      provides: "committed IBKR classification candidates for the single names (D-06 seed candidate)"
  key_links:
    - from: "scripts/infrastructure/classification_ibkr_sourcing.py"
      to: "IBKRProvider.fetch_contract_classification"
      via: "direct call per symbol"
      pattern: "fetch_contract_classification"
    - from: "IBKRProvider.fetch_contract_classification"
      to: "reqContractDetailsAsync"
      via: "asyncio.wait_for with _CONTRACT_DETAILS_TIMEOUT_SEC"
      pattern: "_CONTRACT_DETAILS_TIMEOUT_SEC"
---

<objective>
Source the single-name seed candidates required by D-06: IBKR `ContractDetails.industry`,
`.category`, `.subcategory` (plus `longName` for the reviewer) for every instrument tagged
`single_name_equity` (168 today, including the 40 unlabeled 1d-pilot names), fetched fresh this
phase (RESEARCH Pitfall 4: the 2026-09-18 probe is stale and likely missed the pilot cohort) and
committed as `config/classification/ibkr_classification_candidates.csv`.

Purpose: Plan 03 maps these candidates into `indicagent_v1` level-4 nodes for human review. This
plan never writes to the database and never decides a classification.

Gateway tolerance (orchestrator note): the gateway may be down or logged out (weekly 2FA logout,
todo 395). The script fails loudly and writes nothing in that case, and is safe to rerun. This plan
does not block Plan 01 (schema) or the wave-2 code plans; only Plan 03 waits on the CSV.

Output: provider method with unit tests, the sourcing script, the committed candidates CSV.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/182-security-classification-hierarchy-todo-384/182-CONTEXT.md
@.planning/phases/182-security-classification-hierarchy-todo-384/182-PATTERNS.md

<interfaces>
From src/providers/ibkr.py (existing, verified):
- `_CONTRACT_DETAILS_TIMEOUT_SEC = 30.0` (module constant, line ~155)
- `class IBKRProvider: __init__(self, host: str, port: int, client_id: int, tick_queue_size: int = 5000, settings: Settings | None = None)`
- `async def connect(self) -> bool`, `async def disconnect(self) -> None`, `def is_connected(self) -> bool`
- `qualify_instrument()` (lines ~973-1045) builds `Stock(symbol=..., exchange=instrument.exchange or "SMART", currency="USD")`
  and awaits `asyncio.wait_for(self._ib.reqContractDetailsAsync(contract), timeout=_CONTRACT_DETAILS_TIMEOUT_SEC)`.
  `details[0]` is an ib_async ContractDetails: `.industry`, `.category`, `.subcategory`, `.longName` live on it, not on `.contract`.

New contract this plan adds to src/providers/ibkr.py:
- `@dataclass(frozen=True) class ContractClassification: symbol: str; industry: str; category: str; subcategory: str; long_name: str`
- `async def fetch_contract_classification(self, symbol: str) -> ContractClassification | None`

IBKR client IDs already claimed in scripts/services: 35, 40, 44, 45, 46, 48, 50. Use 47 (free, <= _MAX_CLIENT_ID=50).
Existing one-off script conventions: scripts/infrastructure/universe_expansion_stratified_sourcing.py
(sys.path bootstrap via `Path(__file__).parent.parent.parent`, argparse, structlog, Settings(), IBKRProvider connect/disconnect in try/finally).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IBKRProvider.fetch_contract_classification (D-06 sourcing path)</name>
  <files>src/providers/ibkr.py, tests/unit/providers/test_ibkr_contract_classification.py</files>
  <read_first>
    - src/providers/ibkr.py lines 540-630 (class init, connect/disconnect) and 973-1050 (qualify_instrument's reqContractDetailsAsync call)
    - tests/unit/providers/test_ibkr_equity.py (how existing tests construct IBKRProvider and fake `_ib`)
  </read_first>
  <behavior>
    - A fake `_ib` whose `reqContractDetailsAsync` returns one details object with industry "Technology", category "Semiconductors", subcategory "Electronic Compo-Semicon", longName "NVIDIA CORP" yields ContractClassification("NVDA", "Technology", "Semiconductors", "Electronic Compo-Semicon", "NVIDIA CORP")
    - The contract passed to reqContractDetailsAsync is a Stock with symbol NVDA, exchange SMART, currency USD
    - Empty details list returns None
    - Two details objects with the same (industry, category, subcategory) return the first; two with different triples raise ValueError naming the symbol (ambiguous listing is loud, never a silent pick)
    - Calling before connect (`_ib is None`) raises RuntimeError
    - A reqContractDetailsAsync that never resolves raises TimeoutError (patch _CONTRACT_DETAILS_TIMEOUT_SEC small in the test); the method does not swallow it
  </behavior>
  <action>
    Add the frozen `ContractClassification` dataclass and the `fetch_contract_classification`
    method next to `qualify_instrument` (do not modify `qualify_instrument`: it is on the connect
    path). Reuse the same `Stock(symbol=symbol, exchange="SMART", currency="USD")` construction and
    the same `asyncio.wait_for(..., timeout=_CONTRACT_DETAILS_TIMEOUT_SEC)` wrapper with a comment
    pointing at the existing F4 rationale. Missing attributes on the details object become "" via
    getattr default. Exceptions propagate (no broad except); the caller decides how to record
    per-symbol failures. Keep all ib_async usage inside this file (Ring rule). Tests first.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/providers/test_ibkr_contract_classification.py tests/unit/providers/test_ibkr_equity.py tests/unit/providers/test_ibkr_provider.py -x -q && .venv/bin/ruff check src/providers/ibkr.py tests/unit/providers/test_ibkr_contract_classification.py</automated>
  </verify>
  <acceptance_criteria>
    - All behavior bullets are passing tests
    - `grep -rn "ib_async" scripts/infrastructure/classification_ibkr_sourcing.py` finds nothing once Task 2 lands (ib_async stays in ibkr.py)
    - Existing IBKR provider unit tests still pass
  </acceptance_criteria>
  <done>The provider exposes a tested, timeout-bounded classification fetch.</done>
</task>

<task type="auto">
  <name>Task 2: Sourcing script, live run, committed candidates CSV</name>
  <files>scripts/infrastructure/classification_ibkr_sourcing.py, config/classification/ibkr_classification_candidates.csv</files>
  <read_first>
    - scripts/infrastructure/universe_expansion_stratified_sourcing.py lines 1-70 and 300-370 (bootstrap, settings, provider connect pattern, loud pre-flight failure message)
    - src/core/service_utils.py `format_iso_ts`
  </read_first>
  <action>
    Write `scripts/infrastructure/classification_ibkr_sourcing.py` (module docstring: Phase 182,
    D-06, read-only against the DB, produces a candidate file for human review, never a
    classification decision). Behavior:
    - argparse: `--out` (default `config/classification/ibkr_classification_candidates.csv`),
      `--client-id` (default 47).
    - Symbols: psycopg query of every `instruments` row tagged `single_name_equity` in
      `instrument_tags`, ordered by symbol, active or not (Settings().database_url, normalizing a
      `postgresql+asyncpg://` prefix the way src/config/vocabulary_drift.py does). Fail with exit 3
      if the query returns zero rows.
    - Connect `IBKRProvider(host=settings.ib_host, port=settings.ib_port, client_id=args.client_id)`.
      If `connect()` returns False: print to stderr a message naming the gateway
      (`docker ps --filter name=ib-gateway`), the weekly 2FA logout (todo 395), and that the script is
      safe to rerun; exit 4; write no file.
    - For each symbol sequentially, await `fetch_contract_classification`; record status `ok`
      (non-empty category), `empty` (details returned but industry/category/subcategory all empty),
      `no_details` (None), `ambiguous` (ValueError), `timeout` (TimeoutError), or `error`
      (any other exception, message kept in a `detail` column). Catch per symbol with
      `except ... as error`, never abort the loop on one symbol. Accumulate counts; log once at the
      end (no per-row logging, CLAUDE.md), not per symbol. Disconnect in finally.
    - Output CSV columns in order: symbol, industry, category, subcategory, long_name, status,
      detail, fetched_at (UTC ISO via format_iso_ts, one timestamp per row). Write to a temp file
      in the same directory then `os.replace` so a crash never leaves a partial file.
    - Exit 0 only when every row is `ok`; otherwise exit 2 after writing the file and print the
      non-ok symbols.

    Run it: `.venv/bin/python scripts/infrastructure/classification_ibkr_sourcing.py`.
    - Exit 0: commit the script and the CSV together.
    - Exit 2: rerun once (transient pacing/timeouts). If rows are still non-ok, commit the script and
      the CSV as written and list the non-ok symbols in the SUMMARY; Plan 03 handles them (it must
      not invent IBKR values for them).
    - Exit 3 or 4 (DB empty or gateway unreachable): commit the script only, stop this plan, and
      report the blocker in the SUMMARY with the exact rerun command. Do not fabricate a CSV and do
      not reuse the 2026-09-18 probe output. Plans 01, 04, 05, 06 are unaffected; Plan 03 waits.
  </action>
  <verify>
    <automated>.venv/bin/ruff check scripts/infrastructure/classification_ibkr_sourcing.py && test -f config/classification/ibkr_classification_candidates.csv && .venv/bin/python -c "import csv,sys; r=list(csv.DictReader(open('config/classification/ibkr_classification_candidates.csv'))); assert list(r[0].keys())==['symbol','industry','category','subcategory','long_name','status','detail','fetched_at']; assert all(x['fetched_at'].startswith('2026-') for x in r); print(len(r), sum(x['status']=='ok' for x in r))"</automated>
  </verify>
  <acceptance_criteria>
    - The CSV row count equals `SELECT count(*) FROM instrument_tags WHERE tag='single_name_equity'` (168 at plan time)
    - Every row carries this run's fetched_at (Pitfall 4: no stale reuse)
    - The 40 pilot symbols named in 182-RESEARCH.md (ACTG ... WSHP) are all present as rows
    - Script and CSV are committed; the SUMMARY states the ok count and lists any non-ok symbols
    - If the gateway was down: no CSV exists, the script is committed, and the SUMMARY carries the rerun command
  </acceptance_criteria>
  <done>A fresh, committed IBKR candidate file for all single names, or a loud, documented gateway blocker with a rerun command.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| IBKR API -> candidates CSV | external strings (industry/category/subcategory/longName) enter the repo as review input |
| CSV -> classification | only via human-reviewed mapping in Plan 03, never written to the DB directly |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-05 | Tampering | IBKR-sourced category strings | mitigate | strings are review input only; Plan 03 maps them to fixed indicagent_v1 codes, and the FK from instrument_classification.code to classification_node makes an unreviewed string unrepresentable as an assignment |
| T-182-06 | Denial of service | reqContractDetailsAsync hang | mitigate | asyncio.wait_for with _CONTRACT_DETAILS_TIMEOUT_SEC; per-symbol timeout recorded as a status, loop continues |
| T-182-07 | Spoofing | IBKR client ID collision with a concurrent session | mitigate | dedicated unused client ID 47 (<= _MAX_CLIENT_ID); overridable via --client-id |
| T-182-08 | Repudiation | stale sourcing reused as fresh | mitigate | per-row fetched_at from this run; verify step asserts the column |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/providers/ -x -q`
- Candidate CSV committed with the expected columns and row count, or a documented gateway blocker
</verification>

<success_criteria>
Fresh IBKR classification candidates for all single_name_equity instruments are committed as a
reviewable file, sourced only through src/providers/ibkr.py.
</success_criteria>

<output>
Create `.planning/phases/182-security-classification-hierarchy-todo-384/182-02-SUMMARY.md` when done
</output>
