---
phase: 083-observability-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/observability/spans.py
  - src/observability/otel.py
autonomous: true

must_haves:
  truths:
    - "spans.py exists and exports ATTR_* constants and observed_span async context manager"
    - "OTel Resource includes service.instance.id = hostname:pid"
    - "observed_span sets ERROR status and records exception on raise"
  artifacts:
    - path: "src/observability/spans.py"
      provides: "ATTR_* constants + observed_span() async ctx manager"
      exports: ["ATTR_SYMBOL","ATTR_TF","ATTR_PLUGIN","ATTR_TIER","ATTR_AGENT_ID","ATTR_SIGNAL_ID","ATTR_GROUP_ID","ATTR_BATCH_SZ","ATTR_FLUSH_MS","observed_span"]
    - path: "src/observability/otel.py"
      provides: "service.instance.id Resource attribute"
      contains: "service.instance.id"
  key_links:
    - from: "src/observability/spans.py"
      to: "opentelemetry.trace"
      via: "trace.get_tracer('indicagent')"
      pattern: "get_tracer\\(.indicagent.\\)"
---

<objective>
Create `src/observability/spans.py` (new file) with ATTR_* constants and `observed_span()` async context manager. Add `service.instance.id` to OTel Resource in `otel.py`.

Purpose: Foundation for span enrichment across base classes (Plan 02) and pipeline observability. Purely additive — no call sites change.
Output: Two files updated/created, ready for downstream span enrichment.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/083-observability-hardening/083-CONTEXT.md
@docs/plans/2026-05-15-observability-hardening-design.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/observability/spans.py</name>
  <files>src/observability/spans.py</files>
  <read_first>
    - docs/plans/2026-05-15-observability-hardening-design.md (Change 2 section for exact content)
    - src/observability/otel.py (to see existing tracer init pattern)
  </read_first>
  <action>
    Create new file `src/observability/spans.py` with exact content from design doc Change 2:

    Imports: `from contextlib import asynccontextmanager`, `from opentelemetry import trace`, `from opentelemetry.trace import StatusCode`.

    Module-level ATTR_* string constants (exact values from design):
    - `ATTR_SYMBOL    = "symbol"`
    - `ATTR_TF        = "tf"`
    - `ATTR_PLUGIN    = "plugin_name"`
    - `ATTR_TIER      = "intelligence_tier"`
    - `ATTR_AGENT_ID  = "agent_id"`
    - `ATTR_SIGNAL_ID = "signal_id"`
    - `ATTR_GROUP_ID  = "group_id"`
    - `ATTR_BATCH_SZ  = "batch_size"`
    - `ATTR_FLUSH_MS  = "flush_ms"`

    Async context manager `observed_span(name: str, tracer=None, **attrs)`:
    - default tracer via `trace.get_tracer("indicagent")`
    - uses `_tracer.start_as_current_span(name, attributes=attrs)`
    - on exception: `span.set_status(StatusCode.ERROR, str(exc))` + `span.record_exception(exc)` + re-raise
  </action>
  <verify>
    .venv/bin/python -c "from src.observability.spans import observed_span, ATTR_SYMBOL, ATTR_TF, ATTR_PLUGIN, ATTR_TIER, ATTR_AGENT_ID, ATTR_SIGNAL_ID, ATTR_GROUP_ID, ATTR_BATCH_SZ, ATTR_FLUSH_MS; print('OK')"
    .venv/bin/ruff check src/observability/spans.py
  </verify>
  <acceptance_criteria>
    - File `src/observability/spans.py` exists.
    - Grep `grep -E 'ATTR_(SYMBOL|TF|PLUGIN|TIER|AGENT_ID|SIGNAL_ID|GROUP_ID|BATCH_SZ|FLUSH_MS)' src/observability/spans.py` matches all 9 constants.
    - Grep `grep -E 'async def observed_span' src/observability/spans.py` matches.
    - Grep `grep 'StatusCode.ERROR' src/observability/spans.py` matches.
    - Grep `grep 'record_exception' src/observability/spans.py` matches.
    - Import test command above prints `OK`.
    - `ruff check` exits 0 for this file.
  </acceptance_criteria>
  <done>spans.py importable; constants and observed_span exported with proper error semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add service.instance.id to OTel Resource</name>
  <files>src/observability/otel.py</files>
  <read_first>
    - src/observability/otel.py (must read full file to locate Resource.create() call and current imports)
  </read_first>
  <action>
    In `src/observability/otel.py`:

    1. Ensure imports include `os` and `socket` at top (add `import socket` if missing; `import os` likely present).
    2. Locate the `Resource.create({...})` call (around lines 32-38 per research).
    3. Add new entry to the dict: `"service.instance.id": f"{socket.gethostname()}:{os.getpid()}"`
    4. Do NOT modify or remove existing keys (`service.name`, `service.version`, `deployment.environment`).
    5. Do NOT touch `OTLPMetricExporter` / `PeriodicExportingMetricReader` setup in this plan — that's deferred to Plan 03.
  </action>
  <verify>
    grep -n "service.instance.id" src/observability/otel.py
    grep -n "socket.gethostname" src/observability/otel.py
    .venv/bin/python -c "import src.observability.otel; print('OK')"
    .venv/bin/ruff check src/observability/otel.py
  </verify>
  <acceptance_criteria>
    - `grep -F 'service.instance.id' src/observability/otel.py` matches exactly one line.
    - `grep -E 'socket\.gethostname\(\)' src/observability/otel.py` matches.
    - `grep -E 'os\.getpid\(\)' src/observability/otel.py` matches.
    - `grep -E '^import socket' src/observability/otel.py` matches.
    - Import test command exits 0.
    - `ruff check` exits 0.
  </acceptance_criteria>
  <done>Resource attribute `service.instance.id` formatted as `hostname:pid` present at module init.</done>
</task>

</tasks>

<verification>
.venv/bin/pytest tests/unit/ -q   # green
.venv/bin/ruff check src/observability/
</verification>

<success_criteria>
- spans.py exists with all 9 ATTR_* constants and observed_span
- otel.py Resource includes service.instance.id
- pytest green; ruff clean
</success_criteria>

<output>
After completion, create `.planning/phases/083-observability-hardening/083-01-SUMMARY.md`.
</output>
