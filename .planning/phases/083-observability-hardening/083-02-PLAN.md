---
phase: 083-observability-hardening
plan: 02
type: execute
wave: 2
depends_on: ["083-01"]
files_modified:
  - src/core/agent/base.py
  - src/core/base_group_service.py
  - src/core/ai/base_agent.py
  - src/core/llm/chain.py
  - services/intelligence_pipeline_agent.py
autonomous: true

must_haves:
  truths:
    - "Every base-class start_as_current_span block sets ERROR status and records exception on raise"
    - "Pipeline span sites in intelligence_pipeline_agent.py use observed_span"
    - "Span attributes use ATTR_* constants from spans.py, not raw strings"
  artifacts:
    - path: "src/core/ai/base_agent.py"
      provides: "Span error recording for ai.compute / ai.llm_generate"
      contains: "StatusCode.ERROR"
    - path: "services/intelligence_pipeline_agent.py"
      provides: "observed_span usage at pipeline.process_bar and pipeline.run_i7"
      contains: "observed_span"
  key_links:
    - from: "services/intelligence_pipeline_agent.py"
      to: "src/observability/spans.py"
      via: "from src.observability.spans import observed_span, ATTR_*"
      pattern: "from src\\.observability\\.spans import"
---

<objective>
Enrich every existing `start_as_current_span` block in base classes with: (1) ERROR status + exception recording on raise, (2) ATTR_* constants from `spans.py` instead of raw attribute strings. Replace the 2 pipeline span sites in `intelligence_pipeline_agent.py` with `observed_span`.

Purpose: Spans become useful in Tempo — failed bars show as ERROR, filtering by plugin_name/tier/signal_id works.
Output: Base classes auto-instrument with consistent attribute schema and error semantics.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/083-observability-hardening/083-CONTEXT.md
@docs/plans/2026-05-15-observability-hardening-design.md
@src/observability/spans.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locate all base-class span sites</name>
  <files>(read-only discovery)</files>
  <read_first>
    - src/core/agent/base.py
    - src/core/base_group_service.py
    - src/core/ai/base_agent.py
    - src/core/llm/chain.py
    - services/intelligence_pipeline_agent.py
  </read_first>
  <action>
    Run discovery to enumerate actual span sites and confirm file paths:

    `grep -rn "start_as_current_span" src/core/ services/intelligence_pipeline_agent.py`
    `find src/core -name "base_writer*" -o -name "base_provider*"`

    Research note: `src/core/base_writer.py` and `src/core/base_provider_agent.py` may not exist as standalone files. If absent, skip them — do NOT create. The authoritative span sites are whatever `grep` returns.

    Record in commit message the actual list of (file, line, span name) processed.
  </action>
  <verify>
    grep -rn "start_as_current_span" src/core/ services/intelligence_pipeline_agent.py | wc -l
  </verify>
  <acceptance_criteria>
    - Discovery commands have been run.
    - Subsequent tasks operate only on files that exist (verified via `test -f`).
  </acceptance_criteria>
  <done>Authoritative list of span sites enumerated.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich base-class spans with ERROR status + ATTR_* constants</name>
  <files>
    src/core/agent/base.py
    src/core/base_group_service.py
    src/core/ai/base_agent.py
    src/core/llm/chain.py
  </files>
  <read_first>
    - src/observability/spans.py (for ATTR_* names)
    - src/core/agent/base.py
    - src/core/base_group_service.py
    - src/core/ai/base_agent.py (span sites at lines 82 and 192 per research)
    - src/core/llm/chain.py (span site at line 93 per research)
  </read_first>
  <action>
    For each `start_as_current_span(...)` block in the files above (only those that exist; if `base_writer.py` / `base_provider_agent.py` exist after discovery, include them):

    1. Add to top of file (if missing):
       `from opentelemetry.trace import StatusCode`
       `from src.observability.spans import ATTR_SYMBOL, ATTR_TF, ATTR_PLUGIN, ATTR_TIER, ATTR_AGENT_ID, ATTR_SIGNAL_ID, ATTR_GROUP_ID, ATTR_BATCH_SZ, ATTR_FLUSH_MS` (import only what's used in that file)

    2. Wrap span body in try/except:
       ```
       with tracer.start_as_current_span(name, attributes={...}) as span:
           try:
               <existing body>
           except Exception as exc:
               span.set_status(StatusCode.ERROR, str(exc))
               span.record_exception(exc)
               raise
       ```

    3. In the `attributes={...}` dict (or subsequent `set_attribute` calls), replace raw string keys with ATTR_* constants. Examples:
       - `"symbol"` → `ATTR_SYMBOL`
       - `"tf"` → `ATTR_TF`
       - `"plugin_name"` → `ATTR_PLUGIN`
       - `"intelligence_tier"` → `ATTR_TIER`
       - `"agent_id"` → `ATTR_AGENT_ID`
       - `"signal_id"` → `ATTR_SIGNAL_ID`
       - `"group_id"` → `ATTR_GROUP_ID`
       - `"batch_size"` → `ATTR_BATCH_SZ`
       - `"flush_ms"` → `ATTR_FLUSH_MS`

    4. Do NOT change span names. Do NOT add new spans. Do NOT change business logic.
  </action>
  <verify>
    grep -rn "StatusCode.ERROR" src/core/agent/base.py src/core/base_group_service.py src/core/ai/base_agent.py src/core/llm/chain.py
    .venv/bin/pytest tests/unit/ -q
  </verify>
  <acceptance_criteria>
    - Every file modified contains at least one `StatusCode.ERROR` reference.
    - Every file modified contains at least one `record_exception(` call.
    - Every file modified imports from `src.observability.spans` at least one ATTR_* constant.
    - No remaining literal `"symbol"` / `"plugin_name"` / `"intelligence_tier"` strings inside `set_attribute(` or `attributes={` blocks in these files (grep returns 0).
    - `.venv/bin/pytest tests/unit/ -q` exits 0.
    - `.venv/bin/ruff check src/core/` exits 0.
  </acceptance_criteria>
  <done>Base-class spans report errors and use canonical attribute names.</done>
</task>

<task type="auto">
  <name>Task 3: Replace 2 pipeline span sites with observed_span</name>
  <files>services/intelligence_pipeline_agent.py</files>
  <read_first>
    - services/intelligence_pipeline_agent.py (spans at line 841 = pipeline.process_bar, line 1267 = pipeline.run_i7)
    - src/observability/spans.py
  </read_first>
  <action>
    At the two span sites (`pipeline.process_bar` ~line 841 and `pipeline.run_i7` ~line 1267):

    1. Add import: `from src.observability.spans import observed_span, ATTR_SYMBOL, ATTR_TF`
    2. Replace `with tracer.start_as_current_span("pipeline.process_bar", attributes={"symbol": symbol, "tf": tf}) as span:` (or similar) with:
       `async with observed_span("pipeline.process_bar", **{ATTR_SYMBOL: symbol, ATTR_TF: tf}) as span:`
    3. Same for `pipeline.run_i7` span.
    4. Remove the now-redundant try/except span error handling at these sites (observed_span handles it).
    5. Preserve all existing inner body logic exactly.
  </action>
  <verify>
    grep -n "observed_span" services/intelligence_pipeline_agent.py
    grep -c "start_as_current_span" services/intelligence_pipeline_agent.py
    .venv/bin/pytest tests/unit/ -q
    .venv/bin/ruff check services/intelligence_pipeline_agent.py
  </verify>
  <acceptance_criteria>
    - `grep -c 'observed_span' services/intelligence_pipeline_agent.py` returns >= 2.
    - `grep -E 'start_as_current_span\("pipeline\.(process_bar|run_i7)"' services/intelligence_pipeline_agent.py` returns 0 matches.
    - `grep "from src.observability.spans import" services/intelligence_pipeline_agent.py` matches.
    - Pytest green; ruff clean.
  </acceptance_criteria>
  <done>Both pipeline span sites use observed_span with ATTR_* attributes.</done>
</task>

</tasks>

<verification>
.venv/bin/pytest tests/unit/ -q
.venv/bin/ruff check src/ services/intelligence_pipeline_agent.py
</verification>

<success_criteria>
- All base-class spans record exceptions with ERROR status
- All span attributes use ATTR_* constants
- Pipeline spans use observed_span
</success_criteria>

<output>
After completion, create `.planning/phases/083-observability-hardening/083-02-SUMMARY.md`.
</output>
