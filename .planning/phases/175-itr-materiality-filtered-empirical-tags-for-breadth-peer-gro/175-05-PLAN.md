---
phase: 175-itr-materiality-filtered-empirical-tags-for-breadth-peer-gro
plan: 05
type: execute
wave: 4
depends_on: ["175-01", "175-02", "175-03", "175-04"]
files_modified:
  - docs/foundation/instrument-tag-registry.md
  - docs/foundation/apr-calibration-backlog.md
  - CLAUDE.md
  - .planning/todos/PRIORITIES.md
  - .planning/todos/pending/125-tag-calibrator-discovery-oos-gate-not-enforced.md
  - .planning/todos/pending/126-instrument-tags-valid-to-no-consumer-contract.md
  - .planning/todos/pending/380-itr-materiality-filtered-empirical-tags-and-eq-prefix-naming-collision.md
autonomous: true
requirements: [P175-04, P175-06, P175-07, P175-08]

must_haves:
  truths:
    - "The ITR canonical spec describes TagCalibrator as a 4-pass engine with the materiality evidence columns and the instrument_tags_active read contract"
    - "The two Known Gaps the ITR spec flagged are annotated with their current, accurate status"
    - "All ten new uncalibrated [initial_estimate] materiality threshold keys appear in the APR calibration backlog, and the eleventh key (null_arm_seed, [conventional]) is documented in the ITR spec but deliberately absent from the backlog"
    - "The sign-stability sliding-bar resolution is discoverable from the backlog entry, so a future recalibration starts from the recorded reasoning rather than re-deriving it"
    - "Todos 125 and 126 are closed with resolution records, and 126's stale reference to the deleted equity_regime_model.py is corrected"
    - "Todo 380 remains open, scoped down to the consumer-cutover decision the shadow diagnostic gates"
    - "PRIORITIES.md link integrity holds under the CI guard"
  artifacts:
    - path: "docs/foundation/instrument-tag-registry.md"
      provides: "Canonical 4-pass ITR spec including the materiality filter and its read contract"
      contains: "instrument_tags_active"
    - path: "docs/foundation/apr-calibration-backlog.md"
      provides: "The ten new [initial_estimate] materiality threshold keys registered as needing real calibration, with a pointer to the sign-stability resolution and the plan 04 near-miss evidence"
      contains: "alpha.tag_calibrator.materiality"
  key_links:
    - from: "docs/foundation/instrument-tag-registry.md"
      to: "services/tag_calibrator.py"
      via: "Pass 4 description matching the shipped implementation"
      pattern: "passes_materiality"
    - from: ".planning/todos/PRIORITIES.md"
      to: ".planning/todos/pending/"
      via: "CI-enforced link integrity (tests/unit/test_todo_priorities_link_integrity.py)"
      pattern: "\\(pending/"
---

<pre_execution_gate requirement="P175-08" blocking="true" applies_to="whole phase">
## P175-08 (D-07): cross-AI review of this PLAN.md set. Required BEFORE Wave 1.

**This is a process gate, not a task. It is satisfied outside plan execution, in the window
between `/gsd-plan-phase 175` and `/gsd-execute-phase 175`. No plan in this phase implements
it, and no executor can discharge it.** It is recorded here because plan 05 is the phase's
documentation plan and already carries the D-07 narrative; the gate itself binds the entire
phase and must clear before plan 01 (Wave 1) runs, not before plan 05 (Wave 4) runs.

**Who:** Fable, plus Codex and/or AGY (`codex exec --model "$MODEL" --skip-git-repo-check -`
for Codex; `agy -p "<literal prompt string>"` for AGY, which cannot read stdin).

**What is under review:** all five `175-0N-PLAN.md` files as written, before any code ships.
The review targets the planner resolutions that RESEARCH.md's Open Questions left to judgment:

- **R-01** (plan 02): Pearson rather than Spearman for `partial_loading`, and the new
  `factor_math.py` Pearson sibling to `partial_spearman_ic` that follows from it.
- **R-02** (plan 03): the D-06 null arm runs inline inside `TagCalibrator.execute()` rather
  than in the offline `scripts/analysis/` diagnostic, overriding RESEARCH.md's Architectural
  Responsibility Map row for null-arm validation and its own recommendation.
- **R-03** (plan 01): four control legs only (SPY, TLT, HYG-IEF, UUP), no DBC commodity leg,
  stored as an APR JSON behavioral list.
- **R-04** (plan 01): `passes_materiality` as the statistical gate ONLY, resolving the direct
  contradiction between RESEARCH.md Pitfall 4 and 175-PATTERNS.md line 132.
- **R-06** (plan 02): ΔR² rather than squared partial correlation as the incremental-R²
  definition.
- **R-07** (plan 03): Pass 4 measuring against the symbol's full available history rather than
  `tag_vocabulary.lookback_days`.

**Exit condition:** each of R-01, R-02, R-03, R-04, R-06 and R-07 is either endorsed or
amended by the reviewers, and any amendment is folded back into the affected PLAN.md before
Wave 1 starts. If the review is skipped, this phase has not satisfied P175-08 regardless of
whether every plan executes green.

**Separate, later D-07 review (NOT this gate, do not conflate):** the plan 04 shadow report,
meaning the measured output rather than the code, gets its own cross-AI review. That second
review gates any future consumer cutover for `breadth_vol.py` and
`cross_sectional_regime_model.py`. It is explicitly out of scope for this phase, which ships
shadow-mode measurement only (D-03), and is tracked by todo 380, which Task 2 keeps open.
</pre_execution_gate>

<objective>
Bring the canonical documentation and the todo ledger in line with what phase 175 actually
shipped, and close the two folded todos (125, 126).

Purpose: this project's own standing rules make this load-bearing, not hygiene.
`docs/foundation/instrument-tag-registry.md` is the ITR's single source of truth and already
carries a Known Gaps section naming both folded todos.
`docs/foundation/apr-calibration-backlog.md` is the register that keeps uncalibrated
`[initial_estimate]` gates from silently hardening into settled numbers, and D-05 explicitly
routes the ten new gate thresholds there (the eleventh key, `null_arm_seed`, is
`[conventional]` and deliberately stays out of the backlog). PRIORITIES.md link integrity is
CI-enforced.
Output: updated canonical docs, two closed todos, one re-scoped todo, an intact PRIORITIES.md.

**P175-08 / D-07 note for the executor of this plan:** the `<pre_execution_gate>` block above
is the phase's cross-AI review gate. It carries no task and produces no artifact, so nothing
in this plan's task list discharges it. Do not treat its absence from the task list as a
planning omission, and do not attempt to perform the review during execution: by the time
Wave 4 runs, the window for it has already closed.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/175-itr-materiality-filtered-empirical-tags-for-breadth-peer-gro/175-CONTEXT.md
@.planning/phases/175-itr-materiality-filtered-empirical-tags-for-breadth-peer-gro/175-01-SUMMARY.md
@.planning/phases/175-itr-materiality-filtered-empirical-tags-for-breadth-peer-gro/175-02-SUMMARY.md
@.planning/phases/175-itr-materiality-filtered-empirical-tags-for-breadth-peer-gro/175-03-SUMMARY.md
@.planning/phases/175-itr-materiality-filtered-empirical-tags-for-breadth-peer-gro/175-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the ITR canonical spec, the APR calibration backlog, and CLAUDE.md</name>
  <files>docs/foundation/instrument-tag-registry.md, docs/foundation/apr-calibration-backlog.md, CLAUDE.md</files>
  <read_first>
    - docs/foundation/instrument-tag-registry.md (full file - the existing 3-pass engine description, the instrument_tags/tag_vocabulary schema tables, the alpha.tag_calibrator.* APR namespace listing, and the Known Gaps section naming both folded todos)
    - docs/foundation/apr-calibration-backlog.md (the Section 1 table format `| Key | Value | Gates | Why it's a guess |` and the preamble's gate-shaped-threshold scope note)
    - CLAUDE.md § "Instrument Tag Registry (ITR)" (the single compressed paragraph that must stay accurate and stay compressed)
    - services/tag_calibrator.py (final state after plan 03 - document what shipped, not what was planned; note `MaterialityConfig` carries eleven APR-bound fields, ten thresholds plus `null_arm_seed`)
    - .planning/phases/175-itr-materiality-filtered-empirical-tags-for-breadth-peer-gro/175-04-SUMMARY.md (the measured shadow-report numbers to cite)
  </read_first>
  <action>
(a) `docs/foundation/instrument-tag-registry.md`:

  - Change every "3-pass" / "three-pass" description of `TagCalibrator` to "4-pass", and add a
    Pass 4 subsection describing: the Pearson-convention partial loading against the
    SPY / TLT / HYG-IEF / UUP control set (R-01, R-03), ΔR² as the incremental-R² definition
    (R-06), the disjoint tail-anchored sign-stability windows, the Fisher-z lower-CI bound, and
    the D-06 circular-shift null arm that shifts the factor proxy's own return series only.
    State that Pass 4 runs against the symbol's full available history rather than
    `tag_vocabulary.lookback_days` (R-07), and why: a 4-control partial regression with a 756
    observation floor and 4 x 252-day stability windows cannot fit in Pass 1's 252-bar window.
  - Document the two control-set exclusions: a control leg is dropped when it equals the tag's
    own `factor_series`, and when the candidate symbol is that control or one leg of its
    long-short spread. Name this as the F6.1 / CR-01 tautology guard extended to the controls.
  - Extend the `instrument_tags` schema table with the eleven columns migration 346 added, each
    with its meaning. The `passes_materiality` row MUST state it is the statistical gate ONLY,
    and that readers AND it with `discovery_state = 'confirmed'` and `valid_to IS NULL` via
    `services.tag_calibrator.is_materiality_eligible()`, the canonical predicate (R-04).
  - Add all eleven `alpha.tag_calibrator.materiality.*` keys to the APR namespace listing. The
    ten gate thresholds are flagged `[initial_estimate]` with a pointer to
    `docs/foundation/apr-calibration-backlog.md`. The eleventh, `null_arm_seed`, is flagged
    `[conventional]` (42, the project's standing default seed), is APR-backed per the mandate's
    category 1 ("seeds that affect algorithm output"), carries the warning that changing it
    invalidates every previously computed `null_arm_p_value`, and is deliberately NOT a
    calibration-backlog entry because it is not a gate-shaped threshold. Note that
    `control_factor_series` is a JSON behavioral list, so adding a commodity leg (DBC) later is
    an APR edit rather than a code change.
  - State the measurement universe explicitly (D-01a, amended 2026-09-18): Pass 4 measures every
    KEPT empirical row, where kept is Pass 1-3's `passes_fdr AND abs(loading) >=
    loading_threshold` gate. A row that fails that gate is written `passes_materiality = false`
    rather than measured, so `passes_fdr = false` can never sit beside
    `passes_materiality = true` on the same row.
  - Document the sign-stability gate as a deliberate discrete step at n=1008 (`4 x 252`), not
    a gradual slide: because `min_sample_n` (756) is exactly three `sign_stability_window_days`
    (252) windows, a symbol below n=1008 has three evaluable windows and must show 3-of-3 sign
    agreement, while a symbol at or above it needs 3-of-4 - a hard jump at one specific sample
    count. The agreement bar is stricter for shorter-history symbols, by construction, and is
    retained pending corpus evidence (D-04/D-05). Cite plan 04's NEAR MISS reporting as
    the evidence source for any future recalibration.
  - Add a "Read contract" subsection establishing `instrument_tags_active` as the required read
    path for any tag-membership query (todo 126), and noting that the two live consumers
    (`breadth_vol.py`, reached through `cross_sectional_regime_model.py`'s group resolution, and
    that module's own `_load_tags_by_symbol`) still use the `source='human'` stopgap and have
    NOT been cut over. Shadow mode only, D-03.
  - Update the Known Gaps section. Mark the `discovery_oos_days`-unenforced gap RESOLVED
    (todo 125, this phase: `discovery_state` is a typed column and `is_materiality_eligible`
    reads it). Rewrite the "`sensitivity`/`macro_driver` tags unconsumed" gap to its accurate
    current status: still unconsumed by any live query, but now measured and eligibility
    flagged, with cutover gated on the shadow diagnostic plus the D-07 review.
  - Cite the plan 04 shadow report's headline numbers (empirical rows measured, rows clearing
    the bar, the per-group membership delta) as the current empirical picture, with the date.
    Do not cite any number that is not in `175-04-SUMMARY.md`.

(b) `docs/foundation/apr-calibration-backlog.md`: add one Section 1 row per new key. The
`Gates` column states what each actually gates (for example, `min_partial_loading` gates Pass
4's `passes_materiality` statistical decision in `tag_calibrator.py`). The `Why it's a guess`
column names the source (Codex's proposal for the seven statistical thresholds; the project's
standing `fdr_alpha` convention for `null_arm_alpha`) and states that none were derived from
this corpus's actual partial-loading distribution, which D-05 acknowledges explicitly - and
that the plan 04 GATE ATTRIBUTION output is the first real evidence about which is binding.

Add one line to the `min_sign_stable_windows` / `sign_stability_window_count` rows (or a short
note beneath the table) recording that the sign-stability gate's semantics were explicitly
resolved during phase 175's D-07 cross-AI review rather than left ambiguous: the gate is a
discrete step at n=1008 (3-of-3 below that sample count, 3-of-4 at or above it - a hard jump,
not a gradual slide), it was deliberately left unchanged because nothing downstream is gated
in shadow mode, and the evidence for any
future loosen/tighten/leave-alone decision is plan 04's NEAR MISS section (the sign-stability
window histogram for FAILING candidates plus the per-gate `sole_failure` counts). Point at
`175-03-PLAN.md` § R-07 and `175-04-SUMMARY.md` § NEAR MISS by name, so whoever picks up the
recalibration starts from the recorded reasoning instead of re-deriving it. Do NOT add a row
for `null_arm_seed`: it is `[conventional]`, not an uncalibrated gate.
Bump the file's `**Last Updated:**` line to today's date.

(c) `CLAUDE.md` § "Instrument Tag Registry (ITR)": extend the existing paragraph with one or
two sentences stating that `TagCalibrator` now also computes a materiality filter (Pass 4:
partial loading orthogonalized against a four-leg control set, incremental R², sign stability,
circular-shift null arm) persisted as `instrument_tags` evidence columns; that
`passes_materiality` is the statistical gate only and must be AND-ed with
`discovery_state='confirmed'` and `valid_to IS NULL` via `is_materiality_eligible()`; and that
this is shadow-mode measurement only, with no live consumer reading it and both consumers
still filtering `source='human'`. Keep the compressed single-paragraph style; do not add a new
section. Bump CLAUDE.md's `Version:` patch number.

Prose style across all three files: no em dashes, sentence case headings, no emojis, present
tense, minimal bold.
  </action>
  <verify>
    <automated>grep -c 'alpha.tag_calibrator.materiality' docs/foundation/apr-calibration-backlog.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'alpha.tag_calibrator.materiality' docs/foundation/apr-calibration-backlog.md` returns at least `10` (the ten `[initial_estimate]` gate thresholds)
    - `grep -c 'null_arm_seed' docs/foundation/apr-calibration-backlog.md` returns `0` (the `[conventional]` seed is not a calibration target)
    - `grep -c 'null_arm_seed' docs/foundation/instrument-tag-registry.md` returns at least `1` (all eleven keys are documented in the spec)
    - `grep -ci 'near miss' docs/foundation/apr-calibration-backlog.md` returns at least `1` (the sign-stability resolution points at its evidence source)
    - `grep -c 'instrument_tags_active' docs/foundation/instrument-tag-registry.md` returns at least `1`
    - `grep -c 'is_materiality_eligible' docs/foundation/instrument-tag-registry.md` returns at least `1`
    - `grep -c 'is_materiality_eligible' CLAUDE.md` returns at least `1`
    - `grep -ci '3-pass\|three-pass' docs/foundation/instrument-tag-registry.md` returns `0`
    - `grep -c 'passes_materiality' docs/foundation/instrument-tag-registry.md` returns at least `2`
    - `grep -c '4-pass' docs/foundation/instrument-tag-registry.md` returns at least `1`
    - The em-dash count in `docs/foundation/instrument-tag-registry.md` does not increase versus the pre-task file (project writing rule: no em dashes in newly written prose)
    - CLAUDE.md's `Version:` line differs from its pre-task value
    - Every number cited in the new ITR spec text also appears in `175-04-SUMMARY.md`
  </acceptance_criteria>
  <done>The ITR spec documents the shipped 4-pass engine, all eleven APR keys, the D-01a measurement universe, the sign-stability sliding bar and the read contract; both Known Gaps carry accurate status; the ten threshold keys plus the sign-stability resolution are in the calibration backlog; CLAUDE.md's ITR paragraph is current.</done>
</task>

<task type="auto">
  <name>Task 2: Close todos 125 and 126, re-scope todo 380, keep PRIORITIES.md link integrity</name>
  <files>.planning/todos/PRIORITIES.md, .planning/todos/pending/125-tag-calibrator-discovery-oos-gate-not-enforced.md, .planning/todos/pending/126-instrument-tags-valid-to-no-consumer-contract.md, .planning/todos/pending/380-itr-materiality-filtered-empirical-tags-and-eq-prefix-naming-collision.md</files>
  <read_first>
    - .planning/todos/pending/125-tag-calibrator-discovery-oos-gate-not-enforced.md (full file, including its addendum recommending typed-column promotion over split JSONB state)
    - .planning/todos/pending/126-instrument-tags-valid-to-no-consumer-contract.md (full file - note its stale reference to equity_regime_model.py, deleted by todo 381 on 2026-09-17)
    - .planning/todos/pending/380-itr-materiality-filtered-empirical-tags-and-eq-prefix-naming-collision.md (full file - the source todo for this phase; part 2, the eq_* naming item, was already downgraded to a documentation note on 2026-09-17 and is NOT this phase's work)
    - .planning/todos/PRIORITIES.md lines 310-320 and 490-500 (the existing rows for 380, 125 and 126)
    - tests/unit/test_todo_priorities_link_integrity.py (the CI guard this task must not break: every pending/ file needs a matching link, every link needs an existing file)
    - CLAUDE.md § "Key Rules" (the `git add` multi-pathspec abort gotcha - stage an already-renamed path alone first, letting git auto-detect the rename, before batching it with others)
  </read_first>
  <action>
(a) Append a `## Resolution (2026-09-18, phase 175)` section to
`.planning/todos/pending/125-tag-calibrator-discovery-oos-gate-not-enforced.md` recording: the
gate is now enforced; `discovery_state` and `first_measured_at` were promoted from the
`evidence` JSONB to typed `instrument_tags` columns by migration 346, exactly as this todo's
own addendum recommended; the JSONB copy is retained for backward compatibility (state the
result of plan 03's `grep -rn "discovery_state\|first_measured_at" src/ services/ scripts/`
sweep verbatim); `is_materiality_eligible()` is the read-time consumer of the state; and
`tests/unit/test_tag_calibrator.py::test_discovery_oos_gate_blocks_fresh_discovery` is the
test this todo said was missing.

(b) Append a `## Resolution (2026-09-18, phase 175)` section to
`.planning/todos/pending/126-instrument-tags-valid-to-no-consumer-contract.md` recording: the
`instrument_tags_active` view now exists (migration 346) and is the established read path; the
shadow diagnostic reads through it; `is_materiality_eligible()` additionally re-checks
`valid_to is None` in code. In the same section, CORRECT this todo's stale reference to
`equity_regime_model.py` - that file was deleted by todo 381 on 2026-09-17; its live successor
for the equity breadth universe is `src/intelligence/regime_signals/breadth_vol.py`, reached
through `services/cross_sectional_regime_model.py`'s group resolution. Do not edit the
original body text; record the correction in the resolution section so the historical record
stays intact.

(c) Move both files to `.planning/todos/completed/` with `git mv`, preserving their filenames.

(d) Remove the rows for 125 and 126 from `.planning/todos/PRIORITIES.md`. Per the CI guard's
contract, a `pending/` link pointing at a moved file is a dead end, and a completed todo has no
place in the `pending/`-scoped index. If either row sits in a table whose surrounding prose
references it by number, update that prose too rather than leaving a dangling mention.

(e) Update todo 380's PRIORITIES.md row (do NOT close or move the file): phase 175 shipped the
materiality filter as shadow-mode measurement only. The remaining open item is the consumer
cutover decision for `breadth_vol.py` and `cross_sectional_regime_model.py`, gated on the plan
04 shadow report plus the user's and cross-AI reviewers' judgment. Also append a
`## Phase 175 status (2026-09-18)` section to the todo file itself stating the same, listing
what shipped (Pass 4, migration 346, the eleven APR keys, the diagnostic script, todos 125/126
closed) and what did not (any consumer query change), and restating that part 2 (the `eq_*`
naming item) remains a documentation note requiring no fix.

(f) Stage carefully: `git add` aborts entirely if ANY pathspec fails to match, and a
`pending/` to `completed/` rename is exactly that trap. Stage each renamed path alone first
(`git add -- .planning/todos/completed/125-...md`, then the 126 path), letting git auto-detect
the rename, before batching the remaining edited files.

(g) Run the link-integrity guard and the full unit suite before committing.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_todo_priorities_link_integrity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_todo_priorities_link_integrity.py -x -q` exits 0
    - `.venv/bin/pytest tests/unit/ -q` exits 0
    - `ls .planning/todos/completed/125-tag-calibrator-discovery-oos-gate-not-enforced.md` succeeds
    - `ls .planning/todos/completed/126-instrument-tags-valid-to-no-consumer-contract.md` succeeds
    - `ls .planning/todos/pending/125-tag-calibrator-discovery-oos-gate-not-enforced.md` fails (file moved, not copied)
    - `grep -c 'pending/125-' .planning/todos/PRIORITIES.md` returns `0`
    - `grep -c 'pending/126-' .planning/todos/PRIORITIES.md` returns `0`
    - `grep -c 'pending/380-' .planning/todos/PRIORITIES.md` returns `1` (380 stays open and tracked)
    - `grep -c 'Resolution (2026-09-18, phase 175)' .planning/todos/completed/126-instrument-tags-valid-to-no-consumer-contract.md` returns `1`
    - `grep -c 'breadth_vol.py' .planning/todos/completed/126-instrument-tags-valid-to-no-consumer-contract.md` returns at least `1` (the stale equity_regime_model.py reference is corrected in the resolution section)
    - `git status --porcelain` shows the two todo files as renames (`R`), not as delete-plus-add pairs
  </acceptance_criteria>
  <done>Todos 125 and 126 are closed with resolution records including 126's stale-path correction, 380 is re-scoped and still tracked, and the CI link-integrity guard passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical docs → every future reader and agent | `docs/foundation/` is this project's single source of truth; a stale spec becomes a confidently-wrong premise for the next phase |
| PRIORITIES.md → `pending/` directory | CI-enforced link integrity; a dead link or an untracked todo makes real work invisible |
| APR calibration backlog → threshold legitimacy | An uncalibrated `[initial_estimate]` gate that never reaches the backlog silently hardens into an apparently-settled number |
| planner resolutions (R-01 … R-07) → shipped statistical behavior | Six judgment calls RESEARCH.md left open decide what the filter actually measures; P175-08's pre-execution cross-AI review is the only check on them before code exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-175-23 | Repudiation | ITR spec describing a 3-pass engine after a 4th pass shipped | mitigate | Acceptance criterion asserts zero remaining `3-pass`/`three-pass` occurrences and at least one `4-pass` |
| T-175-24 | Repudiation | uncalibrated thresholds escaping the calibration backlog | mitigate | Acceptance criterion asserts at least 10 `alpha.tag_calibrator.materiality` rows in `apr-calibration-backlog.md` - the exact register D-05 routes them to |
| T-175-24b | Repudiation | the sign-stability resolution existing only in a plan file, invisible when the recalibration is eventually picked up | mitigate | Task 1(b) records it in the calibration backlog itself with named pointers to `175-03-PLAN.md` § R-07 and `175-04-SUMMARY.md` § NEAR MISS; grep-asserted |
| T-175-25 | Spoofing (of evidence) | documenting numbers that were never measured | mitigate | Acceptance criterion requires every cited number to appear in `175-04-SUMMARY.md`, which plan 04 populated from a real run |
| T-175-26 | Tampering | PRIORITIES.md drift when todos move to `completed/` | mitigate | `tests/unit/test_todo_priorities_link_integrity.py` is run as the task's automated verify; `git status` rename-detection is an acceptance criterion so the `git add` multi-pathspec abort cannot silently drop a staged path |
| T-175-27 | Repudiation | 126's stale `equity_regime_model.py` path outliving the file | mitigate | Resolution section corrects it to `breadth_vol.py` via `cross_sectional_regime_model.py`; grep-asserted |
| T-175-28 | Elevation of Privilege | closing todo 380 as if the cutover shipped | mitigate | 380 explicitly stays in `pending/` with its PRIORITIES row retained; acceptance criterion asserts `grep -c 'pending/380-'` returns 1 |
| T-175-29 | Repudiation | P175-08 silently skipped because it maps to no task, leaving six unreviewed statistical judgment calls shipped as if endorsed | mitigate | `<pre_execution_gate>` block states the gate, its owner, its six review targets and its exit condition in the plan set itself; P175-08 carried in this plan's `requirements` so coverage checking cannot lose it |
| T-175-SC | Tampering | supply chain | accept | Documentation-only plan; zero packages, zero code |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_todo_priorities_link_integrity.py -x -q` green
- `.venv/bin/pytest tests/unit/ -q` green
- ITR spec, APR backlog and CLAUDE.md all reflect the shipped 4-pass engine
- Todos 125 and 126 live in `completed/` as git renames; 380 stays open
- P175-08 is NOT verified here. It is a pre-execution gate that cleared (or did not) before
  Wave 1, and its record lives in the phase's execution log rather than in this plan's output.
</verification>

<success_criteria>
- `docs/foundation/instrument-tag-registry.md` documents Pass 4, the eleven evidence columns, the eleven APR keys, the D-01a measurement universe, the sign-stability sliding bar, the `instrument_tags_active` read contract, and the accurate status of both Known Gaps
- `docs/foundation/apr-calibration-backlog.md` carries all ten new `[initial_estimate]` threshold keys with honest provenance, plus the sign-stability sliding-bar resolution and a pointer to its evidence source
- CLAUDE.md's ITR paragraph names the materiality filter and its shadow-mode status
- Todos 125 and 126 closed with resolution records; 126's stale path corrected
- Todo 380 re-scoped to the cutover decision, still tracked in PRIORITIES.md
- Link-integrity CI guard and the full unit suite both green
</success_criteria>

<output>
Create `.planning/phases/175-itr-materiality-filtered-empirical-tags-for-breadth-peer-gro/175-05-SUMMARY.md` when done
</output>
