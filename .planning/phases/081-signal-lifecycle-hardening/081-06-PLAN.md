---
phase: 081-signal-lifecycle-hardening
plan: 06
type: execute
wave: 4
depends_on: ["081-02", "081-03", "081-04", "081-05"]
files_modified:
  - services/signal_metrics_compute_agent.py
  - production/alertmanager-rules.yml
  - src/observability/metrics.py
autonomous: true
requirements:
  - P81-METRICS

must_haves:
  truths:
    - "signal_ledger_backfill_ratio gauge updated periodically by signal_metrics_compute_agent reflecting is_backfill=TRUE / total over last 24h"
    - "Four Prometheus alerts defined: invalid_signal_total > 0 sustained 5min; signal_replay_ohlcv_gap_total > 10 sustained; signal_replay_unresolved_gauge growing 2 cycles; lifecycle_writer_idempotent_skip_total > 100/hour"
    - "All 11 metrics from D-09 are registered and reachable via /metrics endpoints of their respective agents"
  artifacts:
    - path: "src/observability/metrics.py"
      provides: "signal_ledger_backfill_ratio gauge registered (final metric of the 11)"
      contains: "signal_ledger_backfill_ratio"
    - path: "services/signal_metrics_compute_agent.py"
      provides: "Periodic update of signal_ledger_backfill_ratio gauge from signal_ledger query"
      contains: "signal_ledger_backfill_ratio"
    - path: "production/alertmanager-rules.yml"
      provides: "Four Phase 81 alert rules"
      contains: "P81"
  key_links:
    - from: "services/signal_metrics_compute_agent.py"
      to: "signal_ledger_backfill_ratio gauge"
      via: "periodic SQL query and .set()"
      pattern: "signal_ledger_backfill_ratio"
    - from: "production/alertmanager-rules.yml"
      to: "Prometheus rule engine"
      via: "alert: definitions"
      pattern: "alert:"
---

<objective>
Complete metric coverage and wire alerts. The previous plans registered 10 of the 11 metrics from D-09 (publisher, tracker, replay, writer, bar_replay). This plan adds the 11th — `signal_ledger_backfill_ratio` (gauge updated by signal_metrics_compute_agent) — and adds the four Prometheus alert rules from D-09.

Purpose: Operators need alerting on the new failure modes. Without alerts, the metrics are decorative.
Output: Final metric registered + agent updates gauge + alert rules wired.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/081-signal-lifecycle-hardening/081-CONTEXT.md
@.planning/phases/081-signal-lifecycle-hardening/081-02-SUMMARY.md
@.planning/phases/081-signal-lifecycle-hardening/081-03-SUMMARY.md
@.planning/phases/081-signal-lifecycle-hardening/081-04-SUMMARY.md
@.planning/phases/081-signal-lifecycle-hardening/081-05-SUMMARY.md
@docs/plans/2026-05-08-signal-lifecycle-hardening-design.md
@src/observability/metrics.py
@production/alertmanager-rules.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register signal_ledger_backfill_ratio gauge in src/observability/metrics.py</name>
  <files>src/observability/metrics.py</files>
  <read_first>
    - src/observability/metrics.py
  </read_first>
  <action>
    Add the final D-09 metric:

    ```python
    SIGNAL_LEDGER_BACKFILL_RATIO = Gauge(
        "signal_ledger_backfill_ratio",
        "Fraction of signal_ledger rows last 24h with is_backfill=TRUE (training set quality KPI)",
    )
    ```
  </action>
  <verify>
    `grep -c "signal_ledger_backfill_ratio" src/observability/metrics.py` >= 1
    `python -c "from src.observability.metrics import SIGNAL_LEDGER_BACKFILL_RATIO"` exits 0
  </verify>
  <acceptance_criteria>
    - Gauge `signal_ledger_backfill_ratio` registered (no labels)
    - Importable
  </acceptance_criteria>
  <done>All 11 D-09 metrics now registered.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SIGNAL_LEDGER_BACKFILL_RATIO update in signal_metrics_compute_agent.py</name>
  <files>services/signal_metrics_compute_agent.py</files>
  <read_first>
    - services/signal_metrics_compute_agent.py (find existing periodic loop / cycle method where other gauges are computed)
    - .planning/phases/081-signal-lifecycle-hardening/081-CONTEXT.md (D-09)
  </read_first>
  <action>
    In the existing periodic cycle of `SignalMetricsComputeAgent` (find the method that runs each interval and updates other gauges from `signal_ledger`), add a query for the backfill ratio over the last 24 hours and update the gauge:

    ```python
    from src.observability.metrics import SIGNAL_LEDGER_BACKFILL_RATIO

    # Inside the periodic compute method:
    async with self._pool.acquire() as conn:
        row = await conn.fetchrow(
            """
            SELECT
              COUNT(*) FILTER (WHERE is_backfill = TRUE)::float
                / NULLIF(COUNT(*), 0)::float AS backfill_ratio
            FROM signal_ledger
            WHERE timestamp >= NOW() - INTERVAL '24 hours'
            """
        )
    ratio = float(row["backfill_ratio"] or 0.0)
    SIGNAL_LEDGER_BACKFILL_RATIO.set(ratio)
    ```

    Place this update alongside other gauge updates in the same cycle method. Do NOT create a new periodic loop — reuse the existing cadence.

    If the agent does not yet have an asyncpg pool (older psycopg2 patterns), add one (`asyncpg.create_pool(self._settings.database_url)` in `_setup`, close in `_teardown`).
  </action>
  <verify>
    `grep -c "SIGNAL_LEDGER_BACKFILL_RATIO" services/signal_metrics_compute_agent.py` >= 2 (import + .set call)
    `grep -c "is_backfill = TRUE" services/signal_metrics_compute_agent.py` >= 1
    `grep -c "INTERVAL '24 hours'" services/signal_metrics_compute_agent.py` >= 1
    `python -m py_compile services/signal_metrics_compute_agent.py` exits 0
  </verify>
  <acceptance_criteria>
    - Gauge updated each cycle from `signal_ledger` query covering last 24 hours
    - Uses `is_backfill = TRUE` filter
    - `NULLIF(COUNT(*), 0)` guard against division by zero
    - File compiles clean
  </acceptance_criteria>
  <done>11/11 metrics wired and self-updating.</done>
</task>

<task type="auto">
  <name>Task 3: Add four Phase 81 Prometheus alert rules to production/alertmanager-rules.yml</name>
  <files>production/alertmanager-rules.yml</files>
  <read_first>
    - production/alertmanager-rules.yml (existing alert group structure, label conventions)
    - .planning/phases/081-signal-lifecycle-hardening/081-CONTEXT.md (D-09 alert list)
  </read_first>
  <action>
    Append a new group `phase81-signal-lifecycle` to `production/alertmanager-rules.yml` (preserve existing groups). Match the YAML style used by existing groups (read the file first to confirm whether they use `groups:` top-level and exact label conventions). Add:

    ```yaml
    - name: phase81-signal-lifecycle
      interval: 1m
      rules:
        - alert: P81_SignalTrackerInvalidSignals
          expr: increase(signal_tracker_invalid_signal_total[5m]) > 0
          for: 5m
          labels:
            severity: page
            phase: "81"
          annotations:
            summary: "signal_tracker rejecting signals (publisher contract violation)"
            description: "_load_signal() rejected {{ $value }} signals in last 5m. Reason label indicates which field. Publisher must guarantee timestamp/symbol/timeframe/entry_price/stop_loss."

        - alert: P81_SignalReplayOhlcvGap
          expr: increase(signal_replay_ohlcv_gap_total[15m]) > 10
          for: 5m
          labels:
            severity: page
            phase: "81"
          annotations:
            summary: "signal_replay_auditor finding signals with no OHLCV data in window"
            description: "{{ $value }} replay attempts had zero matching bars in market_data_ohlcv. Data integrity issue in OHLCV pipeline."

        - alert: P81_SignalReplayUnresolvedGrowing
          expr: deriv(signal_replay_unresolved_gauge[15m]) > 0
          for: 10m
          labels:
            severity: page
            phase: "81"
          annotations:
            summary: "signal_replay_unresolved_gauge growing across 2+ cycles (replay auditor stuck)"
            description: "Unresolved v1 signals trending upward. North star invariant violated. Investigate signal_replay_auditor logs."

        - alert: P81_LifecycleWriterIdempotentSkipHigh
          expr: increase(lifecycle_writer_idempotent_skip_total[1h]) > 100
          for: 5m
          labels:
            severity: warn
            phase: "81"
          annotations:
            summary: "lifecycle_writer idempotency guard firing frequently (>100/hour)"
            description: "Two-path collision rate elevated. Investigate whether live tracker and replay auditor are racing on the same signals."
    ```

    Verify the file is volume-mounted into Prometheus per CLAUDE.md note (no action needed if already mounted; just confirm via `grep alertmanager-rules.yml production/docker-compose.yml`).
  </action>
  <verify>
    `grep -c "phase81-signal-lifecycle" production/alertmanager-rules.yml` returns 1
    `grep -c "P81_SignalTrackerInvalidSignals\|P81_SignalReplayOhlcvGap\|P81_SignalReplayUnresolvedGrowing\|P81_LifecycleWriterIdempotentSkipHigh" production/alertmanager-rules.yml` >= 4
    `python -c "import yaml; yaml.safe_load(open('production/alertmanager-rules.yml'))"` exits 0
    `grep "alertmanager-rules.yml" production/docker-compose.yml` returns at least one match (mount confirmation)
  </verify>
  <acceptance_criteria>
    - Group `phase81-signal-lifecycle` added with `interval: 1m`
    - All four alerts present with exact names: `P81_SignalTrackerInvalidSignals`, `P81_SignalReplayOhlcvGap`, `P81_SignalReplayUnresolvedGrowing`, `P81_LifecycleWriterIdempotentSkipHigh`
    - YAML parses cleanly
    - File is mounted into Prometheus container (verified via docker-compose.yml grep)
  </acceptance_criteria>
  <done>Alerts armed.</done>
</task>

</tasks>

<verification>
- All 11 D-09 metrics importable from `src.observability.metrics`.
- All four alerts present and YAML-valid.
</verification>

<success_criteria>
Metric coverage 100% complete (11/11), alerts wired, gauge self-updating from DB.
</success_criteria>

<output>
Create `.planning/phases/081-signal-lifecycle-hardening/081-06-SUMMARY.md` listing every metric, the agent that owns it, and every alert with its threshold.
</output>
