---
phase: 080-renaissance-swarm-intelligence-layer
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/intelligence/ai/alpha/regime_coherence_agent.py
  - src/intelligence/ai/alpha/regime_coherence_prompts.py
  - tests/unit/service_tests/test_regime_coherence_agent.py
autonomous: true
requirements: [P80-REGIME]

must_haves:
  truths:
    - "RegimeCoherenceAgentComputeAgent extends BaseMultiplierAgent and starts shadow_only=True"
    - "regime_coherence_prompts.py exposes ACTIVE_VERSION='regime_coherence_v1' and build_regime_coherence_prompt()"
    - "Validator clamps regime_fit and confidence to [0.0, 1.0] and tolerates malformed mismatches"
    - "Multiplier formula regime_fit × confidence is asserted by unit test"
  artifacts:
    - path: "src/intelligence/ai/alpha/regime_coherence_agent.py"
      provides: "RegimeCoherenceAgentComputeAgent — setup-vs-regime fit multiplier"
      contains: "class RegimeCoherenceAgentComputeAgent(BaseMultiplierAgent)"
    - path: "src/intelligence/ai/alpha/regime_coherence_prompts.py"
      provides: "PROMPT_REGISTRY + ACTIVE_VERSION + build_regime_coherence_prompt"
      contains: "ACTIVE_VERSION = \"regime_coherence_v1\""
    - path: "tests/unit/service_tests/test_regime_coherence_agent.py"
      provides: "Unit tests covering parse, multiplier formula, neutral on parse failure"
      contains: "def test_"
  key_links:
    - from: "src/intelligence/ai/alpha/regime_coherence_agent.py"
      to: "src/core/ai/multiplier_agent.py"
      via: "import BaseMultiplierAgent"
      pattern: "from src.core.ai.multiplier_agent import BaseMultiplierAgent"
---

<objective>
Implement RegimeCoherenceAgentComputeAgent (D-05) — judges whether the I7 winner setup TYPE (momentum / mean-reversion / breakout / SMC) is appropriate for the current HMM regime + trend regime. Extends BaseMultiplierAgent. Output: regime_fit × confidence multiplier in [0.0, 1.0]. Shadow only at start.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/080-renaissance-swarm-intelligence-layer/080-CONTEXT.md
@.planning/phases/080-renaissance-swarm-intelligence-layer/080-PATTERNS.md
@src/intelligence/ai/alpha/skeptic_agent.py
@src/intelligence/ai/alpha/skeptic_prompts.py
@src/core/ai/context.py

<interfaces>
Plan 01 outputs (BaseMultiplierAgent, prompt_utils additions) and Plan 04's correlation_agent.py serve as direct templates. Mirror them exactly with regime-specific field names.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create regime_coherence_prompts.py</name>
  <files>src/intelligence/ai/alpha/regime_coherence_prompts.py</files>
  <read_first>
    - src/intelligence/ai/alpha/skeptic_prompts.py
    - src/intelligence/ai/alpha/correlation_prompts.py (Plan 04 sibling)
    - src/core/ai/context.py
  </read_first>
  <behavior>
    - PROMPT_REGISTRY contains key "regime_coherence_v1"
    - ACTIVE_VERSION == "regime_coherence_v1"
    - build_regime_coherence_prompt(ctx) raises TypeError if ctx is not AIContext
    - Template instructs LLM to return JSON: {"regime_fit": float, "confidence": float, "mismatches": [str], "reasoning": str}
  </behavior>
  <action>
    Create file mirroring `correlation_prompts.py` (Plan 04). Substitute:
    - `ACTIVE_VERSION = "regime_coherence_v1"`
    - JSON schema: `{"regime_fit": <float 0..1>, "confidence": <float 0..1>, "mismatches": [<labels>], "reasoning": "<one short sentence>"}`
    - Role text: "You are a regime-coherence analyst. Decide whether the setup TYPE (momentum / mean-reversion / breakout / SMC) is appropriate for the current HMM regime and trend regime."
    - Define semantics: "regime_fit=1.0 means the setup TYPE matches the regime perfectly; 0.0 means it strongly mismatches." `mismatches` is a list of short label strings describing each detected conflict.
    - Function name: `build_regime_coherence_prompt(ctx)`
    - Same `format(...)` keyword args as correlation_prompts.py: symbol, timeframe, winner_plugin, winner_direction_label, winner_confidence, full_context_block.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from src.intelligence.ai.alpha.regime_coherence_prompts import ACTIVE_VERSION, PROMPT_REGISTRY, build_regime_coherence_prompt; assert ACTIVE_VERSION == 'regime_coherence_v1'; assert 'regime_coherence_v1' in PROMPT_REGISTRY; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ACTIVE_VERSION = "regime_coherence_v1"' src/intelligence/ai/alpha/regime_coherence_prompts.py` returns 1
    - `grep -c "def build_regime_coherence_prompt" src/intelligence/ai/alpha/regime_coherence_prompts.py` returns 1
    - `grep -c "regime_fit" src/intelligence/ai/alpha/regime_coherence_prompts.py` returns >= 1
  </acceptance_criteria>
  <done>Prompts file imports cleanly with ACTIVE_VERSION matching agent_id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create regime_coherence_agent.py</name>
  <files>src/intelligence/ai/alpha/regime_coherence_agent.py</files>
  <read_first>
    - src/intelligence/ai/alpha/correlation_agent.py (Plan 04 sibling — mirror structure)
    - src/core/ai/multiplier_agent.py (Plan 01)
    - .planning/phases/080-renaissance-swarm-intelligence-layer/080-CONTEXT.md (D-05 contract)
  </read_first>
  <behavior>
    - agent_id == "regime_coherence_v1"
    - shadow_only is True
    - tiers_needed == frozenset({Tier.I4, Tier.I7, Tier.SMC})
    - latency_budget_ms == 5000.0
    - output_schema declares regime_fit:float, confidence:float, mismatches:list, reasoning:str
    - _validate_regime_coherence_fields rejects non-numeric, clamps both numerics to [0,1], coerces mismatches to list[str]
    - multiplier = regime_fit × llm_confidence
  </behavior>
  <action>
    Create `src/intelligence/ai/alpha/regime_coherence_agent.py` with the exact same structure as `correlation_agent.py` (Plan 04). Substitute:

    - Module docstring: `"""RegimeCoherenceAgentComputeAgent — setup-vs-regime fit multiplier (Phase 80, D-05)."""`
    - Imports adapted: `from src.intelligence.ai.alpha.regime_coherence_prompts import ACTIVE_VERSION, build_regime_coherence_prompt`
    - `_SYSTEM_MESSAGE = "You are a regime-coherence analyst. Output strictly valid JSON. Phase 80 policy: discount-only — regime_fit and confidence in [0.0, 1.0]."`
    - Validator function `_validate_regime_coherence_fields(data)`:
      - keys: `regime_fit` and `confidence` must be int/float; both `clamp(0.0, 1.0)`.
      - `mismatches`: coerce non-list to `[str(value)]`; coerce list elements via `str(...)`.
      - `reasoning`: `str(data.get("reasoning", ""))`.
      - Return None on any type failure.
    - Class declaration:
      ```python
      class RegimeCoherenceAgentComputeAgent(BaseMultiplierAgent):
          """Setup TYPE vs regime fit — is mean-reversion firing in a strong trend?"""

          output_schema: ClassVar[dict] = {
              "regime_fit": float,
              "confidence": float,
              "mismatches": list,
              "reasoning": str,
          }

          agent_id = "regime_coherence_v1"
          group = "alpha"
          tiers_needed = frozenset({Tier.I4, Tier.I7, Tier.SMC})
          latency_budget_ms = 5000.0
          shadow_only = True
      ```
    - Constructor identical to correlation_agent (`super().__init__(name=self.__class__.__name__, **kwargs); self._llm = llm_chain`).
    - `_compute(self, context)` body identical structure to correlation_agent except:
      - Calls `build_regime_coherence_prompt(context)`.
      - Calls `self._parse_multiplier_response(response, _validate_regime_coherence_fields)`.
      - On parse failure logs `"regime_coherence_agent.json_parse_failed"` (named field, not `event=`).
      - Multiplier: `regime_fit = parsed["regime_fit"]; llm_confidence = parsed["confidence"]; multiplier = regime_fit * llm_confidence`.
      - Payload to `_build_multiplier_output`: `{"regime_fit": regime_fit, "mismatches": parsed["mismatches"], "reasoning": parsed["reasoning"]}`.
      - `prompt_version=ACTIVE_VERSION`.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from src.intelligence.ai.alpha.regime_coherence_agent import RegimeCoherenceAgentComputeAgent; from src.core.ai.context import Tier; assert RegimeCoherenceAgentComputeAgent.agent_id == 'regime_coherence_v1'; assert RegimeCoherenceAgentComputeAgent.shadow_only is True; assert Tier.SMC in RegimeCoherenceAgentComputeAgent.tiers_needed; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class RegimeCoherenceAgentComputeAgent(BaseMultiplierAgent)' src/intelligence/ai/alpha/regime_coherence_agent.py` returns 1
    - `grep -c 'agent_id = "regime_coherence_v1"' src/intelligence/ai/alpha/regime_coherence_agent.py` returns 1
    - `grep -c 'tiers_needed = frozenset({Tier.I4, Tier.I7, Tier.SMC})' src/intelligence/ai/alpha/regime_coherence_agent.py` returns 1
    - `grep -c 'multiplier = regime_fit \* llm_confidence' src/intelligence/ai/alpha/regime_coherence_agent.py` returns 1
    - `.venv/bin/ruff check src/intelligence/ai/alpha/regime_coherence_agent.py` exits 0
  </acceptance_criteria>
  <done>Module imports cleanly, ruff passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for regime_coherence_agent</name>
  <files>tests/unit/service_tests/test_regime_coherence_agent.py</files>
  <read_first>
    - tests/unit/service_tests/test_correlation_agent.py (Plan 04 sibling)
    - src/intelligence/ai/alpha/regime_coherence_agent.py
  </read_first>
  <behavior>
    - test_validator_accepts_valid_payload
    - test_validator_rejects_non_numeric_regime_fit
    - test_validator_clamps_out_of_range
    - test_validator_coerces_non_list_mismatches
    - test_class_attributes (agent_id, shadow_only, tiers_needed)
    - test_multiplier_formula_semantics: regime_fit × confidence
  </behavior>
  <action>
    Mirror `test_correlation_agent.py` (Plan 04 Task 3) substituting:
    - Imports: `from src.intelligence.ai.alpha.regime_coherence_agent import RegimeCoherenceAgentComputeAgent, _validate_regime_coherence_fields`
    - Replace `coherence_score` with `regime_fit`, `contradicting_assets` with `mismatches`.
    - `test_class_attributes` asserts `agent_id == "regime_coherence_v1"`, `Tier.SMC in tiers_needed`, `Tier.I7 in tiers_needed`, `shadow_only is True`, `latency_budget_ms == 5000.0`.
    - `test_multiplier_formula_semantics`: `regime_fit, conf = 0.4, 0.5; assert regime_fit * conf == pytest.approx(0.2)`.
    - At least 6 `def test_` functions.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/service_tests/test_regime_coherence_agent.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/unit/service_tests/test_regime_coherence_agent.py` returns >= 6
    - `.venv/bin/pytest tests/unit/service_tests/test_regime_coherence_agent.py -v` exits 0
  </acceptance_criteria>
  <done>All regime coherence tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM provider -> agent | Untrusted text response |
| Agent -> dispatch | Multiplier flows into aggregation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-080-05-01 | Tampering | LLM JSON | mitigate | `_validate_regime_coherence_fields` strict types + `clamp(0,1)` |
| T-080-05-02 | DoS | LLM latency | mitigate | latency_budget_ms=5000 enforced via BaseAIAgent timeout |
| T-080-05-03 | Elevation of Privilege | Multiplier above 1.0 | mitigate | shadow_only=True; aggregator excludes shadow agents from production confidence |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/service_tests/test_regime_coherence_agent.py -v` exits 0
- `.venv/bin/ruff check src/intelligence/ai/alpha/regime_coherence_agent.py src/intelligence/ai/alpha/regime_coherence_prompts.py tests/unit/service_tests/test_regime_coherence_agent.py` exits 0
</verification>

<success_criteria>
- RegimeCoherenceAgentComputeAgent class with tiers {I4, I7, SMC}
- Tests green
- Phase 80 discount-only policy upheld
</success_criteria>

<output>
After completion, create `.planning/phases/080-renaissance-swarm-intelligence-layer/080-05-SUMMARY.md`
</output>
