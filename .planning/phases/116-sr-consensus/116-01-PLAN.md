---
phase: 116-sr-consensus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/features/i3_structure/support_resistance.py
  - tests/unit/intelligence/test_sr_shared_peaks.py
autonomous: true

must_haves:
  truths:
    - "Cluster radius is ATR-proportional (atr_14 * 0.5), not a fixed 0.5% of price"
    - "Lookback window is TF-proportional (1m=60, 5m=60, 15m=80, 1h=120, 4h=120, 1d=60), falls back to 120 when TF unknown"
    - "When no real pivot exists below price, nearest_support is omitted/None — never current_price*0.98"
    - "When no real pivot exists above price, nearest_resistance is omitted/None — never current_price*1.02"
    - "Pivot strength is volume-weighted: len(members) * mean(min(2.0, vol_ratio)) over cluster members"
    - "Existing output field names are unchanged (no new output keys)"
  artifacts:
    - path: "src/intelligence/features/i3_structure/support_resistance.py"
      provides: "ATR-proportional clustering, TF-proportional lookback, volume-weighted strength, no synthetic fallback"
      contains: "cluster_atr_mult"
    - path: "tests/unit/intelligence/test_sr_shared_peaks.py"
      provides: "Updated tests handling the no-pivot/None case + ATR-via-frames[i1] pattern"
      contains: "atr_14"
  key_links:
    - from: "support_resistance.py compute_full"
      to: "get_atr(frames['i1'])"
      via: "ATR read from i1 sub-dict for cluster radius"
      pattern: "get_atr\\(frames"
    - from: "support_resistance.py _cluster_levels"
      to: "atr_14 * cluster_atr_mult"
      via: "ATR-proportional cluster radius"
      pattern: "cluster_atr_mult"
---

<objective>
Fix `struct_SupportResistance` (I3) so it produces clean, instrument-aware, volatility-aware support/resistance inputs instead of fixed-percentage clusters and synthetic phantom levels.

Purpose: This plugin feeds stop/target anchors for five I7 plugins. The current fixed 0.5% cluster tolerance (37pts on ES@7400) merges distinct pivots; the TF-blind 120-bar lookback applies the same window to 5m and 1h; and the synthetic `price*0.98` fallback injects a phantom level 148pts below price when no real pivot exists. Fixing the bad input at source is Step 1 of the three-step SR consensus chain.

Output: Refactored `support_resistance.py` with ATR-proportional clustering, TF-proportional lookback, killed synthetic fallback, and volume-weighted strength. Updated unit tests covering the no-pivot case.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@docs/plans/2026-06-05-sr-consensus.md
@.planning/phases/116-sr-consensus/116-RESEARCH.md
@.planning/phases/116-sr-consensus/116-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ATR-proportional clustering + TF-proportional lookback + volume-weighted strength + kill synthetic fallback</name>
  <files>src/intelligence/features/i3_structure/support_resistance.py</files>
  <read_first>
    - src/intelligence/features/i3_structure/support_resistance.py (the file being modified — current state has cluster_pct=0.005 at line 33, _cluster_levels at line 97, _finalize_cluster at line 122, synthetic fallback at lines 66 and 71)
    - src/intelligence/trading/atr_utils.py (get_atr signature — null-safe, reads features['atr_14'])
    - .planning/phases/116-sr-consensus/116-PATTERNS.md (lines 23-97 — exact replacement patterns for this file)
    - docs/plans/2026-06-05-sr-consensus.md (lines 36-91 — Step 1 design intent)
  </read_first>
  <action>
    Modify `src/intelligence/features/i3_structure/support_resistance.py`:

    1. Add import at top: `from src.intelligence.trading.atr_utils import get_atr`.

    2. Add module-level constant `_LOOKBACK_BY_TF: dict[str, int] = {"1m": 60, "5m": 60, "15m": 80, "1h": 120, "4h": 120, "1d": 60}`.

    3. Dataclass fields: remove `cluster_pct: float = 0.005`; add `cluster_atr_mult: float = 0.5`.

    4. In `compute_full`: after `df = frames.get("main")` and the length guard, read `tf = frames.get("timeframe", "")`, `lookback = _LOOKBACK_BY_TF.get(tf, 120)`, then slice `df = df.iloc[-lookback:]` before extracting high/low/close arrays. Read `atr_14 = get_atr(frames.get("i1") or {})`. Extract the volume array with a guard: `volume = df["volume"].to_numpy(dtype=float) if "volume" in df.columns else None`; compute `mean_volume = float(volume.mean()) if volume is not None and volume.size else 0.0`.

    5. Thread `atr_14` and `current_price` into `_cluster_levels`. In `_cluster_levels`, compute `cluster_radius = (atr_14 * self.cluster_atr_mult) if atr_14 else (current_price * 0.005)` and replace the line `if abs(price - current_cluster[-1][0]) <= current_price * self.cluster_pct:` with `if abs(price - current_cluster[-1][0]) <= cluster_radius:`.

    6. Volume-weighted strength: change `_finalize_cluster` to accept `volume` array and `mean_volume`. Compute `strength = len(members) * (sum(min(2.0, (float(volume[idx]) / mean_volume if mean_volume > 0 else 1.0)) for _, idx in members) / len(members))`. When `volume is None` or `mean_volume <= 0`, the per-member ratio is `1.0` so strength equals `len(members)` (preserves current behavior). Thread `volume` and `mean_volume` through `_cluster_levels` to `_finalize_cluster`.

    7. Kill synthetic fallback: replace line 66 `nearest_r = {"level": current_price * 1.02, "strength": 0, "latest_idx": 0}` with `nearest_r = None`; replace line 71 `nearest_s = {"level": current_price * 0.98, "strength": 0, "latest_idx": 0}` with `nearest_s = None`.

    8. Rewrite the return block to be conditional (build `result: dict[str, Any] = {}`): only populate `nearest_resistance`, `resistance_strength`, `resistance_dist_pct`, `resistance_age_bars` when `nearest_r is not None`; only populate `nearest_support`, `support_strength`, `support_dist_pct`, `support_age_bars` when `nearest_s is not None`. Always populate `sr_level_count = float(len(resistance_clusters) + len(support_clusters))`. Keep all existing field NAMES and their computed formulas (dist_pct, age_bars) exactly as before. Do NOT add any new output keys; the `outputs` frozenset is unchanged.

    Use `except ... as error:` if any try/except is added (project convention). No comments unless the WHY is non-obvious.
  </action>
  <verify>.venv/bin/ruff check src/intelligence/features/i3_structure/support_resistance.py && .venv/bin/python -c "from src.intelligence.features.i3_structure.support_resistance import SupportResistancePlugin, _LOOKBACK_BY_TF; p=SupportResistancePlugin(); assert p.cluster_atr_mult==0.5; assert not hasattr(p,'cluster_pct') or 'cluster_pct' not in SupportResistancePlugin.__dataclass_fields__; print('ok')"</verify>
  <acceptance_criteria>
    - `support_resistance.py` contains `from src.intelligence.trading.atr_utils import get_atr`
    - `support_resistance.py` contains `cluster_atr_mult` and does NOT contain `cluster_pct` in dataclass fields
    - `support_resistance.py` contains `_LOOKBACK_BY_TF` with keys 1m,5m,15m,1h,4h,1d mapping to 60,60,80,120,120,60
    - `support_resistance.py` contains `df.iloc[-lookback:]` (TF-proportional window applied)
    - `support_resistance.py` contains `get_atr(frames.get("i1")` (ATR read from i1 sub-dict)
    - `support_resistance.py` does NOT contain `current_price * 0.98` or `current_price * 1.02` (synthetic fallback removed)
    - `support_resistance.py` `_finalize_cluster` computes strength using `min(2.0,` volume ratio cap
    - The `outputs` frozenset is unchanged (same 9 keys)
    - `ruff check` passes on the file
  </acceptance_criteria>
  <done>Plugin reads ATR from frames['i1'] for cluster radius, slices df to TF-proportional lookback, weights strength by capped volume ratio, returns None/omitted nearest_support/nearest_resistance when no real pivot exists, and keeps all original output field names.</done>
</task>

<task type="auto">
  <name>Task 2: Update test_sr_shared_peaks.py for no-pivot case and ATR-via-frames[i1]</name>
  <files>tests/unit/intelligence/test_sr_shared_peaks.py</files>
  <read_first>
    - tests/unit/intelligence/test_sr_shared_peaks.py (the file being modified — current tests assert key presence and nearest_support<=price / nearest_resistance>=price which break under no-pivot/None)
    - src/intelligence/features/i3_structure/support_resistance.py (the just-modified plugin — verify the new frames['i1'] and timeframe contract)
    - .planning/phases/116-sr-consensus/116-PATTERNS.md (lines 406-440 — test update patterns)
  </read_first>
  <action>
    Modify `tests/unit/intelligence/test_sr_shared_peaks.py`:

    1. Add a helper `_make_frames(n=120, seed=42, tf="5m")` that returns `{"main": _make_ohlcv(n=n, seed=seed), "i1": {"atr_14": <rough atr>}, "timeframe": tf}`. Compute a rough ATR for the test as `float(np.std(np.diff(close)) * 1.4)` from the close array, or pass an explicit `atr_14` such as 5.0.

    2. Update `test_outputs_all_expected_keys`: call `plugin.compute_full(_make_frames())`. Assert `sr_level_count` is always present. For `nearest_resistance`/`nearest_support`/`resistance_strength`/`support_strength`, assert that IF the key is present its value is a float (the no-pivot case omits the key — do not assert unconditional presence).

    3. Update `test_resistance_above_price`: call with frames; assert `if "nearest_resistance" in result: assert result["nearest_resistance"] >= current_price`. No phantom-level assertion.

    4. Update `test_support_below_price`: call with frames; assert `if "nearest_support" in result: assert result["nearest_support"] <= current_price`.

    5. Add `test_no_synthetic_fallback`: construct frames whose data has no pivot below (or above) price, run compute_full, and assert that when `nearest_support` is absent the result does NOT contain a value equal to `current_price * 0.98` (i.e. the synthetic phantom is gone). At minimum assert no output value equals `round(current_price*0.98, 6)` and no output value equals `round(current_price*1.02, 6)`.

    6. Keep `test_short_data_returns_empty` (passing `{"main": _make_ohlcv(n=20)}` still returns `{}` because n<min_lookback=50).

    All test functions should pass frames containing `"i1"` and `"timeframe"` so the new ATR/lookback path is exercised.
  </action>
  <verify>.venv/bin/pytest tests/unit/intelligence/test_sr_shared_peaks.py -q</verify>
  <acceptance_criteria>
    - `tests/unit/intelligence/test_sr_shared_peaks.py` contains a `_make_frames` helper passing `"i1"` and `"timeframe"` keys
    - Test file contains a test asserting no output value equals the synthetic `current_price * 0.98` / `* 1.02` phantom
    - `.venv/bin/pytest tests/unit/intelligence/test_sr_shared_peaks.py -q` exits 0 (all tests pass)
    - No test asserts unconditional presence of `nearest_support`/`nearest_resistance` (presence is now conditional on a real pivot)
  </acceptance_criteria>
  <done>Test suite for the SR plugin passes against the refactored plugin, covers the no-pivot/None case, and proves the synthetic fallback is eliminated.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/intelligence/test_sr_shared_peaks.py -q` passes
- `.venv/bin/ruff check src/intelligence/features/i3_structure/support_resistance.py` passes
- `grep -n "current_price \* 0.98\|current_price \* 1.02\|cluster_pct" src/intelligence/features/i3_structure/support_resistance.py` returns nothing
- `grep -n "cluster_atr_mult\|_LOOKBACK_BY_TF\|get_atr" src/intelligence/features/i3_structure/support_resistance.py` returns matches
</verification>

<success_criteria>
- Cluster radius is ATR-proportional (atr_14 * 0.5) with fallback to 0.5% only when ATR is unavailable
- Lookback window respects the TF table, falling back to 120 for unknown TF
- nearest_support / nearest_resistance are omitted (not synthesized) when no real pivot exists
- Pivot strength is volume-weighted with a 2x cap
- All original output field names preserved; outputs frozenset unchanged
- SR plugin unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/116-sr-consensus/116-01-SUMMARY.md`
</output>
