---
phase: 142A
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/187_alpha_ensemble_ic.sql
  - services/ensemble_ic_engine.py
  - services/service_auditor.py
  - tests/unit/test_ensemble_ic_math.py
  - tests/unit/test_ensemble_ic_config.py
  - tests/unit/test_ensemble_ic_bh_fdr.py
  - tests/unit/test_ensemble_ic_executable_returns.py
  - tests/unit/test_ensemble_ic_wf_stability.py
autonomous: true
requirements: [EIC-01, EIC-03]

must_haves:
  truths:
    - "alpha_ensemble_ic hypertable exists with a walk_forward_stable column (EIC-03 surface) and the 9 cross-sectional regime namespace"
    - "EnsembleICEngine extends BaseBatch, composes ic_engine math (Fisher-z CI, NOT bootstrap), and writes IC rows per (symbol, tf, regime, lookahead)"
    - "ProcessPoolExecutor workers are compute-only (return list[dict]); serial DB writes happen from the main process after corpus-level BH-FDR"
    - "All forward_returns queries filter return_type = 'executable_open_to_open' (Invariant 1)"
    - "EnsembleICEngine crashes loud at startup when alpha_events or forward_returns is empty (correct behavior while Phase B runs)"
    - "All alpha.ensemble_ic.*, alpha.frame.hold_max_bars.* (36 keys), and infra.ensemble_ic_engine.workers APR seeds exist in config_state"
  artifacts:
    - path: "production/migrations/187_alpha_ensemble_ic.sql"
      provides: "alpha_ensemble_ic hypertable DDL + composite PK + cell index + APR seed triple-INSERTs"
      contains: "alpha_ensemble_ic"
    - path: "services/ensemble_ic_engine.py"
      provides: "EnsembleICEngine(BaseBatch) weekly oneshot service"
      exports: ["EnsembleICEngine", "EnsembleICConfig"]
    - path: "services/service_auditor.py"
      provides: "Phase 142A oneshot registration in _DAG_ORDER and _ONESHOT_UNITS"
      contains: "indicagent-ensemble-ic-engine"
    - path: "tests/unit/test_ensemble_ic_math.py"
      provides: "IC parity tests vs ic_engine private functions on synthetic alpha_score"
    - path: "tests/unit/test_ensemble_ic_config.py"
      provides: "EnsembleICConfig.from_apr binding tests + pickle safety"
    - path: "tests/unit/test_ensemble_ic_bh_fdr.py"
      provides: "corpus-level BH-FDR test (single multipletests call, no clustering)"
    - path: "tests/unit/test_ensemble_ic_executable_returns.py"
      provides: "Invariant 1 enforcement test (SQL contains executable_open_to_open)"
    - path: "tests/unit/test_ensemble_ic_wf_stability.py"
      provides: "EIC-03 walk_forward_stable boolean test (max/min fold ratio < wf_stability_ratio)"
  key_links:
    - from: "services/ensemble_ic_engine.py"
      to: "services/ic_engine.py"
      via: "import _fisher_z_ci, _p_values_from_ic, _compute_ic_rolling_metrics, _vectorized_ic, _nan_to_none, ICEngineConfig"
      pattern: "from services\\.ic_engine import"
    - from: "services/ensemble_ic_engine.py"
      to: "src/core/agent/base_batch.py"
      via: "class EnsembleICEngine(BaseBatch) with execute(pool)"
      pattern: "BaseBatch"
    - from: "services/ensemble_ic_engine.py"
      to: "alpha_ensemble_ic"
      via: "asyncpg executemany serial write from main process"
      pattern: "INSERT INTO alpha_ensemble_ic"
    - from: "services/ensemble_ic_engine.py"
      to: "market_regimes"
      via: "JOIN on (tf, bar_ts) → regime_label; 9 cross-sectional labels"
      pattern: "market_regimes"
    - from: "services/service_auditor.py"
      to: "indicagent-ensemble-ic-engine"
      via: "_DAG_ORDER + _ONESHOT_UNITS registration"
      pattern: "ensemble-ic-engine"
---

<objective>
Wave 1 builds the schema and the EnsembleICEngine service that measures IC(alpha_score, forward_return_*) per (symbol, tf, regime, lookahead) using the corrected Phase A methodology. Proves the ensemble OUTPUT has IC before any execution rules are tested.

Purpose: This is the signal proof that must pass before Phase 142B can build any frame. If alpha_score does not predict forward returns, no frame definition will save it. The IC math is COMPOSED from services/ic_engine.py (Fisher-z CI, NOT circular-block-bootstrap — the live code is authoritative over the CONTEXT.md wording), so methodology parity is structural, not re-derived.

Output: migration 187 (alpha_ensemble_ic table + 36 hold_max_bars APR keys + alpha.ensemble_ic.* seeds), EnsembleICEngine service, service_auditor registration, and 5 unit-test files covering the IC math parity, config binding, corpus BH-FDR, executable-returns filter, and walk-forward stability.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/bg/dev/indicagent/.planning/PROJECT.md
@/home/bg/dev/indicagent/.planning/ROADMAP.md
@/home/bg/dev/indicagent/.planning/STATE.md
@/home/bg/dev/indicagent/.planning/phases/142A-ensemble-ic-measurement-planned/142A-CONTEXT.md
@/home/bg/dev/indicagent/.planning/phases/142A-ensemble-ic-measurement-planned/142A-RESEARCH.md
@/home/bg/dev/indicagent/.planning/phases/142A-ensemble-ic-measurement-planned/142A-PATTERNS.md
@/home/bg/dev/indicagent/.planning/phases/142A-ensemble-ic-measurement-planned/142A-VALIDATION.md
@/home/bg/dev/indicagent/CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the live codebase; do NOT re-explore. -->

From src/core/agent/base_batch.py (Ring 0 base class — DO NOT modify):
```python
class BaseBatch(abc.ABC):
    job_name: str            # D-06 label; must match systemd unit %n suffix (kebab-case)
    compute_version: str     # audit tag written to DB rows
    def __init__(self, db_dsn: str) -> None: ...
    @abc.abstractmethod
    async def execute(self, pool: asyncpg.Pool) -> None: ...   # subclasses own business logic
    async def run(self) -> None: ...                          # template: pool -> execute -> D-06 -> teardown
    @staticmethod
    def content_key(*parts: str) -> str: ...                  # SHA-256 first 32 hex chars of '|'.join(parts)
```
BaseBatch.run() emits JOB_COMPLETED_TOTAL.add(1, {"job": job_name, "status": status}) and calls flush_and_shutdown_metrics() in finally. Do NOT reimplement.

From services/ic_engine.py (Ring 2 oneshot — import math, do NOT subclass, do NOT fork):
```python
# Private pure functions — module-level, stateless, Phase-A-corrected:
def _vectorized_ic(ranks_X: np.ndarray, ranks_Y: np.ndarray) -> np.ndarray: ...   # IC vector [n_features]
def _p_values_from_ic(ic_vector: np.ndarray, n: int) -> np.ndarray: ...           # t-approx, df=n-2
def _fisher_z_ci(ic_vector: np.ndarray, n: int) -> tuple[np.ndarray, np.ndarray]: # (ci_lower, ci_upper) — NOT bootstrap
def _compute_ic_rolling_metrics(X_sub, returns_sub, scale_idx, complete_mask, config,
        non_degenerate_mask, n_total_features, stride) -> tuple[sharpe, sharpe_hac, sortino, win_rate, n_windows]: ...
def _nan_to_none(value) -> float | None: ...
@dataclasses.dataclass(frozen=True)
class ICEngineConfig:
    fdr_alpha: float; walk_forward_folds: int; sharpe_window_size: int; sharpe_min_windows: int
    subsample_min_stride: int; min_reliable_n: int; hac_max_lag: int
    lookahead_fast: int; lookahead_mid: int; lookahead_slow: int; lookahead_extended: int
    n_workers: int
    # ... see services/ic_engine.py:240-308 for the full frozen dataclass
    @classmethod
    def from_apr(cls, cfg: dict[str, Any]) -> "ICEngineConfig": ...
```
alpha_score is ONE predictor → X is shape [n_obs, 1]. Collinearity clustering is SKIPPED (it is a feature-level concern). Every (symbol, tf, regime, lookahead) cell IS a representative for BH-FDR.

From src/config/config_service.py (APR write API):
```python
class ConfigService:
    OPS_PREFIXES = (..., "alpha.", ...)   # alpha. already registered (line 51)
    async def set(self, key: str, value: str,
                  changed_by: str = "system", expected_version=None,
                  reason: str = None) -> None: ...   # signature at line 164
```
NOTE: method is `set` (not `set_async`). `alpha.` is in OPS_PREFIXES so alpha.frame.* writes need NO code change.

Live DB facts (verified 2026-06-30):
- alpha_events: alpha_score = double precision NOT NULL; regime = text NULLABLE (9-label populated); (symbol, tf, bar_ts) JOIN keys.
- market_regimes.regime_label has EXACTLY 9 distinct values: high_bear, high_bull, high_neutral, low_bear, low_bull, low_neutral, mid_bear, mid_bull, mid_neutral.
- alpha_ensemble_ic does NOT exist (greenfield — migration 187 is additive).
- alpha_events currently 0 rows (Phase B corpus re-run IN FLIGHT) — startup gate must crash loud.

service_auditor registries (services/service_auditor.py):
- _DAG_ORDER: dict[str, int]; IC pipeline oneshots are priority 8 (ic-engine, ensemble-trainer, alpha-publisher at lines 109-112).
- _ONESHOT_UNITS: frozenset[str]; IC pipeline oneshots added at lines 197-203 (each with "inactive between IC pipeline runs is correct" comment).
- _LAG_THRESHOLDS: N/A for oneshots (excluded from Prometheus lag checks by the comment at line 200).
- _AGENT_ID_TO_UNIT: add only if EnsembleICEngine emits an agent_id label (it does not — BaseBatch emits job_completed_total with job=, not agent_id=).

Migration pattern (production/migrations/186_alpha_ensemble_gate_apr_seeds.sql):
- APR seed = triple-INSERT into config_schema + config_state + config_history, each with `ON CONFLICT (config_key) DO NOTHING`.
- config_schema columns: config_key, value_type, default_value, description (optionally min_value, max_value).
- config_state columns: config_key, config_value, version.
- config_history columns: timestamp, config_key, version, config_value, changed_by, reason.
- Provenance tags in description: [initial_estimate] / [conventional].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 187 — alpha_ensemble_ic hypertable + APR seeds (EIC-01 schema, EIC-03 column, EIC-02 hold_max_bars keys)</name>
  <files>production/migrations/187_alpha_ensemble_ic.sql</files>

  <behavior>
    - After applying migration 187: alpha_ensemble_ic table exists with columns event_row_id, symbol, tf, regime, lookahead, lookahead_bars, is_pooled, n_independent, reliable, ic_value, ic_ci_lower, ic_ci_upper, ic_sharpe, ic_sharpe_hac, bh_adjusted_p, passes_fdr, walk_forward_stable (EIC-03), scored_at; PRIMARY KEY (event_row_id, scored_at).
    - alpha_ensemble_ic is a TimescaleDB hypertable partitioned by scored_at with chunk_time_interval 3 months.
    - Index alpha_ensemble_ic_cell_idx on (symbol, tf, regime, lookahead, scored_at DESC) exists.
    - config_state contains exactly: alpha.ensemble_ic.decay_threshold=0.1, alpha.ensemble_ic.min_qualifying_fraction=0.60, alpha.ensemble_ic.wf_stability_ratio=3.0, alpha.ensemble_ic.gate_lookahead='fast', alpha.ensemble_ic.wf_stability_metric='ic_ratio', alpha.ensemble_ic.min_obs_per_regime, infra.ensemble_ic_engine.workers=12.
    - config_state contains exactly 36 alpha.frame.hold_max_bars.<regime>.<tf> keys for the 9 market_regimes labels x {5m,15m,1h,1d}, with [initial_estimate] descriptions.
    - All descriptions carry provenance tags ([initial_estimate] or [conventional]).
  </behavior>

  <read_first>
    - production/migrations/186_alpha_ensemble_gate_apr_seeds.sql (APR triple-INSERT pattern; lines 13-38)
    - production/migrations/168_ensemble_tables.sql (hypertable + composite PK pattern; alpha_events at lines 95-123, ensemble_alpha at 63-87)
    - .planning/phases/142A-ensemble-ic-measurement-planned/142A-PATTERNS.md (Pattern section for migration 187: lines 245-308)
    - src/core/agent/base_batch.py:103-117 (content_key — the SHA-256 first-32-hex key format for event_row_id)
    - CLAUDE.md "TimescaleDB Tables" + "ON CONFLICT for partial indexes" rule
  </read_first>

  <action>
    Create production/migrations/187_alpha_ensemble_ic.sql as a single idempotent migration with three sections.

    Section 1 — hypertable. CREATE TABLE IF NOT EXISTS alpha_ensemble_ic following the column set in PATTERNS.md (lines 252-279): event_row_id text NOT NULL, symbol text NOT NULL (use 'POOLED' for cross-sectional rows per research OQ-2 recommendation), tf text NOT NULL, regime text NOT NULL, lookahead text NOT NULL (values 'fast'|'mid'|'slow'|'extended'), lookahead_bars integer NOT NULL, is_pooled boolean NOT NULL DEFAULT false, n_independent integer, reliable boolean, ic_value double precision, ic_ci_lower double precision, ic_ci_upper double precision, ic_sharpe double precision, ic_sharpe_hac double precision, bh_adjusted_p double precision, passes_fdr boolean, walk_forward_stable boolean (EIC-03 — required by Phase 144 and the EIC-04 gate), scored_at timestamptz NOT NULL DEFAULT now(), PRIMARY KEY (event_row_id, scored_at). Then SELECT create_hypertable('alpha_ensemble_ic','scored_at',chunk_time_interval => INTERVAL '3 months', if_not_exists => TRUE). Then CREATE INDEX IF NOT EXISTS alpha_ensemble_ic_cell_idx ON alpha_ensemble_ic (symbol, tf, regime, lookahead, scored_at DESC). The composite PK MUST include the partition column scored_at (TimescaleDB requirement — mirrors alpha_events PK at migration 168 line 112).

    Section 2 — alpha.ensemble_ic.* APR seeds. Use the triple-INSERT pattern from migration 186 (config_schema + config_state + config_history, each ON CONFLICT (config_key) DO NOTHING). Seed exactly: alpha.ensemble_ic.decay_threshold=0.1 float [initial_estimate] (EIC-02); alpha.ensemble_ic.min_qualifying_fraction=0.60 float [initial_estimate] (EIC-04 gate — DO NOT bake 0.60 into any service code, only read from APR); alpha.ensemble_ic.wf_stability_ratio=3.0 float [initial_estimate] (EIC-03); alpha.ensemble_ic.gate_lookahead='fast' str [initial_estimate] (EIC-04); alpha.ensemble_ic.wf_stability_metric='ic_ratio' str [initial_estimate] (swappable per research A5); alpha.ensemble_ic.min_obs_per_regime int [initial_estimate] (EIC-05 diagnosis threshold — use the same default alpha.ic.min_obs_per_regime already in APR; verify with SELECT config_value FROM config_state WHERE config_key='alpha.ic.min_obs_per_regime' before hardcoding the literal, and if absent seed 200 to match Phase A min_reliable_n). infra.ensemble_ic_engine.workers=12 int [conventional] (matches ic_engine precedent).

    Section 3 — 36 alpha.frame.hold_max_bars.<regime>.<tf> APR seeds. Generate the 36 keys by cross-joining the 9 market_regimes.regime_label values (high_bear, high_bull, high_neutral, low_bear, low_bull, low_neutral, mid_bear, mid_bull, mid_neutral — these are the LIVE labels, NOT the stale bull/bear/sideways/volatile from the schema doc) with the 4 TFs (5m, 15m, 1h, 1d). Seed each with a default value (use 60 bars as a conservative [initial_estimate] — EIC-02 will overwrite with data-derived values after the first run). Use the triple-INSERT pattern for each key; descriptions MUST say "[initial_estimate] max hold bars; overwritten by EIC-02 IC decay calibration. NOT an ML learning target." This is the OQ-1 resolution: 9-regime namespace, not 4-regime.

    Migration numbering: this is migration 187 (186 is the latest applied in the repo per `ls production/migrations/`). Do NOT add a schema_migrations row unless the project's migration runner requires it — Phase 138/139 migrations applied cleanly without one (check `grep -l "schema_migrations" production/migrations/*.sql` for the established pattern; if prior migrations do not insert into a tracking table, follow that convention).

    Do NOT write any Python code in this task. Do NOT touch services/ensemble_ic_engine.py (Task 2) or services/service_auditor.py (Task 3).
  </action>

  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/187_alpha_ensemble_ic.sql && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT to_regclass('alpha_ensemble_ic');" | grep -q "alpha_ensemble_ic" && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM config_state WHERE config_key LIKE 'alpha.frame.hold_max_bars.%';" | grep -q "^36$" && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM config_state WHERE config_key LIKE 'alpha.ensemble_ic.%';" | grep -q "^6$" && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM information_schema.columns WHERE table_name='alpha_ensemble_ic' AND column_name='walk_forward_stable';" | grep -q "^1$"</automated>
  </verify>

  <acceptance_criteria>
    - alpha_ensemble_ic table exists as a TimescaleDB hypertable (SELECT to_regclass returns 'alpha_ensemble_ic'; SELECT * FROM timescaledb_information.hypertables WHERE hypertable_name='alpha_ensemble_ic' returns 1 row).
    - PRIMARY KEY is (event_row_id, scored_at) — partition column included (CLAUDE.md TimescaleDB rule; migration 168 precedent).
    - walk_forward_stable column exists (EIC-03 surface — Phase 144 and the EIC-04 gate depend on it).
    - 36 alpha.frame.hold_max_bars.<regime>.<tf> keys exist for the 9 LIVE market_regimes labels x 4 TFs (NOT bull/bear/sideways/volatile).
    - 6 alpha.ensemble_ic.* keys exist (decay_threshold, min_qualifying_fraction, wf_stability_ratio, gate_lookahead, wf_stability_metric, min_obs_per_regime).
    - 1 infra.ensemble_ic_engine.workers key exists with value 12.
    - Every description contains a provenance tag ([initial_estimate] or [conventional]).
    - .venv/bin/pytest tests/unit/ -q still green (migration is SQL-only, no Python touched).
  </acceptance_criteria>

  <done>Migration 187 applies cleanly; alpha_ensemble_ic hypertable + cell index + walk_forward_stable column exist; 36 hold_max_bars + 7 other APR seeds present in config_state with the 9-regime namespace; full unit suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EnsembleICEngine service — BaseBatch shell + IC math composition + Wave 0 unit tests (EIC-01, EIC-03)</name>
  <files>services/ensemble_ic_engine.py, tests/unit/test_ensemble_ic_math.py, tests/unit/test_ensemble_ic_config.py, tests/unit/test_ensemble_ic_bh_fdr.py, tests/unit/test_ensemble_ic_executable_returns.py, tests/unit/test_ensemble_ic_wf_stability.py</files>

  <behavior>
    - test_ensemble_ic_math.py: IC(alpha_score[1col], returns) via the composed ic_engine call path equals scipy.stats.spearmanr to within 1e-9 on synthetic data with n=500; Fisher-z CI lower bound is positive when alpha_score truly predicts returns and crosses zero under a null (no-signal) fixture; _p_values_from_ic is in [0,1].
    - test_ensemble_ic_config.py: EnsembleICConfig.from_apr(cfg_dict) binds every alpha.ensemble_ic.* and shared ICEngineConfig key; the dataclass is frozen (dataclasses.is_frozen) and pickle-safe (pickle.dumps then loads round-trips equal).
    - test_ensemble_ic_bh_fdr.py: a single corpus-level multipletests call is applied across ALL cell p-values (no per-symbol loop); with 200 synthetic cells where 10 have p<0.001 and the rest p=0.5, exactly the 10 low-p cells get passes_fdr=true (within BH tolerance).
    - test_ensemble_ic_executable_returns.py: the forward_returns SQL string in services/ensemble_ic_engine.py contains the literal substring "return_type = 'executable_open_to_open'" (Invariant 1 grep gate); no 'theoretical' substring appears.
    - test_ensemble_ic_wf_stability.py: given fold ICs [0.3, 0.4, 0.5], walk_forward_stable computes true when wf_stability_ratio=3.0 (max/min=0.5/0.3=1.67 < 3); given fold ICs [0.1, 0.9] with ratio=9 it computes false; folds with <2 entries yield false.
    - EnsembleICEngine raises RuntimeError at startup when alpha_events is empty (crash-loud gate).
  </behavior>

  <read_first>
    - services/ic_engine.py:376-397 (_fisher_z_ci), 405-422 (_vectorized_ic), 456-462 (_p_values_from_ic), 495-629 (_compute_ic_rolling_metrics + _hac_sharpe_nd), 725-731 (executable_open_to_open filter), 768-799 (mr_dict market_regimes override), 850-954 (the per-cell IC compute body to adapt), 913-940 (expanding-window walk-forward + scale-specific embargo — P3 fix), 1719-1755 (serial write), 1763-1872 (_run_ic_worker compute-only pattern), 2088-2151 (main ProcessPoolExecutor loop), 2213-2240 (corpus-level BH-FDR P2 fix)
    - services/ensemble_trainer.py:32-68 (imports), 76-140 (APR binding + frozen dataclass), 195-216 (startup gate), 224-249 (BaseBatch shell + execute), 464-480 (market_regimes JOIN), 567-577 (asyncpg executemany write), 641-649 (entrypoint)
    - services/alpha_publisher.py:26-53 (BaseBatch + asyncpg imports + content_key usage)
    - src/core/agent/base_batch.py:30-117 (full BaseBatch contract — do NOT reimplement run())
    - src/config/config_service.py:39-52 (OPS_PREFIXES — alpha. already present), 164-171 (async def set signature)
    - tests/unit/test_fisher_z_ci.py (pure-numpy parity test pattern), tests/unit/test_ensemble_meta_fdr.py (FDR test pattern), tests/unit/test_ensemble_trainer.py (config-binding test pattern)
    - .planning/phases/142A-ensemble-ic-measurement-planned/142A-PATTERNS.md (Pattern 1 + 2 + 3 + 4 + service_auditor edits; lines 24-360)
    - .planning/phases/142A-ensemble-ic-measurement-planned/142A-RESEARCH.md (Code Examples: IC cell at lines 388-444, BH-FDR at 446-458, EIC-03 stability gate at 436-443)
    - CLAUDE.md: Invariant 1 (executable_open_to_open), ProcessPoolExecutor compute-only rule, "Exception variable name is error", "All timestamps UTC", gradient naming (fast/mid/slow/extended)
  </read_first>

  <action>
    RED FIRST. Create all 5 test files in tests/unit/ with the behaviors above. Tests are pure-numpy / pure-Python where possible (no DB, no Kafka). For test_ensemble_ic_executable_returns.py, read the source file as a string and assert the substring (a grep-as-test — guards Invariant 1 at CI time). Run `.venv/bin/pytest tests/unit/test_ensemble_ic_*.py -x` and confirm all fail (services/ensemble_ic_engine.py does not exist yet).

    GREEN. Create services/ensemble_ic_engine.py as a single module. Structure (mirror services/ensemble_trainer.py + services/alpha_publisher.py):

    1. Imports per PATTERNS.md lines 29-61: stdlib (asyncio, dataclasses, sys, datetime UTC, pathlib), asyncpg, numpy, structlog, scipy.stats.rankdata, statsmodels.stats.multitest.multipletests. project_root sys.path insert. Then `from src.config.settings import Settings`, `from src.core.agent.base_batch import BaseBatch`, `from src.observability.corpus_manifest import CorpusManifest`, `from src.observability.otel import OTelInitError, init_otel_providers`, and the COMPOSITION imports: `from services.ic_engine import (ICEngineConfig, _compute_ic_rolling_metrics, _fisher_z_ci, _nan_to_none, _p_values_from_ic, _vectorized_ic)`. Also `from src.config.config_service import ConfigService` for EIC-02 writes (Task in Plan 02, but import path is stable now).

    2. Module-level constants: _APR_QUERY = "SELECT config_key, config_value FROM config_state WHERE config_key LIKE 'alpha.%' OR config_key LIKE 'infra.ensemble_ic_engine.%'" (asyncpg fetch). _ENSEMBLE_IC_INSERT_SQL: a single INSERT into alpha_ensemble_ic with the 17 columns named explicitly, using $1..$17 parameter placeholders and ON CONFLICT (event_row_id, scored_at) DO UPDATE SET ic_value=EXCLUDED.ic_value, ic_ci_lower=EXCLUDED.ic_ci_lower, passes_fdr=EXCLUDED.passes_fdr, walk_forward_stable=EXCLUDED.walk_forward_stable (idempotent re-runs — mirrors ic_engine idempotency).

    3. EnsembleICConfig frozen dataclass (PATTERNS.md lines 105-122): reuse ICEngineConfig for the shared keys (fdr_alpha, walk_forward_folds, sharpe_window_size, sharpe_min_windows, subsample_min_stride, min_reliable_n, hac_max_lag, lookahead_fast/mid/slow/extended, n_workers) and add the EnsembleIC-specific keys (decay_threshold, min_qualifying_fraction, wf_stability_ratio, gate_lookahead, wf_stability_metric, min_obs_per_regime). Provide a from_apr(cls, cfg: dict[str, Any]) -> EnsembleICConfig classmethod that reads every key with a fallback default. MUST be frozen + picklable (workers receive it via ProcessPoolExecutor).

    4. A module-level _run_ensemble_ic_worker(args) function (mirror services/ic_engine.py:1763-1872 _run_ic_worker). Workers receive (symbol, tf, dsn, config, run_ts_ns, mr_dict_frozen, existing_keys_frozen) and return a dict {"rows": list[dict], "pvals": list[float], "pval_idxs": list[int]}. Workers open their OWN asyncpg connection (read-only — SELECT only, never INSERT/UPDATE) OR receive pre-fetched numpy arrays; either way workers NEVER open a write connection and NEVER call conn.commit() for writes (CLAUDE.md invariant — concurrent hypertable writers deadlock). Inside the worker, for each regime in the distinct regimes for this (symbol, tf) PLUS a cross-sectional pooled pass (symbol='POOLED' per research OQ-2 recommendation — emit both per-symbol and cross-sectional rows), and for each scale in (fast, mid, slow, extended): subsample with stride=max(subsample_min_stride, lookahead_bars); filter complete rows; if n_valid < min_reliable_n skip; compute ranks_X=rankdata(alpha.reshape(-1,1),axis=0), ranks_Y=rankdata(returns); ic_vector=_vectorized_ic(ranks_X,ranks_Y); ic_value, p_value, ci_lower via the imported helpers; expanding-window walk-forward with embargo_bars=lookahead_bars (P3 fix — NOT max(lookaheads)); collect fold_ics; compute walk_forward_stable per the EIC-03 logic (max/min fold |IC| ratio < wf_stability_ratio when wf_stability_metric='ic_ratio', >=2 folds, else false); IC Sharpe + HAC Sharpe via _compute_ic_rolling_metrics with n_total_features=1, non_degenerate_mask=np.array([True]). All forward_returns queries inside the worker MUST filter WHERE return_type = 'executable_open_to_open' (Invariant 1) AND WHERE bar_ts < alpha.validation.oos_start (in-sample only — OOS reserved for Phase 144; read oos_start from config). The market_regimes JOIN follows services/ensemble_trainer.py:472-477 (JOIN market_regimes mr ON mr.asset_class='equity' AND mr.tf=ae.tf AND mr.ts=ae.bar_ts; stratify on mr.regime_label). event_row_id = BaseBatch.content_key(symbol, tf, regime, lookahead, str(run_ts_ns)).

    5. class EnsembleICEngine(BaseBatch): job_name="ensemble-ic-engine", compute_version="1.0.0". async def execute(self, pool): instantiate CorpusManifest("ensemble_ic_engine", Path(".planning/corpus_manifests")); try await self._execute_inner(pool, manifest) except Exception as error: manifest.add_error(str(error)); try manifest.write() except Exception: pass; raise. _execute_inner: load APR via asyncpg fetch into dict; build EnsembleICConfig.from_apr; call _assert_prerequisites (count alpha_events, count forward_returns WHERE return_type='executable_open_to_open' — raise RuntimeError if either is 0); discover distinct (symbol, tf) from alpha_events; load existing alpha_ensemble_ic keys for idempotency; build worker_args; ProcessPoolExecutor(max_workers=config.n_workers) with exe.map(_run_ensemble_ic_worker, worker_args, chunksize=1); accumulate corpus_all_results, corpus_pvals_flat, corpus_pval_result_idxs; after all workers complete, apply ONE corpus-level multipletests(corpus_pvals_flat, alpha=config.fdr_alpha, method="fdr_bh") and scatter bh_adjusted_p + passes_fdr back via the offset index lists (PATTERNS.md lines 210-220); serial write via a single asyncpg connection: `async with pool.acquire() as wconn: async with wconn.transaction(): await wconn.executemany(_ENSEMBLE_IC_INSERT_SQL, rows_to_write)`; manifest.write().

    6. Entrypoint per PATTERNS.md lines 85-95: if __name__ == "__main__": try init_otel_providers("indicagent-ensemble-ic-engine") except OTelInitError as error: _logger.warning(...); settings=Settings(); db_dsn=settings.database_url.replace("postgresql+asyncpg://","postgresql://"); asyncio.run(EnsembleICEngine(db_dsn=db_dsn).run()).

    Do NOT subclass ICEngine. Do NOT fork the IC math into a local copy. Do NOT add collinearity clustering (alpha_score is 1 predictor — every cell IS representative for BH-FDR). Do NOT use psycopg2 (asyncpg + BaseBatch only). Do NOT register the service in service_auditor.py here (Task 3). Do NOT write hold_max_bars APR keys here (that is Plan 02 Task 1 — EIC-02 is Wave 2).

    REFACTOR. After GREEN, run `.venv/bin/ruff check services/ensemble_ic_engine.py tests/unit/test_ensemble_ic_*.py --fix` and `.venv/bin/black services/ensemble_ic_engine.py tests/unit/test_ensemble_ic_*.py`. Confirm full unit suite green.
  </action>

  <verify>
    <automated>.venv/bin/pytest tests/unit/test_ensemble_ic_math.py tests/unit/test_ensemble_ic_config.py tests/unit/test_ensemble_ic_bh_fdr.py tests/unit/test_ensemble_ic_executable_returns.py tests/unit/test_ensemble_ic_wf_stability.py -x -q && .venv/bin/ruff check services/ensemble_ic_engine.py && .venv/bin/python -c "from services.ensemble_ic_engine import EnsembleICEngine, EnsembleICConfig; from src.core.agent.base_batch import BaseBatch; assert issubclass(EnsembleICEngine, BaseBatch); import dataclasses; assert dataclasses.is_dataclass(EnsembleICConfig); print('imports OK')"</automated>
  </verify>

  <acceptance_criteria>
    - services/ensemble_ic_engine.py exists; `from services.ensemble_ic_engine import EnsembleICEngine, EnsembleICConfig` succeeds; EnsembleICEngine is a BaseBatch subclass; EnsembleICConfig is a frozen dataclass.
    - `grep -c "from services.ic_engine import" services/ensemble_ic_engine.py` returns >= 1 (composition — NOT subclassing, NOT forking).
    - `grep -c "class EnsembleICEngine(ICEngine)" services/ensemble_ic_engine.py` returns 0 (subclassing forbidden).
    - `grep -c "return_type = 'executable_open_to_open'" services/ensemble_ic_engine.py` returns >= 1 (Invariant 1 enforced in source; the test_ensemble_ic_executable_returns grep-as-test guards this).
    - `grep -cE "multipletests\(" services/ensemble_ic_engine.py` returns exactly 1 (single corpus-level BH-FDR call — no per-symbol loop).
    - `grep -cE "executemany|execute_batch" services/ensemble_ic_engine.py` >= 1 (serial write from main). `grep -nE "conn.commit|wconn.commit" services/ensemble_ic_engine.py | grep -v "main\|pool.acquire"` finds no worker-side commits (compute-only workers).
    - `grep -c "market_regimes" services/ensemble_ic_engine.py` >= 1 (regime JOIN on the 9-label cross-sectional source).
    - `grep -c "alpha.validation.oos_start" services/ensemble_ic_engine.py` >= 1 (in-sample filter — OOS reserved for Phase 144).
    - All 5 test files pass; `.venv/bin/pytest tests/unit/ -q` full suite green.
    - Startup gate present: `grep -c "alpha_events is empty\|forward_returns.*empty\|RuntimeError" services/ensemble_ic_engine.py` >= 1 (crash-loud on empty inputs).
  </acceptance_criteria>

  <done>EnsembleICEngine composes ic_engine math (Fisher-z CI, NOT bootstrap), writes IC rows per (symbol, tf, regime, lookahead) including the EIC-03 walk_forward_stable boolean, uses ProcessPoolExecutor with compute-only workers and a single serial write, filters executable_open_to_open + in-sample, crashes loud on empty alpha_events; 5 unit test files green; full unit suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Register indicagent-ensemble-ic-engine in service_auditor (DAG correctness)</name>
  <files>services/service_auditor.py</files>

  <read_first>
    - services/service_auditor.py:57-129 (_DAG_ORDER dict — IC pipeline oneshots at priority 8, lines 106-112)
    - services/service_auditor.py:177-205 (_ONESHOT_UNITS frozenset — IC pipeline entries at lines 197-203 with the "inactive between IC pipeline runs is correct" comment)
    - services/service_auditor.py:140-176 (_AGENT_ID_TO_UNIT — only populate if EnsembleICEngine emits an agent_id label; BaseBatch emits job_completed_total with job=, not agent_id=, so this dict likely needs NO entry)
    - CLAUDE.md "Service registry: when adding a service, update _DAG_ORDER, _LAG_THRESHOLDS, _AGENT_ID_TO_UNIT" + "oneshot _agent.py exceptions" note
  </read_first>

  <action>
    Edit services/service_auditor.py with two additions (the third registry, _LAG_THRESHOLDS, is N/A for oneshots per the comment at line 200 — oneshots are excluded from Prometheus lag checks).

    Addition 1 — _DAG_ORDER: add the entry `"indicagent-ensemble-ic-engine": 8,` immediately after the `"indicagent-alpha-publisher": 8,` line (line 112), with an inline comment `# Phase 142A oneshot; alpha_events -> alpha_ensemble_ic; inactive between IC pipeline runs is correct`. Priority 8 places it in the same IC-pipeline tier as ic-engine / ensemble-trainer / alpha-publisher (its upstream producers).

    Addition 2 — _ONESHOT_UNITS: add `"indicagent-ensemble-ic-engine",  # Phase 142A oneshot; Type=oneshot; inactive between weekly IC runs is correct` to the frozenset, immediately after the `"indicagent-alpha-publisher",` entry (line 203), keeping the Phase 139 block comment above it coherent (extend the comment to "Phase 139 ensemble + alpha emission + Phase 142A ensemble IC oneshots" if needed for accuracy).

    Addition 3 — _AGENT_ID_TO_UNIT: BaseBatch emits JOB_COMPLETED_TOTAL with label `job=` (not `agent_id=`), and EnsembleICEngine does not emit persistence_consumer_lag. Therefore NO _AGENT_ID_TO_UNIT entry is required. Verify this by confirming EnsembleICEngine does not call any metric labeled agent_id= (grep services/ensemble_ic_engine.py for 'agent_id' — should return 0 matches). If it returns 0, do NOT add an _AGENT_ID_TO_UNIT entry; document the rationale in a one-line comment above the dict if helpful. If a future task adds an agent_id emission, add the entry then.

    Do NOT touch _LAG_THRESHOLDS (oneshots are excluded by design). Do NOT modify any other registry. Do NOT create the systemd unit file in this task (that is an ops/deploy concern, not a code change — the service runs via `python -m services.ensemble_ic_engine` or the __main__ block until ops wires the timer).
  </action>

  <verify>
    <automated>.venv/bin/python -c "from services.service_auditor import _DAG_ORDER, _ONESHOT_UNITS; assert _DAG_ORDER.get('indicagent-ensemble-ic-engine') == 8, 'not priority 8'; assert 'indicagent-ensemble-ic-engine' in _ONESHOT_UNITS, 'not in oneshot set'; print('registry OK')" && .venv/bin/ruff check services/service_auditor.py && .venv/bin/pytest tests/unit/ -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "from services.service_auditor import _DAG_ORDER; print(_DAG_ORDER['indicagent-ensemble-ic-engine'])"` prints 8.
    - `python -c "from services.service_auditor import _ONESHOT_UNITS; print('indicagent-ensemble-ic-engine' in _ONESHOT_UNITS)"` prints True.
    - No _LAG_THRESHOLDS entry added (oneshots excluded — the auditor's own comment at line 200 confirms this is correct).
    - `.venv/bin/pytest tests/unit/ -q` full suite green (no test regressions from the registry edit).
  </acceptance_criteria>

  <done>indicagent-ensemble-ic-engine registered at DAG priority 8 and in _ONESHOT_UNITS; full unit suite green; no spurious lag-threshold or agent-id entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> EnsembleICEngine | `--symbols` / `--tf` filter args (if added) cross from operator input into SQL; must use parameterized queries |
| alpha_events/forward_returns (read) -> IC compute | Untrusted in the sense of data-quality (NaN, gaps, OOS contamination); not a classical attack surface |
| IC compute -> alpha_ensemble_ic (write) | Single serial writer; correctness depends on corpus-level BH-FDR applied before write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142A-01 | Tampering | CLI `--symbols`/`--tf` filter args in EnsembleICEngine | mitigate | Use asyncpg parameterized queries ($1, $2) — never f-string user input into SQL. ic_engine precedent uses %s params throughout. |
| T-142A-02 | Tampering | Regime label injection (unknown regime string into alpha_ensemble_ic.regime) | mitigate | Startup gate asserts regime in the 9 distinct market_regimes.regime_label values; reject unknown regimes before compute. |
| T-142A-03 | Information Disclosure | Data exfiltration via IC rows | accept | IC rows are aggregate statistics (Spearman correlations, CIs), not raw positions or PII. No new egress path; DB is local. |
| T-142A-04 | Denial of Service | ProcessPoolExecutor workers opening concurrent write connections -> hypertable index-page deadlock | mitigate | Workers are compute-only (return list[dict]); the single serial asyncpg executemany write happens in main AFTER corpus BH-FDR. CLAUDE.md invariant. |
| T-142A-05 | Elevation of Privilege | APR threshold drift (min_qualifying_fraction silently changed) | mitigate | Threshold is APR-seeded [initial_estimate], read via EnsembleICConfig.from_apr at startup; every change recorded in config_history with changed_by + reason. Never baked into code. |
| T-142A-06 | Tampering | OOS contamination of in-sample IC (invalidates EIC-04 gate) | mitigate | All IC queries filter WHERE bar_ts < alpha.validation.oos_start; OOS half reserved for Phase 144. |
| T-142A-SC | Tampering | pip installs | accept | Zero new packages in this plan (scipy/statsmodels/numpy/asyncpg all pre-existing in .venv, verified by research). No slopcheck gate needed — nothing to install. |
</threat_model>

<verification>
- Migration 187 applies cleanly: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/187_alpha_ensemble_ic.sql` exits 0.
- alpha_ensemble_ic is a hypertable with walk_forward_stable column and the 9-regime namespace in hold_max_bars keys.
- `.venv/bin/pytest tests/unit/test_ensemble_ic_*.py -x -q` — all 5 new test files green.
- `.venv/bin/pytest tests/unit/ -q` — full unit suite green (no regressions).
- `from services.ensemble_ic_engine import EnsembleICEngine, EnsembleICConfig` succeeds; EnsembleICEngine is a BaseBatch subclass; EnsembleICConfig is frozen + picklable.
- `grep -c "from services.ic_engine import" services/ensemble_ic_engine.py` >= 1 (composition, not subclass/fork).
- `grep -c "return_type = 'executable_open_to_open'" services/ensemble_ic_engine.py` >= 1 (Invariant 1).
- service_auditor: `_DAG_ORDER['indicagent-ensemble-ic-engine'] == 8` and unit in `_ONESHOT_UNITS`.
- Execution against the live DB is BLOCKED until Phase B repopulates alpha_events — the startup gate crashing loud is correct behavior, not a failure. No execution attempt in this plan.
</verification>

<success_criteria>
- alpha_ensemble_ic hypertable exists with EIC-03 walk_forward_stable column and the 9-regime hold_max_bars key namespace (36 keys).
- EnsembleICEngine composes ic_engine IC math (Fisher-z CI), parallelizes per-(symbol,tf) with compute-only workers, writes serially after corpus BH-FDR, enforces executable_open_to_open + in-sample filter, crashes loud on empty alpha_events.
- 5 unit-test files cover IC parity, config binding, corpus BH-FDR, executable-returns filter, walk-forward stability.
- service_auditor registers the service at priority 8 in _ONESHOT_UNITS.
- EIC-01 (service) and EIC-03 (column + computation) are delivered. EIC-02, EIC-04, EIC-05 ship in Plan 02 (Wave 2) which depends on this plan's alpha_ensemble_ic rows existing.
</success_criteria>

<output>
Create `.planning/phases/142A-ensemble-ic-measurement-planned/142A-01-SUMMARY.md` when done.
</output>
