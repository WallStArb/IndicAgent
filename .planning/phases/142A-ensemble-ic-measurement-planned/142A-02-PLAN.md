---
phase: 142A
plan: 02
type: execute
wave: 2
depends_on: [142A-01]
files_modified:
  - services/ensemble_ic_engine.py
  - scripts/ops/alpha/ops_ensemble_ic_gate.py
  - scripts/ops/alpha/ops_ensemble_ic_diagnosis.py
  - tests/unit/test_ensemble_ic_decay.py
  - tests/unit/test_ensemble_ic_gate.py
autonomous: true
requirements: [EIC-02, EIC-04, EIC-05]

must_haves:
  truths:
    - "EIC-02: For each (symbol, tf, regime), the first lookahead where ic_sharpe < alpha.ensemble_ic.decay_threshold selects the per-cell hold_bars; cells that failed BH-FDR OR are not reliable (n_valid < min_reliable_n) are excluded from the decay curve regardless of raw ic_sharpe (review finding #6 — noise AND low-N cells must not calibrate a load-bearing parameter); the value written to each alpha.frame.hold_max_bars.<regime>.<tf> key is the MEDIAN of those qualifying per-cell decay points across symbols for that (regime, tf); a (regime, tf) pair with zero qualifying symbols is left unwritten (prior APR value stands) rather than defaulted"
    - "EIC-04: The gate script reads min_qualifying_fraction from APR (NOT baked in), computes the fraction of (symbol, tf, regime) cells with ic_ci_lower > 0 AND passes_fdr=true AND walk_forward_stable=true on the gate_lookahead scale FROM THE LATEST RUN (scored_at = max(scored_at) — review finding #4, D-142A-R2), and emits a PASS/FAIL markdown verdict"
    - "EIC-05: The diagnosis script emits a 4-section markdown report (N-per-cell, pooled-vs-per-symbol gap, TF breakdown, regime coverage) reading from alpha_ensemble_ic + feature_ic_scores, scoped to the latest run (scored_at = max(scored_at) — review finding #4); Section 2 computes a TRUE median per-symbol ci_lower via percentile_cont(0.5) (review finding #2 — the max() bug is fixed)"
    - "All gate thresholds are APR-backed — no magic number 0.60 appears in the gate-evaluation logic"
    - "Phase 144 can re-read the same EIC-04 query on OOS data by running EnsembleICEngine on the OOS window (new scored_at vintage) then re-running the gate against that latest run"
  artifacts:
    - path: "services/ensemble_ic_engine.py"
      provides: "EIC-02 decay-curve -> hold_max_bars APR calibration added to execute()"
      contains: "alpha.frame.hold_max_bars"
    - path: "scripts/ops/alpha/ops_ensemble_ic_gate.py"
      provides: "EIC-04 phase-gate evaluation script (asyncpg + markdown verdict; latest-run scoped)"
      contains: "min_qualifying_fraction"
    - path: "scripts/ops/alpha/ops_ensemble_ic_diagnosis.py"
      provides: "EIC-05 gate-failure diagnosis script (4-section markdown report; latest-run scoped; true median in section 2)"
      contains: "regime coverage"
    - path: "tests/unit/test_ensemble_ic_decay.py"
      provides: "EIC-02 first-threshold lookahead selection test (incl. passes_fdr AND reliable gating)"
    - path: "tests/unit/test_ensemble_ic_gate.py"
      provides: "EIC-04 fraction computation + APR-threshold read test"
  key_links:
    - from: "services/ensemble_ic_engine.py"
      to: "config_state (alpha.frame.hold_max_bars.*)"
      via: "await ConfigService.set(key, value, changed_by=..., reason=...)"
      pattern: "alpha\\.frame\\.hold_max_bars"
    - from: "scripts/ops/alpha/ops_ensemble_ic_gate.py"
      to: "alpha_ensemble_ic"
      via: "SELECT fraction of latest-run cells WHERE lookahead=gate_lookahead AND ic_ci_lower>0 AND passes_fdr AND walk_forward_stable"
      pattern: "alpha_ensemble_ic"
    - from: "scripts/ops/alpha/ops_ensemble_ic_gate.py"
      to: "config_state"
      via: "SELECT config_value::float WHERE config_key='alpha.ensemble_ic.min_qualifying_fraction'"
      pattern: "min_qualifying_fraction"
    - from: "scripts/ops/alpha/ops_ensemble_ic_diagnosis.py"
      to: "alpha_ensemble_ic + feature_ic_scores"
      via: "4 asyncpg fetches (N-per-cell, pooled-vs-per-symbol median gap, TF breakdown, regime coverage), latest-run scoped"
      pattern: "regime_label"
---

<objective>
Wave 2 consumes the alpha_ensemble_ic rows written by Plan 01 to (a) calibrate the 36 hold_max_bars APR keys from the IC decay curve (EIC-02), (b) evaluate the phase gate reading its threshold from APR (EIC-04), and (c) emit a structured diagnosis report when the gate fails (EIC-05). This closes Phase 142A: the ensemble OUTPUT has been measured, calibrated, and gated before Phase 142B builds any frame.

Purpose: EIC-02 replaces [initial_estimate] hold horizons with data-derived values so 142B's frames use realistic stops. EIC-04 is the hard phase gate — if alpha_score does not predict returns in enough cells, Phase 142B must not start. EIC-05 prevents wasted weeks chasing the wrong layer when the gate fails.

Output: EIC-02 calibration wired into EnsembleICEngine.execute(), EIC-04 gate script, EIC-05 diagnosis script, 2 unit-test files.

**Review-driven revisions (2026-07-02, addressing 142A-REVIEWS.md — see CONTEXT.md D-142A-R2):**
- Finding #2 (real bug): EIC-05 Section 2's `per_symbol_median_ci_lower` now uses `percentile_cont(0.5)`, an actual median, not `max()`.
- Finding #4: the gate AND the diagnosis select the latest run via `scored_at = (SELECT max(scored_at) FROM alpha_ensemble_ic)` instead of a rolling 7-day window (deterministic on reruns; consistent with the pinned-per-run scored_at semantics of D-142A-R2).
- Finding #6: EIC-02 calibration now excludes cells that are not `reliable=true` in addition to `passes_fdr=false`, so a low-N cell can never set a load-bearing hold_bars.
- Finding LOW: the diagnosis script emits a prominent warning line if `min_obs_per_regime` is absent from APR (instead of silently defaulting).
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/bg/dev/indicagent/.planning/PROJECT.md
@/home/bg/dev/indicagent/.planning/ROADMAP.md
@/home/bg/dev/indicagent/.planning/STATE.md
@/home/bg/dev/indicagent/.planning/phases/142A-ensemble-ic-measurement-planned/142A-CONTEXT.md
@/home/bg/dev/indicagent/.planning/phases/142A-ensemble-ic-measurement-planned/142A-RESEARCH.md
@/home/bg/dev/indicagent/.planning/phases/142A-ensemble-ic-measurement-planned/142A-PATTERNS.md
@/home/bg/dev/indicagent/.planning/phases/142A-ensemble-ic-measurement-planned/142A-01-PLAN.md
@/home/bg/dev/indicagent/CLAUDE.md

<interfaces>
<!-- Contracts from Plan 01 outputs and existing infra that the executor needs. -->

From Plan 01 deliverables (services/ensemble_ic_engine.py after Wave 1):
```python
class EnsembleICEngine(BaseBatch):
    job_name = "ensemble-ic-engine"
    compute_version = "1.0.0"
    async def execute(self, pool: asyncpg.Pool) -> None: ...
    # _execute_inner pins ONE run_ts = datetime.now(UTC) reused as scored_at for every row (D-142A-R2).
    # writes rows to alpha_ensemble_ic with columns:
    #   symbol, tf, regime, lookahead ('fast'|'mid'|'slow'|'extended'),
    #   lookahead_bars (1|5|20|60), is_pooled, n_independent, reliable, ic_sharpe, ic_ci_lower,
    #   passes_fdr, walk_forward_stable, scored_at (== run_ts for the whole run), ...
```
Plan 02 EXTENDS execute() to call the EIC-02 calibration after the serial write completes. Same file — Plan 02 owns the next edit (no parallel-write conflict; depends_on: [142A-01]).

**scored_at / latest-run selection (D-142A-R2, review finding #4):** scored_at is pinned once per invocation. Wave 2 gate/diagnosis MUST select the latest completed run with `scored_at = (SELECT max(scored_at) FROM alpha_ensemble_ic)` — NOT a rolling `NOW() - INTERVAL '7 days'` window (which could mix vintages and be non-deterministic on manual reruns). A single completed run == one scored_at value == one deterministic gate/diagnosis result.

**`reliable` column semantics (review finding #6):** `reliable = bool(n_valid >= min_reliable_n)` (verified in ic_engine.py:1030/1227/1636). It is an N-sufficiency flag INDEPENDENT of passes_fdr — a cell can pass BH-FDR on borderline N yet be `reliable=false`. Therefore EIC-02 calibration requires BOTH passes_fdr=true AND reliable=true; passes_fdr alone is insufficient because reliable guards the small-N case that FDR does not.

From src/config/config_service.py (EIC-02 write API):
```python
class ConfigService:
    async def set(self, key: str, value: str,
                  changed_by: str = "system", expected_version=None,
                  reason: str = None) -> None: ...   # line 164
    # OPS_PREFIXES includes "alpha." (line 51) — alpha.frame.* writes need NO code change.
    # ConfigService requires a pool: instantiate inside execute() as
    #   ConfigService(database_url=..., pool=pool) then await cfg.initialize() if needed.
```
Method name is `set` (NOT `set_async` — the PATTERNS.md code sketch says `set_async` which is WRONG; the live signature at config_service.py:164 is `async def set`).

From config_state (seeded by migration 187 in Plan 01):
- alpha.ensemble_ic.decay_threshold = 0.1  [initial_estimate]
- alpha.ensemble_ic.min_qualifying_fraction = 0.60  [initial_estimate]  (EIC-04 gate threshold — read, never baked)
- alpha.ensemble_ic.gate_lookahead = 'fast'  [initial_estimate]
- alpha.ensemble_ic.wf_stability_ratio = 3.0  [initial_estimate]
- alpha.ensemble_ic.min_obs_per_regime = 3000  [initial_estimate]  (EIC-05 section-1 starvation threshold)
- alpha.validation.oos_start = '2025-12-24T05:15:00Z'  (existing — STATE.md)
- alpha.frame.hold_max_bars.<regime>.<tf> x 36 keys (9 market_regimes labels x 4 TFs) — seeded [initial_estimate]=60, overwritten by EIC-02

EIC-04 gate SQL (from RESEARCH.md Code Examples lines 487-517 — adapted to select the LATEST run per review finding #4 and D-142A-R2, replacing the rolling 7-day window):
```sql
WITH latest AS (SELECT max(scored_at) AS ts FROM alpha_ensemble_ic),
qualifying AS (
    SELECT symbol, tf, regime FROM alpha_ensemble_ic
    WHERE lookahead = $1
      AND ic_ci_lower > 0 AND passes_fdr = true AND walk_forward_stable = true
      AND scored_at = (SELECT ts FROM latest)
    GROUP BY symbol, tf, regime
),
total AS (
    SELECT COUNT(DISTINCT (symbol, tf, regime)) AS n_total FROM alpha_ensemble_ic
    WHERE scored_at = (SELECT ts FROM latest)
)
SELECT (SELECT COUNT(*) FROM qualifying) AS n_qualifying,
       (SELECT n_total FROM total) AS n_total,
       (SELECT COUNT(*) FROM qualifying)::float / NULLIF((SELECT n_total FROM total), 0) AS fraction;
-- $1 = gate_lookahead (parameterized, from config_state). Gate verdict:
-- fraction >= (SELECT config_value::float FROM config_state WHERE config_key='alpha.ensemble_ic.min_qualifying_fraction')
```

Ops script pattern (scripts/ops/pipeline/ops_pipeline_status.py):
- shebang + sys.path insert + asyncpg pool + render via print_section() + async main + asyncio.run.
- Use Settings().database_url.replace("postgresql+asyncpg://","postgresql://") for the DSN.
- Exit code 0 on PASS, non-zero on FAIL (so CI/systemd can gate on it).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EIC-02 — IC decay curve to hold_max_bars APR calibration (extend EnsembleICEngine.execute)</name>
  <files>services/ensemble_ic_engine.py, tests/unit/test_ensemble_ic_decay.py</files>

  <behavior>
    - test_ensemble_ic_decay.py: given synthetic alpha_ensemble_ic rows for one (symbol, tf, regime) with ic_sharpe = [0.5, 0.4, 0.05, -0.1], passes_fdr = [true, true, true, true], reliable = [true, true, true, true] across [fast, mid, slow, extended] (lookahead_bars [1,5,20,60]) and decay_threshold=0.1, the calibration selects hold_bars=5 (the mid scale is the last one above threshold before slow drops below). Given ic_sharpe = [0.5, 0.4, 0.3, 0.2] with passes_fdr + reliable all true, hold_bars=60 (extended — edge persists). Given ic_sharpe = [0.05, ...] with passes_fdr + reliable true (fast already below), hold_bars=1 (minimal hold). **Given a cell with ic_sharpe=0.9 but passes_fdr=false (statistically indistinguishable from noise despite a high raw value), that cell is EXCLUDED from the decay curve entirely — treated as "no data," never as "edge alive"** (review finding: an unqualified cell must never drive a load-bearing execution parameter). **Given a cell with ic_sharpe=0.9, passes_fdr=true, but reliable=false (passed FDR on borderline N; n_valid < min_reliable_n), that cell is ALSO EXCLUDED** (review finding #6: FDR does not guarantee sufficient N; reliable is the small-N guard). Given a (symbol, tf, regime) group where every cell has passes_fdr=false OR reliable=false, _select_hold_bars_from_decay returns None (signal to the caller: skip this cell, leave the existing APR value untouched, do not write a noise-derived or low-N hold_bars). The function is pure: takes a list of cell dicts + decay_threshold, returns hold_bars: int | None.
    - After EnsembleICEngine.execute() runs the serial write, it calls the calibration which writes AT MOST 36 alpha.frame.hold_max_bars.<regime>.<tf> keys via ConfigService.set (one per (regime, tf) that has at least one symbol with a qualifying — passes_fdr=true AND reliable=true — decay point; the calibration is per-(symbol, tf, regime) but the APR key is per-(regime, tf), so aggregate across symbols by taking the MEDIAN hold_bars across symbols that had a qualifying result for that (regime, tf); (regime, tf) pairs with zero qualifying symbols are skipped and the prior APR value is left unchanged; document the choice in the reason= string).
  </behavior>

  <read_first>
    - services/ensemble_ic_engine.py (the file produced by Plan 01 Task 2 — extend execute(), do not rewrite; note the pinned run_ts and the reliable column it already writes)
    - services/ic_engine.py:1030 (reliable = bool(n_valid >= min_reliable_n) — the semantics EIC-02 gates on per review finding #6)
    - .planning/phases/142A-ensemble-ic-measurement-planned/142A-RESEARCH.md:460-485 (EIC-02 calibration code example — note it uses set_async which is WRONG; live API is ConfigService.set; and it OMITS the passes_fdr/reliable gate which MUST be added)
    - .planning/phases/142A-ensemble-ic-measurement-planned/142A-PATTERNS.md:231-241 (ConfigService.set signature)
    - .planning/phases/142A-ensemble-ic-measurement-planned/142A-CONTEXT.md (D-142A-R2 — scored_at/run vintage semantics)
    - src/config/config_service.py:39-52 (OPS_PREFIXES — alpha. present), 164-171 (async def set signature)
    - services/ic_engine.py (lookahead mapping: alpha.ic.lookahead.fast/mid/slow/extended = 1/5/20/60 — read from APR or ICEngineConfig)
    - CLAUDE.md "Adaptive Parameter Registry" + "Migrate-as-you-go"
  </read_first>

  <action>
    RED FIRST. Create tests/unit/test_ensemble_ic_decay.py with a pure function test: import the calibration helper (name it _select_hold_bars_from_decay or similar — a module-level pure function in services/ensemble_ic_engine.py) and assert the behaviors above (including BOTH the passes_fdr=false exclusion AND the reliable=false exclusion). Run `.venv/bin/pytest tests/unit/test_ensemble_ic_decay.py -x` and confirm it fails (function does not exist yet).

    GREEN. Extend services/ensemble_ic_engine.py:

    1. Add a module-level pure function `_select_hold_bars_from_decay(cells: list[dict], decay_threshold: float, scale_to_bars: dict[str, int]) -> int | None`. cells is a list of dicts each with keys lookahead ('fast'|'mid'|'slow'|'extended'), ic_sharpe (float | None), passes_fdr (bool), and reliable (bool). **Significance + sufficiency gate (review finding #6 — mandatory, not optional): filter OUT any cell where `passes_fdr is not True OR reliable is not True` BEFORE the decay-curve walk. A cell that failed BH-FDR is statistically indistinguishable from noise; a cell that is not reliable passed FDR on insufficient N (n_valid < min_reliable_n) and its ic_sharpe is unstable — either MUST NOT set a decay point for a load-bearing hold_bars.** After filtering, if the remaining cell list is empty, return None (caller interprets this as "no qualifying signal for this (symbol, tf, regime) — do not calibrate, leave the prior APR value in place"). Otherwise sort the qualifying cells by the canonical scale order [fast, mid, slow, extended]. Iterate; at the first cell where ic_sharpe is not None and ic_sharpe < decay_threshold, return the lookahead_bars of the PRECEDING scale (or 1 if fast itself is below threshold). If no scale crosses the threshold, return scale_to_bars['extended'] (=60 — edge persists to extended). Handle None ic_sharpe by skipping (treat as "no data", not "below threshold"). This is the logic in RESEARCH.md lines 470-481, corrected three ways: (a) the APR value written is the PRECEDING scale's lookahead_bars (the last scale where edge was still alive), not the failing scale's; (b) only passes_fdr=true cells may participate; (c) only reliable=true cells may participate — RESEARCH.md's code example omits both gates (b) and (c) and must not be followed verbatim.

    2. In EnsembleICEngine.execute() (or _execute_inner), AFTER the serial write to alpha_ensemble_ic completes successfully, add a calibration phase. Build scale_to_bars from the EnsembleICConfig lookahead keys: {"fast": config.lookahead_fast, "mid": config.lookahead_mid, "slow": config.lookahead_slow, "extended": config.lookahead_extended} (values 1/5/20/60 from APR). Group the just-written results by (symbol, tf, regime); for each group call _select_hold_bars_from_decay to get a per-symbol hold_bars (or None if that symbol had no qualifying cell — exclude None results from the next step, do not treat None as 0 or as "minimal hold"). Then aggregate across symbols for each (regime, tf) using ONLY the symbols that returned a non-None hold_bars — use the MEDIAN hold_bars across those qualifying symbols (more robust than min to outlier symbols; document "median across N qualifying symbols per (regime, tf)" in the reason string, including N). **If a (regime, tf) pair has zero qualifying symbols (every symbol returned None), SKIP writing that key entirely — do not call config_service.set for it, and do not fall back to hold_bars=1 or any other default; the existing APR value (prior calibration or the migration's [initial_estimate] seed) remains authoritative until a future run produces a qualifying cell.** For each (regime, tf) pair that DOES have at least one qualifying symbol, call `await config_service.set(f"alpha.frame.hold_max_bars.{regime}.{tf}", str(hold_bars), changed_by="ensemble-ic-engine", reason=f"calibrated from IC decay curve (EIC-02); median across {n_qualifying} qualifying (passes_fdr=true AND reliable=true) symbols; decay_threshold={config.decay_threshold}")`. Instantiate ConfigService inside execute() using the existing pool, then initialize it to match the documented sibling pattern: `config_service = ConfigService(database_url=self._db_dsn, pool=pool)` then `await config_service.initialize()` (ConfigService accepts a pool kwarg per src/config/config_service.py:54-56; `initialize()` is a no-op when a pool is already provided but every sibling service — ensemble_trainer, alpha_publisher — calls it, and the documented contract expects it).

    3. The 9 regime values come from the distinct regimes in the written rows (the same 9 market_regimes labels). The 4 TFs are the distinct tfs. Up to 9 x 4 = 36 keys are considered; only those with at least one qualifying (passes_fdr=true AND reliable=true) symbol are actually written.

    Do NOT bake decay_threshold=0.1 into the calibration logic — read it from config.decay_threshold (which is bound from APR in EnsembleICConfig.from_apr). Do NOT write hold_max_bars keys for the stale bull/bear/sideways/volatile namespace — only the 9 LIVE labels. Do NOT touch the EIC-01 IC computation or the serial write (Plan 01 owns those; this task only adds the post-write calibration phase). Do NOT let a passes_fdr=false OR reliable=false cell influence the written hold_bars value, directly or via the median (it must be excluded from the group before aggregation, not merely down-weighted).

    REFACTOR. ruff + black on services/ensemble_ic_engine.py and the new test file. Full unit suite green.
  </action>

  <verify>
    <automated>.venv/bin/pytest tests/unit/test_ensemble_ic_decay.py -x -q && .venv/bin/ruff check services/ensemble_ic_engine.py && grep -c "alpha.frame.hold_max_bars" services/ensemble_ic_engine.py | grep -qE "^[1-9]" && grep -c "config.decay_threshold" services/ensemble_ic_engine.py | grep -qE "^[1-9]" && grep -c "passes_fdr" services/ensemble_ic_engine.py | grep -qE "^[1-9]" && grep -c "reliable" services/ensemble_ic_engine.py | grep -qE "^[1-9]" && .venv/bin/pytest tests/unit/ -q</automated>
  </verify>

  <acceptance_criteria>
    - _select_hold_bars_from_decay is a pure module-level function; the decay-curve behaviors pass (below-at-mid -> 5, all-above -> 60, below-at-fast -> 1, passes_fdr=false cell excluded even with high ic_sharpe, reliable=false cell excluded even with passes_fdr=true and high ic_sharpe, all-cells-unqualified -> None).
    - `_select_hold_bars_from_decay` filters on BOTH `passes_fdr` AND `reliable` BEFORE the decay walk (review finding #6: grep confirms both `passes_fdr` and `reliable` are referenced in `_select_hold_bars_from_decay`'s body, not just read elsewhere).
    - `grep -c "await config_service.set" services/ensemble_ic_engine.py` >= 1 (APR write via ConfigService.set, NOT set_async).
    - No baked-in decay_threshold literal: `grep -nE "decay_threshold\s*=\s*0\.1" services/ensemble_ic_engine.py` returns 0 matches in calibration logic (only in from_apr default).
    - Calibration runs AFTER the serial write (grep or source-order check: the hold_max_bars write call follows the executemany INSERT).
    - A (regime, tf) pair with zero qualifying (passes_fdr=true AND reliable=true) symbols results in NO config_service.set call for that key — verified by the None-propagation test cases, not just by code inspection.
    - The reason= string names both gates: `grep -c "passes_fdr=true AND reliable=true" services/ensemble_ic_engine.py` >= 1.
    - `.venv/bin/pytest tests/unit/ -q` full suite green.
  </acceptance_criteria>

  <done>EIC-02 calibration extends EnsembleICEngine.execute(); _select_hold_bars_from_decay is pure, gates on passes_fdr AND reliable, and is tested including the noise-exclusion and low-N-exclusion cases; hold_max_bars APR keys are overwritten via ConfigService.set ONLY for (regime, tf) pairs with at least one statistically qualifying AND reliable symbol, using data-derived values; decay_threshold read from APR not baked in; full unit suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EIC-04 — Phase gate evaluation script (threshold from APR, latest-run scoped)</name>
  <files>scripts/ops/alpha/ops_ensemble_ic_gate.py, tests/unit/test_ensemble_ic_gate.py</files>

  <behavior>
    - test_ensemble_ic_gate.py: a pure helper `_evaluate_gate(n_qualifying: int, n_total: int, min_qualifying_fraction: float) -> tuple[bool, float]` returns (True, 0.75) for (75, 100, 0.60); (False, 0.40) for (40, 100, 0.60); (False, 0.0) for (0, 100, 0.60); handles n_total=0 by returning (False, 0.0) without ZeroDivisionError.
    - ops_ensemble_ic_gate.py reads min_qualifying_fraction and gate_lookahead from config_state via asyncpg, runs the EIC-04 SQL on alpha_ensemble_ic scoped to the LATEST run (scored_at = (SELECT max(scored_at) FROM alpha_ensemble_ic) — review finding #4, D-142A-R2), computes the fraction, compares to the APR threshold, and prints a markdown verdict (PASS or FAIL with n_qualifying/n_total/fraction/threshold). Exit code 0 on PASS, 1 on FAIL.
    - The script NEVER hardcodes 0.60 — the threshold always comes from the config_state query.
    - The script NEVER uses a rolling `NOW() - INTERVAL` window — run selection is the deterministic max(scored_at).
  </behavior>

  <read_first>
    - scripts/ops/pipeline/ops_pipeline_status.py:1-29 (shebang + sys.path + imports), 120-129 (asyncpg fetch helper), 120-209 (print_section + render pattern)
    - .planning/phases/142A-ensemble-ic-measurement-planned/142A-RESEARCH.md:487-517 (EIC-04 gate SQL — use as a base but replace the rolling-window run selection with max(scored_at) per finding #4)
    - .planning/phases/142A-ensemble-ic-measurement-planned/142A-PATTERNS.md:311-340 (ops_ensemble_ic_gate.py pattern)
    - .planning/phases/142A-ensemble-ic-measurement-planned/142A-CONTEXT.md (D-142A-R2 — latest-run selection is the locked consequence)
    - src/config/settings.py (Settings().database_url)
    - CLAUDE.md "Exception variable name is error" + "All timestamps UTC"
  </read_first>

  <action>
    RED FIRST. Create tests/unit/test_ensemble_ic_gate.py testing the pure `_evaluate_gate` helper with the 4 behaviors above. Run `.venv/bin/pytest tests/unit/test_ensemble_ic_gate.py -x` and confirm failure (helper does not exist).

    GREEN. Create scripts/ops/alpha/ops_ensemble_ic_gate.py. Structure (mirror scripts/ops/pipeline/ops_pipeline_status.py):

    1. Shebang `#!/usr/bin/env python3`. Module docstring: "ops_ensemble_ic_gate.py — EIC-04 phase-gate evaluation. Reads alpha_ensemble_ic, computes the fraction of (symbol, tf, regime) cells WHERE lookahead = gate_lookahead AND ic_ci_lower > 0 AND passes_fdr = true AND walk_forward_stable = true on the LATEST run (scored_at = max(scored_at); D-142A-R2 — a single pinned-per-run vintage), compares to alpha.ensemble_ic.min_qualifying_fraction (APR, NOT baked in), emits a PASS/FAIL markdown verdict. Phase 144 re-evaluates OOS by running EnsembleICEngine on the OOS window (which writes a NEW scored_at vintage) and re-running this gate — the query always targets the latest run."

    2. Imports: asyncio, sys, pathlib; sys.path.insert(0, str(Path(__file__).parent.parent.parent.parent)); import asyncpg; from src.config.settings import Settings.

    3. Pure helper `_evaluate_gate(n_qualifying: int, n_total: int, min_qualifying_fraction: float) -> tuple[bool, float]`: fraction = n_qualifying / n_total if n_total else 0.0; passed = fraction >= min_qualifying_fraction; return (passed, fraction). Importable from the test (so the test imports from scripts.ops.alpha.ops_ensemble_ic_gate — ensure the path insert works for the test too, or factor the helper into a location the test can import; simplest is to put the helper at module top and have the test sys.path insert the scripts/ops/alpha dir).

    4. async def main(): open asyncpg pool with Settings().database_url.replace("postgresql+asyncpg://","postgresql://"). Fetch threshold: `SELECT config_value::float FROM config_state WHERE config_key='alpha.ensemble_ic.min_qualifying_fraction'`. Fetch gate_lookahead: `SELECT config_value FROM config_state WHERE config_key='alpha.ensemble_ic.gate_lookahead'`. Run the EIC-04 SQL from the <interfaces> block above (the latest-run version), passing gate_lookahead as `$1` (parameterized — NOT f-string) and using `scored_at = (SELECT max(scored_at) FROM alpha_ensemble_ic)` for latest-run-only (review finding #4 — do NOT use the `NOW() - INTERVAL '7 days'` window). The in-sample constraint is enforced at IC-compute time (Plan 01 filters bar_ts < oos_start before writing), so the gate query does not re-filter; document this in a comment and note that Phase 144 OOS evaluation requires a SEPARATE EnsembleICEngine run on the OOS window (which produces a new latest scored_at) then re-running this script. Handle the empty-table case: if max(scored_at) is NULL (no rows yet), n_total=0 → _evaluate_gate returns (False, 0.0); print a clear "no IC rows yet — run EnsembleICEngine first" line and exit 1.

    5. Render: print markdown sections — "## EIC-04 Phase Gate", "Run (scored_at): {latest_scored_at}", "Threshold (APR): {threshold}", "Gate lookahead (APR): {gate_lookahead}", "Qualifying cells: {n_qualifying}/{n_total}", "Fraction: {fraction:.4f}", "Verdict: PASS" or "FAIL". If FAIL, print a one-line pointer: "Run scripts/ops/alpha/ops_ensemble_ic_diagnosis.py for structured diagnosis (EIC-05)."

    6. Exit code: `sys.exit(0 if passed else 1)`. Wrap fetches in try/except with `except Exception as error:` (CLAUDE.md variable name).

    Make the script executable: `chmod +x scripts/ops/alpha/ops_ensemble_ic_gate.py` (document in the action; the executor runs chmod).

    Do NOT bake 0.60 anywhere. Do NOT query feature_ic_scores (that is EIC-05). Do NOT modify EnsembleICEngine (Task 1 owns the calibration). Do NOT add the script to service_auditor (ops scripts are not daemons). Do NOT use a rolling NOW()-INTERVAL window for run selection (review finding #4).
  </action>

  <verify>
    <automated>.venv/bin/pytest tests/unit/test_ensemble_ic_gate.py -x -q && .venv/bin/ruff check scripts/ops/alpha/ops_ensemble_ic_gate.py && grep -c "min_qualifying_fraction" scripts/ops/alpha/ops_ensemble_ic_gate.py | grep -qE "^[2-9]" && grep -c "max(scored_at)" scripts/ops/alpha/ops_ensemble_ic_gate.py | grep -qE "^[1-9]" && test $(grep -c "NOW() - INTERVAL" scripts/ops/alpha/ops_ensemble_ic_gate.py) -eq 0 && test -x scripts/ops/alpha/ops_ensemble_ic_gate.py && .venv/bin/pytest tests/unit/ -q</automated>
  </verify>

  <acceptance_criteria>
    - scripts/ops/alpha/ops_ensemble_ic_gate.py exists, is executable (test -x returns 0).
    - The threshold is read from config_state via `SELECT config_value::float FROM config_state WHERE config_key='alpha.ensemble_ic.min_qualifying_fraction'` — grep finds exactly this query.
    - NO baked-in 0.60 literal in the gate logic: `grep -nE "= 0\.60|== 0\.6|0\.60\b" scripts/ops/alpha/ops_ensemble_ic_gate.py` returns 0 matches.
    - Run selection is the latest run: `grep -c "max(scored_at)" scripts/ops/alpha/ops_ensemble_ic_gate.py` >= 1 AND `grep -c "NOW() - INTERVAL" scripts/ops/alpha/ops_ensemble_ic_gate.py` == 0 (review finding #4).
    - The SQL filters lookahead = gate_lookahead (parameterized), ic_ci_lower > 0, passes_fdr = true, walk_forward_stable = true.
    - `_evaluate_gate` is a pure importable helper; the 4 behavior tests pass.
    - Exit code 0 on PASS, 1 on FAIL (sys.exit(0 if passed else 1)); empty-table case exits 1 with a clear message.
    - `.venv/bin/pytest tests/unit/ -q` full suite green.
  </acceptance_criteria>

  <done>EIC-04 gate script reads threshold and gate_lookahead from APR, computes the qualifying-cell fraction on the LATEST alpha_ensemble_ic run (max(scored_at), no rolling window), emits a PASS/FAIL markdown verdict with exit code; pure helper unit-tested; no baked-in threshold; full unit suite green.</done>
</task>

<task type="auto">
  <name>Task 3: EIC-05 — Gate failure diagnosis script (4-section markdown report; true median; latest-run scoped)</name>
  <files>scripts/ops/alpha/ops_ensemble_ic_diagnosis.py</files>

  <read_first>
    - scripts/ops/pipeline/ops_pipeline_status.py:120-209 (multi-section fetch + print_section + render pattern)
    - .planning/phases/142A-ensemble-ic-measurement-planned/142A-PATTERNS.md:344-348 (diagnosis script pattern)
    - .planning/phases/142A-ensemble-ic-measurement-planned/142A-CONTEXT.md (EIC-05 spec — the 4 diagnostic sections; D-142A-R2 latest-run selection)
    - .planning/ROADMAP.md:1471-1477 (EIC-05 requirement text — the 4 numbered diagnoses)
    - scripts/ops/alpha/ops_ensemble_ic_gate.py (Task 2 output — the diagnosis script is the FAIL companion; reuse the max(scored_at) run-selection idiom)
    - .planning/phases/141.1-measurement-and-decision-integrity-foundation-make-everythin/141.1-02-PLAN.md (RSCOPE-01 — adds feature_ic_scores.regime_scope with values cross_sectional/symbol_hmm/pooled; Section 2's feature_ic_scores comparison must filter regime_scope='cross_sectional' to match alpha_ensemble_ic's 9-label market_regimes stratification)
    - CLAUDE.md "Exception variable name is error"
  </read_first>

  <action>
    Create scripts/ops/alpha/ops_ensemble_ic_diagnosis.py mirroring the multi-section render pattern from scripts/ops/pipeline/ops_pipeline_status.py. Shebang `#!/usr/bin/env python3`, sys.path insert, asyncpg + Settings imports, async def main().

    The script emits a single markdown document with 4 sections (per EIC-05 / ROADMAP lines 1473-1476), each driven by one asyncpg fetch against alpha_ensemble_ic (and feature_ic_scores for comparison in section 2). **ALL four sections scope to the latest run: compute `latest_scored_at = (SELECT max(scored_at) FROM alpha_ensemble_ic)` once and filter every section query on `scored_at = $1` (review finding #4, D-142A-R2 — NOT a rolling NOW()-INTERVAL window).** If latest_scored_at is NULL (no rows), print "no IC rows yet — run EnsembleICEngine first" and exit 0 (diagnosis is informational).

    **min_obs_per_regime read (review finding LOW — loud warning, not silent default):** read `SELECT config_value::int FROM config_state WHERE config_key='alpha.ensemble_ic.min_obs_per_regime'`. If the row is present, use it. If ABSENT (None), fall back to 200 BUT emit a prominent warning line at the TOP of the report: `> **WARNING:** alpha.ensemble_ic.min_obs_per_regime missing from APR — using fallback=200. This masks APR/migration drift; seed the key (migration 187) before trusting section 1.` Do NOT silently default.

    Section 1 — N per cell: `SELECT symbol, tf, regime, lookahead, n_independent FROM alpha_ensemble_ic WHERE scored_at = $1 ORDER BY n_independent ASC NULLS FIRST LIMIT 50`. Render a markdown table; flag rows where n_independent < min_obs_per_regime with "DATA STARVATION" (low N, not signal absence — expect more cells to pass as alpha_events accumulates).

    Section 2 — Pooled vs per-symbol IC gap (**review finding #2 — the median bug is fixed here**): `SELECT tf, regime, lookahead, max(ic_ci_lower) FILTER (WHERE is_pooled) AS pooled_ci_lower, percentile_cont(0.5) WITHIN GROUP (ORDER BY ic_ci_lower) FILTER (WHERE NOT is_pooled) AS per_symbol_median_ci_lower FROM alpha_ensemble_ic WHERE scored_at = $1 AND ic_ci_lower IS NOT NULL GROUP BY tf, regime, lookahead ORDER BY tf, regime, lookahead`. **The `percentile_cont(0.5) WITHIN GROUP (ORDER BY ic_ci_lower) FILTER (WHERE NOT is_pooled)` computes an ACTUAL median of the per-symbol ci_lower values — the previous `max(CASE WHEN NOT is_pooled ...)` was a bug: it returned the maximum, not the median, so the column named "median" lied. There is exactly ONE pooled (is_pooled=true) row per (tf, regime, lookahead) group, so `max(ic_ci_lower) FILTER (WHERE is_pooled)` correctly selects that single pooled value.** Flag rows where pooled_ci_lower > 0 but per_symbol_median_ci_lower <= 0 with "REGIME GRANULARITY ISSUE" (cross-sectional label too coarse for per-symbol variation). For comparison context, also fetch a count from feature_ic_scores WHERE ic_ci_lower > 0 AND regime_scope = 'cross_sectional' (parameterized) to note whether features pass where the ensemble fails. **The `regime_scope = 'cross_sectional'` filter is required** (column added by Phase 141.1's RSCOPE-01): `alpha_ensemble_ic` stratifies exclusively on the 9-label `market_regimes` vocab, so an unfiltered `feature_ic_scores` count would silently blend in `symbol_hmm` (5-label) and `pooled` rows — the same label-provenance ambiguity RSCOPE-01 was built to eliminate ahead of this exact comparison. If `regime_scope` is absent (141.1 not yet applied to this DB), fall back to the unfiltered count and prefix the section-2 output with a one-line note that the comparison is provenance-unfiltered.

    Section 3 — TF breakdown: `SELECT tf, count(*) AS n_cells, count(*) FILTER (WHERE ic_ci_lower > 0 AND passes_fdr) AS n_qualifying FROM alpha_ensemble_ic WHERE scored_at = $1 GROUP BY tf ORDER BY tf`. Flag if 1h passes but 5m fails (TF-specific problem — 5m has fewer independent obs per regime; not a global ensemble problem).

    Section 4 — Regime coverage: `SELECT regime, count(*) AS n_cells, count(*) FILTER (WHERE ic_ci_lower > 0 AND passes_fdr) AS n_qualifying FROM alpha_ensemble_ic WHERE scored_at = $1 GROUP BY regime ORDER BY regime`. Flag if >= 3 regimes have zero qualifying cells (regime label quality issue — check market_regimes coverage and equity_regime_model correctness).

    Render each section with a print_section(title) helper (copy from ops_pipeline_status.py:120-129). Print a header "## EIC-05 Ensemble IC Diagnosis" (including the run's scored_at) and a footer noting "This report is diagnostic; remediation decisions are human/operator." Exit 0 always (diagnosis is informational, not a gate).

    Make the script executable: chmod +x.

    Do NOT gate exit code on findings (this is a report, not a gate). Do NOT bake min_obs_per_regime as a literal (read from APR; if absent, fall back to 200 WITH the loud warning above — review finding LOW). Do NOT use `max()` over a CASE expression for the per-symbol median (review finding #2 — use percentile_cont). Do NOT use a rolling NOW()-INTERVAL window (review finding #4 — use max(scored_at)). Do NOT modify EnsembleICEngine or the gate script.
  </action>

  <verify>
    <automated>.venv/bin/ruff check scripts/ops/alpha/ops_ensemble_ic_diagnosis.py && test -x scripts/ops/alpha/ops_ensemble_ic_diagnosis.py && grep -cE "DATA STARVATION|REGIME GRANULARITY|TF-specific|regime label quality" scripts/ops/alpha/ops_ensemble_ic_diagnosis.py | grep -qE "^[4-9]" && grep -c "min_obs_per_regime" scripts/ops/alpha/ops_ensemble_ic_diagnosis.py | grep -qE "^[1-9]" && grep -c "percentile_cont" scripts/ops/alpha/ops_ensemble_ic_diagnosis.py | grep -qE "^[1-9]" && grep -c "max(scored_at)" scripts/ops/alpha/ops_ensemble_ic_diagnosis.py | grep -qE "^[1-9]" && test $(grep -c "NOW() - INTERVAL" scripts/ops/alpha/ops_ensemble_ic_diagnosis.py) -eq 0 && .venv/bin/pytest tests/unit/ -q</automated>
  </verify>

  <acceptance_criteria>
    - scripts/ops/alpha/ops_ensemble_ic_diagnosis.py exists, is executable.
    - The 4 diagnostic sections are present and each maps to its EIC-05 root-cause label (grep finds DATA STARVATION, REGIME GRANULARITY ISSUE, TF-specific, regime label quality).
    - **Section 2 uses a TRUE median: `grep -c "percentile_cont" scripts/ops/alpha/ops_ensemble_ic_diagnosis.py` >= 1 AND `grep -c "max(CASE WHEN NOT is_pooled" scripts/ops/alpha/ops_ensemble_ic_diagnosis.py` == 0 (review finding #2 — the bug is gone).**
    - **All sections scope to the latest run: `grep -c "max(scored_at)" scripts/ops/alpha/ops_ensemble_ic_diagnosis.py` >= 1 AND `grep -c "NOW() - INTERVAL" scripts/ops/alpha/ops_ensemble_ic_diagnosis.py` == 0 (review finding #4).**
    - min_obs_per_regime is read from config_state (not baked); when absent, a prominent WARNING line is emitted (review finding LOW): `grep -c "WARNING" scripts/ops/alpha/ops_ensemble_ic_diagnosis.py` >= 1.
    - alpha_ensemble_ic and feature_ic_scores are both queried (feature_ic_scores for section-2 comparison context); the feature_ic_scores query filters regime_scope = 'cross_sectional' when the column exists (Phase 141.1 RSCOPE-01), with a documented unfiltered fallback otherwise.
    - Exit code is 0 unconditionally (diagnosis is informational).
    - `.venv/bin/pytest tests/unit/ -q` full suite green (script-only addition; no production code changed).
  </acceptance_criteria>

  <done>EIC-05 diagnosis script emits the 4-section markdown report (N-per-cell, pooled-vs-per-symbol gap with a TRUE median, TF breakdown, regime coverage) reading alpha_ensemble_ic + feature_ic_scores scoped to the latest run, with root-cause labels, APR-backed thresholds, and a loud warning on missing min_obs_per_regime; executable; full unit suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alpha_ensemble_ic rows (read) -> gate/diagnosis scripts | Data-quality boundary: rows may have NULL ic_sharpe (insufficient N), NULL passes_fdr (pre-BH-FDR); scripts must handle NULLs; run selection must be the deterministic latest vintage |
| config_state (read) -> gate threshold | Threshold integrity: if APR row missing, script must fail loud, not silently default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142A-07 | Tampering | Baked-in gate threshold (0.60 hardcoded in ops_ensemble_ic_gate.py) | mitigate | Threshold read via parameterized asyncpg query from config_state; the test_ensemble_ic_gate acceptance grep verifies no 0.60 literal in the script. |
| T-142A-08 | Denial of Service | Division by zero in fraction computation when n_total=0 | mitigate | _evaluate_gate guards n_total==0 -> fraction=0.0, verdict=FAIL (no crash). |
| T-142A-09 | Information Disclosure | Diagnosis report leaks position-level data | accept | Report emits aggregate cell counts and IC statistics only; no symbol-level positions or PII. |
| T-142A-10 | Tampering | APR threshold drift after EIC-02 calibration writes hold_max_bars | mitigate | Every ConfigService.set call records to config_history with changed_by="ensemble-ic-engine" and a reason string; min_qualifying_fraction is read fresh each gate run (never cached across runs). |
| T-142A-11 | Repudiation | Gate run produces a verdict but no audit trail | mitigate | Gate script prints the run's scored_at, threshold value, gate_lookahead, n_qualifying, n_total, and fraction in the markdown — operator can paste into the phase record. |
| T-142A-13 | Tampering | Non-deterministic gate/diagnosis from mixed run vintages (rolling window) | mitigate | Both scripts pin run selection to scored_at = max(scored_at) (review finding #4, D-142A-R2) — one vintage, one deterministic verdict. |
| T-142A-14 | Repudiation | Diagnosis reports a "median" that is actually a max (finding #2), producing a wrong root-cause conclusion | mitigate | Section 2 uses percentile_cont(0.5) WITHIN GROUP; acceptance grep asserts the old max(CASE...) form is absent. |
| T-142A-SC | Tampering | pip installs | accept | Zero new packages (asyncpg pre-existing). |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_ensemble_ic_decay.py tests/unit/test_ensemble_ic_gate.py -x -q` — both new test files green.
- `.venv/bin/pytest tests/unit/ -q` — full unit suite green.
- `grep -c "await config_service.set\|alpha.frame.hold_max_bars" services/ensemble_ic_engine.py` >= 2 (EIC-02 calibration wired into execute()).
- `grep -c "passes_fdr=true AND reliable=true" services/ensemble_ic_engine.py` >= 1 (EIC-02 dual gate — review finding #6).
- `grep -cE "= 0\.60|== 0\.6" scripts/ops/alpha/ops_ensemble_ic_gate.py` returns 0 (threshold from APR, not baked).
- `grep -c "max(scored_at)" scripts/ops/alpha/ops_ensemble_ic_gate.py` >= 1 AND `grep -c "max(scored_at)" scripts/ops/alpha/ops_ensemble_ic_diagnosis.py` >= 1 (latest-run selection — review finding #4).
- `grep -c "percentile_cont" scripts/ops/alpha/ops_ensemble_ic_diagnosis.py` >= 1 AND the old `max(CASE WHEN NOT is_pooled` form is absent (review finding #2).
- Both ops scripts are executable (test -x).
- Live execution of the gate/diagnosis scripts is BLOCKED until Plan 01 runs against Phase B data (alpha_ensemble_ic will be empty until EnsembleICEngine runs). The scripts must handle the empty-table case gracefully (max(scored_at) IS NULL -> clear "no IC rows yet — run EnsembleICEngine first" message, gate exits 1, diagnosis exits 0; no crash).
</verification>

<success_criteria>
- EIC-02: EnsembleICEngine.execute() calibrates the 36 alpha.frame.hold_max_bars.<regime>.<tf> APR keys from the IC decay curve after each run, gating on passes_fdr AND reliable; _select_hold_bars_from_decay is pure and unit-tested; decay_threshold read from APR.
- EIC-04: ops_ensemble_ic_gate.py reads min_qualifying_fraction + gate_lookahead from APR, computes the qualifying-cell fraction on the latest run (max(scored_at)), emits PASS/FAIL markdown with exit code; no baked-in threshold; no rolling window.
- EIC-05: ops_ensemble_ic_diagnosis.py emits the 4-section markdown report scoped to the latest run, with a TRUE median in section 2 and root-cause labels; thresholds APR-backed with a loud warning on missing min_obs_per_regime.
- Phase 142A complete: ensemble OUTPUT IC is measured (Plan 01), hold horizons calibrated (EIC-02), gate evaluable (EIC-04), failures diagnosable (EIC-05). Phase 142B unblocked once EIC-04 passes on Phase B data.
</success_criteria>

<output>
Create `.planning/phases/142A-ensemble-ic-measurement-planned/142A-02-SUMMARY.md` when done.
</output>
