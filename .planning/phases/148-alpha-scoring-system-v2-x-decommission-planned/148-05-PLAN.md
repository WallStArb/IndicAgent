---
phase: 148
plan: 05
type: execute
wave: 3
depends_on: [148-02, 148-03, 148-04]
files_modified:
  - docs/plans/2026-07-22-phase148-promotion-decision.md
  - .planning/gate_look_log.jsonl
autonomous: true
requirements: [SCORE-02, SCORE-03, SCORE-04]

must_haves:
  truths:
    - "Gate 1 (signal proof) is run exactly once and its verdict recorded in gate_evaluations (gate_id='gate1_signal')"
    - "Gate 2 (execution proof) is run exactly once, after Gate 1, its verdict recorded in gate_evaluations (gate_id='gate2_execution')"
    - "A promotion decision record exists disclosing both independent verdicts, the D-06 known-failing champion numbers, the regime-stratified breakdown, and the SCORE-04 v2.x note"
  artifacts:
    - path: "docs/plans/2026-07-22-phase148-promotion-decision.md"
      provides: "Two-gate promotion verdict + SCORE-04 v2.x note + overall decision"
      contains: "Gate 1"
    - path: ".planning/gate_look_log.jsonl"
      provides: "Auditable run-once trail for the gate scripts"
  key_links:
    - from: "docs/plans/2026-07-22-phase148-promotion-decision.md"
      to: "gate_evaluations"
      via: "cites both gate1_signal and gate2_execution rows"
      pattern: "gate_evaluations"
    - from: "docs/plans/2026-07-22-phase148-promotion-decision.md"
      to: "143.1-08-SHADOW-VALIDATION.md sec 6/7"
      via: "verbatim champion numbers (D-06)"
      pattern: "0\\.38512018365944"
---

<objective>
Run the two OOS proof gates exactly once each (Gate 1 before Gate 2 per D-02), and write the
promotion decision record that synthesizes both independent verdicts plus the SCORE-04 v2.x
note. This is the milestone deliverable: the verdict the entire measurement pipeline exists to
produce. These gate runs are IRREVERSIBLE per D-04 - each runs at most once; re-running "to
check if it passes now" is a protocol violation, not a fix.

Purpose: produce the authoritative signal-proof and execution-proof verdicts and the human
decision record. Output: two gate_evaluations rows, an appended look-log, and the decision doc.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/148-alpha-scoring-system-v2-x-decommission-planned/148-CONTEXT.md
@.planning/phases/148-alpha-scoring-system-v2-x-decommission-planned/148-VALIDATION.md
@docs/plans/OOS-EVAL-PROTOCOL.md
@docs/plans/SHADOW-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Gate 1 (SCORE-02 signal proof) exactly once</name>
  <files>.planning/gate_look_log.jsonl</files>
  <read_first>
    - scripts/ops/corpus/ops_oos_gate1_signal_eval.py (the script built in plan 148-03)
    - docs/plans/OOS-EVAL-PROTOCOL.md (run-once cadence, data-starvation-is-diagnostic failure rule)
    - .planning/gate_look_log.jsonl (confirm no prior gate1_signal run exists before running)
  </read_first>
  <action>
    Before running, assert this gate has not already been run for this milestone: `SELECT count(*) FROM gate_evaluations WHERE gate_id='gate1_signal'` must be 0. If it is not 0, STOP and report - do not run again (D-04).

    Invoke ops_oos_gate1_signal_eval.py once against the live DB over the active ETF universe (get_active_contracts) across timeframes 5m/15m/1h/1d. This run is IRREVERSIBLE per D-04; capture whatever verdict it produces. Expect a meaningful fraction of OOS cells to return walk_forward_stable=None or insufficient-N because the OOS window is much shorter than the in-sample history - per OOS-EVAL-PROTOCOL that data-starvation signal is itself diagnostic (data absence vs signal absence), NOT a defect to patch by lowering min_reliable_n. Confirm the verdict + evidence landed in gate_evaluations (gate_id='gate1_signal') and one entry was appended to .planning/gate_look_log.jsonl.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tc "SELECT count(*)=1 FROM gate_evaluations WHERE gate_id='gate1_signal';" | grep -q t</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 1 gate_evaluations row with gate_id='gate1_signal'
    - That row's evidence jsonb is non-empty and records the oos_start boundary used
    - .planning/gate_look_log.jsonl has exactly one gate1 entry with a report sha256
    - Zero rows were inserted into alpha_ensemble_ic by this run (`SELECT count(*) FROM alpha_ensemble_ic` unchanged from pre-run)
  </acceptance_criteria>
  <done>Gate 1 run once; signal-proof verdict recorded; look-log appended.</done>
</task>

<task type="auto">
  <name>Task 2: Run Gate 2 (SCORE-03 execution proof) exactly once, after Gate 1</name>
  <files>.planning/gate_look_log.jsonl</files>
  <read_first>
    - scripts/analysis/score03_gate2_execution_eval.py (the script built in plan 148-04)
    - .planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-08-SHADOW-VALIDATION.md sections 6 and 7
    - docs/plans/SHADOW-REVIEW.md (the 5 criteria)
  </read_first>
  <action>
    Enforce D-02 sequencing: only proceed once Task 1 recorded its gate1_signal row. Then assert Gate 2 has not already been run: `SELECT count(*) FROM gate_evaluations WHERE gate_id='gate2_execution'` must be 0; if not, STOP (D-04).

    Invoke score03_gate2_execution_eval.py once against weight_epoch='143.1-08-champion'. This run is IRREVERSIBLE per D-04. Confirm the single gate_evaluations row (gate_id='gate2_execution') contains BOTH the pooled criteria and the regime_cells breakdown in its evidence. The known-in-advance pooled result is a FAIL and this is expected, not a surprise to investigate: c2_ci_lower=-0.1214896346368989 (fails >0), c3_sharpe=0.38512018365944 (fails >0.5), c4_max_dd=9.598299843093644 (fails <0.25 ratio), c1=True (69 OOS days). The regime-stratified companion: only 2 of 8 champion cells clear min_clusters=20 coverage (long/mid_bull ci_lower=-0.077 fails; short/mid_bull ci_lower=-0.278 fails); the other 6 are coverage="insufficient". Do NOT shift the OOS window to change this result (D-05: reshifting the window because the visible result is unfavorable is the same banned post-hoc renegotiation as moving a numeric threshold).
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tc "SELECT count(*)=1 FROM gate_evaluations WHERE gate_id='gate2_execution';" | grep -q t</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 1 gate_evaluations row with gate_id='gate2_execution'
    - No separate row with gate_id='FRAME-04' (`SELECT count(*) FROM gate_evaluations WHERE gate_id='FRAME-04'` == 0)
    - That row's evidence jsonb contains both a pooled criteria block AND a regime_cells array
    - Task 1's gate1_signal row exists (D-02 ordering held) before this row's run_ts
  </acceptance_criteria>
  <done>Gate 2 run once, after Gate 1; execution-proof verdict recorded with pooled + regime-stratified evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Write the promotion decision record (SCORE-04 + overall verdict)</name>
  <files>docs/plans/2026-07-22-phase148-promotion-decision.md</files>
  <read_first>
    - docs/plans/SHADOW-REVIEW.md and docs/plans/OOS-EVAL-PROTOCOL.md (governance-rule docs this record answers to)
    - .planning/phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-08-SHADOW-VALIDATION.md sec 6/7 (verbatim champion numbers)
    - The two gate_evaluations rows just written (query them for the actual recorded verdicts)
    - CLAUDE.md doc conventions (Author: provenance line; canonical decision docs live in docs/plans/)
  </read_first>
  <action>
    Create docs/plans/2026-07-22-phase148-promotion-decision.md with these sections:

    (1) Gate 1 - signal proof: state the verdict recorded in gate_evaluations gate1_signal, summarize its IC/CI/walk-forward evidence, and note explicitly how much of the OOS window returned insufficient-N (data starvation as diagnostic, per OOS-EVAL-PROTOCOL).

    (2) Gate 2 - execution proof: cite the champion 143.1-08 pooled numbers VERBATIM (c2_ci_lower=-0.1214896346368989, c3_sharpe=0.38512018365944, c4_max_dd=9.598299843093644, c1=69 OOS days) per D-06, and present the regime-stratified companion (2 of 8 cells clear min_clusters=20; long/mid_bull ci_lower=-0.077 and short/mid_bull ci_lower=-0.278 both fail; 6 cells insufficient coverage) per D-07. Explain what the regime breakdown reveals about WHY the pooled number failed: todo 165 proved on this exact data that a pooled single-window verdict is structurally blind to regime-conditional edge (shorts profited in the COVID crash, were breakeven through the 2022 bear, lost specifically in the one rally window tested). State up front that these 3-of-5-criteria failures were known going in, not discovered during execution.

    (3) Independence statement: Gate 1 and Gate 2 are reported as two independent verdicts; do not conflate a signal failure with an execution failure or vice versa.

    (4) SCORE-04 v2.x comparison note: documentation only. State that no live v2.x comparison population exists - the v2.x I1-I7 pipeline has been failed since 2026-07-17 with its ExecStart target deleted from disk and zero plugins ever promoted/evaluated live - so this is a prose note, not a numeric comparison.

    (5) Overall promotion decision + rationale, grounded in both gate verdicts. Do NOT propose a fix for a failing gate; diagnosing why Gate 2 failed and whether a frame/execution recalibration helps is explicitly out of scope for this phase (deferred idea in CONTEXT.md).

    Add an `Author:` provenance line per project convention. Do not use em dashes.
  </action>
  <verify>
    <automated>grep -q "0.38512018365944" docs/plans/2026-07-22-phase148-promotion-decision.md && grep -q "9.598299843093644" docs/plans/2026-07-22-phase148-promotion-decision.md && grep -qi "Gate 1" docs/plans/2026-07-22-phase148-promotion-decision.md && grep -qi "Gate 2" docs/plans/2026-07-22-phase148-promotion-decision.md && grep -qi "v2.x" docs/plans/2026-07-22-phase148-promotion-decision.md</automated>
  </verify>
  <acceptance_criteria>
    - Doc cites the verbatim champion numbers -0.1214896346368989, 0.38512018365944, and 9.598299843093644
    - Doc has distinct "Gate 1" and "Gate 2" sections stating each verdict independently
    - Doc contains the regime-stratified breakdown (2 of 8 cells clear coverage; both evaluated cells fail)
    - Doc contains the SCORE-04 v2.x note stating no live comparison population exists
    - Doc states an overall promotion decision and does not propose a fix for a failing gate
    - Doc has an `Author:` provenance line and contains no em dash characters
  </acceptance_criteria>
  <done>Promotion decision record written: both independent verdicts disclosed, D-06 numbers cited verbatim, regime-stratified companion shown, SCORE-04 v2.x note included, overall decision stated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> irreversible gate run -> DB | Each gate run is a one-shot that consumes part of the frozen OOS holdout; re-running degrades holdout integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-07 | Repudiation | run-once cadence (D-04) | mitigate | Pre-run count assertion (must be 0) before each gate; append-only gate_look_log.jsonl records every look |
| T-148-08 | Tampering | OOS window boundary (D-05) | mitigate | Window is read from alpha.validation.oos_start, never shifted to change an unfavorable result; the decision record discloses rather than reshifts |
| T-148-09 | Information disclosure | promotion decision record | accept | Internal governance artifact; no external exposure, no secrets |
</threat_model>

<verification>
- Exactly one gate_evaluations row each for gate1_signal and gate2_execution; no FRAME-04 row.
- gate1_signal run_ts precedes gate2_execution run_ts (D-02 ordering).
- .planning/gate_look_log.jsonl has one entry per gate run.
- Promotion decision record exists and passes the grep checks (verbatim numbers, both gates, v2.x note).
- `.venv/bin/pytest tests/unit/ -q` full suite green.
</verification>

<success_criteria>
Both OOS proof gates run exactly once (Gate 1 before Gate 2), verdicts recorded in
gate_evaluations, and a promotion decision record synthesizes both independent verdicts plus
the SCORE-04 v2.x note and the D-06 known-failing champion numbers.
</success_criteria>

<output>
Create `.planning/phases/148-alpha-scoring-system-v2-x-decommission-planned/148-05-SUMMARY.md` when done.
</output>
