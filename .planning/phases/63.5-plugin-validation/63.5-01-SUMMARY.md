# Phase 63.5-01 Summary: Plugin Validation Layer

**Commit:** 8418855f
**Date:** 2026-04-09

## Changes

### Task 1: Fix PluginValidator constructor bugs (`src/core/plugin_validator.py`)

1. **Bare log path** -- Changed `setup_service_logging("plugin_validator")` to `setup_service_logging("logs/plugin_validator.log")`. Bare name causes runtime failure on log file creation.

2. **Prometheus duplicate registration** -- Replaced 3 inline `try/except ValueError` blocks in `__init__` with module-level constants (`_REGISTERED_PLUGINS_GAUGE`, `_VALIDATION_STATUS_GAUGE`, `_VALIDATION_ERRORS_COUNTER`) created via `_get_or_create_gauge()` / `_get_or_create_counter()` helper functions. These use `_PROM_REGISTRY._names_to_collectors` lookup on duplicate, matching the pattern from `src/observability/metrics.py`.

3. **Top-level import** -- `setup_service_logging` was already at top level; `REGISTRY` import added to top level as `_PROM_REGISTRY`.

### Task 1: Failure-path tests (`tests/unit/test_plugin_validator.py`)

Added 5 failure-path tests (total now 12):

| Test | Injection | Verifies |
|------|-----------|----------|
| `test_fails_when_i7_plugin_missing_regime_type` | Delete `regime_type` from class + instance `__dict__` | RuntimeError raised |
| `test_fails_when_trend_setups_contains_unregistered_name` | Patch `pv_mod.TREND_SETUPS` with fake name | RuntimeError raised |
| `test_fails_when_tier_i7_name_not_registered` | Append fake name to `TIER_I7` list | RuntimeError raised |
| `test_fails_when_i7_plugin_has_invalid_regime_type` | Set `regime_type` to `"bogus_value"` on class + instance | RuntimeError raised |
| `test_clean_registry_returns_zero_errors` | Clean registry | `error_count() == 0` |

Key discovery: Dataclass plugin instances have their own copy of `regime_type` in `__dict__`. Deleting from class alone is insufficient -- must also pop from instance dict.

### Task 2: Wire into pipeline startup (`services/intelligence_pipeline_agent.py`)

Added `PluginValidator().validate_all()` call immediately after the existing `registry.validate_tier()` loop (line 444). The existing `validate_tier` loop is retained -- the new call adds comprehensive attribute, schema, orphan, and TREND_SETUPS validation.

## Test Results

- All 12 `test_plugin_validator.py` tests pass
- All 13 `test_intelligence_pipeline_agent.py` tests pass
- Ruff: no new issues introduced
- Pre-commit hooks: all passed
