---
phase: 128-3-table-schema-design-and-adr
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/137_3table_schema.sql
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "production/migrations/137_3table_schema.sql exists and is runnable DDL"
    - "signal_events is created as a TimescaleDB hypertable partitioned on ts with 7-day chunks"
    - "Composite PK (signal_id, ts) on signal_events"
    - "trade_frames FK is FOREIGN KEY (signal_id, signal_ts) REFERENCES signal_events (signal_id, ts)"
    - "signal_ledger_v2 view is created with exact SQL from CONTEXT.md D-05"
    - "Compression policy: segmentby symbol,timeframe; orderby ts DESC; compress after 7 days"
    - "No GIN indexes present (deferred per CONTEXT.md)"
    - "Migration is idempotent (uses IF NOT EXISTS guards)"
  artifacts:
    - path: "production/migrations/137_3table_schema.sql"
      provides: "Runnable CREATE TABLE DDL for 3-table signal architecture"
      contains: "CREATE TABLE signal_events"
  key_links:
    - from: "production/migrations/137_3table_schema.sql"
      to: "signal_events"
      via: "SELECT create_hypertable('signal_events', 'ts', ...)"
      pattern: "create_hypertable"
    - from: "production/migrations/137_3table_schema.sql"
      to: "trade_frames"
      via: "FOREIGN KEY (signal_id, signal_ts) REFERENCES signal_events (signal_id, ts)"
      pattern: "REFERENCES signal_events"
---

<objective>
Produce the runnable DDL migration file for the 3-table signal architecture. This file is the single source of truth for Phase 129 to execute. It must be complete, correct, and require zero design decisions from the Phase 129 executor.

Purpose: Gives Phase 129 a ready-to-run migration with all CREATE TABLE, hypertable, compression, index, and view SQL. Phase 128 writes the file; Phase 129 executes it.
Output: production/migrations/137_3table_schema.sql
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-3-table-schema-design-and-adr/128-CONTEXT.md
@.planning/phases/128-3-table-schema-design-and-adr/128-RESEARCH.md
@production/migrations/136_phase126_i7_apr_params.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 137_3table_schema.sql DDL migration</name>
  <read_first>
    - .planning/phases/128-3-table-schema-design-and-adr/128-CONTEXT.md (D-02 signal_events columns, D-03 trade_frames columns, D-04 trade_executions columns, D-05 signal_ledger_v2 view SQL exact text, D-06 float8 rule, D-07 direction text rule)
    - .planning/phases/128-3-table-schema-design-and-adr/128-RESEARCH.md (Hypertable Configuration section — exact SQL for create_hypertable and compression; Architecture Patterns section — migration file style; Common Pitfalls section)
    - production/migrations/136_phase126_i7_apr_params.sql (style reference: header comment block, section dividers, ON CONFLICT pattern)
  </read_first>
  <files>production/migrations/137_3table_schema.sql</files>
  <action>
    Create production/migrations/137_3table_schema.sql following the style of migration 136 (header comment block, logical sections separated by -- --- dividers).

    **Header comment block** (lines 1-15): Migration 137 description. Purpose: create 3-table signal architecture (signal_events hypertable, trade_frames regular, trade_executions regular). Note that this file is SCHEMA ONLY — Phase 129 executes the data migration from signal_ledger. Note the FK constraint requires signal_ts on trade_frames (hypertable PK is composite). Note that signal_ledger is NOT dropped here.

    **Section 1: signal_events** — CREATE TABLE signal_events with all 26 columns from CONTEXT.md D-02 in this exact order and types:
    - signal_id uuid NOT NULL
    - ts timestamptz NOT NULL (comment: "Bar timestamp at fire time — hypertable partition dimension")
    - symbol text NOT NULL
    - timeframe text NOT NULL
    - setup_plugin text NOT NULL
    - direction text NOT NULL (comment: "long / short — converted from integer 1/-1 in Phase 129")
    - raw_confidence float8 NOT NULL (comment: "Intrinsic composite; immutable after emit")
    - calibrated_confidence float8 (comment: "Nullable; async-populated by calibration pipeline")
    - cis_score float8
    - weights_version int4
    - factor_scores jsonb (comment: "Per-plugin factor breakdown; ML weight optimization")
    - context_features jsonb (comment: "Full flat_features snapshot; SignalRanker feature matrix")
    - ctf_score float8
    - ctf_confirmed bool
    - zone_friction_score float8
    - hmm_regime_at_fire int4
    - plugin_regime_type text
    - garch_sigma_at_fire float8
    - is_shadow bool NOT NULL DEFAULT false
    - is_backfill bool NOT NULL DEFAULT false
    - status text NOT NULL DEFAULT 'pending' (comment: "pending / active / regime_suppressed / expired")
    - signal_schema_version int4 (comment: "int4 not text — correction from signal_ledger.signal_schema_version text")
    - ttl_bars int4
    - expires_at timestamptz
    - signal_computed_at timestamptz (comment: "Pipeline write wall-clock from payload; latency = signal_computed_at - ts")
    - created_at timestamptz NOT NULL DEFAULT now() (comment: "DB insertion time — distinct from signal_computed_at")

    After CREATE TABLE:
    SELECT create_hypertable('signal_events', 'ts', chunk_time_interval => INTERVAL '7 days', if_not_exists => TRUE);
    ALTER TABLE signal_events ADD PRIMARY KEY (signal_id, ts);
    ALTER TABLE signal_events SET (timescaledb.compress, timescaledb.compress_segmentby = 'symbol,timeframe', timescaledb.compress_orderby = 'ts DESC');
    SELECT add_compression_policy('signal_events', INTERVAL '7 days');

    **Indexes on signal_events** (after hypertable + compression setup):
    - CREATE INDEX idx_signal_events_symbol_ts ON signal_events (symbol, ts DESC);
    - CREATE INDEX idx_signal_events_setup_plugin_ts ON signal_events (setup_plugin, ts DESC);
    - CREATE INDEX idx_signal_events_ctf ON signal_events (ctf_confirmed, ts DESC);
    - CREATE INDEX idx_signal_events_regime ON signal_events (hmm_regime_at_fire, ts DESC);
    - CREATE INDEX idx_signal_events_shadow ON signal_events (is_shadow);
    - CREATE INDEX idx_signal_events_backfill ON signal_events (is_backfill);
    - CREATE INDEX idx_signal_events_status_ts ON signal_events (status, ts DESC);
    - CREATE INDEX idx_signal_events_expires ON signal_events (expires_at) WHERE expires_at IS NOT NULL;

    **Section 2: trade_frames** — CREATE TABLE trade_frames with all 20 columns from CONTEXT.md D-03:
    - frame_id uuid NOT NULL PRIMARY KEY
    - signal_id uuid NOT NULL
    - signal_ts timestamptz NOT NULL (comment: "Denormalized from signal_events; required for FK to hypertable composite PK")
    - entry_type text NOT NULL (comment: "at_close / at_pullback / at_limit / at_reclaim / zone_proximal")
    - direction text NOT NULL (comment: "long / short")
    - entry_price float8
    - stop_price float8
    - target_price float8
    - r_per_unit float8 (comment: "(target - entry) / (entry - stop)")
    - ttl_bars int4
    - expires_at timestamptz
    - counterfactual_pnl_r float8 (comment: "Always populated by CounterfactualTracker; ML target variable")
    - counterfactual_mfe float8
    - counterfactual_mae float8
    - counterfactual_bars int4
    - counterfactual_exit_reason text (comment: "target_hit / stop_hit / ttl_expired")
    - counterfactual_measured_at timestamptz
    - was_selected bool NOT NULL DEFAULT false
    - frame_details jsonb (comment: "Stop architecture provenance: stop_basis, stop_type_col, structural_stop_distance_atr, adaptive_buffer_mult, stop_structure_type, stop_structure_age_bars, chandelier_vol_source, trailing_stop_price, trailing_stop_tightening_rate, entry_zone_low, entry_zone_high. Also archives historical shadow_mae, shadow_mfe, shadow_outcome, shadow_tracking_start_ts during Phase 129 migration.")
    - created_at timestamptz NOT NULL DEFAULT now()

    FK constraint: CONSTRAINT fk_trade_frames_signal FOREIGN KEY (signal_id, signal_ts) REFERENCES signal_events (signal_id, ts)

    **Indexes on trade_frames**:
    - CREATE INDEX idx_trade_frames_signal ON trade_frames (signal_id, signal_ts);
    - CREATE INDEX idx_trade_frames_entry_type_pnl ON trade_frames (entry_type, counterfactual_pnl_r);
    - CREATE INDEX idx_trade_frames_selected_pnl ON trade_frames (was_selected, counterfactual_pnl_r);

    **Section 3: trade_executions** — CREATE TABLE trade_executions with all 13 columns from CONTEXT.md D-04:
    - execution_id uuid NOT NULL PRIMARY KEY
    - frame_id uuid NOT NULL
    - actual_fill_price float8
    - actual_exit_price float8
    - actual_pnl_r float8
    - actual_mfe float8
    - actual_mae float8
    - actual_bars int4
    - market_entry_price float8
    - market_entry_gap_bars int4
    - exit_reason text
    - executed_at timestamptz
    - exited_at timestamptz

    FK constraint: CONSTRAINT fk_trade_executions_frame FOREIGN KEY (frame_id) REFERENCES trade_frames (frame_id)

    **Indexes on trade_executions**:
    - CREATE INDEX idx_trade_executions_frame ON trade_executions (frame_id);
    - CREATE INDEX idx_trade_executions_executed_at ON trade_executions (executed_at);

    **Section 4: signal_ledger_v2 view** — Paste EXACTLY the SQL from CONTEXT.md D-05. No modifications. The view must be named signal_ledger_v2. Use CREATE OR REPLACE VIEW. Include a comment above the CREATE VIEW noting that this is the backward-compatibility surface; signal_ledger_full (migration 095) is superseded after Phase 129.

    Do NOT include: GIN indexes, any data migration, any DROP of signal_ledger, any transaction wrapper. This file is schema-only DDL.
  </action>
  <verify>
    grep -c "CREATE TABLE signal_events" production/migrations/137_3table_schema.sql
    grep -c "CREATE TABLE trade_frames" production/migrations/137_3table_schema.sql
    grep -c "CREATE TABLE trade_executions" production/migrations/137_3table_schema.sql
    grep -c "create_hypertable" production/migrations/137_3table_schema.sql
    grep -c "FOREIGN KEY (signal_id, signal_ts) REFERENCES signal_events" production/migrations/137_3table_schema.sql
    grep -c "CREATE OR REPLACE VIEW signal_ledger_v2" production/migrations/137_3table_schema.sql
    grep -c "add_compression_policy" production/migrations/137_3table_schema.sql
    grep -c "counterfactual_pnl_r" production/migrations/137_3table_schema.sql
    # Confirm no GIN indexes:
    grep -c "GIN" production/migrations/137_3table_schema.sql || echo "CORRECT: no GIN indexes"
    # Confirm no DROP signal_ledger:
    grep -c "DROP.*signal_ledger" production/migrations/137_3table_schema.sql || echo "CORRECT: no DROP"
  </verify>
  <done>
    - production/migrations/137_3table_schema.sql exists
    - grep "CREATE TABLE signal_events" returns 1
    - grep "CREATE TABLE trade_frames" returns 1
    - grep "CREATE TABLE trade_executions" returns 1
    - grep "create_hypertable" returns 1
    - grep "FOREIGN KEY (signal_id, signal_ts) REFERENCES signal_events" returns 1 (correct FK pattern)
    - grep "CREATE OR REPLACE VIEW signal_ledger_v2" returns 1
    - grep "add_compression_policy" returns 1
    - grep "counterfactual_pnl_r" returns at least 2 (column definition + view reference)
    - grep "GIN" returns 0 (no GIN indexes)
    - grep "DROP.*signal_ledger" returns 0 (no premature DROP)
    - File has header comment block naming migration 137 and its purpose
  </done>
</task>

</tasks>

<verification>
wc -l production/migrations/137_3table_schema.sql — should be 150+ lines for a complete DDL.
PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent --dry-run -f production/migrations/137_3table_schema.sql 2>&1 — NOT executed in Phase 128; syntax check only if tools support it. Phase 129 executes.
</verification>

<success_criteria>
production/migrations/137_3table_schema.sql is a complete, runnable DDL file that Phase 129 can apply without modifications. All three tables are fully defined, the hypertable and compression config are wired, the FK uses the composite pattern required by TimescaleDB, and the signal_ledger_v2 view matches the exact SQL from CONTEXT.md D-05.
</success_criteria>

<output>
After completion, create .planning/phases/128-3-table-schema-design-and-adr/128-02-SUMMARY.md
</output>
