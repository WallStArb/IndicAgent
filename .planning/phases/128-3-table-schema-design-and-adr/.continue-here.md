---
context: phase
phase: 128-3-table-schema-design-and-adr
task: complete
total_tasks: 3
status: complete
last_updated: 2026-06-16T03:23:45Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified. Phase completed cleanly._

<current_state>
Phase 128 is COMPLETE. All 3 plans executed and merged to main.

Outputs delivered:
- `docs/architecture/signal-trade-separation-ADR.md` — 447 lines, 10 sections, institutional memory for SLA
- `production/migrations/137_3table_schema.sql` — 237 lines, Phase 129-ready DDL
- `capture_signal_features()` deleted from `confidence_utils.py` + orphaned test file removed; 4740 tests green

**Naming corrections applied post-execution (Renaissance audit):**
- `timeframe` → `tf` on `signal_events` (matches `intelligence_features`, per naming-system.md Surface 4)
- `r_per_unit` → `r_multiple` on `trade_frames` (standard quant term, passes whiteboard test)

**Uncommitted working-tree changes** (pre-existing, not Phase 128):
`CLAUDE.md`, `PROJECT.md`, `REQUIREMENTS.md`, `128-01/02-PLAN.md`, `128-CONTEXT.md`, `128-RESEARCH.md`, `126-REVIEW.md`, `124-05-SUMMARY.md`
</current_state>

<completed_work>
- Task 1 (128-01): signal-trade-separation-ADR.md written — all 10 sections including G0 Audit (CLEARED), FK design rationale, Alternatives Considered, Dropped Columns
- Task 2 (128-02): 137_3table_schema.sql written — signal_events hypertable, trade_frames + trade_executions regular tables, 13 indexes, compression policy, signal_ledger_full view
- Task 3 (128-03): capture_signal_features() deleted, tests/unit/intelligence/trading/test_capture_signal_features.py removed, src/intelligence/CLAUDE.md updated
</completed_work>

<remaining_work>
Phase 128 is complete. Next steps across the v2.10 roadmap:

- **Phase 127** (clean replay + validation) — independent, can run now
- **Phase 129** (execute 137_3table_schema.sql migration + data migration from signal_ledger) — requires Phase 128 complete (done)
- **Phase 130** (rewrite writers/trackers/APIs to use 3-table schema; CounterfactualTracker daemon) — requires Phase 129
</remaining_work>

<decisions_made>
- `tf` not `timeframe` on `signal_events` — naming-system.md Surface 4 is explicit; `intelligence_features` uses `tf`; Phase 128 is clean-break opportunity
- `r_multiple` not `r_per_unit` — standard quant finance term, whiteboard test passes; `r_per_unit` is non-standard
- Migration 137 stays in `production/migrations/` — db/migrations/ drift is pre-existing (migrations 122-136 all in production/migrations/); migration location cleanup is a separate task
- `plugin_regime_type` name retained — acceptable, column comment distinguishes it from `hmm_regime_at_fire`
- `signal_ledger_full` view name locked — glossary defines it explicitly as the Phase 128 backward-compat surface
- `direction` column is `text` (`long`/`short`) not `integer` (1/-1) — conversion happens in Phase 129 data migration
- `signal_schema_version` is `int4` not `text` — corrects signal_ledger legacy type
- Shadow tracking columns (`shadow_mae`, `shadow_mfe`, `shadow_outcome`) archived to `frame_details` JSONB in Phase 129, not dropped
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/128-3-table-schema-design-and-adr/128-CONTEXT.md` — all D-0x decisions; authoritative schema spec
2. `docs/architecture/signal-trade-separation-ADR.md` — Phase 128 output; read before Phase 129
3. `production/migrations/137_3table_schema.sql` — the DDL Phase 129 will execute
4. `docs/plans/2026-06-14-v2.10-signal-architecture-refactor.md` — full roadmap context for Phases 128-130

## Infrastructure State
- All services running normally; no Phase 128 changes affect live services (schema-only, not executed yet)
- `signal_ledger` monolith still the live write target until Phase 129 executes and Phase 130 rewrites writers
- 4740 unit tests passing

<context>
Phase 128 was the design phase — ADR + DDL + dead code removal. No live schema changes yet. Phase 129 is the execution phase that applies 137_3table_schema.sql to the live DB and migrates data. Phase 130 is the largest phase — rewrites all writers, trackers, and APIs.

The Renaissance naming audit in this session caught two real issues that were fixed before merge: `timeframe`→`tf` (inconsistent with `intelligence_features`) and `r_per_unit`→`r_multiple` (non-standard term). Both fixes are committed.

The pre-existing uncommitted changes to CLAUDE.md, PROJECT.md, etc. are from the broader v2.10 milestone work earlier in the session — they should be committed as a separate docs update, not mixed with Phase 128 work.
</context>

<next_action>
Start with: `/gsd-verify-work 128` to verify phase goal achievement against the ROADMAP requirement (ARCH-01). Then `/gsd-code-review 128` before advancing. Phase 129 or Phase 127 can follow — Phase 127 is independent and lower risk.
</next_action>
