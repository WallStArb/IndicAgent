---
status: complete
phase: 128-3-table-schema-design-and-adr
source: [128-01-SUMMARY.md, 128-02-SUMMARY.md, 128-03-SUMMARY.md]
started: 2026-06-16T00:00:00Z
updated: 2026-06-16T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test — Migration Applies Clean
expected: Apply the new migration against the live database. Run: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/137_3table_schema.sql — The script runs to completion with no ERROR lines. Tables signal_events, trade_frames, trade_executions, and view signal_ledger_full are all created (or IF NOT EXISTS guards confirm they already exist).
result: issue
reported: "Migration failed at view creation: 'cannot drop columns from view'. CREATE OR REPLACE VIEW signal_ledger_full fails because migration 095 created signal_ledger_full over the old monolith with different columns — PostgreSQL prohibits column removal via CREATE OR REPLACE VIEW."
severity: major
fix_applied: "Replaced CREATE OR REPLACE VIEW with DROP VIEW IF EXISTS signal_ledger_shadow; DROP VIEW IF EXISTS signal_ledger_full; CREATE VIEW in 137_3table_schema.sql. View applied to live DB. All four objects now exist."

### 2. ADR Document Exists and Is Complete
expected: docs/architecture/signal-trade-separation-ADR.md exists. Opening it shows 10 sections: Status/Date/Deciders header, Context, Decision, Full Schema Tables (26/20/13 columns), G0 Audit (CLEARED), Phase 130 Writer Contract, Dropped Columns (staleness_score, staleness_trigger_reason listed explicitly), FK Design on Hypertable, Hypertable Configuration, Alternatives Considered, Consequences.
result: pass

### 3. signal_events Defined as Hypertable with Composite PK and Compression
expected: In production/migrations/137_3table_schema.sql, signal_events has 26 columns, create_hypertable() call with chunk_time_interval => INTERVAL '7 days', composite PRIMARY KEY (signal_id, ts), timescaledb.compress enabled with segmentby (symbol, timeframe), add_compression_policy with 7-day policy, and 8 btree indexes.
result: pass

### 4. trade_frames Has Composite FK Anchored to Hypertable PK via signal_ts
expected: In the migration SQL, trade_frames has a signal_ts column (denormalized FK anchor) and a FOREIGN KEY (signal_id, signal_ts) REFERENCES signal_events (signal_id, ts) constraint — not a single-column FK to signal_id alone. trade_frames has 20 columns and counterfactual_pnl_r present.
result: pass

### 5. signal_ledger_full View Joins All Three Tables
expected: The migration SQL defines CREATE OR REPLACE VIEW signal_ledger_full as a LEFT JOIN across signal_events, trade_frames, and trade_executions. The view name is signal_ledger_full (not signal_ledger_v2). The existing signal_ledger monolith table is NOT dropped in this migration.
result: pass

### 6. capture_signal_features() Deleted — Dead Code Gone
expected: grep -n "def capture_signal_features" src/intelligence/trading/confidence_utils.py returns no output. The function is gone. All remaining exports (compose_confidence, CONF_CEIL, etc.) still importable: python -c "from src.intelligence.trading.confidence_utils import compose_confidence, CONF_CEIL; print('ok')" prints ok. Unit tests pass: .venv/bin/pytest tests/unit/ -q shows green (4740+ passed).
result: pass

## Summary

total: 6
passed: 5
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Migration applies clean with no errors — all three tables and signal_ledger_full view created"
  status: failed
  reason: "User reported: CREATE OR REPLACE VIEW signal_ledger_full failed with 'cannot drop columns from view' — existing view from migration 095 has incompatible column set"
  severity: major
  test: 1
  root_cause: "PostgreSQL CREATE OR REPLACE VIEW cannot remove columns. migration 095 created signal_ledger_full over old signal_ledger monolith; new definition targets signal_events/trade_frames/trade_executions — different schema."
  artifacts:
    - path: "production/migrations/137_3table_schema.sql"
      issue: "CREATE OR REPLACE VIEW used instead of DROP + CREATE"
  missing:
    - "DROP VIEW IF EXISTS signal_ledger_shadow; DROP VIEW IF EXISTS signal_ledger_full; before CREATE VIEW"
  fix_status: "APPLIED in-band — migration SQL patched and view applied to live DB"
