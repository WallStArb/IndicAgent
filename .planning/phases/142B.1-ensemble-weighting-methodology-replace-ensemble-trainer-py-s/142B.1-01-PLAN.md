---
phase: 142B.1-ensemble-weighting-methodology-replace-ensemble-trainer-py-s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/196_ensemble_shrinkage_mean_variance.sql
  - services/ensemble_ic_engine.py
  - tests/unit/test_ensemble_ic_pooled_dispatch.py
autonomous: true
requirements: [REQ-142B1-D01, REQ-142B1-D02, REQ-142B1-D07, REQ-142B1-D08-APR]
must_haves:
  truths:
    - "Migration 196 adds ic_shrunk + shrinkage_weight columns to feature_ic_scores without touching any existing column"
    - "Migration 196 seeds four APR keys (alpha.ic.shrinkage_k, alpha.ensemble.mv_condition_max, alpha.ensemble.weight_method, alpha.ensemble.ic_input) with non-breaking defaults"
    - "EnsembleICEngine dispatches a symbol='POOLED' measurement per (tf, regime) that aggregates alpha_score across symbols grouped by (tf, regime, bar_ts)"
    - "A re-run of ensemble_ic_engine.py populates symbol='POOLED' rows in alpha_ensemble_ic that pass the alpha_ensemble_ic_pooled_symbol_consistent CHECK constraint"
  artifacts:
    - path: "production/migrations/196_ensemble_shrinkage_mean_variance.sql"
      provides: "2 new feature_ic_scores columns + 4 APR keys, idempotent"
      contains: "ADD COLUMN IF NOT EXISTS ic_shrunk"
    - path: "services/ensemble_ic_engine.py"
      provides: "Pooled cross-sectional dispatch (todo 046)"
      contains: "POOLED"
    - path: "tests/unit/test_ensemble_ic_pooled_dispatch.py"
      provides: "Unit coverage for pooled aggregation grain (group by tf/regime/bar_ts before averaging)"
      exports: ["test"]
  key_links:
    - from: "services/ensemble_ic_engine.py"
      to: "build_ensemble_ic_row(symbol='POOLED')"
      via: "existing _POOLED_SYMBOL sentinel + is_pooled derivation"
      pattern: "build_ensemble_ic_row"
    - from: "production/migrations/196_ensemble_shrinkage_mean_variance.sql"
      to: "config_state / config_schema / config_history"
      via: "three-table APR seed with ON CONFLICT DO NOTHING"
      pattern: "config_state"
---

<objective>
Establish "judge readiness" (Wave 0) plus the schema/APR contract that every downstream E1/E2 wave reads. Two independent deliverables: (1) migration 196 adds the 2 new `feature_ic_scores` columns (`ic_shrunk`, `shrinkage_weight`) and seeds the 4 new APR keys with non-breaking defaults; (2) `ensemble_ic_engine.py` gains the missing pooled cross-sectional dispatch (todo 046 / WR-02) so `alpha_ensemble_ic` gets `symbol='POOLED'` rows — the only statistically clean grain to declare a weight-variant winner (D-01).

Purpose: Without the pooled grain, A/B comparison would rely on ad hoc aggregation of 58 noisy per-symbol IC estimates (D-01). Without the columns/APR keys, E1 and E2 have nothing to write to or read from. This plan is the interface-defining foundation for the whole phase — it ships first so downstream waves receive contracts, not a scavenger hunt.

Output: migration 196, pooled dispatch in `ensemble_ic_engine.py`, unit test for the aggregation grain.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142B.1-ensemble-weighting-methodology-replace-ensemble-trainer-py-s/142B.1-CONTEXT.md
@.planning/phases/142B.1-ensemble-weighting-methodology-replace-ensemble-trainer-py-s/142B.1-RESEARCH.md
@.planning/phases/142B.1-ensemble-weighting-methodology-replace-ensemble-trainer-py-s/142B.1-PATTERNS.md
@.planning/todos/pending/046-pooled-cross-sectional-ic-measurement.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase 2026-07-02, verified live. -->

feature_ic_scores PRIMARY KEY (also the bulk-update key downstream E1 uses):
  (feature_name, symbol, tf, regime, lookahead_bars, training_window_end)

ensemble_ic_engine.py current dispatch (fix point, lines ~680-697):
  symbols_rows = await conn.fetch("SELECT DISTINCT symbol, tf FROM alpha_events WHERE bar_ts < $1", oos_start)
  symbol_tf_pairs = [(r["symbol"], r["tf"]) for r in symbols_rows]   # NEVER contains 'POOLED'

Existing sentinel + row builder (do NOT reinvent):
  _POOLED_SYMBOL = "POOLED"                    # line ~103
  build_ensemble_ic_row(symbol=..., ...)       # line ~280; derives is_pooled = (symbol == _POOLED_SYMBOL) at line ~305
  CHECK alpha_ensemble_ic_pooled_symbol_consistent: (symbol = 'POOLED') = is_pooled   # migration 195 line 58

_calibrate_hold_max_bars already skips pooled rows (line ~769-777: `if row.get("is_pooled"): continue`) — no change needed there.

Migration 195 APR three-table seed shape (config_schema INSERT, config_state INSERT, config_history INSERT, each ON CONFLICT (config_key) DO NOTHING) — copy exactly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 196 — 2 feature_ic_scores columns + 4 APR keys</name>
  <read_first>
    - production/migrations/195_alpha_ensemble_ic.sql (full file — the exact BEGIN/COMMIT wrap, idempotency, and three-table APR seed pattern to copy)
    - .planning/phases/142B.1-ensemble-weighting-methodology-replace-ensemble-trainer-py-s/142B.1-PATTERNS.md (section "production/migrations/196_*.sql (NEW)" — column + APR seed spec)
    - .planning/phases/142B.1-ensemble-weighting-methodology-replace-ensemble-trainer-py-s/142B.1-CONTEXT.md (D-07 schema footprint, Claude's Discretion on migration numbering)
    - CLAUDE.md (APR mandate — provenance tags [initial_estimate]/[conventional]/[ML learning target], config_schema+config_state+config_history triad required for every key)
  </read_first>
  <action>
    Create `production/migrations/196_ensemble_shrinkage_mean_variance.sql`, wrapped in a single `BEGIN; ... COMMIT;`, all statements idempotent.
    Columns: `ALTER TABLE feature_ic_scores ADD COLUMN IF NOT EXISTS ic_shrunk double precision, ADD COLUMN IF NOT EXISTS shrinkage_weight double precision;` plus a CHECK constraint `shrinkage_weight IS NULL OR (shrinkage_weight BETWEEN 0 AND 1)` added idempotently (guard with a DO-block that checks pg_constraint, mirroring migration 195's idempotent-constraint pattern). No new table, no create_hypertable call.
    APR keys — seed all four via the three-table triad (config_schema INSERT, config_state INSERT with version=1, config_history INSERT with changed_by='migration_196'), each config_schema/config_state INSERT `ON CONFLICT (config_key) DO NOTHING`:
      1. `alpha.ic.shrinkage_k` — value_type float, default '100', description tagged `[initial_estimate, ML learning target]`, explains `w = n_eff/(n_eff+k)`, seeded so a cell at min_reliable_n=100 gets ~50% shrinkage toward its peer-group prior, validated by the D-05 out-of-fold gate.
      2. `alpha.ensemble.mv_condition_max` — value_type float, default '1000', description tagged `[initial_estimate]`, states this is the np.linalg.cond (2-norm) gate for E2's Σ⁻¹·IC solve (cite 2-norm explicitly per RESEARCH Assumption A3), slated for recalibration once real condition-number distributions are observed.
      3. `alpha.ensemble.weight_method` — value_type str, default `'ic_proportional'` (the existing v1 behavior — purely additive, non-breaking), tagged `[conventional]`, states it selects ic_proportional vs mean_variance in ensemble_trainer.
      4. `alpha.ensemble.ic_input` — value_type str, default `'ic_sharpe_hac'` (keeps ensemble_trainer reading the pre-existing column until E1's hard gate passes, per D-05), tagged `[conventional]`, states it selects ic_sharpe_hac vs ic_shrunk.
    Do NOT modify any existing column, index, or APR key. This migration is strictly additive.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS ic_shrunk\|ADD COLUMN IF NOT EXISTS shrinkage_weight\|alpha.ic.shrinkage_k\|alpha.ensemble.mv_condition_max\|alpha.ensemble.weight_method\|alpha.ensemble.ic_input" production/migrations/196_ensemble_shrinkage_mean_variance.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `production/migrations/196_ensemble_shrinkage_mean_variance.sql` exists and contains a single top-level `BEGIN;` and `COMMIT;`.
    - `grep -c "ON CONFLICT (config_key) DO NOTHING" production/migrations/196_ensemble_shrinkage_mean_variance.sql` returns >= 8 (config_schema + config_state for each of the 4 keys).
    - Each of the 4 APR keys appears in a config_schema, a config_state, AND a config_history INSERT (verify: `grep -c "alpha.ensemble.mv_condition_max"` >= 3).
    - `weight_method` default is the literal `'ic_proportional'` and `ic_input` default is the literal `'ic_sharpe_hac'` (non-breaking defaults — verify both strings present).
    - Every APR description contains a bracketed provenance tag: `grep -oE "\[(initial_estimate|conventional|ML learning target)[^]]*\]" production/migrations/196_ensemble_shrinkage_mean_variance.sql | wc -l` returns >= 4.
    - No `create_hypertable` and no `CREATE TABLE` in the file (grep returns 0 for each).
  </acceptance_criteria>
  <done>Migration 196 exists, idempotent, adds exactly 2 columns + 4 APR keys with non-breaking defaults and provenance tags; no existing schema modified.</done>
</task>

<task type="auto">
  <name>Task 2: Pooled cross-sectional dispatch in ensemble_ic_engine.py (todo 046)</name>
  <read_first>
    - services/ensemble_ic_engine.py (the file being modified — read _execute_inner ~lines 649-760, the _POOLED_SYMBOL sentinel ~103, build_ensemble_ic_row ~280-310, the worker signature ~432-464, and _calibrate_hold_max_bars ~769-777)
    - services/ensemble_trainer.py (lines 388-405 — the canonical symbol='POOLED' cross-sectional query shape that defines "what a clean pooled row looks like": groups by (tf, regime) at UNIVERSE grain)
    - .planning/todos/pending/046-pooled-cross-sectional-ic-measurement.md (suggested approach 1 — the grain to follow literally)
    - .planning/phases/142B.1-ensemble-weighting-methodology-replace-ensemble-trainer-py-s/142B.1-RESEARCH.md (Pitfall 5 — group by (tf, regime, bar_ts) BEFORE averaging across symbols, never average-first-label-second)
    - .planning/phases/142B.1-ensemble-weighting-methodology-replace-ensemble-trainer-py-s/142B.1-PATTERNS.md (section "services/ensemble_ic_engine.py (EXTEND — Wave 0 pooled dispatch)")
  </read_first>
  <action>
    Extend `_execute_inner` so that, in addition to the existing per-(symbol, tf) measurement, it produces one `symbol='POOLED'` measurement per (tf, regime) whose alpha_score series is the cross-sectional aggregate of all symbols sharing that (tf, regime, bar_ts) cell. Implement the aggregation at the correct grain per RESEARCH Pitfall 5: group alpha_events by (tf, regime, bar_ts) and average alpha_score across symbols BEFORE running IC, never average-then-label. Route the pooled series through the existing `build_ensemble_ic_row(symbol=_POOLED_SYMBOL, ...)` path so `is_pooled` is derived automatically and the `alpha_ensemble_ic_pooled_symbol_consistent` CHECK is satisfied at INSERT (a grain bug fails loudly, not silently). Choose main-process fan-in aggregation over per-symbol worker results IF the per-symbol alpha_score-per-bar values are already available post-dispatch (simpler, matches the data shape); otherwise dispatch a dedicated pooled worker task using the same `(symbol, tfs, db_dsn, oos_start, config, run_ts)` tuple signature — decide based on what the existing dispatch already materializes, and document the choice in a code comment. Do NOT change `_calibrate_hold_max_bars` (it already skips pooled rows). Keep per-symbol rows intact (D-03 — per-symbol stays as diagnostic layer, never dropped).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_ensemble_ic_pooled_dispatch.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_POOLED_SYMBOL\|POOLED" services/ensemble_ic_engine.py` shows the pooled dispatch/aggregation uses the existing `_POOLED_SYMBOL` sentinel (no new hardcoded "POOLED" string literal introduced for the row's symbol field).
    - The pooled aggregation groups by (tf, regime, bar_ts) before averaging across symbols — verifiable as a source assertion (a GROUP BY / grouping over those three keys, or an equivalent Python groupby, exists in the new code) and by the unit test in Task 3.
    - Per-symbol measurement path is unchanged (the existing `symbol_tf_pairs` per-symbol dispatch still runs — grep confirms `SELECT DISTINCT symbol, tf FROM alpha_events` still present).
    - `.venv/bin/pytest tests/unit/test_ensemble_ic_pooled_dispatch.py -x` passes.
  </acceptance_criteria>
  <done>ensemble_ic_engine.py dispatches pooled cross-sectional measurements at UNIVERSE grain (one per tf/regime), routed through build_ensemble_ic_row with symbol=POOLED; per-symbol rows retained; unit test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit test for pooled aggregation grain</name>
  <read_first>
    - tests/unit/test_ensemble_ic_worker_fetch.py (closest analog — dispatch/fetch logic unit test style, fixture conventions, no live DB)
    - services/ensemble_ic_engine.py (the aggregation function/branch added in Task 2 — test targets its pure grain logic)
    - .planning/phases/142B.1-ensemble-weighting-methodology-replace-ensemble-trainer-py-s/142B.1-PATTERNS.md (section "tests/unit/test_ensemble_ic_pooled_dispatch.py")
  </read_first>
  <behavior>
    - Test: given synthetic alpha_events rows for 3 symbols sharing one (tf, regime, bar_ts) cell, the pooled aggregate for that cell is the mean of the 3 symbols' alpha_score at that bar_ts (grain: group by tf/regime/bar_ts, then average across symbols).
    - Test: symbols with DIFFERENT regime labels at the same bar_ts are NOT mixed into the same pooled cell (labeling happens before averaging).
    - Test: a pooled cell built for a (tf, regime) produces a row whose symbol equals _POOLED_SYMBOL and would satisfy (symbol='POOLED')=is_pooled (assert against build_ensemble_ic_row output or the aggregation helper's contract).
    - Test: empty input (no alpha_events for a cell) yields no pooled row, no divide-by-zero.
  </behavior>
  <action>
    Create `tests/unit/test_ensemble_ic_pooled_dispatch.py` following the pure-function/no-DB fixture style of `test_ensemble_ic_worker_fetch.py`. Feed synthetic in-memory rows (dicts/lists) to the pooled aggregation helper/branch factored in Task 2 — if the aggregation is currently inline, factor it into a small importable pure function (e.g. `_aggregate_pooled_series` or similar) so it is unit-testable without a live DB, matching the project convention of extracting a pure helper for testability (as ops_ensemble_ic_gate.py does with _evaluate_gate). Assert the four behaviors above.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_ensemble_ic_pooled_dispatch.py -v</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/test_ensemble_ic_pooled_dispatch.py` exists, imports only from `services.ensemble_ic_engine` (no DB/Kafka), and defines >= 4 test functions covering the behaviors above.
    - `.venv/bin/pytest tests/unit/test_ensemble_ic_pooled_dispatch.py -v` reports all tests passing.
    - The "different regime labels not mixed" test fails if the aggregation were changed to average-first-label-second (regression guard for RESEARCH Pitfall 5).
  </acceptance_criteria>
  <done>Pooled aggregation grain is unit-covered and green; regression guard for average-first-label-second in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase has no user-facing input surface, no auth/session boundary, and processes no untrusted external input. It is internal batch statistical computation over already-validated, internally-produced data (`alpha_events`, `forward_returns`, `market_regimes`, `feature_ic_scores`). The only trust boundary is data-integrity: a math/grain bug silently producing wrong pooled IC.

| Boundary | Description |
|----------|-------------|
| alpha_events → pooled aggregation | internally-produced rows; risk is wrong pooling grain, not malicious input |
| migration → config_state / feature_ic_scores | DDL + APR seed; risk is a breaking default or a mutated existing column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142B1-01-01 | Tampering (data-integrity) | pooled aggregation grain in ensemble_ic_engine.py | mitigate | Grain enforced by grouping on (tf, regime, bar_ts) before averaging; Task 3 regression test guards against average-first-label-second; DB CHECK `alpha_ensemble_ic_pooled_symbol_consistent` fails loudly on a grain bug at INSERT |
| T-142B1-01-02 | Tampering (schema regression) | migration 196 | mitigate | Migration is strictly additive (ADD COLUMN IF NOT EXISTS + new APR keys only); acceptance criteria assert zero CREATE TABLE / zero create_hypertable / non-breaking defaults; existing columns untouched |
| T-142B1-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A — zero new packages); nothing to verify |
</threat_model>

<verification>
- Migration 196 present, idempotent, additive-only (Task 1 acceptance criteria).
- `.venv/bin/pytest tests/unit/test_ensemble_ic_pooled_dispatch.py -v` green.
- Operational (post-migration, done at execute time if DB available): applying migration 196 then re-running `ensemble_ic_engine.py` produces `symbol='POOLED'` rows in `alpha_ensemble_ic` satisfying the CHECK constraint — this is the D-02 baseline measurement of the current `weight_version='v1'`.
- `.venv/bin/pytest tests/unit/ -q` green (wave-merge gate).
</verification>

<success_criteria>
- feature_ic_scores has ic_shrunk + shrinkage_weight columns; 4 new APR keys exist with non-breaking defaults.
- ensemble_ic_engine.py produces pooled cross-sectional rows at UNIVERSE grain; per-symbol rows retained.
- Pooled aggregation grain is unit-tested with a Pitfall-5 regression guard.
</success_criteria>

<output>
Create `.planning/phases/142B.1-ensemble-weighting-methodology-replace-ensemble-trainer-py-s/142B.1-01-SUMMARY.md` when done.
</output>
