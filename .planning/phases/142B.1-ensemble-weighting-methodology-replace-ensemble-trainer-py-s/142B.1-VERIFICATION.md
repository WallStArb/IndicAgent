---
phase: 142B.1-ensemble-weighting-methodology-replace-ensemble-trainer-py-s
verified: 2026-07-04T14:30:00Z
status: passed
score: 15/15 must-haves verified
overrides_applied: 0
---

# Phase 142B.1: Ensemble Weighting Methodology Verification Report

**Phase Goal:** Replace `ensemble_trainer.py`'s IC-proportional weighting with better-validated
alternatives (E1 shrunk-IC inputs, E2 mean-variance Σ⁻¹·IC combination), judged by Phase 142A's
`EnsembleICEngine` on OOS-scoped in-sample data, with E3/E4 deferred. Every variant is a new
`weight_version` in the existing `ensemble_weights`/`ensemble_alpha` PK.

**Verified:** 2026-07-04
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (mapped to CONTEXT.md D-01..D-14)

| # | Decision | Truth | Status | Evidence |
|---|---------|-------|--------|----------|
| 1 | D-01 | Pooled cross-sectional pass built in `EnsembleICEngine` at UNIVERSE grain (one row per tf/regime shared by all symbols) | ✓ VERIFIED | `services/ensemble_ic_engine.py` has `_POOLED_SYMBOL`, `_POOLED_WORKER_FETCH_SQL`, `_aggregate_pooled_series`, dispatched via `symbol_to_tfs[_POOLED_SYMBOL]`. Live DB: `alpha_ensemble_ic` has `symbol='POOLED', is_pooled=true` rows. |
| 2 | D-02 | Baseline `v1` pooled IC measurement established (`alpha_ensemble_ic` was 0 rows before) | ✓ VERIFIED | Live DB query: `weight_version='v1', symbol='POOLED', tf='5m', regime='high_bear'`, 16 rows — real, not synthetic. |
| 3 | D-03 | Per-symbol rows retained as diagnostic layer, never dropped | ✓ VERIFIED | Live DB: dozens of per-symbol rows (SPY, TLT, IEF, EMB, etc.) coexist with the POOLED rows under `weight_version='v1'`. Per-symbol dispatch (`SELECT DISTINCT symbol, tf FROM alpha_events`) untouched. |
| 4 | D-04 | E1 (shrinkage) built and sequenced before E2 (mean-variance) so E2 combines de-noised inputs | ✓ VERIFIED | Wave ordering: Plan 03 (E1) landed before Plan 04 (E2); `ensemble_trainer.py`'s `ic_sharpes` array (fed to both `select_features_per_stratum` and `mean_variance_weights`) is the single `ic_input`-resolved column, so E2 automatically inherits whatever E1 currently has live. |
| 5 | D-05 | `shrink_ic` empirical-Bayes formula; hard out-of-fold gate; `ic_input` flips to `ic_shrunk` only on empirical PASS | ✓ VERIFIED | `src/intelligence/ensemble/shrinkage.py::shrink_ic` matches `w=n_eff/(n_eff+k)` spec; `tests/unit/test_ensemble_shrinkage.py` (9 tests) pass. `scripts/ops/alpha/ops_ic_shrinkage.py`'s config flip lives inside the `if passed:` branch only (source-verified + unit-tested). Live DB: `alpha.ensemble.ic_input = 'ic_shrunk'` — the real out-of-fold gate ran against the corpus and passed (mean shrunk error 0.045612 < mean raw error 0.046824, per 03-SUMMARY, consistent with the live config value observed). |
| 6 | D-06 | Leave-one-out peer-group prior keyed on `(group_name, regime, tf)`, correct at n=3 | ✓ VERIFIED | `leave_one_out_group_prior` in `shrinkage.py`; `test_leave_one_out_excludes_self`/`test_leave_one_out_small_group_n_equals_3` pass. `ops_ic_shrinkage.py` buckets on `(group_name, regime, tf)` (confirmed by grep — no naive `AVG(ic...)` self-inclusive average present). |
| 7 | D-07 | `feature_registry.group_name` reused as-is for E1's peer group; no new taxonomy; 2-column schema footprint only | ✓ VERIFIED | Migration 196 adds exactly `ic_shrunk`/`shrinkage_weight` (+ weight_version to `alpha_ensemble_ic`, see item 15 below); no new table; `ops_ic_shrinkage.py` queries `feature_registry` directly. |
| 8 | D-08 | `mean_variance_weights`: `w ∝ Σ⁻¹·IC` via `np.linalg.solve` (never `inv`), condition-number gated, cap/`ic_sign` unchanged, fallback logged | ✓ VERIFIED | `weights.py::mean_variance_weights` uses `np.linalg.solve`; `grep -c np.linalg.inv` = 0. `ensemble_trainer.py::resolve_stratum_weights` reuses `derive_weights` for cap post-MV, skips `cluster_deflate_weights` on success, falls back + `log.warning(...)` on ill-conditioned. 18+15 tests pass (`test_ensemble_mean_variance.py`, `test_ensemble_trainer_weight_method.py`). |
| 9 | D-09 | E3 (hierarchical pooling) / E4 (decay half-lives) explicitly out of scope, not implemented | ✓ VERIFIED | `grep -rn "hierarchical_partial_pooling\|decay_half_life"` across ensemble modules returns nothing — correctly not built. |
| 10 | D-10 | Win rule: `challenger.ic_ci_lower > champion.ic_ci_upper` AND `challenger.walk_forward_stable` | ✓ VERIFIED | `ops_ensemble_weight_compare.py::_evaluate_win_rule` — AND logic; `test_no_win_when_unstable_even_if_ci_non_overlapping` regression-guards the AND (not OR). |
| 11 | D-11 | Per-stratum promotion, no forced single global winner | ✓ VERIFIED | `main()` iterates `set(champion) & set(challenger)` per `(tf, regime)`, reporting one verdict row per stratum. |
| 12 | D-12 | New gate script mirrors EIC-04/EIC-05 pattern, reads `alpha_ensemble_ic` for 2 weight_versions at deterministic per-version latest vintage | ✓ VERIFIED | `_COMPARE_SQL` has a `GROUP BY weight_version` CTE (per-version, not global max); pooled-row filter present; live operational smoke test documented in 05-SUMMARY (graceful "no comparable strata" exit 0 when challenger doesn't exist yet). |
| 13 | D-13 | HMM regime-label look-ahead risk (todo 026/034) carried forward, correctly NOT remediated this phase | ✓ VERIFIED | No regime_writer/HMM refit code touched by this phase's diff (confirmed via file lists in all 5 SUMMARYs); caveat documented, not silently ignored. |
| 14 | D-14 | Every per-stratum row with `regime != '_pooled'` tagged with the HMM look-ahead caveat | ✓ VERIFIED | `_regime_caveat(regime)` helper; `test_regime_caveat_tag_applied_when_regime_not_pooled` passes. |
| 15 | Addendum (load-bearing, not a lettered D-decision) | `alpha_ensemble_ic.weight_version` column threaded end-to-end so two variants' measurements never blend; CR-01/CR-02 blocker-severity gaps in that scoping (crash-loud gate silently zero-row, live-config overwrite by unproven challenger) are fixed | ✓ VERIFIED | Migration 196 Section 3 adds `weight_version NOT NULL` (backfilled `'v1'`); `ensemble_weights`/`ensemble_alpha` already keyed by `weight_version` at the DB level (confirmed live: both PKs include `weight_version`). CR-01/CR-02 fixed in commit `d8b98cfb` (verified in `git log`): `_assert_prerequisites` now takes and filters on `weight_version`; `_calibrate_hold_max_bars` gated on `weight_version == champion`. 2 new regression tests (`TestAssertPrerequisitesWeightVersionScoped`) pass. |

**Score:** 15/15 truths verified (14 lettered decisions D-01..D-14, plus 1 addendum item that is load-bearing for the phase's core "zero schema change for A/B testing" claim).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `production/migrations/196_ensemble_shrinkage_mean_variance.sql` | 2 `feature_ic_scores` columns + 4 APR keys + `alpha_ensemble_ic.weight_version`, idempotent, additive-only | ✓ VERIFIED | 183 lines; `BEGIN`/`COMMIT` wrap; all `ADD COLUMN IF NOT EXISTS`; no `CREATE TABLE`/`create_hypertable`. Applied live (confirmed via live DB query showing populated columns/keys). |
| `services/ensemble_ic_engine.py` | Pooled dispatch (todo 046) + weight_version scoping + CR-01/CR-02 fixes | ✓ VERIFIED | All present, source-verified, unit-tested. |
| `src/intelligence/ensemble/shrinkage.py` | `shrink_ic`, `leave_one_out_group_prior`, pure/DB-free | ✓ VERIFIED | `grep -c "ConfigService\|asyncpg"` = 0. |
| `src/intelligence/ensemble/weights.py` | `mean_variance_weights` alongside `derive_weights`/`cluster_deflate_weights` | ✓ VERIFIED | Exported from `src/intelligence/ensemble/__init__.py` (Plan 04 fixed a missing-export gap same-session). |
| `scripts/ops/alpha/ops_ic_shrinkage.py` | E1 compute pass + hard out-of-fold gate | ✓ VERIFIED | Live-run against real corpus per 03-SUMMARY: 13,567 cells updated, gate PASSED, config flip recorded in `config_history`. |
| `services/ensemble_trainer.py` | `ic_input` toggle (E1) + `weight_method` branch (E2) | ✓ VERIFIED | Both wired; default preserves v1 byte-for-byte (verified via source + tests). |
| `scripts/ops/corpus/ops_corpus_pipeline_run.sh` | `ic_shrinkage` sequenced between `ic_engine` and `ensemble_trainer` | ✓ VERIFIED | Step 5 confirmed via grep; `bash -n` clean per 03-SUMMARY. |
| `scripts/ops/alpha/ops_ensemble_weight_compare.py` | D-12 win-decision gate script | ✓ VERIFIED | Mirrors EIC-04/05 skeleton; live smoke-tested. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `ensemble_ic_engine.py` | `build_ensemble_ic_row(symbol='POOLED')` | `_POOLED_SYMBOL` sentinel | ✓ WIRED | Grep-confirmed; live rows satisfy `alpha_ensemble_ic_pooled_symbol_consistent` CHECK. |
| `ops_ic_shrinkage.py` | `feature_ic_scores.ic_shrunk`/`shrinkage_weight` | `bulk_update_by_key` on 6-col PK | ✓ WIRED | Live: 13,567 rows populated, zero NaN/Inf. |
| `ops_ic_shrinkage.py` | `shrinkage.py::shrink_ic`/`leave_one_out_group_prior` | direct import | ✓ WIRED | Confirmed by grep + passing tests. |
| `ensemble_trainer.py::_process_stratum` | `ic_shrunk` column | `alpha.ensemble.ic_input` APR toggle | ✓ WIRED | Live: `ic_input='ic_shrunk'` in `config_state`; SELECT/WHERE/remap all present. |
| `ensemble_trainer.py::_process_stratum` | `mean_variance_weights` | `resolve_stratum_weights`, gated on `config.weight_method` | ✓ WIRED | `weight_method` currently `'ic_proportional'` live (E2 not yet promoted — correct, since D-10's win rule hasn't been run/passed for E2 yet; this is expected phase-end state, not a gap). |
| `ops_ensemble_weight_compare.py` | `alpha_ensemble_ic` (2 weight_versions) | `GROUP BY weight_version` CTE | ✓ WIRED | SQL-text verified; live smoke test in 05-SUMMARY. |
| `ensemble_ic_engine.py::_assert_prerequisites` | resolved `weight_version` | CR-01 fix (`d8b98cfb`) | ✓ WIRED | Regression test confirms scoped COUNT; no other caller uses the old 1-arg signature. |
| `ensemble_ic_engine.py::_calibrate_hold_max_bars` | champion `weight_version` guard | CR-02 fix (`d8b98cfb`) | ✓ WIRED | Guard present at call site; skip path logs a structured record. |

### Behavioral Spot-Checks / Operational Verification

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Phase-scoped unit suite | `pytest tests/unit/test_ensemble_ic_pooled_dispatch.py test_ensemble_shrinkage.py test_ensemble_mean_variance.py test_ic_shrinkage_step.py test_ensemble_trainer_ic_input.py test_ensemble_trainer_weight_method.py test_ensemble_weight_compare.py test_ensemble_ic_idempotency.py test_ensemble_ic_worker_fetch.py -q` | 94 passed | ✓ PASS |
| Live DB: pooled v1 baseline exists | `SELECT * FROM alpha_ensemble_ic WHERE symbol='POOLED'` | 16 rows, `weight_version='v1'`, `tf='5m'`, `regime='high_bear'` | ✓ PASS |
| Live DB: `ic_shrunk` populated | `SELECT count(*) FROM feature_ic_scores WHERE ic_shrunk IS NOT NULL` | 13,567 | ✓ PASS |
| Live DB: E1 gate result reflected in config | `alpha.ensemble.ic_input` in `config_state` | `'ic_shrunk'` (flipped from default `'ic_sharpe_hac'`) | ✓ PASS |
| Live DB: `ensemble_weights`/`ensemble_alpha` PK includes `weight_version` (zero-schema-change A/B claim) | `\d ensemble_weights`, `\d ensemble_alpha` | Both PKs include `weight_version` | ✓ PASS |
| CR-01/CR-02 fix commit present and isolated | `git show d8b98cfb`, `grep -rn _assert_prerequisites` | Confirmed on `services/ensemble_ic_engine.py` only; no stale callers | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| REQ-142B1-D01 | 01 | Pooled dispatch | ✓ SATISFIED | Truth #1 |
| REQ-142B1-D02 | 01 | v1 baseline | ✓ SATISFIED | Truth #2 |
| REQ-142B1-D07 | 01 | Schema footprint / group_name reuse | ✓ SATISFIED | Truth #7 |
| REQ-142B1-D08-APR | 01 | APR keys for E2 | ✓ SATISFIED | Migration 196 keys 2-4 |
| REQ-142B1-D05-MATH | 02 | shrink_ic math | ✓ SATISFIED | Truth #5 |
| REQ-142B1-D06 | 02, 03 | Leave-one-out prior | ✓ SATISFIED | Truth #6 |
| REQ-142B1-D08-MATH | 02 | mean_variance_weights math | ✓ SATISFIED | Truth #8 |
| REQ-142B1-D04 | 03 | E1 before E2 sequencing | ✓ SATISFIED | Truth #4 |
| REQ-142B1-D05 | 03 | Hard out-of-fold gate | ✓ SATISFIED | Truth #5 |
| REQ-142B1-D05-GATE | 03 | Gate wiring to ic_input flip | ✓ SATISFIED | Truth #5 |
| REQ-142B1-D08 | 04 | E2 branch wiring | ✓ SATISFIED | Truth #8 |
| REQ-142B1-D08-FALLBACK | 04 | Condition-number fallback | ✓ SATISFIED | Truth #8 |
| REQ-142B1-D10 | 05 | Win rule | ✓ SATISFIED | Truth #10 |
| REQ-142B1-D11 | 05 | Per-stratum promotion | ✓ SATISFIED | Truth #11 |
| REQ-142B1-D12 | 05 | Comparison gate script | ✓ SATISFIED | Truth #12 |
| REQ-142B1-D14 | 05 | Regime caveat tagging | ✓ SATISFIED | Truth #14 |

**Not assigned a dedicated REQ-ID in any plan's frontmatter (traceability gap, not a functional gap):**
D-03 (per-symbol retention), D-09 (E3/E4 scope exclusion), D-13 (HMM caveat carry-forward) have no
`REQ-142B1-D03`/`D09`/`D13` tokens in any plan frontmatter. All three are nonetheless directly
verified above (Truths #3, #9, #13) — D-03 was folded into Plan 01's must_haves truths without a
matching requirements-field entry; D-09/D-13 are "correctly do nothing" decisions that don't need
code, so their absence from the requirements list is not a functional omission, only a
documentation-traceability gap. Not blocking.

### Anti-Patterns Found

No blocker-severity anti-patterns. `grep -n "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER"` across every
file touched by this phase (migration 196, `ensemble_ic_engine.py`, `ensemble_trainer.py`,
`shrinkage.py`, `weights.py`, `ops_ic_shrinkage.py`, `ops_ensemble_weight_compare.py`) returns
zero matches.

**Carried-forward code-review findings (142B.1-REVIEW.md), not fixed, assessed for blocking severity:**

| Finding | Severity (review) | Blocks phase goal? | Rationale |
|---|---|---|---|
| WR-01: shrinkage peer-group bucketing omits `lookahead_bars` | Warning | No | The out-of-fold hard gate (D-05) already empirically PASSED against the real corpus with this exact bucketing — the mechanism is validated as-implemented. Worth tightening later, not a demonstrated correctness defect today. |
| WR-02: `alpha_ensemble_ic` PK not extended to include `weight_version` | Warning | No | Not exploitable under the current single-invocation-at-a-time operational model (confirmed: no concurrent-run pattern exists in this codebase's batch scripts). Latent design gap, legitimate follow-up. |
| WR-03: duplicated `_effective_weight_version` helper | Warning | No | Pure DRY/maintainability issue, zero behavioral risk. |
| WR-04: unescaped SQL column interpolation missing safety comment | Warning | No | Feature names originate from `feature_registry` (a controlled, CHECK-constrained internal table), not external input — documentation gap, not an injection vector today. |
| WR-05: migration 196 `config_history` inserts not idempotent | Warning | No | Only pollutes audit trail on a manual re-run of an already-applied migration; does not corrupt `config_state` (which is correctly idempotent). |
| IN-01/IN-02: naive column parsing / manifest name collision | Info | No | Cosmetic/observability only. |

**Recommendation:** These 5 warnings + 2 info items are legitimate deferred follow-up work, not
phase-goal blockers. They should be captured as todos (per this project's "capture todos
immediately" convention) rather than gating this verification. This verifier does not create
todo files; that is the developer's/orchestrator's action item.

### Human Verification Required

None. This phase is entirely internal batch/statistical compute with no user-facing surface
(confirmed by every plan's own threat model: "no user-facing input surface, no auth/session
boundary"). All must-haves were verifiable via source inspection, unit tests, and live database
queries against the already-applied migration and already-run corpus pipeline steps.

### Gaps Summary

No gaps block phase goal achievement. All 14 lettered decisions (D-01..D-14) plus the
load-bearing `weight_version` threading addendum are implemented, wired, and verified live
against the production `indicagent` database — not merely unit-tested in isolation. The two
blocker-severity code-review findings (CR-01, CR-02) were caught and fixed in the same session
(commit `d8b98cfb`) before this verification ran, with their own regression tests passing. The 5
warning + 2 info findings remaining in 142B.1-REVIEW.md are legitimate deferred hygiene/latent-
design-gap items, assessed above and none rise to blocking severity.

E2 (`mean_variance`) has not yet been promoted to live `weight_method` — this is expected end-
of-phase state, not a gap: the phase built the E1/E2/A-B-judge machinery per D-04 through D-12,
and D-10's win-decision rule (via `ops_ensemble_weight_compare.py`) is what determines promotion
per-stratum going forward, which is an operational/ongoing activity outside this phase's build
scope.

---

_Verified: 2026-07-04_
_Verifier: Claude (gsd-verifier)_
