# Phase 142B.1: Ensemble Weighting Methodology - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-02
**Phase:** 142B.1-ensemble-weighting-methodology-replace-ensemble-trainer-py-s
**Areas discussed:** Judge readiness, E1 shrinkage scope, Win decision rule, Regime-stratified caveat

---

## Judge Readiness

| Question | Selected |
|---|---|
| Build todo 046's pooled cross-sectional pass as Wave 0? | ✓ Yes, Wave 0 of this phase |
| Re-run ensemble_ic_engine.py to establish v1 baseline first? | ✓ Yes, baseline v1 first |
| Role of per-symbol IC once pooled exists? | ✓ Keep as diagnostic only |

**Notes:** Third question's first response was the user's standing "think like Renaissance" directive rather than a selection; resolved by applying the project's existing Renaissance data-retention principle (never drop data that could contain signal) — per-symbol IC stays as a diagnostic layer under the pooled gate.

---

## E1 Shrinkage Scope

| Question | Selected |
|---|---|
| Build shrinkage estimator in 142B.1, or defer to E2-on-raw-IC first? | ✓ Build shrinkage in 142B.1 (recommended) |
| Treatment of the out-of-fold acceptance test? | ✓ Hard gate before ensemble consumes ic_shrunk |
| Feature-family taxonomy for shrinkage prior — reuse or build new? | ✓ Reuse feature_registry.group_name as-is |

**Notes:** User paused before the third question to ask a clarifying meta-question: "Do we need a lot more granular tags for this? can we reuse tags? ... same containers with additional taxonomy granularity?" Investigation confirmed `feature_registry.group_name` (10 groups, 3–11 features each, all active) already exists and fits exactly — no new taxonomy needed. Follow-up clarification: user asked whether `regime_group` (Phase 151) was "a similar concept with slightly different granularity" to `feature_registry.group_name`. Investigation of `docs/plans/2026-07-01-cross-sectional-regime-model.md` and `docs/foundation/glossary.md` confirmed these are two separate, non-overlapping constructs over different entity types (instruments vs. feature columns) — `tag_vocabulary`/`instrument_tags` classify securities (falsifiable claims), `regime_group` derives from `instrument_tags`, and `feature_registry.group_name` is an unrelated definitional feature-column category. Further clarification requested: "are we making more tables which might be noise or using existing structures?" — confirmed zero new tables anywhere in this phase (E1 adds 2 columns to the existing `feature_ic_scores`; Wave 0 activates an already-existing-but-dead code path in `alpha_ensemble_ic`). Final clarification on "dead code/tables" — confirmed this referred narrowly to todo 046's already-documented pooled-dispatch gap, already covered by the Wave 0 decision, not a broader cleanup task.

---

## Win Decision Rule

| Question | Selected |
|---|---|
| Numeric win rule per stratum? | ✓ Non-overlapping CI + stability gate (challenger.ic_ci_lower > champion.ic_ci_upper AND walk_forward_stable) |
| Handling mixed per-stratum results? | ✓ Per-stratum promotion (no forced single global winner) |
| Where does comparison logic live? | ✓ New gate script mirroring EIC-04/EIC-05 pattern |

**Notes:** All three recommended options accepted without modification.

---

## Regime-Stratified Caveat

| Question | Selected |
|---|---|
| Does the HMM look-ahead risk (todo 026/034) block per-regime promotion? | ✓ No, proceed — caveat carried forward |
| Should the comparison report flag regime-stratified results specially? | ✓ Yes, label with caveat note when regime != '_pooled' |

**Notes:** Both recommended options accepted.

---

## Claude's Discretion

- Exact migration numbering/file names for new columns and APR keys
- Internal structure of the new comparison gate script beyond "mirrors EIC-04/EIC-05, reads alpha_ensemble_ic, writes structured report"
- Exact wave/plan breakdown mapping to PLAN.md files

## Deferred Ideas

- todo 026/034 (HMM regime-label look-ahead bias) — reviewed, not remediated this phase, caveat carried forward
- todo 040/041 (instrument tag calibrator / vocabulary audit) — reviewed during taxonomy discussion, confirmed unrelated axis, not folded
- todo 043 (ensemble stale-weight cliff APR) — discovered already complete (migration 193); noted so planner doesn't redo it
