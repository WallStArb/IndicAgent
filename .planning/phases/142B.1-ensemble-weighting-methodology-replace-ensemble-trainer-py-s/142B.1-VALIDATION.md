---
phase: 142B.1
slug: ensemble-weighting-methodology-replace-ensemble-trainer-py-s
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-07-02
---

# Phase 142B.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (project standard, `.venv/bin/pytest`) |
| **Config file** | project-root pytest config (existing, not re-verified this run) |
| **Quick run command** | `.venv/bin/pytest tests/unit/test_ensemble_shrinkage.py tests/unit/test_ensemble_mean_variance.py -v` |
| **Full suite command** | `.venv/bin/pytest tests/unit/ -q` |
| **Estimated runtime** | ~60-120 seconds (full unit suite, per project convention) |

---

## Sampling Rate

- **After every task commit:** Run the specific new test file for that task (`.venv/bin/pytest tests/unit/test_ensemble_<X>.py -x`)
- **After every plan wave:** Run `.venv/bin/pytest tests/unit/ -q`
- **Before `/gsd:verify-work`:** Full suite must be green, plus a real (not mocked) run of the new `ops_ensemble_weight_compare.py` script against actual `alpha_ensemble_ic` data as an operational smoke test (mirrors how EIC-04/EIC-05 were validated in 142A)
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

No formal REQUIREMENTS.md phase-ID list exists for 142B.1 — requirements are tracked via CONTEXT.md's D-01..D-14 decisions. Planner converts these into REQ-IDs at plan-breakdown time; the table below is the decision → test map the planner must preserve.

| Decision | Behavior | Test Type | Automated Command | File Exists | Status |
|----------|----------|-----------|-------------------|-------------|--------|
| D-01 | Pooled cross-sectional pass produces `symbol='POOLED'` rows in `alpha_ensemble_ic` | unit + integration | `pytest tests/unit/test_ensemble_ic_pooled_dispatch.py -x` | ❌ W0 | ⬜ pending |
| D-02 | Baseline v1 measurement exists and is reproducible | integration/manual | Re-run `ensemble_ic_engine.py`, verify `alpha_ensemble_ic` rows via `ensemble_alpha` join | ❌ W0 | ⬜ pending |
| D-05 | Shrinkage formula correctness + degenerate-`n_eff` handling + out-of-fold acceptance test | unit | `pytest tests/unit/test_ensemble_shrinkage.py -x` | ❌ E1 | ⬜ pending |
| D-05 (hard gate) | Out-of-fold acceptance test against real corpus data | integration | New oneshot / extension of `ops_ensemble_ic_diagnosis.py`-style report | ❌ E1 | ⬜ pending |
| D-06 | Leave-one-out peer-group prior uses `feature_registry.group_name` correctly, including n=3 groups | unit | `pytest tests/unit/test_ensemble_shrinkage.py::test_leave_one_out_small_group -x` | ❌ E1 | ⬜ pending |
| D-08 | `Σ⁻¹·IC` math correctness, condition-number gate, cap/sign unchanged | unit | `pytest tests/unit/test_ensemble_mean_variance.py -x` | ❌ E2 | ⬜ pending |
| D-10/D-11 | Win-decision rule (non-overlapping CI + walk_forward_stable), per-stratum promotion | unit | `pytest tests/unit/test_ensemble_weight_compare.py -x` | ❌ D-12 | ⬜ pending |
| D-14 | Regime-caveat tagging in comparison report | unit | `pytest tests/unit/test_ensemble_weight_compare.py::test_regime_caveat_tag -x` | ❌ D-12 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/unit/test_ensemble_ic_pooled_dispatch.py` — covers D-01 (todo 046 fix)
- [ ] `tests/unit/test_ensemble_shrinkage.py` — covers D-05, D-06 (new module `src/intelligence/ensemble/shrinkage.py`)
- [ ] `tests/unit/test_ensemble_mean_variance.py` — covers D-08 (new function in `src/intelligence/ensemble/weights.py`)
- [ ] `tests/unit/test_ensemble_weight_compare.py` — covers D-10, D-11, D-14 (new script `scripts/ops/alpha/ops_ensemble_weight_compare.py`)
- [ ] No new test framework/config needed — existing pytest infrastructure (`tests/unit/test_ensemble_*.py`, `test_ic_engine_*.py`) already establishes fixture style and mocking conventions (pure functions fed dicts/arrays directly, no DB mocking needed)

---

## Manual-Only Verifications

*None — all phase behaviors have automated verification. This phase has no user-facing input surface, no auth/session boundary, and processes no untrusted external input; it is internal batch statistical computation over already-validated, internally-produced data (`alpha_events`, `forward_returns`, `market_regimes`, `feature_ic_scores`). The only "threat" analog is data-integrity risk (a math bug silently producing wrong weights), addressed by the hard out-of-fold gate (D-05) and the unit tests above, not by ASVS-style security controls.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 120s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-07-02 (gsd-plan-checker verification pass — 5/5 plans, no blockers)
