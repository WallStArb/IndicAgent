---
context: phase
phase: 139-ensemble-alpha-emission
task: null
total_tasks: null
status: pre-execution
last_updated: 2026-06-23T23:39:17Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through analysis of live data.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Broken feature primitives — 12 features in feature_vectors return constant values (std=0.000). Do NOT run the full 58-symbol backfill or re-run IC engine until these are fixed. Proceeding produces misleading IC scores and a corrupted corpus.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Running backfill with broken features | 12 primitives return constant placeholder values; IC engine returns null for all of them | blocking | Fix FeatureFactory data feeding for batch path before running `--compute-only` on remaining 157 pairs |
| Assuming live path == batch path | FeatureCache.update_cross_asset() is called with real-time bars in live path but batch path may not provide cross-asset or higher-TF bar data | advisory | Read backfill_feature_factory.py FeatureCache initialization before assuming features compute correctly |

<current_state>
Phase 138 (IC Engine) is complete. P8 ran today — 4-symbol corpus (SPY, QQQ, TLT, XLF) is in feature_vectors, forward_returns, and feature_ic_scores. IC discovery report generated. Phase 139 plans exist but have not started execution.

BLOCKER: 12 feature primitives return constant values in feature_vectors. Must fix before full 58-symbol backfill.
</current_state>

<completed_work>

- Phase 138 P8: 4-symbol IC pipeline complete
  - feature_vectors: 16 (symbol,tf) pairs populated, 1.4M+ rows total
  - forward_returns: perfect 1:1 match with feature_vectors
  - feature_ic_scores: 12,444 cells computed; 683 regime-stratified pass walk-forward; 50 pooled pass both gates
  - IC discovery report: docs/analysis/ic-discovery-report.md + .json
  - Top IC: in_overlap QQQ/1h/trending_up IC=0.24, IC Sharpe=1.014
- Migration 168: dropped redundant forward_returns index, added feature_ic_scores temporal ordering index
- backfill_status stale error_msg cleared (VTV/VUG stale UUID errors from pre-fix run)
- Bar data confirmed gap-free (market_data_gaps empty, LAG() check shows 0 non-standard gaps across all symbols)
</completed_work>

<remaining_work>

**Before full backfill:**
- Fix 12 broken feature primitives (see blocker above)
  - Cross-asset: vix_z, yield_slope_z, flight_quality — constant 0.000
  - CTF: ctf_momentum, ctf_vwap_align, ctf_regime_align — constant 0.000
  - Volume profile: va_position, poc_dist_atr — constant 0.500
  - HMM prob: hmm_regime_prob, hmm_entropy — constant 1.000/0.000
  - SR: sr_support_dist, sr_resist_dist — likely constant

**After fix:**
- Re-run backfill_feature_factory --compute-only for remaining 157 pending pairs (46 symbols)
  - All fetch_complete=true, no IBKR connection needed
  - 16 complete pairs (SPY/QQQ/TLT/XLF × 4 TFs) will be skipped automatically
- Re-run IC engine on full 58-symbol corpus
- Execute Phase 139 (ensemble alpha emission)
  - 139-P1-PLAN.md, 139-P2-PLAN.md, 139-P3-PLAN.md exist and are ready
</remaining_work>

<decisions_made>

- Proceed to Phase 139 planning with 4-symbol IC corpus; don't block on 58-symbol run (4-symbol has sufficient statistical power for ensemble construction)
- Fix broken features before full backfill — running 157 more pairs with placeholder values wastes compute
- bar_auditor is NOT v2.x-only infrastructure — it's core bar data gap detection that must run alongside any live bar pipeline (v3.0 included)
- v3.0 is new intelligence only — bar data infrastructure (bar_auditor, bar_aggregator, IBKR provider) is unchanged
- Shadow promotion in v3.0 uses rolling IC decay (alpha_decay_monitor) not binary shadow_registry gate — schema already has is_decaying, decay_detected_at, recovery_eligible_at on feature_ic_scores
</decisions_made>

<blockers>

- **Broken feature primitives (MUST FIX)**: 12 features have std=0.000 in feature_vectors. Root cause: FeatureFactory batch path (backfill) not feeding cross-asset bars, higher-TF bars, or volume profile data to FeatureCache. Live path works because it receives real-time bars. Need to investigate how batch path initializes FeatureCache and whether it calls update_cross_asset() / builds session volume profile.
</blockers>

## Required Reading (in order)

1. `services/backfill_feature_factory.py` — how FeatureCache is initialized in batch path, specifically cross-asset and CTF data feeding
2. `src/intelligence/feature_factory.py` — how cross-asset, CTF, and volume profile features are computed; what FeatureCache state they need
3. `.planning/phases/139-ensemble-alpha-emission/139-RESEARCH.md` — Phase 139 research
4. `docs/analysis/ic-discovery-report.md` — IC results summary; passing features for ensemble

## Infrastructure State

- Live pipeline: NOT running (indicagent-intelligence-pipeline inactive)
- bar_auditor: inactive (expected — no live bar stream)
- backfill_status: 16 complete, 157 pending (all fetch_complete=true)
- feature_vectors: 4 symbols × 4 TFs populated (~1.4M rows)
- forward_returns: 4 symbols × 4 TFs, perfect coverage
- feature_ic_scores: ~12,444 rows, IC discovery report generated
- market_data_gaps: empty (no gaps in OHLCV data)
- DB: migration 168 applied (latest)

<context>
The 4-symbol P8 run completed successfully today. The IC methodology is working — regime-stratified IC is 5-10x stronger than pooled, top features hit IC Sharpe >1.0. The system is architecturally sound. The main problem discovered this session is that 12 feature primitives are broken at the data-feeding layer in the batch path — they compute but return constants because the FeatureCache isn't receiving cross-asset/CTF/volume profile data during batch compute.

Phase 139 plans are ready. The decision was made to not block Phase 139 on the full 58-symbol backfill — 4 symbols is enough to build the ensemble. Fix broken features, then either run the full backfill in parallel or after Phase 139 begins.
</context>

<next_action>
Start by reading services/backfill_feature_factory.py to find how FeatureCache is initialized and whether update_cross_asset() is called with cross-asset bar data (VIX, yield curve, SPY/TLT/SHY). Then read src/intelligence/feature_factory.py to understand what FeatureCache state cross-asset, CTF, and volume profile features need. Fix the data feeding, re-run the 4-symbol compute to validate, then proceed to Phase 139.
</next_action>
