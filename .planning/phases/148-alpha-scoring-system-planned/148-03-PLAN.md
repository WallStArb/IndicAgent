---
phase: 148
plan: 03
type: execute
wave: 2
depends_on: [148-01]
files_modified:
  - scripts/ops/corpus/ops_oos_gate1_signal_eval.py
  - tests/unit/test_oos_gate1_signal_eval.py
autonomous: true
requirements: [SCORE-02]

must_haves:
  truths:
    - "Gate 1 scorer reads ensemble_alpha (not alpha_events) on the OOS side (bar_ts >= oos_start)"
    - "Gate 1 scorer uses _fisher_z_ci methodology, matching the in-sample ensemble_ic_engine baseline, not the newer circular-block bootstrap"
    - "Gate 1 scorer fails loud (raises) when alpha.validation.oos_start is unset"
    - "Gate 1 verdict is written to gate_evaluations, never into alpha_ensemble_ic (D-03: standalone script, not a new EnsembleICEngine mode, because alpha_ensemble_ic has no in-sample/OOS distinguishing column)"
    - "Gate 1 scorer runs at most once for this milestone gate (D-04); it is not a re-runnable smoke test"
  artifacts:
    - path: "scripts/ops/corpus/ops_oos_gate1_signal_eval.py"
      provides: "Standalone one-shot OOS Gate 1 (signal proof) scorer"
      contains: "gate1_signal"
      min_lines: 80
    - path: "tests/unit/test_oos_gate1_signal_eval.py"
      provides: "GREEN SCORE-02 unit tests (oos_start fail-loud + methodology)"
  key_links:
    - from: "ops_oos_gate1_signal_eval.py"
      to: "src.intelligence.statistics.ic_math._fisher_z_ci"
      via: "import (Fisher-z CI, methodology-matched to ensemble_ic_engine)"
      pattern: "_fisher_z_ci"
    - from: "ops_oos_gate1_signal_eval.py"
      to: "gate_evaluations"
      via: "single-row INSERT of verdict + evidence"
      pattern: "INSERT INTO gate_evaluations"
---

<objective>
Build the SCORE-02 OOS Gate 1 (signal proof) scorer: a standalone one-shot script that
measures whether alpha_score predicts forward returns out-of-sample. It is structurally a
sibling of ops_oos_holdout_eval.py but scores ensemble_alpha via ensemble_ic_engine.py's
EXACT IC helper set (Fisher-z, not the newer circular-block bootstrap) so its OOS numbers are
apples-to-apples with the single existing in-sample alpha_ensemble_ic row. This plan BUILDS
and tests the script; the actual one-shot run is Wave 2's deliverable in plan 148-05.

Purpose: Gate 1 answers "is there real signal at all," the foundational question this whole
measurement pipeline exists to settle. Output: the script plus GREEN unit tests.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/148-alpha-scoring-system-planned/148-CONTEXT.md
@.planning/phases/148-alpha-scoring-system-planned/148-RESEARCH.md
@.planning/phases/148-alpha-scoring-system-planned/148-PATTERNS.md

<interfaces>
Reuse targets (import, do not reimplement):

From src/intelligence/statistics/ic_math.py:
  _fisher_z_ci, _vectorized_ic, _p_values_from_ic, _nan_to_none

From services/ensemble_ic_engine.py:
  compute_walk_forward_stable(...)
  _WORKER_FETCH_SQL (lines 516-532) - the in-sample ensemble_alpha JOIN; SCORE-02 copies this
    verbatim EXCEPT `ea.bar_ts < %s` becomes `ea.bar_ts >= %s`. It already contains
    `fr.return_type = 'executable_open_to_open'` and the market_regimes join (resolves
    RESEARCH.md Open Question 1: the executable-returns filter is explicit in this query).

From services/_batch_utils.py:
  load_apr_dict_async, cfg
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ops_oos_gate1_signal_eval.py and turn test_oos_gate1_signal_eval.py GREEN</name>
  <files>scripts/ops/corpus/ops_oos_gate1_signal_eval.py, tests/unit/test_oos_gate1_signal_eval.py</files>
  <read_first>
    - scripts/ops/corpus/ops_oos_holdout_eval.py (full structural template, 431 lines: imports, _read_oos_start fail-loud lines 114-130, _apply_corpus_fdr lines 242-250, _append_look_log lines 323-347, argparse main lines 355-368)
    - services/ensemble_ic_engine.py (_WORKER_FETCH_SQL lines 516-532, import list, compute_walk_forward_stable, fail-loud oos_start lines 952-967)
    - src/intelligence/statistics/ic_math.py (_fisher_z_ci, _vectorized_ic, _p_values_from_ic, _nan_to_none)
    - tests/unit/test_oos_gate1_signal_eval.py (the Wave 0 stub to fill)
    - tests/unit/test_oos_holdout_eval.py (test shape for the sibling scorer)
    - docs/plans/OOS-EVAL-PROTOCOL.md (run-once cadence, fail-loud-on-unset-boundary rule)
  </read_first>
  <behavior>
    - Fail-loud: if alpha.validation.oos_start is unset/empty in config_state, raise RuntimeError; never default to MAX(bar_ts) or silently score the whole corpus.
    - Methodology lock: import and use _fisher_z_ci for the CI; the script must NOT import or call circular_block_bootstrap_ic_serial (RESEARCH.md Pitfall 1). This matches the one existing in-sample alpha_ensemble_ic row so OOS vs in-sample stays comparable.
    - Population lock: read ensemble_alpha, NOT alpha_events (RESEARCH.md Pitfall 2 - alpha_events is the post-emission subset and introduces post-selection bias).
    - OOS side: filter ea.bar_ts >= oos_start.
    - One corpus-wide BH-FDR correction across all cells.
    - Verdict + evidence written to gate_evaluations (gate_id 'gate1_signal'); NOTHING written to alpha_ensemble_ic (that table has no in-sample/OOS distinguishing column, D-03).
  </behavior>
  <action>
    Create scripts/ops/corpus/ops_oos_gate1_signal_eval.py mirroring ops_oos_holdout_eval.py's structure. Import _fisher_z_ci, _vectorized_ic, _p_values_from_ic, _nan_to_none from src.intelligence.statistics.ic_math and compute_walk_forward_stable from services.ensemble_ic_engine. Do NOT import ic_engine.py's circular-block-bootstrap helpers.

    Copy ensemble_ic_engine.py's _WORKER_FETCH_SQL verbatim but change the single predicate `ea.bar_ts < %s` to `ea.bar_ts >= %s`. Keep the table as ensemble_alpha, keep the `fr.return_type = 'executable_open_to_open'` join filter (do not add a second filter, do not remove it), keep the market_regimes join shape.

    Implement a fail-loud _read_oos_start(pool) that reads alpha.validation.oos_start from config_state and raises RuntimeError if NULL/empty (copy ops_oos_holdout_eval.py lines 114-130 near-verbatim). Read the alpha.scoring.* APR via load_apr_dict_async + cfg (do not hardcode thresholds).

    Compute rank-IC + Fisher-z CI + p-values per (symbol, tf, lookahead) cell, apply ONE BH-FDR correction across all cells via multipletests(method="fdr_bh"), compute walk_forward_stable per cell (expect a meaningful fraction to return None/insufficient-N on the shorter OOS window - that is diagnostic per OOS-EVAL-PROTOCOL, not a bug; report it, do not lower min_reliable_n).

    Assemble a verdict and write ONE row to gate_evaluations: gate_id='gate1_signal', result ('pass'|'fail'|'insufficient'), evidence jsonb capturing per-cell IC/CI/p/walk-forward summary and the oos_start boundary used. Append one entry to a look-log at .planning/gate_look_log.jsonl (run_ts, symbols, tfs, report path + sha256) mirroring _append_look_log - this operationalizes D-04's run-once discipline as an auditable trail.

    argparse --symbols/--tf validated against get_active_contracts(settings); parameterized queries only. `except Exception as error:`, `datetime.now(UTC)`. Module docstring must state: run at most once per milestone gate (D-04); do not re-run to check if it passes.

    Fill tests/unit/test_oos_gate1_signal_eval.py: test_fails_loud_when_oos_start_unset (monkeypatch/stub config_state to unset, assert RuntimeError) and test_uses_fisher_z_ci_methodology (assert the module imports _fisher_z_ci and does not reference circular_block_bootstrap - source-level or import-level assertion).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_oos_gate1_signal_eval.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_fisher_z_ci" scripts/ops/corpus/ops_oos_gate1_signal_eval.py` >= 1
    - `grep -c "circular_block_bootstrap" scripts/ops/corpus/ops_oos_gate1_signal_eval.py` == 0
    - `grep -c "ensemble_alpha" scripts/ops/corpus/ops_oos_gate1_signal_eval.py` >= 1 and `grep -c "alpha_events" scripts/ops/corpus/ops_oos_gate1_signal_eval.py` == 0
    - `grep -c "return_type = 'executable_open_to_open'" scripts/ops/corpus/ops_oos_gate1_signal_eval.py` >= 1
    - `grep -Eq "ea.bar_ts >=" scripts/ops/corpus/ops_oos_gate1_signal_eval.py`
    - `grep -c "INSERT INTO alpha_ensemble_ic" scripts/ops/corpus/ops_oos_gate1_signal_eval.py` == 0
    - `grep -c "gate1_signal" scripts/ops/corpus/ops_oos_gate1_signal_eval.py` >= 1
    - `grep -c "as exc" scripts/ops/corpus/ops_oos_gate1_signal_eval.py` == 0
    - `.venv/bin/pytest tests/unit/test_oos_gate1_signal_eval.py -x` passes (oos_start + methodology tests GREEN)
  </acceptance_criteria>
  <done>SCORE-02 script built with Fisher-z methodology, ensemble_alpha OOS read, fail-loud oos_start, and gate_evaluations write path; tests GREEN. Not yet run against live data (that is plan 148-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI -> gate script -> DB | --symbols/--tf args cross into SQL; oos_start boundary governs gate integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-01 | Tampering | --symbols/--tf CLI args | mitigate | Validate against get_active_contracts(settings); parameterized asyncpg queries only, never string interpolation |
| T-148-02 | Tampering / Repudiation | alpha.validation.oos_start boundary | mitigate | Fail-loud _read_oos_start raises on unset/empty; never default to MAX(bar_ts). An unset boundary silently scoring the whole corpus would forge the gate's OOS integrity |
| T-148-03 | Repudiation | run-once cadence (D-04) | mitigate | Append-only .planning/gate_look_log.jsonl with sha256 makes every look auditable |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_oos_gate1_signal_eval.py -x` GREEN.
- Grep gates above all satisfied (Fisher-z present, circular-block absent, ensemble_alpha not alpha_events, no alpha_ensemble_ic write).
- `.venv/bin/pytest tests/unit/ -q` full suite green.
</verification>

<success_criteria>
Standalone OOS Gate 1 scorer exists, methodology-matched to ensemble_ic_engine (Fisher-z),
reads ensemble_alpha OOS side, fails loud on unset oos_start, writes to gate_evaluations only;
unit tests GREEN. Not yet executed.
</success_criteria>

<output>
Create `.planning/phases/148-alpha-scoring-system-planned/148-03-SUMMARY.md` when done.
</output>
