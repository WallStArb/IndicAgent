---
phase: 73-ai-llm-layer-b-architecture-refactor
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - scripts/profile_gemma4_latency.py
  - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-LATENCY-PROFILE.md
autonomous: true
requirements: [LATENCY-BASELINE]
must_haves:
  truths:
    - "scripts/profile_gemma4_latency.py exists and executes against Ollama gemma4:e4b on production hardware"
    - "P50, P95, P99 latency captured at 64-token (alpha) and 512-token (narrative) workloads"
    - "Recommended latency_budget_ms values written to .planning/phases/73-.../73-LATENCY-PROFILE.md (2× P95 rule)"
    - "Plan 06+ agents reference these measured values, not hardcoded guesses"
  artifacts:
    - path: "scripts/profile_gemma4_latency.py"
      provides: "Reproducible latency profiler for gemma4:e4b"
      min_lines: 50
    - path: ".planning/phases/73-ai-llm-layer-b-architecture-refactor/73-LATENCY-PROFILE.md"
      provides: "Recorded P50/P95/P99 + recommended latency_budget_ms"
      contains: "latency_budget_ms"
  key_links:
    - from: "73-LATENCY-PROFILE.md"
      to: "Plan 06 (alpha agents) and Plan 07 (narrative agent)"
      via: "latency_budget_ms = 2× measured P95"
      pattern: "latency_budget_ms"
---

<objective>
Profile Ollama `gemma4:e4b` latency on production hardware (192.168.68.53, AMD ROCm iGPU) at the two real workload shapes: 64-token alpha JSON responses and 512-token narrative prose. Record P50/P95/P99. Compute recommended `latency_budget_ms` (2× measured P95). Plan 06 (alpha agents) and Plan 07 (narrative agent) read this file when setting class attributes.

Per CONTEXT.md locked decision #4: "latency_budget_ms defaults to 5000ms (safe ceiling) until measured; alpha target 3000ms, narrative 60000ms — to be confirmed."

Purpose: Eliminate guessing. Renaissance principle: "instrument everything." Measure before hardcoding.
Output: Profile script (re-runnable for future hardware changes) + recorded measurements + recommended budgets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-CONTEXT.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-AI-SPEC.md
@src/core/llm/providers.py
@src/config/settings.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and run gemma4:e4b latency profiler; record measurements</name>
  <files>
    scripts/profile_gemma4_latency.py
    .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-LATENCY-PROFILE.md
  </files>
  <read_first>
    - src/core/llm/providers.py (OllamaProvider class — get exact API URL/payload shape; do not invent)
    - src/config/settings.py (Settings.ollama_base_url, default model name)
    - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-AI-SPEC.md (Section 4 "Implementation Guidance" — Ollama timeout 60s narrative, 3-5s alpha; Section 4b.5 "Cost and Latency Budget" — Ollama baseline 800ms@64tok, 4-8s@512tok)
    - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-CONTEXT.md (locked decision #4)
    - CLAUDE.md (asyncio rules, structured logging)
    - Verify Ollama is reachable: `curl -s http://localhost:11434/api/tags | head -5`
  </read_first>
  <action>
    Create `scripts/profile_gemma4_latency.py` — a standalone Python script that:

    1. Connects to local Ollama via OllamaProvider (reuse `from src.core.llm.providers import OllamaProvider` — do NOT reinvent the HTTP call).
    2. Issues N=30 calls in two workload shapes (sequential, not concurrent — we measure single-call latency, not throughput):
       - **Alpha workload**: system prompt = "You are a quantitative trading analyst. Respond with valid JSON only: {\"multiplier\": float}. multiplier between 0.5 and 1.5." prompt varies per iteration with realistic content (symbol/regime/signal — load 30 distinct rows from `signal_transform_log` if available via asyncpg, OR fallback to 30 synthetic prompts if DB empty). max_tokens=64.
       - **Narrative workload**: system prompt = "You are a market analyst writing 2-3 sentences of context for a trading signal." prompt varies; max_tokens=512.
    3. Records `time.monotonic()` before each call, after each call, computes elapsed_ms.
    4. After 30 samples per workload, computes P50, P95, P99 via `numpy.percentile([50, 95, 99])`.
    5. Writes a structured report to stdout AND to `.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-LATENCY-PROFILE.md`.

    Report format (LATENCY-PROFILE.md):
    ```markdown
    # Phase 73 — gemma4:e4b Latency Profile

    **Measured:** <ISO timestamp>
    **Hardware:** 192.168.68.53, AMD ROCm iGPU
    **Model:** gemma4:e4b (Ollama)
    **N per workload:** 30

    ## Alpha Workload (max_tokens=64, JSON multiplier)

    | Percentile | Latency (ms) |
    |------------|-------------:|
    | P50 | <value> |
    | P95 | <value> |
    | P99 | <value> |

    **Recommended `latency_budget_ms` for alpha agents:** <2× P95, rounded to nearest 500>

    ## Narrative Workload (max_tokens=512, prose)

    | Percentile | Latency (ms) |
    |------------|-------------:|
    | P50 | <value> |
    | P95 | <value> |
    | P99 | <value> |

    **Recommended `latency_budget_ms` for narrative agent:** <2× P95, rounded to nearest 1000>

    ## Notes
    - Plan 06 (alpha agents) MUST set `latency_budget_ms = <recommended alpha value>` on each agent class.
    - Plan 07 (narrative agent) MUST set `latency_budget_ms = <recommended narrative value>` on NarrativeComputeAgent.
    - If Ollama unavailable at profile time: fallback to defaults from CONTEXT.md (alpha=3000, narrative=60000) and add note here documenting the fallback.
    ```

    Run script:
    ```
    .venv/bin/python scripts/profile_gemma4_latency.py
    ```

    If Ollama is not running or gemma4:e4b not pulled: catch the connection error, write the LATENCY-PROFILE.md with defaults from CONTEXT.md and a note "Ollama unavailable at profile time — using safe defaults; re-run when Ollama is available". Do NOT block the plan.

    Per CLAUDE.md: use UTC timestamps (`datetime.now(UTC)`), structured logging via structlog, and `Settings()` not `os.environ`.
  </action>
  <verify>
    <automated>[ -f scripts/profile_gemma4_latency.py ] &amp;&amp; [ -f .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-LATENCY-PROFILE.md ] &amp;&amp; grep -qE "P95" .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-LATENCY-PROFILE.md &amp;&amp; grep -qE "latency_budget_ms" .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-LATENCY-PROFILE.md &amp;&amp; .venv/bin/python -c "import ast; ast.parse(open('scripts/profile_gemma4_latency.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/profile_gemma4_latency.py` exists, parses as valid Python (ast.parse succeeds)
    - Script imports `OllamaProvider` from `src.core.llm.providers` (verified by grep)
    - `grep -E "P95|p95" scripts/profile_gemma4_latency.py` returns at least one match
    - `.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-LATENCY-PROFILE.md` exists
    - LATENCY-PROFILE.md contains tables with P50/P95/P99 rows AND a "Recommended `latency_budget_ms`" line for both alpha and narrative workloads
    - Script uses UTC timestamps: `grep -E "datetime\.now\(UTC\)|tz=UTC" scripts/profile_gemma4_latency.py` matches
    - Script uses Settings, not os.environ: `! grep -E "os\.environ\[" scripts/profile_gemma4_latency.py` returns nothing
    - Script writes a recommended value greater than the measured P95 (sanity check — 2× rule), OR documents the fallback explicitly
  </acceptance_criteria>
  <done>Profile script exists and is re-runnable; LATENCY-PROFILE.md has measured (or fallback-documented) percentiles and recommended budgets; downstream plans (06, 07) can read this file to set agent class attributes.</done>
</task>

</tasks>

<verification>
- Profile script parses + runs without uncaught exception (or documents Ollama unavailability)
- LATENCY-PROFILE.md exists with P50/P95/P99 + recommendations
- 87 baseline tests still pass (no source files modified other than scripts/)
</verification>

<success_criteria>
Empirical latency baseline captured for the LLM provider that drives all alpha and narrative agents. Future plans (06, 07) reference this measured baseline rather than guessing. Script is reusable when hardware changes.
</success_criteria>

<output>
After completion, create `.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-04-SUMMARY.md`
</output>
