---
phase: 73-ai-llm-layer-b-architecture-refactor
plan: 07
type: execute
wave: 4
depends_on: ["73-01", "73-02", "73-03", "73-04", "73-05", "73-06"]
files_modified:
  - tests/unit/test_skeptic_agent.py
  - tests/unit/test_correlation_agent.py
  - tests/unit/test_volume_agent.py
  - tests/unit/test_narrative_orchestrator.py
  - tests/unit/test_narrative_parsers.py
  - tests/unit/test_narrative_prompts.py
  - tests/unit/test_swarm_dispatch.py
  - tests/unit/test_swarm_dispatch_integration.py
  - tests/unit/test_swarm_protocol.py
  - tests/unit/test_swarm_safety.py
  - tests/unit/ai/test_import_boundaries.py
  - services/swarm_dispatch_service.py
  - src/intelligence/swarm/agents/skeptic_agent.py
  - src/intelligence/swarm/agents/correlation_agent.py
  - src/intelligence/swarm/agents/volume_agent.py
  - src/intelligence/swarm/agents/skeptic_prompts.py
  - src/intelligence/swarm/agents/correlation_prompts.py
  - src/intelligence/swarm/agents/volume_prompts.py
  - src/intelligence/narrative/orchestrator.py
  - src/intelligence/narrative/prompts.py
  - src/intelligence/narrative/parsers.py
  - CLAUDE.md
autonomous: true
requirements: [D-36, D-37, D-38, D-39, D-40, D-41, D-48, D-51]

must_haves:
  truths:
    - "All 87+ baseline tests updated and passing with new import paths"
    - "AST-based import boundary check passes: no forbidden imports in src/core/ai/ or src/intelligence/ai/ (D-36, Gemini review)"
    - "Old files deleted: swarm_dispatch_service.py, old agent locations, old narrative location"
    - "CLAUDE.md service map updated with renamed services"
    - "All agents start with shadow_only=True (D-37, D-48)"
    - "Latency budgets configured per agent type (D-51)"
  artifacts:
    - path: "tests/unit/ai/test_import_boundaries.py"
      provides: "AST-based import boundary enforcement (D-36, Gemini review)"
      contains: "ast.parse"
    - path: "CLAUDE.md"
      provides: "Updated service map with AlphaSwarmComputeAgent and NarrativeGroupComputeAgent"
  key_links:
    - from: "tests/unit/ai/test_import_boundaries.py"
      to: "src/core/ai/"
      via: "AST walk verifying no forbidden imports"
      pattern: "ast.parse"
---

<objective>
Migrate all test imports to new paths, delete old files, enforce import boundaries with AST-based checking, deploy systemd units, update CLAUDE.md.

Purpose: Per D-36 (import boundary discipline), D-37 (shadow_only default), D-38 (graduation auto-flip), D-39-41 (execution order, LLM chain verified), D-48 (shadow mode default), D-51 (latency budgets). This is the final integration wave that cleans up old files, migrates tests, enforces boundaries, and updates documentation.

Output: All tests passing, old files deleted, import boundary check, CLAUDE.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-CONTEXT.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-REVIEWS.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-05-SUMMARY.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-06-SUMMARY.md

<interfaces>
Files being deleted (old locations):
- services/swarm_dispatch_service.py
- src/intelligence/swarm/agents/skeptic_agent.py
- src/intelligence/swarm/agents/skeptic_prompts.py
- src/intelligence/swarm/agents/correlation_agent.py
- src/intelligence/swarm/agents/correlation_prompts.py
- src/intelligence/swarm/agents/volume_agent.py
- src/intelligence/swarm/agents/volume_prompts.py
- src/intelligence/narrative/orchestrator.py
- src/intelligence/narrative/prompts.py
- src/intelligence/narrative/parsers.py
- src/intelligence/narrative/__init__.py

Test files being updated:
- tests/unit/test_skeptic_agent.py
- tests/unit/test_correlation_agent.py
- tests/unit/test_volume_agent.py
- tests/unit/test_narrative_orchestrator.py
- tests/unit/test_narrative_parsers.py
- tests/unit/test_narrative_prompts.py
- tests/unit/test_swarm_dispatch.py
- tests/unit/test_swarm_dispatch_integration.py
- tests/unit/test_swarm_protocol.py
- tests/unit/test_swarm_safety.py
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate test imports, delete old files, create import boundary check, update CLAUDE.md</name>
  <files>
    tests/unit/test_skeptic_agent.py, tests/unit/test_correlation_agent.py,
    tests/unit/test_volume_agent.py, tests/unit/test_narrative_orchestrator.py,
    tests/unit/test_narrative_parsers.py, tests/unit/test_narrative_prompts.py,
    tests/unit/test_swarm_dispatch.py, tests/unit/test_swarm_dispatch_integration.py,
    tests/unit/test_swarm_protocol.py, tests/unit/test_swarm_safety.py,
    tests/unit/ai/test_import_boundaries.py,
    services/swarm_dispatch_service.py, src/intelligence/swarm/agents/skeptic_agent.py,
    src/intelligence/swarm/agents/correlation_agent.py, src/intelligence/swarm/agents/volume_agent.py,
    src/intelligence/swarm/agents/skeptic_prompts.py, src/intelligence/swarm/agents/correlation_prompts.py,
    src/intelligence/swarm/agents/volume_prompts.py,
    src/intelligence/narrative/orchestrator.py, src/intelligence/narrative/prompts.py,
    src/intelligence/narrative/parsers.py, src/intelligence/narrative/__init__.py,
    CLAUDE.md
  </files>

  <read_first>
    - Each test file (full read) to identify exact import lines that need updating
    - CLAUDE.md (Active Services table and naming conventions)
    - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-REVIEWS.md (Gemini import boundary feedback)
  </read_first>

  <action>
    ## Step 1: Update test file imports

    For each test file, update imports from old paths to new paths:

    | Test File | Old Import | New Import |
    |-----------|-----------|------------|
    | test_skeptic_agent.py | `from src.intelligence.swarm.agents.skeptic_agent import SkepticAgentComputeAgent` | `from src.intelligence.ai.alpha.skeptic_agent import SkepticAgentComputeAgent` |
    | test_skeptic_agent.py | `from src.intelligence.swarm.agents.skeptic_prompts import ...` | `from src.intelligence.ai.alpha.skeptic_prompts import ...` |
    | test_skeptic_agent.py | `from src.core.swarm.base_agent import SwarmBaseAgent` | `from src.core.ai.base_agent import BaseAIAgent` |
    | test_skeptic_agent.py | `from src.intelligence.schemas import AgentResult` | `from src.core.ai.output import AgentOutput` |
    | test_skeptic_agent.py | `from src.intelligence.swarm.context import SwarmContext` | `from src.core.ai.context import AIContext` |
    | test_correlation_agent.py | same pattern as skeptic | same pattern as skeptic |
    | test_volume_agent.py | same pattern as skeptic | same pattern as skeptic |
    | test_narrative_orchestrator.py | `from src.intelligence.narrative.orchestrator import ...` | `from src.intelligence.ai.narrative.narrative_agent import NarrativeComputeAgent` |
    | test_narrative_parsers.py | `from src.intelligence.narrative.parsers import ...` | `from src.intelligence.ai.narrative.parsers import ...` |
    | test_narrative_prompts.py | `from src.intelligence.narrative.prompts import ...` | `from src.intelligence.ai.narrative.prompts import ...` |
    | test_swarm_dispatch.py | `from services.swarm_dispatch_service import SwarmDispatchComputeAgent` | `from services.alpha_swarm_agent import AlphaSwarmComputeAgent` |
    | test_swarm_dispatch_integration.py | same as test_swarm_dispatch | same as test_swarm_dispatch |
    | test_swarm_protocol.py | `from src.core.swarm.base_agent import SwarmBaseAgent` | `from src.core.ai.base_agent import BaseAIAgent` |
    | test_swarm_safety.py | `from src.intelligence.swarm.safety import SafeSwarmWrapper` | `from src.core.ai.safe_wrapper import SafeAgentWrapper` |

    For each test file:
    - Replace class references: `SwarmDispatchComputeAgent` -> `AlphaSwarmComputeAgent`, `SwarmBaseAgent` -> `BaseAIAgent`, `SafeSwarmWrapper` -> `SafeAgentWrapper`, `SwarmContext` -> `AIContext`, `AgentResult` -> `AgentOutput`
    - Update any `__new__()` patterns: `SkepticAgentComputeAgent.__new__(SkepticAgentComputeAgent)` stays the same (class name unchanged, just parent changed)
    - Update any assertions on `AgentResult` fields (`.multiplier`, `.confidence`, `.path`) to use `AgentOutput.payload["multiplier"]`, `AgentOutput.payload["confidence"]`, etc.

    ## Step 2: Delete old files

    ```bash
    # Old service (renamed to alpha_swarm_agent.py)
    git rm services/swarm_dispatch_service.py

    # Old agent locations (moved to src/intelligence/ai/alpha/)
    git rm src/intelligence/swarm/agents/skeptic_agent.py
    git rm src/intelligence/swarm/agents/skeptic_prompts.py
    git rm src/intelligence/swarm/agents/correlation_agent.py
    git rm src/intelligence/swarm/agents/correlation_prompts.py
    git rm src/intelligence/swarm/agents/volume_agent.py
    git rm src/intelligence/swarm/agents/volume_prompts.py

    # Old narrative module (moved to src/intelligence/ai/narrative/)
    git rm src/intelligence/narrative/orchestrator.py
    git rm src/intelligence/narrative/prompts.py
    git rm src/intelligence/narrative/parsers.py
    git rm src/intelligence/narrative/__init__.py
    ```

    Keep these files in src/intelligence/swarm/ (per D-28, D-29):
    - src/intelligence/swarm/aggregator.py
    - src/intelligence/swarm/graduation.py
    - src/intelligence/swarm/metrics.py
    - src/intelligence/swarm/interface.py
    - src/intelligence/swarm/registry.py
    - src/intelligence/swarm/prompt_registry.py
    - src/intelligence/swarm/dummy_contributors.py

    ## Step 3: Create AST-based import boundary check (D-36, Gemini review)

    Create `tests/unit/ai/test_import_boundaries.py`:

    ```python
    """Import boundary enforcement for AI layer.

    D-36: src/core/ai/ and src/intelligence/ai/ must NOT import from:
    - src/intelligence/pipeline/
    - src/intelligence/plugins/
    - Any tier plugin implementation

    Only permitted imports:
    - src/intelligence/schemas.py
    - src/core/stream_keys.py
    - Standard library
    - Third-party packages

    Uses AST-based checking (not grep) per Gemini review recommendation.
    """
    import ast
    import os
    from pathlib import Path

    import pytest

    # Directories to check
    _CHECK_DIRS = [
        Path("src/core/ai"),
        Path("src/intelligence/ai"),
    ]

    # Forbidden import prefixes
    _FORBIDDEN_PREFIXES = (
        "src.intelligence.pipeline",
        "src.intelligence.plugins",
        "src.intelligence.trading",  # I7 plugin implementations
        "src.intelligence.patterns",  # I5 plugin implementations
        "src.intelligence.context",  # I4 plugin implementations
        "src.intelligence.composites",  # I2 plugin implementations
        "src.intelligence.structure",  # I3 plugin implementations
    )


    def _collect_imports(filepath: Path) -> list[str]:
        """Parse a Python file with AST and extract all import targets."""
        try:
            tree = ast.parse(filepath.read_text())
        except SyntaxError:
            return []

        imports = []
        for node in ast.walk(tree):
            if isinstance(node, ast.Import):
                for alias in node.names:
                    imports.append(alias.name)
            elif isinstance(node, ast.ImportFrom):
                if node.module:
                    imports.append(node.module)
        return imports


    def _find_python_files(directory: Path) -> list[Path]:
        """Find all .py files in directory tree."""
        if not directory.exists():
            return []
        return list(directory.rglob("*.py"))


    class TestImportBoundaries:
        def test_no_forbidden_imports_in_core_ai(self):
            """src/core/ai/ must not import from pipeline or tier plugins."""
            violations = []
            for filepath in _find_python_files(Path("src/core/ai")):
                for imp in _collect_imports(filepath):
                    for forbidden in _FORBIDDEN_PREFIXES:
                        if imp.startswith(forbidden):
                            violations.append((str(filepath), imp, forbidden))
            assert violations == [], (
                f"Forbidden imports found in src/core/ai/:\n"
                + "\n".join(f"  {f}: {i} (forbidden: {p})" for f, i, p in violations)
            )

        def test_no_forbidden_imports_in_intelligence_ai(self):
            """src/intelligence/ai/ must not import from pipeline or tier plugins."""
            violations = []
            for filepath in _find_python_files(Path("src/intelligence/ai")):
                for imp in _collect_imports(filepath):
                    for forbidden in _FORBIDDEN_PREFIXES:
                        if imp.startswith(forbidden):
                            violations.append((str(filepath), imp, forbidden))
            assert violations == [], (
                f"Forbidden imports found in src/intelligence/ai/:\n"
                + "\n".join(f"  {f}: {i} (forbidden: {p})" for f, i, p in violations)
            )

        def test_shadow_only_true_on_all_agents(self):
            """D-37, D-48: All BaseAIAgent subclasses must have shadow_only=True."""
            import re
            violations = []
            for check_dir in [Path("src/intelligence/ai"), Path("src/core/ai")]:
                for filepath in _find_python_files(check_dir):
                    content = filepath.read_text()
                    # Check for explicit shadow_only = False
                    if re.search(r"shadow_only\s*=\s*False", content):
                        violations.append(str(filepath))
            assert violations == [], (
                f"shadow_only = False found (D-37 violation):\n"
                + "\n".join(f"  {v}" for v in violations)
            )
    ```

    ## Step 4: Update CLAUDE.md Active Services table

    Update the Active Services table in CLAUDE.md:

    | Old Entry | New Entry |
    |-----------|-----------|
    | `Swarm Orchestrator \| indicagent-swarm-orchestrator` | DELETED |
    | `Swarm Dispatch` (if listed) | `Alpha Swarm \| indicagent-alpha-swarm \| LLM alpha multiplier agents (skeptic, correlation, volume)` |
    | `AI Narrative \| indicagent-ai-narrative` | `AI Narrative \| indicagent-ai-narrative \| LLM market narrative generation (5m+ TF gated)` |

    Add to Core Runtime Files:
    - `src/core/ai/` — universal AI agent infrastructure (BaseAIAgent, BaseGroupService, AIContext, AgentOutput, SafeAgentWrapper, LineageRecorder)
    - `src/intelligence/ai/` — mandate-based agent groups (alpha, narrative, risk placeholder)

    ## Step 5: Run full test suite

    ```bash
    .venv/bin/pytest tests/unit/ -v --tb=short 2>&1 | tail -30
    ```

    All tests must pass. If any test fails due to import errors, fix the import path.
  </action>

  <verify>
    <automated>
      bash -c '
        # Verify old files deleted
        test ! -f services/swarm_dispatch_service.py && echo "PASS: old dispatch deleted" || echo "FAIL: old dispatch still exists"
        test ! -f src/intelligence/swarm/agents/skeptic_agent.py && echo "PASS: old skeptic deleted" || echo "FAIL: old skeptic still exists"
        test ! -f src/intelligence/swarm/agents/correlation_agent.py && echo "PASS: old correlation deleted" || echo "FAIL: old correlation still exists"
        test ! -f src/intelligence/swarm/agents/volume_agent.py && echo "PASS: old volume deleted" || echo "FAIL: old volume still exists"
        test ! -f src/intelligence/narrative/orchestrator.py && echo "PASS: old narrative deleted" || echo "FAIL: old narrative still exists"

        # Verify kept files still exist
        test -f src/intelligence/swarm/aggregator.py && echo "PASS: aggregator kept" || echo "FAIL: aggregator missing"
        test -f src/intelligence/swarm/graduation.py && echo "PASS: graduation kept" || echo "FAIL: graduation missing"

        # Verify import boundary test exists
        test -f tests/unit/ai/test_import_boundaries.py && echo "PASS: boundary test exists" || echo "FAIL: boundary test missing"

        # Verify AST-based checking (not just grep)
        grep "ast.parse" tests/unit/ai/test_import_boundaries.py && echo "PASS: AST-based" || echo "FAIL: not AST-based"

        # Run import boundary tests
        .venv/bin/pytest tests/unit/ai/test_import_boundaries.py -v --tb=short 2>&1 | tail -10

        # Run full test suite
        .venv/bin/pytest tests/unit/ -q --tb=short -x 2>&1 | tail -10
      '
    </automated>
  </verify>

  <done>
    - All 12+ test files updated with new import paths
    - Old files deleted (swarm_dispatch_service.py, 6 old agent files, 4 old narrative files)
    - Kept files preserved (aggregator.py, graduation.py, metrics.py, etc.)
    - AST-based import boundary test passes (D-36)
    - shadow_only=True assertion passes (D-37, D-48)
    - CLAUDE.md updated with new service names and architecture
    - Full test suite passes (87+ baseline tests + new tests)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI layer→pipeline | Import boundary enforced by AST-based CI check |
| shadow_only flag | Verified by test — no class-level False declarations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-12 | Tampering | Import boundary | mitigate | AST-based CI check catches all forbidden imports including aliased |
| T-73-13 | Elevation of privilege | shadow_only bypass | mitigate | grep assertion + unit test — no shadow_only=False in source |
</threat_model>

<verification>
```bash
# Import boundary test passes
.venv/bin/pytest tests/unit/ai/test_import_boundaries.py -v

# Shadow mode gate passes
grep -r "shadow_only = False" src/intelligence/ai/ src/core/ai/ && exit 1 || echo "PASS: no shadow_only=False"

# Full test suite passes
.venv/bin/pytest tests/unit/ -q --tb=short -x 2>&1 | tail -5

# Old files gone
! test -f services/swarm_dispatch_service.py
! test -f src/intelligence/swarm/agents/skeptic_agent.py
! test -f src/intelligence/narrative/orchestrator.py
```
</verification>

<success_criteria>
- All test imports updated to new paths
- Old files deleted from repo
- Import boundary check passes (AST-based, zero forbidden imports)
- shadow_only=True on all agents (grep assertion + test)
- CLAUDE.md updated with new services and architecture
- Full unit test suite passes (zero regressions from migration)
- Kept files preserved (aggregator.py, graduation.py)
</success_criteria>

<output>
After completion, create `.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-07-SUMMARY.md`
</output>
