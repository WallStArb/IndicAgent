---
phase: 73-ai-llm-layer-b-architecture-refactor
plan: 06
type: execute
wave: 3
depends_on: [02, 03, 05]
files_modified:
  - src/intelligence/ai/alpha/skeptic_agent.py
  - src/intelligence/ai/alpha/skeptic_prompts.py
  - src/intelligence/ai/alpha/correlation_agent.py
  - src/intelligence/ai/alpha/correlation_prompts.py
  - src/intelligence/ai/alpha/volume_agent.py
  - src/intelligence/ai/alpha/volume_prompts.py
  - src/intelligence/ai/alpha/__init__.py
  - src/intelligence/swarm/agents/skeptic_agent.py
  - src/intelligence/swarm/agents/skeptic_prompts.py
  - src/intelligence/swarm/agents/correlation_agent.py
  - src/intelligence/swarm/agents/correlation_prompts.py
  - src/intelligence/swarm/agents/volume_agent.py
  - src/intelligence/swarm/agents/volume_prompts.py
  - tests/unit/test_skeptic_agent.py
  - tests/unit/test_correlation_agent.py
  - tests/unit/test_volume_agent.py
autonomous: true
requirements: [ALPHA-AGENT-MOVE, BASE-AI-AGENT-REBASE, PROMPT-FILES-MOVE]
must_haves:
  truths:
    - "Skeptic, Correlation, Volume agents live at src/intelligence/ai/alpha/"
    - "All three agents extend BaseAIAgent (not SwarmBaseAgent)"
    - "All three agents return AgentOutput from _compute (not AgentResult)"
    - "Each agent declares group='alpha', tiers_needed=frozenset({...}), shadow_only=True, latency_budget_ms (from 73-LATENCY-PROFILE.md)"
    - "Prompt files moved alongside agents (skeptic_prompts.py, correlation_prompts.py, volume_prompts.py)"
    - "Old files at src/intelligence/swarm/agents/ removed (alongside their prompt peers)"
    - "test_skeptic_agent.py, test_correlation_agent.py, test_volume_agent.py updated to import from new path and assert on AgentOutput"
    - "All three agents pass updated tests"
  artifacts:
    - path: "src/intelligence/ai/alpha/skeptic_agent.py"
      provides: "SkepticAgentComputeAgent extending BaseAIAgent, returning AgentOutput"
      contains: "class SkepticAgentComputeAgent(BaseAIAgent)"
    - path: "src/intelligence/ai/alpha/correlation_agent.py"
      provides: "CorrelationAgentComputeAgent extending BaseAIAgent"
      contains: "class CorrelationAgentComputeAgent(BaseAIAgent)"
    - path: "src/intelligence/ai/alpha/volume_agent.py"
      provides: "VolumeAgentComputeAgent extending BaseAIAgent"
      contains: "class VolumeAgentComputeAgent(BaseAIAgent)"
  key_links:
    - from: "src/intelligence/ai/alpha/skeptic_agent.py"
      to: "src/core/ai/base_agent.BaseAIAgent"
      via: "class extends BaseAIAgent"
      pattern: "class SkepticAgentComputeAgent\\(BaseAIAgent\\)"
    - from: "src/intelligence/ai/alpha/skeptic_agent.py"
      to: "src/intelligence/ai/alpha/skeptic_prompts.py"
      via: "import build_skeptic_prompt"
      pattern: "from src\\.intelligence\\.ai\\.alpha\\.skeptic_prompts"
---

<objective>
Move three alpha agents (Skeptic, Correlation, Volume) and their prompt files from `src/intelligence/swarm/agents/` to `src/intelligence/ai/alpha/`. Rebase from `SwarmBaseAgent` → `BaseAIAgent`. Convert `_compute()` return type from `AgentResult` → `AgentOutput`. Read measured latency budgets from `73-LATENCY-PROFILE.md`. Move prompt files in same plan (per RESEARCH.md Pitfall 1 + CONTEXT.md locked decision #8). Delete old files. Update tests.

Per CONTEXT.md "Module Reorganization" + decision #8 (prompt files move alongside agents).

Purpose: Get the alpha agents living in their new home with the new contract before Plan 08 (alpha_swarm_agent service) wires them.
Output: 3 agents + 3 prompt files at new path; old files deleted; tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-CONTEXT.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-RESEARCH.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-PATTERNS.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-AI-SPEC.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-LATENCY-PROFILE.md
@src/intelligence/swarm/agents/skeptic_agent.py
@src/intelligence/swarm/agents/skeptic_prompts.py
@src/intelligence/swarm/agents/correlation_agent.py
@src/intelligence/swarm/agents/correlation_prompts.py
@src/intelligence/swarm/agents/volume_agent.py
@src/intelligence/swarm/agents/volume_prompts.py
@src/core/ai/base_agent.py
@src/core/ai/context.py
@src/core/ai/output.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move + rebase Skeptic agent (agent + prompt + test)</name>
  <files>
    src/intelligence/ai/alpha/skeptic_agent.py
    src/intelligence/ai/alpha/skeptic_prompts.py
    src/intelligence/swarm/agents/skeptic_agent.py
    src/intelligence/swarm/agents/skeptic_prompts.py
    tests/unit/test_skeptic_agent.py
  </files>
  <read_first>
    - src/intelligence/swarm/agents/skeptic_agent.py (full file — current SkepticAgentComputeAgent extending SwarmBaseAgent)
    - src/intelligence/swarm/agents/skeptic_prompts.py (full file — ACTIVE_VERSION constant + build_skeptic_prompt function)
    - tests/unit/test_skeptic_agent.py (full file — current import paths and assertions)
    - src/core/ai/base_agent.py (BaseAIAgent contract from Plan 02)
    - src/core/ai/context.py (AIContext + Tier from Plan 02)
    - src/core/ai/output.py (AgentOutput from Plan 02)
    - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-PATTERNS.md (section "src/intelligence/ai/alpha/skeptic_agent.py" — exact import + class header changes)
    - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-LATENCY-PROFILE.md (recommended `latency_budget_ms` for alpha agents — 2× P95)
    - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-AI-SPEC.md (Section 3 "Entry Point Pattern" — full alpha agent template; Section 4b.1 "Structured Outputs" — None handling pattern)
  </read_first>
  <action>
    Step 1: Copy `skeptic_prompts.py` → `src/intelligence/ai/alpha/skeptic_prompts.py`. The file is mostly self-contained text constants + builder function; if it imports from `src.intelligence.swarm.context.SwarmContext`, update the import to `from src.core.ai.context import AIContext` and update all type annotations. If it reads context fields like `ctx.i4.hmm_regime`, those field names exist on AIContext too — no field rename needed (per Plan 02, AIContext mirrors SwarmContext field names).

    Step 2: Create `src/intelligence/ai/alpha/skeptic_agent.py` based on the analog `src/intelligence/swarm/agents/skeptic_agent.py` with these specific changes (per PATTERNS.md):

    - Replace imports:
      ```
      # Old:
      from src.core.swarm.base_agent import SwarmBaseAgent
      from src.intelligence.schemas import AgentResult
      from src.intelligence.swarm.agents.skeptic_prompts import ACTIVE_VERSION, build_skeptic_prompt
      from src.intelligence.swarm.context import SwarmContext
      # New:
      from src.core.ai.base_agent import BaseAIAgent
      from src.core.ai.context import AIContext, Tier
      from src.core.ai.output import AgentOutput
      from src.intelligence.ai.alpha.skeptic_prompts import ACTIVE_VERSION, build_skeptic_prompt
      ```
    - Class signature: `class SkepticAgentComputeAgent(BaseAIAgent):`
    - Class attributes (set explicitly per AI-SPEC pitfall #4 about shadow_only):
      ```
      agent_id = "skeptic_v1"
      group = "alpha"
      tiers_needed = frozenset({Tier.I1, Tier.I4, Tier.I6, Tier.I7})
      shadow_only = True
      latency_budget_ms = <value from 73-LATENCY-PROFILE.md alpha recommendation; fallback 3000.0>
      ```
    - `_compute(self, context: AIContext) -> AgentOutput`:
      - If `context.i4 is None`: return `self._neutral(context, error="no_i4_context", latency_ms=0.0)`
      - Build prompt via `build_skeptic_prompt(context)` (the prompt file's signature must accept AIContext now)
      - Call `await self._chain.generate(prompt=..., system=..., max_tokens=64, timeout=self.latency_budget_ms/1000.0, audit_context={"signal_id": str(context.signal_id), "agent_id": self.agent_id, "symbol": context.symbol, "timeframe": context.timeframe})` — audit_context REQUIRED per D-06 (Plan 03 fix)
      - On `text is None`: return `self._neutral(context, error="llm_none", latency_ms=0.0)`
      - Parse JSON, extract failure_probability/confidence/risk_factors/reasoning. Clamp multiplier to [0.5, 1.5].
      - Return:
        ```python
        AgentOutput(
            agent_id=self.agent_id, group=self.group,
            signal_id=context.signal_id, symbol=context.symbol,
            timeframe=context.timeframe, ts=context.ts,
            output_type="multiplier",
            payload={"multiplier": multiplier, "confidence": confidence,
                     "failure_probability": failure_probability,
                     "risk_factors": risk_factors, "reasoning": reasoning,
                     "prompt_version": ACTIVE_VERSION},
            shadow_only=self.shadow_only,
            latency_ms=0.0,  # BaseAIAgent.compute() wrapper sets real value
        )
        ```
    - JSON parse helpers (the standalone functions from analog lines 99-152) move unchanged.

    Step 3: Delete old files: `rm src/intelligence/swarm/agents/skeptic_agent.py src/intelligence/swarm/agents/skeptic_prompts.py`

    Step 4: Update `tests/unit/test_skeptic_agent.py`:
    - Change import from `from src.intelligence.swarm.agents.skeptic_agent import SkepticAgentComputeAgent` → `from src.intelligence.ai.alpha.skeptic_agent import SkepticAgentComputeAgent`
    - Change context fixtures from `SwarmContext(...)` → `AIContext(...)` — field names are identical, only the class name and module change
    - Change assertions reading `result.multiplier` → `output.payload["multiplier"]`
    - Add assertion: `assert output.group == "alpha"` and `assert output.output_type == "multiplier"` for happy path; `assert output.output_type == "neutral"` for error paths
    - Confirm `shadow_only=True` is asserted on at least one fixture

    Run test: `.venv/bin/pytest tests/unit/test_skeptic_agent.py -v --tb=short`
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_skeptic_agent.py -v --tb=short 2>&amp;1 | tail -20 &amp;&amp; [ -f src/intelligence/ai/alpha/skeptic_agent.py ] &amp;&amp; [ -f src/intelligence/ai/alpha/skeptic_prompts.py ] &amp;&amp; ! [ -e src/intelligence/swarm/agents/skeptic_agent.py ] &amp;&amp; ! [ -e src/intelligence/swarm/agents/skeptic_prompts.py ]</automated>
  </verify>
  <acceptance_criteria>
    - `src/intelligence/ai/alpha/skeptic_agent.py` exists; `src/intelligence/ai/alpha/skeptic_prompts.py` exists
    - `src/intelligence/swarm/agents/skeptic_agent.py` does NOT exist; `src/intelligence/swarm/agents/skeptic_prompts.py` does NOT exist
    - `grep -E "^class SkepticAgentComputeAgent\(BaseAIAgent\):" src/intelligence/ai/alpha/skeptic_agent.py` returns one match
    - `grep -E "agent_id = .skeptic_v1." src/intelligence/ai/alpha/skeptic_agent.py` returns one match
    - `grep -E "group = .alpha." src/intelligence/ai/alpha/skeptic_agent.py` returns one match
    - `grep -E "tiers_needed = frozenset" src/intelligence/ai/alpha/skeptic_agent.py` returns one match
    - `grep -E "shadow_only = True" src/intelligence/ai/alpha/skeptic_agent.py` returns one match
    - `grep -E "audit_context=" src/intelligence/ai/alpha/skeptic_agent.py` returns at least one match (D-06 wired)
    - `grep -E "AgentOutput\(" src/intelligence/ai/alpha/skeptic_agent.py` returns at least one match (success path)
    - `! grep -E "SwarmBaseAgent|AgentResult|SwarmContext" src/intelligence/ai/alpha/skeptic_agent.py src/intelligence/ai/alpha/skeptic_prompts.py` returns nothing (clean migration)
    - `! grep -rE "from src\.intelligence\.(pipeline|plugins)" src/intelligence/ai/alpha/skeptic_agent.py src/intelligence/ai/alpha/skeptic_prompts.py` returns nothing (import boundary)
    - `tests/unit/test_skeptic_agent.py` imports from new path: `grep "from src\.intelligence\.ai\.alpha\.skeptic_agent" tests/unit/test_skeptic_agent.py` returns one match
    - `.venv/bin/pytest tests/unit/test_skeptic_agent.py -v` passes (zero failures)
  </acceptance_criteria>
  <done>Skeptic agent + prompts at new path; tests pass; old files gone.</done>
</task>

<task type="auto">
  <name>Task 2: Move + rebase Correlation agent (agent + prompt + test)</name>
  <files>
    src/intelligence/ai/alpha/correlation_agent.py
    src/intelligence/ai/alpha/correlation_prompts.py
    src/intelligence/swarm/agents/correlation_agent.py
    src/intelligence/swarm/agents/correlation_prompts.py
    tests/unit/test_correlation_agent.py
  </files>
  <read_first>
    - src/intelligence/swarm/agents/correlation_agent.py (full file)
    - src/intelligence/swarm/agents/correlation_prompts.py (full file)
    - tests/unit/test_correlation_agent.py (full file)
    - src/intelligence/ai/alpha/skeptic_agent.py (Task 1 result — use as template for this agent's structure)
    - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-PATTERNS.md (section "src/intelligence/ai/alpha/correlation_agent.py")
    - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-LATENCY-PROFILE.md (alpha latency_budget_ms recommendation)
  </read_first>
  <action>
    Same pattern as Task 1, applied to Correlation agent:

    1. Copy `correlation_prompts.py` to new path; update SwarmContext → AIContext imports inside.
    2. Create `src/intelligence/ai/alpha/correlation_agent.py` with class `CorrelationAgentComputeAgent(BaseAIAgent)`:
       - `agent_id = "correlation_v1"`
       - `group = "alpha"`
       - `tiers_needed = frozenset({...})` — read existing analog to determine which tiers it actually uses (likely I1, I4, I6 if it consumes regime + cross-asset spreads)
       - `shadow_only = True`
       - `latency_budget_ms = <from 73-LATENCY-PROFILE.md>`
       - `_compute()` returns AgentOutput with `output_type="multiplier"`, `payload` containing multiplier+confidence and any correlation-specific fields
       - audit_context wired per D-06
    3. Delete old files at `src/intelligence/swarm/agents/correlation_*.py`.
    4. Update `tests/unit/test_correlation_agent.py` — same pattern as test_skeptic_agent.py update.
    5. Run `.venv/bin/pytest tests/unit/test_correlation_agent.py -v --tb=short`
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_correlation_agent.py -v --tb=short 2>&amp;1 | tail -20 &amp;&amp; [ -f src/intelligence/ai/alpha/correlation_agent.py ] &amp;&amp; [ -f src/intelligence/ai/alpha/correlation_prompts.py ] &amp;&amp; ! [ -e src/intelligence/swarm/agents/correlation_agent.py ] &amp;&amp; ! [ -e src/intelligence/swarm/agents/correlation_prompts.py ]</automated>
  </verify>
  <acceptance_criteria>
    - `src/intelligence/ai/alpha/correlation_agent.py` exists; `correlation_prompts.py` exists at new path
    - Old files at `src/intelligence/swarm/agents/correlation_*.py` removed
    - `grep -E "^class CorrelationAgentComputeAgent\(BaseAIAgent\):" src/intelligence/ai/alpha/correlation_agent.py` matches
    - `grep -E "group = .alpha." src/intelligence/ai/alpha/correlation_agent.py` matches
    - `grep -E "tiers_needed = frozenset" src/intelligence/ai/alpha/correlation_agent.py` matches
    - `grep -E "audit_context=" src/intelligence/ai/alpha/correlation_agent.py` matches
    - `! grep -E "SwarmBaseAgent|AgentResult|SwarmContext" src/intelligence/ai/alpha/correlation_agent.py src/intelligence/ai/alpha/correlation_prompts.py` returns nothing
    - `tests/unit/test_correlation_agent.py` imports from new path
    - `.venv/bin/pytest tests/unit/test_correlation_agent.py -v` passes
  </acceptance_criteria>
  <done>Correlation agent + prompts migrated; tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Move + rebase Volume agent + finalize alpha __init__.py exports</name>
  <files>
    src/intelligence/ai/alpha/volume_agent.py
    src/intelligence/ai/alpha/volume_prompts.py
    src/intelligence/ai/alpha/__init__.py
    src/intelligence/swarm/agents/volume_agent.py
    src/intelligence/swarm/agents/volume_prompts.py
    tests/unit/test_volume_agent.py
  </files>
  <read_first>
    - src/intelligence/swarm/agents/volume_agent.py (full file)
    - src/intelligence/swarm/agents/volume_prompts.py (full file)
    - tests/unit/test_volume_agent.py (full file)
    - src/intelligence/ai/alpha/skeptic_agent.py (Task 1 result — template)
    - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-PATTERNS.md (section "volume_agent.py")
    - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-LATENCY-PROFILE.md
  </read_first>
  <action>
    Same pattern, Volume agent edition:

    1. Copy volume_prompts.py to new path with SwarmContext → AIContext fix.
    2. Create `src/intelligence/ai/alpha/volume_agent.py`:
       - `class VolumeAgentComputeAgent(BaseAIAgent)`
       - `agent_id = "volume_v1"`
       - `group = "alpha"`
       - `tiers_needed = frozenset({...})` — likely I1, I4, I7 (volume features + regime + signal context)
       - `shadow_only = True`, `latency_budget_ms = <profile>`
       - `_compute` returns AgentOutput with audit_context wired
    3. Delete old volume files at swarm/agents/.
    4. Update tests/unit/test_volume_agent.py.
    5. Update `src/intelligence/ai/alpha/__init__.py` to export the three agent classes:
       ```python
       """Alpha mandate group: skeptic, correlation, volume agents."""
       from src.intelligence.ai.alpha.skeptic_agent import SkepticAgentComputeAgent
       from src.intelligence.ai.alpha.correlation_agent import CorrelationAgentComputeAgent
       from src.intelligence.ai.alpha.volume_agent import VolumeAgentComputeAgent

       __all__ = [
           "SkepticAgentComputeAgent",
           "CorrelationAgentComputeAgent",
           "VolumeAgentComputeAgent",
       ]
       ```
    6. Run all three alpha-agent tests + the swarm aggregator/writer tests (they should still pass from Plan 05):
       ```
       .venv/bin/pytest tests/unit/test_skeptic_agent.py tests/unit/test_correlation_agent.py tests/unit/test_volume_agent.py tests/unit/test_swarm_aggregator.py tests/unit/test_swarm_writer_agent.py -v --tb=short
       ```
    7. Confirm import boundary clean across the alpha tree:
       ```
       grep -rE "from src\.intelligence\.(pipeline|plugins)" src/intelligence/ai/alpha/
       ```
       Must be empty.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_skeptic_agent.py tests/unit/test_correlation_agent.py tests/unit/test_volume_agent.py tests/unit/test_swarm_aggregator.py tests/unit/test_swarm_writer_agent.py -v --tb=short 2>&amp;1 | tail -30 &amp;&amp; [ -f src/intelligence/ai/alpha/volume_agent.py ] &amp;&amp; ! [ -e src/intelligence/swarm/agents/volume_agent.py ] &amp;&amp; ! grep -rE "from src\.intelligence\.(pipeline|plugins)" src/intelligence/ai/alpha/</automated>
  </verify>
  <acceptance_criteria>
    - `src/intelligence/ai/alpha/volume_agent.py` exists; `volume_prompts.py` exists at new path
    - Old files at `src/intelligence/swarm/agents/volume_*.py` removed
    - `grep -E "^class VolumeAgentComputeAgent\(BaseAIAgent\):" src/intelligence/ai/alpha/volume_agent.py` matches
    - `grep -E "audit_context=" src/intelligence/ai/alpha/volume_agent.py` matches
    - `src/intelligence/ai/alpha/__init__.py` exports all 3 agent classes (`grep -c "ComputeAgent" src/intelligence/ai/alpha/__init__.py` returns >=3)
    - `python -c "from src.intelligence.ai.alpha import SkepticAgentComputeAgent, CorrelationAgentComputeAgent, VolumeAgentComputeAgent"` succeeds
    - `! grep -rE "from src\.intelligence\.(pipeline|plugins)" src/intelligence/ai/alpha/` returns nothing
    - All 5 test files (skeptic, correlation, volume, aggregator, writer) pass
    - `ls src/intelligence/swarm/agents/` should now have ONLY: `__init__.py`, archived files (`_archived_*.py`) — no live agent or prompt files (Skeptic/Correlation/Volume gone)
  </acceptance_criteria>
  <done>All 3 alpha agents at new path with new contract; old files gone; alpha package exports them; import boundary clean.</done>
</task>

</tasks>

<verification>
- All 3 alpha agents at `src/intelligence/ai/alpha/` extending BaseAIAgent
- All 3 prompt files moved alongside
- Old files at `src/intelligence/swarm/agents/{skeptic,correlation,volume}_{agent,prompts}.py` removed
- 5 test files pass: skeptic, correlation, volume agents + swarm aggregator + swarm writer
- Import boundary clean: no pipeline/plugin imports under src/intelligence/ai/alpha/
- 87 baseline tests still pass: `.venv/bin/pytest tests/unit/test_swarm_dispatch.py -q` may FAIL until Plan 08 (this is expected — Plan 06 is mid-wave)
</verification>

<success_criteria>
The 3 alpha agents live at their new path with the BaseAIAgent contract. They return AgentOutput. Their prompts moved with them. The receiving aggregator/writer (from Plan 05) accepts AgentOutput. Plan 08 can now wire the AlphaSwarmComputeAgent group service that orchestrates them.
</success_criteria>

<output>
After completion, create `.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-06-SUMMARY.md`
</output>
