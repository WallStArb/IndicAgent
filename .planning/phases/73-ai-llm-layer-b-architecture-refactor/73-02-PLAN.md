---
phase: 73-ai-llm-layer-b-architecture-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/ai/__init__.py
  - src/core/ai/output.py
  - src/core/ai/context.py
  - src/core/ai/safe_wrapper.py
  - src/core/ai/base_agent.py
  - src/intelligence/ai/__init__.py
  - src/intelligence/ai/alpha/__init__.py
  - src/intelligence/ai/narrative/__init__.py
  - src/intelligence/ai/risk/__init__.py
  - tests/unit/ai/__init__.py
  - tests/unit/ai/test_core_ai_output.py
  - tests/unit/ai/test_core_ai_context.py
  - tests/unit/ai/test_core_ai_safe_wrapper.py
  - tests/unit/ai/test_core_ai_base_agent.py
autonomous: true
requirements: [BASE-AI-AGENT, AI-CONTEXT, AGENT-OUTPUT, SAFE-WRAPPER, GROUP-DIRS]
must_haves:
  truths:
    - "src/core/ai/ exists with 5 files: base_agent.py, output.py, context.py, safe_wrapper.py, __init__.py (base_group_service.py created in Plan 03)"
    - "BaseAIAgent ABC defines compute() concrete wrapper + _compute() abstract; latency_ms is set on returned AgentOutput within 10ms tolerance"
    - "IAIAgent Protocol declares agent_id, group, tiers_needed, shadow_only, latency_budget_ms, async compute(AIContext) -> AgentOutput"
    - "AIContext is a frozen Pydantic v2 model with self-referential lead_context, model_rebuild() called"
    - "AIContextCache.build(symbol, tf, tiers_needed) populates only declared tiers; get_lead() is a public method (D-10)"
    - "AgentOutput is a frozen Pydantic model with payload: dict[str, Any] untyped"
    - "SafeAgentWrapper enforces asyncio.wait_for() and returns neutral AgentOutput on timeout/exception"
    - "Tier enum extends (str, Enum) with BAR, I1, I2, I3, I4, I5, I6, I7"
    - "src/intelligence/ai/{alpha,narrative,risk}/__init__.py exist as empty placeholder packages"
    - "Unit tests for all 4 new core modules pass"
  artifacts:
    - path: "src/core/ai/base_agent.py"
      provides: "BaseAIAgent ABC + IAIAgent Protocol"
      contains: "class BaseAIAgent"
    - path: "src/core/ai/context.py"
      provides: "AIContext, AIContextCache, Tier, TierContext models"
      contains: "class AIContext"
    - path: "src/core/ai/output.py"
      provides: "AgentOutput universal envelope"
      contains: "class AgentOutput"
    - path: "src/core/ai/safe_wrapper.py"
      provides: "SafeAgentWrapper timeout + exception isolation"
      contains: "class SafeAgentWrapper"
    - path: "src/intelligence/ai/risk/__init__.py"
      provides: "Empty placeholder marking future risk group"
  key_links:
    - from: "src/core/ai/base_agent.py"
      to: "src/core/ai/output.py + context.py"
      via: "AgentOutput, AIContext type imports"
      pattern: "from src.core.ai.output import AgentOutput"
    - from: "src/core/ai/safe_wrapper.py"
      to: "asyncio.wait_for"
      via: "timeout enforcement"
      pattern: "asyncio.wait_for"
---

<objective>
Build the universal AI infrastructure under `src/core/ai/`. Five files, but `base_group_service.py` is deferred to Plan 03 (it depends on stream_keys + LLM chain fixes). This plan creates four core files: output, context, safe_wrapper, base_agent — plus the `src/intelligence/ai/{alpha,narrative,risk}/__init__.py` package skeleton, and unit tests for each.

Per CONTEXT.md "New Infrastructure" decisions. Patterns are absorbed from `src/core/swarm/base_agent.py`, `src/intelligence/swarm/context.py`, `src/intelligence/swarm/safety.py`. No external imports beyond pydantic, asyncio, structlog, hashlib stdlib.

Purpose: Create the foundation other plans build on. Wave 1 because no dependencies on stream_keys or LLM fixes.
Output: 4 core files + 4 package __init__.py + unit tests, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-CONTEXT.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-RESEARCH.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-AI-SPEC.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-PATTERNS.md
@docs/plans/2026-04-26-ai-llm-layer-design.md
@src/core/swarm/base_agent.py
@src/intelligence/swarm/context.py
@src/intelligence/swarm/safety.py
@src/intelligence/schemas.py
@CLAUDE.md
</context>

<interfaces>
From src/intelligence/schemas.py — existing AgentResult to compare against AgentOutput shape:
```python
class AgentResult(BaseModel):
    model_config = ConfigDict(frozen=True)
    agent_id: str
    path: Literal["deterministic", "llm_swarm"]
    multiplier: float = Field(..., ge=MIN_MULTIPLIER, le=MAX_MULTIPLIER)
    confidence: float = Field(..., ge=0.0, le=1.0)
    shadow_only: bool = True
    metadata: dict[str, Any] = Field(default_factory=dict)
    latency_ms: float = 0.0
    error: str | None = None
```

From src/core/agent/base.py — BaseAgent that BaseGroupService will extend:
```python
class BaseAgent:
    def __init__(self, name: str, max_idle_seconds: int = 300) -> None: ...
    async def _setup(self) -> None: ...   # subclasses override
    async def _run(self) -> None: ...     # subclasses override
    async def _teardown(self) -> None: ...
    @property
    def running(self) -> bool: ...
    self.logger: structlog.BoundLogger
    self.tracer  # OTel tracer
```

Required AgentOutput shape (from CONTEXT.md + design spec):
```
agent_id: str
group: str
signal_id: UUID
symbol: str
timeframe: str
ts: datetime
output_type: str  # "multiplier", "narrative", "risk_score", "neutral"
payload: dict[str, Any]
shadow_only: bool
latency_ms: float
error: str | None
```

Required AIContext shape:
```
signal_id: UUID
symbol: str
timeframe: str
ts: datetime
trigger: str  # e.g., "signal", "bar"
i1: I1Context | None  # tier sub-objects, populated only if declared
i4: I4Context | None
i6: I6Context | None
i7: I7Context | None
lead_context: AIContext | None  # self-referential
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AgentOutput envelope + Tier enum + AIContext frozen model + AIContextCache</name>
  <files>
    src/core/ai/__init__.py
    src/core/ai/output.py
    src/core/ai/context.py
    src/intelligence/ai/__init__.py
    src/intelligence/ai/alpha/__init__.py
    src/intelligence/ai/narrative/__init__.py
    src/intelligence/ai/risk/__init__.py
    tests/unit/ai/__init__.py
    tests/unit/ai/test_core_ai_output.py
    tests/unit/ai/test_core_ai_context.py
  </files>
  <read_first>
    - src/intelligence/swarm/context.py (analog — full file, ~200 lines: SwarmContext, SwarmContextCache, seed_from_db_row, build, _safe helper, TTL pattern, model_rebuild call)
    - src/intelligence/schemas.py (lines 870-920 for AgentResult/AlphaMultiplier shape)
    - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-PATTERNS.md (sections "src/core/ai/output.py" and "src/core/ai/context.py")
    - services/swarm_dispatch_service.py (lines 350-450 for the _find_lead_context private _cache access D-10 needs to replace)
    - CLAUDE.md (frozen Pydantic + UTC timestamp + str-Enum rules)
  </read_first>
  <behavior>
    - Test 1: `AgentOutput(agent_id="x", group="alpha", signal_id=uuid4(), symbol="ES", timeframe="5m", ts=datetime.now(UTC), output_type="multiplier", payload={"multiplier": 1.2}, shadow_only=True, latency_ms=0.0)` constructs successfully
    - Test 2: AgentOutput is frozen — `o.payload = {}` raises ValidationError
    - Test 3: `payload` accepts arbitrary dict shape (untyped) — `payload={"text": "hello", "tokens": 42}` works
    - Test 4: AgentOutput supports model_copy(update={"latency_ms": 123.4})
    - Test 5: `Tier.I7.value == "i7"` — Tier extends (str, Enum) with members BAR/I1/I2/I3/I4/I5/I6/I7
    - Test 6: AIContext is frozen Pydantic; lead_context self-reference works (model_rebuild() called)
    - Test 7: AIContextCache.update() stores (event, monotonic) tuple; build() returns None after 300s TTL elapses
    - Test 8: AIContextCache.build(symbol, tf, frozenset({Tier.I4})) returns AIContext with i4 populated and i1/i6/i7 = None
    - Test 9: AIContextCache.get_lead(symbol="ES", tf="5m", lead_map={"ES": "ES"}) returns None when lead==base; returns AIContext when prefix-match found
    - Test 10: seed_from_db_row(dict_row) accepts asyncpg-style dict and stores SimpleNamespace proxy
  </behavior>
  <action>
    Create the 4 package skeletons FIRST (empty __init__.py files for src/core/ai/, src/intelligence/ai/, src/intelligence/ai/alpha/, src/intelligence/ai/narrative/, src/intelligence/ai/risk/, tests/unit/ai/).

    `src/intelligence/ai/risk/__init__.py` content (per CONTEXT.md decision):
    ```python
    """Placeholder for future RiskComputeAgent group. No agents yet."""
    ```

    Create `src/core/ai/output.py` with `AgentOutput` frozen Pydantic v2 model. Required fields per CONTEXT.md "Specifics":
    ```python
    from __future__ import annotations
    from datetime import datetime
    from typing import Any
    from uuid import UUID
    from pydantic import BaseModel, ConfigDict, Field

    class AgentOutput(BaseModel):
        model_config = ConfigDict(frozen=True)
        agent_id: str
        group: str
        signal_id: UUID
        symbol: str
        timeframe: str
        ts: datetime
        output_type: str  # "multiplier" | "narrative" | "risk_score" | "neutral"
        payload: dict[str, Any] = Field(default_factory=dict)
        shadow_only: bool = True
        latency_ms: float = 0.0
        error: str | None = None
    ```
    Do NOT typecheck `payload` shape — it is intentionally untyped per design spec.

    Create `src/core/ai/context.py` with:
    1. `Tier(str, Enum)` with members BAR="bar", I1="i1", ..., I7="i7" (str-Enum per CLAUDE.md)
    2. Per-tier frozen `TierContext` sub-models — at minimum I1Context, I4Context, I6Context, I7Context (mirror existing SwarmContext fields verbatim from src/intelligence/swarm/context.py; copy field names exactly). I2/I3/I5 left as placeholder comment per CONTEXT.md deferred ideas.
    3. `AIContext(BaseModel)` frozen, with universal fields (signal_id, symbol, timeframe, ts, trigger) + optional tier contexts (i1, i4, i6, i7 = None) + `lead_context: "AIContext | None" = None` self-reference. Call `AIContext.model_rebuild()` after class def (per Pitfall 8 in RESEARCH.md).
    4. `AIContextCache` class:
       - `_cache: dict[tuple[str, str], tuple[Any, float]] = {}`
       - `_TTL_SECONDS = 300` (module constant)
       - `update(event)`: stores `(event, time.monotonic())`
       - `seed_from_db_row(row: dict)`: SimpleNamespace proxy pattern from SwarmContextCache (preserve verbatim from analog)
       - `build(symbol, tf, tiers_needed: frozenset[Tier]) -> AIContext | None`: TTL check; populate only declared tiers
       - `get_lead(symbol, tf, lead_map: dict[str, str]) -> AIContext | None`: PUBLIC method replacing private `._cache` access in swarm_dispatch_service._find_lead_context. Encapsulates the prefix-search at lines 354-432 of analog. This is the D-10 fix. Used by AlphaSwarmComputeAgent in Plan 06.

    Write tests at `tests/unit/ai/test_core_ai_output.py` and `tests/unit/ai/test_core_ai_context.py` covering the 10 behaviors above. Use `pytest.mark.asyncio` only where needed (AIContextCache is sync — no asyncio).

    Do NOT add any imports from src/intelligence/pipeline/ or any tier plugin (import boundary rule per CONTEXT.md). Only allowed intelligence import: `from src.intelligence.schemas import IntelligenceEvent` (TYPE_CHECKING only).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/ai/test_core_ai_output.py tests/unit/ai/test_core_ai_context.py -v --tb=short 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from src.core.ai.output import AgentOutput; from src.core.ai.context import AIContext, AIContextCache, Tier"` succeeds
    - `grep -E "^class AgentOutput\(BaseModel\)" src/core/ai/output.py` returns a match
    - `grep -E "^class AIContext\(BaseModel\)" src/core/ai/context.py` returns a match
    - `grep -E "^class AIContextCache" src/core/ai/context.py` returns a match
    - `grep -E "^class Tier\(str, Enum\)" src/core/ai/context.py` returns a match
    - `grep -E "def get_lead" src/core/ai/context.py` returns a match (D-10 method)
    - `grep -E "model_rebuild\(\)" src/core/ai/context.py` returns a match (self-reference fix)
    - `grep -E "model_config = ConfigDict\(frozen=True\)" src/core/ai/output.py src/core/ai/context.py` returns at least 2 matches
    - `ls src/intelligence/ai/{__init__.py,alpha/__init__.py,narrative/__init__.py,risk/__init__.py}` all exist
    - `! grep -rE "from src\.intelligence\.(pipeline|plugins)" src/core/ai/ src/intelligence/ai/` returns nothing (import boundary)
    - All 10 unit tests pass: `.venv/bin/pytest tests/unit/ai/ -v` shows >=10 passes, 0 failures
  </acceptance_criteria>
  <done>AgentOutput + AIContext + AIContextCache + Tier all defined and tested. Package skeletons exist. Import boundary clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SafeAgentWrapper + BaseAIAgent ABC + IAIAgent Protocol</name>
  <files>
    src/core/ai/safe_wrapper.py
    src/core/ai/base_agent.py
    tests/unit/ai/test_core_ai_safe_wrapper.py
    tests/unit/ai/test_core_ai_base_agent.py
  </files>
  <read_first>
    - src/intelligence/swarm/safety.py (full ~90 lines — analog for SafeAgentWrapper, contains exact asyncio.wait_for + neutral fallback pattern)
    - src/core/swarm/base_agent.py (full ~80 lines — analog for BaseAIAgent: compute() wrapper, _compute() abstract, _neutral() helper)
    - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-PATTERNS.md (sections "src/core/ai/safe_wrapper.py" and "src/core/ai/base_agent.py")
    - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-AI-SPEC.md (Section 4b.2 "Async-First Design", BaseAIAgent code example)
    - src/core/ai/output.py (Task 1 output, used by both new files)
    - src/core/ai/context.py (Task 1 output, used by both new files)
  </read_first>
  <behavior>
    SafeAgentWrapper:
    - Test 1: wrapper around mock agent with `_compute = AsyncMock(return_value=AgentOutput(...))` returns AgentOutput with latency_ms set via model_copy
    - Test 2: wrapper around agent that `await asyncio.sleep(99)` with `latency_budget_ms=100` returns neutral AgentOutput within 200ms total (timeout enforced)
    - Test 3: returned neutral output has `error="timeout after 0.1s"` (or similar containing "timeout")
    - Test 4: wrapper around agent that raises Exception returns neutral AgentOutput with `error=str(exc)` (no exception propagates)
    - Test 5: wrapper preserves `agent_id`, `group`, `shadow_only` from wrapped agent

    BaseAIAgent:
    - Test 6: subclassing BaseAIAgent without implementing `_compute` raises TypeError on instantiation (ABC contract)
    - Test 7: a concrete subclass with `_compute` returning a valid AgentOutput → `compute(ctx)` returns same with `latency_ms > 0` set
    - Test 8: `_compute` raising RuntimeError → compute() returns neutral AgentOutput (does not propagate)
    - Test 9: timeout enforced — `_compute` sleeping > latency_budget_ms returns neutral with `error="timeout..."`
    - Test 10: IAIAgent Protocol — a class with the right shape passes `isinstance(x, IAIAgent)` via `runtime_checkable`
  </behavior>
  <action>
    Create `src/core/ai/safe_wrapper.py`. Absorb `src/intelligence/swarm/safety.py` verbatim with these renames:
    - `SafeSwarmWrapper` → `SafeAgentWrapper`
    - Method `run(SwarmContext) -> AgentResult` → `compute(AIContext) -> AgentOutput` (renamed `run` → `compute` to match BaseGroupService dispatch pattern from PATTERNS.md and the universal contract)
    - `AgentResult(...)` constructor calls → `AgentOutput(agent_id=..., group=..., signal_id=..., symbol=..., timeframe=..., ts=..., output_type="neutral", payload={}, shadow_only=..., latency_ms=..., error=...)`
    - Drop `path` field references (not in AgentOutput)
    - Constructor: `SafeAgentWrapper(agent: BaseAIAgent)`. Reads `agent.agent_id`, `agent.group`, `agent.shadow_only`, `agent.latency_budget_ms` via getattr with safe defaults.

    The neutral AgentOutput needs `signal_id`, `symbol`, `timeframe`, `ts` — these come from the AIContext passed to `compute()`, NOT from the agent. So `compute(self, context: AIContext)` reads them off the context for the neutral fallback.

    Create `src/core/ai/base_agent.py` containing TWO things:
    1. `IAIAgent` Protocol with `@runtime_checkable` decorator. Declares: `agent_id: str`, `group: str`, `tiers_needed: frozenset[Tier]`, `shadow_only: bool`, `latency_budget_ms: float`, `async def compute(self, context: AIContext) -> AgentOutput`.
    2. `BaseAIAgent(ABC)` (do NOT extend BaseAgent — that's for BaseGroupService in Plan 03; BaseAIAgent is a per-agent base class, lighter weight). Class attributes:
       ```python
       agent_id: str = ""
       group: str = ""
       tiers_needed: frozenset[Tier] = frozenset()
       shadow_only: bool = True
       latency_budget_ms: float = 5000.0
       ```
       Concrete `async def compute(self, context: AIContext) -> AgentOutput`: timing wrapper, calls `asyncio.wait_for(self._compute(context), timeout=self.latency_budget_ms/1000)`; on success returns `result.model_copy(update={"latency_ms": elapsed_ms})`; on TimeoutError or Exception returns `self._neutral(context, error=msg, latency_ms=elapsed_ms)`. Pattern verbatim from src/core/swarm/base_agent.py compute() with type substitution.

       Abstract: `async def _compute(self, context: AIContext) -> AgentOutput: ...`

       `_neutral(context, error, latency_ms)` returns:
       ```python
       AgentOutput(
           agent_id=self.agent_id, group=self.group,
           signal_id=context.signal_id, symbol=context.symbol,
           timeframe=context.timeframe, ts=context.ts,
           output_type="neutral", payload={},
           shadow_only=self.shadow_only,
           latency_ms=latency_ms, error=error,
       )
       ```

    Write 10 unit tests covering behaviors above in `test_core_ai_safe_wrapper.py` and `test_core_ai_base_agent.py`. Use `pytest.mark.asyncio` for async tests. Mock agents with `AsyncMock` for `_compute`.

    CRITICAL: do NOT call `setup_service_logging()` in BaseAIAgent — agents are not services (per CLAUDE.md). Use module-level `logger = structlog.get_logger(__name__)`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/ai/test_core_ai_safe_wrapper.py tests/unit/ai/test_core_ai_base_agent.py -v --tb=short 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from src.core.ai.safe_wrapper import SafeAgentWrapper; from src.core.ai.base_agent import BaseAIAgent, IAIAgent"` succeeds
    - `grep -E "^class SafeAgentWrapper" src/core/ai/safe_wrapper.py` matches
    - `grep -E "^class BaseAIAgent" src/core/ai/base_agent.py` matches
    - `grep -E "^class IAIAgent" src/core/ai/base_agent.py` matches (Protocol)
    - `grep -E "asyncio\.wait_for" src/core/ai/safe_wrapper.py src/core/ai/base_agent.py` matches in BOTH files
    - `grep -E "@abstractmethod" src/core/ai/base_agent.py` matches at least once (for `_compute`)
    - `grep -E "@runtime_checkable" src/core/ai/base_agent.py` matches (IAIAgent Protocol)
    - `grep -E "model_copy\(update=" src/core/ai/base_agent.py` matches (latency injection pattern)
    - `! grep -rE "from src\.intelligence\.(pipeline|plugins)" src/core/ai/` returns nothing (import boundary)
    - All 10 unit tests pass; combined test suite at `tests/unit/ai/` shows >=20 passes total (10 from Task 1 + 10 here), 0 failures
  </acceptance_criteria>
  <done>BaseAIAgent ABC + IAIAgent Protocol + SafeAgentWrapper exist with timeout/exception isolation enforced. All tests green.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/ai/ -v` passes with >=20 tests
- Import smoke: `python -c "from src.core.ai import base_agent, output, context, safe_wrapper"` succeeds
- Import boundary: `! grep -rE "from src\.intelligence\.(pipeline|plugins)" src/core/ai/ src/intelligence/ai/` returns nothing
- Frozen models verified: `grep -c "ConfigDict(frozen=True)" src/core/ai/output.py src/core/ai/context.py` ≥ 2
- D-10 method exists: `grep -c "def get_lead" src/core/ai/context.py` ≥ 1
- 87 baseline tests still pass: `.venv/bin/pytest tests/unit/ -q --ignore=tests/unit/ai 2>&1 | tail -3` shows no new failures vs Plan 01 baseline
</verification>

<success_criteria>
src/core/ai/ has 4 of its 5 final files (base_agent, context, output, safe_wrapper). The 5th (base_group_service.py) lands in Plan 03. src/intelligence/ai/ tree exists with empty packages. 20+ unit tests cover the new infrastructure. Import boundaries respected. Foundation ready for Wave 2 LLM fixes and Wave 3 agent migrations.
</success_criteria>

<output>
After completion, create `.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-02-SUMMARY.md`
</output>
