---
phase: 73-ai-llm-layer-b-architecture-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/swarm_orchestrator_agent.py
  - tests/unit/service_tests/test_swarm_orchestrator_agent.py
  - tests/unit/service_tests/test_swarm_orchestrator_seeding.py
  - /etc/systemd/system/indicagent-swarm-orchestrator.service
autonomous: true
requirements: [AILLM-01, AILLM-02, AILLM-09]
tags: [systemd, deletion, dead-code]

must_haves:
  truths:
    - "indicagent-swarm-orchestrator service is no longer running or enabled"
    - "services/swarm_orchestrator_agent.py no longer exists"
    - "Tests referencing the deleted service no longer cause pytest import failures"
    - "systemctl list-units shows no swarm-orchestrator unit"
  artifacts:
    - path: "services/swarm_orchestrator_agent.py"
      must_not_exist: true
    - path: "/etc/systemd/system/indicagent-swarm-orchestrator.service"
      must_not_exist: true
    - path: "tests/unit/service_tests/test_swarm_orchestrator_agent.py"
      must_not_exist: true
    - path: "tests/unit/service_tests/test_swarm_orchestrator_seeding.py"
      must_not_exist: true
  key_links:
    - from: "systemd"
      to: "(none)"
      via: "stop → disable → rm unit → daemon-reload → reset-failed"
      pattern: "no remaining swarm-orchestrator unit reference"
---

<objective>
Delete the dead `swarm_orchestrator_agent` service (D-01, D-02) and its systemd unit (D-09 watchdog defect cleaned by deletion). The service has been running since 2026-04-24 with zero contributors registered in main(), consuming resources for no purpose. Two test files that import from the deleted module must also be deleted to keep pytest CI-clean.

Purpose: Remove dead code competing with the active swarm dispatch service before any architectural refactor. This is a fully reversible, isolated operation.
Output: Service stopped and disabled, unit file removed, source file deleted, two test files deleted, systemd daemon reloaded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-CONTEXT.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-RESEARCH.md
@services/swarm_orchestrator_agent.py
@tests/unit/service_tests/test_swarm_orchestrator_agent.py
@tests/unit/service_tests/test_swarm_orchestrator_seeding.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stop, disable, and remove the swarm_orchestrator systemd unit</name>
  <files>/etc/systemd/system/indicagent-swarm-orchestrator.service</files>
  <read_first>
    - CLAUDE.md (Infrastructure / Sudo / systemd unit conventions; user_sudo_password.md exists for sudo password if prompts arrive)
    - 73-RESEARCH.md "Systemd Service Changes" section
    - Current systemd state: `systemctl status indicagent-swarm-orchestrator` BEFORE acting (capture output to confirm we are deleting the right unit; do not skip).
  </read_first>
  <action>
    Execute the EXACT sequence below in this order (each step is required; do NOT skip daemon-reload or reset-failed):

    1. `systemctl status indicagent-swarm-orchestrator --no-pager || true` (capture state before)
    2. `sudo systemctl stop indicagent-swarm-orchestrator`
    3. `sudo systemctl disable indicagent-swarm-orchestrator`
    4. `sudo rm -f /etc/systemd/system/indicagent-swarm-orchestrator.service`
    5. `sudo systemctl daemon-reload`
    6. `sudo systemctl reset-failed`

    If sudo prompts for a password, read it from `~/.claude/projects/-home-bg-dev-indicagent/memory/user_sudo_password.md` per CLAUDE.md / MEMORY.md `user_sudo_password.md` reference. Do not log the password.

    Per D-09: this also eliminates the `WatchdogSec=60` + `NotifyAccess=main` defect because the unit file containing them is gone.
  </action>
  <verify>
    <automated>
      systemctl list-units --all | grep -c "swarm-orchestrator" 2>/dev/null | head -1 | grep -qx "0" \
        && ! test -f /etc/systemd/system/indicagent-swarm-orchestrator.service \
        && systemctl status indicagent-swarm-orchestrator 2>&1 | grep -q "could not be found\|not-found\|not be found" \
        && echo "PASS: swarm-orchestrator unit removed"
    </automated>
  </verify>
  <acceptance_criteria>
    - `systemctl list-units --all | grep -c swarm-orchestrator` outputs `0`
    - `test -f /etc/systemd/system/indicagent-swarm-orchestrator.service` exits non-zero (file gone)
    - `systemctl status indicagent-swarm-orchestrator` reports unit "not found" (or equivalent)
    - No other systemd unit was touched (verify by `systemctl list-units --all | grep indicagent | wc -l` is unchanged minus 1)
  </acceptance_criteria>
  <done>swarm-orchestrator systemd unit fully removed: stopped, disabled, file deleted, daemon reloaded, failed-state cleared.</done>
</task>

<task type="auto">
  <name>Task 2: Delete service source file and its dependent test files</name>
  <files>
    services/swarm_orchestrator_agent.py,
    tests/unit/service_tests/test_swarm_orchestrator_agent.py,
    tests/unit/service_tests/test_swarm_orchestrator_seeding.py
  </files>
  <read_first>
    - 73-RESEARCH.md "Files Being Deleted" + "Pitfall 2: Tests Reference Deleted Service File"
    - Confirm by grep that no other production code still imports from `services.swarm_orchestrator_agent`:
      `grep -rn "swarm_orchestrator_agent\|SwarmOrchestratorComputeAgent" src/ services/ tests/ --include="*.py" | grep -v "test_swarm_orchestrator"`
      Expected: zero matches outside the test files that we are also deleting. If any non-test match appears, STOP and report — do not delete.
  </read_first>
  <action>
    1. Run the grep audit above. If anything outside the two doomed test files imports the orchestrator, abort and surface to the human.
    2. Delete the three files (no sudo required, all in repo):
       - `rm services/swarm_orchestrator_agent.py`
       - `rm tests/unit/service_tests/test_swarm_orchestrator_agent.py`
       - `rm tests/unit/service_tests/test_swarm_orchestrator_seeding.py`
    3. Run `.venv/bin/pytest tests/unit/service_tests/ -q --collect-only 2>&1 | tail -20` to confirm pytest still collects without import errors from the deleted module.
    4. Run `.venv/bin/pytest tests/unit/ -q --tb=line 2>&1 | tail -20` to confirm overall unit suite still collects (failures unrelated to this plan are acceptable per RESEARCH baseline of 22 pre-existing failures).

    Per CLAUDE.md naming, no file is renamed — these are pure deletions.
  </action>
  <verify>
    <automated>
      ! test -f services/swarm_orchestrator_agent.py \
        && ! test -f tests/unit/service_tests/test_swarm_orchestrator_agent.py \
        && ! test -f tests/unit/service_tests/test_swarm_orchestrator_seeding.py \
        && [ "$(grep -rln 'from services.swarm_orchestrator_agent\|import services.swarm_orchestrator_agent' --include='*.py' . | wc -l)" = "0" ] \
        && .venv/bin/pytest tests/unit/service_tests/ -q --collect-only > /dev/null 2>&1 \
        && echo "PASS: orchestrator source + tests deleted, no dangling imports"
    </automated>
  </verify>
  <acceptance_criteria>
    - Three files do not exist
    - `grep -rln 'from services.swarm_orchestrator_agent\|import services.swarm_orchestrator_agent' --include='*.py' .` returns no matches
    - `.venv/bin/pytest tests/unit/service_tests/ -q --collect-only` exits 0 (collection clean)
    - Baseline test pass count from RESEARCH (87 swarm/narrative/LLM-related) is preserved (no NEW pre-deletion-passing tests fail because of these deletions)
  </acceptance_criteria>
  <done>Source and dependent test files deleted; pytest collection clean; no dangling references in the codebase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → systemd | sudo is required to modify unit files; password handled per CLAUDE.md |
| Repo → file system | Source file deletes are idempotent and reversible via git history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01-01 | Tampering | systemd unit removal | mitigate | Capture `systemctl status` before deletion; explicit stop+disable+daemon-reload+reset-failed sequence prevents partial state |
| T-73-01-02 | Denial of service | Accidentally deleting active service | mitigate | RESEARCH confirmed orchestrator has zero contributors; grep audit before source deletion confirms no live importer |
| T-73-01-03 | Repudiation | Lost audit of action | accept | Git log + systemd journal preserve full trail; deletion is reversible from git history |
</threat_model>

<verification>
- `systemctl list-units --all | grep -c swarm-orchestrator` = 0
- Three files do not exist on disk
- `grep -rln 'swarm_orchestrator_agent' --include='*.py' .` returns no matches in production code
- `.venv/bin/pytest tests/unit/service_tests/ -q --collect-only` exits 0
- Other indicagent units (intelligence-pipeline, ibkr-provider, bar-aggregator, etc.) remain active and unaffected
</verification>

<success_criteria>
- swarm_orchestrator service is dead, unit file removed, daemon reloaded
- Source file and two test files deleted
- Pytest collection clean (no ImportError due to missing module)
- Plan executable in <5 minutes total
</success_criteria>

<output>
After completion, create `.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-01-SUMMARY.md`
</output>
