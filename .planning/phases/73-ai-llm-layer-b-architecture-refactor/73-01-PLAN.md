---
phase: 73-ai-llm-layer-b-architecture-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /etc/systemd/system/indicagent-swarm-orchestrator.service
  - services/swarm_orchestrator_agent.py
  - tests/unit/service_tests/test_swarm_orchestrator_agent.py
  - tests/unit/service_tests/test_swarm_orchestrator_seeding.py
autonomous: true
requirements: [D-01, D-02, D-09]
must_haves:
  truths:
    - "Dead swarm_orchestrator service is stopped, disabled, and removed from systemd"
    - "swarm_orchestrator_agent.py source file no longer exists"
    - "Tests referencing the deleted service are removed (no import errors)"
    - "87 baseline tests still pass after deletion"
  artifacts:
    - path: "services/swarm_orchestrator_agent.py"
      provides: "MUST NOT EXIST after this plan"
    - path: "/etc/systemd/system/indicagent-swarm-orchestrator.service"
      provides: "MUST NOT EXIST after this plan"
  key_links:
    - from: "systemctl"
      to: "indicagent-swarm-orchestrator"
      via: "systemd"
      pattern: "Unit indicagent-swarm-orchestrator.service could not be found"
---

<objective>
Stop, disable, and delete the dead `swarm_orchestrator_agent` service (PID 281147, currently running with zero contributors). Remove its systemd unit file (which violates CLAUDE.md watchdog discipline with `WatchdogSec=60` + `NotifyAccess=main` but no sd_notify). Delete the two test files that import from it (would fail pytest collection otherwise). This is Wave 1, Plan 01 — no dependencies, must complete before any agent migration touches the swarm tree.

Per D-01, D-02, D-09. The service has been "active (running)" since 2026-04-24 doing nothing — this is the highest-priority defect.

Purpose: Remove a running-but-useless service before any other refactor touches the codebase.
Output: System with no orchestrator service, no unit file, no test files referencing it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-CONTEXT.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stop, disable, and delete swarm_orchestrator systemd unit</name>
  <files>/etc/systemd/system/indicagent-swarm-orchestrator.service</files>
  <read_first>
    - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-RESEARCH.md (sections "Files Being Deleted", "Pitfall 3", "Systemd Service Changes")
    - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-CONTEXT.md (D-01, D-02, D-09; locked decision #7 about live service)
    - CLAUDE.md (systemd watchdog discipline rule)
    - Verify current state: `sudo systemctl status indicagent-swarm-orchestrator`
  </read_first>
  <action>
    Per D-01, D-02, D-09: stop and remove the dead orchestrator service.

    Run, in this exact order (sequential — order matters):
    1. `sudo systemctl stop indicagent-swarm-orchestrator` (must succeed before disable)
    2. `sudo systemctl disable indicagent-swarm-orchestrator` (removes /etc/systemd/system/multi-user.target.wants symlink)
    3. `sudo rm /etc/systemd/system/indicagent-swarm-orchestrator.service`
    4. `sudo systemctl daemon-reload` (clears systemd's unit-file cache; without this, `systemctl status` may still show the old unit)
    5. `sudo systemctl reset-failed indicagent-swarm-orchestrator 2>/dev/null || true` (clear failed-unit residue if any)

    Sudo password is documented in `user_sudo_password.md` per project memory — agent runs autonomously per `feedback_no_human_checkpoints.md`.

    Do NOT delete the source file `services/swarm_orchestrator_agent.py` in this task — Task 2 owns that. This task is systemd-only.
  </action>
  <verify>
    <automated>systemctl status indicagent-swarm-orchestrator 2>&amp;1 | grep -qE "could not be found|not-found|not loaded" &amp;&amp; ! [ -e /etc/systemd/system/indicagent-swarm-orchestrator.service ] &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `systemctl status indicagent-swarm-orchestrator` returns exit code != 0 with message containing "could not be found" OR "not-found" OR "not loaded"
    - `ls /etc/systemd/system/indicagent-swarm-orchestrator.service` returns "No such file or directory"
    - `systemctl is-enabled indicagent-swarm-orchestrator 2>&1` returns "not-found" or "Failed to get unit file state"
    - `systemctl is-active indicagent-swarm-orchestrator 2>&1` returns "inactive" or "unknown" (NOT "active")
    - `pgrep -af swarm_orchestrator_agent.py` returns nothing (PID 281147 gone)
  </acceptance_criteria>
  <done>Service stopped, disabled, unit file removed, daemon reloaded, no orchestrator process running.</done>
</task>

<task type="auto">
  <name>Task 2: Delete swarm_orchestrator_agent.py source + dependent test files</name>
  <files>
    services/swarm_orchestrator_agent.py
    tests/unit/service_tests/test_swarm_orchestrator_agent.py
    tests/unit/service_tests/test_swarm_orchestrator_seeding.py
  </files>
  <read_first>
    - services/swarm_orchestrator_agent.py (confirm it exists and has zero contributors registered in main())
    - tests/unit/service_tests/test_swarm_orchestrator_agent.py (confirm it imports `from services.swarm_orchestrator_agent ...`)
    - tests/unit/service_tests/test_swarm_orchestrator_seeding.py (confirm same import pattern)
    - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-RESEARCH.md (section "Pitfall 2: Tests Reference Deleted Service File")
  </read_first>
  <action>
    Delete the orchestrator source file and its two test files. These test files import `from services.swarm_orchestrator_agent import ...` and will fail pytest collection if left in place after the source is gone (per RESEARCH.md Pitfall 2).

    Run:
    ```
    rm services/swarm_orchestrator_agent.py
    rm tests/unit/service_tests/test_swarm_orchestrator_agent.py
    rm tests/unit/service_tests/test_swarm_orchestrator_seeding.py
    ```

    Verify no other file imports from the deleted module:
    ```
    grep -rn "swarm_orchestrator_agent" --include="*.py" . 2>/dev/null
    ```
    Expected: zero matches (or only matches within `.planning/`, `docs/`, `logs/` — non-Python contexts which are acceptable).

    Then run the baseline test suite to confirm nothing else broke:
    ```
    .venv/bin/pytest tests/unit/ -v --tb=short -q 2>&amp;1 | tail -30
    ```
    The total pass count must be ≥ baseline minus the two deleted test files. Per CONTEXT.md locked decision #9, the 87 swarm/narrative/LLM baseline tests must remain green.
  </action>
  <verify>
    <automated>! [ -e services/swarm_orchestrator_agent.py ] &amp;&amp; ! [ -e tests/unit/service_tests/test_swarm_orchestrator_agent.py ] &amp;&amp; ! [ -e tests/unit/service_tests/test_swarm_orchestrator_seeding.py ] &amp;&amp; ! grep -rn "from services.swarm_orchestrator_agent" --include="*.py" . 2>/dev/null &amp;&amp; ! grep -rn "import services.swarm_orchestrator_agent" --include="*.py" . 2>/dev/null &amp;&amp; .venv/bin/pytest tests/unit/test_swarm_dispatch.py tests/unit/test_skeptic_agent.py tests/unit/test_correlation_agent.py tests/unit/test_volume_agent.py tests/unit/test_narrative_orchestrator.py -q 2>&amp;1 | grep -E "passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - `services/swarm_orchestrator_agent.py` does not exist
    - `tests/unit/service_tests/test_swarm_orchestrator_agent.py` does not exist
    - `tests/unit/service_tests/test_swarm_orchestrator_seeding.py` does not exist
    - `grep -rn "from services.swarm_orchestrator_agent" --include="*.py" .` returns zero matches
    - `grep -rn "^import services.swarm_orchestrator_agent" --include="*.py" .` returns zero matches
    - Existing baseline tests (test_swarm_dispatch, test_skeptic_agent, test_correlation_agent, test_volume_agent, test_narrative_orchestrator) all PASS — no new failures introduced
  </acceptance_criteria>
  <done>Source file gone, two test files gone, baseline tests still green.</done>
</task>

</tasks>

<verification>
- `systemctl status indicagent-swarm-orchestrator` reports unit not found
- No file at `/etc/systemd/system/indicagent-swarm-orchestrator.service`
- No file at `services/swarm_orchestrator_agent.py`
- Two test files removed
- `grep -rn "swarm_orchestrator_agent" --include="*.py" .` is empty (Python source/test only)
- `.venv/bin/pytest tests/unit/test_swarm_dispatch.py tests/unit/test_skeptic_agent.py tests/unit/test_correlation_agent.py tests/unit/test_volume_agent.py tests/unit/test_narrative_orchestrator.py -q` passes
</verification>

<success_criteria>
The dead `swarm_orchestrator_agent` is gone from systemd, gone from source, gone from tests. Baseline tests still pass. System is ready for Wave 2 (build new infrastructure) without a competing orphaned service consuming a topic name and resource slot.
</success_criteria>

<output>
After completion, create `.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-01-SUMMARY.md`
</output>
