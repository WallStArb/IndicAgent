---
phase: 73-ai-llm-layer-b-architecture-refactor
plan: 05
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - src/intelligence/schemas.py
  - src/intelligence/swarm/aggregator.py
  - services/swarm_writer_agent.py
  - tests/unit/test_swarm_aggregator.py
  - tests/unit/test_swarm_writer_agent.py
autonomous: true
requirements: [AGENT-OUTPUT-MIGRATION]
must_haves:
  truths:
    - "AlphaMultiplier.contributors type is dict[str, AgentOutput] (not AgentResult)"
    - "SwarmAggregator.aggregate() accepts list[AgentOutput] and reads payload['multiplier']/payload['confidence']"
    - "swarm_writer_agent.py reads AgentOutput.payload fields when persisting"
    - "All three changes land atomically — no intermediate broken commit"
  artifacts:
    - path: "src/intelligence/schemas.py"
      provides: "AlphaMultiplier with AgentOutput contributors"
    - path: "src/intelligence/swarm/aggregator.py"
      provides: "SwarmAggregator reading from AgentOutput.payload"
    - path: "services/swarm_writer_agent.py"
      provides: "Writer reading AgentOutput.payload"
  key_links:
    - from: "SwarmAggregator.aggregate"
      to: "AgentOutput.payload"
      via: "payload['multiplier'] + payload['confidence']"
      pattern: "payload\\[.multiplier.\\]"
---

<objective>
Atomic AgentResult → AgentOutput migration (per CONTEXT.md locked decision #6: "atomic — never intermediate broken state"). Three coordinated changes:
1. `AlphaMultiplier.contributors` type changes from `dict[str, AgentResult]` → `dict[str, AgentOutput]`
2. `SwarmAggregator.aggregate()` accepts `list[AgentOutput]`, reads `output.payload["multiplier"]` and `output.payload["confidence"]`
3. `swarm_writer_agent.py` reads `AgentOutput` fields when persisting

Plan 05 and Plan 06 are both Wave 3 and must execute back-to-back. Plan 05 unblocks Plan 06 (the agent moves) by ensuring the aggregator/writer accept AgentOutput before agents start producing them.

Per CONTEXT.md decision #6 + RESEARCH.md "AgentResult → AgentOutput Schema Change".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-CONTEXT.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-RESEARCH.md
@.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-PATTERNS.md
@src/intelligence/schemas.py
@src/intelligence/swarm/aggregator.py
@services/swarm_writer_agent.py
@src/core/ai/output.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic migration of AlphaMultiplier + SwarmAggregator + swarm_writer_agent</name>
  <files>
    src/intelligence/schemas.py
    src/intelligence/swarm/aggregator.py
    services/swarm_writer_agent.py
    tests/unit/test_swarm_aggregator.py
    tests/unit/test_swarm_writer_agent.py
  </files>
  <read_first>
    - src/intelligence/schemas.py (lines 870-920 — AgentResult and AlphaMultiplier; note `contributors: dict[str, AgentResult]`)
    - src/intelligence/swarm/aggregator.py (full file — every site reading `result.multiplier`, `result.confidence`, `result.path`, `result.shadow_only`)
    - services/swarm_writer_agent.py (full file — every site reading AgentResult fields)
    - src/core/ai/output.py (Plan 02 output — AgentOutput shape: payload is dict[str, Any]; no top-level path/multiplier/confidence)
    - .planning/phases/73-ai-llm-layer-b-architecture-refactor/73-RESEARCH.md (section "AgentResult → AgentOutput Schema Change" + Open Question #1: drop path_a/path_b split, all agents are LLM)
    - tests/unit/test_swarm_aggregator.py (existing fixtures using AgentResult)
    - tests/unit/test_swarm_writer_agent.py (existing fixtures)
  </read_first>
  <action>
    All three edits in one task. Run tests AFTER all three are applied to avoid intermediate-broken-state commits.

    **Edit 1 — `src/intelligence/schemas.py`:**
    - Add import at top of swarm-section: `from src.core.ai.output import AgentOutput`
    - Change `AlphaMultiplier.contributors: dict[str, AgentResult]` → `dict[str, AgentOutput]`
    - Keep `AgentResult` class definition (Phase 72 signal_transform_log code may still reference it — do not delete; just stop using in AlphaMultiplier)

    **Edit 2 — `src/intelligence/swarm/aggregator.py`:**
    - Replace `from src.intelligence.schemas import AgentResult` with `from src.core.ai.output import AgentOutput`
    - Update `aggregate()` signature from `(path_a: list[AgentResult], path_b: list[AgentResult]) -> AlphaMultiplier` to `(outputs: list[AgentOutput]) -> AlphaMultiplier` (per RESEARCH.md Open Q1: drop path split)
    - Replace `result.multiplier` → `output.payload.get("multiplier", 1.0)`
    - Replace `result.confidence` → `output.payload.get("confidence", 0.0)`
    - Drop any `result.path` references (no longer exists; if filter logic existed for path-based selection, replace with `output.group == "alpha"`)
    - Skip outputs where `output.output_type == "neutral"` or `output.error is not None` from aggregation (don't pollute average with errored outputs)
    - Build `contributors: dict[str, AgentOutput]` keyed by `output.agent_id`

    **Edit 3 — `services/swarm_writer_agent.py`:**
    - Replace AgentResult import with `from src.core.ai.output import AgentOutput`
    - When deserializing Kafka payload: `AgentOutput.model_validate(payload_dict)`
    - Replace every `result.multiplier` → `output.payload.get("multiplier", 1.0)`
    - Replace every `result.confidence` → `output.payload.get("confidence", 0.0)`
    - Replace every `result.metadata` → `output.payload` (the whole payload IS the metadata under the new design)
    - Drop or remap `result.path` references (use `output.group` if a string is needed)
    - Update DB column writes accordingly — the column schema in `signal_transform_log` and any swarm tables stays identical; we're just changing which Python attribute supplies each value

    **Edit 4 — `tests/unit/test_swarm_aggregator.py`:**
    - Replace every fixture `AgentResult(agent_id="x", path="llm_swarm", multiplier=1.2, confidence=0.7, shadow_only=True, ...)` with `AgentOutput(agent_id="x", group="alpha", signal_id=uuid4(), symbol="ES", timeframe="5m", ts=datetime.now(UTC), output_type="multiplier", payload={"multiplier": 1.2, "confidence": 0.7}, shadow_only=True, latency_ms=10.0)`
    - Collapse path_a/path_b setup into a single `outputs` list
    - Update assertions reading `result.multiplier` → `output.payload["multiplier"]`

    **Edit 5 — `tests/unit/test_swarm_writer_agent.py`:**
    - Same fixture migration pattern. Update writer-call assertions.

    Confirm scope of AgentResult uses — only the three swarm/narrative-paths migrate; signal_transform_log code in Phase 72 may still validly use AgentResult:
    ```
    grep -rln "AgentResult" tests/unit/ src/intelligence/swarm/ services/ src/core/
    ```
    Migrate ONLY the swarm aggregator + swarm writer + their tests. Leave Phase 72 signal_transform paths alone.

    Plan 06 (next plan, same wave) updates the alpha agents to return AgentOutput. Tests for skeptic/correlation/volume agents are NOT run here; Plan 06 owns them. Must-run for Plan 05: test_swarm_aggregator + test_swarm_writer_agent only.

    Per CLAUDE.md: maintain UTC timestamps in test fixtures (`datetime.now(UTC)`).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_swarm_aggregator.py tests/unit/test_swarm_writer_agent.py -v --tb=short 2>&amp;1 | tail -30 ; ! grep -nE "result\.multiplier|result\.confidence|result\.path" src/intelligence/swarm/aggregator.py services/swarm_writer_agent.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "dict\[str, AgentOutput\]" src/intelligence/schemas.py` returns at least one match (AlphaMultiplier.contributors)
    - `grep -E "from src\.core\.ai\.output import AgentOutput" src/intelligence/schemas.py src/intelligence/swarm/aggregator.py services/swarm_writer_agent.py` returns 3 matches (one per file)
    - `! grep -nE "result\.multiplier|result\.confidence|result\.path" src/intelligence/swarm/aggregator.py` returns nothing
    - `! grep -nE "result\.multiplier|result\.confidence|result\.path" services/swarm_writer_agent.py` returns nothing
    - `grep -E "payload\.get\(.multiplier.|payload\[.multiplier.\]" src/intelligence/swarm/aggregator.py` returns at least one match
    - `grep -E "def aggregate\(self, outputs: list\[AgentOutput\]" src/intelligence/swarm/aggregator.py` returns one match (signature update)
    - `.venv/bin/pytest tests/unit/test_swarm_aggregator.py tests/unit/test_swarm_writer_agent.py -v` passes (zero failures)
  </acceptance_criteria>
  <done>Schema, aggregator, writer all updated atomically. Aggregator + writer tests pass. Plan 06 can proceed to update agents.</done>
</task>

</tasks>

<verification>
- aggregator + writer tests pass: `.venv/bin/pytest tests/unit/test_swarm_aggregator.py tests/unit/test_swarm_writer_agent.py -v`
- No residual `result.multiplier` / `result.confidence` / `result.path` access in aggregator or writer
- AlphaMultiplier.contributors is `dict[str, AgentOutput]`
- (Note: skeptic/correlation/volume agent tests will fail until Plan 06 lands in same wave — this is expected)
</verification>

<success_criteria>
The schema migration is atomic. AlphaMultiplier, SwarmAggregator, and swarm_writer_agent all consume AgentOutput. Plan 06 can now move agents knowing the receiving infrastructure already accepts AgentOutput.
</success_criteria>

<output>
After completion, create `.planning/phases/73-ai-llm-layer-b-architecture-refactor/73-05-SUMMARY.md`
</output>
