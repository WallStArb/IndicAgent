---
phase: 117-patterncompletion-fix-data-pipeline-validation
plan: 02
type: execute
wave: 2
depends_on: ["117-01"]
requirements: [VAL-02]
files_modified:
  - src/observability/metrics.py
  - services/feature_parity_auditor.py
  - production/systemd/indicagent-feature-parity-auditor.service
  - production/systemd/indicagent-feature-parity-auditor.timer
  - services/service_auditor.py
  - tests/unit/services/test_feature_parity_auditor.py

autonomous: true

must_haves:
  truths:
    - "FeatureParityAuditor runs as a Type=oneshot service triggered every 5 minutes by a systemd timer"
    - "It queries intelligence_features (last 1 hour) for presence of dt_db_confidence, hs_confidence, tri_confidence in pattern_detections"
    - "On 100% NULL of any expected field (count==0 with total>0) it logs an error and sets the OTel gauge to the violation count"
    - "It emits job_completed_total{job=feature-parity-auditor, status=success|failure} and flushes metrics before exit"
  artifacts:
    - path: "src/observability/metrics.py"
      provides: "FEATURE_PARITY_NULL_FIELDS_TOTAL gauge + FEATURE_PARITY_AUDITS_RUN_TOTAL counter (module-level, meter 'indicagent.metrics')"
      contains: "feature_parity_null_fields_total"
    - path: "services/feature_parity_auditor.py"
      provides: "Oneshot auditor querying pattern_detections JSONB key presence + OTel metrics"
      contains: "feature-parity-auditor"
    - path: "production/systemd/indicagent-feature-parity-auditor.timer"
      provides: "5-minute timer"
      contains: "OnCalendar=*:0/5"
    - path: "production/systemd/indicagent-feature-parity-auditor.service"
      provides: "Type=oneshot service invoking the auditor"
      contains: "feature_parity_auditor.py"
  key_links:
    - from: "services/feature_parity_auditor.py"
      to: "intelligence_features.pattern_detections"
      via: "asyncpg JSONB ? key existence query bounded to last 1 hour"
      pattern: "pattern_detections \\? \\$1"
    - from: "services/feature_parity_auditor.py"
      to: "src.observability.metrics.FEATURE_PARITY_NULL_FIELDS_TOTAL"
      via: "import + .set(len(violations), {})"
      pattern: "FEATURE_PARITY_NULL_FIELDS_TOTAL"
    - from: "services/feature_parity_auditor.py"
      to: "JOB_COMPLETED_TOTAL"
      via: "add(1, {job: feature-parity-auditor, status})"
      pattern: "JOB_COMPLETED_TOTAL"
---

<objective>
Build `FeatureParityAuditor`: a timer-triggered oneshot that validates I5 pattern fields are persisted into the correct `pattern_detections` JSONB column after the Wave 1 fix. It is the regression guard for the 117-01 bug.

Purpose: Catch this class of write-path mapping bug within 5 minutes of it recurring. Queries the last hour only (TimescaleDB chunk exclusion keeps it fast on 2.7M+ rows).
Output: New module-level OTel instruments in metrics.py, new oneshot service, systemd .service + .timer pair, service registry entry, unit test for the audit logic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-patterncompletion-fix-data-pipeline-validation/117-RESEARCH.md
@.planning/phases/117-patterncompletion-fix-data-pipeline-validation/117-PATTERNS.md
@services/shadow_auditor.py
@src/observability/metrics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OTel instruments to metrics.py + create FeatureParityAuditor oneshot service</name>
  <files>src/observability/metrics.py, services/feature_parity_auditor.py</files>
  <read_first>
    - src/observability/metrics.py (lines 30-90 for point_gauge/create_counter helpers + flush_and_shutdown_metrics + JOB_COMPLETED_TOTAL; lines 267-277 for the SHADOW_* gauge/counter block to mirror exactly)
    - services/shadow_auditor.py (full file — copy imports including `from src.observability.metrics import (...)`, _amain/main entry-point structure, asyncpg pool acquisition, JOB_COMPLETED_TOTAL + flush_and_shutdown_metrics pattern, how SHADOW_* instruments are imported and used)
    - src/config/settings.py (confirm Settings().database_url and Settings().env_name attributes)
    - .planning/phases/117-patterncompletion-fix-data-pipeline-validation/117-PATTERNS.md (feature_parity_auditor.py section — exact imports, _run_audit query)
  </read_first>
  <action>
    Step A — Add instruments to `src/observability/metrics.py`, mirroring the SHADOW_* block (lines 267-277) exactly so they live under the canonical `indicagent.metrics` meter (NOT an inline `get_meter("indicagent")`):
    - Add `FEATURE_PARITY_NULL_FIELDS_TOTAL = point_gauge("feature_parity_null_fields_total", "Count of expected pattern fields that are 100% NULL in intelligence_features over the last hour")` using the existing `point_gauge()` helper (this is a point gauge, set on each run).
    - Add `FEATURE_PARITY_AUDITS_RUN_TOTAL = _meter.create_counter("feature_parity_audits_run_total", description="Feature-parity audit runs completed")` mirroring the SHADOW_TAIL_RISK_BLOCKED counter definition style.
    Place both near the SHADOW_* block (or in a clearly-labeled `# Feature parity auditor (Phase 117)` comment section).

    Step B — Create `services/feature_parity_auditor.py`, mirroring `services/shadow_auditor.py` structure exactly:
    - Imports: `_path_bootstrap`, asyncio, structlog, asyncpg, `from src.config.settings import Settings`, `from src.core.database_manager import create_pool as create_db_pool`, and `from src.observability.metrics import (FEATURE_PARITY_AUDITS_RUN_TOTAL, FEATURE_PARITY_NULL_FIELDS_TOTAL, JOB_COMPLETED_TOTAL, flush_and_shutdown_metrics)`. Do NOT call `_otel_metrics.get_meter(...)` inline — instruments come from metrics.py only.
    - `async def _run_audit(pool)`: `expected_fields = ["dt_db_confidence", "hs_confidence", "tri_confidence"]`. First fetch total row count with `SELECT COUNT(*) FROM intelligence_features WHERE ts >= NOW() - INTERVAL '1 hour'`; if total==0 log `feature_parity_audit.no_recent_rows` and return (still set gauge to 0 / increment run counter as appropriate before returning). For each field, fetchval `SELECT COUNT(*) FILTER (WHERE pattern_detections ? $1) FROM intelligence_features WHERE ts >= NOW() - INTERVAL '1 hour'` with the field as bind param; append to `violations` when count==0. After loop: `FEATURE_PARITY_NULL_FIELDS_TOTAL.set(len(violations), {})` and `FEATURE_PARITY_AUDITS_RUN_TOTAL.add(1, {})`. Log `feature_parity_audit.violations_found` (fields, total_rows) at error level if violations, else `feature_parity_audit.clean`. Return the `violations` list (so it is unit-testable).
    - `async def _amain()`: `settings = Settings()`, `pool = await create_db_pool(settings.database_url, min_size=2, max_size=5)`, try `await _run_audit(pool)` finally `await pool.close()`.
    - `def main()`: try `asyncio.run(_amain())` then `JOB_COMPLETED_TOTAL.add(1, {"job": "feature-parity-auditor", "status": "success"})`; `except Exception as error:` add `{"job": "feature-parity-auditor", "status": "failure"}` then `raise error`; finally `flush_and_shutdown_metrics()`.
    - `if __name__ == "__main__": main()`.

    The `job` label MUST be exactly `"feature-parity-auditor"` (kebab-case matching the timer unit %n suffix). Exception variable MUST be named `error`. Use asyncpg JSONB natively (no json.loads). Use `setup_service_logging("logs/feature_parity_auditor.log")` if shadow_auditor does so; otherwise match shadow_auditor's logging setup exactly.
  </action>
  <verify>
    .venv/bin/python -c "import ast; ast.parse(open('services/feature_parity_auditor.py').read())" succeeds; `.venv/bin/python -c "from src.observability.metrics import FEATURE_PARITY_NULL_FIELDS_TOTAL, FEATURE_PARITY_AUDITS_RUN_TOTAL"` imports clean; grep confirms job label and JSONB query present.
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/observability/metrics.py` contains `FEATURE_PARITY_NULL_FIELDS_TOTAL = point_gauge(` and `FEATURE_PARITY_AUDITS_RUN_TOTAL = _meter.create_counter(`
    - Source assertion: `services/feature_parity_auditor.py` imports `FEATURE_PARITY_NULL_FIELDS_TOTAL` and `FEATURE_PARITY_AUDITS_RUN_TOTAL` from `src.observability.metrics` and contains NO `get_meter(` call
    - Source assertion: file contains `"job": "feature-parity-auditor"` exactly twice (success + failure paths)
    - Source assertion: file contains `pattern_detections ? $1` and `INTERVAL '1 hour'`
    - Source assertion: file contains `expected_fields = ["dt_db_confidence", "hs_confidence", "tri_confidence"]`
    - Source assertion: file contains `except Exception as error` and `flush_and_shutdown_metrics()` in a finally block
    - CLI: `.venv/bin/ruff check src/observability/metrics.py services/feature_parity_auditor.py` exits 0
  </acceptance_criteria>
  <done>Instruments live in metrics.py under the canonical meter; oneshot service parses, imports them, queries the correct column, emits OTel gauge + job completion counter, ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create systemd .service + .timer (5-minute) and register in _DAG_ORDER</name>
  <files>production/systemd/indicagent-feature-parity-auditor.service, production/systemd/indicagent-feature-parity-auditor.timer, services/service_auditor.py</files>
  <read_first>
    - production/systemd/indicagent-shadow-auditor.service (exact unit template)
    - production/systemd/indicagent-shadow-auditor.timer (exact timer template)
    - services/service_auditor.py (_DAG_ORDER dict, _LAG_THRESHOLDS, _AGENT_ID_TO_UNIT — confirm timer-triggered oneshots are registered by BARE service name with NO `.timer` suffix, e.g. `"indicagent-shadow-auditor"`; find the numeric ordering values)
  </read_first>
  <action>
    Create `production/systemd/indicagent-feature-parity-auditor.service` mirroring the shadow-auditor service: `[Unit]` Description="IndicAgent Feature Parity Auditor", `After=network.target indicagent-infrastructure.target`, `Requires=indicagent-infrastructure.target`; `[Service]` `Type=oneshot`, `WorkingDirectory=/home/bg/dev/indicagent`, `ExecStart=/home/bg/dev/indicagent/.venv/bin/python services/feature_parity_auditor.py`, `Environment=PYTHONUNBUFFERED=1`; `[Install]` `WantedBy=multi-user.target`.

    Create `production/systemd/indicagent-feature-parity-auditor.timer`: `[Unit]` Description="Feature Parity Auditor Timer — every 5 minutes"; `[Timer]` `OnCalendar=*:0/5`, `Persistent=true`, `Unit=indicagent-feature-parity-auditor.service`; `[Install]` `WantedBy=timers.target`.

    In `services/service_auditor.py` add the BARE service name `"indicagent-feature-parity-auditor"` (NO `.timer` suffix) to `_DAG_ORDER`, matching the established convention for all existing timer-triggered oneshots (e.g. `"indicagent-shadow-auditor"`). Use ordering value 8 (or the next available slot consistent with existing oneshot timer entries). If `_LAG_THRESHOLDS` and `_AGENT_ID_TO_UNIT` have entries for oneshot timers, add matching entries; if oneshot timers are intentionally excluded from lag thresholds, follow the existing convention for other timer units (e.g. shadow-auditor) — do not invent a lag threshold that other timers do not have.
  </action>
  <verify>
    systemd-analyze verify production/systemd/indicagent-feature-parity-auditor.timer (if available) passes; grep confirms the bare-name _DAG_ORDER entry.
  </verify>
  <acceptance_criteria>
    - Source assertion: timer file contains `OnCalendar=*:0/5` and `Unit=indicagent-feature-parity-auditor.service`
    - Source assertion: service file contains `Type=oneshot` and `ExecStart=/home/bg/dev/indicagent/.venv/bin/python services/feature_parity_auditor.py`
    - Source assertion: `services/service_auditor.py` contains the BARE name `"indicagent-feature-parity-auditor"` (NOT `"indicagent-feature-parity-auditor.timer"`)
    - CLI: `grep -c '"indicagent-feature-parity-auditor"' services/service_auditor.py` returns >= 1
    - CLI: `grep -c '"indicagent-feature-parity-auditor.timer"' services/service_auditor.py` returns 0
  </acceptance_criteria>
  <done>Systemd pair created, oneshot registered in _DAG_ORDER by bare service name following the shadow-auditor convention.</done>
</task>

<task type="auto">
  <name>Task 3: Unit test the audit logic (violation detection on 100% NULL)</name>
  <files>tests/unit/services/test_feature_parity_auditor.py</files>
  <read_first>
    - services/feature_parity_auditor.py (the _run_audit function created in Task 1)
    - tests/unit/services/ (existing test conventions; how other tests mock asyncpg pool/conn — look for fetchval mock patterns)
    - services/shadow_auditor.py (if it has a companion test, mirror its mocking approach)
  </read_first>
  <action>
    Create `tests/unit/services/test_feature_parity_auditor.py`. Mock the asyncpg pool/connection so `pool.acquire()` yields a fake conn with a programmable `fetchval`. Write two tests:
    - `test_violation_when_field_missing`: total count returns a positive number; the per-field FILTER count returns 0 for `dt_db_confidence`. Assert `_run_audit` returns a violations list containing `dt_db_confidence` (or assert the patched `FEATURE_PARITY_NULL_FIELDS_TOTAL.set` was called with len(violations) >= 1).
    - `test_clean_when_all_fields_present`: total positive, all FILTER counts positive; assert `_run_audit` returns an empty violations list.
    Patch the module-level `FEATURE_PARITY_NULL_FIELDS_TOTAL` gauge and `FEATURE_PARITY_AUDITS_RUN_TOTAL` counter (monkeypatch on the module attribute) so `.set`/`.add` are spy-able. Use `pytest.mark.asyncio` consistent with other async tests in the suite.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/services/test_feature_parity_auditor.py -v passes.
  </verify>
  <acceptance_criteria>
    - Test command: `.venv/bin/pytest tests/unit/services/test_feature_parity_auditor.py -q` exits 0
    - Behavior assertion: test_violation_when_field_missing fails if _run_audit stops flagging count==0 fields
    - Behavior assertion: test_clean_when_all_fields_present asserts zero violations recorded
    - CLI: `.venv/bin/ruff check tests/unit/services/test_feature_parity_auditor.py` exits 0
  </acceptance_criteria>
  <done>Audit logic unit-tested for both violation and clean paths; tests green; ruff clean.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/services/test_feature_parity_auditor.py -q` green
- `.venv/bin/ruff check src/observability/metrics.py services/feature_parity_auditor.py tests/unit/services/test_feature_parity_auditor.py` clean
- `grep '"indicagent-feature-parity-auditor"' services/service_auditor.py` returns a bare-name match (no `.timer` suffix)
- Operator (post-install): `sudo systemctl start indicagent-feature-parity-auditor.service` then `tail logs/feature_parity_auditor.log` shows `feature_parity_audit.clean` (post-Wave-1) or `violations_found` (regression)
</verification>

<success_criteria>
VAL-02 satisfied: FeatureParityAuditor runs every 5 min via systemd timer, validates I5 pattern fields exist in pattern_detections, alerts (error log + OTel gauge) on 100% NULL, emits job_completed_total with the correct kebab-case job label. OTel instruments live in metrics.py under the canonical meter; _DAG_ORDER uses the bare service name.
</success_criteria>

<output>
After completion, create `.planning/phases/117-patterncompletion-fix-data-pipeline-validation/117-02-SUMMARY.md`
</output>
</content>
</invoke>
