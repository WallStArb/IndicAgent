---
phase: 117-patterncompletion-fix-data-pipeline-validation
plan: 00
type: execute
wave: 0
depends_on: []
requirements: [VAL-01, VAL-02, VAL-03]
files_modified:
  - src/intelligence/trading/cvd_divergence.py
  - src/intelligence/trading/ofi_continuation.py
  - src/intelligence/trading/gap_analysis_setup.py
  - src/intelligence/trading/divergence_stack.py
  - src/intelligence/trading/microstructure_utils.py
  - tests/unit/intelligence/test_i6_hmm_confidence_wiring.py
  - production/migrations/120_signal_probe_results.sql
  - services/signal_probe_auditor.py
  - production/systemd/indicagent-signal-probe-auditor.service
  - production/systemd/indicagent-signal-probe-auditor.timer
  - services/service_auditor.py
  - tests/unit/services/test_signal_probe_auditor.py
autonomous: true

must_haves:
  truths:
    - "_CVD_DIV_THRESHOLD in cvd_divergence.py is a nonzero conservative floor (0.002), so pure floating-point noise no longer qualifies as a CVD divergence"
    - "All 6 high-volume plugins (ofi_continuation, cvd_divergence, gap_analysis_setup, divergence_stack, ofi_spike via shared util, cvd_spike via shared util) add ctf_score as an additive confidence contributor instead of discarding I6 data"
    - "All 6 plugins fold hmm_regime_weight into confidence instead of using hmm_regime only for a logging string"
    - "ofi_spike and cvd_spike fetch and use I6 via the shared detect_spike_signal util (they had no I6 access before)"
    - "SignalProbeAuditor runs daily, samples ~1% of unselected NEEDS_REFACTOR signals, simulates activation+forward outcome from market_data_ohlcv, and writes pnl_r/mae/mfe/competing_signals_count rows to signal_probe_results"
    - "SignalProbeAuditor emits job_completed_total{job=signal-probe-auditor} on both success and failure paths"
  artifacts:
    - path: "src/intelligence/trading/cvd_divergence.py"
      provides: "Nonzero _CVD_DIV_THRESHOLD floor + ctf_score/hmm_regime_weight confidence wiring"
      contains: "_CVD_DIV_THRESHOLD: float = 0.002"
    - path: "src/intelligence/trading/microstructure_utils.py"
      provides: "Shared ctf_score + hmm_regime_weight confidence wiring for ofi_spike and cvd_spike"
      contains: "hmm_regime_weight"
    - path: "production/migrations/120_signal_probe_results.sql"
      provides: "signal_probe_results table for ground-truth simulated outcomes on unselected signals"
      contains: "CREATE TABLE IF NOT EXISTS signal_probe_results"
    - path: "services/signal_probe_auditor.py"
      provides: "Daily oneshot that force-simulates outcomes for sampled unselected signals"
      contains: "job-signal-probe-auditor"
    - path: "production/systemd/indicagent-signal-probe-auditor.timer"
      provides: "Daily systemd timer for the probe auditor"
      contains: "OnCalendar"
  key_links:
    - from: "src/intelligence/trading/cvd_divergence.py"
      to: "src/intelligence/utils/gradient_utils.py:hmm_regime_weight"
      via: "import + confidence factor"
      pattern: "hmm_regime_weight\\("
    - from: "services/signal_probe_auditor.py"
      to: "signal_probe_results table + market_data_ohlcv"
      via: "asyncpg SELECT unselected signals, simulate from OHLCV, INSERT probe rows"
      pattern: "INSERT INTO signal_probe_results"
    - from: "services/signal_probe_auditor.py"
      to: "src/observability/metrics.py:JOB_COMPLETED_TOTAL"
      via: "D-06 oneshot completion counter"
      pattern: "job.*signal-probe-auditor"
---

<objective>
Close all Phase 0 deterministic bugs from the signal-quality RCA in a single Wave 0 plan. These are correctness defects that require NO empirical data and are independent of the feature_writer write-path fix in 117-01, so they run first (or in parallel with Wave 1):

1. Fix `_CVD_DIV_THRESHOLD = 0.0` in `cvd_divergence.py` (a threshold of zero is not a threshold).
2. Wire already-computed-but-discarded I6 `ctf_score` and `hmm_regime_weight()` into confidence for 6 high-volume broken plugins (additive only — no change to fundamental signal logic, thresholds, or structure).
3. Build `SignalProbeAuditor` — a new daily oneshot that simulates outcomes for a 1% sample of unselected NEEDS_REFACTOR signals, generating ground-truth data so Phase 117.5 can derive empirical thresholds instead of guessing.

Purpose: Stop noise at the source (CVD floor), make the 6 NEEDS_REFACTOR setups consume the cross-timeframe + regime data they already fetch (VAL-01 upstream data quality), and begin collecting outcome ground truth (VAL-02/VAL-03).
Output: 4 plugin/util edits + 1 wiring test; 1 migration + 1 oneshot service + systemd pair + DAG registration + 1 service test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-patterncompletion-fix-data-pipeline-validation/117-RESEARCH.md
@.planning/phases/117-patterncompletion-fix-data-pipeline-validation/117-PATTERNS.md
@docs/plans/2026-06-07-signal-quality-crisis-root-cause-analysis.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CVD threshold floor + wire ctf_score and hmm_regime_weight into the 4 stateful plugins</name>
  <files>src/intelligence/trading/cvd_divergence.py, src/intelligence/trading/ofi_continuation.py, src/intelligence/trading/gap_analysis_setup.py, src/intelligence/trading/divergence_stack.py</files>
  <read_first>
    - src/intelligence/trading/cvd_divergence.py (the `_CVD_DIV_THRESHOLD: float = 0.0` constant at line 27; the `features` merge at lines 69-79; the `hmm_regime = features.get("hmm_regime")` logging-only usage at line 141; the `raw_conf` computation at lines 127-132)
    - src/intelligence/trading/ofi_continuation.py (features merge lines 64-72; `raw_conf = 0.50 + abs(ofi_ewma) * 0.001` at line 120; hmm_regime logging-only at line 112)
    - src/intelligence/trading/gap_analysis_setup.py (features merge lines 67-75; `base` confidence build at lines 135-148; note this plugin builds `regime_context="gap_open"` not from hmm)
    - src/intelligence/trading/divergence_stack.py (features merge lines 98-106; `raw_div_conf = weighted_score / DIVERGENCE_CONFIDENCE_NORM` at line 237)
    - src/intelligence/utils/gradient_utils.py (hmm_regime_weight signature at line 250 — takes a DIRECTION string "up"/"down"/"ranging", NOT the plugin's regime_type; _HMM_KEY_MAP at line 34)
    - src/intelligence/trading/confidence_utils.py (compose_confidence at line 32 — clamps to [0, CONF_CEIL]; all confidence must still route through this LAST)
  </read_first>
  <action>
    Make these ADDITIVE-ONLY changes. Do not alter any gate threshold, bar-count requirement, direction logic, or signal structure. The only behavioral change is to the raw confidence value before `compose_confidence()`.

    A. cvd_divergence.py constant fix:
    - Change `_CVD_DIV_THRESHOLD: float = 0.0` to `_CVD_DIV_THRESHOLD: float = 0.002` and update the inline comment to "conservative floor; eliminates float noise (empirical value derived in Phase 117.5)".
    - In compute_full, the existing `if cvd_div == 0.0:` reset-and-no_signal gate must become `if abs(cvd_div) < _CVD_DIV_THRESHOLD:` so the new floor is actually enforced (the constant is currently declared but never compared). Keep the `reset_consecutive_state` + `return no_signal()` behavior inside that branch unchanged.

    B. Shared ctf_score + hmm helper. Add an import of `hmm_regime_weight` from `..utils.gradient_utils` to each of the 4 files. For the additive confidence math, write a tiny local helper OR inline the same expression in each file (keep it identical across files for grep-ability):
    - ctf contribution: read `ctf_score = float(features.get("ctf_score", 0.0))`. When `abs(ctf_score) > 0.3`, add `0.15 * min(1.0, abs(ctf_score) / 0.7)` to the raw confidence. When `abs(ctf_score) <= 0.3`, add nothing.
    - hmm contribution: map the SIGNAL direction (the local `direction` int, 1 or -1) to the hmm_regime_weight direction string: `"up"` if direction == 1 else `"down"`. Compute `regime_w = hmm_regime_weight(features, "up" if direction == 1 else "down")` (returns [0,1], 0.5 neutral when missing). Fold it in as a centered factor: add `0.10 * (regime_w - 0.5)` to raw confidence (so 0.5 neutral = no change, high-conviction regime = up to +0.05, contra-regime = down to -0.05). Do NOT replace the existing `regime_context` logging string — leave that line as-is; this is purely a confidence addition.

    C. Apply to each plugin at the point just before its existing `compose_confidence(...)` call:
    - cvd_divergence.py: after `raw_conf += extra_bars * 0.05` (line ~131), add the ctf and hmm contributions to `raw_conf`, then the existing `confidence = compose_confidence(raw_conf)` clamps it.
    - ofi_continuation.py: after `raw_conf, supporting = apply_exhaustion_guard(...)` (line 121), add ctf+hmm contributions to `raw_conf` before `compose_confidence`.
    - gap_analysis_setup.py: after `base, supporting = apply_exhaustion_boost(...)` (line 148), add ctf+hmm contributions to `base` before `compose_confidence(base)`. Use the post-fade-resolution `direction` for the hmm direction string.
    - divergence_stack.py: after `raw_div_conf, supporting_factors = apply_exhaustion_guard(...)` (line 238), add ctf+hmm contributions to `raw_div_conf` before `compose_confidence`.

    D. Append a supporting_factors entry on each plugin when ctf actually contributes (e.g. `f"ctf_score={ctf_score:.3f}"`) so the wiring is observable in logged signals. Optional but preferred for instrumentation.

    Confidence must still pass through `compose_confidence()` last (it clamps to [0, CONF_CEIL]); the additive terms can push raw above 1.0 and the clamp handles it.
  </action>
  <verify>
    grep -n "_CVD_DIV_THRESHOLD: float = 0.002" src/intelligence/trading/cvd_divergence.py and grep -c "hmm_regime_weight(" across the 4 files returns 4+ matches; grep "ctf_score" each of the 4 files returns a confidence-contribution line (not just the discarded features merge).
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "_CVD_DIV_THRESHOLD: float = 0.002" src/intelligence/trading/cvd_divergence.py` matches
    - Source: `grep -n "abs(cvd_div) < _CVD_DIV_THRESHOLD" src/intelligence/trading/cvd_divergence.py` matches (constant now enforced, not just declared)
    - Source: `grep -rl "from ..utils.gradient_utils import" src/intelligence/trading/{cvd_divergence,ofi_continuation,gap_analysis_setup,divergence_stack}.py` lists all 4 OR each file imports hmm_regime_weight
    - Source: `grep -c "hmm_regime_weight(" src/intelligence/trading/cvd_divergence.py src/intelligence/trading/ofi_continuation.py src/intelligence/trading/gap_analysis_setup.py src/intelligence/trading/divergence_stack.py` shows >=1 per file
    - Source: each of the 4 files contains a `ctf_score` read used in a confidence add (`0.15 * min(1.0, abs(ctf_score)`)
    - Behavior: no gate threshold, bar-count constant, or direction expression was changed (only confidence math + the CVD floor enforcement)
    - CLI: `.venv/bin/ruff check src/intelligence/trading/cvd_divergence.py src/intelligence/trading/ofi_continuation.py src/intelligence/trading/gap_analysis_setup.py src/intelligence/trading/divergence_stack.py` exits 0
  </acceptance_criteria>
  <done>CVD floor is a nonzero enforced threshold; all 4 stateful plugins fold ctf_score and hmm_regime_weight into raw confidence before compose_confidence; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ctf_score + hmm_regime_weight into the shared spike util (covers ofi_spike and cvd_spike) and add wiring test</name>
  <files>src/intelligence/trading/microstructure_utils.py, tests/unit/intelligence/test_i6_hmm_confidence_wiring.py</files>
  <read_first>
    - src/intelligence/trading/microstructure_utils.py (full file — `detect_spike_signal` already merges i6 into `features` at lines 49-57; computes `confidence = compose_confidence(0.50 + abs(spike_z) * 0.05)` at line 77; uses hmm_regime only for the `regime_context` string at line 84)
    - src/intelligence/trading/ofi_spike.py and src/intelligence/trading/cvd_spike.py (both delegate entirely to detect_spike_signal — so wiring goes in the shared util, NOT in the two plugin files; do not edit the plugin files)
    - src/intelligence/utils/gradient_utils.py (hmm_regime_weight at line 250)
    - tests/unit/intelligence/ (find an existing plugin unit test for fixture/frames-dict construction conventions; mirror its header and how it builds a `frames` dict with i1/i6 sub-dicts and a small OHLCV DataFrame)
  </read_first>
  <action>
    A. microstructure_utils.py: add `from ..utils.gradient_utils import hmm_regime_weight` import. In `detect_spike_signal`, after `direction = 1 if spike_z > 0 else -1` and before/at the `confidence = compose_confidence(...)` line, build the raw confidence first as `raw = 0.50 + abs(spike_z) * 0.05`, then apply the SAME additive ctf + hmm contributions as Task 1 (ctf_score read from `features`, `+= 0.15 * min(1.0, abs(ctf_score)/0.7)` when `abs(ctf_score) > 0.3`; hmm `regime_w = hmm_regime_weight(features, "up" if direction == 1 else "down")`, `+= 0.10 * (regime_w - 0.5)`), then `confidence = compose_confidence(raw)`. Leave the `regime_context` logging string and all gates (`abs(spike_z) <= _SPIKE_THRESHOLD`) unchanged. Optionally append a `ctf_score=...` supporting factor when it contributes. Both ofi_spike and cvd_spike inherit this automatically — confirm no edits needed in those two files.

    B. tests/unit/intelligence/test_i6_hmm_confidence_wiring.py: write a focused unit test that proves the wiring raises confidence when I6/HMM are favorable and is neutral when they are absent. Strategy that avoids brittle full-pipeline setup:
    - For the shared spike path: call `detect_spike_signal` twice with identical frames except one has `i6 = {"ctf_score": 0.6}` and favorable hmm probs, the other has empty i6 and no hmm keys. Assert the favorable run's `confidence` strictly exceeds the bare run's `confidence` (both must produce a signal — pick a `spike_z` like 3.0 above threshold). Build the minimal `frames` dict (main DataFrame with >= min_lookback rows, `i1` carrying the spike_z + a usable ATR feature so `get_atr_with_floor_from_frames` returns non-None).
    - For at least one stateful plugin (e.g. cvd_divergence): assert that with `abs(cvd_div) < 0.002` the plugin returns no_signal (new floor enforced), and with a clearly-diverging value over N confirmation bars it can fire. Keep this assertion tight; if full firing setup is heavy, at minimum assert the sub-threshold case returns the no_signal dict.
    Use real feature key names confirmed from the source files and gradient_utils (`ctf_score`, `hmm_prob_trending_up`, `hmm_prob_trending_down`). Do not invent keys.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/intelligence/test_i6_hmm_confidence_wiring.py -v passes; grep -n "hmm_regime_weight" src/intelligence/trading/microstructure_utils.py matches.
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "hmm_regime_weight" src/intelligence/trading/microstructure_utils.py` matches
    - Source: `grep -n "ctf_score" src/intelligence/trading/microstructure_utils.py` shows a confidence-contribution read
    - Source: ofi_spike.py and cvd_spike.py UNCHANGED (`git diff --stat` shows no changes to those two files)
    - Test: `.venv/bin/pytest tests/unit/intelligence/test_i6_hmm_confidence_wiring.py -q` exits 0
    - Behavior: test asserts favorable-I6 confidence > bare confidence for the spike path
    - Behavior: test asserts cvd_divergence sub-threshold (`abs(cvd_div) < 0.002`) returns no_signal
    - CLI: `.venv/bin/ruff check src/intelligence/trading/microstructure_utils.py tests/unit/intelligence/test_i6_hmm_confidence_wiring.py` exits 0
  </acceptance_criteria>
  <done>Shared spike util folds ctf_score + hmm_regime_weight into confidence (ofi_spike/cvd_spike inherit it, unmodified); a unit test pins the wiring and the new CVD floor.</done>
</task>

<task type="auto">
  <name>Task 3: SignalProbeAuditor migration + daily oneshot service + systemd pair + DAG registration + test</name>
  <files>production/migrations/120_signal_probe_results.sql, services/signal_probe_auditor.py, production/systemd/indicagent-signal-probe-auditor.service, production/systemd/indicagent-signal-probe-auditor.timer, services/service_auditor.py, tests/unit/services/test_signal_probe_auditor.py</files>
  <read_first>
    - services/shadow_auditor.py (lines 1-50 imports/header; the `_amain()` Settings + create_db_pool pattern; the `main()` JOB_COMPLETED_TOTAL success/failure + flush_and_shutdown_metrics finally block — mirror this exactly)
    - production/systemd/indicagent-shadow-auditor.service and .timer (exact unit structure to clone; Type=oneshot)
    - production/migrations/119_framing_audit_trail.sql (migration header comment format + `Run with: PGPASSWORD=postgres psql ... -f` line; latest migration is 119 so this is 120)
    - services/service_auditor.py (the `_DAG_ORDER` dict around line 57; how `indicagent-shadow-auditor` is registered at line 96 and 172; add the probe auditor following the same oneshot pattern — also check `_LAG_THRESHOLDS` and `_AGENT_ID_TO_UNIT` per CLAUDE.md service-registry rule and update if oneshots appear there)
    - signal_ledger_full view columns (verified): signal_id, timestamp, symbol, timeframe, setup_plugin, direction, was_selected, is_shadow, status, activated_at, entry_zone_low, entry_zone_high, ttl_bars, feature_tf
    - market_data_ohlcv columns (verified): timestamp, symbol, timeframe, open, high, low, close, volume
    - docs/plans/2026-06-07-signal-quality-crisis-root-cause-analysis.md (lines 1032-1048 — SignalProbeAuditor spec: SAMPLE_RATE=0.01, MIN_SAMPLE_N=100, simulate activation + forward outcome, NEEDS_REFACTOR setups; Appendix A near line 1184 lists the 21 NEEDS_REFACTOR setup names)
  </read_first>
  <action>
    A. Migration 120 (`production/migrations/120_signal_probe_results.sql`): create `signal_probe_results` (a plain table is simpler than a JSONB column on the partitioned signal_ledger; choose the table). Columns: `signal_id uuid NOT NULL`, `probed_at timestamptz NOT NULL DEFAULT now()`, `symbol text NOT NULL`, `timeframe text NOT NULL`, `setup_plugin text NOT NULL`, `direction integer NOT NULL`, `sim_activated boolean NOT NULL`, `sim_entry_price double precision`, `sim_exit_price double precision`, `sim_pnl_r double precision`, `sim_mae double precision`, `sim_mfe double precision`, `sim_bars_in_trade integer`, `sim_outcome text`, `bars_forward integer`, `competing_signals_count integer` (count of other signals selected at the same bar/symbol/timeframe — subquery against signal_ledger_full WHERE was_selected=true AND timestamp=sig.timestamp AND symbol=sig.symbol AND timeframe=sig.timeframe; lets Phase 117.5 control for aggregator competition bias when interpreting probe win rates). PK `(signal_id, probed_at)`. Add a `UNIQUE` or index on `signal_id` to support idempotent re-probing skip, plus an index on `(setup_plugin, probed_at)`. Use `CREATE TABLE IF NOT EXISTS`. Header comment + `Run with:` psql line matching migration 119's style.

    B. services/signal_probe_auditor.py — daily oneshot mirroring shadow_auditor structure exactly:
    - Module constants: `SAMPLE_RATE = 0.01`, `MIN_SAMPLE_N = 100` (documented as the Phase 117.5 gate, not enforced here), `MAX_BARS_FORWARD = 20`, and a `NEEDS_REFACTOR_SETUPS: frozenset[str]` containing the 21 setup names from RCA Appendix A (read them from the doc; if the list is impractical to enumerate, derive the sample set in SQL by selecting setups present in signal_ledger that are not in the 6 GOOD setups — but prefer the explicit frozenset for determinism).
    - Module-level OTel metric(s) via `_otel_metrics.get_meter("indicagent")`: a counter `signal_probe_activations_total` (label `setup_plugin`) and optionally a gauge for sampled count. Follow the metrics.py call conventions (counter `.add(1, {...})`).
    - `_select_unselected_sample(conn)`: query `signal_ledger_full` for rows where `was_selected = false AND NOT is_shadow AND activated_at IS NULL AND status IN ('expired','regime_suppressed') AND setup_plugin = ANY($1)` over a recent window (e.g. `timestamp >= now() - interval '2 days'` to bound work) and randomly sample ~1% per setup. Use `TABLESAMPLE`-free approach: `ORDER BY random()` with a per-setup `LIMIT`, OR `WHERE random() < SAMPLE_RATE`. Exclude signal_ids already present in `signal_probe_results` (LEFT JOIN / NOT EXISTS) for idempotency. Skip signals lacking `entry_zone_low`/`entry_zone_high`.
    - `_simulate_outcome(conn, sig)`: fetch up to `MAX_BARS_FORWARD` bars from `market_data_ohlcv` for `(symbol, timeframe)` with `timestamp > sig.timestamp ORDER BY timestamp LIMIT MAX_BARS_FORWARD`. Simulate activation: long activates when a bar's `low <= entry_zone_high` (use `entry_zone_high` as the long entry per scope), short activates when `high >= entry_zone_low`. On activation, set sim_entry_price to the zone edge; then track forward bar-by-bar computing running MFE/MAE in price terms and convert pnl to R using the signal's stop distance (`abs(sim_entry - stop_loss)`; pull `stop_loss` in the select). If never activated within the window, `sim_activated=false`, leave pnl/mae/mfe NULL, `sim_outcome='never_activated'`. Keep the simulation deterministic and simple — exit at end of window with `sim_outcome` like `'window_end'` (full 8-class taxonomy is out of scope; record a coarse outcome string). All timestamps UTC.
    - `_run_probe(pool)`: acquire conn, select sample, simulate each, batch-INSERT into `signal_probe_results` (asyncpg `executemany`/`copy_records_to_table`; pass dicts/values directly, NO json.dumps). Increment the activations counter per setup. Log a structured summary (`probe.summary`, counts per setup).
    - `main()`: `asyncio.run(_amain())` in try; `JOB_COMPLETED_TOTAL.add(1, {"job": "signal-probe-auditor", "status": "success"})` on success; `except Exception as error:` -> failure counter + `raise error`; `finally: flush_and_shutdown_metrics()`. The `job` label MUST be exactly `signal-probe-auditor` to match the unit suffix. Exception variable name is `error`, never `exc`.

    C. systemd pair:
    - indicagent-signal-probe-auditor.service: clone shadow-auditor.service; Description "IndicAgent Signal Probe Auditor"; Type=oneshot; ExecStart `.venv/bin/python services/signal_probe_auditor.py`.
    - indicagent-signal-probe-auditor.timer: clone shadow-auditor.timer but DAILY (e.g. `OnCalendar=*-*-* 03:30:00`, Persistent=true, Unit=indicagent-signal-probe-auditor.service). Not every 5 min — it does bar-data lookback and is heavier.

    D. service_auditor.py: register `indicagent-signal-probe-auditor` in `_DAG_ORDER` as a timer-triggered oneshot (mirror the `indicagent-shadow-auditor` entries with a comment "oneshot: timer-triggered, not a daemon"). Update `_LAG_THRESHOLDS` / `_AGENT_ID_TO_UNIT` only if the existing oneshot (shadow-auditor) appears in them; otherwise leave those untouched (oneshots typically excluded from lag thresholds).

    E. tests/unit/services/test_signal_probe_auditor.py: unit-test the pure simulation logic without a live DB. Extract `_simulate_outcome` (or a pure helper it calls) so it can be tested against an in-memory list of OHLCV bar dicts and a signal dict. Assert: (1) a long signal whose forward bars dip into `entry_zone_high` activates and produces a non-None sim_pnl_r; (2) a signal whose forward bars never touch the zone yields `sim_activated=false` and `sim_outcome='never_activated'`; (3) pnl_r sign matches favorable vs adverse forward movement. Also assert the `job` label constant equals `"signal-probe-auditor"` (guards the D-06 contract).
  </action>
  <verify>
    .venv/bin/pytest tests/unit/services/test_signal_probe_auditor.py -v passes; `.venv/bin/python -c "import ast; ast.parse(open('services/signal_probe_auditor.py').read())"` succeeds; grep "signal-probe-auditor" services/service_auditor.py production/systemd/indicagent-signal-probe-auditor.timer matches.
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "CREATE TABLE IF NOT EXISTS signal_probe_results" production/migrations/120_signal_probe_results.sql` matches
    - Source: `grep -n "INSERT INTO signal_probe_results\|copy_records_to_table\|executemany" services/signal_probe_auditor.py` matches (no json.dumps anywhere)
    - Source: `grep -c "signal-probe-auditor" services/signal_probe_auditor.py` shows the job label on BOTH success and failure paths (>=2)
    - Source: `grep -n "flush_and_shutdown_metrics" services/signal_probe_auditor.py` present in a finally block
    - Source: `grep -n "OnCalendar" production/systemd/indicagent-signal-probe-auditor.timer` shows a DAILY schedule (not `*:0/5`)
    - Source: `grep -n "indicagent-signal-probe-auditor" services/service_auditor.py` registered in _DAG_ORDER
    - Source: `grep -n "except Exception as error" services/signal_probe_auditor.py` (not `exc`)
    - Test: `.venv/bin/pytest tests/unit/services/test_signal_probe_auditor.py -q` exits 0
    - CLI: `.venv/bin/ruff check services/signal_probe_auditor.py tests/unit/services/test_signal_probe_auditor.py` exits 0
  </acceptance_criteria>
  <done>signal_probe_results table migration exists; daily oneshot samples unselected NEEDS_REFACTOR signals, simulates outcomes from OHLCV, writes probe rows, emits D-06 completion counter; systemd timer is daily; service registered in DAG; simulation logic unit-tested.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/intelligence/test_i6_hmm_confidence_wiring.py tests/unit/services/test_signal_probe_auditor.py -q` green
- `.venv/bin/pytest tests/unit/ -q` green (no collection breakage from new imports/files)
- `.venv/bin/ruff check src/intelligence/trading/ services/signal_probe_auditor.py tests/unit/intelligence/test_i6_hmm_confidence_wiring.py tests/unit/services/test_signal_probe_auditor.py` clean
- `grep -c "hmm_regime_weight(" src/intelligence/trading/cvd_divergence.py src/intelligence/trading/ofi_continuation.py src/intelligence/trading/gap_analysis_setup.py src/intelligence/trading/divergence_stack.py src/intelligence/trading/microstructure_utils.py` shows >=1 per file (5 files = all 6 plugins, 2 via shared util)
- Migration apply (operator): `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/120_signal_probe_results.sql` then `\d signal_probe_results` shows the table
</verification>

<success_criteria>
VAL-01 (upstream): CVD threshold is a nonzero enforced floor; all 6 high-volume NEEDS_REFACTOR plugins consume ctf_score + hmm_regime_weight in confidence instead of discarding I6/regime data — no fundamental signal logic changed.
VAL-02/VAL-03 (ground truth): SignalProbeAuditor runs daily, simulates outcomes for ~1% of unselected NEEDS_REFACTOR signals from OHLCV, and writes pnl_r/mae/mfe to signal_probe_results, emitting job_completed_total{job=signal-probe-auditor}. Phase 117.5 can now derive empirical thresholds from this ground truth.
</success_criteria>

<output>
After completion, create `.planning/phases/117-patterncompletion-fix-data-pipeline-validation/117-00-SUMMARY.md`
</output>
