---
phase: 44.1-feature-pipeline-renaissance
plan: "01"
subsystem: core-schemas
tags: [bar-message, session-type, bar-history, bar-accumulator, tdd, wave-0]
dependency_graph:
  requires: []
  provides:
    - src/core/schemas/bar_message.py (BarMessage + SessionType — typed bar contract)
    - src/core/bar_history.py (BarHistory shell — interface for Wave 1)
    - src/core/bar_accumulator.py (BarAccumulator + TradingSession shells — interface for Wave 1)
    - src/intelligence/schemas.py (IntelligenceEvent extended with session_type + pipeline_latency_ms)
  affects:
    - All services that consume IntelligenceEvent (backward-compatible, defaults provided)
    - Wave 1 (44.1-02) TDD implementation targets
tech_stack:
  added: []
  patterns:
    - "SessionType(str, Enum) pattern for DB-compatible enum fields"
    - "Wave 0: class shells with NotImplementedError bodies define contracts before implementation"
    - "xfail stubs for shells; skip stubs for services not yet built"
key_files:
  created:
    - src/core/schemas/__init__.py
    - src/core/schemas/bar_message.py
    - src/core/bar_history.py
    - src/core/bar_accumulator.py
    - tests/unit/core/test_bar_message.py
    - tests/unit/core/test_bar_history.py
    - tests/unit/core/test_bar_accumulator.py
    - tests/unit/intelligence/test_schemas_backward_compat.py
    - tests/unit/service_tests/test_feature_pipeline_service.py
  modified:
    - src/intelligence/schemas.py (SessionType import + 2 new fields on IntelligenceEvent)
decisions:
  - "BarMessage.volume is int (not float) — exchange-reported volumes are always integers; float was a silent bug risk"
  - "SessionType extends str for DB compatibility (no migration needed when persisted)"
  - "IntelligenceEvent new fields have defaults (RTH, 0.0) — fully backward compatible with existing consumers"
  - "Wave 0 pattern: shells raise NotImplementedError so import paths work but tests xfail until Wave 1"
  - "xfail(strict=True) on BarHistory/BarAccumulator so any accidental passing test is caught"
  - "FeaturePipelineService stubs use skip not xfail — service class does not exist yet"
metrics:
  duration_minutes: 6
  completed_date: "2026-03-21"
  tasks_completed: 2
  tasks_total: 2
  files_created: 9
  files_modified: 1
---

# Phase 44.1 Plan 01: Schema Foundations and Test Stubs Summary

Wave 0 contract establishment — BarMessage + SessionType fully implemented; IntelligenceEvent extended with 2 new defaulted fields; BarHistory and BarAccumulator class shells define Wave 1 TDD targets; 24 test stubs collected (19 xfail + 5 skip).

## What Was Built

### Task 1: BarMessage + SessionType + IntelligenceEvent Extension

Created `src/core/schemas/bar_message.py` with the canonical typed bar representation:

- `SessionType(str, Enum)` — RTH/ETH/CRYPTO/FX/CLOSED session classification
- `BarMessage(BaseModel)` — typed bar with `volume: int`, `source: Literal[...]`, `gap_preceding: bool = False`

Extended `IntelligenceEvent` in-place (D-22):
- Added `from src.core.schemas.bar_message import SessionType`
- Added `session_type: SessionType = SessionType.RTH`
- Added `pipeline_latency_ms: float = 0.0`

Both new fields have defaults — zero breaking changes to existing event publishers.

### Task 2: BarHistory and BarAccumulator Shells + Test Stubs

Created class shells with correct method signatures and `NotImplementedError` bodies:

**`src/core/bar_history.py`** — `BarHistory(maxlen=200)` with methods:
- `append(bar)`, `get(symbol, tf)`, `to_dataframe(symbol, tf)`, `is_warm(symbol, tf, min_bars)`, `seed(symbol, tf, bars)`, `migrate_symbol(old, new)`

**`src/core/bar_accumulator.py`** — `TradingSession` + `BarAccumulator(timeframes, session)` with:
- `TradingSession.is_session_break(prev_ts, curr_ts)` for D-14 session-aware close
- `BarAccumulator.update(bar_1m)`, `current_partial(symbol, tf)`

### Test Coverage

| File | Tests | Type |
|------|-------|------|
| `test_bar_message.py` | 16 | Passing |
| `test_schemas_backward_compat.py` | 10 | Passing |
| `test_bar_history.py` | 10 | xfail (Wave 1) |
| `test_bar_accumulator.py` | 9 | xfail (Wave 1) |
| `test_feature_pipeline_service.py` | 5 | skip (Wave 1+) |

Full suite: 2722 passed, 5 skipped, 19 xfailed — zero regressions.

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

The following are intentional Wave 0 stubs (not blocking plan goals):

| File | Nature | Reason |
|------|--------|--------|
| `src/core/bar_history.py` | All methods raise NotImplementedError | Wave 1 (44.1-02) implements |
| `src/core/bar_accumulator.py` | All methods raise NotImplementedError | Wave 1 (44.1-02) implements |

These stubs are intentional — Wave 0 goal is contract definition, not implementation. Wave 1 will implement against these contracts using TDD (xfail → passing).

## Self-Check: PASSED
