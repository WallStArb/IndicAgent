---
phase: 17-llm-wiring-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/trading/session_extremes_setup.py
  - tests/unit/intelligence/trading/test_session_extremes_setup.py
autonomous: true
requirements:
  - LLM-05
must_haves:
  truths:
    - "SessionExtremesSetup emits 'session_extreme_london' when london=1.0, ny=0.0"
    - "SessionExtremesSetup emits 'session_extreme_ny' when london=0.0, ny=1.0"
    - "SessionExtremesSetup emits 'session_extreme_both' when london=1.0, ny=1.0"
    - "The session label (session:london / session:ny / session:both) appears in supporting_factors"
    - "regime_context never contains bare 'london', 'ny', or 'both' strings"
    - "All existing test behavior is preserved (signal firing logic unchanged)"
  artifacts:
    - path: "src/intelligence/trading/session_extremes_setup.py"
      provides: "Corrected regime_context vocabulary for score cache key alignment"
      contains: "session_extreme_"
    - path: "tests/unit/intelligence/trading/test_session_extremes_setup.py"
      provides: "Updated + new tests covering all three session regime strings and supporting_factors metadata"
  key_links:
    - from: "session_extremes_setup.compute_full()"
      to: "llm_calls.regime"
      via: "regime_context field stored verbatim in ai_narrative_service._build_llm_call_payload"
      pattern: "session_extreme_(london|ny|both)"
---

<objective>
Fix SessionExtremesSetup to emit correct regime vocabulary strings so LLM score cache keys are
reachable by adaptive routing.

Purpose: The plugin currently writes "london", "ny", or "both" into regime_context. These values
are stored verbatim in llm_calls.regime. The _apply_score_routing() function in ai_narrative_service
never queries those keys (it queries "trending", "ranging", "volatile", "__all__"). Session extreme
outcomes accumulate in llm_model_scores under unreachable keys, preventing adaptive routing from
ever activating for this signal family. Per CONTEXT.md locked decision: no translation/canonicalization
— raw plugin strings ARE the vocabulary. The fix emits three new distinct regime strings
(session_extreme_london, session_extreme_ny, session_extreme_both) that preserve Renaissance
segmentation granularity.

Output: Updated plugin + updated/new tests. Zero schema changes. Zero service changes.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-llm-wiring-fix/17-CONTEXT.md
@.planning/phases/17-llm-wiring-fix/17-RESEARCH.md

<interfaces>
<!-- Current plugin state — read directly from source -->
<!-- From src/intelligence/trading/session_extremes_setup.py lines 132-151 -->

Current (BROKEN) compute_full() return block:
```python
if session_london and session_ny:
    session_ctx = "both"
elif session_london:
    session_ctx = "london"
else:
    session_ctx = "ny"

return {
    "signal_type": signal_type,
    "direction": direction,
    "bias": side,
    "proximity_atr": round(proximity_atr, 4),
    "confidence": confidence,
    "entry_type": "at_limit",
    "entry_price": round(entry_price, 2),
    "stop_loss": round(stop_loss, 2),
    "targets": targets,
    "regime_context": session_ctx,         # BUG: "london"|"ny"|"both" not in routing vocab
    "supporting_factors": supporting,
}
```

Target (FIXED) — session_ctx determination unchanged, only regime_context and supporting_factors differ:
```python
if session_london and session_ny:
    session_ctx = "both"
elif session_london:
    session_ctx = "london"
else:
    session_ctx = "ny"

regime_ctx = f"session_extreme_{session_ctx}"   # "session_extreme_london"|_ny|_both
supporting.append(f"session:{session_ctx}")      # metadata preserved in supporting_factors

return {
    ...
    "regime_context": regime_ctx,
    "supporting_factors": supporting,
}
```

<!-- From tests/unit/intelligence/trading/test_session_extremes_setup.py -->
Tests that MUST be updated (currently assert old values):
  - test_regime_context_london (line 269): asserts "london" → must assert "session_extreme_london"
  - test_regime_context_ny (line 275): asserts "ny" → must assert "session_extreme_ny"
  - test_fires_with_trend_align_only (line 128): asserts supporting_factors == ["trend_align"]
    → after fix must contain both "trend_align" and "session:london" — update to membership checks
  - test_fires_with_rsi_extreme_only: asserts supporting_factors == ["rsi_extreme"]
    → after fix must contain both "rsi_extreme" and a "session:*" label — update to membership checks

Tests that must be ADDED:
  - test_regime_context_overlap_both: london=1.0, ny=1.0 → "session_extreme_both"
  - test_supporting_factors_includes_session_label: session label in supporting_factors for all 3 cases
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix regime_context vocabulary + update/add tests</name>
  <files>
    src/intelligence/trading/session_extremes_setup.py,
    tests/unit/intelligence/trading/test_session_extremes_setup.py
  </files>
  <behavior>
    - test_regime_context_london: regime_context == "session_extreme_london" (was "london")
    - test_regime_context_ny: regime_context == "session_extreme_ny" (was "ny")
    - test_regime_context_overlap_both (NEW): session_london=1.0, session_ny=1.0 → "session_extreme_both"
    - test_supporting_factors_includes_session_label (NEW): "session:london" in supporting_factors for london; "session:ny" for ny; "session:both" for overlap
    - test_fires_with_trend_align_only: supporting_factors contains "trend_align" (membership check, not equality) AND contains "session:london"
    - test_fires_with_rsi_extreme_only: supporting_factors contains "rsi_extreme" AND "session:london" (update equality assertion to membership checks)
    - All pre-existing tests that do NOT assert on regime_context or supporting_factors must remain GREEN without changes
  </behavior>
  <action>
    TDD sequence — tests first, then implementation:

    STEP 1 — Write failing tests:
    In tests/unit/intelligence/trading/test_session_extremes_setup.py:

    1. Add TestRegimeVocabulary class with:
       - test_regime_context_london: make_frames(session_london=1.0, session_ny=0.0) → assert result["regime_context"] == "session_extreme_london"
       - test_regime_context_ny: make_frames(close=4992.0, session_london=0.0, session_ny=1.0, trend_regime=0.6, rsi_14=30.0) → assert result["regime_context"] == "session_extreme_ny"
       - test_regime_context_overlap_both: make_frames(session_london=1.0, session_ny=1.0) → assert result["regime_context"] == "session_extreme_both"
       - test_supporting_factors_includes_session_label_london: result["supporting_factors"] contains "session:london"
       - test_supporting_factors_includes_session_label_ny: result["supporting_factors"] contains "session:ny"
       - test_supporting_factors_includes_session_label_both: make_frames(session_london=1.0, session_ny=1.0) → result["supporting_factors"] contains "session:both"

    2. Update TestSignalFields:
       - test_regime_context_london: change assert from "london" to "session_extreme_london"
       - test_regime_context_ny: change assert from "ny" to "session_extreme_ny"

    3. Update TestSess03ConfirmingFactorGate:
       - test_fires_with_trend_align_only: replace `assert result.get("supporting_factors") == ["trend_align"]`
         with `factors = result.get("supporting_factors", []); assert "trend_align" in factors; assert any(f.startswith("session:") for f in factors)`
       - test_fires_with_rsi_extreme_only: replace equality assert with membership:
         `factors = result.get("supporting_factors", []); assert "rsi_extreme" in factors; assert any(f.startswith("session:") for f in factors)`

    Confirm tests are RED: `.venv/bin/pytest tests/unit/intelligence/trading/test_session_extremes_setup.py -x -q`

    STEP 2 — Implement fix in session_extremes_setup.py:
    In compute_full(), after the session_ctx assignment block (lines 132-137), insert exactly:
    ```python
    regime_ctx = f"session_extreme_{session_ctx}"
    supporting.append(f"session:{session_ctx}")
    ```
    In the return dict, change `"regime_context": session_ctx` to `"regime_context": regime_ctx`.
    No other changes to the function. No logic changes to signal firing conditions.

    STEP 3 — Confirm GREEN: `.venv/bin/pytest tests/unit/intelligence/trading/test_session_extremes_setup.py -x -q`
    STEP 4 — Run ruff: `.venv/bin/ruff check src/intelligence/trading/session_extremes_setup.py`
    STEP 5 — Run full unit suite: `.venv/bin/pytest tests/unit/ -x -q`
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/trading/test_session_extremes_setup.py -v</automated>
  </verify>
  <done>
    All session_extremes tests GREEN; regime_context values are "session_extreme_london" / "session_extreme_ny" / "session_extreme_both"; supporting_factors contains "session:london" / "session:ny" / "session:both" alongside existing factors; ruff 0 errors; full unit suite green.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "session_extreme_" src/intelligence/trading/session_extremes_setup.py` — must appear in compute_full() return block
2. `grep -n '"london"\|"ny"\|"both"' src/intelligence/trading/session_extremes_setup.py` — must NOT appear as regime_context value (only session_ctx variable assignment is allowed)
3. `.venv/bin/pytest tests/unit/intelligence/trading/test_session_extremes_setup.py -v` — all tests GREEN
4. `.venv/bin/pytest tests/unit/ -x -q` — full suite GREEN
5. `.venv/bin/ruff check .` — 0 errors
</verification>

<success_criteria>
1. SessionExtremesSetup.compute_full() returns regime_context in {"session_extreme_london", "session_extreme_ny", "session_extreme_both"} — never "london", "ny", or "both"
2. supporting_factors always includes the session metadata label (e.g. "session:london") on every fired signal
3. All 3 regime variants are covered by tests (london, ny, both)
4. All pre-existing tests that were not about regime_context remain GREEN with zero changes
5. ruff check . → 0 errors
6. Full unit suite: 1182+ tests GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/17-llm-wiring-fix/17-01-SUMMARY.md`
</output>
