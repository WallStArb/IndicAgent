---
phase: 096-agent-registry
plan: 03
type: execute
wave: 3
depends_on: ["096-01", "096-02"]
requirements: [AGENT-REG-01, AGENT-REG-02, AGENT-REG-03, AGENT-REG-04]
files_modified:
  - src/intelligence/ai/register_agents.py
  - config/agents.yaml
  - src/intelligence/ai/base_group_service.py
  - services/alpha_swarm.py
  - services/narrative_swarm.py
  - tests/unit/services/test_alpha_swarm.py
autonomous: true

must_haves:
  truths:
    - "Reconfiguring an existing agent (shadow_only, latency_budget_ms, prompt_version) is YAML-only — edit config/agents.yaml and restart, no Python changes"
    - "Adding a NEW agent requires exactly one Python edit (append its module path to AGENT_MODULES) plus one YAML edit (add the agent_id entry) — no swarm-service or coordinator code changes"
    - "AgentRegistry is the sole agent construction path — no swarm service instantiates an agent class directly"
    - "BaseSwarmCoordinator._setup builds agents from YAML before the first bar is processed"
    - "A spec with an unknown agent_id fails fast with a descriptive error listing known ids before any bar"
    - "shadow_registry enrollment still runs at startup over the registry-built agent list"
  artifacts:
    - path: "config/agents.yaml"
      provides: "Operator manifest — alpha/narrative/risk sections"
      contains: "alpha:"
    - path: "src/intelligence/ai/register_agents.py"
      provides: "AGENT_MODULES explicit import list + _import_all()"
      contains: "AGENT_MODULES"
    - path: "src/intelligence/ai/base_group_service.py"
      provides: "AgentRegistry.build wiring in _setup, _swarm_deps, _agents init"
      contains: "AgentRegistry.build"
  key_links:
    - from: "src/intelligence/ai/base_group_service.py"
      to: "src/core/ai/registry.py"
      via: "AgentRegistry.build(self.group_id, self._swarm_deps) in _setup"
      pattern: "AgentRegistry\\.build"
    - from: "src/intelligence/ai/base_group_service.py"
      to: "src/intelligence/ai/register_agents.py"
      via: "lazy _import_all() before build"
      pattern: "_import_all\\(\\)"
    - from: "config/agents.yaml"
      to: "src/core/ai/registry.py"
      via: "yaml.safe_load in _load_specs"
      pattern: "agent_id"
---

<objective>
Wire the registry end to end: create the AGENT_MODULES explicit import list, write config/agents.yaml with the verified agent_ids, build agents inside BaseSwarmCoordinator._setup() via AgentRegistry.build(), and remove the hardcoded agent construction from AlphaSwarm and NarrativeSwarm. After this plan an operator reconfigures an existing agent by editing agents.yaml and restarting (no Python edits), and adds a brand-new agent with one AGENT_MODULES line plus one YAML entry (no swarm-service/coordinator changes).

Purpose: This is the integration that delivers AGENT-REG-01..04 — YAML-driven instantiation, sole construction path, fail-fast validation, and DB-authoritative shadow gating.
Output: register_agents.py (new), config/agents.yaml (new), modified base_group_service.py, slimmed AlphaSwarm/NarrativeSwarm, updated alpha_swarm test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/096-agent-registry/096-CONTEXT.md
@.planning/phases/096-agent-registry/096-RESEARCH.md
@.planning/phases/096-agent-registry/096-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create register_agents.py and config/agents.yaml</name>
  <files>src/intelligence/ai/register_agents.py, config/agents.yaml</files>
  <read_first>
    - src/intelligence/register_plugins.py (lines 1-30 and TIER_I7 — the explicit-list analog)
    - .planning/phases/096-agent-registry/096-PATTERNS.md (lines 177-252: AGENT_MODULES + _import_all and agents.yaml structure; the Critical Agent ID Reference table at lines 626-638)
    - src/intelligence/ai/alpha/skeptic_agent.py (confirm agent_id = "skeptic")
    - src/intelligence/ai/alpha/ml_scorer_agent.py (confirm agent_id = "ml_scorer_v1")
  </read_first>
  <action>
    Create src/intelligence/ai/register_agents.py with a module docstring explaining it is the explicit agent import list analogous to TIER_I7 (steps to add an agent: create class, add module path here, add entry to agents.yaml, restart). Define `AGENT_MODULES` as a list of these exact dotted module paths in this order: "src.intelligence.ai.alpha.skeptic_agent", "src.intelligence.ai.alpha.correlation_agent", "src.intelligence.ai.alpha.regime_coherence_agent", "src.intelligence.ai.alpha.counterfactual_agent", "src.intelligence.ai.alpha.ml_scorer_agent", "src.intelligence.ai.narrative.narrative_agent". Define `def _import_all() -> None:` that does `import importlib` then loops `for module_path in AGENT_MODULES: importlib.import_module(module_path)`. Do NOT import TEMPLATE.py (its agent_id "template_evaluator" must never enter the live registry). Then create config/agents.yaml at project root (mkdir config/ if needed) with a header comment block (operator manifest; edit and restart; shadow_only:true is the only valid value; production promotion goes through shadow_registry). Add three top-level sections. `alpha:` is a list with entries: agent_id: skeptic; agent_id: correlation_v1; agent_id: regime_coherence_v1; agent_id: counterfactual_v1; agent_id: ml_scorer_v1 with `shadow_only: true`. `narrative:` is a list with one entry: agent_id: narrative_v1. `risk: []` (empty, scaffolded). Use the EXACT agent_id strings from source (note: skeptic has NO version suffix). Do not set shadow_only:false anywhere.
  </action>
  <verify>.venv/bin/python -c "from src.intelligence.ai.register_agents import _import_all, AGENT_MODULES; _import_all(); from src.core.ai.registry import _REGISTRY; import yaml; raw = yaml.safe_load(open('config/agents.yaml')); ids = [e['agent_id'] for e in raw['alpha']] + [e['agent_id'] for e in raw['narrative']]; assert all(i in _REGISTRY for i in ids), [i for i in ids if i not in _REGISTRY]"</verify>
  <acceptance_criteria>
    - src/intelligence/ai/register_agents.py contains `AGENT_MODULES` and `def _import_all`
    - AGENT_MODULES does NOT contain "TEMPLATE" or "template"
    - config/agents.yaml contains `alpha:`, `narrative:`, `risk: []`, and `agent_id: skeptic` (no _v1 suffix)
    - config/agents.yaml contains `agent_id: ml_scorer_v1` and `shadow_only: true`
    - `grep -c "shadow_only: false" config/agents.yaml` returns 0
    - The Task verify command exits 0 (every yaml agent_id resolves in _REGISTRY after _import_all)
  </acceptance_criteria>
  <done>register_agents.py imports all six live agents and config/agents.yaml lists exactly the verified agent_ids with no force-production flags.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AgentRegistry.build into BaseSwarmCoordinator._setup</name>
  <files>src/intelligence/ai/base_group_service.py</files>
  <read_first>
    - src/intelligence/ai/base_group_service.py (lines 73-82 __init__; lines 84-151 _setup — note _llm_chain set at 130-135, _pool set at 121-127, lineage loop iterates self.agents at line 147, super()._setup() at line 151; lines 153-170 _shadow_registry_ensure_agents)
    - services/alpha_swarm.py (confirm `group_id` returns "alpha" — the registry build key for the alpha section of agents.yaml)
    - services/narrative_swarm.py (confirm `group_id` returns "narrative" — the registry build key for the narrative section)
    - src/core/ai/registry.py (AgentRegistry.build signature)
    - src/core/ai/swarm_deps.py (SwarmDeps constructor)
    - src/intelligence/ai/register_agents.py (_import_all from Task 1)
    - .planning/phases/096-agent-registry/096-PATTERNS.md (lines 313-376: exact _setup wiring + __init__ additions; Pitfall 5 — _import_all must run before build)
  </read_first>
  <action>
    In src/intelligence/ai/base_group_service.py __init__ (after self._lineage = None, ~line 82), add `self._agents: list[BaseAIWorker] = []` and `self._swarm_deps: Any | None = None  # SwarmDeps; Any avoids import at module level`.

    In _setup(), the existing code at lines 137-151 runs in this order: (1) `await self._seed_context_cache()` (~138), (2) the LineageRecorder block (~141-146), (3) the lineage assignment loop `for agent in self.agents: agent._lineage = self._lineage` (line 147), (4) `await super()._setup()` (line 151). The registry build MUST be inserted so that self._agents is populated BEFORE the line-147 lineage loop runs (otherwise the loop iterates an empty list and no agent gets a lineage recorder).

    PRECISE ANCHOR: Insert the registry build block at line 147 — i.e. immediately before the existing `for agent in self.agents:` lineage loop, displacing that loop downward. After insertion the required execution order inside _setup is:
      a. self._llm_chain assigned (existing, ~line 130-135)
      b. self._pool assigned (existing, ~line 121-127)
      c. LineageRecorder created/started (existing, ~line 141-146)
      d. [NEW] _import_all()
      e. [NEW] self._swarm_deps = SwarmDeps(llm_chain=self._llm_chain, pool=self._pool, settings=self.settings)
      f. [NEW] self._agents = AgentRegistry.build(self.group_id, self._swarm_deps)
      g. [NEW] if self._pool is not None: await self._shadow_registry_ensure_agents(self._agents)
      h. [EXISTING, now displaced downward] for agent in self.agents: agent._lineage = self._lineage
      i. [EXISTING] await super()._setup()

    Add the lazy imports at the top of the NEW block inside _setup (avoid module-level circular import): `from src.intelligence.ai.register_agents import _import_all`, `from src.core.ai.registry import AgentRegistry`, `from src.core.ai.swarm_deps import SwarmDeps`. Leave the existing lineage loop body and `await super()._setup()` unchanged — only their position shifts down relative to the new block. Note: subclasses' `agents` property must return `self._agents` (handled in Task 3) so the base lineage loop and enrollment see the built list. Keep `_shadow_registry_ensure_agents` exactly as-is.
  </action>
  <verify>.venv/bin/python -c "import inspect; from src.intelligence.ai.base_group_service import BaseSwarmCoordinator; src = inspect.getsource(BaseSwarmCoordinator._setup); assert 'AgentRegistry.build' in src and '_import_all()' in src and 'SwarmDeps(' in src; assert src.index('AgentRegistry.build') < src.index('for agent in self.agents'), 'build must precede lineage loop'" && .venv/bin/python -c "from src.intelligence.ai.register_agents import _import_all; _import_all(); from src.core.ai.registry import AgentRegistry; from src.core.ai.swarm_deps import SwarmDeps; built = AgentRegistry.build('alpha', SwarmDeps(llm_chain=None, pool=None, settings=None)); assert len(built) == 5, f'expected 5 alpha agents, got {len(built)}'"</verify>
  <acceptance_criteria>
    - base_group_service.py _setup contains `_import_all()`, `SwarmDeps(`, and `AgentRegistry.build(self.group_id, self._swarm_deps)`
    - base_group_service.py __init__ contains `self._agents: list[BaseAIWorker] = []` and `self._swarm_deps`
    - In _setup source order, the `AgentRegistry.build` call appears BEFORE the `for agent in self.agents:` lineage loop (asserted by the verify command's `src.index` check)
    - base_group_service.py _setup contains `await self._shadow_registry_ensure_agents(self._agents)`
    - `AgentRegistry.build('alpha', deps)` returns exactly 5 agents (matches the 5 entries in the agents.yaml `alpha:` section)
    - `.venv/bin/python -c "from src.intelligence.ai.base_group_service import BaseSwarmCoordinator"` exits 0
  </acceptance_criteria>
  <done>BaseSwarmCoordinator._setup imports agent modules, builds SwarmDeps, populates self._agents from AgentRegistry.build (before the lineage loop), and enrolls them in shadow_registry — all before super()._setup() and before any bar is processed.</done>
</task>

<task type="auto">
  <name>Task 3: Remove hardcoded construction from AlphaSwarm and NarrativeSwarm + update alpha_swarm test</name>
  <files>services/alpha_swarm.py, services/narrative_swarm.py, tests/unit/services/test_alpha_swarm.py</files>
  <read_first>
    - services/alpha_swarm.py (lines 41-53 agent imports; lines 119-138 group_id/__init__/agents property; lines 154-202 _setup with hardcoded construction, config propagation loop, MLEvaluator setup, shadow enrollment, SIGUSR1)
    - services/narrative_swarm.py (lines 45-78 group_id/__init__/agents property/_setup; lines 126-139 _process_one_signal uses self._narrative_agent)
    - src/intelligence/ai/alpha/ml_scorer_agent.py (MLEvaluator._setup_models — still called explicitly post-build)
    - tests/unit/services/test_alpha_swarm.py (lines 118-124 and 475-480: source-introspection assertions referencing LineageRecorder and SkepticEvaluator)
    - .planning/phases/096-agent-registry/096-PATTERNS.md (lines 462-555: target AlphaSwarm/NarrativeSwarm _setup; Pitfall 3 find MLEvaluator by type; Pitfall 4 keep config propagation loop)
  </read_first>
  <action>
    In services/alpha_swarm.py: delete the imports of SkepticEvaluator, CorrelationAnalyzer, RegimeCoherenceAnalyzer, CounterfactualEvaluator (lines 49-53) but KEEP `from src.intelligence.ai.alpha.ml_scorer_agent import MLEvaluator` (needed for isinstance). In _setup(), after `await super()._setup()` (self._agents is now populated by the base class), DELETE the hardcoded `self._agents = [...]` list, the `self._agents.append(MLEvaluator(pool=self._pool))`, and `await self._agents[-1]._setup_models()`. Replace the MLEvaluator setup with type-based lookup: `ml = next((a for a in self._agents if isinstance(a, MLEvaluator)), None); if ml is not None: await ml._setup_models()`. KEEP the `self._semaphore = asyncio.Semaphore(...)` line, KEEP the config propagation loop (the `for agent in self._agents: ... ai.agent. ... _apply_shadow_mode_config()` block — Pitfall 4), and DELETE the now-duplicate `if self._pool is not None: await self._shadow_registry_ensure_agents(self._agents)` block (base class does this). KEEP the SIGUSR1 handler registration and the final `alpha_swarm.started` log. Confirm the `agents` property still returns `self._agents`. In services/narrative_swarm.py: in _setup() after `await super()._setup()`, DELETE `self._narrative_agent = NarrativeSynthesizer(llm_chain=self._llm_chain)` and the duplicate `await self._shadow_registry_ensure_agents(self.agents)` block; replace with `self._narrative_agent = next((a for a in self._agents if isinstance(a, NarrativeSynthesizer)), None)`; keep the started log but guard agent_id access (`self._narrative_agent.agent_id if self._narrative_agent else "none"`). Change the `agents` property to return `self._agents` (the base-populated list) instead of `[self._narrative_agent] if self._narrative_agent else []`. Ensure `self._agents` is initialized in NarrativeSwarm — since the base __init__ now sets `self._agents = []`, remove any conflicting init but keep `self._narrative_agent: NarrativeSynthesizer | None = None`. Keep the `from src.intelligence.ai.narrative.narrative_agent import NarrativeSynthesizer` import (needed for isinstance). In tests/unit/services/test_alpha_swarm.py: update the source-introspection assertion at ~line 478 that expects `SkepticEvaluator` in the module — since AlphaSwarm no longer imports it, change that assertion to verify the registry-driven path instead (e.g. assert AlphaSwarm._setup source contains "super()._setup()" and "_setup_models", and that MLEvaluator is still importable in the module). Do not weaken unrelated assertions.
  </action>
  <verify>.venv/bin/pytest tests/unit/services/test_alpha_swarm.py -q && .venv/bin/python -c "import services.alpha_swarm, services.narrative_swarm; import inspect; a = inspect.getsource(services.alpha_swarm); assert 'SkepticEvaluator(' not in a and 'MLEvaluator(pool=' not in a; n = inspect.getsource(services.narrative_swarm); assert 'NarrativeSynthesizer(llm_chain=' not in n"</verify>
  <acceptance_criteria>
    - services/alpha_swarm.py does NOT contain `SkepticEvaluator(`, `CorrelationAnalyzer(`, `RegimeCoherenceAnalyzer(`, `CounterfactualEvaluator(`, or `MLEvaluator(pool=`
    - services/alpha_swarm.py contains `isinstance(a, MLEvaluator)` and `await ml._setup_models()`
    - services/alpha_swarm.py still contains the `ai.agent.` config propagation loop and `_apply_shadow_mode_config`
    - services/alpha_swarm.py _setup does NOT contain a second `_shadow_registry_ensure_agents` call (base class owns enrollment)
    - services/narrative_swarm.py does NOT contain `NarrativeSynthesizer(llm_chain=` and its `agents` property returns `self._agents`
    - `.venv/bin/pytest tests/unit/services/test_alpha_swarm.py -q` exits 0
  </acceptance_criteria>
  <done>Neither swarm service constructs an agent directly; AgentRegistry is the sole construction path; MLEvaluator async setup runs by type lookup; config propagation and shadow enrollment behavior preserved; alpha_swarm test green.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -c "from src.intelligence.ai.register_agents import _import_all; _import_all(); from src.core.ai.swarm_deps import SwarmDeps; from src.core.ai.registry import AgentRegistry; assert len(AgentRegistry.build('alpha', SwarmDeps(llm_chain=None, pool=None, settings=None))) == 5; assert len(AgentRegistry.build('narrative', SwarmDeps(llm_chain=None, pool=None, settings=None))) == 1; assert len(AgentRegistry.build('risk', SwarmDeps(llm_chain=None, pool=None, settings=None))) == 0"` exits 0 (alpha builds 5, narrative builds 1, risk builds 0 — none raise)
- Fail-fast check: `.venv/bin/python -c "from src.intelligence.ai.register_agents import _import_all; _import_all(); from src.core.ai.registry import AgentRegistry, RegistryConfigError, AgentSpec; AgentRegistry.validate('alpha', [AgentSpec(agent_id='does_not_exist')])"` exits non-zero with a message listing known ids
- `.venv/bin/pytest tests/unit/services/test_alpha_swarm.py tests/unit/services/test_skeptic_agent.py tests/unit/core/ -q` exits 0
- `.venv/bin/ruff check services/alpha_swarm.py services/narrative_swarm.py src/intelligence/ai/base_group_service.py src/intelligence/ai/register_agents.py` reports no errors
</verification>

<success_criteria>
- AGENT-REG-01: reconfiguring an existing agent is YAML-only (edit agents.yaml + restart); adding a NEW agent requires one AGENT_MODULES line + one YAML entry — no swarm-service or coordinator code changes
- AGENT-REG-02: AgentRegistry.build is the sole construction path; no swarm service constructs an agent class
- AGENT-REG-03: unknown agent_id / missing-field spec fails fast with a descriptive error before any bar
- AGENT-REG-04: shadow_registry enrollment runs over the built list; YAML cannot force shadow_only:false
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/096-agent-registry/096-03-SUMMARY.md`
</output>
</content>
</invoke>
