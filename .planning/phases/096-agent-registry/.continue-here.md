---
context: phase
phase: 096-agent-registry
task: 0
total_tasks: 3
status: not_started
last_updated: 2026-06-02T08:45:01-04:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints from this session — Phase 096 execution has not started yet._

<current_state>
Phase 096 (Agent Registry) has 3 plans fully written and reviewed. No execution has occurred yet.
This session was devoted to a signal/lifecycle architectural review that identified 6 data integrity bugs,
all of which were fixed and pushed to main (commit f6a44b45).

Ready to execute Phase 096 immediately.
</current_state>

<completed_work>
Session work (not Phase 096 tasks):
- Full signal/lifecycle architecture review against Renaissance principles
- 6 bugs fixed:
  - BUG-01: bootstrap query missing expires_at (TTL disabled on restart)
  - BUG-02: staleness regime drift permanently zero (regime cache added to signal_tracker)
  - BUG-03: fire-and-forget TTL fast-path ghosting signal IDs (now awaited)
  - BUG-04: pnl_ticks never persisted via lifecycle writer
  - CONCERN-01: chandelier trailing stop lost on restart (now persisted to JSONB)
  - CONCERN-04: SETUP_PRIORITY completeness validation at startup
  - CONCERN-05: target list ordering enforced in lifecycle tracker
- Analysis saved to .planning/signal-lifecycle-review.md
- All 4080 unit tests passing
</completed_work>

<remaining_work>
Phase 096 execution — all 3 plans not yet started:

- Plan 01: src/core/ai/agent_dependencies.py + src/core/ai/registry.py + BaseAIWorker __init_subclass__
  - AgentDependencies dataclass (Ring 0)
  - _REGISTRY dict, AgentSpec(BaseModel), AgentRegistry, RegistryError, RegistryConfigError
  - BaseAIWorker.__init_subclass__ self-registration hook
  - tests/unit/core/test_core_ai_registry.py

- Plan 02: AgentRegistry.build() in BaseGroupCoordinator + config/agents.yaml
  - BaseGroupCoordinator._setup() template method integration
  - agents.yaml with alpha/narrative/risk sections
  - register_agents.py AGENT_MODULES explicit list

- Plan 03: Wire AlphaSwarm + NarrativeSwarm + validation gate
  - Replace hardcoded self._agents construction in both swarms
  - AgentRegistry.validate() runs before _run()
  - Integration tests

Deferred from review (not blocking Phase 096):
- CONCERN-02: mutable signal dict refactor
- CONCERN-03: dual consumer group race window
- CONCERN-06: backfill fast-path outcome bias
</remaining_work>

<decisions_made>
Phase 096 design decisions (all in 096-CONTEXT.md):
- D-01: __init_subclass__ + explicit AGENT_MODULES list (no filesystem scanning)
- D-02: YAML per-group sections; shadow_only:false rejected by AgentSpec
- D-03: AgentDependencies dataclass in src/core/ai/ (Ring 0)
- D-04: BaseGroupCoordinator._setup() template method pattern
- D-05: shadow_registry DB is authority; YAML is intent
- D-06: validate() runs synchronously before _run()

Signal lifecycle fix decisions (committed):
- Regime cache approach: per-(symbol,tf) dict updated from i7.signals envelope
- Chandelier persistence: JSONB trailing_stop_price in signal_outcomes
- Fast-path publish: awaited before dedup, returns bool
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/096-agent-registry/096-CONTEXT.md` — full design spec with all decisions
2. `.planning/phases/096-agent-registry/096-01-PLAN.md` — Plan 01 truths and artifacts
3. `src/core/ai/base_agent.py` — BaseAIWorker current __init__ signature (to be changed)
4. `services/alpha_swarm.py:165` — hardcoded construction pattern to eliminate

## Infrastructure State
- All services running normally
- 4080 unit tests green
- main branch at f6a44b45

<context>
The signal/lifecycle review was a detour from Phase 096, but a necessary one — the bugs found
(especially BUG-01 and BUG-02) would have silently corrupted ML training labels and disabled
TTL expiry for all bootstrapped signals on every restart. Alpha quality depends on data integrity.

Phase 096 is the next logical step: the agent registry is the infrastructure layer that makes
AI agents configurable without code changes. Plans are tight and well-designed.
</context>

<next_action>
Run: /gsd-execute-phase
This will execute Plans 01→02→03 in wave order. All plans are autonomous=true.
</next_action>
