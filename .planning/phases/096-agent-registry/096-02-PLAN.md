---
phase: 096-agent-registry
plan: 02
type: execute
wave: 2
depends_on: ["096-01"]
requirements: [AGENT-REG-02]
files_modified:
  - src/core/ai/base_agent.py
  - src/intelligence/ai/alpha/skeptic_agent.py
  - src/intelligence/ai/alpha/correlation_agent.py
  - src/intelligence/ai/alpha/regime_coherence_agent.py
  - src/intelligence/ai/alpha/counterfactual_agent.py
  - src/intelligence/ai/alpha/ml_scorer_agent.py
  - src/intelligence/ai/narrative/narrative_agent.py
  - src/api/routes/narrative.py
  - tests/unit/services/test_skeptic_agent.py
  - tests/unit/services/test_correlation.py
  - tests/unit/services/test_regime_coherence.py
  - tests/unit/services/test_counterfactual_agent.py
  - tests/unit/api/test_narrative_route.py
autonomous: true

must_haves:
  truths:
    - "Every agent is constructed via a single keyword-only `dependencies: AgentDependencies` argument"
    - "LLM agents read llm_chain from dependencies; MLEvaluator reads pool from dependencies"
    - "Constructors fail with a descriptive error when a required dependency is absent (LLM agents require dependencies.llm_chain; MLEvaluator requires dependencies.pool)"
    - "The on-demand narrative API route constructs NarrativeSynthesizer via AgentDependencies, not via llm_chain= — no constructor call site of the migrated agents remains on the old signature"
    - "Existing agent unit tests and the narrative route test pass against the new constructor signature"
  artifacts:
    - path: "src/core/ai/base_agent.py"
      provides: "BaseAIWorker.__init__ accepting keyword-only dependencies: AgentDependencies"
      contains: "dependencies:"
    - path: "src/intelligence/ai/alpha/skeptic_agent.py"
      provides: "SkepticEvaluator constructed from AgentDependencies"
      contains: "dependencies: AgentDependencies"
    - path: "src/intelligence/ai/alpha/ml_scorer_agent.py"
      provides: "MLEvaluator reads pool from dependencies"
      contains: "dependencies.pool"
    - path: "src/api/routes/narrative.py"
      provides: "On-demand narrative route constructs NarrativeSynthesizer via AgentDependencies"
      contains: "AgentDependencies"
  key_links:
    - from: "src/intelligence/ai/alpha/skeptic_agent.py"
      to: "src/core/ai/agent_dependencies.py"
      via: "dependencies.llm_chain extraction in __init__"
      pattern: "dependencies\\.llm_chain"
    - from: "src/intelligence/ai/alpha/ml_scorer_agent.py"
      to: "src/core/ai/agent_dependencies.py"
      via: "dependencies.pool extraction in __init__"
      pattern: "dependencies\\.pool"
    - from: "src/api/routes/narrative.py"
      to: "src/core/ai/agent_dependencies.py"
      via: "AgentDependencies(llm_chain=_get_llm_chain(), ...) construction at request time"
      pattern: "AgentDependencies\\("
---

<objective>
Migrate every agent constructor from loose kwargs (llm_chain=, pool=) to the unified keyword-only `dependencies: AgentDependencies` signature, change BaseAIWorker.__init__ to accept `dependencies`, and update EVERY direct call site — including the on-demand narrative API route — to construct via AgentDependencies. This is the constructor-uniformity change that lets AgentRegistry.build construct every swarm agent identically with `cls(dependencies=agent_dependencies)`.

Purpose: Eliminate the constructor inconsistency (MLEvaluator needs pool; LLM agents need llm_chain) that cannot be absorbed cleanly by **kwargs. After this plan, mypy catches wrong-dep access at type-check time, and no caller anywhere passes `llm_chain=`/`pool=` to a migrated agent.
Output: Six migrated agent files, modified base_agent.py __init__, the migrated narrative API route, and updated agent + route unit tests that instantiate via AgentDependencies.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/096-agent-registry/096-CONTEXT.md
@.planning/phases/096-agent-registry/096-RESEARCH.md
@.planning/phases/096-agent-registry/096-PATTERNS.md
@.planning/phases/096-agent-registry/096-REVIEWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change BaseAIWorker.__init__ to accept keyword-only dependencies: AgentDependencies</name>
  <files>src/core/ai/base_agent.py</files>
  <read_first>
    - src/core/ai/base_agent.py (lines 95-103: current __init__ signature `(self, name: str | None = None, *args, **kwargs)`)
    - src/core/ai/agent_dependencies.py (the AgentDependencies dataclass from 096-01)
    - src/core/ai/evaluator.py (confirm Evaluator does NOT override __init__ — it inherits BaseAIWorker.__init__)
    - .planning/phases/096-agent-registry/096-PATTERNS.md (lines 286-309: target __init__ signature)
  </read_first>
  <action>
    In src/core/ai/base_agent.py change the `__init__` signature from `(self, name: str | None = None, *args: Any, **kwargs: Any)` to `(self, *, dependencies: "AgentDependencies | None" = None, name: str | None = None, **kwargs: Any)`. Keep the existing body: default `name` to `self.__class__.__name__` when None; call `super().__init__(name=name, **kwargs)` (drop the `*args` passthrough since dependencies is now keyword-only); keep `self._timeout_s = self.latency_budget_ms / 1000.0`, `self._lineage = None`, `self._llm = None`, and the `self._agent_labels` dict. Do NOT extract llm_chain/pool here — subclasses read from dependencies in their own __init__. Add `AgentDependencies` to the `if TYPE_CHECKING:` import block (`from src.core.ai.agent_dependencies import AgentDependencies`). The base __init__ ignores `dependencies` for the agents that override __init__ and pull from it; keeping `dependencies` optional on the base preserves the ability to construct mixin/test subclasses without dependencies.
  </action>
  <verify>.venv/bin/python -c "from src.core.ai.base_agent import BaseAIWorker; import inspect; sig = inspect.signature(BaseAIWorker.__init__); assert 'dependencies' in sig.parameters and sig.parameters['dependencies'].kind == inspect.Parameter.KEYWORD_ONLY"</verify>
  <acceptance_criteria>
    - src/core/ai/base_agent.py __init__ signature contains `*, dependencies:` (keyword-only dependencies)
    - src/core/ai/base_agent.py TYPE_CHECKING block contains `from src.core.ai.agent_dependencies import AgentDependencies`
    - `.venv/bin/python -c "from src.core.ai.base_agent import BaseAIWorker"` exits 0
  </acceptance_criteria>
  <done>BaseAIWorker.__init__ accepts keyword-only `dependencies: AgentDependencies | None = None` and no longer accepts positional args; module imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all six agent constructors to dependencies: AgentDependencies with required-dep guards</name>
  <files>src/intelligence/ai/alpha/skeptic_agent.py, src/intelligence/ai/alpha/correlation_agent.py, src/intelligence/ai/alpha/regime_coherence_agent.py, src/intelligence/ai/alpha/counterfactual_agent.py, src/intelligence/ai/alpha/ml_scorer_agent.py, src/intelligence/ai/narrative/narrative_agent.py</files>
  <read_first>
    - src/intelligence/ai/alpha/skeptic_agent.py (lines 23-24 TYPE_CHECKING block, constructor `def __init__(self, llm_chain: LLMProviderChain, **kwargs)`, body `self._llm = llm_chain`)
    - src/intelligence/ai/alpha/correlation_agent.py (constructor — same llm_chain pattern)
    - src/intelligence/ai/alpha/regime_coherence_agent.py (constructor — same llm_chain pattern)
    - src/intelligence/ai/alpha/counterfactual_agent.py (constructor — same llm_chain pattern)
    - src/intelligence/ai/alpha/ml_scorer_agent.py (lines 114-124: `def __init__(self, pool: Any, **kwargs)`, body sets self._pool and self._registry = ModelRegistry(pool))
    - src/intelligence/ai/narrative/narrative_agent.py (lines 16-22 imports LLMProviderChain at MODULE level not TYPE_CHECKING; line 44 shadow_only=False; constructor `def __init__(self, llm_chain: LLMProviderChain, **kwargs)`)
    - src/core/ai/agent_dependencies.py (the AgentDependencies fields: llm_chain, pool, settings)
  </read_first>
  <action>
    For the four LLM alpha agents (skeptic_agent.py SkepticEvaluator, correlation_agent.py CorrelationAnalyzer, regime_coherence_agent.py RegimeCoherenceAnalyzer, counterfactual_agent.py CounterfactualEvaluator): change each `def __init__(self, llm_chain: LLMProviderChain, **kwargs)` to `def __init__(self, *, dependencies: "AgentDependencies", **kwargs)`, keep the existing `super().__init__(name=..., **kwargs)` call (preserve each class's exact `name=` literal), and change the body line `self._llm = llm_chain` to `self._llm = dependencies.llm_chain`. Add a required-dep guard immediately after assignment (Codex MEDIUM concern): `if self._llm is None: raise ValueError(f"{type(self).__name__} requires dependencies.llm_chain")`. Ensure each file's `if TYPE_CHECKING:` block imports `from src.core.ai.agent_dependencies import AgentDependencies` (add it if missing).

    For ml_scorer_agent.py MLEvaluator: change `def __init__(self, pool: Any, **kwargs)` to `def __init__(self, *, dependencies: "AgentDependencies", **kwargs)`, keep `super().__init__(**kwargs)`, add the guard `if dependencies.pool is None: raise ValueError("MLEvaluator requires dependencies.pool")`, then change `self._pool = pool` to `self._pool = dependencies.pool` and `self._registry = ModelRegistry(pool)` to `self._registry = ModelRegistry(dependencies.pool)`; leave `_models`/`_feature_cols` init and the async `_setup_models()` method unchanged. Add AgentDependencies under TYPE_CHECKING in ml_scorer_agent.py.

    For narrative_agent.py NarrativeSynthesizer: change `def __init__(self, llm_chain: LLMProviderChain, **kwargs)` to `def __init__(self, *, dependencies: "AgentDependencies", **kwargs)`, keep `super().__init__(**kwargs)`, change body to `self._llm = dependencies.llm_chain` and add the guard `if self._llm is None: raise ValueError("NarrativeSynthesizer requires dependencies.llm_chain")`; add `from src.core.ai.agent_dependencies import AgentDependencies` under a TYPE_CHECKING guard (narrative_agent.py currently imports LLMProviderChain at module level — leave that import as-is since it may be used elsewhere, but add the TYPE_CHECKING block if one does not exist). Leave `shadow_only = False` on NarrativeSynthesizer untouched (D-05: class default wins when YAML has no override; only explicit YAML shadow_only:false is rejected). NOTE: shadow_only=False on the class is only the in-memory default; the DB row's is_shadow value (written in Plan 03 via _shadow_registry_ensure_agents with is_shadow=TRUE, and then read back and applied to the instance in Plan 03 Task 4) is the authoritative gate (D-05). The class attribute is NOT a force-production bypass.
  </action>
  <verify>.venv/bin/python -c "import src.intelligence.ai.alpha.skeptic_agent, src.intelligence.ai.alpha.correlation_agent, src.intelligence.ai.alpha.regime_coherence_agent, src.intelligence.ai.alpha.counterfactual_agent, src.intelligence.ai.alpha.ml_scorer_agent, src.intelligence.ai.narrative.narrative_agent; from src.core.ai.registry import _REGISTRY; assert set(['skeptic','correlation_v1','regime_coherence_v1','counterfactual_v1','ml_scorer_v1','narrative_v1']) <= set(_REGISTRY)"</verify>
  <acceptance_criteria>
    - All four LLM alpha agent files contain `def __init__(self, *, dependencies: "AgentDependencies"` and `self._llm = dependencies.llm_chain`
    - Each LLM agent and NarrativeSynthesizer raise `ValueError` when `dependencies.llm_chain is None`
    - src/intelligence/ai/alpha/ml_scorer_agent.py contains `dependencies.pool` and `ModelRegistry(dependencies.pool)` and raises ValueError when `dependencies.pool is None`
    - src/intelligence/ai/narrative/narrative_agent.py contains `dependencies: "AgentDependencies"` and `self._llm = dependencies.llm_chain` and still contains `shadow_only = False`
    - The Task verify command exits 0 (all six agent_ids registered after import)
    - `grep -rn "llm_chain:" src/intelligence/ai/alpha/ src/intelligence/ai/narrative/narrative_agent.py` returns no constructor-parameter matches
  </acceptance_criteria>
  <done>All six agents construct via keyword-only `dependencies: AgentDependencies`, read the correct dependency from dependencies, raise a descriptive error when the required dep is missing, and register their agent_id on import.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the on-demand narrative API route to AgentDependencies + update its test</name>
  <files>src/api/routes/narrative.py, tests/unit/api/test_narrative_route.py</files>
  <read_first>
    - src/api/routes/narrative.py (line 29 import of NarrativeSynthesizer; lines 57-69 _get_settings/_get_llm_chain lru_cache helpers; line 219 `agent = NarrativeSynthesizer(llm_chain=_get_llm_chain())` — the request-scoped construction that breaks under the new signature)
    - src/core/ai/agent_dependencies.py (AgentDependencies constructor — llm_chain, pool, settings)
    - tests/unit/api/test_narrative_route.py (patches `src.api.routes.narrative.NarrativeSynthesizer.compute` at ~lines 150, 225 — these patch the METHOD, not the constructor; confirm whether any test constructs NarrativeSynthesizer directly with llm_chain=)
  </read_first>
  <action>
    In src/api/routes/narrative.py: add `from ...core.ai.agent_dependencies import AgentDependencies` to the imports. At line 219, change `agent = NarrativeSynthesizer(llm_chain=_get_llm_chain())` to construct via AgentDependencies: build `agent_dependencies = AgentDependencies(llm_chain=_get_llm_chain(), pool=None, settings=_get_settings())` then `agent = NarrativeSynthesizer(dependencies=agent_dependencies)`. (Use the existing `_get_settings()` lru_cache helper for settings; pool is None because the route's narrative agent does not need a DB pool — DB access in the route goes through the injected `DatabaseManager`, not the agent.) This is a REQUEST-SCOPED construction path, distinct from swarm-startup registry construction — it is the documented exception to "registry is the sole construction path" noted in Plan 03's must_haves. Do NOT route this through AgentRegistry: the registry is for swarm-coordinator startup; the API route is a per-request on-demand path and intentionally constructs directly via AgentDependencies.

    In tests/unit/api/test_narrative_route.py: run the test suite first to see if it breaks. The tests patch `NarrativeSynthesizer.compute` (the method) rather than the constructor, so the construction change may be transparent. If any test constructs `NarrativeSynthesizer(llm_chain=...)` directly, update it to `NarrativeSynthesizer(dependencies=AgentDependencies(llm_chain=<x>, pool=None, settings=None))` and add the AgentDependencies import. If the suite passes unchanged, make no edits to the test file beyond what is required and note that in the SUMMARY.
  </action>
  <verify>.venv/bin/pytest tests/unit/api/test_narrative_route.py -q && .venv/bin/python -c "import inspect, src.api.routes.narrative as n; src = inspect.getsource(n); assert 'NarrativeSynthesizer(llm_chain=' not in src and 'AgentDependencies(' in src"</verify>
  <acceptance_criteria>
    - src/api/routes/narrative.py contains `from ...core.ai.agent_dependencies import AgentDependencies` (or equivalent import path)
    - src/api/routes/narrative.py does NOT contain `NarrativeSynthesizer(llm_chain=`
    - src/api/routes/narrative.py constructs `NarrativeSynthesizer(dependencies=...)` with a AgentDependencies carrying `llm_chain=_get_llm_chain()`
    - `.venv/bin/pytest tests/unit/api/test_narrative_route.py -q` exits 0
  </acceptance_criteria>
  <done>The on-demand narrative route constructs NarrativeSynthesizer via AgentDependencies; no old-signature call site remains anywhere; the route test is green. This is the documented request-scoped exception to registry-sole-construction (see Plan 03 must_haves).</done>
</task>

<task type="auto">
  <name>Task 4: Update agent unit tests to instantiate via AgentDependencies</name>
  <files>tests/unit/services/test_skeptic_agent.py, tests/unit/services/test_correlation.py, tests/unit/services/test_regime_coherence.py, tests/unit/services/test_counterfactual_agent.py</files>
  <read_first>
    - tests/unit/services/test_skeptic_agent.py (how it constructs SkepticEvaluator — currently llm_chain=)
    - tests/unit/services/test_correlation.py (CorrelationAnalyzer construction)
    - tests/unit/services/test_regime_coherence.py (RegimeCoherenceAnalyzer construction)
    - tests/unit/services/test_counterfactual_agent.py (CounterfactualEvaluator construction)
    - src/core/ai/agent_dependencies.py (AgentDependencies constructor: llm_chain, pool, settings)
  </read_first>
  <action>
    In each of the four test files, replace every agent construction that passes `llm_chain=<x>` with construction via AgentDependencies: build `agent_dependencies = AgentDependencies(llm_chain=<x>, pool=None, settings=<existing settings mock or None>)` and construct the agent as `Agent(dependencies=agent_dependencies)`. Add `from src.core.ai.agent_dependencies import AgentDependencies` to each test file. The `<x>` passed as llm_chain must be a non-None mock/fake (the new constructor guard raises ValueError on None llm_chain) — if any test previously passed `llm_chain=None` intentionally, give it a Mock() llm_chain or assert it now raises ValueError. Where a test for MLEvaluator (if present in these files) passes `pool=`, route it through `AgentDependencies(llm_chain=None, pool=<pool>, settings=...)`. Use `None` for settings in tests that do not exercise settings, since AgentDependencies does not validate. Do not change test assertions about agent behavior — only the construction calls.
  </action>
  <verify>.venv/bin/pytest tests/unit/services/test_skeptic_agent.py tests/unit/services/test_correlation.py tests/unit/services/test_regime_coherence.py tests/unit/services/test_counterfactual_agent.py -q</verify>
  <acceptance_criteria>
    - Each of the four test files contains `from src.core.ai.agent_dependencies import AgentDependencies`
    - Each of the four test files constructs its agent with `dependencies=` (not `llm_chain=`)
    - No test passes `llm_chain=None` to an LLM agent and expects success (the new guard raises)
    - `.venv/bin/pytest tests/unit/services/test_skeptic_agent.py tests/unit/services/test_correlation.py tests/unit/services/test_regime_coherence.py tests/unit/services/test_counterfactual_agent.py -q` exits 0
  </acceptance_criteria>
  <done>All four agent unit-test files instantiate agents via AgentDependencies and pass green.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -c "import src.intelligence.ai.alpha.skeptic_agent, src.intelligence.ai.alpha.correlation_agent, src.intelligence.ai.alpha.regime_coherence_agent, src.intelligence.ai.alpha.counterfactual_agent, src.intelligence.ai.alpha.ml_scorer_agent, src.intelligence.ai.narrative.narrative_agent, src.api.routes.narrative"` exits 0
- Full call-site sweep: `rg "(SkepticEvaluator|CorrelationAnalyzer|RegimeCoherenceAnalyzer|CounterfactualEvaluator|MLEvaluator|NarrativeSynthesizer)\(" src/ services/ tests/` shows NO remaining `(llm_chain=` or `(pool=` call (only `(dependencies=` constructions remain, plus the registry's `cls(dependencies=agent_dependencies)`)
- `.venv/bin/pytest tests/unit/services/test_skeptic_agent.py tests/unit/services/test_correlation.py tests/unit/services/test_regime_coherence.py tests/unit/services/test_counterfactual_agent.py tests/unit/api/test_narrative_route.py tests/unit/core/ -q` exits 0
- `.venv/bin/ruff check src/intelligence/ai/alpha/ src/intelligence/ai/narrative/narrative_agent.py src/core/ai/base_agent.py src/api/routes/narrative.py` reports no errors
</verification>

<success_criteria>
- BaseAIWorker.__init__ and all six agent constructors accept keyword-only `dependencies: AgentDependencies`
- LLM agents read dependencies.llm_chain; MLEvaluator reads dependencies.pool; each raises a descriptive error on a missing required dep
- The on-demand narrative API route constructs NarrativeSynthesizer via AgentDependencies — no old-signature call site remains in the entire repo
- All four migrated agent unit-test files and the narrative route test pass
- NarrativeSynthesizer.shadow_only remains False (in-memory default; shadow_registry DB row is_shadow=TRUE applied to the instance in Plan 03 is authoritative)
</success_criteria>

<output>
After completion, create `.planning/phases/096-agent-registry/096-02-SUMMARY.md`
</output>
</content>
