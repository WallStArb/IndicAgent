---
phase: 071-base-agent-infrastructure-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/agent/base.py
  - services/bar_aggregator_agent.py
  - services/roll_compute_agent.py
  - services/ai_narrative_agent.py
  - services/swarm_orchestrator_agent.py
  - services/signal_metrics_compute_agent.py
  - services/cross_asset_service.py
  - services/feature_writer_agent.py
  - services/signal_tracker_compute_agent.py
  - services/intelligence_pipeline_agent.py
  - services/ml_discovery_agent.py
  - services/ml_orchestrator_agent.py
  - services/ml_data_quality_agent.py
  - services/provider_merger_agent.py
  - services/parity_auditor_agent.py
  - services/feature_snapshot_writer_agent.py
  - services/bar_writer_agent.py
  - services/signal_writer_agent.py
  - services/lifecycle_writer_agent.py
  - services/signal_auditor_agent.py
  - services/service_auditor_agent.py
  - services/contract_metadata_writer_agent.py
  - services/bar_auditor_agent.py
  - services/signal_metrics_writer_agent.py
  - services/swarm_writer_agent.py
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "BaseAgent.start() calls init_tracing(self.name) automatically before _setup()"
    - "Module-level flag prevents duplicate init_tracing() calls"
    - "All agents have init_tracing() removed from __main__ blocks"
    - "OTel tracer is available via self.tracer in all agents"
  artifacts:
    - path: "src/core/agent/base.py"
      provides: "BaseAgent with auto init_tracing()"
      contains: "_tracing_initialized: bool = False"
    - path: "services/*/agent.py"
      provides: "Agents without init_tracing() in __main__"
      contains: "if __name__ == '__main__':" (without init_tracing call)
  key_links:
    - from: "src/core/agent/base.py"
      to: "src/observability/otel.py"
      via: "init_tracing() function call"
      pattern: "init_tracing\\(service_name=self.name\\)"
---

<objective>
Auto init_tracing() in BaseAgent — Tracing for Everyone

Implement Change 2 from design doc: BaseAgent.start() calls `init_tracing(self.name)` before `_setup()`, guarded by a module-level flag to ensure idempotency. Remove `init_tracing()` calls from `__main__` blocks in all agents.

Purpose: 6-8 agents call `init_tracing()` in their `__main__` blocks, others don't. The OTel tracer from BaseAgent is a no-op when `init_tracing()` hasn't been called — partial coverage silently. Renaissance rule: instrument everything.

Output: All agents get OTel tracing automatically via BaseAgent.start(). No manual init_tracing() calls in __main__ blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/071-base-agent-infrastructure-alignment/071-CONTEXT.md
@docs/superpowers/specs/2026-04-14-base-agent-infrastructure-alignment-design.md
@src/core/agent/base.py
@src/observability/otel.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auto init_tracing() to BaseAgent.start()</name>
  <files>src/core/agent/base.py</files>
  <read_first>
    - src/core/agent/base.py
    - src/observability/otel.py
  </read_first>
  <action>
    1. Add module-level flag at the top of base.py (after imports, before AGENT_CRASH_TOTAL):
    ```python
    # Module-level flag to ensure init_tracing() is called only once per process
    _tracing_initialized: bool = False
    ```

    2. Add import at top of file if not present:
    ```python
    from src.observability.otel import init_tracing
    ```

    3. In BaseAgent.start() method, before the `self.logger.info("agent.starting", agent=self.name)` line, add:
    ```python
    # Initialize OTel tracing (idempotent — first call wins)
    global _tracing_initialized
    if not _tracing_initialized:
        init_tracing(service_name=self.name)
        _tracing_initialized = True
    ```

    This must be placed AFTER _register_signal_handlers() and BEFORE logging, so tracing is ready for any spans emitted during _setup().
  </action>
  <verify>
    <automated>grep -n "_tracing_initialized: bool = False" src/core/agent/base.py</automated>
    <automated>grep -n "if not _tracing_initialized:" src/core/agent/base.py</automated>
    <automated>grep -n "init_tracing(service_name=self.name)" src/core/agent/base.py</automated>
  </verify>
  <done>
    BaseAgent.start() calls init_tracing() automatically with idempotency guard
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove init_tracing() calls from agent __main__ blocks</name>
  <files>
    services/bar_aggregator_agent.py
    services/roll_compute_agent.py
    services/ai_narrative_agent.py
    services/swarm_orchestrator_agent.py
    services/signal_metrics_compute_agent.py
    services/cross_asset_service.py
    services/feature_writer_agent.py
    services/signal_tracker_compute_agent.py
    services/intelligence_pipeline_agent.py
    services/ml_discovery_agent.py
    services/ml_orchestrator_agent.py
    services/ml_data_quality_agent.py
    services/provider_merger_agent.py
    services/parity_auditor_agent.py
    services/feature_snapshot_writer_agent.py
    services/bar_writer_agent.py
    services/signal_writer_agent.py
    services/lifecycle_writer_agent.py
    services/signal_auditor_agent.py
    services/service_auditor_agent.py
    services/contract_metadata_writer_agent.py
    services/bar_auditor_agent.py
    services/signal_metrics_writer_agent.py
    services/swarm_writer_agent.py
  </files>
  <read_first>
    - services/bar_aggregator_agent.py
    - services/roll_compute_agent.py
    - services/feature_writer_agent.py
  </read_first>
  <action>
    For each agent file that contains `init_tracing(` in its `if __name__ == "__main__":` block:

    1. Remove the `init_tracing(...)` call
    2. Keep the rest of the block intact (asyncio.run(main()), etc.)

    Example transformation:
    BEFORE:
    ```python
    if __name__ == "__main__":
        from src.observability.otel import init_tracing
        init_tracing(service_name="roll_compute_agent")
        asyncio.run(main())
    ```

    AFTER:
    ```python
    if __name__ == "__main__":
        asyncio.run(main())
    ```

    For agents that also call `setup_service_logging()` in __main__, leave those for Plan 03 (vestigial cleanup).

    Do NOT modify:
    - LLMWriterService (llm_writer_service.py) — handled in Plan 05
    - Archived agents (_archived_*.py files)
  </action>
  <verify>
    <automated>! grep -r "init_tracing(" services/*.py | grep "__main__" | grep -v "_archived" | grep -v "llm_writer_service.py" | wc -l | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    All agent __main__ blocks have init_tracing() removed (now automatic via BaseAgent)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BaseAgent→OTEL | init_tracing() configures OpenTelemetry exporter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-071-03 | I | init_tracing() endpoint | accept | OTEL endpoint is internal (localhost:4318); no external data; read-only tracing |
| T-071-04 | D | init_tracing() data leak | accept | Tracing data is internal telemetry; no secrets in spans |
</threat_model>

<verification>
- [ ] _tracing_initialized flag added to base.py
- [ ] BaseAgent.start() calls init_tracing() before logging
- [ ] No init_tracing() calls remain in agent __main__ blocks
- [ ] All agents still have functional __main__ entry points
</verification>

<success_criteria>
- [ ] BaseAgent.start() initializes OTel tracing automatically
- [ ] Module-level flag prevents duplicate init_tracing() calls
- [ ] All agent __main__ blocks are simplified (no init_tracing calls)
- [ ] Unit tests pass: `.venv/bin/pytest tests/unit/ -v`
- [ ] Linting passes: `.venv/bin/ruff check . --fix`
</success_criteria>

<output>
After completion, create `.planning/phases/071-base-agent-infrastructure-alignment/071-02-SUMMARY.md`
</output>
