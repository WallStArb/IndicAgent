---
phase: 29-renaissance-signal-quality
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/intelligence/trading/cis_scorer.py
  - tests/unit/intelligence/test_cis_scorer.py
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "Every CIS computation populates constituent_contributions with per-feature breakdown — never empty dicts"
    - "Each of the 6 bucket methods returns (float, dict[str, float]) — score() unpacks correctly"
    - "bucket_scores values remain floats — not tuples — after refactor"
    - "Existing CIS score values are unchanged — refactor is additive only"
  artifacts:
    - path: "src/intelligence/trading/cis_scorer.py"
      provides: "6 refactored bucket methods + score() assembly"
      contains: "constituent_contributions"
    - path: "tests/unit/intelligence/test_cis_scorer.py"
      provides: "Tests confirming contributions populated"
  key_links:
    - from: "cis_scorer.py _trend/_momentum/_structure/_pattern/_institutional/_regime"
      to: "cis_scorer.py score()"
      via: "tuple unpacking"
      pattern: "trend_score, trend_contrib = self\\._trend"
---

<objective>
Populate `constituent_contributions` in CISResult — the per-feature breakdown of what drove each CIS bucket score. Currently always `{b: {} for b in BUCKET_NAMES}` (a known placeholder).

Purpose: Per-feature contributions cannot be reconstructed after the scoring function evolves. Capture now — Renaissance: never drop data that could contain signal.
Output: All 6 bucket methods return `(float, dict[str, float])`; `score()` assembles contributions into `CISResult.constituent_contributions`.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-renaissance-signal-quality/29-CONTEXT.md
@.planning/phases/29-renaissance-signal-quality/29-RESEARCH.md

@src/intelligence/trading/cis_scorer.py
@tests/unit/intelligence/test_cis_scorer.py
</context>

<feature>
  <name>CIS constituent_contributions population (QUAL-01)</name>
  <files>src/intelligence/trading/cis_scorer.py, tests/unit/intelligence/test_cis_scorer.py</files>
  <behavior>
    TDD cycle — RED then GREEN then REFACTOR:

    RED — write failing tests first:
    - Test: score() returns CISResult where constituent_contributions["trend"] contains at least one feature key with float value
    - Test: constituent_contributions["momentum"] contains "rsi_14" and "macd_histogram_12_26_9" keys
    - Test: constituent_contributions keys == set(BUCKET_NAMES) — all 6 buckets present
    - Test: no bucket's contribution dict is empty ({}) — every bucket has at least one entry
    - Test: existing bucket_scores["trend"] is float, not tuple — refactor preserves return type at score() level
    - Test: CIS score value unchanged after refactor (regression: same inputs produce same cis_score)

    GREEN — implement:
    Refactor all 6 bucket methods to return `(float, dict[str, float])` where the dict maps
    feature names to their weighted contributions. Update score() to unpack tuples and assemble
    contributions. See exact contribution format in CONTEXT.md T0 decision block.

    REFACTOR — clean up:
    Ensure contribution dicts contain exactly the features each bucket reads, no extras.
  </behavior>
  <implementation>
    Refactor each bucket method signature from `def _trend(self, f) -> float` to
    `def _trend(self, f) -> tuple[float, dict[str, float]]`. The contribution dict records
    the weighted value each named feature contributed: e.g., for _trend(), contributions are
    `{"kalman_slope": 0.20 * slope_dir, "trend_regime": 0.35 * clamp(trend_regime), ...}`.

    In score(), update all 6 call sites:
    ```python
    trend_score, trend_contrib = self._trend(features)
    # ... (same pattern for all 6 buckets)
    bucket_scores = {"trend": trend_score, "momentum": momentum_score, ...}
    contributions = {"trend": trend_contrib, "momentum": momentum_contrib, ...}
    ```

    Replace the placeholder at line ~162:
    `constituent_contributions={b: {} for b in BUCKET_NAMES},  # populated in Task 13`
    with:
    `constituent_contributions=contributions,`

    CRITICAL: Do not change the score() method's public signature. CISResult is unchanged.
    Run `.venv/bin/pytest tests/unit/intelligence/test_cis_scorer.py -x` after each bucket
    method refactor — catch the tuple-vs-float pitfall immediately.
  </implementation>
</feature>

<verification>
After RED-GREEN-REFACTOR cycle:
`.venv/bin/pytest tests/unit/intelligence/test_cis_scorer.py -x -v`
Full suite regression: `.venv/bin/pytest tests/unit/ -q --tb=short`
</verification>

<success_criteria>
- All new contribution tests pass (GREEN)
- All existing CIS scorer tests still pass (no regression)
- constituent_contributions contains 6 keys with non-empty dicts
- bucket_scores values are floats (not tuples)
- Full unit suite green
</success_criteria>

<output>
After completion, create `.planning/phases/29-renaissance-signal-quality/29-01-SUMMARY.md`
</output>
