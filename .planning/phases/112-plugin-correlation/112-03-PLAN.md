---
phase: 112-plugin-correlation
plan: 03
type: execute
wave: 2
depends_on: ["112-01"]
requirements: [CORR-02]
files_modified:
  - src/intelligence/pipeline/cache_manager.py
  - src/intelligence/pipeline/signal_processor.py
  - src/intelligence/pipeline/executor.py
  - tests/unit/intelligence/test_correlation_suppression_skip.py
autonomous: true

must_haves:
  truths:
    - "intelligence_pipeline excludes correlation-suppressed plugins from the I7 execution set before any _compute() call"
    - "CacheManager exposes a suppressed_plugins set loaded from shadow_registry.correlation_suppressed"
    - "The suppressed set refreshes on the same 300s schedule as shadow_cache"
    - "A suppressed plugin produces zero signals (its _compute is never invoked)"
  artifacts:
    - path: "src/intelligence/pipeline/cache_manager.py"
      provides: "_suppressed_plugins set, suppressed_plugins property, load in _load_shadow_cache, included in snapshot()"
      contains: "suppressed_plugins"
    - path: "src/intelligence/pipeline/signal_processor.py"
      provides: "suppressed_plugins field on CacheSnapshot"
      contains: "suppressed_plugins"
    - path: "src/intelligence/pipeline/executor.py"
      provides: "skip gate in run_i7_plugins before building PluginTask"
      contains: "suppressed_plugins"
    - path: "tests/unit/intelligence/test_correlation_suppression_skip.py"
      provides: "Test proving a suppressed plugin's _compute is never called"
      contains: "def test_"
  key_links:
    - from: "executor.run_i7_plugins"
      to: "cache_snapshot.suppressed_plugins"
      via: "continue gate before run_in_executor(_timed_plugin_call)"
      pattern: "suppressed_plugins"
    - from: "cache_manager._load_shadow_cache"
      to: "shadow_registry.correlation_suppressed"
      via: "SELECT component_name WHERE correlation_suppressed = true"
      pattern: "correlation_suppressed"
---

<objective>
Wire correlation suppression into the live pipeline so suppressed plugins are excluded from execution before any model runs (D-15/D-16).

Purpose: The migration and batch alone do not stop suppressed plugins from running. The cache_manager's existing shadow_cache only STAMPS is_shadow on emitted signals — it does not skip _compute(). Per the RESEARCH correction (pitfall 3), a separate suppressed_plugins set plus an explicit skip gate in the I7 executor loop is required to actually prevent the work. Inference is not free — do not run models to produce output that will be discarded.
Output: CacheManager.suppressed_plugins property, CacheSnapshot.suppressed_plugins field, executor skip gate, and a unit test proving _compute is never called for a suppressed plugin.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-plugin-correlation/112-CONTEXT.md
@.planning/phases/112-plugin-correlation/112-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add suppressed_plugins to CacheManager and CacheSnapshot</name>
  <files>src/intelligence/pipeline/cache_manager.py, src/intelligence/pipeline/signal_processor.py</files>
  <read_first>
    - src/intelligence/pipeline/cache_manager.py (read: __init__ cache-dict block ~lines 141-147; shadow_cache property ~lines 199-202; snapshot() ~lines 263-283; _load_shadow_cache ~lines 522-532; seed_shadow_cache ~lines 315-317; the 300s refresh loop registration ~line 382)
    - src/intelligence/pipeline/signal_processor.py (read CacheSnapshot dataclass ~lines 114-132 — note shadow_cache is the last field with default_factory=dict)
    - .planning/phases/112-plugin-correlation/112-RESEARCH.md (cache_manager integration section + Pitfall 3: the cleanest approach is a separate suppressed set, NOT querying the VIEW for shadow_cache)
  </read_first>
  <action>
    In `src/intelligence/pipeline/cache_manager.py`:
    1. In `__init__`, alongside `self._shadow_cache: dict[str, bool] = {}` (line ~145), add `self._suppressed_plugins: set[str] = set()`.
    2. Add a property mirroring the shadow_cache property:
       ```python
       @property
       def suppressed_plugins(self) -> set[str]:
           """Plugin names with correlation_suppressed=true in shadow_registry.

           Owned exclusively by the correlation batch (D-10). These plugins are
           excluded from the I7 execution set before any _compute() call (D-15).
           """
           return self._suppressed_plugins
       ```
    3. In `_load_shadow_cache` (line ~522), AFTER the existing shadow_cache load, add a second query (keep shadow_cache reading the BASE table `shadow_registry` — do NOT switch it to the VIEW; the VIEW only changes the is_shadow stamp, which is not the goal here):
       ```python
       suppressed_rows = await self._db.execute_query(
           "SELECT component_name FROM shadow_registry WHERE correlation_suppressed = true"
       )
       self._suppressed_plugins = {r["component_name"] for r in suppressed_rows}
       ```
       Match the existing exception handling (the warning log on load_failed should leave the previous set intact — do not blank the set on a transient DB error).
    4. In `snapshot()` (line ~272), add `suppressed_plugins=set(self._suppressed_plugins)` to the CacheSnapshot constructor call.
    5. Add a seed method next to `seed_shadow_cache`:
       ```python
       def seed_suppressed_plugins(self, names: set[str]) -> None:
           """Atomically replace the correlation-suppressed set (tests/checkpoint restore)."""
           self._suppressed_plugins = set(names)
       ```
    The refresh loop at line ~382 already re-runs `_load_shadow_cache` every 300s — because the suppressed query lives inside that method, the set refreshes on the same schedule automatically (no new loop needed).

    In `src/intelligence/pipeline/signal_processor.py`:
    6. Add a field to the `CacheSnapshot` dataclass after `shadow_cache`:
       ```python
       suppressed_plugins: set = field(default_factory=set)
       ```
       Keep it defaulted so existing call sites and tests that construct CacheSnapshot without it remain valid (backward compatible, same convention as shadow_cache).
  </action>
  <verify>
    `cd /home/bg/dev/indicagent && .venv/bin/python -c "from src.intelligence.pipeline.cache_manager import CacheManager; from src.intelligence.pipeline.signal_processor import CacheSnapshot; cs = CacheSnapshot(perf_weights={}, calibration_curves={}, tod_priors={}, drift_penalties={}, cis_weights={}, cis_weights_version=0); print(type(cs.suppressed_plugins).__name__, len(cs.suppressed_plugins))"`
  </verify>
  <acceptance_criteria>
    - The verify command prints `set 0` (CacheSnapshot has a suppressed_plugins set field defaulting to empty).
    - `grep -n "suppressed_plugins" src/intelligence/pipeline/cache_manager.py` shows: the __init__ attr, the property, the load query, the snapshot() inclusion, and the seed method (>= 5 references).
    - `grep -n "correlation_suppressed = true" src/intelligence/pipeline/cache_manager.py` shows the second query in _load_shadow_cache reads the BASE shadow_registry table (NOT shadow_registry_active).
    - `grep -n "is_shadow FROM shadow_registry\b" src/intelligence/pipeline/cache_manager.py` confirms the original shadow_cache query still reads the base table (unchanged — VIEW switch was deliberately NOT done here per RESEARCH pitfall 3).
    - `grep -n "suppressed_plugins" src/intelligence/pipeline/signal_processor.py` shows the new CacheSnapshot field.
  </acceptance_criteria>
  <done>CacheManager loads and exposes suppressed_plugins; CacheSnapshot carries it; both refresh on the existing 300s shadow loop.</done>
</task>

<task type="auto">
  <name>Task 2: Add the I7 skip gate and a regression test</name>
  <files>src/intelligence/pipeline/executor.py, tests/unit/intelligence/test_correlation_suppression_skip.py</files>
  <read_first>
    - src/intelligence/pipeline/executor.py (read run_i7_plugins ~lines 558-637 — the `for plugin_name in TIER_I7:` loop with should_skip_plugin / circuit breaker / min_lookback guards; and run_i7_complete ~lines 643-704 where shadow_cache=cache_snapshot.shadow_cache is passed in)
    - src/intelligence/pipeline/signal_processor.py (CacheSnapshot now has suppressed_plugins from Task 1)
    - .planning/phases/112-plugin-correlation/112-RESEARCH.md (executor change: skip gate `if plugin_name in suppressed_plugins: continue` before _compute)
    - tests/unit/intelligence/ (read one existing executor/plugin test for the fixture + construction conventions used in this directory)
  </read_first>
  <action>
    In `src/intelligence/pipeline/executor.py`:
    1. Change the `run_i7_plugins` signature to also accept the suppressed set. It currently takes `shadow_cache: dict` (line ~564). Add a parameter `suppressed_plugins: set[str] | None = None` (default None for backward compatibility with any existing direct callers/tests). Normalize at the top of the method: `suppressed = suppressed_plugins or set()`.
    2. Inside the `for plugin_name in TIER_I7:` loop, add the skip gate as the FIRST check (before `self._plugin_cache.get`, before should_skip_plugin, before the circuit breaker and min_lookback guards — so suppressed plugins are excluded before any work, including before _timed_plugin_call is scheduled):
       ```python
       if plugin_name in suppressed:
           continue  # D-15: correlation-suppressed — exclude before any _compute()
       ```
    3. In `run_i7_complete` (line ~696), pass the suppressed set through from the snapshot:
       ```python
       tasks, outputs, sig_state_updates = await self.run_i7_plugins(
           ...,
           shadow_cache=cache_snapshot.shadow_cache,
           suppressed_plugins=cache_snapshot.suppressed_plugins,
       )
       ```
    Do NOT change `_is_shadow` or the signal-stamping loop — suppression is orthogonal to the is_shadow stamp (a suppressed plugin never produces a signal to stamp).

    Create `tests/unit/intelligence/test_correlation_suppression_skip.py`:
    4. A unit test that constructs the executor (or directly invokes run_i7_plugins with a minimal plugin_input), seeds a CacheSnapshot/suppressed set containing a known TIER_I7 plugin name, and asserts that plugin's compute is never invoked while a non-suppressed plugin's is. Use a spy/mock on the plugin's compute method (or assert the suppressed plugin name is absent from the returned tasks list). Pattern the fixtures after an existing executor test in tests/unit/intelligence/. The assertion must prove: suppressed plugin -> 0 tasks/0 calls; non-suppressed plugin -> present in tasks.
  </action>
  <verify>
    `cd /home/bg/dev/indicagent && .venv/bin/ruff check src/intelligence/pipeline/executor.py tests/unit/intelligence/test_correlation_suppression_skip.py --fix && .venv/bin/black src/intelligence/pipeline/executor.py tests/unit/intelligence/test_correlation_suppression_skip.py`
    `cd /home/bg/dev/indicagent && .venv/bin/pytest tests/unit/intelligence/test_correlation_suppression_skip.py -v`
    `cd /home/bg/dev/indicagent && .venv/bin/pytest tests/unit/ -q`
  </verify>
  <acceptance_criteria>
    - `grep -n "suppressed" src/intelligence/pipeline/executor.py` shows the new parameter, the normalization, the skip-gate `continue`, and the pass-through in run_i7_complete (>= 3 references).
    - The skip gate appears textually BEFORE the `self._plugin_cache.get(plugin_name)` line inside the TIER_I7 loop (verifiable by line-number ordering in the file).
    - `.venv/bin/pytest tests/unit/intelligence/test_correlation_suppression_skip.py -v` passes, and the test asserts the suppressed plugin's compute is never called (0 invocations) while a non-suppressed plugin is scheduled.
    - `.venv/bin/pytest tests/unit/ -q` is green (no regressions — the new optional parameter keeps existing callers working).
  </acceptance_criteria>
  <done>Suppressed plugins are excluded from the I7 loop before any compute call; a regression test proves it; full unit suite green.</done>
</task>

</tasks>

<verification>
- CacheManager.suppressed_plugins loads from shadow_registry.correlation_suppressed and refreshes on the 300s shadow loop.
- CacheSnapshot carries the suppressed set per-bar into the executor.
- run_i7_plugins skips suppressed plugins before any _compute().
- Regression test proves a suppressed plugin's compute is never invoked.
- D-16 (aggregator): `grep -rn "shadow_registry" services/aggregator.py | grep -v "#"` returns 0 results — no direct shadow_registry query in the aggregator; suppressed plugins never reach the aggregator because they produce no signals. Note this in the SUMMARY (no aggregator code change required).
- `.venv/bin/pytest tests/unit/ -q` green.
</verification>

<success_criteria>
- Correlation-suppressed plugins do not run in the live pipeline (no wasted inference).
- The skip set refreshes automatically alongside the existing shadow cache.
- The behavior is locked by a unit test.
</success_criteria>

<output>
After completion, create `.planning/phases/112-plugin-correlation/112-03-SUMMARY.md`. Confirm in the SUMMARY that services/aggregator.py needed no change (D-16 satisfied via the executor skip gate) and why.
</output>
