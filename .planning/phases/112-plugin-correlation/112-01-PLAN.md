---
phase: 112-plugin-correlation
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [CORR-02]
files_modified:
  - production/migrations/110_plugin_correlation.sql
  - src/observability/metrics.py
autonomous: true

must_haves:
  truths:
    - "plugin_correlation_pairs table exists with PRIMARY KEY (plugin_a, plugin_b) and CHECK (plugin_a < plugin_b)"
    - "plugin_correlation_summary table exists with computed_at PRIMARY KEY and keeps one row per run (no UPSERT)"
    - "shadow_registry has a correlation_suppressed boolean NOT NULL DEFAULT false column"
    - "shadow_registry_active VIEW filters to live, non-suppressed plugins"
    - "Three point gauges are importable from src.observability.metrics"
  artifacts:
    - path: "production/migrations/110_plugin_correlation.sql"
      provides: "Two new tables, shadow_registry column, shadow_registry_active VIEW"
      contains: "CREATE TABLE plugin_correlation_pairs"
    - path: "src/observability/metrics.py"
      provides: "EFFECTIVE_PLUGIN_COUNT, PLUGIN_CORR_REDUNDANT, PLUGIN_CORR_SUPPRESSED point gauges"
      contains: "EFFECTIVE_PLUGIN_COUNT"
  key_links:
    - from: "shadow_registry_active"
      to: "shadow_registry"
      via: "VIEW SELECT * WHERE NOT is_shadow AND NOT correlation_suppressed"
      pattern: "NOT is_shadow AND NOT correlation_suppressed"
---

<objective>
Create the schema foundation for plugin correlation analysis: two new tables, a new column on shadow_registry, the shadow_registry_active VIEW, and three OTel point gauges in the central metrics registry.

Purpose: Every other plan in this phase depends on these tables, the column, the VIEW, and the metric handles existing. This is the leverage point — get the schema exactly right (especially the VIEW column names, which the spec got wrong) and downstream work is surgical.
Output: `production/migrations/110_plugin_correlation.sql` applied to the live DB, three point gauges registered in `src/observability/metrics.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-plugin-correlation/112-CONTEXT.md
@.planning/phases/112-plugin-correlation/112-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 110 — tables, column, and corrected VIEW</name>
  <files>production/migrations/110_plugin_correlation.sql</files>
  <read_first>
    - production/migrations/109_config_foundation.sql (most recent migration — copy its header/comment style and SQL formatting conventions)
    - production/migrations/095_signal_ledger_split.sql (reference for CREATE VIEW pattern used elsewhere in this project)
    - .planning/phases/112-plugin-correlation/112-RESEARCH.md (read the "Critical Corrections to Spec" section — the VIEW column name is wrong in the original spec)
  </read_first>
  <action>
    Create `production/migrations/110_plugin_correlation.sql` (110 is the next available number; 109 is the latest existing migration). The file contains four DDL statements in this order:

    1. CREATE TABLE plugin_correlation_pairs:
       - `plugin_a text NOT NULL`
       - `plugin_b text NOT NULL`
       - `directional_r double precision NOT NULL`
       - `co_fire_count integer NOT NULL`
       - `computed_at timestamptz NOT NULL`
       - `PRIMARY KEY (plugin_a, plugin_b)` (enables UPSERT — latest snapshot only, D-11)
       - `CHECK (plugin_a < plugin_b)` (canonical ordering, prevents duplicate pairs, D-06/D-11)
       Use `CREATE TABLE IF NOT EXISTS` for idempotency.

    2. CREATE TABLE plugin_correlation_summary:
       - `computed_at timestamptz NOT NULL PRIMARY KEY`
       - `effective_n double precision NOT NULL`
       - `redundant_pairs integer NOT NULL`
       This table keeps full history (plain INSERT, one row per weekly run, ~52/year, D-12). Use `CREATE TABLE IF NOT EXISTS`.

    3. ALTER TABLE shadow_registry ADD COLUMN IF NOT EXISTS correlation_suppressed boolean NOT NULL DEFAULT false (D-13). This column is owned exclusively by the correlation batch (D-10).

    4. CREATE OR REPLACE VIEW shadow_registry_active AS SELECT * FROM shadow_registry WHERE NOT is_shadow AND NOT correlation_suppressed.

    CRITICAL: The original spec (D-14) says `WHERE promoted = true AND NOT correlation_suppressed`. This is WRONG — shadow_registry has NO `promoted` column. It has `is_shadow boolean NOT NULL DEFAULT true` (inverted semantics: is_shadow=false means live/promoted). The correct predicate is `WHERE NOT is_shadow AND NOT correlation_suppressed`. Confirmed against live `\d shadow_registry`.

    The VIEW must be the single interface for future suppression types — design the WHERE so additional suppression columns only extend this VIEW, never the consumers.
  </action>
  <verify>
    Apply: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/110_plugin_correlation.sql`
    Then run idempotency check (apply twice — second run must not error):
    `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/110_plugin_correlation.sql`
  </verify>
  <acceptance_criteria>
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d plugin_correlation_pairs"` shows columns plugin_a, plugin_b, directional_r, co_fire_count, computed_at, a PRIMARY KEY on (plugin_a, plugin_b), and a CHECK constraint containing `plugin_a < plugin_b`.
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d plugin_correlation_summary"` shows computed_at as PRIMARY KEY plus effective_n and redundant_pairs columns.
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d shadow_registry"` includes a `correlation_suppressed | boolean | not null | false` row.
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT pg_get_viewdef('shadow_registry_active')"` output contains the substring `NOT is_shadow` AND the substring `NOT correlation_suppressed`, and does NOT contain the substring `promoted`.
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT count(*) FROM shadow_registry_active"` returns a non-error integer (proves the VIEW resolves against real columns).
    - Running the migration file a second time produces no errors (idempotent — all statements use IF NOT EXISTS / CREATE OR REPLACE).
  </acceptance_criteria>
  <done>All four DDL statements applied; the VIEW uses NOT is_shadow (not `promoted`); migration is idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: Register three point gauges in the central metrics registry</name>
  <files>src/observability/metrics.py</files>
  <read_first>
    - src/observability/metrics.py (read the point_gauge factory at line 82-84 and the existing module-level metric registration pattern, e.g. PLUGIN_FALLBACK_TOTAL at line 91, JOB_COMPLETED_TOTAL at line 382)
    - .planning/phases/112-plugin-correlation/112-CONTEXT.md (D-17, D-18 metric names and label specs)
  </read_first>
  <action>
    Add three module-level point gauges to `src/observability/metrics.py`, in the plugin pipeline metrics region (near PLUGIN_FALLBACK_TOTAL, after the existing plugin metrics block). Use the existing `point_gauge(name, documentation)` factory (returns an OTel gauge, set via `.set(value, {labels})`):

    ```python
    EFFECTIVE_PLUGIN_COUNT = point_gauge(
        "effective_plugin_count",
        "Effective number of statistically independent I7 plugins (participation ratio)",
    )
    PLUGIN_CORR_REDUNDANT = point_gauge(
        "plugin_correlation_redundant_pairs_total",
        "Plugin pairs with directional_r above the redundancy threshold",
    )
    PLUGIN_CORR_SUPPRESSED = point_gauge(
        "plugin_correlation_suppressed_total",
        "Plugins currently correlation-suppressed in shadow_registry",
    )
    ```

    Per D-17, `effective_plugin_count` carries label `scope='global'` at call time (the gauge itself takes no labels at creation — labels are applied in `.set(value, {"scope": "global"})`). These are registered centrally here (NOT inside the batch script) so any module (batch + API scrape path) can import them. Do NOT import prometheus_client; `point_gauge` uses the OTel SDK.
  </action>
  <verify>
    `cd /home/bg/dev/indicagent && .venv/bin/python -c "from src.observability.metrics import EFFECTIVE_PLUGIN_COUNT, PLUGIN_CORR_REDUNDANT, PLUGIN_CORR_SUPPRESSED; print('ok')"`
  </verify>
  <acceptance_criteria>
    - The import command above prints `ok` with exit code 0.
    - `grep -c "point_gauge(" src/observability/metrics.py` increases by exactly 3 versus the pre-edit count (three new gauge registrations).
    - `grep -n "EFFECTIVE_PLUGIN_COUNT\|PLUGIN_CORR_REDUNDANT\|PLUGIN_CORR_SUPPRESSED" src/observability/metrics.py` shows all three names defined at module level (not inside a function).
    - `grep -c "import prometheus_client\|from prometheus_client" src/observability/metrics.py` returns 0.
  </acceptance_criteria>
  <done>Three point gauges importable from src.observability.metrics; no prometheus_client import added.</done>
</task>

</tasks>

<verification>
- Migration 110 applied and idempotent against live DB.
- shadow_registry_active VIEW resolves and uses `NOT is_shadow AND NOT correlation_suppressed` (NOT the spec's wrong `promoted` column).
- Three point gauges importable from the central metrics registry.
- `.venv/bin/pytest tests/unit/ -q` stays green (no regressions from the metrics.py edit).
</verification>

<success_criteria>
- `plugin_correlation_pairs` and `plugin_correlation_summary` tables exist with the specified PKs and the CHECK constraint.
- `shadow_registry.correlation_suppressed` column exists, NOT NULL DEFAULT false.
- `shadow_registry_active` VIEW exists with the corrected predicate.
- EFFECTIVE_PLUGIN_COUNT, PLUGIN_CORR_REDUNDANT, PLUGIN_CORR_SUPPRESSED importable.
</success_criteria>

<output>
After completion, create `.planning/phases/112-plugin-correlation/112-01-SUMMARY.md`
</output>
