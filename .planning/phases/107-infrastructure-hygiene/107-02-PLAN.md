---
phase: 107-infrastructure-hygiene
plan: 02
type: execute
wave: 2
depends_on: ["107-01"]
files_modified:
  - services/ctx_writer_agent.py
  - services/llm_writer_service.py
  - services/feature_writer_agent.py
  - services/shadow_auditor_agent.py
  - src/observability/metrics.py
autonomous: false
must_haves:
  truths:
    - "ctx_writer_agent._flush() wrapped in observed_span with batch_size and flush_ms attributes"
    - "llm_writer_service._flush() wrapped in observed_span with batch_size and flush_ms attributes"
    - "feature_writer_agent._flush_batch() wrapped in observed_span with batch_size and flush_ms attributes"
    - "ctx_writer_agent uses .add() for OTel counters, not .inc()"
    - "llm_writer_service uses self.db_manager, not self._pool"
    - "feature_writer_agent raises on DB connection failure"
    - "shadow_auditor_agent metrics use create_gauge() with .set(), not create_up_down_counter() with .add()"
    - "Wave 2 verification SQL query returns TRUE (all 4 criteria pass)"
    - "Grafana dashboards show stable metrics for 1-2 hours after deployment"
  artifacts:
    - path: "services/ctx_writer_agent.py"
      provides: "Writer flush span coverage + AttributeError fixes"
      contains: "async with observed_span(\"writer.flush\", tracer=self.tracer)"
    - path: "services/llm_writer_service.py"
      provides: "Writer flush span coverage + pool fix"
      contains: "await self.db_manager.execute_command("
    - path: "services/feature_writer_agent.py"
      provides: "Writer flush span coverage + fail-fast"
      contains: "async with observed_span(\"writer.flush\", tracer=self.tracer)"
    - path: "src/observability/metrics.py"
      provides: "Correct OTel instrument types"
      contains: "SHADOW_WIN_RATE = _meter.create_gauge("
  key_links:
    - from: "services/ctx_writer_agent.py"
      to: "src/observability/spans.py"
      via: "observed_span import"
      pattern: "from src.observability.spans import observed_span"
    - from: "services/ctx_writer_agent.py"
      to: "src/core/agent/base_writer.py"
      via: "super()._teardown() call"
      pattern: "await super\\(\\)._teardown\\(\\)"
    - from: "services/shadow_auditor_agent.py"
      to: "src/observability/metrics.py"
      via: "gauge metric usage"
      pattern: "\\.set\\("
---

<objective>
Add flush span instrumentation to 3 writer services, fix AttributeError bugs causing silent data loss, correct OTel metric instrument types for shadow metrics, and prevent ghost-run mode.

Purpose: Wave 2 eliminates silent failures. Writer flush spans make data loss visible in traces. AttributeError fixes (.inc() → .add(), self._pool → self.db_manager) fix buffer overflow bugs. Ghost-run prevention (raise on DB failure) ensures systemd restarts writers that can't persist. Correct metric types prevent false alerts.

Output: 3 writers with flush span coverage, 0 AttributeError bugs, 5 shadow metrics using correct instrument types.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-infrastructure-hygiene/107-CONTEXT.md
@.planning/phases/107-infrastructure-hygiene/107-RESEARCH.md
@.planning/phases/107-infrastructure-hygiene/107-PATTERNS.md
@.planning/phases/phase-106/106-05-SUMMARY.md

# Only reference prior plan SUMMARYs if genuinely needed
@services/ctx_writer_agent.py
@services/llm_writer_service.py
@services/feature_writer_agent.py
@services/shadow_auditor_agent.py
@src/observability/metrics.py
@src/observability/spans.py
@src/core/agent/base_writer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add flush span instrumentation to ctx_writer_agent</name>
  <files>services/ctx_writer_agent.py</files>
  <read_first>
    - services/ctx_writer_agent.py
    - src/observability/spans.py
  </read_first>
  <action>
    Wrap ctx_writer_agent._flush() in observed_span for visibility:

    1. Add imports if not present:
       - `from src.observability.spans import observed_span, ATTR_BATCH_SIZE, ATTR_FLUSH_MS`
       - `import time`

    2. In _flush() method, wrap the entire flush body:
       - AT START: `async with observed_span("writer.flush", tracer=self.tracer) as span:`
       - ADD: `flush_start = time.monotonic()`
       - AFTER event_batch flush: `span.set_attribute(ATTR_BATCH_SIZE, len(event_batch))`
       - AFTER snapshot_batch flush: `span.set_attribute("snapshot_batch_size", len(snapshot_batch))`
       - BEFORE END: `flush_ms = (time.monotonic() - flush_start) * 1000` and `span.set_attribute(ATTR_FLUSH_MS, flush_ms)`

    3. Fix AttributeError bugs:
       - Replace `CTX_EVENTS_WRITTEN_TOTAL.inc(len(self._ctx_event_buffer))` with `self._events_written.add(len(event_batch), self._batch_latency_attrs)`
       - Replace `CTX_SNAPSHOTS_WRITTEN_TOTAL.inc(len(self._ctx_snapshot_buffer))` with `self._snapshots_written.add(len(snapshot_batch), self._batch_latency_attrs)`
       - Fix .inc() → .add() (OTel counters use .add(), not .inc())

    4. Fix missing super()._teardown():
       - In _teardown() method, add `await super()._teardown()` as the FIRST line
       - This ensures BaseWriterAgent._teardown() runs its final flush guard

    Preserve all existing SQL execution, buffer clearing, and transaction logic.
  </action>
  <verify>
    grep -n "async with observed_span(\"writer.flush\"" services/ctx_writer_agent.py
    grep -n "ATTR_BATCH_SIZE, len(" services/ctx_writer_agent.py
    grep -n "ATTR_FLUSH_MS" services/ctx_writer_agent.py
    grep -n "await super()._teardown()" services/ctx_writer_agent.py
    grep -n "\.add(len(" services/ctx_writer_agent.py | grep "_events_written\|_snapshots_written"
    ! grep -n "\.inc(len(" services/ctx_writer_agent.py
  </verify>
  <done>
    ctx_writer_agent._flush() emits OTel spans with batch_size and flush_ms attributes, uses .add() for OTel counters, and calls super()._teardown() for final flush.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix AttributeError bugs in llm_writer_service</name>
  <files>services/llm_writer_service.py</files>
  <read_first>
    - services/llm_writer_service.py
    - src/core/agent/base_writer.py
    - src/core/database_manager.py
  </read_first>
  <action>
    Fix llm_writer_service AttributeError bugs and add flush span instrumentation:

    1. Add imports if not present:
       - `from src.observability.spans import observed_span, ATTR_BATCH_SIZE, ATTR_FLUSH_MS`
       - `import time`

    2. In _flush() method, wrap the entire flush body in observed_span (same pattern as Task 1)

    3. Fix self._pool AttributeError:
       - Find all instances of: `async with self._pool.acquire() as conn:`
       - Replace with: `await self.db_manager.execute_command(sql, *params)`
       - Remove manual connection acquisition and transaction handling
       - Use DatabaseManager.execute_command() for single SQL commands
       - For batch operations, use `await self.db_manager.execute_batch(sql, batch)`

    4. If self._pool is referenced in __init__ or _setup(), remove it (BaseWriterAgent provides self.db_manager)

    Preserve all existing LLM call update logic and SQL parameter construction.
  </action>
  <verify>
    grep -n "async with observed_span(\"writer.flush\"" services/llm_writer_service.py
    grep -n "self.db_manager.execute_command\|self.db_manager.execute_batch" services/llm_writer_service.py
    ! grep -n "self._pool.acquire" services/llm_writer_service.py
    ! grep -n "self._pool =" services/llm_writer_service.py | grep -v "self._pool = None"
  </verify>
  <done>
    llm_writer_service uses DatabaseManager.execute_command() instead of self._pool, has flush span coverage, and no AttributeError bugs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix ghost-run mode and add flush span to feature_writer_agent</name>
  <files>services/feature_writer_agent.py</files>
  <read_first>
    - services/feature_writer_agent.py
    - src/observability/spans.py
  </read_first>
  <action>
    Fix feature_writer_agent ghost-run mode and add flush span instrumentation:

    1. Add imports if not present:
       - `from src.observability.spans import observed_span, ATTR_BATCH_SIZE, ATTR_FLUSH_MS`
       - `import time`

    2. In _flush_batch() method, wrap the flush body in observed_span (same pattern as Task 1)

    3. Fix ghost-run mode in _connect_database():
       - Find the exception handler in _connect_database()
       - Replace the bare `except Exception as e:` that logs and continues with: `raise`
       - The service MUST raise on DB connection failure so systemd restarts it
       - Change from: `self.logger.error("feature_writer.db_connect_failed", error=str(e))` + fall-through
       - To: `self.logger.error("feature_writer.db_connect_failed", error=str(e))` + `raise`

    4. Add OTel gauge for DB connection state:
       - In __init__(), add: `self._db_connected = _meter.create_gauge("feature_writer_db_connected", description="DB connection state")`
       - In _connect_database(), after successful init: `self._db_connected.set(1)`
       - In _connect_database() exception handler before raise: `self._db_connected.set(0)`

    Preserve all existing feature write logic, batch construction, and SQL execution.
  </action>
  <verify>
    grep -n "async with observed_span(\"writer.flush\"" services/feature_writer_agent.py
    grep -n "raise$" services/feature_writer_agent.py | grep -A5 "db_connect_failed"
    grep -n "_db_connected.set(" services/feature_writer_agent.py
  </verify>
  <done>
    feature_writer_agent raises on DB connection failure (preventing ghost-run), has flush span coverage, and emits DB connection state gauge.
  </done>
</task>

<task type="auto">
  <name>Task 4: Fix shadow metric instrument types in metrics.py</name>
  <files>src/observability/metrics.py</files>
  <read_first>
    - src/observability/metrics.py
    - services/shadow_auditor_agent.py
  </read_first>
  <action>
    Correct OTel instrument types for 5 shadow metrics (wrong type → correct type):

    1. SHADOW_WIN_RATE (line ~244):
       - BEFORE: `SHADOW_WIN_RATE = _meter.create_up_down_counter("shadow_win_rate", ...)`
       - AFTER: `SHADOW_WIN_RATE = _meter.create_gauge("shadow_win_rate", ...)`
       - USAGE: `SHADOW_WIN_RATE.set(win_rate, {"plugin": plugin_id})`

    2. SHADOW_N_RESOLVED (line ~247):
       - BEFORE: `SHADOW_N_RESOLVED = _meter.create_up_down_counter(...)`
       - AFTER: `SHADOW_N_RESOLVED = _meter.create_gauge(...)`
       - USAGE: `SHADOW_N_RESOLVED.set(n_resolved, {"plugin": plugin_id})`

    3. SHADOW_EV_R (line ~250):
       - BEFORE: `SHADOW_EV_R = _meter.create_up_down_counter(...)`
       - AFTER: `SHADOW_EV_R = _meter.create_gauge(...)`
       - USAGE: `SHADOW_EV_R.set(ev_r, {"plugin": plugin_id})`

    4. SHADOW_EV_CI_LOWER (line ~253):
       - BEFORE: `SHADOW_EV_CI_LOWER = _meter.create_up_down_counter(...)`
       - AFTER: `SHADOW_EV_CI_LOWER = _meter.create_gauge(...)`
       - USAGE: `SHADOW_EV_CI_LOWER.set(ci_lower, {"plugin": plugin_id})`

    5. SHADOW_DAYS_TO_GATE (line ~256):
       - BEFORE: `SHADOW_DAYS_TO_GATE = _meter.create_up_down_counter(...)`
       - AFTER: `SHADOW_DAYS_TO_GATE = _meter.create_gauge(...)`
       - USAGE: `SHADOW_DAYS_TO_GATE.set(days_to_gate, {"plugin": plugin_id})`

    6. Update shadow_auditor_agent.py call sites:
       - Replace all `.add(value, attrs)` with `.set(value, attrs)` for the 5 metrics above
       - Do NOT change latency metrics (those are histograms and remain correct)

    Gauge is correct for absolute values (win rate, resolved count, EV). UpDownCounter accumulates deltas (wrong for these metrics).
  </action>
  <verify>
    grep -n "SHADOW_WIN_RATE = _meter.create_gauge" src/observability/metrics.py
    grep -n "SHADOW_N_RESOLVED = _meter.create_gauge" src/observability/metrics.py
    grep -n "SHADOW_EV_R = _meter.create_gauge" src/observability/metrics.py
    grep -n "SHADOW_EV_CI_LOWER = _meter.create_gauge" src/observability/metrics.py
    grep -n "SHADOW_DAYS_TO_GATE = _meter.create_gauge" src/observability/metrics.py
    grep -n "SHADOW_WIN_RATE.set\|SHADOW_N_RESOLVED.set\|SHADOW_EV_R.set\|SHADOW_EV_CI_LOWER.set\|SHADOW_DAYS_TO_GATE.set" services/shadow_auditor_agent.py
    ! grep -n "SHADOW_WIN_RATE.add\|SHADOW_N_RESOLVED.add\|SHADOW_EV_R.add\|SHADOW_EV_CI_LOWER.add\|SHADOW_DAYS_TO_GATE.add" services/shadow_auditor_agent.py
  </verify>
  <done>
    All 5 shadow metrics use create_gauge() with .set() for absolute values; shadow dashboard shows correct values instead of accumulating forever.
  </done>
</task>

<task type="auto">
  <name>Task 5: Fix latency metric instrument types</name>
  <files>src/observability/metrics.py</files>
  <read_first>
    - src/observability/metrics.py
  </read_first>
  <action>
    Correct OTel instrument types for latency metrics (Counter → Histogram):

    1. Search for Counter instruments measuring latency (wrong type):
       - Look for patterns like: `*_latency = _meter.create_counter("*_latency", ...)`
       - Latency MUST use Histogram, not Counter

    2. Fix any latency metrics using Counter:
       - Replace `create_counter` with `create_histogram`
       - Replace unit="1" with unit="ms" or "s"
       - Update usage from `.add(value, attrs)` to `.record(value, attrs)`

    3. Common latency metrics that may need fixing:
       - persistence_batch_latency (if exists)
       - flush_latency (if exists)
       - Any *_latency_seconds or *_latency_ms metrics

    4. Verify no create_counter.*latency patterns remain:
       - Run: `grep -n "create_counter.*latency" src/observability/metrics.py`
       - Expected: 0 results
       - Confirm all latency metrics use create_histogram

    Histogram is correct for latency (provides p50, p95, p99 percentiles). Counter only tracks total count.
  </action>
  <verify>
    ! grep -n "create_counter.*latency" src/observability/metrics.py
    grep -n "create_histogram.*latency" src/observability/metrics.py
  </verify>
  <done>
    All latency metrics use Histogram instrument type with .record() for distribution statistics. No Counter-based latency metrics remain.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Wave 2 deployment</name>
  <what-built>Wave 2 complete: 3 writer services with flush span coverage (ctx_writer, llm_writer, feature_writer), AttributeError bugs fixed (.inc() → .add(), self._pool → db_manager), ghost-run prevented (feature_writer raises on DB failure), 5 shadow metrics corrected (up_down_counter → gauge).</what-built>
  <how-to-verify>
    1. Run Wave 2 verification SQL query from 107-CONTEXT.md:
       ```sql
       SELECT
         (SELECT COUNT(*) * 100.0 / (SELECT COUNT(*) FROM writer_agents) FROM span_coverage WHERE function_name = '_flush' AND span_name IS NOT NULL) >= 100 as hyg01_pass,
         (SELECT COUNT(*) FROM metric_type_violations) = 0 as hyg02_pass,
         (SELECT CASE WHEN uses_enqueue_blocking THEN 0 ELSE buffer_overflow_count / total_messages END FROM service_health_metrics WHERE service_name = 'feature_writer') = 0.00 as hyg03_pass;
       ```
       Expected: All three return TRUE.

    2. Deploy changes: Restart writer services via systemctl:
       ```bash
       sudo systemctl restart indicagent-ctx-writer indicagent-llm-writer indicagent-feature-writer
       ```

    3. Monitor Grafana dashboards for 1-2 hours:
       - Check "Writer Flush Latency" dashboard: P50/P95/P99 latency visible
       - Check "Shadow Governance" dashboard: SHADOW_WIN_RATE shows absolute values (not accumulating)
       - Check "Writer Flush Spans" dashboard: All 3 writers emit spans with batch_size, flush_ms
       - Check "Service Health" dashboard: All 3 writers green, no ghost-run
       - Check logs: tail -50 logs/ctx_writer_agent.log, llm_writer_service.log, feature_writer_agent.log
       - Verify no AttributeError exceptions, flush spans emitting

    4. Verify flush span coverage:
       - Query OTel traces for "writer.flush" span name
       - Verify all 3 writers emit spans with batch_size, flush_ms attributes
       - Verify span ERROR status set on flush failures

    5. Verify shadow metrics:
       - Check SHADOW_WIN_RATE gauge: Should show 0.0-1.0 range, not 500%
       - Check SHADOW_N_RESOLVED gauge: Should show absolute count, not accumulating
       - Verify .set() calls in shadow_auditor_agent.py (no .add() on shadow metrics)

    Only proceed to Wave 3 if:
    - SQL query returns TRUE for all 3 criteria
    - Grafana dashboards show stable metrics for 1-2 hours (no anomalies)
    - All writers emitting flush spans with correct attributes
    - Shadow metrics show absolute values (not accumulating)
    - No AttributeError or ghost-run issues in logs
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Wave 3, or describe issues to rollback</resume-signal>
</task>

</tasks>

<verification>
Wave 2 verification queries:

```sql
-- Flush span coverage
SELECT COUNT(*) = 3 as writer_flush_spans_complete
FROM information_schema.writer_spans
WHERE writer_name IN ('ctx_writer', 'llm_writer', 'feature_writer')
AND has_flush_span = TRUE;

-- AttributeError bugs fixed
SELECT COUNT(*) = 0 as attribute_error_bugs_remaining
FROM information_schema.code_defects
WHERE defect_type = 'AttributeError'
AND file_name IN ('ctx_writer_agent.py', 'llm_writer_service.py', 'feature_writer_agent.py');

-- Shadow metric types correct
SELECT COUNT(*) = 5 as shadow_metrics_fixed
FROM otel.instrument_types
WHERE metric_name LIKE 'shadow_%'
AND instrument_type = 'gauge';

-- Latency metrics use histogram
SELECT COUNT(*) = 0 as latency_counter_bugs
FROM otel.instrument_types
WHERE metric_name LIKE '%latency%'
AND instrument_type = 'counter';
```

Success criteria:
- writer_flush_spans_complete = TRUE (3/3 writers)
- attribute_error_bugs_remaining = TRUE (0 bugs)
- shadow_metrics_fixed = TRUE (5/5 metrics)
- latency_counter_bugs = TRUE (0 bugs)
</verification>

<success_criteria>
1. All 3 writer services (ctx_writer, llm_writer, feature_writer) have flush span coverage
2. All AttributeError bugs fixed (.inc() → .add(), self._pool → db_manager)
3. Ghost-run mode prevented (feature_writer raises on DB failure)
4. All 5 shadow metrics use Gauge with .set() instead of UpDownCounter with .add()
5. All latency metrics use Histogram instead of Counter (no create_counter.*latency patterns remain)
6. Grafana dashboards show correct shadow values (no accumulation)
7. Traces show writer.flush spans with batch_size and flush_ms attributes
8. Wave 2 verification query returns TRUE (all 4 criteria pass)
9. Grafana dashboards stable for 1-2 hours post-deployment
</success_criteria>

<output>
After completion, create `.planning/phases/107-infrastructure-hygiene/107-02-SUMMARY.md`
</output>
