---
phase: 107-infrastructure-hygiene
plan: 03
type: execute
wave: 3
depends_on: ["107-02"]
substeps: ["107-03-1", "107-03-2", "107-03-3", "107-03-4"]
files_modified:
  - services/service_auditor_agent.py
  - services/shadow_auditor_agent.py
  - src/core/ml/shadow.py
  - src/core/llm/guardrails.py
  - src/config/settings.py
  - src/intelligence/ai/TEMPLATE_agent.py
  - docs/ideas/architectural-weakness-assessment.md
autonomous: false
must_haves:
  truths:
    - "All 11 missing services added to _DAG_ORDER with justified priorities (current: 31, target: 42+)"
    - "shadow_auditor_agent promotion queries include 'AND is_shadow = FALSE' filter"
    - "Swarm agents (skeptic, correlation, volume) skipped in signal_ledger graduation queries via SQL filter"
    - "ShadowRecorder class deleted from src/core/ml/shadow.py with all import references removed"
    - "GuardrailsValidator class deleted from src/core/llm/guardrails.py with all import references removed"
    - "8 dead Settings fields removed (SWARM_QUEUE_TIMEOUT_MS, LLM_RATE_LIMIT_RPM, LLM_RATE_LIMIT_TPM, SHADOW_CORRELATION_THRESHOLD, SHADOW_MIN_SAMPLES, LANGFUSE_HOST, MLFLOW_TRACKING_URI, RUSTFUSE_PREFIX)"
    - "TEMPLATE_agent.py fixed: self._llm.generate() changed to self._llm_generate()"
    - "architectural-weakness-assessment.md updated with HYGIENE-01 through HYGIENE-09 completion status"
    - "Wave 3 verification SQL query returns TRUE (all 4 criteria pass)"
    - "Grafana dashboards show stable metrics for 1-2 hours after deployment"
  artifacts:
    - path: "services/service_auditor_agent.py"
      provides: "Complete DAG registry with 11 missing services"
      contains: "_DAG_ORDER dict with 42+ entries"
    - path: "services/shadow_auditor_agent.py"
      provides: "Fixed shadow promotion queries"
      contains: "AND is_shadow = FALSE"
    - path: "src/core/ml/shadow.py"
      provides: "Dead code removal"
      contains: "NO ShadowRecorder class"
    - path: "src/core/llm/guardrails.py"
      provides: "Dead code removal"
      contains: "NO GuardrailsValidator class"
    - path: "src/config/settings.py"
      provides: "Dead field removal"
      contains: "NO SWARM_QUEUE_TIMEOUT_MS, LLM_RATE_LIMIT, SHADOW_CORRELATION_THRESHOLD, SHADOW_MIN_SAMPLES, LANGFUSE_HOST, MLFLOW_TRACKING_URI, RUSTFUSE_PREFIX"
    - path: "src/intelligence/ai/TEMPLATE_agent.py"
      provides: "TEMPLATE agent bug fix"
      contains: "self._llm_generate() call pattern documented"
    - path: "docs/ideas/architectural-weakness-assessment.md"
      provides: "Updated assessment with all 9 HYGIENE criteria marked complete"
      contains: "HYGIENE-01 through HYGIENE-09: COMPLETE"
  key_links:
    - from: "services/service_auditor_agent.py"
      to: "systemd unit files"
      via: "_DAG_ORDER priorities"
      pattern: "indicagent-[a-z_-]+: [0-9]+"
    - from: "services/shadow_auditor_agent.py"
      to: "signal_ledger table"
      via: "is_shadow filter"
      pattern: "AND is_shadow = FALSE"
    - from: "src/intelligence/ai/TEMPLATE_agent.py"
      to: "BaseAIAgent._llm_generate()"
      via: "correct call pattern"
      pattern: "self._llm_generate\\(context"
---

<objective>
Add 11 missing services to _DAG_ORDER (current: 31, target: 42+), fix shadow promotion queries to exclude shadow signals, skip swarm agent ledger queries, delete dead code (HYGIENE-05), and update the architectural weakness assessment.

Purpose: Wave 3 reduces complexity and fixes shadow governance. DAG completeness ensures correct restart order (prevents race conditions). Shadow promotion query fixes prevent shadow signals from contaminating live track (optimize for wrong objective). Skipping swarm ledger queries avoids confusion (swarm uses signal_ai_enrichment, not signal_ledger). Dead code deletion (ShadowRecorder, GuardrailsValidator, 8 Settings fields, TEMPLATE bug) reduces cognitive load. Assessment update closes the loop on all 9 HYGIENE criteria.

Output: Complete DAG registry (42+ services), fixed shadow governance queries, deleted dead code, and updated assessment documenting all Phase 107 fixes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-infrastructure-hygiene/107-CONTEXT.md
@.planning/phases/107-infrastructure-hygiene/107-RESEARCH.md
@.planning/phases/107-infrastructure-hygiene/107-PATTERNS.md
@.planning/phases/phase-106/106-02-SUMMARY.md

# Only reference prior plan SUMMARYs if genuinely needed
@services/service_auditor_agent.py
@services/shadow_auditor_agent.py
@src/core/ml/shadow.py
@src/core/llm/guardrails.py
@src/config/settings.py
@src/intelligence/ai/TEMPLATE_agent.py
@docs/ideas/architectural-weakness-assessment.md
@production/systemd/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 11 missing services to _DAG_ORDER</name>
  <files>services/service_auditor_agent.py</files>
  <read_first>
    - services/service_auditor_agent.py
    - production/systemd/
    - .planning/phases/phase-106/106-02-SUMMARY.md
  </read_first>
  <action>
    Add the 11 missing services to _DAG_ORDER in service_auditor_agent.py:

    1. Identify the 11 missing services from architectural-weakness-assessment.md HYGIENE-04 section:
       - Services listed in /etc/systemd/system/indicagent-*.service but not in _DAG_ORDER
       - Current state: 31 services deployed, 103 entries in _DAG_ORDER (includes duplicates and non-service entries)
       - Target: All deployed services represented with unique entries

    2. For each missing service, add to _DAG_ORDER with justified priority:
       - Priority 0-2: Infrastructure sentinels (redpanda-ready, redpanda-watchdog)
       - Priority 3: Data ingestion layer (ibkr-provider, bar-replay)
       - Priority 4-6: Processing layer (bar-aggregator, provider-merger, cross-asset)
       - Priority 7-8: Intelligence layer (intelligence-pipeline, macro-compute)
       - Priority 9-10: Persistence layer (all *-writer agents)
       - Priority 11+: Analytics layer (compute services, auditors)

    3. Add inline comments for each entry documenting upstream/downstream dependencies

    4. Add missing _LAG_THRESHOLDS for new Kafka consumers (500 for typical consumers, 0 for non-consumers)

    5. Add missing _AGENT_ID_TO_UNIT mappings for new services

    6. Verify no cyclic dependencies exist (check that no service depends on itself transitively)

    Refer to 106-02-SUMMARY.md for the pattern used when adding 9 services in Phase 106.
  </action>
  <verify>
    systemctl list-unit-files | grep indicagent | grep -v timer | grep -v target | wc -l
    grep -c '"indicagent-' services/service_auditor_agent.py | grep -E "^4[2-9]$|^5[0-9]$"
  </verify>
  <done>
    _DAG_ORDER contains all deployed services (42+ entries) with justified priorities and documented dependencies.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix shadow promotion queries to exclude shadow signals</name>
  <files>services/shadow_auditor_agent.py</files>
  <read_first>
    - services/shadow_auditor_agent.py
    - docs/ideas/architectural-weakness-assessment.md
  </read_first>
  <action>
    Fix shadow_auditor_agent promotion/demotion queries to exclude shadow signals (HYGIENE-06):

    1. Find all queries reading from signal_ledger for graduation statistics:
       - Look for: `SELECT * FROM signal_ledger WHERE setup_plugin = $1 AND outcome IS NOT NULL`
       - Look for: Bootstrap CI queries, EV[R] queries, win rate queries

    2. Add `AND is_shadow = FALSE` filter to all graduation queries:
       - BEFORE: `WHERE setup_plugin = $1 AND outcome IS NOT NULL`
       - AFTER: `WHERE setup_plugin = $1 AND outcome IS NOT NULL AND is_shadow = FALSE`

    3. Verify query logic:
       - Graduation stats should only reflect live signals (is_shadow=FALSE)
       - Shadow signals (is_shadow=TRUE) must not contaminate EV[R], CI, or win rate
       - The gate `n >= 100 AND bootstrap_ci_lower(pnl_r) > 0.0` applies to live stats only

    4. Document the fix in code comments:
       - Add: "Exclude shadow signals from graduation statistics to prevent optimizing for wrong objective"

    Preserve all existing graduation logic, threshold checks, and SQL parameter binding.
  </action>
  <verify>
    grep -n "AND is_shadow = FALSE" services/shadow_auditor_agent.py | grep -c "signal_ledger"
    grep -n "SELECT.*signal_ledger.*setup_plugin" services/shadow_auditor_agent.py | while read line; do
      if ! echo "$line" | grep -q "is_shadow = FALSE"; then
        echo "Missing is_shadow filter: $line"
        exit 1
      fi
    done
  </verify>
  <done>
    All shadow promotion/demotion queries exclude shadow signals via "AND is_shadow = FALSE" filter (HYGIENE-06).
  </done>
</task>

<task type="auto">
  <name>Task 3: Skip swarm agents in signal_ledger graduation queries</name>
  <files>services/shadow_auditor_agent.py</files>
  <read_first>
    - services/shadow_auditor_agent.py
  </read_first>
  <action>
    Skip swarm agents (skeptic, correlation, volume) in signal_ledger-based graduation queries (HYGIENE-06):

    1. Identify swarm agent IDs:
       - `skeptic`, `correlation`, `volume` (or similar naming)

    2. In graduation query loops, add SQL filter:
       - BEFORE: `WHERE setup_plugin = ANY($1::text[])` (processes all plugins)
       - AFTER: Add SQL filter to exclude swarm agents
       - Pattern: `WHERE setup_plugin = ANY($1::text[]) AND setup_plugin NOT IN ('skeptic', 'correlation', 'volume')`

    3. Document the skip:
       - Add comment: "Swarm agents write to signal_ai_enrichment, not signal_ledger. Skip signal_ledger queries for swarm agents."

    Rationale: Swarm agents use signal_ai_enrichment table (alpha_multiplier_shadow hypertable), not signal_ledger. Querying signal_ledger for swarm agents returns empty results. SQL filter is cleaner than Python skip (pushes filtering to database).
  </action>
  <verify>
    grep -n "NOT IN ('skeptic'" services/shadow_auditor_agent.py
    grep -c "skip swarm\|swarm.*enrichment" services/shadow_auditor_agent.py
  </verify>
  <done>
    Shadow graduation skips swarm agents (skeptic, correlation, volume) in signal_ledger queries via SQL filter because swarm uses signal_ai_enrichment (HYGIENE-06).
  </done>
</task>

<task type="auto">
  <name>Task 4: Update architectural weakness assessment with Phase 107 completion</name>
  <files>docs/ideas/architectural-weakness-assessment.md</files>
  <read_first>
    - docs/ideas/architectural-weakness-assessment.md
    - .planning/phases/107-infrastructure-hygiene/107-CONTEXT.md
  </read_first>
  <action>
    Update architectural-weakness-assessment.md to mark all HYGIENE criteria as complete:

    1. Locate the HYGIENE criteria sections (search for "HYGIENE-01" through "HYGIENE-09")

    2. For each HYGIENE criterion, add completion status:
       - Add: `**Status:** COMPLETE (Phase 107, 2026-05-25)`
       - Brief summary of fix applied

    3. Update the summary table at the top of the document:
       - Mark Phase 107 as COMPLETE
       - Update criteria count: 9/9 criteria complete

    4. Add a section at the end: "Phase 107 Resolutions":
       - List all 9 HYGIENE criteria with brief resolution descriptions
       - Note measurement-driven verification (binary SQL query returns TRUE)

    5. Update any related findings (HF-1 through HF-11) to reference Phase 107 resolution

    Preserve all existing content; only add completion markers and resolution summaries.
  </action>
  <verify>
    grep -c "COMPLETE (Phase 107" docs/ideas/architectural-weakness-assessment.md | grep "^9$"
    grep -n "Phase 107 Resolutions" docs/ideas/architectural-weakness-assessment.md
  </verify>
  <done>
    architectural-weakness-assessment.md documents Phase 107 completion of all 9 HYGIENE criteria with resolution summaries.
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify systemd unit dependencies match _DAG_ORDER</name>
  <files>production/systemd/</files>
  <read_first>
    - production/systemd/
    - services/service_auditor_agent.py
  </read_first>
  <action>
    Verify systemd unit file After= dependencies match _DAG_ORDER priorities:

    1. List all systemd unit files in production/systemd/indicagent-*.service

    2. For each unit file, check After= dependencies:
       - Extract After= list
       - Verify each referenced unit exists in production/systemd/
       - Verify dependencies align with _DAG_ORDER priority ordering

    3. Fix any mismatches:
       - If After= references a unit with higher priority (should start later), swap order
       - If After= references non-existent unit, remove or fix reference
       - Ensure priority 0 units (infrastructure) have no dependencies
       - Ensure priority 1+ units have After= pointing to priority 0-1 units

    4. Document any cyclic dependencies discovered:
       - If A depends on B and B depends on A, this is a cycle
       - Cycles prevent clean startup — must break the cycle
       - Common pattern: merge services or extract shared dependency

    Preserve all existing ExecStart, User, Environment, and other systemd directives.
  </action>
  <verify>
    cd production/systemd && for f in indicagent-*.service; do
      after=$(grep "^After=" "$f" | cut -d= -f2)
      for dep in $after; do
        if [ ! -f "$dep.service" ]; then
          echo "Missing dependency: $f -> $dep"
        fi
      done
    done
  </verify>
  <done>
    All systemd unit After= dependencies reference existing units and align with _DAG_ORDER priorities.
  </done>
</task>

<task type="auto">
  <name>Task 6: Delete dead code (HYGIENE-05)</name>
  <files>src/core/ml/shadow.py src/core/llm/guardrails.py src/config/settings.py src/intelligence/ai/TEMPLATE_agent.py</files>
  <read_first>
    - src/core/ml/shadow.py
    - src/core/llm/guardrails.py
    - src/config/settings.py
    - src/intelligence/ai/TEMPLATE_agent.py
    - .planning/phases/107-infrastructure-hygiene/107-RESEARCH.md
  </read_first>
  <action>
    Delete dead code identified in architectural-weakness-assessment.md (HYGIENE-05):

    1. Delete ShadowRecorder class:
       - File: src/core/ml/shadow.py
       - Remove entire `class ShadowRecorder` (0 instantiations confirmed by grep)
       - Remove any imports of ShadowRecorder from other files

    2. Delete GuardrailsValidator class:
       - File: src/core/llm/guardrails.py
       - Remove entire `class GuardrailsValidator` (0 schemas registered, dead branch in chain.py)
       - Remove any import references

    3. Delete 8 dead Settings fields:
       - File: src/config/settings.py
       - Remove: SWARM_QUEUE_TIMEOUT_MS (deprecated, zero uses)
       - Remove: LLM_RATE_LIMIT_RPM, LLM_RATE_LIMIT_TPM (orphaned, never read)
       - Remove: SHADOW_CORRELATION_THRESHOLD, SHADOW_MIN_SAMPLES (zero uses)
       - Remove: LANGFUSE_HOST (never implemented)
       - Remove: MLFLOW_TRACKING_URI (bypassed by local constant)
       - Remove: RUSTFUSE_PREFIX (never implemented)

    4. Fix TEMPLATE agent bug:
       - File: src/intelligence/ai/TEMPLATE_agent.py
       - Change: `self._llm.generate()` → `self._llm_generate(context, ...)`
       - Add comment: "# NEVER call self._llm.generate() directly — use self._llm_generate() for audit trail"
       - Document the correct pattern in TEMPLATE

    5. Grep and remove all import references:
       - Run: `git grep "ShadowRecorder\|GuardrailsValidator"` across all Python files
       - Remove all import statements and references
       - For Settings fields: grep for field names in usage, remove any references

    Preserve all live code; only delete confirmed-dead classes and fields.
  </action>
  <verify>
    ! grep -r "class ShadowRecorder" src/core/ml/shadow.py
    ! grep -r "class GuardrailsValidator" src/core/llm/guardrails.py
    ! grep -r "SWARM_QUEUE_TIMEOUT_MS\|LLM_RATE_LIMIT\|SHADOW_CORRELATION_THRESHOLD\|SHADOW_MIN_SAMPLES\|LANGFUSE_HOST\|MLFLOW_TRACKING_URI\|RUSTFUSE_PREFIX" src/config/settings.py
    ! grep -r "self._llm.generate()" src/intelligence/ai/TEMPLATE_agent.py
    git grep "ShadowRecorder\|GuardrailsValidator" | wc -l | grep "^0$"
  </verify>
  <done>
    Dead code deleted: ShadowRecorder, GuardrailsValidator, 8 dead Settings fields, TEMPLATE agent bug fixed (HYGIENE-05). All import references removed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Wave 3 deployment</name>
  <what-built>Wave 3 complete: 11 missing services added to _DAG_ORDER (31 → 42+), shadow promotion queries fixed to exclude shadow signals (AND is_shadow = FALSE), swarm agents skipped in signal_ledger queries via SQL filter, dead code deleted (ShadowRecorder, GuardrailsValidator, 8 Settings fields, TEMPLATE bug) with all import references removed, systemd unit dependencies verified, architectural weakness assessment updated.</what-built>
  <how-to-verify>
    1. Run Wave 3 verification SQL query from 107-CONTEXT.md:
       ```sql
       SELECT
         (SELECT COUNT(DISTINCT unit) * 100.0 / (SELECT COUNT(*) FROM systemd_units) FROM dag_order) >= 100 as hyg04_pass,
         (SELECT COUNT(*) FROM dead_code_violations) = 0 as hyg05_pass,
         (SELECT COUNT(*) FROM shadow_governance_violations) = 0 as hyg06_pass;
       ```
       Expected: All three return TRUE.

    2. Deploy changes: Restart service_auditor and shadow_auditor via systemctl:
       ```bash
       sudo systemctl restart indicagent-service-auditor indicagent-shadow-auditor
       ```

    3. Monitor Grafana dashboards for 1-2 hours:
       - Check "Service Auditor" dashboard: All 42+ services visible, lag monitoring works
       - Check "Shadow Governance" dashboard: Promotion stats exclude shadow signals
       - Check "Service Health" dashboard: All services green, no failed dependencies
       - Check logs: tail -50 logs/service_auditor_agent.log, shadow_auditor_agent.log
       - Verify no errors, all services monitored

    4. Verify DAG completeness:
       - Check _DAG_ORDER in service_auditor_agent.py: 42+ entries
       - Verify all systemd units have corresponding _DAG_ORDER entries
       - Check _LAG_THRESHOLDS: All Kafka consumers have thresholds defined
       - Check _AGENT_ID_TO_UNIT: All services have systemd unit mappings

    5. Verify shadow governance fixes:
       - Check promotion queries in shadow_auditor_agent.py: "AND is_shadow = FALSE" present
       - Verify swarm agents skipped via SQL filter: "NOT IN ('skeptic', 'correlation', 'volume')"
       - Query signal_ledger manually: Verify no shadow signals (is_shadow=TRUE) in promotion stats

    6. Verify dead code deletion:
       - git grep for ShadowRecorder: Should return 0 results (no import references)
       - git grep for GuardrailsValidator: Should return 0 results (no import references)
       - Check Settings.py: Dead fields removed
       - Check TEMPLATE_agent.py: Uses self._llm_generate()

    7. Verify systemd unit dependencies:
       - Check all unit files: After= dependencies reference valid units
       - No cyclic dependencies detected
       - Priority ordering matches data flow

    Only proceed to Phase 108 if:
    - SQL query returns TRUE for all 3 criteria
    - Grafana dashboards show stable metrics for 1-2 hours (no anomalies)
    - All 42+ services monitored by service auditor
    - Shadow governance queries exclude shadow signals
    - Dead code fully deleted with no import references, no regressions
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 107 complete, or describe issues to rollback</resume-signal>
</task>

</tasks>

<verification>
Wave 3 verification queries:

```sql
-- DAG completeness
SELECT COUNT(*) >= 42 as dag_completeness
FROM (
  SELECT 1 FROM unnest(string_to_array(slice(_DAG_ORDER, 1, 1000), ',')) AS service
) t;

-- Shadow promotion queries exclude shadow signals
SELECT COUNT(*) > 0 as shadow_promotion_fixed
FROM information_schema.sql_statements
WHERE statement_text LIKE '%signal_ledger%'
AND statement_text LIKE '%setup_plugin = %'
AND statement_text LIKE '%is_shadow = FALSE%'
AND file_name = 'shadow_auditor_agent.py';

-- Swarm agents skipped in signal_ledger queries
SELECT bool_and(swarm_skipped) as swarm_ledger_skip
FROM (
  SELECT
    CASE
      WHEN agent_id IN ('skeptic', 'correlation', 'volume')
      THEN query_text NOT LIKE '%signal_ledger%'
      ELSE TRUE
    END AS swarm_skipped
  FROM graduation_queries
) t;

-- Dead code deleted (HYGIENE-05)
SELECT COUNT(*) = 0 as dead_code_violations
FROM code_audit
WHERE violation_type IN ('ShadowRecorder', 'GuardrailsValidator', 'dead_settings_field', 'TEMPLATE_llm_generate_bug');
```

Success criteria:
- dag_completeness = TRUE (42+ services, up from 31)
- shadow_promotion_fixed = TRUE (1+ queries with is_shadow filter)
- swarm_ledger_skip = TRUE (swarm agents skipped)
- dead_code_violations = TRUE (0 violations)
</verification>

<success_criteria>
1. _DAG_ORDER contains all deployed services (42+ entries, up from 31)
2. All systemd unit dependencies align with _DAG_ORDER priorities
3. Shadow promotion queries exclude shadow signals (AND is_shadow = FALSE) [HYGIENE-06]
4. Swarm agents skipped in signal_ledger graduation queries via SQL filter [HYGIENE-06]
5. Dead code deleted: ShadowRecorder, GuardrailsValidator, 8 Settings fields, TEMPLATE bug fixed [HYGIENE-05]
6. All import references to dead code removed (git grep returns 0 results)
7. architectural-weakness-assessment.md marks all 9 HYGIENE criteria complete
8. Service auditor restarts services in correct dependency order
9. Shadow governance optimizes for live signal performance only
10. Wave 3 verification query returns TRUE (all 4 criteria pass)
11. Grafana dashboards stable for 1-2 hours post-deployment
</success_criteria>

<output>
After completion, create `.planning/phases/107-infrastructure-hygiene/107-03-SUMMARY.md`
</output>
