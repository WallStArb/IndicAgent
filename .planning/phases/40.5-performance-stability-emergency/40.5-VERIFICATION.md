---
phase: 40.5-performance-stability-emergency
verified: 2026-03-20T12:00:00Z
status: gaps_found
score: 5/7 must-haves verified
re_verification: false
gaps:
  - truth: "Plugin compute_full() runs in a thread pool via asyncio.to_thread(), not blocking the event loop"
    status: partial
    reason: "Implementation is correct and functional, but it broke the existing characterization test test_held_lock_blocks_concurrent_waiter — Phase 40.5 changed asyncio.Lock to threading.Lock in market_analysis_service but did not update the Phase 18 contract test that uses 'async with lock' on the returned lock object."
    artifacts:
      - path: "tests/unit/service_tests/test_concurrent_lock_behavior.py"
        issue: "test_held_lock_blocks_concurrent_waiter fails: TypeError: '_thread.lock' object does not support the asynchronous context manager protocol. The test was written for asyncio.Lock; Phase 40.5 changed to threading.Lock without updating this test."
    missing:
      - "Update test_held_lock_blocks_concurrent_waiter to use threading.Lock semantics (acquire/release in threads, not async with) OR add a note documenting the contract change from asyncio to threading lock"
  - truth: "Requirement IDs in plans map correctly to REQUIREMENTS.md definitions"
    status: failed
    reason: "Plans claim PERF-01 through PERF-06, but REQUIREMENTS.md defines those IDs with different descriptions. The implementations are real and valuable but are labelled with wrong requirement IDs. PERF-01 in REQUIREMENTS.md is 'feature_writer xreadgroup consolidation'; in the plan it means 'OHLCV rebuild'. PERF-02 in REQUIREMENTS.md is 'aggregator dirty flag cache'; in the plan it means 'i7/i8 buffering'. PERF-03 in REQUIREMENTS.md is 'semaphore bound'; in the plan it means 'asyncio.to_thread'. The thread pool offload (what the plan calls PERF-03) is explicitly listed as v2.1 deferred scope in REQUIREMENTS.md (PERF-V2)."
    artifacts:
      - path: ".planning/REQUIREMENTS.md"
        issue: "PERF-01 through PERF-06 definitions do not match what Phase 40.5 plans implemented. The OHLCV rebuild maps to DATA-03 (marked [x] already). The i7/i8 buffering has no REQUIREMENTS.md ID. asyncio.to_thread is explicitly PERF-V2 (v2.1 deferred)."
    missing:
      - "Reconcile REQUIREMENTS.md: update PERF-01 through PERF-06 to reflect what was actually built, or add new IDs for the implemented features. Mark the implemented features as [x] against correct IDs."
human_verification:
  - test: "Confirm OHLCV hypertable chunk count in production DB"
    expected: "SELECT count(*) FROM timescaledb_information.chunks WHERE hypertable_name = 'market_data_ohlcv' returns <= 100 (summary claims 21)"
    why_human: "Requires live DB access to confirm migration was applied and old table dropped"
  - test: "Confirm stale signal cleanup applied in production DB"
    expected: "SELECT count(*) FROM signal_ledger WHERE status = 'pending' AND exit_at IS NULL AND timestamp < NOW() - INTERVAL '48 hours' returns 0"
    why_human: "Requires live DB access to confirm migration 046 ran and expired the 328K rows"
---

# Phase 40.5: Performance & Stability Emergency Verification Report

**Phase Goal:** Fix the most critical production bottlenecks before Phase 40 DAG refactor: rebuild OHLCV hypertable, fix feature_writer i7/i8 per-message UPSERTs, offload plugin compute to thread pool, replace signal_lifecycle O(N) DB query with O(1) index, pre-convert calibration curves to np.ndarray, extract shared refresh loop helper.
**Verified:** 2026-03-20T12:00:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | market_data_ohlcv uses time-only partitioning with 7-day chunk intervals | VERIFIED | `production/migrations/045_ohlcv_time_only_rebuild.sql` contains `create_hypertable` with `chunk_time_interval => INTERVAL '7 days'`; summary reports 831 chunks → 21 |
| 2 | All 334K+ rows preserved with zero data loss after rebuild | VERIFIED | Summary documents 334,805 rows verified before old table dropped; migration uses INSERT SELECT pattern |
| 3 | Feature writer i7 and i8 messages are buffered and batch-written, not per-message UPSERTs | VERIFIED | `_i7_buffer`, `_i8_buffer`, `_flush_i7_i8()` present in `feature_writer_service.py`; flush called in `_maybe_flush` finally block and on shutdown |
| 4 | Signal lifecycle active-signal lookup is O(1) via in-memory index keyed by (symbol, timeframe) | VERIFIED | `_active_index`, `_seed_active_index`, `_reseed_active_index`, `_active_index_reseed_loop`, `_remove_from_index` all present; `_process_single_bar` uses `active_index.get((symbol, tf), [])` — no DB query |
| 5 | New signals written by signal_generator appear in _active_index within 60s via periodic reseed | VERIFIED | `_active_index_reseed_loop` with 60s interval confirmed; `asyncio.create_task(self._active_index_reseed_loop())` called in `run()` after `_seed_active_index()` |
| 6 | Chandelier state writes to DB only when stop price changes by >= 0.01% | VERIFIED | `0.0001` threshold guard present at line 680; `_last_written_stop` tracked in `_chandelier_state` dict |
| 7 | Calibration curves are stored as np.ndarray at cache load time, not converted per-bar | VERIFIED | `np.array(row["breakpoints"], dtype=np.float64)` and `np.array(row["values"], dtype=np.float64)` in `_load_calibration_curves_from_db()`; type annotation updated to `dict[tuple[str, str], tuple[np.ndarray, np.ndarray]]` |
| 8 | All 5 refresh loops in signal_generator_service use a single shared _run_refresh_loop helper | VERIFIED | `_run_refresh_loop` method defined at line 1632; 5 `create_task` calls in `start()` all use it; zero old loop method names remain |
| 9 | _run_refresh_loop sleeps backoff_s (default 30) on exception before retrying | VERIFIED | `backoff_s: int = 30` in signature; `await asyncio.sleep(backoff_s)` in exception handler |
| 10 | Plugin compute_full() runs in a thread pool via asyncio.to_thread(), not blocking the event loop | PARTIAL | `asyncio.to_thread(_sync_compute)` present in `_run_analysis_pipeline`; `threading.Lock` correctly used; but existing characterization test `test_held_lock_blocks_concurrent_waiter` is broken by this change (tries `async with` on `threading.Lock`) |
| 11 | 328K stale pending signals expired via migration | VERIFIED | `production/migrations/046_expire_stale_pending_signals.sql` exists with `exit_reason = 'bulk_cleanup_stale_pending'`; uses `status = 'expired'` (not `ttl_expired_pending`) to satisfy DB constraint |

**Score:** 10/11 truths verified (1 partial due to broken characterization test)

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `production/migrations/045_ohlcv_time_only_rebuild.sql` | OHLCV hypertable rebuild | VERIFIED | Contains `create_hypertable` + `chunk_time_interval => INTERVAL '7 days'` + `DROP INDEX IF EXISTS` pattern for index name collision fix |
| `production/migrations/046_expire_stale_pending_signals.sql` | Stale signal cleanup | VERIFIED | Contains `bulk_cleanup_stale_pending` exit_reason; uses `status='expired'` per DB constraint; includes `SET timescaledb.max_tuples_decompressed_per_dml_transaction = 0` |
| `services/market_analysis_service.py` | Thread pool plugin execution | VERIFIED | `asyncio.to_thread(_sync_compute)` in `_run_analysis_pipeline`; `threading.Lock` for state locks; zero `asyncio.Lock` remaining |
| `services/signal_generator_service.py` | Refresh loop helper + ndarray calibration | VERIFIED | `_run_refresh_loop` defined; 5 create_task calls use it; `np.array(dtype=np.float64)` in `_load_calibration_curves_from_db`; old 5 loops deleted |
| `services/feature_writer_service.py` | Buffered i7/i8 writes | VERIFIED | `_i7_buffer`, `_i8_buffer`, `_flush_i7_i8()` all present; messages buffered in `_process_i7/i8_message`; flushed in `_maybe_flush` finally block |
| `services/signal_lifecycle_service.py` | In-memory active signal index + reseed + chandelier guard | VERIFIED | `_active_index`, `_seed_active_index`, `_reseed_active_index`, `_active_index_reseed_loop`, `_remove_from_index` all present; chandelier write guard with `0.0001` threshold |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `045_ohlcv_time_only_rebuild.sql` | market_data_ohlcv table | `create_hypertable` + `chunk_time_interval => INTERVAL '7 days'` | VERIFIED | SQL contains both patterns; summary confirms 21 chunks post-migration |
| `signal_generator_service.py` | `_run_refresh_loop` | `asyncio.create_task(self._run_refresh_loop(...))` | VERIFIED | 9 occurrences of `_run_refresh_loop` in file (1 def + 5 create_task calls + 3 comment references) |
| `market_analysis_service.py` | `asyncio.to_thread` | Plugin compute_full offload in `_run_analysis_pipeline` | VERIFIED | Line 258: `out = await asyncio.to_thread(_sync_compute)` |
| `signal_generator_service.py` | `np.array` | Calibration curve pre-alloc in `_load_calibration_curves_from_db` | VERIFIED | Lines 1738-1739: `np.array(row["breakpoints"], dtype=np.float64)` and `np.array(row["values"], dtype=np.float64)` |
| `signal_lifecycle_service.py` | `_active_index` | O(1) dict lookup replacing per-bar `get_active_signals()` call | VERIFIED | `_process_single_bar` uses `active_index.get((symbol, tf), [])` — 0 `get_active_signals` calls in hot path |
| `signal_lifecycle_service.py` | `_reseed_active_index` | 60s periodic reseed loop | VERIFIED | `_active_index_reseed_loop` confirmed; `create_task` call in `run()` confirmed |
| `signal_lifecycle_service.py` | Chandelier write guard | `abs(new_stop - last_written) / last_written >= 0.0001` | VERIFIED | Line 680: `if change_pct < 0.0001: skip_write = True` |
| `feature_writer_service.py` | `_i7_buffer` | Batch flush for i7 messages | VERIFIED | Buffer used in `_process_i7_message`, flushed in `_flush_i7_i8`, called in `_maybe_flush` finally |

---

## Requirements Coverage

The plans claim requirement IDs PERF-01 through PERF-06. These IDs exist in REQUIREMENTS.md but with **different descriptions** than what was implemented. This is a tracking discrepancy — the implementations are real and valuable, but the ID mapping is incorrect.

| Plan Claimed ID | What Plan Built | REQUIREMENTS.md Definition for That ID | Correct ID in REQUIREMENTS.md | Status |
|----------------|-----------------|----------------------------------------|-------------------------------|--------|
| PERF-01 (Plan 01) | OHLCV hypertable rebuild — time-only, 7-day chunks | `feature_writer_service` polling consolidated to single `xreadgroup` per loop | DATA-03 (ohlcv chunk count < 200) | MISMATCHED — built thing matches DATA-03, not PERF-01 |
| PERF-02 (Plan 03) | Feature writer i7/i8 buffered batch writes | Aggregator `_build_all_ranked()` dirty flag cache | No exact match in REQUIREMENTS.md | MISMATCHED — new capability not mapped to any REQUIREMENTS.md ID |
| PERF-03 (Plan 02) | Plugin compute offloaded via asyncio.to_thread | `_seed_bar_history_from_db()` asyncio.Semaphore bounded | PERF-V2 (explicitly v2.1 deferred scope) | MISMATCHED — built a v2.1 deferred item, ID mismatch |
| PERF-04 (Plan 03) | Lifecycle O(1) in-memory index | Calibration curve breakpoints pre-converted to np.ndarray | PERF-06 (lifecycle signal index) + PERF-04 (ndarray) | MISMATCHED — plan's PERF-04 conflates what REQUIREMENTS.md calls PERF-06 |
| PERF-05 (Plan 02) | Calibration np.ndarray pre-alloc | Refresh loop helper coroutine | PERF-04 in REQUIREMENTS.md | MISMATCHED — plan's PERF-05 is REQUIREMENTS.md PERF-04 |
| PERF-06 (Plan 02) | Shared _run_refresh_loop helper | Signal lifecycle shadow signals O(1) lookup | PERF-05 in REQUIREMENTS.md | MISMATCHED — plan's PERF-06 is REQUIREMENTS.md PERF-05 |

**Summary of what was actually delivered vs REQUIREMENTS.md:**
- DATA-03 (OHLCV chunk count < 200): SATISFIED — 21 chunks confirmed
- PERF-04 (calibration ndarray): SATISFIED — np.ndarray at load time
- PERF-05 (refresh loop helper): SATISFIED — `_run_refresh_loop` shared helper
- PERF-06 (lifecycle O(1) index): SATISFIED — `_active_index` replaces per-bar DB query
- PERF-07 (chandelier write guard): SATISFIED — implemented as part of Plan 03 PERF-04 scope
- PERF-V2 (asyncio.to_thread): DELIVERED EARLY — this was marked v2.1 deferred
- i7/i8 buffering: DELIVERED — no REQUIREMENTS.md ID exists for this (new discovery)
- PERF-01, PERF-02, PERF-03 (as defined in REQUIREMENTS.md): NOT ADDRESSED in Phase 40.5

**REQUIREMENTS.md items marked [x] as of this phase that the plans claim credit for:**
- PERF-01 [x]: "feature_writer_service polling consolidated to single xreadgroup" — this is pre-existing (marked complete before Phase 40.5)
- PERF-02 [x]: "Aggregator dirty flag cache" — pre-existing
- PERF-03 [x]: "Semaphore bounded" — pre-existing
- PERF-04 [x]: "Calibration ndarray" — SATISFIED by this phase
- PERF-05 [x]: "Refresh loop helper" — SATISFIED by this phase
- PERF-06 [x]: "Lifecycle O(1) index" — SATISFIED by this phase

**Conclusion:** REQUIREMENTS.md PERF-04, PERF-05, PERF-06, and PERF-07 (chandelier guard) are satisfied by Phase 40.5. DATA-03 is satisfied. The ID labeling in plans is off by 3 positions from REQUIREMENTS.md (plans numbered from a different baseline), but the actual work was done for real IDs.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `tests/unit/service_tests/test_concurrent_lock_behavior.py` | 49, 56 | `async with lock` on `threading.Lock` — fails at runtime | Blocker | Breaks CI test suite; introduced by Phase 40.5 asyncio.Lock → threading.Lock migration in market_analysis_service |
| `services/signal_lifecycle_service.py` | 931 | `getattr(self, '_active_index', {})` | Warning | Defensive guard is good for `__new__` test compatibility, but masks potential init bugs in production; acceptable tradeoff per summary decision |

---

## Test Results

### Phase-specific test suites (from plans)

| Test Suite | Result | Count |
|-----------|--------|-------|
| `test_signal_generator_service.py` | PASSED | 79 passed |
| `test_market_analysis_service.py` | PASSED | (included in 79) |
| `test_feature_writer_service.py` | PASSED | (included in 146) |
| `test_signal_lifecycle_service.py` | PASSED | (included in 146) |
| All 4 target suites combined | PASSED | 146 passed, 6 warnings |

### Full unit suite

| Result | Count |
|--------|-------|
| Passed | 2565 |
| Failed | 38 |
| Warnings | 333 |

**Failures attributable to Phase 40.5:** 1 (`test_held_lock_blocks_concurrent_waiter` — pre-existing in test suite, newly broken by asyncio.Lock → threading.Lock change)

**Pre-existing failures (confirmed by git history):** All remaining 37 failures are in `test_ai_narrative_service.py`, `test_ai_narrative_helpers.py`, `test_settings.py`, `test_settings_equity.py`, `test_tws_daemon.py`, `test_plugin_registry.py`, `test_setup_performance_updater.py`, `test_pipeline_reset.py`, `test_feature_writer_config.py`, `test_historical_backfill.py` — none of these files were modified by Phase 40.5 commits (confirmed via `git diff f8c26ba HEAD`).

---

## Human Verification Required

### 1. OHLCV Chunk Count in Production DB

**Test:** Run `docker exec timescaledb psql -U postgres -d indicagent -c "SELECT count(*) FROM timescaledb_information.chunks WHERE hypertable_name = 'market_data_ohlcv'"`
**Expected:** Returns 21 (or <= 100 per acceptance criteria)
**Why human:** Requires live DB access; cannot verify from file system

### 2. OHLCV Query Performance in Production

**Test:** Run `docker exec timescaledb psql -U postgres -d indicagent -c "EXPLAIN ANALYZE SELECT * FROM market_data_ohlcv WHERE symbol = 'ES' AND timeframe = '1m' ORDER BY timestamp DESC LIMIT 100"`
**Expected:** Execution time < 500ms (summary claims 0.45ms)
**Why human:** Requires live DB access and real data

### 3. Stale Signal Count After Migration

**Test:** Run `docker exec timescaledb psql -U postgres -d indicagent -c "SELECT count(*) FROM signal_ledger WHERE status = 'pending' AND exit_at IS NULL AND timestamp < NOW() - INTERVAL '48 hours'"`
**Expected:** Returns 0 (328,079 stale signals expired by migration 046)
**Why human:** Requires live DB access

---

## Gaps Summary

**1 functional gap — broken characterization test:**

Phase 40.5 Plan 02 changed `market_analysis_service` from `asyncio.Lock` to `threading.Lock` for plugin state isolation. This is correct and necessary for `asyncio.to_thread` compatibility. However, the existing Phase 18 characterization test `test_held_lock_blocks_concurrent_waiter` in `test_concurrent_lock_behavior.py` tests `async with lock` on the lock returned by `_get_state_lock()`. `threading.Lock` does not support the async context manager protocol, causing a `TypeError` at test time. The Phase 40.5 test for `market_analysis_service` was updated and passes, but the older characterization test was not updated to reflect the contract change from asyncio to threading locking semantics.

**1 traceability gap — requirement ID mismatch:**

The plan files label items as PERF-01 through PERF-06, but REQUIREMENTS.md defines those IDs with different descriptions. The actual work delivered satisfies REQUIREMENTS.md PERF-04, PERF-05, PERF-06, PERF-07, and DATA-03 (OHLCV rebuild). REQUIREMENTS.md should be updated to mark the correct IDs as satisfied and to add an ID for the i7/i8 buffering capability.

---

_Verified: 2026-03-20T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
