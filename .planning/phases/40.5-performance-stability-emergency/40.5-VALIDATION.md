# Phase 40.5: Performance & Stability Emergency - Validation

**Created:** 2026-03-20
**Source:** Extracted from 40.5-RESEARCH.md validation architecture section

---

## Test Framework

| Property | Value |
|----------|-------|
| Framework | pytest |
| Config file | none (uses pytest defaults) |
| Quick run command | `.venv/bin/pytest tests/unit/ -x -q` |
| Full suite command | `.venv/bin/pytest tests/unit/ -v` |

---

## Phase Requirements to Test Map

| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|-------------|
| PERF-01 | OHLCV table recreated as time-only, row count preserved | integration (DB) | manual verify via psql | N/A (DB migration) |
| PERF-02 | Feature writer i7/i8 buffering added; batch writes replace per-message UPSERTs | unit | `.venv/bin/pytest tests/unit/service_tests/test_feature_writer_service.py -x` | Yes |
| PERF-03 | Plugin pipeline wraps compute_full in asyncio.to_thread; event loop unblocked | unit | `.venv/bin/pytest tests/unit/service_tests/test_market_analysis_service.py -x` | Yes |
| PERF-04 | Lifecycle active index O(1) lookup; periodic reseed; chandelier write guard; stale signals expired | unit | `.venv/bin/pytest tests/unit/service_tests/test_signal_lifecycle_service.py -x` | Yes |
| PERF-05 | Calibration curves stored as np.ndarray; np.interp receives ndarray | unit | `.venv/bin/pytest tests/unit/service_tests/test_signal_generator_service.py -x` | Yes |
| PERF-06 | _run_refresh_loop helper replaces 5 loop bodies; includes error backoff | unit | `.venv/bin/pytest tests/unit/service_tests/test_signal_generator_service.py -x` | Yes |

---

## Sampling Rate

- **Per task commit:** `.venv/bin/pytest tests/unit/ -x -q`
- **Per wave merge:** `.venv/bin/pytest tests/unit/ -v`
- **Phase gate:** Full suite green before `/gsd:verify-work`

---

## Wave 0 Gaps

- The OHLCV rebuild (PERF-01) is a DB-only migration with no Python code -- verify via psql query in task acceptance criteria, not unit test
- `tests/unit/service_tests/test_signal_lifecycle_service.py` -- confirm `_seed_active_index`, `_remove_from_index`, and `_reseed_active_index` test coverage after implementation

---

## Verification Commands

```bash
# Quick validation (per-task)
.venv/bin/pytest tests/unit/ -x -q

# Full validation (per-wave)
.venv/bin/pytest tests/unit/ -v

# OHLCV rebuild verification (PERF-01, manual)
docker exec timescaledb psql -U postgres -d indicagent -c "SELECT num_dimensions FROM timescaledb_information.hypertables WHERE hypertable_name = 'market_data_ohlcv'"
# Expected: 1

docker exec timescaledb psql -U postgres -d indicagent -c "SELECT count(*) FROM timescaledb_information.chunks WHERE hypertable_name = 'market_data_ohlcv'"
# Expected: <= 400

# Lifecycle index verification (PERF-04)
grep -c "get_active_signals" services/signal_lifecycle_service.py
# Expected: 1 (only in _seed_active_index, not in _process_single_bar)

# Feature writer buffering (PERF-02)
grep -c "_i7_buffer\|_i8_buffer" services/feature_writer_service.py
# Expected: >= 4 (declarations + usage)

# Refresh loop helper (PERF-06)
grep -c "_run_refresh_loop" services/signal_generator_service.py
# Expected: >= 6 (1 def + 5 create_task calls)
```
