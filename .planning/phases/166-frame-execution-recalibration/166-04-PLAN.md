---
phase: 166-frame-execution-recalibration
plan: 04
type: execute
wave: 2
depends_on: ["166-01"]
files_modified:
  - scripts/analysis/gate166_frame_recalibration_eval.py
  - tests/unit/test_gate166_frame_recalibration_eval.py
autonomous: true
requirements: [D-01d, D-04, D-05]
nyquist_compliant: true

must_haves:
  truths:
    - "A new gate script scores an OOS alpha_frames population and writes one gate_evaluations row per candidate under a NEW gate_id, never gate2_execution (D-04)"
    - "The gate reuses frame_gate_passes/evaluate_frame_gate and SHADOW-REVIEW.md's frozen five criteria unmodified — no new thresholds invented (OQ3 resolution)"
    - "Every evaluation includes a regime-stratified companion breakdown disclosing (not gating on) the mid_bull-only OOS coverage, and the 5m/15m-only tf coverage (D-05, 173)"
    - "Every evaluation's evidence JSON reports the candidate's raw population footprint — total frame_count, eligible-cell count, and per-(regime,tf) frame counts — so a smaller/sparser candidate population cannot look artificially favorable versus another arm without the disparity being visible (Codex concern 2)"
    - "Same-bar_ts frames are aggregated (summed) before any cumulative/path-dependent statistic (172 regression guard)"
    - "Dry-run computes and prints a full verdict with zero writes; the real run atomically re-checks no prior row exists for the gate_id before inserting"
    - "A local dry-run sentinel in the phase dir records each executed dry-run per gate_id; a second dry-run for the same gate_id is refused without an explicit --force override, enforcing Pitfall 5's one-dry-run-per-candidate discipline in code rather than by convention (Codex concern 4)"
  artifacts:
    - path: "scripts/analysis/gate166_frame_recalibration_eval.py"
      provides: "Fresh validation gate for both candidates + baseline control"
      contains: "gate166"
    - path: "tests/unit/test_gate166_frame_recalibration_eval.py"
      provides: "Pure evidence-assembly unit coverage on synthetic rows"
  key_links:
    - from: "scripts/analysis/gate166_frame_recalibration_eval.py"
      to: "gate_evaluations (new gate_id)"
      via: "atomic re-check-then-insert in a transaction"
      pattern: "INSERT INTO gate_evaluations"
---

<objective>
Build the fresh validation gate (D-01d, D-04, D-05): a new script
`scripts/analysis/gate166_frame_recalibration_eval.py` mirroring
`score03_gate2_execution_eval.py` almost exactly. It scores a held-out OOS `alpha_frames`
population, reuses the SHADOW-REVIEW.md frozen five criteria via `frame_gate_passes`/
`evaluate_frame_gate` UNMODIFIED, and writes one `gate_evaluations` row per candidate under a NEW
`gate_id` (never a re-run of `gate2_execution`).

Purpose: This is the arbiter that decides which candidate (scalar vs structural) — if either —
turns Gate 1's OOS-proven signal into profitable OOS P&L (D-03). It answers the phase's core
question: can frame recalibration flip Gate 2's FAIL?

LOCKED DESIGN DECISIONS (resolve RESEARCH Open Questions 3 & A4, stated explicitly):
- OQ3: REUSE SHADOW-REVIEW.md's frozen five criteria verbatim as the absolute PASS/FAIL bar
  (via the existing gate machinery), applied identically to each candidate AND to a `baseline`
  control arm (current global scalars). This gives both an absolute answer ("did recalibration
  clear the Gate-2 bar?") and a comparative ranking — with NO new thresholds to tune (which would
  risk OOS-peeking). Reuse the existing `alpha.validation.regime_gate_min_clusters` (migration
  244) for the regime companion; add no new gate-threshold APR keys.
- A4: per-candidate gate_ids — `gate166_scalar`, `gate166_structural`, `gate166_baseline`. The
  script takes `--candidate {scalar,structural,baseline}` and writes exactly one row for that
  candidate's gate_id per real run.

This plan builds the SCRIPT and its unit tests (synthetic rows, no DB). The one-shot OOS RUNS
happen in Plan 166-06 after each candidate's frames are regenerated.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/166-frame-execution-recalibration/166-CONTEXT.md
@.planning/phases/166-frame-execution-recalibration/166-PATTERNS.md

<interfaces>
From scripts/analysis/score03_gate2_execution_eval.py (the near-verbatim analog — read fully):
- imports (lines 38-66): reuse `_annualized_sharpe`/`_max_drawdown` from phase143_1_08_shadow_validation, `evaluate_frame_gate`/`frame_gate_passes`/`_DEFAULT_BOOTSTRAP_RANDOM_STATE` from counterfactual_tracker
- `_aggregate_pnl_by_bar_ts(rows)` (lines 169-186): SUM pnl_r per distinct bar_ts BEFORE any cumulative walk (todo 172 — MANDATORY)
- `_compute_pooled_criteria(...)` (lines 189-252): the frozen five criteria assembly
- `_compute_regime_companion(...)` (lines 255-282): evaluate_frame_gate with group_key=(direction,regime), min_clusters, coverage="evaluated"/"insufficient"
- `_json_safe(obj)` (lines 402-426): +inf/-inf/NaN → string tokens — REUSE, do not re-derive (thin cells legitimately hit +inf CI bounds)
- `_write_gate2_row(...)` (lines 429-454): atomic re-check-then-insert in one transaction
- `main()` (lines 457-527): --dry-run branch computes+prints, zero writes; real path calls the writer

From services/counterfactual_tracker.py (reused UNMODIFIED — lines 172-283):
- `frame_gate_passes(...)`, `evaluate_frame_gate(rows, ..., group_key, min_clusters)` — 2-tuple group_key only (hard-crashes on other arities — Phase 148 lesson)

From production/migrations/248_alpha_scoring_gate_tables.sql:
- `gate_evaluations (eval_id, gate_id, result, evidence jsonb, run_ts)`, PK (gate_id, run_ts) — idempotent one-shot per gate_id
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure evidence-assembly core (pooled criteria + regime companion + population footprint + 172 aggregation)</name>
  <files>scripts/analysis/gate166_frame_recalibration_eval.py, tests/unit/test_gate166_frame_recalibration_eval.py</files>
  <behavior>
    - Test 1: assemble_gate166_evidence(rows, candidate) returns a dict with the pooled frozen-five criteria + a regime companion list + the candidate label + a result of 'pass'/'fail' (a statistical FAIL is a normal value, never an exception)
    - Test 2: same-bar_ts rows are SUMMED before the cumulative drawdown/Sharpe walk (172 guard) — a fixture with 20+ tied bar_ts asserts aggregation fires
    - Test 3: the regime companion is ALWAYS present; cells below min_clusters are marked coverage="insufficient" and excluded from (not counted as failing) the aggregate (D-05)
    - Test 4: evidence["gate_id"] is derived from candidate and is one of gate166_scalar/gate166_structural/gate166_baseline — never "gate2_execution" (D-04)
    - Test 5: _json_safe converts +inf/-inf/NaN CI bounds to string tokens
    - Test 6: evidence["population"] reports total frame_count, eligible_cell_count, and a per-(regime,tf) frame-count map; a fixture with a deliberately sparse cell asserts that cell's low count is present and visible (Codex concern 2)
  </behavior>
  <read_first>
    - scripts/analysis/score03_gate2_execution_eval.py (full file — the analog to mirror almost exactly)
    - tests/unit/test_score03_gate2_execution_eval.py (test structure + synthetic-row builders to mirror)
    - services/counterfactual_tracker.py (lines 172-283: frame_gate_passes/evaluate_frame_gate signatures — 2-tuple group_key only)
    - docs/plans/SHADOW-REVIEW.md (the frozen five criteria being reused verbatim)
    - .planning/phases/166-frame-execution-recalibration/166-PATTERNS.md (gate166 section: every reused function + line number)
  </read_first>
  <action>
    Create the gate script mirroring score03 structure. Define module-level `_GATE_IDS = {"scalar":"gate166_scalar","structural":"gate166_structural","baseline":"gate166_baseline"}`. Copy `_aggregate_pnl_by_bar_ts`, `_compute_pooled_criteria` (the frozen five), `_compute_regime_companion`, and `_json_safe` from score03 (adapt names, keep behavior). Add a pure `assemble_gate166_evidence(rows, candidate, *, bootstrap params, regime_gate_min_clusters)` that returns the full evidence dict: pooled criteria + regime companion + candidate + gate_id + result + a `population` block + a disclosure block noting 5m/15m-only tf coverage per todo 173 and mid_bull-only regime coverage per D-05. It must never raise on a statistical FAIL. All cumulative statistics run on the `_aggregate_pnl_by_bar_ts` output (172). Reuse `frame_gate_passes`/`evaluate_frame_gate` unmodified with a 2-tuple `(direction, regime)` group_key. Do NOT invent new thresholds — pull the frozen five from the same source score03 uses.

    Add a pure `_compute_population_footprint(rows)` that returns `{"frame_count": <int total pre-aggregation frames>, "eligible_cell_count": <distinct (regime,tf) cells with >= 1 frame>, "cell_frame_counts": {"<regime>.<tf>": <int>, ...}}`. Store its output under `evidence["population"]`. This makes each arm's raw footprint explicit so downstream (166-06 verdict doc) can show population-coverage deltas across arms and a sparser candidate cannot look artificially favorable without the disparity being visible (Codex concern 2). Population counts are descriptive disclosure only — they do NOT feed the pass/fail bar (no new thresholds; OQ3 intact). Use `except X as error:`, `datetime.now(UTC)`.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && .venv/bin/pytest tests/unit/test_gate166_frame_recalibration_eval.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_gate166_frame_recalibration_eval.py -x` passes with >= 6 tests covering the behaviors
    - A test asserts no gate_id in the script ever equals `gate2_execution` (D-04)
    - A test asserts the regime companion is present in every evidence dict (D-05)
    - A test asserts `evidence["population"]` carries `frame_count`, `eligible_cell_count`, and a per-(regime,tf) `cell_frame_counts` map, and that a sparse cell's low count is visible (Codex concern 2)
    - The 20+-tied-bar_ts fixture proves aggregation-before-cumulative-stat (172)
    - The frozen-five criteria come from the same source as score03 (grep: no hardcoded new threshold constants beyond what score03 imports); population counts are descriptive-only and gate nothing
  </acceptance_criteria>
  <done>The pure evidence-assembly core reuses the frozen criteria + machinery, aggregates ties, always includes the regime companion and a population footprint, and uses new gate_ids only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atomic one-shot write + dry-run main() + candidate arg + dry-run sentinel</name>
  <files>scripts/analysis/gate166_frame_recalibration_eval.py, tests/unit/test_gate166_frame_recalibration_eval.py</files>
  <behavior>
    - Test 7: the write path re-checks `SELECT count(*) FROM gate_evaluations WHERE gate_id=$1` inside the same transaction as the INSERT and raises if a row already exists (D-04 one-shot)
    - Test 8: --dry-run computes the full evidence and prints it but performs zero writes (asserted via a mock pool that records calls)
    - Test 9: the dry-run sentinel — a pure `_check_and_record_dryrun(sentinel_path, gate_id, force)` — refuses a second dry-run for the same gate_id (raises) unless force=True, and records the gate_id + timestamp on the first call (Pitfall 5 enforcement, Codex concern 4)
  </behavior>
  <read_first>
    - scripts/analysis/score03_gate2_execution_eval.py (lines 429-527: _write_gate2_row + main + dry-run branch + look-log append)
    - .planning/phases/166-frame-execution-recalibration/166-RESEARCH.md (Pitfall 5: finalize in-sample calibration BEFORE the one OOS dry-run per candidate; >1 dry-run per candidate is a holdout leak)
  </read_first>
  <action>
    Add `_write_gate166_row(pool, evidence, run_ts, look_log_path)` mirroring `_write_gate2_row`: open a transaction, `SELECT count(*) FROM gate_evaluations WHERE gate_id = $1` for the candidate's gate_id, raise RuntimeError if any row exists (one-shot cadence, D-04), else INSERT with `json.dumps(_json_safe(evidence))`, then append a look-log entry.

    Add a pure `_check_and_record_dryrun(sentinel_path, gate_id, force=False)` enforcing Pitfall 5 in code (Codex concern 4): the sentinel is a small JSON file at `.planning/phases/166-frame-execution-recalibration/.gate166_dryrun_sentinel.json` mapping `{gate_id: iso_timestamp}`. On a dry-run, if the gate_id is already present in the sentinel and `force` is False, raise RuntimeError with a message pointing at Pitfall 5 (one dry-run per candidate; re-running peeks at OOS twice); otherwise record `gate_id -> now` and continue. Keep this a small local-file mechanism — no new table, no new subsystem. Make it a pure function taking an explicit `sentinel_path` so the unit test drives it against a tmp path with no real phase dir.

    Add `main()` with argparse: `--candidate {scalar,structural,baseline}` (required), `--oos-start` (default from `alpha.validation.oos_start`), `--dry-run` (full computation + printed verdict + regime companion table + population footprint, ZERO writes), `--force` (bypass the dry-run sentinel; must be typed deliberately), and a real path that fetches OOS rows (`bar_ts >= oos_start`, and for candidate isolation reads the frame population produced under that candidate's geometry — the frames Plan 166-06 regenerates), calls `assemble_gate166_evidence`, and writes one row. On the `--dry-run` branch, call `_check_and_record_dryrun` FIRST (before scoring) so a repeated peek is refused up front. Print a prominent reminder that per Pitfall 5 only ONE dry-run per candidate should precede the real run. Aggregate-not-per-row logging on the fetch loop.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && .venv/bin/pytest tests/unit/test_gate166_frame_recalibration_eval.py -x -q && .venv/bin/python scripts/analysis/gate166_frame_recalibration_eval.py --help</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_gate166_frame_recalibration_eval.py -x` passes with >= 9 total tests
    - `--help` exits 0 and shows `--candidate` with choices scalar/structural/baseline, `--dry-run`, and `--force`
    - The write path raises if a row already exists for the gate_id (asserted)
    - `--dry-run` performs zero writes to gate_evaluations (asserted via mock)
    - `_check_and_record_dryrun` refuses a second dry-run for the same gate_id without `--force` and records on first call (asserted against a tmp sentinel path) — Pitfall 5 enforced in code (Codex concern 4)
    - `.venv/bin/pytest tests/unit/ -q` (full suite) stays green
  </acceptance_criteria>
  <done>The gate writes one idempotent row per candidate gate_id, supports a zero-write dry-run guarded by a per-gate_id sentinel, and takes candidate + force args.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate script → gate_evaluations | Irreversible one-shot verdict crosses into the permanent audit log |
| OOS alpha_frames → gate | Holdout data read exactly once per candidate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-09 | Tampering (double-write) | gate_evaluations one-shot | mitigate | atomic re-check-then-insert in one transaction; raises on existing row (D-04) |
| T-166-10 | Repudiation (silent verdict inconsistency) | path-dependent stat over tied bar_ts | mitigate | _aggregate_pnl_by_bar_ts before every cumulative stat (172) |
| T-166-11 | Information disclosure (holdout leak) | repeated OOS dry-runs | mitigate | `_check_and_record_dryrun` sentinel refuses a 2nd dry-run per gate_id without `--force` (Pitfall 5 enforced in code, not by convention); calibration finalized in-sample first |
| T-166-12 | Denial of service (crash on write) | non-finite floats in jsonb | mitigate | _json_safe sanitizer reused (prior Gate 2 crash precedent) |
| T-166-13 | Repudiation (sparse-population masquerade) | a sparser candidate looking favorable for population reasons | mitigate | evidence["population"] discloses frame_count / eligible_cell_count / per-cell counts per arm (Codex concern 2) |
</threat_model>

<verification>
- Unit tests pass (>= 9 tests); full suite green.
- Gate ids are new (never gate2_execution); regime companion + population footprint always present; ties aggregated.
- Dry-run writes nothing and is refused on a repeat per gate_id without --force; real write is atomic one-shot.
</verification>

<success_criteria>
- `gate166_frame_recalibration_eval.py` scores a candidate's OOS population with the frozen five criteria + regime companion + population footprint, disclosing 5m/15m + mid_bull coverage limits.
- Per-candidate new gate_ids; idempotent one-shot writes; zero-write dry-run guarded by a per-gate_id sentinel.
</success_criteria>

<output>
Create `.planning/phases/166-frame-execution-recalibration/166-04-SUMMARY.md` when done.
</output>
