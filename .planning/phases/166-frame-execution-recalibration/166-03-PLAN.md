---
phase: 166-frame-execution-recalibration
plan: 03
type: execute
wave: 2
depends_on: ["166-01"]
files_modified:
  - src/intelligence/trading/structural_confluence.py
  - tests/unit/test_structural_confluence.py
autonomous: true
requirements: [D-01c, D-03, D-06]
nyquist_compliant: true

must_haves:
  truths:
    - "A new v3-native module ports zone_engine.py's 3-tier confluence resolution (diverse-cluster → single-best → ATR fallback) operating on a generic ZoneCandidate"
    - "The candidate spec table is populated ONLY with Phase-163-live fields (sr_support_dist, sr_resist_dist, resistance/support strength+age, POC/VAH/VAL reconstructed from ATR distances) — zero archived-tier feature names"
    - "The module reads its thresholds from fresh alpha.frame.* APR keys, never the archived feature.zone_engine.* family"
    - "An explicit extension-point comment marks where SMC/swing/fib/anchored-VWAP sources are added once Phases 164/165 land (Part 2 deferral)"
  artifacts:
    - path: "src/intelligence/trading/structural_confluence.py"
      provides: "v3-native confluence stop/target resolution (Part 1)"
      contains: "ZoneCandidate"
    - path: "tests/unit/test_structural_confluence.py"
      provides: "Synthetic-candidate unit coverage using ONLY Phase-163 field names"
  key_links:
    - from: "src/intelligence/trading/structural_confluence.py::resolve_structural_zone"
      to: "Phase-163 feature_vectors columns"
      via: "declarative v3 spec table + price reconstruction"
      pattern: "sr_support_dist|poc_dist_atr"
---

<objective>
Build STRUCTURAL candidate Part 1 (D-01c, D-03.2, D-06): a new v3-native module
`src/intelligence/trading/structural_confluence.py` that ports `zone_engine.py`'s 3-tier
confluence-resolution ARCHITECTURE (the clustering/scoring core is generic over `ZoneCandidate`
and portable nearly unmodified) but whose candidate spec table is populated ONLY with the
structural fields Phase 163 makes live. This is genuine multi-source confluence (VP levels AND
S/R levels as independent evidence), data-limited not architecture-limited.

Purpose: The user explicitly asked whether v2.x's trade-lifecycle/tradeframer structural logic
can be reused in v3 (D-03/D-04). RESEARCH answered: the confluence mechanism is well-designed and
reusable, but every v2.x feature name is 100% absent from v3's live schema (Finding 2/3, Q3) — so
Part 1 reuses the ARCHITECTURE with a narrow-but-real live candidate universe, and Part 2 (SMC/
swing/fib/anchored-VWAP) is deferred pending Phases 164/165 (Q3/Q4).

CRITICAL CONSTRAINTS (RESEARCH Pitfall 3, Q2 naming trap):
- Do NOT copy zone_engine.py's `_SUPPORT_SPECS`/`_RESISTANCE_SPECS` entries — every field name in
  them is absent from v3. Build a fresh v3 spec table with Phase-163 fields only.
- Do NOT import from or build against `src/intelligence/features/smc_context/` or
  `src/intelligence/features/i3_structure/` — those are dead I1-I7-tier orphans (Q2), NOT the live
  v3.0 Feature Factory despite the "features" directory name.
- Reconstruct structural PRICES from Phase 163's ATR-normalized distances:
  `price = entry_price ± distance_atr_field * atr` (Phase 163 D-16 deliberately ships no raw
  price columns). Flag the ATR-consistency assumption (RESEARCH A2) in a comment.

This module builds and unit-tests against SYNTHETIC feature dicts — it does NOT require live
Phase 163 data. Only Plan 166-06's structural frame-generation RUNTIME needs 163 executed.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/166-frame-execution-recalibration/166-CONTEXT.md
@.planning/phases/166-frame-execution-recalibration/166-PATTERNS.md

<interfaces>
From src/intelligence/trading/zone_engine.py (the portable analog — read directly before porting):
- `@dataclass ZoneCandidate` (lines 85-91): price, name, strength(0-1), source_tier, source_family
- `_find_clusters(candidates, atr)` (lines 344-358): group sorted candidates within cluster_radius_atr, keep clusters of len>=2 — generic, portable as-is
- `_source_diversity(cluster)`, `_score_cluster(cluster, atr)`: strength_sum × diversity / width_atr — generic, portable
- `_pick_single_best(candidates, entry, atr)` (lines 378-395): strength×weight + proximity×weight — generic, portable
- `_resolve_zone` (lines 418-477): Tier1 diverse-cluster(>=2 source_tiers) → Tier2 single-best → Tier3 ATR fallback (tier="atr", empty zone)
- `set_config_service`/`_read_config` + `_cluster_radius_atr`/`_strength_weight`/`_proximity_weight` (lines 36-69): the module-level config-read SHAPE to copy — but point it at fresh `alpha.frame.*` keys

Phase-163 live fields (the ONLY legal spec-table entries — from 163-01-PLAN.md / migration 243):
- sr_support_dist, sr_resist_dist (ATR distances to nearest S/R)
- resistance_strength, support_strength, resistance_age_bars, support_age_bars, sr_level_count (D-19 companion fields → map onto _resolve_strength)
- poc_dist_atr, poc_rolling_dist_atr, distance_to_vah_atr, distance_to_val_atr (VP; reconstruct price via entry ± dist*atr)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port ZoneCandidate + generic clustering/scoring core + config read</name>
  <files>src/intelligence/trading/structural_confluence.py, tests/unit/test_structural_confluence.py</files>
  <behavior>
    - Test 1: _find_clusters groups candidates within cluster_radius_atr and drops singletons (len<2)
    - Test 2: _score_cluster rewards strength-sum × source-tier diversity, penalizes width
    - Test 3: _pick_single_best returns the highest strength×proximity candidate
    - Test 4: 3-tier resolution prefers a diverse cluster (>=2 distinct source_tier) over a single level, and returns tier="atr" empty result when no candidates exist
  </behavior>
  <read_first>
    - src/intelligence/trading/zone_engine.py (full file — the architecture being ported; lines 36-101, 344-477 are the load-bearing parts)
    - src/intelligence/trading/atr_utils.py (get_atr_with_floor — reuse if a price-unit ATR accessor is needed; do not recompute ATR)
    - tests/unit/trading/test_zone_engine.py (synthetic-candidate test style to mirror — but use v3 field names in fixtures)
    - .planning/phases/166-frame-execution-recalibration/166-PATTERNS.md (structural_confluence section: portable-nearly-unmodified excerpts + Pitfall 3/Finding 5 warnings)
    - src/config/config_service.py (the set_config_service/get_sync module-level config pattern used by trading utils)
  </read_first>
  <action>
    Create the module with: the `ZoneCandidate` dataclass (price/name/strength/source_tier/source_family) and a `ZoneResult` result type (zone_low/zone_high/tier/candidates); the generic `_find_clusters`/`_source_diversity`/`_score_cluster`/`_pick_single_best` functions ported nearly unmodified from zone_engine.py (they reference no feature names — generic over ZoneCandidate); and a `_resolve_zone(candidates, entry, atr)` implementing the 3-tier resolution (diverse-cluster confluence → single-best → `tier="atr"` empty fallback). Add a module-level `set_config_service()` + `_read_config()` pair (copy zone_engine.py's SHAPE) that reads thresholds from FRESH `alpha.frame.*` keys ONLY: `alpha.frame.cluster_radius_atr`, `alpha.frame.single_level_radius_atr`, `alpha.frame.strength_weight`, `alpha.frame.proximity_weight`, `alpha.frame.zone_buffer_atr`, `alpha.frame.min_width_atr` (seeded in migration 253) — never `feature.zone_engine.*` (Finding 5). Do NOT import anything from the archived plugin tier (`src/intelligence/archive/*`, `src/intelligence/features/smc_context/*`, `src/intelligence/features/i3_structure/*`); verify each import is a live module. Use `except X as error:`.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && .venv/bin/pytest tests/unit/test_structural_confluence.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_structural_confluence.py -x` passes with >= 4 tests covering the behaviors above
    - `grep -E "feature\.zone_engine|feature\.trade_framer|weights\.zone_engine" src/intelligence/trading/structural_confluence.py` returns nothing
    - `grep -E "from src\.intelligence\.(archive|features)" src/intelligence/trading/structural_confluence.py` returns nothing
    - The 3-tier resolution returns `tier="atr"` with an empty zone when given zero candidates (asserted)
  </acceptance_criteria>
  <done>The generic confluence core is ported, reads fresh alpha.frame.* config, imports zero archived-tier modules, and is unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: v3 spec table (Phase-163 fields), collect_candidates + public API + extension point</name>
  <files>src/intelligence/trading/structural_confluence.py, tests/unit/test_structural_confluence.py</files>
  <behavior>
    - Test 5: collect_candidates(features, direction, entry, stop, atr) with a synthetic features dict containing sr_support_dist emits an S/R support candidate at the reconstructed price entry - sr_support_dist*atr (long) between stop and entry
    - Test 6: VP fields (poc_dist_atr / distance_to_vah_atr / distance_to_val_atr) reconstruct POC/VAH/VAL price candidates via entry ± dist*atr
    - Test 7: _resolve_strength maps a candidate to its companion strength field (support_strength / *_age_bars) and decays to the spec default when absent
    - Test 8: resolve_structural_zone returns a confluence tier when a VP level and an S/R level cluster within cluster_radius_atr (two distinct source_tier: "vp" and "sr")
    - Test 9: a features dict missing ALL Phase-163 fields (all None) yields zero candidates → tier="atr" (the Pitfall 3 regression guard)
  </behavior>
  <read_first>
    - src/intelligence/trading/zone_engine.py (lines 111-186: _SUPPORT_SPECS/_RESISTANCE_SPECS shape, _STRENGTH_FIELD, _resolve_strength — copy the SHAPE, not the entries; lines 241-255: the special VP candidate block)
    - .planning/phases/163-vp-sr-structural-primitives/163-01-PLAN.md (exact Phase-163 column names + semantics)
    - .planning/phases/166-frame-execution-recalibration/166-PATTERNS.md (v3 spec-table adaptation, extension-point requirement, A2 ATR-consistency note)
  </read_first>
  <action>
    Add a fresh v3 spec table (tuples of feature_key, display_name, default_strength, source_tier, source_family) populated ONLY with: `sr_support_dist`→("sr_support",0.7,"sr","sr") and `sr_resist_dist`→("sr_resistance",0.7,"sr","sr"). Add a separate VP block (mirroring zone_engine's special-cased VP handling) that reconstructs POC/VAH/VAL candidates from `poc_dist_atr`/`poc_rolling_dist_atr`/`distance_to_vah_atr`/`distance_to_val_atr` via `entry_price ± dist * atr`, source_tier="vp", source_family="vp". Implement `collect_candidates(features, direction, entry, stop, atr)` filtering each reconstructed price to strictly between stop and entry (support side for long, resistance side for short), and `resolve_structural_zone(features, direction, entry, stop, atr)` as the public entry point calling collect → _resolve_zone. Add a `_resolve_strength` mapping candidates to Phase-163 companion fields (`resistance_strength`/`support_strength`/`resistance_age_bars`/`support_age_bars`, normalized to 0-1), decaying to the spec default when absent. Add an explicit `# EXTENSION POINT (Phase 166 Part 2, deferred — todo <NN>):` comment above the spec table stating that SMC/swing/fib/anchored-VWAP source rows are added here once Phases 164/165 + anchored-VWAP scoping land (the todo number is filled in by Plan 166-06 which files the consolidated follow-on todo). Add a comment on the price-reconstruction line flagging RESEARCH A2 (the ATR normalizing Phase-163 distances must match the ATR AlphaFrameWriter/CounterfactualTracker compute — verify once live). Fixtures in the test use ONLY Phase-163 field names, never v2.x names.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && .venv/bin/pytest tests/unit/test_structural_confluence.py -x -q && grep -c "EXTENSION POINT" src/intelligence/trading/structural_confluence.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_structural_confluence.py -x` passes with >= 9 total tests
    - The spec table + VP block reference only these keys: sr_support_dist, sr_resist_dist, poc_dist_atr, poc_rolling_dist_atr, distance_to_vah_atr, distance_to_val_atr, resistance_strength, support_strength, resistance_age_bars, support_age_bars (grep confirms no other feature keys)
    - `grep -c "EXTENSION POINT" src/intelligence/trading/structural_confluence.py` returns >= 1
    - Test 9 (all-None features → tier="atr") passes — the Pitfall 3 regression guard
    - `grep -E "nearest_support|swing_low|ema_21|ob_top|fvg_top|avwap|nearest_hvn" tests/unit/test_structural_confluence.py` returns nothing (no v2.x field names in fixtures)
  </acceptance_criteria>
  <done>The v3 spec table + VP reconstruction produce real multi-source (VP+S/R) confluence from Phase-163 fields, with the Part 2 extension point marked and the ATR-consistency assumption flagged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature dict → confluence resolution | Untrusted/missing feature values cross into price reconstruction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-06 | Tampering (silent wrong answer) | copying archived spec-table entries | mitigate | fresh v3 spec table + Test 9 all-None regression guard + grep gate for v2.x field names |
| T-166-07 | Tampering (namespace coupling) | reading archived feature.zone_engine.* keys | mitigate | fresh alpha.frame.* keys only; grep gate in acceptance criteria |
| T-166-08 | Information disclosure (silent bias) | ATR mismatch in price reconstruction | accept | flagged (RESEARCH A2) as a comment + verified once live in Plan 166-06; low risk pre-runtime |
</threat_model>

<verification>
- Unit tests pass (>= 9 tests); full suite green.
- Zero archived-tier imports; zero archived-namespace config reads; zero v2.x field names.
- Extension point marked; ATR-consistency assumption flagged.
</verification>

<success_criteria>
- `structural_confluence.py` reuses zone_engine's generic clustering/scoring core, reads fresh alpha.frame.* config, and produces VP+S/R confluence from Phase-163 fields only.
- The Pitfall 3 all-None regression guard passes.
- Part 2 extension point is explicit.
</success_criteria>

<output>
Create `.planning/phases/166-frame-execution-recalibration/166-03-SUMMARY.md` when done.
</output>
