---
phase: 30-redpanda-migration
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - services/tws_daemon.py
  - services/timeframes_builder_service.py
  - services/indicator_service.py
  - services/market_analysis_service.py
  - tests/unit/service_tests/test_indicator_service.py
  - tests/unit/daemons/test_tws_daemon.py
autonomous: true
requirements:
  - KAFKA-05
  - KAFKA-08

must_haves:
  truths:
    - "tws_daemon publishes bar events to dev.market.bars and tick events to dev.market.ticks (no Redis XADD)"
    - "tws_daemon no longer calls redis HSET for price:SYMBOL:latest"
    - "timeframes_builder_service consumes from dev.market.bars and publishes higher-TF bars to dev.market.bars"
    - "indicator_service consumes from dev.market.bars and publishes to dev.indicators with key SYMBOL:TF"
    - "indicator_service reads warmup bars from market_data_ohlcv DB table at startup (no xrevrange)"
    - "market_analysis_service consumes from dev.indicators and publishes to dev.intelligence"
    - "All existing unit tests still pass (1659+)"
  artifacts:
    - path: "services/tws_daemon.py"
      provides: "Kafka producer for market.bars + market.ticks; removed Redis XADD + HSET"
      contains: "KafkaProducerClient"
    - path: "services/indicator_service.py"
      provides: "Kafka consumer for market.bars, producer for indicators; DB warmup"
      contains: "KafkaConsumerClient"
    - path: "services/market_analysis_service.py"
      provides: "Kafka consumer for indicators, producer for intelligence"
      contains: "KafkaConsumerClient"
    - path: "services/timeframes_builder_service.py"
      provides: "Kafka consumer+producer for market.bars"
      contains: "KafkaConsumerClient"
  key_links:
    - from: "services/tws_daemon.py"
      to: "dev.market.bars"
      via: "KafkaProducerClient.publish(topic_market_bars(env), bar_dict, key=message_key(symbol, tf))"
      pattern: "topic_market_bars"
    - from: "services/indicator_service.py"
      to: "dev.indicators"
      via: "KafkaProducerClient.publish(topic_indicators(env), indicators_dict, key=message_key(symbol, tf))"
      pattern: "topic_indicators"
    - from: "services/market_analysis_service.py"
      to: "dev.intelligence"
      via: "KafkaProducerClient.publish(topic_intelligence(env), event_dict, key=message_key(symbol, tf))"
      pattern: "topic_intelligence"
---

<objective>
Migrate the hot tier (tws_daemon, timeframes_builder_service) and intelligence input pipeline (indicator_service, market_analysis_service) from DragonflyDB to Redpanda. DragonflyDB stays running for Plan 3-4 services that haven't migrated yet.

Purpose: The data foundation — bar ingestion and indicator computation — flows through Redpanda after this plan. Signal and AI services still read from DragonflyDB until Plan 3.
Output: 4 services migrated to aiokafka; indicator_service warmup reads from DB instead of xrevrange; all existing tests pass.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-redpanda-migration/30-CONTEXT.md
@.planning/phases/30-redpanda-migration/30-RESEARCH.md
@.planning/phases/30-redpanda-migration/30-01-SUMMARY.md

<interfaces>
<!-- New kafka_utils.py API (from Plan 1): -->
<!--   KafkaProducerClient(bootstrap_servers: str) -->
<!--     .start() -> None -->
<!--     .publish(topic: str, msg: dict, key: str | None = None) -> None -->
<!--     .stop() -> None -->
<!--   KafkaConsumerClient(*topics: str, bootstrap_servers: str, group_id: str, auto_offset_reset: str = "latest") -->
<!--     .start() -> None -->
<!--     .stop() -> None -->
<!--     .messages() -> AsyncGenerator[tuple[str, str | None, dict], None]   (topic, key, payload) -->

<!-- New stream_keys.py topic builders (from Plan 1): -->
<!--   topic_market_ticks(env_name: str) -> str   e.g. "dev.market.ticks" -->
<!--   topic_market_bars(env_name: str) -> str    e.g. "dev.market.bars" -->
<!--   topic_indicators(env_name: str) -> str     e.g. "dev.indicators" -->
<!--   topic_intelligence(env_name: str) -> str   e.g. "dev.intelligence" -->
<!--   message_key(symbol: str, timeframe: str | None = None) -> str   e.g. "ES:1m" -->

<!-- Settings.kafka_bootstrap_servers: str (from Plan 1, default "localhost:19092") -->

<!-- Indicator service warmup pattern (from Phase 26, signal_generator): -->
<!--   Read market_data_ohlcv from TimescaleDB at startup using asyncpg -->
<!--   SELECT symbol, timeframe, open, high, low, close, volume, timestamp -->
<!--   FROM market_data_ohlcv WHERE symbol=? AND timeframe=? ORDER BY timestamp DESC LIMIT N -->
<!--   Use min_bars_for_tf(tf) * 2 as N (same multiplier as current _WARMUP_READ_MULTIPLIER=5 but from DB) -->

<!-- tws_daemon: remove redis.asyncio HSET calls for price:SYMBOL:latest (quote_latest) -->
<!-- tws_daemon: remove redis.asyncio XADD calls; keep ib_insync logic in src/providers/ibkr.py intact -->

<!-- msg.topic, msg.key, msg.value available on each aiokafka ConsumerRecord -->
<!-- message key routing: key = message_key(symbol, tf) for bar/indicator messages; key = symbol for ticks -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 1): Migrate tws_daemon and timeframes_builder_service to Redpanda</name>
  <files>
    services/tws_daemon.py
    services/timeframes_builder_service.py
  </files>
  <behavior>
    - tws_daemon: replace redis_client.xadd(market(...), bar_fields, maxlen=N) with self._kafka_producer.publish(topic_market_bars(env_name), bar_dict, key=message_key(symbol, tf))
    - tws_daemon: replace redis_client.xadd(live_tick(...), tick_fields, maxlen=N) with self._kafka_producer.publish(topic_market_ticks(env_name), tick_dict, key=symbol)
    - tws_daemon: remove redis_client.hset(quote_latest(...), ...) calls entirely — no replacement in this plan (_live_quotes in signal_generator is the Plan 3 replacement)
    - tws_daemon: replace await redis.asyncio client init with KafkaProducerClient(bootstrap_servers=settings.kafka_bootstrap_servers); call .start() in setup, .stop() in shutdown
    - tws_daemon: remove all stream_keys imports that no longer apply (live_tick, quote_latest, market, get_stream_maxlen); add topic_market_ticks, topic_market_bars, message_key imports
    - timeframes_builder_service: replace redis xreadgroup consume loop with KafkaConsumerClient consuming topic_market_bars(env_name); replace xadd publish with KafkaProducerClient publishing to topic_market_bars(env_name) with key=message_key(symbol, tf)
    - timeframes_builder_service: remove ensure_consumer_group_with_reset calls; remove redis client setup
    - Both services: remove redis[hiredis] import; use kafka_bootstrap_servers from Settings
    - DragonflyDB is still running — these services stop writing to it but do not remove the DragonflyDB container
  </behavior>
  <action>
    Read services/tws_daemon.py fully first. Identify all Redis XADD, HSET, and redis client init/close calls. Replace with KafkaProducerClient pattern. Import from src.core.kafka_utils and src.core.stream_keys (new topic_* functions).

    Read services/timeframes_builder_service.py fully first. Replace xreadgroup consume loop with KafkaConsumerClient.messages() async for loop. The message key (msg.key decoded as SYMBOL:TF) provides the routing; no _stream_map needed.

    Verify services still import cleanly and pass any existing unit tests.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && .venv/bin/python -c "import services.tws_daemon; import services.timeframes_builder_service; print('imports ok')" && .venv/bin/pytest tests/unit/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>tws_daemon and timeframes_builder_service import without error. Full unit suite passes (1659+ tests). No redis.asyncio import in either service.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1): Migrate indicator_service and market_analysis_service to Redpanda</name>
  <files>
    services/indicator_service.py
    services/market_analysis_service.py
    tests/unit/service_tests/test_indicator_service.py
  </files>
  <behavior>
    - indicator_service: replace redis xreadgroup loop with KafkaConsumerClient consuming topic_market_bars(env_name); extract symbol, tf from message key (msg.key.decode().split(":", 1)); replace xadd publish with KafkaProducerClient publishing to topic_indicators(env_name) with key=message_key(symbol, tf)
    - indicator_service: WARMUP MIGRATION (Pitfall 3): remove xrevrange warmup call; add _seed_bar_history_from_db() that reads last min_bars_for_tf(tf) * 2 bars from market_data_ohlcv via asyncpg at startup per active symbol/TF; fall back silently if DB unreachable (log WARNING, continue with empty history); this follows the Phase 26 pattern in signal_generator_service
    - indicator_service: remove ensure_consumer_group_with_reset calls; remove all stream_keys imports of Redis-style functions; add topic_market_bars, topic_indicators, message_key
    - market_analysis_service: replace the existing single xreadgroup dict call (consuming all indicator streams) with KafkaConsumerClient multi-topic subscribe to topic_indicators(env_name); extract symbol, tf from msg.key; replace xadd publish with KafkaProducerClient publishing to topic_intelligence(env_name), topic_intelligence_i7(env_name), topic_intelligence_i8(env_name) with key=message_key(symbol, tf)
    - market_analysis_service: remove ensure_consumer_group_with_reset; remove _stream_map (stream name → symbol,tf lookup no longer needed); use msg.key.decode().split(":", 1) instead
    - tests/unit/service_tests/test_indicator_service.py: update existing tests to mock KafkaProducerClient and KafkaConsumerClient instead of redis mock; add test asserting publish call uses topic_indicators("dev") as topic and b"ES:1m" as key; assert KAFKA-05
    - Do NOT pass api_version to AIOKafkaConsumer or AIOKafkaProducer
    - DragonflyDB still runs; these services stop writing to it
  </behavior>
  <action>
    Read services/indicator_service.py fully. Identify warmup pattern, xreadgroup loop, xadd publish calls. Replace warmup with DB read using asyncpg (connect to settings.database_url, query market_data_ohlcv). Replace consume loop with KafkaConsumerClient.messages(). Replace publish with KafkaProducerClient.

    Read services/market_analysis_service.py fully. Identify the all-streams xreadgroup call and _stream_map. Replace with single KafkaConsumerClient subscribed to topic_indicators. Replace all xadd calls with KafkaProducerClient.

    Update tests/unit/service_tests/test_indicator_service.py: mock KafkaProducerClient.publish with AsyncMock; assert topic and key arguments for KAFKA-05 coverage.

    Run full unit suite after changes.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && .venv/bin/pytest tests/unit/service_tests/test_indicator_service.py -x -v && .venv/bin/pytest tests/unit/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>indicator_service and market_analysis_service import without Redis dependency. KAFKA-05: test_indicator_service passes asserting publish to "dev.indicators" with key "ES:1m". Full unit suite passes (1659+ tests).</done>
</task>

</tasks>

<verification>
1. `grep -r "redis.asyncio\|xreadgroup\|xadd\|xrevrange\|ensure_consumer_group" /home/bg/dev/indicagent/services/tws_daemon.py services/timeframes_builder_service.py services/indicator_service.py services/market_analysis_service.py` — no matches
2. `grep -r "KafkaProducerClient\|KafkaConsumerClient" /home/bg/dev/indicagent/services/indicator_service.py` — matches found
3. `.venv/bin/pytest tests/unit/ -x -q` — 1659+ tests pass
4. `.venv/bin/pytest tests/unit/service_tests/test_indicator_service.py -v` — includes KAFKA-05 test
</verification>

<success_criteria>
- 4 services (tws_daemon, timeframes_builder, indicator, market_analysis) use aiokafka exclusively
- No Redis stream operations (xadd, xreadgroup, xrevrange) in any of these 4 service files
- indicator_service seeds bar history from market_data_ohlcv at startup
- All 1659+ unit tests pass
- KAFKA-05 covered: indicator_service test asserts correct topic + key in publish call
</success_criteria>

<output>
After completion, create `.planning/phases/30-redpanda-migration/30-02-SUMMARY.md`
</output>
