---
phase: 30-redpanda-migration
plan: 04
type: execute
wave: 4
depends_on: ["30-03"]
files_modified:
  - services/feature_writer_service.py
  - services/llm_writer_service.py
  - src/api/routes/sse.py
  - src/api/main.py
  - src/api/dependencies.py
  - tests/unit/test_sse_stream_builder.py
autonomous: true
requirements:
  - KAFKA-07
  - KAFKA-08

must_haves:
  truths:
    - "feature_writer_service consumes from dev.intelligence and writes to TimescaleDB (no Redis xreadgroup)"
    - "llm_writer_service consumes from dev.llm.calls and dev.llm.outcomes and writes to TimescaleDB"
    - "SSE endpoint receives events from Redpanda topics via a shared broadcaster (no xrevrange or xread)"
    - "SSE snapshot uses per-topic deque(maxlen=30) in the broadcaster — new connections drain deque before live queue"
    - "SSE fan-out uses asyncio.Queue per connected client (not one Kafka consumer per client)"
    - "All 1659+ unit tests still pass"
  artifacts:
    - path: "services/feature_writer_service.py"
      provides: "Kafka consumer for intelligence topic; unchanged DB write logic"
      contains: "KafkaConsumerClient"
    - path: "services/llm_writer_service.py"
      provides: "Kafka consumer for llm.calls + llm.outcomes; unchanged DB write logic"
      contains: "KafkaConsumerClient"
    - path: "src/api/routes/sse.py"
      provides: "SSE endpoint consuming from KafkaSSEBroadcaster instead of Redis xread/xrevrange"
      contains: "KafkaSSEBroadcaster"
    - path: "src/api/dependencies.py"
      provides: "kafka_broadcaster dependency replacing redis_manager for SSE"
      contains: "kafka_broadcaster"
  key_links:
    - from: "src/api/routes/sse.py"
      to: "KafkaSSEBroadcaster"
      via: "dependencies.kafka_broadcaster.subscribe() returns asyncio.Queue; drain deque then poll queue"
      pattern: "kafka_broadcaster"
    - from: "src/api/main.py"
      to: "KafkaSSEBroadcaster"
      via: "lifespan: start broadcaster background task on startup, stop on shutdown"
      pattern: "lifespan"
---

<objective>
Migrate the two writer services (feature_writer, llm_writer) and the SSE API endpoint from DragonflyDB to Redpanda. The SSE migration includes implementing the per-topic deque snapshot pattern (no xrevrange equivalent in Kafka) and per-client asyncio.Queue fan-out.

Purpose: All 8 services are now on Redpanda after this plan. DragonflyDB removal happens in Plan 5.
Output: 2 writer services migrated. SSE uses KafkaSSEBroadcaster with fan-out queue + snapshot deque. All tests pass.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-redpanda-migration/30-CONTEXT.md
@.planning/phases/30-redpanda-migration/30-RESEARCH.md
@.planning/phases/30-redpanda-migration/30-03-SUMMARY.md

<interfaces>
<!-- KafkaProducerClient / KafkaConsumerClient from src/core/kafka_utils.py -->
<!-- topic_* builders from src/core/stream_keys.py -->

<!-- SSE broadcaster pattern (RESEARCH.md Pattern 6 + Pitfall 4): -->
<!--   KafkaSSEBroadcaster lives in src/api/routes/sse.py or a new src/api/kafka_broadcaster.py -->
<!--   Constructor: stores per-topic deque(maxlen=30) for snapshot + list[asyncio.Queue] for fan-out -->
<!--   run(consumer: KafkaConsumerClient): async for topic, key, payload in consumer.messages(): -->
<!--     self._snapshot[topic].appendleft((topic, key, payload))   # deque newest-first -->
<!--     for q in list(self._queues):                              # copy list to avoid mutation -->
<!--       try: q.put_nowait((topic, key, payload))                # non-blocking -->
<!--       except asyncio.QueueFull: pass                          # slow client — skip -->
<!--   subscribe() -> tuple[deque, asyncio.Queue]:                 # returns snapshot deque + live queue -->
<!--   unsubscribe(q: asyncio.Queue): self._queues.remove(q) -->

<!-- SSE endpoint new flow (replaces xrevrange + xread): -->
<!--   On connect: snapshot, live_q = broadcaster.subscribe() -->
<!--   Snapshot phase: for (topic, key, payload) in reversed(list(snapshot)):  -->
<!--     emit SSE event from payload                                -->
<!--   Live phase: while True: event = await asyncio.wait_for(live_q.get(), timeout=5.0) -->
<!--     if event is None: break (disconnect sentinel)             -->
<!--     emit SSE event                                            -->
<!--   On disconnect: broadcaster.unsubscribe(live_q)             -->

<!-- feature_writer_service current Redis dependency: -->
<!--   xreadgroup consuming all intelligence:SYMBOL:TF streams    -->
<!--   The existing sequential stream polling anti-pattern becomes a single KafkaConsumer subscribe -->
<!--   DB write logic (asyncpg inserts to intelligence_features) is UNCHANGED -->

<!-- llm_writer_service current Redis dependency: -->
<!--   xreadgroup consuming llm_calls:stream and llm_outcomes:stream -->
<!--   Replace with KafkaConsumerClient(topic_llm_calls(env_name), topic_llm_outcomes(env_name)) -->
<!--   DB write logic is UNCHANGED -->

<!-- src/api/dependencies.py currently has: -->
<!--   redis_manager: RedisManager | None = None  — KEEP through Plan 4 -->
<!--   Add: kafka_broadcaster: KafkaSSEBroadcaster | None = None -->

<!-- src/api/main.py lifespan: -->
<!--   Currently inits redis_manager; add kafka_broadcaster init + background task start -->
<!--   Keep redis_manager init (for any remaining Redis uses outside SSE) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 1): Migrate feature_writer_service and llm_writer_service to Redpanda</name>
  <files>
    services/feature_writer_service.py
    services/llm_writer_service.py
  </files>
  <behavior>
    - feature_writer_service: remove the sequential xreadgroup polling loop over all intelligence streams; replace with single KafkaConsumerClient(topic_intelligence(env_name), bootstrap_servers=..., group_id="feature_writer_group", auto_offset_reset="latest"); extract symbol, tf from msg.key.decode().split(":", 1); all DB write logic (asyncpg inserts to intelligence_features) unchanged
    - feature_writer_service: remove ensure_consumer_group_with_reset, Redis client init/close; remove all stream_keys Redis imports; add topic_intelligence import from stream_keys
    - feature_writer_service: the existing "process intelligence payload batch" logic — keep entirely; only the consumer source changes
    - llm_writer_service: remove xreadgroup loops consuming llm_calls:stream and llm_outcomes:stream; replace with KafkaConsumerClient(topic_llm_calls(env_name), topic_llm_outcomes(env_name), ...); message dispatch by topic name
    - llm_writer_service: remove Redis client init/close; remove ensure_consumer_group_with_reset; all DB write logic unchanged
    - Both services: no Redis imports remaining after migration
    - DragonflyDB still running — no compose changes in this task
  </behavior>
  <action>
    Read services/feature_writer_service.py fully. Identify the sequential stream polling loop and Redis setup. Replace with KafkaConsumerClient single subscriber. Keep all asyncpg batch insert logic intact.

    Read services/llm_writer_service.py fully. Identify xreadgroup calls for llm_calls and llm_outcomes. Replace with KafkaConsumerClient multi-topic subscribe.

    Run unit suite after changes.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && .venv/bin/python -c "import services.feature_writer_service; import services.llm_writer_service; print('imports ok')" && .venv/bin/pytest tests/unit/service_tests/test_feature_writer_service.py tests/unit/service_tests/test_llm_writer_service.py -x -v && .venv/bin/pytest tests/unit/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>feature_writer_service and llm_writer_service have no Redis xreadgroup/xadd operations. Import cleanly. Existing tests pass. Full suite passes (1659+).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1): Implement KafkaSSEBroadcaster and migrate SSE endpoint to Redpanda fan-out</name>
  <files>
    src/api/routes/sse.py
    src/api/dependencies.py
    src/api/main.py
    tests/unit/test_sse_stream_builder.py
  </files>
  <behavior>
    - Add KafkaSSEBroadcaster class to src/api/routes/sse.py (or new src/api/kafka_broadcaster.py imported there):
      - __init__: self._queues: list[asyncio.Queue] = []; self._snapshot: dict[str, deque] = defaultdict(lambda: deque(maxlen=30))
      - async run(consumer: KafkaConsumerClient): async for topic, key, payload in consumer.messages(): self._snapshot[topic].appendleft({"topic": topic, "key": key, "payload": payload}); for q in list(self._queues): try q.put_nowait({"topic": topic, "key": key, "payload": payload}); except asyncio.QueueFull: pass
      - subscribe() -> tuple[deque, asyncio.Queue]: q = asyncio.Queue(maxsize=500); self._queues.append(q); return self._snapshot, q
      - unsubscribe(q: asyncio.Queue): try self._queues.remove(q); except ValueError: pass
    - SSE sse_events() endpoint rewrite:
      - Remove: redis_manager dependency, xrevrange snapshot loop, xread live loop
      - Add: dependencies.kafka_broadcaster dependency
      - Snapshot phase: for each topic in _build_topic_list(symbol_list, timeframe): for item in reversed(list(broadcaster._snapshot[topic])): emit SSE frame from item
      - Live phase: while True: check request.is_disconnected(); try: item = await asyncio.wait_for(live_q.get(), timeout=5.0); emit SSE frame; except asyncio.TimeoutError: yield heartbeat; except asyncio.CancelledError: break
      - On exit: broadcaster.unsubscribe(live_q)
    - _build_stream_list() renamed to _build_topic_list(): returns topic names (e.g., "dev.intelligence") instead of Redis stream names; no longer one topic-per-TF — topics are flat (e.g., one "dev.indicators" for all symbols/TFs)
    - _event_name_for_stream() → _event_name_for_topic(): maps Kafka topic name to SSE event name; same mapping logic adapted for period-separated names (e.g., "dev.intelligence.i7" → "signal_scorecard", "dev.intelligence" → "intelligence_data")
    - src/api/dependencies.py: add kafka_broadcaster: KafkaSSEBroadcaster | None = None module-level variable
    - src/api/main.py: in lifespan context, create KafkaSSEBroadcaster; create KafkaConsumerClient subscribed to all SSE topics; asyncio.create_task(broadcaster.run(consumer)); set dependencies.kafka_broadcaster; on shutdown: consumer.stop(), task.cancel()
    - tests/unit/test_sse_stream_builder.py: extend with test_kafka_broadcaster_fan_out (KAFKA-07): mock consumer; assert subscribe() returns (deque, Queue); assert after run() processes a message, the queue receives it and _snapshot[topic] has it
  </behavior>
  <action>
    Read src/api/routes/sse.py fully (already read above — 247 lines). The entire xrevrange + xread logic is replaced.

    Implement KafkaSSEBroadcaster. Rewrite sse_events() endpoint. Update _build_stream_list to _build_topic_list. Update _event_name_for_stream to handle period-separated topic names.

    Update src/api/dependencies.py to add kafka_broadcaster variable.

    Update src/api/main.py lifespan to start broadcaster.

    Update tests/unit/test_sse_stream_builder.py with KAFKA-07 fan-out test.

    The existing SSE event name mapping must be preserved — dashboard expects the same event names (tick_data, market_data, indicator_data, intelligence_data, signal_scorecard, signal_data, narrative_data, system_event). Map from Kafka topic names to these same names.

    Note: payload structure changes slightly — instead of {stream, id, payload} the new structure is {topic, key, payload}. The payload dict itself is unchanged (same IntelligenceEvent JSON, same fields). Dashboard reads payload.field — this is unaffected.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && .venv/bin/pytest tests/unit/test_sse_stream_builder.py -x -v && .venv/bin/pytest tests/unit/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>KAFKA-07 test passes: broadcaster fan-out delivers to subscribed queues and snapshot deque populated. SSE endpoint uses KafkaSSEBroadcaster. No xrevrange or xread in sse.py. Full suite passes (1659+).</done>
</task>

</tasks>

<verification>
1. `grep -n "xrevrange\|xread\|redis_manager\|xreadgroup\|xadd" /home/bg/dev/indicagent/src/api/routes/sse.py` — no matches
2. `grep -n "xreadgroup\|xadd\|ensure_consumer_group" /home/bg/dev/indicagent/services/feature_writer_service.py services/llm_writer_service.py` — no matches
3. `grep "kafka_broadcaster" /home/bg/dev/indicagent/src/api/dependencies.py` — present
4. `.venv/bin/pytest tests/unit/test_sse_stream_builder.py -v` — KAFKA-07 test passes
5. `.venv/bin/pytest tests/unit/ -x -q` — 1659+ pass
</verification>

<success_criteria>
- All 8 services (tws_daemon, timeframes_builder, indicator, market_analysis, signal_generator, signal_lifecycle, ai_narrative, feature_writer, llm_writer) use Redpanda exclusively for stream operations
- SSE endpoint uses KafkaSSEBroadcaster with fan-out queue + per-topic deque snapshot
- KAFKA-07 covered in test_sse_stream_builder.py
- All 1659+ unit tests pass
- DragonflyDB container still running (not removed until Plan 5)
</success_criteria>

<output>
After completion, create `.planning/phases/30-redpanda-migration/30-04-SUMMARY.md`
</output>
