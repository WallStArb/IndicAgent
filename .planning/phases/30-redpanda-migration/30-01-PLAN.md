---
phase: 30-redpanda-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/docker-compose.yml
  - requirements.txt
  - src/config/settings.py
  - src/core/stream_keys.py
  - src/core/kafka_utils.py
  - production/scripts/kafka_init_topics.py
  - tests/unit/core/test_kafka_utils.py
  - tests/unit/core/test_kafka_init_topics.py
  - tests/unit/test_stream_keys.py
autonomous: true
requirements:
  - KAFKA-01
  - KAFKA-02
  - KAFKA-03
  - KAFKA-04

must_haves:
  truths:
    - "Redpanda container runs in docker-compose alongside DragonflyDB (dual-run period)"
    - "aiokafka is installed and importable in the venv"
    - "src/core/kafka_utils.py provides KafkaProducerClient and KafkaConsumerClient wrappers"
    - "src/core/stream_keys.py provides topic_* builder functions and message_key() helper"
    - "production/scripts/kafka_init_topics.py creates all 11 topics idempotently"
    - "Settings has kafka_bootstrap_servers field defaulting to localhost:19092"
    - "All existing 1659 unit tests still pass"
  artifacts:
    - path: "src/core/kafka_utils.py"
      provides: "KafkaProducerClient, KafkaConsumerClient — aiokafka wrappers"
      exports: ["KafkaProducerClient", "KafkaConsumerClient"]
    - path: "production/scripts/kafka_init_topics.py"
      provides: "Idempotent topic creation for all 11 topics"
      contains: "AIOKafkaAdminClient"
    - path: "src/core/stream_keys.py"
      provides: "Topic name builders and message_key() — replaces Redis key helpers"
      contains: "topic_market_ticks"
    - path: "tests/unit/core/test_kafka_utils.py"
      provides: "Unit tests for KafkaProducerClient and KafkaConsumerClient (Wave 0)"
    - path: "tests/unit/core/test_kafka_init_topics.py"
      provides: "Unit tests for kafka_init_topics.py (Wave 0)"
  key_links:
    - from: "src/config/settings.py"
      to: "kafka_bootstrap_servers field"
      via: "Field(default='localhost:19092', validation_alias='KAFKA_BOOTSTRAP_SERVERS')"
      pattern: "kafka_bootstrap_servers"
    - from: "src/core/kafka_utils.py"
      to: "aiokafka.AIOKafkaProducer"
      via: "KafkaProducerClient.start()/publish()/stop()"
      pattern: "AIOKafkaProducer"
    - from: "production/docker-compose.yml"
      to: "redpanda container"
      via: "external listener on 19092"
      pattern: "19092:19092"
---

<objective>
Install Redpanda alongside DragonflyDB (dual-run), add aiokafka, rewrite stream_keys.py with topic builders, create kafka_utils.py with producer/consumer wrappers, and write the topic init script.

Purpose: Lay the transport layer foundation that Plans 2-4 will build on. DragonflyDB stays running — this plan does not touch any service code.
Output: kafka_utils.py, topic_init_script, rewritten stream_keys.py, updated docker-compose, updated settings, stub test files (Wave 0), passing unit tests.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-redpanda-migration/30-CONTEXT.md
@.planning/phases/30-redpanda-migration/30-RESEARCH.md
@.planning/phases/30-redpanda-migration/30-VALIDATION.md

<interfaces>
<!-- Current stream_keys.py exports (to be REPLACED — not extended) -->
<!-- src/core/stream_keys.py current API: -->
<!--   prefix(env_name) -> str                    — KEEP but rename logic -->
<!--   live_tick(env_prefix, symbol) -> str        — REPLACE with topic_market_ticks(env_name) -->
<!--   quote_latest(env_prefix, symbol) -> str     — DELETE (Plan 5) -->
<!--   market(env_prefix, symbol, tf) -> str       — REPLACE with topic_market_bars(env_name) -->
<!--   indicators(env_prefix, symbol, tf) -> str   — REPLACE with topic_indicators(env_name) -->
<!--   intelligence(...)                           — REPLACE with topic_intelligence(env_name) -->
<!--   intelligence_i7(...)                        — REPLACE with topic_intelligence_i7(env_name) -->
<!--   intelligence_i8(...)                        — REPLACE with topic_intelligence_i8(env_name) -->
<!--   signals(...)                                — REPLACE with topic_signals(env_name) -->
<!--   signals_aggregated(...)                     — REPLACE with topic_signals_aggregated(env_name) -->
<!--   narratives(...)                             — REPLACE with topic_narratives(env_name) -->
<!--   llm_calls_stream(env_prefix)                — REPLACE with topic_llm_calls(env_name) -->
<!--   llm_outcomes_stream(env_prefix)             — REPLACE with topic_llm_outcomes(env_name) -->
<!--   llm_scores_cache(...), setup_performance_weights_cache(...), drift_ks(...), drift_cusum(...)
<!--     — KEEP through Plans 1-4 dual-run; DELETE in Plan 5 -->
<!--   get_stream_maxlen(...)                      — DELETE (retention moves to topic init script) -->
<!--   message_key(symbol, timeframe=None) -> str  — NEW helper -->

<!-- Current settings.py Redis fields (keep during dual-run): -->
<!--   env_name: str = Field(default="", validation_alias="INDICAGENT_ENV") -->
<!--   redis_host, redis_port, redis_db, redis_max_connections — KEEP through Plan 4 -->
<!--   kafka_bootstrap_servers: str — ADD in this plan -->

<!-- Current stream_utils.py (KEEP during dual-run): -->
<!--   ensure_consumer_group_with_reset(redis_client, stream_name, group_name, ...) -> bool -->
<!--   NOTE: This file stays intact through Plan 4. New kafka_utils.py is the replacement. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): Create stub test files for kafka_utils and kafka_init_topics</name>
  <files>
    tests/unit/core/test_kafka_utils.py
    tests/unit/core/test_kafka_init_topics.py
  </files>
  <behavior>
    - test_kafka_utils.py stubs:
      - test_producer_client_start_and_stop: asserts KafkaProducerClient.start() calls AIOKafkaProducer.start(), stop() calls producer.stop()
      - test_producer_client_publish: asserts publish(topic, msg_dict, key="ES:1m") calls send_and_wait with correct topic, json-encoded value bytes, and b"ES:1m" key bytes
      - test_consumer_client_start_and_stop: asserts KafkaConsumerClient.start() calls AIOKafkaConsumer.start(), stop() calls consumer.stop()
      - test_consumer_client_messages_yields_tuples: asserts messages() async generator yields (topic, key_str, payload_dict) tuples; key None when msg.key is None
    - test_kafka_init_topics.py stubs:
      - test_create_topics_calls_admin_client: asserts create_topics() calls AIOKafkaAdminClient.start() then create_topics() with 11 NewTopic objects
      - test_create_topics_idempotent: asserts TopicExistsException is caught and does not raise
    - All tests MUST FAIL (RED) before kafka_utils.py and kafka_init_topics.py exist — that is the correct Wave 0 state
    - Use pytest.mark.asyncio; mock AIOKafkaProducer, AIOKafkaConsumer, AIOKafkaAdminClient with AsyncMock
    - Do NOT pass api_version to any aiokafka constructor (breaking change in 0.13.0)
  </behavior>
  <action>
    Create tests/unit/core/test_kafka_utils.py with 4 async test functions using AsyncMock for aiokafka classes.
    Create tests/unit/core/test_kafka_init_topics.py with 2 async test functions using AsyncMock for AIOKafkaAdminClient.
    Both files must import from src.core.kafka_utils and production.scripts.kafka_init_topics respectively (will fail import until implementation exists — that is correct for Wave 0).
    Ensure tests/unit/core/__init__.py exists.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && .venv/bin/pytest tests/unit/core/test_kafka_utils.py tests/unit/core/test_kafka_init_topics.py -x 2>&1 | tail -5</automated>
  </verify>
  <done>Both test files exist. Tests fail with ImportError (kafka_utils.py and kafka_init_topics.py don't exist yet) — RED state is correct for Wave 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1): Implement kafka_utils.py and kafka_init_topics.py, then make tests GREEN</name>
  <files>
    src/core/kafka_utils.py
    production/scripts/kafka_init_topics.py
  </files>
  <behavior>
    - KafkaProducerClient.start(): creates AIOKafkaProducer(bootstrap_servers=self._bootstrap) and calls await producer.start(). No api_version param.
    - KafkaProducerClient.publish(topic, msg, key=None): json.dumps(msg).encode() for value; key.encode() if key else None; await self._producer.send_and_wait(topic, value=value, key=key_bytes)
    - KafkaProducerClient.stop(): await self._producer.stop() if self._producer
    - KafkaConsumerClient.__init__(*topics, bootstrap_servers, group_id, auto_offset_reset="latest"): creates AIOKafkaConsumer(*topics, ..., enable_auto_commit=True). No api_version param.
    - KafkaConsumerClient.messages(): async generator yielding (topic_str, key_str_or_None, payload_dict)
    - kafka_init_topics.py: async create_topics(bootstrap_servers) function using AIOKafkaAdminClient + NewTopic for all 11 topics; catches TopicExistsException silently; main() runs asyncio.run(create_topics(bootstrap_servers))
    - 11 topics (with retention.ms): market.ticks=604800000, market.bars=604800000, indicators=604800000, intelligence=604800000, intelligence.i7=86400000, intelligence.i8=86400000, signals=604800000, signals.aggregated=604800000, narratives=604800000, llm.calls=604800000, llm.outcomes=604800000
    - Topic names use env prefix from argv or default "dev" in __main__
  </behavior>
  <action>
    Implement src/core/kafka_utils.py with KafkaProducerClient and KafkaConsumerClient classes exactly as defined in RESEARCH.md code examples section. Use from __future__ import annotations, type hints on all methods.

    Implement production/scripts/kafka_init_topics.py with async create_topics(bootstrap_servers: str) function. Read env_name from sys.argv[1] if provided else "dev". Topics list uses f"{env_name}.{topic_suffix}" naming (e.g., "dev.market.ticks"). idempotency: catch both TopicExistsException and any error where "already exists" in str(e).lower().

    After implementation, run tests — they must go GREEN.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && .venv/bin/pytest tests/unit/core/test_kafka_utils.py tests/unit/core/test_kafka_init_topics.py -x -v</automated>
  </verify>
  <done>All 6 tests in test_kafka_utils.py + test_kafka_init_topics.py pass GREEN. kafka_utils.py and kafka_init_topics.py exist with correct implementations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (Wave 1): Rewrite stream_keys.py with topic builders + update Settings + add Redpanda to docker-compose</name>
  <files>
    src/core/stream_keys.py
    src/config/settings.py
    production/docker-compose.yml
    requirements.txt
    tests/unit/test_stream_keys.py
  </files>
  <behavior>
    - stream_keys.py new API:
      - env_prefix(env_name) helper: returns f"{env_name}." if env_name else "" (period-separated, not colon-separated)
      - topic_market_ticks(env_name), topic_market_bars(env_name), topic_indicators(env_name), topic_intelligence(env_name), topic_intelligence_i7(env_name), topic_intelligence_i8(env_name), topic_signals(env_name), topic_signals_aggregated(env_name), topic_narratives(env_name), topic_llm_calls(env_name), topic_llm_outcomes(env_name) — each returns f"{env_prefix(env_name)}{suffix}"
      - message_key(symbol, timeframe=None) -> str: returns f"{symbol}:{timeframe}" if timeframe else symbol
      - KEEP (for dual-run, delete in Plan 5): all existing Redis key functions (live_tick, quote_latest, market, indicators, intelligence, intelligence_i7, intelligence_i8, signals, signals_aggregated, narratives, narratives_group, llm_calls_stream, llm_outcomes_stream, system_events, llm_scores_cache, setup_performance_weights_cache, drift_ks, drift_cusum, get_stream_maxlen, *_pattern helpers)
      - The old prefix() function (colon-separated) stays as-is for existing callers
    - settings.py: add kafka_bootstrap_servers: str = Field(default="localhost:19092", validation_alias="KAFKA_BOOTSTRAP_SERVERS") after the redis fields
    - docker-compose.yml: add redpanda service block (from RESEARCH.md) with image docker.redpanda.com/redpandadata/redpanda:v25.3.10, ports 19092:19092, 9644:9644; add redpanda-data volume; keep dragonfly service intact
    - requirements.txt: add aiokafka>=0.13.0 line; keep redis[hiredis] line (remove in Plan 5)
    - tests/unit/test_stream_keys.py: add test cases asserting topic_* functions return correct strings for env_name="dev" and env_name="" (no prefix)
    - test_kafka_utils passes env="dev" → "dev.indicators", env="" → "indicators"
  </behavior>
  <action>
    Rewrite src/core/stream_keys.py: add new topic_* functions and message_key() at the TOP of the file (before the existing Redis helpers). Keep all existing functions intact for dual-run compatibility. Add env_prefix() as a new name (different from old prefix() which uses colons).

    Add kafka_bootstrap_servers field to Settings class in src/config/settings.py.

    Add Redpanda service to production/docker-compose.yml following the RESEARCH.md docker-compose service block exactly. Add redpanda-data to volumes section.

    Add aiokafka>=0.13.0 to requirements.txt. Install: cd /home/bg/dev/indicagent && .venv/bin/pip install aiokafka>=0.13.0 -q

    Extend tests/unit/test_stream_keys.py with assertions for new topic_* functions.

    Run full unit suite to verify no regressions.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && .venv/bin/pytest tests/unit/test_stream_keys.py tests/unit/core/ -x -v && .venv/bin/pytest tests/unit/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>All stream_keys tests pass including new topic_* assertions. Full unit suite passes (1659+ tests). Redpanda service block present in docker-compose.yml. kafka_bootstrap_servers in Settings. aiokafka importable in venv.</done>
</task>

</tasks>

<verification>
1. `cd /home/bg/dev/indicagent && .venv/bin/python -c "from src.core.kafka_utils import KafkaProducerClient, KafkaConsumerClient; print('ok')"` — no ImportError
2. `cd /home/bg/dev/indicagent && .venv/bin/python -c "from src.core.stream_keys import topic_indicators, message_key; print(topic_indicators('dev'), message_key('ES', '1m'))"` → `dev.indicators ES:1m`
3. `cd /home/bg/dev/indicagent && .venv/bin/python -c "from src.config.settings import Settings; s=Settings(); print(s.kafka_bootstrap_servers)"` → `localhost:19092`
4. `grep "19092" /home/bg/dev/indicagent/production/docker-compose.yml` — port mapping present
5. `.venv/bin/pytest tests/unit/ -x -q` — 1659+ tests pass
</verification>

<success_criteria>
- kafka_utils.py, kafka_init_topics.py, updated stream_keys.py all importable
- Redpanda service added to docker-compose.yml alongside DragonflyDB
- aiokafka installed in venv
- Settings.kafka_bootstrap_servers = "localhost:19092"
- All 1659 existing unit tests still pass
- 6 new kafka_utils + kafka_init_topics tests pass GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/30-redpanda-migration/30-01-SUMMARY.md`
</output>
