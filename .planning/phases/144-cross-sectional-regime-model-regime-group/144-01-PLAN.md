---
phase: 144-cross-sectional-regime-model-regime-group
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/229_regime_group.sql
  - docs/foundation/glossary.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "D-01: this plan is the schema/APR foundation of Phase 144's locked mechanism-only scope (migration + APR seed, no bundled todo work)"
    - "market_regimes has a regime_group column (renamed from asset_class), existing rows preserved"
    - "alpha.regime.groups APR key exists with value_type='json' seeding the enabled/disabled group configs"
    - "Per-group signal-threshold APR keys exist for equity/rates/commodity/fx namespaces"
    - "glossary.md defines regime_group as a named peer group with a pluggable cross-sectional regime signal"
  artifacts:
    - path: "production/migrations/229_regime_group.sql"
      provides: "Column rename + index rename + APR schema/state seed for regime_group"
      contains: "RENAME COLUMN asset_class TO regime_group"
    - path: "docs/foundation/glossary.md"
      provides: "regime_group glossary entry"
      contains: "regime_group"
  key_links:
    - from: "production/migrations/229_regime_group.sql"
      to: "market_regimes"
      via: "ALTER TABLE ... RENAME COLUMN"
      pattern: "RENAME COLUMN asset_class TO regime_group"
    - from: "production/migrations/229_regime_group.sql"
      to: "config_schema / config_state"
      via: "INSERT alpha.regime.groups (value_type='json')"
      pattern: "alpha.regime.groups"
---

<objective>
Establish the schema + APR foundation for `regime_group`: rename `market_regimes.asset_class` to
`regime_group` (preserving all existing rows and the PK), rename its index, and seed the
`alpha.regime.groups` JSON config plus per-group signal-threshold APR keys. Add the `regime_group`
glossary entry.

Purpose: Everything downstream (dispatcher, ic_engine routing, signal modules) reads the renamed
column and the new APR keys. This is Wave 1 foundation — no code depends on the dispatcher yet, but
the dispatcher and ic_engine both depend on this.
Output: `production/migrations/229_regime_group.sql`, updated `docs/foundation/glossary.md`.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-cross-sectional-regime-model-regime-group/144-CONTEXT.md
@.planning/phases/144-cross-sectional-regime-model-regime-group/144-RESEARCH.md
@.planning/phases/144-cross-sectional-regime-model-regime-group/144-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 229 — rename asset_class to regime_group + seed APR keys</name>
  <files>production/migrations/229_regime_group.sql</files>
  <read_first>
    - docs/plans/archive/2026-07-01-cross-sectional-regime-model.md (Task 1, lines 110-308 — the authoritative migration SQL and APR key list; treat the literal "189" and any "-- Migration 189" comment as STALE)
    - production/migrations/182_equity_regime_model_apr.sql (verbatim APR schema+state INSERT pattern — config_schema then config_state, ON CONFLICT DO NOTHING)
    - production/migrations/174_market_regimes.sql (original market_regimes table + index definition being renamed)
    - production/migrations/228_instrument_tag_vocabulary_v2.sql (confirm highest applied number is 228; note its internal comment says "-- Migration 121" — a stale-comment example NOT to replicate)
  </read_first>
  <action>
    Create production/migrations/229_regime_group.sql. The internal SQL header comment MUST read
    exactly `-- Migration 229: regime_group` (NOT the plan doc's stale "-- Migration 189"; migration
    renumbering has drifted internal comments before per RESEARCH.md Pitfall 5).

    Port Task 1's SQL from the plan doc (lines 110-308) with these concrete corrections:
    (1) Column rename: `ALTER TABLE market_regimes RENAME COLUMN asset_class TO regime_group;` — the
    PK `(asset_class, tf, ts)` tracks the renamed column automatically, no separate PK rebuild.
    (2) Index rename: `ALTER INDEX market_regimes_equity_tf_ts RENAME TO market_regimes_regime_group_tf_ts;`
    (3) Seed `alpha.regime.groups` with `value_type='json'` in config_schema and the JSON array of
    group configs in config_state (equity enabled, rates enabled, commodity_* and fx enabled:false)
    exactly as the plan doc specifies. Use the plan doc's group JSON verbatim.
    (4) Seed per-group signal-threshold APR keys under the `alpha.equity_regime.*`,
    `alpha.rates_regime.*`, `alpha.commodity_momentum_regime.*`, `alpha.fx_regime.*` namespaces per
    the plan doc. For every window-denominated key (realized_vol_window, vix_z_window, ma_window,
    curve_window, credit_window), the `description` MUST state the window is in DAILY bars and is
    `_tf_window`-scaled to the target TF at compute time (this locks the cross-package convention that
    Plan 04's dispatcher pre-scales ALL group window params via `_tf_window`, resolving RESEARCH.md
    Assumption A1 — curve_credit windows scale the same way as breadth_vol). Provenance tags per
    CLAUDE.md APR mandate: `[initial_estimate]` for calibration windows, `[conventional]` for tier
    boundaries.
    (5) All INSERTs use `ON CONFLICT (config_key) DO NOTHING` (schema) / `ON CONFLICT (config_key) DO NOTHING` (state).
    Do NOT drop or retire `alpha.regime.equity_model_enabled` in this migration — its retirement is
    decided in Plan 05 (ic_engine) after a project-wide grep confirms ic_engine is the sole consumer.
  </action>
  <verify>
    <automated>grep -c "RENAME COLUMN asset_class TO regime_group" production/migrations/229_regime_group.sql | grep -qx 1 && grep -q "Migration 229" production/migrations/229_regime_group.sql && ! grep -q "Migration 189" production/migrations/229_regime_group.sql && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep "RENAME COLUMN asset_class TO regime_group" production/migrations/229_regime_group.sql` matches exactly once
    - `grep "Migration 189" production/migrations/229_regime_group.sql` returns NOTHING
    - `grep "value_type" production/migrations/229_regime_group.sql | grep -c "json"` >= 1 (alpha.regime.groups is json-typed)
    - `grep -c "_tf_window" production/migrations/229_regime_group.sql` >= 1 (window keys note daily-bar + _tf_window scaling)
    - The internal header comment is exactly `-- Migration 229: regime_group`
  </acceptance_criteria>
  <done>Migration file exists, renames the column/index, seeds alpha.regime.groups (json) + per-group threshold keys, internal comment matches filename number.</done>
</task>

<task type="auto">
  <name>Task 2: Glossary — add regime_group entry</name>
  <files>docs/foundation/glossary.md</files>
  <read_first>
    - docs/plans/archive/2026-07-01-cross-sectional-regime-model.md (Task 0, lines 71-110 — the glossary entry text)
    - docs/foundation/glossary.md (existing entry format: bolded term + prose + "Contrast with" cross-refs)
  </read_first>
  <action>
    Add the `regime_group` glossary entry using the plan doc's Task 0 text (lines 80-99), matching the
    existing house style (bolded term, prose definition, "Contrast with" cross-reference). Define
    regime_group as a named peer group with a pluggable cross-sectional regime signal (breadth_vol for
    equity, curve_credit for rates), replacing the old `asset_class` column. Cross-reference the
    per-symbol HMM `idiosyncratic/symbol regime` vs this cross-sectional `systematic/market regime`
    distinction already in the glossary. Per CLAUDE.md glossary rule: exactly one definition per term;
    verify `regime_group` is not already defined before inserting.
  </action>
  <verify>
    <automated>grep -c "regime_group" docs/foundation/glossary.md | awk '$1>=1{print "PASS"}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep "regime_group" docs/foundation/glossary.md` matches at least once as a defined term
    - Entry follows the existing bolded-term + prose format
    - No duplicate `regime_group` definition introduced
  </acceptance_criteria>
  <done>glossary.md has a single regime_group entry in house style, cross-referencing the HMM/cross-sectional regime distinction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → APR config_state | Operator-authored `alpha.regime.groups` JSON seeded by this migration is trusted config, not external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-01-M | Tampering | ALTER TABLE column rename | accept | Idempotent DDL; migration applied by hand via `psql -f` against a locally-trusted DB. Data preservation verified by post-apply row-count spot check (Verification section). No external surface. |
| T-144-01-J | Tampering | alpha.regime.groups JSON seed | mitigate | Seeded value is operator-authored and consumed via fail-loud parsing downstream (Plan 04/05 normalize + AmbiguousRegimeGroupError). Malformed JSON surfaces as a loud parse error, never a silent wrong label. |
| T-144-SC | Tampering | package installs | accept | This phase installs zero external packages — supply-chain surface is N/A. |
</threat_model>

<verification>
- Migration applies cleanly against a live DB: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/229_regime_group.sql` (execute only when convenient — not gated on any code here).
- Post-apply schema check: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d market_regimes"` shows `regime_group` column, PK intact, row count unchanged vs. pre-apply.
- APR keys present: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT config_key FROM config_state WHERE config_key LIKE 'alpha.regime%' OR config_key LIKE 'alpha.%_regime.%'"`.
</verification>

<success_criteria>
- 229_regime_group.sql renames the column + index and seeds all APR keys, internal comment number matches filename
- glossary.md defines regime_group once, in house style
- No retirement of alpha.regime.equity_model_enabled in this migration (deferred to Plan 05)
</success_criteria>

<output>
Create `.planning/phases/144-cross-sectional-regime-model-regime-group/144-01-SUMMARY.md` when done
</output>
