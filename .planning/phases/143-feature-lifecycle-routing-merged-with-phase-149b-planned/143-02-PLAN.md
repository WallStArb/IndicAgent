---
phase: 143-feature-lifecycle-routing-merged-with-phase-149b-planned
plan: 02
type: execute
wave: 2
depends_on: [143-01]
files_modified:
  - production/migrations/202_feature_registry_lifecycle_columns.sql
  - production/migrations/203_drop_dead_feature_ic_scores_columns.sql
  - src/intelligence/feature_registry_service.py
  - tests/unit/intelligence/test_feature_registry_service.py
autonomous: true
requirements: [LIFECYCLE-01]
must_haves:
  truths:
    - "feature_registry carries lifecycle counters (consecutive shadow passes/fails, observations since demotion) and pre_shadow_weight as registry columns, not feature_ic_scores columns"
    - "FeatureRegistryService has a SYNCHRONOUS record_transition_sync(conn, ...) method callable from ic_engine's psycopg2/no-event-loop context, mirroring the load_sync precedent"
    - "Automated demotion targets shadow_only; deprecated is operator-only — record_transition_sync rejects/forbids an automated path to deprecated"
    - "shadow_only -> active promotion is gated on 2 consecutive passing runs AND >= alpha.decay.recovery_min_observations (2000) new observations, no calendar cooldown"
    - "The three dead is_decaying/decay_detected_at/recovery_eligible_at columns are dropped from feature_ic_scores"
  artifacts:
    - path: "production/migrations/202_feature_registry_lifecycle_columns.sql"
      provides: "pre_shadow_weight + shadow lifecycle counter columns on feature_registry"
      contains: "pre_shadow_weight"
    - path: "production/migrations/203_drop_dead_feature_ic_scores_columns.sql"
      provides: "removal of is_decaying/decay_detected_at/recovery_eligible_at"
      contains: "DROP COLUMN"
    - path: "src/intelligence/feature_registry_service.py"
      provides: "record_transition_sync + is_promotion_eligible + counter updates"
      contains: "def record_transition_sync"
  key_links:
    - from: "src/intelligence/feature_registry_service.py"
      to: "feature_transition_log + feature_registry"
      via: "record_transition_sync psycopg2 transaction (with conn:)"
      pattern: "record_transition_sync"
    - from: "src/intelligence/feature_registry_service.py"
      to: "config_state alpha.decay.recovery_min_observations"
      via: "is_promotion_eligible observation floor"
      pattern: "recovery_min_observations"
---

<objective>
Amend the EXISTING feature_registry state machine to route feature lifecycle transitions — not build a
parallel one. Three amendments per LIFECYCLE-01: (1) automated ic_demotion targets shadow_only, never
deprecated (deprecated becomes operator-only); (2) add the missing shadow_only -> active promotion,
gated purely on evidence (2 consecutive passing corpus runs AND >= 2000 new independent observations
since demotion, NO calendar cooldown — Fable's reconciliation rejected cooldowns); (3) add
pre_shadow_weight and shadow-run counters as REGISTRY columns. Provide the synchronous
record_transition_sync method ic_engine's post-run hook (Plan 03) will call — the existing async
record_transition() cannot run in ic_engine's psycopg2/no-event-loop context. Drop the three dead
decay columns on feature_ic_scores (D3).

Purpose: Give Plan 03's hook a correct, blocking, transactional write path into the one authoritative
state machine, and encode the evidence-only recovery bar so recovered features re-earn their weight the
same way everything else in this codebase does.

Output: 2 migrations, new sync methods + eligibility predicate on FeatureRegistryService, extended unit
tests (TestRecordTransitionSync).

Do NOT: add is_decaying/decay-style columns to feature_ic_scores (regression to the superseded design);
add a feature_deprecations table; add a feature_transition_log.note column (intel-14 embellishment, not
in ROADMAP's locked LIFECYCLE-01 — flag as a future idea if worth capturing, do not build); add a
calendar/cooldown clock to promotion. Do NOT touch the existing async record_transition() /
_write_transition_record() — leave the async path as-is (its fire-and-forget task-loss risk is a real
bug worth a separate todo, but out of scope here).
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/143-feature-lifecycle-routing-merged-with-phase-149b-planned/143-RESEARCH.md
@.planning/phases/143-feature-lifecycle-routing-merged-with-phase-149b-planned/143-VALIDATION.md
@src/intelligence/feature_registry_service.py
@production/migrations/172_feature_registry.sql
@production/migrations/161_alpha_ic_apr_keys.sql

<interfaces>
<!-- Existing feature_registry_service.py contracts (planning read, full file) -->
- `load_sync(self, conn) -> None` (lines 100-136): psycopg2 precedent to mirror for the new sync write — cursor/execute/fetchall, blocking, no asyncio. `_LOAD_QUERY` (lines 38-42) currently SELECTs only 8 columns; extend it if the new counter columns are needed in-memory for eligibility checks.
- `_write_transition_record(self, feature_name, from_status, to_status, reason, ic_value, ic_sharpe, ic_n) -> None` (async, lines 211-276): two-statement transaction — INSERT feature_transition_log (feature_name, from_status, to_status, triggered_at, trigger_reason, ic_value, ic_sharpe, ic_n) + UPDATE feature_registry SET status. The new sync method mirrors this body using `with conn:` (psycopg2 commits on clean exit, rolls back on exception) instead of `async with conn.transaction():`, and `except Exception as error:`.
- `record_transition()` (sync wrapper, lines 278-313): async fire-and-forget — DO NOT reuse or call from ic_engine. Leave unchanged.
- feature_transition_log.trigger_reason CHECK constraint (migration 172, lines 82-85): allowed values are exactly ('ic_promotion', 'ic_demotion', 'parent_cascade', 'operator_override'). The sync method must only ever write ic_promotion / ic_demotion for automated transitions.
- feature_registry.status CHECK (migration 172, lines 40-41): ('candidate','active','shadow_only','deprecated') — shadow_only is already valid, NO CHECK change needed.
- APR key already live (migration 161): alpha.decay.recovery_min_observations = 2000 (value_type int). REUSE this exact key — do NOT create alpha.ic.decay_recovery_min_observations (duplicate).
- Cascade trigger trg_cascade_parent_deprecation (migration 172, lines 100-123) fires only on transitions TO deprecated. Since automated demotion now targets shadow_only, it correctly will NOT fire for automated demotions — verify, do not modify the trigger.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registry lifecycle-column migration + drop dead decay columns</name>
  <files>production/migrations/202_feature_registry_lifecycle_columns.sql, production/migrations/203_drop_dead_feature_ic_scores_columns.sql</files>
  <read_first>
    - production/migrations/172_feature_registry.sql (feature_registry column list, defaults, CHECK constraints)
    - production/migrations/160_ic_engine_tables.sql lines 100-110 (the three dead columns: is_decaying boolean, decay_detected_at timestamptz, recovery_eligible_at timestamptz)
    - .planning/phases/143-feature-lifecycle-routing-merged-with-phase-149b-planned/143-RESEARCH.md ("State of the Art" — confirms these three columns are never written by any INSERT, safe to drop)
    - ls production/migrations/ | sort | tail -3 (re-confirm 201 from Plan 01 is highest; use 202/203)
  </read_first>
  <action>
    Create 202_feature_registry_lifecycle_columns.sql adding four columns to feature_registry with
    ADD COLUMN IF NOT EXISTS:
      - pre_shadow_weight double precision (nullable) — the feature's ensemble weight captured at
        demotion, restored as starting weight on promotion.
      - consecutive_shadow_passes integer NOT NULL DEFAULT 0 — count of consecutive passing corpus
        runs since entering shadow_only (reset to 0 on any fail).
      - consecutive_shadow_fails integer NOT NULL DEFAULT 0 — count of consecutive failing corpus runs
        for an active feature (drives demotion; reset to 0 on any pass).
      - observations_since_demotion bigint NOT NULL DEFAULT 0 — cumulative new independent
        observations counted since demotion, compared against alpha.decay.recovery_min_observations.
    Add COMMENT ON COLUMN for each explaining it is lifecycle state (lives with status, not on
    feature_ic_scores). Do NOT alter the status CHECK constraint (shadow_only already valid).
    Create 203_drop_dead_feature_ic_scores_columns.sql with
    ALTER TABLE feature_ic_scores DROP COLUMN IF EXISTS is_decaying, DROP COLUMN IF EXISTS
    decay_detected_at, DROP COLUMN IF EXISTS recovery_eligible_at; (per D3 — these are unread dead
    columns, confirmed never written). Include a leading comment citing D3 and the research finding.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/202_feature_registry_lifecycle_columns.sql && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/203_drop_dead_feature_ic_scores_columns.sql && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM information_schema.columns WHERE table_name='feature_registry' AND column_name IN ('pre_shadow_weight','consecutive_shadow_passes','consecutive_shadow_fails','observations_since_demotion')" | grep -qx 4</automated>
  </verify>
  <acceptance_criteria>
    - `\d feature_registry` shows all four new columns with the specified types/defaults
    - `SELECT count(*) FROM information_schema.columns WHERE table_name='feature_ic_scores' AND column_name IN ('is_decaying','decay_detected_at','recovery_eligible_at')` returns 0
    - feature_registry.status CHECK constraint is unchanged (still the original 4-value set)
    - Both migrations idempotent (IF NOT EXISTS / IF EXISTS)
  </acceptance_criteria>
  <done>Registry carries lifecycle counters + pre_shadow_weight; three dead decay columns removed from feature_ic_scores; no CHECK change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: record_transition_sync + promotion-eligibility on FeatureRegistryService</name>
  <files>src/intelligence/feature_registry_service.py, tests/unit/intelligence/test_feature_registry_service.py</files>
  <read_first>
    - src/intelligence/feature_registry_service.py (FULL FILE — load_sync precedent lines 100-136, async _write_transition_record lines 211-276 to mirror, record_transition lines 278-313 to leave untouched)
    - production/migrations/172_feature_registry.sql lines 76-90 (feature_transition_log columns + trigger_reason CHECK values)
    - production/migrations/202_feature_registry_lifecycle_columns.sql (the four counter columns this method updates)
    - .planning/phases/143-feature-lifecycle-routing-merged-with-phase-149b-planned/143-VALIDATION.md (TestRecordTransitionSync expectations)
  </read_first>
  <behavior>
    - Test (TestRecordTransitionSync): record_transition_sync(conn, feature_name, from_status='active', to_status='shadow_only', reason='ic_demotion', ...) executes INSERT into feature_transition_log AND UPDATE feature_registry.status in ONE psycopg2 transaction; on a mid-transaction error both statements roll back (status unchanged, no orphan log row). Use a mock/fake psycopg2 connection that records executed statements and can raise on the second call.
    - Test: an attempt to record an AUTOMATED transition to 'deprecated' (reason in {ic_demotion,ic_promotion}) is rejected (raises ValueError / refuses to write) — deprecated is operator-only.
    - Test: record_transition_sync updates the in-memory cache status to match the DB (mirrors async method's cache write).
    - Test (is_promotion_eligible): returns True only when consecutive_shadow_passes >= 2 AND observations_since_demotion >= alpha.decay.recovery_min_observations (2000); returns False if either condition unmet. NO calendar/date input is consulted.
    - Test: is_promotion_eligible reads the observation floor from the passed config value, not a hard-coded 2000.
  </behavior>
  <action>
    Add a synchronous method record_transition_sync(self, conn, feature_name, from_status, to_status,
    reason, ic_value=None, ic_sharpe=None, ic_n=None) mirroring _write_transition_record's two-statement
    body but using a psycopg2 cursor and `with conn:` transaction semantics (commit on clean exit,
    rollback on exception), and `except Exception as error:` with a structlog
    "feature_registry_service.transition_recorded_sync" / "...transition_write_error_sync" event.
    Guard: if to_status == 'deprecated' and reason in ('ic_promotion','ic_demotion'), raise ValueError
    ("automated transitions may not target deprecated; deprecated is operator-only") BEFORE any write.
    Update the in-memory cache status after a successful commit. Add a companion method
    is_promotion_eligible(self, feature_name, recovery_min_observations) -> bool returning
    (consecutive_shadow_passes >= 2) AND (observations_since_demotion >= recovery_min_observations),
    reading both counters from the in-memory feature dict — extend _LOAD_QUERY to SELECT the four new
    columns so they are cached at load_sync time. Provide a helper to advance the counters
    (increment/reset consecutive passes/fails and add to observations_since_demotion) as part of the
    same sync transaction OR as a sibling sync method the hook can call; keep counter mutation
    transactional with the status write. Do NOT modify record_transition() / _write_transition_record()
    / load()'s async path. The number 2 (consecutive passing runs) is a fixed policy constant from
    ROADMAP LIFECYCLE-01 — encode it as a named module constant with a comment citing the ROADMAP, not
    a magic literal buried in an expression; the 2000 floor stays APR-sourced.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_feature_registry_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/intelligence/test_feature_registry_service.py::TestRecordTransitionSync -x` passes covering: transactional INSERT+UPDATE, rollback-on-error, deprecated-guard rejection, cache update
    - is_promotion_eligible tests pass for both-met / passes-only / obs-only / neither cases and confirm no date/cooldown input
    - `grep -n "async\|await" src/intelligence/feature_registry_service.py` shows the new method contains NEITHER async nor await
    - `grep -n "recovery_min_observations" src/intelligence/feature_registry_service.py` shows the floor is a parameter, not a hard-coded 2000 inside is_promotion_eligible
    - _LOAD_QUERY extended to include the four lifecycle counter columns
    - Existing async record_transition path unchanged (diff shows no edits to lines 211-313 logic)
  </acceptance_criteria>
  <done>A blocking, transactional sync transition writer + evidence-only promotion predicate exist on FeatureRegistryService, callable from ic_engine; deprecated is unreachable via automation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ic_engine (Plan 03) → FeatureRegistryService.record_transition_sync | untrusted transition requests must not corrupt the state machine |
| config_state → is_promotion_eligible | observation-floor value governs recovery gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-03 | Elevation of Privilege | automated path reaching 'deprecated' | mitigate | record_transition_sync raises ValueError if reason is ic_* and to_status is deprecated (deprecated is operator-only) |
| T-143-04 | Denial of Service (silent data loss) | fire-and-forget async transition write | mitigate | new path is a BLOCKING sync `with conn:` transaction — no event-loop teardown can drop the write; errors logged loudly |
| T-143-05 | Tampering | SQL injection via feature_name | mitigate | psycopg2 parameterized placeholders (%s) only; zero string-interpolated values |
| T-143-SC | Tampering | supply chain (pip installs) | accept | zero new packages installed this plan |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/intelligence/test_feature_registry_service.py -q` green
- `.venv/bin/pytest tests/unit/ -q` green
- Both migrations applied; four registry columns present; three dead columns gone
- Manual confirm (done-criteria, not a blocker): cascade trigger unchanged and will not fire on shadow_only demotions
</verification>

<success_criteria>
- Sync transition writer exists, is blocking + transactional, and refuses automated deprecation
- Evidence-only promotion predicate (2 passes + 2000 obs, no cooldown) implemented and APR-sourced
- Registry holds lifecycle counters + pre_shadow_weight; feature_ic_scores dead columns dropped
- Async path untouched; no new bespoke state machine
</success_criteria>

<output>
Create `.planning/phases/143-feature-lifecycle-routing-merged-with-phase-149b-planned/143-02-SUMMARY.md` when done
</output>
