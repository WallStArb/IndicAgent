# v2.10 Data Architecture Evolution — Roadmap Archive

**Shipped:** 2026-06-20
**Phases:** 123, 124, 125, 126, 127, 128, 129, 130, 131, 132, 134, 136 (12 phases)
**Phase 133 CANCELLED:** Superseded by v3.0 Intelligence Vectors architecture. IC measurement runs on `intelligence_features` (all bars), not `signal_events` (selection-biased). The binary corpus rebuild would have been irrelevant under the continuous-score paradigm.
**Plans completed:** ~58 plans
**Timeline:** 2026-06-14 → 2026-06-20 (7 days)

---

## Milestone Goal

Establish correct signal architecture (3-table: signal_events / trade_frames / trade_executions), fully externalize APR (Adaptive Parameter Registry), and deliver clean replay for ML training data. ECL boundary (Phase 123) removes all extrinsic emission suppressors. APR migration (Phase 125) externalizes all 51 constants. Signal universe hardening (Phase 126) wires all confluence-exempt signal-generation plugins and fixes zone width mechanics. 3-table migration (Phases 128-130) separates detection/hypothesis/execution and wires CounterfactualTracker. Clean replay (Phase 127) runs last so the corpus lands directly in the final 3-table schema with counterfactual_pnl_r populated from day one.

**Execution order:** v2.9 → 123 → 124 → 125 → 126 → 128 → 129 → 130 → 127 → 131 → 132 → 134 → 136

**Architecture decisions (made):**
- ECL boundary invariant: zero `no_signal()` calls based on extrinsic vectors; all extrinsic vectors are annotations
- 3-table architecture: `signal_events` / `trade_frames` / `trade_executions`; `counterfactual_pnl_r` on `trade_frames` is the ML training target
- Full plan: `docs/plans/2026-06-14-v2.10-signal-architecture-refactor.md`

---

## Phase 123: ECL Boundary Restoration — COMPLETE 2026-06-14

**Goal:** Remove all extrinsic emission suppressors across all I7 plugins. Add 5 ECL fields plus `context_features` to the signal schema. Promote `context_features` from the `_shadow` dict to a persisted top-level signal field. Collect `factor_scores` dict in all 37 plugins before compositing. Bump `SIGNAL_SCHEMA_VERSION`.

**Plans:** 3/3 complete
**Requirements:** ECL-01, ECL-02, ECL-03 (all verified)

Waves:
- Wave A — Gate removal + schema (replay-blocking, must ship first)
- Wave B — Factor score persistence (same phase, not replay-blocking)
- Wave C — Architecture doc rename and update

---

## Phase 124: Signal Universe Integrity + Cold-Start Hardening — COMPLETE 2026-06-14

**Goal:** Tighten 5 over-firing plugins (15-30%/bar) to < 3%/bar via event vs state detection. Fix ON CONFLICT cold-start contamination in intelligence_features (IS NULL guard only). Add --warmup pass to historical replay.

**Plans:** 7/7 complete
**Requirements:** QUALITY-01, QUALITY-02 (both complete; checkbox gap in REQUIREMENTS.md at close)

Waves:
- Wave A — Migration 130 (CTF columns + backfill) + feature_writer ON CONFLICT fix + --warmup flag
- Wave B — 5 plugin structural rewrites (TrendFollowing, OFIContinuation, PatternCompletion, LiquiditySweepReclaim, AnchoredVWAPReversion) + fire-rate SQL validation

---

## Phase 125: APR Full Migration — All Three Tiers — COMPLETE 2026-06-15

**Goal:** Externalize all 51 numeric constants across all three tiers to APR. Tier A (26 detection gate keys), Tier B (22 confidence weight keys), Tier C (6 zone engine geometry keys). Zero behavior change for Tiers A and B — seeds equal current hard-coded values.

**Plans:** 5/5 complete
**Requirements:** APR-01, APR-02, APR-03 (all verified)

Waves:
- Wave 1 — Migration 132: seed 10 new APR keys + validate_weights_sum helper
- Wave 2 — Wire anchored_vwap_reversion weights + 5 remaining Tier B plugins
- Wave 3 — Wire CIS gate constants + extend _THRESHOLD_KEYS

---

## Phase 126: Signal Universe Hardening — COMPLETE 2026-06-15

**Goal:** Establish a mechanically correct signal universe: all emitted signals have structurally valid zones (ATR-bounded) and full confluence annotation.

**Plans:** 6/6 complete
**Requirements:** SIGNAL-QUALITY-01, SIGNAL-QUALITY-02 (both verified)

Waves:
- Wave 0 — USDJPY anomaly diagnostic
- Wave 1 — Universal zone width gate in frame_trade() + per-asset-class APR seeds (migration 132)
- Wave 2 — Wire 8 confluence-exempt plugins, delete _I7_I6_EXEMPT, diagnose ORB/SessionExtremes
- Wave 3 — Pipeline-layer annotation (_annotate_signal), formalize zone_friction_score, bump SIGNAL_SCHEMA_VERSION to v4
- Wave 4 — IC validation + detection correctness audit; demote anti-signal/unverifiable plugins to shadow_only

---

## Phase 127: Clean Replay + Validation — COMPLETE 2026-06-17

**Goal:** Run full historical replay on corrected pipeline (Phases 123-126 in place) with `--warmup`. Produce Phase 121-02 validation report using correct methodology. Retrain calibration curves on clean corpus.

**Plans:** 3/3 complete (note: originally 4 plans; 127-00 was pre-wave diagnostics, not a blocking plan)
**Requirements:** REPLAY-01, REPLAY-02 (both complete; checkbox gap in REQUIREMENTS.md at close)

Plans:
- 127-01 — Pre-replay baseline on 3-table schema + execute clean replay with --include-rolled
- 127-02 — 3-table validation report (NO Welch's t-test, null distribution, bootstrap CI) + RCA Part VI update
- 127-03 — Trigger ml-training calibration retrain on clean corpus with pre-flight schema check

---

## Phase 128: 3-Table Schema Design and ADR — COMPLETE 2026-06-16

**Goal:** Define full 3-table schema with column types, FK constraints, index strategy. Create ADR. `counterfactual_pnl_r` is required first-class column on trade_frames.

**Plans:** 3/3 complete
**Requirements:** ARCH-01 (complete; checkbox gap in REQUIREMENTS.md at close — see verification gap notes)

Plans:
- 128-01 — ADR: signal-trade-separation-ADR.md
- 128-02 — DDL: production/migrations/137_3table_schema.sql
- 128-03 — Cleanup: delete capture_signal_features() from confidence_utils.py

---

## Phase 129: Database Migration — COMPLETE 2026-06-16

**Goal:** Create 3 new tables. Migrate signal_ledger data. Deploy signal_ledger_full view.

**Plans:** 3/3 complete
**Requirements:** MIGRATE-01 (verified)

Notes:
- 1,443,231 rows migrated; 0 failures; 0 orphaned frames
- 84-row drift (live writes continued to signal_ledger post-migration) — documented as architectural consequence; Phase 130 DROP enforces hard cutover
- signal_ledger read-only enforcement: REVOKE succeeded logically but superuser bypasses it; Phase 130 DROP TABLE is the real gate
- SIGNAL_SCHEMA_VERSION stale comment (type annotation changed str→int); no runtime impact

---

## Phase 130: Script Rewriting — COMPLETE 2026-06-16

**Goal:** Update all writers, trackers, auditors, API endpoints, historical backfill to use 3-table schema. Drop signal_ledger after 48-hour verification window.

**Plans:** 7/7 complete
**Requirements:** REWRITE-01 (verified; 31/31 must-haves)

Waves:
- Wave 1 — OPS_PREFIXES + migration 142 APR seeds (22 keys)
- Wave 2 — Repository rewrite: SignalLedgerRepository → SignalEventsRepository
- Wave 3 — signal_writer + lifecycle_writer + APR; signal_tracker bootstrap; API routes; backfill scripts (4 parallel plans)
- Wave 4 — Migration 143 DROP signal_ledger + view rename + D-15 doc updates

---

## Phase 131: Signal Generation Integrity — COMPLETE 2026-06-17

**Goal:** Fix all known signal-generation integrity bugs so the corpus rebuild in Phase 133 produces clean, unbiased training data.

**Plans:** 7/7 complete
**Status in VERIFICATION:** human_needed (2 deferred checks: ctf_score gate at exact window, and append to validation report — both informational, not blocking)

Plans:
- 131-01 — A4 diagnostic: asset_class=None for rolled-contract symbols
- 131-02 — A6 BOCPD look-ahead fix + B7 SQL fan-out fix
- 131-03 — A4 fix (asset_class injection) + trad_PrevDayLevelTest + trad_CrossAssetDivergence annotation
- 131-04 — A7 fix: _seed_last_events_from_db() + intelligence_cache DB seed
- 131-05 — trad_AnchoredVWAPReversion gate ordering fix
- 131-06 — B6 backfill integrity crash fix
- 131-07 — Verification: 1-week sample replay + 2-week plugin coverage

---

## Phase 132: Stop-Zone Geometry + APR Migration — COMPLETE 2026-06-18

**Goal:** Stops measured from actual entry price (not zone edge). Every tunable numeric constant in trade_framer.py lives in APR. stopped_at_entry rate < 5%.

**Plans:** 5/5 complete

Plans:
- 132-01 — A2 measurement: 2-week sample replay + lifecycle_replay, stopped_at_entry rate
- 132-02 — APR migration 144: 19 module-level constants → _cfg() + _THRESHOLD_KEYS
- 132-03 — APR migration 145: 12 adaptive buffer piecewise coefficients
- 132-04 — APR migration 146: 4 per-asset-class stop floor keys
- 132-05 — Verification: 1-month replay + stopped_at_entry < 5% gate + 35-key audit

---

## Phase 133: Clean Corpus Rebuild — CANCELLED

**Reason for cancellation:** Superseded by v3.0 Intelligence Vectors architecture (decided 2026-06-20). IC measurement eventually runs on `intelligence_features` (all bars, no selection bias) rather than `signal_events` (only bars where a plugin fired). The binary corpus rebuild would have produced training data for the old paradigm — irrelevant once plugins emit continuous scores. Phase A of v3.0 (IC on existing signal_events corpus, exploratory) replaces this phase.

---

## Phase 134: Signal Classification Type Safety — COMPLETE 2026-06-18

**Goal:** All classification columns type-enforced end-to-end. SignalOutcome persisted to trade_executions.outcome. EntryType Python enum replaces 15+ string literals. PostgreSQL ENUM types enforce valid values at write time.

**Plans:** 3/3 complete
**VERIFICATION:** passed (7/7 must-haves)

Plans:
- 134-01 — Persist SignalOutcome to trade_executions: add outcome column, wire lifecycle_replay, backfill
- 134-02 — EntryType enum: create Python enum, replace 15+ string literals, add DB CHECK constraint
- 134-03 — PostgreSQL ENUM type sweep: exit_reason, outcome, entry_type, signal_events.status

---

## Phase 135: Controlled Vocabulary System — DEFERRED

Deferred indefinitely (superseded by v3.0 priority). Design doc preserved at `docs/plans/2026-06-18-controlled-vocabulary-system.md`. Three DB tables (`controlled_vocabulary`, `vocabulary_group`, `vocabulary_group_member`) + VocabularyService + `/api/vocabulary/{namespace}` endpoint. First consumer: dashboard signal filter dropdowns.

---

## Phase 136: Post-Reboot System Repair — COMPLETE 2026-06-19

**Goal:** Repair system after 2026-06-18 reboot. Six work units: recover 1,343 orphaned intelligence_features rows (W1), feature_writer pre-flight schema check + JSONB write path fix (W2), intelligence pipeline graceful SIGTERM shutdown (W3), disable FVGFill plugin noise (W4), validate_signal failure reason via ValidationResult NamedTuple (W5), plugin_utils ATR label + epsilon guard (W6).

**Plans:** 6/6 complete
**VERIFICATION:** passed (14/14 must-haves; 1 human check: SIGTERM under live conditions)

Plans:
- 136-01 — W6 ATR label fix + W5 ValidationResult NamedTuple (Wave 1, parallel)
- 136-02 — W4 FVGFill disable + test sweep (Wave 1, parallel)
- 136-03 — W3 intelligence_pipeline graceful SIGTERM (Wave 1, parallel)
- 136-04 — W2 feature_writer pre-flight schema check + JSONB CTF-key exclusion (Wave 2)
- 136-05 — W1 historical replay to recover 1,343 orphaned rows (Wave 3)
- 136-06 — Migration 130 Statement 3 JSONB cleanup (Wave 4)
