---
phase: 27-signal-lifecycle-stream-events
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified: [services/signal_lifecycle_service.py]
autonomous: true
requirements: [SLES-01]
user_setup: []

must_haves:
  truths:
    - "Normal active signal exit publishes terminal event to stream"
    - "Shadow signal (regime_suppressed) exit publishes terminal event to stream"
    - "Both exit paths call _publish_terminal_event() with correct parameters"
  artifacts:
    - path: "services/signal_lifecycle_service.py"
      provides: "Terminal event calls in both exit paths"
      min_lines: 10
  key_links:
    - from: "signal_lifecycle_service._evaluate_signals_against_bar() (active path)"
      to: "_publish_terminal_event()"
      via: "transition.exit_reason branch"
      pattern: "_publish_terminal_event\\(signal_id=.*transition.exit_price"
    - from: "signal_lifecycle_service._evaluate_signals_against_bar() (shadow path)"
      to: "_publish_terminal_event()"
      via: "regime_suppressed exit branch"
      pattern: "regime_suppressed.*_publish_terminal_event"
---

<objective>
Wire `_publish_terminal_event()` calls into both signal exit paths (normal active signals and shadow regime-suppressed signals) in `signal_lifecycle_service.py`.

Purpose: Ensure that every signal termination — whether from an active trade exit or a shadow signal tracking for gate validation — publishes a terminal lifecycle event to the stream. This makes the stream a complete event log for both live and counterfactual signal lifecycles.

Output: Terminal event publication wired into both exit code paths with asyncio.create_task() for non-blocking async calls.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-signal-lifecycle-stream-events/27-01-SUMMARY.md
@docs/plans/2026-03-06-signal-lifecycle-stream-events-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire terminal event into active signal exit path</name>
  <files>services/signal_lifecycle_service.py</files>
  <action>
    In `_evaluate_signals_against_bar()` method, in the normal (non-regime_suppressed) signal evaluation path:

    Locate the `elif transition.exit_reason:` block (line ~448-495) where active signals transition to exit state.

    After DB update completes via `update_signal_status()` and before continuing to next signal, add:

    ```python
    # Publish terminal event to signals stream for dashboard resolved state
    asyncio.create_task(self._publish_terminal_event(
        signal_id=sid,
        symbol=symbol,
        timeframe=timeframe,
        outcome=outcome,
        exit_price=transition.exit_price,
        bar_ts=bar_time.isoformat(),
    ))
    ```

    This must be placed AFTER `self._mae.pop(sid, None)` cleanup lines but BEFORE the `continue` that moves to next signal.

    Verify the call uses `outcome` variable (already resolved in the block) and `transition.exit_price` (available from transition object).
  </action>
  <verify>
    <automated>grep -n "_publish_terminal_event" services/signal_lifecycle_service.py | grep -c "asyncio.create_task"</automated>
  </verify>
  <done>Terminal event publication called with asyncio.create_task in active signal exit path, parameters match available variables in scope</done>
</task>

<task type="auto">
  <name>Task 2: Wire terminal event into shadow signal exit path</name>
  <files>services/signal_lifecycle_service.py</files>
  <action>
    In `_evaluate_signals_against_bar()` method, in the `if status == "regime_suppressed":` block (line ~267-402):

    Locate the shadow signal exit handling block where `transition.exit_reason` is truthy (line ~328-402).

    After DB update via `update_signal_status()` and after cleanup of in-memory state (`self._mae.pop(sid, None)` etc.), add:

    ```python
    # Publish terminal event to signals stream for dashboard resolved state
    asyncio.create_task(self._publish_terminal_event(
        signal_id=sid,
        symbol=symbol,
        timeframe=timeframe,
        outcome=outcome,
        exit_price=transition.exit_price,
        bar_ts=bar_time.isoformat(),
    ))
    ```

    This must be placed AFTER the regime_suppressed DB update but BEFORE the `continue` at line ~402 that skips normal pending/active paths.

    Note: The design doc (lines 385-392) already has this call — verify it exists and is correct. If missing, add it. If present, ensure parameters match variable names in scope.
  </action>
  <verify>
    <automated>grep -A 5 "regime_suppressed.*exit" services/signal_lifecycle_service.py | grep "_publish_terminal_event"</automated>
  </verify>
  <done>Terminal event publication called with asyncio.create_task in shadow signal exit path (if not already present), or verified existing call is correct</done>
</task>

</tasks>

<verification>
- grep shows 2 calls to _publish_terminal_event() via asyncio.create_task (one per exit path)
- Both calls pass symbol, timeframe, outcome, exit_price, and bar_ts correctly
- Code placement is after DB updates and before continue statements
</verification>

<success_criteria>
- Both active and shadow signal exit paths publish terminal events
- asyncio.create_task used for non-blocking async calls
- No duplicate calls within same exit path
- Service restart verifies terminal events fire correctly (manual verification)
</success_criteria>

<output>
After completion, create `.planning/phases/27-signal-lifecycle-stream-events/27-02-SUMMARY.md`
</output>
