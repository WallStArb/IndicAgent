---
phase: 27-signal-lifecycle-stream-events
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [services/signal_lifecycle_service.py, tests/unit/service_tests/test_signal_lifecycle_service.py]
autonomous: true
requirements: [SLES-01]
user_setup: []

must_haves:
  truths:
    - "Signal lifecycle service publishes terminal events to Redis stream on every signal exit"
    - "Terminal event contains direction=0, signal_id, status, outcome, exit_price, symbol, timeframe, timestamp"
    - "Unit tests verify terminal event publication for all 8 outcome classes"
  artifacts:
    - path: "services/signal_lifecycle_service.py"
      provides: "_publish_terminal_event() helper method"
      min_lines: 30
    - path: "tests/unit/service_tests/test_signal_lifecycle_service.py"
      provides: "Unit tests for terminal event publishing"
      min_lines: 50
  key_links:
    - from: "signal_lifecycle_service._publish_terminal_event()"
      to: "signals:SYMBOL:TF:aggregated"
      via: "redis_client.xadd()"
      pattern: "xadd.*signals:.*aggregated.*direction.*0"
    - from: "signal_lifecycle_service._evaluate_signals_against_bar()"
      to: "_publish_terminal_event()"
      via: "exit path calls terminal event publisher"
      pattern: "_publish_terminal_event\\(signal_id,.*\\)"
---

<objective>
Implement `_publish_terminal_event()` helper in `signal_lifecycle_service.py` with unit tests to verify terminal signal lifecycle events are published to the aggregated signal stream.

Purpose: Create the mechanism for signal lifecycle service to broadcast signal termination events (stop-out, target hit, expiry) to the dashboard and other downstream consumers. This completes the event stream so signals are not just born but also closed in the stream record.

Output: Terminal event publication method with full unit test coverage for all 8 outcome classes.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-03-06-signal-lifecycle-stream-events-design.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement _publish_terminal_event() helper method</name>
  <files>services/signal_lifecycle_service.py</files>
  <behavior>
    - Test 1: Method publishes to correct Redis stream key (signals:SYMBOL:TF:aggregated)
    - Test 2: Payload includes direction="0" sentinel value
    - Test 3: Payload includes all required fields: signal_id, status, outcome, exit_price, symbol, timeframe, timestamp
    - Test 4: exit_price handles None case (empty string in payload)
    - Test 5: Uses env_prefix from Settings (supports dev/production environments)
  </behavior>
  <action>
    Implement `_publish_terminal_event()` async method in `SignalLifecycleService` class:
    - Signature: `async def _publish_terminal_event(self, signal_id: str, symbol: str, timeframe: str, outcome: str, exit_price: float | None, bar_ts: str) -> None`
    - Build stream key using `sk_signals_aggregated(self.env_prefix, symbol, timeframe)`
    - Create payload dict with string values (Redis requirement):
      * direction: "0" (sentinel for terminal state)
      * signal_id: signal_id string
      * status: outcome string
      * outcome: outcome string
      * exit_price: str(exit_price) if exit_price is not None else ""
      * symbol: symbol
      * timeframe: timeframe
      * timestamp: bar_ts
    - Call `await redis_client.xadd(stream_key, payload, maxlen=200, approximate=True)`
    - Add warning log on exception (already exists in design)
    - Guard against redis_client being None (early-return)
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/service_tests/test_signal_lifecycle_service.py::test_publish_terminal_event -xvs</automated>
  </verify>
  <done>_publish_terminal_event() method implemented with Redis xadd call, all required fields in payload, error handling in place, and unit tests passing</done>
</task>

</tasks>

<verification>
- Unit tests verify terminal event payload structure and Redis publication
- All 8 outcome classes can be published without errors
- Method correctly handles None exit_price (empty string in payload)
</verification>

<success_criteria>
- `_publish_terminal_event()` method exists and is callable
- Method publishes correct payload to `signals:SYMBOL:TF:aggregated` stream
- Unit tests pass for all outcome classes
- No regressions in existing signal_lifecycle_service tests
</success_criteria>

<output>
After completion, create `.planning/phases/27-signal-lifecycle-stream-events/27-01-SUMMARY.md`
</output>
