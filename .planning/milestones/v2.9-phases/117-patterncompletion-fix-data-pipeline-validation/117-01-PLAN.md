---
phase: 117-patterncompletion-fix-data-pipeline-validation
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [VAL-01]
files_modified:
  - services/feature_writer.py
  - tests/unit/services/test_feature_writer_column_mapping.py
autonomous: true

must_haves:
  truths:
    - "After deploy, intelligence_features.pattern_detections contains dt_db_confidence on live bars (non-zero row count within 1 hour)"
    - "intelligence_features.regime_features contains I3 structure fields (swing_high, nearest_resistance)"
    - "intelligence_features.confluence_scores contains I4 context fields (GARCH/Kalman/AVWAP)"
    - "A unit test pins the tier-to-column mapping so the swap cannot silently regress"
  artifacts:
    - path: "services/feature_writer.py"
      provides: "Corrected tier-to-column mapping in _record_to_insert_params (i5->pattern_detections, i3->regime_features, i4->confluence_scores)"
      contains: "event.i5.model_dump(exclude_none=True),  # $10 pattern_detections"
    - path: "tests/unit/services/test_feature_writer_column_mapping.py"
      provides: "Regression test asserting $10/$11/$12 carry i5/i3/i4 respectively"
      contains: "_record_to_insert_params"
  key_links:
    - from: "services/feature_writer.py:_record_to_insert_params"
      to: "_INSERT_FEATURE_SQL columns pattern_detections/regime_features/confluence_scores"
      via: "positional tuple args $10/$11/$12"
      pattern: "event\\.i5\\.model_dump.*pattern_detections"
---

<objective>
Fix the tier-to-column mapping bug in `services/feature_writer.py` where I5 pattern data is written to `confluence_scores`, I3 structure to `pattern_detections`, and I4 context to `regime_features`. After the fix, each tier's data lands in its semantically-correct JSONB column.

Purpose: Unblocks the FeatureParityAuditor (Wave 2) and all downstream analytics on `pattern_detections`. The in-process signal generation was never broken (reads `frames["i5"]` directly); this fix corrects only DB persistence.
Output: 3-line argument swap in `_record_to_insert_params` plus a regression unit test pinning the mapping.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-patterncompletion-fix-data-pipeline-validation/117-RESEARCH.md
@.planning/phases/117-patterncompletion-fix-data-pipeline-validation/117-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap i3/i4/i5 args to correct JSONB columns in _record_to_insert_params</name>
  <files>services/feature_writer.py</files>
  <read_first>
    - services/feature_writer.py (read _INSERT_FEATURE_SQL column declarations near lines 63-97 AND _record_to_insert_params lines 166-215 to confirm positional order $10/$11/$12)
    - .planning/phases/117-patterncompletion-fix-data-pipeline-validation/117-RESEARCH.md (Pattern 1 — verified correct mapping)
    - src/intelligence/schemas.py (confirm I3=structure, I4=context, I5=patterns field ownership)
  </read_first>
  <action>
    In `services/feature_writer.py` `_record_to_insert_params()`, the return tuple currently has at positions $10/$11/$12: `event.i3.model_dump(exclude_none=True)` (# $10 pattern_detections), `event.i4.model_dump(exclude_none=True)` (# $11 regime_features), `event.i5.model_dump(exclude_none=True)` (# $12 confluence_scores).

    Reorder these three lines so the data matches the column name:
    - $10 pattern_detections gets `event.i5.model_dump(exclude_none=True)` (I5Patterns: dt_db_confidence, hs_confidence, tri_confidence)
    - $11 regime_features gets `event.i3.model_dump(exclude_none=True)` (I3Structure: swing_high, nearest_resistance, trend structure, session levels)
    - $12 confluence_scores gets `event.i4.model_dump(exclude_none=True)` (I4Context: GARCH, Kalman, AVWAP, VP, SessionContext)

    Update the inline comment on each of the three lines to state the new tier and example fields. Do NOT touch any other tuple position ($8 i1, $9 market_context, $13 smc, $14 i6 remain unchanged). The tuple stays 32 elements and the SQL is unchanged.
  </action>
  <verify>
    grep -n "pattern_detections\|regime_features\|confluence_scores" services/feature_writer.py shows event.i5 on the pattern_detections line, event.i3 on regime_features, event.i4 on confluence_scores.
  </verify>
  <acceptance_criteria>
    - Source assertion: line `event.i5.model_dump(exclude_none=True),  # $10 pattern_detections` present in services/feature_writer.py
    - Source assertion: line `event.i3.model_dump(exclude_none=True),  # $11 regime_features` present
    - Source assertion: line `event.i4.model_dump(exclude_none=True),  # $12 confluence_scores` present
    - Behavior assertion: return tuple from _record_to_insert_params still has exactly 32 elements (no element added or removed)
    - CLI: `.venv/bin/ruff check services/feature_writer.py` exits 0
  </acceptance_criteria>
  <done>The three tier args at $10/$11/$12 carry i5/i3/i4 respectively; tuple length unchanged at 32; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression unit test pinning the tier-to-column mapping</name>
  <files>tests/unit/services/test_feature_writer_column_mapping.py</files>
  <read_first>
    - services/feature_writer.py (_record_to_insert_params signature and BarIntelligenceRecord/IntelligenceEvent construction; check existing test imports/fixtures)
    - tests/unit/services/ (find an existing feature_writer test for fixture/import conventions; if none, mirror another tests/unit/services test header)
    - src/intelligence/schemas.py (IntelligenceEvent and tier sub-model field names to populate sentinel values)
  </read_first>
  <action>
    Create `tests/unit/services/test_feature_writer_column_mapping.py`. Build a minimal `BarIntelligenceRecord` whose `intelligence` event has distinguishable sentinel values per tier: set an I5-only field (`dt_db_confidence`) on `event.i5`, an I3-only field (`swing_high` or another confirmed I3Structure field) on `event.i3`, and an I4-only field (a confirmed I4Context field e.g. a GARCH/Kalman/AVWAP key) on `event.i4`.

    Call `_record_to_insert_params(record)` and assert by positional index (0-indexed positions 9, 10, 11 map to $10, $11, $12):
    - params[9] (pattern_detections) contains the I5 sentinel key `dt_db_confidence`
    - params[10] (regime_features) contains the I3 sentinel key
    - params[11] (confluence_scores) contains the I4 sentinel key
    - len(params) == 32

    Use real schema field names from src/intelligence/schemas.py; do not invent fields. If constructing a full BarIntelligenceRecord is heavy, populate only required fields and the three tier sub-models.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/services/test_feature_writer_column_mapping.py -v passes.
  </verify>
  <acceptance_criteria>
    - Test command: `.venv/bin/pytest tests/unit/services/test_feature_writer_column_mapping.py -q` exits 0 with at least one passing test
    - Behavior assertion: test fails if the $10/$11/$12 args are swapped back (the test asserts params[9] has the I5 key, not the I3 key)
    - Source assertion: test file references `_record_to_insert_params` and asserts `len(params) == 32`
    - CLI: `.venv/bin/ruff check tests/unit/services/test_feature_writer_column_mapping.py` exits 0
  </acceptance_criteria>
  <done>Regression test passes and would fail if the tier-to-column mapping regresses.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/services/test_feature_writer_column_mapping.py -q` green
- `.venv/bin/ruff check services/feature_writer.py tests/unit/services/test_feature_writer_column_mapping.py` clean
- Post-deploy DB confirmation (operator, after pipeline restart): `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM intelligence_features WHERE pattern_detections ? 'dt_db_confidence' AND ts >= NOW() - INTERVAL '1 hour'"` returns non-zero
</verification>

<success_criteria>
VAL-01 satisfied: I5 pattern fields (dt_db_confidence, hs_confidence, tri_confidence) persist into pattern_detections on live bars; I3 -> regime_features; I4 -> confluence_scores; regression test pins the mapping.
</success_criteria>

<output>
After completion, create `.planning/phases/117-patterncompletion-fix-data-pipeline-validation/117-01-SUMMARY.md`
</output>
