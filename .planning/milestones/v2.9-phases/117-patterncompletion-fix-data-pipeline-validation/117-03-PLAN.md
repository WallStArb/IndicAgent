---
phase: 117-patterncompletion-fix-data-pipeline-validation
plan: 03
type: execute
wave: 2
depends_on: ["117-01"]
requirements: [VAL-03]
files_modified:
  - src/observability/metrics.py
  - services/confidence_calibration_monitor.py
  - production/systemd/indicagent-confidence-calibration-monitor.service
  - production/systemd/indicagent-confidence-calibration-monitor.timer
  - services/service_auditor.py
  - tests/unit/services/test_confidence_calibration_monitor.py

autonomous: true

must_haves:
  truths:
    - "ConfidenceCalibrationMonitor computes CORR(cis_score, was_selected::int) per setup_plugin from signal_ledger over the last 7 days, gated at N>=100"
    - "It publishes a per-setup OTel gauge signal_confidence_calibration{setup_plugin}"
    - "It bumps an alert counter when calibration < 0.3 with N>=100, framed as 'not predictive of aggregator selection'"
    - "It emits job_completed_total{job=confidence-calibration-monitor, status} and flushes metrics before exit"
  artifacts:
    - path: "src/observability/metrics.py"
      provides: "SIGNAL_CONFIDENCE_CALIBRATION gauge + CONFIDENCE_CALIBRATION_ALERTS_TOTAL counter (module-level, meter 'indicagent.metrics')"
      contains: "signal_confidence_calibration"
    - path: "services/confidence_calibration_monitor.py"
      provides: "Oneshot per-setup confidence-calibration monitor with OTel metrics"
      contains: "confidence-calibration-monitor"
    - path: "production/systemd/indicagent-confidence-calibration-monitor.timer"
      provides: "Timer triggering the monitor"
      contains: "Unit=indicagent-confidence-calibration-monitor.service"
    - path: "production/systemd/indicagent-confidence-calibration-monitor.service"
      provides: "Type=oneshot service invoking the monitor"
      contains: "confidence_calibration_monitor.py"
  key_links:
    - from: "services/confidence_calibration_monitor.py"
      to: "signal_ledger.cis_score / was_selected"
      via: "asyncpg CORR() aggregate grouped by setup_plugin"
      pattern: "CORR\\(cis_score, was_selected"
    - from: "services/confidence_calibration_monitor.py"
      to: "src.observability.metrics.SIGNAL_CONFIDENCE_CALIBRATION"
      via: "import + set(calibration, {setup_plugin})"
      pattern: "SIGNAL_CONFIDENCE_CALIBRATION"
---

<objective>
Build `ConfidenceCalibrationMonitor`: a timer-triggered oneshot that measures, per setup_plugin, the correlation between `cis_score` and aggregator selection (`was_selected`), publishing it as an OTel gauge and alerting when a setup's confidence is non-predictive (correlation < 0.3 at N>=100).

Purpose: Make per-setup confidence-formula quality visible in Grafana. The correlation measures aggregator-selection predictiveness (NOT profitability) — `was_selected` is downstream of `cis_score`, so framing must say "confidence not predictive of aggregator selection."
Output: New module-level OTel instruments in metrics.py, new oneshot service, systemd .service + .timer pair, registry entry, unit test for the alert threshold logic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-patterncompletion-fix-data-pipeline-validation/117-RESEARCH.md
@.planning/phases/117-patterncompletion-fix-data-pipeline-validation/117-PATTERNS.md
@services/shadow_auditor.py
@src/observability/metrics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OTel instruments to metrics.py + create ConfidenceCalibrationMonitor oneshot service</name>
  <files>src/observability/metrics.py, services/confidence_calibration_monitor.py</files>
  <read_first>
    - src/observability/metrics.py (lines 30-90 for point_gauge/create_counter helpers + flush_and_shutdown_metrics + JOB_COMPLETED_TOTAL; lines 267-277 for the SHADOW_* gauge/counter block to mirror exactly)
    - services/shadow_auditor.py (full file — imports including `from src.observability.metrics import (...)`, _amain/main structure, asyncpg pool, JOB_COMPLETED_TOTAL + flush_and_shutdown_metrics, how SHADOW_* instruments are imported and used)
    - .planning/phases/117-patterncompletion-fix-data-pipeline-validation/117-PATTERNS.md (confidence_calibration_monitor.py section — exact query and framing)
    - .planning/phases/117-patterncompletion-fix-data-pipeline-validation/117-RESEARCH.md (Pattern 4 — circularity caveat, correct alert framing)
  </read_first>
  <action>
    Step A — Add instruments to `src/observability/metrics.py`, mirroring the SHADOW_* block (lines 267-277) so they live under the canonical `indicagent.metrics` meter (NOT an inline `get_meter("indicagent")`):
    - Add `SIGNAL_CONFIDENCE_CALIBRATION = point_gauge("signal_confidence_calibration", "Per-setup correlation between cis_score and aggregator selection (was_selected)")` using the existing `point_gauge()` helper.
    - Add `CONFIDENCE_CALIBRATION_ALERTS_TOTAL = _meter.create_counter("confidence_calibration_alerts_total", description="Per-setup low-calibration alerts (correlation < 0.3 at N>=100)")` mirroring the SHADOW_TAIL_RISK_BLOCKED counter definition style.
    Place both near the SHADOW_* block (or in a clearly-labeled `# Confidence calibration monitor (Phase 117)` comment section).

    Step B — Create `services/confidence_calibration_monitor.py`, mirroring `services/shadow_auditor.py` structure:
    - Imports: same import block as shadow_auditor (`_path_bootstrap`, asyncio, structlog, asyncpg, `from src.config.settings import Settings`, `from src.core.database_manager import create_pool as create_db_pool`) plus `from src.observability.metrics import (CONFIDENCE_CALIBRATION_ALERTS_TOTAL, JOB_COMPLETED_TOTAL, SIGNAL_CONFIDENCE_CALIBRATION, flush_and_shutdown_metrics)`. Do NOT call `_otel_metrics.get_meter(...)` inline — instruments come from metrics.py only.
    - Module constants: `_MIN_N = 100`, `_CALIBRATION_ALERT_THRESHOLD = 0.3`.
    - `async def _run_audit(pool)`: `async with pool.acquire() as conn:` fetch rows from `SELECT setup_plugin, CORR(cis_score, was_selected::int)::float AS calibration, COUNT(*) AS n FROM signal_ledger WHERE timestamp >= NOW() - INTERVAL '7 days' AND NOT is_shadow AND cis_score IS NOT NULL GROUP BY setup_plugin HAVING COUNT(*) >= 100 ORDER BY calibration ASC`. For each row: read `row["setup_plugin"]`, `row["calibration"]`, `row["n"]`. Call `SIGNAL_CONFIDENCE_CALIBRATION.set(calibration if calibration is not None else 0.0, {"setup_plugin": plugin})`. If `calibration is not None and calibration < _CALIBRATION_ALERT_THRESHOLD and n >= _MIN_N`: `CONFIDENCE_CALIBRATION_ALERTS_TOTAL.add(1, {"setup_plugin": plugin})` and `logger.warning("confidence_calibration.not_predictive_of_selection", plugin=plugin, calibration=round(calibration,4), n=n)`. Otherwise log `confidence_calibration.result` at info level. Return the processed rows (so it is unit-testable).
    - `_amain()` and `main()` identical structure to shadow_auditor, but the `job` label MUST be exactly `"confidence-calibration-monitor"` (matching the timer unit %n suffix). Exception variable `error`. `flush_and_shutdown_metrics()` in finally.

    Alert log/metric framing MUST use "not predictive of aggregator selection", never "not predictive of profitability" (the correlation is circular per RESEARCH.md Pattern 4). asyncpg returns dicts/datetimes natively — no json.loads, no manual timestamp parsing.
  </action>
  <verify>
    .venv/bin/python -c "import ast; ast.parse(open('services/confidence_calibration_monitor.py').read())" succeeds; `.venv/bin/python -c "from src.observability.metrics import SIGNAL_CONFIDENCE_CALIBRATION, CONFIDENCE_CALIBRATION_ALERTS_TOTAL"` imports clean; grep confirms job label, CORR query, gauge name.
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/observability/metrics.py` contains `SIGNAL_CONFIDENCE_CALIBRATION = point_gauge(` and `CONFIDENCE_CALIBRATION_ALERTS_TOTAL = _meter.create_counter(`
    - Source assertion: `services/confidence_calibration_monitor.py` imports `SIGNAL_CONFIDENCE_CALIBRATION` and `CONFIDENCE_CALIBRATION_ALERTS_TOTAL` from `src.observability.metrics` and contains NO `get_meter(` call
    - Source assertion: file contains `"job": "confidence-calibration-monitor"` exactly twice
    - Source assertion: file contains `CORR(cis_score, was_selected::int)` and `HAVING COUNT(*) >= 100`
    - Source assertion: file contains constant `_CALIBRATION_ALERT_THRESHOLD = 0.3`
    - Source assertion: alert log key uses "selection" framing (e.g. `confidence_calibration.not_predictive_of_selection`), not "profitability"
    - CLI: `.venv/bin/ruff check src/observability/metrics.py services/confidence_calibration_monitor.py` exits 0
  </acceptance_criteria>
  <done>Instruments live in metrics.py under the canonical meter; monitor parses, imports them, computes per-setup correlation, publishes gauge, alerts below 0.3 at N>=100 with correct framing, emits job completion, ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create systemd .service + .timer and register in _DAG_ORDER</name>
  <files>production/systemd/indicagent-confidence-calibration-monitor.service, production/systemd/indicagent-confidence-calibration-monitor.timer, services/service_auditor.py</files>
  <read_first>
    - production/systemd/indicagent-shadow-auditor.service (unit template)
    - production/systemd/indicagent-shadow-auditor.timer (timer template)
    - services/service_auditor.py (_DAG_ORDER, _LAG_THRESHOLDS, _AGENT_ID_TO_UNIT — confirm timer oneshots are registered by BARE service name with NO `.timer` suffix, e.g. `"indicagent-shadow-auditor"`)
  </read_first>
  <action>
    Create `production/systemd/indicagent-confidence-calibration-monitor.service`: `[Unit]` Description="IndicAgent Confidence Calibration Monitor", `After=network.target indicagent-infrastructure.target`, `Requires=indicagent-infrastructure.target`; `[Service]` `Type=oneshot`, `WorkingDirectory=/home/bg/dev/indicagent`, `ExecStart=/home/bg/dev/indicagent/.venv/bin/python services/confidence_calibration_monitor.py`, `Environment=PYTHONUNBUFFERED=1`; `[Install]` `WantedBy=multi-user.target`.

    Create `production/systemd/indicagent-confidence-calibration-monitor.timer`: `[Unit]` Description="Confidence Calibration Monitor Timer — every 30 minutes"; `[Timer]` `OnCalendar=*:0/30`, `Persistent=true`, `Unit=indicagent-confidence-calibration-monitor.service`; `[Install]` `WantedBy=timers.target`. (30-minute interval: the query spans 7 days, so high frequency is unnecessary — per RESEARCH.md/PATTERNS.md guidance.)

    In `services/service_auditor.py` add the BARE service name `"indicagent-confidence-calibration-monitor"` (NO `.timer` suffix) to `_DAG_ORDER`, matching the established convention for all existing timer-triggered oneshots (e.g. `"indicagent-shadow-auditor"`). Use ordering value 8 (or the next slot after the feature-parity-auditor entry). Follow the same lag-threshold convention used for other timer oneshots (e.g. shadow-auditor) — do not add a lag threshold those units lack.
  </action>
  <verify>
    grep confirms the bare-name _DAG_ORDER entry and timer Unit reference; systemd-analyze verify on the timer passes if available.
  </verify>
  <acceptance_criteria>
    - Source assertion: timer contains `Unit=indicagent-confidence-calibration-monitor.service` and an `OnCalendar=` line
    - Source assertion: service contains `Type=oneshot` and `ExecStart=.../services/confidence_calibration_monitor.py`
    - Source assertion: `services/service_auditor.py` contains the BARE name `"indicagent-confidence-calibration-monitor"` (NOT `"indicagent-confidence-calibration-monitor.timer"`)
    - CLI: `grep -c '"indicagent-confidence-calibration-monitor"' services/service_auditor.py` returns >= 1
    - CLI: `grep -c '"indicagent-confidence-calibration-monitor.timer"' services/service_auditor.py` returns 0
  </acceptance_criteria>
  <done>Systemd pair created, monitor registered in _DAG_ORDER by bare service name following the shadow-auditor convention.</done>
</task>

<task type="auto">
  <name>Task 3: Unit test the alert threshold logic</name>
  <files>tests/unit/services/test_confidence_calibration_monitor.py</files>
  <read_first>
    - services/confidence_calibration_monitor.py (the _run_audit + threshold constants from Task 1)
    - tests/unit/services/ (async test conventions, asyncpg fetch mocking)
  </read_first>
  <action>
    Create `tests/unit/services/test_confidence_calibration_monitor.py`. Mock the asyncpg pool/conn so `conn.fetch(...)` returns a list of fake row dicts. Patch the module-level `SIGNAL_CONFIDENCE_CALIBRATION` gauge and `CONFIDENCE_CALIBRATION_ALERTS_TOTAL` counter (monkeypatch on the module attribute) to spy on calls. Write tests:
    - `test_alert_on_low_calibration`: one row with calibration=0.1, n=150. Assert the alert counter `.add` was called once with `{"setup_plugin": <plugin>}`.
    - `test_no_alert_on_high_calibration`: one row with calibration=0.8, n=150. Assert the alert counter `.add` was NOT called; assert the gauge `.set` WAS called with 0.8.
    - `test_none_calibration_fallback`: one row with calibration=None, n=150. Assert gauge `.set` is called with 0.0 fallback and no alert.
    Use `pytest.mark.asyncio`.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/services/test_confidence_calibration_monitor.py -v passes.
  </verify>
  <acceptance_criteria>
    - Test command: `.venv/bin/pytest tests/unit/services/test_confidence_calibration_monitor.py -q` exits 0
    - Behavior assertion: test_alert_on_low_calibration asserts the alert counter was incremented exactly once for calibration < 0.3 at n>=100
    - Behavior assertion: test_no_alert_on_high_calibration asserts no alert increment and gauge set to 0.8
    - CLI: `.venv/bin/ruff check tests/unit/services/test_confidence_calibration_monitor.py` exits 0
  </acceptance_criteria>
  <done>Alert threshold logic unit-tested for low/high/none paths; tests green; ruff clean.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/services/test_confidence_calibration_monitor.py -q` green
- `.venv/bin/ruff check src/observability/metrics.py services/confidence_calibration_monitor.py tests/unit/services/test_confidence_calibration_monitor.py` clean
- `grep '"indicagent-confidence-calibration-monitor"' services/service_auditor.py` returns a bare-name match (no `.timer` suffix)
- Operator (post-install): `sudo systemctl start indicagent-confidence-calibration-monitor.service` then `tail logs/confidence_calibration_monitor.log` shows per-setup calibration results
</verification>

<success_criteria>
VAL-03 satisfied: ConfidenceCalibrationMonitor computes CORR(cis_score, was_selected) per setup, publishes signal_confidence_calibration{setup_plugin}, alerts when correlation < 0.3 at N>=100 with correct (selection, not profitability) framing, emits job_completed_total. OTel instruments live in metrics.py under the canonical meter; _DAG_ORDER uses the bare service name.
</success_criteria>

<output>
After completion, create `.planning/phases/117-patterncompletion-fix-data-pipeline-validation/117-03-SUMMARY.md`
</output>
</content>
</invoke>
