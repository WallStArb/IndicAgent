---
phase: 122-production-hardening
plan: 12
type: execute
wave: 1
depends_on: [11]
files_modified:
  - src/intelligence/services/hmm_trainer.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "hmm_trainer.py SQL uses technical_indicators instead of i1 for JSONB column access"
  artifacts: []
  key_links:
    - from: "src/intelligence/services/hmm_trainer.py"
      to: "intelligence_features.technical_indicators"
      via: "SQL JSONB accessor"
      pattern: "technical_indicators->>'rsi_14'"
---

<objective>
Fix the SQL in hmm_trainer.py that references the renamed i1 column.

Migration 126 (plan 11) renames i1 → technical_indicators in intelligence_features. hmm_trainer.py
was not included in plan 11 but has SQL that directly queries `i1->>'rsi_14'` etc. — it will fail
with "column i1 does not exist" after migration 126 runs.
</objective>

<tasks>

<task id="1" name="Fix hmm_trainer.py SQL">
In `src/intelligence/services/hmm_trainer.py` lines 164 and 176-179:

- Line 164 comment: `i1 JSONB` → `technical_indicators JSONB`
- Line 176: `(i1->>'rsi_14')::float` → `(technical_indicators->>'rsi_14')::float`
- Line 177: `(i1->>'adx_14')::float` → `(technical_indicators->>'adx_14')::float`
- Line 178: `(i1->>'atr_14')::float` → `(technical_indicators->>'atr_14')::float`
- Line 179: `(i1->>'macd_histogram_12_26_9')::float` → `(technical_indicators->>'macd_histogram_12_26_9')::float`

Verify no i1/i3/i4/i5 column refs remain in SQL across the codebase:
```bash
grep -rn "i1->>\|i3->>\|i4->>\|i5->>" src/ services/ production/scripts/ --include="*.py"
```

Run unit tests:
```bash
.venv/bin/pytest tests/unit/ -q --tb=short 2>&1 | tail -10
```

Commit: `fix(122-12): update hmm_trainer SQL to use technical_indicators column`
</task>

</tasks>
