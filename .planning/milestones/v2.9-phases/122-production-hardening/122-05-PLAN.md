---
phase: 122-production-hardening
plan: 05
type: execute
wave: 3
depends_on: [03, 04]
files_modified:
  - production/migrations/125_rename_intelligence_features_columns.sql
  - services/feature_writer.py
  - production/scripts/run_historical_pipeline.py
  - src/api/routes/signals.py
  - src/api/routes/features.py
  - src/api/routes/narrative.py
  - src/intelligence/trading/zone_engine.py
autonomous: true

must_haves:
  truths:
    - "intelligence_features table has columns named i1, i3, i4, i5 — technical_indicators/pattern_detections/regime_features/confluence_scores no longer exist"
    - "feature_writer _INSERT_FEATURE_SQL references i1/i3/i4/i5 — no old column names appear"
    - "run_historical_pipeline _INSERT_FEATURE_SYNC_SQL and _load_precomputed_features SELECT reference i1/i3/i4/i5"
    - "API routes signals.py, features.py, narrative.py reference i1/i3/i4/i5 in all SQL queries and row access"
    - "zone_engine collect_candidates calls get_atr_with_floor(features, symbol) instead of get_atr with hardcoded 0.5 fallback"
    - "pytest exits 0"
  artifacts:
    - path: "production/migrations/125_rename_intelligence_features_columns.sql"
      provides: "4 RENAME COLUMN statements (technical_indicators→i1, pattern_detections→i5, regime_features→i3, confluence_scores→i4)"
      contains: "RENAME COLUMN technical_indicators TO i1"
    - path: "services/feature_writer.py"
      provides: "_INSERT_FEATURE_SQL using new column names i1/i3/i4/i5"
    - path: "production/scripts/run_historical_pipeline.py"
      provides: "_INSERT_FEATURE_SYNC_SQL and _load_precomputed_features using i1/i3/i4/i5"
    - path: "src/api/routes/signals.py"
      provides: "SQL queries and row accessors using i1/i3/i4/i5"
    - path: "src/api/routes/features.py"
      provides: "SQL queries and row accessors using i1/i3/i4/i5"
    - path: "src/api/routes/narrative.py"
      provides: "SQL queries and row accessors using i1/i3/i4/i5"
    - path: "src/intelligence/trading/zone_engine.py"
      provides: "collect_candidates using get_atr_with_floor(features, symbol)"
  key_links:
    - from: "services/feature_writer.py _INSERT_FEATURE_SQL"
      to: "intelligence_features i1 column"
      via: "column name i1 in INSERT statement"
      pattern: "i1.*jsonb|technical_indicators"
    - from: "zone_engine.collect_candidates"
      to: "atr_utils.get_atr_with_floor"
      via: "import and call replacing get_atr"
      pattern: "get_atr_with_floor"
---

<objective>
Apply migration 125 (rename 4 columns in intelligence_features) and update all read/write sites to use the new tier-aligned names. Also fix zone_engine.py:231 — replace the instrument-agnostic `get_atr(features) or 0.5` fallback with `get_atr_with_floor(features, symbol)`.

Purpose: Columns in intelligence_features now match tier names exactly (i1/i3/i4/i5) eliminating the legacy naming mismatch. The ATR fix corrects a silent wrong-answer: a hardcoded 0.5 fallback that is meaningless for NQ (tick 0.25) or VX (tick 0.05).
Output: Migration 125 SQL file, updated feature_writer, historical pipeline, 3 API routes, and zone_engine.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/122-production-hardening/122-CONTEXT.md
@services/feature_writer.py
@production/scripts/run_historical_pipeline.py
@src/api/routes/signals.py
@src/api/routes/features.py
@src/api/routes/narrative.py
@src/intelligence/trading/zone_engine.py
@src/intelligence/trading/atr_utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 125 and update feature_writer + historical pipeline column names</name>
  <files>
    production/migrations/125_rename_intelligence_features_columns.sql
    services/feature_writer.py
    production/scripts/run_historical_pipeline.py
  </files>
  <read_first>
    - services/feature_writer.py (_INSERT_FEATURE_SQL at line 63 — current column list; _record_to_insert_params comments at lines 190-203)
    - production/scripts/run_historical_pipeline.py (_INSERT_FEATURE_SYNC_SQL at line 560; _load_precomputed_features at line 982; _event_to_sync_params column comments at line 638) — NOTE: this file was already modified by plan 06 (uuid4 fix, lines ~780-820); read current file state before editing
    - .planning/phases/122-production-hardening/122-CONTEXT.md (D-11)
  </read_first>
  <action>
    Create production/migrations/125_rename_intelligence_features_columns.sql:
    - 4 statements: RENAME COLUMN technical_indicators TO i1; RENAME COLUMN pattern_detections TO i5; RENAME COLUMN regime_features TO i3; RENAME COLUMN confluence_scores TO i4
    - Each wrapped in ALTER TABLE intelligence_features
    - No IF EXISTS guard — column rename is idempotent only via DO $$ blocks; keep it simple, apply once
    - Add header comment: "Migration 125: Rename legacy tier columns to match canonical tier names. smc and cross_timeframe_context already correct. market_context (cross-asset) retains its name."

    In services/feature_writer.py _INSERT_FEATURE_SQL (line 63):
    - Rename 4 columns in the INSERT column list: technical_indicators→i1, pattern_detections→i5, regime_features→i3, confluence_scores→i4
    - The $N placeholder numbers remain unchanged (still $8, $10, $11, $12)
    - Update inline comments in _record_to_insert_params return tuple to match new column names:
      - "$8 technical_indicators" comment → "$8 i1"
      - "$10 pattern_detections" comment → "$10 i5"
      - "$11 regime_features" comment → "$11 i3"
      - "$12 confluence_scores" comment → "$12 i4"
    - _UPDATE_MARKET_CTX_SQL references only market_context — no change needed

    In production/scripts/run_historical_pipeline.py:
    - _INSERT_FEATURE_SYNC_SQL (line 560): rename technical_indicators→i1, pattern_detections→i5, regime_features→i3, confluence_scores→i4 in the column list
    - _event_to_sync_params (line 638): update column order comments to match new names
    - _load_precomputed_features SELECT (line 991): rename the 4 columns in the string; update the unpacking variable names in the for loop at line 1003 (the variables ti, pd_det, rf, cs are local — rename to i1_data, i5_data, i3_data, i4_data for clarity, or keep short names — either way the SELECT column string must use new names); update the docstring
  </action>
  <verify>
    grep -n "technical_indicators\|pattern_detections\|regime_features\|confluence_scores" services/feature_writer.py production/scripts/run_historical_pipeline.py
    Expected: zero matches (all replaced)

    grep -n "i1\|i3\|i4\|i5" services/feature_writer.py | grep "INSERT\|SELECT\|\$8\|\$10\|\$11\|\$12"
    Expected: lines showing i1/i3/i4/i5 in SQL and comments

    cat production/migrations/125_rename_intelligence_features_columns.sql | grep "RENAME COLUMN" | wc -l
    Expected: 4
  </verify>
  <acceptance_criteria>
    - production/migrations/125_rename_intelligence_features_columns.sql exists and contains exactly 4 RENAME COLUMN statements
    - grep "technical_indicators\|pattern_detections\|regime_features\|confluence_scores" services/feature_writer.py returns zero matches
    - grep "technical_indicators\|pattern_detections\|regime_features\|confluence_scores" production/scripts/run_historical_pipeline.py returns zero matches
    - _load_precomputed_features SELECT string contains "i1, i5, i3, i4" (in declaration order matching the original column sequence)
    - _INSERT_FEATURE_SYNC_SQL column list contains i1, i5, i3, i4
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update API routes and fix zone_engine ATR floor</name>
  <files>
    src/api/routes/signals.py
    src/api/routes/features.py
    src/api/routes/narrative.py
    src/intelligence/trading/zone_engine.py
  </files>
  <read_first>
    - src/api/routes/signals.py (lines 130-155 row accessor; lines 922-940 feat_query; lines 1023-1024 JOIN query)
    - src/api/routes/features.py (lines 53-86 SELECT query and tier mapping loop)
    - src/api/routes/narrative.py (lines 124-130 _build_signal_context; lines 165-166 _SIGNAL_QUERY)
    - src/intelligence/trading/zone_engine.py (line 17 import; line 231 get_atr call; collect_candidates signature)
    - src/intelligence/trading/atr_utils.py (get_atr_with_floor signature — confirm it accepts (features, symbol))
    - .planning/phases/122-production-hardening/122-CONTEXT.md (D-11, D-14)
  </read_first>
  <action>
    Canonical column mapping (apply this everywhere, no exceptions):
      technical_indicators → i1
      pattern_detections   → i5
      regime_features      → i3
      confluence_scores    → i4

    In src/api/routes/signals.py:
    - Row accessor block (lines ~140-149): apply canonical mapping to all row["old_name"] references
    - feat_query SELECT (lines ~923-924): rename all 4 columns using canonical mapping
    - feat_row accessors (lines ~934-937): apply canonical mapping
    - JOIN query SELECT (lines ~1023-1024): rename all 4 columns using canonical mapping
    - JOIN query row accessors (lines ~1035+): apply canonical mapping

    In src/api/routes/features.py:
    - SELECT statement (lines ~55-56): rename all 4 columns using canonical mapping
    - Tier mapping loop (lines ~77-80): update the DB column name element in each tuple — second element changes from old name to new name per canonical mapping
    - Second SELECT if present (lines ~125-126): same column renames
    - Row accessors (lines ~150-159): apply canonical mapping

    In src/api/routes/narrative.py:
    - _build_signal_context row.get calls (lines ~125-130): apply canonical mapping to all 4 columns
    - _SIGNAL_QUERY column references (lines ~165-166): rename f.technical_indicators, f.pattern_detections, f.regime_features, f.confluence_scores using canonical mapping

    In src/intelligence/trading/zone_engine.py:
    - Import line: replace `from src.intelligence.trading.atr_utils import get_atr` with `from src.intelligence.trading.atr_utils import get_atr, get_atr_with_floor`
    - collect_candidates line 231: replace `atr = get_atr(features) or 0.5` with:
        symbol = features.get("symbol", "")
        atr = get_atr_with_floor(features, symbol)
        if atr is None:
            return []
    - The `if atr is None: return []` guard must appear before the direction/spec selection block
    - Keep the original `get_atr` import — verify it is still used elsewhere in zone_engine.py before removing; if unused after this change, remove it
  </action>
  <verify>
    grep -rn "technical_indicators\|pattern_detections\|regime_features\|confluence_scores" src/api/routes/ src/intelligence/trading/zone_engine.py
    Expected: zero matches

    grep -n "get_atr_with_floor\|get_atr(features) or" src/intelligence/trading/zone_engine.py
    Expected: get_atr_with_floor present; "get_atr(features) or 0.5" absent

    .venv/bin/pytest tests/unit/ -q --tb=short 2>&1 | tail -5
    Expected: exits 0
  </verify>
  <acceptance_criteria>
    - grep "technical_indicators\|pattern_detections\|regime_features\|confluence_scores" src/api/routes/signals.py src/api/routes/features.py src/api/routes/narrative.py returns zero matches
    - grep "technical_indicators\|pattern_detections\|regime_features\|confluence_scores" src/intelligence/trading/zone_engine.py returns zero matches
    - zone_engine.py line 231 area contains get_atr_with_floor(features, symbol) (not "get_atr(features) or 0.5")
    - zone_engine.py has `if atr is None: return []` guard before the direction/spec selection
    - SELECT column_name FROM information_schema.columns WHERE table_name='intelligence_features' AND column_name IN ('i1','i3','i4','i5') returns 4 rows (after migration 125 applied)
    - SELECT column_name FROM information_schema.columns WHERE table_name='intelligence_features' AND column_name IN ('technical_indicators','pattern_detections','regime_features','confluence_scores') returns 0 rows (after migration 125 applied)
    - .venv/bin/pytest tests/unit/ -q exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
# Column rename complete across all write/read sites
grep -rn "technical_indicators\|pattern_detections\|regime_features\|confluence_scores" \
  services/feature_writer.py \
  production/scripts/run_historical_pipeline.py \
  src/api/routes/signals.py \
  src/api/routes/features.py \
  src/api/routes/narrative.py \
  src/intelligence/trading/zone_engine.py
# Expected: zero matches

# ATR fix
grep -n "get_atr_with_floor" src/intelligence/trading/zone_engine.py
# Expected: import line + call at line ~231

# Migration file present
ls -la production/migrations/125_rename_intelligence_features_columns.sql

# Tests green
.venv/bin/pytest tests/unit/ -q --tb=short 2>&1 | tail -5

# After applying migration: verify DB columns
PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c \
  "SELECT column_name FROM information_schema.columns WHERE table_name='intelligence_features' AND column_name IN ('i1','i3','i4','i5','technical_indicators','pattern_detections','regime_features','confluence_scores') ORDER BY column_name;"
# Expected: only i1, i3, i4, i5 (4 rows)
```
</verification>

<success_criteria>
- Migration 125 SQL file has exactly 4 RENAME COLUMN statements
- Zero references to technical_indicators/pattern_detections/regime_features/confluence_scores in all modified files
- zone_engine collect_candidates uses get_atr_with_floor(features, symbol) with None guard
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/122-production-hardening/122-05-SUMMARY.md`
</output>
