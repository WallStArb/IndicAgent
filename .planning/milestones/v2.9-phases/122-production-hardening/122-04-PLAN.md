---
phase: 122-production-hardening
plan: 04
type: execute
wave: 2
depends_on: [03]
files_modified:
  - services/feature_writer.py
  - production/scripts/run_historical_pipeline.py
  - tests/unit/scripts/test_run_historical_pipeline.py
autonomous: true
requirements:
  - I2-PERSIST-04

must_haves:
  truths:
    - "feature_writer _record_to_insert_params produces a 33-element tuple with i2 as a separate dict"
    - "_INSERT_FEATURE_SQL includes i2 column after cross_timeframe_context and before trading_signals"
    - "market_context in the live insert tuple contains only cross_asset_snapshot data (not i2 fields)"
    - "_event_to_sync_params produces a 14-element tuple with i2 as element 14"
    - "_INSERT_FEATURE_SYNC_SQL includes i2 as column 14 with %s::jsonb placeholder"
  artifacts:
    - path: "services/feature_writer.py"
      provides: "_record_to_insert_params with i2 split from market_context; 33-element tuple; _INSERT_FEATURE_SQL with i2 column"
    - path: "production/scripts/run_historical_pipeline.py"
      provides: "_event_to_sync_params 14-element tuple; _INSERT_FEATURE_SYNC_SQL with i2 column"
    - path: "tests/unit/scripts/test_run_historical_pipeline.py"
      provides: "test_returns_13_tuple updated to test_returns_14_tuple"
  key_links:
    - from: "feature_writer._record_to_insert_params"
      to: "intelligence_features i2 column"
      via: "i2_data = event.i2.model_dump(exclude_none=True)"
      pattern: "i2_data.*model_dump"
    - from: "_event_to_sync_params"
      to: "intelligence_features i2 column"
      via: "json.dumps(event.i2.model_dump(exclude_none=True))"
      pattern: "event\\.i2\\.model_dump"
---

<!-- Note: Code can be written in parallel with Plans 01/02/03 but deployment requires
     migration 124 (Plan 03) to be applied first. wave:2/depends_on:[03] encodes this. -->

<objective>
Split i2 out of market_context in the live feature_writer and add it to the historical sync INSERT. After this plan both write paths produce separate i2 and market_context columns, completing the column boundary established by migration 124.

Purpose: From deploy forward, every INSERT into intelligence_features writes i2 and market_context as clean, disjoint columns. Historical and live pipelines are now symmetric in column layout.
Output: feature_writer with 33-element insert tuple, historical pipeline with 14-element sync tuple, updated tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@docs/plans/2026-06-12-i2-persistence-design.md
@services/feature_writer.py
@production/scripts/run_historical_pipeline.py
@tests/unit/scripts/test_run_historical_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split i2 from market_context in feature_writer</name>
  <files>services/feature_writer.py</files>
  <read_first>
    - services/feature_writer.py (_INSERT_FEATURE_SQL at line 63, _record_to_insert_params at line 166)
    - docs/plans/2026-06-12-i2-persistence-design.md (Component F)
  </read_first>
  <action>
    In _INSERT_FEATURE_SQL (line 63):

    1. Add `i2` to the column list after `cross_timeframe_context` and before `trading_signals`:
       Current columns line: `confluence_scores, smc, cross_timeframe_context, trading_signals,`
       New: `confluence_scores, smc, cross_timeframe_context, i2, trading_signals,`

    2. Add the corresponding `$N::jsonb` placeholder. Current VALUES section has positions $1-$32.
       After cross_timeframe_context ($14) and before trading_signals ($15), insert a new $15::jsonb.
       Shift all subsequent placeholder numbers up by 1: old $15 becomes $16, old $16 becomes $17, ... old $32 becomes $33.
       The tuple grows from 32 to 33 elements.

    In _record_to_insert_params (line 166):

    3. Replace the current market_ctx construction:
       ```python
       market_ctx = {
           **event.i2.model_dump(exclude_none=True),
           **(cross_asset_snapshot or {}),
       }
       ```
       With:
       ```python
       i2_data = event.i2.model_dump(exclude_none=True)
       market_ctx = cross_asset_snapshot or {}
       ```

    4. Update the docstring: "Build a 33-element tuple of INSERT parameters for _INSERT_FEATURE_SQL." (was 32-element)

    5. Insert `i2_data` into the return tuple after `event.i6.model_dump(exclude_none=True)` (the cross_timeframe_context value at position $14) and before the trading_signals list.
       Current positions in the return tuple:
       - position $14 (index 13): event.i6.model_dump(exclude_none=True)  # cross_timeframe_context
       - position $15 (index 14): [s.model_dump() for s in record.ranked_signals]  # trading_signals
       After change:
       - position $14 (index 13): event.i6.model_dump(exclude_none=True)  # cross_timeframe_context
       - position $15 (index 14): i2_data                                  # i2
       - position $16 (index 15): [s.model_dump() for s in record.ranked_signals]  # trading_signals
       ... and all subsequent positions shift by 1, ending with position $33 at event.feature_schema_version.

    6. Update the inline comments on the trailing elements to reflect their new $N positions ($16-$33).

    _UPDATE_MARKET_CTX_SQL is NOT changed — it only patches market_context and never touches i2.
  </action>
  <verify>
    python -c "
    import sys; sys.path.insert(0, '.')
    from services.feature_writer import _record_to_insert_params
    import inspect
    src = inspect.getsource(_record_to_insert_params)
    assert 'i2_data = event.i2.model_dump' in src, 'i2_data split missing'
    assert '**event.i2.model_dump' not in src, 'old i2 merge still present in market_ctx'
    print('Source assertions PASS')
    "

    python -c "
    import sys; sys.path.insert(0, '.')
    from services.feature_writer import _INSERT_FEATURE_SQL
    assert ', i2,' in _INSERT_FEATURE_SQL or 'i2,' in _INSERT_FEATURE_SQL, 'i2 not in INSERT SQL'
    print('SQL assertion PASS')
    "

    python -c "
    import sys; sys.path.insert(0, '.')
    # Count \$N placeholders — should be 33 (last should be \$33)
    from services.feature_writer import _INSERT_FEATURE_SQL
    import re
    max_pos = max(int(m) for m in re.findall(r'\\\$(\d+)', _INSERT_FEATURE_SQL))
    assert max_pos == 33, f'Expected \$33, got \${max_pos}'
    print('Tuple size assertion PASS: 33 params')
    "
  </verify>
  <done>
    - _record_to_insert_params docstring says "33-element tuple"
    - i2_data is constructed as `event.i2.model_dump(exclude_none=True)` separately from market_ctx
    - market_ctx is constructed as `cross_asset_snapshot or {}` only
    - Return tuple contains i2_data at position $15 (index 14) between cross_timeframe_context and trading_signals
    - _INSERT_FEATURE_SQL contains `i2` in column list and `$15::jsonb` in VALUES
    - Highest $N placeholder in _INSERT_FEATURE_SQL is $33
    - _UPDATE_MARKET_CTX_SQL is unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Add i2 to historical _INSERT_FEATURE_SYNC_SQL and _event_to_sync_params</name>
  <files>production/scripts/run_historical_pipeline.py</files>
  <read_first>
    - production/scripts/run_historical_pipeline.py (_INSERT_FEATURE_SYNC_SQL at line 560, _INSERT_FEATURE_SYNC_TEMPLATE at line 570, _event_to_sync_params at line 638)
    - docs/plans/2026-06-12-i2-persistence-design.md (Component G)
  </read_first>
  <action>
    In _INSERT_FEATURE_SYNC_SQL (line 560):

    1. Add `i2` to the column list after `cross_timeframe_context`:
       Current: `bar, technical_indicators, pattern_detections, regime_features, confluence_scores, smc, cross_timeframe_context`
       New: `bar, technical_indicators, pattern_detections, regime_features, confluence_scores, smc, cross_timeframe_context, i2`
       Total: 14 columns (ts, symbol, tf, platform, source, schema_version, bar, technical_indicators, pattern_detections, regime_features, confluence_scores, smc, cross_timeframe_context, i2).

    In _INSERT_FEATURE_SYNC_TEMPLATE (line 570):

    2. Add `%s::jsonb` for i2 as the 14th placeholder:
       Current: `"(%s, %s, %s, %s, %s, %s, %s::jsonb, %s::jsonb, %s::jsonb, %s::jsonb, %s::jsonb, %s::jsonb, %s::jsonb)"`
       New: `"(%s, %s, %s, %s, %s, %s, %s::jsonb, %s::jsonb, %s::jsonb, %s::jsonb, %s::jsonb, %s::jsonb, %s::jsonb, %s::jsonb)"`
       Count: 6 plain %s + 8 %s::jsonb = 14 total placeholders.

    In _event_to_sync_params (line 638):

    3. Update docstring: "Serialize IntelligenceEvent to a 14-element tuple for psycopg2 batch insert."

    4. Update column order comment:
       Old last line: `confluence_scores, smc, cross_timeframe_context`
       New: `confluence_scores, smc, cross_timeframe_context, i2`

    5. Append i2 as the 14th element in the return tuple:
       After `json.dumps(event.i6.model_dump(exclude_none=True)),  # cross_timeframe_context`
       Add: `json.dumps(event.i2.model_dump(exclude_none=True)),  # i2`

    6. Update _insert_features_sync docstring: "rows: list of 14-element tuples from _event_to_sync_params()"
  </action>
  <verify>
    python -c "
    import sys; sys.path.insert(0, '.')
    import inspect
    from production.scripts.run_historical_pipeline import _INSERT_FEATURE_SYNC_SQL, _INSERT_FEATURE_SYNC_TEMPLATE
    assert ', i2' in _INSERT_FEATURE_SYNC_SQL, 'i2 not in SYNC SQL column list'
    assert _INSERT_FEATURE_SYNC_TEMPLATE.count('%s') == 14, f'Expected 14 placeholders, got {_INSERT_FEATURE_SYNC_TEMPLATE.count(\"%s\")}'
    print('SQL assertions PASS')
    "

    python -c "
    import sys; sys.path.insert(0, '.')
    from src.intelligence.schemas import I1Indicators, I2Events, I3Structure, I4Context, I5Patterns, I6Confluence, IntelligenceEvent, OHLCVBar, SMCContext
    from datetime import UTC, datetime
    from production.scripts.run_historical_pipeline import _event_to_sync_params
    event = IntelligenceEvent(
        ts=datetime(2026,1,1,9,30,tzinfo=UTC),
        symbol='ESH6', tf='1m', source='backfill',
        bar=OHLCVBar(o=5100.0, h=5105.0, l=5095.0, c=5102.0, v=1000),
        i1=I1Indicators(), i2=I2Events(), i3=I3Structure(), i4=I4Context(),
        i5=I5Patterns(), smc=SMCContext(), i6=I6Confluence(),
    )
    params = _event_to_sync_params(event)
    assert len(params) == 14, f'Expected 14-tuple, got {len(params)}'
    assert isinstance(params[13], str), 'i2 element must be JSON string'
    print('Tuple assertion PASS: 14 elements')
    "
  </verify>
  <done>
    - _INSERT_FEATURE_SYNC_SQL column list ends with `, i2` (14th column)
    - _INSERT_FEATURE_SYNC_TEMPLATE has exactly 14 %s placeholders (6 plain + 8 ::jsonb)
    - _event_to_sync_params docstring says "14-element tuple"
    - _event_to_sync_params returns a 14-element tuple
    - Element at index 13 is `json.dumps(event.i2.model_dump(exclude_none=True))` — a JSON string
    - _insert_features_sync docstring says "14-element tuples"
  </done>
</task>

<task type="auto">
  <name>Task 3: Update _event_to_sync_params tuple length test and _make_event helper</name>
  <files>tests/unit/scripts/test_run_historical_pipeline.py</files>
  <read_first>
    - tests/unit/scripts/test_run_historical_pipeline.py (TestEventToSyncParams at line 926, _make_event helper at line 927)
    - production/scripts/run_historical_pipeline.py (_event_to_sync_params after Task 2)
  </read_first>
  <action>
    In TestEventToSyncParams._make_event (line 927):

    1. _make_event constructs IntelligenceEvent. After Plan 02 Task 3, it already includes i2=I2Events().
       If Plan 02 Task 3 already added i2 to _make_event, verify it is present; if not, add it now:
       - Add `I2Events` to the import list inside _make_event
       - Add `i2=I2Events()` to the IntelligenceEvent constructor call

    In test_returns_13_tuple (line 953):

    2. Update to test_returns_14_tuple:
       Old: `assert len(_event_to_sync_params(self._make_event())) == 13`
       New: `assert len(_event_to_sync_params(self._make_event())) == 14`
       Rename the method from test_returns_13_tuple to test_returns_14_tuple.

    3. In test_jsonb_columns_are_strings (line 964): the loop `for i, col in enumerate(params[6:], start=6)` now covers params[6] through params[13]. The assertion checks each is str. No change needed to the assertion logic — just verify no off-by-one from the count change.

    Verify no other tests in the file assert == 13 for _event_to_sync_params tuple length.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/scripts/test_run_historical_pipeline.py::TestEventToSyncParams -v
    Expected: test_returns_14_tuple PASSED, test_first_element_is_datetime PASSED, test_jsonb_columns_are_strings PASSED

    grep "== 13" tests/unit/scripts/test_run_historical_pipeline.py
    Expected: zero matches relating to _event_to_sync_params
  </verify>
  <done>
    - test_returns_13_tuple renamed to test_returns_14_tuple
    - Assertion is `len(...) == 14`
    - _make_event includes `i2=I2Events()` in IntelligenceEvent construction
    - All TestEventToSyncParams tests pass
    - grep "== 13" returns no matches for sync params tuple assertions
  </done>
</task>

</tasks>

<verification>
```bash
.venv/bin/pytest tests/unit/scripts/test_run_historical_pipeline.py::TestEventToSyncParams -v
.venv/bin/pytest tests/unit/ -q --tb=short 2>&1 | tail -10
.venv/bin/ruff check services/feature_writer.py production/scripts/run_historical_pipeline.py
.venv/bin/black --check services/feature_writer.py production/scripts/run_historical_pipeline.py
```

Deployment order: This plan (F+G) deploys as step 2 in the rollout — after migration 124 (Plan 03) has been applied. feature_writer must not be deployed before the i2 column exists in the DB.

Full rollout sequence:
1. Apply migration 124 (Plan 03 artifact)
2. Deploy feature_writer (this plan — Task 1)
3. Restart intelligence_pipeline (Plan 01 — I2Events strict validation activates)
4. Historical pipeline (Plans 02 and 04 — available for next replay run)
</verification>

<success_criteria>
- _record_to_insert_params produces a 33-element tuple with i2 as element at index 14
- _INSERT_FEATURE_SQL contains i2 column and $15::jsonb placeholder
- market_ctx in feature_writer is constructed as `cross_asset_snapshot or {}` only (no i2 fields)
- _event_to_sync_params produces a 14-element tuple
- _INSERT_FEATURE_SYNC_TEMPLATE has 14 placeholders
- test_returns_14_tuple passes
- .venv/bin/pytest tests/unit/ -q exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/122-production-hardening/122-04-SUMMARY.md`
</output>
