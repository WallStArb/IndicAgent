---
phase: 122-production-hardening
plan: 01
type: execute
wave: 2
depends_on: [03]
files_modified:
  - src/intelligence/schemas.py
  - src/intelligence/register_plugins.py
  - tests/unit/intelligence/test_schemas.py
  - tests/unit/intelligence/test_i2_schema.py
autonomous: true
requirements:
  - I2-PERSIST-01

must_haves:
  truths:
    - "I2Events rejects any dict containing an undeclared field name with a ValidationError"
    - "I2Events accepts all 19 composite plugin fields (rsi_accel, macd_accel, roc_accel, inflection_flag, rsi_curvature, macd_hist_slope, price_accel, hma_slope, hma_accel, deriv_osc, deriv_osc_signal, deriv_osc_cross_bullish, deriv_osc_cross_bearish, exhaustion_score, exhaustion_side, exhaustion_bars, accel_regime, accel_score, accel_agreement)"
    - "I2Events no longer declares any of the 8 MACD fields (macd_cross_bullish, macd_cross_bearish, macd_cross_bars_ago, macd_hist_positive, macd_hist_turning_up, macd_negative_support_test, macd_price_divergence_bullish, macd_price_divergence_bearish)"
    - "validate_schema_coverage() includes all 10 I2 plugins and crashes startup if any I2 plugin emits an undeclared field"
  artifacts:
    - path: "src/intelligence/schemas.py"
      provides: "I2Events with 45 declared fields, extra='forbid'"
      contains: "extra=\"forbid\""
    - path: "src/intelligence/register_plugins.py"
      provides: "validate_schema_coverage covers I2"
      contains: "I2Events"
    - path: "tests/unit/intelligence/test_schemas.py"
      provides: "I2Events schema contract tests"
    - path: "tests/unit/intelligence/test_i2_schema.py"
      provides: "Updated I2Events field assertions — removed MACD fields asserted absent"
  key_links:
    - from: "register_plugins.py validate_schema_coverage"
      to: "I2Events"
      via: "tier_checks list"
      pattern: "I2Events"
    - from: "I2Events"
      to: "plugin outputs"
      via: "extra='forbid' enforcement"
      pattern: "extra.*forbid"
---

<!-- Note: Code can be written in parallel with Plans 02/03/04 but deployment requires
     migration 124 (Plan 03) to be applied first. wave:2/depends_on:[03] encodes this. -->

<objective>
Fix the I2Events schema contract: remove 8 dead MACD field declarations, add 19 undeclared composite plugin fields, and replace extra="allow" with extra="forbid". Enable I2 validation in register_plugins.py so startup crashes instead of silently accepting undeclared fields.

Purpose: Eliminate the schema contract void that lets any plugin output pass silently through I2Events. After this plan, I2Events is the complete, enforced contract for I2 tier outputs.
Output: Updated I2Events schema (45 fields, extra="forbid") + I2 validation in register_plugins + tests proving the contract.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-06-12-i2-persistence-design.md
@src/intelligence/schemas.py
@src/intelligence/register_plugins.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite I2Events schema to 45-field strict contract</name>
  <files>src/intelligence/schemas.py</files>
  <read_first>
    - src/intelligence/schemas.py (current I2Events definition starting at line 99)
    - docs/plans/2026-06-12-i2-persistence-design.md (Component A — exact field list)
  </read_first>
  <action>
    In I2Events:

    1. Replace `model_config = ConfigDict(extra="allow")` with `model_config = ConfigDict(extra="forbid")`.

    2. Remove these 8 MACD field declarations (they belong to I3Structure.struct_MACDEvents):
       - macd_cross_bullish, macd_cross_bearish, macd_cross_bars_ago, macd_hist_positive,
         macd_hist_turning_up, macd_negative_support_test, macd_price_divergence_bullish,
         macd_price_divergence_bearish

    3. Add 19 composite plugin fields after the existing bridge composites block:

       # cmp_MomentumAccel (9 fields)
       rsi_accel: float | None = None
       macd_accel: float | None = None
       roc_accel: float | None = None
       inflection_flag: float | None = None
       rsi_curvature: float | None = None
       macd_hist_slope: float | None = None
       price_accel: float | None = None
       hma_slope: float | None = None
       hma_accel: float | None = None

       # cmp_DerivativeOscillator (4 fields)
       deriv_osc: float | None = None
       deriv_osc_signal: float | None = None
       deriv_osc_cross_bullish: float | None = None
       deriv_osc_cross_bearish: float | None = None

       # cmp_ExhaustionScore (3 fields)
       exhaustion_score: float | None = None
       exhaustion_side: str | None = None
       exhaustion_bars: float | None = None

       # cmp_AccelerationRegime (3 fields)
       accel_regime: str | None = None
       accel_score: float | None = None
       accel_agreement: float | None = None

    4. Update the class docstring to:
       - List all 10 I2 plugins with their field counts: evt_MACDEvents(removed), evt_RSIEvents, evt_StochasticEvents, evt_ADXEvents, evt_VolumeEvents, bridge composites (DonchianPosition, OBVMomentum), cmp_MomentumAccel(9), cmp_DerivativeOscillator(4), cmp_ExhaustionScore(3), cmp_AccelerationRegime(3)
       - Note total of 45 declared fields

    Field count check: 34 existing fields (lines 109-153 in current file) minus 8 MACD removals = 26, plus 19 composite additions = 45 total.
    The exhaustion_side and accel_regime fields are `str | None = None` (not float).
  </action>
  <verify>
    python -c "from src.intelligence.schemas import I2Events; print(len(I2Events.model_fields))"
    Expected output: 45

    python -c "from src.intelligence.schemas import I2Events; I2Events(rsi_accel=0.1, exhaustion_side='long', accel_regime='trending'); print('composite fields accepted')"
    Expected: prints "composite fields accepted" without error

    python -c "
    from pydantic import ValidationError
    from src.intelligence.schemas import I2Events
    try:
        I2Events(macd_cross_bullish=1.0)
        print('FAIL: should have raised')
    except ValidationError:
        print('PASS: extra field rejected')
    "
    Expected: PASS: extra field rejected
  </verify>
  <done>
    - I2Events.model_fields has exactly 45 keys
    - I2Events(**{"rsi_accel": 0.1}) succeeds
    - I2Events(**{"macd_cross_bullish": 1.0}) raises ValidationError
    - I2Events(**{"undeclared_xyz": 1.0}) raises ValidationError
    - None of macd_cross_bullish, macd_cross_bearish, macd_cross_bars_ago, macd_hist_positive, macd_hist_turning_up, macd_negative_support_test, macd_price_divergence_bullish, macd_price_divergence_bearish appear in I2Events.model_fields
  </done>
</task>

<task type="auto">
  <name>Task 2: Enable I2 schema validation in register_plugins.py</name>
  <files>src/intelligence/register_plugins.py</files>
  <read_first>
    - src/intelligence/register_plugins.py (validate_schema_coverage function, lines 154-onward)
    - src/intelligence/schemas.py (confirm I2Events import is available)
  </read_first>
  <action>
    In validate_schema_coverage (currently named validate_schema_coverage at line ~154):

    1. Update the docstring: remove "I1 and I2 are skipped (extra='allow')" — change to "I1 is skipped (extra='allow')".

    2. Add I2Events import alongside the existing schema imports at the top of register_plugins.py.
       Current import (line 105): `from .schemas import I3Structure, I4Context, I5Patterns, I6Confluence, SMCContext`
       New: `from .schemas import I2Events, I3Structure, I4Context, I5Patterns, I6Confluence, SMCContext`

    3. Add I2 entry as the first item in tier_checks list:
       (
           "I2",
           [
               rsi_events_plugin,
               stoch_events_plugin,
               adx_events_plugin,
               volume_events_plugin,
               donchian_pos_plugin,
               obv_momentum_plugin,
               momentum_accel_plugin,
               deriv_osc_plugin,
               exhaustion_score_plugin,
               accel_regime_plugin,
           ],
           I2Events,
       ),

    The 10 I2 plugins referenced are already imported at lines 72-82 of register_plugins.py:
    rsi_events_plugin, stoch_events_plugin, adx_events_plugin, volume_events_plugin,
    donchian_pos_plugin, obv_momentum_plugin, momentum_accel_plugin, deriv_osc_plugin,
    exhaustion_score_plugin, accel_regime_plugin.

    Note: `ma_compare_plugin` is in `TIER_I1`, not `TIER_I2` — do not include it in the I2 tier_checks entry.

    Check TIER_I2 in register_plugins.py to get the definitive list of I2 plugin names and cross-reference with the plugin import variables. Include all 10 plugins that are genuinely in TIER_I2.
  </action>
  <verify>
    python -c "
    from src.intelligence.register_plugins import register_all_plugins, validate_schema_coverage
    register_all_plugins()
    validate_schema_coverage()
    print('PASS: validation passed')
    "
    Expected: PASS: validation passed (no RuntimeError)

    python -c "
    from src.intelligence.register_plugins import validate_schema_coverage
    import inspect
    src = inspect.getsource(validate_schema_coverage)
    assert 'I2Events' in src, 'I2Events not in validation'
    assert 'I2 are skipped' not in src, 'old skip comment still present'
    print('PASS')
    "
  </verify>
  <done>
    - validate_schema_coverage includes an "I2" tuple in tier_checks referencing I2Events
    - validate_schema_coverage() does not raise when called after register_all_plugins()
    - The "I2 are skipped (extra='allow')" comment is removed from the docstring
    - I2Events is imported in register_plugins.py
  </done>
</task>

<task type="auto">
  <name>Task 3: Add I2Events schema contract tests</name>
  <files>
    tests/unit/intelligence/test_schemas.py
    tests/unit/intelligence/test_i2_schema.py
  </files>
  <read_first>
    - tests/unit/intelligence/ (check for existing test_schemas.py or similar file)
    - tests/unit/intelligence/test_i2_schema.py (check for existing assertions on macd_cross_bullish or other MACD fields being removed)
    - src/intelligence/schemas.py (final I2Events after Task 1)
  </read_first>
  <action>
    Check if tests/unit/intelligence/test_schemas.py already exists. If it does, append; if not, create.

    Add a TestI2EventsSchema class with these tests:

    class TestI2EventsSchema:
        def test_rejects_undeclared_extra_field(self):
            """I2Events must raise ValidationError on any undeclared field."""
            from pydantic import ValidationError
            from src.intelligence.schemas import I2Events
            with pytest.raises(ValidationError):
                I2Events(**{"undeclared_xyz": 1.0})

        def test_rejects_former_macd_cross_bullish(self):
            """macd_cross_bullish was removed from I2Events (belongs to I3Structure)."""
            from pydantic import ValidationError
            from src.intelligence.schemas import I2Events
            with pytest.raises(ValidationError):
                I2Events(**{"macd_cross_bullish": 1.0})

        def test_accepts_all_19_composite_fields(self):
            """All 19 composite plugin fields must be accepted without error."""
            from src.intelligence.schemas import I2Events
            composite_fields = {
                "rsi_accel": 0.1, "macd_accel": 0.2, "roc_accel": 0.3,
                "inflection_flag": 1.0, "rsi_curvature": 0.05,
                "macd_hist_slope": 0.01, "price_accel": 0.002,
                "hma_slope": 0.5, "hma_accel": 0.1,
                "deriv_osc": 0.3, "deriv_osc_signal": 0.2,
                "deriv_osc_cross_bullish": 1.0, "deriv_osc_cross_bearish": 0.0,
                "exhaustion_score": 0.7, "exhaustion_side": "long", "exhaustion_bars": 3.0,
                "accel_regime": "trending", "accel_score": 0.6, "accel_agreement": 0.8,
            }
            event = I2Events(**composite_fields)
            assert event.rsi_accel == 0.1
            assert event.exhaustion_side == "long"
            assert event.accel_regime == "trending"

        def test_field_count_is_45(self):
            """I2Events must declare exactly 45 fields."""
            from src.intelligence.schemas import I2Events
            assert len(I2Events.model_fields) == 45

        def test_none_defaults_on_empty_construction(self):
            """I2Events() with no args must not raise."""
            from src.intelligence.schemas import I2Events
            event = I2Events()
            assert event.rsi_accel is None
            assert event.exhaustion_side is None

    Add `import pytest` at the top if not present.

    Also update tests/unit/intelligence/test_i2_schema.py:
    - Read the file and find any assertions that use `hasattr(I2Events(), 'macd_cross_bullish')` or
      assert presence of any of the 8 removed MACD fields: macd_cross_bullish, macd_signal_cross_bullish,
      macd_hist_cross_bullish, macd_cross_bearish, macd_cross_bars_ago, macd_hist_positive,
      macd_hist_turning_up, macd_negative_support_test, macd_price_divergence_bullish,
      macd_price_divergence_bearish.
    - Remove or invert any such assertions: they must now assert these fields are NOT present
      (e.g., `assert not hasattr(I2Events(), 'macd_cross_bullish')`).
    - Add assertions that at least one composite field IS present:
      `assert hasattr(I2Events(), 'rsi_accel')`
      `assert hasattr(I2Events(), 'deriv_osc')`
      `assert hasattr(I2Events(), 'exhaustion_score')`
      `assert hasattr(I2Events(), 'accel_regime')`
    - Add assertion that `I2Events(**{'undeclared_field': 1})` raises `ValidationError`.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/intelligence/test_schemas.py -v -k "TestI2Events"
    Expected: all 5 tests pass (PASSED), no FAILED or ERROR

    .venv/bin/pytest tests/unit/intelligence/test_i2_schema.py -v
    Expected: all tests pass, no FAILED or ERROR

    .venv/bin/pytest tests/unit/ -q --tb=short 2>&1 | tail -10
    Expected: exits 0
  </verify>
  <done>
    - tests/unit/intelligence/test_schemas.py exists with TestI2EventsSchema class
    - All 5 tests in TestI2EventsSchema pass under pytest
    - test_rejects_undeclared_extra_field passes (ValidationError raised)
    - test_accepts_all_19_composite_fields passes (all 19 fields accepted)
    - test_field_count_is_45 passes
    - tests/unit/intelligence/test_i2_schema.py contains no assertion that macd_cross_bullish,
      macd_signal_cross_bullish, macd_hist_cross_bullish, or any of the other removed MACD fields
      ARE present on I2Events
    - tests/unit/intelligence/test_i2_schema.py asserts these MACD fields are NOT present
    - tests/unit/intelligence/test_i2_schema.py asserts composite fields (rsi_accel, deriv_osc,
      exhaustion_score, accel_regime) ARE present
  </done>
</task>

</tasks>

<verification>
Run full schema + register validation:

```bash
.venv/bin/pytest tests/unit/intelligence/test_schemas.py -v
.venv/bin/pytest tests/unit/intelligence/test_i2_schema.py -v
.venv/bin/pytest tests/unit/ -q --tb=short 2>&1 | tail -20
python -c "from src.intelligence.register_plugins import register_all_plugins, validate_schema_coverage; register_all_plugins(); validate_schema_coverage(); print('OK')"
```

Deployment order note: This plan (A+B) deploys as step 3 in the rollout — after migration 124 (Plan 03) and feature_writer deploy (Plan 04). The intelligence_pipeline restart triggers validate_schema_coverage() at startup.
</verification>

<success_criteria>
- I2Events.model_fields contains exactly 45 keys
- I2Events(**{"macd_cross_bullish": 1.0}) raises pydantic.ValidationError
- I2Events(**{"rsi_accel": 0.1, "exhaustion_side": "long"}) succeeds
- validate_schema_coverage() completes without RuntimeError after register_all_plugins()
- .venv/bin/pytest tests/unit/ -q exits 0
- tests/unit/intelligence/test_i2_schema.py contains no assertion on MACD fields that were removed from I2Events
</success_criteria>

<output>
After completion, create `.planning/phases/122-production-hardening/122-01-SUMMARY.md`
</output>
