---
phase: 119-remaining-16-setup-refactoring
plan: "04"
type: execute
wave: 2
depends_on: ["119-01", "119-02", "119-03"]
requirements: [REFACTOR-08]
files_modified:
  - docs/architecture/i7-setup-confidence-patterns.md
  - CLAUDE.md
  - src/intelligence/CLAUDE.md
autonomous: true
must_haves:
  truths:
    - "A new doc `docs/architecture/i7-setup-confidence-patterns.md` exists with verified `current` status and recipe-card format, enumerating all 6 GOOD patterns with the canonical code structure and the D-01/D-03 gate thresholds."
    - "The doc names `ofi_continuation.py` as the canonical reference implementation using SYMBOL references (class + method names), NOT fixed line numbers."
    - "The doc's compliance table reflects ACTUAL scope: 22 compliant I7 setups (5 Phase 118 + 17 Phase 119) AND a clearly labeled list of the 8 `_I7_I6_EXEMPT` plugins not yet I6-integrated. The table does NOT claim all TIER_I7 are compliant."
    - "The doc states the exact gate thresholds: `_MIN_REGIME_WEIGHT = 0.30`, `_MIN_CTF_SCORE = 0.25`, z-score gate `>= 2.0`, and the `hmm_trending_weight` rule for `regime_type=\"any\"` plugins."
    - "The doc includes an anti-pattern section listing: `hmm_regime_weight(features, \"any\")` (returns 0.5 fallback), binary HMM regime equality checks, OHLCV/df access before the gate, and HMM probability used as a confidence factor."
    - "The doc distinguishes pre-entry gates vs confidence factors vs captured extrinsic fields vs zone friction penalties, and states whether zone friction is in-scope for the 6 GOOD patterns."
    - "CLAUDE.md Plugin System section links the new doc and states that I7 setups must follow the 6 GOOD patterns + that validate_tier() enforces requires_i6_confluence=True (with the documented _I7_I6_EXEMPT temporary carve-out)."
    - "src/intelligence/CLAUDE.md \"Creating a New I7 Plugin\" section references the new doc."
    - "No fabricated claims: every code reference (function names, constants, ClassVars) in the doc is verified against the source files; no invented line numbers."
---

<objective>
Document the 6 GOOD patterns as a canonical architecture reference and link it from CLAUDE.md and src/intelligence/CLAUDE.md, so future I7 setups are authored compliant-by-default. The doc must reflect ACTUAL enforcement scope (22 compliant + 8 temporarily exempt), use symbol references not line numbers, and include an anti-pattern section.

Review-resolved facts (audited 2026-06-10):
- TIER_I7 has 37 entries. After Phase 119: 22 are requires_i6_confluence=True (5 Phase-118 + 17 Phase-119), 8 are in `_I7_I6_EXEMPT` (still False, deferred), and the remaining are the 2 aggregators / GOOD setups already True. The doc MUST NOT claim "all I7 compliant" — it states 22 compliant and lists the 8 exempt. (Resolves Codex/Gemini "22 vs all-I7" scope-language HIGH.)
- Line-number citations drift after refactors — use symbol references (`OFIContinuationPlugin` + the relevant method, named constants, ClassVar identifiers). (Resolves Codex/Gemini "fragile line ranges" MEDIUM.)
- Zone friction penalties: per test_i7_extrinsic_contract.py, zone friction is an EXTRINSIC penalty stripped from confidence in Phase 118 for momentum_breakout/trend_following/liquidity_hunt, but retained as an intrinsic structural gate for supply_demand_setup. It is NOT one of the 6 GOOD patterns — the doc clarifies it as a separate captured/gated context. (Resolves Codex MEDIUM "zone friction".)
</objective>

<must_haves>
  <truths>
    - A new doc `docs/architecture/i7-setup-confidence-patterns.md` exists with verified `current` status and recipe-card format, enumerating all 6 GOOD patterns with the canonical code structure and D-01/D-03 gate thresholds.
    - The doc names `ofi_continuation.py` as the canonical reference using SYMBOL references (class + method names), NOT fixed line numbers.
    - The doc's compliance table reflects ACTUAL scope: 22 compliant (5 Phase 118 + 17 Phase 119) AND a labeled list of the 8 `_I7_I6_EXEMPT` plugins; it does NOT claim all TIER_I7 are compliant.
    - The doc states the exact gate thresholds: `_MIN_REGIME_WEIGHT = 0.30`, `_MIN_CTF_SCORE = 0.25`, z-score gate `>= 2.0`, and the `hmm_trending_weight` rule for `regime_type="any"`.
    - The doc includes an anti-pattern section: `hmm_regime_weight(features, "any")`, binary HMM equality checks, OHLCV before gate, HMM as a confidence factor.
    - The doc distinguishes pre-entry gates vs confidence factors vs captured extrinsic fields vs zone friction penalties, and states whether zone friction is in the 6 GOOD patterns.
    - CLAUDE.md Plugin System section links the new doc + states the 6 GOOD patterns + validate_tier() enforcement with the _I7_I6_EXEMPT carve-out.
    - src/intelligence/CLAUDE.md "Creating a New I7 Plugin" section references the new doc.
    - No fabricated claims: every code reference is verified against source; no invented line numbers.
  </truths>
</must_haves>

<tasks>

  <task type="auto">
    <name>Author docs/architecture/i7-setup-confidence-patterns.md (symbol refs, accurate scope, anti-patterns)</name>
    <read_first>
      - docs/foundation/documentation-system.md — recipe-card format + verified-status convention
      - src/intelligence/trading/ofi_continuation.py — canonical reference; cite the class name + ClassVar identifiers + the 4-factor composite by symbol, NOT line numbers
      - src/intelligence/register_plugins.py — TIER_I7, _I7_I6_EXEMPT (8 names), _PHASE_119_PLUGINS (17 names)
      - tests/unit/intelligence/test_i7_extrinsic_contract.py — the extrinsic-vs-intrinsic distinction and zone-friction handling (the module docstring describes it)
      - .planning/phases/119-remaining-16-setup-refactoring/119-CONTEXT.md — D-01, D-02, D-03, D-04 threshold decisions
      - .planning/phases/119-remaining-16-setup-refactoring/119-01-SUMMARY.md — per-plugin 4-factor weights chosen in Wave 1
      - .planning/phases/119-remaining-16-setup-refactoring/119-02-SUMMARY.md — per-plugin weights chosen in Wave 2
    </read_first>
    <action>
      Create `docs/architecture/i7-setup-confidence-patterns.md` following the recipe-card format from `docs/foundation/documentation-system.md`, with a status line marking it `current` and dated 2026-06-10. Sections:
        1. Purpose — why I7 confidence integrity matters (SoC: confidence = intrinsic signal strength; regime + I6 = eligibility gates).
        2. The 6 GOOD Patterns — enumerate each with its concrete code shape: (1) multi-factor intrinsic confidence (4 named clamp01 factors, weights summing to 1.0, wrapped in compose_confidence; note MomentumBreakout/VWAPDeviation keep sound 3-factor formulas per D-04); (2) requires_i6_confluence=True; (3) strict dual gate before OHLCV extraction; (4) continuous hmm_regime_weight (not binary); (5) early-gate optimization (cheap checks before extract_ohlcv); (6) shadow_only=True until Phase 120 promotion.
        3. Gate thresholds — `_MIN_REGIME_WEIGHT = 0.30`, `_MIN_CTF_SCORE = 0.25`, z-score features gate at `>= 2.0`; and the rule that `regime_type="any"` plugins MUST use `hmm_trending_weight(features)` because `hmm_regime_weight(features, "any")` returns the 0.5 neutral fallback. Include the per-regime gate shapes: trend = up/down dual `hmm_regime_weight`; mean_reversion = `hmm_regime_weight(..., "ranging")`; any = `hmm_trending_weight`.
        4. Pattern vocabulary — a short table distinguishing four concepts: pre-entry GATE (regime weight, ctf_score, z-score; below threshold returns no_signal), CONFIDENCE FACTOR (intrinsic signal-strength score in the composite), CAPTURED EXTRINSIC FIELD (written by capture_signal_features for ML, never scored), ZONE FRICTION PENALTY (supply/demand zone context — stripped from confidence in Phase 118 for trend/momentum setups, retained as an intrinsic structural gate only for supply_demand_setup; NOT one of the 6 GOOD patterns).
        5. Canonical reference — point to `ofi_continuation.py` by SYMBOL: the `OFIContinuationPlugin` class, its `shadow_only`/`requires_i6_confluence` ClassVars, and its 4-factor composite in the compute method. Do NOT cite line numbers.
        6. Compliant setups — table of the 22 compliant I7 setups: 5 from Phase 118 (OFIContinuation, PatternCompletion, GapAnalysisSetup, CVDDivergence, DivergenceStack) + 17 from Phase 119 (the _PHASE_119_PLUGINS set), noting MomentumBreakout and VWAPDeviation keep 3-factor formulas. SEPARATELY list the 8 `_I7_I6_EXEMPT` plugins (regime_transition, prev_day_level_test, anchored_vwap_reversion, poc_rejection, hvn_rejection, cross_asset_divergence, mean_reversion, squeeze_expansion) under a "Not yet I6-integrated (deferred)" heading. Do NOT claim all TIER_I7 are compliant.
        7. Enforcement — validate_tier() raises ArchitectureViolation if requires_i6_confluence is not True for any I7 plugin NOT in `_I7_I6_EXEMPT`; the exempt set is a documented temporary carve-out to be removed when those 8 are refactored. shadow_only is verified by code review + the test sweep (truth lives in the shadow_registry DB table, per D-02).
        8. Anti-patterns — explicit DON'T list: `hmm_regime_weight(features, "any")` (silent 0.5 pass), binary HMM regime equality (`hmm in (1,2)`, `hmm not in (1.0,2.0)`, `hmm_regime_prob < X`), any `extract_ohlcv`/`df["..."]`/`.to_numpy(`/ATR/`frame_trade` access before the dual gate, and HMM probability used as a confidence factor (HMM is gate-only; direction selection may read hmm_regime but confidence may not).
      Use single dash, no em dash. Do not invent line numbers — cite verified symbols or omit.
    </action>
    <acceptance_criteria>
      - `test -f docs/architecture/i7-setup-confidence-patterns.md` succeeds.
      - `grep -n "current" docs/architecture/i7-setup-confidence-patterns.md` shows a status line.
      - `grep -n "_MIN_REGIME_WEIGHT\|_MIN_CTF_SCORE\|hmm_trending_weight\|0.30\|2.0" docs/architecture/i7-setup-confidence-patterns.md` shows the thresholds and the any-regime rule.
      - `grep -n "ofi_continuation\|OFIContinuationPlugin" docs/architecture/i7-setup-confidence-patterns.md` shows the canonical reference by symbol.
      - `grep -n "_I7_I6_EXEMPT\|deferred\|Not yet I6" docs/architecture/i7-setup-confidence-patterns.md` shows the exempt-set list; the compliant table lists all 17 Phase-119 + 5 Phase-118 plugin names.
      - `grep -ni "anti-pattern\|zone friction" docs/architecture/i7-setup-confidence-patterns.md` shows both the anti-pattern section and the zone-friction clarification.
      - `grep -c "—" docs/architecture/i7-setup-confidence-patterns.md` returns 0 (no em dash).
    </acceptance_criteria>
    <files>docs/architecture/i7-setup-confidence-patterns.md</files>
    <verify>test -f docs/architecture/i7-setup-confidence-patterns.md && grep -q 'current' docs/architecture/i7-setup-confidence-patterns.md && ! grep -q '—' docs/architecture/i7-setup-confidence-patterns.md</verify>
    <done>New doc exists with current status, recipe-card format, the 6 GOOD patterns, gate thresholds, symbol-based canonical reference, accurate 22-compliant + 8-exempt table, zone-friction clarification, and anti-pattern section; no em dash.</done>
  </task>

  <task type="auto">
    <name>Link the new doc from CLAUDE.md and src/intelligence/CLAUDE.md</name>
    <read_first>
      - CLAUDE.md — Plugin System section (lines ~117-123); Shadow governance bullet; I6→I7 confluence bullet
      - src/intelligence/CLAUDE.md — "Creating a New I7 Plugin" subsection
    </read_first>
    <action>
      In CLAUDE.md Plugin System section, add one bullet (single dash, no em dash): I7 setup confidence integrity - all I7 setups (except the documented `_I7_I6_EXEMPT` carve-out) follow the 6 GOOD patterns (4-factor intrinsic confidence, dual regime+I6 gate before OHLCV, shadow_only=True), enforced by `validate_tier()` which raises ArchitectureViolation unless `requires_i6_confluence=True`. Link `docs/architecture/i7-setup-confidence-patterns.md`. Keep the existing "I6→I7 confluence" and "Shadow governance" bullets - add, do not replace.

      In `src/intelligence/CLAUDE.md`, under "Creating a New I7 Plugin", add a reference line pointing to `docs/architecture/i7-setup-confidence-patterns.md` as the required pattern guide (4-factor confidence + dual gate + shadow_only), so new plugins are compliant by default.
    </action>
    <acceptance_criteria>
      - `grep -n "i7-setup-confidence-patterns.md" CLAUDE.md src/intelligence/CLAUDE.md` returns a match in both files.
      - `grep -ni "6 GOOD\|4-factor\|dual.*gate" CLAUDE.md` returns a match in the Plugin System section.
      - The diff added to CLAUDE.md introduces no em dash character (verify the added lines use single dashes only).
    </acceptance_criteria>
    <files>CLAUDE.md, src/intelligence/CLAUDE.md</files>
    <verify>grep -rn 'i7-setup-confidence-patterns.md' CLAUDE.md src/intelligence/CLAUDE.md</verify>
    <done>Both CLAUDE.md and src/intelligence/CLAUDE.md link the new doc; CLAUDE.md Plugin System section states the 6 GOOD patterns + validate_tier enforcement + the _I7_I6_EXEMPT carve-out; no em dash introduced.</done>
  </task>

</tasks>

<verification>
  - `test -f docs/architecture/i7-setup-confidence-patterns.md && grep -q "current" docs/architecture/i7-setup-confidence-patterns.md` succeeds.
  - `grep -rn "i7-setup-confidence-patterns.md" CLAUDE.md src/intelligence/CLAUDE.md` shows links in both.
  - `grep -n "_MIN_CTF_SCORE\|_MIN_REGIME_WEIGHT\|hmm_trending_weight" docs/architecture/i7-setup-confidence-patterns.md` confirms the gate thresholds are documented.
  - `grep -ni "anti-pattern\|_I7_I6_EXEMPT" docs/architecture/i7-setup-confidence-patterns.md` confirms the anti-pattern section and the exempt-set scope are present.
</verification>

<output>
After completion, create `.planning/phases/119-remaining-16-setup-refactoring/119-04-SUMMARY.md` documenting the new doc path, the CLAUDE.md links added, the accurate compliance scope (22 compliant + 8 exempt), and confirmation that all cited symbol references were verified against source (no invented line numbers, no em dash).
</output>
