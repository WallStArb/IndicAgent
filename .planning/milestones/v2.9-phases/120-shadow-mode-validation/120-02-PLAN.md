---
phase: 120-shadow-mode-validation
plan: 02
type: execute
wave: 1
depends_on: []
requirements: [SHADOW-01]
files_modified:
  - services/shadow_auditor.py
autonomous: true

must_haves:
  truths:
    - "shadow_auditor.py no longer promotes any component (promotion path fully removed)"
    - "shadow_auditor.py still demotes live components exactly as before (demotion path unchanged)"
    - "shadow_auditor.py still runs as a oneshot with JOB_COMPLETED_TOTAL job label shadow-auditor"
  artifacts:
    - path: "services/shadow_auditor.py"
      provides: "Demotion-only shadow auditor (promotion removed)"
      contains: "_check_demotion"
  key_links:
    - from: "services/shadow_auditor.py::_run_audit"
      to: "_check_demotion"
      via: "called for non-shadow rows only; no _check_promotion call remains"
      pattern: "_check_demotion"

threat_model:
  assets:
    - "shadow_auditor demotion logic (catches live performance degradation every 30 min)"
  threats:
    - threat: "Accidentally removing a symbol shared by the demotion path (e.g. bootstrap_ci_lower) breaks demotion"
      severity: high
      mitigation: "bootstrap_ci_lower is used by _check_demotion (line 280) — KEEP it. Only remove symbols proven promotion-exclusive. Verified import/symbol usage before deletion."
    - threat: "Demotion behavior silently changes during surgical edit"
      severity: high
      mitigation: "_check_demotion body and its call site are not touched; unit tests for demotion must remain green"
---

<objective>
Surgically reduce services/shadow_auditor.py to demotion-only by removing the promotion path. This enforces the SoC split (D-01): shadow_auditor = demotion (30-min cadence), shadow_validator = promotion (weekly, Plan 01).

Purpose: Promotion now lives exclusively in shadow_validator.py. Leaving _check_promotion in shadow_auditor would create two conflicting promotion code paths.
Output: services/shadow_auditor.py reduced to demotion-only with all promotion-exclusive symbols removed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-shadow-mode-validation/120-CONTEXT.md
@.planning/phases/120-shadow-mode-validation/120-RESEARCH.md
@.planning/phases/120-shadow-mode-validation/120-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove promotion path from shadow_auditor.py (demotion-only)</name>
  <files>services/shadow_auditor.py</files>
  <read_first>
    - services/shadow_auditor.py (FULL file, all 358 lines — must see exact current state of imports, constants, pure functions, _run_audit, _check_promotion, _check_demotion)
    - .planning/phases/120-shadow-mode-validation/120-CONTEXT.md (D-10 — exact removal scope)
    - .planning/phases/120-shadow-mode-validation/120-RESEARCH.md (shadow_auditor.py Surgical Removal section)
  </read_first>
  <action>
    Remove exactly the promotion-exclusive code. Verified facts (do not re-derive incorrectly):
    - bootstrap_ci_lower (import line 23) is used by BOTH _check_promotion (line 138) AND _check_demotion (line 280). KEEP the import.
    - _check_demotion uses ZERO SHADOW_* metrics. _check_promotion uses all 8 SHADOW_* metric imports.

    1. CALL SITE in _run_audit() (the `if row["is_shadow"]: await _check_promotion(pool, env_name, dict(row))` else `await _check_demotion(...)` branch): remove the promotion call. Replace the if/else with `if not row["is_shadow"]: await _check_demotion(pool, env_name, dict(row))` so shadow rows do nothing and non-shadow rows are demoted. Keep the swarm_agent skip and the trailing debug log.

    2. DELETE the entire `_check_promotion(...)` function (currently lines 113-252).

    3. DELETE promotion-exclusive pure functions: `_should_promote` and `_tail_risk_blocks_promotion`. KEEP `_should_demote` and `_ev_r_below_threshold` (demotion helpers).

    4. DELETE promotion-exclusive module constants: `_WIN_OUTCOMES`, `TAIL_GATE_MIN_SKEWNESS`, `TAIL_GATE_MIN_RECOVERY`, and the "Tail-risk gate thresholds" comment banner.

    5. REMOVE these 8 metric imports from the `from src.observability.metrics import (...)` block (used only by _check_promotion): SHADOW_DAYS_TO_GATE, SHADOW_EV_CI_LOWER, SHADOW_EV_R, SHADOW_N_RESOLVED, SHADOW_PROMOTION_READY, SHADOW_TAIL_GATE_DB_ERROR, SHADOW_TAIL_RISK_BLOCKED, SHADOW_WIN_RATE. KEEP JOB_COMPLETED_TOTAL and flush_and_shutdown_metrics.

    DO NOT touch: _check_demotion body, _should_demote, _ev_r_below_threshold, bootstrap_ci_lower import, _amain(), main(), the JOB_COMPLETED_TOTAL job label ("shadow-auditor"), or any demotion metrics.

    After the edit, confirm no remaining reference to any removed symbol exists in the file (grep for each removed name must return nothing).
  </action>
  <verify>cd /home/bg/dev/indicagent && .venv/bin/ruff check services/shadow_auditor.py && for sym in _check_promotion _should_promote _tail_risk_blocks_promotion _WIN_OUTCOMES TAIL_GATE_MIN_SKEWNESS TAIL_GATE_MIN_RECOVERY SHADOW_DAYS_TO_GATE SHADOW_EV_CI_LOWER SHADOW_EV_R SHADOW_N_RESOLVED SHADOW_PROMOTION_READY SHADOW_TAIL_RISK_BLOCKED SHADOW_WIN_RATE; do if grep -q "$sym" services/shadow_auditor.py; then echo "LEFTOVER: $sym"; exit 1; fi; done && grep -q "bootstrap_ci_lower" services/shadow_auditor.py && grep -q "_check_demotion" services/shadow_auditor.py && echo "demotion-only clean"</verify>
  <done>shadow_auditor.py is demotion-only: zero promotion-exclusive symbols remain, bootstrap_ci_lower + _check_demotion retained, imports cleanly.</done>
  <acceptance_criteria>
    - ruff passes on services/shadow_auditor.py with no unused-import or undefined-name errors
    - The verify loop prints "demotion-only clean" (zero leftovers for all 13 removed symbols)
    - grep "bootstrap_ci_lower" services/shadow_auditor.py still returns the import + the _check_demotion usage (KEPT)
    - grep "_check_promotion" services/shadow_auditor.py returns nothing
    - _run_audit contains a call to _check_demotion and no call to _check_promotion
    - .venv/bin/python -c "import services.shadow_auditor" succeeds (no ImportError from removed metric names)
    - If demotion unit tests exist (grep -rl "shadow_auditor" tests/), they remain green: .venv/bin/pytest -q $(grep -rl "shadow_auditor" tests/unit 2>/dev/null)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- shadow_auditor.py imports cleanly and passes ruff
- All 13 promotion-exclusive symbols removed; bootstrap_ci_lower and _check_demotion retained
- Existing demotion tests (if any) pass
</verification>

<success_criteria>
- shadow_auditor.py is demotion-only: no _check_promotion, no promotion-exclusive constants/functions/imports
- Demotion path and oneshot entry point unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/120-shadow-mode-validation/120-02-SUMMARY.md`
</output>
