---
phase: 120-shadow-mode-validation
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [SHADOW-01, SHADOW-02]
files_modified:
  - services/shadow_validator.py
  - src/observability/metrics.py
  - production/migrations/121_signal_ledger_shadow_view.sql
autonomous: true

must_haves:
  truths:
    - "A weekly oneshot script evaluates all 22 refactored I7 setups against a 5-gate promotion check"
    - "Setups passing all 5 gates are promoted by writing is_shadow=FALSE to shadow_registry"
    - "Setups failing any gate stay shadow and the failing gate + value + threshold is logged"
    - "Every run emits 6 OTel gauges per setup for all 22 setups (promoted or not)"
    - "signal_ledger_shadow view exists and returns only is_shadow=true rows from signal_ledger_full"
    - "All 5 gates use pnl_r-based outcome metrics — was_selected is NOT used (shadow signals are structurally excluded from winner selection and was_selected is always False)"
  artifacts:
    - path: "services/shadow_validator.py"
      provides: "Weekly oneshot 5-gate promotion validator"
      min_lines: 180
    - path: "src/observability/metrics.py"
      provides: "6 shadow_validation_* point gauges"
      contains: "SHADOW_VALIDATION_WIN_RATE"
    - path: "production/migrations/121_signal_ledger_shadow_view.sql"
      provides: "signal_ledger_shadow view"
      contains: "CREATE VIEW signal_ledger_shadow"
  key_links:
    - from: "services/shadow_validator.py"
      to: "shadow_registry"
      via: "UPDATE is_shadow=FALSE on promotion (WHERE is_shadow=TRUE guard)"
      pattern: "UPDATE shadow_registry"
    - from: "services/shadow_validator.py"
      to: "signal_ledger + signal_outcomes"
      via: "5-gate outcome metrics query (n_resolved, wins, avg_pnl_r, calibration_corr)"
      pattern: "FROM signal_ledger"
    - from: "services/shadow_validator.py"
      to: "topic_alert_requests"
      via: "CRITICAL Kafka alert per promotion"
      pattern: "topic_alert_requests"

threat_model:
  assets:
    - "shadow_registry promotion state (controls which I7 setups go live)"
    - "signal_ledger statistical sample (basis for promotion decisions)"
  threats:
    - threat: "False promotion from contaminated sample (pre-refactor signals counted)"
      severity: high
      mitigation: "Query filters WHERE shadow_tracking_start_ts IS NOT NULL so only post-refactor signals count (D-03)"
    - threat: "SQL injection via setup_plugin name"
      severity: low
      mitigation: "Plugin names are a hardcoded module constant frozenset; query uses parameterized $1 placeholder, never string interpolation"
    - threat: "Premature promotion below statistical confidence"
      severity: high
      mitigation: "5-gate sequential check requires N>=100 AND binomtest p<0.05 AND positive expectancy AND calibration>=0.3 before any write"
---

<objective>
Build the standalone weekly ShadowModeValidator oneshot script that gates promotion of the 21 refactored I7 setups through a 5-criteria sequential statistical check, plus the supporting OTel gauges and the signal_ledger_shadow DB view.

Purpose: Setups earn promotion through proof. Only setups with N>=100 resolved signals, meaningful selection rate, statistical significance, positive expectancy, and calibrated confidence graduate from shadow to live.
Output: services/shadow_validator.py (new), 6 new gauges in metrics.py, migration 121 (view).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-shadow-mode-validation/120-CONTEXT.md
@.planning/phases/120-shadow-mode-validation/120-RESEARCH.md
@.planning/phases/120-shadow-mode-validation/120-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 6 shadow_validation_* OTel point gauges to metrics.py</name>
  <files>src/observability/metrics.py</files>
  <read_first>
    - src/observability/metrics.py (read the existing shadow metrics block ending at SHADOW_TAIL_GATE_DB_ERROR near line 277, and the point_gauge() helper at line 82 to copy its exact signature)
    - .planning/phases/120-shadow-mode-validation/120-CONTEXT.md (D-08 — exact gauge names)
  </read_first>
  <action>
    Insert a new block immediately AFTER the existing SHADOW_TAIL_GATE_DB_ERROR definition (around line 280), introduced by a comment banner "Shadow validation metrics (Phase 120)".

    Create exactly these 6 module-level gauges using the existing `point_gauge(name, documentation)` helper (NOT create_gauge, NOT counter, NOT _meter.create_*):
    - `SHADOW_VALIDATION_N` = point_gauge("shadow_validation_n", "Resolved shadow outcome count per setup (weekly validator run)")
    - `SHADOW_VALIDATION_WIN_RATE` = point_gauge("shadow_validation_win_rate", "Fraction of resolved shadow outcomes with pnl_r > 0")
    - `SHADOW_VALIDATION_P_VALUE` = point_gauge("shadow_validation_p_value", "Binomial test p-value (win rate vs 50% baseline, one-sided)")
    - `SHADOW_VALIDATION_AVG_PNL_R` = point_gauge("shadow_validation_avg_pnl_r", "Average pnl_r across resolved shadow outcomes")
    - `SHADOW_VALIDATION_CALIBRATION` = point_gauge("shadow_validation_calibration", "CORR(cis_score, (pnl_r > 0)::int) — confidence predicts profitable outcomes")
    - `SHADOW_VALIDATION_PROMOTED` = point_gauge("shadow_validation_promoted", "1=promoted to live this run, 0=still in shadow")

    The label key used at call sites will be "setup_plugin" (set in Task 3) — point_gauge takes only name + documentation, labels are passed at .set() time. Do NOT pass a labels list to point_gauge.
  </action>
  <verify>.venv/bin/python -c "from src.observability.metrics import SHADOW_VALIDATION_N, SHADOW_VALIDATION_WIN_RATE, SHADOW_VALIDATION_P_VALUE, SHADOW_VALIDATION_AVG_PNL_R, SHADOW_VALIDATION_CALIBRATION, SHADOW_VALIDATION_PROMOTED; print('ok')"</verify>
  <done>All 6 SHADOW_VALIDATION_* point gauges defined in metrics.py and importable.</done>
  <acceptance_criteria>
    - All 6 names import successfully via the verify command printing "ok"
    - grep -c "point_gauge(\"shadow_validation_" src/observability/metrics.py returns 6
    - SHADOW_VALIDATION_WIN_RATE exists (not SHADOW_VALIDATION_SELECTION_RATE)
    - No occurrence of `create_gauge(` or `_meter.create_counter(` introduced for these 6 metrics
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create migration 121 signal_ledger_shadow view</name>
  <files>production/migrations/121_signal_ledger_shadow_view.sql</files>
  <read_first>
    - production/migrations/095_signal_ledger_split.sql (view creation pattern + confirm signal_ledger_full defines is_shadow, was_selected, cis_score, pnl_r)
    - .planning/phases/120-shadow-mode-validation/120-CONTEXT.md (D-04)
  </read_first>
  <action>
    Create new file production/migrations/121_signal_ledger_shadow_view.sql.

    File number MUST be 121 — migration 120 (120_signal_probe_results.sql) already exists and is deployed; reusing 120 is a collision.

    Content: a header comment explaining the view provides a clean nameable interface for shadow-specific queries, base is signal_ledger_full (Phase 095, JOINs signal_ledger + signal_outcomes), then a single statement:
    CREATE VIEW signal_ledger_shadow AS SELECT * FROM signal_ledger_full WHERE is_shadow = true;

    No schema changes, no new columns, no DROP. View only.
  </action>
  <verify>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/121_signal_ledger_shadow_view.sql && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tc "SELECT count(*) FROM signal_ledger_shadow LIMIT 1;"</verify>
  <done>signal_ledger_shadow view exists in the live DB and returns is_shadow=true rows.</done>
  <acceptance_criteria>
    - File production/migrations/121_signal_ledger_shadow_view.sql exists
    - Applying the migration succeeds with no psql error
    - `SELECT viewname FROM pg_views WHERE viewname='signal_ledger_shadow'` returns one row after apply
    - Source contains exactly the string "CREATE VIEW signal_ledger_shadow AS" and "WHERE is_shadow = true"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement shadow_validator.py oneshot with 5-gate promotion check</name>
  <files>services/shadow_validator.py</files>
  <read_first>
    - services/shadow_auditor.py (copy script skeleton verbatim: _path_bootstrap import at line 17, _amain()/main() at lines 336-358, JOB_COMPLETED_TOTAL + flush_and_shutdown_metrics usage, create_db_pool call, asyncpg query pattern)
    - services/alert_monitor.py (lines 70-72 — alert payload reads "severity", "message", "source"; NOT "title"/"body")
    - src/core/stream_keys.py (topic_alert_requests signature at line 295 — takes env_name)
    - src/core/kafka_utils.py (KafkaProducerClient.__init__(bootstrap_servers) at line 62, start() at 66, publish(topic, msg, key=None) at 95 — topic is POSITIONAL, body kwarg is msg= not value=)
    - src/core/database_manager.py (create_pool(database_url, ...) at line 25)
    - .planning/phases/120-shadow-mode-validation/120-CONTEXT.md (D-02 5-gate logic, D-05 write path, D-06 setup list, D-07 alert, D-08 metrics)
    - .planning/phases/120-shadow-mode-validation/120-RESEARCH.md (Code Examples section — _check_promotion_criteria reference, shadow_transition_log column mapping)
  </read_first>
  <action>
    Create services/shadow_validator.py following services/shadow_auditor.py structure exactly.

    IMPORTS: `from __future__ import annotations`, then stdlib (asyncio, datetime UTC/datetime), then `import _path_bootstrap  # noqa: F401` as first non-stdlib import, then asyncpg, structlog, `from scipy.stats import binomtest`, then src imports: Settings, create_pool as create_db_pool, KafkaProducerClient, topic_alert_requests, and from src.observability.metrics import JOB_COMPLETED_TOTAL, flush_and_shutdown_metrics + the 6 SHADOW_VALIDATION_* gauges from Task 1.

    MODULE CONSTANT `_SHADOW_VALIDATION_SETUPS: frozenset[str]` containing exactly these 22 verified names (verified 2026-06-10 against _PHASE_119_PLUGINS in register_plugins.py:667; authoritative source cited in comment):
    Phase 118 (5): trad_OFIContinuation, trad_PatternCompletion, trad_GapAnalysisSetup, trad_CVDDivergence, trad_DivergenceStack
    Phase 119 (17): trad_OFISpike, trad_CVDSpike, trad_OFIDivergence, trad_FailedBreakout, trad_CandlestickPatternSetup, trad_SessionExtremesSetup, trad_LiquidityHunt, trad_DeltaExhaustion, trad_LVNBreakout, trad_VWAPReclaim, trad_VWAPDeviation, trad_MomentumBreakout, trad_ORB15, trad_ORB30, trad_SecondLegContinuation, trad_VCP, trad_DualDivergence
    Total = 22 (assert len == 22 at module load).

    PURE GATE FUNCTION `_check_promotion_criteria(n_resolved, wins, avg_pnl_r, calibration_corr) -> tuple[bool, str, str]` returning (should_promote, failure_reason, detail). Sequential short-circuit, cheapest first, per D-02:
    - n_resolved < 100 -> (False, "insufficient_n", f"{n_resolved} resolved < 100")
    - win_rate = wins/n_resolved; if < 0.5 -> (False, "low_win_rate", f"{win_rate:.1%} < 50%")
    - result = binomtest(wins, n_resolved, p=0.5, alternative="greater"); if result.pvalue >= 0.05 -> (False, "not_significant", f"p={result.pvalue:.3f} >= 0.05")
    - if avg_pnl_r is None or avg_pnl_r <= 0 -> (False, "negative_expectancy", f"avg_pnl_r={avg_pnl_r}")
    - if calibration_corr is None or calibration_corr < 0.3 -> (False, "poor_calibration", f"corr={calibration_corr} < 0.3")
    - else (True, "", "")

    PER-SETUP QUERY (one fetchrow per setup, parameterized $1 = plugin_name):
    SELECT COUNT(*) FILTER (WHERE so.pnl_r IS NOT NULL) AS n_resolved, COUNT(*) FILTER (WHERE so.pnl_r > 0) AS wins, AVG(so.pnl_r) FILTER (WHERE so.pnl_r IS NOT NULL) AS avg_pnl_r, CORR(sl.cis_score, (so.pnl_r > 0)::int) FILTER (WHERE so.pnl_r IS NOT NULL) AS calibration_corr FROM signal_ledger sl LEFT JOIN signal_outcomes so USING (signal_id) WHERE sl.setup_plugin = $1 AND sl.is_shadow = true AND sl.shadow_tracking_start_ts IS NOT NULL
    Treat n_resolved of 0/None as 0; avg_pnl_r/calibration_corr may be None.

    NOTE — do NOT use was_selected: shadow signals are excluded from winner selection by design (signal_processor.py:410-416 builds eligible_ranked from non-shadow only). was_selected is structurally always False for shadow signals, making any gate based on it permanently unpassable.

    MAIN LOOP `_run_validator(pool, settings)`: iterate the 22 setups. For each: run query, compute win_rate and the binomtest pvalue (compute pvalue for metric emission even when earlier gate failed — guard n_resolved>0 to avoid div-by-zero; emit p_value gauge as 1.0 when n_resolved==0). Call _check_promotion_criteria. ALWAYS emit all 6 gauges with label {"setup_plugin": name}: N=n_resolved, win_rate (rounded 4), p_value (rounded 4), avg_pnl_r (rounded 4, 0.0 if None), calibration (rounded 4, 0.0 if None), promoted (1 if promoted else 0). On gate failure: logger.info("shadow_validator.gate_fail", plugin=name, reason=reason, detail=detail). On all 5 passing: perform promotion write (below) then logger.info("shadow_validator.promoted", plugin=name, n=n_resolved, ...).

    PROMOTION WRITE (only on full pass):
    - UPDATE shadow_registry SET is_shadow=FALSE, promoted_at=$1, demotion_consecutive_count=0 WHERE component_name=$2 AND is_shadow=TRUE  (params: datetime.now(UTC), plugin_name — the AND is_shadow=TRUE guard prevents re-promoting an already-demoted setup if the validator runs concurrently with shadow_auditor)
    - INSERT INTO shadow_transition_log (component_name, component_type, from_state, to_state, trigger_reason, n, ev_r, ci_lower, win_rate) VALUES ($1, 'i7_plugin', 'shadow', 'live', 'shadow_validator_5gate', $2, $3, $4, $5)  (params: plugin_name, n_resolved, avg_pnl_r, calibration_corr, win_rate — ci_lower stores calibration_corr, win_rate stores actual win_rate; semantically correct columns)

    PROMOTION ALERT (only on full pass), Kafka optional: construct producer = KafkaProducerClient(bootstrap_servers=settings.kafka_bootstrap_servers), await producer.start() once before the loop (or lazily). Publish via positional topic: await producer.publish(topic_alert_requests(settings.env_name), {"severity": "CRITICAL", "message": f"Shadow Promotion: {name}\nN={n_resolved}, win_rate={win_rate:.1%}, p={pvalue:.3f}, avg_pnl_r={avg_pnl_r:.3f}, calibration={calibration_corr:.3f}", "source": "shadow_validator"}). Wrap Kafka start/publish in try/except Exception as error: logger.warning("shadow_validator.kafka_unavailable", error=str(error)) and continue — promotion DB write must NOT be rolled back if Kafka fails.

    ENTRY POINT: copy shadow_auditor.py _amain()/main() exactly but call _run_validator and use job label "shadow-validator":
    main() wraps asyncio.run(_amain()); on success JOB_COMPLETED_TOTAL.add(1, {"job": "shadow-validator", "status": "success"}); on Exception as error add status "failure" and re-raise; finally flush_and_shutdown_metrics().

    Use datetime.now(UTC) only. Exception variable name is `error`. Never use event= structlog kwarg.
  </action>
  <verify>.venv/bin/python -c "import ast,sys; ast.parse(open('services/shadow_validator.py').read()); print('parse ok')" && .venv/bin/ruff check services/shadow_validator.py && .venv/bin/python -c "from services.shadow_validator import _SHADOW_VALIDATION_SETUPS, _check_promotion_criteria; assert len(_SHADOW_VALIDATION_SETUPS)==22, len(_SHADOW_VALIDATION_SETUPS); assert _check_promotion_criteria(50,30,1.0,0.5)==(False,'insufficient_n','50 resolved < 100'); assert _check_promotion_criteria(200,90,1.0,0.5)==(False,'low_win_rate','45.0% < 50%'); assert _check_promotion_criteria(200,150,0.5,0.5)[0] is True; assert _check_promotion_criteria(200,150,-0.1,0.5)[1]=='negative_expectancy'; assert _check_promotion_criteria(200,150,0.5,0.1)[1]=='poor_calibration'; print('gates ok')"</verify>
  <done>shadow_validator.py runs the 5-gate check over all 22 setups, promotes on full pass, emits 6 gauges per setup.</done>
  <acceptance_criteria>
    - services/shadow_validator.py parses, passes ruff, and the verify command prints "gates ok"
    - len(_SHADOW_VALIDATION_SETUPS) == 22 with all 22 names matching the list in <action>
    - _check_promotion_criteria short-circuits in the documented order (insufficient_n before low_win_rate before not_significant before negative_expectancy before poor_calibration)
    - Source contains "from scipy.stats import binomtest", "import _path_bootstrap", "UPDATE shadow_registry", "AND is_shadow=TRUE", "INSERT INTO shadow_transition_log", "topic_alert_requests", JOB_COMPLETED_TOTAL job label "shadow-validator", and "flush_and_shutdown_metrics"
    - Source does NOT contain "was_selected" (wrong metric — shadow signals are excluded from winner selection)
    - Alert payload dict uses keys "severity"/"message"/"source" and NOT "title"/"body"
    - producer.publish call passes topic positionally (publish(topic_alert_requests(...), {...}))
    - SHADOW_VALIDATION_WIN_RATE imported (not SHADOW_VALIDATION_SELECTION_RATE)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- All three artifacts exist and import/apply cleanly
- 5-gate logic verified by the parametrized assertions in Task 3 verify
- View returns rows from signal_ledger_full filtered to is_shadow=true
- 6 gauges importable from metrics.py
</verification>

<success_criteria>
- A new weekly oneshot validator evaluates all 21 setups through the locked 5-gate check
- Promotion writes is_shadow=FALSE to shadow_registry and logs a transition; failures log the failing gate
- All 6 OTel gauges emit per setup every run
- signal_ledger_shadow view exists
</success_criteria>

<output>
After completion, create `.planning/phases/120-shadow-mode-validation/120-01-SUMMARY.md`
</output>
