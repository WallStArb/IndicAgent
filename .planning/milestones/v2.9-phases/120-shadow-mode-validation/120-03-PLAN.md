---
phase: 120-shadow-mode-validation
plan: 03
type: execute
wave: 2
depends_on: ["120-01"]
requirements: [SHADOW-03]
files_modified:
  - production/systemd/indicagent-shadow-validator.timer
  - production/systemd/indicagent-shadow-validator.service
  - services/service_auditor.py
  - production/grafana/dashboards/shadow-validation.json
  - docs/cheatsheet.md
autonomous: true

must_haves:
  truths:
    - "A weekly systemd timer (Mon 07:00 UTC) triggers the shadow_validator oneshot service"
    - "indicagent-shadow-validator is registered in service_auditor DAG and oneshot units"
    - "A Grafana dashboard shows per-setup N, selection_rate, p_value, avg_pnl_r, calibration, and promoted status"
  artifacts:
    - path: "production/systemd/indicagent-shadow-validator.timer"
      provides: "Weekly Mon 07:00 UTC timer"
      contains: "OnCalendar=Mon"
    - path: "production/systemd/indicagent-shadow-validator.service"
      provides: "oneshot service running shadow_validator.py"
      contains: "Type=oneshot"
    - path: "production/grafana/dashboards/shadow-validation.json"
      provides: "Shadow validation observability dashboard"
      contains: "shadow_validation_n"
  key_links:
    - from: "indicagent-shadow-validator.timer"
      to: "indicagent-shadow-validator.service"
      via: "Unit= directive"
      pattern: "Unit=indicagent-shadow-validator.service"
    - from: "indicagent-shadow-validator.service"
      to: "services/shadow_validator.py"
      via: "ExecStart"
      pattern: "shadow_validator.py"
    - from: "production/grafana/dashboards/shadow-validation.json"
      to: "shadow_validation_* metrics"
      via: "Prometheus PromQL targets"
      pattern: "shadow_validation_"

threat_model:
  assets:
    - "systemd unit definitions (control when promotion gate runs)"
    - "service_auditor health registry (operator visibility)"
  threats:
    - threat: "Validator job not visible to health monitoring, silent failures unnoticed"
      severity: medium
      mitigation: "Register indicagent-shadow-validator in _DAG_ORDER and _ONESHOT_UNITS so JOB_COMPLETED_TOTAL and oneshot health checks track it"
    - threat: "Timer misfires off-hours mid-trading-week corrupting partial state"
      severity: low
      mitigation: "Validator is read-mostly; only writes shadow_registry on full 5-gate pass. Mon 07:00 UTC chosen after weekend data settles. Persistent=true catches missed fires."
---

<objective>
Operationalize the shadow_validator: add the weekly systemd timer + oneshot service, register the unit in service_auditor health monitoring, ship the Grafana dashboard, and document the promotion ceremony.

Purpose: A planned but unscheduled validator never runs. This plan wires the script into the systemd + observability fabric so it executes weekly and operators can see each setup's progress toward the N=100 promotion gate.
Output: timer + service units, service_auditor registration, Grafana dashboard, cheatsheet entry.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-shadow-mode-validation/120-CONTEXT.md
@.planning/phases/120-shadow-mode-validation/120-RESEARCH.md
@.planning/phases/120-shadow-mode-validation/120-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create systemd timer + service units and register in service_auditor</name>
  <files>production/systemd/indicagent-shadow-validator.timer, production/systemd/indicagent-shadow-validator.service, services/service_auditor.py</files>
  <read_first>
    - production/systemd/indicagent-hmm-training.timer (canonical oneshot timer pattern)
    - production/systemd/indicagent-hmm-training.service (canonical oneshot service pattern — User, WorkingDirectory, PYTHONPATH, ExecStart, TimeoutStartSec)
    - services/service_auditor.py (lines 57-104 _DAG_ORDER incl. existing "indicagent-shadow-auditor": 8; lines 169-185 _ONESHOT_UNITS incl. "indicagent-shadow-auditor")
    - .planning/phases/120-shadow-mode-validation/120-CONTEXT.md (D-09 timer spec)
    - .planning/phases/120-shadow-mode-validation/120-PATTERNS.md (systemd + service_auditor sections)
  </read_first>
  <action>
    Create production/systemd/indicagent-shadow-validator.timer:
    [Unit] Description="Shadow Mode Validator Timer -- weekly 5-gate promotion check"
    [Timer] OnCalendar=Mon *-*-* 07:00:00 UTC ; Persistent=true ; Unit=indicagent-shadow-validator.service
    [Install] WantedBy=timers.target

    Create production/systemd/indicagent-shadow-validator.service (mirror hmm-training.service):
    [Unit] Description="IndicAgent Shadow Mode Validator -- weekly 5-gate promotion script" ; After=network.target indicagent-infrastructure.target ; Requires=indicagent-infrastructure.target
    [Service] Type=oneshot ; Restart=no ; User=bg ; WorkingDirectory=/home/bg/dev/indicagent ; Environment=PYTHONPATH=/home/bg/dev/indicagent ; Environment=PYTHONUNBUFFERED=1 ; ExecStart=/home/bg/dev/indicagent/.venv/bin/python services/shadow_validator.py ; TimeoutStartSec=300
    [Install] WantedBy=multi-user.target

    Edit services/service_auditor.py:
    - Add to _DAG_ORDER (alongside the existing "indicagent-shadow-auditor": 8 entry): "indicagent-shadow-validator": 8,  with a comment "oneshot: weekly Mon 07:00 UTC, promotion-only".
    - Add to _ONESHOT_UNITS (after "indicagent-shadow-auditor"): "indicagent-shadow-validator".
    - Do NOT add to _AGENT_ID_TO_UNIT (that dict maps BaseDaemon agents; shadow_validator is a oneshot script, not a BaseDaemon).
    - If _LAG_THRESHOLDS exists and contains the shadow-auditor entry as None/omitted, follow the same treatment (oneshots have no consumer lag).
  </action>
  <verify>cd /home/bg/dev/indicagent && grep -q "OnCalendar=Mon \*-\*-\* 07:00:00 UTC" production/systemd/indicagent-shadow-validator.timer && grep -q "Unit=indicagent-shadow-validator.service" production/systemd/indicagent-shadow-validator.timer && grep -q "Type=oneshot" production/systemd/indicagent-shadow-validator.service && grep -q "services/shadow_validator.py" production/systemd/indicagent-shadow-validator.service && grep -q '"indicagent-shadow-validator"' services/service_auditor.py && .venv/bin/ruff check services/service_auditor.py && systemd-analyze verify production/systemd/indicagent-shadow-validator.service 2>&1 | grep -vi "warning" || true</verify>
  <done>Weekly timer + oneshot service exist; indicagent-shadow-validator registered in _DAG_ORDER and _ONESHOT_UNITS.</done>
  <acceptance_criteria>
    - Both unit files exist with the exact directives above (verify greps pass)
    - Timer OnCalendar is "Mon *-*-* 07:00:00 UTC" and Persistent=true
    - Service ExecStart points at the venv python and services/shadow_validator.py, Type=oneshot, TimeoutStartSec=300
    - "indicagent-shadow-validator" appears in BOTH _DAG_ORDER and _ONESHOT_UNITS in service_auditor.py
    - "indicagent-shadow-validator" does NOT appear in _AGENT_ID_TO_UNIT
    - ruff passes on service_auditor.py
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create Grafana shadow-validation dashboard and document promotion ceremony</name>
  <files>production/grafana/dashboards/shadow-validation.json, docs/cheatsheet.md</files>
  <read_first>
    - production/grafana/dashboards/operations.json (structure: panels array, datasource uid, table fieldConfig/thresholds, schemaVersion, uid, tags)
    - production/grafana/dashboards/plugin-latency.json (example of per-plugin label grouping in a PromQL target)
    - docs/cheatsheet.md (find the services / shadow section to append the new entry consistently)
    - .planning/phases/120-shadow-mode-validation/120-CONTEXT.md (Specific Ideas — dashboard panels)
    - .planning/phases/120-shadow-mode-validation/120-PATTERNS.md (dashboard structure pattern)
  </read_first>
  <action>
    Create production/grafana/dashboards/shadow-validation.json modeled on operations.json. Required structure: top-level keys annotations, editable=true, graphTooltip, panels[], schemaVersion (match operations.json value, e.g. 38), tags=["shadow","validation"], title="Shadow Mode Validation", uid="shadow-validation", version=1. Datasource: {"type":"prometheus","uid":"prometheus"} (match the uid used by operations.json — read it, do not assume).

    Panels (3):
    1. Row header "Shadow Validation Status".
    2. Table panel titled "Per-Setup Promotion Metrics" with these PromQL targets each using instant=true and `format: "table"`, grouped by setup_plugin label: shadow_validation_n, shadow_validation_win_rate, shadow_validation_p_value, shadow_validation_avg_pnl_r, shadow_validation_calibration, shadow_validation_promoted. Add a thresholds fieldConfig coloring shadow_validation_promoted: red at null/0, green at 1.
    3. Time-series panel titled "N Accumulation Toward Gate (N=100)" with target shadow_validation_n grouped by setup_plugin (legend {{setup_plugin}}), plus a threshold/annotation marker at y=100.

    All metric names MUST match the 6 gauges created in Plan 01 exactly (shadow_validation_n, shadow_validation_win_rate, shadow_validation_p_value, shadow_validation_avg_pnl_r, shadow_validation_calibration, shadow_validation_promoted). The label key is setup_plugin.

    The JSON MUST be valid (parseable). Append a "Shadow Mode Validation" entry to docs/cheatsheet.md describing: weekly timer (Mon 07:00 UTC), 5-gate criteria summary (N>=100, selection_rate>=5%, binomtest p<0.05, avg_pnl_r>0, calibration>=0.3), manual run command (.venv/bin/python services/shadow_validator.py), and the dashboard name. Match the existing cheatsheet formatting.
  </action>
  <verify>cd /home/bg/dev/indicagent && .venv/bin/python -c "import json; d=json.load(open('production/grafana/dashboards/shadow-validation.json')); assert d['uid']=='shadow-validation'; assert d['title']=='Shadow Mode Validation'; body=json.dumps(d); [body.index(m) for m in ['shadow_validation_n','shadow_validation_win_rate','shadow_validation_p_value','shadow_validation_avg_pnl_r','shadow_validation_calibration','shadow_validation_promoted']]; assert 'setup_plugin' in body; print('dashboard ok')" && grep -qi "shadow" docs/cheatsheet.md && grep -q "services/shadow_validator.py" docs/cheatsheet.md</verify>
  <done>Valid Grafana dashboard JSON referencing all 6 shadow_validation_* metrics by setup_plugin; cheatsheet documents the ceremony + manual run command.</done>
  <acceptance_criteria>
    - production/grafana/dashboards/shadow-validation.json is valid JSON (python json.load succeeds)
    - Dashboard uid=="shadow-validation", title=="Shadow Mode Validation"
    - All 6 shadow_validation_* metric names present in the dashboard JSON (the verify .index() calls all succeed -> "dashboard ok")
    - The label key "setup_plugin" appears in the dashboard JSON
    - docs/cheatsheet.md contains a Shadow Mode Validation entry including the manual-run command "services/shadow_validator.py"
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Both systemd units exist with correct directives; service_auditor registers the unit in DAG + oneshot sets
- Grafana dashboard is valid JSON referencing all 6 metrics by exact name
- cheatsheet documents the weekly promotion ceremony and manual run command
</verification>

<success_criteria>
- Weekly timer triggers shadow_validator.py oneshot at Mon 07:00 UTC
- Unit visible to service_auditor health monitoring
- Operators can read per-setup promotion metrics and N-toward-gate in Grafana
</success_criteria>

<output>
After completion, create `.planning/phases/120-shadow-mode-validation/120-03-SUMMARY.md`
</output>
