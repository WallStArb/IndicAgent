---
phase: 110-renaissance-rename
plan: 04
type: execute
wave: 4
depends_on: ["110-03"]
requirements_addressed: [RENAME-04]
files_modified:
  - services/*.py (33 Ring 2 daemon file renames via git mv)
  - services/ml_training_agent.py (launcher import fix — imports renamed ml_trainer module)
  - services/hmm_training_agent.py (launcher import fix — imports renamed hmm_trainer module)
  - src/intelligence/services/feature_validation_compute_agent.py (rename to feature_validation_analyzer.py)
  - tests/unit/services/test_feature_validation_compute_agent.py (rename to test_feature_validation_analyzer.py + import fix)
  - production/systemd/*.service (ExecStart path updates)
  - src/config/outbox_dispatcher.py
  - src/api/routes/narrative.py
  - src/api/routes/health.py
  - src/api/routes/drift.py
  - src/api/routes/ai_stats.py
  - src/api/routes/signals.py
  - dashboard/src/hooks/use-observability-stream.ts
  - CLAUDE.md
  - docs/foundation/naming-conventions.md
autonomous: true

must_haves:
  truths:
    - "Ring 2 service file names no longer carry the _agent suffix; each file name derives mechanically from its renamed class (bar_aggregator_agent.py -> bar_aggregator.py)"
    - "Every systemd unit ExecStart points at the renamed file path; no unit references a non-existent file"
    - "API routes import the renamed Ring 1/2 classes (NarrativeSynthesizer); Tier 3 Python abbreviations (bar_ctx, i7_ctx, resp, exc) are expanded to full words"
    - "CLAUDE.md Pending renames table is removed and all inline old-name references are updated to new names"
    - "The two launcher scripts (ml_training_agent.py, hmm_training_agent.py) import the renamed ml_trainer/hmm_trainer modules and import cleanly; feature_validation_compute_agent.py is renamed to feature_validation_analyzer.py with its test file"
    - "pytest tests/unit/ passes; ruff check . passes; mypy introduces no new errors"
  artifacts:
    - path: "services/bar_aggregator.py"
      provides: "renamed file (was bar_aggregator_agent.py)"
      contains: "class BarAggregator(BaseDaemon)"
    - path: "production/systemd/indicagent-bar-aggregator.service"
      provides: "ExecStart pointing at renamed file"
      contains: "services/bar_aggregator.py"
    - path: "src/api/routes/narrative.py"
      provides: "import of NarrativeSynthesizer; bar_context/i7_context full words"
      contains: "import NarrativeSynthesizer"
    - path: "services/ml_training_agent.py"
      provides: "launcher importing renamed ml_trainer module"
      contains: "from src.intelligence.services.ml_trainer import MLTrainer"
    - path: "src/intelligence/services/feature_validation_analyzer.py"
      provides: "renamed file (was feature_validation_compute_agent.py)"
      contains: "class FeatureValidationAnalyzer"
    - path: "CLAUDE.md"
      provides: "updated naming references, Pending renames table removed"
  key_links:
    - from: "production/systemd/*.service ExecStart"
      to: "services/*.py renamed files"
      via: "python services/<new_name>.py"
      pattern: "ExecStart=.*services/"
    - from: "src/api/routes/narrative.py"
      to: "src/intelligence/ai/narrative/narrative_agent.py"
      via: "import NarrativeSynthesizer"
      pattern: "import NarrativeSynthesizer"
---

<objective>
Wave 4 of the atomic rename: file-name renames following the class renames, systemd ExecStart updates, API import + Tier 3 abbreviation fixes, dashboard reconciliation, and CLAUDE.md update. Depends on Wave 3 (Plan 03) because file names derive from the now-renamed Ring 2 classes.

SYSTEMD NAMING DECISION (plan-review consensus — Gemini + Codex):
- Systemd unit FILENAMES (`indicagent-bar-aggregator.service`, `indicagent-service-auditor.service`, etc.) are PRESERVED unchanged. They already use role-noun form from prior naming work and are the stable "external identity" of each service (what `systemctl enable/disable/status` commands target).
- Only the `ExecStart=` SCRIPT PATH inside each unit changes (`services/bar_aggregator_agent.py` -> `services/bar_aggregator.py`).
- This prevents any `systemctl enable/disable/status` command from breaking across the rename, and keeps the `_AGENT_ID_TO_UNIT` unit-name values valid (they already match the preserved unit filenames).

Scope:
1. Rename Ring 2 service files to drop the `_agent` suffix (and align `*_service.py` / module names to the new class). `bar_aggregator_agent.py` -> `bar_aggregator.py`, etc. Also rename `src/intelligence/services/feature_validation_compute_agent.py` -> `feature_validation_analyzer.py` (its class was renamed to `FeatureValidationAnalyzer` in Wave 3), its test file, and fix the two launcher scripts' imports (`services/ml_training_agent.py`, `services/hmm_training_agent.py`).
2. Update every `production/systemd/*.service` ExecStart that references a renamed file path.
3. API routes: import `NarrativeSynthesizer` (was `NarrativeComputeAgent` — already renamed in Wave 2, fix the import-site if any module path changed); expand Tier 3 abbreviations: `bar_ctx`->`bar_context`, `i7_ctx`->`i7_context` (src/api/routes/narrative.py), `resp`->`response` (src/api/routes/health.py), `except Exception as exc:`->`except Exception as error:` (drift.py, ai_stats.py, narrative.py, signals.py).
4. Dashboard: reconcile `use-observability-stream.ts`.
5. CLAUDE.md: remove "Pending renames" bullet list and update all inline old-name references.
6. Replace `docs/foundation/naming-conventions.md` with a redirect to the new spec.

CRITICAL CONFLICT RESOLUTION — dashboard display strings vs agent_id exception:
The design spec Section 9 says to update `use-observability-stream.ts` display strings (e.g. `"AlphaSwarmComputeAgent"` -> `"AlphaSwarm"`). HOWEVER, verification of the actual file shows these strings are the VALUES of `UNIT_TO_AGENT` and the KEY of `agentAge["AlphaSwarmComputeAgent"]` (line 213), and they are matched against the Prometheus `agent` label (`r.labels["agent"]`, line 207). That `agent` label equals the `name="..."` constructor argument, which Plan 03 PRESERVED unchanged under the `agent_id` operational exception.

Therefore: changing these dashboard strings to the new class names would BREAK the dashboard's Prometheus lookup (the metric still reports the old agent_id). The `agent_id` operational exception (CLAUDE.md + naming spec Section 10) OVERRIDES the Section 9 dashboard-string instruction here, because Section 10 is explicit that `agent_id` stays for operational compatibility and Section 9's dashboard table predates recognizing these strings ARE agent_id values.

RESOLUTION: Do NOT change the `UNIT_TO_AGENT` values, the `agentAge[...]` keys, or any string that is matched against the Prometheus `agent` label. Add a short code comment in the file explaining these strings are agent_id values preserved by the operational exception. This is the operationally-correct interpretation; record the deviation from Section 9 in the SUMMARY.

Purpose: Make file names, systemd units, and Ring 3 surfaces consistent with the renamed classes; complete the atomic rename so the branch can fast-forward merge to main.
Output: renamed files, updated systemd/API/CLAUDE.md; full CI green; phase complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-renaissance-rename/110-CONTEXT.md
@docs/foundation/naming-system.md
@docs/plans/2026-05-30-renaissance-naming-system-design.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv Ring 2 service files to drop _agent suffix; update imports + systemd ExecStart</name>
  <files>services/*.py (33 Ring 2 daemon renames), services/ml_training_agent.py, services/hmm_training_agent.py, production/systemd/*.service, src/intelligence/services/*.py, tests/unit/services/test_feature_validation_compute_agent.py, src/config/outbox_dispatcher.py</files>
  <read_first>
    - services/ directory listing (the *_agent.py daemon files + cross_asset_service.py + llm_writer_service.py; 33 of these are renamed, the 2 ml/hmm-training launcher *_agent.py files are NOT renamed — see mapping below)
    - production/systemd/indicagent-bar-aggregator.service (ExecStart=...services/bar_aggregator_agent.py — the pattern across all units)
    - One test that imports a service module path, e.g. tests/integration/test_all_signals_resolved.py (`from services.signal_replay_auditor_agent import SignalReplayAuditor`)
    - src/config/outbox_dispatcher.py (stays in src/config/; class already OutboxPublisher from Wave 3 — decide if file renames to outbox_publisher.py)
    - services/ml_training_agent.py:16 (`from src.intelligence.services.ml_training_compute_agent import MLTrainingComputeAgent` — this launcher imports the module being renamed; BOTH the module path and class symbol change after Wave 3 + this task)
    - services/hmm_training_agent.py:19 (`from src.intelligence.services.hmm_training_compute_agent import HMMTrainingComputeAgent` — same launcher import problem)
    - src/intelligence/services/feature_validation_compute_agent.py (class is FeatureValidationAnalyzer after Wave 3; file renames to feature_validation_analyzer.py here)
    - tests/unit/services/test_feature_validation_compute_agent.py (imports from the module path being renamed; rename test file + fix import)
  </read_first>
  <action>
    On branch `rename/phase-110`. Rename each Ring 2 service file so the file name matches its (already renamed) class in snake_case, dropping the `_agent` suffix. Use `git mv` to preserve history. The mapping (current file -> new file), derived mechanically from the Wave 3 class names:

    - intelligence_pipeline_agent.py -> intelligence_pipeline.py
    - alpha_swarm_agent.py -> alpha_swarm.py
    - narrative_group_compute_agent.py -> narrative_swarm.py
    - bar_aggregator_agent.py -> bar_aggregator.py
    - provider_merger_agent.py -> provider_merger.py
    - cross_asset_service.py -> cross_asset_analyzer.py
    - macro_compute_agent.py -> macro_analyzer.py
    - signal_metrics_compute_agent.py -> signal_metrics_analyzer.py
    - graduation_compute_agent.py -> graduation_analyzer.py
    - ml_discovery_agent.py -> ml_discovery_analyzer.py
    - signal_tracker_compute_agent.py -> signal_tracker.py
    - alerting_agent.py -> alert_monitor.py
    - ml_orchestrator_agent.py -> ml_orchestrator.py
    - bar_writer_agent.py -> bar_writer.py
    - feature_writer_agent.py -> feature_writer.py
    - signal_writer_agent.py -> signal_writer.py
    - lifecycle_writer_agent.py -> lifecycle_writer.py
    - lineage_writer_agent.py -> lineage_writer.py
    - llm_writer_service.py -> llm_writer.py
    - ctx_writer_agent.py -> context_writer.py
    - swarm_ledger_writer_agent.py -> swarm_ledger_writer.py
    - signal_metrics_writer_agent.py -> signal_metrics_writer.py
    - graduation_writer_agent.py -> graduation_writer.py
    - bar_auditor_agent.py -> bar_auditor.py
    - signal_auditor_agent.py -> signal_auditor.py
    - signal_replay_auditor_agent.py -> signal_replay_auditor.py
    - service_auditor_agent.py -> service_auditor.py
    - ml_data_quality_agent.py -> data_quality_auditor.py
    - bar_replay_provider_agent.py -> bar_replay_provider.py
    - ibkr_provider_agent.py -> ibkr_provider.py
    - src/intelligence/services/ml_training_compute_agent.py -> ml_trainer.py
    - src/intelligence/services/hmm_training_compute_agent.py -> hmm_trainer.py
    - src/intelligence/services/feature_validation_compute_agent.py -> feature_validation_analyzer.py
    - src/config/outbox_dispatcher.py -> outbox_publisher.py

    Test file rename (mirrors the source rename):
    - tests/unit/services/test_feature_validation_compute_agent.py -> tests/unit/services/test_feature_validation_analyzer.py (and fix its `from src.intelligence.services.feature_validation_analyzer import FeatureValidationAnalyzer` import)

    IMPORTANT — service_auditor_agent.py is referenced by name in MEMORY/CLAUDE.md as the canonical `_DAG_ORDER` registry. Renaming the file to service_auditor.py is correct; update any `from services.service_auditor_agent import` references.

    Process:
    1. For each: `git mv services/<old>.py services/<new>.py` (and the src/ ones to their dirs).
    2. Update EVERY import that references the old module path. Find them:
       `git grep -ln "services\.bar_aggregator_agent\|services\.alpha_swarm_agent\|..." -- '*.py'` — or more robustly, after all git mv's, run pytest/ruff which will surface broken imports, then fix each `from services.<old> import` / `import services.<old>` to the new module name across `src/`, `services/`, `tests/`, `production/scripts/`, and any top-level scripts (historical_backfill.py, lifecycle_replay.py).
    3. Update systemd ExecStart in `production/systemd/*.service`: every `ExecStart=... python services/<old>.py` -> `python services/<new>.py`. There are ~38 unit files. Use grep to find each: `grep -rln "services/.*_agent.py\|services/cross_asset_service.py\|services/llm_writer_service.py" production/systemd/`. systemd UNIT names (the `indicagent-bar-aggregator` filenames) stay unchanged per naming spec Section 10 — only the ExecStart script PATH changes.
    4. Any oneshot timer scripts in `production/scripts/` that invoke these modules: update the path.

    LAUNCHER SCRIPTS (critical — these are NOT renamed but their imports break):
    `services/ml_training_agent.py` and `services/hmm_training_agent.py` are thin launcher wrappers (NOT daemons, NOT renamed). They import from the module paths renamed above:
    - ml_training_agent.py:16 `from src.intelligence.services.ml_training_compute_agent import MLTrainingComputeAgent` -> `from src.intelligence.services.ml_trainer import MLTrainer` (and update the two body references `MLTrainingComputeAgent(settings)` / `MLTrainingComputeAgent._run()` -> `MLTrainer`)
    - hmm_training_agent.py:19 `from src.intelligence.services.hmm_training_compute_agent import HMMTrainingComputeAgent` -> `from src.intelligence.services.hmm_trainer import HMMTrainer` (and update body references `HMMTrainingComputeAgent(...)` / `.start()` -> `HMMTrainer`)
    These launcher files keep their own names (they are the systemd ExecStart targets for indicagent-ml-training / indicagent-hmm-training timers — do NOT rename the launcher files themselves, only fix their imports).

    Do NOT rename files that are not Ring 2 daemons being renamed (e.g. the launcher scripts ml_training_agent.py / hmm_training_agent.py stay; any daemon not in the Section 9 table stays). Only the 33 daemon files + feature_validation_compute_agent.py listed above are renamed; the 2 launchers and the test file have import-only / test-rename fixes.
  </action>
  <verify>
    `ls services/bar_aggregator.py services/feature_writer.py services/service_auditor.py services/context_writer.py` all exist.
    `ls services/bar_aggregator_agent.py 2>&1` errors (old file gone).
    `git grep -n "services/.*_compute_agent.py\|services/.*_writer_agent.py\|services/.*_auditor_agent.py\|services/.*_provider_agent.py\|services/cross_asset_service.py\|services/llm_writer_service.py\|services/ctx_writer_agent.py\|services/alerting_agent.py" production/systemd/` returns zero (all ExecStart updated).
    `.venv/bin/python -c "import services.bar_aggregator, services.service_auditor, services.feature_writer, services.context_writer, services.ibkr_provider; print('Wave 4 file smoke: OK')"` exits 0 and prints `Wave 4 file smoke: OK`.
    `.venv/bin/python -c "import services.ml_training_agent"` exits 0 (launcher import resolves after module rename).
    `.venv/bin/python -c "import services.hmm_training_agent"` exits 0.
    `.venv/bin/python -c "from src.intelligence.services.feature_validation_analyzer import FeatureValidationAnalyzer"` exits 0.
    `ls src/intelligence/services/feature_validation_compute_agent.py 2>&1` errors (old file gone).
  </verify>
  <acceptance_criteria>
    - `services/bar_aggregator.py` exists; `services/bar_aggregator_agent.py` does NOT exist
    - `services/context_writer.py` exists; `services/ctx_writer_agent.py` does NOT exist
    - `production/systemd/indicagent-bar-aggregator.service` ExecStart references `services/bar_aggregator.py`
    - Launcher-path check, part 1: `grep -rln "services/.*_agent.py" production/systemd/ | grep -v "ml.training\|hmm.training"` returns 0 results (only the two launcher units may legitimately reference an `_agent.py` ExecStart path)
    - Launcher-path check, part 2: `grep -rln "services/ml_training_agent.py\|services/hmm_training_agent.py" production/systemd/` returns exactly 2 results (the indicagent-ml-training / indicagent-hmm-training timer units still reference the preserved launcher file paths)
    - `git grep -n "services\.bar_aggregator_agent\|services\.ctx_writer_agent\|services\.service_auditor_agent" -- '*.py'` returns 0 results
    - `.venv/bin/python -c "import services.service_auditor"` exits 0
    - Wave 4 file import smoke test: `.venv/bin/python -c "import services.bar_aggregator, services.service_auditor, services.feature_writer, services.context_writer, services.ibkr_provider; print('Wave 4 file smoke: OK')"` prints `Wave 4 file smoke: OK` and exits 0 (renamed files import at their new module paths)
    - `.venv/bin/python -c "import services.ml_training_agent"` exits 0 (launcher imports renamed `ml_trainer`/`MLTrainer`)
    - `.venv/bin/python -c "import services.hmm_training_agent"` exits 0 (launcher imports renamed `hmm_trainer`/`HMMTrainer`)
    - `src/intelligence/services/feature_validation_analyzer.py` exists; `feature_validation_compute_agent.py` does NOT exist
    - `tests/unit/services/test_feature_validation_analyzer.py` exists; the old test file does NOT exist
    - `git grep -n "feature_validation_compute_agent\|ml_training_compute_agent\|hmm_training_compute_agent" -- '*.py'` returns 0 results (no stale module paths)
  </acceptance_criteria>
  <done>33 daemon files + feature_validation_analyzer.py renamed; 2 launcher imports fixed; test file renamed; all imports + systemd ExecStart updated; modules import cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: API route import + Tier 3 abbreviation fixes</name>
  <files>src/api/routes/narrative.py, src/api/routes/health.py, src/api/routes/drift.py, src/api/routes/ai_stats.py, src/api/routes/signals.py</files>
  <read_first>
    - src/api/routes/narrative.py (line 29 import path of NarrativeSynthesizer; line 96 bar_ctx, 98 BarContext, 107 i7_ctx, 122 bar=bar_ctx, 134 i7=i7_ctx, 219 construction)
    - src/api/routes/health.py (lines 93-95 `resp`)
    - src/api/routes/drift.py, ai_stats.py, signals.py (`except Exception as exc:` occurrences)
  </read_first>
  <action>
    On branch `rename/phase-110`:

    1. src/api/routes/narrative.py: the symbol `NarrativeComputeAgent` was already renamed to `NarrativeSynthesizer` in Wave 2. Confirm the import on line 29 and usage on line 219 read `NarrativeSynthesizer`. If the file module path for the narrative agent did not change (file rename in Task 1 was for SERVICES, not src/intelligence/ai/narrative/narrative_agent.py — that intelligence file is NOT renamed), the import path `from ...intelligence.ai.narrative.narrative_agent import NarrativeSynthesizer` is correct as-is.

    2. Tier 3 abbreviation fixes (full-word expansion, local variables only):
       - narrative.py: rename local var `bar_ctx` -> `bar_context` (lines 96, 98, 122) and `i7_ctx` -> `i7_context` (lines 107, 134). These are local Python variables, not DB columns or topic keys — safe to expand.
       - health.py: rename local `resp` -> `response` (lines 93-95).
       - drift.py, ai_stats.py, narrative.py, signals.py: change `except Exception as exc:` -> `except Exception as error:` and update the `exc` references inside each handler body to `error`.

    Do NOT touch DB column names, Kafka topic strings, or the `i1`-`i8`/`smc` tier codes (Tier 1/2 permitted abbreviations — Section 6).
  </action>
  <verify>
    `git grep -nw "bar_ctx\|i7_ctx" src/api/routes/narrative.py` returns zero.
    `git grep -nw "resp" src/api/routes/health.py` returns zero (in the affected lines).
    `git grep -n "except Exception as exc:" src/api/routes/drift.py src/api/routes/ai_stats.py src/api/routes/narrative.py src/api/routes/signals.py` returns zero.
    `git grep -n "NarrativeComputeAgent" src/api/routes/narrative.py` returns zero; `git grep -n "NarrativeSynthesizer" src/api/routes/narrative.py` returns the import + usage.
  </verify>
  <acceptance_criteria>
    - `git grep -nw "bar_ctx" src/api/routes/narrative.py` returns 0 results
    - `git grep -nw "i7_ctx" src/api/routes/narrative.py` returns 0 results
    - `git grep -n "except Exception as exc:" src/api/routes/drift.py src/api/routes/ai_stats.py src/api/routes/narrative.py src/api/routes/signals.py` returns 0 results
    - `git grep -n "import NarrativeSynthesizer" src/api/routes/narrative.py` returns 1 result
    - `.venv/bin/python -c "import src.api.routes.narrative, src.api.routes.health"` exits 0
  </acceptance_criteria>
  <done>API imports point at renamed classes; Tier 3 abbreviations expanded; API modules import cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile dashboard observability stream (preserve agent_id strings)</name>
  <files>dashboard/src/hooks/use-observability-stream.ts</files>
  <read_first>
    - dashboard/src/hooks/use-observability-stream.ts (lines 64-90 UNIT_TO_AGENT; lines 205-214 agentAge build + `r.labels["agent"]` source; line 213 agentAge["AlphaSwarmComputeAgent"]; line 214 agentAge["intelligence_pipeline_agent"])
    - docs/foundation/naming-system.md Section 10 (agent_id exception) and design spec Section 9 dashboard table
  </read_first>
  <action>
    On branch `rename/phase-110`.

    The `UNIT_TO_AGENT` values and `agentAge[...]` keys are matched against the Prometheus `agent` label (`r.labels["agent"]`), which equals the preserved `name="..."` constructor strings. Per the operational exception (Section 10 overrides Section 9 here — see objective), these strings MUST NOT change or the dashboard lookup breaks.

    Action: Do NOT change any `UNIT_TO_AGENT` value or `agentAge` key string. Instead, add a single clarifying comment block above `UNIT_TO_AGENT` (around line 64):
    ```
    // These string values are Prometheus `agent` label values (agent_id), preserved
    // verbatim under the Phase 110 operational exception (naming-system.md Section 10).
    // They intentionally retain old class-name form because the metric label is unchanged.
    ```
    Make no functional change to this file. This task exists to (a) confirm no incorrect rename was applied and (b) document the deviation in-code.

    If a previous wave's sed accidentally rewrote any string in this TypeScript file (it should not have — the waves only touched .py files), restore it.
  </action>
  <verify>
    `git grep -n '"AlphaSwarmComputeAgent"' dashboard/src/hooks/use-observability-stream.ts` STILL returns the UNIT_TO_AGENT value and the agentAge key (preserved).
    `git grep -n "operational exception" dashboard/src/hooks/use-observability-stream.ts` returns the new comment.
  </verify>
  <acceptance_criteria>
    - `git grep -n '"AlphaSwarmComputeAgent"' dashboard/src/hooks/use-observability-stream.ts` returns the preserved agent_id strings (UNIT_TO_AGENT value + agentAge key)
    - `git grep -n "Section 10" dashboard/src/hooks/use-observability-stream.ts` returns the explanatory comment
    - `cd dashboard && npx tsc --noEmit` exits 0 (or, if tsc is unavailable, the file parses with no syntax change to logic)
  </acceptance_criteria>
  <done>Dashboard agent_id strings preserved and documented; no functional change.</done>
</task>

<task type="auto">
  <name>Task 4: Update CLAUDE.md and supersede naming-conventions.md</name>
  <files>CLAUDE.md, docs/foundation/naming-conventions.md</files>
  <read_first>
    - CLAUDE.md (the "Pending renames" bullet under Core Runtime Files; inline references to old class names like BaseMultiplierAgent, SkepticComputeAgent, NarrativeComputeAgent, AIContext, NarrativeGroupComputeAgent/NarrativeGroupComputeAgent, BaseAgent, BaseWriterAgent; the `ai_narrative_agent.py`/`AINarrativeAgent` note; the Service DAG file references)
    - docs/foundation/naming-conventions.md (to be superseded)
    - docs/foundation/naming-system.md (the canonical spec to redirect to)
  </read_first>
  <action>
    On branch `rename/phase-110`:

    1. CLAUDE.md edits:
       - Remove the entire "**Pending renames (conventions established, code not yet updated):**" bullet (it lists BaseMultiplierAgent->BaseScorer etc.). NOTE: that bullet uses DIFFERENT target names (BaseScorer/SkepticScorer) than this phase's actual targets (Evaluator/SkepticEvaluator). The Phase 110 rename used the naming-system.md Section 9 targets (Evaluator, SkepticEvaluator, CorrelationAnalyzer, etc.), which SUPERSEDE the old "Pending renames" bullet. Delete the bullet entirely — the renames are now done at the Section 9 targets.
       - Update inline references throughout CLAUDE.md from old names to new names: `BaseAgent`->`BaseDaemon`, `BaseWriterAgent`->`BaseWriter`, `BaseAIAgent`->`BaseAIWorker`, `BaseGroupService`->`BaseSwarmCoordinator`, `BaseMultiplierAgent`->`Evaluator`, `AIContext`->`SignalContext`, `NarrativeGroupComputeAgent`->`NarrativeSwarm`, `NarrativeComputeAgent`->`NarrativeSynthesizer` (the individual), `narrative_group_compute_agent.py`->`narrative_swarm.py`, `NarrativeGroupComputeAgent` maps to `indicagent-ai-narrative` line. Preserve `agent_id` label mentions and the operational-exception note.
       - In the "AI agents" / "Adding an AI Agent" section, update `BaseAIAgent`->`BaseAIWorker` and `_agents` examples (`AlphaSwarmComputeAgent._agents` -> `AlphaSwarm._agents`).
       - Update Service DAG / Core Runtime Files file-path references that changed (e.g. `services/narrative_group_compute_agent.py (NarrativeGroupComputeAgent)` -> `services/narrative_swarm.py (NarrativeSwarm)`).
       - Bump the `Version:` header in CLAUDE.md line 3 and update the Status line to note Phase 110 complete.

    2. docs/foundation/naming-conventions.md: replace its entire contents with a redirect stub pointing to `docs/foundation/naming-system.md` (per design spec Section 9 "Documentation"). Keep it short: a title and a "This document is superseded by docs/foundation/naming-system.md" line.

    Do NOT change the `agent_id` operational-exception rule text or the `_DAG_ORDER`/`_AGENT_ID_TO_UNIT` guidance (those reference systemd unit names, which are unchanged).
  </action>
  <verify>
    `git grep -n "Pending renames" CLAUDE.md` returns zero.
    `git grep -nw "BaseMultiplierAgent\|SkepticComputeAgent\|NarrativeComputeAgent\|AIContext\|BaseGroupService" CLAUDE.md` returns zero.
    `git grep -n "superseded by" docs/foundation/naming-conventions.md` returns the redirect line.
  </verify>
  <acceptance_criteria>
    - `git grep -n "Pending renames" CLAUDE.md` returns 0 results
    - `git grep -nw "BaseMultiplierAgent\|SkepticComputeAgent\|BaseGroupService\|BaseAIAgent\|AIContext\|NarrativeGroupComputeAgent" CLAUDE.md` returns 0 results
    - `git grep -n "BaseDaemon\|Evaluator\|SignalContext\|NarrativeSwarm" CLAUDE.md` returns non-zero (new names present)
    - `docs/foundation/naming-conventions.md` contains "superseded by" and references `naming-system.md`
  </acceptance_criteria>
  <done>CLAUDE.md updated to new vocabulary; Pending renames removed; naming-conventions.md redirects.</done>
</task>

<task type="auto">
  <name>Task 5: Final CI gate, full-repo grep sweep, commit, merge to main</name>
  <files>(no source files — runs tests/lint, commits, merges to main)</files>
  <read_first>
    - CLAUDE.md (Done-Coding SOP + Core Commands)
  </read_first>
  <action>
    On branch `rename/phase-110`:

    1. `.venv/bin/ruff check . --fix` then `.venv/bin/ruff check .`
    2. `.venv/bin/black .`
    3. `.venv/bin/mypy src/ --ignore-missing-imports`
    4. `.venv/bin/pytest tests/unit/ -q`

    5. Full-repo grep sweep — confirm zero CODE references to ALL retired identifiers (the only permitted remaining occurrences are inside `name="..."` string literals, `_AGENT_ID_TO_UNIT` keys, the dashboard agent_id strings, and `.planning/`/`docs/` historical references). Scope is REPO-WIDE — include oneshot scripts in `production/scripts/` and root-level scripts (`historical_backfill.py`, `lifecycle_replay.py`):
       `git grep -nw "BaseAgent\|BaseWriterAgent\|BaseProviderAgent\|BaseAIAgent\|BaseGroupService\|BaseMultiplierAgent" -- '*.py' 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' 'production/**/*.py'` -> 0
       `git grep -nw "ComputeAgent\|WriterAgent\|AuditorAgent\|ProviderAgent\|DispatcherAgent\|GroupComputeAgent" -- '*.py' 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' 'production/**/*.py'` -> only `name=` literal / dict-key contexts (no `class`/`import`/path lines)
       `git grep -rn "_agent.py" production/systemd/` -> only the two preserved launcher units (ml-training, hmm-training); verify each

    6. Non-Python surface check (ADVISORY — print results, do NOT fail the gate; the user manually fixes any hits). Renamed identifiers can hide in build/deploy files not covered by the Python greps:
       - Makefile (if present): `grep -n "bar_aggregator_agent\|alpha_swarm_agent\|service_auditor_agent\|ctx_writer_agent" Makefile 2>/dev/null || true`
       - docker-compose / Dockerfiles: `grep -rn "_agent.py\|ComputeAgent\|WriterAgent" production/ --include="*.yml" --include="*.yaml" --include="Dockerfile" 2>/dev/null || true`
       Record any hits in the SUMMARY for manual follow-up.

    7. Commit Wave 4: `git add -A && git commit -m "refactor(110): file renames, systemd ExecStart, API/Tier3 fixes, CLAUDE.md (Wave 4)" -m "Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"`

    8. Done-Coding SOP merge (per CLAUDE.md): `git checkout main && git merge --ff-only rename/phase-110 && git branch -d rename/phase-110 && git worktree prune`. Do NOT push unless the user asks.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/ -q` exits 0. `.venv/bin/ruff check .` exits 0.
    `git branch --show-current` is `main`; `git log --oneline -4` shows all 4 wave commits fast-forwarded onto main.
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/ -q` exits 0
    - `.venv/bin/ruff check .` exits 0
    - `git grep -nw "BaseAgent\|BaseWriterAgent\|BaseProviderAgent\|BaseAIAgent\|BaseGroupService\|BaseMultiplierAgent" -- '*.py' 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' 'production/**/*.py'` returns 0 results (repo-wide: includes production/scripts/ oneshots and root-level scripts)
    - `git grep -nw "ComputeAgent\|WriterAgent\|AuditorAgent\|ProviderAgent\|DispatcherAgent" -- '*.py' 'src/**/*.py' 'services/**/*.py' 'tests/**/*.py' 'production/**/*.py'` returns only `name=`-literal / `_AGENT_ID_TO_UNIT`-key contexts (zero `class `/`import `/module-path lines)
    - Non-Python surface checks (advisory) have been run and any hits recorded in the SUMMARY: `grep -n "bar_aggregator_agent\|alpha_swarm_agent\|service_auditor_agent" Makefile 2>/dev/null || true` and `grep -rn "_agent.py\|ComputeAgent\|WriterAgent" production/ --include="*.yml" --include="*.yaml" --include="Dockerfile" 2>/dev/null || true`
    - `.venv/bin/python -c "import services.ml_training_agent, services.hmm_training_agent"` exits 0 (launcher imports resolve post-rename)
    - `git grep -n "feature_validation_compute_agent\|ml_training_compute_agent\|hmm_training_compute_agent" -- '*.py'` returns 0 results
    - `git branch --show-current` outputs `main`
    - `git log --oneline -4` shows the four Phase 110 wave commits
  </acceptance_criteria>
  <done>Full CI green; all retired identifiers gone from code; branch merged ff-only to main; Phase 110 complete.</done>
</task>

</tasks>

<verification>
- Ring 2 files renamed (drop _agent); systemd ExecStart updated; imports resolve.
- API imports + Tier 3 abbreviations fixed; dashboard agent_id strings preserved + documented.
- CLAUDE.md updated; naming-conventions.md redirects.
- Full-repo grep: zero retired identifiers in code; pytest + ruff green; merged to main.
</verification>

<success_criteria>
- `git grep -nw "BaseAgent\|BaseWriterAgent\|BaseProviderAgent\|BaseAIAgent\|BaseGroupService\|BaseMultiplierAgent\|ComputeAgent\|WriterAgent\|AuditorAgent\|ProviderAgent\|DispatcherAgent"` returns zero CODE/class/import results across src/, services/, tests/ (agent_id string literals + dict keys are the only permitted exceptions)
- Every systemd ExecStart points at an existing renamed file
- CLAUDE.md Pending renames removed; new names throughout
- pytest tests/unit/ + ruff pass; branch fast-forward merged to main
</success_criteria>

<output>
After completion, create `.planning/phases/110-renaissance-rename/110-04-SUMMARY.md`
</output>
