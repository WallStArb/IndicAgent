# Phase 107.5: Signal Lifecycle Architecture Fix - Discussion Log

**Date:** 2026-05-27
**Areas discussed:** Historical Backfill, expires_at Implementation, Rollout Sequencing, Replay Auditor Final State

---

## Area 1: Historical Backfill

**Q: Should we backfill entry_zone_low/high for the 958k historical signals?**
Options: No backfill (accept NULLs) / Backfill all via one-time SQL script
Selected: Backfill all via one-time SQL script

**Q: Inline in Fix 2 migration, or separate step after?**
Options: Inline / Separate step after Fix 2
Selected: Renaissance framing applied — separate, batched, with reconciliation report

Key insight from discussion: DDL separate from DML (migrations contain schema-only changes). Backfill script is idempotent, batched (10k rows), generates a reconciliation report (matched/unmatched/NULL count before+after). Must be reviewed and signed off before Fix 3 begins.

---

## Area 2: expires_at Implementation

**Q: Generated column or written at signal creation?**
Options: DB generated column / Written at signal creation
Selected: Renaissance framing — written at signal creation in signal_writer_agent.py using shared `tf_to_seconds()` utility in service_utils.py

**Q: How to handle NULL old rows during transition?**
Options: Fall back to bar-count TTL / Backfill expires_at first then deploy
Selected: Renaissance framing — backfill expires_at first (self-contained SQL from signal_ledger, no JOIN), verify zero NULLs, then deploy evaluator changes. No fallback code.

Key insight: expires_at backfill is simpler than zone backfill — entirely self-contained within signal_ledger, no JOIN to intelligence_features needed.

---

## Area 3: Rollout Sequencing

**Q: Fix 1 alone or with Fix 2?**
Options: Fix 1 first alone / Together
Selected: Renaissance framing — Fix 1 alone. One change, one observable outcome, confirm before proceeding.

**Q: lifecycle_tracker and signal_tracker in same deployment for Fix 3?**
Options: Both together / lifecycle_tracker first
Selected: Renaissance framing — both together, atomic. Splitting creates a window where two evaluators disagree on TTL model — exactly the dual-model problem being eliminated.

Key insight: The backfill verification is the safety gate. Once expires_at has zero NULLs, both evaluators deploy together.

---

## Area 4: Replay Auditor Final State

**Q: Steady-state parameters after Fix 4?**
Options: Revert to pre-emergency defaults / Keep current / Make configurable via Settings
Selected: Renaissance framing — expose in Settings with canary-appropriate defaults (BATCH_SIZE=100, INTERVAL=300s). Triage values stay until Fix 4 ships, then revert automatically.

**Q: NULL fallback for zone fields after LATERAL JOIN removed?**
Options: No fallback (assume fully populated) / Keep NULL fallback to entry_price
Selected: Renaissance framing — no fallback. NULL zones post-backfill = data integrity bug. Surface via error metric + DLQ, not silent fallback.

---

## Recurring Theme

The user applied a consistent "Renaissance/Jim Simons" standard to every question: atomic verifiable deployments, no silent fallbacks, no dual code paths, migration-first before code changes, operational parameters in config, scientific method (one change → one measurement → confirm). This principle is captured as D-01 through D-05 in CONTEXT.md and should guide all implementation decisions in this phase.
