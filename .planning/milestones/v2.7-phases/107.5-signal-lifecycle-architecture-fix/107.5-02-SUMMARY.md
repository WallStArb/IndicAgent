---
phase: 107.5-signal-lifecycle-architecture-fix
plan: 02
subsystem: signals
tags: [signal-writer, zone-fields, migration, field-name-mismatch]

# Dependency graph
requires:
  - 107.5-01
provides:
  - "New signals written after this deploy have non-NULL entry_zone_low and entry_zone_high in signal_ledger"
affects: [signal-writer, signal_ledger]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "signal dict keys (zone_low/zone_high) differ from LedgerEntry field names (entry_zone_low/entry_zone_high) — always check signal_schema.py for canonical dict key names before reading sig.get()"

key-files:
  created:
    - production/migrations/096_signal_ledger_zone_fields.sql
  modified:
    - services/signal_writer_agent.py

key-decisions:
  - "Dict key names in signal dict are zone_low/zone_high (no entry_ prefix); LedgerEntry field names retain the entry_ prefix — two different naming layers"
  - "Migration 096 is a DDL-only no-op safety check; historical NULL backfill is deferred to Plan 107.5-03"

patterns-established:
  - "Always verify signal dict keys against signal_schema.py make_signal_from_frame() before writing sig.get() accessors in writer agents"

requirements-completed:
  - LIFECYCLE-FIX-02

# Metrics
duration: 8min
completed: 2026-05-27
---

# Phase 107.5 Plan 02: Fix Zone Field Names in SignalWriterAgent Summary

**Two-line fix corrects the field name mismatch between signal_schema.py (zone_low/zone_high) and signal_writer_agent.py (_build_entries used entry_zone_low/entry_zone_high), ending 958k signals with NULL zone fields**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-05-27T17:07:00Z
- **Completed:** 2026-05-27T17:15:00Z
- **Tasks:** 2
- **Files modified:** 1
- **Files created:** 1

## Accomplishments

- Fixed `_build_entries` in `signal_writer_agent.py`: changed `sig.get("entry_zone_low")` to `sig.get("zone_low")` and `sig.get("entry_zone_high")` to `sig.get("zone_high")` (lines 207-208)
- Created `production/migrations/096_signal_ledger_zone_fields.sql` with idempotent `ADD COLUMN IF NOT EXISTS` for `entry_zone_low` and `entry_zone_high`
- Confirmed migration runs cleanly on current schema (NOTICE: column already exists, skipping - no error)
- Confirmed both columns exist in `signal_ledger` via psql `\d signal_ledger`
- 23 related unit tests pass

## Task Commits

1. **Task 1: Fix zone field names in signal_writer_agent.py** - `833d9523` (fix)
2. **Task 2: Write migration 096 - DDL safety check for zone columns** - `55de8dc4` (chore)

## Files Created/Modified

- `services/signal_writer_agent.py` - Fixed two dict key names in `_build_entries`: `entry_zone_low` -> `zone_low`, `entry_zone_high` -> `zone_high`
- `production/migrations/096_signal_ledger_zone_fields.sql` - Idempotent DDL safety check confirming zone columns exist

## Decisions Made

- Dict key names in the signal dict use the shorter form (`zone_low`/`zone_high`), while the `LedgerEntry` dataclass fields use the longer form (`entry_zone_low`/`entry_zone_high`) — these are two distinct naming layers that must not be conflated
- Migration 096 is DDL-only; the DML backfill of 958k historical NULLs is a separate concern handled by Plan 107.5-03 to avoid a long-running transaction blocking the migration

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. The mismatch was confirmed by cross-referencing `signal_schema.py` lines 223-224 (`sig["zone_low"]` / `sig["zone_high"]`) with `signal_writer_agent.py` lines 207-208. The fix was exactly two characters removed per line.

## Next Phase Readiness

- Plan 02 complete. Signal writer will now correctly populate `entry_zone_low` and `entry_zone_high` for all new signals.
- Plan 107.5-03 (DML backfill of historical NULL zone fields) can proceed independently.

---
*Phase: 107.5-signal-lifecycle-architecture-fix*
*Completed: 2026-05-27*
