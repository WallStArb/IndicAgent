---
phase: 100-plugin-shared-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [100-01]
files_modified:
  - src/intelligence/plugins/mixins.py
  - tests/unit/intelligence/test_incremental_mixin.py
  - src/intelligence/features/i1_indicators/atr.py
autonomous: true
requirements:
  - PLUGIN-INFRA-01
  - PLUGIN-INFRA-02

must_haves:
  truths:
    - "IncrementalMixin automatically attaches _state to output dict in both compute_full and compute_next"
    - "IncrementalMixin uses state is None (not if not state) for fallback check -- empty dict {} does not trigger fallback"
    - "State ownership is documented: state is mutable, plugins mutate in place via _compute_next_core, mixin attaches the same state dict back to output"
    - "compute_full fallback in compute_next receives the same frames argument passed to compute_next -- executor passes full historical frames to both paths"
    - "State parameter in _compute_next_core is never None (mixin guarantees this)"
    - "Mixin prevents the 5 HIGH bugs: missing _state returns, state parameter shadowing, self._state reads"
    - "ATR plugin migrated to mixin produces identical outputs to original implementation"
    - "Full-vs-incremental replay test proves compute_full(N bars) == seed(compute_full) + compute_next x N"
  artifacts:
    - path: "src/intelligence/plugins/mixins.py"
      provides: "IncrementalMixin class for incremental plugins"
      contains: "IncrementalMixin", "compute_full", "compute_next", "_compute_full_core", "_compute_next_core", "_seed_state"
    - path: "tests/unit/intelligence/test_incremental_mixin.py"
      provides: "Unit tests for IncrementalMixin contract including state ownership and replay parity"
      contains: "test_state_attached", "test_fallback_to_full", "test_state_never_none", "test_full_vs_incremental_replay"
    - path: "src/intelligence/features/i1_indicators/atr.py"
      provides: "First plugin migrated to IncrementalMixin (reference implementation)"
      contains: "IncrementalMixin", "_compute_full_core", "_compute_next_core", "_seed_state"
  key_links:
    - from: "src/intelligence/plugins/mixins.py"
      to: "src/intelligence/features/i1_indicators/atr.py"
      via: "class ATRPlugin(IncrementalMixin)"
      pattern: "class ATRPlugin.*IncrementalMixin"
    - from: "src/intelligence/pipeline/executor.py"
      to: "src/intelligence/plugins/mixins.py"
      via: "_timed_plugin_call validates _state in result"
      pattern: '"_state" not in result'
---

<objective>
Create IncrementalMixin class that owns the state contract for incremental plugins. The mixin provides compute_full() and compute_next() methods that automatically handle state threading, fallback-to-full logic, and _state attachment. Plugin authors implement only domain logic in _compute_full_core(), _compute_next_core(), and _seed_state(). Migrate ATR plugin as the reference implementation.

Purpose: The design doc identified 5 HIGH-severity bugs where incremental plugins either (1) read self._state instead of the state parameter, or (2) forget to return _state in compute_next. The mixin prevents both bugs by owning the state contract entirely. Plugins never touch the state parameter -- the mixin passes a guaranteed-non-None state to _compute_next_core.

Review item resolution:
- Actionable 1: Fix if not state to state is None -- prevents empty dict {} from triggering fallback
- Actionable 2: Document state ownership model in mixin docstring -- mutable in-place mutation contract
- Actionable 3: Verify executor window contract -- executor passes same full frames dict to both compute_full and compute_next via functools.partial, confirmed at executor.py line 322-328
- Actionable 4: Add full-vs-incremental replay test as a mixin conformance test

Output: IncrementalMixin class in mixins.py, comprehensive unit tests with replay parity verification, and ATR plugin migrated to the mixin as proof of concept.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/100-plugin-shared-infrastructure/100-RESEARCH.md
@.planning/phases/100-plugin-shared-infrastructure/100-01-SUMMARY.md
@docs/plans/2026-05-21-plugin-infrastructure-design.md
@src/intelligence/plugins/mixins.py
@src/intelligence/pipeline/executor.py
@src/intelligence/features/i1_indicators/atr.py
@tests/unit/intelligence/test_plugin_incremental.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IncrementalMixin class to mixins.py with state is None fallback and documented state ownership</name>
  <files>
    src/intelligence/plugins/mixins.py
  </files>
  <read_first>
    src/intelligence/plugins/mixins.py
    src/intelligence/features/i1_indicators/atr.py
    src/intelligence/pipeline/executor.py
  </read_first>
  <action>
    Add IncrementalMixin class to src/intelligence/plugins/mixins.py. Append AFTER the existing __all__ and function definitions (do not overwrite them).

    The class MUST have this exact contract:

    class IncrementalMixin:
        """Owns fallback-to-full and _state return contract for incremental plugins.

        State ownership model (MUTABLE IN-PLACE CONTRACT):
        - State is a plain dict, passed by reference to _compute_next_core.
        - Plugins MUTATE state in place: state["key"] = new_value.
        - The mixin attaches the SAME state dict back to output: result["_state"] = state.
        - _compute_next_core MUST NOT return a new state dict -- it mutates the one it receives.
        - This matches the existing ATR pattern: s["prev_atr"] = new_atr; s["prev_close"] = close.
        - Deques, lists, and nested dicts in state are all valid -- mutation is by reference.

        Executor window contract:
        - The executor (executor.py line 322-328) passes the same `frames` dict to both
          compute_full and compute_next via functools.partial. There is no per-call frame slicing.
        - Therefore compute_next's fallback to compute_full(frames) is semantically correct:
          it receives the full historical frames, not incremental single-bar windows.

        Plugins implementing this mixin must provide:
        - _compute_full_core(frames) -> dict: Pure computation, returns outputs only (no _state).
          Returns {} when insufficient data.
        - _compute_next_core(frames, state) -> dict: Pure incremental computation.
          State is guaranteed non-None and non-empty (mixin guarantees this).
          Mutate state in place -- do NOT create a new state dict.
          Returns {} when computation cannot proceed.
        - _seed_state(frames) -> dict: Extract state from full computation for incremental seeding.
          Returns the initial state dict (may be empty {} if no state needed).

        The mixin provides:
        - compute_full(frames, *, state=None): Calls _compute_full_core, attaches _state via _seed_state.
        - compute_next(frames, *, state=None): Falls back to compute_full if state is None,
          else calls _compute_next_core and attaches _state (the same mutated dict).
        """

    Key implementation details:
    1. In compute_next, use `if state is None:` for the fallback check -- NOT `if not state:`.
       Review item 1: empty dict {} must NOT trigger fallback. An empty state dict means
       the executor had no saved state (plugin_states.get returns {} default at executor.py line 322),
       and the mixin should attempt incremental computation with the empty state rather than
       falling back silently. If _compute_next_core cannot handle empty state, it returns {}.
    2. In compute_next, after calling _compute_next_core, attach state:
       `result["_state"] = state` -- this is the SAME dict object that _compute_next_core mutated.
    3. All three abstract methods raise NotImplementedError.

    Update __all__ to include "IncrementalMixin".
  </action>
  <verify>
    `.venv/bin/python -c "from src.intelligence.plugins.mixins import IncrementalMixin; print('import works')"` prints "import works".
    `grep "class IncrementalMixin" src/intelligence/plugins/mixins.py` returns exactly 1 line.
    `grep "if state is None" src/intelligence/plugins/mixins.py` returns at least 1 line (fallback check uses is None, not truthiness).
    `grep "MUTABLE IN-PLACE CONTRACT" src/intelligence/plugins/mixins.py` returns exactly 1 line (state ownership documented).
    `grep "Executor window contract" src/intelligence/plugins/mixins.py` returns exactly 1 line (executor contract documented).
    `grep "IncrementalMixin" src/intelligence/plugins/mixins.py | grep "__all__"` returns a line containing IncrementalMixin.
    `.venv/bin/python -c "from src.intelligence.plugins.mixins import wilders_update, update_ema, get_main_df, IncrementalMixin; print('all exports work')"` succeeds (existing functions not broken).
  </verify>
  <acceptance_criteria>
    - src/intelligence/plugins/mixins.py contains class IncrementalMixin after existing function definitions
    - compute_next uses `if state is None:` NOT `if not state:` for fallback check
    - Docstring contains "MUTABLE IN-PLACE CONTRACT" section explaining state mutation semantics
    - Docstring contains "Executor window contract" section confirming frames are full historical
    - Defines compute_full method that calls _compute_full_core and attaches _state via _seed_state
    - Defines compute_next method that falls back only when state is None, then calls _compute_next_core and attaches _state
    - Defines three abstract methods: _compute_full_core, _compute_next_core, _seed_state (each raises NotImplementedError)
    - __all__ exports both existing functions and IncrementalMixin
    - Existing wilders_update, update_ema, get_main_df functions are unchanged
  </acceptance_criteria>
  <done>
    IncrementalMixin class exists with state is None fallback, documented state ownership model (mutable in-place mutation), and executor window contract documentation. The mixin owns state threading (fallback, _state attachment) so plugins only implement domain logic.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for IncrementalMixin contract including state ownership and full-vs-incremental replay</name>
  <files>
    tests/unit/intelligence/test_incremental_mixin.py
  </files>
  <read_first>
    src/intelligence/plugins/mixins.py
    tests/unit/intelligence/test_plugin_incremental.py
    src/intelligence/pipeline/executor.py
  </read_first>
  <action>
    Create tests/unit/intelligence/test_incremental_mixin.py with test coverage.

    class TestIncrementalMixinContract:
    - test_state_attached_in_compute_full(): MockPlugin.compute_full returns dict with _state key
    - test_state_attached_in_compute_next(): MockPlugin.compute_next with state returns dict with _state key
    - test_fallback_to_full_when_state_is_none(): compute_next(state=None) calls compute_full
    - test_empty_dict_state_does_NOT_trigger_fallback(): compute_next(state={}) calls _compute_next_core, NOT compute_full. This is the critical test for review item 1.
    - test_compute_next_receives_non_none_state(): _compute_next_core never receives None state
    - test_state_is_same_object_after_mutation(): result["_state"] is the same dict object that _compute_next_core received (identity check with `is`)
    - test_empty_result_returns_empty_dict(): When _compute_full_core returns {}, compute_full returns {}

    class TestIncrementalMixinReplayParity:
    Full-vs-incremental replay test (review item 4). Uses a SimpleMovingAverage MockPlugin:
    - _compute_full_core: computes SMA over full window, returns {f"sma_{period}": float_val}
    - _compute_next_core: maintains running sum and count in state, returns updated SMA
    - _seed_state: extracts {sum: float, count: int} from full computation
    - test_full_equals_seed_plus_incremental(): Generate N=50 synthetic bars. Run compute_full on all N bars to get reference output. Then seed state from compute_full on first 20 bars, and call compute_next for each remaining bar one at a time (bars 21..50). Verify the final incremental output matches compute_full on all 50 bars within tolerance 1e-10.
    - test_state_accumulates_correctly(): Verify state["count"] and state["sum"] increment correctly across compute_next calls.

    Create a MockIncrementalPlugin for testing:
    - Implements _compute_full_core, _compute_next_core, _seed_state
    - Inherits from IncrementalMixin
    - Simple enough to verify manually but exercises state mutation, seeding, and incremental update

    Use unittest.mock.patch or direct class construction. Verify that executor validation (raise ValueError if "_state" not in result) never fails for mixin-based plugins.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/test_incremental_mixin.py -v` exits 0 with all tests passing.
    `.venv/bin/pytest tests/unit/intelligence/test_incremental_mixin.py --collect-only` shows at least 9 collected tests.
    `grep -c "def test_" tests/unit/intelligence/test_incremental_mixin.py` returns at least 9.
    `grep "test_empty_dict_state_does_NOT_trigger_fallback" tests/unit/intelligence/test_incremental_mixin.py` returns exactly 1.
    `grep "test_state_is_same_object_after_mutation" tests/unit/intelligence/test_incremental_mixin.py` returns exactly 1.
    `grep "test_full_equals_seed_plus_incremental" tests/unit/intelligence/test_incremental_mixin.py` returns exactly 1.
  </verify>
  <acceptance_criteria>
    - File tests/unit/intelligence/test_incremental_mixin.py exists
    - Contains class TestIncrementalMixinContract with at least 7 test methods
    - test_empty_dict_state_does_NOT_trigger_fallback passes -- empty dict does NOT fallback
    - test_state_is_same_object_after_mutation passes -- result["_state"] is the same dict object
    - Contains class TestIncrementalMixinReplayParity with at least 2 test methods
    - test_full_equals_seed_plus_incremental proves compute_full(N) == seed + compute_next x remaining
    - MockIncrementalPlugin inherits IncrementalMixin with all three abstract methods implemented
    - All tests pass when run with pytest
  </acceptance_criteria>
  <done>
    Comprehensive unit tests verify IncrementalMixin contract: _state always attached, empty dict does NOT trigger fallback (state is None check), state ownership is same-object mutation, and full-vs-incremental replay parity holds. Mock plugin demonstrates the pattern.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate ATR plugin to IncrementalMixin (reference implementation)</name>
  <files>
    src/intelligence/features/i1_indicators/atr.py
  </files>
  <read_first>
    src/intelligence/features/i1_indicators/atr.py
    src/intelligence/plugins/mixins.py
    tests/unit/intelligence/test_plugin_incremental.py
  </read_first>
  <action>
    Refactor src/intelligence/features/i1_indicators/atr.py to inherit from IncrementalMixin.

    1. Add import: from src.intelligence.plugins.mixins import IncrementalMixin, wilders_update
    2. Change class declaration: class ATRPlugin(IncrementalMixin):
    3. Remove _state field (no longer needed, mixin handles it)
    4. Rename compute_full to _compute_full_core:
       - Remove out["_state"] = state line (mixin handles it)
       - Remove the state variable initialization
       - Keep all ATR computation logic identical
       - Return out dict without _state
    5. Rename compute_next to _compute_next_core:
       - Remove `if not state:` fallback (mixin handles it -- and uses state is None, not truthiness)
       - Remove out["_state"] = state line (mixin handles it)
       - Keep all incremental computation logic identical
       - State is mutated in place: s["prev_atr"] = new_atr; s["prev_close"] = close (matches existing pattern)
       - Return out dict without _state
    6. Create _seed_state method that extracts the state dict:
       - Copy the state initialization logic from original compute_full
       - Return dict: {f"atr_{p}": {"prev_atr": float(val), "prev_close": float(close.iloc[-1])} for p in self.periods}
    7. Remove any remaining self._state references
    8. Replace inline Wilder's smoothing `(1 - alpha) * s["prev_atr"] + alpha * tr` with wilders_update(s["prev_atr"], tr, p) call
    9. Update docstring to mention IncrementalMixin usage and mutable state contract

    The refactoring MUST NOT change the mathematical computation -- only extract state management to the mixin. Verify that existing test_plugin_incremental.py::TestATRIncremental still passes after migration.

    IMPORTANT: The existing ATR uses `if not state:` for fallback in compute_next. After migration, the mixin's compute_next uses `if state is None:` instead. This is a correctness improvement -- empty dict state will now reach _compute_next_core instead of falling back. However, the executor (line 322) passes `plugin_states.get(plugin_name, {})` which is `{}` for cold start. At line 97, the executor checks `state` truthiness and falls back to compute_full when state is empty dict. So the mixin's compute_next is only called when the executor has a truthy state dict, meaning the state is always populated when _compute_next_core is reached.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/test_plugin_incremental.py::TestATRIncremental -v` exits 0 with test passing.
    `grep "IncrementalMixin" src/intelligence/features/i1_indicators/atr.py` returns exactly 1 line.
    `grep -c "self._state" src/intelligence/features/i1_indicators/atr.py` returns 0 (no _state field).
    `grep "_compute_full_core\|_compute_next_core\|_seed_state" src/intelligence/features/i1_indicators/atr.py | wc -l` returns at least 3.
    `grep "wilders_update" src/intelligence/features/i1_indicators/atr.py` returns at least 1 (uses shared utility).
    `grep "def compute_full\|def compute_next" src/intelligence/features/i1_indicators/atr.py` returns 0 (no direct compute_full/compute_next -- mixin provides them).
  </verify>
  <acceptance_criteria>
    - ATRPlugin class declaration includes (IncrementalMixin)
    - ATRPlugin has method _compute_full_core (not compute_full)
    - ATRPlugin has method _compute_next_core (not compute_next)
    - ATRPlugin has method _seed_state
    - ATRPlugin does not define _state field
    - ATRPlugin does not define compute_full or compute_next methods (mixin provides them)
    - ATRPlugin uses wilders_update from mixins.py for Wilder's smoothing
    - State is mutated in place in _compute_next_core (s["prev_atr"] = ...)
    - TestATRIncremental test passes (proves output parity)
  </acceptance_criteria>
  <done>
    ATR plugin successfully migrated to IncrementalMixin. Mathematical computation unchanged (test passes), state management owned by mixin, uses shared wilders_update utility. Serves as reference implementation for remaining incremental plugins. Mixin uses state is None fallback (not truthiness), and state ownership is documented as mutable in-place mutation.
  </done>
</task>

</tasks>

<verification>
- `.venv/bin/python -c "from src.intelligence.plugins.mixins import IncrementalMixin; from src.intelligence.features.i1_indicators.atr import ATRPlugin; assert isinstance(ATRPlugin(), IncrementalMixin)"` succeeds
- `.venv/bin/pytest tests/unit/intelligence/test_incremental_mixin.py tests/unit/intelligence/test_plugin_incremental.py::TestATRIncremental -v` exits 0 with all tests passing
- `grep -c "self._state" src/intelligence/features/i1_indicators/atr.py` returns 0
- `grep "_compute_full_core\|_compute_next_core\|_seed_state" src/intelligence/features/i1_indicators/atr.py | wc -l` returns 3
- `grep "if state is None" src/intelligence/plugins/mixins.py` returns at least 1 (mixin uses is None)
- `grep "test_empty_dict_state_does_NOT_trigger_fallback\|test_full_equals_seed_plus_incremental" tests/unit/intelligence/test_incremental_mixin.py | wc -l` returns 2
</verification>

<success_criteria>
IncrementalMixin class exists and owns the state contract for incremental plugins. Fallback uses state is None (not truthiness). State ownership is documented: mutable in-place mutation, _compute_next_core must not return new state. Executor window contract documented. Full-vs-incremental replay test proves parity. ATR plugin migrated and passes all existing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/100-plugin-shared-infrastructure/100-02-SUMMARY.md`
</output>
