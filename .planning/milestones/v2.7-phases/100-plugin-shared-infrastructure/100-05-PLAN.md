---
phase: 100-plugin-shared-infrastructure
plan: 05
type: execute
wave: 4
depends_on: [100-01]
files_modified:
  - src/intelligence/features/i1_indicators/bollinger.py
  - src/intelligence/features/i1_indicators/moving_averages.py
  - src/intelligence/features/i1_indicators/macd.py
  - src/intelligence/features/i1_indicators/roc_ppo.py
  - src/intelligence/features/i2_composites/acceleration_regime.py
  - src/intelligence/features/i1_indicators/ac_oscillator.py
  - src/intelligence/features/i1_indicators/cci.py
autonomous: true
requirements:
  - PLUGIN-INFRA-04

must_haves:
  truths:
    - "All I1 indicator plugins migrated use get_main_df() for OHLCV DataFrame extraction"
    - "Bollinger plugin uses get_main_df() instead of inline frames.get('main')"
    - "MovingAverages plugin uses get_main_df()"
    - "MACD plugin uses get_main_df()"
    - "ROC_PPO plugin uses get_main_df()"
    - "AC Oscillator plugin uses get_main_df()"
    - "CCI plugin uses get_main_df()"
    - "AccelerationRegime (I2) uses get_main_df()"
    - "All migrated plugins handle None return from get_main_df() correctly"
    - "No duplicated OHLCV extraction logic remains in migrated plugins"
  artifacts:
    - path: "src/intelligence/features/i1_indicators/bollinger.py"
      provides: "Bollinger plugin migrated to get_main_df"
      contains: "from src.intelligence.plugins.mixins import get_main_df"
    - path: "src/intelligence/features/i1_indicators/moving_averages.py"
      provides: "MovingAverages plugin migrated to get_main_df"
      contains: "get_main_df"
    - path: "src/intelligence/features/i1_indicators/macd.py"
      provides: "MACD plugin migrated to get_main_df"
      contains: "get_main_df"
    - path: "src/intelligence/features/i1_indicators/roc_ppo.py"
      provides: "ROC/PPO plugin migrated to get_main_df"
      contains: "get_main_df"
    - path: "src/intelligence/features/i2_composites/acceleration_regime.py"
      provides: "AccelerationRegime plugin migrated to get_main_df"
      contains: "get_main_df"
  key_links:
    - from: "src/intelligence/plugins/mixins.py"
      to: "All migrated plugins"
      via: "get_main_df import"
      pattern: "from src.intelligence.plugins.mixins import get_main_df"
    - from: "extract_ohlcv in plugin_utils.py"
      to: "get_main_df in mixins.py"
      via: "Similar pattern for DataFrame extraction"
      pattern: "def get_main_df|def extract_ohlcv"
---

<objective>
Migrate I1 and I2 indicator plugins to use get_main_df() shared utility for OHLCV DataFrame extraction. This eliminates duplicated frames.get("main") guard logic across 28+ I1 plugins and promotes consistent min_bars validation.

Review item resolution:
- Concern 8 (Codex): get_main_df returns None when data insufficient -- callers must check for None. Each migrated plugin must include `if df is None: return {}` immediately after the get_main_df call.

Output: 7 I1/I2 plugins migrated to use get_main_df(). All have consistent OHLCV extraction pattern with explicit None handling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/100-plugin-shared-infrastructure/100-RESEARCH.md
@.planning/phases/100-plugin-shared-infrastructure/100-01-SUMMARY.md
@docs/plans/2026-05-21-plugin-infrastructure-design.md
@src/intelligence/plugins/mixins.py
@src/intelligence/features/i1_indicators/bollinger.py
@src/intelligence/features/i1_indicators/moving_averages.py
@src/intelligence/features/i1_indicators/macd.py
@src/intelligence/features/i1_indicators/roc_ppo.py
@src/intelligence/features/i2_composites/acceleration_regime.py
@src/intelligence/features/i1_indicators/ac_oscillator.py
@src/intelligence/features/i1_indicators/cci.py
@src/intelligence/trading/plugin_utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Bollinger, MovingAverages, and MACD to get_main_df()</name>
  <files>
    src/intelligence/features/i1_indicators/bollinger.py
    src/intelligence/features/i1_indicators/moving_averages.py
    src/intelligence/features/i1_indicators/macd.py
  </files>
  <read_first>
    src/intelligence/features/i1_indicators/bollinger.py
    src/intelligence/features/i1_indicators/moving_averages.py
    src/intelligence/features/i1_indicators/macd.py
    src/intelligence/plugins/mixins.py
    src/intelligence/trading/plugin_utils.py
  </read_first>
  <action>
    Migrate 3 plugins to use get_main_df(). Pattern is identical for each:

    1. Add import: from src.intelligence.plugins.mixins import get_main_df
    2. In compute_full:
       - Replace `df = frames.get("main")` with `df = get_main_df(frames, self.min_lookback)`
       - Remove the subsequent `if df is None or len(df) < N: return {}` check (get_main_df handles min_bars)
       - Add: `if df is None: return {}` immediately after get_main_df call (handles None return)
    3. In compute_next (if it exists):
       - Replace `df = windows.get("main")` with `df = get_main_df(windows, 1)` or appropriate min_bars
       - Add: `if df is None: return {}` immediately after
    4. Remove all frames.get("main") calls

    For MACD specifically:
    - Also add import: from src.intelligence.plugins.mixins import update_ema
    - Replace inline EMA alpha calculations with update_ema() calls (fast EMA, slow EMA, signal EMA)

    IMPORTANT: get_main_df returns None (not raises) when data insufficient. Every call site must check for None.
  </action>
  <verify>
    `grep "from src.intelligence.plugins.mixins import get_main_df" src/intelligence/features/i1_indicators/bollinger.py src/intelligence/features/i1_indicators/moving_averages.py src/intelligence/features/i1_indicators/macd.py | wc -l` returns 3.
    `grep "if df is None: return {}" src/intelligence/features/i1_indicators/bollinger.py src/intelligence/features/i1_indicators/moving_averages.py src/intelligence/features/i1_indicators/macd.py | wc -l` returns at least 3 (None check after each get_main_df call).
    `grep -c 'frames.get("main")' src/intelligence/features/i1_indicators/bollinger.py src/intelligence/features/i1_indicators/moving_averages.py src/intelligence/features/i1_indicators/macd.py` returns "0 0 0".
    `grep "update_ema" src/intelligence/features/i1_indicators/macd.py | wc -l` returns at least 1.
  </verify>
  <acceptance_criteria>
    - All 3 plugins import get_main_df from mixins.py
    - All 3 use get_main_df() in compute_full with self.min_lookback
    - All 3 have explicit `if df is None: return {}` after get_main_df calls
    - All 3 have no frames.get("main") calls remaining
    - MACD uses update_ema for EMA calculations
  </acceptance_criteria>
  <done>
    Bollinger, MovingAverages, and MACD migrated to get_main_df() with explicit None handling. MACD also uses update_ema shared utility.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate ROC_PPO, AC Oscillator, CCI, and AccelerationRegime to get_main_df()</name>
  <files>
    src/intelligence/features/i1_indicators/roc_ppo.py
    src/intelligence/features/i1_indicators/ac_oscillator.py
    src/intelligence/features/i1_indicators/cci.py
    src/intelligence/features/i2_composites/acceleration_regime.py
  </files>
  <read_first>
    src/intelligence/features/i1_indicators/roc_ppo.py
    src/intelligence/features/i1_indicators/ac_oscillator.py
    src/intelligence/features/i1_indicators/cci.py
    src/intelligence/features/i2_composites/acceleration_regime.py
    src/intelligence/plugins/mixins.py
  </read_first>
  <action>
    Migrate 4 more plugins to get_main_df(). Same pattern as Task 1:

    1. Add import: from src.intelligence.plugins.mixins import get_main_df
    2. Replace frames.get("main") with get_main_df(frames, self.min_lookback) in compute_full
    3. Replace frames.get("main") or windows.get("main") with get_main_df(...) in compute_next if applicable
    4. Add `if df is None: return {}` after every get_main_df call
    5. Remove all frames.get("main") / windows.get("main") calls

    For ROC_PPO specifically:
    - Also add import: from src.intelligence.plugins.mixins import update_ema
    - Replace inline EMA calculations with update_ema() calls

    For AccelerationRegime (I2):
    - Path is src/intelligence/features/i2_composites/acceleration_regime.py
    - I2 plugins consume I1 outputs but also need OHLCV for derivative computation

    IMPORTANT: get_main_df returns None when data insufficient. Every call site must check for None.
  </action>
  <verify>
    `grep "from src.intelligence.plugins.mixins import get_main_df" src/intelligence/features/i1_indicators/roc_ppo.py src/intelligence/features/i1_indicators/ac_oscillator.py src/intelligence/features/i1_indicators/cci.py src/intelligence/features/i2_composites/acceleration_regime.py | wc -l` returns 4.
    `grep "if df is None: return {}" src/intelligence/features/i1_indicators/roc_ppo.py src/intelligence/features/i1_indicators/ac_oscillator.py src/intelligence/features/i1_indicators/cci.py src/intelligence/features/i2_composites/acceleration_regime.py | wc -l` returns at least 4.
    `grep -c 'frames.get("main")\|windows.get("main")' src/intelligence/features/i1_indicators/roc_ppo.py src/intelligence/features/i1_indicators/ac_oscillator.py src/intelligence/features/i1_indicators/cci.py src/intelligence/features/i2_composites/acceleration_regime.py` returns "0 0 0 0".
    `grep "update_ema" src/intelligence/features/i1_indicators/roc_ppo.py | wc -l` returns at least 1.
  </verify>
  <acceptance_criteria>
    - All 4 plugins import get_main_df from mixins.py
    - All 4 use get_main_df() with self.min_lookback
    - All 4 have explicit `if df is None: return {}` after get_main_df calls
    - All 4 have no frames.get("main") or windows.get("main") calls remaining
    - ROC_PPO uses update_ema for EMA calculations
  </acceptance_criteria>
  <done>
    ROC_PPO, AC Oscillator, CCI, and AccelerationRegime migrated to get_main_df() with explicit None handling. ROC_PPO also uses update_ema. I2 tier adoption proves cross-tier applicability.
  </done>
</task>

</tasks>

<verification>
- `grep -r "from src.intelligence.plugins.mixins import get_main_df" src/intelligence/features/i1_indicators/ src/intelligence/features/i2_composites/ | wc -l` returns at least 7
- `grep -r 'frames.get("main")' src/intelligence/features/i1_indicators/bollinger.py src/intelligence/features/i1_indicators/moving_averages.py src/intelligence/features/i1_indicators/macd.py src/intelligence/features/i1_indicators/roc_ppo.py src/intelligence/features/i1_indicators/ac_oscillator.py src/intelligence/features/i1_indicators/cci.py src/intelligence/features/i2_composites/acceleration_regime.py | wc -l` returns 0
- `grep "if df is None: return {}" src/intelligence/features/i1_indicators/bollinger.py src/intelligence/features/i1_indicators/moving_averages.py src/intelligence/features/i1_indicators/macd.py src/intelligence/features/i1_indicators/roc_ppo.py src/intelligence/features/i1_indicators/ac_oscillator.py src/intelligence/features/i1_indicators/cci.py src/intelligence/features/i2_composites/acceleration_regime.py | wc -l` returns at least 7
- `grep -c "update_ema" src/intelligence/features/i1_indicators/macd.py src/intelligence/features/i1_indicators/roc_ppo.py` returns at least 4
</verification>

<success_criteria>
All 7 I1/I2 plugins migrated to get_main_df() shared utility. Every call site has explicit `if df is None: return {}` guard after get_main_df. MACD and ROC/PPO also use update_ema(). Duplicated OHLCV extraction logic removed. Min_bars guard pattern centralized in get_main_df(). None-return contract properly handled by all callers.
</success_criteria>

<output>
After completion, create `.planning/phases/100-plugin-shared-infrastructure/100-05-SUMMARY.md`
</output>
