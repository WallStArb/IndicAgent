---
phase: 109-config-foundation-self-healing-engine
plan: 02
type: execute
wave: 2
depends_on: [109-01]
files_modified:
  - src/config/outbox_dispatcher.py
  - services/config_service_agent.py
  - services/outbox_dispatcher_agent.py
  - src/observability/metrics.py
  - src/core/stream_keys.py
  - services/service_auditor_agent.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "OutboxDispatcher polls config_outbox for pending rows"
    - "OutboxDispatcher publishes to topic_config_updates"
    - "OutboxDispatcher updates outbox status with retry semantics"
    - "ConfigServiceAgent serves HTTP API on port 9001 for config operations"
    - "ConfigServiceAgent exposes OTel metrics on port 9005 (separate from HTTP API)"
    - "API rejects non-OPS keys with clear error message"
    - "API requires Bearer token if CONFIG_API_TOKEN is set"
    - "API returns 401 if auth token missing or invalid"
    - "Kafka topic_config_updates exists and is compacted"
    - "Config update event contract is explicit and versioned"
    - "Config metrics defined and emitted"
    - "service_auditor _DAG_ORDER and _AGENT_ID_TO_UNIT register the 3 new Phase 109 services (config-service, outbox-dispatcher, self-healing-agent)"

  artifacts:
    - path: "src/config/outbox_dispatcher.py"
      provides: "OutboxDispatcherAgent for Kafka publish"
      exports: ["OutboxDispatcherAgent"]
    - path: "services/config_service_agent.py"
      provides: "FastAPI service for ConfigService on port 9001"
      exports: ["ConfigServiceAPI"]
    - path: "services/outbox_dispatcher_agent.py"
      provides: "Systemd unit entry for outbox dispatcher"
      exports: ["main() entry point"]
    - path: "src/core/stream_keys.py"
      provides: "topic_config_updates function"
      contains: "def topic_config_updates(env_name: str)"
  key_links:
    - from: "services/outbox_dispatcher_agent.py"
      to: "src/config/outbox_dispatcher.py"
      via: "inheritance"
      pattern: "class OutboxDispatcherAgent\\(BaseAgent\\)"
    - from: "services/config_service_agent.py"
      to: "src/config/config_service.py"
      via: "direct import"
      pattern: "from src.config.config_service import ConfigService"
    - from: "src/config/outbox_dispatcher.py"
      to: "src/core/stream_keys.py"
      via: "import"
      pattern: "from src.core.stream_keys import topic_config_updates"
---

<objective>
Create OutboxDispatcher agent for Kafka propagation, ConfigService FastAPI HTTP API on port 9001 (OPS config only), config metrics, and stream key for config updates topic.

Purpose: Enable config change propagation via Kafka (outbox pattern) and HTTP access for OPS config operations only. Add observability metrics. API docs clarify three-layer invariant.
Output: OutboxDispatcherAgent, ConfigServiceAgent on port 9001 (HTTP) / 9005 (OTel metrics), config metrics, topic_config_updates stream key (systemd units created in Plan 03 Task 4)

**Port convention (per review consensus, Codex MEDIUM finding):**
- 9001 = Config HTTP API (uvicorn)
- 9005 = Config Service OTel metrics scrape endpoint
- 9002 = Self-Healing HTTP API (Plan 05)
- 9007 = Self-Healing OTel metrics scrape endpoint
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-config-foundation-self-healing-engine/109-CONTEXT.md
@.planning/phases/109-config-foundation-self-healing-engine/109-RESEARCH.md
@.planning/phases/109-config-foundation-self-healing-engine/109-PATTERNS.md
@.planning/phases/109-config-foundation-self-healing-engine/109-REVIEWS.md
@.planning/phases/109-config-foundation-self-healing-engine/109-01-SUMMARY.md

@src/config/config_service.py
@src/core/kafka_utils.py
@src/core/stream_keys.py
@src/observability/metrics.py
@services/alerting_agent.py
@services/feature_writer_agent.py
@production/systemd/indicagent-alerting-agent.service
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config and self-healing metrics</name>
  <files>src/observability/metrics.py</files>
  <read_first>
    - src/observability/metrics.py (existing metric patterns)
  </read_first>
  <action>
    Add to src/observability/metrics.py after line ~400 (after signal processor metrics):

    # Config metrics (Phase 109)
    CONFIG_SET_TOTAL = _meter.create_counter("config_set_total", description="Config set operations by key and outcome")
    CONFIG_VALIDATION_FAILED_TOTAL = _meter.create_counter("config_validation_failed_total", description="Config validation failures by key and reason")
    CONFIG_REVERT_TOTAL = _meter.create_counter("config_revert_total", description="Config revert operations by key")
    CONFIG_OUTBOX_PENDING = _meter.create_up_down_counter("config_outbox_pending", description="Pending config outbox entries awaiting Kafka publish")
    CONFIG_OUTBOX_PUBLISH_LATENCY_SECONDS = _meter.create_histogram("config_outbox_publish_latency_seconds", description="Config outbox to Kafka publish latency", unit="s")
    CONFIG_RELOAD_TOTAL = _meter.create_counter("config_reload_total", description="Config hot-reload events by agent and key")
    CONFIG_RELOAD_LATENCY_SECONDS = _meter.create_histogram("config_reload_latency_seconds", description="Time from Kafka receive to in-memory cache update (review feedback - Gemini suggestion)", unit="s")
    CONFIG_AUTH_FAILED_TOTAL = _meter.create_counter("config_auth_failed_total", description="Config API auth failures by reason")

    # Self-healing metrics (Phase 109)
    REMEDIATION_ATTEMPT_TOTAL = _meter.create_counter("remediation_attempt_total", description="Remediation attempts by state_variable and action")
    REMEDIATION_SUCCESS_TOTAL = _meter.create_counter("remediation_success_total", description="Successful remediation outcomes")
    REMEDIATION_DURATION_SECONDS = _meter.create_histogram("remediation_duration_seconds", description="Remediation execution latency", unit="s")
    REMEDIATION_SUCCESS_RATE = _meter.create_gauge("remediation_success_rate", description="30-day rolling success rate per action")
    REMEDIATION_MEASURE_FAILED_TOTAL = _meter.create_counter("remediation_measure_failed_total", description="Prometheus measurement failures (fail-closed, no remediation triggered)")
    WEBHOOK_RECEIVED_TOTAL = _meter.create_counter("webhook_received_total", description="Alertmanager webhook requests received")
    WEBHOOK_AUTH_FAILED_TOTAL = _meter.create_counter("webhook_auth_failed_total", description="Webhook authentication failures by reason")
    WEBHOOK_VALIDATION_FAILED_TOTAL = _meter.create_counter("webhook_validation_failed_total", description="Webhook payload validation failures")

    Follow exact existing pattern: _meter.create_counter/create_up_down_counter/create_histogram/gauge with description. Unit 's' for latency histograms. CONFIG_RELOAD_LATENCY_SECONDS added per Gemini review suggestion to detect agents falling behind. REMEDIATION_MEASURE_FAILED_TOTAL added per consensus to track Prometheus failures separately.
  </action>
  <verify>
    Run: .venv/bin/python -c "from src.observability.metrics import CONFIG_SET_TOTAL, CONFIG_RELOAD_TOTAL, CONFIG_RELOAD_LATENCY_SECONDS, CONFIG_AUTH_FAILED_TOTAL, REMEDIATION_ATTEMPT_TOTAL, REMEDIATION_MEASURE_FAILED_TOTAL, WEBHOOK_AUTH_FAILED_TOTAL, WEBHOOK_VALIDATION_FAILED_TOTAL; print('Metrics import successful')"
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "^CONFIG_" src/observability/metrics.py` >= 8
    - Source assertion: `grep -c "^REMEDIATION_\|^WEBHOOK_" src/observability/metrics.py` >= 7
    - Import command above prints "Metrics import successful"
    - CONFIG_RELOAD_LATENCY_SECONDS and REMEDIATION_MEASURE_FAILED_TOTAL both defined (new metrics from review)
  </acceptance_criteria>
  <done>
    All new metrics defined (8 config + 8 self-healing). Import works. Metrics match OTel SDK pattern. CONFIG_RELOAD_LATENCY_SECONDS tracks reload lag. REMEDIATION_MEASURE_FAILED_TOTAL tracks Prometheus failures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add topic_config_updates to stream_keys with explicit event contract</name>
  <files>src/core/stream_keys.py</files>
  <read_first>
    - src/core/stream_keys.py (existing topic builder pattern)
  </read_first>
  <action>
    Add to src/core/stream_keys.py after topic_alert_requests function (~line 293):

    def topic_config_updates(env_name: str) -> str:
        """Kafka topic for OPS config change propagation (compacted).

        Topic configuration (created via rpk/admin tools):
          - cleanup.policy=compact
          - min.cleanable.dirty.ratio=0.1
          - partitions=1 (single-partition for global ordering; key is config_key)

        Partition key: config_key (TEXT) - ensures per-key ordering.

        Event contract (JSON payload):
          {
            "schema_version": 1,            # config update event schema version
            "config_key": "regime.prob_min",
            "config_value": "0.35",          # string-encoded; consumer parses by value_type
            "value_type": "float",            # int|float|bool|json|string
            "version": 7,                     # config_state.version after update
            "changed_at": "2026-05-29T12:00:00Z",
            "changed_by": "operator@example.com",
            "operation": "set" | "revert",
            "reason": "optional reason text",
            "redacted": false,
            "correlation_id": "uuid4"
          }

        Tombstone behavior: value=null deletes key from compacted log (not used currently;
        revert publishes a new value, never null). New consumers receive full latest state
        by reading from beginning of compacted topic.

        NOTE: This topic carries OPS config only (hot-reloadable).
        INFRA config (DATABASE_URL, secrets) lives in .env files.
        STRUCT config (plugin tiers, DAG order) lives in code and requires deployment.
        """
        return f"{env_prefix(env_name)}config.updates"

    Follow existing env_prefix() pattern. Docstring includes explicit event contract (Codex MEDIUM finding: "Define config update event contract explicitly").
  </action>
  <verify>
    Run: .venv/bin/python -c "from src.core.stream_keys import topic_config_updates; assert topic_config_updates('') == 'config.updates'; assert topic_config_updates('dev') == 'dev.config.updates'; print('OK')"
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -q "def topic_config_updates" src/core/stream_keys.py` returns 0
    - Source assertion: docstring contains "schema_version", "config_key", "value_type", "operation"
    - Source assertion: docstring contains "cleanup.policy=compact"
    - Behavior: `topic_config_updates('')` returns `'config.updates'`
    - Behavior: `topic_config_updates('dev')` returns `'dev.config.updates'`
  </acceptance_criteria>
  <done>
    Function topic_config_updates added. Docstring specifies partition key, full event schema (including schema_version, value_type, operation), compacted topic config, and three-layer invariant.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create OutboxDispatcherAgent with adaptive polling and retry semantics</name>
  <files>src/config/outbox_dispatcher.py, services/outbox_dispatcher_agent.py</files>
  <read_first>
    - src/config/config_service.py (outbox schema)
    - src/core/agent/base.py (BaseAgent lifecycle)
    - src/core/kafka_utils.py (KafkaProducerClient)
    - src/core/stream_keys.py (topic_config_updates event contract)
    - services/feature_writer_agent.py (writer agent pattern)
  </read_first>
  <action>
    Create src/config/outbox_dispatcher.py with:

    1. OutboxDispatcherAgent(BaseAgent) class. Override `_config_layer = "INFRA"` so the dispatcher itself does NOT subscribe to config updates (avoids circular dependency on the topic it publishes to).

    2. async def _setup(self): creates KafkaProducerClient with bootstrap_servers from settings, calls await self._producer.start()

    3. async def _teardown(self): calls await self._producer.stop() if producer exists

    4. topics_consumed returns [] (agent polls DB, not Kafka)

    5. async def _run(self) -> None: adaptive poll loop (per Codex MEDIUM finding: 100ms aggressive, adaptive backoff better):
       - Idle backoff: start at 100ms; on empty poll, double up to max 2000ms; on non-empty, reset to 100ms.
       - Each iteration:
         a. async with self._db_pool.acquire() as conn: async with conn.transaction():
         b. Claim: UPDATE config_outbox SET status='publishing' WHERE id IN (SELECT id FROM config_outbox WHERE status='pending' AND (next_attempt_at IS NULL OR next_attempt_at <= NOW()) FOR UPDATE SKIP LOCKED LIMIT 100) RETURNING id, config_key, config_value, version, changed_at, retry_count
         c. If empty: commit, sleep current_backoff, current_backoff = min(current_backoff*2, 2000ms), continue
         d. Reset current_backoff to 100ms
         e. For each row: build payload matching topic_config_updates docstring schema:
            {"schema_version": 1, "config_key": row["config_key"], "config_value": row["config_value"], "value_type": <fetched from config_schema>, "version": row["version"], "changed_at": row["changed_at"].isoformat(), "changed_by": "system", "operation": "set", "reason": None, "redacted": <is_secret from schema>, "correlation_id": str(uuid.uuid4())}
            Note: fetch value_type from config_schema in same transaction (single JOIN query OR cache schema lookups).
         f. Try publish via self._producer.publish(topic_config_updates(self.env_name), msg=payload, key=row["config_key"])
         g. On success: UPDATE config_outbox SET status='published' WHERE id=$1; CONFIG_OUTBOX_PENDING.add(-1, {}); CONFIG_OUTBOX_PUBLISH_LATENCY_SECONDS.record(elapsed, {})
         h. On failure: UPDATE config_outbox SET status='pending', retry_count=retry_count+1, next_attempt_at=NOW() + INTERVAL '1 second' * POWER(2, LEAST(retry_count, 6)) WHERE id=$1; log error with structured fields
         i. Commit transaction

    6. Use time.monotonic() for latency measurement.

    7. Use uuid.uuid4() for correlation_id.

    8. Use `msg=` kwarg for KafkaProducerClient.publish (CLAUDE.md gotcha: "value=" silently fails).

    9. Import from src.core.stream_keys import topic_config_updates. Import CONFIG_OUTBOX_PENDING, CONFIG_OUTBOX_PUBLISH_LATENCY_SECONDS from src.observability.metrics.

    Create services/outbox_dispatcher_agent.py:

    #!/usr/bin/env python3
    """OutboxDispatcherAgent — publishes OPS config updates to Kafka.

    Polls config_outbox for pending rows, publishes to topic_config_updates,
    updates status with retry semantics. Transactional outbox pattern.

    Adaptive polling: 100ms when backlog exists, exponential backoff to 2s when idle
    (per review feedback - reduces DB load during quiet periods).
    """
    if __name__ == "__main__":
        import asyncio
        from src.config.outbox_dispatcher import OutboxDispatcherAgent
        asyncio.run(OutboxDispatcherAgent().start())

    Follow BaseAgent lifecycle from services/alerting_agent.py. FOR UPDATE SKIP LOCKED enables horizontal scaling.
  </action>
  <verify>
    Test 1: Insert config_outbox row with status='pending', run dispatcher for 2 seconds, verify row status='published' and message present in topic with all schema fields (schema_version, value_type, operation, correlation_id).
    Test 2: Simulate Kafka publish failure (kill broker briefly), verify status returns to 'pending' with retry_count incremented and next_attempt_at = approximately NOW() + 2^retry_count seconds.
    Test 3: Run dispatcher against empty outbox for 10 seconds, verify poll interval grows from 100ms toward 2000ms (check logs/timing).
    Run: .venv/bin/python services/outbox_dispatcher_agent.py
  </verify>
  <acceptance_criteria>
    - Behavior: Inserted pending outbox row becomes 'published' within 500ms of dispatcher startup
    - Behavior: Published Kafka message JSON includes keys: schema_version, config_key, config_value, value_type, version, changed_at, changed_by, operation, correlation_id
    - Behavior: Failed publish increments retry_count by 1 and sets next_attempt_at to between NOW() and NOW()+128s
    - Source assertion: `grep -q "msg=" src/config/outbox_dispatcher.py` returns 0 (uses correct kwarg)
    - Source assertion: `grep -q "FOR UPDATE SKIP LOCKED" src/config/outbox_dispatcher.py` returns 0
    - Source assertion: adaptive backoff code path present (e.g., `min(.*2000` or similar bounded doubling)
    - Source assertion: `_config_layer = "INFRA"` set on OutboxDispatcherAgent to skip self-subscription
  </acceptance_criteria>
  <done>
    OutboxDispatcherAgent polls config_outbox with adaptive backoff (100ms->2000ms). Transactional claim with pending->publishing->published. Publishes payload matching documented event contract. Failure paths increment retry_count with exponential backoff (max 64s). Uses msg= kwarg (not value=). Dispatcher itself does not subscribe to config topic (avoids circular dependency).
  </done>
</task>

<task type="auto">
  <name>Task 4: Create ConfigService HTTP API with mandatory auth on port 9001</name>
  <files>services/config_service_agent.py</files>
  <read_first>
    - src/config/config_service.py (ConfigService API)
    - src/api/main.py (FastAPI pattern)
    - src/config/settings.py (settings pattern)
  </read_first>
  <action>
    Create services/config_service_agent.py with:

    1. #!/usr/bin/env python3 shebang

    2. Module docstring explaining three-layer invariant (INFRA/STRUCT/OPS) and that this API handles OPS only. Document port convention: 9001=HTTP API, 9005=OTel metrics scrape endpoint.

    3. FastAPI app: app = FastAPI(title="IndicAgent Config Service", version="1.0.0")

    4. ConfigService singleton: config_service = None

    5. API Token: CONFIG_API_TOKEN = os.getenv("CONFIG_API_TOKEN"). Document in docstring that token CAN be rotated by updating env + restarting service (Gemini suggestion noted; live rotation deferred to Phase 110).

    6. Auth dependency function verify_auth(request: Request): if CONFIG_API_TOKEN is set, require Authorization: Bearer <token> header matching CONFIG_API_TOKEN. On missing header: CONFIG_AUTH_FAILED_TOTAL.add(1, {"reason": "missing_header"}); raise HTTPException(401). On invalid: emit reason=invalid_token; raise 401. If CONFIG_API_TOKEN not set (dev mode), pass through.

    7. Startup handler initializes ConfigService with settings.database_url, calls await config_service.initialize().

    8. POST /api/config/set endpoint with Depends(verify_auth). Body: {"key": str, "value": Any, "changed_by": str, "expected_version": int | None, "reason": str | None}. Calls await config_service.set(**body). On ConfigValidationError -> 422 (preserve message). On ConfigVersionConflict -> 409. On success: emit CONFIG_SET_TOTAL.add(1, {"key": key, "changed_by": changed_by, "outcome": "success"}) and return ConfigChange JSON.

    9. GET /api/config/get/{key} endpoint with Depends(verify_auth). Query param default. Returns {"key": key, "value": value}.

    10. GET /api/config/list endpoint with Depends(verify_auth). No category parameter (OPS only). Returns dict.

    11. POST /api/config/revert endpoint with Depends(verify_auth). Body: {"key": str, "version": int, "changed_by": str, "reason": str | None}.

    12. Exception handlers: ConfigValidationError -> 422, ConfigVersionConflict -> 409, generic Exception -> 500.

    13. if __name__ == "__main__": import uvicorn; uvicorn.run(app, host="0.0.0.0", port=9001). Comment: "Port 9001 = Config HTTP API. OTel metrics exposed on port 9005 via env METRICS_PORT (configured in systemd unit)."

    Follow FastAPI pattern from src/api/main.py. Auth is MANDATORY when CONFIG_API_TOKEN is set.
  </action>
  <verify>
    Test 1: Start without CONFIG_API_TOKEN. curl POST /api/config/set with valid OPS body returns 200.
    Test 2: Set CONFIG_API_TOKEN=secret. curl POST without header -> 401. curl POST with wrong Bearer -> 401. curl POST with correct Bearer + OPS key -> 200.
    Test 3: curl POST with key="DATABASE_URL" -> 422 with message about INFRA keys.
    Test 4: curl POST with stale expected_version -> 409.
  </verify>
  <acceptance_criteria>
    - CLI: `curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:9001/api/config/set -H "Content-Type: application/json" -d '{"key":"regime.prob_min","value":"0.35","changed_by":"test"}'` returns `401` when CONFIG_API_TOKEN set and no Authorization header
    - CLI: same with `-H "Authorization: Bearer wrong"` returns `401`
    - CLI: same with correct Bearer returns `200`
    - CLI: same with `-d '{"key":"DATABASE_URL","value":"x","changed_by":"test"}'` returns `422`
    - Source assertion: `grep -q "port=9001" services/config_service_agent.py` returns 0
    - Source assertion: comment in file explicitly states `Port 9001 = HTTP API` and `9005 = OTel metrics`
    - Behavior: CONFIG_AUTH_FAILED_TOTAL counter increments on each 401 (verifiable via `:9005/metrics`)
  </acceptance_criteria>
  <done>
    HTTP API on port 9001. Bearer auth mandatory when CONFIG_API_TOKEN set. INFRA/STRUCT keys rejected with 422. Version conflicts -> 409. CONFIG_AUTH_FAILED_TOTAL metric emits. Port comment makes 9001/9005 split explicit.
  </done>
</task>


<task type="auto">
  <name>Task 5: Register new Phase 109 services in service_auditor DAG</name>
  <files>services/service_auditor_agent.py</files>
  <read_first>
    - services/service_auditor_agent.py (current _DAG_ORDER, _AGENT_ID_TO_UNIT, _LAG_THRESHOLDS structure)
  </read_first>
  <action>
    CLAUDE.md MANDATORY RULE: "When adding a service, update _DAG_ORDER, _LAG_THRESHOLDS, _AGENT_ID_TO_UNIT".
    Phase 109 adds THREE new systemd services that MUST be registered:
      - indicagent-config-service        (Plan 02 Task 4)
      - indicagent-outbox-dispatcher     (Plan 02 Task 3 + Plan 03 Task 4)
      - indicagent-self-healing-agent    (Plan 05 Task 1)

    1. In services/service_auditor_agent.py, locate `_DAG_ORDER`. Insert the three new units
       at an appropriate layer. These are infrastructure services that should run AFTER the
       existing L6 writers (so they are visible to writers as dependencies):

         Add after the existing L6 writer block (or at the layer designated for infra services):
           "indicagent-config-service",
           "indicagent-outbox-dispatcher",
           "indicagent-self-healing-agent",

       Preserve existing entries' order; only ADD the three lines. If there is an explicit
       layer-grouping comment scheme in the file, insert each line under a comment like
       `# L6+ infrastructure (Phase 109)`.

    2. Locate `_AGENT_ID_TO_UNIT` and add three new entries:
           "config-service":       "indicagent-config-service",
           "outbox-dispatcher":    "indicagent-outbox-dispatcher",
           "self-healing-agent":   "indicagent-self-healing-agent",

       Use the exact agent_id strings emitted by each agent (verify by grep against
       `self.agent_id` or `self.name` in services/config_service_agent.py,
       services/outbox_dispatcher_agent.py, and services/self_healing_agent.py).

    3. DO NOT add entries to `_LAG_THRESHOLDS` here. The dict itself is being removed in
       Plan 05 Task 3 (loaded from config DB). Skipping _LAG_THRESHOLDS in this task avoids
       a merge conflict with Plan 05.

    4. Add a brief comment block above the inserted entries:
           # Phase 109 services (config foundation + self-healing engine).
           # Lag thresholds for these units, when needed, will be seeded into config_state
           # under alert.lag.* keys by Plan 05 Task 3.
  </action>
  <verify>
    Run: grep -n "indicagent-config-service\|indicagent-outbox-dispatcher\|indicagent-self-healing-agent" services/service_auditor_agent.py
      (must show at least one match per unit in _DAG_ORDER and at least one match per unit in _AGENT_ID_TO_UNIT)
    Run: .venv/bin/python -c "from services.service_auditor_agent import _DAG_ORDER, _AGENT_ID_TO_UNIT; assert 'indicagent-config-service' in _DAG_ORDER; assert 'indicagent-outbox-dispatcher' in _DAG_ORDER; assert 'indicagent-self-healing-agent' in _DAG_ORDER; assert _AGENT_ID_TO_UNIT['config-service']=='indicagent-config-service'; assert _AGENT_ID_TO_UNIT['outbox-dispatcher']=='indicagent-outbox-dispatcher'; assert _AGENT_ID_TO_UNIT['self-healing-agent']=='indicagent-self-healing-agent'; print('OK')"
    Run: .venv/bin/python -m py_compile services/service_auditor_agent.py
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "indicagent-config-service" services/service_auditor_agent.py` >= 2 (once in _DAG_ORDER, once in _AGENT_ID_TO_UNIT)
    - Source assertion: `grep -c "indicagent-outbox-dispatcher" services/service_auditor_agent.py` >= 2
    - Source assertion: `grep -c "indicagent-self-healing-agent" services/service_auditor_agent.py` >= 2
    - Behavior: importing _DAG_ORDER and _AGENT_ID_TO_UNIT from services/service_auditor_agent passes the asserts in <verify>
    - Source assertion: `grep -q "_LAG_THRESHOLDS" services/service_auditor_agent.py` still returns 0 (we did NOT touch _LAG_THRESHOLDS here; Plan 05 will remove it)
  </acceptance_criteria>
  <done>
    Three new Phase 109 services registered in _DAG_ORDER and _AGENT_ID_TO_UNIT. _LAG_THRESHOLDS intentionally untouched (deferred to Plan 05). Satisfies CLAUDE.md mandatory service-registration rule.
  </done>
</task>

</tasks>

<verification>
Start OutboxDispatcherAgent and verify adaptive polling + Kafka publish + retry semantics. Start ConfigServiceAgent on 9001, verify auth (401/200), key domain rejection (422), version conflict (409). Confirm published Kafka events match documented schema (schema_version, value_type, operation, correlation_id fields all present).
</verification>

<success_criteria>
1. topic_config_updates returns correct topic; docstring documents schema_version=1 event contract + compacted topic config
2. OutboxDispatcherAgent uses adaptive polling (100ms->2000ms backoff)
3. OutboxDispatcherAgent overrides _config_layer="INFRA" to skip self-subscription
4. OutboxDispatcherAgent uses msg= kwarg (not value=)
5. Published events include schema_version, value_type, operation, correlation_id
6. ConfigServiceAgent HTTP API on port 9001 (explicit; metrics on 9005)
7. POST /api/config/set returns 401 without/with-invalid Bearer when CONFIG_API_TOKEN set
8. POST /api/config/set returns 422 on INFRA/STRUCT keys
9. POST /api/config/set returns 409 on version conflict
10. CONFIG_AUTH_FAILED_TOTAL and CONFIG_RELOAD_LATENCY_SECONDS metrics defined
11. Failed publishes increment retry_count with exponential backoff (max 64s)
12. services/service_auditor_agent.py registers indicagent-config-service, indicagent-outbox-dispatcher, indicagent-self-healing-agent in _DAG_ORDER AND _AGENT_ID_TO_UNIT (CLAUDE.md mandatory rule)
</success_criteria>

<output>
After completion, create `.planning/phases/109-config-foundation-self-healing-engine/109-02-SUMMARY.md` with:
- Adaptive polling verification (100ms->2000ms)
- Event contract verification (all required fields in Kafka payload)
- Port convention confirmation (9001 HTTP, 9005 metrics)
- Auth 401/200 test results
- Retry semantics with exponential backoff
- Three-layer invariant enforcement
</output>
