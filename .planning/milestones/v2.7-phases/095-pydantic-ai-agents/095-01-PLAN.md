---
phase: 095-pydantic-ai-agents
slug: pydantic-ai-agents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - src/core/ai/worker_context.py
  - tests/unit/core/test_core_ai_worker_context.py
autonomous: true
user_setup: []
requirements:
  - AGENT-EXEC-02

must_haves:
  truths:
    - "pydantic-ai is installed and importable in the project venv"
    - "A frozen WorkerContext dataclass exists at src/core/ai/worker_context.py and imports cleanly"
    - "WorkerContext carries signal_context, llm_chain, db_pool, memory_client with the latter two defaulting to None"
    - "Mutating any WorkerContext field after construction raises FrozenInstanceError"
    - "Importing WorkerContext does NOT import Ring 1 SignalContext at runtime (Ring 0 boundary preserved)"
  artifacts:
    - path: "requirements.txt"
      provides: "pydantic-ai dependency pin"
      contains: "pydantic-ai>=1.0,<2"
    - path: "src/core/ai/worker_context.py"
      provides: "WorkerContext frozen dep container for _run_typed()"
      contains: "class WorkerContext"
      min_lines: 15
    - path: "tests/unit/core/test_core_ai_worker_context.py"
      provides: "Construction, defaults, immutability, and Ring-0 import tests"
      contains: "FrozenInstanceError"
  key_links:
    - from: "src/core/ai/worker_context.py"
      to: "src.core.llm.chain.LLMProviderChain"
      via: "TYPE_CHECKING-only annotation (string form at runtime)"
      pattern: "TYPE_CHECKING"
---

<objective>
Add the `pydantic-ai` dependency and create the `WorkerContext` frozen dependency container that carries per-run deps into `_run_typed()` and into the `LLMAdapter` bridge (Plan 03).

Purpose: `WorkerContext` is the immutable container the FunctionModel bridge reads to build audit context without coupling the bridge to `BaseAIWorker`. `db_pool` and `memory_client` fields are reserved now (defaulting to None) so Phase 097 (Zep memory) never has to change `WorkerContext` and touch every `_run_typed()` call site.

Output: `pydantic-ai>=1.0,<2` in requirements.txt; `src/core/ai/worker_context.py` with the `WorkerContext` dataclass; unit tests proving construction, defaults, immutability, and Ring 0 import cleanliness.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/095-pydantic-ai-agents/095-CONTEXT.md
@.planning/phases/095-pydantic-ai-agents/095-RESEARCH.md
@CLAUDE.md

# Ring 0 precedent for TYPE_CHECKING-only Ring 1 imports
@src/core/ai/base_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pydantic-ai dependency and install</name>
  <read_first>
    - requirements.txt (find the existing pydantic / pydantic-settings lines to place the pin near them)
  </read_first>
  <files>requirements.txt</files>
  <action>
    Add the line `pydantic-ai>=1.0,<2` to requirements.txt, grouped near the existing `pydantic>=2.12.0` and `pydantic-settings>=2.12.0` lines. Do not pin a patch version; the `<2` ceiling guards against the next major. Then install into the project venv with `.venv/bin/pip install "pydantic-ai>=1.0,<2"` (use the venv pip, not bare pip). Per CONTEXT.md D-05/D-06, the package provides `pydantic_ai.Agent`, `pydantic_ai.models.function.FunctionModel`, `pydantic_ai.models.function.AgentInfo`, and `pydantic_ai.messages.ModelResponse`/`ToolCallPart` — Plan 03 and Plan 04 import these.
  </action>
  <verify>`.venv/bin/python -c "import pydantic_ai; from pydantic_ai.models.function import FunctionModel, AgentInfo; from pydantic_ai.messages import ModelResponse, ToolCallPart; from pydantic_ai import Agent; print(pydantic_ai.__version__)"` prints a 1.x version and exits 0</verify>
  <acceptance_criteria>
    - `grep "pydantic-ai>=1.0,<2" requirements.txt` returns the line
    - The import verify command above exits 0 and prints a version starting with "1."
  </acceptance_criteria>
  <done>pydantic-ai 1.x is pinned in requirements.txt and importable from the venv, including FunctionModel, AgentInfo, ModelResponse, ToolCallPart, and Agent</done>
</task>

<task type="auto">
  <name>Task 2: Create WorkerContext frozen dataclass</name>
  <read_first>
    - src/core/ai/base_agent.py (lines 28-34 — the existing TYPE_CHECKING block importing LLMProviderChain and SignalContext; mirror this guard pattern exactly)
    - .planning/phases/095-pydantic-ai-agents/095-CONTEXT.md (D-01 through D-04)
  </read_first>
  <files>src/core/ai/worker_context.py</files>
  <action>
    Create `src/core/ai/worker_context.py` (Ring 0). Module starts with `from __future__ import annotations`. Use `from dataclasses import dataclass` and `from typing import TYPE_CHECKING, Any`. Under `if TYPE_CHECKING:` import `LLMProviderChain` from `src.core.llm.chain` (D-04: TYPE_CHECKING-only annotation; runtime passes a concrete value so no runtime import is needed — this matches the base_agent.py precedent). Define `@dataclass(frozen=True)` `class WorkerContext` with exactly four fields in this order: `signal_context: Any` (D-03: typed `Any` because Ring 0 cannot import Ring 1 `SignalContext` at runtime — mirrors the `AgentOutput.payload: dict[str, Any]` precedent), `llm_chain: "LLMProviderChain"` (string annotation), `db_pool: Any | None = None` (reserved for Phase 097), `memory_client: Any | None = None` (reserved for Phase 097). Add a class docstring noting it is an in-memory dep container, never serialized, and that db_pool/memory_client are reserved for Phase 097 Zep memory. Do NOT make it a Pydantic model (D-02).
  </action>
  <verify>`.venv/bin/python -c "from src.core.ai.worker_context import WorkerContext; import dataclasses; assert dataclasses.is_dataclass(WorkerContext); f={x.name:x for x in dataclasses.fields(WorkerContext)}; assert list(f)==['signal_context','llm_chain','db_pool','memory_client']; print('ok')"` prints ok</verify>
  <acceptance_criteria>
    - `src/core/ai/worker_context.py` defines `@dataclass(frozen=True) class WorkerContext`
    - Fields are exactly `signal_context`, `llm_chain`, `db_pool`, `memory_client` in that order; the latter two default to `None`
    - The `LLMProviderChain` import is inside a `TYPE_CHECKING` block; `grep -c "import.*SignalContext" src/core/ai/worker_context.py` returns 0 (no SignalContext import at all, runtime or TYPE_CHECKING — signal_context is `Any`)
    - Module is not a Pydantic model (`grep -c "BaseModel" src/core/ai/worker_context.py` returns 0)
  </acceptance_criteria>
  <done>WorkerContext exists as a frozen dataclass with four ordered fields, two optional, Ring 0 boundary preserved via TYPE_CHECKING and Any typing</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for WorkerContext</name>
  <read_first>
    - src/core/ai/worker_context.py (the file written in Task 2)
    - tests/unit/core/test_core_ai_output.py (sibling test style/conventions for core ai)
  </read_first>
  <files>tests/unit/core/test_core_ai_worker_context.py</files>
  <action>
    Create `tests/unit/core/test_core_ai_worker_context.py`. Tests: (1) construct a WorkerContext passing a sentinel object for signal_context and a sentinel for llm_chain, leaving db_pool/memory_client defaulted; assert the two defaults are None and the two passed values round-trip. (2) construct with all four fields and assert all round-trip. (3) immutability: attempt `wc.signal_context = something` inside `pytest.raises(dataclasses.FrozenInstanceError)`. (4) Ring 0 import cleanliness: read the source file text and assert no runtime (non-TYPE_CHECKING) import of `SignalContext` exists — assert `"SignalContext" not in source` OR that any reference is confined to a TYPE_CHECKING guard. Use plain pytest (no asyncio needed). Import `dataclasses` for FrozenInstanceError.
  </action>
  <verify>`.venv/bin/pytest tests/unit/core/test_core_ai_worker_context.py -q` passes</verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/core/test_core_ai_worker_context.py -q` exits 0
    - A test asserts `FrozenInstanceError` on field mutation
    - A test asserts db_pool and memory_client default to None
    - A test asserts the source has no runtime SignalContext import
  </acceptance_criteria>
  <done>WorkerContext has passing unit tests covering construction, defaults, immutability, and Ring 0 import cleanliness</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -c "import pydantic_ai"` succeeds
- `.venv/bin/pytest tests/unit/core/test_core_ai_worker_context.py -q` passes
- `.venv/bin/ruff check src/core/ai/worker_context.py tests/unit/core/test_core_ai_worker_context.py` clean
</verification>

<success_criteria>
pydantic-ai 1.x installed and pinned; WorkerContext frozen dataclass exists with four correctly-ordered fields (two optional), Ring 0 boundary intact, fully unit tested.
</success_criteria>

<output>
After completion, create `.planning/phases/095-pydantic-ai-agents/095-01-SUMMARY.md`
</output>
