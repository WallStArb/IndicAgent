---
phase: 101-composite-fitness-function
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/115_agent_fitness.sql
  - src/config/settings.py
  - src/observability/metrics.py
  - src/intelligence/ai/fitness/__init__.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "agent_fitness hypertable exists with exactly the 11 D-02 columns (agent_id, evaluated_at, accuracy_score, novelty_score, calibration_score, regime_score, efficiency_score, composite_score, n_resolved, promotion_ready, dimensions_jsonb) and is queryable"
    - "shadow_registry has a promotion_baseline column for DemotionGate trigger 1"
    - "The two source tables fitness_auditor scans over the 90-day lookback (signal_ledger via signal_ledger_full, llm_calls) have VERIFIED indexes supporting (key, time) lookups so the hourly scan is bounded — migration 115 creates any missing index idempotently (review CONCERN 1a: indexes must be verified to exist, not trusted)"
    - "Settings exposes all FITNESS_* threshold constants read by fitness_auditor (including FITNESS_LOOKBACK_DAYS for query-scope bounding)"
    - "FITNESS_* OTel metrics are importable from src/observability/metrics.py (stddev gauge, variance diagnostic gauge, gate-status gauge with a -2=no_live_agents value, plus the three per-run scan-cost gauges rows_scanned/agents_evaluated/runtime_ms)"
    - "src/intelligence/ai/fitness/ is a Python package importable by calculators"
  artifacts:
    - path: "production/migrations/115_agent_fitness.sql"
      provides: "agent_fitness hypertable + shadow_registry.promotion_baseline column + verified source-table indexes for the 90-day scan"
      contains: "create_hypertable('agent_fitness'"
    - path: "src/config/settings.py"
      provides: "FITNESS_* threshold Settings fields"
      contains: "FITNESS_ACCURACY_MIN_N"
    - path: "src/observability/metrics.py"
      provides: "FITNESS_* OTel metric instruments"
      contains: "FITNESS_COMPOSITE_SCORE"
    - path: "src/intelligence/ai/fitness/__init__.py"
      provides: "fitness package init"
      min_lines: 1
  key_links:
    - from: "fitness_auditor.py (plan 06)"
      to: "Settings.FITNESS_ACCURACY_MIN_N"
      via: "Settings() field read"
      pattern: "fitness_accuracy_min_n|FITNESS_ACCURACY_MIN_N"
    - from: "agent_fitness"
      to: "shadow_registry"
      via: "promotion_baseline ALTER TABLE"
      pattern: "promotion_baseline"
    - from: "fitness_auditor.py (plan 06) 90-day scan"
      to: "signal_ledger / llm_calls source-table indexes"
      via: "(key, time DESC) index supporting the bounded lookback query"
      pattern: "idx_.*signal_ledger|idx_.*llm_calls"
---

<objective>
Lay the shared substrate for the composite fitness system: the `agent_fitness` TimescaleDB hypertable, the `shadow_registry.promotion_baseline` column, verified source-table indexes for the 90-day scan, all `FITNESS_*` Settings constants, all `FITNESS_*` OTel metric instruments (including per-run scan-cost gauges), and the empty `src/intelligence/ai/fitness/` package. Every downstream plan (calculators, gates, auditor) imports from these artifacts.

Purpose: This is the dependency root. Calculators need the package to exist; `fitness_auditor.py` needs Settings + metrics + the table + bounded-query indexes; gates need nothing here but the package. Building this first unblocks all parallel work in Wave 2.
Output: Migration 115, modified Settings, modified metrics, fitness package init.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-composite-fitness-function/101-CONTEXT.md
@.planning/phases/101-composite-fitness-function/101-PATTERNS.md
@.planning/phases/101-composite-fitness-function/101-REVIEWS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 115 — agent_fitness hypertable + promotion_baseline column + verified source-table indexes</name>
  <files>production/migrations/115_agent_fitness.sql</files>
  <read_first>
    - production/migrations/086_validation_results.sql (idempotent ADD COLUMN IF NOT EXISTS + create_hypertable pattern; exact analog)
    - production/migrations/095_signal_ledger_split.sql (compression policy + compress_segmentby pattern, lines 47-52; AND the existing signal_ledger / signal_outcomes indexes — read these to determine which (setup_plugin, timestamp) and (signal_id) indexes already exist so you only CREATE what is missing)
    - production/migrations/019_llm_intelligence_layer.sql + production/migrations/087_llm_calls_agent_attrs.sql (existing llm_calls indexes — determine whether an (agent_id, called_at DESC) index already exists before creating one)
    - production/migrations/077_shadow_governance.sql (shadow_registry definition — confirm promotion_baseline does not already exist)
  </read_first>
  <action>
    Create migration `115_agent_fitness.sql`. It must:
    1. `CREATE TABLE IF NOT EXISTS agent_fitness` with exactly these 11 columns (D-02 — count is 11, not 10; the cross-AI review miscounted): `agent_id TEXT NOT NULL`, `evaluated_at TIMESTAMPTZ NOT NULL`, `accuracy_score DOUBLE PRECISION`, `novelty_score DOUBLE PRECISION`, `calibration_score DOUBLE PRECISION`, `regime_score DOUBLE PRECISION`, `efficiency_score DOUBLE PRECISION`, `composite_score DOUBLE PRECISION`, `n_resolved INTEGER`, `promotion_ready BOOLEAN`, `dimensions_jsonb JSONB`. All five dimension scores plus composite are nullable (NULL = not yet computable). Primary key is the composite `(agent_id, evaluated_at)`.
    2. Call `SELECT create_hypertable('agent_fitness', 'evaluated_at', chunk_time_interval => INTERVAL '7 days', if_not_exists => TRUE);`
    3. Enable compression: `ALTER TABLE agent_fitness SET (timescaledb.compress, timescaledb.compress_segmentby = 'agent_id', timescaledb.compress_orderby = 'evaluated_at DESC');`
    4. `SELECT add_compression_policy('agent_fitness', INTERVAL '7 days', if_not_exists => TRUE);`
    5. Add an index for the DISTINCT ON current-state query: `CREATE INDEX IF NOT EXISTS idx_agent_fitness_agent_eval ON agent_fitness (agent_id, evaluated_at DESC);`
    6. `ALTER TABLE shadow_registry ADD COLUMN IF NOT EXISTS promotion_baseline DOUBLE PRECISION;`
    7. SOURCE-TABLE SCAN INDEXES (review CONCERN 1a — the hourly 90-day scan over signal_ledger + llm_calls must be index-supported, and the indexes must be VERIFIED to exist rather than trusted). After reading the existing indexes in migrations 095/019/087, add ONLY the missing ones, all `CREATE INDEX IF NOT EXISTS` so the statement is a no-op if already present: (a) `idx_signal_ledger_plugin_ts` on `signal_ledger (setup_plugin, timestamp DESC)` to support the I7-plugin lookback query `WHERE setup_plugin=$1 AND timestamp > NOW() - make_interval(days => $2)`; (b) confirm `signal_outcomes` has an index on `signal_id` for the JOIN (create `idx_signal_outcomes_signal_id` IF NOT EXISTS if absent); (c) `idx_llm_calls_agent_called` on `llm_calls (agent_id, called_at DESC)` to support the swarm lookback query `WHERE agent_id=$1 AND called_at > NOW() - make_interval(days => $2)`. Add a comment above this block: `-- 90-day fitness scan support (verified index coverage; see Phase 101 review CONCERN 1)`.
    NO retention policy — fitness history is permanent (D-01, specifics). Every statement must be idempotent (IF NOT EXISTS). Include a migration header comment block matching the 086 format (Purpose, Idempotency, Dependencies on 077_shadow_governance.sql + 095_signal_ledger_split.sql + TimescaleDB).
    Apply the migration: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/115_agent_fitness.sql`.
  </action>
  <verify>
    `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d agent_fitness"` lists all 11 columns. `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT * FROM timescaledb_information.hypertables WHERE hypertable_name = 'agent_fitness';"` returns one row. `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d shadow_registry" | grep promotion_baseline` shows the column.
    Index coverage: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT indexname FROM pg_indexes WHERE tablename IN ('signal_ledger','llm_calls','signal_outcomes');"` shows a (setup_plugin, timestamp) index on signal_ledger, an (agent_id, called_at) index on llm_calls, and a signal_id index on signal_outcomes (whether pre-existing or created by this migration).
  </verify>
  <acceptance_criteria>
    - production/migrations/115_agent_fitness.sql contains `create_hypertable('agent_fitness'`
    - 115_agent_fitness.sql contains `ADD COLUMN IF NOT EXISTS promotion_baseline DOUBLE PRECISION`
    - 115_agent_fitness.sql contains `CREATE INDEX IF NOT EXISTS` statements covering signal_ledger(setup_plugin, timestamp) and llm_calls(agent_id, called_at) (skipped/no-op only if a read of 095/019/087 proves an equivalent index already exists, in which case the SUMMARY records which pre-existing index satisfies each scan)
    - `\d agent_fitness` lists columns: agent_id, evaluated_at, accuracy_score, novelty_score, calibration_score, regime_score, efficiency_score, composite_score, n_resolved, promotion_ready, dimensions_jsonb
    - timescaledb_information.hypertables returns a row for agent_fitness
    - pg_indexes confirms a (setup_plugin, timestamp) index on signal_ledger and an (agent_id, called_at) index on llm_calls exist after the migration
    - Re-running the migration file a second time produces no error (idempotent)
    - `\d shadow_registry` shows promotion_baseline column of type double precision
  </acceptance_criteria>
  <done>agent_fitness hypertable exists with the exact D-02 column set, compression configured (7-day, no retention), shadow_registry has promotion_baseline, and the source tables for the 90-day scan have verified index coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Add FITNESS_* Settings fields and FITNESS_* OTel metrics</name>
  <files>src/config/settings.py, src/observability/metrics.py</files>
  <read_first>
    - src/config/settings.py (existing SWARM_*/SHADOW_* Field pattern around lines 191-216; class Settings declaration; model_config)
    - src/observability/metrics.py (point_gauge factory and SHADOW_* block; _meter.create_counter pattern)
    - .planning/phases/101-composite-fitness-function/101-PATTERNS.md (exact Field and metric snippets, lines 300-391)
    - .planning/phases/101-composite-fitness-function/101-REVIEWS.md (CONCERN 1c: per-run scan-cost metrics; CONCERN 2: gate-status must distinguish insufficient_population from no_live_agents)
  </read_first>
  <action>
    In `src/config/settings.py`, add these fields to the `Settings` class using the existing `Field(default=..., validation_alias="...", description="...")` pattern: `FITNESS_ACCURACY_MIN_N: int = 50`, `FITNESS_CALIBRATION_MIN_N: int = 30`, `FITNESS_REGIME_MIN_N_PER_REGIME: int = 10`, `FITNESS_REGIME_MIN_REGIMES: int = 2`, `FITNESS_EFFICIENCY_MIN_N: int = 20`, `FITNESS_STALENESS_THRESHOLD_HOURS: int = 4`, `FITNESS_EFFICIENCY_TOKEN_CEILING: int = 4096`, `FITNESS_NOVELTY_MIN_OVERLAP: int = 20`, `FITNESS_VARIANCE_GATE_MIN_POPULATION: int = 5`, `FITNESS_VARIANCE_GATE_THRESHOLD: float = 0.2` (this threshold gates population *standard deviation* of composite_score — `statistics.pstdev` — NOT true variance; for bounded [0,1] composites true variance 0.2 is unattainable, so the FIT-06 gate is on stddev per the resolved ROADMAP terminology; keep the legacy name `FITNESS_VARIANCE_GATE_THRESHOLD` to avoid churn but document the stddev semantics in the field `description`), `FITNESS_LOOKBACK_DAYS: int = 90` (rolling lookback window applied to BOTH the signal_ledger_full read and the llm_calls read in fitness_auditor, so hourly audits never scan unbounded history — addresses the resource-risk review finding). Use UPPER_SNAKE attribute names matching the existing SWARM_* convention in this file, each with a matching `validation_alias` of the same name. Each field needs a one-line `description`.
    In `src/observability/metrics.py`, immediately after the SHADOW_* block, add a `# Fitness auditor metrics (Phase 101)` section with: `FITNESS_COMPOSITE_SCORE = point_gauge("fitness_composite_score", "5D composite fitness score per agent")`, `FITNESS_PROMOTION_READY = point_gauge("fitness_promotion_ready", "1 when PromotionGate all criteria pass")`, `FITNESS_POPULATION_STDDEV = point_gauge("fitness_population_stddev", "FIT-06 gate metric: population stddev (statistics.pstdev) of composite_score across live agents; gate passes when >= FITNESS_VARIANCE_GATE_THRESHOLD")`, `FITNESS_POPULATION_VARIANCE = point_gauge("fitness_population_variance", "Diagnostic: population variance (pstdev^2) of composite_score across live agents — emitted alongside stddev so operators can see both and never confuse the gate metric")`, `FITNESS_DEMOTION_TRIGGERED = _meter.create_counter("fitness_demotion_triggered_total", description="DemotionGate triggers fired per agent and trigger name")`, `FITNESS_STALE_SKIP_TOTAL = _meter.create_counter("fitness_stale_skip_total", description="Agents skipped by shadow_auditor due to stale agent_fitness row")`, `FITNESS_VARIANCE_GATE_STATUS = point_gauge("fitness_variance_gate_status", "FIT-06 gate: 1=passed, 0=blocked, -1=insufficient_population, -2=no_live_agents")` (review CONCERN 2 — the gate must distinguish a population that is too small from a population that is empty), `FITNESS_SCAN_ROWS = point_gauge("fitness_scan_rows", "Per-run total source rows scanned by fitness_auditor across all agents (scan-cost observability — review CONCERN 1c)")`, `FITNESS_AGENTS_EVALUATED = point_gauge("fitness_agents_evaluated", "Per-run count of agents the fitness_auditor evaluated")`, `FITNESS_AUDIT_RUNTIME_MS = point_gauge("fitness_audit_runtime_ms", "Per-run wall-clock runtime of one fitness_auditor pass in milliseconds — operators alert if this approaches the 60-minute timer window")`.
    Do not use prometheus_client. Use only the existing `point_gauge` and `_meter.create_counter` factories already in metrics.py.
  </action>
  <verify>
    `.venv/bin/python -c "from src.config.settings import Settings; s=Settings(); assert s.FITNESS_ACCURACY_MIN_N==50 and s.FITNESS_VARIANCE_GATE_THRESHOLD==0.2 and s.FITNESS_EFFICIENCY_TOKEN_CEILING==4096 and s.FITNESS_LOOKBACK_DAYS==90; print('settings ok')"`.
    `.venv/bin/python -c "from src.observability.metrics import FITNESS_COMPOSITE_SCORE, FITNESS_POPULATION_STDDEV, FITNESS_POPULATION_VARIANCE, FITNESS_DEMOTION_TRIGGERED, FITNESS_STALE_SKIP_TOTAL, FITNESS_VARIANCE_GATE_STATUS, FITNESS_PROMOTION_READY, FITNESS_SCAN_ROWS, FITNESS_AGENTS_EVALUATED, FITNESS_AUDIT_RUNTIME_MS; print('metrics ok')"`.
  </verify>
  <acceptance_criteria>
    - settings.py contains FITNESS_ACCURACY_MIN_N, FITNESS_CALIBRATION_MIN_N, FITNESS_REGIME_MIN_N_PER_REGIME, FITNESS_REGIME_MIN_REGIMES, FITNESS_EFFICIENCY_MIN_N, FITNESS_STALENESS_THRESHOLD_HOURS, FITNESS_EFFICIENCY_TOKEN_CEILING, FITNESS_NOVELTY_MIN_OVERLAP, FITNESS_VARIANCE_GATE_MIN_POPULATION, FITNESS_VARIANCE_GATE_THRESHOLD, FITNESS_LOOKBACK_DAYS
    - The settings import-and-assert command prints "settings ok" and exits 0
    - The metrics import command (including FITNESS_SCAN_ROWS, FITNESS_AGENTS_EVALUATED, FITNESS_AUDIT_RUNTIME_MS) prints "metrics ok" and exits 0
    - metrics.py FITNESS_VARIANCE_GATE_STATUS description mentions -2=no_live_agents
    - metrics.py contains no new `import prometheus_client`
    - `.venv/bin/ruff check src/config/settings.py src/observability/metrics.py` exits 0
  </acceptance_criteria>
  <done>All 11 FITNESS_* Settings fields (including FITNESS_LOOKBACK_DAYS) load with their defaults and all 10 FITNESS_* metric instruments (gate metrics + variance diagnostic + three per-run scan-cost gauges) are importable.</done>
</task>

<task type="auto">
  <name>Task 3: Create the fitness package init</name>
  <files>src/intelligence/ai/fitness/__init__.py</files>
  <read_first>
    - src/intelligence/ai/alpha/__init__.py (role-match analog for an ai subpackage __init__)
  </read_first>
  <action>
    Create `src/intelligence/ai/fitness/__init__.py` with a module docstring `"""Composite fitness evaluation: 5 dimension calculators, geometric-mean composite, and pure promotion/demotion gates (Phase 101)."""`. Keep it minimal — do NOT re-export calculators yet (they are created in plans 02-05; premature imports would break this plan's import test). The package may be expanded by later plans.
  </action>
  <verify>
    `.venv/bin/python -c "import src.intelligence.ai.fitness; print('fitness package ok')"` prints and exits 0.
  </verify>
  <acceptance_criteria>
    - src/intelligence/ai/fitness/__init__.py exists and contains a module docstring
    - `import src.intelligence.ai.fitness` succeeds (exit 0)
    - __init__.py does not import accuracy/calibration/regime/efficiency/novelty/composite/gates modules (which do not exist yet)
  </acceptance_criteria>
  <done>The fitness package is importable, ready for Wave 2 calculator modules.</done>
</task>

</tasks>

<verification>
- Migration 115 applied; agent_fitness hypertable + shadow_registry.promotion_baseline exist; source-table scan indexes verified present.
- Settings loads all FITNESS_* fields; all 10 metrics importable; fitness package importable.
- `.venv/bin/ruff check src/config/settings.py src/observability/metrics.py src/intelligence/ai/fitness/` exits 0.
</verification>

<success_criteria>
The dependency root is complete: the table, columns, verified scan indexes, Settings constants, OTel metrics (including per-run scan-cost gauges and the -2=no_live_agents gate status), and package exist. Wave 2 (calculators) and Wave 3 (auditor) can import everything they need from here.
</success_criteria>

<output>
After completion, create `.planning/phases/101-composite-fitness-function/101-01-SUMMARY.md`. Record which source-table scan indexes were pre-existing vs newly created (review CONCERN 1a evidence).
</output>
