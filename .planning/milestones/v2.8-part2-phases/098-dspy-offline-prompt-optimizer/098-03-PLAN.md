---
phase: 098-dspy-offline-prompt-optimizer
plan: 03
type: execute
wave: 3
depends_on: ["098-02"]
files_modified:
  - services/dspy_optimizer.py
  - production/systemd/indicagent-dspy-optimizer.service
  - production/systemd/indicagent-dspy-optimizer.timer
  - services/service_auditor.py
autonomous: true
requirements: [OPT-03]

must_haves:
  truths:
    - "Running services/dspy_optimizer.py executes one optimization pass and exits (no daemon loop)"
    - "Exactly one job_completed_total{job=dspy-optimizer, status} is emitted per run, with status taken from the optimizer's terminal status (success | failure | skipped_data_gate)"
    - "indicagent-dspy-optimizer.timer fires weekly on Monday 07:00 UTC"
    - "indicagent-dspy-optimizer is registered in both _DAG_ORDER and _ONESHOT_UNITS so its inactive-between-runs state is not treated as a failure"
    - "The optimizer never opens a Kafka consumer or writes to the live inference path"
  artifacts:
    - path: "services/dspy_optimizer.py"
      provides: "oneshot main() entrypoint wrapping DSPyOptimizer, emitting exactly one terminal counter"
      contains: "DSPyOptimizer"
    - path: "production/systemd/indicagent-dspy-optimizer.service"
      provides: "Type=oneshot systemd unit"
      contains: "Type=oneshot"
    - path: "production/systemd/indicagent-dspy-optimizer.timer"
      provides: "weekly Monday 07:00 UTC timer"
      contains: "OnCalendar=Mon *-*-* 07:00:00 UTC"
  key_links:
    - from: "services/dspy_optimizer.py"
      to: "JOB_COMPLETED_TOTAL"
      via: "add(1, {job: dspy-optimizer, status: agent._last_status})"
      pattern: "job.*dspy-optimizer"
    - from: "indicagent-dspy-optimizer.service"
      to: "services/dspy_optimizer.py"
      via: "ExecStart python services/dspy_optimizer.py"
      pattern: "dspy_optimizer.py"
---

<objective>
Build the oneshot entrypoint and systemd timer that drive OPT-03: the optimizer runs as a timer-triggered batch job (not a daemon) and does not touch the live inference path. The entrypoint mirrors `ml_training_agent.py`; the systemd units mirror the ml-training unit pair with a weekly Monday schedule.

D-08 / REVIEWS Finding 5 (DST fix): the schedule is stated as "weekly, Monday 07:00 UTC" with NO ET translation. The previous "02:00 ET" annotation was ambiguous across DST (02:00 EDT = 06:00 UTC; 02:00 EST = 07:00 UTC). Drop the ET label entirely. The timer fires Monday 07:00 UTC year-round; for a weekly batch job the exact wall-clock hour is immaterial.

REVIEWS Finding 7 (single terminal counter): the entrypoint emits EXACTLY ONE `job_completed_total` per run. The status comes from the optimizer's terminal status field (`agent._last_status`, set in Plan 02). The `skipped_data_gate` state is NOT a separately-emitted counter from inside the class — it is carried on `self._last_status` and emitted once here. main() must not double-count.

Purpose: Make the DSPyOptimizer (Plan 02) runnable and schedulable, with the mandatory D-11 oneshot OTel contract emitted exactly once.
Output: `services/dspy_optimizer.py`, `indicagent-dspy-optimizer.service`, `indicagent-dspy-optimizer.timer`, and registrations in `_DAG_ORDER` + `_ONESHOT_UNITS`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/098-dspy-offline-prompt-optimizer/098-CONTEXT.md
@.planning/phases/098-dspy-offline-prompt-optimizer/098-PATTERNS.md
@.planning/phases/098-dspy-offline-prompt-optimizer/098-REVIEWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create oneshot entrypoint services/dspy_optimizer.py with single terminal counter</name>
  <files>services/dspy_optimizer.py</files>
  <read_first>
    - services/ml_training_agent.py (canonical oneshot main(): _path_bootstrap first import, try/except/finally, JOB_COMPLETED_TOTAL, flush_and_shutdown_metrics)
    - src/intelligence/optimization/dspy_optimizer.py (DSPyOptimizer class and its `self._last_status` field — Plan 02)
    - CLAUDE.md "Oneshot contract (D-06)" (job label must match systemd unit %n suffix exactly: dspy-optimizer)
  </read_first>
  <action>
    Create `services/dspy_optimizer.py` modeled on `ml_training_agent.py`:
    - module docstring referencing Phase 098 and the weekly Monday 07:00 UTC timer (NO "02:00 ET" — REVIEWS Finding 5)
    - `import _path_bootstrap  # noqa: F401` as the FIRST import before any `from src.*` (RESEARCH pitfall 6 — do not reorder)
    - `from src.config.settings import Settings`
    - `from src.intelligence.optimization.dspy_optimizer import DSPyOptimizer`
    - `from src.observability.metrics import JOB_COMPLETED_TOTAL, flush_and_shutdown_metrics`

    main() structure (REVIEWS Finding 7 — emit exactly one terminal counter):
    - try block: `settings = Settings()`; `agent = DSPyOptimizer(settings)`; `asyncio.run(agent.start())`; then emit ONE counter using the optimizer's terminal status: `JOB_COMPLETED_TOTAL.add(1, {"job": "dspy-optimizer", "status": agent._last_status})`. Because DSPyOptimizer._run() swallows its own exceptions and records "success"/"failure"/"skipped_data_gate" on `self._last_status`, this single emission covers all three terminal states.
    - except Exception block (catches only an UNEXPECTED outer exception that escapes agent.start(), e.g. failure constructing Settings before the optimizer's own try/except is reached): `JOB_COMPLETED_TOTAL.add(1, {"job": "dspy-optimizer", "status": "failure"})` then re-raise.
    - finally block: `flush_and_shutdown_metrics()`.
    The `job` label value MUST be exactly `"dspy-optimizer"`.
    Do NOT emit `skipped_data_gate` from a separate code path — it arrives only via `agent._last_status` (REVIEWS Finding 7: no double count).
  </action>
  <verify>
    `.venv/bin/python -c "import ast; ast.parse(open('services/dspy_optimizer.py').read())"`
    `.venv/bin/ruff check services/dspy_optimizer.py`
    `grep -c "JOB_COMPLETED_TOTAL.add" services/dspy_optimizer.py` (expect 2: one success-path using agent._last_status, one outer-failure fallback)
  </verify>
  <acceptance_criteria>
    - `services/dspy_optimizer.py` first import line is `import _path_bootstrap` (before any `from src.` import)
    - File contains `from src.intelligence.optimization.dspy_optimizer import DSPyOptimizer`
    - The success path emits `JOB_COMPLETED_TOTAL.add(1, {"job": "dspy-optimizer", "status": agent._last_status})` (status sourced from the optimizer, NOT a hardcoded "success")
    - The only other counter emission is inside the `except` block with status "failure" followed by a re-raise
    - There are exactly two `JOB_COMPLETED_TOTAL.add` calls total (one terminal-status emission, one outer-exception fallback) — never three
    - The literal string `"skipped_data_gate"` does NOT appear in services/dspy_optimizer.py (it is carried via agent._last_status, set in the class)
    - File contains `flush_and_shutdown_metrics()` inside a `finally` block
    - `ruff check services/dspy_optimizer.py` exits 0
  </acceptance_criteria>
  <done>services/dspy_optimizer.py wraps DSPyOptimizer and emits exactly one job_completed_total per run, sourcing the status from agent._last_status, with a single outer-exception fallback; no double-counting of skipped_data_gate.</done>
</task>

<task type="auto">
  <name>Task 2: Create systemd service + timer; register in _DAG_ORDER and _ONESHOT_UNITS</name>
  <files>production/systemd/indicagent-dspy-optimizer.service, production/systemd/indicagent-dspy-optimizer.timer, services/service_auditor.py</files>
  <read_first>
    - production/systemd/indicagent-ml-training.service (Type=oneshot template: User, WorkingDirectory, Environment, TimeoutStartSec)
    - production/systemd/indicagent-ml-training.timer (OnCalendar/Persistent/Unit structure)
    - services/service_auditor.py (lines 57-100: _DAG_ORDER and the "oneshot timer service; no lag threshold needed" comment style at line 91; lines 162-179: _ONESHOT_UNITS frozenset where indicagent-ml-training is listed)
  </read_first>
  <action>
    Create `production/systemd/indicagent-dspy-optimizer.service` copying the ml-training service file, changing only:
    - `Description=IndicAgent DSPy Offline Prompt Optimizer -- weekly BootstrapFewShot compilation`
    - `ExecStart=/home/bg/dev/indicagent/.venv/bin/python services/dspy_optimizer.py`
    Keep `Type=oneshot`, `User=bg`, `WorkingDirectory=/home/bg/dev/indicagent`, `Environment=PYTHONPATH=...`, `Environment=PYTHONUNBUFFERED=1`, `TimeoutStartSec=7200`, `[Install] WantedBy=multi-user.target`.

    Create `production/systemd/indicagent-dspy-optimizer.timer` copying the ml-training timer, changing:
    - `Description=DSPy Optimizer Timer -- weekly Monday 07:00 UTC` (NO ET annotation — REVIEWS Finding 5)
    - `OnCalendar=Mon *-*-* 07:00:00 UTC`
    - `Unit=indicagent-dspy-optimizer.service`
    - keep `Persistent=true` and `[Install] WantedBy=timers.target`.

    Register the service in `services/service_auditor.py`:
    - Add `"indicagent-dspy-optimizer": 8,  # oneshot timer service; no lag threshold needed` to `_DAG_ORDER` (alongside indicagent-ml-training at line ~91, using the same DAG tier value 8).
    - Add `"indicagent-dspy-optimizer",  # Type=oneshot, timer-triggered weekly` to the `_ONESHOT_UNITS` frozenset (REVIEWS Codex Plan 03 concern — so the auditor does not treat its inactive-between-runs state as a failure and does not try to start it directly).
    Do NOT add a `_LAG_THRESHOLDS` entry (timer-triggered oneshots are not consumer-lag monitored — match how ml-training is handled).
  </action>
  <verify>
    `systemd-analyze verify production/systemd/indicagent-dspy-optimizer.service` (expect no errors)
    `grep -n "indicagent-dspy-optimizer" services/service_auditor.py` (expect a match in _DAG_ORDER AND in _ONESHOT_UNITS)
    `.venv/bin/python -c "import services.service_auditor"`
  </verify>
  <acceptance_criteria>
    - `production/systemd/indicagent-dspy-optimizer.service` contains `Type=oneshot` and `ExecStart=/home/bg/dev/indicagent/.venv/bin/python services/dspy_optimizer.py`
    - `production/systemd/indicagent-dspy-optimizer.timer` contains `OnCalendar=Mon *-*-* 07:00:00 UTC` and `Unit=indicagent-dspy-optimizer.service`
    - The timer Description does NOT contain "ET" (DST ambiguity removed)
    - `grep -n "indicagent-dspy-optimizer" services/service_auditor.py` returns at least two matches: one within the `_DAG_ORDER` dict and one within the `_ONESHOT_UNITS` frozenset
    - `.venv/bin/python -c "import services.service_auditor"` exits 0
    - `systemd-analyze verify production/systemd/indicagent-dspy-optimizer.service` reports no syntax errors
  </acceptance_criteria>
  <done>The systemd service+timer pair exists (Type=oneshot, weekly Monday 07:00 UTC, no ET label) and the service is registered in both _DAG_ORDER and _ONESHOT_UNITS.</done>
</task>

</tasks>

<verification>
- Entrypoint parses, ruff-clean, _path_bootstrap first, exactly one terminal counter sourced from agent._last_status
- Service is Type=oneshot pointing at services/dspy_optimizer.py
- Timer fires Monday 07:00 UTC, Persistent=true, no ET annotation
- Service registered in _DAG_ORDER AND _ONESHOT_UNITS; service_auditor imports cleanly
</verification>

<success_criteria>
- Optimizer is runnable as a oneshot and schedulable weekly Monday 07:00 UTC
- D-11 job_completed_total contract satisfied with exactly one emission per run (no double count)
- No daemon loop, no Kafka consumer, no live-path coupling (OPT-03)
</success_criteria>

<output>
After completion, create `.planning/phases/098-dspy-offline-prompt-optimizer/098-03-SUMMARY.md`
</output>
