---
phase: 098-dspy-offline-prompt-optimizer
plan: 05
type: execute
wave: 4
depends_on: ["098-02", "phase_096"]
deferred_until: phase_096
files_modified:
  - src/intelligence/ai/base_group_service.py
  - src/intelligence/optimization/prompt_loader.py
  - tests/unit/intelligence/optimization/test_prompt_loader.py
autonomous: true
requirements: [OPT-02]

must_haves:
  truths:
    - "At BaseSwarmCoordinator startup, the active compiled program for each agent is loaded from prompt_versions exactly once"
    - "The loaded compiled program is injected into each agent via the Phase 096 AgentDependencies injection point"
    - "No prompt_versions read occurs in the live per-call inference path (DAG Invariant 3)"
    - "An agent with no active prompt_version row falls back to its file-based ACTIVE_VERSION default without error"
    - "A new active version takes effect only on the next service restart"
  artifacts:
    - path: "src/intelligence/optimization/prompt_loader.py"
      provides: "load_active_compiled_prompts(pool) -> dict[agent_id, compiled_dict]"
      contains: "status = 'active'"
    - path: "src/intelligence/ai/base_group_service.py"
      provides: "startup injection call wired into BaseSwarmCoordinator._setup()"
  key_links:
    - from: "src/intelligence/ai/base_group_service.py"
      to: "prompt_versions"
      via: "load_active_compiled_prompts during BaseSwarmCoordinator._setup, once per startup"
      pattern: "load_active_compiled_prompts"
    - from: "load_active_compiled_prompts"
      to: "AgentDependencies"
      via: "compiled_prompt injected into agent dependencies"
      pattern: "compiled_prompt"
---

<objective>
Wire the inference-time consumption of compiled programs (D-06): at `BaseSwarmCoordinator._setup()`, load the `active` compiled program per agent from `prompt_versions` once and inject it via the Phase 096 `AgentDependencies` mechanism. This completes the read half of OPT-02 — the A/B routing field flip in `prompt_versions.status` actually changes inference behavior on the next restart.

NAMING (REVIEWS Finding / Plan 05 HIGH): the coordinator class is `BaseSwarmCoordinator` in `src/intelligence/ai/base_group_service.py` (verified from source 2026-06-03, line 29). Use `BaseSwarmCoordinator` consistently throughout this plan and the implementation. Do NOT use `BaseGroupCoordinator` anywhere — that name does not exist in the codebase.

FRAMING (REVIEWS Finding 6): the stored artifact is a DSPy compiled few-shot program state (demos + metadata), not a rewritten prompt. The live-consumption adapter contract (how a compiled program becomes inference material) is a Phase 096 concern and must be confirmed in Task 1 before any wiring — see REVIEWS consensus "Define the live-consumption adapter before Plan 05".

DEFERRED: This plan depends on Phase 096 (AgentRegistry / AgentDependencies), which is NOT yet built. The executor MUST NOT attempt this plan until Phase 096 has shipped. `AgentDependencies` does not exist in the codebase as of 2026-06-03 — importing it now will fail.

Purpose: Make promotion observable in production without violating DAG Invariant 3 (no DB in live path; load at startup only, same model as contract rolls and calibration curves).
Output: `prompt_loader.py` startup loader, injection wired into `BaseSwarmCoordinator._setup()`, unit tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/098-dspy-offline-prompt-optimizer/098-CONTEXT.md
@.planning/phases/098-dspy-offline-prompt-optimizer/098-RESEARCH.md
@.planning/phases/098-dspy-offline-prompt-optimizer/098-REVIEWS.md
@.planning/phases/096-agent-registry/096-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GATE — confirm Phase 096 AgentDependencies exists and define the consumption adapter</name>
  <files>src/intelligence/ai/base_group_service.py</files>
  <read_first>
    - .planning/phases/096-agent-registry/096-CONTEXT.md (AgentRegistry, AgentDependencies, BaseSwarmCoordinator wiring decisions D-01..D-06)
    - src/intelligence/ai/base_group_service.py (BaseSwarmCoordinator class at line 29; _setup at line 84; self._pool created ~line 123; super()._setup() ~line 151)
  </read_first>
  <action>
    Verify Phase 096 has shipped before proceeding. Run `grep -rn "class AgentDependencies" src/` and `grep -rn "class AgentRegistry" src/`. If neither exists, STOP and report that Phase 098 Plan 05 is blocked on Phase 096 — do not stub or invent the injection point.
    If they exist:
    - Read their definitions to learn the exact field name and injection call for compiled programs, and confirm whether `AgentDependencies` already has a `compiled_prompt` field or needs one added (per CONTEXT.md code_context: "extend it with `compiled_prompt: dict | None = None`").
    - Define and document the live-consumption adapter contract (REVIEWS consensus item 3): decide whether at inference time agents (a) load the compiled program via `dspy` and execute `dspy.Predict`, or (b) extract the compiled few-shot demos from the JSON and render them into the existing PROMPT_REGISTRY template as few-shot examples. Record the chosen contract in the SUMMARY so the implementation in Task 2 is unambiguous. The injected value is `compiled_dict` (the JSONB from prompt_versions), and the agent must emit the candidate's `version_tag` as its `prompt_version` into `llm_calls` when the candidate program is active (so A/B stats in Plan 04 can attribute calls).
  </action>
  <verify>
    `grep -rn "class AgentDependencies" src/` returns a definition (otherwise plan is correctly blocked)
  </verify>
  <acceptance_criteria>
    - If `class AgentDependencies` does NOT exist in src/, the task halts with a clear "blocked on Phase 096" report and no files are modified
    - If it exists, the executor has identified the exact injection field/call, whether `compiled_prompt` needs adding, and has recorded the live-consumption adapter contract (execute DSPy program vs render demos into PROMPT_REGISTRY)
  </acceptance_criteria>
  <done>Phase 096 dependency confirmed present (or plan correctly halted as blocked), and the live-consumption adapter contract is decided and documented.</done>
</task>

<task type="auto">
  <name>Task 2: Build prompt_loader and wire startup injection into BaseSwarmCoordinator</name>
  <files>src/intelligence/optimization/prompt_loader.py, src/intelligence/ai/base_group_service.py</files>
  <read_first>
    - src/intelligence/ai/base_group_service.py (BaseSwarmCoordinator._setup: self._pool created ~line 123, super()._setup() ~line 151; inject after pool creation, before/at super()._setup())
    - src/intelligence/optimization/agent_signatures.py (AGENT_SIGNATURES — its KEYS are the class-level agent_ids whose active program to load: correlation_v1, skeptic, counterfactual_v1, regime_coherence_v1, narrative_v1)
    - production/migrations/115_prompt_versions.sql (status='active' query shape; one-active-per-agent partial unique index guarantees at most one row per agent)
    - The Phase 096 AgentDependencies definition + adapter contract identified in Task 1
    - CLAUDE.md (asyncpg JSONB returns dict directly; UTC; structlog rules)
  </read_first>
  <action>
    Create `src/intelligence/optimization/prompt_loader.py` with `async def load_active_compiled_prompts(pool) -> dict[str, dict]`. Implement with a SINGLE batched query (REVIEWS Plan 05 LOW suggestion — simpler than one query per agent): `SELECT DISTINCT ON (agent_id) agent_id, compiled_prompt, version_tag FROM prompt_versions WHERE status = 'active' AND agent_id = ANY($1) ORDER BY agent_id, promoted_at DESC` passing `list(AGENT_SIGNATURES.keys())`. asyncpg returns `compiled_prompt` as a dict (JSONB) — no json.loads. Return `{agent_id: compiled_dict}` only for agents with an active row; agents with no active row are absent from the dict (caller falls back to file ACTIVE_VERSION). Log `prompt_loader.loaded` (agent_id, version_tag, n_demos=len(compiled_dict.get("predict",{}).get("demos",[]))) per loaded agent and `prompt_loader.fallback` (agent_id) for each AGENT_SIGNATURES key absent from the result.

    In `BaseSwarmCoordinator._setup()`, after `self._pool` is created (~line 123) and before `super()._setup()` (~line 151), call `active_prompts = await load_active_compiled_prompts(self._pool)` and inject each agent's compiled program into its Phase 096 `AgentDependencies` per the adapter contract from Task 1 (use the exact field/call confirmed there; add `compiled_prompt: dict | None = None` to AgentDependencies if not already present). This is the ONLY prompt_versions read in the service lifetime — no per-call read (DAG Invariant 3).
    Do not add a Kafka topic or hot-swap mechanism (deferred per CONTEXT.md).
  </action>
  <verify>
    `.venv/bin/python -c "from src.intelligence.optimization.prompt_loader import load_active_compiled_prompts"`
    `.venv/bin/python -c "import src.intelligence.ai.base_group_service"`
    `.venv/bin/ruff check src/intelligence/optimization/ src/intelligence/ai/base_group_service.py`
    `grep -c "BaseGroupCoordinator" src/intelligence/optimization/prompt_loader.py src/intelligence/ai/base_group_service.py` (expect 0 — wrong name must not appear)
  </verify>
  <acceptance_criteria>
    - `src/intelligence/optimization/prompt_loader.py` defines `async def load_active_compiled_prompts` and its query contains `status = 'active'` and `agent_id = ANY(`
    - The loader returns a dict keyed by the class-level agent_id (AGENT_SIGNATURES keys); agents without an active row are omitted (fallback)
    - `base_group_service.py` contains exactly one call to `load_active_compiled_prompts` inside `BaseSwarmCoordinator._setup()` (grep count == 1)
    - The string `BaseGroupCoordinator` appears in NEITHER modified file (grep count == 0); only `BaseSwarmCoordinator` is used
    - No `load_active_compiled_prompts` or `prompt_versions` SELECT appears in any per-call/_compute path (grep of agent _compute methods shows none)
    - `compiled_prompt` is injected via AgentDependencies (the field is present on AgentDependencies)
    - imports + `ruff check` exit 0
  </acceptance_criteria>
  <done>Active compiled programs load once at startup via a single batched query and inject through AgentDependencies into BaseSwarmCoordinator; missing agents fall back to file defaults; no live-path DB read; no BaseGroupCoordinator references.</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for prompt_loader fallback + batched load</name>
  <files>tests/unit/intelligence/optimization/test_prompt_loader.py</files>
  <read_first>
    - src/intelligence/optimization/prompt_loader.py (Task 2 — single batched query returning rows)
    - tests/unit/intelligence/optimization/ (existing test style from Plan 04)
  </read_first>
  <action>
    Create `tests/unit/intelligence/optimization/test_prompt_loader.py`. Using a fake/mock asyncpg pool (no live DB), test: (a) an agent with an active row returns its compiled_dict keyed by the class-level agent_id; (b) an agent with no active row is absent from the returned dict (fallback path); (c) the loader issues exactly ONE batched query against the pool (assert the mock fetch was awaited once, since Task 2 uses a single `agent_id = ANY($1)` query — not one per agent). Keep tests CI-clean (no DB, no Ollama).
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/optimization/test_prompt_loader.py -q`
  </verify>
  <acceptance_criteria>
    - `test_prompt_loader.py` exists with tests for active-present, active-absent (fallback), and single-batched-query behavior
    - `pytest tests/unit/intelligence/optimization/test_prompt_loader.py -q` exits 0
    - Tests use a mock pool (no create_db_pool, no live psql)
  </acceptance_criteria>
  <done>prompt_loader fallback and single-batched-load behavior are unit-tested CI-clean.</done>
</task>

</tasks>

<verification>
- Phase 096 gate confirmed and live-consumption adapter contract decided before any modification
- load_active_compiled_prompts reads status='active' once at startup via a single batched query, returns agent_id->compiled_dict keyed by class-level agent_id, omits missing agents
- Injection wired into BaseSwarmCoordinator._setup() exactly once; no per-call DB read; no BaseGroupCoordinator references
- Fallback + batched-load behavior unit-tested; pytest green
</verification>

<success_criteria>
- Promoted active compiled programs take effect on next restart via AgentDependencies injection
- DAG Invariant 3 preserved (single startup read, no live-path DB)
- Agents without an active variant fall back cleanly to file ACTIVE_VERSION
- Consistent BaseSwarmCoordinator naming throughout
</success_criteria>

<output>
After completion, create `.planning/phases/098-dspy-offline-prompt-optimizer/098-05-SUMMARY.md`
</output>
