---
phase: 053.2-bar-aggregator-compute-agent
plan: "03"
subsystem: feature-compute-agent
tags: [bar-accumulator, feature-compute-agent, soc, dag, kafka, claude-md]
dependency_graph:
  requires: [053.2-01, 053.2-02]
  provides: [FCA BarAccumulator removal, HTF topic subscription, CLAUDE.md service table update]
  affects: [feature_compute_agent, CLAUDE.md, services/feature_pipeline_service.py]
tech_stack:
  added: []
  patterns: [single-topic-consumer-to-dual, backward-compat-shim, SoC extraction]
key_files:
  created:
    - services/feature_pipeline_service.py
  modified:
    - services/feature_compute_agent.py
    - CLAUDE.md
decisions:
  - "Created feature_pipeline_service.py shim re-exporting FeatureComputeAgent as FeaturePipelineService — 12 existing tests reference old class name; shim avoids touching test files (Rule 2 auto-fix)"
  - "BarAccumulator comment references in docstrings retained — documents the extraction history without being functional references"
  - "Pre-existing ruff E501 at line 705 deferred — out of scope per deviation scope boundary rule"
metrics:
  duration_minutes: 8
  tasks_completed: 2
  files_created: 1
  files_modified: 2
  completed_date: "2026-03-28"
---

# Phase 053.2 Plan 03: FCA BarAccumulator Removal Summary

**One-liner:** FeatureComputeAgent simplified to pure intelligence consumer — BarAccumulator import/instance removed, `_publish_htf_bar()` deleted, HTF topic subscription added, CLAUDE.md service table updated with Bar Aggregator row.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Remove BarAccumulator from FCA, add HTF topic subscription | `51ee4f8` | services/feature_compute_agent.py, services/feature_pipeline_service.py |
| 2 | Update CLAUDE.md Active Services table | `a4691c1` | CLAUDE.md |

## What Was Built

### feature_compute_agent.py (D-01 through D-05)

- **Removed** `from src.core.bar_accumulator import BarAccumulator` import
- **Removed** `self._bar_accumulator = BarAccumulator()` instance variable
- **Simplified** `_process_bar()` — removes `htf_bars = self._bar_accumulator.update(bar)` loop and `all_bars = [bar] + htf_bars` iteration; now processes each arriving bar independently (1m or HTF) through I1-I6 pipeline
- **Removed** `_publish_htf_bar()` method — BarAggregatorComputeAgent owns HTF bar publishing now
- **Added** `topic_market_bars_htf(env)` to topics subscription list alongside `topic_market_bars(env)`
- **Updated** module docstring, class docstring, and method docstring to reflect new DAG topology

### CLAUDE.md (D-17)

- Added `| Bar Aggregator | \`indicagent-bar-aggregator-compute\` | 1m→HTF bar aggregation (5m-1d) via BarAccumulator; publishes to \`market.bars.htf\` | :9120 |` row after Data Provider
- Updated Feature Compute row description to reflect dual-topic subscription
- Updated `feature_compute_agent subscribes to:` comment — now documents Phase 53.2 SoC
- Updated HTF bar flow section to show new DAG: `bar_aggregator_agent → market.bars.htf → feature_compute_agent (I1-I6)`

### services/feature_pipeline_service.py (backward compat shim)

Created re-export shim so 12 existing tests that import `FeaturePipelineService`, `PRICE_SENSITIVE_PLUGINS`, `VIX_REGIME_TF`, `_INSERT_OHLCV_SQL`, and `_adjust_price_state` from `services.feature_pipeline_service` continue to work.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing critical functionality] Created feature_pipeline_service.py backward compat shim**
- **Found during:** Task 1 verification — running `pytest tests/unit/service_tests/test_feature_pipeline_service.py` immediately failed with `ModuleNotFoundError: No module named 'services.feature_pipeline_service'`
- **Issue:** 12 tests import from `services.feature_pipeline_service` with class name `FeaturePipelineService`; file doesn't exist in this worktree (the plan's acceptance criteria requires these tests to pass)
- **Fix:** Created `services/feature_pipeline_service.py` shim re-exporting `FeatureComputeAgent as FeaturePipelineService` plus `PRICE_SENSITIVE_PLUGINS`, `VIX_REGIME_TF`, `_INSERT_OHLCV_SQL`, `_adjust_price_state`
- **Files modified:** `services/feature_pipeline_service.py` (created)
- **Commit:** `51ee4f8`

## Verification Results

| Check | Result |
|-------|--------|
| `grep -c "BarAccumulator" services/feature_compute_agent.py` | 2 (comment-only references in docstrings) |
| `grep -c "_publish_htf_bar" services/feature_compute_agent.py` | 0 |
| `grep -c "topic_market_bars_htf" services/feature_compute_agent.py` | 3 (import + comment + subscription) |
| Topics subscription line | `[topic_market_bars(env), topic_market_bars_htf(env)]` confirmed |
| `grep "indicagent-bar-aggregator-compute" CLAUDE.md` | Bar Aggregator row present |
| `wc -l services/feature_compute_agent.py` | 1295 (> min_lines 1100) |
| All 12 feature pipeline tests | PASS |
| Ruff | 1 pre-existing E501 at line 705 (out of scope) |

## Self-Check: PASSED
