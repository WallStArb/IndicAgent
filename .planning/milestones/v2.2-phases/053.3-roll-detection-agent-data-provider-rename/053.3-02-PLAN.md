---
phase: 053.3-roll-detection-agent-data-provider-rename
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/data_provider_agent.py
  - services/indicagent-data-provider.service
  - production/daemons/high_frequency_tws_daemon.py
  - tests/unit/daemons/test_tws_daemon.py
  - CLAUDE.md
autonomous: true
requirements:
  - D-07
  - D-08
  - D-09
  - D-10
  - D-19
  - D-20

must_haves:
  truths:
    - "DataProviderAgent class exists in data_provider_agent.py"
    - "No RollMonitor instantiation or check_roll call in DataProviderAgent"
    - "All imports referencing tws_daemon are redirected"
    - "Systemd unit renamed from indicagent-tws to indicagent-data-provider"
    - "CLAUDE.md Active Services table shows Data Provider"
  artifacts:
    - path: "services/data_provider_agent.py"
      provides: "Renamed DataProviderAgent class"
      contains: "class DataProviderAgent"
    - path: "services/indicagent-data-provider.service"
      provides: "New systemd unit"
      contains: "indicagent-data-provider"
    - path: "tests/unit/daemons/test_tws_daemon.py"
      provides: "Updated test imports"
      contains: "DataProviderAgent"
  key_links:
    - from: "production/daemons/high_frequency_tws_daemon.py"
      to: "services/data_provider_agent.py"
      via: "import path"
      pattern: "from services.data_provider_agent import DataProviderAgent"
    - from: "tests/unit/daemons/test_tws_daemon.py"
      to: "services/data_provider_agent.py"
      via: "test import"
      pattern: "from services.data_provider_agent import DataProviderAgent"
---

<objective>
Rename tws_daemon.py to data_provider_agent.py, rename TwsDaemon class to DataProviderAgent, remove RollMonitor from it, and update all imports and systemd units.

Purpose: Establishes provider-agnostic naming (D-07) and removes roll detection coupling (D-19) from the data provider. This is a rename + subtraction, not a rewrite.
Output: `services/data_provider_agent.py`, updated systemd unit, updated test imports, updated CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/053.3-roll-detection-agent-data-provider-rename/053.3-CONTEXT.md

<interfaces>
<!-- Current TwsDaemon structure (lines 367-955 in services/tws_daemon.py) -->
Key attributes to preserve in DataProviderAgent:
- self.settings, self.env_name, self._kafka_producer, self._kafka_bootstrap
- self._symbol_to_base: dict[str, str]
- self._reconnect_attempts, self._ib, self._bars_state
- Metrics on port :9100 (unchanged)

Key attributes/methods to REMOVE:
- self._roll_monitor = RollMonitor(self.settings) (line 430)
- self._roll_monitor.update_volume(base_symbol, ...) (line 743)
- self._roll_monitor.check_roll(base_symbol, ...) (line 744)
- await self._roll_monitor._on_roll_confirmed(...) (lines 745-752)
- _on_roll_confirmed method (lines 271-364 — lives on RollMonitor class, not TwsDaemon)
- RollMonitor class definition (lines 73-365) — moves to Plan 03

<!-- Production daemon wrapper -->
From production/daemons/high_frequency_tws_daemon.py:
- Imports TwsDaemon from services.tws_daemon
- Wraps it for the systemd ExecStart path

<!-- Existing systemd unit pattern -->
From services/indicagent-signal-tracker.service:
- [Unit] Description, After, Wants
- [Service] Type=simple, User=bg, WorkingDirectory, PYTHONPATH, PYTHONUNBUFFERED=1
- ExecStart, Restart=always, RestartSec=10, SyslogIdentifier, LimitNOFILE
- [Install] WantedBy=multi-user.target
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv tws_daemon.py, rename class, remove RollMonitor</name>
  <files>services/data_provider_agent.py, services/tws_daemon.py</files>
  <read_first>
    - services/tws_daemon.py (full file — understand class structure, RollMonitor lines 73-365, TwsDaemon lines 367-955, _emit_bar roll detection call site lines 743-752, constructor line 430)
    - production/daemons/high_frequency_tws_daemon.py (import path to update)
  </read_first>
  <action>
    **Step 1 — `git mv services/tws_daemon.py services/data_provider_agent.py`** (per D-07).

    **Step 2 — In `services/data_provider_agent.py`:**

    a) **Delete the entire `RollMonitor` class** (lines 73-365, approximately). This includes:
       - `PAPER_SKIP_CONTRACTS` and `PAPER_ACCOUNT_HOSTS` class constants
       - `__init__`, `_is_paper_account`, `should_skip_symbol`, `get_threshold`, `_apply_tod_adjustment`, `update_volume`, `check_roll`, `_on_roll_confirmed` methods
       - All imports used exclusively by RollMonitor (e.g., `get_roll_window`, `derive_roll_chain` from `src.providers.ibkr`)

    b) **Rename class `TwsDaemon` to `DataProviderAgent`** globally in the file. Update the `if __name__ == "__main__":` block to reference `DataProviderAgent`.

    c) **Remove `self._roll_monitor` from `__init__`** (line ~430): delete `self._roll_monitor = RollMonitor(self.settings)`.

    d) **Remove the roll detection call site in `_emit_bar()`** (lines ~743-752): delete the block:
       ```python
       self._roll_monitor.update_volume(base_symbol, float(state["volume"]))
       if self._roll_monitor.check_roll(base_symbol, bar_utc):
           await self._roll_monitor._on_roll_confirmed(
               base_symbol=base_symbol, old_symbol=symbol,
               kafka_producer=self._kafka_producer, env_name=self.env_name,
           )
       ```

    e) **Remove the `ROLL_MONITOR_ENABLED` docstring reference** (line ~91 — currently a comment/docstring, not code) per D-04.

    f) **Update the `__main__` block** if it references `TwsDaemon` — change to `DataProviderAgent`.

    **Step 3 — Update `production/daemons/high_frequency_tws_daemon.py`:**
    - Change `from services.tws_daemon import TwsDaemon` to `from services.data_provider_agent import DataProviderAgent`
    - Update all references to `TwsDaemon` to `DataProviderAgent` throughout the file

    **Step 4 — Verify no other files in `services/` or `src/` import from `services.tws_daemon`:**
    Run `grep -rn "from services.tws_daemon\|import tws_daemon" services/ src/ production/` and fix any remaining references.
  </action>
  <verify>
    <automated>grep -rn "tws_daemon\|TwsDaemon" services/ src/ production/ --include="*.py" | grep -v "__pycache__" | grep -v ".pyc" && echo "FAIL: tws_daemon references remain" || echo "PASS: no tws_daemon references in services/src/production"</automated>
  </verify>
  <acceptance_criteria>
    - `services/tws_daemon.py` no longer exists (git mv completed)
    - `services/data_provider_agent.py` contains `class DataProviderAgent` (not `class TwsDaemon`)
    - `grep -c "RollMonitor" services/data_provider_agent.py` returns 0
    - `grep -c "_roll_monitor" services/data_provider_agent.py` returns 0
    - `grep -c "_on_roll_confirmed" services/data_provider_agent.py` returns 0
    - `grep -c "check_roll" services/data_provider_agent.py` returns 0
    - `grep -c "ROLL_MONITOR_ENABLED" services/data_provider_agent.py` returns 0
    - `production/daemons/high_frequency_tws_daemon.py` contains `from services.data_provider_agent import DataProviderAgent`
    - `grep -rn "from services.tws_daemon" services/ src/ production/ --include="*.py"` returns empty
  </acceptance_criteria>
  <done>data_provider_agent.py exists with DataProviderAgent class, no RollMonitor code, no tws_daemon references in services/src/production Python files</done>
</task>

<task type="auto">
  <name>Task 2: Update test imports, systemd unit, and CLAUDE.md</name>
  <files>tests/unit/daemons/test_tws_daemon.py, services/indicagent-data-provider.service, CLAUDE.md</files>
  <read_first>
    - tests/unit/daemons/test_tws_daemon.py (imports, TwsDaemon references, _roll_monitor fixture usage — must understand __new__ pattern)
    - services/indicagent-tws.service (current unit file to use as template for rename)
    - CLAUDE.md (Active Services table location — search for "TWS Daemon")
    - docs/cheatsheet.md (references to tws_daemon.py — optional doc update)
  </read_first>
  <action>
    **Step 1 — Update `tests/unit/daemons/test_tws_daemon.py`:**

    a) Change `from services.tws_daemon import TwsDaemon` to `from services.data_provider_agent import DataProviderAgent`
    b) Replace all `TwsDaemon` references with `DataProviderAgent` (class instantiation, `__new__`, type annotations)
    c) Remove `daemon._roll_monitor = ...` from test fixture helpers (e.g., `_make_rtb_loop_daemon()`) since DataProviderAgent no longer has `_roll_monitor`
    d) Remove any test functions that specifically test roll monitor integration within the daemon (they move to Plan 03's new test file)
    e) Update `patch("services.tws_daemon.X")` paths to `patch("services.data_provider_agent.X")` for all remaining patches

    NOTE: Do NOT rename the test file itself in this task — git mv of test files can break CI discovery. The test file stays as `test_tws_daemon.py` with updated contents (or rename if you prefer — both are acceptable).

    **Step 2 — Create `services/indicagent-data-provider.service`** (per D-08):

    ```ini
    [Unit]
    Description=IndicAgent DataProviderAgent — IBKR market data publisher
    After=network-online.target
    StartLimitIntervalSec=300
    StartLimitBurst=5

    [Service]
    Type=simple
    User=bg
    WorkingDirectory=/home/bg/dev/indicagent
    Environment=PYTHONPATH=/home/bg/dev/indicagent
    Environment=PYTHONUNBUFFERED=1
    ExecStart=/home/bg/dev/indicagent/.venv/bin/python services/data_provider_agent.py --client-id 35
    Restart=always
    RestartSec=10
    StandardOutput=journal
    StandardError=journal
    SyslogIdentifier=indicagent-data-provider
    LimitNOFILE=65536

    [Install]
    WantedBy=multi-user.target
    ```

    NOTE: The `ExecStart` points to `services/data_provider_agent.py` directly (NOT through the production/daemons wrapper). Preserve `--client-id 35` argument. Check the existing `indicagent-tws.service` ExecStart to see if it uses the wrapper or direct path — if wrapper, check whether the wrapper is still needed after updating it in Task 1. If the wrapper just imports and runs the daemon, point ExecStart directly.

    **Step 3 — Update CLAUDE.md "Active Services" table** (per D-10):

    Find the row: `| TWS Daemon | indicagent-tws |` and change to:
    `| Data Provider | indicagent-data-provider | Dual IBKR streams: 5s RTB → 1m aggregation + official 1m reconciliation; publishes to market.bars/market.ticks | — |`

    Also update any other CLAUDE.md references to `tws_daemon` in the "Core Runtime Files" section or elsewhere. Search for "tws_daemon" and "tws" (case-insensitive) in CLAUDE.md.

    **Step 4 — Update `docs/cheatsheet.md`** if it references `services/tws_daemon.py` — change to `services/data_provider_agent.py`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/daemons/test_tws_daemon.py -v --co 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/daemons/test_tws_daemon.py` contains `from services.data_provider_agent import DataProviderAgent`
    - `grep -c "TwsDaemon" tests/unit/daemons/test_tws_daemon.py` returns 0
    - `grep -c "_roll_monitor" tests/unit/daemons/test_tws_daemon.py` returns 0
    - `services/indicagent-data-provider.service` contains `ExecStart=` pointing to `data_provider_agent.py`
    - `services/indicagent-data-provider.service` contains `SyslogIdentifier=indicagent-data-provider`
    - `services/indicagent-data-provider.service` contains `PYTHONUNBUFFERED=1`
    - `grep "Data Provider" CLAUDE.md` returns at least 1 match in the Active Services table
    - `grep -c "TWS Daemon" CLAUDE.md` returns 0 in the Active Services table row (may remain in historical context)
    - `.venv/bin/pytest tests/unit/daemons/test_tws_daemon.py --co` exits 0 (tests are discoverable)
  </acceptance_criteria>
  <done>All test imports redirect to data_provider_agent, new systemd unit created, CLAUDE.md table updated, no TwsDaemon references remain in test file</done>
</task>

</tasks>

<verification>
- `grep -rn "tws_daemon\|TwsDaemon" services/ src/ production/ tests/unit/daemons/ --include="*.py" | grep -v __pycache__` — should return empty (no stale references)
- `.venv/bin/pytest tests/unit/daemons/test_tws_daemon.py -v` — all tests pass
- `cat services/indicagent-data-provider.service | grep ExecStart` — correct path
- `.venv/bin/ruff check services/data_provider_agent.py` — clean
</verification>

<success_criteria>
- services/tws_daemon.py no longer exists; services/data_provider_agent.py has DataProviderAgent class
- No RollMonitor code in DataProviderAgent (D-19)
- All Python imports updated (D-09)
- Systemd unit renamed (D-08)
- CLAUDE.md Active Services table updated (D-10)
- Existing daemon tests pass with new imports
</success_criteria>

<output>
After completion, create `.planning/phases/053.3-roll-detection-agent-data-provider-rename/053.3-02-SUMMARY.md`
</output>
