---
phase: 053.3-roll-detection-agent-data-provider-rename
plan: "01"
subsystem: core-schemas
tags: [schema, kafka, roll-detection, tdd]
dependency_graph:
  requires: []
  provides:
    - RollEvent pydantic schema (src/core/schemas/market_events.py)
    - BarGapRequest stub (src/core/schemas/market_events.py)
    - topic_roll_events stream key (src/core/stream_keys.py)
  affects:
    - Plans 053.3-02, 053.3-03, 053.3-04 (consume RollEvent + topic_roll_events)
    - Phase 53.1 (consumes BarGapRequest stub)
tech_stack:
  added: []
  patterns:
    - Pydantic v2 BaseModel with typed fields (follows bar_message.py pattern)
    - topic_* stream key builder (follows env_prefix pattern in stream_keys.py)
key_files:
  created:
    - src/core/schemas/market_events.py
    - tests/unit/test_market_events_schema.py
  modified:
    - src/core/stream_keys.py
decisions:
  - topic_roll_events inserted after topic_market_bars_htf grouped with market.* topics
  - BarGapRequest uses same 4 required fields as plan spec — stub only; Phase 53.1 owns implementation
  - All 8 RollEvent fields required (no defaults) — partial roll data must not flow downstream
metrics:
  duration_seconds: 77
  completed_date: "2026-03-28"
  tasks_total: 1
  tasks_completed: 1
  files_created: 2
  files_modified: 1
---

# Phase 053.3 Plan 01: RollEvent Schema + topic_roll_events Summary

**One-liner:** RollEvent pydantic schema (8 fields) and topic_roll_events() stream key establishing the typed interface-first contract for RollComputeAgent extraction.

## What Was Built

Created the typed contract foundation for Phase 053.3:

1. **`src/core/schemas/market_events.py`** — Two pydantic models:
   - `RollEvent` with 8 required fields: `symbol`, `old_contract`, `new_contract`, `roll_gap_price`, `roll_gap_pct`, `detection_ts`, `volume_zscore`, `confirmation_count`
   - `BarGapRequest` stub with 4 fields for Phase 53.1 BarAuditorAgent

2. **`src/core/stream_keys.py`** — Added `topic_roll_events(env_name: str) -> str` returning `f"{env_prefix(env_name)}market.events.roll"`. Inserted after `topic_market_bars_htf`, grouped with `market.*` topics.

3. **`tests/unit/test_market_events_schema.py`** — 7 TDD tests covering all acceptance criteria.

## TDD Execution

- **RED:** Tests failed with `ModuleNotFoundError: No module named 'src.core.schemas.market_events'` — confirmed no schema existed.
- **GREEN:** Created schema file and stream key function; all 7 tests passed immediately.
- **Lint fix:** Ruff auto-fixed import block ordering in test file (`I001`).

## Commits

| Hash | Message | Files |
|------|---------|-------|
| 3b386e9 | feat(053.3-01): add RollEvent schema, BarGapRequest stub, and topic_roll_events | src/core/schemas/market_events.py, src/core/stream_keys.py, tests/unit/test_market_events_schema.py |

## Verification

- `.venv/bin/pytest tests/unit/test_market_events_schema.py -v` → 7 passed
- `grep -c "class RollEvent" src/core/schemas/market_events.py` → 1
- `grep -c "def topic_roll_events" src/core/stream_keys.py` → 1
- `.venv/bin/ruff check src/core/schemas/market_events.py src/core/stream_keys.py tests/unit/test_market_events_schema.py` → All checks passed

## Deviations from Plan

None — plan executed exactly as written. Lint auto-fix applied by ruff (import sort in test file) — not a deviation, standard toolchain.

## Known Stubs

- `BarGapRequest` in `src/core/schemas/market_events.py` — intentional stub for Phase 53.1 BarAuditorAgent. Fields are complete (symbol, tf, start_ts, end_ts). No logic to implement until Phase 53.1 produces the consumer.

## Self-Check

Files created:
- src/core/schemas/market_events.py — FOUND
- tests/unit/test_market_events_schema.py — FOUND

Commit exists: 3b386e9 — FOUND

## Self-Check: PASSED
