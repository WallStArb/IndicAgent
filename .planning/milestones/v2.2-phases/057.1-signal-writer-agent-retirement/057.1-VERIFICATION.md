---
phase: 057.1-signal-writer-agent-retirement
verified: 2026-03-30T14:05:00Z
status: passed
score: 10/10 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: 9/10
  gaps_closed:
    - "indicagent-signal-tracker.service Wants dependency on signal-generator removed"
  gaps_remaining: []
  regressions: []
---

# Phase 57.1: Signal Writer Agent Retirement — Verification Report

**Phase Goal:** Retire signal_generator_agent and establish SignalWriterAgent as the dedicated DB persistence layer for I7 signals. Fixes the winner-publish bug (routes to topic_signals_aggregated instead of topic_intelligence), adds intelligence.i7.signals topic for all ranked signals, creates thin WriterAgent that converts payload to LedgerEntry to signal_ledger.

**Verified:** 2026-03-30T14:05:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (stale Wants dependency removed)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | `topic_intelligence_i7_signals()` returns `<env>.intelligence.i7.signals` | VERIFIED | `src/core/stream_keys.py` line 167: `return f"{env_prefix(env_name)}intelligence.i7.signals"` |
| 2 | `_run_i7` publishes ALL ranked signals to `intelligence.i7.signals` | VERIFIED | Lines 1129-1140: `self._enqueue(topic_intelligence_i7_signals(...), ..., {"signals": ranked})` — full ranked list including regime_suppressed |
| 3 | `_run_i7` routes winner to `topic_signals_aggregated` (not `topic_intelligence`) | VERIFIED | Lines 1142-1149: winner enqueued to `topic_signals_aggregated(...)` only |
| 4 | `services/signal_writer_agent.py` exists with `SignalWriterAgent` class | VERIFIED | File exists, class `SignalWriterAgent(BaseAgent)` at line 52 |
| 5 | `SignalWriterAgent` consumes `intelligence.i7.signals`, converts to `LedgerEntry`, batch-inserts | VERIFIED | `_setup()` wires consumer to `topic_intelligence_i7_signals`; `_run()` calls `_payload_to_ledger_entries()` then `_flush()` → `repo.insert_signals()` |
| 6 | All `LedgerEntry` fields populated including attribution fields | VERIFIED | `_payload_to_ledger_entries()` lines 164-196: all fields set including `composite_rank`, `hmm_regime_at_fire`, `regime_type_at_fire`, `is_shadow`, `pre_quality_confidence`, `pre_calibration_confidence` |
| 7 | Regime-suppressed signals written with `status=REGIME_SUPPRESSED` | VERIFIED | Lines 159-163: `sig.get("status") == "regime_suppressed"` maps to `SignalStatus.REGIME_SUPPRESSED` |
| 8 | `indicagent-signal-generator` is stopped and disabled | VERIFIED (partial) | `UnitFileState=disabled`, `ActiveState=failed` — unit disabled and will not restart; functional retirement achieved |
| 9 | `indicagent-signal-tracker.service` has no `Wants` dependency on signal-generator | VERIFIED | `/etc/systemd/system/indicagent-signal-tracker.service` now contains only `After=network-online.target` in `[Unit]` section — stale `Wants=` and `After=...signal-generator` lines removed; service `ActiveState=active SubState=running` |
| 10 | 21 unit tests in `test_signal_writer_agent.py` pass | VERIFIED | `pytest tests/unit/service_tests/test_signal_writer_agent.py` → 21 passed in 0.08s |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/core/stream_keys.py` | `topic_intelligence_i7_signals()` function | VERIFIED | Line 160-167, returns correct topic string |
| `services/intelligence_pipeline_agent.py` | `_run_i7` routing fix + all-signals publish | VERIFIED | Winner → `topic_signals_aggregated`; all ranked → `topic_intelligence_i7_signals` |
| `services/signal_writer_agent.py` | `SignalWriterAgent` WriterAgent | VERIFIED | 213-line substantive implementation, fully wired |
| `services/_archived_signal_generator_agent.py` | Archived copy of retired agent | VERIFIED | File exists at that path |
| `services/signal_generator_agent.py` | Must NOT exist (retired) | VERIFIED | File absent from `services/` |
| `tests/unit/service_tests/test_signal_writer_agent.py` | 21 passing unit tests | VERIFIED | All 21 pass |
| `production/systemd/indicagent-signal-writer.service` | Reference systemd unit | VERIFIED | File exists |
| `/etc/systemd/system/indicagent-signal-writer.service` | Installed service, active+enabled | VERIFIED | `ActiveState=active`, `UnitFileState=enabled` |
| `/etc/systemd/system/indicagent-signal-tracker.service` | No dependency on signal-generator | VERIFIED | Only `After=network-online.target` — stale Wants/After lines removed; service active+running |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `_run_i7` | `intelligence.i7.signals` topic | `self._enqueue(topic_intelligence_i7_signals(...))` | WIRED | Line 1130-1140 |
| `_run_i7` | `signals.aggregated` topic | `self._enqueue(topic_signals_aggregated(...))` | WIRED | Line 1145-1149, winner only |
| `SignalWriterAgent._run` | `SignalLedgerRepository.insert_signals` | `_payload_to_ledger_entries` → `_flush` | WIRED | Lines 109-123, 125-136 |
| `indicagent-signal-writer` systemd unit | `signal_writer_agent.py` | `ExecStart=...python services/signal_writer_agent.py` | WIRED | Confirmed active+enabled |
| `signal-tracker` service | retired `signal-generator` | `Wants=` dependency | REMOVED | Stale link eliminated; `[Unit]` section clean |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `SignalWriterAgent._run` | `payload` from `self._consumer.messages()` | `intelligence.i7.signals` Kafka topic | Yes — populated by `intelligence_pipeline_agent._run_i7` after each bar | FLOWING |
| `_payload_to_ledger_entries` | `LedgerEntry` list | signal dicts in payload | Yes — all attribution fields from pipeline annotations | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 21 unit tests pass | `pytest tests/unit/service_tests/test_signal_writer_agent.py` | 21 passed in 0.08s | PASS |
| SignalWriterAgent service active | `systemctl show indicagent-signal-writer --property=ActiveState` | `ActiveState=active` | PASS |
| signal-generator service disabled | `systemctl show indicagent-signal-generator --property=UnitFileState` | `UnitFileState=disabled` | PASS |
| signal_generator_agent.py absent | `ls services/signal_generator_agent.py` | No such file | PASS |
| signal-tracker no stale dependency | `cat /etc/systemd/system/indicagent-signal-tracker.service` | Only `After=network-online.target` in [Unit] | PASS |
| signal-tracker service running | `systemctl show indicagent-signal-tracker --property=ActiveState,SubState` | `ActiveState=active SubState=running` | PASS |

### Anti-Patterns Found

None — all previously identified anti-patterns resolved.

### Human Verification Required

None — all checks are automatable for this phase.

### Gaps Summary

No gaps remain. The single gap from initial verification — stale `Wants=indicagent-signal-generator.service` and `After=...indicagent-signal-generator.service` dependency in `/etc/systemd/system/indicagent-signal-tracker.service` — has been corrected. The installed unit now contains only `After=network-online.target`, the service daemon-reloaded, and `indicagent-signal-tracker` is confirmed active and running.

All 10 must-haves are fully verified: the new Kafka topic is wired, winner routing is corrected, SignalWriterAgent is substantive and active, the retired service is disabled and absent from `services/`, the signal-tracker service has no stale dependency, and all 21 unit tests pass.

---

_Verified: 2026-03-30T14:05:00Z_
_Verifier: Claude (gsd-verifier)_
