---
phase: 49.2-hmm-operational-fixes-observability-fallback-logging-warm-up-noise
verified: 2026-03-23T20:00:00Z
status: passed
score: 5/5 must-haves verified
re_verification: false
---

# Phase 49.2: HMM Operational Fixes — Verification Report

**Phase Goal:** Make HMM regime gating observable and self-correcting — structlog warnings on 2D fallback, hmm_n_dims + hmm_warmed_up in intelligence_features JSONB, warm-up noise suppressed through existing regime_gate prob_min floor.
**Verified:** 2026-03-23T20:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                              | Status     | Evidence                                                                                                                                 |
|----|----------------------------------------------------------------------------------------------------|------------|------------------------------------------------------------------------------------------------------------------------------------------|
| 1  | structlog warning fires when HMM falls back to 2D mode, naming the missing field(s)               | VERIFIED   | `_resolve_dims()` calls `logger.warning("hmm_fallback_2d", missing_fields=missing)` (line 202) and `logger.warning("hmm_fallback_2d_no_features")` (line 184). Tests `test_fallback_2d_emits_warning` and `test_fallback_2d_warning_no_features_dict` pass. |
| 2  | hmm_n_dims (2 or 5) appears in every HMM output dict and persists to intelligence_features.smc JSONB | VERIFIED   | `hmm_n_dims` in `outputs` frozenset (lines 98-99), in `_state` (line 135, 171, 275), and returned from `_build_output()` (line 292). Tests `test_n_dims_5d_output`, `test_n_dims_2d_output`, `test_n_dims_in_compute_next` all pass. |
| 3  | hmm_warmed_up (bool) appears in every HMM output dict and persists to intelligence_features.smc JSONB | VERIFIED   | `hmm_warmed_up` in `outputs` frozenset (line 99), derived as `bars_processed >= min_lookback` in `_build_output()` (line 283), returned at line 293. Tests `test_warmed_up_true_full_history` and `test_warmed_up_in_output_keys` pass. |
| 4  | hmm_regime_prob is 0.0 during warm-up, triggering existing regime_gate prob_min suppression       | VERIFIED   | `_build_output()` returns `hmm_regime_prob = regime_prob if warmed_up else 0.0` (line 287). Test `test_warmup_false_when_bars_processed_low` confirms `hmm_regime_prob == 0.0` when `bars_processed < min_lookback`. regime_gate.py unchanged (zero diff). |
| 5  | All four hmm_prob_* fields are zeroed during warm-up for audit trail consistency                   | VERIFIED   | Lines 287-290 zero all four prob fields when `warmed_up is False`. Test `test_warmup_false_when_bars_processed_low` asserts all four fields are 0.0. |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact                                              | Expected                                                                              | Status     | Details                                                                                                     |
|-------------------------------------------------------|---------------------------------------------------------------------------------------|------------|-------------------------------------------------------------------------------------------------------------|
| `src/intelligence/smart_money/hmm_regime.py`          | HMM plugin with bars_processed counter, n_dims tracking, warm-up suppression, structlog fallback warning | VERIFIED   | 298 lines. `logger = structlog.get_logger(__name__)` present (line 16). `bars_processed` initialized in `_reset_state` (line 274), incremented in `_forward_step` (line 264), checked in `_build_output` (line 282). All 4 prob fields zeroed on warm-up. |
| `src/intelligence/schemas.py`                         | SMCContext with hmm_n_dims and hmm_warmed_up fields                                   | VERIFIED   | `hmm_n_dims: int \| None = None` at line 624, `hmm_warmed_up: bool \| None = None` at line 625. Both follow `hmm_regime_duration`. |
| `tests/unit/intelligence/test_smart_money_plugins.py` | 8+ new test methods in TestHMMRegime covering all 4 behaviors                         | VERIFIED   | 1033 lines, 59 total test methods. 11 new TestHMMRegime methods + 2 TestSMCContextHMMFields methods = 13 new tests. All 19 HMM/SMC tests pass. |

---

### Key Link Verification

| From                                      | To                              | Via                                                                           | Status  | Details                                                                                                                                   |
|-------------------------------------------|---------------------------------|-------------------------------------------------------------------------------|---------|-------------------------------------------------------------------------------------------------------------------------------------------|
| `src/intelligence/smart_money/hmm_regime.py` | `src/intelligence/schemas.py` | `hmm_n_dims` and `hmm_warmed_up` output keys match SMCContext field names    | WIRED   | Output keys at `_build_output()` lines 292-293 exactly match SMCContext field names `hmm_n_dims` and `hmm_warmed_up` at schema lines 624-625. |
| `src/intelligence/smart_money/hmm_regime.py` | `src/intelligence/pipeline/regime_gate.py` | `hmm_regime_prob=0.0` during warm-up triggers `prob_min=0.30` suppression (zero changes to regime_gate) | WIRED   | `hmm_regime_prob` zeroed at line 287 when `warmed_up is False`. `git diff src/intelligence/pipeline/regime_gate.py` produces no output — file unchanged. |

---

### Data-Flow Trace (Level 4)

Not applicable for this phase. The artifacts are plugin logic and schema definitions, not components that render dynamic data. The persistence path (plugin output dict → SMCContext JSONB → `intelligence_features.smc`) is governed by the existing feature_writer_service contract; `hmm_n_dims` and `hmm_warmed_up` are new keys in the output dict that flow into the JSONB automatically once the dict is populated — no new wiring required.

---

### Behavioral Spot-Checks

| Behavior                                      | Command                                                                                                                                                    | Result    | Status  |
|-----------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------|---------|
| Schema contract: SMCContext accepts new fields | `.venv/bin/python -c "from src.intelligence.schemas import SMCContext; c = SMCContext(hmm_n_dims=5, hmm_warmed_up=True); print(c.hmm_n_dims, c.hmm_warmed_up)"` | `5 True`  | PASS    |
| All 19 HMM/SMC tests pass                     | `.venv/bin/pytest tests/unit/intelligence/test_smart_money_plugins.py -v -k "HMM or SMC" --tb=short`                                                       | 19 passed | PASS    |
| regime_gate.py unchanged                      | `git diff src/intelligence/pipeline/regime_gate.py`                                                                                                         | (empty)   | PASS    |
| signal_generator_service.py unchanged         | `git diff services/signal_generator_service.py`                                                                                                             | (empty)   | PASS    |

---

### Requirements Coverage

| Requirement | Source Plan | Description                                                        | Status    | Evidence                                                                                                  |
|-------------|-------------|--------------------------------------------------------------------|-----------|-----------------------------------------------------------------------------------------------------------|
| HMM-01      | 49.2-01     | structlog warning fires on 2D fallback, naming missing fields      | SATISFIED | `_resolve_dims()` emits `hmm_fallback_2d` with `missing_fields` kwarg and `hmm_fallback_2d_no_features`. Tests pass. |
| HMM-02      | 49.2-01     | `hmm_n_dims` (2 or 5) in every HMM output dict                    | SATISFIED | In `outputs` frozenset, `_state`, and `_build_output()` return dict. Tests pass.                          |
| HMM-03      | 49.2-01     | `hmm_warmed_up` (bool) in every HMM output dict                   | SATISFIED | In `outputs` frozenset and `_build_output()`. Derived from `bars_processed >= min_lookback`. Tests pass.  |
| HMM-04      | 49.2-01     | All 4 `hmm_prob_*` fields zeroed during warm-up                   | SATISFIED | `_build_output()` zeroes all four fields when `warmed_up is False` (lines 287-290). Tests pass.           |

Note: HMM-01 through HMM-04 are phase-local requirements. They do not appear in `.planning/REQUIREMENTS.md` (which covers v2.0 milestone requirements only). No orphaned requirements found.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/intelligence/schemas.py` | 259 | `E501` line too long (101 chars, limit 100) — comment in docstring | Info | Pre-existing ruff violation confirmed in SUMMARY.md ("Pre-existing ruff E501 error in schemas.py line 259"). No impact on functionality. Not introduced by this phase. |

No stubs, empty implementations, or placeholder patterns were introduced by this phase.

---

### Human Verification Required

#### 1. Warm-up suppression in live pipeline

**Test:** Restart `indicagent-feature-pipeline` service, watch `logs/feature_pipeline_service.log` for `hmm_warmed_up=False` entries in the first 20 bars per symbol/timeframe.
**Expected:** During the first `min_lookback=20` bars after restart, all `hmm_prob_*` fields are 0.0 in the intelligence event JSONB and no signals fire through the regime gate on those bars.
**Why human:** Requires a live service restart and real-time bar injection through the full pipeline. Cannot be verified from static code analysis or unit tests alone.

#### 2. 2D fallback warning in service logs

**Test:** If any instrument is missing RSI/ADX/ATR/MACD in the feature pipeline (e.g., insufficient history), check `logs/feature_pipeline_service.log` for structured `hmm_fallback_2d` events.
**Expected:** Structured log lines with `event=hmm_fallback_2d missing_fields=[...]` appear, giving operators visibility into degraded HMM emission mode.
**Why human:** Requires live conditions where I1 features are not yet populated (e.g., immediately after adding a new instrument). Not reproducible in unit tests against existing fixture data.

---

### Gaps Summary

No gaps. All 5 must-have truths are verified against the actual codebase. All 4 requirements satisfied. The one ruff E501 warning in `schemas.py` is pre-existing and was present before this phase began.

---

_Verified: 2026-03-23T20:00:00Z_
_Verifier: Claude (gsd-verifier)_
