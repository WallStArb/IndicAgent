---
phase: 18-financial-math-safety
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/llm_providers.py
autonomous: true
gap_closure: true
requirements:
  - API-04
must_haves:
  truths:
    - "All LLM providers use configurable timeout from Settings"
  artifacts:
    - path: "src/intelligence/llm_providers.py"
      provides: "LLM providers with configurable timeout"
      exports: ["OpenRouterProvider", "AnthropicProvider", "OllamaProvider"]
  key_links:
    - from: "src/intelligence/llm_providers.py"
      to: "src/config/settings.py"
      via: "Settings.llm_timeout_sec"
      pattern: "Settings\(\)\.llm_timeout_sec"
---

<objective>
Update all LLM providers to use configurable timeout from Settings

Purpose: Currently only ZAIProvider uses the configurable timeout from Settings (line 178). OpenRouterProvider, AnthropicProvider, and OllamaProvider either hardcode timeout behavior or don't expose it in __init__. This breaks the requirement that all LLM providers use configurable timeouts.

Output: All LLM providers accept optional timeout parameter in __init__ with default from Settings.llm_timeout_sec
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-financial-math-safety/18-VERIFICATION.md
@src/intelligence/llm_providers.py

# Current State
- Line 24-27: _default_llm_timeout() function exists and calls Settings().llm_timeout_sec
- ZAIProvider (line 168-179): Uses timeout parameter in __init__, defaults to _default_llm_timeout()
- OpenRouterProvider (line 47-59): No timeout parameter in __init__
- AnthropicProvider (line 101-115): No timeout parameter in __init__
- OllamaProvider (line 223-233): No timeout parameter in __init__
- All providers have generate() method that accepts timeout parameter, but the instance-level timeout is not stored

# Gap
Only ZAIProvider has instance-level timeout storage. Other providers don't expose timeout in __init__, so they can't be configured with a default timeout from Settings.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add timeout parameter to OpenRouterProvider, AnthropicProvider, and OllamaProvider</name>
  <files>src/intelligence/llm_providers.py</files>
  <action>
  Update __init__ signatures for OpenRouterProvider, AnthropicProvider, and OllamaProvider to include optional timeout parameter with default from Settings:

  1. OpenRouterProvider (line 50-59):
     - Add `timeout: float | None = None` parameter to __init__
     - Add `self.timeout = timeout or _default_llm_timeout()` in __init__ body

  2. AnthropicProvider (line 106-115):
     - Add `timeout: float | None = None` parameter to __init__
     - Add `self.timeout = timeout or _default_llm_timeout()` in __init__ body

  3. OllamaProvider (line 226-233):
     - Add `timeout: float | None = None` parameter to __init__
     - Add `self.timeout = timeout or _default_llm_timeout()` in __init__ body

  Pattern to follow (from ZAIProvider):
  ```python
  def __init__(
      self,
      model: str = "glm-5",
      api_key: str = "",
      base_url: str = "https://api.z.ai/api/paas/v4",
      timeout: float | None = None,
  ) -> None:
      self.model = model
      self.api_key = api_key
      self.base_url = base_url.rstrip("/")
      self.timeout = timeout or _default_llm_timeout()
      self.provider_id = f"zai:{model}"
  ```

  Note: Do NOT modify the generate() method signatures - they already accept timeout as a parameter.
  </action>
  <verify>
    <automated>grep -A 3 "def __init__" /home/bg/dev/indicagent/src/intelligence/llm_providers.py | grep -E "(timeout|_default_llm_timeout)"</automated>
  </verify>
  <done>All three providers (OpenRouterProvider, AnthropicProvider, OllamaProvider) have timeout parameter in __init__ with default from Settings</done>
</task>

</tasks>

<verification>
After completion, verify:
1. grep -A 3 "class OpenRouterProvider" shows timeout parameter in __init__
2. grep -A 3 "class AnthropicProvider" shows timeout parameter in __init__
3. grep -A 3 "class OllamaProvider" shows timeout parameter in __init__
4. All four providers now use _default_llm_timeout() as default
</verification>

<success_criteria>
- All LLM providers (ZAIProvider, OpenRouterProvider, AnthropicProvider, OllamaProvider) have timeout in __init__
- All defaults to Settings.llm_timeout_sec via _default_llm_timeout()
- grep shows all four classes use timeout parameter
</success_criteria>

<output>
After completion, create `.planning/phases/18-financial-math-safety/18-05-SUMMARY.md`
</output>
