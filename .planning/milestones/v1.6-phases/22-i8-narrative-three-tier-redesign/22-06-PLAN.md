---
phase: 22-i8-narrative-three-tier-redesign
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - services/ai_narrative_service.py
autonomous: true
requirements:
  - I8-10

must_haves:
  truths:
    - "narrative_short provider list exists in config providers object"
    - "narrative_deep provider list exists in config providers object"
    - "Both entries start with z-ai/glm-4.7-flash via openrouter (same as per_signal initially)"
    - "_build_chains() reads pcfg['narrative_short'] and pcfg['narrative_deep'] without KeyError"
    - "Service _build_chains() succeeds: prints short_chain and deep_chain types"
  artifacts:
    - path: "services/ai_narrative_service.py"
      provides: "narrative_short and narrative_deep provider entries in default_config"
      contains: "narrative_short"
  key_links:
    - from: "services/ai_narrative_service.py _build_chains()"
      to: "default_config['providers']['narrative_short']"
      via: "_load_config() default_config dict"
      pattern: "narrative_short.*openrouter|narrative_deep.*openrouter"
---

<objective>
Add narrative_short and narrative_deep provider entries to the AI narrative service config so that _build_chains() reads them directly without fallback. This completes the chain infrastructure for the three-tier system.

Purpose: Explicit config entries per call type make routing auditable and allow independent model selection per tier as performance data accumulates over time.
Output: narrative_short and narrative_deep entries in default_config providers, _build_chains() uses them directly.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-03-09-i8-narrative-implementation.md

<interfaces>
<!-- _load_config() in services/ai_narrative_service.py (line ~371): -->
Contains default_config dict with providers["per_signal"] list at line ~397.
This is where narrative_short and narrative_deep entries must be added.

<!-- providers["per_signal"] provider list (copy pattern): -->
  {"type": "openrouter", "model": "z-ai/glm-4.7-flash"},
  {"type": "openrouter", "model": "qwen/qwen3.5-flash-02-23"},
  {"type": "openrouter", "model": "stepfun/step-3.5-flash:free"},
  {"type": "openrouter", "model": "meta-llama/llama-3.3-70b-instruct:free"},
  {"type": "openrouter", "model": "arcee-ai/trinity-large-preview:free"},
  {"type": "ollama",     "model": "qwen3.5:9b"},

<!-- _build_chains() at line 330 (from 22-02): -->
self.short_chain = LLMChain([_make_provider(s) for s in pcfg.get("narrative_short", pcfg["per_signal"])])
self.deep_chain  = LLMChain([_make_provider(s) for s in pcfg.get("narrative_deep",  pcfg["per_signal"])])
Once default_config has the keys, change .get() to direct access: pcfg["narrative_short"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add narrative_short and narrative_deep to default_config providers</name>
  <files>services/ai_narrative_service.py</files>
  <action>
    Step 1: READ services/ai_narrative_service.py lines 371-430 (_load_config) to see the default_config providers dict.

    Step 2: Add narrative_short and narrative_deep entries to default_config["providers"] after the existing "group" list. Use the same provider list as "per_signal" (identical providers initially — model routing will diverge as performance data accumulates).

    Add these two keys to the providers dict:
      "narrative_short": [
          {"type": "openrouter", "model": "z-ai/glm-4.7-flash"},
          {"type": "openrouter", "model": "qwen/qwen3.5-flash-02-23"},
          {"type": "openrouter", "model": "stepfun/step-3.5-flash:free"},
          {"type": "openrouter", "model": "meta-llama/llama-3.3-70b-instruct:free"},
          {"type": "openrouter", "model": "arcee-ai/trinity-large-preview:free"},
          {"type": "ollama",     "model": "qwen3.5:9b"},
      ],
      "narrative_deep": [
          {"type": "openrouter", "model": "z-ai/glm-4.7-flash"},
          {"type": "openrouter", "model": "qwen/qwen3.5-flash-02-23"},
          {"type": "openrouter", "model": "stepfun/step-3.5-flash:free"},
          {"type": "openrouter", "model": "meta-llama/llama-3.3-70b-instruct:free"},
          {"type": "openrouter", "model": "arcee-ai/trinity-large-preview:free"},
          {"type": "ollama",     "model": "qwen3.5:9b"},
      ],

    Step 3: In _build_chains(), update the .get() fallback to direct access:
      Change: pcfg.get("narrative_short", pcfg["per_signal"])
      To:     pcfg["narrative_short"]
      Change: pcfg.get("narrative_deep",  pcfg["per_signal"])
      To:     pcfg["narrative_deep"]

    Step 4: Run the instantiation check to confirm no KeyError.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from services.ai_narrative_service import AIMarketNarrativeService; svc = AIMarketNarrativeService.__new__(AIMarketNarrativeService); svc._build_chains(); print('short:', type(svc.short_chain).__name__); print('deep:', type(svc.deep_chain).__name__); print('OK')" 2>&1</automated>
  </verify>
  <done>Prints "short: LLMChain", "deep: LLMChain", "OK" — no KeyError or ImportError. Full unit suite passes.</done>
</task>

</tasks>

<verification>
`.venv/bin/python -c "from services.ai_narrative_service import AIMarketNarrativeService; svc = AIMarketNarrativeService.__new__(AIMarketNarrativeService); svc._build_chains(); print('OK')" 2>&1`
Prints OK with no error.

`.venv/bin/pytest tests/unit/ -q 2>&1 | tail -5`
Full unit suite passes.
</verification>

<success_criteria>
- default_config["providers"] contains "narrative_short" and "narrative_deep" keys
- Each has same provider list as "per_signal" (6 providers)
- _build_chains() uses pcfg["narrative_short"] and pcfg["narrative_deep"] directly (no fallback)
- Service _build_chains() instantiates without error
- Full unit suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/22-i8-narrative-three-tier-redesign/22-06-SUMMARY.md`
</output>
