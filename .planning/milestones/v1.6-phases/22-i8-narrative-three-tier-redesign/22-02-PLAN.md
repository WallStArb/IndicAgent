---
phase: 22-i8-narrative-three-tier-redesign
plan: "02"
type: tdd
wave: 1
depends_on: []
files_modified:
  - services/ai_narrative_service.py
  - tests/unit/service_tests/test_ai_narrative_service.py
autonomous: true
requirements:
  - I8-07

must_haves:
  truths:
    - "SYSTEM_PROMPT contains no banned phrases: capitalize, execute long, protect the position, suggests, price momentum"
    - "SYSTEM_PROMPT establishes senior trading desk analyst voice"
    - "AIMarketNarrativeService has short_chain and deep_chain attributes after _build_chains()"
    - "short_chain and deep_chain are separate LLMChain instances (identity check: short_chain is not deep_chain)"
    - "_apply_score_routing() loop covers narrative_short and narrative_deep call types"
    - "per_signal_chain alias points to short_chain (backward compat)"
  artifacts:
    - path: "services/ai_narrative_service.py"
      provides: "Updated SYSTEM_PROMPT, short_chain, deep_chain in _build_chains, updated _apply_score_routing"
      contains: "short_chain"
    - path: "tests/unit/service_tests/test_ai_narrative_service.py"
      provides: "4 tests for SYSTEM_PROMPT voice and chain separation"
      contains: "test_system_prompt_prohibits_passive_voice_phrases"
  key_links:
    - from: "services/ai_narrative_service.py _apply_score_routing()"
      to: "short_chain and deep_chain"
      via: "loop: ('narrative_short', self.short_chain), ('narrative_deep', self.deep_chain)"
      pattern: "narrative_short.*short_chain|narrative_deep.*deep_chain"
---

<objective>
Replace SYSTEM_PROMPT with senior trading desk analyst voice, add short_chain and deep_chain to _build_chains(), and update _apply_score_routing() to cover the two new call types. These are the chain infrastructure changes that Task 3 (concurrent calls) depends on.

Purpose: Separating short_chain and deep_chain allows independent model routing per call type. The system prompt voice change eliminates passive hedging language from all future narrative output.
Output: Updated SYSTEM_PROMPT, two new chain attributes, updated routing loop, 4 passing tests.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-03-09-i8-narrative-implementation.md

<interfaces>
<!-- Existing in services/ai_narrative_service.py -->

SYSTEM_PROMPT at line ~49 (currently):
```python
SYSTEM_PROMPT = (
    "You are a market intelligence AI..."  # old voice — replace this entirely
)
```

_build_chains() at line 330:
```python
def _build_chains(self) -> None:
    ...
    self.per_signal_chain = LLMChain([_make_provider(s) for s in pcfg["per_signal"]])
    self.group_chain = LLMChain([_make_provider(s) for s in pcfg["group"]])
    self._per_signal_timeout = float(pcfg["openrouter_timeout_sec"])
    self._group_timeout = float(pcfg["openrouter_timeout_sec"])
```

_apply_score_routing() at line 726 — currently:
```python
for call_type, _chain in [
    ("per_signal", self.per_signal_chain),
    ("group_synthesis", self.group_chain),
]:
```

The config has providers["per_signal"] — Task 6 adds providers["narrative_short"] and providers["narrative_deep"].
For this task, _build_chains() must read from pcfg["narrative_short"] and pcfg["narrative_deep"].
Use a safe default: if those keys are missing from pcfg, fall back to pcfg["per_signal"].
Pattern: `pcfg.get("narrative_short", pcfg["per_signal"])`
</interfaces>
</context>

<feature>
  <name>SYSTEM_PROMPT voice update and short/deep chain separation</name>
  <files>services/ai_narrative_service.py, tests/unit/service_tests/test_ai_narrative_service.py</files>
  <behavior>
    4 tests (add to tests/unit/service_tests/test_ai_narrative_service.py):

    1. test_system_prompt_prohibits_passive_voice_phrases
       banned = ["capitalize", "execute long", "protect the position", "suggests", "price momentum"]
       for phrase in banned: assert phrase not in SYSTEM_PROMPT.lower()

    2. test_system_prompt_establishes_analyst_voice
       assert "trading desk" in SYSTEM_PROMPT.lower() or "analyst" in SYSTEM_PROMPT.lower()
       assert "passive voice" in SYSTEM_PROMPT.lower() or "precise" in SYSTEM_PROMPT.lower()

    3. test_service_has_short_chain
       svc = AIMarketNarrativeService.__new__(AIMarketNarrativeService)
       svc._build_chains()
       assert hasattr(svc, "short_chain")
       assert hasattr(svc, "deep_chain")

    4. test_service_short_chain_is_separate_from_deep_chain
       svc = AIMarketNarrativeService.__new__(AIMarketNarrativeService)
       svc._build_chains()
       assert svc.short_chain is not svc.deep_chain
  </behavior>
  <implementation>
    RED: Add 4 tests. Run — expect failures (SYSTEM_PROMPT has banned phrases, no short_chain/deep_chain).
    Confirm: `.venv/bin/pytest tests/unit/service_tests/test_ai_narrative_service.py::test_system_prompt_prohibits_passive_voice_phrases tests/unit/service_tests/test_ai_narrative_service.py::test_service_has_short_chain -v`

    GREEN:

    1. Replace SYSTEM_PROMPT (lines ~49-57) with the exact text from Task 2 of the implementation plan:
       "You are a senior trading desk analyst briefing a portfolio manager. Write with precision and economy — every word must earn its place. Never use passive voice. Never hedge with 'suggests' or 'indicates' — the system computed these signals with statistical confidence; state conclusions directly. Never restate the setup name or direction label — the PM already sees those. Never use these phrases: 'capitalize on', 'execute long orders', 'protect the position', 'price momentum suggests', 'within the established regime', 'deliver a risk-to-reward'. Your job: explain WHY this structure matters right now and WHAT to do about it."

    2. In _build_chains(), add after existing chain assignments:
       self.short_chain = LLMChain([_make_provider(s) for s in pcfg.get("narrative_short", pcfg["per_signal"])])
       self.deep_chain  = LLMChain([_make_provider(s) for s in pcfg.get("narrative_deep",  pcfg["per_signal"])])
       self.per_signal_chain = self.short_chain  # alias for backward compat, removed in Task 9 cleanup

    3. In _apply_score_routing(), replace the loop to include new call types:
       for call_type, _chain in [
           ("narrative_short", self.short_chain),
           ("narrative_deep",  self.deep_chain),
           ("group_synthesis", self.group_chain),
       ]:

    Run `.venv/bin/pytest tests/unit/service_tests/test_ai_narrative_service.py -v` — all 4 new tests plus all prior tests must pass.

    NOTE: per_signal_chain = self.short_chain means test_service_has_short_chain passes via identity.
    _build_chains() reads pcfg — if config file doesn't have narrative_short/narrative_deep keys yet,
    the .get() fallback to pcfg["per_signal"] ensures no KeyError. Task 6 adds them properly.
  </implementation>
</feature>

<verification>
`.venv/bin/pytest tests/unit/service_tests/test_ai_narrative_service.py -v 2>&1 | tail -20`
All 4 new tests pass. All prior test_ai_narrative_service.py tests still pass.
`.venv/bin/pytest tests/unit/ -q 2>&1 | tail -5`
Full unit suite passes.
</verification>

<success_criteria>
- SYSTEM_PROMPT contains none of the 5 banned phrases (case-insensitive)
- SYSTEM_PROMPT contains "trading desk" or "analyst"
- AIMarketNarrativeService has both short_chain and deep_chain after _build_chains()
- short_chain is not deep_chain (separate instances)
- per_signal_chain is short_chain (alias)
- _apply_score_routing loop contains narrative_short and narrative_deep entries
- Full unit suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/22-i8-narrative-three-tier-redesign/22-02-SUMMARY.md`
</output>
