---
phase: 22-i8-narrative-three-tier-redesign
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - services/ai_narrative_service.py
  - tests/unit/service_tests/test_ai_narrative_helpers.py
autonomous: true
requirements:
  - I8-01
  - I8-02

must_haves:
  truths:
    - "extract_short_context() returns conclusion-level fields: regime label/prob, killzone, entry/stop/target, confidence, structural_label"
    - "extract_deep_context() returns a superset of extract_short_context() — every key present in short is present in deep"
    - "extract_short_context() and extract_deep_context() handle empty intel dict gracefully (None for missing intel fields)"
    - "build_short_prompt() emits confidence-gated execution instruction: DIRECT (>=75%), CONDITIONAL (50-74%), MONITOR (<50%)"
    - "build_deep_prompt() includes FVG/OB/S-D level data from deep context"
    - "build_action_tag() returns [MONITOR] for <50%, [WAIT — BULLISH/BEARISH] for 50-74%, [DIRECTION STRUCTURAL_LABEL] for >=75%"
    - "get_structural_label() maps all 17 known plugin names correctly; unknown plugins use plugin.upper()[:16]"
    - "All 16 unit tests pass"
  artifacts:
    - path: "services/ai_narrative_service.py"
      provides: "extract_short_context, extract_deep_context, build_short_prompt, build_deep_prompt, build_action_tag, get_structural_label"
      exports:
        - extract_short_context
        - extract_deep_context
        - build_short_prompt
        - build_deep_prompt
        - build_action_tag
        - get_structural_label
    - path: "tests/unit/service_tests/test_ai_narrative_helpers.py"
      provides: "16 unit tests for all pure functions"
      contains: "test_extract_short_context_includes_regime"
  key_links:
    - from: "tests/unit/service_tests/test_ai_narrative_helpers.py"
      to: "services/ai_narrative_service.py"
      via: "from services.ai_narrative_service import extract_short_context, ..."
      pattern: "extract_short_context|extract_deep_context|build_short_prompt|build_deep_prompt|build_action_tag|get_structural_label"
---

<objective>
Add six pure helper functions to ai_narrative_service.py: intelligence context extractors, tier-specific prompt builders, action tag builder, and structural label mapper. These are the foundation of the three-tier system — no I/O, fully testable in isolation.

Purpose: Pure functions with no external dependencies allow thorough TDD before any async or service changes. They define the data contracts for the entire three-tier redesign.
Output: Six exported functions in ai_narrative_service.py + 16 passing unit tests in test_ai_narrative_helpers.py.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-03-09-i8-narrative-implementation.md

<interfaces>
<!-- Existing functions in services/ai_narrative_service.py (add new functions after these) -->

Existing at line 173:
```python
def build_narrative_prompt(signal: dict[str, Any]) -> str:
    """Single-call prompt builder — WILL BE RETIRED in phase 22 Task 9 cleanup."""
```

Existing at line 193:
```python
def _build_llm_call_payload(...) -> dict[str, Any]:
```

Existing at line 237:
```python
def _promote_model_in_chain(chain: Any, model_provider_id: str | None) -> None:
```

Existing test file location: tests/unit/service_tests/test_ai_narrative_helpers.py
(file already exists but may be empty or have prior stubs — READ it before modifying)
</interfaces>
</context>

<feature>
  <name>Intelligence context extraction and tier prompt builders</name>
  <files>services/ai_narrative_service.py, tests/unit/service_tests/test_ai_narrative_helpers.py</files>
  <behavior>
    Test fixtures (copy exactly from docs/plans/2026-03-09-i8-narrative-implementation.md Task 1):

    _SIGNAL = {
      "symbol": "GCJ6", "timeframe": "5m", "direction": 1,
      "direction_label": "Bullish", "confidence": 0.78,
      "setup_plugin": "LiquiditySweepReclaim", "signal_type": "sweep_long",
      "entry_price": "5108.7", "stop_loss": "5100.47",
      "profit_target": "5143.84", "risk_reward_ratio": "4.27",
      "regime_context": "bullish", "supporting_factors": "ma_alignment_bullish,fvg_fill",
      "signal_id": "sig-abc123", "timestamp": "2026-03-09T14:20:00Z",
    }

    _INTEL = {
      "hmm_regime": "2", "hmm_regime_prob": "0.87",
      "fvg_bottom": "5095.0", "fvg_top": "5108.0",
      "ob_bottom": "5099.0", "ob_top": "5110.0",
      "confluence_score": "0.82", "trend_confluence_score": "0.75",
      "killzone_name": "london", "in_london_killzone": "1",
      "nearest_demand_low": "5090.0", "nearest_demand_high": "5098.0",
    }

    16 tests — all specifications in Task 1 of the implementation plan:
    1. test_extract_short_context_includes_regime — ctx["hmm_regime"] == "2", ctx["hmm_regime_prob"] == "0.87", ctx["killzone"] == "london"
    2. test_extract_short_context_includes_signal_fields — ctx["confidence"] == 0.78, ctx["entry"] == "5108.7", etc.
    3. test_extract_short_context_empty_intel — ctx["entry"] == "5108.7", ctx["hmm_regime"] is None
    4. test_extract_deep_context_includes_fvg_bounds — fvg_bottom/fvg_top/ob_bottom present
    5. test_extract_deep_context_is_superset_of_short — all short keys present in deep
    6. test_build_short_prompt_contains_key_fields — entry/stop/confidence in prompt
    7. test_build_short_prompt_includes_confidence_instruction — high conf (78%) → "direct"/"entry"/"act now"
    8. test_build_short_prompt_low_confidence_instructs_wait — conf 0.52 → "wait"/"conditional"
    9. test_build_deep_prompt_contains_fvg_bounds — "5095" or "fvg" in prompt (case-insensitive)
    10. test_build_action_tag_high_confidence_bullish — "BULLISH" in tag, "WAIT" not in tag
    11. test_build_action_tag_mid_confidence_shows_wait — conf 0.60 → "WAIT" and "BULLISH" in tag
    12. test_build_action_tag_low_confidence_shows_monitor — conf 0.40 → "MONITOR" in tag
    13. test_build_action_tag_bearish — direction=-1 → "BEARISH" in tag
    14. test_get_structural_label_sweep — "LiquiditySweepReclaim" → "SWEEP RECLAIM"
    15. test_get_structural_label_fvg — "FVGFill" → "FVG FILL"
    16. test_get_structural_label_unknown — isinstance(str), len > 0
  </behavior>
  <implementation>
    RED: Write all 16 tests in test_ai_narrative_helpers.py. Run — expect ImportError (functions don't exist yet).
    Confirm: `.venv/bin/pytest tests/unit/service_tests/test_ai_narrative_helpers.py -x -q 2>&1 | head -20`

    GREEN: Add to services/ai_narrative_service.py, below build_narrative_prompt():
    - _STRUCTURAL_LABELS dict (17 entries — copy from Task 1 implementation code verbatim)
    - get_structural_label(setup_plugin: str) -> str
    - build_action_tag(signal: dict[str, Any]) -> str
    - extract_short_context(signal, intel) -> dict[str, Any]
    - extract_deep_context(signal, intel) -> dict[str, Any] — calls extract_short_context then updates with deep fields
    - build_short_prompt(signal, ctx) -> str — starts with "/no_think\n\n", confidence-gated execution instruction
    - build_deep_prompt(signal, ctx) -> str — starts with "/no_think\n\n", 3-sentence structure

    All implementation code is specified verbatim in docs/plans/2026-03-09-i8-narrative-implementation.md Task 1. Copy it exactly.

    Run `.venv/bin/pytest tests/unit/service_tests/test_ai_narrative_helpers.py -v` — all 16 must pass.
  </implementation>
</feature>

<verification>
`.venv/bin/pytest tests/unit/service_tests/test_ai_narrative_helpers.py -v 2>&1 | tail -25`
All 16 tests pass. No regressions in full unit suite.
`.venv/bin/pytest tests/unit/ -q 2>&1 | tail -5`
</verification>

<success_criteria>
- All 16 tests in test_ai_narrative_helpers.py pass
- extract_deep_context is a strict superset of extract_short_context (superset test passes)
- build_short_prompt for confidence 0.78 contains direct entry instruction text
- build_short_prompt for confidence 0.52 contains conditional/wait text
- build_action_tag for confidence 0.40 returns "[MONITOR]"
- get_structural_label("LiquiditySweepReclaim") returns "SWEEP RECLAIM"
- Full unit test suite still passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/22-i8-narrative-three-tier-redesign/22-01-SUMMARY.md`
</output>
