---
phase: 22-i8-narrative-three-tier-redesign
plan: "07"
type: execute
wave: 3
depends_on:
  - "22-03"
  - "22-05"
  - "22-06"
files_modified:
  - services/ai_narrative_service.py
autonomous: false
requirements:
  - I8-07
  - I8-08
  - I8-09
  - I8-10

must_haves:
  truths:
    - "narratives stream shows narrative_type: short and narrative_type: deep entries appearing in pairs"
    - "llm_calls:stream shows narrative_short and narrative_deep call_type entries"
    - "llm_writer_service persists both narrative types to llm_calls hypertable"
    - "Old per_signal call type no longer appears in llm_calls stream"
    - "build_narrative_prompt() retired — function deleted or commented with tombstone"
    - "per_signal_chain alias removed from _build_chains()"
    - "per_signal entry removed from _apply_score_routing() loop"
    - "Full unit suite passes with 1318+ tests"
  artifacts:
    - path: "services/ai_narrative_service.py"
      provides: "Cleaned up service: old per_signal path retired"
      contains: "narrative_short"
  key_links:
    - from: "llm_calls:stream entries"
      to: "llm_calls hypertable"
      via: "llm_writer_service consumes narrative_short and narrative_deep call types"
      pattern: "narrative_short|narrative_deep"
---

<objective>
Verify the live three-tier pipeline end-to-end, then retire the old single-call path: remove build_narrative_prompt(), per_signal_chain alias, and per_signal from the routing loop. Confirm llm_writer_service persists both narrative types.

Purpose: Clean final state — no dead code, no confusion between old and new paths. The llm_calls audit log and llm_writer persistence are the final proof that data flows correctly through the full pipeline.
Output: Retired old path, confirmed live stream output, final test run at 1318+ tests.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-03-09-i8-narrative-implementation.md
@.planning/phases/22-i8-narrative-three-tier-redesign/22-03-SUMMARY.md
@.planning/phases/22-i8-narrative-three-tier-redesign/22-05-SUMMARY.md
@.planning/phases/22-i8-narrative-three-tier-redesign/22-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>All three-tier changes are complete in code. Need service restart to go live.</what-built>
  <how-to-verify>
    Please run in your terminal:
    sudo systemctl restart indicagent-ai-narrative
    journalctl -u indicagent-ai-narrative -f --no-pager

    Watch for startup messages. Service should start without error.
    Press Ctrl+C after confirming it started successfully.
  </how-to-verify>
  <resume-signal>Type "restarted" when the service is running, or paste any error output</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify live stream output shows two-tier pairs</name>
  <files>services/ai_narrative_service.py</files>
  <action>
    Wait ~2 minutes for a signal to arrive (or verify from recent stream history).

    Check llm_calls stream for narrative_short and narrative_deep entries:
    .venv/bin/python -c "
import redis, time
r = redis.Redis()
msgs = r.xrevrange('development:llm_calls:stream', count=30)
found = {'narrative_short': 0, 'narrative_deep': 0, 'per_signal': 0}
for _, fields in msgs:
    ct = fields.get(b'call_type', b'').decode()
    if ct in found:
        found[ct] += 1
        sym = fields.get(b'symbol', b'').decode()
        lat = fields.get(b'latency_ms', b'').decode()
        print(f'{ct}: {sym} ({lat}ms)')
print('Summary:', found)
"

    Expected: narrative_short and narrative_deep counts > 0; per_signal count = 0.

    Check narratives stream for narrative_type field:
    .venv/bin/python -c "
import redis
r = redis.Redis()
# Try a few known symbols
for sym in ['ESH6', 'GCJ6', 'CLJ6']:
    for tf in ['5m', '15m']:
        key = f'development:narratives:{sym}:{tf}'
        msgs = r.xrevrange(key, count=4)
        for _, fields in msgs:
            nt = fields.get(b'narrative_type', b'MISSING').decode()
            text = fields.get(b'narrative', b'').decode()[:60]
            print(f'{key}: type={nt} | {text}')
"

    Expected: see entries with narrative_type=short and narrative_type=deep.

    If no signals have arrived yet: check service is healthy and note this in SUMMARY.md as "pending verification".
    Do NOT block on this — proceed to cleanup.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import redis; r=redis.Redis(); msgs=r.xrevrange('development:llm_calls:stream', count=10); types=[f.get(b'call_type',b'').decode() for _,f in msgs]; print('types seen:', set(types))" 2>&1</automated>
  </verify>
  <done>llm_calls stream shows narrative_short and/or narrative_deep entries (or service is confirmed healthy with no signals yet).</done>
</task>

<task type="auto">
  <name>Task 3: Retire old single-call path</name>
  <files>services/ai_narrative_service.py</files>
  <action>
    READ services/ai_narrative_service.py fully before editing.

    Remove the old single-call path:

    1. Remove build_narrative_prompt() function (line ~173). Add a tombstone comment above where it was:
       # build_narrative_prompt() retired in phase 22 (three-tier redesign) — use build_short_prompt() / build_deep_prompt()

    2. In _build_chains(): remove the per_signal_chain alias line:
       Remove: self.per_signal_chain = self.short_chain

    3. In _apply_score_routing(): the loop should now only have:
       ("narrative_short", self.short_chain),
       ("narrative_deep",  self.deep_chain),
       ("group_synthesis", self.group_chain),
       Remove the ("per_signal", ...) entry if it was not already removed in 22-02.

    4. Scan for any remaining references to per_signal_chain or build_narrative_prompt in the file:
       grep -n "per_signal_chain\|build_narrative_prompt\|per_signal" services/ai_narrative_service.py
       Remove any remaining references that are no longer needed.
       NOTE: "per_signal" may still appear in comments or default_config (keep those — they document history).
       Only remove functional code references.

    Run ruff: .venv/bin/ruff check services/ai_narrative_service.py --fix
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/ -q 2>&1 | tail -10</automated>
  </verify>
  <done>Full unit suite passes with 1318+ tests. No references to per_signal_chain in functional code. build_narrative_prompt() no longer callable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete three-tier narrative pipeline: deterministic action_tag, short narrative ~500ms, deep narrative ~5-8s. Old per_signal path retired.</what-built>
  <how-to-verify>
    1. Open dashboard at http://localhost:3000
    2. Navigate to any symbol with recent signals
    3. Verify narrative panel shows:
       - Amber mono action_tag badge (e.g. "[BULLISH SWEEP RECLAIM]")
       - Short narrative text (2 sentences)
       - "▼ Full analysis" toggle button
       - Clicking toggle shows deep narrative (or skeleton if still loading)
    4. Check browser console — no errors
    5. Run: journalctl -u indicagent-ai-narrative -n 20 --no-pager
       Verify log shows "Narrative published" entries with call_type=narrative_short and call_type=narrative_deep
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Final test run: `.venv/bin/pytest tests/unit/ -q 2>&1 | tail -5`
1318+ tests passing.

Ruff: `.venv/bin/ruff check services/ai_narrative_service.py 2>&1 | tail -5`
No new errors beyond pre-existing E501.
</verification>

<success_criteria>
- narratives stream contains entries with narrative_type field ("short" or "deep")
- llm_calls stream contains narrative_short and narrative_deep entries (not per_signal)
- build_narrative_prompt() removed (tombstone comment left)
- per_signal_chain alias removed from _build_chains()
- per_signal removed from _apply_score_routing() loop
- Full unit suite passes at 1318+ tests
- User approves dashboard visual with action_tag + short narrative + deep expand
</success_criteria>

<output>
After completion, create `.planning/phases/22-i8-narrative-three-tier-redesign/22-07-SUMMARY.md`
</output>
