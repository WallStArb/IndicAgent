---
phase: 22-i8-narrative-three-tier-redesign
plan: "03"
type: tdd
wave: 2
depends_on:
  - "22-01"
  - "22-02"
files_modified:
  - services/ai_narrative_service.py
  - tests/unit/service_tests/test_ai_narrative_service.py
autonomous: true
requirements:
  - I8-03
  - I8-04
  - I8-08

must_haves:
  truths:
    - "_process_single_message() fires two asyncio tasks (narrative_short + narrative_deep) concurrently"
    - "Both tasks publish call_type to llm_calls:stream independently"
    - "narratives stream messages include narrative_type field ('short' or 'deep')"
    - "Intelligence context is fetched via xrevrange on intelligence:SYMBOL:TF before building prompts"
    - "narrative_short call updates narrative:{symbol}:{timeframe}:latest hash for backward compat"
    - "_run_narrative_call() is a new standalone async method handling a single tier LLM call end-to-end"
  artifacts:
    - path: "services/ai_narrative_service.py"
      provides: "_run_narrative_call() method, updated _process_single_message(), sk_intelligence import"
      contains: "_run_narrative_call"
    - path: "tests/unit/service_tests/test_ai_narrative_service.py"
      provides: "Async test verifying two concurrent tasks fire and publish"
      contains: "test_process_message_fires_two_narrative_tasks"
  key_links:
    - from: "services/ai_narrative_service.py _process_single_message()"
      to: "asyncio.create_task(_run_narrative_call(...))"
      via: "two create_task calls: narrative_short and narrative_deep"
      pattern: "create_task.*_run_narrative_call"
    - from: "_run_narrative_call()"
      to: "redis xadd on narratives stream"
      via: "msg dict with narrative_type field"
      pattern: "narrative_type.*call_type.replace"
    - from: "_process_single_message()"
      to: "redis xrevrange on intelligence stream"
      via: "sk_intelligence(self.env_prefix, symbol, timeframe)"
      pattern: "xrevrange.*intel_stream"
---

<objective>
Rewrite _process_single_message() to fetch intelligence context from Redis, build tier-specific prompts, and fire two concurrent background tasks (narrative_short, narrative_deep). Add new _run_narrative_call() method that handles each tier independently — LLM call, llm_calls publish, narratives stream publish.

Purpose: This is the core concurrency change. Two tasks run without blocking each other or the main processing loop. The narratives stream gains a narrative_type discriminator field.
Output: Updated _process_single_message(), new _run_narrative_call(), narrative_type in stream messages, 1 async integration test.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-03-09-i8-narrative-implementation.md
@.planning/phases/22-i8-narrative-three-tier-redesign/22-01-SUMMARY.md
@.planning/phases/22-i8-narrative-three-tier-redesign/22-02-SUMMARY.md

<interfaces>
<!-- From 22-01: pure functions now available in ai_narrative_service.py -->
```python
def extract_short_context(signal: dict[str, Any], intel: dict[str, Any]) -> dict[str, Any]: ...
def extract_deep_context(signal: dict[str, Any], intel: dict[str, Any]) -> dict[str, Any]: ...
def build_short_prompt(signal: dict[str, Any], ctx: dict[str, Any]) -> str: ...
def build_deep_prompt(signal: dict[str, Any], ctx: dict[str, Any]) -> str: ...
def build_action_tag(signal: dict[str, Any]) -> str: ...
```

<!-- From 22-02: chain attributes now on service -->
```python
self.short_chain  # LLMChain for narrative_short
self.deep_chain   # LLMChain for narrative_deep
self._per_signal_timeout  # float timeout
self._preferred_models    # dict[str, dict[str, str]]
```

<!-- Existing imports to add to: -->
from src.core.stream_keys import narratives as sk_narratives  # already imported at line 41
# ADD: from src.core.stream_keys import intelligence as sk_intelligence

<!-- Existing helper already available: -->
def _build_llm_call_payload(call_type, signal_data, group_name, prompt, response, latency_ms, succeeded, model_id) -> dict[str, Any]: ...
def _promote_model_in_chain(chain: Any, model_provider_id: str | None) -> None: ...
from src.core.stream_keys import llm_calls_stream  # already imported (check line ~37)

<!-- _process_single_message() signature (read current impl at line 491): -->
async def _process_single_message(
    self,
    symbol: str,
    timeframe: str,
    fields: dict,
    stream: str,
    msg_id: bytes,
) -> bool: ...
```

<!-- stream_keys.intelligence signature: -->
```python
def intelligence(env_prefix: str, symbol: str, timeframe: str) -> str:
    return f"{env_prefix}intelligence:{symbol}:{timeframe}"
```
</interfaces>
</context>

<feature>
  <name>Concurrent narrative tier calls with intelligence context enrichment</name>
  <files>services/ai_narrative_service.py, tests/unit/service_tests/test_ai_narrative_service.py</files>
  <behavior>
    1 async integration test (add to tests/unit/service_tests/test_ai_narrative_service.py):

    test_process_message_fires_two_narrative_tasks:
    - Build AIMarketNarrativeService.__new__ with all required attrs (see implementation plan Task 3 test fixture exactly)
    - mock_redis.xrevrange returns [] (no intel context — graceful degradation)
    - mock_redis.xadd captures call_type from data dict into published_call_types list
    - short_chain and deep_chain both mocked as AsyncMock with generate returning "Test narrative."
    - After await svc._process_single_message(...) + await asyncio.sleep(0.1):
      assert result is True
      assert "narrative_short" in published_call_types
      assert "narrative_deep" in published_call_types

    Stub test (leave as pass):
    test_narrative_stream_message_has_type_field — passes (stub per CONTEXT.md deferred decisions)
  </behavior>
  <implementation>
    RED: Add test_process_message_fires_two_narrative_tasks. Run — expect failure (current _process_single_message fires one per_signal call, not two tasks).
    Confirm: `.venv/bin/pytest tests/unit/service_tests/test_ai_narrative_service.py::test_process_message_fires_two_narrative_tasks -v`

    GREEN: Make three changes to services/ai_narrative_service.py:

    1. Add import at top (near other stream_keys imports):
       from src.core.stream_keys import intelligence as sk_intelligence  # noqa: E402
       (Only add if not already present — grep first)

    2. Add _run_narrative_call() method to AIMarketNarrativeService (copy verbatim from Task 3 in implementation plan):
       - Reads preferred model from self._preferred_models[call_type]
       - Calls chain.generate(prompt, SYSTEM_PROMPT, max_tokens=500, timeout=self._per_signal_timeout)
       - Publishes to llm_calls:stream via _build_llm_call_payload + redis.xadd
       - Publishes to narratives:SYMBOL:TF stream with narrative_type = call_type.replace("narrative_", "")
       - narrative_type field: "short" for narrative_short, "deep" for narrative_deep
       - If call_type == "narrative_short": also updates narrative:{symbol}:{timeframe}:latest hash + increments metrics

    3. Rewrite _process_single_message() to:
       a. Keep existing counterfactual payload logic (before confidence gate) — READ lines 491-530 first, preserve any existing logic
       b. After confidence gate passes:
          - Fetch intel: xrevrange on sk_intelligence(self.env_prefix, symbol, timeframe), count=1
          - Decode fields to intel_ctx dict (handle bytes keys/values)
          - Build short_ctx = extract_short_context(signal_data, intel_ctx)
          - Build deep_ctx = extract_deep_context(signal_data, intel_ctx)
          - Build short_prompt = build_short_prompt(signal_data, short_ctx)
          - Build deep_prompt = build_deep_prompt(signal_data, deep_ctx)
          - asyncio.create_task(_run_narrative_call(signal_data, symbol, timeframe, "narrative_short", short_prompt, self.short_chain))
          - asyncio.create_task(_run_narrative_call(signal_data, symbol, timeframe, "narrative_deep", deep_prompt, self.deep_chain))
          - return True

    READ _process_single_message() fully (lines 491-630) before rewriting — preserve the confidence check, counterfactual logic, and skipped signal metric increments. Only replace the per_signal LLM call section.

    Run full test file:
    `.venv/bin/pytest tests/unit/service_tests/test_ai_narrative_service.py tests/unit/service_tests/test_ai_narrative_helpers.py -v 2>&1 | tail -30`

    Fix any tests that used "per_signal" call_type — update expected call_type to "narrative_short".
  </implementation>
</feature>

<verification>
`.venv/bin/pytest tests/unit/service_tests/test_ai_narrative_service.py::test_process_message_fires_two_narrative_tasks -v`
Test passes with "narrative_short" and "narrative_deep" both in published_call_types.

`.venv/bin/pytest tests/unit/ -q 2>&1 | tail -5`
Full unit suite passes with no regressions.
</verification>

<success_criteria>
- test_process_message_fires_two_narrative_tasks passes
- Both narrative_short and narrative_deep appear in published_call_types after asyncio.sleep(0.1)
- _run_narrative_call() is a standalone async method on the service class
- narrative_type in stream msg equals call_type.replace("narrative_", "") → "short" or "deep"
- narrative_short path updates latest hash (backward compat)
- sk_intelligence import added (no duplicate import error)
- Full unit suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/22-i8-narrative-three-tier-redesign/22-03-SUMMARY.md`
</output>
