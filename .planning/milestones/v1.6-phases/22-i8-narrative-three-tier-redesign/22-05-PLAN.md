---
phase: 22-i8-narrative-three-tier-redesign
plan: "05"
type: execute
wave: 2
depends_on:
  - "22-04"
files_modified:
  - dashboard/src/components/narrative-panel.tsx
autonomous: false
requirements:
  - I8-05
  - I8-06

must_haves:
  truths:
    - "action_tag badge renders above narrative text in amber mono font"
    - "narrative_short renders as primary text (always visible when present)"
    - "Loading skeleton shown when signal exists but narrative_short not yet arrived"
    - "Expand/collapse toggle shows '▼ Full analysis' / '▲ Hide analysis' below short text"
    - "narrative_deep renders when expanded (skeleton if not yet arrived)"
    - "Fallback to narrative field if narrative_short missing (backward compat)"
  artifacts:
    - path: "dashboard/src/components/narrative-panel.tsx"
      provides: "Three-tier narrative panel: action_tag badge + short + expand/deep"
      contains: "expanded"
  key_links:
    - from: "dashboard/src/components/narrative-panel.tsx NarrativeCard"
      to: "NarrativeData.action_tag"
      via: "const actionTag = narrative?.action_tag"
      pattern: "action_tag"
    - from: "NarrativeCard expand button"
      to: "narrative_deep text"
      via: "expanded state → conditional render"
      pattern: "expanded.*narrative_deep|narrative_deep.*expanded"
---

<objective>
Update narrative-panel.tsx to render the three-tier layout: action_tag badge (always shown), narrative_short as primary text with loading skeleton, and expand/collapse toggle revealing narrative_deep. Requires 22-04 because NarrativeData must have the new fields.

Purpose: The UI is the user-visible output of the entire redesign. Immediate action_tag from signal data (no LLM wait), short narrative on arrival, deep on demand.
Output: Updated NarrativeCard component with expand state, action tag badge, skeleton loading, amber styling.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-03-09-i8-narrative-implementation.md
@.planning/phases/22-i8-narrative-three-tier-redesign/22-04-SUMMARY.md

<interfaces>
<!-- Updated NarrativeData (from 22-04): -->
```typescript
interface NarrativeData {
  symbol: string;
  timeframe: string;
  action_bias: string;
  action_tag: string;           // "[BULLISH SWEEP RECLAIM]" etc.
  narrative_short?: string;
  narrative_deep?: string;
  timestamp: string;
  receivedAt: number;
  signal_id?: string;
  narrative?: string;           // backward compat alias
}
```

<!-- Current NarrativeCard in narrative-panel.tsx (line 99-165): -->
Reads: data.narrative (single text), data.action_bias, data.symbol, data.timeframe, data.timestamp, data.receivedAt
Renders: header row with symbol/TF/bias/timestamp, then <p> with text
No expand state currently.

<!-- Design rules from CONTEXT.md (locked): -->
- action_tag badge: text-xs font-mono text-amber-400
- narrative_short: primary text, always visible
- Expand/collapse: "▼ Full analysis" / "▲ Hide analysis"
- narrative_deep: shown on expand, skeleton if not yet arrived
- Skeleton: "h-8 bg-white/5 rounded animate-pulse" (short), "h-12 bg-white/5 rounded animate-pulse mt-2" (deep)
- Deep text: text-sm leading-relaxed mt-2 text-white/80

<!-- Dashboard styling conventions: -->
- Text sizes: text-[0.6rem] for narrative text (matches existing)
- Amber: #f59e0b inline or text-amber-400 class
- No CSS var(--amber) — use text-amber-400
- 1s re-render tick on signal-card.tsx — derived values must use useMemo to avoid per-second recomputation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update NarrativeCard with three-tier layout</name>
  <files>dashboard/src/components/narrative-panel.tsx</files>
  <action>
    READ the full current file (166 lines) before editing.

    Changes to NarrativeCard function (lines 99-165):

    1. Add useState import (add to existing imports at top of file):
       import { useMemo, useState } from "react";

    2. Inside NarrativeCard, derive tier fields:
       const actionTag = data.action_tag ?? "";
       const shortText = data.narrative_short ?? data.narrative ?? null;
       const deepText  = data.narrative_deep ?? null;

    3. Add expand state:
       const [expanded, setExpanded] = useState(false);

    4. Replace the <p> narrative render block with the three-tier layout from Task 5 of the implementation plan:
       a. If actionTag: render <div className="text-xs font-mono text-amber-400 mb-1">{actionTag}</div>
       b. shortText present: render <p className="text-[0.6rem] text-[var(--text-secondary)] italic leading-relaxed m-0">{shortText}</p>
          shortText absent: render skeleton <div className="h-8 bg-white/5 rounded animate-pulse" />
       c. If shortText present: render expand/collapse button below it
          className: "text-xs text-white/40 hover:text-white/60 mt-2 block"
          label: expanded ? "▲ Hide analysis" : "▼ Full analysis"
          onClick: () => setExpanded(e => !e)
       d. If expanded:
          deepText present: <p className="text-[0.6rem] leading-relaxed mt-2 text-white/80 italic m-0">{deepText}</p>
          deepText absent: <div className="h-12 bg-white/5 rounded animate-pulse mt-2" />

    Preserve all existing header row logic (symbol, TF, action_bias, staleness, timestamps).
    Preserve the borderLeftColor accentColor logic.
    Keep the existing outer container className unchanged.

    Compile check: cd dashboard && npx tsc --noEmit
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent/dashboard && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles clean. NarrativeCard renders action_tag, narrative_short/skeleton, expand button, narrative_deep/skeleton on expand.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Three-tier narrative panel: action_tag badge, short narrative, expand/collapse deep narrative.</what-built>
  <how-to-verify>
    Start dev server if not running:
    cd /home/bg/dev/indicagent/dashboard && npm run dev -- --port 3000 --hostname 0.0.0.0 > /tmp/dash.log 2>&1 &
    Open http://localhost:3000

    Verify:
    1. Narrative panel shows amber mono text above narrative (action_tag badge) — if a narrative exists
    2. Primary narrative text visible (narrative_short or fallback to narrative)
    3. "▼ Full analysis" toggle button appears below narrative text
    4. Clicking toggle reveals deep narrative slot (either deep text or skeleton pulse)
    5. Clicking again hides it with "▲ Hide analysis"
    6. No TypeScript errors in browser console

    If no live narratives: verify the skeleton appears (white/5 bg pulse) instead of text.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any layout issues</resume-signal>
</task>

</tasks>

<verification>
TypeScript: `cd /home/bg/dev/indicagent/dashboard && npx tsc --noEmit 2>&1 | head -20` — zero errors.
Visual: action_tag badge visible in amber, expand/collapse toggle functional.
</verification>

<success_criteria>
- action_tag badge renders with text-xs font-mono text-amber-400 styling
- narrative_short shown as primary text; skeleton shown when missing
- Expand button present when shortText is available
- narrative_deep shown on expand; skeleton shown if deep not yet arrived
- TypeScript compiles with no errors
- Group narrative path unchanged (still shows group narrative as before)
- User approves visual layout
</success_criteria>

<output>
After completion, create `.planning/phases/22-i8-narrative-three-tier-redesign/22-05-SUMMARY.md`
</output>
