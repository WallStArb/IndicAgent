---
phase: 142B-frame-simulation-counterfactual-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/214_alpha_frames_schema.sql
  - services/alpha_frame_writer.py
  - services/service_auditor.py
  - scripts/infrastructure/backfill/infrastructure_truncate_derived_tables.sh
  - docs/plans/SHADOW-REVIEW.md
  - tests/unit/test_alpha_frames_schema.py
  - tests/unit/test_alpha_frame_writer_geometry.py
  - tests/unit/test_alpha_frame_writer.py
autonomous: true
requirements: [FRAME-01, FRAME-03]
must_haves:
  truths:
    - "alpha_frames table exists with lifecycle CHECK constraint: closed_ic_decay present, closed_reversal absent (D-04)"
    - "alpha_frames is a hypertable on bar_ts with a composite PK (frame_id, bar_ts) — NOT frame_id-alone; the partitioning column bar_ts is contained in every unique index (PK and uq_alpha_frames_variant), so create_hypertable applies cleanly (review H1)"
    - "No FK from alpha_frames to alpha_events; frame provenance is carried by corpus_run_id + weight_epoch columns, and a corpus rebuild truncates alpha_frames alongside alpha_events (review M1)"
    - "Every alpha_events row maps to at most one primary alpha_frames row via an idempotent content_key-derived text frame_id; ON CONFLICT target is uq_alpha_frames_variant (event_id, bar_ts, frame_variant)"
    - "AlphaFrameWriter --backfill processes the full alpha_events backlog per (symbol,tf) partition in chunks with no single long-running write transaction (D-05); nightly oneshot cadence takes over incrementally once backfill completes (D-06)"
    - "Geometry columns (entry/stop/target/r_multiple) are written NULL at frame creation and filled by CounterfactualTracker at T+1 (Plan 02); the price-unit ATR they need is computed from market_data_ohlcv, NOT read from feature_vectors (which has no price-unit ATR column — only normalized atr_z/range_vs_atr/true_range_pct) (review H2)"
    - "Target geometry uses the ATR path as the sole path (sr_resist_dist is 100% NULL); stop/target/hold/atr_period all load from APR"
    - "Frames are written for the full alpha_events population, not a filtered subset (D-07)"
    - "gross_expected_r / cost_r / net_expected_r diagnostic snapshots (D-03) are populated non-NULL on every written frame with documented unit interpretation in column comments (review M5): gross_expected_r = abs(alpha_score) * alpha.frame.target_r_multiple, net_expected_r = gross_expected_r - cost_r"
    - "SHADOW-REVIEW.md exists, frozen, with numerically-evaluable gross-pnl gate criteria (D-01, review M6), net_expected_r as a mandatory reporting column (D-02), and the day-clustered block-bootstrap method + its residual-correlation caveat documented (review H4)"
  artifacts:
    - path: "production/migrations/214_alpha_frames_schema.sql"
      provides: "alpha_frames hypertable (composite PK, no FK) + alpha.frame.* / alpha.scoring.* / infra.* APR key seeds"
      contains: "CREATE TABLE IF NOT EXISTS alpha_frames"
    - path: "services/alpha_frame_writer.py"
      provides: "AlphaFrameWriter(BaseBatch) FRAME-01 geometry (pure fn) + chunked write"
      exports: ["AlphaFrameWriter", "compute_frame_geometry", "compute_expected_r_snapshot"]
      min_lines: 120
    - path: "docs/plans/SHADOW-REVIEW.md"
      provides: "Frozen pre-commitment gate criteria for Phase 147 promotion"
      contains: "counterfactual_pnl_r"
  key_links:
    - from: "services/alpha_frame_writer.py"
      to: "alpha_events / alpha_frames"
      via: "anti-join LEFT JOIN alpha_frames ... WHERE af.frame_id IS NULL, per-(symbol,tf) chunked executemany insert"
      pattern: "LEFT JOIN alpha_frames"
    - from: "production/migrations/214_alpha_frames_schema.sql"
      to: "config_schema / config_state"
      via: "APR seed triad for alpha.frame.stop_atr_mult, alpha.frame.target_r_multiple, alpha.frame.atr_period, alpha.scoring.min_strategy_n, alpha.scoring.bootstrap_max_n"
      pattern: "alpha.frame.target_r_multiple"
---

<objective>
Create the `alpha_frames` schema (migration 214) with the D-04-corrected lifecycle state machine,
a TimescaleDB-valid identity strategy (composite PK containing the partition column), no FK to a
truncated hypertable, and every APR key this phase consumes. Build `AlphaFrameWriter` — a `BaseBatch`
oneshot that writes one hypothetical frame per `alpha_events` row (FRAME-01) with geometry columns
left NULL for Plan 02's T+1 fill. Ship the frozen `SHADOW-REVIEW.md` pre-commitment document with
numerically-evaluable criteria before any counterfactual data is collected.

Purpose: `alpha_frames` is the single canonical home for every counterfactual claim in the system
(platform-canonical-simulator binding rule). FRAME-01 turns proven signal IC into hypothetical
positions whose P&L Plan 02 will score. The pre-committed SHADOW-REVIEW.md fixes the Phase 147
promotion criteria before the data can bias them.

Output: migration 214, `services/alpha_frame_writer.py`, `docs/plans/SHADOW-REVIEW.md`, service
registration, truncate-script update, and RED→GREEN unit tests for schema DDL, geometry math, and
idempotent writes.

This plan was revised from a cross-AI review (142B-REVIEWS.md) that verified load-bearing claims
against the live DB. Fixes: H1 (PK vs hypertable), H2 (ATR source), M1 (FK/truncation), M5
(expected-R units), M6 (numeric SHADOW criteria), plus H4's gate-method documentation.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v3.1-phases/142B-frame-simulation-counterfactual-tracking/142B-CONTEXT.md
@.planning/milestones/v3.1-phases/142B-frame-simulation-counterfactual-tracking/142B-RESEARCH.md
@.planning/milestones/v3.1-phases/142B-frame-simulation-counterfactual-tracking/142B-PATTERNS.md
@.planning/milestones/v3.1-phases/142B-frame-simulation-counterfactual-tracking/142B-REVIEWS.md

<interfaces>
<!-- alpha_events (read source) — live schema verified 2026-07-09 -->
alpha_events: PRIMARY KEY (event_id text, bar_ts timestamptz); columns symbol text, tf text,
  regime text (nullable), weight_version text, ensemble_version text, alpha_score double NOT NULL,
  alpha_ci_lower double, alpha_ci_upper double, direction text NOT NULL CHECK IN ('long','short'),
  cost_hurdle double NOT NULL. 12,258,206 rows, 78 (symbol,tf) child tables. NOTE (review M1):
  truncate_derived_tables.sh TRUNCATEs alpha_events on every corpus rebuild — alpha_frames must
  NOT hold an FK to it (would block the TRUNCATE or CASCADE-wipe frames), and must itself be
  truncated on rebuild.

<!-- feature_vectors — NOT the ATR source. Verified 2026-07-09: has NO price-unit ATR column.
     Only normalized derivatives exist (atr_z, range_vs_atr, true_range_pct, poc_dist_atr), all of
     which are unusable as a price-unit stop distance. sr_support_dist/sr_resist_dist are also 100%
     NULL. The price-unit ATR for geometry is computed from market_data_ohlcv in Plan 02 (review H2).
     compute_frame_geometry takes `atr` as a caller-supplied parameter — it never reads a table. -->

<!-- BaseBatch contract (src/core/agent/base_batch.py) -->
class BaseBatch(abc.ABC):
    job_name: str          # kebab-case, must equal systemd unit %n suffix
    compute_version: str
    async def run(self) -> None          # template: _setup_pool → execute(pool) → _emit_completion → teardown
    async def execute(self, pool) -> None  # subclass implements only this
    @staticmethod
    def content_key(*parts: str) -> str    # sha256(join('|'))[:32]; deterministic TEXT id
# _emit_completion() auto-emits JOB_COMPLETED_TOTAL.add(1, {"job": job_name, "status": status}) — D-06, no extra code
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 214 — alpha_frames hypertable + APR key seeds + service/truncate registration</name>
  <files>production/migrations/214_alpha_frames_schema.sql, services/service_auditor.py, scripts/infrastructure/backfill/infrastructure_truncate_derived_tables.sh, tests/unit/test_alpha_frames_schema.py</files>
  <read_first>
    - docs/plans/archive/2026-06-25-v30-alpha-lifecycle-schema.md (lines 122-193: the governing alpha_frames DDL — columns, indexes, uq constraint; every part governs EXCEPT the status CHECK (D-04), the sole-PK identity (review H1), and the FK to alpha_events (review M1))
    - production/migrations/195_alpha_ensemble_ic.sql (analog: CREATE TABLE + create_hypertable with a COMPOSITE PK that contains the partition column scored_at + config_schema/config_state/config_history APR seed triad shape)
    - services/service_auditor.py (_DAG_ORDER dict + _ONESHOT_UNITS frozenset + _AGENT_ID_TO_UNIT — locate the indicagent-ensemble-ic-engine entries to mirror)
    - scripts/infrastructure/backfill/infrastructure_truncate_derived_tables.sh (the TRUNCATE list ~lines 53-61; alpha_events is truncated at line 54)
    - tests/unit/test_ensemble_ic_config.py (schema-assertion test style)
    - .planning/milestones/v3.1-phases/142B-frame-simulation-counterfactual-tracking/142B-CONTEXT.md (D-04 CHECK constraint, D-03 diagnostic snapshot columns)
    - .planning/milestones/v3.1-phases/142B-frame-simulation-counterfactual-tracking/142B-REVIEWS.md (H1 PK strategy, M1 FK position)
  </read_first>
  <action>
    Create `production/migrations/214_alpha_frames_schema.sql`. Build the `CREATE TABLE IF NOT EXISTS
    alpha_frames` from the schema doc's DDL (lines 122-193) with these FOUR deviations:

    (1) The `status` CHECK constraint is EXACTLY `CHECK (status IN ('open', 'closed_stop',
    'closed_target', 'closed_max_hold', 'closed_ic_decay'))` per D-04 — `closed_reversal` is dropped
    entirely, `closed_ic_decay` is added. Do NOT copy the schema doc's literal status block.

    (2) Add two provenance columns `corpus_run_id text` and `weight_epoch text` (Pitfall 6 —
    canonical-simulator provenance clause).

    (3) IDENTITY / PARTITIONING (review H1 — the schema doc's `frame_id uuid PRIMARY KEY` is
    INCOMPATIBLE with a bar_ts hypertable and WILL fail at create_hypertable). Declare `frame_id text
    NOT NULL` (it is the deterministic content_key hex string, not a uuid). Make the primary key the
    COMPOSITE `PRIMARY KEY (frame_id, bar_ts)` — TimescaleDB requires every unique index, including the
    PK, to contain the partitioning column `bar_ts`. Keep `uq_alpha_frames_variant UNIQUE (event_id,
    bar_ts, frame_variant)` as the idempotency / `ON CONFLICT` target (it already contains bar_ts, so it
    is hypertable-valid). Keep the schema doc's non-unique indexes. Then
    `SELECT create_hypertable('alpha_frames', 'bar_ts', if_not_exists => TRUE)`. Plan 02's per-frame
    UPDATE keys on `(frame_id, bar_ts)` and is served by this composite PK — no separate frame_id index
    is needed (co-decided with Plan 02, review M3/M4).

    (4) NO FOREIGN KEY (review M1). Do NOT add the schema doc's FK to `alpha_events(event_id, bar_ts)`.
    Add a SQL comment stating the reasoned position: `alpha_events` is a hypertable truncated on every
    corpus rebuild by `infrastructure_truncate_derived_tables.sh`; an FK would either block that TRUNCATE
    or (with CASCADE) silently wipe all frames, and a new `weight_version` orphans old frames'
    parentage regardless — so referential integrity adds fragility without value here. Provenance is
    instead carried by the `corpus_run_id` and `weight_epoch` columns (this project's data-retention +
    anti-join-checkpoint design does not require an enforced FK). Preserve the D-03 diagnostic snapshot
    columns gross_expected_r / net_expected_r / cost_r as real, populated columns; add a SQL
    `COMMENT ON COLUMN` for each documenting the units (review M5): `gross_expected_r` = the ex-ante
    expected payoff magnitude in R at entry (`abs(alpha_score) × alpha.frame.target_r_multiple`; a
    diagnostic magnitude, NOT a probability and NOT a gate input — D-03); `cost_r` = per-row cost hurdle
    in return space copied from `alpha_events.cost_hurdle`; `net_expected_r` = `gross_expected_r - cost_r`,
    reporting-only.

    In the same migration, seed these NEW APR keys using the config_schema + config_state +
    config_history triad shape from migration 195 (each with `ON CONFLICT (config_key) DO NOTHING`
    on schema/state; changed_by='migration_214'):
      - `alpha.frame.stop_atr_mult` = 1.5, type float, description tagged `[initial_estimate]` — NOT an ML target
      - `alpha.frame.target_r_multiple` = 2.0, type float, tagged `[initial_estimate]` — use THIS exact
        name, NOT `target_r_fallback` (Pitfall 3); seed exactly one of the two names
      - `alpha.frame.atr_period` = 14, type int, tagged `[conventional]` — trailing bar count for the
        price-unit ATR that Plan 02 computes from market_data_ohlcv for geometry (review H2)
      - `alpha.scoring.min_strategy_n` = 30, type int, tagged `[conventional]` — FRAME-04 (Plan 02)
        reads this frame-count sufficiency floor; do NOT defer to Phase 144 (Pitfall 4)
      - `alpha.scoring.bootstrap_max_n` = 5000, type int, tagged `[conventional]` — FRAME-04 gate: above
        this day-cluster count, use the analytic one-sided CLT lower bound instead of BCa (review H4)
      - `alpha.scoring.bootstrap_batch` = 1000, type int, tagged `[conventional]` — scipy.stats.bootstrap
        `batch=` parameter to cap peak resample memory (review H4)
      - `infra.alpha_frame_writer.chunk_size` = 50000, type int, tagged `[conventional]`
      - `infra.counterfactual_tracker.itersize` = 5000, type int, tagged `[conventional]` (consumed Plan 02)
      - `infra.counterfactual_tracker.workers` = 12, type int, tagged `[conventional]` (consumed Plan 02)
    Do NOT re-seed the 36 existing `alpha.frame.hold_max_bars.<regime>.<tf>` keys (they already exist
    from migration 195/142A). Do NOT add `alpha.frame.grid_stop_atr_mults` — the 4-variant grid is
    out of scope (frame_variant='primary' only).

    In `services/service_auditor.py`: add `"indicagent-alpha-frame-writer": 8` to `_DAG_ORDER` and
    `"indicagent-alpha-frame-writer"` to `_ONESHOT_UNITS`, mirroring the indicagent-ensemble-ic-engine
    entries. Do NOT add an `_AGENT_ID_TO_UNIT` entry (that dict is for BaseDaemon lag services, not
    oneshots — Pitfall 5). Do NOT add topic_alpha_frames to stream_keys.py (no consumer — Pitfall 5).

    In `scripts/infrastructure/backfill/infrastructure_truncate_derived_tables.sh` (review M1): add a
    `psql -c "TRUNCATE alpha_frames;"` line alongside the existing `TRUNCATE alpha_events;` (line 54)
    so a corpus rebuild wipes derived frames too, and update the pre/post count-report SELECTs and the
    "Truncate all nine tables" confirmation prompt wording to include alpha_frames. This keeps
    alpha_frames consistent with a fresh alpha_events after every rebuild, and is the operational
    substitute for the FK-CASCADE that deviation (4) deliberately omits.

    Create `tests/unit/test_alpha_frames_schema.py` asserting against the migration SQL text (read the
    .sql file as a string, mirror test_ensemble_ic_config.py's no-DB style): the status CHECK contains
    'closed_ic_decay' and 'closed_max_hold', does NOT contain 'closed_reversal'; the file contains
    'corpus_run_id' and 'weight_epoch'; the PK line is the composite `PRIMARY KEY (frame_id, bar_ts)`
    (assert both 'frame_id' and 'bar_ts' appear in the PK declaration) and the file does NOT contain
    'frame_id uuid PRIMARY KEY' nor a standalone `frame_id ... PRIMARY KEY` on its own; the file does
    NOT contain 'REFERENCES alpha_events' (no FK, review M1); the file contains 'create_hypertable' and
    'uq_alpha_frames_variant'; it seeds config_key 'alpha.frame.target_r_multiple', 'alpha.frame.atr_period',
    'alpha.scoring.min_strategy_n', and 'alpha.scoring.bootstrap_max_n', and does NOT contain the string
    'target_r_fallback'.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_alpha_frames_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "closed_reversal" production/migrations/214_alpha_frames_schema.sql` returns 0
    - `grep -c "closed_ic_decay" production/migrations/214_alpha_frames_schema.sql` returns >= 1
    - `grep -c "target_r_fallback" production/migrations/214_alpha_frames_schema.sql` returns 0
    - `grep -c "REFERENCES alpha_events" production/migrations/214_alpha_frames_schema.sql` returns 0 (no FK, review M1)
    - `grep -c "frame_id uuid" production/migrations/214_alpha_frames_schema.sql` returns 0 (frame_id is text; review H1)
    - The PK declaration is composite and contains bar_ts (grep for `PRIMARY KEY (frame_id, bar_ts)`); create_hypertable('alpha_frames','bar_ts'...) present
    - Migration seeds config keys: alpha.frame.stop_atr_mult, alpha.frame.target_r_multiple, alpha.frame.atr_period, alpha.scoring.min_strategy_n, alpha.scoring.bootstrap_max_n, alpha.scoring.bootstrap_batch, infra.alpha_frame_writer.chunk_size, infra.counterfactual_tracker.itersize, infra.counterfactual_tracker.workers (grep each config_key string present)
    - `grep -c "indicagent-alpha-frame-writer" services/service_auditor.py` returns >= 2 (in _DAG_ORDER and _ONESHOT_UNITS)
    - `grep -c "TRUNCATE alpha_frames" scripts/infrastructure/backfill/infrastructure_truncate_derived_tables.sh` returns >= 1 (review M1)
    - Apply-time check (review H1): applying migration 214 against the live DB (or a scratch schema) succeeds — create_hypertable does NOT raise "cannot create a unique index without the column 'bar_ts'". Run the migration and confirm `alpha_frames` appears as a hypertable (`SELECT * FROM timescaledb_information.hypertables WHERE hypertable_name='alpha_frames'` returns a row)
    - test_alpha_frames_schema.py passes
  </acceptance_criteria>
  <done>Migration 214 creates alpha_frames as a hypertable with the corrected state machine, a composite PK containing bar_ts, no FK, provenance columns, documented D-03 column units, and all nine new APR keys under the correct names; registers the writer as a oneshot only; adds alpha_frames to the corpus-rebuild truncate script; and the schema-assertion test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AlphaFrameWriter service — FRAME-01 pure geometry + expected-R snapshot + chunked idempotent write</name>
  <files>services/alpha_frame_writer.py, tests/unit/test_alpha_frame_writer_geometry.py, tests/unit/test_alpha_frame_writer.py</files>
  <read_first>
    - services/alpha_publisher.py (full — BaseBatch subclass, imports, manifest-wrapped execute(), _CHUNK_SIZE accumulate-and-flush, content_key()-derived id, CLI entrypoint with init_otel_providers/DSN-strip/asyncio.run)
    - services/_batch_utils.py (cfg helper + load_apr_dict_async for APR loading)
    - src/core/agent/base_batch.py (BaseBatch.run/execute/content_key contract)
    - tests/unit/test_ensemble_ic_config.py (pure-fn/dataclass config-binding test style)
    - .planning/milestones/v3.1-phases/142B-frame-simulation-counterfactual-tracking/142B-RESEARCH.md (Pattern 2 chunked write; Pattern 4 anti-join --backfill; Pitfall 1 ATR-only path)
    - .planning/milestones/v3.1-phases/142B-frame-simulation-counterfactual-tracking/142B-REVIEWS.md (H2 ATR source, L1 long read transaction)
  </read_first>
  <behavior>
    - Geometry (pure fn, ATR-only target path since sr_resist_dist is 100% NULL; `atr` is a
      caller-supplied price-unit value — review H2 — never read from feature_vectors):
      given direction='long', entry, atr, stop_atr_mult=1.5, target_r_multiple=2.0 →
      stop_price = entry - 1.5*atr; stop_distance = entry - stop_price; target_price = entry + 2.0*stop_distance;
      r_multiple = (target_price - entry)/(entry - stop_price) == 2.0
    - Geometry for direction='short': stop_price = entry + 1.5*atr (stop ABOVE entry);
      stop_distance = stop_price - entry; target_price = entry - 2.0*stop_distance (target BELOW entry);
      r_multiple = (entry - target_price)/(stop_price - entry) == 2.0
    - No conditional branch on sr_resist_dist exists (or if present, is dead and not load-bearing)
    - Expected-R diagnostic snapshot (pure fn, D-03): given alpha_score=0.4, target_r_multiple=2.0, cost_r=0.05 →
      gross_expected_r = abs(alpha_score) * target_r_multiple = 0.8; net_expected_r = gross_expected_r - cost_r = 0.75.
      Direction-agnostic: alpha_score=-0.4 also yields gross_expected_r = 0.8 (abs magnitude — the traded
      direction is already chosen by the `direction` column). All three of gross/cost/net written non-NULL.
    - frame_id = BaseBatch.content_key(event_id, str(bar_ts), 'primary') — deterministic TEXT; re-running the
      writer over the same alpha_events row yields the same frame_id (idempotency via ON CONFLICT DO NOTHING
      on uq_alpha_frames_variant)
    - FrameConfig.from_apr binds alpha.frame.stop_atr_mult / alpha.frame.target_r_multiple / alpha.frame.atr_period;
      applies documented defaults (1.5 / 2.0 / 14) when keys absent
  </behavior>
  <action>
    Write `services/alpha_frame_writer.py` defining `class AlphaFrameWriter(BaseBatch)` with
    `job_name = "alpha-frame-writer"`, `compute_version = "1.0.0"`, constructor
    `__init__(self, db_dsn: str, backfill: bool = False)`. Mirror alpha_publisher.py's import block but
    drop all Kafka imports (Pitfall 5). Implement a manifest-wrapped `execute()` exactly like
    alpha_publisher.py (CorpusManifest("alpha_frame_writer", ...), `except Exception as error:` —
    exception var name is `error` per CLAUDE.md).

    Geometry: implement a pure module-level function `compute_frame_geometry(direction, entry_price,
    atr, stop_atr_mult, target_r_multiple) -> tuple[stop_price, target_price, r_multiple]` implementing
    the ATR-only math in <behavior>, correct for BOTH long and short. `atr` is a caller-supplied
    price-unit value (review H2) — this function does NOT read any table and does not branch on
    sr_resist_dist/sr_support_dist. Plan 02's CounterfactualTracker imports this function and supplies
    an ATR computed from market_data_ohlcv at T+1 fill time. Bind stop_atr_mult, target_r_multiple, and
    atr_period from APR via a small `FrameConfig` dataclass with a `from_apr(cfg_dict)` classmethod
    (defaults 1.5 / 2.0 / 14), mirroring test_ensemble_ic_config.py's binding pattern. All numeric params
    come from APR — no hardcoded thresholds survive in the code (CLAUDE.md APR mandate); the dataclass
    defaults are the documented fallback only.

    Expected-R diagnostic snapshot (D-03 — the three columns gross_expected_r / cost_r / net_expected_r
    MUST be non-NULL on every frame; NULLs here are a task failure): implement a pure module-level
    helper `compute_expected_r_snapshot(alpha_score, target_r_multiple, cost_r) -> tuple[float, float]`
    returning (gross_expected_r, net_expected_r), defined EXACTLY as:
      - `gross_expected_r = abs(alpha_score) * target_r_multiple` — the ex-ante expected payoff magnitude
        in R at entry: the model's directional-confidence magnitude (`alpha_events.alpha_score`) scaled by
        the frame's design R-multiple on a win (`alpha.frame.target_r_multiple` = 2.0). This is a
        diagnostic magnitude, not a probability and not a gate input (units documented in the migration
        column comment per M5). Use the APR `target_r_multiple`, NOT the `r_multiple` geometry column
        (written NULL at FRAME-01 time). Use `abs(alpha_score)` so the snapshot is direction-agnostic.
      - `cost_r` = copied directly from `alpha_events.cost_hurdle` for this row — NOT re-derived live from
        the `alpha.quant.cost_hurdle.<tf>` APR key. `alpha_events.cost_hurdle` (migration 185) is the
        per-row audit snapshot stamped by `alpha_publisher.py`'s `_cost_hurdle_for_tf` at publish time;
        re-deriving from live APR would make `net_expected_r` silently drift from historical truth on the
        next cost-hurdle recalibration + backfill re-run. Copy-through only, same pattern as
        `weight_epoch = alpha_events.weight_version`.
      - `net_expected_r = gross_expected_r - cost_r`.
    These three are diagnostic snapshots only (D-03) — they do NOT feed FRAME-04's gate (D-01).

    NOTE on geometry timing (review H2): entry_price / stop_price / target_price / r_multiple require the
    T+1 open AND a price-unit ATR, both of which CounterfactualTracker (Plan 02) produces from
    market_data_ohlcv during its scan. At FRAME-01 write time those price columns are written NULL;
    AlphaFrameWriter writes the APR SNAPSHOT columns (stop_atr_mult, max_hold_bars), the signal snapshot
    from alpha_events, AND the D-03 expected-R snapshot triad (computable at creation because it uses the
    APR target_r_multiple and alpha_score, not the NULL geometry columns). Keep compute_frame_geometry a
    pure function in this file so Plan 02's tracker imports and reuses it for the T+1 fill.

    Write path (review L1 — do NOT hold one 12.2M-row read transaction): read alpha_events via the
    anti-join query, but iterate it PER (symbol, tf) partition so each partition's read+flush is its own
    short transaction rather than one giant streaming transaction over all 78 child tables. For each
    (symbol, tf) partition run
    `SELECT ae.* FROM alpha_events ae LEFT JOIN alpha_frames af ON af.event_id = ae.event_id AND
    af.bar_ts = ae.bar_ts AND af.frame_variant = 'primary' WHERE ae.symbol = $1 AND ae.tf = $2 AND
    af.frame_id IS NULL` (parameterized; same query serves nightly-incremental and --backfill — Pattern
    4). Stream each partition via asyncpg `conn.cursor(...)`, accumulate output tuples, flush via
    `await wconn.executemany(_INSERT_SQL, chunk)` every `infra.alpha_frame_writer.chunk_size` (50000)
    rows, then a final flush at partition end — never one giant in-memory list, never one long
    transaction (D-05 / review L1). Insert uses `ON CONFLICT (event_id, bar_ts, frame_variant) DO NOTHING`
    for idempotency.

    Per-frame column population: frame_id = content_key(event_id, str(bar_ts), 'primary') (TEXT);
    frame_variant='primary'; symbol/tf/bar_ts/regime/direction/alpha_score/alpha_ci_lower/alpha_ci_upper
    copied from the alpha_events row; entry_price/stop_price/target_price/r_multiple written NULL (filled
    by Plan 02); weight_epoch = alpha_events.weight_version (copy-through, A1); corpus_run_id = a single
    ISO-timestamp string pinned once per invocation (mirror ensemble_ic_engine.py's run_ts pattern, A2)
    and stamped on every frame; max_hold_bars snapshot = the value of
    `alpha.frame.hold_max_bars.<regime>.<tf>` for the row's cell; stop_atr_mult snapshot = the APR value;
    status='open'. D-03 diagnostic triad: gross_expected_r / cost_r / net_expected_r via
    `compute_expected_r_snapshot(alpha_score, target_r_multiple, cost_r)` where `cost_r` is
    `alpha_events.cost_hurdle` copied through, written non-NULL on every row.

    Add a `--backfill` CLI flag (`action="store_true"`) and the standard CLI entrypoint
    (`init_otel_providers("indicagent-alpha-frame-writer")`, DSN strip, `asyncio.run(...)`) copied from
    alpha_publisher.py's shape. Register no Kafka anything.

    Create `tests/unit/test_alpha_frame_writer_geometry.py` (pure-fn geometry cases from <behavior>,
    long and short, r_multiple == 2.0) and `tests/unit/test_alpha_frame_writer.py` (FrameConfig.from_apr
    binds and defaults incl. atr_period; content_key-derived frame_id is stable across two calls with
    identical inputs — idempotency proof; `compute_expected_r_snapshot(0.4, 2.0, 0.05)` returns (0.8,
    0.75) and `compute_expected_r_snapshot(-0.4, 2.0, 0.05)` also returns (0.8, 0.75) with both returns
    strictly non-None/non-NaN; and a grep-style assertion that the module contains no 'sr_resist_dist'
    load-bearing branch and does not read ATR from feature_vectors — the geometry function source has no
    'sr_resist_dist' and no 'feature_vectors' reference).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_alpha_frame_writer_geometry.py tests/unit/test_alpha_frame_writer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `compute_frame_geometry('long', 100.0, 2.0, 1.5, 2.0)` returns stop=97.0, target=106.0, r_multiple=2.0
    - `compute_frame_geometry('short', 100.0, 2.0, 1.5, 2.0)` returns stop=103.0, target=94.0, r_multiple=2.0
    - `compute_expected_r_snapshot(0.4, 2.0, 0.05)` returns (0.8, 0.75); `compute_expected_r_snapshot(-0.4, 2.0, 0.05)` also returns (0.8, 0.75) — D-03 formula proven, direction-agnostic
    - The _INSERT_SQL column list includes gross_expected_r, cost_r, and net_expected_r, and every row tuple supplies all three as non-NULL values
    - `content_key(event_id, str(bar_ts), 'primary')` is byte-identical across two invocations with the same inputs
    - `grep -c "sr_resist_dist" services/alpha_frame_writer.py` returns 0 (ATR-only path, no dead branch)
    - `grep -c "feature_vectors" services/alpha_frame_writer.py` returns 0 (ATR is caller-supplied, not read here — review H2)
    - `grep -c "topic_alpha_frames\|KafkaProducer" services/alpha_frame_writer.py` returns 0
    - Exception handler uses `as error` (grep `except Exception as error` present; `as exc` absent)
    - The anti-join write SQL uses parameterized placeholders only (no f-string/%-format SQL of user/data values) and is scoped per (symbol, tf) partition (review L1)
    - Both geometry and writer tests pass
  </acceptance_criteria>
  <done>AlphaFrameWriter writes one idempotent primary frame per pending alpha_events row via per-(symbol,tf) chunked anti-join flush (no long transaction), leaves geometry columns NULL for Plan 02's T+1 fill, exposes a pure direction-correct ATR-only compute_frame_geometry taking a caller-supplied price-unit ATR, populates the D-03 gross/cost/net expected-R triad non-NULL via a unit-tested helper, and RED→GREEN unit tests are green.</done>
</task>

<task type="auto">
  <name>Task 3: SHADOW-REVIEW.md frozen pre-commitment document</name>
  <files>docs/plans/SHADOW-REVIEW.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 142B section: the "Renaissance pre-commitment" criteria block, lines ~988-997)
    - .planning/milestones/v3.1-phases/142B-frame-simulation-counterfactual-tracking/142B-CONTEXT.md (D-01 gross gate, D-02 net_expected_r reporting column)
    - .planning/milestones/v3.1-phases/142B-frame-simulation-counterfactual-tracking/142B-REVIEWS.md (M5 gross_expected_r units, M6 numeric criteria, H4 bootstrap method + caveat)
    - docs/plans/archive/2026-06-25-v30-alpha-lifecycle-schema.md (section-heading discipline, structural precedent only)
  </read_first>
  <action>
    Write `docs/plans/SHADOW-REVIEW.md` as a one-page frozen pre-commitment document specifying the
    exact NUMERIC criteria required for Phase 147 live promotion. Commit these as final values:

    (1) >= 60 trading days of closed alpha_frames (primary variant).
    (2) mean(counterfactual_pnl_r) > 0 at 95% CI (one-tailed) on OOS data, evaluated via a
        DAY-CLUSTERED BLOCK BOOTSTRAP (review H4): frames are aggregated to a per-calendar-day mean
        pnl_r within each cell BEFORE resampling, and the bootstrap resamples day-clusters, not
        individual frames — because frames opened on nearly every bar with hold horizons up to 60 bars
        have massively overlapping price paths, so per-frame i.i.d. resampling produces a drastically
        too-tight (anticonservative) CI. Method: `scipy.stats.bootstrap` with `method='BCa'` for
        day-cluster counts <= `alpha.scoring.bootstrap_max_n`, else an analytic one-sided CLT lower
        bound (BCa's bias-correction jackknife is both unnecessary and infeasible at high cluster
        counts). Passes iff the day-clustered CI lower bound > 0.
    (3) Sharpe of counterfactual_pnl_r > 0.5 annualized.
    (4) Maximum drawdown of the cumulative counterfactual_pnl_r equity curve (in R units) does NOT
        exceed 25% of the running peak cumulative R at the trough — i.e.
        `max_peak_to_trough_decline_R / peak_cumulative_R_at_trough < 0.25` (review M6: base stated
        explicitly as peak cumulative R; drawdown measured in R units, not an unspecified percentage).
    (5) NO IC-SHARPE CLIFF, defined numerically (review M6): EnsembleICEngine IC Sharpe over the
        trailing 20 trading days of the shadow period >= 0.5 × the full-shadow-period IC Sharpe
        (`last_20d_IC_Sharpe / full_period_IC_Sharpe >= 0.5`).

    Add a "Statistical caveats (frozen)" subsection documenting the residual anticonservatism the
    day-clustering does NOT remove: day-clustering pools all frames in a (tf, regime) cell that share a
    calendar date into one cluster (collapsing within-symbol intraday overlap AND cross-symbol same-day
    correlation into a single conservative unit); the remaining cross-symbol same-day correlation is a
    known, direction-conservative, accepted residual. State that this caveat is frozen with the criteria
    and may NOT be renegotiated after data collection.

    Per D-01: state explicitly that all pass/fail criteria are evaluated on GROSS counterfactual_pnl_r,
    with the reasoned rationale (gating on the unvalidated alpha.quant.cost_hurdle.* keys would conflate
    "does the frame capture IC as P&L" with "is our cost estimate right" — a third question). Per D-02:
    add a mandatory `net_expected_r` reporting column (gross minus the calibrated
    alpha.quant.cost_hurdle.* keys) reported alongside every gross metric — explicitly labeled as
    REPORTING ONLY, NOT A GATE. Document the interpretation of the diagnostic `gross_expected_r` /
    `net_expected_r` columns (review M5): `gross_expected_r = abs(alpha_score) × alpha.frame.target_r_multiple`
    is an ex-ante expected-payoff MAGNITUDE in R (a diagnostic, not a realized P&L and not a
    win-probability), and `net_expected_r = gross_expected_r - cost_r`; both are reporting-only and must
    never be confused with the realized `counterfactual_pnl_r` that the gate evaluates.

    Include the ROADMAP's no-post-hoc-negotiation clause verbatim ("Post-hoc gate negotiation is not
    permitted. If the gate fails, diagnose — don't renegotiate."). Add a header line stating the document
    is frozen as of the commit date and must exist before any counterfactual data is collected.

    Do NOT use "v1"/"simplified"/"placeholder" framing anywhere. Do NOT introduce criteria beyond the
    five above. This is a decision record, not a design doc.
  </action>
  <verify>
    <automated>test -f docs/plans/SHADOW-REVIEW.md && grep -c "net_expected_r" docs/plans/SHADOW-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - File docs/plans/SHADOW-REVIEW.md exists
    - Contains all five criteria strings: "60 trading days", "95% CI", "Sharpe", "drawdown", "IC Sharpe"
    - Criterion 4 states its base numerically (peak cumulative R; grep "peak cumulative R" present) and criterion 5 defines "cliff" numerically (grep "0.5" near the last-20-day IC Sharpe clause) — review M6
    - Documents the day-clustered block-bootstrap method and the residual-correlation caveat (grep "day-cluster" / "block bootstrap" present) — review H4
    - Contains the word GROSS applied to the gate and states net_expected_r is reporting-only, not a gate
    - Documents the gross_expected_r units interpretation (review M5)
    - Contains the no-post-hoc-negotiation clause
    - `grep -ci "placeholder\|simplified\|v1 version" docs/plans/SHADOW-REVIEW.md` returns 0
  </acceptance_criteria>
  <done>SHADOW-REVIEW.md is frozen in git with five numerically-evaluable promotion criteria, the day-clustered block-bootstrap method + caveat, the D-01 gross-gate decision, the D-02 net_expected_r reporting-only column, and documented D-03 expected-R units, before Plan 02's first production run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alpha_events (DB) → AlphaFrameWriter | Internal table read; no external/user input crosses here |
| AlphaFrameWriter → alpha_frames (DB) | Batch write via parameterized SQL; single serial writer connection |
| Migration 214 → TimescaleDB catalog | DDL apply; create_hypertable must accept the composite PK/uq constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142B-01 | Tampering | Anti-join / INSERT SQL in alpha_frame_writer.py | mitigate | Parameterized queries only ($1/executemany params); acceptance criteria greps for no f-string/%-format SQL of data values (V5 ASVS) |
| T-142B-02 | Denial of Service | Full 12.2M-row --backfill pass | mitigate | Per-(symbol,tf) chunked accumulate-and-flush at infra.alpha_frame_writer.chunk_size; no unbounded fetch, no single long-running transaction (D-05, review L1) |
| T-142B-03 | Tampering (config) | Numeric geometry params | mitigate | All numeric params (stop_atr_mult, target_r_multiple, atr_period, chunk_size) load via APR/ConfigService; migration seeds them; no hardcoded thresholds (CLAUDE.md APR mandate, V5) |
| T-142B-09 | Denial of Service / Integrity (apply-time) | Migration 214 create_hypertable on alpha_frames | mitigate | Composite PK (frame_id, bar_ts) + uq (event_id, bar_ts, frame_variant) both contain the partition column bar_ts, so create_hypertable applies cleanly instead of erroring (review H1); no FK to the truncated alpha_events hypertable (review M1); apply-time acceptance check confirms the hypertable is created |
| T-142B-SC | Tampering | package installs | accept | Zero new third-party packages this phase (RESEARCH Package Legitimacy Audit: not applicable) — no supply-chain surface introduced |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_alpha_frames_schema.py tests/unit/test_alpha_frame_writer_geometry.py tests/unit/test_alpha_frame_writer.py -x -q` green
- `.venv/bin/pytest tests/unit/ -q` shows no new failures
- Migration 214 applies cleanly (idempotent CREATE TABLE IF NOT EXISTS + ON CONFLICT DO NOTHING seeds) AND create_hypertable succeeds with the composite PK — alpha_frames appears in timescaledb_information.hypertables (review H1)
- alpha_frame_writer.py contains no Kafka imports, no sr_resist_dist branch, and no feature_vectors read (ATR is caller-supplied, review H2)
- infrastructure_truncate_derived_tables.sh truncates alpha_frames alongside alpha_events (review M1)
</verification>

<success_criteria>
- alpha_frames hypertable exists with D-04 lifecycle CHECK (closed_ic_decay in, closed_reversal out), composite PK (frame_id, bar_ts), no FK, plus corpus_run_id/weight_epoch
- Nine new APR keys seeded under the correct names (target_r_multiple not target_r_fallback; atr_period; bootstrap_max_n/batch)
- AlphaFrameWriter writes idempotent primary frames via per-partition chunked anti-join with geometry columns NULL for Plan 02, and a pure direction-correct ATR-only compute_frame_geometry taking a caller-supplied ATR
- D-03 gross_expected_r / cost_r / net_expected_r diagnostic snapshots populated non-NULL on every frame via the unit-tested compute_expected_r_snapshot helper, with documented column-comment units
- SHADOW-REVIEW.md frozen with five numeric criteria, day-clustered block-bootstrap method + caveat, gross gate (D-01) + net_expected_r reporting column (D-02)
- indicagent-alpha-frame-writer registered as a oneshot only; alpha_frames added to the corpus-rebuild truncate script
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/142B-frame-simulation-counterfactual-tracking/142B-01-SUMMARY.md` when done
</output>
</content>
</invoke>
