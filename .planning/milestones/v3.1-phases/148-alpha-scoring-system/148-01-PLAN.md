---
phase: 148
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/248_alpha_scoring_gate_tables.sql
  - tests/unit/test_alpha_scorer.py
  - tests/unit/test_oos_gate1_signal_eval.py
  - tests/unit/test_score03_gate2_execution_eval.py
autonomous: true
requirements: [SCORE-01, SCORE-02, SCORE-03]

must_haves:
  truths:
    - "alpha_strategy_scores table exists in the live DB"
    - "gate_evaluations table exists in the live DB"
    - "alpha.scoring.min_sharpe / max_drawdown_ratio / min_ic_alpha_score_corr APR keys are queryable in config_state"
    - "Three Wave 0 test files collect without import errors and fail RED"
  artifacts:
    - path: "production/migrations/248_alpha_scoring_gate_tables.sql"
      provides: "alpha_strategy_scores + gate_evaluations DDL and missing alpha.scoring.* APR seeds"
      contains: "CREATE TABLE IF NOT EXISTS alpha_strategy_scores"
    - path: "tests/unit/test_alpha_scorer.py"
      provides: "SCORE-01 test scaffold (corr + decile bucketing stubs)"
    - path: "tests/unit/test_oos_gate1_signal_eval.py"
      provides: "SCORE-02 test scaffold (oos_start + methodology stubs)"
    - path: "tests/unit/test_score03_gate2_execution_eval.py"
      provides: "SCORE-03 test scaffold (regime_stratified stub)"
  key_links:
    - from: "248_alpha_scoring_gate_tables.sql"
      to: "config_state"
      via: "config_schema/config_state/config_history INSERT triple"
      pattern: "alpha\\.scoring\\.(min_sharpe|max_drawdown_ratio|min_ic_alpha_score_corr)"
---

<objective>
Lay the shared foundation for Phase 148: create the two tables this phase writes to
(alpha_strategy_scores, gate_evaluations), seed the alpha.scoring.* APR keys the schema doc
specified but nobody ever migrated in, and create the three Wave 0 test scaffolds that the
Wave 2 build plans will turn GREEN.

Purpose: Every downstream plan (SCORE-01/02/03) writes to a table or reads an APR key that
does not exist yet. This plan removes that blocker once, cleanly, before any code is written.
Output: One applied migration, three collectable-but-RED test files.
</objective>

<review_revisions>
Revised from cross-AI review (148-REVIEWS.md). Change traceable to a specific finding:
- Codex LOW ("migration-audit-label drift"): the `config_history.changed_by` string must be
  DERIVED from the actual migration number used at execution time, not a stale hardcoded
  `migration_248` literal. If the renumbering fallback fires (248 already taken), the audit
  label would otherwise silently diverge from the real filename. Fixed in Task 1 action +
  a new acceptance criterion making changed_by/filename consistency a verifiable condition.
Everything else in this plan (migration number 248, table DDL, the three APR seeds, the
provenance-class split, Wave 0 scaffolds) is unchanged from the reviewed version.
</review_revisions>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.1-phases/148-alpha-scoring-system/148-RESEARCH.md
@.planning/milestones/v3.1-phases/148-alpha-scoring-system/148-PATTERNS.md
@.planning/milestones/v3.1-phases/148-alpha-scoring-system/148-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration for alpha_strategy_scores + gate_evaluations tables and missing alpha.scoring.* APR keys</name>
  <files>production/migrations/248_alpha_scoring_gate_tables.sql</files>
  <read_first>
    - production/migrations/244_regime_gate_min_clusters.sql (APR-triple template; copy its config_schema/config_state/config_history shape and its "PRE-REGISTERED, NOT TUNABLE POST-HOC" provenance narrative)
    - production/migrations/236_alpha_frames_is_shadow.sql (simplest DDL-only precedent; BEGIN/COMMIT shape)
    - docs/plans/archive/2026-06-25-v30-alpha-lifecycle-schema.md (alpha_strategy_scores intended column set; note this doc's phase numbers are stale, trust the schema/logic only)
    - .planning/milestones/v3.1-phases/148-alpha-scoring-system/148-PATTERNS.md (migration-numbering section; live alpha_frames text-ID convention)
  </read_first>
  <action>
    First re-verify the head migration number at execution time with `ls production/migrations/ | sort -V | tail -5`; the expected next number is 248 (247 landed 2026-07-22 as `247_regime_groups_dual_write_symbol_hmm.sql`, an unrelated concurrent workstream; Phase 162, also planned this session but scheduled to execute after this phase, has already reserved 249-252 for its own four migrations -- do not use those either). This repo has had concurrent-session number collisions before (two files share 240), so if 248 is already taken by the time this executes, use the next free integer above 252 and adjust the filename and this plan's files_modified accordingly.

    Let MIGRATION_NUM be the actual integer used for the filename (248 in the common case, or the fallback integer if 248 was taken). Everywhere this action references `migration_248`, substitute `migration_<MIGRATION_NUM>` for the ACTUAL number used -- the config_history changed_by label must always match the real filename, never a stale literal (Codex review LOW: renumbering-fallback audit-label drift).

    Create `production/migrations/<MIGRATION_NUM>_alpha_scoring_gate_tables.sql` as a single BEGIN/COMMIT block containing:

    (a) `CREATE TABLE IF NOT EXISTS alpha_strategy_scores` with columns: symbol text, tf text, regime text, alpha_score_decile integer, sample_n integer, n_clusters integer, win_rate double precision, sharpe_annualized double precision, max_drawdown double precision, ic_alpha_score_corr double precision, ci_lower double precision, ci_upper double precision, compute_version text, run_ts timestamptz NOT NULL DEFAULT now(), PRIMARY KEY (symbol, tf, regime, alpha_score_decile, run_ts). Use text/double-precision types (never uuid) to match the live alpha_frames.frame_id text convention (RESEARCH.md Pitfall 4).

    (b) `CREATE TABLE IF NOT EXISTS gate_evaluations` with columns: eval_id text, gate_id text NOT NULL, result text NOT NULL, evidence jsonb NOT NULL, run_ts timestamptz NOT NULL DEFAULT now(), PRIMARY KEY (gate_id, run_ts). Use a loose jsonb evidence column with no enforced sub-schema (matches llm_calls / integrity_monitor convention, RESEARCH.md Open Question 3).

    (c) The config_schema/config_state/config_history INSERT triple (each ON CONFLICT DO NOTHING) for exactly these three never-seeded keys: alpha.scoring.min_sharpe=0.5 (float, min 0.0 max 5.0), alpha.scoring.max_drawdown_ratio=0.25 (float, min 0.0 max 1.0), alpha.scoring.min_ic_alpha_score_corr=0.3 (float, min -1.0 max 1.0). These keys split into two provenance classes:
      - The two GATE-THRESHOLD keys (min_sharpe encoding SHADOW-REVIEW criterion 3, max_drawdown_ratio encoding criterion 4): each config_schema description must start with '[conventional]', cite the SHADOW-REVIEW.md criterion it encodes, and include the exact phrase 'PRE-REGISTERED: not tunable post-hoc.' These are the enforced Gate 2 thresholds, read at the call site by plan 148-04.
      - min_ic_alpha_score_corr is DIAGNOSTIC-ONLY: ROADMAP.md explicitly defines ic_alpha_score_corr as a monotonicity diagnostic, NOT a gate (no plan thresholds against it). Its config_schema description must start with '[conventional]', cite the SCORE-01 ic_alpha_score_corr diagnostic, and explicitly state 'DIAGNOSTIC-ONLY, not a gate threshold' -- do NOT use the 'not tunable post-hoc' gate-threshold framing for this key.
      config_history changed_by='migration_<MIGRATION_NUM>' (the ACTUAL number used, matching the filename) with a reason noting the key was never previously migrated.

    Do NOT re-seed the four already-live keys (alpha.scoring.min_strategy_n=30, bootstrap_max_n=5000, bootstrap_batch=1000, bootstrap_random_state=42); ON CONFLICT DO NOTHING makes accidental inclusion harmless but they should not be listed.

    Apply the migration with `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/<MIGRATION_NUM>_alpha_scoring_gate_tables.sql`.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tc "SELECT to_regclass('alpha_strategy_scores') IS NOT NULL AND to_regclass('gate_evaluations') IS NOT NULL AND (SELECT count(*) FROM config_state WHERE config_key IN ('alpha.scoring.min_sharpe','alpha.scoring.max_drawdown_ratio','alpha.scoring.min_ic_alpha_score_corr'))=3;" | grep -q t</automated>
  </verify>
  <acceptance_criteria>
    - `\d alpha_strategy_scores` shows a table whose PK is (symbol, tf, regime, alpha_score_decile, run_ts)
    - `\d gate_evaluations` shows a table with a jsonb evidence column
    - SELECT config_value FROM config_state WHERE config_key='alpha.scoring.min_sharpe' returns '0.5'
    - SELECT config_value FROM config_state WHERE config_key='alpha.scoring.max_drawdown_ratio' returns '0.25'
    - SELECT config_value FROM config_state WHERE config_key='alpha.scoring.min_ic_alpha_score_corr' returns '0.3'
    - The four pre-existing keys (min_strategy_n, bootstrap_max_n, bootstrap_batch, bootstrap_random_state) still hold their prior values (30/5000/1000/42), unchanged
    - No uuid column type appears in either CREATE TABLE statement
    - The config_history changed_by label for the three new keys equals `migration_<N>` where `<N>` is the actual migration filename integer used (Codex LOW audit-label drift): `SELECT DISTINCT changed_by FROM config_history WHERE config_key LIKE 'alpha.scoring.%' AND changed_by LIKE 'migration_%'` returns the SAME integer that appears in the applied migration's filename -- if the renumbering fallback fired, both moved together, no stale `migration_248` literal remains
  </acceptance_criteria>
  <done>Both tables exist in the live DB, three new APR keys seeded, migration file created and applied, changed_by label matches the actual migration filename number.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 test scaffolds for SCORE-01/02/03</name>
  <files>tests/unit/test_alpha_scorer.py, tests/unit/test_oos_gate1_signal_eval.py, tests/unit/test_score03_gate2_execution_eval.py</files>
  <read_first>
    - tests/unit/test_ensemble_ic_gate.py (pure-function unit-test shape)
    - tests/unit/test_counterfactual_tracker.py (bootstrap-gate test shape)
    - .planning/milestones/v3.1-phases/148-alpha-scoring-system/148-VALIDATION.md (exact -k filter names each stub must contain)
  </read_first>
  <action>
    Create three test files as failing RED stubs. Each stub function must exist by name so the Wave 2 plans have a concrete target, and each must currently fail (e.g. body is `pytest.fail("Wave 0 stub - implemented in Wave 2 SCORE build plan")`). Do NOT import the not-yet-built production modules (services.alpha_scorer, scripts.ops.corpus.ops_oos_gate1_signal_eval, scripts.analysis.score03_gate2_execution_eval) at module scope; either defer those imports inside the test body or omit them entirely so `pytest --collect-only` succeeds without ImportError.

    Required function names (VALIDATION.md -k targets):
    - tests/unit/test_alpha_scorer.py: `test_buckets_deciles_and_filters_min_strategy_n`, `test_ic_alpha_score_corr_monotonic` (the `corr` -k target)
    - tests/unit/test_oos_gate1_signal_eval.py: `test_fails_loud_when_oos_start_unset` (the `oos_start` -k target), `test_uses_fisher_z_ci_methodology` (the `methodology` -k target), `test_dry_run_writes_nothing` (the dry-run escape-hatch target, Wave 2 148-03 fills it), `test_gate1_evidence_payload_shape` (the integration-style exact-payload-shape target, Wave 2 148-03 fills it)
    - tests/unit/test_score03_gate2_execution_eval.py: `test_cites_champion_143_1_08_numbers`, `test_regime_stratified_companion_required` (the `regime_stratified` -k target), `test_dry_run_writes_nothing` (the dry-run escape-hatch target, Wave 2 148-04 fills it), `test_gate2_evidence_payload_shape` (the integration-style exact-payload-shape target, Wave 2 148-04 fills it)
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_alpha_scorer.py tests/unit/test_oos_gate1_signal_eval.py tests/unit/test_score03_gate2_execution_eval.py --collect-only -q 2>&1 | grep -Eq "test_ic_alpha_score_corr_monotonic|tests collected"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only` lists all named test functions (including the two new dry-run stubs and the two new payload-shape stubs) with zero collection errors
    - Running the three files produces only failures/xfails (no errors, no passes) - they are RED by construction
    - No module-scope import of services.alpha_scorer, ops_oos_gate1_signal_eval, or score03_gate2_execution_eval
    - The exact -k target substrings (`corr`, `oos_start`, `methodology`, `regime_stratified`, `dry_run`, `payload_shape`) each match at least one function name
  </acceptance_criteria>
  <done>Three test files exist, collect cleanly, and fail RED; Wave 2 targets are named (including the dry-run and payload-shape stubs the review added).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> DB (DDL/config write) | Migration runs with existing DB credentials; no external/untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-F1 | Tampering | migration APR seeds | mitigate | ON CONFLICT DO NOTHING on all three INSERTs so a concurrent/re-run migration cannot overwrite an already-live value |
| T-148-F2 | Repudiation | APR provenance | mitigate | config_history row records changed_by (matching actual migration filename number) + reason for every seeded key (auditable trail) |
</threat_model>

<verification>
- Both new tables resolvable via to_regclass; three new APR keys present with exact seeded values.
- config_history changed_by matches the actual migration filename integer (no stale literal).
- Three test files collect without ImportError and fail RED.
- Full suite still green otherwise: `.venv/bin/pytest tests/unit/ -q` (existing tests unaffected).
</verification>

<success_criteria>
alpha_strategy_scores and gate_evaluations exist; alpha.scoring.min_sharpe/max_drawdown_ratio/min_ic_alpha_score_corr seeded; changed_by matches the actual migration number; four Wave 0 scaffolds (including dry-run + payload-shape stubs) collectable and RED.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/148-alpha-scoring-system/148-01-SUMMARY.md` when done.
</output>
