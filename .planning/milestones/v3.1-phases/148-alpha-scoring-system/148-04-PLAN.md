---
phase: 148
plan: 04
type: execute
wave: 2
depends_on: [148-01]
files_modified:
  - scripts/analysis/score03_gate2_execution_eval.py
  - tests/unit/test_score03_gate2_execution_eval.py
autonomous: true
requirements: [SCORE-03]

must_haves:
  truths:
    - "Gate 2 scorer queries alpha_frames weight_epoch='143.1-08-champion' only (no challenger comparison)"
    - "Gate 2 scorer always pairs the pooled verdict with a regime-stratified companion via evaluate_frame_gate group_key=(direction, regime) (D-07: a pooled verdict must never stand alone)"
    - "Gate 2 verdict is written to gate_evaluations under a single gate_id 'gate2_execution' that also serves as FRAME-04 (D-08: SCORE-03 and FRAME-04 are the same gate, no second FRAME-04 gate_id)"
    - "The 143.1-08 champion numbers are adopted (cited), not recomputed from a different population (D-06)"
    - "All 5 SHADOW-REVIEW criteria are evaluated: c1-c4 plus criterion 5 via the documented c7_confident_loss operational proxy (the literal last_20d/full-period IC-Sharpe cliff is N/A on the champion population -- no recurring ensemble_ic_engine cadence exists to form the trailing-vs-full split)"
    - "Gate 2 c3 Sharpe and c4 max-drawdown verdicts read their thresholds from alpha.scoring.min_sharpe / alpha.scoring.max_drawdown_ratio APR keys at the call site (not hardcoded 0.5/0.25 literals), so a /config/parameters edit actually moves the verdict"
    - "Gate 2 scorer supports --dry-run: full computation, printed would-be verdict, ZERO writes to gate_evaluations and ZERO look-log append"
    - "The real (non-dry-run) gate write is atomic: within one DB transaction it re-asserts no prior gate2_execution row then INSERTs, with a pre-run snapshot captured into evidence + look-log"
    - "A statistical FAIL outcome writes a normal result='fail' row -- it is NOT raised as an exception"
  artifacts:
    - path: "scripts/analysis/score03_gate2_execution_eval.py"
      provides: "Standalone one-shot OOS Gate 2 (execution proof) scorer with --dry-run escape hatch, == FRAME-04"
      contains: "gate2_execution"
      min_lines: 80
    - path: "tests/unit/test_score03_gate2_execution_eval.py"
      provides: "GREEN SCORE-03 unit tests (champion numbers + regime-stratified companion + dry-run + exact evidence payload shape)"
  key_links:
    - from: "score03_gate2_execution_eval.py"
      to: "services.counterfactual_tracker.evaluate_frame_gate"
      via: "regime-stratified companion, 2-tuple group_key=(direction, regime)"
      pattern: "group_key=lambda row: \\(row\\[.direction.\\], row\\[.regime.\\]\\)"
    - from: "score03_gate2_execution_eval.py"
      to: "gate_evaluations"
      via: "atomic single-row INSERT gate_id='gate2_execution' within one transaction"
      pattern: "gate2_execution"
---

<objective>
Build the SCORE-03 OOS Gate 2 (execution proof) scorer: a standalone one-shot script that
answers whether the frame simulation captures signal as real P&L out-of-sample. Per D-08 this
IS FRAME-04, not a separate gate. Per D-06 it ADOPTS the champion's already-computed 143.1-08
numbers rather than re-deriving them theatrically, and per D-07 it MUST pair the pooled
verdict with a regime-stratified companion breakdown (the exact machinery todo 165 built and
reviewed for this purpose). It is structurally a slimmed, persistence-adding version of
phase143_1_08_shadow_validation.py's evaluate_epoch, champion-only, minus the challenger/A-B
logic. This plan BUILDS and tests the script; the actual one-shot run is in plan 148-05.

Purpose: Gate 2 answers "does the signal survive execution." Output: script plus GREEN tests.
</objective>

<review_revisions>
Revised from cross-AI review (148-REVIEWS.md). Changes traceable to specific findings:
- Consensus Concern 1 ("evaluate_frame_gate group-key reuse is structurally risky" -- Antigravity
  HIGH on 148-04's `(direction, regime)` mapping): the (direction, regime) key IS the blessed
  2-tuple todo-165 pattern the helper's docstring explicitly supports (verified line ~954: the
  helper unpacks exactly two elements, and (direction, regime) is a 2-tuple, so it round-trips).
  A new verify-first step (Task 1 STEP 0) forces the executor to confirm this round-trip in
  isolation BEFORE the full integration, so no surprise mid-build.
- Consensus Concern 2 ("irreversible runs lack a pre-run snapshot and safe-retry story"): the real
  gate write is now wrapped in a single DB transaction (re-assert count=0 THEN INSERT atomically),
  with a pre-run snapshot (APR values used, input-population row count, oos_start, champion epoch,
  fetch_sql_sha256) captured into BOTH the evidence jsonb and the look-log entry.
- Consensus Concern 3 ("validation lighter than the irreversible stakes warrant"): added a
  `--dry-run` flag (full compute, print would-be verdict, no gate_evaluations write, no look-log
  append) and an integration-style test asserting the EXACT evidence JSONB key set.
- Secondary (Antigravity MEDIUM on 148-05): a statistical FAIL is an EXPECTED result row
  (result='fail'), never an exception. On this champion population the pooled verdict is a
  KNOWN 3-of-5-criteria FAIL (D-06) -- that must land as a normal row, not a crash.
- Divergent view weighed (Codex HIGH, criterion-5 proxy): Codex flagged that criterion 5 is an
  operational proxy (c7_confident_loss) rather than the literal IC-Sharpe cliff. This was CONSIDERED
  and INTENTIONALLY RETAINED: it is a pre-registered, already-disclosed design decision (148-CONTEXT
  D-06), not new information from this review. The proxy is explicitly LABELED as a proxy in the
  evidence (c5_proxy_note) so it can never be mistaken for the literal criterion -- not silently
  dropped, not silently substituted.
The champion-only scope, all-5-criteria evaluation, APR-driven c3/c4 thresholds, mandatory
regime-stratified companion, single gate2_execution gate_id (==FRAME-04), and wave/dependency
structure are unchanged.
</review_revisions>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v3.1-phases/148-alpha-scoring-system/148-CONTEXT.md
@.planning/milestones/v3.1-phases/148-alpha-scoring-system/148-RESEARCH.md
@.planning/milestones/v3.1-phases/148-alpha-scoring-system/148-PATTERNS.md

<interfaces>
Reuse targets (import or copy-with-citation, do not rederive):

From services/counterfactual_tracker.py:
  _DEFAULT_BOOTSTRAP_RANDOM_STATE
  frame_gate_passes(pnl_r, cluster_ids, min_n, bootstrap_max_n, bootstrap_batch,
    bootstrap_random_state) -> (passes, ci_lower, ci_upper)
  evaluate_frame_gate(rows, min_n, bootstrap_max_n, bootstrap_batch, bootstrap_random_state,
    group_key=None, min_clusters=None) -> list[dict] (cell dicts with coverage in
    {"evaluated","insufficient"}); only coverage=="evaluated" cells count toward the verdict.
    SHAPE CONSTRAINT (verified line ~954): group_key MUST return a 2-TUPLE -- the helper does
    `for (dim_a, dim_b), bucket in groups.items():`. (direction, regime) is a valid 2-tuple and
    round-trips: the returned dict's "tf" field carries direction (dim_a), "regime" carries regime
    (dim_b). Read direction from verdict["tf"] and regime from verdict["regime"] when reassembling.

From scripts/analysis/phase143_1_08_shadow_validation.py:
  _max_drawdown(pnl_r_ordered) -> (dd|None, fails: bool)  (WR-03 frozen edge: peak<=0 fails)
  _annualized_sharpe(pnl_r_ordered, bar_ts_ordered) -> float|None
  _OOS_QUERY_SQL (lines 32-41), _load_apr (lines 71-84), evaluate_epoch (lines 87-166)
  CAVEAT: _load_apr returns only (min_n, bootstrap_max_n, bootstrap_batch, bootstrap_random_state,
    regime_gate_min_clusters) -- it does NOT extract the gate thresholds. This plan MUST extend the
    config load (it already fetches every alpha.scoring.% key into apr_cfg) to also pull
    alpha.scoring.min_sharpe and alpha.scoring.max_drawdown_ratio and thread them into c3/c4 at the
    call site. Do NOT reuse the 5-tuple verbatim and then hardcode 0.5/0.25.
  CAVEAT: evaluate_epoch already computes c7_confident_loss / c7_regime_stratified_no_confident_loss
    (short-side confident-loss tail check). KEEP this logic -- it is adopted here as criterion 5s
    operational proxy. Only the challenger/A-B and c6 non-regression logic is dropped.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build score03_gate2_execution_eval.py (with group-key verify-first, --dry-run, atomic write + snapshot) and turn test_score03_gate2_execution_eval.py GREEN</name>
  <files>scripts/analysis/score03_gate2_execution_eval.py, tests/unit/test_score03_gate2_execution_eval.py</files>
  <read_first>
    - scripts/analysis/phase143_1_08_shadow_validation.py (full, 257 lines; especially evaluate_epoch lines 87-166, _max_drawdown/_annualized_sharpe lines 44-68, _load_apr lines 71-84, _OOS_QUERY_SQL lines 32-41, verdict-print lines 228-253)
    - services/counterfactual_tracker.py -- MANDATORY: read evaluate_frame_gate INCLUDING the `for (dim_a, dim_b), bucket in groups.items():` unpack line (~954) and the docstring note that (direction, regime) is the todo-165 2-tuple pattern; also frame_gate_passes, _DEFAULT_BOOTSTRAP_RANDOM_STATE
    - docs/plans/SHADOW-REVIEW.md (the frozen 5 criteria: min sample, mean P&L CI >0, Sharpe >0.5, max drawdown <0.25, no IC-Sharpe cliff)
    - .planning/milestones/v3.1-phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-08-SHADOW-VALIDATION.md sections 6 and 7 (the champion numbers to cite verbatim)
    - tests/unit/test_score03_gate2_execution_eval.py (the Wave 0 stub to fill)
    - docs/plans/OOS-EVAL-PROTOCOL.md (run-once cadence, fail-loud oos_start)
  </read_first>
  <behavior>
    - Champion-only: query alpha_frames with weight_epoch='143.1-08-champion', frame_variant='primary', status!='open', bar_ts>=oos_start, counterfactual_pnl_r NOT NULL. No challenger epoch, no A/B comparison (that was the 143.1-08 decision, not this gate).
    - Statistical FAIL is NOT an error (Antigravity MEDIUM): the KNOWN pooled outcome on this champion population is a 3-of-5-criteria FAIL (D-06). A 'fail' verdict is a NORMAL result value that writes a result='fail' row. Do NOT raise for a statistical failure; reserve exceptions for genuine system faults (DB unreachable, unset oos_start).
    - Pooled verdict, all 5 SHADOW-REVIEW criteria (no criterion silently dropped):
      c1 = n_distinct_days >= 60; c2 = frame_gate_passes(...) CI lower > 0; c3 = sharpe is not None AND sharpe > min_sharpe (min_sharpe from alpha.scoring.min_sharpe APR, default 0.5); c4 = dd is not None AND dd < max_drawdown_ratio (max_drawdown_ratio from alpha.scoring.max_drawdown_ratio APR, default 0.25; dd is the ratio returned by _max_drawdown -- dd is None means the WR-03 peak<=0 frozen edge and fails outright); c5 = the criterion-5 operational proxy via c7_confident_loss (short-side confident-loss tail: fails iff n_short>0 and the short-side bootstrap CI upper < 0).
    - Criterion 5 rationale (cite in evidence, LABEL as proxy -- Codex HIGH weighed and retained per D-06): the literal criterion 5 (last_20d_IC_Sharpe / full_period_IC_Sharpe >= 0.5) is N/A on this champion population -- no recurring ensemble_ic_engine cadence exists to form a trailing-vs-full-period split (same reason phase143_1_08_shadow_validation.pys docstring documents). c7_confident_loss is therefore adopted as its documented operational proxy and LABELED as such (c5_proxy_note) in the evidence, NOT silently dropped, NOT silently substituted for the literal criterion.
    - APR-driven thresholds: read c3/c4 thresholds from the alpha.scoring.* namespace and apply them at the CALL SITE against the frozen functions returned values. Do NOT edit the frozen _annualized_sharpe/_max_drawdown bodies (WR-03 edges) and do NOT reuse _max_drawdowns baked-in `dd_fails` flag for c4 -- recompute c4_passes = dd is not None and dd < max_drawdown_ratio so the APR key actually governs the verdict.
    - Regime-stratified companion (MANDATORY, D-07): evaluate_frame_gate(rows, min_n=1, ..., group_key=lambda row: (row["direction"], row["regime"]), min_clusters=alpha.validation.regime_gate_min_clusters). Only coverage=="evaluated" cells count; report insufficient-coverage cells explicitly, never silently drop them, never emit the pooled verdict without the regime table in the same output/row.
    - Adopt, do not recompute: the evidence JSON and any printed summary cite 143.1-08-SHADOW-VALIDATION.md sec 6/7 numbers; do not silently re-derive them from a different population (D-06).
    - Single gate_id: write ONE gate_evaluations row gate_id='gate2_execution' which also satisfies FRAME-04 (D-08); do NOT create a second 'FRAME-04' gate_id.
    - --dry-run: runs the FULL computation (pooled c1-c5 + regime companion + evidence + snapshot assembly) and PRINTS the would-be verdict/evidence, but performs ZERO writes to gate_evaluations and ZERO append to the look-log. Emit a "[DRY-RUN] no rows written, look-log untouched" line. This lets a developer verify script correctness (including that the known 3-of-5 numbers reproduce) WITHOUT consuming the one-shot gate.
    - Atomic real write (Consensus Concern 2): in the non-dry-run path, after computing the verdict, open a SINGLE asyncpg transaction that (1) re-asserts count(*) FROM gate_evaluations WHERE gate_id='gate2_execution' == 0 inside the transaction and aborts (rollback) if a prior row exists, then (2) INSERTs the one verdict row -- both in the same transaction. Append the look-log entry only AFTER commit.
    - Pre-run snapshot: before the irreversible write, capture {oos_start, weight_epoch, apr_values_used (min_sharpe/max_drawdown_ratio/regime_gate_min_clusters/bootstrap_*), input_population_row_count (COUNT of champion frames scored), fetch_sql_sha256}. Embed into evidence jsonb AND the look-log entry alongside the report sha256.
  </behavior>
  <action>
    STEP 0 (group-key verify-first, Consensus Concern 1 -- do BEFORE the full integration): open
    services/counterfactual_tracker.py, read the `for (dim_a, dim_b), bucket in groups.items():`
    unpack line and the verdict-dict builder. Confirm the helper takes a 2-tuple group_key and that
    the output dict's "tf"=dim_a, "regime"=dim_b. Then run a tiny isolated check in a scratch
    python -c (or throwaway pytest) feeding ~8 synthetic rows spanning 2 directions x 2 regimes
    through evaluate_frame_gate(rows, min_n=1, bootstrap_max_n=100, bootstrap_batch=50,
    bootstrap_random_state=42, group_key=lambda row: (row["direction"], row["regime"]),
    min_clusters=2) and verify each returned dict's "tf" field equals the direction you fed and
    "regime" equals the regime, and coverage flags behave. Only once confirmed do you wire the
    production call site. Do NOT discover the group-key shape mid-integration.

    Create scripts/analysis/score03_gate2_execution_eval.py adapting phase143_1_08_shadow_validation.py's evaluate_epoch minus the challenger and c6 logic. Import frame_gate_passes, evaluate_frame_gate, _DEFAULT_BOOTSTRAP_RANDOM_STATE from services.counterfactual_tracker. Reuse _max_drawdown and _annualized_sharpe by importing them from phase143_1_08_shadow_validation.py, or copy them verbatim with a source-citation comment; do NOT rederive these (they encode the WR-03 frozen edge cases).

    Query with _OOS_QUERY_SQL-shaped SQL bound to weight_epoch='143.1-08-champion'. Load config via the _load_apr pattern (lines 71-84) BUT extend it: the reused _load_apr returns only (min_n, bootstrap_max_n, bootstrap_batch, bootstrap_random_state, regime_gate_min_clusters) and never extracts the gate thresholds. Because it already fetches every alpha.scoring.% key into apr_cfg, additionally extract min_sharpe = _cfg(apr_cfg, "alpha.scoring.min_sharpe", 0.5) and max_drawdown_ratio = _cfg(apr_cfg, "alpha.scoring.max_drawdown_ratio", 0.25) from that same fetch (write a small extended loader, or unpack apr_cfg directly -- do NOT reuse the 5-tuple _load_apr verbatim and then hardcode 0.5/0.25). Also read alpha.validation.regime_gate_min_clusters. Fail loud if alpha.validation.oos_start is unset (raise; never default).

    Compute the pooled c1-c5 exactly as the behavior block specifies: c3_passes = sharpe is not None and sharpe > min_sharpe; c4_passes = dd is not None and dd < max_drawdown_ratio (dd from _max_drawdown; dd is None -> WR-03 peak<=0 frozen edge -> fail; do NOT use `not dd_fails`); c5 via the c7_confident_loss short-side tail logic reused verbatim from evaluate_epoch (compute short-side frame_gate_passes CI, confident_loss = n_short>0 and short_ci_upper < 0), surfaced and LABELED as the criterion-5 operational proxy. Then compute the regime-stratified companion with the exact call: evaluate_frame_gate(rows, min_n=1, bootstrap_max_n=..., bootstrap_batch=..., bootstrap_random_state=..., group_key=lambda row: (row["direction"], row["regime"]), min_clusters=regime_gate_min_clusters). Derive c2_regime_stratified_passes = all(cell passes) over coverage=="evaluated" cells, or None if none clear coverage.

    Assemble a pre-run snapshot dict and the evidence jsonb carrying: pooled {c1_min_60_days, c2_ci_lower, c2_ci_upper, c2_passes, c3_sharpe, c3_passes, c3_min_sharpe_threshold, c4_max_dd, c4_passes, c4_max_drawdown_ratio_threshold, c5_confident_loss, c5_passes, c5_proxy_note} -- where c3_min_sharpe_threshold/c4_max_drawdown_ratio_threshold record the actual APR values used and c5_proxy_note records that criterion 5 uses the c7_confident_loss operational proxy (literal IC-Sharpe cliff N/A on this population). Also carry c7_regime_stratified_no_confident_loss, the full regime_cells list (each with coverage), c2_regime_stratified_passes, the pre-run snapshot, an overall result in {'pass','fail'}, and a provenance field citing 143.1-08-SHADOW-VALIDATION.md sec 6/7.

    Write path split by mode:
      - If args.dry_run: print the assembled verdict + evidence (including pooled criteria, regime cells, snapshot) and RETURN without touching gate_evaluations or the look-log; emit "[DRY-RUN] no rows written, look-log untouched".
      - Else (real run): open ONE asyncpg transaction; SELECT count(*) FROM gate_evaluations WHERE gate_id='gate2_execution'; if > 0 rollback and exit with "already run (D-04)"; else INSERT the single verdict row (gate_id='gate2_execution', which also satisfies FRAME-04 per D-08 -- do NOT create a second 'FRAME-04' gate_id); COMMIT. After commit append ONE look-log entry (run_ts, epoch, report path + sha256, pre-run snapshot).

    Print cell-by-cell regime coverage; never print the pooled verdict without the regime table alongside. argparse: add `--dry-run` (store_true) alongside standard args; `except Exception as error:` for system faults only, `datetime.now(UTC)`, parameterized queries. Module docstring states: run at most once per milestone gate (D-04); use --dry-run for dev verification; this gate IS FRAME-04.

    Fill tests/unit/test_score03_gate2_execution_eval.py with FOUR tests:
      - test_cites_champion_143_1_08_numbers: assert the pooled evidence/report reflects the adopted champion numbers, the query binds weight_epoch='143.1-08-champion' (not a recomputation over a different epoch), the evidence carries all 5 criteria keys c1..c5 including the c5 confident-loss proxy AND the c5_proxy_note label, and that c3/c4 verdicts trace to the APR thresholds rather than baked-in literals -- e.g. construct evidence under a shifted alpha.scoring.min_sharpe and assert c3_passes flips accordingly.
      - test_regime_stratified_companion_required: assert the code path always produces a regime_cells breakdown alongside the pooled verdict via the (direction, regime) 2-tuple group_key -- feed rows and assert both pooled and regime keys are present in the returned/written evidence, and that a returned regime cell's direction/regime were correctly remapped from verdict["tf"]/verdict["regime"] (the group-key round-trip is asserted, not eyeballed).
      - test_dry_run_writes_nothing: drive the dry-run path over synthetic/mocked data; assert NO gate_evaluations INSERT and NO look-log append occur.
      - test_gate2_evidence_payload_shape (integration-style, Consensus Concern 3): call the evidence-assembly function on synthetic champion rows and assert the EXACT evidence key set -- pooled {c1..c5 keys incl c3_min_sharpe_threshold, c4_max_drawdown_ratio_threshold, c5_proxy_note}, regime_cells, c2_regime_stratified_passes, snapshot (with its {oos_start, weight_epoch, apr_values_used, input_population_row_count, fetch_sql_sha256}), result, and provenance -- asserting payload shape, not merely that a row exists.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_score03_gate2_execution_eval.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "143.1-08-champion" scripts/analysis/score03_gate2_execution_eval.py` >= 1
    - `grep -c "gate2_execution" scripts/analysis/score03_gate2_execution_eval.py` >= 1
    - `grep -c "FRAME-04" scripts/analysis/score03_gate2_execution_eval.py` matches only in comments/docstring, and there is NO `gate_id='FRAME-04'` or `gate_id = 'FRAME-04'` assignment (`grep -c "gate_id.*FRAME-04" ...` == 0)
    - `grep -Eq "group_key=lambda row: \(row\[.direction.\], row\[.regime.\]\)" scripts/analysis/score03_gate2_execution_eval.py` (2-tuple, verified round-trip)
    - `grep -c "from services.counterfactual_tracker import" scripts/analysis/score03_gate2_execution_eval.py` >= 1
    - `grep -c "scipy.stats.bootstrap" scripts/analysis/score03_gate2_execution_eval.py` == 0 (no new hand-rolled bootstrap)
    - `grep -c "as exc" scripts/analysis/score03_gate2_execution_eval.py` == 0
    - `grep -c "alpha.scoring.min_sharpe" scripts/analysis/score03_gate2_execution_eval.py` >= 1 (c3 threshold read from APR)
    - `grep -c "alpha.scoring.max_drawdown_ratio" scripts/analysis/score03_gate2_execution_eval.py` >= 1 (c4 threshold read from APR)
    - c4 verdict recomputed from the APR ratio at the call site, NOT the baked-in fail flag: `grep -Ec "not dd_fails" scripts/analysis/score03_gate2_execution_eval.py` == 0
    - c3 verdict uses the APR variable, no hardcoded Sharpe literal at the call site: `grep -Ec "sharpe > 0\.5" scripts/analysis/score03_gate2_execution_eval.py` == 0
    - Criterion 5 evaluated via the c7_confident_loss proxy AND labeled: `grep -Ec "confident_loss" scripts/analysis/score03_gate2_execution_eval.py` >= 1 and `grep -Ec "c5_proxy_note" scripts/analysis/score03_gate2_execution_eval.py` >= 1
    - `grep -Ec "dry.?run" scripts/analysis/score03_gate2_execution_eval.py` >= 1 (dry-run flag exists)
    - Atomic transactional write: `grep -Eic "transaction\(\)|BEGIN|async with .*transaction" scripts/analysis/score03_gate2_execution_eval.py` >= 1
    - Snapshot captured: `grep -Eic "snapshot" scripts/analysis/score03_gate2_execution_eval.py` >= 1 and `grep -Eic "sha256" scripts/analysis/score03_gate2_execution_eval.py` >= 1
    - `.venv/bin/pytest tests/unit/test_score03_gate2_execution_eval.py -x` passes (all four tests GREEN: champion numbers + APR thresholds, regime companion + round-trip, dry-run, exact payload shape)
  </acceptance_criteria>
  <done>SCORE-03 script built: champion-only, all 5 SHADOW-REVIEW criteria (c1-c4 plus criterion 5 via the labeled c7_confident_loss proxy), c3/c4 thresholds APR-driven at the call site, pooled + mandatory regime-stratified companion via a verified (direction, regime) 2-tuple, --dry-run escape hatch, atomic transactional write + pre-run snapshot, statistical-fail-is-a-row semantics, single gate2_execution gate_id (== FRAME-04), adopts champion numbers; four tests GREEN. Not yet run (that is plan 148-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI -> gate script -> DB | oos_start boundary governs gate integrity; alpha_frames read is trusted corpus data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-04 | Tampering / Repudiation | alpha.validation.oos_start boundary | mitigate | Fail-loud read raises on unset/empty; never default to MAX(bar_ts) |
| T-148-05 | Repudiation | pooled verdict masking regime-conditional edge | mitigate | Regime-stratified companion is non-optional; the code cannot emit a pooled verdict without the per-cell breakdown (D-07) |
| T-148-06 | Tampering | any CLI args | mitigate | Parameterized queries only; no string interpolation of epoch/symbol values |
| T-148-06b | Repudiation | one-shot gate integrity | mitigate | Atomic transactional check+INSERT (rollback on crash), pre-run snapshot in evidence + look-log so drift is diagnosable and the write is safely retryable |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_score03_gate2_execution_eval.py -x` GREEN (all four tests).
- Grep gates satisfied: champion epoch bound, single gate2_execution gate_id, regime-stratified companion present, no hand-rolled bootstrap, dry-run + transaction + snapshot present.
- `.venv/bin/pytest tests/unit/ -q` full suite green.
</verification>

<success_criteria>
Standalone OOS Gate 2 scorer exists, champion-only, all 5 SHADOW-REVIEW criteria evaluated
(c1-c4 plus criterion 5 via the labeled c7_confident_loss proxy), c3/c4 thresholds read from
the alpha.scoring.* APR keys at the call site, pooled + mandatory regime-stratified companion
via a verified (direction, regime) 2-tuple, --dry-run escape hatch, atomic transactional write
with a pre-run snapshot, statistical FAIL treated as a normal result row, single gate_id serving
both SCORE-03 and FRAME-04; tests GREEN. Not yet executed.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/148-alpha-scoring-system/148-04-SUMMARY.md` when done.
</output>
