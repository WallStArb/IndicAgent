---
phase: 171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix
plan: 05
type: execute
wave: 2
depends_on: ["171-01", "171-02", "171-03", "171-04"]
files_modified:
  - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-PILOT-GATE.md
  - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-05-seed-stability-pilot.json
  - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-05-n-restarts-comparison.json
  - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-05-provenance-report.json
autonomous: true
requirements: [REQ-3, REQ-5]

must_haves:
  truths:
    - "The D-03 comparison arm reads the retired full-history-fit labels from feature_vectors BEFORE the pilot NULL-out destroys them — the ordering is enforced, not hoped for"
    - "_hmm_seed_stability_check has a real-corpus verdict for every pilot cell with sufficient history, and every skipped cell carries an arithmetic reason, closing REQ-5's invoked-nowhere status"
    - "After the pilot relabel, every pilot (symbol, tf) cell carries exactly one labeling method's output, proven by the checked-in provenance verifier, not asserted"
    - "The production config value alpha.hmm.walk_forward.enabled is still false after the pilot — the pilot ran via the CLI override, never a config flip"
    - "An explicit written GO or NO-GO verdict exists with per-criterion evidence, and the criteria are mechanical-correctness criteria, never IC performance"
    - "A NO-GO leaves the corpus in one uniform labeling method, not half-migrated"
  artifacts:
    - path: ".planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-PILOT-GATE.md"
      provides: "D-01's staged go/no-go decision with per-criterion evidence and the D-03 n_restarts verdict"
      contains: "PILOT VERDICT"
    - path: ".planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-05-provenance-report.json"
      provides: "REQ-3 per-cell provenance proof for the pilot scope"
      contains: "verdict"
    - path: ".planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-05-seed-stability-pilot.json"
      provides: "REQ-5's first real-data exercise of _hmm_seed_stability_check"
      contains: "min_pairwise_agreement"
  key_links:
    - from: ".planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-PILOT-GATE.md"
      to: "cache/hmm_n_restarts_comparison_results.json"
      via: "copied evidence file read for the D-03 attribution verdict"
      pattern: "171-05-n-restarts-comparison.json"
    - from: "services/regime_writer.py --walk-forward"
      to: "services/regime_writer.py::_compute_symbol_tf_walk_forward"
      via: "the 171-01 Task 2 CLI override, never a config_state mutation"
      pattern: "--walk-forward"
---

<objective>
Run D-01's staged pilot on 8 symbols across 4 timeframes and render an explicit, evidence-backed
GO/NO-GO decision before the full-corpus blast radius is sunk.

Purpose: CONTEXT.md D-01 requires a staged pilot with an explicit gate before committing the full
231-symbol x 4-tf universe, and names the pilot as the first live run of `_hmm_seed_stability_check`
(REQ-5). This plan runs the two diagnostics plan 171-04 built, executes the pilot-scope NULL-out +
walk-forward relabel with the tool plan 171-03 built and the CLI override plan 171-01 built, and
writes the verdict.

**The gate is a mechanical-correctness gate, not a performance gate.** The ROADMAP is explicit
that this phase ships regardless of the Gate 4 ordinal-IC pilot's negative result — a weak or
negative IC number is an expected, already-known finding and is NEVER grounds for NO-GO. The gate
exists to catch a degenerate cell, a broken provenance post-condition, or an unexplained coverage
hole before 231 symbols are relabeled.

Output: `171-PILOT-GATE.md` plus three copied evidence JSONs, and 8 pilot symbols carrying
proven single-method walk-forward labels (on GO) or restored to the corpus's uniform pre-pilot
method (on NO-GO).
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-CONTEXT.md
@.planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-RESEARCH.md
@.planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-VALIDATION.md
@CLAUDE.md
</context>

<interfaces>
Tooling this plan consumes (all built by wave 1, verified signatures at plan time):

- `scripts/analysis/hmm_walk_forward_seed_stability_pilot.py` (171-04 Task 1) —
  `--symbols`, `--tf`, `--seeds`, `--boundaries`. Writes
  `cache/hmm_seed_stability_pilot_results.json`. Read-only. Gate: FAIL when
  `min_pairwise_agreement <= 0.25`.
- `scripts/analysis/hmm_n_restarts_walk_forward_comparison_pilot.py` (171-04 Task 2) —
  `--symbols`, `--tf`, `--n-restarts` (default 3), `--block-size`, `--n-boot`, `--seed`.
  Writes `cache/hmm_n_restarts_comparison_results.json`. Read-only. **Reads
  `feature_vectors.regime` as its `prod_labels` arm** — this is the ordering constraint below.
- `scripts/ops/corpus/ops_regime_null_out_and_verify.py` (171-03) —
  `--symbols` (REQUIRED), `--tf`, `--manifest`, `--dry-run`,
  `--mode {null-out,verify-post-null,verify-post-relabel}`. Writes
  `cache/regime_relabel_provenance_report.json`. Prints `REQ-3 PROVENANCE: PASS|FAIL`.
- `services/regime_writer.py` — `--symbols`, `--tf`, `--workers`, `--refit`, and the 171-01
  Task 2 additions `--walk-forward` / `--no-walk-forward` (in-memory override of the APR key
  `alpha.hmm.walk_forward.enabled`; never writes `config_state`).

Live values read at plan time (re-read at execution time, do not trust this snapshot):
- `alpha.hmm.walk_forward.enabled = false`, `alpha.hmm.n_restarts = 1`,
  `alpha.hmm.random_state = 42`, `infra.regime_writer.workers = 12`.
- `feature_vectors` currently holds 80 distinct symbols per tf (the corpus is smaller than the
  231-instrument universe; todo 259's backfill is still expanding it).

Pilot scope (fixed for this plan, matching 171-04's `_PILOT_CELLS` default):
`SPY IWM TLT GLD XLE EEM FXY SMH` x `1d 1h 15m 5m` = 32 cells.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pre-NULL-out diagnostics — seed stability (REQ-5) and the D-03 comparison arm</name>
  <files>.planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-05-seed-stability-pilot.json, .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-05-n-restarts-comparison.json</files>
  <read_first>
    - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-04-PLAN.md
      (both tasks — the exact CLI surface, output JSON schema, and the stated gate semantics of
      the two scripts this task runs; in particular Task 2's `prod_labels` arm and the sentence
      "which is exactly why plan 171-05 sequences this script BEFORE the NULL-out")
    - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-04-SUMMARY.md
      (what actually shipped versus what the plan specified — the CLI flags may have been
      adjusted during execution; trust the SUMMARY over the PLAN where they differ)
    - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-CONTEXT.md
      D-01 and D-03 (the staged-pilot requirement and the attribution safeguard)
    - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-RESEARCH.md
      Pitfall 3 (walk-forward runs ~20 HMM fits per cell where the single-fit path runs 1 — this
      is what makes the 5m cells expensive)
  </read_first>
  <action>
**Ordering constraint — the single most important instruction in this plan.** Both script runs in
this task MUST complete BEFORE Task 2's NULL-out. `hmm_n_restarts_walk_forward_comparison_pilot.py`
fetches the existing `feature_vectors.regime` values as its `prod_labels` comparison arm. Those
values are the retired full-history-fit method's output and exist ONLY until Task 2 NULLs them.
Running Task 2 first permanently destroys the baseline D-03's attribution safeguard compares
against, and it cannot be reconstructed from the walk-forward path. If this task has not produced
both JSON files, Task 2 must not start.

Run tf-staged, cheapest first, so a degenerate result surfaces before the expensive timeframes are
paid for. Stage A is `1d 1h`; Stage B is `15m 5m`. Review Stage A's output before launching
Stage B.

Seed stability (REQ-5), Stage A:
`.venv/bin/python scripts/analysis/hmm_walk_forward_seed_stability_pilot.py --symbols SPY IWM TLT GLD XLE EEM FXY SMH --tf 1d 1h 2>&1 | tee logs/171_05_seed_stability_stage_a.log`

Then Stage B with `--tf 15m 5m`, tee'd to `logs/171_05_seed_stability_stage_b.log`. The 5m cells
fit HMMs on prefixes up to roughly 390k rows across 3 seeds x 3 boundaries; if a stage is going to
run for hours, launch it with `nohup ... &` and poll the log rather than blocking. If the script's
own JSON output is overwritten between stages, merge the per-cell records rather than letting
Stage B clobber Stage A — check the script's write mode before launching Stage B and, if it
truncates, copy Stage A's JSON aside first and merge afterward.

D-03 comparison (same staging):
`.venv/bin/python scripts/analysis/hmm_n_restarts_walk_forward_comparison_pilot.py --symbols SPY IWM TLT GLD XLE EEM FXY SMH --tf 1d 1h --n-restarts 3 2>&1 | tee logs/171_05_n_restarts_stage_a.log`
then the `--tf 15m 5m` stage. Keep `--n-restarts 3`, matching 171-04's default and todo 108's
deterministic seed derivation (`alpha.hmm.random_state + i` = 42, 43, 44). Do not lower `--n-boot`
below the script's default for the real run — the reduced value in 171-04's own acceptance
criteria was a smoke-test setting only.

Do NOT run any other `ic_engine`, `regime_writer`, or `backfill_feature_factory` process
concurrently with these. Per CLAUDE.md's contention gotcha, check `ps aux | grep -E "ic_engine|infrastructure_run_historical_pipeline|regime_writer|backfill_feature_factory"`
first and record what was running. These two scripts are read-only, so a concurrent read-only job
is tolerable; a concurrent WRITER against `feature_vectors` is not, because it would change the
`prod_labels` arm underneath the comparison.

Copy both result files into the phase evidence directory under stable names so a later `cache/`
overwrite cannot destroy this plan's evidence:
`mkdir -p .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence`
then copy `cache/hmm_seed_stability_pilot_results.json` to
`evidence/171-05-seed-stability-pilot.json` and `cache/hmm_n_restarts_comparison_results.json` to
`evidence/171-05-n-restarts-comparison.json`.

For every cell the seed-stability script reports as skipped, verify the skip arithmetically rather
than accepting it: read that cell's `n_obs` from the JSON and confirm
`n_obs < initial_warmup_bars + n_components` using that tf's live
`alpha.hmm.walk_forward.initial_warmup_bars.<tf>` value (1d=504, 1h=3300, 15m=13200, 5m=39600)
and `feature.hmm.n_components` (5). A skip that does not satisfy this inequality is an unexplained
coverage hole and must be reported, not swallowed — it is gate criterion G3 in Task 3.
  </action>
  <verify>
    <automated>test -s .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-05-seed-stability-pilot.json && test -s .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-05-n-restarts-comparison.json</automated>
  </verify>
  <acceptance_criteria>
    - Both evidence JSONs exist, are non-empty, and pass a required-key SHAPE check, not merely
      a bare `json.load()`. A bare parse would accept a file a script left half-written after
      crashing between opening the output file and writing its results — the file parses, the
      required field is simply absent, and the gap only surfaces in Task 3 as a missing citation.
      Write a one-liner
      (`.venv/bin/python -c "import json,glob; ..."`) asserting, against the real schema recorded
      in `171-04-SUMMARY.md` (trust the SUMMARY over the PLAN where they differ):
      `171-05-seed-stability-pilot.json` contains at least one per-cell record and every record
      carries a non-null `min_pairwise_agreement` OR an explicit non-null skip reason;
      `171-05-n-restarts-comparison.json` carries, per evaluated cell, a non-null
      walk-forward-vs-production block, a non-null n_restarts=1-vs-3 block, and a non-null
      `D-03 ATTRIBUTION` verdict. A missing or null required key FAILS this criterion — do not
      proceed to Task 2 with a malformed evidence file; re-run the producing script instead.
    - The seed-stability JSON contains a record for at least one cell of every one of the 8 pilot
      symbols, and every record carries a numeric `min_pairwise_agreement` or an explicit skip
      reason.
    - The n_restarts JSON contains, per evaluated cell, both a walk-forward-vs-production block
      and an n_restarts=1-vs-n_restarts=3 block (D-03's two independently attributable arms).
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM feature_vectors WHERE symbol IN ('SPY','IWM','TLT','GLD','XLE','EEM','FXY','SMH') AND regime IS NOT NULL"`
      returns a NON-ZERO count at the end of this task — proof the NULL-out has not run yet and
      the `prod_labels` arm was read against live baseline data.
    - Every skipped cell in the seed-stability JSON satisfies
      `n_obs < initial_warmup_bars + 5` for its tf, checked and recorded.
  </acceptance_criteria>
  <done>REQ-5's invoked-nowhere status is closed with real-corpus output, D-03's two arms are
  measured against the still-intact production baseline, and both result sets are preserved as
  phase evidence outside the volatile `cache/` directory.</done>
</task>

<task type="auto">
  <name>Task 2: Pilot-scope NULL-out and walk-forward relabel with provenance proof</name>
  <files>.planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-05-provenance-report.json</files>
  <read_first>
    - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-03-PLAN.md
      and 171-03-SUMMARY.md (the NULL-out/verify tool's real CLI surface, manifest semantics, and
      the meaning of the `pass` / `fail` / `no_labels` verdicts)
    - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-01-SUMMARY.md
      (confirm `--walk-forward` / `--no-walk-forward` shipped and how the effective value is
      logged as `walk_forward_source`)
    - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-RESEARCH.md
      "Critical Finding" (why the NULL-out is mandatory and not optional)
    - services/regime_writer.py lines 1440-1470 (`_discover_symbols` — the skip-fully-labeled
      behavior that makes an explicit `--symbols` list mandatory for every run in this phase)
    - CLAUDE.md, the ProcessPoolExecutor orphan-reaping gotcha (killing `regime_writer.py`'s main
      process does NOT reap its forkserver workers, which keep holding DB connections and writing)
  </read_first>
  <action>
Step 1 — back up the baseline before destroying it. The pilot cells' existing labels are the only
copy of the retired method's output for those cells. Dump them first:

`PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\copy (SELECT symbol, tf, bar_ts, regime, hmm_prob_trending_up, hmm_prob_ranging, hmm_prob_trending_down, hmm_regime_prob, hmm_entropy, hmm_duration, hmm_churn FROM feature_vectors WHERE symbol IN ('SPY','IWM','TLT','GLD','XLE','EEM','FXY','SMH') AND tf IN ('1d','1h','15m','5m') AND regime IS NOT NULL ORDER BY symbol, tf, bar_ts) TO PROGRAM 'gzip > cache/regime_pilot_baseline_backup.csv.gz' CSV HEADER"`

Record the row count written. This is CLAUDE.md's "never drop data that could contain signal"
applied literally, and it is what makes a NO-GO restoration exact rather than a recomputation
that merely ought to reproduce the same values.

Step 2 — dry run, then NULL-out. Use a pilot-specific manifest path so the pilot's
`verified_null` entries can never cause plan 171-06's full rollout to skip those same cells:

`.venv/bin/python scripts/ops/corpus/ops_regime_null_out_and_verify.py --symbols SPY IWM TLT GLD XLE EEM FXY SMH --tf 1d 1h 15m 5m --manifest cache/regime_null_out_manifest_pilot.json --dry-run`

Review the printed per-cell plan and `pre_null_labeled` counts against the backup row count from
Step 1, then re-run without `--dry-run`, tee'd to `logs/171_05_null_out.log`. Record each cell's
`rows_affected` and `elapsed_s` — these are the per-cell measurements that let plan 171-06 size
the full rollout from data rather than from a guess, per
`docs/foundation/performance-investigation-sop.md`.

Step 3 — relabel through the CLI override, never a config flip:

`.venv/bin/python services/regime_writer.py --symbols SPY IWM TLT GLD XLE EEM FXY SMH --tf 1d 1h --refit --walk-forward 2>&1 | tee logs/171_05_relabel_stage_a.log`

then the `--tf 15m 5m` stage. `--symbols` is mandatory on every invocation: `_discover_symbols()`
skips fully-labeled symbols, and after the NULL-out these cells are unlabeled so discovery would
technically find them — but relying on that is the exact trap RESEARCH.md documents, and an
explicit list is the only form that is correct in both states.

Confirm in each run's `regime_writer.starting` log line that `walk_forward_enabled=true` AND
`walk_forward_source=cli`. A `walk_forward_source=apr` here means the override did not take and
the run must be treated as suspect.

If a relabel run must be killed, follow CLAUDE.md's orphan-reaping rule exactly: kill the main
PID, then `ps aux | grep regime_writer.py | awk '{print $2}' | xargs -r kill`, and confirm zero
remain before restarting. Orphaned forkserver workers keep writing to `feature_vectors`.

Step 4 — prove provenance:

`.venv/bin/python scripts/ops/corpus/ops_regime_null_out_and_verify.py --symbols SPY IWM TLT GLD XLE EEM FXY SMH --tf 1d 1h 15m 5m --manifest cache/regime_null_out_manifest_pilot.json --mode verify-post-relabel 2>&1 | tee logs/171_05_provenance.log`

Copy `cache/regime_relabel_provenance_report.json` to
`evidence/171-05-provenance-report.json`. Every cell must be `pass` or `no_labels`; any `fail`
is gate criterion G1's blocking condition in Task 3.

Do NOT run `services/ic_engine.py` at any point in this plan. Between this task and plan 171-06,
the corpus is deliberately mixed at the (symbol, tf) grain — 8 symbols on walk-forward labels,
the rest on full-history labels. Provenance is still single-method WITHIN each cell, which is
what REQ-3 requires, but an `ic_engine` pass over that state would produce
`feature_ic_scores` rows measured against a half-migrated corpus, which is exactly the
mixed-vintage measurement this project has been burned by before.

Do NOT change `alpha.hmm.walk_forward.enabled`. Re-assert at the end of this task that it is
still `false`.
  </action>
  <verify>
    <automated>test -s .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-05-provenance-report.json && test -s cache/regime_pilot_baseline_backup.csv.gz && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT config_value FROM config_state WHERE config_key='alpha.hmm.walk_forward.enabled'" | grep -qx false</automated>
  </verify>
  <acceptance_criteria>
    - `cache/regime_pilot_baseline_backup.csv.gz` exists and its decompressed line count is within
      1 of the pre-NULL-out labeled row count recorded in Step 1
      (`zcat cache/regime_pilot_baseline_backup.csv.gz | wc -l`, allowing for the CSV header).
    - `logs/171_05_provenance.log` contains a `REQ-3 PROVENANCE:` line, and
      `evidence/171-05-provenance-report.json` carries a non-null `verdict` field for EVERY cell
      record (a record with a missing or null `verdict` is a malformed evidence file, not a pass)
      and contains zero records with `"verdict": "fail"` — or, if any `fail` exist, they are named
      explicitly in the Task 3 gate document as a G1 failure.
    - Every pilot cell reported `no_labels` is explained by the insufficient-history arithmetic
      (`n_obs < initial_warmup_bars + 5`) and that check is recorded.
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT config_value FROM config_state WHERE config_key='alpha.hmm.walk_forward.enabled'"`
      returns `false`.
    - `grep -c "walk_forward_source" logs/171_05_relabel_stage_a.log` is at least 1 and the value
      logged is `cli`.
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM feature_vectors WHERE symbol='SPY' AND tf='1d' AND regime IS NOT NULL"`
      returns a non-zero count strictly less than the pre-pilot count for that cell — the warmup
      prefix is now genuinely unlabeled.
    - No `services/ic_engine.py` process was started during this plan
      (`ps aux | grep ic_engine | grep -v grep` empty, recorded).
  </acceptance_criteria>
  <done>The 8 pilot symbols carry walk-forward labels with per-cell provenance proven by the
  checked-in verifier, the retired method's output for those cells is preserved on disk, and
  production config was never touched.</done>
</task>

<task type="auto">
  <name>Task 3: Render the D-01 go/no-go verdict</name>
  <files>.planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-PILOT-GATE.md</files>
  <read_first>
    - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-05-seed-stability-pilot.json
    - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-05-n-restarts-comparison.json
    - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/evidence/171-05-provenance-report.json
    - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-CONTEXT.md
      D-00 (the governing principle: empirical verification over verification-by-inspection),
      D-01 (the gate itself), D-03 (n_restarts stays at its current value unless the pilot's
      parallel-arm comparison actually shows otherwise)
    - .planning/ROADMAP.md Phase 171 goal paragraph — specifically "This ships regardless of the
      Gate 4 ordinal-IC pilot's negative result... the pilot measures the fix's downstream
      effects, it does not gate whether to do the fix"
  </read_first>
  <action>
Write `171-PILOT-GATE.md` in the phase directory. Evaluate exactly these five criteria against the
three evidence JSONs. Every criterion's verdict must cite the specific number or cell name it was
derived from — a bare PASS with no cited value is a D-00 violation.

**If a required field is missing or null in any evidence JSON, this task MUST abort and report
the malformed file — never estimate, omit, paraphrase, or substitute a value for the missing
citation.** The required fields are the ones Task 1's shape check enumerates
(`min_pairwise_agreement` or an explicit skip reason per seed-stability cell; both verdict blocks
plus the `D-03 ATTRIBUTION` line in the n_restarts JSON; a `verdict` per cell in the provenance
report). Filling a gap with a plausible-looking number is precisely the verification-by-inspection
D-00 forbids, and it would silently invalidate whichever criterion rests on it. The correct
response is to stop, name the file and the absent key, and re-run the script that produced it.

**G1 — Provenance (BLOCKING).** Zero cells in `171-05-provenance-report.json` carry
`"verdict": "fail"`. Any `fail` is NO-GO: it means either the NULL-out did not cover a cell or a
stale pre-fix value survived in the warmup prefix, and shipping that at 231-symbol scale
reproduces the exact mixed-provenance failure class RESEARCH.md's Critical Finding documents.

**G2 — Seed stability (BLOCKING above a threshold).** Count cells whose
`min_pairwise_agreement <= 0.25` at any sampled boundary. That threshold is the ~21.7-22.1%
five-label chance baseline migration 292's own descriptions record — at or below it the labels are
seed noise, not a regime measurement. Rule: 0 failing cells = PASS. Exactly 1 failing cell = PASS
WITH QUARANTINE, and that cell must be named in this document so plan 171-06 can exclude it and
report it rather than silently relabel it. 2 or more failing cells = NO-GO, because that pattern
is systemic degeneracy rather than one bad symbol, and it must be understood before the full
blast radius is sunk. Report the corpus-wide MINIMUM `min_pairwise_agreement` and the cell that
produced it as the headline number, never an average.

**G3 — Coverage (BLOCKING).** Every one of the 32 pilot cells is accounted for as either
relabeled-and-passing, or explicitly skipped with the insufficient-history arithmetic
(`n_obs < initial_warmup_bars + 5`) satisfied and shown. An unexplained `no_labels` or an
unexplained skip is NO-GO — an unexplained coverage hole at 32 cells becomes an invisible one at
924 cells.

**G4 — Runtime sizing (REPORT-ONLY, no pass/fail).** From the per-cell `elapsed_s` values in the
NULL-out manifest and the relabel logs, extrapolate the full-rollout wall-clock for the corpus
scope as it exists at execution time (derive the cell count from
`SELECT tf, count(DISTINCT symbol) FROM feature_vectors GROUP BY tf`, which was 80 symbols per tf
at plan time, not the 231-instrument universe). State the number explicitly. There is no
threshold; the failure mode this criterion exists to prevent is launching a multi-day job with no
estimate at all, per RESEARCH.md Pitfall 3's warning that walk-forward runs ~20 HMM fits per cell
where the single-fit path runs 1.

**G5 — D-03 n_restarts attribution (DECIDES A VALUE, DOES NOT GATE THE ROLLOUT).** Read the
`D-03 ATTRIBUTION` verdict from `171-05-n-restarts-comparison.json`. Per 171-04's stated gate, the
multi-restart arm is preferred only when it is `a_significantly_better` on a strict majority of
evaluated cells; anything else is INCONCLUSIVE and `alpha.hmm.n_restarts` stays at 1. State the
resulting value plan 171-06 must use. This criterion NEVER produces a NO-GO — it selects a
parameter for the rollout, nothing more.

**Explicitly NOT a criterion: IC magnitude, IC sign, or label agreement with the production
baseline.** The walk-forward-vs-production IC block in `171-05-n-restarts-comparison.json` is
recorded as evidence and interpreted in prose, but a weak, negative, or non-significant IC is the
already-known Gate 4 result and is not grounds for NO-GO. The ROADMAP settled this on 2026-08-04:
this is repair of a confirmed causal-law violation, and causal bugs get fixed regardless of
measured benefit. If an executor finds itself reasoning toward NO-GO from an IC number, that
reasoning is out of scope for this gate.

End the document with a single unambiguous line:
`PILOT VERDICT: GO` or `PILOT VERDICT: NO-GO`, followed by, on GO, the exact
`alpha.hmm.n_restarts` value and any quarantined cell names plan 171-06 must carry forward; or,
on NO-GO, the specific criterion that failed and the concrete investigation it requires.

**On NO-GO, restore the corpus to one uniform method before finishing this task.** Leaving 8
symbols on walk-forward labels while 72 remain on full-history labels is a half-migrated corpus
that no downstream consumer can interpret. Restore by re-running the NULL-out for the pilot scope
and then `services/regime_writer.py --symbols <pilot 8> --tf 1d 1h 15m 5m --refit --no-walk-forward`,
then re-running `--mode verify-post-relabel` to confirm the restored state, and spot-check the
restored labels against `cache/regime_pilot_baseline_backup.csv.gz` for at least one cell to
confirm the full-history fit is reproducible. Record the restoration in this document.

On GO, do NOT flip `alpha.hmm.walk_forward.enabled` here — that flip belongs to plan 171-06,
gated behind its own blocking precondition check.
  </action>
  <verify>
    <automated>grep -Eq "^PILOT VERDICT: (GO|NO-GO)$" .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-PILOT-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `171-PILOT-GATE.md` contains exactly one line matching `^PILOT VERDICT: (GO|NO-GO)$`.
    - The document contains a per-criterion section for G1 through G5, and each of G1/G2/G3 cites
      at least one concrete number or cell name from the evidence JSONs.
    - The document states the corpus-wide MINIMUM `min_pairwise_agreement` and names the cell that
      produced it.
    - The document states an explicit `alpha.hmm.n_restarts` value for plan 171-06 to use.
    - The document states an explicit extrapolated full-rollout wall-clock estimate and the cell
      count it was derived from.
    - `grep -c "does not gate\|not grounds for NO-GO" 171-PILOT-GATE.md` is at least 1 — the
      IC-is-not-a-gate framing is recorded, so a later reader cannot mistake the evidence for a
      performance verdict.
    - On NO-GO only: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(DISTINCT method_marker) FROM (SELECT symbol FROM feature_vectors WHERE regime IS NOT NULL GROUP BY symbol) s, LATERAL (SELECT 1 AS method_marker) m"`
      is not a meaningful uniformity test — instead, confirm restoration by re-running
      `--mode verify-post-relabel` and recording that every pilot cell now reports the restored
      state, plus a spot-check of one cell against the backup CSV.
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT config_value FROM config_state WHERE config_key='alpha.hmm.walk_forward.enabled'"`
      still returns `false`.
  </acceptance_criteria>
  <done>D-01's gate has an explicit written verdict backed by cited evidence, the n_restarts value
  for the rollout is decided empirically rather than assumed, and the corpus is in a coherent
  state whichever way the verdict went.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None in the ASVS sense. Per RESEARCH.md's Security Domain section this phase is internal
batch-compute correctness work: no network-facing surface, no authentication/session/access-control
boundary, no untrusted input. No ASVS L1 category applies. The genuine risk class is data
integrity and measurement validity, below.

## Correctness Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-19 | Irreversible data loss (destroy-before-read) | `feature_vectors.regime` pilot cells | mitigate | Task 1's diagnostics must complete before Task 2's NULL-out; enforced by an acceptance criterion asserting a non-zero labeled row count at the END of Task 1, plus a gzip backup of the pilot cells' baseline before any UPDATE |
| T-171-20 | Silent production config mutation | `alpha.hmm.walk_forward.enabled` | mitigate | Pilot runs via the 171-01 `--walk-forward` in-memory override; `walk_forward_source=cli` asserted in the run log and the config value re-asserted `false` at three separate points |
| T-171-21 | Cross-plan manifest contamination | `cache/regime_null_out_manifest.json` | mitigate | Pilot uses `--manifest cache/regime_null_out_manifest_pilot.json` so its `verified_null` entries can never cause plan 171-06's full rollout to skip the same cells |
| T-171-22 | Evidence loss to cache overwrite | `cache/*.json` result files | mitigate | All three result files copied into the phase `evidence/` directory under stable names before the gate is rendered |
| T-171-23 | Scope creep of the gate into a performance verdict | D-01 go/no-go | mitigate | Gate criteria are enumerated exhaustively (G1-G5) and IC magnitude is explicitly excluded in writing, with a grep-enforced acceptance criterion; the ROADMAP's ships-regardless decision is cited inline |
| T-171-24 | Half-migrated corpus | `feature_vectors` across pilot and non-pilot symbols | mitigate | Explicit no-`ic_engine`-between-plans constraint, plus a mandatory NO-GO restoration path back to one uniform labeling method verified against the backup |
| T-171-SC | Tampering (supply chain) | package installs | accept | This plan installs no packages; all tooling was built in wave 1 from existing dependencies |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/ -q` green (wave merge gate per 171-VALIDATION.md).
- Three evidence JSONs present in the phase `evidence/` directory and parseable.
- `171-PILOT-GATE.md` carries a single `PILOT VERDICT:` line.
- `alpha.hmm.walk_forward.enabled` still `false` in `config_state`.
- `config_history` has no new row for `alpha.hmm.walk_forward.enabled` from this plan:
  `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT timestamp, changed_by, reason FROM config_history WHERE config_key='alpha.hmm.walk_forward.enabled' ORDER BY timestamp DESC LIMIT 5"`.
</verification>

<success_criteria>
- REQ-5 is closed: `_hmm_seed_stability_check` has run against real corpus data across 8 symbols
  and 4 timeframes, with a recorded per-cell verdict and a reported corpus-wide minimum.
- REQ-3 is proven at pilot scale: every pilot cell carries single-method provenance, verified by
  the checked-in tool rather than asserted.
- D-03's attribution safeguard produced a verdict, and `alpha.hmm.n_restarts` for the rollout is
  set by that verdict rather than assumed.
- D-01's gate is answered in writing with cited evidence, and the answer is a correctness verdict,
  not a performance verdict.
- Production config is untouched and the corpus is in a coherent state.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-05-SUMMARY.md`
when done.
</output>
