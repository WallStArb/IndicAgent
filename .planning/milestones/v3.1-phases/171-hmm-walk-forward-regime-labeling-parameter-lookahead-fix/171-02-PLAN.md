---
phase: 171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/229-regime-writer-hmm-retry-logic-structurally-unreachable.md
  - .planning/todos/completed/229-regime-writer-hmm-retry-logic-structurally-unreachable.md
  - .planning/todos/PRIORITIES.md
  - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-APR-VERIFICATION.md
autonomous: true
requirements: [REQ-1]

must_haves:
  truths:
    - "All 8 tf-calibrated walk-forward APR keys are confirmed present in config_state with the provenance tags D-02 requires, and the confirmation is recorded as evidence rather than asserted from memory"
    - "1d's refit_every_bars/initial_warmup_bars are explicitly disclosed as [initial_estimate], not [rca_analysis], per D-02"
    - "Todo 229's record matches reality: it is closed, cites commit ba8a74ef, and PRIORITIES.md no longer describes it as deliberately deferred"
  artifacts:
    - path: ".planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-APR-VERIFICATION.md"
      provides: "recorded config_state evidence for REQ-1, captured not assumed"
      contains: "alpha.hmm.walk_forward.refit_every_bars.1d"
    - path: ".planning/todos/completed/229-regime-writer-hmm-retry-logic-structurally-unreachable.md"
      provides: "closed todo 229 record citing the commit that fixed it"
      contains: "ba8a74ef"
  key_links:
    - from: ".planning/todos/PRIORITIES.md"
      to: ".planning/todos/completed/229-regime-writer-hmm-retry-logic-structurally-unreachable.md"
      via: "corrected row pointing at the completed/ path"
      pattern: "completed/229-regime-writer"
---

<objective>
Reconcile this phase's two paper-only requirements with reality: confirm REQ-1's tf-calibrated APR
keys actually shipped (RESEARCH.md found migration 292 already seeded all 8 with the exact values
the ROADMAP requirement specifies), and correct todo 229's stale records.

Purpose: REQ-1 is the one requirement RESEARCH.md classified as ALREADY SHIPPED. The honest
completion of a shipped requirement is captured evidence, not a rebuild — but "verified by
inspection" is exactly what D-00 forbids, so the verification must be an executed query whose
output is recorded. Separately, CONTEXT.md's Folded Todos section names three concrete record
corrections for todo 229 that are pure housekeeping, ungated on any other work in this phase.

Output: a recorded REQ-1 evidence file, a closed todo 229, and a corrected PRIORITIES.md row.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-CONTEXT.md
@.planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-RESEARCH.md
@docs/foundation/adaptive-parameter-registry.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture REQ-1 APR verification evidence</name>
  <files>.planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-APR-VERIFICATION.md</files>
  <read_first>
    - production/migrations/292_hmm_walk_forward_apr.sql (the migration that seeded the keys —
      read the header comment for the per-tf provenance reasoning)
    - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-RESEARCH.md
      "Summary" section, Requirement 1 bullet (the claim being verified)
    - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-CONTEXT.md
      D-02 (the 1d calibration decision and its required disclosure)
    - services/regime_writer.py lines 118-130 (`_WALK_FORWARD_DEFAULT_PARAMS`, the in-code
      fallbacks that must match the seeded values)
  </read_first>
  <action>
Run against the live database and capture verbatim output:
`PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT s.config_key, s.config_value, sch.description FROM config_state s JOIN config_schema sch USING (config_key) WHERE s.config_key LIKE 'alpha.hmm.walk_forward%' OR s.config_key IN ('alpha.hmm.n_restarts','alpha.hmm.random_state') ORDER BY s.config_key"`

Write `171-APR-VERIFICATION.md` in the phase directory containing: (a) the verbatim query and its
output; (b) a per-key table asserting each of the 8 tf-calibrated keys matches the ROADMAP
requirement's specified value — `refit_every_bars` 1h=1650, 15m=6600, 5m=19800, 1d=252 and
`initial_warmup_bars` 1h=3300, 15m=13200, 5m=39600, 1d=504; (c) the provenance tag each key's
description carries, explicitly noting that 1h/15m are `[rca_analysis]` and 5m/1d are
`[initial_estimate]`, which is what D-02 requires — 1d's values were derived by density-scaling
the "~1 trading year refit / ~2 year warmup" heuristic at ~252 bars/year and are disclosed as an
estimate, not presented as measured; (d) confirmation that
`_WALK_FORWARD_DEFAULT_PARAMS` in `services/regime_writer.py` lines 122-127 matches the seeded
values exactly, so an APR read failure degrades to the same numbers rather than silently
different ones; (e) the current `alpha.hmm.n_restarts` value (baseline for D-03's pilot arm) and
`alpha.hmm.random_state`.

State the verdict plainly at the top: REQ-1 required no new implementation because migration 292
(commit `1300ec8d`, 2026-08-05) already shipped it. Do NOT write a new migration. Do NOT change
any config value in this task.

If any key is missing or any value disagrees with the list above, STOP and report the discrepancy
rather than silently writing a corrective migration — a value mismatch here would mean
RESEARCH.md's Requirement-1 finding is wrong and the phase's premises need re-checking.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-APR-VERIFICATION.md && grep -c "initial_estimate" .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-APR-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `171-APR-VERIFICATION.md` exists and contains all 8 key names
      (`alpha.hmm.walk_forward.refit_every_bars.{5m,15m,1h,1d}` and
      `alpha.hmm.walk_forward.initial_warmup_bars.{5m,15m,1h,1d}`).
    - The file records the literal values 1650, 6600, 19800, 252, 3300, 13200, 39600, 504.
    - The file states that 1d's two keys carry `[initial_estimate]`, not `[rca_analysis]`.
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM config_state WHERE config_key LIKE 'alpha.hmm.walk_forward%'"`
      returns 9 (8 tf keys plus the enabled flag).
    - `git status --short production/migrations/` is empty — no migration was added.
  </acceptance_criteria>
  <done>REQ-1's already-shipped status is backed by captured query output, and 1d's estimate
  provenance is disclosed in writing per D-02.</done>
</task>

<task type="auto">
  <name>Task 2: Close todo 229 and correct its PRIORITIES.md row</name>
  <files>.planning/todos/pending/229-regime-writer-hmm-retry-logic-structurally-unreachable.md, .planning/todos/completed/229-regime-writer-hmm-retry-logic-structurally-unreachable.md, .planning/todos/PRIORITIES.md</files>
  <read_first>
    - .planning/todos/pending/229-regime-writer-hmm-retry-logic-structurally-unreachable.md (full)
    - .planning/todos/PRIORITIES.md line 170 (the stale "implementation deliberately deferred" row)
      and line 193 (todo 108's cross-reference to 229, which must stay accurate after the move)
    - .planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-CONTEXT.md
      "Folded Todos" section (the three specific corrections required)
    - services/regime_writer.py lines 738-754 (the shipped fix, so the closing note describes what
      actually landed rather than what was proposed)
  </read_first>
  <action>
Confirm the fix is live before closing anything: run
`git log --oneline -1 ba8a74ef` and
`grep -n "monitor_.iter < model.monitor_.n_iter" services/regime_writer.py`, expecting hits in
BOTH `_compute_symbol_tf` and `_walk_forward_hmm_full`. If either is absent, STOP and report —
do not close a todo whose fix is not actually present.

Move the todo file with `git mv .planning/todos/pending/229-regime-writer-hmm-retry-logic-structurally-unreachable.md .planning/todos/completed/`,
then append a closing note to the moved file stating: the fix landed in commit `ba8a74ef`
(2026-08-05) replacing hmmlearn 0.3.3's always-True `monitor_.converged` with
`model.monitor_.iter < model.monitor_.n_iter`; it is live in both the single-fit path
(`_compute_symbol_tf`) and the walk-forward path (`_walk_forward_hmm_full`); the blast-radius
verification that commit deferred to "the next scheduled corpus rebuild" is Phase 171's own
full-corpus refit (plan 171-06), whose per-segment `iters_used` records (plan 171-01) supply the
full-scale evidence; and the pending file was never moved, which is what this correction fixes.

Correct PRIORITIES.md's row for 229: replace the "**Design decision settled and proven** —
implementation deliberately deferred, sequenced behind the next full corpus run" wording with a
COMPLETED marker citing commit `ba8a74ef` and pointing at
`completed/229-regime-writer-hmm-retry-logic-structurally-unreachable.md`. Follow whatever
convention PRIORITIES.md already uses for completed entries; if it has no completed section,
remove the row from the pending ranking table and note the closure inline where the file's own
structure makes that natural. Also update todo 108's row (line 193), which currently says
"Re-read 229 before scoping any further work here" — keep the cross-reference but repoint it at
the `completed/` path so the link does not break.

Per CLAUDE.md's git gotcha: stage the renamed path alone first (`git add -- .planning/todos/completed/229-*.md`,
letting git auto-detect the rename) before batching it with the PRIORITIES.md edit — a multi-pathspec
`git add` aborts entirely if any path no longer matches.

Do NOT touch todos 226, 108, or 167 in this task. 226's data collection is plan 171-06's job,
108's verdict comes from plan 171-05's pilot, and 167's verification is plan 171-07's.
  </action>
  <verify>
    <automated>test -f .planning/todos/completed/229-regime-writer-hmm-retry-logic-structurally-unreachable.md && test ! -f .planning/todos/pending/229-regime-writer-hmm-retry-logic-structurally-unreachable.md && grep -c "ba8a74ef" .planning/todos/completed/229-regime-writer-hmm-retry-logic-structurally-unreachable.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/todos/pending/ | grep -c "^229-"` returns 0.
    - `grep -c "ba8a74ef" .planning/todos/completed/229-regime-writer-hmm-retry-logic-structurally-unreachable.md`
      is at least 1.
    - `grep -n "deliberately deferred" .planning/todos/PRIORITIES.md` returns no line referring to
      todo 229.
    - `grep -n "pending/229-regime-writer" .planning/todos/PRIORITIES.md` returns nothing (the
      stale pending path is gone from all rows, including todo 108's cross-reference).
    - `git status --short .planning/todos/` shows the rename staged as R, not as an
      add-plus-delete pair.
  </acceptance_criteria>
  <done>Todo 229's record matches the shipped code: closed, commit-cited, and PRIORITIES.md no
  longer misdescribes it as deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None in the ASVS sense — this plan performs read-only database queries and edits planning markdown.
Per RESEARCH.md's Security Domain section, the phase has no network-facing surface, no
authentication/session/access-control boundary, and no user input; no ASVS L1 category applies.

## Correctness Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-04 | Information integrity (stale record) | `.planning/todos/PRIORITIES.md`, todo 229 | mitigate | Fix presence is confirmed by grep against `services/regime_writer.py` BEFORE the todo is closed; closing a todo whose fix is absent is an explicit STOP condition |
| T-171-05 | Verification-by-inspection (D-00 violation) | REQ-1 sign-off | mitigate | Verdict must be backed by captured `psql` output written to `171-APR-VERIFICATION.md`, not asserted from RESEARCH.md's summary |
| T-171-06 | Unintended schema mutation | `production/migrations/` | mitigate | Explicit acceptance criterion that `git status --short production/migrations/` is empty; a value mismatch is a STOP-and-report, never a silent corrective migration |
| T-171-SC | Tampering (supply chain) | package installs | accept | No packages installed by this plan |
</threat_model>

<verification>
- `171-APR-VERIFICATION.md` exists with captured query output and all 8 key/value pairs.
- Todo 229 lives in `completed/` with a `ba8a74ef` citation; no stale `pending/229-` reference
  survives in PRIORITIES.md.
- `git status --short` shows only the four intended paths changed.
</verification>

<success_criteria>
- REQ-1 is closed with executed evidence, not an assertion.
- 1d's `[initial_estimate]` provenance is disclosed in writing, satisfying D-02's disclosure
  requirement.
- No migration, no config value change, no code change from this plan.
- Todo 229's three CONTEXT.md-specified record corrections are all applied.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/171-hmm-walk-forward-regime-labeling-parameter-lookahead-fix/171-02-SUMMARY.md`
when done.
</output>
