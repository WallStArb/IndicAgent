---
phase: 144-cross-sectional-regime-model-regime-group
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/regime_signals/commodity_momentum_ts.py
  - src/intelligence/regime_signals/fx_dollar_carry.py
  - tests/unit/test_regime_signals_commodity_momentum_ts.py
  - tests/unit/test_regime_signals_fx_dollar_carry.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "commodity_momentum_ts exposes the compute()/build_tiers()/PROB_KEYS contract and is DB-free"
    - "fx_dollar_carry exposes the compute()/build_tiers()/PROB_KEYS contract and is DB-free"
    - "Both modules exist and are importable even though their regime_groups ship enabled:false"
    - "Tier vocabularies do not overlap with equity or rates label sets"
  artifacts:
    - path: "src/intelligence/regime_signals/commodity_momentum_ts.py"
      provides: "Commodity time-series-momentum signal (ships disabled)"
    - path: "src/intelligence/regime_signals/fx_dollar_carry.py"
      provides: "FX dollar/carry signal (ships disabled)"
  key_links:
    - from: "src/intelligence/regime_signals/commodity_momentum_ts.py"
      to: "REGISTRY contract"
      via: "compute()/build_tiers()/PROB_KEYS"
      pattern: "def compute"
---

<objective>
Build the two DISABLED-on-ship signal modules — `commodity_momentum_ts.py` and `fx_dollar_carry.py`.
Their `regime_group`s ship `enabled: false` (commodity/fx enablement is gated on todo 041's tag
taxonomy audit, D-04), but the modules themselves are built now per CONTEXT.md Claude's Discretion
(cheap, already spec'd with tests; todo 041 gates enablement, not module existence).

Purpose: Registry completeness (Plan 04's `__init__.py` imports all four modules). Wave 1 —
independent pure functions, no dependency on migration or dispatcher.
Output: 2 module files + 2 test files.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v3.1-phases/144-cross-sectional-regime-model-regime-group/144-RESEARCH.md
@.planning/milestones/v3.1-phases/144-cross-sectional-regime-model-regime-group/144-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: commodity_momentum_ts signal module (ships disabled)</name>
  <files>src/intelligence/regime_signals/commodity_momentum_ts.py, tests/unit/test_regime_signals_commodity_momentum_ts.py</files>
  <read_first>
    - docs/plans/archive/2026-07-01-cross-sectional-regime-model.md (Task 6 "commodity_momentum_ts Signal Module", lines 2221-2456 — module code + test file; RESEARCH.md confirmed complete with no drift, port as-is)
    - src/intelligence/regime_signals/curve_credit.py (structural sibling to mirror: same compute()/build_tiers()/PROB_KEYS shape, DB-free — note Plan 02 creates this; if not yet present, mirror the plan doc's contract directly)
  </read_first>
  <behavior>
    - compute() returns a time-series-momentum signal tuple or None; DB-free
    - build_tiers() buckets into a commodity-specific tier vocabulary that does NOT collide with equity/rates label strings
    - Warmup positions before the window fills are NaN
  </behavior>
  <action>
    Create src/intelligence/regime_signals/commodity_momentum_ts.py from the plan doc's Task 6 module
    code (lines 2354-2456). New math, no live predecessor, no drift (RESEARCH.md) — port as-written.
    Window params are daily-denominated and _tf_window-scaled by the dispatcher (Plan 04) before
    compute() is called; the module stays TF-agnostic (same convention as breadth_vol/curve_credit).
    Tier vocabulary MUST NOT overlap with equity (low/mid/high x bear/neutral/bull) or rates
    (steep/flat/inverted x wide/tight) label strings — Pitfall 4 label-uniqueness invariant; add a
    code comment noting it. Exception variable name is `error`.
    Create tests/unit/test_regime_signals_commodity_momentum_ts.py from the plan doc's Task 6 test file.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_regime_signals_commodity_momentum_ts.py -v && ! grep -q "import psycopg2" src/intelligence/regime_signals/commodity_momentum_ts.py && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_regime_signals_commodity_momentum_ts.py -v` all green
    - `grep "import psycopg2" src/intelligence/regime_signals/commodity_momentum_ts.py` returns NOTHING
    - `grep "def compute" src/intelligence/regime_signals/commodity_momentum_ts.py` matches
    - A code comment documents the label-vocabulary non-overlap invariant
  </acceptance_criteria>
  <done>commodity_momentum_ts.py exists with the signal + non-overlapping tiers, DB-free; test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fx_dollar_carry signal module (ships disabled)</name>
  <files>src/intelligence/regime_signals/fx_dollar_carry.py, tests/unit/test_regime_signals_fx_dollar_carry.py</files>
  <read_first>
    - docs/plans/archive/2026-07-01-cross-sectional-regime-model.md (Task 7 "fx_dollar_carry Signal Module", lines 2456-2641 — module code + test file; RESEARCH.md confirmed complete, port as-is)
    - src/intelligence/regime_signals/curve_credit.py (structural sibling to mirror)
  </read_first>
  <behavior>
    - compute() returns an fx dollar/carry signal tuple or None; DB-free
    - build_tiers() buckets into an fx-specific tier vocabulary that does NOT collide with any other group's labels
    - Warmup positions before the window fills are NaN
  </behavior>
  <action>
    Create src/intelligence/regime_signals/fx_dollar_carry.py from the plan doc's Task 7 module code
    (lines 2561-2641). New math, no drift — port as-written. Window params daily-denominated,
    _tf_window-scaled by the dispatcher (TF-agnostic module). Tier vocabulary MUST NOT overlap with
    equity/rates/commodity label strings (Pitfall 4); add a code comment. Note: IBIT (crypto) routes
    into the fx group via tag_filter `["fx_*", "crypto"]` per the ROADMAP Phase 144 crypto-into-fx
    decision — this is a routing config concern (Plan 01 migration / Plan 04 dispatcher), not a change
    to this module's math. Exception variable name is `error`.
    Create tests/unit/test_regime_signals_fx_dollar_carry.py from the plan doc's Task 7 test file.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_regime_signals_fx_dollar_carry.py -v && ! grep -q "import psycopg2" src/intelligence/regime_signals/fx_dollar_carry.py && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_regime_signals_fx_dollar_carry.py -v` all green
    - `grep "import psycopg2" src/intelligence/regime_signals/fx_dollar_carry.py` returns NOTHING
    - `grep "def compute" src/intelligence/regime_signals/fx_dollar_carry.py` matches
    - A code comment documents the label-vocabulary non-overlap invariant
  </acceptance_criteria>
  <done>fx_dollar_carry.py exists with the signal + non-overlapping tiers, DB-free; test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure in-memory signal computation — no DB, no network, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-03-LB | Tampering | label-vocabulary collision | mitigate | feature_ic_scores has no regime_group column (Pitfall 4) — group identity is implicit in label-string uniqueness. Non-overlapping tier vocabularies (enforced by code comment + build_tiers design) prevent two groups' regimes silently colliding under one `regime` string. Low severity since these groups ship disabled. |
| T-144-SC | Tampering | package installs | accept | Zero new packages. Supply-chain N/A. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_regime_signals_commodity_momentum_ts.py tests/unit/test_regime_signals_fx_dollar_carry.py -v` green
- Neither module imports psycopg2 (DB-free)
- Tier vocabularies distinct from equity/rates
</verification>

<success_criteria>
- Both disabled-on-ship signal modules exist, pure/DB-free, matching the REGISTRY contract
- Tier vocabularies non-overlapping across all four groups
- Tests pass
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/144-cross-sectional-regime-model-regime-group/144-03-SUMMARY.md` when done
</output>
