---
phase: 144-cross-sectional-regime-model-regime-group
plan: 05
type: execute
wave: 3
depends_on: ["144-01", "144-04"]
files_modified:
  - services/ic_engine.py
  - tests/unit/test_ic_engine_routing.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "_build_symbol_regime_class maps each symbol to exactly one enabled regime_group; raises AmbiguousRegimeGroupError on multi-match; omits (does not default) zero-match symbols"
    - "ICEngineConfig carries regime_groups_json, bound once in from_apr, normalizing the already-parsed-list vs raw-string JSON shapes"
    - "All four asset_class='equity' SQL sites query regime_group instead"
    - "_compute_cross_sectional_tf filters feature_vectors by symbol = ANY(symbol_list) — the contamination fix — with config and rng threaded unchanged"
    - "The cross-sectional pass loops enabled_groups, pooling only each group's peer symbols"
  artifacts:
    - path: "services/ic_engine.py"
      provides: "regime_group routing + cross-sectional peer-scoping fix"
      contains: "_build_symbol_regime_class"
    - path: "tests/unit/test_ic_engine_routing.py"
      provides: "Routing + ambiguity + omission unit tests"
      contains: "AmbiguousRegimeGroupError"
  key_links:
    - from: "services/ic_engine.py"
      to: "services.cross_sectional_regime_model._parse_group_configs"
      via: "import"
      pattern: "_parse_group_configs"
    - from: "services/ic_engine.py _compute_cross_sectional_tf"
      to: "feature_vectors"
      via: "WHERE fv.symbol = ANY(%(symbol_list)s)"
      pattern: "symbol_list"
    - from: "services/ic_engine.py"
      to: "market_regimes"
      via: "WHERE regime_group (4 sites)"
      pattern: "regime_group"
---

<objective>
Add `regime_group` routing to `services/ic_engine.py`: build the symbol->group map (fail-loud on
ambiguity, omit unrouted), load each group's regime dict separately, and — the actual bug fix —
scope cross-sectional IC pooling to each group's peer symbols so `fi_*` bonds / GLD/SLV/VNQ / IBIT
no longer contaminate the equity cells.

CRITICAL: `ic_engine.py` has drifted since the plan doc (Phase 143/143.1). Re-grep live line numbers
before editing. Apply the plan doc's Task 5 INTENT against the CURRENT shapes:
- Config is a frozen `ICEngineConfig` dataclass (`from_apr()`), NOT a loose `_load_apr` dict — ADD a
  field, don't reintroduce a dict.
- `_compute_cross_sectional_tf` has a 10-arg signature including `config: ICEngineConfig` and
  `rng: np.random.Generator` (Phase 143.1). ADD `regime_group` + `symbol_list` params; do NOT revert
  to the plan doc's `apr` dict signature.
- JSON gotcha: `alpha.regime.groups` returns an already-parsed list once seeded — normalize in
  `from_apr` (never `str()` a list[dict], never json.loads a parsed list).

Purpose: Wires group-scoped regime labels into IC stratification and fixes the confirmed
contamination bug (D-01). Depends on Plan 01 (regime_group column) and Plan 04 (imports the
dispatcher's `_parse_group_configs`).
Output: modified ic_engine.py + routing test.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v3.1-phases/144-cross-sectional-regime-model-regime-group/144-RESEARCH.md
@.planning/milestones/v3.1-phases/144-cross-sectional-regime-model-regime-group/144-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _build_symbol_regime_class + ICEngineConfig.regime_groups_json + routing test</name>
  <files>services/ic_engine.py, tests/unit/test_ic_engine_routing.py</files>
  <read_first>
    - services/ic_engine.py (RE-GREP FIRST: grep -n for "class ICEngineConfig", "def from_apr", "equity_model_enabled" — the frozen dataclass ~311-372 and from_apr ~385-406; do NOT trust the plan doc's pre-143.1 line numbers)
    - tests/unit/test_ic_engine_staleness.py (established pattern for testing a pure function imported directly from services.ic_engine, incl. the project-root sys.path insert)
    - services/cross_sectional_regime_model.py (import _parse_group_configs from here — Plan 04's JSON-safe version)
    - docs/plans/archive/2026-07-01-cross-sectional-regime-model.md (Task 5 Step 1/3, lines 1689-1742 — _build_symbol_regime_class + AmbiguousRegimeGroupError; this block is NEW code, correct as written; the config-loading around it is stale)
  </read_first>
  <behavior>
    - A symbol matching exactly one enabled group is mapped to it
    - A symbol whose tags match >1 enabled group raises AmbiguousRegimeGroupError (never resolved by array order)
    - A symbol matching 0 enabled groups is omitted from the result (never defaulted to "equity")
    - A disabled group is excluded from matching entirely
    - from_apr binds regime_groups_json from an already-parsed list (json.dumps it) or a raw string (pass through); never str() on a list
  </behavior>
  <action>
    RE-GREP live line numbers first (research is a snapshot; ic_engine.py changes weekly).

    Add to services/ic_engine.py:
    (1) class AmbiguousRegimeGroupError(ValueError) and _build_symbol_regime_class(tags_by_symbol,
    group_configs) -> dict[str, str] from the plan doc Task 5 Step 1/3 (lines 1689-1742) — new code,
    correct as-written: prefix-match tag_filter (rstrip "*"), raise on len(matches)>1, omit on
    len(matches)==0, skip disabled groups.
    (2) Add field regime_groups_json: str = "[]" to the frozen ICEngineConfig dataclass (defaulted,
    so the direct-constructor call in tests/unit/test_hac_ic_sharpe.py does not break on field-count
    growth — same convention as every post-143 field).
    (3) In from_apr(), bind regime_groups_json by normalizing the JSON-typed APR value: if
    cfg.get_sync("alpha.regime.groups", []) is NOT a str, json.dumps() it; else pass through. Never
    str() a list[dict] (yields invalid JSON — repr single-quotes/True/False).
    (4) Add "from services.cross_sectional_regime_model import _parse_group_configs" and, in main()
    after config binding, derive group_configs = _parse_group_configs(config.regime_groups_json) and
    enabled_groups = [g for g in group_configs if g.get("enabled", True)].
    Exception variable name is error.

    Retirement decision on alpha.regime.equity_model_enabled (RESEARCH.md Open Question 2): run
    grep -rn "equity_model_enabled" across services/ src/ scripts/. If ic_engine.py is the SOLE
    consumer, retire the standalone kill-switch in favor of bool(enabled_groups) (avoid two drifting
    on/off sources of truth); if other consumers exist, keep it as a narrow "is equity among
    enabled_groups" check and note why in a comment. Record the decision in the SUMMARY.

    Create tests/unit/test_ic_engine_routing.py (mirror test_ic_engine_staleness.py import pattern):
    single-group match, ambiguous multi-match raises AmbiguousRegimeGroupError, zero-match omission,
    disabled-group exclusion.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_ic_engine_routing.py -v && python3 -c "import sys; s=open('services/ic_engine.py').read(); sys.exit(0 if 'regime_groups_json' in s and '_build_symbol_regime_class' in s else 1)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - .venv/bin/pytest tests/unit/test_ic_engine_routing.py -v all green (match / ambiguity-raises / omission / disabled-exclusion)
    - grep "regime_groups_json" services/ic_engine.py matches (field + from_apr binding)
    - grep "_build_symbol_regime_class" services/ic_engine.py matches
    - grep "str(cfg.get_sync" services/ic_engine.py near the alpha.regime.groups binding returns NOTHING (no repr-string bug)
    - SUMMARY records the equity_model_enabled retirement decision with grep evidence
  </acceptance_criteria>
  <done>Routing function + AmbiguousRegimeGroupError + ICEngineConfig field + JSON-safe from_apr binding + _parse_group_configs import present; routing test green; retirement decision recorded.</done>
</task>

<task type="auto">
  <name>Task 2: regime_group SQL sites + _compute_cross_sectional_tf symbol_list fix + per-group loop</name>
  <files>services/ic_engine.py</files>
  <read_first>
    - services/ic_engine.py (RE-GREP FIRST: grep -n "asset_class" — expect 4 sites near 503/1559/2722/2834; grep -n for "def _compute_cross_sectional_tf", "def _assert_prerequisites", "equity_model_enabled". Read the LIVE _compute_cross_sectional_tf signature ~1454-1465 (10 args incl config + rng), its chunk_sql ~1591-1602, the regime-timestamp prefetch ~1555-1571, the cross-sectional caller loop ~2820-2876, and the _persist_corpus_results gate ~2034)
    - docs/plans/archive/2026-07-01-cross-sectional-regime-model.md (Task 5 Steps 5/10/11 — the INTENT of the SQL generalization + symbol_list fix + per-group loop; its literal signatures are pre-143.1 stale)
  </read_first>
  <action>
    RE-GREP the 4 asset_class sites and all signatures before editing — line numbers drift weekly.

    (1) Generalize all four WHERE asset_class='equity' sites (currently ~503, 1559, 2722, 2834) to
    parameterized WHERE regime_group = %s driven by the group in scope. The count-check ~503, the
    mr_dict load ~2722, and the label-set query ~2834 loop over enabled_groups.
    (2) _assert_prerequisites: extend the current live 3-arg signature (conn, tfs,
    equity_model_enabled) with group awareness (plan doc Task 5 Step 5 intent) — loop enabled_groups,
    query market_regimes WHERE regime_group=%s AND tf=%s. Add params, do not wholesale-replace the
    signature with the plan doc's stale expectation.
    (3) _compute_cross_sectional_tf: ADD regime_group: str and symbol_list: list[str] as TWO
    ADDITIONAL params on the CURRENT signature — keep config: ICEngineConfig and rng: np.random.Generator
    unchanged (do NOT revert to an apr dict; that regresses Phase 143.1). In chunk_sql (~1591) add
    "AND fv.symbol = ANY(%(symbol_list)s)" — THE contamination fix — and pass symbol_list in the
    params dict. Update the regime-timestamp prefetch (~1555) from WHERE asset_class = 'equity' to
    WHERE regime_group = %(regime_group)s.
    (4) mr_dict loading: generalize the single mr_dict_by_tf to
    mr_dicts_by_group: dict[str, dict[str, dict]] ({group -> {tf -> {ts -> label}}}), built once by
    looping enabled_groups; thread into worker args as
    mr_dicts_by_group.get(symbol_regime_class.get(symbol)) so each worker gets only its own group's
    dict (picklability unchanged).
    (5) Cross-sectional pass caller (~2820): loop enabled_groups × tfs × that-group's-cs-regimes (not
    just tfs × cs_regimes), passing regime_group=group_name and symbol_list=peer_symbols_for_group
    into _compute_cross_sectional_tf.
    (6) Generalize equity_model_enabled bool gates (e.g. _persist_corpus_results ~2034
    "if equity_model_enabled and corpus_cs_rows:") to "if bool(enabled_groups) and corpus_cs_rows:" —
    preserve boolean semantics, do NOT delete the gates (RESEARCH.md Anti-Pattern). After editing, run
    grep -n "equity_model_enabled" services/ic_engine.py and verify every remaining hit is either
    intentionally kept or generalized — no stale reference silently no-ops the cross-sectional write.
    Exception variable name is error.
  </action>
  <verify>
    <automated>test $(grep -c "asset_class='equity'\|asset_class = 'equity'" services/ic_engine.py) -eq 0 && grep -q "symbol = ANY(%(symbol_list)s)" services/ic_engine.py && python3 -c "import ast; ast.parse(open('services/ic_engine.py').read())" && .venv/bin/pytest tests/unit/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "asset_class='equity'" and "asset_class = 'equity'" in services/ic_engine.py both return 0 (all 4 sites generalized)
    - grep "symbol = ANY(%(symbol_list)s)" services/ic_engine.py matches (contamination fix present)
    - services/ic_engine.py parses as valid Python
    - Full unit suite .venv/bin/pytest tests/unit/ -q green (only the pre-existing test_no_smooth_or_backward_in_factory failure tolerated; broad ic_engine collection errors indicate a wrong-baseline patch — Pitfall 2)
    - grep -n "equity_model_enabled" audited: no stale reference left un-generalized
  </acceptance_criteria>
  <done>All 4 asset_class sites -> regime_group; _compute_cross_sectional_tf takes symbol_list (peer-scoped pooling) with config+rng intact; mr_dicts_by_group + per-group cross-sectional loop; equity_model_enabled gates generalized; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator APR (alpha.regime.groups) -> routing / dispatch | tag_filter + signal_type from operator-authored JSON, not external input |
| ic_engine -> feature_vectors / market_regimes (DB) | Parameterized reads over a locally-trusted DB connection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-05-SQL | Tampering | 4 regime_group SQL sites + chunk_sql symbol_list | mitigate | All new WHERE clauses use bound params (%(regime_group)s, %(symbol_list)s) — group names / symbol lists are never string-interpolated into SQL. feature_cols/return_cols remain internally-derived schema identifiers (existing accepted pattern). |
| T-144-05-ROUTE | Spoofing/Tampering | _build_symbol_regime_class | mitigate | Fail-loud on ambiguous multi-group match (AmbiguousRegimeGroupError); omit-not-default on zero-match with loud logging — never silently mislabels a symbol's regime. Config-authoring error surfaces as a crash, not a wrong IC number. |
| T-144-05-JSON | Denial of Service | from_apr regime_groups_json binding | mitigate | Normalizes already-parsed-list vs raw-string; eliminates the str()-on-list invalid-JSON crash path. |
| T-144-05-CONTAM | Information Disclosure (data integrity) | cross-sectional pooling | mitigate | The symbol_list filter is the fix for the confirmed contamination bug (non-peer symbols leaking into a group's pooled IC cells) — the core correctness deliverable of this phase. |
| T-144-SC | Tampering | package installs | accept | Zero new packages. Supply-chain N/A. |
</threat_model>

<verification>
- .venv/bin/pytest tests/unit/test_ic_engine_routing.py -v green
- Full suite .venv/bin/pytest tests/unit/ -q green (broad ic_engine collection errors => wrong-baseline patch, revisit against live signatures)
- grep -c "asset_class" services/ic_engine.py == 0
- grep "symbol = ANY(%(symbol_list)s)" services/ic_engine.py present
- Post-run peer-scoping spot check (needs live DB, gated to Plan 06 window): compare symbol membership in equity-labelled feature_ic_scores rows before/after — fi_* bonds absent from equity cells
</verification>

<success_criteria>
- Symbol->group routing with fail-loud ambiguity and omit-not-default, unit tested
- ICEngineConfig.regime_groups_json bound once, JSON-shape-safe; config + rng threading preserved
- All 4 asset_class sites generalized to regime_group; cross-sectional pooling peer-scoped via symbol_list
- equity_model_enabled gates generalized (no stale no-op); retirement decision recorded
- Unit suite green
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/144-cross-sectional-regime-model-regime-group/144-05-SUMMARY.md` when done
</output>
