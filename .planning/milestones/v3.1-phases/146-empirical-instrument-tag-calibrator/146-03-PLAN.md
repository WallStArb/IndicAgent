---
phase: 146-empirical-instrument-tag-calibrator
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/statistics/factor_math.py
  - tests/unit/test_factor_math.py
autonomous: true
requirements: [TAG-01]
must_haves:
  truths:
    - "A standardized OLS loading (signed, [-1,1]) is computed for an instrument-return vs factor-return series"
    - "A HAC (Newey-West Bartlett) standard error / p-value is produced for the loading, reusing the ic_math kernel pattern"
    - "long_short_daily_returns builds the HYG-IEF/TIP-IEF/IEF-SHY/XLE-SPY spread from two close-price arrays"
    - "vol_beta's factor series reuses breadth_vol's causal SPY-realized-vol proxy, not a re-derived whole-series rank"
    - "factor_math imports CI/p-value/FDR/HAC/condition-number from ic_math rather than reimplementing them"
  artifacts:
    - path: "src/intelligence/statistics/factor_math.py"
      provides: "OLS loading + HAC SE, long-short constructor, vol-proxy adapter — pure functions"
      min_lines: 60
      contains: "def long_short_daily_returns"
    - path: "tests/unit/test_factor_math.py"
      provides: "synthetic-fixture correctness tests for loading, HAC inflation, long-short"
      contains: "def test_ols_loading_synthetic"
  key_links:
    - from: "src/intelligence/statistics/factor_math.py"
      to: "src/intelligence/statistics/ic_math.py"
      via: "import of _fisher_z_ci/_p_values_from_ic/apply_bh_fdr/_hac_sharpe_nd/check_condition_number"
      pattern: "from src.intelligence.statistics.ic_math import"
    - from: "src/intelligence/statistics/factor_math.py"
      to: "src/intelligence/regime_signals/breadth_vol.py"
      via: "import of _compute_vix_pct_rank for vol_beta factor input"
      pattern: "_compute_vix_pct_rank"
---

<objective>
Wave 1 statistics module (D-02, D-12). Build `factor_math.py` — the pure-function measurement kernel
the TagCalibrator service (Plan 04) calls: standardized OLS loading, HAC-robust standard error/
p-value, the shared long-short factor-series constructor (four call sites), and the vol_beta
factor-input adapter. All CI/p-value/FDR/HAC/condition-number math is IMPORTED from ic_math, not
reimplemented (F4 — this repo already paid the extraction cost once, todo 048/069).

Purpose: Keep the genuinely-new math (standardized loading + long-short spread + vol adapter) in one
Ring 1 module with no DB and no config coupling, mirroring ic_math's own structure. The service
becomes thin orchestration on top.
Output: factor_math.py + its unit tests.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v3.1-phases/146-empirical-instrument-tag-calibrator/146-PATTERNS.md
@.planning/milestones/v3.1-phases/146-empirical-instrument-tag-calibrator/146-RESEARCH.md

<interfaces>
Reusable ic_math functions to IMPORT (verified live, src/intelligence/statistics/ic_math.py) —
do not reimplement:
- _fisher_z_ci(ic_vector: np.ndarray, n: int) -> tuple[np.ndarray, np.ndarray]   # lines 121-149
- _p_values_from_ic(ic_vector: np.ndarray, n: int, df: int | None = None) -> np.ndarray  # 353-366; pass explicit df for the long-short construction's extra fitted parameter
- apply_bh_fdr(p_values: list[float], alpha: float) -> tuple[np.ndarray, np.ndarray]  # 412-431 (used by Plan 04, re-exported or imported there)
- _hac_sharpe_nd(window_ics, max_lag, mean_ic=None, var0=None) -> np.ndarray  # 724-763; reuse the Bartlett-kernel inflation-factor loop (gamma_k/rho_k/weight accumulation) as the pattern for the loading's HAC SE — NOT the function verbatim (it is Sharpe-specific)
- check_condition_number(matrix: np.ndarray, condition_max: float) -> tuple[bool, float]  # 439-452

Reusable vol proxy (verified live, src/intelligence/regime_signals/breadth_vol.py):
- _compute_vix_pct_rank(spy_close: pd.Series, realized_vol_window: int, vix_z_window: int) -> pd.Series  # 91-135; causal bisect-based expanding rank, DB-free. MUST be reused verbatim — do NOT substitute a whole-series pandas .rank(pct=True) (Phase 141 P0-T2 look-ahead invariant).

New math to write (per RESEARCH.md A2 / PATTERNS.md):
- Standardized loading = signed Pearson correlation of the two return series = cov(x,y)/(std(x)*std(y)),
  bounded [-1,1]. This IS the univariate-OLS beta standardized by sigma_factor/sigma_instrument — do
  not pull in statsmodels.OLS.
- long_short_daily_returns(long_close, short_close) = log-return(long) - log-return(short).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: factor_math.py — loading, HAC SE/p-value, long-short constructor, vol adapter</name>
  <files>src/intelligence/statistics/factor_math.py</files>
  <read_first>
    - src/intelligence/statistics/ic_math.py (module docstring/provenance style, Protocol config pattern, the five reusable functions and _hac_sharpe_nd's inflation-factor loop lines ~724-763)
    - src/intelligence/regime_signals/breadth_vol.py (full — _compute_vix_pct_rank interface + causal-rank invariant)
    - .planning/milestones/v3.1-phases/146-empirical-instrument-tag-calibrator/146-PATTERNS.md (factor_math.py section)
  </read_first>
  <behavior>
    - Test 1 (test_ols_loading_synthetic): for two synthetic return series with a known true Pearson
      correlation r, standardized_loading returns r within tolerance and the sign is correct.
    - Test 2 (test_hac_se_inflation): on autocorrelated synthetic residuals, the HAC standard error is
      strictly larger than the naive (iid) standard error — the Newey-West inflation is applied.
    - Test 3 (test_long_short_constructor): long_short_daily_returns on two known close-price arrays
      equals element-wise log-return(long) minus log-return(short), length N-1.
  </behavior>
  <action>
    Create factor_math.py as a pure-functions Ring 1 module (no DB, no config imports; a minimal
    duck-typed Protocol for hac_max_lag matching ic_math's SharpeWindowConfig style, NOT an import of
    TagCalibratorConfig). Import _fisher_z_ci, _p_values_from_ic, apply_bh_fdr, _hac_sharpe_nd,
    check_condition_number from src.intelligence.statistics.ic_math. Implement:
    (1) long_short_daily_returns(long_close, short_close) — the shared spread constructor (four call
    sites: HYG-IEF, TIP-IEF, IEF-SHY, XLE-SPY), returns log-return(long) - log-return(short).
    (2) standardized_loading(instrument_ret, factor_ret) — signed Pearson correlation
    cov/(std*std), clamped/handled for the degenerate zero-variance case, returns a float in [-1,1]
    (F3 standardized loading). Include a sample_n and a check_condition_number/degenerate guard so a
    near-constant series does not produce a spurious loading.
    (3) loading_hac_pvalue(instrument_ret, factor_ret, hac_max_lag) — compute the loading's two-tailed
    p-value using _p_values_from_ic with a HAC-inflated effective sample size derived from the
    Newey-West Bartlett-kernel inflation factor (reuse the _hac_sharpe_nd gamma_k/rho_k/weight loop
    pattern; do not re-derive the Bartlett formula). Pass an explicit df to _p_values_from_ic to
    account for the long-short construction's extra fitted parameter where applicable.
    (4) spy_realized_vol_factor(spy_close, realized_vol_window, vix_z_window) — thin adapter returning
    breadth_vol._compute_vix_pct_rank(...) verbatim as vol_beta's factor input (D-02); reuse the
    causal version, never a whole-series rank.
    Module docstring must state: pure functions only, reuses ic_math kernel, extends not duplicates.
    Exception variables named `error` (never `exc`). No fenced OHLCV/raw-table reads here — inputs are
    arrays passed in by the service.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_factor_math.py -v && .venv/bin/ruff check src/intelligence/statistics/factor_math.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "from src.intelligence.statistics.ic_math import" src/intelligence/statistics/factor_math.py` shows the reused functions are imported (not redefined).
    - `grep -c "def _fisher_z_ci\|def apply_bh_fdr\|def check_condition_number" src/intelligence/statistics/factor_math.py` returns 0 (no reimplementation).
    - `grep -n "_compute_vix_pct_rank" src/intelligence/statistics/factor_math.py` shows the vol proxy is reused from breadth_vol.
    - factor_math.py contains long_short_daily_returns, standardized_loading, loading_hac_pvalue, spy_realized_vol_factor.
    - No `import asyncpg`, no `market_data_ohlcv` reference in factor_math.py (pure module, no DB).
    - `except Exception as exc` does not appear (uses `error`).
    - ruff clean.
  </acceptance_criteria>
  <done>Pure measurement kernel: standardized loading + HAC p-value + long-short constructor + vol adapter, all reusing ic_math.</done>
</task>

<task type="auto">
  <name>Task 2: test_factor_math.py — synthetic-fixture correctness</name>
  <files>tests/unit/test_factor_math.py</files>
  <read_first>
    - tests/unit/test_ensemble_ic_math.py (closest sibling: synthetic-fixture pure-function test style)
    - src/intelligence/statistics/factor_math.py (the functions under test, from Task 1)
  </read_first>
  <action>
    Write the three tests named in the behavior block: test_ols_loading_synthetic (known true
    correlation recovered within tolerance, correct sign, degenerate zero-variance handled),
    test_hac_se_inflation (autocorrelated residuals → HAC SE strictly > naive SE), and
    test_long_short_constructor (spread equals log-return difference, length N-1). Add one edge test
    that spy_realized_vol_factor returns a causal series (last value does not change when a future bar
    is appended — asserts the causal-rank invariant is preserved, not a whole-series rank). Use
    numpy-seeded synthetic arrays; no DB, no network — CI-clean.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_factor_math.py -v</automated>
  </verify>
  <acceptance_criteria>
    - test_ols_loading_synthetic, test_hac_se_inflation, test_long_short_constructor all pass.
    - The causal-invariant test for spy_realized_vol_factor passes (appending a future bar does not alter earlier values).
    - Tests use only numpy/pandas fixtures, no DB or network access.
  </acceptance_criteria>
  <done>factor_math correctness pinned by synthetic-fixture unit tests, including the causal-vol invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller arrays → factor_math | Pure functions receive numeric arrays from the service; no external/user input, no I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-05 | Tampering (data-integrity) | standardized_loading on degenerate/ill-conditioned series | mitigate | Zero-variance / near-constant guard + check_condition_number so a spurious loading is not emitted; unit test covers the degenerate case. |
| T-146-06 | Tampering (data-integrity) | vol_beta factor input | mitigate | Reuse breadth_vol's causal _compute_vix_pct_rank verbatim; causal-invariant unit test prevents accidental look-ahead (whole-series rank) reintroduction. |
| T-146-SC | Tampering | package installs | accept | Zero new packages — statsmodels/scipy/numpy already present and in active use by ic_math. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_factor_math.py -v` green.
- No reimplementation of ic_math kernel functions (grep gate).
- `.venv/bin/pytest tests/unit/ -q` stays green.
</verification>

<success_criteria>
factor_math.py provides the standardized loading, HAC p-value, shared long-short constructor, and
causal vol adapter — all reusing ic_math — so Plan 04's service is thin orchestration.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/146-empirical-instrument-tag-calibrator/146-03-SUMMARY.md` when done.
</output>
