---
phase: 170-concept-registry-feature-domain-migration-feature-registry-r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/283_concept_registry_feature_domain_schema.sql
  - tests/unit/test_concept_parent_lineage.py
autonomous: true
requirements: [L-5, L-8, L-9, L-10]
user_setup: []

must_haves:
  truths:
    - "A concept can have more than one parent concept, with real FK-enforced referential integrity on both sides of every edge"
    - "An INSERT into concept_parent that would make a concept its own ancestor is rejected by the database, not merely unlikely"
    - "An UPDATE that repoints an existing edge into a cycle is rejected by the same guard, so the trigger's UPDATE arm is proven and not merely declared"
    - "Deprecating a parent concept auto-deprecates every child concept in every domain, with one parent_cascade audit row per cascaded child recording that child's true prior status"
    - "A multi-level chain (grandparent -> parent -> child) fully cascades from one status flip"
    - "concept_gate can hold the shadow_only -> active recovery counters that feature_registry holds today"
    - "concept_registry can express is_control / control_expectation as typed, indexable columns"
  artifacts:
    - path: "production/migrations/283_concept_registry_feature_domain_schema.sql"
      provides: "concept_parent join table + cycle guard + cascade trigger + concept_registry identity columns + concept_gate recovery counters"
      contains: "fn_cascade_concept_parent_deprecation"
    - path: "tests/unit/test_concept_parent_lineage.py"
      provides: "DB-level proof of cycle rejection on both the insert and update paths, single-level cascade, multi-level cascade, and cascade audit-row fidelity"
      contains: "def test_cascade_multi_level"
  key_links:
    - from: "concept_registry.status UPDATE to 'deprecated'"
      to: "concept_transition_log"
      via: "trg_cascade_concept_parent_deprecation"
      pattern: "trigger_reason = 'parent_cascade'"
    - from: "concept_parent"
      to: "concept_registry.concept_id"
      via: "FK on both child_concept_id and parent_concept_id"
      pattern: "REFERENCES concept_registry"
---

<objective>
Add the four schema capabilities `concept_registry`/`concept_gate` are missing versus
`feature_registry`, so the `domain='feature'` fold-in (Plan 03) has somewhere to land without
silently dropping lineage, cascade-deprecation, control-canary identity, or shadow-recovery
mechanics.

Purpose: todo 118's L-5/L-8/L-9/L-10. These are one-way-door gaps — `feature_registry` is being
DROPped at the end of this phase, so anything not ported here is destroyed, not deferred.

Output: migration `283_concept_registry_feature_domain_schema.sql` applied to the live
`indicagent` database, plus a DB-level test proving the cycle guard and the cascade trigger
actually behave (a trigger nobody has fired is a claim, not a mechanism).

Scope guard: this plan adds structure ONLY. It seeds zero `domain='feature'` rows — that is
Plan 03. It changes zero application code.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/todos/pending/118-migrate-feature-domain-into-concept-registry.md

<interfaces>
Live schema facts, verified against the `indicagent` database this session — do NOT re-derive,
and do NOT trust todo 118's "migrations 231/232" reference (wrong; the Concept Registry MVP is
migration **225**, `225_concept_registry_mvp.sql`; the ensemble_strategy seed is **226**; 231 is
the controlled-vocabulary schema).

`concept_registry` (12 columns today):
  concept_id UUID PK DEFAULT gen_random_uuid()
  domain TEXT NOT NULL   CHECK (domain IN ('feature','ensemble_strategy'))   <- 'feature' already allowed
  name TEXT NOT NULL, description TEXT NOT NULL
  status TEXT NOT NULL DEFAULT 'candidate' CHECK (status IN ('candidate','shadow_only','active','deprecated'))
  enabled BOOLEAN NOT NULL DEFAULT false
  parent_concept_id UUID NULL REFERENCES concept_registry(concept_id)   <- single-parent, the L-8 gap
  redundancy_group TEXT, metadata JSONB, added_phase TEXT, sensitivity TEXT, created_at TIMESTAMPTZ
  UNIQUE (domain, name)

`concept_gate` (22 columns today), PK `concept_id` FK to concept_registry:
  gate_metric_name, gate_eval_method (CHECK IN 'oos_holdout','walk_forward','bootstrap_ci'),
  min_gate_metric, min_gate_n, min_promotion_consecutive, min_new_observations,
  demotion_threshold, decay_floor, regime_scope, fdr_required, fdr_alpha,
  last_eval_metric, last_eval_n, last_eval_at, last_eval_corpus_build_ref,
  baseline_metric, decay_ratio, promotion_consecutive, promotion_eval_metrics, updated_at
  NO consecutive_shadow_passes, NO observations_since_demotion   <- the L-5 gap

`concept_transition_log` — TimescaleDB hypertable on triggered_at, PK (id, triggered_at):
  id BIGSERIAL, concept_id UUID NOT NULL FK, domain TEXT NOT NULL, name TEXT NOT NULL,
  from_status TEXT NOT NULL, to_status TEXT NOT NULL, trigger_reason TEXT NOT NULL
  CHECK (trigger_reason IN ('promotion','demotion_performance','demotion_decay',
                            'demotion_redundancy','operator_override','parent_cascade',
                            'candidate_timeout','implementation_change','genesis_seed'))
  corpus_build_ref, gate_metric, gate_n, ci_lower, decay_ratio, regime_scope,
  triggered_at TIMESTAMPTZ NOT NULL DEFAULT now(), notes TEXT
  ('parent_cascade' is ALREADY in the CHECK — no CHECK change needed for L-9.)

Live `feature_registry` mechanism being replaced (`\d feature_registry`, and
`SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname='fn_cascade_parent_deprecation'`):
  parent_features TEXT[]  — 8 rows non-NULL, each with exactly 2 parents (16 edges total)
  is_control BOOLEAN NOT NULL DEFAULT false (5 rows true), control_expectation TEXT
      CHECK-free today, live values: 'negative_control', 'positive_control'
  consecutive_shadow_passes INTEGER NOT NULL DEFAULT 0 (all 0 today)
  observations_since_demotion BIGINT NOT NULL DEFAULT 0 (all 0 today)
  group_name TEXT NOT NULL with an 11-value CHECK
      ('momentum','volume','volatility','structure','session','oscillator','calendar',
       'cross_tf','macro','regime','control')
  trg_cascade_parent_deprecation AFTER UPDATE OF status -> fn_cascade_parent_deprecation()

`fn_cascade_parent_deprecation()` body (verbatim behaviour to be generalized, NOT copied):
  IF NEW.status='deprecated' AND OLD.status<>'deprecated' THEN
      UPDATE feature_registry SET status='deprecated'
        WHERE tier='1_interaction' AND status<>'deprecated' AND NEW.feature_name = ANY(parent_features);
      INSERT INTO feature_transition_log (feature_name, from_status, to_status, trigger_reason)
        SELECT feature_name, 'active', 'deprecated', 'parent_cascade'
        FROM feature_registry WHERE tier='1_interaction' AND NEW.feature_name = ANY(parent_features);
  END IF;

TWO REAL BUGS in that live function, and in the replacement SQL todo 118 L-9 proposes (which is a
near-literal translation of it). Both MUST be fixed in the replacement, not carried forward:
  BUG-1 (ordering): the audit INSERT runs AFTER the UPDATE. In todo 118's proposed version the
        INSERT additionally filters `WHERE cr.status != 'deprecated'` — by then every cascaded
        child IS 'deprecated', so that INSERT selects ZERO rows and the cascade is silently
        unaudited. Fix: log first, then update.
  BUG-2 (fabricated from_status): both versions hard-code `'active'` as from_status. A
        `shadow_only` or `candidate` child cascaded to `deprecated` gets an audit row claiming a
        transition that never happened — corrupting the exact audit trail this table exists to
        keep trustworthy (canonical doc Invariant 9's stated rationale). Fix: select the child's
        real current `status`.

Migration conventions (from `production/migrations/282_compression_auditor_apr.sql` and
`225_concept_registry_mvp.sql`): file named `NNN_snake_case_description.sql`; leading `--`
comment block stating migration number, todo/phase provenance, and WHY each change exists;
single `BEGIN;` ... `COMMIT;`; re-runnable (`IF NOT EXISTS` / `ON CONFLICT DO NOTHING`) where
possible. Migrations are applied by hand:
  `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/<file>`
There is no `schema_migrations` tracking table in this project.

`tests/integration/test_migration_schema_sync.py` diffs the live `indicagent` schema against a
replay of `production/migrations/` into `indicagent_test`. Anything applied to live must exist
verbatim in the committed migration file, or that test fails.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and apply migration 283 (concept_parent, cycle guard, cascade trigger, identity + recovery columns)</name>
  <files>production/migrations/283_concept_registry_feature_domain_schema.sql</files>
  <read_first>
    - .planning/todos/pending/118-migrate-feature-domain-into-concept-registry.md (L-5, L-8, L-9,
      L-10 — the source-of-truth designs; L-9's SQL carries BUG-1/BUG-2 above, fix both)
    - production/migrations/225_concept_registry_mvp.sql (in full — the table definitions being
      extended, the hypertable declaration, the `-- provenance` comment style, the
      config_schema/config_state/config_history triad idiom with its `WHERE NOT EXISTS` guard)
    - production/migrations/282_compression_auditor_apr.sql (most recent migration: header shape,
      BEGIN/COMMIT framing)
    - production/migrations/209_feature_registry_lifecycle_columns.sql (how
      consecutive_shadow_passes / observations_since_demotion were originally defined — match
      types exactly: INTEGER and BIGINT, both NOT NULL DEFAULT 0)
  </read_first>
  <action>
    First confirm the migration number is free: `ls production/migrations/ | sort -V | tail -3`.
    Expect `282_compression_auditor_apr.sql` last; use 283. If 283 is taken, use the next free
    number and rename the file accordingly (keep the `_concept_registry_feature_domain_schema`
    suffix).

    Write `production/migrations/283_concept_registry_feature_domain_schema.sql` with a header
    comment block citing: Phase 170, todo 118 items L-5/L-8/L-9/L-10, and an explicit note that
    it deliberately seeds zero `domain='feature'` rows (that is migration 284). Include in the
    header the BUG-1/BUG-2 explanation above, so the deviation from todo 118's literal SQL is
    self-documenting.

    Inside one BEGIN/COMMIT, in this order:

    (1) L-8 lineage join table:
        CREATE TABLE IF NOT EXISTS concept_parent (
            child_concept_id  UUID NOT NULL REFERENCES concept_registry(concept_id),
            parent_concept_id UUID NOT NULL REFERENCES concept_registry(concept_id),
            created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
            PRIMARY KEY (child_concept_id, parent_concept_id),
            CONSTRAINT concept_parent_no_self_check CHECK (child_concept_id <> parent_concept_id)
        );
        CREATE INDEX IF NOT EXISTS concept_parent_parent_idx ON concept_parent (parent_concept_id);
        Comment that `concept_registry.parent_concept_id` (the single-parent FK column) is
        superseded by this table but is NOT dropped here — dropping it is out of scope for this
        phase because `domain='ensemble_strategy'` may still reference it; the feature domain
        uses `concept_parent` exclusively.
        Comment also that the table carries NO ordinality column and that this is deliberate: no
        consumer of lineage depends on parent ORDER (Plan 07 traces the single reader end to end
        and proves it with an invariance test). If a future consumer ever does, the fix is an
        explicit ordinality column seeded from `unnest(...) WITH ORDINALITY`, not an implicit
        reliance on insertion order.

    (2) L-8 cycle guard — `fn_concept_parent_cycle_guard()` plpgsql, BEFORE INSERT OR UPDATE ON
        concept_parent FOR EACH ROW, trigger name `trg_concept_parent_cycle_guard`. Body: raise
        if NEW.parent_concept_id is reachable as a DESCENDANT of NEW.child_concept_id, i.e.
            IF EXISTS (
                WITH RECURSIVE descendants(id) AS (
                    SELECT NEW.child_concept_id
                    UNION
                    SELECT cp.child_concept_id
                    FROM concept_parent cp JOIN descendants d ON cp.parent_concept_id = d.id
                )
                SELECT 1 FROM descendants WHERE id = NEW.parent_concept_id
            ) THEN
                RAISE EXCEPTION 'concept_parent cycle rejected: parent % is already a descendant of child %',
                    NEW.parent_concept_id, NEW.child_concept_id
                    USING ERRCODE = 'check_violation';
            END IF;
        Comment WHY this exists: the cascade trigger in (4) is deliberately recursive, and
        recursion is only safe because the lineage DAG has no cycles — this guard makes that an
        enforced invariant, not an assumption.

        Comment the guard's KNOWN LIMIT explicitly, in the migration header as well as at the
        trigger, so nobody later mistakes it for stronger than it is:
          The recursive check reads only COMMITTED edges. Two concurrent transactions that each
          insert one leg of a two-edge cycle (A->B in one, B->A in the other) can therefore both
          pass and both commit, because neither sees the other's uncommitted row. Closing that
          would require SERIALIZABLE isolation or a table-level advisory lock on every edge write.
          Neither is warranted in this phase: `concept_parent`'s ONLY writer is migration 284,
          which inserts all 16 edges inside a single transaction, and no runtime service writes
          lineage edges. State the contract that follows — any future component that writes
          `concept_parent` outside a single-transaction seed MUST take
          `pg_advisory_xact_lock()` on a fixed key covering the table first, or the cycle guard
          is best-effort for that writer. Recording that contract here is what keeps the
          limitation a known, bounded design decision rather than a latent surprise.

    (3) L-10 + group identity columns on concept_registry:
        ALTER TABLE concept_registry
            ADD COLUMN IF NOT EXISTS is_control BOOLEAN NOT NULL DEFAULT false,
            ADD COLUMN IF NOT EXISTS control_expectation TEXT,
            ADD COLUMN IF NOT EXISTS group_name TEXT;
        ALTER TABLE concept_registry ADD CONSTRAINT concept_registry_control_expectation_check
            CHECK (control_expectation IS NULL OR control_expectation IN ('negative_control','positive_control'));
        CREATE INDEX IF NOT EXISTS concept_registry_group_name_idx ON concept_registry (group_name);
        CREATE INDEX IF NOT EXISTS concept_registry_domain_status_idx ON concept_registry (domain, status);
        `group_name` is deliberately UNCONSTRAINED TEXT here (no 11-value CHECK): the vocabulary
        is domain-specific and `concept_registry` is cross-domain; the controlled-vocabulary
        system (`vocabulary_drift.py`, Phase 161) is the mechanism that polices its values, not a
        table CHECK. State this in a column comment.
        PLANNER NOTE, record verbatim in the migration header: `group_name` is NOT named in todo
        118's "folds into metadata JSONB" list, but it is not decorative either — it is read with
        real SQL-level filtering by `ops_ic_shrinkage.py` (leave-one-out peer-group shrinkage, a
        live corpus-pipeline step), `ops_ensemble_ablation.py` (`SELECT DISTINCT group_name`,
        family labels) and `ops_broadcast_feature_audit.py`. It is given a dedicated column by
        the same rule todo 118 applies to `is_control` ("both scripts do real WHERE filtering
        today; moving to JSONB predicates would be a pure regression for no benefit").
        `tier` DOES follow todo 118 and goes to `metadata` JSONB in Plan 03 — do not add a tier
        column here.

    (4) L-9 cascade trigger — `fn_cascade_concept_parent_deprecation()` plpgsql, AFTER UPDATE OF
        status ON concept_registry FOR EACH ROW, trigger name
        `trg_cascade_concept_parent_deprecation`. Body, guarded by
        `IF NEW.status = 'deprecated' AND OLD.status <> 'deprecated' THEN`, executes the audit
        INSERT FIRST and the UPDATE SECOND (BUG-1 fix), and selects the child's real status as
        from_status (BUG-2 fix):
            INSERT INTO concept_transition_log
                (concept_id, domain, name, from_status, to_status, trigger_reason, triggered_at, notes)
            SELECT cr.concept_id, cr.domain, cr.name, cr.status, 'deprecated', 'parent_cascade', NOW(),
                   'Cascaded from parent concept ' || NEW.name || ' (domain ' || NEW.domain || ')'
            FROM concept_registry cr
            JOIN concept_parent cp ON cp.child_concept_id = cr.concept_id
            WHERE cp.parent_concept_id = NEW.concept_id AND cr.status <> 'deprecated';

            UPDATE concept_registry
            SET status = 'deprecated', enabled = false
            WHERE status <> 'deprecated'
              AND concept_id IN (SELECT child_concept_id FROM concept_parent
                                 WHERE parent_concept_id = NEW.concept_id);
        Comment the two deliberate generalizations over the feature_registry original: (a) no
        `tier = '1_interaction'` filter — scoping comes from the join table, so cascade works
        across every domain automatically; (b) the inner UPDATE re-fires this same trigger per
        touched child, so multi-level chains cascade with no extra code (safe only because of
        the cycle guard in (2)).

    (5) L-5 recovery counters on concept_gate:
        ALTER TABLE concept_gate
            ADD COLUMN IF NOT EXISTS consecutive_shadow_passes INTEGER NOT NULL DEFAULT 0,
            ADD COLUMN IF NOT EXISTS observations_since_demotion BIGINT NOT NULL DEFAULT 0;
        Column comments citing Phase 143's LIFECYCLE-01 shadow_only -> active recovery mechanics
        and `alpha.decay.recovery_min_passes` / `alpha.decay.recovery_min_observations` as the
        floors these are compared against.

    (6) APR: no new keys. State explicitly in the header that the feature domain reuses the
        EXISTING `alpha.decay.recovery_min_observations` (2000) and `alpha.decay.recovery_min_passes`
        (2) keys rather than minting `alpha.concept_registry.feature_*` siblings, because those two
        keys already ARE the feature-recovery floors that `ic_engine` reads today — a second pair
        would be two sources of truth for one threshold.

    Apply it:
      PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent \
        -f production/migrations/283_concept_registry_feature_domain_schema.sql
    A non-zero exit or any ERROR line means the migration is wrong — fix the file and re-apply,
    never hand-patch the live DB (test_migration_schema_sync.py will catch the drift).
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM information_schema.columns WHERE table_name='concept_gate' AND column_name IN ('consecutive_shadow_passes','observations_since_demotion')" | grep -qx 2 &amp;&amp; PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM information_schema.columns WHERE table_name='concept_registry' AND column_name IN ('is_control','control_expectation','group_name')" | grep -qx 3 &amp;&amp; PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM pg_proc WHERE proname IN ('fn_cascade_concept_parent_deprecation','fn_concept_parent_cycle_guard')" | grep -qx 2 &amp;&amp; PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT to_regclass('public.concept_parent') IS NOT NULL"</automated>
  </verify>
  <acceptance_criteria>
    - `production/migrations/283_concept_registry_feature_domain_schema.sql` exists and is committed.
    - `\d concept_parent` shows: PK `(child_concept_id, parent_concept_id)`, two FKs to
      `concept_registry(concept_id)`, CHECK `concept_parent_no_self_check`, index
      `concept_parent_parent_idx`, and trigger `trg_concept_parent_cycle_guard`.
    - `trg_concept_parent_cycle_guard` is declared `BEFORE INSERT OR UPDATE` — the UPDATE arm is
      not optional, and Task 2 test 5 exercises it.
    - The migration header records the cycle guard's committed-reads-only concurrency limit and
      the advisory-lock contract for any future non-seed writer.
    - `\d concept_registry` shows `is_control boolean not null default false`,
      `control_expectation text` with CHECK `concept_registry_control_expectation_check`,
      `group_name text`, and trigger `trg_cascade_concept_parent_deprecation`.
    - `\d concept_gate` shows `consecutive_shadow_passes integer not null default 0` and
      `observations_since_demotion bigint not null default 0`.
    - `SELECT count(*) FROM concept_registry WHERE domain='feature'` returns **0** (this plan
      seeds nothing).
    - `SELECT count(*) FROM concept_registry` still returns 5 and
      `SELECT count(*) FROM concept_gate` still returns 5 — no existing ensemble_strategy row
      was disturbed.
    - `pg_get_functiondef` for `fn_cascade_concept_parent_deprecation` contains the audit
      `INSERT INTO concept_transition_log` textually BEFORE the `UPDATE concept_registry`, and
      contains `cr.status` (not the literal `'active'`) in the from_status position.
    - The function body contains no `tier` string filter.
  </acceptance_criteria>
  <done>Migration 283 is written, committed, and applied to `indicagent`; all four schema
  capabilities exist; zero feature rows seeded; existing ensemble_strategy rows untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the cycle guard and the cascade trigger against a real database</name>
  <files>tests/unit/test_concept_parent_lineage.py</files>
  <read_first>
    - production/migrations/283_concept_registry_feature_domain_schema.sql (the file just written)
    - tests/unit/test_concept_registry_service.py (existing test conventions for this subsystem:
      fixture shape, how a DB-backed vs pure-helper test is structured, marker usage)
    - tests/integration/conftest.py (the `migrated_test_database` fixture and the pinned-baseline
      + post-baseline-replay mechanism — migration 283 is post-baseline so it replays)
  </read_first>
  <action>
    Write `tests/unit/test_concept_parent_lineage.py`. If the existing concept-registry tests in
    `tests/unit/test_concept_registry_service.py` are pure-helper tests with no DB, this file is
    the first DB-touching one in that family: put it under the `integration` pytest marker and
    the `indicagent_test` database fixture from `tests/integration/conftest.py` instead, moving
    the file to `tests/integration/test_concept_parent_lineage.py`. Decide by reading both
    conftest files; do not invent a third fixture mechanism.

    Every test creates its own throwaway concept rows with unique names
    (`f"_t170_{uuid4().hex[:8]}"`), `domain='ensemble_strategy'` (already-allowed domain, keeps
    this test independent of Plan 03's feature seeding), and cleans up in a fixture teardown or
    inside an aborted transaction. Never mutate the 5 real seeded rows.

    Five required tests:

    1. `test_multi_parent_lineage_allowed` — one child with TWO parents inserts cleanly; both
       edges present; `SELECT count(*) FROM concept_parent WHERE child_concept_id = :child` == 2.
       This is the capability `concept_registry.parent_concept_id` structurally could not express.

    2. `test_cycle_rejected` — build A -> B (B child of A), then attempt to insert A as a child of
       B. Assert `asyncpg.exceptions.CheckViolationError` (or the raised
       `PostgresError` whose message contains `concept_parent cycle rejected`). Also assert the
       direct self-edge (child == parent) is rejected by
       `concept_parent_no_self_check`.

    3. `test_cascade_single_level_audits_true_from_status` — parent P (status 'active') with two
       children: C1 status 'active', C2 status 'shadow_only'. UPDATE P to 'deprecated'. Assert:
       both children are now 'deprecated'; `concept_transition_log` has exactly one
       `trigger_reason='parent_cascade'` row per child; C1's row has `from_status='active'` and
       **C2's row has `from_status='shadow_only'`** (this is the BUG-2 regression test — the old
       feature_registry trigger would have written 'active' for both); both rows carry the
       correct `domain` and `name`.

    4. `test_cascade_multi_level` — chain G -> P -> C (C child of P, P child of G), all 'active'.
       UPDATE G to 'deprecated'. Assert P and C are BOTH 'deprecated' and that
       `concept_transition_log` holds a `parent_cascade` row for each of P and C. This is the
       trigger-recursion property; without it, only P would flip.

    5. `test_cycle_rejected_on_update_path` — the guard is declared
       `BEFORE INSERT OR UPDATE`, so its UPDATE arm needs its own coverage; an insert-only test
       suite would leave half the trigger's declared surface unexercised. Build A -> B -> C
       (three concepts, two edges, no cycle), then `UPDATE concept_parent SET parent_concept_id =
       <C> WHERE child_concept_id = <B> AND parent_concept_id = <A>` — repointing B's parent to
       its own descendant. Assert the same cycle rejection fires, and assert afterwards that the
       original edge is intact (the failed UPDATE left no partial state).

    Also assert in test 3 that each cascaded child's `enabled` is now `false`.

    NOT covered, deliberately, and stated in the module docstring rather than left silent: a
    concurrent two-transaction cycle (A->B and B->A inserted simultaneously by two sessions) is
    NOT tested and NOT prevented, because the recursive guard reads only committed edges. That is
    a documented, bounded limit recorded in migration 283's header, and it is acceptable because
    `concept_parent`'s only writer is migration 284's single-transaction seed. Reference the
    advisory-lock contract from that header so a future writer finds the requirement instead of
    rediscovering the hole.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_concept_parent_lineage.py tests/integration/test_concept_parent_lineage.py -q 2>/dev/null | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All five named tests exist and pass: `test_multi_parent_lineage_allowed`,
      `test_cycle_rejected`, `test_cascade_single_level_audits_true_from_status`,
      `test_cascade_multi_level`, `test_cycle_rejected_on_update_path`.
    - `test_cascade_single_level_audits_true_from_status` asserts a literal `'shadow_only'`
      from_status for the shadow child — a test that only checks 'active' does not close BUG-2
      and is not acceptable.
    - `test_cycle_rejected_on_update_path` exercises an actual `UPDATE concept_parent` (not a
      second INSERT) and additionally asserts the pre-existing edge survived the rejection.
    - The module docstring states the concurrent-insertion limit and points at migration 283's
      advisory-lock contract — the gap is recorded, not silently unaddressed.
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM concept_registry"`
      returns 5 after the suite runs (no test residue in the live DB; tests target
      `indicagent_test`).
    - `.venv/bin/ruff check tests/ --fix` and `.venv/bin/black tests/` are clean.
  </acceptance_criteria>
  <done>The cycle guard and cascade trigger are proven by executed tests on both the insert and
  update paths, including the multi-level recursion property and the true-from_status audit
  fidelity that the feature_registry original got wrong, with the one uncovered concurrency case
  explicitly documented rather than silently assumed away.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/automation -> concept_registry.status | the only write that can cascade-deprecate other concepts |
| migration file -> live indicagent DB | DDL applied by hand with superuser credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-170-01 | Tampering | `fn_cascade_concept_parent_deprecation` recursion | mitigate | `trg_concept_parent_cycle_guard` rejects any edge creating a cycle on both the INSERT and UPDATE paths, each with its own test; without it a cyclic DAG turns one status flip into unbounded trigger recursion (a self-inflicted DoS on the write path) |
| T-170-02 | Repudiation | `concept_transition_log` parent_cascade rows | mitigate | audit INSERT runs before the UPDATE and selects the child's real `status` as `from_status`, so no cascade is unlogged and no logged transition is fabricated |
| T-170-03 | Elevation of Privilege | `concept_parent` referential integrity | mitigate | FK constraints on BOTH `child_concept_id` and `parent_concept_id` make an orphaned lineage reference structurally impossible, replacing `feature_registry.parent_features`' unenforced name-string array |
| T-170-04 | Tampering | concurrent cycle insertion | accept | the recursive guard reads only committed edges, so two simultaneous transactions could each insert one leg of a cycle. Accepted for this phase: `concept_parent`'s sole writer is migration 284's single-transaction seed and no runtime service writes edges. The limit and the `pg_advisory_xact_lock()` requirement for any future non-seed writer are recorded in migration 283's header and the lineage test module's docstring |
| T-170-SC | Tampering | package installs | accept | this plan installs no npm/pip/cargo packages; no legitimacy gate applies |
</threat_model>

<verification>
1. `\d concept_parent`, `\d concept_registry`, `\d concept_gate` against live `indicagent` show
   every column/constraint/trigger listed in Task 1's acceptance criteria.
2. `.venv/bin/pytest tests/unit/ -q` is fully green (no collateral breakage — nothing in the
   application layer reads these new columns yet, so any failure here is a real regression).
3. `SELECT count(*) FROM concept_registry WHERE domain='feature'` == 0.
</verification>

<success_criteria>
The four `feature_registry`-vs-`concept_registry` schema gaps (L-5, L-8, L-9, L-10) are closed at
the database level, the cascade and cycle mechanisms are proven by executed tests across both of
the cycle guard's declared write paths, the one uncovered concurrency case is a recorded and
bounded decision rather than an unexamined gap, and no application code or existing row has
changed.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/170-concept-registry-feature-domain-migration-feature-registry-r/170-01-SUMMARY.md` when done.
</output>
