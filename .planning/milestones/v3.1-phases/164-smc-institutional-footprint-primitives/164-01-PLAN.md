---
phase: 164-smc-institutional-footprint-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/259_smc_institutional_footprint.sql
  - src/intelligence/schemas.py
  - src/intelligence/feature_factory.py
  - src/intelligence/features/feature_vector_persistence.py
  - src/intelligence/feature_cache.py
  - services/feature_vector_pipeline.py
  - services/backfill_feature_factory.py
autonomous: true
requirements: ["REQ-164-09"]

must_haves:
  truths:
    - "feature_vectors table has 36 new SMC columns (DOUBLE PRECISION), ATR-distance / bounded / count / ordinal only — zero raw _top/_bottom/_level/_midpoint columns"
    - "ic_engine startup drift gate passes: feature_registry feature_name set == FeatureVector dataclass field set"
    - "FeatureVector constructs without crash: all 36 new fields threaded through _build_feature_vector at both compute() and compute_batch() call sites (placeholder None until Plans 02-04 wire real values)"
    - "FeatureCache.update_overnight_range() mutator populates real overnight high/low session state with UTC-20:00 boundary reset; not yet invoked (call sites added in Plan 04)"
    - "New feature.smc.* APR keys exist in config_state; FeatureFactoryConfig carries all SMC params wired from APR in both config build sites; no new hardcoded numeric constants in src/ or services/"
  artifacts:
    - path: "production/migrations/259_smc_institutional_footprint.sql"
      provides: "36 feature_vectors columns + 36 feature_registry rows (group_name='smart_money', added_phase='164') + feature.smc.* APR seeds"
      contains: "ob_bull_dist_atr"
    - path: "src/intelligence/schemas.py"
      provides: "36 new FeatureVector fields as one contiguous smart-money block, all float | None; docstring field-count + group table updated"
      contains: "amd_phase"
    - path: "src/intelligence/feature_cache.py"
      provides: "update_overnight_range() mutator + overnight state fields + amd_phase/amd_manipulation_detected/amd_distribution_direction/manip_strength exposed fields"
      contains: "def update_overnight_range"
    - path: "src/intelligence/features/feature_vector_persistence.py"
      provides: "_SMC_FIELD_NAMES append-only slice threaded through INSERT column list, $N placeholders, _TOTAL_COLUMNS, params tuple"
      contains: "_SMC_FIELD_NAMES"
  key_links:
    - from: "src/intelligence/schemas.py FeatureVector"
      to: "production/migrations/259 feature_registry rows"
      via: "identical field-name set (ic_engine drift gate)"
      pattern: "ob_bull_dist_atr"
    - from: "src/intelligence/features/feature_vector_persistence.py _SMC_FIELD_NAMES"
      to: "FEATURE_VECTOR_INSERT_SQL column list + params tuple"
      via: "derive-by-name slice of dataclasses.fields(FeatureVector)"
      pattern: "_SMC_FIELD_NAMES"
    - from: "src/intelligence/feature_cache.py update_overnight_range"
      to: "FeatureFactoryConfig.smc_* / feature.smc.amd.* APR keys"
      via: "config argument (UTC session-boundary hours)"
      pattern: "overnight"
---

<objective>
Establish the complete data contract for all 8 SMC institutional-footprint primitives so the three
downstream compute plans (02: order-blocks/breaker/mitigation; 03: FVG/sweeps/pools; 04:
zones/BOS-CHoCH/AMD) wire against fixed, in-sync interfaces with zero drift.

This adds 36 new `feature_vectors` columns — ALL ATR-distance / bounded / count / ordinal per the
field-by-field raw-price audit in 164-RESEARCH.md (NEVER a raw `_top`/`_bottom`/`_level`/`_midpoint`
column, the exact D-16 mistake Phase 163 had to correct) — plus matching `feature_registry` rows
(keeping the `ic_engine` drift gate satisfied), the `FeatureVector` dataclass fields threaded through
`_build_feature_vector` with placeholder `None` (so the live/batch pipeline never crashes between
this plan and Plan 04, writing NULLs that backfill later exactly as Phase 163 did), the
`_SMC_FIELD_NAMES` persistence slice, every `feature.smc.*` APR key (migrate-as-you-go for every
hardcoded constant in the archived source), the `FeatureFactoryConfig` fields wired from APR in both
config build sites, and the new `FeatureCache.update_overnight_range()` mutator AMD needs.

Purpose: One atomic contract change so no intermediate state trips the `ic_engine`
registry-vs-dataclass alignment gate, and so Plans 02-04 only replace `None` placeholders with real
computed values — no schema/registry/persistence churn in the compute plans.
Output: Migration 259, updated schema/domain/persistence/config, and a working (not-yet-invoked)
overnight-range mutator.
</objective>

<source_coverage_audit>
Multi-source coverage for the whole Phase 164 plan set (01-04). Every source item is COVERED.

GOAL (ROADMAP Phase 164): Port 8 archived SMC plugins into atomic distance/strength/duration/count
primitives. COVERED — OB/breaker/mitigation (Plan 02), FVG/sweeps/pools (Plan 03),
zones/BOS-CHoCH/AMD (Plan 04); contract for all (Plan 01).

REQ (derived, no REQUIREMENTS.md exists — per Phase 163 precedent):
- REQ-164-01 Order Blocks -> Plan 02
- REQ-164-02 Breaker/Mitigation (stateless, derived from OB) -> Plan 02
- REQ-164-03 Fair Value Gap -> Plan 03
- REQ-164-04 Liquidity Sweeps -> Plan 03
- REQ-164-05 Liquidity Pools (single-tf descoped) -> Plan 03
- REQ-164-06 Supply/Demand Zones -> Plan 04
- REQ-164-07 BOS/CHoCH -> Plan 04
- REQ-164-08 AMD Cycle -> Plan 04
- REQ-164-09 Data contract (migration/schema/persistence/APR/drift-gate/mutator) -> Plan 01

RESEARCH (164-RESEARCH.md) constraints: cross-plugin ordering OB->breaker/mitigation (hard),
FVG+pools->zones (soft) -> honored by wave/plan sequencing; PWH/PWL/PDH/PDL descope -> Plan 03;
stateless derivation (no self._state) -> Plans 02/04; clamp manip_strength, ordinal-encode amd_phase,
drop ob_mitigation_status/bsl_type/ssl_type/bos_confidence -> Plans 01/04; migration 259 re-verify
-> Plan 01 Task 1. ALL COVERED.

CONTEXT: No CONTEXT.md; ROADMAP's Phase 164 section carries the decision set. Exclusions are NOT
gaps: premium_discount, archive hmm_regime, ict_killzones, bocpd_changepoint (ROADMAP "explicitly
excluded"); PWH/PWL/PDH/PDL named levels (RESEARCH descope, needs cross-tf injection, out of scope);
historical feature_vectors backfill (deferred to consolidated 163/164/165 pass, todo 176); shared
collinearity/incremental-IC sweep (phase-exit follow-up, ROADMAP promotion-bar note).
</source_coverage_audit>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-RESEARCH.md
@.planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Exact analogs verified in 164-PATTERNS.md (2026-07-25). Use directly; no exploration needed. -->

Migration analog — production/migrations/255_vp_structural_primitives.sql:
  Section 1: ALTER TABLE feature_vectors ADD COLUMN IF NOT EXISTS <name> DOUBLE PRECISION; + COMMENT ON COLUMN (lines 56-107). DOUBLE PRECISION not `real` — every column added after migration 201 is double; ADD COLUMN NULL against the compressed hypertable is metadata-only (no decompress step).
  Section 2: feature_registry INSERT — columns (feature_name, group_name, tier, formula_short, normalization, linear_ready, requires_htf, status, added_phase); ON CONFLICT (feature_name) DO NOTHING (lines 116-153).
  Section 3: APR 3-table seed — config_schema (config_key, value_type, default_value, min_value, max_value, description) + config_state (config_key, config_value, version) + config_history (timestamp, config_key, version, config_value, changed_by='migration_259', reason); all ON CONFLICT DO NOTHING (lines 159-249).

FeatureVector — src/intelligence/schemas.py (frozen dataclass, ALL fields float|None with NO default):
  Phase 163 session block precedent lines 1264-1291 (resistance_strength...sr_level_count).
  Class docstring "Groups and field order are binding" table + "Total: N" line (currently 172).
  _build_feature_vector() at feature_factory.py:3417+ — zero defaults; every new field is a required kwarg (missing => TypeError at call time). Two call sites: compute() ~3932+, compute_batch() loop ~4200.

FeatureCache — src/intelligence/feature_cache.py:
  update_wk_vwap() lines 165-188 — session-boundary-reset accumulator TEMPLATE (ISO-week key -> replace with UTC-hour-rollover key).
  update_session_vp() lines 190-272 — "store raw state, derive normalized later in compute()" split precedent.
  Exposed non-underscore float attrs read by compute()/_build_feature_vector: above_wk_vwap (L64), poc_dist_atr (L67).
  utc_datetime_from_df (src/intelligence/utils.py) — already live, extracts bar UTC hour.

FEATURE_VECTOR_DOMAIN — feature_factory.py lines 84-106 (one "<field>": "<domain>" entry per field).

Persistence — src/intelligence/features/feature_vector_persistence.py:
  _STRUCTURAL_VP_SR_FIELD_NAMES slice lines 102-113 (derive-by-name via _ALL_FEATURE_VECTOR_FIELD_NAMES.index(first):index(last)+1).
  Threaded through: FEATURE_VECTOR_INSERT_SQL column list (~L179), $N placeholder generator (~L210-221), _TOTAL_COLUMNS (~L235), feature_vector_to_insert_params return tuple (~L438).
  Guard test: tests/unit/test_feature_vector_persistence_completeness.py (structural, auto-covers new fields).

FeatureFactoryConfig — feature_factory.py lines 488-504 (defaulted dataclass fields, e.g. session_vp_*, sr_lookback_by_tf=field(default_factory=...)).

Config wiring:
  Live — services/feature_vector_pipeline.py: _THRESHOLD_KEYS tuple (L589-600) + _prewarm_threshold_config() build (L602-621, 723-733), _int/_float/_dict helpers.
  Batch — services/backfill_feature_factory.py: inline cfg.get_sync(key, default) block (L498-506); _get_dict_config() shared JSON-dict parser.

ic_engine drift gate — services/ic_engine.py (~L3259-3265): feature_registry.feature_name set MUST equal FeatureVector dataclass field set, else RuntimeError at startup.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 259 columns+registry + FeatureVector/domain/persistence contract for 36 SMC fields</name>
  <files>production/migrations/259_smc_institutional_footprint.sql, src/intelligence/schemas.py, src/intelligence/feature_factory.py, src/intelligence/features/feature_vector_persistence.py</files>
  <read_first>
    - production/migrations/255_vp_structural_primitives.sql (FULL — the 3-section template + DOUBLE PRECISION header note + migration-collision warning)
    - src/intelligence/schemas.py lines 1215-1291 (docstring group table + "Total:" line + Phase 163 session block to append after)
    - src/intelligence/feature_factory.py lines 84-106 (FEATURE_VECTOR_DOMAIN), 3417-3470 (_build_feature_vector signature), and both call sites (compute() ~3932+, compute_batch() loop ~4200)
    - src/intelligence/features/feature_vector_persistence.py lines 90-240 and ~430-445 (3-slice chain + INSERT SQL + $N generator + _TOTAL_COLUMNS + params tuple + docstring changelog)
    - services/ic_engine.py lines ~3250-3270 (drift gate)
  </read_first>
  <action>
    FIRST re-verify the migration number: run `ls production/migrations/ | sort -V | tail -3`; if 259 is taken, use the next free number and update the filename + all `migration_<N>` references. RESEARCH Open Question 3 flags this — the project hit migration-number collisions twice before.

    Add exactly these 36 SMC columns/fields. NONE is a raw price level (per the 164-RESEARCH.md Field-by-Field Raw-Price Audit — never a `_top`/`_bottom`/`_level`/`_midpoint`/`_high`/`_low`). Names are the binding contract Plans 02-04 must produce verbatim (drift gate is exact-set equality):
    - Order Blocks (Plan 02): ob_bull_dist_atr, ob_bear_dist_atr, ob_strength, ob_mitigated_flag
    - Breaker/Mitigation (Plan 02): breaker_dist_atr, breaker_block_active, ob_mitigation_pct
    - Fair Value Gap (Plan 03): fvg_dist_atr, fvg_size_atr, fvg_open_count
    - Liquidity Sweeps (Plan 03): sweep_detected, sweep_strength, reclaim_velocity, bars_since_last_sweep
    - Liquidity Pools (Plan 03): bsl_dist_atr, ssl_dist_atr, bsl_touches, ssl_touches, pool_count
    - Supply/Demand Zones (Plan 04): demand_dist_atr, supply_dist_atr, demand_freshness, supply_freshness, active_demand_zones, active_supply_zones, zone_friction_score
    - BOS/CHoCH (Plan 04): bos_strength, choch_strength, bos_direction, choch_direction, smc_trend_direction, bars_since_last_shift
    - AMD Cycle (Plan 04): amd_phase, amd_manipulation_detected, amd_distribution_direction, manip_strength

    Dropped-as-redundant per the audit (do NOT add): ob_mitigation_status (redundant with ob_mitigation_pct), bsl_type/ssl_type (byte-identical to significance), bos_confidence (byte-identical to bos_strength), any raw `_level`/`_top`/`_bottom`/`_midpoint` field, and NO separate bos_dist_atr (A3: bos_strength substitutes).

    (a) Create production/migrations/259_smc_institutional_footprint.sql mirroring migration 255's 3-section structure. Section 1: `ALTER TABLE feature_vectors ADD COLUMN IF NOT EXISTS <name> DOUBLE PRECISION;` + one COMMENT ON COLUMN per field documenting its formula (e.g. ob_bull_dist_atr = abs(close - nearest_bull_ob_mid)/ATR; amd_phase ordinal map 0.0=unknown/1.0=accumulation/2.0=manipulation/3.0=distribution per RESEARCH Pitfall 4). Section 2: 36 feature_registry rows, group_name='smart_money', tier='2_theory', status='active', added_phase='164', ON CONFLICT (feature_name) DO NOTHING; normalization tags — z_scored for the 11 ATR-distance/magnitude fields (ob_bull_dist_atr, ob_bear_dist_atr, breaker_dist_atr, fvg_dist_atr, fvg_size_atr, bsl_dist_atr, ssl_dist_atr, demand_dist_atr, supply_dist_atr, bos_strength, choch_strength); bounded_unsigned for the 12 [0,1]/flag fields (ob_strength, ob_mitigated_flag, breaker_block_active, ob_mitigation_pct, sweep_detected, sweep_strength, reclaim_velocity, demand_freshness, supply_freshness, zone_friction_score, amd_manipulation_detected, manip_strength); unbounded_ratio for the 8 count/age fields (fvg_open_count, bars_since_last_sweep, bsl_touches, ssl_touches, pool_count, active_demand_zones, active_supply_zones, bars_since_last_shift); none for the 5 categorical direction/ordinal fields (bos_direction, choch_direction, smc_trend_direction, amd_phase, amd_distribution_direction). linear_ready=false, requires_htf=false for all. Leave Section 3 (APR seeds) to Task 2 — same file, appended after.
    (b) src/intelligence/schemas.py: add all 36 fields as ONE contiguous commented block (`# Session-level — Smart Money Concepts (36, Phase 164 Plan 01)`) appended after the Phase 163 session block (immediately before the 3 nullable cross-sectional fields, matching where canary/163 fields were appended). All typed `float | None`. Update the docstring group table with the new block and bump "Total:" (172 -> 208). Contiguity is REQUIRED for the persistence slice technique.
    (c) src/intelligence/feature_factory.py: add 36 `"<field>": "smart_money"` entries to FEATURE_VECTOR_DOMAIN. Thread all 36 into _build_feature_vector's signature AND into BOTH its call sites (compute() and compute_batch()) as placeholder `None` (e.g. `ob_bull_dist_atr=None, ...`). No SMC compute logic in this plan.
    (d) src/intelligence/features/feature_vector_persistence.py: add a 4th slice `_SMC_FIELD_NAMES` after `_STRUCTURAL_VP_SR_FIELD_NAMES`, derived by name as `_ALL_FEATURE_VECTOR_FIELD_NAMES[index("ob_bull_dist_atr"):index("manip_strength")+1]`. Thread it through all 4 places `_STRUCTURAL_VP_SR_FIELD_NAMES` is used: INSERT column list, $N placeholder range, _TOTAL_COLUMNS, params-tuple unpack `*(getattr(vector, name) for name in _SMC_FIELD_NAMES)`. Never hand-type a column list. Add a dated changelog entry to the module docstring for Phase 164's column count.
  </action>
  <verify>
    <automated>ls production/migrations/ | sort -V | tail -3 && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/259_smc_institutional_footprint.sql && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM information_schema.columns WHERE table_name='feature_vectors' AND column_name IN ('ob_bull_dist_atr','ob_bear_dist_atr','ob_strength','ob_mitigated_flag','breaker_dist_atr','breaker_block_active','ob_mitigation_pct','fvg_dist_atr','fvg_size_atr','fvg_open_count','sweep_detected','sweep_strength','reclaim_velocity','bars_since_last_sweep','bsl_dist_atr','ssl_dist_atr','bsl_touches','ssl_touches','pool_count','demand_dist_atr','supply_dist_atr','demand_freshness','supply_freshness','active_demand_zones','active_supply_zones','zone_friction_score','bos_strength','choch_strength','bos_direction','choch_direction','smc_trend_direction','bars_since_last_shift','amd_phase','amd_manipulation_detected','amd_distribution_direction','manip_strength')" | grep -qx 36</automated>
    <automated>.venv/bin/python -c "import dataclasses; from src.intelligence.schemas import FeatureVector; n={f.name for f in dataclasses.fields(FeatureVector)}; req={'ob_bull_dist_atr','ob_bear_dist_atr','ob_strength','ob_mitigated_flag','breaker_dist_atr','breaker_block_active','ob_mitigation_pct','fvg_dist_atr','fvg_size_atr','fvg_open_count','sweep_detected','sweep_strength','reclaim_velocity','bars_since_last_sweep','bsl_dist_atr','ssl_dist_atr','bsl_touches','ssl_touches','pool_count','demand_dist_atr','supply_dist_atr','demand_freshness','supply_freshness','active_demand_zones','active_supply_zones','zone_friction_score','bos_strength','choch_strength','bos_direction','choch_direction','smc_trend_direction','bars_since_last_shift','amd_phase','amd_manipulation_detected','amd_distribution_direction','manip_strength'}; assert req<=n, req-n; print('ok')"</automated>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT string_agg(feature_name, ',' ORDER BY feature_name) FROM feature_registry" > /tmp/registry_164.txt && .venv/bin/python -c "import dataclasses; from src.intelligence.schemas import FeatureVector; reg=set(open('/tmp/registry_164.txt').read().strip().split(',')); dc={f.name for f in dataclasses.fields(FeatureVector)}; assert reg==dc, f'DRIFT registry-only={reg-dc} dataclass-only={dc-reg}'; print('ok drift gate')"</automated>
    <automated>.venv/bin/pytest tests/unit/test_feature_vector_persistence_completeness.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - feature_vectors has exactly 36 new columns as type `double precision` (confirm: `SELECT data_type FROM information_schema.columns WHERE table_name='feature_vectors' AND column_name='ob_bull_dist_atr'` = `double precision`).
    - feature_registry has 36 new rows (added_phase='164', group_name='smart_money'); registry feature_name set == FeatureVector dataclass field set.
    - No column/field named `*_top`, `*_bottom`, `*_level`, `*_midpoint`, and no ob_mitigation_status/bsl_type/ssl_type/bos_confidence/bos_dist_atr anywhere.
    - _SMC_FIELD_NAMES resolves to exactly 36 names; persistence completeness guard green.
    - `.venv/bin/ruff check src/intelligence/schemas.py src/intelligence/feature_factory.py src/intelligence/features/feature_vector_persistence.py` clean.
  </acceptance_criteria>
  <done>36 SMC columns exist end-to-end (DB, registry, dataclass, domain, persistence slice); registry==dataclass; _build_feature_vector threads all 36 as None placeholders so the pipeline constructs without crash; no raw-price or redundant fields.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 259 APR seeds + FeatureFactoryConfig SMC fields + wire both config sites</name>
  <files>production/migrations/259_smc_institutional_footprint.sql, src/intelligence/feature_factory.py, services/feature_vector_pipeline.py, services/backfill_feature_factory.py</files>
  <read_first>
    - production/migrations/255_vp_structural_primitives.sql lines 159-249 (APR 3-table seed with [conventional] provenance)
    - src/intelligence/archive/smc_context/order_blocks.py, fair_value_gap.py, liquidity_sweeps.py, liquidity_pools.py, supply_demand_zones.py, breaker_blocks.py, mitigation_blocks.py, bos_choch.py, amd_cycle.py (SKIM each for hardcoded numerics: `: int =`, `: float =`, InputSpec lookback, ATR multipliers, impulse thresholds, reclaim-bar counts, AMD 20/0/10/21 UTC hours, zone freshness-decay windows)
    - src/intelligence/feature_factory.py lines 488-504 (FeatureFactoryConfig defaulted-field pattern)
    - services/feature_vector_pipeline.py lines 589-621, 723-733 (_THRESHOLD_KEYS + _prewarm _int/_float/_dict)
    - services/backfill_feature_factory.py lines 498-506 (inline cfg.get_sync + _get_dict_config)
  </read_first>
  <action>
    (a) Enumerate every hardcoded numeric constant across the 8 archived SMC plugin files (impulse_bars, impulse_atr_mult, base-body ratios, per-plugin lookback windows, reclaim-bar counts, FVG min-gap thresholds, sweep wick-beyond thresholds, zone freshness-decay windows, AMD's 4 UTC session-boundary hours 20/0/10/21). For each, add a `feature.smc.<concept>.<param>` APR key. Append Section 3 to migration 259: config_schema + config_state + config_history (changed_by='migration_259') for every key, ON CONFLICT DO NOTHING. Every description carries a `[conventional]` provenance tag (values copied verbatim from the archived plugin's unvalidated ICT-community defaults — NOT `[rca_analysis]`), states the owning plan, and notes "Not an ML learning target." Namespace `feature.smc.<concept>.*` per RESEARCH A4 (e.g. feature.smc.order_blocks.impulse_atr_mult, feature.smc.amd.accum_start_utc_hour). Any per-tf lookback that varies by timeframe gets a JSON-text dict value (like feature.sr.lookback_by_tf).
    (b) src/intelligence/feature_factory.py: add one defaulted FeatureFactoryConfig field per APR key (e.g. `smc_ob_impulse_atr_mult: float = <archived default>`), each `= <default>` (never bare — same rationale comment as the existing session_vp_* fields: protects the ~6 direct FeatureFactoryConfig(...) test construction sites). Dict-shaped keys use `field(default_factory=lambda: {...})`.
    (c) services/feature_vector_pipeline.py: add every new `feature.smc.*` key to _THRESHOLD_KEYS with its default, and a matching `_int`/`_float`/`_dict` call inside _prewarm_threshold_config()'s FeatureFactoryConfig(...) construction.
    (d) services/backfill_feature_factory.py: add matching `smc_*=int/float(cfg.get_sync("feature.smc.<concept>.<param>", <default>))` lines (use _get_dict_config for dict-shaped keys) to the config-construction block.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/259_smc_institutional_footprint.sql && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM config_state WHERE config_key LIKE 'feature.smc.%'" | grep -qE '^[1-9]'</automated>
    <automated>.venv/bin/python -c "from src.intelligence.feature_factory import FeatureFactoryConfig; import dataclasses; names={f.name for f in dataclasses.fields(FeatureFactoryConfig)}; assert any(n.startswith('smc_') for n in names), 'no smc_ config fields'; print('ok')"</automated>
    <automated>.venv/bin/ruff check src/intelligence/feature_factory.py services/feature_vector_pipeline.py services/backfill_feature_factory.py</automated>
  </verify>
  <acceptance_criteria>
    - Every hardcoded numeric surfaced during the archived-source skim has a matching `feature.smc.*` APR key in config_state (grep the ported concepts; zero hardcoded numeric thresholds remain to be introduced in Plans 02-04's compute code).
    - FeatureFactoryConfig carries a `smc_*` field per APR key, each with a default equal to the seeded value.
    - Both config build sites (pipeline _prewarm + backfill inline) populate every new `smc_*` field from its `feature.smc.*` key.
    - All descriptions carry `[conventional]` provenance and "Not an ML learning target."
    - ruff clean on the three edited Python files.
  </acceptance_criteria>
  <done>Every SMC hardcoded constant is APR-backed under feature.smc.*; FeatureFactoryConfig carries all SMC params with matching defaults, wired from APR in both live and batch config sites.</done>
</task>

<task type="auto">
  <name>Task 3: FeatureCache.update_overnight_range() mutator + AMD state fields (compute-only, not yet invoked)</name>
  <files>src/intelligence/feature_cache.py</files>
  <read_first>
    - src/intelligence/feature_cache.py lines 60-90 (exposed float attrs like above_wk_vwap/poc_dist_atr), 165-188 (update_wk_vwap boundary-reset template), 190-272 (update_session_vp state-field precedent)
    - src/intelligence/archive/smc_context/amd_cycle.py (FULL — the overnight accumulation + manipulation-detection logic, lines ~81-122)
    - src/intelligence/utils.py (utc_datetime_from_df signature — already imported by archived amd_cycle.py)
  </read_first>
  <action>
    Add a new session-boundary-reset mutator following update_wk_vwap()'s exact shape, keyed on UTC-hour rollover into the 20:00 accumulation window instead of ISO week.

    (a) Add internal state fields (repr=False, NOT FeatureVector fields, analogous to _wk_tp_vol_sum/_wk_vol_sum/_wk_year_week): `_overnight_high: float | None = None`, `_overnight_low: float | None = None`, `_overnight_day: <boundary-key type> = None`.
    (b) Add exposed non-underscore float attrs read later by compute()/_build_feature_vector (matching above_wk_vwap=0.0 / poc_dist_atr=0.0 style): `amd_phase: float = 0.0`, `amd_manipulation_detected: float = 0.0`, `amd_distribution_direction: float = 0.0`, `manip_strength: float = 0.0`.
    (c) Implement `update_overnight_range(self, bar_ts, high, low, config) -> None`: derive the boundary key from bar_ts's UTC hour using the APR-configured session hours (feature.smc.amd.* from Task 2 — NOT hardcoded 20/0/10/21); on boundary-key change into a new accumulation window, reset _overnight_high/_overnight_low; accumulate `_overnight_high = max(prior, high)`, `_overnight_low = min(prior, low)` during the accumulation phase. Port the manipulation-detection logic from amd_cycle.py (breach overnight range then reverse) to set amd_manipulation_detected and a raw manip_strength ratio — but the manip_strength CLAMP to [0,1] and the amd_phase ordinal encoding are applied in the compute() derivation in Plan 04, not here (this mutator only tracks the raw overnight range + phase-transition state). Do NOT carry amd_cycle.py's `self._state` dict pattern — use the typed FeatureCache fields directly. Read every numeric threshold from `config`; introduce zero hardcoded constants.

    NOTE: this mutator is delivered but NOT invoked in this plan — call sites (compute_batch loop, live per-bar handler, and warm-up replay block) are added in Plan 04 alongside AMD's compute derivation.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from src.intelligence.feature_cache import FeatureCache; import inspect; assert hasattr(FeatureCache,'update_overnight_range'); sig=inspect.signature(FeatureCache.update_overnight_range); assert list(sig.parameters)[1:]==['bar_ts','high','low','config'], list(sig.parameters); c=FeatureCache(); assert hasattr(c,'amd_phase') and hasattr(c,'manip_strength'); print('ok')"</automated>
    <automated>.venv/bin/ruff check src/intelligence/feature_cache.py</automated>
  </verify>
  <acceptance_criteria>
    - update_overnight_range exists with signature (bar_ts, high, low, config); resets _overnight_high/_overnight_low on UTC session-boundary rollover; accumulates min/max within the window.
    - Feeding a synthetic overnight session of bars into a fresh FeatureCache yields finite _overnight_high >= _overnight_low, and a boundary-crossing bar resets them.
    - New exposed attrs amd_phase/amd_manipulation_detected/amd_distribution_direction/manip_strength default 0.0.
    - No hardcoded numeric constant in feature_cache.py — all thresholds from the `config` argument.
    - ruff clean on feature_cache.py.
  </acceptance_criteria>
  <done>update_overnight_range() tracks real overnight high/low with UTC-20:00 boundary reset and exposes AMD state fields; ready for Plan 04 to invoke and derive clamped/ordinal outputs. Not yet called anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| market_data_ohlcv_tradeable -> FeatureFactory | Already-validated internal OHLCV (price-sanity guarded upstream); no external/untrusted input crosses in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-01 | Tampering (silent wrong data) | New SMC columns could persist constant/default values (the todo-153 failure mode) | mitigate | Contract-only in this plan (values intentionally NULL until Plans 02-04); Plans 02-04 each add a non-constant regression test; drift gate crashes ic_engine if registry/dataclass diverge. |
| T-164-02 | Information disclosure (cross-symbol leakage) | Persisting raw price levels breaks cross-sectional IC (non-stationary) | mitigate | D-16 enforced via the Field-by-Field Raw-Price Audit: migration + FeatureVector contain zero `_top`/`_bottom`/`_level`/`_midpoint` columns; drop-redundant list applied. |
| T-164-03 | Denial of service (pipeline crash) | Adding required (no-default) FeatureVector fields without threading them through _build_feature_vector => TypeError on every bar | mitigate | All 36 fields threaded as None placeholders through both _build_feature_vector call sites; import-construct smoke check in verify. |
| T-164-SC | Tampering (supply chain) | package installs | accept | No new npm/pip/cargo packages — numpy/pandas/find_peaks/find_troughs/clamp/linear_ramp are already-live deps. No install task exists. |
</threat_model>

<verification>
- Migration 259 applies cleanly against the live DB; 36 columns present as `double precision`.
- `dataclasses.fields(FeatureVector)` name set == `SELECT feature_name FROM feature_registry` set (ic_engine alignment gate would pass).
- `feature.smc.*` APR keys resolve via ConfigService; FeatureFactoryConfig carries all smc_* fields.
- No raw-price / redundant feature names anywhere in the new schema.
- update_overnight_range() exists and tracks overnight range with boundary reset (not yet invoked).
</verification>

<success_criteria>
The full SMC data contract exists end-to-end with zero drift: 36 new columns (ATR-distance / bounded /
count / ordinal only) across feature_vectors + feature_registry + FeatureVector + FEATURE_VECTOR_DOMAIN
+ persistence INSERT, all APR-backed under feature.smc.*, FeatureFactoryConfig wired in both config
sites, plus a working (not-yet-invoked) overnight-range mutator — ready for Plans 02-04 to replace
None placeholders with real computed values.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-01-SUMMARY.md` when done.
</output>
