---
phase: 164-smc-institutional-footprint-primitives
plan: 04
type: execute
wave: 4
depends_on: ["164-01", "164-02", "164-03"]
files_modified:
  - src/intelligence/feature_factory.py
  - services/feature_vector_pipeline.py
  - services/backfill_feature_factory.py
  - tests/unit/intelligence/test_smc_zones.py
  - tests/unit/intelligence/test_smc_structure.py
  - tests/unit/intelligence/test_smc_amd_cycle.py
autonomous: true
requirements: ["REQ-164-06", "REQ-164-07", "REQ-164-08"]

must_haves:
  truths:
    - "supply/demand zones produce demand_dist_atr/supply_dist_atr/demand_freshness/supply_freshness/active_demand_zones/active_supply_zones/zone_friction_score, consuming the FVG+pools intermediates from Plan 03 in the same pass"
    - "BOS/CHoCH produce bos_strength/choch_strength/bos_direction/choch_direction/smc_trend_direction/bars_since_last_shift; dropped bos_level/bos_confidence absent"
    - "AMD produces amd_phase (ordinal 0/1/2/3), amd_manipulation_detected, amd_distribution_direction, and manip_strength CLAMPED to [0,1]; overnight state survives across bars via update_overnight_range(), resets at UTC-20:00 boundary, and is replayed on warm-up after restart"
    - "update_overnight_range() is invoked in compute_batch()'s loop, the live per-bar handler, AND the warm-up replay block — no cold-start reset inconsistency vs update_wk_vwap/update_session_vp"
  artifacts:
    - path: "src/intelligence/feature_factory.py"
      provides: "_compute_supply_demand_zones(), _compute_bos_choch(), _derive_amd_cycle() helpers wired into both compute paths; final 17 None placeholders replaced"
      contains: "_derive_amd_cycle"
    - path: "services/feature_vector_pipeline.py"
      provides: "update_overnight_range() call in per-bar handler + warm-up replay block"
      contains: "update_overnight_range"
    - path: "services/backfill_feature_factory.py"
      provides: "update_overnight_range() call in the batch per-bar loop"
      contains: "update_overnight_range"
    - path: "tests/unit/intelligence/test_smc_amd_cycle.py"
      provides: "overnight-state-survival + boundary-reset + manip_strength [0,1] clamp + amd_phase ordinal regression"
      contains: "manip_strength"
  key_links:
    - from: "_compute_supply_demand_zones"
      to: "Plan 03 fvg_midpoint + price_in_premium in-pass locals"
      via: "same compute pass, ordered after FVG/pools"
      pattern: "fvg_midpoint"
    - from: "services warm-up replay + per-bar handlers"
      to: "FeatureCache.update_overnight_range()"
      via: "call site added adjacent to update_session_vp()"
      pattern: "update_overnight_range"
---

<objective>
Port the final SMC cluster — Supply/Demand Zones, BOS/CHoCH, and the AMD Cycle — into both compute
paths, and wire the overnight-range mutator's call sites. REQ-164-06/07/08.

Supply/Demand Zones has a SOFT dependency (RESEARCH.md) on Plan 03's FVG (`fvg_midpoint`) and Liquidity
Pools (`price_in_premium`) in-pass intermediates; this plan's zones block is placed after them in the
mandated single-pass order and reads those locals directly. AMD invokes the
`FeatureCache.update_overnight_range()` mutator built in Plan 01 — this plan adds its call sites
(compute_batch loop, live per-bar handler, and the warm-up replay block) and derives the clamped
`manip_strength` + ordinal `amd_phase` in `compute()` from the cache's overnight state.

Replace the final 17 None placeholders: demand_dist_atr, supply_dist_atr, demand_freshness,
supply_freshness, active_demand_zones, active_supply_zones, zone_friction_score (zones);
bos_strength, choch_strength, bos_direction, choch_direction, smc_trend_direction,
bars_since_last_shift (BOS/CHoCH); amd_phase, amd_manipulation_detected, amd_distribution_direction,
manip_strength (AMD — note manip_strength counts within AMD's group; totals 17 across the three
concepts, of which amd_* come partly from the cache mutator).

Purpose: Complete the 36-field contract with real values and close the AMD state-lifecycle loop.
Output: three helpers + mutator call-site wiring across two services + three test files.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-RESEARCH.md
@.planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-PATTERNS.md
@.planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-01-SUMMARY.md
@.planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Verified analogs (164-PATTERNS.md). Use directly. -->

Stateless window-scan helper analog: _compute_sr_dist_atr (feature_factory.py:3332-3414).
Cache-reading derive analog: _derive_session_vp(cache, close_, atr_val, poc_price_rolling) -> dict (feature_factory.py:3180-3265) — reads cache raw state + atr_val, returns ATR-normalized/bounded dict, never persists raw levels. This is the direct template for _derive_amd_cycle.

Mutator call-site wiring (add update_overnight_range next to each update_session_vp call):
  compute_batch loop — feature_factory.py ~4200-4201 (cache.update_session_vp(bar_ts, high_, low_, close_, vol_, config))
  live per-bar — services/feature_vector_pipeline.py ~1012-1015 (cache.update_session_vp(bar.ts, bar.high, bar.low, bar.close, float(bar.volume), config))
  warm-up replay — services/feature_vector_pipeline.py ~174-202 (replays update_wk_vwap/update_session_vp over buffered history after restart) — AMD needs the SAME replay or overnight state cold-starts every restart.

Port sources (SKIM; drop raw fields per audit):
  supply_demand_zones.py — Rally-Base-Drop/Drop-Base-Rally; demand_dist_atr/supply_dist_atr ALREADY ATR-normalized (keep, drop raw nearest_demand_high/low, nearest_supply_high/low); demand_freshness/supply_freshness via freshness_decay (keep bounded); zone_friction_score; active zone counts. Reads fvg_midpoint + price_in_premium (soft — default if absent).
  bos_choch.py — find_peaks/find_troughs + get_atr; bos_strength/choch_strength ALREADY ATR-normalized (keep, drop raw bos_level); bos_confidence byte-identical to bos_strength (DROP); NEW bars_since_last_shift (track break-bar index); direction flags; smc_trend_direction.
  amd_cycle.py — overnight-range + manipulation detection (now in Plan 01's update_overnight_range mutator); amd_phase is a STRING (ordinal-encode 0/1/2/3 per migration 259 COMMENT); manip_strength UNBOUNDED in source -> clamp/linear_ramp to [0,1] before persisting.

APR: feature.smc.zones.* / feature.smc.bos.* / feature.smc.amd.* config fields seeded in Plan 01; read thresholds from config only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 tests + _compute_supply_demand_zones() + _compute_bos_choch()</name>
  <files>tests/unit/intelligence/test_smc_zones.py, tests/unit/intelligence/test_smc_structure.py, src/intelligence/feature_factory.py</files>
  <behavior>
    - A Rally-Base-Drop fixture forms a supply zone -> supply_dist_atr finite, supply_freshness in [0,1], active_supply_zones>=1; mirror demand side.
    - zone_friction_score bounded; changes when fvg_midpoint/price_in_premium intermediates are present vs absent (soft-dependency exercised, not vacuous).
    - A higher-high break fixture -> bos_direction=1.0, bos_strength finite ATR-normalized; a change-of-character fixture -> choch_direction fires; smc_trend_direction reflects the 2-swing trend; bars_since_last_shift a non-negative count.
    - atr<=0/short window -> fallbacks, never raise; deterministic.
  </behavior>
  <read_first>
    - tests/unit/intelligence/test_support_resistance_primitives.py (fixture/assertion precedent)
    - src/intelligence/archive/smc_context/supply_demand_zones.py, bos_choch.py (FULL — detection geometry; ignore plugin scaffolding/self._state)
    - src/intelligence/feature_factory.py 3180-3265 (_derive_session_vp), 3332-3414 (_compute_sr_dist_atr), the Plan 03 FVG/pools block placement (append zones after; read fvg_midpoint/price_in_premium locals), 3852-3863/3932+/4200 (wiring)
    - src/intelligence/utils.py + utils/gradient_utils.py (find_peaks/find_troughs/freshness_decay/clamp)
  </read_first>
  <action>
    (a) Create tests/unit/intelligence/test_smc_zones.py (RED): Rally-Base-Drop / Drop-Base-Rally fixtures; assert the 7 zone fields (finite dist, [0,1] freshness, bounded friction, count>=1); include a case toggling the fvg_midpoint/price_in_premium presence to prove the soft dependency actually changes zone_friction_score (RESEARCH Pitfall 1 vacuous-pass guard); raw-price-absence (no nearest_demand_high/low etc.); determinism.
    (b) Create tests/unit/intelligence/test_smc_structure.py (RED): higher-high BOS fixture, change-of-character CHoCH fixture; assert bos_strength/choch_strength ATR-normalized finite, direction flags in {-1,0,1}, smc_trend_direction, bars_since_last_shift>=0; assert bos_level/bos_confidence NOT present as fields; determinism.
    (c) Implement _compute_supply_demand_zones(opens, highs, lows, closes, close_, atr_val, tf, config, fvg_midpoint, price_in_premium) -> dict[str,float] with _ZONE_FALLBACK; keep the already-ATR-normalized demand_dist_atr/supply_dist_atr, freshness_decay-bounded freshness, drop raw zone highs/lows; consume fvg_midpoint/price_in_premium (default gracefully if None). Implement _compute_bos_choch(highs, lows, closes, close_, atr_val, tf, config) -> dict[str,float] with _BOS_FALLBACK; keep bos_strength/choch_strength, drop bos_level/bos_confidence, add bars_since_last_shift + direction flags + smc_trend_direction. Wire both into compute()/compute_batch() after the FVG/pools block, in order zones -> bos_choch (per RESEARCH single-pass order); replace the 13 zone+BOS None placeholders in both _build_feature_vector calls. All thresholds from `config`; zero hardcoded numerics.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_smc_zones.py tests/unit/intelligence/test_smc_structure.py -x -q</automated>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_feature_factory_batch_parity.py -x -q</automated>
    <automated>grep -Eq "bos_confidence|\\bbos_level\\b|nearest_demand_high|nearest_supply_high" src/intelligence/feature_factory.py && echo "FAIL raw/redundant leak" || echo "ok"</automated>
    <automated>.venv/bin/ruff check src/intelligence/feature_factory.py</automated>
  </verify>
  <acceptance_criteria>
    - test_smc_zones.py + test_smc_structure.py GREEN; batch parity green.
    - Soft-dependency test proves zone_friction_score differs with/without fvg_midpoint/price_in_premium (non-vacuous).
    - No bos_level/bos_confidence/nearest_*_high/low persisted field; direction flags in {-1,0,1}.
    - No hardcoded numeric in the zones/BOS blocks (config-driven).
    - ruff clean.
  </acceptance_criteria>
  <done>Supply/demand zones + BOS/CHoCH compute in both paths with real values, zones consuming Plan 03's FVG/pools intermediates; 13 placeholders replaced; suites GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _derive_amd_cycle() (clamp + ordinal) + update_overnight_range() call-site wiring</name>
  <files>src/intelligence/feature_factory.py, services/feature_vector_pipeline.py, services/backfill_feature_factory.py, tests/unit/intelligence/test_smc_amd_cycle.py</files>
  <behavior>
    - Feeding an overnight accumulation window then a manipulation wick beyond the overnight range then a distribution phase -> amd_phase transitions across ordinal values {0,1,2,3}, amd_manipulation_detected=1.0 on the breach-and-reverse bar, amd_distribution_direction reflects the distribution leg.
    - manip_strength is ALWAYS in [0,1] even when the manipulation wick overshoots the overnight range by >100% (clamp verified with an extreme fixture) — the exact RESEARCH Pitfall 3 case.
    - Overnight state survives across many bars (set during 20:00-00:00 UTC, still readable during 10:00-21:00 UTC) and resets at the UTC-20:00 boundary.
    - After a simulated warm-up replay, overnight state is non-cold (matches update_session_vp's replay behavior).
    - atr/insufficient-state -> neutral defaults, never raise; deterministic.
  </behavior>
  <read_first>
    - src/intelligence/feature_factory.py 3180-3265 (_derive_session_vp template — reads cache state, returns derived dict), the compute()/compute_batch() wiring points, and compute_batch ~4200 (update_session_vp call site)
    - src/intelligence/feature_cache.py — the update_overnight_range() mutator + _overnight_high/_overnight_low + amd_* exposed fields added in Plan 01
    - services/feature_vector_pipeline.py ~174-202 (warm-up replay block) and ~1012-1015 (per-bar update_session_vp call)
    - services/backfill_feature_factory.py — its compute_batch invocation / per-bar loop (mirror where update_session_vp is driven; if driven inside compute_batch, confirm the mutator call belongs there)
    - src/intelligence/utils.py (clamp/linear_ramp), src/intelligence/archive/smc_context/amd_cycle.py (ordinal string->number mapping + phase logic)
    - tests/unit/intelligence/test_smc_amd_cycle.py (spec)
  </read_first>
  <action>
    (a) Create tests/unit/intelligence/test_smc_amd_cycle.py (RED): fixtures per the behavior block — overnight-state-survival across bars, UTC-20:00 boundary reset, manip_strength [0,1] clamp under an extreme overshoot, amd_phase ordinal transitions, warm-up replay non-cold. Drive update_overnight_range() per bar then read the derived AMD fields off compute().
    (b) Implement _derive_amd_cycle(cache, close_, atr_val, config) -> dict[str,float] following _derive_session_vp's shape: read cache._overnight_high/_overnight_low + the manipulation-detection state, ordinal-encode amd_phase (0.0=unknown/1.0=accumulation/2.0=manipulation/3.0=distribution, matching migration 259's COMMENT), and CLAMP manip_strength to [0,1] via linear_ramp/clamp before returning (archived source is unbounded — RESEARCH Pitfall 3). Return amd_phase, amd_manipulation_detected, amd_distribution_direction, manip_strength. Wire into compute()/compute_batch() after the BOS block (last in the SMC pass); replace the final 4 AMD None placeholders in both _build_feature_vector calls.
    (c) Add cache.update_overnight_range(bar_ts, high, low, config) call sites immediately adjacent to every update_session_vp() call: compute_batch()'s per-bar loop (feature_factory.py ~4200), the live per-bar handler (feature_vector_pipeline.py ~1012-1015), and the warm-up replay block (feature_vector_pipeline.py ~174-202). If backfill_feature_factory drives the mutator outside compute_batch, add it there too so live and batch stay identical.
    All thresholds from `config` (feature.smc.amd.*); zero hardcoded numerics.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_smc_amd_cycle.py -x -q</automated>
    <automated>.venv/bin/pytest tests/unit/intelligence/ -q</automated>
    <automated>grep -c "update_overnight_range" src/intelligence/feature_factory.py services/feature_vector_pipeline.py | grep -v ':0'</automated>
    <automated>.venv/bin/ruff check src/intelligence/feature_factory.py services/feature_vector_pipeline.py services/backfill_feature_factory.py</automated>
  </verify>
  <acceptance_criteria>
    - test_smc_amd_cycle.py GREEN; manip_strength provably in [0,1] under the extreme-overshoot fixture.
    - update_overnight_range() invoked in compute_batch loop, live per-bar handler, AND warm-up replay (grep confirms presence in feature_factory.py + feature_vector_pipeline.py).
    - amd_phase only ever takes values in {0.0,1.0,2.0,3.0}; no string ever reaches the FeatureVector.
    - Full unit suite `.venv/bin/pytest tests/unit/ -q` GREEN (all 36 SMC fields now non-constant end-to-end; drift gate holds; persistence completeness holds).
    - No hardcoded numeric in the AMD derivation; ruff clean.
  </acceptance_criteria>
  <done>AMD cycle computes clamped/ordinal outputs from the overnight-range mutator, whose call sites are wired into batch, live, and warm-up replay paths with no cold-start inconsistency; all 36 SMC fields carry real values; full unit suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| market_data_ohlcv_tradeable -> FeatureFactory | Internal already-validated OHLCV; no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-01 | Tampering (silent wrong data) | zones/BOS/AMD fields default-constant | mitigate | Three test files assert non-constant values; soft-dependency + clamp + ordinal cases are non-vacuous. |
| T-164-02 | Information disclosure | raw nearest_demand_high/bos_level leaking | mitigate | Audit-driven drops; grep gate; drift gate holds. |
| T-164-03 | Tampering (invalid feature values) | unbounded manip_strength / string amd_phase entering training data | mitigate | manip_strength clamped [0,1] (extreme-overshoot test); amd_phase ordinal-only (asserted in {0,1,2,3}). |
| T-164-07 | Tampering (state lifecycle) | overnight state cold-starts on restart while VP/SR state does not | mitigate | update_overnight_range wired into the warm-up replay block alongside update_session_vp; replay test. |
| T-164-05 | Denial of service | NaN/Inf/raise on cold-start/atr<=0 | mitigate | _ZONE_FALLBACK/_BOS_FALLBACK + neutral AMD defaults; never raises (V5). |
| T-164-SC | Tampering (supply chain) | package installs | accept | No new packages. |
</threat_model>

<verification>
- test_smc_zones.py + test_smc_structure.py + test_smc_amd_cycle.py GREEN; full `tests/unit/` suite GREEN.
- update_overnight_range wired into batch, live, and warm-up replay; grep-confirmed.
- manip_strength ∈ [0,1]; amd_phase ∈ {0,1,2,3}; zones soft-dependency exercised.
- Drift gate + persistence completeness still hold; no raw/redundant field leaks.
- ruff clean across the three edited files.
</verification>

<success_criteria>
Supply/Demand Zones, BOS/CHoCH, and AMD Cycle compute in both live and batch paths, completing all 36
SMC fields with real ATR-normalized/bounded/count/ordinal values; the overnight-range mutator's
lifecycle is closed across batch, live, and warm-up replay; the full unit suite is green with the
ic_engine drift gate and persistence completeness guard both satisfied. Phase scope ends here — live
compute path wired, unit-tested, migration applied; historical backfill deliberately deferred to the
consolidated 163/164/165 pass (todo 176) and the shared collinearity/incremental-IC sweep is a
phase-exit follow-up, neither a task in this phase.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-04-SUMMARY.md` when done.
</output>
