---
phase: 164-smc-institutional-footprint-primitives
plan: 03
type: execute
wave: 3
depends_on: ["164-01", "164-02"]
files_modified:
  - src/intelligence/feature_factory.py
  - tests/unit/intelligence/test_smc_fvg.py
  - tests/unit/intelligence/test_smc_liquidity.py
autonomous: true
requirements: ["REQ-164-03", "REQ-164-04", "REQ-164-05"]

must_haves:
  truths:
    - "compute()/compute_batch() produce non-constant fvg_dist_atr/fvg_size_atr/fvg_open_count on a synthetic 3-candle imbalance series"
    - "liquidity sweeps produce bounded sweep_strength/reclaim_velocity in [0,1], sweep_detected flag, and a bars_since_last_sweep count"
    - "liquidity pools produce bsl_dist_atr/ssl_dist_atr/bsl_touches/ssl_touches/pool_count from single-timeframe sources only — PWH/PWL/PDH/PDL descoped, no 1d-bar access attempted"
    - "no raw fvg_top/fvg_bottom/fvg_midpoint/sweep_level/bsl_level/ssl_level leaks into a FeatureVector field; dropped bsl_type/ssl_type not present"
  artifacts:
    - path: "src/intelligence/feature_factory.py"
      provides: "_compute_fvg(), _compute_liquidity_sweeps(), _compute_liquidity_pools() helpers wired into both compute paths, plus the intermediate fvg_midpoint/price_in_premium locals Plan 04's zones block consumes"
      contains: "_compute_liquidity_pools"
    - path: "tests/unit/intelligence/test_smc_fvg.py"
      provides: "FVG non-constant + raw-price-absence regression"
      contains: "fvg_size_atr"
    - path: "tests/unit/intelligence/test_smc_liquidity.py"
      provides: "sweeps + pools regression (bounds, single-tf descope, raw-price absence)"
      contains: "bsl_dist_atr"
  key_links:
    - from: "_compute_fvg / _compute_liquidity_pools"
      to: "Plan 04 supply/demand zones block (same compute pass)"
      via: "fvg_midpoint + price_in_premium intermediate locals (soft dependency)"
      pattern: "fvg_midpoint"
    - from: "_compute_liquidity_sweeps"
      to: "_build_feature_vector call sites"
      via: "dict keys replacing None placeholders"
      pattern: "sweep_strength"
---

<objective>
Port the second SMC cluster — Fair Value Gaps, Liquidity Sweeps, and Liquidity Pools — into both
compute paths. This is REQ-164-03/04/05. Liquidity Pools is DESCOPED per RESEARCH.md: PWH/PWL/PDH/PDL
named weekly/daily levels need 1d-bar access the single-timeframe compute() signature cannot provide,
so only single-tf-derivable components survive (equal-highs/equal-lows + session high/low).

This plan also produces the intermediate `fvg_midpoint` and `price_in_premium` locals that Plan 04's
supply/demand-zones block reads (a SOFT dependency — zones default gracefully if absent, but the
computed zone_strength/zone_friction_score differ, so the single-pass order FVG+pools -> zones must
hold). This plan places FVG and pools ahead of the zones block Plan 04 appends.

Replace the None placeholders for: fvg_dist_atr, fvg_size_atr, fvg_open_count (FVG); sweep_detected,
sweep_strength, reclaim_velocity, bars_since_last_sweep (sweeps); bsl_dist_atr, ssl_dist_atr,
bsl_touches, ssl_touches, pool_count (pools).

Purpose: Land the three independent (post-descope) window-scan primitives and stage the FVG/pools
intermediates zones needs.
Output: three helpers + wiring + two test files.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-RESEARCH.md
@.planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-PATTERNS.md
@.planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-01-SUMMARY.md
@.planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Verified analogs (164-PATTERNS.md). Use directly. -->

Helper shape: _compute_sr_dist_atr (feature_factory.py:3332-3414) — _<CONCEPT>_FALLBACK constant, atr_valid guard, config.<name>_lookback windowing, reuse find_peaks/find_troughs. Wiring: unpack dict into locals (~3852-3863), pass to _build_feature_vector (~3932+), compute_batch loop (~4200). OB block from Plan 02 is already in place ahead of this section.

Port sources (SKIM; drop raw fields per audit):
  fair_value_gap.py — 3-candle imbalance scan; fvg_size_pct (percentage, NOT atr), raw fvg_top/fvg_bottom/fvg_midpoint (DROP raw; but retain fvg_midpoint as an in-pass LOCAL for Plan 04 zones). NEW derivations: fvg_size_atr=(top-bottom)/atr, fvg_dist_atr=(close-fvg_midpoint)/atr. fvg_open_count = count of unfilled gaps.
  liquidity_sweeps.py — find_swing_highs/find_swing_lows + wick-beyond; sweep_strength/reclaim_velocity already bounded [0,1] via linear_ramp (keep); raw sweep_level (DROP). NEW: bars_since_last_sweep (track bar index of last sweep in-window). sweep_detected flag.
  liquidity_pools.py — bsl_dist_atr/ssl_dist_atr ALREADY correctly ATR-normalized (keep, drop raw bsl_level/ssl_level only); bsl_type/ssl_type byte-identical to significance (DROP). Also exposes price_in_premium — retain as an in-pass LOCAL for Plan 04 zones. DESCOPE PWH/PWL/PDH/PDL (frames.get("1d") unavailable): keep only equal-highs/equal-lows (find_peaks/find_troughs) + session-high/low (rolling max/min over the current session's bars). bsl_touches/ssl_touches = touch counts; pool_count.

Raw-price audit + descope + drop-redundant details: 164-RESEARCH.md Field-by-Field table + "cross-timeframe data gap" section.
APR: feature.smc.fvg.* / feature.smc.liquidity.* config fields seeded in Plan 01; read thresholds from config only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 tests + _compute_fvg() (FVG)</name>
  <files>tests/unit/intelligence/test_smc_fvg.py, tests/unit/intelligence/test_smc_liquidity.py, src/intelligence/feature_factory.py</files>
  <read_first>
    - tests/unit/intelligence/test_support_resistance_primitives.py (Phase 163 fixture/assertion precedent)
    - src/intelligence/archive/smc_context/fair_value_gap.py (FULL — 3-candle imbalance geometry)
    - src/intelligence/archive/smc_context/liquidity_sweeps.py, liquidity_pools.py (SKIM — to author the liquidity test fixtures deterministically; implementation is Task 2)
    - src/intelligence/feature_factory.py lines 3332-3414 (helper shape), 3852-3863/3932+/4200 (wiring), and the Plan 02 OB block placement (append FVG after it)
  </read_first>
  <action>
    (a) Create tests/unit/intelligence/test_smc_fvg.py (RED for _compute_fvg): a 3-candle bullish imbalance fixture (candle1.high < candle3.low) forming an unfilled FVG; assert fvg_size_atr=(top-bottom)/atr finite non-zero, fvg_dist_atr finite, fvg_open_count>=1; a filled-gap fixture asserts fvg_open_count decrements; raw-price-absence (no fvg_top/fvg_bottom/fvg_midpoint attribute on FeatureVector); determinism.
    (b) Create tests/unit/intelligence/test_smc_liquidity.py (RED for _compute_liquidity_sweeps AND _compute_liquidity_pools — both implemented in Task 2): a wick-beyond-swing-high-then-reclaim fixture asserts sweep_detected=1.0, sweep_strength/reclaim_velocity in [0,1], bars_since_last_sweep integer-valued>=0; an equal-highs cluster fixture asserts bsl_dist_atr finite + bsl_touches>=2 + pool_count>=1; assert PWH/PWL/PDH/PDL are NOT computed (no attribute / helper never reads a 1d frame); raw-price-absence (no sweep_level/bsl_level/ssl_level, no bsl_type/ssl_type); determinism.
    (c) Implement _compute_fvg(opens, highs, lows, closes, close_, atr_val, tf, config) -> dict[str,float] with an _FVG_FALLBACK constant, atr_valid guard, config-driven lookback/min-gap thresholds. Return fvg_dist_atr, fvg_size_atr, fvg_open_count. ALSO return fvg_midpoint as a separate key used only as an in-pass LOCAL (NOT persisted, NOT a FeatureVector field) for Plan 04 zones. Wire into compute()/compute_batch() after the OB block; replace the 3 FVG None placeholders in both _build_feature_vector calls; stash fvg_midpoint in a local variable for later in the pass.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast; [ast.parse(open(f).read()) for f in ['tests/unit/intelligence/test_smc_fvg.py','tests/unit/intelligence/test_smc_liquidity.py']]; print('parse ok')"</automated>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_smc_fvg.py -x -q</automated>
    <automated>.venv/bin/ruff check src/intelligence/feature_factory.py</automated>
  </verify>
  <acceptance_criteria>
    - Both test files parse/collect; test_smc_liquidity.py fails RED (its helpers land in Task 2).
    - test_smc_fvg.py GREEN after _compute_fvg lands.
    - fvg_midpoint is threaded as an in-pass local only — grep confirms it is NOT in _SMC_FIELD_NAMES / FeatureVector / _build_feature_vector kwargs.
    - No fvg_top/fvg_bottom/fvg_midpoint persisted field; no hardcoded numeric in the FVG block (config-driven).
    - ruff clean.
  </acceptance_criteria>
  <done>_compute_fvg wired; FVG fields carry real ATR-normalized values; fvg_midpoint staged as an in-pass local for Plan 04; liquidity test authored RED for Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _compute_liquidity_sweeps() + _compute_liquidity_pools() (single-tf descoped)</name>
  <files>src/intelligence/feature_factory.py</files>
  <behavior>
    - Wick beyond a swing high/low then close back inside -> sweep_detected=1.0, sweep_strength in [0,1], reclaim_velocity in [0,1], bars_since_last_sweep a non-negative count.
    - No sweep in window -> sweep_detected=0.0, bars_since_last_sweep reflects distance to last (or a sentinel), never NaN.
    - Equal-highs cluster -> bsl_dist_atr=abs(close-bsl_level)/atr finite, bsl_touches>=2, pool_count>=1; equal-lows -> ssl side. Session high/low included as pool sources; PWH/PWL/PDH/PDL NOT computed.
    - atr_val<=0 or short window -> _SWEEP_FALLBACK / _POOL_FALLBACK; never raise.
    - Deterministic.
  </behavior>
  <read_first>
    - src/intelligence/archive/smc_context/liquidity_sweeps.py (FULL — wick-beyond + reclaim geometry; sweep_strength/reclaim_velocity linear_ramp usage)
    - src/intelligence/archive/smc_context/liquidity_pools.py (FULL — equal-highs/lows + session high/low; the PWH/PWL/PDH/PDL frames.get("1d") block to OMIT; bsl_dist_atr/price_in_premium computation)
    - src/intelligence/utils.py (find_peaks/find_troughs/linear_ramp/clamp)
    - src/intelligence/feature_factory.py FVG-block placement from Task 1 (append sweeps then pools after)
    - tests/unit/intelligence/test_smc_liquidity.py (the spec)
  </read_first>
  <action>
    Add _SWEEP_FALLBACK and _POOL_FALLBACK module constants and two pure helpers:
    - _compute_liquidity_sweeps(highs, lows, closes, close_, atr_val, tf, config) -> dict[str,float]: port liquidity_sweeps.py's wick-beyond-swing + reclaim detection using find_peaks/find_troughs; keep sweep_strength/reclaim_velocity's existing linear_ramp [0,1] bounding; add bars_since_last_sweep (track the in-window bar index of the last detected sweep); set sweep_detected. Drop raw sweep_level.
    - _compute_liquidity_pools(highs, lows, closes, close_, atr_val, tf, config) -> dict[str,float]: port ONLY the single-tf sources — equal-highs/equal-lows via find_peaks/find_troughs and session-high/low via rolling max/min over the current session's bars. DO NOT read a 1d frame; DO NOT compute PWH/PWL/PDH/PDL. Keep the already-correct bsl_dist_atr/ssl_dist_atr; add bsl_touches/ssl_touches (touch counts) and pool_count. Drop raw bsl_level/ssl_level and the redundant bsl_type/ssl_type. ALSO return price_in_premium as an in-pass LOCAL (not persisted) for Plan 04 zones.
    All thresholds from `config` (feature.smc.liquidity.*); zero hardcoded numerics. Wire both helpers into compute()/compute_batch() after the FVG block; replace the 9 sweep+pool None placeholders in both _build_feature_vector calls; stash price_in_premium in a local for later in the pass.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_smc_liquidity.py tests/unit/intelligence/test_smc_fvg.py -x -q</automated>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_feature_factory_batch_parity.py -x -q</automated>
    <automated>grep -Eq "PWH|PWL|PDH|PDL|frames.get\\([\"']1d" src/intelligence/feature_factory.py && echo "FAIL: 1d/PWH leak" || echo "ok: descope holds"</automated>
    <automated>.venv/bin/ruff check src/intelligence/feature_factory.py</automated>
  </verify>
  <acceptance_criteria>
    - test_smc_liquidity.py + test_smc_fvg.py all GREEN; batch parity green.
    - No PWH/PWL/PDH/PDL or `frames.get("1d")` in feature_factory.py (descope holds).
    - No sweep_level/bsl_level/ssl_level/bsl_type/ssl_type persisted field.
    - price_in_premium is an in-pass local only (not in _SMC_FIELD_NAMES / FeatureVector).
    - sweep_strength/reclaim_velocity assert within [0,1] in tests; no hardcoded numeric in the two helpers.
    - ruff clean.
  </acceptance_criteria>
  <done>Liquidity sweeps + single-tf-descoped pools compute in both paths with bounded/count/ATR-normalized values; PWH/PWL/PDH/PDL descoped; price_in_premium staged for Plan 04; suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| market_data_ohlcv_tradeable -> FeatureFactory | Internal already-validated OHLCV; no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-01 | Tampering (silent wrong data) | FVG/sweep/pool fields default-constant | mitigate | test_smc_fvg.py + test_smc_liquidity.py assert non-constant, bounded, ATR-correct values. |
| T-164-02 | Information disclosure | raw fvg_midpoint/sweep_level/bsl_level leaking as a feature | mitigate | fvg_midpoint/price_in_premium kept as in-pass locals only; audit-driven drop of raw levels; grep gate. |
| T-164-06 | Tampering (scope creep into unavailable data) | attempting 1d-frame PWH/PWL/PDH/PDL access | mitigate | Descope enforced; grep gate fails on PWH/PWL/PDH/PDL or frames.get("1d"). |
| T-164-05 | Denial of service | NaN/Inf/raise on cold-start or atr<=0 | mitigate | _FVG_FALLBACK/_SWEEP_FALLBACK/_POOL_FALLBACK early returns; atr_valid guard; never raises. |
| T-164-SC | Tampering (supply chain) | package installs | accept | No new packages. |
</threat_model>

<verification>
- test_smc_fvg.py + test_smc_liquidity.py GREEN; batch parity green.
- Descope grep gate passes (no PWH/PWL/PDH/PDL / 1d frame).
- fvg_midpoint + price_in_premium are in-pass locals, not persisted.
- ruff clean.
</verification>

<success_criteria>
FVG, Liquidity Sweeps, and single-timeframe-descoped Liquidity Pools compute in both paths with real
ATR-normalized/bounded/count values; PWH/PWL/PDH/PDL correctly out of scope; the FVG/pools
intermediates Plan 04's zones block needs are staged in the same compute pass.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-03-SUMMARY.md` when done.
</output>
