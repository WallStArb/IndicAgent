---
phase: 164-smc-institutional-footprint-primitives
plan: 02
type: execute
wave: 2
depends_on: ["164-01"]
files_modified:
  - src/intelligence/feature_factory.py
  - tests/unit/intelligence/test_smc_order_blocks.py
autonomous: true
requirements: ["REQ-164-01", "REQ-164-02"]

must_haves:
  truths:
    - "compute() and compute_batch() produce non-constant ob_bull_dist_atr/ob_bear_dist_atr/ob_strength/ob_mitigated_flag on a synthetic impulse+order-block bar series"
    - "breaker_dist_atr/breaker_block_active/ob_mitigation_pct are derived statelessly from order_blocks' own active-OB list within the same compute pass — no self._state, no cross-call memory"
    - "all 7 fields are ATR-normalized/bounded/count/flag only; no raw ob_top/ob_bottom/breaker_block_top/bottom leaks into a FeatureVector field"
  artifacts:
    - path: "src/intelligence/feature_factory.py"
      provides: "_compute_order_blocks() helper (returns 7 OB+breaker+mitigation fields) wired into compute()/compute_batch() and threaded into _build_feature_vector (replacing None placeholders)"
      contains: "_compute_order_blocks"
    - path: "tests/unit/intelligence/test_smc_order_blocks.py"
      provides: "non-constant regression + raw-price-absence + breaker-derives-from-OB (sequenced fixture) tests"
      contains: "ob_bull_dist_atr"
  key_links:
    - from: "src/intelligence/feature_factory.py _compute_order_blocks"
      to: "_build_feature_vector call sites (compute + compute_batch)"
      via: "dict keys replacing None placeholders"
      pattern: "ob_bull_dist_atr"
    - from: "breaker/mitigation derivation"
      to: "order_blocks active_obs list (same pass)"
      via: "in-function variable, not FeatureCache/self._state"
      pattern: "active_obs"
---

<objective>
Port the first SMC dependency cluster — Order Blocks and the Breaker/Mitigation blocks derived from
them — into `FeatureFactory.compute()`/`compute_batch()` as one stateless pass. This is REQ-164-01
and REQ-164-02, sequenced together because RESEARCH.md establishes breaker_blocks.py and
mitigation_blocks.py have a HARD dependency on order_blocks.py's output and the archived versions
carry `self._state` cross-call memory that must NOT survive the port (v3 compute() is a documented
pure function).

Replace the None placeholders Plan 01 threaded for: ob_bull_dist_atr, ob_bear_dist_atr, ob_strength,
ob_mitigated_flag (order blocks), breaker_dist_atr, breaker_block_active, ob_mitigation_pct
(breaker/mitigation) with real values derived within a single compute pass.

Purpose: Prove the hardest cross-plugin sequencing case first (hard OB->breaker/mitigation chain,
statelessly) so Plans 03-04 follow an established in-function-threading pattern.
Output: _compute_order_blocks() helper + wiring + the test_smc_order_blocks.py regression suite.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-RESEARCH.md
@.planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-PATTERNS.md
@.planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Verified analogs (164-PATTERNS.md). Use directly. -->

Stateless window-scan helper analog — src/intelligence/feature_factory.py:
  _compute_sr_dist_atr(highs, lows, close_, atr_val, volume, tf, config) -> dict[str,float] (lines 3332-3414)
  Shape to copy: module-level _<CONCEPT>_FALLBACK: dict[str,float] for insufficient-data early return (never raise);
  atr_valid = atr_val is not None and math.isfinite(atr_val) and atr_val > 0 as first check;
  config.<name>_lookback_by_tf.get(tf, <default>) for per-tf windowing; reuse find_peaks/find_troughs.

compute() call-site wiring — feature_factory.py ~3852-3863 (how _sr_fields dict outputs are unpacked into locals then passed to _build_feature_vector ~3932+); compute_batch() loop wires the same helpers per bar (~4200).

Port source (SKIM, do not literally 1:1 port):
  src/intelligence/archive/smc_context/order_blocks.py — OrderBlocksPlugin.compute_full: impulse+opposing-candle OB scan over ~100-bar window; ob_distance_pct (percentage, NOT atr), ob_strength, ob_mitigated, raw ob_top/ob_bottom (DROP raw). compute_next just delegates to compute_full (line 157-158).
  src/intelligence/archive/smc_context/breaker_blocks.py — reads OB output (ob_type/ob_top/ob_bottom/ob_mitigated); has breaker_dist_atr already ATR-normalized (keep), raw breaker_block_top/bottom (DROP). Uses self._state across calls — DO NOT port that; derive from OB's active list in-pass.
  src/intelligence/archive/smc_context/mitigation_blocks.py — reads OB output; ob_mitigation_pct already [0,1] (keep); ob_mitigation_status string (DROP — redundant).

FeatureFactoryConfig SMC fields + feature.smc.order_blocks.* APR keys: added in Plan 01 (smc_ob_* impulse/lookback params). Read thresholds from config only — no new hardcoded numerics.

Raw-price audit (164-RESEARCH.md): ob_bull_dist_atr/ob_bear_dist_atr = abs(close - ob_mid)/atr (new ATR derivation; archived only has ob_distance_pct percentage). Track SEPARATE nearest-bullish and nearest-bearish OB (archived returns only the single latest across both types). breaker_dist_atr already ATR-normalized in source.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — failing test_smc_order_blocks.py (OB + breaker + mitigation, sequenced fixtures)</name>
  <files>tests/unit/intelligence/test_smc_order_blocks.py</files>
  <read_first>
    - tests/unit/intelligence/test_support_resistance_primitives.py (Phase 163 new-file precedent: synthetic OHLCV fixture construction + FeatureFactory.compute() invocation + non-constant assertions)
    - src/intelligence/archive/smc_context/order_blocks.py (FULL — exact impulse/opposing-candle detection geometry to build a triggering fixture)
    - src/intelligence/archive/smc_context/breaker_blocks.py, mitigation_blocks.py (the OB->breaker/mitigation transition geometry to fixture the sequenced dependency)
    - src/intelligence/feature_factory.py compute() signature (~3785-3811) and FeatureFactoryConfig construction for a test config
  </read_first>
  <action>
    Create tests/unit/intelligence/test_smc_order_blocks.py with deterministic synthetic-OHLCV fixtures (RED — helper does not exist yet). Cover:
    - A bullish-impulse-then-opposing-candle series that forms a detectable bullish order block; assert ob_bull_dist_atr is finite, non-zero, and equals abs(close - ob_mid)/atr within tolerance; assert ob_strength in [0,1].
    - A mirror bearish-order-block series; assert ob_bear_dist_atr fires.
    - RESEARCH Pitfall 1 (vacuous-pass guard): a fixture that forms an order block AND then breaks/mitigates it, so breaker_block_active=1.0 and ob_mitigation_pct in (0,1] are actually exercised — NOT a "no OB present" series that passes the empty branch vacuously.
    - Raw-price-absence: assert the constructed FeatureVector has no attribute ob_top/ob_bottom/breaker_block_top/breaker_block_bottom (they must not exist as fields).
    - Determinism: identical input twice -> identical output (pure-function contract).
    Use FeatureFactory.compute() directly with a FeatureFactoryConfig built from the smc_ob_* defaults.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast; ast.parse(open('tests/unit/intelligence/test_smc_order_blocks.py').read()); print('parses')" && .venv/bin/pytest tests/unit/intelligence/test_smc_order_blocks.py -x -q; test $? -ne 0 && echo "RED as expected (helper not yet implemented)"</automated>
  </verify>
  <acceptance_criteria>
    - Test file parses and collects; every test currently FAILS (helper unimplemented) — RED confirmed.
    - Fixtures deterministically trigger a bullish OB, a bearish OB, and an OB-then-break/mitigate sequence (no vacuous empty-branch pass).
    - Assertions cover non-constant values, ob_mid ATR formula, [0,1] bounds, raw-price-field absence, determinism.
  </acceptance_criteria>
  <done>test_smc_order_blocks.py exists, parses, collects, and fails RED — the executable spec for Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _compute_order_blocks() (OB + stateless breaker/mitigation) + wire into compute paths</name>
  <files>src/intelligence/feature_factory.py</files>
  <behavior>
    - Bullish impulse + opposing candle within the lookback window -> ob_bull_dist_atr = abs(close - nearest_bull_ob_mid)/atr, finite, non-zero.
    - Bearish equivalent -> ob_bear_dist_atr fires; nearest-bull and nearest-bear tracked separately.
    - ob_strength in [0,1]; ob_mitigated_flag in {0.0,1.0}.
    - An OB later broken/reclaimed -> breaker_block_active=1.0, breaker_dist_atr finite; partial mitigation -> ob_mitigation_pct in (0,1].
    - atr_val <= 0 or insufficient bars -> all 7 fields fall back to the _OB_FALLBACK constant (never raise, never NaN/Inf).
    - Deterministic: identical inputs -> identical outputs.
  </behavior>
  <read_first>
    - src/intelligence/feature_factory.py lines 3332-3414 (_compute_sr_dist_atr helper shape), 3852-3863 (unpack-into-locals wiring), 3932+ (_build_feature_vector call site), ~4200 (compute_batch per-bar loop)
    - src/intelligence/archive/smc_context/order_blocks.py, breaker_blocks.py, mitigation_blocks.py (FULL — the detection geometry to port; ignore their self._state / InputSpec / plugin scaffolding)
    - src/intelligence/utils.py (find_peaks/find_troughs/clamp signatures)
    - tests/unit/intelligence/test_smc_order_blocks.py (the spec from Task 1)
  </read_first>
  <action>
    Add a module-level `_OB_FALLBACK: dict[str, float]` (7 keys all 0.0) and a pure helper
    `_compute_order_blocks(opens, highs, lows, closes, close_, atr_val, tf, config) -> dict[str, float]`:
    - Guard `atr_valid` first; return dict(_OB_FALLBACK) if invalid or window too short.
    - Scan the config.smc_ob_lookback window (from Plan 01's APR-backed config field) for impulse+opposing-candle order blocks, porting order_blocks.py's detection geometry but retaining the FULL active-OB candidate list in-pass (not just the single latest) so nearest-bullish and nearest-bearish can be reported separately.
    - Compute ob_bull_dist_atr = abs(close_ - nearest_bull_ob_mid)/atr_val and ob_bear_dist_atr = abs(close_ - nearest_bear_ob_mid)/atr_val; ob_strength (bounded, reuse the archived impulse/body-ratio strength, clamp to [0,1]); ob_mitigated_flag for the nearest OB.
    - Derive breaker/mitigation STATELESSLY from the same active-OB list (RESEARCH's explicit correction — no self._state, no FeatureCache): a breaker = an OB that price has broken and closed beyond then re-approached; set breaker_block_active, breaker_dist_atr = abs(close_ - breaker_mid)/atr_val, and ob_mitigation_pct = fraction of the nearest OB's range price has eroded, in [0,1]. All thresholds from `config` (feature.smc.order_blocks.*), zero hardcoded numerics.
    - Never return or persist a raw ob_top/ob_bottom/breaker_block_top/bottom.
    Wire the helper into compute() (unpack the 7 dict keys into locals, matching the _sr_fields pattern at ~3852-3863) and compute_batch()'s per-bar loop, and REPLACE the 7 None placeholders in BOTH _build_feature_vector call sites with these values. Place the OB block first in the SMC section (order_blocks -> ... per RESEARCH's mandated single-pass order) so Plans 03-04 append after it.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_smc_order_blocks.py -x -q</automated>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_feature_factory_batch_parity.py -x -q</automated>
    <automated>.venv/bin/ruff check src/intelligence/feature_factory.py && grep -c "smart_money" src/intelligence/feature_factory.py | grep -qE '^[0-9]+'</automated>
  </verify>
  <acceptance_criteria>
    - test_smc_order_blocks.py all GREEN.
    - Live/batch parity test still green for the new fields.
    - No `ob_top`/`ob_bottom`/`breaker_block_top`/`breaker_block_bottom` string appears as a persisted field name anywhere in feature_factory.py's _build_feature_vector call.
    - breaker/mitigation derive from the in-pass active-OB list — grep confirms no new `self._state`/FeatureCache field added for these.
    - No hardcoded numeric threshold introduced in the OB block — all read from `config`.
    - ruff clean on feature_factory.py.
  </acceptance_criteria>
  <done>Order-blocks + stateless breaker/mitigation compute live in both paths; 7 fields carry real non-constant ATR-normalized/bounded values; test suite GREEN; raw-price and self._state both absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| market_data_ohlcv_tradeable -> FeatureFactory | Internal already-validated OHLCV; no external input crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-01 | Tampering (silent wrong data) | OB/breaker fields default-constant (todo-153 mode) | mitigate | test_smc_order_blocks.py asserts non-constant, ATR-formula-correct values across fixtures; vacuous-pass guard forces the break/mitigate branch. |
| T-164-02 | Information disclosure | raw ob_top/ob_bottom/breaker_top leaking into a training feature | mitigate | Field-by-Field audit applied; test asserts those attributes do not exist; drift gate holds. |
| T-164-04 | Tampering (non-reproducible state) | archived self._state cross-call memory | mitigate | Breaker/mitigation derived statelessly from OB's in-pass list; determinism test enforces identical-input->identical-output. |
| T-164-05 | Denial of service | NaN/Inf/raise on cold-start or atr<=0 | mitigate | _OB_FALLBACK early-return; atr_valid guard first; never raises (V5). |
| T-164-SC | Tampering (supply chain) | package installs | accept | No new packages. |
</threat_model>

<verification>
- test_smc_order_blocks.py GREEN (non-constant, ATR-correct, break/mitigate branch exercised, determinism, raw-price absence).
- Batch parity test green.
- No self._state / FeatureCache field added for breaker/mitigation.
- ruff clean.
</verification>

<success_criteria>
Order Blocks + Breaker/Mitigation compute in both live and batch paths, producing real
ATR-normalized/bounded/flag values for all 7 fields, derived in one stateless pass with the hard
OB->breaker/mitigation dependency respected and no raw-price or cross-call state leakage.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-02-SUMMARY.md` when done.
</output>
