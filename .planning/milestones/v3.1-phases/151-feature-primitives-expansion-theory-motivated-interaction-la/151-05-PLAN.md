---
phase: 151-feature-primitives-expansion-theory-motivated-interaction-la
plan: 05
type: execute
wave: 4
depends_on: ["151-04"]
files_modified:
  - src/intelligence/schemas.py
  - src/intelligence/feature_factory.py
  - src/intelligence/features/feature_vector_persistence.py
  - services/backfill_feature_factory.py
  - production/migrations/263_named_interaction_primitives.sql
  - tests/unit/intelligence/test_feature_factory_batch.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "The 5 named tier-1 interaction candidates from ROADMAP's consolidated roster exist as feature_vectors columns: ret_div_1m_5m, ret_div_5m_1h, ret_div_1h_1d, opex_flag, quad_witching_flag"
    - "Every new tier=1_interaction registry row has exactly 2 non-empty parent_features — never the empty array ROADMAP's design-rules text specifies"
    - "Cross-TF divergences are NULL (not 0.0) at timeframes where the pair is undefined"
    - "ops_interaction_primitives_pilot.py::_load_interaction_features loads all rows without raising its 2-parent ValueError"
    - "Every new feature_registry row has a matching concept_registry (domain='feature') + concept_gate row, and every parent_features pair has a matching concept_parent edge, satisfying Phase 170's ic_engine parity precondition"
  artifacts:
    - path: "production/migrations/263_named_interaction_primitives.sql"
      provides: "5 nullable feature_vectors columns + 5 tier=1_interaction registry rows + 5 paired concept_registry/concept_gate rows + 10 concept_parent edges (Phase 170 parity)"
      contains: "quad_witching_flag"
    - path: "src/intelligence/feature_factory.py"
      provides: "cross-TF divergence and OPEX/quad-witching flag compute"
      contains: "_opex_flag"
  key_links:
    - from: "production/migrations/263_named_interaction_primitives.sql"
      to: "scripts/ops/alpha/ops_interaction_primitives_pilot.py"
      via: "parent_features arity contract enforced by _load_interaction_features"
      pattern: "parent_features"
    - from: "services/backfill_feature_factory.py::_build_ctf_series"
      to: "src/intelligence/feature_factory.py::compute_batch"
      via: "ctf_by_ts bisect lookup extended with the HTF last log return"
      pattern: "ctf_by_ts"
---

<objective>
Register and compute the 5 named tier-1 interaction candidates from ROADMAP's consolidated
2026-07-24 roster: todo 066's three cross-timeframe return divergences and todo 104's two
event flags.

Purpose: These are the only tier-1 candidates that arrived pre-specified and Fable-reviewed. They
also exercise the two registration shapes plan 151-06's designed interactions do not: an
`requires_htf=true` cross-TF feature, and a sparse binary event flag that must be excluded from
the standard Spearman IC path.

**Binding correction — do not follow ROADMAP.md verbatim.** ROADMAP's Theory-Motivated Interaction
Layer design-rules paragraph says interaction features register with `parent_features=[]`. That is
wrong. Every one of the 8 live `tier=1_interaction` rows has exactly 2 non-empty
`parent_features`, and `scripts/ops/alpha/ops_interaction_primitives_pilot.py:132-139` hard-raises
`ValueError` on any other arity. Migration 169's own column comment defines `1_interaction` as
"deterministic combination of two tier-0 features."

Output: 5 new nullable `FeatureVector` fields, 5 `feature_vectors` columns, 5 `feature_registry`
rows (`tier='1_interaction'`), extended cross-TF plumbing, unit coverage.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v3.1-phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-RESEARCH.md
@.planning/milestones/v3.1-phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-PATTERNS.md
@.planning/milestones/v3.1-phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-04-SUMMARY.md
@.planning/todos/deferred/066-cross-tf-divergence-primitives.md
@CLAUDE.md
</context>

<interfaces>
Live contracts (verified 2026-07-24 by direct source read and DB query — do not re-derive):

Live `tier=1_interaction` row shape (all 8 rows, `group_name='volume'`, `requires_htf=false`):
    feature_name        | parent_features            | formula_short
    vol_body_product    | {body_ratio,volume_z}      | body_ratio * volume_z
    ret_vol_ratio_fast  | {ret_lag_fast,atr_z}       | ret_lag_fast / atr_z
    price_vol_corr_fast | {ret_lag_1,volume_z}       | rolling Pearson(|log ret|, volume) fast

`scripts/ops/alpha/ops_interaction_primitives_pilot.py:115-141` — `_load_interaction_features`
queries `WHERE tier = '1_interaction' AND status = 'active'` and raises `ValueError` for any row
whose `len(parent_features) != 2`. The query itself needs no change; new rows are picked up
automatically. Sparse-flag exclusion is plan 151-08's job, not this plan's.

`services/backfill_feature_factory.py:130-135`:
    _CTF_HIGHER_TF: dict[str, str] = {"5m": "1h", "15m": "1h", "1h": "1d", "1d": "1d"}
`services/backfill_feature_factory.py:314` — `_build_ctf_series(htf_bars, config) -> dict`
    returns `{htf_bar_ts: (ctf_momentum, ctf_vwap_align, ctf_regime_align)}`, single-pass O(n),
    all values causal. Consumed via bisect against `ctf_ts_list` in `compute_batch`.
`services/backfill_feature_factory.py:1028-1035` — the per-symbol CTF build site.

Live corpus coverage in `market_data_ohlcv_tradeable` (2026-07-24) — LOAD-BEARING for `ret_div_1m_5m`:
    `1m` = 1,237,857 rows, 2026-03-23 → 2026-06-23   (~3 months)
    `5m` = 25,387,548 rows, 2006-06-02 → 2026-07-07  (~20 years)
`feature_vectors` timeframes are exactly {5m, 15m, 1h, 1d} — there is NO 1m feature grain.

Existing calendar helpers in `src/intelligence/feature_factory.py`: `_dow_encoding` (~1623),
`_quarter_position` (~2842). Plan 151-01 added `_quarter_cycle_encoding`, `_tdom_encoding`,
`_minute_of_hour_encoding` alongside them.

After plan 151-04: `len(dataclasses.fields(FeatureVector))` == 200, `feature_registry` has 200 rows,
`FEATURE_VECTOR_INSERT_SQL_PSYCOPG2` has 209 placeholders. This plan takes them to 205 / 205 / 214.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Cross-TF return divergences (3 nullable fields)</name>
  <files>src/intelligence/schemas.py, src/intelligence/feature_factory.py, services/backfill_feature_factory.py</files>
  <read_first>
    - services/backfill_feature_factory.py lines 314-400 (_build_ctf_series full body)
    - services/backfill_feature_factory.py lines 118-140, 1020-1075 (CTF map, per-symbol build, compute_batch call)
    - src/intelligence/feature_factory.py lines 4126-4175 (compute_batch signature + batch/live contract docstring)
    - src/intelligence/feature_factory.py — grep `ctf_by_ts` and `ctf_ts_list` for the bisect lookup block
    - src/intelligence/schemas.py lines 1204-1300 (docstring + Cross-timeframe field block)
    - .planning/todos/deferred/066-cross-tf-divergence-primitives.md
  </read_first>
  <action>
Add 3 `float | None` fields to `FeatureVector`, declared contiguously immediately after
`rate_beta_z` (plan 151-04's last field): `ret_div_1m_5m`, `ret_div_5m_1h`, `ret_div_1h_1d`.

These are nullable BY DESIGN, not defensively. Each is pinned to a specific timeframe pair, and
`feature_vectors` has one row per (symbol, tf, bar_ts). Emit a value ONLY at the lower timeframe of
the pair; emit `None` everywhere else:
  `ret_div_5m_1h` — populated at `tf == "5m"` only
  `ret_div_1h_1d` — populated at `tf == "1h"` only
  `ret_div_1m_5m` — populated at `tf == "5m"` only, and only for bar timestamps where 1m OHLCV
    exists
`None`, not `0.0`, is the correct value where undefined — Phase 140.5's P1 resolution for VP/SR
established exactly this ("the correct answer is NULL, not 0.000"), and a 0.0 divergence is a
substantive claim (two timeframes agreeing) that would be indistinguishable from "not computed."

Extend `_build_ctf_series` in `services/backfill_feature_factory.py` to emit a fourth value: the
HTF bar's own last log return (`log(close[k] / close[k-1])`, causal, 0.0 at k=0). Replace the
3-tuple with a `typing.NamedTuple` (`CtfValues`) carrying `ctf_momentum`, `ctf_vwap_align`,
`ctf_regime_align`, `htf_last_log_ret`, mirroring the `CrossAssetValues` change plan 151-04 made —
positional unpacking of a growing tuple is exactly the drift risk both changes remove. Update the
bisect consumer in `compute_batch` accordingly.

Add a new per-symbol builder for the 1m side, called only when `tf == "5m"`:
  `_build_ltf_return_series(ltf_bars: list[dict], target_ts_list: list) -> dict`
  returning `{target_bar_ts: last_1m_log_return_at_or_before(target_bar_ts)}`, built by a single
  merge walk over the two sorted timestamp lists (O(n+m)), causal — never look past the target bar's
  own timestamp. Fetch its input via `_fetch_bars_from_db(conn, symbol, "1m")` in the per-symbol
  setup block next to the existing CTF build (line ~1028), guarded by `if tf == "5m"`.

Thread the result into `FeatureFactory.compute_batch()` as a new
`ltf_ret_by_ts: dict | None = None` keyword, same shape as the existing `ctf_by_ts`. Compute:
  `ret_div_1m_5m = ltf_ret_by_ts[bar_ts] - own_bar_log_return`, `None` when the key is absent
  `ret_div_5m_1h = own_bar_log_return - ctf.htf_last_log_ret` at tf=5m, else `None`
  `ret_div_1h_1d = own_bar_log_return - ctf.htf_last_log_ret` at tf=1h, else `None`
where `own_bar_log_return` is this bar's own log return (reuse the already-computed `ret_lag_1`
series rather than recomputing).

**Log the coverage once, never per row** (CLAUDE.md's never-log-per-row-over-the-corpus rule):
at the end of each per-symbol compute, emit a single structured log line with the count of bars
where each of the three divergences was non-None, alongside the total bar count.

Set `FEATURE_VECTOR_DOMAIN` entries for all 3 to `"quant"`, matching the existing `ctf_momentum`
mapping. Add all 3 to `_cold_start_vector()` as `None`, and to the live-path `compute()`
constructor as `None` (the live path has no LTF/HTF divergence plumbing today; batch is the
corpus/IC-measurement path — record this asymmetry in the SUMMARY, and reference the todo plan
151-04 Task 4 filed for the analogous live-path cross-asset gap).

**Coverage constraint to record explicitly in the SUMMARY, not to work around:** `ret_div_1m_5m`
will be non-None for roughly 1% of the 5m corpus, because 1m OHLCV covers 2026-03-23 → 2026-06-23
while 5m covers 2006-06-02 → 2026-07-07. This is a real data-availability limit, not an
implementation defect. It is implemented anyway rather than dropped so that the IC screen in plan
151-08 produces a measured verdict on it. If `ic_engine`'s minimum-observation gate rejects the
column, that is the honest outcome — record it, do not paper over it.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_feature_factory_batch.py tests/unit/services/test_backfill_feature_factory.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import dataclasses; from src.intelligence.schemas import FeatureVector; print(len(dataclasses.fields(FeatureVector)))"` prints 203
    - `grep -c "class CtfValues" services/backfill_feature_factory.py` returns 1 and `grep -rl "class CtfValues" src/ services/ | wc -l` returns 1 (exactly one definition project-wide)
    - `grep -rn "CtfValues(" src/ services/` shows zero positional constructions - every call site passes keyword arguments, so a field-order mistake cannot silently change behaviour (addresses cross-AI review concern, Codex MEDIUM: positional-unpacking brittleness in growing tuple payloads)
    - `grep -c "def _build_ltf_return_series" services/backfill_feature_factory.py` returns 1
    - A unit test asserts `compute_batch(..., tf="15m", ...)` yields `ret_div_5m_1h is None` and `ret_div_1h_1d is None` on every bar
    - A unit test asserts `compute_batch(..., tf="5m", ...)` with a synthetic `ctf_by_ts` yields `ret_div_5m_1h == own_log_ret - htf_last_log_ret` to 1e-12
    - A unit test asserts `_build_ltf_return_series` never returns a value derived from a 1m bar whose timestamp is strictly after the target 5m bar timestamp (causality)
    - `grep -n "ctf_momentum" src/intelligence/feature_factory.py` shows `ctf_momentum` still read from the same source (existing three CTF features unchanged); a regression test asserts their values match pre-change to 1e-12
  </acceptance_criteria>
  <done>Three cross-TF divergences compute in the batch path from a causal merge of LTF/HTF return series, are NULL at every timeframe where the pair is undefined, and the three pre-existing ctf_* features are byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: OPEX and quad-witching event flags (2 fields)</name>
  <files>src/intelligence/schemas.py, src/intelligence/feature_factory.py, src/intelligence/features/feature_vector_persistence.py</files>
  <read_first>
    - src/intelligence/feature_factory.py lines 1615-1640, 2835-2865 (calendar helper block, including plan 151-01's additions)
    - docs/research/signal-temporal-atomic-primitives.md lines 195-300 (event-flag doctrine, controls, power analysis)
    - src/intelligence/features/feature_vector_persistence.py lines 75-130, 170-255, 271-310
  </read_first>
  <action>
Add 2 `float` fields to `FeatureVector` immediately after `ret_div_1h_1d`, completing this plan's
contiguous 5-field run: `opex_flag`, `quad_witching_flag`. These are non-nullable (a calendar flag
is always computable from `bar_ts`) and are encoded as `1.0`/`0.0` floats, matching the existing
binary calendar features (`in_ny_session`, `power_hour`, `above_wk_vwap`).

Update the class docstring: opening line to `205 orthogonal feature primitives`, tally to
`Total: 205 (193 required + 12 optional [3 cross-sectional + 5 canary + 2 factor beta + 3 cross-TF
divergence])`, and add a group line naming the 5 additions with their tier-1 provenance.

Add two pure helpers next to the other calendar functions, both taking a timezone-aware UTC
`bar_ts: datetime` and returning `float`:
  `_opex_flag(bar_ts) -> float` — `1.0` when `bar_ts.weekday() == 4` (Friday) AND the bar date is
    in the third week of the month, else `0.0`. Compute "third week" as
    `week_of_month = (bar_ts.day - 1) // 7 + 1` and require `week_of_month == 3` — matching the
    source doc's `(dow == Friday) AND (week_of_month == 3)` formula literally. Do NOT introduce a
    market-holiday table; the source doc explicitly rejects it as nonstationary institutional data.
  `_quad_witching_flag(bar_ts) -> float` — `1.0` when `_opex_flag(bar_ts) == 1.0` AND
    `bar_ts.month % 3 == 0`, else `0.0`. Call `_opex_flag` directly, do not restate its condition.

Wire both into `compute()`, `compute_batch()`, and `_cold_start_vector()` (compute the real value
at cold start — `bar_ts` is available). Add 2 `FEATURE_VECTOR_DOMAIN` entries, both `"calendar"`.

Then extend `src/intelligence/features/feature_vector_persistence.py`:
1. Module-docstring paragraph dated to this session recording 209 → 214 columns (migration 263,
   Phase 151 Plan 05), matching the existing dated-paragraph style.
2. A seventh derived slice `_PHASE151_INTERACTION_NAMED_FIELD_NAMES` spanning index of
   `ret_div_1m_5m` through index of `quad_witching_flag` inclusive — the
   `_STRUCTURAL_VP_SR_FIELD_NAMES` idiom, never a hand-typed list.
3. Append to the `FEATURE_VECTOR_INSERT_SQL` column list, append the `$N` placeholder range block,
   extend `_TOTAL_COLUMNS`.
4. Update `feature_vector_to_insert_params`' docstring column map and change `209-element` to
   `214-element`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_feature_vector_persistence_completeness.py tests/unit/intelligence/test_feature_factory_p7.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import dataclasses; from src.intelligence.schemas import FeatureVector; print(len(dataclasses.fields(FeatureVector)))"` prints 205
    - `python -c "from src.intelligence.features.feature_vector_persistence import FEATURE_VECTOR_INSERT_SQL_PSYCOPG2 as s; print(s.count('%s'))"` prints 214
    - `_quad_witching_flag` body contains a call to `_opex_flag(` (no restated condition)
    - Unit test: `_opex_flag` is 1.0 for 2026-03-20 (third Friday of March 2026) and 0.0 for 2026-03-13 and 2026-03-27
    - Unit test: `_quad_witching_flag` is 1.0 for 2026-03-20 and 0.0 for 2026-04-17 (a non-quarter-end OPEX Friday)
    - Unit test: over a full synthetic year of daily bars, `sum(opex_flag) == 12` and `sum(quad_witching_flag) == 4`
    - `test_every_feature_vector_field_is_a_sql_column` and `test_insert_params_tuple_length_matches_sql` both pass
  </acceptance_criteria>
  <done>Both event flags compute from bar_ts alone with no holiday table, fire exactly 12x and 4x per year, and all 5 Plan-05 fields reach the INSERT tuple.</done>
</task>

<task type="auto">
  <name>Task 3: Migration 263 — 5 tier-1 interaction registry rows with exactly 2 parents each</name>
  <files>production/migrations/263_named_interaction_primitives.sql, tests/unit/intelligence/test_feature_registry_service.py</files>
  <read_first>
    - production/migrations/255_vp_structural_primitives.sql (full file — template)
    - production/migrations/206_partial_ic_interaction_primitives.sql (APR-triplet second example)
    - scripts/ops/alpha/ops_interaction_primitives_pilot.py lines 110-145 (_load_interaction_features ValueError guard)
    - tests/unit/intelligence/test_feature_registry_service.py (existing assertion style)
  </read_first>
  <action>
Re-verify the next-free migration number: `ls production/migrations/ | sort -t_ -k1 -n | tail -5`.
Provisional target 263 (259-262 reserved by plans 151-01 through 151-04). If taken, use the actual
next-free number and update the filename accordingly.

Write `production/migrations/263_named_interaction_primitives.sql`, `BEGIN;`/`COMMIT;`-wrapped.
No new APR keys in this migration — todo 066's own note ("No new APR keys — differences of two
atomic per-TF returns, no window") was confirmed correct by its 2026-07-24 Fable review, and the
two flags are parameterless.

Section 2 DDL — 5 `ALTER TABLE feature_vectors ADD COLUMN IF NOT EXISTS <name> DOUBLE PRECISION;`
in dataclass order, each with a `COMMENT ON COLUMN`. The three divergence comments MUST state the
timeframe-pinning and null semantics: `NULL at every timeframe other than <lower tf of the pair>.
Phase 151.` The `ret_div_1m_5m` comment must additionally state:
`Additionally NULL wherever 1m OHLCV is absent -- 1m coverage is 2026-03-23..2026-06-23 versus 5m's
2006-06-02..2026-07-07, so expect roughly 1 percent non-null coverage. Deliberate, documented data
limitation, not a defect.`

Section 3 `feature_registry` INSERT — 5 rows, all `tier='1_interaction'`, `status='active'`,
`added_phase='151'`, `ON CONFLICT (feature_name) DO NOTHING`. **Every row must supply exactly 2
non-empty `parent_features`** — see the objective's binding correction.

| feature_name | group_name | parent_features | requires_htf | normalization | linear_ready | formula_short |
|---|---|---|---|---|---|---|
| ret_div_1m_5m | cross_tf | {ret_lag_1,ret_lag_2} | true | unbounded_signed | true | last 1m log return - own 5m log return (1m constituent has no same-row column; controls are same-tf short-horizon return proxies) |
| ret_div_5m_1h | cross_tf | {ret_lag_1,ctf_momentum} | true | unbounded_signed | true | own 5m log return - last 1h log return |
| ret_div_1h_1d | cross_tf | {ret_lag_1,ctf_momentum} | true | unbounded_signed | true | own 1h log return - last 1d log return |
| opex_flag | calendar | {dow_sin,week_of_month_sin} | false | bounded_unsigned | false | dow==Friday AND week_of_month==3 (monthly options expiration) |
| quad_witching_flag | calendar | {dow_sin,month_sin} | false | bounded_unsigned | false | opex_flag AND month mod 3 == 0 (quarterly quad-witching) |

Parent-selection rationale, to restate in the SUMMARY: the parents are the same-row columns
`partial_spearman_ic` will control for. `ctf_momentum` is the only HTF-derived column in the
schema, so it is the honest second control for the two well-defined divergence pairs.
`ret_div_1m_5m`'s 1m constituent has NO same-row column at any persisted timeframe; its controls
are therefore one-sided (`ret_lag_1`/`ret_lag_2`, same-tf short-horizon return structure). Record
that as a known measurement caveat — do not pretend the control is complete.
The two flags' parents are exactly the controls `docs/research/signal-temporal-atomic-primitives.md`
lines 202-203 prescribe, restricted to tier-0 columns (migration 169 defines `1_interaction` as a
combination of two tier-0 features, so `quad_witching_flag` takes `dow_sin`/`month_sin` rather
than the tier-1 `opex_flag`).

Section 4 — Phase 170 `concept_registry` parity (added retroactively: this plan was written
2026-07-24, before Phase 170 shipped its dual-write cutover 2026-08-04/05). `ic_engine.py`'s
`_apply_feature_transitions` now runs a PARITY PRECONDITION on every corpus pass that hard-stops
if any `feature_registry` row lacks a `concept_registry` counterpart (Phase 170 Plan 06,
T-170-17). Unlike plans 151-01/03/04, this migration's rows are `tier='1_interaction'` with real
`parent_features`, so a `concept_parent` edge insert is required too (migration 283's L-8 join
table — a single `parent_concept_id` FK cannot represent this). Add immediately after Section 3,
same file, same `BEGIN`/`COMMIT`, deriving every field from the `feature_registry` rows Section 3
just inserted — field mapping matches migration 284's feature-domain seed exactly:

```sql
-- Phase 170 parity: concept_registry/concept_gate/concept_parent mirror for this migration's 5 rows.
INSERT INTO concept_registry
    (domain, name, description, status, enabled, group_name, is_control, control_expectation,
     added_phase, created_at, metadata)
SELECT
    'feature', fr.feature_name, fr.formula_short, fr.status, (fr.status = 'active'),
    fr.group_name, fr.is_control, fr.control_expectation, fr.added_phase, fr.added_at,
    jsonb_strip_nulls(jsonb_build_object(
        'tier', fr.tier, 'formula_short', fr.formula_short, 'normalization', fr.normalization,
        'linear_ready', fr.linear_ready, 'requires_htf', fr.requires_htf,
        'window_apr_keys', to_jsonb(fr.window_apr_keys), 'source_dims', to_jsonb(fr.source_dims),
        'notes', fr.notes, 'apr_namespace', 'feature.'
    ))
FROM feature_registry fr
WHERE fr.feature_name IN (
    'ret_div_1m_5m', 'ret_div_5m_1h', 'ret_div_1h_1d', 'opex_flag', 'quad_witching_flag'
)
ON CONFLICT (domain, name) DO NOTHING;

INSERT INTO concept_gate
    (concept_id, gate_metric_name, gate_eval_method, min_gate_metric, min_gate_n,
     fdr_required, fdr_alpha)
SELECT cr.concept_id, 'ic_sharpe_hac', 'bootstrap_ci', fr.min_ic_sharpe, fr.min_ic_n,
       fr.fdr_required, fr.fdr_alpha
FROM feature_registry fr
JOIN concept_registry cr ON cr.domain = 'feature' AND cr.name = fr.feature_name
WHERE fr.feature_name IN (
    'ret_div_1m_5m', 'ret_div_5m_1h', 'ret_div_1h_1d', 'opex_flag', 'quad_witching_flag'
)
ON CONFLICT (concept_id) DO NOTHING;

INSERT INTO concept_parent (child_concept_id, parent_concept_id)
SELECT child.concept_id, parent.concept_id
FROM feature_registry fr
CROSS JOIN LATERAL unnest(fr.parent_features) AS pf(parent_name)
JOIN concept_registry child  ON child.domain = 'feature' AND child.name = fr.feature_name
JOIN concept_registry parent ON parent.domain = 'feature' AND parent.name = pf.parent_name
WHERE fr.feature_name IN (
    'ret_div_1m_5m', 'ret_div_5m_1h', 'ret_div_1h_1d', 'opex_flag', 'quad_witching_flag'
)
ON CONFLICT DO NOTHING;
```

Add to this task's acceptance criteria:
- `SELECT count(*) FROM concept_registry WHERE domain='feature' AND added_phase='151' AND name IN ('ret_div_1m_5m','ret_div_5m_1h','ret_div_1h_1d','opex_flag','quad_witching_flag');` returns 5
- `SELECT count(*) FROM concept_gate cg JOIN concept_registry cr USING (concept_id) WHERE cr.domain='feature' AND cr.added_phase='151' AND cr.name IN ('ret_div_1m_5m','ret_div_5m_1h','ret_div_1h_1d','opex_flag','quad_witching_flag');` returns 5
- `SELECT count(*) FROM concept_parent cp JOIN concept_registry child ON child.concept_id = cp.child_concept_id WHERE child.domain='feature' AND child.name IN ('ret_div_1m_5m','ret_div_5m_1h','ret_div_1h_1d','opex_flag','quad_witching_flag');` returns 10

Apply:
`PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/263_named_interaction_primitives.sql`

Add a regression test to `tests/unit/intelligence/test_feature_registry_service.py` guarding the
Pitfall-1 correction permanently:
  `test_every_interaction_row_has_exactly_two_parents` — loads all `tier='1_interaction'` rows
  (mock or fixture, matching the file's existing style) and asserts `len(parent_features) == 2` and
  that neither element is empty/None for every row. This is the guard that stops a future migration
  from re-introducing ROADMAP's `parent_features=[]`.

Then prove the live contract holds end to end: run
`PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT feature_name, array_length(parent_features,1) FROM feature_registry WHERE tier='1_interaction' ORDER BY 1;"`
and confirm all 13 rows (8 pre-existing + 5 new) report `2`. Record the output in the SUMMARY.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM feature_registry;"` returns 205
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM feature_registry WHERE tier='1_interaction';"` returns 13
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM feature_registry WHERE tier='1_interaction' AND (parent_features IS NULL OR array_length(parent_features,1) <> 2);"` returns 0
    - `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tA -c "SELECT count(*) FROM feature_registry WHERE added_phase='151' AND requires_htf=true;"` returns 3
    - `test_every_interaction_row_has_exactly_two_parents` exists and passes
    - `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
  <done>Migration 263 applied; all 13 tier-1 rows carry exactly 2 parents; the arity contract is guarded by a permanent regression test; 205-row registry matches the 205-field dataclass.</done>
</task>

</tasks>

<threat_model>
No applicable ASVS Level 1 category (internal compute + DDL over already-ingested OHLCV, no
external input, no auth surface, no new packages). Per `151-RESEARCH.md`'s Security Domain section.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-151-09 | Tampering | `parent_features` arity contract | mitigate | Exactly-2 parents on every new row plus a permanent `test_every_interaction_row_has_exactly_two_parents` regression test; the pilot script's own `ValueError` guard is the second line of defense |
| T-151-10 | Information Disclosure | look-ahead bias in the LTF/HTF merge | mitigate | `_build_ltf_return_series` is a causal merge walk with a dedicated unit test asserting no value derives from a bar strictly after the target timestamp |
</threat_model>

<verification>
1. `.venv/bin/pytest tests/unit/ -q` green.
2. `.venv/bin/ruff check src/intelligence/ services/backfill_feature_factory.py` exits 0.
3. Registry/dataclass alignment: 205 == 205.
4. `python -c "import asyncio,asyncpg"` + a direct run of `ops_interaction_primitives_pilot.py`'s
   `_load_interaction_features` against the live DB returns 13 rows without raising.
5. The three pre-existing `ctf_*` features are output-identical (regression test in Task 1).
</verification>

<success_criteria>
- 5 new `DOUBLE PRECISION` columns; the 3 divergences nullable and timeframe-pinned
- 5 new `tier='1_interaction'` registry rows, each with exactly 2 non-empty parents
- `concept_registry` (domain='feature') and `concept_gate` each have 5 matching new rows, plus 10 `concept_parent` edges (Phase 170 parity — Section 4)
- `ret_div_1m_5m`'s ~1% coverage limitation documented in the migration comment and the SUMMARY
- `opex_flag` fires 12x/year and `quad_witching_flag` 4x/year, proven by unit test
- Full unit suite green
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/151-feature-primitives-expansion-theory-motivated-interaction-la/151-05-SUMMARY.md` when done.
Record the ACTUAL migration number, the parent-selection rationale, the per-symbol coverage counts
for all three divergences, and the batch-vs-live asymmetry note.
</output>
