---
phase: 162-ic-engine-corpus-pipeline-throughput-incremental-recompute-t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/_batch_utils.py
  - src/intelligence/statistics/ic_math.py
  - services/ic_engine.py
  - services/ensemble_ic_engine.py
  - production/migrations/249_ic_feature_block_apr_keys.sql
  - tests/unit/test_batch_utils_short_lived_conn.py
  - tests/unit/test_ic_math_walk_forward_folds.py
  - tests/unit/test_ic_engine_compute_split.py
autonomous: true
requirements: [SC-6, SC-7]

must_haves:
  truths:
    - "A worker-side dsn connection that raises mid-fetch still closes (no leak)"
    - "build_walk_forward_folds returns bit-identical fold boundaries to the 4 inline copies it replaces"
    - "The cross-sectional per-scale block is extracted into a helper mirroring _compute_one_regime_cell"
    - "Feature-blocked rank/IC/CI/fold compute produces bit-identical feature_ic_scores to the unblocked path"
    - "Peak transient memory no longer scales with n_features; a cell above alpha.ic.max_cell_rows fails loudly"
  artifacts:
    - path: "services/_batch_utils.py"
      provides: "short_lived_conn(dsn) contextmanager (todo 129)"
      contains: "def short_lived_conn"
    - path: "src/intelligence/statistics/ic_math.py"
      provides: "build_walk_forward_folds pure boundary math (todo 009E)"
      contains: "def build_walk_forward_folds"
    - path: "services/ic_engine.py"
      provides: "_compute_one_cross_sectional_cell + shared feature-blocked _subsample_and_rank helper"
      contains: "feature_block_columns"
    - path: "production/migrations/249_ic_feature_block_apr_keys.sql"
      provides: "alpha.ic.feature_block_columns + alpha.ic.max_cell_rows APR seeds"
      contains: "alpha.ic.max_cell_rows"
  key_links:
    - from: "services/ic_engine.py::_compute_one_regime_cell"
      to: "services/ic_engine.py::_subsample_and_rank"
      via: "shared feature-blocked helper call"
      pattern: "_subsample_and_rank"
    - from: "services/ic_engine.py::_compute_one_cross_sectional_cell"
      to: "services/ic_engine.py::_subsample_and_rank"
      via: "shared feature-blocked helper call"
      pattern: "_subsample_and_rank"
    - from: "services/ic_engine.py"
      to: "src/intelligence/statistics/ic_math.py::build_walk_forward_folds"
      via: "import + call at former inline fold sites"
      pattern: "build_walk_forward_folds"
---

<objective>
Structural refactor pass over `ic_engine.py`'s two worker functions (`_compute_symbol_tf`'s
already-extracted `_compute_one_regime_cell` and the still-inline `_compute_cross_sectional_tf`),
landing three coupled changes as one wave, each gated on bit-identical `feature_ic_scores` output
before the next begins. This is deliberately FIRST in the phase: the Plan 03 fingerprint hashes
first-party source bytes, so any refactor landing after it ships invalidates every fingerprint and
buys a full 25-30h recompute. Land all structural churn here, once, against final code.

Purpose: Close todos 129 (connection leak), 009E (`build_walk_forward_folds` extraction), and
139/140 (shared rank helper + feature-axis memory bound) on the two hottest functions in one pass,
so a fourth OOM as the corpus grows cannot recur and a hand-pasted rank fix cannot diverge between
the two siblings.

Output: `short_lived_conn(dsn)` in `_batch_utils.py`; `build_walk_forward_folds` in `ic_math.py`;
`_compute_one_cross_sectional_cell` + shared feature-blocked `_subsample_and_rank` in `ic_engine.py`;
migration 248 seeding `alpha.ic.feature_block_columns` and `alpha.ic.max_cell_rows`; three test files.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.1-phases/162-ic-engine-corpus-pipeline-throughput-incremental-recompute-t/162-RESEARCH.md
@.planning/milestones/v3.1-phases/162-ic-engine-corpus-pipeline-throughput-incremental-recompute-t/162-PATTERNS.md
@.planning/milestones/v3.1-phases/162-ic-engine-corpus-pipeline-throughput-incremental-recompute-t/162-VALIDATION.md

<interfaces>
<!-- Extracted from live main @ 2f5334f2. Executor should use these directly. -->

Existing connection helpers:
- services/_batch_utils.py::connect_db_from_url(db_url: str) -> Any   (psycopg2, autocommit off)
- services/ic_engine.py::_short_lived_conn(settings: Settings)        (Settings-based, main-process only — do NOT reuse for worker dsn sites)

Already-extracted symbol-side sibling to mirror for the cross-sectional extraction:
- services/ic_engine.py::_compute_one_regime_cell(...)   @ lines 779-1107

Still-inline block to extract (the `for scale_idx, scale in enumerate(_SCALES):` loop):
- services/ic_engine.py::_compute_cross_sectional_tf(...) @ signature lines 1765-1778, inline block 2054-2179
- Note: cross-sectional block has TWO extras the symbol path lacks — an e-value-pilot column and a
  `max_workers=` bootstrap knob (config.cross_sectional_bootstrap_threads at the _circular_block_bootstrap_ic call).

Walk-forward fold formula (4 identical inline copies to replace):
- services/ic_engine.py:978 (in _compute_one_regime_cell)
- services/ic_engine.py:1533 (daily context-features scalar loop — has an EXTRA `n_valid >= folds*2+embargo` guard; leave that guard at the call site)
- services/ic_engine.py:2123 (in _compute_cross_sectional_tf)
- services/ensemble_ic_engine.py:818
  Formula per fold k: train_end=int(n_valid*(k+1)/(n_folds+1)); test_start=train_end+embargo_bars;
  test_end=int(n_valid*(k+2)/(n_folds+1)); skip if test_start>=test_end or (test_end-test_start)<min_reliable_n.

Root-cause transient to bound (2026-07-19 OOM), services/ic_engine.py:2085-2094:
  ranks_X_scale = rankdata(X_sub_nd, axis=0).astype(np.float32)[valid_mask]
  rankdata() always returns float64 regardless of input dtype — this is the largest live allocation
  at the least-subsampled ("fast") scale for the biggest cross-sectional cells.

ic_math.py contract: pure functions only, no DB, no config loading, no module-global mutable state
besides the documented _Z95 constant; config accessed via Protocol, never by importing ICEngineConfig.

APR from_apr() per-tf dict precedent (services/ic_engine.py:598-606):
  bootstrap_block_size={tf: int(cfg.get_sync(f"alpha.ic.bootstrap_block_size.{tf}", default)) for tf, default in (...)}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract short_lived_conn(dsn) and migrate the 3 worker-side sites (todo 129)</name>
  <files>services/_batch_utils.py, services/ic_engine.py, tests/unit/test_batch_utils_short_lived_conn.py</files>
  <behavior>
    - short_lived_conn(dsn) yields a live connection then closes it in a finally block
    - If the caller raises inside the `with` block, conn.close() is STILL called exactly once
    - The 3 worker-side dsn call sites in ic_engine.py (_compute_symbol_tf x2, _compute_cross_sectional_tf x1)
      route through the context manager instead of hand-rolled open/close
  </behavior>
  <action>
    Add `from contextlib import contextmanager` to `_batch_utils.py` imports. Add
    `@contextmanager def short_lived_conn(dsn: str)` immediately after `connect_db_from_url`,
    wrapping `connect_db_from_url(dsn)` in try/yield/finally-close. This is the worker-side (dsn,
    picklable) sibling of ic_engine's Settings-based `_short_lived_conn` — do NOT confuse the two;
    do NOT widen scope to `regime_writer.py`/`equity_regime_model.py`/`backfill_feature_factory.py`/
    `cross_sectional_regime_model.py` (explicitly deferred per RESEARCH Deferred Ideas). Migrate the
    3 hand-rolled dsn `open -> use -> close` sites in `_compute_symbol_tf` (x2) and
    `_compute_cross_sectional_tf` (x1) onto `with short_lived_conn(dsn) as conn:`. Preserve the
    existing "close connection before the CPU-only clustering/bootstrap phase" ordering (todo 125/102)
    — if a site closes mid-function before clustering, keep that by scoping the `with` block to only
    the fetch phase, not the whole function. Use exception variable name `error` in any new except.
    Write `test_batch_utils_short_lived_conn.py`: monkeypatch `services._batch_utils.connect_db_from_url`
    to return a `Mock()` conn; assert `conn.close()` called once on normal exit AND once when the body
    raises (pytest.raises + mock_conn.close.assert_called_once()). DB-free, no fixtures.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_batch_utils_short_lived_conn.py tests/unit/test_ic_engine_compute_split.py -x -q</automated>
  </verify>
  <done>short_lived_conn exists and closes on exception; all 3 worker dsn sites migrated; new test green; existing compute-split tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract build_walk_forward_folds into ic_math.py, replace 4 inline copies (todo 009E)</name>
  <files>src/intelligence/statistics/ic_math.py, services/ic_engine.py, services/ensemble_ic_engine.py, tests/unit/test_ic_math_walk_forward_folds.py</files>
  <behavior>
    - build_walk_forward_folds(n_valid, n_folds, embargo_bars, min_reliable_n) returns list[(test_start, test_end)]
    - Output is bit-identical to each of the 4 existing inline formulas for a parametrized grid of (n_valid, n_folds, embargo_bars, min_reliable_n)
    - The line-1533 caller's extra `n_valid >= folds*2+embargo` precondition stays at that call site, NOT folded into the shared function
  </behavior>
  <action>
    Add `build_walk_forward_folds(n_valid: int, n_folds: int, embargo_bars: int, min_reliable_n: int)
    -> list[tuple[int, int]]` to `ic_math.py`, placed beside `apply_bh_fdr` (its todo-048/069 sibling).
    Pure boundary math only — no ranking, no IC, no config import (honors the module's stated pure-function
    contract). Body is exactly the per-fold formula in the interfaces block. Replace the 3 inline copies in
    `ic_engine.py` (in `_compute_one_regime_cell`, the daily context-features scalar loop, and
    `_compute_cross_sectional_tf`) and the 1 copy in `ensemble_ic_engine.py` with calls to it, leaving each
    caller's downstream rank/IC step untouched. Leave the line-1533 caller's `n_valid >= folds*2+embargo`
    guard local to that site. Write `test_ic_math_walk_forward_folds.py`: parametrize over a grid and assert
    the returned pairs equal an independent inline recomputation of the same formula (proves extraction is
    faithful). DB-free.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_ic_math_walk_forward_folds.py tests/unit/test_ic_engine_compute_split.py -x -q</automated>
  </verify>
  <done>build_walk_forward_folds in ic_math.py; all 4 inline copies replaced; new test green; bit-identical fold boundaries confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract _compute_one_cross_sectional_cell + shared feature-blocked _subsample_and_rank (todos 139/140)</name>
  <files>services/ic_engine.py, production/migrations/249_ic_feature_block_apr_keys.sql, tests/unit/test_ic_engine_compute_split.py</files>
  <behavior>
    - The cross-sectional per-scale block is extracted into a helper whose shape mirrors _compute_one_regime_cell
    - A shared _subsample_and_rank helper computes rank/IC/CI/fold work in feature-column blocks of size alpha.ic.feature_block_columns, writing into a preallocated float32 output
    - Output is bit-identical to the pre-refactor unblocked path on the regression reference cell
    - A cell whose row count exceeds alpha.ic.max_cell_rows fails loudly (error row in run summary, nonzero job status), never routes to an alternate algorithm
  </behavior>
  <action>
    Author `production/migrations/249_ic_feature_block_apr_keys.sql` (config_schema + config_state
    ON CONFLICT DO NOTHING + config_history WHERE NOT EXISTS triple, per migration 225's pattern):
    `alpha.ic.feature_block_columns` (int, default e.g. 32, provenance `[initial_estimate]` — number of
    feature columns processed per block; a peak-memory-vs-loop-overhead knob, not a statistic; not an ML
    target) and `alpha.ic.max_cell_rows` (int, default sized ~1.5-2x today's largest 5m/low_bull cell
    ~599K observations, provenance `[rca_analysis]` — a crash-loud ceiling, not a router). Re-check
    `ls production/migrations/ | sort -t_ -k1 -n | tail -3` immediately before writing; increment past
    any 247+ that has landed. Bind both keys in `ICEngineConfig.from_apr()` and add the fields to the
    dataclass (defaulted, matching the existing post-143 defaulted-field convention so direct-constructor
    test sites do not break). Extract `_compute_cross_sectional_tf`'s inline `for scale_idx, scale in
    enumerate(_SCALES):` block (2054-2179) into `_compute_one_cross_sectional_cell(...)` with a signature
    comparable to `_compute_one_regime_cell` — preserving the cross-sectional-only e-value-pilot column and
    `max_workers=` bootstrap knob. Then extract the shared `_subsample_and_rank(...)` helper called by BOTH
    `_compute_one_regime_cell` and `_compute_one_cross_sectional_cell`, implementing the feature-BLOCKED
    rewrite: process `rankdata(X_sub_nd, axis=0)` in bounded column blocks of `feature_block_columns`,
    writing each block into a preallocated float32 output, so peak transient is O(n_sub x block) not
    O(n_sub x n_features). CRITICAL RNG invariant: draw the bootstrap resample block-start index matrix
    ONCE per scale (shape resamples x ceil(n_valid/block_size)) BEFORE the feature-block loop and reuse it
    across blocks — drawing inside the block loop reorders RNG consumption and changes CI draws. Chunk the
    FEATURE axis only, never the time/row axis (rankdata over a row-block is a different statistic — Risk 8,
    out of scope by definition). Add the crash-loud `max_cell_rows` ceiling check. Extend
    `test_ic_engine_compute_split.py` with `inspect.getsource` parity cases: the new cross-sectional helper
    preserves the float32 rankdata cast; both cell functions call `_subsample_and_rank`; and a synthetic
    unit assertion that feature-blocked output equals unblocked output on a small in-memory array (pure numpy,
    no DB). Gate each internal step on bit-identical output before proceeding.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_ic_engine_compute_split.py tests/unit/test_ic_engine_parallelism.py tests/unit/test_ic_engine_idempotency.py -x -q</automated>
  </verify>
  <done>Cross-sectional cell extracted; shared feature-blocked _subsample_and_rank called by both cell fns; RNG index matrix precomputed once per scale; max_cell_rows crash-loud ceiling; migration 248 seeds both APR keys; feature-blocked output bit-identical to unblocked on the reference cell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream tables -> ic_engine compute | trusted internal batch read; no external/untrusted input crosses here |
| ProcessPoolExecutor worker <-> main process | picklable dsn/config only; workers are compute-only, never write |

This is an internal batch measurement service: no HTTP surface, no auth, no session, no user
input, no injection surface. Traditional ASVS web categories (authentication, session management,
access control, injection) are **N/A** — there is no attacker-reachable boundary. The relevant
threat class is data-integrity ("silent wrong answers are worse than loud crashes").

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-01-01 | Tampering | feature-blocked rank rewrite altering the statistic | mitigate | Feature-axis chunking only (output-invariant); RNG resample index matrix precomputed once per scale; each step gated bit-identical vs unblocked path before proceeding |
| T-162-01-02 | Denial of Service | oversized cell OOM | mitigate | alpha.ic.max_cell_rows crash-loud ceiling — fails loudly, never silently switches algorithm; feature-block bound caps peak transient at O(n_sub x block) |
| T-162-01-03 | Repudiation | connection leak on worker exception hiding failures | mitigate | short_lived_conn(dsn) guarantees close() in finally; tested via injected exception |
| T-162-01-SC | Tampering | migration 248 collides with reserved 247 | mitigate | Re-check next-free migration number at write time; use 248, never 247 |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/ -q` fully green after the wave.
- After each of the 3 internal steps, `feature_ic_scores` output is bit-identical to pre-refactor
  on the regression reference (do not proceed on "looks right").
- Synthetic oversized-cell memory check (criterion 6) runs ONLY with `ps aux | grep ic_engine`
  confirmed clear (resource contention, Risk 5) — this is a closing manual gate, not a unit test.
</verification>

<success_criteria>
- SC-7: post-refactor feature_ic_scores bit-identical after each internal step; build_walk_forward_folds
  unit-tested against the inline copies' boundaries; injected worker exception no longer leaks a connection.
- SC-6: peak transient memory in both worker functions no longer scales with n_features; a synthetic
  cell ~2x today's largest completes within a measured resident-memory budget and produces bit-identical
  output; a cell above alpha.ic.max_cell_rows fails loudly.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/162-ic-engine-corpus-pipeline-throughput-incremental-recompute-t/162-01-SUMMARY.md` when done.
</output>
