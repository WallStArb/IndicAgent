---
phase: 141.1-measurement-and-decision-integrity-foundation-make-everythin
verified: 2026-07-02T13:47:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
---

# Phase 141.1: Measurement and Decision Integrity Foundation Verification Report

**Phase Goal:** Make everything that feeds ensemble IC measurement — and any future decision/action layer built on top of it — causal, provenance-tracked, and honestly calibrated, before Phase 142A measures OOS ensemble IC on top of it.

**Verified:** 2026-07-02T13:47:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | OOS holdout enforcement: `TRAINING_WINDOW_END = min(MAX(bar_ts), alpha.validation.oos_start)` in the corpus orchestrator, with `alpha.validation.oos_start` gaining real readers | VERIFIED | `scripts/ops/corpus/ops_corpus_pipeline_run.sh:198-236` computes `LEAST(MAX(bar_ts), NULLIF(oos_start,'')::timestamptz)`, fails loud on empty `feature_vectors` (`RAW_MAX_BAR_TS` guard, `exit 1`), warns loudly when no holdout is in effect. Live SQL confirms clamp resolves to `2025-12-24T05:15:00Z` (<= stored `oos_start`). Readers now exist in `ops_corpus_pipeline_run.sh`, `ops_oos_holdout_eval.py`, `ic_engine.py` (docstring/gate), `forward_return_writer.py` (docstring) — up from zero. |
| 2 | Pre-committed, read-only OOS evaluation step exists | VERIFIED | `docs/plans/OOS-EVAL-PROTOCOL.md` committed before OOS data examined; states holdout window, enforcement points, both interim (`ops_oos_holdout_eval.py`) and authoritative (Phase 142A/144 EnsembleICEngine) scorers, once-per-gate cadence, no-tuning rule. `ops_oos_holdout_eval.py` grep for `INSERT INTO\|UPDATE \|DELETE FROM\|TRUNCATE` returns 0 (strictly read-only); reuses `forward_log_return` from `forward_return_writer.py` and `ic_engine.py`'s pure IC helpers by composition. |
| 3 | CR-01 fix landed: `ic_engine.py`/`forward_return_writer.py` no longer silently default `--training-window-end` to bare `MAX(bar_ts)` | VERIFIED | Both files: `--training-window-end` argparse arg has `required=True`, no default fallback branch remains in either file (confirmed by reading `services/ic_engine.py:1932-2019` and `services/forward_return_writer.py:444-499`). `docs/plans/OOS-EVAL-PROTOCOL.md` updated (commit `7fa1f487`) to describe two enforcement layers instead of overstating one. Migration 194 adds `alpha.validation.oos_significant_drop_fraction` (WR-02 magic-number fix), confirmed live in `config_state` (`0.5`) with 5 new unit tests for `_drop_verdict`. |
| 4 | Weight-epoch / silent-retrain fix: `ensemble_weights`/`ensemble_alpha` no longer silently keep stale weights on `ON CONFLICT DO NOTHING`; real per-run `weight_version` epoch threaded through `ensemble_trainer.py` and `alpha_publisher.py`; `alpha_events` `event_id` incorporates `weight_version` | VERIFIED | `grep -c "DO NOTHING" services/ensemble_trainer.py` = 0; `grep -c "DO UPDATE SET"` = 2 (both `ensemble_weights` and `ensemble_alpha` sites). `--weight-version` CLI override present and threaded symmetrically in both `ensemble_trainer.py` and `alpha_publisher.py` via `_effective_weight_version`. `alpha_publisher.py:327` builds `event_id` via 5-arg `content_key(symbol, tf, bar_ts_ns, self.ensemble_version, weight_version)` (no bare 4-arg form remains). Orchestrator derives `WEIGHT_EPOCH` via pure digit-extraction of the clamped freeze point, validates non-empty/non-degenerate (`exit 1` guard), passes `--weight-version "$WEIGHT_EPOCH"` to both step 5 and step 6. |
| 5 | `regime_scope` schema fix: `feature_ic_scores` disambiguates `symbol_hmm`/`cross_sectional`/`pooled`, written from all 3 `ic_engine.py` insert paths, backfilled for existing rows | VERIFIED | Live DB: `regime_scope` column NOT NULL (`is_nullable='NO'` confirmed); `SELECT DISTINCT regime_scope` returns exactly `{cross_sectional, pooled, symbol_hmm}`; zero misclassified `forward_filter` rows (`0` mismatches on the historical-HMM-preserved check). `_resolve_regime_scope` pure resolver present in `ic_engine.py` (3 references — definition + call sites), `regime_scope` appears 7x across `_INSERT_BODY` + 3 append-dict sites. Migration 192 idempotent, self-validating `DO $$` sync block. |
| 6 | Cost hurdle calibration: `alpha.quant.cost_hurdle.*` no longer a no-op-by-construction gate; real calibration script implementing todo 030 Steps 0-3 exists, writes empirical values via audited `ConfigService.set` | VERIFIED | `scripts/ops/corpus/ops_cost_hurdle_calibration.py` exists (24KB), implements Steps 0-3 (cost-floor verdict/percentile calibration/threshold validation/gap-contamination check), 12 passing unit tests for pure verdict helpers, `ConfigService`(5 refs)/`reason=`(4 refs)/`RuntimeError`(4 refs, crash-loud on empty corpus) all present, zero direct corpus-table mutation (grep for `INSERT INTO\|UPDATE feature_\|UPDATE alpha_\|TRUNCATE` = 0), `--write-todo` gate confirmed. Live APR values for `alpha.quant.cost_hurdle.*` remain `0.0` today — this is expected and explicitly scoped: the project is still blocked on the Phase B corpus re-run (`.planning/STATE.md`: "Phase 142 — BLOCKED — pending Phase B corpus re-run"), and both this plan's own SUMMARY and the roadmap's "Partial-corpus note" state the calibrator is an operator step run *after* Phase B completes, not a same-session corpus mutation. The must-have is "a real calibration script must exist" — it does, and is correct/tested. |
| 7 | CR-02 (OOS look-log audit trail) — deliberately deferred | N/A (expected) | Captured as `.planning/todos/pending/044-oos-look-log-audit-trail.md`, referenced explicitly in commit `7fa1f487` message: "CR-02 (OOS look-log audit trail) captured as todo 044 — a design decision on log format/storage, not a same-session fix." This is a documented, intentional deferral of a Warning-severity finding, not a phase gap. |

**Score:** 6/6 applicable truths verified (CR-02 deferral is expected, not a truth to verify)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/ops/corpus/ops_corpus_pipeline_run.sh` | TRAINING_WINDOW_END clamp + WEIGHT_EPOCH derivation | VERIFIED | `bash -n` syntax OK; clamp + guard + WEIGHT_EPOCH block all present and wired to steps 3-6 |
| `scripts/ops/corpus/ops_oos_holdout_eval.py` | Read-only OOS holdout IC scorer | VERIFIED | 0 mutation statements; imports `forward_log_return` + `ic_engine` helpers; `_drop_verdict` extracted (WR-02 fix) |
| `docs/plans/OOS-EVAL-PROTOCOL.md` | Pre-committed governance doc | VERIFIED | Names EnsembleICEngine as authoritative scorer; describes 2-layer enforcement post-fix |
| `tests/unit/test_oos_holdout_eval.py` | Clamp/window/return/verdict unit tests | VERIFIED | 12 tests, all pass (includes 5 new `_drop_verdict` tests from the CR-01 fix commit) |
| `production/migrations/192_feature_ic_scores_regime_scope.sql` | regime_scope column + backfill + CHECK | VERIFIED | Applied live; NOT NULL; 3-value CHECK; 0 mismatches on sync-validation query |
| `services/ic_engine.py` | `_resolve_regime_scope` + `--training-window-end` required | VERIFIED | Resolver present + wired at 3 insert sites; required flag confirmed, no fallback branch |
| `tests/unit/test_regime_scope.py` | Resolver + sync unit tests | VERIFIED | 9 tests pass |
| `production/migrations/193_ensemble_weight_stale_max_days.sql` | APR key (todo 043) | VERIFIED | Live value = 90, `[conventional]` provenance |
| `services/ensemble_trainer.py` | Per-run weight epoch, DO UPDATE, APR stale cliff | VERIFIED | 0 `DO NOTHING`, 2 `DO UPDATE SET`, `weight_stale_max_days` wired, no `> 90` literal |
| `services/alpha_publisher.py` | `--weight-version` override + event_id fold-in | VERIFIED | 5-arg `content_key` confirmed at emission site; `weight_version_override` in `__init__` signature |
| `tests/unit/test_ensemble_weight_epoch.py` | Epoch/DO-UPDATE/stale-cliff tests | VERIFIED | Tests pass |
| `tests/unit/test_alpha_publisher_event_id.py` | event_id distinctness tests | VERIFIED | Tests pass |
| `scripts/ops/corpus/ops_cost_hurdle_calibration.py` | Steps 0-3 calibration script | VERIFIED | Exists, tested, crash-loud gate, audited writes, no corpus-table mutation |
| `tests/unit/test_cost_hurdle_calibration.py` | Verdict helper unit tests | VERIFIED | 12 tests pass |
| `production/migrations/194_oos_significant_drop_fraction.sql` | WR-02 fix: APR-backed magic number | VERIFIED | Applied live, value 0.5, `[initial_estimate]` provenance |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `ops_corpus_pipeline_run.sh` | `config_state (alpha.validation.oos_start)` | `LEAST(...)` clamp SQL | WIRED | Confirmed by live SQL query matching stored value |
| `ops_oos_holdout_eval.py` | `ic_engine.py` + `forward_return_writer.py` | import composition | WIRED | `from services.forward_return_writer import forward_log_return`; `from services.ic_engine import ...` both present |
| `ic_engine.py` (`_resolve_regime_scope`) | `feature_ic_scores.regime_scope` | `_INSERT_BODY` column/placeholder | WIRED | 7 `regime_scope` refs across insert body + 3 append-dict sites |
| `production/migrations/192` backfill | `feature_ic_scores.regime_label_source` | provenance-driven CASE | WIRED | 0 mismatches confirmed against live table |
| `ops_corpus_pipeline_run.sh` | `ensemble_trainer.py` + `alpha_publisher.py` | `--weight-version "$WEIGHT_EPOCH"` | WIRED | Both step 5 and step 6 invocations carry the flag |
| `ensemble_trainer.py` | `ensemble_weights`/`ensemble_alpha` | `ON CONFLICT ... DO UPDATE SET` | WIRED | Confirmed via grep + read of both insert sites |
| `alpha_publisher.py` (event_id) | `alpha_events ON CONFLICT (event_id, bar_ts) DO NOTHING` | `content_key(..., weight_version)` | WIRED | 5-arg form confirmed at line 327 |
| `ops_cost_hurdle_calibration.py` | `config_state` via `ConfigService.set` | audited APR write | WIRED (not yet executed against populated corpus — expected, see Truth 6) | `ConfigService`/`reason=` present; live `cost_hurdle.*` values still 0.0 pending Phase B corpus completion (operator step, out of this phase's scope per roadmap's own partial-corpus note) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Orchestrator bash syntax valid | `bash -n scripts/ops/corpus/ops_corpus_pipeline_run.sh` | exit 0 | PASS |
| OOS clamp resolves correctly against live DB | `SELECT LEAST(MAX(bar_ts), oos_start)` | `2025-12-24 05:15:00+00` (<= stored oos_start) | PASS |
| regime_scope fully backfilled, no NULLs/mismatches | Live SQL sync-validation queries | 0 mismatches, 0 NULLs, correct 3-value set | PASS |
| `ic_engine.py`/`forward_return_writer.py` require `--training-window-end` | grep for `required=True` + absence of fallback branch | confirmed in both files | PASS |
| Migration 194 applied live | `SELECT config_value FROM config_state WHERE config_key='alpha.validation.oos_significant_drop_fraction'` | `0.5` | PASS |
| Full 5 phase-specific test files | `.venv/bin/pytest tests/unit/test_oos_holdout_eval.py tests/unit/test_regime_scope.py tests/unit/test_cost_hurdle_calibration.py tests/unit/test_ensemble_weight_epoch.py tests/unit/test_alpha_publisher_event_id.py -q` | 52 passed | PASS |
| Full unit suite (excluding known pre-existing hang) | `.venv/bin/pytest tests/unit/ -q --ignore=tests/unit/providers/test_ibkr_equity.py` | 33 failed (all pre-existing, documented in deferred-items.md), 5352 passed, 41 skipped | PASS (failures pre-date phase, confirmed unrelated) |
| ruff clean on all phase-touched Python files | `.venv/bin/ruff check <11 files>` | All checks passed | PASS |

### Requirements Coverage

No REQUIREMENTS.md exists for this project (confirmed per task instructions — not a gap). Plan-level `requirements:` tags (OOS-01, RSCOPE-01, COST-01, WEPOCH-01, WEPOCH-043) map 1:1 to the four ROADMAP must-haves and are satisfied per the truths table above.

### Anti-Patterns Found

None. Scanned all phase-touched files (`ops_corpus_pipeline_run.sh`, `ops_oos_holdout_eval.py`, `ops_cost_hurdle_calibration.py`, `ic_engine.py`, `ensemble_trainer.py`, `alpha_publisher.py`, `forward_return_writer.py`, migrations 192-194, `OOS-EVAL-PROTOCOL.md`) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`/stub patterns. Zero unreferenced debt markers found. The only `TODO` string matches are the literal template name `_GAP_FOLLOWUP_TODO_TEMPLATE` (a todo-file-writer helper, not a debt marker) in `ops_cost_hurdle_calibration.py`.

### Post-Review Fix Verification (CR-01/CR-02/WR-02)

The task explicitly flagged commit `7fa1f487` as needing independent verification, not just SUMMARY trust:

- **CR-01 (ic_engine.py/forward_return_writer.py silent MAX(bar_ts) fallback):** FIXED, confirmed. `--training-window-end` is `required=True` in both files' argparse blocks; no `else`/fallback branch computing a bare `MAX(bar_ts)` for the training window remains in either file.
- **WR-02 (unbacked magic-number threshold in OOS eval script):** FIXED, confirmed. Migration 194 adds `alpha.validation.oos_significant_drop_fraction` (live value `0.5`); `_drop_verdict()` pure function extracted with 5 new unit tests covering all verdict branches.
- **IN-01 (doc overstated single enforcement point):** FIXED, confirmed. `docs/plans/OOS-EVAL-PROTOCOL.md` "Enforcement points" section now describes both the orchestrator clamp computation and the required-flag enforcement in both consumer scripts.
- **CR-02 (OOS look-log audit trail):** Deliberately deferred as `.planning/todos/pending/044-oos-look-log-audit-trail.md`, per explicit statement in commit `7fa1f487`. This is expected per task instructions, not a gap.

### Human Verification Required

None. All must-haves are verifiable via static analysis, live database queries, and automated test execution — no UI, real-time behavior, or external-service integration is in scope for this phase.

### Gaps Summary

No gaps found. All four ROADMAP must-haves are implemented, wired, tested, and — where the roadmap's own language implies a running artifact (OOS clamp, regime_scope) — verified against the live database. The one item that remains at its seeded default (`alpha.quant.cost_hurdle.* = 0.0`) is explicitly scoped by the roadmap's own "Partial-corpus note" and Plan 03's SUMMARY as an operator step to be run after the in-progress Phase B corpus re-run completes (confirmed still in progress per `.planning/STATE.md`: "Phase 142 — BLOCKED — pending Phase B corpus re-run"). The phase's deliverable was the calibration script itself, which exists, is correct, is unit-tested, and integrates with the audited APR path — not the corpus execution, which is explicitly out of scope until Phase B finishes.

---

_Verified: 2026-07-02T13:47:00Z_
_Verifier: Claude (gsd-verifier)_
