---
phase: 141.1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ops/corpus/ops_cost_hurdle_calibration.py
  - tests/unit/test_cost_hurdle_calibration.py
autonomous: true
requirements: [COST-01]

must_haves:
  truths:
    - "A committed, re-runnable calibration script implements todo 030 Steps 0-3 (external cost-floor verdict, cost-hurdle percentiles, emission-threshold validation, gap-contamination check) over the current corpus tables"
    - "The script writes the empirical cost-hurdle and threshold APR values through ConfigService.set so every change is recorded in config_history with changed_by + reason"
    - "The cost-hurdle no-op is closed: alpha.quant.cost_hurdle.5m/.15m move from 0.0 to their empirical P10/P25 when the corpus is populated"
    - "The script crashes loud (RuntimeError) when alpha_events/forward_returns are empty rather than writing meaningless zeros"
    - "The gap-contamination finding is ALWAYS reported (and the proposed follow-up todo text printed to the report/console); the follow-up todo FILE is written only when --write-todo is passed (default: report-only), so a runtime script never mutates the repo unless explicitly asked"
  artifacts:
    - path: "scripts/ops/corpus/ops_cost_hurdle_calibration.py"
      provides: "Steps 0-3 empirical calibration + APR writes via ConfigService + markdown report + optional --write-todo"
      contains: "cost_hurdle"
    - path: "tests/unit/test_cost_hurdle_calibration.py"
      provides: "Percentile + threshold-verdict + gap-verdict pure-function unit tests"
  key_links:
    - from: "scripts/ops/corpus/ops_cost_hurdle_calibration.py"
      to: "config_state via ConfigService.set"
      via: "await ConfigService.set(key, value, changed_by='cost_hurdle_calibration', reason='rca_analysis')"
      pattern: "ConfigService"
    - from: "scripts/ops/corpus/ops_cost_hurdle_calibration.py"
      to: "alpha_events / forward_returns / feature_ic_scores"
      via: "read-only percentile + IC queries"
      pattern: "percentile_cont"
---

<objective>
The four `alpha.quant.cost_hurdle.*` APR keys are all seeded `0.0` today — a real no-op gate. 98.3% of current `alpha_events` sit in the 5m/15m band that todo 030 Step 0 already found net-negative-to-marginal after external costs. Before Phase 142B runs frame simulation on `alpha_events`, that population must reflect a real tradeable filter, not a zero gate.

Deliver todo 030's calibration as a committed, idempotent, re-runnable script that: records the external cost-floor verdict (Step 0), sets `alpha.quant.cost_hurdle.5m/.15m` to the empirical P10/P25 of their `alpha_ci_lower` distribution (Step 1), validates whether the emission thresholds are binding and adjusts only on a meaningful gap (Step 2), and checks gap contamination in `forward_returns` (Step 3). Every APR write goes through `ConfigService.set` so provenance lands in `config_history` with `changed_by` + `reason`.

Partial-corpus note: the script gates on a populated corpus (RuntimeError on empty alpha_events/forward_returns) and timestamps the row counts in its report, so a partial/in-flight Phase B corpus produces provisional percentiles that are visibly partial — it does not require full corpus completion, but early-run outputs should be treated as provisional.

The keys already exist (migrations 185/186) so NO migration is needed. The script is standalone and touches NO other service file — deliberately avoiding `ic_engine.py` (owned by Plan 02) and the corpus bash (owned by Plans 01/04) so it runs in parallel in Wave 1 with zero file conflict. The one conditional ic_engine gap-filter change from todo 030 Step 3 is NOT applied here (it is data-dependent and would conflict with Plan 02); instead the script reports the finding and, only when explicitly asked via --write-todo, captures a follow-up todo (default is report-only — no runtime repo mutation).
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/bg/dev/indicagent/.planning/PROJECT.md
@/home/bg/dev/indicagent/.planning/ROADMAP.md
@/home/bg/dev/indicagent/.planning/STATE.md
@/home/bg/dev/indicagent/.planning/todos/pending/030-cost-hurdle-apr-calibration.md
@/home/bg/dev/indicagent/CLAUDE.md

<interfaces>
Live facts verified 2026-07-02:
- APR keys exist in config_state: alpha.quant.cost_hurdle.{5m,15m,1h,1d} (all 0.0), alpha.quant.threshold.{5m=1.5,15m=1.2,1h=1.0,1d=0.8}.
- alpha_events currently has ~172K rows (partial Phase B run in flight); columns include alpha_score, alpha_ci_lower, tf. forward_returns has has_gap_before_entry (bool) and return_fast. feature_ic_scores has ic_mean/ic_value + tf.
- ConfigService (src/config/config_service.py): `async def set(self, key, value, changed_by='system', expected_version=None, reason=None)` at line 165; OPS_PREFIXES includes 'alpha.' (line 39) so alpha.quant.* writes are allowed. config_history columns: timestamp, version, config_key, config_value, changed_by, reason.
- Provenance tag convention (CLAUDE.md APR mandate): use reason='rca_analysis' for empirically-derived values.
- todo 030 exact SQL: Step 1 percentile_cont(0.10/0.25/0.50) of alpha_ci_lower per tf; Step 2 percentile_cont(0.05/0.10/0.25) of abs(alpha_score) per tf; Step 3 avg/stddev(return_fast) grouped by has_gap_before_entry + avg(ic_mean) per tf.
- Step 0 verdict already recorded in todo 030 (5m fast/mid + 15m fast net-negative on unshrunk IC). The script re-emits the verdict table into its report but writes NO APR from Step 0 (it is a verdict, not a calibration).
- DB connection: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent. Todo capture location (CLAUDE.md MEMORY): .planning/todos/pending/.
- CLAUDE.md: "Exception variable name is error"; "All timestamps UTC"; structlog logging.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cost-hurdle / threshold / gap calibration script + pure-function unit tests</name>
  <files>scripts/ops/corpus/ops_cost_hurdle_calibration.py, tests/unit/test_cost_hurdle_calibration.py</files>

  <behavior>
    - test_cost_hurdle_calibration.py (RED first): `_select_cost_hurdle(percentiles: dict) -> float` returns the P25 (or configured percentile) value from a per-tf percentile dict; `_threshold_verdict(current: float, p10: float, p50: float) -> str` returns 'not_binding' when current < p10, 'over_filtering' when current > p50, else 'ok'; `_gap_verdict(mean_gap: float, mean_nogap: float, gap_fraction: float) -> str` returns 'not_supported' when gap_fraction < 0.05 or the return means are within tolerance, else 'material'.
    - Running the script against empty alpha_events raises RuntimeError (no zero writes).
    - When alpha_events is populated, the script sets alpha.quant.cost_hurdle.5m and .15m to their empirical percentiles and records the change in config_history.
    - The script emits a markdown report with the Step 0 verdict table, Step 1 chosen hurdles, Step 2 threshold verdicts, and Step 3 gap verdict.
    - Step 3 gap follow-up: when the gap verdict is 'material', the script ALWAYS reports the finding and prints the proposed follow-up todo text (to the report + console); it writes the todo FILE under .planning/todos/pending/ ONLY when --write-todo is passed. Default (no flag) is strictly report-only — no repo mutation from the runtime script.
  </behavior>

  <read_first>
    - .planning/todos/pending/030-cost-hurdle-apr-calibration.md (the full Step 0-3 spec + the exact SQL for each step + the APR keys to calibrate)
    - src/config/config_service.py:39-52 (OPS_PREFIXES — alpha. present), 60-70 (ConfigService.__init__ + initialize() — MUST be awaited before any get/set touches the DB pool), 99-120 (get_sync), 165-235 (async def set + config_history write)
    - services/forward_return_writer.py:436-490 (standalone-script argparse/settings/psycopg2-or-asyncpg entrypoint pattern to mirror)
    - services/alpha_publisher.py:26-53 + 455-477 (asyncpg + Settings + entrypoint pattern; ConfigService usage)
    - CLAUDE.md APR mandate (provenance tags, config_history), "Exception variable name is error", "asyncpg JSONB/timestamp" rules
    - .planning/todos/pending/043-ensemble-weight-stale-cliff-apr.md (todo-capture file format to mirror if Step 3 writes a follow-up under --write-todo)
  </read_first>

  <action>
    RED FIRST. Create tests/unit/test_cost_hurdle_calibration.py with pure-function tests for `_select_cost_hurdle`, `_threshold_verdict`, `_gap_verdict` (no DB). Run `.venv/bin/pytest tests/unit/test_cost_hurdle_calibration.py -x` and confirm failure.

    GREEN. Create scripts/ops/corpus/ops_cost_hurdle_calibration.py (standalone; mirror services/alpha_publisher.py's Settings + asyncpg + entrypoint shape):
    - argparse: --dry-run (compute + report only, no APR writes), --cost-percentile (choices 'p10'/'p25', default 'p25'), --report-path (default docs/analysis/cost-hurdle-calibration.md), --write-todo (default False; when a material gap is found, write the follow-up todo FILE — otherwise the script only reports/prints the proposed todo text).
    - setup_service_logging("logs/cost_hurdle_calibration.log"); optional init_otel (try/except OTelInitError as error).
    - Construct `ConfigService(database_url=...)` and `await config_service.initialize()` BEFORE any `.get`/`.set` call — the pool is not live until initialize() runs (this is the established pattern at src/config/config_service.py:60-70; a script that calls `.set()` without it fails at runtime, not at test time, since the unit tests are pure-function only).
    - Startup gate: `SELECT count(*) FROM alpha_events` and `SELECT count(*) FROM forward_returns`; if either is 0, raise RuntimeError("cost-hurdle calibration requires a populated corpus — alpha_events/forward_returns empty") — never write 0.0 as if it were calibrated.
    - Step 0 (report only): re-emit the external cost-floor verdict table (median IC gross E[R] per tf/scale vs cost floor) into the report, sourced from todo 030's recorded verdict; note it is unshrunk-IC upper-bound. NO APR write.
    - Step 1: run todo 030's Step 1 SQL (percentile_cont 0.10/0.25/0.50 of alpha_ci_lower per tf). For 5m and 15m, choose the hurdle via `_select_cost_hurdle` at the --cost-percentile; leave 1h/1d at 0.0 (cost immaterial at longer horizons per todo). Write alpha.quant.cost_hurdle.5m and .15m via `await ConfigService.set(key, str(value), changed_by='cost_hurdle_calibration', reason='rca_analysis')` unless --dry-run.
    - Step 2: run Step 2 SQL (percentile_cont 0.05/0.10/0.25 of abs(alpha_score) per tf); for each tf compute `_threshold_verdict(current_threshold, p10, p50)`; only when the verdict is 'over_filtering' (current > P50) write a lowered alpha.quant.threshold.{tf} (to P25) via ConfigService.set with reason='rca_analysis'; when 'not_binding' leave as-is and record the note. Report every verdict.
    - Step 3: run Step 3 SQL (avg/stddev(return_fast) by has_gap_before_entry, + avg(ic_mean) per tf); compute `_gap_verdict`; if 'material', do NOT edit ic_engine here. ALWAYS record the finding in the report and PRINT the proposed follow-up todo text (the recommended one-line `WHERE has_gap_before_entry = false` change to the ic_engine forward_returns join) to the report/console. WRITE the follow-up todo file under .planning/todos/pending/ (e.g. 0NN-gap-contamination-ic-filter.md) ONLY when args.write_todo is True. If 'not_supported', record no-action.
    - Write the markdown report to --report-path with all four steps.
    - Every APR write must record config_history (ConfigService.set does this) — verify by including the write count in the report.

    The three verdict/selection helpers must be module-level pure functions so the unit tests exercise them without a DB. Do NOT edit ic_engine.py, ensemble_trainer.py, alpha_publisher.py, or the corpus bash. Do NOT add a migration (keys already exist).

    REFACTOR. `.venv/bin/ruff check scripts/ops/corpus/ops_cost_hurdle_calibration.py tests/unit/test_cost_hurdle_calibration.py --fix` and `.venv/bin/black` both.
  </action>

  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_cost_hurdle_calibration.py -x -q` passes; verdict-boundary cases covered (current<p10, current>p50, gap_fraction<0.05).
    - `grep -c "ConfigService" scripts/ops/corpus/ops_cost_hurdle_calibration.py` >= 1 and `grep -c "reason=" scripts/ops/corpus/ops_cost_hurdle_calibration.py` >= 1 (writes go through the audited APR path with a reason).
    - `grep -cE "cost_hurdle.5m|cost_hurdle.15m" scripts/ops/corpus/ops_cost_hurdle_calibration.py` >= 1 (targets the seeded-at-zero keys).
    - `grep -c "RuntimeError" scripts/ops/corpus/ops_cost_hurdle_calibration.py` >= 1 (crash-loud on empty corpus).
    - `grep -cE "INSERT INTO|UPDATE feature_|UPDATE alpha_|TRUNCATE" scripts/ops/corpus/ops_cost_hurdle_calibration.py` returns 0 (no direct corpus-table mutation; APR writes go through ConfigService, not raw SQL against corpus tables).
    - `grep -c "has_gap_before_entry" scripts/ops/corpus/ops_cost_hurdle_calibration.py` >= 1 (Step 3 present).
    - `grep -c "write-todo\|write_todo" scripts/ops/corpus/ops_cost_hurdle_calibration.py` >= 2 (flag defined + gates the todo-file write); the todo-file write is guarded by `args.write_todo` (default report-only — verify the write branch is inside an `if args.write_todo:` guard).
    - `grep -c "initialize()" scripts/ops/corpus/ops_cost_hurdle_calibration.py` >= 1 (ConfigService pool is initialized before any get/set call).
    - `.venv/bin/ruff check scripts/ops/corpus/ops_cost_hurdle_calibration.py` clean; `.venv/bin/pytest tests/unit/ -q` full suite green.
    - `--dry-run` mode makes zero APR writes (verify: config_history row count for alpha.quant.* unchanged after a --dry-run invocation, or assert the write branch is guarded by `not args.dry_run`).
  </acceptance_criteria>

  <verify>
    <automated>.venv/bin/pytest tests/unit/test_cost_hurdle_calibration.py -x -q && grep -q "ConfigService" scripts/ops/corpus/ops_cost_hurdle_calibration.py && grep -q "initialize()" scripts/ops/corpus/ops_cost_hurdle_calibration.py && grep -q "RuntimeError" scripts/ops/corpus/ops_cost_hurdle_calibration.py && test "$(grep -cE 'write-todo|write_todo' scripts/ops/corpus/ops_cost_hurdle_calibration.py)" -ge 2 && test "$(grep -cE 'INSERT INTO|UPDATE feature_|UPDATE alpha_|TRUNCATE' scripts/ops/corpus/ops_cost_hurdle_calibration.py)" = "0" && .venv/bin/ruff check scripts/ops/corpus/ops_cost_hurdle_calibration.py && .venv/bin/pytest tests/unit/ -q</automated>
  </verify>

  <done>A re-runnable, corpus-gated calibration script implements todo 030 Steps 0-3, writes empirical cost-hurdle/threshold APR values through the audited ConfigService path, reports the gap-contamination finding (writing a follow-up todo file only under --write-todo — default report-only), crashes loud on an empty corpus, and mutates no corpus table; pure helpers unit-tested; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| alpha_events/forward_returns (read) -> calibration | An incomplete/in-flight corpus would produce misleading percentiles |
| calibration -> config_state (APR write) | Wrong hurdle values change what emits as a tradeable alpha_event |
| calibration -> .planning/todos/pending (repo write) | A runtime script mutating the repo mixes execution with planning; must be opt-in |
| CLI args -> calibration | --cost-percentile / --report-path / --write-todo cross operator input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-1411-03-01 | Tampering | Calibrating against an empty or partial corpus, writing meaningless (e.g. 0.0) hurdles that look calibrated | mitigate | Startup RuntimeError on empty alpha_events/forward_returns; the report timestamps the corpus row counts so a partial run is visible; --dry-run lets an operator inspect before committing writes. |
| T-1411-03-02 | Repudiation | An APR change with no audit trail (who/why) | mitigate | All writes go through ConfigService.set with changed_by='cost_hurdle_calibration' + reason='rca_analysis' -> config_history row per change. No raw UPDATE against config_state. |
| T-1411-03-03 | Elevation of Privilege | Silently applying an ic_engine gap-filter code change (scope creep, file conflict with Plan 02), OR a runtime script mutating the repo (writing a todo file) without operator intent | mitigate | Step 3 NEVER edits ic_engine.py; it only REPORTS + prints the proposed todo text by default, and writes a follow-up todo file ONLY when --write-todo is explicitly passed (report-only default keeps execution-time analysis separate from repo mutation). |
| T-1411-03-04 | Tampering | Over-tightening thresholds and starving emissions | mitigate | Thresholds are only lowered (never raised) and only on an 'over_filtering' verdict (current > P50); 'not_binding' leaves them untouched. |
| T-1411-03-05 | Information Disclosure | Report leaks nothing sensitive | accept | Aggregate percentiles; local DB + repo file. |
| T-1411-03-SC | Tampering | pip installs | accept | Zero new packages (asyncpg/numpy/statsmodels pre-existing). |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_cost_hurdle_calibration.py -x -q` green; verdict helpers cover the boundary cases.
- The script crashes loud on empty corpus and makes zero writes in --dry-run.
- APR writes route through ConfigService.set with reason (config_history audited); no raw corpus-table mutation.
- Step 3 reports the gap finding and prints the proposed follow-up todo; it writes a todo file only under --write-todo — no ic_engine edit here, no repo mutation by default.
- `.venv/bin/pytest tests/unit/ -q` full suite green.
- Final empirical values land when the script is run against the completed Phase B corpus (per phase note — execution against the live populated corpus is an operator step after Phase B finishes; this plan delivers the runnable, tested calibrator).
</verification>

<success_criteria>
- The cost-hurdle no-op is closable: a committed script sets alpha.quant.cost_hurdle.5m/.15m to empirical percentiles when the corpus is populated.
- Emission thresholds are validated and adjusted only on a meaningful over-filtering gap.
- Gap contamination is measured and reported; a follow-up todo file is written only when --write-todo is passed (default report-only).
- Every APR change is audited via config_history; the script is idempotent, corpus-gated, and mutates no corpus table.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/141.1-measurement-and-decision-integrity-foundation-make-everythin/141.1-03-SUMMARY.md` when done.
</output>
