---
phase: 166-frame-execution-recalibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/253_alpha_frame_stop_target_calibration.sql
  - scripts/analysis/diagnose166_frame_calibration.py
  - tests/unit/test_diagnose166_frame_calibration.py
autonomous: true
requirements: [D-01a, D-02, D-06]
nyquist_compliant: true

must_haves:
  truths:
    - "Phase 163 VP/SR columns are verified live (sr_support_dist NOT NULL count > 0) before the structural-candidate runtime is attempted"
    - "A Phase-163-not-live status (sr_support_dist all NULL) recorded at Task 0 is a VALID, non-failing phase outcome that gates only the structural arm — the phase still completes with the baseline + scalar arms scored and the structural arm marked 'not evaluable — Phase 163 prerequisite unmet'; it is NOT a re-planning trigger (RESEARCH Pitfall 2 / Open Question 1: Phase 163 stays an external cross-phase prerequisite checked at runtime, not force-executed inside Phase 166)"
    - "The diagnosis empirically confirms D-02's baseline facts: hold_max_bars IS already calibrated (EIC-02, CR-02 champion-gated) while stop_atr_mult/target_r_multiple are NOT — single global [initial_estimate] scalars from migration 205/214, never conditioned on regime/tf, never revisited since Phase 142B"
    - "A read-only diagnosis compares current global stop_atr_mult(1.5)/target_r_multiple(2.0) scalars against the empirical uncensored MAE/MFE excursion percentiles per (regime,tf) (stop/target have no literal IC-decay-curve analog, per RESEARCH Finding 1/Pitfall 1), and cites — without recomputing — hold_max_bars' already-verified post-096-fix decay-curve calibration as the baseline-confirmation reference"
    - "The diagnosis discloses the folded caveats: hold_max_bars censoring (088), post-096-fix baseline confirmation, path-dependence (172), and 5m/15m-only OOS coverage (173)"
    - "Migration 253 seeds fresh alpha.frame.* APR keys (per-cell scalar keys, structural-confluence thresholds, geometry_source selector) without reusing archived feature.trade_framer.* / feature.zone_engine.* families"
  artifacts:
    - path: "production/migrations/253_alpha_frame_stop_target_calibration.sql"
      provides: "APR keys for both candidates"
      contains: "alpha.frame.stop_atr_mult"
    - path: "scripts/analysis/diagnose166_frame_calibration.py"
      provides: "Read-only frame-calibration diagnosis (D-01a)"
    - path: "tests/unit/test_diagnose166_frame_calibration.py"
      provides: "Unit coverage for the diagnosis pure functions"
  key_links:
    - from: "scripts/analysis/diagnose166_frame_calibration.py"
      to: "alpha_frames.counterfactual_mfe / counterfactual_mae"
      via: "in-sample SELECT (bar_ts < oos_start)"
      pattern: "counterfactual_m(f|a)e"
---

<objective>
Lay the foundation for Phase 166: seed the fresh `alpha.frame.*` APR namespace both calibration
candidates need (migration 253), deliver the read-only diagnosis of why the current global
stop/target scalars fail (D-01a), and gate the Phase 163 prerequisite that the structural
candidate depends on (D-06).

Purpose: Every downstream plan (scalar calibration, structural module, writer wiring, gate)
reads keys this migration seeds. The diagnosis is the D-01(a) deliverable — the empirical case
that `stop_atr_mult=1.5` / `target_r_multiple=2.0` were never conditioned on regime/tf and
should be recalibrated. Confirming Phase 163 data is live early prevents the "silent all-ATR-
fallback structural candidate" trap (RESEARCH Pitfall 2).

Output: migration 253, `diagnose166_frame_calibration.py`, its unit test, and a verified answer
to whether Phase 163 has executed.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.1-phases/166-frame-execution-recalibration/166-CONTEXT.md
@.planning/milestones/v3.1-phases/166-frame-execution-recalibration/166-RESEARCH.md
@.planning/milestones/v3.1-phases/166-frame-execution-recalibration/166-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Verify Phase 163 prerequisite (D-06 Wave 0 gate)</name>
  <files>(verification only — no file changes)</files>
  <read_first>
    - .planning/milestones/v3.1-phases/166-frame-execution-recalibration/166-CONTEXT.md (D-06: Phase 163 is a real prerequisite for the structural candidate)
    - .planning/milestones/v3.1-phases/163-vp-sr-structural-primitives/163-01-PLAN.md (what Phase 163 populates)
  </read_first>
  <action>
    Run `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT count(*) FROM feature_vectors WHERE sr_support_dist IS NOT NULL;"`. Phase 163's columns exist in schema but are 100% NULL until it executes (RESEARCH Pitfall 2). If the count is 0, Phase 163 has NOT executed: record this as a HARD PREREQUISITE in the plan SUMMARY and instruct that `/gsd-execute-phase 163` MUST complete before Plan 166-06's structural arm runs. Frame this recorded status explicitly as a VALID, non-blocking phase outcome — it gates ONLY Plan 166-06's structural arm; the baseline + scalar arms, the diagnosis, and the verdict doc still complete regardless (RESEARCH Open Question 1: Phase 163 is a runtime-checked external prerequisite, deliberately NOT force-executed inside Phase 166). Do NOT block the scalar-candidate, diagnosis, structural-module-build, or gate-script code (none need live 163 data — unit tests use synthetic dicts). Only Plan 166-06's structural frame-generation runtime is gated. Also verify the sibling columns exist: `poc_dist_atr`, `poc_rolling_dist_atr`, `distance_to_vah_atr`, `distance_to_val_atr`, `resistance_strength`, `support_strength`, `resistance_age_bars`, `support_age_bars`, `sr_level_count` (via `\d feature_vectors`).
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT CASE WHEN count(*)>0 THEN 'LIVE' ELSE 'NULL_PENDING_163' END FROM feature_vectors WHERE sr_support_dist IS NOT NULL;"</automated>
  </verify>
  <acceptance_criteria>
    - The psql query runs and returns either `LIVE` or `NULL_PENDING_163`
    - The SUMMARY records the observed status and the explicit statement that Plan 166-06's structural arm is gated on Phase 163 being LIVE
    - A `NULL_PENDING_163` status is explicitly recorded as a VALID, non-blocking phase outcome (it gates only Plan 166-06's structural arm; the baseline + scalar arms and the verdict doc still complete, with structural marked "not evaluable — Phase 163 prerequisite unmet") — it is not a phase failure or a re-planning trigger
    - All 10 Phase-163 columns above are confirmed present in the `feature_vectors` schema (present, possibly NULL)
  </acceptance_criteria>
  <done>Phase 163 data-liveness status is recorded; the structural runtime dependency is documented for Plan 166-06.</done>
</task>

<task type="auto">
  <name>Task 1: Migration 253 — seed alpha.frame.* APR keys for both candidates</name>
  <files>production/migrations/253_alpha_frame_stop_target_calibration.sql</files>
  <read_first>
    - production/migrations/190_alpha_ensemble_ic.sql (the 36-key per-(regime,tf) DO-block loop at lines 167-210 — copy this loop shape exactly; regimes = high_bear/high_bull/high_neutral/low_bear/low_bull/low_neutral/mid_bear/mid_bull/mid_neutral, tfs = 5m/15m/1h/1d)
    - production/migrations/243_frame_min_stop_price_fraction.sql (single-key seed with provenance comment, config_schema+config_state+config_history, ON CONFLICT DO NOTHING, BEGIN/COMMIT)
    - production/migrations/205_alpha_frames_schema.sql (lines 74-75, 158: existing global alpha.frame.stop_atr_mult/target_r_multiple scalar seeds and the "does not re-seed per-cell keys" note)
    - src/config/config_service.py (lines 39-83: OPS_PREFIXES must include `alpha.` for every new key to pass _validate_key_domain)
    - .planning/milestones/v3.1-phases/166-frame-execution-recalibration/166-PATTERNS.md ("production/migrations/253" section, Finding 5 namespace discipline)
  </read_first>
  <action>
    Create migration 253 wrapped in BEGIN/COMMIT. Seed these fresh `alpha.frame.*` keys (all `[initial_estimate]`, ON CONFLICT DO NOTHING, each with a config_schema + config_state + config_history triple):

    (a) Per-(regime,tf) scalar-candidate keys via a DO-block FOREACH loop identical to migration 190's: `alpha.frame.stop_atr_mult.<regime>.<tf>` (float, seed '1.5', min 0.1 max 10.0) and `alpha.frame.target_r_multiple.<regime>.<tf>` (float, seed '2.0', min 0.1 max 20.0). 36 keys each. Description must state "overwritten by _calibrate_stop_target (EIC-scalar) after first champion run; NOT an ML learning target".

    (b) Scalar calibration params under `alpha.ensemble_ic.*` (read by ensemble_ic_engine): `alpha.ensemble_ic.stop_mae_percentile` (float '90.0', min 50 max 99 — percentile of winner MAE for stop placement), `alpha.ensemble_ic.target_mfe_percentile` (float '50.0', min 10 max 90 — percentile of time-exit MFE for target placement), `alpha.ensemble_ic.stop_target_min_qualifying_symbols` (int '3', min 1 max 80).

    (c) Structural-confluence thresholds under fresh `alpha.frame.*` (do NOT reuse feature.zone_engine.* / feature.trade_framer.* — Finding 5): `alpha.frame.cluster_radius_atr` (float '0.5'), `alpha.frame.single_level_radius_atr` (float '0.25'), `alpha.frame.zone_buffer_atr` (float '0.1'), `alpha.frame.min_width_atr` (float '0.05'), `alpha.frame.strength_weight` (float '0.6', min 0 max 1), `alpha.frame.proximity_weight` (float '0.4', min 0 max 1), `alpha.frame.structure_snap_proximity_atr` (float '1.5', min 0.1 max 5.0). Copy the numeric defaults from `zone_engine.py`'s current `feature.zone_engine.*` values where an analog exists; document provenance in each description.

    (d) Geometry-source selector: `alpha.frame.geometry_source` (string, default 'global', description enumerating the three legal values global|per_cell_scalar|structural and noting `global` preserves current AlphaFrameWriter behavior — backward-compatible). This is the switch Plan 166-06 flips to regenerate each candidate's frame population.

    Do NOT add any new gate-threshold APR keys — the gate reuses SHADOW-REVIEW.md's frozen five criteria and the existing `alpha.validation.regime_gate_min_clusters` (migration 244) unchanged (OQ3 resolution, avoids OOS-peeking via new-threshold tuning).
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && grep -c "alpha.frame.stop_atr_mult\|alpha.frame.target_r_multiple\|alpha.frame.geometry_source\|alpha.frame.cluster_radius_atr\|alpha.ensemble_ic.stop_mae_percentile" production/migrations/253_alpha_frame_stop_target_calibration.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^--' production/migrations/253_*.sql | grep -c "alpha.frame.geometry_source"` returns >= 1
    - The DO-block loop iterates exactly 9 regimes × 4 tfs for both stop_atr_mult and target_r_multiple
    - No occurrence of `feature.trade_framer.` or `feature.zone_engine.` or `weights.zone_engine.` as a WRITE target anywhere in the file
    - After applying the migration, `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM config_schema WHERE config_key LIKE 'alpha.frame.stop_atr_mult.%';"` returns 36
    - `alpha.frame.geometry_source` default value is exactly `global`
  </acceptance_criteria>
  <done>Migration 253 applied; all per-cell, calibration-param, structural-threshold, and geometry-source keys exist in config_schema/config_state; no archived namespace reused.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Diagnosis script — current calibration vs IC decay curve vs MAE/MFE (D-01a)</name>
  <files>scripts/analysis/diagnose166_frame_calibration.py, tests/unit/test_diagnose166_frame_calibration.py</files>
  <behavior>
    - Test 1: `_summarize_excursions(rows)` returns per-(regime,tf) MAE/MFE percentile summaries computed only from in-sample rows (bar_ts < oos_start), rescaling counterfactual_mae/mfe from R-units to ATR-units correctly given each row's snapshotted stop_atr_mult
    - Test 2: given a synthetic mix of closed_stop / closed_target / closed_max_hold frames, the summary flags closed_stop MAE as right-censored and excludes it from the stop-placement distribution (088 alignment), keeping closed_target frames for stop and closed_max_hold frames for target
    - Test 3: `_compare_to_current(summary, current_global={'stop_atr_mult':1.5,'target_r_multiple':2.0})` returns a per-cell delta showing where the empirical excursion percentile diverges from the current global scalar
  </behavior>
  <read_first>
    - scripts/analysis/score03_gate2_execution_eval.py (imports/argparse/dry-run/main structure to mirror; _aggregate_pnl_by_bar_ts for the 172 caveat)
    - services/counterfactual_tracker.py (lines 105-151: exit-status literals closed_stop/closed_target/closed_max_hold/closed_ic_decay; determine_exit contract; the MAE censoring semantics)
    - .planning/milestones/v3.1-phases/166-frame-execution-recalibration/166-RESEARCH.md (Finding 1 MAE/MFE rescaling; "State of the Art" live-verified post-096-fix confirmation to CITE; folded todos 088/096/172/173)
    - services/ensemble_ic_engine.py (lines 1063-1126: how hold_max_bars was calibrated — the baseline this diagnosis compares against)
  </read_first>
  <action>
    Create a READ-ONLY diagnosis script (no writes to any table). It computes, per (regime, tf), from in-sample `alpha_frames` rows (`bar_ts < alpha.validation.oos_start`):
    (1) the empirical MAE distribution (ATR-rescaled, `mae_atr = counterfactual_mae * snapshotted stop_atr_mult`) among `counterfactual_status='closed_target'` frames — the uncensored subpopulation for stop placement;
    (2) the empirical MFE distribution (R-units) among `counterfactual_status='closed_max_hold'` frames — the uncensored subpopulation for target placement;
    (3) the current live per-cell `alpha.frame.hold_max_bars.<regime>.<tf>` values and the global `stop_atr_mult=1.5`/`target_r_multiple=2.0` scalars;
    and prints a comparison table showing where the current scalars sit relative to the empirical percentiles.
    Disclose in the printed report and a module docstring: (a) the 088 censoring caveat and the uncensored-subpopulation method chosen to sidestep it; (b) the RESEARCH-verified fact that the live hold_max_bars keys ARE post-096-fix (decay_threshold=0.05, champion aliasing confirmed) — cite it, do not re-derive; (c) todo 172's same-bar_ts path-dependence (aggregate before any cumulative stat); (d) todo 173's 5m/15m-only OOS coverage. Make the excursion-summary and comparison logic pure functions (`_summarize_excursions`, `_compare_to_current`) so the unit test exercises them on synthetic rows with no DB. Filter any forward_returns join to `return_type='executable_open_to_open'` if used (Invariant 1). Use `except X as error:`, `datetime.now(UTC)`, aggregate-not-per-row logging.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && .venv/bin/pytest tests/unit/test_diagnose166_frame_calibration.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_diagnose166_frame_calibration.py -x` passes with >= 3 tests
    - The script writes to zero tables (grep confirms no INSERT/UPDATE/DELETE/config_service.set)
    - `_summarize_excursions` excludes `closed_stop` frames from the stop-placement MAE distribution and this is asserted in a test
    - The module docstring names all four folded caveats (088, 096, 172, 173) explicitly
    - Running `.venv/bin/python scripts/analysis/diagnose166_frame_calibration.py --help` exits 0
  </acceptance_criteria>
  <done>The diagnosis runs read-only, compares current scalars to empirical uncensored excursion percentiles per (regime,tf), and discloses every folded caveat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → config_state | New APR key values cross into the tunable-config store |
| diagnosis script → live DB | Read-only query surface, no write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-01 | Tampering | migration 253 config keys | mitigate | config_schema min_value/max_value bounds on every new key; ConfigService._validate_key_domain requires `alpha.` OPS prefix |
| T-166-02 | Tampering | diagnosis script (accidental write) | mitigate | script is read-only by contract; acceptance criterion greps for zero INSERT/UPDATE/DELETE |
| T-166-SC | Tampering | package installs | accept | Phase installs zero external packages (RESEARCH: no pip/npm step) |
</threat_model>

<verification>
- Migration 253 applies cleanly; 36+36 per-cell keys + all threshold/selector keys present in config_schema.
- No archived `feature.trade_framer.*`/`feature.zone_engine.*` namespace written.
- Diagnosis unit tests pass; script is read-only; caveats disclosed.
- Phase 163 liveness status recorded for Plan 166-06 gating.
</verification>

<success_criteria>
- `alpha.frame.geometry_source` exists (default `global`), and 72 per-cell scalar keys exist.
- The diagnosis (D-01a) produces a per-(regime,tf) comparison of current scalars vs empirical uncensored MAE/MFE percentiles with all folded caveats disclosed.
- Phase 163 prerequisite status is known and documented, framed as a non-blocking outcome for the phase overall.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/166-frame-execution-recalibration/166-01-SUMMARY.md` when done.
</output>
