---
phase: 142.5-renaissance-primitives
plan: "05.5"
subsystem: feature-engineering
tags: [feature-factory, feature-vector, apr, numpy, tdd, price-volume, correlation]

# Dependency graph
requires:
  - phase: 142.5-00 (Renaissance Primitives Test Infrastructure, Wave 0)
    provides: test_price_volume_interactions / test_compute_batch_parity RED
      tests (already existed, GREEN now); RENAISSANCE_PRIMITIVE_FIELDS
      canonical list (already included this plan's 8 field names at count 91)
  - phase: 142.5-01 (Bar Anatomy, Lagged Returns, Open-to-Close Split)
    provides: body_ratio, ret_lag_fast, range_vs_atr parent scalars
  - phase: 142.5-02 (Temporal Coordinates + Volume Structure)
    provides: up_vol_ratio_fast parent scalar (precomputed series)
  - phase: 142.5-04 (Alternative Volatility + Volatility Dynamics, merged
      before this plan despite lower plan number)
    provides: FeatureVector at 144 fields going into this plan (matches the
      phase outline's own eventual target; confirmed via direct field-count
      check rather than trusting the plan's stale narrative text)
provides:
  - 8 price-volume interaction primitives: vol_body_product,
    ret_vol_product_fast, price_vol_corr_fast, price_vol_corr_slow,
    range_vol_product, up_vol_body_diff, ret_vol_ratio_fast,
    vol_skew_product
  - FeatureVector extended to 152 fields (61 baseline + 18 Plan01 + 22
    Plan02 + 14 Plan05 + 21 Plan03 + 8 Plan04 + 8 Plan05.5 = final total)
  - FeatureFactoryConfig extended with 2 feature.price_vol_corr.fast/slow
    APR fields (the only 2 of 8 interactions needing a new window)
  - 7 new pure-value helper functions (_vol_body_product, _ret_vol_product,
    _range_vol_product, _up_vol_body_diff, _ret_vol_ratio,
    _vol_skew_product, _price_vol_corr) + 1 new _price_vol_corr_series_full
    vectorized batch precompute function
  - FEATURE_VECTOR_DOMAIN, _cold_start_vector, and _REGISTRY_ROW_COUNT
    synced to 152 (final)
  - Production config-prewarm wiring (feature_vector_pipeline.py,
    backfill_feature_factory.py) for the 2 new APR keys
affects: [142.5-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Window-free scalar combinator: 6 of the 8 interactions are pure O(1)
       products/ratios/differences of already-computed parent scalars, with
       zero new APR window -- the phase's first primitive category where
       most fields are NOT independently parameterized rolling windows.
       Only the 2 rolling-correlation interactions get new APR keys."
    - "Baseline-scalar local capture in compute(): volume_z, atr_z, and
       ret_skew_z were previously inlined directly into the
       _build_feature_vector() call (`volume_z=_series_last(s.volume_z,
       0.0)`) rather than captured as named locals, unlike compute_batch()
       where they already existed as locals. This plan introduced
       volume_z_val/atr_z_val/ret_skew_z_val/up_vol_ratio_fast_val locals in
       compute() (mirroring compute_batch()'s existing pattern) so the
       interaction helpers can reuse them without recomputation or
       duplicated series lookups -- first plan to require this kind of
       backward-compatible refactor of compute()'s existing baseline code
       rather than only adding new lines."
    - "_price_vol_corr_series_full follows the exact
       _high_low_corr_series_full O(n x window) nested-loop pattern
       (bounded per-bar work via array slicing, not O(n^2)), but precomputes
       abs(diff(log(closes))) once over the full array before the loop
       (rather than recomputing per bar) since the streaming function's own
       eps-guard semantics required matching that exact alignment for
       byte-exact parity."

key-files:
  created: []
  modified:
    - src/intelligence/feature_factory.py
    - src/intelligence/schemas.py
    - src/intelligence/feature_registry_service.py
    - services/feature_vector_pipeline.py
    - services/backfill_feature_factory.py
    - tests/unit/test_feature_factory.py
    - tests/unit/intelligence/test_feature_factory_batch_parity.py
    - tests/unit/intelligence/test_feature_factory_p7.py
    - tests/unit/pipeline/pipeline_helpers.py
    - tests/unit/services/test_backfill_feature_factory.py
    - tests/unit/services/test_feature_vector_writer.py
    - tests/unit/services/test_feature_vector_writer_column_mapping.py

key-decisions:
  - "Worktree branch was already caught up through Plan 04 (base commit
     fb69baa5, 'chore: merge executor worktree' for 142.5-04) at spawn time
     -- confirmed the actual FeatureVector field count directly
     (dataclasses.fields(FeatureVector) == 144) rather than trusting the
     plan text's own objective narrative (which implied the migration
     already references these 8 columns -- it does not; migration 206 only
     has Plan 05's 14 breakout-distance columns so far, confirmed by
     grepping the file directly). This plan's base of 144 matched the
     plan's own frontmatter target exactly (144 -> 152), unlike Plans
     03/04/05 which each had to reconcile a stale assumed base against the
     actual dependency-DAG-valid merge order."
  - "Did not add a second, differently-named 'test_price_volume_interactions'
     function -- Wave 0 (142.5-00) already committed this exact test name
     with 8 assertions exercised through FeatureFactory.compute()
     (integration-style), matching the established one-test-per-primitive-
     group pattern used by every prior Renaissance-primitive test group in
     this file (test_bar_anatomy_primitives, test_alt_volatility,
     test_volatility_dynamics, test_breakout_distance, etc. -- none of
     which have a parallel direct-helper-unit test). Rather than duplicate
     that test under a new name, this plan added a genuinely new
     supplementary test (test_price_volume_interaction_helpers_direct) that
     imports the 7 helpers directly and asserts exact products/ratios plus
     the 3 epsilon-guard edge cases (ret_vol_ratio at atr_z=0, price_vol_corr
     on insufficient history, price_vol_corr on degenerate constant-volume/
     constant-price input) that the integration-style test cannot easily
     exercise via compute() alone -- covering the plan's literal Task 1
     behavior spec without introducing a duplicate test name (which the
     project's own pre-commit hook flags as an error)."
  - "6 of the 8 interactions carry zero APR window by design (per the plan's
     explicit spec: 'Do NOT invent APR params for the six window-free
     interactions') -- FeatureFactoryConfig gained exactly 2 new fields
     (price_vol_corr_fast=10, price_vol_corr_slow=30 as conventional
     fast/slow defaults, matching the magnitude of existing fast/slow window
     pairs like dollar_vol_window=20 / high_low_corr_window=20), not 8. This
     is the first Renaissance-primitive plan in the phase where the
     field-count-to-APR-key ratio is not 1:1 or close to it."
  - "_price_vol_corr's scalar and vectorized (_price_vol_corr_series_full)
     implementations both precompute abs(diff(log(closes))) once rather than
     per-window-slice, then index/slice bounded windows from that single
     precomputed array -- this exactly mirrors how _high_low_corr_series_full
     precomputes nothing extra (H/L are already raw arrays) but keeps the
     same O(window)-per-bar cost discipline; verified index alignment
     (abs_rets[k] pairs with volumes[k+1]) analytically before implementing,
     then confirmed byte-exact via a 199-bar compute()/compute_batch()
     parity sweep at 7 spot-check indices plus a full-sweep non-finite check
     (0 non-finite values, 0 RuntimeWarnings under `python -W error`)."

requirements-completed: [REQ-142.5-09]

# Metrics
duration: 25min
completed: 2026-07-07
---

# Phase 142.5 Plan 05.5: Price-Volume Interaction Primitives Summary

**8 deterministic price-volume interaction primitives (6 O(1) scalar
products/ratios/differences of already-computed parent features + 2 rolling
Pearson correlations between |log return| and volume) wired into
compute()/compute_batch(), closing the phase's only gap that had DB/APR
scaffolding but zero implementation -- extending FeatureVector to its final
152 fields with full cold-start safety and verified compute()/compute_batch()
parity.**

## Performance

- **Duration:** 25 min
- **Started:** 2026-07-07T19:07:00Z (approx.)
- **Completed:** 2026-07-07T19:32:00Z
- **Tasks:** 3/3 completed
- **Files modified:** 12 (5 core intelligence/service files, 6 test fixture
  files, 1 planning summary)

## Accomplishments

- Confirmed the actual FeatureVector base field count directly
  (`dataclasses.fields(FeatureVector) == 144`, matching Plan 04's own
  documented final count) before writing any code, per the pattern
  established by Plans 03/04/05 for reconciling assumed-vs-actual merge
  order.
- Implemented 6 window-free O(1) scalar combinator helper functions
  (`_vol_body_product`, `_ret_vol_product`, `_range_vol_product`,
  `_up_vol_body_diff`, `_ret_vol_ratio` with an epsilon guard on
  `abs(atr_z) < eps`, `_vol_skew_product`) -- pure products/ratio/difference
  of two already-computed parent scalars, zero new APR window per the
  plan's explicit spec.
- Implemented `_price_vol_corr` (rolling Pearson correlation between
  `|diff(log(closes))|` and volume over an APR-backed window), with guards
  for insufficient history (`len(closes) < window + 1`) and degenerate
  input (near-zero variance in either sliced series), clamping any
  `np.corrcoef` NaN to 0.0. Bounded [-1, 1].
- Implemented `_price_vol_corr_series_full`, the vectorized O(n x window)
  batch-precompute counterpart (same cost class as
  `_high_low_corr_series_full`), wired into `_PrecomputedSeries` /
  `_precompute_series` so `compute()` and `compute_batch()` share one
  source of truth for the 2 rolling-correlation fields.
- Extended `FeatureFactoryConfig` with exactly 2 new APR fields
  (`price_vol_corr_fast`, `price_vol_corr_slow`) -- confirmed no phantom
  APR keys were added for the other 6 window-free interactions.
- Wired all 8 primitives into both `FeatureFactory.compute()` and
  `compute_batch()`. In `compute()`, captured `volume_z`, `atr_z`,
  `ret_skew_z`, and `up_vol_ratio_fast` into named locals (previously
  inlined directly into the `_build_feature_vector()` call) so the 6
  scalar combinators can reuse them without recomputation -- mirroring the
  pattern `compute_batch()` already used for these same baseline fields.
- Extended `FeatureVector` to 152 fields (144 -> 152), inserted after the
  Plan 04 volatility-dynamics block (dataclass field-ordering requirement,
  same pattern as Plans 01-05).
- Updated `FEATURE_VECTOR_DOMAIN` (152 entries, all 8 new fields ==
  "quant"), `_cold_start_vector()` (8 neutral 0.0 fallbacks, verified no
  `TypeError` on 0-bar history), and
  `feature_registry_service._REGISTRY_ROW_COUNT` (144 -> 152, final total
  for the phase).
- Verified byte-exact `compute()`/`compute_batch()` parity at bar indices
  10/30/60/90/120/149/198 across a 199-bar synthetic series, plus a full
  sweep under `python -W error` confirming zero non-finite values and zero
  `RuntimeWarning`s for all 8 fields.
- `test_price_volume_interactions` and `test_compute_batch_parity` (both
  already existed from Wave 0's RED test infrastructure, including the 8
  field names already present in `RENAISSANCE_PRIMITIVE_FIELDS` at count
  91) are now GREEN.
- Added a new supplementary test, `test_price_volume_interaction_helpers_direct`,
  importing the 7 helpers directly and asserting exact products/ratios plus
  the 3 epsilon-guard/degenerate-input edge cases (see key-decisions) that
  the pre-existing integration-style test does not exercise.

## Task Commits

Each task was committed atomically (with additional collateral-fix commits
per the established multi-plan pattern):

1. **Task 3 (schema part, landed first — wiring requires the fields to
   exist): Extend FeatureVector dataclass with 8 new fields (152 total)** -
   `245cd314` (test)
2. **Tasks 1 + 2: Implement 7 price-volume-interaction helper functions,
   extend FeatureFactoryConfig with 2 APR fields, wire into
   compute()/compute_batch(), update FEATURE_VECTOR_DOMAIN + _cold_start_vector
   + `_make_config()` test fixture + direct helper-level tests** - `c4e7aa74`
   (feat)
3. **Task 3 (registry part): bump `_REGISTRY_ROW_COUNT` to 152** -
   `db11ed47` (feat)

**Collateral fix commits (not separate plan tasks, see Deviations):**
- `4fab6986` (fix): update 6 collateral test fixtures broken by the schema
  extension
- `25c8c6d4` (fix): wire 2 new APR keys into the 2 production
  config-builders

## Files Created/Modified

- `src/intelligence/feature_factory.py` — 7 new pure-value helper functions
  (`_vol_body_product`, `_ret_vol_product`, `_range_vol_product`,
  `_up_vol_body_diff`, `_ret_vol_ratio`, `_vol_skew_product`,
  `_price_vol_corr`), 1 `_price_vol_corr_series_full` batch precompute
  function, `_PrecomputedSeries` +2 array fields, `_precompute_series`
  wiring, `_build_feature_vector()` +8 params, `compute()`/`compute_batch()`
  wiring (compute() gained 4 new named locals for previously-inlined
  baseline scalars), `FEATURE_VECTOR_DOMAIN` +8 entries,
  `_cold_start_vector()` +8 fallbacks, `FeatureFactoryConfig` +2 APR fields
- `src/intelligence/schemas.py` — `FeatureVector` +8 fields (152 total),
  docstring updated
- `src/intelligence/feature_registry_service.py` — `_REGISTRY_ROW_COUNT` 144
  -> 152, with a note documenting the final total
- `services/feature_vector_pipeline.py` — `_THRESHOLD_KEYS` +
  `_prewarm_threshold_config()` source the 2 new APR keys (production
  config-builder, Rule 2 fix)
- `services/backfill_feature_factory.py` — `_build_feature_factory_config()`
  sources the 2 new APR keys (production config-builder, Rule 2 fix)
- `tests/unit/test_feature_factory.py` — `_make_config()` fixture +2
  fields; `test_all_fields_are_finite_floats` count 144 -> 152 (collateral
  fix); added `test_price_volume_interaction_helpers_direct`; added
  `import pytest`
- `tests/unit/intelligence/test_feature_factory_batch_parity.py` —
  `_make_cfg()` fixture +2 fields; both `_build_feature_vector(...)` direct
  calls (`test_build_feature_vector_guards_nan`,
  `test_build_feature_vector_none_passthrough`) +8 kwargs (collateral fix)
- `tests/unit/intelligence/test_feature_factory_p7.py` — local `_make_cfg()`
  fixture +2 fields; `test_feature_vector_domain_complete` count 144 -> 152
  (collateral fix)
- `tests/unit/pipeline/pipeline_helpers.py` — `agent._feature_factory_config`
  fixture +2 fields (collateral fix)
- `tests/unit/services/test_backfill_feature_factory.py` — config fixture
  +2 fields; `_make_zero_vector()` +8 fields (collateral fix)
- `tests/unit/services/test_feature_vector_writer.py` —
  `_make_valid_feature_vector()` +8 fields (collateral fix)
- `tests/unit/services/test_feature_vector_writer_column_mapping.py` —
  sentinel `FeatureVector(...)` construction +8 distinguishable values
  (60.01-60.08, collateral fix)

## Decisions Made

See `key-decisions` in frontmatter for the full list. Summary of the most
significant:
- This plan's base count (144) matched its own frontmatter target exactly
  (144 -> 152) — the worktree's actual fork point (post-Plan-04) lined up
  with the plan's assumed dependency set, unlike Plans 03/04/05 which each
  had to reconcile a stale assumed base.
- Reused Wave 0's pre-existing `test_price_volume_interactions` integration
  test rather than duplicating it under a new name; added a genuinely new
  supplementary direct-helper test instead.
- Exactly 2 new APR fields added (`price_vol_corr_fast`/`slow`), not 8 — the
  other 6 interactions are pure window-free scalar combinators per the
  plan's explicit spec.
- `compute()` gained 4 new named locals (`volume_z_val`, `atr_z_val`,
  `ret_skew_z_val`, `up_vol_ratio_fast_val`) for baseline scalars that were
  previously inlined directly into the `_build_feature_vector()` call —
  the first plan in this phase requiring this kind of backward-compatible
  refactor of `compute()`'s pre-existing code (not just additive new lines).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Collateral test-fixture breakage from extending
FeatureFactoryConfig/FeatureVector**
- **Found during:** Post-wiring full-suite verification sweep.
- **Issue:** Same mechanical consequence Plans 01-04 documented for their
  own extensions: 6 test files construct `FeatureFactoryConfig(...)` or
  `FeatureVector(...)` directly with explicit kwargs (not via the shared
  `_make_config()`/`_make_cfg()` fixtures), and 2 tests hardcode a
  `144`-field/entry count assertion. All of these broke with
  `TypeError`/`AssertionError` once the dataclasses gained the 10 new
  required fields (8 FeatureVector + 2 config).
- **Fix:** Added the new fields with neutral/sentinel values to each
  construction site; updated the two hardcoded counts from 144 to 152.
- **Files modified:** `tests/unit/test_feature_factory.py`,
  `tests/unit/services/test_feature_vector_writer.py`,
  `tests/unit/services/test_feature_vector_writer_column_mapping.py`,
  `tests/unit/services/test_backfill_feature_factory.py`,
  `tests/unit/intelligence/test_feature_factory_p7.py`,
  `tests/unit/intelligence/test_feature_factory_batch_parity.py`,
  `tests/unit/pipeline/pipeline_helpers.py`
- **Verification:** `pytest tests/unit/test_feature_factory.py
  tests/unit/intelligence/ tests/unit/pipeline/
  tests/unit/services/test_feature_vector_writer.py
  tests/unit/services/test_feature_vector_writer_column_mapping.py
  tests/unit/services/test_backfill_feature_factory.py` — 3649 passed, only
  2 pre-existing unrelated failures remain (see Issues Encountered).
- **Committed in:** `4fab6986`

**2. [Rule 2 - Missing Critical] Production config-builders would crash the
live pipeline on startup**
- **Found during:** After Task 2's config extension, verified via a
  repo-wide grep for other `FeatureFactoryConfig(` construction sites.
- **Issue:** `services/feature_vector_pipeline.py::_prewarm_threshold_config()`
  and `services/backfill_feature_factory.py::_build_feature_factory_config()`
  both construct `FeatureFactoryConfig(...)` with the pre-extension field
  set. Since `FeatureFactoryConfig` is a non-optional-argument frozen
  dataclass, both would raise `TypeError: missing 2 required positional
  arguments` at daemon startup / backfill invocation the moment this plan's
  schema change landed.
- **Fix:** Added the 2 new `feature.price_vol_corr.fast/slow` APR keys to
  `_THRESHOLD_KEYS` and the constructor call in both files (defaults
  10/30, matching the test fixture defaults).
- **Files modified:** `services/feature_vector_pipeline.py`,
  `services/backfill_feature_factory.py`
- **Verification:** `pytest tests/unit/pipeline/ tests/unit/services/
  test_backfill_feature_factory.py
  tests/unit/services/test_feature_vector_pipeline_publisher_normalization.py
  tests/unit/services/test_pipeline_backpressure.py
  tests/unit/services/test_service_auditor.py` — all pass.
- **Committed in:** `25c8c6d4`

---

**Total deviations:** 2 auto-fixed (1 Rule 1 bug fix, 1 Rule 2
missing-critical-functionality). Both were prerequisites for the plan's own
schema change to land safely; no architectural changes, no scope creep.

## Issues Encountered

- `tests/unit/test_feature_factory.py::TestRegimePrimitives::test_no_smooth_or_backward_in_factory`
  fails on a pre-existing false-positive text match introduced by Plan 04
  (its `parkinson_vol_z_series_full`/`garman_klass_vol_z_series_full`
  docstrings and local variable named `smoothed` trip a grep-based test
  that scans the whole file for `_smooth|smoothed|backward` looking for
  backward-looking HMM smoothers). Confirmed via `git stash` that this
  failure exists identically on the pre-plan base commit — unrelated to
  any file touched by this plan. Out of scope; not fixed (Rule 1 scope
  boundary: only auto-fix issues directly caused by the current task's
  changes).
- `tests/unit/intelligence/test_binary_pattern_scanner.py::test_zero_binary_violations`
  fails on a pre-existing violation in `src/intelligence/ensemble/weights.py:192`,
  unrelated to any file touched by this plan (same failure already noted by
  every prior 142.5 plan's summary). Out of scope; not fixed.
- Did not apply or modify
  `production/migrations/206_add_renaissance_primitives.sql` — this plan's
  task list has no migration step (same scope as Plans 01/02/03/04); the
  migration currently only has Plan 05's 14 breakout-distance columns
  (confirmed by grep — the plan's own objective narrative overstates the
  migration's current state; Plan 06 owns appending the remaining
  columns/seeds/registry rows per the migration file's own header
  comment). The 8 new fields remain write-only within the in-memory
  `FeatureVector`.
- `pytest tests/unit/` full run not attempted (every prior 142.5 plan noted
  it times out on a long-running/network-dependent test unrelated to this
  plan). Verified instead via the plan's own explicit verification commands
  plus a targeted sweep of `tests/unit/test_feature_factory.py` +
  `tests/unit/intelligence/` + `tests/unit/pipeline/` +
  `tests/unit/services/test_backfill_feature_factory.py` +
  `tests/unit/services/test_feature_vector_writer*.py` +
  `tests/unit/services/test_feature_vector_pipeline_publisher_normalization.py`
  + `tests/unit/services/test_pipeline_backpressure.py` +
  `tests/unit/services/test_service_auditor.py` (3649 tests total: all
  pass except the 2 pre-existing failures documented above).
- Manually verified `compute()`/`compute_batch()` parity at bar indices
  10/30/60/90/120/149/198 plus a full 199-bar `-W error` sweep confirming
  zero non-finite values and zero `RuntimeWarning`s for all 8 fields (see
  Accomplishments).

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- `test_price_volume_interactions` and `test_compute_batch_parity` are now
  GREEN. `FeatureVector` is at its final 152 fields — this was the last
  plan in the phase's build sequence to add new fields.
- `FEATURE_VECTOR_DOMAIN` (152 entries) and `_REGISTRY_ROW_COUNT` (152) are
  synced to the final total.
- The 8 new fields are NOT yet persisted to `feature_vectors` (no DB column
  applied — migration 206 currently only has Plan 05's 14 columns; this
  plan does not touch it, per its own task scope). Plan 06 owns applying
  the completed migration (appending the remaining columns/APR
  seeds/registry rows to migration 206, per the file's own header comment)
  and reconciling `_REGISTRY_ROW_COUNT`/`FEATURE_VECTOR_DOMAIN` if any
  drift occurred.
- No blockers for Plan 06.

---
*Phase: 142.5-renaissance-primitives*
*Completed: 2026-07-07*

## Self-Check: PASSED

All claimed files exist (`src/intelligence/feature_factory.py`,
`src/intelligence/schemas.py`, `src/intelligence/feature_registry_service.py`,
`services/feature_vector_pipeline.py`, `services/backfill_feature_factory.py`,
this SUMMARY.md) and all claimed commit hashes (`245cd314`, `c4e7aa74`,
`db11ed47`, `4fab6986`, `25c8c6d4`) resolve in `git log --oneline --all`. No
missing items.
