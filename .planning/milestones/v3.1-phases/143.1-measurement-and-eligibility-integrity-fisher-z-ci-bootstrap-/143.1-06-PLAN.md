---
phase: 143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap
plan: 06
type: execute
wave: 5
depends_on: ["143.1-02", "143.1-05"]
files_modified:
  - src/intelligence/statistics/ic_math.py
  - services/ic_engine.py
  - tests/unit/test_e_values.py
  - production/migrations/2XX_e_value_pilot_column.sql
autonomous: true
requirements: [C]
must_haves:
  truths:
    - "A per-cell e-process is persisted and updated each corpus rerun for tf=5m only (pilot scope, not all timeframes)"
    - "Cumulative e-value promotion/demotion thresholds are anytime-valid (evidence compounds across reruns instead of resetting)"
    - "The e-process self-verifies against Component D canaries: canary e-values decay toward zero"
  artifacts:
    - path: "src/intelligence/statistics/ic_math.py"
      provides: "e-value kernel (likelihood-ratio or universal-inference on IC sign)"
      contains: "def "
    - path: "production/migrations/2XX_e_value_pilot_column.sql"
      provides: "feature_ic_scores e-value / e-process column(s), 5m pilot"
      contains: "e_value"
  key_links:
    - from: "services/ic_engine.py"
      to: "src/intelligence/statistics/ic_math.py"
      via: "update per-cell e-process for tf=5m in the corpus loop"
      pattern: "e_value"
    - from: "e-process self-verification"
      to: "feature_registry is_control canaries"
      via: "canary e-values must decay toward zero"
      pattern: "is_control"
---

<objective>
Component C (todo 079, medium-high priority). Pilot anytime-valid e-values for `tf=5m` ONLY. Persist a per-cell e-process updated each corpus rerun (likelihood-ratio or universal-inference e-value on IC sign is sufficient - exact kernel is Claude's discretion). Promotion requires cumulative e-value > 1/alpha; demotion symmetric. Evidence compounds across reruns instead of resetting each build - hardening the Concept Registry invariant from "no re-roll on same corpus build" to "no free re-roll on ANY build".

Scope discipline: pilot only, 5m only - do NOT roll out to all timeframes this phase (genuinely new statistical machinery; source doc cautions to pilot first). Kernel function (sibling to ic_math.py), one new column per cell, manifest plumbing, no new service.

Self-verification dependency: the e-process must be checkable against Component D's canaries - canary e-values must decay toward zero. This plan therefore depends on Plan 02 (Component D) landing. It also depends on Plan 05 for ic_engine.py/ic_math.py file ownership serialization.

Output: e-value kernel, 5m e-process persistence + manifest plumbing, migration, unit + canary-decay self-verification coverage.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.1-phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-CONTEXT.md
@.planning/milestones/v3.1-phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-RESEARCH.md

<interfaces>
From src/intelligence/statistics/ic_math.py:
- existing kernel style + `_vectorized_ic`, `apply_bh_fdr` for reuse.

From services/ic_engine.py:
- `--tf` CLI scoping confirmed - the pilot restricts to 5m via the existing tf argument / branch.
- `_compute_cross_sectional_tf` writes feature_ic_scores; the e-process update reads the prior cell's cumulative e-value (persisted column) and multiplies by this run's e-value factor.
- manifest plumbing: ic_engine records run inputs/outputs in a manifest (same mechanism referenced in ensemble_trainer weight_version scoping) - the e-process update is one new persisted column, not a new table.

From Component D (Plan 02): feature_registry is_control=true canaries - the noise/dead canaries' e-values must decay toward zero across reruns (self-verification); the acausal placebo's e-value should grow (positive control).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: e-value kernel (IC-sign likelihood-ratio / universal-inference) in ic_math.py</name>
  <files>src/intelligence/statistics/ic_math.py, tests/unit/test_e_values.py</files>
  <read_first>
    - src/intelligence/statistics/ic_math.py (kernel style, _vectorized_ic)
    - .planning/todos/pending/079-anytime-valid-e-values-corpus-reruns.md (e-value definition + promotion/demotion thresholds)
    - docs/research/fable-2026-07-07-renaissance-layer-refinements.md §L4-1 (source section)
    - CONTEXT.md Component C (likelihood-ratio OR universal-inference e-value on IC sign is enough)
  </read_first>
  <behavior>
    - Test: for a cell with consistent positive IC sign across reruns, the cumulative e-value grows above 1/alpha (promotion threshold).
    - Test: for a null cell (IC sign random), the cumulative e-value stays bounded / decays toward zero (no free re-roll accumulation).
    - Test: the e-value factor is a valid e-value (non-negative, expectation <= 1 under the null - assert on a synthetic null).
    - Test: determinism given fixed inputs.
  </behavior>
  <action>
    Add an e-value kernel to src/intelligence/statistics/ic_math.py computing a per-run e-value factor on the IC sign (likelihood-ratio or universal-inference variant - choose one, document the choice). Provide the cumulative update (prior cumulative e-value * this-run factor) and the promotion (cumulative > 1/alpha) / demotion (symmetric) test. Reuse existing IC math; no new external dependency. Write tests/unit/test_e_values.py FIRST covering the behaviors above, including the null-boundedness property that makes it anytime-valid.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_e_values.py -v</automated>
  </verify>
  <acceptance_criteria>
    - e-value kernel + cumulative-update + promotion/demotion test exist in ic_math.py.
    - `.venv/bin/pytest tests/unit/test_e_values.py -v` passes including the null-boundedness (anytime-valid) assertion.
    - The chosen variant (likelihood-ratio vs universal-inference) is documented in the kernel docstring.
  </acceptance_criteria>
  <done>Anytime-valid e-value kernel exists, compounds correctly, stays bounded under the null, and is unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 5m-only e-process persistence + manifest plumbing + canary-decay self-verification</name>
  <files>services/ic_engine.py, production/migrations/2XX_e_value_pilot_column.sql, tests/unit/test_e_values.py</files>
  <read_first>
    - services/ic_engine.py --tf handling, _compute_cross_sectional_tf write path, manifest recording
    - production/migrations/217/218_*.sql (recent column/table addition shape)
    - Plan 02 (Component D) canary feature_registry rows for the self-verification join
    - CLAUDE.md "never log per-row inside a corpus loop"
  </read_first>
  <behavior>
    - Test: e-process update only runs for tf=5m (other timeframes leave the column null / untouched).
    - Test: canary self-verification - the noise/dead canaries' cumulative e-value does NOT cross the promotion threshold (decays toward zero); assertion flags if it does.
  </behavior>
  <action>
    Persist the per-cell cumulative e-value as a new feature_ic_scores column (migration `2XX_e_value_pilot_column.sql` - VERIFY next free number by listing production/migrations/ at execution time; planning tip is 220, but Plans 01/02 (wave 1), 04 (wave 3), and 05 (wave 4) all claim numbers in this range before this plan's wave 5 runs - do not assume any fixed number, list the directory and take the actual next-free number; todo 095 collision risk). In `_compute_cross_sectional_tf`, for tf=5m only, read the prior cumulative e-value, multiply by this run's e-value factor (Task 1 kernel), persist, and record in the manifest. Restrict strictly to 5m via the existing --tf branch (pilot scope). Add a canary-decay self-verification check (reuse or sit alongside Plan 02's ops_canary_integrity_assert.py): the is_control noise/dead canaries' e-values must decay toward zero; flag loudly if a negative-control canary's cumulative e-value crosses the promotion threshold. Accumulate/report once, no per-cell logging.
  </action>
  <verify>
    <automated>grep -c "e_value" production/migrations/2*_e_value_pilot_column.sql services/ic_engine.py; .venv/bin/pytest tests/unit/test_e_values.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Migration adds the e-process column to feature_ic_scores (verify via `production/migrations/2*_e_value_pilot_column.sql`, filename number determined at execution time).
    - e-process update is gated to tf=5m only (grep shows the tf branch guarding the update).
    - Canary-decay self-verification exists and flags a negative-control canary crossing the promotion threshold.
    - `.venv/bin/pytest tests/unit/test_e_values.py -v` passes.
  </acceptance_criteria>
  <done>5m e-process persists and compounds across reruns, manifest records it, and canary e-values are self-verified to decay toward zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature_ic_scores (prior e-value) -> ic_engine read/update | Existing table read/write; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143.1-06-V2 | Spoofing/Auth | none | accept | N/A - internal batch measurement code, no network surface |
| T-143.1-06-V3 | Session | none | accept | N/A |
| T-143.1-06-V4 | Access Control | none | accept | N/A |
| T-143.1-06-V5 | Input Validation | none | accept | N/A - no new APR keys or external input |
| T-143.1-06-V6 | Cryptography | none | accept | N/A |
| T-143.1-06-INT | Data/statistical integrity | e-process | mitigate | anytime-valid null-boundedness property unit-tested; canary-decay self-verification catches a mis-specified e-value that would falsely accumulate |
| T-143.1-06-SC | Tampering (supply chain) | none | accept | N/A - no npm/pip/cargo installs; no new packages |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/test_e_values.py -v` green.
- Full suite no new failures.
- Canary-decay self-verification passes once the corpus re-run (Plan 07) populates 5m e-values.
</verification>

<success_criteria>
- 5m-only e-process persists and compounds across reruns; anytime-valid kernel unit-tested; canary e-values self-verified to decay toward zero.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-06-SUMMARY.md` when done.
</output>
