---
phase: 143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap
plan: 07
type: execute
wave: 6
depends_on: ["143.1-01", "143.1-02", "143.1-03", "143.1-04", "143.1-05", "143.1-06"]
files_modified:
  - docs/plans/methodology-change-ledger.md
  - .planning/milestones/v3.1-phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-07-CORPUS-RERUN.md
autonomous: true
requirements: [A, B, C, D, F]
must_haves:
  truths:
    - "A SINGLE full-pipeline corpus re-run from Step 1 (backfill_feature_factory --compute-only) reflects Components A + B + C + D + F together (D's new canary FeatureVector columns force Step 1, not an ic_engine-only re-run)"
    - "Component A's staged-validation gate passed BEFORE this re-run was triggered"
    - "The mainline champion (Steps 7-8: ensemble_trainer + alpha_publisher) ran with alpha.ensemble.sign_symmetric OFF (--no-sign-symmetric), so Component E's sign-symmetric weighting is NOT shipped to the champion epoch by this routine re-run - this run produces only the corrected MEASUREMENT corpus; the champion-vs-challenger comparison is Plan 08"
    - "Component D canary integrity assertion ran during the re-run and did not halt on a false-positive (negative controls null in the POOLED family, acausal placebo clears)"
    - "Component F A/B result (raw vs vol-normalized POOLED rankings) is recorded; keep-or-retire decision made"
    - "Component B decomposition columns and Component C 5m e-values are populated corpus-wide"
    - "feature_ic_scores carries measurement-layer evidence of the sign-symmetric walk-forward (ic_sign=-1 rows with correctly-computed passes_walkforward / ic_ci_upper), proving Plan 04's cross-sectional fix landed in the load-bearing function - independent of the flag-OFF champion eligibility"
  artifacts:
    - path: ".planning/milestones/v3.1-phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-07-CORPUS-RERUN.md"
      provides: "Re-run manifest: row counts, champion flag-OFF confirmation, canary assertion result, F A/B result, sign-symmetric measurement proof, before/after emission-threshold note"
      min_lines: 30
  key_links:
    - from: "scripts/ops/corpus/ops_corpus_pipeline_run.sh --from-step 1"
      to: "feature_vectors / feature_ic_scores"
      via: "full pipeline recompute landing all canary columns + sign-aware walk-forward CI + decomposition + e-values"
      pattern: "from-step 1"
---

<objective>
Execute the ONE full-pipeline corpus re-run this entire phase bundles into, and validate the measurement-layer components (A, B, C, D, F) on the live result. Because Component D adds new canary `FeatureVector` columns, this re-run MUST start from Step 1 (`backfill_feature_factory.py --compute-only`), a multi-hour full recompute across all symbols/timeframes/history - NOT a cheap `ic_engine.py`-only invocation (RESEARCH Pitfall 6). Landing D before this single re-run makes it genuinely one re-run covering A+B+C+D+F, consistent with the phase's batching rationale.

CHAMPION STAYS ON OLD ELIGIBILITY (Fable review BLOCKER 2): Plan 04 made the sign-symmetric weighting behavior gated by the `alpha.ensemble.sign_symmetric` APR flag (default false). This re-run's Steps 7-8 (ensemble_trainer + alpha_publisher, the mainline champion) MUST run with the flag OFF (`--no-sign-symmetric`) so the champion epoch keeps its pre-Component-E long-only eligibility on the CORRECTED corpus. Do NOT ship Component E's behavior to the champion here. The single ic_engine Step 5, however, writes the sign-aware walk-forward measurement UNCONDITIONALLY (equivalence-preserving for positive features) so the corpus carries correct `passes_walkforward`/`ic_ci_upper` for BOTH signs - the raw material Plan 08's challenger (flag ON) will train from. This run produces the corrected measurement corpus; it does NOT perform the champion/challenger comparison (that is Plan 08).

HARD PRECONDITION: Component A's staged-validation gate (Plan 01 Task 3) must have passed - the bootstrap CI shows a SUSPECT rate within the pre-committed bound - AND the bootstrap runtime benchmark (Plan 01 Task 4) budget must be recorded - before this re-run is triggered. Do not start otherwise.

Purpose: this is not "future work" - it is the deliverable that produces the corrected, re-measured corpus Phase 144 builds on. Component E's shadow-mode validation (Plan 08) runs on top of this re-run's output.

Output: a completed full-pipeline re-run with the champion trained flag-OFF, a recorded canary assertion result, Component F's A/B result + keep/retire decision, populated B/C columns, and confirmation that feature_ic_scores carries the sign-symmetric walk-forward measurement (proof Plan 04's fix landed in the load-bearing cross-sectional function).
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.1-phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-CONTEXT.md
@.planning/milestones/v3.1-phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-RESEARCH.md

<interfaces>
scripts/ops/corpus/ops_corpus_pipeline_run.sh:
- `--from-step N` and `--symbols SYM1,SYM2`. Steps: 1 backfill_feature_factory (--compute-only), 2 regime_writer, 3 forward_return_writer, 4 equity_regime_model, 5 ic_engine, 6 ic_shrinkage, 7 ensemble_trainer, 8 alpha_publisher. Corpus Consistency Gate runs after step 2 (regime-label uniformity).
- backfill_feature_factory silently skips all symbols if backfill_status is empty - seed from market_data_ohlcv after any truncation (STATE.md Corpus Pipeline Gotcha).
- New canary columns require Step 1 so feature_vectors gets them populated corpus-wide.
- Champion flag-OFF: ensemble_trainer (Step 7) accepts `--no-sign-symmetric` (Plan 04 Task 2's CLI override). If ops_corpus_pipeline_run.sh does not already pass a sign-symmetric flag to Step 7, run Step 7-8 explicitly with `--no-sign-symmetric` (or confirm the APR default is false so the champion is unaffected) - record which mechanism was used.
scripts/ops/alpha/ops_canary_integrity_assert.py (Plan 02) - runs after Step 5.
scripts/ops/alpha/ops_vol_normalized_target_ab.py (Plan 03) - the F A/B comparison, run post-re-run.
Live DB: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "..."`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm Component A staged gate + runtime budget, seed backfill_status, trigger the single full-pipeline re-run from Step 1 with the champion flag OFF</name>
  <files>.planning/milestones/v3.1-phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-07-CORPUS-RERUN.md</files>
  <read_first>
    - scripts/ops/corpus/ops_corpus_pipeline_run.sh (full - step order, Corpus Consistency Gate, --from-step, resume-on-failure, whether Step 7 already forwards a sign-symmetric flag)
    - docs/plans/methodology-change-ledger.md (confirm the Component A staged-validation entry AND the bootstrap runtime-budget entry from Plan 01 exist and passed)
    - .planning/STATE.md Corpus Pipeline Gotcha (backfill_status seeding) + Corpus pipeline state memory notes
    - CONTEXT.md/RESEARCH.md Pitfall 6 (why Step 1 is mandatory); Plan 04 objective (champion flag-OFF rationale)
  </read_first>
  <action>
    Verify the Plan 01 Task 3 staged-validation gate passed (methodology-change-ledger shows the SUSPECT rate within the pre-committed bound) AND Plan 01 Task 4's bootstrap runtime budget is recorded - if either is missing, STOP and do not run. Seed backfill_status from market_data_ohlcv if empty (STATE.md gotcha SQL). Run `bash scripts/ops/corpus/ops_corpus_pipeline_run.sh --from-step 1` as a background/long-running operation (multi-hour). Ensure Steps 7-8 (ensemble_trainer + alpha_publisher, the champion) run with `alpha.ensemble.sign_symmetric` OFF: either the APR default is false (confirm via psql) or pass `--no-sign-symmetric` explicitly to Step 7 - record which. This single run lands A (bootstrap CI in step 5), D (canary columns in step 1 + assertion after step 5), B (decomposition columns in step 5), C (5m e-values in step 5), and the UNCONDITIONAL sign-aware walk-forward measurement. The Corpus Consistency Gate (regime-label uniformity) must pass after step 2. Create 143.1-07-CORPUS-RERUN.md recording the run start, the --from-step 1 rationale (D's new columns), the champion flag-OFF confirmation, and per-step completion.
  </action>
  <verify>
    <automated>bash -n scripts/ops/corpus/ops_corpus_pipeline_run.sh && echo "orchestrator syntax OK"; grep -c "from-step 1\|Step 1\|sign_symmetric\|no-sign-symmetric" .planning/milestones/v3.1-phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-07-CORPUS-RERUN.md</automated>
    <human-check>Full-pipeline re-run completes all 8 steps from Step 1; feature_vectors shows the new canary columns populated; the champion (Steps 7-8) trained with sign_symmetric OFF (recorded); the Corpus Consistency Gate passed after step 2.</human-check>
  </verify>
  <acceptance_criteria>
    - Component A staged gate + runtime budget confirmed in the ledger before the run started.
    - The re-run used `--from-step 1` (recorded rationale: D's new FeatureVector columns).
    - Steps 7-8 ran with alpha.ensemble.sign_symmetric OFF (mechanism recorded: APR default false or explicit --no-sign-symmetric).
    - 143.1-07-CORPUS-RERUN.md records run start, step order, champion flag-OFF, and completion; canary columns populated in feature_vectors (psql count > 0).
  </acceptance_criteria>
  <done>The single full-pipeline corpus re-run from Step 1 completed with the champion trained flag-OFF, landing A+B+C+D+F in one pass, with canary columns populated corpus-wide.</done>
</task>

<task type="auto">
  <name>Task 2: Validate D (canary assertion), F (raw-vs-vol A/B), B/C column population, and sign-symmetric MEASUREMENT landed (with zero-count discriminator)</name>
  <files>.planning/milestones/v3.1-phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-07-CORPUS-RERUN.md, docs/plans/methodology-change-ledger.md</files>
  <read_first>
    - scripts/ops/alpha/ops_canary_integrity_assert.py (Plan 02), scripts/ops/alpha/ops_vol_normalized_target_ab.py (Plan 03)
    - RESEARCH.md Pitfall 1 warning sign (if alpha_events short-side count still ~0, the walk-forward fix landed in the wrong function); Fable review MINOR 8 (the zero-count discriminator)
    - docs/plans/methodology-change-ledger.md (F A/B recording location from Plan 03)
    - CONTEXT.md Component E emission-threshold re-measurement note
  </read_first>
  <action>
    After the re-run: (1) confirm the canary integrity assertion (ops_canary_integrity_assert.py) passed during the run - negative-control canaries null in the POOLED family, acausal placebo cleared the gate; record the result. (2) Run ops_vol_normalized_target_ab.py and record the raw-vs-vol-normalized POOLED ranking comparison; make the keep-or-retire decision per the locked contract (retire if materially identical) and record it in the methodology-change-ledger. (3) Confirm B's sign_hit_rate + magnitude-conditional IC columns and C's 5m e-values are populated (psql counts). (4) CRITICAL sign-symmetry MEASUREMENT proof (this is a MEASUREMENT-layer query against feature_ic_scores, independent of the flag-OFF champion eligibility): query the count of `ic_sign = -1` rows that WOULD be sign-symmetric-eligible - `SELECT count(*) FROM feature_ic_scores WHERE symbol='POOLED' AND is_pooled=true AND regime != '_pooled' AND ic_sign = -1 AND ic_ci_upper < 0 AND passes_walkforward = true AND passes_fdr = true`. A non-zero count proves Plan 04's walk-forward fix landed in the load-bearing cross-sectional function. ZERO-COUNT DISCRIMINATOR (MINOR 8): a legitimate zero (no genuine contrarian survives honest measurement) is indistinguishable from "fix landed in the wrong function" unless discriminated - so if that count is zero, run the fallback `SELECT count(*) FROM feature_ic_scores WHERE symbol='POOLED' AND is_pooled=true AND regime != '_pooled' AND ic_sign = -1 AND passes_walkforward = true` (contrarians that pass walk-forward regardless of the CI gate). If that fallback is non-zero, the walk-forward fix DID take effect (resolve in favor of "fix correct, just no surviving contrarians after the CI gate"); if it is literally zero (no ic_sign=-1 row passes walk-forward at all), RE-OPEN the wrong-function hypothesis and halt. Record all in 143.1-07-CORPUS-RERUN.md. Do NOT re-litigate EIC-04's prior PASS verdict (explicitly deferred to the project owner) - record that A's CI changed, not a verdict reversal.
  </action>
  <verify>
    <automated>grep -c "canary\|vol_normalized\|ic_sign\|e_value\|sign_hit_rate\|discriminator\|passes_walkforward" .planning/milestones/v3.1-phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-07-CORPUS-RERUN.md</automated>
    <human-check>Canary assertion passed (POOLED negative controls null, placebo clears); F A/B recorded with keep/retire decision; B/C columns populated; the sign-symmetric measurement proof (or its zero-count discriminator resolution) is recorded.</human-check>
  </verify>
  <acceptance_criteria>
    - Canary assertion result recorded: POOLED negative controls did NOT clear the gate; acausal placebo DID.
    - Component F A/B ranking comparison recorded with an explicit keep-or-retire decision in the methodology-change-ledger.
    - B's two columns and C's 5m e-values are populated (psql counts > 0 recorded).
    - The sign-symmetric measurement proof query is recorded; if its count is zero, the MINOR-8 discriminator fallback query result and its interpretation (fix-correct-but-no-survivors vs re-open-wrong-function) are recorded.
    - Emission-threshold re-measurement flagged for Plan 08 (thresholds were calibrated on a long-only-skewed distribution).
  </acceptance_criteria>
  <done>All measurement-layer components validated on the live re-run; sign-symmetric measurement confirmed present (or its zero-count case discriminated); F decision recorded; canary integrity confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus orchestrator -> live DB tables | Full recompute writes feature_vectors/feature_ic_scores; internal, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143.1-07-V2 | Spoofing/Auth | none | accept | N/A - local corpus batch operation, no network surface |
| T-143.1-07-V3 | Session | none | accept | N/A |
| T-143.1-07-V4 | Access Control | none | accept | N/A |
| T-143.1-07-V5 | Input Validation | none | accept | N/A - no new APR keys or external input in this plan |
| T-143.1-07-V6 | Cryptography | none | accept | N/A |
| T-143.1-07-INT | Data/statistical integrity | full re-run | mitigate | staged-validation precondition + champion flag-OFF (no unproven behavior shipped to champion) + canary assertion during the run + sign-symmetry measurement proof with zero-count discriminator prevent a silently-wrong corpus or a silently-shipped scoring change reaching Phase 144 |
| T-143.1-07-SC | Tampering (supply chain) | none | accept | N/A - no npm/pip/cargo installs; no new packages |
</threat_model>

<verification>
- Full-pipeline re-run completed all 8 steps from Step 1; Corpus Consistency Gate passed; champion trained flag-OFF.
- Canary assertion passed; F A/B recorded; B/C columns populated; sign-symmetric measurement proof (or discriminator resolution) recorded.
- 143.1-07-CORPUS-RERUN.md and methodology-change-ledger updated.
</verification>

<success_criteria>
- One full-pipeline re-run from Step 1 reflects A+B+C+D+F together with the champion on old eligibility (flag OFF); the corpus is trustworthy for Phase 144 and for Plan 08's shadow validation.
- Sign-symmetric measurement confirmed present (Pitfall 1 proof, with MINOR-8 discriminator for the zero case); F keep/retire decided; canary integrity confirmed.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/143.1-measurement-and-eligibility-integrity-fisher-z-ci-bootstrap-/143.1-07-SUMMARY.md` when done.
</output>
