---
phase: 161-controlled-vocabulary-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/237_controlled_vocabulary_schema.sql
  - production/migrations/238_controlled_vocabulary_seed_namespaces.sql
autonomous: true
requirements: []

must_haves:
  truths:
    - "Three tables (controlled_vocabulary, vocabulary_group, vocabulary_group_member) exist with the design-doc schema shape and FK integrity"
    - "Exactly 6 namespaces are seeded with their full code sets: regime_hmm (5), regime_cross_sectional_equity (9), regime_cross_sectional_rates (6), timeframe (5), asset_class (3), tier (3)"
    - "Vocabulary groups seed the overlapping-facet memberships for regime_hmm, regime_cross_sectional_equity, and regime_cross_sectional_rates"
    - "Both migrations are idempotent (safe to re-run) and no archived-SLA namespace is seeded"
    - "D-02: tag_vocabulary and instrument_tags are untouched by this migration — no shared table, no FK to either"
    - "D-03: regime_hmm's overlapping trending/transition/bullish_bias/bearish_bias groups are seeded as independent vocabulary_group rows, not a single ordered scale"
  artifacts:
    - path: "production/migrations/237_controlled_vocabulary_schema.sql"
      provides: "3-table registry schema DDL"
      contains: "CREATE TABLE"
    - path: "production/migrations/238_controlled_vocabulary_seed_namespaces.sql"
      provides: "Seed rows for the 6 live namespaces + groups"
      contains: "INSERT INTO controlled_vocabulary"
  key_links:
    - from: "vocabulary_group_member"
      to: "controlled_vocabulary"
      via: "FOREIGN KEY (namespace, code)"
      pattern: "FOREIGN KEY \\(namespace, code\\)"
    - from: "vocabulary_group_member"
      to: "vocabulary_group"
      via: "FOREIGN KEY (namespace, group_name)"
      pattern: "FOREIGN KEY \\(namespace, group_name\\)"
---

<objective>
Create the three-table Controlled Vocabulary schema (migration 237) and seed the six live
namespaces plus their vocabulary groups (migration 238). This is the storage foundation every
other plan in the phase reads from.

Purpose: Give the codebase a single migration-governed source of truth for symbolic taxonomies
(the "APR for symbolic codes"), replacing hardcoded label sets scattered across consumers.
Output: Two applied SQL migrations; 3 tables populated with exactly the 6 locked namespaces.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v3.1-phases/161-controlled-vocabulary-system/161-CONTEXT.md
@.planning/milestones/v3.1-phases/161-controlled-vocabulary-system/161-PATTERNS.md
@docs/research/concept-controlled-vocabulary.md
@docs/research/fable-2026-07-16-controlled-vocabulary-open-questions-review.md

<interfaces>
<!-- Design-doc schema (docs/research/concept-controlled-vocabulary.md lines 56-84). -->
<!-- controlled_vocabulary: PK (namespace, code); cols namespace, code, label, description, -->
<!--   sort_order INT DEFAULT 0, is_deprecated BOOLEAN DEFAULT FALSE, created_at. -->
<!-- vocabulary_group: PK (namespace, group_name); cols namespace, group_name, label, -->
<!--   description, sort_order INT DEFAULT 0. -->
<!-- vocabulary_group_member: PK (namespace, group_name, code); FK (namespace, code) -> -->
<!--   controlled_vocabulary; FK (namespace, group_name) -> vocabulary_group. -->
<!-- Migration provenance-header + BEGIN/COMMIT + idempotency conventions: analog is -->
<!-- production/migrations/233_concept_registry_mvp.sql (see 161-PATTERNS.md). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 237 — three-table Controlled Vocabulary schema</name>
  <files>production/migrations/237_controlled_vocabulary_schema.sql</files>
  <read_first>
    - /home/bg/dev/indicagent/production/migrations/233_concept_registry_mvp.sql (header/provenance + CREATE TABLE + COMMENT ON TABLE + idempotency conventions to copy exactly)
    - /home/bg/dev/indicagent/production/migrations/227_instrument_tag_vocabulary.sql (the `(symbol, tag)` join-table PK precedent for vocabulary_group_member)
    - /home/bg/dev/indicagent/docs/research/concept-controlled-vocabulary.md (schema section, lines 56-84 — authoritative table shapes)
    - /home/bg/dev/indicagent/.planning/milestones/v3.1-phases/161-controlled-vocabulary-system/161-PATTERNS.md (migration 237 pattern assignment)
  </read_first>
  <action>
    First confirm 237 is still a free migration number (`ls production/migrations/ | grep '^237'` must be empty); if taken, use the next free integer and note it in the SUMMARY.
    Write the migration with a provenance header comment citing docs/research/concept-controlled-vocabulary.md as the design source and this phase's CONTEXT.md D-01/D-03/D-04/D-04b (six-namespace scope). Wrap all DDL in a single `BEGIN;` ... `COMMIT;`.
    Create three tables, all with `CREATE TABLE IF NOT EXISTS`:
    - `controlled_vocabulary`: columns `namespace TEXT NOT NULL`, `code TEXT NOT NULL`, `label TEXT NOT NULL`, `description TEXT`, `sort_order INT NOT NULL DEFAULT 0`, `is_deprecated BOOLEAN NOT NULL DEFAULT FALSE`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`, `PRIMARY KEY (namespace, code)`.
    - `vocabulary_group`: columns `namespace TEXT NOT NULL`, `group_name TEXT NOT NULL`, `label TEXT NOT NULL`, `description TEXT`, `sort_order INT NOT NULL DEFAULT 0`, `PRIMARY KEY (namespace, group_name)`.
    - `vocabulary_group_member`: columns `namespace TEXT NOT NULL`, `group_name TEXT NOT NULL`, `code TEXT NOT NULL`, `PRIMARY KEY (namespace, group_name, code)`, `FOREIGN KEY (namespace, code) REFERENCES controlled_vocabulary(namespace, code)`, `FOREIGN KEY (namespace, group_name) REFERENCES vocabulary_group(namespace, group_name)`.
    Add a `COMMENT ON TABLE controlled_vocabulary` stating the row's epistemic kind — an authoritative/flat vocabulary definition — explicitly distinct from tag_vocabulary's weighted/falsifiable-hypothesis rows (reinforces D-02). Add short `COMMENT ON TABLE` for the other two tables.
    Do NOT add a shared base table, ENUM types, or any archived-SLA namespace scaffolding. Do NOT touch tag_vocabulary or instrument_tags (D-02).
  </action>
  <verify>
    <automated>psql -U postgres -h localhost -d indicagent -f production/migrations/237_controlled_vocabulary_schema.sql && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('controlled_vocabulary','vocabulary_group','vocabulary_group_member')" | grep -qx 3</automated>
  </verify>
  <acceptance_criteria>
    - The three tables exist: `SELECT count(*) FROM information_schema.tables WHERE table_name IN ('controlled_vocabulary','vocabulary_group','vocabulary_group_member')` returns 3.
    - `controlled_vocabulary` PK is `(namespace, code)` and `vocabulary_group_member` PK is `(namespace, group_name, code)` (verify via `\d`).
    - `vocabulary_group_member` has both composite FKs targeting `controlled_vocabulary(namespace, code)` and `vocabulary_group(namespace, group_name)`.
    - Re-running the migration file a second time exits 0 with no error (idempotent via `CREATE TABLE IF NOT EXISTS`).
    - `grep -c 'tag_vocabulary\|instrument_tags\|CREATE TYPE' production/migrations/237_controlled_vocabulary_schema.sql` returns 0.
  </acceptance_criteria>
  <done>Three tables created with correct composite PKs and FKs; migration idempotent; no ENUM/tag_vocabulary references.</done>
</task>

<task type="auto">
  <name>Task 2: Create migration 238 — seed 6 namespaces + vocabulary groups</name>
  <files>production/migrations/238_controlled_vocabulary_seed_namespaces.sql</files>
  <read_first>
    - /home/bg/dev/indicagent/production/migrations/227_instrument_tag_vocabulary.sql (seed section lines 49-60: comment-banner-per-category + aligned literal-tuple INSERT style)
    - /home/bg/dev/indicagent/production/migrations/237_controlled_vocabulary_schema.sql (the schema this seed populates — read after Task 1)
    - /home/bg/dev/indicagent/docs/research/fable-2026-07-16-controlled-vocabulary-open-questions-review.md (V1: the rates namespace split + rates group facets — this SUPERSEDES RESEARCH.md's single 9-label regime_cross_sectional)
    - /home/bg/dev/indicagent/.planning/milestones/v3.1-phases/161-controlled-vocabulary-system/161-PATTERNS.md (live-verified seed counts, lines 114-127)
  </read_first>
  <action>
    Write migration 238 with a provenance header citing the Fable review (fable-2026-07-16-controlled-vocabulary-open-questions-review.md) for the equity/rates split and CONTEXT.md D-03/D-04/D-04b for the groups. Wrap in `BEGIN;` ... `COMMIT;`. Every INSERT uses `ON CONFLICT DO NOTHING` on the natural composite key (`ON CONFLICT (namespace, code)` for controlled_vocabulary, `ON CONFLICT (namespace, group_name)` for vocabulary_group, `ON CONFLICT (namespace, group_name, code)` for vocabulary_group_member) so the migration is idempotent.
    Seed `controlled_vocabulary` rows (one banner comment per namespace, aligned tuples), with sensible human-readable `label` text and ascending `sort_order`:
    - `regime_hmm` (5 codes, ordered by emission mean): trending_down, transition_down, ranging, transition_up, trending_up.
    - `regime_cross_sectional_equity` (9 codes): low_bull, low_neutral, low_bear, mid_bull, mid_neutral, mid_bear, high_bull, high_neutral, high_bear.
    - `regime_cross_sectional_rates` (6 codes): flat_tight, flat_wide, steep_tight, steep_wide, inverted_tight, inverted_wide.
    - `timeframe` (5 codes): 1m, 5m, 15m, 1h, 1d.
    - `asset_class` (3 codes): equity, futures, fx.
    - `tier` (3 codes): 0_atomic, 1_interaction, 2_theory. Seed ALL 3 — the "2 live values" note in CONTEXT.md's code_context is a stale snapshot; RESEARCH.md Critical Finding 2 and 161-PATTERNS.md confirm 3 live values.
    Seed `vocabulary_group` + `vocabulary_group_member` per D-03/D-04/D-04b:
    - regime_hmm groups: `trending` = {trending_down, trending_up}; `transition` = {transition_down, transition_up}; `bullish_bias` = {transition_up, trending_up}; `bearish_bias` = {transition_down, trending_down}. `ranging` stays ungrouped (do NOT create a group of one).
    - regime_cross_sectional_equity groups (two crossed facets): vol-tier `low_vol` = {low_bull, low_neutral, low_bear}, `mid_vol` = {mid_bull, mid_neutral, mid_bear}, `high_vol` = {high_bull, high_neutral, high_bear}; direction `bull` = {low_bull, mid_bull, high_bull}, `neutral` = {low_neutral, mid_neutral, high_neutral}, `bear` = {low_bear, mid_bear, high_bear}.
    - regime_cross_sectional_rates groups (two crossed facets): curve-shape `flat` = {flat_tight, flat_wide}, `steep` = {steep_tight, steep_wide}, `inverted` = {inverted_tight, inverted_wide}; width `tight` = {flat_tight, steep_tight, inverted_tight}, `wide` = {flat_wide, steep_wide, inverted_wide}.
    Do NOT seed signal_outcome, entry_type, signal_status, or session_type (D-01: archived, deferred).
  </action>
  <verify>
    <automated>psql -U postgres -h localhost -d indicagent -f production/migrations/238_controlled_vocabulary_seed_namespaces.sql && PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(DISTINCT namespace) FROM controlled_vocabulary" | grep -qx 6</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT count(DISTINCT namespace) FROM controlled_vocabulary` returns 6.
    - Per-namespace code counts: `SELECT namespace, count(*) FROM controlled_vocabulary GROUP BY 1` yields regime_hmm=5, regime_cross_sectional_equity=9, regime_cross_sectional_rates=6, timeframe=5, asset_class=3, tier=3.
    - `SELECT count(*) FROM controlled_vocabulary WHERE namespace IN ('signal_outcome','entry_type','signal_status','session_type')` returns 0.
    - Every `vocabulary_group_member` row resolves both FKs (no insert error means membership rows reference real codes/groups); `SELECT count(*) FROM vocabulary_group WHERE namespace='regime_hmm'` returns 4 and `regime_cross_sectional_equity` returns 6 and `regime_cross_sectional_rates` returns 5.
    - Re-running migration 238 exits 0 (idempotent via ON CONFLICT DO NOTHING).
  </acceptance_criteria>
  <done>Six namespaces seeded with exact code counts; groups seeded per D-03/D-04/D-04b; no archived namespace present; migration idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration author → DB DDL/DML | Reviewed, committed SQL applied by operator; no runtime/untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-01 | Tampering | migration 238 seed rows | accept | Seed values are static literals in a reviewed migration; FK constraints reject any membership row referencing a non-existent code/group at apply time (fails loud) |
| T-161-02 | Information Disclosure | vocabulary tables | accept | Rows are non-sensitive symbolic taxonomy metadata; no PII/secrets |
| T-161-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this phase (RESEARCH.md Package Legitimacy Audit: N/A) — pure SQL |
</threat_model>

<verification>
- Apply 237 then 238 in order against the live DB; both idempotent on re-run.
- `SELECT namespace, count(*) FROM controlled_vocabulary GROUP BY 1 ORDER BY 1` matches the six expected counts.
- Group membership counts match D-03/D-04/D-04b.
</verification>

<success_criteria>
- Three vocabulary tables exist with correct composite PKs and FKs.
- Exactly 6 namespaces seeded with correct code and group counts.
- No archived-SLA namespace seeded; tag_vocabulary/instrument_tags untouched.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/161-controlled-vocabulary-system/161-01-SUMMARY.md` when done.
</output>
