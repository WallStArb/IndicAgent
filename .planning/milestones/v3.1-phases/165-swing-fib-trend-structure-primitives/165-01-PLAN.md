---
phase: 165-swing-fib-trend-structure-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/267_swing_fib_trend_structure_primitives.sql
  - src/intelligence/schemas.py
  - src/intelligence/feature_factory.py
  - src/intelligence/features/feature_vector_persistence.py
  - services/feature_vector_pipeline.py
  - services/backfill_feature_factory.py
  - tests/unit/test_feature_factory.py
  - tests/unit/test_canary_predictors.py
  - tests/unit/intelligence/test_feature_factory_p7.py
  - tests/unit/services/test_feature_vector_writer.py
  - tests/unit/services/test_feature_vector_writer_column_mapping.py
  - tests/unit/services/test_backfill_feature_factory.py
autonomous: true
requirements: ["D-01", "D-02", "D-03", "D-04", "D-06"]

must_haves:
  truths:
    - "feature_vectors has 41 new DOUBLE PRECISION columns; zero raw-price columns (no swing_high, swing_low, fib_236..fib_786, nearest_fib_level, weekly_pivot, prior_session_high, nearest_session_level, asian_session_high) and zero raw-index columns (no swing_high_idx/swing_low_idx)"
    - "ic_engine startup drift gate passes: feature_registry.feature_name set == dataclasses.fields(FeatureVector) name set"
    - "All 41 new FeatureVector fields are typed float | None (D-01's nullable contract), declared before the defaulted canary block so dataclass ordering stays legal"
    - "FeatureVector constructs without crash on every bar: all 41 threaded through _build_feature_vector and both call sites plus _cold_start_vector as None placeholders (real values arrive in Plans 02-04)"
    - "17 new feature.swing.*/feature.trend_structure.*/feature.swing_momentum.*/feature.fib.*/feature.session_levels.* APR keys exist in config_state and are wired into FeatureFactoryConfig from BOTH config build sites"
  artifacts:
    - path: "production/migrations/267_swing_fib_trend_structure_primitives.sql"
      provides: "41 feature_vectors columns + COMMENT ON COLUMN each + 41 feature_registry rows (group_name='session', tier='2_theory', added_phase='165') + 17 APR 3-table seeds"
      contains: "swing_high_dist_atr"
    - path: "src/intelligence/schemas.py"
      provides: "41 new FeatureVector fields as one contiguous float | None block; docstring group table + Total line updated"
      contains: "swing_velocity_bias"
    - path: "src/intelligence/feature_factory.py"
      provides: "41 FEATURE_VECTOR_DOMAIN 'structural' entries + 16 defaulted FeatureFactoryConfig fields + 41 None-defaulted _build_feature_vector kwargs"
      contains: "swing_pivot_window"
    - path: "src/intelligence/features/feature_vector_persistence.py"
      provides: "_SWING_FIB_TREND_FIELD_NAMES append-only derive-by-name slice threaded into _ALL_COLUMN_NAMES and the params tuple"
      contains: "_SWING_FIB_TREND_FIELD_NAMES"
  key_links:
    - from: "src/intelligence/schemas.py FeatureVector"
      to: "production/migrations/267 feature_registry rows"
      via: "identical field-name set (ic_engine startup drift gate)"
      pattern: "swing_high_dist_atr"
    - from: "src/intelligence/features/feature_vector_persistence.py _SWING_FIB_TREND_FIELD_NAMES"
      to: "_ALL_COLUMN_NAMES + feature_vector_to_insert_params return tuple"
      via: "derive-by-name slice of _ALL_FEATURE_VECTOR_FIELD_NAMES"
      pattern: "_SWING_FIB_TREND_FIELD_NAMES"
    - from: "services/feature_vector_pipeline.py + services/backfill_feature_factory.py"
      to: "FeatureFactoryConfig swing_*/trend_structure_*/swing_momentum_*/fib_*/session_levels_* fields"
      via: "cfg.get_sync('feature.<ns>.<key>', default)"
      pattern: "feature.swing.pivot_window"
---

<objective>
Establish the complete data contract for all 41 Phase 165 swing/fib/trend/session primitives in one
atomic change, so the three downstream compute plans (02: swing detection + trend structure;
03: swing momentum + fibonacci zones; 04: session levels) only replace `None` placeholders with real
computed values and never touch schema, registry, persistence, or APR again.

Purpose: `ic_engine` and `feature_registry_service` both enforce an exact-set alignment gate between
`feature_registry.feature_name` and `dataclasses.fields(FeatureVector)`. Splitting the schema change
across compute plans would leave that gate red between plans. One migration, one contract change.

Output: Migration 267 (41 columns + 41 registry rows + 17 APR keys), updated `FeatureVector` /
`FEATURE_VECTOR_DOMAIN` / `FeatureFactoryConfig` / persistence INSERT, both config build sites wired,
and every hardcoded field-count assertion in the test suite bumped by exactly 41.
</objective>

<source_coverage_audit>
Multi-source coverage for the whole Phase 165 plan set (01-04). Every source item is COVERED.

GOAL (ROADMAP Phase 165): port 5 remaining `i3_structure/` plugins into real v3 `FeatureVector`
primitives, 41 new columns, fixing 2 correctness bugs, with macd/bocpd/fib-extensions parked.
COVERED — contract (Plan 01), swing_detector + trend_structure (Plan 02), swing_momentum +
fibonacci_zones (Plan 03), session_levels (Plan 04).

REQ: no `.planning/REQUIREMENTS.md` exists for this project (confirmed 2026-07-27). Per the Phase
163/164/166 precedent, 165-CONTEXT.md's D-01..D-15 decision IDs ARE the requirement set and are used
verbatim in each plan's `requirements` frontmatter.

RESEARCH (165-RESEARCH.md) findings:
- Finding A (silent-numeric-default bug in trend_structure.py + swing_detector.py) -> Plan 01 (nullable
  types) + Plan 02 (all-None fallback dicts + nullability regression test). COVERED.
- Finding B (three undeclared pivot-window conventions) -> Plan 01 APR keys `feature.swing.pivot_window`
  (shared by swing_detector/trend_structure) and `feature.swing_momentum.confirm_n` (separate). COVERED.
- Finding C (six undeclared magic numbers in swing_momentum.py) -> Plan 01 APR keys. COVERED.
- Finding D (swing_high_idx/swing_low_idx dead weight) -> Plan 01, dropped, never added. COVERED.
- Section 3 (no better port source; `find_peaks`/`find_troughs` is canonical) -> Plans 02/03 port
  directly from `i3_structure/`. COVERED.
- Section 1 session_levels bar-count bug narrower than described (Asian sub-feature already correct)
  -> Plan 04 rewrites only prior_session/overnight/weekly; Asian gets ATR conversion only. COVERED.

CONTEXT (165-CONTEXT.md) decisions:
- D-01 nullable-field fix -> Plan 01 (types) + Plan 02 (behavior + test). COVERED.
- D-02 drop swing_high_idx/swing_low_idx -> Plan 01 (never declared). COVERED.
- D-03 swing_velocity_trend -> swing_velocity_bias {-1,0,1} -> Plan 01 (name) + Plan 03 (encoding). COVERED.
- D-04 fibonacci_zones drops 8 raw/intermediate fields, keeps 4 -> Plan 01 + Plan 03. COVERED.
- D-05 delete fibonacci_zones' cross-plugin fallback -> Plan 03. COVERED.
- D-06 all new APR namespaces -> Plan 01 Task 2. COVERED.
- D-07/D-08/D-09 session_levels bar-count bug + session-boundary mutator + ISO-week accumulator
  extension -> Plan 04. COVERED.
- D-13 gap_filled -> Plan 01 (column) + Plan 04 (compute). COVERED.
- D-15 swing_volume_confirmation -> Plan 01 (column) + Plan 03 (compute). COVERED.
- D-12 promotion-bar note (test price_position against va_position too) -> recorded in migration 267's
  COMMENT ON COLUMN for price_position; the IC test itself runs via ic_engine (explicitly out of this
  phase's scope per CONTEXT Deferred Ideas). COVERED as documentation.

Exclusions (NOT gaps — all are explicit CONTEXT.md Deferred Ideas):
D-10 `macd_events.py` parked (no MACD indicator exists in v3); D-11 `bocpd_changepoint.py` parked
(needs its own latency benchmark); D-14 fibonacci extension levels (1.272/1.618/2.0) deferred until
the base 4 fib fields clear an incremental-IC test; `bars_since_gap_fill` velocity companion;
`nearest_session_level_type` categorical companion; `ctx_SRConsensus`; running the incremental-IC
promotion test itself; the stray `src/intelligence/features/smc_context/` duplicate-directory
housekeeping. Historical `feature_vectors` backfill is deferred to the single consolidated
`backfill_feature_factory.py --compute-only --refresh` pass covering Phases 163/164/165 together
(STATE.md Tier 0 sequencing decision, todo 176) — not this phase's scope.
</source_coverage_audit>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v3.1-phases/165-swing-fib-trend-structure-primitives/165-CONTEXT.md
@.planning/milestones/v3.1-phases/165-swing-fib-trend-structure-primitives/165-RESEARCH.md
@.planning/milestones/v3.1-phases/165-swing-fib-trend-structure-primitives/165-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Verified live at plan-writing time (2026-07-27). Use directly; no codebase exploration needed. -->

Migration analog — production/migrations/255_vp_structural_primitives.sql (Phase 163, commit 4dc708f4):
  Section 1 (lines 56-107): ALTER TABLE feature_vectors ADD COLUMN IF NOT EXISTS <name> DOUBLE PRECISION;
    + one COMMENT ON COLUMN per field. DOUBLE PRECISION, never `real` — every column added after
    migration 201 is double. ADD COLUMN with NULL default against the compressed hypertable is
    metadata-only (no decompress_chunk step).
  Section 2 (lines 116-153): INSERT INTO feature_registry
    (feature_name, group_name, tier, formula_short, normalization, linear_ready, requires_htf, status, added_phase)
    ON CONFLICT (feature_name) DO NOTHING. There is NO is_bounded/is_directional column pair.
  Section 3 (lines 159-249): APR 3-table seed per key — config_schema
    (config_key, value_type, default_value, min_value, max_value, description)
    + config_state (config_key, config_value, version)
    + config_history (timestamp, config_key, version, config_value, changed_by, reason);
    all ON CONFLICT DO NOTHING.

FeatureVector — src/intelligence/schemas.py:
  Live dataclass starts at line 1204 (class docstring with the binding group table + "Total: 172" line).
  Phase 163 session block: lines 1264-1291 (poc_dist_atr ... sr_level_count), all `float | None`, NO default.
  Defaulted block starts at `canary_noise_gaussian` (~line 1489) and runs through momentum_rank_z/
  volume_rank_z/volatility_rank_z at the end. A non-defaulted field CANNOT be declared after a
  defaulted one — new fields must go BEFORE the `# Canary / Control Predictors` comment.
  ARCHIVED TRAP: schemas.py lines 228-341 contain a dead `I3Structure` Pydantic model that defines
  swing_high/trend_direction/price_position by the same names. Never edit or copy from that block.

FEATURE_VECTOR_DOMAIN — src/intelligence/feature_factory.py lines 62-106.
  Phase 163's entries at lines 88-106 are the exact analog: one `"<field>": "structural",` per field.
  Guard test: tests/unit/intelligence/test_feature_factory_p7.py:428 asserts
  `len(FEATURE_VECTOR_DOMAIN) == 172` and `set(keys) == fv_fields`.

FeatureFactoryConfig — src/intelligence/feature_factory.py:
  class at line 268; docstring `field: APR feature.<ns>.<key>` list ends at line 367;
  defaulted-field block lines 487-504 (canary_rng_seed, session_vp_*, sr_*, sr_lookback_by_tf uses
  field(default_factory=lambda: {...})). Every field ABOVE line 487 is non-defaulted — new fields
  must be appended INSIDE the defaulted block (after line 504).

_build_feature_vector — src/intelligence/feature_factory.py line 3417, keyword-only (`*,`).
  Phase 163's 17 None-defaulted kwargs at lines 3445-3461 are the exact analog.
  Call sites: compute() ~3932-3970, compute_batch() ~4560-4570, plus _cold_start_vector at line 4729
  (a direct FeatureVector(...) construction, NOT via _build_feature_vector — needs its own 41 None args).

Persistence — src/intelligence/features/feature_vector_persistence.py:
  _STRUCTURAL_VP_SR_FIELD_NAMES slice, lines 102-113: derive-by-name via
  _ALL_FEATURE_VECTOR_FIELD_NAMES.index(first) : .index(last) + 1.
  Threaded in exactly 2 places: _ALL_COLUMN_NAMES tuple (lines 232-237) and the params tuple
  (line 486, `*(getattr(vector, name) for name in ...)`). _TOTAL_COLUMNS / _INSERT_COLUMNS_SQL /
  _VALUES_PLACEHOLDERS_SQL are all DERIVED from _ALL_COLUMN_NAMES (lines 238-241) — no manual edits.
  Module docstring carries a dated changelog; line 149 carries the "# 181 columns (as of ...)" comment.

Config build sites (both must be updated in the same commit):
  Live  — services/feature_vector_pipeline.py lines 745-753, local `_int()`/`_float()`/`_dict()` wrappers
          defined at lines 631/636.
  Batch — services/backfill_feature_factory.py lines 505-512, direct
          `int(cfg.get_sync("feature.x.y", default))` / `_get_dict_config(...)`.

Hardcoded counts that MUST be updated (grep-verified 2026-07-27, all currently 172 or 181):
  tests/unit/test_feature_factory.py:692           len(fields) == 172
  tests/unit/test_canary_predictors.py:420         len(dataclasses.fields(fv)) == 172
  tests/unit/intelligence/test_feature_factory_p7.py:428  len(FEATURE_VECTOR_DOMAIN) == 172
  tests/unit/services/test_feature_vector_writer.py:249,367,524   == 181
  tests/unit/services/test_feature_vector_writer_column_mapping.py:246,465  == 181 (and :466 indexes params[180])
  tests/unit/services/test_backfill_feature_factory.py:472  == 181
  src/intelligence/schemas.py FeatureVector docstring "Total: 172"
  src/intelligence/features/feature_vector_persistence.py:149 "# 181 columns (as of ...)"
  NOTE: if Phase 164 executed first these baselines will read 208/217 instead. Do NOT hardcode 213/222 —
  read each current value and add exactly 41.

ic_engine drift gate — services/ic_engine.py:4191 (`dataclass_names = {f.name for f in dataclasses.fields(FeatureVector)}`)
  and src/intelligence/feature_registry_service.py:44 (`_REGISTRY_ROW_COUNT = len(dataclasses.fields(FeatureVector))`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 267 — 41 columns + COMMENTs + 41 feature_registry rows</name>
  <files>production/migrations/267_swing_fib_trend_structure_primitives.sql</files>
  <read_first>
    - production/migrations/255_vp_structural_primitives.sql (FULL — the 3-section template, the DOUBLE-PRECISION header note, the feature_registry real-column-set note, and the migration-numbering-collision note)
    - .planning/milestones/v3.1-phases/165-swing-fib-trend-structure-primitives/165-CONTEXT.md (D-01 through D-15; the "Canonical References" block naming group_name='session'/tier='2_theory' and the normalization vocabulary)
    - .planning/milestones/v3.1-phases/165-swing-fib-trend-structure-primitives/165-PATTERNS.md section 1 (migration pattern + normalization tag assignments)
  </read_first>
  <action>
    FIRST re-verify the migration number: run `ls production/migrations/ | sort -V | tail -3`. 265 was the highest at plan-writing time and Phase 164 is sequenced to claim 266 before this phase executes, so 267 is the expected next-free number — but if 267 is taken, use the actual next-free number and update BOTH the filename and every `migration_<N>` string in the file. This project hit migration-number collisions three times (216, 243->255, 259).

    Write Section 1 and Section 2 of production/migrations/267_swing_fib_trend_structure_primitives.sql
    mirroring migration 255's structure. Section 3 (APR seeds) is appended by Task 2 into this same file.

    Header note block (mirroring 255's): state the verified migration number and what it collided with;
    state DOUBLE PRECISION (not `real`); state that ADD COLUMN NULL on the compressed hypertable is
    metadata-only; state that NO raw price levels and NO raw bar indices are persisted (D-02/D-04 —
    the dropped names are swing_high, swing_low, swing_high_idx, swing_low_idx, fib_swing_high,
    fib_swing_low, fib_236, fib_382, fib_500, fib_618, fib_786, nearest_fib_level,
    nearest_session_level, and the raw prior_session_*/overnight_*/weekly_*/asian_session_* levels);
    state that historical backfill of these columns is deferred to the consolidated 163/164/165
    `--compute-only --refresh` pass (todo 176), not this migration.

    Section 1 — `ALTER TABLE feature_vectors ADD COLUMN IF NOT EXISTS <name> DOUBLE PRECISION;` plus one
    `COMMENT ON COLUMN feature_vectors.<name> IS '...formula... NULL when <condition>. Phase 165.';`
    for EVERY one of these exactly-41 names. The names are the binding contract Plans 02-04 must
    produce verbatim (the drift gate is exact-set equality).

    Swing Detection (7, Plan 02, D-01/D-02) — every field NULL when its swing does not exist or ATR <= 0:
      swing_high_dist_atr        (most recent confirmed swing high price - close) / ATR
      swing_low_dist_atr         (close - most recent confirmed swing low price) / ATR
      swing_high_type            +1 higher-high, -1 lower-high; NULL when fewer than 2 confirmed swing highs
      swing_low_type             +1 higher-low, -1 lower-low; NULL when fewer than 2 confirmed swing lows
      swing_pattern              +1 (HH and HL), -1 (LH and LL), 0 mixed; NULL when either type is NULL
      swing_high_age_bars        bars since most recent confirmed swing high
      swing_low_age_bars         bars since most recent confirmed swing low
    Trend Structure (6, Plan 02, D-01) — ALL SIX NULL together when fewer than 2 confirmed swing highs
    OR fewer than 2 confirmed swing lows exist in the lookback window. The COMMENT must spell this out
    explicitly and must state that the archived plugin's numeric placeholders (trend_direction=0.0,
    price_position=0.5) are the bug being fixed, not the contract:
      trend_direction            +1 bullish-leg majority, -1 bearish-leg majority, 0 tie
      trend_strength             dominant-leg fraction, ATR/price-range scaled, clamped [0,1]
      trend_leg_count            count of dominant-direction swing legs
      structure_integrity        1 - overlap_count / max_overlaps, [0,1]
      price_position             (close - recent swing low) / recent swing range, [0,1]  -- D-12: incremental-IC evaluation MUST screen this against va_position (Phase 163) and premium_discount_pct (Phase 164) as well as bb_pct_b/price_percentile/stoch_k
      trend_duration_bars        bars since the start of the current directional swing streak
    Swing Momentum (8, Plan 03, D-03/D-15) — ALL EIGHT NULL together when fewer than
    feature.swing_momentum.max_extremes confirmed extremes exist:
      swing_amplitude_ratio      last swing ATR-amplitude / mean of last 3
      swing_amplitude_expanding  1 when the last 3 amplitudes are monotonically increasing else 0
      swing_amplitude_intensity  linear_ramp(amplitude_ratio, lo, hi) when expanding else 0
      swing_velocity_bars        bars elapsed for the most recent swing leg
      swing_velocity_bias        +1 accelerating, -1 decelerating, 0 stable (D-03: numeric encoding of the archived string enum swing_velocity_trend)
      struct_energy              clamp(amplitude_ratio * speed_factor / energy_divisor, 0, 1)
      struct_accel_bias          +1 HH+HL, -1 LH+LL, 0 mixed
      swing_volume_confirmation  mean volume over the most recent confirmed swing leg / mean volume over the lookback window (D-15)
    Fibonacci Zones (4, Plan 03, D-04/D-05) — ALL FOUR NULL when swing_range <= 0 or ATR <= 0:
      nearest_fib_ratio          which of 0.236/0.382/0.500/0.618/0.786 is nearest to close
      nearest_fib_dist_atr       abs(close - nearest fib level) / ATR
      fib_cluster_strength       fraction of fib-level pairs within the cluster threshold
      in_fib_discount_zone       1 when fib_500 <= close <= fib_786 else 0
    Session Levels (16, Plan 04, D-07/D-08/D-09/D-13) — each NULL when its source level is unavailable
    (cold start / no prior session / no prior ISO week) or ATR <= 0:
      prior_session_high_dist_atr    (prior session high - close) / ATR
      prior_session_low_dist_atr     (close - prior session low) / ATR
      prior_session_close_dist_atr   (close - prior session close) / ATR
      overnight_high_dist_atr        (overnight-block high - close) / ATR
      overnight_low_dist_atr         (close - overnight-block low) / ATR
      overnight_range_pct            (overnight high - overnight low) / overnight low
      opening_gap_pct                (current session open - prior session close) / prior session close
      weekly_pivot_dist_atr          (close - weekly pivot) / ATR
      weekly_r1_dist_atr             (weekly R1 - close) / ATR
      weekly_r2_dist_atr             (weekly R2 - close) / ATR
      weekly_s1_dist_atr             (close - weekly S1) / ATR
      weekly_s2_dist_atr             (close - weekly S2) / ATR
      nearest_level_dist_atr         abs(close - nearest of the 7 session/weekly levels) / ATR
      asian_session_high_dist_atr    (Asian-session high - close) / ATR
      asian_session_low_dist_atr     (close - Asian-session low) / ATR
      gap_filled                     1 when session_low <= prior_session_close <= session_high since the current session open else 0 (D-13)

    Section 2 — 41 `feature_registry` rows, all `group_name='session'`, `tier='2_theory'`,
    `linear_ready=false`, `requires_htf=false`, `status='active'`, `added_phase='165'`,
    `ON CONFLICT (feature_name) DO NOTHING`. `formula_short` restates the COMMENT formula in one line.
    `normalization` assignments (vocabulary confirmed from the live table, per 165-PATTERNS.md):
      'z_scored' (signed, symmetric around zero): swing_high_dist_atr, swing_low_dist_atr,
        opening_gap_pct, prior_session_high_dist_atr, prior_session_low_dist_atr,
        prior_session_close_dist_atr, overnight_high_dist_atr, overnight_low_dist_atr,
        weekly_pivot_dist_atr, weekly_r1_dist_atr, weekly_r2_dist_atr, weekly_s1_dist_atr,
        weekly_s2_dist_atr, asian_session_high_dist_atr, asian_session_low_dist_atr  (15)
      'unbounded_ratio' (non-negative comparable scalars / counts / ages): swing_high_age_bars,
        swing_low_age_bars, trend_leg_count, trend_duration_bars, swing_amplitude_ratio,
        swing_velocity_bars, swing_volume_confirmation, nearest_fib_dist_atr,
        nearest_level_dist_atr, overnight_range_pct  (10)
      'bounded_signed' ({-1,0,1} categoricals): swing_high_type, swing_low_type, swing_pattern,
        trend_direction, swing_velocity_bias, struct_accel_bias  (6)
      'bounded_unsigned' ([0,1] / binary flags): trend_strength, structure_integrity, price_position,
        swing_amplitude_expanding, swing_amplitude_intensity, struct_energy, nearest_fib_ratio,
        fib_cluster_strength, in_fib_discount_zone, gap_filled  (10)
    15 + 10 + 6 + 10 = 41.
  </action>
  <verify>
    <automated>ls production/migrations/ | sort -V | tail -3</automated>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -v ON_ERROR_STOP=1 -f production/migrations/267_swing_fib_trend_structure_primitives.sql</automated>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM information_schema.columns WHERE table_name='feature_vectors' AND data_type='double precision' AND column_name IN ('swing_high_dist_atr','swing_low_dist_atr','swing_high_type','swing_low_type','swing_pattern','swing_high_age_bars','swing_low_age_bars','trend_direction','trend_strength','trend_leg_count','structure_integrity','price_position','trend_duration_bars','swing_amplitude_ratio','swing_amplitude_expanding','swing_amplitude_intensity','swing_velocity_bars','swing_velocity_bias','struct_energy','struct_accel_bias','swing_volume_confirmation','nearest_fib_ratio','nearest_fib_dist_atr','fib_cluster_strength','in_fib_discount_zone','prior_session_high_dist_atr','prior_session_low_dist_atr','prior_session_close_dist_atr','overnight_high_dist_atr','overnight_low_dist_atr','overnight_range_pct','opening_gap_pct','weekly_pivot_dist_atr','weekly_r1_dist_atr','weekly_r2_dist_atr','weekly_s1_dist_atr','weekly_s2_dist_atr','nearest_level_dist_atr','asian_session_high_dist_atr','asian_session_low_dist_atr','gap_filled')" | grep -qx 41</automated>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM information_schema.columns WHERE table_name='feature_vectors' AND column_name IN ('swing_high','swing_low','swing_high_idx','swing_low_idx','fib_swing_high','fib_swing_low','fib_236','fib_382','fib_500','fib_618','fib_786','nearest_fib_level','nearest_session_level','prior_session_high','prior_session_low','prior_session_close','overnight_high','overnight_low','weekly_pivot','weekly_r1','weekly_r2','weekly_s1','weekly_s2','asian_session_high','asian_session_low','swing_velocity_trend')" | grep -qx 0</automated>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM feature_registry WHERE added_phase='165' AND group_name='session' AND tier='2_theory'" | grep -qx 41</automated>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM feature_registry WHERE added_phase='165' AND normalization NOT IN ('z_scored','unbounded_ratio','bounded_signed','bounded_unsigned')" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - Migration file name uses the number actually reported by `ls production/migrations/ | sort -V | tail -3` as next-free, and every `migration_<N>` string inside the file matches that number.
    - Exactly 41 new `feature_vectors` columns exist, all `data_type='double precision'`.
    - Zero columns exist among the 26 forbidden raw-price/raw-index/pre-encoding names listed in verify (D-02/D-03/D-04 enforcement).
    - `SELECT count(*) FROM feature_registry WHERE added_phase='165'` returns 41, all with group_name='session', tier='2_theory', linear_ready=false, requires_htf=false, status='active'.
    - Every one of the 41 columns has a non-null `COMMENT ON COLUMN` stating its formula AND its NULL condition (`SELECT count(*) FROM pg_description d JOIN pg_attribute a ON a.attrelid=d.objoid AND a.attnum=d.objsubid WHERE a.attrelid='feature_vectors'::regclass AND a.attname IN (<41 names>)` returns 41).
    - `price_position`'s COMMENT names D-12's extended near-neighbor screen (va_position and premium_discount_pct).
    - Re-running the migration is idempotent (second `psql -f` invocation exits 0).
  </acceptance_criteria>
  <done>41 new DOUBLE PRECISION columns and 41 matching feature_registry rows exist in the live DB; zero raw-price or raw-index columns; every column documents its formula and NULL condition.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 267 Section 3 — 17 APR keys + FeatureFactoryConfig fields + both config build sites</name>
  <files>production/migrations/267_swing_fib_trend_structure_primitives.sql, src/intelligence/feature_factory.py, services/feature_vector_pipeline.py, services/backfill_feature_factory.py</files>
  <read_first>
    - production/migrations/255_vp_structural_primitives.sql lines 155-251 (the 3-table APR seed with `[conventional]` provenance and the "Not an ML learning target." closing sentence)
    - src/intelligence/feature_factory.py lines 268-300 and 340-368 (FeatureFactoryConfig docstring `field: APR key` list) and lines 480-505 (the defaulted-field block that new fields must be appended INSIDE)
    - src/intelligence/features/i3_structure/swing_detector.py (FULL, 95 lines — `neighbor: int = 5`, `min_lookback: int = 60`, `InputSpec(lookback=120)`)
    - src/intelligence/features/i3_structure/trend_structure.py (FULL, 181 lines — `neighbor: int = 5`, the `/ 5.0` divisor at line 96, the `high[-20:]`/`low[-20:]` slice at line 95)
    - src/intelligence/features/i3_structure/swing_momentum.py (FULL, 258 lines — `_CONFIRM_N=3`, `_MAX_EXTREMES=6`, `_REFERENCE_BARS=20`, `clamp(..., 0.1, 3.0)` at line 120, `/3.0` at line 121, `linear_ramp(..., 1.0, 2.0)` at line 103, `min_lookback: int = 60`)
    - src/intelligence/features/i3_structure/fibonacci_zones.py (FULL, 108 lines — `atr_14 / 2.0` and `swing_range / 20.0` at line 82)
    - src/intelligence/features/i3_structure/session_levels.py lines 22-25 and 164-177 (the ET hour-mask constants 20/4)
    - services/feature_vector_pipeline.py lines 625-660 and 740-760 (`_int`/`_float`/`_dict` wrappers and the FeatureFactoryConfig(...) construction)
    - services/backfill_feature_factory.py lines 495-520 (`cfg.get_sync` block and `_get_dict_config`)
  </read_first>
  <action>
    (a) Append Section 3 to migration 267: the 3-table seed (config_schema + config_state + config_history,
    `changed_by='migration_267'`, all `ON CONFLICT DO NOTHING`) for exactly these 17 keys. Every
    description starts with the `[conventional]` provenance tag (these are the archived plugins' own
    unvalidated defaults, copied verbatim — NOT `[rca_analysis]`), names the owning plan, and ends with
    "Not an ML learning target.":
      feature.swing.pivot_window                       int   5    min 2   max 50   -- shared by swing_detector AND trend_structure (D-06: same find_peaks/find_troughs call, same value; deliberately NOT unified with feature.sr.window=10, which scopes a different S/R clustering operation)
      feature.swing.lookback_bars                      int   120  min 20  max 1000 -- bounded causal window for the shared pivot pass; matches the archived InputSpec(lookback=120)
      feature.trend_structure.atr_strength_divisor     float 5.0  min 0.5 max 50.0
      feature.trend_structure.range_lookback_bars      int   20   min 5   max 200  -- the high[-20:]/low[-20:] slice RESEARCH Finding A found the prior survey missed
      feature.swing_momentum.confirm_n                 int   3    min 1   max 20   -- deliberately separate from feature.swing.pivot_window (D-06 / RESEARCH Finding B: different algorithm, self-contained by design)
      feature.swing_momentum.max_extremes              int   6    min 4   max 20   -- description MUST state it must stay EVEN (represents complete swings; 6 = 3 complete swings)
      feature.swing_momentum.lookback_bars             int   60   min 20 max 1000  -- bounded causal window for _detect_swing_extremes; matches the archived min_lookback/InputSpec(lookback=60)
      feature.swing_momentum.reference_bars            int   20   min 1   max 200
      feature.swing_momentum.speed_factor_min          float 0.1  min 0.01 max 10.0
      feature.swing_momentum.speed_factor_max          float 3.0  min 0.1 max 100.0
      feature.swing_momentum.energy_divisor            float 3.0  min 0.1 max 100.0
      feature.swing_momentum.intensity_ramp_lo         float 1.0  min 0.0 max 100.0
      feature.swing_momentum.intensity_ramp_hi         float 2.0  min 0.0 max 100.0
      feature.fib.cluster_atr_divisor                  float 2.0  min 0.1 max 100.0
      feature.fib.cluster_fallback_divisor             float 20.0 min 0.1 max 1000.0 -- used only when ATR is unavailable
      feature.session_levels.asia_start_et_hour        int   20   min 0   max 23
      feature.session_levels.asia_end_et_hour          int   4    min 0   max 23
    The 5 Fibonacci retracement ratios (0.236/0.382/0.500/0.618/0.786) are APR-EXEMPT — definitional
    mathematical constants, same exemption class as "the 5 in momentum_z_5" per CLAUDE.md. Record that
    exemption in a SQL comment above the feature.fib.* block so a future reader does not "fix" it.

    (b) src/intelligence/feature_factory.py: add these 17 fields to FeatureFactoryConfig, each with a
    Python-level default equal to the seeded value, appended INSIDE the existing defaulted block after
    line 504 (never as bare annotations — a non-defaulted field there would break every direct
    `FeatureFactoryConfig(...)` construction in the test suite; this is the same rationale the existing
    `canary_rng_seed`/`session_vp_*` comment states). Each field carries a trailing
    `# feature.<ns>.<key>` comment matching the existing style. Also add one
    `field_name: APR feature.<ns>.<key>` line per field to the class docstring's parameter list (which
    currently ends at line 367) — that list is what a future reader greps:
      swing_pivot_window: int = 5
      swing_lookback_bars: int = 120
      trend_structure_atr_strength_divisor: float = 5.0
      trend_structure_range_lookback_bars: int = 20
      swing_momentum_confirm_n: int = 3
      swing_momentum_max_extremes: int = 6
      swing_momentum_lookback_bars: int = 60
      swing_momentum_reference_bars: int = 20
      swing_momentum_speed_factor_min: float = 0.1
      swing_momentum_speed_factor_max: float = 3.0
      swing_momentum_energy_divisor: float = 3.0
      swing_momentum_intensity_ramp_lo: float = 1.0
      swing_momentum_intensity_ramp_hi: float = 2.0
      fib_cluster_atr_divisor: float = 2.0
      fib_cluster_fallback_divisor: float = 20.0
      session_levels_asia_start_et_hour: int = 20
      session_levels_asia_end_et_hour: int = 4

    (c) services/feature_vector_pipeline.py: add one `<field>=_int("feature.<ns>.<key>", <default>)` or
    `_float(...)` line per new config field to the FeatureFactoryConfig(...) construction alongside the
    existing `session_vp_*`/`sr_*` block at lines 745-753, and register each key wherever that file
    enumerates its prewarm/threshold key list (grep `feature.sr.window` in that file to find every place
    the Phase 163 keys are listed and mirror all of them).

    (d) services/backfill_feature_factory.py: add the matching
    `<field>=int(cfg.get_sync("feature.<ns>.<key>", <default>))` / `float(...)` lines to the config block
    at lines 505-512.

    No new hardcoded numeric constant may be introduced anywhere by Plans 02-04 — every tunable those
    plans need is seeded here.
  </action>
  <verify>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -v ON_ERROR_STOP=1 -f production/migrations/267_swing_fib_trend_structure_primitives.sql</automated>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM config_state WHERE config_key IN ('feature.swing.pivot_window','feature.swing.lookback_bars','feature.trend_structure.atr_strength_divisor','feature.trend_structure.range_lookback_bars','feature.swing_momentum.confirm_n','feature.swing_momentum.max_extremes','feature.swing_momentum.lookback_bars','feature.swing_momentum.reference_bars','feature.swing_momentum.speed_factor_min','feature.swing_momentum.speed_factor_max','feature.swing_momentum.energy_divisor','feature.swing_momentum.intensity_ramp_lo','feature.swing_momentum.intensity_ramp_hi','feature.fib.cluster_atr_divisor','feature.fib.cluster_fallback_divisor','feature.session_levels.asia_start_et_hour','feature.session_levels.asia_end_et_hour')" | grep -qx 17</automated>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM config_schema WHERE config_key LIKE 'feature.swing%' OR config_key LIKE 'feature.trend_structure.%' OR config_key LIKE 'feature.fib.%' OR config_key LIKE 'feature.session_levels.%' AND description NOT LIKE '%[conventional]%'" | grep -qE '^[0-9]+$'</automated>
    <automated>.venv/bin/python -c "
import dataclasses
from src.intelligence.feature_factory import FeatureFactoryConfig
f={x.name: x for x in dataclasses.fields(FeatureFactoryConfig)}
exp={'swing_pivot_window':5,'swing_lookback_bars':120,'trend_structure_atr_strength_divisor':5.0,'trend_structure_range_lookback_bars':20,'swing_momentum_confirm_n':3,'swing_momentum_max_extremes':6,'swing_momentum_lookback_bars':60,'swing_momentum_reference_bars':20,'swing_momentum_speed_factor_min':0.1,'swing_momentum_speed_factor_max':3.0,'swing_momentum_energy_divisor':3.0,'swing_momentum_intensity_ramp_lo':1.0,'swing_momentum_intensity_ramp_hi':2.0,'fib_cluster_atr_divisor':2.0,'fib_cluster_fallback_divisor':20.0,'session_levels_asia_start_et_hour':20,'session_levels_asia_end_et_hour':4}
missing=[k for k in exp if k not in f]
assert not missing, missing
bad=[k for k,v in exp.items() if f[k].default != v]
assert not bad, bad
print('ok 17 config fields')"</automated>
    <automated>.venv/bin/python -c "
import re
for p in ('services/feature_vector_pipeline.py','services/backfill_feature_factory.py'):
    src=open(p).read()
    for k in ('feature.swing.pivot_window','feature.swing.lookback_bars','feature.trend_structure.atr_strength_divisor','feature.trend_structure.range_lookback_bars','feature.swing_momentum.confirm_n','feature.swing_momentum.max_extremes','feature.swing_momentum.lookback_bars','feature.swing_momentum.reference_bars','feature.swing_momentum.speed_factor_min','feature.swing_momentum.speed_factor_max','feature.swing_momentum.energy_divisor','feature.swing_momentum.intensity_ramp_lo','feature.swing_momentum.intensity_ramp_hi','feature.fib.cluster_atr_divisor','feature.fib.cluster_fallback_divisor','feature.session_levels.asia_start_et_hour','feature.session_levels.asia_end_et_hour'):
        assert k in src, (p,k)
print('ok both config sites')"</automated>
    <automated>.venv/bin/ruff check src/intelligence/feature_factory.py services/feature_vector_pipeline.py services/backfill_feature_factory.py</automated>
  </verify>
  <acceptance_criteria>
    - All 17 keys exist in `config_schema`, `config_state` (version 1) and `config_history` (changed_by='migration_267'); re-running the migration is idempotent.
    - Every one of the 17 `config_schema.description` values contains `[conventional]` and ends with "Not an ML learning target."; `feature.swing_momentum.max_extremes`'s description states it must stay even.
    - A SQL comment above the `feature.fib.*` block records that the 5 Fibonacci ratios themselves are APR-exempt definitional constants.
    - `FeatureFactoryConfig` carries all 17 fields with defaults exactly equal to the seeded values, and each appears in the class docstring's `field: APR key` list.
    - Both `services/feature_vector_pipeline.py` and `services/backfill_feature_factory.py` reference all 17 key strings.
    - ruff clean on the three edited Python files.
  </acceptance_criteria>
  <done>Every tunable Plans 02-04 need is APR-backed under feature.swing.*/feature.trend_structure.*/feature.swing_momentum.*/feature.fib.*/feature.session_levels.*, carried on FeatureFactoryConfig with matching defaults, and wired from ConfigService in both the live and batch entrypoints.</done>
</task>

<task type="auto">
  <name>Task 3: FeatureVector 41 nullable fields + domain registry + _build_feature_vector/_cold_start placeholders + persistence slice + test-count blast radius</name>
  <files>src/intelligence/schemas.py, src/intelligence/feature_factory.py, src/intelligence/features/feature_vector_persistence.py, tests/unit/test_feature_factory.py, tests/unit/test_canary_predictors.py, tests/unit/intelligence/test_feature_factory_p7.py, tests/unit/services/test_feature_vector_writer.py, tests/unit/services/test_feature_vector_writer_column_mapping.py, tests/unit/services/test_backfill_feature_factory.py</files>
  <read_first>
    - src/intelligence/schemas.py lines 1204-1291 (live FeatureVector docstring group table + "Total: 172" line + the Phase 163 session block, the `float | None` precedent) AND lines 1485-1511 (the defaulted canary + cross-sectional tail that new non-defaulted fields must precede)
    - src/intelligence/feature_factory.py lines 62-106 (FEATURE_VECTOR_DOMAIN), 3417-3465 (_build_feature_vector keyword-only signature + Phase 163's None-defaulted block), 3930-3975 (compute() call site), 4550-4600 (compute_batch() call site), 4729-4790 (_cold_start_vector direct FeatureVector(...) construction)
    - src/intelligence/features/feature_vector_persistence.py lines 1-50 (module docstring changelog), 73-114 (the 3 derive-by-name slices), 145-245 (column-name tuple + _ALL_COLUMN_NAMES + derived SQL), 465-495 (params tuple tail)
    - tests/unit/intelligence/test_feature_factory_p7.py lines 415-432 (FEATURE_VECTOR_DOMAIN completeness gate)
    - services/ic_engine.py lines 4185-4200 (registry-vs-dataclass drift gate)
  </read_first>
  <action>
    (a) src/intelligence/schemas.py — add all 41 fields to the LIVE `FeatureVector` dataclass (the one
    starting at line 1204; NEVER the dead `I3Structure` Pydantic model at lines 228-341, which defines
    swing_high/trend_direction/price_position by the same names and is exactly the trap 165-PATTERNS.md
    warns about). Insert them as ONE contiguous block immediately BEFORE the
    `# Canary / Control Predictors` comment block — a non-defaulted field cannot follow a defaulted one,
    and contiguity is required by the persistence slice technique. Every field is `float | None` with NO
    default. Sub-section the block with five comments in this exact field order:
      `# Swing Detection (7, Phase 165 Plan 02, D-01/D-02)`
        swing_high_dist_atr, swing_low_dist_atr, swing_high_type, swing_low_type, swing_pattern,
        swing_high_age_bars, swing_low_age_bars
      `# Trend Structure (6, Phase 165 Plan 02, D-01 nullable-fix)`
        trend_direction, trend_strength, trend_leg_count, structure_integrity, price_position,
        trend_duration_bars
      `# Swing Momentum (8, Phase 165 Plan 03, D-03/D-15)`
        swing_amplitude_ratio, swing_amplitude_expanding, swing_amplitude_intensity,
        swing_velocity_bars, swing_velocity_bias, struct_energy, struct_accel_bias,
        swing_volume_confirmation
      `# Fibonacci Zones (4, Phase 165 Plan 03, D-04/D-05)`
        nearest_fib_ratio, nearest_fib_dist_atr, fib_cluster_strength, in_fib_discount_zone
      `# Session Levels (16, Phase 165 Plan 04, D-07..D-09/D-13)`
        prior_session_high_dist_atr, prior_session_low_dist_atr, prior_session_close_dist_atr,
        overnight_high_dist_atr, overnight_low_dist_atr, overnight_range_pct, opening_gap_pct,
        weekly_pivot_dist_atr, weekly_r1_dist_atr, weekly_r2_dist_atr, weekly_s1_dist_atr,
        weekly_s2_dist_atr, nearest_level_dist_atr, asian_session_high_dist_atr,
        asian_session_low_dist_atr, gap_filled
    Add matching rows to the docstring's binding group table and update the `Total:` line by adding
    exactly 41 to whatever it currently reads (172 if Phase 164 has not landed, 208 if it has — read
    the file, do not assume). The docstring's leading sentence and the "Non-optional fields typed float,
    no defaults" claim must be updated to note that this block is `float | None` by design (D-01: a
    numeric placeholder for "insufficient data" is the todo-153 failure mode).

    (b) src/intelligence/feature_factory.py — add 41 `"<field>": "structural",` entries to
    FEATURE_VECTOR_DOMAIN, appended after the Phase 163 Support/Resistance block (line 106), with the
    same five sub-section comments. A field missing from this dict is silently invisible to IC
    measurement even though its column persists.

    (c) src/intelligence/feature_factory.py — add all 41 as `<field>: float | None = None,` kwargs to
    `_build_feature_vector`'s keyword-only signature (mirroring Phase 163's block at lines 3445-3461,
    with a comment stating the values are placeholders until Plans 02-04), pass them through to the
    FeatureVector(...) construction inside that function, and explicitly pass `<field>=None` at BOTH
    call sites (compute() and compute_batch()) so the intent is visible in the diff rather than relying
    on the default. Also add all 41 as explicit `None` arguments to `_cold_start_vector`'s direct
    `FeatureVector(...)` construction at line 4729 — that is a second construction site that does NOT
    go through `_build_feature_vector` and will raise TypeError without them.

    (d) src/intelligence/features/feature_vector_persistence.py — add a fourth derive-by-name slice
    `_SWING_FIB_TREND_FIELD_NAMES = _ALL_FEATURE_VECTOR_FIELD_NAMES[_ALL_FEATURE_VECTOR_FIELD_NAMES.index("swing_high_dist_atr") : _ALL_FEATURE_VECTOR_FIELD_NAMES.index("gap_filled") + 1]`
    immediately after `_STRUCTURAL_VP_SR_FIELD_NAMES`. Never hand-type the tuple. Append it to
    `_ALL_COLUMN_NAMES` (after `_STRUCTURAL_VP_SR_FIELD_NAMES`) and add the matching
    `*(getattr(vector, name) for name in _SWING_FIB_TREND_FIELD_NAMES),` line at the end of the params
    tuple. `_TOTAL_COLUMNS`/`_INSERT_COLUMNS_SQL`/`_VALUES_PLACEHOLDERS_SQL` are all derived and need no
    manual edit. Update the `# NNN columns (as of ...)` comment at line 149 and add a dated changelog
    entry to the module docstring naming migration 267 and the 41 new columns.

    (e) Test-count blast radius — for EACH of these assertions, read the current asserted integer and
    replace it with current + 41 (do NOT hardcode a precomputed number; Phase 164 may or may not have
    landed first, which shifts every baseline by 36):
      tests/unit/test_feature_factory.py:692, tests/unit/test_canary_predictors.py:420,
      tests/unit/intelligence/test_feature_factory_p7.py:428 (all currently 172)
      tests/unit/services/test_feature_vector_writer.py:249,367,524,
      tests/unit/services/test_feature_vector_writer_column_mapping.py:246,465,
      tests/unit/services/test_backfill_feature_factory.py:472 (all currently 181)
    test_feature_vector_writer_column_mapping.py:457-466 asserts `params[180] == 61.17` is the LAST
    element (sr_level_count sentinel). That index is now mid-tuple — re-point that assertion at the new
    last index (`len(params) - 1`) with a `gap_filled` sentinel, and keep a separate assertion pinning
    sr_level_count at its own (still index 180) position so both boundaries stay guarded. Update each
    touched test's docstring to explain the new count arithmetic rather than leaving a bare number.
    Any direct `FeatureVector(...)` construction that breaks (grep `FeatureVector(` across tests/ and
    src/) needs the 41 new `None` kwargs added.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import dataclasses
from src.intelligence.schemas import FeatureVector
f=[x.name for x in dataclasses.fields(FeatureVector)]
new=['swing_high_dist_atr','swing_low_dist_atr','swing_high_type','swing_low_type','swing_pattern','swing_high_age_bars','swing_low_age_bars','trend_direction','trend_strength','trend_leg_count','structure_integrity','price_position','trend_duration_bars','swing_amplitude_ratio','swing_amplitude_expanding','swing_amplitude_intensity','swing_velocity_bars','swing_velocity_bias','struct_energy','struct_accel_bias','swing_volume_confirmation','nearest_fib_ratio','nearest_fib_dist_atr','fib_cluster_strength','in_fib_discount_zone','prior_session_high_dist_atr','prior_session_low_dist_atr','prior_session_close_dist_atr','overnight_high_dist_atr','overnight_low_dist_atr','overnight_range_pct','opening_gap_pct','weekly_pivot_dist_atr','weekly_r1_dist_atr','weekly_r2_dist_atr','weekly_s1_dist_atr','weekly_s2_dist_atr','nearest_level_dist_atr','asian_session_high_dist_atr','asian_session_low_dist_atr','gap_filled']
i=f.index(new[0])
assert f[i:i+41]==new, ('not contiguous / wrong order', f[i:i+41])
tm={x.name:x.type for x in dataclasses.fields(FeatureVector)}
bad=[n for n in new if 'None' not in str(tm[n])]
assert not bad, ('not nullable', bad)
assert f.index('gap_filled') < f.index('canary_noise_gaussian'), 'new block must precede the defaulted canary block'
print('ok 41 contiguous nullable fields')"</automated>
    <automated>.venv/bin/python -c "
import dataclasses
from src.intelligence.feature_factory import FEATURE_VECTOR_DOMAIN
from src.intelligence.schemas import FeatureVector
fv={x.name for x in dataclasses.fields(FeatureVector)}
assert set(FEATURE_VECTOR_DOMAIN)==fv, ('domain drift', set(FEATURE_VECTOR_DOMAIN)^fv)
new=['swing_high_dist_atr','trend_direction','swing_velocity_bias','nearest_fib_ratio','gap_filled','price_position','swing_volume_confirmation']
assert all(FEATURE_VECTOR_DOMAIN[n]=='structural' for n in new)
print('ok domain complete')"</automated>
    <automated>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT string_agg(feature_name, ',' ORDER BY feature_name) FROM feature_registry" > /tmp/registry_165.txt && .venv/bin/python -c "
import dataclasses
from src.intelligence.schemas import FeatureVector
reg=set(open('/tmp/registry_165.txt').read().strip().split(','))
dc={f.name for f in dataclasses.fields(FeatureVector)}
assert reg==dc, f'DRIFT registry-only={sorted(reg-dc)} dataclass-only={sorted(dc-reg)}'
print('ok drift gate')"</automated>
    <automated>.venv/bin/python -c "
from src.intelligence.features.feature_vector_persistence import _SWING_FIB_TREND_FIELD_NAMES, _ALL_COLUMN_NAMES, _TOTAL_COLUMNS
assert len(_SWING_FIB_TREND_FIELD_NAMES)==41, len(_SWING_FIB_TREND_FIELD_NAMES)
assert _ALL_COLUMN_NAMES[-41:]==_SWING_FIB_TREND_FIELD_NAMES
assert _TOTAL_COLUMNS==len(_ALL_COLUMN_NAMES)
assert len(set(_ALL_COLUMN_NAMES))==_TOTAL_COLUMNS, 'duplicate column name'
print('ok persistence slice', _TOTAL_COLUMNS)"</automated>
    <automated>.venv/bin/pytest tests/unit/test_feature_vector_persistence_completeness.py tests/unit/test_feature_factory.py tests/unit/test_canary_predictors.py tests/unit/intelligence/test_feature_factory_p7.py tests/unit/services/test_feature_vector_writer.py tests/unit/services/test_feature_vector_writer_column_mapping.py tests/unit/services/test_backfill_feature_factory.py -x -q</automated>
    <automated>.venv/bin/pytest tests/unit/ -q</automated>
    <automated>.venv/bin/ruff check src/intelligence/schemas.py src/intelligence/feature_factory.py src/intelligence/features/feature_vector_persistence.py</automated>
  </verify>
  <acceptance_criteria>
    - `dataclasses.fields(FeatureVector)` contains the 41 new names as one contiguous run in the documented order, every one typed `float | None`, all positioned before `canary_noise_gaussian`.
    - `set(FEATURE_VECTOR_DOMAIN) == {f.name for f in dataclasses.fields(FeatureVector)}` and all 41 new entries map to `"structural"`.
    - `feature_registry.feature_name` set equals the dataclass field-name set (the exact gate `ic_engine.py:4191` and `feature_registry_service.py:44` enforce at startup).
    - `_SWING_FIB_TREND_FIELD_NAMES` has length 41, is the final segment of `_ALL_COLUMN_NAMES`, and `_ALL_COLUMN_NAMES` has no duplicates; `_TOTAL_COLUMNS` equals the previous total + 41.
    - `_cold_start_vector` and both `_build_feature_vector` call sites construct a `FeatureVector` without TypeError (covered by the full unit run).
    - Every previously-hardcoded field/param count assertion equals its prior value + 41; the column-mapping last-element sentinel assertion now targets `gap_filled` at `len(params)-1` while sr_level_count stays pinned at its own index.
    - `.venv/bin/pytest tests/unit/ -q` is fully green.
  </acceptance_criteria>
  <done>All 41 columns exist end-to-end (DB, registry, dataclass, domain, persistence INSERT) with zero drift; the pipeline constructs a FeatureVector on every path with NULL placeholders; the whole unit suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| market_data_ohlcv_tradeable -> FeatureFactory | Already-validated internal OHLCV (price-sanity guarded upstream by `price_sanity_status`); no external or untrusted input crosses in this phase. |
| migration 267 -> live TimescaleDB `feature_vectors` hypertable | DDL executed by an operator against a compressed hypertable holding the whole training corpus. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-01 | Tampering (silent wrong data) | New columns persisting a fake-but-numeric "no signal" value, the exact todo-153 / D-01 failure mode | mitigate | All 41 fields typed `float | None` with NO default; migration COMMENTs state each NULL condition; Plans 02-04 use all-`None` fallback dicts (never `_SR_FALLBACK`'s zero pattern) and add a dedicated nullability regression test. |
| T-165-02 | Information disclosure (cross-symbol leakage) | Persisting raw price levels or raw bar indices breaks cross-sectional IC (non-stationary, non-comparable) | mitigate | D-02/D-04 enforced by a verify query asserting zero columns exist among the 26 forbidden raw-price/raw-index names. |
| T-165-03 | Denial of service (pipeline crash) | Adding non-defaulted `FeatureVector` fields without threading them through every construction site raises TypeError on every bar | mitigate | All 41 threaded through `_build_feature_vector`, both its call sites, and `_cold_start_vector`; full `tests/unit/` run is a verify gate. |
| T-165-04 | Repudiation (untracked config change) | A tunable landing as an inline magic number instead of an APR key | mitigate | 17 keys seeded via the 3-table `config_schema`/`config_state`/`config_history` pattern with `changed_by='migration_267'` and `[conventional]` provenance; Plans 02-04 forbidden from introducing new numeric constants. |
| T-165-05 | Denial of service (corpus downtime) | DDL against a compressed hypertable forcing chunk decompression/rewrite | accept | `ADD COLUMN ... DOUBLE PRECISION` with a NULL default is metadata-only on TimescaleDB (migration 197/206/216/255 precedent); no `ALTER COLUMN TYPE`, no table rewrite, no backfill UPDATE in this migration. |
| T-165-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero new packages. numpy, pandas, `find_peaks`/`find_troughs`, `clamp`, `linear_ramp`, `get_atr`, `_et_from_utc` are all already-live in-repo dependencies. No install task exists in this phase. |
</threat_model>

<verification>
- Migration 267 applies cleanly and idempotently against the live DB; 41 columns present as `double precision`; 41 `feature_registry` rows with `added_phase='165'`.
- Zero forbidden raw-price / raw-index / pre-encoding column names exist.
- `dataclasses.fields(FeatureVector)` name set == `SELECT feature_name FROM feature_registry` set (the `ic_engine` startup gate would pass).
- All 41 new dataclass fields are `float | None`, contiguous, and precede the defaulted canary block.
- 17 `feature.*` APR keys resolve from `config_state`; `FeatureFactoryConfig` carries all 17 with matching defaults; both config build sites reference every key.
- `.venv/bin/pytest tests/unit/ -q` green with every field/param count assertion bumped by exactly 41.
</verification>

<success_criteria>
The full Phase 165 data contract exists end-to-end with zero drift: 41 new ATR-distance / bounded /
count / categorical columns (never a raw price, never a raw bar index) across `feature_vectors`,
`feature_registry`, `FeatureVector`, `FEATURE_VECTOR_DOMAIN` and the persistence INSERT; all tunables
APR-backed under five new namespaces and wired from `ConfigService` in both the live and batch
entrypoints; the pipeline constructs successfully on every code path with `None` placeholders — ready
for Plans 02-04 to replace those placeholders with real computed values without touching schema again.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/165-swing-fib-trend-structure-primitives/165-01-SUMMARY.md` when done.
</output>
