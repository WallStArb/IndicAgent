---
phase: 165-swing-fib-trend-structure-primitives
plan: 04
type: execute
wave: 4
depends_on: ["165-03"]
files_modified:
  - src/intelligence/feature_cache.py
  - src/intelligence/feature_factory.py
  - services/feature_vector_pipeline.py
  - tests/unit/intelligence/test_session_levels_cache.py
autonomous: true
requirements: ["D-06", "D-07", "D-08", "D-09", "D-13"]

must_haves:
  truths:
    - "Session boundaries are detected from the bar TIMESTAMP (ET calendar/session-day transition), never from a bar count — _SESSION_BARS=390 / _WEEK_BARS=1950 / _OVERNIGHT_BARS=60 exist nowhere in the v3 tree (D-07)"
    - "FeatureCache carries prior-session high/low/close, running session high/low/open, a frozen overnight high/low, a latched gap_filled flag, Asian-block high/low, and prior-COMPLETED-week high/low/close — every one None until its first real boundary transition, never a manufactured default (D-01 discipline)"
    - "The weekly high/low/close accumulator lives inside update_wk_vwap()'s existing _wk_year_week reset block — no second parallel weekly-boundary mechanism exists (D-09)"
    - "update_session_levels() is invoked at all 3 required call sites: compute_batch()'s per-bar loop, the live per-bar handler, and the warm-up replay block (the T-164-07 cold-start gap Phase 164 had to fix retroactively)"
    - "No new FeatureCache field collides with Phase 164's AMD accumulator (_overnight_high/_overnight_low/_overnight_day) or Phase 163's session-VP accumulator (_session_day/_sess_bars)"
  artifacts:
    - path: "src/intelligence/feature_cache.py"
      provides: "22 new _sl_*/_wk_*/_prior_wk_* state fields, update_session_levels() timestamp-driven mutator, and update_wk_vwap()'s weekly H/L/C + prior-week snapshot extension"
      contains: "def update_session_levels"
    - path: "tests/unit/intelligence/test_session_levels_cache.py"
      provides: "mutator unit tests: session rollover, overnight freeze, gap-fill latch, Asian block cycle, weekly rollover snapshot, cold-start None, no-collision guard"
      contains: "test_session_levels_rollover_snapshots_prior_session"
  key_links:
    - from: "src/intelligence/feature_cache.py update_session_levels"
      to: "src.core.bar_accumulator._RTH_OPEN_ET / _RTH_CLOSE_ET via _et_from_utc"
      via: "DST-correct ET wall-clock comparison, identical to update_session_vp's session_day derivation"
      pattern: "_RTH_CLOSE_ET"
    - from: "services/feature_vector_pipeline.py + src/intelligence/feature_factory.py compute_batch"
      to: "FeatureCache.update_session_levels"
      via: "per-bar call BEFORE compute(), alongside update_session_vp / update_overnight_range"
      pattern: "update_session_levels\\("
    - from: "FeatureCache.update_wk_vwap"
      to: "_prior_wk_high / _prior_wk_low / _prior_wk_close"
      via: "snapshot inside the existing `if year_week != self._wk_year_week:` reset block (D-09)"
      pattern: "_prior_wk_close"
---

<objective>
Build the timestamp-driven session/weekly state layer `session_levels.py`'s 16 columns need, replacing
the archived plugin's bar-count session detection (`_SESSION_BARS=390` etc., wrong on every timeframe
v3 actually runs — D-07) with a `FeatureCache` mutator following Phase 163's `update_session_vp()`
pattern, and extending `update_wk_vwap()`'s existing ISO-week reset block with weekly high/low/close
rather than building a second weekly-boundary mechanism (D-09).

Purpose: this is the one genuinely new engineering shape in Phase 165. The other four ported files are
stateless `compute()`-time pure functions; `session_levels.py` needs cross-bar accumulated state with
correct boundary semantics. Getting the state layer right and proven in isolation — before any
`FeatureVector` field consumes it — is what keeps Plan 05's derivation a thin, verifiable mapping.

Output: 22 new `FeatureCache` state fields, one new `update_session_levels()` mutator, an extended
`update_wk_vwap()`, all 3 call sites wired in the same commit, and a dedicated mutator test file.
This plan adds ZERO `FeatureVector` values — Plan 05 derives all 16 columns from the state built here.
</objective>

<source_coverage_audit>
Plan 04's slice of the phase-wide audit recorded in full in 165-01-PLAN.md. Items covered HERE:

GOAL (ROADMAP Phase 165): the state half of `session_levels.py`'s 16-column rewrite, explicitly scoped
by ROADMAP as "built as its own plan within this phase... real session-boundary rewrite... every
primitive it needs already exists from Phase 163". COVERED by Tasks 1-3.

REQ: no `.planning/REQUIREMENTS.md` exists. 165-CONTEXT.md's D-XX decision IDs are the requirement set.

RESEARCH (165-RESEARCH.md):
- Section 1 `session_levels.py` (the bar-count bug is real but NARROWER than the ROADMAP survey
  implied: the Asian-session sub-feature is already timestamp-based and correct; only
  prior_session_*/overnight_*/weekly_* need the rewrite) -> Task 1 rewrites the three broken
  sub-concepts and ports the Asian ET-hour masking as-is into an accumulator. COVERED.
- Section 3 (`ctx_SessionContext` is a TIME-of-day plugin, not a substitute for this PRICE-pivot
  concept; not wired into feature_factory.py) -> recorded in Task 1's read_first so the two are not
  confused. COVERED as documentation.

CONTEXT (165-CONTEXT.md):
- D-06 `feature.session_levels.asia_start_et_hour`/`asia_end_et_hour` (seeded by Plan 01) consumed in
  Task 1; no bar-count APR key is introduced (that is the bug being removed). COVERED.
- D-07 bar-count bug fixed; Asian sub-feature not rewritten. COVERED (Task 1).
- D-08 session-boundary-transition mutator following update_session_vp()'s pattern. COVERED (Task 1).
- D-09 extend the existing ISO-week accumulator, do not build a second. COVERED (Task 1c).
- D-13 `gap_filled` off the running session high/low the D-08 mutator already tracks — zero new state.
  COVERED (Task 1b).
- Claude's Discretion (exact FeatureCache internal state shape) — exercised in Task 1 and documented.

Exclusions (NOT gaps — explicit CONTEXT.md Deferred Ideas): `bars_since_gap_fill` velocity companion;
`nearest_session_level_type` categorical companion. The 16 `FeatureVector` columns themselves are
Plan 05's scope, not omitted.
</source_coverage_audit>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v3.1-phases/165-swing-fib-trend-structure-primitives/165-CONTEXT.md
@.planning/milestones/v3.1-phases/165-swing-fib-trend-structure-primitives/165-RESEARCH.md
@.planning/milestones/v3.1-phases/165-swing-fib-trend-structure-primitives/165-PATTERNS.md
@.planning/milestones/v3.1-phases/165-swing-fib-trend-structure-primitives/165-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Verified live at plan-writing time (2026-07-28, AFTER Phase 164 landed). Re-anchor on the named
     symbols, not the integers, if 164-era code shifts again. -->

Already imported at the top of src/intelligence/feature_cache.py:
  line 21: from datetime import UTC, datetime, timedelta
  line 26: from src.core.bar_accumulator import _RTH_OPEN_ET
  line 27: from src.intelligence.context.session_context import _et_from_utc
`_RTH_CLOSE_ET` is NOT yet imported — src/core/bar_accumulator.py line 41 defines
`_RTH_CLOSE_ET = time(16, 0)` (and line 40 `_RTH_OPEN_ET = time(9, 30)`, line 39
`_ET = ZoneInfo("America/New_York")`). Extend the existing import, do not re-derive the constant.

Mutator template A — session-boundary reset, FeatureCache.update_session_vp()
(src/intelligence/feature_cache.py lines 209-298). The exact DST-correct session-day derivation to
COPY (lines 235-249), including its own comment explaining why ET wall-clock is used here rather than
the `feature.session.ny_start_utc_hour` APR value:
    ts = bar_ts if bar_ts.tzinfo is not None else bar_ts.replace(tzinfo=UTC)
    et = _et_from_utc(ts)
    et_date = et.date()
    session_day = et_date if et.time() >= _RTH_OPEN_ET else et_date - timedelta(days=1)
    if session_day != self._session_day:
        self._sess_bars = []
        self._session_day = session_day
  NOTE the key consequence of this derivation: post-close bars (16:00-23:59 ET of day D) and pre-open
  bars (00:00-09:29 ET of day D+1) BOTH map to session_day D. The overnight block is therefore exactly
  the set of non-RTH bars belonging to one session_day, and it completes precisely at the session_day
  rollover. Task 1 depends on this property.

Mutator template B — ISO-week accumulator, FeatureCache.update_wk_vwap()
(src/intelligence/feature_cache.py lines 184-207). D-09's mandate is to EXTEND this reset block:
    iso = bar_ts.isocalendar()
    year_week = (iso.year, iso.week)
    if year_week != self._wk_year_week:
        self._wk_tp_vol_sum = 0.0
        self._wk_vol_sum = 0.0
        self._wk_year_week = year_week
    ...
  Called from advance_bar() (line 327), which runs AFTER compute() — see Task 1(c) for the one-bar
  conservative lag this implies and why it is accepted rather than worked around.

Mutator template C — cycle-key accumulator with per-cycle flag reset,
FeatureCache.update_overnight_range() (src/intelligence/feature_cache.py lines 385-473, Phase 164).
This is the shape to copy for the ASIAN-block accumulator (a window that straddles midnight):
    cycle_day = ts.date() if hour >= accum_start else (ts.date() - timedelta(days=1))
    if cycle_day != self._overnight_day:
        self._overnight_high = None
        self._overnight_low = None
        self._overnight_day = cycle_day
        ... per-cycle flags cleared ...

NAME-COLLISION HAZARD (the single most important thing to get right in Task 1). FeatureCache ALREADY
owns these names, for two DIFFERENT concepts, and neither may be reused or clobbered:
  _overnight_high / _overnight_low / _overnight_day   (lines 103-105) — Phase 164 AMD accumulation
      window, keyed on UTC hour >= feature.smc.amd.accum_start_utc_hour (default 20:00 UTC).
  _session_day / _sess_bars                            (lines 111-112) — Phase 163 session-VP
      accumulator, keyed on the ET session day.
  Sharing `_session_day` between two mutators would silently break BOTH: whichever runs first flips the
  key, the second sees "no change" and never resets. Every field this plan adds carries an `_sl_`
  prefix (session-levels) or the `_wk_`/`_prior_wk_` weekly prefix. This plan's overnight concept
  (non-RTH bars between one session close and the next session open, ET-derived) is a genuinely
  different window from Phase 164's (20:00-24:00 UTC accumulation phase) — they are tracked separately
  by design, and Task 1's docstring must say why so a future reader does not "deduplicate" them.

Existing internal-state field convention — src/intelligence/feature_cache.py lines 84-127: every
non-FeatureVector intermediate is `field(default=..., repr=False)` and `_`-prefixed, e.g.
  _sess_poc: float | None = field(default=None, repr=False)
  _session_day: object = field(default=None, repr=False)

Call sites that must ALL be updated in the same commit (Phase 164's T-164-07 is the precedent for
what happens when one is missed — `update_overnight_range()` shipped in Plan 01 and sat uninvoked
until Plan 04 wired it):
  1. src/intelligence/feature_factory.py compute_batch() per-bar loop, lines 5540-5553:
         cache.update_session_vp(bar_ts, high_, low_, close_, vol_, config)
         cache.update_overnight_range(bar_ts, high_, low_, config)
     — both fire on EVERY bar including warm-up (before the `if i < warm_up_bars: ... continue` at
     line 5556). `open_` is read later at line 5573; the new call needs it read earlier or read
     directly from `bar["open"]`.
  2. services/feature_vector_pipeline.py `_process_bar_compute`, lines 1172-1181:
         cache.update_session_vp(bar.ts, bar.high, bar.low, bar.close, float(bar.volume), config)
         cache.update_overnight_range(bar.ts, bar.high, bar.low, config)
     — immediately BEFORE `FeatureFactory.compute(...)` at line 1184.
  3. services/feature_vector_pipeline.py `_get_cache` warm-up replay, lines 202-223: three sequential
     `for bar in buffered:` loops replaying update_wk_vwap / update_session_vp /
     update_overnight_range. `buffered = list(self._bar_history.get(symbol, tf))[:-1]` — the most
     recent bar is excluded because it is the bar currently being processed.
     `buffered` elements are `BarMessage` (src/core/schemas/bar_message.py line 51) and DO carry
     `.open`, `.high`, `.low`, `.close`, `.volume`, `.ts`.

APR config fields available from Plan 01 (FeatureFactoryConfig, both defaulted — do not re-add):
  config.session_levels_asia_start_et_hour: int = 20   # feature.session_levels.asia_start_et_hour
  config.session_levels_asia_end_et_hour: int = 4      # feature.session_levels.asia_end_et_hour
No bar-count APR key exists or may be added — removing bar counts IS D-07's fix.

CONTEXT-ONLY, NOT A PORT SOURCE: src/intelligence/context/session_context.py (`ctx_SessionContext`,
328 lines) is a TIME-of-day session plugin (killzones, opening-range, power-hour gradients). It is not
wired into feature_factory.py and is NOT a substitute for this plan's PRICE-pivot concept. Only its
`_et_from_utc` helper is used (already imported).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FeatureCache session/weekly state — 22 new fields, update_session_levels() timestamp mutator, update_wk_vwap() ISO-week H/L/C extension</name>
  <files>src/intelligence/feature_cache.py</files>
  <behavior>
    - Cold cache (no call yet): every new `_sl_*`, `_wk_high/_wk_low/_wk_close` and `_prior_wk_*` field reads None (or 0.0 for the `_sl_gap_filled` flag) — never a manufactured price.
    - Feeding bars entirely within one ET session day: `_sl_session_high`/`_sl_session_low` track the running extremes, `_sl_session_open` stays the open of the FIRST bar of that session day, `_sl_prior_session_*` stay None (no session has completed yet).
    - Crossing an ET session-day boundary: the completed session's running high/low/close are snapshotted into `_sl_prior_session_high`/`_low`/`_close`, and the running fields reset to the new session's first bar.
    - Overnight freeze: non-RTH bars (ET wall-clock before 09:30 or at/after 16:00) accumulate into `_sl_on_acc_high`/`_sl_on_acc_low`; at the next session-day rollover those values move into `_sl_overnight_high`/`_sl_overnight_low` and the accumulators clear. A session day with zero non-RTH bars leaves the frozen overnight fields at their prior value (None on cold start), never at a fabricated number.
    - gap_filled latch: once `_sl_session_low <= _sl_prior_session_close <= _sl_session_high` holds within a session day, `_sl_gap_filled` is 1.0 and stays 1.0 for the rest of that session day; it resets to 0.0 at the next rollover. It stays 0.0 for the whole session when `_sl_prior_session_close` is None.
    - Asian block: bars whose ET hour is >= config.session_levels_asia_start_et_hour OR < config.session_levels_asia_end_et_hour accumulate into `_sl_asia_high`/`_sl_asia_low`, keyed on the ET date the block STARTED (today when hour >= start, yesterday otherwise); bars outside the window leave the accumulator untouched. A new block resets both to None before accumulating.
    - Weekly: within one ISO week, `_wk_high`/`_wk_low` track running extremes and `_wk_close` holds the latest close. At the ISO-week boundary those three are snapshotted into `_prior_wk_high`/`_prior_wk_low`/`_prior_wk_close` and the running trio resets. `_prior_wk_*` stay None until a first full week has rolled over.
    - `update_wk_vwap()`'s existing behaviour is unchanged: `above_wk_vwap` and the VWAP accumulators produce identical values before and after this task on the same bar sequence.
    - No new field name collides with `_overnight_high`/`_overnight_low`/`_overnight_day`/`_session_day`/`_sess_bars`.
    - `update_session_levels()` never raises on any input (naive datetime, zero prices, a single bar, out-of-order-free but sparse history).
  </behavior>
  <read_first>
    - src/intelligence/feature_cache.py lines 33-130 (the dataclass field block, including the internal-state convention and the two existing accumulator field groups that must not be collided with) and lines 184-330 (update_wk_vwap, update_session_vp, _reset_session_vp_state, advance_bar) and lines 385-473 (update_overnight_range, the cycle-key template)
    - src/core/bar_accumulator.py lines 35-60 (`_ET`, `_RTH_OPEN_ET`, `_RTH_CLOSE_ET` and their use)
    - src/intelligence/features/i3_structure/session_levels.py (FULL, 435 lines — lines 22-24's `_SESSION_BARS`/`_WEEK_BARS`/`_OVERNIGHT_BARS` are the D-07 bug being deleted; `_compute_next_core()` lines 274-432 is the reference for WHAT state is tracked, NOT for how boundaries are detected; lines 164-177/244-255/365-376's Asian ET-hour masking is the one already-correct sub-feature; lines 328-344's `wh = max(psh, weekly_high or psh)` running-forever weekly accumulation is a second bug — a weekly level that never resets — which the ISO-week snapshot replaces)
    - .planning/milestones/v3.1-phases/165-swing-fib-trend-structure-primitives/165-CONTEXT.md D-06, D-07, D-08, D-09, D-13 and the "Claude's Discretion" block
    - .planning/milestones/v3.1-phases/165-swing-fib-trend-structure-primitives/165-PATTERNS.md section 7 (FeatureCache mutator patterns A and B, and the "new dataclass fields needed" list)
  </read_first>
  <action>
    (a) Extend the import at src/intelligence/feature_cache.py line 26 to
    `from src.core.bar_accumulator import _RTH_CLOSE_ET, _RTH_OPEN_ET`.

    (b) Add these 16 `_sl_*` fields to the `FeatureCache` dataclass, as one contiguous block immediately
    after the existing session-VP raw-level block (after `_sess_in_lvn`), each
    `field(default=..., repr=False)`, headed by a comment block that states: (i) these are internal
    session-levels state, never `FeatureVector` fields; (ii) `_sl_overnight_*` is a DIFFERENT window
    from Phase 164's `_overnight_*` (ET non-RTH block between two session opens, vs. the AMD 20:00-UTC
    accumulation phase) and the two are deliberately tracked separately; (iii) `_sl_session_day` is a
    separate key from `_session_day` because two mutators sharing one boundary key would each see "no
    change" after the other flipped it. Fields:
      _sl_session_day: object = None            # ET session day key (see update_session_vp's derivation)
      _sl_session_open: float | None = None     # open of the first bar of the current session day
      _sl_session_high: float | None = None     # running high since that open
      _sl_session_low: float | None = None      # running low since that open
      _sl_session_close: float | None = None    # latest close within the current session day
      _sl_prior_session_high: float | None = None
      _sl_prior_session_low: float | None = None
      _sl_prior_session_close: float | None = None
      _sl_gap_filled: float = 0.0               # D-13 latch, reset each session day
      _sl_on_acc_high: float | None = None      # running non-RTH accumulator for the CURRENT block
      _sl_on_acc_low: float | None = None
      _sl_overnight_high: float | None = None   # frozen at the last completed session-day rollover
      _sl_overnight_low: float | None = None
      _sl_asia_day: object = None               # ET date the current Asian block started
      _sl_asia_high: float | None = None
      _sl_asia_low: float | None = None
    plus the weekly trio and the prior-week snapshot trio (6 more, placed beside the existing
    `_wk_tp_vol_sum`/`_wk_vol_sum`/`_wk_year_week` block so the weekly state reads as one group):
      _wk_high: float | None = None
      _wk_low: float | None = None
      _wk_close: float | None = None
      _prior_wk_high: float | None = None
      _prior_wk_low: float | None = None
      _prior_wk_close: float | None = None

    (c) Extend `update_wk_vwap()` (D-09: extend, do not build a second weekly mechanism). Inside the
    EXISTING `if year_week != self._wk_year_week:` reset block, before resetting, snapshot the completed
    week: when `self._wk_high is not None`, copy `_wk_high`/`_wk_low`/`_wk_close` into
    `_prior_wk_high`/`_prior_wk_low`/`_prior_wk_close`; then set the running trio to None. After the
    reset block, accumulate: `_wk_high = high if _wk_high is None else max(_wk_high, high)`,
    `_wk_low = low if _wk_low is None else min(_wk_low, low)`, `_wk_close = close`. Leave the VWAP
    accumulation lines and the `above_wk_vwap` assignment byte-identical.
    Extend the docstring with two points a future reader needs:
      - Weekly pivot levels are derived (in Plan 05) from the PRIOR COMPLETED week's high/low/close,
        not the week in progress. Using the running week would make the pivot partially
        self-referential (its `close` component is the current bar) and would re-anchor intraweek;
        prior-week anchoring is the standard pivot-point convention and is causally clean.
      - `update_wk_vwap()` is called from `advance_bar()`, which runs AFTER `compute()`. On the first
        bar of a new ISO week, `compute()` therefore still reads the week-before-last's snapshot — a
        one-bar LAG at each week boundary. This is accepted deliberately: a lag can only ever withhold
        information, never leak future information, so it is the safe direction. Do not "fix" it by
        moving the weekly reset into a pre-compute mutator; that would duplicate the ISO-week boundary
        mechanism D-09 exists to prevent.

    (d) Define `update_session_levels(self, bar_ts, open_, high, low, close, config) -> None` beneath
    `update_session_vp` / `_reset_session_vp_state`. Never raises. Structure:
      1. Normalize `ts` and derive `et = _et_from_utc(ts)` exactly as `update_session_vp` does.
      2. `session_day = et.date() if et.time() >= _RTH_OPEN_ET else et.date() - timedelta(days=1)` —
         the same DST-correct ET wall-clock comparison, for the same reason (this gates a STATEFUL
         accumulator reset, so a DST misfire would corrupt state, not just one read-only flag).
      3. `in_rth = _RTH_OPEN_ET <= et.time() < _RTH_CLOSE_ET`.
      4. Rollover branch — when `session_day != self._sl_session_day`:
         - when `self._sl_session_day is not None`: snapshot `_sl_session_high`/`_low`/`_close` into
           `_sl_prior_session_high`/`_low`/`_close`; and when `_sl_on_acc_high is not None`, move
           `_sl_on_acc_high`/`_sl_on_acc_low` into `_sl_overnight_high`/`_sl_overnight_low`.
         - clear `_sl_on_acc_high`/`_sl_on_acc_low` to None, set `_sl_gap_filled = 0.0`, set
           `_sl_session_open = open_`, `_sl_session_high = high`, `_sl_session_low = low`, and
           `_sl_session_day = session_day`.
         The FIRST-EVER call (cold cache, `_sl_session_day is None`) therefore seeds the running
         session without inventing a prior session — `_sl_prior_session_*` stay None, which is exactly
         what Plan 05 renders as NULL columns.
      5. Non-rollover branch: `_sl_session_high = max(...)`, `_sl_session_low = min(...)` guarding the
         None cold case.
      6. Always: `_sl_session_close = close`.
      7. Overnight accumulation: when `not in_rth`, extend `_sl_on_acc_high`/`_sl_on_acc_low` with this
         bar's high/low (seeding from None). RTH bars never touch the accumulator. Because post-close
         bars of day D and pre-open bars of day D+1 share session_day D (see the interfaces note), the
         accumulator holds exactly one complete overnight block by the time step 4 freezes it.
      8. gap_filled latch (D-13): when `_sl_prior_session_close` is not None and
         `_sl_session_low <= _sl_prior_session_close <= _sl_session_high`, set `_sl_gap_filled = 1.0`.
         Checking the running extremes is equivalent to "at any point since the session open" because
         they are monotone; no additional state is needed, exactly as D-13 predicted.
      9. Asian block: `asia_start = config.session_levels_asia_start_et_hour`,
         `asia_end = config.session_levels_asia_end_et_hour`. When
         `et.hour >= asia_start or et.hour < asia_end`, derive
         `asia_day = et.date() if et.hour >= asia_start else et.date() - timedelta(days=1)`; when
         `asia_day != self._sl_asia_day`, reset `_sl_asia_high`/`_sl_asia_low` to None and set
         `_sl_asia_day = asia_day`; then extend both with this bar's high/low. Bars outside the window
         leave all three untouched, so the most recent block's values persist — this is the
         already-correct timestamp-based behaviour D-07 says not to rewrite, expressed as an
         accumulator instead of a dataframe mask.
    The docstring must state explicitly that NO bar count appears anywhere in this method, name the
    three archived constants being retired (`_SESSION_BARS`/`_WEEK_BARS`/`_OVERNIGHT_BARS`) and their
    real magnitudes at v3's live timeframes (390 bars is ~5 sessions at 5m, ~15 at 15m, ~60 at 1h,
    ~390 calendar days at 1d — wrong on every timeframe v3 runs, per RESEARCH Section 1), and that the
    ATR-normalized outputs are derived in `compute()` by Plan 05, never here (no `atr_val` is available
    in a mutator — the same division of labour as `update_session_vp` / `_derive_session_vp`).
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import dataclasses, inspect
from src.intelligence.feature_cache import FeatureCache
names={f.name for f in dataclasses.fields(FeatureCache)}
new={'_sl_session_day','_sl_session_open','_sl_session_high','_sl_session_low','_sl_session_close','_sl_prior_session_high','_sl_prior_session_low','_sl_prior_session_close','_sl_gap_filled','_sl_on_acc_high','_sl_on_acc_low','_sl_overnight_high','_sl_overnight_low','_sl_asia_day','_sl_asia_high','_sl_asia_low','_wk_high','_wk_low','_wk_close','_prior_wk_high','_prior_wk_low','_prior_wk_close'}
missing=sorted(new-names); assert not missing, missing
c=FeatureCache()
for n in new - {'_sl_gap_filled'}:
    assert getattr(c,n) is None, ('cold-start must be None, not a manufactured value', n)
assert c._sl_gap_filled == 0.0
for legacy in ('_overnight_high','_overnight_low','_overnight_day','_session_day','_sess_bars'):
    assert legacy in names, ('Phase 163/164 field was renamed or removed', legacy)
assert list(inspect.signature(FeatureCache.update_session_levels).parameters)==['self','bar_ts','open_','high','low','close','config']
print('ok 22 state fields, cold-start None, no collision, signature')"</automated>
    <automated>.venv/bin/python -c "
src=open('src/intelligence/feature_cache.py').read()
body='\n'.join(l for l in src.splitlines() if not l.lstrip().startswith('#'))
for bad in ('_SESSION_BARS','_WEEK_BARS','_OVERNIGHT_BARS','390','1950'):
    assert bad not in body, ('D-07: bar-count session detection must not exist', bad)
assert '_RTH_CLOSE_ET' in src and '_et_from_utc' in src
i=src.index('def update_session_levels')
j=src.index('def ', src.index(chr(10), src.index('\"\"\"', src.index('\"\"\"', i)+3)))
print('ok no bar-count constants, ET wall-clock used')"</automated>
    <automated>.venv/bin/python -c "
import re
src=open('src/intelligence/feature_cache.py').read()
i=src.index('def update_wk_vwap'); j=src.index('def update_session_vp')
blk=src[i:j]
assert '_prior_wk_close' in blk and '_wk_high' in blk, 'D-09: weekly H/L/C + snapshot must live inside update_wk_vwap'
assert blk.count('isocalendar()')==1, 'exactly one ISO-week boundary check may exist in this method'
assert src.count('isocalendar()')==1, 'D-09: no second parallel weekly-boundary mechanism anywhere in the file'
print('ok D-09 single weekly mechanism')"</automated>
    <automated>.venv/bin/python -c "
from datetime import UTC, datetime
from src.intelligence.feature_cache import FeatureCache
from src.intelligence.feature_factory import FeatureFactoryConfig
c=FeatureCache(); cfg=FeatureFactoryConfig()
# never-raises smoke across hostile inputs
for ts,o,h,l,cl in [(datetime(2026,3,8,6,30),0.0,0.0,0.0,0.0),(datetime(2026,7,1,14,35,tzinfo=UTC),1.0,1.0,1.0,1.0),(datetime(2026,7,1,23,0,tzinfo=UTC),1e9,1e9,-1e9,0.0)]:
    c.update_session_levels(ts,o,h,l,cl,cfg)
print('ok never raises')"</automated>
    <automated>.venv/bin/ruff check src/intelligence/feature_cache.py</automated>
  </verify>
  <acceptance_criteria>
    - All 22 new `FeatureCache` fields exist, are `repr=False`, and read `None` (or `0.0` for `_sl_gap_filled`) on a cold instance.
    - `_overnight_high`/`_overnight_low`/`_overnight_day`/`_session_day`/`_sess_bars` still exist untouched; no new field reuses any of those names.
    - `_SESSION_BARS`, `_WEEK_BARS`, `_OVERNIGHT_BARS` and the literals `390`/`1950` appear nowhere in `feature_cache.py` with comments stripped (D-07).
    - `update_session_levels(bar_ts, open_, high, low, close, config)` never raises on naive timestamps, zero prices, or extreme values, and uses `_et_from_utc` + `_RTH_OPEN_ET`/`_RTH_CLOSE_ET` for every boundary decision.
    - The weekly high/low/close accumulation and the prior-week snapshot live inside `update_wk_vwap()`'s existing `_wk_year_week` reset block; `isocalendar()` appears exactly once in the whole file (D-09).
    - `update_wk_vwap()`'s pre-existing VWAP/`above_wk_vwap` behaviour is byte-identical on the same input sequence (proven by the untouched existing tests staying green).
    - `update_session_levels`'s docstring names the three retired bar-count constants and states that ATR normalization is Plan 05's job, not the mutator's.
    - `.venv/bin/ruff check src/intelligence/feature_cache.py` clean.
  </acceptance_criteria>
  <done>FeatureCache carries a complete, timestamp-driven session/overnight/Asian/weekly state layer with correct boundary semantics, cold-start Nones instead of manufactured defaults, no collision with the two existing accumulators, and no bar count anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Wire update_session_levels() into all 3 call sites — batch loop, live per-bar handler, warm-up replay</name>
  <files>src/intelligence/feature_factory.py, services/feature_vector_pipeline.py</files>
  <read_first>
    - src/intelligence/feature_factory.py lines 5523-5560 (compute_batch's per-bar preamble: `bar_ts` normalization, `high_`/`low_`/`close_`/`vol_` extraction, the two existing `cache.update_*` calls, and the `if i < warm_up_bars: ... continue` gate they must precede) plus line 5573 (`open_ = float(bar["open"])`, currently read AFTER the warm-up gate)
    - services/feature_vector_pipeline.py lines 1163-1198 (`_process_bar_compute`'s cache-update ordering: refresh_regime -> CTF -> update_session_vp -> update_overnight_range -> compute() -> advance_bar)
    - services/feature_vector_pipeline.py lines 169-224 (`_get_cache`'s warm-up replay docstring and its three sequential `for bar in buffered:` loops)
    - src/core/schemas/bar_message.py lines 51-80 (`BarMessage` field list — confirms `.open` is available for the warm-up replay)
    - .planning/milestones/v3.1-phases/164-smc-institutional-footprint-primitives/164-04-SUMMARY.md (T-164-07: what shipping a mutator with zero call sites cost, and the structural test added to prevent a recurrence)
  </read_first>
  <action>
    (a) src/intelligence/feature_factory.py `compute_batch()`: move the `open_ = float(bar["open"])`
    read from its current position (after the warm-up gate) up into the per-bar preamble beside
    `high_`/`low_`/`close_`/`vol_`, then add
    `cache.update_session_levels(bar_ts, open_, high_, low_, close_, config)` immediately after the
    existing `cache.update_overnight_range(bar_ts, high_, low_, config)` call and BEFORE the
    `if i < warm_up_bars:` gate — so the accumulator stays current through warm-up, exactly like the
    two mutators above it. Delete the now-duplicate `open_` read further down. Carry a comment matching
    the two neighbours' style, naming Phase 165 Plan 04 and stating that firing on every bar including
    warm-up is what keeps the session/overnight/weekly state from cold-starting mid-session.

    (b) services/feature_vector_pipeline.py `_process_bar_compute`: add
    `cache.update_session_levels(bar.ts, bar.open, bar.high, bar.low, bar.close, config)` immediately
    after the existing `cache.update_overnight_range(...)` call and before
    `FeatureFactory.compute(...)`. Comment it as mirroring `compute_batch()`'s per-bar call, matching
    the existing two comments' wording.

    (c) services/feature_vector_pipeline.py `_get_cache`: add a fourth
    `for bar in buffered:` loop calling
    `cache.update_session_levels(bar.ts, bar.open, bar.high, bar.low, bar.close, self._feature_factory_config)`
    after the `update_overnight_range` loop, and extend the method docstring with a paragraph in the
    same shape as the existing `update_session_vp`/`update_overnight_range` paragraphs: without the
    replay, session/overnight/weekly state cold-starts on every restart that does not land exactly on a
    session boundary, silently freezing all 16 Plan 05 columns at NULL until a full session and a full
    ISO week have elapsed — the same T-164-07 gap Phase 164 had to fix retroactively.

    (d) Add a structural regression test asserting all 3 call sites exist, following the precedent
    Phase 164 Plan 04 established for `update_overnight_range()`. Put it in the new file Task 3
    creates (`tests/unit/intelligence/test_session_levels_cache.py`) as
    `test_session_levels_wired_at_all_three_call_sites`: read
    `src/intelligence/feature_factory.py` and `services/feature_vector_pipeline.py` as text, strip
    comment-only lines, and assert `update_session_levels(` appears exactly once in
    `feature_factory.py` and exactly twice in `feature_vector_pipeline.py`, with a failure message
    naming T-164-07 and the three sites by name.

    Do NOT add the call to `advance_bar()` — that runs AFTER `compute()`. Every mutator whose state
    `compute()` reads must fire before it, matching `update_session_vp`/`update_overnight_range`.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
ff='\n'.join(l for l in open('src/intelligence/feature_factory.py').read().splitlines() if not l.lstrip().startswith('#'))
fp='\n'.join(l for l in open('services/feature_vector_pipeline.py').read().splitlines() if not l.lstrip().startswith('#'))
assert ff.count('update_session_levels(')==1, ('compute_batch call site', ff.count('update_session_levels('))
assert fp.count('update_session_levels(')==2, ('live handler + warm-up replay', fp.count('update_session_levels('))
i=ff.index('update_session_levels('); j=ff.index('if i < warm_up_bars')
assert i < j, 'batch call must precede the warm-up gate so state stays current through warmup'
print('ok 3 call sites, correct batch ordering')"</automated>
    <automated>.venv/bin/python -c "
ff=open('src/intelligence/feature_factory.py').read()
assert ff.count('open_ = float(bar[\"open\"])')==1, 'the moved open_ read must not be duplicated'
print('ok single open_ read in compute_batch')"</automated>
    <automated>.venv/bin/python -c "
fp=open('services/feature_vector_pipeline.py').read()
i=fp.index('cache.update_session_levels'); j=fp.index('FeatureFactory.compute(')
assert i < j, 'live mutator call must precede compute()'
print('ok live ordering')"</automated>
    <automated>.venv/bin/pytest tests/unit/services/ tests/unit/intelligence/ -q</automated>
    <automated>.venv/bin/ruff check src/intelligence/feature_factory.py services/feature_vector_pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - `update_session_levels(` appears exactly once in `feature_factory.py` (compute_batch's per-bar loop, before the warm-up gate) and exactly twice in `feature_vector_pipeline.py` (live per-bar handler and warm-up replay), with comment-only lines stripped from the count.
    - The batch call fires before `if i < warm_up_bars:`; the live call fires before `FeatureFactory.compute(`; neither is placed in `advance_bar()`.
    - `open_` is read exactly once in `compute_batch()`, in the per-bar preamble.
    - `_get_cache`'s docstring gains a paragraph explaining the warm-up replay's purpose in the same shape as the existing `update_session_vp` / `update_overnight_range` paragraphs, naming T-164-07.
    - `test_session_levels_wired_at_all_three_call_sites` exists and passes.
    - `.venv/bin/pytest tests/unit/services/ tests/unit/intelligence/ -q` green; ruff clean on both edited files.
  </acceptance_criteria>
  <done>The session-levels mutator fires on every bar in both the live and batch paths and is replayed over buffered history on restart — the T-164-07 cold-start gap cannot recur, and a structural test proves it.</done>
</task>

<task type="auto">
  <name>Task 3: Mutator unit tests — session rollover, overnight freeze, gap-fill latch, Asian cycle, weekly snapshot, DST boundary, cold start</name>
  <files>tests/unit/intelligence/test_session_levels_cache.py</files>
  <read_first>
    - src/intelligence/feature_cache.py — the `update_session_levels` / extended `update_wk_vwap` written in Task 1, plus the dataclass field block
    - tests/unit/intelligence/test_support_resistance_primitives.py lines 1-60 (module docstring style, seeded RNG convention, `_make_cfg` shape)
    - tests/unit/ — grep for an existing `FeatureCache` mutator test (`grep -rln "update_session_vp\|update_overnight_range" tests/unit/`) and follow whichever file's fixture style is closest; reuse its ET/UTC timestamp helpers rather than writing new ones
    - .planning/milestones/v3.1-phases/165-swing-fib-trend-structure-primitives/165-CONTEXT.md D-07, D-08, D-09, D-13
  </read_first>
  <action>
    Create tests/unit/intelligence/test_session_levels_cache.py. Module docstring: "Regression: Phase 165
    FeatureCache session-levels state is timestamp-driven (never bar-count, D-07), resets on real ET
    session / ISO-week / Asian-block boundaries, and reads None rather than a manufactured value before
    its first boundary transition (D-01)."

    Build one shared helper `_bars_utc(start_utc, count, step_minutes, price_fn)` producing
    `(ts, open, high, low, close)` tuples on a UTC grid, and a `_cfg()` returning a default
    `FeatureFactoryConfig` (the two `session_levels_asia_*` defaults are 20/4 from Plan 01). Every test
    name must be file-scoped unique and prefixed `test_session_levels_*` or `test_weekly_*` — the repo's
    pre-commit hook rejects duplicate test function names across tests/.

    Write these tests:
    (a) `test_session_levels_cold_start_is_none` — a fresh `FeatureCache`, then a single bar fed
    through `update_session_levels`: assert `_sl_prior_session_high`/`_low`/`_close`,
    `_sl_overnight_high`/`_low`, `_prior_wk_high`/`_low`/`_close` are all still `None` and
    `_sl_gap_filled == 0.0`, with a failure message naming D-01 (no manufactured first-session value).
    (b) `test_session_levels_rollover_snapshots_prior_session` — feed one full ET session day of 5m
    bars with a known high/low/close, then the first bar of the next session day; assert
    `_sl_prior_session_high`/`_low`/`_close` equal the first day's values exactly and the running
    `_sl_session_high`/`_low`/`_open` now reflect only the new day's first bar.
    (c) `test_session_levels_overnight_freezes_at_rollover` — feed RTH bars, then non-RTH bars with a
    deliberately distinct high/low, then the next session's first RTH bar; assert `_sl_overnight_high`/
    `_sl_overnight_low` equal the non-RTH block's extremes and `_sl_on_acc_high`/`_sl_on_acc_low` are
    back to `None`. Also assert RTH bars alone never populate `_sl_on_acc_high`.
    (d) `test_session_levels_gap_filled_latches_and_resets` — construct a session whose running range
    straddles the prior session's close: assert `_sl_gap_filled` flips to 1.0 on the bar that first
    brackets it, STAYS 1.0 on a later bar that no longer brackets it, and resets to 0.0 at the next
    session-day rollover. Then a session that never brackets: assert it stays 0.0 throughout.
    (e) `test_session_levels_asian_block_cycle_key` — feed bars spanning 19:00 ET (outside),
    20:30 ET (inside, block start), 02:00 ET next day (inside, same block), 05:00 ET (outside) and
    20:30 ET the following evening (inside, NEW block): assert `_sl_asia_high`/`_sl_asia_low` cover the
    first block's extremes across the midnight straddle, are untouched by the outside bars, and reset
    at the new block. Parametrize `session_levels_asia_start_et_hour`/`asia_end_et_hour` to non-default
    values in one case and assert the block boundaries move accordingly (proving the APR keys are read).
    (f) `test_weekly_snapshot_on_iso_week_rollover` — feed bars across an ISO-week boundary through
    `update_wk_vwap`: assert `_prior_wk_high`/`_low`/`_close` are `None` during the first week, equal
    the first week's extremes and final close after the rollover, and that `_wk_high`/`_wk_low` restart
    from the new week's first bar.
    (g) `test_weekly_vwap_behaviour_unchanged` — feed a fixed bar sequence and assert `above_wk_vwap`
    and the internal `_wk_tp_vol_sum`/`_wk_vol_sum` match hand-computed values, proving the D-09
    extension did not perturb the pre-existing VWAP path.
    (h) `test_session_levels_dst_boundary` — feed bars around the 2026 US DST transitions (March and
    November) and assert the session-day key advances exactly once per calendar session, never twice
    and never skipping — the reason this mutator uses `_et_from_utc` + `_RTH_OPEN_ET` rather than the
    `feature.session.ny_*_utc_hour` APR values (same rationale `update_session_vp` documents).
    (i) `test_session_levels_no_collision_with_amd_or_vp_state` — drive `update_session_levels`,
    `update_session_vp` and `update_overnight_range` over the SAME bar sequence on one cache and assert
    `_session_day`, `_sess_bars`, `_overnight_day`, `_overnight_high` and `_overnight_low` hold exactly
    the values they hold when `update_session_levels` is NOT called. This is the direct guard for the
    shared-boundary-key hazard.
    (j) `test_session_levels_wired_at_all_three_call_sites` — the structural test specified by Task 2(d).

    Apply commit `a748d13d`'s mutation-verification discipline before considering this task done:
    temporarily change `update_session_levels`'s rollover branch to seed `_sl_prior_session_*` from the
    current bar instead of leaving them None on the first call, and confirm test (a) FAILS; temporarily
    change the session-day derivation to a bar counter and confirm tests (b), (c) and (h) FAIL;
    temporarily make `update_session_levels` write to `_session_day` instead of `_sl_session_day` and
    confirm test (i) FAILS. Revert all three local edits before committing and record the observed
    failures in the plan SUMMARY.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_session_levels_cache.py -x -q</automated>
    <automated>.venv/bin/python -c "
src=open('tests/unit/intelligence/test_session_levels_cache.py').read()
for t in ('test_session_levels_cold_start_is_none','test_session_levels_rollover_snapshots_prior_session','test_session_levels_overnight_freezes_at_rollover','test_session_levels_gap_filled_latches_and_resets','test_session_levels_asian_block_cycle_key','test_weekly_snapshot_on_iso_week_rollover','test_weekly_vwap_behaviour_unchanged','test_session_levels_dst_boundary','test_session_levels_no_collision_with_amd_or_vp_state','test_session_levels_wired_at_all_three_call_sites'):
    assert t in src, t
names=[l.split('(')[0] for l in src.split('def ')[1:] if l.startswith('test_')]
assert len(names)==len(set(names)), 'duplicate test function name in file'
print('ok 10 tests present, names unique')"</automated>
    <automated>.venv/bin/pytest tests/unit/ -q</automated>
    <automated>.venv/bin/ruff check tests/unit/intelligence/test_session_levels_cache.py</automated>
    <automated>.venv/bin/black --check tests/unit/intelligence/test_session_levels_cache.py src/intelligence/feature_cache.py</automated>
  </verify>
  <acceptance_criteria>
    - All 10 named tests exist, carry file-scoped-unique names, and pass.
    - `test_session_levels_no_collision_with_amd_or_vp_state` compares Phase 163/164 accumulator state with and without the new mutator running, and fails when the new mutator writes to `_session_day`.
    - `test_session_levels_asian_block_cycle_key` parametrizes the two `feature.session_levels.asia_*` values and asserts the block boundaries move, proving the APR keys are read.
    - `test_weekly_vwap_behaviour_unchanged` pins `above_wk_vwap` and the VWAP accumulators against hand-computed values.
    - The mutation check was actually run: the SUMMARY records which tests failed under (i) a seeded first prior-session, (ii) a bar-counter session key, and (iii) a shared `_session_day` write, and all three local edits were reverted.
    - `.venv/bin/pytest tests/unit/ -q` fully green; ruff and black clean.
  </acceptance_criteria>
  <done>The session-levels state layer has mutation-verified unit coverage for every boundary transition it implements, including the DST case and the accumulator-collision hazard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| market_data_ohlcv_tradeable -> FeatureCache mutators | Already-validated internal OHLCV; no external or untrusted input crosses. |
| Cross-bar accumulated cache state -> persisted feature_vectors row | State that survives a boundary it should have reset at silently poisons every subsequent row until the next restart. |
| Service restart -> FeatureCache reconstruction | State not replayed on restart cold-starts mid-session, producing live/batch divergence invisible to any per-bar test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-14 | Tampering (silent state corruption) | Two mutators sharing one boundary key (`_session_day`) — whichever runs first flips it, the second never resets | mitigate | Every new field carries an `_sl_` prefix; `test_session_levels_no_collision_with_amd_or_vp_state` compares Phase 163/164 accumulator state with and without the new mutator and fails under a shared write. |
| T-165-15 | Tampering (silent wrong data) | Carrying forward the archived bar-count session detection (`_SESSION_BARS=390`), which is wrong on every timeframe v3 runs | mitigate | Boundaries derived from `_et_from_utc` + `_RTH_OPEN_ET`/`_RTH_CLOSE_ET`; a source assertion proves the three constants and the literals `390`/`1950` exist nowhere in `feature_cache.py`; `test_session_levels_dst_boundary` proves the ET derivation survives both DST transitions. |
| T-165-16 | Tampering (look-ahead leakage) | Deriving weekly pivots from the week IN PROGRESS makes the `close` component the current bar — partially self-referential | mitigate | Pivots anchor on `_prior_wk_*`, snapshotted only at an ISO-week rollover; `test_weekly_snapshot_on_iso_week_rollover` pins the values. The `advance_bar()` ordering adds a one-bar lag at week boundaries, accepted deliberately because a lag can only withhold information, never leak it. |
| T-165-17 | Denial of service (train/serve divergence) | Shipping a mutator with missing call sites — exactly what happened to `update_overnight_range()` in Phase 164 Plan 01 (T-164-07) | mitigate | All 3 call sites wired in Task 2's single commit; `test_session_levels_wired_at_all_three_call_sites` is a structural gate asserting the exact per-file call counts with comments stripped. |
| T-165-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero new packages. `_et_from_utc`, `_RTH_OPEN_ET`, `_RTH_CLOSE_ET`, `numpy` and `datetime` are already-live in-repo dependencies. No install task exists in this plan. |
</threat_model>

<verification>
- 22 new `FeatureCache` fields exist, cold-start to `None` (or `0.0` for the gap flag), and collide with nothing Phase 163 or 164 owns.
- `_SESSION_BARS` / `_WEEK_BARS` / `_OVERNIGHT_BARS` and their literal values exist nowhere; every boundary decision goes through `_et_from_utc` + the ET RTH constants.
- `isocalendar()` appears exactly once in `feature_cache.py` — the weekly H/L/C accumulation and prior-week snapshot live inside `update_wk_vwap()`'s existing reset block (D-09).
- `update_wk_vwap()`'s pre-existing VWAP / `above_wk_vwap` output is unchanged.
- `update_session_levels(` appears exactly once in `feature_factory.py` and exactly twice in `feature_vector_pipeline.py`, in the correct order relative to the warm-up gate and `compute()`.
- 10 mutator tests pass, including the DST-boundary and accumulator-collision guards, with the mutation check recorded in the SUMMARY.
- `.venv/bin/pytest tests/unit/ -q` green; ruff and black clean.
</verification>

<success_criteria>
`FeatureCache` carries a complete, timestamp-driven session/overnight/Asian-block/weekly state layer
with correct boundary semantics on every timeframe v3 actually runs, replacing the archived plugin's
bar-count detection outright. Every accumulator reads `None` until its first real boundary transition
rather than a manufactured default, the weekly extension lives inside the single existing ISO-week
mechanism, no new field collides with Phase 163's or Phase 164's accumulators, and the mutator fires at
all three call sites so live and batch cannot diverge. Plan 05 can now derive all 16 session-levels
columns as a thin mapping over proven state.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/165-swing-fib-trend-structure-primitives/165-04-SUMMARY.md` when done.
</output>
</content>
