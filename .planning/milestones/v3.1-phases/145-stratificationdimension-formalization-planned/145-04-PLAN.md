---
phase: 145-stratificationdimension-formalization-planned
plan: 04
type: execute
wave: 3
depends_on: ["145-01", "145-02", "145-03"]
files_modified:
  - src/intelligence/stratification/volatility_pct.py
  - tests/unit/test_volatility_pct_pilot.py
autonomous: true
requirements: [D-02, D-06, D-07]
must_haves:
  truths:
    - "volatility_pct is a real StratificationDimension implementation with grain='per_symbol' and causality_basis='expanding_window'"
    - "Its score() output never changes retroactively when future bars are appended"
    - "It passes the D-05 acausal-placebo registration gate end to end via contract.validate_registration"
    - "Its compute() applies the same min_hold smoothing value as the incumbent HMM so effective-N comparisons in the substitution test are between equally-autocorrelated sequences"
    - "It reuses causal_expanding_rank rather than reimplementing percentile ranking"
    - "No file under src/intelligence/stratification/ imports from services/"
  artifacts:
    - path: "src/intelligence/stratification/volatility_pct.py"
      provides: "D-06's single pilot candidate dimension"
      exports: ["VolatilityPct", "NAME", "GRAIN", "LABELS", "CAUSALITY_BASIS", "OBSERVATION_BASIS", "build_tiers"]
    - path: "tests/unit/test_volatility_pct_pilot.py"
      provides: "Shape, causal-property, smoothing-parity and registration coverage for the pilot provider"
      min_lines: 150
  key_links:
    - from: "src/intelligence/stratification/volatility_pct.py"
      to: "src/intelligence/regime_signals/causal_rank.py::causal_expanding_rank"
      via: "direct import"
      pattern: "from src.intelligence.regime_signals.causal_rank import causal_expanding_rank"
    - from: "src/intelligence/stratification/volatility_pct.py"
      to: "src/intelligence/stratification/gates.py::min_hold_smooth"
      via: "direct import for HMM smoothing parity"
      pattern: "min_hold_smooth"
---

<objective>
Implement `volatility_pct`, the one and only candidate dimension this phase builds (D-06), as a
conforming `StratificationDimension` provider: expanding percentile rank of an already-causal
realized-volatility feature, bucketed into three labels, smoothed with the same `min_hold_bars`
value the incumbent HMM uses.

Purpose: running exactly one well-understood candidate all the way through the corrected gate
stack validates the new FDR-correction and effective-N mechanisms themselves before the candidate
pool opens. Fewer tests run also means less FDR budget spent while the correction mechanism is
itself unproven.

Output: `volatility_pct.py` plus a CI-clean unit suite proving shape, the causal property,
smoothing parity, and that the provider clears `validate_registration` with the real D-05 gate.

Explicitly NOT in scope (D-06): `dispersion`, `factor_regime`, `term_structure`, `volume_pct`,
`skew_tail`, `session_position`, E1-E4, and every deferred HMM variant. Do not build or test any
of them.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v3.1-phases/145-stratificationdimension-formalization-planned/145-CONTEXT.md
@.planning/milestones/v3.1-phases/145-stratificationdimension-formalization-planned/145-RESEARCH.md
@.planning/milestones/v3.1-phases/145-stratificationdimension-formalization-planned/145-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- Everything this provider composes. All Ring 1. -->

`src/intelligence/regime_signals/causal_rank.py` (IMPORT directly, do not reimplement):
    def causal_expanding_rank(series: pd.Series) -> pd.Series
    # Each position's rank is computed against all PRIOR valid values only. NaN values are
    # skipped (not inserted into the sorted window). Ties average bisect_left/bisect_right.
    # Extracted specifically to stop this bisect logic being duplicated per module; two
    # independent look-ahead bugs (todo 092) came from guessed absolute thresholds instead.

`src/intelligence/regime_signals/breadth_vol.py` (the live module whose shape this mirrors):
    PROB_KEYS: tuple[str, str]
    def compute(ref_bars, params) -> tuple[pd.Series, pd.Series] | None
    def build_tiers(params) -> tuple[list[tuple[str, float]], list[tuple[str, float]]]
    def _compute_vix_pct_rank(spy_close, realized_vol_window, vix_z_window) -> pd.Series
    # Shape: log-return realized vol -> rolling z-score -> causal expanding rank.
    # Module contains no psycopg import and no DB access — keep that property.

`services/cross_sectional_regime_model.py::_bucket(vals, tiers)` (Ring 2 — READ for the algorithm,
do NOT import): assigns tier names by threshold; `tiers` sorted ascending by upper bound, last
entry is `inf`; a value is assigned to the FIRST tier whose upper bound strictly exceeds it.

`src/intelligence/stratification/contract.py` (plan 145-01): `StratificationDimension`,
`validate_registration(provider, placebo_gate)`, `Grain`, `CausalityBasis`.

`src/intelligence/stratification/gates.py` (plan 145-02): `min_hold_smooth(labels, min_hold)`,
`effective_n_from_transitions(labels)`, `_MIN_HOLD_BARS_DEFAULT = 3`.

`src/intelligence/stratification/acausal_placebo_gate.py` (plan 145-03): `make_placebo_gate(...)`.

Live `feature_vectors` columns available as raw inputs (psql-verified 2026-08-06, all already
causal and already z-scored): `garman_klass_vol_z`, `parkinson_vol_z`, `vol_std_z`,
`volatility_rank_z`, `vol_percentile`, `vol_of_vol`. This provider uses `garman_klass_vol_z` as
its primary input column name and must accept the column name as a parameter so the pilot script
can swap it without editing the module.

**D-07 APR exception (binding):** `alpha.volatility_regime.low_pct` and
`alpha.volatility_regime.high_pct` do NOT exist in `config_state` (psql-verified). Hold them as
module-level constants with a comment citing D-07 and Phase 170. Do not add a migration.

**Ring rule:** Ring 1 module. Zero imports from `services/`.

**Raw-table boundary:** this module reads no database at all. If a future caller supplies OHLCV it
must come from the `market_data_ohlcv_tradeable` view, never the raw `market_data_ohlcv` table
(CI-enforced by `tests/unit/test_market_data_ohlcv_boundary.py`).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write the volatility_pct pilot provider test suite</name>
  <files>tests/unit/test_volatility_pct_pilot.py</files>
  <read_first>
    - tests/unit/test_regime_signals_breadth_vol.py (read the header and `TestComputeReturnShape` — the fixture-construction and shape-assertion pattern to mirror, adapted from cross-sectional to per-symbol grain)
    - tests/unit/test_regime_signals_causal_rank.py (read in full — `test_causal_property_future_value_does_not_change_past_ranks` is the mandatory causal regression to replicate)
    - src/intelligence/regime_signals/breadth_vol.py (read in full — module docstring convention, `compute`/`build_tiers` split, `_compute_vix_pct_rank`)
    - src/intelligence/stratification/contract.py, gates.py, acausal_placebo_gate.py (the interfaces this provider composes)
  </read_first>
  <behavior>
    identity/conformance:
    - `VolatilityPct.name == "volatility_pct"`, `grain == "per_symbol"`,
      `causality_basis == "expanding_window"`, `fit_window is None`,
      `labels == ("vol_low", "vol_mid", "vol_high")`,
      `observation_basis == frozenset({"realized_volatility"})`.
    - D-02 check: no attribute or code path special-cases a regime_group name; a source-level
      assertion that the comment-stripped module contains zero occurrences of the literals
      `"rates"`, `"equity"`, `"fx"`.

    score():
    - Returns a `pd.Series` of the same length and index as the input frame.
    - Every non-NaN value is within [0.0, 1.0].
    - Causal property (mandatory): ranks for the first N rows are unchanged when an extreme
      value is appended as row N+1 — mirror
      `test_causal_property_future_value_does_not_change_past_ranks` exactly.
    - NaN input rows produce NaN score rows and do not shift the ranks of surrounding rows.
    - Accepts a `params["input_column"]` override and reads that column instead of the default.
    - Raises `KeyError` (not a silent fallback) when the configured input column is absent.

    compute():
    - Returns a `pd.Series` whose non-null values are drawn only from `LABELS`.
    - With `params["min_hold_bars"] = 1` a single-bar excursion in the underlying score survives
      as its own label run; with the default `min_hold_bars` of 3 it is absorbed. Assert via
      `effective_n_from_transitions` on both outputs — the smoothed one has strictly fewer visits.
    - Default `min_hold_bars` equals `gates._MIN_HOLD_BARS_DEFAULT` (3) so labels are as
      autocorrelated as the incumbent HMM's; assert equality against the imported constant, not a
      hardcoded 3.
    - Tier boundaries: a monotone increasing input produces `vol_low` for the lowest ~33% of
      causal ranks and `vol_high` for the top ~33%, with `build_tiers`' defaults.
    - Empty and single-row frames return an empty/single-element Series without raising.

    registration (`-k registration`):
    - `contract.validate_registration(VolatilityPct(), make_placebo_gate(frame, forward_returns, seed=145))`
      completes without raising — the pilot provider clears the real D-05 gate, not a stub.
    - `check_prefix_invariance(VolatilityPct(), frame, params)` returns `passed=True`.
  </behavior>
  <action>
Create `tests/unit/test_volatility_pct_pilot.py`. Mirror
`tests/unit/test_regime_signals_breadth_vol.py`'s header: `from __future__ import annotations`,
project-root `sys.path` insert, module docstring stating CI-clean (no DB, no network) and citing
Phase 145 D-06 as the pilot this covers.

Build fixtures with a `_make_frame(values)` helper returning a `pd.DataFrame` with a
`pd.date_range(tz="UTC")` index or a `bar_ts` column plus a `garman_klass_vol_z` column, mirroring
`_make_bars` in the breadth_vol test. Use `np.random.default_rng(145)` for the random-input cases;
use explicit hand-written value lists for the tier-boundary and smoothing assertions so the
expected labels are inspectable rather than statistical.

For the registration test, construct a forward-return array with a genuine causal relationship to
the input so the placebo check's null comparison is meaningful, and pass an explicit `seed`.

Imports under test: `from src.intelligence.stratification.volatility_pct import (
CAUSALITY_BASIS, GRAIN, LABELS, NAME, OBSERVATION_BASIS, VolatilityPct, build_tiers)`. The module
does not exist yet — RED until Task 2.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_volatility_pct_pilot.py -q 2>&1 | tail -5; test -f tests/unit/test_volatility_pct_pilot.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_volatility_pct_pilot.py` exists.
    - `.venv/bin/pytest tests/unit/test_volatility_pct_pilot.py -q` fails with a collection/import
      error naming `src.intelligence.stratification.volatility_pct` (RED proven).
    - `grep -c "def test_" tests/unit/test_volatility_pct_pilot.py` returns at least 16.
    - A test whose name contains `causal_property` exists.
    - `grep -c "make_placebo_gate" tests/unit/test_volatility_pct_pilot.py` returns at least 1.
    - `grep -c "_MIN_HOLD_BARS_DEFAULT" tests/unit/test_volatility_pct_pilot.py` returns at least 1.
    - `grep -c "default_rng" tests/unit/test_volatility_pct_pilot.py` returns at least 1.
  </acceptance_criteria>
  <done>RED suite exists covering identity, score causality, compute labeling/smoothing parity, and real-gate registration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement the volatility_pct provider</name>
  <files>src/intelligence/stratification/volatility_pct.py</files>
  <read_first>
    - tests/unit/test_volatility_pct_pilot.py (the RED suite — it is the spec)
    - src/intelligence/regime_signals/breadth_vol.py (read in full — module docstring convention including the CALIBRATION note, `compute`/`build_tiers` split, and the "no DB access in this module" property)
    - src/intelligence/regime_signals/causal_rank.py (read in full — the function being imported)
    - services/cross_sectional_regime_model.py (read `_bucket`, ~lines 196-205 — the tier-assignment algorithm to mirror locally; do NOT import)
    - src/intelligence/stratification/gates.py (read `min_hold_smooth` and `_MIN_HOLD_BARS_DEFAULT`)
  </read_first>
  <behavior>
    Turns every assertion in `tests/unit/test_volatility_pct_pilot.py` green without editing that
    test file.
  </behavior>
  <action>
Create `src/intelligence/stratification/volatility_pct.py` with:

Module-level identity constants matching the contract's `ClassVar` fields:
`NAME = "volatility_pct"`, `GRAIN = "per_symbol"`, `CAUSALITY_BASIS = "expanding_window"`,
`FIT_WINDOW = None`, `LABELS = ("vol_low", "vol_mid", "vol_high")`,
`OBSERVATION_BASIS = frozenset({"realized_volatility"})`.

Module constants with D-07 / Phase 170 provenance comments (see `<interfaces>`):
`_LOW_PCT_DEFAULT = 0.33` (target key `alpha.volatility_regime.low_pct`),
`_HIGH_PCT_DEFAULT = 0.67` (target key `alpha.volatility_regime.high_pct`),
`_INPUT_COLUMN_DEFAULT = "garman_klass_vol_z"` (target key
`alpha.regime_stratification.volatility_pct.input_column`; comment must state why an
already-causal, already-z-scored `feature_vectors` column is used as the raw input rather than
recomputing realized volatility from OHLCV, and note that switching this column invalidates any
prior pilot result).

`def build_tiers(params: dict[str, Any]) -> list[tuple[str, float]]` — returns
`[("vol_low", low_pct), ("vol_mid", high_pct), ("vol_high", float("inf"))]`, mirroring
`breadth_vol.build_tiers`' ascending-upper-bound-with-inf-last convention.

`def _bucket(vals: np.ndarray, tiers: list[tuple[str, float]]) -> np.ndarray` — local mirror of
`services/cross_sectional_regime_model.py::_bucket`'s algorithm (assign the last tier's name, then
walk the tiers in reverse overwriting where `vals < upper`). NaN scores must map to `None`, not to
a tier. Docstring records that this is a deliberate Ring-boundary duplication, names the source
function and file, and cites the Ring rule as why it is not imported.

`class VolatilityPct` implementing the Protocol structurally (no inheritance needed):
- `ClassVar` fields set from the module constants above.
- `score(self, frame, params=None) -> pd.Series` — read `params["input_column"]` (default
  `_INPUT_COLUMN_DEFAULT`), raise `KeyError` naming the missing column when absent, and return
  `causal_expanding_rank(frame[col].astype(float))`. No new rank implementation.
- `compute(self, frame, params=None) -> pd.Series` — call `self.score(...)`, bucket via
  `build_tiers`/`_bucket`, then apply `min_hold_smooth(labels, params.get("min_hold_bars",
  gates._MIN_HOLD_BARS_DEFAULT))`, returning a `pd.Series` on the frame's index. Import
  `min_hold_smooth` and the default constant from `src.intelligence.stratification.gates`.

Module docstring must follow `breadth_vol.py`'s convention: state the signal construction, state
the correctness invariant (causal expanding rank only, never a whole-series `pandas.rank` — cite
todo 092's two confirmed look-ahead bugs), state that this module performs no DB access, and add
an explicit SMOOTHING note per Pitfall 5 recording that `compute()` returns POST-smoothing labels
using the same `min_hold_bars` value as the incumbent HMM, so that
`effective_n_from_transitions()` comparisons in the substitution test are between
equally-autocorrelated sequences rather than comparing HMM-smoothed labels against a raw
percentile series. Cite D-06 as the reason this is the phase's only candidate and name the
deferred candidates that are explicitly NOT built here.

Ring rule: imports limited to stdlib, numpy, pandas, and `src/`. Zero imports from `services/`.
No `psycopg`, `asyncpg`, or `Settings` import.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_volatility_pct_pilot.py -x -q && .venv/bin/pytest tests/unit/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_volatility_pct_pilot.py -x -q` passes with 0 failures.
    - `.venv/bin/pytest tests/unit/ -q` passes (no regression).
    - `grep -v '^\s*#' src/intelligence/stratification/volatility_pct.py | grep -c "from services\.\|import services\.\|psycopg\|asyncpg\|Settings"` returns 0.
    - `grep -c "from src.intelligence.regime_signals.causal_rank import causal_expanding_rank" src/intelligence/stratification/volatility_pct.py` returns 1.
    - `grep -c "min_hold_smooth" src/intelligence/stratification/volatility_pct.py` returns at least 1.
    - `grep -c "D-07" src/intelligence/stratification/volatility_pct.py` returns at least 3.
    - `grep -c "D-06" src/intelligence/stratification/volatility_pct.py` returns at least 1.
    - `grep -v '^\s*#' src/intelligence/stratification/volatility_pct.py | grep -c "\.rank(" ` returns 0
      (no whole-series pandas rank).
    - `.venv/bin/ruff check src/intelligence/stratification/` and
      `.venv/bin/black --check src/intelligence/stratification/` both exit 0.
  </acceptance_criteria>
  <done>volatility_pct exists as a conforming, causally-clean, HMM-smoothing-parity provider that clears the real D-05 registration gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw feature column → percentile label | A non-causal rank transform silently converts a clean input into a leaking label |
| candidate labels → substitution-test comparison | Differently-smoothed sequences compared as if equally autocorrelated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-14 | Tampering | `score()` rank transform | mitigate | Imports `causal_expanding_rank` directly; a source assertion forbids any `.rank(` call; a causal-property regression test mirrors `test_causal_property_future_value_does_not_change_past_ranks` |
| T-145-15 | Tampering | Guessed absolute vol thresholds | mitigate | Tiers cut on causal percentile ranks, never on raw z-score levels — the exact todo 092 root cause `causal_rank.py` was extracted to prevent |
| T-145-16 | Repudiation | Substitution test comparing unequally-smoothed sequences | mitigate | `compute()` applies `min_hold_smooth` with the same default as the incumbent HMM (`_MIN_HOLD_BARS_DEFAULT = 3`, matching live `feature.hmm.min_hold_bars`); a test asserts the default against the imported constant, not a literal |
| T-145-17 | Tampering | Provider registering with an unverified causality claim | mitigate | A test calls `contract.validate_registration` with the real `make_placebo_gate` from plan 145-03, not a stub |
| T-145-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
1. `.venv/bin/pytest tests/unit/test_volatility_pct_pilot.py -v` — all green.
2. `.venv/bin/pytest tests/unit/ -q` — full suite green.
3. `.venv/bin/ruff check . --fix && .venv/bin/black .` — clean.
4. `grep -v '^\s*#' src/intelligence/stratification/*.py | grep -c "from services\."` returns 0.
5. `git diff --name-only` shows zero files under `production/migrations/` and zero changes to
   `services/`.
</verification>

<success_criteria>
- `volatility_pct` is the phase's only new candidate dimension; no other candidate is built.
- It conforms structurally to `StratificationDimension` and clears `validate_registration` with the
  real D-05 gate.
- Its `score()` is provably causal and its `compute()` is smoothing-parity with the incumbent HMM.
- It reuses `causal_expanding_rank`; it does not reimplement percentile ranking.
- Zero Ring 2 imports, zero DB access, zero migrations.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/145-stratificationdimension-formalization-planned/145-04-SUMMARY.md` when done
</output>
