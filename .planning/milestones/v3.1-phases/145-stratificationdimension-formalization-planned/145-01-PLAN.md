---
phase: 145-stratificationdimension-formalization-planned
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/stratification/__init__.py
  - src/intelligence/stratification/contract.py
  - tests/unit/test_stratification_contract.py
  - docs/research/stratification-dimension-unification.md
  - docs/foundation/glossary.md
autonomous: true
requirements: [D-01, D-02, D-07, GOAL-protocol, GOAL-rowgrain]
must_haves:
  truths:
    - "A StratificationDimension Protocol exists as real importable Python code with name/grain/labels/causality_basis/fit_window/observation_basis identity fields and compute()/score() methods"
    - "registry_name('hmm_price_vol', 'equity') and registry_name('hmm_price_vol', 'rates') produce two distinct concept_registry names (Option B row-grain, D-01)"
    - "A provider that omits a required contract field, declares an out-of-vocabulary causality_basis, or declares causality_basis='fitted' without a fit_window is rejected at registration with a raised exception, not a warning"
    - "validate_registration() cannot be called without supplying an acausal-placebo gate callable (D-05 precondition is structurally unskippable)"
    - "The contract's label output is consumable by services/ic_engine.py's existing _build_regime_passes without modifying ic_engine.py"
    - "No file under src/intelligence/stratification/ imports from services/"
  artifacts:
    - path: "src/intelligence/stratification/contract.py"
      provides: "StratificationDimension Protocol, Option B registry-name encoding, registration validator"
      exports: ["StratificationDimension", "CausalityBasis", "Grain", "registry_name", "parse_registry_name", "validate_registration", "RegistrationError", "to_regime_pass"]
    - path: "src/intelligence/stratification/__init__.py"
      provides: "Package marker for the new Ring 1 stratification subpackage"
    - path: "tests/unit/test_stratification_contract.py"
      provides: "Protocol conformance, D-01 naming, D-02 agnosticism, and ic_engine compatibility coverage"
      min_lines: 120
    - path: "docs/foundation/glossary.md"
      provides: "Canonical definitions for `stratification dimension` and `stratum`"
      contains: "stratification dimension"
  key_links:
    - from: "tests/unit/test_stratification_contract.py"
      to: "services.ic_engine._build_regime_passes"
      via: "test-only import asserting output-shape compatibility"
      pattern: "from services.ic_engine import"
    - from: "src/intelligence/stratification/contract.py"
      to: "validate_registration placebo_gate parameter"
      via: "required positional argument, no default"
      pattern: "placebo_gate"
---

<objective>
Write the `StratificationDimension` `Protocol` as real, importable Ring 1 code, encode the
ratified Option B `concept_registry` row-grain convention (D-01) as a tested pure function, and
ratify that decision plus this phase's known limitation in the canonical design doc and glossary.

Purpose: every later plan in this phase (gate primitives, acausal-placebo gate, the
`volatility_pct` pilot) implements or validates against this contract. It is the interface-first
task of the phase — the contract lands before anything that consumes it.

Output: `src/intelligence/stratification/` package with `contract.py`, a unit test file covering
Protocol conformance + D-01 naming + `ic_engine.py` compatibility, and ratification edits to
`docs/research/stratification-dimension-unification.md` and `docs/foundation/glossary.md`.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v3.1-phases/145-stratificationdimension-formalization-planned/145-CONTEXT.md
@.planning/milestones/v3.1-phases/145-stratificationdimension-formalization-planned/145-RESEARCH.md
@.planning/milestones/v3.1-phases/145-stratificationdimension-formalization-planned/145-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contracts the executor needs. Extracted from live code — no exploration required. -->

`services/ic_engine.py` (Ring 2 — READ ONLY, never import from src/intelligence/):

    def _resolve_regime_scope(is_pooled: bool, cross_sectional: bool) -> str
        # returns one of: "pooled" | "cross_sectional" | "symbol_hmm"

    def _build_regime_passes(
        regime_aligned_market: np.ndarray,
        distinct_regimes: list,
        regime_aligned: np.ndarray,
        cross_sectional: bool,
        dual_write_symbol_hmm: bool,
        cluster_regime_conditioned: bool,
        primary_resolved_scope: str,
    ) -> list[tuple[np.ndarray, list, str]]
        # each element is (label_array, distinct_labels, resolved_scope)

    class AmbiguousRegimeGroupError(ValueError)
    def _build_symbol_regime_class(
        tags_by_symbol: dict[str, set[str]], group_configs: list[dict]
    ) -> dict[str, str]   # symbol -> regime_group name

`src/intelligence/plugins/base.py` (ARCHIVED v2.x subsystem — shape reference ONLY, do not
import or subclass). Its `PatternPlugin(Protocol)` uses `ClassVar`-typed identity fields plus
instance methods; `PluginRegistry.validate_tier()` raises `ValueError` at startup on an
unregistered name. Mirror that shape, not its `compute_full`/`compute_next` split.

`src/intelligence/regime_signals/breadth_vol.py` (LIVE — the behavioral split to mirror):
module-level `PROB_KEYS`, `compute(ref_bars, params) -> tuple[pd.Series, pd.Series] | None`,
`build_tiers(params) -> tuple[list[tuple[str, float]], ...]`. No DB access inside the module.

Live `alpha.regime.groups` APR value contains regime_group names:
`equity`, `rates`, `commodity_energy`, `commodity_metals`, `commodity_agri`, `fx`.

Live `concept_transition_log` columns (target shape for later plans, informational here):
`id, concept_id, domain, name, from_status, to_status, trigger_reason, gate_metric, gate_n,
ci_lower, decay_ratio, regime_scope, corpus_build_ref, notes, triggered_at`.

**Hard live-schema constraint (psql-verified 2026-08-06):** `concept_registry.domain` CHECK is
`domain = ANY (ARRAY['feature','ensemble_strategy'])` and table `concept_gate_stack` does not
exist. This plan writes NO SQL and NO migration. Do not widen that CHECK (D-07 defers it to
Phase 170).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write the contract conformance test suite</name>
  <files>tests/unit/test_stratification_contract.py</files>
  <read_first>
    - tests/unit/test_ic_engine_routing.py (the project-root `sys.path` insert + class-per-behavior pattern, and the ONE sanctioned precedent for a test importing `services.ic_engine` internals)
    - tests/unit/test_regime_signals_breadth_vol.py (fixture-construction style for regime-signal modules)
    - src/intelligence/plugins/base.py (Protocol shape being mirrored — read, do not import)
    - .planning/milestones/v3.1-phases/145-stratificationdimension-formalization-planned/145-PATTERNS.md (section `tests/unit/test_stratification_contract.py`)
  </read_first>
  <behavior>
    - `registry_name("hmm_price_vol", "equity") == "hmm_price_vol__equity"` and
      `registry_name("hmm_price_vol", "rates") == "hmm_price_vol__rates"`; the two are not equal (D-01).
    - `parse_registry_name("hmm_price_vol__equity") == ("hmm_price_vol", "equity")` — round-trips
      `registry_name` for every regime_group name in the live `alpha.regime.groups` vocabulary
      (`equity`, `rates`, `commodity_energy`, `commodity_metals`, `commodity_agri`, `fx`).
    - `registry_name` raises `ValueError` when either argument already contains the `"__"`
      separator (ambiguous parse) or is empty.
    - D-02 agnosticism: `registry_name`/`parse_registry_name`/`validate_registration` accept an
      arbitrary regime_group string (e.g. `"some_future_group"`) with no special-casing — a
      parametrized test over all six live groups plus one invented group must behave identically.
    - A minimal conforming fake provider (defined in the test file) passes `validate_registration`
      when supplied a placebo gate callable that returns without raising.
    - A provider missing any of `name`/`grain`/`labels`/`causality_basis`/`observation_basis`
      raises `RegistrationError`.
    - A provider with `causality_basis="magic"` (outside the Literal vocabulary) raises `RegistrationError`.
    - A provider with `causality_basis="fitted"` and `fit_window=None` raises `RegistrationError`
      (D-05 rationale: `fitted` providers must declare their fit window).
    - A provider with `causality_basis="expanding_window"` and `fit_window=None` passes.
    - A provider with empty `labels`, or `labels` containing duplicates, raises `RegistrationError`.
    - `grain` outside `{"per_symbol", "cross_sectional"}` raises `RegistrationError`.
    - `validate_registration` propagates (does not swallow) an exception raised by the supplied
      placebo gate callable.
    - `validate_registration` cannot be invoked without a placebo gate: calling it with only the
      provider raises `TypeError` (structural proof D-05's precondition is unskippable).
    - ic_engine compat (`-k ic_engine_compat`): `to_regime_pass(labels_series, "volatility_pct")`
      returns a `(np.ndarray, list[str], str)` triple; feeding that triple's elements into
      `services.ic_engine._build_regime_passes` as `regime_aligned_market`/`distinct_regimes`/
      `primary_resolved_scope` (with `cross_sectional=False`, `dual_write_symbol_hmm=False`,
      `cluster_regime_conditioned=False`, `regime_aligned` = the same array) returns a
      single-element list whose entry equals the input triple. Import `_build_regime_passes` in the
      TEST only.
    - ic_engine compat: every value in `services.ic_engine._build_symbol_regime_class`'s output for
      a small fixture tag map round-trips through `registry_name`/`parse_registry_name` unchanged.
    - Ring-boundary regression: a source-level assertion that no `.py` file under
      `src/intelligence/stratification/` contains a `from services.` or `import services.` line,
      counted on comment-stripped content.
  </behavior>
  <action>
Create `tests/unit/test_stratification_contract.py` as a RED-first suite covering every item in
`<behavior>`. Follow `tests/unit/test_ic_engine_routing.py` exactly for the module header:
`from __future__ import annotations`, project-root `sys.path` insert via
`Path(__file__).parent.parent.parent`, class-per-behavior organization, no fixtures/mocks (these
are pure functions). CI-clean: no DB, no network.

Import the code under test as `from src.intelligence.stratification.contract import (
CausalityBasis, Grain, RegistrationError, StratificationDimension, parse_registry_name,
registry_name, to_regime_pass, validate_registration)`. These do not exist yet — the suite is
expected to fail at collection until Task 2 lands.

Define the fake providers inside the test file as plain classes with `ClassVar` attributes (no
inheritance from the Protocol needed — structural typing). Give the conforming one
`name="fake_dim"`, `grain="per_symbol"`, `labels=("lo","mid","hi")`,
`causality_basis="expanding_window"`, `fit_window=None`,
`observation_basis=frozenset({"realized_volatility"})`, and `compute`/`score` methods returning
a `pd.Series`.

For the Ring-boundary regression test, glob `src/intelligence/stratification/*.py`, strip lines
whose stripped form starts with `#`, and assert zero remaining lines match `from services.` or
`import services.`. Use the comment-stripped count, never a bare grep of the raw file.

Docstring convention (project-wide, see PATTERNS.md "cite the todo/incident"): the module
docstring must name Phase 145 and cite D-01/D-02/D-05 by ID as what each test group proves.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_stratification_contract.py -x -q 2>&1 | tail -5; test -f tests/unit/test_stratification_contract.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_stratification_contract.py` exists.
    - Running `.venv/bin/pytest tests/unit/test_stratification_contract.py -q` fails with an
      import/collection error naming `src.intelligence.stratification.contract` (RED state proven,
      not a vacuously passing suite).
    - `grep -c "def test_" tests/unit/test_stratification_contract.py` returns at least 14.
    - `grep -c "ic_engine_compat" tests/unit/test_stratification_contract.py` returns at least 1.
    - `grep -c "D-01" tests/unit/test_stratification_contract.py` returns at least 1.
  </acceptance_criteria>
  <done>The RED suite exists, fails for the right reason (contract module absent), and covers all listed behaviors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement the StratificationDimension contract module</name>
  <files>src/intelligence/stratification/__init__.py, src/intelligence/stratification/contract.py</files>
  <read_first>
    - tests/unit/test_stratification_contract.py (the RED suite from Task 1 — it is the spec)
    - src/intelligence/plugins/base.py (ClassVar Protocol shape + `validate_tier` hard-crash pattern; reference only, do not import)
    - src/intelligence/regime_signals/__init__.py (trivial package-init convention to mirror)
    - src/intelligence/regime_signals/breadth_vol.py (live `compute()`/`build_tiers()` split and module-docstring convention)
    - .planning/milestones/v3.1-phases/145-stratificationdimension-formalization-planned/145-CONTEXT.md (D-01, D-02, D-05, D-07 wording to cite in docstrings)
  </read_first>
  <behavior>
    Implementation must turn every assertion in `tests/unit/test_stratification_contract.py` green
    without modifying that test file.
  </behavior>
  <action>
Create `src/intelligence/stratification/__init__.py` mirroring
`src/intelligence/regime_signals/__init__.py` (module docstring only, no re-exports).

Create `src/intelligence/stratification/contract.py` exporting exactly:

- `Grain = Literal["per_symbol", "cross_sectional"]`
- `CausalityBasis = Literal["deterministic", "expanding_window", "fitted"]`
- `REGISTRY_NAME_SEPARATOR = "__"` (module constant)
- `class StratificationDimension(Protocol)` with `ClassVar` identity fields `name: ClassVar[str]`,
  `grain: ClassVar[Grain]`, `labels: ClassVar[tuple[str, ...]]`,
  `causality_basis: ClassVar[CausalityBasis]`, `fit_window: ClassVar[str | None]`,
  `observation_basis: ClassVar[frozenset[str]]`, plus instance methods
  `score(self, frame: pd.DataFrame, params: dict[str, Any]) -> pd.Series` and
  `compute(self, frame: pd.DataFrame, params: dict[str, Any]) -> pd.Series`. `score()` returns the
  continuous underlying value (percentile rank, posterior) that the orthogonality gate needs;
  `compute()` returns the discrete labels.
- `class RegistrationError(ValueError)` — plain undecorated subclass with a docstring stating why
  it exists and when it fires, matching `AmbiguousRegimeGroupError`'s minimalism (no custom
  `__init__`, no error-code taxonomy).
- `def registry_name(dimension: str, regime_group: str) -> str` — D-01 Option B encoder. Joins
  with `REGISTRY_NAME_SEPARATOR`. Raises `ValueError` on empty inputs or on either argument
  already containing the separator.
- `def parse_registry_name(name: str) -> tuple[str, str]` — inverse; raises `ValueError` when the
  name does not split into exactly two non-empty parts.
- `def to_regime_pass(labels: pd.Series, resolved_scope: str) -> tuple[np.ndarray, list[str], str]`
  — adapter producing the exact `(label_array, distinct_labels, resolved_scope)` triple shape that
  `services/ic_engine.py::_build_regime_passes` already emits and `_compute_symbol_tf` already
  consumes. `distinct_labels` excludes `None`/NaN, matching `_build_regime_passes`'s own
  `[r for r in set(regime_aligned) if r is not None]` filter. This module must NOT import
  `services.ic_engine`; the compatibility is asserted by the test, not by an import.
- `def validate_registration(provider: Any, placebo_gate: Callable[[Any], None]) -> None` —
  `placebo_gate` is a REQUIRED positional parameter with no default, so D-05's precondition cannot
  be silently skipped. Validation order (cheapest structural checks first, mirroring
  `_check_occupation_gate`'s guard-ordering discipline): required-attribute presence → `grain` in
  the `Grain` vocabulary → `causality_basis` in the `CausalityBasis` vocabulary → `labels`
  non-empty and duplicate-free → `fit_window` non-None when `causality_basis == "fitted"` → then
  call `placebo_gate(provider)`, letting any exception it raises propagate unchanged.

Docstrings must follow the project convention of citing the decision/incident that motivated the
shape: `registry_name`'s docstring cites D-01 and the Phase 144 D-05 verdict (HMM live-quality for
`equity`, deficient for `rates`) as why one row per `(dimension, regime_group)` is forced rather
than stylistic; `validate_registration`'s docstring cites D-05 and names todo 243 / todo 248 /
todo 204 as the three prior unverified-causality incidents; the module docstring cites D-02
(regime_group-agnostic: no fallback/shadow logic may be hard-coded to `rates` or any other group)
and D-07 (no `concept_registry` write happens in this phase — the live `domain` CHECK admits only
`feature`/`ensemble_strategy`, widening it is Phase 170's work).

Ring rule: this module imports only from stdlib, `numpy`, `pandas`, and `src/`. Zero imports from
`services/`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/test_stratification_contract.py -x -q && .venv/bin/ruff check src/intelligence/stratification/ tests/unit/test_stratification_contract.py && .venv/bin/black --check src/intelligence/stratification/ tests/unit/test_stratification_contract.py</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/unit/test_stratification_contract.py -x -q` passes with 0 failures.
    - `.venv/bin/pytest tests/unit/ -q` passes (no regression in the existing suite).
    - `grep -v '^\s*#' src/intelligence/stratification/*.py | grep -c "from services\.\|import services\." ` returns 0.
    - `grep -c "D-01" src/intelligence/stratification/contract.py` returns at least 1.
    - `grep -c "D-07" src/intelligence/stratification/contract.py` returns at least 1.
    - No file matching `production/migrations/*` was created or modified by this task.
    - `.venv/bin/ruff check src/intelligence/stratification/` and `.venv/bin/black --check src/intelligence/stratification/` both exit 0.
  </acceptance_criteria>
  <done>Contract module exists, full unit suite green, Ring boundary clean, lint/format clean.</done>
</task>

<task type="auto">
  <name>Task 3: Ratify Option B row-grain and the phase's accepted limitation in the canonical docs</name>
  <files>docs/research/stratification-dimension-unification.md, docs/foundation/glossary.md</files>
  <read_first>
    - docs/research/stratification-dimension-unification.md (read § The Contract lines ~280-345 and § Governance lines ~391-460 — the paragraphs being ratified)
    - docs/foundation/glossary.md (read the `regime`, `regime_group`, and `conditioning layer` entries around lines 75-130 — the existing vocabulary these additions must not collide with)
    - .planning/milestones/v3.1-phases/145-stratificationdimension-formalization-planned/145-CONTEXT.md (D-01 rationale and the `## Known, accepted limitation` block, verbatim source for this edit)
    - src/intelligence/stratification/contract.py (the shipped signatures being documented)
  </read_first>
  <action>
Edit `docs/research/stratification-dimension-unification.md`:

1. In § Governance, replace the "Mechanism note (2026-07-04, cluster review F2)" paragraph's
   provisional wording ("Provisionally: one `concept_registry` row per (dimension, regime_group)…
   Decide for real at v3.15 planning, not by assertion in this paragraph") with a RATIFIED
   statement: Option B is decided as of Phase 145 (D-01) — one `concept_registry` row per
   `(dimension, regime_group)`, encoded in `name` via the `__` separator (e.g.
   `hmm_price_vol__equity`, `hmm_price_vol__rates`), reusing the existing `UNIQUE(domain, name)`
   constraint, zero new columns, each cell carrying its own `status` and its own
   `concept_transition_log` history. State the forcing evidence plainly: Phase 144's D-05 verdict
   left the incumbent HMM simultaneously live-quality for `equity` and deficient for `rates`, which
   a single global-status row cannot represent. Cite the shipped encoder
   `src/intelligence/stratification/contract.py::registry_name`/`parse_registry_name`.
2. In the same section, state that the schema write itself is NOT done: `concept_registry.domain`
   is still CHECK-constrained to `('feature','ensemble_strategy')` and `concept_gate_stack` does
   not exist as a table (both psql-verified 2026-08-06), so `regime_model` rows cannot be written
   until Phase 170 lands. Phase 145's gate cascade is pure Python, not a DB-backed state machine.
3. In § The Contract, replace the Protocol sketch with a pointer to the shipped code, naming the
   module path and the six identity fields plus `compute()`/`score()`, and note the two additions
   beyond the original sketch: `fit_window` (required when `causality_basis == "fitted"`) and
   `observation_basis` (the frozenset gate 0's structural redundancy pre-filter reads).
4. Add a short new subsection under § Governance titled "Known limitation: candidate generation is
   theory-first" stating plainly, per CONTEXT.md's `## Known, accepted limitation` block, that
   candidate-dimension *generation* is human-curated/theory-first while *validation* is empirical
   via the three-gate cascade — the project's "empirical over theoretical" framing currently
   applies only to validation. State why it is not being fixed here (a data-driven generation step
   would multiply the FDR burden before the correction mechanism itself is proven) rather than
   implying the process is empirical end to end.
5. Add a `## Doc provenance` line or extend the existing header note recording that D-01 was
   ratified 2026-08-06 by `/gsd-discuss-phase 145`, record `145-CONTEXT.md` as the decision record.

Edit `docs/foundation/glossary.md`: add two new `###` entries in the same style and location
grouping as the existing `regime` / `regime_group` / `conditioning layer` entries:
- `stratification dimension` — one axis of conditioning; one provider implementing
  `src/intelligence/stratification/contract.py::StratificationDimension`, one label vocabulary,
  one `causality_basis`. State the label identity invariant: a label is only meaningful as a
  `(dimension, label)` pair; no consumer stores a bare label without its dimension qualifier.
  Note `feature_ic_scores.regime_scope` is the shipped 3-value special case of this rule.
- `stratum` — one cell within a dimension (one label value), and by extension one
  `(dimension, label)` or joint `(dimension_a label, dimension_b label)` measurement cell.
Also amend the existing `conditioning layer` entry with a one-line cross-reference to the new
contract module, without redefining it. Do NOT redefine `regime` — per the design doc it stays
reserved for the HMM price/vol dimension specifically and must not become a generic term.
  </action>
  <verify>
    <automated>grep -c "hmm_price_vol__equity" docs/research/stratification-dimension-unification.md && grep -c "### \`stratification dimension\`" docs/foundation/glossary.md && grep -c "### \`stratum\`" docs/foundation/glossary.md && ! grep -qi "Provisionally: one \`concept_registry\` row" docs/research/stratification-dimension-unification.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hmm_price_vol__equity" docs/research/stratification-dimension-unification.md` returns at least 1.
    - The literal string `Decide for real at v3.15 planning` no longer appears in
      `docs/research/stratification-dimension-unification.md`.
    - `grep -c "concept_gate_stack" docs/research/stratification-dimension-unification.md` returns
      at least 1 and the surrounding text states the table does not exist yet.
    - `docs/foundation/glossary.md` contains a `### \`stratification dimension\`` heading and a
      `### \`stratum\`` heading.
    - `grep -c "src/intelligence/stratification/contract.py" docs/research/stratification-dimension-unification.md` returns at least 1.
    - The glossary's existing `### \`regime\`` entry is unchanged in meaning (still scoped to the
      HMM price/vol dimension, not generalized).
    - No em dashes introduced in newly written prose (project writing rule).
  </acceptance_criteria>
  <done>Option B is ratified (not provisional) in the canonical design doc, the live-schema blocker is documented, the theory-first generation limitation is stated plainly, and the two new vocabulary terms exist in the glossary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider self-declaration → governance gate | A `StratificationDimension` implementation asserts its own `causality_basis`; nothing external verifies the claim without a gate |
| design doc → executable code | A ratified decision that lives only in prose can silently diverge from the shipped encoder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-01 | Tampering | `causality_basis` ClassVar on any provider | mitigate | `validate_registration` takes `placebo_gate` as a required positional argument with no default, so D-05's empirical check cannot be skipped by omission; a test asserts calling it without the gate raises `TypeError` |
| T-145-02 | Tampering | `fitted` providers with undeclared fit window | mitigate | `validate_registration` raises `RegistrationError` when `causality_basis == "fitted"` and `fit_window is None` — the exact ambiguity todo 026/248 found in the incumbent HMM |
| T-145-03 | Repudiation | D-01 ratification recorded in prose only | mitigate | `registry_name`/`parse_registry_name` are the executable encoding, unit-tested across all six live regime_group names; the doc cites the module path |
| T-145-04 | Elevation of Privilege | Ring 1 → Ring 2 import creep | mitigate | Source-level regression test asserts zero `from services.`/`import services.` lines in `src/intelligence/stratification/*.py`, counted on comment-stripped content |
| T-145-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages; RESEARCH.md confirmed `statsmodels`/`scipy` are pre-existing dependencies and no `requirements.txt`/`pyproject.toml` change is needed. Package Legitimacy Gate not applicable |
</threat_model>

<verification>
1. `.venv/bin/pytest tests/unit/test_stratification_contract.py -v` — all green.
2. `.venv/bin/pytest tests/unit/ -q` — full unit suite green, no regression.
3. `.venv/bin/ruff check . --fix && .venv/bin/black .` — clean.
4. `grep -v '^\s*#' src/intelligence/stratification/*.py | grep -c "from services\."` returns 0.
5. `git status --short` shows no changes under `production/migrations/`.
</verification>

<success_criteria>
- `src/intelligence/stratification/contract.py` exists and exports `StratificationDimension`,
  `CausalityBasis`, `Grain`, `RegistrationError`, `registry_name`, `parse_registry_name`,
  `to_regime_pass`, `validate_registration`.
- D-01's Option B encoding produces distinct names per `(dimension, regime_group)` and round-trips.
- D-02 agnosticism is proven by a parametrized test over all six live regime groups plus an
  invented one, with identical behavior.
- `validate_registration` cannot run without a placebo gate callable.
- `ic_engine.py` is byte-unchanged; compatibility is asserted only from the test file.
- Design doc ratifies Option B and documents the live-schema blocker; glossary carries the two new
  vocabulary entries.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/145-stratificationdimension-formalization-planned/145-01-SUMMARY.md` when done
</output>
