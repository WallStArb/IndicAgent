---
phase: 163-vp-sr-structural-primitives
plan: 02
type: execute
wave: 2
depends_on: ["163-01"]
files_modified:
  - src/intelligence/feature_factory.py
  - services/feature_vector_pipeline.py
  - tests/unit/intelligence/test_volume_profile_primitives.py
autonomous: true
requirements: ["TODO-153", "D-03", "D-05", "D-13", "D-16"]

must_haves:
  truths:
    - "poc_dist_atr and va_position vary across bars (not stuck at 0.0 / 0.5) in both live compute() and batch compute_batch()"
    - "The 12 new VP fields carry real ATR-normalized/bounded values, equal between live and batch for the same bar sequence"
    - "The stale 'requires I3 intraday injection' None-branch is removed from compute_batch(); batch path computes VP for real"
    - "cache.update_session_vp() is invoked once per bar in both the live pipeline and compute_batch (including warmup bars)"
  artifacts:
    - path: "src/intelligence/feature_factory.py"
      provides: "VP field derivation (ATR-normalization from cache raw levels + atr_val) across all FeatureVector construction sites"
      contains: "distance_to_vah_atr"
    - path: "tests/unit/intelligence/test_volume_profile_primitives.py"
      provides: "regression test: VP fields non-constant + live==batch parity"
      contains: "def test_"
  key_links:
    - from: "services/feature_vector_pipeline.py _process_bar_compute"
      to: "cache.update_session_vp()"
      via: "per-bar call before FeatureFactory.compute()"
      pattern: "update_session_vp"
    - from: "src/intelligence/feature_factory.py compute()/compute_batch()"
      to: "cache._sess_poc / atr_val"
      via: "ATR-normalized derivation of poc_dist_atr, va_position, *_dist_atr"
      pattern: "_sess_poc"
---

<objective>
Wire the session-VP mutator (Plan 01) into both compute paths and derive the 14 VP FeatureVector
outputs (the 2 original VP fields `poc_dist_atr`/`va_position` + the 12 new ATR-distance/bounded
fields) from the cache's raw session levels plus the compute-path `atr_val`. This is where VP goes
from "computed internally" to "persisted for real," closing the live+batch half of todo 153 for
volume profile. Also removes the factually-wrong "requires I3 intraday injection" batch None-branch
(D-05).

Purpose: A user querying `feature_vectors` after a corpus run sees non-constant, ATR-normalized VP
values (not the frozen 0.0/0.5 defaults), identical between the live streaming path and the batch
backfill path.
Output: Wired call sites, derived outputs at every FeatureVector construction site, and a regression
unit test that would have caught the original todo-153 bug.
</objective>

<execution_context>
@/home/bg/dev/indicagent/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/dev/indicagent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v3.1-phases/163-vp-sr-structural-primitives/163-CONTEXT.md
@.planning/milestones/v3.1-phases/163-vp-sr-structural-primitives/163-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: FeatureCache carries internal raw SESSION levels; derive ATR outputs here. -->
<!-- The ROLLING-480 track (poc_price_rolling) is NOT on the cache — compute it stateless inline (D-18). -->
FeatureCache internal state (set by update_session_vp, Plan 01):
  _sess_poc, _sess_vah, _sess_val, _sess_hvn_above, _sess_hvn_below,
  _sess_lvn_above, _sess_lvn_below, _sess_hvn_nearest, _sess_price_in_va, _sess_in_lvn   # RAW prices / flags

FeatureVector fields to populate (D-16 corrected, ATR-normalized/bounded):
  poc_dist_atr        = (close - _sess_poc) / atr_val
  va_position         = clamp((close - _sess_val) / (_sess_vah - _sess_val), 0, 1)
  nearest_hvn_above_dist_atr = (_sess_hvn_above - close) / atr_val   # None if node absent
  nearest_hvn_below_dist_atr = (close - _sess_hvn_below) / atr_val
  nearest_lvn_above_dist_atr = (_sess_lvn_above - close) / atr_val
  nearest_lvn_below_dist_atr = (close - _sess_lvn_below) / atr_val
  nearest_hvn_dist_atr       = abs(close - _sess_hvn_nearest) / atr_val   # legacy, keep sign convention of source
  va_width_atr               = (_sess_vah - _sess_val) / atr_val   # D-17: == distance_to_vah_atr + distance_to_val_atr (exact collinearity); keep + add code comment
  distance_to_vah_atr        = (_sess_vah - close) / atr_val
  distance_to_val_atr        = (close - _sess_val) / atr_val
  price_in_value_area        = _sess_price_in_va   # already 0/1, no ATR
  in_lvn                     = _sess_in_lvn         # already 0/1, no ATR (liquidity-void flag, D-17)
  # Rolling-480 track (D-18): poc_price_rolling computed STATELESS inline from last
  # config.session_vp_rolling_window bars via ported _compute_profile/_compute_value_area;
  # intermediate value, NOT persisted as a raw level.
  poc_rolling_dist_atr               = (close - poc_price_rolling) / atr_val   # guard atr_valid and poc_price_rolling is not None
  poc_session_rolling_divergence_atr = (_sess_poc - poc_price_rolling) / atr_val   # D-18: == poc_rolling_dist_atr - poc_dist_atr (exact identity); keep + code comment

FeatureVector construction sites (enumerate with grep, see action):
  _build_feature_vector helper (~L3060-3266), compute() live (~L3530), compute_batch (~L4090),
  _cold_start_vector (~L4266).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire update_session_vp call sites + derive 14 VP outputs; remove D-05 None-branch</name>
  <files>src/intelligence/feature_factory.py, services/feature_vector_pipeline.py</files>
  <behavior>
    - On an intraday bar sequence, poc_dist_atr changes as close moves relative to the session POC (not frozen 0.0).
    - va_position is bounded [0,1] and moves toward 1.0 as close approaches session VAH, 0.0 toward VAL.
    - When atr_val == 0 (cold start) or a directional node is absent, the corresponding field falls back cleanly (0.0 / None) without raising.
    - compute() (live) and compute_batch() produce identical VP values for the same ordered bars.
    - poc_rolling_dist_atr and poc_session_rolling_divergence_atr are finite and generally differ from poc_dist_atr early in a session (rolling window dominated by prior-session bars), converging near session close.
    - tf == '1d' keeps neutral defaults for session-anchored VP fields (a single daily bar has no intraday distribution).
  </behavior>
  <read_first>
    - src/intelligence/feature_factory.py lines 3400-3572 (compute() live path: atr_val at 3441, session-level block 3446-3455, FeatureVector construction ~3530-3572)
    - src/intelligence/feature_factory.py lines 3700-3745 (compute_batch signature + the D-05 docstring at 3719-3726)
    - src/intelligence/feature_factory.py lines 3748-3766 (loop head + warmup-skip + advance_bar at 3759)
    - src/intelligence/feature_factory.py lines 3814-3841 (batch session-level None-branch to remove)
    - src/intelligence/feature_factory.py lines 4090-4136 (batch FeatureVector construction) and 4245-4312 (advance_bar 4248, _cold_start_vector)
    - src/intelligence/feature_factory.py lines 3060-3266 (_build_feature_vector helper: params 3084-3087, construction 3220-3266)
    - services/feature_vector_pipeline.py lines 895-965 (_process_bar_compute; compute() call at 935)
  </read_first>
  <action>
    (1) Add a per-bar `cache.update_session_vp(bar_ts, high, low, close, volume, config)` call:
      - Live: in services/feature_vector_pipeline.py `_process_bar_compute`, immediately BEFORE the
        `FeatureFactory.compute(bars_dicts, ...)` call (~line 935), using the current bar's ts/OHLCV
        and the prewarmed FeatureFactoryConfig. (Note: this is a NEW call site — the live pipeline
        does not currently call advance_bar/update_wk_vwap; add it here explicitly.)
      - Batch: in compute_batch's loop, call `cache.update_session_vp(bar_ts, high_, low_, close_,
        vol_, config)` for EVERY bar including warmup — place it before the `if i < warm_up_bars`
        skip (line ~3757) so the session accumulator stays current through warmup. Ensure bar_ts is
        UTC-aware (reuse the existing tzinfo guard).
    (2) Compute the rolling-480 track POC statelessly inline in compute() and compute_batch() (D-18):
      over the last config.session_vp_rolling_window bars (compute(): bars[-N:]; compute_batch:
      bars[max(0, i-N+1):i+1]) call the ported _compute_profile + _compute_value_area to obtain
      poc_price_rolling. Intermediate value — do NOT persist it as a raw column.
    (3) Add a single private helper `_derive_session_vp(cache, close_, atr_val, poc_price_rolling)` in
      feature_factory.py returning the 14 VP values (dict or tuple) using the ATR-normalization
      formulas in the <interfaces> block. Clamp va_position to [0,1]; return 0.0 for poc_dist_atr/
      va-derived fields and None for directional *_dist_atr / poc_rolling_dist_atr /
      poc_session_rolling_divergence_atr when the underlying raw level is None or atr_val <= 0. Pass
      through in_lvn (0/1) from cache. Add a one-line code comment on the va_width_atr line noting it
      equals distance_to_vah_atr + distance_to_val_atr exactly (D-17), and on the
      poc_session_rolling_divergence_atr line noting it equals poc_rolling_dist_atr - poc_dist_atr
      exactly (D-18) — both documented collinearities kept deliberately.
    (4) Replace the session-level VP assignment blocks with calls to `_derive_session_vp` (passing the
      inline-computed poc_price_rolling):
      - Live compute() (currently 3446-3455): keep the `if tf == '1d'` neutral branch; else derive
        from cache raw levels + atr_val.
      - compute_batch() (currently 3814-3841): REMOVE the entire `if cross_asset_by_date is not None:
        poc_dist_atr_val = None ...` None-branch (D-05). Replace with: `if tf == '1d'` neutral, else
        `_derive_session_vp(cache, close_, atr_val)`. Leave sr_support_dist_val / sr_resist_dist_val
        reading `cache.sr_support_dist`/`cache.sr_resist_dist` UNCHANGED (Plan 03 makes S/R real).
      - _cold_start_vector (4256-4264): neutral defaults for all VP fields (cold start has no session).
    (5) Thread the 12 new fields through EVERY FeatureVector construction site. First run
      `grep -n "poc_dist_atr=" src/intelligence/feature_factory.py` to enumerate all sites, then add
      the 12 new `<field>=<value>` kwargs adjacent to `poc_dist_atr=`/`va_position=` at each site
      (helper _build_feature_vector params + body, compute() live, compute_batch, _cold_start_vector).
      Update the _build_feature_vector helper signature (params near 3084-3087) to accept the 12 new args.
    (6) Fix the compute_batch docstring (3719-3726) to remove the "requires I3 intraday injection"
      claim; state VP is computed from OHLCV in both paths.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_volume_profile_primitives.py -x -q</automated>
    <automated>grep -n "requires I3 intraday injection" src/intelligence/feature_factory.py | grep -v '^#' | wc -l | grep -qx 0</automated>
    <automated>.venv/bin/python -c "import ast,sys; src=open('src/intelligence/feature_factory.py').read(); assert 'update_session_vp' in open('services/feature_vector_pipeline.py').read(); print('wired')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "poc_dist_atr=" src/intelligence/feature_factory.py` equals the count of `nearest_hvn_above_dist_atr=` (every VP construction site carries all fields).
    - No occurrence of `= None` VP assignment tied to `cross_asset_by_date` remains; `grep -n "requires I3" src/intelligence/feature_factory.py` returns nothing.
    - `services/feature_vector_pipeline.py` calls `cache.update_session_vp(` exactly once in `_process_bar_compute` before `FeatureFactory.compute(`.
    - Live compute() and compute_batch() produce equal VP values for an identical ordered bar list (asserted in Task 2 test).
    - `.venv/bin/ruff check src/intelligence/feature_factory.py services/feature_vector_pipeline.py` clean.
  </acceptance_criteria>
  <done>VP outputs are derived and persisted at every construction site in both paths; the stale I3 None-branch is gone; the mutator is invoked per bar in live and batch.</done>
</task>

<task type="auto">
  <name>Task 2: Regression unit test — VP non-constant + live/batch parity</name>
  <files>tests/unit/intelligence/test_volume_profile_primitives.py</files>
  <read_first>
    - src/intelligence/feature_factory.py (compute / compute_batch signatures and FeatureFactoryConfig construction defaults)
    - tests/unit/intelligence/ (an existing FeatureFactory test for the harness/fixture pattern — grep for compute_batch usage)
    - src/intelligence/feature_cache.py (FeatureCache constructor / update_session_vp)
  </read_first>
  <action>
    Create tests/unit/intelligence/test_volume_profile_primitives.py. Build a realistic single-session
    intraday bar sequence (~60-120 1m/5m bars with varying high/low/close/volume spanning a price
    range, timestamps within one NY session so no reset fires mid-sequence). Construct a
    FeatureFactoryConfig with the session_vp defaults.
    Tests:
    (a) test_vp_fields_non_constant_batch: run FeatureFactory.compute_batch over the sequence; collect
      poc_dist_atr and va_position across bars; assert each has >1 distinct finite value (this is the
      regression that would have caught the frozen-default bug — assert NOT all == 0.0 and NOT all == 0.5).
      Assert va_position values are within [0,1]. Assert at least one of the 12 new fields
      (e.g. distance_to_vah_atr) is non-null and non-constant, and that poc_rolling_dist_atr and
      poc_session_rolling_divergence_atr are finite and non-constant across bars.
    (b) test_live_batch_parity: feed the SAME ordered bars one-at-a-time through the live pattern
      (cache.update_session_vp then FeatureFactory.compute on growing history) and compare the final
      bar's VP FeatureVector fields to the corresponding compute_batch row; assert equal within a
      tight tolerance (1e-6) for the shared fields.
    (c) test_no_raw_price_persisted: assert FeatureVector has no attribute named poc_price/vah/val/
      poc_price_rolling/vah_rolling/val_rolling/nearest_hvn_level/nearest_lvn_level (D-16/D-18 guard —
      poc_price_rolling is used only as an intermediate).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/intelligence/test_volume_profile_primitives.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All three tests pass and are CI-clean (`.venv/bin/pytest tests/unit/intelligence/test_volume_profile_primitives.py -q`).
    - test_vp_fields_non_constant_batch fails if poc_dist_atr or va_position is reverted to a constant default (verify by temporarily forcing a constant locally, then reverting — document in SUMMARY, do not commit the break).
    - test_no_raw_price_persisted enumerates the forbidden raw-price attribute names explicitly.
  </acceptance_criteria>
  <done>A committed regression test proves VP fields are non-constant and live==batch, and guards against raw-price persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| market_data_ohlcv_tradeable -> FeatureFactory | Internal validated OHLCV; no external input crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-03 | Tampering (silent wrong data) | Live/batch VP divergence produces path-dependent training corpus | mitigate | test_live_batch_parity asserts equality; single _derive_session_vp helper used by both paths. |
| T-163-04 | Repudiation (undetected regression) | VP silently reverts to constants in future refactors (todo-153 recurrence) | mitigate | test_vp_fields_non_constant_batch is a committed regression gate. |
| T-163-SC | Tampering (supply chain) | package installs | accept | No new packages introduced. |
</threat_model>

<verification>
- pytest test_volume_profile_primitives passes.
- poc_dist_atr / va_position are non-constant across a realistic bar sequence in the batch path.
- Live and batch VP outputs match to 1e-6 for the same bars.
- No "requires I3" text and no cross_asset-tied None assignment remain in feature_factory.py.
</verification>

<success_criteria>
Volume-profile structural primitives compute real, ATR-normalized, non-constant values persisted
identically in the live and batch paths, with a regression test guarding the todo-153 failure mode
and D-16's no-raw-price rule.
</success_criteria>

<output>
Create `.planning/milestones/v3.1-phases/163-vp-sr-structural-primitives/163-02-SUMMARY.md` when done.
</output>
