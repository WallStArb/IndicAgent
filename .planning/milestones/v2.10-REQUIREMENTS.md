# Requirements Archive: v2.10 Data Architecture Evolution

**Milestone:** v2.10
**Defined:** 2026-06-14
**Archived:** 2026-06-20
**Status:** COMPLETE (with noted gaps)

---

## v1 Requirements

### ECL Boundary Restoration (Phase 123)

- [x] **ECL-01**: Zero I7 plugins call `no_signal()` based on any extrinsic vector (CTF score, zone_friction, exhaustion state) — all extrinsic vectors are annotations on the emitted signal, never emission gates
- [x] **ECL-02**: `signal_events` schema has five new fields: `ctf_score`, `ctf_confirmed`, `zone_friction_score`, `factor_scores` (JSONB), `context_features` (JSONB) — all populated at emit time
- [x] **ECL-03**: `SIGNAL_SCHEMA_VERSION` is incremented; `context_features` is populated in `signal_processor.py` (not the `_shadow` dict); all 37 I7 plugins collect `factor_scores` dict before compositing

### Signal Universe Integrity (Phase 124)

- [x] **QUALITY-01**: All 5 over-firing plugins (firing > 3%/bar) are corrected to event-onset detection; post-fix fire rate confirmed < 3%/bar per SQL validation
- [x] **QUALITY-02**: `intelligence_features` ON CONFLICT guard uses `WHERE ctf_score IS NULL` only — the `OR ctf_score = 0.0` branch that overwrites valid neutral-CTF readings is removed; `--warmup` pass operational in `run_historical_pipeline.py`

Note: QUALITY-01 and QUALITY-02 were marked [x] complete in requirement body but showed as Pending in traceability table — documentation tracking gap, not a code gap. Phase 124 has 7/7 SUMMARYs.

### APR Full Migration (Phase 125)

- [x] **APR-01**: All 26 Tier A detection gate constants (`threshold.*`, `feature.*`) externalized to `config_state`; zero hard-coded values in `src/` (grep confirms); all Tier A plugins load from ConfigService at `compute_full()` time
- [x] **APR-02**: All 22 Tier B confidence weight constants (`weights.*`) externalized to `config_state`; weight sum invariant enforced via `_validate_weights_sum()` in all Tier B plugins
- [x] **APR-03**: All 6 Tier C zone engine geometry constants (`feature.zone_engine.*`, `weights.zone_engine.*`) externalized to `config_state`; `zone_engine.py` loads from ConfigService at startup

### Signal Universe Hardening (Phase 126)

- [x] **SIGNAL-QUALITY-01**: `frame_trade()` in `trade_framer.py` calls `_reject_frame("zone_too_narrow:{zone_source}", ...)` AFTER `_resolve_zone_bounds()` returns, when `zone_width < min_zone_width_atr × ATR`; gate applies to all zone source types (supply_demand, fvg, ob, structural, sweep_band, atr_fallback); APR keys `feature.zone_engine.min_zone_width_atr.equity` = 1.5, `feature.zone_engine.min_zone_width_atr.fx` = 1.0, `feature.zone_engine.min_zone_width_atr.futures` = 1.5; `stopped_at_entry` rate < 15% measurement deferred to Phase 127 (REPLAY-01)
- [x] **SIGNAL-QUALITY-02**: `_I7_I6_EXEMPT` frozenset deleted from `register_plugins.py`; ECL annotation moved to pipeline layer via `signal_processor._annotate_signal()`; per-plugin `requires_i6_confluence=True` and `capture_signal_features()` calls removed; `capture_signal_features()` marked DEPRECATED in `confidence_utils.py`; `SIGNAL_SCHEMA_VERSION` bumped to "v4" in `signal_schema.py`; all 8 formerly-exempt plugins remain in TIER_I7 with full ECL annotation via pipeline; anti-signal plugins demoted to `shadow_only=True`

### Clean Replay and Validation (Phase 127)

- [x] **REPLAY-01**: Full historical replay completes on the corrected pipeline (Phases 123-126 applied) with `--warmup`; `context_features` coverage > 99% for non-cold-start signals; all 5 over-firing plugins confirmed < 3% fire rate in replay output
- [x] **REPLAY-02**: Phase 121-02 deferred validation report produced using correct methodology — signal volume delta (pre/post ECL fix), CTF as feature analysis, firing rate distribution, cold-start null distribution; no cross-population Welch's t-test; calibration curves retrained on clean corpus; RCA Part VI updated

Note: REPLAY-01 and REPLAY-02 showed as Pending in traceability table but Phase 127 has 3/3 SUMMARYs. Documentation tracking gap.

### 3-Table Signal Architecture (Phases 128-130)

- [x] **ARCH-01**: `signal_events`, `trade_frames`, `trade_executions` tables created with full schemas, FK constraints, and indexes as defined in `docs/plans/2026-06-14-v2.10-signal-architecture-refactor.md`; `signal_ledger_full` backward-compat view deployed; `counterfactual_pnl_r` is a required first-class column on `trade_frames`
- [x] **MIGRATE-01**: All `signal_ledger` data migrated into the 3-table schema with row-count verification (1,443,231 rows; 0 failures; 0 orphaned frames); `signal_ledger` retained read-only during transition window
- [x] **REWRITE-01**: All writers, trackers, auditors, API endpoints, and historical backfill scripts write to and read from the 3-table schema; `signal_ledger` dropped after verification window (migration 143); `signal_ledger` (renamed from signal_ledger_full in Phase 130) is the sole backward-compatibility surface

Note: ARCH-01 showed as Pending in traceability table. Phase 128 VERIFICATION noted this gap. Phase 128 has 2/3 SUMMARYs. Documentation gap, no code defect.

---

## Notes on Requirement Tracking Gaps

At milestone close, 5 requirements appeared "Pending" in the traceability table (QUALITY-01/02, REPLAY-01/02, ARCH-01) despite their phases being complete. These were documentation tracking failures — the REQUIREMENTS.md traceability table was not updated when phases closed. The code changes were made and verified. Future milestones: update traceability table at phase completion time, not just the checkbox.

---

## Future Requirements (carried to v2.11)

| Requirement | Trigger |
|-------------|---------|
| CounterfactualTracker daemon — populates `counterfactual_pnl_r` on every trade frame regardless of execution | Requires `trade_frames` table (Phase 128) — now exists |
| I6 DB bootstrap at daemon startup — eliminates cold-start permanently | Requires `intelligence_features` accumulation |
| APR ML optimization — regress `factor_scores` against `counterfactual_pnl_r` to discover optimal weights | Requires 30-90 days of `counterfactual_pnl_r` data; all 54 APR keys already externalized (Phase 125) |
| SignalRanker (LightGBM) — replace calibration + `perf_multiplier` chain with end-to-end trained ranking model | Requires `context_features` (Phase 123) + `counterfactual_pnl_r` (Phase 130 CounterfactualTracker) |

---

## Out of Scope (Confirmed)

| Feature | Reason |
|---------|--------|
| Moving signal generation or trade framing out of IntelligencePipeline | Principle 12 (Signal Generation Invariant) — settled |
| Execution engine / order routing | Intelligence platform only |
| v2.8 Part 2 AI platform phases (096-099, 101-103) | Separate milestone |
| CounterfactualTracker daemon | v2.11 — requires trade_frames to exist and accumulate first |
| Phase 133 Clean Corpus Rebuild | CANCELLED: superseded by v3.0 IC measurement on intelligence_features |
| Phase 135 Controlled Vocabulary System | DEFERRED: superseded by v3.0 priority |

---

*Requirements defined: 2026-06-14*
*Archived at milestone close: 2026-06-20*
