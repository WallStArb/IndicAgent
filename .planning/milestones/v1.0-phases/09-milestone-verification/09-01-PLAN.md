---
phase: 09-milestone-verification
plan: 01
type: verification
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-historical-data/03-VERIFICATION.md
autonomous: true
requirements:
  - HST-01
  - HST-02
  - HST-03
must_haves:
  truths:
    - "03-VERIFICATION.md exists with status: passed"
    - "HST-01: signal_ledger has 2700+ signals (query live DB)"
    - "HST-02: intelligence_features has rows with feature history"
    - "HST-03: signal_ledger signals have matching intelligence_features rows (0 orphans)"
  artifacts:
    - path: ".planning/phases/03-historical-data/03-VERIFICATION.md"
      provides: "Phase 03 formal verification artifact"
      contains: "HST-01"
---

<objective>
Write Phase 03 VERIFICATION.md by querying the live database for actual row counts and JOIN integrity. HST-01 requires 2,700+ signals in signal_ledger. HST-02 requires intelligence_features rows. HST-03 requires 0 orphan signals (signals without matching features).

Purpose: Formally verify Phase 3 (Historical Data) which has no VERIFICATION.md artifact — closes unverified phase gap from v1.0 audit.
Output: .planning/phases/03-historical-data/03-VERIFICATION.md with status: passed or gaps_found.
</objective>

<tasks>

<task type="verification">
  <name>Task 1: Query live DB for Phase 03 verification data</name>
  <files>
    .planning/phases/03-historical-data/03-VERIFICATION.md
  </files>
  <action>
    Run these queries to gather verification data:

    ```python
    import asyncio, asyncpg
    from src.config.settings import Settings

    async def verify():
        s = Settings()
        conn = await asyncpg.connect(s.database_url)

        # HST-01: signal count
        r = await conn.fetchrow("SELECT COUNT(*) as n FROM signal_ledger")
        print(f"HST-01 signal_ledger rows: {r['n']}")

        # HST-02: intelligence_features count
        r = await conn.fetchrow("SELECT COUNT(*) as n FROM intelligence_features")
        print(f"HST-02 intelligence_features rows: {r['n']}")

        # HST-03: orphan check (signals with feature_ts but no matching feature row)
        r = await conn.fetchrow("""
            SELECT COUNT(*) as orphans
            FROM signal_ledger sl
            WHERE sl.feature_ts IS NOT NULL
              AND NOT EXISTS (
                SELECT 1 FROM intelligence_features f
                WHERE f.ts = sl.feature_ts AND f.symbol = sl.symbol AND f.timeframe = sl.feature_tf
              )
        """)
        print(f"HST-03 orphan signals: {r['orphans']}")

        # Date coverage
        r = await conn.fetchrow("""
            SELECT MIN(timestamp) as earliest, MAX(timestamp) as latest
            FROM signal_ledger
        """)
        print(f"Date range: {r['earliest']} to {r['latest']}")

        await conn.close()

    asyncio.run(verify())
    ```

    Record the actual numbers, then write 03-VERIFICATION.md.

    Mark HST-01 satisfied if signal_ledger >= 2700 signals.
    Mark HST-02 satisfied if intelligence_features > 0 rows.
    Mark HST-03 satisfied if orphans == 0.

    Set status: passed if all 3 pass, gaps_found if any fail.
  </action>
  <verify>
    cat .planning/phases/03-historical-data/03-VERIFICATION.md | head -5
    # Should show status: passed or status: gaps_found
  </verify>
  <done>
    03-VERIFICATION.md written with actual DB counts and requirement status.
  </done>
</task>

</tasks>

<verification>
1. cat .planning/phases/03-historical-data/03-VERIFICATION.md — file exists with status field
2. status is passed if signal_ledger >= 2700, intelligence_features > 0, orphans == 0
</verification>

<success_criteria>
- 03-VERIFICATION.md exists with status: passed
- HST-01, HST-02, HST-03 all verified against live DB
- Actual row counts documented
</success_criteria>

<output>
After completion, create `.planning/phases/09-milestone-verification/09-01-SUMMARY.md` using the summary template.
</output>
