---
phase: 128-3-table-schema-design-and-adr
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/trading/confidence_utils.py
  - src/intelligence/CLAUDE.md
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "capture_signal_features() function is deleted from confidence_utils.py"
    - "SHADOW_FEATURE_KEYS in feature_builder.py is untouched"
    - "No live caller of capture_signal_features() exists in src/"
    - "src/intelligence/CLAUDE.md updated to remove the capture_signal_features() entry"
  artifacts:
    - path: "src/intelligence/trading/confidence_utils.py"
      provides: "Cleaned confidence utilities without deprecated function"
      contains: "compose_confidence"
  key_links:
    - from: "src/intelligence/trading/confidence_utils.py"
      to: "capture_signal_features deletion"
      via: "grep -n 'def capture_signal_features' returns no results"
      pattern: "def capture_signal_features"
---

<objective>
Delete the deprecated capture_signal_features() function from confidence_utils.py. Research confirmed zero live callers — this was deferred from Phase 126 (D-10) and is now safe to execute. Update src/intelligence/CLAUDE.md to remove the stale reference.

Purpose: Remove dead code that references the old signal monolith pattern. The function was the sole signal feature capture mechanism before _annotate_signal() replaced it in Phase 126.
Output: Cleaned confidence_utils.py, updated CLAUDE.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-3-table-schema-design-and-adr/128-CONTEXT.md
@.planning/phases/128-3-table-schema-design-and-adr/128-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight caller audit and delete capture_signal_features()</name>
  <read_first>
    - src/intelligence/trading/confidence_utils.py (full file — understand the function body, what surrounds it, and what must be preserved: CONF_CEIL, MIN_REGIME_WEIGHT, MIN_CTF_SCORE, compose_confidence, ConfluenceWeightProfile)
    - src/intelligence/CLAUDE.md (find the confidence_utils.py row in the shared utilities table — line 26 per research)
    - src/intelligence/ml/feature_builder.py (confirm SHADOW_FEATURE_KEYS is a standalone constant, not a call to capture_signal_features)
  </read_first>
  <files>
    src/intelligence/trading/confidence_utils.py
    src/intelligence/CLAUDE.md
  </files>
  <action>
    Step 1 — Run pre-flight caller check before touching any file:
    grep -rn "capture_signal_features(" src/ --include="*.py"
    Expected output: only the definition line in confidence_utils.py. If any other .py file appears as a caller (not a comment), STOP and report — do not delete.

    Step 2 — Confirm SHADOW_FEATURE_KEYS safety:
    grep -n "SHADOW_FEATURE_KEYS" src/intelligence/ml/feature_builder.py
    Expected: a constant assignment (list or frozenset of key name strings). Must NOT be a call to capture_signal_features(). If it is a call, STOP and report.

    Step 3 — Delete capture_signal_features() from confidence_utils.py:
    - Read the full file first (see read_first).
    - Identify the function definition block: starts at "def capture_signal_features(" and includes its full docstring and body.
    - Also identify and remove the module-level docstring paragraph about capture_signal_features() (the block in the module docstring that starts "capture_signal_features() captures I4 macro context..." — this describes the function and should be removed when the function is deleted).
    - Preserve everything else in the file: CONF_CEIL constant, MIN_REGIME_WEIGHT constant, MIN_CTF_SCORE constant, compose_confidence() function, ConfluenceWeightProfile dataclass, _config_service lazy loader, and any imports still needed by the remaining code.
    - Remove any imports that were only used by capture_signal_features() (check for Any import from typing if used only there; keep if used elsewhere).
    - Write the cleaned file.

    Step 4 — Update src/intelligence/CLAUDE.md:
    - Find the row in the shared utilities table for confidence_utils.py. It currently includes "capture_signal_features()" in the Key exports column and a description of the 30-key shadow dict.
    - Remove "capture_signal_features()" from the Key exports column entry for confidence_utils.py.
    - Remove the shadow dict description sentence that begins "capture_signal_features() writes _shadow dict...".
    - Preserve ConfluenceWeightProfile and compose_confidence() references in that row.
    - No other changes to CLAUDE.md.

    Step 5 — Post-deletion verification:
    grep -rn "capture_signal_features" src/ --include="*.py"
    Expected: zero results. If any .py file still references it (as a call, not a comment), that is a regression — fix it.
  </action>
  <verify>
    grep -rn "def capture_signal_features" src/intelligence/trading/confidence_utils.py || echo "CORRECT: function deleted"
    grep -rn "capture_signal_features(" src/ --include="*.py" | grep -v "^Binary" || echo "CORRECT: zero live callers"
    grep -n "SHADOW_FEATURE_KEYS" src/intelligence/ml/feature_builder.py
    grep -n "compose_confidence" src/intelligence/trading/confidence_utils.py
    grep -n "CONF_CEIL" src/intelligence/trading/confidence_utils.py
    .venv/bin/python -c "from src.intelligence.trading.confidence_utils import compose_confidence, CONF_CEIL, MIN_REGIME_WEIGHT, MIN_CTF_SCORE; print('imports OK')"
    .venv/bin/pytest tests/unit/ -q --tb=short -x 2>&1 | tail -10
  </verify>
  <done>
    - grep "def capture_signal_features" src/intelligence/trading/confidence_utils.py returns no match
    - grep -rn "capture_signal_features(" src/ --include="*.py" returns zero .py call sites (comments in .md files are acceptable)
    - grep "SHADOW_FEATURE_KEYS" src/intelligence/ml/feature_builder.py returns a constant assignment (not a function call)
    - Python import of compose_confidence, CONF_CEIL, MIN_REGIME_WEIGHT, MIN_CTF_SCORE from confidence_utils succeeds with no ImportError
    - pytest tests/unit/ passes (no regressions introduced)
    - src/intelligence/CLAUDE.md no longer lists capture_signal_features() in the confidence_utils.py row Key exports
  </done>
</task>

</tasks>

<verification>
Full import check: .venv/bin/python -c "from src.intelligence.trading import confidence_utils; print('OK')"
Full unit test run: .venv/bin/pytest tests/unit/ -q --tb=short 2>&1 | tail -5
</verification>

<success_criteria>
capture_signal_features() is deleted from confidence_utils.py with no regressions. SHADOW_FEATURE_KEYS in feature_builder.py is untouched. All remaining exports from confidence_utils (compose_confidence, CONF_CEIL, MIN_REGIME_WEIGHT, MIN_CTF_SCORE, ConfluenceWeightProfile) import cleanly. Unit tests pass.
</success_criteria>

<output>
After completion, create .planning/phases/128-3-table-schema-design-and-adr/128-03-SUMMARY.md
</output>
