---
phase: 123-ecl-boundary-restoration
plan: 03
type: execute
wave: 2
depends_on: ["123-01"]
files_modified:
  - docs/architecture/setup-confidence-patterns.md
autonomous: true
requirements: [ECL-01]

must_haves:
  truths:
    - "The architecture doc presents the CTF dual-gate as an ANTI-pattern, not a GOOD pattern"
    - "Section 7 no longer frames 'Phase 119 (17 plugins)' as a category with a dual HMM+CTF gate"
    - "No live cross-reference to the old filename i7-setup-confidence-patterns.md exists in docs/, src/, or tests/ (the historical reference inside the v2.10 spec doc is exempt)"
    - "The ECL boundary invariant is stated: only the HMM regime gate may suppress emission; all extrinsic vectors are annotations"
  artifacts:
    - path: "docs/architecture/setup-confidence-patterns.md"
      provides: "ECL-consistent architecture doc; Section 7 reflects dissolved Phase 119 category and factor_scores universality"
      contains: "ECL boundary invariant"
  key_links: []
---

<objective>
Bring the I7 setup-confidence architecture doc fully consistent with the Phase 123 ECL boundary. The file was already renamed (git mv done in a prior commit) and Sections 3, 4, 5, and 9 already describe ECL. The remaining gap is Section 7 ("Compliant Setups"), which still presents "Phase 119 (17 plugins)" as a category defined by a dual HMM+CTF gate — that category is dissolved in Phase 123 (the _PHASE_119_PLUGINS frozenset is deleted, no plugin has a CTF gate). This plan reconciles the doc with the post-123 reality and confirms zero stale cross-references.

Purpose: Satisfy the documentation half of ECL-01 — the canonical I7 authoring reference must not present CTF gating as a GOOD/historical pattern, and must reflect that all setup plugins are now uniform (no CTF-gate sub-category) and all collect factor_scores.
Output: Updated docs/architecture/setup-confidence-patterns.md; verified cross-reference cleanliness.

This plan is code-independent (doc-only) and runs in parallel with Plan 02. It depends on 123-01 only for conceptual accuracy (the gate removal that the doc now describes as done).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-ecl-boundary-restoration/123-CONTEXT.md
@.planning/phases/123-ecl-boundary-restoration/123-PATTERNS.md
@docs/architecture/setup-confidence-patterns.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile Section 7 (Compliant Setups) with the dissolved Phase 119 category + universal factor_scores</name>
  <files>docs/architecture/setup-confidence-patterns.md</files>
  <read_first>
    - docs/architecture/setup-confidence-patterns.md (read the FULL file — Sections 2 Pattern 3, 4 Vocabulary, 5 ECL, 7 Compliant Setups lines ~187-240, 9 Anti-patterns are the relevant areas)
    - src/intelligence/register_plugins.py (TIER_I7 list ~631-668, _I7_I6_EXEMPT ~671-682 — confirm the exempt set still exists after 123-01; only _PHASE_119_PLUGINS was deleted)
    - .planning/phases/123-ecl-boundary-restoration/123-CONTEXT.md (Architecture Doc section — what Wave C must convey)
  </read_first>
  <action>
    Section 7 "Compliant Setups": The "Phase 119 (17 plugins)" subsection currently describes those 17 plugins as having a "dual HMM+CTF gate". After Phase 123 there is no CTF gate. Update Section 7:
    - Remove the framing that distinguishes Phase 119 plugins by a "dual HMM+CTF gate". They now have a single HMM regime gate plus a CTF ECL annotation (same as all other compliant plugins).
    - Merge or relabel the Phase 118 / Phase 119 subsections so the doc no longer implies the Phase 119 group is structurally different. A simple history-preserving approach: keep the plugin tables (they are useful inventories) but change the section intro to state that all listed plugins follow the uniform pattern — single regime gate, intrinsic-factor composite, ctf_score as an ECL annotation, factor_scores collected before compositing. Note Phase 123 dissolved the former Phase-119 CTF-gate category.
    - Anywhere the doc says these plugins "have dual HMM+CTF gate" or references CTF as a gate for a GOOD pattern, correct it to "single regime gate + CTF ECL annotation".
    - The "Not yet I6-integrated (deferred)" subsection (the 8 _I7_I6_EXEMPT plugins) stays — _I7_I6_EXEMPT was NOT deleted in 123-01 (only _PHASE_119_PLUGINS was). Leave it intact.
    - If Section 2 Pattern 3 or Pattern 5 still describes "ctf_score float comparison" as a gate check, ensure it is framed as ECL annotation / not an emission gate (Pattern 3 already does this — verify and fix only if stale).
    - Add or confirm a one-line statement of the ECL boundary invariant in Section 5: "Only the HMM regime gate may suppress emission; all extrinsic confidence vectors (CTF, zone_friction, exhaustion) are annotations on the emitted signal, never gates." (Section 5 already states the invariant — confirm wording matches; refine if needed.)
    - Confirm factor_scores is described as collected by all setup plugins (Section 2 Pattern 1 / Pattern 3 mention factor_scores; ensure it reads as universal, not Phase-119-specific).
  </action>
  <verify>
    - `grep -n "dual HMM+CTF gate\|dual gate" docs/architecture/setup-confidence-patterns.md` — zero hits, OR only in Section 9 anti-patterns / historical context clearly marked as removed
    - `grep -n "ECL boundary invariant\|Only the HMM regime gate" docs/architecture/setup-confidence-patterns.md` — present
    - `grep -n "_PHASE_119_PLUGINS" docs/architecture/setup-confidence-patterns.md` — zero hits (doc must not reference the deleted frozenset)
    - `grep -n "factor_scores" docs/architecture/setup-confidence-patterns.md` — present and framed as universal
  </verify>
  <acceptance_criteria>
    - Section 7 no longer presents Phase 119 plugins as having a dual HMM+CTF gate; they are described as uniform (single regime gate + CTF ECL annotation)
    - Doc contains zero references to _PHASE_119_PLUGINS (the deleted frozenset)
    - The _I7_I6_EXEMPT (8 deferred plugins) subsection is preserved intact
    - The ECL boundary invariant is stated explicitly ("Only the HMM regime gate may suppress emission")
    - factor_scores is described as collected by all setup plugins before compositing
    - CTF gating appears ONLY in Section 9 (anti-patterns) as a WRONG example
  </acceptance_criteria>
  <done>Section 7 no longer presents Phase 119 dual HMM+CTF gate category; doc has zero _PHASE_119_PLUGINS refs; ECL boundary invariant stated; factor_scores framed as universal; CTF gating only in Section 9 anti-patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Verify cross-reference cleanliness for the old filename</name>
  <files>docs/architecture/setup-confidence-patterns.md</files>
  <read_first>
    - docs/architecture/setup-confidence-patterns.md (confirm correct filename in place)
    - .planning/phases/123-ecl-boundary-restoration/123-RESEARCH.md (Pitfall 7 — rename already done; confirmed only the v2.10 spec doc references the old name)
  </read_first>
  <action>
    Run `grep -rln "i7-setup-confidence-patterns" docs/ src/ tests/`. Research confirms the only hit is `docs/plans/2026-06-14-v2.10-signal-architecture-refactor.md` (the master spec, where the reference is historical context describing the rename). 

    Decision: the v2.10 spec doc reference is a historical artifact inside a plan document, NOT a live cross-reference in canonical docs/src/tests. Do NOT rewrite the spec doc's historical narrative. If grep reveals ANY OTHER live cross-reference (in src/, tests/, src/intelligence/CLAUDE.md, docs/foundation/glossary.md, or any canonical doc) pointing readers to the old filename, update it to `setup-confidence-patterns.md`.

    Do NOT run `git mv` — the file already exists at the correct path (Pitfall 7).
  </action>
  <verify>
    - `grep -rln "i7-setup-confidence-patterns" docs/ src/ tests/` returns ONLY docs/plans/2026-06-14-v2.10-signal-architecture-refactor.md (historical spec) or zero hits
    - `ls docs/architecture/setup-confidence-patterns.md` exists
    - `grep -rn "setup-confidence-patterns.md" src/intelligence/CLAUDE.md docs/foundation/glossary.md` shows the correct filename in live cross-refs
  </verify>
  <acceptance_criteria>
    - No live cross-reference (in src/, tests/, or canonical docs) points to the old i7-setup-confidence-patterns.md filename
    - The only remaining old-name occurrence is the historical narrative in the v2.10 spec plan doc (acceptable — it documents the rename event)
    - No git mv performed (file already at correct path)
  </acceptance_criteria>
  <done>No live cross-ref to old filename in src/ tests/ canonical docs; only historical v2.10 spec narrative remains; no git mv performed.</done>
</task>

</tasks>

<verification>
1. `grep -n "_PHASE_119_PLUGINS" docs/architecture/setup-confidence-patterns.md` — zero hits
2. `grep -n "dual HMM+CTF gate" docs/architecture/setup-confidence-patterns.md` — zero hits (or only as a marked anti-pattern)
3. `grep -rln "i7-setup-confidence-patterns" docs/ src/ tests/` — only the v2.10 spec plan doc (historical) or zero
4. `grep -n "ECL boundary invariant\|Only the HMM regime gate" docs/architecture/setup-confidence-patterns.md` — present
</verification>

<success_criteria>
- ECL-01 (documentation half): the canonical I7 authoring reference frames CTF gating as an anti-pattern and presents all setup plugins as uniform (single regime gate + ECL annotation + factor_scores)
- Section 7 reconciled with the dissolved Phase 119 category
- Zero live cross-references to the old filename
- ECL boundary invariant stated explicitly
</success_criteria>

<output>
After completion, create `.planning/phases/123-ecl-boundary-restoration/123-03-SUMMARY.md` documenting: which sections required edits vs were already ECL-consistent, and confirmation that the only old-filename reference is the historical v2.10 spec narrative.
</output>
