---
phase: 123-ecl-boundary-restoration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/trading/signal_schema.py
  - src/intelligence/trading/plugin_utils.py
  - src/intelligence/trading/confidence_utils.py
  - src/intelligence/trading/microstructure_utils.py
  - src/intelligence/trading/delta_exhaustion.py
  - src/intelligence/trading/ofi_spike.py
  - src/intelligence/trading/cvd_spike.py
  - src/intelligence/trading/ofi_divergence.py
  - src/intelligence/trading/failed_breakout.py
  - src/intelligence/trading/candlestick_pattern_setup.py
  - src/intelligence/trading/session_extremes_setup.py
  - src/intelligence/trading/liquidity_hunt.py
  - src/intelligence/trading/lvn_breakout.py
  - src/intelligence/trading/vwap_reclaim.py
  - src/intelligence/trading/vwap_deviation.py
  - src/intelligence/trading/momentum_breakout.py
  - src/intelligence/trading/orb15.py
  - src/intelligence/trading/orb30.py
  - src/intelligence/trading/second_leg_continuation.py
  - src/intelligence/trading/vcp.py
  - src/intelligence/trading/dual_divergence.py
  - src/intelligence/trading/supply_demand_setup.py
  - src/intelligence/register_plugins.py
  - services/signal_writer.py
  - tests/unit/intelligence/test_i7_extrinsic_contract.py
  - tests/unit/intelligence/test_i6_confluence_enforcement.py
autonomous: true
requirements: [ECL-01, ECL-02, ECL-03]

must_haves:
  truths:
    - "No I7 plugin returns no_signal() based on ctf_score, zone_friction, or exhaustion state"
    - "A signal that previously failed the CTF gate (abs(ctf_score) < min) now emits with ctf_confirmed=False"
    - "ctf_score=None when the feature is absent at emit time; ctf_score=0.0 only when the reading is genuinely neutral"
    - "context_features is a populated top-level field on every emitted signal (not only features_snapshot)"
    - "SIGNAL_SCHEMA_VERSION exists in signal_schema.py with string value 'v3'"
    - "_PHASE_119_PLUGINS frozenset no longer exists anywhere in src/ or tests/"
    - "pytest tests/unit/intelligence/ passes green"
  artifacts:
    - path: "src/intelligence/trading/signal_schema.py"
      provides: "SIGNAL_SCHEMA_VERSION constant + 5 ECL params on make_signal_from_frame + 5 ECL fields in REQUIRED_PIPELINE_FIELDS"
      contains: "SIGNAL_SCHEMA_VERSION"
    - path: "src/intelligence/trading/plugin_utils.py"
      provides: "emit_signal forwards ECL kwargs through **signal_fields to make_signal_from_frame"
    - path: "src/intelligence/trading/confidence_utils.py"
      provides: "null-preserving CTF/exhaustion extraction (no `or 0.0`)"
    - path: "services/signal_writer.py"
      provides: "reads 5 ECL fields from Kafka payload (no DB write yet)"
  key_links:
    - from: "I7 plugins (17 PHASE_119 + supply_demand)"
      to: "make_signal_from_frame / emit_signal"
      via: "ctf_score=, ctf_confirmed=, zone_friction_score= kwargs"
      pattern: "ctf_confirmed="
    - from: "signal_schema.make_signal_from_frame"
      to: "emitted signal dict"
      via: "sig['context_features'] assignment"
      pattern: "context_features"
    - from: "signal_processor.prepare_signals_or_dlq"
      to: "REQUIRED_PIPELINE_FIELDS gate"
      via: "frozenset membership"
      pattern: "REQUIRED_PIPELINE_FIELDS"
---

<objective>
Restore the ECL (Extrinsic Confidence Layer) boundary: remove every extrinsic emission suppressor from I7 plugins and promote the resulting metadata to first-class signal-schema fields. After this plan, the only permitted emission suppressor is the HMM regime gate; CTF score, zone friction, and exhaustion state become annotations on the emitted signal, never gates.

This is the replay-blocking critical path for v2.10 (Phase 126 clean replay cannot run until this ships). It eliminates Bias Layer 1 (emission suppression) so all signal firings reach the training corpus.

Purpose: Satisfy ECL-01 (zero extrinsic emission gates), ECL-02 (5 new schema fields populated at emit time), ECL-03 (SIGNAL_SCHEMA_VERSION bumped, context_features promoted, factor_scores threading wired).
Output: Modified signal schema, threading utilities, 17 PHASE_119 plugins + supply_demand, deleted _PHASE_119_PLUGINS frozenset, updated tests, signal_writer reading new payload fields.

CRITICAL ORDERING CONSTRAINT: REQUIRED_PIPELINE_FIELDS addition (Task 5) MUST be the LAST code change. Adding the 5 new fields to REQUIRED_PIPELINE_FIELDS before all plugins emit them causes every signal to DLQ at the terminal boundary gate in signal_processor.prepare_signals_or_dlq. Tasks 1-4 add the fields as OPTIONAL (schema accepts them, defaults applied); only Task 5 makes them REQUIRED.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-ecl-boundary-restoration/123-CONTEXT.md
@.planning/phases/123-ecl-boundary-restoration/123-RESEARCH.md
@.planning/phases/123-ecl-boundary-restoration/123-PATTERNS.md
@CLAUDE.md
@src/intelligence/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema foundation — SIGNAL_SCHEMA_VERSION + 5 optional ECL params + emit_signal threading + confidence_utils null fix</name>
  <files>src/intelligence/trading/signal_schema.py, src/intelligence/trading/plugin_utils.py, src/intelligence/trading/confidence_utils.py</files>
  <read_first>
    - src/intelligence/trading/signal_schema.py (current REQUIRED_PIPELINE_FIELDS lines ~45-54, make_signal_from_frame signature lines ~195-298)
    - src/intelligence/trading/plugin_utils.py (emit_signal lines ~201-265 — note **signal_fields catch-all)
    - src/intelligence/trading/confidence_utils.py (capture_signal_features lines ~166-232 — the `or 0.0` / `, 0.0)` fallbacks)
    - .planning/phases/123-ecl-boundary-restoration/123-PATTERNS.md (GROUP A and GROUP E excerpts)
  </read_first>
  <action>
    In src/intelligence/trading/signal_schema.py:
    - Add module constant after imports: `SIGNAL_SCHEMA_VERSION: str = "v3"` (STRING, not int — DB column signal_schema_version is `text NOT NULL`; historical values "v1","v2"). Add a comment noting integer semantics are intended but text preserved for DB compat until Phase 128 drops signal_ledger.
    - Add 5 new keyword-only parameters to `make_signal_from_frame`, all `= None`, placed after `features_snapshot`: `ctf_score: float | None = None`, `ctf_confirmed: bool | None = None`, `zone_friction_score: float | None = None`, `factor_scores: dict | None = None`, `context_features: dict | None = None`.
    - Inside make_signal_from_frame, after the existing sig dict is built (after framing-audit assignments near lines ~284-298), assign: `sig["ctf_score"] = ctf_score`, `sig["ctf_confirmed"] = ctf_confirmed`, `sig["zone_friction_score"] = zone_friction_score`, `sig["factor_scores"] = factor_scores if factor_scores is not None else {}`, `sig["context_features"] = context_features if context_features is not None else {}`.
    - DO NOT add anything to REQUIRED_PIPELINE_FIELDS in this task (that is Task 5). The fields are optional now.

    In src/intelligence/trading/plugin_utils.py:
    - Verify emit_signal forwards extra kwargs via `**signal_fields` to make_signal_from_frame. Because make_signal_from_frame now declares the 5 new explicit params, ECL kwargs passed to emit_signal flow through automatically. NO signature change to emit_signal is required. Confirm the forwarding line exists and add a comment noting ECL fields pass through.

    In src/intelligence/trading/confidence_utils.py (capture_signal_features):
    - Add a module-level helper `def _nullable_float(features: dict, key: str) -> float | None:` returning `float(_raw) if (_raw := features.get(key)) is not None else None` (use explicit two-line form, no walrus if it reduces clarity).
    - Replace ALL `float(features.get("ctf_score", 0.0))` and `float(features.get(..., 0.0))` and `float(features.get(...) or 0.0)` fallbacks for the 6 CTF base fields (ctf_score, ctf_trend_alignment, ctf_structure_alignment, ctf_regime_agreement, ctf_fvg_alignment, ctf_ob_alignment) with `_nullable_float(features, "<key>")`.
    - Replace the exhaustion field fallbacks (`exhaustion_score` and any sibling `, 0.0)` exhaustion reads) with the same null-preserving pattern.
    - DO NOT touch the I4 macro fields that already use `features.get(key)` without `or 0.0` — they are already correct.
  </action>
  <verify>
    - `grep -n "SIGNAL_SCHEMA_VERSION" src/intelligence/trading/signal_schema.py` shows `= "v3"`
    - `grep -n "ctf_confirmed\|context_features\|factor_scores\|zone_friction_score" src/intelligence/trading/signal_schema.py` shows the 5 new params and 5 sig assignments
    - `grep -rn '"ctf_score", 0\.0\|ctf_score") or 0\.0\|ctf_score", 0\.0)' src/intelligence/trading/confidence_utils.py` returns zero hits
    - `.venv/bin/python -c "from src.intelligence.trading.signal_schema import make_signal_from_frame, SIGNAL_SCHEMA_VERSION; assert SIGNAL_SCHEMA_VERSION == 'v3'"`
    - `.venv/bin/ruff check src/intelligence/trading/signal_schema.py src/intelligence/trading/plugin_utils.py src/intelligence/trading/confidence_utils.py`
  </verify>
  <acceptance_criteria>
    - `SIGNAL_SCHEMA_VERSION: str = "v3"` exists in signal_schema.py (string type, value exactly "v3")
    - make_signal_from_frame has 5 new keyword-only params (ctf_score, ctf_confirmed, zone_friction_score, factor_scores, context_features) all defaulting to None
    - factor_scores and context_features default to {} in the emitted sig dict; ctf_score/ctf_confirmed/zone_friction_score default to None
    - REQUIRED_PIPELINE_FIELDS is UNCHANGED in this task (still 6 fields)
    - capture_signal_features uses null-preserving extraction for all 6 CTF base fields + exhaustion fields; zero `or 0.0` / `, 0.0)` fallbacks remain for those fields
    - emit_signal signature is unchanged; ECL kwargs forward through **signal_fields
    - ruff passes on all three files
  </acceptance_criteria>
  <done>SIGNAL_SCHEMA_VERSION="v3" exists; make_signal_from_frame accepts 5 optional ECL params; REQUIRED_PIPELINE_FIELDS unchanged; capture_signal_features uses null-preserving extraction; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Strip CTF composite from delta_exhaustion.py and microstructure_utils.py + ECL annotation</name>
  <files>src/intelligence/trading/delta_exhaustion.py, src/intelligence/trading/microstructure_utils.py</files>
  <read_first>
    - src/intelligence/trading/delta_exhaustion.py (Gate 2 CTF gate lines ~96-99, composite lines ~143-154)
    - src/intelligence/trading/microstructure_utils.py (CTF gate lines ~82-85, composite lines ~102-111, detect_spike_signal full function)
    - src/intelligence/trading/confidence_utils.py (confirm rel_volume_score export ~line 65, clamp01 ~line 60, get_min_ctf_score)
    - .planning/phases/123-ecl-boundary-restoration/123-PATTERNS.md (GROUP C and GROUP D excerpts with exact before/after weights)
    - .planning/phases/123-ecl-boundary-restoration/123-RESEARCH.md (Open Question 1 — factor mapping ambiguity for delta_exhaustion)
  </read_first>
  <action>
    These two files have CTF in the confidence COMPOSITE (not just a gate), so they need rebalancing beyond gate removal. Grep the current composite in EACH file first to map real variable names to target weight slots — do not assume names.

    delta_exhaustion.py:
    - REMOVE the Gate 2 block: `ctf_score = float(features.get("ctf_score") or 0.0)` + `if abs(ctf_score) < get_min_ctf_score(): return no_signal()`.
    - REPLACE with ECL annotation: `_ctf_raw = features.get("ctf_score")`; `ctf_score: float | None = float(_ctf_raw) if _ctf_raw is not None else None`; `ctf_confirmed: bool | None = (abs(ctf_score) >= get_min_ctf_score()) if ctf_score is not None else None`.
    - REMOVE `ctf_score_factor` computation from the composite.
    - Rebalance composite to target `0.35*exhaustion + 0.30*momentum_reversal + 0.25*volume + 0.10*persistence` (weights sum to 1.0). Current intrinsic factors are `cvd_z_score` (=exhaustion slot), `price_fail_score` (=momentum_reversal slot), `hmm_mean_reversion_score` (=volume slot per spec rebalance to 0.25), and a 4th `persistence` slot at 0.10. Since the plugin lacks a distinct persistence factor, compute `persistence_score = clamp01(abs(cvd_spike_z) / 3.0)` (grep the actual cvd spike z variable name first). Final: `raw_conf = 0.35*cvd_z_score + 0.30*price_fail_score + 0.25*hmm_mean_reversion_score + 0.10*persistence_score`. Document the variable->slot mapping in a comment.
    - Add `rel_volume_score` to the confidence_utils import only if you use it; otherwise keep imports minimal. Keep get_min_ctf_score import (still needed for ctf_confirmed).
    - Pass `ctf_score=ctf_score, ctf_confirmed=ctf_confirmed` to the make_signal_from_frame / emit_signal call.

    microstructure_utils.py detect_spike_signal:
    - REMOVE the Gate 2 CTF gate (`ctf_score = float(features.get("ctf_score") or 0.0)` + `if abs(ctf_score) < get_min_ctf_score(): return no_signal()`).
    - REPLACE with the same null-preserving ECL annotation (ctf_score + ctf_confirmed).
    - REMOVE `ctf_factor` from the composite. Rebalance to `raw = 0.50 * z_score_score + 0.30 * volume_score + 0.20 * persistence_score` (weights sum to 1.0; preserve existing persistence_score logic with the price_return_z fallback of 0.3).
    - Keep get_min_ctf_score import (needed for ctf_confirmed).
    - Pass `ctf_score=ctf_score, ctf_confirmed=ctf_confirmed` to make_signal_from_frame.
  </action>
  <verify>
    - `grep -n "ctf_score_factor\|ctf_factor" src/intelligence/trading/delta_exhaustion.py src/intelligence/trading/microstructure_utils.py` returns zero hits
    - `grep -n "abs(ctf_score) < get_min_ctf_score()" src/intelligence/trading/delta_exhaustion.py src/intelligence/trading/microstructure_utils.py` returns zero hits (no gate)
    - `grep -n "ctf_confirmed" src/intelligence/trading/delta_exhaustion.py src/intelligence/trading/microstructure_utils.py` shows the annotation in both
    - `.venv/bin/python -c "import src.intelligence.trading.delta_exhaustion, src.intelligence.trading.microstructure_utils"`
    - `.venv/bin/pytest tests/unit/intelligence/ -k "delta_exhaustion or microstructure or spike" -q`
  </verify>
  <acceptance_criteria>
    - Neither file calls no_signal() based on ctf_score
    - delta_exhaustion composite weights sum to exactly 1.0 (0.35+0.30+0.25+0.10) with no ctf_score_factor term
    - microstructure_utils composite is `0.50*z_score_score + 0.30*volume_score + 0.20*persistence_score` (sums to 1.0), no ctf_factor term
    - Both files compute ctf_score (float|None) and ctf_confirmed (bool|None) via null-preserving extraction
    - Both files pass ctf_score= and ctf_confirmed= to the signal construction call
    - get_min_ctf_score import retained in both (used for ctf_confirmed)
    - Targeted pytest passes
  </acceptance_criteria>
  <done>Neither file gates on ctf_score; both composites rebalanced to sum 1.0 with no ctf factor; both pass ctf_score/ctf_confirmed; targeted pytest passes.</done>
</task>

<task type="auto">
  <name>Task 3: Remove CTF gates from the 15 remaining PHASE_119 plugins + supply_demand zone_friction annotation + context_features promotion across all touched plugins</name>
  <files>src/intelligence/trading/{ofi_spike,cvd_spike,ofi_divergence,failed_breakout,candlestick_pattern_setup,session_extremes_setup,liquidity_hunt,lvn_breakout,vwap_reclaim,vwap_deviation,momentum_breakout,orb15,orb30,second_leg_continuation,vcp,dual_divergence,supply_demand_setup}.py</files>
  <read_first>
    - .planning/phases/123-ecl-boundary-restoration/123-CONTEXT.md (CTF Gate Removal section — the 17-plugin list and exact replacement pattern; mtf_alignment is EXEMPT)
    - .planning/phases/123-ecl-boundary-restoration/123-PATTERNS.md (GROUP B gate-removal pattern, GROUP F context_features Form 1 + Form 2, GROUP G zone_friction)
    - src/intelligence/trading/ofi_spike.py and src/intelligence/trading/cvd_spike.py (these delegate to microstructure_utils.detect_spike_signal — confirm whether they have their own CTF gate or inherit it)
    - src/intelligence/trading/supply_demand_setup.py (confirm NO zone_friction gate exists — research says zero hits)
    - One representative plugin from each callsite form (Form 1 inline: ofi_continuation.py ~177-192; Form 2 post-assign)
  </read_first>
  <action>
    Apply CTF gate removal to the 15 PHASE_119 plugins NOT already done in Task 2 (delta_exhaustion done; microstructure done). The 15 files: ofi_spike, cvd_spike, ofi_divergence, failed_breakout, candlestick_pattern_setup, session_extremes_setup, liquidity_hunt, lvn_breakout, vwap_reclaim, vwap_deviation, momentum_breakout, orb15, orb30, second_leg_continuation, vcp, dual_divergence. (That is 16 names; ofi_spike + cvd_spike may have NO own gate because they delegate to detect_spike_signal — grep each before editing. If a plugin has no `abs(ctf_score) < get_min_ctf_score()` gate, it needs only the context_features promotion in this task, not gate removal.)

    For EACH plugin that HAS the gate:
    - REMOVE `ctf_score = float(features.get("ctf_score") or 0.0)` + `if abs(ctf_score) < get_min_ctf_score(): return no_signal()`.
    - REPLACE with: `_ctf_raw = features.get("ctf_score")`; `ctf_score: float | None = float(_ctf_raw) if _ctf_raw is not None else None`; `ctf_confirmed: bool | None = (abs(ctf_score) >= get_min_ctf_score()) if ctf_score is not None else None`.
    - Pass `ctf_score=ctf_score, ctf_confirmed=ctf_confirmed` to make_signal_from_frame / emit_signal.
    - If ctf_score appeared ONLY in the gate (not the composite), no composite rebalance needed. If a plugin uses ctf in its composite (other than the two handled in Task 2), grep it and rebalance the same way (remove the ctf factor, redistribute its weight proportionally across remaining intrinsic factors so weights sum to 1.0; document the mapping in a comment).
    - DO NOT touch mtf_alignment.py — CTF is its intrinsic signal (EXEMPT).

    supply_demand_setup.py (zone_friction annotation — NO gate to remove, research confirmed zero hits):
    - Before the make_signal_from_frame call, add: `_zf_raw = features.get("zone_friction_score")`; `zone_friction_score: float | None = float(_zf_raw) if _zf_raw is not None else None`.
    - Pass `zone_friction_score=zone_friction_score` to make_signal_from_frame.

    context_features promotion (ALL plugins touched in Tasks 2 and 3, plus supply_demand):
    - For each plugin's capture_signal_features() callsite, capture the return value into a local `ctx` and pass BOTH `features_snapshot=ctx` AND `context_features=ctx` (Form 1 inline) or assign both `signal["features_snapshot"] = ctx` and `signal["context_features"] = ctx` (Form 2 post-assign). features_snapshot is preserved for backward compat; context_features is the new canonical field; both point to the same dict object.

    Confirm exhaustion_guard is NOT a suppressor: run `grep -rn "exhaustion_guard\|exhaustion_score.*no_signal\|no_signal.*exhaustion" src/intelligence/trading/`. Expected: zero `no_signal` suppressors (apply_exhaustion_guard is a confidence penalty only). Document the confirmation in the task summary; make no code change for exhaustion.
  </action>
  <verify>
    - `grep -rln "abs(ctf_score) < get_min_ctf_score()" src/intelligence/trading/` returns ONLY mtf_alignment.py (the exempt plugin) or zero files
    - `grep -rn "ctf_confirmed=" src/intelligence/trading/` shows the annotation passed in all 16 touched PHASE_119 plugins
    - `grep -n "zone_friction_score" src/intelligence/trading/supply_demand_setup.py` shows the annotation + pass-through
    - `grep -rln "context_features=ctx\|context_features\"\] =" src/intelligence/trading/` covers every plugin touched in Tasks 2 and 3
    - `grep -rn "no_signal.*exhaustion\|exhaustion.*no_signal" src/intelligence/trading/` returns zero hits
    - `.venv/bin/pytest tests/unit/intelligence/ -q -k "i7 or signal or plugin"` (expect some test_i7_extrinsic_contract failures here — those are fixed in Task 4)
  </verify>
  <acceptance_criteria>
    - Zero PHASE_119 plugins (excluding mtf_alignment) call no_signal() based on ctf_score
    - All 16 touched PHASE_119 plugins pass ctf_score= and ctf_confirmed= to signal construction
    - supply_demand_setup.py annotates zone_friction_score (float|None) and passes it through; it has NO zone_friction gate (none existed)
    - mtf_alignment.py is UNCHANGED (CTF is intrinsic)
    - Every plugin touched in Tasks 2-3 sets context_features to the same dict as features_snapshot
    - exhaustion_guard confirmed as a non-suppressor via grep (zero no_signal+exhaustion hits); no exhaustion code changed
    - Any composite that contained a ctf factor is rebalanced so weights sum to 1.0
  </acceptance_criteria>
  <done>Zero PHASE_119 plugins (except mtf_alignment) gate on ctf_score; all pass ctf_score/ctf_confirmed; supply_demand annotates zone_friction_score; context_features set on all touched plugins; exhaustion_guard confirmed non-suppressor.</done>
</task>

<task type="auto">
  <name>Task 4: Delete _PHASE_119_PLUGINS frozenset + fix both importing test files</name>
  <files>src/intelligence/register_plugins.py, tests/unit/intelligence/test_i7_extrinsic_contract.py, tests/unit/intelligence/test_i6_confluence_enforcement.py</files>
  <read_first>
    - src/intelligence/register_plugins.py (the _PHASE_119_PLUGINS block lines ~684-710 including its comment)
    - tests/unit/intelligence/test_i7_extrinsic_contract.py (import at ~line 33, perturbation logic ~505-509, test_phase_119_plugins_count ~536-540, test_extrinsic_still_captured_in_features_snapshot ~564-597, module docstring 1-24)
    - tests/unit/intelligence/test_i6_confluence_enforcement.py (import ~line 15, parametrize using sorted(_PHASE_119_PLUGINS) ~line 104)
    - .planning/phases/123-ecl-boundary-restoration/123-PATTERNS.md (GROUP I, GROUP J, GROUP K)
    - .planning/phases/123-ecl-boundary-restoration/123-RESEARCH.md (Open Question 4 — shadow_only test replacement)
  </read_first>
  <action>
    register_plugins.py:
    - Delete the entire `_PHASE_119_PLUGINS: frozenset[str] = frozenset([...])` block AND its preceding comment (lines ~684-710). No replacement constant — the category is dissolved.

    tests/unit/intelligence/test_i7_extrinsic_contract.py:
    - Remove the `from src.intelligence.register_plugins import _PHASE_119_PLUGINS` import (or remove _PHASE_119_PLUGINS from a multi-name import).
    - Change the perturbation-key filter so ctf_score is ALWAYS perturbable: replace the dict comprehension that excluded ctf_score for PHASE_119 plugins with `perturbation_keys = copy.deepcopy(_EXTRINSIC_KEYS)` (ensure `import copy` present). The invariant under test becomes: perturbing ctf_score (e.g. to 0.9) must NOT change confidence for ANY plugin (CTF is no longer a gate or composite factor for any plugin).
    - Delete `test_phase_119_plugins_count()` entirely.
    - Extend `test_extrinsic_still_captured_in_features_snapshot` (or add a sibling test) to also assert `context_features` is populated: `ctx = result.get("context_features", {})`; `assert ctx`; `assert "ctf_score" in ctx`; and that the ctf value in context_features matches the perturbed value.
    - Update the module docstring to describe the Phase 123 ECL dissolution (no Phase 119 gate exemption).

    tests/unit/intelligence/test_i6_confluence_enforcement.py:
    - Remove `_PHASE_119_PLUGINS` from the import.
    - Replace the parametrize source `sorted(_PHASE_119_PLUGINS)` at ~line 104. Per research Open Question 4: parametrize over the I7 plugins that declare `shadow_only=True` (derive that list at import time from the registry by filtering TIER_I7 plugin instances on `shadow_only`), OR if that test's intent (verify shadow_only on refactored plugins) is now covered by a uniform tier-wide assertion, replace it with a single assertion that all non-exempt I7 plugins have requires_i6_confluence=True. Choose the smaller change that keeps the test meaningful; document the choice in a comment.
  </action>
  <verify>
    - `grep -rn "_PHASE_119_PLUGINS" src/ tests/` returns ZERO hits
    - `.venv/bin/python -c "import src.intelligence.register_plugins"`
    - `.venv/bin/pytest tests/unit/intelligence/test_i7_extrinsic_contract.py tests/unit/intelligence/test_i6_confluence_enforcement.py -q` passes
  </verify>
  <acceptance_criteria>
    - `grep -rn "_PHASE_119_PLUGINS" src/ tests/` returns zero hits
    - register_plugins.py imports cleanly (no NameError, no leftover comment referencing Phase 119 gate plugins)
    - test_i7_extrinsic_contract.py: ctf_score is perturbable for all plugins; test_phase_119_plugins_count deleted; context_features asserted populated
    - test_i6_confluence_enforcement.py: no _PHASE_119_PLUGINS reference; parametrize replaced with a meaningful source; test passes
    - Both test files pass under pytest
  </acceptance_criteria>
  <done>grep _PHASE_119_PLUGINS in src/ tests/ returns zero; both test files updated and passing; ctf_score perturbable for all plugins; context_features asserted populated.</done>
</task>

<task type="auto">
  <name>Task 5: Add 5 ECL fields to REQUIRED_PIPELINE_FIELDS (LAST) + signal_writer reads payload + full intelligence suite green</name>
  <files>src/intelligence/trading/signal_schema.py, services/signal_writer.py</files>
  <read_first>
    - src/intelligence/trading/signal_schema.py (REQUIRED_PIPELINE_FIELDS frozenset, now still 6 fields after Task 1)
    - src/intelligence/pipeline/signal_processor.py (prepare_signals_or_dlq REQUIRED_PIPELINE_FIELDS gate ~lines 516-526 — read to confirm the DLQ behavior, do NOT change it)
    - services/signal_writer.py (_payload_to_ledger_entries / _parse_signals — where sig fields are read; LedgerEntry construction)
    - src/persistence/repository/signal_ledger_repository.py (LedgerEntry dataclass — confirm it has NO ECL fields; decide whether to add optional None-default attrs)
    - .planning/phases/123-ecl-boundary-restoration/123-PATTERNS.md (GROUP L, GROUP M)
    - .planning/phases/123-ecl-boundary-restoration/123-RESEARCH.md (Open Question 3 — defer DB write to Phase 128)
  </read_first>
  <action>
    THIS TASK RUNS LAST. By now all 16 PHASE_119 plugins + delta_exhaustion + microstructure + supply_demand emit the ECL fields, and make_signal_from_frame defaults factor_scores/context_features to {} for every other plugin. So adding the fields to REQUIRED_PIPELINE_FIELDS is now safe (no DLQ blast).

    signal_schema.py:
    - Add the 5 ECL field names to REQUIRED_PIPELINE_FIELDS: `"ctf_score"`, `"ctf_confirmed"`, `"zone_friction_score"`, `"factor_scores"`, `"context_features"`. Add a comment: `{}` sentinel = plugin emitted no factors / no context; `None` = field-not-written. (Note: every signal built via make_signal_from_frame now carries all 5 keys, so the membership check passes; ctf_score/ctf_confirmed/zone_friction_score keys are present even when their VALUE is None — the gate checks key presence via `REQUIRED_PIPELINE_FIELDS - set(sig.keys())`, not value.)

    services/signal_writer.py:
    - In the payload-to-ledger path, read the 5 ECL fields from the signal payload: `_ctf_score = sig.get("ctf_score")`, `_ctf_confirmed = sig.get("ctf_confirmed")`, `_zone_friction_score = sig.get("zone_friction_score")`, `_factor_scores = sig.get("factor_scores", {})`, `_context_features = sig.get("context_features", {})`. Add a comment that these persist to signal_events in Phase 128; for Phase 123 they exist in the Kafka payload and are read here but NOT written to the DB (no migration). Do NOT add them to _INSERT_SQL or _to_row.
    - Per research Open Question 3: defer LedgerEntry + DB column changes to Phase 128. If (and only if) a test breaks because it expects LedgerEntry to accept these fields, add them as optional attrs defaulting to None on the LedgerEntry dataclass WITHOUT adding them to _to_row/_INSERT_SQL. Otherwise leave LedgerEntry untouched.

    Final verification: run the full intelligence unit suite.
  </action>
  <verify>
    - `grep -n "ctf_score\|ctf_confirmed\|zone_friction_score\|factor_scores\|context_features" src/intelligence/trading/signal_schema.py | grep -i "REQUIRED_PIPELINE" -A6` confirms all 5 are now in the frozenset
    - `grep -n "context_features\|ctf_score\|factor_scores" services/signal_writer.py` shows the payload reads
    - `.venv/bin/pytest tests/unit/intelligence/ -q` is GREEN (Phase 123 success criterion 1)
    - `grep -rn '"ctf_score".*or 0\.0\|"ctf.*", 0\.0' src/` returns zero hits (success criterion 2)
    - `grep -rn "_PHASE_119_PLUGINS" src/ tests/` returns zero hits (success criterion 3)
    - `grep -rn "zone_friction.*no_signal\|no_signal.*zone_friction" src/intelligence/trading/` returns zero hits (success criterion 4)
    - `.venv/bin/ruff check src/ services/signal_writer.py` clean
  </verify>
  <acceptance_criteria>
    - REQUIRED_PIPELINE_FIELDS contains all 5 ECL field names (11 fields total) and this was the LAST code change before verification
    - signal_writer.py reads all 5 ECL fields from the payload with {} defaults for the two dict fields; no DB write of these fields (no _INSERT_SQL change)
    - LedgerEntry dataclass unchanged unless a test forced optional-attr additions (documented if so)
    - `pytest tests/unit/intelligence/ -q` passes green
    - All 4 grep-based Phase 123 success criteria (ctf or 0.0, _PHASE_119_PLUGINS, zone_friction no_signal) return zero hits
    - ruff clean on src/ and signal_writer.py
  </acceptance_criteria>
  <done>REQUIRED_PIPELINE_FIELDS has all 5 ECL fields (added LAST); signal_writer reads them from payload (no DB write); pytest tests/unit/intelligence/ green; all 4 grep success criteria zero hits.</done>
</task>

</tasks>

<verification>
After all 5 tasks:
1. `.venv/bin/pytest tests/unit/intelligence/ -q` — green (success criterion 1)
2. `grep -rn '"ctf_score".*or 0\.0\|"ctf.*", 0\.0' src/` — zero hits (success criterion 2)
3. `grep -rn "_PHASE_119_PLUGINS" src/ tests/` — zero hits (success criterion 3)
4. `grep -rn "zone_friction.*no_signal\|no_signal.*zone_friction" src/intelligence/trading/` — zero hits (success criterion 4)
5. `grep -n "SIGNAL_SCHEMA_VERSION" src/intelligence/trading/signal_schema.py` — shows `= "v3"` (success criterion 8)
6. `grep -rln "abs(ctf_score) < get_min_ctf_score()" src/intelligence/trading/` — only mtf_alignment.py or zero files (ECL-01)
7. `.venv/bin/ruff check . && .venv/bin/black --check src/intelligence/trading/ services/signal_writer.py` — clean
</verification>

<success_criteria>
- ECL-01: Zero I7 plugins (excluding mtf_alignment intrinsic) call no_signal() on ctf_score, zone_friction, or exhaustion state; all extrinsic vectors are annotations
- ECL-02: 5 new fields (ctf_score, ctf_confirmed, zone_friction_score, factor_scores, context_features) present on every emitted signal at emit time
- ECL-03 (partial): SIGNAL_SCHEMA_VERSION = "v3"; context_features populated as top-level field (factor_scores full population is Plan 02)
- A formerly-gated signal (abs(ctf_score) < min) now emits with ctf_confirmed=False instead of being suppressed
- pytest tests/unit/intelligence/ green
</success_criteria>

<output>
After completion, create `.planning/phases/123-ecl-boundary-restoration/123-01-SUMMARY.md` documenting: which plugins had real CTF gates vs delegated/none, the exhaustion_guard non-suppressor confirmation, the delta_exhaustion variable->weight-slot mapping decision, and the test_i6_confluence_enforcement parametrize-replacement choice.
</output>
