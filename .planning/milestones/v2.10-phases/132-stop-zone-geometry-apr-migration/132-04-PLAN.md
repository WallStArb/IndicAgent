---
phase: 132-stop-zone-geometry-apr-migration
plan: 04
type: execute
wave: 3
depends_on: ["132-03"]
files_modified:
  - production/migrations/148_phase132_stop_floor_per_class.sql
  - src/intelligence/trading/trade_framer.py
  - services/intelligence_pipeline.py
autonomous: true
gap_closure: false

must_haves:
  truths:
    - "Four per-asset-class stop floor APR keys exist with empirical seed values (fx 1.0, commodity_small_tick 1.5, equity_etf 1.0, futures_large_tick 1.0)"
    - "trade_framer.py routes the minimum stop floor by asset_class, mirroring the existing _min_zone_width_atr pattern, with the universal min_stop_atr as fallback"
    - "The 1-tick absolute gate (stop <= entry + 1 tick) remains hardcoded and unchanged — it is a correctness invariant, not a tunable"
    - "At seed values, every asset class produces a stop >= its respective floor and stops are unchanged vs Plan 03 output (floors equal-or-above the universal 1.0 baseline introduce no narrowing)"
  artifacts:
    - path: "production/migrations/148_phase132_stop_floor_per_class.sql"
      provides: "config_schema + config_state + config_history for 4 per-class stop floor keys"
      contains: "feature.trade_framer.stop_multiplier_floor.commodity_small_tick"
    - path: "src/intelligence/trading/trade_framer.py"
      provides: "_min_stop_multiplier_floor(asset_class) router applied in stop calculation"
  key_links:
    - from: "_resolve_stop_long / _resolve_stop_short min_stop calculation"
      to: "_min_stop_multiplier_floor(asset_class)"
      via: "per-class floor applied above the universal min_stop_atr floor"
      pattern: "_min_stop_multiplier_floor"
    - from: "_min_stop_multiplier_floor"
      to: "config_state"
      via: "_cfg with asset-class-specific key + universal fallback"
      pattern: "stop_multiplier_floor"
---

<objective>
Add the four A3 per-asset-class minimum stop floor APR keys (migration 148) using the empirically-derived seed values from RESEARCH §4.3, and route the minimum stop floor by `asset_class` in trade_framer.py. The per-class floor sits ABOVE the existing universal `feature.trade_framer.min_stop_atr` (1.0 ATR) and the 1-tick absolute gate. asset_class is supplied by Phase 131 A4 (fixed in run_historical_pipeline.py:1669-1687 and the live pipeline DI).

Depends on Plan 03: trade_framer.py adaptive buffer wiring is complete; this plan adds the per-class router (a new function modeled on _min_zone_width_atr at line 168-177) and applies it in the stop calculation.

Purpose: A3 per-asset-class stop geometry (CONTEXT D-02). Renaissance empirical-over-theoretical: seed values were computed from 5M intelligence_features ATR_14 medians per class (NG's 1.5x ATR/tick headroom justifies a 1.5 ATR commodity floor; all others adequate at 1.0). The 1-tick gate is preserved as the absolute floor (CONTEXT D-01b, RESEARCH §1.1).
Output: migration 148, _min_stop_multiplier_floor router, prewarm registrations, per-class floor regression test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-stop-zone-geometry-apr-migration/132-CONTEXT.md
@.planning/phases/132-stop-zone-geometry-apr-migration/132-RESEARCH.md
@docs/foundation/adaptive-parameter-registry.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 148 (4 per-class stop floor keys) + register in _THRESHOLD_KEYS</name>
  <read_first>
    - production/migrations/142_phase130_apr_seeds.sql (3-block migration format)
    - .planning/phases/132-stop-zone-geometry-apr-migration/132-RESEARCH.md §4.3 (recommended seed values + rationale) and §4.2 (per-class ATR/tick data)
    - .planning/phases/132-stop-zone-geometry-apr-migration/132-CONTEXT.md D-02 (the 4 key names + asset class -> instrument mapping)
    - services/intelligence_pipeline.py lines 376-405 (_THRESHOLD_KEYS format)
  </read_first>
  <files>production/migrations/148_phase132_stop_floor_per_class.sql, services/intelligence_pipeline.py</files>
  <action>
    Create `production/migrations/148_phase132_stop_floor_per_class.sql` (3-block format). Seed the four keys at the empirically-justified values from RESEARCH §4.3:

    | APR key | type | seed | min | max | rationale |
    |---------|------|------|-----|-----|-----------|
    | feature.trade_framer.stop_multiplier_floor.fx | float | 1.0 | 0.5 | 3.0 | FX small ticks but adequate ATR/tick (class median 8.9x) |
    | feature.trade_framer.stop_multiplier_floor.commodity_small_tick | float | 1.5 | 0.5 | 3.0 | NG only 1.5x ATR/tick headroom — higher floor for safety |
    | feature.trade_framer.stop_multiplier_floor.equity_etf | float | 1.0 | 0.5 | 3.0 | strong ATR/tick (class median 15.2x) |
    | feature.trade_framer.stop_multiplier_floor.futures_large_tick | float | 1.0 | 0.5 | 3.0 | strong ATR/tick for all major contracts (class median 7.7x) |

    config_schema descriptions: include `[initial_estimate]`, "ML learning target" (these are tuned per class after Phase 133 corpus rebuild), and the derivation note "seed from intelligence_features.technical_indicators->>'atr_14' median per asset class / tick_size; first real value after Phase 133". config_history `changed_by`='migration_148', `reason`='Phase 132 A3 per-asset-class stop floor seed'.

    Add the 4 keys to `_THRESHOLD_KEYS` in services/intelligence_pipeline.py under comment `# --- migration 148: Phase 132 A3 per-asset-class stop floors ---`. Defaults equal seeds.

    Apply: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/148_phase132_stop_floor_per_class.sql`.
  </action>
  <verify>
    `SELECT COUNT(*) FROM config_state WHERE config_key LIKE 'feature.trade_framer.stop_multiplier_floor.%';` returns 4; commodity seed is 1.5.
  </verify>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM config_state WHERE config_key LIKE 'feature.trade_framer.stop_multiplier_floor.%'` returns exactly 4
    - `SELECT config_value FROM config_state WHERE config_key='feature.trade_framer.stop_multiplier_floor.commodity_small_tick'` returns '1.5'
    - `SELECT config_value FROM config_state WHERE config_key='feature.trade_framer.stop_multiplier_floor.fx'` returns '1.0'
    - `grep -c "stop_multiplier_floor" services/intelligence_pipeline.py` returns at least 4
    - migration 148 applied with no psql error
  </acceptance_criteria>
  <done>4 per-class stop floor keys exist in config_state at empirical seeds (commodity 1.5, others 1.0) and are registered in _THRESHOLD_KEYS; migration 148 applied cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add _min_stop_multiplier_floor router + apply in stop calculation</name>
  <read_first>
    - src/intelligence/trading/trade_framer.py lines 168-177 (_min_zone_width_atr — the EXACT pattern to mirror: default via _cfg, asset-class override when _config_service is not None)
    - src/intelligence/trading/trade_framer.py _resolve_stop_long (line 537+, min_stop calc at line 544) and _resolve_stop_short (~line 600+, max_stop at line 613) — where the universal min_stop floor is applied
    - src/intelligence/trading/trade_framer.py the 1-tick gate logic (grep "1 tick"/"tick_size"/entry + tick — do NOT modify) and validate_stop_against_zone at line 1084, stop distance floor gate at line 1095-1110
    - .planning/phases/132-stop-zone-geometry-apr-migration/132-CONTEXT.md D-01b (min_stop_atr 1.0 raises the floor above the 0.5 ATR zone_engine gate) and D-02 (1-tick gate must remain)
  </read_first>
  <files>src/intelligence/trading/trade_framer.py</files>
  <action>
    Add a new module-level function modeled EXACTLY on `_min_zone_width_atr` (line 168-177):

    `def _min_stop_multiplier_floor(asset_class: str | None) -> float:` returns `_cfg("feature.trade_framer.min_stop_atr", 1.0)` as the universal default, and when `asset_class and _config_service is not None`, returns `_cfg(f"feature.trade_framer.stop_multiplier_floor.{asset_class}", default)`. This means: with no asset_class or no config service, the universal min_stop_atr applies; with a known asset_class, the per-class floor applies (defaulting to the universal value if the class key is absent).

    Apply it in the min_stop / max_stop calculation. Currently `min_stop = entry - atr * _adaptive_buffer(features, MIN_STOP_ATR_MULTIPLIER, ...)` — after Plan 02/03 this reads `_cfg("feature.trade_framer.min_stop_atr", 1.0)` as base_mult. Change the base_mult to use the per-class floor: read `asset_class = features.get("asset_class")`, then use `_min_stop_multiplier_floor(asset_class)` as the base multiplier passed to `_adaptive_buffer`. Apply symmetrically in _resolve_stop_long (min_stop) and _resolve_stop_short (max_stop).

    The per-class floor produces a multiplier >= the universal 1.0 for every class (fx/equity/futures = 1.0, commodity = 1.5), so at seed values no stop becomes NARROWER than Plan 03 output — equity/futures/fx are identical, commodity stops widen slightly. This is the intended A3 behavior. Document this in a code comment.

    DO NOT touch the 1-tick gate, validate_stop_against_zone, or the stop distance floor gate (line 1095-1110) — those are existing Phase 126 invariants. The per-class floor is an additional floor ABOVE the 1-tick gate, never below it.

    Run `.venv/bin/ruff check src/intelligence/trading/trade_framer.py --fix` and `.venv/bin/black src/intelligence/trading/trade_framer.py`.
  </action>
  <verify>
    `grep -n "_min_stop_multiplier_floor" src/intelligence/trading/trade_framer.py` shows the definition + at least 2 call sites (long + short); the 1-tick gate logic is unchanged; import succeeds.
  </verify>
  <acceptance_criteria>
    - `grep -c "_min_stop_multiplier_floor" src/intelligence/trading/trade_framer.py` returns at least 3 (1 def + 2 usages)
    - `_min_stop_multiplier_floor` reads `feature.trade_framer.stop_multiplier_floor.{asset_class}` with `feature.trade_framer.min_stop_atr` as the fallback default
    - The 1-tick gate logic is byte-unchanged (grep the tick gate string before and after; it must still exist)
    - `.venv/bin/ruff check src/intelligence/trading/trade_framer.py` exits 0
    - `.venv/bin/python -c "import src.intelligence.trading.trade_framer"` exits 0
  </acceptance_criteria>
  <done>_min_stop_multiplier_floor(asset_class) router added (mirroring _min_zone_width_atr) and applied in long+short stop calculation; 1-tick gate unchanged; module imports clean.</done>
</task>

<task type="auto">
  <name>Task 3: Per-class floor regression test</name>
  <read_first>
    - tests/unit/intelligence/ (existing trade_framer test style; the regression tests from Plan 02/03)
    - src/intelligence/trading/trade_framer.py _min_stop_multiplier_floor + _resolve_stop_long + set_config_service
    - .planning/phases/132-stop-zone-geometry-apr-migration/132-RESEARCH.md §4.3 (seed values)
  </read_first>
  <files>tests/unit/intelligence/ (per-class stop floor regression test)</files>
  <action>
    Add unit tests proving:
    - `_min_stop_multiplier_floor("commodity_small_tick")` returns 1.5 (with mock ConfigService seeded), `_min_stop_multiplier_floor("equity_etf")` returns 1.0, `_min_stop_multiplier_floor("fx")` returns 1.0, `_min_stop_multiplier_floor("futures_large_tick")` returns 1.0
    - `_min_stop_multiplier_floor(None)` returns the universal 1.0 (min_stop_atr fallback)
    - `_min_stop_multiplier_floor("unknown_class")` returns the universal 1.0 (class key absent → falls back to default)
    - With features carrying `asset_class="commodity_small_tick"`, the resolved stop is at least 1.5*ATR from entry (after adaptive buffer at vol_ratio=1.0 the multiplier is 1.5); with `asset_class="equity_etf"` it is at least 1.0*ATR — confirming the per-class floor is applied
    - The 1-tick gate still floors stops for instruments where 1.0*ATR would be sub-tick (construct a pathological tiny-ATR features dict and assert stop differs from entry by >= 1 tick)

    Use a STRICT mock ConfigService seeded at migration-146 values: `get_sync(key, default)` returns the seed value for known keys and RAISES `ValueError(f"Unknown APR key in test: {key!r}")` for any key not in the seed dict. Reset set_config_service(None) in teardown.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/intelligence/ -k "stop_floor or stop_multiplier" -q` passes; full unit suite green.
  </verify>
  <acceptance_criteria>
    - A test asserts `_min_stop_multiplier_floor("commodity_small_tick")` == 1.5 under seeded config
    - A test asserts `_min_stop_multiplier_floor(None)` and `_min_stop_multiplier_floor("unknown_class")` both return 1.0
    - A test asserts the 1-tick gate still applies a >= 1 tick stop distance for a pathological tiny-ATR case
    - `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
  <done>Tests confirm per-class floor routing (commodity 1.5, others/unknown/None 1.0) and that the 1-tick gate still floors a pathological tiny-ATR case; full unit suite green.</done>
</task>

</tasks>

<verification>
- 4 per-class stop floor keys exist at empirical seed values (commodity 1.5, others 1.0)
- _min_stop_multiplier_floor router applied in long + short stop calculation
- 1-tick gate unchanged
- per-class floor regression test green; full unit suite green
</verification>

<success_criteria>
- `SELECT COUNT(*) FROM config_state WHERE config_key LIKE 'feature.trade_framer.stop_multiplier_floor.%'` returns 4
- per-class floor routing works; commodity floor is 1.5 ATR, others 1.0 ATR
- 1-tick absolute gate preserved (not migrated to APR)
- no stop narrows vs Plan 03 at seed values
</success_criteria>

<output>
After completion, create `.planning/phases/132-stop-zone-geometry-apr-migration/132-04-SUMMARY.md`
</output>
