---
phase: 126-signal-universe-hardening
plan: 02
type: execute
wave: 2
depends_on: ["126-00"]
files_modified:
  - src/intelligence/register_plugins.py
  - src/intelligence/trading/regime_transition.py
  - src/intelligence/trading/prev_day_level_test.py
  - src/intelligence/trading/anchored_vwap_reversion.py
  - src/intelligence/trading/poc_rejection.py
  - src/intelligence/trading/hvn_rejection.py
  - src/intelligence/trading/cross_asset_divergence.py
  - src/intelligence/trading/mean_reversion.py
  - src/intelligence/trading/squeeze_expansion.py
  - src/intelligence/trading/session_extremes_setup.py
  - src/intelligence/trading/orb15.py
  - src/intelligence/trading/orb30.py
  - src/intelligence/trading/fvg_fill.py
  - tests/unit/intelligence/test_orb_plugins.py
autonomous: true
requirements:
  - SIGNAL-QUALITY-02
must_haves:
  truths:
    - "All 8 formerly-exempt plugins carry requires_i6_confluence = True and emit confluence annotation"
    - "_I7_I6_EXEMPT frozenset is deleted from register_plugins.py with no remaining references"
    - "SessionExtremesSetup, ORB15, ORB30 each have a documented verdict (CORRECT-RARE / BROKEN / SCOPE-MISMATCH) in their docstring"
    - "trad_MeanReversion dual-gate conflict is diagnosed and either relaxed or demoted to shadow_only=True with rationale"
    - "trad_FVGFill root cause is identified (entry-timing defect confirmed/refuted) with a fix or shadow_only=True"
  artifacts:
    - path: "src/intelligence/register_plugins.py"
      provides: "_I7_I6_EXEMPT deleted; 8 plugins compliant"
      contains: "TIER_I7"
    - path: "src/intelligence/trading/mean_reversion.py"
      provides: "Dual-gate fix or shadow_only disposition"
    - path: "src/intelligence/trading/fvg_fill.py"
      provides: "Entry-timing audit verdict in docstring"
    - path: "tests/unit/intelligence/test_orb_plugins.py"
      provides: "Synthetic RTH-open ORB15/ORB30 unit test"
  key_links:
    - from: "src/intelligence/register_plugins.py"
      to: "validate_tier() in src/intelligence/plugins/base.py"
      via: "removing the _I7_I6_EXEMPT carve-out so confluence is enforced uniformly"
      pattern: "_I7_I6_EXEMPT"
---

<objective>
Close the signal-generation plugin completeness gaps for Root Crimes 2 and 4. (a) Wire the 8 confluence-exempt plugins to I6 and delete the `_I7_I6_EXEMPT` exemption category. (b) Diagnose the 3 zero-signal time-specific plugins (SessionExtremesSetup, ORB15, ORB30) — no "probably rarely triggered" verdicts allowed. (c) Fix the trad_MeanReversion dual-gate mutual exclusion. (d) Audit trad_FVGFill's catastrophic 8.93% equity win rate (diagnostic-first, no code change until root cause confirmed).

Purpose: Every signal-generation plugin must annotate confluence and either fire reliably or be explicitly demoted to shadow_only=True with documented rationale. Removal from TIER_I7 is never valid (D-12) — every firing is training data.

Output: 8 compliant plugins, deleted exempt set, documented verdicts for 3 time-specific plugins, MeanReversion + FVGFill dispositions, ORB unit test.

NOTE ON WAVE 3 INTERACTION (D-06): This plan wires the 8 plugins with `requires_i6_confluence = True` and `capture_signal_features()`. Wave 3 (P126-06) THEN strips `capture_signal_features()` from ALL plugins and removes the `requires_i6_confluence` enforcement entirely. Both are correct: Wave 2 brings them to current-architecture compliance; Wave 3 migrates annotation to the pipeline layer. The `_I7_I6_EXEMPT` deletion happens HERE (Wave 2); the `requires_i6_confluence` ClassVar removal happens in Wave 3.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-signal-universe-hardening/126-CONTEXT.md
@docs/plans/2026-06-14-phase-126-signal-universe-hardening.md
@docs/plans/2026-06-14-phase-126-usdjpy-diagnostic.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire 8 exempt plugins to I6, delete _I7_I6_EXEMPT</name>
  <files>src/intelligence/register_plugins.py, src/intelligence/plugins/base.py, the 8 plugin files</files>
  <read_first>
    - src/intelligence/register_plugins.py (lines 671-682 — the _I7_I6_EXEMPT frozenset and the 8 plugin name references; TIER_I7 list)
    - src/intelligence/plugins/base.py (validate_tier() at line 113 — understand how requires_i6_confluence + the exempt carve-out interact)
    - A compliant reference plugin that already has requires_i6_confluence = True and calls capture_signal_features() (grep for a non-exempt I7 plugin, e.g. `grep -ln "requires_i6_confluence = True" src/intelligence/trading/*.py | head -1` and read it for the exact pattern)
    - Each of the 8 plugin files before editing: regime_transition.py, prev_day_level_test.py, anchored_vwap_reversion.py, poc_rejection.py, hvn_rejection.py, cross_asset_divergence.py, mean_reversion.py, squeeze_expansion.py
    - .planning/phases/126-signal-universe-hardening/126-CONTEXT.md (D-06)
  </read_first>
  <action>
    For each of the 8 formerly-exempt plugins (regime_transition, prev_day_level_test, anchored_vwap_reversion, poc_rejection, hvn_rejection, cross_asset_divergence, mean_revert/mean_reversion, squeeze_exp/squeeze_expansion):
    1. Set the ClassVar `requires_i6_confluence = True`.
    2. Ensure `capture_signal_features(...)` is called in `compute_full()` and its result passed as `context_features=` to `make_signal_from_frame()` — replicate the exact pattern from the compliant reference plugin. If the call already exists, confirm it is reached on the emission path.
    3. Confirm `ctf_score`, `ctf_confirmed`, `zone_friction_score` are populated in the emitted signal payload (matching how the reference plugin does it).

    Then in register_plugins.py:
    4. Delete the `_I7_I6_EXEMPT` frozenset definition (lines ~671-682).
    5. Remove any reference to `_I7_I6_EXEMPT` anywhere: `grep -rn "_I7_I6_EXEMPT" src/ tests/` must return empty after the edit.
    6. Confirm validate_tier() in base.py no longer has a path that exempts these plugins (if the exempt set was passed into validate_tier, remove that argument/branch so confluence compliance is enforced with no carve-out).

    Do NOT remove the `requires_i6_confluence` ClassVar itself — that removal is Wave 3's job (D-06). This plan only sets it True and deletes the exempt set.
  </action>
  <verify>
    `grep -rn "_I7_I6_EXEMPT" src/ tests/` returns empty. Each of the 8 plugin files contains `requires_i6_confluence = True`. `.venv/bin/pytest tests/unit/ -q` green.
  </verify>
  <done>All 8 plugins have requires_i6_confluence = True and emit confluence annotation; _I7_I6_EXEMPT deleted with zero remaining references; pytest green.</done>
  <acceptance_criteria>
    - `grep -rn "_I7_I6_EXEMPT" src/ tests/` returns zero results
    - Each of the 8 named plugin files contains the literal `requires_i6_confluence = True`
    - Each of the 8 plugin compute_full() emission paths calls capture_signal_features() and passes context_features to make_signal_from_frame()
    - validate_tier() has no carve-out branch referencing the deleted exempt set
    - `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Diagnose 3 time-specific plugins + fix trad_MeanReversion dual-gate</name>
  <files>src/intelligence/trading/session_extremes_setup.py, src/intelligence/trading/orb15.py, src/intelligence/trading/orb30.py, src/intelligence/trading/mean_reversion.py, tests/unit/intelligence/test_orb_plugins.py</files>
  <read_first>
    - src/intelligence/trading/session_extremes_setup.py (compute_full gate conditions)
    - src/intelligence/trading/orb15.py and src/intelligence/trading/orb30.py (compute_full RTH-open range + breakout logic)
    - src/intelligence/trading/mean_reversion.py (Gate A trend_regime threshold, Gate B kalman_price_position threshold)
    - docs/plans/2026-06-14-phase-126-signal-universe-hardening.md (Wave 2 diagnosis steps lines 398-403; MeanReversion SQL probe + decision lines 405-428; D-14, D-16 in CONTEXT.md)
    - production/migrations/128_threshold_config_params.sql (config_schema/config_state seed format for the new threshold APR key)
    - CLAUDE.md (DB query invocation; intelligence_features tier columns: trend_regime in i1, kalman_price_position in i3)
  </read_first>
  <action>
    PART A — Time-specific plugins (D-16): For each of SessionExtremesSetup, ORB15, ORB30, read compute_full() and identify exact gate conditions. Determine a verdict and write it in the plugin docstring as one of:
    - CORRECT-RARE: gate logic is correct, fires legitimately rarely (e.g. ORB once per RTH session). No code change. Document why in docstring.
    - BROKEN: gate condition is wrong. Fix the gate condition.
    - SCOPE-MISMATCH: gate is correct but instrument universe lacks coverage (e.g. no Asian-session instruments). Set `shadow_only = True` with docstring note "instrument universe lacks <X> coverage; set shadow_only=False when instruments added."
    For SessionExtremesSetup, run a quick SQL count of bars matching the session-transition condition to back the verdict. For ORB15/ORB30 verify (a) equity instruments with RTH data exist in the corpus, (b) the first-15/30-min range calc is correct, (c) breakout detection triggers.

    Write tests/unit/intelligence/test_orb_plugins.py with a synthetic RTH-open bar sequence (9:30 ET open) that exercises ORB15 and ORB30: feed bars forming a 15/30-min opening range, then a breakout bar, assert the plugin fires (or assert the documented CORRECT-RARE / BROKEN behavior).

    PART B — trad_MeanReversion dual-gate (D-14): Run the dual-gate mutual-exclusion SQL probe (design doc lines 411-422) against intelligence_features over the last 30 days, counting gate_a_ok, gate_b_ok, both_gates_ok, total_bars (trend_regime in i1 JSONB, kalman_price_position in i3 JSONB — adapt column extraction to actual JSONB tier columns).
    - If `both_gates_ok / total_bars < 0.01`: lower Gate A threshold (trend_regime_max) to 0.2 and add APR key `threshold.mean_reversion.trend_regime_max` = 0.2. Create migration `133_phase126_mean_reversion.sql` (unconditionally — migration 132 is created and applied by P126-01 which runs in parallel, so 132 is never available to append to). Wire mean_reversion.py to read the threshold via config service (or hard-coded fallback = 0.2 per D-18, with TODO comment naming the APR key).
    - If `both_gates_ok / total_bars >= 0.01`: gates are not mutually exclusive; diagnose the real cause (wrong threshold values or wrong feature keys) and fix; still add the APR key for tunability.
    - If after the fix trad_MeanReversion still cannot reach >100 fires in a 30-day window: set `shadow_only = True` with docstring rationale "dual-gate mutual exclusion diagnosed; relaxed to <threshold> but insufficient activation — parked for redesign."

    Record the probe result and the chosen disposition in the SUMMARY.
  </action>
  <verify>
    Each of the 3 time-specific plugin docstrings contains one of CORRECT-RARE/BROKEN/SCOPE-MISMATCH. mean_reversion.py reflects the chosen disposition (relaxed threshold + APR key OR shadow_only=True). `.venv/bin/pytest tests/unit/intelligence/test_orb_plugins.py -q` exits 0.
  </verify>
  <done>Each time-specific plugin docstring has a CORRECT-RARE/BROKEN/SCOPE-MISMATCH verdict; MeanReversion probe run and disposition applied (relaxed+APR key or shadow_only=True); ORB unit test green.</done>
  <acceptance_criteria>
    - session_extremes_setup.py, orb15.py, orb30.py docstrings each contain exactly one of the strings: CORRECT-RARE, BROKEN, SCOPE-MISMATCH
    - tests/unit/intelligence/test_orb_plugins.py exists with a synthetic RTH-open bar sequence and exits 0
    - The MeanReversion dual-gate SQL probe was run; both_gates_ok/total_bars ratio captured in SUMMARY
    - mean_reversion.py either: (a) reads `threshold.mean_reversion.trend_regime_max` via config service with the threshold relaxed, OR (b) has `shadow_only = True` with a docstring rationale string
    - If a threshold APR key was added, config_state contains a row with key `threshold.mean_reversion.trend_regime_max`
    - `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: trad_FVGFill structural audit (diagnostic-first)</name>
  <files>src/intelligence/trading/fvg_fill.py</files>
  <read_first>
    - src/intelligence/trading/fvg_fill.py (compute_full in full — FVG definition, entry trigger, zone_low/zone_high mapping, entry_type)
    - docs/plans/2026-06-14-phase-126-signal-universe-hardening.md (P126-04 lines 430-455; D-15 in CONTEXT.md)
    - An SMC tier reference for fvg_bottom/fvg_top features (grep `fvg_bottom\|fvg_top` in src/intelligence/ to see if FVGFill should read SMC tier output vs recompute)
    - CLAUDE.md (entry_type values: at_close, at_pullback, at_limit, at_reclaim, zone_proximal; DB query invocation)
  </read_first>
  <action>
    Diagnostic-first per D-15 — do NOT change code until root cause is confirmed.

    1. Read fvg_fill.py compute_full() and document in the SUMMARY:
       - What defines an FVG (which feature fields, what calculation)
       - What triggers entry — price proximity to gap (close near fvg_bottom) vs confirmed penetration (low < fvg_bottom AND close > fvg_bottom)
       - How zone coordinates map to zone_low/zone_high (does zone_width depend on actual gap size?)
       - What entry_type the plugin uses (at_close = aggressive/approach; at_limit = at fill price)
       - Whether FVG is read from SMC tier (fvg_bottom/fvg_top) or recomputed in the plugin body
    2. Run the FVGFill zone-width comparison SQL (design doc lines 441-450) comparing trad_FVGFill to trad_SupplyDemandSetup, trad_LiquiditySweepReclaim, trad_CHoCHReversal.
    3. Verdict (write into the fvg_fill.py docstring):
       - ENTRY-TIMING DEFECT confirmed (fires on approach, not on fill confirmation): fix compute_full() to require penetration + close inside the gap before triggering (low penetrates fvg boundary AND close back inside gap). Add an APR key for the confirmation threshold if one is needed. After the fix, run a before/after SQL comparison on signal_ledger to confirm equity win rate direction improves.
       - ZONE QUALITY issue (stale/spread-artifact gaps): cross-validate FVG coordinates against SMC tier output; switch to reading SMC fvg_bottom/fvg_top if recomputed locally.
       - NO CLEAR DEFECT: set `shadow_only = True` with docstring rationale "catastrophic equity performance (8.93% win rate), mechanism unknown, parked for redesign."

    Record the diagnostic findings and chosen verdict in the SUMMARY.
  </action>
  <verify>
    fvg_fill.py docstring contains the FVGFill verdict (entry-timing fix applied / zone-quality fix / shadow_only=True). `.venv/bin/pytest tests/unit/ -q` green.
  </verify>
  <done>FVGFill mechanics documented; zone-width comparison run; docstring carries an explicit verdict (entry-timing fix / zone-quality fix / shadow_only=True); pytest green.</done>
  <acceptance_criteria>
    - SUMMARY documents the FVG definition, entry trigger, zone mapping, entry_type, and SMC-vs-recompute finding from fvg_fill.py
    - The FVGFill zone-width comparison SQL was run and its result captured
    - fvg_fill.py docstring contains one explicit verdict among: entry-timing defect (with the penetration+close-inside fix), zone-quality fix, or shadow_only=True with rationale
    - If shadow_only chosen, fvg_fill.py contains `shadow_only = True` and the rationale string
    - If an entry-timing fix was applied, a before/after signal_ledger comparison is documented in SUMMARY
    - `.venv/bin/pytest tests/unit/ -q` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `grep -rn "_I7_I6_EXEMPT" src/ tests/` empty
- 8 plugins compliant (requires_i6_confluence = True)
- 3 time-specific plugin verdicts in docstrings
- MeanReversion + FVGFill dispositions documented and applied
- `.venv/bin/pytest tests/unit/ -q` green; ruff clean on touched files
</verification>

<success_criteria>
Maps to design doc Success Criteria 5, 6, 7 and Roadmap SC 4, 5, 6. SIGNAL-QUALITY-02.
</success_criteria>

<output>
After completion, create `.planning/phases/126-signal-universe-hardening/126-02-SUMMARY.md`
</output>
