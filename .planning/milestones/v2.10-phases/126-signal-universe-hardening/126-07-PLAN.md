---
phase: 126-signal-universe-hardening
plan: "07"
type: execute
wave: 5
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SIGNAL-QUALITY-01
  - SIGNAL-QUALITY-02
must_haves:
  truths:
    - "REQUIREMENTS.md SIGNAL-QUALITY-01 text describes gate in frame_trade() returning _reject_frame, not _resolve_zone_bounds() returning no_signal()"
    - "REQUIREMENTS.md SIGNAL-QUALITY-01 APR key suffixes are .equity/.fx/.futures with values 1.5/1.0/1.5"
    - "REQUIREMENTS.md SIGNAL-QUALITY-01 notes stopped_at_entry rate measurement is Phase 127 scope (REPLAY-01)"
    - "REQUIREMENTS.md SIGNAL-QUALITY-02 text names _I7_I6_EXEMPT (not _CONFLUENCE_EXEMPT_PLUGINS)"
    - "REQUIREMENTS.md SIGNAL-QUALITY-02 describes pipeline annotation via signal_processor._annotate_signal(), not per-plugin capture_signal_features() calls"
    - "REQUIREMENTS.md SIGNAL-QUALITY-02 mentions SIGNAL_SCHEMA_VERSION bumped to v4"
    - "Both SIGNAL-QUALITY-01 and SIGNAL-QUALITY-02 marked [x] Complete in traceability table"
    - "Last updated date in REQUIREMENTS.md updated to 2026-06-15"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement text for SIGNAL-QUALITY-01 and SIGNAL-QUALITY-02"
      contains: "frame_trade"
---

<objective>
Close the three REQUIREMENTS.md staleness gaps identified in Phase 126 VERIFICATION.md. All gaps are documentation fixes — no code changes. The implementation is architecturally correct; the requirements text was written before final design decisions (D-01, D-06) were made and before Wave 3 (pipeline annotation) was added to Phase 126 scope.

Gap 1 — SIGNAL-QUALITY-01: Text says gate in `_resolve_zone_bounds()` returning `no_signal()`. Actual: gate in `frame_trade()` returning `_reject_frame("zone_too_narrow:...")`. APR key names wrong (equity_etf/forex vs equity/fx). Seed values wrong (0.5/0.25/0.35 vs 1.5/1.0/1.5). stopped_at_entry rate measurement deferred to Phase 127.

Gap 2 — SIGNAL-QUALITY-02: Text demands `_CONFLUENCE_EXEMPT_PLUGINS` deleted (actual: `_I7_I6_EXEMPT`), per-plugin `requires_i6_confluence=True` and `capture_signal_features()` calls (both removed in Wave 3 via pipeline annotation). Annotation now lives in `signal_processor._annotate_signal()`. Schema version bumped to v4.

Gap 3 — Traceability table: Both requirements marked `Pending` despite implementation complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-signal-universe-hardening/126-CONTEXT.md
@.planning/phases/126-signal-universe-hardening/126-VERIFICATION.md
</context>

<tasks>

<task id="126-07-T01">
<title>Update SIGNAL-QUALITY-01 requirement text and mark complete</title>
<read_first>
  - .planning/REQUIREMENTS.md — current SIGNAL-QUALITY-01 text at line 27
  - .planning/phases/126-signal-universe-hardening/126-VERIFICATION.md — gap 1 detail (stale gate location, key names, threshold values, stopped_at_entry deferral)
  - .planning/phases/126-signal-universe-hardening/126-CONTEXT.md — D-01, D-03 decisions (gate in frame_trade, APR key names, data-derived thresholds)
  - production/migrations/134_phase126_apr_seeds.sql — actual APR key names and seed values as inserted
</read_first>
<action>
Replace the SIGNAL-QUALITY-01 line in .planning/REQUIREMENTS.md with text that reflects the actual implementation:

- Gate location: `frame_trade()` AFTER `_resolve_zone_bounds()` returns, using `_reject_frame("zone_too_narrow:{zone_source}", ...)` — NOT in `_resolve_zone_bounds()` and NOT returning `no_signal()`
- APR keys: `feature.zone_engine.min_zone_width_atr.equity` = 1.5, `feature.zone_engine.min_zone_width_atr.fx` = 1.0, `feature.zone_engine.min_zone_width_atr.futures` = 1.5 (data-derived from noise-band analysis)
- stopped_at_entry rate < 15%: measurement deferred to Phase 127 (REPLAY-01) — gate is enforced prospectively in Phase 126 code; historical signal_ledger lacks outcome data
- Change `[ ]` to `[x]`
</action>
<acceptance_criteria>
  - REQUIREMENTS.md line 27 contains "frame_trade" and "_reject_frame" — not "_resolve_zone_bounds" or "no_signal"
  - REQUIREMENTS.md line 27 contains ".equity" and ".fx" and ".futures" as APR key suffixes
  - REQUIREMENTS.md line 27 contains "1.5" and "1.0" as threshold values
  - REQUIREMENTS.md line 27 contains "Phase 127" referencing the stopped_at_entry measurement deferral
  - REQUIREMENTS.md line 27 starts with "- [x]" not "- [ ]"
</acceptance_criteria>
</task>

<task id="126-07-T02">
<title>Update SIGNAL-QUALITY-02 requirement text and mark complete</title>
<read_first>
  - .planning/REQUIREMENTS.md — current SIGNAL-QUALITY-02 text at line 28
  - .planning/phases/126-signal-universe-hardening/126-VERIFICATION.md — gap 2 detail (frozenset name, pipeline annotation architecture, v4 schema version)
  - .planning/phases/126-signal-universe-hardening/126-CONTEXT.md — D-06 (delete _I7_I6_EXEMPT), D-07 (pipeline annotation via _annotate_signal), D-10 (deprecate capture_signal_features), D-11 (SIGNAL_SCHEMA_VERSION bump)
  - src/intelligence/pipeline/signal_processor.py — confirm _annotate_signal() function name and location
</read_first>
<action>
Replace the SIGNAL-QUALITY-02 line in .planning/REQUIREMENTS.md with text that reflects the actual implementation:

- Frozenset deleted: `_I7_I6_EXEMPT` (not `_CONFLUENCE_EXEMPT_PLUGINS`)
- Annotation architecture: moved to `signal_processor._annotate_signal()` pipeline layer — NOT per-plugin `requires_i6_confluence=True` or `capture_signal_features()` calls (both attributes removed in Wave 3)
- `capture_signal_features()` marked DEPRECATED in `confidence_utils.py` (not deleted — Phase 128)
- `SIGNAL_SCHEMA_VERSION` bumped to "v4" in `signal_schema.py`
- All 8 formerly-exempt plugins still registered in TIER_I7 with full ECL annotation via pipeline
- Anti-signal plugins demoted to `shadow_only=True` with rationale per audit results (Wave 4)
- Change `[ ]` to `[x]`
</action>
<acceptance_criteria>
  - REQUIREMENTS.md line 28 contains "_I7_I6_EXEMPT" — not "_CONFLUENCE_EXEMPT_PLUGINS"
  - REQUIREMENTS.md line 28 contains "_annotate_signal" referencing pipeline annotation
  - REQUIREMENTS.md line 28 does NOT demand per-plugin "capture_signal_features()" calls (those were intentionally removed)
  - REQUIREMENTS.md line 28 contains "v4" referencing SIGNAL_SCHEMA_VERSION
  - REQUIREMENTS.md line 28 starts with "- [x]" not "- [ ]"
</acceptance_criteria>
</task>

<task id="126-07-T03">
<title>Update traceability table and last-updated date</title>
<read_first>
  - .planning/REQUIREMENTS.md — traceability table around lines 77-78 and last-updated line around line 92
</read_first>
<action>
1. In the traceability table, change both:
   - `| SIGNAL-QUALITY-01 | Phase 126 | Pending |` → `| SIGNAL-QUALITY-01 | Phase 126 | Complete |`
   - `| SIGNAL-QUALITY-02 | Phase 126 | Pending |` → `| SIGNAL-QUALITY-02 | Phase 126 | Complete |`

2. Update the last-updated line to: `*Last updated: 2026-06-15 — Phase 126 gap closure: SIGNAL-QUALITY-01/02 text corrected and marked Complete; stopped_at_entry measurement deferred to Phase 127 REPLAY-01*`
</action>
<acceptance_criteria>
  - REQUIREMENTS.md traceability table shows "| SIGNAL-QUALITY-01 | Phase 126 | Complete |"
  - REQUIREMENTS.md traceability table shows "| SIGNAL-QUALITY-02 | Phase 126 | Complete |"
  - Last-updated line contains "2026-06-15"
  - No other rows in the table changed
</acceptance_criteria>
</task>

</tasks>

<verification>
Read .planning/REQUIREMENTS.md after all tasks complete. Confirm:
1. SIGNAL-QUALITY-01 text: "frame_trade" + "_reject_frame" + ".equity"/".fx"/".futures" + "1.5"/"1.0" + "Phase 127" (deferral) + "[x]"
2. SIGNAL-QUALITY-02 text: "_I7_I6_EXEMPT" + "_annotate_signal" + "v4" + "[x]"; does NOT contain "capture_signal_features" as a current requirement
3. Traceability table: both rows show "Complete"
4. Last-updated date: 2026-06-15
</verification>
