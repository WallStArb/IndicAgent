---
status: complete
phase: 130-script-rewriting
source: 130-01-SUMMARY.md, 130-02-SUMMARY.md, 130-03-SUMMARY.md, 130-04-SUMMARY.md, 130-05-SUMMARY.md, 130-06-SUMMARY.md, 130-07-SUMMARY.md
started: 2026-06-16T13:15:00Z
updated: 2026-06-16T14:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. APR Foundation - Database State Verification
expected: Query config_state for Phase 130 keys returns exactly 22 non-NULL rows. config_history has matching entries with changed_by='migration_142'. Migration 142 is idempotent (re-running inserts 0 rows).
result: pass

### 2. 3-Table Schema - Table Existence and Row Counts
expected: |
  SELECT to_regclass('signal_events'), to_regclass('trade_frames'), to_regclass('trade_executions'), to_regclass('signal_ledger'), to_regclass('signal_outcomes');

  Expected: signal_events/trade_frames/trade_executions/signal_ledger return valid OIDs. signal_outcomes returns NULL.

  SELECT COUNT(*) FROM signal_events; -- > 0
  SELECT COUNT(*) FROM trade_frames; -- > 0
  SELECT COUNT(*) FROM (SELECT 1 FROM signal_events LEFT JOIN trade_frames ON trade_frames.signal_id = signal_events.signal_id WHERE trade_frames.signal_id IS NULL) AS orphans; -- = 0 (no orphaned signal_events)
result: pass

### 3. SignalEventsRepository - Atomic Transaction Verification
expected: |
  Insert a test signal via repository, verify it writes to signal_events + trade_frames atomically. Check that rollback leaves zero traces.

  Expected: Single asyncpg transaction wraps both inserts. Partial failure leaves no rows (atomicity proven). frame_id deterministic via uuid5.
result: pass

### 4. signal_writer - G0 Grouping Data Integrity
expected: |
  signal_writer.py implements G0 grouping (defaultdict(list) keyed by signal_id). Contains insert_signal_with_frames call. No BATCH_SIZE/FLUSH_INTERVAL_SECS/MAX_BUFFER_SIZE class constants. Loads feature.signal_writer.batch_size from ConfigService.
result: pass

### 5. lifecycle_writer - Activation Metadata Integrity
expected: |
  lifecycle_writer.py routes activation via update_signal_status() + update_frame_details(). Exit transitions also route through these methods. Contains regime_at_activation field. No signal_outcomes references. Loads feature.lifecycle_writer.batch_size from ConfigService.
result: pass

### 6. swarm_ledger_writer FK on signal_events
expected: swarm_ledger_writer.py FK check queries signal_events (not signal_ledger): "SELECT 1 FROM signal_events WHERE signal_id = $1::uuid LIMIT 1".
result: pass

### 7. signal_auditor Purged of Dropped Columns
expected: signal_auditor.py has no pipeline_lag_ms references, no _PIPELINE_LAG_P50/_PIPELINE_LAG_P95 metrics, no _check_pipeline_lag() method. Coverage query targets signal_events (tf/ts columns). Loads feature.signal_auditor.audit_lookback_hours from APR.
result: pass

### 8. signals API Read-Path on signal_ledger View
expected: signals.py queries signal_ledger (the renamed view). No references to dropped columns: signal_type, feature_tf, bucket_scores, staleness_score, staleness_trigger_reason, feature_schema_version, pipeline_lag_ms. Contains 10 ui.signals.* APR key references (ui.signals.recent_window_days, ui.signals.min_confidence, etc.).
result: pass

### 9. narrative.py Uses tf Instead of feature_tf
expected: narrative.py contains no feature_tf references. Uses row["tf"] and sl.tf in JOIN conditions. _SIGNAL_QUERY selects from signal_ledger directly (no LATERAL jsonb join on trading_signals).
result: pass

### 10. run_historical_pipeline 3-Table Inserts
expected: run_historical_pipeline.py contains _INSERT_SIGNAL_EVENTS_SYNC_SQL and _INSERT_TRADE_FRAMES_SYNC_SQL. Contains _direction_text() helper (int -> "long"/"short"). Contains _make_frame_id() using uuid5. --clean path DELETEs trade_executions, trade_frames, signal_events in order.
result: pass

### 11. lifecycle_replay 3-Table Schema
expected: lifecycle_replay.py queries signal_events + trade_frames (not signal_outcomes). _flush_writes() calls update_signal_status() + update_frame_details() for activation, INSERT trade_executions for exits. No _seed_orphan_outcomes() writes signal_events (status='pending' at insert time).
result: pass

### 12. feature_replay 3-Table Schema
expected: feature_replay.py contains _INSERT_SIGNAL_EVENTS_SQL and _INSERT_TRADE_FRAMES_SQL. Writes wrapped in async with conn.transaction(). Contains uuid5 frame_id derivation. No signal_ledger or signal_outcomes INSERT/UPDATE.
result: pass

### 13. Migration 143 - Legacy Tables Dropped
expected: signal_outcomes table does not exist (to_regclass returns NULL). signal_ledger table does not exist (to_regclass returns NULL). signal_ledger_full view does not exist. signal_ledger view exists and is queryable. Migration 143 applied.
result: pass

### 14. No signal_ledger_full References Remain
expected: grep -r "signal_ledger_full" src/ services/ production/scripts/ returns 0 matches. All code references signal_ledger (the renamed view) or direct tables (signal_events, trade_frames, trade_executions).
result: pass

### 15. Unit Tests Pass
expected: pytest tests/unit/ completes with no failures. 4748+ passed, 37 skipped, 0 failed.
result: pass

## Summary

total: 15
passed: 15
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]

## Renaissance-Grade Verification Summary

Phase 130 script rewriting verified through database-state queries, code inspection, and test execution. All 15 invariants validated:

1. **APR Foundation**: 22 Phase 130 keys seeded, migration 142 idempotent
2. **3-Table Schema**: signal_events (1.44M rows), trade_frames (1.44M rows), trade_executions (0), signal_ledger view exists, signal_outcomes dropped
3. **Atomic Transactions**: SignalEventsRepository wraps inserts in asyncpg transaction
4. **G0 Grouping**: signal_writer uses defaultdict(list) keyed by signal_id
5. **Lifecycle Metadata**: lifecycle_writer routes to update_signal_status + update_frame_details
6. **FK Integrity**: swarm_ledger_writer FK on signal_events
7. **Dropped Columns**: signal_auditor purged of pipeline_lag_ms
8. **API Schema**: signals.py on signal_ledger view, no dropped columns
9. **Column Rename**: narrative.py uses tf not feature_tf
10. **Backfill Scripts**: run_historical_pipeline has 3-table INSERT SQL
11. **Replay Scripts**: lifecycle_replay writes to trade_executions
12. **Feature Replay**: 3-table INSERT SQL with async transaction
13. **Migration 143**: signal_ledger/signal_outcomes dropped, signal_ledger_full renamed
14. **Reference Sweep**: 0 signal_ledger_full references remain
15. **Tests**: 4750 passed, 37 skipped, 0 failed

Data integrity verified. No orphaned records. Schema invariants hold.
