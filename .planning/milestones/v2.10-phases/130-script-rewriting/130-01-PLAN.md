---
phase: 130-script-rewriting
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [REWRITE-01]
files_modified:
  - src/config/config_service.py
  - production/migrations/142_phase130_apr_seeds.sql
autonomous: true

must_haves:
  truths:
    - "ui.* and weights.* parameter keys can be written via ConfigService.set()"
    - "All 22 Phase 130 APR keys exist in both config_schema and config_state with seed values"
    - "Services reading these keys via ConfigService.get() resolve real values, not just code defaults"
  artifacts:
    - path: "src/config/config_service.py"
      provides: "OPS_PREFIXES tuple including ui. and weights."
      contains: '"ui."'
    - path: "production/migrations/142_phase130_apr_seeds.sql"
      provides: "config_schema + config_state inserts for 22 keys"
      contains: "feature.signal_writer.batch_size"
  key_links:
    - from: "src/config/config_service.py OPS_PREFIXES"
      to: "ConfigService.set() prefix validation"
      via: "any(key.startswith(prefix) for prefix in self.OPS_PREFIXES)"
      pattern: "OPS_PREFIXES"
    - from: "production/migrations/142_phase130_apr_seeds.sql"
      to: "config_state table"
      via: "INSERT ... ON CONFLICT DO NOTHING"
      pattern: "INSERT INTO config_state"
---

<objective>
Lay the APR foundation for the Phase 130 write-path rewrite: extend OPS_PREFIXES so ui.* and weights.* keys are writable through ConfigService, and seed all 22 new Phase 130 parameter keys into config_schema + config_state via migration 142.

Purpose: Every service rewritten in later waves loads its operational constants from APR. Those keys must exist before the services try to read them. This plan is independent of the repository rewrite and runs first (Wave 1).
Output: Updated config_service.py OPS_PREFIXES; new migration 142_phase130_apr_seeds.sql applied to the database.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-script-rewriting/130-CONTEXT.md
@.planning/phases/130-script-rewriting/130-RESEARCH.md
@docs/foundation/adaptive-parameter-registry.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ui. and weights. to OPS_PREFIXES</name>
  <read_first>
    - src/config/config_service.py (read the OPS_PREFIXES ClassVar at line 39 and the set() validation at line 73 to see current prefix tuple and how it gates writes)
    - CLAUDE.md (the APR section note: "ui.* requires one-line change first: add ui. to OPS_PREFIXES")
  </read_first>
  <action>
    In src/config/config_service.py, add the two string entries "ui." and "weights." to the OPS_PREFIXES ClassVar tuple (currently at line 39). Preserve all existing entries (regime., swarm., alert., ai., feature., threshold., roll., cross_asset., macro.). The tuple type annotation ClassVar[tuple[str, ...]] is unchanged. This is the prerequisite for seeding ui.signals.* keys and for making the pre-existing weights.* keys writable via ConfigService.set() rather than direct SQL only. Do not change the set() validation logic at line 73 — it already iterates OPS_PREFIXES.
  </action>
  <verify>.venv/bin/python -c "from src.config.config_service import ConfigService; assert 'ui.' in ConfigService.OPS_PREFIXES and 'weights.' in ConfigService.OPS_PREFIXES, ConfigService.OPS_PREFIXES; print('ok')"</verify>
  <acceptance_criteria>
    - config_service.py OPS_PREFIXES tuple contains the literal string "ui."
    - config_service.py OPS_PREFIXES tuple contains the literal string "weights."
    - All 9 pre-existing prefix entries remain present in the tuple
    - Command above prints "ok" and exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create migration 142 seeding all 22 Phase 130 APR keys</name>
  <read_first>
    - .planning/phases/130-script-rewriting/130-CONTEXT.md (D-12 table — the authoritative list of all 22 keys, seed values, and provenance tags)
    - production/migrations/134_phase126_apr_seeds.sql (existing APR seed migration — replicate its config_schema + config_state INSERT structure, column names, and ON CONFLICT pattern)
    - production/migrations/141_trade_frames_labeled_data_index.sql (confirm 141 is the latest applied migration so 142 is the correct next number)
  </read_first>
  <action>
    Create production/migrations/142_phase130_apr_seeds.sql. Insert all 22 keys from CONTEXT.md D-12 into config_schema (config_key, description, data_type, is_ml_target) and config_state (config_key, config_value, version=1), using ON CONFLICT (config_key) DO NOTHING for idempotency. Match the exact column set used by migration 134. Keys and seed values (from D-12):
    feature.signal_writer.batch_size=100 (integer), feature.signal_writer.flush_interval_secs=5.0 (float), feature.signal_writer.max_buffer_size=10000 (integer), feature.lifecycle_writer.batch_size=100 (integer), feature.lifecycle_writer.flush_interval_secs=5.0 (float), feature.lifecycle_writer.max_buffer_size=10000 (integer), feature.signal_tracker.bootstrap_pending_window_days=7 (integer), feature.signal_tracker.bootstrap_active_window_days=30 (integer), feature.signal_tracker.bootstrap_dedup_window_days=3 (integer), feature.signal_tracker.bootstrap_max_attempts=3 (integer), threshold.signal_tracker.staleness_score=0.5 (float), feature.signal_auditor.audit_lookback_hours=1 (integer), ui.signals.recent_window_days=90 (integer), ui.signals.min_confidence=0.40 (float), ui.signals.min_cis_score=0.35 (float), ui.signals.today_window_hours=24 (integer), ui.signals.yesterday_window_hours=48 (integer), ui.signals.short_window_days=7 (integer), ui.signals.medium_window_days=30 (integer), ui.signals.latency_threshold_minutes=5 (integer), ui.signals.max_results=500 (integer), ui.signals.top_n_results=10 (integer).
    Every description must carry its provenance tag from D-12 ([initial_estimate], [data-derived]) and note "NOT ML target" since all are operational/UX parameters (is_ml_target=false for all 22). Do NOT include the DROP statements — those belong to plan 07's migration 143. After writing the file, apply it: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/142_phase130_apr_seeds.sql
  </action>
  <verify>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT count(*) FROM config_state WHERE config_key LIKE 'feature.signal_writer.%' OR config_key LIKE 'ui.signals.%' OR config_key = 'threshold.signal_tracker.staleness_score' OR config_key LIKE 'feature.signal_tracker.bootstrap_%' OR config_key = 'feature.signal_auditor.audit_lookback_hours' OR config_key LIKE 'feature.lifecycle_writer.%'"</verify>
  <acceptance_criteria>
    - File production/migrations/142_phase130_apr_seeds.sql exists
    - File contains all 22 config_key literals from D-12 (grep each key name returns a match)
    - File contains no "DROP TABLE" statement
    - Verify query returns 22
    - PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT config_value FROM config_state WHERE config_key='ui.signals.min_confidence'" returns 0.40
  </acceptance_criteria>
</task>

</tasks>

<verification>
- ConfigService.OPS_PREFIXES contains "ui." and "weights."
- Migration 142 applied; 22 keys present in config_state with correct seed values
- No DROP statements in migration 142
</verification>

<success_criteria>
- ui.* and weights.* are writable via ConfigService.set() prefix validation
- All 22 Phase 130 APR keys resolvable via ConfigService.get() with seeded values
- Migration 142 is idempotent (ON CONFLICT DO NOTHING) and re-runnable
</success_criteria>

<output>
After completion, create `.planning/phases/130-script-rewriting/130-01-SUMMARY.md`
</output>
