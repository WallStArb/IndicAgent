---
phase: 130-script-rewriting
plan: 04
type: execute
wave: 3
depends_on: [130-01, 130-02]
requirements: [REWRITE-01]
files_modified:
  - services/signal_tracker.py
  - services/swarm_ledger_writer.py
  - services/signal_auditor.py
  - services/signal_probe_auditor.py
autonomous: true

must_haves:
  truths:
    - "signal_tracker bootstrap loads active signals by querying signal_events + trade_frames directly, with non-NULL lifecycle state"
    - "swarm_ledger_writer FK existence check reads signal_events, not signal_ledger"
    - "signal_auditor coverage/lag queries run against the post-rename schema without referencing dropped columns"
    - "signal_probe_auditor no longer LEFT JOINs the dropped signal_outcomes table"
  artifacts:
    - path: "services/signal_tracker.py"
      provides: "bootstrap via SignalEventsRepository.get_active_signals_for_bootstrap"
      contains: "get_active_signals_for_bootstrap"
    - path: "services/swarm_ledger_writer.py"
      provides: "FK check on signal_events"
      contains: "FROM signal_events"
  key_links:
    - from: "services/signal_tracker.py _bootstrap_active_signals"
      to: "SignalEventsRepository.get_active_signals_for_bootstrap"
      via: "direct 3-table JOIN, APR-bounded windows"
      pattern: "get_active_signals_for_bootstrap"
    - from: "services/swarm_ledger_writer.py"
      to: "signal_events existence check"
      via: "SELECT 1 FROM signal_events WHERE signal_id"
      pattern: "FROM signal_events WHERE signal_id"
---

<objective>
Rewrite the tracker and the audit/FK services. signal_tracker's bootstrap must query signal_events + trade_frames directly (the signal_ledger_full view NULLs lifecycle fields). swarm_ledger_writer's FK existence check moves to signal_events. signal_auditor drops references to dropped columns (pipeline_lag_ms). signal_probe_auditor removes its breaking signal_outcomes JOIN. signal_tracker also migrates its bootstrap window/attempt constants to APR.

Purpose: These services either read lifecycle state (tracker bootstrap, which RESEARCH flagged as broken against the view) or have hidden dependencies on dropped tables/columns (probe_auditor JOIN, auditor pipeline_lag_ms). All consume the SignalEventsRepository from plan 02.
Output: signal_tracker.py bootstrap rewritten + APR; swarm_ledger_writer.py FK on signal_events; signal_auditor.py and signal_probe_auditor.py free of dropped-table/column references.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-script-rewriting/130-CONTEXT.md
@.planning/phases/130-script-rewriting/130-RESEARCH.md
@src/persistence/repository/signal_events_repository.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite signal_tracker bootstrap query and migrate constants to APR</name>
  <read_first>
    - services/signal_tracker.py (read the bootstrap region: lines ~1137-1250 _bootstrap_active_signals, _bootstrap_apply_signal, the MAE/MFE bootstrap at ~548, and __init__ constants at ~127; the file is 1302 lines — focus on bootstrap + constants)
    - src/persistence/repository/signal_events_repository.py (get_active_signals_for_bootstrap signature and the canonical dict shape it returns, including targets adapted as a single-element list)
    - .planning/phases/130-script-rewriting/130-RESEARCH.md (Pattern 6 bootstrap JOIN query, Pitfall 1 NULL columns from view, Open Question 2 targets column, Pattern 7 APR loading)
    - .planning/phases/130-script-rewriting/130-CONTEXT.md (D-12 APR keys feature.signal_tracker.bootstrap_*, threshold.signal_tracker.staleness_score; D-14 docstring updates)
  </read_first>
  <action>
    Replace signal_tracker's bootstrap DB read so it calls repository.get_active_signals_for_bootstrap() instead of the inline signal_ledger_full query (line ~1166). The new query (implemented in plan 02) JOINs signal_events + trade_frames and pulls lifecycle metadata (entry_zone_low/high, trailing_stop_price, chandelier_vol_source, activated_at) from trade_frames.frame_details JSONB, returning non-NULL lifecycle state. Adapt the canonical dict construction: target_price becomes targets=[target_price] for evaluate_signal() compatibility. Remove the MAE/MFE bootstrap-from-signal_outcomes logic (~548) and its comment; live signals default MAE/MFE to 0.0 (correct), bootstrapped signals read MAE/MFE from frame_details if present. Migrate hardcoded constants to APR loaded in _setup(): bootstrap_max_attempts -> feature.signal_tracker.bootstrap_max_attempts (default 3); the SQL INTERVAL day windows -> feature.signal_tracker.bootstrap_pending_window_days (7), bootstrap_active_window_days (30), bootstrap_dedup_window_days (3), passed as integer params into $N * INTERVAL '1 day'; staleness threshold imported from lifecycle_tracker -> threshold.signal_tracker.staleness_score (0.5). Update the import to SignalEventsRepository. Update all docstrings referencing signal_ledger_full bootstrap to reference signal_events (D-14). except X as error:; no event= to structlog.
  </action>
  <verify>.venv/bin/python -c "import services.signal_tracker as m, inspect; src=inspect.getsource(m); assert 'get_active_signals_for_bootstrap' in src and 'signal_events_repository' in src; assert 'FROM signal_ledger_full' not in src; assert 'feature.signal_tracker.bootstrap_pending_window_days' in src; print('ok')"</verify>
  <acceptance_criteria>
    - signal_tracker.py calls get_active_signals_for_bootstrap (grep matches)
    - signal_tracker.py contains no "FROM signal_ledger_full" and no "signal_outcomes"
    - signal_tracker.py loads feature.signal_tracker.bootstrap_pending_window_days and bootstrap_max_attempts from config_service
    - signal_tracker.py imports from signal_events_repository
    - Verify command prints "ok"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update swarm_ledger_writer FK check, signal_auditor, and signal_probe_auditor</name>
  <read_first>
    - services/swarm_ledger_writer.py (read the FK existence check at line ~214 and the surrounding retry/backoff context at lines 33-44, 76, 198-214)
    - services/signal_auditor.py (read the coverage query at ~215, the pipeline_lag_ms metrics at ~63-67 and ~190-273, and the INTERVAL '1 hour' at ~273)
    - services/signal_probe_auditor.py (read the LEFT JOIN signal_outcomes at line 216-217 and the second query at ~275)
    - .planning/phases/130-script-rewriting/130-CONTEXT.md (D-06 swarm FK check; D-12 audit_lookback_hours)
    - .planning/phases/130-script-rewriting/130-RESEARCH.md (column gaps table: pipeline_lag_ms dropped; signal_probe_auditor caveat)
  </read_first>
  <action>
    swarm_ledger_writer.py: change the FK existence check from "SELECT 1 FROM signal_ledger WHERE signal_id = $1::uuid LIMIT 1" to "SELECT 1 FROM signal_events WHERE signal_id = $1::uuid LIMIT 1" (direct table, no JOIN overhead per D-06). The signal_ai_enrichment table and the retry/backoff logic are unchanged. Update comments that say "signal_ledger row visible" to "signal_events row visible".
    signal_auditor.py: pipeline_lag_ms no longer exists in the new schema (RESEARCH column-gaps table). Remove the P50/P95 pipeline_lag_ms metrics and their lag query (the column is dropped, not relocated). Keep the coverage count query but ensure it reads signal_events directly. Migrate the INTERVAL '1 hour' lookback to feature.signal_auditor.audit_lookback_hours (default 1) loaded from config_service in _setup(), passed as integer param into $N * INTERVAL '1 hour'. Update the docstring to drop pipeline_lag_ms claims and reference signal_events.
    signal_probe_auditor.py: remove "LEFT JOIN signal_outcomes so USING (signal_id)" at line 217; any so.* columns referenced must be sourced from signal_events instead (status) or dropped if no replacement exists. This query is SELECT-only but breaks after signal_outcomes DROP (RESEARCH Tertiary finding) — it must be fixed in Phase 130. Change the FROM signal_ledger_full references to signal_events where the columns selected are detection-level; leave view references only where the JOIN view genuinely provides the columns post-I8.
  </action>
  <verify>.venv/bin/python -c "import services.swarm_ledger_writer, services.signal_auditor, services.signal_probe_auditor; print('import ok')"</verify>
  <acceptance_criteria>
    - swarm_ledger_writer.py contains "FROM signal_events WHERE signal_id" and contains no "FROM signal_ledger WHERE signal_id"
    - signal_probe_auditor.py contains no "LEFT JOIN signal_outcomes"
    - grep -c "signal_outcomes" services/signal_probe_auditor.py returns 0
    - signal_auditor.py contains no "pipeline_lag_ms"
    - signal_auditor.py loads feature.signal_auditor.audit_lookback_hours from config_service
    - All three modules import without error (verify command prints "import ok")
  </acceptance_criteria>
</task>

</tasks>

<verification>
- signal_tracker bootstrap reads signal_events + trade_frames directly with non-NULL lifecycle state
- swarm_ledger_writer FK check on signal_events
- signal_auditor free of dropped pipeline_lag_ms; lookback from APR
- signal_probe_auditor free of signal_outcomes JOIN
</verification>

<success_criteria>
- Tracker bootstrap no longer silently loads zero-state signals from the NULLing view
- No service has a hidden write or JOIN dependency on signal_outcomes / dropped columns
- APR-backed tracker windows and auditor lookback
</success_criteria>

<output>
After completion, create `.planning/phases/130-script-rewriting/130-04-SUMMARY.md`
</output>
