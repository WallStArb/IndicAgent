---
phase: 130-script-rewriting
plan: 02
type: execute
wave: 2
depends_on: [130-01]
requirements: [REWRITE-01]
files_modified:
  - src/persistence/repository/signal_events_repository.py
  - src/persistence/repository/signal_ledger_repository.py
  - src/intelligence/enums/signal_status.py
  - src/intelligence/enums/__init__.py
  - src/intelligence/schemas.py
  - src/intelligence/ml/confidence_calibrator.py
  - src/intelligence/trading/lifecycle_tracker.py
  - src/intelligence/weight_updater.py
autonomous: true

must_haves:
  truths:
    - "SignalEventsRepository writes signal_events + trade_frames atomically in one transaction"
    - "Lifecycle status updates target signal_events.status; activation metadata targets trade_frames.frame_details JSONB"
    - "Bootstrap query reads signal_events + trade_frames directly, not the signal_ledger_full view"
    - "All 13 importers resolve the new SignalEventsRepository class name without ImportError"
  artifacts:
    - path: "src/persistence/repository/signal_events_repository.py"
      provides: "SignalEventsRepository class with 3-table SQL"
      contains: "class SignalEventsRepository"
      min_lines: 200
    - path: "src/persistence/repository/signal_ledger_repository.py"
      provides: "backward-compat shim re-exporting SignalEventsRepository"
      contains: "SignalEventsRepository"
  key_links:
    - from: "SignalEventsRepository.insert_signal_with_frames"
      to: "signal_events + trade_frames tables"
      via: "async with conn.transaction()"
      pattern: "INSERT INTO signal_events"
    - from: "SignalEventsRepository.get_active_signals_for_bootstrap"
      to: "signal_events LEFT JOIN trade_frames"
      via: "direct table JOIN (not view)"
      pattern: "FROM signal_events"
    - from: "all importers"
      to: "signal_events_repository"
      via: "from src.persistence.repository.signal_events_repository import"
      pattern: "signal_events_repository import"
---

<objective>
Rewrite the central persistence hub. Rename SignalLedgerRepository to SignalEventsRepository (file signal_ledger_repository.py to signal_events_repository.py), retarget every SQL statement from signal_ledger/signal_outcomes to the 3-table schema (signal_events / trade_frames / trade_executions), and update all importers. This single rewrite propagates the new schema to signal_writer, lifecycle_writer, and signal_tracker without per-service SQL patching.

Purpose: Per CONTEXT D-05, all SQL lives in this repository, not scattered across services. Rewriting it once is the highest-leverage change in Phase 130. It is the prerequisite for every Wave 3 plan.
Output: New signal_events_repository.py with SignalEventsRepository; old file becomes a thin re-export shim; all 13 importers updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-script-rewriting/130-CONTEXT.md
@.planning/phases/130-script-rewriting/130-RESEARCH.md
@production/migrations/137_3table_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create signal_events_repository.py with 3-table SQL</name>
  <read_first>
    - src/persistence/repository/signal_ledger_repository.py (the full rewrite target — read all 863 lines to capture every method signature, the LedgerEntry dataclass, SignalStatus enum, the SIGNAL_OUTCOME constants, and all current SQL strings that callers depend on)
    - production/migrations/137_3table_schema.sql (authoritative DDL — exact column names, types, constraints, PKs for signal_events, trade_frames, trade_executions, and the signal_ledger_full view definition)
    - production/scripts/migrate_signal_ledger.py (established uuid5 frame_id generation and direction int-to-text encoding patterns)
    - .planning/phases/130-script-rewriting/130-RESEARCH.md (Patterns 1-7: signal_events INSERT, trade_frames INSERT, transaction wrapping, status UPDATE, frame_details UPDATE, bootstrap JOIN query)
    - .planning/phases/130-script-rewriting/130-CONTEXT.md (D-02 G0 grouping, D-08 new columns, D-11 transaction atomicity, signal_outcomes column mapping table)
  </read_first>
  <action>
    Create src/persistence/repository/signal_events_repository.py. Define class SignalEventsRepository (Vocabulary B — Repository is acceptable for Ring 0 persistence). Provide these methods, retargeted to the 3-table schema:
    - insert_signal_with_frames(signal_event: dict, trade_frames: list[dict]) -> None: atomic, async with conn.transaction(): one INSERT INTO signal_events (PK conflict target (signal_id, ts) DO NOTHING) then N INSERT INTO trade_frames (PK conflict target frame_id DO NOTHING). frame_id via uuid.uuid5(uuid.NAMESPACE_DNS, f"{signal_id}:{entry_type}"). direction converted to text "long"/"short". counterfactual_pnl_r passed as NULL. signal_schema_version passed as the int SIGNAL_SCHEMA_VERSION constant (do not str() it). concurrent_signal_count and concurrent_plugins set to NULL (RESEARCH Pitfall 5 — separate-process constraint; v2.11 populates them).
    - update_signal_status(signal_id: str, status: str) -> None: standalone UPDATE signal_events SET status=$2 WHERE signal_id=$1::uuid (idempotent, retryable).
    - update_frame_details(signal_id: str, meta: dict) -> None: UPDATE trade_frames SET frame_details = frame_details || $2::jsonb WHERE signal_id=$1::uuid — this is where activation lifecycle fields (activated_at, activation_price, zone_entry_pct, bars_to_activation) and regime_at_activation land (RESEARCH Pitfall 2).
    - record_execution(...) -> None: standalone INSERT INTO trade_executions (actual_pnl_r, actual_fill_price, exit_reason, exited_at).
    - get_active_signals_for_bootstrap() -> list[dict]: direct signal_events LEFT JOIN trade_frames query per RESEARCH Pattern 6 (NOT signal_ledger_full — the view NULLs lifecycle fields). Pull entry_zone_low/high, trailing_stop_price, chandelier_vol_source, activated_at from trade_frames.frame_details JSONB. Adapt target_price into a single-element list when building the canonical dict so evaluate_signal() compatibility holds (RESEARCH Open Question 2). Use parameterized INTERVAL via $N * INTERVAL '1 day' for the pending/active window bounds (windows passed in from caller, sourced from APR).
    Preserve the SignalStatus enum, SIGNAL_OUTCOME constants, and LedgerEntry-equivalent dataclasses callers need; split LedgerEntry into SignalEventRow + TradeFrameRow if cleaner per the new contract, but keep a LedgerEntry alias importable so callers that still reference it do not break at collection. Pass dicts directly to asyncpg for JSONB columns (factor_scores, context_features, frame_details) — no json.dumps. Use format_iso_ts for any inline timestamp serialization. Rewrite the module and class docstrings to describe the 3-table schema (D-14). Use except X as error: naming. No mechanism words in new identifiers.
  </action>
  <verify>.venv/bin/python -c "from src.persistence.repository.signal_events_repository import SignalEventsRepository; r=SignalEventsRepository; assert hasattr(r,'insert_signal_with_frames') and hasattr(r,'update_signal_status') and hasattr(r,'update_frame_details') and hasattr(r,'get_active_signals_for_bootstrap'); print('ok')"</verify>
  <acceptance_criteria>
    - File src/persistence/repository/signal_events_repository.py exists
    - Contains "class SignalEventsRepository"
    - Contains "INSERT INTO signal_events" and "INSERT INTO trade_frames"
    - Contains "async with conn.transaction()" (atomic insert path)
    - Contains "FROM signal_events" in the bootstrap query and does NOT contain "FROM signal_ledger_full" in the bootstrap method
    - Contains "uuid5" for frame_id generation
    - grep -c "INSERT INTO signal_outcomes\|UPDATE signal_outcomes\|INSERT INTO signal_ledger " src/persistence/repository/signal_events_repository.py returns 0
    - Verify command prints "ok"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Convert old repository file to re-export shim and update all importers</name>
  <read_first>
    - src/persistence/repository/signal_ledger_repository.py (current file — will become a shim)
    - src/intelligence/enums/signal_status.py, src/intelligence/enums/__init__.py, src/intelligence/schemas.py, src/intelligence/ml/confidence_calibrator.py, src/intelligence/trading/lifecycle_tracker.py, src/intelligence/weight_updater.py (the 6 src/ importers — read each import line referencing signal_ledger_repository to update the path/symbol)
  </read_first>
  <action>
    Replace the body of src/persistence/repository/signal_ledger_repository.py with a thin backward-compat shim: re-export SignalEventsRepository, SignalStatus, the SIGNAL_OUTCOME constants, and LedgerEntry alias from signal_events_repository (from src.persistence.repository.signal_events_repository import *  plus an explicit SignalLedgerRepository = SignalEventsRepository alias for any lagging caller). Then update the 6 src/ importers listed in files_modified to import directly from signal_events_repository: replace "from src.persistence.repository.signal_ledger_repository import X" with "from src.persistence.repository.signal_events_repository import X". The 4 service/script importers (signal_writer, lifecycle_writer, signal_tracker, run_historical_pipeline, feature_replay, signal_replay_auditor) are updated in their own Wave 3 plans — but the shim alias guarantees they still import successfully in the interim. Run a tree-wide grep sweep to confirm no import resolves to a now-missing symbol.
  </action>
  <verify>.venv/bin/python -c "import src.intelligence.enums.signal_status, src.intelligence.schemas, src.intelligence.ml.confidence_calibrator, src.intelligence.trading.lifecycle_tracker, src.intelligence.weight_updater; from src.persistence.repository.signal_ledger_repository import SignalEventsRepository, SignalLedgerRepository; assert SignalLedgerRepository is SignalEventsRepository; print('ok')"</verify>
  <acceptance_criteria>
    - signal_ledger_repository.py imports from signal_events_repository (contains "signal_events_repository")
    - signal_ledger_repository.py defines "SignalLedgerRepository = SignalEventsRepository" (alias for lagging callers)
    - All 6 src/ importers reference signal_events_repository (grep "signal_events_repository" in each returns a match)
    - Verify command prints "ok" (all importers collect, alias identity holds)
    - .venv/bin/pytest tests/unit/ -q exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- SignalEventsRepository exists with atomic insert, status update, frame_details update, execution record, and direct-JOIN bootstrap
- No INSERT/UPDATE targeting signal_outcomes or signal_ledger in the new repository
- Backward-compat shim keeps all 13 importers collecting
- tests/unit green
</verification>

<success_criteria>
- Single repository rewrite retargets all persistence to the 3-table schema
- frame_id deterministic via uuid5; direction text-encoded; JSONB passed as dict
- All importers resolve; no ImportError at pytest collection
</success_criteria>

<output>
After completion, create `.planning/phases/130-script-rewriting/130-02-SUMMARY.md`
</output>
