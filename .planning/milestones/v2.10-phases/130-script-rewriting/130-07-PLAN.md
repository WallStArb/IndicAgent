---
phase: 130-script-rewriting
plan: 07
type: execute
wave: 4
depends_on: [130-03, 130-04, 130-05, 130-06]
requirements: [REWRITE-01]
files_modified:
  - production/migrations/143_drop_signal_ledger.sql
  - src/api/routes/signals.py
  - src/api/routes/narrative.py
  - services/signal_probe_auditor.py
  - docs/architecture/architecture-overview.md
  - docs/architecture/architecture-dag-topology.md
  - docs/concepts/temporal-data-architecture.md
  - docs/concepts/adaptive-intelligence.md
  - docs/concepts/event-driven-fabric.md
  - CLAUDE.md
autonomous: true

must_haves:
  truths:
    - "After 48h verification, signal_outcomes and signal_ledger are dropped and signal_ledger_full is renamed to signal_ledger"
    - "No code references signal_ledger_full after the rename (all updated to signal_ledger)"
    - "Outer-ring docs and CLAUDE.md describe the 3-table architecture, not the dropped monolith"
  artifacts:
    - path: "production/migrations/143_drop_signal_ledger.sql"
      provides: "DROP + view rename DDL"
      contains: "ALTER VIEW signal_ledger_full RENAME TO signal_ledger"
  key_links:
    - from: "production/migrations/143_drop_signal_ledger.sql"
      to: "signal_ledger_full view"
      via: "rename to signal_ledger after dropping monolith"
      pattern: "RENAME TO signal_ledger"
    - from: "all code referencing signal_ledger_full"
      to: "signal_ledger (post-rename)"
      via: "reference sweep before/with the rename"
      pattern: "signal_ledger_full"
---

<objective>
Finalize the migration. After the 48-hour production verification window confirms all writers are clean on the 3-table schema, run migration 143 to DROP signal_outcomes and signal_ledger and rename signal_ledger_full to signal_ledger. Sweep all remaining signal_ledger_full code references to signal_ledger (they break after the rename). Update the stale outer-ring docs and CLAUDE.md per D-15.

Purpose: This is the terminal step. CONTEXT D-04 dictates the exact drop sequence; D-15 mandates doc updates AFTER the drop (the old table serves reads during the window). RESEARCH Pitfall 4 warns any lingering signal_ledger_full reference breaks post-rename.

Operational gate: The 48-hour window is an operational checkpoint, not a code dependency. The executor verifies clean production operation (no DLQ growth on signal_writer/lifecycle_writer, signal_events row counts advancing) before running migration 143. If the window has not elapsed, this plan stops at the verification step and resumes later — the migration is the only irreversible action.
Output: Migration 143 applied; zero signal_ledger_full references in code; 5 docs + CLAUDE.md updated to the 3-table reality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-script-rewriting/130-CONTEXT.md
@.planning/phases/130-script-rewriting/130-RESEARCH.md
@docs/foundation/documentation-system.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep signal_ledger_full references then create and apply migration 143</name>
  <read_first>
    - .planning/phases/130-script-rewriting/130-CONTEXT.md (D-04 exact drop sequence, D-03 drop order signal_outcomes first)
    - .planning/phases/130-script-rewriting/130-RESEARCH.md (Migration 143 DROP SQL example; Pitfall 4 signal_ledger_full reference after I8; the read-only services inventory)
    - production/migrations/141_trade_frames_labeled_data_index.sql (confirm latest migration; 143 is next after 142 from plan 01)
    - src/api/routes/signals.py and src/api/routes/narrative.py (the signal_ledger_full references left by plan 05 that must become signal_ledger)
    - services/signal_probe_auditor.py (FROM signal_ledger_full references left by plan 04)
  </read_first>
  <action>
    First, verify the operational gate: confirm the 48-hour window has elapsed and production is clean — check signal_writer/lifecycle_writer DLQ is not growing and signal_events row count is advancing via psql. If not satisfied, stop and record in the SUMMARY that migration 143 is pending the window; do not run the DROP.
    When the gate passes: run a tree-wide grep for "signal_ledger_full" across src/, services/, production/scripts/ and replace every occurrence with "signal_ledger" (the view's post-rename name). This includes the references plan 05 (signals.py, narrative.py) and plan 04 (signal_probe_auditor.py) intentionally left for this sweep, plus any read-only services from the RESEARCH inventory (confidence_calibration_monitor, data_quality_auditor, signal_metrics_analyzer, signal_replay_auditor, ml_discovery_analyzer, shadow_auditor, graduation_analyzer, alpha_swarm, quality_floor_bootstrap). These read-only services need only the name swap (D-07).
    Then create production/migrations/143_drop_signal_ledger.sql with the exact D-04 sequence: DROP TABLE IF EXISTS signal_outcomes; DROP TABLE IF EXISTS signal_ledger CASCADE; ALTER VIEW signal_ledger_full RENAME TO signal_ledger; Apply it: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/143_drop_signal_ledger.sql
  </action>
  <verify>PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT to_regclass('signal_ledger') IS NOT NULL AS view_exists, to_regclass('signal_outcomes') IS NULL AS outcomes_dropped, (SELECT count(*) FROM pg_views WHERE viewname='signal_ledger_full') = 0 AS full_renamed"</verify>
  <acceptance_criteria>
    - production/migrations/143_drop_signal_ledger.sql exists and contains "DROP TABLE IF EXISTS signal_outcomes", "DROP TABLE IF EXISTS signal_ledger CASCADE", "ALTER VIEW signal_ledger_full RENAME TO signal_ledger"
    - grep -rl "signal_ledger_full" src/ services/ production/scripts/ returns no files
    - Verify query returns t|t|t (view exists as signal_ledger, signal_outcomes dropped, signal_ledger_full no longer a view)
    - PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -tAc "SELECT 1 FROM signal_ledger LIMIT 1" succeeds (queries hit the join view)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update outer-ring docs and CLAUDE.md to the 3-table architecture</name>
  <read_first>
    - .planning/phases/130-script-rewriting/130-CONTEXT.md (D-15 doc table — exact stale claims and required updates for each of the 5 docs + CLAUDE.md)
    - docs/foundation/documentation-system.md (status model, inner vs outer ring doc rules, "replace not annotate" guidance)
    - docs/architecture/architecture-overview.md, docs/architecture/architecture-dag-topology.md, docs/concepts/temporal-data-architecture.md, docs/concepts/adaptive-intelligence.md, docs/concepts/event-driven-fabric.md (read the specific stale sections named in D-15)
    - CLAUDE.md (the TimescaleDB Tables section: "signal_ledger — legacy monolith (dropped Phase 129)")
  </read_first>
  <action>
    Apply the D-15 doc updates now that the drop has run:
    - docs/architecture/architecture-overview.md: change "signal_ledger ... dropped Phase 129" to Phase 130; update the table row to signal_events / trade_frames / trade_executions.
    - docs/architecture/architecture-dag-topology.md: replace the Mermaid node SIGLED[("signal_ledger...")] with signal_events + trade_frames; update the SignalWriter I/O table row.
    - docs/concepts/temporal-data-architecture.md: rewrite the "signal_ledger is the crown jewel" section to describe the 3-table architecture as the training dataset.
    - docs/concepts/adaptive-intelligence.md: update "fitness dataset — signal_ledger" references to signal_events + trade_frames.
    - docs/concepts/event-driven-fabric.md: update topic_signal_ledger -> signal_writer_service topic references to the new signal_events path.
    - CLAUDE.md: in the SLA section under TimescaleDB Tables, change "signal_ledger_full — join view (Phase 128). Renamed to signal_ledger in Phase 129..." and "signal_ledger — legacy monolith (... dropped Phase 129)" to reflect Phase 130: signal_ledger is now the join view (renamed from signal_ledger_full); the monolith and signal_outcomes are dropped. Update the SLA column-reference gotcha line that says "Query via signal_ledger_full (Phase 128) or signal_ledger (Phase 129+)" to "Query via signal_ledger (the join view)".
    Replace stale inline references rather than annotating them (documentation-system rule). Do not touch inner-ring recipe-card status fields for outer-ring docs.
  </action>
  <verify>grep -rl "dropped Phase 129\|signal_ledger_full" docs/architecture/architecture-overview.md docs/architecture/architecture-dag-topology.md docs/concepts/temporal-data-architecture.md docs/concepts/adaptive-intelligence.md docs/concepts/event-driven-fabric.md CLAUDE.md; test -z "$(grep -rl 'dropped Phase 129' docs/ CLAUDE.md)" && echo "no stale phase-129 drop claim"</verify>
  <acceptance_criteria>
    - No doc in the D-15 list contains "dropped Phase 129"
    - architecture-overview.md references signal_events / trade_frames / trade_executions
    - architecture-dag-topology.md Mermaid no longer has a signal_ledger monolith node (grep "signal_ledger\")" returns 0 for the old node label)
    - CLAUDE.md TimescaleDB Tables section describes signal_ledger as the join view, not the legacy monolith
    - grep "signal_ledger_full" CLAUDE.md returns 0 matches in the query-guidance lines
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Migration 143 applied: signal_outcomes + signal_ledger dropped, signal_ledger_full renamed to signal_ledger
- Zero signal_ledger_full references in code
- 5 docs + CLAUDE.md updated to 3-table architecture; no "dropped Phase 129" claims remain
</verification>

<success_criteria>
- The 3-table schema is the sole signal persistence surface; signal_ledger is the backward-compat join view
- Code and docs are internally consistent with the post-drop reality
- 48h operational gate respected (drop is the only irreversible action and is gated on verified clean operation)
</success_criteria>

<output>
After completion, create `.planning/phases/130-script-rewriting/130-07-SUMMARY.md`
</output>
