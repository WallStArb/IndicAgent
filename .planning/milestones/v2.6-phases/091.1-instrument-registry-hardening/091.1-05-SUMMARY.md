---
phase: 091.1-instrument-registry-hardening
plan: "05"
status: complete
wave: 3
commits:
  - "test(091.1-05): fix make_cm() factory + add instrument registry integration tests"
---

# Phase 091.1 Plan 05: Test Updates Summary

**Fix 8 complete — unit tests green, integration test file created with three tests.**

## Performance

- **Duration:** ~10 min
- **Completed:** 2026-05-20
- **Tasks:** 2 (1 commit)
- **Files modified:** 1 / Files created: 1

## Accomplishments

- **Task 1 (make_cm() factory update):** Added `on_instruments_changed=None` parameter to `make_cm()` and threaded it through to `CacheManager(...)`. Updated `test_reload_instruments_cache_calls_invalidate` to inject `invalidate_mock` via the factory rather than monkeypatching the module attribute — monkeypatch approach was broken because CacheManager holds a reference captured at construction time, not a live module lookup.
- **Task 2 (integration tests):** Created `tests/integration/test_instrument_registry.py` with three tests:
  - `test_get_active_contracts_returns_nonzero` — confirms live DB returns instruments
  - `test_all_required_symbols_present` — asserts SPY, USDJPY, EURUSD present
  - `test_trigger_installed` — asyncpg query to `information_schema.triggers` for `trg_instruments_notify`

## Verification

```
pytest tests/unit/pipeline_tests/test_cache_manager.py -q  # 20 passed, 0 failed
pytest tests/unit/ -q | tail -3                             # 7 pre-existing failures (unchanged), 3399 passed
grep -c "def test_" tests/integration/test_instrument_registry.py  # → 3
grep -c "trg_instruments_notify" tests/integration/test_instrument_registry.py  # → 2
```

## Deviations from Plan

The monkeypatch approach in `test_reload_instruments_cache_calls_invalidate` was broken by design — CacheManager no longer calls `invalidate_active_contracts_cache` directly, it calls the injected callback. Fixed by passing `invalidate_mock` directly via `make_cm(on_instruments_changed=invalidate_mock)`. The test docstring was updated to reflect the new architecture.

## Next Phase Readiness

- Phase 091.1 fully complete — all 5 plans have SUMMARYs
- No blockers for next phase

---
*Phase: 091.1-instrument-registry-hardening*
*Completed: 2026-05-20*
