---
phase: 091.1-instrument-registry-hardening
plan: "03"
subsystem: database
tags: [postgres, asyncpg, pg_notify, triggers, database_manager]

# Dependency graph
requires:
  - phase: 091-instrument-registry
    provides: instruments table and pg_notify trigger SQL in add_instruments_trigger.sql
provides:
  - DatabaseManager.ensure_instruments_trigger() idempotent trigger installation method
affects:
  - 091.1-04 (intelligence_pipeline_agent calls ensure_instruments_trigger on startup)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Idempotent DDL via CREATE OR REPLACE - safe to call on every startup without side effects"

key-files:
  created: []
  modified:
    - src/core/database_manager.py

key-decisions:
  - "Append ensure_instruments_trigger after upsert_instruments at end of public API section"
  - "Single execute_command call with combined function + trigger DDL to minimize round trips"

patterns-established:
  - "ensure_instruments_trigger: idempotent trigger DDL in DatabaseManager; call at service startup, not in migration scripts"

requirements-completed: []

# Metrics
duration: 5min
completed: 2026-05-19
---

# Phase 091.1 Plan 03: Ensure Instruments Trigger Summary

**DatabaseManager.ensure_instruments_trigger() added - idempotent pg_notify trigger DDL via CREATE OR REPLACE, eliminating need for manual add_instruments_trigger.sql application**

## Performance

- **Duration:** 5 min
- **Started:** 2026-05-19T21:15:00Z
- **Completed:** 2026-05-19T21:20:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Added `ensure_instruments_trigger()` async method to `DatabaseManager`
- Method uses `CREATE OR REPLACE` for both the PL/pgSQL function and the trigger - fully idempotent
- Single `await self.execute_command(sql)` call with combined DDL block
- Unit test suite remains green (3405 passed, 1 pre-existing unrelated failure)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add ensure_instruments_trigger to DatabaseManager** - `a31d72f4` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `src/core/database_manager.py` - Added `ensure_instruments_trigger()` method after `upsert_instruments`

## Decisions Made
- Placed method at end of file after `upsert_instruments` to group with public API methods
- Single execute_command call (not two separate calls) - keeps DDL atomic and reduces connection round trips

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `DatabaseManager.ensure_instruments_trigger()` ready for consumption by plan 091.1-04 (intelligence_pipeline_agent wiring)
- No blockers

---
*Phase: 091.1-instrument-registry-hardening*
*Completed: 2026-05-19*
