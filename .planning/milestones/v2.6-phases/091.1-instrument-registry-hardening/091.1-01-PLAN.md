---
phase: 091.1-instrument-registry-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/pipeline/cache_manager.py
autonomous: true
must_haves:
  truths:
    - "CacheManager accepts an on_instruments_changed callback parameter"
    - "CacheManager invokes the callback instead of importing from settings"
    - "self._instruments_cache attribute does not exist in CacheManager"
    - "asyncio.get_event_loop() is not called anywhere in cache_manager.py"
    - "instruments_listener_connected OTel gauge is created and updated in listener"
  artifacts:
    - path: "src/intelligence/pipeline/cache_manager.py"
      provides: "Hardened CacheManager with callback injection, no dead state, fixed asyncio API, listener health metric"
      contains: "on_instruments_changed"
  key_links:
    - from: "CacheManager.__init__"
      to: "self._on_instruments_changed"
      via: "callback parameter stored as attribute"
      pattern: "_on_instruments_changed"
    - from: "_reload_instruments_cache"
      to: "self._on_instruments_changed()"
      via: "direct invocation, no import"
      pattern: "self._on_instruments_changed()"
    - from: "_run_instruments_listener"
      to: "self._listener_connected"
      via: "gauge.add(1) on connect, gauge.add(-1) on exception"
      pattern: "_listener_connected\\.add"
---

<objective>
Apply 4 of the 8 hardening fixes to cache_manager.py: callback injection (Fix 1), dead state removal (Fix 2), asyncio.get_running_loop fix (Fix 5), and listener health metric (Fix 6).

Purpose: CacheManager must not import from settings (circular dependency violation). Dead state wastes memory and misleads readers. Deprecated asyncio API triggers Python 3.10+ warnings. Missing observability on listener health makes silent listener death undetectable.

Output: cache_manager.py with no settings import, no _instruments_cache attribute, get_running_loop(), and an instruments_listener_connected OTel gauge.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/091.1-instrument-registry-hardening/091.1-CONTEXT.md
@src/intelligence/pipeline/cache_manager.py
@src/observability/metrics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Callback injection (Fix 1) + remove dead state (Fix 2)</name>
  <files>src/intelligence/pipeline/cache_manager.py</files>
  <read_first>
    - src/intelligence/pipeline/cache_manager.py lines 120-165 (CacheManager.__init__ signature and body)
    - src/intelligence/pipeline/cache_manager.py lines 435-462 (_reload_instruments_cache full body)
    - Check imports section at top of file for any existing settings imports to remove
  </read_first>
  <action>
    1. Add `Callable` to the `from typing import TYPE_CHECKING, Any` import — change to `from typing import TYPE_CHECKING, Any, Callable`.

    2. Update `CacheManager.__init__` signature to add: `on_instruments_changed: Callable[[], None] | None = None` as a new keyword parameter after `symbols`.

    3. In `__init__` body, store it immediately after setting `self._symbol_filter`:
       `self._on_instruments_changed = on_instruments_changed or (lambda: None)`

    4. Delete the line `self._instruments_cache: list = []` (around line 164) entirely. The comment block above it (starting "# Instruments cache — populated by the LISTEN/NOTIFY handler") is also dead — delete it too.

    5. In `_reload_instruments_cache()`, replace these three lines:
       ```
       self._instruments_cache = instruments
       # Lazy import to avoid circular import at module level.
       from src.config.settings import invalidate_active_contracts_cache  # noqa: PLC0415
       invalidate_active_contracts_cache()
       ```
       with a single line: `self._on_instruments_changed()`

    6. Also delete the `# Lazy import to avoid circular import` comment and the noqa line. After the replacement, the log line `self._logger.info("cache_manager.instruments_reloaded", count=len(instruments))` remains.

    Note: The `instruments` local variable in `_reload_instruments_cache` is still used for the log count — do not remove the `instruments = [...]` assignment.
  </action>
  <verify>
    grep -n "invalidate_active_contracts_cache\|_instruments_cache\|from src.config.settings" src/intelligence/pipeline/cache_manager.py
    # Must return zero results
    grep -n "on_instruments_changed\|_on_instruments_changed" src/intelligence/pipeline/cache_manager.py
    # Must show __init__ param and self._on_instruments_changed() call
  </verify>
  <done>
    - `from src.config.settings` not present anywhere in cache_manager.py
    - `_instruments_cache` not present anywhere in cache_manager.py
    - `invalidate_active_contracts_cache` not present anywhere in cache_manager.py
    - `on_instruments_changed` appears in `__init__` signature
    - `self._on_instruments_changed()` called in `_reload_instruments_cache`
  </done>
</task>

<task type="auto">
  <name>Task 2: asyncio.get_running_loop fix (Fix 5) + listener health metric (Fix 6)</name>
  <files>src/intelligence/pipeline/cache_manager.py</files>
  <read_first>
    - src/intelligence/pipeline/cache_manager.py lines 424-434 (_on_instrument_notify body)
    - src/intelligence/pipeline/cache_manager.py lines 395-425 (_run_instruments_listener body — find the LISTEN execute call and the exception handler)
    - src/observability/metrics.py lines 26-28 (gauge function signature — it is `gauge`, not `up_down_counter`)
  </read_first>
  <action>
    1. In `_on_instrument_notify()`, change:
       `asyncio.get_event_loop().call_soon_threadsafe(...)`
       to:
       `asyncio.get_running_loop().call_soon_threadsafe(...)`

    2. In `CacheManager.__init__`, add the listener health gauge after `self._on_instruments_changed = ...`:
       ```python
       from src.observability.metrics import gauge  # noqa: PLC0415
       self._listener_connected = gauge(
           "instruments_listener_connected",
           "1 when LISTEN instruments connection is active, 0 when reconnecting",
       )
       ```
       Note: `gauge` is the correct function name in metrics.py (it calls `_meter.create_up_down_counter` internally). The CONTEXT.md mistakenly says `up_down_counter` — use `gauge`.

    3. In `_run_instruments_listener()`, locate the line `await conn.execute("LISTEN instruments")`. On the line immediately after it add:
       `self._listener_connected.add(1)`

    4. In the same method's `except Exception as exc:` handler, locate the `await asyncio.sleep(5)` line. On the line immediately before `await asyncio.sleep(5)` add:
       `self._listener_connected.add(-1)`
  </action>
  <verify>
    grep -n "get_event_loop\|get_running_loop" src/intelligence/pipeline/cache_manager.py
    # Must show get_running_loop, not get_event_loop
    grep -n "_listener_connected" src/intelligence/pipeline/cache_manager.py
    # Must appear in __init__, after LISTEN execute, and before asyncio.sleep(5)
    .venv/bin/python -c "from src.intelligence.pipeline.cache_manager import CacheManager; print('import ok')"
  </verify>
  <done>
    - `asyncio.get_event_loop()` does not appear in cache_manager.py
    - `asyncio.get_running_loop()` used in `_on_instrument_notify`
    - `self._listener_connected` initialized in `__init__` via `gauge()`
    - `self._listener_connected.add(1)` after LISTEN conn.execute
    - `self._listener_connected.add(-1)` before asyncio.sleep(5) in exception handler
    - `python -c "from src.intelligence.pipeline.cache_manager import CacheManager"` succeeds
  </done>
</task>

</tasks>

<verification>
.venv/bin/python -c "from src.intelligence.pipeline.cache_manager import CacheManager; print('import ok')"
grep -c "get_event_loop" src/intelligence/pipeline/cache_manager.py  # must be 0
grep -c "_instruments_cache" src/intelligence/pipeline/cache_manager.py  # must be 0
grep -c "from src.config.settings" src/intelligence/pipeline/cache_manager.py  # must be 0
.venv/bin/pytest tests/unit/pipeline_tests/test_cache_manager.py -q  # must pass (will fail on constructor — plan 05 fixes that)
</verification>

<success_criteria>
- cache_manager.py imports no settings module
- No _instruments_cache attribute exists
- get_running_loop() used in _on_instrument_notify
- instruments_listener_connected gauge initialized and updated in listener loop
- Module imports without error
</success_criteria>

<output>
After completion, create `.planning/phases/091.1-instrument-registry-hardening/091.1-01-SUMMARY.md`
</output>
