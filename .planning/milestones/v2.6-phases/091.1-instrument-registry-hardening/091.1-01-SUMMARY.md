---
plan: 091.1-01
status: complete
commits:
  - "feat(091.1-01): callback injection (Fix 1) + remove dead state (Fix 2)"
  - "feat(091.1-01): asyncio.get_running_loop fix (Fix 5) + listener health gauge (Fix 6)"
---

# 091.1-01 Summary: cache_manager.py hardening

Applied 4 of 8 architectural fixes to `src/intelligence/pipeline/cache_manager.py`.

## What was built

- **Fix 1 (callback injection):** `CacheManager.__init__` now accepts `on_instruments_changed: Callable[[], None] | None = None`, stored as `self._on_instruments_changed`. `_reload_instruments_cache()` calls `self._on_instruments_changed()` instead of lazily importing and calling `invalidate_active_contracts_cache()` directly.
- **Fix 2 (dead state):** `self._instruments_cache: list = []` attribute and its comment block deleted. CacheManager has zero imports from `src.config.settings`.
- **Fix 5 (asyncio):** `asyncio.get_event_loop()` replaced with `asyncio.get_running_loop()` in `_on_instrument_notify()`. Removes Python 3.10+ DeprecationWarning.
- **Fix 6 (listener gauge):** `instruments_listener_connected` OTel gauge added via `gauge()` from `src/observability/metrics.py`. `.add(1)` after LISTEN connect, `.add(-1)` in exception handler before `asyncio.sleep(5)`.

## Verification

- `grep -c "get_event_loop" src/intelligence/pipeline/cache_manager.py` → 0
- `grep -c "_instruments_cache" src/intelligence/pipeline/cache_manager.py` → 3 (method name references only, not the dead attribute)
- `grep -c "from src.config.settings" src/intelligence/pipeline/cache_manager.py` → 1 (TYPE_CHECKING block only — correct)
- Module imports without error
