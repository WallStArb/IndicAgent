---
phase: 091-instrument-registry
plan: 05
type: execute
wave: 4
depends_on: ["091-04"]
files_modified:
  - tests/unit/test_settings.py
  - tests/unit/config/test_settings_equity.py
  - tests/unit/providers/test_ibkr_adapter.py
  - tests/unit/service_tests/test_feature_writer_agent.py
  - tests/unit/api/test_api_utils.py
autonomous: true
requirements: [INST-05]

must_haves:
  truths:
    - "tests/unit/ exits 0 under pytest after settings.contracts is removed"
    - "No test in the suite constructs Settings(contracts=[...]) or asserts on s.contracts / s.instruments"
    - "Tests that previously validated the deleted build_contracts() logic are deleted (not weakened)"
    - "Tests that exercised the runtime contract list now mock get_active_contracts() to return a fake list[Instrument]"
  artifacts:
    - path: tests/unit/test_settings.py
      provides: "Settings tests purged of s.contracts assertions"
      contains: "get_active_contracts"
    - path: tests/unit/config/test_settings_equity.py
      provides: "Equity coverage tests via get_active_contracts mock or DB-state tests"
      contains: "get_active_contracts"
  key_links:
    - from: tests/unit/test_settings.py
      to: get_active_contracts
      via: "monkeypatch or unittest.mock.patch"
      pattern: "get_active_contracts"
---

<objective>
Fix the 16+ unit tests that break when `settings.contracts` is removed in Plan 091-04. The fix pattern: tests that asserted on the hardcoded list now either (a) mock `get_active_contracts()` to return a fake list, or (b) get deleted if they only validated the now-deleted `build_contracts()` logic.

Purpose: Realize INST-05 - all callers (including tests) work after the registry flip.
Output: Green `pytest tests/unit/` suite.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/091-instrument-registry/091-RESEARCH.md
@tests/unit/test_settings.py
@tests/unit/config/test_settings_equity.py
@tests/unit/providers/test_ibkr_adapter.py
@tests/unit/service_tests/test_feature_writer_agent.py
@tests/unit/api/test_api_utils.py
</context>

<threat_model>
- Deleting too many tests reduces coverage: distinguish between "tested the now-deleted validator" (delete) vs "tested runtime behavior" (port to mock). Err on the side of porting when in doubt.
- Mock drift: mocking `get_active_contracts()` everywhere means tests don't catch real DB-mock divergence. Acceptable for unit tests; integration tests cover real DB.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Purge tests/unit/test_settings.py of s.contracts assertions</name>
  <files>tests/unit/test_settings.py</files>
  <read_first>
    - tests/unit/test_settings.py (entire file - identify all tests in TestBuildContractsBaseSymbolTemplates and any other s.contracts users)
    - .planning/phases/091-instrument-registry/091-RESEARCH.md "Group A" section
  </read_first>
  <action>
    For each test that constructs `Settings()` and asserts on `s.contracts` (research identifies 7 tests in `TestBuildContractsBaseSymbolTemplates`):
    - If the test validated the deleted `build_contracts()` validator behavior (e.g., "test_contracts_built_from_base_symbol_template", "test_default_contracts_populated"): DELETE the test method entirely.
    - If the test validated runtime contract shape: rewrite to use `unittest.mock.patch("src.config.settings.get_active_contracts")` and assert on the patched return value's properties.
    Add one new test: `def test_settings_has_no_contracts_attribute(): s = Settings(); assert not hasattr(s, "contracts")` to lock in the removal.
    Run the file in isolation to confirm green.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/test_settings.py -v` exits 0. `grep -c "s\.contracts" tests/unit/test_settings.py` returns 0. `grep -c "test_settings_has_no_contracts_attribute" tests/unit/test_settings.py` returns 1.
  </verify>
  <acceptance_criteria>
    - All `s.contracts` and `s.instruments` references removed from this file.
    - New negative test `test_settings_has_no_contracts_attribute` present and passing.
    - File exits 0 under pytest.
  </acceptance_criteria>
  <done>test_settings.py reflects the post-removal Settings class.</done>
</task>

<task type="auto">
  <name>Task 2: Update tests/unit/config/test_settings_equity.py to mock get_active_contracts</name>
  <files>tests/unit/config/test_settings_equity.py</files>
  <read_first>
    - tests/unit/config/test_settings_equity.py (entire file - identify all 15 tests in TestPilotETFs and TestFullETFRollout)
    - .planning/phases/091-instrument-registry/091-RESEARCH.md "Group A" section
  </read_first>
  <action>
    For each of the 15 tests asserting on `s.instruments` or `s.contracts`:
    - Tests that assert pure equity-set membership/count: rewrite to mock `get_active_contracts()` with a `list[Instrument]` containing the expected equity symbols, then assert on the mocked return.
    - The `test_total_instrument_count_60` test (and similar count assertions): convert to a DB-state test that queries `instruments` via a fixture (if such a fixture exists) OR mock `get_active_contracts()` to return a list of the expected length. Pick the lower-friction option.
    - Helper functions (e.g., `_get_etf_symbols(s)`) that internally read `s.instruments`: refactor to call `get_active_contracts()` so the mock propagates.
    Run the file in isolation to confirm green.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/config/test_settings_equity.py -v` exits 0. `grep -c "s\.instruments\|s\.contracts" tests/unit/config/test_settings_equity.py` returns 0.
  </verify>
  <acceptance_criteria>
    - All 15 tests pass.
    - Zero `s.contracts` or `s.instruments` references remain.
    - Tests use `get_active_contracts` mock or DB-state pattern.
  </acceptance_criteria>
  <done>Equity coverage tests pass under the DB-backed registry.</done>
</task>

<task type="auto">
  <name>Task 3: Spot-fix three remaining test files (ibkr_adapter, feature_writer, api_utils)</name>
  <files>tests/unit/providers/test_ibkr_adapter.py tests/unit/service_tests/test_feature_writer_agent.py tests/unit/api/test_api_utils.py</files>
  <read_first>
    - tests/unit/providers/test_ibkr_adapter.py (around line 277)
    - tests/unit/service_tests/test_feature_writer_agent.py (around line 446)
    - tests/unit/api/test_api_utils.py (around line 42)
    - .planning/phases/091-instrument-registry/091-RESEARCH.md "Group C" and "Tests That Are Fine" sections
  </read_first>
  <action>
    Three discrete edits, one per file:
    1. `tests/unit/providers/test_ibkr_adapter.py:~277`: Replace `settings.contracts` access with `patch("src.providers.ibkr.get_active_contracts")` (or whichever module imports it) returning a list of fake Instruments matching what the test expects.
    2. `tests/unit/service_tests/test_feature_writer_agent.py:~446`: Replace `s.contracts = instruments` mutation with `monkeypatch.setattr("src.config.settings.get_active_contracts", lambda settings=None: instruments)` or equivalent. If feature_writer doesn't actually import get_active_contracts, patch wherever feature_writer reads its contract list from.
    3. `tests/unit/api/test_api_utils.py:~42`: Replace `mock_settings.contracts = [...]` with patching `get_active_contracts` to return the same list. Since `resolve_contract()` is being updated in Plan 091-03 to call `get_active_contracts`, the patch path is `src.api.utils.get_active_contracts` if imported into utils, otherwise `src.config.settings.get_active_contracts`.
    After each file edit, run that file in isolation to confirm green before moving to the next.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/providers/test_ibkr_adapter.py tests/unit/service_tests/test_feature_writer_agent.py tests/unit/api/test_api_utils.py -v` exits 0. `grep -rn "settings.contracts\|mock_settings.contracts\|s\.contracts" tests/unit/providers/test_ibkr_adapter.py tests/unit/service_tests/test_feature_writer_agent.py tests/unit/api/test_api_utils.py` returns 0 matches.
  </verify>
  <acceptance_criteria>
    - All three files pass under pytest.
    - Zero `settings.contracts`, `mock_settings.contracts`, or `s.contracts` references remain in these three files.
  </acceptance_criteria>
  <done>Last three spot-fixes complete; entire unit suite should now pass.</done>
</task>

<task type="auto">
  <name>Task 4: Full unit suite green-check</name>
  <files>tests/unit/</files>
  <read_first>
    - .planning/phases/091-instrument-registry/091-RESEARCH.md section 5 "Test Impact" (the full list of impacted files)
  </read_first>
  <action>
    Run the entire unit test suite: `.venv/bin/pytest tests/unit/ -q`. Fix any additional breakages not caught by research (the research grepped for known patterns but a fresh failure can surface anything). For each failure, apply the same pattern: if it asserts on `s.contracts` or `s.instruments`, mock `get_active_contracts()` instead. Do NOT skip or xfail tests - fix them. If a test failure is unrelated to this phase (pre-existing flaky test), document it in the SUMMARY but do not silence it.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/ -q` exits 0. Final line shows `N passed` with zero failures and zero errors (skipped tests are acceptable if they were already skipped pre-phase).
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/ -q` exits with code 0.
    - Zero new test failures introduced by Phase 091.
    - Any incidental fixes are documented inline (commit message or comment).
  </acceptance_criteria>
  <done>Full unit suite green - INST-05 satisfied.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/ -q` exits 0
- `grep -rn "settings\.contracts\|s\.contracts\|mock_settings\.contracts\|s\.instruments" tests/unit/` returns 0 matches
</verification>

<success_criteria>
Full unit suite passes with the registry-driven Settings class. No legacy references to `s.contracts` remain in tests.
</success_criteria>

<output>
After completion, create `.planning/phases/091-instrument-registry/091-05-SUMMARY.md`.
</output>
