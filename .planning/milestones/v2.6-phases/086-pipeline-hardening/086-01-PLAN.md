---
phase: 086-pipeline-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/observability/circuit_breaker.py
  - services/intelligence_pipeline_agent.py
autonomous: true
requirements: [PIPE-01, PIPE-03, PIPE-04]
must_haves:
  truths:
    - "A plugin that raises 3 consecutive times opens its circuit breaker and is skipped on subsequent bars"
    - "Reopening (HALF_OPEN) is governed by CircuitBreaker.timeout_sec=300"
    - "Checkpoint write failure surfaces as an exception in logs, not as a swallowed warning"
    - "When the output queue is full, the signal publish path blocks rather than dropping the bar"
    - "Full-queue events are visible via the existing intelligence_pipeline_output_buffer_drops_total counter"
  artifacts:
    - path: "services/intelligence_pipeline_agent.py"
      provides: "Per-plugin circuit breaker wiring + blocking enqueue helper + raising checkpoint"
      contains: "_plugin_circuit_breakers"
  key_links:
    - from: "_run_i1 / _run_tier / _run_i7_inner"
      to: "cb.allow_request()"
      via: "skip guard before tasks.append — allow_request() handles OPEN→HALF_OPEN time check"
      pattern: "if not cb.allow_request\\(\\): continue"
    - from: "_collect_plugin_results exception branch"
      to: "cb.record_failure() + CIRCUIT_BREAKER_STATE.set(1)"
      via: "in-line after PLUGIN_ERRORS_TOTAL.add"
      pattern: "cb.record_failure\\(\\)"
    - from: "_collect_plugin_results success branch"
      to: "cb.record_success() + CIRCUIT_BREAKER_STATE.set(0) on recovery"
      via: "if cb exists and cb.state != OPEN"
      pattern: "cb.record_success\\(\\)"
    - from: "_publish_signals_or_dlq + winner enqueue in _run_i7_inner"
      to: "_enqueue_blocking"
      via: "await replacing enqueue paths; _publish_signals_or_dlq made async"
      pattern: "await self\\._enqueue_blocking"
---

<objective>
Harden three failure modes inside IntelligencePipelineComputeAgent: per-plugin circuit breaker (PIPE-01), raising checkpoint write (PIPE-03), and blocking enqueue for signal output (PIPE-04).

Purpose: A misbehaving plugin must not consume the bar repeatedly; a checkpoint failure must not be silently swallowed; a full output queue must back-pressure rather than drop signals.
Output: Updated `services/intelligence_pipeline_agent.py` with circuit breaker dict, `_enqueue_blocking`, and a raising `_write_local_checkpoint`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/086-pipeline-hardening/086-RESEARCH.md
@.planning/phases/086-pipeline-hardening/086-PATTERNS.md
@src/observability/circuit_breaker.py
@src/observability/metrics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CircuitBreaker API + wire per-plugin breakers into pipeline (PIPE-01)</name>
  <files>src/observability/circuit_breaker.py, services/intelligence_pipeline_agent.py</files>
  <read_first>
    - src/observability/circuit_breaker.py (full file — confirmed: has record_failure(), state property, call(); does NOT have allow_request() or record_success())
    - services/intelligence_pipeline_agent.py (current __init__, _run_i1, _run_tier, _run_i7_inner, _collect_plugin_results)
    - src/observability/metrics.py (CIRCUIT_BREAKER_STATE instrument definition, PLUGIN_ERRORS_TOTAL)
  </read_first>
  <action>
    Step A — Extend `src/observability/circuit_breaker.py` with two new methods on `CircuitBreaker`:

    1. Add `allow_request(self) -> bool` — handles OPEN→HALF_OPEN time-based transition without needing `call()`:
       ```python
       def allow_request(self) -> bool:
           """Return True if a request should be allowed. Transitions OPEN→HALF_OPEN on timeout."""
           if self._state in (CircuitState.CLOSED, CircuitState.HALF_OPEN):
               return True
           if self._state == CircuitState.OPEN:
               if time.time() - self._last_failure_time >= self.timeout_sec:
                   self._state = CircuitState.HALF_OPEN
                   return True
           return False
       ```

    2. Add `record_success(self) -> None` — resets failure count and closes circuit from HALF_OPEN:
       ```python
       def record_success(self) -> None:
           """Record a successful execution; resets failure count and closes from HALF_OPEN."""
           self._failures = 0
           if self._state == CircuitState.HALF_OPEN:
               self._state = CircuitState.CLOSED
       ```

    Step B — Wire into `IntelligencePipelineComputeAgent` in `services/intelligence_pipeline_agent.py`:

    1. Import at top: `from src.observability.circuit_breaker import CircuitBreaker, CircuitState` and ensure `CIRCUIT_BREAKER_STATE` is imported from `src.observability.metrics`.
    2. In __init__ (near `_output_queue` setup around line 476), add: `self._plugin_circuit_breakers: dict[str, CircuitBreaker] = {}`.
    3. Add helper method `_get_plugin_cb(self, plugin_name: str) -> CircuitBreaker` — lazily constructs `CircuitBreaker(failure_threshold=3, timeout_sec=300)` and caches in the dict.
    4. In each plugin-dispatch site — `_run_i1`, `_run_tier`, and `_run_i7_inner` — before `tasks.append(...)`, call `cb = self._get_plugin_cb(plugin_name)` and `if not cb.allow_request(): continue`. Using `allow_request()` (not a raw `state == OPEN` check) ensures the OPEN→HALF_OPEN timeout transition fires and allows recovery probes.
    5. In `_collect_plugin_results` exception branch (after `PLUGIN_ERRORS_TOTAL.add(1, ...)`):
       - `cb = self._get_plugin_cb(task.plugin_name); cb.record_failure()`
       - If `cb.state == CircuitState.OPEN`: `CIRCUIT_BREAKER_STATE.set(1, {"plugin_name": task.plugin_name})` and `self.logger.warning("plugin.circuit_breaker_opened", plugin=task.plugin_name)`
    6. In `_collect_plugin_results` success branch (where tuple result is merged):
       - `cb = self._get_plugin_cb(task.plugin_name); prev_state = cb.state; cb.record_success()`
       - If `prev_state != CircuitState.CLOSED` (i.e., recovered from HALF_OPEN): `CIRCUIT_BREAKER_STATE.set(0, {"plugin_name": task.plugin_name})` and `self.logger.info("plugin.circuit_breaker_closed", plugin=task.plugin_name)`

    Do NOT use `src/core/plugin_circuit_breaker.py`. Use `src/observability/circuit_breaker.py` exclusively.
    Use structlog keyword args; never `event=`.
  </action>
  <verify>
    grep -n "def allow_request\|def record_success" src/observability/circuit_breaker.py
    grep -n "_plugin_circuit_breakers" services/intelligence_pipeline_agent.py
    grep -n "allow_request\(\)" services/intelligence_pipeline_agent.py
    grep -n "record_success\(\)\|circuit_breaker_closed" services/intelligence_pipeline_agent.py
    grep -n "circuit_breaker_opened" services/intelligence_pipeline_agent.py
    .venv/bin/ruff check src/observability/circuit_breaker.py services/intelligence_pipeline_agent.py
    .venv/bin/pytest tests/unit/ -k "pipeline or circuit" -q
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/observability/circuit_breaker.py` defines `allow_request(self) -> bool` with OPEN→HALF_OPEN time-check logic
    - Source assertion: `src/observability/circuit_breaker.py` defines `record_success(self) -> None` that resets `_failures = 0` and transitions HALF_OPEN→CLOSED
    - Source assertion: `services/intelligence_pipeline_agent.py` contains `self._plugin_circuit_breakers: dict[str, CircuitBreaker]` in __init__
    - Source assertion: `_get_plugin_cb` method exists with `failure_threshold=3, timeout_sec=300`
    - Source assertion: each of `_run_i1`, `_run_tier`, `_run_i7_inner` contains `if not cb.allow_request(): continue` before `tasks.append`
    - Source assertion: `_collect_plugin_results` exception branch contains `cb.record_failure()` AND conditional `CIRCUIT_BREAKER_STATE.set(1, ...)`
    - Source assertion: `_collect_plugin_results` success branch contains `cb.record_success()` AND conditional `CIRCUIT_BREAKER_STATE.set(0, ...)` on recovery
    - CLI: `.venv/bin/ruff check src/observability/circuit_breaker.py services/intelligence_pipeline_agent.py` exits 0
    - Source assertion: no import of `src.core.plugin_circuit_breaker` is added
  </acceptance_criteria>
  <done>
    CircuitBreaker extended with allow_request()+record_success(); pipeline wired with skip guard that recovers; both OPEN and CLOSED transitions emit metrics; ruff clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make _write_local_checkpoint raise on failure (PIPE-03)</name>
  <files>services/intelligence_pipeline_agent.py</files>
  <read_first>
    - services/intelligence_pipeline_agent.py (current `_write_local_checkpoint` at lines 1526-1537 and its caller `_teardown` at line 701)
  </read_first>
  <action>
    Modify `_write_local_checkpoint(self) -> None` in `services/intelligence_pipeline_agent.py`:

    1. Remove the `try:` block and the `except Exception as exc: self.logger.warning("state.checkpoint_write_failed", error=str(exc))` swallow.
    2. Update the docstring from "Best-effort — never raises." to "Raises on failure (OSError, etc.). Caller logs via agent.run_failed."
    3. Keep the body unchanged: build `payload`, write to `_CHECKPOINT_PATH.with_suffix('.tmp')`, atomic rename, log `state.checkpoint_written`.
    4. Do NOT wrap the call in `_teardown()` (line 701) with try/except — let the exception propagate so it surfaces as an ERROR via the standard agent shutdown error path.

    Use structlog keyword args; never `event=`. Timestamps remain `datetime.now(UTC)`.
  </action>
  <verify>
    grep -n "Best-effort — never raises" services/intelligence_pipeline_agent.py     # must return nothing
    grep -n "state.checkpoint_write_failed" services/intelligence_pipeline_agent.py  # must return nothing
    grep -n "Raises on failure" services/intelligence_pipeline_agent.py
    .venv/bin/ruff check services/intelligence_pipeline_agent.py
  </verify>
  <acceptance_criteria>
    - Source assertion: `services/intelligence_pipeline_agent.py` no longer contains the string `state.checkpoint_write_failed`
    - Source assertion: `services/intelligence_pipeline_agent.py` no longer contains the string `Best-effort — never raises`
    - Source assertion: `_write_local_checkpoint` body does NOT contain `try:` or `except` (verify with focused grep on the function)
    - Source assertion: `_teardown` does NOT wrap the call to `_write_local_checkpoint()` in try/except
    - CLI: `.venv/bin/ruff check services/intelligence_pipeline_agent.py` exits 0
  </acceptance_criteria>
  <done>
    `_write_local_checkpoint` raises naturally; docstring updated; ruff clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add _enqueue_blocking and convert signal-path enqueues (PIPE-04)</name>
  <files>services/intelligence_pipeline_agent.py</files>
  <read_first>
    - services/intelligence_pipeline_agent.py (current `_enqueue` at lines 1017-1022, `_publish_signals_or_dlq` around line 1581, winner enqueue inside `_run_i7_inner` around line 1505, `_enqueue_intel_journal` at line 1647, IntelligenceEvent enqueue site at line 886)
  </read_first>
  <action>
    Modify `services/intelligence_pipeline_agent.py`:

    1. Add new async method `_enqueue_blocking(self, topic: str, key: str, value: Any) -> None` alongside existing `_enqueue`. Body:
       - If `self._output_queue.full()`: `self._output_buffer_drops.add(1)` and `self.logger.warning("output_queue.full_blocking", qsize=self._output_queue.qsize())`.
       - Then `await self._output_queue.put((topic, key, value))` (this blocks until space available).

    2. Convert `_publish_signals_or_dlq` from `def` to `async def` (verified sync at line 1564). Update its single call site in `_run_i7_inner` (line 1489: `published = self._publish_signals_or_dlq(...)`) to `published = await self._publish_signals_or_dlq(...)`. Inside the method, replace:
       - The `intelligence.i7.signals` enqueue (line 1630 area) — the hot success path — with `await self._enqueue_blocking(...)`.
       - The CIS-failure DLQ enqueue (line 1581 area) — the error path — also with `await self._enqueue_blocking(...)` for consistent backpressure.

    3. Convert the winner enqueue in `_run_i7_inner` (line 1505: `self._enqueue(topic_signals_aggregated(...), ...)`) to `await self._enqueue_blocking(...)`.

    4. Leave `self._enqueue(...)` non-blocking for:
       - `_enqueue_intel_journal` — journal path stays non-blocking.
       - IntelligenceEvent enqueue (line 886) — non-signal path stays non-blocking.

    Use the existing `self._output_buffer_drops` counter — do not introduce a new metric.
    Structlog keyword args; never `event=`.
  </action>
  <verify>
    grep -n "_enqueue_blocking" services/intelligence_pipeline_agent.py
    grep -n "output_queue.full_blocking" services/intelligence_pipeline_agent.py
    grep -n "await self._enqueue_blocking" services/intelligence_pipeline_agent.py
    .venv/bin/ruff check services/intelligence_pipeline_agent.py
    .venv/bin/pytest tests/unit/ -k "pipeline or enqueue" -q
  </verify>
  <acceptance_criteria>
    - Source assertion: `services/intelligence_pipeline_agent.py` defines `async def _enqueue_blocking(self, topic: str, key: str, value: Any) -> None`
    - Source assertion: body contains `await self._output_queue.put((topic, key, value))` (not `put_nowait`)
    - Source assertion: body contains `self.logger.warning("output_queue.full_blocking"` with `qsize` kwarg
    - Source assertion: `_publish_signals_or_dlq` is declared `async def` (not `def`)
    - Source assertion: `_run_i7_inner` call site uses `await self._publish_signals_or_dlq(...)`
    - Source assertion: `_publish_signals_or_dlq` contains `await self._enqueue_blocking(` for both the success path (i7.signals) and the CIS-failure DLQ path
    - Source assertion: `_run_i7_inner` winner enqueue (topic_signals_aggregated) uses `await self._enqueue_blocking(`
    - Source assertion: `_enqueue_intel_journal` and the IntelligenceEvent enqueue site continue to use the synchronous `self._enqueue(`
    - Source assertion: no new metric instrument is added (`intelligence_pipeline_output_buffer_drops_total` is the sole full-queue counter)
    - CLI: `.venv/bin/ruff check services/intelligence_pipeline_agent.py` exits 0
  </acceptance_criteria>
  <done>
    Blocking enqueue helper exists; signal-path call sites converted; journal/event paths remain non-blocking; ruff clean.
  </done>
</task>

</tasks>

<verification>
- All three changes coexist in one file without ruff or black violations
- Unit tests under `tests/unit/` that touch the pipeline agent still pass
- No new dependency on `src/core/plugin_circuit_breaker.py`
- No introduction of `prometheus_client` imports
- Structlog calls use keyword args (`plugin=`, `qsize=`, `error=`) — never `event=`
</verification>

<success_criteria>
- PIPE-01: `CircuitBreaker` extended with `allow_request()` + `record_success()`; pipeline wired with skip guard (`allow_request()`) + failure/success branches; OPEN and recovery transitions emit `CIRCUIT_BREAKER_STATE` gauge
- PIPE-03: `_write_local_checkpoint` raises naturally; no try/except swallow
- PIPE-04: `_enqueue_blocking` exists; `_publish_signals_or_dlq` is async; all signal/winner enqueue paths use `await _enqueue_blocking`
- `.venv/bin/ruff check src/observability/circuit_breaker.py services/intelligence_pipeline_agent.py` exits 0
- `.venv/bin/pytest tests/unit/ -q` exits 0
</success_criteria>

<output>
Create `.planning/phases/086-pipeline-hardening/086-01-SUMMARY.md` documenting the wiring decisions, any deviations, and a recommendation on whether to add a unit test fixture that simulates 3 consecutive plugin failures to lock in PIPE-01 behavior.
</output>
