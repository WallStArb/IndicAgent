---
phase: 086-pipeline-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/routes/health.py
autonomous: true
requirements: [OBS-02]
must_haves:
  truths:
    - "GET /health/system returns machine-readable JSON with consumer_lag, dlq_depth, signal_replay_unresolved, and agent_heartbeats fields"
    - "The endpoint never returns 503 on Prometheus timeout; it returns degraded (null/empty) values for any missing metric"
    - "Prometheus query timeout is bounded at 3 seconds total"
  artifacts:
    - path: "src/api/routes/health.py"
      provides: "GET /system route appended to existing health router"
      contains: "/system"
  key_links:
    - from: "GET /api/health/system route handler"
      to: "aiohttp.ClientSession -> http://localhost:9090/api/v1/query"
      via: "four sequential Prometheus queries with try/except wrappers"
      pattern: "aiohttp.ClientSession"
    - from: "response JSON schema"
      to: "consumer_lag / dlq_depth / signal_replay_unresolved / agent_heartbeats keys"
      via: "fixed top-level dict shape"
      pattern: "agent_heartbeats"
---

<objective>
Add a machine-readable system health endpoint at `GET /api/health/system` that aggregates Prometheus metrics (consumer lag by group, DLQ depth, `signal_replay_unresolved`, agent heartbeats) into a single JSON response (OBS-02).

Purpose: External monitoring and the project dashboard need one authoritative endpoint to read system health without scraping Prometheus directly.
Output: New `/system` route appended to `src/api/routes/health.py`, automatically served under `/api/health/system` by the existing router registration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/086-pipeline-hardening/086-RESEARCH.md
@.planning/phases/086-pipeline-hardening/086-PATTERNS.md
@src/api/main.py
@services/service_auditor_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /system Prometheus-aggregation route to health router</name>
  <files>src/api/routes/health.py</files>
  <read_first>
    - src/api/routes/health.py (full file — existing `/`, `/database`, `/full` routes and router declaration at top)
    - src/api/main.py (confirm `app.include_router(health.router, prefix="/api/health", ...)` registration is present)
    - services/service_auditor_agent.py (Prometheus query pattern at lines 598-608 — copy aiohttp usage)
    - src/observability/metrics.py (confirm metric names: `persistence_consumer_lag_records` or `persistence_consumer_lag`, `agent_dlq_total`, `signal_replay_unresolved_gauge`, `agent_last_message_timestamp_seconds`)
  </read_first>
  <action>
    Modify `src/api/routes/health.py`:

    1. Add necessary imports at top (only if not already present): `import aiohttp`, `from datetime import UTC, datetime`.
    2. Append a new route handler to the existing `router` (do NOT create a new router):
       ```
       @router.get("/system")
       async def system_health() -> dict:
           """Machine-readable system health: lag, DLQ depth, replay gauge, agent heartbeats."""
       ```
    3. Inside the handler:
       - Define `prom_url = "http://localhost:9090/api/v1/query"` and `timeout = aiohttp.ClientTimeout(total=3)`.
       - Initialize result dict:
         - `timestamp`: `datetime.now(UTC).isoformat()`
         - `consumer_lag`: `{}` (dict keyed by `agent_id`)
         - `dlq_depth`: `None`
         - `signal_replay_unresolved`: `None`
         - `agent_heartbeats`: `{}` (dict keyed by agent label, value ISO8601 timestamp)
       - Open `aiohttp.ClientSession(timeout=timeout)` as `session`. Inside the session, run four independent queries, each wrapped in its own `try/except Exception: pass` so a single failed query degrades that field only:
         - Query `persistence_consumer_lag_records` (confirm exact metric name when reading `src/observability/metrics.py` — if the canonical name is `persistence_consumer_lag` instead, use that). For each result, extract `agent_id = item["metric"].get("agent_id", "unknown")` and populate `result["consumer_lag"][agent_id] = int(float(item["value"][1]))`.
         - Query `agent_dlq_total`. Sum all `float(i["value"][1])` across results and set `result["dlq_depth"] = int(total)`.
         - Query `signal_replay_unresolved_gauge`. If results exist, set `result["signal_replay_unresolved"] = int(float(items[0]["value"][1]))`.
         - Query `agent_last_message_timestamp_seconds`. For each result, extract the agent label using the SAME key BaseAgent emits in `_last_msg_ts_attrs` (verify by reading `src/core/agent/base.py`). Convert via `datetime.fromtimestamp(ts, tz=UTC).isoformat()` and store under `result["agent_heartbeats"][agent_key]`.
       - Return `result`.
    4. Do NOT change router prefix or registration in `src/api/main.py`. The health router is mounted at `prefix="/health"` (confirmed: `app.include_router(health.router, prefix="/health", ...)` at main.py:131). The endpoint is therefore reachable at `/health/system`, NOT `/api/health/system`.
    5. Use Pydantic response model only if the existing routes use one; otherwise return a plain dict for FastAPI auto-serialization. Match the style of the surrounding routes.

    Structlog is not typically used in route handlers; if the file already uses it for other routes, follow the same convention. Otherwise, no logging inside the handler (silent degradation is preferred per Pitfall 5).

    Timestamps must be UTC — `datetime.now(UTC)`, never `datetime.utcnow()`.
  </action>
  <verify>
    grep -n '@router.get("/system")' src/api/routes/health.py
    grep -n "system_health" src/api/routes/health.py
    grep -n "http://localhost:9090/api/v1/query" src/api/routes/health.py
    grep -n "agent_last_message_timestamp_seconds" src/api/routes/health.py
    grep -n "agent_dlq_total" src/api/routes/health.py
    grep -n "signal_replay_unresolved_gauge" src/api/routes/health.py
    .venv/bin/ruff check src/api/routes/health.py
    .venv/bin/pytest tests/unit/ -k "health or api_routes" -q
    # Live smoke (only if API is running):
    # curl -sS http://localhost:8000/api/health/system | python -m json.tool
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/api/routes/health.py` contains `@router.get("/system")` decorator
    - Source assertion: handler `async def system_health(...)` returns a dict with keys `timestamp`, `consumer_lag`, `dlq_depth`, `signal_replay_unresolved`, `agent_heartbeats`
    - Source assertion: Prometheus URL string `http://localhost:9090/api/v1/query` is used
    - Source assertion: timeout uses `aiohttp.ClientTimeout(total=3)`
    - Source assertion: each Prometheus query is wrapped in its own `try/except Exception` so single-query failure degrades that field to its default (null/empty) without raising
    - Source assertion: timestamps use `datetime.now(UTC)` and `datetime.fromtimestamp(ts, tz=UTC)` — no `utcnow()` or naive datetime
    - Source assertion: queried metric names match the source-of-truth names defined in `src/observability/metrics.py` (verify in read_first)
    - Behavior assertion: with API running, `curl http://localhost:8000/health/system` returns a JSON object with the five fields and HTTP 200, even when Prometheus is unreachable (all values degraded but structure intact)
    - CLI: `.venv/bin/ruff check src/api/routes/health.py` exits 0
  </acceptance_criteria>
  <done>
    `/api/health/system` endpoint returns aggregated JSON; Prometheus timeouts produce degraded values, never 503; ruff clean.
  </done>
</task>

</tasks>

<verification>
- Endpoint is reachable via existing router prefix `/api/health` (no main.py changes required)
- All four metric queries use the canonical names defined in `src/observability/metrics.py`
- Total query budget bounded at 3 seconds (single ClientTimeout shared across the session)
- Failure of any single query degrades only that field; structure of response is always intact
- No `prometheus_client` imports anywhere in the file
</verification>

<success_criteria>
- OBS-02: GET /health/system returns machine-readable JSON with the four required fields plus a timestamp
- `.venv/bin/ruff check src/api/routes/health.py` exits 0
- `.venv/bin/pytest tests/unit/ -q` exits 0
</success_criteria>

<output>
Create `.planning/phases/086-pipeline-hardening/086-04-SUMMARY.md` documenting: (a) the exact Prometheus metric names used (resolved from `src/observability/metrics.py` source of truth), (b) the agent label key chosen for `agent_heartbeats`, and (c) a curl example with the live response shape captured during testing.
</output>
