---
phase: 084-base-agent-hardening
plan: 02
type: execute
wave: 2
depends_on: ["084-01"]
files_modified:
  - src/core/agent/base_writer.py
  - tests/unit/test_base_writer_agent.py
autonomous: true
requirements: [INFRA-01, INFRA-02]

must_haves:
  truths:
    - "Subclasses can declare payload_model as a Pydantic class attribute; base validates each message before _parse_payload()"
    - "ValidationError on the Pydantic gate routes the raw payload to DLQ and increments _parse_failures_total — does not raise"
    - "Subclasses without payload_model behave exactly as before (raw dict passed to _parse_payload) — backward compatible"
    - "_do_flush() re-raises the underlying exception after incrementing _flush_errors_total; buffer is preserved"
    - "Existing tests that previously expected _do_flush() to swallow exceptions are updated to assert raise + buffer preserved"
  artifacts:
    - path: "src/core/agent/base_writer.py"
      provides: "Pydantic payload gate + flush re-raise contract"
      contains: "payload_model"
    - path: "tests/unit/test_base_writer_agent.py"
      provides: "Updated flush failure tests + new Pydantic gate tests"
  key_links:
    - from: "src/core/agent/base_writer.py:_run"
      to: "type(self).payload_model.model_validate(payload)"
      via: "Pydantic validation gate before _parse_payload"
      pattern: "payload_model"
    - from: "src/core/agent/base_writer.py:_do_flush except block"
      to: "raise"
      via: "re-raise after metrics increment"
      pattern: "self\\._flush_errors_total\\.add"
---

<objective>
Implement BaseWriterAgent contracts: Pydantic payload validation gate (INFRA-01) and _do_flush() raise-or-DLQ enforcement (INFRA-02). Update tests that previously depended on the swallowed-exception behavior.

Purpose: Foundation for Phase 085 writer migration. Each persistence writer will declare a payload_model and gain automatic DLQ routing for malformed messages, eliminating per-writer validation boilerplate.

Output: payload_model ClassVar on BaseWriterAgent, Pydantic gate inserted in _run() before _parse_payload, _do_flush() except block re-raises, three updated/new tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/084-base-agent-hardening/084-CONTEXT.md
@.planning/phases/084-base-agent-hardening/084-RESEARCH.md
@.planning/phases/084-base-agent-hardening/084-PATTERNS.md
@.planning/phases/084-base-agent-hardening/084-01-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Pydantic payload_model gate and re-raise in _do_flush in base_writer.py</name>
  <files>src/core/agent/base_writer.py</files>
  <read_first>
    - src/core/agent/base_writer.py (full file — class attr region lines 79-83, _parse_payload abstract method around line 144, _do_flush except block lines 280-285, _run parse+buffer block around lines 312-318, _maybe_route_to_dlq usage)
    - .planning/phases/084-base-agent-hardening/084-PATTERNS.md (section "src/core/agent/base_writer.py (INFRA-01, INFRA-02)")
    - src/core/schemas/dlq_payload.py (DLQ payload schema — confirm shape, no change required)
  </read_first>
  <action>
    Modify src/core/agent/base_writer.py in place. Three changes:

    1. Add `payload_model: ClassVar[type[BaseModel] | None] = None` as a class attribute on BaseWriterAgent, grouped with existing BATCH_SIZE / FLUSH_INTERVAL_SECS / MAX_BUFFER_SIZE / BUFFER_ALERT_PCT (around lines 79-83). Add the required imports at the top of the module: `from typing import ClassVar` (if not already imported) and `from pydantic import BaseModel, ValidationError`.

    2. In _run(), insert a Pydantic validation gate immediately before the existing `_parse_payload(payload)` call (around lines 312-318). Use `model_cls = type(self).payload_model` (not `self.payload_model` — class-level lookup avoids instance attribute shadowing). When `model_cls is not None`: try `validated = model_cls.model_validate(payload)`; on success call `rows = self._parse_payload(validated)`; on `ValidationError` increment `self._parse_failures_total` and call `await self._maybe_route_to_dlq(payload, exc)`, then `continue` the consume loop (do not raise, do not buffer). When `model_cls is None`: keep the existing behavior `rows = self._parse_payload(payload)` exactly.

       Preserve all surrounding behavior: the existing wrapping try/except, the rows-None path (`self._parse_failures_total.add(1)` + `_maybe_route_to_dlq(payload, Exception("Parse failed"))`), the `_buffer_rows(rows)` call, and the outer exception re-raise.

    3. In _do_flush() (lines 280-285), append `raise` as the final statement of the except block, after `self.logger.exception("flush_failed", batch_size=len(batch))`. Do not change the success-path buffer clear, the span, or the _flush_errors_total increment. The exception now propagates to the caller; the buffer stays intact because the buffer clear only happens on the success path (already correct).

    Do NOT change the `_parse_payload` abstract method signature (`def _parse_payload(self, payload: dict) -> list | None`). Subclasses that opt into payload_model receive a BaseModel instance at runtime — document this in the docstring only.

    Do NOT modify the success path or commit logic in _do_flush. Do NOT remove or modify the existing `_flush_errors_total.add(1)` call — keep it before the raise.
  </action>
  <verify>
    .venv/bin/ruff check src/core/agent/base_writer.py
    .venv/bin/python -c "from src.core.agent.base_writer import BaseWriterAgent; assert BaseWriterAgent.payload_model is None; print('ok')"
    grep -n "payload_model" src/core/agent/base_writer.py
    grep -n "ValidationError" src/core/agent/base_writer.py
    grep -nE "^\s+raise$" src/core/agent/base_writer.py
  </verify>
  <acceptance_criteria>
    - src/core/agent/base_writer.py contains `payload_model: ClassVar[type[BaseModel] | None] = None`
    - src/core/agent/base_writer.py contains `from pydantic import` line referencing both `BaseModel` and `ValidationError`
    - src/core/agent/base_writer.py contains `type(self).payload_model`
    - src/core/agent/base_writer.py contains `model_validate(payload)`
    - src/core/agent/base_writer.py contains `except ValidationError`
    - The _do_flush except block ends with a bare `raise` statement on its own line after `self.logger.exception("flush_failed"`
    - The line `self._flush_errors_total.add(1)` is still present in _do_flush
    - Import command exits 0 and confirms default `payload_model is None`
    - ruff check exits 0
  </acceptance_criteria>
  <done>BaseWriterAgent.payload_model is declarable; ValidationError routes to DLQ; _do_flush re-raises.</done>
</task>

<task type="auto">
  <name>Task 2: Update existing flush tests and add Pydantic gate tests</name>
  <files>tests/unit/test_base_writer_agent.py</files>
  <read_first>
    - tests/unit/test_base_writer_agent.py (full file — focus on TestOffsetCommit.test_no_commit_on_flush_failure around line 189 and test_flush_errors_counter_increments_on_failure around line 309; existing fixtures and minimal writer subclass scaffolding)
    - src/core/agent/base_writer.py (post Task 1 state)
    - .planning/phases/084-base-agent-hardening/084-PATTERNS.md (section "Test files (INFRA-01, INFRA-02, INFRA-03, INFRA-05 coverage)")
  </read_first>
  <action>
    Make two test updates and add three new tests in tests/unit/test_base_writer_agent.py.

    UPDATES (existing tests):

    1. test_no_commit_on_flush_failure (around line 189): replace the line `await agent._do_flush()` with the `pytest.raises(RuntimeError, match="DB down")` context manager wrapping the same call (preserve the existing exception message expectation, or whichever the mock raises). Keep the post-call assertions `assert len(agent._buffer) == 1` and `agent._consumer.commit.assert_not_awaited()` unchanged.

    2. test_flush_errors_counter_increments_on_failure (around line 309): wrap the `await agent._do_flush()` call with `pytest.raises(RuntimeError)`. Keep the counter assertion afterwards.

    NEW TESTS — append to the file (or to an appropriate test class):

    3. `test_payload_model_default_is_none` — synchronous. Imports BaseWriterAgent and asserts `BaseWriterAgent.payload_model is None`.

    4. `test_pydantic_validation_error_routes_to_dlq` — async. Define a minimal concrete writer subclass with a Pydantic `BaseModel` (e.g., `class _M(BaseModel): id: int`) assigned to `payload_model`. Mock `_maybe_route_to_dlq` (AsyncMock) and `_parse_failures_total` counter (MagicMock). Run a single iteration of the parse path with `payload = {"id": "not_an_int"}` (use the existing test fixture pattern that drives a single message through _run, or call the parse gate directly if exposed; otherwise replicate the gate inline by extracting it into a helper or stage the test through the consumer loop with a one-shot stop). Assert: `_maybe_route_to_dlq` was awaited once with `(payload, exc)` where exc is a ValidationError; `_parse_failures_total.add` was called with 1; the buffer remains empty.

    5. `test_pydantic_validation_success_passes_validated_model_to_parse` — async. Same writer subclass as test 4 but `payload = {"id": 42}`. Override `_parse_payload` on the subclass to record its argument. Assert: `_parse_payload` was called with a `BaseModel` instance whose `.id == 42` (not a raw dict).

    Use the existing async test fixture conventions in the file. If existing tests already drive _run() through a managed consumer stub, reuse that pattern. If extending the gate path through _run() is overly complex, it is acceptable to factor the gate into a small private helper method (e.g., `_apply_payload_model(payload) -> tuple[list | None, Exception | None]`) on BaseWriterAgent and call that helper directly from tests 4 and 5 — but only if such refactor is small (≤15 lines) and keeps _run() semantics identical.

    Do NOT modify other unrelated tests. Do NOT introduce new top-level fixtures that shadow existing ones.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/test_base_writer_agent.py -v
    .venv/bin/ruff check tests/unit/test_base_writer_agent.py
  </verify>
  <acceptance_criteria>
    - tests/unit/test_base_writer_agent.py contains `pytest.raises(RuntimeError` inside the body of test_no_commit_on_flush_failure
    - tests/unit/test_base_writer_agent.py contains `pytest.raises(RuntimeError` inside the body of test_flush_errors_counter_increments_on_failure
    - tests/unit/test_base_writer_agent.py contains function `def test_payload_model_default_is_none(`
    - tests/unit/test_base_writer_agent.py contains function `async def test_pydantic_validation_error_routes_to_dlq(`
    - tests/unit/test_base_writer_agent.py contains function `async def test_pydantic_validation_success_passes_validated_model_to_parse(`
    - pytest tests/unit/test_base_writer_agent.py exits 0
    - ruff check exits 0
  </acceptance_criteria>
  <done>All existing tests still pass under the new re-raise contract; three new Pydantic gate tests pass.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/test_base_writer_agent.py -v` exits 0
- `.venv/bin/ruff check src/core/agent/base_writer.py tests/unit/test_base_writer_agent.py` exits 0
- INFRA-01: Subclass declaring `payload_model` triggers ValidationError -> DLQ route automatically (verified by test 4)
- INFRA-02: _do_flush() raises on failure; buffer stays intact (verified by updated tests 1+2)
</verification>

<success_criteria>
- BaseWriterAgent.payload_model declarable as ClassVar[type[BaseModel] | None]
- Pydantic gate validates payload and routes ValidationError to DLQ before _parse_payload
- _do_flush re-raises after _flush_errors_total increment
- Backward compatibility preserved (subclasses without payload_model unchanged)
- INFRA-01 and INFRA-02 verified by unit tests
</success_criteria>

<output>
After completion, create `.planning/phases/084-base-agent-hardening/084-02-SUMMARY.md`
</output>
