---
phase: 084-base-agent-hardening
plan: 03
type: execute
wave: 2
depends_on: ["084-01"]
files_modified:
  - src/core/ai/base_agent.py
  - src/core/ai/base_group_service.py
  - services/alpha_swarm_agent.py
  - tests/unit/test_base_group_service.py
autonomous: true
requirements: [INFRA-04, INFRA-06]

must_haves:
  truths:
    - "BaseAIAgent._on_error() increments AI_AGENT_ERRORS_TOTAL counter with {agent_id, error_type} labels — no longer pass"
    - "BaseAIAgent._on_error() publishes an agent_prediction lineage event when self._lineage is set"
    - "BaseGroupService instantiates LineageRecorder once in _setup() and stops it in _teardown(); each constituent agent has its _lineage attribute set"
    - "BaseGroupService._graduation_loop() stub and has_graduation class attribute are deleted; dispatch uses method-override detection"
    - "AlphaSwarmAgent retains its real _graduation_loop() override; its has_graduation = True and its self-instantiated LineageRecorder are removed (consolidated onto base)"
  artifacts:
    - path: "src/core/ai/base_agent.py"
      provides: "_on_error OTel counter emission + optional LineageRecorder publish"
      contains: "AI_AGENT_ERRORS_TOTAL"
    - path: "src/core/ai/base_group_service.py"
      provides: "LineageRecorder wiring + dead graduation loop removed"
      contains: "LineageRecorder"
    - path: "services/alpha_swarm_agent.py"
      provides: "Consumer-side cleanup: has_graduation removed, self-instantiated LineageRecorder removed"
  key_links:
    - from: "src/core/ai/base_agent.py:_on_error"
      to: "AI_AGENT_ERRORS_TOTAL counter"
      via: ".add(1, {agent_id, error_type})"
      pattern: "AI_AGENT_ERRORS_TOTAL\\.add"
    - from: "src/core/ai/base_group_service.py:_setup"
      to: "LineageRecorder.start"
      via: "instantiated and started; assigned to each agent._lineage"
      pattern: "LineageRecorder\\("
    - from: "src/core/ai/base_group_service.py:_run"
      to: "_graduation_loop dispatch via override detection"
      via: "type(self)._graduation_loop is not BaseGroupService._graduation_loop"
      pattern: "type\\(self\\)\\._graduation_loop"
---

<objective>
Wire INFRA-04 (BaseAIAgent._on_error OTel emission + lineage publish) and resolve INFRA-06 dead code (delete graduation loop stub, wire LineageRecorder in BaseGroupService, consolidate AlphaSwarmAgent's self-instantiated recorder onto the base).

Purpose: Every AI agent error becomes observable (counter + optional lineage event). The placeholder graduation loop in the base is removed — AlphaSwarmAgent keeps its real implementation through method-override dispatch. LineageRecorder is no longer dead code.

Output: AI_AGENT_ERRORS_TOTAL emission in _on_error; LineageRecorder lifecycle in BaseGroupService; graduation stub deleted; AlphaSwarmAgent's duplicate plumbing removed; tests covering all four behaviors.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/084-base-agent-hardening/084-CONTEXT.md
@.planning/phases/084-base-agent-hardening/084-RESEARCH.md
@.planning/phases/084-base-agent-hardening/084-PATTERNS.md
@.planning/phases/084-base-agent-hardening/084-01-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AI_AGENT_ERRORS_TOTAL + lineage publish in BaseAIAgent._on_error and add _lineage attribute</name>
  <files>src/core/ai/base_agent.py</files>
  <read_first>
    - src/core/ai/base_agent.py (current state — __init__ block, _on_error at lines 266-271, existing imports, agent_id attribute access)
    - src/core/ai/lineage.py (LineageRecorder.record() signature at lines 44-56)
    - src/observability/metrics.py (post 084-01 — confirm AI_AGENT_ERRORS_TOTAL is exported)
    - .planning/phases/084-base-agent-hardening/084-PATTERNS.md (section "src/core/ai/base_agent.py (INFRA-04)")
  </read_first>
  <action>
    Modify src/core/ai/base_agent.py:

    1. Import AI_AGENT_ERRORS_TOTAL from src.observability.metrics (group with existing metric imports). Import `LineageRecorder` from src.core.ai.lineage for the type hint (use `from __future__ import annotations` if not already present, or import inside TYPE_CHECKING block — match existing style).

    2. In BaseAIAgent.__init__, add an instance attribute `self._lineage: LineageRecorder | None = None`. Place it near the existing attribute assignments. The BaseGroupService is responsible for setting this after construction (Task 2).

    3. Replace the body of `_on_error(self, error: Exception)` (currently `pass`) with two operations:
       a. Increment `AI_AGENT_ERRORS_TOTAL.add(1, {"agent_id": self.agent_id, "error_type": type(error).__name__})`.
       b. If `self._lineage is not None`, call `self._lineage.record(signal_id=UUID(int=0), event_type="agent_prediction", source=self.agent_id, metadata={"error": str(error), "error_type": type(error).__name__})`. Use `UUID(int=0)` as the sentinel for non-signal errors (import `from uuid import UUID` if not already imported).

    Keep the existing docstring; update it briefly to state that the counter is emitted and lineage is published when available. Method remains `async`.

    Do NOT change the method signature. Do NOT raise from _on_error (it is a hook called from a higher-level except path). Do NOT add new sleeps or awaits other than the existing `async` declaration — `LineageRecorder.record()` is synchronous per lineage.py.
  </action>
  <verify>
    .venv/bin/ruff check src/core/ai/base_agent.py
    .venv/bin/python -c "from src.core.ai.base_agent import BaseAIAgent; import inspect; src = inspect.getsource(BaseAIAgent._on_error); assert 'AI_AGENT_ERRORS_TOTAL' in src; assert 'self._lineage' in src; print('ok')"
    grep -n "AI_AGENT_ERRORS_TOTAL" src/core/ai/base_agent.py
    grep -n "self._lineage" src/core/ai/base_agent.py
  </verify>
  <acceptance_criteria>
    - src/core/ai/base_agent.py imports `AI_AGENT_ERRORS_TOTAL`
    - src/core/ai/base_agent.py contains `self._lineage` initialized to None in __init__
    - src/core/ai/base_agent.py `_on_error` body contains `AI_AGENT_ERRORS_TOTAL.add(1,` with labels including `agent_id` and `error_type`
    - src/core/ai/base_agent.py `_on_error` body contains `self._lineage.record(`
    - src/core/ai/base_agent.py `_on_error` body contains `signal_id=UUID(int=0)` and `event_type="agent_prediction"`
    - src/core/ai/base_agent.py does NOT contain `_on_error` body as just `pass` (verify by grepping the method)
    - ruff check exits 0
    - The inspect-based assertion command exits 0
  </acceptance_criteria>
  <done>_on_error is no longer a no-op; counter fires every call; lineage publishes when wired.</done>
</task>

<task type="auto">
  <name>Task 2: Wire LineageRecorder lifecycle in BaseGroupService; delete graduation stub + has_graduation; dispatch via override detection</name>
  <files>src/core/ai/base_group_service.py</files>
  <read_first>
    - src/core/ai/base_group_service.py (full file — has_graduation at line 46, _setup, _run lines 182-199 with `if self.has_graduation` block at 190-192, _teardown at 201-210, _graduation_loop stub at 282-303)
    - services/alpha_swarm_agent.py (has_graduation = True at line 121, _graduation_loop override at lines 217-230, self._lineage instantiation at 180-184)
    - src/core/ai/lineage.py (LineageRecorder __init__ signature — producer + env_name)
    - .planning/phases/084-base-agent-hardening/084-PATTERNS.md (section "src/core/ai/base_group_service.py (INFRA-06)")
  </read_first>
  <action>
    Modify src/core/ai/base_group_service.py with five surgical changes:

    1. Delete the class attribute `has_graduation: bool = False` (line 46).

    2. Delete the entire method `_graduation_loop()` (lines 282-303 — the stub with TODO comments). This removes the placeholder; AlphaSwarmAgent retains its own override.

    3. In `_run()`, replace the block:
           if self.has_graduation:
               graduation_task = asyncio.create_task(self._graduation_loop())
               tasks.append(graduation_task)
       with override-detection dispatch:
           if type(self)._graduation_loop is not BaseGroupService._graduation_loop:
               graduation_task = asyncio.create_task(self._graduation_loop())
               tasks.append(graduation_task)
       However: since the base stub is being deleted in step 2, `BaseGroupService._graduation_loop` will no longer exist as an attribute on the base. To keep override detection clean, use `hasattr(type(self), "_graduation_loop")` as the gate: `if hasattr(type(self), "_graduation_loop"):`. Concretely: the dispatch fires only if the concrete subclass declares the method. AlphaSwarmAgent does, so its task runs.

    4. In `_setup()`, add LineageRecorder wiring. Import `from src.core.ai.lineage import LineageRecorder`. After the existing `self._llm_chain` setup and before `_seed_context_cache()` (or at an equivalent stable point — match the surrounding ordering pattern in the file), add a guarded instantiation:
       - If `not hasattr(self, "_lineage") or self._lineage is None`: create `self._lineage = LineageRecorder(producer=self._producer, env_name=self.env_name)` and `await self._lineage.start()`.
       - Then iterate `for agent in self.agents:` and set `agent._lineage = self._lineage`. Use `getattr(self, "agents", [])` if `self.agents` is constructed later in `_setup` — adjust placement so the loop runs after agents exist.

       Note: the existing AlphaSwarmAgent._setup() calls `await super()._setup()` first, then constructs its own LineageRecorder. After this change, AlphaSwarmAgent will inherit the base recorder via the guard; the AlphaSwarmAgent self-construction is removed in Task 3.

    5. In `_teardown()` (lines 201-210), add at the very beginning of the method body:
           if getattr(self, "_lineage", None) is not None:
               await self._lineage.stop()
       Existing teardown continues unchanged after this guard.

    Do NOT change any other method. Do NOT modify the consumer/producer setup. The dispatch change in step 3 must be evaluated with the deletion in step 2 — if `_graduation_loop` is fully removed from the base, `hasattr(type(self), "_graduation_loop")` is the only correct check (the `is not` comparison breaks when the base attribute does not exist).
  </action>
  <verify>
    .venv/bin/ruff check src/core/ai/base_group_service.py
    .venv/bin/python -c "from src.core.ai.base_group_service import BaseGroupService; assert not hasattr(BaseGroupService, 'has_graduation'); assert not hasattr(BaseGroupService, '_graduation_loop'); print('ok')"
    grep -n "LineageRecorder" src/core/ai/base_group_service.py
    grep -n "self._lineage" src/core/ai/base_group_service.py
    grep -nE "hasattr\(type\(self\), .{1,5}_graduation_loop" src/core/ai/base_group_service.py
  </verify>
  <acceptance_criteria>
    - src/core/ai/base_group_service.py does NOT contain `has_graduation` (any occurrence)
    - src/core/ai/base_group_service.py does NOT contain `def _graduation_loop` (stub deleted)
    - src/core/ai/base_group_service.py contains `from src.core.ai.lineage import LineageRecorder`
    - src/core/ai/base_group_service.py contains `self._lineage = LineageRecorder(`
    - src/core/ai/base_group_service.py contains `await self._lineage.start()`
    - src/core/ai/base_group_service.py contains `await self._lineage.stop()`
    - src/core/ai/base_group_service.py contains `agent._lineage = self._lineage`
    - src/core/ai/base_group_service.py contains `hasattr(type(self), "_graduation_loop")`
    - The python -c assertion (no has_graduation, no _graduation_loop on base) exits 0
    - ruff check exits 0
  </acceptance_criteria>
  <done>Graduation stub eliminated; LineageRecorder lifecycle in place; override-based dispatch preserves AlphaSwarmAgent's behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Remove AlphaSwarmAgent.has_graduation and its self-instantiated LineageRecorder; verify override still dispatches</name>
  <files>services/alpha_swarm_agent.py</files>
  <read_first>
    - services/alpha_swarm_agent.py (full file — class header, has_graduation = True at line 121, _setup with LineageRecorder lines 180-184, _graduation_loop override at lines 217-230)
    - src/core/ai/base_group_service.py (post Task 2 state — confirm dispatch uses hasattr and base provides _lineage)
    - src/core/ai/lineage.py (LineageRecorder API — start/stop/record signatures already documented in Task 1)
  </read_first>
  <action>
    Modify services/alpha_swarm_agent.py with two surgical changes:

    1. Delete the class attribute `has_graduation = True` (line 121). The base no longer references this attribute; dispatch now uses method-override detection (Task 2 step 3).

    2. Delete the local LineageRecorder construction in `_setup()` (lines 180-184):
           self._lineage = LineageRecorder(
               producer=self._producer,
               env_name=self.env_name,
           )
           await self._lineage.start()
       Also delete the corresponding `await self._lineage.stop()` call in this file's _teardown if present (the base now handles it). If AlphaSwarmAgent's _setup imports LineageRecorder solely for this construction, remove that import as well (verify by grepping after deletion).

    Do NOT modify `_graduation_loop()` (lines 217-230) — that override is preserved; the new override-detection dispatch will still pick it up. Do NOT modify the agent's compute / multiplier logic. Do NOT delete any other AlphaSwarmAgent code.

    After the change, `self._lineage` is set by `BaseGroupService._setup()` via `super()._setup()` and propagated to each constituent agent.
  </action>
  <verify>
    .venv/bin/ruff check services/alpha_swarm_agent.py
    grep -n "has_graduation" services/alpha_swarm_agent.py
    grep -n "self._lineage = LineageRecorder" services/alpha_swarm_agent.py
    .venv/bin/python -c "from services.alpha_swarm_agent import AlphaSwarmComputeAgent; from src.core.ai.base_group_service import BaseGroupService; assert hasattr(AlphaSwarmComputeAgent, '_graduation_loop'); print('graduation override intact')"
  </verify>
  <acceptance_criteria>
    - services/alpha_swarm_agent.py does NOT contain `has_graduation`
    - services/alpha_swarm_agent.py does NOT contain `self._lineage = LineageRecorder(`
    - services/alpha_swarm_agent.py still contains the method definition for `_graduation_loop` (override preserved)
    - The python -c verification exits 0 (override intact)
    - ruff check exits 0
  </acceptance_criteria>
  <done>AlphaSwarmAgent consolidated onto base LineageRecorder; graduation override still dispatched.</done>
</task>

<task type="auto">
  <name>Task 4: Add unit tests for INFRA-04 and INFRA-06 in test_base_group_service.py</name>
  <files>tests/unit/test_base_group_service.py</files>
  <read_first>
    - tests/unit/test_base_group_service.py (current state if it exists; otherwise inspect tests/unit/ for an existing related test file; ls tests/unit/ first)
    - src/core/ai/base_group_service.py (post Task 2 state)
    - src/core/ai/base_agent.py (post Task 1 state)
    - src/core/ai/lineage.py (LineageRecorder API)
    - .planning/phases/084-base-agent-hardening/084-PATTERNS.md
  </read_first>
  <action>
    Create or extend tests/unit/test_base_group_service.py. If the file does not exist, create it with the same header conventions used by other tests in tests/unit/ (pytest, pytest-asyncio, structlog disabled or captured per project pattern — match an existing file such as tests/unit/test_base_agent.py for fixture style).

    Add these tests (function names must match exactly):

    1. `test_base_group_service_has_no_graduation_attr` — synchronous. Imports BaseGroupService and asserts `not hasattr(BaseGroupService, "has_graduation")` and `not hasattr(BaseGroupService, "_graduation_loop")`.

    2. `test_on_error_increments_ai_agent_errors_total` — async. Construct a minimal BaseAIAgent subclass (or use an existing test double pattern in the codebase). Patch `src.core.ai.base_agent.AI_AGENT_ERRORS_TOTAL.add` with MagicMock. Call `await agent._on_error(RuntimeError("boom"))`. Assert the mock was called once with positional `1` and kwargs/labels dict containing `agent_id` and `error_type` == `"RuntimeError"`.

    3. `test_on_error_publishes_to_lineage_when_set` — async. Same fixture as test 2; set `agent._lineage = MagicMock(record=MagicMock())`. Call `await agent._on_error(ValueError("x"))`. Assert `agent._lineage.record.call_count == 1`; check kwargs include `event_type="agent_prediction"`, `source=agent.agent_id`, and metadata dict contains `"error_type": "ValueError"`.

    4. `test_on_error_skips_lineage_when_none` — async. Same fixture; ensure `agent._lineage is None`. Call `await agent._on_error(RuntimeError("y"))`. Assert no AttributeError raised and the counter still incremented (mock AI_AGENT_ERRORS_TOTAL.add as in test 2).

    5. `test_graduation_dispatch_via_override_detection` — synchronous. Define a local subclass of BaseGroupService that defines its own `_graduation_loop`. Assert `hasattr(type(instance), "_graduation_loop")` is True for the subclass and False for a plain BaseGroupService (note: cannot instantiate BaseGroupService directly if abstract; perform the check at the class level via `hasattr(SubClass, "_graduation_loop")` and `hasattr(BaseGroupService, "_graduation_loop")`).

    Each test should be self-contained. Use `unittest.mock.patch` / `MagicMock` / `AsyncMock` per existing project conventions. Async tests use `@pytest.mark.asyncio`.

    Do NOT introduce integration-level fixtures (no real Kafka, no real DB). Mock self._producer if any test constructs a BaseGroupService instance.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/test_base_group_service.py -v
    .venv/bin/ruff check tests/unit/test_base_group_service.py
  </verify>
  <acceptance_criteria>
    - tests/unit/test_base_group_service.py contains function `def test_base_group_service_has_no_graduation_attr(`
    - tests/unit/test_base_group_service.py contains function `async def test_on_error_increments_ai_agent_errors_total(`
    - tests/unit/test_base_group_service.py contains function `async def test_on_error_publishes_to_lineage_when_set(`
    - tests/unit/test_base_group_service.py contains function `async def test_on_error_skips_lineage_when_none(`
    - tests/unit/test_base_group_service.py contains function `def test_graduation_dispatch_via_override_detection(`
    - pytest tests/unit/test_base_group_service.py exits 0 with at least 5 tests passing
    - ruff check exits 0
  </acceptance_criteria>
  <done>INFRA-04 and INFRA-06 covered by unit tests.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/test_base_group_service.py tests/unit/test_base_agent.py -v` exits 0
- `.venv/bin/ruff check src/core/ai/base_agent.py src/core/ai/base_group_service.py services/alpha_swarm_agent.py tests/unit/test_base_group_service.py` exits 0
- `grep -rn "has_graduation" src/ services/` returns no matches
- `grep -rn "def _graduation_loop" src/core/ai/base_group_service.py` returns no matches
- AlphaSwarmComputeAgent still has _graduation_loop method (override preserved)
</verification>

<success_criteria>
- INFRA-04: _on_error emits AI_AGENT_ERRORS_TOTAL and publishes lineage when available
- INFRA-06: graduation stub deleted; LineageRecorder fully wired in BaseGroupService; AlphaSwarmAgent consolidated
- Override-based dispatch keeps AlphaSwarmAgent._graduation_loop running
- No double-instantiation of LineageRecorder
- All four behaviors covered by unit tests
</success_criteria>

<output>
After completion, create `.planning/phases/084-base-agent-hardening/084-03-SUMMARY.md`
</output>
