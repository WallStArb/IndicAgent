---
phase: 084-base-agent-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/observability/metrics.py
  - src/core/agent/base.py
  - tests/unit/test_base_agent.py
autonomous: true
requirements: [INFRA-03, INFRA-05]

must_haves:
  truths:
    - "Subclasses can override SETUP_RETRY_ATTEMPTS and SETUP_RETRY_BACKOFF_S as class attributes"
    - "Subclasses can opt into circuit-breaker protection by setting circuit_breaker = True"
    - "_setup_with_retry() reads attempts/backoff from class attributes, not hardcoded locals"
    - "When circuit_breaker is True and all setup retries fail, _cb_open is set and AGENT_CIRCUIT_BREAKER_STATE gauge reports 2 (open)"
    - "agent_setup_retries_total counter increments once per retry attempt"
    - "agent_dlq_total counter increments unconditionally in _send_to_dlq()"
  artifacts:
    - path: "src/observability/metrics.py"
      provides: "AGENT_DLQ_TOTAL, AGENT_SETUP_RETRIES_TOTAL, AGENT_CIRCUIT_BREAKER_STATE, AI_AGENT_ERRORS_TOTAL OTel instruments"
      contains: "agent_dlq_total"
    - path: "src/core/agent/base.py"
      provides: "Class attribute retry/CB config + open-gate logic in start()"
      contains: "SETUP_RETRY_ATTEMPTS"
    - path: "tests/unit/test_base_agent.py"
      provides: "Coverage for class attr defaults, override, and CB open-gate behavior"
  key_links:
    - from: "src/core/agent/base.py:_setup_with_retry"
      to: "self.SETUP_RETRY_ATTEMPTS / self.SETUP_RETRY_BACKOFF_S"
      via: "instance attribute lookup resolves to class attribute"
      pattern: "self\\.SETUP_RETRY_ATTEMPTS"
    - from: "src/core/agent/base.py:start"
      to: "_setup_with_retry when circuit_breaker is True"
      via: "branch on self.circuit_breaker"
      pattern: "if self\\.circuit_breaker"
    - from: "src/core/agent/base.py:_send_to_dlq"
      to: "AGENT_DLQ_TOTAL counter"
      via: ".add(1, self._dlq_attrs)"
      pattern: "AGENT_DLQ_TOTAL\\.add"
---

<objective>
Add configurable retry + opt-in circuit-breaker support to BaseAgent, plus four new OTel instruments (agent_dlq_total, agent_setup_retries_total, agent_circuit_breaker_state, ai_agent_errors_total).

Purpose: Foundation for Phase 085 writer migration. Eliminates 3x duplicated retry scaffolding (bar_aggregator_agent, bar_writer_agent, signal_tracker_compute_agent will migrate in Phase 085). Provides observable per-agent setup/DLQ/CB health.

Output: BaseAgent class attrs (SETUP_RETRY_ATTEMPTS=3, SETUP_RETRY_BACKOFF_S=2.0, circuit_breaker=False), reworked _setup_with_retry(), CB open-gate in start(), AGENT_DLQ_TOTAL increment in _send_to_dlq(), four new instruments registered, tests covering defaults/override/CB behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/084-base-agent-hardening/084-CONTEXT.md
@.planning/phases/084-base-agent-hardening/084-RESEARCH.md
@.planning/phases/084-base-agent-hardening/084-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register four new OTel instruments in metrics.py</name>
  <files>src/observability/metrics.py</files>
  <read_first>
    - src/observability/metrics.py (current state; instruments live around line 67 CIRCUIT_BREAKER_STATE, line 199 AGENT_LAST_MESSAGE_TIMESTAMP_SECONDS, line 264 DLQ_MESSAGES_TOTAL, line 350 AI_AGENT_INVOCATIONS_TOTAL)
    - .planning/phases/084-base-agent-hardening/084-PATTERNS.md (section "src/observability/metrics.py (D-09 + INFRA-04)")
  </read_first>
  <action>
    Add a "Base agent hardening metrics (Phase 84)" section near the existing "Agent liveness" section (around line 196). Use the project-standard pattern `_meter.create_counter(name, description=...)` / `_meter.create_gauge(name, description=...)` — same `_meter` variable already in the module.

    Add exactly these four instruments:
    - Counter `agent_dlq_total` — description: "Per-agent DLQ event count (all paths, including log-only discard)". Module-level name: AGENT_DLQ_TOTAL.
    - Counter `agent_setup_retries_total` — description: "Setup retry attempts per agent (each retry loop iteration)". Module-level name: AGENT_SETUP_RETRIES_TOTAL.
    - Gauge `agent_circuit_breaker_state` — description: "Agent setup circuit breaker state: 0=closed, 1=half-open, 2=open". Module-level name: AGENT_CIRCUIT_BREAKER_STATE. Use create_gauge (same as existing CIRCUIT_BREAKER_STATE at line 67).
    - Counter `ai_agent_errors_total` — description: "AI agent _compute() errors by agent_id and error_type". Module-level name: AI_AGENT_ERRORS_TOTAL.

    Do NOT create a duplicate `agent_last_processed_timestamp` — AGENT_LAST_MESSAGE_TIMESTAMP_SECONDS already covers D-09 item 2.

    Do NOT modify existing instruments. Do NOT import prometheus_client (per CLAUDE.md — direct OTel SDK only).
  </action>
  <verify>
    .venv/bin/python -c "from src.observability.metrics import AGENT_DLQ_TOTAL, AGENT_SETUP_RETRIES_TOTAL, AGENT_CIRCUIT_BREAKER_STATE, AI_AGENT_ERRORS_TOTAL; print('ok')"
    .venv/bin/ruff check src/observability/metrics.py
  </verify>
  <acceptance_criteria>
    - src/observability/metrics.py contains string `AGENT_DLQ_TOTAL = _meter.create_counter(`
    - src/observability/metrics.py contains string `"agent_dlq_total"`
    - src/observability/metrics.py contains string `AGENT_SETUP_RETRIES_TOTAL = _meter.create_counter(`
    - src/observability/metrics.py contains string `"agent_setup_retries_total"`
    - src/observability/metrics.py contains string `AGENT_CIRCUIT_BREAKER_STATE = _meter.create_gauge(`
    - src/observability/metrics.py contains string `"agent_circuit_breaker_state"`
    - src/observability/metrics.py contains string `AI_AGENT_ERRORS_TOTAL = _meter.create_counter(`
    - src/observability/metrics.py contains string `"ai_agent_errors_total"`
    - Import command exits 0
    - ruff check exits 0
    - No occurrence of `prometheus_client` added to metrics.py
  </acceptance_criteria>
  <done>All four instruments importable from src.observability.metrics; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add class attrs, rewrite _setup_with_retry, add CB open-gate in start, wire AGENT_DLQ_TOTAL in _send_to_dlq</name>
  <files>src/core/agent/base.py</files>
  <read_first>
    - src/core/agent/base.py (current state — class attr region near top, __init__ lines 121-124, start() lines 187-200, _send_to_dlq() at line 332, _setup_with_retry() lines 446-469)
    - src/observability/metrics.py (must already export AGENT_DLQ_TOTAL, AGENT_SETUP_RETRIES_TOTAL, AGENT_CIRCUIT_BREAKER_STATE from Task 1)
    - .planning/phases/084-base-agent-hardening/084-PATTERNS.md (section "src/core/agent/base.py (INFRA-03, INFRA-05, D-09)")
    - services/alpha_swarm_agent.py (only to confirm no consumer reads SETUP_RETRY_ATTEMPTS today — do not modify)
  </read_first>
  <action>
    Modify src/core/agent/base.py in place. Five surgical changes:

    1. Add three class attributes on BaseAgent at the class body level (near other class-level defaults): `SETUP_RETRY_ATTEMPTS: int = 3`, `SETUP_RETRY_BACKOFF_S: float = 2.0`, `circuit_breaker: bool = False`. Plain annotated assignments (no ClassVar — match the existing BATCH_SIZE style from base_writer.py lines 79-83).

    2. Import AGENT_DLQ_TOTAL, AGENT_SETUP_RETRIES_TOTAL, AGENT_CIRCUIT_BREAKER_STATE from src.observability.metrics at the top of the file (group with existing metric imports).

    3. In __init__, add three cached attribute dicts and the open flag, alongside existing self._crash_attrs / self._setup_success_attrs / self._setup_latency_attrs:
       - self._dlq_attrs = {"agent_id": self._agent_label}   # NOTE: label key is `agent_id` (D-09)
       - self._cb_attrs = {"agent": self._agent_label}
       - self._cb_open: bool = False

    4. Rewrite _setup_with_retry() body (lines 446-469): replace local `_attempts = 3` with `self.SETUP_RETRY_ATTEMPTS`; replace local `_backoff_base = 2.0` with `self.SETUP_RETRY_BACKOFF_S`; in the warning branch (the path that sleeps), call `AGENT_SETUP_RETRIES_TOTAL.add(1, self._cb_attrs)` before the asyncio.sleep. The for-loop and exception re-raise on final attempt must remain semantically identical — only attempts/backoff source and the retry counter are added.

    5. In start() setup block (lines 187-200): branch on `self.circuit_breaker`. When True, call `await self._setup_with_retry()`; when False, call `await self._setup()` (existing behavior). Wrap the True-branch call so that if it raises after all retries: set `self._cb_open = True`; call `AGENT_CIRCUIT_BREAKER_STATE.set(2, self._cb_attrs)`; then re-raise. Existing AGENT_SETUP_FAILURE_TOTAL increment and re-raise on the outer except path remain unchanged.

    6. In _send_to_dlq() (line 332), at the top of the method body before any conditional Kafka-routing logic, call `AGENT_DLQ_TOTAL.add(1, self._dlq_attrs)`. The existing DLQ_MESSAGES_TOTAL increment in the Kafka-routed branch is preserved unchanged (different metric, different labels).

    Do NOT change the abstract `_run` signature. Do NOT delete `_attempts`/`_backoff_base` references elsewhere — they are local to _setup_with_retry only. Do NOT introduce a half-open transition in this plan (only 0=closed default and 2=open on total failure); half-open=1 remains reserved for future use.
  </action>
  <verify>
    .venv/bin/ruff check src/core/agent/base.py
    .venv/bin/python -c "from src.core.agent.base import BaseAgent; assert BaseAgent.SETUP_RETRY_ATTEMPTS == 3; assert BaseAgent.SETUP_RETRY_BACKOFF_S == 2.0; assert BaseAgent.circuit_breaker is False; print('ok')"
    grep -n "self.SETUP_RETRY_ATTEMPTS" src/core/agent/base.py
    grep -n "AGENT_DLQ_TOTAL.add" src/core/agent/base.py
    grep -n "AGENT_SETUP_RETRIES_TOTAL.add" src/core/agent/base.py
    grep -n "AGENT_CIRCUIT_BREAKER_STATE.set" src/core/agent/base.py
  </verify>
  <acceptance_criteria>
    - src/core/agent/base.py contains line matching `SETUP_RETRY_ATTEMPTS: int = 3`
    - src/core/agent/base.py contains line matching `SETUP_RETRY_BACKOFF_S: float = 2.0`
    - src/core/agent/base.py contains line matching `circuit_breaker: bool = False`
    - src/core/agent/base.py contains `self._cb_open: bool = False`
    - src/core/agent/base.py contains `self._dlq_attrs = {"agent_id": self._agent_label}`
    - src/core/agent/base.py contains `self.SETUP_RETRY_ATTEMPTS` (at least one occurrence)
    - src/core/agent/base.py contains `self.SETUP_RETRY_BACKOFF_S` (at least one occurrence)
    - src/core/agent/base.py contains `AGENT_DLQ_TOTAL.add(1, self._dlq_attrs)`
    - src/core/agent/base.py contains `AGENT_SETUP_RETRIES_TOTAL.add(1, self._cb_attrs)`
    - src/core/agent/base.py contains `AGENT_CIRCUIT_BREAKER_STATE.set(2, self._cb_attrs)`
    - src/core/agent/base.py contains `if self.circuit_breaker`
    - src/core/agent/base.py does NOT contain the literal line `_attempts = 3` (hardcoded local removed)
    - src/core/agent/base.py does NOT contain the literal line `_backoff_base = 2.0` (hardcoded local removed)
    - ruff check exits 0
    - The class-attr import-time assertion command exits 0
  </acceptance_criteria>
  <done>BaseAgent exposes class-attr config; CB opt-in gates retry+open behavior; per-agent DLQ counter fires on every DLQ path.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for INFRA-03 class attrs and INFRA-05 circuit breaker open-gate</name>
  <files>tests/unit/test_base_agent.py</files>
  <read_first>
    - tests/unit/test_base_agent.py (existing test scaffolding — MinimalAgent fixture, test_base_agent_tracks_setup_failure pattern around line 397, test_teardown_called_on_run_exception around line 159)
    - src/core/agent/base.py (post Task 2 state — class attrs and CB logic)
    - .planning/phases/084-base-agent-hardening/084-PATTERNS.md (section "tests/unit/test_base_agent.py")
  </read_first>
  <action>
    Append (do not replace existing tests) the following new tests to tests/unit/test_base_agent.py. Use the existing MinimalAgent fixture pattern; if MinimalAgent is not exposed, define a local subclass inline per test as needed. Match existing pytest_asyncio / unittest.mock.patch idioms in the file.

    New tests (function names must match exactly):

    1. `test_setup_retry_class_attrs_default` — synchronous test. Asserts default class attr values on BaseAgent: SETUP_RETRY_ATTEMPTS == 3, SETUP_RETRY_BACKOFF_S == 2.0, circuit_breaker is False.

    2. `test_setup_retry_class_attrs_overridable` — synchronous. Defines a local subclass declaring SETUP_RETRY_ATTEMPTS = 1 and SETUP_RETRY_BACKOFF_S = 0.1; asserts the subclass picks up the overrides.

    3. `test_circuit_breaker_default_off` — synchronous. Asserts default BaseAgent.circuit_breaker is False and a default-constructed agent has _cb_open == False.

    4. `test_circuit_breaker_opens_after_all_retries_fail` — async. Defines a local subclass with `circuit_breaker = True`, `SETUP_RETRY_ATTEMPTS = 2`, `SETUP_RETRY_BACKOFF_S = 0.0`, an `_setup` that always raises RuntimeError("always fails"), and a no-op `_run`. Patch BaseAgent._register_signal_handlers as in existing tests. Call `await agent.start()` inside `pytest.raises(RuntimeError)`. Assert: `agent._cb_open is True` after start raises.

    5. `test_setup_retry_counter_increments_on_retry` — async. Same fixture style as test 4 but with `SETUP_RETRY_ATTEMPTS = 3`, `_setup` raising RuntimeError. Patch or monkey-patch AGENT_SETUP_RETRIES_TOTAL.add (import path: `src.core.agent.base.AGENT_SETUP_RETRIES_TOTAL`) to count calls. Assert call count == SETUP_RETRY_ATTEMPTS - 1 (one retry counter per pre-final attempt). Use unittest.mock.patch with autospec or MagicMock side_effect tracker.

    Each test must be independent (no shared state). Use `pytest.mark.asyncio` decorator for async tests.

    Do NOT modify existing tests in this file. Do NOT introduce sleep beyond SETUP_RETRY_BACKOFF_S = 0.0.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/test_base_agent.py -v -k "setup_retry or circuit_breaker"
    .venv/bin/ruff check tests/unit/test_base_agent.py
  </verify>
  <acceptance_criteria>
    - tests/unit/test_base_agent.py contains function `def test_setup_retry_class_attrs_default(`
    - tests/unit/test_base_agent.py contains function `def test_setup_retry_class_attrs_overridable(`
    - tests/unit/test_base_agent.py contains function `def test_circuit_breaker_default_off(`
    - tests/unit/test_base_agent.py contains function `async def test_circuit_breaker_opens_after_all_retries_fail(`
    - tests/unit/test_base_agent.py contains function `async def test_setup_retry_counter_increments_on_retry(`
    - pytest -k "setup_retry or circuit_breaker" exits 0 with at least 5 tests collected and passed
    - ruff check exits 0
  </acceptance_criteria>
  <done>All five new tests pass; INFRA-03 and INFRA-05 covered by unit tests.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/test_base_agent.py -v` — all tests green (existing + 5 new)
- `.venv/bin/ruff check src/observability/metrics.py src/core/agent/base.py tests/unit/test_base_agent.py` exits 0
- Importing BaseAgent does not raise; class-attr defaults match documented values
- No occurrence of `_attempts = 3` or `_backoff_base = 2.0` remains in src/core/agent/base.py
</verification>

<success_criteria>
- BaseAgent has SETUP_RETRY_ATTEMPTS, SETUP_RETRY_BACKOFF_S, circuit_breaker class attrs
- _setup_with_retry() consumes the class attrs instead of hardcoded locals
- start() routes through _setup_with_retry() when circuit_breaker=True and opens the gate on total failure
- _send_to_dlq() increments AGENT_DLQ_TOTAL on every invocation
- Four new OTel instruments registered in metrics.py and importable
- INFRA-03 and INFRA-05 requirements verified by passing unit tests
</success_criteria>

<output>
After completion, create `.planning/phases/084-base-agent-hardening/084-01-SUMMARY.md`
</output>
