---
phase: 085-persistence-writer-migration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/ai/lineage.py
  - src/intelligence/schemas.py
autonomous: true
requirements:
  - PERSIST-01
  - PERSIST-04

must_haves:
  truths:
    - "LineageEvent Pydantic model exists in src/core/ai/lineage.py co-located with LineageRecorder"
    - "SignalMetricsEvent discriminated union exists in src/intelligence/schemas.py with three variant models"
    - "Both models can be imported cleanly and pass model_validate() for valid payloads"
    - "Invalid payloads raise ValidationError when model_validate() is called"
  artifacts:
    - path: "src/core/ai/lineage.py"
      provides: "LineageEvent Pydantic model"
      contains: "class LineageEvent(BaseModel)"
    - path: "src/intelligence/schemas.py"
      provides: "SignalMetricsEvent discriminated union"
      contains: "SignalMetricsEvent"
  key_links:
    - from: "src/core/ai/lineage.py LineageEvent"
      to: "services/lineage_writer_agent.py payload_model"
      via: "import LineageEvent from src.core.ai.lineage"
      pattern: "from src\\.core\\.ai\\.lineage import LineageEvent"
    - from: "src/intelligence/schemas.py SignalMetricsEvent"
      to: "services/signal_metrics_writer_agent.py payload_model"
      via: "import SignalMetricsEvent from src.intelligence.schemas"
      pattern: "from src\\.intelligence\\.schemas import SignalMetricsEvent"
---

<objective>
Define the two Pydantic payload schemas that PERSIST-01 and PERSIST-04 writer migrations depend on.
`LineageEvent` goes into `src/core/ai/lineage.py` (co-located with its sole producer `LineageRecorder`).
`SignalMetricsEvent` goes into `src/intelligence/schemas.py` as a discriminated union with three variant
models, one per event_type handled by `signal_metrics_writer_agent`.

Purpose: Writers in Plan 04 will set `payload_model = LineageEvent` and `payload_model = SignalMetricsEvent`
to get free DLQ routing and validation from `BaseWriterAgent`. The schemas must exist first.

Output: `LineageEvent` model in `lineage.py`, `MetricsComputedEvent` + `ICComputedEvent` +
`MetricsDQFailureEvent` + `SignalMetricsEvent` union in `schemas.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/085-persistence-writer-migration/085-CONTEXT.md
@.planning/phases/085-persistence-writer-migration/085-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LineageEvent model to lineage.py</name>
  <files>src/core/ai/lineage.py</files>
  <read_first>
    - src/core/ai/lineage.py — read entire file; field list comes from LineageRecorder.record() signature, not from imagination
    - src/intelligence/schemas.py — reference for Pydantic BaseModel + ConfigDict style used in this codebase
  </read_first>
  <action>
    Add `LineageEvent` Pydantic model to `src/core/ai/lineage.py` immediately above the `LineageRecorder` class (co-location per D-04). Import `pydantic.BaseModel` and `pydantic.ConfigDict` at the top of the file if not already present.

    Field list is derived exactly from `LineageRecorder.record()` signature — do not invent fields:
    - `ts: str` — ISO-8601 UTC timestamp (set by record() before publishing)
    - `signal_id: str` — UUID as string (JSON transport)
    - `event_type: str` — 'transform' | 'agent_prediction' | 'lifecycle'
    - `source: str` — transform_id or agent_id
    - `dag_order: int | None = None`
    - `multiplier: float | None = None`
    - `metadata: dict = {}` — event-specific JSONB; use `default_factory=dict` to avoid mutable default
    - `is_shadow: bool = True`
    - `symbol: str = ""`
    - `tf: str = ""`

    Model config: `model_config = ConfigDict(extra="forbid")` — rejects unknown fields at validation boundary.

    Add a docstring: "Kafka payload schema for topic_signal_lineage events. Field list derived from LineageRecorder.record() signature. Co-located with LineageRecorder (sole producer) per D-04."

    Do NOT modify LineageRecorder itself.
  </action>
  <verify>
    Run: `cd /home/bg/dev/indicagent && .venv/bin/python -c "from src.core.ai.lineage import LineageEvent; e = LineageEvent(ts='2026-01-01T00:00:00Z', signal_id='abc', event_type='transform', source='test'); print(e.model_dump())"`
    Must print dict with all fields, no exception.

    Run: `cd /home/bg/dev/indicagent && .venv/bin/python -c "from src.core.ai.lineage import LineageEvent; from pydantic import ValidationError; [LineageEvent.model_validate({'extra_junk': 1})]"` must raise ValidationError (missing required fields).

    Run: `.venv/bin/ruff check src/core/ai/lineage.py` — zero errors.
  </verify>
  <done>
    - `class LineageEvent(BaseModel)` exists in `src/core/ai/lineage.py`
    - All 10 fields present with correct types and defaults
    - `model_config = ConfigDict(extra="forbid")` present
    - `from src.core.ai.lineage import LineageEvent` imports cleanly
    - Ruff clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Add SignalMetricsEvent discriminated union to schemas.py</name>
  <files>src/intelligence/schemas.py</files>
  <read_first>
    - src/intelligence/schemas.py — read full file to find insertion point near other bus schemas; understand existing import pattern for Annotated, Union, Literal, Field
    - services/signal_metrics_writer_agent.py — read the three handler functions (_handle_metrics_computed lines 50-124, _handle_ic_computed lines 127-154, _handle_dq_failure lines 157-175) to derive exact field lists; do not guess
  </read_first>
  <action>
    Add three variant models and the `SignalMetricsEvent` union to `src/intelligence/schemas.py`. Place them as a cohesive block after the existing `BarIntelligenceRecord` class (or wherever other bus schemas live — read the file first and find the right location).

    Derive all fields from the handler function signatures in `signal_metrics_writer_agent.py`. Do not invent fields.

    `MetricsComputedEvent(BaseModel)` — `model_config = ConfigDict(extra="forbid")`:
    - `event_type: Literal["metrics_computed"]`
    - Fields from `_handle_metrics_computed()`: `track`, `setup_plugin`, `tf`, `regime_type` (str), `window_days` (int), `symbol: str = "*"`, `n` (int), `n_outliers` (int), `never_activated_pct: float | None = None`, `win_rate: float | None = None`, `avg_r: float | None = None`, `std_r: float | None = None`, `sharpe: float | None = None`, `p_value: float | None = None`, `avg_mae: float | None = None`, `avg_mfe: float | None = None`, `computed_at: str`

    `ICComputedEvent(BaseModel)` — `model_config = ConfigDict(extra="forbid")`:
    - `event_type: Literal["ic_computed"]`
    - Fields from `_handle_ic_computed()`: `setup_plugin`, `tf`, `regime_type`, `window_days` (int), `symbol: str = "*"`, `n` (int), `ic: float | None = None`, `p_value: float | None = None`, `is_significant: bool = False`, `computed_at: str`

    `MetricsDQFailureEvent(BaseModel)` — `model_config = ConfigDict(extra="forbid")`:
    - `event_type: Literal["metrics_dq_failure"]`
    - Fields from `_handle_dq_failure()`: `signal_id: str`, `reason_code: str`, `entry_price: float | None = None`, `stop_loss: float | None = None`, `pnl_r: float | None = None`, `direction: str | None = None`, `hmm_regime: str | None = None`, `setup_plugin: str | None = None`

    Discriminated union using Pydantic's `Annotated` + `Field(discriminator="event_type")`:
    ```
    SignalMetricsEvent = Annotated[
        Union[MetricsComputedEvent, ICComputedEvent, MetricsDQFailureEvent],
        Field(discriminator="event_type"),
    ]
    ```

    Add required imports: `Annotated`, `Union`, `Literal` from `typing`; `Field` from `pydantic` — only add what is not already imported.
  </action>
  <verify>
    Run: `.venv/bin/python -c "from src.intelligence.schemas import SignalMetricsEvent, MetricsComputedEvent, ICComputedEvent, MetricsDQFailureEvent; from pydantic import TypeAdapter; ta = TypeAdapter(SignalMetricsEvent); e = ta.validate_python({'event_type': 'metrics_computed', 'track': 'T1', 'setup_plugin': 'X', 'tf': '1m', 'regime_type': 'bull', 'window_days': 30, 'n': 100, 'n_outliers': 5, 'computed_at': '2026-01-01T00:00:00Z'}); print(type(e).__name__)"` must print `MetricsComputedEvent`.

    Run: `.venv/bin/python -c "from src.intelligence.schemas import SignalMetricsEvent; from pydantic import TypeAdapter, ValidationError; ta = TypeAdapter(SignalMetricsEvent); ta.validate_python({'event_type': 'unknown_type'})"` must raise ValidationError.

    Run: `.venv/bin/ruff check src/intelligence/schemas.py` — zero errors.
  </verify>
  <done>
    - `MetricsComputedEvent`, `ICComputedEvent`, `MetricsDQFailureEvent` classes present in `schemas.py`
    - `SignalMetricsEvent` union defined with `discriminator="event_type"`
    - All three variants import cleanly from `src.intelligence.schemas`
    - TypeAdapter validates correct payloads and rejects unknown event_type
    - Ruff clean
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
- `from src.core.ai.lineage import LineageEvent` imports successfully
- `from src.intelligence.schemas import SignalMetricsEvent` imports successfully
- `.venv/bin/pytest tests/unit/ -q` passes with no regressions
- `.venv/bin/ruff check src/core/ai/lineage.py src/intelligence/schemas.py` — zero errors
</verification>

<success_criteria>
`LineageEvent` Pydantic model present in `src/core/ai/lineage.py` with 10 fields matching `LineageRecorder.record()` signature. `SignalMetricsEvent` discriminated union present in `src/intelligence/schemas.py` with three typed variant models. Both import cleanly. Unit tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/085-persistence-writer-migration/085-01-SUMMARY.md`
</output>
