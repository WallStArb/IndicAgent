---
phase: 82-ml-intelligence-quality-qualitative-foundation
plan: 02
type: execute
wave: 2
depends_on: [82-01]
files_modified:
  - src/intelligence/features/smc_context/hmm_regime.py
  - src/intelligence/register_plugins.py
  - src/intelligence/schemas.py
  - services/intelligence_pipeline_agent.py
  - tests/unit/test_hmm_regime_multitf.py
autonomous: true
requirements:
  - P82-HMM-MULTITF
  - P82-REGIME-TRANSITION
user_setup: []

must_haves:
  truths:
    - "HMMRegimePlugin can be instantiated with timeframe and lookback parameters"
    - "Four named HMM instances exist for 1m/5m/15m/1h, each with TF-appropriate lookback"
    - "hmm_regime_entropy and hmm_regime_velocity are emitted by every HMM instance per bar"
    - "Per-TF param file config/hmm_parameters_{tf}.json is loaded when present, falling back to base file"
    - "intelligence_pipeline_agent reloads HMM parameters on SIGUSR1"
  artifacts:
    - path: "src/intelligence/features/smc_context/hmm_regime.py"
      provides: "Parameterized HMMRegimePlugin with entropy and velocity outputs"
      contains: "hmm_regime_entropy"
    - path: "src/intelligence/register_plugins.py"
      provides: "Four named HMM instances registered in TIER_SMC"
      contains: "HMMRegime_5m"
    - path: "src/intelligence/schemas.py"
      provides: "SMCContext extended with hmm_regime_entropy and hmm_regime_velocity"
      contains: "hmm_regime_entropy"
    - path: "tests/unit/test_hmm_regime_multitf.py"
      provides: "Unit tests for multi-TF parameterization and entropy/velocity computation"
  key_links:
    - from: "src/intelligence/register_plugins.py"
      to: "src/intelligence/features/smc_context/hmm_regime.py"
      via: "HMMRegimePlugin(timeframe=..., lookback=...) construction"
      pattern: "HMMRegimePlugin\\(timeframe="
    - from: "services/intelligence_pipeline_agent.py"
      to: "src/intelligence/features/smc_context/hmm_regime.py"
      via: "SIGUSR1 reload of per-TF parameter files"
      pattern: "SIGUSR1"
---

<objective>
Parameterize `HMMRegimePlugin` for multi-TF instances (1m/5m/15m/1h), add `hmm_regime_entropy` (Shannon entropy across 3 state probs) and `hmm_regime_velocity` (rate of change of dominant prob) to its outputs, register all four instances in `TIER_SMC`, extend `SMCContext`, and add SIGUSR1 hot-reload of per-TF parameter files in the intelligence pipeline agent.

Purpose: Every I7 plugin on TF X consumes its own TF's HMM regime context, and the regime gate (Plan 04) consumes entropy/velocity to drive soft-band confidence.
Output: Four HMM instances live in the registry, emitting full per-TF regime features.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-CONTEXT.md
@.planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-RESEARCH.md
@.planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-PATTERNS.md
@src/intelligence/features/smc_context/hmm_regime.py
@src/intelligence/register_plugins.py
@src/intelligence/schemas.py
@services/alpha_swarm_agent.py
</context>

<threat_model>
- No new external network calls or auth surfaces.
- Parameter file loading is read-only and falls back to defaults if file absent — no crash on missing config.
- SIGUSR1 handler must be idempotent and not block the event loop.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize HMMRegimePlugin, add entropy/velocity outputs, extend SMCContext</name>
  <files>src/intelligence/features/smc_context/hmm_regime.py, src/intelligence/schemas.py</files>
  <read_first>
    - src/intelligence/features/smc_context/hmm_regime.py
    - src/intelligence/schemas.py
    - .planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-PATTERNS.md (HMMRegimePlugin section)
    - .planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-RESEARCH.md (P82-HMM-MULTITF section)
  </read_first>
  <action>
    Modify `src/intelligence/features/smc_context/hmm_regime.py`:
    - Convert dataclass fields: add `timeframe: str = "1m"` and `lookback: int = 200` as init params.
    - Change `name`, `inputs`, `outputs` to `field(init=False)`.
    - In `__post_init__`: set `self.name = f"smc_HMMRegime_{self.timeframe}"`, set `self.inputs = (InputSpec(symbol=".*", timeframe=self.timeframe, lookback=self.lookback),)`, set `self.outputs` to the existing frozenset plus the two new strings `"hmm_regime_entropy"` and `"hmm_regime_velocity"`.
    - Extend `_load_parameters()` call: attempt `Path(f"config/hmm_parameters_{self.timeframe}.json")` first; if not present, fall back to `Path("config/hmm_parameters.json")`; if neither exists, use existing default behavior.
    - In `_state` initialization, add a deque `prob_history` with TF-adaptive maxlen using the table `{"1m": 5, "5m": 5, "15m": 4, "1h": 3}` — store as a module-level constant `VELOCITY_WINDOW_BY_TF`.
    - In `_build_output()`:
      * Compute `hmm_regime_entropy` as Shannon entropy across the 3 alpha state probs using `-np.sum(p * np.log2(p + 1e-10))`.
      * Append current dominant-state prob to `_state["prob_history"]`; compute `hmm_regime_velocity = (prob_history[-1] - prob_history[0]) / max(1, len(prob_history) - 1)` when at least 2 samples exist, else 0.0.
      * Include both fields in the returned dict.
    - Add a public method `reload_parameters(self) -> None` that reruns `_load_parameters()` with the same TF-suffixed path logic and replaces `self._A`, `self._means`, `self._variances`. Used by SIGUSR1 handler in Task 3.

    Modify `src/intelligence/schemas.py`:
    - Locate `SMCContext` class (around lines 645–653 per research).
    - Add two new fields: `hmm_regime_entropy: float | None = None` and `hmm_regime_velocity: float | None = None`.
    - Keep field ordering grouped with existing `hmm_regime_*` fields.
    - Do NOT add `extra="forbid"` to SMCContext (it currently allows extras — preserve).
  </action>
  <verify>
    `.venv/bin/python -c "from src.intelligence.features.smc_context.hmm_regime import HMMRegimePlugin; p = HMMRegimePlugin(timeframe='5m', lookback=200); assert p.name == 'smc_HMMRegime_5m'; assert p.inputs[0].timeframe == '5m'; assert 'hmm_regime_entropy' in p.outputs; assert 'hmm_regime_velocity' in p.outputs; print('OK')"` exits 0 and prints OK.
    `.venv/bin/python -c "from src.intelligence.schemas import SMCContext; m = SMCContext(); assert hasattr(m, 'hmm_regime_entropy') and hasattr(m, 'hmm_regime_velocity'); print('OK')"` exits 0.
  </verify>
  <acceptance_criteria>
    - `HMMRegimePlugin.__init__` accepts `timeframe: str` and `lookback: int` kwargs
    - `HMMRegimePlugin(timeframe="5m", lookback=200).name == "smc_HMMRegime_5m"`
    - `HMMRegimePlugin(timeframe="5m", lookback=200).inputs[0].timeframe == "5m"`
    - `HMMRegimePlugin().outputs` frozenset contains both `"hmm_regime_entropy"` and `"hmm_regime_velocity"`
    - Module-level constant `VELOCITY_WINDOW_BY_TF` exists with keys `"1m"`, `"5m"`, `"15m"`, `"1h"` mapping to ints 5, 5, 4, 3
    - `HMMRegimePlugin.reload_parameters` is a callable method on the class
    - `SMCContext` model has attributes `hmm_regime_entropy` and `hmm_regime_velocity` both typed `float | None`
    - `.venv/bin/ruff check src/intelligence/features/smc_context/hmm_regime.py src/intelligence/schemas.py` exits 0
  </acceptance_criteria>
  <done>
    HMMRegimePlugin is multi-TF capable, emits entropy and velocity, schemas accept the new fields, and a public reload hook exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register four HMM instances in TIER_SMC with TF-appropriate lookbacks</name>
  <files>src/intelligence/register_plugins.py</files>
  <read_first>
    - src/intelligence/register_plugins.py
    - .planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-CONTEXT.md (D-02 lookback table)
  </read_first>
  <action>
    In `src/intelligence/register_plugins.py`:
    - Locate the existing single `hmm_plugin = HMMRegimePlugin()` instantiation and the `TIER_SMC` tuple/list (around line 503 per research).
    - Replace the single instance with four explicit instances:
      * `hmm_1m_plugin = HMMRegimePlugin(timeframe="1m", lookback=200)`
      * `hmm_5m_plugin = HMMRegimePlugin(timeframe="5m", lookback=200)`
      * `hmm_15m_plugin = HMMRegimePlugin(timeframe="15m", lookback=150)`
      * `hmm_1h_plugin = HMMRegimePlugin(timeframe="1h", lookback=100)`
    - Update `TIER_SMC` to include all four instances. Remove the old `hmm_plugin` reference.
    - Preserve all other plugins in `TIER_SMC` and import ordering. Keep tier placement in TIER_SMC (do NOT move to TIER_I4 — research recommendation is to minimize schema churn this phase).
    - If any other module imports `hmm_plugin` by that exact name, add a backward-compatible alias `hmm_plugin = hmm_1m_plugin` at module bottom and grep-confirm no other importers exist.
  </action>
  <verify>
    `.venv/bin/python -c "from src.intelligence.register_plugins import TIER_SMC; names = [p.name for p in TIER_SMC]; assert 'smc_HMMRegime_1m' in names; assert 'smc_HMMRegime_5m' in names; assert 'smc_HMMRegime_15m' in names; assert 'smc_HMMRegime_1h' in names; print('OK')"` exits 0.
    `grep -rn "hmm_plugin" src/ services/ tests/ --include="*.py"` shows only references compatible with the alias (no broken imports).
  </verify>
  <acceptance_criteria>
    - `register_plugins.py` defines all four names: `hmm_1m_plugin`, `hmm_5m_plugin`, `hmm_15m_plugin`, `hmm_1h_plugin`
    - Each has the exact lookback: 1m=200, 5m=200, 15m=150, 1h=100 (matching CONTEXT.md D-02)
    - `TIER_SMC` contains all four HMM instances by reference
    - `[p.name for p in TIER_SMC]` includes `smc_HMMRegime_1m`, `smc_HMMRegime_5m`, `smc_HMMRegime_15m`, `smc_HMMRegime_1h`
    - `.venv/bin/ruff check src/intelligence/register_plugins.py` exits 0
    - `.venv/bin/python -c "import src.intelligence.register_plugins"` exits 0 (module imports cleanly)
  </acceptance_criteria>
  <done>
    Four HMM instances are wired into TIER_SMC ready for pipeline dispatch.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add SIGUSR1 handler in intelligence_pipeline_agent to hot-reload HMM parameters</name>
  <files>services/intelligence_pipeline_agent.py, tests/unit/test_hmm_regime_multitf.py</files>
  <read_first>
    - services/intelligence_pipeline_agent.py
    - services/alpha_swarm_agent.py (SIGUSR1 handler lines 188–193)
    - src/intelligence/features/smc_context/hmm_regime.py (after Task 1)
    - src/intelligence/register_plugins.py (after Task 2)
  </read_first>
  <action>
    In `services/intelligence_pipeline_agent.py`:
    - Import `signal as _signal` (or use existing import).
    - In the agent's `start()` or `setup()` method (locate the asyncio event loop attachment), add `loop.add_signal_handler(_signal.SIGUSR1, self._on_hmm_sigusr1)` once the loop is available. Match the exact pattern used in `services/alpha_swarm_agent.py` lines 188–193.
    - Add method `_on_hmm_sigusr1(self) -> None` that iterates the four HMM instances (resolve via `register_plugins.TIER_SMC` filtered by `isinstance(p, HMMRegimePlugin)` or by name prefix `"smc_HMMRegime_"`) and calls `p.reload_parameters()` on each. Log the reload event via `structlog` with key `hmm_reload=True` and the list of TFs reloaded.
    - Wrap each `reload_parameters()` call in try/except logging failures per TF; do not crash the pipeline on a single failure.

    Create `tests/unit/test_hmm_regime_multitf.py`:
    - Test `test_hmm_regime_plugin_name_per_tf`: instantiate with each of the four TFs and assert names and `inputs[0].timeframe`/`inputs[0].lookback` match CONTEXT.md D-02 table.
    - Test `test_hmm_regime_outputs_include_entropy_and_velocity`: instantiate `HMMRegimePlugin(timeframe="5m", lookback=200)` and assert both new field names are in `.outputs` frozenset.
    - Test `test_hmm_regime_entropy_math`: drive `_build_output()` (or compute entropy directly) with `alpha = np.array([1/3, 1/3, 1/3])` and assert entropy approx equals `log2(3)` (1.585) within tolerance; with `alpha = np.array([1.0, 0.0, 0.0])` assert entropy approx 0.0.
    - Test `test_hmm_regime_velocity_window_by_tf`: import `VELOCITY_WINDOW_BY_TF` and assert keys map to 5, 5, 4, 3 for 1m/5m/15m/1h.
    - Test `test_hmm_regime_reload_parameters_idempotent`: call `reload_parameters()` twice on a fresh instance; assert no exception and `_A.shape[0] == _K`.
    - Test `test_register_plugins_exposes_four_hmm_instances`: import `TIER_SMC` and assert the four expected `smc_HMMRegime_*` names are present.
    - Mock filesystem reads where needed using `tmp_path` or `unittest.mock.patch`.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/test_hmm_regime_multitf.py -v` exits 0 with all tests passing.
    `grep -n "add_signal_handler.*SIGUSR1" services/intelligence_pipeline_agent.py` returns at least one line referencing `_on_hmm_sigusr1`.
  </verify>
  <acceptance_criteria>
    - `services/intelligence_pipeline_agent.py` contains the literal substring `add_signal_handler(_signal.SIGUSR1` or `add_signal_handler(signal.SIGUSR1`
    - `services/intelligence_pipeline_agent.py` defines method `_on_hmm_sigusr1`
    - `tests/unit/test_hmm_regime_multitf.py` exists and includes all six named tests listed in the action
    - `.venv/bin/pytest tests/unit/test_hmm_regime_multitf.py -v` exits 0
    - `.venv/bin/ruff check services/intelligence_pipeline_agent.py tests/unit/test_hmm_regime_multitf.py` exits 0
    - SIGUSR1 handler iterates HMM instances and calls `reload_parameters()` (verified by grep for `reload_parameters` in the handler block)
  </acceptance_criteria>
  <done>
    Intelligence pipeline can hot-reload all four HMM parameter sets via SIGUSR1, validated by unit tests covering parameterization, entropy/velocity math, and the reload contract.
  </done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/test_hmm_regime_multitf.py -v` exits 0.
- `.venv/bin/ruff check src/intelligence/features/smc_context/hmm_regime.py src/intelligence/register_plugins.py src/intelligence/schemas.py services/intelligence_pipeline_agent.py` exits 0.
- `.venv/bin/python -c "from src.intelligence.register_plugins import TIER_SMC; assert sum(1 for p in TIER_SMC if p.name.startswith('smc_HMMRegime_')) == 4"` exits 0.
</verification>

<success_criteria>
- Four HMM instances live in TIER_SMC with correct lookbacks.
- HMM output dict per bar includes `hmm_regime_entropy` and `hmm_regime_velocity`.
- SMCContext schema accepts the new fields without ValidationError.
- Pipeline reloads HMM params on SIGUSR1.
- Unit tests cover parameterization, entropy math, velocity window, and reload contract.
</success_criteria>

<output>
After completion, create `.planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-02-SUMMARY.md` summarizing parameterization changes, schema additions, tier placement decision (kept in TIER_SMC), SIGUSR1 wiring, and test outcomes.
</output>
