---
phase: 82-ml-intelligence-quality-qualitative-foundation
plan: 06
type: execute
wave: 4
depends_on: [82-02]
files_modified:
  - production/migrations/085_ctx_schema.sql
  - src/core/stream_keys.py
  - services/ctx_writer_agent.py
  - services/feature_writer_agent.py
  - services/service_auditor_agent.py
  - production/systemd/indicagent-ctx-writer.service
  - tests/unit/test_ctx_writer_agent.py
  - tests/unit/test_stream_keys_ctx.py
autonomous: true
requirements:
  - P82-CTX-SCHEMA
user_setup: []

must_haves:
  truths:
    - "ctx_events and ctx_snapshots tables exist with documented schema"
    - "intelligence_features has a ctx JSONB column (nullable)"
    - "topic_ctx_snapshot() is exported from src/core/stream_keys.py with env_prefix support"
    - "CtxWriterAgent consumes topic_ctx_snapshot() and persists to both ctx_events and ctx_snapshots"
    - "feature_writer_agent performs an as-of LEFT JOIN against ctx_snapshots at bar insert time and sets ctx column"
    - "indicagent-ctx-writer is registered in _DAG_ORDER at L6"
    - "Migration 085 is idempotent (IF NOT EXISTS)"
  artifacts:
    - path: "production/migrations/085_ctx_schema.sql"
      provides: "ctx_events + ctx_snapshots tables and intelligence_features.ctx column"
      contains: "ctx_events"
    - path: "src/core/stream_keys.py"
      provides: "topic_ctx_snapshot helper"
      contains: "topic_ctx_snapshot"
    - path: "services/ctx_writer_agent.py"
      provides: "CtxWriterAgent (BaseWriterAgent subclass)"
      exports: ["CtxWriterAgent"]
    - path: "services/feature_writer_agent.py"
      provides: "as-of join resolves ctx_snapshots into intelligence_features.ctx"
    - path: "services/service_auditor_agent.py"
      provides: "ctx-writer entry in _DAG_ORDER, _LAG_THRESHOLDS, _AGENT_ID_TO_UNIT"
  key_links:
    - from: "services/ctx_writer_agent.py"
      to: "topic_ctx_snapshot"
      via: "consumer subscription"
      pattern: "topic_ctx_snapshot"
    - from: "services/feature_writer_agent.py"
      to: "ctx_snapshots"
      via: "as-of LEFT JOIN on valid_from <= bar_ts < valid_to"
      pattern: "ctx_snapshots"
---

<objective>
Establish the CTX qualitative data infrastructure: migration 085 (ctx_events hypertable, ctx_snapshots table, intelligence_features.ctx column), `topic_ctx_snapshot()` stream key, `CtxWriterAgent` consuming that topic, `feature_writer_agent` as-of join resolving the active snapshot at bar insert time, and DAG registration.

Purpose: Phase 82 collects the substrate. Phase 83 will activate provider lanes and AIContext prompt rendering once shadow validation passes.
Output: New migration + stream key + writer agent + feature_writer integration + DAG entry + systemd unit + tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-CONTEXT.md
@.planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-RESEARCH.md
@.planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-PATTERNS.md
@src/core/stream_keys.py
@services/lifecycle_writer_agent.py
@services/feature_writer_agent.py
@services/service_auditor_agent.py
@production/migrations/084_ai_enrichment_tables.sql
</context>

<threat_model>
- Migration 085 MUST use `CREATE TABLE IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`, `ADD COLUMN IF NOT EXISTS` for safe replay.
- `ctx` JSONB accepts untrusted payloads from external providers. `CtxWriterAgent` must validate that `event_type` is in an allowlist `{"earnings","macro","news"}`, truncate `payload` JSON to a max byte size (e.g. 64 KiB), and reject any message lacking a valid `valid_from` timestamp.
- as-of join in feature_writer uses parameter binding only; no string interpolation.
- Index `ctx_snapshots (symbol, valid_from, valid_to)` is required to prevent as-of join from becoming a hot-path scan.
- No new external network calls.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Migration 085 — ctx_events hypertable, ctx_snapshots, intelligence_features.ctx column</name>
  <files>production/migrations/085_ctx_schema.sql</files>
  <read_first>
    - production/migrations/084_ai_enrichment_tables.sql
    - .planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-CONTEXT.md (D-06 schema section)
    - .planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-RESEARCH.md (P82-CTX-SCHEMA section)
  </read_first>
  <action>
    Create `production/migrations/085_ctx_schema.sql` with the following ordered statements:
    1. Header comment: migration 085, purpose (CTX foundation), dependencies (none), idempotency note.
    2. `CREATE TABLE IF NOT EXISTS ctx_events (event_ts TIMESTAMPTZ NOT NULL, symbol TEXT, event_type TEXT NOT NULL, source TEXT NOT NULL, payload JSONB NOT NULL);`
    3. `SELECT create_hypertable('ctx_events', 'event_ts', if_not_exists => TRUE);`
    4. `CREATE INDEX IF NOT EXISTS ctx_events_symbol_type_idx ON ctx_events (symbol, event_type, event_ts DESC);`
    5. `CREATE TABLE IF NOT EXISTS ctx_snapshots (symbol TEXT, event_type TEXT NOT NULL, valid_from TIMESTAMPTZ NOT NULL, valid_to TIMESTAMPTZ, ctx JSONB NOT NULL, PRIMARY KEY (symbol, event_type, valid_from));`
    6. `CREATE INDEX IF NOT EXISTS ctx_snapshots_lookup_idx ON ctx_snapshots (symbol, valid_from, valid_to);`
    7. `ALTER TABLE intelligence_features ADD COLUMN IF NOT EXISTS ctx JSONB;`

    Idempotency: every statement either uses `IF NOT EXISTS` or `if_not_exists => TRUE`.

    Do NOT execute the migration. Operational deployment handles it.
  </action>
  <verify>
    `test -f production/migrations/085_ctx_schema.sql && echo OK` prints OK.
    `grep -c "IF NOT EXISTS" production/migrations/085_ctx_schema.sql` returns at least 5.
    `grep -q "create_hypertable('ctx_events'" production/migrations/085_ctx_schema.sql && echo OK` prints OK.
  </verify>
  <acceptance_criteria>
    - File `production/migrations/085_ctx_schema.sql` exists
    - Contains `CREATE TABLE IF NOT EXISTS ctx_events`
    - Contains `CREATE TABLE IF NOT EXISTS ctx_snapshots`
    - Contains `ALTER TABLE intelligence_features ADD COLUMN IF NOT EXISTS ctx JSONB`
    - Contains `create_hypertable('ctx_events', 'event_ts'` (positional or keyword)
    - Contains `CREATE INDEX IF NOT EXISTS ctx_snapshots_lookup_idx ON ctx_snapshots (symbol, valid_from, valid_to)`
    - Header comment references migration 085 and CTX foundation
    - All DDL is idempotent
  </acceptance_criteria>
  <done>
    Migration is idempotent and creates the full CTX schema substrate.
  </done>
</task>

<task type="auto">
  <name>Task 2: topic_ctx_snapshot stream key + CtxWriterAgent + systemd unit + DAG registration</name>
  <files>src/core/stream_keys.py, services/ctx_writer_agent.py, services/service_auditor_agent.py, production/systemd/indicagent-ctx-writer.service, tests/unit/test_stream_keys_ctx.py</files>
  <read_first>
    - src/core/stream_keys.py
    - services/lifecycle_writer_agent.py (BaseWriterAgent pattern donor)
    - services/service_auditor_agent.py (existing _DAG_ORDER, _LAG_THRESHOLDS, _AGENT_ID_TO_UNIT)
    - production/systemd/indicagent-lifecycle-writer.service (existing donor unit; if name differs, use any L6 writer unit as donor)
  </read_first>
  <action>
    Edit `src/core/stream_keys.py`:
    - Add a function `def topic_ctx_snapshot(env_name: str) -> str` returning `f"{env_prefix(env_name)}ctx.snapshot"`. Use the same env-prefix helper used by sibling topic functions in the file.
    - Match style of existing `topic_*` functions (docstring + return signature).

    Create `services/ctx_writer_agent.py`:
    - Class `CtxWriterAgent(BaseWriterAgent)` mirroring `services/lifecycle_writer_agent.py` structure.
    - Class attributes: `BATCH_SIZE = 50`, `FLUSH_INTERVAL_SECS = 10.0`.
    - Subscribe to `topic_ctx_snapshot(env_name)` (resolve `env_name` from Settings).
    - Define an allowlist `_ALLOWED_EVENT_TYPES = frozenset({"earnings", "macro", "news"})` at module top.
    - Define `_MAX_PAYLOAD_BYTES = 64 * 1024` constant.
    - Async method `_process_message(self, msg: dict) -> None`:
      * Validate required keys: `event_ts`, `event_type`, `source`, `payload`. Drop with structlog warning if missing.
      * Reject if `event_type` not in allowlist; log and skip.
      * Reject if `len(json.dumps(payload))` exceeds `_MAX_PAYLOAD_BYTES`; log and skip.
      * Buffer an `INSERT INTO ctx_events` row (parameter-bound).
      * If `valid_from` and `ctx` are present in the message, buffer an `UPSERT` on `ctx_snapshots`: insert the new (symbol, event_type, valid_from) row and update the previous snapshot's `valid_to` to the new `valid_from` (same symbol, event_type, where `valid_to IS NULL`).
    - `_flush(self) -> None`: write both buffers in a single transaction; on failure log + clear (BaseWriterAgent pattern).
    - Pass dict (not `json.dumps`) to asyncpg jsonb params per CLAUDE.md rule.
    - `setup_service_logging("logs/ctx_writer_agent.log")` in entry function `main()`.
    - At module bottom add `if __name__ == "__main__": asyncio.run(main())` mirroring sibling writers.

    Create `production/systemd/indicagent-ctx-writer.service` modeled on the lifecycle-writer unit:
    - `Type=simple` (long-running consumer), `Restart=on-failure`, `RestartSec=5`, `User=indicagent`, `ExecStart=/opt/indicagent/.venv/bin/python -m services.ctx_writer_agent`, `Environment=PYTHONPATH=/opt/indicagent`, `[Install] WantedBy=multi-user.target`.

    Edit `services/service_auditor_agent.py`:
    - In `_DAG_ORDER`: add `"indicagent-ctx-writer"` to the L6 layer entry alongside other writers (find the existing L6 tuple/list and append).
    - In `_LAG_THRESHOLDS`: add a threshold entry for the ctx-writer consumer group (mirror the magnitude used by lifecycle/lineage writers — e.g. 100 messages or whatever sibling writers use).
    - In `_AGENT_ID_TO_UNIT`: add mapping `"ctx_writer_agent" -> "indicagent-ctx-writer.service"` (use exact key style of existing entries).

    Create `tests/unit/test_stream_keys_ctx.py`:
    - Test `test_topic_ctx_snapshot_dev`: `topic_ctx_snapshot("dev") == "dev.ctx.snapshot"`.
    - Test `test_topic_ctx_snapshot_prod`: assert the prod env name produces the prod-prefixed key (mirror existing topic tests in the suite for prefix format).
    - Test `test_topic_ctx_snapshot_uses_dots_not_colons`: assert `":" not in topic_ctx_snapshot("dev")` (CLAUDE.md rule).
  </action>
  <verify>
    `.venv/bin/python -c "from src.core.stream_keys import topic_ctx_snapshot; assert topic_ctx_snapshot('dev') == 'dev.ctx.snapshot'; print('OK')"` exits 0.
    `.venv/bin/python -c "from services.ctx_writer_agent import CtxWriterAgent; print('OK')"` exits 0.
    `grep -n "indicagent-ctx-writer" services/service_auditor_agent.py` returns at least 2 matches (DAG order + unit mapping).
    `.venv/bin/pytest tests/unit/test_stream_keys_ctx.py -v` exits 0.
  </verify>
  <acceptance_criteria>
    - `src/core/stream_keys.py` defines function `topic_ctx_snapshot(env_name: str) -> str`
    - `topic_ctx_snapshot("dev")` returns the literal string `"dev.ctx.snapshot"` (no colons)
    - `services/ctx_writer_agent.py` defines class `CtxWriterAgent` inheriting from `BaseWriterAgent`
    - File defines module-level constants `_ALLOWED_EVENT_TYPES` and `_MAX_PAYLOAD_BYTES`
    - File subscribes to `topic_ctx_snapshot(...)` (verified by grep for the symbol)
    - File contains `INSERT INTO ctx_events` (case insensitive) and `ctx_snapshots` table references
    - `services/service_auditor_agent.py` contains `"indicagent-ctx-writer"` in `_DAG_ORDER` and `_AGENT_ID_TO_UNIT`
    - `production/systemd/indicagent-ctx-writer.service` contains `ExecStart` referencing `services.ctx_writer_agent`
    - `tests/unit/test_stream_keys_ctx.py` includes all three named tests
    - `.venv/bin/pytest tests/unit/test_stream_keys_ctx.py -v` exits 0
    - `.venv/bin/ruff check src/core/stream_keys.py services/ctx_writer_agent.py services/service_auditor_agent.py tests/unit/test_stream_keys_ctx.py` exits 0
  </acceptance_criteria>
  <done>
    Topic helper, writer agent, systemd unit, and DAG registry entries are in place; topic naming is correct (dots, no colons).
  </done>
</task>

<task type="auto">
  <name>Task 3: feature_writer_agent as-of join + CtxWriterAgent unit tests</name>
  <files>services/feature_writer_agent.py, tests/unit/test_ctx_writer_agent.py</files>
  <read_first>
    - services/feature_writer_agent.py
    - services/ctx_writer_agent.py (after Task 2)
    - production/migrations/085_ctx_schema.sql (after Task 1)
    - .planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-RESEARCH.md (As-Of Join section)
  </read_first>
  <action>
    Edit `services/feature_writer_agent.py`:
    - Locate the existing INSERT statement that writes `intelligence_features` (around line 63 per research).
    - Extend the column list to include `ctx`. The new value is resolved via a correlated subquery in the same INSERT:
      `(SELECT jsonb_object_agg(event_type, ctx ORDER BY event_type) FROM ctx_snapshots WHERE (symbol = $<sym_param> OR symbol IS NULL) AND valid_from <= $<ts_param> AND (valid_to IS NULL OR valid_to > $<ts_param>))`
      Use the existing parameter binding indices; if the existing INSERT uses positional `$1..$N`, reuse the same `$sym` and `$ts` params.
    - If a NULL result is returned (no snapshots active), the JSONB column simply stores NULL — match the migration's nullable definition.
    - Do NOT add any new index here (the migration already created `ctx_snapshots_lookup_idx`).
    - Preserve existing batch behavior, batch sizing, and logging.

    Create `tests/unit/test_ctx_writer_agent.py`:
    - Use mocked DB manager and the existing test fixtures pattern for writer agents (mirror tests for lifecycle_writer_agent if present).
    - Test `test_inserts_ctx_event_on_valid_message`: send a message with all required keys; assert one INSERT call on `ctx_events`.
    - Test `test_upserts_ctx_snapshot_and_closes_prior_valid_to`: send a snapshot for (AAPL, earnings, valid_from=T1); send another at T2; assert the second UPSERT updates the prior row's `valid_to` to T2.
    - Test `test_rejects_disallowed_event_type`: send a message with `event_type="random"`; assert no DB call made and a warning logged.
    - Test `test_truncates_or_rejects_oversize_payload`: send a message whose JSON payload exceeds `_MAX_PAYLOAD_BYTES`; assert no DB call made and the agent logs the rejection.
    - Test `test_rejects_missing_required_keys`: send messages missing `event_ts`, `event_type`, or `payload`; assert each is rejected.
    - Test `test_passes_dict_to_asyncpg_jsonb_not_string`: capture the args passed to the mocked DB exec call; assert the JSONB-typed param is a dict (not `str`).
    - Test `test_feature_writer_insert_includes_ctx_column`: parse the INSERT SQL string from `feature_writer_agent.py` (load the module source) and assert the column list contains `ctx` and the SQL contains the substring `ctx_snapshots` (the as-of subquery). This is a static-content test, not a live DB test.
  </action>
  <verify>
    `.venv/bin/pytest tests/unit/test_ctx_writer_agent.py -v` exits 0.
    `grep -n "ctx_snapshots" services/feature_writer_agent.py` returns at least one match.
    `.venv/bin/ruff check services/feature_writer_agent.py tests/unit/test_ctx_writer_agent.py` exits 0.
  </verify>
  <acceptance_criteria>
    - `services/feature_writer_agent.py` INSERT statement includes a `ctx` column in its column list
    - `services/feature_writer_agent.py` contains the literal substring `ctx_snapshots` (referencing the as-of subquery)
    - `services/feature_writer_agent.py` uses parameter binding for both the symbol and timestamp in the as-of subquery (no f-string interpolation)
    - `tests/unit/test_ctx_writer_agent.py` exists and includes all seven named tests
    - `.venv/bin/pytest tests/unit/test_ctx_writer_agent.py -v` exits 0
    - `.venv/bin/ruff check services/feature_writer_agent.py tests/unit/test_ctx_writer_agent.py` exits 0
  </acceptance_criteria>
  <done>
    Feature writer resolves ctx as-of per bar, ctx writer validates and persists messages, and tests cover both the happy paths and the threat-model rejections.
  </done>
</task>

</tasks>

<verification>
- Migration 085 file is idempotent (5+ `IF NOT EXISTS` occurrences).
- `.venv/bin/pytest tests/unit/test_ctx_writer_agent.py tests/unit/test_stream_keys_ctx.py -v` exits 0.
- `.venv/bin/python -c "from src.core.stream_keys import topic_ctx_snapshot; from services.ctx_writer_agent import CtxWriterAgent; print('OK')"` exits 0.
- DAG order updated for ctx-writer at L6.
</verification>

<success_criteria>
- ctx_events + ctx_snapshots tables defined idempotently; intelligence_features has nullable ctx column.
- topic_ctx_snapshot exported and consumed by CtxWriterAgent.
- feature_writer as-of join resolves the active snapshot per bar without breaking existing behavior when no snapshots exist.
- CtxWriterAgent enforces event_type allowlist and payload size cap.
- DAG and systemd surfaces ready for installation.
</success_criteria>

<output>
After completion, create `.planning/phases/82-ml-intelligence-quality-qualitative-foundation/82-06-SUMMARY.md` summarizing migration 085, the topic naming, the writer agent's validation rules, the as-of join SQL, and the DAG L6 entry.
</output>
