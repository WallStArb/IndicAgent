---
phase: 081-signal-lifecycle-hardening
plan: 07
type: execute
wave: 4
depends_on: ["081-02", "081-03", "081-04", "081-05"]
files_modified:
  - tests/unit/services/test_signal_tracker_load_signal.py
  - tests/unit/services/test_signal_tracker_backfill_fast_path.py
  - tests/unit/services/test_intelligence_pipeline_publisher_normalization.py
  - tests/unit/services/test_signal_replay_auditor.py
  - tests/unit/services/test_bar_replay_provider.py
  - tests/unit/services/test_lifecycle_tracker_d02.py
  - tests/unit/services/test_signal_tracker_no_db_writes.py
autonomous: true
requirements:
  - P81-TESTS

must_haves:
  truths:
    - "All 16 unit tests from design spec Section 8 are implemented and pass under .venv/bin/pytest"
    - "test_compute_agent_no_db_writes verifies SignalTrackerComputeAgent has no INSERT/UPDATE/DELETE statements"
    - "test__load_signal_bootstrap_kafka_identical proves both paths produce byte-identical canonical dicts"
    - "test_replay_outcome_parametric covers all 8 outcome types × long/short (16 cases)"
  artifacts:
    - path: "tests/unit/services/test_signal_tracker_load_signal.py"
      provides: "Tests for _load_signal canonical, empty timestamp reject, bootstrap/kafka identity"
      contains: "test__load_signal"
    - path: "tests/unit/services/test_signal_tracker_backfill_fast_path.py"
      provides: "Tests for backfill TTL fast-path expired/carried-forward"
      contains: "test_backfill"
    - path: "tests/unit/services/test_intelligence_pipeline_publisher_normalization.py"
      provides: "Test publisher is_backfill computed correctly"
      contains: "test_publisher_is_backfill_computed"
    - path: "tests/unit/services/test_signal_replay_auditor.py"
      provides: "Replay auditor outcome parametric, both tracks independent, skips v0, skips TTL not elapsed, ohlcv gap counter"
      contains: "test_replay_outcome_parametric"
    - path: "tests/unit/services/test_bar_replay_provider.py"
      provides: "Bar replay topic routing, ordering, checkpoint resume"
      contains: "test_bar_replay"
    - path: "tests/unit/services/test_lifecycle_tracker_d02.py"
      provides: "D-02 violation counter test"
      contains: "test_d02_violation_counter"
    - path: "tests/unit/services/test_signal_tracker_no_db_writes.py"
      provides: "Static check that compute agent has no DB write statements"
      contains: "test_compute_agent_no_db_writes"
  key_links:
    - from: "All test files"
      to: "Modified Phase 81 source modules"
      via: "import + fixture"
      pattern: "from services\\."
---

<objective>
Implement all 16 unit tests from Section 8 of the design spec. These are the regression net that locks in the contract: publisher invariants, canonical loader, backfill fast-path, replay outcomes, bar replay routing, no-DB-writes assertion.

Purpose: Without these tests the contract erodes within weeks. The two north-star tests (`test_replay_outcome_parametric`, `test_all_signals_resolved`) prove the training set is correct.
Output: 7 test files, 16 tests, all green under `.venv/bin/pytest`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/081-signal-lifecycle-hardening/081-CONTEXT.md
@.planning/phases/081-signal-lifecycle-hardening/081-02-SUMMARY.md
@.planning/phases/081-signal-lifecycle-hardening/081-03-SUMMARY.md
@.planning/phases/081-signal-lifecycle-hardening/081-04-SUMMARY.md
@.planning/phases/081-signal-lifecycle-hardening/081-05-SUMMARY.md
@docs/plans/2026-05-08-signal-lifecycle-hardening-design.md
@services/signal_tracker_compute_agent.py
@services/intelligence_pipeline_agent.py
@services/signal_replay_auditor_agent.py
@services/bar_replay_provider_agent.py
@services/lifecycle_tracker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create _load_signal + lifecycle_tracker D-02 + no-DB-writes tests (5 tests)</name>
  <files>
    tests/unit/services/test_signal_tracker_load_signal.py
    tests/unit/services/test_lifecycle_tracker_d02.py
    tests/unit/services/test_signal_tracker_no_db_writes.py
  </files>
  <read_first>
    - services/signal_tracker_compute_agent.py (to mirror _load_signal contract exactly; understand `__new__` test pattern from CLAUDE.md)
    - services/lifecycle_tracker.py (D-02 counter location)
    - tests/unit/service_tests/ (existing patterns — find an existing service test that uses `ServiceClass.__new__(ServiceClass)`)
    - .planning/phases/081-signal-lifecycle-hardening/081-03-SUMMARY.md (canonical field list)
    - docs/plans/2026-05-08-signal-lifecycle-hardening-design.md (Section 8)
  </read_first>
  <action>
    **File `tests/unit/services/test_signal_tracker_load_signal.py`** — three tests:

    1. `test__load_signal_canonical`: instantiate `SignalTrackerComputeAgent` via `__new__` bypass, manually set `self._log = MagicMock()`. Pass a complete kafka-shape dict (with all required fields + extras) → assert returned dict has all 17 canonical fields with correct types (signal_id is str, timestamp is tz-aware UTC datetime, ttl_bars is int, is_backfill is bool, etc.).

    2. `test__load_signal_rejects_empty_timestamp`: pass dict with `timestamp=""` → assert returns `None` AND `SIGNAL_TRACKER_INVALID_SIGNAL_TOTAL.labels(reason="empty_timestamp")._value.get()` increased by 1. Repeat for `timestamp=None`. Use `prometheus_client.REGISTRY` reset between cases via `_value._value = 0` or capture before/after delta.

    3. `test__load_signal_bootstrap_kafka_identical`: build two raw inputs that differ only in source shape (kafka uses `tf` field, bootstrap uses `timeframe`; kafka uses string ISO timestamp, bootstrap returns datetime; etc. — but represent the same underlying signal). Pass both through `_load_signal` → assert `result_kafka == result_bootstrap` (byte-identical dict equality).

    **File `tests/unit/services/test_lifecycle_tracker_d02.py`** — one test:

    4. `test_d02_violation_counter`: instantiate the lifecycle tracker, capture the D-02 counter value, feed it a signal with `activated_at=datetime.now(UTC)` and `status="pending"` → assert counter incremented exactly 1, AND assert state was NOT mutated (status remained "pending"). This proves the workaround is gone but the assertion still fires.

    **File `tests/unit/services/test_signal_tracker_no_db_writes.py`** — one test:

    5. `test_compute_agent_no_db_writes`: read the source file with `pathlib.Path("services/signal_tracker_compute_agent.py").read_text()` and assert NONE of these substrings appear (case-insensitive regex against SQL keywords applied to signal_ledger):
        - `INSERT INTO signal_ledger`
        - `UPDATE signal_ledger`
        - `DELETE FROM signal_ledger`
        - `bulk_startup_expire`
        - `orphaned_pre_restart`
        - `activation_probability`
       Use `re.search(pattern, source, re.IGNORECASE)` per pattern; assert all return None.

    Use `@pytest.fixture` for the agent instance via `__new__`. Mock `_log` with `MagicMock()`. Import counter from `src.observability.metrics`.
  </action>
  <verify>
    `test -f tests/unit/services/test_signal_tracker_load_signal.py`
    `test -f tests/unit/services/test_lifecycle_tracker_d02.py`
    `test -f tests/unit/services/test_signal_tracker_no_db_writes.py`
    `.venv/bin/pytest tests/unit/services/test_signal_tracker_load_signal.py tests/unit/services/test_lifecycle_tracker_d02.py tests/unit/services/test_signal_tracker_no_db_writes.py -v` exits 0 with 5 passing tests
  </verify>
  <acceptance_criteria>
    - 5 test functions present with names: `test__load_signal_canonical`, `test__load_signal_rejects_empty_timestamp`, `test__load_signal_bootstrap_kafka_identical`, `test_d02_violation_counter`, `test_compute_agent_no_db_writes`
    - All 5 pass under `.venv/bin/pytest`
    - `test_compute_agent_no_db_writes` does static source-text scan (no runtime mocks needed)
  </acceptance_criteria>
  <done>Loader contract + D-02 + no-DB-writes locked in.</done>
</task>

<task type="auto">
  <name>Task 2: Create backfill fast-path + publisher normalization tests (3 tests)</name>
  <files>
    tests/unit/services/test_signal_tracker_backfill_fast_path.py
    tests/unit/services/test_intelligence_pipeline_publisher_normalization.py
  </files>
  <read_first>
    - services/signal_tracker_compute_agent.py (`_ingest_signal` decision tree)
    - services/intelligence_pipeline_agent.py (publisher normalization block)
    - .planning/phases/081-signal-lifecycle-hardening/081-02-SUMMARY.md
    - .planning/phases/081-signal-lifecycle-hardening/081-03-SUMMARY.md
  </read_first>
  <action>
    **File `tests/unit/services/test_signal_tracker_backfill_fast_path.py`** — two tests:

    1. `test_backfill_fast_path_expired`: instantiate tracker via `__new__`, mock `_publish_ttl_expired_transition` and `_add_to_active_index`. Build canonical signal with `is_backfill=True`, `ttl_bars=10`, `timeframe="1m"`, `timestamp=datetime.now(UTC) - timedelta(minutes=20)` (20 bars elapsed > 10 ttl). Call `_ingest_signal(canonical)`. Assert `_publish_ttl_expired_transition` called exactly 1, `_add_to_active_index` NOT called, `signal_id` added to `_signal_ids`, `SIGNAL_TRACKER_BACKFILL_FAST_PATH_TOTAL` incremented.

    2. `test_backfill_carried_forward`: same setup, but `timestamp=datetime.now(UTC) - timedelta(minutes=5)` (5 bars elapsed < 10 ttl). Assert `_add_to_active_index` called exactly 1, `_publish_ttl_expired_transition` NOT called, fast-path counter NOT incremented.

    **File `tests/unit/services/test_intelligence_pipeline_publisher_normalization.py`** — one test:

    3. `test_publisher_is_backfill_computed`: extract or import the normalization logic from `intelligence_pipeline_agent.py`. If it's inlined, refactor it into a small helper `_normalize_signal_payload(signals, bar_ts, computed_at, tf)` and call that helper from both the agent and this test. Test cases:
        - `bar_ts = computed_at = T`, `tf="1m"` → `is_backfill = False` for all signals, `timestamp == T`
        - `bar_ts = T`, `computed_at = T + 30 seconds`, `tf="1m"` (within tf_secs=60) → `is_backfill = False`
        - `bar_ts = T`, `computed_at = T + 90 seconds`, `tf="1m"` (>60s) → `is_backfill = True`
        - `bar_ts = T`, `computed_at = T + 2 hours`, `tf="1h"` (>3600s) → `is_backfill = True`
       Assert `ttl_bars` and `signal_schema_version` defaults applied via setdefault when missing.

    If extracting the helper is not feasible (the publish site has too many local-variable dependencies), instead invoke the agent's full publish path with mocked Kafka producer and inspect the `msg=` argument for the assertion. Choose the cleaner approach based on what you find when reading `intelligence_pipeline_agent.py`.
  </action>
  <verify>
    `test -f tests/unit/services/test_signal_tracker_backfill_fast_path.py`
    `test -f tests/unit/services/test_intelligence_pipeline_publisher_normalization.py`
    `.venv/bin/pytest tests/unit/services/test_signal_tracker_backfill_fast_path.py tests/unit/services/test_intelligence_pipeline_publisher_normalization.py -v` exits 0 with 3 passing tests
  </verify>
  <acceptance_criteria>
    - 3 test functions present with names: `test_backfill_fast_path_expired`, `test_backfill_carried_forward`, `test_publisher_is_backfill_computed`
    - All 3 pass
    - Backfill tests verify mock call counts AND counter increments AND _signal_ids membership
    - Publisher test covers all four time/tf combinations above
  </acceptance_criteria>
  <done>Fast-path and publisher contracts locked in.</done>
</task>

<task type="auto">
  <name>Task 3: Create signal_replay_auditor tests (5 tests)</name>
  <files>tests/unit/services/test_signal_replay_auditor.py</files>
  <read_first>
    - services/signal_replay_auditor_agent.py (full implementation)
    - services/lifecycle_tracker.py (evaluate_signal/evaluate_market_entry signatures and outcome enum values)
    - .planning/phases/081-signal-lifecycle-hardening/081-05-SUMMARY.md
  </read_first>
  <action>
    Create `tests/unit/services/test_signal_replay_auditor.py` with five test functions. Use `__new__` instantiation pattern; mock `_pool` (asyncpg) and `_producer` (KafkaProducerClient) using `AsyncMock`.

    1. `test_replay_outcome_parametric` — parametrize via `@pytest.mark.parametrize` over the 8 outcome values (`stopped_at_entry`, `stopped_in_trade`, `target_1`, `target_1_2`, `target_full`, `ttl_expired_ahead`, `ttl_expired_behind`, `never_activated`) × 2 directions (long/short) = 16 cases. For each, construct a synthetic OHLCV bar sequence engineered to produce that outcome (e.g. for `target_full` long: bars walk steadily up past all targets; for `stopped_in_trade` short: bars activate then reverse past stop). Call `_replay_signal` (or directly `evaluate_signal`) and assert the returned `LifecycleTransition` payload has `outcome == expected_outcome` and `pnl_r` matches the engineered value within tolerance ±0.01.

    2. `test_replay_both_tracks_independent`: zone never activates (price stays below entry zone for a long signal) but market entry was filled at a known price → assert zone-track outcome == "never_activated" AND market-entry track resolves with a non-zero pnl_r. Both events must be published.

    3. `test_replay_skips_v0_signals`: mock `_fetch_unresolved` query result to include rows where `signal_schema_version='v0'` would be returned IF the WHERE clause were wrong. Verify the actual SQL string in `_fetch_unresolved` contains `signal_schema_version = 'v1'` (static assertion via `inspect.getsource(SignalReplayAuditorAgent._fetch_unresolved)` regex match).

    4. `test_replay_skips_ttl_not_elapsed`: build a row with `timestamp=NOW() - 1 minute`, `ttl_bars=10`, `timeframe="1m"` (10 minutes window, only 1 minute elapsed). Call `_cycle` (or `_replay_signal` directly) and assert `_fetch_window_bars` was NOT called for this row, NO publish happened.

    5. `test_replay_ohlcv_gap_counter`: mock `_fetch_window_bars` to return `[]`. Call `_replay_signal` on a row past TTL. Assert `SIGNAL_REPLAY_OHLCV_GAP_TOTAL.labels(symbol=..., timeframe=...)._value.get()` incremented by 1, NO publish happened.

    For the parametric outcome test, you may need fixture helpers that generate OHLCV bar sequences for each outcome — write a helper `_bars_for_outcome(outcome: str, direction: int, signal: dict) -> list[dict]` at the top of the test file that returns the appropriate sequence.
  </action>
  <verify>
    `test -f tests/unit/services/test_signal_replay_auditor.py`
    `.venv/bin/pytest tests/unit/services/test_signal_replay_auditor.py -v` exits 0
    `.venv/bin/pytest tests/unit/services/test_signal_replay_auditor.py -v -k parametric --collect-only` shows 16 collected tests (8 outcomes × 2 directions)
  </verify>
  <acceptance_criteria>
    - 5 test functions present with names: `test_replay_outcome_parametric`, `test_replay_both_tracks_independent`, `test_replay_skips_v0_signals`, `test_replay_skips_ttl_not_elapsed`, `test_replay_ohlcv_gap_counter`
    - `test_replay_outcome_parametric` parametrized to 16 test instances (8 outcomes × 2 directions)
    - All tests pass
  </acceptance_criteria>
  <done>Replay auditor contract locked.</done>
</task>

<task type="auto">
  <name>Task 4: Create bar_replay_provider tests (3 tests)</name>
  <files>tests/unit/services/test_bar_replay_provider.py</files>
  <read_first>
    - services/bar_replay_provider_agent.py
    - .planning/phases/081-signal-lifecycle-hardening/081-04-SUMMARY.md
  </read_first>
  <action>
    Create `tests/unit/services/test_bar_replay_provider.py`:

    1. `test_bar_replay_topic_routing`: instantiate via `__new__`, mock `_producer` with AsyncMock. Build two rows: one with `timeframe="1m"`, one with `timeframe="15m"`. Call `_publish_bar` on each. Assert producer.publish was called with `topic == topic_market_bars(env)` for the 1m row and `topic == topic_market_bars_htf(env)` for the 15m row.

    2. `test_bar_replay_ordering`: assert by static introspection that the SQL in `_fetch_batch` contains `ORDER BY timestamp ASC` followed by a `CASE timeframe ... END ASC` clause that maps `1m→1, 5m→5, 15m→15, 1h→60, 4h→240, 1d→1440`. Use `inspect.getsource(BarReplayProviderAgent._fetch_batch)` and regex-match.

    3. `test_bar_replay_checkpoint_resume`: write a checkpoint file at a tmp path (monkeypatch `CHECKPOINT_PATH`), invoke `_load_checkpoint` and assert it returns the correct datetime. Then call `_save_checkpoint(new_ts)` and read the file back, assert content matches. Then construct a new agent instance and assert `_load_checkpoint` returns `new_ts`. This proves resumption.

    Use `tmp_path` pytest fixture and `monkeypatch.setattr("services.bar_replay_provider_agent.CHECKPOINT_PATH", tmp_path / "ckpt.json")`.
  </action>
  <verify>
    `test -f tests/unit/services/test_bar_replay_provider.py`
    `.venv/bin/pytest tests/unit/services/test_bar_replay_provider.py -v` exits 0 with 3 passing tests
  </verify>
  <acceptance_criteria>
    - 3 test functions: `test_bar_replay_topic_routing`, `test_bar_replay_ordering`, `test_bar_replay_checkpoint_resume`
    - Topic routing test calls publish and asserts on the topic argument
    - Ordering test is a static source assertion via `inspect.getsource`
    - Checkpoint test uses monkeypatched path and verifies persistence + reload
    - All pass
  </acceptance_criteria>
  <done>Bar replay contract locked.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/unit/services/test_signal_tracker_load_signal.py tests/unit/services/test_signal_tracker_backfill_fast_path.py tests/unit/services/test_intelligence_pipeline_publisher_normalization.py tests/unit/services/test_signal_replay_auditor.py tests/unit/services/test_bar_replay_provider.py tests/unit/services/test_lifecycle_tracker_d02.py tests/unit/services/test_signal_tracker_no_db_writes.py -v` — all 16 tests pass.
- Test count by `--collect-only` shows >= 16 tests across the seven files (parametric one expands to 16 instances; total collected >= 30).
</verification>

<success_criteria>
All 16 unit tests from design spec Section 8 implemented and passing.
</success_criteria>

<output>
Create `.planning/phases/081-signal-lifecycle-hardening/081-07-SUMMARY.md` with the full test inventory mapped to design spec Section 8 rows.
</output>
