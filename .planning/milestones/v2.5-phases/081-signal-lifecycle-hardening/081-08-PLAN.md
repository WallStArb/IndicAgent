---
phase: 081-signal-lifecycle-hardening
plan: 08
type: execute
wave: 4
depends_on: ["081-04", "081-05", "081-07"]
files_modified:
  - services/service_auditor_agent.py
  - tests/integration/test_lifecycle_writer_idempotency.py
  - tests/integration/test_is_backfill_roundtrip.py
  - tests/integration/test_all_signals_resolved.py
  - tests/integration/test_market_entry_completeness.py
autonomous: true
requirements:
  - P81-MIGRATION
  - P81-TESTS

must_haves:
  truths:
    - "Both new services bar_replay and signal_replay registered in _DAG_ORDER of service_auditor_agent.py"
    - "_LAG_THRESHOLDS and _AGENT_ID_TO_UNIT updated for the new services"
    - "All 4 integration tests from design spec Section 8 are implemented; gated by pytest marker so unit CI stays green"
    - "test_all_signals_resolved (north star) passes against a seeded local environment: COUNT(*) WHERE exit_at IS NULL = 0"
  artifacts:
    - path: "services/service_auditor_agent.py"
      provides: "DAG registration for bar_replay (L1) and signal_replay (L9)"
      contains: "bar_replay"
    - path: "tests/integration/test_lifecycle_writer_idempotency.py"
      provides: "Two-EXIT collision -> second is no-op"
      contains: "test_lifecycle_writer_idempotency_counter"
    - path: "tests/integration/test_is_backfill_roundtrip.py"
      provides: "Publisher -> DB -> ML filter exclusion roundtrip"
      contains: "test_is_backfill_roundtrip"
    - path: "tests/integration/test_all_signals_resolved.py"
      provides: "North star: known OHLCV -> replay -> all v1 signals resolved"
      contains: "test_all_signals_resolved"
    - path: "tests/integration/test_market_entry_completeness.py"
      provides: "Every signal with market_entry_price gets market_entry_outcome"
      contains: "test_market_entry_completeness"
  key_links:
    - from: "services/service_auditor_agent.py::_DAG_ORDER"
      to: "indicagent-bar-replay.service + indicagent-signal-replay.service"
      via: "dict entry with dag_order layer L1/L9"
      pattern: "indicagent-bar-replay\\|indicagent-signal-replay"
---

<objective>
Two final tasks that close the phase: register the new services in the DAG so the auditor monitors them (P81-MIGRATION DAG portion), and implement the four integration tests from Section 8 — including the north-star `test_all_signals_resolved` that proves the entire system regenerates a clean training set end-to-end (P81-TESTS).

Purpose: DAG registration is non-negotiable per CLAUDE.md ("Service registry = `_DAG_ORDER` ... single source of truth"). Integration tests are the last line of defense — unit tests verify components, integration verifies they compose correctly.
Output: Updated DAG + 4 integration tests gated by `@pytest.mark.integration`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/081-signal-lifecycle-hardening/081-CONTEXT.md
@.planning/phases/081-signal-lifecycle-hardening/081-04-SUMMARY.md
@.planning/phases/081-signal-lifecycle-hardening/081-05-SUMMARY.md
@docs/plans/2026-05-08-signal-lifecycle-hardening-design.md
@services/service_auditor_agent.py
@services/lifecycle_writer_agent.py
@services/signal_replay_auditor_agent.py
@services/bar_replay_provider_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register bar_replay (L1) and signal_replay (L9) in services/service_auditor_agent.py _DAG_ORDER</name>
  <files>services/service_auditor_agent.py</files>
  <read_first>
    - services/service_auditor_agent.py (entire file — find `_DAG_ORDER`, `_LAG_THRESHOLDS`, `_AGENT_ID_TO_UNIT` dicts)
    - .planning/phases/081-signal-lifecycle-hardening/081-CONTEXT.md (D-10)
  </read_first>
  <action>
    Update `services/service_auditor_agent.py`:

    1. Add to `_DAG_ORDER` dict — insert at the appropriate layer position (L1 alongside `indicagent-ibkr-provider`; L9 alongside `indicagent-signal-auditor`). Match the exact field structure used by neighboring entries (likely `ServiceSpec(unit, metrics_port, lag_threshold_messages, dag_order, market_hours_only)` per CLAUDE.md). Inspect the file for the actual constructor — DO NOT guess.

       ```python
       # L1
       "indicagent-bar-replay": ServiceSpec(
           unit="indicagent-bar-replay.service",
           metrics_port=<next_free_port>,        # match neighbor convention
           lag_threshold_messages=None,           # not a Kafka consumer
           dag_order=1,
           market_hours_only=False,               # one-shot, no schedule
       ),

       # L9
       "indicagent-signal-replay": ServiceSpec(
           unit="indicagent-signal-replay.service",
           metrics_port=<next_free_port>,
           lag_threshold_messages=None,           # not a Kafka consumer
           dag_order=9,
           market_hours_only=False,
       ),
       ```

       Pick `metrics_port` values that don't collide with existing entries — read the file and use the next free integer. Document the assigned ports in the SUMMARY.

    2. Update `_AGENT_ID_TO_UNIT` mapping (the dict mapping Prometheus `agent_id` label -> systemd unit name):
       ```python
       "bar_replay_provider": "indicagent-bar-replay.service",
       "signal_replay_auditor": "indicagent-signal-replay.service",
       ```
       The keys must EXACTLY match the `agent_id` class attribute in each new service module.

    3. `_LAG_THRESHOLDS` is for Kafka consumers only. Neither new service consumes Kafka (bar_replay produces; signal_replay reads DB). Do NOT add entries.

    4. Confirm the auditor's restart-priority logic uses `dag_order` numerically — by setting bar_replay=1 and signal_replay=9, restart order is correct without further changes.
  </action>
  <verify>
    `grep -c "indicagent-bar-replay" services/service_auditor_agent.py` >= 2 (DAG entry + AGENT_ID mapping)
    `grep -c "indicagent-signal-replay" services/service_auditor_agent.py` >= 2
    `grep -c "bar_replay_provider" services/service_auditor_agent.py` >= 1
    `grep -c "signal_replay_auditor" services/service_auditor_agent.py` >= 1
    `python -m py_compile services/service_auditor_agent.py` exits 0
    `python -c "from services.service_auditor_agent import _DAG_ORDER; assert 'indicagent-bar-replay' in _DAG_ORDER and 'indicagent-signal-replay' in _DAG_ORDER"` exits 0
  </verify>
  <acceptance_criteria>
    - `_DAG_ORDER` contains keys `"indicagent-bar-replay"` and `"indicagent-signal-replay"`
    - bar_replay entry has `dag_order=1`; signal_replay entry has `dag_order=9`
    - `_AGENT_ID_TO_UNIT` maps `"bar_replay_provider"` -> `"indicagent-bar-replay.service"` and `"signal_replay_auditor"` -> `"indicagent-signal-replay.service"`
    - Both services have `lag_threshold_messages=None` (not Kafka consumers)
    - Module compiles and imports clean
  </acceptance_criteria>
  <done>DAG monitoring extended to cover both new services.</done>
</task>

<task type="auto">
  <name>Task 2: Create 4 integration tests with @pytest.mark.integration gating</name>
  <files>
    tests/integration/test_lifecycle_writer_idempotency.py
    tests/integration/test_is_backfill_roundtrip.py
    tests/integration/test_all_signals_resolved.py
    tests/integration/test_market_entry_completeness.py
  </files>
  <read_first>
    - tests/integration/ (existing test file — find an example integration test that uses live TimescaleDB, identify the connection helper / fixture pattern; e.g., `pytest.fixture` that yields an asyncpg connection)
    - services/lifecycle_writer_agent.py (terminal UPDATE statement)
    - services/signal_replay_auditor_agent.py (`_cycle` entry point)
    - services/intelligence_pipeline_agent.py (publisher path — for is_backfill roundtrip)
    - .planning/phases/081-signal-lifecycle-hardening/081-CONTEXT.md
    - docs/plans/2026-05-08-signal-lifecycle-hardening-design.md (Section 8)
  </read_first>
  <!-- NOTE: market_entry_outcome column confirmed pre-existing in signal_ledger (text, nullable).
       Full market-entry track columns: market_entry_price, market_entry_exit_price, market_entry_pnl_r,
       market_entry_mae, market_entry_mfe, market_entry_bars_in_trade, market_entry_outcome, market_entry_gap_bars,
       market_entry_at, market_entry_exit_at. No migration needed. -->
  <action>
    Each integration test file starts with:
    ```python
    import pytest
    pytestmark = pytest.mark.integration
    ```
    so they only run when explicitly invoked (`.venv/bin/pytest -m integration tests/integration/`).

    **`tests/integration/test_lifecycle_writer_idempotency.py`** — `test_lifecycle_writer_idempotency_counter`:
    - Connect to live TimescaleDB (use existing fixture pattern from neighboring integration test).
    - INSERT a v1 signal_ledger row with `exit_at=NULL`.
    - Publish a `LifecycleTransition` EXIT event (or call the writer's update method directly with terminal fields).
    - Assert: row now has `exit_at IS NOT NULL`, all terminal fields set.
    - Capture `LIFECYCLE_WRITER_IDEMPOTENT_SKIP_TOTAL._value.get()` baseline.
    - Publish a SECOND EXIT event for the same `signal_id` with different terminal values.
    - Assert: row's `exit_at`, `outcome`, `pnl_r` UNCHANGED from first write.
    - Assert: counter incremented by exactly 1.
    - Cleanup: DELETE test row.

    **`tests/integration/test_is_backfill_roundtrip.py`** — `test_is_backfill_roundtrip`:
    - Publish a signal payload through the publisher path with `is_backfill=True` set (use `bar_ts` more than `tf_seconds` in the past so the publisher computes is_backfill=True).
    - Wait for downstream writer to persist (poll `signal_ledger` for the test signal_id with retry/backoff up to 30s).
    - Query `SELECT is_backfill FROM signal_ledger WHERE signal_id = $1` -> assert `True`.
    - Run the ML filter `SELECT COUNT(*) FROM signal_ledger WHERE signal_id = $1 AND is_backfill = FALSE` -> assert 0 (correctly excluded).
    - Cleanup.

    **`tests/integration/test_all_signals_resolved.py`** — `test_all_signals_resolved` (north star):
    - Seed N=20 v1 signals into `signal_ledger` with known `timestamp` (T - 1 hour), `ttl_bars=10`, `timeframe="1m"`, `exit_at=NULL`, varied direction/entry_price/stop_loss/targets.
    - Seed `market_data_ohlcv` bars covering each signal's [T, T+10min] window with engineered prices that produce known outcomes.
    - Instantiate `SignalReplayAuditorAgent`, call `_cycle()` once.
    - Wait briefly (asyncio.sleep + poll) for `LifecycleWriterAgent` to apply the published transitions.
    - Assert: `SELECT COUNT(*) FROM signal_ledger WHERE exit_at IS NULL AND signal_schema_version='v1' AND timestamp < NOW() - INTERVAL '2 minutes'` returns 0.
    - Cleanup all seeded rows.

    If running the live `LifecycleWriterAgent` is impractical from the test, alternative: subscribe to `topic_lifecycle_transitions` directly in the test, drain the publish events, and apply the UPDATE manually to assert correctness — the goal is to prove the replay path produces complete payloads, not to also test the writer (covered by `test_lifecycle_writer_idempotency_counter`).

    **`tests/integration/test_market_entry_completeness.py`** — `test_market_entry_completeness`:
    - Seed signals with `market_entry_price` set (i.e. signals that activated).
    - Seed OHLCV bars to drive market-entry track to a known outcome.
    - Run replay cycle.
    - Assert every seeded signal has `market_entry_outcome IS NOT NULL` after replay.
    - If `market_entry_outcome` is a separate column, query it; if it's part of a JSONB or sub-track in the same row, assert via the appropriate path.
    - Cleanup.

    For all four tests: use a unique signal_id prefix (e.g. `f"p81-test-{uuid4()}"`) to ensure no collision with live data, and ensure cleanup runs even on test failure (`try/finally` or fixture teardown).
  </action>
  <verify>
    `for f in tests/integration/test_lifecycle_writer_idempotency.py tests/integration/test_is_backfill_roundtrip.py tests/integration/test_all_signals_resolved.py tests/integration/test_market_entry_completeness.py; do test -f "$f" || exit 1; done`
    `grep -c "pytestmark = pytest.mark.integration" tests/integration/test_lifecycle_writer_idempotency.py tests/integration/test_is_backfill_roundtrip.py tests/integration/test_all_signals_resolved.py tests/integration/test_market_entry_completeness.py | grep -v ":0"` shows 4 files all having the marker
    `.venv/bin/pytest tests/integration/test_lifecycle_writer_idempotency.py tests/integration/test_is_backfill_roundtrip.py tests/integration/test_all_signals_resolved.py tests/integration/test_market_entry_completeness.py --collect-only` collects 4 tests
    `.venv/bin/pytest -m integration tests/integration/test_lifecycle_writer_idempotency.py tests/integration/test_is_backfill_roundtrip.py tests/integration/test_all_signals_resolved.py tests/integration/test_market_entry_completeness.py -v` exits 0 (when live infra is up; document in SUMMARY that this requires running TimescaleDB + Redpanda)
  </verify>
  <acceptance_criteria>
    - 4 integration test files exist with one test each: `test_lifecycle_writer_idempotency_counter`, `test_is_backfill_roundtrip`, `test_all_signals_resolved`, `test_market_entry_completeness`
    - All 4 files have `pytestmark = pytest.mark.integration` at module top
    - `test_all_signals_resolved` asserts the north-star invariant: zero unresolved v1 signals after replay
    - All tests clean up their seeded rows via try/finally or fixture teardown
    - Tests use unique signal_id prefix to avoid live-data collision
  </acceptance_criteria>
  <done>Phase test coverage complete: 16 unit + 4 integration = 20 tests total.</done>
</task>

</tasks>

<verification>
- `python -c "from services.service_auditor_agent import _DAG_ORDER, _AGENT_ID_TO_UNIT; assert 'indicagent-bar-replay' in _DAG_ORDER; assert 'indicagent-signal-replay' in _DAG_ORDER; assert _AGENT_ID_TO_UNIT.get('bar_replay_provider') == 'indicagent-bar-replay.service'; assert _AGENT_ID_TO_UNIT.get('signal_replay_auditor') == 'indicagent-signal-replay.service'"` succeeds.
- All 4 integration test files present, marked, and structurally valid.
</verification>

<success_criteria>
DAG fully extended; 4 integration tests implemented; the entire phase is now covered by tests with the north-star `test_all_signals_resolved` proving end-to-end correctness.
</success_criteria>

<output>
Create `.planning/phases/081-signal-lifecycle-hardening/081-08-SUMMARY.md` with:
- Final DAG entries (port assignments)
- Integration test inventory mapped to design Section 8
- Operational replay procedure (the bash sequence from CONTEXT.md `<specifics>`)
- Phase exit checklist (migration applied? both services started? north-star metric == 0?)
</output>
