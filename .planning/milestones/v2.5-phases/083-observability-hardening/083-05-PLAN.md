---
phase: 083-observability-hardening
plan: 05
type: execute
wave: 4
depends_on: ["083-04"]
files_modified:
  - production/migrations/088_dlq_events.sql
  - services/dlq_drain_agent.py
  - production/systemd/indicagent-dlq-drain.service
  - production/scripts/ensure_topics.sh
  - services/service_auditor_agent.py
  - src/core/stream_keys.py
  - services/shadow_auditor_agent.py
autonomous: true

must_haves:
  truths:
    - "dlq_events hypertable exists with 30-day retention"
    - "dlq_drain_agent subscribes to all 15 active DLQ topics and writes to dlq_events"
    - "Systemd unit indicagent-dlq-drain.service installed and starts the agent"
    - "ensure_topics.sh provisions all 15 DLQ topics with retention.ms=604800000 idempotently"
    - "stream_keys.py has zero topic_bar_audit_dlq, topic_cross_asset_dlq, topic_signal_audit_dlq, topic_shadow_transitions"
    - "shadow_auditor_agent does not publish to topic_shadow_transitions"
    - "6 orphaned Redpanda topics deleted (confirmed empty first)"
    - "dlq_drain_agent registered in _DAG_ORDER at L9"
  artifacts:
    - path: "production/migrations/088_dlq_events.sql"
      provides: "dlq_events table + hypertable + retention policy"
      contains: "create_hypertable"
    - path: "services/dlq_drain_agent.py"
      provides: "DLQ drain consumer"
      min_lines: 100
    - path: "production/systemd/indicagent-dlq-drain.service"
      provides: "systemd unit for dlq drain"
      contains: "ExecStart"
    - path: "production/scripts/ensure_topics.sh"
      provides: "Idempotent DLQ topic provisioning with 7-day retention"
      contains: "retention.ms=604800000"
  key_links:
    - from: "services/dlq_drain_agent.py"
      to: "dlq_events table"
      via: "asyncpg upsert on (agent, source_topic, routed_at)"
      pattern: "INSERT INTO dlq_events"
    - from: "services/service_auditor_agent.py"
      to: "dlq_drain_agent"
      via: "_DAG_ORDER, _LAG_THRESHOLDS, _AGENT_ID_TO_UNIT entries"
      pattern: "dlq_drain"
---

<objective>
Ship the DLQ history substrate: new `dlq_events` hypertable, new `dlq_drain_agent` (L9) consuming all 15 active DLQ topics, systemd unit, idempotent topic-provisioning script with 7-day retention, register in `_DAG_ORDER`. Delete 3 dead DLQ topic functions + `topic_shadow_transitions` from `stream_keys.py`, remove publish call from `shadow_auditor_agent`, delete 6 orphaned Redpanda topics (after confirming empty).

Purpose: DLQ messages become queryable history (was: write-only black hole). Dead topics/code gone.
Output: One consumer, one table, one source of DLQ truth.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/083-observability-hardening/083-CONTEXT.md
@.planning/phases/083-observability-hardening/083-RESEARCH.md
@docs/plans/2026-05-15-observability-hardening-design.md
@services/parity_auditor_agent.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 088_dlq_events.sql migration + apply</name>
  <files>production/migrations/088_dlq_events.sql</files>
  <read_first>
    - production/migrations/ (list to confirm 087_llm_calls_agent_attrs.sql is the latest)
    - docs/plans/2026-05-15-observability-hardening-design.md (Change 5 — table schema)
  </read_first>
  <action>
    Create `production/migrations/088_dlq_events.sql`:

    ```sql
    CREATE TABLE IF NOT EXISTS dlq_events (
        id            BIGSERIAL,
        routed_at     TIMESTAMPTZ NOT NULL,
        agent         TEXT NOT NULL,
        source_topic  TEXT NOT NULL,
        dlq_topic     TEXT NOT NULL,
        error_type    TEXT NOT NULL,
        error_message TEXT NOT NULL,
        payload       JSONB NOT NULL,
        retry_count   INT NOT NULL DEFAULT 0
    );
    SELECT create_hypertable('dlq_events', 'routed_at', if_not_exists => TRUE);
    SELECT add_retention_policy('dlq_events', INTERVAL '30 days', if_not_exists => TRUE);
    CREATE UNIQUE INDEX IF NOT EXISTS dlq_events_dedup_idx
        ON dlq_events (agent, source_topic, routed_at);
    ```

    Apply:
    `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/088_dlq_events.sql`
  </action>
  <verify>
    test -f production/migrations/088_dlq_events.sql
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d dlq_events"
    PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT * FROM _timescaledb_catalog.hypertable WHERE table_name='dlq_events'"
  </verify>
  <acceptance_criteria>
    - File exists at `production/migrations/088_dlq_events.sql`.
    - `\d dlq_events` shows all 9 columns with correct types.
    - hypertable query returns 1 row for `dlq_events`.
    - Unique index `dlq_events_dedup_idx` exists.
  </acceptance_criteria>
  <done>dlq_events table provisioned with retention + dedup index.</done>
</task>

<task type="auto">
  <name>Task 2: Create dlq_drain_agent.py + systemd unit</name>
  <files>
    services/dlq_drain_agent.py
    production/systemd/indicagent-dlq-drain.service
  </files>
  <read_first>
    - services/parity_auditor_agent.py (full file — reference structure)
    - production/systemd/indicagent-alerting-agent.service (unit pattern reference)
    - src/core/stream_keys.py (15 active DLQ topic functions)
    - src/intelligence/schemas.py (DLQPayload schema if present, else use base/agent.py definition)
  </read_first>
  <action>
    Create `services/dlq_drain_agent.py` (~150 lines) following the BaseAgent + KafkaConsumerClient pattern (this agent is a continuous consumer, not a timer):

    1. Subclass BaseAgent. `agent_id = "dlq_drain"`. `topics_consumed` lists all 15 active DLQ topics by calling `topic_*_dlq(env_name)` for each.
    2. `__init__`: set `self._pool = None`, `self._consumer = None`.
    3. `_setup()`: create asyncpg pool, subscribe consumer to all 15 topics with group `dlq_drain_consumer`.
    4. `_run()`: loop `await self._consumer.poll_one()` (or equivalent), parse message as `DLQPayload`. On parse failure: log raw payload + continue.
    5. For each parsed message: asyncpg `INSERT INTO dlq_events (routed_at, agent, source_topic, dlq_topic, error_type, error_message, payload, retry_count) VALUES ($1,...,$8) ON CONFLICT (agent, source_topic, routed_at) DO NOTHING`.
    6. Emit `structlog` log per event: `{event="dlq_drained", agent=..., source_topic=..., error_type=..., error_message=...}`.
    7. `stop()`: close consumer, close pool, super().stop().
    8. Standard `if __name__ == "__main__":` runner with `setup_service_logging("logs/dlq_drain_agent.log")`.

    The 15 DLQ topics (research findings):
    `topic_roll_dlq`, `topic_signal_dlq`, `topic_bar_aggregator_dlq`, `topic_bar_writer_dlq`, `topic_feature_writer_dlq`, `topic_signal_writer_dlq`, `topic_lifecycle_writer_dlq`, `topic_intelligence_pipeline_dlq`, `topic_signal_tracker_dlq`, `topic_llm_writer_dlq`, `topic_transform_graduation_dlq`, `topic_signal_lineage_dlq`, `topic_ml_orchestrator_dlq`, `topic_gap_fill_dlq`, `topic_health_events_dlq`.

    Create `production/systemd/indicagent-dlq-drain.service` using `indicagent-alerting-agent.service` as template:
    - `Description=IndicAgent DLQ Drain — consumes all DLQ topics, writes to dlq_events`
    - `ExecStart=/home/bg/dev/indicagent/.venv/bin/python services/dlq_drain_agent.py`
    - `SyslogIdentifier=indicagent-dlq-drain`
    - `After=network-online.target redpanda.service`
    - All other fields (User=bg, Restart=always, etc.) match the alerting-agent template.

    Install: `sudo cp production/systemd/indicagent-dlq-drain.service /etc/systemd/system/ && sudo systemctl daemon-reload`.
  </action>
  <verify>
    test -f services/dlq_drain_agent.py
    test -f production/systemd/indicagent-dlq-drain.service
    .venv/bin/python -c "import services.dlq_drain_agent; print('OK')"
    .venv/bin/ruff check services/dlq_drain_agent.py
    grep -c "topic_.*_dlq" services/dlq_drain_agent.py   # must be >= 15
    grep "INSERT INTO dlq_events" services/dlq_drain_agent.py
    grep "ON CONFLICT (agent, source_topic, routed_at)" services/dlq_drain_agent.py
  </verify>
  <acceptance_criteria>
    - `services/dlq_drain_agent.py` exists; line count >= 100.
    - `grep -cE "topic_[a-z_]+_dlq" services/dlq_drain_agent.py` returns >= 15.
    - `grep -F 'INSERT INTO dlq_events' services/dlq_drain_agent.py` matches.
    - `grep -F 'ON CONFLICT (agent, source_topic, routed_at)' services/dlq_drain_agent.py` matches.
    - Importable via Python.
    - ruff exits 0.
    - `production/systemd/indicagent-dlq-drain.service` exists with `ExecStart=` and `SyslogIdentifier=indicagent-dlq-drain`.
  </acceptance_criteria>
  <done>dlq_drain_agent ships as a continuous Kafka consumer writing to dlq_events.</done>
</task>

<task type="auto">
  <name>Task 3: Register dlq_drain in service_auditor _DAG_ORDER</name>
  <files>services/service_auditor_agent.py</files>
  <read_first>
    - services/service_auditor_agent.py (locate _DAG_ORDER, _LAG_THRESHOLDS, _AGENT_ID_TO_UNIT)
  </read_first>
  <action>
    In `services/service_auditor_agent.py`:

    1. Add `"dlq_drain"` to `_DAG_ORDER` at L9 (alongside `signal_auditor`, `parity_auditor`, `alerting_agent`).
    2. Add `dlq_drain` entry to `_LAG_THRESHOLDS` with sensible default (mirror `parity_auditor` or `signal_auditor`).
    3. Add `"dlq_drain": "indicagent-dlq-drain"` to `_AGENT_ID_TO_UNIT`.
  </action>
  <verify>
    grep -n "dlq_drain" services/service_auditor_agent.py   # must match >= 3 lines (DAG, lag, unit)
    grep "indicagent-dlq-drain" services/service_auditor_agent.py
    .venv/bin/pytest tests/unit/ -q
    .venv/bin/ruff check services/service_auditor_agent.py
  </verify>
  <acceptance_criteria>
    - `grep -c 'dlq_drain' services/service_auditor_agent.py` returns >= 3.
    - `grep -F 'indicagent-dlq-drain' services/service_auditor_agent.py` matches.
    - pytest + ruff exit 0.
  </acceptance_criteria>
  <done>service_auditor monitors dlq_drain.</done>
</task>

<task type="auto">
  <name>Task 4: ensure_topics.sh + delete dead topics + dead stream_keys</name>
  <files>
    production/scripts/ensure_topics.sh
    src/core/stream_keys.py
    services/shadow_auditor_agent.py
  </files>
  <read_first>
    - src/core/stream_keys.py (lines 176, 363, 369, 383 for dead funcs + shadow transitions)
    - services/shadow_auditor_agent.py (line 27 import of topic_shadow_transitions; find publish call)
  </read_first>
  <action>
    Step A — Create `production/scripts/ensure_topics.sh`:
    ```bash
    #!/usr/bin/env bash
    set -euo pipefail
    DLQ_TOPICS=(
      market.events.roll.dlq
      intelligence.signal.dlq
      bar.aggregator.dlq
      bar.writer.dlq
      feature.writer.dlq
      intelligence.signal.writer.dlq
      lifecycle.writer.dlq
      intelligence.pipeline.dlq
      signal.tracker.dlq
      llm.writer.dlq
      intelligence.transform.graduation.dlq
      intelligence.signal_lineage.dlq
      ml.orchestrator.dlq
      gap_fill.dlq
      intelligence.service_auditor.journal.dlq
    )
    for topic in "${DLQ_TOPICS[@]}"; do
      docker exec redpanda rpk topic create "$topic" --replicas 1 -c retention.ms=604800000 2>/dev/null || true
      docker exec redpanda rpk topic alter-config "$topic" --set retention.ms=604800000
    done
    ```
    Make executable: `chmod +x production/scripts/ensure_topics.sh`. Run it.

    Step B — Confirm 6 orphaned topics empty, then delete:
    ```
    for t in intelligence.shadow.transitions intelligence.signal.audit market.data.quality ml.data_quality.alerts pipeline.data_quality system.health.events; do
        docker exec redpanda rpk topic describe "$t" 2>/dev/null   # check zero messages
        docker exec redpanda rpk topic delete "$t" 2>/dev/null || true
    done
    ```

    Step C — `src/core/stream_keys.py` cleanup:
    - Delete function `topic_bar_audit_dlq` (line ~363).
    - Delete function `topic_signal_audit_dlq` (line ~369).
    - Delete function `topic_cross_asset_dlq` (line ~383).
    - Delete function `topic_shadow_transitions` (line ~176).

    Step D — `services/shadow_auditor_agent.py`:
    - Remove import `from src.core.stream_keys import topic_shadow_transitions` (line ~27).
    - Find the `.publish(topic_shadow_transitions(...), ...)` call (likely inside `_check_promotion` / `_check_demotion`) and delete the publish statement only. KEEP the DB write (`shadow_transition_log INSERT` line ~171) and KEEP `_check_promotion` / `_check_demotion` logic.
    - If `ShadowTransitionEvent` import becomes unused, remove it too.
    - If the `producer` parameter to `_run_audit` is now unused, leave the parameter (it may be used for other publishes — verify with grep `\.publish\(` within the file).
  </action>
  <verify>
    test -x production/scripts/ensure_topics.sh
    docker exec redpanda rpk topic list | grep -E "intelligence\.shadow\.transitions|intelligence\.signal\.audit|market\.data\.quality|ml\.data_quality\.alerts|pipeline\.data_quality|system\.health\.events"   # must be empty
    docker exec redpanda rpk topic describe bar.aggregator.dlq | grep retention.ms   # 604800000
    grep -nE "topic_bar_audit_dlq|topic_signal_audit_dlq|topic_cross_asset_dlq|topic_shadow_transitions" src/core/stream_keys.py   # must be empty
    grep -n "topic_shadow_transitions" services/shadow_auditor_agent.py   # must be empty
    .venv/bin/pytest tests/unit/ -q
    .venv/bin/ruff check src/core/stream_keys.py services/shadow_auditor_agent.py
  </verify>
  <acceptance_criteria>
    - `production/scripts/ensure_topics.sh` is executable.
    - `rpk topic list | grep -E "shadow\.transitions|signal\.audit|data\.quality"` returns empty.
    - `rpk topic describe bar.aggregator.dlq` shows `retention.ms=604800000`.
    - `grep -E 'topic_bar_audit_dlq|topic_signal_audit_dlq|topic_cross_asset_dlq|topic_shadow_transitions' src/core/stream_keys.py` returns zero.
    - `grep 'topic_shadow_transitions' services/shadow_auditor_agent.py` returns zero.
    - pytest + ruff exit 0.
  </acceptance_criteria>
  <done>Dead topics gone from Redpanda + code; DLQ topics provisioned with 7-day retention.</done>
</task>

</tasks>

<verification>
.venv/bin/pytest tests/unit/ -q
.venv/bin/ruff check src/ services/
# Manual: publish test DLQPayload to any DLQ topic, verify row appears in dlq_events within 10s
PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM dlq_events"
</verification>

<success_criteria>
- dlq_events table + retention policy
- dlq_drain_agent shipping; systemd unit installed
- service_auditor registers dlq_drain
- 7-day retention on all DLQ topics
- 6 orphan topics + 4 dead stream_keys functions removed
</success_criteria>

<output>
After completion, create `.planning/phases/083-observability-hardening/083-05-SUMMARY.md`.
</output>
