---
phase: 083-observability-hardening
plan: 04
type: execute
wave: 3
depends_on: ["083-03"]
files_modified:
  - src/core/agent/base.py
  - services/bar_aggregator_agent.py
  - services/graduation_compute_agent.py
  - services/llm_writer_service.py
autonomous: true

must_haves:
  truths:
    - "BaseAgent has _get_producer() method returning kafka_producer or _producer or None"
    - "BaseAgent._send_to_dlq uses _get_producer() — no more dual hasattr branches"
    - "bar_aggregator_agent has no _dlq_producer field and no inline DLQ produce()"
    - "graduation_compute_agent uses inherited _send_to_dlq, no inline publish to topic_transform_graduation_dlq"
    - "llm_writer_service has no _send_to_dlq override and no _dlq_producer field"
    - "DLQ_DEPTH metric is removed from base.py and llm_writer_service.py imports/usage"
  artifacts:
    - path: "src/core/agent/base.py"
      provides: "_get_producer() method on BaseAgent; refactored _send_to_dlq"
      contains: "def _get_producer"
  key_links:
    - from: "services/bar_aggregator_agent.py"
      to: "src/core/agent/base.py"
      via: "inherited _send_to_dlq + _dlq_topic() override"
      pattern: "def _dlq_topic"
    - from: "services/llm_writer_service.py"
      to: "src/core/agent/base.py"
      via: "inherited _send_to_dlq (override removed)"
      pattern: "_send_to_dlq"
---

<objective>
Add `BaseAgent._get_producer()`. Consolidate three inline DLQ implementations (`bar_aggregator_agent`, `graduation_compute_agent`, `llm_writer_service`) to use the inherited `BaseAgent._send_to_dlq()` plus `_dlq_topic()` override. Remove `DLQ_DEPTH` import/usage from `base.py` and `llm_writer_service.py`.

Purpose: One DLQ routing implementation, not four. Future DLQ topics inherit consolidation for free.
Output: BaseAgent owns producer selection; subclasses only declare their DLQ topic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/083-observability-hardening/083-CONTEXT.md
@.planning/phases/083-observability-hardening/083-RESEARCH.md
@docs/plans/2026-05-15-observability-hardening-design.md
@src/core/agent/base.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BaseAgent._get_producer() and refactor _send_to_dlq</name>
  <files>src/core/agent/base.py</files>
  <read_first>
    - src/core/agent/base.py (full file; _send_to_dlq is lines 339-416; DLQ_DEPTH import at line ~349; .inc() calls at lines 379 and 394)
  </read_first>
  <action>
    In `src/core/agent/base.py`:

    1. Add method to `BaseAgent`:
       ```python
       def _get_producer(self):
           if hasattr(self, "_kafka_producer") and self._kafka_producer is not None:
               return self._kafka_producer
           if hasattr(self, "_producer") and self._producer is not None:
               return self._producer
           return None
       ```
    2. Refactor `_send_to_dlq`: replace the two-branch `hasattr` lookup with one call to `self._get_producer()`. If it returns None, log warning and return early (preserve current behavior).
    3. Remove `DLQ_DEPTH` from the `from src.observability.metrics import ...` line.
    4. Remove the two `DLQ_DEPTH.inc()` / `DLQ_DEPTH.labels(...).inc()` calls inside `_send_to_dlq`.
    5. Keep `DLQ_MESSAGES_TOTAL.add(1, {...})` call (canonical counter).
    6. Do NOT change the method signature `_send_to_dlq(self, msg, error)`.
  </action>
  <verify>
    grep -n "def _get_producer" src/core/agent/base.py
    grep -n "DLQ_DEPTH" src/core/agent/base.py    # must be empty
    grep -n "DLQ_MESSAGES_TOTAL" src/core/agent/base.py    # must match
    .venv/bin/pytest tests/unit/ -q
    .venv/bin/ruff check src/core/agent/base.py
  </verify>
  <acceptance_criteria>
    - `grep -F 'def _get_producer' src/core/agent/base.py` matches.
    - `grep -F 'DLQ_DEPTH' src/core/agent/base.py` returns zero.
    - `grep -F 'DLQ_MESSAGES_TOTAL' src/core/agent/base.py` matches at least once.
    - `_send_to_dlq` body contains exactly one `self._get_producer()` call.
    - pytest + ruff exit 0.
  </acceptance_criteria>
  <done>BaseAgent has single producer selector; DLQ_DEPTH eliminated from base.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate bar_aggregator_agent DLQ</name>
  <files>services/bar_aggregator_agent.py</files>
  <read_first>
    - services/bar_aggregator_agent.py (full file; especially lines 123-124, 224-229, 254, 261, 396-397, 467-471 per research)
    - src/core/agent/base.py (post Task-1, to see inherited _send_to_dlq + _dlq_topic contract)
    - src/core/stream_keys.py (locate topic_bar_aggregator_dlq function)
  </read_first>
  <action>
    In `services/bar_aggregator_agent.py`:

    1. Resolve the naming conflict: the instance attribute `self._dlq_topic: str = ""` (line ~124) shadows the BaseAgent `_dlq_topic()` method. DELETE the `self._dlq_topic = ""` instance attribute line and its initialization in `__init__`. DELETE every reference to `self._dlq_topic` as an attribute (not as method call).
    2. Delete the `self._dlq_producer: KafkaProducerClient | None = None` field (line ~123) and all references.
    3. Delete the dedicated DLQ producer start in `_setup()` (lines ~224-229) and stop in stop logic (lines ~254, 261, 396-397).
    4. Delete the inline `.produce()` DLQ call (lines ~467-471). Replace with `await self._send_to_dlq(<msg>, <exc>)` matching the BaseAgent signature.
    5. Add a method override on the class:
       ```python
       def _dlq_topic(self) -> str | None:
           return topic_bar_aggregator_dlq(self.env_name)
       ```
       (Ensure `topic_bar_aggregator_dlq` is imported from `src.core.stream_keys`.)
  </action>
  <verify>
    grep -n "_dlq_producer" services/bar_aggregator_agent.py   # must be empty
    grep -n "self._dlq_topic =" services/bar_aggregator_agent.py   # must be empty (no attribute assignment)
    grep -n "def _dlq_topic" services/bar_aggregator_agent.py   # must match
    grep -n "_send_to_dlq" services/bar_aggregator_agent.py   # must match
    .venv/bin/pytest tests/unit/ -q
    .venv/bin/ruff check services/bar_aggregator_agent.py
  </verify>
  <acceptance_criteria>
    - `grep '_dlq_producer' services/bar_aggregator_agent.py` returns zero.
    - `grep -E 'self\._dlq_topic\s*=' services/bar_aggregator_agent.py` returns zero (no attribute assignment).
    - `grep -E 'def _dlq_topic' services/bar_aggregator_agent.py` matches.
    - `grep 'topic_bar_aggregator_dlq' services/bar_aggregator_agent.py` matches.
    - `grep 'self._send_to_dlq' services/bar_aggregator_agent.py` matches.
    - pytest + ruff exit 0.
  </acceptance_criteria>
  <done>bar_aggregator_agent uses inherited DLQ routing; attribute/method conflict resolved.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate graduation_compute_agent and llm_writer_service DLQ</name>
  <files>
    services/graduation_compute_agent.py
    services/llm_writer_service.py
  </files>
  <read_first>
    - services/graduation_compute_agent.py (lines 285-310 for inline DLQ pattern)
    - services/llm_writer_service.py (lines 40, 416, 497-499, 565-567, 617-620, 687, 705 per research)
    - src/core/agent/base.py (post Task-1)
  </read_first>
  <action>
    `services/graduation_compute_agent.py`:
    1. Delete the inline DLQ block (lines ~291-308): dict construction + `self._producer.publish(topic_transform_graduation_dlq(...), ...)`.
    2. Replace with `await self._send_to_dlq(payload, exc)` using the original payload and exception.
    3. Add method override:
       ```python
       def _dlq_topic(self) -> str | None:
           return topic_transform_graduation_dlq(self.env_name)
       ```
       Ensure `topic_transform_graduation_dlq` is imported.

    `services/llm_writer_service.py`:
    4. Delete `_send_to_dlq` override method entirely. The inherited `BaseAgent._send_to_dlq(self, msg, error)` takes over.
    5. Delete `self._dlq_producer: KafkaProducerClient | None = None` field (line ~416) and its start (lines ~565-567) and stop (lines ~617-620).
    6. KEEP the existing `_dlq_topic()` method override at lines ~497-499 (already correct pattern). Verify it returns `topic_llm_writer_dlq(self.env_name)`.
    7. Remove `DLQ_DEPTH` from the import line (~40). Keep `DLQ_MESSAGES_TOTAL` if used.
    8. Update call sites that called `self._send_to_dlq(payload, source_topic, error_type)` (3-arg) — change to `self._send_to_dlq(payload, exc)` (2-arg matching BaseAgent). Verify each call site has access to an exception object; if only an error string is available, wrap as `RuntimeError(error_type_str)` before passing.
    9. Verify `topics_consumed` property is set correctly on the class (BaseAgent uses `self.topics_consumed[0]` as source_topic in its DLQ payload). If not present or wrong, add/fix the property to list the agent's actual subscription topics.
  </action>
  <verify>
    grep -n "_dlq_producer" services/graduation_compute_agent.py services/llm_writer_service.py   # must be empty
    grep -n "def _send_to_dlq" services/llm_writer_service.py   # must be empty (override removed)
    grep -n "def _dlq_topic" services/graduation_compute_agent.py services/llm_writer_service.py   # both must match
    grep -n "DLQ_DEPTH" services/llm_writer_service.py   # must be empty
    grep -n "topic_transform_graduation_dlq" services/graduation_compute_agent.py
    .venv/bin/pytest tests/unit/ -q
    .venv/bin/ruff check services/graduation_compute_agent.py services/llm_writer_service.py
  </verify>
  <acceptance_criteria>
    - `grep '_dlq_producer' services/graduation_compute_agent.py services/llm_writer_service.py` returns zero.
    - `grep -E 'def _send_to_dlq' services/llm_writer_service.py` returns zero matches (override deleted).
    - `grep -E 'def _dlq_topic' services/graduation_compute_agent.py` matches.
    - `grep -E 'def _dlq_topic' services/llm_writer_service.py` matches.
    - `grep 'DLQ_DEPTH' services/llm_writer_service.py` returns zero.
    - `grep 'self._send_to_dlq' services/graduation_compute_agent.py services/llm_writer_service.py` matches >=1 in each.
    - pytest + ruff exit 0.
  </acceptance_criteria>
  <done>All three inline DLQ implementations consolidated to inherited BaseAgent path.</done>
</task>

</tasks>

<verification>
.venv/bin/pytest tests/unit/ -q
.venv/bin/ruff check src/ services/
</verification>

<success_criteria>
- _get_producer() owned by BaseAgent
- DLQ_DEPTH gone from base.py and llm_writer
- Three inline DLQ impls consolidated
</success_criteria>

<output>
After completion, create `.planning/phases/083-observability-hardening/083-04-SUMMARY.md`.
</output>
