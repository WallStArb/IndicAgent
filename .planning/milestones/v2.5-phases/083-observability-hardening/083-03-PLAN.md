---
phase: 083-observability-hardening
plan: 03
type: execute
wave: 2
depends_on: ["083-01"]
files_modified:
  - src/observability/metrics.py
  - src/observability/otel.py
  - src/core/plugin_circuit_breaker.py
  - services/signal_metrics_compute_agent.py
  - services/signal_metrics_writer_agent.py
  - services/service_auditor_agent.py
autonomous: true

must_haves:
  truths:
    - "metrics.py has a single module-level _meter and zero prometheus_client imports"
    - "All call sites use OTel .add()/.record() — zero remaining .labels(...).inc()/.set()/.observe() patterns"
    - "Dead metrics PLUGIN_EXECUTION_TOTAL, PLUGIN_EXECUTION_TIME, FEATURES_TIER_LATENCY_SECONDS, DLQ_DEPTH are gone"
    - "Wrapper classes OTelCounter/OTelGauge/OTelHistogram and _safe_* helpers are gone"
    - "SERVICE_UP_GAUGE defined in metrics.py; service_auditor_agent imports it as a constant"
    - "init_tracing calls removed from signal_metrics_compute_agent and signal_metrics_writer_agent"
  artifacts:
    - path: "src/observability/metrics.py"
      provides: "All 62 OTel SDK instruments via single _meter"
      contains: "_meter = otel_metrics.get_meter"
    - path: "src/core/plugin_circuit_breaker.py"
      provides: "Direct PLUGIN_DURATION_MS.record() calls"
      contains: "PLUGIN_DURATION_MS.record"
  key_links:
    - from: "src/observability/metrics.py"
      to: "opentelemetry.metrics"
      via: "single _meter from get_meter('indicagent')"
      pattern: "_meter = otel_metrics\\.get_meter"
    - from: "services/service_auditor_agent.py"
      to: "src/observability/metrics.py"
      via: "SERVICE_UP_GAUGE import"
      pattern: "from src.observability.metrics import.*SERVICE_UP_GAUGE"
---

<objective>
Replace all prometheus_client and OTel wrapper-class metrics in `metrics.py` with direct OTel SDK instruments via a single shared `_meter`. Delete dead metrics, wrapper classes, `_safe_*` helpers, `record_plugin_execution()`. Migrate all call sites (across services/ and src/) from `.labels().inc()/.set()/.observe()` to `.add()/.record()` with label dicts. Move `SERVICE_UP_GAUGE` into `metrics.py`. Remove stray `init_tracing` calls.

Purpose: Single metrics system, no dual-registration bugs, exemplar support.
Output: One canonical OTel-SDK metrics module; clean call sites everywhere.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/083-observability-hardening/083-CONTEXT.md
@.planning/phases/083-observability-hardening/083-RESEARCH.md
@docs/plans/2026-05-15-observability-hardening-design.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite metrics.py on single OTel _meter; move SERVICE_UP_GAUGE in</name>
  <files>
    src/observability/metrics.py
    src/observability/otel.py
  </files>
  <read_first>
    - src/observability/metrics.py (full file — 858 lines, 66 metrics)
    - src/observability/otel.py (locate OTLPMetricExporter + PeriodicExportingMetricReader setup)
    - docs/plans/2026-05-15-observability-hardening-design.md (Change 1)
    - services/service_auditor_agent.py (lines 35, 152-156: SERVICE_UP_GAUGE definition)
  </read_first>
  <action>
    Rewrite `src/observability/metrics.py`:

    1. Remove imports: `from prometheus_client import Counter, Gauge, Histogram`, `from prometheus_client import REGISTRY as _REGISTRY`.
    2. Add: `from opentelemetry import metrics as otel_metrics` (likely already present).
    3. Define exactly ONE module-level `_meter = otel_metrics.get_meter("indicagent")`.
    4. Delete wrapper classes entirely: `OTelCounter`, `_OTelLabeledCounter`, `OTelGauge`, `_OTelLabeledGauge`, `OTelHistogram`, `_OTelLabeledHistogram`.
    5. Delete helpers entirely: `_safe_counter`, `_safe_gauge`, `_safe_histogram`, `_counter_helpers`, `_gauge_helpers`, `_SWARM_BUCKETS` (inline buckets where used — note OTel histograms don't accept explicit buckets at create-time, so just drop the bucket spec).
    6. Delete dead metrics + helper:
       - `PLUGIN_EXECUTION_TOTAL`
       - `PLUGIN_EXECUTION_TIME`
       - `FEATURES_TIER_LATENCY_SECONDS`
       - `DLQ_DEPTH`
       - `record_plugin_execution()` function
    7. For every remaining metric (Counter/Gauge/Histogram), replace definition with one of:
       - `NAME = _meter.create_counter("metric_name", description="...")`
       - `NAME = _meter.create_up_down_counter("metric_name", description="...")`  (for all Gauges)
       - `NAME = _meter.create_histogram("metric_name", description="...", unit="...")`
    8. Update helper factory functions:
       - `def counter(name, documentation): return _meter.create_counter(name, description=documentation)`
       - `def gauge(name, documentation): return _meter.create_up_down_counter(name, description=documentation)`
    9. Add named constant `SERVICE_UP_GAUGE = _meter.create_up_down_counter("indicagent_service_up", description="Service-up gauge keyed by systemd unit")` (replaces the inline OTelGauge in service_auditor_agent).
    10. Preserve all metric names exactly (including `_total` suffixes) to keep Grafana dashboards working.

    Then in `src/observability/otel.py`:
    11. **Do NOT remove MeterProvider, OTLPMetricExporter, or PeriodicExportingMetricReader from otel.py.** These are the global provider infrastructure — `_meter` in metrics.py relies on `metrics.set_meter_provider(...)` being called here. Leave otel.py's metric-reader setup entirely untouched. The only otel.py change in this phase is `service.instance.id` (already done in Plan 01).
  </action>
  <verify>
    grep -c "_meter = otel_metrics.get_meter" src/observability/metrics.py   # must be 1
    grep -E "from prometheus_client" src/observability/metrics.py            # must be empty
    grep -E "OTelCounter|OTelGauge|OTelHistogram|_safe_counter|_safe_gauge|_safe_histogram" src/observability/metrics.py   # must be empty
    grep -E "PLUGIN_EXECUTION_TOTAL|PLUGIN_EXECUTION_TIME|FEATURES_TIER_LATENCY_SECONDS|DLQ_DEPTH|record_plugin_execution" src/observability/metrics.py   # must be empty
    grep "SERVICE_UP_GAUGE" src/observability/metrics.py                     # must match
    .venv/bin/python -c "from src.observability.metrics import SERVICE_UP_GAUGE; print('OK')"
    .venv/bin/ruff check src/observability/
  </verify>
  <acceptance_criteria>
    - Exactly one `_meter = otel_metrics.get_meter("indicagent")` line in metrics.py.
    - Zero `from prometheus_client` lines in metrics.py.
    - Zero occurrences of `OTelCounter`, `OTelGauge`, `OTelHistogram`, `_OTelLabeled*`, `_safe_counter`, `_safe_gauge`, `_safe_histogram`, `_counter_helpers`, `_gauge_helpers`, `_SWARM_BUCKETS` in metrics.py.
    - Zero occurrences of `PLUGIN_EXECUTION_TOTAL`, `PLUGIN_EXECUTION_TIME`, `FEATURES_TIER_LATENCY_SECONDS`, `DLQ_DEPTH`, `record_plugin_execution` anywhere in metrics.py.
    - `SERVICE_UP_GAUGE` exported from metrics.py and importable.
    - `ruff check src/observability/` exits 0.
  </acceptance_criteria>
  <done>metrics.py uses single _meter, all OTel SDK; SERVICE_UP_GAUGE relocated.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all call sites from prometheus_client API to OTel API</name>
  <files>
    src/ (multiple)
    services/ (multiple)
    src/core/plugin_circuit_breaker.py
  </files>
  <read_first>
    - src/core/plugin_circuit_breaker.py (lines 328, 413 — record_plugin_execution call sites)
    - Output of: `grep -rln "\.labels(" src/ services/`
    - src/observability/metrics.py (post-Task-1 state, to know label semantics)
  </read_first>
  <action>
    Migrate every call site of every metric from prometheus_client API to OTel API:

    1. Enumerate sites: `grep -rn "\.labels(" src/ services/ | grep -v "/migrations/" | grep -v "/__pycache__/"`
    2. For each match, transform per table:
       - `METRIC.labels(k=v, ...).inc()` → `METRIC.add(1, {"k": v, ...})`
       - `METRIC.labels(k=v, ...).inc(n)` → `METRIC.add(n, {"k": v, ...})`
       - `METRIC.labels(k=v, ...).set(x)` → `METRIC.add(x, {"k": v, ...})`
       - `METRIC.labels(k=v, ...).observe(x)` → `METRIC.record(x, {"k": v, ...})`
    3. For metrics without labels: `METRIC.inc()` → `METRIC.add(1)`, `METRIC.set(x)` → `METRIC.add(x)`, `METRIC.observe(x)` → `METRIC.record(x)`.
    4. `src/core/plugin_circuit_breaker.py`:
       - Remove import of `record_plugin_execution` (line ~43).
       - Add import of `PLUGIN_DURATION_MS` from `src.observability.metrics`.
       - Replace `record_plugin_execution(...)` calls at lines ~328 and ~413 with:
         `PLUGIN_DURATION_MS.record(execution_time_ms, {"plugin_name": plugin_name, "tier": intelligence_tier})`
       - Drop the `symbol`/`timeframe` args; `PLUGIN_DURATION_MS` does not use them.
    5. `services/service_auditor_agent.py`:
       - Line ~35: change import to `from src.observability.metrics import SERVICE_AUDITOR_SERVICE_RESTARTS_TOTAL, SERVICE_UP_GAUGE` (remove `OTelGauge`).
       - Delete lines ~152-156: the inline `SERVICE_UP_GAUGE = OTelGauge(...)` construction.
       - Line ~322: change `SERVICE_UP_GAUGE.labels(unit=unit).set(1 if has_metrics else 0)` to `SERVICE_UP_GAUGE.add(1 if has_metrics else 0, {"unit": unit})`.
    6. `services/signal_metrics_compute_agent.py` and `services/signal_metrics_writer_agent.py`:
       - Remove `from src.observability.otel import init_tracing` import.
       - Remove `init_tracing("signal-metrics-compute")` / `init_tracing("signal-metrics-writer")` call.
       - Migrate any `.labels()` calls in these files per the table above.
    7. Run `.venv/bin/pytest tests/unit/ -q` after migration; fix any breakage caused by API mismatch.
  </action>
  <verify>
    grep -rn "\.labels(" src/ services/ | grep -v __pycache__       # must be empty
    grep -rn "record_plugin_execution" src/ services/               # must be empty
    grep -rn "init_tracing" services/signal_metrics_compute_agent.py services/signal_metrics_writer_agent.py   # must be empty
    grep -rn "OTelGauge" services/                                  # must be empty
    grep "PLUGIN_DURATION_MS.record" src/core/plugin_circuit_breaker.py
    grep "SERVICE_UP_GAUGE.add" services/service_auditor_agent.py
    .venv/bin/pytest tests/unit/ -q
    .venv/bin/ruff check src/ services/
  </verify>
  <acceptance_criteria>
    - `grep -rn "\.labels(" src/ services/` returns zero non-pycache matches.
    - `grep -rn "record_plugin_execution" src/ services/` returns zero matches.
    - `grep -rn "init_tracing" services/signal_metrics_*.py` returns zero matches.
    - `grep -rn "OTelGauge\|OTelCounter\|OTelHistogram" src/ services/` returns zero matches.
    - `grep -F 'PLUGIN_DURATION_MS.record(' src/core/plugin_circuit_breaker.py` matches at least twice.
    - `grep -F 'SERVICE_UP_GAUGE.add(' services/service_auditor_agent.py` matches.
    - `grep -F 'from src.observability.metrics import' services/service_auditor_agent.py` line contains `SERVICE_UP_GAUGE`.
    - `pytest tests/unit/ -q` exits 0.
    - `ruff check src/ services/` exits 0.
  </acceptance_criteria>
  <done>Single OTel API across the codebase; dead metrics and init_tracing fully removed.</done>
</task>

</tasks>

<verification>
.venv/bin/pytest tests/unit/ -q
.venv/bin/ruff check src/ services/
# Manual after deploy: curl localhost:8889/metrics | grep plugin_fallbacks_total   (returns >=1 line)
</verification>

<success_criteria>
- Single _meter in metrics.py, zero prometheus_client usage
- All call sites use OTel API
- Dead metrics gone; SERVICE_UP_GAUGE moved
- pytest + ruff green
</success_criteria>

<output>
After completion, create `.planning/phases/083-observability-hardening/083-03-SUMMARY.md`.
</output>
