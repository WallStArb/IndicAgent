---
status: complete
phase: 083-observability-hardening
source: 083-01-SUMMARY.md, 083-02-SUMMARY.md, 083-03-SUMMARY.md, 083-04-SUMMARY.md, 083-05-SUMMARY.md, 083-06-SUMMARY.md
started: 2026-05-15T23:58:00Z
updated: 2026-05-16T01:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. API /metrics endpoint redirects to OTel collector
expected: The /metrics endpoint on :8000 no longer serves prometheus-format metrics. It returns a 404 with a message pointing to :8889/metrics (the OTel Collector).
result: pass

### 2. OTel metrics visible at collector
expected: `curl -s http://localhost:8889/metrics | grep indicagent` returns metric lines (e.g. indicagent_bar_processed_total, indicagent_intelligence_pipeline_latency). Indicagent-named metrics are present at the OTel Collector scrape endpoint.
result: issue
reported: "No indicagent-named metrics appear at :8889. Only OTel SDK internal telemetry from a test script appeared. The OTLP pipeline itself works (test metric confirmed delivered), but running services emit nothing."
severity: major

### 3. DLQ drain service registered and running
expected: `systemctl status indicagent-dlq-drain` shows the service as active (running). The service appears in `systemctl list-units --all | grep indicagent`.
result: issue
reported: "Service is loaded but inactive (dead). Also has a config error: StartLimitIntervalSec in [Service] section — systemd ignores it there (should be in [Unit])."
severity: major

### 4. dlq_events table exists and is queryable
expected: `PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "SELECT COUNT(*) FROM dlq_events;"` returns a count (0 is fine — table exists). No "table does not exist" error.
result: pass

### 5. Dead Redpanda topics removed
expected: `docker exec redpanda rpk topic list | grep -E "intelligence.shadow.transitions|intelligence.signal.audit|market.data.quality|ml.data_quality.alerts|pipeline.data_quality|system.health.events"` returns empty — none of the 6 orphaned topics appear.
result: issue
reported: "3 of 6 orphaned topics still exist: intelligence.signal.audit, market.data.quality, system.health.events."
severity: minor

### 6. Five new alert rules in alertmanager config
expected: `grep -c "alert:" production/alertmanager-rules.yml` shows 5 more alerts than before. Specifically `grep "LLMCircuitBreakerOpen\|SwarmCapacitySkipRateHigh\|ShadowPluginEVDecayed\|PipelineP95LatencyRegression\|DLQActivity" production/alertmanager-rules.yml` matches all 5 names.
result: pass

### 7. prometheus-client fully removed
expected: `grep prometheus-client requirements.txt` returns empty (not in requirements). `grep -r "from prometheus_client\|import prometheus_client" src/ services/ tests/` returns empty — no remaining imports.
result: pass

### 8. Unit tests green
expected: `.venv/bin/pytest tests/unit/ -q` completes with 3248+ passed and 0 failures (1 pre-existing skip is fine). No new test failures introduced by this phase.
result: pass

## Summary

total: 8
passed: 5
issues: 3
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "indicagent-named metrics appear at :8889/metrics on the OTel Collector"
  status: failed
  reason: "User reported: No indicagent-named metrics appear at :8889. Only OTel SDK internal telemetry from a test script appeared. The OTLP pipeline itself works (test metric confirmed delivered), but running services emit nothing."
  severity: major
  test: 2
  root_cause: "otel.py:43 checks `__class__.__name__ == 'ProxyMeterProvider'` but the actual default class name is `_ProxyMeterProvider` (leading underscore). The condition is always False so init_otel_providers() never sets the real MeterProvider. All metrics go to the no-op proxy and are silently dropped."
  artifacts:
    - path: "src/observability/otel.py"
      issue: "Line 43: class name guard uses 'ProxyMeterProvider' but SDK returns '_ProxyMeterProvider'"
  missing:
    - "Fix guard to use isinstance check or include '_ProxyMeterProvider' in the condition"

- truth: "indicagent-dlq-drain service is active (running)"
  status: failed
  reason: "User reported: Service is loaded but inactive (dead). Also has a config error: StartLimitIntervalSec in [Service] section — systemd ignores it there (should be in [Unit])."
  severity: major
  test: 3
  root_cause: "Service was never started (disabled/dead). Also has a mis-placed StartLimitIntervalSec key in [Service] section instead of [Unit], causing systemd warnings."
  artifacts:
    - path: "/etc/systemd/system/indicagent-dlq-drain.service"
      issue: "StartLimitIntervalSec in [Service] section, should be in [Unit]"
  missing:
    - "Start and enable the service: sudo systemctl enable --now indicagent-dlq-drain"
    - "Move StartLimitIntervalSec to [Unit] section in service file"

- truth: "All 6 orphaned Redpanda topics removed (intelligence.shadow.transitions, intelligence.signal.audit, market.data.quality, ml.data_quality.alerts, pipeline.data_quality, system.health.events)"
  status: failed
  reason: "User reported: 3 of 6 orphaned topics still exist: intelligence.signal.audit, market.data.quality, system.health.events."
  severity: minor
  test: 5
  root_cause: "Topic deletion was incomplete — 3 of 6 topics were not deleted."
  artifacts: []
  missing:
    - "Delete remaining 3 topics: docker exec redpanda rpk topic delete intelligence.signal.audit market.data.quality system.health.events"
