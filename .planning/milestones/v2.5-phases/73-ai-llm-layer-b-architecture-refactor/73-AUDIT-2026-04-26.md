# Phase 73 Plan Audit — 2026-04-26

**Audited:** 2026-04-26
**Finding:** ⚠️ **PHASE 73 PLANS REQUIRE MAJOR REVISION** — Tech stack and agent infrastructure have evolved significantly since planning

## Executive Summary

Phase 73 ("AI LLM Layer B+ Architecture Refactor") was planned on 2026-04-26, but it assumes an architecture from Phase 64's era. **Since Phase 64 was planned, we've shipped:**

- ✅ Phase 67-68: Observability Hardening (2026-04-23)
- ✅ Phase 69: Writer Agent Renaissance Refactor (2026-04-23)
- ✅ Phase 71: BaseAgent Infrastructure Alignment (2026-04-14)
- ✅ Phase 72: Signal Transform Log (2026-04-25)
- 🔄 Phase 74: BarNormalizerAgent (currently in progress)

**These phases introduced fundamental changes to agent infrastructure that Phase 73 does not account for.**

## Critical Misalignments

### 1. BaseAgent vs BaseAIAgent (NEW from Phase 71)

**Phase 73 assumes:**
- Create `src/core/ai/base_agent.py` with `BaseAIAgent` ABC
- New `IAIAgent` Protocol
- New `SafeAgentWrapper` for timeout + exception isolation

**Current reality (from Phase 71):**
- `src/core/agent/base.py` has **Renaissance-standard `BaseAgent`** with:
  - SIGTERM/SIGINT drain via asyncio event
  - Structured logging via structlog
  - Consumer lag reporting
  - OTel tracer support
  - Prometheus metrics auto-start
  - `_setup()`/`_teardown()` lifecycle hooks
  - `running` property, `start/stop/run` contract

- `src/core/swarm/base_agent.py` has **`SwarmBaseAgent`** extending `BaseAgent` with:
  - Automatic shadow recording via `ShadowRecorder`
  - Per-agent asyncio timeout (`latency_budget_ms`)
  - DLQ publish on exceptions
  - OTel span wrapping `compute()`

**Issue:** Phase 73's `BaseAIAgent` design overlaps significantly with existing `BaseAgent` + `SwarmBaseAgent`. The plans don't specify whether to:
- Extend `SwarmBaseAgent` → `BaseAIAgent` (inheritance)
- Absorb `BaseAgent` into `BaseAIAgent` (replacement)
- Create parallel hierarchy (duplication risk)

### 2. AgentResult vs AgentOutput Schema (from Phase 72)

**Phase 73 assumes:**
- Migrate from `AgentResult` to `AgentOutput` schema
- `AgentOutput.payload` is untyped dict
- `SwarmAggregator.aggregate()` updated in same wave

**Current reality (from Phase 72):**
- Phase 72 shipped **`signal_transform_log`** hypertable
- Schema: `agent_id`, `signal_id`, `symbol`, `timeframe`, `ts`, `output_type`, `payload` (JSONB), `shadow_only`, `latency_ms`
- This schema assumes `AgentOutput` is already the target

**Issue:** Phase 73-08 plan mentions "AgentResult → AgentOutput migration" but Phase 72 already created the `signal_transform_log` table. Which schema is it using? Are we doing a second migration?

### 3. Group Service Architecture

**Phase 73 assumes:**
- Create `BaseGroupService` shared dispatcher
- Properties: `agents`, `trigger_topics`, `output_topic`
- Methods: `_setup`, `_run`, `_handle_trigger`, `_graduation_loop`, `_teardown`

**Current reality:**
- `services/swarm_dispatch_service.py` has `SwarmDispatchComputeAgent`
- Extends `SwarmBaseAgent`
- Has `SwarmAggregator` for aggregation
- Subscribes to `topic_intelligence_i7_signals`
- Publishes to `topic_swarm_alpha` (aggregate) + `topic_swarm_results` (per-agent)

**Issue:** Phase 73 doesn't specify whether `BaseGroupService` should:
- Replace `SwarmDispatchComputeAgent` entirely (rewrite)
- Extract shared infrastructure from it (refactor)
- Coexist as a parallel pattern (confusion)

### 4. Swarm Orchestration Layer

**Phase 73 assumes:**
- Delete `services/swarm_orchestrator_agent.py` (defect D-01)
- Delete `/etc/systemd/system/indicagent-swarm-orchestrator.service` (defect D-02)

**Current reality:**
```bash
$ systemctl list-units | grep swarm-orchestrator
indicagent-swarm-orchestrator.service   loaded active running
```

**Issue:** The orchestrator is **still deployed and running**. Phase 73 assumes it's dead code. We need to verify:
- Is it actually doing anything?
- Did Phase 67-68 make it redundant?
- Or did we misunderstand its purpose?

### 5. Context Management

**Phase 73 assumes:**
- Absorb `src/intelligence/swarm/context.py` → `src/core/ai/context.py`
- Generalize `SwarmContext` → `AIContext`
- `AIContextCache.build(symbol, tf, tiers_needed)` with tier filtering

**Current reality:**
- `src/intelligence/swarm/context.py` has `SwarmContext` + `SwarmContextCache`
- Uses `IntelligenceEvent` as the source
- Has TTL-based expiry (300s default)
- `seed_from_db_row()` for startup seeding

**Issue:** Phase 73 doesn't address:
- What happens to `SwarmContextCache.seed_from_db_row()` pattern?
- Do we keep dual caches during migration?
- How do we handle hot-swapping contexts in running services?

### 6. Import Boundary Discipline

**Phase 73 assumes:**
- `src/core/ai/` and `src/intelligence/ai/` import only from `src/intelligence/schemas.py` and `src/core/stream_keys.py`
- Never import from `src/intelligence/pipeline/` or tier plugins

**Current reality:**
- `src/intelligence/narrative/` imports from `src/intelligence/pipeline/` (needs verification)
- `SwarmBaseAgent` imports from `src/intelligence/schemas.py` ✅
- Phase 72's `TransformRecorder` threads through pipeline

**Issue:** We need to audit existing import boundaries before enforcing new ones. Phase 73 assumes we know the current state — we might not.

## What Phase 73 Got Right

✅ **Shadow mode discipline** — `shadow_only=True` by default is correct and aligns with Renaissance principles

✅ **Graduation loop auto-promotion** — Spearman correlation gates (ρ ≥ 0.15, n ≥ 30, p < 0.05) are scientifically grounded

✅ **LLM chain fixes (D-03 through D-08)** — Cache key collision, rate limiter, guardrails, auto-audit, real tokens are all real defects that need fixing

✅ **Narrative TF gate** — Preventing 1m bar narrative calls is a critical cost control

✅ **Import boundary discipline** — AI layer should not depend on pipeline/plugins for testability

## Recommended Actions

### Option A: Re-plan Phase 73 from First Principles (RECOMMENDED)

1. **Suspend Phase 73 execution** — Don't start 73-01 through 73-09 until we reconcile with reality

2. **Create Phase 73-Audit sub-phase** (similar to Phase 65 gradient audit):
   - Task 1: Audit current agent infrastructure hierarchy
     - Document `BaseAgent` → `SwarmBaseAgent` relationship
     - Map all extending classes
     - Identify shadow recording mechanisms
   - Task 2: Audit context management
     - Document `SwarmContext` vs `AIContext` overlap
     - Map context caches and their TTL policies
     - Verify `seed_from_db_row()` pattern usage
   - Task 3: Audit swarm orchestration layer
     - Verify if `swarm_orchestrator_agent` is actually dead
     - Check what `swarm_dispatch_service` actually does
     - Map all swarm-related systemd units and their dependencies
   - Task 4: Audit LLM chain defects
     - Verify D-03 through D-08 still exist in current codebase
     - Check if any were fixed incidentally in Phases 67-72
     - Test cache key collision (D-08) with current implementation

3. **Create revised Phase 73 plans** based on audit findings:
   - Decide: extend `SwarmBaseAgent` vs replace with `BaseAIAgent`
   - Decide: refactor `SwarmDispatchComputeAgent` vs create `BaseGroupService`
   - Decide: migrate `AgentResult` → `AgentOutput` vs use existing schema
   - Decide: keep `swarm_orchestrator` vs delete (with proof it's unused)

4. **Update Phase 73-AI-SPEC.md** with:
   - Current `BaseAgent` lifecycle from Phase 71
   - Current `SwarmBaseAgent` shadow recording from Phase 56
   - Current `signal_transform_log` schema from Phase 72
   - Current context management patterns
   - Revised architectural decisions based on audit

### Option B: Execute Phase 73 As-Planned (HIGH RISK)

⚠️ **Not recommended** — High probability of:
- Breaking Phase 71's BaseAgent alignments
- Duplicating `SwarmBaseAgent` functionality in `BaseAIAgent`
- Creating conflicting context management systems
- Accidentally deleting active `swarm_orchestrator` service
- Schema migration conflicts with Phase 72's `signal_transform_log`

### Option C: Defer Phase 73 Until After Phase 74

Phase 74 (BarNormalizerAgent) is already in progress. Let it complete, then circle back to Phase 73 with a fresh audit. This gives us time to:
- Let Phase 71-72 patterns stabilize in production
- Gather real-world data on `signal_transform_log` usage
- Verify `swarm_orchestrator` actual purpose

## Questions Requiring Resolution

1. **BaseAgent hierarchy:** Should `BaseAIAgent` extend `SwarmBaseAgent`, replace it, or coexist?

2. **Group service pattern:** Should `BaseGroupService` replace `SwarmDispatchComputeAgent`, or extract shared code from it?

3. **Schema migration:** Are we migrating `AgentResult` → `AgentOutput`, or does `signal_transform_log` already use `AgentOutput`?

4. **Orchestrator deletion:** Is `swarm_orchestrator_agent.py` actually unused, or does it still serve a purpose post-Phase-72?

5. **Context coexistence:** Can `SwarmContext` and `AIContext` coexist during migration, or is this a flag-day conversion?

6. **Import boundaries:** What are the current import violations in `src/intelligence/narrative/` and `src/intelligence/swarm/`?

## Next Steps

**Immediate (today):**
1. Suspend Phase 73 planning
2. Decide: Option A (re-plan from audit) vs Option C (defer until post-74)

**If Option A:**
1. Create Phase 73-Audit sub-phase (4 tasks, ~2 days)
2. Execute audit against current codebase
3. Generate revised architecture decision record
4. Update Phase 73 plans (73-01 through 73-09) with revised assumptions
5. Resume Phase 73 execution

**If Option C:**
1. Complete Phase 74 execution (BarNormalizerAgent)
2. Create Phase 73-Audit after 74 ships
3. Resume Phase 73 with fresh perspective

---

**Recommendation:** **Option A — Re-plan Phase 73 from First Principles**

The risk of architectural debt from executing outdated plans exceeds the cost of a 2-day audit. Phase 73 touches core agent infrastructure — getting this wrong will cascade into all future AI/LLM work.

---

*Generated: 2026-04-26*
*Auditor: Claude (Sonnet 4.6)*
