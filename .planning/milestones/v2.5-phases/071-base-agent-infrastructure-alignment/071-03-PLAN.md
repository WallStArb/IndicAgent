---
phase: 071-base-agent-infrastructure-alignment
plan: 03
type: execute
wave: 2
depends_on: [071-01, 071-02]
files_modified:
  - services/bar_aggregator_agent.py
  - services/roll_compute_agent.py
  - services/ai_narrative_agent.py
  - services/swarm_orchestrator_agent.py
  - services/signal_metrics_compute_agent.py
  - services/cross_asset_service.py
  - services/signal_tracker_compute_agent.py
  - services/intelligence_pipeline_agent.py
  - services/ml_discovery_agent.py
  - services/ml_orchestrator_agent.py
  - services/ml_data_quality_agent.py
  - services/provider_merger_agent.py
  - services/parity_auditor_agent.py
  - services/feature_snapshot_writer_agent.py
  - services/bar_writer_agent.py
  - services/feature_writer_agent.py
  - services/signal_writer_agent.py
  - services/lifecycle_writer_agent.py
  - services/signal_auditor_agent.py
  - services/service_auditor_agent.py
  - services/contract_metadata_writer_agent.py
  - services/bar_auditor_agent.py
  - services/signal_metrics_writer_agent.py
  - services/swarm_writer_agent.py
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "All vestigial setup_service_logging() calls removed from __main__ blocks"
    - "BaseAgent.__init__() already handles logging setup (Phase 52.2)"
    - "No duplicate lag_task creation in any agent _run() methods"
    - "BaseAgent.start() already creates lag task at line 155"
  artifacts:
    - path: "services/*.py"
      provides: "Agents without vestigial setup_service_logging() in __main__"
      contains: "if __name__ == '__main__':" (without setup_service_logging call)
  key_links:
    - from: "services/*_agent.py"
      to: "src/core/agent/base.py"
      via: "BaseAgent.start() lag task creation"
      pattern: "lag_task = asyncio.create_task\\(self._report_consumer_lag\\(\\)\\)"
---

<objective>
Remove Vestigial setup_service_logging() Calls + Duplicate Lag Task Creation

Implement Changes 4 and 6 from design doc:
- Change 4: Remove manual `setup_service_logging()` calls from `__main__` blocks where BaseAgent already handles it
- Change 6: Remove manual `lag_task = asyncio.create_task(self._report_consumer_lag())` from agents that create their own, since BaseAgent.start() already creates this task

Purpose: 
- Change 4: Since Phase 52.2, BaseAgent auto-configures logging in `__init__()` (line 88-92). The `__main__` calls are harmless (idempotent "first call wins" per WR-05 fix) but are vestigial noise.
- Change 6: BaseAgent.start() creates the lag task at line 155. Several agents create a second one in their `_run()` method, resulting in two concurrent lag reporting loops per agent.

Output: Clean __main__ blocks with no vestigial setup calls. No duplicate lag tasks in _run() methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/071-base-agent-infrastructure-alignment/071-CONTEXT.md
@docs/superpowers/specs/2026-04-14-base-agent-infrastructure-alignment-design.md
@src/core/agent/base.py
@src/core/service_utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove vestigial setup_service_logging() calls from __main__ blocks</name>
  <files>
    services/bar_aggregator_agent.py
    services/roll_compute_agent.py
    services/ai_narrative_agent.py
    services/swarm_orchestrator_agent.py
    services/signal_metrics_compute_agent.py
    services/cross_asset_service.py
    services/signal_tracker_compute_agent.py
    services/intelligence_pipeline_agent.py
    services/ml_discovery_agent.py
    services/ml_orchestrator_agent.py
    services/ml_data_quality_agent.py
    services/provider_merger_agent.py
    services/parity_auditor_agent.py
    services/feature_snapshot_writer_agent.py
    services/bar_writer_agent.py
    services/feature_writer_agent.py
    services/signal_writer_agent.py
    services/lifecycle_writer_agent.py
    services/signal_auditor_agent.py
    services/service_auditor_agent.py
    services/contract_metadata_writer_agent.py
    services/bar_auditor_agent.py
    services/signal_metrics_writer_agent.py
    services/swarm_writer_agent.py
  </files>
  <read_first>
    - services/bar_aggregator_agent.py
    - services/feature_writer_agent.py
    - src/core/agent/base.py
  </read_first>
  <action>
    For each agent file that contains `setup_service_logging(` in its `if __name__ == "__main__":` block:

    1. Remove the `setup_service_logging(...)` call
    2. Remove the import `from src.core.service_utils import setup_service_logging` if it exists
    3. Keep the rest of the block intact (asyncio.run(main()), etc.)

    Example transformation (from bar_aggregator_agent.py lines 545-550):
    BEFORE:
    ```python
    if __name__ == "__main__":
        from src.core.service_utils import setup_service_logging
        from src.observability.otel import init_tracing

        setup_service_logging("logs/bar_aggregator_agent.log")
        init_tracing("bar_aggregator_agent")
        asyncio.run(main())
    ```

    AFTER:
    ```python
    if __name__ == "__main__":
        asyncio.run(main())
    ```

    Note: init_tracing() was already removed in Plan 02. This task removes setup_service_logging().

    Do NOT modify:
    - LLMWriterService (llm_writer_service.py) — handled in Plan 05
    - Archived agents (_archived_*.py files)
    - feature_writer_agent.py _setup_logging() method (that's different — it's a class method, not __main__)
  </action>
  <verify>
    <automated>! grep -r "setup_service_logging(" services/*.py | grep "__main__" | grep -v "_archived" | grep -v "llm_writer_service.py" | wc -l | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    All agent __main__ blocks have setup_service_logging() removed (now automatic via BaseAgent)
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate lag_task creation from agents</name>
  <files>
    services/roll_compute_agent.py
    services/signal_metrics_compute_agent.py
    services/signal_auditor_agent.py
    services/signal_metrics_writer_agent.py
    services/service_auditor_agent.py
    services/contract_metadata_writer_agent.py
    services/bar_aggregator_agent.py
    services/cross_asset_service.py
    services/swarm_orchestrator_agent.py
    services/ai_narrative_agent.py
    services/parity_auditor_agent.py
  </files>
  <read_first>
    - services/roll_compute_agent.py
    - services/bar_aggregator_agent.py
    - services/signal_metrics_compute_agent.py
    - src/core/agent/base.py
  </read_first>
  <action>
    For each of the 11 agents that create their own lag_task in _run():

    1. Remove `lag_task = asyncio.create_task(self._report_consumer_lag())`
    2. Remove the associated `lag_task.cancel()` and `await lag_task` from the finally block
    3. Remove the try/except around `await lag_task` if it exists

    Example transformation (from roll_compute_agent.py lines 346, 419-423):
    BEFORE:
    ```python
    async def _run(self) -> None:
        lag_task = asyncio.create_task(self._report_consumer_lag())
        try:
            async for _topic, _key, payload in self._kafka_consumer.messages():
                # ... processing ...
        finally:
            lag_task.cancel()
            try:
                await lag_task
            except asyncio.CancelledError:
                pass
    ```

    AFTER:
    ```python
    async def _run(self) -> None:
        # lag_task created by BaseAgent.start() at line 155
        async for _topic, _key, payload in self._kafka_consumer.messages():
            # ... processing ...
    ```

    BaseAgent.start() already creates and manages the lag task (line 155), so these manual creations are duplicates.

    Do NOT remove the _report_consumer_lag() method itself — those will be addressed in Plan 04.
  </action>
  <verify>
    <automated>! grep -r "lag_task = asyncio.create_task(self._report_consumer_lag())" services/*.py | grep -v "_archived" | wc -l | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    No duplicate lag_task creation in agent _run() methods (BaseAgent.start() handles it)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BaseAgent→logging | setup_service_logging() configures file logging |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-071-05 | D | Log file path | accept | Logs are internal telemetry; no secrets in log output |
| T-071-06 | R | Lag metric reporting | accept | Prometheus metrics are read-only; no injection risk |
</threat_model>

<verification>
- [ ] No setup_service_logging() calls in agent __main__ blocks
- [ ] No duplicate lag_task = asyncio.create_task(self._report_consumer_lag()) in agents
- [ ] All agents still have functional __main__ entry points
- [ ] BaseAgent.start() still creates lag task at line 155
</verification>

<success_criteria>
- [ ] All vestigial setup_service_logging() calls removed from __main__ blocks
- [ ] No duplicate lag_task creation in any of the 11 identified agents
- [ ] Unit tests pass: `.venv/bin/pytest tests/unit/ -v`
- [ ] Linting passes: `.venv/bin/ruff check . --fix`
- [ ] Services still log to correct files (BaseAgent handles it)
</success_criteria>

<output>
After completion, create `.planning/phases/071-base-agent-infrastructure-alignment/071-03-SUMMARY.md`
</output>
