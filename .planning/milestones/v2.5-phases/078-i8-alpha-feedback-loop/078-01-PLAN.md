---
phase: 078-i8-alpha-feedback-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/alpha_swarm_agent.py
  - tests/unit/service_tests/test_alpha_swarm_agent.py
autonomous: true
requirements:
  - P78-POOL-FIX
  - P78-SEGMENT-KEY
  - P78-VOLUME-PROFILE
  - P78-LEAD-MAP
user_setup: []

must_haves:
  truths:
    - "AlphaSwarmComputeAgent creates exactly one asyncpg pool via super()._setup() — no second create_pool call exists in the file"
    - "AlphaSwarmComputeAgent._setup() instantiates exactly ONE LineageRecorder; ShadowRecorder and TransformRecorder are no longer imported or instantiated"
    - "Segment key is built from hmm_regime numeric prefix and timeframe (e.g. '1.5m'), never 'unknown.*'"
    - "ES symbols resolve NQ as the lead instrument via _LEAD_MAP; ES never self-leads"
    - "_extract_volume_profile() is removed from alpha_swarm_agent.py (replaced by VolumeZscorePlugin in Plan 06)"
    - "After Phase 78 the swarm writes only via topic_signal_lineage() → LineageWriterAgent → signal_lineage; alpha_multiplier_shadow and signal_transform_log receive no new swarm rows"
  artifacts:
    - path: "services/alpha_swarm_agent.py"
      provides: "Single-pool setup, LineageRecorder-only write path, I4-enriched segment keys, NQ→ES lead mapping"
      contains: "LineageRecorder"
  key_links:
    - from: "alpha_swarm_agent.py::_setup"
      to: "BaseGroupService._setup"
      via: "super()._setup() only — no second create_pool"
      pattern: "asyncpg.create_pool"
    - from: "alpha_swarm_agent.py::_setup"
      to: "src/core/ai/lineage.py::LineageRecorder"
      via: "self._lineage = LineageRecorder(producer=self._producer, env=self.env_name)"
      pattern: "LineageRecorder\\("
    - from: "alpha_swarm_agent.py::_record_swarm_result"
      to: "topic_signal_lineage"
      via: "LineageRecorder.record(event_type='agent_prediction', ...)"
      pattern: "agent_prediction"
---

<objective>
Wave 1 plan that absorbs both the live bug fixes (D-35..D-38) AND the Phase 73 lineage migration completion (D-01..D-08). After this plan:

- One asyncpg pool (via `super()._setup()` from `BaseGroupService`) — no leak
- Segment keys are built from `hmm_regime + timeframe` (numeric prefix), never `unknown.*`
- ES → NQ lead mapping is hardcoded in `_LEAD_MAP`
- `_extract_volume_profile()` is removed — VolumeZscorePlugin (Plan 06) replaces it
- The swarm writes via a SINGLE `LineageRecorder` to `topic_signal_lineage()`. `ShadowRecorder` and `TransformRecorder` are no longer imported or used inside `services/alpha_swarm_agent.py` — their classes are archived in place by Plan 02.

Why: The graduation loop in Plan 03 reads `signal_lineage` JOIN `signal_ledger`. Without this migration, Plan 03 measures a frozen historical table (`signal_transform_log`) and would never see new swarm predictions. The bug fixes ensure the rows we DO write carry the correct segment_key and lead context.

Output: a single-file refactor of `services/alpha_swarm_agent.py` plus updated unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/078-i8-alpha-feedback-loop/78-CONTEXT.md
@services/alpha_swarm_agent.py
@src/core/ai/lineage.py
@src/core/ai/base_group_service.py
@src/core/stream_keys.py
@src/core/ml/shadow.py
@src/core/ml/transform_recorder.py

<interfaces>
LineageRecorder API (read `src/core/ai/lineage.py` before implementing — extract the actual constructor signature, `record()` signature, and `flush()` semantics. The class is Kafka-first: it publishes to `topic_signal_lineage()` via the provided producer; `LineageWriterAgent` consumes that topic and persists to `signal_lineage`. Do NOT pass a DB pool to LineageRecorder.)

stream_keys helpers used here:
- `topic_signal_lineage(env)` — single topic for all agent prediction events
- `topic_signal_lineage_dlq(env)` — dead-letter destination for malformed events

Settings/env propagation:
- `BaseGroupService` exposes `self._producer` (KafkaProducerClient) and `self.env_name` (string) — pass both to `LineageRecorder`. Verify exact attribute names by reading `src/core/ai/base_group_service.py`.

Outcome JOIN contract used by Plan 03:
- `signal_lineage.event_type = 'agent_prediction'`
- `signal_lineage.signal_id` JOINs `signal_ledger.signal_id`
- `signal_ledger.outcome` and `signal_ledger.pnl_r` are the realized labels
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace ShadowRecorder + TransformRecorder with single LineageRecorder; fix the pool leak</name>
  <files>services/alpha_swarm_agent.py, tests/unit/service_tests/test_alpha_swarm_agent.py</files>
  <read_first>
    - services/alpha_swarm_agent.py (full file — current _setup, _record_swarm_result, _process_one_signal, imports)
    - src/core/ai/lineage.py (full file — actual LineageRecorder constructor + record() signature)
    - src/core/ai/base_group_service.py (read _setup, identify exact attribute name for the producer and env)
    - src/core/ml/shadow.py (current ShadowRecorder API — what it was writing, so we know what to map)
    - src/core/ml/transform_recorder.py (current TransformRecorder API — same)
    - src/core/stream_keys.py (confirm `topic_signal_lineage` exists)
    - tests/unit/service_tests/test_alpha_swarm_agent.py (current test patterns; mocks)
  </read_first>
  <behavior>
    - `_setup()` calls `super()._setup()` exactly once and creates NO additional asyncpg pool
    - `_setup()` instantiates exactly one `LineageRecorder` and stores it on `self._lineage`
    - `_setup()` does NOT instantiate `ShadowRecorder` or `TransformRecorder` (imports removed)
    - `_record_swarm_result(...)` calls `self._lineage.record(event_type="agent_prediction", signal_id=..., agent_name=..., prediction=..., segment_key=..., features_snapshot=..., timestamp=...)` — exact kwargs to be verified by reading `lineage.py`
    - The recorded `segment_key` is `f"{hmm_regime}.{timeframe}"` where `hmm_regime` is the numeric value from the I4-enriched context (never `"unknown"`); raise/log + skip if the regime is None
    - `signal_id` is converted via `str(...)` before passing into LineageRecorder (per CLAUDE.md UUID rule)
    - On shutdown (`_teardown()`), `await self._lineage.flush()` is called before super teardown
    - Unit test: a fake producer captures messages; one signal in → one Kafka message on `topic_signal_lineage()` with `event_type == "agent_prediction"` and a numeric segment_key prefix; ZERO calls to ShadowRecorder/TransformRecorder methods
  </behavior>
  <action>
    1. Remove imports: `from src.core.ml.shadow import ShadowRecorder` and `from src.core.ml.transform_recorder import TransformRecorder` from `services/alpha_swarm_agent.py`. Add `from src.core.ai.lineage import LineageRecorder` and `from src.core.stream_keys import topic_signal_lineage`.
    2. Rewrite `_setup()`:
       - Call `await super()._setup()` exactly once.
       - Delete any direct `asyncpg.create_pool(...)` call (the pool comes from BaseGroupService).
       - Replace `self._shadow = ShadowRecorder(...)` and `self._transform = TransformRecorder(...)` with a single `self._lineage = LineageRecorder(producer=self._producer, env=self.env_name)` (use the exact constructor signature from `src/core/ai/lineage.py`; if it differs, match it — record the deviation in SUMMARY.md).
    3. Rewrite `_record_swarm_result(...)`:
       - Build `segment_key = f"{hmm_regime}.{timeframe}"`. `hmm_regime` MUST come from the I4-enriched context (`ctx.i4.hmm_regime` — verify exact field name against `src/intelligence/schemas.py::I4Context`). If `hmm_regime is None`, log a structlog warning with `kind="missing_hmm_regime"` and return without recording.
       - Call `await self._lineage.record(event_type="agent_prediction", signal_id=str(signal_id), agent_name=agent.name, prediction=agent_output.prediction, segment_key=segment_key, features_snapshot=features_snapshot, timestamp=timestamp)`. Match LineageRecorder's actual kwargs.
       - Remove all calls to ShadowRecorder/TransformRecorder methods.
    4. Implement `_teardown()` (or the existing teardown hook) so it `await self._lineage.flush()` before calling super.
    5. Delete `_extract_volume_profile()` and any internal references to it from `alpha_swarm_agent.py`. The volume z-score will be provided by `VolumeZscorePlugin` (Plan 06). If `_process_one_signal` currently passes `volume_profile=...`, drop that argument.
    6. Tests in `tests/unit/service_tests/test_alpha_swarm_agent.py`: use the `__new__` bypass pattern from CLAUDE.md. Inject a fake `KafkaProducerClient` mock; manually set `self._lineage = LineageRecorder(producer=fake_producer, env="test")`. Assert that processing one signal produces exactly one published message to `topic_signal_lineage("test")` with `event_type == "agent_prediction"` and `segment_key` matching `^\d+\.[0-9]+m$`. Assert ShadowRecorder/TransformRecorder are not imported (use `import services.alpha_swarm_agent as m; assert not hasattr(m, 'ShadowRecorder')`).
    Per D-08, D-01, D-04, D-35, D-38.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/service_tests/test_alpha_swarm_agent.py -x -v && .venv/bin/ruff check services/alpha_swarm_agent.py && grep -c "ShadowRecorder\|TransformRecorder\|_extract_volume_profile" services/alpha_swarm_agent.py | grep -q "^0$" && grep -q "LineageRecorder" services/alpha_swarm_agent.py && grep -c "asyncpg.create_pool" services/alpha_swarm_agent.py | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ShadowRecorder\|TransformRecorder' services/alpha_swarm_agent.py` returns `0`
    - `grep -c 'asyncpg.create_pool' services/alpha_swarm_agent.py` returns `0`
    - `grep -c '_extract_volume_profile' services/alpha_swarm_agent.py` returns `0`
    - `grep -q 'LineageRecorder' services/alpha_swarm_agent.py` succeeds
    - All tests in `tests/unit/service_tests/test_alpha_swarm_agent.py` pass
    - `.venv/bin/ruff check services/alpha_swarm_agent.py` exits 0
  </acceptance_criteria>
  <done>One pool, one LineageRecorder, no Shadow/Transform writes, no volume_profile stub. Tests prove the swarm writes via Kafka to `topic_signal_lineage()` with a correctly-formed numeric segment_key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: I4-enrich the AIContext + hardcode NQ→ES lead map</name>
  <files>services/alpha_swarm_agent.py, tests/unit/service_tests/test_alpha_swarm_agent.py</files>
  <read_first>
    - services/alpha_swarm_agent.py (post-Task-1 state — _process_one_signal, AIContext build path)
    - src/core/ai/context.py (current AIContext + AIContextCache.build signature)
    - src/intelligence/schemas.py (I4Context fields — confirm `hmm_regime` exists; the segment-key fix from Task 1 depends on it)
  </read_first>
  <behavior>
    - The AIContext consumed by `_record_swarm_result` always has `ctx.i4` populated for symbols where I4 ran (which is all live symbols)
    - `_LEAD_MAP: dict[str, str]` is a module-level constant; for `"ES"` it returns `"NQ"`; for any non-mapped symbol it returns the symbol itself (self-lead default)
    - `_LEAD_MAP` is the ONLY source for lead resolution in alpha_swarm_agent.py — no inline conditionals
    - Test: build a fake intelligence event with `i4.hmm_regime=1` for symbol ES; assert the recorded lineage row has `segment_key="1.{tf}"` and the lead resolves to `NQ`
  </behavior>
  <action>
    1. At module top of `services/alpha_swarm_agent.py` (under imports), add:
       ```python
       _LEAD_MAP: dict[str, str] = {"ES": "NQ"}

       def _resolve_lead(symbol: str) -> str:
           return _LEAD_MAP.get(symbol, symbol)
       ```
    2. In `_process_one_signal` (or wherever lead is resolved before calling agents), replace any prior lead-resolution logic with `lead = _resolve_lead(signal.symbol)`. Pass `lead` into the AIContext build via the existing build path.
    3. Confirm `AIContextCache.build(event)` already populates `ctx.i4` from `event.i4`. If `ctx.i4` is None at the point of `_record_swarm_result`, FIX THE CONTEXT BUILD — do NOT add a fallback in `_record_swarm_result`. The right fix is in `src/core/ai/context.py` (the broader Plan 05 changes will overhaul this file; here we only ensure i4 is non-None when the upstream event has i4 populated).
    4. Add unit test asserting: for an event with `i4.hmm_regime=1` and `tf="5m"`, the captured `segment_key == "1.5m"`. For symbol `ES`, the resolved lead is `NQ`. For symbol `NQ`, the lead is `NQ` (self-lead default).
    Per D-36, D-37.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/service_tests/test_alpha_swarm_agent.py::test_segment_key_uses_numeric_regime tests/unit/service_tests/test_alpha_swarm_agent.py::test_es_lead_is_nq -x -v && grep -q '_LEAD_MAP: dict\[str, str\] = {"ES": "NQ"}' services/alpha_swarm_agent.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '_LEAD_MAP' services/alpha_swarm_agent.py` succeeds
    - `_resolve_lead("ES") == "NQ"` and `_resolve_lead("NQ") == "NQ"` (test-verified)
    - Recorded segment_key matches `^\d+\.[0-9]+m$` (numeric prefix), never starts with `unknown.`
  </acceptance_criteria>
  <done>Segment keys carry the real hmm_regime; ES routes to NQ; no inline lead resolution remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka producer → topic_signal_lineage | The swarm publishes immutable audit events; downstream consumer (LineageWriterAgent) trusts schema |
| AIContext (i4) → segment_key | Untyped legacy paths could pass `None` regime; the segment_key contract requires numeric prefix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | Tampering | LineageRecorder publish | mitigate | LineageRecorder uses Pydantic schema validation before publish (existing in src/core/ai/lineage.py) |
| T-78-02 | Repudiation | Audit gap if swarm silently drops events on missing regime | mitigate | log structlog warning `kind="missing_hmm_regime"` with signal_id; do not silently swallow |
| T-78-03 | Information disclosure | features_snapshot may contain large floats (no PII) | accept | local infra, no external consumer; rationale: signals are not PII |
| T-78-04 | Denial of service | LineageRecorder backpressure on slow consumer | accept | KafkaProducerClient already configured with bounded queue; if it blocks, swarm slows naturally |
</threat_model>

<verification>
- `grep -RIn "ShadowRecorder\|TransformRecorder" services/alpha_swarm_agent.py` returns nothing
- `grep -RIn "asyncpg.create_pool" services/alpha_swarm_agent.py` returns nothing
- `.venv/bin/pytest tests/unit/service_tests/test_alpha_swarm_agent.py -x` passes
- `.venv/bin/ruff check services/alpha_swarm_agent.py` exits 0
</verification>

<success_criteria>
- All four bugs fixed (POOL, SEGMENT-KEY, VOLUME-PROFILE removal, LEAD-MAP)
- Swarm write path is exactly: `LineageRecorder.record() → topic_signal_lineage() → LineageWriterAgent → signal_lineage`
- ShadowRecorder and TransformRecorder are no longer imported by `services/alpha_swarm_agent.py`
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/078-i8-alpha-feedback-loop/078-01-SUMMARY.md`
</output>
