---
phase: 080-renaissance-swarm-intelligence-layer
plan: 06
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/intelligence/ai/alpha/counterfactual_agent.py
  - src/intelligence/ai/alpha/counterfactual_prompts.py
  - tests/unit/service_tests/test_counterfactual_agent.py
autonomous: true
requirements: [P80-COUNTERFACTUAL]

must_haves:
  truths:
    - "CounterfactualAgentComputeAgent extends BaseMultiplierAgent and starts shadow_only=True"
    - "counterfactual_prompts.py exposes ACTIVE_VERSION='counterfactual_v1' and build_counterfactual_prompt()"
    - "Validator clamps plausibility and confidence to [0.0, 1.0] and tolerates malformed lists"
    - "Multiplier formula plausibility × confidence is asserted by unit test"
  artifacts:
    - path: "src/intelligence/ai/alpha/counterfactual_agent.py"
      provides: "CounterfactualAgentComputeAgent — validation/invalidation reasoning multiplier"
      contains: "class CounterfactualAgentComputeAgent(BaseMultiplierAgent)"
    - path: "src/intelligence/ai/alpha/counterfactual_prompts.py"
      provides: "PROMPT_REGISTRY + ACTIVE_VERSION + build_counterfactual_prompt"
      contains: "ACTIVE_VERSION = \"counterfactual_v1\""
    - path: "tests/unit/service_tests/test_counterfactual_agent.py"
      provides: "Unit tests covering validator + multiplier formula"
      contains: "def test_"
  key_links:
    - from: "src/intelligence/ai/alpha/counterfactual_agent.py"
      to: "src/core/ai/multiplier_agent.py"
      via: "import BaseMultiplierAgent"
      pattern: "from src.core.ai.multiplier_agent import BaseMultiplierAgent"
---

<objective>
Implement CounterfactualAgentComputeAgent (D-06) — judges plausibility of validation/invalidation conditions for the I7 winner signal. Multiplier = plausibility × confidence in [0.0, 1.0]. Shadow only at start.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/080-renaissance-swarm-intelligence-layer/080-CONTEXT.md
@.planning/phases/080-renaissance-swarm-intelligence-layer/080-PATTERNS.md
@src/intelligence/ai/alpha/skeptic_agent.py
@src/core/ai/context.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create counterfactual_prompts.py</name>
  <files>src/intelligence/ai/alpha/counterfactual_prompts.py</files>
  <read_first>
    - src/intelligence/ai/alpha/correlation_prompts.py (Plan 04 sibling — mirror structure)
    - src/intelligence/ai/alpha/skeptic_prompts.py
  </read_first>
  <behavior>
    - PROMPT_REGISTRY has key "counterfactual_v1"
    - ACTIVE_VERSION == "counterfactual_v1"
    - build_counterfactual_prompt(ctx) raises TypeError for non-AIContext
    - Template instructs JSON schema {"plausibility": float, "confidence": float, "validation_conditions": [str], "invalidation_conditions": [str], "reasoning": str}
  </behavior>
  <action>
    Create file mirroring `correlation_prompts.py`. Substitute:
    - `ACTIVE_VERSION = "counterfactual_v1"`
    - JSON schema in template: `{"plausibility": <float 0..1>, "confidence": <float 0..1>, "validation_conditions": [<short clauses>], "invalidation_conditions": [<short clauses>], "reasoning": "<one short sentence>"}`
    - Role text: "You are a counterfactual reasoning analyst. Given the I7 winner signal, list what conditions must hold for it to succeed (validation_conditions) and what would invalidate it (invalidation_conditions), then judge how plausible each set is given the current pipeline context."
    - Define semantics: "plausibility=1.0 means validation conditions are highly likely AND invalidation conditions are unlikely; 0.0 means the opposite."
    - Function name: `build_counterfactual_prompt(ctx)`
    - Same `format(...)` keyword args as correlation_prompts: symbol, timeframe, winner_plugin, winner_direction_label, winner_confidence, full_context_block.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from src.intelligence.ai.alpha.counterfactual_prompts import ACTIVE_VERSION, PROMPT_REGISTRY, build_counterfactual_prompt; assert ACTIVE_VERSION == 'counterfactual_v1'; assert 'counterfactual_v1' in PROMPT_REGISTRY; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ACTIVE_VERSION = "counterfactual_v1"' src/intelligence/ai/alpha/counterfactual_prompts.py` returns 1
    - `grep -c "def build_counterfactual_prompt" src/intelligence/ai/alpha/counterfactual_prompts.py` returns 1
    - `grep -c "plausibility" src/intelligence/ai/alpha/counterfactual_prompts.py` returns >= 1
  </acceptance_criteria>
  <done>Prompts file imports, ACTIVE_VERSION matches agent_id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create counterfactual_agent.py</name>
  <files>src/intelligence/ai/alpha/counterfactual_agent.py</files>
  <read_first>
    - src/intelligence/ai/alpha/correlation_agent.py (Plan 04 sibling — mirror structure)
    - src/core/ai/multiplier_agent.py (Plan 01)
    - .planning/phases/080-renaissance-swarm-intelligence-layer/080-CONTEXT.md (D-06 contract)
  </read_first>
  <behavior>
    - agent_id == "counterfactual_v1"
    - shadow_only is True
    - tiers_needed == frozenset({Tier.I1, Tier.I4, Tier.I7})
    - latency_budget_ms == 5000.0
    - output_schema declares plausibility:float, confidence:float, validation_conditions:list, invalidation_conditions:list, reasoning:str
    - _validate_counterfactual_fields rejects non-numeric, clamps to [0,1], coerces both list fields to list[str]
    - multiplier = plausibility × llm_confidence
  </behavior>
  <action>
    Create `src/intelligence/ai/alpha/counterfactual_agent.py` with structure mirroring `correlation_agent.py`. Substitute:

    - Module docstring: `"""CounterfactualAgentComputeAgent — validation/invalidation reasoning multiplier (Phase 80, D-06)."""`
    - `_SYSTEM_MESSAGE = "You are a counterfactual reasoning analyst. Output strictly valid JSON. Phase 80 policy: discount-only — plausibility and confidence in [0.0, 1.0]."`
    - Validator `_validate_counterfactual_fields(data)`:
      - Reject non-dict; reject non-numeric `plausibility` or `confidence`.
      - Clamp both to [0,1].
      - For each of `validation_conditions` and `invalidation_conditions`: if not list, wrap as `[str(value)]`; otherwise `[str(x) for x in value]`.
      - `reasoning = str(data.get("reasoning", ""))`.
      - Return dict with all five keys; None on failure.
    - Class declaration:
      ```python
      class CounterfactualAgentComputeAgent(BaseMultiplierAgent):
          """Counterfactual reasoning — what must be true for this signal to work?"""

          output_schema: ClassVar[dict] = {
              "plausibility": float,
              "confidence": float,
              "validation_conditions": list,
              "invalidation_conditions": list,
              "reasoning": str,
          }

          agent_id = "counterfactual_v1"
          group = "alpha"
          tiers_needed = frozenset({Tier.I1, Tier.I4, Tier.I7})
          latency_budget_ms = 5000.0
          shadow_only = True
      ```
    - Constructor identical to correlation_agent.
    - `_compute(self, context)` body identical structure to correlation_agent except:
      - Calls `build_counterfactual_prompt(context)`.
      - Calls `self._parse_multiplier_response(response, _validate_counterfactual_fields)`.
      - Log fail event: `"counterfactual_agent.json_parse_failed"` with named fields.
      - `plausibility = parsed["plausibility"]; llm_confidence = parsed["confidence"]; multiplier = plausibility * llm_confidence`.
      - Payload: `{"plausibility": plausibility, "validation_conditions": parsed["validation_conditions"], "invalidation_conditions": parsed["invalidation_conditions"], "reasoning": parsed["reasoning"]}`.
      - `prompt_version=ACTIVE_VERSION`.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from src.intelligence.ai.alpha.counterfactual_agent import CounterfactualAgentComputeAgent; from src.core.ai.context import Tier; assert CounterfactualAgentComputeAgent.agent_id == 'counterfactual_v1'; assert CounterfactualAgentComputeAgent.shadow_only is True; assert Tier.I7 in CounterfactualAgentComputeAgent.tiers_needed; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class CounterfactualAgentComputeAgent(BaseMultiplierAgent)' src/intelligence/ai/alpha/counterfactual_agent.py` returns 1
    - `grep -c 'agent_id = "counterfactual_v1"' src/intelligence/ai/alpha/counterfactual_agent.py` returns 1
    - `grep -c 'tiers_needed = frozenset({Tier.I1, Tier.I4, Tier.I7})' src/intelligence/ai/alpha/counterfactual_agent.py` returns 1
    - `grep -c 'multiplier = plausibility \* llm_confidence' src/intelligence/ai/alpha/counterfactual_agent.py` returns 1
    - `.venv/bin/ruff check src/intelligence/ai/alpha/counterfactual_agent.py` exits 0
  </acceptance_criteria>
  <done>Module imports cleanly, ruff passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for counterfactual_agent</name>
  <files>tests/unit/service_tests/test_counterfactual_agent.py</files>
  <read_first>
    - tests/unit/service_tests/test_correlation_agent.py
    - src/intelligence/ai/alpha/counterfactual_agent.py
  </read_first>
  <behavior>
    - test_validator_accepts_valid_payload
    - test_validator_rejects_non_numeric_plausibility
    - test_validator_clamps_out_of_range
    - test_validator_coerces_non_list_validation_conditions
    - test_validator_coerces_non_list_invalidation_conditions
    - test_class_attributes
    - test_multiplier_formula_semantics
  </behavior>
  <action>
    Mirror `test_correlation_agent.py` substituting:
    - Imports: `from src.intelligence.ai.alpha.counterfactual_agent import CounterfactualAgentComputeAgent, _validate_counterfactual_fields`
    - Field substitutions: `coherence_score` → `plausibility`; add tests for both `validation_conditions` and `invalidation_conditions` list coercion (each of which becomes `["X"]` when given the string `"X"`).
    - `test_class_attributes` asserts `agent_id == "counterfactual_v1"`, `shadow_only is True`, `Tier.I7 in tiers_needed`, `latency_budget_ms == 5000.0`.
    - `test_multiplier_formula_semantics`: `plausibility, conf = 0.5, 0.6; assert plausibility * conf == pytest.approx(0.30)`.
    - At least 7 `def test_` functions.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/service_tests/test_counterfactual_agent.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/unit/service_tests/test_counterfactual_agent.py` returns >= 7
    - `.venv/bin/pytest tests/unit/service_tests/test_counterfactual_agent.py -v` exits 0
  </acceptance_criteria>
  <done>All counterfactual tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM provider -> agent | Untrusted text |
| Agent -> dispatch | Multiplier into aggregation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-080-06-01 | Tampering | LLM JSON | mitigate | Strict-types-or-None validator + `clamp(0,1)` on numerics |
| T-080-06-02 | DoS | LLM latency | mitigate | latency_budget_ms=5000 timeout via BaseAIAgent |
| T-080-06-03 | Elevation of Privilege | Multiplier above 1.0 | mitigate | shadow_only=True; dispatch excludes shadow from production confidence |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/service_tests/test_counterfactual_agent.py -v` exits 0
- `.venv/bin/ruff check src/intelligence/ai/alpha/counterfactual_agent.py src/intelligence/ai/alpha/counterfactual_prompts.py tests/unit/service_tests/test_counterfactual_agent.py` exits 0
</verification>

<success_criteria>
- CounterfactualAgentComputeAgent class with tiers {I1, I4, I7}
- Tests green
- Phase 80 discount-only policy upheld
</success_criteria>

<output>
After completion, create `.planning/phases/080-renaissance-swarm-intelligence-layer/080-06-SUMMARY.md`
</output>
