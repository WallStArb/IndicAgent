---
phase: 080-renaissance-swarm-intelligence-layer
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/intelligence/ai/alpha/skeptic_agent.py
  - src/intelligence/ai/alpha/skeptic_prompts.py
  - src/intelligence/ai/TEMPLATE_agent.py
autonomous: true
requirements: [P80-SKEPTIC, P80-BASE]

must_haves:
  truths:
    - "SkepticAgentComputeAgent extends BaseMultiplierAgent (not BaseAIAgent)"
    - "skeptic_agent.py no longer defines _JSON_BLOCK_RE, _parse_skeptic_response, or _validate_skeptic_fields"
    - "_validate_skeptic_fields lives in skeptic_prompts.py and is imported into the agent"
    - "Existing skeptic tests in tests/unit/service_tests/test_alpha_swarm_agent.py still pass"
    - "TEMPLATE_agent.py shows BaseMultiplierAgent as canonical base for new swarm agents"
  artifacts:
    - path: "src/intelligence/ai/alpha/skeptic_agent.py"
      provides: "Refactored Skeptic agent extending BaseMultiplierAgent with output_schema ClassVar"
      contains: "class SkepticAgentComputeAgent(BaseMultiplierAgent)"
    - path: "src/intelligence/ai/alpha/skeptic_prompts.py"
      provides: "Prompt registry + _validate_skeptic_fields validator (moved out of agent file per D-03)"
      contains: "def _validate_skeptic_fields"
    - path: "src/intelligence/ai/TEMPLATE_agent.py"
      provides: "Updated template showing BaseMultiplierAgent inheritance + helper usage"
      contains: "class TemplateComputeAgent(BaseMultiplierAgent)"
  key_links:
    - from: "src/intelligence/ai/alpha/skeptic_agent.py"
      to: "src/core/ai/multiplier_agent.py"
      via: "import BaseMultiplierAgent"
      pattern: "from src.core.ai.multiplier_agent import BaseMultiplierAgent"
    - from: "src/intelligence/ai/alpha/skeptic_agent.py"
      to: "src/intelligence/ai/alpha/skeptic_prompts.py"
      via: "import _validate_skeptic_fields"
      pattern: "from src.intelligence.ai.alpha.skeptic_prompts import.*_validate_skeptic_fields"
    - from: "src/intelligence/ai/alpha/skeptic_agent.py"
      to: "src/core/ai/prompt_utils.py"
      via: "import clamp"
      pattern: "from src.core.ai.prompt_utils import.*clamp"
---

<objective>
Refactor `SkepticAgentComputeAgent` to extend the new `BaseMultiplierAgent` from Plan 01, removing duplicated parse/clamp logic. Move `_validate_skeptic_fields` (Skeptic-specific JSON schema validator) out of the agent file and into `skeptic_prompts.py` (where it lives alongside the prompt content it validates). Update `TEMPLATE_agent.py` to show the new canonical pattern. Existing skeptic tests in `tests/unit/service_tests/test_alpha_swarm_agent.py` MUST continue passing.

Purpose: Locks in the new agent contract before three new agents (Plan 04) follow the same pattern. Skeptic becomes the runtime reference implementation. Satisfies D-03 (no `_JSON_BLOCK_RE`, `_parse_skeptic_response`, or `_validate_skeptic_fields` defined in the agent file).

Output: Refactored skeptic agent + skeptic_prompts validator move + updated template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/080-renaissance-swarm-intelligence-layer/080-CONTEXT.md
@.planning/phases/080-renaissance-swarm-intelligence-layer/080-PATTERNS.md
@.planning/phases/080-renaissance-swarm-intelligence-layer/080-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor SkepticAgentComputeAgent to extend BaseMultiplierAgent + move validator to skeptic_prompts.py</name>
  <files>src/intelligence/ai/alpha/skeptic_agent.py, src/intelligence/ai/alpha/skeptic_prompts.py</files>
  <read_first>
    - src/intelligence/ai/alpha/skeptic_agent.py (entire file — current state, identify lines to change)
    - src/intelligence/ai/alpha/skeptic_prompts.py (entire file — current state; this is where _validate_skeptic_fields will move)
    - src/core/ai/multiplier_agent.py (after Plan 01 — base class signature)
    - src/core/ai/prompt_utils.py (after Plan 01 — JSON_BLOCK_RE, parse_llm_json, clamp imports)
    - tests/unit/service_tests/test_alpha_swarm_agent.py (current skeptic tests — must not break)
    - .planning/phases/080-renaissance-swarm-intelligence-layer/080-PATTERNS.md (lines 368-391 — exact change list)
    - .planning/phases/080-renaissance-swarm-intelligence-layer/080-CONTEXT.md (D-03 — output_schema and multiplier formula)
  </read_first>
  <behavior>
    - SkepticAgentComputeAgent(...).output_schema == {"failure_probability": float, "confidence": float, "risk_factors": list, "reasoning": str}
    - Module-level _JSON_BLOCK_RE constant no longer exists in skeptic_agent.py
    - Module-level _parse_skeptic_response function no longer exists in skeptic_agent.py
    - _validate_skeptic_fields is DEFINED in skeptic_prompts.py (NOT in skeptic_agent.py); skeptic_agent.py imports it from there
    - _context_to_dict still exists in skeptic_agent.py (agent-specific helper, kept)
    - When _compute receives a valid LLM response, returned AgentOutput has output_type == "multiplier" and payload["multiplier"] == clamp((1.0 - failure_probability) * confidence, 0.0, 2.0)
    - All existing tests in tests/unit/service_tests/test_alpha_swarm_agent.py pass (any test that imports _validate_skeptic_fields directly is updated to import from skeptic_prompts)
  </behavior>
  <action>
Edit `src/intelligence/ai/alpha/skeptic_agent.py` and `src/intelligence/ai/alpha/skeptic_prompts.py`:

1. **Move `_validate_skeptic_fields` to `skeptic_prompts.py`** (D-03 compliance):
   - Cut the entire `def _validate_skeptic_fields(...)` function (and its module-level docstring/comments) out of `skeptic_agent.py`.
   - Paste it verbatim into `src/intelligence/ai/alpha/skeptic_prompts.py`, placed after the `PROMPT_REGISTRY`/`ACTIVE_VERSION` declarations.
   - Ensure any imports the validator needs (e.g. typing, numbers, etc.) are present in `skeptic_prompts.py`.
   - In `skeptic_agent.py`, replace its prior definition with: `from src.intelligence.ai.alpha.skeptic_prompts import _validate_skeptic_fields`.

2. Replace `from src.core.ai.base_agent import BaseAIAgent` with `from src.core.ai.multiplier_agent import BaseMultiplierAgent`.

3. Add `from src.core.ai.prompt_utils import clamp` (parse_llm_json is now used internally by base class — only `clamp` is needed in this file if any inline clamps remain).

4. Add `from typing import ClassVar` if not present.

5. Change class declaration: `class SkepticAgentComputeAgent(BaseMultiplierAgent):`

6. Inside the class, immediately under the docstring, add:
```python
output_schema: ClassVar[dict] = {
    "failure_probability": float,
    "confidence": float,
    "risk_factors": list,
    "reasoning": str,
}
```

7. Delete module-level `_JSON_BLOCK_RE = re.compile(r"\{[^{}]*\}", re.DOTALL)` from skeptic_agent.py (line 33 region — confirm by grep before deleting).

8. Delete module-level `def _parse_skeptic_response(...)` function from skeptic_agent.py (lines ~108-129 region — body that does direct JSON parse + regex fallback).

9. In `_compute()`, replace the call to `_parse_skeptic_response(response)` with `self._parse_multiplier_response(response, _validate_skeptic_fields)` (where `_validate_skeptic_fields` is now imported from skeptic_prompts).

10. In `_compute()`, replace the manual `AgentOutput(...)` construction with:
```python
return self._build_multiplier_output(
    context=context,
    multiplier=multiplier,  # (1.0 - failure_probability) * confidence
    confidence=llm_confidence,
    payload={
        "failure_probability": failure_probability,
        "risk_factors": parsed["risk_factors"],
        "reasoning": parsed["reasoning"],
    },
    prompt_version=ACTIVE_VERSION,
)
```

11. Replace any inline `max(0.0, min(2.0, multiplier))` clamp with `clamp(multiplier, 0.0, 2.0)` from prompt_utils — though note `_build_multiplier_output` clamps internally, so this inline clamp can be removed entirely.

12. Keep `_context_to_dict()` unchanged in skeptic_agent.py — it is agent-specific.

13. Remove unused imports (e.g., `import re` if no longer used; `import json` if no longer used).

After editing, run the existing test suite to confirm no regression:
```bash
.venv/bin/pytest tests/unit/service_tests/test_alpha_swarm_agent.py -v
```

If tests fail because they reference `_JSON_BLOCK_RE`, `_parse_skeptic_response`, or `_validate_skeptic_fields` directly from `skeptic_agent`, update the imports:
- `_JSON_BLOCK_RE` / `parse_llm_json` → `from src.core.ai.prompt_utils import JSON_BLOCK_RE, parse_llm_json`
- `_validate_skeptic_fields` → `from src.intelligence.ai.alpha.skeptic_prompts import _validate_skeptic_fields`
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/unit/service_tests/test_alpha_swarm_agent.py -v && .venv/bin/pytest tests/unit/test_multiplier_agent.py tests/unit/test_prompt_utils.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from src.core.ai.multiplier_agent import BaseMultiplierAgent" src/intelligence/ai/alpha/skeptic_agent.py` returns 1
    - `grep -c "class SkepticAgentComputeAgent(BaseMultiplierAgent)" src/intelligence/ai/alpha/skeptic_agent.py` returns 1
    - `grep -c "from src.core.ai.base_agent import BaseAIAgent" src/intelligence/ai/alpha/skeptic_agent.py` returns 0
    - `grep -c "_JSON_BLOCK_RE = re.compile" src/intelligence/ai/alpha/skeptic_agent.py` returns 0
    - `grep -c "^def _parse_skeptic_response" src/intelligence/ai/alpha/skeptic_agent.py` returns 0
    - `grep -c "^def _validate_skeptic_fields" src/intelligence/ai/alpha/skeptic_agent.py` returns 0
    - `grep -c "_validate_skeptic_fields" src/intelligence/ai/alpha/skeptic_agent.py` returns 1 (the import line only)
    - `grep -c "^def _validate_skeptic_fields" src/intelligence/ai/alpha/skeptic_prompts.py` returns 1
    - `grep -c "output_schema: ClassVar\[dict\]" src/intelligence/ai/alpha/skeptic_agent.py` returns 1
    - `grep -c "_context_to_dict" src/intelligence/ai/alpha/skeptic_agent.py` returns >= 1 (helper preserved in agent file)
    - `.venv/bin/pytest tests/unit/service_tests/test_alpha_swarm_agent.py -v` exits 0
    - `.venv/bin/ruff check src/intelligence/ai/alpha/skeptic_agent.py src/intelligence/ai/alpha/skeptic_prompts.py` exits 0
  </acceptance_criteria>
  <done>Skeptic agent extends BaseMultiplierAgent, uses base class helpers, declares output_schema, deletes redundant module-level helpers, moves _validate_skeptic_fields to skeptic_prompts.py per D-03, all existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update TEMPLATE_agent.py to show BaseMultiplierAgent canonical pattern</name>
  <files>src/intelligence/ai/TEMPLATE_agent.py</files>
  <read_first>
    - src/intelligence/ai/TEMPLATE_agent.py (current state)
    - src/intelligence/ai/alpha/skeptic_agent.py (after Task 1 — canonical reference)
    - .planning/phases/080-renaissance-swarm-intelligence-layer/080-PATTERNS.md (lines 627-693 — exact template body)
    - .planning/phases/080-renaissance-swarm-intelligence-layer/080-CONTEXT.md (D-12 spec)
  </read_first>
  <action>
Edit `src/intelligence/ai/TEMPLATE_agent.py`:

1. Replace `from src.core.ai.base_agent import BaseAIAgent` with:
```python
from src.core.ai.multiplier_agent import BaseMultiplierAgent
from src.core.ai.prompt_utils import clamp, parse_llm_json
```
Plus add `from typing import ClassVar` if missing.

2. Change `class TemplateComputeAgent(BaseAIAgent):` to `class TemplateComputeAgent(BaseMultiplierAgent):`

3. Add output_schema ClassVar declaration immediately under docstring:
```python
output_schema: ClassVar[dict] = {
    "score": float,         # agent-specific key (rename per agent)
    "confidence": float,    # always required
    "reasoning": str,       # always required
}
```

4. Update `_compute()` body to demonstrate `self._parse_multiplier_response()` and `self._build_multiplier_output()` per the spec in PATTERNS.md lines 658-691. Comments must include:
- "Phase 80: discount-only formula — multiplier should be ≤ 1.0 until calibration data exists"
- "Always extend BaseMultiplierAgent, never BaseAIAgent directly"

5. Add a header comment block at the top:
```python
"""TEMPLATE_agent.py — canonical skeleton for Phase 80 multiplier agents.

Copy this file when adding a new swarm agent. Required steps:
1. Pick a unique agent_id ending in _v1 (lowercase, underscored).
2. Define output_schema ClassVar with the LLM JSON keys you expect.
3. Implement _compute(): build prompt → LLM call → parse → return multiplier output.
4. Pair with a <name>_prompts.py exposing PROMPT_REGISTRY and ACTIVE_VERSION; agent-specific
   field validators (e.g. _validate_<agent>_fields) belong in the prompts file too.
5. Register in services/alpha_swarm_agent.py self._agents list.

Reference implementation: src/intelligence/ai/alpha/skeptic_agent.py
Authoring protocol: src/intelligence/ai/AUTHORING.md
"""
```
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast; ast.parse(open('src/intelligence/ai/TEMPLATE_agent.py').read())" && grep -c "class TemplateComputeAgent(BaseMultiplierAgent)" src/intelligence/ai/TEMPLATE_agent.py | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from src.core.ai.multiplier_agent import BaseMultiplierAgent" src/intelligence/ai/TEMPLATE_agent.py` returns 1
    - `grep -c "class TemplateComputeAgent(BaseMultiplierAgent)" src/intelligence/ai/TEMPLATE_agent.py` returns 1
    - `grep -c "from src.core.ai.base_agent import BaseAIAgent" src/intelligence/ai/TEMPLATE_agent.py` returns 0
    - `grep -c "output_schema: ClassVar\[dict\]" src/intelligence/ai/TEMPLATE_agent.py` returns 1
    - `grep -c "_parse_multiplier_response\|_build_multiplier_output" src/intelligence/ai/TEMPLATE_agent.py` returns at least 2
    - `.venv/bin/python -c "import ast; ast.parse(open('src/intelligence/ai/TEMPLATE_agent.py').read())"` exits 0 (file is syntactically valid Python)
    - `.venv/bin/ruff check src/intelligence/ai/TEMPLATE_agent.py` exits 0
  </acceptance_criteria>
  <done>Template file demonstrates BaseMultiplierAgent inheritance, output_schema declaration, and _parse_multiplier_response/_build_multiplier_output helper usage; file is valid Python and ruff-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM response → Skeptic.parse | Untrusted external string parsed; behavior already covered by base class JSON_BLOCK_RE bound |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-080-09 | Tampering | Refactor introduces silent bug in production Skeptic | mitigate | Existing tests in tests/unit/service_tests/test_alpha_swarm_agent.py MUST pass post-refactor (acceptance criterion). Multiplier formula `(1.0 - failure_probability) * confidence` preserved verbatim. Validator function moved (not modified) to skeptic_prompts.py. |
| T-080-10 | Information Disclosure | TEMPLATE_agent.py is documentation | accept | Template is reference code only, not loaded at runtime. No production impact. |
</threat_model>

<verification>
- `.venv/bin/pytest tests/unit/service_tests/test_alpha_swarm_agent.py tests/unit/test_multiplier_agent.py tests/unit/test_prompt_utils.py -v` all green
- `.venv/bin/ruff check src/intelligence/ai/alpha/skeptic_agent.py src/intelligence/ai/alpha/skeptic_prompts.py src/intelligence/ai/TEMPLATE_agent.py` clean
- `.venv/bin/black --check src/intelligence/ai/alpha/skeptic_agent.py src/intelligence/ai/alpha/skeptic_prompts.py src/intelligence/ai/TEMPLATE_agent.py` clean
- Pre-commit hook passes: I7 regime_type check, plugin class naming check (skeptic isn't a plugin so this is informational)
</verification>

<success_criteria>
- Skeptic refactored to extend BaseMultiplierAgent
- output_schema declared with four keys per D-03
- Module-level _JSON_BLOCK_RE, _parse_skeptic_response, AND _validate_skeptic_fields removed from skeptic_agent.py per D-03
- _validate_skeptic_fields now lives in skeptic_prompts.py and is imported into the agent
- Existing tests still pass (zero regression)
- Template updated to show new canonical pattern
</success_criteria>

<output>
After completion, create `.planning/phases/080-renaissance-swarm-intelligence-layer/080-03-SUMMARY.md`
</output>
