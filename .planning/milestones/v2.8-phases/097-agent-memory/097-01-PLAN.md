---
phase: 097-agent-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/114_agent_memory_schema.sql
autonomous: true
requirements: [MEM-01, MEM-02]

must_haves:
  truths:
    - "All 6 memory tables exist with correct columns, ENUMs, CHECK constraints"
    - "memory_calibration_promoted enforces sample_n >= 30 at the DB layer (C-03)"
    - "memory_episodes_labeled has an HNSW index on embedding for recall"
    - "memory_regime_transitions has at most one open period per (symbol, timeframe)"
    - "memory_system_state seeded with single row (epoch=1)"
  artifacts:
    - path: "production/migrations/114_agent_memory_schema.sql"
      provides: "6 tables + 3 ENUMs + indexes + hypertables + compression policies"
      contains: "CHECK (sample_n >= 30)"
  key_links:
    - from: "memory_episodes_labeled"
      to: "pgvector HNSW"
      via: "USING hnsw (embedding vector_cosine_ops)"
      pattern: "USING hnsw"
    - from: "memory_episodes_raw.regime_epoch"
      to: "memory_system_state.current_regime_epoch"
      via: "live pipeline reads epoch at insert time"
      pattern: "regime_epoch"
---

<objective>
Create the complete agent-memory database schema: 6 custom pgvector tables, 3 ENUMs, all indexes, hypertable conversions, and TimescaleDB compression policies. This is the foundation every Wave 2/3 backend reads and writes.

Purpose: Encode all four Renaissance constraints (C-01 regime_epoch, C-02 n_eligible, C-03 N>=30 CHECK, C-04 memory_assisted) structurally in the schema so they cannot be bypassed by application code.
Output: `production/migrations/114_agent_memory_schema.sql` — runnable against TimescaleDB with pgvector 0.8.2 already installed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/097-agent-memory/097-CONTEXT.md
@docs/plans/2026-06-02-agent-memory-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration file with ENUMs, support table, and episode-layer tables</name>
  <read_first>
    - docs/plans/2026-06-02-agent-memory-design.md (Section 5.1 through 5.4 — exact SQL DDL)
    - production/migrations/109_config_foundation.sql (migration header/style convention)
    - production/migrations/095_signal_ledger_split.sql (hypertable + index conventions)
  </read_first>
  <action>
    Create production/migrations/114_agent_memory_schema.sql. Begin with a header comment block: migration number 114, phase 097, purpose, and date 2026-06-02. Wrap the whole migration in a single transaction (BEGIN; ... COMMIT;) where TimescaleDB allows (create_hypertable and add_compression_policy run fine inside a transaction).

    Idempotency requirement: the migration must apply cleanly on both a fresh DB and a DB where it has already been run. Use `CREATE TABLE IF NOT EXISTS` for all tables, `CREATE INDEX IF NOT EXISTS` for all indexes, `create_hypertable(..., if_not_exists => TRUE)` for all hypertables, and guard compression policies with `IF NOT EXISTS` semantics (check `timescaledb_information.jobs` before calling `add_compression_policy`). ENUM guards already use the `EXCEPTION WHEN duplicate_object` pattern.

    Emit in this exact order:
    1. memory_system_state table (use `CREATE TABLE IF NOT EXISTS`) from design Section 5.1 (id PK default 1, current_regime_epoch INTEGER NOT NULL DEFAULT 1, epoch_updated_at TIMESTAMPTZ, single_row CHECK id=1), followed by `INSERT INTO memory_system_state DEFAULT VALUES ON CONFLICT (id) DO NOTHING` so exactly one row exists on first run and no error on re-run.
    2. The three ENUMs verbatim from Section 5.2: memory_episode_kind ('episodic','disagreement','relational'), memory_regime_label ('ranging','trending_up','trending_down'), memory_spc_stat (5 values). Guard each with a DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN NULL; END $$; block so re-running the migration does not error on existing types.
    3. memory_episodes_raw table verbatim from Section 5.3 including chk_raw_outcome CHECK with the exact 9-value outcome set from D-09, the JSONB payload comment block, create_hypertable on 'ts', and the two partial indexes (mem_raw_ready_for_label, mem_raw_signal). embedding is vector(768), NULLABLE here.
    4. memory_episodes_labeled table verbatim from Section 5.4: embedding vector(768) NOT NULL, embedding_text NOT NULL, outcome NOT NULL with chk_labeled_outcome, pnl_r NOT NULL, plus mae/mfe/bars_in_trade. create_hypertable on 'ts'. Then the HNSW index mem_labeled_hnsw (m=32, ef_construction=128), mem_labeled_cohort, mem_labeled_epoch. Then the compress ALTER + add_compression_policy('memory_episodes_labeled', INTERVAL '30 days').

    Use exact column names and constraint names from the design doc — these are referenced by backend SQL in Wave 2. Do NOT rename anything. Do NOT add columns not in the design.
  </action>
  <verify>
    Run the migration against the live DB: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/114_agent_memory_schema.sql — must complete with no errors. Then confirm tables exist: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "\d memory_episodes_labeled" shows the HNSW index and embedding vector(768) NOT NULL.
  </verify>
  <acceptance_criteria>
    - 114_agent_memory_schema.sql exists and applies cleanly (psql exit 0)
    - "\dt memory_*" lists memory_system_state, memory_episodes_raw, memory_episodes_labeled
    - "SELECT count(*) FROM memory_system_state" returns 1
    - "\d memory_episodes_labeled" shows index mem_labeled_hnsw using hnsw
    - chk_raw_outcome and chk_labeled_outcome reference all 9 D-09 outcome values, never win/loss/break_even
    - Re-running the migration file a second time does not error (idempotent ENUM guards)
  </acceptance_criteria>
  <done>All three episode-layer tables + support table + ENUMs exist with correct constraints and indexes.</done>
</task>

<task type="auto">
  <name>Task 2: Add calibration and regime satellite tables to the migration</name>
  <read_first>
    - docs/plans/2026-06-02-agent-memory-design.md (Section 5.5, 5.6, 5.7 — exact SQL DDL)
    - production/migrations/114_agent_memory_schema.sql (the file created in Task 1)
  </read_first>
  <action>
    Append to production/migrations/114_agent_memory_schema.sql (before the final COMMIT) the three satellite tables verbatim from the design doc:

    1. memory_calibration_promoted (Section 5.5): append-only regular table. Include chk_cal_sample_n CHECK (sample_n >= 30) — this is C-03 and is non-negotiable. Include all Brier-decomposition columns (brier_score, base_rate, reliability_score, resolution_score, skill_score), IC columns (information_coefficient, ic_t_stat, ic_p_value), CI/bootstrap columns (ci_lower, ci_upper, bootstrap_block_length, p_value_raw, p_value_corrected, n_hypotheses_tested), selection-bias columns (n_eligible, p_signal), and feedback-loop columns (memory_assisted_n, memory_assisted_fraction, feedback_loop_p, feedback_loop_quarantine, quarantine_review_at). Add the four indexes: mem_cal_cohort_latest, mem_cal_quarantine_review, mem_cal_skill, mem_cal_drift.
    2. memory_calibration_spc (Section 5.6): hypertable on 'ts', stat_name memory_spc_stat, ewma_* columns, cusum_* columns (both cusum_h_sigma and cusum_h_absolute), ks_* columns. Indexes mem_spc_alarms (partial WHERE any alarm TRUE), mem_spc_cohort. Compress ALTER + add_compression_policy INTERVAL '7 days'.
    3. memory_regime_transitions (Section 5.7): hypertable on 'ts_start'. Include chk_win_rate and chk_transition_probs_sum (probs sum to 1.0 +/- 0.001) CHECK constraints. Include the UNIQUE INDEX mem_reg_open ... WHERE ts_end IS NULL (structural single-open-period guarantee), plus mem_reg_history, mem_reg_duration.

    Use exact column/constraint/index names from the design doc.
  </action>
  <verify>
    Re-apply the full migration: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -f production/migrations/114_agent_memory_schema.sql. Then attempt to insert a calibration row with sample_n=10 and confirm it is rejected: PGPASSWORD=postgres psql -U postgres -h localhost -d indicagent -c "INSERT INTO memory_calibration_promoted (agent_id, regime_epoch, window_start, window_end, sample_n, mean_prediction, actual_rate, calibration_error, calibration_error_variance, brier_score, base_rate, reliability_score, resolution_score, skill_score, information_coefficient, ic_t_stat, ic_p_value, ci_lower, ci_upper, bootstrap_block_length, p_value_raw, p_value_corrected, n_hypotheses_tested) VALUES ('test',1,NOW(),NOW(),10,0.5,0.5,0,0,0,0.5,0,0,0,0,0,1,0,1,5,1,1,1)" must FAIL with a check constraint violation.
  </verify>
  <acceptance_criteria>
    - Full migration applies cleanly
    - INSERT with sample_n=10 into memory_calibration_promoted FAILS on chk_cal_sample_n
    - "\d memory_regime_transitions" shows unique index mem_reg_open with predicate (ts_end IS NULL)
    - "\d memory_calibration_spc" shows compression enabled
    - All six memory tables present: "\dt memory_*" lists exactly 6 tables
  </acceptance_criteria>
  <done>All 6 tables exist; C-03 N>=30 gate proven at DB layer; single-open-regime guarantee in place.</done>
</task>

</tasks>

<verification>
- "\dt memory_*" lists exactly 6 tables (memory_system_state, memory_episodes_raw, memory_episodes_labeled, memory_calibration_promoted, memory_calibration_spc, memory_regime_transitions)
- All three ENUMs exist: "\dT memory_*"
- Migration is idempotent (second apply does not error)
</verification>

<success_criteria>
The schema fully encodes C-01 (regime_epoch NOT NULL on episodes), C-02 (n_eligible nullable column), C-03 (sample_n >= 30 CHECK), C-04 (memory_assisted NOT NULL DEFAULT FALSE) and supports the recall hot path via HNSW index.
</success_criteria>

<output>
After completion, create `.planning/phases/097-agent-memory/097-01-SUMMARY.md`
</output>
