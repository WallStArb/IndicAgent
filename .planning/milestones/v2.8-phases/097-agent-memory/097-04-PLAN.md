---
phase: 097-agent-memory
plan: 04
type: execute
wave: 2
depends_on: ["097-01", "097-02", "097-03"]
files_modified:
  - src/core/memory/backends/mem0.py
  - src/core/memory/client.py
  - src/core/memory/writer.py
  - src/core/ai/worker_context.py
autonomous: true
requirements: [MEM-01, MEM-03, MEM-04]

must_haves:
  truths:
    - "MemoryClient composes the four backends and exposes recall/calibration/recall_regime_history"
    - "MemoryClient records memory_recall_latency_ms (p95 measurable) and hit/miss/timeout counters"
    - "MemoryEpisodeWriter.store() is fire-and-forget via asyncio.Queue(maxsize=500); full queue drops + counter"
    - "EmbeddingWorker drains queue, embeds, INSERTs into memory_episodes_raw"
    - "WorkerContext.memory_client is typed MemoryClient | None (stub filled)"
    - "Mem0 calls wrapped in asyncio.to_thread"
  artifacts:
    - path: "src/core/memory/client.py"
      provides: "MemoryClient read-only facade over 4 backends with OTel"
      contains: "memory_recall_latency_ms"
    - path: "src/core/memory/writer.py"
      provides: "MemoryEpisodeWriter + EmbeddingWorker"
      contains: "Queue"
  key_links:
    - from: "src/core/ai/worker_context.py"
      to: "MemoryClient"
      via: "memory_client: MemoryClient | None field"
      pattern: "memory_client"
    - from: "MemoryEpisodeWriter.store"
      to: "asyncio.Queue(maxsize=500)"
      via: "non-blocking put_nowait; drop on QueueFull"
      pattern: "put_nowait"
---

<objective>
Compose the read-only MemoryClient from the four backends, build the write-only MemoryEpisodeWriter with its background EmbeddingWorker, wire the Mem0 backend, and fill the WorkerContext stub. This completes the live read/write path and makes recall p95 latency observable (MEM-04).

Purpose: Deliver the MemoryClient facade agents receive via WorkerContext (MEM-01), with OTel latency instrumentation (MEM-04), behind the AGENT_MEMORY_ENABLED gate (MEM-03).
Output: src/core/memory/{mem0.py,client.py,writer.py} + WorkerContext update.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/097-agent-memory/097-CONTEXT.md
@docs/plans/2026-06-02-agent-memory-design.md
@src/core/memory/types.py
@src/core/memory/backends.py
@src/core/ai/worker_context.py
@src/observability/metrics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Mem0BackendImpl (tiers 4/7, asyncio.to_thread)</name>
  <read_first>
    - docs/plans/2026-06-02-agent-memory-design.md (Section 8 MEM0_CONFIG; D-18 Mem0 configuration; D-14 Mem0 scope)
    - src/core/memory/backends.py (Mem0Backend Protocol)
    - src/config/settings.py (ollama_base_url, DB connection params)
  </read_first>
  <action>
    Use context7 to confirm the current mem0 Python SDK Memory.from_config / search API surface before coding.

    Create src/core/memory/backends/mem0.py with class Mem0BackendImpl implementing the Mem0Backend Protocol. Constructor builds the MEM0_CONFIG dict from design Section 8 (vector_store provider "pgvector", collection_name "mem0_memories", embedding_model_dims 768; embedder provider "ollama", model "nomic-embed-text", ollama_base_url from Settings; NO graph store). CRITICAL: disable Mem0's LLM extraction entirely — set enable_graph=False and custom_fact_extraction_prompt=None; the mem0 add() path must NOT trigger any LLM calls (non-deterministic extraction would corrupt structured agent context). Use Mem0 as a pure pgvector store with structured metadata only — all content is stored verbatim as passed, no LLM-based deduplication or fact extraction. Lazy-initialize the synchronous Memory client (import mem0 inside __init__ or first use so unit tests without mem0 installed can still import the module via try/except guarded import).

    async search(query, tier, symbol, timeframe, limit) -> list[dict]: wrap the synchronous mem0 search call in asyncio.to_thread (D-13/D-18 — SDK is synchronous, must never block the event loop). Pass metadata filters tier/symbol/timeframe. Return [] on any error or if mem0 unavailable, never raise. Only tiers 4 (narrative) and 7 (annotation) are valid — guard against other tiers.

    If mem0 import fails (not installed), the backend must degrade to a no-op returning [] so the rest of the system still functions — log a warning once at construction.
  </action>
  <verify>
    .venv/bin/python -c "from src.core.memory.backends.mem0 import Mem0BackendImpl; print('import ok')" succeeds even if mem0 is not installed (guarded import). grep -q "asyncio.to_thread" src/core/memory/backends/mem0.py succeeds.
  </verify>
  <acceptance_criteria>
    - Module imports cleanly whether or not mem0 is installed (guarded import + no-op fallback)
    - search() wraps the sync SDK in asyncio.to_thread
    - MEM0_CONFIG matches design Section 8 (pgvector + ollama nomic-embed-text, 768 dims, no graph store)
    - Returns [] on error, never raises
    - .venv/bin/ruff check passes
  </acceptance_criteria>
  <done>Tier 4/7 Mem0 backend implemented with non-blocking async wrapper and graceful degradation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement MemoryClient facade with OTel latency instrumentation</name>
  <read_first>
    - docs/plans/2026-06-02-agent-memory-design.md (Section 6.1-6.5; D-19 read-only facade; D-21 metrics)
    - src/core/memory/backends/episodic.py (recall signature — plan 03)
    - src/core/memory/backends/calibration.py + regime.py (plan 03)
    - src/observability/metrics.py (MEMORY_RECALL_LATENCY_MS, MEMORY_RECALL_RESULTS_TOTAL, MEMORY_CALIBRATION_APPLIED — plan 02)
  </read_first>
  <action>
    Create src/core/memory/client.py with class MemoryClient. Constructor takes the four backend instances (EpisodicBackend, CalibrationBackend, RegimeBackend, Mem0Backend) and the EmbeddingService, plus recall_limit (default 10 from config). Read-only — NO write methods (D-19).

    async recall(context, agent_id, limit=None) -> list[Episode]: embed the context via EmbeddingService.embed_context, extract cohort keys (symbol, hmm_regime, entry_type, regime_epoch) from the context, time the call with a monotonic clock, delegate to EpisodicBackend.recall. Record MEMORY_RECALL_LATENCY_MS with {"tier": "1", "symbol": symbol} (this is the MEM-04 p95 signal). Increment MEMORY_RECALL_RESULTS_TOTAL with result "hit" (len>0), "miss" (len==0), or "timeout" (empty due to wait_for timeout — distinguish via the backend returning a sentinel or by catching here). Returns [] never raises.

    async calibration(agent_id, symbol, hmm_regime, entry_type, regime_epoch) -> CalibrationStats | None: delegate to CalibrationBackend.get_calibration(). When a promoted row exists, return it (bootstrapped=True already set on the returned CalibrationStats) and increment MEMORY_CALIBRATION_APPLIED with {"agent_id": agent_id, "stable": str(stats.correction_factor_stable).lower()}. Cold-start handling (F3): when get_calibration() returns None (below N≥30 gate or no promoted row), call CalibrationBackend.get_partial_sample_n() to get the raw labeled episode count for the cohort. If sample_n > 0, return CalibrationStats(bootstrapped=False, sample_n=sample_n, correction_factor=None, correction_factor_stable=False, feedback_loop_quarantine=False, all stat fields=0.0 or None). This lets agents distinguish "no data at all" (returns None, sample_n=0) from "data exists but below promotion gate" (returns partial CalibrationStats with bootstrapped=False). Return None only when sample_n=0. NOTE: MemoryClient does NOT query the DB directly — all data access goes through backend Protocol methods (D-19).

    async recall_regime_history(symbol, timeframe) -> RegimeHistory | None: delegate to RegimeBackend, record MEMORY_RECALL_LATENCY_MS with tier "5".

    async recall_annotations(symbol, timeframe, limit=10) -> list[dict]: delegate to Mem0Backend.search with tier "annotation".

    Every method already inherits the 40ms timeout from the backends; the client adds metric recording only and must itself never raise.
  </action>
  <verify>
    .venv/bin/python -c "from src.core.memory.client import MemoryClient; print('import ok')". grep -q "memory_recall_latency_ms\|MEMORY_RECALL_LATENCY_MS" src/core/memory/client.py succeeds. Confirm no write/store method exists: grep -c "def store\|def write\|INSERT" src/core/memory/client.py returns 0.
  </verify>
  <acceptance_criteria>
    - MemoryClient exposes recall, calibration, recall_regime_history, recall_annotations — and NO write method
    - recall records MEMORY_RECALL_LATENCY_MS (MEM-04 p95 measurable) and hit/miss/timeout counter
    - calibration records MEMORY_CALIBRATION_APPLIED with stable label
    - Client methods never raise (graceful degradation preserved)
    - .venv/bin/ruff check passes
  </acceptance_criteria>
  <done>Read-only MemoryClient facade complete with latency observability for MEM-04.</done>
</task>

<task type="auto">
  <name>Task 3: Implement MemoryEpisodeWriter + EmbeddingWorker and fill WorkerContext stub</name>
  <read_first>
    - docs/plans/2026-06-02-agent-memory-design.md (Section 7.1 live path; D-13 fire-and-forget queue maxsize 500; C-04 memory_assisted)
    - src/core/memory/embedding.py (embed_context — plan 03)
    - src/core/ai/worker_context.py (the stub to fill — db_pool + memory_client fields)
    - src/observability/metrics.py (MEMORY_WRITE_QUEUE_DEPTH, MEMORY_WRITE_DROPPED_TOTAL)
    - config/memory.yaml (queue_maxsize=500)
    - production/migrations/114_agent_memory_schema.sql (memory_episodes_raw insertable columns)
  </read_first>
  <action>
    Create src/core/memory/writer.py with two classes:

    MemoryEpisodeWriter (write-only, held by signal pipeline only): constructor takes an asyncpg pool, EmbeddingService, queue_maxsize (default 500), and a current_epoch provider. Holds an asyncio.Queue(maxsize=queue_maxsize). Method store(raw_episode) -> None: NON-blocking — use queue.put_nowait; on asyncio.QueueFull, increment MEMORY_WRITE_DROPPED_TOTAL.add(1) and drop (never block bar processing, never raise — D-13). Stamp regime_epoch from the current-epoch provider (C-01) and memory_assisted from whether the originating recall returned results (C-04) — both passed in by the caller on the raw_episode. After each enqueue/drain, update MEMORY_WRITE_QUEUE_DEPTH.set(queue.qsize()).

    EmbeddingWorker (background drain coroutine): async start() launches a long-lived task running _drain(). _drain() loops: await queue.get(), call EmbeddingService.embed_context to produce (vector, embedding_text); INSERT INTO memory_episodes_raw with asyncpg (pass payload as dict, regime_epoch, memory_assisted, n_eligible NULL, outcome NULL). When vector is not None, include embedding cast ::vector and embedding_text; when vector is None, INSERT the row with embedding=NULL and embedding_text=NULL (raw schema allows NULL embedding — back-fill job filters WHERE embedding IS NOT NULL before labeling). Never skip/drop the raw row due to embedding failure — the episode must be persisted regardless of embedding availability. Catch all exceptions per-row, log, continue — a write failure never propagates. Provide async stop() for graceful shutdown (drain remaining or cancel).

    Observability — queue depth and stall detection (F1): after every put_nowait or drain iteration, emit MEMORY_WRITE_QUEUE_DEPTH.set(queue.qsize()). In _drain(), track last_drain_completed_at = time.monotonic() after each successful INSERT; emit MEMORY_EMBED_STALL_SECONDS.set(time.monotonic() - last_drain_completed_at) every iteration so Grafana can alert on stall > 30s. Alert thresholds to document in Grafana: queue_depth > 200 (40% of maxsize=500) → warning; stall_seconds > 30 → warning. These make embedding degradation visible rather than silent — the system drops episodes under Ollama saturation by design (D-13), but that degradation must be observable.

    Update src/core/ai/worker_context.py: change the docstring references from "Zep memory integration" to "agent memory integration (Phase 097)"; change memory_client type from Any | None to MemoryClient | None using a TYPE_CHECKING import of MemoryClient (mirror the existing LLMProviderChain TYPE_CHECKING pattern so Ring 0 stays import-light at runtime). Keep db_pool as-is. The dataclass stays frozen with the same defaults (None).
  </action>
  <verify>
    .venv/bin/python -c "from src.core.memory.writer import MemoryEpisodeWriter, EmbeddingWorker; print('ok')". .venv/bin/python -c "from src.core.ai.worker_context import WorkerContext; import typing; print('wc ok')". grep -q "put_nowait" src/core/memory/writer.py and grep -q "MemoryClient" src/core/ai/worker_context.py both succeed.
  </verify>
  <acceptance_criteria>
    - store() uses put_nowait and increments MEMORY_WRITE_DROPPED_TOTAL on QueueFull, never blocks/raises
    - EmbeddingWorker._drain INSERTs into memory_episodes_raw with embedding cast ::vector, payload as dict, regime_epoch + memory_assisted stamped
    - MEMORY_WRITE_QUEUE_DEPTH updated via .set(qsize())
    - WorkerContext.memory_client typed MemoryClient | None via TYPE_CHECKING import; dataclass still frozen
    - .venv/bin/ruff check src/core/memory/writer.py src/core/ai/worker_context.py passes
  </acceptance_criteria>
  <done>Write path (fire-and-forget queue + background embed/insert) complete; WorkerContext stub filled with typed MemoryClient.</done>
</task>

</tasks>

<verification>
- MemoryClient + MemoryEpisodeWriter + EmbeddingWorker + Mem0BackendImpl all import
- WorkerContext.memory_client is MemoryClient | None
- Write path is non-blocking with drop counter; read path records p95 latency
</verification>

<success_criteria>
Agents can receive a MemoryClient via WorkerContext (MEM-01), recall latency is OTel-measurable (MEM-04), and the write path never impacts the signal pipeline. All behind AGENT_MEMORY_ENABLED (MEM-03).
</success_criteria>

<output>
After completion, create `.planning/phases/097-agent-memory/097-04-SUMMARY.md`
</output>

<task type="auto">
  <name>Task 4: MemoryClientFactory — startup wiring behind AGENT_MEMORY_ENABLED</name>
  <read_first>
    - src/config/settings.py (agent_memory_enabled field added in Plan 02)
    - src/core/memory/client.py (MemoryClient constructor, Plan 04 Task 2)
    - src/core/memory/writer.py (MemoryEpisodeWriter, Plan 04 Task 3)
    - src/core/ai/worker_context.py (WorkerContext.memory_client stub, Plan 04 Task 3)
    - src/intelligence/ai/alpha/alpha_swarm.py (example service _setup() that constructs WorkerContext)
  </read_first>
  <action>
    Create src/core/memory/factory.py with a single public function:

    build_memory_client(settings: Settings, db_pool: asyncpg.Pool) -> MemoryClient | None

    When settings.agent_memory_enabled is False: log "memory.disabled" at INFO level, return None.
    When True: construct PgvectorEpisodicBackend(db_pool), PgvectorCalibrationBackend(db_pool),
    PgvectorRegimeBackend(db_pool), Mem0BackendImpl(settings), compose into MemoryClient, return it.
    On any construction error: log "memory.build_failed error=..." at WARNING, return None (never raise).

    Also add a build_memory_writer(settings, db_pool) -> MemoryEpisodeWriter | None with same gating logic.

    Call sites: services that construct WorkerContext for AI agents (alpha_swarm._setup()) should call
    build_memory_client(self.settings, self._db_pool) and store result; pass it into WorkerContext
    at compute time via WorkerContext(signal_context=..., llm_chain=..., db_pool=..., memory_client=client).
    Add this call to alpha_swarm._setup() as the reference wiring.
  </action>
  <verify>
    .venv/bin/python -c "from src.core.memory.factory import build_memory_client, build_memory_writer; print('ok')".
    grep -q "agent_memory_enabled" src/core/memory/factory.py succeeds.
    grep -q "build_memory_client" src/intelligence/ai/alpha/alpha_swarm.py succeeds.
  </verify>
  <acceptance_criteria>
    - build_memory_client returns None when settings.agent_memory_enabled=False (verifiable via unit test with mock settings)
    - build_memory_client returns MemoryClient instance when settings.agent_memory_enabled=True and db_pool provided
    - alpha_swarm._setup() calls build_memory_client and stores result; passes memory_client into WorkerContext
    - No exception propagates from factory.py to caller under any construction failure
    - .venv/bin/ruff check src/core/memory/factory.py passes
  </acceptance_criteria>
  <done>AGENT_MEMORY_ENABLED gate is live in the signal path — agents receive MemoryClient when flag is True, None when False.</done>
</task>
