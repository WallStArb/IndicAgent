---
phase: 097-agent-memory
plan: 06
type: execute
wave: 3
depends_on: ["097-01", "097-02", "097-03", "097-04"]
files_modified:
  - tests/unit/core/test_memory_client.py
  - tests/unit/core/test_memory_writer.py
  - tests/unit/core/test_embedding_service.py
autonomous: true
requirements: [MEM-01, MEM-04]

must_haves:
  truths:
    - "Unit tests cover MemoryClient recall/calibration with stubbed backends (no live DB/Ollama)"
    - "Tests prove graceful degradation: backend timeout/error yields [] / None, never raises"
    - "Tests prove MemoryEpisodeWriter drops on QueueFull and increments the drop counter"
    - "Tests prove EmbeddingService.serialize uses percentile tokens"
    - "pytest tests/unit/core/ is green"
  artifacts:
    - path: "tests/unit/core/test_memory_client.py"
      provides: "MemoryClient unit tests with fake backends"
      contains: "def test_"
    - path: "tests/unit/core/test_memory_writer.py"
      provides: "writer queue-drop + drain tests"
      contains: "QueueFull"
    - path: "tests/unit/core/test_embedding_service.py"
      provides: "serialize + embed failure tests"
      contains: "def test_"
  key_links:
    - from: "test_memory_client"
      to: "MemoryClient"
      via: "fake backend objects injected via constructor"
      pattern: "MemoryClient("
---

<objective>
Add CI-clean unit tests for the three load-bearing classes (MemoryClient, MemoryEpisodeWriter/EmbeddingWorker, EmbeddingService) using fakes — no live DB, no live Ollama. These tests lock the graceful-degradation and fire-and-forget contracts.

Purpose: Prove MEM-01 (recall returns Episodes / [] safely) and MEM-04 (latency metric recorded) at the unit level, and guard the never-raise / never-block contracts against regression.
Output: tests/unit/core/test_memory_client.py, test_memory_writer.py, test_embedding_service.py.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/097-agent-memory/097-CONTEXT.md
@src/core/memory/client.py
@src/core/memory/writer.py
@src/core/memory/embedding.py
@src/core/memory/types.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit-test MemoryClient with fake backends</name>
  <read_first>
    - src/core/memory/client.py (recall/calibration/recall_regime_history signatures + metric calls)
    - src/core/memory/types.py (Episode, CalibrationStats constructors for building fixtures)
    - tests/unit/ (existing async test style — pytest.mark.asyncio or anyio; mirror an existing async test file)
  </read_first>
  <action>
    Create tests/unit/core/test_memory_client.py. Define lightweight fake backend classes implementing the four Protocols (async methods returning canned Episodes/CalibrationStats, or raising/sleeping to simulate failure). Also a fake EmbeddingService whose embed_context returns ([0.0]*768, "ES 5m ...").

    Tests:
    - test_recall_returns_episodes: fake EpisodicBackend returns 2 Episodes; assert MemoryClient.recall returns them and the result count is correct.
    - test_recall_empty_is_miss: fake returns []; assert recall returns [] (and does not raise).
    - test_recall_backend_error_returns_empty: fake EpisodicBackend.recall raises; assert MemoryClient.recall returns [] and does NOT propagate the exception (graceful degradation, MEM-01 contract).
    - test_calibration_none_when_missing: fake CalibrationBackend returns None; assert calibration returns None.
    - test_calibration_returns_stats: fake returns a CalibrationStats with correction_factor_stable True; assert returned object passes through.
    - test_no_write_method: assert not hasattr(MemoryClient, "store") and not hasattr(MemoryClient, "write") (read-only invariant, D-19).

    Use pytest async style consistent with the repo. Do not connect to any DB or Ollama.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/core/test_memory_client.py -q passes (all tests green, no DB/network access).
  </verify>
  <acceptance_criteria>
    - test_memory_client.py has >= 6 tests, all passing
    - Backend-error test proves recall returns [] without raising
    - read-only invariant test passes (no store/write attr)
    - No live DB or Ollama used (fakes only)
  </acceptance_criteria>
  <done>MemoryClient recall/calibration and graceful-degradation contracts are unit-proven.</done>
</task>

<task type="auto">
  <name>Task 2: Unit-test MemoryEpisodeWriter queue-drop and EmbeddingService serialization</name>
  <read_first>
    - src/core/memory/writer.py (store put_nowait + QueueFull drop; EmbeddingWorker._drain)
    - src/core/memory/embedding.py (serialize percentile format; embed None-on-error)
    - src/observability/metrics.py (MEMORY_WRITE_DROPPED_TOTAL)
  </read_first>
  <action>
    Create tests/unit/core/test_memory_writer.py:
    - test_store_enqueues: construct MemoryEpisodeWriter with a fake pool + fake EmbeddingService and queue_maxsize=2; call store twice; assert queue depth == 2.
    - test_store_drops_when_full: with queue_maxsize=1, call store twice; assert the second call does NOT raise and the episode is dropped (queue size stays 1). Patch/observe MEMORY_WRITE_DROPPED_TOTAL or assert via a counter spy that a drop occurred. The key assertion: store never raises and never blocks even when full.
    - test_drain_skips_null_embedding: fake EmbeddingService.embed_context returns (None, "text"); run one _drain iteration; assert no INSERT was attempted on the fake pool (row skipped, raw allows NULL embedding so backfill filters it).
    - test_drain_inserts_on_success: fake embed_context returns ([0.0]*768, "text"); run one _drain iteration; assert the fake pool received exactly one execute/insert call.

    Create tests/unit/core/test_embedding_service.py:
    - test_serialize_uses_percentiles: build a fake context object (simple namespace) with rsi_pct/atr_pct/hmm_prob fields; assert serialize output contains "rsi_pct:" and "atr_pct:" and the leading symbol/timeframe/entry_type tokens, and does NOT contain a raw RSI integer like "rsi:55".
    - test_serialize_handles_missing_fields: context missing optional fields; assert serialize still returns a non-empty string with mandatory tokens, no exception.
    - test_embed_returns_none_on_http_error: patch the HTTP client to raise/timeout; assert embed returns None and does not raise.
    - test_embed_returns_none_on_dim_mismatch: patch HTTP to return a 384-length embedding; assert embed returns None (dimension guard).

    Use monkeypatch/respx/unittest.mock to fake the Ollama HTTP call — no live Ollama. Place fake pool as an object whose execute/fetch are AsyncMocks recording calls.
  </action>
  <verify>
    .venv/bin/pytest tests/unit/core/test_memory_writer.py tests/unit/core/test_embedding_service.py -q passes. Full suite green: .venv/bin/pytest tests/unit/core/ -q.
  </verify>
  <acceptance_criteria>
    - Writer tests prove store never raises/blocks when full and increments drop on QueueFull
    - _drain skips NULL-embedding rows and inserts on success
    - EmbeddingService tests prove percentile serialization (rsi_pct present, raw rsi absent) and None-on-error/dim-mismatch
    - No live DB or Ollama (all faked)
    - .venv/bin/pytest tests/unit/core/ -q is fully green
  </acceptance_criteria>
  <done>Writer fire-and-forget/drop and EmbeddingService percentile+failure contracts are unit-proven; full unit suite green.</done>
</task>

</tasks>

<verification>
- .venv/bin/pytest tests/unit/core/ -q is green
- All tests run without DB or Ollama access (fakes/mocks only)
</verification>

<success_criteria>
The three load-bearing classes have CI-clean unit coverage proving the never-raise (MEM-01), latency-recording (MEM-04), and fire-and-forget invariants that the rest of the system depends on.
</success_criteria>

<output>
After completion, create `.planning/phases/097-agent-memory/097-06-SUMMARY.md`
</output>
