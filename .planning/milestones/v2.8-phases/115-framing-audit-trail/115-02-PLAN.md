---
phase: 115
plan: 02
type: tdd
wave: 2
depends_on:
  - 115-01
files_modified:
  - src/intelligence/trading/signal_schema.py
  - src/intelligence/trading/cross_asset_divergence.py
  - tests/unit/intelligence/test_signal_schema.py
autonomous: true
must_haves:
  truths:
    - "make_signal_from_frame() is the sole authority for stop_basis, structural_stop_distance_atr, adaptive_buffer_mult, plugin_regime_type in the signal dict"
    - "All four audit fields are present in the signal dict produced by make_signal_from_frame() — even when TradeFrame uses defaults"
    - "cross_asset_divergence.py does NOT manually inject signal['stop_basis'] after calling make_signal_from_frame()"
    - "All TestFramingAuditPropagation tests pass"
  artifacts:
    - path: "src/intelligence/trading/signal_schema.py"
      provides: "Four audit field assignments in make_signal_from_frame()"
      contains: "adaptive_buffer_mult"
    - path: "src/intelligence/trading/cross_asset_divergence.py"
      provides: "Removed manual stop_basis injection"
    - path: "tests/unit/intelligence/test_signal_schema.py"
      provides: "TestFramingAuditPropagation test class"
      contains: "TestFramingAuditPropagation"
  key_links:
    - from: "make_signal_from_frame()"
      to: "signal dict"
      via: "sig['stop_basis'] = tf.stop_basis (and 3 other assignments)"
      pattern: "sig\\[.adaptive_buffer_mult.\\]"
---

<objective>
Propagate all four framing audit fields from TradeFrame through make_signal_from_frame() into the signal dict, and remove the manual injection in cross_asset_divergence.py that violates the construction invariant.

Purpose: The signal dict is the transport layer to signal_writer and signal_ledger. make_signal_from_frame() must be the single point where TradeFrame fields enter the dict — no plugin-level injection allowed.
Output: Four audit fields in every signal dict + 5 passing propagation tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@docs/superpowers/plans/2026-06-05-framing-audit-trail.md
@src/intelligence/trading/signal_schema.py
@src/intelligence/trading/cross_asset_divergence.py
@tests/unit/intelligence/test_signal_schema.py
</context>

<tasks>

<task type="tdd">
  <name>Task 2: Propagate framing audit fields through make_signal_from_frame() and clean cross_asset_divergence</name>
  <files>
    src/intelligence/trading/signal_schema.py
    src/intelligence/trading/cross_asset_divergence.py
    tests/unit/intelligence/test_signal_schema.py
  </files>
  <read_first>
    - src/intelligence/trading/signal_schema.py (full file — find make_signal_from_frame() and locate where stop_type is already assigned, ~line 276; find all existing sig[] assignments to know where to append)
    - src/intelligence/trading/cross_asset_divergence.py (search for "stop_basis" to find the manual injection line ~234 that must be deleted)
    - tests/unit/intelligence/test_signal_schema.py (read all existing fixtures: _viable_frame(), _frame_kwargs(), existing test classes — needed to write _frame_with_audit() and _frame_kwargs() references correctly)
    - docs/superpowers/plans/2026-06-05-framing-audit-trail.md (Task 2 Steps 2.1-2.5 for exact test code, fixture definition, and implementation)
  </read_first>
  <action>
    RED phase — write tests first:
    Add _frame_with_audit() helper and class TestFramingAuditPropagation to tests/unit/intelligence/test_signal_schema.py per source plan Task 2 Step 2.1. The fixture builds a TradeFrame with stop_basis="structure_snap", structural_stop_distance_atr=0.8, adaptive_buffer_mult=0.968, plugin_regime_type="trend". Run: .venv/bin/pytest tests/unit/intelligence/test_signal_schema.py::TestFramingAuditPropagation -v. Must fail with KeyError on stop_basis.

    Note: 115-01-PLAN.md must be completed first (declared in depends_on). TradeFrame must have adaptive_buffer_mult and plugin_regime_type fields before the _frame_with_audit() fixture can construct instances with those kwargs.

    GREEN phase — implement:
    1. In make_signal_from_frame() in signal_schema.py, after the last existing sig[] assignment before the return statement, add four assignments per source plan Task 2 Step 2.2:
       - sig["stop_basis"] = tf.stop_basis
       - sig["structural_stop_distance_atr"] = tf.structural_stop_distance_atr
       - sig["adaptive_buffer_mult"] = tf.adaptive_buffer_mult
       - sig["plugin_regime_type"] = tf.plugin_regime_type

    2. In cross_asset_divergence.py, find and delete the line: signal["stop_basis"] = tf.stop_basis (~line 234). Verify no other manual injections for the other three audit fields exist in that file.

    Run: .venv/bin/pytest tests/unit/intelligence/test_signal_schema.py tests/unit/intelligence/test_trade_framer.py -q to confirm all pass.
  </action>
  <verify>.venv/bin/pytest tests/unit/intelligence/test_signal_schema.py -q --tb=short 2>&1 | tail -10</verify>
  <done>All TestFramingAuditPropagation tests PASS. grep confirms: grep -n "adaptive_buffer_mult" src/intelligence/trading/signal_schema.py returns the assignment line. grep for 'signal\["stop_basis"\]' in cross_asset_divergence.py returns 0 results.</done>
</task>

</tasks>

<verification>
```bash
# Audit fields in signal_schema
grep -n "stop_basis\|structural_stop_distance_atr\|adaptive_buffer_mult\|plugin_regime_type" \
  src/intelligence/trading/signal_schema.py

# Manual injection removed
grep -n 'signal\["stop_basis"\]' src/intelligence/trading/cross_asset_divergence.py
# → 0 results

# Tests pass
.venv/bin/pytest tests/unit/intelligence/test_signal_schema.py -q --tb=short 2>&1 | tail -5
```
</verification>

<success_criteria>
- make_signal_from_frame() assigns all four audit fields: stop_basis, structural_stop_distance_atr, adaptive_buffer_mult, plugin_regime_type
- cross_asset_divergence.py has no manual stop_basis injection after make_signal_from_frame() call
- .venv/bin/pytest tests/unit/intelligence/test_signal_schema.py exits 0 with all TestFramingAuditPropagation tests green
- stop_type was already present in signal dict — no change needed (confirm it is not duplicated)
</success_criteria>

<output>
After completion, create docs/superpowers/plans/framing-audit-trail/115-02-SUMMARY.md with: files changed, key decisions, test count, any deviations from plan.
</output>
