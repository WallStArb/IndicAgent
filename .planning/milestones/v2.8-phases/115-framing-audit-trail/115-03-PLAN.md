---
phase: 115
plan: 03
type: execute
wave: 3
depends_on:
  - 115-01
  - 115-02
files_modified:
  - src/intelligence/trading/microstructure_utils.py
  - src/intelligence/trading/cvd_spike.py
  - src/intelligence/trading/ofi_spike.py
  - src/intelligence/trading/anchored_vwap_reversion.py
  - src/intelligence/trading/choch_reversal.py
  - src/intelligence/trading/cross_asset_divergence.py
  - src/intelligence/trading/cvd_divergence.py
  - src/intelligence/trading/delta_exhaustion.py
  - src/intelligence/trading/divergence_stack.py
  - src/intelligence/trading/dual_divergence.py
  - src/intelligence/trading/failed_breakout.py
  - src/intelligence/trading/fvg_fill.py
  - src/intelligence/trading/hvn_rejection.py
  - src/intelligence/trading/lvn_breakout.py
  - src/intelligence/trading/mean_reversion.py
  - src/intelligence/trading/mtf_alignment.py
  - src/intelligence/trading/ofi_continuation.py
  - src/intelligence/trading/ofi_divergence.py
  - src/intelligence/trading/orb15.py
  - src/intelligence/trading/orb30.py
  - src/intelligence/trading/pattern_completion.py
  - src/intelligence/trading/poc_rejection.py
  - src/intelligence/trading/prev_day_level_test.py
  - src/intelligence/trading/regime_transition.py
  - src/intelligence/trading/second_leg_continuation.py
  - src/intelligence/trading/trend_following.py
  - src/intelligence/trading/vcp.py
  - src/intelligence/trading/vwap_reclaim.py
  - tests/unit/intelligence/test_trade_framer.py
autonomous: true
must_haves:
  truths:
    - "Every frame_trade() call in src/intelligence/trading/ passes regime_type=self.regime_type or regime_type=regime_type"
    - "detect_spike_signal() in microstructure_utils.py accepts regime_type: str = 'any' and passes it to frame_trade()"
    - "cvd_spike.py and ofi_spike.py pass regime_type=self.regime_type to detect_spike_signal()"
    - "Hurst tightening fires for plugins with regime_type='trend' when hurst_exponent > 0.55"
    - "The acceptance grep returns 0 unwired call sites"
    - "TestRegimeTypeWired tests pass"
    - "Full unit suite passes"
  artifacts:
    - path: "src/intelligence/trading/microstructure_utils.py"
      provides: "detect_spike_signal() with regime_type param"
      contains: "regime_type: str = \"any\""
    - path: "src/intelligence/trading/cvd_spike.py"
      provides: "regime_type=self.regime_type passed to detect_spike_signal()"
      contains: "regime_type=self.regime_type"
    - path: "src/intelligence/trading/ofi_spike.py"
      provides: "regime_type=self.regime_type passed to detect_spike_signal()"
      contains: "regime_type=self.regime_type"
  key_links:
    - from: "25 plugin files"
      to: "frame_trade()"
      via: "regime_type=self.regime_type kwarg"
      pattern: "frame_trade\\(.*regime_type=self\\.regime_type"
    - from: "cvd_spike.py + ofi_spike.py"
      to: "detect_spike_signal()"
      via: "regime_type=self.regime_type"
      pattern: "detect_spike_signal\\(.*regime_type=self\\.regime_type"
---

<objective>
Wire regime_type to all 26 frame_trade() call sites so Hurst tightening is live for every plugin that declares a regime_type class attribute.

Purpose: Without this wiring, regime_type is never passed to frame_trade(), so _adaptive_buffer() always uses its default (no Hurst adjustment) and plugin_regime_type is always None. This is the activation step for the entire audit trail.
Output: 28 files modified, acceptance grep returns 0, full unit suite green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@docs/superpowers/plans/2026-06-05-framing-audit-trail.md
@src/intelligence/trading/microstructure_utils.py
@src/intelligence/trading/cvd_spike.py
@src/intelligence/trading/ofi_spike.py
</context>

<tasks>

<task type="auto">
  <name>Task 3a: Write contract tests and update microstructure_utils + spike callers</name>
  <files>
    tests/unit/intelligence/test_trade_framer.py
    src/intelligence/trading/microstructure_utils.py
    src/intelligence/trading/cvd_spike.py
    src/intelligence/trading/ofi_spike.py
  </files>
  <read_first>
    - src/intelligence/trading/microstructure_utils.py (read detect_spike_signal() full signature and body — find the frame_trade() call inside it)
    - src/intelligence/trading/cvd_spike.py (find the detect_spike_signal() call — note current args)
    - src/intelligence/trading/ofi_spike.py (find the detect_spike_signal() call — note current args)
    - docs/superpowers/plans/2026-06-05-framing-audit-trail.md (Task 3 Steps 3.1-3.3 for exact test code and function signatures)
  </read_first>
  <action>
    Write failing contract tests first (source plan Task 3 Step 3.1):
    Add class TestRegimeTypeWired to tests/unit/intelligence/test_trade_framer.py with 2 tests:
    - test_hurst_tightening_fires_for_trend_regime_type: imports _adaptive_buffer, f={"garch_vol_ratio":1.0,"hurst_exponent":0.75}, asserts mult < 1.0 for regime_type="trend"
    - test_no_hurst_tightening_when_regime_type_none: same features, asserts mult == approx(1.0) for regime_type=None
    Run tests — both should PASS already (tests the existing _adaptive_buffer contract, not new code).

    Update microstructure_utils.py (source plan Task 3 Step 3.2):
    - Add regime_type: str = "any" to detect_spike_signal() signature
    - Pass regime_type=regime_type in the internal frame_trade() call

    Update cvd_spike.py (source plan Task 3 Step 3.3):
    - Add regime_type=self.regime_type to the detect_spike_signal() call

    Update ofi_spike.py (source plan Task 3 Step 3.3):
    - Add regime_type=self.regime_type to the detect_spike_signal() call
  </action>
  <verify>.venv/bin/pytest tests/unit/intelligence/test_trade_framer.py::TestRegimeTypeWired -v 2>&1 | tail -8</verify>
  <done>Both TestRegimeTypeWired tests PASS. grep -n "regime_type" src/intelligence/trading/microstructure_utils.py shows the param in detect_spike_signal() signature and the kwarg in the frame_trade() call inside it.</done>
</task>

<task type="auto">
  <name>Task 3b: Wire regime_type to all 25 direct plugin frame_trade() call sites</name>
  <files>
    src/intelligence/trading/anchored_vwap_reversion.py
    src/intelligence/trading/choch_reversal.py
    src/intelligence/trading/cross_asset_divergence.py
    src/intelligence/trading/cvd_divergence.py
    src/intelligence/trading/delta_exhaustion.py
    src/intelligence/trading/divergence_stack.py
    src/intelligence/trading/dual_divergence.py
    src/intelligence/trading/failed_breakout.py
    src/intelligence/trading/fvg_fill.py
    src/intelligence/trading/hvn_rejection.py
    src/intelligence/trading/lvn_breakout.py
    src/intelligence/trading/mean_reversion.py
    src/intelligence/trading/mtf_alignment.py
    src/intelligence/trading/ofi_continuation.py
    src/intelligence/trading/ofi_divergence.py
    src/intelligence/trading/orb15.py
    src/intelligence/trading/orb30.py
    src/intelligence/trading/pattern_completion.py
    src/intelligence/trading/poc_rejection.py
    src/intelligence/trading/prev_day_level_test.py
    src/intelligence/trading/regime_transition.py
    src/intelligence/trading/second_leg_continuation.py
    src/intelligence/trading/trend_following.py
    src/intelligence/trading/vcp.py
    src/intelligence/trading/vwap_reclaim.py
  </files>
  <read_first>
    - docs/superpowers/plans/2026-06-05-framing-audit-trail.md (Task 3 Step 3.4 — complete file list with approximate line numbers grouped by regime_type category: trend=8 files, mean_reversion=9 files, any=8 files)
    - Run the authoritative grep FIRST to get exact line numbers before editing: grep -rn "frame_trade(" src/intelligence/trading/ --include="*.py" | grep -v "def frame_trade\|test_\|#" | sort
  </read_first>
  <action>
    For each of the 25 plugin files, find the frame_trade(...) call and add regime_type=self.regime_type as the final keyword argument. The pattern is identical across all files — only the call site line number differs.

    Each plugin already declares self.regime_type as a class attribute ("trend", "mean_reversion", or "any") — do NOT change these values. Only thread the existing attribute through to the function call.

    Do NOT modify these files that declare regime_type but do NOT call frame_trade(): momentum_breakout, squeeze_expansion, liquidity_hunt, liquidity_sweep_reclaim, session_extremes_setup, vwap_deviation, candlestick_pattern_setup, gap_analysis_setup, supply_demand_setup.

    After all 25 files are edited, run the acceptance check — must return 0 results:
    grep -rn "frame_trade(" src/intelligence/trading/ --include="*.py" | grep -v "def frame_trade\|regime_type=\|microstructure_utils\|test_\|trade_framer.py"

    If any file is listed, it still needs the kwarg added. Repeat until 0 results.

    Then run the full unit suite:
    .venv/bin/pytest tests/unit/ -q --tb=short 2>&1 | tail -10
  </action>
  <verify>
    grep -rn "frame_trade(" src/intelligence/trading/ --include="*.py" | grep -v "def frame_trade\|regime_type=\|microstructure_utils\|test_\|trade_framer.py"
    # → must return 0 lines
    .venv/bin/pytest tests/unit/ -q --tb=short 2>&1 | tail -5
  </verify>
  <done>Acceptance grep returns 0 results (all frame_trade calls pass regime_type=). Full unit suite exits 0 with all tests green.</done>
</task>

</tasks>

<verification>
```bash
# Acceptance check: 0 unwired call sites
grep -rn "frame_trade(" src/intelligence/trading/ --include="*.py" \
  | grep -v "def frame_trade\|regime_type=\|microstructure_utils\|test_\|trade_framer.py"

# Contract tests pass
.venv/bin/pytest tests/unit/intelligence/test_trade_framer.py::TestRegimeTypeWired -v

# Full suite
.venv/bin/pytest tests/unit/ -q --tb=short 2>&1 | tail -5
```
</verification>

<success_criteria>
- Acceptance grep returns exactly 0 results
- detect_spike_signal() accepts regime_type: str = "any" and passes it to frame_trade()
- cvd_spike.py and ofi_spike.py pass regime_type=self.regime_type to detect_spike_signal()
- All 25 direct plugin files pass regime_type=self.regime_type to frame_trade()
- Full unit suite green
</success_criteria>

<output>
After completion, create docs/superpowers/plans/framing-audit-trail/115-03-SUMMARY.md with: exact count of files modified, acceptance grep output (0 lines), any files that needed special handling, test results.
</output>
