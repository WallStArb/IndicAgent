---
phase: 115
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/intelligence/trading/trade_framer.py
  - tests/unit/intelligence/test_trade_framer.py
autonomous: true
must_haves:
  truths:
    - "TradeFrame has adaptive_buffer_mult field (float, default 1.0) with __post_init__ guard: value must be > 0"
    - "TradeFrame has plugin_regime_type field (str | None, default None)"
    - "frame_trade() computes adaptive_buffer_mult via _adaptive_buffer(features, 1.0, regime_type) and stores it on the returned TradeFrame"
    - "frame_trade() stores regime_type kwarg as plugin_regime_type on the returned TradeFrame"
    - "All TestFrameTradeAuditFields tests pass: normal regime, high vol, Hurst tightening, positivity invariant, regime stored, None when not passed"
  artifacts:
    - path: "src/intelligence/trading/trade_framer.py"
      provides: "TradeFrame dataclass with adaptive_buffer_mult and plugin_regime_type fields"
      contains: "adaptive_buffer_mult"
    - path: "tests/unit/intelligence/test_trade_framer.py"
      provides: "TestFrameTradeAuditFields test class with 6 tests"
      contains: "TestFrameTradeAuditFields"
  key_links:
    - from: "frame_trade() body"
      to: "TradeFrame return value"
      via: "adaptive_buffer_mult= and plugin_regime_type= kwargs in return TradeFrame(...)"
      pattern: "adaptive_buffer_mult=adaptive_buffer_mult"
---

<objective>
Extend TradeFrame with two new audit fields (adaptive_buffer_mult, plugin_regime_type) and wire frame_trade() to compute and store them at fire time.

Purpose: These fields are the source of truth for the framing audit trail. All downstream consumers (signal dict, signal_ledger) read from TradeFrame — this is the single capture point.
Output: Extended TradeFrame dataclass + 6 passing tests covering all edge cases.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@docs/superpowers/plans/2026-06-05-framing-audit-trail.md
@src/intelligence/trading/trade_framer.py
@tests/unit/intelligence/test_trade_framer.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Extend TradeFrame with adaptive_buffer_mult and plugin_regime_type</name>
  <files>
    src/intelligence/trading/trade_framer.py
    tests/unit/intelligence/test_trade_framer.py
  </files>
  <read_first>
    - src/intelligence/trading/trade_framer.py (read the full TradeFrame dataclass ~lines 133-165, the frame_trade() function signature and return block, and _adaptive_buffer() implementation)
    - tests/unit/intelligence/test_trade_framer.py (read all existing tests to understand fixture patterns and imports)
    - docs/superpowers/plans/2026-06-05-framing-audit-trail.md (Task 1, Steps 1.1-1.5 for exact test code and implementation)
  </read_first>
  <action>
    RED phase — write tests first, run to confirm AttributeError:
    Add class TestFrameTradeAuditFields to tests/unit/intelligence/test_trade_framer.py. The 6 test methods are in source plan Task 1 Step 1.1. Run: .venv/bin/pytest tests/unit/intelligence/test_trade_framer.py::TestFrameTradeAuditFields -v. Must fail with AttributeError.

    GREEN phase — implement:
    1. In TradeFrame dataclass (after line 164, structural_stop_distance_atr field), add two fields per source plan Task 1 Step 1.2:
       - adaptive_buffer_mult: float = 1.0
       - plugin_regime_type: str | None = None
       - __post_init__(self) -> None: raise ValueError if adaptive_buffer_mult <= 0

    2. In frame_trade() body per source plan Task 1 Step 1.3:
       - Place adaptive_buffer_mult = _adaptive_buffer(features, 1.0, regime_type) BEFORE the first _reject_frame() call in the function (after ATR emergency fallback at ~line 956), NOT just before _classify_stop_basis. There are 4 early _reject_frame() returns before _classify_stop_basis — placing after them would leave the variable out of scope on those paths. NOTE: Source plan Step 1.3 says "before _classify_stop_basis" — that placement is superseded by this instruction. Use the placement described here (before first _reject_frame).
       - Pass atr * adaptive_buffer_mult to _classify_stop_basis instead of bare atr
       - In the return TradeFrame(...) block, add: adaptive_buffer_mult=adaptive_buffer_mult, plugin_regime_type=regime_type

    Run: .venv/bin/pytest tests/unit/intelligence/test_trade_framer.py -v -q to confirm all pass.

    Note: frame_trade() already accepts regime_type kwarg — verify the existing signature before adding it. If regime_type already threads into _adaptive_buffer, only the capture-and-store step is missing.
  </action>
  <verify>.venv/bin/pytest tests/unit/intelligence/test_trade_framer.py -v -q 2>&1 | tail -10</verify>
  <done>All TestFrameTradeAuditFields tests PASS. TradeFrame has adaptive_buffer_mult and plugin_regime_type fields. grep confirms: grep -n "adaptive_buffer_mult\|plugin_regime_type" src/intelligence/trading/trade_framer.py returns hits in the dataclass and in the return statement.</done>
</task>

</tasks>

<verification>
```bash
# Fields on dataclass
grep -n "adaptive_buffer_mult\|plugin_regime_type" src/intelligence/trading/trade_framer.py

# __post_init__ guard present
grep -n "__post_init__" src/intelligence/trading/trade_framer.py

# All tests pass
.venv/bin/pytest tests/unit/intelligence/test_trade_framer.py -q --tb=short 2>&1 | tail -5
```
</verification>

<success_criteria>
- TradeFrame dataclass contains adaptive_buffer_mult: float = 1.0 and plugin_regime_type: str | None = None
- __post_init__ raises ValueError when adaptive_buffer_mult <= 0
- frame_trade() computes adaptive_buffer_mult via _adaptive_buffer(features, 1.0, regime_type) and stores both new fields on return value
- .venv/bin/pytest tests/unit/intelligence/test_trade_framer.py exits 0 with all 6 TestFrameTradeAuditFields tests green
</success_criteria>

<output>
After completion, create docs/superpowers/plans/framing-audit-trail/115-01-SUMMARY.md with: files changed, key decisions, test count, any deviations from plan.
</output>
