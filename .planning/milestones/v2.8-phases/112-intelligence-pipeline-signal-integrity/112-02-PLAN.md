---
phase: 112-intelligence-pipeline-signal-integrity
plan: 02
type: execute
wave: 2
depends_on: ["112-01"]
requirements: [PIPE-INT-02]
files_modified:
  - src/intelligence/trading/cis_scorer.py
  - src/intelligence/pipeline/signal_processor.py
  - src/intelligence/pipeline/quality_gate.py
  - src/config/settings.py
  - src/intelligence/plugins/base.py
  - src/intelligence/pipeline/executor.py
  - src/intelligence/pipeline/winner_selector.py
  - src/intelligence/pipeline/ranker.py
  - src/intelligence/trading/aggregator.py
  - src/intelligence/setup_performance_updater.py
  - tests/unit/intelligence/test_perf03_migration.py
  - src/intelligence/trading/signal_schema.py
  # quality_floor_bootstrap.py — new helper script for the empirical floor query (permitted path, not inline pipeline)
autonomous: true
must_haves:
  truths:
    - "apply_calibration is NOT called in SignalProcessor.process(); calibration applies to filtered CIS inside CISScorer.score() and calibrated_confidence is stamped from cis_result.calibrated_cis"
    - "The empirical quality floor startup query runs OUTSIDE the pipeline daemon (via a permitted bootstrap helper or oneshot script) and the computed floor is written to a config file or env var that quality_gate reads at startup — not an inline DB call from the pipeline"
    - "Signals below the empirically-derived SIGNAL_MIN_PUBLISHABLE_CONFIDENCE floor are rejected and counted by intelligence_pipeline_quality_floor_rejections_total"
    - "_I1_ALIAS_MAP exists, importable, and every key is asserted present in I1Indicators.model_fields at import time"
    - "Pipeline startup raises a named RuntimeError if any supports_incremental=True plugin lacks _state_migration_complete=True"
    - "At least 2 behavioral unit tests for representative incremental plugins (e.g. kalman_trend, garch_volatility) verify that state read-back produces different output on the second call (proving the plugin is NOT cold-starting every bar)"
    - "SETUP_PRIORITY no longer appears anywhere in src/ or services/; adjusted_rank = perf_multiplier with a 0.5 warm-up penalty for sample_size < 30"
    - "winner_selector default is long_bias=False with confidence as secondary tiebreak"
    - "SIGNAL_SCHEMA_VERSION is bumped from 'v1' to 'v2' (text) as the LAST change in this plan, with the in-flight active-signal count logged"
  artifacts:
    - path: "src/intelligence/trading/cis_scorer.py"
      provides: "calibrated_cis field on CISResult + apply_calibration after Kalman in score()"
      contains: "calibrated_cis"
    - path: "src/intelligence/pipeline/signal_processor.py"
      provides: "_I1_ALIAS_MAP + apply_calibration removal"
      contains: "_I1_ALIAS_MAP"
    - path: "src/intelligence/pipeline/quality_gate.py"
      provides: "empirical confidence floor + rejection counter; floor value loaded from file/env, not inline DB"
      contains: "SIGNAL_MIN_PUBLISHABLE_CONFIDENCE"
    - path: "src/intelligence/plugins/base.py"
      provides: "_state_migration_complete + fast_path ClassVar on PatternPlugin"
      contains: "_state_migration_complete"
    - path: "tests/unit/intelligence/test_perf03_migration.py"
      provides: "flag audit test + behavioral state-writeback tests for 2+ incremental plugins"
      contains: "_state_migration_complete"
    - path: "src/intelligence/trading/signal_schema.py"
      provides: "SIGNAL_SCHEMA_VERSION bumped to v2"
      contains: "SIGNAL_SCHEMA_VERSION"
  key_links:
    - from: "src/intelligence/trading/cis_scorer.py score()"
      to: "winner calibrated_confidence"
      via: "cis_result.calibrated_cis stamped onto selected winner"
      pattern: "calibrated_cis"
    - from: "quality_floor bootstrap helper / config file"
      to: "src/intelligence/pipeline/quality_gate.py"
      via: "floor threshold loaded from file/env at quality_gate startup — no inline DB call"
      pattern: "min_confidence|SIGNAL_MIN_PUBLISHABLE_CONFIDENCE"
    - from: "src/intelligence/pipeline/executor.py __init__"
      to: "plugin _state_migration_complete attributes"
      via: "RuntimeError raised for incomplete incremental plugins"
      pattern: "_state_migration_complete"
---

<objective>
Fix the three co-active contamination bugs and the directional/ranking asymmetries, all atomically. Move calibration from per-signal plugin outputs to CIS-level (Design B), add an empirical quality floor (routed through a DAG-compliant bootstrap path, not inline pipeline DB access), complete the PERF-03 migration with startup enforcement and behavioral tests, add the I1 field alias map, remove SETUP_PRIORITY for data-driven ranking with a warm-up penalty, and flip long_bias to False. The SIGNAL_SCHEMA_VERSION bump is the final, atomic step.

Purpose: Calibration was a category error (isotonic curves trained on CIS scores were applied to per-signal outputs); no floor let noise into training data; legacy plugins cold-started every bar. These must ship together so the contamination boundary established in Plan 01 marks genuinely clean data.
Output: Corrected calibration architecture, quality floor (DAG-compliant bootstrap path), complete PERF-03 enforcement with behavioral tests, data-driven ranking, SIGNAL_SCHEMA_VERSION = "v2".
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-intelligence-pipeline-signal-integrity/112-CONTEXT.md
@.planning/phases/112-intelligence-pipeline-signal-integrity/112-RESEARCH.md
@docs/plans/2026-06-02-intelligence-pipeline-signal-integrity.md
@.planning/phases/112-intelligence-pipeline-signal-integrity/112-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move calibration to CIS level (Design B) + add quality floor (DAG-compliant) + I1 alias map</name>
  <read_first>
    - src/intelligence/pipeline/signal_processor.py (apply_calibration call at lines 388-396; _build_features_from_event at line 84; the process() pipeline stage order; the apply_quality_gate call site that passes `features` as 2nd positional arg)
    - src/intelligence/trading/cis_scorer.py (CISResult dataclass lines 59-70; score() lines 121-200; the Kalman/filtered score path — calibration must apply AFTER Kalman filtering of the raw CIS)
    - src/intelligence/pipeline/calibrator.py (apply_calibration function signature and how calibration_curves are indexed by tf/symbol)
    - src/intelligence/pipeline/quality_gate.py (apply_quality_gate signature lines 15-21: `(signals, thresholds, *, tf, recorder)`; result loop lines 54-81 — floor must filter AFTER multipliers applied)
    - src/intelligence/schemas.py (I1Indicators model — to enumerate model_fields for the alias-map assertion)
    - src/config/settings.py (Settings class — add SIGNAL_MIN_PUBLISHABLE_CONFIDENCE)
    - src/observability/metrics.py (counter() pattern; .add(1, {label: val}) call convention)
    - CLAUDE.md DAG Invariant #3: "No analyzer or pipeline daemon touches the database — only BaseWriter, BaseTracker, and BaseAuditor subclasses perform DB operations."
    - docs/plans/2026-06-02-intelligence-pipeline-signal-integrity.md (sections 1-A, 1-B, 1-C; lines 87-122)
  </read_first>
  <action>
    CALIBRATION (Design B, D-04):
    - In `src/intelligence/trading/cis_scorer.py`: add field `calibrated_cis: float | None = None` to the `CISResult` dataclass. In `score()`, after the raw CIS is computed and Kalman-filtered (the filtered_cis stage), apply calibration to the filtered CIS using the isotonic curve for the relevant tf/symbol, and store the result in `CISResult.calibrated_cis`. CISScorer must accept the calibration curves (pass them into `score()` or set on the scorer instance — match how the pipeline currently supplies `cache_snapshot.calibration_curves`). Reuse the existing calibration application logic from `calibrator.py` (do NOT hand-roll isotonic interpolation).
    - In `src/intelligence/pipeline/signal_processor.py`: REMOVE the `apply_calibration(...)` block at lines 388-396 (the per-signal `tod_adjusted` calibration and its `_record_dropped("calibration", ...)`). At the winner-stamping site, set the winner's `calibrated_confidence` from `cis_result.calibrated_cis` (not from any per-signal isotonic).

    QUALITY FLOOR — DAG-COMPLIANT BOOTSTRAP (1-B, D-05):
    CRITICAL: The empirical floor query MUST NOT run inline in pipeline initialization. The pipeline is a DB-ignorant daemon (CLAUDE.md DAG Invariant #3). Use the following pattern:

    Create a standalone helper script `services/quality_floor_bootstrap.py` (a oneshot, not a daemon). This script:
    - Runs the win-rate query against `signal_ledger_full` filtered to `feature_schema_version >= 2`: find the lowest confidence bucket where `win_rate >= 0.50`; if total outcomes < 500, output the default 0.12; otherwise output the derived bucket lower bound.
    - Writes the result as a single float to `.pipeline_quality_floor` in the project root (or to a path from settings — Claude's discretion, document the path).
    - Is designed to be run at system startup BEFORE the pipeline daemon starts (e.g. via a systemd ExecStartPre or a manual pre-launch step).

    In `src/intelligence/pipeline/quality_gate.py`:
    - Add a module-level function `load_quality_floor(path: str, default: float = 0.12) -> float` that reads the float from the file written by the bootstrap script. If the file is absent or unreadable, returns the default and logs a WARNING (structlog, no event= kwarg).
    - Call `load_quality_floor(...)` once at pipeline startup (via the quality_gate or from the pipeline init — NOT from an async DB call).
    - Add a new kwarg `min_confidence: float = 0.0` to `apply_quality_gate` (default no-op preserves backward compat). After the multiplier loop builds `result`, drop any signal whose final `confidence < min_confidence`. The call site in signal_processor.py passes `min_confidence=settings.SIGNAL_MIN_PUBLISHABLE_CONFIDENCE` (from Settings) OR the loaded floor value — Claude's discretion on which drives the runtime floor; document in SUMMARY.

    Add `SIGNAL_MIN_PUBLISHABLE_CONFIDENCE: float` to `src/config/settings.py` Settings (default 0.12 per D-05, used as the config-level floor and the default for load_quality_floor).

    Create OTel counter `intelligence_pipeline_quality_floor_rejections_total` via `counter()` from src/observability/metrics.py; on each dropped signal call `.add(1, {"tf": tf})`.

    I1 ALIAS MAP (1-C):
    - Add module-level `_I1_ALIAS_MAP: dict[str, str]` to `src/intelligence/pipeline/signal_processor.py` mapping any legacy/aliased feature names to their canonical I1Indicators field names (enumerate from the keys signal_processor currently reads vs. I1Indicators.model_fields). At import time, assert every VALUE in `_I1_ALIAS_MAP` exists in `I1Indicators.model_fields` (raise a clear error naming the offending key if not). The alias map will be consumed in Plan 05 (flat feature precompute) — here it must merely exist, validate at import, and be importable.
  </action>
  <verify>
    .venv/bin/python -c "from src.intelligence.pipeline.signal_processor import _I1_ALIAS_MAP; print('alias OK')" && grep -n "apply_calibration" src/intelligence/pipeline/signal_processor.py | grep -v "import" || echo "no apply_calibration call in signal_processor (expected)" && .venv/bin/python -c "from src.intelligence.trading.cis_scorer import CISResult; assert 'calibrated_cis' in CISResult.__dataclass_fields__; print('cis OK')" && .venv/bin/pytest tests/unit/intelligence/ -q -k "quality_gate or cis or calibrat" 2>&1 | tail -5
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "from src.intelligence.pipeline.signal_processor import _I1_ALIAS_MAP"` exits 0 (gate 1-C)
    - `grep -n "apply_calibration" src/intelligence/pipeline/signal_processor.py` shows NO call to apply_calibration in process() (only possibly a removed import); the per-signal calibration block at former lines 388-396 is gone (gate 1-A)
    - `.venv/bin/python -c "from src.intelligence.trading.cis_scorer import CISResult; print('calibrated_cis' in CISResult.__dataclass_fields__)"` prints True
    - `grep -n "calibrated_cis" src/intelligence/pipeline/signal_processor.py` shows the winner's calibrated_confidence is sourced from cis_result.calibrated_cis
    - `grep -n "SIGNAL_MIN_PUBLISHABLE_CONFIDENCE" src/config/settings.py` returns a definition with default 0.12
    - `grep -n "intelligence_pipeline_quality_floor_rejections_total" src/intelligence/pipeline/quality_gate.py` returns a counter creation
    - The alias-map import-time assertion fails loudly if a value is not in I1Indicators.model_fields (verified by reading the assert that names the offending key)
    - services/quality_floor_bootstrap.py exists as a standalone oneshot script (NOT imported or called from the pipeline daemon at runtime)
    - `grep -rn "signal_ledger_full\|asyncpg\|database_manager" services/intelligence_pipeline.py src/intelligence/pipeline/quality_gate.py` returns no DB query from quality_gate or the pipeline init path — floor is read from file, not DB (DAG Invariant #3 preserved)
    - The SUMMARY documents the chosen bootstrap invocation approach (ExecStartPre or manual) and the floor file path
  </acceptance_criteria>
  <done>Calibration is CIS-level only; signals below the floor are rejected and counted; the I1 alias map validates at import; the empirical floor query runs in a permitted oneshot helper, not inline in the pipeline daemon.</done>
</task>

<task type="auto">
  <name>Task 2: Complete PERF-03 migration (class attribute + startup enforcement + audit 31 plugins + behavioral state tests)</name>
  <read_first>
    - src/intelligence/plugins/base.py (PatternPlugin Protocol at line 41; existing ClassVar attrs lines 19-24: name, outputs, min_lookback, supports_incremental, capability_tags)
    - src/intelligence/pipeline/executor.py (__init__ and _plugin_cache construction; this is where the RuntimeError check goes)
    - src/intelligence/register_plugins.py (TIER_I1..TIER_I7 lists; used to enumerate plugins for the audit test)
    - src/intelligence/CLAUDE.md (PERF-03 / plugin state write-back rules; "Plugin state write-back is load-bearing")
    - docs/plans/2026-06-02-intelligence-pipeline-signal-integrity.md (sections 1-D, 3-A; lines 124-141, 271-273)
    - 112-RESEARCH.md (the explicit list of 31 supports_incremental=True plugins — lines 159-171; includes kalman_trend and garch_volatility which are the behavioral test targets)
    - src/intelligence/context/kalman_trend.py and src/intelligence/features/smc_context (or wherever garch_volatility lives) — read the incremental compute method signature and state parameter pattern for the two behavioral test targets
  </read_first>
  <action>
    BASE ATTRS (D-07, 3-A prep):
    - In `src/intelligence/plugins/base.py` PatternPlugin Protocol: add `_state_migration_complete: ClassVar[bool] = False` and `fast_path: ClassVar[bool] = False`. (fast_path execution branch ships in Plan 05; here only the attribute is added so plugins can opt in.)

    AUDIT 31 INCREMENTAL PLUGINS (D-07):
    - For each of the 31 plugins with `supports_incremental=True` (enumerated in 112-RESEARCH.md lines 159-171: 26 I1 indicators, bollinger_squeeze, market_profile, bocpd_changepoint, kalman_trend, garch_volatility, volume_zscore), READ the plugin's incremental compute method and confirm it correctly uses the `state=` parameter (true PERF-03 compliance — uses the passed state, writes it back, does not recompute from scratch every bar). For each confirmed-compliant plugin, set `_state_migration_complete = True` as a class-level attribute on that plugin class. If any plugin is NOT actually compliant, leave it False and record the non-compliance in the SUMMARY (do NOT fake compliance).

    STARTUP ENFORCEMENT (D-07):
    - In `src/intelligence/pipeline/executor.py.__init__` (after `_plugin_cache` is built): collect `incremental_names` = names of all cached plugins where `supports_incremental` is True; compute `incomplete = [n for n in incremental_names if not getattr(self._plugin_cache.get(n), "_state_migration_complete", False)]`; if `incomplete`, raise a named `RuntimeError(f"PERF-03 migration incomplete for plugins: {incomplete}")`. This is a hard crash by design (not an assert, so it survives -O).

    FLAG AUDIT TEST (gate 1-D):
    - Create `tests/unit/intelligence/test_perf03_migration.py` that registers all plugins (via register_all_plugins / the tier lists), enumerates every plugin with `supports_incremental=True`, and asserts each has `_state_migration_complete is True`. The test fails with the list of any non-compliant plugin names.

    BEHAVIORAL STATE TESTS (Codex concern — flag-only compliance is insufficient):
    - In the same `test_perf03_migration.py` file (or a companion test function), add at least 2 behavioral unit tests for representative incremental plugins to prove they actually read and write state across calls (not cold-starting every bar). Use kalman_trend and garch_volatility as the targets (both are in the 31-plugin list and are clearly stateful):
      1. Call the plugin's compute method with `state={}` (fresh) and a synthetic bar (build a minimal bar/frame dict). Capture the output and the returned/mutated state.
      2. Call the same plugin's compute method again with the STATE from step 1 and a DIFFERENT synthetic bar. Capture the second output.
      3. Assert the second output DIFFERS from what the plugin would produce on a cold-start with the same second bar — concretely, assert that at least one output field changes between the first and second call (proving the plugin read prior state). If the plugin's output is identical on both calls regardless of prior state, the plugin is actually cold-starting and the test must FAIL.
    - These tests do NOT need to be numerically precise; they need to prove state propagation. Document in the test what output field is expected to change (e.g. for kalman_trend: the smoothed value or variance should drift; for garch: the sigma estimate should update from the initial prior).
  </action>
  <verify>
    .venv/bin/python -c "from src.intelligence.plugins.base import PatternPlugin; print('base OK')" && .venv/bin/pytest tests/unit/intelligence/test_perf03_migration.py -q 2>&1 | tail -10
  </verify>
  <acceptance_criteria>
    - `grep -n "_state_migration_complete\|fast_path" src/intelligence/plugins/base.py` shows both ClassVar attrs on PatternPlugin defaulting to False
    - `.venv/bin/pytest tests/unit/intelligence/test_perf03_migration.py -q` passes — every supports_incremental=True plugin has _state_migration_complete=True (gate 1-D)
    - `grep -n "PERF-03 migration incomplete" src/intelligence/pipeline/executor.py` shows the RuntimeError check in __init__
    - The executor check uses `raise RuntimeError(...)` not `assert`
    - All 31 plugins enumerated in 112-RESEARCH.md are covered by the flag audit test (test failure message would list any missing one)
    - test_perf03_migration.py includes behavioral tests for kalman_trend and garch_volatility (or equivalent) that CALL the plugin twice with state propagation and ASSERT the second output differs from cold-start — `grep -n "behavioral\|second.*call\|cold.start" tests/unit/intelligence/test_perf03_migration.py` shows the tests exist
    - The behavioral tests pass (state actually propagates — not a flag-only check)
  </acceptance_criteria>
  <done>Pipeline refuses to start if any incremental plugin has not confirmed PERF-03 migration; all 31 are confirmed compliant; behavioral tests for 2+ plugins prove actual state propagation (not flag-only compliance).</done>
</task>

<task type="auto">
  <name>Task 3: Remove SETUP_PRIORITY (data-driven ranking + warm-up penalty), set long_bias=False, then bump SIGNAL_SCHEMA_VERSION (LAST)</name>
  <read_first>
    - src/intelligence/pipeline/ranker.py (line 65-79: `adjusted_rank = priority * perf_multiplier`; perf_weights dict keyed by (plugin_name, tf, symbol) -> float; note it currently has NO sample_size, metadata sets sample_size:0)
    - src/intelligence/trading/aggregator.py (SETUP_PRIORITY dict at line 35; usages lines 475-598; the all_ranked construction and active derivation)
    - src/intelligence/pipeline/winner_selector.py (long_bias=True default at line 20; tiebreak logic)
    - src/intelligence/setup_performance_updater.py (lines 99-158: _compute_perf_multipliers, run_setup_performance_update; perf_weights is dict[plugin -> float]; eligibility WHERE sample_size >= 30)
    - src/intelligence/CLAUDE.md ("Aggregator active must come from all_ranked"; "Setups with sample_size < 30 use multiplier=1.0")
    - docs/plans/2026-06-02-intelligence-pipeline-signal-integrity.md (sections 1-F, 2-C; lines 148-155, 230-239; D-06 atomic co-deploy; D-16 warm-up penalty)
  </read_first>
  <action>
    SETUP_PRIORITY REMOVAL + WARM-UP PENALTY (2-C, D-16):
    - Remove the `SETUP_PRIORITY` dict from `src/intelligence/trading/aggregator.py` and all references to it in aggregator.py and `src/intelligence/pipeline/ranker.py` (including the `from ... import SETUP_PRIORITY` in ranker.py line 11).
    - In ranker.py change `adjusted_rank = priority * perf_multiplier` to `adjusted_rank = perf_multiplier`. Update docstrings (lines 3, 31, 52) to drop the SETUP_PRIORITY reference.
    - Warm-up penalty (D-16): the ranker needs sample_size to penalize unvalidated setups. Extend the perf_weights interface to carry sample_size: change `run_setup_performance_update` / `_compute_perf_multipliers` in setup_performance_updater.py to return `dict[(plugin, tf, symbol) -> (perf_multiplier: float, sample_size: int)]` (or add a parallel `sample_sizes` dict — Claude's discretion, but pick one and apply consistently). In ranker.py: if `sample_size < 30`, force `perf_multiplier = 0.5` (warm-up penalty, below neutral). Above-neutral ranking requires `sample_size >= 30` AND positive performance (the existing bootstrap_ci / Sharpe-rank logic already gates the upper band — leave that intact, just add the 0.5 floor for under-30). Update the ranker metadata that currently hardcodes `sample_size: 0` to emit the real sample_size.

    LONG_BIAS (1-F, D-06):
    - In `src/intelligence/pipeline/winner_selector.py` change the `long_bias: bool = True` default (line 20) to `long_bias: bool = False`. Where long_bias previously broke ties toward longs, use `confidence` as the secondary sort key (descending) after the primary adjusted_rank ordering. Update any caller that relies on the old True default.

    SIGNAL_SCHEMA_VERSION BUMP (D-03 — MUST BE THE FINAL EDIT IN THIS PLAN):
    - Only after ALL of the above (and Tasks 1-2) are complete: in `src/intelligence/trading/signal_schema.py` change `SIGNAL_SCHEMA_VERSION = "v1"` to `SIGNAL_SCHEMA_VERSION = "v2"` (TEXT, not integer — the signal_ledger.signal_schema_version column is `text NOT NULL` and signal_replay_auditor compares it as a string).
    - Before/at bump, run the transition-window query and LOG the count: `SELECT COUNT(*) FROM signal_ledger WHERE signal_schema_version = 'v1' AND exit_at IS NULL` — log the in-flight active-signal count so any in-flight 'v1' signals are visible to the operator (they will be evaluated by the replay auditor). This logging can be a one-shot helper script or a startup log line; document which in the SUMMARY.
  </action>
  <verify>
    grep -rn "SETUP_PRIORITY" src/ services/ ; echo "exit=$?" && grep -n "long_bias" src/intelligence/pipeline/winner_selector.py | head -3 && .venv/bin/python -c "from src.intelligence.trading.signal_schema import SIGNAL_SCHEMA_VERSION; assert SIGNAL_SCHEMA_VERSION == 'v2'; print('version OK')" && .venv/bin/pytest tests/unit/intelligence/ -q -k "rank or winner or aggregat" 2>&1 | tail -5
  </verify>
  <acceptance_criteria>
    - `grep -r "SETUP_PRIORITY" src/ services/` returns empty (gate 2-C)
    - `grep -n "adjusted_rank = perf_multiplier" src/intelligence/pipeline/ranker.py` returns the line (no `priority *` factor)
    - ranker applies `perf_multiplier = 0.5` when `sample_size < 30` (grep confirms the warm-up penalty branch)
    - `grep -n "long_bias" src/intelligence/pipeline/winner_selector.py` shows `long_bias: bool = False` as the default
    - `.venv/bin/python -c "from src.intelligence.trading.signal_schema import SIGNAL_SCHEMA_VERSION; print(SIGNAL_SCHEMA_VERSION)"` prints `v2` (text, not 2)
    - The transition-window count query is executed/logged at bump time (SUMMARY records the count observed)
    - SIGNAL_SCHEMA_VERSION was edited LAST (commit order / SUMMARY confirms it follows Tasks 1-2 and the SETUP_PRIORITY + long_bias edits)
  </acceptance_criteria>
  <done>Ranking is fully data-driven with a warm-up penalty, directional bias removed, and SIGNAL_SCHEMA_VERSION is "v2" as the final atomic step.</done>
</task>

</tasks>

<verification>
- Gate 1-A: apply_calibration not in SignalProcessor.process()
- Gate 1-B: intelligence_pipeline_quality_floor_rejections_total counter exists and increments on rejection; floor loaded from file (not inline DB call); DAG Invariant #3 preserved
- Gate 1-C: `_I1_ALIAS_MAP` importable
- Gate 1-D: test_perf03_migration.py passes for all incremental plugins (flag audit AND behavioral state tests)
- Gate 2-C: `grep -r "SETUP_PRIORITY" src/ services/` empty
- SIGNAL_SCHEMA_VERSION == "v2" and was the last edit
- `.venv/bin/pytest tests/unit/ -q` green
</verification>

<success_criteria>
- Design B calibration: CIS-level, calibrated_confidence from calibrated_cis
- Empirical quality floor via DAG-compliant bootstrap helper (no DB access from pipeline daemon); rejection counter
- PERF-03 startup enforcement + all 31 plugins compliant + passing flag-audit test + behavioral state-propagation tests for 2+ plugins
- _I1_ALIAS_MAP exists and validates at import
- Data-driven ranking, warm-up penalty 0.5 for sample_size < 30, long_bias=False
- SIGNAL_SCHEMA_VERSION = "v2" (last edit, with in-flight count logged)
- PIPE-INT-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/112-intelligence-pipeline-signal-integrity/112-02-SUMMARY.md`
</output>
