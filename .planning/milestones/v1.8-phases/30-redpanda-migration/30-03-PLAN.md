---
phase: 30-redpanda-migration
plan: 03
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - services/signal_generator_service.py
  - services/signal_lifecycle_service.py
  - services/ai_narrative_service.py
  - tests/unit/service_tests/test_signal_generator_service.py
autonomous: true
requirements:
  - KAFKA-06
  - KAFKA-08

must_haves:
  truths:
    - "signal_generator_service consumes from dev.intelligence and dev.market.ticks (not Redis streams)"
    - "signal_generator_service maintains _live_quotes in-process dict updated from ticks topic"
    - "signal_generator_service publishes to dev.signals and dev.signals.aggregated with key SYMBOL:TF"
    - "signal_generator_service no longer calls redis HGETALL for price:SYMBOL:latest"
    - "signal_lifecycle_service consumes from dev.market.bars and dev.signals.aggregated (not Redis)"
    - "signal_lifecycle_service publishes terminal events to dev.signals.aggregated and dev.llm.outcomes"
    - "ai_narrative_service consumes from dev.intelligence and publishes to dev.narratives and dev.llm.calls"
    - "ai_narrative_service maintains _llm_scores_cache in-process dict seeded from llm_model_scores DB table at startup"
    - "All 1659+ unit tests still pass"
  artifacts:
    - path: "services/signal_generator_service.py"
      provides: "Kafka consumer for intelligence + ticks; producer for signals + signals.aggregated; _live_quotes dict"
      contains: "_live_quotes"
    - path: "services/signal_lifecycle_service.py"
      provides: "Kafka consumer for market.bars + signals.aggregated; producer for signals.aggregated + llm.outcomes"
      contains: "KafkaConsumerClient"
    - path: "services/ai_narrative_service.py"
      provides: "Kafka consumer for intelligence; producer for narratives + llm.calls; in-process _llm_scores_cache"
      contains: "_llm_scores_cache"
  key_links:
    - from: "services/signal_generator_service.py"
      to: "_live_quotes dict"
      via: "KafkaConsumerClient ticks topic handler: self._live_quotes[symbol] = tick_payload"
      pattern: "_live_quotes"
    - from: "services/signal_generator_service.py"
      to: "dev.signals.aggregated"
      via: "KafkaProducerClient.publish(topic_signals_aggregated(env), signal_dict, key=message_key(symbol, tf))"
      pattern: "topic_signals_aggregated"
    - from: "services/ai_narrative_service.py"
      to: "_llm_scores_cache"
      via: "DB warm from llm_model_scores at startup; reads self._llm_scores_cache[call_type][regime][model] instead of Redis HGETALL"
      pattern: "_llm_scores_cache"
---

<objective>
Migrate signal_generator_service, signal_lifecycle_service, and ai_narrative_service from DragonflyDB to Redpanda. This plan also handles the price:SYMBOL:latest Redis hash removal (signal_generator gains _live_quotes in-process dict from ticks topic) and the llm_scores_cache Redis hash removal (ai_narrative gains in-process dict seeded from DB).

Purpose: All business logic services now produce and consume via Redpanda. Only the writer services and SSE remain on DragonflyDB — they migrate in Plan 4.
Output: 3 services migrated. _live_quotes dict replaces Redis HGETALL. _llm_scores_cache replaces Redis HSET/HGETALL. All tests pass.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-redpanda-migration/30-CONTEXT.md
@.planning/phases/30-redpanda-migration/30-RESEARCH.md
@.planning/phases/30-redpanda-migration/30-02-SUMMARY.md

<interfaces>
<!-- KafkaProducerClient / KafkaConsumerClient from src/core/kafka_utils.py — same API as Plan 2 -->
<!-- topic_* builders from src/core/stream_keys.py: -->
<!--   topic_intelligence(env_name), topic_market_ticks(env_name), topic_market_bars(env_name) -->
<!--   topic_signals(env_name), topic_signals_aggregated(env_name) -->
<!--   topic_narratives(env_name), topic_llm_calls(env_name), topic_llm_outcomes(env_name) -->
<!--   message_key(symbol, timeframe=None) -> str -->

<!-- signal_generator_service current Redis dependencies to remove: -->
<!--   redis HGETALL(quote_latest(env_prefix, symbol)) -> dict  — replace with self._live_quotes.get(symbol) -->
<!--   redis GET(setup_performance_weights_cache(env_prefix)) -> JSON str  — KEEP for now; Plan 5 removes -->
<!--   redis GET(drift_ks(...)) -> severity str                — KEEP for now; Plan 5 removes -->
<!--   NOTE: setup_performance_weights_cache and drift_ks Redis reads stay in this plan. -->
<!--   signal_generator_service still needs a Redis client for THOSE TWO reads through Plan 4. -->
<!--   Only the stream operations (xreadgroup, xadd) and HGETALL(price) are removed here. -->

<!-- signal_generator_service multi-topic consumer pattern (RESEARCH.md Pattern 3): -->
<!--   consumer = KafkaConsumerClient( -->
<!--     topic_intelligence(env_name), -->
<!--     topic_market_ticks(env_name),   # populates _live_quotes -->
<!--     bootstrap_servers=..., group_id="signal_generator_group", auto_offset_reset="latest" -->
<!--   ) -->
<!--   async for topic, key, payload in consumer.messages(): -->
<!--     if topic == topic_intelligence(env_name): -->
<!--       symbol, tf = key.split(":", 1) -->
<!--       await handle_intelligence(symbol, tf, payload) -->
<!--     elif topic == topic_market_ticks(env_name): -->
<!--       symbol = key  # tick key is SYMBOL only (no tf) -->
<!--       self._live_quotes[symbol] = payload  # keep only latest -->

<!-- ai_narrative_service Redis dependencies to remove: -->
<!--   Redis HSET(llm_scores_cache(env_prefix, call_type, regime), model_name, score_json) -->
<!--   Redis HGETALL(llm_scores_cache(env_prefix, call_type, regime)) -->
<!--   Replace: self._llm_scores_cache: dict[str, dict[str, dict]] keyed [call_type][regime][model] -->
<!--   DB warm on startup: SELECT call_type, model, regime, win_rate, avg_pnl_r, sample_size, p_value -->
<!--     FROM llm_model_scores WHERE p_value < 0.05; populate self._llm_scores_cache -->
<!--   Periodic refresh: re-warm from DB every 15 min (same as current llm_model_scores refresh interval) -->
<!--   KEEP Redis client in ai_narrative_service ONLY if it still publishes to streams — -->
<!--   after migration it no longer does, so Redis client can be fully removed from this service -->

<!-- signal_lifecycle_service: consumes market bars AND signals.aggregated; publishes outcomes -->
<!--   Remove: xreadgroup calls; add: KafkaConsumerClient(topic_market_bars, topic_signals_aggregated) -->
<!--   Remove: xadd to signals:SYMBOL:TF:aggregated for terminal events -->
<!--   Add: KafkaProducerClient.publish to topic_signals_aggregated + topic_llm_outcomes -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 1): Migrate signal_generator_service — remove Redis HGETALL, add _live_quotes, switch to Redpanda streams</name>
  <files>
    services/signal_generator_service.py
    tests/unit/service_tests/test_signal_generator_service.py
  </files>
  <behavior>
    - Remove all Redis xreadgroup/xadd stream operations from signal_generator_service
    - Add self._live_quotes: dict[str, dict] = {} instance variable (initialized in __init__ or _setup)
    - Replace HGETALL(quote_latest(env_prefix, symbol)) with self._live_quotes.get(symbol) (returns dict or None — same semantics as HGETALL miss)
    - Add KafkaConsumerClient subscribed to BOTH topic_intelligence(env_name) AND topic_market_ticks(env_name)
    - In message dispatch loop: topic == topic_intelligence → handle intelligence (existing business logic); topic == topic_market_ticks → self._live_quotes[key] = payload (key is SYMBOL without TF)
    - Add KafkaProducerClient; replace xadd to signals/signals_aggregated with publish to topic_signals(env_name) and topic_signals_aggregated(env_name) with key=message_key(symbol, tf)
    - KEEP: Redis client for setup_performance_weights_cache GET and drift_ks GET — these Redis reads remain until Plan 5. The Redis client is still needed.
    - Remove: Redis client stream operations only (xreadgroup, xadd, ensure_consumer_group_with_reset)
    - tests: update mocks to AsyncMock KafkaProducerClient.publish; add test asserting KAFKA-06: _live_quotes updated when ticks message arrives on ticks topic
    - Do NOT remove redis client entirely — still needed for perf weights + drift reads
  </behavior>
  <action>
    Read services/signal_generator_service.py fully. Map all Redis usages: (1) stream xreadgroup/xadd — replace with Kafka; (2) HGETALL for price — replace with _live_quotes; (3) GET for perf_weights and drift_ks — keep with Redis.

    Add _live_quotes dict. Wire KafkaConsumerClient for multi-topic consume. Wire KafkaProducerClient for signal publish.

    Update tests/unit/service_tests/test_signal_generator_service.py: mock KafkaConsumerClient; add test for KAFKA-06 (_live_quotes updated from tick message); keep existing tests passing by updating mock targets.

    Run unit suite.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && .venv/bin/pytest tests/unit/service_tests/test_signal_generator_service.py -x -v && .venv/bin/pytest tests/unit/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>KAFKA-06 test passes: _live_quotes updated from ticks topic message. signal_generator_service has no Redis stream operations. Full unit suite passes (1659+).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1): Migrate signal_lifecycle_service and ai_narrative_service to Redpanda; replace llm_scores_cache with in-process dict</name>
  <files>
    services/signal_lifecycle_service.py
    services/ai_narrative_service.py
  </files>
  <behavior>
    - signal_lifecycle_service:
      - Replace xreadgroup consume loops (market bars + signals.aggregated) with KafkaConsumerClient(*[topic_market_bars(env_name), topic_signals_aggregated(env_name)], ...)
      - In message dispatch: topic == market.bars → bar evaluation loop (existing business logic); topic == signals.aggregated → signal tracking update
      - Replace xadd to signals:SYMBOL:TF:aggregated (terminal events) with KafkaProducerClient.publish(topic_signals_aggregated(env_name), event_dict, key=message_key(symbol, tf))
      - Replace xadd to llm_outcomes:stream with KafkaProducerClient.publish(topic_llm_outcomes(env_name), outcome_dict, key=None)
      - Remove ensure_consumer_group_with_reset calls; remove Redis client entirely from this service (no remaining Redis ops after migration)
    - ai_narrative_service:
      - Replace xreadgroup consume loop with KafkaConsumerClient(topic_intelligence(env_name), ...)
      - Replace xadd to narratives + llm_calls with KafkaProducerClient.publish to topic_narratives(env_name) and topic_llm_calls(env_name)
      - Replace Redis HSET/HGETALL for llm_scores_cache with self._llm_scores_cache: dict[str, dict[str, dict]] = {}
      - Add _warm_llm_scores_cache_from_db() async method: asyncpg query against llm_model_scores table (SELECT call_type, model, regime, win_rate, avg_pnl_r, sample_size, p_value WHERE p_value < 0.05); populate self._llm_scores_cache[call_type][regime][model]
      - Call _warm_llm_scores_cache_from_db() at startup; schedule periodic re-warm every 15 min (asyncio.create_task with sleep loop)
      - Remove Redis client entirely from ai_narrative_service (no remaining Redis ops)
      - Remove ensure_consumer_group_with_reset calls
      - All existing llm_scores_cache write/read sites updated to use self._llm_scores_cache in-memory
  </behavior>
  <action>
    Read services/signal_lifecycle_service.py fully. Map all Redis operations. Replace stream ops with Kafka. Remove Redis client.

    Read services/ai_narrative_service.py fully. Map all Redis operations including HSET/HGETALL for llm_scores_cache. Replace stream ops with Kafka. Replace cache ops with in-process dict + DB warm.

    The DB warm uses asyncpg (already a dependency). Connect via settings.database_url, register JSONB codecs if needed (same pattern as feature_writer_service).

    Run unit suite after changes.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && .venv/bin/python -c "import services.signal_lifecycle_service; import services.ai_narrative_service; print('imports ok')" && .venv/bin/pytest tests/unit/service_tests/test_signal_lifecycle_service.py tests/unit/service_tests/test_ai_narrative_service.py -x -v && .venv/bin/pytest tests/unit/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>signal_lifecycle_service and ai_narrative_service import without Redis stream operations. _llm_scores_cache is an in-process dict in ai_narrative_service. All existing tests pass. Full suite passes (1659+).</done>
</task>

</tasks>

<verification>
1. `grep -n "xreadgroup\|xadd\|xrevrange\|HSET\|HGETALL\|llm_scores_cache\|quote_latest" /home/bg/dev/indicagent/services/signal_generator_service.py services/signal_lifecycle_service.py services/ai_narrative_service.py` — no stream/hash operations remain in lifecycle or narrative; signal_generator has no stream/HGETALL ops (perf_weights and drift GET remain)
2. `grep "_live_quotes" /home/bg/dev/indicagent/services/signal_generator_service.py` — present
3. `grep "_llm_scores_cache" /home/bg/dev/indicagent/services/ai_narrative_service.py` — present
4. `.venv/bin/pytest tests/unit/ -x -q` — 1659+ pass
</verification>

<success_criteria>
- 3 services migrated to Redpanda
- _live_quotes dict in signal_generator replaces Redis HGETALL for price lookup
- _llm_scores_cache in-process dict in ai_narrative replaces Redis HSET/HGETALL
- signal_generator still has Redis client for perf_weights + drift_ks (removed in Plan 5)
- KAFKA-06 covered in test_signal_generator_service.py
- All 1659+ unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/30-redpanda-migration/30-03-SUMMARY.md`
</output>
