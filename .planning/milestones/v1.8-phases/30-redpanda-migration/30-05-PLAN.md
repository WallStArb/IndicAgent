---
phase: 30-redpanda-migration
plan: 05
type: execute
wave: 5
depends_on: ["30-04"]
files_modified:
  - production/docker-compose.yml
  - requirements.txt
  - src/core/stream_keys.py
  - src/core/stream_utils.py
  - src/config/settings.py
  - services/signal_generator_service.py
  - services/drift_monitor_service.py
autonomous: false
requirements:
  - KAFKA-08

must_haves:
  truths:
    - "DragonflyDB container removed from docker-compose.yml"
    - "redis[hiredis] removed from requirements.txt"
    - "No remaining redis.asyncio imports in any service file"
    - "signal_generator_service reads perf weights from setup_performance DB table (not Redis GET)"
    - "drift_monitor_service writes KS drift state to drift_state DB table (not Redis key)"
    - "signal_generator_service reads drift penalty from drift_state DB table in-memory dict (refreshed every 4h)"
    - "stream_keys.py has all Redis-specific helpers removed (llm_scores_cache, setup_performance_weights_cache, drift_ks, drift_cusum, quote_latest, get_stream_maxlen)"
    - "stream_utils.py is empty or deleted (ensure_consumer_group_with_reset fully removed)"
    - "E2E: historical_backfill --replay-only populates intelligence_features and signal_ledger via Redpanda pipeline"
    - "All 1659+ unit tests still pass"
  artifacts:
    - path: "production/docker-compose.yml"
      provides: "Redpanda only — no DragonflyDB service"
      contains: "redpanda"
    - path: "src/core/stream_keys.py"
      provides: "Topic builders only — all Redis key helpers removed"
      contains: "topic_market_ticks"
    - path: "src/core/stream_utils.py"
      provides: "Empty or deleted — ensure_consumer_group_with_reset gone"
    - path: "services/signal_generator_service.py"
      provides: "Reads perf_weights from DB; reads drift_penalty from in-memory dict refreshed from DB"
      contains: "_load_perf_weights_from_db"
    - path: "services/drift_monitor_service.py"
      provides: "Writes KS drift severity to drift_state table (not Redis)"
      contains: "drift_state"
  key_links:
    - from: "services/drift_monitor_service.py"
      to: "drift_state table"
      via: "INSERT/UPDATE drift_state (symbol, tf, ks_severity, cusum_severity, updated_at)"
      pattern: "drift_state"
    - from: "services/signal_generator_service.py"
      to: "_drift_penalties dict"
      via: "_refresh_drift_penalties_from_db(): SELECT symbol, tf, ks_severity FROM drift_state; updates self._drift_penalties; called at startup + every 4h"
      pattern: "_drift_penalties"
    - from: "services/signal_generator_service.py"
      to: "_load_perf_weights_from_db()"
      via: "SELECT setup_plugin, win_rate, avg_pnl_r FROM setup_performance WHERE sample_size >= 30; replaces Redis GET"
      pattern: "_load_perf_weights_from_db"
---

<objective>
Complete the migration by removing DragonflyDB entirely: drop the container from docker-compose, remove redis[hiredis] from requirements, clean up all remaining Redis non-stream dependencies (perf weights cache → DB read, drift state → DB table), and remove Redis-specific helpers from stream_keys.py and stream_utils.py.

Purpose: DragonflyDB is fully removed from the stack. The only persistent stores are Redpanda (hot tier) and TimescaleDB (cold tier).
Output: Zero Redis dependencies. All services use only Kafka + DB. E2E pipeline verified. DragonflyDB container gone.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-redpanda-migration/30-CONTEXT.md
@.planning/phases/30-redpanda-migration/30-RESEARCH.md
@.planning/phases/30-redpanda-migration/30-04-SUMMARY.md

<interfaces>
<!-- Three remaining Redis non-stream dependencies (from RESEARCH.md Pitfalls 6, 7, 8): -->

<!-- 1. setup_performance_weights_cache (Pitfall 8) — signal_generator_service: -->
<!--    Current: redis_client.get(setup_performance_weights_cache(env_prefix)) → JSON str of perf weights -->
<!--    Fix: _load_perf_weights_from_db() reads from setup_performance table directly: -->
<!--      SELECT setup_plugin, win_rate, avg_pnl_r, sample_size, sharpe -->
<!--      FROM setup_performance WHERE sample_size >= 30 -->
<!--    Called at startup and every 60 min (unchanged schedule). No Redis. -->
<!--    Remove redis_client from signal_generator_service entirely after this. -->

<!-- 2. drift_ks Redis keys (Pitfall 7) — drift_monitor_service + signal_generator_service: -->
<!--    Current: drift_monitor writes redis.set(drift_ks(env_prefix, symbol, tf), severity_str) -->
<!--             signal_generator reads redis.get(drift_ks(env_prefix, symbol, tf)) -->
<!--    Fix: Add drift_state DB table: -->
<!--      CREATE TABLE IF NOT EXISTS drift_state ( -->
<!--        symbol TEXT NOT NULL, tf TEXT NOT NULL, -->
<!--        ks_severity TEXT NOT NULL DEFAULT 'none', -->
<!--        cusum_severity TEXT NOT NULL DEFAULT 'none', -->
<!--        updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), -->
<!--        PRIMARY KEY (symbol, tf) -->
<!--      ); -->
<!--    drift_monitor: INSERT INTO drift_state (symbol, tf, ks_severity, updated_at) VALUES (...) -->
<!--                   ON CONFLICT (symbol, tf) DO UPDATE SET ks_severity=..., updated_at=NOW() -->
<!--    signal_generator: _refresh_drift_penalties_from_db(): -->
<!--      SELECT symbol, tf, ks_severity FROM drift_state; populate self._drift_penalties dict -->
<!--      Called at startup + every 4h (asyncio.create_task with sleep loop) -->
<!--      _read_drift_penalty(symbol, tf): returns self._drift_penalties.get((symbol, tf), "none") -->

<!-- 3. drift_cusum Redis keys: -->
<!--    Current: CUSUMMonitor writes redis.set(drift_cusum(env_prefix, setup_plugin), ...) -->
<!--    Fix: use cusum_severity column in same drift_state table, keyed by setup_plugin in tf column -->
<!--    OR simplest: store CUSUM state in a separate dict in signal_generator (in-memory) -->
<!--    Recommendation: drift_state table handles both KS (per symbol/tf) and CUSUM (per setup_plugin) -->
<!--    Use a separate drift_cusum_state table or add a text primary key that works for both -->
<!--    Simplest: one drift_state table; for CUSUM rows, symbol=setup_plugin, tf="cusum" sentinel -->

<!-- Migration SQL: production/migrations/030_drift_state.sql (new file) -->
<!-- Apply via: docker cp .../030_drift_state.sql timescaledb:/tmp/ && docker exec timescaledb psql -f /tmp/... -->

<!-- stream_keys.py cleanup — REMOVE these functions in Plan 5: -->
<!--   quote_latest(), llm_scores_cache(), setup_performance_weights_cache() -->
<!--   drift_ks(), drift_cusum(), get_stream_maxlen() -->
<!--   ticks_pattern(), market_pattern(), indicators_pattern() — and other pattern helpers -->
<!--   narratives_group(), system_events() — if no remaining callers -->
<!--   Keep only: env_prefix (old colon version if still needed), new topic_* functions, message_key() -->

<!-- stream_utils.py cleanup: ensure_consumer_group_with_reset() is the only function -->
<!-- Either delete the file entirely or replace with empty module + comment pointing to kafka_utils.py -->
<!-- Tests in tests/unit/core/test_stream_utils.py may need updating or deletion -->

<!-- Settings cleanup: remove redis_host, redis_port, redis_db, redis_max_connections if no callers remain -->
<!-- WARNING: check all callers first with grep before removing Settings fields -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 1): Create drift_state DB migration + replace drift Redis keys with DB table</name>
  <files>
    production/migrations/030_drift_state.sql
    services/drift_monitor_service.py
    services/signal_generator_service.py
  </files>
  <behavior>
    - Create production/migrations/030_drift_state.sql:
      CREATE TABLE IF NOT EXISTS drift_state (symbol TEXT NOT NULL, tf TEXT NOT NULL, ks_severity TEXT NOT NULL DEFAULT 'none', cusum_severity TEXT NOT NULL DEFAULT 'none', updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), PRIMARY KEY (symbol, tf));
      For CUSUM rows: use symbol=setup_plugin_name, tf='_cusum' as primary key (sentinel)
    - drift_monitor_service: replace redis.set(drift_ks(...), severity) with asyncpg INSERT INTO drift_state ON CONFLICT DO UPDATE; remove Redis client from drift_monitor_service entirely (all its state was Redis keys); add asyncpg DB connection at startup
    - drift_monitor_service: replace redis.set(drift_cusum(...), ...) with UPDATE drift_state SET cusum_severity=... WHERE symbol=setup_plugin AND tf='_cusum' (INSERT ON CONFLICT DO UPDATE)
    - signal_generator_service: add self._drift_penalties: dict[tuple[str,str], str] = {}; add _refresh_drift_penalties_from_db(): asyncpg SELECT symbol, tf, ks_severity FROM drift_state WHERE tf != '_cusum'; updates self._drift_penalties[(symbol, tf)] = severity; call at startup + every 4h via asyncio.create_task
    - signal_generator_service: replace _read_drift_penalty(symbol, tf) body from redis.get(drift_ks(...)) to return self._drift_penalties.get((symbol, tf), "none")
    - signal_generator_service: after these changes, check if any Redis operations remain; if setup_performance_weights_cache is the only remaining Redis op, remove Redis client too (or defer to same task)
    - Do NOT remove redis Settings fields yet — verify no other callers first
  </behavior>
  <action>
    Create production/migrations/030_drift_state.sql. Apply migration: docker cp to timescaledb container, execute psql.

    Read drift_monitor_service.py fully. Replace all Redis writes with asyncpg writes to drift_state table.

    Read signal_generator_service.py fully (updated from Plan 3). Replace _read_drift_penalty Redis GET with dict lookup. Add _refresh_drift_penalties_from_db() method. Schedule 4h refresh task.

    Run unit tests after changes.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && .venv/bin/pytest tests/unit/service_tests/test_signal_generator_service.py -x -v && .venv/bin/pytest tests/unit/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>drift_state migration file created. drift_monitor_service has no Redis writes. signal_generator_service reads drift penalty from in-process dict refreshed from DB. Unit suite passes (1659+).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1): Replace perf_weights Redis cache with direct DB read in signal_generator; clean up stream_keys.py and stream_utils.py</name>
  <files>
    services/signal_generator_service.py
    src/core/stream_keys.py
    src/core/stream_utils.py
    tests/unit/core/test_stream_utils.py
    tests/unit/test_stream_keys.py
  </files>
  <behavior>
    - signal_generator_service: replace _load_perf_weights() body: remove redis_client.get(setup_performance_weights_cache(...)); replace with asyncpg query: SELECT setup_plugin, win_rate, avg_pnl_r FROM setup_performance WHERE sample_size >= 30; build weights dict in same format as before; remove Redis client from signal_generator_service entirely (after verifying no remaining Redis ops)
    - stream_keys.py: REMOVE these functions (no remaining callers after all 8 services migrated): quote_latest(), llm_scores_cache(), setup_performance_weights_cache(), drift_ks(), drift_cusum(), get_stream_maxlen(); also remove: ticks_pattern(), market_pattern(), indicators_pattern(), intelligence_pattern(), intelligence_i7_pattern(), intelligence_i8_pattern(), signals_pattern(), patterns_pattern(), narratives_pattern(); also remove old live_tick(), market(), indicators(), intelligence(), intelligence_i7(), intelligence_i8(), signals(), signals_aggregated(), narratives(), narratives_group(), system_events() — all replaced by topic_* functions; also remove old prefix() (colon-based) if no callers
    - WARNING: Before removing any function from stream_keys.py, run grep to confirm no remaining imports of that function across services/ and src/api/
    - stream_utils.py: replace entire file content with a deprecation stub: module-level comment saying "Replaced by kafka_utils.py in Phase 30. This file retained as empty stub to avoid ImportError if any test imports it." Then empty body. Delete the test file tests/unit/core/test_stream_utils.py only if all its tests are no longer meaningful (they test ensure_consumer_group_with_reset which no longer exists); if deleting, confirm count of remaining tests stays 1659+
    - tests/unit/test_stream_keys.py: remove tests for deleted stream_keys functions (quote_latest, get_stream_maxlen, etc.); keep and expand topic_* function tests
    - settings.py: grep for redis_host/redis_port/redis_db/redis_max_connections callers across codebase; if zero service files import them, remove from Settings. If any test file uses them, keep but add a deprecation comment.
  </behavior>
  <action>
    First: grep for all remaining redis.asyncio imports across services/: grep -rn "redis.asyncio\|from redis import\|import redis" services/. Fix any remaining ones.

    Replace _load_perf_weights() in signal_generator_service with asyncpg DB read.

    Clean stream_keys.py: remove Redis helper functions. Verify with grep before each removal.

    Replace stream_utils.py with deprecation stub.

    Update test_stream_keys.py to remove tests for deleted functions.

    Run full unit suite and confirm test count.
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && grep -rn "redis.asyncio\|import redis\b" services/ src/core/ src/api/ 2>/dev/null | grep -v "ibkr.py\|#" && .venv/bin/pytest tests/unit/ -v 2>&1 | tail -15</automated>
  </verify>
  <done>No redis.asyncio imports in services/, src/core/, or src/api/ (except ibkr.py if applicable). signal_generator reads perf_weights from DB. stream_keys.py has only topic_* functions and message_key(). stream_utils.py is a deprecation stub. Full unit suite passes.</done>
</task>

<task type="auto">
  <name>Task 3 (Wave 2): Remove DragonflyDB from docker-compose; remove redis[hiredis] from requirements</name>
  <files>
    production/docker-compose.yml
    requirements.txt
  </files>
  <action>
    Read production/docker-compose.yml fully. Remove the entire `dragonfly:` service block. Remove `dragonfly-data:` from the volumes section.

    Read requirements.txt fully. Remove the line containing `redis[hiredis]` (and any plain `redis` dependency). Keep `aiokafka>=0.13.0`.

    Confirm docker-compose.yml still has: timescaledb, ollama, prometheus, grafana, redpanda services. Confirm redpanda-data volume present.

    Run unit tests to confirm nothing imported redis as a required dependency.

    NOTE: Do NOT restart Docker services — this is a config-only change. User will restart services manually with: cd production && docker compose up -d
  </action>
  <verify>
    <automated>cd /home/bg/dev/indicagent && grep "dragonfly" production/docker-compose.yml && echo "FAIL: dragonfly still present" || echo "PASS: dragonfly removed" && grep "redis\[hiredis\]" requirements.txt && echo "FAIL: redis still present" || echo "PASS: redis removed" && .venv/bin/pytest tests/unit/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>No dragonfly service in docker-compose.yml. No redis[hiredis] in requirements.txt. Unit suite still passes (1659+).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Redpanda migration across all 8 services. DragonflyDB removed from docker-compose.yml and requirements.txt. All Redis non-stream dependencies replaced. stream_keys.py cleaned. Unit tests passing.

    To complete E2E validation, the user needs to:
    1. Restart Docker infrastructure: cd production && docker compose up -d (starts Redpanda, keeps timescaledb/ollama/prometheus/grafana)
    2. Run topic init script: .venv/bin/python production/scripts/kafka_init_topics.py (creates all 11 topics)
    3. Restart all 8 systemd services one at a time: sudo systemctl restart indicagent-{tws,indicator,market-analysis,signal-generator,signal-lifecycle,ai-narrative,feature-writer,llm-writer}
    4. Verify each service starts cleanly: journalctl -u indicagent-indicator -f (should see Kafka consumer group join, no Redis errors)
    5. Run historical replay: .venv/bin/python production/scripts/historical_backfill.py --replay-only --days 1
    6. Verify intelligence_features populated: docker exec timescaledb psql -U postgres -d indicagent -c "SELECT COUNT(*) FROM intelligence_features WHERE created_at > NOW() - INTERVAL '1 hour'"
    7. Open dashboard: verify signal cards update with live data
  </what-built>
  <how-to-verify>
    Step-by-step:
    1. Run: cd /home/bg/dev/indicagent/production && docker compose ps — confirm redpanda running, no dragonfly container
    2. Run: .venv/bin/python production/scripts/kafka_init_topics.py — should print "topics created" or "already exist"
    3. Restart services (ask user to run in terminal if sudo needed)
    4. Run: journalctl -u indicagent-indicator -n 50 — look for "Kafka consumer started" and no "NoBrokersAvailable" errors
    5. Run: .venv/bin/python production/scripts/historical_backfill.py --replay-only --days 1
    6. Run: docker exec timescaledb psql -U postgres -d indicagent -c "SELECT COUNT(*) FROM intelligence_features WHERE created_at > NOW() - INTERVAL '2 hours'"
    7. Confirm count > 0 (pipeline flowing end-to-end through Redpanda)
    8. Open http://localhost:3000 — signal cards should update with live data
  </how-to-verify>
  <resume-signal>Type "approved" if E2E pipeline flows (intelligence_features populated, dashboard updating). Describe any errors observed.</resume-signal>
</task>

</tasks>

<verification>
1. `grep -rn "redis.asyncio\|hiredis\|from redis import\|import redis\b" /home/bg/dev/indicagent/services/ /home/bg/dev/indicagent/src/core/ /home/bg/dev/indicagent/src/api/ | grep -v "ibkr.py\|#"` — no matches
2. `grep "dragonfly" /home/bg/dev/indicagent/production/docker-compose.yml` — no matches
3. `grep "redis\[hiredis\]" /home/bg/dev/indicagent/requirements.txt` — no matches
4. `grep -n "quote_latest\|llm_scores_cache\|setup_performance_weights_cache\|drift_ks\|get_stream_maxlen" /home/bg/dev/indicagent/src/core/stream_keys.py` — no matches
5. `.venv/bin/pytest tests/unit/ -v` — 1659+ tests pass
6. (After E2E): `docker exec timescaledb psql -U postgres -d indicagent -c "SELECT COUNT(*) FROM intelligence_features WHERE created_at > NOW() - INTERVAL '2 hours'"` — count > 0
</verification>

<success_criteria>
- DragonflyDB container removed from docker-compose.yml
- redis[hiredis] removed from requirements.txt
- Zero redis.asyncio imports in services/, src/core/, src/api/ (except src/providers/ibkr.py if applicable)
- stream_keys.py contains only topic_* builders and message_key()
- stream_utils.py is a deprecation stub
- drift_state DB table created; drift_monitor writes to it; signal_generator reads from it
- signal_generator reads perf_weights directly from setup_performance DB table
- All 1659+ unit tests pass
- E2E: intelligence_features populated after replay-only backfill through Redpanda pipeline
- Dashboard receives live updates via SSE Kafka broadcaster
</success_criteria>

<output>
After completion, create `.planning/phases/30-redpanda-migration/30-05-SUMMARY.md`
</output>
