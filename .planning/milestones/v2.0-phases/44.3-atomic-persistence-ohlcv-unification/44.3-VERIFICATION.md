---
phase: 44.3-atomic-persistence-ohlcv-unification
verified: 2026-03-22T00:00:00Z
status: passed
score: 8/8 must-haves verified
gaps: []
human_verification:
  - test: "Live signal_scorecard SSE events visible from intelligence.record"
    expected: "curl -N http://localhost:8000/api/stream/ES/1m shows signal_scorecard events with ranked_signals from BarIntelligenceRecord during market hours"
    why_human: "Market was closed during all phase execution and verification windows — SSE wiring verified via code audit and consumer group state, not live event observation"
  - test: "market_data_ohlcv receiving live 1m bars during market hours"
    expected: "SELECT count(*) FROM market_data_ohlcv WHERE timestamp > now() - interval '5 minutes' returns > 0"
    why_human: "Market was closed (midnight UTC) during Task 2 checkpoint — FeaturePipelineService confirmed subscribed and code path verified, but live data cannot be observed until market open"
  - test: "intelligence_features rows have i7 not null at insert time (post-Phase 44.3)"
    expected: "SELECT count(*) AS total, count(i7) AS i7_populated FROM intelligence_features WHERE computed_at > now() - interval '10 minutes' shows total = i7_populated"
    why_human: "Requires live market-hours traffic through the new BarIntelligenceRecord pipeline — market closed during verification"
---

# Phase 44.3: Atomic Persistence + OHLCV Unification Verification Report

**Phase Goal:** FeatureWriterService consumes `development.intelligence.record` only and performs a single atomic INSERT per bar — no UPSERTs, no partial rows, no race conditions. i8 persistence migrated from FeatureWriterService to LLMWriterService. FeaturePipelineService becomes the sole live writer to `market_data_ohlcv`, creating a single OHLCV ground truth. All `intelligence_features` rows complete at insert time.

**Verified:** 2026-03-22
**Status:** passed (3 items need human verification at next market open — all automated checks pass)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `intelligence_features` has 11 new columns for BarIntelligenceRecord atomic INSERT | VERIFIED | DB query: all 11 columns confirmed in `information_schema.columns` (winner_plugin, winner_confidence, winner_direction, signals_evaluated, signals_after_quality, signals_after_regime, signals_after_tod, signals_after_calibration, ledger_written, i7_computed_at, session_type) |
| 2 | FeatureWriterService subscribes to `development.intelligence.record` only — not intelligence, intelligence.i7, or intelligence.i8 | VERIFIED | `topic_intelligence_record` is the only intelligence topic in subscription list (line 397); no reference to old topic names in process loop |
| 3 | FeatureWriterService performs single atomic INSERT with all 31 columns from BarIntelligenceRecord | VERIFIED | `_INSERT_FEATURE_SQL` contains all 31 columns ($1-$31); `_record_to_insert_params()` builds the matching 31-element tuple; ON CONFLICT DO NOTHING |
| 4 | All i7/i8 two-phase write code removed from FeatureWriterService | VERIFIED | `grep -c "_process_i7_message\|_process_i8_message\|_flush_i7_i8\|_UPSERT_I7_SQL\|_UPSERT_I8_SQL\|_i7_buffer\|_i8_buffer"` returns 0 |
| 5 | LLMWriterService owns i8 persistence via plain UPDATE (not INSERT ON CONFLICT) | VERIFIED | `_UPSERT_I8_SQL` is `UPDATE intelligence_features SET i8 = $4::jsonb WHERE ts = $1 AND symbol = $2 AND tf = $3`; starts with UPDATE; 18 references to the i8 write path in llm_writer_service.py |
| 6 | FeaturePipelineService writes live 1m bars to market_data_ohlcv (HTF excluded) | VERIFIED | `_queue_ohlcv_write()` has `if bar.tf != "1m": return` guard; `_INSERT_OHLCV_SQL` with ON CONFLICT DO NOTHING; flush wired into `_process_market_data` loop and `stop()`; 7 references to OHLCV write path |
| 7 | SSE broadcaster consumes intelligence.record, not intelligence.i7; signal_scorecard built from BarIntelligenceRecord.ranked_signals | VERIFIED | `sse.py` subscribed to `topic_intelligence_record` (lines 24, 107, 163); `_extract_signal_scorecard_payload()` extracts ranked_signals; no `topic_intelligence_i7` reference in sse.py |
| 8 | intelligence.i7 topic retired — producer removed, topic deleted | VERIFIED | No `topic_intelligence_i7` in services/ or src/ (except deprecated definition in stream_keys.py); DEPRECATED comment added to `topic_intelligence_i7()` in stream_keys.py; topic deleted from Redpanda (confirmed by human in plan 04) |

**Score:** 8/8 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `production/migrations/048_intelligence_features_record_columns.sql` | DB migration adding 11 columns to intelligence_features | VERIFIED | File exists; 11 `ADD COLUMN IF NOT EXISTS` statements; applied to live DB confirmed |
| `src/core/stream_keys.py` | `topic_intelligence_record()` function; `topic_intelligence_i7()` marked deprecated | VERIFIED | `topic_intelligence_record` at line 155; DEPRECATED comment at line 64 |
| `services/feature_writer_service.py` | Simplified service: single buffer, atomic INSERT, no i7/i8 paths | VERIFIED | Contains `_record_to_insert_params`; 18 occurrences of new code; 0 occurrences of old two-phase code |
| `tests/unit/service_tests/test_feature_writer_service.py` | Tests for simplified single-buffer logic | VERIFIED | 13 matches for `test_record_to_insert_params`; 24/24 tests pass |
| `services/llm_writer_service.py` | i8 UPSERT capability (UPDATE, not INSERT) | VERIFIED | `_UPSERT_I8_SQL` starts with UPDATE; `_i8_buffer`, `_process_i8_message`, `_flush_i8` all present |
| `tests/unit/service_tests/test_llm_writer_service.py` | Tests for i8 buffering and flush | VERIFIED | `test_process_i8_message` present; 14/14 tests pass |
| `services/feature_pipeline_service.py` | Live OHLCV batch writes: `_queue_ohlcv_write`, `_flush_ohlcv`, `_INSERT_OHLCV_SQL` | VERIFIED | All three present; 1m-only guard; flush in main loop and shutdown |
| `tests/unit/service_tests/test_feature_pipeline_service.py` | OHLCV write tests | VERIFIED | 6 OHLCV-specific tests pass |
| `src/api/routes/sse.py` | SSE subscribed to intelligence.record, not intelligence.i7 | VERIFIED | `topic_intelligence_record` in subscription list; `_extract_signal_scorecard_payload()` method exists |
| `dashboard/src/lib/types.ts` | RankedSignal interface updated with new fields | VERIFIED | `signal_id`, `plugin`, `raw_confidence`, `calibrated_confidence`, `regime_eligible`, `quality_score`, `tod_multiplier`, `adjusted_rank`, `is_winner` all present |
| `dashboard/src/components/signal-scorecard.tsx` | Uses `plugin`, `calibrated_confidence`, `adjusted_rank`, `is_winner` | VERIFIED | All four fields referenced; sorted by `adjusted_rank` descending; no stale field references |
| `tests/unit/api/test_sse_intelligence_record.py` | SSE intelligence.record unit tests | VERIFIED | 10/10 tests pass |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `feature_writer_service.py` | `src/core/stream_keys.py` | `topic_intelligence_record()` | WIRED | Imported at line 35; used in subscription setup at line 397 |
| `feature_writer_service.py` | `intelligence_features` table | `_INSERT_FEATURE_SQL` with 31 params | WIRED | SQL defined lines 51-74; called in `_process_single_message()` via `execute_batch` at line 509 |
| `feature_writer_service.py` | `src/intelligence/schemas.py` | `BarIntelligenceRecord.model_validate_json` | WIRED | 18 occurrences of BarIntelligenceRecord in the file |
| `llm_writer_service.py` | `intelligence_features` table | `UPDATE SET i8 WHERE ts AND symbol AND tf` | WIRED | `_UPSERT_I8_SQL` (UPDATE) at lines 98-102; called at line 640 |
| `llm_writer_service.py` | `src/core/stream_keys.py` | `topic_intelligence_i8()` | WIRED | Imported at line 36; subscription added to topic list |
| `feature_pipeline_service.py` | `market_data_ohlcv` table | `_ohlcv_buffer` batch INSERT ON CONFLICT DO NOTHING | WIRED | `_INSERT_OHLCV_SQL` defined at line 107; `_flush_ohlcv` calls `conn.executemany` at line 595 |
| `feature_pipeline_service.py` | `development.intelligence.record` topic (producer) | publishes BarIntelligenceRecord | WIRED | `topic_intelligence_record` referenced in feature_pipeline_service (source of the records consumed by feature_writer) |
| `src/api/routes/sse.py` | `development.intelligence.record` | `topic_intelligence_record()` in `_build_topic_list()` | WIRED | Three references: import (24), variable (107), subscription (163) |
| `dashboard/src/hooks/use-market-stream.ts` | `src/api/routes/sse.py` | SSE `signal_scorecard` event, `JSON.parse(payload.data)` | WIRED | `es.addEventListener("signal_scorecard", ...)` at line 837; parses to `RankedSignal[]` |

---

### Requirements Coverage

The requirement IDs PIPE-08, PIPE-09, PIPE-10 are **phase-local requirements** defined in `44.3-RESEARCH.md` and the ROADMAP section. They do not appear in the central `.planning/REQUIREMENTS.md` (which uses DATA-/PERF-/INTEL-/etc. namespaces). This is consistent with the spec-driven design pattern for the Phase 44 sub-series.

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PIPE-08 | 44.3-01, 44.3-03, 44.3-04 | FeatureWriterService single atomic INSERT from intelligence.record; SSE rewired | SATISFIED | Zero old two-phase code; 31-column INSERT; SSE consuming intelligence.record; intelligence.i7 retired |
| PIPE-09 | 44.3-01, 44.3-02 | i8 persistence migrated to LLMWriterService via plain UPDATE | SATISFIED | LLMWriterService has full i8 buffer/flush path; UPDATE SQL confirmed (not INSERT ON CONFLICT) |
| PIPE-10 | 44.3-03 | FeaturePipelineService sole live writer to market_data_ohlcv | SATISFIED | `_queue_ohlcv_write` with 1m-only guard; batch INSERT with ON CONFLICT DO NOTHING wired in pipeline |

**Orphaned central requirements:** None — no `.planning/REQUIREMENTS.md` entries are mapped to Phase 44.3.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `services/feature_writer_service.py` | 76-88 | `_UPSERT_ROLL_BOUNDARY_SQL` and `_UPSERT_CROSS_ASSET_SQL` still perform JSONB MERGE UPSERTs | INFO | These are intentional — they write roll boundary metadata and cross-asset features into the i7 JSONB field of existing rows (not creating partial rows). These are NOT the race-condition UPSERTs that were removed. No action needed. |

No blocker or warning anti-patterns found. The roll_boundary and cross_asset UPSERT paths are distinct from the eliminated i7/i8 two-phase partial-row writes — they merge metadata into already-complete rows and are outside the scope of Phase 44.3 removal.

---

### Human Verification Required

The following items require human verification at the next market open (all automated code checks pass):

#### 1. Live signal_scorecard SSE events from intelligence.record

**Test:** `curl -N http://localhost:8000/api/stream/ES/1m 2>/dev/null | grep signal_scorecard | head -5`
**Expected:** Events appear with ranked_signals parsed from BarIntelligenceRecord (fields: signal_id, plugin, calibrated_confidence, adjusted_rank, is_winner)
**Why human:** Market was closed (midnight UTC) during all execution and post-cutover verification — SSE topic routing verified via code audit and consumer group LAG=0 state, but live events were not observable

#### 2. market_data_ohlcv receiving live 1m bars

**Test:** `docker exec timescaledb psql -U postgres -d indicagent -c "SELECT count(*) FROM market_data_ohlcv WHERE timestamp > now() - interval '5 minutes';"`
**Expected:** Count > 0 (non-zero) during RTH
**Why human:** Market closed during verification window; service correctly subscribed and code path verified, but live OHLCV row insertion requires actual 1m bars from FeaturePipelineService

#### 3. intelligence_features rows complete at insert time (i7 not null)

**Test:** `docker exec timescaledb psql -U postgres -d indicagent -c "SELECT count(*) AS total, count(i7) AS i7_populated FROM intelligence_features WHERE computed_at > now() - interval '10 minutes';"`
**Expected:** total = i7_populated (zero rows with i7 IS NULL in recent data)
**Why human:** Requires live market-hours traffic flowing through the new BarIntelligenceRecord pipeline — market closed during post-cutover regression in plan 03

---

### Gaps Summary

No gaps. All automated checks pass across all 4 plans:

- Plan 01: DB migration applied (11 columns verified in DB), FeatureWriterService simplified (0 old code refs, 24/24 tests pass)
- Plan 02: LLMWriterService i8 UPSERT wired (UPDATE not INSERT, 14/14 tests pass)
- Plan 03: FeaturePipelineService OHLCV writes added (1m-only guard, 6/6 OHLCV tests pass)
- Plan 04: SSE rewired (no intelligence.i7 in sse.py, RankedSignal types updated, 10/10 SSE tests pass, intelligence.i7 producer retired)

Full unit suite: 2662/2662 tests pass. Ruff: all checks passed on all modified files.

The 3 human verification items are not gaps — they are live-data confirmations that cannot be performed programmatically when markets are closed. The code path for each is fully implemented and wired.

---

_Verified: 2026-03-22_
_Verifier: Claude (gsd-verifier)_
