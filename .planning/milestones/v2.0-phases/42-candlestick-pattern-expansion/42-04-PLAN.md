---
phase: 42-candlestick-pattern-expansion
plan: 04
type: execute
wave: 2
depends_on:
  - 42-03
files_modified:
  - services/signal_generator_service.py
autonomous: true
requirements:
  - CANDLE-02

must_haves:
  truths:
    - "signal_generator_service loads pattern_reliability weights with 15-min async cache"
    - "Preloaded weights dict injected into frames['pattern_weights'] before I7 plugin loop"
    - "CandlestickPatternSetup.compute_full() remains synchronous — reads frames['pattern_weights']"
    - "DB weights used instead of fallback when DB available; fallback activates gracefully on error"
  artifacts:
    - path: services/signal_generator_service.py
      provides: "Pattern weights cache + frames injection"
      contains: "_pattern_reliability_cache, _pattern_reliability_cache_ts, frames['pattern_weights']"
  key_links:
    - from: signal_generator_service.py
      to: pattern_reliability table
      via: "async _load_pattern_reliability_weights() with 15-min TTL cache"
      pattern: "_pattern_reliability_cache"
    - from: signal_generator_service.py
      to: candlestick_pattern_setup.py
      via: "frames['pattern_weights'] dict injected before I7 loop"
      pattern: "frames\\[.pattern_weights.\\]"
---

<objective>
Add pattern_reliability weight loading to signal_generator_service and inject preloaded weights into frames.

Purpose: Complete the adaptive learning feedback loop while keeping compute_full() synchronous.
The service owns the async DB cache (15-min TTL); the plugin reads from frames["pattern_weights"].
This avoids async protocol violations in the plugin layer.

Critical: This is the ENABLING task for Renaissance-grade "let the system run" — without weight injection,
pattern_reliability table is unused dead code.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-candlestick-pattern-expansion/42-CONTEXT.md
@.planning/phases/42-candlestick-pattern-expansion/42-03-PLAN.md

# Design: Service-level cache, sync plugin

compute_full() in all I7 plugins is SYNCHRONOUS (not awaited at line 1044).
Making it async would break all 27 other I7 plugins. The solution:
- Service loads pattern weights asynchronously (it is already async)
- Service caches the weights dict with 15-min TTL (same pattern as CIS calibration cache)
- Service injects preloaded weights dict into frames["pattern_weights"] before the I7 loop
- Plugin reads frames.get("pattern_weights") synchronously — no DB queries in plugin

# Existing service patterns (reference)

## CIS calibration cache (follow this pattern)
```python
_cis_scorer: CISScorer | None = None
_cis_calibration_cache_ts: datetime | None = None
_cis_calibration_cache_ttl_sec = 1800  # 30 minutes
```

## DB query (asyncpg)
```python
rows = await self.db_manager.execute_query("""
    SELECT pattern_name, base_confidence FROM pattern_reliability
    WHERE is_bootstrap = true OR sample_size >= 30
""")
```

## I7 plugin invocation (current — synchronous, NOT awaited)
```python
result = plugin.compute_full(frames)  # line 1044 — sync call
```

## From 42-03: plugin expects
```python
pattern_weights = frames.get("pattern_weights") or fallback_weights
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pattern_reliability weight cache to signal_generator_service</name>
  <files>services/signal_generator_service.py</files>

  <read_first>
    - services/signal_generator_service.py (full file — find CIS calibration cache pattern)
    - .planning/phases/42-candlestick-pattern-expansion/42-03-PLAN.md (what plugin expects)
  </read_first>

  <action>
    In services/signal_generator_service.py:

    1. Add module-level cache variables near the CIS calibration cache (search for "_cis_calibration_cache_ts"):

    # Phase 42: pattern_reliability weight cache (15-min TTL)
    _pattern_reliability_cache: dict[str, float] | None = None
    _pattern_reliability_cache_ts: datetime | None = None
    _pattern_reliability_cache_ttl_sec: int = 900  # 15 minutes

    2. Add async helper function (after other module-level helpers, before the service class or inside it):

    async def _load_pattern_reliability_weights(
        db_manager: "DatabaseManager",
    ) -> dict[str, float]:
        """Load pattern confidence weights from pattern_reliability table with 15-min cache.

        Returns preloaded dict for injection into frames["pattern_weights"].
        Bootstrap priors (is_bootstrap=true) always included; calibrated weights
        (sample_size >= 30) override bootstrap when available.
        """
        global _pattern_reliability_cache, _pattern_reliability_cache_ts

        now = datetime.now(UTC)
        if (
            _pattern_reliability_cache is not None
            and _pattern_reliability_cache_ts is not None
            and (now - _pattern_reliability_cache_ts).total_seconds() < _pattern_reliability_cache_ttl_sec
        ):
            return _pattern_reliability_cache

        try:
            rows = await db_manager.execute_query("""
                SELECT pattern_name, base_confidence
                FROM pattern_reliability
                WHERE is_bootstrap = true OR sample_size >= 30
            """)
            weights = {r["pattern_name"]: float(r["base_confidence"]) for r in rows}
            _pattern_reliability_cache = weights
            _pattern_reliability_cache_ts = now
            logger.info(f"Pattern reliability weights loaded from DB: {len(weights)} patterns")
            return weights
        except Exception as exc:
            logger.warning(f"Pattern reliability load failed, using fallback: {exc}")
            return {}  # Plugin will use its own fallback_weights

    3. In _process_bar (or wherever the I7 frames dict is built, around line 1044):
       Load weights once per bar, then inject:

       # Phase 42: inject pattern_reliability weights for CandlestickPatternSetup
       pattern_weights = await _load_pattern_reliability_weights(self.db_manager)

       Then add to frames dict:
       frames = {
           "main": df,
           "features": features,
           "indicators": indicators,
           "intelligence": intelligence,
           "i6": i6_payload,
           "pattern_weights": pattern_weights,  # Phase 42: preloaded from pattern_reliability
       }

    NOTE: Only CandlestickPatternSetup (42-03) reads frames["pattern_weights"]; other I7 plugins ignore it.
    DO NOT modify the plugin.compute_full(frames) call — it is already synchronous and correct.
  </action>

  <verify>
    <automated>
      grep -n "_pattern_reliability_cache\|pattern_weights" services/signal_generator_service.py | head -10
    </automated>
  </verify>

  <done>
    - _pattern_reliability_cache, _pattern_reliability_cache_ts, _pattern_reliability_cache_ttl_sec declared
    - _load_pattern_reliability_weights async function added with TTL cache
    - frames["pattern_weights"] injected before plugin.compute_full() call
    - Service logs "Pattern reliability weights loaded from DB: N patterns" on cache refresh
    - plugin.compute_full() call unchanged (still sync, not awaited)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify DB weight injection works end-to-end</name>
  <files>
    services/signal_generator_service.py
    src/intelligence/trading/candlestick_pattern_setup.py
  </files>

  <read_first>
    - services/signal_generator_service.py (frames injection and cache)
  </read_first>

  <action>
    Verify the complete weight injection pipeline:

    1. Check migration applied:
       docker exec timescaledb psql -U postgres -d indicagent -c "SELECT COUNT(*) FROM pattern_reliability WHERE is_bootstrap = true;"
       Expected: 10 rows

    2. Verify frames["pattern_weights"] injection present:
       grep -n "pattern_weights" services/signal_generator_service.py

    3. Restart signal_generator and check logs:
       echo '!123Angelina' | /usr/bin/sudo.ws -S systemctl restart indicagent-signal-generator
       sleep 3
       tail -50 logs/signal_generator.log | grep -i "pattern_reliability\|pattern weights"
       Expected: "Pattern reliability weights loaded from DB: 10 patterns"

    4. Verify pattern_reliability table queried:
       docker exec timescaledb psql -U postgres -d indicagent -c "
       SELECT calls, query FROM pg_stat_statements
       WHERE query LIKE '%pattern_reliability%'
       ORDER BY calls DESC LIMIT 1;"

    5. Verify plugin uses injected weights (not fallback):
       grep -n "fallback_weights" src/intelligence/trading/candlestick_pattern_setup.py
       The fallback should only trigger if frames["pattern_weights"] is empty/missing.
  </action>

  <verify>
    <automated>
      docker exec timescaledb psql -U postgres -d indicagent -t -c "SELECT COUNT(*) FROM pattern_reliability WHERE is_bootstrap = true;" 2>&1 | grep -q "10"
    </automated>
  </verify>

  <done>
    - pattern_reliability table has 10 bootstrap rows
    - frames["pattern_weights"] injection confirmed in code
    - Service logs show DB weights loaded (not fallback path)
    - DB query for pattern_reliability confirmed via pg_stat_statements
  </done>
</task>

</tasks>

<verification>
1. Cache vars declared: grep "_pattern_reliability_cache" services/signal_generator_service.py
2. Injection present: grep '"pattern_weights"' services/signal_generator_service.py
3. DB table: 10 bootstrap priors in pattern_reliability
4. Logs: "Pattern reliability weights loaded from DB: 10 patterns" on restart
5. Plugin sync: grep "async def\|await" src/intelligence/trading/candlestick_pattern_setup.py shows no new async
</verification>

<success_criteria>
- signal_generator_service loads pattern_reliability weights asynchronously with 15-min cache
- Preloaded weights dict injected into frames["pattern_weights"] before I7 plugin loop
- compute_full() remains synchronous — no async/await added to plugin
- DB weights used instead of fallback when available; empty dict triggers plugin fallback on error
- Full feedback loop: bootstrap → patterns fire → outcomes recorded (42-05) → weights calibrated → DB weights injected
</success_criteria>

<output>
After completion, create `.planning/phases/42-candlestick-pattern-expansion/42-04-SUMMARY.md` with DB injection results.
</output>
