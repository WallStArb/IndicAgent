---
phase: 42-candlestick-pattern-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - production/migrations/047_pattern_reliability.sql
  - src/core/database_manager.py
autonomous: true
requirements:
  - CANDLE-02

must_haves:
  truths:
    - "pattern_reliability table exists with PRIMARY KEY on (pattern_name, timeframe)"
    - "Bootstrap priors seeded for all 10 new patterns with literature-based base_confidence values"
    - "is_bootstrap flag distinguishes priors (true) from data-driven weights (false)"
    - "Migration is idempotent (can run multiple times without error)"
  artifacts:
    - path: production/migrations/047_pattern_reliability.sql
      provides: "DB schema for pattern reliability tracking + bootstrap seed data"
      min_lines: 50
    - path: pattern_reliability (table)
      provides: "Pattern confidence weights with statistical tracking"
      contains: "pattern_name, timeframe, base_confidence, sample_size, win_rate, p_value, ic_score, is_bootstrap, last_updated"
  key_links:
    - from: 42-03 (CandlestickPatternSetup I7)
      to: pattern_reliability table
      via: "_load_pattern_weights() query"
      pattern: "SELECT.*base_confidence.*FROM pattern_reliability"
    - from: 42-03 (weight_updater extension)
      to: pattern_reliability table
      via: "calibration UPDATE query"
      pattern: "UPDATE pattern_reliability.*SET base_confidence"
---

<objective>
Create pattern_reliability table with bootstrap priors for database-driven confidence weights.

Purpose: Enable adaptive pattern reliability tracking with literature-based priors, allowing Phase 46 ML analysis to calibrate weights based on live outcomes.
Output: DB table, bootstrap seed data, migration applied
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-candlestick-pattern-expansion/42-CONTEXT.md

# Bootstrap Priors (from 42-CONTEXT.md decisions)

Tier 1 — High Reliability (base_confidence 0.70):
- abandoned_baby_bull: 0.70
- abandoned_baby_bear: 0.70
- kicker_bull: 0.70
- kicker_bear: 0.70

Tier 2 — Moderate Reliability (base_confidence 0.55-0.60):
- harami_bull: 0.60
- harami_bear: 0.60
- tweezer_top: 0.60
- tweezer_bottom: 0.60
- belt_hold_bull: 0.55
- belt_hold_bear: 0.55

# Existing DB Tables (for reference)

cis_weights table structure (from weight_updater.py):
```sql
CREATE TABLE cis_weights (
    version INT,
    weights_type TEXT,
    symbol TEXT,
    timeframe TEXT,
    asset_cluster TEXT,
    trend_w FLOAT,
    momentum_w FLOAT,
    structure_w FLOAT,
    pattern_w FLOAT,
    institutional_w FLOAT,
    regime_w FLOAT,
    sample_size INT
);
```

Migration pattern: Number sequentially after last migration (046)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 047_pattern_reliability.sql with table + seed data</name>
  <files>production/migrations/047_pattern_reliability.sql</files>

  <read_first>
    - production/migrations/046_expire_stale_pending_signals.sql (migration pattern reference)
    - .planning/phases/42-candlestick-pattern-expansion/42-CONTEXT.md (bootstrap priors section)
  </read_first>

  <action>
    Create production/migrations/047_pattern_reliability.sql with:

    -- Migration 047: Pattern Reliability Table
    -- Stores per-pattern confidence weights with bootstrap priors and live calibration
    -- Phase 42: Candlestick Pattern Expansion

    -- Create pattern_reliability table
    CREATE TABLE IF NOT EXISTS pattern_reliability (
        pattern_name TEXT NOT NULL,
        timeframe TEXT NOT NULL,
        base_confidence FLOAT NOT NULL,
        sample_size INTEGER DEFAULT 0,
        win_rate FLOAT,
        p_value FLOAT,
        ic_score FLOAT,
        is_bootstrap BOOLEAN DEFAULT true,
        last_updated TIMESTAMP DEFAULT NOW(),
        PRIMARY KEY (pattern_name, timeframe)
    );

    -- Index for bootstrap queries (used by I7 plugin to load priors)
    CREATE INDEX IF NOT EXISTS idx_pattern_reliability_bootstrap
        ON pattern_reliability(is_bootstrap)
        WHERE is_bootstrap = true;

    -- Index for calibration updates (used by weight_updater)
    CREATE INDEX IF NOT EXISTS idx_pattern_reliability_sample_size
        ON pattern_reliability(sample_size)
        WHERE sample_size >= 30;

    -- Seed literature-based bootstrap priors for 10 new candlestick patterns
    -- Timeframe default: 1m (plugin will query across all TFs in Phase 46)
    INSERT INTO pattern_reliability (pattern_name, timeframe, base_confidence, is_bootstrap) VALUES
        -- Tier 1: High Reliability (0.70)
        ('abandoned_baby_bull', '1m', 0.70, true),
        ('abandoned_baby_bear', '1m', 0.70, true),
        ('kicker_bull', '1m', 0.70, true),
        ('kicker_bear', '1m', 0.70, true),
        -- Tier 2: Moderate Reliability (0.55-0.60)
        ('harami_bull', '1m', 0.60, true),
        ('harami_bear', '1m', 0.60, true),
        ('tweezer_top', '1m', 0.60, true),
        ('tweezer_bottom', '1m', 0.60, true),
        ('belt_hold_bull', '1m', 0.55, true),
        ('belt_hold_bear', '1m', 0.55, true)
    ON CONFLICT (pattern_name, timeframe) DO NOTHING;

    -- Idempotency check: report existing rows if re-run
    DO $$
    BEGIN
        IF EXISTS (SELECT 1 FROM pattern_reliability WHERE is_bootstrap = true) THEN
            RAISE NOTICE 'pattern_reliability: bootstrap priors already exist, skipping seed';
        END IF;
    END $$;

    -- Grant permissions (match existing migration pattern)
    GRANT SELECT ON pattern_reliability TO indicagent_app;
    GRANT UPDATE (sample_size, win_rate, p_value, ic_score, base_confidence, is_bootstrap, last_updated) ON pattern_reliability TO indicagent_app;

    -- Verification query
    SELECT
        pattern_name,
        base_confidence,
        is_bootstrap
    FROM pattern_reliability
    WHERE is_bootstrap = true
    ORDER BY pattern_name;
  </action>

  <verify>
    <automated>
      docker exec timescaledb psql -U postgres -d indicagent -c "\d pattern_reliability" 2>&1 | grep -E "pattern_name|timeframe|base_confidence|is_bootstrap"
    </automated>
  </verify>

  <done>
    - Migration file exists at production/migrations/047_pattern_reliability.sql
    - Table schema includes all required columns
    - 10 bootstrap rows seeded with correct base_confidence values per tier
    - Indexes created on is_bootstrap and sample_size
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply migration and verify table creation</name>
  <files>
    production/migrations/047_pattern_reliability.sql
    src/core/database_manager.py (for connection reference, not modified)
  </files>

  <read_first>
    - production/migrations/047_pattern_reliability.sql
    - src/core/database_manager.py (connection string reference)
  </read_first>

  <action>
    Apply the migration using TimescaleDB docker exec:

    1. Copy migration to container:
       docker cp production/migrations/047_pattern_reliability.sql timescaledb:/tmp/047_pattern_reliability.sql

    2. Execute migration:
       docker exec timescaledb psql -U postgres -d indicagent -f /tmp/047_pattern_reliability.sql

    3. Verify table creation:
       docker exec timescaledb psql -U postgres -d indicagent -c "\d pattern_reliability"

    4. Verify bootstrap seed data:
       docker exec timescaledb psql -U postgres -d indicagent -c "SELECT pattern_name, base_confidence, is_bootstrap FROM pattern_reliability WHERE is_bootstrap = true ORDER BY pattern_name;"

    Expected output: 10 rows with base_confidence 0.55-0.70, all is_bootstrap=true
  </action>

  <verify>
    <automated>
      docker exec timescaledb psql -U postgres -d indicagent -t -c "SELECT COUNT(*) FROM pattern_reliability WHERE is_bootstrap = true;" 2>&1 | grep -q "10"
    </automated>
  </verify>

  <done>
    - pattern_reliability table exists in indicagent database
    - 10 bootstrap priors seeded correctly
    - Indexes idx_pattern_reliability_bootstrap and idx_pattern_reliability_sample_size exist
    - Query returns 10 rows with correct base_confidence values
  </done>
</task>

</tasks>

<verification>
1. Table structure: `\d pattern_reliability` shows all columns with correct types
2. Bootstrap count: `SELECT COUNT(*) FROM pattern_reliability WHERE is_bootstrap = true;` returns 10
3. Tier 1 patterns: `SELECT pattern_name, base_confidence FROM pattern_reliability WHERE pattern_name LIKE '%abandoned%' OR pattern_name LIKE '%kicker%';` shows base_confidence = 0.70
4. Tier 2 patterns: `SELECT pattern_name, base_confidence FROM pattern_reliability WHERE pattern_name LIKE '%harami%' OR pattern_name LIKE '%tweezer%' OR pattern_name LIKE '%belt%';` shows base_confidence 0.55-0.60
</verification>

<success_criteria>
- pattern_reliability table created with PRIMARY KEY (pattern_name, timeframe)
- 10 bootstrap priors seeded with correct literature-based values
- Indexes enable efficient queries for I7 plugin (bootstrap lookup) and weight_updater (calibration updates)
- Migration is idempotent (re-running does not create duplicate rows)
</success_criteria>

<output>
After completion, create `.planning/phases/42-candlestick-pattern-expansion/42-02-SUMMARY.md` with migration results.
</output>
