---
phase: 12-signal-integrity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/intelligence/trading/trend_following.py
  - src/intelligence/trading/momentum_breakout.py
  - src/intelligence/trading/liquidity_hunt.py
  - src/intelligence/trading/mtf_alignment.py
  - src/intelligence/trading/squeeze_expansion.py
  - src/intelligence/trading/mean_reversion.py
  - src/intelligence/trading/vwap_deviation.py
  - src/intelligence/trading/fvg_fill.py
  - src/intelligence/trading/liquidity_sweep_reclaim.py
  - src/intelligence/trading/session_extremes_setup.py
  - src/intelligence/trading/choch_reversal.py
  - src/intelligence/trading/regime_transition.py
  - src/intelligence/trading/divergence_stack.py
  - src/intelligence/trading/pattern_completion.py
  - src/intelligence/trading/gap_analysis_setup.py
  - src/intelligence/trading/candlestick_pattern_setup.py
  - src/intelligence/trading/supply_demand_setup.py
autonomous: true
requirements: [SIGINT-01]

must_haves:
  truths:
    - "Every I7 plugin class has a regime_type attribute with value 'trend', 'mean_reversion', or 'any'"
    - "5 plugins have regime_type='trend', 5 have regime_type='mean_reversion', 7 have regime_type='any'"
    - "No plugin logic is changed — only the class attribute is added"
    - "test_all_i7_plugins_have_regime_type_attribute passes (RED → GREEN)"
  artifacts:
    - path: "src/intelligence/trading/trend_following.py"
      provides: "regime_type = 'trend'"
    - path: "src/intelligence/trading/mean_reversion.py"
      provides: "regime_type = 'mean_reversion'"
    - path: "src/intelligence/trading/choch_reversal.py"
      provides: "regime_type = 'any'"
  key_links:
    - from: "all 17 I7 plugin classes"
      to: "aggregator.aggregate()"
      via: "regime_type attribute read at runtime by aggregator"
      pattern: "regime_type.*=.*['\"]"
---

<objective>
Add `regime_type` class attribute to all 17 I7 plugin dataclasses (SIGINT-01).

Purpose: Self-documenting regime classification at the plugin level. Replaces the opaque REGIME_ELIGIBILITY dict in aggregator.py with an attribute that lives where the plugin is defined. Zero logic change — pure metadata addition.

Output: 17 plugin files each with one new class-level attribute added.
</objective>

<execution_context>
@/home/bg/.claude/get-shit-done/workflows/execute-plan.md
@/home/bg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-signal-integrity/12-CONTEXT.md
@.planning/phases/12-signal-integrity/12-RESEARCH.md

<interfaces>
<!-- Established plugin dataclass pattern — confirmed from trend_following.py -->
```python
# Pattern to follow for ALL I7 plugins:
@dataclass
class TrendFollowingPlugin:
    name: str = "trad_TrendFollowing"
    outputs: frozenset[str] = frozenset({...})
    capability_tags: frozenset[str] = frozenset({"trading", "trend"})
    inputs: list[InputSpec] = (InputSpec(...),)
    regime_type: str = "trend"   # ADD THIS LINE — place after capability_tags or after inputs
    # ... existing numeric/threshold attrs below
    _state: dict = field(default_factory=dict)
```

CRITICAL: `regime_type` must have a DEFAULT VALUE (not just a type annotation) for
dataclass compatibility. Use `regime_type: str = "trend"` (not just `regime_type: str`).
Place BEFORE `_state` field. Position after existing string/frozenset class attrs.

<!-- Full regime map — from CONTEXT.md locked decisions: -->
Trend plugins (regime_type = "trend"):
  - TrendFollowingPlugin        → src/intelligence/trading/trend_following.py
  - MomentumBreakoutPlugin      → src/intelligence/trading/momentum_breakout.py
  - LiquidityHuntPlugin         → src/intelligence/trading/liquidity_hunt.py
  - MTFAlignmentPlugin          → src/intelligence/trading/mtf_alignment.py
  - SqueezeExpansionPlugin      → src/intelligence/trading/squeeze_expansion.py

Mean-reversion plugins (regime_type = "mean_reversion"):
  - MeanReversionPlugin         → src/intelligence/trading/mean_reversion.py
  - VWAPDeviationPlugin         → src/intelligence/trading/vwap_deviation.py
  - FVGFillPlugin               → src/intelligence/trading/fvg_fill.py
  - LiquiditySweepReclaimPlugin → src/intelligence/trading/liquidity_sweep_reclaim.py
  - SessionExtremesSetupPlugin  → src/intelligence/trading/session_extremes_setup.py

Any-regime plugins (regime_type = "any"):
  - CHoCHReversalPlugin         → src/intelligence/trading/choch_reversal.py
  - RegimeTransitionPlugin      → src/intelligence/trading/regime_transition.py
  - DivergenceStackPlugin       → src/intelligence/trading/divergence_stack.py
  - PatternCompletionPlugin     → src/intelligence/trading/pattern_completion.py
  - GapAnalysisSetupPlugin      → src/intelligence/trading/gap_analysis_setup.py
  - CandlestickPatternSetupPlugin → src/intelligence/trading/candlestick_pattern_setup.py
  - SupplyDemandSetupPlugin     → src/intelligence/trading/supply_demand_setup.py
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regime_type to 5 trend plugins</name>
  <files>
    src/intelligence/trading/trend_following.py,
    src/intelligence/trading/momentum_breakout.py,
    src/intelligence/trading/liquidity_hunt.py,
    src/intelligence/trading/mtf_alignment.py,
    src/intelligence/trading/squeeze_expansion.py
  </files>
  <action>
    Read each file. Locate the @dataclass class definition. Add one line:

        regime_type: str = "trend"

    Place after `capability_tags` or `inputs` lines, before any `_state: dict` field.
    Do NOT change any method logic, constants, or other fields.

    For SqueezeExpansion specifically: "trend" is correct — squeeze resolves into sustained
    moves in trending regimes; ranging markets produce false breakouts (from CONTEXT.md decision).
  </action>
  <verify>
    <automated>.venv/bin/python -c "from src.intelligence.trading.trend_following import TrendFollowingPlugin; from src.intelligence.trading.momentum_breakout import MomentumBreakoutPlugin; from src.intelligence.trading.liquidity_hunt import LiquidityHuntPlugin; from src.intelligence.trading.mtf_alignment import MTFAlignmentPlugin; from src.intelligence.trading.squeeze_expansion import SqueezeExpansionPlugin; plugins=[TrendFollowingPlugin(),MomentumBreakoutPlugin(),LiquidityHuntPlugin(),MTFAlignmentPlugin(),SqueezeExpansionPlugin()]; assert all(getattr(p,'regime_type')=='trend' for p in plugins), 'FAIL'; print('OK - all trend plugins regime_type=trend')"</automated>
  </verify>
  <done>All 5 trend plugins have regime_type="trend" attribute with no other code changes.</done>
</task>

<task type="auto">
  <name>Task 2: Add regime_type to 5 mean-reversion plugins + 7 any-regime plugins</name>
  <files>
    src/intelligence/trading/mean_reversion.py,
    src/intelligence/trading/vwap_deviation.py,
    src/intelligence/trading/fvg_fill.py,
    src/intelligence/trading/liquidity_sweep_reclaim.py,
    src/intelligence/trading/session_extremes_setup.py,
    src/intelligence/trading/choch_reversal.py,
    src/intelligence/trading/regime_transition.py,
    src/intelligence/trading/divergence_stack.py,
    src/intelligence/trading/pattern_completion.py,
    src/intelligence/trading/gap_analysis_setup.py,
    src/intelligence/trading/candlestick_pattern_setup.py,
    src/intelligence/trading/supply_demand_setup.py
  </files>
  <action>
    Read each file. Add regime_type to each @dataclass class definition:

    Mean-reversion group (5 plugins):
      mean_reversion.py → MeanReversionPlugin: regime_type: str = "mean_reversion"
      vwap_deviation.py → VWAPDeviationPlugin: regime_type: str = "mean_reversion"
      fvg_fill.py → FVGFillPlugin: regime_type: str = "mean_reversion"
      liquidity_sweep_reclaim.py → LiquiditySweepReclaimPlugin: regime_type: str = "mean_reversion"
      session_extremes_setup.py → SessionExtremesSetupPlugin: regime_type: str = "mean_reversion"

    Any-regime group (7 plugins):
      choch_reversal.py → CHoCHReversalPlugin: regime_type: str = "any"
      regime_transition.py → RegimeTransitionPlugin: regime_type: str = "any"
      divergence_stack.py → DivergenceStackPlugin: regime_type: str = "any"
      pattern_completion.py → PatternCompletionPlugin: regime_type: str = "any"
      gap_analysis_setup.py → GapAnalysisSetupPlugin: regime_type: str = "any"
      candlestick_pattern_setup.py → CandlestickPatternSetupPlugin: regime_type: str = "any"
      supply_demand_setup.py → SupplyDemandSetupPlugin: regime_type: str = "any"

    Place after capability_tags or inputs, before _state field. Zero logic changes.

    Note on any-regime rationale (for code comment if desired):
    - CHoCHReversal, RegimeTransition: fire AT regime transitions — gating on current regime
      would suppress them at the exact moment they should fire
    - DivergenceStack: signals exhaustion/regime change, works in any regime
    - PatternCompletion: reversal patterns fire when current trend is ending
    - GapAnalysisSetup: internal fade/continuation sub-type handles regime implicitly
    - CandlestickPatternSetup: confluence score is its own quality gate
    - SupplyDemandSetup: works in both trending pullback-to-zone and ranging bounce contexts
  </action>
  <verify>
    <automated>.venv/bin/python -c "
from src.intelligence.trading.mean_reversion import MeanReversionPlugin
from src.intelligence.trading.vwap_deviation import VWAPDeviationPlugin
from src.intelligence.trading.fvg_fill import FVGFillPlugin
from src.intelligence.trading.liquidity_sweep_reclaim import LiquiditySweepReclaimPlugin
from src.intelligence.trading.session_extremes_setup import SessionExtremesSetupPlugin
from src.intelligence.trading.choch_reversal import CHoCHReversalPlugin
from src.intelligence.trading.regime_transition import RegimeTransitionPlugin
from src.intelligence.trading.divergence_stack import DivergenceStackPlugin
from src.intelligence.trading.pattern_completion import PatternCompletionPlugin
from src.intelligence.trading.gap_analysis_setup import GapAnalysisSetupPlugin
from src.intelligence.trading.candlestick_pattern_setup import CandlestickPatternSetupPlugin
from src.intelligence.trading.supply_demand_setup import SupplyDemandSetupPlugin

mr_group = [MeanReversionPlugin(), VWAPDeviationPlugin(), FVGFillPlugin(), LiquiditySweepReclaimPlugin(), SessionExtremesSetupPlugin()]
any_group = [CHoCHReversalPlugin(), RegimeTransitionPlugin(), DivergenceStackPlugin(), PatternCompletionPlugin(), GapAnalysisSetupPlugin(), CandlestickPatternSetupPlugin(), SupplyDemandSetupPlugin()]

assert all(p.regime_type == 'mean_reversion' for p in mr_group), 'FAIL mean_reversion group'
assert all(p.regime_type == 'any' for p in any_group), 'FAIL any group'
print('OK - all 12 plugins have correct regime_type')
"</automated>
  </verify>
  <done>
    12 plugin files updated. 5 have regime_type="mean_reversion", 7 have regime_type="any".
    test_all_i7_plugins_have_regime_type_attribute (from Plan 01) now passes GREEN.
    Full unit suite shows no regressions.
  </done>
</task>

</tasks>

<verification>
<automated>.venv/bin/pytest tests/unit/intelligence/test_i7_registration.py -v -m unit</automated>
Full suite: `.venv/bin/pytest tests/unit/ -v -m unit 2>&1 | tail -20`
</verification>

<success_criteria>
- All 17 I7 plugin classes have regime_type attribute with correct value
- test_all_i7_plugins_have_regime_type_attribute passes (RED → GREEN)
- Zero regressions in full unit suite (1083 tests still passing)
- No ruff errors: `.venv/bin/ruff check src/intelligence/trading/`
</success_criteria>

<output>
After completion, create `.planning/phases/12-signal-integrity/12-02-SUMMARY.md`
</output>
