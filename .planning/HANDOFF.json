{
  "version": "1.0",
  "timestamp": "2026-06-17T07:47:53Z",
  "phase": "127",
  "phase_name": "clean-replay-validation",
  "phase_dir": ".planning/phases/127-clean-replay-validation",
  "plan": "lifecycle-replay-fix",
  "task": "lifecycle-replay-fix",
  "total_tasks": 6,
  "status": "paused",
  "completed_tasks": [
    {"id": 1, "name": "Stop lifecycle services + neutralize auto-restart", "status": "done"},
    {"id": 2, "name": "Wipe all signal-derived tables + features (TRUNCATE CASCADE)", "status": "done"},
    {"id": 3, "name": "Regenerate features + signals (--workers 8, no warmup)", "status": "done", "progress": "1,036,513 signals / 1,036,513 frames / 4.9M features / 114 symbols / 0 orphans"}
  ],
  "remaining_tasks": [
    {"id": 4, "name": "Fix lifecycle_replay FK violation (frame_id entry_type mismatch)", "status": "not_started"},
    {"id": 5, "name": "Re-run lifecycle_replay --workers 8", "status": "not_started"},
    {"id": 6, "name": "Restart services + validate", "status": "not_started"}
  ],
  "blockers": [
    {"description": "lifecycle_replay FK violation: insert into trade_executions violates fk_trade_executions_frame on every symbol/tf. Likely entry_type divergence — lifecycle_replay.py:1014,1041 hardcode _make_frame_id(sid,'at_close') but frames may use other entry_types (at_pullback/at_limit/at_reclaim/zone_proximal).", "type": "technical", "workaround": "Confirm with SELECT entry_type,count(*) FROM trade_frames GROUP BY 1; then fix lifecycle to read actual entry_type from the frame."}
  ],
  "human_actions_pending": [],
  "decisions": [
    {"decision": "Full clean-slate wipe (all signal-derived + features)", "rationale": "features were stale — 0/4.2M rows had volume_sma_20/hmm_probability/zone_friction_score/rel_volume", "phase": "127"},
    {"decision": "--workers 8 without --warmup", "rationale": "warmup proven no-op (local caches die between passes); correctness preserved, ~8x faster", "phase": "127"},
    {"decision": "counterfactual_pnl_r left NULL in rebuild", "rationale": "populated by live alpha_swarm CounterfactualEvaluator (Phase 130), not historical replay", "phase": "127"}
  ],
  "uncommitted_files": [".planning/STATE.md", ".planning/phases/127-clean-replay-validation/.continue-here.md", ".planning/todos/pending/2026-06-16-replay-issues-and-findings.md", ".planning/todos/pending/2026-06-16-remove-dead-warmup-flag.md", ".planning/todos/pending/2026-06-16-cleanup-obsolete-replay-scripts.md"],
  "next_action": "Run SELECT entry_type, count(*) FROM trade_frames GROUP BY 1; then read lifecycle_replay.py:1010-1045 to confirm frame_id uses hardcoded 'at_close'. Fix the lookup, re-run lifecycle_replay.py --workers 8, then restore services (remove no-restart drop-ins, start writers + auditors).",
  "context_notes": "Backfill (hardest part, 116 symbols) succeeded overnight. One bug from done. FK violation is systematic/deterministic, not data loss — entry_type in frame_id lookup is prime suspect. Safety gate worked: services stayed down on failed lifecycle, production protected. Sudo: echo '!123Angelina' | /usr/bin/sudo.ws -S <cmd>."
}
