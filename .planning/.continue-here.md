---
context: default
phase: bug-fix-sweep
task: 5
total_tasks: 9
status: in_progress
last_updated: 2026-06-01T23:23:07Z
---

# Bug-Fix Sweep — Resume Here

No blocking constraints discovered this session.

<current_state>
All services healthy and running. llm_writer fixed and draining Kafka backlog (succeeded NOT NULL crash fixed).
DB at max_connections=300. Git clean, main pushed (commit 88f9a57b). Context was exhausted mid-sweep.

5 bugs remain. All are independent — fix the test first, then dispatch 4 parallel agents.
</current_state>

<completed_work>

- **llm_writer crash** (DONE, commit 88f9a57b): `_publish_audit` in `src/core/llm/chain.py` never emitted `succeeded` on success/regular paths. Writer received None, hit NOT NULL on `llm_calls.succeeded`. Fix: emit `succeeded=audit_context.get("succeeded", bool(response))`; writer defaults `_bool("succeeded") or False` for queued messages.
- **Storage cleanup** (DONE): 4.4GB freed — 3.4GB pre-pg18 backup, 772MB dashboard .next, 207MB rotated logs, pycaches, playwright artifacts, 5 stale agent worktrees + branches.
- **max_connections=300** (DONE): `docker compose up -d timescaledb` applied; verified `SHOW max_connections` = 300.
- **Phase audit docs** (DONE, commit 393cf980): stale class names in `src/intelligence/CLAUDE.md` (BaseAIAgent→BaseAIWorker, LLMChain→LiteLLMBackend), stale docstring in `services/lineage_writer.py`.
- **Memory cleanup** (DONE): removed 9 stale memory files; added `project_carryover_defects.md` tracking CR-01 through CR-04.

**Confirmed already fixed (no work needed):**
- CR-01 BaseSwarmCoordinator PascalCase name= — `base_group_service.py:74` has no `name=` arg, already fixed.
- CIS weight loading bug — commit d4714db7, 2026-05-19.
- 19 of 20 previously-failing unit tests — fixed in prior phases; only 1 remains.
</completed_work>

<remaining_work>

### Bug 1 — `test_monthly_energy_roll` (1 failing test)
**File:** `tests/unit/scripts/test_roll_batch.py::TestDetectRolls::test_monthly_energy_roll`
**Symptom:** `detect_rolls(date(2026, 4, 18))` returns `[]` — `"CL" in symbols` fails.
**CL cycle:** All 12 months (`["F","G","H","J","K","M","N","Q","U","V","X","Z"]` in `production/scripts/roll_batch.py:82`).
**Investigation:** Read `get_roll_window` in `src/config/contracts.py` — check if April 18 falls inside CL's roll window. The test uses a hardcoded past date. `get_roll_window` may use the current real date to determine the active contract rather than the passed `today` arg.

### Bug 2 — CR-02: DLQDrain missing `_record_message_consumed`
**File:** `services/dlq_drain.py`
**Fix:** Add `self._record_message_consumed()` call in the message processing loop after each successfully processed message. See other services for the call pattern.

### Bug 3 — CR-03: `ml_signal_training_agent.py` missing `JOB_COMPLETED_TOTAL`
**File:** `services/ml_signal_training_agent.py`
**Fix:** Per D-06 OTel contract (CLAUDE.md), oneshot scripts MUST emit `job_completed_total{job, status}` at script exit. Label `job` must match systemd unit `%n` suffix exactly (`ml-signal-training-materialize`). Add at both success and failure exit paths. See `services/roll_batch.py` for reference pattern.

### Bug 4 — CR-04: FastAPI services WatchdogSec without sd_notify
**Files:** `production/systemd/indicagent-self-healing-agent.service`, `production/systemd/indicagent-config-service.service`
**Fix:** Either add `sd_notify(WATCHDOG=1)` to the service code, or remove `WatchdogSec` from the unit file if the service has no main loop. FastAPI services without a loop should remove WatchdogSec.

### Bug 5 — A-02: `failure_reason` column in `llm_calls`
**Context:** `chain.py._publish_audit` emits `failure_reason` in Kafka payload on structured generation failures but `llm_calls` has no such column — silently dropped. Documented deferral from Phase 094.
**Fix:**
1. Migration: `ALTER TABLE llm_calls ADD COLUMN IF NOT EXISTS failure_reason TEXT;`
2. `services/llm_writer.py`: add `failure_reason` to `_INSERT_LLM_CALL_SQL`, `_parse_params`, `_parsed_to_insert_tuple`
3. Run migration, restart `indicagent-llm-writer`
</remaining_work>

<decisions_made>

- CR-01 already fixed in a prior phase — `base_group_service.py:74` confirmed has no `name=` override.
- CIS weight loading already fixed (commit d4714db7). Memory file was stale and has been removed.
- 20-failing-tests memory was stale — only 1 test fails now. Other 19 fixed in prior phases.
- All 4 session fixes committed to main and pushed to origin.
</decisions_made>

<blockers>
None. All services running. No external dependencies needed.
</blockers>

## Required Reading (in order)
1. `src/config/contracts.py` — `get_roll_window` function, understand boundary condition before fixing test
2. `services/dlq_drain.py` — message processing loop for CR-02
3. `services/ml_signal_training_agent.py` — exit points for CR-03
4. `production/systemd/indicagent-self-healing-agent.service` and `indicagent-config-service.service` — WatchdogSec lines for CR-04

## Infrastructure State
- All 30+ indicagent services: `active running` (timers `inactive dead` between runs = correct)
- DB: TimescaleDB max_connections=300, healthy
- Kafka: Redpanda healthy, llm_writer draining backlog
- Git: main branch, clean, pushed to origin (commit 88f9a57b)

<context>
Session did a full audit + cleanup sweep. Found and fixed the active llm_writer crash (succeeded NOT NULL).
The 5 remaining bugs are independent — after fixing the test, dispatch 4 parallel agents
(one per CR-02, CR-03, CR-04, A-02). Jim Simons principle: zero known failures tolerated.
</context>

<next_action>
1. Read `src/config/contracts.py::get_roll_window` — understand why CL returns empty for date(2026,4,18)
2. Fix the test (update hardcoded date or fix the function boundary)
3. Dispatch 4 parallel agents: CR-02 (dlq_drain), CR-03 (ml_signal_training), CR-04 (WatchdogSec), A-02 (failure_reason migration)
4. Run full test suite, commit all fixes, push
</next_action>
