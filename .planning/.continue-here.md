---
context: default
phase: null
task: null
status: paused
last_updated: "2026-06-05T15:58:42.926Z"
last_updated: 2026-06-02T13:30:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Calibration Design A/B — `confidence_calibrator.py:75` trains on `cis_score AS confidence` but `apply_calibration` in the pipeline receives per-signal plugin `confidence`. These may be different distributions. Spec section 1-A has two branches (Design A = per-signal calibration, Design B = CIS-only calibration). You MUST resolve which design is correct before implementing 1-A or running /gsd-plan-phase. See "Calibration Design Intent" section in the spec.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Spec-before-investigation | Writing a fix spec for 1-A (alpha decay order) without first confirming what the calibration model was trained on. The fix assumed plugin confidence is the calibration input; the training query uses cis_score. | blocking | Resolve Design A/B via query + code read BEFORE planning |
| Arbitrary thresholds | Original spec set quality floor at 0.15 with no basis. Revised to empirical derivation. | advisory | All thresholds must have an empirical derivation query documented in the spec |

<current_state>
Spec v2 written and committed (9cbb049c). 22 findings across IntelligencePipeline (I1-I7) and SignalTracker/LifecycleWriter. All 10 defects from self-review are resolved. One open question (Design A/B calibration) must be answered before /gsd-plan-phase.

This will be Phase 097. Phase 096 (agent-registry) already has 3 PLAN.md files written but not executed — it is independent and can proceed separately or after 097.
</current_state>

<completed_work>
- Signal lifecycle deferred concerns read (CONCERN-02/03/06 + MAE/MFE loss + regime cache cold-start)
- Full intelligence pipeline architectural review — 22 findings in 5 categories (data integrity, architecture, latency, missing capabilities, dead code)
- Simons framing: contamination boundary versioning must precede all logic fixes
- Spec v1 written (80430c78) — covered all 22 findings in 4 phases
- Spec self-review — 10 defects found and fixed in v2 (9cbb049c)
</completed_work>

<remaining_work>
- Resolve Design A vs B calibration question (blocking)
- Run /gsd-plan-phase to generate phase 097 implementation plan
</remaining_work>

<decisions_made>
- One phase for all 22 findings (not split by system)
- Phase 0 = Forensic Boundary Prep — FEATURE_SCHEMA_VERSION=2 (NULL=tainted), CHECKPOINT_VERSION=2 (discard on mismatch = intentional state flush), SIGNAL_SCHEMA_VERSION bump deferred to end of Phase 1
- Quality floor: empirically derived (find confidence bucket where win_rate < 0.45), not hardcoded
- Circuit breakers: enable with threshold=10/timeout=60, not shadow defaults
- Warm-up penalty: sample_size < 30 → perf_multiplier=0.5, not 1.0
- Soft regime gate: confidence *= hmm_prob_in_favorable_regime (hmm_regime_prob confirmed in hmm_regime.py:406)
- Stale carry-forward on deadline miss: non-stateful plugins only (stateful = GARCH/Kalman/HMM would desync)
- signal_ledger_full VIEW must be updated in all migrations
- feature_schema_version added to both intelligence_features AND signal_ledger
</decisions_made>

<blockers>
- Design A/B calibration ambiguity — see blocking constraint above
</blockers>

## Required Reading (in order)
1. `docs/plans/2026-06-02-intelligence-pipeline-signal-integrity.md` — the full spec with all 22 findings and phase breakdown. Read completely before planning.
2. `src/intelligence/ml/confidence_calibrator.py` — specifically lines 60-100 — to resolve Design A/B
3. `src/intelligence/pipeline/calibrator.py` — `apply_calibration` function — to understand the inference path
4. `.planning/signal-lifecycle-review.md` — original signal lifecycle findings for full context

## Infrastructure State
- All services running normally (no changes made this session — spec only)
- Phase 096 (agent-registry) plans written but not executed — independent of this work

## Pre-Execution Critique Required
- Design artifact: `docs/plans/2026-06-02-intelligence-pipeline-signal-integrity.md`
- Critique focus: Design A/B calibration resolution must update spec section 1-A before planning. All other sections are ready.
- Gate: Resolve Design A/B, update spec if needed, THEN run /gsd-plan-phase

<context>
Deep architectural review session. The pipeline has three co-active data corruption bugs running simultaneously. The key Simons insight: you cannot trust any training data generated before the contamination boundary is established. Phase 0 (version markers) ships first as a forensic boundary, then logic fixes follow.

22 findings. 4-phase remediation plan. The spec is rigorous and complete modulo the calibration design question. This should be Phase 097.
</context>

<next_action>
1. Read confidence_calibrator.py:60-100 and calibrator.py apply_calibration function
2. Run: SELECT setup_plugin, cis_score, confidence FROM signal_ledger WHERE cis_score IS NOT NULL LIMIT 100 — compare distributions
3. If cis_score ≈ confidence: Design A confirmed (per-signal calibration, fix is decay order)
4. If cis_score ≠ confidence: Design B — update spec 1-A to route calibration to CIS score only
5. Then: /gsd-plan-phase
</next_action>
