---
phase: ad-hoc-maintenance
task: lifecycle-replay-dual-track
status: in_progress
last_updated: 2026-03-15T00:00:00.000Z
---

<current_state>
Pipeline reset (`pipeline_reset.py --keep-ohlcv --clear-llm`) is still running — replaying the full I1→I7 pipeline for all 60 active instruments across all timeframes. (PL and SOL deactivated 2026-03-14, leaving 60 active.) This was needed to clear corrupt dual-track data caused by an inverted condition bug in lifecycle_replay.py (`mep is not None` should have been `mep is None`).
</current_state>

<completed_work>

- CodeRabbit review run — all 14 findings addressed (13 fixed, 1 intentionally skipped)
- `lifecycle_replay.py`: fixed inverted market entry condition (`mep is not None` → `mep is None`) — critical bug that was skipping ALL 455k historical signals
- `lifecycle_replay.py`: removed dead code, unused imports, unimplemented --resume flag, f-string without placeholders
- `pipeline_reset.py`: fixed `UnboundLocalError: cannot access local variable 'asyncio'` when using `--keep-ohlcv` — caused by redundant local `import asyncio` inside `main()` shadowing module-level import
- `pipeline_reset.py`: added Kafka timeout, sleep after topic deletion, specific exception type check, fallback warning
- `sse.py`: fixed unbounded keyless slot growth (single `__keyless` slot)
- `signal_lifecycle_service.py`: added `self._resolved_market.add(sid)` in finally block
- `dashboard/src/lib/symbol-config.ts`: added `asset_class: "futures"` to all 21 futures in defaultConfig
- `src/intelligence/schemas.py`: fixed I4Context docstring field count (62 → 60)
- `docs/plans/2026-03-14-market-entry-dual-track-design.md`: fixed markdown table + migration 031 schema (all 8 market_entry_* columns)
- `technical_indicators` legacy table: dropped (migration 032), removed from code/tests/docs/todos
- CLAUDE.md: updated pipeline diagram to show I2 (`I2→I6` not `I3→I6`), added i2 to Typed Bus JSONB list
- All commits pushed to main

Recent commits (newest first):
- `976fb9e` cleanup: drop technical_indicators legacy EAV table
- `b70cf2c` fix(pipeline_reset): remove redundant local asyncio import
- `f046c1b` fix: address CodeRabbit review findings

</completed_work>

<remaining_work>

1. **Wait for pipeline_reset.py to finish** — currently running, replaying I1→I7 for all 60 instruments. When done it prints a summary and prompts to restart services. Run:
   ```
   sudo systemctl start indicagent-market-analysis indicagent-feature-writer \
     indicagent-signal-generator indicagent-signal-lifecycle indicagent-ai-narrative
   ```

2. **Run lifecycle_replay.py** — backfill dual-track market entry outcomes for all historical signals:
   ```bash
   .venv/bin/python production/scripts/lifecycle_replay.py --workers 4
   ```

3. **Verify dual-track coverage** after replay:
   ```sql
   SELECT symbol,
     COUNT(*) FILTER (WHERE market_entry_outcome IS NOT NULL) AS market_filled,
     COUNT(*) AS total
   FROM signal_ledger
   GROUP BY symbol ORDER BY symbol;
   ```

4. **Verify SSE fix is working** (broadcaster latest-per-key + earliest offset):
   ```bash
   curl -s "http://localhost:8000/api/sse/events?symbols=ES,NQ&timeframe=1m" --max-time 10 | grep 'event:' | sort | uniq -c
   ```
   Should show intelligence_data, indicator_data, etc. — not just tick_data.

</remaining_work>

<decisions_made>

- `auto_offset_reset="earliest"` for SSE broadcaster is intentional — replays seeded events on startup to populate latest-per-key snapshot (CodeRabbit flagged this but it was kept deliberately)
- Latest-per-key snapshot (not deque) is the correct architecture for stateful intelligence topics
- `technical_indicators` table dropped — was EAV legacy, never written to by any active service
- Pipeline reset with `--keep-ohlcv --clear-llm` was the right call — clears corrupt dual-track data without re-fetching 7yr OHLCV

</decisions_made>

<context>
Markets closed for weekend. No live bar flow. All intelligence data will be seeded from DB on service restart.

The dual-track lifecycle feature tracks two parallel outcome tracks per signal:
- Zone track: waits for price to pull back into the setup zone before activating
- Market track: immediate fill at bar N+1 open (market order simulation)

The lifecycle_replay.py bug (`mep is not None`) was causing ALL historical signals to be skipped for market track processing since historical signals always have NULL market_entry_price before replay.
</context>

<next_action>
1. Wait for pipeline_reset.py to finish (currently running) — paste the final summary output
2. Restart services (commands above)
3. Run lifecycle_replay.py --workers 4
4. Verify dual-track coverage SQL
5. Verify SSE fix
</next_action>
