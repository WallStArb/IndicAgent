---
phase: ad-hoc-debugging
task: in-progress
status: in_progress
last_updated: 2026-03-15T02:46:27.743Z
---

<current_state>
Debugging "no intelligence flowing to UX" — two fixes applied to SSE layer, not yet fully verified. Signal replay is actively running in background (do not truncate signal_ledger until it finishes).
</current_state>

<completed_work>

- Diagnosed root cause of missing intelligence in dashboard SSE snapshot
- Fix 1 (sse.py): Changed `KafkaSSEBroadcaster._snapshot` from `deque(maxlen=30)` per topic → `_latest: dict[str, dict[str, dict]]` (latest-per-key). The deque only held 30 messages but 240+ seeded events were published, so ES/NQ/etc were dropped. New approach stores the most recent message per `(topic, key)` pair — every symbol/TF always in snapshot.
- Fix 2 (main.py): Changed `auto_offset_reset="latest"` → `"earliest"` for SSE broadcaster consumer. Was starting at END of topic after restart, missing all seeded events. Now replays from beginning on startup to populate `_latest` cache.
- Deleted `sse_broadcaster` consumer group so next start reads from offset 0.
- API restarted twice — broadcaster consumer group shows lag=0 (consumed all history), but SSE test still returned only tick_data.

</completed_work>

<remaining_work>

- **Verify SSE fix is working**: `curl -s "http://localhost:8000/api/sse/events?symbols=ES,NQ&timeframe=1m" --max-time 10 | grep 'event:' | sort | uniq -c` — should show intelligence_data, indicator_data, etc., not just tick_data.
- **Root cause of tick-only snapshot unclear**: Broadcaster consumed all messages (lag=0) but SSE still returned only tick_data. Possible issues:
  1. Race condition — client connects before `_broadcaster_task` processes historical messages (broadcaster runs as background asyncio task, client may connect during replay)
  2. Exception in `run()` being swallowed silently — check API logs more carefully
  3. The `_latest` dict might not be populated due to an asyncio ordering issue
- **API log investigation**: `sudo journalctl -u indicagent-api -n 50 --no-pager` — look for errors in broadcaster task or exception stack traces
- **Signal replay**: A separate process is running historical replay for new ETFs WITH lifecycle evaluation. Do NOT truncate `signal_ledger` until this finishes. Monitor: `watch -n 10 'docker exec timescaledb psql -U postgres -d indicagent -c "SELECT status, COUNT(*) FROM signal_ledger GROUP BY status;"'`
- **After signal replay finishes**: Check if signal_ledger looks clean; decide if additional cleanup needed.

</remaining_work>

<decisions_made>

- Latest-per-key snapshot (not deque) is the correct architecture for stateful topics — intelligence/indicator events represent current state per symbol/TF, not a time series of events
- `auto_offset_reset="earliest"` is correct for SSE broadcaster — needs to replay historical seeded events on startup to populate snapshot
- Do NOT truncate signal_ledger while replay is running — the replay is generating properly-labeled historical signals (with lifecycle outcomes)

</decisions_made>

<blockers>

- SSE still returning only tick_data despite fixes — root cause not yet confirmed. Could be asyncio task ordering (broadcaster not finished replaying when first client request arrives) or a silent exception in broadcaster.run(). Need to investigate API logs.
- sudo-rs `-S` stdin password doesn't work for interactive TTY commands — use `echo "!123Angelina" | sudo -S <cmd>` for automation.

</blockers>

<context>
The core intelligence pipeline is healthy — market_analysis_service published 240 seeded events at 23:34, all consumed by sse_broadcaster (lag=0). The issue is that the SSE snapshot isn't being served to clients correctly. The fix logic is right but something in the async task scheduling or exception handling may be preventing `_latest` from being populated before the first client request arrives.

The Hurst validation errors (I4Context "extra inputs not permitted") from earlier in the logs appear to be from an old code run — current schema DOES have hurst_exponent fields. Not a current bug.

Markets closed for weekend — no live bar flow. All intelligence data is seeded from DB (Friday's close).
</context>

<next_action>
1. Check API logs for exceptions: `echo "!123Angelina" | sudo -S journalctl -u indicagent-api -n 100 --no-pager | grep -E "error|Error|Exception|Traceback|broadcaster"`
2. Test SSE endpoint: `curl -s "http://localhost:8000/api/sse/events?symbols=ES,NQ&timeframe=1m" --max-time 10 | grep 'event:' | sort | uniq -c`
3. If still only tick_data: add a startup delay before serving requests (give broadcaster time to replay history), OR move broadcaster replay to happen synchronously before `yield` in lifespan
4. Wait for signal replay to complete before any signal_ledger cleanup
</next_action>
