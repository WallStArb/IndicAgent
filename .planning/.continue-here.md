---
context: default
phase: null
task: null
total_tasks: null
status: in_progress
last_updated: 2026-05-22T11:48:26.640Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: gauge-type-split — `gauge()` in `metrics.py` returns `UpDownCounter` (use `.add(delta)`). `point_gauge()` returns `create_gauge` (use `.set(value)`). Do NOT change `gauge()` — it has many `.add()` callers. Only use `point_gauge()` for new absolute-value metrics.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| gauge-factory-change | Changing `gauge()` from UpDownCounter to Gauge broke intelligence pipeline (21 `.add()` callers) | blocking | Always audit all callsites before changing a shared factory function |
| _state-whack-a-mole | Fixing incremental plugin `_state` issues one-by-one as circuit breakers fire in prod | advisory | The remaining work (startup validator + CI test) eliminates this permanently |

<current_state>
Ad-hoc maintenance session complete. All changes committed and pushed (7cab1b50).
System is clean and running. Two structural prevention tasks remain unstarted.

The recurring `_state` bug in incremental plugins has been fixed for all current plugins,
but there is NO structural enforcement. The next developer who writes an incremental plugin
using the direct pattern (not IncrementalMixin) will repeat the mistake.
</current_state>

<completed_work>

- Fixed 6 I1 plugins missing `_state` in `compute_next()`: historical_volatility, parabolic_sar, stochastic_rsi, aroon, chandelier, donchian (commit 5b9db4e7)
- Fixed `gauge()` vs `point_gauge()` OTel type split — feature_writer health monitor was logging 5000 errors/session (commit 5b9db4e7)
- Marked 25,712 corrupt `intelligence_features` rows (2026-05-21T18:53 → 2026-05-22T11:38) with `ctx.data_quality = "partial_i1_circuit_break_2026-05-21"` (commit 7cab1b50)
- Deleted 39 rotated log files + all empty stub logs (commit 7cab1b50)
- Ran full system health check: all 38 services running, DB fresh, 0 DLQ events
</completed_work>

<remaining_work>

**Task 1: Startup validator** (HIGH PRIORITY — Jim Simons fix)
- File: `src/intelligence/pipeline/executor.py`
- Where: `PluginExecutor.__init__()` after plugins are loaded, OR in `register_plugins.py`
- What: For every plugin where `supports_incremental=True` AND plugin has `compute_next`, call `compute_next()` with a minimal seeded state dict and assert `"_state" in result`. If any fail, raise `RuntimeError(f"{plugin.name}: compute_next violates _state contract")` — pipeline refuses to start.
- Key: Use the mixin's `compute_next` as the reference for what "correct" looks like.

**Task 2: CI unit test** (HIGH PRIORITY)
- File: `tests/unit/intelligence/test_plugin_state_contract.py`
- What: Load all plugins from `TIER_I1`…`TIER_I7` in `register_plugins.py`. For each with `supports_incremental=True`: seed state via `compute_full()` with synthetic OHLCV frames, then call `compute_next()` with that seeded state, assert `"_state" in result` and result is non-empty.
- This catches violations at PR time, not at runtime with corrupted prod data.
</remaining_work>

<decisions_made>

- Marked corrupt rows instead of deleting: 98K signal_ledger rows reference the 16.8-hour window. Deletion causes orphans. Filter with `WHERE ctx->>'data_quality' IS NULL` in ML training / backtests.
- `point_gauge()` separate from `gauge()`: 3 callsites needed `.set()`, ~20+ needed `.add()`. Changing the factory broke the pipeline. New factory preserves backward compat.
- All current incremental plugins now pass the `_state` contract (AST + grep audit confirmed 0 violations).
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `src/intelligence/pipeline/executor.py` — where the `_state` validation runs (line 104-114), where startup validator should be added
2. `src/intelligence/plugins/mixins.py` — `IncrementalMixin.compute_next()` at line 196 — the reference implementation that always attaches `_state`
3. `src/intelligence/register_plugins.py` — `TIER_I1`…`TIER_I7` constants, all registered plugins

## Infrastructure State
- All 38 indicagent systemd services: running
- intelligence-pipeline: restarted at 11:37 UTC, clean (no circuit breakers)
- feature-writer: restarted at 11:38 UTC, clean (no metric errors)
- Corrupt data window: `intelligence_features` rows 2026-05-21T18:53 → 2026-05-22T11:38 marked with `ctx.data_quality` flag

<context>
Session was an ad-hoc health check that found two live bugs:
1. 6 I1 plugins firing circuit breakers every bar since 2026-05-21T18:53 — `compute_next()` missing `_state` in return
2. feature_writer health monitor logging `'_ProxyUpDownCounter' object has no attribute 'set'` every 5 seconds

Both fixed. Root cause identified: no structural enforcement of the `_state` contract.
The mixin enforces it automatically; direct implementors must manually remember it.
Two prevention tasks remain: startup validator (fail-fast) + CI test (fail-at-PR).
</context>

<next_action>
Implement startup validator in `src/intelligence/pipeline/executor.py` or `register_plugins.py`:
iterate all incremental plugins, call `compute_next()` with seeded state, assert `_state` in result.
Fail hard with `RuntimeError` if any plugin violates it — pipeline should refuse to start, not open circuit breakers silently.
Then write `tests/unit/intelligence/test_plugin_state_contract.py`.
</next_action>
